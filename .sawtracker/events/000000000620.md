---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-266.p1 (revision 1): battery: minivm lane — build the prototype and run its 346-case harness (closes the prototypes/ typecheck gap)","created":"1789153516","id":"c1"}],"created":"1789153313","description":"User-ruled Sep 11: prototypes/ is typechecked by NO gate stage (lexdiff/astdiff lex+parse only), which let SL-238.p1 r1's minivm build break escape the suite, the battery and the server gate — codex caught it in review. Add a tracked battery.sh lane that (1) builds prototypes/minivm/src/main.saw with sawc (--module-path sawlex=selfhost/lexer) and (2) runs prototypes/minivm/test_minivm.py against the binary (346 cases, VM + native, ~1-2 min). OUTSIDE the suite lock (not suite-shaped; touches no shared .build state — give it its own build dir under .build/minivm-battery/). Lead-authored small harness patch; harness edits do not make this a compiler branch (per-commit gate policy).","id":"SL-266","labels":["harness","prototype"],"priority":"normal","project":"SL","revision":2,"sequence":620,"stage":"queued","status":"open","title":"battery: minivm lane — build the prototype and run its 346-case harness (closes the prototypes/ typecheck gap)","updated":"1789153516"},"before":{"assignee":"","author":"agent:claude-sawlang","comments":[],"created":"1789153313","description":"User-ruled Sep 11: prototypes/ is typechecked by NO gate stage (lexdiff/astdiff lex+parse only), which let SL-238.p1 r1's minivm build break escape the suite, the battery and the server gate — codex caught it in review. Add a tracked battery.sh lane that (1) builds prototypes/minivm/src/main.saw with sawc (--module-path sawlex=selfhost/lexer) and (2) runs prototypes/minivm/test_minivm.py against the binary (346 cases, VM + native, ~1-2 min). OUTSIDE the suite lock (not suite-shaped; touches no shared .build state — give it its own build dir under .build/minivm-battery/). Lead-authored small harness patch; harness edits do not make this a compiler branch (per-commit gate policy).","id":"SL-266","labels":["harness","prototype"],"priority":"normal","project":"SL","revision":1,"sequence":618,"stage":"queued","status":"open","title":"battery: minivm lane — build the prototype and run its 346-case harness (closes the prototypes/ typecheck gap)","updated":"1789153313"},"id":"SL-266","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/tools/battery.sh b/tools/battery.sh\\nindex a7d5f4a5..459299d1 100755\\n--- a/tools/battery.sh\\n+++ b/tools/battery.sh\\n@@ -78,6 +78,7 @@ STAGES=(\\n     \\\"corodiff|no|coroutine twin parity: suspending vs non-suspending|$PY tools/corodiff.py --quick\\\"\\n     \\\"bench|no|warehouse benchmark: checksums gate, timing reports|__BENCH__\\\"\\n     \\\"selfhostlex|no|the selfhost lexer's own tests compile and pass|__SELFHOSTLEX__\\\"\\n+    \\\"minivm|no|the minivm prototype builds and its differential harness passes|__MINIVM__\\\"\\n     \\\"reemit|yes|two compiles in ONE process emit identical unopt IR, opt IR and object|$PY tools/reemitdiff.py\\\"\\n     \\\"irdet|yes|IR determinism over the WHOLE corpus (not a sample)|__IRDET__\\\"\\n     \\\"gmgate|yes|ownership + concurrency oracles under Guard Malloc (macOS)|$PY tools/gmgate.py\\\"\\n@@ -181,6 +182,21 @@ run_selfhostlex() {\\n     [ \\\"$failed\\\" -eq 0 ]\\n }\\n \\n+# prototypes/minivm was typechecked by NO stage (SL-266, Sep 11: lexdiff/\\n+# astdiff only lex/parse it, and neither the suite nor the server gate ever\\n+# opens it) — SL-238.p1 r1 shipped a minivm build break past every gate, and\\n+# only review caught it. This builds the prototype against the selfhost lexer\\n+# it consumes, then runs its full differential harness (VM + clang-native,\\n+# explicit expected-output oracles). Its own build dir: the lane must never\\n+# clobber a developer's .build/minivm.\\n+run_minivm() {\\n+    mkdir -p .build/minivm-battery\\n+    \\\"$PY\\\" sawc/sawc.py prototypes/minivm/src/main.saw \\\\\\n+        --module-path sawlex=selfhost/lexer \\\\\\n+        -o .build/minivm-battery/minivm || return 1\\n+    \\\"$PY\\\" prototypes/minivm/test_minivm.py --binary .build/minivm-battery/minivm\\n+}\\n+\\n echo \\\"battery: $REPO\\\"\\n echo \\\"battery: python $PY\\\"\\n started=$(date +%s)\\n@@ -206,6 +222,8 @@ for entry in \\\"${STAGES[@]}\\\"; do\\n         run_bench\\n     elif [ \\\"$cmd\\\" = \\\"__SELFHOSTLEX__\\\" ]; then\\n         run_selfhostlex\\n+    elif [ \\\"$cmd\\\" = \\\"__MINIVM__\\\" ]; then\\n+        run_minivm\\n     else\\n         $cmd\\n     fi\\n\",\"id\":\"SL-266\",\"request_id\":\"sl266-p1-attempt1\",\"title\":\"battery: minivm lane — build the prototype and run its 346-case harness (closes the prototypes/ typecheck gap)\"}","request_id":"sl266-p1-attempt1","sequence":620,"timestamp":"1789153516"}
---
