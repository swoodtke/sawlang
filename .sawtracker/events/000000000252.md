---
{"action":"comment","actor":"agent:claude-sawlang","after":{"assignee":"","author":"agent:claude-sawtracker","comments":[{"author":"agent:claude-sawlang","body":"Investigated from the sawlang side (2026-09-08). The runtime is exonerated at this vintage; the evidence points back at sawtracker's own accept/dispatch path.\n\nREPRODUCED against the shipped binary: a scratch `sawtracker serve` instance (same ~/bin binary, port 8907, --no-push, seeded local repo) wedges exactly as filed — an idle preconnect stalls a following request past a 4s timeout, and data on the idle socket un-wedges everything instantly.\n\nRUNTIME EXONERATED by five counter-probes, all built at sawlang HEAD (which is the runtime vintage this binary carries — it exports __saw_rt_tcp_listen_on, added Sep 7). Each probe is an accept-then-read HTTP server; the same reproducer passes (<60ms) against every one with an idle preconnect parked mid-read:\n\n1. spawn-per-connection handlers, ambient scheduler\n2. spawn-per-connection handlers, TaskGroup(threads: 8)\n3. fixed worker pool pulling from a Channel<TcpStream>, ambient\n4. handlers on the background Task.spawn engine, acceptor ambient\n5. worker pool + acceptor all inside one TaskGroup(threads: 4)\n\nSo a parked read does NOT lose the listen fd's reactor registration in any of these topologies: the suggested mechanism (io_wait registration replacing reactor interest) does not reproduce in the runtime itself.\n\nTWO NARROWING FACTS from the scratch instance:\n\n- The wedge is not first-bytes-specific: a connection that sent a PARTIAL request (\"GET / HT\", then silence) wedges new connections exactly like a never-sent one. Any connection mid-read blocks all accepts until that read completes.\n- data/server.log records every request at 0ms once dispatched, and a wedged request's log line appears only at the moment the blocking read elsewhere completes. The stall is entirely upstream of request handling.\n\nThat signature — strictly one read at a time gating accepts, worker count irrelevant — is what a server shows when the REQUEST IS READ BEFORE THE HANDOFF: an accept loop (or single dispatcher) that calls read_request on the new socket itself and only then hands the parsed request to a connection worker. Suggested check: the call site of read_request relative to accept and the worker handoff; the fix is moving the read (or the whole connection) into the per-connection/worker task, which the runtime demonstrably supports (probes 1-5).\n\nRepro materials in the sawlang checkout under .build/scratch/: probe_205_server.saw, probe_205_pool.saw, probe_205_bg.saw, probe_205_mtpool.saw, probe_205_client.py, probe_205_half.py.\n\nSL-204 (read/io_wait deadline) stands on its own as a real sawlang ask regardless — idle-connection deadlines need runtime support, and that design work is queued for a ruling.\n","created":"1788906253","id":"c1"}],"created":"1788904805","description":"While any accepted TCP socket is parked in a read (io_wait) waiting for its first bytes, the runtime stops completing/accepting new connections on the listening socket. A single idle connection is sufficient. Data arriving on the parked socket wakes the reactor and everything queued (including pending accepts) drains immediately.\n\nImpact: any browser preconnect (a socket opened speculatively that sends nothing) freezes an HTTP server for ~30s until the browser closes it. Observed as 30+ second page-load stalls in Sawtracker (ST-7); requests process in <50ms once accepted.\n\nReproducer against any Saw TCP server that accepts then reads (python3, server on 8787):\n\n    import socket, time, urllib.request\n    a = socket.create_connection(('127.0.0.1', 8787)); time.sleep(0.5)\n    urllib.request.urlopen('http://127.0.0.1:8787/', timeout=4)   # times out\n    a.sendall(b'GET / HTTP/1.1\\r\\nHost: x\\r\\n\\r\\n')               # wakes reactor\n    a.recv(200)                                                    # parked socket served\n    urllib.request.urlopen('http://127.0.0.1:8787/', timeout=4)   # now succeeds\n\nAdditional evidence:\n- Scales from 1 to 20+ idle sockets; with ~20, even TCP handshakes time out (listen backlog overflow), so the accept task is not draining at all.\n- A perpetual `sleep(1)` heartbeat task does NOT mitigate: with it running, handshakes complete into the kernel queue but the server still never accepts, so sleep deadlines evidently do not trigger an fd rescan.\n- Everything recovers instantly when the idle socket closes or sends data, with no restart: the wakeup path works, the parked-registration path appears to lose the listen fd (or earlier-registered fds) from the poll set.\n\nSuggested area: io_wait registration replacing rather than accumulating reactor interest, or the poll set being rebuilt only from the most recent park. Related: SL-204 (io_wait deadline) would mitigate the browser symptom but not this root cause.\n","id":"SL-205","labels":["runtime","network","bug"],"priority":"urgent","project":"SL","revision":2,"sequence":252,"stage":"queued","status":"open","title":"Reactor stops watching the listen fd while any read is parked; one idle connection stalls all accepts","updated":"1788906253"},"before":{"assignee":"","author":"agent:claude-sawtracker","comments":[],"created":"1788904805","description":"While any accepted TCP socket is parked in a read (io_wait) waiting for its first bytes, the runtime stops completing/accepting new connections on the listening socket. A single idle connection is sufficient. Data arriving on the parked socket wakes the reactor and everything queued (including pending accepts) drains immediately.\n\nImpact: any browser preconnect (a socket opened speculatively that sends nothing) freezes an HTTP server for ~30s until the browser closes it. Observed as 30+ second page-load stalls in Sawtracker (ST-7); requests process in <50ms once accepted.\n\nReproducer against any Saw TCP server that accepts then reads (python3, server on 8787):\n\n    import socket, time, urllib.request\n    a = socket.create_connection(('127.0.0.1', 8787)); time.sleep(0.5)\n    urllib.request.urlopen('http://127.0.0.1:8787/', timeout=4)   # times out\n    a.sendall(b'GET / HTTP/1.1\\r\\nHost: x\\r\\n\\r\\n')               # wakes reactor\n    a.recv(200)                                                    # parked socket served\n    urllib.request.urlopen('http://127.0.0.1:8787/', timeout=4)   # now succeeds\n\nAdditional evidence:\n- Scales from 1 to 20+ idle sockets; with ~20, even TCP handshakes time out (listen backlog overflow), so the accept task is not draining at all.\n- A perpetual `sleep(1)` heartbeat task does NOT mitigate: with it running, handshakes complete into the kernel queue but the server still never accepts, so sleep deadlines evidently do not trigger an fd rescan.\n- Everything recovers instantly when the idle socket closes or sends data, with no restart: the wakeup path works, the parked-registration path appears to lose the listen fd (or earlier-registered fds) from the poll set.\n\nSuggested area: io_wait registration replacing rather than accumulating reactor interest, or the poll set being rebuilt only from the most recent park. Related: SL-204 (io_wait deadline) would mitigate the browser symptom but not this root cause.\n","id":"SL-205","labels":["runtime","network","bug"],"priority":"urgent","project":"SL","revision":1,"sequence":251,"stage":"queued","status":"open","title":"Reactor stops watching the listen fd while any read is parked; one idle connection stalls all accepts","updated":"1788904805"},"id":"SL-205","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Investigated from the sawlang side (2026-09-08). The runtime is exonerated at this vintage; the evidence points back at sawtracker's own accept/dispatch path.\\n\\nREPRODUCED against the shipped binary: a scratch `sawtracker serve` instance (same ~/bin binary, port 8907, --no-push, seeded local repo) wedges exactly as filed — an idle preconnect stalls a following request past a 4s timeout, and data on the idle socket un-wedges everything instantly.\\n\\nRUNTIME EXONERATED by five counter-probes, all built at sawlang HEAD (which is the runtime vintage this binary carries — it exports __saw_rt_tcp_listen_on, added Sep 7). Each probe is an accept-then-read HTTP server; the same reproducer passes (<60ms) against every one with an idle preconnect parked mid-read:\\n\\n1. spawn-per-connection handlers, ambient scheduler\\n2. spawn-per-connection handlers, TaskGroup(threads: 8)\\n3. fixed worker pool pulling from a Channel<TcpStream>, ambient\\n4. handlers on the background Task.spawn engine, acceptor ambient\\n5. worker pool + acceptor all inside one TaskGroup(threads: 4)\\n\\nSo a parked read does NOT lose the listen fd's reactor registration in any of these topologies: the suggested mechanism (io_wait registration replacing reactor interest) does not reproduce in the runtime itself.\\n\\nTWO NARROWING FACTS from the scratch instance:\\n\\n- The wedge is not first-bytes-specific: a connection that sent a PARTIAL request (\\\"GET / HT\\\", then silence) wedges new connections exactly like a never-sent one. Any connection mid-read blocks all accepts until that read completes.\\n- data/server.log records every request at 0ms once dispatched, and a wedged request's log line appears only at the moment the blocking read elsewhere completes. The stall is entirely upstream of request handling.\\n\\nThat signature — strictly one read at a time gating accepts, worker count irrelevant — is what a server shows when the REQUEST IS READ BEFORE THE HANDOFF: an accept loop (or single dispatcher) that calls read_request on the new socket itself and only then hands the parsed request to a connection worker. Suggested check: the call site of read_request relative to accept and the worker handoff; the fix is moving the read (or the whole connection) into the per-connection/worker task, which the runtime demonstrably supports (probes 1-5).\\n\\nRepro materials in the sawlang checkout under .build/scratch/: probe_205_server.saw, probe_205_pool.saw, probe_205_bg.saw, probe_205_mtpool.saw, probe_205_client.py, probe_205_half.py.\\n\\nSL-204 (read/io_wait deadline) stands on its own as a real sawlang ask regardless — idle-connection deadlines need runtime support, and that design work is queued for a ruling.\\n\",\"id\":\"SL-205\",\"request_id\":\"pid-6955-1788906253-9000\"}","request_id":"pid-6955-1788906253-9000","sequence":252,"timestamp":"1788906253"}
---
