---
{"action":"create","actor":"agent:claude-sl210","after":{"assignee":"","author":"agent:claude-sl210","comments":[],"created":"1788980031","description":"LEAK. Extracting a payload out of a FRESH TEMPORARY, inline, never releases the\nextracted value: the payload's `deinit` does not run and its memory is never\nfreed. Both extraction forms are affected — the Result `try!`/`try?` family and\nthe Optional `!` — while the same extraction out of a BOUND value is correct.\nFound Sep 9 by SL-210 (design 267 unit A) while writing the control row of the\nSL-219 pin; NOT introduced by that unit, and not fixed there (SL-210 is\nbehavior-preserving).\n\n## Measured evidence\n\nCompiles clean, exit 0. `Res` is NoCopy with a printing `deinit`; every value\nbuilt below should print exactly one `drop`.\n\n    struct Res { w: Int }\n    extension Res: NoCopy { func deinit(&var self) { print(\"drop {self.w}\") } }\n    enum Bad { case Nope }\n    extension Bad: NoCopy {}\n\n    func made(n: Int) -> Result<Res, Bad> { Res(w: n) }\n    func maybe(n: Int) -> Res? { Res(w: n) }\n    func plain(n: Int) -> Res { Res(w: n) }\n\n    func main() {\n        print(\"A {(try! made(1)).w}\")   // try! over a temporary\n        print(\"B {maybe(2)!.w}\")        // `!` over a temporary\n        print(\"C {plain(3).w}\")         // a plain temporary, no extraction\n        let d = try? made(4)\n        print(\"D {d!.w}\")               // extraction out of a BOUND value\n        print(\"end\")\n    }\n\nObserved:\n\n    A 1\n    B 2\n    C 3\n    drop 3        <- the plain temporary is released\n    D 4\n    end\n    drop 4        <- the bound value is released\n    (exit 0)\n\n`drop 1` and `drop 2` NEVER RUN. Four values built, two released.\n\n## The class (obligation 4)\n\nThe two rows that leak are exactly the two where a payload is EXTRACTED from a\ntemporary inline; the two that are correct are the ones where either no\nextraction happens (`plain(3).w`) or the container is a named binding whose\nscope registers a cleanup (`let d = ...` then `d!`). So the axis is\nTEMPORARY + EXTRACTION, and it holds across both container kinds — Result and\nOptional — which is what makes it a class rather than a `try` quirk.\n\nMECHANISM, as far as this unit probed it: the extraction hands an owned value\nout of a container temporary, and the statement's temporary bookkeeping tracks\nthe CONTAINER it registered, not the value the extraction produced. The\ncontainer is consumed by the extraction and the payload is adopted by nobody,\nso no cleanup is registered for it on any path. SL-210's decision ledger\nrecords the boundary as expected — `take / adopt-temporary` — so the FRONT half\nis answering correctly and the gap is on the cleanup-registration side, which\nputs it with SL-213 (unit D, the codegen half) rather than with SL-218/SL-219.\nThat attribution is a HYPOTHESIS from the probe matrix, not a read of the\nemission path; whoever picks this up should confirm it before scoping.\n\n## Relationship to the neighbours\n\nNot SL-218 or SL-219, which are the opposite error at the same boundary: those\nare an ALIASING subject judged as a temporary (a double free), this is a\ngenuine temporary whose extracted payload nobody releases (a leak). One is the\nproducer question answered wrong, the other is the cleanup question not asked.\nThey should be swept together — the payload-extraction boundary is one row of\ndesign 267's inventory and both faces of it are wrong — but they are distinct\nfixes.\n\n## Pin\n\nNone added by SL-210: the unit's two pins (V86/V87) are the double-free pair,\nand a leak needs an oracle a `deinit`-counting example cannot give on its own\n(the program exits 0 with the right output). The natural oracle is the `gmgate`\nlane or an explicit built-vs-dropped counter; scoping that is this issue's\nfirst task. The SL-219 pin's CONTROL row names this issue so the control is not\nread as asserting the temporary path is correct.\n","id":"SL-220","labels":["compiler","ownership","bug"],"priority":"high","project":"SL","revision":1,"sequence":292,"stage":"backlog","status":"open","title":"A payload extracted from a fresh temporary inline is never released: `(try! f()).x` and `f()!.x` both leak","updated":"1788980031"},"before":null,"id":"SL-220","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl210\",\"description\":\"LEAK. Extracting a payload out of a FRESH TEMPORARY, inline, never releases the\\nextracted value: the payload's `deinit` does not run and its memory is never\\nfreed. Both extraction forms are affected — the Result `try!`/`try?` family and\\nthe Optional `!` — while the same extraction out of a BOUND value is correct.\\nFound Sep 9 by SL-210 (design 267 unit A) while writing the control row of the\\nSL-219 pin; NOT introduced by that unit, and not fixed there (SL-210 is\\nbehavior-preserving).\\n\\n## Measured evidence\\n\\nCompiles clean, exit 0. `Res` is NoCopy with a printing `deinit`; every value\\nbuilt below should print exactly one `drop`.\\n\\n    struct Res { w: Int }\\n    extension Res: NoCopy { func deinit(&var self) { print(\\\"drop {self.w}\\\") } }\\n    enum Bad { case Nope }\\n    extension Bad: NoCopy {}\\n\\n    func made(n: Int) -> Result<Res, Bad> { Res(w: n) }\\n    func maybe(n: Int) -> Res? { Res(w: n) }\\n    func plain(n: Int) -> Res { Res(w: n) }\\n\\n    func main() {\\n        print(\\\"A {(try! made(1)).w}\\\")   // try! over a temporary\\n        print(\\\"B {maybe(2)!.w}\\\")        // `!` over a temporary\\n        print(\\\"C {plain(3).w}\\\")         // a plain temporary, no extraction\\n        let d = try? made(4)\\n        print(\\\"D {d!.w}\\\")               // extraction out of a BOUND value\\n        print(\\\"end\\\")\\n    }\\n\\nObserved:\\n\\n    A 1\\n    B 2\\n    C 3\\n    drop 3        <- the plain temporary is released\\n    D 4\\n    end\\n    drop 4        <- the bound value is released\\n    (exit 0)\\n\\n`drop 1` and `drop 2` NEVER RUN. Four values built, two released.\\n\\n## The class (obligation 4)\\n\\nThe two rows that leak are exactly the two where a payload is EXTRACTED from a\\ntemporary inline; the two that are correct are the ones where either no\\nextraction happens (`plain(3).w`) or the container is a named binding whose\\nscope registers a cleanup (`let d = ...` then `d!`). So the axis is\\nTEMPORARY + EXTRACTION, and it holds across both container kinds — Result and\\nOptional — which is what makes it a class rather than a `try` quirk.\\n\\nMECHANISM, as far as this unit probed it: the extraction hands an owned value\\nout of a container temporary, and the statement's temporary bookkeeping tracks\\nthe CONTAINER it registered, not the value the extraction produced. The\\ncontainer is consumed by the extraction and the payload is adopted by nobody,\\nso no cleanup is registered for it on any path. SL-210's decision ledger\\nrecords the boundary as expected — `take / adopt-temporary` — so the FRONT half\\nis answering correctly and the gap is on the cleanup-registration side, which\\nputs it with SL-213 (unit D, the codegen half) rather than with SL-218/SL-219.\\nThat attribution is a HYPOTHESIS from the probe matrix, not a read of the\\nemission path; whoever picks this up should confirm it before scoping.\\n\\n## Relationship to the neighbours\\n\\nNot SL-218 or SL-219, which are the opposite error at the same boundary: those\\nare an ALIASING subject judged as a temporary (a double free), this is a\\ngenuine temporary whose extracted payload nobody releases (a leak). One is the\\nproducer question answered wrong, the other is the cleanup question not asked.\\nThey should be swept together — the payload-extraction boundary is one row of\\ndesign 267's inventory and both faces of it are wrong — but they are distinct\\nfixes.\\n\\n## Pin\\n\\nNone added by SL-210: the unit's two pins (V86/V87) are the double-free pair,\\nand a leak needs an oracle a `deinit`-counting example cannot give on its own\\n(the program exits 0 with the right output). The natural oracle is the `gmgate`\\nlane or an explicit built-vs-dropped counter; scoping that is this issue's\\nfirst task. The SL-219 pin's CONTROL row names this issue so the control is not\\nread as asserting the temporary path is correct.\\n\",\"labels\":[\"compiler\",\"ownership\",\"bug\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"pid-10613-1788980031-9000\",\"title\":\"A payload extracted from a fresh temporary inline is never released: `(try! f()).x` and `f()!.x` both leak\"}","request_id":"pid-10613-1788980031-9000","sequence":292,"timestamp":"1788980031"}
---
