---
{"action":"patch_review","actor":"agent:claude-air-sawlang","after":{"acceptance":[],"assignee":"agent:claude-u2a","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-air-sawlang","body":"Review of the brief (claude-air-sawlang), checked against LANGUAGE_SPEC and the lockdown docs. This grammar becomes the single authority for syntax, so gaps in the brief's inventory become gaps in the grammar. Six points:\n\n1. **Two listed constructs do not exist.**\n   - **`loop`.** The spec says \"there is no `loop` keyword (it was dropped as redundant)\", and the reservation is retired (design 55). The infinite loop is a conditionless `while { }`.\n   - **`const` declarations.** Under \"Compile-Time Evaluation\", \"`const func`, macros, and compile-time reflection below are still *planned*\". The only current `const` is the const-generic parameter, `<const N: Int>`. If `const func` goes in, it should be marked `pending`.\n2. **Declarations the list omits:**\n   - `type` aliases (contextual `type`), at module level and as associated types in traits and extensions;\n   - the inline `module m { … }`;\n   - `borrows struct`, `unsafe struct`, and `unsafe static var`;\n   - the visibility modifiers: `public`, `public(package)`, `public(parent)`, and the contextual `private` on fields;\n   - `public import` re-exports;\n   - `static func` methods (design 236), and `consumes` in a method's effect slot, which has a canonical order;\n   - top-level `static_assert`;\n   - `blocking` inside `extern \"C\" { }`.\n3. **Expressions the list omits:**\n   - `lends self`, the borrowing-struct initializer's proof;\n   - prefix `*p` (design 219);\n   - the call-site `&x` and `&var x` sigils, and the `(&x) as UnsafePointer<T>` crossing;\n   - `try(as E.Case) f()` routing (design 234);\n   - `#file`, `#line` and `#function`;\n   - `not`, and the wrapping operators `&+`, `&-` and `&*`, for the precedence table;\n   - a tuple index chain, `t.0.1` (design 161);\n   - `sizeof<T>()` and `alignof<T>()`;\n   - `if var`, `guard var` and `while var`.\n4. **Disambiguation points to add to §6:**\n   - **`{`:** a block, a closure, a Map or Set literal (`{:}`, `{1, 2}`), or an interpolation. `{}` and `{expr}` are always closures.\n   - **The `borrow let|var` prefix's extent and precedence.** SL:borrowing §2.2 says it covers the place \"up to and including its `borrows` call\", and that is a *type* fact, while the tree records syntax only. So the grammar should give the prefix a purely syntactic operand, a postfix expression, and state how it binds against `??`, the binary operators and `=`. Cases: `borrow let doc.section_at(x).get(\"k\") ?? \"\"` and `borrow var v[i].x = borrow let v[j].x`. The split at the last `borrows` call is typecheck's.\n   - **`sizeof`/`alignof` written bare in a generic-argument list** (`Ring<sizeof<UInt64>()>`, DF-307a), and why `1 << 8` cannot appear there (design 185).\n   - **Prefix `*` versus multiplication** (`a * *p`), and a tuple index never taking a following `.`.\n5. **The context list misses the positions design 224 calls \"a container's head\",** which is where many of the Python compiler's position bugs lived:\n   - the `for` head: either range endpoint, or the iterator;\n   - the `if let`/`guard let`/`while let` subject;\n   - the right side of `&&`, `||` and `??`.\n\n   Also missing:\n   - the `borrow` head, the `lend` operand, the `try` subject, and a catch body;\n   - a collection-literal element, a struct-literal field value, and an enum payload argument;\n   - an assignment target, a compound assignment's right side, and a `break` value;\n   - a capture list;\n   - attribute arguments (`@align(N)`, `@test(refuses: \"…\")`);\n   - the const positions: an array length and a repeat count.\n6. **Sources: add SL:architecture §3.1 and §3.2.** Those sections hold the lexer and parser contracts:\n   - the tree records syntax only;\n   - one `Call` node;\n   - flat operator and `else if` lists;\n   - postfix chains nested and charged per hop;\n   - an interpolation as literal segments plus token ranges;\n   - newlines as tokens;\n   - contextual words stay identifiers.\n\n   §6 paraphrases some of these, but the AST node kinds that U2b turns into the dump contract have to follow them.\n","created":"1790346166","id":"c1","kind":"review"},{"author":"agent:claude-sawlang","body":"Scope update (user, Sep 25): the grammar's permanent home is GRAMMAR.md at the repository root, beside LANGUAGE_SPEC.md, for future readers. It is reviewed in the shared doc SL:grammar, then landed through a patch. LANGUAGE_SPEC stays authoritative for meaning, and GRAMMAR.md for syntax, each production citing its spec section.","created":"1790346894","id":"c2"},{"author":"agent:claude-sawlang","body":"Format ruled (user, Sep 25: option 1): GRAMMAR.md is literate Markdown and the single source. Every production is in a fenced ebnf block with a header line (# name status= spec= node=) and a @name annotation per alternative. The notation is defined once in an ebnf-notation fence, and the context and disambiguation tables have fixed schemas. U2b adds the extractor and a lint lane: no unnamed alternative, no undefined or unused nonterminal, no malformed fence, and no table row naming a missing production.","created":"1790347024","id":"c3"},{"author":"agent:claude-air-sawlang","body":"Two follow-ups on the format ruling and the brief:\n\n1. **The lint lane needs two more checks.**\n   - *Names are unique:* two productions or alternatives sharing a `@name` would make every test that cites it ambiguous.\n   - *`spec=` resolves:* each cited section must be a heading in LANGUAGE_SPEC.md, and `status=` must be one of the closed set (`current`, `lockdown`, `retired`, `removed`, `pending`). Otherwise the citations rot as the spec is edited, which is what the stable names exist to prevent.\n2. **The brief still lists `loop` and `const`** (statements; declarations), and none of c1's six points is in it yet. If the U2a agent is already running from this text, please forward c1, or the grammar will define a keyword the spec says does not exist and miss `type` aliases, `borrows struct`, `lends self`, the `borrow` prefix's extent, and the container-head contexts.\n","created":"1790347059","id":"c4"},{"author":"agent:claude-sawlang","body":"Thanks; all of c1 and c4 are forwarded to the U2a agent, which is running. That covers: dropping loop and const declarations (const func stays pending); the added declarations (type aliases, inline module, borrows/unsafe struct, unsafe static var, the visibility forms, public import, static func, the consumes slot order, top-level static_assert, blocking externs); the added expressions (lends self, prefix *, the & / &var sigils and the pointer crossing, try(as E.Case), #file/#line/#function, not and the wrapping operators, tuple-index chains, sizeof/alignof, if/guard/while var); the four disambiguation points (the { forms, the borrow prefix's purely syntactic postfix operand and how it binds, sizeof/alignof in generic arguments, prefix * versus multiplication); design 224's container heads and the other listed contexts; and SL:architecture §3.1 and §3.2 as contract sources. Unique names and a resolving spec= with a closed status set are in the format rules, and U2b's lint lane will enforce them.","created":"1790347119","id":"c5"},{"author":"agent:claude-sawlang","body":"Relayed ruling. The user said, in the lead session on Sep 25: \"i agree with all the recommendations.\"\n\n# SL:grammar rulings, Sep 25 2026\n\nThe user said: \"i agree with all the recommendations.\" The numbers below are SL:grammar r1's §15 question numbers.\n\n## Behaviour changes against today's parser\n- **Q1, prefix operators against `as`:** follow Appendix B. Prefix operators bind tighter than `as`, so `-x as Int8` is `(-x) as Int8` and `~b as UInt64` is `(~b) as UInt64`. The prototype test that expects `~(b as UInt64)` is wrong, and gets fixed at migration (U3).\n- **Q2, the extent of `try`:** `try` sits at the prefix tier and covers its operand only. `try parse_id() as UserId` casts the unwrapped value, as the spec's example reads.\n- **Q3, `??`:** groups right to left, so `a ?? b ?? 0` is `a ?? (b ?? 0)`.\n- **Q4, comparison chains:** refused. `a < b < c` gets a `removed`-style refusal with the fixit `a < b && b < c`.\n- **Q5, `(T)` in a type:** it is grouping. A one-element tuple is `(T,)`, so `((Int) -> Int)?` is an optional function.\n- **Q8, the head reset:** inside a head, brackets, closure bodies and interpolation segments start a fresh level where trailing closures attach again.\n- **Q19, spellings that depend on spacing:**\n  - The lexer keeps longest match.\n  - The parser splits a `>=` (and `>>`, `>>=`) that closes a committed generic list, so `Vector<Int>= v` parses.\n  - A warning, not an error, flags `o!= 5` (a comparison directly after a postfix `!`) and `a&-b` (a wrapping operator directly before a unary minus). The category name is for the warnings work; the grammar records the rule.\n\n## Additions\n- **Q6:** `case None` is a pattern.\n- **Q7 (SL-58):** a boolean `guard cond else { … }`. The `else` block must leave the scope, as with `guard let`.\n- **Q9:** open ranges `..b`, `..=b` and `..` are added. The grammar admits them as general range expressions; the positions that accept them are a later stage's question.\n- **Q16:** a user `panic(\"…\")` gets one rule name of its own, and `@test(panics: …)` gains an optional `text:` slot, as `refuses:` has. A panics test always names a rule. The name is settled in SL:testing; the lead proposes `panic.explicit`.\n\n## Choices where the spec is silent\n- **Q10:** two struct fields or enum cases on one line need a comma between them. `struct P { x: Int y: Int }` is refused.\n- **Q11:** extension heads and a trait's parent list take qualified paths.\n- **Q12:** `borrow` stays contextual, recognized only before `let` or `var`.\n- **Q13:** a lone name in a pattern is a case or a binding by resolution, not by capitalization.\n- **Q14:** `export` declarations are retired in favour of `public import`. They become a `removed` form, with the fixit to use `public import`.\n- **Q15:** there are no implicit members. The spec's prose (`.Less`, `.Monotonic`) is corrected to the qualified spelling.\n- **Q17:**\n  - `@test` may combine with other attributes; the grammar accepts it, and a later stage refuses meaningless combinations.\n  - A test method may not sit inside an ordinary extension.\n  - A `///` comment may not document a test case.\n- **Q18:** there are no borrow bindings in `guard`, `while`, at the top of a `case`, or inside a tuple pattern.\n- **Q20:** identifiers are ASCII only.\n- **Q21:** for the nesting-depth charge, a nested call counts one level, argument list included.\n- **Q22:** `constexpr` is accepted in every effect slot, trait requirements included, and a later stage refuses it on requirements. Function types do not carry it.\n- **Q23:** the grammar accepts generic parameters on trait requirements. Type checking refuses them for now, as today.\n\n## From the U2a report\n- `syntax.pat.refused-qualified-variant` (`case Color.Red`) is confirmed as a `removed` form, with the hint `case Red`.\n- The reference-type position check (`&T` in a disallowed position) moves from the parser to a later stage. Conformance rows R07–R34 are refused there.\n\n## Q24: owed as spec text\nNo ruling was needed. The spec updates are carried on SL-393.\n","created":"1790352757","id":"c6"},{"author":"agent:claude-sawlang","body":"Relayed ruling, Sep 25. This amends Q15 in c6; the user answered \"yes\" to the lead's proposal.\n\nImplicit member expressions are a lockdown addition: `.Case` and `.Case(payload…)`, for enum cases only. They apply only where the expected type is determined, through one typecheck funnel that names its entry points:\n- an annotated `let` or `var`, and an assignment to a typed place;\n- an argument to a non-generic parameter;\n- `return`, and a match arm's result;\n- a field initializer, and a collection literal element;\n- the side of `==` or `!=` opposite a typed operand;\n- the right side of `??`, and a default argument value.\n\nAn optional expected type is looked through. With no expected type, it is an error with the fixit `Enum.Case`. An implicit member never selects an overload; two accepting candidates are an ambiguity error. `Enum.Case` stays valid, and patterns keep the bare `case North`. It is not in the bootstrap subset. The full text is in the rulings file the grammar revision works from.\n","created":"1790352903","id":"c7"},{"author":"agent:claude-air-sawlang","body":"Two grammar-side points on c7's implicit members, for SL:grammar r2. The typecheck-side points (missing positions, what gets looked through, `v.push(.North)`) are in a thread on SL:architecture's funnel text.\n\n1. **`case .North` in a pattern.** c7 keeps patterns bare. Swift readers will write the dotted form, and today it gets no guidance. Make it a `removed` form with the fixit `case North`, beside `case Color.Red` (syntax.pat.refused-qualified-variant).\n2. **Lines that start with `.`**\n   - **Today:** under syntax.rule.operator-continuation, a leading `.` begins a new statement, and every such line is an error. Today's parser says \"Unexpected token: DOT\" for a method chain continued on the next line (`let n = s⏎ .len()`).\n   - **With implicit members:** that line parses as an implicit member. In a tail position it is then looked up on the return type. So the refusal's fixit, `Enum.Case`, points the wrong way for the likeliest cause, a Swift-style chain.\n   - **Suggested fix:** when an implicit member that starts a line fails to resolve, name the chain reading too: \"a method chain continues only inside parentheses\". Also state whether a trailing closure attaches to `.Case`, since syntax.rule.trailing-closure speaks of \"a name or member\".","created":"1790353058","id":"c8"},{"author":"agent:claude-sawlang","body":"Both c8 points are accepted and forwarded to the r2 agent:\n1. `case .North` becomes a removed form with the fixit `case North`.\n2. A line starting with `.` stays a new statement. When the implicit member there fails to resolve, the diagnostic names the chain reading (\"a method chain continues only inside parentheses\") as well as the Enum.Case fixit. The trailing-closure rule treats `.Case` exactly as it treats `Enum.Case`.\nThe typecheck points are in SL:architecture r41 (t35, resolved).","created":"1790353150","id":"c9"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-400.p1 (revision 1): GRAMMAR.md: the formal grammar of Saw, reviewed as SL:grammar r4","created":"1790359558","id":"c10"},{"author":"agent:claude-sawlang","body":"Submitted SL-400.p1. It adds GRAMMAR.md at the repo root, SL:grammar r4 verbatim, and one CLAUDE.md sentence naming it beside LANGUAGE_SPEC.md.\n- r4 applies every ruling here (c6; Q15 in c7 and c8) and your threads t1 to t6, all resolved; nothing is pending.\n- The recognizer's verdicts over all 3451 tracked .saw files match r2's exactly: 3362 accepted.\n- Once landed, GRAMMAR.md is the single source, and edits go by patch.\n- U2b builds the in-tree extractor, generator and lint lane over this file.\nThe path-aware gate runs only the compiler tests for this change.","created":"1790359564","id":"c11"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-400.p1 (revision 1)","created":"1790359581","id":"c12"},{"author":"agent:claude-air-sawlang","body":"Approved patch SL-400.p1 (revision 1)\n\nReview of SL-400.p1 r1 (claude-air-sawlang): **approve**.\n\n- **GRAMMAR.md is SL:grammar r4 byte for byte.** I diffed it against `doc show SL:grammar --revision 4`, the latest revision, and every review thread (t1 to t6) is resolved in that text.\n- **It lints clean on the patch's base.** Checked: 37 fences, 277 productions, 421 named alternatives and 71 rules.\n  - production and alternative names are unique;\n  - every status is from the closed set;\n  - every `spec=` names an existing LANGUAGE_SPEC.md heading;\n  - every nonterminal is defined and used;\n  - every cited `syntax.*` name exists.\n  \n  It also has no trailing whitespace.\n- **CLAUDE.md:** the one sentence is accurate. One optional wording point: SL-400 c2 and GRAMMAR.md's own preamble make GRAMMAR.md authoritative for spelling, and its §15 says the grammar's spelling holds where the spec's text lags. So \"LANGUAGE_SPEC.md is authoritative for meaning, and GRAMMAR.md for spelling\" would keep a reader from treating the spec's older spellings as binding.","created":"1790359611","id":"c13"}],"created":"1790346057","description":"Unit U2a of the self-hosted compiler epic (SL-398): the formal grammar. Its output is a shared document, SL:grammar, drafted locally and published by the lead after review. It touches no repository file, so it runs alongside U1 (SL-399) without conflict.\n\n## Why\n\nThe parser must handle all valid Saw, and its tests must prove it. The plan (user, Sep 25: \"that's exactly what i had in mind\"):\n1. **U2a (this unit):** a formal, machine-readable grammar of the whole language, with a stable name on every production and alternative, and the contexts each construct may appear in.\n2. **U2b:** a generator walks that grammar and emits snippets paired with their expected canonical AST dumps. It adds golden cases for the known tricky points, a negative (mutation) corpus, and a coverage lane that fails when an alternative or a construct-in-context pair goes untested.\n3. **U4 onward:** the parser, written test-first against U2b's corpus.\n\nSo this grammar is the single authority for syntax that the parser, the tests and the spec all cite. It must be complete, precise, and consumable by a program.\n\n## Sources\n\nThe spec is the authority. Where the Python parser differs, the spec wins, and the difference is listed.\n- `LANGUAGE_SPEC.md`, all of it, and the lockdown docs `SL:borrowing` and `SL:testing` (`~/bin/sawtracker doc show SL:borrowing`), which add and change syntax.\n- The rulings that live outside the spec: the SL-393 list (`~/bin/sawtracker show SL-393`), which covers SL-59 statement arms, SL-73 the general postfix call, SL-309 `??` in cast targets (the spec is wrong there), SL-310 trailing closures, SL-45, design 274's owed items, and SL-380's per-hop postfix charging. Also `designs/259-selfhost-parser.md` (rulings R1–R8), `designs/274-parser-track-reconciliation.md` (N1–N12, B1, U5′) and `designs/reviews/parser-census-sep1.md`.\n- The token set: the lexer in `selfhost/lexer/src/lib.saw`. U1 is moving it to `compiler/lex/`; read whichever exists.\n- Evidence of what parses today: `sawc/parser/`. You may also write scratch probes under `.build/scratch/` and compile them with `./.venv/bin/python sawc/sawc.py` (see CLAUDE.md, \"Scratch compilations\"). Evidence only, never authority.\n- The node vocabulary the prototypes used: `prototypes/minivm/M18_AST.md` (a reference, not a constraint).\n\n## Output: `.build/scratch/docs/grammar.md`\n\n1. **Notation.** A small, regular EBNF, defined at the top, that a Python generator can parse: productions, alternatives, optional, zero-or-more, one-or-more, terminals as token kinds or quoted spellings, and a way to attach a name to each alternative. Keep it boring and unambiguous.\n2. **The lexical layer**, briefly:\n   - the token kinds (from the lexer);\n   - where a newline ends a statement, and where it is insignificant (inside `()`, `[]` and committed generic `<>`, design 129; trailing-operator continuation, R3);\n   - interpolation;\n   - doc-comment trivia;\n   - contextual words.\n3. **The grammar**, area by area, covering *everything* the spec and the lockdown docs define:\n   - declarations: `func`, `struct`, `enum` (raw backings), `trait`, `extension`, `init`, `static`, `import` in every form (no `const` declarations; `const func` is planned, so mark it `pending`), plus the additions in the Air review, SL-400 c1, `extern` blocks, and the attributes (`@export`, `@section`, `@align`, `@synthesize`, and every `@test` form);\n   - types: paths and qualifiers, generics and const generics, defaults, `T?`, `&T`/`&var T`, slices `&[T]`, `[T; N]`, tuples and named tuples, function types with effects (`unsafe`, `sync`, `borrows`, `borrows(sync)`, `consumes`), and `any Trait`;\n   - expressions:\n     - a precedence and associativity table;\n     - literals, including interpolated strings, collection literals and repeat literals;\n     - calls with labels, trailing closures, and the general postfix call;\n     - closures and capture lists;\n     - `move`, `try` / `try!` / `try?`, the `catch` forms, `??`, `?.`, `!`, `as` casts, ranges;\n     - `if` and `match` as values;\n     - the `borrow` statement and argument forms;\n   - statements: `let`/`var` (with destructuring), assignment and compound assignment, `return`/`break`/`continue`, `for`/`while` (there is no `loop`: the infinite loop is a conditionless `while`), `guard`, `if let`/`while let`, `match` with statement and expression arms, the `borrow` block, and `lend`;\n   - patterns: literals, ranges, guards, tuples, enum payloads, bindings, and wildcards;\n   - the lockdown forms: `borrow let|var` blocks, the statement and argument forms, `for borrow let|var` (Proposed), `func []` and `func []=`, multi-argument subscripts, the `default:` label, tuple lends, and `borrows(sync)`.\n4. **A stable name on every production and every alternative:** `syntax.<area>.<construct>[.<variant>]`, lowercase, words joined by hyphens (for example `syntax.expr.call.trailing-closure`). Tests will cite these names (SL:testing §6). Each production also carries:\n   - its spec reference (section heading);\n   - its status: `current`, `lockdown` (new or changed by SL:borrowing or SL:testing), `retired` (still parsed but refused later with a hint, such as inline place writes), `removed` (a parse error, such as `#lend_var`), or `pending` (awaiting a ruling);\n   - a preliminary AST node kind, which U2b turns into the dump contract.\n5. **The context table.** For every expression-level and statement-level construct, the contexts it may appear in, and the restrictions in each. Contexts include:\n   - statement, tail (block value), operand, argument, receiver, index;\n   - interpolation, condition (`if`/`while`/`guard`), match scrutinee, match arm body, closure body;\n   - initializer, default parameter value, static initializer, const-generic argument, return value.\n\n   Restrictions include, for example, whether a trailing closure is allowed in a condition. This is the matrix the generator walks. Missed contexts are where most of the Python parser's bugs lived.\n6. **Disambiguation rules.** Each known tricky point, with its ruled resolution and source:\n   - generic `<` versus less-than;\n   - a trailing closure versus a block;\n   - newline significance;\n   - `??` and `?` after `as T?`;\n   - statement versus value tails;\n   - `x = 1 y` (B1, refused);\n   - a leading minus after a block;\n   - struct-init versus call, which is gone: there is one `Call` node;\n   - flat `else if`;\n   - postfix chains charged per hop;\n   - the 256 depth limit and exactly what counts against it.\n7. **Open questions.** Everything the spec leaves unsaid or contradicts, each with the options. Mark the productions that depend on the three pending SL:borrowing rulings (threads t21 to t23; for example `if borrow var x = e` and `case Some(borrow var x)`) as `pending`, with the proposed spelling.\n8. **The one-time check against today's parser.** For each area, note where `sawc/parser/` accepts or refuses something differently from your grammar, with a probe file where you ran one. These become U2b golden cases or open questions.\n\n## Rules\n- Read-only except for your output file and scratch probes under `.build/scratch/`. Do not touch `compiler/`, `selfhost/`, `tools/`, `build.sh` or any tracked file, since U1 is working there. No tracker writes.\n- Do not run the test suite or any battery lane.\n- Style: plain, direct sentences. The EBNF itself should be clean enough that a reader learns the language's syntax from it.\n- Completeness beats polish. When unsure whether something is syntax, include it and mark it.\n\n## Report\n- the doc's size and structure;\n- the production and alternative counts per area;\n- the context-table size;\n- the open questions;\n- the differences from today's parser;\n- anything the lead or the user must rule on.\n","id":"SL-400","labels":["compiler","language"],"order":2,"parent":"SL-398","priority":"normal","project":"SL","queue_order":0,"revision":15,"sequence":2616,"stage":"queued","status":"open","title":"U2a: the formal grammar (SL:grammar): EBNF with stable names, contexts and disambiguation","updated":"1790359611"},"before":{"acceptance":[],"assignee":"agent:claude-u2a","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-air-sawlang","body":"Review of the brief (claude-air-sawlang), checked against LANGUAGE_SPEC and the lockdown docs. This grammar becomes the single authority for syntax, so gaps in the brief's inventory become gaps in the grammar. Six points:\n\n1. **Two listed constructs do not exist.**\n   - **`loop`.** The spec says \"there is no `loop` keyword (it was dropped as redundant)\", and the reservation is retired (design 55). The infinite loop is a conditionless `while { }`.\n   - **`const` declarations.** Under \"Compile-Time Evaluation\", \"`const func`, macros, and compile-time reflection below are still *planned*\". The only current `const` is the const-generic parameter, `<const N: Int>`. If `const func` goes in, it should be marked `pending`.\n2. **Declarations the list omits:**\n   - `type` aliases (contextual `type`), at module level and as associated types in traits and extensions;\n   - the inline `module m { … }`;\n   - `borrows struct`, `unsafe struct`, and `unsafe static var`;\n   - the visibility modifiers: `public`, `public(package)`, `public(parent)`, and the contextual `private` on fields;\n   - `public import` re-exports;\n   - `static func` methods (design 236), and `consumes` in a method's effect slot, which has a canonical order;\n   - top-level `static_assert`;\n   - `blocking` inside `extern \"C\" { }`.\n3. **Expressions the list omits:**\n   - `lends self`, the borrowing-struct initializer's proof;\n   - prefix `*p` (design 219);\n   - the call-site `&x` and `&var x` sigils, and the `(&x) as UnsafePointer<T>` crossing;\n   - `try(as E.Case) f()` routing (design 234);\n   - `#file`, `#line` and `#function`;\n   - `not`, and the wrapping operators `&+`, `&-` and `&*`, for the precedence table;\n   - a tuple index chain, `t.0.1` (design 161);\n   - `sizeof<T>()` and `alignof<T>()`;\n   - `if var`, `guard var` and `while var`.\n4. **Disambiguation points to add to §6:**\n   - **`{`:** a block, a closure, a Map or Set literal (`{:}`, `{1, 2}`), or an interpolation. `{}` and `{expr}` are always closures.\n   - **The `borrow let|var` prefix's extent and precedence.** SL:borrowing §2.2 says it covers the place \"up to and including its `borrows` call\", and that is a *type* fact, while the tree records syntax only. So the grammar should give the prefix a purely syntactic operand, a postfix expression, and state how it binds against `??`, the binary operators and `=`. Cases: `borrow let doc.section_at(x).get(\"k\") ?? \"\"` and `borrow var v[i].x = borrow let v[j].x`. The split at the last `borrows` call is typecheck's.\n   - **`sizeof`/`alignof` written bare in a generic-argument list** (`Ring<sizeof<UInt64>()>`, DF-307a), and why `1 << 8` cannot appear there (design 185).\n   - **Prefix `*` versus multiplication** (`a * *p`), and a tuple index never taking a following `.`.\n5. **The context list misses the positions design 224 calls \"a container's head\",** which is where many of the Python compiler's position bugs lived:\n   - the `for` head: either range endpoint, or the iterator;\n   - the `if let`/`guard let`/`while let` subject;\n   - the right side of `&&`, `||` and `??`.\n\n   Also missing:\n   - the `borrow` head, the `lend` operand, the `try` subject, and a catch body;\n   - a collection-literal element, a struct-literal field value, and an enum payload argument;\n   - an assignment target, a compound assignment's right side, and a `break` value;\n   - a capture list;\n   - attribute arguments (`@align(N)`, `@test(refuses: \"…\")`);\n   - the const positions: an array length and a repeat count.\n6. **Sources: add SL:architecture §3.1 and §3.2.** Those sections hold the lexer and parser contracts:\n   - the tree records syntax only;\n   - one `Call` node;\n   - flat operator and `else if` lists;\n   - postfix chains nested and charged per hop;\n   - an interpolation as literal segments plus token ranges;\n   - newlines as tokens;\n   - contextual words stay identifiers.\n\n   §6 paraphrases some of these, but the AST node kinds that U2b turns into the dump contract have to follow them.\n","created":"1790346166","id":"c1","kind":"review"},{"author":"agent:claude-sawlang","body":"Scope update (user, Sep 25): the grammar's permanent home is GRAMMAR.md at the repository root, beside LANGUAGE_SPEC.md, for future readers. It is reviewed in the shared doc SL:grammar, then landed through a patch. LANGUAGE_SPEC stays authoritative for meaning, and GRAMMAR.md for syntax, each production citing its spec section.","created":"1790346894","id":"c2"},{"author":"agent:claude-sawlang","body":"Format ruled (user, Sep 25: option 1): GRAMMAR.md is literate Markdown and the single source. Every production is in a fenced ebnf block with a header line (# name status= spec= node=) and a @name annotation per alternative. The notation is defined once in an ebnf-notation fence, and the context and disambiguation tables have fixed schemas. U2b adds the extractor and a lint lane: no unnamed alternative, no undefined or unused nonterminal, no malformed fence, and no table row naming a missing production.","created":"1790347024","id":"c3"},{"author":"agent:claude-air-sawlang","body":"Two follow-ups on the format ruling and the brief:\n\n1. **The lint lane needs two more checks.**\n   - *Names are unique:* two productions or alternatives sharing a `@name` would make every test that cites it ambiguous.\n   - *`spec=` resolves:* each cited section must be a heading in LANGUAGE_SPEC.md, and `status=` must be one of the closed set (`current`, `lockdown`, `retired`, `removed`, `pending`). Otherwise the citations rot as the spec is edited, which is what the stable names exist to prevent.\n2. **The brief still lists `loop` and `const`** (statements; declarations), and none of c1's six points is in it yet. If the U2a agent is already running from this text, please forward c1, or the grammar will define a keyword the spec says does not exist and miss `type` aliases, `borrows struct`, `lends self`, the `borrow` prefix's extent, and the container-head contexts.\n","created":"1790347059","id":"c4"},{"author":"agent:claude-sawlang","body":"Thanks; all of c1 and c4 are forwarded to the U2a agent, which is running. That covers: dropping loop and const declarations (const func stays pending); the added declarations (type aliases, inline module, borrows/unsafe struct, unsafe static var, the visibility forms, public import, static func, the consumes slot order, top-level static_assert, blocking externs); the added expressions (lends self, prefix *, the & / &var sigils and the pointer crossing, try(as E.Case), #file/#line/#function, not and the wrapping operators, tuple-index chains, sizeof/alignof, if/guard/while var); the four disambiguation points (the { forms, the borrow prefix's purely syntactic postfix operand and how it binds, sizeof/alignof in generic arguments, prefix * versus multiplication); design 224's container heads and the other listed contexts; and SL:architecture §3.1 and §3.2 as contract sources. Unique names and a resolving spec= with a closed status set are in the format rules, and U2b's lint lane will enforce them.","created":"1790347119","id":"c5"},{"author":"agent:claude-sawlang","body":"Relayed ruling. The user said, in the lead session on Sep 25: \"i agree with all the recommendations.\"\n\n# SL:grammar rulings, Sep 25 2026\n\nThe user said: \"i agree with all the recommendations.\" The numbers below are SL:grammar r1's §15 question numbers.\n\n## Behaviour changes against today's parser\n- **Q1, prefix operators against `as`:** follow Appendix B. Prefix operators bind tighter than `as`, so `-x as Int8` is `(-x) as Int8` and `~b as UInt64` is `(~b) as UInt64`. The prototype test that expects `~(b as UInt64)` is wrong, and gets fixed at migration (U3).\n- **Q2, the extent of `try`:** `try` sits at the prefix tier and covers its operand only. `try parse_id() as UserId` casts the unwrapped value, as the spec's example reads.\n- **Q3, `??`:** groups right to left, so `a ?? b ?? 0` is `a ?? (b ?? 0)`.\n- **Q4, comparison chains:** refused. `a < b < c` gets a `removed`-style refusal with the fixit `a < b && b < c`.\n- **Q5, `(T)` in a type:** it is grouping. A one-element tuple is `(T,)`, so `((Int) -> Int)?` is an optional function.\n- **Q8, the head reset:** inside a head, brackets, closure bodies and interpolation segments start a fresh level where trailing closures attach again.\n- **Q19, spellings that depend on spacing:**\n  - The lexer keeps longest match.\n  - The parser splits a `>=` (and `>>`, `>>=`) that closes a committed generic list, so `Vector<Int>= v` parses.\n  - A warning, not an error, flags `o!= 5` (a comparison directly after a postfix `!`) and `a&-b` (a wrapping operator directly before a unary minus). The category name is for the warnings work; the grammar records the rule.\n\n## Additions\n- **Q6:** `case None` is a pattern.\n- **Q7 (SL-58):** a boolean `guard cond else { … }`. The `else` block must leave the scope, as with `guard let`.\n- **Q9:** open ranges `..b`, `..=b` and `..` are added. The grammar admits them as general range expressions; the positions that accept them are a later stage's question.\n- **Q16:** a user `panic(\"…\")` gets one rule name of its own, and `@test(panics: …)` gains an optional `text:` slot, as `refuses:` has. A panics test always names a rule. The name is settled in SL:testing; the lead proposes `panic.explicit`.\n\n## Choices where the spec is silent\n- **Q10:** two struct fields or enum cases on one line need a comma between them. `struct P { x: Int y: Int }` is refused.\n- **Q11:** extension heads and a trait's parent list take qualified paths.\n- **Q12:** `borrow` stays contextual, recognized only before `let` or `var`.\n- **Q13:** a lone name in a pattern is a case or a binding by resolution, not by capitalization.\n- **Q14:** `export` declarations are retired in favour of `public import`. They become a `removed` form, with the fixit to use `public import`.\n- **Q15:** there are no implicit members. The spec's prose (`.Less`, `.Monotonic`) is corrected to the qualified spelling.\n- **Q17:**\n  - `@test` may combine with other attributes; the grammar accepts it, and a later stage refuses meaningless combinations.\n  - A test method may not sit inside an ordinary extension.\n  - A `///` comment may not document a test case.\n- **Q18:** there are no borrow bindings in `guard`, `while`, at the top of a `case`, or inside a tuple pattern.\n- **Q20:** identifiers are ASCII only.\n- **Q21:** for the nesting-depth charge, a nested call counts one level, argument list included.\n- **Q22:** `constexpr` is accepted in every effect slot, trait requirements included, and a later stage refuses it on requirements. Function types do not carry it.\n- **Q23:** the grammar accepts generic parameters on trait requirements. Type checking refuses them for now, as today.\n\n## From the U2a report\n- `syntax.pat.refused-qualified-variant` (`case Color.Red`) is confirmed as a `removed` form, with the hint `case Red`.\n- The reference-type position check (`&T` in a disallowed position) moves from the parser to a later stage. Conformance rows R07–R34 are refused there.\n\n## Q24: owed as spec text\nNo ruling was needed. The spec updates are carried on SL-393.\n","created":"1790352757","id":"c6"},{"author":"agent:claude-sawlang","body":"Relayed ruling, Sep 25. This amends Q15 in c6; the user answered \"yes\" to the lead's proposal.\n\nImplicit member expressions are a lockdown addition: `.Case` and `.Case(payload…)`, for enum cases only. They apply only where the expected type is determined, through one typecheck funnel that names its entry points:\n- an annotated `let` or `var`, and an assignment to a typed place;\n- an argument to a non-generic parameter;\n- `return`, and a match arm's result;\n- a field initializer, and a collection literal element;\n- the side of `==` or `!=` opposite a typed operand;\n- the right side of `??`, and a default argument value.\n\nAn optional expected type is looked through. With no expected type, it is an error with the fixit `Enum.Case`. An implicit member never selects an overload; two accepting candidates are an ambiguity error. `Enum.Case` stays valid, and patterns keep the bare `case North`. It is not in the bootstrap subset. The full text is in the rulings file the grammar revision works from.\n","created":"1790352903","id":"c7"},{"author":"agent:claude-air-sawlang","body":"Two grammar-side points on c7's implicit members, for SL:grammar r2. The typecheck-side points (missing positions, what gets looked through, `v.push(.North)`) are in a thread on SL:architecture's funnel text.\n\n1. **`case .North` in a pattern.** c7 keeps patterns bare. Swift readers will write the dotted form, and today it gets no guidance. Make it a `removed` form with the fixit `case North`, beside `case Color.Red` (syntax.pat.refused-qualified-variant).\n2. **Lines that start with `.`**\n   - **Today:** under syntax.rule.operator-continuation, a leading `.` begins a new statement, and every such line is an error. Today's parser says \"Unexpected token: DOT\" for a method chain continued on the next line (`let n = s⏎ .len()`).\n   - **With implicit members:** that line parses as an implicit member. In a tail position it is then looked up on the return type. So the refusal's fixit, `Enum.Case`, points the wrong way for the likeliest cause, a Swift-style chain.\n   - **Suggested fix:** when an implicit member that starts a line fails to resolve, name the chain reading too: \"a method chain continues only inside parentheses\". Also state whether a trailing closure attaches to `.Case`, since syntax.rule.trailing-closure speaks of \"a name or member\".","created":"1790353058","id":"c8"},{"author":"agent:claude-sawlang","body":"Both c8 points are accepted and forwarded to the r2 agent:\n1. `case .North` becomes a removed form with the fixit `case North`.\n2. A line starting with `.` stays a new statement. When the implicit member there fails to resolve, the diagnostic names the chain reading (\"a method chain continues only inside parentheses\") as well as the Enum.Case fixit. The trailing-closure rule treats `.Case` exactly as it treats `Enum.Case`.\nThe typecheck points are in SL:architecture r41 (t35, resolved).","created":"1790353150","id":"c9"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-400.p1 (revision 1): GRAMMAR.md: the formal grammar of Saw, reviewed as SL:grammar r4","created":"1790359558","id":"c10"},{"author":"agent:claude-sawlang","body":"Submitted SL-400.p1. It adds GRAMMAR.md at the repo root, SL:grammar r4 verbatim, and one CLAUDE.md sentence naming it beside LANGUAGE_SPEC.md.\n- r4 applies every ruling here (c6; Q15 in c7 and c8) and your threads t1 to t6, all resolved; nothing is pending.\n- The recognizer's verdicts over all 3451 tracked .saw files match r2's exactly: 3362 accepted.\n- Once landed, GRAMMAR.md is the single source, and edits go by patch.\n- U2b builds the in-tree extractor, generator and lint lane over this file.\nThe path-aware gate runs only the compiler tests for this change.","created":"1790359564","id":"c11"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-400.p1 (revision 1)","created":"1790359581","id":"c12"}],"created":"1790346057","description":"Unit U2a of the self-hosted compiler epic (SL-398): the formal grammar. Its output is a shared document, SL:grammar, drafted locally and published by the lead after review. It touches no repository file, so it runs alongside U1 (SL-399) without conflict.\n\n## Why\n\nThe parser must handle all valid Saw, and its tests must prove it. The plan (user, Sep 25: \"that's exactly what i had in mind\"):\n1. **U2a (this unit):** a formal, machine-readable grammar of the whole language, with a stable name on every production and alternative, and the contexts each construct may appear in.\n2. **U2b:** a generator walks that grammar and emits snippets paired with their expected canonical AST dumps. It adds golden cases for the known tricky points, a negative (mutation) corpus, and a coverage lane that fails when an alternative or a construct-in-context pair goes untested.\n3. **U4 onward:** the parser, written test-first against U2b's corpus.\n\nSo this grammar is the single authority for syntax that the parser, the tests and the spec all cite. It must be complete, precise, and consumable by a program.\n\n## Sources\n\nThe spec is the authority. Where the Python parser differs, the spec wins, and the difference is listed.\n- `LANGUAGE_SPEC.md`, all of it, and the lockdown docs `SL:borrowing` and `SL:testing` (`~/bin/sawtracker doc show SL:borrowing`), which add and change syntax.\n- The rulings that live outside the spec: the SL-393 list (`~/bin/sawtracker show SL-393`), which covers SL-59 statement arms, SL-73 the general postfix call, SL-309 `??` in cast targets (the spec is wrong there), SL-310 trailing closures, SL-45, design 274's owed items, and SL-380's per-hop postfix charging. Also `designs/259-selfhost-parser.md` (rulings R1–R8), `designs/274-parser-track-reconciliation.md` (N1–N12, B1, U5′) and `designs/reviews/parser-census-sep1.md`.\n- The token set: the lexer in `selfhost/lexer/src/lib.saw`. U1 is moving it to `compiler/lex/`; read whichever exists.\n- Evidence of what parses today: `sawc/parser/`. You may also write scratch probes under `.build/scratch/` and compile them with `./.venv/bin/python sawc/sawc.py` (see CLAUDE.md, \"Scratch compilations\"). Evidence only, never authority.\n- The node vocabulary the prototypes used: `prototypes/minivm/M18_AST.md` (a reference, not a constraint).\n\n## Output: `.build/scratch/docs/grammar.md`\n\n1. **Notation.** A small, regular EBNF, defined at the top, that a Python generator can parse: productions, alternatives, optional, zero-or-more, one-or-more, terminals as token kinds or quoted spellings, and a way to attach a name to each alternative. Keep it boring and unambiguous.\n2. **The lexical layer**, briefly:\n   - the token kinds (from the lexer);\n   - where a newline ends a statement, and where it is insignificant (inside `()`, `[]` and committed generic `<>`, design 129; trailing-operator continuation, R3);\n   - interpolation;\n   - doc-comment trivia;\n   - contextual words.\n3. **The grammar**, area by area, covering *everything* the spec and the lockdown docs define:\n   - declarations: `func`, `struct`, `enum` (raw backings), `trait`, `extension`, `init`, `static`, `import` in every form (no `const` declarations; `const func` is planned, so mark it `pending`), plus the additions in the Air review, SL-400 c1, `extern` blocks, and the attributes (`@export`, `@section`, `@align`, `@synthesize`, and every `@test` form);\n   - types: paths and qualifiers, generics and const generics, defaults, `T?`, `&T`/`&var T`, slices `&[T]`, `[T; N]`, tuples and named tuples, function types with effects (`unsafe`, `sync`, `borrows`, `borrows(sync)`, `consumes`), and `any Trait`;\n   - expressions:\n     - a precedence and associativity table;\n     - literals, including interpolated strings, collection literals and repeat literals;\n     - calls with labels, trailing closures, and the general postfix call;\n     - closures and capture lists;\n     - `move`, `try` / `try!` / `try?`, the `catch` forms, `??`, `?.`, `!`, `as` casts, ranges;\n     - `if` and `match` as values;\n     - the `borrow` statement and argument forms;\n   - statements: `let`/`var` (with destructuring), assignment and compound assignment, `return`/`break`/`continue`, `for`/`while` (there is no `loop`: the infinite loop is a conditionless `while`), `guard`, `if let`/`while let`, `match` with statement and expression arms, the `borrow` block, and `lend`;\n   - patterns: literals, ranges, guards, tuples, enum payloads, bindings, and wildcards;\n   - the lockdown forms: `borrow let|var` blocks, the statement and argument forms, `for borrow let|var` (Proposed), `func []` and `func []=`, multi-argument subscripts, the `default:` label, tuple lends, and `borrows(sync)`.\n4. **A stable name on every production and every alternative:** `syntax.<area>.<construct>[.<variant>]`, lowercase, words joined by hyphens (for example `syntax.expr.call.trailing-closure`). Tests will cite these names (SL:testing §6). Each production also carries:\n   - its spec reference (section heading);\n   - its status: `current`, `lockdown` (new or changed by SL:borrowing or SL:testing), `retired` (still parsed but refused later with a hint, such as inline place writes), `removed` (a parse error, such as `#lend_var`), or `pending` (awaiting a ruling);\n   - a preliminary AST node kind, which U2b turns into the dump contract.\n5. **The context table.** For every expression-level and statement-level construct, the contexts it may appear in, and the restrictions in each. Contexts include:\n   - statement, tail (block value), operand, argument, receiver, index;\n   - interpolation, condition (`if`/`while`/`guard`), match scrutinee, match arm body, closure body;\n   - initializer, default parameter value, static initializer, const-generic argument, return value.\n\n   Restrictions include, for example, whether a trailing closure is allowed in a condition. This is the matrix the generator walks. Missed contexts are where most of the Python parser's bugs lived.\n6. **Disambiguation rules.** Each known tricky point, with its ruled resolution and source:\n   - generic `<` versus less-than;\n   - a trailing closure versus a block;\n   - newline significance;\n   - `??` and `?` after `as T?`;\n   - statement versus value tails;\n   - `x = 1 y` (B1, refused);\n   - a leading minus after a block;\n   - struct-init versus call, which is gone: there is one `Call` node;\n   - flat `else if`;\n   - postfix chains charged per hop;\n   - the 256 depth limit and exactly what counts against it.\n7. **Open questions.** Everything the spec leaves unsaid or contradicts, each with the options. Mark the productions that depend on the three pending SL:borrowing rulings (threads t21 to t23; for example `if borrow var x = e` and `case Some(borrow var x)`) as `pending`, with the proposed spelling.\n8. **The one-time check against today's parser.** For each area, note where `sawc/parser/` accepts or refuses something differently from your grammar, with a probe file where you ran one. These become U2b golden cases or open questions.\n\n## Rules\n- Read-only except for your output file and scratch probes under `.build/scratch/`. Do not touch `compiler/`, `selfhost/`, `tools/`, `build.sh` or any tracked file, since U1 is working there. No tracker writes.\n- Do not run the test suite or any battery lane.\n- Style: plain, direct sentences. The EBNF itself should be clean enough that a reader learns the language's syntax from it.\n- Completeness beats polish. When unsure whether something is syntax, include it and mark it.\n\n## Report\n- the doc's size and structure;\n- the production and alternative counts per area;\n- the context-table size;\n- the open questions;\n- the differences from today's parser;\n- anything the lead or the user must rule on.\n","id":"SL-400","labels":["compiler","language"],"order":2,"parent":"SL-398","priority":"normal","project":"SL","queue_order":0,"revision":14,"sequence":2615,"stage":"queued","status":"open","title":"U2a: the formal grammar (SL:grammar): EBNF with stable names, contexts and disambiguation","updated":"1790359581"},"id":"SL-400","project":"SL","request":"{\"action\":\"patch_review\",\"actor\":\"agent:claude-air-sawlang\",\"body\":\"Review of SL-400.p1 r1 (claude-air-sawlang): **approve**.\\n\\n- **GRAMMAR.md is SL:grammar r4 byte for byte.** I diffed it against `doc show SL:grammar --revision 4`, the latest revision, and every review thread (t1 to t6) is resolved in that text.\\n- **It lints clean on the patch's base.** Checked: 37 fences, 277 productions, 421 named alternatives and 71 rules.\\n  - production and alternative names are unique;\\n  - every status is from the closed set;\\n  - every `spec=` names an existing LANGUAGE_SPEC.md heading;\\n  - every nonterminal is defined and used;\\n  - every cited `syntax.*` name exists.\\n  \\n  It also has no trailing whitespace.\\n- **CLAUDE.md:** the one sentence is accurate. One optional wording point: SL-400 c2 and GRAMMAR.md's own preamble make GRAMMAR.md authoritative for spelling, and its §15 says the grammar's spelling holds where the spec's text lags. So \\\"LANGUAGE_SPEC.md is authoritative for meaning, and GRAMMAR.md for spelling\\\" would keep a reader from treating the spec's older spellings as binding.\",\"id\":\"SL-400.p1\",\"request_id\":\"pid-99672-1790359611-8000\",\"verdict\":\"approve\"}","request_id":"pid-99672-1790359611-8000","sequence":2616,"timestamp":"1790359611"}
---
