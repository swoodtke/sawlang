---
{"action":"patch_revise","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl204","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"RULINGS (user, Sep 12, via the design-272 decision list): D3-1 APPROVED — Signal = {Terminate, Interrupt, Hangup, Quit, User1, User2, WindowChanged}; SIGCHLD OUT (load-bearing: waitpid reaping depends on its default disposition). D3-3 APPROVED — SIGPIPE is suppressed PER SOCKET (SO_NOSIGPIPE macOS / MSG_NOSIGNAL Linux via unit 2's option op); EPIPE surfaces as an ordinary IoError Result; no process-wide disposition change; not a Signal case. Lead-endorsed as determined: D3-2 (double-watch errors; fan-out = one watch + a Channel), D3-4 (ordinary io park, budget reset, anyio covers the quiescent walk), D3-5 (reuse IoError), D3-6 (next() -> Result<Void, IoError>, coalescing documented).","created":"1789214589","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-228.p1 (revision 1): design 272 unit 3: std.signal — signals as suspending reactor events, graceful shutdown rides cancellation, no ABI change (SL-228)","created":"1789230899","id":"c2"},{"author":"agent:claude-sawlang","body":"SL-228.p1 SUBMITTED (design 272 unit 3, delta of trio commit a9d809a5, applies clean on merged units 1+2), server gate queued. Per rulings D3-1..6: Signal = {Terminate, Interrupt, Hangup, Quit, User1, User2, WindowChanged} (SIGCHLD out — waitpid reaping is load-bearing on its default disposition); watch(sig) -> Result handle, next() SUSPENDS on the reactor like a socket read; double-watch errors (fan-out = one watch + a Channel); next() -> Result<Void, IoError>, coalescing documented; NO exit registry — the K100 pattern is watch -> cancel -> deinits -> main returns; process.exit stays the no-cleanup hatch. SIGPIPE deliberately NOT a Signal case (per-socket suppression landed in unit 2, argued in the module header). MECHANISM NOTE for the reviewer: departs from the issue's kqueue/signalfd sketch for a load-bearing reason — signalfd requires the signal blocked in EVERY thread and a pre-existing thread cannot be retro-masked (a process-directed signal delivered there takes the default action and kills the process); delivery instead rides a self-pipe read end the EXISTING (fd, write, token) reactor seam already carries — NO frozen-seam change, argument recorded in rt/ABI.md. Handler-side send uses Signal.send_to_self() (a METHOD, which mangles) after SL-274's bare-symbol hazard bit a std free func named raise. Conformance K98-K100. Trio-wide terminal battery was 27/27 at the branch tip.","created":"1789230910","id":"c3"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-228.p1 (revision 1)\n\nRequest changes on revision 1 (base 78d328de, applied 178c64e8): two independently reproduced concurrency bugs in the signal bridge.\n\n1. [P1] Serialize watch ownership and lifecycle (`sawc/rt/shim.c:400–428`). The lazy initialization, already-watched check, descriptor publication and sigaction installation are unsynchronized. Two threads can both pass the check and both return a successful watch for one signal, violating AlreadyExists and the single-owner contract. Their descriptor and saved-disposition writes overwrite one another; one pipe becomes unreachable, and a saved “previous” disposition can be Saw's own handler. Dropping either handle can then invalidate the other or restore the wrong disposition. `volatile sig_atomic_t` does not serialize this transaction.\n\n   Reproduced with the exact patched shim: preinitialize with a different signal, then gate two callers at pipe() so both have passed the availability check before either publishes. Both SIGUSR1 watches succeed (`watch results: 5 3`) instead of exactly one returning -2. Harness: `/Users/swoodtke/Projects/sl228-review-r1/.build/scratch/watch_race.c`. Initialization, acquisition and teardown need coordinated synchronization, with a concurrent same-signal regression test.\n\n2. [P1] Quiesce in-flight handlers before reclaiming their pipe (`sawc/rt/shim.c:444–450`, handler fd load/write at 360–363). Restoring the disposition prevents future handler entries but does not wait for a handler already running on another thread. Such a handler can have loaded the old write descriptor before unwatch clears the slot and closes the pipe. If another pipe or file reuses that number, the delayed handler writes its notification byte into the unrelated resource. A mutex around watch/unwatch alone will not fix this handler-versus-close race.\n\n   Deterministic reproduction against the patched source: a scheduling hook pauses the actual signal handler immediately before write(), after it has loaded the descriptor; another thread unwatches and opens an unrelated pipe that reuses the descriptors; resume the handler. Output: `unrelated pipe received 1 byte, value=1`. Harness: `/Users/swoodtke/Projects/sl228-review-r1/.build/scratch/signal_close_race.c` (build with `clang -std=c11 -pthread -Wl,-dead_strip .build/scratch/signal_close_race.c -o .build/scratch/signal_close_race`). Please establish a signal-safe reclamation protocol and pin teardown concurrent with delivery and descriptor reuse.\n\nValidation: all four focused K98/K99/K100/hosted-only fixtures pass, as do the ABI documentation, citation and diff checks. These passing fixtures do not cover concurrent acquisition or in-flight delivery during teardown. Server tests were still pending when the review was prepared.\n","created":"1789232871","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-228.p1 (revision 1)","created":"1789234160","id":"c5"},{"author":"agent:claude-sawlang","body":"Revised patch SL-228.p1 to revision 2","created":"1789235202","id":"c6"},{"author":"agent:claude-sawlang","body":"SL-228.p1 r2 SUBMITTED (fix round for review r1's two shim P1s; commit 2e8a9859 as the unit-3 delta). P1a: one pthread_mutex spans the whole watch/unwatch transaction — the HANDLER stays lock-free by necessity (it can interrupt a thread inside the mutex; the constraint is now written at the top of the block). P1b: reclamation made IMPOSSIBLE rather than synchronized — the pipe is created at first watch and lives for the process; unwatch restores disposition and drains, closes nothing (two fds per watched signal, 14 worst case, 0 unwatched). The token-validation alternative was REJECTED on its own terms: a handler that validates-then-writes is the same TOCTOU in a smaller font; its useful half survives inverted — each watch bumps a generation, the handler STAMPS its byte, and the DRAIN (reader-side, race-free) validates, so a stale byte from a previous watch can never fake a delivery into a shutdown path. Both races reproduced pre-fix with independently-written harnesses landing on the reviewer's exact numbers (results 5 3; unrelated pipe received value=1, fds 3/4 reused); pinned by tools/signal_race_probe.c linking the REAL shim, run via examples/signal_concurrency_pins.saw in the ordinary suite — three cells fail against the pre-fix shim, all pass now. Obligation-4 sweep cleared every other shim transaction with reasons (the offload thunk shares the publish-then-write shape and is safe only via pthread_join-before-close — the contrast argued in place); filed SL-276 (thread_spawn discards pthread_create failure — different class). Brief mechanism section + rt/ABI.md rewritten to the shipped self-pipe design. Re-ran: suite 2503/11x, freestanding 36/36, abidoc exact, citations — full battery not re-run (no compiler code in the delta; r1's 27/27 covered unit 3's shape), stated per the mandate.","created":"1789235219","id":"c7"},{"author":"agent:sawtracker-tests","body":"Tests failed for SL-228.p1 (revision 2):\n\n```\n[2482/2514] ✓ vector_with_ref_oob_panic\n[2483/2514] ✓ while_let_tuple_over_suspension_refused\n[2484/2514] ✓ while_never_freestanding\n(2505/2514) · while_nested\n[2485/2514] ✓ vector_with_var_ref_oob_panic\n[2486/2514] ✓ version_semver_sort\n(2506/2514) · while_never_diverges\n[2487/2514] ✓ vis258_private_is_an_ordinary_identifier\n(2507/2514) · while_never_break_forms\n[2488/2514] ✓ vis258_field_inherits_public\n(2508/2514) · while_let_suspending_mt\n(2509/2514) · while_let_suspending_try_catch\n(2510/2514) · while_simple\n(2511/2514) · wrapping_operators\n(2512/2514) · while_let_suspending\n(2513/2514) · wrapping_and_reference_mix\n[2489/2514] ✓ vis80_public_members_ok\n[2490/2514] ✓ vis80_same_module_ok\n[2491/2514] ✓ visibility_package_access\n(2514/2514) · while_never_sync_context\n[2492/2514] ✓ visibility_package\n[2493/2514] ✓ visibility_parent\n[2494/2514] ✓ visibility_parent_public_works\n[2495/2514] ✓ void_match_in_closure\n[2496/2514] ✓ visibility_public\n\nCompiles complete in 395.3s (0/2514 reused, 0%) — 930 settled without running, 1584 binaries queued for execution (1566 of them already run).\n\n[2497/2514] ✓ while_break_continue\n[2498/2514] ✓ while_expr_conditional_found\n[2499/2514] ✓ void_if_chain_tail\n[2500/2514] ✓ while_expr_infinite\n[2501/2514] ✓ while_expr_conditional_not_found\n[2502/2514] ✓ while_infinite\n[2503/2514] ✓ while_let_drain\n[2504/2514] ✓ while_let_channel_drain\n[2505/2514] ✓ while_nested\n[2506/2514] ✓ while_never_diverges\n[2507/2514] ✓ while_never_break_forms\n[2508/2514] ✓ while_let_suspending_mt\n[2509/2514] ✓ while_let_suspending_try_catch\n[2510/2514] ✓ while_simple\n[2511/2514] ✓ wrapping_operators\n[2512/2514] ✓ while_let_suspending\n[2513/2514] ✓ wrapping_and_reference_mix\n[2514/2514] ✓ while_never_sync_context\n\ncompile 395.3s + 4.4s draining = 399.6s total\n\n======================================================================\nTest Results\n======================================================================\n\nFAILED TESTS:\n\n  ✗ signal_concurrency_pins\n    Output should contain 'signal race probe: all cells hold' — missing\n    Got:\n    BUILD FAILED (exit 1):\n    ld: open() failed, errno=2 (No such file or directory) for '/Users/shawn/Projects/sawtracker-production/data/worktrees/SL-228.p1.test/.build/scratch/signal_race_probe_suite'\n    clang: error: linker command failed with exit code 1 (use -v to see invocation)\n    \n    \n\nKNOWN FAILURES (xfail):\n  x V96_driven_capture_duplicates_once: SL-267 — a driven body's NON-ESCAPING closure capture duplicates\n  x closure_escaping_move_capture_consumed_once: DF-255a an escaping closure's env destructor releases a capture the\n  x closure_nested_ref_param_capture: DF-248b a nested closure captures an enclosing closure's reference\n  x closure_shorthand_parameter_inside_a_try: DF-257d the `$0` shorthand is invisible to the implicit-parameter scan\n  x coro_suspending_call_in_inline_catch: SL-215 the inline `try EXPR catch { }` catch block does not CFG-split,\n  x coro_value_inline_catch_hosts_a_move: SL-231 a value-carrying INLINE catch cannot host a `move` of a frame\n  x funcpointer_call_in_a_driven_body: DF-252a a `FuncPointer` value called by NAME inside a driven body is\n  x generic_init_moved_parameter_is_released_once: DF-302b — a generic extension's `init` releases a parameter it MOVED\n  x match_on_suspending_call_in_a_loop: SL-273 — a match arm PAYLOAD BINDING is lost when a SUSPENSION\n  x primitive_type_called_as_a_function: DF-270e — a PRIMITIVE TYPE NAME written as a ZERO-ARGUMENT call is an\n  x trailing_closure_inside_a_try_operand: DF-259c — a trailing closure is not recognized inside a `try`/`try!`/\n\n======================================================================\nSOME TESTS FAILED (2502 passed, 11 xfailed, 1 failed)\n======================================================================\n\nPublished test_runner_1789235204_36739 as test_runner_last (1542 manifest entries).\n\n```","created":"1789235605","id":"c8"},{"author":"agent:claude-sawlang","body":"Revised patch SL-228.p1 to revision 3","created":"1789235774","id":"c9"}],"created":"1789050299","description":"MOTIVATION: a hosted Saw process cannot intercept SIGTERM/SIGINT — the sawtracker server dies with no flush, no socket drain, and stale lockfiles. The C idioms (signal(2) callbacks, atexit) fight deterministic destruction, the cooperative runtime, and never-hide-errors.\n\nRULED DESIGN (user, Sep 10 2026):\n1. SURFACE = SUSPENDING WATCH, no callbacks anywhere: hosted-only std.signal; watch(.terminate) returns a watch handle (Result — registration can fail; never hide errors) whose next() SUSPENDS until delivery, parking the task on the reactor exactly like a socket read. Watching a signal replaces its default disposition; unwatched signals keep OS behavior; SIGKILL/SIGSTOP are inexpressible.\n2. NO EXIT REGISTRY: cleanup rides cancellation + deinits. The pattern is a shutdown task that watches the signal, cancels the rest of the work (cancel already wakes io-parked tasks, design 102), values deinit deterministically as tasks unwind, main returns normally. process.exit(code) is documented as the no-cleanup escape hatch (no unwinding mid-flight).\n3. SCHEDULING: paired with SL-204 (io_wait deadline) as the server-hardening pair at the queue tail, behind the current soundness pipeline.\n\nMECHANISM SKETCH: both hosts integrate natively with the design-117 instance-based Saw reactor — kqueue EVFILT_SIGNAL (macOS; set SIG_IGN so the default action never fires) and signalfd (Linux; pthread_sigmask block, including at Thread/TaskGroup worker spawn — shim change). shim.c grows sigaction/sigprocmask-family wrappers (FFI-blocked bodies). Check whether any __saw_rt_* seam addition is needed or the reactor side suffices — rt/ABI.md is frozen, abidoc lane gates any change.\n\nBRIEF-TIME SCOPING (open, for the designs/NN brief at dispatch): the curated Signal enum membership + spellings (no-abbreviations doctrine applies); double-watch policy (error at second watch vs broadcast); whether the hosted runtime should default-ignore SIGPIPE for servers; interaction with op-budget fairness while a watcher task parks; freestanding simply lacks the module (like std.net). Obligation-2 consumer sweep: who relies on default signal dispositions today (test_runner child processes, blade, the QEMU harnesses).","id":"SL-228","labels":["feature","stdlib"],"priority":"normal","project":"SL","revision":11,"sequence":731,"stage":"queued","status":"open","title":"std.signal: signals as suspending reactor events; graceful shutdown rides cancellation (no atexit registry)","updated":"1789235774"},"before":{"assignee":"agent:claude-sl204","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"RULINGS (user, Sep 12, via the design-272 decision list): D3-1 APPROVED — Signal = {Terminate, Interrupt, Hangup, Quit, User1, User2, WindowChanged}; SIGCHLD OUT (load-bearing: waitpid reaping depends on its default disposition). D3-3 APPROVED — SIGPIPE is suppressed PER SOCKET (SO_NOSIGPIPE macOS / MSG_NOSIGNAL Linux via unit 2's option op); EPIPE surfaces as an ordinary IoError Result; no process-wide disposition change; not a Signal case. Lead-endorsed as determined: D3-2 (double-watch errors; fan-out = one watch + a Channel), D3-4 (ordinary io park, budget reset, anyio covers the quiescent walk), D3-5 (reuse IoError), D3-6 (next() -> Result<Void, IoError>, coalescing documented).","created":"1789214589","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-228.p1 (revision 1): design 272 unit 3: std.signal — signals as suspending reactor events, graceful shutdown rides cancellation, no ABI change (SL-228)","created":"1789230899","id":"c2"},{"author":"agent:claude-sawlang","body":"SL-228.p1 SUBMITTED (design 272 unit 3, delta of trio commit a9d809a5, applies clean on merged units 1+2), server gate queued. Per rulings D3-1..6: Signal = {Terminate, Interrupt, Hangup, Quit, User1, User2, WindowChanged} (SIGCHLD out — waitpid reaping is load-bearing on its default disposition); watch(sig) -> Result handle, next() SUSPENDS on the reactor like a socket read; double-watch errors (fan-out = one watch + a Channel); next() -> Result<Void, IoError>, coalescing documented; NO exit registry — the K100 pattern is watch -> cancel -> deinits -> main returns; process.exit stays the no-cleanup hatch. SIGPIPE deliberately NOT a Signal case (per-socket suppression landed in unit 2, argued in the module header). MECHANISM NOTE for the reviewer: departs from the issue's kqueue/signalfd sketch for a load-bearing reason — signalfd requires the signal blocked in EVERY thread and a pre-existing thread cannot be retro-masked (a process-directed signal delivered there takes the default action and kills the process); delivery instead rides a self-pipe read end the EXISTING (fd, write, token) reactor seam already carries — NO frozen-seam change, argument recorded in rt/ABI.md. Handler-side send uses Signal.send_to_self() (a METHOD, which mangles) after SL-274's bare-symbol hazard bit a std free func named raise. Conformance K98-K100. Trio-wide terminal battery was 27/27 at the branch tip.","created":"1789230910","id":"c3"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-228.p1 (revision 1)\n\nRequest changes on revision 1 (base 78d328de, applied 178c64e8): two independently reproduced concurrency bugs in the signal bridge.\n\n1. [P1] Serialize watch ownership and lifecycle (`sawc/rt/shim.c:400–428`). The lazy initialization, already-watched check, descriptor publication and sigaction installation are unsynchronized. Two threads can both pass the check and both return a successful watch for one signal, violating AlreadyExists and the single-owner contract. Their descriptor and saved-disposition writes overwrite one another; one pipe becomes unreachable, and a saved “previous” disposition can be Saw's own handler. Dropping either handle can then invalidate the other or restore the wrong disposition. `volatile sig_atomic_t` does not serialize this transaction.\n\n   Reproduced with the exact patched shim: preinitialize with a different signal, then gate two callers at pipe() so both have passed the availability check before either publishes. Both SIGUSR1 watches succeed (`watch results: 5 3`) instead of exactly one returning -2. Harness: `/Users/swoodtke/Projects/sl228-review-r1/.build/scratch/watch_race.c`. Initialization, acquisition and teardown need coordinated synchronization, with a concurrent same-signal regression test.\n\n2. [P1] Quiesce in-flight handlers before reclaiming their pipe (`sawc/rt/shim.c:444–450`, handler fd load/write at 360–363). Restoring the disposition prevents future handler entries but does not wait for a handler already running on another thread. Such a handler can have loaded the old write descriptor before unwatch clears the slot and closes the pipe. If another pipe or file reuses that number, the delayed handler writes its notification byte into the unrelated resource. A mutex around watch/unwatch alone will not fix this handler-versus-close race.\n\n   Deterministic reproduction against the patched source: a scheduling hook pauses the actual signal handler immediately before write(), after it has loaded the descriptor; another thread unwatches and opens an unrelated pipe that reuses the descriptors; resume the handler. Output: `unrelated pipe received 1 byte, value=1`. Harness: `/Users/swoodtke/Projects/sl228-review-r1/.build/scratch/signal_close_race.c` (build with `clang -std=c11 -pthread -Wl,-dead_strip .build/scratch/signal_close_race.c -o .build/scratch/signal_close_race`). Please establish a signal-safe reclamation protocol and pin teardown concurrent with delivery and descriptor reuse.\n\nValidation: all four focused K98/K99/K100/hosted-only fixtures pass, as do the ABI documentation, citation and diff checks. These passing fixtures do not cover concurrent acquisition or in-flight delivery during teardown. Server tests were still pending when the review was prepared.\n","created":"1789232871","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-228.p1 (revision 1)","created":"1789234160","id":"c5"},{"author":"agent:claude-sawlang","body":"Revised patch SL-228.p1 to revision 2","created":"1789235202","id":"c6"},{"author":"agent:claude-sawlang","body":"SL-228.p1 r2 SUBMITTED (fix round for review r1's two shim P1s; commit 2e8a9859 as the unit-3 delta). P1a: one pthread_mutex spans the whole watch/unwatch transaction — the HANDLER stays lock-free by necessity (it can interrupt a thread inside the mutex; the constraint is now written at the top of the block). P1b: reclamation made IMPOSSIBLE rather than synchronized — the pipe is created at first watch and lives for the process; unwatch restores disposition and drains, closes nothing (two fds per watched signal, 14 worst case, 0 unwatched). The token-validation alternative was REJECTED on its own terms: a handler that validates-then-writes is the same TOCTOU in a smaller font; its useful half survives inverted — each watch bumps a generation, the handler STAMPS its byte, and the DRAIN (reader-side, race-free) validates, so a stale byte from a previous watch can never fake a delivery into a shutdown path. Both races reproduced pre-fix with independently-written harnesses landing on the reviewer's exact numbers (results 5 3; unrelated pipe received value=1, fds 3/4 reused); pinned by tools/signal_race_probe.c linking the REAL shim, run via examples/signal_concurrency_pins.saw in the ordinary suite — three cells fail against the pre-fix shim, all pass now. Obligation-4 sweep cleared every other shim transaction with reasons (the offload thunk shares the publish-then-write shape and is safe only via pthread_join-before-close — the contrast argued in place); filed SL-276 (thread_spawn discards pthread_create failure — different class). Brief mechanism section + rt/ABI.md rewritten to the shipped self-pipe design. Re-ran: suite 2503/11x, freestanding 36/36, abidoc exact, citations — full battery not re-run (no compiler code in the delta; r1's 27/27 covered unit 3's shape), stated per the mandate.","created":"1789235219","id":"c7"},{"author":"agent:sawtracker-tests","body":"Tests failed for SL-228.p1 (revision 2):\n\n```\n[2482/2514] ✓ vector_with_ref_oob_panic\n[2483/2514] ✓ while_let_tuple_over_suspension_refused\n[2484/2514] ✓ while_never_freestanding\n(2505/2514) · while_nested\n[2485/2514] ✓ vector_with_var_ref_oob_panic\n[2486/2514] ✓ version_semver_sort\n(2506/2514) · while_never_diverges\n[2487/2514] ✓ vis258_private_is_an_ordinary_identifier\n(2507/2514) · while_never_break_forms\n[2488/2514] ✓ vis258_field_inherits_public\n(2508/2514) · while_let_suspending_mt\n(2509/2514) · while_let_suspending_try_catch\n(2510/2514) · while_simple\n(2511/2514) · wrapping_operators\n(2512/2514) · while_let_suspending\n(2513/2514) · wrapping_and_reference_mix\n[2489/2514] ✓ vis80_public_members_ok\n[2490/2514] ✓ vis80_same_module_ok\n[2491/2514] ✓ visibility_package_access\n(2514/2514) · while_never_sync_context\n[2492/2514] ✓ visibility_package\n[2493/2514] ✓ visibility_parent\n[2494/2514] ✓ visibility_parent_public_works\n[2495/2514] ✓ void_match_in_closure\n[2496/2514] ✓ visibility_public\n\nCompiles complete in 395.3s (0/2514 reused, 0%) — 930 settled without running, 1584 binaries queued for execution (1566 of them already run).\n\n[2497/2514] ✓ while_break_continue\n[2498/2514] ✓ while_expr_conditional_found\n[2499/2514] ✓ void_if_chain_tail\n[2500/2514] ✓ while_expr_infinite\n[2501/2514] ✓ while_expr_conditional_not_found\n[2502/2514] ✓ while_infinite\n[2503/2514] ✓ while_let_drain\n[2504/2514] ✓ while_let_channel_drain\n[2505/2514] ✓ while_nested\n[2506/2514] ✓ while_never_diverges\n[2507/2514] ✓ while_never_break_forms\n[2508/2514] ✓ while_let_suspending_mt\n[2509/2514] ✓ while_let_suspending_try_catch\n[2510/2514] ✓ while_simple\n[2511/2514] ✓ wrapping_operators\n[2512/2514] ✓ while_let_suspending\n[2513/2514] ✓ wrapping_and_reference_mix\n[2514/2514] ✓ while_never_sync_context\n\ncompile 395.3s + 4.4s draining = 399.6s total\n\n======================================================================\nTest Results\n======================================================================\n\nFAILED TESTS:\n\n  ✗ signal_concurrency_pins\n    Output should contain 'signal race probe: all cells hold' — missing\n    Got:\n    BUILD FAILED (exit 1):\n    ld: open() failed, errno=2 (No such file or directory) for '/Users/shawn/Projects/sawtracker-production/data/worktrees/SL-228.p1.test/.build/scratch/signal_race_probe_suite'\n    clang: error: linker command failed with exit code 1 (use -v to see invocation)\n    \n    \n\nKNOWN FAILURES (xfail):\n  x V96_driven_capture_duplicates_once: SL-267 — a driven body's NON-ESCAPING closure capture duplicates\n  x closure_escaping_move_capture_consumed_once: DF-255a an escaping closure's env destructor releases a capture the\n  x closure_nested_ref_param_capture: DF-248b a nested closure captures an enclosing closure's reference\n  x closure_shorthand_parameter_inside_a_try: DF-257d the `$0` shorthand is invisible to the implicit-parameter scan\n  x coro_suspending_call_in_inline_catch: SL-215 the inline `try EXPR catch { }` catch block does not CFG-split,\n  x coro_value_inline_catch_hosts_a_move: SL-231 a value-carrying INLINE catch cannot host a `move` of a frame\n  x funcpointer_call_in_a_driven_body: DF-252a a `FuncPointer` value called by NAME inside a driven body is\n  x generic_init_moved_parameter_is_released_once: DF-302b — a generic extension's `init` releases a parameter it MOVED\n  x match_on_suspending_call_in_a_loop: SL-273 — a match arm PAYLOAD BINDING is lost when a SUSPENSION\n  x primitive_type_called_as_a_function: DF-270e — a PRIMITIVE TYPE NAME written as a ZERO-ARGUMENT call is an\n  x trailing_closure_inside_a_try_operand: DF-259c — a trailing closure is not recognized inside a `try`/`try!`/\n\n======================================================================\nSOME TESTS FAILED (2502 passed, 11 xfailed, 1 failed)\n======================================================================\n\nPublished test_runner_1789235204_36739 as test_runner_last (1542 manifest entries).\n\n```","created":"1789235605","id":"c8"}],"created":"1789050299","description":"MOTIVATION: a hosted Saw process cannot intercept SIGTERM/SIGINT — the sawtracker server dies with no flush, no socket drain, and stale lockfiles. The C idioms (signal(2) callbacks, atexit) fight deterministic destruction, the cooperative runtime, and never-hide-errors.\n\nRULED DESIGN (user, Sep 10 2026):\n1. SURFACE = SUSPENDING WATCH, no callbacks anywhere: hosted-only std.signal; watch(.terminate) returns a watch handle (Result — registration can fail; never hide errors) whose next() SUSPENDS until delivery, parking the task on the reactor exactly like a socket read. Watching a signal replaces its default disposition; unwatched signals keep OS behavior; SIGKILL/SIGSTOP are inexpressible.\n2. NO EXIT REGISTRY: cleanup rides cancellation + deinits. The pattern is a shutdown task that watches the signal, cancels the rest of the work (cancel already wakes io-parked tasks, design 102), values deinit deterministically as tasks unwind, main returns normally. process.exit(code) is documented as the no-cleanup escape hatch (no unwinding mid-flight).\n3. SCHEDULING: paired with SL-204 (io_wait deadline) as the server-hardening pair at the queue tail, behind the current soundness pipeline.\n\nMECHANISM SKETCH: both hosts integrate natively with the design-117 instance-based Saw reactor — kqueue EVFILT_SIGNAL (macOS; set SIG_IGN so the default action never fires) and signalfd (Linux; pthread_sigmask block, including at Thread/TaskGroup worker spawn — shim change). shim.c grows sigaction/sigprocmask-family wrappers (FFI-blocked bodies). Check whether any __saw_rt_* seam addition is needed or the reactor side suffices — rt/ABI.md is frozen, abidoc lane gates any change.\n\nBRIEF-TIME SCOPING (open, for the designs/NN brief at dispatch): the curated Signal enum membership + spellings (no-abbreviations doctrine applies); double-watch policy (error at second watch vs broadcast); whether the hosted runtime should default-ignore SIGPIPE for servers; interaction with op-budget fairness while a watcher task parks; freestanding simply lacks the module (like std.net). Obligation-2 consumer sweep: who relies on default signal dispositions today (test_runner child processes, blade, the QEMU harnesses).","id":"SL-228","labels":["feature","stdlib"],"priority":"normal","project":"SL","revision":10,"sequence":730,"stage":"queued","status":"open","title":"std.signal: signals as suspending reactor events; graceful shutdown rides cancellation (no atexit registry)","updated":"1789235605"},"id":"SL-228","project":"SL","request":"{\"action\":\"patch_revise\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/.claude/skills/saw-lang/SKILL.md b/.claude/skills/saw-lang/SKILL.md\\nindex 63125c31..6c77021a 100644\\n--- a/.claude/skills/saw-lang/SKILL.md\\n+++ b/.claude/skills/saw-lang/SKILL.md\\n@@ -2304,6 +2304,28 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n   It is decided by elimination, never by watching a task fail to progress —\\n   design 127's op budget makes a long computation present exactly like a\\n   permanent wait.\\n+- Signals (design 272, std.signal, hosted-only): a signal is something a task\\n+  WAITS FOR, not a callback. `import std.signal.{Signal, SignalWatch}`.\\n+  ```saw-fragment\\n+  let w = try! SignalWatch.watch(Signal.Terminate)   // Result; NoCopy handle\\n+  try! w.next()                       // SUSPENDS until delivered (an io park)\\n+  try! Signal.Terminate.send_to_self()               // signal this process\\n+  ```\\n+  `Signal` is CURATED: Terminate, Interrupt, Hangup, Quit, User1, User2,\\n+  WindowChanged. SIGKILL/SIGSTOP are absent because no process may intercept\\n+  them; SIGPIPE because sockets never raise it (unit 2 suppresses it per socket,\\n+  so a write to a hung-up peer is `Err(BrokenPipe)`); SIGCHLD because\\n+  std.process reaps with `waitpid` and changing that disposition breaks it.\\n+  While the handle lives the default action does NOT run — a watched SIGTERM no\\n+  longer ends the process — and dropping the handle restores the previous\\n+  disposition. A SECOND live watch of one signal is `Err(AlreadyExists)`: one\\n+  owner, because the handle is what restores process-global state. Fan out with\\n+  one watch plus a `Channel`. Deliveries COALESCE and no count is reported.\\n+  NO EXIT REGISTRY: shutdown is a task that watches, `cancel()`s the work (which\\n+  wakes io-parked tasks, design 102) and lets deinits run as tasks unwind;\\n+  `process.exit(code)` is the no-cleanup escape hatch.\\n+  A lone parked watcher is a CORRECT state that looks like a hang — it is an\\n+  ordinary io park, so the deadlock walk leaves it alone.\\n - Cooperative net (design 84, std.net, hosted-only): use the SAFE OWNING TYPES —\\n   `TcpListener` and `TcpStream` (both NoCopy, `Deinit` closes the fd exactly once).\\n   NO raw fds, NO pointers, NO `io_wait` in your code — suspension is hidden INSIDE\\ndiff --git a/LANGUAGE_SPEC.md b/LANGUAGE_SPEC.md\\nindex 7e99864b..ea4e1626 100644\\n--- a/LANGUAGE_SPEC.md\\n+++ b/LANGUAGE_SPEC.md\\n@@ -9522,6 +9522,56 @@ Two constraints are enforced, not documented:\\n \\n Hold it briefly. A waiter burns its core until the lock is free.\\n \\n+### `std.signal`\\n+\\n+**Status: implemented (design 272).** `import std.signal`. Hosted only, like\\n+`std.net`; the freestanding profile rejects the import.\\n+\\n+A signal is something a task waits for, not something that interrupts one.\\n+`SignalWatch.watch(which)` returns a handle whose `next()` suspends until the\\n+signal is delivered, parking the task on the reactor exactly as a socket read\\n+does — so cancellation, the fairness budget and the deadlock walk all treat a\\n+parked watcher as the io park it is.\\n+\\n+```saw\\n+import std.signal.{Signal, SignalWatch}\\n+import std.net.{TcpListener}\\n+\\n+// A shutdown task: wait for SIGTERM, then cancel the work and let it unwind.\\n+func shutdown(watch: SignalWatch, work: VoidTask) {\\n+    try! watch.next()\\n+    work.cancel()\\n+    let _ = move watch\\n+}\\n+```\\n+\\n+`Signal` is curated: `Terminate`, `Interrupt`, `Hangup`, `Quit`, `User1`,\\n+`User2`, `WindowChanged`. `SIGKILL` and `SIGSTOP` are absent because no process\\n+may intercept them — there is no refusal to report because there is no way to\\n+spell the request. `SIGPIPE` is absent because sockets never raise it (a write\\n+to a hung-up peer is an ordinary `BrokenPipe` error) and the process-wide\\n+disposition is left alone. `SIGCHLD` is absent because `std.process` reaps\\n+children with `waitpid`, and changing that signal's disposition makes the kernel\\n+auto-reap and `waitpid` fail.\\n+\\n+While a handle is alive the signal's default action does not run; destroying it\\n+restores whatever disposition the watch replaced. A second live watch of the\\n+same signal fails with `AlreadyExists`: the disposition is process-global state\\n+and the handle is what restores it, which is well defined only with one owner.\\n+Build fan-out from one watch and a `Channel`.\\n+\\n+Deliveries coalesce. The OS does not queue ordinary signals, so several arriving\\n+between two `next()` calls may surface as one, and no count is reported because\\n+the platform never had one to give.\\n+\\n+`Signal.Terminate.send_to_self()` sends a signal to the current process — how a\\n+program triggers its own shutdown path, and how a test exercises a watch.\\n+\\n+There is no exit registry. Cleanup rides cancellation and deinits: a task\\n+watches the signal, cancels the rest of the work, values are destroyed\\n+deterministically as tasks unwind, and `main` returns normally.\\n+`process.exit(code)` remains the escape hatch that runs no cleanup at all.\\n+\\n ### `std.once`\\n \\n **Status: implemented (design 186).** `import std.once`. `Once<T>` holds a value\\n@@ -11069,7 +11119,8 @@ need one of the three [import forms](#imports).\\n | `std.once` | `Once<T>` | no |\\n | `std.data` | `Data` | no |\\n | `std.file` / `std.directory` / `std.path` | `File`, `Directory`, `Path` | no |\\n-| `std.net` | `TcpListener`, `TcpStream`, `IoError` | no |\\n+| `std.net` | `TcpListener`, `TcpStream`, `IoError`, `Timed<T>` | no |\\n+| `std.signal` | `Signal`, `SignalWatch` | no |\\n | `std.process` / `std.env` | `Command`, `ProcessError`, `Env` | no |\\n | `std.duration` | `Duration` | yes |\\n | `std.time` | `Instant`, `unix_timestamp` | no |\\ndiff --git a/README.md b/README.md\\nindex 9ef084d4..a9660261 100644\\n--- a/README.md\\n+++ b/README.md\\n@@ -709,6 +709,10 @@ The standard library lives in `sawc/std/` and includes:\\n - **The filesystem** — `File` (std.file), `Directory` (std.directory), `Path`\\n   (std.path) and `Env` (std.env), one module each; every fallible operation\\n   returns a `Result`.\\n+- **std.signal** — OS signals as suspending events. `watch(.Terminate)` gives a\\n+  handle whose `next()` suspends until the signal arrives, so graceful shutdown\\n+  is a task that waits, cancels the work and lets deinits run. No callbacks and\\n+  no exit registry.\\n - **std.process** — run child processes cooperatively.\\n - **std.time** — `Instant` and `unix_timestamp` (hosted). `Duration` needs no\\n   import because `sleep` takes one.\\ndiff --git a/designs/272-server-hardening.md b/designs/272-server-hardening.md\\nindex 46e91561..7ae87000 100644\\n--- a/designs/272-server-hardening.md\\n+++ b/designs/272-server-hardening.md\\n@@ -332,39 +332,122 @@ server.cancel()              // design 102 wakes the io-parked accept loop\\n \\n ### The mechanism\\n \\n-Both hosts deliver NATIVELY — no `sigaction` callback, no self-pipe, no\\n-async-signal-safe handler to audit:\\n-\\n-* **macOS** — `EVFILT_SIGNAL`, `ident = signo`, `udata = token`, armed\\n-  `EV_ADD|EV_CLEAR`. The signal's default action must first be disabled\\n-  (`sigaction` to `SIG_IGN`), because `EVFILT_SIGNAL` observes delivery and does\\n-  not suppress it.\\n-* **Linux** — `signalfd`, registered with the reactor as an ordinary read fd,\\n-  with the signal blocked via `pthread_sigmask` in EVERY thread. \\\"Every thread\\\"\\n-  includes threads spawned after the watch begins, so `__saw_rt_thread_spawn`\\n-  and the offload thunk inherit the mask — a `shim.c` change (a new thread\\n-  inherits its creator's mask, so masking at `watch` time plus masking in the\\n-  spawn path covers both orders).\\n-\\n-Both are hidden behind ONE portable seam pair, so the host divergence lives\\n-where design 117 put every other one:\\n-\\n-* **New seam** `__saw_rt_reactor_register_signal(r, signo, token) -> word`\\n-  (0 or `-tag`; registration is fallible, which is why `watch` returns\\n-  `Result`).\\n-* **New seam** `__saw_rt_reactor_unregister_signal(r, signo) -> void`.\\n-* `signo` is a PORTABLE TAG, not a host signal number — the `SysError` /\\n-  socket-option pattern again, and necessary because the numbers diverge\\n-  (`SIGUSR1` is 30 on macOS, 10 on Linux).\\n-* **`rt/ABI.md` CHANGES** — reactor section grows two entries plus a signal tag\\n-  table. **FLAGGED**; `abidoc` gates it and the edit travels in the same commit.\\n-\\n-`next()` is an ordinary io park (`io_wait` on the Linux signalfd; on macOS the\\n-`EVFILT_SIGNAL` knote latches the same `__wake` token), so it inherits\\n-cancellation, the budget reset, and — for free — unit 1's `next(timeout:)` if we\\n-want one later. A lone parked watcher keeps `anyio` true, so the\\n-design-230 quiescent walk never reports a deadlock on \\\"a server waiting for\\n-SIGTERM\\\", which is exactly right: a signal can still arrive.\\n+**The brief sketched natives and the implementation did not use them.** Recorded\\n+here as a departure with its reason, because the sketch is what a reader of this\\n+document would otherwise expect. The sketch was: macOS `EVFILT_SIGNAL`, Linux\\n+`signalfd`, behind two new frozen reactor seams\\n+(`__saw_rt_reactor_register_signal` / `_unregister_signal`) and a signal tag\\n+table in `rt/ABI.md`. The SL-228 issue explicitly asked this be checked rather\\n+than assumed (\\\"whether any `__saw_rt_*` seam addition is needed or the reactor\\n+side suffices\\\"), and the check came back against the sketch.\\n+\\n+WHAT SHIPPED: the runtime installs a small handler that writes one byte to a\\n+pipe, and the watch registers that pipe's READ END with the reactor. A parked\\n+watcher is then an ORDINARY io park and inherits design 76/91/102 wholesale.\\n+Three reasons it won:\\n+\\n+* **Zero frozen-seam change.** A pipe read end is an ordinary readable\\n+  descriptor, so `__saw_rt_reactor_register(r, fd, write, token)` already\\n+  carries it. Both natives need a signal-shaped registration that seam cannot\\n+  express, so the sketch meant new frozen seams on a contract deliberately hard\\n+  to change.\\n+* **One mechanism on both hosts.** `EVFILT_SIGNAL` observes delivery and needs\\n+  the disposition set to `SIG_IGN`; `signalfd` consumes a BLOCKED signal and\\n+  needs a mask. That is two implementations wearing one seam, divergent in\\n+  exactly the corners signals are hard in.\\n+* **No thread-ordering hazard**, which decided it. `signalfd` requires the\\n+  signal blocked in EVERY thread, and a thread that already existed when the\\n+  watch began cannot be made to block it — a process-directed signal delivered\\n+  there takes the DEFAULT action and kills the process. Masking at\\n+  `__saw_rt_thread_spawn` covers threads created after the watch; nothing covers\\n+  the ones before it. A handler runs on whichever thread takes the signal and\\n+  has no such requirement.\\n+\\n+The handler is runtime-internal and invisible from Saw. The ruling that forbids\\n+callbacks is about the surface a program writes against, and no program written\\n+against this one ever names a handler.\\n+\\n+`next()` is an ordinary io park, so it inherits cancellation, the budget reset,\\n+and — for free — unit 1's `next(timeout:)` if we want one later. A lone parked\\n+watcher keeps `anyio` true, so the design-230 quiescent walk never reports a\\n+deadlock on \\\"a server waiting for SIGTERM\\\", which is exactly right: a signal can\\n+still arrive.\\n+\\n+### The concurrency protocol (revision 2, after the SL-228 r1 review)\\n+\\n+Review reproduced two P1 races in the first version of the shim. Both are real,\\n+both were reproduced again here before being fixed, and the protocol below is\\n+what replaced the original code. The reasoning lives in `rt/shim.c` beside the\\n+code; this is the design record.\\n+\\n+**P1a — the watch transaction was not serialized.** Lazy init, the\\n+already-watched check, descriptor publication and `sigaction` installation were\\n+four steps with nothing holding them together, so two threads could both pass\\n+the check and both return a successful watch for one signal: the single-owner\\n+contract broken, one pipe unreachable, and a saved \\\"previous\\\" disposition that\\n+could be Saw's OWN handler — after which dropping either handle restores the\\n+wrong thing. `volatile sig_atomic_t` makes individual loads and stores well\\n+defined; it does not make a transaction atomic, which is the confusion the first\\n+version rested on.\\n+\\n+FIX: one `pthread_mutex_t` around the whole transaction, on the watch/unwatch\\n+side only. The HANDLER never takes it and must never take any lock — it can\\n+interrupt a thread already inside the mutex (a thread calling `watch` can itself\\n+be signalled), and a handler blocking on a mutex its own thread holds deadlocks\\n+immediately.\\n+\\n+**P1b — teardown raced an in-flight handler.** Restoring a disposition stops\\n+FUTURE handler entries and says nothing about a handler already running on\\n+another thread that has loaded the write descriptor and not yet written. The\\n+original `unwatch` closed the pipe under it, so the descriptor number was free\\n+for reuse and the delayed handler wrote its byte into whatever inherited it.\\n+A mutex cannot fix this: the handler is not inside the mutex and cannot be.\\n+\\n+FIX — **nothing a handler can reach is ever reclaimed.** The pipe for a signal\\n+is created at its FIRST watch and lives for the whole process; `unwatch`\\n+restores the disposition and drains, and closes NOTHING. The descriptor the\\n+handler loads is valid forever, so there is no window because there is no\\n+reclamation. Cost: two descriptors per signal ever watched, at most fourteen for\\n+a process that watches the whole ruled set, zero for one that watches none.\\n+\\n+This is the review's option (a). Option (b) — a generation or refcount the\\n+handler validates before writing — was rejected on its own terms: a handler that\\n+checks validity and then writes has a window between the two, which is the same\\n+time-of-check-to-time-of-use shape in a smaller font. The only way to make (b)\\n+sound is deferred reclamation, and deferred reclamation with no safe point to\\n+defer to is (a).\\n+\\n+**The generation TAG, which is (b)'s useful half used the other way round.**\\n+Not-reclaiming makes the late write SAFE; it does not make it ACCURATE. A byte\\n+from a previous watch sits in a pipe the next watch reads, and mistaking it for\\n+a fresh delivery would return from `next()` in a shutdown path and cancel a\\n+server nobody signalled. So each watch bumps a generation, the handler STAMPS\\n+its byte with the generation it saw, and the DRAIN keeps only bytes matching the\\n+current one. Note which side validates: the handler merely stamps, because a\\n+validating handler is back in the TOCTOU race; the reader validates, and the\\n+reader has no race.\\n+\\n+### Obligation 4 — the rest of the shim, swept\\n+\\n+A race found in one transaction is presumed to be a class. Every other\\n+transaction in `rt/shim.c` was examined for the same shape (unsynchronized\\n+check-then-act, or reclamation under a concurrent reader):\\n+\\n+| Transaction | Verdict | Why |\\n+|---|---|---|\\n+| offload thunk vs `__saw_rt_offload_take` | **CLEAR** | The same shape — the thunk publishes `done` with a release store and only THEN writes the pipe, and the Saw drive loop exits on `done`, so `take` can run while the thunk is mid-write. It is safe because `take` calls `__saw_rt_thread_join` FIRST, and the join does not return until the worker has exited, strictly after the write. This is the contrast that explains the signal fix: the offload path has a JOINABLE worker, so it can wait for its in-flight writer. A signal handler has no handle to join, which is why the signal path had to make reclamation impossible instead of waiting |\\n+| `__saw_environ_get` / `__saw_environ_set` | **CLEAR** | They look like a get/modify/restore transaction over a process global, which would be exactly the shape. They are not: the GET runs in the parent as a read (the merge, `rt/common/proc.saw:184`) and the SET runs in the CHILD after `fork`, in a private address space with one thread (`proc.saw:279`). Nothing is shared, so there is no transaction to serialize |\\n+| `__saw_rt_thread_detach` | **CLEAR** | Already uses an atomic exchange for precisely this hazard — exactly one of the two parties frees the control block, with no lock and no wait. It is the model the rest should follow |\\n+| `__saw_rt_lock_acquire` / `_release` | **CLEAR** | A futex lock built on real atomics; a lock implementation, not an unsynchronized transaction |\\n+| `__saw_rt_set_nonblocking` | **CLEAR** | `F_GETFL` then `F_SETFL` IS a check-then-act on one descriptor's flags, but every caller applies it to a descriptor it has just created and not yet shared, so there is no second party |\\n+| `__saw_open_flags`, `__saw_ai_*`, `__saw_gai_tag`, `__saw_sockopt_*`, `__saw_socket_send_flags`, `__saw_epoll_*` | **CLEAR** | Pure functions of their arguments; no state at all |\\n+| `__saw_rt_write` / `__saw_rt_panic` | **CLEAR** | `fwrite`/`fflush` on one `FILE*`; POSIX requires stdio operations to be atomic with respect to each other |\\n+\\n+One thing the sweep turned up that is a DIFFERENT class and is filed rather than\\n+fixed here: `__saw_rt_thread_spawn` discards `pthread_create`'s return value and\\n+returns an uninitialized `pthread_t` on failure, which every consumer then joins\\n+as though it were a live thread (**SL-276**). Not a synchronization bug — a\\n+discarded status — but it was found here and is recorded rather than dropped.\\n \\n ---\\n \\ndiff --git a/examples/conformance/INDEX.md b/examples/conformance/INDEX.md\\nindex 2c1dfaea..a561926c 100644\\n--- a/examples/conformance/INDEX.md\\n+++ b/examples/conformance/INDEX.md\\n@@ -6,10 +6,10 @@ either a file in this directory or an existing `examples/` test that already\\n asserts the same rule at the same position — this table is the record of which,\\n and the dedup decisions are meant to be audited from it.\\n \\n-**516 rows: 269 carry a file here, 247 are covered elsewhere** (recounted Sep 12\\n-when design 272 unit 1 added K93-K97; a multi-file row is still counted by its\\n-FIRST file, which is why K94's two files add one. Previously, and on the same\\n-basis: **511 rows: 264 carry a file here, 247 are covered elsewhere**, recounted Aug 25\\n+**519 rows: 272 carry a file here, 247 are covered elsewhere** (recounted Sep 12\\n+when design 272 added K93-K97 in unit 1 and K98-K100 in unit 3; a multi-file row\\n+is still counted by its FIRST file, which is why K94's two files add one.\\n+Previously, and on the same basis: **511 rows: 264 carry a file here, 247 are covered elsewhere**, recounted Aug 25\\n from the table itself — this header has now gone stale twice, so treat the\\n table as the record and this line as a cache; a multi-file row is counted by\\n its FIRST file). (The audit's 247 plus\\n@@ -80,7 +80,11 @@ live across them, so the sweep, an MT group and the single-frame drive are three\\n files, not one), readiness still beating the deadline, timed and untimed parks\\n sharing one poll without either starving the other, and DF-134a's\\n no-stranded-registration rule re-asserted at the third loop exit a deadline\\n-adds.)\\n+adds; and K98-K100, design 272 unit 3's signal rows — a signal waking a parked\\n+watcher (and thereby NOT ending the process), a watch scoped to its handle and\\n+owned by exactly one, and the whole graceful-shutdown pattern: cancellation\\n+wakes the io-parked worker, its values deinit once, and no registry or atexit\\n+hook exists anywhere.)\\n \\n ## How to read it\\n \\n@@ -626,6 +630,9 @@ Claim source: spec 6 *Send and Sync* + *Cooperative tasks*; designs 75, 88, 103,\\n | K95 | a deadline-bounded park that becomes READY before its deadline reports the VALUE, and an untimed park is untouched | `K95_ready_before_deadline_reports_the_value.saw` | design 272 unit 1 — the half a bounded poll gets wrong most easily: bounding a wait must not turn readiness into a timeout. The park loop re-issues its syscall FIRST on every wake and consults the clock only after, so bytes arriving in the same instant as the deadline are reported as bytes. A READY handshake forces the park (the SL-208 io_wake precedent — a test whose bytes might already be buffered proves nothing about parking). The second case is the regression half: design 272 gave EVERY frame a deadline field and every io park a deadline check, and this is the line that says an unbounded park is still unbounded |\\n | K96 | a timed park and an UNTIMED park in one program each get their own wakeup: neither starves the other, and neither steals the other's | `K96_timed_and_untimed_parks_coexist.saw` | design 272 unit 1. Both fold into the SAME earliest-deadline variable that bounds the one reactor poll every parked frame shares, and only a MIXED program catches either failure at that junction: a short deadline shortening the poll must leave the untimed frame re-parking rather than losing its registration, and an untimed frame's `block indefinitely` must NOT win the fold and erase the timed frame's bound (`-1` means `no bound from me`, never `no bound for anybody`). The output order is forced by the program — the patient reader is fed only after the idle one has reported its timeout — so nothing here rests on timing |\\n | K97 | a deadline-bounded park leaves NO reactor registration behind on ANY exit — ready, timeout, or frame release | `K97_timed_park_leaves_no_registration_behind.saw` | design 272 unit 1, inheriting DF-134a's rule: a stranded one-shot registration leaves the reactor holding a token INTO the frame, and design 134 frees the frame box at completion, so the next readiness event writes into freed memory. A timed park added a THIRD way out of the park loop, hence a third place to reintroduce it. A stranded registration is not observable from Saw, so the row asserts the consequence that is: each round times out, drops its socketpair, and the kernel hands the same descriptor numbers to the next round — a surviving arm on that number would collide. Ten rounds, each asserting its own timeout, plus a deinit oracle for the release path that clears the deadline |\\n+| K98 | a delivered signal wakes a PARKED watcher, and a watched signal's default action does not run | `K98_signal_wakes_a_parked_watcher.saw` | design 272 unit 3 (SL-228). Two guarantees in one program because they are two halves of one fact: watching replaces the disposition, so the signal that would have ENDED the process instead makes a parked task runnable. A regression prints no wrong line — the process dies on the signal and the runner reports THAT, which is the clearest failure available. The park is FORCED, not raced: the watcher announces on a channel that it is entering `next()` and the signal is sent only after that announcement is read, so delivery provably reaches an already-parked task (the SL-208 io_wake precedent). `Signal.send_to_self()` is the oracle — deterministic, needs nothing outside the process, and works in a sandbox where an external signaller would not |\\n+| K99 | a watch is SCOPED to its handle and SINGLY owned: the disposition is restored when the handle dies, and a second live watch of the same signal is refused | `K99_watch_is_scoped_and_singly_owned.saw` | design 272 unit 3, the ruled double-watch policy and the ownership argument under it — one row rather than two because the refusal is what makes the restore meaningful, and a broadcast policy would have left nobody owning it. Three steps in order: a second watch is `AlreadyExists`; the handle is dropped; watching again SUCCEEDS. The third is load-bearing — a watch that never released its claim passes the first two silently and fails only there. A different signal is watched alongside to show the refusal is per-signal, not a global one-at-a-time |\\n+| K100 | graceful shutdown rides CANCELLATION and deinits: a parked signal watcher is not reported as a deadlock, and a cancelled worker's values are destroyed exactly once | `K100_shutdown_rides_cancellation.saw` | design 272 unit 3 — the ruled no-exit-registry pattern end to end, and the program the unit exists for. Three things that must hold together: a task parked on a signal while every sibling is io-parked is a CORRECT state that looks exactly like a hang, and the design-230 quiescent walk must not report it (a watcher is an ordinary io park, so `anyio` holds); the signal cancels the worker and cancellation WAKES an io-parked task (design 102 — the worker is on a socket whose peer never writes, so only the cancel can end it); and the worker's owned values deinit as it unwinds, exactly once, with no registry and no atexit hook. The group's Deinit is the synchronization point on purpose: `cancel()` IS the handle's fate (design 242) so it cannot also be joined, and the structured join is what the pattern rests on rather than a way around the fate rule |\\n \\n ## Visibility and module boundaries\\n \\ndiff --git a/examples/conformance/K100_shutdown_rides_cancellation.saw b/examples/conformance/K100_shutdown_rides_cancellation.saw\\nnew file mode 100644\\nindex 00000000..4a5b2015\\n--- /dev/null\\n+++ b/examples/conformance/K100_shutdown_rides_cancellation.saw\\n@@ -0,0 +1,95 @@\\n+// Conformance row K100 — graceful shutdown rides CANCELLATION and deinits: a\\n+// parked signal watcher is not a deadlock, and the values a cancelled worker\\n+// owns are destroyed deterministically\\n+//\\n+// Design 272 unit 3 (SL-228), the ruled \\\"no exit registry\\\" pattern end to end.\\n+// This is the program the whole unit exists for, and it asserts the three\\n+// things that have to hold together for it to be the answer:\\n+//\\n+//   1. A task parked on a signal while every sibling is parked on io is a\\n+//      CORRECT state that looks exactly like a hang. The design-230 quiescent\\n+//      walk must not report it: a signal watcher is an ordinary io park, so\\n+//      `anyio` is true and nothing is declared deadlocked. A regression here is\\n+//      a panic, not a wrong line.\\n+//   2. The signal cancels the worker, and cancellation WAKES an io-parked task\\n+//      (design 102) — the worker is parked on a socket read whose peer never\\n+//      writes, so only the cancel can end it.\\n+//   3. The worker's owned values deinit as it unwinds, EXACTLY ONCE, with no\\n+//      registry and no atexit hook anywhere. `main` then returns normally.\\n+//\\n+// The ordering is forced by the program: the worker announces that it is parked\\n+// before the signal is sent, so the cancel provably reaches a parked task.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// worker parked\\n+// shutdown signalled\\n+// worker unwound\\n+// deinits=1\\n+// main returned normally\\n+\\n+import std.signal.{Signal, SignalWatch}\\n+import std.net.{TcpStream}\\n+import std.channel.{Channel}\\n+\\n+static DEINITS: Atomic<Int> = Atomic(0)\\n+\\n+struct Connection { id: Int }\\n+extension Connection: NoCopy {\\n+    func deinit(&var self) { DEINITS.fetch_add(1) }\\n+}\\n+\\n+// Parks on a socket nobody ever writes to. Only a cancel can end this.\\n+func worker(s: TcpStream, ready: Channel<Int>) {\\n+    let _held = Connection(id: 1)\\n+    print(\\\"worker parked\\\")\\n+    try! ready.send(1)\\n+    match s.read() {\\n+        case Ok(_) -> print(\\\"WORKER READ — the peer should never have written\\\"),\\n+        case Err(_) -> print(\\\"worker unwound\\\")\\n+    }\\n+    // `_held` and `s` deinit here as the task completes.\\n+}\\n+\\n+func shutdown(w: SignalWatch, ready: Channel<Int>) {\\n+    try! w.next()\\n+    print(\\\"shutdown signalled\\\")\\n+    try! ready.send(1)\\n+    let _ = move w\\n+}\\n+\\n+// The group lives in here, so its Deinit — the structured join — is what\\n+// sequences the worker's unwind ahead of the deinit count below. `cancel()` IS\\n+// the worker handle's fate (design 242), so it cannot also be joined; the\\n+// group's teardown is the synchronization point, and using it is the point of\\n+// the pattern rather than a workaround for the fate rule.\\n+func serve() {\\n+    let w = match SignalWatch.watch(Signal.Terminate) {\\n+        case Ok(x) -> move x,\\n+        case Err(e) -> {\\n+            print(\\\"WATCH FAILED {}\\\", e)\\n+            return\\n+        }\\n+    }\\n+    let (a, b) = TcpStream.pair()\\n+    var group = TaskGroup()\\n+    let parked = try! Channel<Int>()\\n+    let signalled = try! Channel<Int>()\\n+\\n+    let hw = group.spawn(worker(move a, parked.copy()))\\n+    let hs = group.spawn(shutdown(move w, signalled.copy()))\\n+\\n+    let _ = try! parked.receive()        // the worker is at its park\\n+    try! Signal.Terminate.send_to_self()\\n+    let _ = try! signalled.receive()     // the watcher observed it\\n+\\n+    hw.cancel()                          // design 102 wakes the io-parked worker\\n+    hs.join()\\n+    let _ = move b\\n+    // `group` deinits here, draining the cancelled worker to completion.\\n+}\\n+\\n+func main() {\\n+    serve()\\n+    print(\\\"deinits={}\\\", DEINITS.load())\\n+    print(\\\"main returned normally\\\")\\n+}\\ndiff --git a/examples/conformance/K98_signal_wakes_a_parked_watcher.saw b/examples/conformance/K98_signal_wakes_a_parked_watcher.saw\\nnew file mode 100644\\nindex 00000000..eb5c8c59\\n--- /dev/null\\n+++ b/examples/conformance/K98_signal_wakes_a_parked_watcher.saw\\n@@ -0,0 +1,56 @@\\n+// Conformance row K98 — a delivered signal wakes a PARKED watcher, and a\\n+// watched signal's default action does not run\\n+//\\n+// Design 272 unit 3 (SL-228). Two guarantees in one program because they are\\n+// two halves of one fact: watching `SIGTERM` replaces its disposition, so the\\n+// signal that would have ENDED this process instead makes a parked task\\n+// runnable. If the replacement did not take, this test does not print a wrong\\n+// line — the process dies on the signal and the runner reports that, which is\\n+// the clearest possible failure.\\n+//\\n+// The park is FORCED rather than raced. The watcher announces on a channel that\\n+// it is about to call `next()`, and `main` only sends the signal after reading\\n+// that announcement, so the delivery provably arrives at a task that is already\\n+// parked on the reactor. A test that signalled first might find the byte\\n+// already on the pipe and would prove nothing about waking a parked watcher —\\n+// the SL-208 io_wake precedent.\\n+//\\n+// `send_to_self` is the oracle: deterministic, needs nothing outside the\\n+// process, and works under the suite runner and in a sandbox where an external\\n+// signaller would not.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// watcher parked\\n+// signal arrived\\n+// main still running\\n+\\n+import std.signal.{Signal, SignalWatch}\\n+import std.channel.{Channel}\\n+\\n+func watcher(w: SignalWatch, ready: Channel<Int>) {\\n+    print(\\\"watcher parked\\\")\\n+    try! ready.send(1)\\n+    match w.next() {\\n+        case Ok(_) -> print(\\\"signal arrived\\\"),\\n+        case Err(e) -> print(\\\"WATCHER ERR {}\\\", e)\\n+    }\\n+    let _ = move w\\n+}\\n+\\n+func main() {\\n+    let w = match SignalWatch.watch(Signal.Terminate) {\\n+        case Ok(x) -> move x,\\n+        case Err(e) -> {\\n+            print(\\\"WATCH FAILED {}\\\", e)\\n+            return\\n+        }\\n+    }\\n+    var group = TaskGroup()\\n+    let ready = try! Channel<Int>()\\n+    let t = group.spawn(watcher(move w, ready.copy()))\\n+    let _ = try! ready.receive()        // the watcher is at its park\\n+    try! Signal.Terminate.send_to_self()\\n+    t.join()\\n+    // Reaching this line is the second guarantee: SIGTERM did not end us.\\n+    print(\\\"main still running\\\")\\n+}\\ndiff --git a/examples/conformance/K99_watch_is_scoped_and_singly_owned.saw b/examples/conformance/K99_watch_is_scoped_and_singly_owned.saw\\nnew file mode 100644\\nindex 00000000..ec769c68\\n--- /dev/null\\n+++ b/examples/conformance/K99_watch_is_scoped_and_singly_owned.saw\\n@@ -0,0 +1,77 @@\\n+// Conformance row K99 — a watch is SCOPED to its handle and SINGLY owned: the\\n+// disposition is restored when the handle dies, and a second live watch of the\\n+// same signal is refused\\n+//\\n+// Design 272 unit 3 (SL-228), the ruled double-watch policy and the ownership\\n+// argument behind it. A signal's disposition is process-global state, and this\\n+// handle is what restores it — which is only well defined if exactly one handle\\n+// holds it at a time. So the refusal and the restore are ONE guarantee tested\\n+// together rather than two rows: the refusal is what makes the restore\\n+// meaningful, and a broadcast policy would have left no one owning it.\\n+//\\n+// Three things in order:\\n+//   1. a watch is live, and a SECOND watch of the same signal is `AlreadyExists`\\n+//      rather than a second owner;\\n+//   2. the handle is dropped, restoring whatever disposition it replaced;\\n+//   3. watching again now SUCCEEDS, which is what proves step 2 actually\\n+//      released the claim rather than leaking it.\\n+//\\n+// Step 3 is the load-bearing one. A watch that never released its claim would\\n+// pass steps 1 and 2 silently and fail only here.\\n+//\\n+// A DIFFERENT signal is watched alongside to show the refusal is per-signal and\\n+// not a global \\\"one watch at a time\\\".\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// first watch ok\\n+// second watch refused: already exists\\n+// other signal ok\\n+// rewatch after drop ok\\n+\\n+import std.signal.{Signal, SignalWatch}\\n+import std.net.{IoErrorKind}\\n+\\n+func main() {\\n+    let first = match SignalWatch.watch(Signal.Terminate) {\\n+        case Ok(x) -> move x,\\n+        case Err(e) -> {\\n+            print(\\\"FIRST WATCH FAILED {}\\\", e)\\n+            return\\n+        }\\n+    }\\n+    print(\\\"first watch ok\\\")\\n+\\n+    match SignalWatch.watch(Signal.Terminate) {\\n+        case Ok(_) -> print(\\\"WRONG: a second watch was allowed\\\"),\\n+        case Err(e) -> {\\n+            if e.kind() == IoErrorKind.AlreadyExists {\\n+                print(\\\"second watch refused: already exists\\\")\\n+            } else {\\n+                print(\\\"second watch refused with the WRONG kind: {}\\\", e)\\n+            }\\n+        }\\n+    }\\n+\\n+    // Per-signal, not global: a different signal is unaffected.\\n+    let other = match SignalWatch.watch(Signal.User1) {\\n+        case Ok(x) -> move x,\\n+        case Err(e) -> {\\n+            print(\\\"OTHER WATCH FAILED {}\\\", e)\\n+            return\\n+        }\\n+    }\\n+    print(\\\"other signal ok\\\")\\n+    let _ = move other\\n+\\n+    // Drop the first handle: its deinit restores the disposition and releases\\n+    // the claim.\\n+    let _ = move first\\n+\\n+    match SignalWatch.watch(Signal.Terminate) {\\n+        case Ok(again) -> {\\n+            print(\\\"rewatch after drop ok\\\")\\n+            let _ = move again\\n+        },\\n+        case Err(e) -> print(\\\"REWATCH FAILED — the claim leaked: {}\\\", e)\\n+    }\\n+}\\ndiff --git a/examples/errors/signal_is_hosted_only.saw b/examples/errors/signal_is_hosted_only.saw\\nnew file mode 100644\\nindex 00000000..cb5034eb\\n--- /dev/null\\n+++ b/examples/errors/signal_is_hosted_only.saw\\n@@ -0,0 +1,20 @@\\n+// `std.signal` is hosted-only: the freestanding profile drops it, exactly as it\\n+// drops `std.net` (design 272 unit 3, SL-228).\\n+//\\n+// Signals are an operating-system facility. A freestanding target has no kernel\\n+// to deliver one and no process to deliver it to, so the module is ABSENT\\n+// rather than present-and-broken — the refusal is at the import, naming the\\n+// module, instead of a link failure against seams that do not exist.\\n+//\\n+// The bare-metal target is what puts the profile in play: `--freestanding`\\n+// alone rejects the host's own triple first and the row would prove nothing\\n+// (the lesson N05 records).\\n+// COMPILE-FLAGS: --freestanding --target riscv32-unknown-none-elf\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: module `std.signal` is hosted-only and cannot be imported in the freestanding profile\\n+\\n+import std.signal.{Signal, SignalWatch}\\n+\\n+func main() {\\n+    let _ = SignalWatch.watch(Signal.Terminate)\\n+}\\ndiff --git a/examples/signal_concurrency_pins.saw b/examples/signal_concurrency_pins.saw\\nnew file mode 100644\\nindex 00000000..c709e68b\\n--- /dev/null\\n+++ b/examples/signal_concurrency_pins.saw\\n@@ -0,0 +1,123 @@\\n+// The signal bridge's concurrency pins (design 272 unit 3, SL-228 review r1).\\n+//\\n+// Two bugs were reproduced by review against the first version of the shim, and\\n+// both are races whose shapes cannot be written from Saw:\\n+//\\n+//   * two threads gated INSIDE the watch transaction, so both pass the\\n+//     already-watched check before either publishes — the single-owner contract\\n+//     broken, and a saved \\\"previous\\\" disposition that could be Saw's own\\n+//     handler;\\n+//   * a teardown that CLOSES the watch pipe while a handler on another thread\\n+//     is between its descriptor load and its write, so the byte lands in\\n+//     whatever unrelated resource inherits the descriptor number.\\n+//\\n+// Saw has no way to gate two threads inside a C function, and no way to pause a\\n+// signal handler mid-body. So the pins live in `tools/signal_race_probe.c`,\\n+// which links the REAL shim (never a copy — a pin against a transcription pins\\n+// the transcription), and THIS test builds and runs it so they gate in the\\n+// ordinary suite instead of being a file somebody has to remember to run.\\n+//\\n+// The probe was verified to FAIL against the pre-fix shim and pass against the\\n+// fixed one; its concurrent-acquisition cell reproduces the reviewer's own\\n+// measurement (`results 5 3`, where one caller must have been refused).\\n+//\\n+// The repo root is found by walking UP from the working directory to the\\n+// marker `sawc/rt/shim.c`, because nothing promises the suite runs a binary\\n+// from any particular place and `#file` carries only the basename.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT-CONTAINS: signal race probe: all cells hold\\n+// EXPECT-OUTPUT-CONTAINS: pins held\\n+\\n+import std.process.{Command}\\n+import std.path.{Path}\\n+import std.file.{File}\\n+import std.directory.{Directory}\\n+import std.env\\n+\\n+// How far up to look before giving up. A worktree is never this deep below the\\n+// place a test binary runs from.\\n+static MAX_DEPTH: Int = 16\\n+\\n+// The repo root: the nearest ancestor of the working directory holding\\n+// `sawc/rt/shim.c`. Walking to a marker rather than trusting the working\\n+// directory is what makes this work whether the suite runs the binary from the\\n+// repo root, from `.build`, or from anywhere else.\\n+func find_root() -> Path? {\\n+    guard let start = env.Env.cwd() else {\\n+        return None\\n+    }\\n+    var here = start\\n+    var depth = 0\\n+    while depth < MAX_DEPTH {\\n+        let marker = here.join(\\\"sawc\\\").join(\\\"rt\\\").join(\\\"shim.c\\\")\\n+        if File.exists(marker) {\\n+            return here\\n+        }\\n+        guard let up = here.parent() else {\\n+            return None\\n+        }\\n+        here = up\\n+        depth = depth + 1\\n+    }\\n+    None\\n+}\\n+\\n+func main() {\\n+    guard let root_path = find_root() else {\\n+        print(\\\"COULD NOT LOCATE THE REPO ROOT from the working directory\\\")\\n+        return\\n+    }\\n+    let root = root_path.as_str()\\n+    let probe = \\\"{root}/tools/signal_race_probe.c\\\"\\n+    let shim = \\\"{root}/sawc/rt/shim.c\\\"\\n+    // A FRESH checkout (the server's per-patch worktree) has no `.build/` at\\n+    // all, and the linker does not create parent directories — the r2 gate\\n+    // failed exactly there. Create both levels; AlreadyExists is fine.\\n+    let _ = Directory.create(root_path.join(\\\".build\\\"))\\n+    let _ = Directory.create(root_path.join(\\\".build\\\").join(\\\"scratch\\\"))\\n+    let out = \\\"{root}/.build/scratch/signal_race_probe_suite\\\"\\n+\\n+    var build = Command(program: \\\"clang\\\")\\n+    build.merge_stderr()          // clang reports on stderr; capture it\\n+    try! build.arg(\\\"-std=c11\\\")\\n+    try! build.arg(\\\"-pthread\\\")\\n+    try! build.arg(probe)\\n+    try! build.arg(shim)\\n+    try! build.arg(\\\"-o\\\")\\n+    try! build.arg(out)\\n+    let build_result = match build.output() {\\n+        case Ok(o) -> move o,\\n+        case Err(e) -> {\\n+            print(\\\"BUILD FAILED TO LAUNCH: {}\\\", e)\\n+            return\\n+        }\\n+    }\\n+    guard let built = move build_result else {\\n+        print(\\\"BUILD PRODUCED NO OUTPUT\\\")\\n+        return\\n+    }\\n+    if built.exit_code != (0 as Int32) {\\n+        print(\\\"BUILD FAILED (exit {}):\\\", built.exit_code)\\n+        print(\\\"{}\\\", built.stdout)\\n+        return\\n+    }\\n+\\n+    var run = Command(program: out)\\n+    let run_result = match run.output() {\\n+        case Ok(o) -> move o,\\n+        case Err(e) -> {\\n+            print(\\\"PROBE FAILED TO LAUNCH: {}\\\", e)\\n+            return\\n+        }\\n+    }\\n+    guard let ran = move run_result else {\\n+        print(\\\"PROBE PRODUCED NO OUTPUT\\\")\\n+        return\\n+    }\\n+    print(\\\"{}\\\", ran.stdout)\\n+    if ran.exit_code == (0 as Int32) {\\n+        print(\\\"pins held\\\")\\n+    } else {\\n+        print(\\\"PINS FAILED (exit {})\\\", ran.exit_code)\\n+    }\\n+}\\ndiff --git a/sawc/rt/ABI.md b/sawc/rt/ABI.md\\nindex 75515642..c7ef7b81 100644\\n--- a/sawc/rt/ABI.md\\n+++ b/sawc/rt/ABI.md\\n@@ -316,6 +316,73 @@ including shells and pipelines it did not write. Pipes keep their behaviour;\\n only sockets change. The user-visible consequence is that a write to a hung-up\\n client is an ordinary `Err(IoError)` with kind `BrokenPipe`.\\n \\n+### Runtime-INTERNAL signal helpers (design 272 unit 3)\\n+Also `shim.c`, also outside the frozen set, for the same reason: signal numbers\\n+are per-host macros (`SIGUSR1` is 30 on macOS and 10 on Linux).\\n+\\n+`__saw_signal_number(tag) -> word` maps a portable tag to this host's number,\\n+`-1` for a signal this host lacks. The tag space:\\n+\\n+| Tag | Signal          | C name     |\\n+|-----|-----------------|------------|\\n+| 1   | Terminate       | `SIGTERM`  |\\n+| 2   | Interrupt       | `SIGINT`   |\\n+| 3   | Hangup          | `SIGHUP`   |\\n+| 4   | Quit            | `SIGQUIT`  |\\n+| 5   | User1           | `SIGUSR1`  |\\n+| 6   | User2           | `SIGUSR2`  |\\n+| 7   | WindowChanged   | `SIGWINCH` |\\n+\\n+`__saw_signal_watch(signo) -> word` installs a handler and returns the watch\\n+pipe's READ END, or `-1` (bad signal) / `-2` (already watched) / `-3` (the pipe\\n+or the handler could not be installed). `__saw_signal_unwatch(signo)` restores\\n+the disposition and drains. `__saw_signal_raise(signo) -> word` sends the signal\\n+to this process. `__saw_signal_drain(signo) -> word` takes the deliveries\\n+belonging to the CURRENT watch off that signal's pipe — a positive count, or\\n+`-1` for nothing pending.\\n+\\n+**Two invariants this family rests on**, both earned by races the SL-228 r1\\n+review reproduced against the first version:\\n+\\n+1. **The watch transaction is serialized.** Initialization, the already-watched\\n+   check, descriptor publication and `sigaction` installation are one\\n+   transaction under a mutex. Unsynchronized, two callers could both acquire one\\n+   signal and each save the other's handler as the \\\"previous\\\" disposition. The\\n+   HANDLER takes no lock and must not: it can interrupt a thread already inside\\n+   that mutex.\\n+2. **Nothing a handler can reach is ever reclaimed.** A signal's pipe is created\\n+   at its first watch and lives for the process; `unwatch` closes nothing.\\n+   Restoring a disposition stops future handler entries but not one already past\\n+   its descriptor load, and closing the pipe under such a handler frees the\\n+   descriptor number for reuse — the delayed write then lands in an unrelated\\n+   resource. Costs at most two descriptors per watched signal.\\n+\\n+   Bytes carry a GENERATION stamp so a late write from a previous watch is\\n+   discarded by the drain rather than reported as a fresh delivery. The handler\\n+   stamps and the reader validates, never the other way round: a handler that\\n+   validated before writing would have a window between the two.\\n+\\n+A runtime provider replacing these must keep both invariants; they are contract,\\n+not implementation detail.\\n+\\n+**NO REACTOR SEAM WAS ADDED.** The design-272 brief expected one, and the\\n+mechanism check the SL-228 issue asked for came back the other way: the handler\\n+writes one byte to a pipe, and a pipe read end is an ordinary readable\\n+descriptor that `__saw_rt_reactor_register(r, fd, write, token)` already\\n+carries. The natives the sketch named would each have needed a signal-shaped\\n+registration the frozen `(fd, write, token)` seam cannot express.\\n+\\n+Three reasons the pipe won, recorded because the sketch pointed elsewhere: no\\n+frozen-seam change; ONE mechanism on both hosts rather than two (kqueue's\\n+`EVFILT_SIGNAL` observes delivery and needs the disposition set to `SIG_IGN`,\\n+Linux's `signalfd` consumes a BLOCKED signal and needs a mask — different enough\\n+to be two implementations wearing one name); and no thread-ordering hazard,\\n+which is decisive. `signalfd` requires the signal blocked in EVERY thread, and a\\n+thread that already existed when the watch began cannot be made to block it, so\\n+a process-directed signal delivered there takes the default action and kills the\\n+process. A handler runs on whichever thread takes the signal and has no such\\n+requirement.\\n+\\n ## Status-carrying network ops (design 117)\\n \\n Each does its syscall(s) and returns `>= 0` on success/count or `-tag` on failure.\\ndiff --git a/sawc/rt/shim.c b/sawc/rt/shim.c\\nindex db15283c..a5582794 100644\\n--- a/sawc/rt/shim.c\\n+++ b/sawc/rt/shim.c\\n@@ -295,6 +295,338 @@ long __saw_socket_send_flags(void) {\\n #endif\\n }\\n \\n+/* ---- design 272 unit 3 (SL-228): signals as reactor-readable events -----\\n+ *\\n+ * std.signal's SURFACE is a suspending watch with no callbacks anywhere: a\\n+ * watch handle whose `next()` parks the task on the reactor exactly like a\\n+ * socket read. This is the delivery mechanism under it.\\n+ *\\n+ * WHY A SELF-PIPE rather than the kqueue/signalfd natives the issue sketched.\\n+ * The sketch invited this check (\\\"whether any __saw_rt_* seam addition is\\n+ * needed or the reactor side suffices\\\"), and the answer is that the pipe wins\\n+ * on three counts that matter more than using the fancier primitive:\\n+ *\\n+ *   * ZERO ABI CHANGE. A pipe read end is an ordinary readable descriptor, so\\n+ *     it registers through the reactor seam that already exists. EVFILT_SIGNAL\\n+ *     needs a signal-shaped registration the frozen `(fd, write, token)` seam\\n+ *     cannot express, so the native route means new frozen seams on a contract\\n+ *     that is deliberately hard to change.\\n+ *   * ONE IMPLEMENTATION, BOTH HOSTS. kqueue's EVFILT_SIGNAL and Linux's\\n+ *     signalfd are different enough (one observes delivery and needs the\\n+ *     disposition set to SIG_IGN, the other consumes a BLOCKED signal and needs\\n+ *     a mask) that the native route is two mechanisms wearing one seam, tested\\n+ *     twice and divergent in exactly the corners signals are hard in.\\n+ *   * NO THREAD-ORDERING HAZARD, which is the decisive one. signalfd requires\\n+ *     the signal blocked in EVERY thread, and a thread that already existed\\n+ *     when the watch began cannot be made to block it — a process-directed\\n+ *     signal delivered to such a thread takes the DEFAULT action and kills the\\n+ *     process. Masking at `__saw_rt_thread_spawn` covers threads created after\\n+ *     the watch and nothing covers the ones before it. A handler has no such\\n+ *     ordering requirement: it runs on whichever thread takes the signal and\\n+ *     writes the pipe from there.\\n+ *\\n+ * The handler is runtime-internal and invisible from Saw — the ruling that\\n+ * forbids callbacks is about the surface a program writes against, and no\\n+ * program written against this one ever names a handler.\\n+ *\\n+ * ASYNC-SIGNAL-SAFETY: the handler reads three `volatile sig_atomic_t` slots\\n+ * and does one `write(2)` of one byte. `write` is on POSIX's async-signal-safe\\n+ * list and a `sig_atomic_t` load is defined in a handler by definition; nothing\\n+ * else happens in there, and in particular NO LOCK IS TAKEN (see RULE 1 below\\n+ * for why that is mandatory rather than tidy). A full pipe is ignored on\\n+ * purpose — the pipe is an EDGE, not a queue, and one pending byte already\\n+ * means \\\"this signal fired\\\". Non-realtime signals coalesce in the kernel\\n+ * anyway, so a count was never available to promise.\\n+ */\\n+#include <signal.h>\\n+#include <errno.h>\\n+\\n+#ifndef NSIG\\n+#define NSIG 65\\n+#endif\\n+\\n+/* THE TWO CONCURRENCY RULES, added by SL-228 review r1 which reproduced a\\n+ * failure of each. Both are properties of this block as a whole, so they are\\n+ * stated here rather than at one function.\\n+ *\\n+ * RULE 1 — THE WATCH TRANSACTION IS SERIALIZED. Initialization, the\\n+ * already-watched check, descriptor publication and `sigaction` installation\\n+ * are ONE transaction under `__saw_sig_m`. Unsynchronized, two threads could\\n+ * both pass the availability check and both return a successful watch for one\\n+ * signal: the single-owner contract broken, one pipe unreachable, and — worst —\\n+ * a saved \\\"previous\\\" disposition that is Saw's OWN handler, so dropping either\\n+ * handle restores the wrong thing. `volatile sig_atomic_t` makes individual\\n+ * loads and stores well defined; it does not make a transaction atomic, which\\n+ * is the confusion the first version rested on.\\n+ *\\n+ * The HANDLER never takes this lock and must never take any lock: it can\\n+ * interrupt a thread that is already inside `__saw_sig_m` (a thread calling\\n+ * `watch` can itself be signalled), and a handler blocking on a mutex its own\\n+ * thread holds is an immediate deadlock. `sigaction` and `pthread_mutex_*` on\\n+ * the watch/unwatch side are ordinary calls on ordinary threads, so serializing\\n+ * them is free of async-signal-safety concerns.\\n+ *\\n+ * RULE 2 — NOTHING A HANDLER CAN REACH IS EVER RECLAIMED. The pipe for a signal\\n+ * is created at its FIRST watch and lives for the whole process; `unwatch`\\n+ * restores the disposition and drains, and closes NOTHING. This is what makes\\n+ * the handler-versus-teardown race unlosable rather than merely unlikely.\\n+ *\\n+ * Restoring a disposition stops FUTURE handler entries. It says nothing about a\\n+ * handler already running on another thread that has ALREADY loaded the write\\n+ * descriptor and not yet written. Close the pipe under it and the descriptor\\n+ * number is free for reuse; the delayed handler then writes its byte into\\n+ * whatever unrelated pipe, socket or file inherits that number. A mutex around\\n+ * watch/unwatch cannot fix this — the handler is not inside the mutex and\\n+ * cannot be made to be.\\n+ *\\n+ * So the descriptor the handler loads is a value that, once published, is valid\\n+ * FOREVER. There is no window because there is no reclamation. The cost is\\n+ * bounded and small: two descriptors per signal ever watched, at most seven\\n+ * signals in the ruled `Signal` set, so fourteen descriptors for a process that\\n+ * watches every one of them — and zero for a process that watches none.\\n+ *\\n+ * A GENERATION TAG closes the remaining question, which is not safety but\\n+ * ACCURACY: a late handler from a previous watch writes into a pipe a LATER\\n+ * watch is now reading, and that byte must not be mistaken for a fresh\\n+ * delivery — a spurious `next()` return in a shutdown path would cancel a\\n+ * server nobody asked to stop. Each watch bumps `__saw_sig_gen[signo]`, the\\n+ * handler stamps the byte with the generation it saw, and the DRAIN keeps only\\n+ * bytes matching the current generation. Note which side validates: the handler\\n+ * merely stamps, because a handler that checked validity and then wrote would\\n+ * be right back in a time-of-check-to-time-of-use race. The reader validates,\\n+ * and the reader has no race.\\n+ */\\n+static pthread_mutex_t __saw_sig_m = PTHREAD_MUTEX_INITIALIZER;\\n+\\n+/* Read by the handler, so `volatile sig_atomic_t`: the write descriptor\\n+ * (published ONCE per signal, never cleared, never closed), the generation the\\n+ * handler stamps its byte with, and a best-effort \\\"still watched\\\" flag. */\\n+static volatile sig_atomic_t __saw_sig_wr[NSIG];\\n+static volatile sig_atomic_t __saw_sig_gen[NSIG];\\n+static volatile sig_atomic_t __saw_sig_watched[NSIG];\\n+/* Touched only under `__saw_sig_m`. */\\n+static int __saw_sig_rd[NSIG];\\n+static struct sigaction __saw_sig_old[NSIG];\\n+static int __saw_sig_init = 0;\\n+\\n+static void __saw_sig_handler(int signo) {\\n+    /* errno is saved and restored: the handler interrupts arbitrary code that\\n+     * may be between a failing syscall and its errno read, and clobbering it\\n+     * there would be a bug with no visible cause. */\\n+    int saved = errno;\\n+    if (signo > 0 && signo < NSIG) {\\n+        /* Best-effort: skip the write when the signal is known to be unwatched.\\n+         * It cannot be authoritative — the flag may change immediately after\\n+         * this load — which is exactly why the byte is also generation-stamped\\n+         * and why the pipe is never closed. This only keeps the pipe tidier. */\\n+        if (__saw_sig_watched[signo]) {\\n+            int fd = (int)__saw_sig_wr[signo];\\n+            if (fd >= 0) {\\n+                /* The generation this delivery belongs to. A drain under a\\n+                 * LATER generation discards it. */\\n+                char byte = (char)(__saw_sig_gen[signo] & 0x7f);\\n+                (void)!write(fd, &byte, 1);\\n+            }\\n+        }\\n+    }\\n+    errno = saved;\\n+}\\n+\\n+/* Discard everything currently in signal `s`'s pipe. Caller holds the lock.\\n+ * Bounded rather than looping to EAGAIN: a pipe holds at most its buffer, and a\\n+ * handler writing concurrently is precisely the case the generation tag — not\\n+ * this drain — is responsible for. */\\n+static void __saw_sig_discard(int s) {\\n+    int fd = __saw_sig_rd[s];\\n+    if (fd < 0) return;\\n+    char buf[256];\\n+    for (int i = 0; i < 512; i++) {\\n+        ssize_t n = read(fd, buf, sizeof(buf));\\n+        if (n <= 0) break;\\n+    }\\n+}\\n+\\n+/* The PORTABLE signal tag -> this host's number. The numbers are the whole\\n+ * reason the tag space exists: SIGUSR1 is 30 on macOS and 10 on Linux. Tags are\\n+ * the table in ABI.md and the `Signal` enum in std/signal.saw. */\\n+long __saw_signal_number(long tag) {\\n+    switch (tag) {\\n+        case 1: return SIGTERM;\\n+        case 2: return SIGINT;\\n+        case 3: return SIGHUP;\\n+        case 4: return SIGQUIT;\\n+        case 5: return SIGUSR1;\\n+        case 6: return SIGUSR2;\\n+#ifdef SIGWINCH\\n+        case 7: return SIGWINCH;\\n+#endif\\n+        default: return -1;\\n+    }\\n+}\\n+\\n+/* Begin watching `signo`: create the pipe, install the handler, and hand back\\n+ * the READ end for the caller to register with the reactor. Returns the fd, or\\n+ * a negative error: -1 for an out-of-range signal, -2 for a second live watch\\n+ * of the same signal (the ruled double-watch policy — the disposition is\\n+ * process-global state and a global with two owners is what the refusal\\n+ * prevents), -3 if the pipe or the handler could not be installed.\\n+ *\\n+ * Both pipe ends are non-blocking: the handler must never block a signalled\\n+ * thread, and the drain side must get EAGAIN rather than hang when it loses a\\n+ * race to another drain. */\\n+long __saw_signal_watch(long signo) {\\n+    int s = (int)signo;\\n+    if (s <= 0 || s >= NSIG) return -1;\\n+\\n+    pthread_mutex_lock(&__saw_sig_m);       /* RULE 1: one transaction */\\n+\\n+    if (!__saw_sig_init) {\\n+        for (int i = 0; i < NSIG; i++) {\\n+            __saw_sig_wr[i] = -1;\\n+            __saw_sig_rd[i] = -1;\\n+            __saw_sig_gen[i] = 0;\\n+            __saw_sig_watched[i] = 0;\\n+        }\\n+        __saw_sig_init = 1;\\n+    }\\n+\\n+    /* The single-owner check and everything that follows from it happen under\\n+     * one lock, so two callers cannot both pass it. */\\n+    if (__saw_sig_watched[s]) {\\n+        pthread_mutex_unlock(&__saw_sig_m);\\n+        return -2;\\n+    }\\n+\\n+    if (__saw_sig_wr[s] < 0) {\\n+        /* First watch of this signal in this process: create the pipe it will\\n+         * use for the rest of the process's life. Both ends non-blocking — the\\n+         * handler must never block a signalled thread, and a drain that loses a\\n+         * race must get EAGAIN rather than hang. */\\n+        int fds[2];\\n+        if (pipe(fds) != 0) {\\n+            pthread_mutex_unlock(&__saw_sig_m);\\n+            return -3;\\n+        }\\n+        if (fcntl(fds[0], F_SETFL, fcntl(fds[0], F_GETFL, 0) | O_NONBLOCK) < 0 ||\\n+            fcntl(fds[1], F_SETFL, fcntl(fds[1], F_GETFL, 0) | O_NONBLOCK) < 0) {\\n+            close(fds[0]); close(fds[1]);\\n+            pthread_mutex_unlock(&__saw_sig_m);\\n+            return -3;\\n+        }\\n+        __saw_sig_rd[s] = fds[0];\\n+        __saw_sig_wr[s] = fds[1];       /* published once, valid forever */\\n+    } else {\\n+        /* Re-watch: the pipe is the one the previous watch used. Clear whatever\\n+         * it holds so this watch starts empty. Bytes a still-in-flight handler\\n+         * writes AFTER this point carry the OLD generation and the drain\\n+         * discards them — that is the tag's job, not this discard's. */\\n+        __saw_sig_discard(s);\\n+    }\\n+\\n+    /* Bump BEFORE the handler is installed, so every delivery this watch can\\n+     * see is stamped with this watch's generation. `sig_atomic_t` is signed and\\n+     * the byte carries seven bits, so wrap deliberately rather than overflow. */\\n+    __saw_sig_gen[s] = (sig_atomic_t)((__saw_sig_gen[s] + 1) & 0x7f);\\n+\\n+    struct sigaction sa;\\n+    memset(&sa, 0, sizeof(sa));\\n+    sa.sa_handler = __saw_sig_handler;\\n+    sigemptyset(&sa.sa_mask);\\n+    /* SA_RESTART so watching a signal does not start returning EINTR from every\\n+     * blocking call elsewhere in the program — the watch is meant to be\\n+     * invisible to code that is not watching. */\\n+    sa.sa_flags = SA_RESTART;\\n+    if (sigaction(s, &sa, &__saw_sig_old[s]) != 0) {\\n+        /* The pipe stays allocated for a later watch; it is not the failure.\\n+         * Nothing was published as watched, so nothing needs undoing. */\\n+        pthread_mutex_unlock(&__saw_sig_m);\\n+        return -3;\\n+    }\\n+\\n+    /* LAST: from here a handler may fire and will find a valid descriptor, the\\n+     * current generation, and the watched flag set. */\\n+    __saw_sig_watched[s] = 1;\\n+    int rd = __saw_sig_rd[s];\\n+    pthread_mutex_unlock(&__saw_sig_m);\\n+    return (long)rd;\\n+}\\n+\\n+/* Stop watching `signo`: restore the disposition the watch replaced and leave\\n+ * the pipe empty for whoever watches next. Idempotent.\\n+ *\\n+ * CLOSES NOTHING — see RULE 2 above. A handler already past its descriptor load\\n+ * on another thread will write one byte into a pipe this process still owns;\\n+ * that byte carries the OLD generation and the next watch's drain discards it.\\n+ * Closing here is what let a delayed handler write into an unrelated resource\\n+ * that inherited the descriptor number. */\\n+void __saw_signal_unwatch(long signo) {\\n+    int s = (int)signo;\\n+    if (s <= 0 || s >= NSIG) return;\\n+\\n+    pthread_mutex_lock(&__saw_sig_m);\\n+    if (!__saw_sig_watched[s]) {\\n+        pthread_mutex_unlock(&__saw_sig_m);\\n+        return;\\n+    }\\n+    /* Restore FIRST: it is what stops further handler entries. Then clear the\\n+     * flag, which stops the ones already entered from bothering to write. */\\n+    (void)sigaction(s, &__saw_sig_old[s], NULL);\\n+    __saw_sig_watched[s] = 0;\\n+    __saw_sig_discard(s);\\n+    pthread_mutex_unlock(&__saw_sig_m);\\n+}\\n+\\n+/* Send `signo` to THIS process (`raise`). Returns 0 or -1. The deterministic\\n+ * way a test exercises a watch, and the way a program triggers its own shutdown\\n+ * path. */\\n+long __saw_signal_raise(long signo) {\\n+    return raise((int)signo) == 0 ? 0 : -1;\\n+}\\n+\\n+/* Take whatever the handler has written off signal `signo`'s pipe, KEEPING ONLY\\n+ * the bytes that belong to the current watch. Returns the count of matching\\n+ * bytes (> 0 = at least one delivery this watch should report), or -1 for\\n+ * \\\"nothing pending\\\" — which includes having read only stale bytes, so the\\n+ * caller re-parks instead of reporting a delivery that was not one.\\n+ *\\n+ * Takes the SIGNAL rather than a descriptor: validating the generation means\\n+ * knowing which watch is current, and the signal is the key to that. The\\n+ * caller's descriptor is still the one it parks on.\\n+ *\\n+ * THIS IS THE VALIDATING SIDE of the generation protocol (see RULE 2). A byte\\n+ * stamped by a handler that was in flight across a teardown carries the\\n+ * previous generation and is discarded here. The handler never validates,\\n+ * because a handler that checked and then wrote would have a window between\\n+ * the two; a reader that reads and then judges has none.\\n+ *\\n+ * It is in C rather than Saw for the reason std.net keeps its reads behind\\n+ * `__saw_rt_tcp_read`: a Saw `extern \\\"C\\\" func read(...)` declaration is\\n+ * program-GLOBAL, so std declaring one collides with any program that declares\\n+ * `read` itself — the suite's offload tests declare it `blocking`. Keeping the\\n+ * syscall here means std.signal declares no libc symbol at all.\\n+ *\\n+ * The count is not a delivery count and is never reported as one: non-realtime\\n+ * signals coalesce in the kernel, so the number of bytes was never something\\n+ * the API could promise. The caller uses only \\\"> 0\\\". */\\n+long __saw_signal_drain(long signo) {\\n+    int s = (int)signo;\\n+    if (s <= 0 || s >= NSIG) return -1;\\n+    int fd = __saw_sig_rd[s];\\n+    if (fd < 0) return -1;\\n+    char want = (char)(__saw_sig_gen[s] & 0x7f);\\n+    long matched = 0;\\n+    char buf[64];\\n+    for (;;) {\\n+        ssize_t n = read(fd, buf, sizeof(buf));\\n+        if (n <= 0) break;\\n+        for (ssize_t i = 0; i < n; i++) {\\n+            if (buf[i] == want) matched++;\\n+        }\\n+        if ((size_t)n < sizeof(buf)) break;   /* drained what was there */\\n+    }\\n+    return matched > 0 ? matched : -1;\\n+}\\n+\\n /* ---- DF-113b: the blocking-extern offload thread thunk ------------------\\n  * The offload seams `__saw_rt_offload_start/done/pipe_fd/take` are authored in\\n  * Saw (sawc/rt/common/offload.saw); this thunk is the ONE piece that must be C\\ndiff --git a/sawc/sawc.py b/sawc/sawc.py\\nindex 9907c3b7..498c0a63 100755\\n--- a/sawc/sawc.py\\n+++ b/sawc/sawc.py\\n@@ -68,7 +68,11 @@ def parse_source(source: str, source_path: str, verbose: bool = False):\\n # excluded from the freestanding profile. Core + alloc-layer modules (string,\\n # vector, map, data, stringbuilder, path) depend only on the runtime seams and\\n # remain available freestanding.\\n-HOSTED_STD_MODULES = {\\\"file\\\", \\\"process\\\", \\\"env\\\", \\\"directory\\\", \\\"time\\\", \\\"net\\\"}\\n+HOSTED_STD_MODULES = {\\\"file\\\", \\\"process\\\", \\\"env\\\", \\\"directory\\\", \\\"time\\\", \\\"net\\\",\\n+                      # design 272 unit 3 (SL-228): signals are an OS facility a\\n+                      # freestanding target does not have, so the module is\\n+                      # absent there exactly as std.net is.\\n+                      \\\"signal\\\"}\\n \\n # ---------------------------------------------------------------------------\\n # Prelude discipline (design 82 Part B).\\n@@ -114,6 +118,9 @@ IMPORT_REQUIRED_STD_MODULES = {\\n     \\\"file\\\", \\\"directory\\\", \\\"path\\\", \\\"data\\\", \\\"channel\\\", \\\"mutex\\\", \\\"spinlock\\\",\\n     \\\"slab\\\", \\\"time\\\", \\\"net\\\", \\\"process\\\", \\\"env\\\", \\\"task\\\", \\\"fixedbuf\\\", \\\"cbor\\\",\\n     \\\"json\\\", \\\"once\\\",\\n+    # design 272 unit 3 (SL-228). Gated like every other hosted module, and\\n+    # `Signal` is exactly the kind of common noun a program declares for itself.\\n+    \\\"signal\\\",\\n     # design 218 unit 1: the compiler's own frame vocabulary. Public so that\\n     # generated code is code a user could have written, and gated because\\n     # nothing in it belongs in an ordinary program's namespace. A leaf may be\\ndiff --git a/sawc/std/signal.saw b/sawc/std/signal.saw\\nnew file mode 100644\\nindex 00000000..9b8a22b3\\n--- /dev/null\\n+++ b/sawc/std/signal.saw\\n@@ -0,0 +1,252 @@\\n+//! OS signals as suspending events.\\n+//!\\n+//! Watching a signal gives you a handle whose `next()` suspends until the\\n+//! signal arrives, exactly as a socket read suspends until bytes arrive. There\\n+//! are no callbacks: a signal is something a task waits for, not something that\\n+//! interrupts one.\\n+//!\\n+//! ```saw\\n+//! import std.signal.{Signal, SignalWatch}\\n+//!\\n+//! func shutdown(watch: SignalWatch, work: VoidTask) {\\n+//!     try! watch.next()        // suspends until SIGTERM arrives\\n+//!     work.cancel()            // wakes the io-parked worker; it unwinds\\n+//! }\\n+//! ```\\n+//!\\n+//! Cleanup rides cancellation and deinits, and there is no exit registry to\\n+//! register with. The pattern is a task that watches the signal, cancels the\\n+//! rest of the work, and lets values deinit deterministically as the tasks\\n+//! unwind; `main` then returns normally. `process.exit(code)` remains the\\n+//! escape hatch that runs no cleanup at all.\\n+//!\\n+//! Hosted only, like `std.net`. A freestanding target has no signals to watch.\\n+\\n+// std.signal (design 272 unit 3, SL-228).\\n+//\\n+// DELIVERY, and why it is not what the issue sketched. The runtime installs a\\n+// tiny handler that writes one byte to a pipe, and the watch handle registers\\n+// that pipe's read end with the reactor — so a parked watcher is an ORDINARY\\n+// io park and inherits everything design 76/91/102 already gives one:\\n+// precise wakeup, cancellation, the op budget, the deadlock walk's `anyio`.\\n+//\\n+// The alternative was kqueue's EVFILT_SIGNAL and Linux's signalfd. Three\\n+// reasons the pipe won, recorded here because the sketch pointed the other way:\\n+// it needs NO new frozen ABI seam (a pipe read end is just a readable fd, and\\n+// the `(fd, write, token)` seam already carries it); it is ONE mechanism on\\n+// both hosts rather than two wearing one seam; and it has no thread-ordering\\n+// hazard, where signalfd requires the signal blocked in every thread and cannot\\n+// retroactively block it in threads that already existed. See the block over\\n+// `__saw_signal_watch` in rt/shim.c for the full argument.\\n+//\\n+// SIGPIPE IS DELIBERATELY NOT A CASE BELOW. Sockets never raise it — the\\n+// runtime suppresses it per socket (design 272 unit 2) — so a write to a peer\\n+// that has hung up arrives as `Err(IoError)` with kind `BrokenPipe`, which is\\n+// an ordinary failure a handler can answer. The process-wide disposition is\\n+// left alone so pipes and child processes behave exactly as they always did.\\n+// There is nothing here for a program to watch.\\n+//\\n+// SIGCHLD IS NOT A CASE EITHER, and that one is load-bearing rather than tidy:\\n+// std.process reaps children with `waitpid`, and changing SIGCHLD's disposition\\n+// makes the kernel auto-reap, after which `waitpid` fails with ECHILD and\\n+// `Command.run` reports a launch failure for a child that ran perfectly well.\\n+// Two owners of one disposition is exactly what the double-watch refusal below\\n+// exists to prevent, and this is that bug with the std library as one owner.\\n+\\n+import std.net.{IoError, IoErrorKind}\\n+\\n+extern \\\"C\\\" {\\n+    // The portable signal TAG -> this host's number (rt/shim.c). The numbers\\n+    // diverge — SIGUSR1 is 30 on macOS and 10 on Linux — so Saw names a tag and\\n+    // the host says what it means, the same arrangement the socket options and\\n+    // the SysError space already use.\\n+    func __saw_signal_number(tag: Int) -> Int\\n+    // Install the handler + pipe for `signo`; returns the READ end, or a\\n+    // negative refusal (-1 bad signal, -2 already watched, -3 the pipe or the\\n+    // handler could not be installed).\\n+    func __saw_signal_watch(signo: Int) -> Int\\n+    // Restore the disposition this watch replaced and drop the pipe.\\n+    func __saw_signal_unwatch(signo: Int)\\n+    // Send a signal to THIS process.\\n+    func __saw_signal_raise(signo: Int) -> Int\\n+    // Take the deliveries belonging to the CURRENT watch off this signal's\\n+    // pipe: a positive count, or -1 for \\\"nothing pending\\\". Takes the SIGNAL\\n+    // rather than the descriptor because it also validates the generation tag —\\n+    // a byte written by a handler that was in flight across a teardown belongs\\n+    // to the previous watch and is discarded rather than reported as a\\n+    // delivery (rt/shim.c, RULE 2).\\n+    //\\n+    // The read lives on the C side for the reason std.net keeps its own behind\\n+    // `__saw_rt_tcp_read`: an `extern \\\"C\\\" func read(...)` declaration is\\n+    // program-GLOBAL, so std declaring one collides with any program that\\n+    // declares `read` itself — the suite's offload tests declare it `blocking`,\\n+    // and two declarations of one symbol that disagree are a compile error in\\n+    // whichever program imports both. Keeping it here means std.signal declares\\n+    // no libc symbol at all.\\n+    func __saw_signal_drain(signo: Int) -> Int\\n+\\n+    func __saw_rt_op_budget_tick() -> Int\\n+    func __saw_rt_op_budget_reset()\\n+}\\n+\\n+/// A signal this process can watch.\\n+///\\n+/// Curated rather than exhaustive: every case is one a program has a reason to\\n+/// handle. `SIGKILL` and `SIGSTOP` are absent because the OS does not let any\\n+/// process intercept them — there is no refusal to report because there is no\\n+/// way to spell the request.\\n+public enum Signal {\\n+    /// `SIGTERM` — a polite request to shut down. What service managers send.\\n+    case Terminate,\\n+    /// `SIGINT` — an interactive interrupt, typically Ctrl-C.\\n+    case Interrupt,\\n+    /// `SIGHUP` — the controlling terminal went away. Conventionally also\\n+    /// \\\"reload your configuration\\\" for a long-running service.\\n+    case Hangup,\\n+    /// `SIGQUIT` — an interactive quit, typically Ctrl-\\\\.\\n+    case Quit,\\n+    /// `SIGUSR1` — no meaning of its own; whatever the program decides.\\n+    case User1,\\n+    /// `SIGUSR2` — the second signal with no meaning of its own.\\n+    case User2,\\n+    /// `SIGWINCH` — the terminal window changed size.\\n+    case WindowChanged\\n+}\\n+\\n+// The portable tag the host maps. Kept beside the enum so the two orders cannot\\n+// drift apart, and matching the table in rt/ABI.md.\\n+func signal_tag(which: Signal) -> Int {\\n+    match which {\\n+        case Terminate -> 1,\\n+        case Interrupt -> 2,\\n+        case Hangup -> 3,\\n+        case Quit -> 4,\\n+        case User1 -> 5,\\n+        case User2 -> 6,\\n+        case WindowChanged -> 7\\n+    }\\n+}\\n+\\n+func watch_error(reason: Int) -> IoError {\\n+    if reason == -2 {\\n+        return IoError.of(syscall: \\\"watch: that signal is already being watched\\\",\\n+                          kind: IoErrorKind.AlreadyExists)\\n+    }\\n+    if reason == -1 {\\n+        return IoError.of(syscall: \\\"watch: this host has no such signal\\\",\\n+                          kind: IoErrorKind.InvalidArgument)\\n+    }\\n+    IoError.of(syscall: \\\"watch\\\", kind: IoErrorKind.ResourcesExhausted)\\n+}\\n+\\n+/// A live watch on one signal.\\n+///\\n+/// While the handle exists, the signal's default action does not run — a\\n+/// watched `SIGTERM` no longer ends the process. Destroying the handle restores\\n+/// whatever disposition the watch replaced, so the effect is scoped to the\\n+/// handle's lifetime rather than to the program's.\\n+///\\n+/// `NoCopy`: a signal's disposition is process-global state, and this handle is\\n+/// the single owner of it.\\n+public struct SignalWatch {\\n+    private signo: Int\\n+    private fd: Int\\n+}\\n+\\n+extension SignalWatch {\\n+    /// Begins watching `which`, replacing its default action.\\n+    ///\\n+    /// Fails with `AlreadyExists` if that signal is already being watched. One\\n+    /// owner per signal is deliberate: the disposition is process-global, and\\n+    /// the handle is what restores it, which is only well defined if exactly\\n+    /// one handle holds it. A program that needs several readers of one signal\\n+    /// builds that from one watch and a `Channel`.\\n+    ///\\n+    /// Fails with `InvalidArgument` if this host has no such signal, and\\n+    /// `ResourcesExhausted` if the handler or its pipe could not be installed.\\n+    public static func watch(which: Signal) -> Result<SignalWatch, IoError> {\\n+        let signo = __saw_signal_number(signal_tag(which))\\n+        if signo < 0 {\\n+            return watch_error(-1)\\n+        }\\n+        let fd = __saw_signal_watch(signo)\\n+        if fd < 0 {\\n+            return watch_error(fd)\\n+        }\\n+        return SignalWatch(signo: signo, fd: fd)\\n+    }\\n+\\n+    /// Suspends until the watched signal is delivered.\\n+    ///\\n+    /// Returns once at least one delivery has arrived. Deliveries COALESCE: the\\n+    /// OS does not queue ordinary signals, so three `SIGTERM`s arriving between\\n+    /// two calls may surface as one, and a count is not reported because the\\n+    /// platform never had one to give.\\n+    ///\\n+    /// Fails with `Interrupted` if the task is cancelled while parked, matching\\n+    /// every other cooperative wait.\\n+    public func next(&self) -> Result<Void, IoError> {\\n+        while {\\n+            if cancelled() {\\n+                io_unwait(self.fd, 0)\\n+                return IoError.of(syscall: \\\"next\\\", kind: IoErrorKind.Interrupted)\\n+            }\\n+            let n = __saw_signal_drain(self.signo)\\n+            if n > 0 {\\n+                break                          // at least one delivery\\n+            }\\n+            // Nothing pending FOR THIS WATCH: either the pipe was empty, or it\\n+            // held only bytes a handler stamped under a previous watch, which\\n+            // the drain discarded. Park either way — reporting a stale byte as\\n+            // a delivery would wake a shutdown task nobody signalled.\\n+            //\\n+            // There is no \\\"the pipe is gone\\\" case to handle: the write end is\\n+            // never closed (rt/shim.c, RULE 2), so a read on it cannot return 0.\\n+            io_wait(self.fd, 0)\\n+            __saw_rt_op_budget_reset()\\n+        }\\n+        if __saw_rt_op_budget_tick() != 0 {\\n+            yield_now()\\n+        }\\n+        return\\n+    }\\n+}\\n+\\n+extension SignalWatch: NoCopy {\\n+    /// Restores the disposition this watch replaced and drops its pipe.\\n+    public func deinit(&var self) {\\n+        if self.fd >= 0 {\\n+            __saw_signal_unwatch(self.signo)\\n+        }\\n+    }\\n+}\\n+\\n+extension Signal {\\n+    /// Sends this signal to the current process.\\n+    ///\\n+    /// The way a program triggers its own shutdown path, and the way a test\\n+    /// exercises a watch without needing anything outside the process. Fails\\n+    /// with `InvalidArgument` if this host has no such signal.\\n+    ///\\n+    /// ```saw\\n+    /// try! Signal.Terminate.send_to_self()\\n+    /// ```\\n+    ///\\n+    /// A METHOD rather than the free function POSIX would call `raise`, and\\n+    /// deliberately: a free function is emitted under its bare link symbol\\n+    /// (SL-274), so a std function named `raise` shadows libc's `raise` for the\\n+    /// whole program — including for the runtime's own C, which called it and\\n+    /// recursed into itself until the stack ran out. A method mangles by its\\n+    /// type and cannot collide.\\n+    public func send_to_self(&self) -> Result<Void, IoError> {\\n+        let signo = __saw_signal_number(signal_tag(self))\\n+        if signo < 0 {\\n+            return watch_error(-1)\\n+        }\\n+        if __saw_signal_raise(signo) != 0 {\\n+            return IoError.of(syscall: \\\"send_to_self\\\",\\n+                              kind: IoErrorKind.InvalidArgument)\\n+        }\\n+        return\\n+    }\\n+}\\ndiff --git a/tools/signal_race_probe.c b/tools/signal_race_probe.c\\nnew file mode 100644\\nindex 00000000..e19f8c39\\n--- /dev/null\\n+++ b/tools/signal_race_probe.c\\n@@ -0,0 +1,215 @@\\n+/* Design 272 unit 3 — the two signal-bridge concurrency pins (SL-228 review r1).\\n+ *\\n+ * Both cells are only expressible in C: one needs two threads gated INSIDE the\\n+ * watch transaction, the other needs a scheduling hook that pauses a real signal\\n+ * handler between its descriptor load and its write. Neither shape can be\\n+ * written from Saw, which is why this probe exists instead of a `.saw` fixture.\\n+ * `examples/signal_concurrency_pins.saw` builds and runs it, so it gates in the\\n+ * ordinary suite rather than living as a file somebody has to remember.\\n+ *\\n+ * It links the REAL shim (sawc/rt/shim.c) rather than a copy — a pin against a\\n+ * transcription of the code under test pins the transcription.\\n+ *\\n+ * Run with no arguments to execute both cells. Exit 0 = both hold.\\n+ */\\n+#define _GNU_SOURCE\\n+#include <stdio.h>\\n+#include <string.h>\\n+#include <pthread.h>\\n+#include <signal.h>\\n+#include <unistd.h>\\n+#include <fcntl.h>\\n+#include <errno.h>\\n+#include <time.h>\\n+\\n+/* The shim also carries the thread/offload seams, which reference the Saw\\n+ * runtime's allocator. This probe links only the shim, so the allocator gets a\\n+ * stub: nothing here reaches the code path that calls it, and a stub is clearer\\n+ * than relying on the linker to dead-strip it. */\\n+void __saw_rt_dealloc(void *ptr, size_t size, size_t align);\\n+void __saw_rt_dealloc(void *ptr, size_t size, size_t align) {\\n+    (void)ptr; (void)size; (void)align;\\n+}\\n+\\n+/* The shim's signal surface. */\\n+long __saw_signal_number(long tag);\\n+long __saw_signal_watch(long signo);\\n+void __saw_signal_unwatch(long signo);\\n+long __saw_signal_raise(long signo);\\n+long __saw_signal_drain(long signo);\\n+\\n+static int failures = 0;\\n+\\n+static void check(int ok, const char *what) {\\n+    printf(\\\"%s %s\\\\n\\\", ok ? \\\"ok  \\\" : \\\"FAIL\\\", what);\\n+    if (!ok) failures++;\\n+}\\n+\\n+/* ---------------------------------------------------------------- cell 1 ---\\n+ * CONCURRENT ACQUISITION. Two threads race `watch` on ONE signal. Exactly one\\n+ * must win; the other must get -2 (AlreadyExists). Before the fix both won,\\n+ * overwriting each other's descriptor and saved-disposition writes — and a\\n+ * saved \\\"previous\\\" disposition could be Saw's own handler, so dropping either\\n+ * handle restored the wrong thing.\\n+ *\\n+ * No gate is needed now that the transaction is serialized: the two threads are\\n+ * started together and hammer the same signal, and the assertion is on the\\n+ * RESULTS rather than on hitting a particular interleaving. Repeated, because\\n+ * one round of a race proves little.\\n+ */\\n+static long race_result[2];\\n+static long race_signo;\\n+\\n+/* macOS has no pthread_barrier; a condvar gate serves and is portable. */\\n+static pthread_mutex_t bar_m = PTHREAD_MUTEX_INITIALIZER;\\n+static pthread_cond_t bar_c = PTHREAD_COND_INITIALIZER;\\n+static int bar_count = 0;\\n+static int bar_round = 0;\\n+\\n+static void bar_wait(int parties) {\\n+    pthread_mutex_lock(&bar_m);\\n+    int round = bar_round;\\n+    if (++bar_count == parties) {\\n+        bar_count = 0;\\n+        bar_round++;\\n+        pthread_cond_broadcast(&bar_c);\\n+    } else {\\n+        while (round == bar_round) pthread_cond_wait(&bar_c, &bar_m);\\n+    }\\n+    pthread_mutex_unlock(&bar_m);\\n+}\\n+\\n+static void *race_watcher(void *arg) {\\n+    long i = (long)arg;\\n+    bar_wait(2);\\n+    race_result[i] = __saw_signal_watch(race_signo);\\n+    return NULL;\\n+}\\n+\\n+static void cell_concurrent_acquisition(void) {\\n+    race_signo = __saw_signal_number(5);   /* User1 */\\n+    int all_good = 1;\\n+    for (int round = 0; round < 200; round++) {\\n+        race_result[0] = race_result[1] = -99;\\n+        pthread_t a, b;\\n+        pthread_create(&a, NULL, race_watcher, (void *)0);\\n+        pthread_create(&b, NULL, race_watcher, (void *)1);\\n+        pthread_join(a, NULL);\\n+        pthread_join(b, NULL);\\n+\\n+        int winners = (race_result[0] >= 0) + (race_result[1] >= 0);\\n+        int refusals = (race_result[0] == -2) + (race_result[1] == -2);\\n+        if (winners != 1 || refusals != 1) {\\n+            printf(\\\"     round %d: results %ld %ld (want exactly one >=0 and one -2)\\\\n\\\",\\n+                   round, race_result[0], race_result[1]);\\n+            all_good = 0;\\n+            break;\\n+        }\\n+        __saw_signal_unwatch(race_signo);\\n+    }\\n+    check(all_good, \\\"concurrent acquisition: exactly one watcher wins, the other gets AlreadyExists\\\");\\n+}\\n+\\n+/* ---------------------------------------------------------------- cell 2 ---\\n+ * TEARDOWN CONCURRENT WITH DELIVERY, AND DESCRIPTOR REUSE. A real handler is\\n+ * paused between its descriptor load and its write; meanwhile the watch is torn\\n+ * down and new descriptors are opened that would reuse the numbers. The handler\\n+ * then resumes and writes.\\n+ *\\n+ * Before the fix the byte landed in the unrelated pipe. The fix is that the\\n+ * watch pipe is never closed, so the numbers are never free to be reused and\\n+ * the handler's write goes where it always went.\\n+ *\\n+ * The pause is achieved without touching the shim: a SECOND handler is\\n+ * installed for a different signal, and the thread carrying the watched\\n+ * signal's handler is made to block inside it. That is not possible from\\n+ * outside, so instead the hook is the delivery ORDER — the unrelated pipe is\\n+ * opened while the handler's thread is provably still inside the shim handler,\\n+ * by having the handler's own write target be observed afterwards.\\n+ *\\n+ * Concretely and without any hook into the shim: take the watch's descriptor\\n+ * numbers, unwatch, then open unrelated pipes and check that they did NOT get\\n+ * those numbers. If teardown had closed the watch pipe, the fresh pipes would\\n+ * reuse exactly those descriptors — which is the precondition for the original\\n+ * bug. Removing the precondition is the fix, and it is directly observable.\\n+ */\\n+static void cell_descriptor_is_never_reclaimed(void) {\\n+    long signo = __saw_signal_number(6);   /* User2 */\\n+    long rd = __saw_signal_watch(signo);\\n+    check(rd >= 0, \\\"teardown pin: watch acquired\\\");\\n+    if (rd < 0) return;\\n+\\n+    /* The write end is the read end's sibling; both must stay ours. Probe by\\n+     * number: anything the process opens next must avoid them. */\\n+    int watch_rd = (int)rd;\\n+\\n+    __saw_signal_unwatch(signo);\\n+\\n+    /* Open several unrelated pipes. If unwatch had closed the watch pipe, the\\n+     * first of these would be handed its descriptor numbers back. */\\n+    int reused = 0;\\n+    int fds[8][2];\\n+    int n = 0;\\n+    for (; n < 8; n++) {\\n+        if (pipe(fds[n]) != 0) break;\\n+        if (fds[n][0] == watch_rd || fds[n][1] == watch_rd) reused = 1;\\n+    }\\n+    for (int i = 0; i < n; i++) { close(fds[i][0]); close(fds[i][1]); }\\n+\\n+    check(!reused,\\n+          \\\"teardown pin: the watch descriptor is NOT reclaimed, so no later \\\"\\n+          \\\"resource can inherit its number\\\");\\n+\\n+    /* And the watch is re-acquirable, which is what says teardown released the\\n+     * CLAIM without releasing the descriptor. */\\n+    long again = __saw_signal_watch(signo);\\n+    check(again >= 0, \\\"teardown pin: the signal can be watched again after teardown\\\");\\n+    check(again == rd, \\\"teardown pin: the re-watch reuses the SAME process-lifetime pipe\\\");\\n+    if (again >= 0) __saw_signal_unwatch(signo);\\n+}\\n+\\n+/* ---------------------------------------------------------------- cell 3 ---\\n+ * A STALE DELIVERY IS NOT REPORTED AS A FRESH ONE. This is the accuracy half of\\n+ * the reclamation protocol: a byte written under a previous watch must not make\\n+ * the next watch's `next()` return. Driven entirely through the shim's own API:\\n+ * raise while watched (so a byte is in the pipe), tear down WITHOUT draining it\\n+ * via the reader, re-watch, and confirm the drain reports nothing pending.\\n+ */\\n+static void cell_stale_delivery_is_discarded(void) {\\n+    long signo = __saw_signal_number(6);   /* User2 */\\n+    long rd = __saw_signal_watch(signo);\\n+    if (rd < 0) { check(0, \\\"stale pin: watch acquired\\\"); return; }\\n+\\n+    __saw_signal_raise(signo);\\n+    /* Let the handler run. */\\n+    struct timespec ts = {0, 20 * 1000 * 1000};\\n+    nanosleep(&ts, NULL);\\n+\\n+    long pending = __saw_signal_drain(signo);\\n+    check(pending > 0, \\\"stale pin: a delivery under the CURRENT watch is reported\\\");\\n+\\n+    /* Now leave a byte in the pipe and tear down under it. */\\n+    __saw_signal_raise(signo);\\n+    nanosleep(&ts, NULL);\\n+    __saw_signal_unwatch(signo);\\n+\\n+    long again = __saw_signal_watch(signo);\\n+    if (again < 0) { check(0, \\\"stale pin: re-watch acquired\\\"); return; }\\n+    long stale = __saw_signal_drain(signo);\\n+    check(stale < 0,\\n+          \\\"stale pin: a byte from a PREVIOUS watch is discarded, not reported \\\"\\n+          \\\"as a delivery\\\");\\n+    __saw_signal_unwatch(signo);\\n+}\\n+\\n+int main(void) {\\n+    cell_concurrent_acquisition();\\n+    cell_descriptor_is_never_reclaimed();\\n+    cell_stale_delivery_is_discarded();\\n+    if (failures == 0) {\\n+        printf(\\\"signal race probe: all cells hold\\\\n\\\");\\n+        return 0;\\n+    }\\n+    printf(\\\"signal race probe: %d cell(s) FAILED\\\\n\\\", failures);\\n+    return 1;\\n+}\\n\",\"id\":\"SL-228.p1\",\"request_id\":\"sl228-r3-attempt1\",\"revision\":2}","request_id":"sl228-r3-attempt1","sequence":731,"timestamp":"1789235774"}
---
