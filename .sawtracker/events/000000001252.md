---
{"action":"create","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790176271","description":"Found by the SL-353 sweep (Sep 23; SL-353's sweep-report comment, 'secondary finding'). The registration funnel `__saw_exec_io_register` (sawc/std/taskgroup.saw:566-568) → `rt_reactor_register` (sawc/rt/host_macos/reactor.saw:111-119) ignores `kevent`'s return at reactor.saw:105, 118, 142, 150 and returns `Void`, so a failed arm (bad fd, EBADF, ENOMEM) leaves the task parked forever with the same signature as SL-353's race — the investigation had to spend a probe (abort on any kevent < 0) to rule it out. Doctrine: never hide errors. Fix shape (folded into the SL-353 fix brief as R6): the funnel reports a failed arm and the executor panics naming the fd and errno — a failed registration is an invariant violation, not a recoverable condition; a pin injects a closed fd and expects the panic. Linux host_linux/reactor.saw to be checked for the same pattern.","id":"SL-354","labels":["bug","runtime","doctrine"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1252,"stage":"backlog","status":"open","title":"never-hide-errors gap: the reactor arm discards kevent's return (reactor.saw:105/118/142/150) and __saw_exec_io_register returns Void — a failed registration is unreportable and presents exactly as SL-353's lost wake","updated":"1790176271"},"before":null,"id":"SL-354","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"description\":\"Found by the SL-353 sweep (Sep 23; SL-353's sweep-report comment, 'secondary finding'). The registration funnel `__saw_exec_io_register` (sawc/std/taskgroup.saw:566-568) → `rt_reactor_register` (sawc/rt/host_macos/reactor.saw:111-119) ignores `kevent`'s return at reactor.saw:105, 118, 142, 150 and returns `Void`, so a failed arm (bad fd, EBADF, ENOMEM) leaves the task parked forever with the same signature as SL-353's race — the investigation had to spend a probe (abort on any kevent < 0) to rule it out. Doctrine: never hide errors. Fix shape (folded into the SL-353 fix brief as R6): the funnel reports a failed arm and the executor panics naming the fd and errno — a failed registration is an invariant violation, not a recoverable condition; a pin injects a closed fd and expects the panic. Linux host_linux/reactor.saw to be checked for the same pattern.\",\"labels\":[\"bug\",\"runtime\",\"doctrine\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"pid-73874-1790176271-9000\",\"stage\":\"backlog\",\"title\":\"never-hide-errors gap: the reactor arm discards kevent's return (reactor.saw:105/118/142/150) and __saw_exec_io_register returns Void — a failed registration is unreportable and presents exactly as SL-353's lost wake\"}","request_id":"pid-73874-1790176271-9000","sequence":1252,"timestamp":"1790176271"}
---
