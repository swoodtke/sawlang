---
{"action":"create","actor":"agent:claude-air-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-air-sawlang","closed":"","comments":[],"created":"1790196968","description":"Found by an SL-357 wave-3 comment agent on Sep 23; I reproduced it on main (.build/scratch/unsigned_compound.saw).\n\n| Operation | Result | Correct |\n|---|---|---|\n| `var a: UInt8 = 200; a /= 3` | 238 | 66 |\n| `c %= 7` (c: UInt8 = 200) | 0 | 4 |\n| `UInt64 18446744073709551600 /= 2` | 18446744073709551608 | 9223372036854775800 |\n\nThe binary `/` and `%` operators give the right answers (operators.py picks udiv/urem).\n\n**Mechanism:** `codegen/statements.py _apply_compound_op(op, left, right, signed)` uses `signed` only to decide whether to emit the INT_MIN/-1 overflow check, then emits `sdiv`/`srem` unconditionally. Every compound-assignment position funnels through this one function:\n- the local, field, index and deref forms in statements.py (5 call sites);\n- the optional-chain `x?.f /= y` path in optionals.py.\n\nSo the class is these two branches of one funnel. The other compound ops already respect `signed`: `+`/`-`/`*` go through `_checked_arith`, shifts through `_emit_shift`, and bitwise ops are sign-agnostic.\n\n**Fix:** udiv/urem when `not signed`, mirroring operators.py.\n\n**Pins:** `/=` and `%=` for UInt8, UInt16, UInt32, UInt64 and UInt, each in local, struct-field, Vector-index and optional-chain position; a signed control row; divide-by-zero still panics.\n\nSilent wrong code, so high priority. Fix-on-discovery; the implementer is claude-air-sawlang.","id":"SL-370","labels":["bug","codegen","miscompile"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":1,"sequence":1359,"stage":"backlog","status":"open","title":"Unsigned compound division miscompiles: x /= y and x %= y emit sdiv/srem for unsigned types (UInt8 200 /= 3 gives 238; binary / is correct)","updated":"1790196968"},"before":null,"id":"SL-370","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-air-sawlang\",\"body\":\"\",\"description\":\"Found by an SL-357 wave-3 comment agent on Sep 23; I reproduced it on main (.build/scratch/unsigned_compound.saw).\\n\\n| Operation | Result | Correct |\\n|---|---|---|\\n| `var a: UInt8 = 200; a /= 3` | 238 | 66 |\\n| `c %= 7` (c: UInt8 = 200) | 0 | 4 |\\n| `UInt64 18446744073709551600 /= 2` | 18446744073709551608 | 9223372036854775800 |\\n\\nThe binary `/` and `%` operators give the right answers (operators.py picks udiv/urem).\\n\\n**Mechanism:** `codegen/statements.py _apply_compound_op(op, left, right, signed)` uses `signed` only to decide whether to emit the INT_MIN/-1 overflow check, then emits `sdiv`/`srem` unconditionally. Every compound-assignment position funnels through this one function:\\n- the local, field, index and deref forms in statements.py (5 call sites);\\n- the optional-chain `x?.f /= y` path in optionals.py.\\n\\nSo the class is these two branches of one funnel. The other compound ops already respect `signed`: `+`/`-`/`*` go through `_checked_arith`, shifts through `_emit_shift`, and bitwise ops are sign-agnostic.\\n\\n**Fix:** udiv/urem when `not signed`, mirroring operators.py.\\n\\n**Pins:** `/=` and `%=` for UInt8, UInt16, UInt32, UInt64 and UInt, each in local, struct-field, Vector-index and optional-chain position; a signed control row; divide-by-zero still panics.\\n\\nSilent wrong code, so high priority. Fix-on-discovery; the implementer is claude-air-sawlang.\",\"labels\":[\"bug\",\"codegen\",\"miscompile\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"pid-79581-1790196968-15000\",\"title\":\"Unsigned compound division miscompiles: x /= y and x %= y emit sdiv/srem for unsigned types (UInt8 200 /= 3 gives 238; binary / is correct)\"}","request_id":"pid-79581-1790196968-15000","sequence":1359,"timestamp":"1790196968"}
---
