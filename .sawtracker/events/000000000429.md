---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-244.p1 (revision 1): minivm M1: lexer numeric foundation (branch commit f97585f9)","created":"1789087544","id":"c1"}],"created":"1789086325","description":"SL-242 epic child, landing patch 2/14. Signed/unsigned 8-64-bit + Byte, literal bases + suffixes, checked as casts, truncating conversions, wrapping ops, checked shifts. Contract: prototypes/minivm/M1_NUMBERS.md. Patch = branch commit f97585f9.","id":"SL-244","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":429,"stage":"queued","status":"open","title":"minivm M1: lexer numeric foundation — full integer type lattice, literals, casts, wrapping/bitwise/shifts","updated":"1789087544"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086325","description":"SL-242 epic child, landing patch 2/14. Signed/unsigned 8-64-bit + Byte, literal bases + suffixes, checked as casts, truncating conversions, wrapping ops, checked shifts. Contract: prototypes/minivm/M1_NUMBERS.md. Patch = branch commit f97585f9.","id":"SL-244","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":406,"stage":"queued","status":"open","title":"minivm M1: lexer numeric foundation — full integer type lattice, literals, casts, wrapping/bitwise/shifts","updated":"1789086325"},"id":"SL-244","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/DESIGN.md b/prototypes/minivm/DESIGN.md\\nindex 2e664da2..0949eaf8 100644\\n--- a/prototypes/minivm/DESIGN.md\\n+++ b/prototypes/minivm/DESIGN.md\\n@@ -15,6 +15,10 @@ paths, with explicit expected outputs as a third oracle. Compile-time rejection\\n fixtures must have a located error. Runtime overflow and division-by-zero fixtures\\n must fail consistently. This does not claim the prototype can compile itself.\\n \\n+The original Int32-only contract below records the baseline.\\n+[M1_NUMBERS.md](M1_NUMBERS.md) supersedes its numeric types, operations, default\\n+literal type, and signed-minimum remainder behavior.\\n+\\n ## Source contract\\n \\n * One source file; reuse selfhost/lexer via --module-path sawlex=selfhost/lexer.\\ndiff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nnew file mode 100644\\nindex 00000000..9cfb92da\\n--- /dev/null\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -0,0 +1,219 @@\\n+# Lexer dependency inventory\\n+\\n+Scope: the source to admit is `selfhost/lexer/src/lib.saw` (1,215 lines at the\\n+`5af086c4` baseline). This inventory is intentionally narrower than general Saw\\n+support. A feature is included only when that file uses it, or when it is an\\n+unavoidable implementation dependency of one of the public std operations it\\n+calls. The existing mini-VM accepts only scalar `Int32`/`Bool` programs; its\\n+README explicitly excludes strings, collections, references, generics, and\\n+methods. The items below are therefore outstanding unless a milestone says they\\n+are already exercised by the current 43-case baseline.\\n+\\n+## Exact source surface used by the lexer\\n+\\n+### Declarations, names, and construction\\n+\\n+- Module-scope typed constants: the `static B_*: Int = ...` declarations at\\n+  `lib.saw:17-76`. They require `Int`, including negative literals/values, rather\\n+  than the prototype's source-only `Int32`. Constants are referenced from any\\n+  function and must be initialized as compile-time scalar constants; mutable or\\n+  dynamically initialized statics are not needed.\\n+- Payload-free public enum `TokenKind` at `lib.saw:133-235`, with qualified case\\n+  names (`TokenKind.IntLit`) and unqualified cases in `match`. Equality and\\n+  inequality on this enum are required (`lib.saw:985` and elsewhere). Numeric\\n+  discriminants, payload cases, raw-value APIs, and user-defined enum layout are\\n+  not required by this lexer.\\n+- Public field structs `Token`, `LexError`, `DocComment`, and `LexResult` at\\n+  `lib.saw:242-279`, plus private `Lexer` at `lib.saw:559-565`. Construction uses\\n+  order-independent named fields, field reads, and mutable field assignment\\n+  through `&var self` (`advance`, `lib.saw:576-586`). Visibility must parse and\\n+  type-check; cross-module consumers need the four public models and their public\\n+  fields. Private fields and custom initializers are unnecessary here.\\n+- `extension LexResult: NoCopy {}` (`lib.saw:279`) and `extension Lexer { ... }`\\n+  (`lib.saw:567-1189`). The former must suppress implicit copying; the latter\\n+  supplies instance methods with `&self` and `&var self` receivers. Traits in\\n+  general and extension dispatch across arbitrary types are outside this slice.\\n+- Public and private free functions, expression-bodied functions, implicit tail\\n+  returns, explicit `return`, and named arguments (all struct construction and\\n+  `Scalar(value: cp)` at `lib.saw:699`). Default arguments, closures, overload\\n+  declarations in user code, and function values are unused.\\n+\\n+### Types, operators, and conversions\\n+\\n+- Primitive types: `Int`, `Bool`, `String`, `Void`, `Byte`, `UInt`, `UInt8`,\\n+  `UInt16`, and `UInt32`. Only `Int` arithmetic is performed by lexer code:\\n+  checked `+ - * / %`, comparisons, and assignment forms `+=`/`-=`\\n+  (`lib.saw:576-586`, `985`, `1104-1117`). `UInt` only receives `String.to_uint`\\n+  results and participates in `<=`; fixed unsigned types are used for their\\n+  `max` constants and casts. No source floating-point arithmetic is needed even\\n+  though the lexer emits `FloatLit` tokens.\\n+- Static primitive constants `UInt8.max`, `UInt16.max`, and `UInt32.max`, then\\n+  widening casts `as UInt` (`literal_fits`, `lib.saw:539-553`). `UInt.max` and\\n+  `Int.max` are not directly used by this source.\\n+- Checked widening `s.byte_at(i) as Int` (`lib.saw:89`) and constructor/cast chain\\n+  `Byte(UInt8.from(truncating: u))` (`lib.saw:95-97`). This requires static method\\n+  lookup on a primitive and the intrinsic low-bit conversion behavior of\\n+  `UInt8.from(truncating:)`. It does not require compiling all of `std.numeric`.\\n+- Boolean `not`, `&&`, and `||`, with short-circuit evaluation (for example\\n+  `lib.saw:602-604`, `626-637`, and `967-1175`). Parenthesized expressions and\\n+  precedence must cover these alongside comparisons and arithmetic.\\n+- String literals including escape sequences used in diagnostics, but the\\n+  compiler need not implement interpolation to compile this file: apparent\\n+  braces in its diagnostic strings are escaped. Runtime string equality is used\\n+  both by `==` in `keyword_kind`'s string `match` and explicitly by\\n+  `name_str.equals(...)` at `lib.saw:931-932`.\\n+\\n+### Ownership, references, generics, and fallibility\\n+\\n+- Shared references `&T`, mutable references `&var T`, address-taking `&expr` /\\n+  `&var place`, and receiver exclusivity. Required examples include `ubyte(&s,\\n+  i)`, `read_unicode_escape(&var self, out: &var StringBuilder)`, and passing\\n+  `&var docs` through `tokenize`. References need load/store and lifetime safety\\n+  sufficient for direct calls; reference fields, returned references, pointer\\n+  arithmetic, and unsafe user pointers are not used by lexer source.\\n+- Copy versus move behavior must distinguish scalar/Copy values (`Int`, `Bool`,\\n+  `String`, payload-free `TokenKind`, `Byte`, unsigned integers) from aggregate\\n+  ownership. `LexResult` is explicitly `NoCopy`; its two vectors are moved into\\n+  the result at `lib.saw:1210-1215`. Vector elements include owning `String` and\\n+  `String?`, so pushing/returning structs must retain/move/drop exactly once.\\n+  The lexer explicitly moves both vectors into `LexResult` at `lib.saw:1215`;\\n+  other transfers and copies must follow each payload's copy policy.\\n+- Generic type application is needed for `Vector<Token>`, `Vector<DocComment>`,\\n+  `Result<T, LexError>`, and `String?`. The lexer declares no generic function or\\n+  type itself. A monomorphized built-in/runtime implementation is sufficient;\\n+  generic declaration parsing, bounds, defaults, and arbitrary specialization\\n+  are needed only if the mini-VM chooses to compile the std source rather than\\n+  expose these few APIs intrinsically.\\n+- Optional construction/patterns: `None`, implicit wrapping of a bare `String`\\n+  on the success tail of `try_read_int_suffix`, and `if let name = optional`\\n+  (`lib.saw:476`, `540`, `787-817`, `857`, `904`). Optional payload moves must be\\n+  correct for `String`.\\n+- `Result<T,E>`, `try expr`, and `try! expr`. `try` propagates `LexError` from\\n+  lexer helpers (`lib.saw:739`, `967-1177`, `1210-1215`); `try!` unwraps\\n+  allocation results from builders/vectors and `Scalar(value:)`. Only `Ok`/`Err`\\n+  representation, success extraction, error propagation, and trap-on-error are\\n+  needed. `catch`, `try?`, and general `Error` conformance are unused.\\n+\\n+### Control flow and syntax sugar\\n+\\n+- `if`/`else if` statements, value-producing `if` expressions (`lib.saw:650,\\n+  780, 845-846`), `while`, and early `return`. `for`, `break`, `continue`, and\\n+  `guard` do not occur in this lexer.\\n+- Exhaustive `match` over strings with literal cases and `_`\\n+  (`keyword_kind`, `lib.saw:289-326`), and over payload-free enum values with\\n+  both qualified and unqualified case patterns (`kind_name`, `lib.saw:330-438`).\\n+  Guards, payload destructuring, nested patterns, and non-string scalar matches\\n+  are unnecessary.\\n+- Local inference, shadowing, `_` discard bindings, and compound assignment.\\n+  The postfix/member grammar must distinguish method calls, field access, and\\n+  static enum/type members. Vector subscripting is required for token lookback.\\n+  Array literals, tuples, ranges, and interpolation syntax are not used to\\n+  implement the lexer itself.\\n+\\n+## Minimal std/runtime contract\\n+\\n+These are the only public behaviors the lexer calls. Their current production\\n+definitions are useful specifications, not a requirement that the mini-VM first\\n+compile their full implementations.\\n+\\n+- `String.len(&self) -> Int` (`sawc/std/string.saw:53`): UTF-8 byte length,\\n+  including embedded NUL. `String.byte_at(&self, index: Int) -> Byte`\\n+  (`:75`): unsigned byte, bounds checked. `substring(&self, start: Int, end: Int)\\n+  -> String` (`:426`): copied half-open byte range, bounds checked. `is_empty()`\\n+  is used at `lib.saw:855,927` and can be intrinsic or derived from `len == 0`.\\n+  `equals(&self, other: String) -> Bool` (`:360`) and `==` need byte-content\\n+  equality. `to_uint(&self, radix: Int) -> UInt?` (`:677`) must accept bases\\n+  2/8/10/16 and return `None` on invalid digits or overflow past 64 bits. The\\n+  lexer does not need trimming, Unicode iteration, replacement, joins, hashing,\\n+  or general formatting from `std.string`.\\n+- `StringBuilder()` (`sawc/std/stringbuilder.saw:58`),\\n+  `append(&var self, String)` (`:146`), `append(..., Int)` (`:191`),\\n+  `append(..., Byte)` (`:254`), `append(..., Scalar)` (`:348`), and\\n+  `build(&self) -> String` (`:414`). Appends preserve raw bytes; integer append\\n+  is decimal; scalar append is valid UTF-8; build returns independent string\\n+  contents and may leave the builder reusable. All append overloads return\\n+  `Result<Void, AllocError>`, but lexer paths deliberately use `try!`. Capacity,\\n+  fixed mode, allocator traits, `UnsafeSend`/`UnsafeSync`, raw pointers, memcpy,\\n+  and growth algorithms in the std implementation are transitive implementation\\n+  details and should not become source-language prerequisites.\\n+- `Vector<T>()`, `len(&self) -> Int`, indexed shared read, and\\n+  `push(&var self, value: T) -> Result<Void, AllocError>` are used throughout;\\n+  production declarations are `sawc/std/vector.saw:43,67,93,231`. Required\\n+  instantiations are `Vector<Token>` and `Vector<DocComment>`. Bounds-checked\\n+  indexing appears only for token lookback (`lib.saw:985`). `pop`, iteration,\\n+  reserve, copy, custom allocators, and mutable indexing are unused by this\\n+  lexer. The std type's allocator parameter/default and pointer implementation\\n+  are therefore avoidable if the mini-VM provides an opaque monomorphic vector.\\n+- `Scalar(value: Int) -> Result<Scalar, InvalidScalar>`\\n+  (`sawc/std/scalar.saw:64,85`) and builder append encode one already-validated\\n+  scalar. The lexer independently rejects out-of-range and surrogate code points\\n+  before construction. Payload enum `InvalidScalar`, Printable/Error traits, and\\n+  the rest of `std.scalar` are transitive if construction is intrinsic.\\n+- `Byte`, `UInt*`, `String`, `Optional`, `Result`, `AllocError`, and `NoCopy` are\\n+  prelude-visible in the production compiler. For this prototype they may be\\n+  compiler-known types. Reproducing their complete std declarations would pull\\n+  in allocators, unsafe pointers, traits, synthesis, deinitializers, and FFI that\\n+  the lexer never exercises.\\n+\\n+## Dependency order and isolated gates\\n+\\n+Each gate should add a tiny source fixture and assert both VM behavior and a\\n+precise rejection. Keep the existing 43 tests green at every gate.\\n+\\n+1. **Wide scalar foundation.** Add source `Int`, unsigned fixed-width values,\\n+   `Byte`, the listed operators/casts, `from(truncating:)`, and `.max`. Test\\n+   `Int` values beyond `Int32`, byte 255 round-trip, the three maxima widened to\\n+   `UInt`, signed `-1`, short-circuit suppression of a trapping RHS, and reject\\n+   unsupported casts/overflow.\\n+2. **Strings and static constants.** Add module `static Int` constants, owned\\n+   strings, literals, equality, `len`, `byte_at`, `substring`, `is_empty`, and\\n+   `to_uint(radix)`. Test embedded NUL/non-ASCII byte length, unsigned bytes,\\n+   valid/invalid bounds, radix parsing at `UInt.max`, and overflow to `None`.\\n+3. **Structs, enums, and methods.** Add named-field structs, payload-free enums,\\n+   equality, qualified members, extensions, `&self`/`&var self`, field mutation,\\n+   and named calls. Test an `advance`-shaped mutable receiver and reject mutation\\n+   through `&self`, wrong labels, missing fields, and non-exhaustive enum match.\\n+4. **Optional/Result control flow.** Add `T?`, `Result<T,E>`, `None`, implicit\\n+   optional injection, `if let`, `try`, and `try!`. Test success/error\\n+   propagation with owning String payloads and verify `try!` traps. Reject `try`\\n+   where the enclosing result error is incompatible.\\n+5. **Opaque builder and vector generics.** Expose only the contracts listed\\n+   above and implement correct ownership for `Vector<Token>` /\\n+   `Vector<DocComment>`. Test growth, indexing bounds, push of structs containing\\n+   `String?`, builder overload selection, build independence, and Unicode scalar\\n+   UTF-8 output. Reject unsupported generic types/operations explicitly.\\n+6. **Lexer syntax completion.** Add string and enum `match`, if-expressions,\\n+   public declarations, `NoCopy`, compound assignments, tail expressions, and\\n+   `_` discards. Compile focused extracts of `keyword_kind`, `kind_name`,\\n+   `literal_fits`, and `Lexer.advance`; compare their outputs to hand-written\\n+   oracles.\\n+7. **Whole-module compile and differential lexing.** Compile the unchanged\\n+   `selfhost/lexer/src/lib.saw`, call `lex` and `lex_all`, and compare canonical\\n+   token/error/doc records against the production lexer. Minimum cases: every\\n+   keyword/operator, comments/docs, decimal/base/float/suffix boundaries,\\n+   escaped and interpolated strings, Unicode scalar edges, raw multi-byte UTF-8\\n+   columns, EOF, and each error path. This gate is where ownership/leak checks\\n+   for repeated lexing matter; agreement between two engines still needs fixed\\n+   expected records for representative cases.\\n+\\n+## Outstanding design choices to settle before implementation\\n+\\n+- Numeric representation is settled in M1_NUMBERS.md: 64-bit bit-pattern cells,\\n+  with signed/unsigned interpretation carried by slot types. `UInt` must retain\\n+  all 64 bits from `to_uint`; treating those cells as signed mathematical values\\n+  during comparisons, casts, arithmetic, or printing would be incorrect.\\n+- Decide whether String/Builder/Vector are opaque runtime handles or lowered\\n+  native aggregates. Opaque handles keep allocator, unsafe-pointer, FFI, trait,\\n+  and generic-stdlib implementation out of the prerequisite chain and are the\\n+  smallest lexer-specific route.\\n+- Define retain/move/drop points before vectors of `Token` are enabled. The\\n+  observable lexer contract does not expose refcounts, but stale/double-freed\\n+  `String` and `String?` payloads will otherwise make differential results\\n+  nondeterministic.\\n+- Define how `try!` failure is surfaced by the mini-VM and emitted LLVM. Lexer\\n+  allocation failures are allowed to trap, but the two backends need one tested\\n+  behavior.\\n+- Keep unsupported production language features as located rejections. Compiling\\n+  the complete production std files is not a milestone requirement and would\\n+  silently expand this project into traits, allocators, unsafe pointers, FFI,\\n+  synthesis, and deinitialization before the lexer can run.\\ndiff --git a/prototypes/minivm/M1_NUMBERS.md b/prototypes/minivm/M1_NUMBERS.md\\nnew file mode 100644\\nindex 00000000..86c8b374\\n--- /dev/null\\n+++ b/prototypes/minivm/M1_NUMBERS.md\\n@@ -0,0 +1,103 @@\\n+# M1: lexer numeric foundation\\n+\\n+Status: implemented; final validation recorded in README.md. Baseline: 5af086c4.\\n+This supersedes the scalar-only portions of DESIGN.md. Primary owns this contract,\\n+model/verifier, integration and review. Sol owns bounded implementations. No\\n+production compiler/library changes or tracker entries. Commit passing milestones.\\n+\\n+## Scope and compatibility\\n+\\n+Support Int8/16/32/64, UInt8/16/32/64, platform Int/UInt (64-bit for this\\n+prototype), and the prelude Byte distinct type with UInt8 representation.\\n+Unsuffixed integers now default to Int (= I64), as Saw specifies. Update old\\n+Int32 overflow fixtures to make their width explicit; retain their assertions.\\n+Support decimal/hex/binary/octal literals and all fixed-width suffixes. Literal\\n+range failures are located compile errors. Negative signed minimum literals work.\\n+Unsigned full-width values must survive constants, calls, comparisons and printing.\\n+\\n+Bare literals adopt an expected numeric type at declarations, assignments, call\\n+arguments and returns, and beside a typed binary operand. Do not silently narrow\\n+an already typed expression. Lossless integer widening at transfer sites is allowed;\\n+other conversions require `as` or `T.from(truncating: value)`. Full constant-expression\\n+adoption is a later isolated frontend task; reject unsupported combinations cleanly.\\n+Byte construction is `Byte(u8)` (bare literal may adopt UInt8); Byte may project\\n+to its underlying UInt8, and explicit integer casts can widen it. General distinct\\n+types and user-defined methods remain later milestones. Byte arithmetic is outside\\n+this milestone; Byte equality and printing its numeric byte value are supported.\\n+An unwrapped literal assigned directly to Byte is rejected, as in Saw; the\\n+constructor is required. Literal adoption does not bypass the distinct type.\\n+\\n+`T.min`/`T.max` are scalar constants. `T.from(truncating: x)` explicitly retains\\n+low target-width bits and interprets signed targets in two's complement. `x as T`\\n+checks both sign and range and never silently truncates. Optional-returning\\n+`T.from(x)` waits for Optional. Bool/integer conversions are rejected.\\n+\\n+Checked +,-,* and unary negation trap on overflow. Division/rem trap on zero.\\n+Signed minimum / -1 AND signed minimum % -1 trap, matching LANGUAGE_SPEC.md;\\n+the initial prototype's remainder-zero exception is intentionally removed.\\n+`&+`, `&-`, `&*` wrap at the operand width. Bitwise &,|,^,~,<<,>> operate on\\n+integer operands; shifts check 0 <= count < lhs width, right shift is arithmetic\\n+for signed and logical for unsigned, and left shift truncates to width.\\n+Binary arithmetic/bitwise operands must have the same type after literal adoption.\\n+Shift count may be any integer type; the result has lhs type.\\n+No eager Boolean &&/|| implementation: those require separate short-circuit lowering.\\n+\\n+Precedence follows Saw (tightest first): postfix/casts, unary, multiply, add, shifts, comparison,\\n+equality, bitwise &, bitwise ^, bitwise |. Use actual lexer token kinds; it may\\n+represent a shift as consecutive Lt/Gt tokens. The source parser must distinguish\\n+this from comparisons. Existing explicit-return source syntax remains supported.\\n+\\n+## Representation and API\\n+\\n+ValueType adds I8,I16,I64,U8,U16,U32,U64,Byte to existing I32,Bool,Void.\\n+Int and Int64 share I64; UInt and UInt64 share U64 on this fixed 64-bit target.\\n+Every numeric runtime cell and Instruction.immediate remains a host Int carrying\\n+64 bits. Signed values are sign-extended; unsigned widths below 64 are zero-extended;\\n+U64 uses the same bits, even when the host Int reading is negative. Never use\\n+checked `as Int` to convert U64 storage: use Int.from(truncating: value), and\\n+UInt.from(truncating: cell) for the reverse. Constants and slots use this canonical\\n+representation. LLVM cells/ABI remain i64 with signed/unsigned interpretation by type.\\n+\\n+Op adds Cast, Truncate, WrapAdd, WrapSub, WrapMul, BitAnd, BitOr, BitXor,\\n+BitNot, Shl, Shr. Cast/Truncate use dst and a; source/target types come from slots.\\n+Binary operations use dst,a,b; unary operations dst,a. The verifier checks canonical\\n+constants, operation type rules and existing control-flow invariants.\\n+\\n+model.saw helpers (public): integer_type(t)->Bool (includes Byte), arithmetic_type(t)\\n+excludes Byte, integer_signed(t)->Bool, integer_width(t)->Int (0 for Bool/Void),\\n+integer_min(t)->Int, integer_max(t)->Int (U64.max has bit pattern -1),\\n+integer_canonical(value:Int,t)->Bool, integer_normalize(value:Int,t)->Int,\\n+integer_fits(value:Int,source:ValueType,target:ValueType)->Bool,\\n+integer_widens(source:ValueType,target:ValueType)->Bool.\\n+\\n+## Runtime implementation constraints\\n+\\n+VM: no host checked arithmetic may overflow before our diagnostic. For signed\\n+64-bit add/sub/multiply precheck bounds (or use wrapping arithmetic plus sound\\n+overflow detection). For unsigned use UInt bit-pattern reads and precheck UInt.max\\n+for add/multiply/subtract, then check the target width. Use wrapping operations only\\n+for wrap/bit semantics. Guard division's min/-1 and zero before host division.\\n+\\n+LLVM: use llvm.sadd/ssub/smul.with.overflow.i64 or unsigned equivalents plus narrow\\n+width checks, or an equally correct explicit precheck. Do not attach unjustified\\n+nsw/nuw. Guard min/-1 and zero before sdiv/srem; never produce poison even on the\\n+error path at -O2. Unsigned comparisons/div/rem use unsigned LLVM operations.\\n+Narrow bit operations normalize the result. Checked casts branch to a trap on\\n+invalid range/sign; truncating casts normalize without a trap. Unsigned print uses\\n+an unsigned 64-bit printf format. Keep current success stdout and exit statuses.\\n+\\n+Exact diagnostic lines (exit 1): runtime error: integer overflow;\\n+runtime error: division by zero; runtime error: integer cast out of range;\\n+runtime error: shift out of range. Existing VM budget/depth behavior is unchanged.\\n+\\n+## Acceptance\\n+\\n+Keep all baseline behavior except the two documented scalar compatibility changes\\n+(default Int and min%-1). Add small isolated tests for: each width/signedness;\\n+literal bounds and min/max; UInt64 above Int.max; checked and truncating casts;\\n+Byte constructor/projection; contextual literal adoption and invalid typed narrowing;\\n+checked overflow at each extreme; wrapping; signed division/rem; bitwise precedence\\n+and shift ranges. Every positive/runtime-error case has an explicit output/status\\n+oracle and runs through VM and clang -O0/-O2. Compile failures require a located\\n+diagnostic. Cross-check representative boundary expressions against the existing\\n+Saw compiler as a separate compatibility oracle. No performance work in M1.\\ndiff --git a/prototypes/minivm/M2_RECORDS.md b/prototypes/minivm/M2_RECORDS.md\\nnew file mode 100644\\nindex 00000000..ddb8714c\\n--- /dev/null\\n+++ b/prototypes/minivm/M2_RECORDS.md\\n@@ -0,0 +1,83 @@\\n+# M2: value records over scalar fields\\n+\\n+Status: design, to implement after M1 passes. This is the struct subsection of\\n+the lexer dependency checklist. Strings, owning containers, methods, and references\\n+will build on this layout in later isolated milestones.\\n+\\n+## Observable source behavior\\n+\\n+Single-file named structs with scalar fields or other named structs. Fields may\\n+be comma/newline separated and may carry `public` (cross-module visibility is\\n+validated in the module milestone). Constructors use named fields exactly once,\\n+in any written order; evaluation follows written order. Record field reads,\\n+nested field access, whole-record assignment, and mutation through a mutable\\n+local are supported. Immutable locals and value parameters cannot be mutated.\\n+Passing and returning a record have value semantics. A copy followed by mutation\\n+must leave the original unchanged. Records are nominal: equal layouts do not\\n+make two declared types interchangeable. No record arithmetic, implicit equality,\\n+printing, references, custom initializers, or user-defined copy policies yet.\\n+\\n+The first slice requires at least one field and caps a flattened value at 256\\n+words with a located diagnostic. Recursive by-value records are rejected, including\\n+indirect cycles; forward type references are allowed. Numeric casts and literal\\n+adoption apply at scalar field initialization/assignment exactly as at local\\n+transfers. Built-in type names, duplicate records, duplicate fields, missing or\\n+extra constructor labels, and wrong field types get explicit diagnostics.\\n+\\n+## Representation decision\\n+\\n+Records are flattened into contiguous scalar slots, recursively in declaration\\n+order. They do not become shared mutable heap handles. This needs no allocator\\n+or provisional garbage collector, and matches scalar record value semantics.\\n+Later String/Vector handles can occupy scalar words with separate ownership rules;\\n+their retention/destruction is not silently supplied by this scalar-only milestone.\\n+\\n+Planned model additions:\\n+\\n+* `ValueType.Record(index: Int)` identifies a nominal declaration in Program.\\n+* `RecordIR` carries name, ordered field names and ValueTypes.\\n+* `Program.records` stores the declarations. Numeric helpers return false/zero\\n+  for Record. All actual FuncIR.slots remain flattened scalar types.\\n+* FuncIR.params/result retain source types. Parameter storage is the concatenation\\n+  of their flattened layouts starting at slot zero. Compiler and verifier share\\n+  one checked layout function, not independent offset calculations.\\n+* ExprValue.slot is the first slot of a contiguous value; ExprValue.ty retains\\n+  its source type. Field access computes the relevant subrange.\\n+  A constructor allocates a complete contiguous result range separately from\\n+  initializer temporaries, then populates fields in declaration order. Every\\n+  aggregate expression producer must preserve this contiguity invariant.\\n+* No record instruction is required: construction/copy/assignment lowers to\\n+  scalar Copies after evaluating source operands. The frontend preserves source\\n+  evaluation order independently of declaration-order layout.\\n+* Call.args lists the flattened argument slots; Call.dst is the start of the\\n+  result slot range, or -1 for Void. Return.a identifies the returned range.\\n+  The verifier derives and validates the width/types from the callee signature.\\n+\\n+The VM returns a vector of result cells rather than a single Int and initializes\\n+fresh parameter slots on each call. LLVM returns scalar i64 for scalar types,\\n+void for Void, or an LLVM aggregate of i64 fields for Record; record arguments\\n+are flattened scalar ABI parameters. The LLVM emitter packs/unpacks aggregate\\n+results with insertvalue/extractvalue. No LLVM bindings or layout assumptions\\n+about the production Saw ABI are introduced.\\n+\\n+All record indices and the complete by-value layout graph must be checked before\\n+indexing or summing widths. Calls validate flattened argument types and complete\\n+destination ranges; Returns validate complete source ranges. VM call arguments\\n+and return words are snapshots, not borrowed slices into another frame. Even a\\n+one-field record uses an LLVM aggregate return type consistently at both ends.\\n+\\n+## Tests and division of work\\n+\\n+Primary implements/reviews model/layout/verifier and defines all interfaces before\\n+delegation. Sol frontend task handles declarations, constructor/field syntax,\\n+typechecking, and scalar lowering. Sol engine task handles flattened argument and\\n+aggregate return ABI in VM and LLVM. A bounded Sol test task adds fixtures, then\\n+the primary independently reviews aliasing, overlap, and evaluation order.\\n+\\n+Minimal acceptance examples: two-field construction; reordered labels with\\n+side-effecting calls; independent copy then field mutation; nested records;\\n+assignment through nested fields; records as parameters/results; forward record\\n+declaration; two nominally distinct equal-layout types rejected; immutable\\n+mutation rejected; missing/duplicate/unknown labels; direct/indirect type cycles.\\n+All success/error fixtures use explicit oracles and both VM/native paths, with\\n+native -O0/-O2. Retain M1 and baseline tests. Commit only after integration passes.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 2eec9419..783b272e 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -42,12 +42,18 @@ func main() {\\n }\\n ```\\n \\n-Single-file programs support `Int32`, `Bool`, functions, positional calls,\\n+Single-file programs support signed and unsigned 8/16/32/64-bit integers,\\n+`Int`/`UInt` (64-bit), `Byte`, `Bool`, functions, positional calls,\\n recursion, `let`/`var`, assignment, lexical scopes, statement `if`/`else` and\\n `while`, explicit returns, and scalar printing. Arithmetic is checked; signed\\n division truncates toward zero. Overflow and division by zero print an error\\n and exit with status 1. Boolean conditions require `Bool`.\\n \\n+Integer literals support decimal, hex, binary, octal and fixed-width suffixes;\\n+bare literals default to `Int`. Numeric operations include checked `as` casts,\\n+truncating conversions, wrapping arithmetic, bitwise operations, and checked\\n+shift counts. See [M1_NUMBERS.md](M1_NUMBERS.md) for the precise numeric contract.\\n+\\n Newlines separate statements. Omit the result annotation for Void functions;\\n `main()` must take no arguments and return Void. Value-returning functions need\\n explicit returns. There are no imports, strings, collections, references,\\n@@ -66,10 +72,15 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. Initial validation passed\\n-all 43 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The numeric milestone passes\\n+all 91 cases, including native execution at both `-O0` and `-O2`.\\n+\\n+The independent representation checks live in `tests/numeric_contract.saw`;\\n+build and run them with the Python compiler.\\n \\n-See [DESIGN.md](DESIGN.md) for the instruction schema and semantics. Source is\\n+See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\n+schema and semantics. [LEXER_DEPENDENCIES.md](LEXER_DEPENDENCIES.md) tracks the\\n+remaining dependencies needed to compile the existing lexer with this prototype. Source is\\n split into `frontend.saw`, `model.saw`, `verify.saw`, `vm.saw`, `llvm.saw`, and\\n `main.saw`. The verifier checks structural indices and types; initialization\\n is established by the frontend, so this is not a loader for untrusted bytecode.\\ndiff --git a/prototypes/minivm/examples/arithmetic.saw b/prototypes/minivm/examples/arithmetic.saw\\nindex 53db36de..1ccef893 100644\\n--- a/prototypes/minivm/examples/arithmetic.saw\\n+++ b/prototypes/minivm/examples/arithmetic.saw\\n@@ -4,5 +4,5 @@ func main() {\\n     print(6 * 7)\\n     print(-7 / 3)\\n     print(-7 % 3)\\n-    print(-2147483648 % -1)\\n+    print(-2147483648 % 2)\\n }\\ndiff --git a/prototypes/minivm/examples/numbers/bits_and_shifts.saw b/prototypes/minivm/examples/numbers/bits_and_shifts.saw\\nnew file mode 100644\\nindex 00000000..f4a53fbd\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/bits_and_shifts.saw\\n@@ -0,0 +1,15 @@\\n+func main() {\\n+    let u: UInt8 = 240\\n+    let s: Int8 = -16\\n+    print(u & 204)\\n+    print(u | 3)\\n+    print(u ^ 170)\\n+    print(~u)\\n+    print(s >> 2)\\n+    print(u >> 2)\\n+    print((1 as UInt8) << 7)\\n+    print((255 as UInt8) << 4)\\n+    print(1 + 2 << 3)\\n+    print((1 as UInt8) | (2 as UInt8) ^ (3 as UInt8) & (1 as UInt8))\\n+    print((8 as UInt8) >> (1 as Int16))\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/byte.saw b/prototypes/minivm/examples/numbers/byte.saw\\nnew file mode 100644\\nindex 00000000..c3079902\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/byte.saw\\n@@ -0,0 +1,9 @@\\n+func echo(x: Byte) -> Byte { return x }\\n+func main() {\\n+    let b: Byte = Byte(255)\\n+    print(b)\\n+    print(echo(b))\\n+    print(b == Byte(UInt8.max))\\n+    print(b as UInt8)\\n+    print(b as UInt64)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/cast_negative_unsigned.saw b/prototypes/minivm/examples/numbers/cast_negative_unsigned.saw\\nnew file mode 100644\\nindex 00000000..8e0d89b6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/cast_negative_unsigned.saw\\n@@ -0,0 +1 @@\\n+func main() { print((-1) as UInt8) }\\ndiff --git a/prototypes/minivm/examples/numbers/cast_too_large.saw b/prototypes/minivm/examples/numbers/cast_too_large.saw\\nnew file mode 100644\\nindex 00000000..b83604d7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/cast_too_large.saw\\n@@ -0,0 +1 @@\\n+func main() { print((256 as UInt16) as UInt8) }\\ndiff --git a/prototypes/minivm/examples/numbers/cast_unsigned_signed.saw b/prototypes/minivm/examples/numbers/cast_unsigned_signed.saw\\nnew file mode 100644\\nindex 00000000..da2971bc\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/cast_unsigned_signed.saw\\n@@ -0,0 +1 @@\\n+func main() { print(UInt64.max as Int64) }\\ndiff --git a/prototypes/minivm/examples/numbers/casts.saw b/prototypes/minivm/examples/numbers/casts.saw\\nnew file mode 100644\\nindex 00000000..6de2fd55\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/casts.saw\\n@@ -0,0 +1,10 @@\\n+func main() {\\n+    let a: Int8 = -1\\n+    let b: UInt8 = 255\\n+    let c: UInt32 = 2147483648\\n+    print(a as Int64)\\n+    print(b as UInt64)\\n+    print(b as Int16)\\n+    print(c as UInt64)\\n+    print((127 as Int8) as Int)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/complement_after_cast.saw b/prototypes/minivm/examples/numbers/complement_after_cast.saw\\nnew file mode 100644\\nindex 00000000..3a69dbd4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/complement_after_cast.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let x: Int = 255\\n+    print(~x as UInt8)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/contextual.saw b/prototypes/minivm/examples/numbers/contextual.saw\\nnew file mode 100644\\nindex 00000000..b78f2571\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/contextual.saw\\n@@ -0,0 +1,12 @@\\n+func take8(x: Int8) { print(x) }\\n+func give16() -> UInt16 { return 65535 }\\n+func main() {\\n+    let declared: Int16 = 12\\n+    var assigned: UInt32 = 1\\n+    assigned = 4294967295\\n+    take8(-128)\\n+    print(give16())\\n+    print(declared + 3)\\n+    print(3 + declared)\\n+    print(assigned)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/literals_and_aliases.saw b/prototypes/minivm/examples/numbers/literals_and_aliases.saw\\nnew file mode 100644\\nindex 00000000..89796d8b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/literals_and_aliases.saw\\n@@ -0,0 +1,34 @@\\n+func main() {\\n+    let i8: Int8 = -128\\n+    let i16: Int16 = 32767\\n+    let i32: Int32 = -2147483648\\n+    let i64: Int64 = 9223372036854775807\\n+    let int: Int = -9223372036854775808\\n+    let u8: UInt8 = 255\\n+    let u16: UInt16 = 65535\\n+    let u32: UInt32 = 4294967295\\n+    let u64: UInt64 = 18446744073709551615\\n+    let uint: UInt = 9223372036854775808\\n+    print(i8)\\n+    print(i16)\\n+    print(i32)\\n+    print(i64)\\n+    print(int)\\n+    print(u8)\\n+    print(u16)\\n+    print(u32)\\n+    print(u64)\\n+    print(uint)\\n+    print(0xffu8)\\n+    print(0xffffu16)\\n+    print(0xffffffffu32)\\n+    print(0xffffffffffffffffu64)\\n+    print(0b101010u8)\\n+    print(0o777u16)\\n+    print(127i8)\\n+    print(32767i16)\\n+    print(2147483647i32)\\n+    print(9223372036854775807i64)\\n+    print(-128i8)\\n+    print(2147483647 + 1)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/lossless_widening.saw b/prototypes/minivm/examples/numbers/lossless_widening.saw\\nnew file mode 100644\\nindex 00000000..b734b4f6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/lossless_widening.saw\\n@@ -0,0 +1,13 @@\\n+func take64(x: Int64) { print(x) }\\n+func widen_return(x: UInt8) -> UInt64 { return x }\\n+func main() {\\n+    let small: Int8 = -12\\n+    let wide: Int64 = small\\n+    var assigned: Int32 = 0\\n+    assigned = small\\n+    take64(small)\\n+    print(wide)\\n+    print(assigned)\\n+    let byte: UInt8 = 255\\n+    print(widen_return(byte))\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_div_i64.saw b/prototypes/minivm/examples/numbers/overflow_div_i64.saw\\nnew file mode 100644\\nindex 00000000..0622ee56\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_div_i64.saw\\n@@ -0,0 +1 @@\\n+func main() { print(Int64.min / -1) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_i16_sub.saw b/prototypes/minivm/examples/numbers/overflow_i16_sub.saw\\nnew file mode 100644\\nindex 00000000..fb7f5f9a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_i16_sub.saw\\n@@ -0,0 +1 @@\\n+func main() { print(Int16.min - 1) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_i64_add.saw b/prototypes/minivm/examples/numbers/overflow_i64_add.saw\\nnew file mode 100644\\nindex 00000000..b94cf702\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_i64_add.saw\\n@@ -0,0 +1 @@\\n+func main() { print(Int64.max + 1) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_i64_mul.saw b/prototypes/minivm/examples/numbers/overflow_i64_mul.saw\\nnew file mode 100644\\nindex 00000000..0a87db9f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_i64_mul.saw\\n@@ -0,0 +1 @@\\n+func main() { print(Int64.max * 2) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_i64_sub.saw b/prototypes/minivm/examples/numbers/overflow_i64_sub.saw\\nnew file mode 100644\\nindex 00000000..53363f46\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_i64_sub.saw\\n@@ -0,0 +1 @@\\n+func main() { print(Int64.min - 1) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_i8_add.saw b/prototypes/minivm/examples/numbers/overflow_i8_add.saw\\nnew file mode 100644\\nindex 00000000..e7e9b3f9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_i8_add.saw\\n@@ -0,0 +1 @@\\n+func main() { print(Int8.max + 1) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_i8_mul.saw b/prototypes/minivm/examples/numbers/overflow_i8_mul.saw\\nnew file mode 100644\\nindex 00000000..640751ec\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_i8_mul.saw\\n@@ -0,0 +1 @@\\n+func main() { print((64 as Int8) * 2) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_neg_i64.saw b/prototypes/minivm/examples/numbers/overflow_neg_i64.saw\\nnew file mode 100644\\nindex 00000000..7fa62cac\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_neg_i64.saw\\n@@ -0,0 +1 @@\\n+func main() { print(-Int64.min) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_neg_i8.saw b/prototypes/minivm/examples/numbers/overflow_neg_i8.saw\\nnew file mode 100644\\nindex 00000000..1decdebf\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_neg_i8.saw\\n@@ -0,0 +1 @@\\n+func main() { print(-Int8.min) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_rem_i32.saw b/prototypes/minivm/examples/numbers/overflow_rem_i32.saw\\nnew file mode 100644\\nindex 00000000..f5245df2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_rem_i32.saw\\n@@ -0,0 +1 @@\\n+func main() { print(Int32.min % -1) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_rem_i64.saw b/prototypes/minivm/examples/numbers/overflow_rem_i64.saw\\nnew file mode 100644\\nindex 00000000..c9b900e2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_rem_i64.saw\\n@@ -0,0 +1 @@\\n+func main() { print(Int64.min % -1) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_u16_sub.saw b/prototypes/minivm/examples/numbers/overflow_u16_sub.saw\\nnew file mode 100644\\nindex 00000000..b462eb1e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_u16_sub.saw\\n@@ -0,0 +1 @@\\n+func main() { print((0 as UInt16) - 1) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_u32_mul.saw b/prototypes/minivm/examples/numbers/overflow_u32_mul.saw\\nnew file mode 100644\\nindex 00000000..812bc295\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_u32_mul.saw\\n@@ -0,0 +1 @@\\n+func main() { print((65536 as UInt32) * 65536) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_u64_add.saw b/prototypes/minivm/examples/numbers/overflow_u64_add.saw\\nnew file mode 100644\\nindex 00000000..4b9b829f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_u64_add.saw\\n@@ -0,0 +1 @@\\n+func main() { print(UInt64.max + 1) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_u64_mul.saw b/prototypes/minivm/examples/numbers/overflow_u64_mul.saw\\nnew file mode 100644\\nindex 00000000..4fc91c5f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_u64_mul.saw\\n@@ -0,0 +1 @@\\n+func main() { print(UInt.max * 2u64) }\\ndiff --git a/prototypes/minivm/examples/numbers/overflow_u64_sub.saw b/prototypes/minivm/examples/numbers/overflow_u64_sub.saw\\nnew file mode 100644\\nindex 00000000..89268136\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/overflow_u64_sub.saw\\n@@ -0,0 +1 @@\\n+func main() { print(0u64 - 1u64) }\\ndiff --git a/prototypes/minivm/examples/numbers/reject_bool_cast.saw b/prototypes/minivm/examples/numbers/reject_bool_cast.saw\\nnew file mode 100644\\nindex 00000000..654d3d52\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_bool_cast.saw\\n@@ -0,0 +1 @@\\n+func main() { print(true as Int) }\\ndiff --git a/prototypes/minivm/examples/numbers/reject_byte_literal.saw b/prototypes/minivm/examples/numbers/reject_byte_literal.saw\\nnew file mode 100644\\nindex 00000000..25016bf9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_byte_literal.saw\\n@@ -0,0 +1 @@\\n+func main() { let b: Byte = 255 }\\ndiff --git a/prototypes/minivm/examples/numbers/reject_literal_range.saw b/prototypes/minivm/examples/numbers/reject_literal_range.saw\\nnew file mode 100644\\nindex 00000000..b95d0299\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_literal_range.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let x: UInt8 = 256\\n+    print(x)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/reject_mixed_binary.saw b/prototypes/minivm/examples/numbers/reject_mixed_binary.saw\\nnew file mode 100644\\nindex 00000000..75db5fd2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_mixed_binary.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    let a: Int8 = 1\\n+    let b: Int16 = 2\\n+    print(a + b)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/reject_spaced_shift.saw b/prototypes/minivm/examples/numbers/reject_spaced_shift.saw\\nnew file mode 100644\\nindex 00000000..06f82530\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_spaced_shift.saw\\n@@ -0,0 +1 @@\\n+func main() { print(1 < < 2) }\\ndiff --git a/prototypes/minivm/examples/numbers/reject_typed_narrowing.saw b/prototypes/minivm/examples/numbers/reject_typed_narrowing.saw\\nnew file mode 100644\\nindex 00000000..6950f95d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_typed_narrowing.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    let wide: Int64 = 12\\n+    let narrow: Int8 = wide\\n+    print(narrow)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/reject_typed_narrowing_argument.saw b/prototypes/minivm/examples/numbers/reject_typed_narrowing_argument.saw\\nnew file mode 100644\\nindex 00000000..cc66b9c4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_typed_narrowing_argument.saw\\n@@ -0,0 +1,5 @@\\n+func take(x: Int8) { print(x) }\\n+func main() {\\n+    let wide: Int64 = 12\\n+    take(wide)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/reject_typed_narrowing_assignment.saw b/prototypes/minivm/examples/numbers/reject_typed_narrowing_assignment.saw\\nnew file mode 100644\\nindex 00000000..50214dd0\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_typed_narrowing_assignment.saw\\n@@ -0,0 +1,6 @@\\n+func main() {\\n+    let wide: Int64 = 12\\n+    var narrow: Int8 = 1\\n+    narrow = wide\\n+    print(narrow)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/reject_typed_narrowing_return.saw b/prototypes/minivm/examples/numbers/reject_typed_narrowing_return.saw\\nnew file mode 100644\\nindex 00000000..1009948a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_typed_narrowing_return.saw\\n@@ -0,0 +1,5 @@\\n+func narrow() -> Int8 {\\n+    let wide: Int64 = 12\\n+    return wide\\n+}\\n+func main() { print(narrow()) }\\ndiff --git a/prototypes/minivm/examples/numbers/reject_uncontextual_u64.saw b/prototypes/minivm/examples/numbers/reject_uncontextual_u64.saw\\nnew file mode 100644\\nindex 00000000..bc2ceb19\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/reject_uncontextual_u64.saw\\n@@ -0,0 +1 @@\\n+func main() { print(18446744073709551615) }\\ndiff --git a/prototypes/minivm/examples/numbers/review_literals.saw b/prototypes/minivm/examples/numbers/review_literals.saw\\nnew file mode 100644\\nindex 00000000..f6e5fdbd\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/review_literals.saw\\n@@ -0,0 +1,11 @@\\n+func u64(x: UInt64) -> UInt64 { return x }\\n+func maximum() -> UInt64 { return 18446744073709551615 }\\n+func main() {\\n+    print(1 + 2i8)\\n+    print(3 - 2u8)\\n+    print(1 < 2u8)\\n+    print(-1 as Int8)\\n+    print(u64(18446744073709551615))\\n+    print(maximum())\\n+    print(18446744073709551615 == UInt64.max)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/review_unsigned.saw b/prototypes/minivm/examples/numbers/review_unsigned.saw\\nnew file mode 100644\\nindex 00000000..2dc40ba4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/review_unsigned.saw\\n@@ -0,0 +1,10 @@\\n+func main() {\\n+    print(UInt64.max / 3u64)\\n+    print(UInt64.max % 2u64)\\n+    print(UInt.max >> 63)\\n+    print(~0u64)\\n+    print(255u8 << 1)\\n+    print(-127i8 - 1)\\n+    print(Int.min * 1)\\n+    print(Int.min / 1)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/shift_large_unsigned.saw b/prototypes/minivm/examples/numbers/shift_large_unsigned.saw\\nnew file mode 100644\\nindex 00000000..2b78d5a1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/shift_large_unsigned.saw\\n@@ -0,0 +1 @@\\n+func main() { print(1 << UInt.max) }\\ndiff --git a/prototypes/minivm/examples/numbers/shift_negative.saw b/prototypes/minivm/examples/numbers/shift_negative.saw\\nnew file mode 100644\\nindex 00000000..5f34faf5\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/shift_negative.saw\\n@@ -0,0 +1 @@\\n+func main() { print((1 as UInt8) << -1) }\\ndiff --git a/prototypes/minivm/examples/numbers/shift_width.saw b/prototypes/minivm/examples/numbers/shift_width.saw\\nnew file mode 100644\\nindex 00000000..99cf35c1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/shift_width.saw\\n@@ -0,0 +1 @@\\n+func main() { print((1 as UInt8) >> 8) }\\ndiff --git a/prototypes/minivm/examples/numbers/truncation.saw b/prototypes/minivm/examples/numbers/truncation.saw\\nnew file mode 100644\\nindex 00000000..2210fdf9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/truncation.saw\\n@@ -0,0 +1,10 @@\\n+func main() {\\n+    print(UInt8.from(truncating: -1))\\n+    print(Int8.from(truncating: 255))\\n+    print(UInt16.from(truncating: 65537))\\n+    print(Int16.from(truncating: 65535))\\n+    print(UInt32.from(truncating: 18446744073709551615u64))\\n+    print(Int32.from(truncating: 4294967295u64))\\n+    print(UInt64.from(truncating: -1))\\n+    print(Int64.from(truncating: 18446744073709551615u64))\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/types_and_bounds.saw b/prototypes/minivm/examples/numbers/types_and_bounds.saw\\nnew file mode 100644\\nindex 00000000..0a3fab79\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/types_and_bounds.saw\\n@@ -0,0 +1,22 @@\\n+func main() {\\n+    print(Int8.min)\\n+    print(Int8.max)\\n+    print(Int16.min)\\n+    print(Int16.max)\\n+    print(Int32.min)\\n+    print(Int32.max)\\n+    print(Int64.min)\\n+    print(Int64.max)\\n+    print(Int.min)\\n+    print(Int.max)\\n+    print(UInt8.min)\\n+    print(UInt8.max)\\n+    print(UInt16.min)\\n+    print(UInt16.max)\\n+    print(UInt32.min)\\n+    print(UInt32.max)\\n+    print(UInt64.min)\\n+    print(UInt64.max)\\n+    print(UInt.min)\\n+    print(UInt.max)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/u64_calls.saw b/prototypes/minivm/examples/numbers/u64_calls.saw\\nnew file mode 100644\\nindex 00000000..69d22790\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/u64_calls.saw\\n@@ -0,0 +1,9 @@\\n+func echo(x: UInt64) -> UInt64 { return x }\\n+func above_signed(x: UInt64) -> Bool { return x > 9223372036854775807 }\\n+func main() {\\n+    let high: UInt64 = 18446744073709551615\\n+    print(high)\\n+    print(echo(high))\\n+    print(above_signed(high))\\n+    print(high == UInt64.max)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/unary_cast_order.saw b/prototypes/minivm/examples/numbers/unary_cast_order.saw\\nnew file mode 100644\\nindex 00000000..9a1c2cea\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/unary_cast_order.saw\\n@@ -0,0 +1,8 @@\\n+func main() {\\n+    let x: Int = 127\\n+    print(-x as Int8)\\n+    let b: UInt8 = 0\\n+    print(~b as UInt64)\\n+    print(-1\\n+        as Int8)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/unary_cast_range.saw b/prototypes/minivm/examples/numbers/unary_cast_range.saw\\nnew file mode 100644\\nindex 00000000..5ba4ff62\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/unary_cast_range.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let x: Int = 128\\n+    print(-x as Int8)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/unary_cast_widen.saw b/prototypes/minivm/examples/numbers/unary_cast_widen.saw\\nnew file mode 100644\\nindex 00000000..58c239e3\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/unary_cast_widen.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let x: Int8 = -128\\n+    print(-x as Int64)\\n+}\\ndiff --git a/prototypes/minivm/examples/numbers/wrapping.saw b/prototypes/minivm/examples/numbers/wrapping.saw\\nnew file mode 100644\\nindex 00000000..05e9bb66\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/numbers/wrapping.saw\\n@@ -0,0 +1,12 @@\\n+func main() {\\n+    let i8max: Int8 = Int8.max\\n+    let i8min: Int8 = Int8.min\\n+    let u8max: UInt8 = UInt8.max\\n+    print(i8max &+ 1)\\n+    print(i8min &- 1)\\n+    print(i8max &* 2)\\n+    print(u8max &+ 1)\\n+    print((0 as UInt8) &- 1)\\n+    print((128 as UInt8) &* 2)\\n+    print(UInt64.max &+ 1)\\n+}\\ndiff --git a/prototypes/minivm/examples/overflow_add.saw b/prototypes/minivm/examples/overflow_add.saw\\nindex 11a38196..df679710 100644\\n--- a/prototypes/minivm/examples/overflow_add.saw\\n+++ b/prototypes/minivm/examples/overflow_add.saw\\n@@ -1 +1,4 @@\\n-func main() { print(2147483647 + 1) }\\n+func main() {\\n+    let x: Int32 = 2147483647\\n+    print(x + 1)\\n+}\\ndiff --git a/prototypes/minivm/examples/overflow_div.saw b/prototypes/minivm/examples/overflow_div.saw\\nindex 76d445c4..3854e141 100644\\n--- a/prototypes/minivm/examples/overflow_div.saw\\n+++ b/prototypes/minivm/examples/overflow_div.saw\\n@@ -1 +1,4 @@\\n-func main() { print(-2147483648 / -1) }\\n+func main() {\\n+    let x: Int32 = -2147483648\\n+    print(x / -1)\\n+}\\ndiff --git a/prototypes/minivm/examples/overflow_mul.saw b/prototypes/minivm/examples/overflow_mul.saw\\nindex 252e7099..a8bf4ff6 100644\\n--- a/prototypes/minivm/examples/overflow_mul.saw\\n+++ b/prototypes/minivm/examples/overflow_mul.saw\\n@@ -1 +1,4 @@\\n-func main() { print(50000 * 50000) }\\n+func main() {\\n+    let x: Int32 = 50000\\n+    print(x * 50000)\\n+}\\ndiff --git a/prototypes/minivm/examples/overflow_neg.saw b/prototypes/minivm/examples/overflow_neg.saw\\nindex 86546876..ae715bd6 100644\\n--- a/prototypes/minivm/examples/overflow_neg.saw\\n+++ b/prototypes/minivm/examples/overflow_neg.saw\\n@@ -1 +1,4 @@\\n-func main() { print(-(-2147483648)) }\\n+func main() {\\n+    let x: Int32 = -2147483648\\n+    print(-x)\\n+}\\ndiff --git a/prototypes/minivm/examples/overflow_sub.saw b/prototypes/minivm/examples/overflow_sub.saw\\nindex b70518b1..05bc667f 100644\\n--- a/prototypes/minivm/examples/overflow_sub.saw\\n+++ b/prototypes/minivm/examples/overflow_sub.saw\\n@@ -1 +1,4 @@\\n-func main() { print(-2147483648 - 1) }\\n+func main() {\\n+    let x: Int32 = -2147483648\\n+    print(x - 1)\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 534625a5..076adc9a 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -31,6 +31,50 @@ struct ExprValue {\\n     ty: ValueType\\n     slot: Int\\n     call: Bool\\n+    literal: Bool\\n+    literal_value: UInt\\n+    literal_negative: Bool\\n+}\\n+\\n+func expr_value(ty: ValueType, slot: Int, call: Bool) -> ExprValue {\\n+    ExprValue(ty: ty, slot: slot, call: call, literal: false,\\n+              literal_value: 0, literal_negative: false)\\n+}\\n+\\n+func type_name(t: ValueType) -> String {\\n+    match t {\\n+        case I8 -> \\\"Int8\\\", case I16 -> \\\"Int16\\\", case I32 -> \\\"Int32\\\", case I64 -> \\\"Int\\\",\\n+        case U8 -> \\\"UInt8\\\", case U16 -> \\\"UInt16\\\", case U32 -> \\\"UInt32\\\", case U64 -> \\\"UInt\\\",\\n+        case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case Void -> \\\"Void\\\"\\n+    }\\n+}\\n+\\n+func named_integer_type(name: String) -> ValueType? {\\n+    match name {\\n+        case \\\"Int8\\\" -> Some(ValueType.I8), case \\\"Int16\\\" -> Some(ValueType.I16),\\n+        case \\\"Int32\\\" -> Some(ValueType.I32), case \\\"Int64\\\" -> Some(ValueType.I64),\\n+        case \\\"Int\\\" -> Some(ValueType.I64), case \\\"UInt8\\\" -> Some(ValueType.U8),\\n+        case \\\"UInt16\\\" -> Some(ValueType.U16), case \\\"UInt32\\\" -> Some(ValueType.U32),\\n+        case \\\"UInt64\\\" -> Some(ValueType.U64), case \\\"UInt\\\" -> Some(ValueType.U64),\\n+        case \\\"Byte\\\" -> Some(ValueType.Byte), case _ -> None\\n+    }\\n+}\\n+\\n+func suffix_type(s: String) -> ValueType? {\\n+    match s {\\n+        case \\\"i8\\\" -> Some(ValueType.I8), case \\\"i16\\\" -> Some(ValueType.I16),\\n+        case \\\"i32\\\" -> Some(ValueType.I32), case \\\"i64\\\" -> Some(ValueType.I64),\\n+        case \\\"u8\\\" -> Some(ValueType.U8), case \\\"u16\\\" -> Some(ValueType.U16),\\n+        case \\\"u32\\\" -> Some(ValueType.U32), case \\\"u64\\\" -> Some(ValueType.U64),\\n+        case _ -> None\\n+    }\\n+}\\n+\\n+func literal_magnitude(value: String) -> UInt? {\\n+    if value.starts_with(\\\"0x\\\") { return value.substring(2, value.len()).to_uint(16) }\\n+    if value.starts_with(\\\"0b\\\") { return value.substring(2, value.len()).to_uint(2) }\\n+    if value.starts_with(\\\"0o\\\") { return value.substring(2, value.len()).to_uint(8) }\\n+    value.to_uint()\\n }\\n \\n struct Compiler {\\n@@ -75,10 +119,18 @@ extension Compiler {\\n         if self.paren_depth > 0 { self.skip_newlines() }\\n     }\\n \\n+    func literal_followed_by_cast(&self) -> Bool {\\n+        var i = self.pos + 1\\n+        if self.paren_depth > 0 {\\n+            while i < self.tokens.len() && self.tokens[i].kind == TokenKind.Newline { i += 1 }\\n+        }\\n+        i < self.tokens.len() && self.tokens[i].kind == TokenKind.As\\n+    }\\n+\\n     func parse_type(&var self) -> Result<ValueType, ProtoError> {\\n         let t = try self.expect(TokenKind.Ident, \\\"expected type\\\")\\n-        if t.value == \\\"Int32\\\" { return ValueType.I32 }\\n         if t.value == \\\"Bool\\\" { return ValueType.Bool }\\n+        if let ty = named_integer_type(t.value) { return ty }\\n         problem(\\\"unsupported type `{t.value}`\\\", t.line, t.col)\\n     }\\n \\n@@ -236,23 +288,76 @@ extension Compiler {\\n         return\\n     }\\n \\n+    func literal_fits_target(&self, v: &ExprValue, target: ValueType) -> Bool {\\n+        if not v.literal || not integer_type(target) || target == ValueType.Byte { return false }\\n+        if v.literal_negative {\\n+            if not integer_signed(target) { return false }\\n+            return v.literal_value <= UInt.from(truncating: integer_max(target)) + 1\\n+        }\\n+        v.literal_value <= UInt.from(truncating: integer_max(target))\\n+    }\\n+\\n+    func adopt_literal(&var self, v: ExprValue, target: ValueType, t: &Token) -> Result<ExprValue, ProtoError> {\\n+        if not self.literal_fits_target(&v, target) {\\n+            return problem(\\\"integer literal does not fit {type_name(target)}\\\", t.line, t.col)\\n+        }\\n+        var bits = Int.from(truncating: v.literal_value)\\n+        if v.literal_negative {\\n+            if v.literal_value == UInt.from(truncating: Int.max) + 1 { bits = Int.min }\\n+            else { bits = 0 - bits }\\n+        }\\n+        bits = integer_normalize(bits, target)\\n+        self.slots[v.slot] = target\\n+        var i = self.code.len()\\n+        while i > 0 {\\n+            i -= 1\\n+            if self.code[i].op == Op.Const && self.code[i].dst == v.slot {\\n+                self.code[i].immediate = bits\\n+                break\\n+            }\\n+        }\\n+        ExprValue(ty: target, slot: v.slot, call: false, literal: true,\\n+                  literal_value: v.literal_value, literal_negative: v.literal_negative)\\n+    }\\n+\\n+    func transfer(&var self, v: ExprValue, target: ValueType, t: &Token) -> Result<ExprValue, ProtoError> {\\n+        if v.ty == target {\\n+            if v.literal && not self.literal_fits_target(&v, target) {\\n+                return problem(\\\"integer literal does not fit {type_name(target)}\\\", t.line, t.col)\\n+            }\\n+            return v\\n+        }\\n+        if v.literal { return self.adopt_literal(v, target, &t) }\\n+        if integer_widens(v.ty, target) {\\n+            let dst = self.new_slot(target)\\n+            let at = self.emit_at(Op.Cast, t.line, t.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = v.slot\\n+            return expr_value(target, dst, false)\\n+        }\\n+        problem(\\\"cannot implicitly convert {type_name(v.ty)} to {type_name(target)}\\\", t.line, t.col)\\n+    }\\n+\\n     func parse_primary(&var self) -> Result<ExprValue, ProtoError> {\\n         if self.depth > 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n         if self.at(TokenKind.IntLit) {\\n             let t = self.take()\\n-            if t.value.starts_with(\\\"0x\\\") || t.value.starts_with(\\\"0b\\\") || t.value.starts_with(\\\"0o\\\") {\\n-                return problem(\\\"only decimal integer literals are supported\\\", t.line, t.col)\\n-            }\\n+            guard let magnitude = literal_magnitude(t.value) else { return problem(\\\"invalid integer literal\\\", t.line, t.col) }\\n+            var ty = ValueType.I64\\n+            var bare = true\\n             if let suffix = t.suffix {\\n-                if suffix != \\\"i32\\\" { return problem(\\\"unsupported integer suffix `{suffix}`\\\", t.line, t.col) }\\n+                bare = false\\n+                guard let st = suffix_type(suffix) else { return problem(\\\"unsupported integer suffix `{suffix}`\\\", t.line, t.col) }\\n+                ty = st\\n             }\\n-            guard let value = t.value.to_int() else { return problem(\\\"invalid integer literal\\\", t.line, t.col) }\\n-            if value > 2_147_483_647 { return problem(\\\"integer literal does not fit Int32\\\", t.line, t.col) }\\n-            let dst = self.new_slot(ValueType.I32)\\n+            let maximum = UInt.from(truncating: integer_max(ty))\\n+            if not bare && integer_signed(ty) && magnitude > maximum { return problem(\\\"integer literal does not fit {type_name(ty)}\\\", t.line, t.col) }\\n+            let dst = self.new_slot(ty)\\n             let at = self.emit_at(Op.Const, t.line, t.col)\\n             self.code[at].dst = dst\\n-            self.code[at].immediate = value\\n-            return ExprValue(ty: ValueType.I32, slot: dst, call: false)\\n+            self.code[at].immediate = integer_normalize(Int.from(truncating: magnitude), ty)\\n+            return ExprValue(ty: ty, slot: dst, call: false, literal: bare,\\n+                             literal_value: magnitude, literal_negative: false)\\n         }\\n         if self.at(TokenKind.True_) || self.at(TokenKind.False_) {\\n             let t = self.take()\\n@@ -260,7 +365,7 @@ extension Compiler {\\n             let at = self.emit_at(Op.Const, t.line, t.col)\\n             self.code[at].dst = dst\\n             self.code[at].immediate = if t.kind == TokenKind.True_ { 1 } else { 0 }\\n-            return ExprValue(ty: ValueType.Bool, slot: dst, call: false)\\n+            return expr_value(ValueType.Bool, dst, false)\\n         }\\n         if self.at(TokenKind.LParen) {\\n             let _ = self.take()\\n@@ -276,14 +381,71 @@ extension Compiler {\\n         }\\n         if self.at(TokenKind.Ident) {\\n             let name = self.take()\\n+            if let ty = named_integer_type(name.value) {\\n+                if self.at(TokenKind.Dot) { return self.parse_integer_member(name, ty) }\\n+                if self.at(TokenKind.LParen) && ty == ValueType.Byte { return self.parse_byte(name) }\\n+            }\\n             if self.at(TokenKind.LParen) { return self.parse_call(name) }\\n             let bi = self.binding_index(name.value)\\n             if bi < 0 { return problem(\\\"unknown name `{name.value}`\\\", name.line, name.col) }\\n-            return ExprValue(ty: self.bindings[bi].ty, slot: self.bindings[bi].slot, call: false)\\n+            return expr_value(self.bindings[bi].ty, self.bindings[bi].slot, false)\\n         }\\n         self.fail(\\\"expected expression; this syntax is unsupported\\\")\\n     }\\n \\n+    func parse_byte(&var self, name: Token) -> Result<ExprValue, ProtoError> {\\n+        let _ = self.take()\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        var value = try self.parse_expr()\\n+        value = try self.transfer(value, ValueType.U8, &name)\\n+        self.skip_newlines()\\n+        let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after Byte value\\\")\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        let dst = self.new_slot(ValueType.Byte)\\n+        let at = self.emit_at(Op.Cast, name.line, name.col)\\n+        self.code[at].dst = dst\\n+        self.code[at].a = value.slot\\n+        expr_value(ValueType.Byte, dst, false)\\n+    }\\n+\\n+    func parse_integer_member(&var self, name: Token, ty: ValueType) -> Result<ExprValue, ProtoError> {\\n+        let _ = self.take()\\n+        let member = try self.expect(TokenKind.Ident, \\\"expected numeric member\\\")\\n+        if member.value == \\\"min\\\" || member.value == \\\"max\\\" {\\n+            if ty == ValueType.Byte { return problem(\\\"Byte has no numeric limit members\\\", member.line, member.col) }\\n+            if self.at(TokenKind.LParen) { return problem(\\\"integer limits are constants\\\", member.line, member.col) }\\n+            let dst = self.new_slot(ty)\\n+            let at = self.emit_at(Op.Const, member.line, member.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].immediate = if member.value == \\\"min\\\" { integer_min(ty) } else { integer_max(ty) }\\n+            return expr_value(ty, dst, false)\\n+        }\\n+        if member.value != \\\"from\\\" { return problem(\\\"unsupported numeric member `{member.value}`\\\", member.line, member.col) }\\n+        let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after from\\\")\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        let label = try self.expect(TokenKind.Ident, \\\"only `from(truncating:)` is supported\\\")\\n+        if label.value != \\\"truncating\\\" { return problem(\\\"only `from(truncating:)` is supported\\\", label.line, label.col) }\\n+        let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after truncating\\\")\\n+        self.skip_newlines()\\n+        var value = try self.parse_expr()\\n+        value = try self.transfer(value, value.ty, &name)\\n+        if not integer_type(value.ty) || ty == ValueType.Byte { return problem(\\\"truncating conversion requires integer types\\\", name.line, name.col) }\\n+        self.skip_newlines()\\n+        let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after conversion\\\")\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        let dst = self.new_slot(ty)\\n+        let at = self.emit_at(Op.Truncate, name.line, name.col)\\n+        self.code[at].dst = dst\\n+        self.code[at].a = value.slot\\n+        expr_value(ty, dst, false)\\n+    }\\n+\\n     func parse_call(&var self, name: Token) -> Result<ExprValue, ProtoError> {\\n         let fi = self.signature_index(name.value)\\n         if fi < 0 { return problem(\\\"unknown function `{name.value}`\\\", name.line, name.col) }\\n@@ -296,11 +458,11 @@ extension Compiler {\\n         if not self.at(TokenKind.RParen) {\\n             var more = true\\n             while more {\\n-                let value = try self.parse_expr()\\n+                var value = try self.parse_expr()\\n                 if ai >= self.signatures[fi].params.len() {\\n                     return problem(\\\"too many arguments to `{name.value}`\\\", name.line, name.col)\\n                 }\\n-                try self.require_type(value.ty, self.signatures[fi].params[ai], &name)\\n+                value = try self.transfer(value, self.signatures[fi].params[ai], &name)\\n                 try! args.push(value.slot)\\n                 ai += 1\\n                 self.skip_newlines()\\n@@ -322,7 +484,7 @@ extension Compiler {\\n         self.code[at].target = fi\\n         self.code[at].dst = dst\\n         self.code[at].args = move args\\n-        ExprValue(ty: rt, slot: dst, call: true)\\n+        expr_value(rt, dst, true)\\n     }\\n \\n     func parse_unary_inner(&var self) -> Result<ExprValue, ProtoError> {\\n@@ -330,44 +492,90 @@ extension Compiler {\\n         if self.at(TokenKind.Exclaim) || self.at(TokenKind.Not_) {\\n             let t = self.take()\\n             self.expr_space()\\n-            let v = try self.parse_unary()\\n+            var v = try self.parse_unary()\\n+            v = try self.transfer(v, v.ty, &t)\\n             try self.require_type(v.ty, ValueType.Bool, &t)\\n             let dst = self.new_slot(ValueType.Bool)\\n             let at = self.emit_at(Op.Not, t.line, t.col)\\n             self.code[at].dst = dst\\n             self.code[at].a = v.slot\\n-            return ExprValue(ty: ValueType.Bool, slot: dst, call: false)\\n+            return expr_value(ValueType.Bool, dst, false)\\n         }\\n         if self.at(TokenKind.Minus) {\\n             let t = self.take()\\n             self.expr_space()\\n-            if self.at(TokenKind.IntLit) {\\n-                let lit = self.token()\\n-                if not lit.value.starts_with(\\\"0x\\\") && not lit.value.starts_with(\\\"0b\\\") && not lit.value.starts_with(\\\"0o\\\") {\\n-                    if let n = lit.value.to_int() {\\n-                        if n == 2_147_483_648 {\\n-                            let owned = self.take()\\n-                            if let suffix = owned.suffix {\\n-                                if suffix != \\\"i32\\\" { return problem(\\\"unsupported integer suffix `{suffix}`\\\", owned.line, owned.col) }\\n-                            }\\n-                            let dst = self.new_slot(ValueType.I32)\\n-                            let at = self.emit_at(Op.Const, t.line, t.col)\\n-                            self.code[at].dst = dst\\n-                            self.code[at].immediate = -2_147_483_648\\n-                            return ExprValue(ty: ValueType.I32, slot: dst, call: false)\\n-                        }\\n-                    }\\n+            if self.at(TokenKind.IntLit) && not self.literal_followed_by_cast() {\\n+                let lit = self.take()\\n+                guard let magnitude = literal_magnitude(lit.value) else { return problem(\\\"invalid integer literal\\\", lit.line, lit.col) }\\n+                var ty = ValueType.I64\\n+                var bare = true\\n+                if let suffix = lit.suffix {\\n+                    bare = false\\n+                    guard let st = suffix_type(suffix) else { return problem(\\\"unsupported integer suffix `{suffix}`\\\", lit.line, lit.col) }\\n+                    ty = st\\n                 }\\n+                if not integer_signed(ty) { return problem(\\\"cannot negate an unsigned integer\\\", t.line, t.col) }\\n+                let limit = UInt.from(truncating: integer_max(ty)) + 1\\n+                if magnitude > limit { return problem(\\\"integer literal does not fit {type_name(ty)}\\\", lit.line, lit.col) }\\n+                var bits = Int.from(truncating: magnitude)\\n+                if magnitude == UInt.from(truncating: Int.max) + 1 { bits = Int.min }\\n+                else { bits = 0 - bits }\\n+                bits = integer_normalize(bits, ty)\\n+                let dst = self.new_slot(ty)\\n+                let at = self.emit_at(Op.Const, t.line, t.col)\\n+                self.code[at].dst = dst\\n+                self.code[at].immediate = bits\\n+                let value = ExprValue(ty: ty, slot: dst, call: false, literal: bare,\\n+                                      literal_value: magnitude, literal_negative: true)\\n+                return value\\n             }\\n             let v = try self.parse_unary()\\n-            try self.require_type(v.ty, ValueType.I32, &t)\\n-            let dst = self.new_slot(ValueType.I32)\\n+            if not arithmetic_type(v.ty) || not integer_signed(v.ty) { return problem(\\\"unary `-` requires a signed integer\\\", t.line, t.col) }\\n+            let dst = self.new_slot(v.ty)\\n             let at = self.emit_at(Op.Neg, t.line, t.col)\\n             self.code[at].dst = dst\\n             self.code[at].a = v.slot\\n-            return ExprValue(ty: ValueType.I32, slot: dst, call: false)\\n+            return expr_value(v.ty, dst, false)\\n         }\\n-        self.parse_primary()\\n+        if self.at(TokenKind.Tilde) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            var v = try self.parse_unary()\\n+            v = try self.transfer(v, v.ty, &t)\\n+            if not arithmetic_type(v.ty) { return problem(\\\"operator `~` requires an integer\\\", t.line, t.col) }\\n+            let dst = self.new_slot(v.ty)\\n+            let at = self.emit_at(Op.BitNot, t.line, t.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = v.slot\\n+            return expr_value(v.ty, dst, false)\\n+        }\\n+        self.parse_cast()\\n+    }\\n+\\n+    func parse_cast(&var self) -> Result<ExprValue, ProtoError> {\\n+        let value = try self.parse_primary()\\n+        self.parse_casts(value)\\n+    }\\n+\\n+    func parse_casts(&var self, initial: ExprValue) -> Result<ExprValue, ProtoError> {\\n+        var value = initial\\n+        self.expr_space()\\n+        while self.at(TokenKind.As) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            let target = try self.parse_type()\\n+            value = try self.transfer(value, value.ty, &t)\\n+            if not integer_type(value.ty) || not integer_type(target) || target == ValueType.Byte {\\n+                return problem(\\\"`as` requires integer source and target types\\\", t.line, t.col)\\n+            }\\n+            let dst = self.new_slot(target)\\n+            let at = self.emit_at(Op.Cast, t.line, t.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = value.slot\\n+            value = expr_value(target, dst, false)\\n+            self.expr_space()\\n+        }\\n+        value\\n     }\\n \\n     func parse_unary(&var self) -> Result<ExprValue, ProtoError> {\\n@@ -385,22 +593,37 @@ extension Compiler {\\n         self.code[at].dst = dst\\n         self.code[at].a = left.slot\\n         self.code[at].b = right.slot\\n-        ExprValue(ty: result, slot: dst, call: false)\\n+        expr_value(result, dst, false)\\n+    }\\n+\\n+    func common_type(&self, left: &ExprValue, right: &ExprValue,\\n+                     t: &Token) -> Result<ValueType, ProtoError> {\\n+        if left.ty == right.ty {\\n+            if left.literal && not self.literal_fits_target(&left, left.ty) { return problem(\\\"integer literal does not fit {type_name(left.ty)}\\\", t.line, t.col) }\\n+            if right.literal && not self.literal_fits_target(&right, right.ty) { return problem(\\\"integer literal does not fit {type_name(right.ty)}\\\", t.line, t.col) }\\n+            return left.ty\\n+        }\\n+        if left.literal && not right.literal { return right.ty }\\n+        if right.literal && not left.literal { return left.ty }\\n+        problem(\\\"numeric operands must have the same type\\\", t.line, t.col)\\n     }\\n \\n     func parse_mul(&var self) -> Result<ExprValue, ProtoError> {\\n         self.expr_space()\\n         var left = try self.parse_unary()\\n         self.expr_space()\\n-        while self.at(TokenKind.Star) || self.at(TokenKind.Slash) || self.at(TokenKind.Percent) {\\n+        while self.at(TokenKind.Star) || self.at(TokenKind.Slash) || self.at(TokenKind.Percent) || self.at(TokenKind.WrapMul) {\\n             let t = self.take()\\n             self.expr_space()\\n-            let right = try self.parse_unary()\\n+            var right = try self.parse_unary()\\n             self.expr_space()\\n-            try self.require_type(left.ty, ValueType.I32, &t)\\n-            try self.require_type(right.ty, ValueType.I32, &t)\\n-            let op = if t.kind == TokenKind.Star { Op.Mul } else if t.kind == TokenKind.Slash { Op.Div } else { Op.Rem }\\n-            left = try self.binary(left, right, op, ValueType.I32, &t)\\n+            let common = try self.common_type(&left, &right, &t)\\n+            if left.ty != common { left = try self.adopt_literal(left, common, &t) }\\n+            if right.ty != common { right = try self.adopt_literal(right, common, &t) }\\n+            if not arithmetic_type(left.ty) { return problem(\\\"arithmetic requires integer operands\\\", t.line, t.col) }\\n+            let op = if t.kind == TokenKind.Star { Op.Mul } else if t.kind == TokenKind.Slash { Op.Div } else if t.kind == TokenKind.Percent { Op.Rem } else { Op.WrapMul }\\n+            let result = left.ty\\n+            left = try self.binary(left, right, op, result, &t)\\n         }\\n         left\\n     }\\n@@ -408,49 +631,140 @@ extension Compiler {\\n     func parse_add(&var self) -> Result<ExprValue, ProtoError> {\\n         var left = try self.parse_mul()\\n         self.expr_space()\\n-        while self.at(TokenKind.Plus) || self.at(TokenKind.Minus) {\\n+        while self.at(TokenKind.Plus) || self.at(TokenKind.Minus) || self.at(TokenKind.WrapAdd) || self.at(TokenKind.WrapSub) {\\n             let t = self.take()\\n             self.expr_space()\\n-            let right = try self.parse_mul()\\n+            var right = try self.parse_mul()\\n             self.expr_space()\\n-            try self.require_type(left.ty, ValueType.I32, &t)\\n-            try self.require_type(right.ty, ValueType.I32, &t)\\n-            left = try self.binary(left, right, if t.kind == TokenKind.Plus { Op.Add } else { Op.Sub }, ValueType.I32, &t)\\n+            let common = try self.common_type(&left, &right, &t)\\n+            if left.ty != common { left = try self.adopt_literal(left, common, &t) }\\n+            if right.ty != common { right = try self.adopt_literal(right, common, &t) }\\n+            if not arithmetic_type(left.ty) { return problem(\\\"arithmetic requires integer operands\\\", t.line, t.col) }\\n+            let op = if t.kind == TokenKind.Plus { Op.Add } else if t.kind == TokenKind.Minus { Op.Sub } else if t.kind == TokenKind.WrapAdd { Op.WrapAdd } else { Op.WrapSub }\\n+            let result = left.ty\\n+            left = try self.binary(left, right, op, result, &t)\\n         }\\n         left\\n     }\\n \\n-    func parse_compare(&var self) -> Result<ExprValue, ProtoError> {\\n+    func adjacent(&self, a: &Token, b: &Token) -> Bool {\\n+        a.line == b.line && b.col == a.col + 1\\n+    }\\n+\\n+    func shift_ahead(&self, kind: TokenKind) -> Bool {\\n+        if self.pos + 1 >= self.tokens.len() || not self.at(kind) || self.tokens[self.pos + 1].kind != kind { return false }\\n+        let a = self.token()\\n+        self.adjacent(&a, &self.tokens[self.pos + 1])\\n+    }\\n+\\n+    func parse_shift(&var self) -> Result<ExprValue, ProtoError> {\\n         var left = try self.parse_add()\\n         self.expr_space()\\n+        while self.shift_ahead(TokenKind.Lt) || self.shift_ahead(TokenKind.Gt) {\\n+            let t = self.take()\\n+            let _ = self.take()\\n+            self.expr_space()\\n+            var right = try self.parse_add()\\n+            self.expr_space()\\n+            left = try self.transfer(left, left.ty, &t)\\n+            right = try self.transfer(right, right.ty, &t)\\n+            if not arithmetic_type(left.ty) || not arithmetic_type(right.ty) { return problem(\\\"shift requires integer operands\\\", t.line, t.col) }\\n+            let result = left.ty\\n+            left = try self.binary(left, right, if t.kind == TokenKind.Lt { Op.Shl } else { Op.Shr }, result, &t)\\n+        }\\n+        left\\n+    }\\n+\\n+    func parse_compare(&var self) -> Result<ExprValue, ProtoError> {\\n+        var left = try self.parse_shift()\\n+        self.expr_space()\\n         if self.at(TokenKind.Lt) || self.at(TokenKind.Lte) || self.at(TokenKind.Gt) || self.at(TokenKind.Gte) {\\n             let t = self.take()\\n             self.expr_space()\\n-            let right = try self.parse_add()\\n+            var right = try self.parse_shift()\\n             self.expr_space()\\n-            try self.require_type(left.ty, ValueType.I32, &t)\\n-            try self.require_type(right.ty, ValueType.I32, &t)\\n+            let common = try self.common_type(&left, &right, &t)\\n+            if left.ty != common { left = try self.adopt_literal(left, common, &t) }\\n+            if right.ty != common { right = try self.adopt_literal(right, common, &t) }\\n+            if not arithmetic_type(left.ty) { return problem(\\\"comparison requires integer operands\\\", t.line, t.col) }\\n             let op = if t.kind == TokenKind.Lt { Op.Lt } else if t.kind == TokenKind.Lte { Op.Le } else if t.kind == TokenKind.Gt { Op.Gt } else { Op.Ge }\\n             left = try self.binary(left, right, op, ValueType.Bool, &t)\\n         }\\n         left\\n     }\\n \\n-    func parse_expr(&var self) -> Result<ExprValue, ProtoError> {\\n+    func parse_equality(&var self) -> Result<ExprValue, ProtoError> {\\n         self.expr_space()\\n         var left = try self.parse_compare()\\n         self.expr_space()\\n         if self.at(TokenKind.Eq) || self.at(TokenKind.Neq) {\\n             let t = self.take()\\n             self.expr_space()\\n-            let right = try self.parse_compare()\\n+            var right = try self.parse_compare()\\n             self.expr_space()\\n+            let common = try self.common_type(&left, &right, &t)\\n+            if left.ty != common { left = try self.adopt_literal(left, common, &t) }\\n+            if right.ty != common { right = try self.adopt_literal(right, common, &t) }\\n             if left.ty == ValueType.Void || left.ty != right.ty { return problem(\\\"equality operands must have the same scalar type\\\", t.line, t.col) }\\n             left = try self.binary(left, right, if t.kind == TokenKind.Eq { Op.Eq } else { Op.Ne }, ValueType.Bool, &t)\\n         }\\n         left\\n     }\\n \\n+    func parse_bit_and(&var self) -> Result<ExprValue, ProtoError> {\\n+        var left = try self.parse_equality()\\n+        self.expr_space()\\n+        while self.at(TokenKind.Ampersand) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            var right = try self.parse_equality()\\n+            self.expr_space()\\n+            let common = try self.common_type(&left, &right, &t)\\n+            if left.ty != common { left = try self.adopt_literal(left, common, &t) }\\n+            if right.ty != common { right = try self.adopt_literal(right, common, &t) }\\n+            if not arithmetic_type(left.ty) { return problem(\\\"bitwise operator requires integer operands\\\", t.line, t.col) }\\n+            let result = left.ty\\n+            left = try self.binary(left, right, Op.BitAnd, result, &t)\\n+        }\\n+        left\\n+    }\\n+\\n+    func parse_bit_xor(&var self) -> Result<ExprValue, ProtoError> {\\n+        var left = try self.parse_bit_and()\\n+        self.expr_space()\\n+        while self.at(TokenKind.Caret) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            var right = try self.parse_bit_and()\\n+            self.expr_space()\\n+            let common = try self.common_type(&left, &right, &t)\\n+            if left.ty != common { left = try self.adopt_literal(left, common, &t) }\\n+            if right.ty != common { right = try self.adopt_literal(right, common, &t) }\\n+            if not arithmetic_type(left.ty) { return problem(\\\"bitwise operator requires integer operands\\\", t.line, t.col) }\\n+            let result = left.ty\\n+            left = try self.binary(left, right, Op.BitXor, result, &t)\\n+        }\\n+        left\\n+    }\\n+\\n+    func parse_expr(&var self) -> Result<ExprValue, ProtoError> {\\n+        var left = try self.parse_bit_xor()\\n+        self.expr_space()\\n+        while self.at(TokenKind.Pipe) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            var right = try self.parse_bit_xor()\\n+            self.expr_space()\\n+            let common = try self.common_type(&left, &right, &t)\\n+            if left.ty != common { left = try self.adopt_literal(left, common, &t) }\\n+            if right.ty != common { right = try self.adopt_literal(right, common, &t) }\\n+            if not arithmetic_type(left.ty) { return problem(\\\"bitwise operator requires integer operands\\\", t.line, t.col) }\\n+            let result = left.ty\\n+            left = try self.binary(left, right, Op.BitOr, result, &t)\\n+        }\\n+        left\\n+    }\\n+\\n     func parse_block(&var self) -> Result<Bool, ProtoError> {\\n         if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n         self.depth += 1\\n@@ -478,9 +792,10 @@ extension Compiler {\\n                 annotated = Some(try self.parse_type())\\n             }\\n             let _ = try self.expect(TokenKind.Assign, \\\"local declarations require an initializer\\\")\\n-            let value = try self.parse_expr()\\n+            var value = try self.parse_expr()\\n             if value.ty == ValueType.Void { return problem(\\\"cannot initialize a local with Void\\\", name.line, name.col) }\\n-            if let ty = annotated { try self.require_type(value.ty, ty, &name) }\\n+            if let ty = annotated { value = try self.transfer(value, ty, &name) }\\n+            else { value = try self.transfer(value, value.ty, &name) }\\n             let dst = self.new_slot(value.ty)\\n             let at = self.emit_at(Op.Copy, name.line, name.col)\\n             self.code[at].dst = dst\\n@@ -492,8 +807,8 @@ extension Compiler {\\n             let t = self.take()\\n             var src = -1\\n             if not self.at(TokenKind.Newline) && not self.at(TokenKind.RBrace) {\\n-                let value = try self.parse_expr()\\n-                try self.require_type(value.ty, self.current_result, &t)\\n+                var value = try self.parse_expr()\\n+                value = try self.transfer(value, self.current_result, &t)\\n                 src = value.slot\\n             } else if self.current_result != ValueType.Void { return problem(\\\"non-Void return requires a value\\\", t.line, t.col) }\\n             let at = self.emit_at(Op.Return, t.line, t.col)\\n@@ -551,7 +866,8 @@ extension Compiler {\\n             self.depth += 1\\n             self.paren_depth += 1\\n             self.skip_newlines()\\n-            let value = try self.parse_expr()\\n+            var value = try self.parse_expr()\\n+            value = try self.transfer(value, value.ty, &t)\\n             self.skip_newlines()\\n             let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after print argument\\\")\\n             self.paren_depth -= 1\\n@@ -567,8 +883,8 @@ extension Compiler {\\n             let bi = self.binding_index(name.value)\\n             if bi < 0 { return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col) }\\n             if not self.bindings[bi].mutable { return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col) }\\n-            let value = try self.parse_expr()\\n-            try self.require_type(value.ty, self.bindings[bi].ty, &name)\\n+            var value = try self.parse_expr()\\n+            value = try self.transfer(value, self.bindings[bi].ty, &name)\\n             let at = self.emit_at(Op.Copy, name.line, name.col)\\n             self.code[at].dst = self.bindings[bi].slot\\n             self.code[at].a = value.slot\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex 60d21ef7..f92b2df0 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -47,22 +47,59 @@ func store_temp(out: &var StringBuilder, instruction_index: Int,\\n     put(&var out, \\\"\\\\n\\\")\\n }\\n \\n+func emit_normalized_result(out: &var StringBuilder, instruction_index: Int,\\n+                            dst: Int, suffix: String, t: ValueType) {\\n+    let width = integer_width(t)\\n+    if width == 64 {\\n+        store_temp(&var out, instruction_index, suffix, dst)\\n+    } else {\\n+        put(&var out, \\\"  \\\")\\n+        temp_name(&var out, instruction_index, \\\"nl\\\")\\n+        put(&var out, \\\" = shl i64 \\\")\\n+        temp_name(&var out, instruction_index, suffix)\\n+        put(&var out, \\\", \\\")\\n+        put_int(&var out, 64 - width)\\n+        put(&var out, \\\"\\\\n  \\\")\\n+        temp_name(&var out, instruction_index, \\\"norm\\\")\\n+        put(&var out, if integer_signed(t) { \\\" = ashr i64 \\\" } else { \\\" = lshr i64 \\\" })\\n+        temp_name(&var out, instruction_index, \\\"nl\\\")\\n+        put(&var out, \\\", \\\")\\n+        put_int(&var out, 64 - width)\\n+        put(&var out, \\\"\\\\n\\\")\\n+        store_temp(&var out, instruction_index, \\\"norm\\\", dst)\\n+    }\\n+    next_block(&var out, instruction_index)\\n+}\\n+\\n func emit_checked_result(out: &var StringBuilder, instruction_index: Int,\\n-                         dst: Int, value_suffix: String) {\\n+                         dst: Int, value_suffix: String, t: ValueType,\\n+                         intrinsic_bad: Bool) {\\n     put(&var out, \\\"  \\\")\\n     temp_name(&var out, instruction_index, \\\"lo\\\")\\n-    put(&var out, \\\" = icmp slt i64 \\\")\\n+    put(&var out, if integer_signed(t) { \\\" = icmp slt i64 \\\" } else { \\\" = icmp ult i64 \\\" })\\n     temp_name(&var out, instruction_index, value_suffix)\\n-    put(&var out, \\\", -2147483648\\\\n  \\\")\\n+    put(&var out, \\\", \\\")\\n+    put_int(&var out, integer_min(t))\\n+    put(&var out, \\\"\\\\n  \\\")\\n     temp_name(&var out, instruction_index, \\\"hi\\\")\\n-    put(&var out, \\\" = icmp sgt i64 \\\")\\n+    put(&var out, if integer_signed(t) { \\\" = icmp sgt i64 \\\" } else { \\\" = icmp ugt i64 \\\" })\\n     temp_name(&var out, instruction_index, value_suffix)\\n-    put(&var out, \\\", 2147483647\\\\n  \\\")\\n-    temp_name(&var out, instruction_index, \\\"bad\\\")\\n+    put(&var out, \\\", \\\")\\n+    put_int(&var out, integer_max(t))\\n+    put(&var out, \\\"\\\\n  \\\")\\n+    temp_name(&var out, instruction_index, if intrinsic_bad { \\\"rangebad\\\" } else { \\\"bad\\\" })\\n     put(&var out, \\\" = or i1 \\\")\\n     temp_name(&var out, instruction_index, \\\"lo\\\")\\n     put(&var out, \\\", \\\")\\n     temp_name(&var out, instruction_index, \\\"hi\\\")\\n+    if intrinsic_bad {\\n+        put(&var out, \\\"\\\\n  \\\")\\n+        temp_name(&var out, instruction_index, \\\"bad\\\")\\n+        put(&var out, \\\" = or i1 \\\")\\n+        temp_name(&var out, instruction_index, \\\"rangebad\\\")\\n+        put(&var out, \\\", \\\")\\n+        temp_name(&var out, instruction_index, \\\"ov\\\")\\n+    }\\n     put(&var out, \\\"\\\\n  br i1 \\\")\\n     temp_name(&var out, instruction_index, \\\"bad\\\")\\n     put(&var out, \\\", label %i\\\")\\n@@ -79,25 +116,40 @@ func emit_checked_result(out: &var StringBuilder, instruction_index: Int,\\n }\\n \\n func emit_binary(out: &var StringBuilder, instruction_index: Int,\\n-                 inst: &Instruction, llvm_op: String, checked: Bool) {\\n+                 inst: &Instruction, llvm_op: String, checked: Bool, t: ValueType) {\\n     put(&var out, \\\"  \\\")\\n     load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n     put(&var out, \\\"  \\\")\\n     load_slot(&var out, instruction_index, \\\"b\\\", inst.b)\\n     put(&var out, \\\"  \\\")\\n-    temp_name(&var out, instruction_index, \\\"v\\\")\\n+    temp_name(&var out, instruction_index, if checked { \\\"pair\\\" } else { \\\"v\\\" })\\n     put(&var out, \\\" = \\\")\\n-    put(&var out, llvm_op)\\n-    put(&var out, \\\" i64 \\\")\\n+    if checked {\\n+        put(&var out, \\\"call \\\\{i64, i1\\\\} @llvm.\\\")\\n+        put(&var out, if integer_signed(t) { \\\"s\\\" } else { \\\"u\\\" })\\n+        put(&var out, llvm_op)\\n+        put(&var out, \\\".with.overflow.i64(i64 \\\")\\n+    } else {\\n+        put(&var out, llvm_op)\\n+        put(&var out, \\\" i64 \\\")\\n+    }\\n     temp_name(&var out, instruction_index, \\\"a\\\")\\n     put(&var out, \\\", \\\")\\n+    if checked { put(&var out, \\\"i64 \\\") }\\n     temp_name(&var out, instruction_index, \\\"b\\\")\\n-    put(&var out, \\\"\\\\n\\\")\\n+    put(&var out, if checked { \\\")\\\\n  \\\" } else { \\\"\\\\n\\\" })\\n     if checked {\\n-        emit_checked_result(&var out, instruction_index, inst.dst, \\\"v\\\")\\n+        temp_name(&var out, instruction_index, \\\"v\\\")\\n+        put(&var out, \\\" = extractvalue \\\\{i64, i1\\\\} \\\")\\n+        temp_name(&var out, instruction_index, \\\"pair\\\")\\n+        put(&var out, \\\", 0\\\\n  \\\")\\n+        temp_name(&var out, instruction_index, \\\"ov\\\")\\n+        put(&var out, \\\" = extractvalue \\\\{i64, i1\\\\} \\\")\\n+        temp_name(&var out, instruction_index, \\\"pair\\\")\\n+        put(&var out, \\\", 1\\\\n\\\")\\n+        emit_checked_result(&var out, instruction_index, inst.dst, \\\"v\\\", t, true)\\n     } else {\\n-        store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n-        next_block(&var out, instruction_index)\\n+        emit_normalized_result(&var out, instruction_index, inst.dst, \\\"v\\\", t)\\n     }\\n }\\n \\n@@ -124,8 +176,83 @@ func emit_compare(out: &var StringBuilder, instruction_index: Int,\\n     next_block(&var out, instruction_index)\\n }\\n \\n+func emit_cast(out: &var StringBuilder, instruction_index: Int,\\n+               inst: &Instruction, source: ValueType, target: ValueType,\\n+               checked: Bool) {\\n+    put(&var out, \\\"  \\\")\\n+    load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+    if checked {\\n+        put(&var out, \\\"  \\\")\\n+        temp_name(&var out, instruction_index, \\\"lo\\\")\\n+        if integer_signed(source) {\\n+            put(&var out, \\\" = icmp slt i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"a\\\")\\n+            put(&var out, \\\", \\\")\\n+            put_int(&var out, integer_min(target))\\n+        } else { put(&var out, \\\" = icmp ne i64 0, 0\\\") }\\n+        put(&var out, \\\"\\\\n  \\\")\\n+        temp_name(&var out, instruction_index, \\\"hi\\\")\\n+        put(&var out, if integer_signed(source) && integer_signed(target) { \\\" = icmp sgt i64 \\\" } else { \\\" = icmp ugt i64 \\\" })\\n+        temp_name(&var out, instruction_index, \\\"a\\\")\\n+        put(&var out, \\\", \\\")\\n+        put_int(&var out, integer_max(target))\\n+        put(&var out, \\\"\\\\n  \\\")\\n+        temp_name(&var out, instruction_index, \\\"bad\\\")\\n+        put(&var out, \\\" = or i1 \\\")\\n+        temp_name(&var out, instruction_index, \\\"lo\\\")\\n+        put(&var out, \\\", \\\")\\n+        temp_name(&var out, instruction_index, \\\"hi\\\")\\n+        put(&var out, \\\"\\\\n  br i1 \\\")\\n+        temp_name(&var out, instruction_index, \\\"bad\\\")\\n+        put(&var out, \\\", label %i\\\")\\n+        put_int(&var out, instruction_index)\\n+        put(&var out, \\\".cast, label %i\\\")\\n+        put_int(&var out, instruction_index)\\n+        put(&var out, \\\".castok\\\\n\\\\ni\\\")\\n+        put_int(&var out, instruction_index)\\n+        put(&var out, \\\".cast:\\\\n  call void @minivm_cast()\\\\n  unreachable\\\\n\\\\ni\\\")\\n+        put_int(&var out, instruction_index)\\n+        put(&var out, \\\".castok:\\\\n\\\")\\n+    }\\n+    emit_normalized_result(&var out, instruction_index, inst.dst, \\\"a\\\", target)\\n+}\\n+\\n+func emit_shift(out: &var StringBuilder, instruction_index: Int,\\n+                inst: &Instruction, t: ValueType, left: Bool) {\\n+    put(&var out, \\\"  \\\")\\n+    load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+    put(&var out, \\\"  \\\")\\n+    load_slot(&var out, instruction_index, \\\"b\\\", inst.b)\\n+    put(&var out, \\\"  \\\")\\n+    temp_name(&var out, instruction_index, \\\"bad\\\")\\n+    put(&var out, \\\" = icmp uge i64 \\\")\\n+    temp_name(&var out, instruction_index, \\\"b\\\")\\n+    put(&var out, \\\", \\\")\\n+    put_int(&var out, integer_width(t))\\n+    put(&var out, \\\"\\\\n  br i1 \\\")\\n+    temp_name(&var out, instruction_index, \\\"bad\\\")\\n+    put(&var out, \\\", label %i\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".shift, label %i\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".shiftok\\\\n\\\\ni\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".shift:\\\\n  call void @minivm_shift()\\\\n  unreachable\\\\n\\\\ni\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".shiftok:\\\\n  \\\")\\n+    temp_name(&var out, instruction_index, \\\"v\\\")\\n+    if left { put(&var out, \\\" = shl i64 \\\") }\\n+    else if integer_signed(t) { put(&var out, \\\" = ashr i64 \\\") }\\n+    else { put(&var out, \\\" = lshr i64 \\\") }\\n+    temp_name(&var out, instruction_index, \\\"a\\\")\\n+    put(&var out, \\\", \\\")\\n+    temp_name(&var out, instruction_index, \\\"b\\\")\\n+    put(&var out, \\\"\\\\n\\\")\\n+    emit_normalized_result(&var out, instruction_index, inst.dst, \\\"v\\\", t)\\n+}\\n+\\n func emit_division(out: &var StringBuilder, instruction_index: Int,\\n-                   inst: &Instruction, remainder: Bool) {\\n+                   inst: &Instruction, remainder: Bool, t: ValueType) {\\n     put(&var out, \\\"  \\\")\\n     load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n     put(&var out, \\\"  \\\")\\n@@ -134,29 +261,58 @@ func emit_division(out: &var StringBuilder, instruction_index: Int,\\n     temp_name(&var out, instruction_index, \\\"zero\\\")\\n     put(&var out, \\\" = icmp eq i64 \\\")\\n     temp_name(&var out, instruction_index, \\\"b\\\")\\n-    put(&var out, \\\", 0\\\\n  br i1 \\\")\\n+    put(&var out, \\\", 0\\\\n\\\")\\n+    if integer_signed(t) {\\n+        put(&var out, \\\"  \\\")\\n+        temp_name(&var out, instruction_index, \\\"min\\\")\\n+        put(&var out, \\\" = icmp eq i64 \\\")\\n+        temp_name(&var out, instruction_index, \\\"a\\\")\\n+        put(&var out, \\\", \\\")\\n+        put_int(&var out, integer_min(t))\\n+        put(&var out, \\\"\\\\n  \\\")\\n+        temp_name(&var out, instruction_index, \\\"negone\\\")\\n+        put(&var out, \\\" = icmp eq i64 \\\")\\n+        temp_name(&var out, instruction_index, \\\"b\\\")\\n+        put(&var out, \\\", -1\\\\n  \\\")\\n+        temp_name(&var out, instruction_index, \\\"minneg\\\")\\n+        put(&var out, \\\" = and i1 \\\")\\n+        temp_name(&var out, instruction_index, \\\"min\\\")\\n+        put(&var out, \\\", \\\")\\n+        temp_name(&var out, instruction_index, \\\"negone\\\")\\n+        put(&var out, \\\"\\\\n\\\")\\n+    }\\n+    put(&var out, \\\"  br i1 \\\")\\n     temp_name(&var out, instruction_index, \\\"zero\\\")\\n     put(&var out, \\\", label %i\\\")\\n     put_int(&var out, instruction_index)\\n     put(&var out, \\\".divzero, label %i\\\")\\n     put_int(&var out, instruction_index)\\n-    put(&var out, \\\".divide\\\\n\\\\ni\\\")\\n+    put(&var out, if integer_signed(t) { \\\".divcheck\\\\n\\\\ni\\\" } else { \\\".divide\\\\n\\\\ni\\\" })\\n     put_int(&var out, instruction_index)\\n     put(&var out, \\\".divzero:\\\\n  call void @minivm_divzero()\\\\n  unreachable\\\\n\\\\ni\\\")\\n     put_int(&var out, instruction_index)\\n+    if integer_signed(t) {\\n+        put(&var out, \\\".divcheck:\\\\n  br i1 \\\")\\n+        temp_name(&var out, instruction_index, \\\"minneg\\\")\\n+        put(&var out, \\\", label %i\\\")\\n+        put_int(&var out, instruction_index)\\n+        put(&var out, \\\".divoverflow, label %i\\\")\\n+        put_int(&var out, instruction_index)\\n+        put(&var out, \\\".divide\\\\n\\\\ni\\\")\\n+        put_int(&var out, instruction_index)\\n+        put(&var out, \\\".divoverflow:\\\\n  call void @minivm_overflow()\\\\n  unreachable\\\\n\\\\ni\\\")\\n+        put_int(&var out, instruction_index)\\n+    }\\n     put(&var out, \\\".divide:\\\\n  \\\")\\n     temp_name(&var out, instruction_index, \\\"v\\\")\\n-    put(&var out, if remainder { \\\" = srem i64 \\\" } else { \\\" = sdiv i64 \\\" })\\n+    if integer_signed(t) { put(&var out, if remainder { \\\" = srem i64 \\\" } else { \\\" = sdiv i64 \\\" }) }\\n+    else { put(&var out, if remainder { \\\" = urem i64 \\\" } else { \\\" = udiv i64 \\\" }) }\\n     temp_name(&var out, instruction_index, \\\"a\\\")\\n     put(&var out, \\\", \\\")\\n     temp_name(&var out, instruction_index, \\\"b\\\")\\n     put(&var out, \\\"\\\\n\\\")\\n-    if remainder {\\n-        store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n-        next_block(&var out, instruction_index)\\n-    } else {\\n-        emit_checked_result(&var out, instruction_index, inst.dst, \\\"v\\\")\\n-    }\\n+    store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+    next_block(&var out, instruction_index)\\n }\\n \\n func emit_instruction(out: &var StringBuilder, program: &Program,\\n@@ -179,21 +335,49 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n             next_block(&var out, instruction_index)\\n         },\\n-        case Add -> emit_binary(&var out, instruction_index, &inst, \\\"add\\\", true),\\n-        case Sub -> emit_binary(&var out, instruction_index, &inst, \\\"sub\\\", true),\\n-        case Mul -> emit_binary(&var out, instruction_index, &inst, \\\"mul\\\", true),\\n-        case Div -> emit_division(&var out, instruction_index, &inst, false),\\n-        case Rem -> emit_division(&var out, instruction_index, &inst, true),\\n+        case Add -> emit_binary(&var out, instruction_index, &inst, \\\"add\\\", true, function.slots[inst.dst]),\\n+        case Sub -> emit_binary(&var out, instruction_index, &inst, \\\"sub\\\", true, function.slots[inst.dst]),\\n+        case Mul -> emit_binary(&var out, instruction_index, &inst, \\\"mul\\\", true, function.slots[inst.dst]),\\n+        case Div -> emit_division(&var out, instruction_index, &inst, false, function.slots[inst.dst]),\\n+        case Rem -> emit_division(&var out, instruction_index, &inst, true, function.slots[inst.dst]),\\n         case Neg -> {\\n             put(&var out, \\\"  \\\")\\n             load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n             put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"pair\\\")\\n+            put(&var out, \\\" = call \\\\{i64, i1\\\\} @llvm.ssub.with.overflow.i64(i64 0, i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"a\\\")\\n+            put(&var out, \\\")\\\\n  \\\")\\n             temp_name(&var out, instruction_index, \\\"v\\\")\\n-            put(&var out, \\\" = sub i64 0, \\\")\\n+            put(&var out, \\\" = extractvalue \\\\{i64, i1\\\\} \\\")\\n+            temp_name(&var out, instruction_index, \\\"pair\\\")\\n+            put(&var out, \\\", 0\\\\n  \\\")\\n+            temp_name(&var out, instruction_index, \\\"ov\\\")\\n+            put(&var out, \\\" = extractvalue \\\\{i64, i1\\\\} \\\")\\n+            temp_name(&var out, instruction_index, \\\"pair\\\")\\n+            put(&var out, \\\", 1\\\\n\\\")\\n+            emit_checked_result(&var out, instruction_index, inst.dst, \\\"v\\\", function.slots[inst.dst], true)\\n+        },\\n+        case WrapAdd -> emit_binary(&var out, instruction_index, &inst, \\\"add\\\", false, function.slots[inst.dst]),\\n+        case WrapSub -> emit_binary(&var out, instruction_index, &inst, \\\"sub\\\", false, function.slots[inst.dst]),\\n+        case WrapMul -> emit_binary(&var out, instruction_index, &inst, \\\"mul\\\", false, function.slots[inst.dst]),\\n+        case BitAnd -> emit_binary(&var out, instruction_index, &inst, \\\"and\\\", false, function.slots[inst.dst]),\\n+        case BitOr -> emit_binary(&var out, instruction_index, &inst, \\\"or\\\", false, function.slots[inst.dst]),\\n+        case BitXor -> emit_binary(&var out, instruction_index, &inst, \\\"xor\\\", false, function.slots[inst.dst]),\\n+        case BitNot -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"v\\\")\\n+            put(&var out, \\\" = xor i64 \\\")\\n             temp_name(&var out, instruction_index, \\\"a\\\")\\n-            put(&var out, \\\"\\\\n\\\")\\n-            emit_checked_result(&var out, instruction_index, inst.dst, \\\"v\\\")\\n+            put(&var out, \\\", -1\\\\n\\\")\\n+            emit_normalized_result(&var out, instruction_index, inst.dst, \\\"v\\\", function.slots[inst.dst])\\n         },\\n+        case Cast -> emit_cast(&var out, instruction_index, &inst, function.slots[inst.a], function.slots[inst.dst], true),\\n+        case Truncate -> emit_cast(&var out, instruction_index, &inst, function.slots[inst.a], function.slots[inst.dst], false),\\n+        case Shl -> emit_shift(&var out, instruction_index, &inst, function.slots[inst.dst], true),\\n+        case Shr -> emit_shift(&var out, instruction_index, &inst, function.slots[inst.dst], false),\\n         case Not -> {\\n             put(&var out, \\\"  \\\")\\n             load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n@@ -211,10 +395,10 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n         },\\n         case Eq -> emit_compare(&var out, instruction_index, &inst, \\\"eq\\\"),\\n         case Ne -> emit_compare(&var out, instruction_index, &inst, \\\"ne\\\"),\\n-        case Lt -> emit_compare(&var out, instruction_index, &inst, \\\"slt\\\"),\\n-        case Le -> emit_compare(&var out, instruction_index, &inst, \\\"sle\\\"),\\n-        case Gt -> emit_compare(&var out, instruction_index, &inst, \\\"sgt\\\"),\\n-        case Ge -> emit_compare(&var out, instruction_index, &inst, \\\"sge\\\"),\\n+        case Lt -> emit_compare(&var out, instruction_index, &inst, if integer_signed(function.slots[inst.a]) { \\\"slt\\\" } else { \\\"ult\\\" }),\\n+        case Le -> emit_compare(&var out, instruction_index, &inst, if integer_signed(function.slots[inst.a]) { \\\"sle\\\" } else { \\\"ule\\\" }),\\n+        case Gt -> emit_compare(&var out, instruction_index, &inst, if integer_signed(function.slots[inst.a]) { \\\"sgt\\\" } else { \\\"ugt\\\" }),\\n+        case Ge -> emit_compare(&var out, instruction_index, &inst, if integer_signed(function.slots[inst.a]) { \\\"sge\\\" } else { \\\"uge\\\" }),\\n         case Jump -> {\\n             put(&var out, \\\"  br label %i\\\")\\n             put_int(&var out, inst.target)\\n@@ -278,10 +462,14 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 put(&var out, \\\", ptr @.true, ptr @.false\\\\n  call i32 @puts(ptr \\\")\\n                 temp_name(&var out, instruction_index, \\\"str\\\")\\n                 put(&var out, \\\")\\\\n\\\")\\n-            } else {\\n+            } else if integer_signed(function.slots[inst.a]) {\\n                 put(&var out, \\\"  call i32 (ptr, ...) @printf(ptr @.fmt_i64, i64 \\\")\\n                 temp_name(&var out, instruction_index, \\\"a\\\")\\n                 put(&var out, \\\")\\\\n\\\")\\n+            } else {\\n+                put(&var out, \\\"  call i32 (ptr, ...) @printf(ptr @.fmt_u64, i64 \\\")\\n+                temp_name(&var out, instruction_index, \\\"a\\\")\\n+                put(&var out, \\\")\\\\n\\\")\\n             }\\n             next_block(&var out, instruction_index)\\n         },\\n@@ -350,13 +538,24 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     var out = StringBuilder()\\n     put(&var out, \\\"; minivm textual LLVM IR\\\\n\\\")\\n     put(&var out, \\\"@.fmt_i64 = private unnamed_addr constant [5 x i8] c\\\\\\\"%ld\\\\\\\\0A\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    put(&var out, \\\"@.fmt_u64 = private unnamed_addr constant [5 x i8] c\\\\\\\"%lu\\\\\\\\0A\\\\\\\\00\\\\\\\"\\\\n\\\")\\n     put(&var out, \\\"@.true = private unnamed_addr constant [5 x i8] c\\\\\\\"true\\\\\\\\00\\\\\\\"\\\\n\\\")\\n     put(&var out, \\\"@.false = private unnamed_addr constant [6 x i8] c\\\\\\\"false\\\\\\\\00\\\\\\\"\\\\n\\\")\\n     put(&var out, \\\"@.overflow = private unnamed_addr constant [32 x i8] c\\\\\\\"runtime error: integer overflow\\\\\\\\00\\\\\\\"\\\\n\\\")\\n     put(&var out, \\\"@.divzero = private unnamed_addr constant [32 x i8] c\\\\\\\"runtime error: division by zero\\\\\\\\00\\\\\\\"\\\\n\\\\n\\\")\\n-    put(&var out, \\\"declare i32 @printf(ptr, ...)\\\\ndeclare i32 @puts(ptr)\\\\ndeclare void @exit(i32)\\\\n\\\\n\\\")\\n+    put(&var out, \\\"@.cast = private unnamed_addr constant [41 x i8] c\\\\\\\"runtime error: integer cast out of range\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    put(&var out, \\\"@.shift = private unnamed_addr constant [34 x i8] c\\\\\\\"runtime error: shift out of range\\\\\\\\00\\\\\\\"\\\\n\\\\n\\\")\\n+    put(&var out, \\\"declare i32 @printf(ptr, ...)\\\\ndeclare i32 @puts(ptr)\\\\ndeclare void @exit(i32)\\\\n\\\")\\n+    put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.sadd.with.overflow.i64(i64, i64)\\\\n\\\")\\n+    put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.ssub.with.overflow.i64(i64, i64)\\\\n\\\")\\n+    put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.smul.with.overflow.i64(i64, i64)\\\\n\\\")\\n+    put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.uadd.with.overflow.i64(i64, i64)\\\\n\\\")\\n+    put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.usub.with.overflow.i64(i64, i64)\\\\n\\\")\\n+    put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.umul.with.overflow.i64(i64, i64)\\\\n\\\\n\\\")\\n     put(&var out, \\\"define internal void @minivm_overflow() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.overflow)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     put(&var out, \\\"define internal void @minivm_divzero() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.divzero)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n+    put(&var out, \\\"define internal void @minivm_cast() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.cast)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n+    put(&var out, \\\"define internal void @minivm_shift() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.shift)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     var function_index = 0\\n     while function_index < program.functions.len() {\\n         emit_function(&var out, &program, function_index,\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex 7e04e437..0ffea967 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -1,15 +1,90 @@\\n-public enum ValueType { case I32, case Bool, case Void }\\n+public enum ValueType {\\n+    case I8, case I16, case I32, case I64,\\n+    case U8, case U16, case U32, case U64, case Byte,\\n+    case Bool, case Void\\n+}\\n @synthesize\\n extension ValueType: Equatable {}\\n \\n public enum Op {\\n     case Const, case Copy, case Add, case Sub, case Mul, case Div, case Rem,\\n     case Neg, case Not, case Eq, case Ne, case Lt, case Le, case Gt, case Ge,\\n-    case Jump, case Branch, case Call, case Print, case Return\\n+    case Jump, case Branch, case Call, case Print, case Return,\\n+    case Cast, case Truncate, case WrapAdd, case WrapSub, case WrapMul,\\n+    case BitAnd, case BitOr, case BitXor, case BitNot, case Shl, case Shr\\n }\\n @synthesize\\n extension Op: Equatable {}\\n \\n+public func integer_type(t: ValueType) -> Bool {\\n+    return integer_width(t) > 0\\n+}\\n+\\n+public func arithmetic_type(t: ValueType) -> Bool {\\n+    return integer_type(t) && t != ValueType.Byte\\n+}\\n+\\n+public func integer_signed(t: ValueType) -> Bool {\\n+    return t == ValueType.I8 || t == ValueType.I16 || t == ValueType.I32 || t == ValueType.I64\\n+}\\n+\\n+public func integer_width(t: ValueType) -> Int {\\n+    if t == ValueType.I8 || t == ValueType.U8 || t == ValueType.Byte { return 8 }\\n+    if t == ValueType.I16 || t == ValueType.U16 { return 16 }\\n+    if t == ValueType.I32 || t == ValueType.U32 { return 32 }\\n+    if t == ValueType.I64 || t == ValueType.U64 { return 64 }\\n+    return 0\\n+}\\n+\\n+public func integer_min(t: ValueType) -> Int {\\n+    if not integer_signed(t) { return 0 }\\n+    if t == ValueType.I64 { return Int.min }\\n+    return 0 - (1 << (integer_width(t) - 1))\\n+}\\n+\\n+public func integer_max(t: ValueType) -> Int {\\n+    if t == ValueType.I64 { return Int.max }\\n+    if t == ValueType.U64 { return -1 }\\n+    let width = integer_width(t)\\n+    if width == 0 { return 0 }\\n+    if integer_signed(t) { return (1 << (width - 1)) - 1 }\\n+    return (1 << width) - 1\\n+}\\n+\\n+public func integer_normalize(value: Int, t: ValueType) -> Int {\\n+    let width = integer_width(t)\\n+    if width == 64 || width == 0 { return value }\\n+    let mask = (1 << width) - 1\\n+    let low = value & mask\\n+    if integer_signed(t) && low >= (1 << (width - 1)) { return low - (1 << width) }\\n+    return low\\n+}\\n+\\n+public func integer_canonical(value: Int, t: ValueType) -> Bool {\\n+    return integer_type(t) && integer_normalize(value, t) == value\\n+}\\n+\\n+public func integer_fits(value: Int, source: ValueType, target: ValueType) -> Bool {\\n+    if not integer_type(source) || not integer_type(target) { return false }\\n+    if integer_signed(source) && value < 0 {\\n+        return integer_signed(target) && value >= integer_min(target)\\n+    }\\n+    let unsigned_value = UInt.from(truncating: value)\\n+    let maximum = UInt.from(truncating: integer_max(target))\\n+    return unsigned_value <= maximum\\n+}\\n+\\n+public func integer_widens(source: ValueType, target: ValueType) -> Bool {\\n+    if not integer_type(source) || not integer_type(target) { return false }\\n+    if source == target { return true }\\n+    if target == ValueType.Byte { return false }\\n+    if source == ValueType.Byte && target == ValueType.U8 { return true }\\n+    if integer_signed(source) == integer_signed(target) {\\n+        return integer_width(target) > integer_width(source)\\n+    }\\n+    return not integer_signed(source) && integer_signed(target) && integer_width(target) > integer_width(source)\\n+}\\n+\\n public struct Instruction {\\n     public op: Op\\n     public dst: Int\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex 84a7587b..cc4b8371 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -17,20 +17,29 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n         case Const -> {\\n             if dst == ValueType.Void { return invalid(&i) }\\n             if dst == ValueType.Bool && i.immediate != 0 && i.immediate != 1 { return invalid(&i) }\\n-            if dst == ValueType.I32 && (i.immediate < -2_147_483_648 || i.immediate > 2_147_483_647) { return invalid(&i) }\\n+            if integer_type(dst) && not integer_canonical(i.immediate, dst) { return invalid(&i) }\\n         },\\n         case Copy -> {\\n             if dst == ValueType.Void || a != dst { return invalid(&i) }\\n         },\\n-        case Add -> {\\n-            if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) }\\n-        },\\n-        case Sub -> { if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n-        case Mul -> { if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n-        case Div -> { if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n-        case Rem -> { if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n+        case Add -> { try check_binary(&i, dst, a, b) },\\n+        case Sub -> { try check_binary(&i, dst, a, b) },\\n+        case Mul -> { try check_binary(&i, dst, a, b) },\\n+        case Div -> { try check_binary(&i, dst, a, b) },\\n+        case Rem -> { try check_binary(&i, dst, a, b) },\\n+        case WrapAdd -> { try check_binary(&i, dst, a, b) },\\n+        case WrapSub -> { try check_binary(&i, dst, a, b) },\\n+        case WrapMul -> { try check_binary(&i, dst, a, b) },\\n+        case BitAnd -> { try check_binary(&i, dst, a, b) },\\n+        case BitOr -> { try check_binary(&i, dst, a, b) },\\n+        case BitXor -> { try check_binary(&i, dst, a, b) },\\n+        case Shl -> { if not arithmetic_type(dst) || a != dst || not arithmetic_type(b) { return invalid(&i) } },\\n+        case Shr -> { if not arithmetic_type(dst) || a != dst || not arithmetic_type(b) { return invalid(&i) } },\\n+        case Cast -> { if not integer_type(dst) || not integer_type(a) { return invalid(&i) } },\\n+        case Truncate -> { if not integer_type(dst) || not integer_type(a) { return invalid(&i) } },\\n+        case BitNot -> { if not arithmetic_type(dst) || a != dst { return invalid(&i) } },\\n         case Neg -> {\\n-            if dst != ValueType.I32 || a != ValueType.I32 { return invalid(&i) }\\n+            if not integer_signed(dst) || a != dst { return invalid(&i) }\\n         },\\n         case Not -> {\\n             if dst != ValueType.Bool || a != ValueType.Bool { return invalid(&i) }\\n@@ -40,11 +49,11 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n         },\\n         case Ne -> { if dst != ValueType.Bool || a == ValueType.Void || a != b { return invalid(&i) } },\\n         case Lt -> {\\n-            if dst != ValueType.Bool || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) }\\n+            if dst != ValueType.Bool || not arithmetic_type(a) || b != a { return invalid(&i) }\\n         },\\n-        case Le -> { if dst != ValueType.Bool || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n-        case Gt -> { if dst != ValueType.Bool || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n-        case Ge -> { if dst != ValueType.Bool || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n+        case Le -> { if dst != ValueType.Bool || not arithmetic_type(a) || b != a { return invalid(&i) } },\\n+        case Gt -> { if dst != ValueType.Bool || not arithmetic_type(a) || b != a { return invalid(&i) } },\\n+        case Ge -> { if dst != ValueType.Bool || not arithmetic_type(a) || b != a { return invalid(&i) } },\\n         case Jump -> {\\n             if i.target < 0 || i.target >= f.code.len() { return invalid(&i) }\\n         },\\n@@ -74,6 +83,11 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n     return\\n }\\n \\n+func check_binary(i: &Instruction, dst: ValueType, a: ValueType, b: ValueType) -> Result<Void, ProtoError> {\\n+    if not arithmetic_type(dst) || a != dst || b != dst { return invalid(&i) }\\n+    return\\n+}\\n+\\n func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n     if f.code.len() == 0 || f.slots.len() < f.params.len() {\\n         return problem(\\\"internal prototype error: invalid function\\\", 0, 0)\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex db2949cd..9e0eb8ef 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -1,89 +1,144 @@\\n import src.model.*\\n \\n-static I32_MIN: Int = -2_147_483_648\\n-static I32_MAX: Int = 2_147_483_647\\n static MAX_CALL_DEPTH: Int = 128\\n+struct CallResult { value: Int, has_value: Bool }\\n+func runtime_error(message: String) -> ProtoError { problem(message, 0, 0) }\\n+func overflow_error() -> ProtoError { runtime_error(\\\"runtime error: integer overflow\\\") }\\n \\n-struct CallResult {\\n-    value: Int\\n-    has_value: Bool\\n+func checked_binary(a: Int, b: Int, t: ValueType, op: Op) -> Result<Int, ProtoError> {\\n+    if integer_signed(t) {\\n+        let lo = integer_min(t)\\n+        let hi = integer_max(t)\\n+        if op == Op.Add {\\n+            if (b > 0 && a > hi - b) || (b < 0 && a < lo - b) { return overflow_error() }\\n+            return a + b\\n+        }\\n+        if op == Op.Sub {\\n+            if (b < 0 && a > hi + b) || (b > 0 && a < lo + b) { return overflow_error() }\\n+            return a - b\\n+        }\\n+        if a == 0 || b == 0 { return 0 }\\n+        if a > 0 {\\n+            if b > 0 { if a > hi / b { return overflow_error() } }\\n+            else { if b < lo / a { return overflow_error() } }\\n+        } else {\\n+            if b > 0 { if a < lo / b { return overflow_error() } }\\n+            else { if a < hi / b { return overflow_error() } }\\n+        }\\n+        return a * b\\n+    }\\n+    let ua = UInt.from(truncating: a)\\n+    let ub = UInt.from(truncating: b)\\n+    let hi = UInt.from(truncating: integer_max(t))\\n+    var result: UInt = 0\\n+    if op == Op.Add {\\n+        if ub > hi || ua > hi - ub { return overflow_error() }\\n+        result = ua + ub\\n+    } else if op == Op.Sub {\\n+        if ua < ub { return overflow_error() }\\n+        result = ua - ub\\n+    } else {\\n+        if ua != 0 && ub > hi / ua { return overflow_error() }\\n+        result = ua * ub\\n+    }\\n+    Int.from(truncating: result)\\n }\\n \\n-func runtime_error(message: String) -> ProtoError {\\n-    problem(message, 0, 0)\\n+func wrapping_binary(a: Int, b: Int, t: ValueType, op: Op) -> Int {\\n+    let ua = UInt.from(truncating: a)\\n+    let ub = UInt.from(truncating: b)\\n+    var result = ua\\n+    if op == Op.WrapAdd { result = ua &+ ub }\\n+    else if op == Op.WrapSub { result = ua &- ub }\\n+    else { result = ua &* ub }\\n+    integer_normalize(Int.from(truncating: result), t)\\n }\\n \\n-func checked_i32(value: Int) -> Result<Int, ProtoError> {\\n-    if value < I32_MIN || value > I32_MAX {\\n-        return runtime_error(\\\"runtime error: integer overflow\\\")\\n+func divide(a: Int, b: Int, t: ValueType, remainder: Bool) -> Result<Int, ProtoError> {\\n+    if b == 0 { return runtime_error(\\\"runtime error: division by zero\\\") }\\n+    if integer_signed(t) {\\n+        if a == integer_min(t) && b == -1 { return overflow_error() }\\n+        return if remainder { a % b } else { a / b }\\n     }\\n-    value\\n+    let ua = UInt.from(truncating: a)\\n+    let ub = UInt.from(truncating: b)\\n+    Int.from(truncating: if remainder { ua % ub } else { ua / ub })\\n }\\n \\n-func run_function(program: &Program, function_index: Int, arguments: &Vector<Int>,\\n-                  remaining: &var Int, depth: Int) -> Result<CallResult, ProtoError> {\\n-    if depth > MAX_CALL_DEPTH {\\n-        return runtime_error(\\\"runtime error: call depth exceeded\\\")\\n+func shift(a: Int, c: Int, ct: ValueType, t: ValueType, left: Bool) -> Result<Int, ProtoError> {\\n+    if (integer_signed(ct) && c < 0) || UInt.from(truncating: c) >= UInt.from(truncating: integer_width(t)) { return runtime_error(\\\"runtime error: shift out of range\\\") }\\n+    let n = Int.from(truncating: UInt.from(truncating: c))\\n+    if left { return integer_normalize(Int.from(truncating: UInt.from(truncating: a) << n), t) }\\n+    if integer_signed(t) { return integer_normalize(a >> n, t) }\\n+    integer_normalize(Int.from(truncating: UInt.from(truncating: a) >> n), t)\\n+}\\n+\\n+func compare(a: Int, b: Int, t: ValueType, op: Op) -> Int {\\n+    var yes = false\\n+    if integer_signed(t) {\\n+        if op == Op.Lt { yes = a < b } else if op == Op.Le { yes = a <= b } else if op == Op.Gt { yes = a > b } else { yes = a >= b }\\n+    } else {\\n+        let ua = UInt.from(truncating: a)\\n+        let ub = UInt.from(truncating: b)\\n+        if op == Op.Lt { yes = ua < ub } else if op == Op.Le { yes = ua <= ub } else if op == Op.Gt { yes = ua > ub } else { yes = ua >= ub }\\n     }\\n+    if yes { 1 } else { 0 }\\n+}\\n \\n+func run_function(program: &Program, function_index: Int, arguments: &Vector<Int>, remaining: &var Int, depth: Int) -> Result<CallResult, ProtoError> {\\n+    if depth > MAX_CALL_DEPTH { return runtime_error(\\\"runtime error: call depth exceeded\\\") }\\n     let function = program.functions[function_index].copy()\\n     var slots = Vector<Int>()\\n-    var slot_index = 0\\n-    while slot_index < function.slots.len() {\\n+    var si = 0\\n+    while si < function.slots.len() {\\n         try! slots.push(0)\\n-        slot_index = slot_index + 1\\n+        si = si + 1\\n     }\\n-    var argument_index = 0\\n-    while argument_index < arguments.len() {\\n-        slots[argument_index] = arguments[argument_index]\\n-        argument_index = argument_index + 1\\n+    var ai = 0\\n+    while ai < arguments.len() {\\n+        slots[ai] = arguments[ai]\\n+        ai = ai + 1\\n     }\\n-\\n     var pc = 0\\n     while pc < function.code.len() {\\n-        if remaining <= 0 {\\n-            return runtime_error(\\\"runtime error: instruction budget exceeded\\\")\\n-        }\\n+        if remaining <= 0 { return runtime_error(\\\"runtime error: instruction budget exceeded\\\") }\\n         remaining = remaining - 1\\n-\\n         let inst = function.code[pc].copy()\\n+        let t = if inst.dst >= 0 { function.slots[inst.dst] } else { ValueType.Void }\\n         match inst.op {\\n             case Const -> { slots[inst.dst] = inst.immediate },\\n             case Copy -> { slots[inst.dst] = slots[inst.a] },\\n-            case Add -> { slots[inst.dst] = try checked_i32(slots[inst.a] + slots[inst.b]) },\\n-            case Sub -> { slots[inst.dst] = try checked_i32(slots[inst.a] - slots[inst.b]) },\\n-            case Mul -> { slots[inst.dst] = try checked_i32(slots[inst.a] * slots[inst.b]) },\\n-            case Div -> {\\n-                let lhs = slots[inst.a]\\n-                let rhs = slots[inst.b]\\n-                if rhs == 0 {\\n-                    return runtime_error(\\\"runtime error: division by zero\\\")\\n-                }\\n-                if lhs == I32_MIN && rhs == -1 {\\n-                    return runtime_error(\\\"runtime error: integer overflow\\\")\\n-                }\\n-                slots[inst.dst] = lhs / rhs\\n+            case Add -> { slots[inst.dst] = try checked_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n+            case Sub -> { slots[inst.dst] = try checked_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n+            case Mul -> { slots[inst.dst] = try checked_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n+            case WrapAdd -> { slots[inst.dst] = wrapping_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n+            case WrapSub -> { slots[inst.dst] = wrapping_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n+            case WrapMul -> { slots[inst.dst] = wrapping_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n+            case Div -> { slots[inst.dst] = try divide(slots[inst.a], slots[inst.b], t, false) },\\n+            case Rem -> { slots[inst.dst] = try divide(slots[inst.a], slots[inst.b], t, true) },\\n+            case Neg -> {\\n+                let a = slots[inst.a]\\n+                if a == integer_min(t) { return overflow_error() }\\n+                slots[inst.dst] = 0 - a\\n             },\\n-            case Rem -> {\\n-                let lhs = slots[inst.a]\\n-                let rhs = slots[inst.b]\\n-                if rhs == 0 {\\n-                    return runtime_error(\\\"runtime error: division by zero\\\")\\n-                }\\n-                if lhs == I32_MIN && rhs == -1 {\\n-                    slots[inst.dst] = 0\\n-                } else {\\n-                    slots[inst.dst] = lhs % rhs\\n-                }\\n+            case Cast -> {\\n+                if not integer_fits(slots[inst.a], function.slots[inst.a], t) { return runtime_error(\\\"runtime error: integer cast out of range\\\") }\\n+                slots[inst.dst] = integer_normalize(slots[inst.a], t)\\n             },\\n-            case Neg -> { slots[inst.dst] = try checked_i32(0 - slots[inst.a]) },\\n+            case Truncate -> { slots[inst.dst] = integer_normalize(slots[inst.a], t) },\\n+            case BitAnd -> { slots[inst.dst] = integer_normalize(slots[inst.a] & slots[inst.b], t) },\\n+            case BitOr -> { slots[inst.dst] = integer_normalize(slots[inst.a] | slots[inst.b], t) },\\n+            case BitXor -> { slots[inst.dst] = integer_normalize(slots[inst.a] ^ slots[inst.b], t) },\\n+            case BitNot -> { slots[inst.dst] = integer_normalize(~slots[inst.a], t) },\\n+            case Shl -> { slots[inst.dst] = try shift(slots[inst.a], slots[inst.b], function.slots[inst.b], t, true) },\\n+            case Shr -> { slots[inst.dst] = try shift(slots[inst.a], slots[inst.b], function.slots[inst.b], t, false) },\\n             case Not -> { slots[inst.dst] = if slots[inst.a] == 0 { 1 } else { 0 } },\\n             case Eq -> { slots[inst.dst] = if slots[inst.a] == slots[inst.b] { 1 } else { 0 } },\\n             case Ne -> { slots[inst.dst] = if slots[inst.a] != slots[inst.b] { 1 } else { 0 } },\\n-            case Lt -> { slots[inst.dst] = if slots[inst.a] < slots[inst.b] { 1 } else { 0 } },\\n-            case Le -> { slots[inst.dst] = if slots[inst.a] <= slots[inst.b] { 1 } else { 0 } },\\n-            case Gt -> { slots[inst.dst] = if slots[inst.a] > slots[inst.b] { 1 } else { 0 } },\\n-            case Ge -> { slots[inst.dst] = if slots[inst.a] >= slots[inst.b] { 1 } else { 0 } },\\n+            case Lt -> { slots[inst.dst] = compare(slots[inst.a], slots[inst.b], function.slots[inst.a], inst.op) },\\n+            case Le -> { slots[inst.dst] = compare(slots[inst.a], slots[inst.b], function.slots[inst.a], inst.op) },\\n+            case Gt -> { slots[inst.dst] = compare(slots[inst.a], slots[inst.b], function.slots[inst.a], inst.op) },\\n+            case Ge -> { slots[inst.dst] = compare(slots[inst.a], slots[inst.b], function.slots[inst.a], inst.op) },\\n             case Jump -> {\\n                 pc = inst.target\\n                 continue\\n@@ -95,29 +150,22 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 }\\n             },\\n             case Call -> {\\n-                var call_arguments = Vector<Int>()\\n+                var args = Vector<Int>()\\n                 var i = 0\\n                 while i < inst.args.len() {\\n-                    try! call_arguments.push(slots[inst.args[i]])\\n+                    try! args.push(slots[inst.args[i]])\\n                     i = i + 1\\n                 }\\n-                let result = try run_function(&program, inst.target, &call_arguments,\\n-                                              &var remaining, depth + 1)\\n-                if inst.dst >= 0 {\\n-                    slots[inst.dst] = result.value\\n-                }\\n+                let result = try run_function(&program, inst.target, &args, &var remaining, depth + 1)\\n+                if inst.dst >= 0 { slots[inst.dst] = result.value }\\n             },\\n             case Print -> {\\n-                if function.slots[inst.a] == ValueType.Bool {\\n-                    if slots[inst.a] == 0 { print(\\\"false\\\") } else { print(\\\"true\\\") }\\n-                } else {\\n-                    print(slots[inst.a])\\n-                }\\n+                let pt = function.slots[inst.a]\\n+                if pt == ValueType.Bool { if slots[inst.a] == 0 { print(\\\"false\\\") } else { print(\\\"true\\\") } }\\n+                else if integer_signed(pt) { print(slots[inst.a]) } else { print(UInt.from(truncating: slots[inst.a])) }\\n             },\\n             case Return -> {\\n-                if inst.a < 0 {\\n-                    return CallResult(value: 0, has_value: false)\\n-                }\\n+                if inst.a < 0 { return CallResult(value: 0, has_value: false) }\\n                 return CallResult(value: slots[inst.a], has_value: true)\\n             }\\n         }\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex d99b21c0..26c8bffc 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -24,15 +24,26 @@ class RunCase:\\n     name: str\\n     stdout: str\\n     exit_code: int = 0\\n+    section: str = \\\"core\\\"\\n \\n \\n @dataclass(frozen=True)\\n class RejectCase:\\n     name: str\\n     message: str\\n+    section: str = \\\"core\\\"\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"numbers/unary_cast_order\\\", \\\"-127\\\\n18446744073709551615\\\\n-1\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/unary_cast_range\\\", \\\"runtime error: integer cast out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/unary_cast_widen\\\", \\\"128\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/complement_after_cast\\\", \\\"0\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/review_literals\\\", \\\"3\\\\n1\\\\ntrue\\\\n-1\\\\n18446744073709551615\\\\n18446744073709551615\\\\ntrue\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/review_unsigned\\\", \\\"6148914691236517205\\\\n1\\\\n1\\\\n18446744073709551615\\\\n254\\\\n-128\\\\n-9223372036854775808\\\\n-9223372036854775808\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_u64_mul\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_u64_sub\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/shift_large_unsigned\\\", \\\"runtime error: shift out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n     RunCase(\\\"arithmetic\\\", \\\"12\\\\n-3\\\\n42\\\\n-2\\\\n-1\\\\n0\\\\n\\\"),\\n     RunCase(\\\"comparisons\\\", \\\"true\\\\nfalse\\\\ntrue\\\\ntrue\\\\ntrue\\\\nfalse\\\\ntrue\\\\ntrue\\\\n\\\"),\\n     RunCase(\\\"precedence\\\", \\\"14\\\\n20\\\\n-1\\\\ntrue\\\\n\\\"),\\n@@ -59,10 +70,42 @@ RUN_CASES = (\\n     RunCase(\\\"overflow_div\\\", \\\"runtime error: integer overflow\\\\n\\\", 1),\\n     RunCase(\\\"div_zero\\\", \\\"runtime error: division by zero\\\\n\\\", 1),\\n     RunCase(\\\"rem_zero\\\", \\\"runtime error: division by zero\\\\n\\\", 1),\\n+    RunCase(\\\"numbers/types_and_bounds\\\", \\\"-128\\\\n127\\\\n-32768\\\\n32767\\\\n-2147483648\\\\n2147483647\\\\n-9223372036854775808\\\\n9223372036854775807\\\\n-9223372036854775808\\\\n9223372036854775807\\\\n0\\\\n255\\\\n0\\\\n65535\\\\n0\\\\n4294967295\\\\n0\\\\n18446744073709551615\\\\n0\\\\n18446744073709551615\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/literals_and_aliases\\\", \\\"-128\\\\n32767\\\\n-2147483648\\\\n9223372036854775807\\\\n-9223372036854775808\\\\n255\\\\n65535\\\\n4294967295\\\\n18446744073709551615\\\\n9223372036854775808\\\\n255\\\\n65535\\\\n4294967295\\\\n18446744073709551615\\\\n42\\\\n511\\\\n127\\\\n32767\\\\n2147483647\\\\n9223372036854775807\\\\n-128\\\\n2147483648\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/u64_calls\\\", \\\"18446744073709551615\\\\n18446744073709551615\\\\ntrue\\\\ntrue\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/contextual\\\", \\\"-128\\\\n65535\\\\n15\\\\n15\\\\n4294967295\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/lossless_widening\\\", \\\"-12\\\\n-12\\\\n-12\\\\n255\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/casts\\\", \\\"-1\\\\n255\\\\n255\\\\n2147483648\\\\n127\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/truncation\\\", \\\"255\\\\n-1\\\\n1\\\\n-1\\\\n4294967295\\\\n-1\\\\n18446744073709551615\\\\n-1\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/byte\\\", \\\"255\\\\n255\\\\ntrue\\\\n255\\\\n255\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/wrapping\\\", \\\"-128\\\\n127\\\\n-2\\\\n0\\\\n255\\\\n0\\\\n0\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/bits_and_shifts\\\", \\\"192\\\\n243\\\\n90\\\\n15\\\\n-4\\\\n60\\\\n128\\\\n240\\\\n24\\\\n3\\\\n4\\\\n\\\", section=\\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_i64_add\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_i64_sub\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_i64_mul\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_i8_add\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_i16_sub\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_i8_mul\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_u64_add\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_u16_sub\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_u32_mul\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_neg_i64\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_neg_i8\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_div_i64\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_rem_i64\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/overflow_rem_i32\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/cast_negative_unsigned\\\", \\\"runtime error: integer cast out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/cast_too_large\\\", \\\"runtime error: integer cast out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/cast_unsigned_signed\\\", \\\"runtime error: integer cast out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/shift_negative\\\", \\\"runtime error: shift out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"numbers/shift_width\\\", \\\"runtime error: shift out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n )\\n \\n REJECT_CASES = (\\n-    RejectCase(\\\"reject_type\\\", \\\"type mismatch\\\"),\\n+    RejectCase(\\\"numbers/reject_byte_literal\\\", \\\"Byte\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"numbers/reject_uncontextual_u64\\\", \\\"integer literal\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"numbers/reject_spaced_shift\\\", \\\"expression\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"reject_type\\\", \\\"integer literal does not fit Bool\\\"),\\n     RejectCase(\\\"reject_mutability\\\", \\\"cannot assign to immutable local\\\"),\\n     RejectCase(\\\"reject_arity\\\", \\\"wrong number of arguments\\\"),\\n     RejectCase(\\\"reject_unknown\\\", \\\"unknown name\\\"),\\n@@ -70,13 +113,20 @@ REJECT_CASES = (\\n     RejectCase(\\\"reject_missing_return\\\", \\\"may reach its end without returning\\\"),\\n     RejectCase(\\\"reject_semicolon\\\", \\\"semicolons are not supported\\\"),\\n     RejectCase(\\\"reject_string\\\", \\\"expected expression\\\"),\\n-    RejectCase(\\\"reject_bool_arithmetic\\\", \\\"type mismatch\\\"),\\n+    RejectCase(\\\"reject_bool_arithmetic\\\", \\\"arithmetic requires integer operands\\\"),\\n     RejectCase(\\\"reject_import\\\", \\\"only function declarations are supported\\\"),\\n     RejectCase(\\\"reject_scope_leak\\\", \\\"unknown name\\\"),\\n     RejectCase(\\\"reject_unary_depth\\\", \\\"expression nesting limit exceeded\\\"),\\n     RejectCase(\\\"reject_reserved_print\\\", \\\"reserved\\\"),\\n     RejectCase(\\\"reject_truncated\\\", \\\"unterminated function body\\\"),\\n     RejectCase(\\\"reject_large_literal\\\", \\\"integer literal\\\"),\\n+    RejectCase(\\\"numbers/reject_typed_narrowing\\\", \\\"convert\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"numbers/reject_typed_narrowing_assignment\\\", \\\"convert\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"numbers/reject_typed_narrowing_argument\\\", \\\"convert\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"numbers/reject_typed_narrowing_return\\\", \\\"convert\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"numbers/reject_mixed_binary\\\", \\\"same type\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"numbers/reject_bool_cast\\\", \\\"requires integer source and target\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"numbers/reject_literal_range\\\", \\\"range\\\", \\\"numbers\\\"),\\n )\\n \\n \\n@@ -104,13 +154,17 @@ def test_shared_case(binary: Path, clang: str, case: RunCase, temp: Path) -> lis\\n \\n     emitted = invoke([str(binary), \\\"emit-llvm\\\", str(source)])\\n     if emitted.returncode != 0 or emitted.stderr:\\n-        failures.extend(check_result(f\\\"{case.name} emit\\\", emitted, emitted.stdout, 0))\\n+        failures.append(\\n+            f\\\"{case.name} emit: exit {emitted.returncode}\\\\n\\\"\\n+            f\\\"stdout: {emitted.stdout!r}\\\\nstderr: {emitted.stderr!r}\\\"\\n+        )\\n         return failures\\n-    ll_path = temp / f\\\"{case.name}.ll\\\"\\n+    artifact_name = case.name.replace(\\\"/\\\", \\\"-\\\")\\n+    ll_path = temp / f\\\"{artifact_name}.ll\\\"\\n     ll_path.write_text(emitted.stdout, encoding=\\\"utf-8\\\")\\n     for optimization in (\\\"-O0\\\", \\\"-O2\\\"):\\n         suffix = optimization.removeprefix(\\\"-\\\")\\n-        native_path = temp / f\\\"{case.name}-{suffix}\\\"\\n+        native_path = temp / f\\\"{artifact_name}-{suffix}\\\"\\n         compiled = invoke([clang, optimization, str(ll_path), \\\"-o\\\", str(native_path)])\\n         if compiled.returncode != 0:\\n             failures.append(\\n@@ -156,6 +210,10 @@ def main() -> int:\\n     parser = argparse.ArgumentParser()\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n+    parser.add_argument(\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\"), default=\\\"all\\\",\\n+        help=\\\"run all cases or one isolated test section\\\",\\n+    )\\n     args = parser.parse_args()\\n     binary = args.binary.resolve()\\n     if not binary.is_file():\\n@@ -164,20 +222,29 @@ def main() -> int:\\n     failures: list[str] = []\\n     with tempfile.TemporaryDirectory(prefix=\\\"minivm-test-\\\") as directory:\\n         temp = Path(directory)\\n-        for case in RUN_CASES:\\n+        run_cases = tuple(\\n+            case for case in RUN_CASES\\n+            if args.section == \\\"all\\\" or case.section == args.section\\n+        )\\n+        reject_cases = tuple(\\n+            case for case in REJECT_CASES\\n+            if args.section == \\\"all\\\" or case.section == args.section\\n+        )\\n+        for case in run_cases:\\n             try:\\n                 failures.extend(test_shared_case(binary, args.clang, case, temp))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        for case in REJECT_CASES:\\n+        for case in reject_cases:\\n             try:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        for name, budget, message in (\\n+        limit_cases = () if args.section == \\\"numbers\\\" else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n-        ):\\n+        )\\n+        for name, budget, message in limit_cases:\\n             try:\\n                 failures.extend(test_vm_limit(binary, name, budget, message))\\n             except subprocess.TimeoutExpired:\\n@@ -188,7 +255,7 @@ def main() -> int:\\n             print(f\\\"FAIL: {failure}\\\")\\n         print(f\\\"minivm: {len(failures)} failure(s)\\\")\\n         return 1\\n-    total = len(RUN_CASES) + len(REJECT_CASES) + 2\\n+    total = len(run_cases) + len(reject_cases) + len(limit_cases)\\n     print(f\\\"minivm: {total} cases passed\\\")\\n     return 0\\n \\ndiff --git a/prototypes/minivm/tests/numeric_contract.saw b/prototypes/minivm/tests/numeric_contract.saw\\nnew file mode 100644\\nindex 00000000..41b89f4e\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/numeric_contract.saw\\n@@ -0,0 +1,29 @@\\n+import src.model.*\\n+\\n+// Representation tests are independent of source parsing and both engines.\\n+func main() {\\n+    assert(integer_width(ValueType.U64) == 64, \\\"numeric contract\\\")\\n+    assert(integer_width(ValueType.Byte) == 8, \\\"numeric contract\\\")\\n+    assert(integer_min(ValueType.I8) == -128, \\\"numeric contract\\\")\\n+    assert(integer_max(ValueType.U32) == 4_294_967_295, \\\"numeric contract\\\")\\n+    assert(integer_max(ValueType.U64) == -1, \\\"numeric contract\\\")\\n+    assert(integer_normalize(255, ValueType.I8) == -1, \\\"numeric contract\\\")\\n+    assert(integer_normalize(-1, ValueType.U8) == 255, \\\"numeric contract\\\")\\n+    assert(integer_normalize(65_535, ValueType.I16) == -1, \\\"numeric contract\\\")\\n+    assert(integer_normalize(-1, ValueType.U32) == 4_294_967_295, \\\"numeric contract\\\")\\n+    assert(integer_canonical(-1, ValueType.U64), \\\"numeric contract\\\")\\n+    assert(not integer_canonical(-1, ValueType.U32), \\\"numeric contract\\\")\\n+    assert(integer_fits(-1, ValueType.U64, ValueType.U64), \\\"numeric contract\\\")\\n+    assert(not integer_fits(-1, ValueType.U64, ValueType.I64), \\\"numeric contract\\\")\\n+    assert(not integer_fits(-1, ValueType.I64, ValueType.U64), \\\"numeric contract\\\")\\n+    assert(integer_fits(255, ValueType.U64, ValueType.U8), \\\"numeric contract\\\")\\n+    assert(not integer_fits(256, ValueType.U64, ValueType.U8), \\\"numeric contract\\\")\\n+    assert(integer_fits(-128, ValueType.I64, ValueType.I8), \\\"numeric contract\\\")\\n+    assert(not integer_fits(-129, ValueType.I64, ValueType.I8), \\\"numeric contract\\\")\\n+    assert(integer_widens(ValueType.U32, ValueType.I64), \\\"numeric contract\\\")\\n+    assert(not integer_widens(ValueType.U64, ValueType.I64), \\\"numeric contract\\\")\\n+    assert(not integer_widens(ValueType.I32, ValueType.U64), \\\"numeric contract\\\")\\n+    assert(integer_widens(ValueType.Byte, ValueType.U8), \\\"numeric contract\\\")\\n+    assert(not integer_widens(ValueType.U8, ValueType.Byte), \\\"numeric contract\\\")\\n+    print(\\\"numeric contract passed\\\")\\n+}\\n\",\"id\":\"SL-244\",\"request_id\":\"pid-91399-1789087543-5000\",\"title\":\"minivm M1: lexer numeric foundation (branch commit f97585f9)\"}","request_id":"pid-91399-1789087543-5000","sequence":429,"timestamp":"1789087544"}
---
