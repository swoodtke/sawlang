---
{"action":"create","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790262864","description":"Found by the SL-340 implementer (Sep 24). The cell is `c14`, in `.claude/worktrees/sl340/.build/scratch/`.\n\nA placement write of a by-value parameter through a pointer drops the value TWICE when the pointer is not a plain identifier:\n- `(p + 0)[0] = value`, in a generic body, prints `drop placed-expr` twice. That is a double free for a heap-owning `T`.\n- The `p[0] = value` form drops once, which is correct.\n\nMechanism (obligation 4, named by the implementer; verify in code): the placement bookkeeping in `codegen/statements.py` `_generate_assignment` clears the source binding's drop flag only when `stmt.target.array_expr` is an `Identifier`. Every other pointer spelling leaves the flag set, so the source is released at scope exit after its bytes were placed.\n\nPositions to sweep, one row each, with a printing deinit counting exactly one drop:\n- a method-call pointer: `self.cell.ptr()[0] = value`, which is exactly `Once.set`'s shape. `Once` itself is unreachable with an owning `T` today, because it is static-only and statics refuse Deinit payloads. A user cell-carrying type is reachable.\n- pointer arithmetic: `(p + i)[0]`;\n- a field pointer: `self.buf[0]`;\n- an indexed pointer inside a struct;\n- the non-generic twin of each;\n- a moved local as the source, as well as a parameter.\n\nThe fix belongs in the one place that decides whether a placement write consumed its source, keyed on the SOURCE rather than on the target's spelling. Unsafe-domain code, but correct-looking code double-frees.\n","id":"SL-378","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":1,"sequence":1448,"stage":"backlog","status":"open","title":"A placement write through a non-identifier pointer drops its by-value source twice: (p + 0)[0] = value, self.cell.ptr()[0] = value (p[0] = value is correct)","updated":"1790262864"},"before":null,"id":"SL-378","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"description\":\"Found by the SL-340 implementer (Sep 24). The cell is `c14`, in `.claude/worktrees/sl340/.build/scratch/`.\\n\\nA placement write of a by-value parameter through a pointer drops the value TWICE when the pointer is not a plain identifier:\\n- `(p + 0)[0] = value`, in a generic body, prints `drop placed-expr` twice. That is a double free for a heap-owning `T`.\\n- The `p[0] = value` form drops once, which is correct.\\n\\nMechanism (obligation 4, named by the implementer; verify in code): the placement bookkeeping in `codegen/statements.py` `_generate_assignment` clears the source binding's drop flag only when `stmt.target.array_expr` is an `Identifier`. Every other pointer spelling leaves the flag set, so the source is released at scope exit after its bytes were placed.\\n\\nPositions to sweep, one row each, with a printing deinit counting exactly one drop:\\n- a method-call pointer: `self.cell.ptr()[0] = value`, which is exactly `Once.set`'s shape. `Once` itself is unreachable with an owning `T` today, because it is static-only and statics refuse Deinit payloads. A user cell-carrying type is reachable.\\n- pointer arithmetic: `(p + i)[0]`;\\n- a field pointer: `self.buf[0]`;\\n- an indexed pointer inside a struct;\\n- the non-generic twin of each;\\n- a moved local as the source, as well as a parameter.\\n\\nThe fix belongs in the one place that decides whether a placement write consumed its source, keyed on the SOURCE rather than on the target's spelling. Unsafe-domain code, but correct-looking code double-frees.\\n\",\"label\":\"bug\",\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"pid-86475-1790262864-10000\",\"title\":\"A placement write through a non-identifier pointer drops its by-value source twice: (p + 0)[0] = value, self.cell.ptr()[0] = value (p[0] = value is correct)\"}","request_id":"pid-86475-1790262864-10000","sequence":1448,"timestamp":"1790262864"}
---
