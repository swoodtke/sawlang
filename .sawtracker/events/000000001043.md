---
{"action":"create","actor":"agent:claude-sawlang","after":{"assignee":"","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Found by the lead's adversarial validation of design 275 U1 (SL-318.p4), Sep 19. PRE-EXISTING: identical on main 8d752a78 and on the U1 branch (U1 is behaviour-preserving), so it is not U1's regression — it is a discovery seam of exactly the class the epic exists to end, and it belongs on U2's worklist.\n\nA MODULE-QUALIFIED call to an imported suspending GENERIC free function, sitting inside a driven body, is never framed: the driven frame's resume plain-calls the instantiation and the post-transform `sync` check then refuses the program.\n\n```saw\n// modules/alpha/lib.saw\nimport std.task.{yield_now}\npublic func outer<T>(x: Int) -> Int {\n    let r = inner<T>(x)\n    r + 1\n}\nfunc inner<T>(x: Int) -> Int {\n    let r = leaf(x)\n    r + 10\n}\nfunc leaf(x: Int) -> Int { yield_now()  x + 100 }\n\n// probe4.saw\nimport alpha\nfunc chain(n: Int) -> Int {\n    let r = alpha.outer<Int>(n)      // QUALIFIED + GENERIC + embedded\n    r\n}\nfunc main() { print(\"chain {} {}\", __saw_drive(chain(1)), __saw_drive_steps(chain(1))) }\n```\n\n```\nerror: cannot suspend in `sync func` method: method `__Frame_chain.resume` calls\n`outer$1$Int` → `inner` → `leaf` → yield_now (`leaf` suspends at line 27)\n```\n\nTHE MATRIX that isolates the cell (all compiled on both compilers, byte-identical):\n- `import alpha.{outer}` + bare `outer<Int>(n)` — WORKS: `chain 112 1`, three frames.\n- qualified NON-generic (`inner.worker(n)`, `s.t.hop(n)`, K116) — WORKS.\n- qualified GENERIC, `let`-bound or tail, statement position — REFUSED as above.\nSo the cell is qualified × generic × embedded. Not silent (a refusal), and the refusal is the post-lowering totality check design 275 U2 makes mechanical — but it is the wrong diagnostic: the program is sound, and the message blames the author's `sync` context for a frame the transform declined to build.\n\nMECHANISM (named per obligation 4, from the diagnostic + the K116 evidence, not yet stepped in the debugger): `alpha.outer<Int>(n)` parses as a `MethodCall` carrying the typechecker's `module_free_call` stamp (SL-208). The generic PROMOTION (`_promote_nested_generic_calls`, now `FrameLedger.instantiation_is_boundary` + `rename_call_to`) walks `FunctionCall` nodes for generic instantiations to key and frame; a qualified generic call is not a `FunctionCall`, so its instantiation is never promoted, no frame row exists for `outer$1$Int` in the embedded position, and the call is lowered plain. SL-217 is the ROOT sibling of the same parser shape (a qualified suspending call cannot be a drive/spawn root); this is the EMBEDDED face, and it is the generic case only because SL-208 taught the non-generic classifiers about the stamp and the promotion was not in that sweep. In U1's terms: no consumer ever ASKED the ledger about this site, so the miss-is-an-ICE invariant never fires — which is precisely U2's coverage check (a): every reachable suspension site has a `SiteDecision`. Once U2 lands, this site must get one, and the shape table's answer is EMBED.\n\nWorkaround today: the selective import (`import alpha.{outer}`) and the bare spelling.\n","created":"1789845297","id":"c1"}],"created":"1789845297","description":"","id":"SL-330","labels":[],"priority":"normal","project":"SL","revision":1,"sequence":1043,"stage":"backlog","status":"open","title":"a module-qualified call to an imported suspending GENERIC free function is never framed in a driven body — the resume plain-calls the instantiation and the sync check refuses a sound program (qualified × generic × embedded; SL-217's embedded sibling)","updated":"1789845297"},"before":null,"id":"SL-330","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Found by the lead's adversarial validation of design 275 U1 (SL-318.p4), Sep 19. PRE-EXISTING: identical on main 8d752a78 and on the U1 branch (U1 is behaviour-preserving), so it is not U1's regression — it is a discovery seam of exactly the class the epic exists to end, and it belongs on U2's worklist.\\n\\nA MODULE-QUALIFIED call to an imported suspending GENERIC free function, sitting inside a driven body, is never framed: the driven frame's resume plain-calls the instantiation and the post-transform `sync` check then refuses the program.\\n\\n```saw\\n// modules/alpha/lib.saw\\nimport std.task.{yield_now}\\npublic func outer<T>(x: Int) -> Int {\\n    let r = inner<T>(x)\\n    r + 1\\n}\\nfunc inner<T>(x: Int) -> Int {\\n    let r = leaf(x)\\n    r + 10\\n}\\nfunc leaf(x: Int) -> Int { yield_now()  x + 100 }\\n\\n// probe4.saw\\nimport alpha\\nfunc chain(n: Int) -> Int {\\n    let r = alpha.outer<Int>(n)      // QUALIFIED + GENERIC + embedded\\n    r\\n}\\nfunc main() { print(\\\"chain {} {}\\\", __saw_drive(chain(1)), __saw_drive_steps(chain(1))) }\\n```\\n\\n```\\nerror: cannot suspend in `sync func` method: method `__Frame_chain.resume` calls\\n`outer$1$Int` → `inner` → `leaf` → yield_now (`leaf` suspends at line 27)\\n```\\n\\nTHE MATRIX that isolates the cell (all compiled on both compilers, byte-identical):\\n- `import alpha.{outer}` + bare `outer<Int>(n)` — WORKS: `chain 112 1`, three frames.\\n- qualified NON-generic (`inner.worker(n)`, `s.t.hop(n)`, K116) — WORKS.\\n- qualified GENERIC, `let`-bound or tail, statement position — REFUSED as above.\\nSo the cell is qualified × generic × embedded. Not silent (a refusal), and the refusal is the post-lowering totality check design 275 U2 makes mechanical — but it is the wrong diagnostic: the program is sound, and the message blames the author's `sync` context for a frame the transform declined to build.\\n\\nMECHANISM (named per obligation 4, from the diagnostic + the K116 evidence, not yet stepped in the debugger): `alpha.outer<Int>(n)` parses as a `MethodCall` carrying the typechecker's `module_free_call` stamp (SL-208). The generic PROMOTION (`_promote_nested_generic_calls`, now `FrameLedger.instantiation_is_boundary` + `rename_call_to`) walks `FunctionCall` nodes for generic instantiations to key and frame; a qualified generic call is not a `FunctionCall`, so its instantiation is never promoted, no frame row exists for `outer$1$Int` in the embedded position, and the call is lowered plain. SL-217 is the ROOT sibling of the same parser shape (a qualified suspending call cannot be a drive/spawn root); this is the EMBEDDED face, and it is the generic case only because SL-208 taught the non-generic classifiers about the stamp and the promotion was not in that sweep. In U1's terms: no consumer ever ASKED the ledger about this site, so the miss-is-an-ICE invariant never fires — which is precisely U2's coverage check (a): every reachable suspension site has a `SiteDecision`. Once U2 lands, this site must get one, and the shape table's answer is EMBED.\\n\\nWorkaround today: the selective import (`import alpha.{outer}`) and the bare spelling.\\n\",\"label\":\"bug\",\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"pid-81294-1789845297-6000\",\"title\":\"a module-qualified call to an imported suspending GENERIC free function is never framed in a driven body — the resume plain-calls the instantiation and the sync check refuses a sound program (qualified × generic × embedded; SL-217's embedded sibling)\"}","request_id":"pid-81294-1789845297-6000","sequence":1043,"timestamp":"1789845297"}
---
