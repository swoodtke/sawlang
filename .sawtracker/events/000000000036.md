---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791162","description":"## Scope and status\n\nDF-257a: Apply the same defaulted-init selection to bare and qualified calls\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/234-fallibility-flip.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/234-fallibility-flip.md)\n- [sawlang/designs/53-ergonomics.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/53-ergonomics.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 80–80\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L80-L80)\n\n- DF‑257a — the two construction checkers SELECT an init differently, so an init with a defaulted parameter resolves at the bare spelling and is `no matching initializer` at the module-qualified one (entry below, filed Aug 25 by design 234 unit 3's hazard sweep; PRE-EXISTING, probe-refuted in both directions)\n\n### sawlang TODO lines 1486–1528\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L1486-L1528)\n\n## DF‑257a — the two construction checkers SELECT an init differently, so a\n## DEFAULTED parameter resolves at the bare spelling and not the qualified one\n## (filed Aug 25 by design 234 unit 3's hazard sweep; PRE-EXISTING)\n\nDF‑245a's landing named this as one of two things unit 3 would meet. Probed both\ndirections:\n\n```saw\n// hz/lib.saw\npublic struct Plain { value: Int }\nextension Plain { public init(n: Int = 5) -> Plain { Plain(value: n) } }\n```\n```saw\nimport hz.lib.{Plain}     func main() { let p = Plain()      }   // compiles, n = 5\nimport hz.lib             func main() { let p = lib.Plain()  }   // refused\n// error: no matching initializer for `Plain` with parameters:\n//   hint: field init expects: value; available init methods: [['n']]\n```\n\nMECHANISM (obligation 4): `_check_struct_init` matches an init by \"the provided\nnames are a SUBSET of the parameters and every omitted one has a default\"\n(design 53); `_check_module_struct_init` matches by SET EQUALITY\n(`provided_params == init_param_names`), so a defaulted parameter the caller\nomits removes the match instead of being filled. One rule, two implementations,\nand only one of them learned about defaults. The mechanism reaches every\nconstruction the qualified path serves — a defaulted init is the found cell, and\nthe sibling cells are anything else the subset rule accepts that equality does\nnot.\n\nTHE FALLIBLE FORM MAKES IT WORSE, which is how design 234 met it: the qualified\npath returns the RECEIVER type on a failed match, so `try! lib.Holder()` then\nreports a SECOND error about the caller's own `try` (``try` requires a Result\ntype, got `Holder``) — the caller is told its spelling is wrong when the callee's\nsignature is what was never read. The non-fallible twin above reports once.\n\nNOT REACHED BY DESIGN 234's FLIP: every constructor the flip touches\n(`Vector(capacity:)`, `Data(capacity:)`, `Arc(value:)`, `Channel()`,\n`StringBuilder(capacity:)`, `TaskGroup(threads:)`) has no defaulted parameter,\nand the one std init that does — `cbor.CborEncoder(max_depth: Int = 64)` — does\nnot become fallible. So the flip records this rather than fixing it: the fix is a\nselection rule moving to ONE predicate both checkers ask, which is its own\nfunnel-extraction commit. [53, 234, 245]\n","id":"SL-36","labels":["todo-import","backlog","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":36,"status":"open","title":"DF-257a: Apply the same defaulted-init selection to bare and qualified calls","updated":"1788791162"},"before":null,"id":"SL-36","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-257a: Apply the same defaulted-init selection to bare and qualified calls\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/234-fallibility-flip.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/234-fallibility-flip.md)\\n- [sawlang/designs/53-ergonomics.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/53-ergonomics.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 80–80\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L80-L80)\\n\\n- DF‑257a — the two construction checkers SELECT an init differently, so an init with a defaulted parameter resolves at the bare spelling and is `no matching initializer` at the module-qualified one (entry below, filed Aug 25 by design 234 unit 3's hazard sweep; PRE-EXISTING, probe-refuted in both directions)\\n\\n### sawlang TODO lines 1486–1528\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L1486-L1528)\\n\\n## DF‑257a — the two construction checkers SELECT an init differently, so a\\n## DEFAULTED parameter resolves at the bare spelling and not the qualified one\\n## (filed Aug 25 by design 234 unit 3's hazard sweep; PRE-EXISTING)\\n\\nDF‑245a's landing named this as one of two things unit 3 would meet. Probed both\\ndirections:\\n\\n```saw\\n// hz/lib.saw\\npublic struct Plain { value: Int }\\nextension Plain { public init(n: Int = 5) -> Plain { Plain(value: n) } }\\n```\\n```saw\\nimport hz.lib.{Plain}     func main() { let p = Plain()      }   // compiles, n = 5\\nimport hz.lib             func main() { let p = lib.Plain()  }   // refused\\n// error: no matching initializer for `Plain` with parameters:\\n//   hint: field init expects: value; available init methods: [['n']]\\n```\\n\\nMECHANISM (obligation 4): `_check_struct_init` matches an init by \\\"the provided\\nnames are a SUBSET of the parameters and every omitted one has a default\\\"\\n(design 53); `_check_module_struct_init` matches by SET EQUALITY\\n(`provided_params == init_param_names`), so a defaulted parameter the caller\\nomits removes the match instead of being filled. One rule, two implementations,\\nand only one of them learned about defaults. The mechanism reaches every\\nconstruction the qualified path serves — a defaulted init is the found cell, and\\nthe sibling cells are anything else the subset rule accepts that equality does\\nnot.\\n\\nTHE FALLIBLE FORM MAKES IT WORSE, which is how design 234 met it: the qualified\\npath returns the RECEIVER type on a failed match, so `try! lib.Holder()` then\\nreports a SECOND error about the caller's own `try` (``try` requires a Result\\ntype, got `Holder``) — the caller is told its spelling is wrong when the callee's\\nsignature is what was never read. The non-fallible twin above reports once.\\n\\nNOT REACHED BY DESIGN 234's FLIP: every constructor the flip touches\\n(`Vector(capacity:)`, `Data(capacity:)`, `Arc(value:)`, `Channel()`,\\n`StringBuilder(capacity:)`, `TaskGroup(threads:)`) has no defaulted parameter,\\nand the one std init that does — `cbor.CborEncoder(max_depth: Int = 64)` — does\\nnot become fallible. So the flip records this rather than fixing it: the fix is a\\nselection rule moving to ONE predicate both checkers ask, which is its own\\nfunnel-extraction commit. [53, 234, 245]\\n\",\"labels\":[\"todo-import\",\"backlog\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-7b5785663c9a9c6c0c57-create\",\"title\":\"DF-257a: Apply the same defaulted-init selection to bare and qualified calls\"}","request_id":"todo-20260907-7b5785663c9a9c6c0c57-create","sequence":36,"timestamp":"1788791162"}
---
