---
{"action":"create","actor":"agent:claude-sl280","after":{"assignee":"","author":"agent:claude-sl280","comments":[],"created":"1789657827","description":"Blade's dependency resolver contains a genuine SUSPENDING RECURSION (`visit -> visit`), and it compiles today only because SL-280's keying bug hides it. The SL-280 funnel fix builds the frame the cycle check asks about, so the refusal fires and **blade no longer compiles** — which blocks the `freestanding` and `bootstrap` gate lanes and therefore blocks SL-280 from landing.\n\n## The failure, measured\n\nBranch `worktree-agent-aedc86066afe1a158` (the SL-280 `callee_frame_key` funnel), per-commit gate:\n\n    suite:        2542 passed, 10 xfailed   (green)\n    freestanding: FAILS at \"building blade\"\n\n        error: suspending recursion is not allowed: the suspending-call cycle\n        `visit -> visit` has no compile-time frame size (design 44 embeds callee\n        frames by value). Break the cycle or drive the inner call separately.\n          --> blade/src/main.saw:150:1\n\nSame command on the same tree with only the three compiler files reverted to HEAD (`git stash` of `sawc/coro_transform.py`, `sawc/typechecker/effects.py`, `sawc/typechecker/expressions.py`): `Compiled blade/src/main.saw -> .build/scratch/blade_base`. So it is the fix that surfaces it, and nothing else on the branch.\n\n## Why it was hidden, exactly\n\n`blade/src/resolver.saw:271` — `func visit(...)`, module-PRIVATE in module `resolver`, self-recursive at `:392` (descends into a dependency's dependencies), and genuinely suspending: `ls_remote_tags` / `clone_tag` (`blade/src/git.saw:142,157`) call `Command.output()` / `Command.run()`, whose `reap` loop parks on `io_wait` / `sleep` (`sawc/std/process.saw:473-507`).\n\n`_analyze_nesting`'s gate (`sawc/coro_transform.py:1776-1787`) refuses a cycle only when EVERY frame on it will actually be built. SL-227 wrote that gate specifically so blade would keep compiling, and recorded why: the closure walk never reached `visit`, so no `__Frame_visit` was ever built and the cycle was never embedded by value.\n\nThe reason the walk never reached it IS SL-280. Verified in the baseline IR (`.build/scratch/blade_base.ll`, pre-fix compiler):\n\n  * `__Frame_main` — present.\n  * `__Frame_resolve` — present. `resolve` is PUBLIC, so its frame key equals its bare name and design 208's splice took it.\n  * `__Frame_visit` — **0 occurrences**. `visit` is private, so design 249 tags it `visit$m$src_resolver`, the splice table was keyed by the bare name, the edge was declined with no diagnostic, and every `Command.output()` under the whole dependency walk lowered through codegen's out-of-frame park fallback — the blocked-thread behaviour SL-280 was filed for, shipping in the package manager.\n\nSo blade is not an innocent bystander: it is an instance of the bug. The fix reaches it, finds a frame it has to build, and the frame is self-recursive, which design 44 cannot size.\n\nThis was flagged as a hazard in SL-280 c3 (\"the existing carve-out this must not break: blade's dependency resolver relies on a suspending callee never being embedded\") and in SL-227's landing note (\"A near-miss that refused to compile blade was caught by the freestanding gate during the batch and corrected to ask will-this-frame-be-built\"). The correction SL-227 made is exactly the gate that stops protecting blade once the frame is genuinely built.\n\n## Consequence for SL-274\n\nSL-274 (module-mangle + internal linkage for every free function) widens the same discovery. Whatever answer is chosen here has to hold for it too — it cannot be a fix that depends on `visit` staying undiscovered, because after SL-274 nothing stays undiscovered.\n\n## Options (needs a ruling)\n\n1. **De-recurse blade's `visit`** — the design-honest answer. Replace the DFS recursion with an explicit worklist in `resolve` (entries = pending edge, plus a pop-marker entry so the ancestor `stack` used for cycle detection keeps its exact meaning; children pushed in reverse to preserve left-to-right order). Roughly 80 lines in `blade/src/resolver.saw`, covered by `blade/tests/resolver_{cycle,transitive,two_sources,conflict,constraint}.saw` + `git_resolve.saw` + `git_local_repo.saw` under the `bootstrap` lane. Blade's behaviour is unchanged; the language stops being asked for something it does not offer.\n2. **Support suspending recursion** — lift design 44's by-value frame embedding for a cycle (heap-allocated frames for recursive suspending callees). Large, and a language-level decision.\n3. **Keep the callee undiscovered** — have the closure walk decline an edge that would close a suspending cycle. This is the status quo restated: a silent degradation (the park blocks the executor thread) on exactly the shape SL-280 exists to stop, and it contradicts designs 96/101/104 and the never-hide-errors doctrine. Recorded for completeness, not recommended.\n\nRecommend 1. Filed rather than implemented because it is a behaviour-sensitive change to the package manager's core algorithm, and SL-227's precedent is that the lead ruled on this tension once already.\n\n## Where the work is parked\n\n`worktree-agent-aedc86066afe1a158` carries the finished SL-280 compiler unit (funnel + splice-emission invariant + structural-suspension phase fix + 4 new tests + 4 conformance rows K101-K104). Full suite green; freestanding red at this one build.\n","id":"SL-304","labels":["bug","compiler","coroutine","blade"],"priority":"high","project":"SL","revision":1,"sequence":894,"stage":"backlog","status":"open","title":"Blade's resolver has a genuine suspending recursion that SL-280's fix exposes — blade stops compiling","updated":"1789657827"},"before":null,"id":"SL-304","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl280\",\"description\":\"Blade's dependency resolver contains a genuine SUSPENDING RECURSION (`visit -> visit`), and it compiles today only because SL-280's keying bug hides it. The SL-280 funnel fix builds the frame the cycle check asks about, so the refusal fires and **blade no longer compiles** — which blocks the `freestanding` and `bootstrap` gate lanes and therefore blocks SL-280 from landing.\\n\\n## The failure, measured\\n\\nBranch `worktree-agent-aedc86066afe1a158` (the SL-280 `callee_frame_key` funnel), per-commit gate:\\n\\n    suite:        2542 passed, 10 xfailed   (green)\\n    freestanding: FAILS at \\\"building blade\\\"\\n\\n        error: suspending recursion is not allowed: the suspending-call cycle\\n        `visit -> visit` has no compile-time frame size (design 44 embeds callee\\n        frames by value). Break the cycle or drive the inner call separately.\\n          --> blade/src/main.saw:150:1\\n\\nSame command on the same tree with only the three compiler files reverted to HEAD (`git stash` of `sawc/coro_transform.py`, `sawc/typechecker/effects.py`, `sawc/typechecker/expressions.py`): `Compiled blade/src/main.saw -> .build/scratch/blade_base`. So it is the fix that surfaces it, and nothing else on the branch.\\n\\n## Why it was hidden, exactly\\n\\n`blade/src/resolver.saw:271` — `func visit(...)`, module-PRIVATE in module `resolver`, self-recursive at `:392` (descends into a dependency's dependencies), and genuinely suspending: `ls_remote_tags` / `clone_tag` (`blade/src/git.saw:142,157`) call `Command.output()` / `Command.run()`, whose `reap` loop parks on `io_wait` / `sleep` (`sawc/std/process.saw:473-507`).\\n\\n`_analyze_nesting`'s gate (`sawc/coro_transform.py:1776-1787`) refuses a cycle only when EVERY frame on it will actually be built. SL-227 wrote that gate specifically so blade would keep compiling, and recorded why: the closure walk never reached `visit`, so no `__Frame_visit` was ever built and the cycle was never embedded by value.\\n\\nThe reason the walk never reached it IS SL-280. Verified in the baseline IR (`.build/scratch/blade_base.ll`, pre-fix compiler):\\n\\n  * `__Frame_main` — present.\\n  * `__Frame_resolve` — present. `resolve` is PUBLIC, so its frame key equals its bare name and design 208's splice took it.\\n  * `__Frame_visit` — **0 occurrences**. `visit` is private, so design 249 tags it `visit$m$src_resolver`, the splice table was keyed by the bare name, the edge was declined with no diagnostic, and every `Command.output()` under the whole dependency walk lowered through codegen's out-of-frame park fallback — the blocked-thread behaviour SL-280 was filed for, shipping in the package manager.\\n\\nSo blade is not an innocent bystander: it is an instance of the bug. The fix reaches it, finds a frame it has to build, and the frame is self-recursive, which design 44 cannot size.\\n\\nThis was flagged as a hazard in SL-280 c3 (\\\"the existing carve-out this must not break: blade's dependency resolver relies on a suspending callee never being embedded\\\") and in SL-227's landing note (\\\"A near-miss that refused to compile blade was caught by the freestanding gate during the batch and corrected to ask will-this-frame-be-built\\\"). The correction SL-227 made is exactly the gate that stops protecting blade once the frame is genuinely built.\\n\\n## Consequence for SL-274\\n\\nSL-274 (module-mangle + internal linkage for every free function) widens the same discovery. Whatever answer is chosen here has to hold for it too — it cannot be a fix that depends on `visit` staying undiscovered, because after SL-274 nothing stays undiscovered.\\n\\n## Options (needs a ruling)\\n\\n1. **De-recurse blade's `visit`** — the design-honest answer. Replace the DFS recursion with an explicit worklist in `resolve` (entries = pending edge, plus a pop-marker entry so the ancestor `stack` used for cycle detection keeps its exact meaning; children pushed in reverse to preserve left-to-right order). Roughly 80 lines in `blade/src/resolver.saw`, covered by `blade/tests/resolver_{cycle,transitive,two_sources,conflict,constraint}.saw` + `git_resolve.saw` + `git_local_repo.saw` under the `bootstrap` lane. Blade's behaviour is unchanged; the language stops being asked for something it does not offer.\\n2. **Support suspending recursion** — lift design 44's by-value frame embedding for a cycle (heap-allocated frames for recursive suspending callees). Large, and a language-level decision.\\n3. **Keep the callee undiscovered** — have the closure walk decline an edge that would close a suspending cycle. This is the status quo restated: a silent degradation (the park blocks the executor thread) on exactly the shape SL-280 exists to stop, and it contradicts designs 96/101/104 and the never-hide-errors doctrine. Recorded for completeness, not recommended.\\n\\nRecommend 1. Filed rather than implemented because it is a behaviour-sensitive change to the package manager's core algorithm, and SL-227's precedent is that the lead ruled on this tension once already.\\n\\n## Where the work is parked\\n\\n`worktree-agent-aedc86066afe1a158` carries the finished SL-280 compiler unit (funnel + splice-emission invariant + structural-suspension phase fix + 4 new tests + 4 conformance rows K101-K104). Full suite green; freestanding red at this one build.\\n\",\"labels\":[\"bug\",\"compiler\",\"coroutine\",\"blade\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"pid-10747-1789657827-6000\",\"stage\":\"backlog\",\"title\":\"Blade's resolver has a genuine suspending recursion that SL-280's fix exposes — blade stops compiling\"}","request_id":"pid-10747-1789657827-6000","sequence":894,"timestamp":"1789657827"}
---
