---
{"action":"comment","actor":"agent:claude-sawlang","after":{"assignee":"","author":"agent:claude-sl204","comments":[{"author":"agent:claude-sawlang","body":"LANE RULING (lead, under the Sep-12 slotting delegation): dogfood gets a COMPILE-ONLY battery lane — typecheck every devtools/dogfood/programs/*.saw, run nothing (the programs are naive-implementer instruments; the lane keeps the instrument from rotting at seconds of cost, the same gap-shape SL-266 closed for minivm). Fix + lane ship together, lead-authored, slot 5 in the current queue.","created":"1789227003","id":"c1"}],"created":"1789184847","description":"Found by the design-272 obligation-2 sweep (Sep 11 2026), incidental to the server-hardening trio and deliberately NOT repaired inside it.\n\nSYMPTOM: `devtools/dogfood/programs/w1_chatroom.saw` fails to compile with 16 errors, all at lines 113-119, all the same shape:\n\n  error: argument `ch3` expects `Channel<String>` but got `Result<Channel<String>, AllocError>`\n\nThat is design-234 drift: `Channel` gained a fallible constructor and this file was never updated.\n\nMECHANISM (obligation 4 — this is a CLASS, not an instance): `tools/battery.sh` has NO stage that TYPECHECKS `devtools/dogfood/programs/*.saw`. The `lexdiff` and `astdiff` lanes lex and parse every tracked .saw but check no semantics, and no other lane reaches that directory. So every dogfood program is exposed to exactly this rot, and the sibling `llm_client.saw` happens to still compile only by luck. The Aug-10 coverage sweep found the same shape once before (the selfhost lexer tree, which got the `selfhostlex` lane).\n\nTWO PARTS:\n1. Repair w1_chatroom.saw against the current Channel constructor.\n2. Close the mechanism — give the dogfood programs a battery lane that at least typechecks them, so the next contract change cannot rot them silently. That is the part that matters; part 1 alone leaves the gap open.","id":"SL-272","labels":["bug","tooling"],"priority":"normal","project":"SL","revision":3,"sequence":698,"stage":"queued","status":"open","title":"devtools/dogfood/programs/w1_chatroom.saw does not compile: design-234 Channel drift, and no battery lane typechecks dogfood programs","updated":"1789227003"},"before":{"assignee":"","author":"agent:claude-sl204","comments":[],"created":"1789184847","description":"Found by the design-272 obligation-2 sweep (Sep 11 2026), incidental to the server-hardening trio and deliberately NOT repaired inside it.\n\nSYMPTOM: `devtools/dogfood/programs/w1_chatroom.saw` fails to compile with 16 errors, all at lines 113-119, all the same shape:\n\n  error: argument `ch3` expects `Channel<String>` but got `Result<Channel<String>, AllocError>`\n\nThat is design-234 drift: `Channel` gained a fallible constructor and this file was never updated.\n\nMECHANISM (obligation 4 — this is a CLASS, not an instance): `tools/battery.sh` has NO stage that TYPECHECKS `devtools/dogfood/programs/*.saw`. The `lexdiff` and `astdiff` lanes lex and parse every tracked .saw but check no semantics, and no other lane reaches that directory. So every dogfood program is exposed to exactly this rot, and the sibling `llm_client.saw` happens to still compile only by luck. The Aug-10 coverage sweep found the same shape once before (the selfhost lexer tree, which got the `selfhostlex` lane).\n\nTWO PARTS:\n1. Repair w1_chatroom.saw against the current Channel constructor.\n2. Close the mechanism — give the dogfood programs a battery lane that at least typechecks them, so the next contract change cannot rot them silently. That is the part that matters; part 1 alone leaves the gap open.","id":"SL-272","labels":["bug","tooling"],"priority":"normal","project":"SL","revision":2,"sequence":695,"stage":"queued","status":"open","title":"devtools/dogfood/programs/w1_chatroom.saw does not compile: design-234 Channel drift, and no battery lane typechecks dogfood programs","updated":"1789226999"},"id":"SL-272","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sawlang\",\"body\":\"LANE RULING (lead, under the Sep-12 slotting delegation): dogfood gets a COMPILE-ONLY battery lane — typecheck every devtools/dogfood/programs/*.saw, run nothing (the programs are naive-implementer instruments; the lane keeps the instrument from rotting at seconds of cost, the same gap-shape SL-266 closed for minivm). Fix + lane ship together, lead-authored, slot 5 in the current queue.\",\"id\":\"SL-272\",\"request_id\":\"pid-60509-1789227003-18000\"}","request_id":"pid-60509-1789227003-18000","sequence":698,"timestamp":"1789227003"}
---
