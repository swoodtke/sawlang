---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791157","description":"## Scope and status\n\nDF-247a: Keep ordinary calls resolvable when a function is also a spawn root\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/242-thread-task-split.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/242-thread-task-split.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 69–69\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L69-L69)\n\n- DF‑247a — a function that is a `group.spawn` ROOT is `undefined function` at every other call of it in the same module (entry below, filed by design 242 unit 0's census; PRE-EXISTING, stash-verified). The fix owes the ROOT MATRIX its mechanism reaches, not the one probed cell\n\n### sawlang TODO lines 1249–1285\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L1249-L1285)\n\n## DF‑247a — a function that is a `group.spawn` ROOT is `undefined function`\n## at every OTHER call of it in the same module (filed Aug 22 by design 242\n## unit 0's census probes; PRE-EXISTING, verified by stash against the branch\n## point 361ea0bf)\n\n```saw\nfunc work(n: Int) -> Int { n * n }\n\nfunc main() {\n    print(work(2))                    // error: undefined function `work`\n    var group = TaskGroup()\n    let h = group.spawn(work(5))\n    print(h.join())\n}\n```\n\nReordering the two does not help, and the error anchors on the ORDINARY call,\nwhich is the one line that has nothing wrong with it. `examples/` has no\nprogram that spawns a function and also calls it, which is why the shape has\nsat unnoticed; it is easy to reach the moment a body is worth both running\ninline and running as a task.\n\nMECHANISM (obligation 4): the coroutine transform's spawn-root rewrite. A\n`group.spawn(f(...))` records `f` a spawn root, and the transform replaces the\nauthored `f` with the frame machinery (`__Frame_f`, `f$spawnroot`,\n`__spawn_f`) before the entry module is re-typechecked — so the ordinary call,\nwhich was never rewritten, resolves against a function table the original name\nhas left. The mechanism reaches every ROOT KIND the transform substitutes, so\nthe sweep the fix owes is the root matrix: `group.spawn`, `__saw_drive` /\n`__saw_drive_steps`, a `Thread.spawn { }` closure body naming a spawned\nfunction, and a spawn root that is also called from a THIRD function rather\nthan from the spawner. Only the first cell is probed so far (the two spellings\nabove); the rest are unprobed and presumed to be the same cell.\n\nNot pinned yet — a pin belongs with the sweep, and design 242 stopped at\nrecording the shape rather than growing an unrelated fix. [52b, 242]\n","id":"SL-25","labels":["todo-import","backlog","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":25,"status":"open","title":"DF-247a: Keep ordinary calls resolvable when a function is also a spawn root","updated":"1788791157"},"before":null,"id":"SL-25","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-247a: Keep ordinary calls resolvable when a function is also a spawn root\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/242-thread-task-split.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/242-thread-task-split.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 69–69\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L69-L69)\\n\\n- DF‑247a — a function that is a `group.spawn` ROOT is `undefined function` at every other call of it in the same module (entry below, filed by design 242 unit 0's census; PRE-EXISTING, stash-verified). The fix owes the ROOT MATRIX its mechanism reaches, not the one probed cell\\n\\n### sawlang TODO lines 1249–1285\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L1249-L1285)\\n\\n## DF‑247a — a function that is a `group.spawn` ROOT is `undefined function`\\n## at every OTHER call of it in the same module (filed Aug 22 by design 242\\n## unit 0's census probes; PRE-EXISTING, verified by stash against the branch\\n## point 361ea0bf)\\n\\n```saw\\nfunc work(n: Int) -> Int { n * n }\\n\\nfunc main() {\\n    print(work(2))                    // error: undefined function `work`\\n    var group = TaskGroup()\\n    let h = group.spawn(work(5))\\n    print(h.join())\\n}\\n```\\n\\nReordering the two does not help, and the error anchors on the ORDINARY call,\\nwhich is the one line that has nothing wrong with it. `examples/` has no\\nprogram that spawns a function and also calls it, which is why the shape has\\nsat unnoticed; it is easy to reach the moment a body is worth both running\\ninline and running as a task.\\n\\nMECHANISM (obligation 4): the coroutine transform's spawn-root rewrite. A\\n`group.spawn(f(...))` records `f` a spawn root, and the transform replaces the\\nauthored `f` with the frame machinery (`__Frame_f`, `f$spawnroot`,\\n`__spawn_f`) before the entry module is re-typechecked — so the ordinary call,\\nwhich was never rewritten, resolves against a function table the original name\\nhas left. The mechanism reaches every ROOT KIND the transform substitutes, so\\nthe sweep the fix owes is the root matrix: `group.spawn`, `__saw_drive` /\\n`__saw_drive_steps`, a `Thread.spawn { }` closure body naming a spawned\\nfunction, and a spawn root that is also called from a THIRD function rather\\nthan from the spawner. Only the first cell is probed so far (the two spellings\\nabove); the rest are unprobed and presumed to be the same cell.\\n\\nNot pinned yet — a pin belongs with the sweep, and design 242 stopped at\\nrecording the shape rather than growing an unrelated fix. [52b, 242]\\n\",\"labels\":[\"todo-import\",\"backlog\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-e7c6ee2bc47eb1b5d573-create\",\"title\":\"DF-247a: Keep ordinary calls resolvable when a function is also a spawn root\"}","request_id":"todo-20260907-e7c6ee2bc47eb1b5d573-create","sequence":25,"timestamp":"1788791157"}
---
