---
{"action":"close","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl318u2","author":"agent:claude-sl318u4","comments":[{"author":"agent:claude-sawlang","body":"USER RULING (Sep 20, relayed by the lead): folded into design 275 U2 (running now) as a BOUNDED add-on — the seed table's node-id keying so the first settling matches the entry graph's edges (or a stated reason it cannot), verified by the probe in this issue's body plus a two-violation program that reports BOTH at the first settling. Not a restructuring of settling; if the fix is not the keying, the agent files what it finds and reports. Relayed to agent:claude-sl318u2 by message.\n","created":"1789926668","id":"c1"},{"author":"agent:claude-sl318u2","body":"REPRODUCED and FIXED AT THE KEYING by design 275 U2 (branch sl318u2, commit 2a60ff36; not yet submitted — U2 goes out as SL-318.p6). Settling untouched.\n\nMy first pass reported this as non-reproducing: main and the branch, cold cache and warm, four program shapes, all reported both violations at round 1. Then the two-violation test I had just added PASSED in isolation and FAILED in the full suite twenty minutes later — same tree, same commit. The symptom is CACHE-STATE DEPENDENT. A freshly built std pair agrees with itself; a RESTORED one need not.\n\nMEASURED: five of the eight blobs in .build/stdcache/ held a seed table keyed to an AST that was not the one stored beside them. Command.output keyed 20761 in the table and 62190 in both the restored AST and the namespace symbol the entry edges resolve through — this issue's own two numbers, now off the blob rather than off a round dump. Every inconsistent blob was written by a process whose node_id bound was an order of magnitude past a fresh std build's (37997 vs 182671-733519), i.e. by a process that had already compiled something — the state _prepared_builtins' own docstring says the pair must not be stored from. One blob holds an AST of TWELVE methods beside a full std namespace. A pickle round trip preserves node_id (probed directly), so the inconsistency is at the STORE, not the restore.\n\nTWO KEYING FIXES: stdcache._pair_is_consistent — store declines to publish a pair whose seed keys are not in the AST it is about to pickle beside them (one walk per cache MISS; declining costs one cold std build, publishing costs every later compile a diagnostic). And sawc._rekeyed_std_seed — the handover re-keys a restored table onto the AST this compile actually holds, so blobs already on disk are harmless; the fallback key is (owner, method), the granularity every other std suspension census already uses, and an overloaded pair re-keys onto every match (the conservative direction). A consistent table is returned unchanged.\n\nPROVEN against the real bad blob, kept at .build/scratch/badblob/: 18 dangling keys -> 0 after the re-key, and Command.output's namespace symbol id seeded after and not before. End to end, with that blob on disk the two-violation program reports BOTH errors on three consecutive runs.\n\nTEST: examples/errors/thread_body_reports_both_violations.saw (a Thread.spawn { boundary() } beside a Thread.spawn reaching Command.output through four hops), asserting both messages and '2 error(s) generated'. K112 keeps its own file, as the ruling requires. _effect_seed_std_methods' docstring carries the measurement and what enforces the invariant.\n\nGates: suite 2574/10x, freestanding 36/36, battery 30 stages green, IR byte-compare vs main 0 mismatches over 1578 artifacts.","created":"1789938894","id":"c2"},{"author":"agent:claude-sawlang","body":"LANDED Sep 21 in SL-318.p6 (design 275 U2, apply 0c1e7325 / merge ded0605a) per the user's ruling (c1): REPRODUCED and fixed at the KEYING, no change to settling. Cache-state dependent, which is why it looked environmental: five of eight stdcache blobs on the machine held a seed table keyed to an AST generation that was not the one stored beside them (`Command.output` keyed 20761 in the table, 62190 in the AST and namespace symbol), each written by a process whose id bound was an order of magnitude past a fresh std build's. Fixes: `stdcache.store` refuses to publish an inconsistent pair; `sawc._rekeyed_std_seed` re-keys a restored table onto the AST the compile holds. Codex r1 (P2) required IDENTITY, not integer membership — r2 keys on `(owner, name)` at publication, load validation and repair alike: wrong-method IDs and missing IDs are rejected/repaired, an unresolvable identity is an invariant failure, an inconsistent on-disk pair falls back to a cold build and is removed; the handover checks against the unfiltered std AST. New `stdseed` battery lane (`tools/test_std_seed_keying.py`, 31 stages now) covers the wrong-id scenario beside a sound-cache control; `examples/errors/thread_body_reports_both_violations.saw` is the standing test (it flapped before the fix).\n","created":"1789993132","id":"c3"}],"created":"1789775114","description":"Found while pinning design 275 U4's thread-body message (SL-318 U4 review,\nlead FIX 1). Pre-existing keying property of the std seed, made visible by the\nnew refusal.\n\nA `sync` violation reached through a SEEDED std method is not reported at the\nFIRST settling of the effect graph — only at a later one — because the seed\ntable's node ids do not match the entry graph's edges yet:\n\n    --- finalize round 1\n        thread body closure: causes {} edges=[62190]\n        seed 20761 `Command.output`: {cooperative, blocking}\n        seed TABLE has 18 entries; 62190 present: False\n    --- finalize round 3\n        thread body closure: causes {cooperative, blocking} edges=[62190]\n        seed 62190 `Command.output`: {cooperative, blocking}\n        seed TABLE has 18 entries; 62190 present: True\n\n(`.build/scratch/probe_dump_thread_nodes.py` in the sl318u4 worktree, over\n`.build/scratch/probe_thread_output.saw`.) Design 206 keys the table by\n`Method.node_id` because that identity is \"preserved verbatim across the std\ncache's pickle\"; in round 1 the ids the ENTRY graph's edges carry are a\ndifferent generation (62190 vs 20761), and a later stage re-hands a table whose\nkeys do match.\n\nTHE CONSEQUENCE, and it is the reason this is worth a line: the pipeline stops\nafter a settling that REPORTED errors, so a program that has any OTHER compile\nerror never reaches the later round and the std-method violation is silently\ndropped from that compile. Probed: `Thread.spawn { [move c] in c.output() }`\nalone is refused; the same cell BESIDE a `Thread.spawn { boundary() }` (which is\nrefused at round 1) reports only the first, and the program compiles no further,\nso the author sees the second violation only after fixing the first. A CLEAN\nprogram always reaches the later round, so nothing is mis-compiled — what is\nlost is diagnostic completeness in one compile.\n\nNot caused by U4 (the seed's KEYING is design 206's, unchanged); U4 only made a\nrefusal exist that reaches through a seeded leaf in this context. Recorded here\nso SL-318 U1's ledger, which takes the seeded-leaf population over, either keys\nthe table so round 1 matches or states why the first settling cannot.\n\nCorpus effect: the conformance cell that pins the message lives in its OWN file\n(`examples/conformance/K112_thread_body_refuses_a_multi_cause_std_method.saw`)\nfor exactly this reason — beside another refused cell its message would not be\nemitted.\n","id":"SL-327","labels":["design-275"],"priority":"low","project":"SL","revision":6,"sequence":1107,"stage":"queued","status":"closed","title":"a sync violation through a seeded std method is missed at the first settling","updated":"1789993132"},"before":{"assignee":"agent:claude-sl318u2","author":"agent:claude-sl318u4","comments":[{"author":"agent:claude-sawlang","body":"USER RULING (Sep 20, relayed by the lead): folded into design 275 U2 (running now) as a BOUNDED add-on — the seed table's node-id keying so the first settling matches the entry graph's edges (or a stated reason it cannot), verified by the probe in this issue's body plus a two-violation program that reports BOTH at the first settling. Not a restructuring of settling; if the fix is not the keying, the agent files what it finds and reports. Relayed to agent:claude-sl318u2 by message.\n","created":"1789926668","id":"c1"},{"author":"agent:claude-sl318u2","body":"REPRODUCED and FIXED AT THE KEYING by design 275 U2 (branch sl318u2, commit 2a60ff36; not yet submitted — U2 goes out as SL-318.p6). Settling untouched.\n\nMy first pass reported this as non-reproducing: main and the branch, cold cache and warm, four program shapes, all reported both violations at round 1. Then the two-violation test I had just added PASSED in isolation and FAILED in the full suite twenty minutes later — same tree, same commit. The symptom is CACHE-STATE DEPENDENT. A freshly built std pair agrees with itself; a RESTORED one need not.\n\nMEASURED: five of the eight blobs in .build/stdcache/ held a seed table keyed to an AST that was not the one stored beside them. Command.output keyed 20761 in the table and 62190 in both the restored AST and the namespace symbol the entry edges resolve through — this issue's own two numbers, now off the blob rather than off a round dump. Every inconsistent blob was written by a process whose node_id bound was an order of magnitude past a fresh std build's (37997 vs 182671-733519), i.e. by a process that had already compiled something — the state _prepared_builtins' own docstring says the pair must not be stored from. One blob holds an AST of TWELVE methods beside a full std namespace. A pickle round trip preserves node_id (probed directly), so the inconsistency is at the STORE, not the restore.\n\nTWO KEYING FIXES: stdcache._pair_is_consistent — store declines to publish a pair whose seed keys are not in the AST it is about to pickle beside them (one walk per cache MISS; declining costs one cold std build, publishing costs every later compile a diagnostic). And sawc._rekeyed_std_seed — the handover re-keys a restored table onto the AST this compile actually holds, so blobs already on disk are harmless; the fallback key is (owner, method), the granularity every other std suspension census already uses, and an overloaded pair re-keys onto every match (the conservative direction). A consistent table is returned unchanged.\n\nPROVEN against the real bad blob, kept at .build/scratch/badblob/: 18 dangling keys -> 0 after the re-key, and Command.output's namespace symbol id seeded after and not before. End to end, with that blob on disk the two-violation program reports BOTH errors on three consecutive runs.\n\nTEST: examples/errors/thread_body_reports_both_violations.saw (a Thread.spawn { boundary() } beside a Thread.spawn reaching Command.output through four hops), asserting both messages and '2 error(s) generated'. K112 keeps its own file, as the ruling requires. _effect_seed_std_methods' docstring carries the measurement and what enforces the invariant.\n\nGates: suite 2574/10x, freestanding 36/36, battery 30 stages green, IR byte-compare vs main 0 mismatches over 1578 artifacts.","created":"1789938894","id":"c2"}],"created":"1789775114","description":"Found while pinning design 275 U4's thread-body message (SL-318 U4 review,\nlead FIX 1). Pre-existing keying property of the std seed, made visible by the\nnew refusal.\n\nA `sync` violation reached through a SEEDED std method is not reported at the\nFIRST settling of the effect graph — only at a later one — because the seed\ntable's node ids do not match the entry graph's edges yet:\n\n    --- finalize round 1\n        thread body closure: causes {} edges=[62190]\n        seed 20761 `Command.output`: {cooperative, blocking}\n        seed TABLE has 18 entries; 62190 present: False\n    --- finalize round 3\n        thread body closure: causes {cooperative, blocking} edges=[62190]\n        seed 62190 `Command.output`: {cooperative, blocking}\n        seed TABLE has 18 entries; 62190 present: True\n\n(`.build/scratch/probe_dump_thread_nodes.py` in the sl318u4 worktree, over\n`.build/scratch/probe_thread_output.saw`.) Design 206 keys the table by\n`Method.node_id` because that identity is \"preserved verbatim across the std\ncache's pickle\"; in round 1 the ids the ENTRY graph's edges carry are a\ndifferent generation (62190 vs 20761), and a later stage re-hands a table whose\nkeys do match.\n\nTHE CONSEQUENCE, and it is the reason this is worth a line: the pipeline stops\nafter a settling that REPORTED errors, so a program that has any OTHER compile\nerror never reaches the later round and the std-method violation is silently\ndropped from that compile. Probed: `Thread.spawn { [move c] in c.output() }`\nalone is refused; the same cell BESIDE a `Thread.spawn { boundary() }` (which is\nrefused at round 1) reports only the first, and the program compiles no further,\nso the author sees the second violation only after fixing the first. A CLEAN\nprogram always reaches the later round, so nothing is mis-compiled — what is\nlost is diagnostic completeness in one compile.\n\nNot caused by U4 (the seed's KEYING is design 206's, unchanged); U4 only made a\nrefusal exist that reaches through a seeded leaf in this context. Recorded here\nso SL-318 U1's ledger, which takes the seeded-leaf population over, either keys\nthe table so round 1 matches or states why the first settling cannot.\n\nCorpus effect: the conformance cell that pins the message lives in its OWN file\n(`examples/conformance/K112_thread_body_refuses_a_multi_cause_std_method.saw`)\nfor exactly this reason — beside another refused cell its message would not be\nemitted.\n","id":"SL-327","labels":["design-275"],"priority":"low","project":"SL","revision":5,"sequence":1085,"stage":"queued","status":"open","title":"a sync violation through a seeded std method is missed at the first settling","updated":"1789938894"},"id":"SL-327","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"LANDED Sep 21 in SL-318.p6 (design 275 U2, apply 0c1e7325 / merge ded0605a) per the user's ruling (c1): REPRODUCED and fixed at the KEYING, no change to settling. Cache-state dependent, which is why it looked environmental: five of eight stdcache blobs on the machine held a seed table keyed to an AST generation that was not the one stored beside them (`Command.output` keyed 20761 in the table, 62190 in the AST and namespace symbol), each written by a process whose id bound was an order of magnitude past a fresh std build's. Fixes: `stdcache.store` refuses to publish an inconsistent pair; `sawc._rekeyed_std_seed` re-keys a restored table onto the AST the compile holds. Codex r1 (P2) required IDENTITY, not integer membership — r2 keys on `(owner, name)` at publication, load validation and repair alike: wrong-method IDs and missing IDs are rejected/repaired, an unresolvable identity is an invariant failure, an inconsistent on-disk pair falls back to a cold build and is removed; the handover checks against the unfiltered std AST. New `stdseed` battery lane (`tools/test_std_seed_keying.py`, 31 stages now) covers the wrong-id scenario beside a sound-cache control; `examples/errors/thread_body_reports_both_violations.saw` is the standing test (it flapped before the fix).\\n\",\"id\":\"SL-327\",\"request_id\":\"pid-72823-1789993132-12000\"}","request_id":"pid-72823-1789993132-12000","sequence":1107,"timestamp":"1789993132"}
---
