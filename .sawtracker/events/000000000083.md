---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791230","description":"## Scope and status\n\nDF-172d, third sighting. A binary expression still cannot wrap across lines unless brackets already enclose it, and the shapes that hit it here are the ones the…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/129-newlines-in-brackets.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/129-newlines-in-brackets.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 2651–2661\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L2651-L2661)\n\n- **DF‑172d, third sighting.** A binary expression still cannot wrap across\n  lines unless brackets already enclose it, and the shapes that hit it here\n  are the ones the finding predicts: OR-ing named descriptor bits, and\n  composing a 64-bit value out of two 32-bit halves. Same family, worth\n  recording because it is now the third independent brief to pay it: a\n  postfix `.method()` on the line AFTER a closing `)` does not parse either\n  (`UnsafeMemory<...>(\\n  addr)\\n  .write(v)`), since the newline after the\n  bracket ends the statement — the fix is a `let` for the receiver, which\n  reads better anyway, but it is the same \"wrapping a long expression is\n  where Saw's newline rule bites\" story.\n\n### sawlang TODO lines 9436–9465\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L9436-L9465)\n\n- **DF‑172d — LANGUAGE PAIN, filed. A binary expression cannot be wrapped\n  across lines outside brackets — NEITHER spelling works.** Design 129 made\n  newlines insignificant inside `()`/`[]`/committed `<>`, but a bare\n  continuation is still a statement end, so both of the two things a\n  programmer reaches for are parse errors:\n\n  ```saw\n  let d = base | DESC_VALID | DESC_PAGE\n        | ATTR_AF | ATTR_UXN            // error: Unexpected token: PIPE\n  let d = base | DESC_VALID | DESC_PAGE |\n          ATTR_AF | ATTR_UXN            // error: Unexpected token: NEWLINE\n  ```\n\n  The working spelling is a pair of parentheses around the whole expression,\n  which is the shape this branch adopted:\n\n  ```saw\n  let d = (base | DESC_VALID | DESC_PAGE\n           | ATTR_AF | ATTR_UXN)\n  ```\n\n  This is not a corner: OR-ing eight named bits into a hardware descriptor is\n  the single most common line in a page-table or register driver, and it does\n  not fit in 79 columns. The parenthesis is a workaround a reader has to\n  decode as \"line continuation\" rather than as grouping, and forgetting it\n  gives an error that names a token rather than the rule. Worth a decision:\n  a trailing binary operator suppressing the newline is the low-risk half\n  (the parser has already committed to needing an operand), a leading one\n  needs lookahead. Neither is in this brief's scope.\n\n### sawos TODO lines 238–238\n\n[Original record](https://github.com/swoodtke/sawos/blob/754ec9570aef54609820cc7e28457c7623547670/designs/todo.md#L238-L238)\n\n- SL‑8 — DF‑172d STILL BITES (already filed upstream as DF‑172d; listed here as a cross-reference, not a new issue): unbracketed binary expressions do not wrap across lines; hit again in designs 2 and 4. Resolution tracked in sawlang's own DF. **THIRD SITE, design 6**: `if (a & X) != 0\\n && (b & Y) == 0 {` in `kcore.dispatch`'s `map_access`, fixed by parenthesising the whole condition. **FOURTH AND FIFTH SITES, design 21** (`tests/svc-uart-*`), and the second of them is a shape the earlier sites had not shown: an `if` CONDITION carrying a three-term sum (`traps == answered + 2 * discarded + 1`) is the familiar one, fixed by hoisting the arithmetic into a parenthesised `let`; but a wrapped ASSIGNMENT (`window_discarded =\\n    window_discarded + 1`, split only because the arm was deeply indented) fails as `Unexpected token: NEWLINE` at the LINE BELOW, and its diagnostic points into the continuation rather than at the assignment that ran out of room. It bites hardest where indentation is deepest, which is exactly where a line is most likely to need wrapping; the in-tree fix was the compound form (`window_discarded += 1`), which fits.\n","id":"SL-83","labels":["todo-import","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":83,"status":"open","title":"DF-172d, third sighting. A binary expression still cannot wrap across lines unless brackets already enclose it, and the shapes that hit it here are the ones the…","updated":"1788791230"},"before":null,"id":"SL-83","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-172d, third sighting. A binary expression still cannot wrap across lines unless brackets already enclose it, and the shapes that hit it here are the ones the…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/129-newlines-in-brackets.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/129-newlines-in-brackets.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 2651–2661\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L2651-L2661)\\n\\n- **DF‑172d, third sighting.** A binary expression still cannot wrap across\\n  lines unless brackets already enclose it, and the shapes that hit it here\\n  are the ones the finding predicts: OR-ing named descriptor bits, and\\n  composing a 64-bit value out of two 32-bit halves. Same family, worth\\n  recording because it is now the third independent brief to pay it: a\\n  postfix `.method()` on the line AFTER a closing `)` does not parse either\\n  (`UnsafeMemory<...>(\\\\n  addr)\\\\n  .write(v)`), since the newline after the\\n  bracket ends the statement — the fix is a `let` for the receiver, which\\n  reads better anyway, but it is the same \\\"wrapping a long expression is\\n  where Saw's newline rule bites\\\" story.\\n\\n### sawlang TODO lines 9436–9465\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L9436-L9465)\\n\\n- **DF‑172d — LANGUAGE PAIN, filed. A binary expression cannot be wrapped\\n  across lines outside brackets — NEITHER spelling works.** Design 129 made\\n  newlines insignificant inside `()`/`[]`/committed `<>`, but a bare\\n  continuation is still a statement end, so both of the two things a\\n  programmer reaches for are parse errors:\\n\\n  ```saw\\n  let d = base | DESC_VALID | DESC_PAGE\\n        | ATTR_AF | ATTR_UXN            // error: Unexpected token: PIPE\\n  let d = base | DESC_VALID | DESC_PAGE |\\n          ATTR_AF | ATTR_UXN            // error: Unexpected token: NEWLINE\\n  ```\\n\\n  The working spelling is a pair of parentheses around the whole expression,\\n  which is the shape this branch adopted:\\n\\n  ```saw\\n  let d = (base | DESC_VALID | DESC_PAGE\\n           | ATTR_AF | ATTR_UXN)\\n  ```\\n\\n  This is not a corner: OR-ing eight named bits into a hardware descriptor is\\n  the single most common line in a page-table or register driver, and it does\\n  not fit in 79 columns. The parenthesis is a workaround a reader has to\\n  decode as \\\"line continuation\\\" rather than as grouping, and forgetting it\\n  gives an error that names a token rather than the rule. Worth a decision:\\n  a trailing binary operator suppressing the newline is the low-risk half\\n  (the parser has already committed to needing an operand), a leading one\\n  needs lookahead. Neither is in this brief's scope.\\n\\n### sawos TODO lines 238–238\\n\\n[Original record](https://github.com/swoodtke/sawos/blob/754ec9570aef54609820cc7e28457c7623547670/designs/todo.md#L238-L238)\\n\\n- SL‑8 — DF‑172d STILL BITES (already filed upstream as DF‑172d; listed here as a cross-reference, not a new issue): unbracketed binary expressions do not wrap across lines; hit again in designs 2 and 4. Resolution tracked in sawlang's own DF. **THIRD SITE, design 6**: `if (a & X) != 0\\\\n && (b & Y) == 0 {` in `kcore.dispatch`'s `map_access`, fixed by parenthesising the whole condition. **FOURTH AND FIFTH SITES, design 21** (`tests/svc-uart-*`), and the second of them is a shape the earlier sites had not shown: an `if` CONDITION carrying a three-term sum (`traps == answered + 2 * discarded + 1`) is the familiar one, fixed by hoisting the arithmetic into a parenthesised `let`; but a wrapped ASSIGNMENT (`window_discarded =\\\\n    window_discarded + 1`, split only because the arm was deeply indented) fails as `Unexpected token: NEWLINE` at the LINE BELOW, and its diagnostic points into the continuation rather than at the assignment that ran out of room. It bites hardest where indentation is deepest, which is exactly where a line is most likely to need wrapping; the in-tree fix was the compound form (`window_discarded += 1`), which fits.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-05e5062befb7c6ccbf14-create\",\"title\":\"DF-172d, third sighting. A binary expression still cannot wrap across lines unless brackets already enclose it, and the shapes that hit it here are the ones the…\"}","request_id":"todo-20260907-05e5062befb7c6ccbf14-create","sequence":83,"timestamp":"1788791230"}
---
