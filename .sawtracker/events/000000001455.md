---
{"action":"comment","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"SCOPE (user, Sep 24; recorded by the lead): option (b), flat representation, applies to the chains that are NOT bounded by type depth and do occur in real or generated code:\n- operator chains (&&/||, arithmetic, ??);\n- else-if chains, one arm list including `else if let` arms, charged once per chain.\nPostfix chains stay nested:\n- f()()() needs a function type nested per call, and type nesting is capped at 32;\n- a.b.c and a[i][j] need nested struct/array types;\n- builder method chains and cast chains.\nThe user's words: \"those are contrived examples which would probably never happen in real code so if they have different behaviour, its probably fine (unless the linearization is free)\". Linearization of postfix chains is not free: they mix member, method, index, optional-hop and cast node kinds.\n\nMeasured on main (e62b3164), .build/scratch/probe_chain_kinds.py in the lead checkout:\n\n| chain | 100 | 300 | 1000 |\n|---|---|---|---|\n| && chain | ok | ICE | ICE |\n| else if chain | ok | ICE (Identifier) | raw RecursionError traceback in the parser |\n| builder method chain | ok | ok | ICE |\n| as-cast chain | ok | ok | ICE |\n| flat statements, with and without yield_now | ok | ok | ok |\n\n?? and String + also ICE at 1000; at 100 and 300 the probe itself had type errors.\n\nOpen for the brief: whatever stays nested must fail with a clean diagnostic, never an ICE. The candidates are charging each postfix hop against the design-259 depth budget (parser-only; a deliberate policy, since codex's SL-2 c24 warns against charging flat operators), or accepting U0′'s raised recursion ceiling. The user rules on this when the brief is written, after U0′ and SL-73.\n","created":"1790264558","id":"c1"},{"author":"agent:claude-sawlang","body":"USER RULING (Sep 24, relayed by the lead): a chain that stays nested is charged against the design-259 depth budget of 256. The user's words: \"i think a 256 depth limit is fine\".\n\nConcretely:\n- Each postfix hop (call, member, index, optional hop, method call, `as` cast) in one chain charges one level. The hops do NOT release until the chain ends.\n- The 257th hop is refused with the ordinary \"nesting exceeds the parser depth limit (256)\" diagnostic, anchored at that hop.\n- A chain inside an argument list or group adds to the surrounding depth, as every other charge does.\n\nThis is a parser-only change to the depth funnel. It lands as a unit of this epic, not in U0′ r3, so the U0′ review and the M21 alignment built on it are not reopened. The M21 prototype adopts the same rule in its follow-up.\n","created":"1790265130","id":"c2"}],"created":"1790264003","description":"Measured on main (e62b3164) with `.build/scratch/probe_flat_chain.py` in the lead checkout. The program is `let x = 1 + 1 + … + 1`, then `print(\"{}\", x)`:\n\n| terms | result |\n|---|---|\n| 100, 200, 300 | compiles; prints the right sum |\n| 400 | `internal compiler error at …:2:291 (BinaryOp): maximum recursion depth exceeded` |\n| 500, 2000, 5000, 12000 | the same ICE |\n\nA left-associative chain parses iteratively and has constant source nesting, but its AST is a left spine as deep as the chain is long. Every pass that recurses over expressions pays at least one Python frame per term, and some pay several. The interpreter's default limit of about 1000 frames runs out somewhere between 300 and 400 terms. This is an ICE on an accepted program, not a clean diagnostic, so it violates \"never hide errors\" and the fuzz oracle's contract.\n\nContext:\n- SL-2.p2 (U0′) raises the interpreter limit to 10,192 the first time the parser's depth funnel charges a level. That moves this threshold for most real files, but not for a file whose parse never enters `nested()`. codex's c24 on SL-2 also measured a 12,000-term chain overflowing U0′'s own parser tree walks, which U0′ r3 makes iterative.\n- The same shape: an `else if` chain (the If → else Block → If nesting) and long method or optional chains. The user flagged the 256-arm `else if` limit on Sep 24 as undesirable, and the lead proposed a flat representation for that chain.\n\nMechanism (obligation 4): a left-deep representation of a flat source construct, walked recursively by passes that assume tree depth tracks source nesting. Sweep:\n- binary chains in every operator tier (`+`, `&&`, `??`, comparison chains where they are legal);\n- postfix method, member and optional chains (`a.f().g()…`);\n- `else if` chains.\nEach is measured end to end for the length at which the compiler fails, and the pass that fails is named.\n\nFix shape, needs a design decision:\n- (a) Walk chains iteratively in each pass. That is many passes, and each new pass reintroduces the risk.\n- (b) Represent associative chains flat: an n-ary operand list and a flat else-if arm list, so that tree depth equals source nesting. Passes then iterate lists. This is the larger change, and the durable one.\n- (c) A pass-agnostic guard: a structural chain-length limit with a clean diagnostic. Honest, but a real program can hit it.\n\nThe lead's recommendation is (b), sequenced after U0′ and SL-73, together with the flat else-if chain. The user rules on the shape.\n","id":"SL-380","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":3,"sequence":1455,"stage":"backlog","status":"open","title":"A flat 400-term binary chain (1 + 1 + ... + 1) is an ICE: maximum recursion depth exceeded — left-deep chains (operators, method chains, else-if) are walked recursively by every pass","updated":"1790265130"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"SCOPE (user, Sep 24; recorded by the lead): option (b), flat representation, applies to the chains that are NOT bounded by type depth and do occur in real or generated code:\n- operator chains (&&/||, arithmetic, ??);\n- else-if chains, one arm list including `else if let` arms, charged once per chain.\nPostfix chains stay nested:\n- f()()() needs a function type nested per call, and type nesting is capped at 32;\n- a.b.c and a[i][j] need nested struct/array types;\n- builder method chains and cast chains.\nThe user's words: \"those are contrived examples which would probably never happen in real code so if they have different behaviour, its probably fine (unless the linearization is free)\". Linearization of postfix chains is not free: they mix member, method, index, optional-hop and cast node kinds.\n\nMeasured on main (e62b3164), .build/scratch/probe_chain_kinds.py in the lead checkout:\n\n| chain | 100 | 300 | 1000 |\n|---|---|---|---|\n| && chain | ok | ICE | ICE |\n| else if chain | ok | ICE (Identifier) | raw RecursionError traceback in the parser |\n| builder method chain | ok | ok | ICE |\n| as-cast chain | ok | ok | ICE |\n| flat statements, with and without yield_now | ok | ok | ok |\n\n?? and String + also ICE at 1000; at 100 and 300 the probe itself had type errors.\n\nOpen for the brief: whatever stays nested must fail with a clean diagnostic, never an ICE. The candidates are charging each postfix hop against the design-259 depth budget (parser-only; a deliberate policy, since codex's SL-2 c24 warns against charging flat operators), or accepting U0′'s raised recursion ceiling. The user rules on this when the brief is written, after U0′ and SL-73.\n","created":"1790264558","id":"c1"}],"created":"1790264003","description":"Measured on main (e62b3164) with `.build/scratch/probe_flat_chain.py` in the lead checkout. The program is `let x = 1 + 1 + … + 1`, then `print(\"{}\", x)`:\n\n| terms | result |\n|---|---|\n| 100, 200, 300 | compiles; prints the right sum |\n| 400 | `internal compiler error at …:2:291 (BinaryOp): maximum recursion depth exceeded` |\n| 500, 2000, 5000, 12000 | the same ICE |\n\nA left-associative chain parses iteratively and has constant source nesting, but its AST is a left spine as deep as the chain is long. Every pass that recurses over expressions pays at least one Python frame per term, and some pay several. The interpreter's default limit of about 1000 frames runs out somewhere between 300 and 400 terms. This is an ICE on an accepted program, not a clean diagnostic, so it violates \"never hide errors\" and the fuzz oracle's contract.\n\nContext:\n- SL-2.p2 (U0′) raises the interpreter limit to 10,192 the first time the parser's depth funnel charges a level. That moves this threshold for most real files, but not for a file whose parse never enters `nested()`. codex's c24 on SL-2 also measured a 12,000-term chain overflowing U0′'s own parser tree walks, which U0′ r3 makes iterative.\n- The same shape: an `else if` chain (the If → else Block → If nesting) and long method or optional chains. The user flagged the 256-arm `else if` limit on Sep 24 as undesirable, and the lead proposed a flat representation for that chain.\n\nMechanism (obligation 4): a left-deep representation of a flat source construct, walked recursively by passes that assume tree depth tracks source nesting. Sweep:\n- binary chains in every operator tier (`+`, `&&`, `??`, comparison chains where they are legal);\n- postfix method, member and optional chains (`a.f().g()…`);\n- `else if` chains.\nEach is measured end to end for the length at which the compiler fails, and the pass that fails is named.\n\nFix shape, needs a design decision:\n- (a) Walk chains iteratively in each pass. That is many passes, and each new pass reintroduces the risk.\n- (b) Represent associative chains flat: an n-ary operand list and a flat else-if arm list, so that tree depth equals source nesting. Passes then iterate lists. This is the larger change, and the durable one.\n- (c) A pass-agnostic guard: a structural chain-length limit with a clean diagnostic. Honest, but a real program can hit it.\n\nThe lead's recommendation is (b), sequenced after U0′ and SL-73, together with the flat else-if chain. The user rules on the shape.\n","id":"SL-380","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":2,"sequence":1454,"stage":"backlog","status":"open","title":"A flat 400-term binary chain (1 + 1 + ... + 1) is an ICE: maximum recursion depth exceeded — left-deep chains (operators, method chains, else-if) are walked recursively by every pass","updated":"1790264558"},"id":"SL-380","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sawlang\",\"body\":\"USER RULING (Sep 24, relayed by the lead): a chain that stays nested is charged against the design-259 depth budget of 256. The user's words: \\\"i think a 256 depth limit is fine\\\".\\n\\nConcretely:\\n- Each postfix hop (call, member, index, optional hop, method call, `as` cast) in one chain charges one level. The hops do NOT release until the chain ends.\\n- The 257th hop is refused with the ordinary \\\"nesting exceeds the parser depth limit (256)\\\" diagnostic, anchored at that hop.\\n- A chain inside an argument list or group adds to the surrounding depth, as every other charge does.\\n\\nThis is a parser-only change to the depth funnel. It lands as a unit of this epic, not in U0′ r3, so the U0′ review and the M21 alignment built on it are not reopened. The M21 prototype adopts the same rule in its follow-up.\\n\",\"id\":\"SL-380\",\"request_id\":\"pid-95885-1790265130-6000\"}","request_id":"pid-95885-1790265130-6000","sequence":1455,"timestamp":"1790265130"}
---
