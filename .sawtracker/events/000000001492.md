---
{"action":"patch_review","actor":"agent:codex","after":{"acceptance":[],"assignee":"agent:claude-sl340","author":"agent:claude-sl318u3","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 3 in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188483","id":"c1"},{"author":"agent:claude-sawlang","body":"Scope widened by the lead (Sep 24), after the implementer's census and a lead reproduction on main:\n- Nominal alias values over an owning type never drop. With type Tag = Noisy, neither Tag(Noisy(...)) nor Tag(move n) prints a drop. This is the same two halves as the cell. The alias construction also skips _check_value_transfer, and the drop glue does not resolve the alias.\n- One predicate, 'the type whose lifecycle stands for this one' (the cell's payload, the alias's underlying type), now answers drop, retain, release, copy and _needs_cleanup.\n- Codegen side of half 1: calls._generate_function_call lowers both intercepted arguments with _generate_expression rather than _gen_transfer_value. It gets a one-line fix per arm, approved; the SL-355 receiver-arm hunks are elsewhere in calls.py.\n- UnsafeMemory.write is routed like ptr[i] = v.\nFiled out of scope: SL-378 (placement write through a non-identifier pointer drops its source twice; Once.set has the shape) and SL-379 (UnsafeMemory.read returns an unretained alias; needs a ruling).\n","created":"1790262880","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-340.p1 (revision 1): A cell or alias construction is a value transfer, and a cell or alias drops, retains and copies as the type it stands for (Mutex use-after-free + cell/alias payload leak)","created":"1790273293","id":"c3"},{"author":"agent:claude-sawlang","body":"SL-340.p1 submitted: one commit fb4e0afe on origin/main. The brief is in the description; the scope was widened to aliases in c2.\n\nMechanism as found in the code:\n- Half 1, checker. Three intercepted constructions called only `_transfer_compatible`, never `_check_value_transfer`: the `UnsafeMutableInterior` arm of `_check_function_call`, `_check_alias_construction`, and the `write` arm of `_check_um_method`. All three now call it. They are listed as entries 21-23 of the funnel docstring in typechecker/types.py.\n- Half 1, codegen. `calls._generate_function_call` lowered the cell and alias arguments with `_generate_expression`, and `_generate_um_method` did the same for `write`. Nothing retired the source or retained a Copy-tier value. All three now use `_gen_transfer_value`. These are the only calls.py changes; SL-355's calls.py hunks are in other functions.\n- Half 2. There is a new `resources._lifecycle_type`, with ENTRY POINTS: `_get_cleanup_behavior`, `_needs_cleanup`, `_emit_drop_at`, `_emit_retain_at`, `_emit_release_at` and `_emit_copy_value`. It resolves a layout-transparent wrapper to its payload and a distinct alias to its underlying type, so drop, retain and copy answer from one place. An alias cannot carry a lifecycle of its own: an alias conformance is refused by the orphan rule, which the lead checked on both compilers. So resolving through the alias skips nothing.\n- std: `Mutex.init` and `SpinLock.init` spell `move value`.\n\nCensus of intercepted constructions:\n- Routed: `UnsafeMutableInterior(x)`, `Alias(x)`, `UnsafeMemory.write(v)`.\n- Integer-only, so not routed: `Atomic(x)`, `UnsafeMemory(addr)`, `E.from(raw:)`.\n- `mod.Alias(x)` is refused outright.\n\nCorpus sweep: nothing is newly refused beyond the two std inits. `Once.set` is untouched; its placement write is SL-378's shape, and `Once` cannot hold an owning T today because statics refuse Deinit payloads.\n\nConformance rows K170-K179, from the Studio range. Every row fails on origin/main (SIGSEGV, garbage reads, drops at construction, or no drop at all) and passes on the branch:\n- K170 Mutex; K171 SpinLock; K172 Once (a static refusal pin); K173 user cells;\n- K174 nested positions; K175 driven; K176 refusals (three errors files);\n- K177 transfer accept-side; K178 aliases; K179 trivial controls.\nK141's XFAIL marker is removed.\n\nGates:\n- Implementer's full battery: 33/33 green, 4389s (the dispatch predates the Sep 24 targeted-tests ruling). suite 2674 passed / 9 xfailed; transferdecisions, astgraft, closurestate, irdet, reemit, gmgate, bootstrap and freestanding all ok.\n- Lead spot checks on the tip (under the lock): 33 targeted pins pass, plus transferdecisions and astgraft green.\n- The lead's original cells now print correctly. sl340_probe gives `first 1`, with `drop kept` once at end of main. The cell payload drops in sl340_drop2, and both alias values drop.\n\nFiled from the sweep: SL-381, SL-382, SL-383 and SL-384.\n\nDesign note, not a bug: cells have no copy side. A container of a cell over a Copy-tier payload cannot derive Copy, because the cell is NoCopy. So no compiler-inserted copy ever duplicates a cell, and the predicate's retain half is exercised through aliases only (K178).\n","created":"1790273309","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-340.p1 (revision 1)","created":"1790277064","id":"c5"},{"author":"agent:codex","body":"Requested changes on patch SL-340.p1 (revision 1)\n\nREQUEST CHANGES — SL-340.p1 r1\n\nExact revision: base ab71c7d6, applied 6d3025be, reconstructed tree b24445bddce5d108c4e821f451197e9d3acc011c. Two independent Sol source reviews covered lifecycle/codegen and checker/std/conformance. Parent adjudicated their findings and ran new bounded probes on this exact fixed-SL350-descendant tree. Author c4's battery/targeted evidence accepted without repeating it. Server gate was pending at review.\n\nP1 — lifecycle normalization erases the cell's declared NoCopy policy during generic forwarding.\n\nsawc/codegen/resources.py:195-208 peels UnsafeMutableInterior<T> to T, and _get_cleanup_behavior applies that normalization at :229 before consulting conformances. A direct UnsafeMutableInterior value is explicitly NoCopy (builtin.saw:389-393; namespace.py:2097-2143). The checker preserves that nominal policy, but a generic body's transfer remains deferred until instantiation; _transfer_needs_copy at resources.py:1693-1709 re-derives it through _get_cleanup_behavior. For a cell over a Copy payload, the new code answers implicit_copy and calls the payload's copy hook instead of moving the NoCopy cell.\n\nParent compiled and ran this bounded program (trivial Int payload; no UAF/crash path):\n\nstruct Loud { n: Int }\nextension Loud: Copy {\n    func copy(&self) -> Loud {\n        print(\"copy {}\", self.n)\n        Loud(n: self.n + 1)\n    }\n    func deinit(&var self) { print(\"drop {}\", self.n) }\n}\nfunc hand<T>(x: T) unsafe -> T { x }\nfunc main() unsafe {\n    let c = UnsafeMutableInterior(Loud(n: 1))\n    let out = hand(move c)\n    print(\"value {}\", out.ptr()[0].n)\n    print(\"scope end\")\n}\n\nActual, exit 0:\n  copy 1\n  drop 1\n  value 2\n  scope end\n  drop 2\n\nRequired for a NoCopy cell transfer: no copy-hook call, value 1, one final drop of 1. The generic's unsafe declaration is required because its instantiated signature names the unsafe cell type; the probe obeys that requirement. This is an observable wrong transfer, not just unnecessary work. A payload's representation/lifecycle glue can stand for the cell's storage without overriding the cell's nominal copying policy. Keep those questions separate, particularly at the generic transfer fallback. Existing K173 forwards Noisy as T rather than a cell as T, so it misses this consumer.\n\nNonblocking coverage/report corrections:\n- K176's ledger says all three intercepted constructions cover both NoCopy and ExplicitCopy refusal. unsafe_memory_write_is_a_transfer.saw:14-18 covers only NoCopy. alias_construction_is_a_transfer.saw repeats the same expected diagnostic for local/parameter cases; test_runner.py:1447-1452 tests membership, so one error satisfies both. Use distinguishable refusal cells for the claimed matrix rather than duplicate expectations.\n- The owning Once set/get/drop acceptance is blocked by the pre-existing static-payload refusal, as c4 itself states. K172 proves that refusal, not owning-Once lifecycle. Likewise K179 is a control. Do not read c4's blanket 'every row fails on main' as evidence for these unchanged/refusal rows. No request to expand Once or fix SL-378 under this review.\n\nParent-rejected hypotheses (not blockers):\n- A reviewer proposed that the lifecycle resolver's 32-hop bound silently leaks a 33-deep cell. Actual depth32 probe prints `scope end` then `drop 7`; depth33 is CLEANLY REFUSED by the existing borrowing-struct value fence before codegen. Follow-up source review confirms alias and mixed chains follow the same fence. The reachable-leak finding is withdrawn; no demand to change the cap on that evidence.\n- A reviewer proposed missing positive coverage for automatic/derived Copy of a struct containing a cell. Actual current compiler refuses both: an undeclared wrapper needs Copy, and @synthesize Copy refuses the NoCopy field. The author's stated current limitation is therefore accurate for these shapes. Namespace policy versus checker behavior is an inherited discrepancy, not a new patch regression or a reason to narrow the language's approved Copy policy. No positive cell-field-copy run is claimed.\n\nOther source conclusions: the three intercepted checker/codegen pairs route cell, alias and UnsafeMemory.write through the transfer checkpoint/lowering once. Mutex/SpinLock inits both move their parameters. No missed owning corpus migration found. For reachable types, alias/transparent storage has the payload LLVM layout; the same-address drop glue is ABI-compatible. UnsafeMemory.write is raw placement and intentionally does not destroy old contents. Owning aliases and ordinary lock/cell paths have substantive existing author coverage.\n\nEvidence: local://sl340-new-lifecycle-smokes.json and archived probe sources. New execution was limited to the trivial generic-cell miscompile and the safe depth32 destruction control; the depth33 and field-copy cases stopped on clean diagnostics. No old compiler, known UAF/SIGSEGV, full suite, freestanding, battery, formatter or implementation edits.\n\nFreeze m176 respected: this withholds approval and records the defect; it does not authorize a new compiler revision round or merge.\n","created":"1790277221","id":"c6"}],"created":"1790049564","description":"USE-AFTER-FREE: `Mutex(value: move v)` at a heap-owning payload FREES the payload at construction. Found on MAIN (`da84578e`) while building design 275 U3's ruled `Mutex` conformance row.\n\nREPRO (main, exit 0, no diagnostic):\n\n```saw\nimport std.mutex.{Mutex}\n\nfunc main() {\n    var v: Vector<Int> = [1, 2, 3]\n    let guarded = Mutex(value: move v)\n    print(guarded.lock({ items in items[0] }))   // prints garbage\n}\n```\n\n`items.len()` reads 3 — the length word survives — and `items[0]` reads through a buffer pointer whose allocation was already released, so the value is whatever the allocator left there. Nondeterministic across runs.\n\nMECHANISM (obligation 4). `Mutex<T>.init(value: T)` is\n\n```saw\npublic init(value: T) unsafe -> Mutex<T> {\n    Mutex<T>(state: UnsafeMutableInterior(0),\n             value: UnsafeMutableInterior(value))\n}\n```\n\nand the emitted `Mutex_init_value` reads:\n\n    %value.1 = alloca %Vector\n    store %Vector %value, %Vector* %value.1\n    %value.dropflag = alloca i1\n    store i1 1, i1* %value.dropflag          ; design 42: registered for cleanup\n    %value.2 = load %Vector, %Vector* %value.1\n    %.5 = insertvalue %Mutex undef, i64 0, 0\n    %.6 = insertvalue %Mutex %.5, %Vector %value.2, 1     ; the payload moves IN\n    %value.needsdrop = load i1, i1* %value.dropflag        ; …and the flag is STILL 1\n    br i1 %value.needsdrop, label %drop.value, ...\n    drop.value:\n      call void @Vector_deinit(%Vector* %value.1)          ; the buffer is freed here\n    ret %Mutex %.6                                         ; …and returned anyway\n\nAn `init`'s by-value parameter is registered for scope-exit cleanup and the flag is cleared by whatever CONSUMES it. Here the consumer is `UnsafeMutableInterior(value)` — a cell CONSTRUCTOR whose argument is the payload — and the transfer into it is not recognised as the move that retires the parameter, so the parameter is released at the end of the init body while its bytes have already been copied into the returned struct.\n\nCLASS — every constructor whose parameter is consumed only by an `UnsafeMutableInterior(...)` construction, at every heap-owning payload:\n- `Mutex<Vector<Int>>`, `Mutex<Data>`, `Mutex<String>`, `Mutex<Map<..>>` — measured on `Vector`;\n- `Once<T>`'s `set` path and `SpinLock<T>`'s own initializer are the neighbours to probe (they take the same shape);\n- a USER type following the design-186 interior-mutability idiom (`struct Counter { cell: UnsafeMutableInterior<Int> }` with an `init`) is the same shape, and is only invisible at `Int` because a trivial payload has no `deinit` to run.\nA payload with no `deinit` (`Mutex<Int>`, `Mutex<Bool>`) is unaffected, which is why the whole corpus passes: every `Mutex` in the tree guards a trivial or refcount-free payload.\n\nWHY IT SURFACED NOW: design 275 U3's ruled conformance row is \"a `for` over the guarded collection inside `lock` is a sync window and works\" — the first program in the tree to put a heap-owning container inside a `Mutex`.\n\nPIN: `examples/conformance/K141_mutex_guarded_for_is_sync.saw` (XFAIL, citing this issue). Its XPASS flip is the validation; the paired REFUSAL half (a suspension inside a `lock` body is refused by the `sync`-body rule) is `examples/errors/mutex_guarded_for_suspends.saw` and passes today.\n\n\n---\n\n# BRIEF (lead, Sep 24): READY\n\n## Re-measured on main\nCells are in the lead checkout at `.build/scratch/sl340_probe.saw`, `sl340_drop.saw`, `sl340_drop2.saw` and `sl340_ctrl.saw`.\n- `Mutex(value: move v)` with a `Vector<Int>`: `items[0]` reads garbage on 1 run in 3. This is the finding, and it still reproduces.\n- `Mutex(value: Noisy(name: \"kept\"))`: `drop kept` prints AT CONSTRUCTION, before the first `lock`, and nothing prints when the Mutex dies.\n- **A second, hidden half: a cell NEVER drops its payload.** A user `struct Holder { plain: Noisy, cell: UnsafeMutableInterior<Noisy> }` going out of scope prints `drop plain field` and nothing for the cell. This holds whether the payload came from an rvalue, a by-value parameter, or `move` of a local. Every heap-owning cell payload leaks.\n- **The two halves hide each other today.** Construction from a parameter frees the payload early, and the owner never frees it again, so nothing double-frees. Fixing either half alone turns the bug into a leak or a double free. **Both land in one patch.**\n\n## Mechanism (obligation 4), confirmed in code\n\n**Half 1: the intercepted construction bypasses the value-transfer checkpoint.**\n- Control: an ordinary call `sink(value)` with a NoCopy parameter is refused: \"cannot copy value of type `Noisy` which implements NoCopy; use `move`\".\n- `UnsafeMutableInterior(value)` with the same parameter is accepted silently. Its interception in `typechecker/expressions.py` (the `expr.name == \"UnsafeMutableInterior\"` branch, which stamps `is_interior_cell_construct`) checks only `_transfer_compatible`, never `_check_value_transfer`.\n- So the argument is neither refused as a copy nor recorded as moved. Codegen never clears the parameter's drop flag, and the parameter is released at scope exit after its bytes were copied into the cell.\n- This is the DF-216a class: a compiler-intercepted construction that skips the transfer funnel.\n\n**Half 2: the drop glue has no path for a layout-transparent wrapper.**\n- `UnsafeMutableInterior<T>` lowers to a bare `T` (`type_identity.is_layout_transparent`). It deliberately gets no monomorphized `deinit`, because a method taking the wrapper's address could never be called with the field's address.\n- `_emit_drop_at` (`codegen/resources.py`) therefore finds no deinit symbol, falls to the struct-field path, and drops nothing.\n- The right drop of a transparent wrapper is its payload's drop at the same address. The same holds for `_needs_cleanup`, and for the copy/retain paths wherever a transparent wrapper's payload is refcounted or Copy-tier. Answer all of these through ONE predicate, the payload's own, so the three places cannot disagree.\n\n## The fix\n1. **Construction.** Every compiler-intercepted construction whose argument is a VALUE routes that argument through `_check_value_transfer`, exactly as a call argument does:\n   - a NoCopy/ExplicitCopy argument must be written `move x` (or `.copy()`);\n   - a moved one is recorded as moved, so codegen clears its flag.\n   Enumerate the intercepted constructions (the `is_*_construct` stamps: `Atomic(...)`, `UnsafeMutableInterior(...)`, `UnsafeMemory(...)`, plus the alias back-conversion `UserId(i)`, `E.from(raw:)` and `UnsafeMemory.write` that `_transfer_compatible`'s docstring names). Probe each with an owning argument wherever the type admits one: an alias of `String`/`Vector`, and `UnsafeMemory.write` of a non-trivial `T` if legal. Route every one that can carry an owning value; record WHY each of the others cannot.\n2. **Drop.** A layout-transparent wrapper drops, retains and copies as its payload does, at the same address, and `_needs_cleanup` answers through the payload. Route this through one place, and name its entry points in the docstring.\n3. **Std migration (the consumer sweep).** Once item 1 lands, `Mutex.init`, `SpinLock`'s initializer and any other `UnsafeMutableInterior(value)` over a generic or owning `T` must spell `move value`.\n   - Grep every `UnsafeMutableInterior(` and `Atomic(` construction in sawc/std, blade/, libs/, devtools/, selfhost/, prototypes/ and examples/.\n   - Each is either migrated to `move` (a by-value owner) or already fine (a literal or a trivial type). List them in the report.\n   - `Once.set` writes through the pointer (`self.value.ptr()[0] = value`). Probe it: `Once<Vector<Int>>` set, get, then drop, with the drop count. Also check whether that assignment drops a zero-initialized old value (a hazard for a zeroed `String`/`Vector` slot).\n\n## Conformance (obligation 3) and position matrix\nConformance rows come FIRST, from the Studio's range **K170-K179** (the Air holds K154 and K160-K169). They go beside the existing `K141_mutex_guarded_for_is_sync.saw`, which is XFAIL citing this issue: remove that marker in this patch, because its XPASS is the validation.\n\nEvery row counts drops with a printing deinit and requires EXACTLY ONE drop, at the owner's end:\n1. `Mutex(value: move v)` over `Vector<Int>`: reads are correct, and the buffer is freed once when the Mutex dies. Use an ASan build or a drop-counting payload.\n2. `Mutex<Noisy>`, from an rvalue and from a moved local.\n3. `SpinLock<T>` with a heap-owning payload, both sources.\n4. `Once<T>` with a heap-owning payload: set, get (via `.copy()`), then drop.\n5. A user cell struct (`Holder` above), from an rvalue, a by-value parameter and a moved local.\n6. A cell nested as an `Optional` payload, an enum payload, a fixed-array element and a generic struct field.\n7. A Mutex held across a `yield_now()` in a driven body, dropped once. This is the frame twin.\n8. REFUSAL: `UnsafeMutableInterior(value)` with a NoCopy/ExplicitCopy parameter and no `move` → \"cannot copy … use `move`\", pinned in examples/errors/.\n9. CONTROL: `Mutex<Int>`, `Atomic(0)` and `static` Mutex/SpinLock are unchanged. The all-zero static costs no image bytes and never drops.\n\n## Gates and files\n- Full terminal battery. `transferdecisions` is live again: a new writer of `needs_copy`/`payload_needs_copy` must go through `carry_retain_stamps`, so check it.\n- Files: `sawc/typechecker/expressions.py` (the interception branches), `sawc/codegen/resources.py` (drop glue plus `_needs_cleanup`), possibly `sawc/codegen/structs.py`/`generics.py` for retain/copy, `sawc/std/{mutex,spinlock,once}.saw`, `examples/conformance/`, `examples/errors/`.\n- This does not overlap SL-345 (the Air's call-exclusivity funnel and closure checker) or SL-355's `calls.py`/`coro_transform.py` hunks. If the retain path turns out to need `calls.py`, stop and report before editing it.\n","id":"SL-340","labels":["bug","design-275"],"order":3,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":12,"sequence":1492,"stage":"queued","status":"open","title":"USE-AFTER-FREE: Mutex(value: move v) frees a heap-owning payload at construction — an UnsafeMutableInterior(...) argument does not retire the init parameter","updated":"1790277221"},"before":{"acceptance":[],"assignee":"agent:claude-sl340","author":"agent:claude-sl318u3","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 3 in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188483","id":"c1"},{"author":"agent:claude-sawlang","body":"Scope widened by the lead (Sep 24), after the implementer's census and a lead reproduction on main:\n- Nominal alias values over an owning type never drop. With type Tag = Noisy, neither Tag(Noisy(...)) nor Tag(move n) prints a drop. This is the same two halves as the cell. The alias construction also skips _check_value_transfer, and the drop glue does not resolve the alias.\n- One predicate, 'the type whose lifecycle stands for this one' (the cell's payload, the alias's underlying type), now answers drop, retain, release, copy and _needs_cleanup.\n- Codegen side of half 1: calls._generate_function_call lowers both intercepted arguments with _generate_expression rather than _gen_transfer_value. It gets a one-line fix per arm, approved; the SL-355 receiver-arm hunks are elsewhere in calls.py.\n- UnsafeMemory.write is routed like ptr[i] = v.\nFiled out of scope: SL-378 (placement write through a non-identifier pointer drops its source twice; Once.set has the shape) and SL-379 (UnsafeMemory.read returns an unretained alias; needs a ruling).\n","created":"1790262880","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-340.p1 (revision 1): A cell or alias construction is a value transfer, and a cell or alias drops, retains and copies as the type it stands for (Mutex use-after-free + cell/alias payload leak)","created":"1790273293","id":"c3"},{"author":"agent:claude-sawlang","body":"SL-340.p1 submitted: one commit fb4e0afe on origin/main. The brief is in the description; the scope was widened to aliases in c2.\n\nMechanism as found in the code:\n- Half 1, checker. Three intercepted constructions called only `_transfer_compatible`, never `_check_value_transfer`: the `UnsafeMutableInterior` arm of `_check_function_call`, `_check_alias_construction`, and the `write` arm of `_check_um_method`. All three now call it. They are listed as entries 21-23 of the funnel docstring in typechecker/types.py.\n- Half 1, codegen. `calls._generate_function_call` lowered the cell and alias arguments with `_generate_expression`, and `_generate_um_method` did the same for `write`. Nothing retired the source or retained a Copy-tier value. All three now use `_gen_transfer_value`. These are the only calls.py changes; SL-355's calls.py hunks are in other functions.\n- Half 2. There is a new `resources._lifecycle_type`, with ENTRY POINTS: `_get_cleanup_behavior`, `_needs_cleanup`, `_emit_drop_at`, `_emit_retain_at`, `_emit_release_at` and `_emit_copy_value`. It resolves a layout-transparent wrapper to its payload and a distinct alias to its underlying type, so drop, retain and copy answer from one place. An alias cannot carry a lifecycle of its own: an alias conformance is refused by the orphan rule, which the lead checked on both compilers. So resolving through the alias skips nothing.\n- std: `Mutex.init` and `SpinLock.init` spell `move value`.\n\nCensus of intercepted constructions:\n- Routed: `UnsafeMutableInterior(x)`, `Alias(x)`, `UnsafeMemory.write(v)`.\n- Integer-only, so not routed: `Atomic(x)`, `UnsafeMemory(addr)`, `E.from(raw:)`.\n- `mod.Alias(x)` is refused outright.\n\nCorpus sweep: nothing is newly refused beyond the two std inits. `Once.set` is untouched; its placement write is SL-378's shape, and `Once` cannot hold an owning T today because statics refuse Deinit payloads.\n\nConformance rows K170-K179, from the Studio range. Every row fails on origin/main (SIGSEGV, garbage reads, drops at construction, or no drop at all) and passes on the branch:\n- K170 Mutex; K171 SpinLock; K172 Once (a static refusal pin); K173 user cells;\n- K174 nested positions; K175 driven; K176 refusals (three errors files);\n- K177 transfer accept-side; K178 aliases; K179 trivial controls.\nK141's XFAIL marker is removed.\n\nGates:\n- Implementer's full battery: 33/33 green, 4389s (the dispatch predates the Sep 24 targeted-tests ruling). suite 2674 passed / 9 xfailed; transferdecisions, astgraft, closurestate, irdet, reemit, gmgate, bootstrap and freestanding all ok.\n- Lead spot checks on the tip (under the lock): 33 targeted pins pass, plus transferdecisions and astgraft green.\n- The lead's original cells now print correctly. sl340_probe gives `first 1`, with `drop kept` once at end of main. The cell payload drops in sl340_drop2, and both alias values drop.\n\nFiled from the sweep: SL-381, SL-382, SL-383 and SL-384.\n\nDesign note, not a bug: cells have no copy side. A container of a cell over a Copy-tier payload cannot derive Copy, because the cell is NoCopy. So no compiler-inserted copy ever duplicates a cell, and the predicate's retain half is exercised through aliases only (K178).\n","created":"1790273309","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-340.p1 (revision 1)","created":"1790277064","id":"c5"}],"created":"1790049564","description":"USE-AFTER-FREE: `Mutex(value: move v)` at a heap-owning payload FREES the payload at construction. Found on MAIN (`da84578e`) while building design 275 U3's ruled `Mutex` conformance row.\n\nREPRO (main, exit 0, no diagnostic):\n\n```saw\nimport std.mutex.{Mutex}\n\nfunc main() {\n    var v: Vector<Int> = [1, 2, 3]\n    let guarded = Mutex(value: move v)\n    print(guarded.lock({ items in items[0] }))   // prints garbage\n}\n```\n\n`items.len()` reads 3 — the length word survives — and `items[0]` reads through a buffer pointer whose allocation was already released, so the value is whatever the allocator left there. Nondeterministic across runs.\n\nMECHANISM (obligation 4). `Mutex<T>.init(value: T)` is\n\n```saw\npublic init(value: T) unsafe -> Mutex<T> {\n    Mutex<T>(state: UnsafeMutableInterior(0),\n             value: UnsafeMutableInterior(value))\n}\n```\n\nand the emitted `Mutex_init_value` reads:\n\n    %value.1 = alloca %Vector\n    store %Vector %value, %Vector* %value.1\n    %value.dropflag = alloca i1\n    store i1 1, i1* %value.dropflag          ; design 42: registered for cleanup\n    %value.2 = load %Vector, %Vector* %value.1\n    %.5 = insertvalue %Mutex undef, i64 0, 0\n    %.6 = insertvalue %Mutex %.5, %Vector %value.2, 1     ; the payload moves IN\n    %value.needsdrop = load i1, i1* %value.dropflag        ; …and the flag is STILL 1\n    br i1 %value.needsdrop, label %drop.value, ...\n    drop.value:\n      call void @Vector_deinit(%Vector* %value.1)          ; the buffer is freed here\n    ret %Mutex %.6                                         ; …and returned anyway\n\nAn `init`'s by-value parameter is registered for scope-exit cleanup and the flag is cleared by whatever CONSUMES it. Here the consumer is `UnsafeMutableInterior(value)` — a cell CONSTRUCTOR whose argument is the payload — and the transfer into it is not recognised as the move that retires the parameter, so the parameter is released at the end of the init body while its bytes have already been copied into the returned struct.\n\nCLASS — every constructor whose parameter is consumed only by an `UnsafeMutableInterior(...)` construction, at every heap-owning payload:\n- `Mutex<Vector<Int>>`, `Mutex<Data>`, `Mutex<String>`, `Mutex<Map<..>>` — measured on `Vector`;\n- `Once<T>`'s `set` path and `SpinLock<T>`'s own initializer are the neighbours to probe (they take the same shape);\n- a USER type following the design-186 interior-mutability idiom (`struct Counter { cell: UnsafeMutableInterior<Int> }` with an `init`) is the same shape, and is only invisible at `Int` because a trivial payload has no `deinit` to run.\nA payload with no `deinit` (`Mutex<Int>`, `Mutex<Bool>`) is unaffected, which is why the whole corpus passes: every `Mutex` in the tree guards a trivial or refcount-free payload.\n\nWHY IT SURFACED NOW: design 275 U3's ruled conformance row is \"a `for` over the guarded collection inside `lock` is a sync window and works\" — the first program in the tree to put a heap-owning container inside a `Mutex`.\n\nPIN: `examples/conformance/K141_mutex_guarded_for_is_sync.saw` (XFAIL, citing this issue). Its XPASS flip is the validation; the paired REFUSAL half (a suspension inside a `lock` body is refused by the `sync`-body rule) is `examples/errors/mutex_guarded_for_suspends.saw` and passes today.\n\n\n---\n\n# BRIEF (lead, Sep 24): READY\n\n## Re-measured on main\nCells are in the lead checkout at `.build/scratch/sl340_probe.saw`, `sl340_drop.saw`, `sl340_drop2.saw` and `sl340_ctrl.saw`.\n- `Mutex(value: move v)` with a `Vector<Int>`: `items[0]` reads garbage on 1 run in 3. This is the finding, and it still reproduces.\n- `Mutex(value: Noisy(name: \"kept\"))`: `drop kept` prints AT CONSTRUCTION, before the first `lock`, and nothing prints when the Mutex dies.\n- **A second, hidden half: a cell NEVER drops its payload.** A user `struct Holder { plain: Noisy, cell: UnsafeMutableInterior<Noisy> }` going out of scope prints `drop plain field` and nothing for the cell. This holds whether the payload came from an rvalue, a by-value parameter, or `move` of a local. Every heap-owning cell payload leaks.\n- **The two halves hide each other today.** Construction from a parameter frees the payload early, and the owner never frees it again, so nothing double-frees. Fixing either half alone turns the bug into a leak or a double free. **Both land in one patch.**\n\n## Mechanism (obligation 4), confirmed in code\n\n**Half 1: the intercepted construction bypasses the value-transfer checkpoint.**\n- Control: an ordinary call `sink(value)` with a NoCopy parameter is refused: \"cannot copy value of type `Noisy` which implements NoCopy; use `move`\".\n- `UnsafeMutableInterior(value)` with the same parameter is accepted silently. Its interception in `typechecker/expressions.py` (the `expr.name == \"UnsafeMutableInterior\"` branch, which stamps `is_interior_cell_construct`) checks only `_transfer_compatible`, never `_check_value_transfer`.\n- So the argument is neither refused as a copy nor recorded as moved. Codegen never clears the parameter's drop flag, and the parameter is released at scope exit after its bytes were copied into the cell.\n- This is the DF-216a class: a compiler-intercepted construction that skips the transfer funnel.\n\n**Half 2: the drop glue has no path for a layout-transparent wrapper.**\n- `UnsafeMutableInterior<T>` lowers to a bare `T` (`type_identity.is_layout_transparent`). It deliberately gets no monomorphized `deinit`, because a method taking the wrapper's address could never be called with the field's address.\n- `_emit_drop_at` (`codegen/resources.py`) therefore finds no deinit symbol, falls to the struct-field path, and drops nothing.\n- The right drop of a transparent wrapper is its payload's drop at the same address. The same holds for `_needs_cleanup`, and for the copy/retain paths wherever a transparent wrapper's payload is refcounted or Copy-tier. Answer all of these through ONE predicate, the payload's own, so the three places cannot disagree.\n\n## The fix\n1. **Construction.** Every compiler-intercepted construction whose argument is a VALUE routes that argument through `_check_value_transfer`, exactly as a call argument does:\n   - a NoCopy/ExplicitCopy argument must be written `move x` (or `.copy()`);\n   - a moved one is recorded as moved, so codegen clears its flag.\n   Enumerate the intercepted constructions (the `is_*_construct` stamps: `Atomic(...)`, `UnsafeMutableInterior(...)`, `UnsafeMemory(...)`, plus the alias back-conversion `UserId(i)`, `E.from(raw:)` and `UnsafeMemory.write` that `_transfer_compatible`'s docstring names). Probe each with an owning argument wherever the type admits one: an alias of `String`/`Vector`, and `UnsafeMemory.write` of a non-trivial `T` if legal. Route every one that can carry an owning value; record WHY each of the others cannot.\n2. **Drop.** A layout-transparent wrapper drops, retains and copies as its payload does, at the same address, and `_needs_cleanup` answers through the payload. Route this through one place, and name its entry points in the docstring.\n3. **Std migration (the consumer sweep).** Once item 1 lands, `Mutex.init`, `SpinLock`'s initializer and any other `UnsafeMutableInterior(value)` over a generic or owning `T` must spell `move value`.\n   - Grep every `UnsafeMutableInterior(` and `Atomic(` construction in sawc/std, blade/, libs/, devtools/, selfhost/, prototypes/ and examples/.\n   - Each is either migrated to `move` (a by-value owner) or already fine (a literal or a trivial type). List them in the report.\n   - `Once.set` writes through the pointer (`self.value.ptr()[0] = value`). Probe it: `Once<Vector<Int>>` set, get, then drop, with the drop count. Also check whether that assignment drops a zero-initialized old value (a hazard for a zeroed `String`/`Vector` slot).\n\n## Conformance (obligation 3) and position matrix\nConformance rows come FIRST, from the Studio's range **K170-K179** (the Air holds K154 and K160-K169). They go beside the existing `K141_mutex_guarded_for_is_sync.saw`, which is XFAIL citing this issue: remove that marker in this patch, because its XPASS is the validation.\n\nEvery row counts drops with a printing deinit and requires EXACTLY ONE drop, at the owner's end:\n1. `Mutex(value: move v)` over `Vector<Int>`: reads are correct, and the buffer is freed once when the Mutex dies. Use an ASan build or a drop-counting payload.\n2. `Mutex<Noisy>`, from an rvalue and from a moved local.\n3. `SpinLock<T>` with a heap-owning payload, both sources.\n4. `Once<T>` with a heap-owning payload: set, get (via `.copy()`), then drop.\n5. A user cell struct (`Holder` above), from an rvalue, a by-value parameter and a moved local.\n6. A cell nested as an `Optional` payload, an enum payload, a fixed-array element and a generic struct field.\n7. A Mutex held across a `yield_now()` in a driven body, dropped once. This is the frame twin.\n8. REFUSAL: `UnsafeMutableInterior(value)` with a NoCopy/ExplicitCopy parameter and no `move` → \"cannot copy … use `move`\", pinned in examples/errors/.\n9. CONTROL: `Mutex<Int>`, `Atomic(0)` and `static` Mutex/SpinLock are unchanged. The all-zero static costs no image bytes and never drops.\n\n## Gates and files\n- Full terminal battery. `transferdecisions` is live again: a new writer of `needs_copy`/`payload_needs_copy` must go through `carry_retain_stamps`, so check it.\n- Files: `sawc/typechecker/expressions.py` (the interception branches), `sawc/codegen/resources.py` (drop glue plus `_needs_cleanup`), possibly `sawc/codegen/structs.py`/`generics.py` for retain/copy, `sawc/std/{mutex,spinlock,once}.saw`, `examples/conformance/`, `examples/errors/`.\n- This does not overlap SL-345 (the Air's call-exclusivity funnel and closure checker) or SL-355's `calls.py`/`coro_transform.py` hunks. If the retain path turns out to need `calls.py`, stop and report before editing it.\n","id":"SL-340","labels":["bug","design-275"],"order":3,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":11,"sequence":1489,"stage":"queued","status":"open","title":"USE-AFTER-FREE: Mutex(value: move v) frees a heap-owning payload at construction — an UnsafeMutableInterior(...) argument does not retire the init parameter","updated":"1790277064"},"id":"SL-340","project":"SL","request":"{\"action\":\"patch_review\",\"actor\":\"agent:codex\",\"body\":\"REQUEST CHANGES — SL-340.p1 r1\\n\\nExact revision: base ab71c7d6, applied 6d3025be, reconstructed tree b24445bddce5d108c4e821f451197e9d3acc011c. Two independent Sol source reviews covered lifecycle/codegen and checker/std/conformance. Parent adjudicated their findings and ran new bounded probes on this exact fixed-SL350-descendant tree. Author c4's battery/targeted evidence accepted without repeating it. Server gate was pending at review.\\n\\nP1 — lifecycle normalization erases the cell's declared NoCopy policy during generic forwarding.\\n\\nsawc/codegen/resources.py:195-208 peels UnsafeMutableInterior<T> to T, and _get_cleanup_behavior applies that normalization at :229 before consulting conformances. A direct UnsafeMutableInterior value is explicitly NoCopy (builtin.saw:389-393; namespace.py:2097-2143). The checker preserves that nominal policy, but a generic body's transfer remains deferred until instantiation; _transfer_needs_copy at resources.py:1693-1709 re-derives it through _get_cleanup_behavior. For a cell over a Copy payload, the new code answers implicit_copy and calls the payload's copy hook instead of moving the NoCopy cell.\\n\\nParent compiled and ran this bounded program (trivial Int payload; no UAF/crash path):\\n\\nstruct Loud { n: Int }\\nextension Loud: Copy {\\n    func copy(&self) -> Loud {\\n        print(\\\"copy {}\\\", self.n)\\n        Loud(n: self.n + 1)\\n    }\\n    func deinit(&var self) { print(\\\"drop {}\\\", self.n) }\\n}\\nfunc hand<T>(x: T) unsafe -> T { x }\\nfunc main() unsafe {\\n    let c = UnsafeMutableInterior(Loud(n: 1))\\n    let out = hand(move c)\\n    print(\\\"value {}\\\", out.ptr()[0].n)\\n    print(\\\"scope end\\\")\\n}\\n\\nActual, exit 0:\\n  copy 1\\n  drop 1\\n  value 2\\n  scope end\\n  drop 2\\n\\nRequired for a NoCopy cell transfer: no copy-hook call, value 1, one final drop of 1. The generic's unsafe declaration is required because its instantiated signature names the unsafe cell type; the probe obeys that requirement. This is an observable wrong transfer, not just unnecessary work. A payload's representation/lifecycle glue can stand for the cell's storage without overriding the cell's nominal copying policy. Keep those questions separate, particularly at the generic transfer fallback. Existing K173 forwards Noisy as T rather than a cell as T, so it misses this consumer.\\n\\nNonblocking coverage/report corrections:\\n- K176's ledger says all three intercepted constructions cover both NoCopy and ExplicitCopy refusal. unsafe_memory_write_is_a_transfer.saw:14-18 covers only NoCopy. alias_construction_is_a_transfer.saw repeats the same expected diagnostic for local/parameter cases; test_runner.py:1447-1452 tests membership, so one error satisfies both. Use distinguishable refusal cells for the claimed matrix rather than duplicate expectations.\\n- The owning Once set/get/drop acceptance is blocked by the pre-existing static-payload refusal, as c4 itself states. K172 proves that refusal, not owning-Once lifecycle. Likewise K179 is a control. Do not read c4's blanket 'every row fails on main' as evidence for these unchanged/refusal rows. No request to expand Once or fix SL-378 under this review.\\n\\nParent-rejected hypotheses (not blockers):\\n- A reviewer proposed that the lifecycle resolver's 32-hop bound silently leaks a 33-deep cell. Actual depth32 probe prints `scope end` then `drop 7`; depth33 is CLEANLY REFUSED by the existing borrowing-struct value fence before codegen. Follow-up source review confirms alias and mixed chains follow the same fence. The reachable-leak finding is withdrawn; no demand to change the cap on that evidence.\\n- A reviewer proposed missing positive coverage for automatic/derived Copy of a struct containing a cell. Actual current compiler refuses both: an undeclared wrapper needs Copy, and @synthesize Copy refuses the NoCopy field. The author's stated current limitation is therefore accurate for these shapes. Namespace policy versus checker behavior is an inherited discrepancy, not a new patch regression or a reason to narrow the language's approved Copy policy. No positive cell-field-copy run is claimed.\\n\\nOther source conclusions: the three intercepted checker/codegen pairs route cell, alias and UnsafeMemory.write through the transfer checkpoint/lowering once. Mutex/SpinLock inits both move their parameters. No missed owning corpus migration found. For reachable types, alias/transparent storage has the payload LLVM layout; the same-address drop glue is ABI-compatible. UnsafeMemory.write is raw placement and intentionally does not destroy old contents. Owning aliases and ordinary lock/cell paths have substantive existing author coverage.\\n\\nEvidence: local://sl340-new-lifecycle-smokes.json and archived probe sources. New execution was limited to the trivial generic-cell miscompile and the safe depth32 destruction control; the depth33 and field-copy cases stopped on clean diagnostics. No old compiler, known UAF/SIGSEGV, full suite, freestanding, battery, formatter or implementation edits.\\n\\nFreeze m176 respected: this withholds approval and records the defect; it does not authorize a new compiler revision round or merge.\\n\",\"id\":\"SL-340.p1\",\"request_id\":\"pid-63746-1790277221-10000\",\"revision\":1,\"verdict\":\"request-changes\"}","request_id":"pid-63746-1790277221-10000","sequence":1492,"timestamp":"1790277221"}
---
