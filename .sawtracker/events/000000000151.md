---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791261","description":"## Scope and status\n\nVerify frame layout for spawn parameters containing std task types\n\nThe executor matrix is landed; do not reimplement executor unification. Only re-probe the reported transitive std-type parameter/frame-layout issue, and close it with evidence if later frame changes fixed it.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Re-run the precise historical probe at current HEAD, recording compiler revision, target and output; distinguish the original symptom from any surviving residue.\n2. Compare with later design landings and existing pins. If resolved, close with the relevant commit/test; if still present, preserve a minimal reproducer and update the scope.\n3. Do not schedule a speculative implementation until the verification identifies a remaining defect or missing contract.\n\n## Acceptance criteria\n\n- [ ] The task has a recorded current-status verdict supported by a probe, code reference or landing record.\n- [ ] Any surviving work has a bounded scope and regression oracle; otherwise the task is closed with its resolution.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 9823–9841\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L9823-L9841)\n\n- **Test matrix — LANDED (worktree).** Three NEW tests for behavior the old split\n  executors could not produce (suite 888->891): `net_accept_loop_concurrent`\n  (ACCEPTANCE — a server task accept-loops N=3, SPAWNING a handler per connection\n  into its OWN group that runs eagerly on the shared scheduler while the server\n  parks, + 3 concurrent client tasks; round-trips all N, deterministic 3/3);\n  `taskgroup_spawn_and_loop` (the core gap — main parks in a sleep-loop while its\n  spawned child INTERLEAVES `0,100,101,1,102,2,7`, not the old\n  `0,1,2,100,101,102,7`); `taskgroup_nested_ambient` (nested groups + a task\n  joining its own inner children = the reentrancy hazard, cross-group eager\n  interleave). Existing coverage survives and validates the rest under the ambient\n  scheduler: `taskgroup_sleep_ordering`/`structured_join`/`unjoined_drop`/\n  `two_task_yield`/`cancel_check`, `net_io_sleep_interleave`, `net_serve_two/three_\n  connections`. Updated the now-stale per-group-executor comments in\n  `taskgroup_nested_groups` + `taskgroup_suspending_parent_sleep` (results kept).\n  **DF finding (pre-existing, reproduces on parent):** spawning a function whose\n  param transitively references a std struct (e.g. `f(h: TaskHandle<Int>)`) ICEs\n  \"Undefined struct: TaskGroup\" during frame layout — unrelated to executor\n  unification; reentrancy is instead tested via nested-group joins. [89, 52b, 76]\n","id":"SL-151","labels":["todo-import","needs-verification","verification","plan"],"priority":"normal","project":"SL","revision":1,"sequence":151,"status":"open","title":"Verify frame layout for spawn parameters containing std task types","updated":"1788791261"},"before":null,"id":"SL-151","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nVerify frame layout for spawn parameters containing std task types\\n\\nThe executor matrix is landed; do not reimplement executor unification. Only re-probe the reported transitive std-type parameter/frame-layout issue, and close it with evidence if later frame changes fixed it.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Re-run the precise historical probe at current HEAD, recording compiler revision, target and output; distinguish the original symptom from any surviving residue.\\n2. Compare with later design landings and existing pins. If resolved, close with the relevant commit/test; if still present, preserve a minimal reproducer and update the scope.\\n3. Do not schedule a speculative implementation until the verification identifies a remaining defect or missing contract.\\n\\n## Acceptance criteria\\n\\n- [ ] The task has a recorded current-status verdict supported by a probe, code reference or landing record.\\n- [ ] Any surviving work has a bounded scope and regression oracle; otherwise the task is closed with its resolution.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 9823–9841\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L9823-L9841)\\n\\n- **Test matrix — LANDED (worktree).** Three NEW tests for behavior the old split\\n  executors could not produce (suite 888->891): `net_accept_loop_concurrent`\\n  (ACCEPTANCE — a server task accept-loops N=3, SPAWNING a handler per connection\\n  into its OWN group that runs eagerly on the shared scheduler while the server\\n  parks, + 3 concurrent client tasks; round-trips all N, deterministic 3/3);\\n  `taskgroup_spawn_and_loop` (the core gap — main parks in a sleep-loop while its\\n  spawned child INTERLEAVES `0,100,101,1,102,2,7`, not the old\\n  `0,1,2,100,101,102,7`); `taskgroup_nested_ambient` (nested groups + a task\\n  joining its own inner children = the reentrancy hazard, cross-group eager\\n  interleave). Existing coverage survives and validates the rest under the ambient\\n  scheduler: `taskgroup_sleep_ordering`/`structured_join`/`unjoined_drop`/\\n  `two_task_yield`/`cancel_check`, `net_io_sleep_interleave`, `net_serve_two/three_\\n  connections`. Updated the now-stale per-group-executor comments in\\n  `taskgroup_nested_groups` + `taskgroup_suspending_parent_sleep` (results kept).\\n  **DF finding (pre-existing, reproduces on parent):** spawning a function whose\\n  param transitively references a std struct (e.g. `f(h: TaskHandle<Int>)`) ICEs\\n  \\\"Undefined struct: TaskGroup\\\" during frame layout — unrelated to executor\\n  unification; reentrancy is instead tested via nested-group joins. [89, 52b, 76]\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"verification\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-36176781e2e5f6b3495a-create\",\"title\":\"Verify frame layout for spawn parameters containing std task types\"}","request_id":"todo-20260907-36176781e2e5f6b3495a-create","sequence":151,"timestamp":"1788791261"}
---
