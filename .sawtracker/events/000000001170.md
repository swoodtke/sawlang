---
{"action":"create","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790073059","description":"Found by the lead's design 275 U3 validation (Sep 22), PRE-EXISTING on main `da84578e` and identical on the `sl318u3` branch — NOT U3's, filed beside it.\n\n## The finding\n\nA closure passed to a method on a reference-parameter root may reach that root INSIDE the method's window, and nothing charges it. The exclusive form is a use-after-free write:\n\n```saw\nfunc grow_then_write(v: &var Vector<Int>) -> Int {\n    v.with_var_ref(0, { e in\n        var k = 0\n        while k < 64 { try! v.push(k)  k = k + 1 }   // reallocates under `e`\n        e = 77                                        // lands in the FREED buffer\n    })\n    v[0]                                              // prints 1: the write was lost\n}\n```\n\nCompiles cleanly, exit 0, prints `1`. Cells measured (worktree `.build/scratch/lead/l6_with_var_ref_hole.saw`, main and branch agree): `with_var_ref` + 64 pushes + write → `1` (write lost into freed memory); `with_ref` + push → compiles, `2`; `Vector.each` whose body pushes → compiles, `4` (the visitor walks a buffer that reallocates under it). A LOCAL root is already refused, for an unrelated reason — the closure would have to COPY the vector (`cannot copy value of type Vector<Int> which implements ExplicitCopy`) — so the hole is specific to a `&`/`&var` PARAMETER root, which a closure captures by re-borrow (design 216).\n\n## The mechanism (obligation 4)\n\nDesign 199 joins a NESTED CALL's references to the outer call's access set, so `v.with_var_ref(0, reset(&var v))` is refused. A closure's BORROW CAPTURES are not joined against the RECEIVER of the call the closure is passed to: the receiver borrow (`&var v` for `with_var_ref`, `&v` for `each`/`with_ref`) and the closure's captured re-borrow of the same root are never compared. Design 146 called `with_ref`/`with_var_ref` \"invalidation-proof\"; the proof rested on the closure SCOPE, and a captured reference reaches through it.\n\nSiblings the mechanism reaches (every `&self`/`&var self` method taking a NON-ESCAPING closure, called on a reference-param root whose closure names that root): `Vector.with_ref`/`with_var_ref`/`each`/`map`/`fold`/`each_indexed`/`sort_by`, `Map.each`/`each_key`/`each_value`, `Set.each`, `Mutex.lock`/`SpinLock.lock` (a body that re-enters the same lock through the captured root), `Arc.with_unique`. The `&var`-receiver ones are the memory-unsafe half; the `&self` ones are the iterator-invalidation half.\n\n## The fix shape\n\nOne rule at the call-exclusivity funnel (`types._check_call_exclusivity`, which design 199 already widened): a closure argument's borrow captures — implicit reference-parameter captures included — JOIN the call's access set, so a capture overlapping the receiver's path is the ordinary exclusivity error naming both. The window record design 275 U3 introduced (`sawc/windows.py`) is the other place the same fact could live, and SL-343 (with_ref/with_var_ref windows across a suspension) is the brief that will want it settled first. Conformance row owed beside K135.\n\nRepro cells: `.build/scratch/lead/l6_with_var_ref_hole.saw` in worktree `sl318u3` (copy into the fix's pin).\n","id":"SL-345","labels":["bug","borrows","soundness"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":1,"sequence":1170,"stage":"backlog","status":"open","title":"USE-AFTER-FREE: a closure's captured reference-param root is not charged against the receiver of the call it is passed to (v.with_var_ref(0, { e in try! v.push(k)  e = 77 }) compiles; write lands in the freed buffer)","updated":"1790073059"},"before":null,"id":"SL-345","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"description\":\"Found by the lead's design 275 U3 validation (Sep 22), PRE-EXISTING on main `da84578e` and identical on the `sl318u3` branch — NOT U3's, filed beside it.\\n\\n## The finding\\n\\nA closure passed to a method on a reference-parameter root may reach that root INSIDE the method's window, and nothing charges it. The exclusive form is a use-after-free write:\\n\\n```saw\\nfunc grow_then_write(v: &var Vector<Int>) -> Int {\\n    v.with_var_ref(0, { e in\\n        var k = 0\\n        while k < 64 { try! v.push(k)  k = k + 1 }   // reallocates under `e`\\n        e = 77                                        // lands in the FREED buffer\\n    })\\n    v[0]                                              // prints 1: the write was lost\\n}\\n```\\n\\nCompiles cleanly, exit 0, prints `1`. Cells measured (worktree `.build/scratch/lead/l6_with_var_ref_hole.saw`, main and branch agree): `with_var_ref` + 64 pushes + write → `1` (write lost into freed memory); `with_ref` + push → compiles, `2`; `Vector.each` whose body pushes → compiles, `4` (the visitor walks a buffer that reallocates under it). A LOCAL root is already refused, for an unrelated reason — the closure would have to COPY the vector (`cannot copy value of type Vector<Int> which implements ExplicitCopy`) — so the hole is specific to a `&`/`&var` PARAMETER root, which a closure captures by re-borrow (design 216).\\n\\n## The mechanism (obligation 4)\\n\\nDesign 199 joins a NESTED CALL's references to the outer call's access set, so `v.with_var_ref(0, reset(&var v))` is refused. A closure's BORROW CAPTURES are not joined against the RECEIVER of the call the closure is passed to: the receiver borrow (`&var v` for `with_var_ref`, `&v` for `each`/`with_ref`) and the closure's captured re-borrow of the same root are never compared. Design 146 called `with_ref`/`with_var_ref` \\\"invalidation-proof\\\"; the proof rested on the closure SCOPE, and a captured reference reaches through it.\\n\\nSiblings the mechanism reaches (every `&self`/`&var self` method taking a NON-ESCAPING closure, called on a reference-param root whose closure names that root): `Vector.with_ref`/`with_var_ref`/`each`/`map`/`fold`/`each_indexed`/`sort_by`, `Map.each`/`each_key`/`each_value`, `Set.each`, `Mutex.lock`/`SpinLock.lock` (a body that re-enters the same lock through the captured root), `Arc.with_unique`. The `&var`-receiver ones are the memory-unsafe half; the `&self` ones are the iterator-invalidation half.\\n\\n## The fix shape\\n\\nOne rule at the call-exclusivity funnel (`types._check_call_exclusivity`, which design 199 already widened): a closure argument's borrow captures — implicit reference-parameter captures included — JOIN the call's access set, so a capture overlapping the receiver's path is the ordinary exclusivity error naming both. The window record design 275 U3 introduced (`sawc/windows.py`) is the other place the same fact could live, and SL-343 (with_ref/with_var_ref windows across a suspension) is the brief that will want it settled first. Conformance row owed beside K135.\\n\\nRepro cells: `.build/scratch/lead/l6_with_var_ref_hole.saw` in worktree `sl318u3` (copy into the fix's pin).\\n\",\"labels\":[\"bug\",\"borrows\",\"soundness\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"pid-72713-1790073059-7000\",\"stage\":\"backlog\",\"title\":\"USE-AFTER-FREE: a closure's captured reference-param root is not charged against the receiver of the call it is passed to (v.with_var_ref(0, { e in try! v.push(k)  e = 77 }) compiles; write lands in the freed buffer)\"}","request_id":"pid-72713-1790073059-7000","sequence":1170,"timestamp":"1790073059"}
---
