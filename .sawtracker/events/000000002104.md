---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl222","closed":"1790341662","comments":[{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \"oracle known wrong\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `drop.discarded-try-catch`.","created":"1790341662","id":"c1","kind":"landing"}],"created":"1789083435","description":"In SYNC code, `try EXPR catch { … }` written as a bare STATEMENT whose value is an owning type never destroys that value. The value is discarded, so it should deinit at the statement; it does not, and nothing is reported.\n\nRepro (no suspension anywhere, so the coroutine transform never runs; compiled with the pristine main checkout):\n\n  struct Res { name: String }\n  extension Res: NoCopy {\n      func deinit(&var self) { print(\"drop {}\", self.name) }\n  }\n  struct Bad { why: String }\n  extension Bad: Error {\n      func format(&self, into: &var StringBuilder) { try! into.append(self.why) }\n  }\n  func produce() -> Result<Res, Bad> { Res(name: \"made\") }\n\n  func discard_it() -> Int {\n      try produce() catch { Res(name: \"fallback\") }\n      7\n  }\n  func bind_it() -> Int {\n      let kept = try produce() catch { Res(name: \"fallback\") }\n      8\n  }\n  func main() { print(discard_it())  print(bind_it()) }\n\nObserved: '7' then 'drop made' then '8'. Wanted: 'drop made / 7 / drop made / 8'. The BOUND control (`bind_it`) is correct, which localizes it to the DISCARD of a statement-position `TryExpr` value rather than to the try lowering generally.\n\nA leak, not a double free -- no memory is freed twice and the program exits 0. It matters because `try f() catch { fallback }` in statement position reads as a complete, handled operation, and design 151 deliberately made a discarded RESULT a compile error precisely so this shape is written rather than dropped; the value the catch supplies then quietly outlives everything.\n\nPRE-EXISTING and SYNC-side: reproduces on unpatched origin/main with no suspension in the program. Found by SL-222's review-r5 ownership audit, where the DRIVEN twin of the same body was the one that behaved correctly (it does destroy the value) -- so the coroutine path and the sync path disagree, and here it is the sync path that is wrong. Not fixed in SL-222's branch: nothing in that batch touches the sync discard path.","id":"SL-240","labels":["frozen","hazard"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":3,"sequence":2104,"stage":"backlog","status":"closed","title":"A discarded statement-position inline try/catch leaks its owning value (sync)","updated":"1790341662"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sl222","closed":"","comments":[],"created":"1789083435","description":"In SYNC code, `try EXPR catch { … }` written as a bare STATEMENT whose value is an owning type never destroys that value. The value is discarded, so it should deinit at the statement; it does not, and nothing is reported.\n\nRepro (no suspension anywhere, so the coroutine transform never runs; compiled with the pristine main checkout):\n\n  struct Res { name: String }\n  extension Res: NoCopy {\n      func deinit(&var self) { print(\"drop {}\", self.name) }\n  }\n  struct Bad { why: String }\n  extension Bad: Error {\n      func format(&self, into: &var StringBuilder) { try! into.append(self.why) }\n  }\n  func produce() -> Result<Res, Bad> { Res(name: \"made\") }\n\n  func discard_it() -> Int {\n      try produce() catch { Res(name: \"fallback\") }\n      7\n  }\n  func bind_it() -> Int {\n      let kept = try produce() catch { Res(name: \"fallback\") }\n      8\n  }\n  func main() { print(discard_it())  print(bind_it()) }\n\nObserved: '7' then 'drop made' then '8'. Wanted: 'drop made / 7 / drop made / 8'. The BOUND control (`bind_it`) is correct, which localizes it to the DISCARD of a statement-position `TryExpr` value rather than to the try lowering generally.\n\nA leak, not a double free -- no memory is freed twice and the program exits 0. It matters because `try f() catch { fallback }` in statement position reads as a complete, handled operation, and design 151 deliberately made a discarded RESULT a compile error precisely so this shape is written rather than dropped; the value the catch supplies then quietly outlives everything.\n\nPRE-EXISTING and SYNC-side: reproduces on unpatched origin/main with no suspension in the program. Found by SL-222's review-r5 ownership audit, where the DRIVEN twin of the same body was the one that behaved correctly (it does destroy the value) -- so the coroutine path and the sync path disagree, and here it is the sync path that is wrong. Not fixed in SL-222's branch: nothing in that batch touches the sync discard path.","id":"SL-240","labels":["frozen","hazard"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":2,"sequence":2103,"stage":"backlog","status":"open","title":"A discarded statement-position inline try/catch leaks its owning value (sync)","updated":"1790341661"},"id":"SL-240","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \\\"oracle known wrong\\\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `drop.discarded-try-catch`.\",\"id\":\"SL-240\",\"request_id\":\"pid-55645-1790341662-7000\"}","request_id":"pid-55645-1790341662-7000","sequence":2104,"timestamp":"1790341662"}
---
