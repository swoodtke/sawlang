---
{"action":"patch_add","actor":"agent:claude-air-sawlang","after":{"acceptance":[],"assignee":"agent:claude-air-sawlang","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 2 in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188482","id":"c1"},{"author":"agent:claude-sawlang","body":"RULING RELAYED (user, Sep 24, in the Studio session; recorded by the lead, since agents cannot post the ruling kind): Q1 is DECLARED-DERIVED. An implicit closure capture takes the binding as it is: a &var binding (or self in a &var self method) joins the call's access set as an exclusive borrow whatever the body does, and a &T binding joins as shared. The user's words: \"the obvious answer is the closure captures the variable from the scope as is - if the user wants a different capture, then they need to spell out what they want explicitly.\" A read-only visitor migrates by spelling [&v], which compiles today. The brief (description r6) is updated and marked READY.\n","created":"1790259931","id":"c2"},{"author":"agent:claude-air-sawlang","body":"Submitted patch SL-345.p1 (revision 1): An implicit reference capture joins the call's access set (declared-derived, user ruling c2): the with_var_ref use-after-free and its self twin are refused; a read-only visitor spells [&v]","created":"1790271823","id":"c3"}],"created":"1790073059","description":"Found by the lead's design 275 U3 validation (Sep 22), PRE-EXISTING on main `da84578e` and identical on the `sl318u3` branch — NOT U3's, filed beside it.\n\n## The finding\n\nA closure passed to a method on a reference-parameter root may reach that root INSIDE the method's window, and nothing charges it. The exclusive form is a use-after-free write:\n\n```saw\nfunc grow_then_write(v: &var Vector<Int>) -> Int {\n    v.with_var_ref(0, { e in\n        var k = 0\n        while k < 64 { try! v.push(k)  k = k + 1 }   // reallocates under `e`\n        e = 77                                        // lands in the FREED buffer\n    })\n    v[0]                                              // prints 1: the write was lost\n}\n```\n\nCompiles cleanly, exit 0, prints `1`. Cells measured (worktree `.build/scratch/lead/l6_with_var_ref_hole.saw`, main and branch agree): `with_var_ref` + 64 pushes + write → `1` (write lost into freed memory); `with_ref` + push → compiles, `2`; `Vector.each` whose body pushes → compiles, `4` (the visitor walks a buffer that reallocates under it). A LOCAL root is already refused, for an unrelated reason — the closure would have to COPY the vector (`cannot copy value of type Vector<Int> which implements ExplicitCopy`) — so the hole is specific to a `&`/`&var` PARAMETER root, which a closure captures by re-borrow (design 216).\n\n## The mechanism (obligation 4)\n\nDesign 199 joins a NESTED CALL's references to the outer call's access set, so `v.with_var_ref(0, reset(&var v))` is refused. A closure's BORROW CAPTURES are not joined against the RECEIVER of the call the closure is passed to: the receiver borrow (`&var v` for `with_var_ref`, `&v` for `each`/`with_ref`) and the closure's captured re-borrow of the same root are never compared. Design 146 called `with_ref`/`with_var_ref` \"invalidation-proof\"; the proof rested on the closure SCOPE, and a captured reference reaches through it.\n\nSiblings the mechanism reaches (every `&self`/`&var self` method taking a NON-ESCAPING closure, called on a reference-param root whose closure names that root): `Vector.with_ref`/`with_var_ref`/`each`/`map`/`fold`/`each_indexed`/`sort_by`, `Map.each`/`each_key`/`each_value`, `Set.each`, `Mutex.lock`/`SpinLock.lock` (a body that re-enters the same lock through the captured root), `Arc.with_unique`. The `&var`-receiver ones are the memory-unsafe half; the `&self` ones are the iterator-invalidation half.\n\n## The fix shape\n\nOne rule at the call-exclusivity funnel (`types._check_call_exclusivity`, which design 199 already widened): a closure argument's borrow captures — implicit reference-parameter captures included — JOIN the call's access set, so a capture overlapping the receiver's path is the ordinary exclusivity error naming both. The window record design 275 U3 introduced (`sawc/windows.py`) is the other place the same fact could live, and SL-343 (with_ref/with_var_ref windows across a suspension) is the brief that will want it settled first. Conformance row owed beside K135.\n\nRepro cells: `.build/scratch/lead/l6_with_var_ref_hole.saw` in worktree `sl318u3` (copy into the fix's pin).\n\n---\n\n# BRIEF (lead, Sep 24): READY. Q1 ruled by the user on Sep 24: DECLARED-DERIVED\n\n## Re-measured on main bf342c5c\nCells are in the lead checkout at `.build/scratch/sl345_probe.saw` and `sl345_probe2.saw`, updated for SL-347's `;` rule.\n- `&var` parameter root: `with_var_ref`, 64 pushes, then `e = 77`. Compiles and prints `1`; the write is lost into freed memory.\n- **`self` sibling, NEW:** in a `&var self` method, `self.v.with_var_ref(0, { e in … self.v.push(k) … e = 77 })` compiles and dies with SIGBUS.\n- Read-only implicit capture: `v.each({ [&var total] x in total = total + x + v.len() })` with `v: &var Vector<Int>` compiles today and prints `15`. Under the Q1 ruling it is REFUSED. The spelled shared capture `v.each({ [&v, &var total] x in … v.len() })` compiles today (prints `15`) and stays legal: it is the migration. The spelled `[&var v]` form is already refused today (\"exclusive access violation\").\n- A closure held in a `let` and then passed in is already refused (\"an escaping closure cannot capture `v`, a reference\"), so the hole is closure LITERAL arguments only.\n\n## Mechanism, confirmed in code\nThe `ClosureExpr` branch of `_check_call_exclusivity` joins only `capture_specs`, the written `[..]` list. An IMPLICIT capture of a reference binding is not joined. That covers a `&`/`&var` parameter, `self` in a `&self`/`&var self` method, and an enclosing window's parameter. `capture_modes` records such a capture as `plain` (or as `self`'s receiver mode), and codegen captures the pointer (design 216). The capture is therefore a live borrow that the Law never sees. The residue is one missing input to the existing funnel; there is no second funnel to build.\n\n## The rule\nA non-escaping closure argument's implicit reference captures join the call's access set, exactly as a written `[&x]`/`[&var x]` does. They then meet the pairwise overlap test, the design-189 task-borrow cross-check, and design 275 U3's open-window check, all of which run over the same entries.\n\nAccess kind of an implicit capture: **the closure captures the binding as it is** (user ruling, Sep 24).\n- A `&T` binding, or `self` in a `&self` method, joins as `imm`.\n- A `&var T` binding, or `self` in a `&var self` method, joins as `mut`, whatever the body does with it.\n- A different capture must be spelled out. `[&v]` takes a shared re-borrow of a `&var` binding, and it is the migration for a read-only visitor.\n\nThe rule depends only on the binding's declaration. No analysis of the body is involved.\n\nThe rejected alternative was use-derived: `mut` only when the body writes through the name. The ruling's reason: the obvious meaning of an implicit capture is the variable as it stands in the enclosing scope, and a user who wants something else writes it down.\n\n## Implementation shape (obligation 1: the existing funnel, one new input)\n- The closure checker records the access kind of each implicit reference capture in ONE declared annotation on `ClosureExpr`, for example `reference_capture_access: Dict[str, str]`, declared with `annotation(...)` so the astgraft lane stays green. Fill it where `captures`/`capture_modes` are computed, from each captured binding's declared reference type (and `self_capture_mode` for `self`).\n- `_check_call_exclusivity` reads that annotation beside `capture_specs`. Reading it from a closure argument that has not been checked yet is an internal-compiler-error invariant naming the call, never a silently empty set. Verify the check order at every one of the funnel's fifteen named entry points.\n- EXCLUSION: the synthesized place-window closure's receiver root (`place_window_root`) must not join. `_synthesize_place_window_captures` leaves that one name plain on purpose; joining it would make every `v[i]` window conflict with itself. Say why in the code.\n\n## Position matrix (tests cover it row by row)\n1. `&var` parameter root; `with_var_ref`, push, write → refused. This is the finding.\n2. `&var self` method; `self.v.with_var_ref` plus `self.v.push` → refused. SIGBUS today.\n3. `&var` parameter; `with_ref`, then push → refused.\n4. `&var` parameter; a body that pushes under each of `each`, `map`, `fold`, `each_indexed` and `sort_by` → refused, one cell per method.\n5. `Map.each`/`each_key`/`each_value` and `Set.each`, with a body mutating through the parameter root → refused.\n6. `Arc.with_unique`, with a body touching the same root mutably → refused.\n7. A read-only implicit capture of a `&var` parameter under a `&self` visitor (`v.each { x in … v.len() }`) → REFUSED, with the `[&v]` hint. The spelled `v.each { [&v] x in … v.len() }` → ACCEPTED; this is the control. Add the same pair for `self` in a `&var self` method.\n8. Disjoint roots, `a.each { x in b.push(x) }` with both `&var` parameters → accepted.\n9. A `&T` parameter captured implicitly: under a `&self` visitor → accepted; under a `&var self` receiver → refused.\n10. An enclosing window's parameter as the root (`with_var_ref` nested inside `with_var_ref`, the inner body touching the outer element parameter) → probe first, then decide from the evidence and report.\n11. Reached through an intermediate closure literal, `v.each { x in w.each { y in v.push(y) } }` → refused at the outer call.\n12. The driven twin of rows 1, 2 and 7, after a `yield_now()` → same verdicts.\n13. Inside an open `for x in v.iter()` window, a closure argument that implicitly captures `v` mutably → refused by the window check.\n14. Place-window lowerings (`v[i].mutate(…)`, `v[0] = w[1]`) are unchanged, and their existing tests stay green (the exclusion above).\n\nOut of scope: `Mutex.lock`/`SpinLock.lock` re-entry. Both sides are `&self`, so a re-entrant lock is a deadlock, not an exclusivity violation. Record that and move on.\n\n## Obligations\n- **Obligation 3:** conformance rows FIRST, beside K135 in `examples/conformance/`, with their `INDEX.md` lines, as the branch's first commit.\n- **Obligation 2 (consumer sweep):** this refuses programs that compile today. Before landing, list every tracked `.saw` file the new rule refuses (examples/, blade/, libs/, devtools/, selfhost/, prototypes/). Classify each one:\n  - A read-only use that the ruling now refuses is MIGRATED in this branch by spelling the capture (`[&v]` / `[&self]`), and listed in the report.\n  - A body that really mutates the root is a real hazard: report it; do not migrate it.\n  - Anything the rule refuses that is neither is a false positive against the rule: STOP and report.\n  - Never widen an exemption ad hoc.\n- **Diagnostic:** the ordinary exclusivity error naming both sides (the implicit capture and the receiver). The hint says the closure captures `v` as the `&var` it is, and to write `[&v]` for a shared capture. If the body writes `v`, the hint instead says to pass the element or parameter in rather than reaching the root. Pin the wording in examples/errors/.\n- **Docs:** LANGUAGE_SPEC.md's closure-capture section and the saw-lang skill state the rule (an implicit capture takes the binding as declared), with the `[&v]` idiom for a shared read inside a visitor.\n\n## Gates and files\n- Gates: full terminal battery; `astgraft` covers the new annotation.\n- Files: `sawc/typechecker/types.py` (the funnel), `sawc/typechecker/expressions.py` (the closure checker), `sawc/ast_nodes.py` (the annotation), `examples/conformance/`, `examples/errors/`.\n- Also LANGUAGE_SPEC.md and `.claude/skills/saw-lang/SKILL.md` (closure-capture sections only). SL-2 U0′ edits other sections of those two files concurrently, so resolve any rebase hunk by hunk.\n- Otherwise disjoint from the parser work (SL-2 U0′, SL-73).\n- SL-343 (with_ref windows across a suspension) wants this settled first. This brief does not change the structure of `windows.py`.\n","id":"SL-345","labels":["bug","borrows","soundness"],"order":2,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":10,"sequence":1463,"stage":"queued","status":"open","title":"USE-AFTER-FREE: a closure's captured reference-param root is not charged against the receiver of the call it is passed to (v.with_var_ref(0, { e in try! v.push(k)  e = 77 }) compiles; write lands in the freed buffer)","updated":"1790271823"},"before":{"acceptance":[],"assignee":"agent:claude-air-sawlang","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 2 in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188482","id":"c1"},{"author":"agent:claude-sawlang","body":"RULING RELAYED (user, Sep 24, in the Studio session; recorded by the lead, since agents cannot post the ruling kind): Q1 is DECLARED-DERIVED. An implicit closure capture takes the binding as it is: a &var binding (or self in a &var self method) joins the call's access set as an exclusive borrow whatever the body does, and a &T binding joins as shared. The user's words: \"the obvious answer is the closure captures the variable from the scope as is - if the user wants a different capture, then they need to spell out what they want explicitly.\" A read-only visitor migrates by spelling [&v], which compiles today. The brief (description r6) is updated and marked READY.\n","created":"1790259931","id":"c2"}],"created":"1790073059","description":"Found by the lead's design 275 U3 validation (Sep 22), PRE-EXISTING on main `da84578e` and identical on the `sl318u3` branch — NOT U3's, filed beside it.\n\n## The finding\n\nA closure passed to a method on a reference-parameter root may reach that root INSIDE the method's window, and nothing charges it. The exclusive form is a use-after-free write:\n\n```saw\nfunc grow_then_write(v: &var Vector<Int>) -> Int {\n    v.with_var_ref(0, { e in\n        var k = 0\n        while k < 64 { try! v.push(k)  k = k + 1 }   // reallocates under `e`\n        e = 77                                        // lands in the FREED buffer\n    })\n    v[0]                                              // prints 1: the write was lost\n}\n```\n\nCompiles cleanly, exit 0, prints `1`. Cells measured (worktree `.build/scratch/lead/l6_with_var_ref_hole.saw`, main and branch agree): `with_var_ref` + 64 pushes + write → `1` (write lost into freed memory); `with_ref` + push → compiles, `2`; `Vector.each` whose body pushes → compiles, `4` (the visitor walks a buffer that reallocates under it). A LOCAL root is already refused, for an unrelated reason — the closure would have to COPY the vector (`cannot copy value of type Vector<Int> which implements ExplicitCopy`) — so the hole is specific to a `&`/`&var` PARAMETER root, which a closure captures by re-borrow (design 216).\n\n## The mechanism (obligation 4)\n\nDesign 199 joins a NESTED CALL's references to the outer call's access set, so `v.with_var_ref(0, reset(&var v))` is refused. A closure's BORROW CAPTURES are not joined against the RECEIVER of the call the closure is passed to: the receiver borrow (`&var v` for `with_var_ref`, `&v` for `each`/`with_ref`) and the closure's captured re-borrow of the same root are never compared. Design 146 called `with_ref`/`with_var_ref` \"invalidation-proof\"; the proof rested on the closure SCOPE, and a captured reference reaches through it.\n\nSiblings the mechanism reaches (every `&self`/`&var self` method taking a NON-ESCAPING closure, called on a reference-param root whose closure names that root): `Vector.with_ref`/`with_var_ref`/`each`/`map`/`fold`/`each_indexed`/`sort_by`, `Map.each`/`each_key`/`each_value`, `Set.each`, `Mutex.lock`/`SpinLock.lock` (a body that re-enters the same lock through the captured root), `Arc.with_unique`. The `&var`-receiver ones are the memory-unsafe half; the `&self` ones are the iterator-invalidation half.\n\n## The fix shape\n\nOne rule at the call-exclusivity funnel (`types._check_call_exclusivity`, which design 199 already widened): a closure argument's borrow captures — implicit reference-parameter captures included — JOIN the call's access set, so a capture overlapping the receiver's path is the ordinary exclusivity error naming both. The window record design 275 U3 introduced (`sawc/windows.py`) is the other place the same fact could live, and SL-343 (with_ref/with_var_ref windows across a suspension) is the brief that will want it settled first. Conformance row owed beside K135.\n\nRepro cells: `.build/scratch/lead/l6_with_var_ref_hole.saw` in worktree `sl318u3` (copy into the fix's pin).\n\n---\n\n# BRIEF (lead, Sep 24): READY. Q1 ruled by the user on Sep 24: DECLARED-DERIVED\n\n## Re-measured on main bf342c5c\nCells are in the lead checkout at `.build/scratch/sl345_probe.saw` and `sl345_probe2.saw`, updated for SL-347's `;` rule.\n- `&var` parameter root: `with_var_ref`, 64 pushes, then `e = 77`. Compiles and prints `1`; the write is lost into freed memory.\n- **`self` sibling, NEW:** in a `&var self` method, `self.v.with_var_ref(0, { e in … self.v.push(k) … e = 77 })` compiles and dies with SIGBUS.\n- Read-only implicit capture: `v.each({ [&var total] x in total = total + x + v.len() })` with `v: &var Vector<Int>` compiles today and prints `15`. Under the Q1 ruling it is REFUSED. The spelled shared capture `v.each({ [&v, &var total] x in … v.len() })` compiles today (prints `15`) and stays legal: it is the migration. The spelled `[&var v]` form is already refused today (\"exclusive access violation\").\n- A closure held in a `let` and then passed in is already refused (\"an escaping closure cannot capture `v`, a reference\"), so the hole is closure LITERAL arguments only.\n\n## Mechanism, confirmed in code\nThe `ClosureExpr` branch of `_check_call_exclusivity` joins only `capture_specs`, the written `[..]` list. An IMPLICIT capture of a reference binding is not joined. That covers a `&`/`&var` parameter, `self` in a `&self`/`&var self` method, and an enclosing window's parameter. `capture_modes` records such a capture as `plain` (or as `self`'s receiver mode), and codegen captures the pointer (design 216). The capture is therefore a live borrow that the Law never sees. The residue is one missing input to the existing funnel; there is no second funnel to build.\n\n## The rule\nA non-escaping closure argument's implicit reference captures join the call's access set, exactly as a written `[&x]`/`[&var x]` does. They then meet the pairwise overlap test, the design-189 task-borrow cross-check, and design 275 U3's open-window check, all of which run over the same entries.\n\nAccess kind of an implicit capture: **the closure captures the binding as it is** (user ruling, Sep 24).\n- A `&T` binding, or `self` in a `&self` method, joins as `imm`.\n- A `&var T` binding, or `self` in a `&var self` method, joins as `mut`, whatever the body does with it.\n- A different capture must be spelled out. `[&v]` takes a shared re-borrow of a `&var` binding, and it is the migration for a read-only visitor.\n\nThe rule depends only on the binding's declaration. No analysis of the body is involved.\n\nThe rejected alternative was use-derived: `mut` only when the body writes through the name. The ruling's reason: the obvious meaning of an implicit capture is the variable as it stands in the enclosing scope, and a user who wants something else writes it down.\n\n## Implementation shape (obligation 1: the existing funnel, one new input)\n- The closure checker records the access kind of each implicit reference capture in ONE declared annotation on `ClosureExpr`, for example `reference_capture_access: Dict[str, str]`, declared with `annotation(...)` so the astgraft lane stays green. Fill it where `captures`/`capture_modes` are computed, from each captured binding's declared reference type (and `self_capture_mode` for `self`).\n- `_check_call_exclusivity` reads that annotation beside `capture_specs`. Reading it from a closure argument that has not been checked yet is an internal-compiler-error invariant naming the call, never a silently empty set. Verify the check order at every one of the funnel's fifteen named entry points.\n- EXCLUSION: the synthesized place-window closure's receiver root (`place_window_root`) must not join. `_synthesize_place_window_captures` leaves that one name plain on purpose; joining it would make every `v[i]` window conflict with itself. Say why in the code.\n\n## Position matrix (tests cover it row by row)\n1. `&var` parameter root; `with_var_ref`, push, write → refused. This is the finding.\n2. `&var self` method; `self.v.with_var_ref` plus `self.v.push` → refused. SIGBUS today.\n3. `&var` parameter; `with_ref`, then push → refused.\n4. `&var` parameter; a body that pushes under each of `each`, `map`, `fold`, `each_indexed` and `sort_by` → refused, one cell per method.\n5. `Map.each`/`each_key`/`each_value` and `Set.each`, with a body mutating through the parameter root → refused.\n6. `Arc.with_unique`, with a body touching the same root mutably → refused.\n7. A read-only implicit capture of a `&var` parameter under a `&self` visitor (`v.each { x in … v.len() }`) → REFUSED, with the `[&v]` hint. The spelled `v.each { [&v] x in … v.len() }` → ACCEPTED; this is the control. Add the same pair for `self` in a `&var self` method.\n8. Disjoint roots, `a.each { x in b.push(x) }` with both `&var` parameters → accepted.\n9. A `&T` parameter captured implicitly: under a `&self` visitor → accepted; under a `&var self` receiver → refused.\n10. An enclosing window's parameter as the root (`with_var_ref` nested inside `with_var_ref`, the inner body touching the outer element parameter) → probe first, then decide from the evidence and report.\n11. Reached through an intermediate closure literal, `v.each { x in w.each { y in v.push(y) } }` → refused at the outer call.\n12. The driven twin of rows 1, 2 and 7, after a `yield_now()` → same verdicts.\n13. Inside an open `for x in v.iter()` window, a closure argument that implicitly captures `v` mutably → refused by the window check.\n14. Place-window lowerings (`v[i].mutate(…)`, `v[0] = w[1]`) are unchanged, and their existing tests stay green (the exclusion above).\n\nOut of scope: `Mutex.lock`/`SpinLock.lock` re-entry. Both sides are `&self`, so a re-entrant lock is a deadlock, not an exclusivity violation. Record that and move on.\n\n## Obligations\n- **Obligation 3:** conformance rows FIRST, beside K135 in `examples/conformance/`, with their `INDEX.md` lines, as the branch's first commit.\n- **Obligation 2 (consumer sweep):** this refuses programs that compile today. Before landing, list every tracked `.saw` file the new rule refuses (examples/, blade/, libs/, devtools/, selfhost/, prototypes/). Classify each one:\n  - A read-only use that the ruling now refuses is MIGRATED in this branch by spelling the capture (`[&v]` / `[&self]`), and listed in the report.\n  - A body that really mutates the root is a real hazard: report it; do not migrate it.\n  - Anything the rule refuses that is neither is a false positive against the rule: STOP and report.\n  - Never widen an exemption ad hoc.\n- **Diagnostic:** the ordinary exclusivity error naming both sides (the implicit capture and the receiver). The hint says the closure captures `v` as the `&var` it is, and to write `[&v]` for a shared capture. If the body writes `v`, the hint instead says to pass the element or parameter in rather than reaching the root. Pin the wording in examples/errors/.\n- **Docs:** LANGUAGE_SPEC.md's closure-capture section and the saw-lang skill state the rule (an implicit capture takes the binding as declared), with the `[&v]` idiom for a shared read inside a visitor.\n\n## Gates and files\n- Gates: full terminal battery; `astgraft` covers the new annotation.\n- Files: `sawc/typechecker/types.py` (the funnel), `sawc/typechecker/expressions.py` (the closure checker), `sawc/ast_nodes.py` (the annotation), `examples/conformance/`, `examples/errors/`.\n- Also LANGUAGE_SPEC.md and `.claude/skills/saw-lang/SKILL.md` (closure-capture sections only). SL-2 U0′ edits other sections of those two files concurrently, so resolve any rebase hunk by hunk.\n- Otherwise disjoint from the parser work (SL-2 U0′, SL-73).\n- SL-343 (with_ref windows across a suspension) wants this settled first. This brief does not change the structure of `windows.py`.\n","id":"SL-345","labels":["bug","borrows","soundness"],"order":2,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":9,"sequence":1439,"stage":"queued","status":"open","title":"USE-AFTER-FREE: a closure's captured reference-param root is not charged against the receiver of the call it is passed to (v.with_var_ref(0, { e in try! v.push(k)  e = 77 }) compiles; write lands in the freed buffer)","updated":"1790259959"},"id":"SL-345","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-air-sawlang\",\"diff\":\"diff --git a/.claude/skills/saw-lang/SKILL.md b/.claude/skills/saw-lang/SKILL.md\\nindex 20aca26f..dcaabc91 100644\\n--- a/.claude/skills/saw-lang/SKILL.md\\n+++ b/.claude/skills/saw-lang/SKILL.md\\n@@ -4308,6 +4308,42 @@ construct in the owner and lend `&driver` down.\\n   write through it is then refused as it would be in a `&self` method. Only the\\n   `&` forms exist — `[self]` and `[move self]` are parse errors, because the\\n   receiver's own mode decides the capture's.\\n+  **AN IMPLICIT CAPTURE TAKES THE BINDING AS DECLARED (SL-345, user ruling Sep\\n+  24).** Naming a `&var T` parameter (or `self` in a `&var self` method) holds it\\n+  EXCLUSIVELY for the whole call the closure is passed to, whatever the body\\n+  does; a `&T` one (or `self` in a `&self` method) holds it shared. The capture\\n+  joins the call's access set like a written `[&x]`, so a visitor body that\\n+  reaches its own receiver is the ordinary exclusivity error:\\n+  ```saw-error\\n+  // error-contains: the closure captures `v` as the `&var` binding it is\\n+  func overwrite_first(v: &var Vector<Int>) -> Int {\\n+      v.with_var_ref(0, { e in\\n+          let _ = v.push(9)       // would reallocate under `e`\\n+          e = 77\\n+      })\\n+      v[0]\\n+  }\\n+  ```\\n+  A READ-ONLY body is refused too (the mode is the declaration's, not the\\n+  body's). The idiom is to spell the shared capture — `[&v]`, or `[&self]`:\\n+  ```saw\\n+  func weighted(v: &var Vector<Int>) -> Int {\\n+      var total = 0\\n+      v.each({ [&v, &var total] x in total = total + x * v.len() })\\n+      total\\n+  }\\n+  ```\\n+  A body that WRITES the root has no spelling that helps: pass it the element\\n+  or a value computed before the call. Disjoint roots compose\\n+  (`a.each({ x in let _ = b.push(x) })`). Treat all of it as caught now and\\n+  SUSPECT in older builds, where every such call compiled: the `with_var_ref`\\n+  shape wrote into a freed buffer (its `self` twin died with SIGBUS), and a\\n+  pushing `each` walked a buffer that reallocated under it.\\n+  GOTCHA, driven bodies: the `[&v]` spelling of a reference PARAMETER and a\\n+  `[&self]` visitor that runs its body more than once both fail after a\\n+  suspension today (reported with SL-345), and a driven `[&var total]` loses\\n+  its writes (SL-315) — keep read-only visitors over reference parameters in\\n+  sync helpers for now.\\n   The enclosing method may be sync OR SUSPENDING (design 218 stage 3 closed\\n   DF-216g); both read the live receiver. Treat the suspending half as working\\n   now and SUSPECT in older builds, where it was an ICE. In a suspending body the\\ndiff --git a/LANGUAGE_SPEC.md b/LANGUAGE_SPEC.md\\nindex 7977b8f4..5ec05c82 100644\\n--- a/LANGUAGE_SPEC.md\\n+++ b/LANGUAGE_SPEC.md\\n@@ -3982,6 +3982,48 @@ refused exactly as it would be in a `&self` method. `self` appears in a capture\\n list only behind `&` or `&var` — `[self]` and `[move self]` are parse errors,\\n since the receiver's own mode decides the capture's.\\n \\n+**An implicit capture takes the binding as it is declared.** A closure that\\n+names a `&var T` parameter, or `self` in a `&var self` method, holds it\\n+exclusively for the whole call it is passed to, whatever the body does with it.\\n+One that names a `&T` parameter, or `self` in a `&self` method, holds it shared.\\n+The capture is one of the call's accesses: it meets the Law of Exclusivity\\n+beside the receiver and the other arguments, exactly as a written `[&x]` does.\\n+So a body cannot reach the collection that is lending it an element:\\n+\\n+```saw-error\\n+// error-contains: the closure captures `v` as the `&var` binding it is\\n+func overwrite_first(v: &var Vector<Int>) -> Int {\\n+    v.with_var_ref(0, { e in\\n+        let _ = v.push(9)       // would reallocate the buffer `e` points into\\n+        e = 77\\n+    })\\n+    v[0]\\n+}\\n+// error: exclusive access violation: the closure captures `v` as the `&var`\\n+// binding it is, and `v` is also accessed in the same call\\n+```\\n+\\n+The mode comes from the declaration and nothing else, so a body that only\\n+reads a `&var` binding is refused beside a visitor too. Write the capture you\\n+mean: `[&v]` takes a shared borrow, which composes with a shared receiver, and\\n+`[&self]` does the same for the receiver.\\n+\\n+```saw\\n+func weighted(v: &var Vector<Int>) -> Int {\\n+    var total = 0\\n+    v.each({ [&v, &var total] x in total = total + x * v.len() })\\n+    total\\n+}\\n+```\\n+\\n+Without `[&v]` the call is refused, and the diagnostic names `[&v]`. When the\\n+body writes the root, a shared capture cannot help; hand the body what it needs\\n+through the closure's parameter (the element the call lends, or a value\\n+computed before the call). Captures of different roots compose:\\n+`a.each({ x in let _ = b.push(x) })` walks `a` and grows `b`. A closure passed\\n+to an escaping parameter cannot capture a reference at all (above), so the rule\\n+concerns closures passed straight to the call that runs them.\\n+\\n The enclosing method may be `sync` or **suspending**; both compile and both read\\n the live receiver. A driven method's receiver lives behind the task frame, and\\n the frame lends it to the closure as a handle it duplicates for the purpose.\\ndiff --git a/examples/conformance/INDEX.md b/examples/conformance/INDEX.md\\nindex 7adfa7ec..3f8212f6 100644\\n--- a/examples/conformance/INDEX.md\\n+++ b/examples/conformance/INDEX.md\\n@@ -6,6 +6,10 @@ either a file in this directory or an existing `examples/` test that already\\n asserts the same rule at the same position — this table is the record of which,\\n and the dedup decisions are meant to be audited from it.\\n \\n+**580 rows: 300 carry a file here, 280 are covered elsewhere** (recounted Sep 24\\n+when SL-345 added K160-K164, a closure's implicit reference captures joining the\\n+call's access set — K160, K162 and K163 carry a file here, K161 and K164 point at\\n+`examples/errors/` files); previously, and on the same basis:\\n **575 rows: 297 carry a file here, 278 are covered elsewhere** (recounted Sep 22\\n when design 275 U3 added K132-K153, the statement-scoped window and the two\\n missing splits — K133, K135, K136, K137, K138, K140, K141 and K147 carry a file\\n@@ -753,6 +757,11 @@ Claim source: spec 6 *Send and Sync* + *Cooperative tasks*; designs 75, 88, 103,\\n | K147 | the INLINE `try EXPR catch { … }` SPLITS — a suspending call in its catch embeds, and a `break`/`continue` written there reaches the loop it names | `K147_inline_catch_splits.saw` + `coro_suspending_call_in_inline_catch.saw` + `coro_inline_catch_loop_exit_in_child_positions.saw` | SL-215, closed by design 275 U3. A `TryExpr` carrying a `catch_block` is the inline form, and nothing in the split walk ever looked at that slot: `_collect_calls`'s try/catch arm matched `TryCatchExpr` only, so a suspending call in the catch fell to the one rejector. THE SAME SLOT COST A SILENT MISCOMPILE beside the refusal — `_mark_ob_block`'s clause-2 marking lists `IfExpr`, `MatchExpr` and `TryCatchExpr` and not `TryExpr`, so a `try f() catch { break }` inside a spanning loop kept a RAW `break` that left the resume method's `while true` DISPATCH loop: the break was lost and a `continue` spun forever, at exit 0. The transform rewrites the inline form into the BLOCK form it means (`try { try EXPR } catch { B }`), so design 196 unit 3's split does the work and both faces close at once. Rows: the suspending catch, the ERROR BINDING across the suspension, the three `try` variants in one body, a routed `try(as E.Case)` beside an inline catch, and a catch that `break`s or `continue`s its loop — each against its sync twin. Each rewrite takes a FRESH error binding (`__saw_caughtN`) with the author's `error` reads renamed, because the block form's caught error travels in a frame field NAMED by the binding and two normalized catches in one body would otherwise share one field — measured as ``argument `value` expects `Alpha` but got `Beta``, and sawtracker's accept loop has exactly two. AT EVERY CHILD POSITION (codex r3 #1): the normalization walked its children by hand and stopped at anything not an `ASTNode` — `Argument` is a plain dataclass, a struct-literal field and a map entry are tuples — so an inline catch inside `id(…)`, `Pair(a: …)`, `{k: …}` or `read(&a[…])` was never rewritten and its raw `break` left the dispatcher (the code after the loop skipped, or the driver re-entering forever); it rides `ast_walk.map_nodes` now. The SAME symptom sat one mechanism over: the value-conditional lowering asked only \\\"spans a suspension\\\", so a normalized `try { } catch { break }` (and a value `if`/`match` arm that jumps) in a `let` or an argument inside a spanning loop lowered in place — `_vc_spans` asks the loop-exit clause too. The child-positions file is the matrix: every wrapper the funnel names × `break`/`continue` × `while`/collection `for`, plus the suspending catch, each against its sync twin |\\n | K148 | DF-196b's multi-error refusal is inherited whole by the inline form | `errors/inline_catch_multi_error.saw` | design 275 U3 / SL-215. A suspending `try { } catch { }` may raise only ONE error type — the catch would bind a union the split lowering cannot build — and a one-expression inline catch satisfies that by construction rather than by an exemption |\\n | K149 | the MOTIVATING CONSUMER's loop shape compiles and runs: a collection `for` whose body SUSPENDS, PROPAGATES an error, and writes a root that is not the one being iterated | `K149_sawtracker_loop_shape.saw` | SL-317's own repro, sawtracker `src/store.saw:285`. Three things at once, which is why it is the shape worth pinning rather than the minimal one; it was the clean refusal \\\"use a `while` loop over an index\\\" before U3. The io is replaced by a suspending read so the row runs in the ordinary battery — the SHAPE is the assertion, not the syscall — and the sync twin is the oracle |\\n+| K160 | NO USE-AFTER-FREE THROUGH A CAPTURED ROOT: a closure argument's IMPLICIT capture of a reference binding — a `&`/`&var` parameter, an enclosing closure's reference parameter, `self` in a borrowing method — joins the call's access set at the binding's DECLARED mode, so a body that reaches the receiver's root is the ordinary exclusivity error. Every `&self`/`&var self` visitor over a non-escaping closure: `with_var_ref`, `with_ref`, `each`, `map`, `fold`, `each_indexed`, `sort_by`, `Map.each`/`each_key`/`each_value`, `Set.each`, `Arc.with_unique`; plus a root reached through an intermediate closure, an enclosing window's own parameter, and the free-function / two-closure / generic call forms | `K160_implicit_capture_joins_the_call_access_set.saw` | SL-345. Design 216 captures a reference binding as a POINTER into the caller's storage, which is a live borrow for the whole call — and `_check_call_exclusivity` joined only the WRITTEN `[&x]` list, so the pointer went uncharged: `v.with_var_ref(0, { e in … v.push(k) … e = 77 })` compiled and wrote into the freed buffer (printed `1`; the `self` spelling died with SIGBUS). The closure checker now records each implicit reference capture's mode in one annotation (`ClosureExpr.reference_capture_access`) and the funnel reads it beside the written list, so the pairwise overlap test, design 189's task-borrow cross-check and design 275 U3's open-window check all see it. One file, one binding name per cell, so each expectation proves its own cell. `Mutex.lock`/`SpinLock.lock` re-entry is NOT a row: both sides are `&self`, so it is a deadlock, not an exclusivity violation |\\n+| K161 | AN IMPLICIT CAPTURE TAKES THE BINDING AS DECLARED — a `&var` binding (or `self` in a `&var self` method) joins EXCLUSIVE whatever the body does with it, so a read-only visitor body is refused, and the diagnostic names `[&v]` / `[&self]`; a body that WRITES the root is told to pass what it needs in through the closure's parameter instead | `errors/implicit_capture_read_only_visitor.saw` | SL-345, user ruling (Sep 24): declared-derived, not use-derived. The obvious meaning of an implicit capture is the variable as it stands in the enclosing scope; a user who wants less writes it down. The rule reads only the binding's declaration — no analysis of the body decides the MODE (the body is consulted only to pick which hint to show) |\\n+| K162 | …and what COMPOSES: a spelled `[&v]`/`[&self]` under a shared visitor, DISJOINT roots, a `&T` binding beside a shared receiver or a `&` argument, an enclosing window's parameter under a window on a disjoint root, a spelled `[&inner]` of an enclosing window's parameter, a closure that names no root, a spelled `[&v]` inside an open `for` window, and the PLACE-WINDOW lowerings, whose synthesized closure leaves the window's own root out of the access set | `K162_implicit_capture_controls_compose.saw` | SL-345. The half a rule that merely \\\"refused any closure naming the receiver\\\" would break. The place-window exclusion is the one name a window closure captures plainly on purpose (`place_window_root`): joining it would make every `v[i]` window conflict with itself. The place-window corpus (`place_*`, `closure_nested_ref_param_capture.saw`, the design-146/188 pins) is the rest of this row's coverage |\\n+| K163 | K160 and K161 hold in a DRIVEN body — a `&var` parameter or `self` captured after a `yield_now()` gets the same verdicts | `K163_implicit_capture_driven_twins.saw` | SL-345. The rule is judged on the source the author wrote, before the coroutine transform builds a frame, so a suspension cannot change a verdict. The ACCEPT twins are not pinned driven: a driven `[&v]` of a reference parameter and a driven `[&self]` under a visitor that runs its body more than once both fail today for reasons outside this rule (reported with SL-345's landing), and the driven `[&var total]` accumulator is SL-315 |\\n+| K164 | an implicit `&var` capture inside an open `for x in v.iter()` window is an exclusive access to the window's root, refused by the window's own check even when the body only reads | `errors/implicit_capture_inside_a_for_window.saw` | SL-345, beside K132/K134. The capture arrives at design 275 U3's window check through the call's access set, which is where every other closure capture already arrived; the spelled `[&v]` twin is a cell of K162 |\\n \\n \\n ## Visibility and module boundaries\\ndiff --git a/examples/conformance/K160_implicit_capture_joins_the_call_access_set.saw b/examples/conformance/K160_implicit_capture_joins_the_call_access_set.saw\\nnew file mode 100644\\nindex 00000000..61ff22e7\\n--- /dev/null\\n+++ b/examples/conformance/K160_implicit_capture_joins_the_call_access_set.saw\\n@@ -0,0 +1,198 @@\\n+// K160 (SL-345): AN IMPLICIT CAPTURE OF A REFERENCE BINDING JOINS THE CALL'S\\n+// ACCESS SET, at the mode the binding is DECLARED with.\\n+//\\n+// A closure passed straight to a non-escaping parameter captures a `&`/`&var`\\n+// parameter, an enclosing closure's reference parameter, or `self` in a\\n+// borrowing method as a POINTER into the caller's storage (design 216). That\\n+// pointer is a live borrow for the whole call, exactly as a written\\n+// `[&var x]` is, so it meets the Law of Exclusivity against the receiver, the\\n+// other arguments and the other closures' captures. A `&var` binding joins as\\n+// an exclusive borrow whatever the body does with it; a `&T` binding joins as\\n+// a shared one.\\n+//\\n+// Each cell names its root differently, so each expectation below proves one\\n+// cell was refused:\\n+//\\n+//   * `with_var_ref` on a `&var` parameter whose body pushes, then writes the\\n+//     element (the push reallocates under the element borrow)\\n+//   * the same through `self` in a `&var self` method\\n+//   * `with_ref` whose body pushes\\n+//   * each of `each`, `map`, `fold`, `each_indexed`, `sort_by`\\n+//   * `Map.each` / `each_key` / `each_value` and `Set.each`\\n+//   * `Arc.with_unique` whose body replaces the `Arc` it is borrowing through\\n+//   * a root reached only through an INTERMEDIATE closure literal\\n+//   * an enclosing window's own reference parameter as the root\\n+//   * the other call forms: a free function beside a `&` argument, two closure\\n+//     arguments both capturing one root, a generic function\\n+//   * a `&T` parameter as the receiver of a `&var self` visitor (refused by the\\n+//     immutable-receiver rule before the capture is compared at all)\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_with_var_ref` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `self` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_with_ref` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_each` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_map` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_fold` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_each_indexed` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_sort_by` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `m_each` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `m_each_key` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `m_each_value` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `s_each` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `a_unique` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_outer` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `inner` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_free` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_two` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the closure captures `v_generic` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: cannot call `&var self` method `sort_by` on immutable variable `v_shared`\\n+\\n+import std.arc.{Arc}\\n+\\n+struct Holder {\\n+    v: Vector<Int>\\n+}\\n+extension Holder: NoCopy {}\\n+\\n+extension Holder {\\n+    func grow_then_write(&var self) -> Int {\\n+        self.v.with_var_ref(0, { e in\\n+            var k = 0\\n+            while k < 64 {\\n+                let _ = self.v.push(k)\\n+                k = k + 1\\n+            }\\n+            e = 77\\n+        })\\n+        self.v[0]\\n+    }\\n+}\\n+\\n+func grow_then_write(v_with_var_ref: &var Vector<Int>) -> Int {\\n+    v_with_var_ref.with_var_ref(0, { e in\\n+        var k = 0\\n+        while k < 64 {\\n+            let _ = v_with_var_ref.push(k)\\n+            k = k + 1\\n+        }\\n+        e = 77\\n+    })\\n+    v_with_var_ref[0]\\n+}\\n+\\n+func with_ref_push(v_with_ref: &var Vector<Int>) -> Int {\\n+    v_with_ref.with_ref(0, { e in\\n+        let _ = v_with_ref.push(e)\\n+        e\\n+    })\\n+}\\n+\\n+func each_push(v_each: &var Vector<Int>) -> Int {\\n+    v_each.each({ x in let _ = v_each.push(x) })\\n+    v_each.len()\\n+}\\n+\\n+func map_push(v_map: &var Vector<Int>) -> Int {\\n+    let m = try! v_map.map<Int>({ x in let _ = v_map.push(x); x })\\n+    m.len()\\n+}\\n+\\n+func fold_push(v_fold: &var Vector<Int>) -> Int {\\n+    v_fold.fold<Int>(0, { acc, x in let _ = v_fold.push(x); acc + x })\\n+}\\n+\\n+func each_indexed_push(v_each_indexed: &var Vector<Int>) -> Int {\\n+    v_each_indexed.each_indexed({ i, x in let _ = v_each_indexed.push(i + x) })\\n+    v_each_indexed.len()\\n+}\\n+\\n+func sort_by_push(v_sort_by: &var Vector<Int>) -> Int {\\n+    v_sort_by.sort_by({ a, b in\\n+        let _ = v_sort_by.push(1)\\n+        if a < b { Ordering.Less } else { Ordering.Greater }\\n+    })\\n+    v_sort_by.len()\\n+}\\n+\\n+func map_each_insert(m_each: &var Map<Int, Int>) -> Int {\\n+    m_each.each({ k, x in m_each[k + 100] = x })\\n+    m_each.len()\\n+}\\n+\\n+func map_each_key_insert(m_each_key: &var Map<Int, Int>) -> Int {\\n+    m_each_key.each_key({ k in m_each_key[k + 100] = 1 })\\n+    m_each_key.len()\\n+}\\n+\\n+func map_each_value_insert(m_each_value: &var Map<Int, Int>) -> Int {\\n+    m_each_value.each_value({ x in m_each_value[x + 100] = 1 })\\n+    m_each_value.len()\\n+}\\n+\\n+func set_each_insert(s_each: &var Set<Int>) -> Int {\\n+    s_each.each({ x in let _ = s_each.insert(x + 100) })\\n+    s_each.len()\\n+}\\n+\\n+func with_unique_replace(a_unique: &var Arc<Vector<Int>>) -> Int {\\n+    let n = a_unique.with_unique({ inner in\\n+        a_unique = try! Arc(value: Vector<Int>())\\n+        inner.len()\\n+    })\\n+    n ?? -1\\n+}\\n+\\n+func through_an_intermediate(v_outer: &var Vector<Int>, w: &Vector<Int>) -> Int {\\n+    v_outer.each({ x in w.each({ y in let _ = v_outer.push(x + y) }) })\\n+    v_outer.len()\\n+}\\n+\\n+func enclosing_window_parameter(vv: &var Vector<Vector<Int>>) -> Int {\\n+    vv.with_var_ref(0, { inner in\\n+        inner.with_var_ref(0, { x in\\n+            let _ = inner.push(5)\\n+            x = 9\\n+        })\\n+    })\\n+    vv[0][0]\\n+}\\n+\\n+func apply(v: &Vector<Int>, body: () sync -> Void) -> Int {\\n+    body()\\n+    v.len()\\n+}\\n+\\n+func free_function_beside_a_reference(v_free: &var Vector<Int>) -> Int {\\n+    apply(&v_free, { let _ = v_free.push(1) })\\n+}\\n+\\n+func both(first: () sync -> Void, second: () sync -> Void) {\\n+    first()\\n+    second()\\n+}\\n+\\n+func two_closures(v_two: &var Vector<Int>) -> Int {\\n+    both({ let _ = v_two.push(1) }, { let _ = v_two.push(2) })\\n+    v_two.len()\\n+}\\n+\\n+func apply_generic<T>(x: &T, body: () sync -> Void) {\\n+    body()\\n+}\\n+\\n+func generic_function(v_generic: &var Vector<Int>) -> Int {\\n+    apply_generic(&v_generic, { let _ = v_generic.push(1) })\\n+    v_generic.len()\\n+}\\n+\\n+func shared_parameter_under_an_exclusive_visitor(v_shared: &Vector<Int>) -> Int {\\n+    v_shared.sort_by({ a, b in\\n+        if a < v_shared.len() { Ordering.Less } else { Ordering.Greater }\\n+    })\\n+    v_shared.len()\\n+}\\n+\\n+func main() {\\n+    var v: Vector<Int> = [1, 2, 3]\\n+    print(\\\"{}\\\", grow_then_write(&var v))\\n+}\\ndiff --git a/examples/conformance/K162_implicit_capture_controls_compose.saw b/examples/conformance/K162_implicit_capture_controls_compose.saw\\nnew file mode 100644\\nindex 00000000..bfba943f\\n--- /dev/null\\n+++ b/examples/conformance/K162_implicit_capture_controls_compose.saw\\n@@ -0,0 +1,161 @@\\n+// K162 (SL-345): THE OTHER HALF OF K160 — what an implicit or spelled capture\\n+// may still do beside the call it is passed to.\\n+//\\n+// K160 joins a closure's implicit reference captures to the call's access set.\\n+// Joining is not refusing: the overlap test decides, and these cells are the\\n+// ones it must let through, each measured by its output:\\n+//\\n+//   * a SPELLED shared capture `[&v]` of a `&var` parameter under a shared\\n+//     visitor — the migration for a read-only body\\n+//   * the same spelled `[&self]` in a `&var self` method\\n+//   * DISJOINT roots: the visitor walks `a` while the body pushes onto `b`\\n+//   * a `&T` parameter captured implicitly under a shared visitor (shared\\n+//     beside shared)\\n+//   * a `&T` parameter captured implicitly beside a `&` argument of it\\n+//   * an enclosing window's element parameter captured by a window on a\\n+//     DISJOINT root\\n+//   * a spelled `[&inner]` of an enclosing window's own parameter under a\\n+//     shared visitor on it\\n+//   * a closure that does not name the root at all\\n+//   * a spelled `[&v]` inside an open `for x in v.iter()` window\\n+//   * the PLACE-WINDOW lowerings (`v[0].bump(...)`, `v[1] = w[0]`, a receiver\\n+//     field in a `&var self` method): their synthesized closure leaves the\\n+//     window's own root out of the access set, so a window never conflicts\\n+//     with itself\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// spelled shared param 18\\n+// spelled shared self 18\\n+// disjoint roots 6\\n+// shared beside shared 15\\n+// shared beside a shared argument 3\\n+// window on a disjoint root 14\\n+// spelled enclosing window parameter 15\\n+// no capture of the root 2\\n+// spelled inside a for window 15\\n+// place windows 32\\n+// place window on a receiver field 12\\n+\\n+struct Tally {\\n+    items: Vector<Int>\\n+}\\n+extension Tally: NoCopy {}\\n+\\n+extension Tally {\\n+    func weighted(&var self) -> Int {\\n+        var total = 0\\n+        self.items.each({ [&self, &var total] x in total = total + x * self.items.len() })\\n+        total\\n+    }\\n+}\\n+\\n+func spelled_shared_param(v: &var Vector<Int>) -> Int {\\n+    var total = 0\\n+    v.each({ [&v, &var total] x in total = total + x * v.len() })\\n+    total\\n+}\\n+\\n+func disjoint_roots(a: &var Vector<Int>, b: &var Vector<Int>) -> Int {\\n+    a.each({ x in let _ = b.push(x) })\\n+    b.len()\\n+}\\n+\\n+func shared_beside_shared(v: &Vector<Int>) -> Int {\\n+    var total = 0\\n+    v.each({ [&var total] x in total = total + x + v.len() })\\n+    total\\n+}\\n+\\n+func apply(v: &Vector<Int>, body: () sync -> Int) -> Int {\\n+    body() + v.len()\\n+}\\n+\\n+func shared_beside_a_shared_argument(v: &Vector<Int>) -> Int {\\n+    apply(&v, { v.len() - 3 })\\n+}\\n+\\n+func window_on_a_disjoint_root(vv: &var Vector<Int>, ww: &var Vector<Int>) -> Int {\\n+    vv.with_var_ref(0, { e in\\n+        ww.with_var_ref(0, { f in f = f + e })\\n+    })\\n+    ww[0]\\n+}\\n+\\n+func spelled_enclosing_window_parameter(vv: &var Vector<Vector<Int>>) -> Int {\\n+    var total = 0\\n+    vv.with_var_ref(0, { [&var total] inner in\\n+        inner.each({ [&inner, &var total] x in total = total + x + inner.len() })\\n+    })\\n+    total\\n+}\\n+\\n+func no_capture_of_the_root(v: &var Vector<Int>) -> Int {\\n+    v.with_var_ref(0, { e in e = e + 1 })\\n+    v[0]\\n+}\\n+\\n+func run(body: () sync -> Void) {\\n+    body()\\n+}\\n+\\n+func spelled_inside_a_for_window(v: &var Vector<Int>) -> Int {\\n+    var total = 0\\n+    for x in v.iter() {\\n+        run({ [&v, &var total] in total = total + x + v.len() })\\n+    }\\n+    total\\n+}\\n+\\n+struct Cell {\\n+    n: Int\\n+}\\n+\\n+extension Cell {\\n+    func bump(&var self, by: Int) {\\n+        self.n = self.n + by\\n+    }\\n+}\\n+\\n+func place_windows(v: &var Vector<Cell>, w: &Vector<Cell>) -> Int {\\n+    v[0].bump(by: w[0].n)\\n+    v[1] = Cell(n: w[0].n + v[0].n)\\n+    v[0].n + v[1].n\\n+}\\n+\\n+struct Board {\\n+    cells: Vector<Cell>,\\n+    step: Int\\n+}\\n+extension Board: NoCopy {}\\n+\\n+extension Board {\\n+    func advance(&var self) -> Int {\\n+        self.cells[0].bump(by: self.step)\\n+        self.cells[0].n\\n+    }\\n+}\\n+\\n+func main() {\\n+    var v: Vector<Int> = [1, 2, 3]\\n+    print(\\\"spelled shared param {}\\\", spelled_shared_param(&var v))\\n+    var t = Tally(items: [1, 2, 3])\\n+    print(\\\"spelled shared self {}\\\", t.weighted())\\n+    var a: Vector<Int> = [1, 2, 3]\\n+    var b: Vector<Int> = [4, 5, 6]\\n+    print(\\\"disjoint roots {}\\\", disjoint_roots(&var a, &var b))\\n+    print(\\\"shared beside shared {}\\\", shared_beside_shared(&v))\\n+    print(\\\"shared beside a shared argument {}\\\", shared_beside_a_shared_argument(&v))\\n+    var p: Vector<Int> = [4]\\n+    var q: Vector<Int> = [10]\\n+    print(\\\"window on a disjoint root {}\\\", window_on_a_disjoint_root(&var p, &var q))\\n+    var vv: Vector<Vector<Int>> = [[1, 2, 3]]\\n+    print(\\\"spelled enclosing window parameter {}\\\", spelled_enclosing_window_parameter(&var vv))\\n+    var one: Vector<Int> = [1]\\n+    print(\\\"no capture of the root {}\\\", no_capture_of_the_root(&var one))\\n+    print(\\\"spelled inside a for window {}\\\", spelled_inside_a_for_window(&var v))\\n+    var cells: Vector<Cell> = [Cell(n: 1), Cell(n: 2)]\\n+    let src: Vector<Cell> = [Cell(n: 10)]\\n+    print(\\\"place windows {}\\\", place_windows(&var cells, &src))\\n+    var board = Board(cells: [Cell(n: 5)], step: 7)\\n+    print(\\\"place window on a receiver field {}\\\", board.advance())\\n+}\\ndiff --git a/examples/conformance/K163_implicit_capture_driven_twins.saw b/examples/conformance/K163_implicit_capture_driven_twins.saw\\nnew file mode 100644\\nindex 00000000..81931ba6\\n--- /dev/null\\n+++ b/examples/conformance/K163_implicit_capture_driven_twins.saw\\n@@ -0,0 +1,74 @@\\n+// K163 (SL-345): K160 and K161 IN A DRIVEN BODY — the same verdicts after a\\n+// suspension.\\n+//\\n+// The enclosing function suspends (`yield_now()`), so its reference parameters\\n+// and its receiver live in a coroutine frame. The rule is about the capture as\\n+// the author wrote it, so it is judged on the source the author wrote, before\\n+// the frame exists, and a suspension changes nothing:\\n+//\\n+//   * `with_var_ref` whose body pushes onto a `&var` parameter root\\n+//   * the same through `self` in a `&var self` method\\n+//   * a read-only body naming a `&var` parameter under a shared visitor\\n+//   * the same through `self`\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: the closure captures `grow` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: the body writes through `grow`, which this call is using\\n+// EXPECT-ERROR-CONTAINS: the body writes through `self`, which this call is using\\n+// EXPECT-ERROR-CONTAINS: the closure captures `read` as the `&var` binding it is\\n+// EXPECT-ERROR-CONTAINS: The body only reads `read`: write `[&read]` to capture a shared borrow instead\\n+// EXPECT-ERROR-CONTAINS: The body only reads `self`: write `[&self]` to capture a shared borrow instead\\n+\\n+import std.task.{yield_now}\\n+\\n+struct Tally {\\n+    items: Vector<Int>\\n+}\\n+extension Tally: NoCopy {}\\n+\\n+extension Tally {\\n+    func grow_then_write(&var self) -> Int {\\n+        yield_now()\\n+        self.items.with_var_ref(0, { e in\\n+            var k = 0\\n+            while k < 64 {\\n+                let _ = self.items.push(k)\\n+                k = k + 1\\n+            }\\n+            e = 77\\n+        })\\n+        self.items[0]\\n+    }\\n+\\n+    func weighted(&var self) -> Int {\\n+        yield_now()\\n+        var total = 0\\n+        self.items.each({ [&var total] x in total = total + x * self.items.len() })\\n+        total\\n+    }\\n+}\\n+\\n+func grow_then_write(grow: &var Vector<Int>) -> Int {\\n+    yield_now()\\n+    grow.with_var_ref(0, { e in\\n+        var k = 0\\n+        while k < 64 {\\n+            let _ = grow.push(k)\\n+            k = k + 1\\n+        }\\n+        e = 77\\n+    })\\n+    grow[0]\\n+}\\n+\\n+func weighted(read: &var Vector<Int>) -> Int {\\n+    yield_now()\\n+    var total = 0\\n+    read.each({ [&var total] x in total = total + x * read.len() })\\n+    total\\n+}\\n+\\n+func main() {\\n+    var v: Vector<Int> = [1, 2, 3]\\n+    print(\\\"{}\\\", grow_then_write(&var v))\\n+    print(\\\"{}\\\", weighted(&var v))\\n+}\\ndiff --git a/examples/errors/implicit_capture_inside_a_for_window.saw b/examples/errors/implicit_capture_inside_a_for_window.saw\\nnew file mode 100644\\nindex 00000000..6f6632fd\\n--- /dev/null\\n+++ b/examples/errors/implicit_capture_inside_a_for_window.saw\\n@@ -0,0 +1,28 @@\\n+// K164 (SL-345): AN IMPLICIT `&var` CAPTURE MEETS AN OPEN `for` WINDOW.\\n+//\\n+// `for x in v.iter()` holds `v` shared for the whole statement (K132). A\\n+// closure argument inside the body that names the `&var` parameter `v`\\n+// captures it exclusively (K161), so it is an exclusive access to the\\n+// window's root — refused by the window's own check, even though this body\\n+// only reads `v.len()`. The spelled `[&v]` twin composes with the window; it is\\n+// a cell of K162.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: exclusive access violation: `v` cannot be accessed by reference here — the window opened at line\\n+// EXPECT-ERROR-CONTAINS: a `for` over a borrowed iterator holds the collection for the whole loop\\n+\\n+func run(body: () sync -> Void) {\\n+    body()\\n+}\\n+\\n+func spread(v: &var Vector<Int>) -> Int {\\n+    var total = 0\\n+    for x in v.iter() {\\n+        run({ [&var total] in total = total + x + v.len() })\\n+    }\\n+    total\\n+}\\n+\\n+func main() {\\n+    var v: Vector<Int> = [1, 2, 3]\\n+    print(\\\"{}\\\", spread(&var v))\\n+}\\ndiff --git a/examples/errors/implicit_capture_read_only_visitor.saw b/examples/errors/implicit_capture_read_only_visitor.saw\\nnew file mode 100644\\nindex 00000000..ed5dceb9\\n--- /dev/null\\n+++ b/examples/errors/implicit_capture_read_only_visitor.saw\\n@@ -0,0 +1,52 @@\\n+// K161 (SL-345): AN IMPLICIT CAPTURE TAKES THE BINDING AS DECLARED — a\\n+// read-only body does not narrow it.\\n+//\\n+// `v` is a `&var` parameter, so a closure that names it holds it exclusively\\n+// for the whole call, even though this body only reads `v.len()`. The visitor\\n+// borrows `v` shared as its receiver, and the two overlap. The hint names the\\n+// spelling that asks for less: `[&v]` captures a shared borrow, which composes\\n+// with the shared receiver (see K162).\\n+//\\n+// `self` in a `&var self` method is the same rule over the receiver spelling,\\n+// and its hint names `[&self]`.\\n+//\\n+// The third cell's body WRITES the root, so a shared capture would not help;\\n+// its hint says to hand the body what it needs through the closure's\\n+// parameter instead of reaching the root from inside.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: exclusive access violation: the closure captures `v` as the `&var` binding it is, and `v` is also accessed in the same call\\n+// EXPECT-ERROR-CONTAINS: The body only reads `v`: write `[&v]` to capture a shared borrow instead\\n+// EXPECT-ERROR-CONTAINS: exclusive access violation: the closure captures `self` as the `&var` binding it is, and `self.items` is also accessed in the same call\\n+// EXPECT-ERROR-CONTAINS: The body only reads `self`: write `[&self]` to capture a shared borrow instead\\n+// EXPECT-ERROR-CONTAINS: the body writes through `w`, which this call is using\\n+// EXPECT-ERROR-ABSENT: write `[&w]`\\n+\\n+struct Tally {\\n+    items: Vector<Int>\\n+}\\n+extension Tally: NoCopy {}\\n+\\n+extension Tally {\\n+    func weighted(&var self) -> Int {\\n+        var total = 0\\n+        self.items.each({ [&var total] x in total = total + x * self.items.len() })\\n+        total\\n+    }\\n+}\\n+\\n+func weighted(v: &var Vector<Int>) -> Int {\\n+    var total = 0\\n+    v.each({ [&var total] x in total = total + x * v.len() })\\n+    total\\n+}\\n+\\n+func doubled(w: &var Vector<Int>) -> Int {\\n+    w.each({ x in let _ = w.push(x * 2) })\\n+    w.len()\\n+}\\n+\\n+func main() {\\n+    var v: Vector<Int> = [1, 2, 3]\\n+    print(\\\"{}\\\", weighted(&var v))\\n+    print(\\\"{}\\\", doubled(&var v))\\n+}\\ndiff --git a/sawc/ast_nodes.py b/sawc/ast_nodes.py\\nindex b0cac3a2..1c01b10a 100644\\n--- a/sawc/ast_nodes.py\\n+++ b/sawc/ast_nodes.py\\n@@ -2166,6 +2166,13 @@ class ClosureExpr(Expression):\\n     # inside the open window, is refused (design 188).\\n     is_place_window: bool = annotation(False)\\n     place_window_root: Optional[str] = annotation(None)\\n+    # Each implicit capture of a reference binding (a `&`/`&var` parameter, an\\n+    # enclosing closure's reference parameter, `self` in a borrowing method) ->\\n+    # 'mut' or 'imm', the mode the binding is declared with. The capture is a\\n+    # pointer into the caller's storage, so the call-exclusivity funnel joins\\n+    # it to the access set beside the written `[&x]` list. None until the\\n+    # closure checker has run; the funnel treats None as an invariant failure.\\n+    reference_capture_access: Optional[Dict[str, str]] = annotation(None)\\n \\n \\n # Statements\\ndiff --git a/sawc/typechecker/expressions.py b/sawc/typechecker/expressions.py\\nindex c0c6db0f..de42ab0f 100644\\n--- a/sawc/typechecker/expressions.py\\n+++ b/sawc/typechecker/expressions.py\\n@@ -13366,6 +13366,42 @@ class ExpressionsMixin:\\n                                      line=expr.line, column=expr.column))\\n         expr.capture_specs = specs\\n \\n+    def _implicit_reference_capture_access(self, expr, captures, spec_by_name,\\n+                                           outer_scope, self_capture_mode,\\n+                                           borrow_ok) -> Dict[str, str]:\\n+        \\\"\\\"\\\"Each implicit capture that lowers to a pointer into the caller's\\n+        storage -> the access it holds for the call: 'mut' or 'imm'.\\n+\\n+        The mode is the binding's declared one, never what the body does with\\n+        it: a `&var` binding (or `self` in a `&var self` method) is held\\n+        exclusively, a `&T` binding shared. A written `[&x]` names its own\\n+        mode and is joined from the spec list instead. The place-window\\n+        closure's receiver root is left out: that one name is a plain capture\\n+        on purpose, and joining it would make every `v[i]` window conflict\\n+        with itself. An escaping closure contributes nothing — its reference\\n+        capture is already refused by the frame-pointer rule (SL-345).\\n+        \\\"\\\"\\\"\\n+        access: Dict[str, str] = {}\\n+        if not borrow_ok:\\n+            return access\\n+        window_root = (getattr(expr, 'place_window_root', None)\\n+                       if getattr(expr, 'is_place_window', False) else None)\\n+        for name in captures:\\n+            if name in spec_by_name or name == window_root:\\n+                continue\\n+            if name == 'self':\\n+                if self_capture_mode is None:\\n+                    continue\\n+                exclusive = (self_capture_mode == 'ref_var'\\n+                             and self._self_borrow_is_exclusive())\\n+                access[name] = 'mut' if exclusive else 'imm'\\n+                continue\\n+            info = outer_scope.lookup(name)\\n+            ctype = info.type if info is not None else None\\n+            if ctype is not None and ctype.kind == TypeKind.REFERENCE:\\n+                access[name] = 'mut' if ctype.reference_mutable else 'imm'\\n+        return access\\n+\\n     def _place_window_root_capture_error(self, expr, cap_name, ctype) -> bool:\\n         \\\"\\\"\\\"DF-248a: the window body names the window's OWN ROOT. Reported here,\\n         or False when this is not that case.\\n@@ -13916,6 +13952,9 @@ class ExpressionsMixin:\\n                     continue\\n                 self._frame_pointer_escape_error(\\n                     expr, cap_name, cap_type, is_self_borrow)\\n+        expr.reference_capture_access = self._implicit_reference_capture_access(\\n+            expr, captures, spec_by_name, outer_scope, self_capture_mode,\\n+            borrow_ok)\\n         # Escape analysis (design 21b E1): a closure used in value position (bound\\n         # to a let/var, returned, stored, or a struct field) outlives the frame\\n         # that built it, so its environment must be heap-allocated with captured\\ndiff --git a/sawc/typechecker/types.py b/sawc/typechecker/types.py\\nindex 55a0b248..2d882b6d 100644\\n--- a/sawc/typechecker/types.py\\n+++ b/sawc/typechecker/types.py\\n@@ -17,7 +17,8 @@ from ast_nodes import (\\n     MemberAccess, ArrayIndex, TupleIndex, SelfExpr, ClosureExpr,\\n     BindOptional, OptionalEvalExpr, ForceUnwrap, MethodCall, CastExpr,\\n     IfExpr, IfLetExpr, MatchExpr, TryExpr, TryCatchExpr,\\n-    OptionalWrap, ResultOkWrap, ResultErrWrap, expr_diverges\\n+    OptionalWrap, ResultOkWrap, ResultErrWrap, expr_diverges,\\n+    AssignStatement, CompoundAssignStatement, OptionalChainAssign\\n )\\n from ast_walk import child_nodes\\n from errors import ErrorKind\\n@@ -5838,15 +5839,21 @@ class TypeUtilsMixin:\\n         THE ACCESS SET, in collection order: the receiver; each `&`/`&var`\\n         argument; each `move` argument; each `o.take()` argument's receiver\\n         (design 131 -- it writes `None` back during call setup); each borrow\\n-        CAPTURE of a closure argument (design 16/29); and each `&`/`&var` a\\n-        NESTED call in this argument list creates (design 199 -- an argument's\\n-        borrow extends over the whole call expression). Live task-capture\\n-        borrows are cross-checked against the whole set (design 189) before the\\n-        pairwise overlap pass runs within it.\\n+        CAPTURE of a closure argument, written (`[&x]`, design 16/29) or\\n+        implicit (a reference binding or `self` the body names, at its declared\\n+        mode -- `ClosureExpr.reference_capture_access`, SL-345); and each\\n+        `&`/`&var` a NESTED call in this argument list creates (design 199 --\\n+        an argument's borrow extends over the whole call expression). Live\\n+        task-capture borrows are cross-checked against the whole set (design\\n+        189) before the pairwise overlap pass runs within it.\\n+\\n+        Every entry point checks its arguments before calling here, so a\\n+        closure argument's captures are already recorded; one that is not is\\n+        an internal error, never an empty set.\\n \\n         ENTRY POINTS (obligation 1 -- a funnel names its entries). Every call\\n-        form in the language reaches the Law through here, and all fifteen live\\n-        in `typechecker/expressions.py`:\\n+        form in the language reaches the Law through here, and every entry\\n+        lives in `typechecker/expressions.py`:\\n           * `_check_function_call` (two sites: the plain call and the generic\\n             instantiation), `_check_overloaded_function_call`\\n           * `_check_method_call`, `_check_overloaded_method_call`,\\n@@ -5854,10 +5861,12 @@ class TypeUtilsMixin:\\n             `_check_type_param_method_call`, `_check_existential_method_call`\\n           * `_check_static_method_call`, `_check_overloaded_static_method_call`\\n           * `_check_module_function_call`, `_check_overloaded_module_function_call`\\n-          * `_init_matches` and `_check_module_struct_init` -- a memberwise\\n-            struct literal, whose field values are argument positions\\n+          * `_check_struct_init` and `_check_module_struct_init` -- a\\n+            memberwise struct literal, whose field values are argument positions\\n           * `_check_optional_take` -- `o.take()` as a call in its own right\\n             (receiver only, no arguments)\\n+          * `_check_optional_presence` -- `o.is_some()` / `o.is_none()`\\n+            (receiver only, no arguments)\\n         A new call form that does not route through this method is a hole in\\n         the Law, not a missing feature: add the entry here and to this list.\\n         \\\"\\\"\\\"\\n@@ -5870,6 +5879,9 @@ class TypeUtilsMixin:\\n         # cross-check can say \\\"captured into another task\\\" rather than the\\n         # generic by-reference phrasing.\\n         capture_entries = set()\\n+        # id(name node) -> (name, kind, closure) for each implicit reference\\n+        # capture, so an overlap names the capture and says how to spell less.\\n+        implicit_captures = {}\\n \\n         # A method receiver is always a borrow (`&self`/`&var self` -- the parser\\n         # requires it; static/init calls pass receiver=None). Collect it either\\n@@ -5940,6 +5952,28 @@ class TypeUtilsMixin:\\n                     capture_entries.add(id(name_expr))\\n                     entries.append(('mut' if spec.mode == 'ref_var' else 'imm',\\n                                     path, name_expr, spec.line, spec.column))\\n+                # An implicit capture of a reference binding is the same\\n+                # pointer a written `[&x]` is, held at the binding's declared\\n+                # mode (SL-345).\\n+                access = value.reference_capture_access\\n+                if access is None:\\n+                    raise AssertionError(\\n+                        f\\\"the closure argument at line {value.line} reached \\\"\\n+                        f\\\"the call-exclusivity check before `_check_closure` \\\"\\n+                        f\\\"recorded its reference captures; every entry point \\\"\\n+                        f\\\"of `_check_call_exclusivity` checks its arguments \\\"\\n+                        f\\\"first\\\")\\n+                for name, kind in access.items():\\n+                    name_expr = (SelfExpr(line=value.line, column=value.column)\\n+                                 if name == 'self' else\\n+                                 Identifier(name=name, line=value.line,\\n+                                            column=value.column))\\n+                    path = self._build_access_path(name_expr)\\n+                    if path is None:\\n+                        continue\\n+                    implicit_captures[id(name_expr)] = (name, kind, value)\\n+                    entries.append((kind, path, name_expr,\\n+                                    value.line, value.column))\\n \\n         # An argument's borrow extends over the WHOLE call expression, nested\\n         # calls included (design 199, closing DF-188j): a `&`/`&var` written\\n@@ -6027,6 +6061,17 @@ class TypeUtilsMixin:\\n                 else:\\n                     m_expr, m_line, m_col = ej, lj, cj\\n                     other = ei\\n+                # An implicit capture is named first: nothing at the call site\\n+                # spells it, so the other side alone would not say what to fix.\\n+                implicit = next((e for e in (ei, ej)\\n+                                 if id(e) in implicit_captures), None)\\n+                if implicit is not None:\\n+                    self._report_implicit_capture_overlap(\\n+                        implicit_captures[id(implicit)],\\n+                        ej if implicit is ei else ei,\\n+                        lj if implicit is ej else li,\\n+                        cj if implicit is ej else ci)\\n+                    continue\\n                 # A place window is not an ordinary path, so it gets the\\n                 # diagnostic that says what it holds (design 188 unit 2). The\\n                 # place may sit UNDER a `!` or a `?.` hop (SL-333's sweep), so\\n@@ -6100,6 +6145,77 @@ class TypeUtilsMixin:\\n         if place_entries:\\n             self._deferred_place_access.append((entries, place_entries))\\n \\n+    def _report_implicit_capture_overlap(self, record, other, line, column):\\n+        \\\"\\\"\\\"The exclusivity error for an implicit closure capture that overlaps\\n+        another access of the same call (SL-345). `record` is the funnel's\\n+        `(name, kind, closure)`; `other` is the access it collides with.\\\"\\\"\\\"\\n+        name, kind, closure = record\\n+        sigil = '&var' if kind == 'mut' else '&'\\n+        if kind != 'mut':\\n+            hint = (f\\\"an implicit capture takes the binding as it is declared, \\\"\\n+                    f\\\"so the closure holds a shared borrow of `{name}` for the \\\"\\n+                    f\\\"whole call, and a `&var` access cannot overlap it\\\")\\n+        elif self._closure_writes_root(closure, name):\\n+            hint = (f\\\"the body writes through `{name}`, which this call is \\\"\\n+                    f\\\"using. Pass what the body needs in through the closure's \\\"\\n+                    f\\\"parameter (the element the call lends, or a value \\\"\\n+                    f\\\"computed before the call) instead of reaching `{name}` \\\"\\n+                    f\\\"from inside\\\")\\n+        else:\\n+            hint = (f\\\"an implicit capture takes the binding as it is declared, \\\"\\n+                    f\\\"so the closure holds `{name}` exclusively for the whole \\\"\\n+                    f\\\"call. The body only reads `{name}`: write `[&{name}]` to \\\"\\n+                    f\\\"capture a shared borrow instead\\\")\\n+        self._error(\\n+            ErrorKind.EXCLUSIVITY_VIOLATION,\\n+            f\\\"exclusive access violation: the closure captures `{name}` as the \\\"\\n+            f\\\"`{sigil}` binding it is, and `{self._render_lvalue_path(other)}` \\\"\\n+            f\\\"is also accessed in the same call\\\",\\n+            line, column, hint=hint)\\n+\\n+    def _closure_writes_root(self, closure, name) -> bool:\\n+        \\\"\\\"\\\"Does `closure`'s body (nested closures included) write through the\\n+        binding `name`? Chooses the hint only; the capture's mode never depends\\n+        on it. A write is an assignment whose target roots in `name`, a `&var`\\n+        of a path rooted there, or a `&var self` method called on one.\\\"\\\"\\\"\\n+        def roots_here(node) -> bool:\\n+            path = self._build_access_path(node) if node is not None else None\\n+            return path is not None and path[0] == name\\n+\\n+        def mutating_call(call) -> bool:\\n+            if getattr(call, 'place_lowered', False):\\n+                return bool(getattr(call, 'place_receiver_exclusive', False))\\n+            receiver_type = getattr(call.object, 'resolved_type', None)\\n+            if (receiver_type is not None\\n+                    and receiver_type.kind == TypeKind.REFERENCE):\\n+                receiver_type = receiver_type.inner_type\\n+            owner = getattr(receiver_type, 'struct_name', None) or getattr(\\n+                receiver_type, 'enum_name', None)\\n+            if owner is None:\\n+                return False\\n+            info = self.namespace.lookup_method(owner, call.method_name)\\n+            return bool(getattr(info, 'self_mutable', False))\\n+\\n+        stack = [closure.body]\\n+        seen = set()\\n+        while stack:\\n+            node = stack.pop()\\n+            if node is None or id(node) in seen:\\n+                continue\\n+            seen.add(id(node))\\n+            if isinstance(node, (AssignStatement, CompoundAssignStatement,\\n+                                 OptionalChainAssign)):\\n+                if roots_here(node.target):\\n+                    return True\\n+            elif isinstance(node, ReferenceExpr):\\n+                if node.mutable and roots_here(node.expr):\\n+                    return True\\n+            elif isinstance(node, MethodCall):\\n+                if roots_here(node.object) and mutating_call(node):\\n+                    return True\\n+            stack.extend(child_nodes(node))\\n+        return False\\n+\\n     def _reference_access_mode(self, ref):\\n         \\\"\\\"\\\"THE mode a by-reference argument borrows its ROOT at, and the place\\n         that decides it (SL-333 R4).\\n\",\"diff_bytes\":48663,\"diff_files\":11,\"diff_hunks\":19,\"diff_tail\":\"ts ROOT at, and the place\\n         that decides it (SL-333 R4).\\n\",\"id\":\"SL-345\",\"request_id\":\"pid-6258-1790271822-6000\",\"title\":\"An implicit reference capture joins the call's access set (declared-derived, user ruling c2): the with_var_ref use-after-free and its self twin are refused; a read-only visitor spells [&v]\"}","request_id":"pid-6258-1790271822-6000","sequence":1463,"timestamp":"1790271823"}
---
