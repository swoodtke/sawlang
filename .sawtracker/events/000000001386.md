---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"TIMELINE (lead, Sep 23, EDT): 17:19 lock held by an agent gate; 17:34 the lead's SL-371 battery takes it (worktree sl371, chained until-mkdir/battery/rmdir); 18:56 the lock dir is RE-CREATED (mtime) while that battery is mid-gmgate — agent:claude-sl2-u0's selfhostlex+lexdiff re-run (its worktree's artifacts 18:55-18:58) cleared a live lock as stale and took it, then released it; 19:07 (23:07Z) the server's SL-357.p3 gate takes the lock; 19:10:19 the lead's battery ends and its chained rmdir removes the SERVER's lock; 19:19 the server's build.sh EXIT trap rmdir fails and set -e turns a passing gate into FAILED. Overlaps: the agent's two lanes (2 min) and the server's suite start (3 min) ran concurrently with the lead's freestanding stage; all three sets of results passed, so no result is suspect. The structural fault is the unowned release; the fix in the description makes a foreign release impossible and a stale judgment verifiable.","created":"1790205692","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-374.p1 (revision 1): SL-374: owner-checked suite lock — tools/suite_lock.sh is the one taker and releaser (run/acquire/release/status/clear-stale); build.sh uses it; suitelock lane; CLAUDE.md names it the only form","created":"1790206061","id":"c2"}],"created":"1790205652","description":"FOUND by agent:claude-air-sawlang (chat m120, Sep 23): SL-357.p3 r1's server gate passed everything (suite 2656/10 xfail, freestanding 36/36) but exited 1 because build.sh's EXIT trap ran 'rmdir $LOCK' after something else had removed the lock mid-run (23:07-23:19Z). MECHANISM (obligation 4, a class): the lock is a bare mkdir at /private/tmp/claude-<uid>/saw-suite-lock, and EVERY release is an unconditional rmdir by whoever reaches that line — build.sh's trap, tools/battery.sh's callers, and each agent's hand-written 'until mkdir ...; gate; rmdir' chain. Nothing records who holds it, so (a) a taker whose mkdir-wait was killed by a tool timeout proceeds as if it held the lock and later rmdir's someone else's, (b) a 'stale' judgment cannot be verified, and (c) once one lock is wrongly released, the NEXT taker's lock is released by the original holder's chained rmdir at its end — a chain that the lead's own SL-371 battery (17:34-19:2x EDT) most likely continued: its lock dir was re-created at 18:56 while its battery was mid-run, so its end-of-run rmdir removed the server's. Overlapping suite runs are the other consequence. FIX: (1) ONE script, tools/suite_lock.sh: 'acquire' does mkdir + writes owner (pid, host, start time, label) into the dir, waiting with a bounded sleep; 'release' removes the dir ONLY if the owner file names the caller's pid (else prints who holds it and exits 0 without touching it); 'run -- CMD...' = acquire, run, release-own in one process with the trap tolerant of a missing dir and the command's real exit status preserved; 'status' prints the holder and whether its pid is alive; 'clear-stale' removes it only when the recorded pid is dead. (2) build.sh test and tools/battery.sh use it; CLAUDE.md's suite-lock paragraph and every agent prompt say 'tools/suite_lock.sh run -- <gate>' instead of the hand-written chain; the sawos repo keeps its own path via an env var. (3) The Air's suggested trap hardening is subsumed: release-own never fails the run. Harness change, not a compiler branch; gate = the script's own tests (acquire/release/foreign-release refused/stale cleared) + one suite run through it.","id":"SL-374","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":3,"sequence":1386,"stage":"backlog","status":"open","title":"The suite lock is unowned: any holder's chained rmdir can release ANOTHER holder's lock (SL-357.p3 gate reported FAILED after a passing run); make the lock owner-checked and give every taker one script","updated":"1790206061"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"TIMELINE (lead, Sep 23, EDT): 17:19 lock held by an agent gate; 17:34 the lead's SL-371 battery takes it (worktree sl371, chained until-mkdir/battery/rmdir); 18:56 the lock dir is RE-CREATED (mtime) while that battery is mid-gmgate — agent:claude-sl2-u0's selfhostlex+lexdiff re-run (its worktree's artifacts 18:55-18:58) cleared a live lock as stale and took it, then released it; 19:07 (23:07Z) the server's SL-357.p3 gate takes the lock; 19:10:19 the lead's battery ends and its chained rmdir removes the SERVER's lock; 19:19 the server's build.sh EXIT trap rmdir fails and set -e turns a passing gate into FAILED. Overlaps: the agent's two lanes (2 min) and the server's suite start (3 min) ran concurrently with the lead's freestanding stage; all three sets of results passed, so no result is suspect. The structural fault is the unowned release; the fix in the description makes a foreign release impossible and a stale judgment verifiable.","created":"1790205692","id":"c1"}],"created":"1790205652","description":"FOUND by agent:claude-air-sawlang (chat m120, Sep 23): SL-357.p3 r1's server gate passed everything (suite 2656/10 xfail, freestanding 36/36) but exited 1 because build.sh's EXIT trap ran 'rmdir $LOCK' after something else had removed the lock mid-run (23:07-23:19Z). MECHANISM (obligation 4, a class): the lock is a bare mkdir at /private/tmp/claude-<uid>/saw-suite-lock, and EVERY release is an unconditional rmdir by whoever reaches that line — build.sh's trap, tools/battery.sh's callers, and each agent's hand-written 'until mkdir ...; gate; rmdir' chain. Nothing records who holds it, so (a) a taker whose mkdir-wait was killed by a tool timeout proceeds as if it held the lock and later rmdir's someone else's, (b) a 'stale' judgment cannot be verified, and (c) once one lock is wrongly released, the NEXT taker's lock is released by the original holder's chained rmdir at its end — a chain that the lead's own SL-371 battery (17:34-19:2x EDT) most likely continued: its lock dir was re-created at 18:56 while its battery was mid-run, so its end-of-run rmdir removed the server's. Overlapping suite runs are the other consequence. FIX: (1) ONE script, tools/suite_lock.sh: 'acquire' does mkdir + writes owner (pid, host, start time, label) into the dir, waiting with a bounded sleep; 'release' removes the dir ONLY if the owner file names the caller's pid (else prints who holds it and exits 0 without touching it); 'run -- CMD...' = acquire, run, release-own in one process with the trap tolerant of a missing dir and the command's real exit status preserved; 'status' prints the holder and whether its pid is alive; 'clear-stale' removes it only when the recorded pid is dead. (2) build.sh test and tools/battery.sh use it; CLAUDE.md's suite-lock paragraph and every agent prompt say 'tools/suite_lock.sh run -- <gate>' instead of the hand-written chain; the sawos repo keeps its own path via an env var. (3) The Air's suggested trap hardening is subsumed: release-own never fails the run. Harness change, not a compiler branch; gate = the script's own tests (acquire/release/foreign-release refused/stale cleared) + one suite run through it.","id":"SL-374","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":2,"sequence":1384,"stage":"backlog","status":"open","title":"The suite lock is unowned: any holder's chained rmdir can release ANOTHER holder's lock (SL-357.p3 gate reported FAILED after a passing run); make the lock owner-checked and give every taker one script","updated":"1790205692"},"id":"SL-374","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/CLAUDE.md b/CLAUDE.md\\nindex b9b815f2..cca4b31b 100644\\n--- a/CLAUDE.md\\n+++ b/CLAUDE.md\\n@@ -399,20 +399,28 @@ known-ledger entry (corodiff_known.txt, sawfuzz_known.txt) in the SAME\\n commit, and its gate includes that harness's lane.\\n \\n **THE SUITE LOCK (machine-wide, sawlang's):** all suite-shaped invocations\\n-in THIS repo (test_runner, battery.sh, freestanding_runner) serialize\\n-through a mkdir lock at `/private/tmp/claude-<uid>/saw-suite-lock`\\n-(uid = `id -u`; create the parent once per machine; the sawos repo has\\n-its OWN lock path so the two never queue on each other). Acquire + gate +\\n-release in ONE chained FOREGROUND command: `until mkdir <lock>\\n-2>/dev/null; do sleep 15; done; <gate>; rc=$?; rmdir <lock>; echo\\n-GATE=$rc` — in SANDBOXED agent worktrees, where the chained form is\\n-refused, SPLIT it: the bare mkdir-wait as its own call, each gate as its\\n-own call, the bare rmdir immediately after; the never-background and\\n-never-stop-while-waiting-or-holding rules apply unchanged. Never\\n-background the wait or the gate (a stopped agent's background waiters die\\n-silently); never hold the lock while editing; if the command times out,\\n-rerun the same command. Clear a stale lock only VERIFIED-DEAD: no suite\\n-process exists (pgrep) AND the holder is identified dead.\\n+in THIS repo (test_runner, battery.sh, freestanding_runner, `build.sh\\n+test`) serialize through ONE owner-checked lock, taken and released ONLY\\n+by `tools/suite_lock.sh` (SL-374; the lock dir is\\n+`/private/tmp/claude-<uid>/saw-suite-lock` with an `owner` file naming\\n+pid/token/host/label; the sawos repo sets `SAW_SUITE_LOCK` to its own\\n+path so the two never queue on each other). The one form:\\n+`tools/suite_lock.sh run --label <what> -- <gate>` — a FOREGROUND call\\n+that waits, runs the gate, releases only what it took, and exits with\\n+the gate's own status. In SANDBOXED agent worktrees, where a long\\n+foreground call is refused, SPLIT it: `tools/suite_lock.sh acquire\\n+--token <issue>-<actor>` as its own call, each gate as its own call,\\n+`tools/suite_lock.sh release --token <the same>` immediately after — a\\n+release under any other token touches nothing and says who holds it.\\n+Never write a bare `mkdir`/`rmdir` of the lock path: a release that is\\n+not owner-checked can remove ANOTHER holder's lock, and the next holder's\\n+release then removes the server's (that chain turned a passing patch\\n+gate into FAILED on Sep 23). Never background the wait or the gate (a\\n+stopped agent's background waiters die silently); never hold the lock\\n+while editing; if the command times out, rerun the same command.\\n+`tools/suite_lock.sh status` says who holds it and whether that pid is\\n+alive; `tools/suite_lock.sh clear-stale` is the ONLY way to clear one,\\n+and it refuses unless the recorded pid is dead and no gate process runs.\\n \\n **DESIGN DOCTRINE (user rulings, standing):**\\n - **Never hide errors** — failures surface as Result/Optional; no\\ndiff --git a/build.sh b/build.sh\\nindex 5d2607ee..53565336 100755\\n--- a/build.sh\\n+++ b/build.sh\\n@@ -20,10 +20,8 @@ if [ ! -x .venv/bin/python ]; then\\n     .venv/bin/python -m pip install --quiet -r sawc/requirements.txt\\n fi\\n \\n-LOCK=\\\"/private/tmp/claude-$(id -u)/saw-suite-lock\\\"\\n-mkdir -p \\\"$(dirname \\\"$LOCK\\\")\\\"\\n-until mkdir \\\"$LOCK\\\" 2>/dev/null; do sleep 15; done\\n-trap 'rmdir \\\"$LOCK\\\"' EXIT INT TERM\\n-\\n-.venv/bin/python test_runner.py\\n-.venv/bin/python tools/freestanding_runner.py\\n+# The lock is owner-checked (tools/suite_lock.sh): this run can only release\\n+# what it took, so a lock someone else holds at the end never turns a passing\\n+# gate into a failure, and the gate's own status is what the server sees.\\n+exec tools/suite_lock.sh run --label \\\"build.sh test\\\" -- sh -c \\\\\\n+    '.venv/bin/python test_runner.py && .venv/bin/python tools/freestanding_runner.py'\\ndiff --git a/tools/battery.sh b/tools/battery.sh\\nindex b9e04c9a..95275998 100755\\n--- a/tools/battery.sh\\n+++ b/tools/battery.sh\\n@@ -63,6 +63,7 @@ STAGES=(\\n     \\\"lexdiff|no|the Saw lexer against sawc's, over every tracked .saw|$PY tools/lexdiff.py\\\"\\n     \\\"astdiff|no|every .saw dumps completely and byte-stably|$PY tools/astdiff.py\\\"\\n     \\\"astgraft|no|no pass stamps an AST attribute no class declares|$PY tools/test_ast_graft.py\\\"\\n+    \\\"suitelock|no|the suite lock releases only for its holder and verifies staleness|$PY tools/test_suite_lock.py\\\"\\n     \\\"transferdecisions|no|every ownership boundary carries an explicit decision|$PY tools/test_transfer_decisions.py\\\"\\n     \\\"producertaxonomy|no|every value-position node says how it produces a value|$PY tools/test_producer_taxonomy.py\\\"\\n     \\\"corodiscovery|no|one ledger answers every coroutine frame decision|$PY tools/test_coro_discovery.py\\\"\\ndiff --git a/tools/suite_lock.sh b/tools/suite_lock.sh\\nnew file mode 100755\\nindex 00000000..e43fe7f9\\n--- /dev/null\\n+++ b/tools/suite_lock.sh\\n@@ -0,0 +1,181 @@\\n+#!/bin/sh\\n+# The machine-wide suite lock, owner-checked. ONE taker and ONE releaser for\\n+# every suite-shaped run on this host (test_runner, battery.sh,\\n+# freestanding_runner, build.sh test), so a release can never remove a lock\\n+# somebody else holds and a \\\"stale\\\" judgment is verifiable (SL-374).\\n+#\\n+#   tools/suite_lock.sh run [--label L] -- CMD ...   acquire, run CMD, release\\n+#                                                    own; exits with CMD's status\\n+#   tools/suite_lock.sh acquire --token T [--label L] wait for the lock, hold it\\n+#                                                    under token T (split use:\\n+#                                                    a caller that cannot chain)\\n+#   tools/suite_lock.sh release --token T            release ONLY if T holds it\\n+#   tools/suite_lock.sh status                       who holds it; exit 1 if free\\n+#   tools/suite_lock.sh clear-stale                  remove it only when its\\n+#                                                    recorded pid is dead and no\\n+#                                                    gate process is running\\n+#\\n+# The lock is a directory; its `owner` file records pid, token, host, label and\\n+# start time. `run` owns by pid (the script's own, which lives for the whole\\n+# command); `acquire` owns by token, because the acquiring shell exits.\\n+# SAW_SUITE_LOCK overrides the path (another repo keeps its own lock).\\n+\\n+set -u\\n+\\n+LOCK=\\\"${SAW_SUITE_LOCK:-/private/tmp/claude-$(id -u)/saw-suite-lock}\\\"\\n+OWNER=\\\"$LOCK/owner\\\"\\n+# The processes whose presence means a gate is running somewhere on this host.\\n+# Overridable so the contract test can run beside a real gate.\\n+GATE_PATTERN=\\\"${SAW_SUITE_LOCK_GATE_PATTERN:-test_runner\\\\.py|battery\\\\.sh|freestanding_runner\\\\.py|build\\\\.sh test|suite_lock\\\\.sh run}\\\"\\n+WAIT_SECONDS=\\\"${SAW_SUITE_LOCK_WAIT:-15}\\\"\\n+\\n+usage() {\\n+    sed -n '2,20p' \\\"$0\\\" >&2\\n+    exit 2\\n+}\\n+\\n+owner_field() {\\n+    # owner_field NAME — the value recorded for NAME, empty if none.\\n+    [ -f \\\"$OWNER\\\" ] || return 0\\n+    sed -n \\\"s/^$1=//p\\\" \\\"$OWNER\\\" 2>/dev/null | head -n 1\\n+}\\n+\\n+describe_holder() {\\n+    if [ -f \\\"$OWNER\\\" ]; then\\n+        printf 'held by pid=%s token=%s host=%s label=%s since=%s\\\\n' \\\\\\n+            \\\"$(owner_field pid)\\\" \\\"$(owner_field token)\\\" \\\"$(owner_field host)\\\" \\\\\\n+            \\\"$(owner_field label)\\\" \\\"$(owner_field started)\\\"\\n+    elif [ -d \\\"$LOCK\\\" ]; then\\n+        echo \\\"held by an UNRECORDED taker (a bare mkdir); treat as live\\\"\\n+    else\\n+        echo \\\"free\\\"\\n+    fi\\n+}\\n+\\n+write_owner() {\\n+    # write_owner PID TOKEN LABEL\\n+    {\\n+        echo \\\"pid=$1\\\"\\n+        echo \\\"token=$2\\\"\\n+        echo \\\"host=$(hostname 2>/dev/null || echo unknown)\\\"\\n+        echo \\\"label=$3\\\"\\n+        echo \\\"started=$(date '+%Y-%m-%dT%H:%M:%S')\\\"\\n+    } > \\\"$OWNER\\\"\\n+}\\n+\\n+wait_and_take() {\\n+    # wait_and_take PID TOKEN LABEL — block until the mkdir succeeds.\\n+    mkdir -p \\\"$(dirname \\\"$LOCK\\\")\\\"\\n+    tries=0\\n+    until mkdir \\\"$LOCK\\\" 2>/dev/null; do\\n+        tries=$((tries + 1))\\n+        if [ $((tries % 8)) -eq 1 ]; then\\n+            echo \\\"suite lock: waiting — $(describe_holder)\\\" >&2\\n+        fi\\n+        sleep \\\"$WAIT_SECONDS\\\"\\n+    done\\n+    write_owner \\\"$1\\\" \\\"$2\\\" \\\"$3\\\"\\n+}\\n+\\n+pid_alive() {\\n+    [ -n \\\"$1\\\" ] && kill -0 \\\"$1\\\" 2>/dev/null\\n+}\\n+\\n+gate_processes() {\\n+    pgrep -fl \\\"$GATE_PATTERN\\\" 2>/dev/null | grep -v \\\"suite_lock.sh \\\\(status\\\\|clear-stale\\\\)\\\" || true\\n+}\\n+\\n+release_if_owner() {\\n+    # release_if_owner FIELD VALUE — remove the lock only when FIELD matches.\\n+    if [ ! -d \\\"$LOCK\\\" ]; then\\n+        echo \\\"suite lock: already free (nothing released)\\\" >&2\\n+        return 0\\n+    fi\\n+    if [ \\\"$(owner_field \\\"$1\\\")\\\" = \\\"$2\\\" ] && [ -n \\\"$2\\\" ]; then\\n+        rm -f \\\"$OWNER\\\"\\n+        rmdir \\\"$LOCK\\\" 2>/dev/null || rm -rf \\\"$LOCK\\\"\\n+        return 0\\n+    fi\\n+    echo \\\"suite lock: NOT released — $(describe_holder); this caller ($1=$2) does not hold it\\\" >&2\\n+    return 0\\n+}\\n+\\n+cmd=\\\"${1:-}\\\"\\n+[ -n \\\"$cmd\\\" ] || usage\\n+shift\\n+\\n+label=\\\"\\\"\\n+token=\\\"\\\"\\n+while [ $# -gt 0 ]; do\\n+    case \\\"$1\\\" in\\n+        --label) label=\\\"${2:-}\\\"; shift 2 ;;\\n+        --token) token=\\\"${2:-}\\\"; shift 2 ;;\\n+        --) shift; break ;;\\n+        -*) usage ;;\\n+        *) break ;;\\n+    esac\\n+done\\n+\\n+case \\\"$cmd\\\" in\\n+    run)\\n+        [ $# -gt 0 ] || usage\\n+        token=\\\"run-$$-$(date +%s)\\\"\\n+        wait_and_take \\\"$$\\\" \\\"$token\\\" \\\"${label:-$1}\\\"\\n+        # A signal releases what this process holds and nothing else; the\\n+        # command's own status is what the caller sees on the normal path.\\n+        trap 'release_if_owner pid \\\"$$\\\"; exit 130' INT\\n+        trap 'release_if_owner pid \\\"$$\\\"; exit 143' TERM\\n+        \\\"$@\\\"\\n+        rc=$?\\n+        trap - INT TERM\\n+        release_if_owner pid \\\"$$\\\"\\n+        exit $rc\\n+        ;;\\n+    acquire)\\n+        [ -n \\\"$token\\\" ] || { echo \\\"suite lock: acquire needs --token T (you release with the same token)\\\" >&2; exit 2; }\\n+        wait_and_take \\\"\\\" \\\"$token\\\" \\\"${label:-$token}\\\"\\n+        echo \\\"suite lock: acquired under token $token\\\" >&2\\n+        ;;\\n+    release)\\n+        [ -n \\\"$token\\\" ] || { echo \\\"suite lock: release needs --token T\\\" >&2; exit 2; }\\n+        release_if_owner token \\\"$token\\\"\\n+        ;;\\n+    status)\\n+        describe_holder\\n+        if [ -d \\\"$LOCK\\\" ]; then\\n+            pid=\\\"$(owner_field pid)\\\"\\n+            if [ -n \\\"$pid\\\" ]; then\\n+                if pid_alive \\\"$pid\\\"; then echo \\\"holder pid $pid: alive\\\"; else echo \\\"holder pid $pid: DEAD\\\"; fi\\n+            else\\n+                echo \\\"token-held: liveness is not recorded; gate processes on this host:\\\"\\n+                gate_processes\\n+            fi\\n+            exit 0\\n+        fi\\n+        exit 1\\n+        ;;\\n+    clear-stale)\\n+        if [ ! -d \\\"$LOCK\\\" ]; then echo \\\"suite lock: free\\\"; exit 0; fi\\n+        pid=\\\"$(owner_field pid)\\\"\\n+        if [ -z \\\"$pid\\\" ]; then\\n+            echo \\\"suite lock: REFUSED — no recorded pid to verify ($(describe_holder)); a token-held or bare lock is cleared by its taker\\\" >&2\\n+            exit 1\\n+        fi\\n+        if pid_alive \\\"$pid\\\"; then\\n+            echo \\\"suite lock: REFUSED — holder pid $pid is alive\\\" >&2\\n+            exit 1\\n+        fi\\n+        running=\\\"$(gate_processes)\\\"\\n+        if [ -n \\\"$running\\\" ]; then\\n+            echo \\\"suite lock: REFUSED — pid $pid is dead but gate processes are running:\\\" >&2\\n+            echo \\\"$running\\\" >&2\\n+            exit 1\\n+        fi\\n+        echo \\\"suite lock: clearing — $(describe_holder); pid $pid is dead and no gate process runs\\\"\\n+        rm -f \\\"$OWNER\\\"\\n+        rmdir \\\"$LOCK\\\" 2>/dev/null || rm -rf \\\"$LOCK\\\"\\n+        ;;\\n+    *)\\n+        usage\\n+        ;;\\n+esac\\ndiff --git a/tools/test_suite_lock.py b/tools/test_suite_lock.py\\nnew file mode 100644\\nindex 00000000..02cbf2b4\\n--- /dev/null\\n+++ b/tools/test_suite_lock.py\\n@@ -0,0 +1,125 @@\\n+\\\"\\\"\\\"The suite lock's contract (SL-374): owner-checked release, verified staleness.\\n+\\n+Runs tools/suite_lock.sh against a private lock path, so it never touches the\\n+real lock. Each check states the rule it pins:\\n+  1. `run` holds the lock for the command's whole life and returns ITS status.\\n+  2. A release by a caller that does not hold the lock touches nothing.\\n+  3. `acquire --token` / `release --token` pair; a wrong token cannot release.\\n+  4. `clear-stale` refuses a live pid and clears a dead one.\\n+  5. A second `run` waits until the first releases.\\n+\\\"\\\"\\\"\\n+import os\\n+import subprocess\\n+import sys\\n+import tempfile\\n+import time\\n+\\n+REPO = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))\\n+SCRIPT = os.path.join(REPO, \\\"tools\\\", \\\"suite_lock.sh\\\")\\n+\\n+\\n+def sl(env, *args, **kw):\\n+    # A bounded wait: a check that never returns is a failure to report, not a\\n+    # hang to sit in.\\n+    kw.setdefault(\\\"timeout\\\", 30)\\n+    return subprocess.run([\\\"sh\\\", SCRIPT, *args], env=env, capture_output=True,\\n+                          text=True, **kw)\\n+\\n+\\n+def clear(lock):\\n+    import shutil\\n+    shutil.rmtree(lock, ignore_errors=True)\\n+\\n+\\n+def main() -> int:\\n+    problems = []\\n+    with tempfile.TemporaryDirectory() as tmp:\\n+        lock = os.path.join(tmp, \\\"lock\\\")\\n+        # A private lock path, a fast wait, and a gate pattern that matches no\\n+        # process: the host may be running a real gate while this probe runs.\\n+        env = dict(os.environ, SAW_SUITE_LOCK=lock, SAW_SUITE_LOCK_WAIT=\\\"1\\\",\\n+                   SAW_SUITE_LOCK_GATE_PATTERN=\\\"^no-such-gate-process$\\\")\\n+\\n+        # 1. run: held during, released after, status preserved.\\n+        r = sl(env, \\\"run\\\", \\\"--label\\\", \\\"probe\\\", \\\"--\\\", \\\"sh\\\", \\\"-c\\\",\\n+               f\\\"test -f '{lock}/owner' && grep -q '^label=probe$' '{lock}/owner' && exit 7\\\")\\n+        if r.returncode != 7:\\n+            problems.append(f\\\"run: expected the command's status 7, got {r.returncode}: {r.stderr}\\\")\\n+        if os.path.isdir(lock):\\n+            problems.append(\\\"run: lock still held after the command finished\\\")\\n+\\n+        # 2. foreign release: a lock we do not hold stays.\\n+        os.makedirs(lock)\\n+        with open(os.path.join(lock, \\\"owner\\\"), \\\"w\\\") as f:\\n+            f.write(\\\"pid=\\\\ntoken=someone-else\\\\nhost=x\\\\nlabel=y\\\\nstarted=z\\\\n\\\")\\n+        r = sl(env, \\\"release\\\", \\\"--token\\\", \\\"not-mine\\\")\\n+        if r.returncode != 0 or not os.path.isdir(lock):\\n+            problems.append(f\\\"foreign release: must leave the lock and exit 0 (rc={r.returncode}, exists={os.path.isdir(lock)})\\\")\\n+        if \\\"NOT released\\\" not in r.stderr:\\n+            problems.append(\\\"foreign release: must say the lock was not released\\\")\\n+        # and the true holder releases it\\n+        r = sl(env, \\\"release\\\", \\\"--token\\\", \\\"someone-else\\\")\\n+        if os.path.isdir(lock):\\n+            problems.append(\\\"owner release: the holder's release must remove the lock\\\")\\n+\\n+        # 3. acquire/release by token.\\n+        r = sl(env, \\\"acquire\\\", \\\"--token\\\", \\\"agent-a\\\", \\\"--label\\\", \\\"split use\\\")\\n+        if r.returncode != 0 or not os.path.isdir(lock):\\n+            problems.append(f\\\"acquire: rc={r.returncode} {r.stderr}\\\")\\n+        r = sl(env, \\\"release\\\", \\\"--token\\\", \\\"agent-b\\\")\\n+        if not os.path.isdir(lock):\\n+            problems.append(\\\"release with the wrong token removed the lock\\\")\\n+        r = sl(env, \\\"clear-stale\\\")\\n+        if r.returncode == 0 or not os.path.isdir(lock):\\n+            problems.append(\\\"clear-stale must refuse a token-held lock (no pid to verify)\\\")\\n+        sl(env, \\\"release\\\", \\\"--token\\\", \\\"agent-a\\\")\\n+        if os.path.isdir(lock):\\n+            problems.append(\\\"release with the right token left the lock in place\\\")\\n+        clear(lock)\\n+\\n+        # 4. clear-stale: live pid refused, dead pid cleared.\\n+        sleeper = subprocess.Popen([\\\"sleep\\\", \\\"30\\\"])\\n+        try:\\n+            os.makedirs(lock)\\n+            with open(os.path.join(lock, \\\"owner\\\"), \\\"w\\\") as f:\\n+                f.write(f\\\"pid={sleeper.pid}\\\\ntoken=t\\\\nhost=x\\\\nlabel=live\\\\nstarted=z\\\\n\\\")\\n+            r = sl(env, \\\"clear-stale\\\")\\n+            if r.returncode == 0 or not os.path.isdir(lock):\\n+                problems.append(\\\"clear-stale cleared a lock whose pid is alive\\\")\\n+            r = sl(env, \\\"status\\\")\\n+            if \\\"alive\\\" not in r.stdout:\\n+                problems.append(f\\\"status must report a live holder: {r.stdout!r}\\\")\\n+        finally:\\n+            sleeper.kill()\\n+            sleeper.wait()\\n+        # the pid is now dead; no gate process is running in this probe\\n+        r = sl(env, \\\"clear-stale\\\")\\n+        if r.returncode != 0 or os.path.isdir(lock):\\n+            problems.append(f\\\"clear-stale must clear a dead-pid lock: rc={r.returncode} {r.stderr}\\\")\\n+\\n+        # 5. a second run waits for the first.\\n+        clear(lock)\\n+        first = subprocess.Popen([\\\"sh\\\", SCRIPT, \\\"run\\\", \\\"--\\\", \\\"sleep\\\", \\\"3\\\"], env=env)\\n+        time.sleep(0.5)\\n+        t0 = time.time()\\n+        r = sl(env, \\\"run\\\", \\\"--\\\", \\\"true\\\")\\n+        waited = time.time() - t0\\n+        first.wait()\\n+        if waited < 2.0:\\n+            problems.append(f\\\"second run did not wait for the first (waited {waited:.1f}s)\\\")\\n+        if r.returncode != 0:\\n+            problems.append(f\\\"second run failed: {r.stderr}\\\")\\n+\\n+    if problems:\\n+        print(\\\"SUITE-LOCK GATE FAILED\\\")\\n+        for p in problems:\\n+            print(\\\"  \\\" + p)\\n+        return 1\\n+    print(\\\"suite-lock gate: run holds and returns the command's status, a foreign \\\"\\n+          \\\"release touches nothing, tokens pair, staleness is verified by pid, \\\"\\n+          \\\"a second run waits.\\\")\\n+    return 0\\n+\\n+\\n+if __name__ == \\\"__main__\\\":\\n+    sys.exit(main())\\n\",\"diff_bytes\":17167,\"diff_files\":5,\"diff_hunks\":5,\"diff_tail\":\" return 0\\n+\\n+\\n+if __name__ == \\\"__main__\\\":\\n+    sys.exit(main())\\n\",\"id\":\"SL-374\",\"request_id\":\"pid-59129-1790206059-6000\",\"title\":\"SL-374: owner-checked suite lock — tools/suite_lock.sh is the one taker and releaser (run/acquire/release/status/clear-stale); build.sh uses it; suitelock lane; CLAUDE.md names it the only form\"}","request_id":"pid-59129-1790206059-6000","sequence":1386,"timestamp":"1790206061"}
---
