---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl318u3","closed":"","comments":[],"created":"1790051263","description":"FOLLOW-UP (a) of design 275 U3, filed by the unit that built the machinery. THE GENERIC STATEMENT-SCOPED BORROW WINDOW: a keyword-introduced block that binds a lent PLACE for the statement's extent — the non-closure form of `with_ref`, able to suspend, `return`, `break` and propagate errors — with an `else` arm for a `borrows -> &T?` head.\n\nWHY IT IS CHEAP NOW (the reuse obligation, user, Sep 20). U3 built the window as a STRUCTURE with `for` as its first client, so this brief adds a client and nothing else:\n\n- `sawc/windows.py` already holds the record (root path, mode, origin, extent statement, and a GENERIC resource slot — deliberately not an `__iter` field nor an `Iterator`-shaped payload) and `WindowTable` is the ONE chokepoint, whose docstring already names this as the reserved second client. Root accounting, the exclusivity conflicts, the move/reassign/take/swap refusals and the close-on-every-route accounting BY RESOLVED CONTROL-FLOW TARGET are all there; `windows_left_by` takes the extents an edge's target is still inside, which is a question about scopes and not about loops.\n- `typechecker/borrowing.py`'s `open_for_window` / `close_for_window` is the `for` ADAPTER — a thin caller that supplies its head and its body and reads back nothing for-specific. This brief writes a second adapter beside it.\n- `codegen/loops.py`'s `_open_window_resource` / `_close_window_resource` is the SYNC lowering of the resource — one routine for any client, which destroys the resource and only then ends the charge.\n- The coroutine transform keys \"a window is live across this suspension\" on the RECORD. For `for` that is the `while let` rewrite plus the iterator's ordinary frame residency; a generic window's binding is the same question with a different resource.\n\nWHAT THE BRIEF STILL OWES, and none of it is machinery:\n\n1. THE SPELLING. A keyword, the head (a `borrows` call on a place), the binding, and the `else` arm for a conditional lend. `for` reads as `for <name> in <head> { }`; the generic form wants the same shape with the binding's MODE visible, since a window may be opened shared or exclusive at its use site and a statement form has no \"use site\" to read it off. Candidates and the trade-off are the user's ruling to make.\n2. THE ACQUISITION / RESULT BINDING. B3's ACQUIRE sequence is fixed (receiver place, the head's shared borrow over the whole call expression, arguments left to right, the producer, the hand-off with no gap), so what is new is only what the statement BINDS: the lent place itself, under a name the body may read and — at an exclusive window — write.\n3. THE `else` ARM. A `borrows -> &T?` head may find nothing to lend, and the absent path opens NO window and runs NO epilogue (design 141's conditional lend). The record's lifecycle already separates ACQUIRE from ACTIVE for exactly this: RELEASE never runs on a window that was never activated. The brief says what the `else` arm may do and what the statement's value is when it takes it.\n4. THE CLEANUP OPERATION. B3 makes RELEASE client-provided. `for`'s destroys the borrowing iterator; an accessor window's runs the accessor's EPILOGUE, which is a resume of a paused frame rather than a drop — that is the shape the API was left general for, and it is what (b) needs as well.\n5. DIAGNOSTICS. They already name \"the window\" generically and take the introducer's wording from the client (`_CLIENT_WORDING` / `_CLIENT_FIX` in `typechecker/borrowing.py`); this brief adds two rows there and forks no message.\n\nWHAT IT MUST NOT DO: no second implementation of root accounting, pinning, suspension persistence or exit cleanup (codex c44). The lead's validation of U3 was a grep for `ForLoop` in the chokepoint and in the transform's window handling finding only the adapter; the same grep for the new client's node is this brief's.\n\nRELATED: (b) `with_ref`/`with_var_ref` bodies and accessor `lend` windows spanning a suspension, and (c) the exclusive-window derivation, are the two other U3 follow-ups. (c) is a PREREQUISITE if this statement's first shipped mode is exclusive.\n","id":"SL-342","labels":["superseded"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":2194,"stage":"backlog","status":"open","title":"The generic statement-scoped borrow window: a keyword block binding a lent place, with an else arm for a conditional lend","updated":"1790341678"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sl318u3","closed":"","comments":[],"created":"1790051263","description":"FOLLOW-UP (a) of design 275 U3, filed by the unit that built the machinery. THE GENERIC STATEMENT-SCOPED BORROW WINDOW: a keyword-introduced block that binds a lent PLACE for the statement's extent — the non-closure form of `with_ref`, able to suspend, `return`, `break` and propagate errors — with an `else` arm for a `borrows -> &T?` head.\n\nWHY IT IS CHEAP NOW (the reuse obligation, user, Sep 20). U3 built the window as a STRUCTURE with `for` as its first client, so this brief adds a client and nothing else:\n\n- `sawc/windows.py` already holds the record (root path, mode, origin, extent statement, and a GENERIC resource slot — deliberately not an `__iter` field nor an `Iterator`-shaped payload) and `WindowTable` is the ONE chokepoint, whose docstring already names this as the reserved second client. Root accounting, the exclusivity conflicts, the move/reassign/take/swap refusals and the close-on-every-route accounting BY RESOLVED CONTROL-FLOW TARGET are all there; `windows_left_by` takes the extents an edge's target is still inside, which is a question about scopes and not about loops.\n- `typechecker/borrowing.py`'s `open_for_window` / `close_for_window` is the `for` ADAPTER — a thin caller that supplies its head and its body and reads back nothing for-specific. This brief writes a second adapter beside it.\n- `codegen/loops.py`'s `_open_window_resource` / `_close_window_resource` is the SYNC lowering of the resource — one routine for any client, which destroys the resource and only then ends the charge.\n- The coroutine transform keys \"a window is live across this suspension\" on the RECORD. For `for` that is the `while let` rewrite plus the iterator's ordinary frame residency; a generic window's binding is the same question with a different resource.\n\nWHAT THE BRIEF STILL OWES, and none of it is machinery:\n\n1. THE SPELLING. A keyword, the head (a `borrows` call on a place), the binding, and the `else` arm for a conditional lend. `for` reads as `for <name> in <head> { }`; the generic form wants the same shape with the binding's MODE visible, since a window may be opened shared or exclusive at its use site and a statement form has no \"use site\" to read it off. Candidates and the trade-off are the user's ruling to make.\n2. THE ACQUISITION / RESULT BINDING. B3's ACQUIRE sequence is fixed (receiver place, the head's shared borrow over the whole call expression, arguments left to right, the producer, the hand-off with no gap), so what is new is only what the statement BINDS: the lent place itself, under a name the body may read and — at an exclusive window — write.\n3. THE `else` ARM. A `borrows -> &T?` head may find nothing to lend, and the absent path opens NO window and runs NO epilogue (design 141's conditional lend). The record's lifecycle already separates ACQUIRE from ACTIVE for exactly this: RELEASE never runs on a window that was never activated. The brief says what the `else` arm may do and what the statement's value is when it takes it.\n4. THE CLEANUP OPERATION. B3 makes RELEASE client-provided. `for`'s destroys the borrowing iterator; an accessor window's runs the accessor's EPILOGUE, which is a resume of a paused frame rather than a drop — that is the shape the API was left general for, and it is what (b) needs as well.\n5. DIAGNOSTICS. They already name \"the window\" generically and take the introducer's wording from the client (`_CLIENT_WORDING` / `_CLIENT_FIX` in `typechecker/borrowing.py`); this brief adds two rows there and forks no message.\n\nWHAT IT MUST NOT DO: no second implementation of root accounting, pinning, suspension persistence or exit cleanup (codex c44). The lead's validation of U3 was a grep for `ForLoop` in the chokepoint and in the transform's window handling finding only the adapter; the same grep for the new client's node is this brief's.\n\nRELATED: (b) `with_ref`/`with_var_ref` bodies and accessor `lend` windows spanning a suspension, and (c) the exclusive-window derivation, are the two other U3 follow-ups. (c) is a PREREQUISITE if this statement's first shipped mode is exclusive.\n","id":"SL-342","labels":["design","borrows"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1165,"stage":"backlog","status":"open","title":"The generic statement-scoped borrow window: a keyword block binding a lent place, with an else arm for a conditional lend","updated":"1790051263"},"id":"SL-342","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-342\",\"labels\":[\"superseded\"],\"request_id\":\"pid-56706-1790341678-5000\",\"revision\":1}","request_id":"pid-56706-1790341678-5000","sequence":2194,"timestamp":"1790341678"}
---
