---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791232","description":"## Scope and status\n\nDF-218b (BOGUS-REFUSAL, found probing DF-218a, pre-existing and unrelated to its fix). <place>.take() on an optional-typed place is refused, and the diagnostic…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n4. Use counted ownership events and, where applicable, an address sanitizer; compare sync/driven and owning/copyable controls. Check error, early-return and teardown edges.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n- [ ] Each owned resource is released exactly once at the required lifetime boundary, with no lost writes or stale owner.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 3869–3889\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L3869-L3889)\n\n- **DF‑218b (BOGUS-REFUSAL, found probing DF‑218a, pre-existing and\n  unrelated to its fix). `<place>.take()` on an optional-typed place is\n  refused, and the diagnostic names a synthesized binding.**\n  `s.value().take()` on a `Slot<Res?>` reports ``cannot call `take()` on\n  immutable variable `__p76` `` — a compiler-internal window parameter in a\n  user-facing message, with a `let`-vs-`var` hint pointing at nothing the\n  author wrote. MECHANISM: `place_uses._method_mutates` picks the window\n  flavor by looking the method up on the receiver's owning STRUCT\n  (`_method_owner_name` -> `ns.lookup_method`), and an OPTIONAL receiver has\n  no struct owner, so every `Optional` method reads as non-mutating and the\n  window opens SHARED. `take` is `&var self` and needs an exclusive one.\n  SIBLINGS (obligation 4): the mechanism reaches exactly the\n  compiler-implemented `Optional` methods, since those are the ones with no\n  struct owner to look up — `take` is the only mutating member today, and\n  `is_some`/`is_none` are `&self` so a shared window is correct for them.\n  Any future `&var self` addition to `Optional` lands in the same hole. Two\n  fixes are owed and they are separable: teach the flavor decision about\n  `Optional`'s own methods, and stop synthesized binding names reaching a\n  diagnostic. Not scheduled; moving a payload out of a slot's optional\n  element has the `Slot.take()`/`Optional.take` spellings meanwhile.\n\n### sawlang TODO lines 5009–5018\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L5009-L5018)\n\n- **DF‑218b AMENDMENT (measured, Aug 14): the one-line flavor fix LEAKS.**\n  Teaching `place_uses._method_mutates` that `Optional.take` is `&var self`\n  (so an optional-typed place opens an EXCLUSIVE window) does remove the\n  wrong-noun refusal — and changes drop behavior:\n  `coro_iflet_move_scrutinee_releases_payload` moves from `t6 in 2 / t6 driven\n  0 / t7 plain 0` to `t6 in 3 / t6 driven 1 / t7 plain 1`, i.e. a retained\n  payload on the PLAIN path as well as the driven one. So the flavor decision\n  and the write-back are coupled, and DF‑218b's first half is not a one-liner:\n  it owes its own analysis of what an exclusive window does to the payload.\n","id":"SL-86","labels":["todo-import","needs-verification","decision-required","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":86,"status":"open","title":"DF-218b (BOGUS-REFUSAL, found probing DF-218a, pre-existing and unrelated to its fix). <place>.take() on an optional-typed place is refused, and the diagnostic…","updated":"1788791232"},"before":null,"id":"SL-86","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-218b (BOGUS-REFUSAL, found probing DF-218a, pre-existing and unrelated to its fix). <place>.take() on an optional-typed place is refused, and the diagnostic…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n4. Use counted ownership events and, where applicable, an address sanitizer; compare sync/driven and owning/copyable controls. Check error, early-return and teardown edges.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n- [ ] Each owned resource is released exactly once at the required lifetime boundary, with no lost writes or stale owner.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 3869–3889\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L3869-L3889)\\n\\n- **DF‑218b (BOGUS-REFUSAL, found probing DF‑218a, pre-existing and\\n  unrelated to its fix). `<place>.take()` on an optional-typed place is\\n  refused, and the diagnostic names a synthesized binding.**\\n  `s.value().take()` on a `Slot<Res?>` reports ``cannot call `take()` on\\n  immutable variable `__p76` `` — a compiler-internal window parameter in a\\n  user-facing message, with a `let`-vs-`var` hint pointing at nothing the\\n  author wrote. MECHANISM: `place_uses._method_mutates` picks the window\\n  flavor by looking the method up on the receiver's owning STRUCT\\n  (`_method_owner_name` -> `ns.lookup_method`), and an OPTIONAL receiver has\\n  no struct owner, so every `Optional` method reads as non-mutating and the\\n  window opens SHARED. `take` is `&var self` and needs an exclusive one.\\n  SIBLINGS (obligation 4): the mechanism reaches exactly the\\n  compiler-implemented `Optional` methods, since those are the ones with no\\n  struct owner to look up — `take` is the only mutating member today, and\\n  `is_some`/`is_none` are `&self` so a shared window is correct for them.\\n  Any future `&var self` addition to `Optional` lands in the same hole. Two\\n  fixes are owed and they are separable: teach the flavor decision about\\n  `Optional`'s own methods, and stop synthesized binding names reaching a\\n  diagnostic. Not scheduled; moving a payload out of a slot's optional\\n  element has the `Slot.take()`/`Optional.take` spellings meanwhile.\\n\\n### sawlang TODO lines 5009–5018\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L5009-L5018)\\n\\n- **DF‑218b AMENDMENT (measured, Aug 14): the one-line flavor fix LEAKS.**\\n  Teaching `place_uses._method_mutates` that `Optional.take` is `&var self`\\n  (so an optional-typed place opens an EXCLUSIVE window) does remove the\\n  wrong-noun refusal — and changes drop behavior:\\n  `coro_iflet_move_scrutinee_releases_payload` moves from `t6 in 2 / t6 driven\\n  0 / t7 plain 0` to `t6 in 3 / t6 driven 1 / t7 plain 1`, i.e. a retained\\n  payload on the PLAIN path as well as the driven one. So the flavor decision\\n  and the write-back are coupled, and DF‑218b's first half is not a one-liner:\\n  it owes its own analysis of what an exclusive window does to the payload.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"decision-required\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-5af69bcb5475fad22de3-create\",\"title\":\"DF-218b (BOGUS-REFUSAL, found probing DF-218a, pre-existing and unrelated to its fix). <place>.take() on an optional-typed place is refused, and the diagnostic…\"}","request_id":"todo-20260907-5af69bcb5475fad22de3-create","sequence":86,"timestamp":"1788791232"}
---
