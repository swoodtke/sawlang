---
{"action":"patch_add","actor":"agent:codex","after":{"assignee":"agent:codex","author":"agent:claude-sawlang","comments":[{"author":"agent:codex","body":"Submitted patch SL-258.p1 (revision 1): M15: bounded Vector ownership in the Saw compiler prototype\n\nAdds the bounded Vector support needed for the lexer milestone: compiler-known Vector<T> with Copy elements, empty construction, len/push/get/checked indexing, references, whole-local moves and vector-containing results. Elements can own Strings and Optional/Result payloads; get/index produce independent snapshots. Both the VM and textual LLVM backend implement unique ownership, cleanup and transactional push failure.\n\nGeneral generics, noncopyable elements, vector-bearing by-value parameters/reassignment and vector-valued control-flow joins remain outside this slice. Move analysis is conservative across branches and rejects moves of outer locals inside loops. M15_VECTORS.md specifies the source and instruction contracts; the inventory records StringBuilder.clear() and break as known remaining SL-259 integration work.\n\nValidation: all 424 prototype cases pass (30 in the Vector section); all 13 selected Vector sawc differential cases agree. Independent verifier, VM and source contracts pass; native ownership/failure contracts pass at O0/O2 and under ASan. A separate 33-element String-owning record oracle agrees with VM, native O0/O2/ASan and Python sawc. Citation and conflict-marker checks pass. Production finding SL-285 records Python's acceptance of an incorrect get(value:) label; the prototype rejects it and this rejection is excluded from shared-subset comparison.\n\nThe production Python compiler and lexer are unchanged. This patch follows merged SL-257.\n","created":"1789405015","id":"c1"}],"created":"1789086393","description":"SL-242 epic child, remaining work. The largest remaining LEXER_DEPENDENCIES.md item, and the gating dependency for the post-lexer arena AST (nodes-in-Vectors, index links — ruled Sep 10). Milestone doc owed at dispatch.","id":"SL-258","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":3,"sequence":793,"stage":"backlog","status":"open","title":"minivm: Vector support for the lexer subset","updated":"1789405015"},"before":{"assignee":"agent:codex","author":"agent:claude-sawlang","comments":[],"created":"1789086393","description":"SL-242 epic child, remaining work. The largest remaining LEXER_DEPENDENCIES.md item, and the gating dependency for the post-lexer arena AST (nodes-in-Vectors, index links — ruled Sep 10). Milestone doc owed at dispatch.","id":"SL-258","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":781,"stage":"backlog","status":"open","title":"minivm: Vector support for the lexer subset","updated":"1789401715"},"id":"SL-258","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:codex\",\"body\":\"Adds the bounded Vector support needed for the lexer milestone: compiler-known Vector<T> with Copy elements, empty construction, len/push/get/checked indexing, references, whole-local moves and vector-containing results. Elements can own Strings and Optional/Result payloads; get/index produce independent snapshots. Both the VM and textual LLVM backend implement unique ownership, cleanup and transactional push failure.\\n\\nGeneral generics, noncopyable elements, vector-bearing by-value parameters/reassignment and vector-valued control-flow joins remain outside this slice. Move analysis is conservative across branches and rejects moves of outer locals inside loops. M15_VECTORS.md specifies the source and instruction contracts; the inventory records StringBuilder.clear() and break as known remaining SL-259 integration work.\\n\\nValidation: all 424 prototype cases pass (30 in the Vector section); all 13 selected Vector sawc differential cases agree. Independent verifier, VM and source contracts pass; native ownership/failure contracts pass at O0/O2 and under ASan. A separate 33-element String-owning record oracle agrees with VM, native O0/O2/ASan and Python sawc. Citation and conflict-marker checks pass. Production finding SL-285 records Python's acceptance of an incorrect get(value:) label; the prototype rejects it and this rejection is excluded from shared-subset comparison.\\n\\nThe production Python compiler and lexer are unchanged. This patch follows merged SL-257.\\n\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex c99246be..54af3c91 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -1,7 +1,7 @@\\n # Lexer dependency inventory\\n \\n-Scope: the source to admit is `selfhost/lexer/src/lib.saw` (1,215 lines at the\\n-`5af086c4` baseline). This inventory is intentionally narrower than general Saw\\n+Scope: the source to admit is `selfhost/lexer/src/lib.saw` (1,328 lines in the\\n+current checkout). This inventory is intentionally narrower than general Saw\\n support. A feature is included only when that file uses it, or when it is an\\n unavoidable implementation dependency of one of the public std operations it\\n calls. This inventory was taken at the 43-case Int32/Bool baseline. M1 now supplies\\n@@ -25,7 +25,10 @@ checked unsigned parsing and the unchanged literal_fits helper (346 cases).\\n M13 adds checked nominal `Scalar` construction and code-point readout, with\\n opaque `InvalidScalar` results. M14 adds local `StringBuilder`, all four append\\n overloads, snapshots and reference forwarding, including unchanged `char_str`\\n-and `escape_text` extracts. Next is `Vector`. The lexer has\\n+and `escape_text` extracts. M15 adds bounded owning `Vector<T>` construction,\\n+push/get/index/len, references, whole-local moves and vector-containing results.\\n+Known remaining whole-lexer gaps are `StringBuilder.clear()` and the `break` in\\n+`Lexer.tokenize`; both belong to SL-259. The lexer has\\n no imports, so its first whole-file test can combine the unchanged library with\\n a small wrapper without implementing general module loading.\\n The dependency groups below are an inventory, not the implementation commit order;\\n@@ -49,18 +52,18 @@ the numbered milestone designs define each isolated slice.\\n   `LexResult` at `lib.saw:262-318` (design 268 added `StringSegment` and the\\n   payload-free enum `SegmentKind` at `lib.saw:236`, and gave `Token` its\\n   `seg_start`/`seg_count` fields), plus private `Lexer` at\\n-  `lib.saw:598-604`. Construction uses\\n+  `lib.saw:637-643`. Construction uses\\n   order-independent named fields, field reads, and mutable field assignment\\n-  through `&var self` (`advance`, `lib.saw:576-586`). Visibility must parse and\\n+  through `&var self` (`advance`, `lib.saw:654-664`). Visibility must parse and\\n   type-check; cross-module consumers need the four public models and their public\\n   fields. Private fields and custom initializers are unnecessary here.\\n - `extension LexResult: NoCopy {}` (`lib.saw:320`) and `extension Lexer { ... }`\\n-  (`lib.saw:567-1189`). The former must suppress implicit copying; the latter\\n+  (`lib.saw:645-1318`). The former must suppress implicit copying; the latter\\n   supplies instance methods with `&self` and `&var self` receivers. Traits in\\n   general and extension dispatch across arbitrary types are outside this slice.\\n - Public and private free functions, expression-bodied functions, implicit tail\\n   returns, explicit `return`, and named arguments (all struct construction and\\n-  `Scalar(value: cp)` at `lib.saw:699`). Default arguments, closures, overload\\n+  `Scalar(value: cp)` at `lib.saw:778`). Default arguments, closures, overload\\n   declarations in user code, and function values are unused.\\n \\n ### Types, operators, and conversions\\n@@ -73,7 +76,7 @@ the numbered milestone designs define each isolated slice.\\n   `max` constants and casts. No source floating-point arithmetic is needed even\\n   though the lexer emits `FloatLit` tokens.\\n - Static primitive constants `UInt8.max`, `UInt16.max`, and `UInt32.max`, then\\n-  widening casts `as UInt` (`literal_fits`, `lib.saw:539-553`). `UInt.max` and\\n+  widening casts `as UInt` (`literal_fits`, `lib.saw:617-625`). `UInt.max` and\\n   `Int.max` are not directly used by this source.\\n - Checked widening `s.byte_at(i) as Int` (`lib.saw:89`) and constructor/cast chain\\n   `Byte(UInt8.from(truncating: u))` (`lib.saw:95-97`). This requires static method\\n@@ -86,7 +89,7 @@ the numbered milestone designs define each isolated slice.\\n   compiler need not implement interpolation to compile this file: apparent\\n   braces in its diagnostic strings are escaped. Runtime string equality is used\\n   both by `==` in `keyword_kind`'s string `match` and explicitly by\\n-  `name_str.equals(...)` at `lib.saw:931-932`.\\n+  `name_str.equals(...)` at `lib.saw:1043-1044`.\\n \\n ### Ownership, references, generics, and fallibility\\n \\n@@ -125,8 +128,9 @@ the numbered milestone designs define each isolated slice.\\n ### Control flow and syntax sugar\\n \\n - `if`/`else if` statements, value-producing `if` expressions (`lib.saw:650,\\n-  780, 845-846`), `while`, and early `return`. `for`, `break`, `continue`, and\\n-  `guard` do not occur in this lexer.\\n+  780, 845-846`), `while`, early `return`, and `break` from the tokenization\\n+  loop (`lib.saw:1090`). `for`, `continue`, and `guard` do not occur in this\\n+  lexer. The prototype still needs the `break` slice tracked by SL-259.\\n - Exhaustive `match` over strings with literal cases and `_`\\n   (`keyword_kind`, `lib.saw:289-326`), and over payload-free enum values with\\n   both qualified and unqualified case patterns (`kind_name`, `lib.saw:330-438`).\\n@@ -148,7 +152,7 @@ compile their full implementations.\\n   including embedded NUL. `String.byte_at(&self, index: Int) -> Byte`\\n   (`:75`): unsigned byte, bounds checked. `substring(&self, start: Int, end: Int)\\n   -> String` (`:426`): copied half-open byte range, bounds checked. `is_empty()`\\n-  is used at `lib.saw:855,927` and can be intrinsic or derived from `len == 0`.\\n+  is used at `lib.saw:964,1039` and can be intrinsic or derived from `len == 0`.\\n   `equals(&self, other: String) -> Bool` (`:360`) and `==` need byte-content\\n   equality. `to_uint(&self, radix: Int) -> UInt?` (`:677`) must accept bases\\n   2/8/10/16 and return `None` on invalid digits or overflow past 64 bits. The\\n@@ -157,21 +161,29 @@ compile their full implementations.\\n - `StringBuilder()` (`sawc/std/stringbuilder.saw:58`),\\n   `append(&var self, String)` (`:146`), `append(..., Int)` (`:191`),\\n   `append(..., Byte)` (`:254`), `append(..., Scalar)` (`:348`), and\\n-  `build(&self) -> String` (`:414`). Appends preserve raw bytes; integer append\\n+  `build(&self) -> String` (`:414`), and `clear(&var self)` (`:394`). Appends preserve raw bytes; integer append\\n   is decimal; scalar append is valid UTF-8; build returns independent string\\n   contents and may leave the builder reusable. All append overloads return\\n   `Result<Void, AllocError>`, but lexer paths deliberately use `try!`. Capacity,\\n   fixed mode, allocator traits, `UnsafeSend`/`UnsafeSync`, raw pointers, memcpy,\\n   and growth algorithms in the std implementation are transitive implementation\\n-  details and should not become source-language prerequisites.\\n-- `Vector<T>()`, `len(&self) -> Int`, indexed shared read, and\\n+  details and should not become source-language prerequisites. M14 implements\\n+  every listed operation except `clear`; the lexer calls it at `lib.saw:844`,\\n+  and SL-259 tracks that remaining intrinsic.\\n+- `Vector<T>()`, `len(&self) -> Int`, `get(&self, index: Int) -> T?`, indexed\\n+  shared read, and\\n   `push(&var self, value: T) -> Result<Void, AllocError>` are used throughout;\\n-  production declarations are `sawc/std/vector.saw:43,67,93,231`. Required\\n-  instantiations are `Vector<Token>` and `Vector<DocComment>`. Bounds-checked\\n-  indexing appears only for token lookback (`lib.saw:985`). `pop`, iteration,\\n+  production declarations are `sawc/std/vector.saw:67,93,138,231`. Required\\n+  instantiations are `Vector<Token>`, `Vector<DocComment>`, and\\n+  `Vector<StringSegment>`. `get` reads string segments while formatting\\n+  (`lib.saw:553`); bounds-checked indexing appears for token lookback\\n+  (`lib.saw:1102`). `pop`, iteration,\\n   reserve, copy, custom allocators, and mutable indexing are unused by this\\n-  lexer. The std type's allocator parameter/default and pointer implementation\\n-  are therefore avoidable if the mini-VM provides an opaque monomorphic vector.\\n+  lexer. M15 supplies this bounded intrinsic surface with unique ownership and\\n+  Copy elements. The Python compiler currently also accepts the wrong\\n+  `get(value:)` label; SL-285 tracks that production compiler defect.\\n+  The std type's allocator parameter/default and pointer implementation remain\\n+  outside the prototype.\\n - `Scalar(value: Int) -> Result<Scalar, InvalidScalar>`\\n   (`sawc/std/scalar.saw:64,85`) and builder append encode one already-validated\\n   scalar. The lexer independently rejects out-of-range and surrogate code points\\n@@ -204,16 +216,17 @@ precise rejection. Keep all earlier milestone tests green at every gate.\\n 4. **Optional/Result control flow — complete through M11.** `T?`, fixed\\n    `Result<T,E>`, `None`, implicit injection, `if let`, `try`, and `try!` are\\n    covered with owning String payloads, propagation, and trap behavior.\\n-5. **Opaque builder and vector generics.** Expose only the contracts listed\\n-   above and implement correct ownership for `Vector<Token>` /\\n-   `Vector<DocComment>`. Test growth, indexing bounds, push of structs containing\\n+5. **Opaque builder and vector generics — complete through M15.** The prototype\\n+   exposes the bounded builder/vector contracts above, except `clear`, and implements ownership for\\n+   `Vector<Token>`, `Vector<DocComment>` and `Vector<StringSegment>`. The gates\\n+   cover growth, get/index bounds, push of structs containing\\n    `String?`, builder overload selection, build independence, and Unicode scalar\\n    UTF-8 output. Reject unsupported generic types/operations explicitly.\\n-6. **Lexer syntax completion.** Add string and enum `match`, if-expressions,\\n+6. **Lexer syntax completion.** String and enum `match`, if-expressions,\\n    public declarations, `NoCopy`, compound assignments, tail expressions, and\\n-   `_` discards. Compile focused extracts of `keyword_kind`, `kind_name`,\\n-   `literal_fits`, and `Lexer.advance`; compare their outputs to hand-written\\n-   oracles.\\n+   `_` discards are present, and focused extracts cover `keyword_kind`,\\n+   `kind_name`, `literal_fits`, and `Lexer.advance`. SL-259 still owes\\n+   `StringBuilder.clear()` and `break` before the unchanged lexer compiles.\\n 7. **Whole-module compile and differential lexing.** Compile the unchanged\\n    `selfhost/lexer/src/lib.saw`, call `lex` (one entry point since design 268,\\n    returning tokens + doc trivia + string segments), and compare canonical\\ndiff --git a/prototypes/minivm/M15_VECTORS.md b/prototypes/minivm/M15_VECTORS.md\\nnew file mode 100644\\nindex 00000000..e867d7e4\\n--- /dev/null\\n+++ b/prototypes/minivm/M15_VECTORS.md\\n@@ -0,0 +1,163 @@\\n+# M15: vectors and bounded ownership transfer\\n+\\n+SL-258 follows M14. The exact lexer needs Vector<Token>, Vector<DocComment>\\n+and Vector<StringSegment>, with String-owning Copy elements. It returns\\n+Result<Vector<Token>,LexError> and moves three vectors into NoCopy LexResult.\\n+Implement storage and ownership as separately testable parts of one milestone.\\n+Do not implement general generics, recursive data, or noncopyable elements.\\n+\\n+## Source contract\\n+\\n+Reserve Vector<T>. T may be any existing non-Void, non-reference Copy value:\\n+integers, Bool, enum, String, records, Optional and Result composed of these.\\n+Reject Builder, Vector and vector-containing records/sums anywhere in T.\\n+Element identity is nominal, not just a flattened layout match.\\n+\\n+Support empty Vector<T>(), len(), push(value:), get(index:), and checked\\n+read-only indexing v[index], including field projection on the copied result.\\n+Labels are optional but exact when present. len returns Int; push returns\\n+Result<Void,AllocError>; get returns T?, None for negative/out-of-range indices.\\n+[] traps with `runtime error: vector index out of bounds`. Both get forms return\\n+owned element snapshots; there are no escaping element references or indexed\\n+writes. Vector receivers are named local/field places or reference parameters.\\n+Register receiver borrow before evaluating arguments/index: push mutable,\\n+len/get/index shared. Evaluate inputs once. Mutable/shared vector reference\\n+forwarding and vector fields through record references work.\\n+\\n+Vectors are NoCopy, as are any record or Optional/Result containing a vector.\\n+Permit fresh-value local initialization, explicit `move name` of a whole local,\\n+vector-containing record construction, returns, Result wrapping/try extraction,\\n+and consuming a fresh Result/Optional in matches/conditional binding.\\n+Reject implicit reads of named noncopyable values. move checks active borrows,\\n+transfers ownership into a fresh temporary, and marks the entire binding moved;\\n+all later reads, moves, field accesses or borrows of that root are errors.\\n+Only whole direct local moves are supported: reject moving fields, dereferenced\\n+parameters or indexed elements. Copy values need not gain move syntax here.\\n+\\n+Permit exactly empty `extension R: NoCopy {}` when R actually contains a vector;\\n+reject this marker for otherwise-copyable records rather than pretending to\\n+enforce a general user-defined copy policy. Other trait bodies remain excluded.\\n+Vector-containing by-value parameters, reassignment, static values and general\\n+value-producing if/match joins are deferred; use references for parameters.\\n+Returning an owned temporary/fresh record or an explicit moved local works.\\n+Do not reject by-value *results*: the lexer requires them.\\n+\\n+Use conservative moved-binding tracking across branches: a move in any parsed\\n+branch invalidates the binding after that branch, even if another path could\\n+retain it. Document this limitation. Reject moving a binding declared outside\\n+the current loop from inside the loop; otherwise a second iteration could use\\n+inactive storage. Inner-loop locals may be moved. No source escape is allowed\\n+through these restrictions. Tests must distinguish these bounded restrictions\\n+from behavior required by production Saw; only shared-subset tests compare.\\n+\\n+## Shared model and instructions\\n+\\n+Add ValueType.Vector(index), VectorIR(element:ValueType), Program.vectors and\\n+Program.vector_fail_after (-1 disabled). Existing Program literals supply an\\n+empty vector table and -1. A Vector word is an owning leaf, including inside\\n+flattened records and sum payloads. value_layout needs no vector table because\\n+the leaf stays one word. Helpers: vector_type(t), vector_element(vectors,t),\\n+copyable_type(records,sums,t), validate_vectors(records,sums,vectors).\\n+Validate descriptor indices wherever types occur and reject noncopyable elements.\\n+\\n+Add these operations (unused fields retain instruction() defaults):\\n+\\n+| Op | dst | a | b | immediate |\\n+| --- | --- | --- | --- | --- |\\n+| VectorNew | Vector<I> | unused | unused | 0 |\\n+| VectorLen | Int | Ref<Vector<I>> | unused | 0 |\\n+| VectorIndex | element layout | Ref<Vector<I>> | Int index | 0 |\\n+| VectorGet | Optional<element> layout | Ref<Vector<I>> | Int index | Optional descriptor |\\n+| VectorPush | Result<Void,AllocError> layout | mutable Ref<Vector<I>> | element layout | Result descriptor |\\n+| MoveVector | Vector<I> | Vector<I> source | unused | 0 |\\n+\\n+Check exact element and nominal sum descriptor identity, full ranges, defaults,\\n+and reference mutability. MoveVector requires distinct same-typed slots, drops\\n+the old destination, transfers the source and zeros it. Inactive zero is legal\\n+for MoveVector and Drop; it is never a valid method receiver. Generic Copy,\\n+LoadRef and StoreRef reject vector-containing values. Address/RefField remain\\n+legal. Internal aggregate transfer emits MoveVector for vector leaves, existing\\n+Copy for other words; stage through fresh slots before replacing overlapping\\n+ranges. Source implicit noncopyable reads must be rejected before reaching this\\n+helper. All sum initialization clears vector words with Drop, not Const.\\n+\\n+By-value vector-containing parameters are verifier errors for this slice.\\n+Return transfers each vector leaf out and zeros the source BEFORE frame cleanup;\\n+String leaves retain as before. Call results adopt returned vector leaves,\\n+dropping any old destination. All owning slots initialize inactive and clean up\\n+on scope exit, loops, return and propagated errors. Preserve existing Builder\\n+restrictions and existing String copy semantics.\\n+\\n+## VM representation\\n+\\n+Use a separate Vector<VectorEntry> heap. Entry has descriptor:Int,\\n+alive:Bool, words:Vector<Int>, length:Int. Handles are positive indices;\\n+zero is inactive. Validate live handles and descriptor identity on operations.\\n+Constructor creates a live empty entry. Cleanup releases every String leaf of\\n+every element using its descriptor layout, then marks the entry inactive.\\n+Vector elements cannot own vectors, so destruction does not recurse into this\\n+heap. Final execution checks live vector entries as well as String references.\\n+\\n+Push stages old raw words plus incoming raw words in a new host Vector<Int>,\\n+recovering fallible push failures before changing the entry. Once staging\\n+succeeds, retain incoming String leaves, replace storage and increment length.\\n+Existing element handles relocate without retain/release. Failed push leaves\\n+content/length and input ownership unchanged; argument temporaries clean up in\\n+their normal scope. Get/index copy String leaves with retain. Host bookkeeping\\n+and infallible constructor OOM remain interpreter infrastructure limitations.\\n+\\n+## Native representation\\n+\\n+Vector owning word is a unique pointer to a stable 16-byte header:\\n+{ i64 length, ptr data }. New mallocs the header with length0/data null; failure\\n+is a runtime allocation trap (constructor is infallible in the source subset).\\n+No refcount/sharing or capacity API. Push allocates exactly (len+1)*width*8 bytes,\\n+copies existing words and incoming words, retains incoming String leaves, then\\n+installs the new buffer/length and frees old raw storage without dropping its\\n+relocated elements. Growth on every push is acceptable for this prototype.\\n+\\n+Emit descriptor-specific drop/push/index helpers (or equivalent verified static\\n+metadata), using the element's flattened layout for String retain/release.\\n+Get/index may return an internal pointer into storage to the emitter, which\\n+immediately copies/retains fields into destination slots; never expose that\\n+pointer as source-level borrowed storage. Optional Get must clear ALL inactive\\n+payload words on misses and when reusing destination slots in loops.\\n+Track live vector headers and include their count in successful-exit checks.\\n+Keep native by-value result ABI pointer classification consistent for vector\\n+leaves in flat aggregate returns. Existing String ownership remains separate.\\n+\\n+## Errors and deterministic failure tests\\n+\\n+Bad zero/dead handles: `runtime error: invalid vector handle`.\\n+Unrepresentable length/buffer arithmetic: `runtime error: vector length overflow`.\\n+Check len>=0, width>0, len+1 and multiplication against Int.max before allocation\\n+or data access. Do not wrap or saturate requested bytes. Push allocation failure\\n+returns [false,requested_bytes,8]; success [true,0,0]. Initial empty header has\\n+no element buffer, and the push error reports the requested *buffer* allocation.\\n+\\n+Program.vector_fail_after is a separate one-shot counter shared across calls:\\n+-1 disabled, 0 fails next representable push then resets to -1, positive N\\n+decrements only after success. No CLI/source flag. Invalid inputs and real\\n+allocation failure do not consume a positive counter. Both backends obey this.\\n+\\n+## Independent gates and division of work\\n+\\n+Parent owns this design, shared model/verifier, migration of existing Program\\n+literals, integration, review and final submission. Sol frontend owns\\n+frontend.saw, source fixtures and harness registration. Sol VM owns vm.saw and\\n+new vector runtime contracts. Sol native owns llvm.saw and vector_runtime.saw.\\n+Agents use isolated worktrees; only owned files are integrated.\\n+\\n+First prove empty/push/len/index/get independently with Int and String, then\\n+String-owning records/Optional/String-backed enum records, then move/Result/\\n+aggregate/reference/cleanup paths. Assert snapshots survive later pushes and\\n+vector destruction, get misses clear old payloads, once-only argument/index\\n+evaluation, bounds traps, exact failure fields and recovery. Test moved roots,\\n+double move, borrow after move, noncopyable element rejection, nominal descriptor\\n+mixups, aggregate implicit copy, and outer-loop moves. Native owning cases run\\n+at O0/O2 and under ASan. Full existing harness and bounded sawc differential\\n+gate the patch. Production findings go to sawtracker, not compatibility hacks.\\n+\\n+The latest lexer also needs StringBuilder.clear() and break. They are separate\\n+whole-lexer integration gaps for SL-259, not hidden parts of Vector support.\\n+Update the inventory accordingly; M15 alone does not claim whole-lexer success.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 04bb81ec..ca9d64bd 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -66,8 +66,9 @@ shift counts. See [M1_NUMBERS.md](M1_NUMBERS.md) for the precise numeric contrac\\n Newlines separate statements. Omit the result annotation for Void functions;\\n `main()` must take no arguments and return Void. Value-returning functions need\\n a value on every continuing path, supplied by an explicit return or final expression.\\n-There are no imports, collections, user-defined generics, or concurrency in the\\n-accepted subset. `Result<T,E>` is a compiler-known fixed generic type.\\n+There are no imports, general collections, user-defined generics, or concurrency\\n+in the accepted subset. `Result<T,E>` and `Vector<T>` are compiler-known fixed\\n+generic types.\\n Semicolons remain unsupported. Boolean `&&`/`||` short-circuit, mutable integers\\n and fields support `+=`/`-=`/`*=`/`/=`/`%=`, and module `static` integer/Bool\\n constants accept literal and numeric-limit initializers. See\\n@@ -132,6 +133,20 @@ fields. Builders cannot yet be copied, reassigned, returned by value or stored\\n in aggregates. Append preserves previous snapshots and leaves content unchanged\\n on allocation failure. See [M14_STRING_BUILDER.md](M14_STRING_BUILDER.md).\\n \\n+`Vector<T>` supports empty construction, `len`, `push`, `get`, checked read-only\\n+indexing, and shared/mutable reference forwarding. Elements may be non-Void,\\n+non-reference Copy values, including records and Optional/Result values that own\\n+Strings; nested vectors and other noncopyable elements are rejected. Vectors are\\n+unique owning values: whole local moves, vector-containing aggregate construction\\n+and by-value results are supported, while implicit copies, by-value parameters,\\n+reassignment, statics, indexed writes and escaping element references remain\\n+outside this slice. Push failure preserves the vector and its input, and get or\\n+index returns an owned element snapshot. See [M15_VECTORS.md](M15_VECTORS.md).\\n+Move tracking is conservative across branches, and moving an outer local inside\\n+a loop is rejected. Empty `NoCopy` markers are accepted only on records that\\n+contain vectors; general explicit copy policies and vector-valued control-flow\\n+joins remain unsupported.\\n+\\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n only to VM execution; native code uses the host call stack and has no budget.\\n@@ -144,8 +159,10 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second execution timeouts. The first fourteen milestones\\n-cover 394 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second execution timeouts. Native execution is\\n+checked at both `-O0` and `-O2`. The first fifteen milestones cover 424 cases;\\n+the Vector section covers 30, with 13 shared-subset cases also checked against\\n+Python sawc using `--section vectors --sawc sawc/sawc.py`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw`,\\n `tests/record_contract.saw`, `tests/enum_contract.saw`,\\n@@ -153,14 +170,15 @@ Independent representation checks live in `tests/numeric_contract.saw`,\\n `tests/string_contract.saw`, `tests/owning_record_contract.saw`,\\n `tests/optional_contract.saw`, `tests/result_contract.saw`,\\n `tests/uint_parse_contract.saw`, `tests/scalar_contract.saw`,\\n-`tests/builder_contract.saw`, `tests/builder_vm_contract.saw`, and\\n-`tests/builder_source_contract.saw`; build and run them\\n+`tests/builder_contract.saw`, `tests/builder_vm_contract.saw`,\\n+`tests/builder_source_contract.saw`, `tests/vector_contract.saw`,\\n+`tests/vector_vm_contract.saw`, and `tests/vector_source_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n `--section enums`, `--section values`, `--section references`, or\\n `--section receivers`, `--section strings`, `--section owning_records` or\\n `--section optionals`, `--section results`, `--section unsigned_parse`, or\\n-`--section scalars`, or `--section builders` for an isolated\\n+`--section scalars`, `--section builders`, or `--section vectors` for an isolated\\n integration gate.\\n \\n To run the initial SL-260 shared-subset differential as well:\\ndiff --git a/prototypes/minivm/examples/vectors/basic.saw b/prototypes/minivm/examples/vectors/basic.saw\\nnew file mode 100644\\nindex 00000000..f3866d72\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/basic.saw\\n@@ -0,0 +1,12 @@\\n+func main() {\\n+    var v = Vector<Int>()\\n+    try! v.push(value: 10)\\n+    try! v.push(20)\\n+    try! v.push(value: 30)\\n+    print(v.len())\\n+    print(v[0])\\n+    print(v[2])\\n+    if let x = v.get(index: 1) { print(x) }\\n+    if let x = v.get(-1) { print(x) } else { print(\\\"none\\\") }\\n+    if let x = v.get(3) { print(x) } else { print(\\\"none\\\") }\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/evaluation_once.saw b/prototypes/minivm/examples/vectors/evaluation_once.saw\\nnew file mode 100644\\nindex 00000000..a9681b1e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/evaluation_once.saw\\n@@ -0,0 +1,12 @@\\n+func index() -> Int { print(\\\"index\\\")\\n+0 }\\n+func value() -> Int { print(\\\"value\\\")\\n+9 }\\n+\\n+func main() {\\n+    var v = Vector<Int>()\\n+    try! v.push(7)\\n+    print(v[index()])\\n+    try! v.push(value())\\n+    print(v.len())\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/forward_element_record.saw b/prototypes/minivm/examples/vectors/forward_element_record.saw\\nnew file mode 100644\\nindex 00000000..5a821515\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/forward_element_record.saw\\n@@ -0,0 +1,9 @@\\n+struct Holder { rows: Vector<Row> }\\n+extension Holder: NoCopy {}\\n+struct Row { text: String, suffix: String? }\\n+\\n+func main() {\\n+    var holder = Holder(rows: Vector<Row>())\\n+    try! holder.rows.push(Row(text: \\\"forward\\\", suffix: None))\\n+    print(holder.rows[0].text)\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/forward_nocopy_vector.saw b/prototypes/minivm/examples/vectors/forward_nocopy_vector.saw\\nnew file mode 100644\\nindex 00000000..47d47808\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/forward_nocopy_vector.saw\\n@@ -0,0 +1,8 @@\\n+extension Holder: NoCopy {}\\n+struct Holder { values: Vector<Int> }\\n+\\n+func main() {\\n+    var holder = Holder(values: Vector<Int>())\\n+    try! holder.values.push(42)\\n+    print(holder.values[0])\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/index_at_end.saw b/prototypes/minivm/examples/vectors/index_at_end.saw\\nnew file mode 100644\\nindex 00000000..ecb9fb4c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/index_at_end.saw\\n@@ -0,0 +1,3 @@\\n+func main() { var v = Vector<Int>()\\n+try! v.push(1)\\n+print(v[1]) }\\ndiff --git a/prototypes/minivm/examples/vectors/index_negative.saw b/prototypes/minivm/examples/vectors/index_negative.saw\\nnew file mode 100644\\nindex 00000000..a9752b76\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/index_negative.saw\\n@@ -0,0 +1,2 @@\\n+func main() { var v = Vector<Int>()\\n+print(v[-1]) }\\ndiff --git a/prototypes/minivm/examples/vectors/moves_results.saw b/prototypes/minivm/examples/vectors/moves_results.saw\\nnew file mode 100644\\nindex 00000000..5eaae6b2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/moves_results.saw\\n@@ -0,0 +1,24 @@\\n+struct Pack { values: Vector<String>, label: String }\\n+extension Pack: NoCopy {}\\n+\\n+func make() -> Result<Vector<String>, AllocError> {\\n+    var values = Vector<String>()\\n+    try values.push(\\\"one\\\")\\n+    move values\\n+}\\n+\\n+func pack() -> Pack {\\n+    var values = Vector<String>()\\n+    try! values.push(\\\"x\\\")\\n+    try! values.push(\\\"y\\\")\\n+    Pack(values: move values, label: \\\"two\\\")\\n+}\\n+\\n+func main() {\\n+    var values = try! make()\\n+    print(values[0])\\n+    var p = pack()\\n+    print(p.values.len())\\n+    print(p.values[0])\\n+    print(p.values[1])\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/references_fields.saw b/prototypes/minivm/examples/vectors/references_fields.saw\\nnew file mode 100644\\nindex 00000000..91215d8c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/references_fields.saw\\n@@ -0,0 +1,21 @@\\n+struct Holder { values: Vector<String> }\\n+extension Holder: NoCopy {}\\n+\\n+func add(values: &var Vector<String>, value: String) {\\n+    try! values.push(value: value)\\n+}\\n+\\n+func count(values: &Vector<String>) -> Int { values.len() }\\n+\\n+func fill(holder: &var Holder) {\\n+    add(&var holder.values, \\\"left\\\")\\n+    add(&var holder.values, \\\"right\\\")\\n+}\\n+\\n+func main() {\\n+    var holder = Holder(values: Vector<String>())\\n+    fill(&var holder)\\n+    print(count(&holder.values))\\n+    print(holder.values[0])\\n+    print(holder.values[1])\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_assignment.saw b/prototypes/minivm/examples/vectors/reject_assignment.saw\\nnew file mode 100644\\nindex 00000000..482968e6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_assignment.saw\\n@@ -0,0 +1,3 @@\\n+func main() { var a = Vector<Int>()\\n+let b = Vector<Int>()\\n+a = move b }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_borrow_after_move.saw b/prototypes/minivm/examples/vectors/reject_borrow_after_move.saw\\nnew file mode 100644\\nindex 00000000..9370020f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_borrow_after_move.saw\\n@@ -0,0 +1,3 @@\\n+func main() { var a = Vector<Int>()\\n+let b = move a\\n+print(a.len()) }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_builder_element.saw b/prototypes/minivm/examples/vectors/reject_builder_element.saw\\nnew file mode 100644\\nindex 00000000..01f0bd2c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_builder_element.saw\\n@@ -0,0 +1 @@\\n+func main() { let v = Vector<StringBuilder>() }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_constructor_arity.saw b/prototypes/minivm/examples/vectors/reject_constructor_arity.saw\\nnew file mode 100644\\nindex 00000000..854e080f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_constructor_arity.saw\\n@@ -0,0 +1 @@\\n+func main() { let v = Vector<Int>(1) }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_double_move.saw b/prototypes/minivm/examples/vectors/reject_double_move.saw\\nnew file mode 100644\\nindex 00000000..e85dd6d2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_double_move.saw\\n@@ -0,0 +1,3 @@\\n+func main() { var a = Vector<Int>()\\n+let b = move a\\n+let c = move a }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_forward_nocopy_copyable.saw b/prototypes/minivm/examples/vectors/reject_forward_nocopy_copyable.saw\\nnew file mode 100644\\nindex 00000000..5e32614a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_forward_nocopy_copyable.saw\\n@@ -0,0 +1,4 @@\\n+extension Plain: NoCopy {}\\n+struct Plain { value: Int }\\n+\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_get_label.saw b/prototypes/minivm/examples/vectors/reject_get_label.saw\\nnew file mode 100644\\nindex 00000000..4f2086d7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_get_label.saw\\n@@ -0,0 +1,2 @@\\n+func main() { let v = Vector<Int>()\\n+let x = v.get(value: 0) }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_implicit_copy.saw b/prototypes/minivm/examples/vectors/reject_implicit_copy.saw\\nnew file mode 100644\\nindex 00000000..795f26f2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_implicit_copy.saw\\n@@ -0,0 +1,2 @@\\n+func main() { let a = Vector<Int>()\\n+let b = a }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_index_type.saw b/prototypes/minivm/examples/vectors/reject_index_type.saw\\nnew file mode 100644\\nindex 00000000..8b29fdb1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_index_type.saw\\n@@ -0,0 +1,2 @@\\n+func main() { let v = Vector<Int>()\\n+print(v[true]) }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_move_field.saw b/prototypes/minivm/examples/vectors/reject_move_field.saw\\nnew file mode 100644\\nindex 00000000..8828d326\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_move_field.saw\\n@@ -0,0 +1,4 @@\\n+struct Box { values: Vector<Int> }\\n+extension Box: NoCopy {}\\n+func main() { let b = Box(values: Vector<Int>())\\n+let v = move b.values }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_move_reference.saw b/prototypes/minivm/examples/vectors/reject_move_reference.saw\\nnew file mode 100644\\nindex 00000000..0bea3c8d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_move_reference.saw\\n@@ -0,0 +1,3 @@\\n+func bad(v: &var Vector<Int>) { let x = move v }\\n+func main() { var v = Vector<Int>()\\n+bad(&var v) }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_nocopy_body.saw b/prototypes/minivm/examples/vectors/reject_nocopy_body.saw\\nnew file mode 100644\\nindex 00000000..3f94646e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_nocopy_body.saw\\n@@ -0,0 +1,3 @@\\n+struct Box { values: Vector<Int> }\\n+extension Box: NoCopy { func bad(&self) {} }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_nocopy_copyable.saw b/prototypes/minivm/examples/vectors/reject_nocopy_copyable.saw\\nnew file mode 100644\\nindex 00000000..1ff5065f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_nocopy_copyable.saw\\n@@ -0,0 +1,3 @@\\n+struct Plain { n: Int }\\n+extension Plain: NoCopy {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_noncopy_record_element.saw b/prototypes/minivm/examples/vectors/reject_noncopy_record_element.saw\\nnew file mode 100644\\nindex 00000000..56f9c4d4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_noncopy_record_element.saw\\n@@ -0,0 +1,3 @@\\n+struct Box { values: Vector<Int> }\\n+extension Box: NoCopy {}\\n+func main() { let v = Vector<Box>() }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_optional_constructor.saw b/prototypes/minivm/examples/vectors/reject_optional_constructor.saw\\nnew file mode 100644\\nindex 00000000..a2918e13\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_optional_constructor.saw\\n@@ -0,0 +1 @@\\n+func main() { let v = Vector<Int>?() }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_outer_loop_move.saw b/prototypes/minivm/examples/vectors/reject_outer_loop_move.saw\\nnew file mode 100644\\nindex 00000000..bc791879\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_outer_loop_move.saw\\n@@ -0,0 +1,4 @@\\n+func main() { let v = Vector<Int>()\\n+var once = true\\n+while once { let x = move v\\n+once = false } }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_push_label.saw b/prototypes/minivm/examples/vectors/reject_push_label.saw\\nnew file mode 100644\\nindex 00000000..e59ee0cf\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_push_label.saw\\n@@ -0,0 +1,2 @@\\n+func main() { var v = Vector<Int>()\\n+try! v.push(index: 1) }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_push_type.saw b/prototypes/minivm/examples/vectors/reject_push_type.saw\\nnew file mode 100644\\nindex 00000000..6983066f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_push_type.saw\\n@@ -0,0 +1,2 @@\\n+func main() { var v = Vector<Int>()\\n+try! v.push(\\\"x\\\") }\\ndiff --git a/prototypes/minivm/examples/vectors/reject_value_control.saw b/prototypes/minivm/examples/vectors/reject_value_control.saw\\nnew file mode 100644\\nindex 00000000..c022417b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_value_control.saw\\n@@ -0,0 +1,3 @@\\n+func main() {\\n+    let v = if true { Vector<Int>() } else { Vector<Int>() }\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_value_parameter.saw b/prototypes/minivm/examples/vectors/reject_value_parameter.saw\\nnew file mode 100644\\nindex 00000000..8282636f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_value_parameter.saw\\n@@ -0,0 +1,2 @@\\n+func bad(v: Vector<Int>) {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_vector_element.saw b/prototypes/minivm/examples/vectors/reject_vector_element.saw\\nnew file mode 100644\\nindex 00000000..f9437db4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/reject_vector_element.saw\\n@@ -0,0 +1 @@\\n+func main() { let v = Vector<Vector<Int>>() }\\ndiff --git a/prototypes/minivm/examples/vectors/strings_records.saw b/prototypes/minivm/examples/vectors/strings_records.saw\\nnew file mode 100644\\nindex 00000000..f5328128\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/vectors/strings_records.saw\\n@@ -0,0 +1,16 @@\\n+enum Kind { case Word, case Number }\\n+struct Item { kind: Kind, value: String, line: Int, col: Int, suffix: String? }\\n+\\n+func main() {\\n+    var strings = Vector<String>()\\n+    try! strings.push(\\\"old\\\")\\n+    let snapshot = strings[0]\\n+    try! strings.push(\\\"new\\\")\\n+    print(snapshot)\\n+    print(strings[1])\\n+    var items = Vector<Item>()\\n+    try! items.push(Item(kind: Kind.Word, value: \\\"a\\\", line: 1, col: 2, suffix: None))\\n+    try! items.push(Item(kind: Kind.Number, value: \\\"b\\\", line: 3, col: 4, suffix: \\\"!\\\"))\\n+    print(items[0].value)\\n+    if let item = items.get(1) { print(item.value) }\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 4930abf0..590c880a 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -31,6 +31,8 @@ struct Binding {\\n     slot: Int\\n     mutable: Bool\\n     depth: Int\\n+    active: Bool\\n+    loop_depth: Int\\n }\\n \\n struct Place {\\n@@ -94,7 +96,7 @@ func type_name(t: ValueType) -> String {\\n         case U8 -> \\\"UInt8\\\", case U16 -> \\\"UInt16\\\", case U32 -> \\\"UInt32\\\", case U64 -> \\\"UInt\\\",\\n         case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case String -> \\\"String\\\", case Builder -> \\\"StringBuilder\\\", case Void -> \\\"Void\\\",\\n         case Record(_) -> \\\"record\\\", case Enum(_) -> \\\"enum\\\", case Ref(_) -> \\\"reference\\\",\\n-        case Optional(_) -> \\\"Optional\\\", case Result(_) -> \\\"Result\\\"\\n+        case Optional(_) -> \\\"Optional\\\", case Result(_) -> \\\"Result\\\", case Vector(_) -> \\\"Vector\\\"\\n     }\\n }\\n \\n@@ -133,6 +135,12 @@ struct Compiler {\\n     enums: Vector<EnumIR>\\n     references: Vector<ReferenceIR>\\n     sums: Vector<SumIR>\\n+    vectors: Vector<VectorIR>\\n+    vector_lines: Vector<Int>\\n+    vector_cols: Vector<Int>\\n+    nocopy_records: Vector<Int>\\n+    nocopy_lines: Vector<Int>\\n+    nocopy_cols: Vector<Int>\\n     strings: Vector<String>\\n     statics: Vector<StaticBinding>\\n     signatures: Vector<Signature>\\n@@ -151,6 +159,7 @@ struct Compiler {\\n     scalar_record: Int\\n     invalid_scalar_record: Int\\n     alloc_error_record: Int\\n+    loop_depth: Int\\n }\\n extension Compiler: NoCopy {}\\n \\n@@ -211,7 +220,7 @@ extension Compiler {\\n             if reference_type(target) { return problem(\\\"references to references are not supported\\\", amp.line, amp.col) }\\n             var supported = scalar_type(target)\\n             match target { case Record(_) -> { supported = true }, case _ -> {} }\\n-            if target == ValueType.String || target == ValueType.Builder { supported = true }\\n+            if target == ValueType.String || target == ValueType.Builder || vector_type(target) { supported = true }\\n             match target { case Optional(_) -> { supported = true }, case _ -> {} }\\n             match target { case Result(_) -> { supported = true }, case _ -> {} }\\n             if not supported { return problem(\\\"only data references are supported\\\", amp.line, amp.col) }\\n@@ -234,6 +243,15 @@ extension Compiler {\\n             }\\n             ty = self.intern_result(success, error)\\n         }\\n+        else if t.value == \\\"Vector\\\" {\\n+            let _ = try self.expect(TokenKind.Lt, \\\"Vector requires exactly one type argument\\\")\\n+            let element = try self.parse_type_inner(type_depth + 1)\\n+            let _ = try self.expect(TokenKind.Gt, \\\"Vector requires exactly one type argument\\\")\\n+            if element == ValueType.Void || reference_type(element) || element == ValueType.Builder || vector_type(element) {\\n+                return problem(\\\"Vector elements must be non-Void Copy values\\\", t.line, t.col)\\n+            }\\n+            ty = self.intern_vector(element, t.line, t.col)\\n+        }\\n         else if t.value == \\\"Void\\\" { ty = ValueType.Void }\\n         else if t.value == \\\"Bool\\\" { ty = ValueType.Bool }\\n         else if t.value == \\\"String\\\" { ty = ValueType.String }\\n@@ -302,6 +320,66 @@ extension Compiler {\\n         ValueType.Ref(self.references.len() - 1)\\n     }\\n \\n+    func intern_vector(&var self, element: ValueType, line: Int, col: Int) -> ValueType {\\n+        var i = 0\\n+        while i < self.vectors.len() {\\n+            if self.vectors[i].element == element { return ValueType.Vector(i) }\\n+            i += 1\\n+        }\\n+        try! self.vectors.push(VectorIR(element: element))\\n+        try! self.vector_lines.push(line)\\n+        try! self.vector_cols.push(col)\\n+        ValueType.Vector(self.vectors.len() - 1)\\n+    }\\n+\\n+    func validate_vector_elements(&self) -> Result<Void, ProtoError> {\\n+        var i = 0\\n+        while i < self.vectors.len() {\\n+            if not copyable_type(&self.records, &self.sums, self.vectors[i].element) {\\n+                return problem(\\\"Vector elements must be non-Void Copy values\\\", self.vector_lines[i], self.vector_cols[i])\\n+            }\\n+            i += 1\\n+        }\\n+        return\\n+    }\\n+\\n+    func validate_nocopy_markers(&self) -> Result<Void, ProtoError> {\\n+        var i = 0\\n+        while i < self.nocopy_records.len() {\\n+            if copyable_type(&self.records, &self.sums, ValueType.Record(self.nocopy_records[i])) {\\n+                return problem(\\\"NoCopy marker requires a vector-containing record\\\", self.nocopy_lines[i], self.nocopy_cols[i])\\n+            }\\n+            i += 1\\n+        }\\n+        return\\n+    }\\n+\\n+    func vector_containing_type(&self, ty: ValueType) -> Bool {\\n+        let layout = try value_layout(&self.records, &self.sums, ty) catch { return false }\\n+        var i = 0\\n+        while i < layout.len() {\\n+            if vector_type(layout[i]) { return true }\\n+            i += 1\\n+        }\\n+        false\\n+    }\\n+\\n+    func validate_signature_vectors(&self) -> Result<Void, ProtoError> {\\n+        var i = 0\\n+        while i < self.signatures.len() {\\n+            var p = 0\\n+            while p < self.signatures[i].params.len() {\\n+                let ty = self.signatures[i].params[p]\\n+                if not reference_type(ty) && self.vector_containing_type(ty) {\\n+                    return problem(\\\"vector-containing parameters must be references\\\", self.signatures[i].line, self.signatures[i].col)\\n+                }\\n+                p += 1\\n+            }\\n+            i += 1\\n+        }\\n+        return\\n+    }\\n+\\n     func intern_string(&var self, value: String) -> Int {\\n         var i = 0\\n         while i < self.strings.len() {\\n@@ -361,12 +439,16 @@ extension Compiler {\\n                 }\\n                 \\\"Result\\\"\\n             },\\n+            case Vector(index) -> {\\n+                if index >= 0 && index < self.vectors.len() { return \\\"Vector<{self.display_type(self.vectors[index].element)}>\\\" }\\n+                \\\"Vector\\\"\\n+            },\\n             case _ -> type_name(ty)\\n         }\\n     }\\n \\n     func reserved_type_name(&self, name: String) -> Bool {\\n-        if name == \\\"Bool\\\" || name == \\\"String\\\" || name == \\\"StringBuilder\\\" || name == \\\"Void\\\" || name == \\\"Scalar\\\" || name == \\\"InvalidScalar\\\" || name == \\\"AllocError\\\" { return true }\\n+        if name == \\\"Bool\\\" || name == \\\"String\\\" || name == \\\"StringBuilder\\\" || name == \\\"Vector\\\" || name == \\\"Void\\\" || name == \\\"Scalar\\\" || name == \\\"InvalidScalar\\\" || name == \\\"AllocError\\\" { return true }\\n         if let _ = named_integer_type(name) { return true }\\n         false\\n     }\\n@@ -445,6 +527,7 @@ extension Compiler {\\n     func scan_extension(&var self) -> Result<Void, ProtoError> {\\n         let extension_token = self.take()\\n         let target = try self.expect(TokenKind.Ident, \\\"expected extension target\\\")\\n+        if target.value == \\\"Vector\\\" { return problem(\\\"builtin type `Vector` cannot be extended\\\", target.line, target.col) }\\n         let owner = self.record_index(target.value)\\n         if target.value == \\\"StringBuilder\\\" || target.value == \\\"AllocError\\\" {\\n             return problem(\\\"builtin type `{target.value}` cannot be extended\\\", target.line, target.col)\\n@@ -453,7 +536,18 @@ extension Compiler {\\n         if owner == self.scalar_record || owner == self.invalid_scalar_record {\\n             return problem(\\\"builtin record `{target.value}` cannot be extended\\\", target.line, target.col)\\n         }\\n-        if self.at(TokenKind.Colon) { return self.fail(\\\"traits are not supported\\\") }\\n+        if self.at(TokenKind.Colon) {\\n+            let _ = self.take()\\n+            let marker = try self.expect(TokenKind.Ident, \\\"expected NoCopy marker\\\")\\n+            if marker.value != \\\"NoCopy\\\" { return problem(\\\"traits are not supported\\\", marker.line, marker.col) }\\n+            try! self.nocopy_records.push(owner)\\n+            try! self.nocopy_lines.push(target.line)\\n+            try! self.nocopy_cols.push(target.col)\\n+            let _ = try self.expect(TokenKind.LBrace, \\\"NoCopy marker requires an empty body\\\")\\n+            self.skip_newlines()\\n+            let _ = try self.expect(TokenKind.RBrace, \\\"NoCopy marker requires an empty body\\\")\\n+            return\\n+        }\\n         if not self.at(TokenKind.LBrace) {\\n             return problem(\\\"generic extensions are not supported\\\", extension_token.line, extension_token.col)\\n         }\\n@@ -496,6 +590,7 @@ extension Compiler {\\n                     let pt = try self.parse_type()\\n                     if pt == ValueType.Void { return problem(\\\"parameter cannot have Void type\\\", pn.line, pn.col) }\\n                     if pt == ValueType.Builder { return problem(\\\"StringBuilder parameters must be references\\\", pn.line, pn.col) }\\n+                    if not reference_type(pt) && self.vector_containing_type(pt) { return problem(\\\"vector-containing parameters must be references\\\", pn.line, pn.col) }\\n                     try! pnames.push(pn.value)\\n                     try! ptypes.push(pt)\\n                     self.skip_newlines()\\n@@ -561,6 +656,7 @@ extension Compiler {\\n             if name.value == \\\"print\\\" {\\n                 return problem(\\\"`print` is reserved by the prototype\\\", name.line, name.col)\\n             }\\n+            if name.value == \\\"Vector\\\" { return problem(\\\"function name `Vector` is reserved\\\", name.line, name.col) }\\n             if self.signature_index(name.value) >= 0 {\\n                 return problem(\\\"duplicate function `{name.value}`\\\", name.line, name.col)\\n             }\\n@@ -594,6 +690,7 @@ extension Compiler {\\n                     let pt = try self.parse_type()\\n                     if pt == ValueType.Void { return problem(\\\"parameter cannot have Void type\\\", pn.line, pn.col) }\\n                     if pt == ValueType.Builder { return problem(\\\"StringBuilder parameters must be references\\\", pn.line, pn.col) }\\n+                    if not reference_type(pt) && self.vector_containing_type(pt) { return problem(\\\"vector-containing parameters must be references\\\", pn.line, pn.col) }\\n                     try! pnames.push(pn.value)\\n                     try! ptypes.push(pt)\\n                     self.skip_newlines()\\n@@ -831,14 +928,14 @@ extension Compiler {\\n             let temporary = try self.new_value(ty)\\n             var word = 0\\n             while word < layout.len() {\\n-                let snapshot_copy = self.emit_at(Op.Copy, t.line, t.col)\\n+                let snapshot_copy = self.emit_at(if vector_type(layout[word]) { Op.MoveVector } else { Op.Copy }, t.line, t.col)\\n                 self.code[snapshot_copy].dst = temporary + word\\n                 self.code[snapshot_copy].a = src + word\\n                 word += 1\\n             }\\n             word = 0\\n             while word < layout.len() {\\n-                let destination_copy = self.emit_at(Op.Copy, t.line, t.col)\\n+                let destination_copy = self.emit_at(if vector_type(layout[word]) { Op.MoveVector } else { Op.Copy }, t.line, t.col)\\n                 self.code[destination_copy].dst = dst + word\\n                 self.code[destination_copy].a = temporary + word\\n                 word += 1\\n@@ -847,7 +944,7 @@ extension Compiler {\\n         }\\n         var i = 0\\n         while i < layout.len() {\\n-            let at = self.emit_at(Op.Copy, t.line, t.col)\\n+            let at = self.emit_at(if vector_type(layout[i]) { Op.MoveVector } else { Op.Copy }, t.line, t.col)\\n             self.code[at].dst = dst + i\\n             self.code[at].a = src + i\\n             i += 1\\n@@ -964,6 +1061,9 @@ extension Compiler {\\n     func read_place(&var self, place: Place, t: &Token) -> Result<ExprValue, ProtoError> {\\n         try self.check_read(place.root, &t)\\n         if place.ty == ValueType.Builder { return problem(\\\"StringBuilder values cannot be copied\\\", t.line, t.col) }\\n+        if not copyable_type(&self.records, &self.sums, place.ty) {\\n+            return problem(\\\"cannot implicitly copy noncopyable value; use `move`\\\", t.line, t.col)\\n+        }\\n         if place.indirect {\\n             let dst = try self.new_value(place.ty)\\n             let at = self.emit_at(Op.LoadRef, t.line, t.col)\\n@@ -996,7 +1096,8 @@ extension Compiler {\\n             }\\n         }\\n         try! self.bindings.push(Binding(name: name.value, ty: ty, slot: slot,\\n-                                        mutable: mutable, depth: self.depth))\\n+                                        mutable: mutable, depth: self.depth, active: true,\\n+                                        loop_depth: self.loop_depth))\\n         return\\n     }\\n \\n@@ -1058,7 +1159,7 @@ extension Compiler {\\n         let layout = try value_layout(&self.records, &self.sums, target)\\n         var i = 0\\n         while i < layout.len() {\\n-            if layout[i] == ValueType.String {\\n+            if owning_type(layout[i]) {\\n                 let drop = self.emit_at(Op.Drop, t.line, t.col)\\n                 self.code[drop].a = dst + i\\n             } else {\\n@@ -1111,7 +1212,7 @@ extension Compiler {\\n         let layout = try value_layout(&self.records, &self.sums, target)\\n         var i = 0\\n         while i < layout.len() {\\n-            if layout[i] == ValueType.String {\\n+            if owning_type(layout[i]) {\\n                 let drop = self.emit_at(Op.Drop, t.line, t.col)\\n                 self.code[drop].a = dst + i\\n             } else {\\n@@ -1137,7 +1238,7 @@ extension Compiler {\\n         let layout = try value_layout(&self.records, &self.sums, target)\\n         var i = 0\\n         while i < layout.len() {\\n-            if layout[i] == ValueType.String {\\n+            if owning_type(layout[i]) {\\n                 let drop = self.emit_at(Op.Drop, t.line, t.col)\\n                 self.code[drop].a = dst + i\\n             } else {\\n@@ -1275,6 +1376,29 @@ extension Compiler {\\n \\n     func parse_atom(&var self) -> Result<ExprValue, ProtoError> {\\n         if self.depth > 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n+        if self.at(TokenKind.Move_) {\\n+            let move_token = self.take()\\n+            let name = try self.expect(TokenKind.Ident, \\\"move requires a whole local\\\")\\n+            if self.at(TokenKind.Dot) || self.at(TokenKind.LBracket) {\\n+                return problem(\\\"move requires a whole local\\\", move_token.line, move_token.col)\\n+            }\\n+            let bi = self.binding_index(name.value)\\n+            if bi < 0 { return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col) }\\n+            if not self.bindings[bi].active { return problem(\\\"use of moved value `{name.value}`\\\", name.line, name.col) }\\n+            if reference_type(self.bindings[bi].ty) { return problem(\\\"cannot move through a reference\\\", move_token.line, move_token.col) }\\n+            if copyable_type(&self.records, &self.sums, self.bindings[bi].ty) || self.bindings[bi].ty == ValueType.Builder {\\n+                return problem(\\\"move is supported only for vector-containing locals\\\", move_token.line, move_token.col)\\n+            }\\n+            if self.loop_depth > self.bindings[bi].loop_depth {\\n+                return problem(\\\"cannot move a local declared outside the current loop\\\", move_token.line, move_token.col)\\n+            }\\n+            try self.check_read(bi, &move_token)\\n+            try self.check_write(bi, &move_token)\\n+            let dst = try self.new_value(self.bindings[bi].ty)\\n+            try self.emit_copy_value(dst, self.bindings[bi].slot, self.bindings[bi].ty, &move_token)\\n+            self.bindings[bi].active = false\\n+            return expr_value(self.bindings[bi].ty, dst, false)\\n+        }\\n         if self.at(TokenKind.If) { return self.parse_if_value(ValueType.Void, false) }\\n         if self.at(TokenKind.Match) { return self.parse_match_value(ValueType.Void, false) }\\n         if self.at(TokenKind.NoneKw) {\\n@@ -1344,6 +1468,14 @@ extension Compiler {\\n         if result_constructor {\\n             return self.parse_result_constructor()\\n         }\\n+        var vector_constructor = false\\n+        if self.at(TokenKind.Ident) && constructor_token.value == \\\"Vector\\\" && self.pos + 1 < self.tokens.len() {\\n+            vector_constructor = self.tokens[self.pos + 1].kind == TokenKind.Lt\\n+        }\\n+        if vector_constructor {\\n+            let vector_ty = try self.parse_type()\\n+            return self.parse_vector_constructor(vector_ty, constructor_token)\\n+        }\\n         if self.at(TokenKind.Ident) || self.at(TokenKind.SelfKw) {\\n             let candidate = self.token()\\n             if self.binding_index(candidate.value) >= 0 {\\n@@ -1357,6 +1489,7 @@ extension Compiler {\\n                         return self.parse_string_intrinsic(receiver, method)\\n                     }\\n                     if place.ty == ValueType.Builder { return self.parse_builder_method(place, method) }\\n+                    if vector_type(place.ty) { return self.parse_vector_method(place, method) }\\n                     let owner = match place.ty { case Record(index) -> index, case _ -> -1 }\\n                     if owner < 0 { return problem(\\\"method receiver must be a record place\\\", dot.line, dot.col) }\\n                     if owner == self.scalar_record {\\n@@ -1379,6 +1512,9 @@ extension Compiler {\\n                     if mi < 0 { return problem(\\\"record `{self.records[owner].name}` has no method `{method.value}`\\\", method.line, method.col) }\\n                     return self.parse_method_call(place, method, mi)\\n                 }\\n+                if vector_type(place.ty) && self.at(TokenKind.LBracket) {\\n+                    return self.parse_vector_index(place, candidate)\\n+                }\\n                 return self.read_place(place, &candidate)\\n             }\\n         }\\n@@ -1504,6 +1640,7 @@ extension Compiler {\\n             if self.static_index(name.value) >= 0 { return problem(\\\"cannot assign to immutable static `{name.value}`\\\", name.line, name.col) }\\n             return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col)\\n         }\\n+        if not self.bindings[bi].active { return problem(\\\"use of moved value `{name.value}`\\\", name.line, name.col) }\\n         var place = Place(ty: self.bindings[bi].ty, slot: self.bindings[bi].slot,\\n                           mutable: self.bindings[bi].mutable, indirect: false, root: bi,\\n                           ref_ty: ValueType.Void)\\n@@ -1650,6 +1787,127 @@ extension Compiler {\\n         expr_value(result_ty, dst, true)\\n     }\\n \\n+    func vector_reference(&var self, receiver: Place, mutable: Bool,\\n+                          name: &Token) -> Result<Int, ProtoError> {\\n+        if mutable && not receiver.mutable { return problem(\\\"cannot call a mutating method on an immutable receiver\\\", name.line, name.col) }\\n+        try self.register_borrow(&receiver, mutable, &name)\\n+        let ref_ty = self.intern_reference(receiver.ty, mutable)\\n+        let slot = self.new_slot(ref_ty)\\n+        if receiver.indirect {\\n+            if not reference_transfer(&self.references, receiver.ref_ty, ref_ty) { return problem(\\\"receiver reference mutability does not match method\\\", name.line, name.col) }\\n+            let copy = self.emit_at(Op.Copy, name.line, name.col)\\n+            self.code[copy].dst = slot\\n+            self.code[copy].a = receiver.slot\\n+        } else {\\n+            let addr = self.emit_at(Op.Addr, name.line, name.col)\\n+            self.code[addr].dst = slot\\n+            self.code[addr].a = receiver.slot\\n+        }\\n+        slot\\n+    }\\n+\\n+    func parse_vector_constructor(&var self, ty: ValueType,\\n+                                  anchor: Token) -> Result<ExprValue, ProtoError> {\\n+        if not vector_type(ty) { return problem(\\\"Vector constructor requires a non-optional Vector type\\\", anchor.line, anchor.col) }\\n+        let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after Vector type\\\")\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        if not self.at(TokenKind.RParen) { return problem(\\\"Vector constructor accepts no arguments\\\", anchor.line, anchor.col) }\\n+        let _ = self.take()\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        let dst = self.new_slot(ty)\\n+        let at = self.emit_at(Op.VectorNew, anchor.line, anchor.col)\\n+        self.code[at].dst = dst\\n+        expr_value(ty, dst, true)\\n+    }\\n+\\n+    func parse_vector_method(&var self, receiver: Place,\\n+                             name: Token) -> Result<ExprValue, ProtoError> {\\n+        if name.value != \\\"len\\\" && name.value != \\\"push\\\" && name.value != \\\"get\\\" {\\n+            return problem(\\\"Vector has no method `{name.value}`\\\", name.line, name.col)\\n+        }\\n+        let saved = self.active_borrows.len()\\n+        let mutable = name.value == \\\"push\\\"\\n+        let receiver_slot = try self.vector_reference(receiver, mutable, &name)\\n+        let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after Vector method\\\")\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        if name.value == \\\"len\\\" {\\n+            if not self.at(TokenKind.RParen) { return problem(\\\"Vector.len accepts no arguments\\\", name.line, name.col) }\\n+            let _ = self.take()\\n+            self.paren_depth -= 1\\n+            self.depth -= 1\\n+            let dst = self.new_slot(ValueType.I64)\\n+            let at = self.emit_at(Op.VectorLen, name.line, name.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = receiver_slot\\n+            self.restore_borrows(saved)\\n+            return expr_value(ValueType.I64, dst, true)\\n+        }\\n+        let parameter = if name.value == \\\"push\\\" { \\\"value\\\" } else { \\\"index\\\" }\\n+        if self.at(TokenKind.RParen) { return problem(\\\"Vector.{name.value} requires exactly one argument\\\", name.line, name.col) }\\n+        if self.at(TokenKind.Ident) && self.pos + 1 < self.tokens.len() && self.tokens[self.pos + 1].kind == TokenKind.Colon {\\n+            let label = self.take()\\n+            let _ = self.take()\\n+            if label.value != parameter { return problem(\\\"argument label `{label.value}` does not match parameter `{parameter}`\\\", label.line, label.col) }\\n+            self.expr_space()\\n+        }\\n+        let expected = if name.value == \\\"push\\\" { try vector_element(&self.vectors, receiver.ty) } else { ValueType.I64 }\\n+        var argument = try self.parse_expected_expr(expected)\\n+        if argument.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n+        argument = try self.snapshot(argument, &name)\\n+        self.skip_newlines()\\n+        if self.at(TokenKind.Comma) { return problem(\\\"Vector.{name.value} requires exactly one argument\\\", name.line, name.col) }\\n+        let _ = try self.expect(TokenKind.RParen, \\\"Vector method requires exactly one argument\\\")\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        if name.value == \\\"push\\\" {\\n+            let result_ty = self.builder_result_type()\\n+            let dst = try self.new_value(result_ty)\\n+            let at = self.emit_at(Op.VectorPush, name.line, name.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = receiver_slot\\n+            self.code[at].b = argument.slot\\n+            match result_ty { case Result(index) -> { self.code[at].immediate = index }, case _ -> {} }\\n+            self.restore_borrows(saved)\\n+            return expr_value(result_ty, dst, true)\\n+        }\\n+        let element = try vector_element(&self.vectors, receiver.ty)\\n+        let result_ty = self.intern_optional(element)\\n+        let dst = try self.new_value(result_ty)\\n+        let at = self.emit_at(Op.VectorGet, name.line, name.col)\\n+        self.code[at].dst = dst\\n+        self.code[at].a = receiver_slot\\n+        self.code[at].b = argument.slot\\n+        match result_ty { case Optional(index) -> { self.code[at].immediate = index }, case _ -> {} }\\n+        self.restore_borrows(saved)\\n+        expr_value(result_ty, dst, true)\\n+    }\\n+\\n+    func parse_vector_index(&var self, receiver: Place,\\n+                            anchor: Token) -> Result<ExprValue, ProtoError> {\\n+        let saved = self.active_borrows.len()\\n+        let receiver_slot = try self.vector_reference(receiver, false, &anchor)\\n+        let _ = try self.expect(TokenKind.LBracket, \\\"expected `[` for Vector index\\\")\\n+        self.depth += 1\\n+        var index = try self.parse_expected_expr(ValueType.I64)\\n+        if index.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", anchor.line, anchor.col) }\\n+        index = try self.snapshot(index, &anchor)\\n+        let _ = try self.expect(TokenKind.RBracket, \\\"expected `]` after Vector index\\\")\\n+        self.depth -= 1\\n+        let element = try vector_element(&self.vectors, receiver.ty)\\n+        let dst = try self.new_value(element)\\n+        let at = self.emit_at(Op.VectorIndex, anchor.line, anchor.col)\\n+        self.code[at].dst = dst\\n+        self.code[at].a = receiver_slot\\n+        self.code[at].b = index.slot\\n+        self.restore_borrows(saved)\\n+        expr_value(element, dst, false)\\n+    }\\n+\\n     func parse_primary(&var self) -> Result<ExprValue, ProtoError> {\\n         var value = try self.parse_atom()\\n         self.expr_space()\\n@@ -2849,7 +3107,8 @@ extension Compiler {\\n         try self.emit_copy_value(dst, subject.slot + offset, payload, &name)\\n         self.scope_depth -= 1\\n         try! self.bindings.push(Binding(name: name.value, ty: payload, slot: dst,\\n-                                        mutable: mutable, depth: self.depth + 1))\\n+                                        mutable: mutable, depth: self.depth + 1,\\n+                                        active: true, loop_depth: self.loop_depth))\\n         return\\n     }\\n \\n@@ -2908,6 +3167,12 @@ extension Compiler {\\n             if has_expected { target = expected }\\n         }\\n         else { target = try self.merge_control_type(&then_value.value, &else_value.value, expected, has_expected, &it) }\\n+        if target == ValueType.Builder {\\n+            return problem(\\\"StringBuilder values cannot be copied\\\", it.line, it.col)\\n+        }\\n+        if not copyable_type(&self.records, &self.sums, target) {\\n+            return problem(\\\"vector-containing value control flow is not supported\\\", it.line, it.col)\\n+        }\\n         let dst = try self.new_value(target)\\n         var joins = Vector<Int>()\\n         if then_exit >= 0 { try self.append_value_exit(then_exit, then_value.value, target, dst, &it, &var joins) }\\n@@ -2965,6 +3230,12 @@ extension Compiler {\\n         if then_value.always_returns { target = if has_expected { expected } else { else_value.value.ty } }\\n         else if else_value.always_returns { target = if has_expected { expected } else { then_value.value.ty } }\\n         else { target = try self.merge_control_type(&then_value.value, &else_value.value, expected, has_expected, &it) }\\n+        if target == ValueType.Builder {\\n+            return problem(\\\"StringBuilder values cannot be copied\\\", it.line, it.col)\\n+        }\\n+        if not copyable_type(&self.records, &self.sums, target) {\\n+            return problem(\\\"vector-containing value control flow is not supported\\\", it.line, it.col)\\n+        }\\n         let dst = try self.new_value(target)\\n         var joins = Vector<Int>()\\n         if then_exit >= 0 { try self.append_value_exit(then_exit, then_value.value, target, dst, &it, &var joins) }\\n@@ -3180,6 +3451,12 @@ extension Compiler {\\n             if candidate >= 0 { target = values[candidate].ty }\\n             else if saw_typed { return problem(\\\"control-flow branches have incompatible types\\\", mt.line, mt.col) }\\n         }\\n+        if target == ValueType.Builder {\\n+            return problem(\\\"StringBuilder values cannot be copied\\\", mt.line, mt.col)\\n+        }\\n+        if not copyable_type(&self.records, &self.sums, target) {\\n+            return problem(\\\"vector-containing value control flow is not supported\\\", mt.line, mt.col)\\n+        }\\n         let dst = try self.new_value(target)\\n         var joins = Vector<Int>()\\n         n = 0\\n@@ -3312,6 +3589,12 @@ extension Compiler {\\n             if candidate >= 0 { target = values[candidate].ty }\\n             else if saw_typed { return problem(\\\"control-flow branches have incompatible types\\\", mt.line, mt.col) }\\n         }\\n+        if target == ValueType.Builder {\\n+            return problem(\\\"StringBuilder values cannot be copied\\\", mt.line, mt.col)\\n+        }\\n+        if not copyable_type(&self.records, &self.sums, target) {\\n+            return problem(\\\"vector-containing value control flow is not supported\\\", mt.line, mt.col)\\n+        }\\n         let dst = try self.new_value(target)\\n         var joins = Vector<Int>()\\n         var n = 0\\n@@ -3659,7 +3942,9 @@ extension Compiler {\\n             self.code[branch].a = cond.slot\\n             let exit = self.emit_at(Op.Jump, t.line, t.col)\\n             self.code[branch].target = self.code.len()\\n+            self.loop_depth += 1\\n             let _ = try self.parse_block()\\n+            self.loop_depth -= 1\\n             let back = self.emit_at(Op.Jump, t.line, t.col)\\n             self.code[back].target = cond_start\\n             self.code[exit].target = self.code.len()\\n@@ -3699,6 +3984,7 @@ extension Compiler {\\n             let place = parsed.1\\n             let assign = self.take()\\n             if place.ty == ValueType.Builder { return problem(\\\"StringBuilder values cannot be assigned\\\", assign.line, assign.col) }\\n+            if not copyable_type(&self.records, &self.sums, place.ty) { return problem(\\\"vector-containing values cannot be assigned\\\", assign.line, assign.col) }\\n             if not place.mutable {\\n                 if place.indirect { return problem(\\\"cannot assign through a shared reference\\\", name.line, name.col) }\\n                 return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col)\\n@@ -3742,6 +4028,7 @@ extension Compiler {\\n         self.scope_depth = 0\\n         self.expr_depth = 0\\n         self.paren_depth = 0\\n+        self.loop_depth = 0\\n         self.bindings = Vector<Binding>()\\n         self.active_borrows = Vector<Borrow>()\\n         self.pending_writes = Vector<Int>()\\n@@ -3799,6 +4086,9 @@ extension Compiler {\\n                     let c = self.emit_at(Op.StringConst, self.signatures[index].line, self.signatures[index].col)\\n                     self.code[c].dst = slot + word\\n                     self.code[c].immediate = self.intern_string(\\\"\\\")\\n+                } else if vector_type(layout[word]) {\\n+                    let c = self.emit_at(Op.Drop, self.signatures[index].line, self.signatures[index].col)\\n+                    self.code[c].a = slot + word\\n                 } else {\\n                 let c = self.emit_at(Op.Const, self.signatures[index].line, self.signatures[index].col)\\n                 self.code[c].dst = slot + word\\n@@ -3848,18 +4138,25 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n     try! records.push(RecordIR(name: \\\"AllocError\\\", field_names: move alloc_fields,\\n                                field_types: move alloc_types, line: 0, col: 0))\\n     var compiler = Compiler(source: source, tokens: move tokens, records: move records, enums: Vector<EnumIR>(),\\n-                            references: Vector<ReferenceIR>(), sums: Vector<SumIR>(), strings: Vector<String>(), statics: Vector<StaticBinding>(),\\n+                            references: Vector<ReferenceIR>(), sums: Vector<SumIR>(), vectors: Vector<VectorIR>(),\\n+                            vector_lines: Vector<Int>(), vector_cols: Vector<Int>(), strings: Vector<String>(), statics: Vector<StaticBinding>(),\\n+                            nocopy_records: Vector<Int>(), nocopy_lines: Vector<Int>(), nocopy_cols: Vector<Int>(),\\n                             signatures: Vector<Signature>(), pos: 0, depth: 0, scope_depth: 0,\\n                             expr_depth: 0, paren_depth: 0,\\n                             bindings: Vector<Binding>(), active_borrows: Vector<Borrow>(), pending_writes: Vector<Int>(),\\n                             string_slots: Vector<StringSlot>(),\\n                             slots: Vector<ValueType>(),\\n                             code: Vector<Instruction>(), current_result: ValueType.Void,\\n-                            scalar_record: 0, invalid_scalar_record: 1, alloc_error_record: 2)\\n+                            scalar_record: 0, invalid_scalar_record: 1, alloc_error_record: 2,\\n+                            loop_depth: 0)\\n     try compiler.collect_record_names()\\n     try compiler.scan_signatures()\\n+    try compiler.validate_nocopy_markers()\\n+    try compiler.validate_vector_elements()\\n+    try compiler.validate_signature_vectors()\\n     try validate_records(&compiler.records, &compiler.sums)\\n     try validate_sums(&compiler.records, &compiler.sums)\\n+    try validate_vectors(&compiler.records, &compiler.sums, &compiler.vectors)\\n     try validate_enums(&compiler.enums)\\n     var functions = Vector<FuncIR>()\\n     var main_index = -1\\n@@ -3870,8 +4167,10 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         try! functions.push(move f)\\n         i += 1\\n     }\\n+    try compiler.validate_vector_elements()\\n     Program(enums: compiler.enums.copy(), records: compiler.records.copy(), references: compiler.references.copy(), sums: compiler.sums.copy(),\\n             strings: compiler.strings.copy(),\\n             functions: move functions, main_index: main_index,\\n-            alloc_error_record: compiler.alloc_error_record, builder_fail_after: -1)\\n+            alloc_error_record: compiler.alloc_error_record, builder_fail_after: -1,\\n+            vectors: compiler.vectors.copy(), vector_fail_after: -1)\\n }\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex c1dedc2d..96411dfe 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -1,6 +1,7 @@\\n import src.model.*\\n import src.string_runtime.*\\n import src.builder_runtime.*\\n+import src.vector_runtime.*\\n import src.uint_parse_runtime.*\\n \\n func put(out: &var StringBuilder, text: String) {\\n@@ -32,7 +33,7 @@ func emit_aggregate_type(out: &var StringBuilder, layout: &Vector<ValueType>) {\\n     var word_index = 0\\n     while word_index < layout.len() {\\n         if word_index > 0 { put(&var out, \\\", \\\") }\\n-        put(&var out, if string_type(layout[word_index]) { \\\"ptr\\\" } else { \\\"i64\\\" })\\n+        put(&var out, if string_type(layout[word_index]) || vector_type(layout[word_index]) { \\\"ptr\\\" } else { \\\"i64\\\" })\\n         word_index = word_index + 1\\n     }\\n     put(&var out, \\\" \\\\}\\\")\\n@@ -48,7 +49,7 @@ func emit_value_type(out: &var StringBuilder, records: &Vector<RecordIR>, sums:\\n         put(&var out, \\\"i64\\\")\\n         return\\n     }\\n-    if string_type(t) {\\n+    if string_type(t) || vector_type(t) {\\n         put(&var out, \\\"ptr\\\")\\n         return\\n     }\\n@@ -112,6 +113,80 @@ func assign_string_temp(out: &var StringBuilder, instruction_index: Int,\\n     store_ref_temp(&var out, instruction_index, suffix, slot)\\n }\\n \\n+func vector_helper_name(out: &var StringBuilder, prefix: String, descriptor: Int) {\\n+    put(&var out, \\\"@minivm_vector_\\\")\\n+    put(&var out, prefix)\\n+    put_int(&var out, descriptor)\\n+}\\n+\\n+func emit_vector_helpers(out: &var StringBuilder, program: &Program) -> Result<Void, ProtoError> {\\n+    var descriptor = 0\\n+    while descriptor < program.vectors.len() {\\n+        let element = program.vectors[descriptor].element\\n+        let layout = try value_layout(&program.records, &program.sums, element)\\n+        let width = layout.len()\\n+        put(&var out, \\\"define internal void \\\")\\n+        vector_helper_name(&var out, \\\"drop_\\\", descriptor)\\n+        put(&var out, \\\"(ptr %header) \\\\{\\\\nentry:\\\\n  %null = icmp eq ptr %header, null\\\\n  br i1 %null, label %done, label %start\\\\nstart:\\\\n  %len = call i64 @minivm_vector_len(ptr %header, i64 \\\")\\n+        put_int(&var out, width)\\n+        put(&var out, \\\")\\\\n  %datap = getelementptr i8, ptr %header, i64 8\\\\n  %data = load ptr, ptr %datap\\\\n  br label %loop\\\\nloop:\\\\n  %i = phi i64 [0, %start], [%next, %body]\\\\n  %more = icmp ult i64 %i, %len\\\\n  br i1 %more, label %body, label %free\\\\nbody:\\\\n  %base = mul i64 %i, \\\")\\n+        put_int(&var out, width)\\n+        put(&var out, \\\"\\\\n\\\")\\n+        var word = 0\\n+        while word < width {\\n+            if string_type(layout[word]) {\\n+                put(&var out, \\\"  %at\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\" = add i64 %base, \\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\"\\\\n  %ptr\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\" = getelementptr i64, ptr %data, i64 %at\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\"\\\\n  %value\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\" = load ptr, ptr %ptr\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\"\\\\n  call void @minivm_string_release(ptr %value\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\")\\\\n\\\")\\n+            }\\n+            word += 1\\n+        }\\n+        put(&var out, \\\"  %next = add i64 %i, 1\\\\n  br label %loop\\\\nfree:\\\\n  call void @minivm_vector_free_header(ptr %header)\\\\n  br label %done\\\\ndone:\\\\n  ret void\\\\n\\\\}\\\\n\\\\n\\\")\\n+\\n+        put(&var out, \\\"define internal \\\\{i64, i64, i64\\\\} \\\")\\n+        vector_helper_name(&var out, \\\"push_\\\", descriptor)\\n+        put(&var out, \\\"(ptr %header, ptr %incoming) \\\\{\\\\nentry:\\\\n  %prepared = call \\\\{i64, ptr, i64\\\\} @minivm_vector_prepare_push(ptr %header, i64 \\\")\\n+        put_int(&var out, width)\\n+        put(&var out, \\\")\\\\n  %ok = extractvalue \\\\{i64, ptr, i64\\\\} %prepared, 0\\\\n  %new_data = extractvalue \\\\{i64, ptr, i64\\\\} %prepared, 1\\\\n  %bytes = extractvalue \\\\{i64, ptr, i64\\\\} %prepared, 2\\\\n  %success = icmp ne i64 %ok, 0\\\\n  br i1 %success, label %install, label %failure\\\\ninstall:\\\\n  %len = load i64, ptr %header\\\\n  %base = mul i64 %len, \\\")\\n+        put_int(&var out, width)\\n+        put(&var out, \\\"\\\\n\\\")\\n+        word = 0\\n+        while word < width {\\n+            if string_type(layout[word]) {\\n+                put(&var out, \\\"  %incomingp\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\" = getelementptr i64, ptr %incoming, i64 \\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\"\\\\n  %incomingv\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\" = load ptr, ptr %incomingp\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\"\\\\n  call void @minivm_string_retain(ptr %incomingv\\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\")\\\\n\\\")\\n+            }\\n+            word += 1\\n+        }\\n+        put(&var out, \\\"  %tail = getelementptr i64, ptr %new_data, i64 %base\\\\n  call ptr @memcpy(ptr %tail, ptr %incoming, i64 \\\")\\n+        put_int(&var out, width * 8)\\n+        put(&var out, \\\")\\\\n  call void @minivm_vector_commit_push(ptr %header, ptr %new_data)\\\\n  ret \\\\{i64, i64, i64\\\\} \\\\{i64 1, i64 0, i64 0\\\\}\\\\nfailure:\\\\n  %err0 = insertvalue \\\\{i64, i64, i64\\\\} undef, i64 0, 0\\\\n  %err1 = insertvalue \\\\{i64, i64, i64\\\\} %err0, i64 %bytes, 1\\\\n  %err2 = insertvalue \\\\{i64, i64, i64\\\\} %err1, i64 8, 2\\\\n  ret \\\\{i64, i64, i64\\\\} %err2\\\\n\\\\}\\\\n\\\\n\\\")\\n+        descriptor += 1\\n+    }\\n+    return\\n+}\\n+\\n func put_hex_byte(out: &var StringBuilder, byte: Int) {\\n     let digits = \\\"0123456789ABCDEF\\\"\\n     put(&var out, \\\"\\\\\\\\\\\")\\n@@ -439,6 +514,33 @@ func emit_builder_append(out: &var StringBuilder, function: &FuncIR,\\n     next_block(&var out, instruction_index)\\n }\\n \\n+func vector_descriptor(t: ValueType) -> Int {\\n+    match t { case Vector(index) -> index, case _ -> -1 }\\n+}\\n+\\n+func emit_vector_element_copy(out: &var StringBuilder, instruction_index: Int,\\n+                              source_suffix: String, dst: Int,\\n+                              layout: &Vector<ValueType>) {\\n+    var word = 0\\n+    while word < layout.len() {\\n+        put(&var out, \\\"  \\\")\\n+        temp_name(&var out, instruction_index, \\\"elementp{word}\\\")\\n+        put(&var out, \\\" = getelementptr i64, ptr \\\")\\n+        temp_name(&var out, instruction_index, source_suffix)\\n+        put(&var out, \\\", i64 \\\")\\n+        put_int(&var out, word)\\n+        put(&var out, \\\"\\\\n  \\\")\\n+        temp_name(&var out, instruction_index, \\\"element{word}\\\")\\n+        put(&var out, if string_type(layout[word]) { \\\" = load ptr, ptr \\\" } else { \\\" = load i64, ptr \\\" })\\n+        temp_name(&var out, instruction_index, \\\"elementp{word}\\\")\\n+        put(&var out, \\\"\\\\n\\\")\\n+        if string_type(layout[word]) {\\n+            assign_string_temp(&var out, instruction_index, \\\"element{word}\\\", dst + word, true)\\n+        } else { store_temp(&var out, instruction_index, \\\"element{word}\\\", dst + word) }\\n+        word += 1\\n+    }\\n+}\\n+\\n func emit_instruction(out: &var StringBuilder, program: &Program,\\n                       function: &FuncIR, instruction_index: Int,\\n                       inst: &Instruction) -> Result<Void, ProtoError> {\\n@@ -761,6 +863,15 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             put(&var out, \\\")\\\\n\\\")\\n             if string_type(callee.result) {\\n                 assign_string_temp(&var out, instruction_index, \\\"call\\\", inst.dst, false)\\n+            } else if vector_type(callee.result) {\\n+                put(&var out, \\\"  \\\")\\n+                load_ref_slot(&var out, instruction_index, \\\"oldcallvector\\\", inst.dst)\\n+                put(&var out, \\\"  call void \\\")\\n+                vector_helper_name(&var out, \\\"drop_\\\", vector_descriptor(callee.result))\\n+                put(&var out, \\\"(ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"oldcallvector\\\")\\n+                put(&var out, \\\")\\\\n\\\")\\n+                store_ref_temp(&var out, instruction_index, \\\"call\\\", inst.dst)\\n             } else if scalar_type(callee.result) {\\n                 store_temp(&var out, instruction_index, \\\"call\\\", inst.dst)\\n             } else if callee.result != ValueType.Void {\\n@@ -777,6 +888,15 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                     put(&var out, \\\"\\\\n\\\")\\n                     if string_type(result_layout[result_index]) {\\n                         assign_string_temp(&var out, instruction_index, \\\"result{result_index}\\\", inst.dst + result_index, false)\\n+                    } else if vector_type(result_layout[result_index]) {\\n+                        put(&var out, \\\"  \\\")\\n+                        load_ref_slot(&var out, instruction_index, \\\"oldresultvector{result_index}\\\", inst.dst + result_index)\\n+                        put(&var out, \\\"  call void \\\")\\n+                        vector_helper_name(&var out, \\\"drop_\\\", vector_descriptor(result_layout[result_index]))\\n+                        put(&var out, \\\"(ptr \\\")\\n+                        temp_name(&var out, instruction_index, \\\"oldresultvector{result_index}\\\")\\n+                        put(&var out, \\\")\\\\n\\\")\\n+                        store_ref_temp(&var out, instruction_index, \\\"result{result_index}\\\", inst.dst + result_index)\\n                     } else {\\n                         store_temp(&var out, instruction_index, \\\"result{result_index}\\\", inst.dst + result_index)\\n                     }\\n@@ -888,10 +1008,179 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             emit_builder_append(&var out, &function, instruction_index, &inst,\\n                                 \\\"minivm_builder_append_scalar\\\")\\n         },\\n+        case VectorNew -> {\\n+            let descriptor = vector_descriptor(function.slots[inst.dst])\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"oldvector\\\", inst.dst)\\n+            put(&var out, \\\"  call void \\\")\\n+            vector_helper_name(&var out, \\\"drop_\\\", descriptor)\\n+            put(&var out, \\\"(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"oldvector\\\")\\n+            put(&var out, \\\")\\\\n  \\\")\\n+            temp_name(&var out, instruction_index, \\\"newvector\\\")\\n+            put(&var out, \\\" = call ptr @minivm_vector_new()\\\\n\\\")\\n+            store_ref_temp(&var out, instruction_index, \\\"newvector\\\", inst.dst)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case VectorLen -> {\\n+            let target = try reference_target(&program.references, function.slots[inst.a])\\n+            let descriptor = vector_descriptor(target)\\n+            let layout = try value_layout(&program.records, &program.sums,\\n+                                          try vector_element(&program.vectors, target))\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"vectorref\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"vector\\\")\\n+            put(&var out, \\\" = load ptr, ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"vectorref\\\")\\n+            put(&var out, \\\"\\\\n  \\\")\\n+            temp_name(&var out, instruction_index, \\\"length\\\")\\n+            put(&var out, \\\" = call i64 @minivm_vector_len(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"vector\\\")\\n+            put(&var out, \\\", i64 \\\")\\n+            put_int(&var out, layout.len())\\n+            put(&var out, \\\")\\\\n\\\")\\n+            store_temp(&var out, instruction_index, \\\"length\\\", inst.dst)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case VectorIndex -> {\\n+            let target = try reference_target(&program.references, function.slots[inst.a])\\n+            let layout = try value_layout(&program.records, &program.sums,\\n+                                          try vector_element(&program.vectors, target))\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"vectorref\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"vector\\\")\\n+            put(&var out, \\\" = load ptr, ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"vectorref\\\")\\n+            put(&var out, \\\"\\\\n  \\\")\\n+            load_slot(&var out, instruction_index, \\\"index\\\", inst.b)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"elementptr\\\")\\n+            put(&var out, \\\" = call ptr @minivm_vector_get_ptr(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"vector\\\")\\n+            put(&var out, \\\", i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"index\\\")\\n+            put(&var out, \\\", i64 \\\")\\n+            put_int(&var out, layout.len())\\n+            put(&var out, \\\", i1 true)\\\\n\\\")\\n+            emit_vector_element_copy(&var out, instruction_index, \\\"elementptr\\\", inst.dst, &layout)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case VectorGet -> {\\n+            let target = try reference_target(&program.references, function.slots[inst.a])\\n+            let layout = try value_layout(&program.records, &program.sums,\\n+                                          try vector_element(&program.vectors, target))\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"vectorref\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"vector\\\")\\n+            put(&var out, \\\" = load ptr, ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"vectorref\\\")\\n+            put(&var out, \\\"\\\\n  \\\")\\n+            load_slot(&var out, instruction_index, \\\"index\\\", inst.b)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"elementptr\\\")\\n+            put(&var out, \\\" = call ptr @minivm_vector_get_ptr(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"vector\\\")\\n+            put(&var out, \\\", i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"index\\\")\\n+            put(&var out, \\\", i64 \\\")\\n+            put_int(&var out, layout.len())\\n+            put(&var out, \\\", i1 false)\\\\n  \\\")\\n+            temp_name(&var out, instruction_index, \\\"found\\\")\\n+            put(&var out, \\\" = icmp ne ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"elementptr\\\")\\n+            put(&var out, \\\", null\\\\n  br i1 \\\")\\n+            temp_name(&var out, instruction_index, \\\"found\\\")\\n+            put(&var out, \\\", label %i\\\")\\n+            put_int(&var out, instruction_index)\\n+            put(&var out, \\\".found, label %i\\\")\\n+            put_int(&var out, instruction_index)\\n+            put(&var out, \\\".missing\\\\n\\\\ni\\\")\\n+            put_int(&var out, instruction_index)\\n+            put(&var out, \\\".found:\\\\n  store i64 1, ptr \\\")\\n+            slot_name(&var out, inst.dst)\\n+            put(&var out, \\\"\\\\n\\\")\\n+            emit_vector_element_copy(&var out, instruction_index, \\\"elementptr\\\", inst.dst + 1, &layout)\\n+            next_block(&var out, instruction_index)\\n+            put(&var out, \\\"\\\\ni\\\")\\n+            put_int(&var out, instruction_index)\\n+            put(&var out, \\\".missing:\\\\n  store i64 0, ptr \\\")\\n+            slot_name(&var out, inst.dst)\\n+            put(&var out, \\\"\\\\n\\\")\\n+            var word = 0\\n+            while word < layout.len() {\\n+                if string_type(layout[word]) {\\n+                    put(&var out, \\\"  \\\")\\n+                    load_ref_slot(&var out, instruction_index, \\\"missold{word}\\\", inst.dst + 1 + word)\\n+                    put(&var out, \\\"  call void @minivm_string_release(ptr \\\")\\n+                    temp_name(&var out, instruction_index, \\\"missold{word}\\\")\\n+                    put(&var out, \\\")\\\\n  store ptr null, ptr \\\")\\n+                } else { put(&var out, \\\"  store i64 0, ptr \\\") }\\n+                slot_name(&var out, inst.dst + 1 + word)\\n+                put(&var out, \\\"\\\\n\\\")\\n+                word += 1\\n+            }\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case VectorPush -> {\\n+            let target = try reference_target(&program.references, function.slots[inst.a])\\n+            let descriptor = vector_descriptor(target)\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"vectorref\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"vector\\\")\\n+            put(&var out, \\\" = load ptr, ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"vectorref\\\")\\n+            put(&var out, \\\"\\\\n  \\\")\\n+            temp_name(&var out, instruction_index, \\\"push\\\")\\n+            put(&var out, \\\" = call \\\\{i64, i64, i64\\\\} \\\")\\n+            vector_helper_name(&var out, \\\"push_\\\", descriptor)\\n+            put(&var out, \\\"(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"vector\\\")\\n+            put(&var out, \\\", ptr \\\")\\n+            slot_name(&var out, inst.b)\\n+            put(&var out, \\\")\\\\n\\\")\\n+            var word = 0\\n+            while word < 3 {\\n+                put(&var out, \\\"  \\\")\\n+                temp_name(&var out, instruction_index, \\\"pushword{word}\\\")\\n+                put(&var out, \\\" = extractvalue \\\\{i64, i64, i64\\\\} \\\")\\n+                temp_name(&var out, instruction_index, \\\"push\\\")\\n+                put(&var out, \\\", \\\")\\n+                put_int(&var out, word)\\n+                put(&var out, \\\"\\\\n\\\")\\n+                store_temp(&var out, instruction_index, \\\"pushword{word}\\\", inst.dst + word)\\n+                word += 1\\n+            }\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case MoveVector -> {\\n+            let descriptor = vector_descriptor(function.slots[inst.a])\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"moved\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"moveold\\\", inst.dst)\\n+            put(&var out, \\\"  call void \\\")\\n+            vector_helper_name(&var out, \\\"drop_\\\", descriptor)\\n+            put(&var out, \\\"(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"moveold\\\")\\n+            put(&var out, \\\")\\\\n\\\")\\n+            store_ref_temp(&var out, instruction_index, \\\"moved\\\", inst.dst)\\n+            put(&var out, \\\"  store ptr null, ptr \\\")\\n+            slot_name(&var out, inst.a)\\n+            put(&var out, \\\"\\\\n\\\")\\n+            next_block(&var out, instruction_index)\\n+        },\\n         case Drop -> {\\n             put(&var out, \\\"  \\\")\\n             load_ref_slot(&var out, instruction_index, \\\"value\\\", inst.a)\\n-            put(&var out, \\\"  call void @minivm_string_release(ptr \\\")\\n+            if vector_type(function.slots[inst.a]) {\\n+                put(&var out, \\\"  call void \\\")\\n+                vector_helper_name(&var out, \\\"drop_\\\", vector_descriptor(function.slots[inst.a]))\\n+                put(&var out, \\\"(ptr \\\")\\n+            } else { put(&var out, \\\"  call void @minivm_string_release(ptr \\\") }\\n             temp_name(&var out, instruction_index, \\\"value\\\")\\n             put(&var out, \\\")\\\\n  store ptr null, ptr \\\")\\n             slot_name(&var out, inst.a)\\n@@ -939,6 +1228,12 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n                 temp_name(&var out, instruction_index, \\\"ret\\\")\\n                 put(&var out, \\\")\\\\n\\\")\\n+            } else if vector_type(function.result) {\\n+                put(&var out, \\\"  \\\")\\n+                load_ref_slot(&var out, instruction_index, \\\"ret\\\", inst.a)\\n+                put(&var out, \\\"  store ptr null, ptr \\\")\\n+                slot_name(&var out, inst.a)\\n+                put(&var out, \\\"\\\\n\\\")\\n             } else if function.result != ValueType.Void && not scalar_type(function.result) {\\n                 let result_layout = try value_layout(&program.records, &program.sums, function.result)\\n                 var result_index = 0\\n@@ -949,6 +1244,11 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                         put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n                         temp_name(&var out, instruction_index, \\\"retword{result_index}\\\")\\n                         put(&var out, \\\")\\\\n\\\")\\n+                    } else if vector_type(result_layout[result_index]) {\\n+                        load_ref_slot(&var out, instruction_index, \\\"retword{result_index}\\\", inst.a + result_index)\\n+                        put(&var out, \\\"  store ptr null, ptr \\\")\\n+                        slot_name(&var out, inst.a + result_index)\\n+                        put(&var out, \\\"\\\\n\\\")\\n                     } else { load_slot(&var out, instruction_index, \\\"retword{result_index}\\\", inst.a + result_index) }\\n                     result_index += 1\\n                 }\\n@@ -958,7 +1258,11 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 if owning_type(function.slots[cleanup]) {\\n                     put(&var out, \\\"  \\\")\\n                     load_ref_slot(&var out, instruction_index, \\\"cleanup{cleanup}\\\", cleanup)\\n-                    put(&var out, \\\"  call void @minivm_string_release(ptr \\\")\\n+                    if vector_type(function.slots[cleanup]) {\\n+                        put(&var out, \\\"  call void \\\")\\n+                        vector_helper_name(&var out, \\\"drop_\\\", vector_descriptor(function.slots[cleanup]))\\n+                        put(&var out, \\\"(ptr \\\")\\n+                    } else { put(&var out, \\\"  call void @minivm_string_release(ptr \\\") }\\n                     temp_name(&var out, instruction_index, \\\"cleanup{cleanup}\\\")\\n                     put(&var out, \\\")\\\\n  store ptr null, ptr \\\")\\n                     slot_name(&var out, cleanup)\\n@@ -972,6 +1276,10 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 put(&var out, \\\"  ret ptr \\\")\\n                 temp_name(&var out, instruction_index, \\\"ret\\\")\\n                 put(&var out, \\\"\\\\n\\\")\\n+            } else if vector_type(function.result) {\\n+                put(&var out, \\\"  ret ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"ret\\\")\\n+                put(&var out, \\\"\\\\n\\\")\\n             } else if scalar_type(function.result) {\\n                 put(&var out, \\\"  \\\")\\n                 load_slot(&var out, instruction_index, \\\"ret\\\", inst.a)\\n@@ -989,7 +1297,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                     put(&var out, \\\" \\\")\\n                     if result_index == 0 { put(&var out, \\\"undef\\\") }\\n                     else { temp_name(&var out, instruction_index, \\\"retagg{result_index - 1}\\\") }\\n-                    put(&var out, if string_type(result_layout[result_index]) { \\\", ptr \\\" } else { \\\", i64 \\\" })\\n+                    put(&var out, if string_type(result_layout[result_index]) || vector_type(result_layout[result_index]) { \\\", ptr \\\" } else { \\\", i64 \\\" })\\n                     temp_name(&var out, instruction_index, \\\"retword{result_index}\\\")\\n                     put(&var out, \\\", \\\")\\n                     put_int(&var out, result_index)\\n@@ -1127,6 +1435,8 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.umul.with.overflow.i64(i64, i64)\\\\n\\\\n\\\")\\n     emit_string_runtime(&var out)\\n     emit_builder_runtime(&var out, program.builder_fail_after)\\n+    emit_vector_runtime(&var out, program.vector_fail_after)\\n+    try emit_vector_helpers(&var out, &program)\\n     emit_uint_parser(&var out)\\n     put(&var out, \\\"define internal void @minivm_overflow() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.overflow)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     put(&var out, \\\"define internal void @minivm_divzero() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.divzero)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n@@ -1141,6 +1451,6 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     }\\n     put(&var out, \\\"define i32 @main() \\\\{\\\\nentry:\\\\n  call void @f\\\")\\n     put_int(&var out, program.main_index)\\n-    put(&var out, \\\"()\\\\n  call void @minivm_string_check_live()\\\\n  ret i32 0\\\\n\\\\}\\\\n\\\")\\n+    put(&var out, \\\"()\\\\n  call void @minivm_string_check_live()\\\\n  call void @minivm_vector_check_live()\\\\n  ret i32 0\\\\n\\\\}\\\\n\\\")\\n     out.build()\\n }\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex f80950e9..fc6e3e0f 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -2,7 +2,7 @@ public enum ValueType {\\n     case I8, case I16, case I32, case I64,\\n     case U8, case U16, case U32, case U64, case Byte,\\n     case Bool, case Void, case String, case Builder, case Record(index: Int), case Enum(index: Int), case Ref(index: Int),\\n-    case Optional(index: Int), case Result(index: Int)\\n+    case Optional(index: Int), case Result(index: Int), case Vector(index: Int)\\n }\\n @synthesize\\n extension ValueType: Equatable {}\\n@@ -16,6 +16,7 @@ public enum Op {\\n     case StringConst, case StringLen, case StringByteAt, case StringSlice, case StringToUInt,\\n     case BuilderNew, case BuilderBuild, case BuilderAppendString, case BuilderAppendByte,\\n     case BuilderAppendInt, case BuilderAppendScalar, case Drop,\\n+    case VectorNew, case VectorLen, case VectorIndex, case VectorGet, case VectorPush, case MoveVector,\\n     case BitAnd, case BitOr, case BitXor, case BitNot, case Shl, case Shr\\n }\\n @synthesize\\n@@ -141,8 +142,16 @@ public struct Program {\\n     public main_index: Int\\n     public alloc_error_record: Int\\n     public builder_fail_after: Int\\n+    public vectors: Vector<VectorIR>\\n+    public vector_fail_after: Int\\n }\\n \\n+public struct VectorIR {\\n+    public element: ValueType\\n+}\\n+@synthesize\\n+extension VectorIR: ExplicitCopy {}\\n+\\n public struct ReferenceIR {\\n     public target: ValueType\\n     public mutable: Bool\\n@@ -191,7 +200,42 @@ public func printable_type(t: ValueType) -> Bool {\\n \\n public func string_type(t: ValueType) -> Bool { t == ValueType.String }\\n public func builder_type(t: ValueType) -> Bool { t == ValueType.Builder }\\n-public func owning_type(t: ValueType) -> Bool { string_type(t) || builder_type(t) }\\n+public func vector_type(t: ValueType) -> Bool {\\n+    match t { case Vector(_) -> true, case _ -> false }\\n+}\\n+public func owning_type(t: ValueType) -> Bool { string_type(t) || builder_type(t) || vector_type(t) }\\n+\\n+public func vector_element(vectors: &Vector<VectorIR>, t: ValueType) -> Result<ValueType, ProtoError> {\\n+    match t {\\n+        case Vector(index) -> {\\n+            if index < 0 || index >= vectors.len() { return problem(\\\"invalid vector type index\\\", 0, 0) }\\n+            return vectors[index].element\\n+        },\\n+        case _ -> { return problem(\\\"expected vector type\\\", 0, 0) }\\n+    }\\n+}\\n+\\n+public func copyable_type(records: &Vector<RecordIR>, sums: &Vector<SumIR>, t: ValueType) -> Bool {\\n+    if t == ValueType.Void || reference_type(t) || builder_type(t) { return false }\\n+    let layout = try value_layout(&records, &sums, t) catch { return false }\\n+    var word = 0\\n+    while word < layout.len() {\\n+        if vector_type(layout[word]) || builder_type(layout[word]) { return false }\\n+        word += 1\\n+    }\\n+    true\\n+}\\n+\\n+public func validate_vectors(records: &Vector<RecordIR>, sums: &Vector<SumIR>, vectors: &Vector<VectorIR>) -> Result<Void, ProtoError> {\\n+    var index = 0\\n+    while index < vectors.len() {\\n+        if not copyable_type(&records, &sums, vectors[index].element) {\\n+            return problem(\\\"Vector elements must be non-Void Copy values\\\", 0, 0)\\n+        }\\n+        index += 1\\n+    }\\n+    return\\n+}\\n \\n public func scalar_type(t: ValueType) -> Bool {\\n     match t {\\n@@ -277,7 +321,7 @@ public func reference_transfer(refs: &Vector<ReferenceIR>, source: ValueType, ta\\n public func validate_references(refs: &Vector<ReferenceIR>) -> Result<Void, ProtoError> {\\n     var n = 0\\n     while n < refs.len() {\\n-        if not scalar_type(refs[n].target) && not string_type(refs[n].target) && not builder_type(refs[n].target) {\\n+        if not scalar_type(refs[n].target) && not owning_type(refs[n].target) {\\n             match refs[n].target {\\n                 case Record(_) -> {},\\n                 case Optional(_) -> {},\\n@@ -318,7 +362,7 @@ func append_layout(records: &Vector<RecordIR>, sums: &Vector<SumIR>, t: ValueTyp\\n                    record_path: &var Vector<Int>, sum_path: &var Vector<Int>, depth: Int,\\n                    words: &var Vector<ValueType>) -> Result<Void, ProtoError> {\\n     if builder_type(t) { return problem(\\\"StringBuilder cannot be stored in an aggregate\\\", 0, 0) }\\n-    if scalar_type(t) || string_type(t) {\\n+    if scalar_type(t) || string_type(t) || vector_type(t) {\\n         if words.len() >= 256 { return problem(\\\"record layout exceeds 256 words\\\", 0, 0) }\\n         try! words.push(t)\\n         return\\n@@ -392,7 +436,7 @@ func append_layout(records: &Vector<RecordIR>, sums: &Vector<SumIR>, t: ValueTyp\\n \\n public func value_layout(records: &Vector<RecordIR>, sums: &Vector<SumIR>, t: ValueType) -> Result<Vector<ValueType>, ProtoError> {\\n     var words = Vector<ValueType>()\\n-    if reference_type(t) || string_type(t) || builder_type(t) {\\n+    if reference_type(t) || owning_type(t) {\\n         try! words.push(t)\\n         return move words\\n     }\\ndiff --git a/prototypes/minivm/src/vector_runtime.saw b/prototypes/minivm/src/vector_runtime.saw\\nnew file mode 100644\\nindex 00000000..fd59430f\\n--- /dev/null\\n+++ b/prototypes/minivm/src/vector_runtime.saw\\n@@ -0,0 +1,21 @@\\n+func vector_ir(out: &var StringBuilder, text: String) { try! out.append(text) }\\n+\\n+public func emit_vector_runtime(out: &var StringBuilder, fail_after: Int) {\\n+    vector_ir(&var out, \\\"@.vector_alloc_error = private unnamed_addr constant [40 x i8] c\\\\\\\"runtime error: vector allocation failed\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    vector_ir(&var out, \\\"@.vector_handle_error = private unnamed_addr constant [37 x i8] c\\\\\\\"runtime error: invalid vector handle\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    vector_ir(&var out, \\\"@.vector_index_error = private unnamed_addr constant [42 x i8] c\\\\\\\"runtime error: vector index out of bounds\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    vector_ir(&var out, \\\"@.vector_length_error = private unnamed_addr constant [38 x i8] c\\\\\\\"runtime error: vector length overflow\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    vector_ir(&var out, \\\"@.vector_live_error = private unnamed_addr constant [38 x i8] c\\\\\\\"runtime error: live vector allocation\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    vector_ir(&var out, \\\"@minivm_vector_live = internal global i64 0\\\\n@minivm_vector_fail_after = internal global i64 \\\")\\n+    try! out.append(fail_after)\\n+    vector_ir(&var out, \\\"\\\\n\\\\n\\\")\\n+    vector_ir(&var out, \\\"define internal void @minivm_vector_fatal(ptr %message) \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr %message)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n+    vector_ir(&var out, \\\"define internal ptr @minivm_vector_new() \\\\{\\\\nentry:\\\\n  %header = call ptr @malloc(i64 16)\\\\n  %null = icmp eq ptr %header, null\\\\n  br i1 %null, label %fail, label %init\\\\nfail:\\\\n  call void @minivm_vector_fatal(ptr @.vector_alloc_error)\\\\n  unreachable\\\\ninit:\\\\n  store i64 0, ptr %header\\\\n  %datap = getelementptr i8, ptr %header, i64 8\\\\n  store ptr null, ptr %datap\\\\n  %live = load i64, ptr @minivm_vector_live\\\\n  %next = add i64 %live, 1\\\\n  store i64 %next, ptr @minivm_vector_live\\\\n  ret ptr %header\\\\n\\\\}\\\\n\\\\n\\\")\\n+    vector_ir(&var out, \\\"define internal i64 @minivm_vector_len(ptr %header, i64 %width) \\\\{\\\\nentry:\\\\n  %null = icmp eq ptr %header, null\\\\n  %bad_width = icmp sle i64 %width, 0\\\\n  %bad0 = or i1 %null, %bad_width\\\\n  br i1 %bad0, label %bad, label %load\\\\nload:\\\\n  %len = load i64, ptr %header\\\\n  %negative = icmp slt i64 %len, 0\\\\n  %limit = udiv i64 1152921504606846975, %width\\\\n  %too_long = icmp ugt i64 %len, %limit\\\\n  %bad_len = or i1 %negative, %too_long\\\\n  br i1 %bad_len, label %overflow, label %done\\\\nbad:\\\\n  call void @minivm_vector_fatal(ptr @.vector_handle_error)\\\\n  unreachable\\\\noverflow:\\\\n  call void @minivm_vector_fatal(ptr @.vector_length_error)\\\\n  unreachable\\\\ndone:\\\\n  ret i64 %len\\\\n\\\\}\\\\n\\\\n\\\")\\n+    vector_ir(&var out, \\\"define internal ptr @minivm_vector_get_ptr(ptr %header, i64 %index, i64 %width, i1 %checked) \\\\{\\\\nentry:\\\\n  %len = call i64 @minivm_vector_len(ptr %header, i64 %width)\\\\n  %negative = icmp slt i64 %index, 0\\\\n  %past = icmp uge i64 %index, %len\\\\n  %miss = or i1 %negative, %past\\\\n  br i1 %miss, label %missing, label %address\\\\nmissing:\\\\n  br i1 %checked, label %trap, label %none\\\\ntrap:\\\\n  call void @minivm_vector_fatal(ptr @.vector_index_error)\\\\n  unreachable\\\\nnone:\\\\n  ret ptr null\\\\naddress:\\\\n  %datap = getelementptr i8, ptr %header, i64 8\\\\n  %data = load ptr, ptr %datap\\\\n  %word = mul i64 %index, %width\\\\n  %at = getelementptr i64, ptr %data, i64 %word\\\\n  ret ptr %at\\\\n\\\\}\\\\n\\\\n\\\")\\n+    // Returns {success,new buffer,requested bytes}; the header remains unchanged.\\n+    vector_ir(&var out, \\\"define internal \\\\{i64, ptr, i64\\\\} @minivm_vector_prepare_push(ptr %header, i64 %width) \\\\{\\\\nentry:\\\\n  %len = call i64 @minivm_vector_len(ptr %header, i64 %width)\\\\n  %limit = udiv i64 1152921504606846975, %width\\\\n  %full = icmp uge i64 %len, %limit\\\\n  br i1 %full, label %overflow, label %size\\\\noverflow:\\\\n  call void @minivm_vector_fatal(ptr @.vector_length_error)\\\\n  unreachable\\\\nsize:\\\\n  %new_len = add i64 %len, 1\\\\n  %words = mul i64 %new_len, %width\\\\n  %bytes = mul i64 %words, 8\\\\n  %counter = load i64, ptr @minivm_vector_fail_after\\\\n  %inject = icmp eq i64 %counter, 0\\\\n  br i1 %inject, label %inject_fail, label %allocate\\\\ninject_fail:\\\\n  store i64 -1, ptr @minivm_vector_fail_after\\\\n  br label %failure\\\\nallocate:\\\\n  %new_data = call ptr @malloc(i64 %bytes)\\\\n  %null = icmp eq ptr %new_data, null\\\\n  br i1 %null, label %failure, label %copy\\\\ncopy:\\\\n  %old_words = mul i64 %len, %width\\\\n  %old_bytes = mul i64 %old_words, 8\\\\n  %datap = getelementptr i8, ptr %header, i64 8\\\\n  %old_data = load ptr, ptr %datap\\\\n  %has_old = icmp ne i64 %old_bytes, 0\\\\n  br i1 %has_old, label %copy_old, label %copied\\\\ncopy_old:\\\\n  call ptr @memcpy(ptr %new_data, ptr %old_data, i64 %old_bytes)\\\\n  br label %copied\\\\ncopied:\\\\n  %positive = icmp sgt i64 %counter, 0\\\\n  br i1 %positive, label %decrement, label %success\\\\ndecrement:\\\\n  %counter_next = sub i64 %counter, 1\\\\n  store i64 %counter_next, ptr @minivm_vector_fail_after\\\\n  br label %success\\\\nsuccess:\\\\n  %ok0 = insertvalue \\\\{i64, ptr, i64\\\\} undef, i64 1, 0\\\\n  %ok1 = insertvalue \\\\{i64, ptr, i64\\\\} %ok0, ptr %new_data, 1\\\\n  %ok2 = insertvalue \\\\{i64, ptr, i64\\\\} %ok1, i64 %bytes, 2\\\\n  ret \\\\{i64, ptr, i64\\\\} %ok2\\\\nfailure:\\\\n  %no0 = insertvalue \\\\{i64, ptr, i64\\\\} undef, i64 0, 0\\\\n  %no1 = insertvalue \\\\{i64, ptr, i64\\\\} %no0, ptr null, 1\\\\n  %no2 = insertvalue \\\\{i64, ptr, i64\\\\} %no1, i64 %bytes, 2\\\\n  ret \\\\{i64, ptr, i64\\\\} %no2\\\\n\\\\}\\\\n\\\\n\\\")\\n+    vector_ir(&var out, \\\"define internal void @minivm_vector_commit_push(ptr %header, ptr %new_data) \\\\{\\\\nentry:\\\\n  %len = load i64, ptr %header\\\\n  %datap = getelementptr i8, ptr %header, i64 8\\\\n  %old_data = load ptr, ptr %datap\\\\n  %new_len = add i64 %len, 1\\\\n  store ptr %new_data, ptr %datap\\\\n  store i64 %new_len, ptr %header\\\\n  call void @free(ptr %old_data)\\\\n  ret void\\\\n\\\\}\\\\n\\\\n\\\")\\n+    vector_ir(&var out, \\\"define internal void @minivm_vector_free_header(ptr %header) \\\\{\\\\nentry:\\\\n  %datap = getelementptr i8, ptr %header, i64 8\\\\n  %data = load ptr, ptr %datap\\\\n  call void @free(ptr %data)\\\\n  call void @free(ptr %header)\\\\n  %live = load i64, ptr @minivm_vector_live\\\\n  %next = sub i64 %live, 1\\\\n  store i64 %next, ptr @minivm_vector_live\\\\n  ret void\\\\n\\\\}\\\\n\\\\n\\\")\\n+    vector_ir(&var out, \\\"define internal void @minivm_vector_check_live() \\\\{\\\\nentry:\\\\n  %live = load i64, ptr @minivm_vector_live\\\\n  %bad = icmp ne i64 %live, 0\\\\n  br i1 %bad, label %fail, label %done\\\\nfail:\\\\n  call void @minivm_vector_fatal(ptr @.vector_live_error)\\\\n  unreachable\\\\ndone:\\\\n  ret void\\\\n\\\\}\\\\n\\\\n\\\")\\n+}\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex 83f05536..2fdf1094 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -11,6 +11,9 @@ func invalid(i: &Instruction) -> ProtoError {\\n \\n func check_type(p: &Program, t: ValueType) -> Result<Void, ProtoError> {\\n     match t {\\n+        case Vector(index) -> {\\n+            if index < 0 || index >= p.vectors.len() { return problem(\\\"internal prototype error: invalid vector type index\\\", 0, 0) }\\n+        },\\n         case Enum(index) -> {\\n             if index < 0 || index >= p.enums.len() { return problem(\\\"internal prototype error: invalid enum type index\\\", 0, 0) }\\n         },\\n@@ -35,6 +38,11 @@ func check_type(p: &Program, t: ValueType) -> Result<Void, ProtoError> {\\n \\n func check_declared_types(p: &Program) -> Result<Void, ProtoError> {\\n     var n = 0\\n+    while n < p.vectors.len() {\\n+        try check_type(&p, p.vectors[n].element)\\n+        n += 1\\n+    }\\n+    n = 0\\n     while n < p.references.len() {\\n         try check_type(&p, p.references[n].target)\\n         n += 1\\n@@ -70,6 +78,9 @@ func check_declared_types(p: &Program) -> Result<Void, ProtoError> {\\n         while word < f.params.len() {\\n             try check_type(&p, f.params[word])\\n             if builder_type(f.params[word]) { return problem(\\\"StringBuilder by-value parameters are not supported\\\", 0, 0) }\\n+            if not reference_type(f.params[word]) && not copyable_type(&p.records, &p.sums, f.params[word]) {\\n+                return problem(\\\"noncopyable by-value parameters are not supported\\\", 0, 0)\\n+            }\\n             word += 1\\n         }\\n         word = 0\\n@@ -131,6 +142,45 @@ func check_builder_append(p: &Program, f: &FuncIR, i: &Instruction,\\n     return\\n }\\n \\n+func check_vector_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void, ProtoError> {\\n+    if i.target != -1 || i.args.len() != 0 { return invalid(&i) }\\n+    if i.op == Op.VectorNew {\\n+        if not vector_type(slot_type(&f, i.dst)) || i.a != -1 || i.b != -1 || i.immediate != 0 { return invalid(&i) }\\n+        return\\n+    }\\n+    if i.op == Op.MoveVector {\\n+        if (not vector_type(slot_type(&f, i.dst)) || slot_type(&f, i.a) != slot_type(&f, i.dst) ||\\n+            i.a == i.dst || i.b != -1 || i.immediate != 0) { return invalid(&i) }\\n+        return\\n+    }\\n+    let ref = slot_type(&f, i.a)\\n+    if not reference_type(ref) { return invalid(&i) }\\n+    let vector = try reference_target(&p.references, ref)\\n+    if not vector_type(vector) { return invalid(&i) }\\n+    let element = try vector_element(&p.vectors, vector)\\n+    let layout = try value_layout(&p.records, &p.sums, element)\\n+    if i.op == Op.VectorLen {\\n+        if slot_type(&f, i.dst) != ValueType.I64 || i.b != -1 || i.immediate != 0 { return invalid(&i) }\\n+    } else if i.op == Op.VectorIndex {\\n+        if slot_type(&f, i.b) != ValueType.I64 || i.immediate != 0 || not range_matches(&f, i.dst, &layout) { return invalid(&i) }\\n+    } else if i.op == Op.VectorGet {\\n+        if slot_type(&f, i.b) != ValueType.I64 || i.immediate < 0 || i.immediate >= p.sums.len() { return invalid(&i) }\\n+        let sum = p.sums[i.immediate].copy()\\n+        if sum.kind != SumKind.Optional || sum.payload != element { return invalid(&i) }\\n+        let result_layout = try value_layout(&p.records, &p.sums, ValueType.Optional(i.immediate))\\n+        if not range_matches(&f, i.dst, &result_layout) { return invalid(&i) }\\n+    } else {\\n+        if not reference_mutable(&p.references, ref) || not range_matches(&f, i.b, &layout) { return invalid(&i) }\\n+        if p.alloc_error_record < 0 || i.immediate < 0 || i.immediate >= p.sums.len() { return invalid(&i) }\\n+        let sum = p.sums[i.immediate].copy()\\n+        if (sum.kind != SumKind.Result || sum.payload != ValueType.Void ||\\n+            sum.error != ValueType.Record(p.alloc_error_record)) { return invalid(&i) }\\n+        let result_layout: Vector<ValueType> = [ValueType.Bool, ValueType.I64, ValueType.I64]\\n+        if not range_matches(&f, i.dst, &result_layout) { return invalid(&i) }\\n+    }\\n+    return\\n+}\\n+\\n func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void, ProtoError> {\\n     let dst = slot_type(&f, i.dst)\\n     let a = slot_type(&f, i.a)\\n@@ -181,6 +231,12 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n         case BuilderAppendByte -> { try check_builder_append(&p, &f, &i, ValueType.Byte) },\\n         case BuilderAppendInt -> { try check_builder_append(&p, &f, &i, ValueType.I64) },\\n         case BuilderAppendScalar -> { try check_builder_append(&p, &f, &i, ValueType.I64) },\\n+        case VectorNew -> { try check_vector_instruction(&p, &f, &i) },\\n+        case VectorLen -> { try check_vector_instruction(&p, &f, &i) },\\n+        case VectorIndex -> { try check_vector_instruction(&p, &f, &i) },\\n+        case VectorGet -> { try check_vector_instruction(&p, &f, &i) },\\n+        case VectorPush -> { try check_vector_instruction(&p, &f, &i) },\\n+        case MoveVector -> { try check_vector_instruction(&p, &f, &i) },\\n         case Drop -> {\\n             if not owning_type(a) { return invalid(&i) }\\n         },\\n@@ -193,14 +249,14 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n         case LoadRef -> {\\n             if not reference_type(a) { return invalid(&i) }\\n             let target = try reference_target(&p.references, a)\\n-            if builder_type(target) { return invalid(&i) }\\n+            if not copyable_type(&p.records, &p.sums, target) { return invalid(&i) }\\n             let layout = try value_layout(&p.records, &p.sums, target)\\n             if not range_matches(&f, i.dst, &layout) { return invalid(&i) }\\n         },\\n         case StoreRef -> {\\n             if not reference_mutable(&p.references, a) { return invalid(&i) }\\n             let target = try reference_target(&p.references, a)\\n-            if builder_type(target) { return invalid(&i) }\\n+            if not copyable_type(&p.records, &p.sums, target) { return invalid(&i) }\\n             let layout = try value_layout(&p.records, &p.sums, target)\\n             if not range_matches(&f, i.b, &layout) { return invalid(&i) }\\n         },\\n@@ -333,6 +389,7 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n \\n public func verify(program: &Program) -> Result<Void, ProtoError> {\\n     if program.builder_fail_after < -1 { return problem(\\\"invalid builder failure counter\\\", 0, 0) }\\n+    if program.vector_fail_after < -1 { return problem(\\\"invalid vector failure counter\\\", 0, 0) }\\n     if program.alloc_error_record < -1 || program.alloc_error_record >= program.records.len() {\\n         return problem(\\\"invalid AllocError builtin identity\\\", 0, 0)\\n     }\\n@@ -351,6 +408,7 @@ public func verify(program: &Program) -> Result<Void, ProtoError> {\\n     try check_declared_types(&program)\\n     try validate_sums(&program.records, &program.sums)\\n     try validate_records(&program.records, &program.sums)\\n+    try validate_vectors(&program.records, &program.sums, &program.vectors)\\n     if program.main_index < 0 || program.main_index >= program.functions.len() {\\n         return problem(\\\"internal prototype error: missing main\\\", 0, 0)\\n     }\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 23c6249e..1a634f5c 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -5,6 +5,8 @@ static MAX_CALL_DEPTH: Int = 128\\n struct CallResult { values: Vector<Int> }\\n extension CallResult: NoCopy {}\\n struct StringEntry { value: String, refs: Int }\\n+struct VectorEntry { descriptor: Int, alive: Bool, words: Vector<Int>, length: Int }\\n+extension VectorEntry: NoCopy {}\\n func runtime_error(message: String) -> ProtoError { problem(message, 0, 0) }\\n func overflow_error() -> ProtoError { runtime_error(\\\"runtime error: integer overflow\\\") }\\n \\n@@ -157,6 +159,108 @@ func string_assign(cells: &var Vector<Int>, dst: Int, source: Int,\\n     return\\n }\\n \\n+func vector_entry(heap: &Vector<VectorEntry>, handle: Int, descriptor: Int) -> Result<Int, ProtoError> {\\n+    if handle <= 0 { return runtime_error(\\\"runtime error: invalid vector handle\\\") }\\n+    let index = handle - 1\\n+    if index >= heap.len() || not heap[index].alive || heap[index].descriptor != descriptor {\\n+        return runtime_error(\\\"runtime error: invalid vector handle\\\")\\n+    }\\n+    index\\n+}\\n+\\n+func vector_descriptor(t: ValueType) -> Result<Int, ProtoError> {\\n+    match t {\\n+        case Vector(descriptor) -> { return descriptor },\\n+        case _ -> { return runtime_error(\\\"runtime error: invalid vector handle\\\") }\\n+    }\\n+}\\n+\\n+func vector_allocate(heap: &var Vector<VectorEntry>, descriptor: Int) -> Int {\\n+    var index = 0\\n+    while index < heap.len() {\\n+        if not heap[index].alive {\\n+            heap[index].descriptor = descriptor\\n+            heap[index].alive = true\\n+            heap[index].words = Vector<Int>()\\n+            heap[index].length = 0\\n+            return index + 1\\n+        }\\n+        index += 1\\n+    }\\n+    try! heap.push(VectorEntry(descriptor: descriptor, alive: true,\\n+        words: Vector<Int>(), length: 0))\\n+    heap.len()\\n+}\\n+\\n+func vector_release(program: &Program, vectors: &var Vector<VectorEntry>,\\n+                    strings: &var Vector<StringEntry>, handle: Int,\\n+                    descriptor: Int) -> Result<Void, ProtoError> {\\n+    if handle == 0 { return }\\n+    let index = try vector_entry(&vectors, handle, descriptor)\\n+    let element = try vector_element(&program.vectors, ValueType.Vector(descriptor))\\n+    let layout = try value_layout(&program.records, &program.sums, element)\\n+    if layout.len() <= 0 { return runtime_error(\\\"runtime error: invalid vector handle\\\") }\\n+    var word = 0\\n+    while word < vectors[index].words.len() {\\n+        if string_type(layout[word % layout.len()]) {\\n+            try string_release(&var strings, vectors[index].words[word])\\n+        }\\n+        word += 1\\n+    }\\n+    vectors[index].words = Vector<Int>()\\n+    vectors[index].length = 0\\n+    vectors[index].alive = false\\n+    return\\n+}\\n+\\n+func vector_assign_element(program: &Program, cells: &var Vector<Int>, dst: Int,\\n+                           source: &Vector<Int>, source_start: Int, element: ValueType,\\n+                           strings: &var Vector<StringEntry>) -> Result<Void, ProtoError> {\\n+    let layout = try value_layout(&program.records, &program.sums, element)\\n+    var word = 0\\n+    while word < layout.len() {\\n+        if string_type(layout[word]) {\\n+            let handle = source[source_start + word]\\n+            if handle != 0 {\\n+                let index = try string_entry(&strings, handle)\\n+                if strings[index].refs == Int.max { return runtime_error(\\\"runtime error: string allocation failed\\\") }\\n+            }\\n+        }\\n+        word += 1\\n+    }\\n+    word = 0\\n+    while word < layout.len() {\\n+        if string_type(layout[word]) { try string_retain(&var strings, source[source_start + word]) }\\n+        word += 1\\n+    }\\n+    word = 0\\n+    while word < layout.len() {\\n+        if string_type(layout[word]) {\\n+            try string_assign(&var cells, dst + word, source[source_start + word], &var strings, false)\\n+        } else { cells[dst + word] = source[source_start + word] }\\n+        word += 1\\n+    }\\n+    return\\n+}\\n+\\n+func vector_clear_element(program: &Program, cells: &var Vector<Int>, dst: Int,\\n+                          element: ValueType, strings: &var Vector<StringEntry>) -> Result<Void, ProtoError> {\\n+    let layout = try value_layout(&program.records, &program.sums, element)\\n+    var word = 0\\n+    while word < layout.len() {\\n+        if string_type(layout[word]) { try string_assign(&var cells, dst + word, 0, &var strings, false) }\\n+        else { cells[dst + word] = 0 }\\n+        word += 1\\n+    }\\n+    return\\n+}\\n+\\n+func vector_result(cells: &var Vector<Int>, dst: Int, success: Bool, size: Int) {\\n+    cells[dst] = if success { 1 } else { 0 }\\n+    cells[dst + 1] = if success { 0 } else { size }\\n+    cells[dst + 2] = if success { 0 } else { 8 }\\n+}\\n+\\n func checked_binary(a: Int, b: Int, t: ValueType, op: Op) -> Result<Int, ProtoError> {\\n     if integer_signed(t) {\\n         let lo = integer_min(t)\\n@@ -242,8 +346,10 @@ func valid_reference(cells: &Vector<Int>, address: Int, width: Int) -> Bool {\\n }\\n \\n func run_function(program: &Program, function_index: Int, arguments: &Vector<Int>,\\n-                  cells: &var Vector<Int>, heap: &var Vector<StringEntry>, remaining: &var Int,\\n-                  fail_after: &var Int, depth: Int) -> Result<CallResult, ProtoError> {\\n+                  cells: &var Vector<Int>, heap: &var Vector<StringEntry>,\\n+                  vector_heap: &var Vector<VectorEntry>, remaining: &var Int,\\n+                  fail_after: &var Int, vector_fail_after: &var Int,\\n+                  depth: Int) -> Result<CallResult, ProtoError> {\\n     if depth > MAX_CALL_DEPTH { return runtime_error(\\\"runtime error: call depth exceeded\\\") }\\n     let function = program.functions[function_index].copy()\\n     let base = cells.len()\\n@@ -396,11 +502,17 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                     try! args.push(cells[base + inst.args[i]])\\n                     i = i + 1\\n                 }\\n-                let result = try run_function(&program, inst.target, &args, &var cells, &var heap, &var remaining, &var fail_after, depth + 1)\\n+                let result = try run_function(&program, inst.target, &args, &var cells, &var heap,\\n+                    &var vector_heap, &var remaining, &var fail_after, &var vector_fail_after, depth + 1)\\n                 var result_index = 0\\n                 while result_index < result.values.len() {\\n                     if string_type(function.slots[inst.dst + result_index]) {\\n                         try string_assign(&var cells, base + inst.dst + result_index, result.values[result_index], &var heap, false)\\n+                    } else if vector_type(function.slots[inst.dst + result_index]) {\\n+                        let descriptor = try vector_descriptor(function.slots[inst.dst + result_index])\\n+                        try vector_release(&program, &var vector_heap, &var heap,\\n+                            cells[base + inst.dst + result_index], descriptor)\\n+                        cells[base + inst.dst + result_index] = result.values[result_index]\\n                     } else { cells[base + inst.dst + result_index] = result.values[result_index] }\\n                     result_index = result_index + 1\\n                 }\\n@@ -466,11 +578,168 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n             case BuilderAppendScalar -> {\\n                 try builder_append(inst.op, &var cells, base, inst.dst, inst.a, inst.b, &var heap, &var fail_after)\\n             },\\n+            case VectorNew -> {\\n+                match function.slots[inst.dst] {\\n+                    case Vector(descriptor) -> {\\n+                        try vector_release(&program, &var vector_heap, &var heap, cells[base + inst.dst], descriptor)\\n+                        cells[base + inst.dst] = vector_allocate(&var vector_heap, descriptor)\\n+                    },\\n+                    case _ -> { return runtime_error(\\\"runtime error: invalid vector handle\\\") }\\n+                }\\n+            },\\n+            case VectorLen -> {\\n+                let address = cells[base + inst.a]\\n+                if not valid_reference(&cells, address, 1) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                match try reference_target(&program.references, function.slots[inst.a]) {\\n+                    case Vector(descriptor) -> {\\n+                        let index = try vector_entry(&vector_heap, cells[address], descriptor)\\n+                        cells[base + inst.dst] = vector_heap[index].length\\n+                    },\\n+                    case _ -> { return runtime_error(\\\"runtime error: invalid vector handle\\\") }\\n+                }\\n+            },\\n+            case VectorIndex -> {\\n+                let address = cells[base + inst.a]\\n+                if not valid_reference(&cells, address, 1) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                match try reference_target(&program.references, function.slots[inst.a]) {\\n+                    case Vector(descriptor) -> {\\n+                        let index = try vector_entry(&vector_heap, cells[address], descriptor)\\n+                        let at = cells[base + inst.b]\\n+                        if at < 0 || at >= vector_heap[index].length {\\n+                            return runtime_error(\\\"runtime error: vector index out of bounds\\\")\\n+                        }\\n+                        let element = try vector_element(&program.vectors, ValueType.Vector(descriptor))\\n+                        let layout = try value_layout(&program.records, &program.sums, element)\\n+                        try vector_assign_element(&program, &var cells, base + inst.dst,\\n+                            &vector_heap[index].words, at * layout.len(), element, &var heap)\\n+                    },\\n+                    case _ -> { return runtime_error(\\\"runtime error: invalid vector handle\\\") }\\n+                }\\n+            },\\n+            case VectorGet -> {\\n+                let address = cells[base + inst.a]\\n+                if not valid_reference(&cells, address, 1) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                match try reference_target(&program.references, function.slots[inst.a]) {\\n+                    case Vector(descriptor) -> {\\n+                        let index = try vector_entry(&vector_heap, cells[address], descriptor)\\n+                        let element = try vector_element(&program.vectors, ValueType.Vector(descriptor))\\n+                        let at = cells[base + inst.b]\\n+                        if at < 0 || at >= vector_heap[index].length {\\n+                            cells[base + inst.dst] = 0\\n+                            try vector_clear_element(&program, &var cells, base + inst.dst + 1, element, &var heap)\\n+                        } else {\\n+                            cells[base + inst.dst] = 1\\n+                            let layout = try value_layout(&program.records, &program.sums, element)\\n+                            try vector_assign_element(&program, &var cells, base + inst.dst + 1,\\n+                                &vector_heap[index].words, at * layout.len(), element, &var heap)\\n+                        }\\n+                    },\\n+                    case _ -> { return runtime_error(\\\"runtime error: invalid vector handle\\\") }\\n+                }\\n+            },\\n+            case VectorPush -> {\\n+                let address = cells[base + inst.a]\\n+                if not valid_reference(&cells, address, 1) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                match try reference_target(&program.references, function.slots[inst.a]) {\\n+                    case Vector(descriptor) -> {\\n+                        let index = try vector_entry(&vector_heap, cells[address], descriptor)\\n+                        let element = try vector_element(&program.vectors, ValueType.Vector(descriptor))\\n+                        let layout = try value_layout(&program.records, &program.sums, element)\\n+                        let length = vector_heap[index].length\\n+                        if length < 0 || layout.len() <= 0 || length == Int.max || length + 1 > Int.max / layout.len() || (length + 1) * layout.len() > Int.max / 8 {\\n+                            return runtime_error(\\\"runtime error: vector length overflow\\\")\\n+                        }\\n+                        let requested = (length + 1) * layout.len() * 8\\n+                        var incoming = Vector<Int>()\\n+                        var word = 0\\n+                        var staging_failed = false\\n+                        while word < layout.len() {\\n+                            match incoming.push(cells[base + inst.b + word]) {\\n+                                case Ok(_) -> {}, case Err(_) -> { staging_failed = true }\\n+                            }\\n+                            if staging_failed { break }\\n+                            word += 1\\n+                        }\\n+                        if staging_failed {\\n+                            vector_result(&var cells, base + inst.dst, false, requested)\\n+                            pc += 1\\n+                            continue\\n+                        }\\n+                        if vector_fail_after == 0 {\\n+                            vector_fail_after = -1\\n+                            vector_result(&var cells, base + inst.dst, false, requested)\\n+                            pc += 1\\n+                            continue\\n+                        }\\n+                        var staged = Vector<Int>()\\n+                        word = 0\\n+                        while word < vector_heap[index].words.len() {\\n+                            match staged.push(vector_heap[index].words[word]) {\\n+                                case Ok(_) -> {}, case Err(_) -> { staging_failed = true }\\n+                            }\\n+                            if staging_failed { break }\\n+                            word += 1\\n+                        }\\n+                        word = 0\\n+                        while not staging_failed && word < layout.len() {\\n+                            match staged.push(incoming[word]) {\\n+                                case Ok(_) -> {}, case Err(_) -> { staging_failed = true }\\n+                            }\\n+                            word += 1\\n+                        }\\n+                        if staging_failed {\\n+                            vector_result(&var cells, base + inst.dst, false, requested)\\n+                            pc += 1\\n+                            continue\\n+                        }\\n+                        word = 0\\n+                        while word < layout.len() {\\n+                            if string_type(layout[word]) {\\n+                                let handle = incoming[word]\\n+                                if handle != 0 {\\n+                                    let string_index = try string_entry(&heap, handle)\\n+                                    if heap[string_index].refs == Int.max {\\n+                                        return runtime_error(\\\"runtime error: string allocation failed\\\")\\n+                                    }\\n+                                }\\n+                            }\\n+                            word += 1\\n+                        }\\n+                        word = 0\\n+                        while word < layout.len() {\\n+                            if string_type(layout[word]) { try string_retain(&var heap, incoming[word]) }\\n+                            word += 1\\n+                        }\\n+                        vector_heap[index].words = move staged\\n+                        vector_heap[index].length = length + 1\\n+                        if vector_fail_after > 0 { vector_fail_after -= 1 }\\n+                        vector_result(&var cells, base + inst.dst, true, 0)\\n+                    },\\n+                    case _ -> { return runtime_error(\\\"runtime error: invalid vector handle\\\") }\\n+                }\\n+            },\\n+            case MoveVector -> {\\n+                let descriptor = try vector_descriptor(function.slots[inst.dst])\\n+                if cells[base + inst.a] != 0 {\\n+                    let _ = try vector_entry(&vector_heap, cells[base + inst.a], descriptor)\\n+                }\\n+                try vector_release(&program, &var vector_heap, &var heap,\\n+                    cells[base + inst.dst], descriptor)\\n+                cells[base + inst.dst] = cells[base + inst.a]\\n+                cells[base + inst.a] = 0\\n+            },\\n             case Drop -> {\\n-                if builder_type(function.slots[inst.a]) && cells[base + inst.a] != 0 {\\n-                    let _ = try builder_entry(&heap, cells[base + inst.a])\\n+                let drop_type = function.slots[inst.a]\\n+                if vector_type(drop_type) {\\n+                    let descriptor = try vector_descriptor(drop_type)\\n+                    try vector_release(&program, &var vector_heap, &var heap,\\n+                        cells[base + inst.a], descriptor)\\n+                } else {\\n+                    if builder_type(drop_type) && cells[base + inst.a] != 0 {\\n+                        let _ = try builder_entry(&heap, cells[base + inst.a])\\n+                    }\\n+                    try string_release(&var heap, cells[base + inst.a])\\n                 }\\n-                try string_release(&var heap, cells[base + inst.a])\\n                 cells[base + inst.a] = 0\\n             },\\n             case Panic -> { return runtime_error(\\\"runtime error: try! failed\\\") },\\n@@ -481,12 +750,17 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 while result_index < layout.len() {\\n                     if string_type(layout[result_index]) { try string_retain(&var heap, cells[base + inst.a + result_index]) }\\n                     try! values.push(cells[base + inst.a + result_index])\\n+                    if vector_type(layout[result_index]) { cells[base + inst.a + result_index] = 0 }\\n                     result_index = result_index + 1\\n                 }\\n                 var slot = 0\\n                 while slot < function.slots.len() {\\n                     if owning_type(function.slots[slot]) {\\n-                        try string_release(&var heap, cells[base + slot])\\n+                        if vector_type(function.slots[slot]) {\\n+                            let descriptor = try vector_descriptor(function.slots[slot])\\n+                            try vector_release(&program, &var vector_heap, &var heap,\\n+                                cells[base + slot], descriptor)\\n+                        } else { try string_release(&var heap, cells[base + slot]) }\\n                         cells[base + slot] = 0\\n                     }\\n                     slot += 1\\n@@ -503,14 +777,22 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n public func execute(program: &Program, budget: Int) -> Result<Void, ProtoError> {\\n     var remaining = budget\\n     var fail_after = program.builder_fail_after\\n+    var vector_fail_after = program.vector_fail_after\\n     let arguments = Vector<Int>()\\n     var cells = Vector<Int>()\\n     var heap = Vector<StringEntry>()\\n-    let _ = try run_function(&program, program.main_index, &arguments, &var cells, &var heap, &var remaining, &var fail_after, 1)\\n+    var vector_heap = Vector<VectorEntry>()\\n+    let _ = try run_function(&program, program.main_index, &arguments, &var cells, &var heap,\\n+        &var vector_heap, &var remaining, &var fail_after, &var vector_fail_after, 1)\\n     var index = 0\\n     while index < heap.len() {\\n         if heap[index].refs != 0 { return runtime_error(\\\"runtime error: live string allocation\\\") }\\n         index += 1\\n     }\\n+    index = 0\\n+    while index < vector_heap.len() {\\n+        if vector_heap[index].alive { return runtime_error(\\\"runtime error: live vector allocation\\\") }\\n+        index += 1\\n+    }\\n     return\\n }\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex 50682cea..9331a90f 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -39,6 +39,15 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"vectors/basic\\\", \\\"3\\\\n10\\\\n30\\\\n20\\\\nnone\\\\nnone\\\\n\\\", section=\\\"vectors\\\", compare_sawc=True),\\n+    RunCase(\\\"vectors/strings_records\\\", \\\"old\\\\nnew\\\\na\\\\nb\\\\n\\\", section=\\\"vectors\\\", compare_sawc=True),\\n+    RunCase(\\\"vectors/references_fields\\\", \\\"2\\\\nleft\\\\nright\\\\n\\\", section=\\\"vectors\\\", compare_sawc=True),\\n+    RunCase(\\\"vectors/moves_results\\\", \\\"one\\\\n2\\\\nx\\\\ny\\\\n\\\", section=\\\"vectors\\\", compare_sawc=True),\\n+    RunCase(\\\"vectors/evaluation_once\\\", \\\"index\\\\n7\\\\nvalue\\\\n2\\\\n\\\", section=\\\"vectors\\\", compare_sawc=True),\\n+    RunCase(\\\"vectors/forward_element_record\\\", \\\"forward\\\\n\\\", section=\\\"vectors\\\", compare_sawc=True),\\n+    RunCase(\\\"vectors/forward_nocopy_vector\\\", \\\"42\\\\n\\\", section=\\\"vectors\\\"),\\n+    RunCase(\\\"vectors/index_negative\\\", \\\"runtime error: vector index out of bounds\\\\n\\\", 1, \\\"vectors\\\"),\\n+    RunCase(\\\"vectors/index_at_end\\\", \\\"runtime error: vector index out of bounds\\\\n\\\", 1, \\\"vectors\\\"),\\n     RunCase(\\\"builders/snapshots\\\", \\\"\\\\na\\\\nab\\\\na\\\\nab\\\\nother\\\\n\\\", section=\\\"builders\\\", compare_sawc=True),\\n     RunCase(\\\"builders/append_values\\\", \\\"120\\\\n0\\\\n255\\\\n45\\\\n57\\\\n50\\\\n50\\\\n51\\\\n51\\\\n55\\\\n50\\\\n48\\\\n51\\\\n54\\\\n56\\\\n53\\\\n52\\\\n55\\\\n55\\\\n53\\\\n56\\\\n48\\\\n56\\\\n48\\\\n127\\\\n194\\\\n128\\\\n223\\\\n191\\\\n224\\\\n160\\\\n128\\\\n237\\\\n159\\\\n191\\\\n238\\\\n128\\\\n128\\\\n239\\\\n191\\\\n191\\\\n244\\\\n143\\\\n191\\\\n191\\\\n\\\", section=\\\"builders\\\", compare_sawc=True),\\n     RunCase(\\\"builders/references\\\", \\\"start-mut\\\\nstart-mut-shared\\\\n\\\", section=\\\"builders\\\", compare_sawc=True),\\n@@ -239,6 +248,27 @@ RUN_CASES = (\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"vectors/reject_implicit_copy\\\", \\\"cannot implicitly copy noncopyable value\\\", \\\"vectors\\\", True),\\n+    RejectCase(\\\"vectors/reject_double_move\\\", \\\"use of moved value\\\", \\\"vectors\\\", True),\\n+    RejectCase(\\\"vectors/reject_borrow_after_move\\\", \\\"use of moved value\\\", \\\"vectors\\\", True),\\n+    RejectCase(\\\"vectors/reject_move_field\\\", \\\"move requires a whole local\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_move_reference\\\", \\\"cannot move through a reference\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_vector_element\\\", \\\"Vector elements must be non-Void Copy values\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_builder_element\\\", \\\"Vector elements must be non-Void Copy values\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_noncopy_record_element\\\", \\\"Vector elements must be non-Void Copy values\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_value_parameter\\\", \\\"vector-containing parameters must be references\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_assignment\\\", \\\"vector-containing values cannot be assigned\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_outer_loop_move\\\", \\\"cannot move a local declared outside the current loop\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_nocopy_copyable\\\", \\\"NoCopy marker requires a vector-containing record\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_forward_nocopy_copyable\\\", \\\"NoCopy marker requires a vector-containing record\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_nocopy_body\\\", \\\"NoCopy marker requires an empty body\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_constructor_arity\\\", \\\"Vector constructor accepts no arguments\\\", \\\"vectors\\\", True),\\n+    RejectCase(\\\"vectors/reject_push_type\\\", \\\"cannot implicitly convert\\\", \\\"vectors\\\", True),\\n+    RejectCase(\\\"vectors/reject_push_label\\\", \\\"argument label\\\", \\\"vectors\\\", True),\\n+    RejectCase(\\\"vectors/reject_get_label\\\", \\\"argument label\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_index_type\\\", \\\"cannot implicitly convert\\\", \\\"vectors\\\", True),\\n+    RejectCase(\\\"vectors/reject_value_control\\\", \\\"vector-containing value control flow is not supported\\\", \\\"vectors\\\"),\\n+    RejectCase(\\\"vectors/reject_optional_constructor\\\", \\\"Vector constructor requires a non-optional Vector type\\\", \\\"vectors\\\"),\\n     RejectCase(\\\"builders/reject_copy\\\", \\\"StringBuilder values cannot be copied\\\", \\\"builders\\\", True),\\n     RejectCase(\\\"builders/reject_assignment\\\", \\\"StringBuilder values cannot be assigned\\\", \\\"builders\\\"),\\n     RejectCase(\\\"builders/reject_value_parameter\\\", \\\"StringBuilder parameters must be references\\\", \\\"builders\\\"),\\n@@ -567,7 +597,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--sawc-python\\\", default=sys.executable,\\n                         help=\\\"Python interpreter with sawc dependencies (default: this interpreter)\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\", \\\"results\\\", \\\"unsigned_parse\\\", \\\"scalars\\\", \\\"builders\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\", \\\"results\\\", \\\"unsigned_parse\\\", \\\"scalars\\\", \\\"builders\\\", \\\"vectors\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\ndiff --git a/prototypes/minivm/tests/builder_contract.saw b/prototypes/minivm/tests/builder_contract.saw\\nindex fbdac86b..2bf9df20 100644\\n--- a/prototypes/minivm/tests/builder_contract.saw\\n+++ b/prototypes/minivm/tests/builder_contract.saw\\n@@ -55,7 +55,7 @@ func program() -> Program {\\n         slots: move slots, code: move code))\\n     Program(strings: [\\\"ab\\\"], records: move records, references: move refs, sums: move sums,\\n         enums: Vector<EnumIR>(), functions: move functions, main_index: 0,\\n-        alloc_error_record: 0, builder_fail_after: -1)\\n+        alloc_error_record: 0, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1)\\n }\\n \\n func main() {\\ndiff --git a/prototypes/minivm/tests/builder_vm_contract.saw b/prototypes/minivm/tests/builder_vm_contract.saw\\nindex 8746bc1c..44b9b9f6 100644\\n--- a/prototypes/minivm/tests/builder_vm_contract.saw\\n+++ b/prototypes/minivm/tests/builder_vm_contract.saw\\n@@ -180,7 +180,7 @@ func contract_program() -> Program {\\n         result: ValueType.Void, slots: move append_slots, code: move append_code))\\n     let program = Program(strings: move strings, references: move refs, enums: Vector<EnumIR>(),\\n         records: move records, sums: move sums, functions: move functions,\\n-        main_index: 0, alloc_error_record: 0, builder_fail_after: 1)\\n+        main_index: 0, alloc_error_record: 0, builder_fail_after: 1, vectors: Vector<VectorIR>(), vector_fail_after: -1)\\n     try! verify(&program)\\n     move program\\n }\\ndiff --git a/prototypes/minivm/tests/enum_contract.saw b/prototypes/minivm/tests/enum_contract.saw\\nindex df1be290..23fe5888 100644\\n--- a/prototypes/minivm/tests/enum_contract.saw\\n+++ b/prototypes/minivm/tests/enum_contract.saw\\n@@ -67,7 +67,7 @@ func base_program() -> Program {\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move identity_params,\\n         result: ValueType.Enum(0), slots: move identity_slots,\\n         code: move identity_code))\\n-    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+    Program(alloc_error_record: -1, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1,\\n         strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: move enums, records: move records, sums: Vector<SumIR>(),\\n             functions: move functions, main_index: 0)\\n }\\ndiff --git a/prototypes/minivm/tests/optional_contract.saw b/prototypes/minivm/tests/optional_contract.saw\\nindex 0506c200..fa42032c 100644\\n--- a/prototypes/minivm/tests/optional_contract.saw\\n+++ b/prototypes/minivm/tests/optional_contract.saw\\n@@ -56,7 +56,7 @@ func base_program() -> Program {\\n     try! functions.push(FuncIR(name: \\\"echo\\\", params: move echo_params, result: ValueType.Optional(0),\\n         slots: move echo_slots, code: move echo_code))\\n     let strings: Vector<String> = [\\\"some\\\"]\\n-    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+    Program(alloc_error_record: -1, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1,\\n         strings: move strings, references: move references, enums: Vector<EnumIR>(),\\n         records: move records, sums: move sums, functions: move functions, main_index: 0)\\n }\\ndiff --git a/prototypes/minivm/tests/owning_record_contract.saw b/prototypes/minivm/tests/owning_record_contract.saw\\nindex 79b1968c..4ef40c25 100644\\n--- a/prototypes/minivm/tests/owning_record_contract.saw\\n+++ b/prototypes/minivm/tests/owning_record_contract.saw\\n@@ -68,7 +68,7 @@ func base_program() -> Program {\\n         slots: move slots, code: move code))\\n     try! funcs.push(FuncIR(name: \\\"echo\\\", params: move params, result: ValueType.Record(0),\\n         slots: move echo_slots, code: move echo_code))\\n-    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+    Program(alloc_error_record: -1, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1,\\n         strings: move strings, references: move refs, enums: Vector<EnumIR>(),\\n             sums: Vector<SumIR>(),\\n         records: move records, functions: move funcs, main_index: 0)\\ndiff --git a/prototypes/minivm/tests/receiver_contract.saw b/prototypes/minivm/tests/receiver_contract.saw\\nindex cdc35743..bc376fbd 100644\\n--- a/prototypes/minivm/tests/receiver_contract.saw\\n+++ b/prototypes/minivm/tests/receiver_contract.saw\\n@@ -57,7 +57,7 @@ func base_program() -> Program {\\n     var funcs = Vector<FuncIR>()\\n     try! funcs.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n         slots: move slots, code: move code))\\n-    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+    Program(alloc_error_record: -1, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1,\\n         strings: Vector<String>(), references: move refs, enums: Vector<EnumIR>(), records: move records, sums: Vector<SumIR>(),\\n         functions: move funcs, main_index: 0)\\n }\\ndiff --git a/prototypes/minivm/tests/record_contract.saw b/prototypes/minivm/tests/record_contract.saw\\nindex c8112fc1..8410d3f0 100644\\n--- a/prototypes/minivm/tests/record_contract.saw\\n+++ b/prototypes/minivm/tests/record_contract.saw\\n@@ -47,7 +47,7 @@ func call_program(records: &Vector<RecordIR>) -> Program {\\n         slots: slots.copy(), code: move main_code))\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move params, result: ValueType.Record(0),\\n         slots: move slots, code: move callee_code))\\n-    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+    Program(alloc_error_record: -1, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1,\\n         strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(), records: records.copy(), sums: Vector<SumIR>(), functions: move functions, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/reference_contract.saw b/prototypes/minivm/tests/reference_contract.saw\\nindex c25f2d35..5df4c40e 100644\\n--- a/prototypes/minivm/tests/reference_contract.saw\\n+++ b/prototypes/minivm/tests/reference_contract.saw\\n@@ -55,7 +55,7 @@ func base_program() -> Program {\\n         slots: move slots, code: move code))\\n     try! funcs.push(FuncIR(name: \\\"read\\\", params: move read_params, result: ValueType.I64,\\n         slots: move read_slots, code: move read_code))\\n-    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+    Program(alloc_error_record: -1, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1,\\n         strings: Vector<String>(), references: move refs, enums: move enums, records: Vector<RecordIR>(), sums: Vector<SumIR>(),\\n         functions: move funcs, main_index: 0)\\n }\\ndiff --git a/prototypes/minivm/tests/result_contract.saw b/prototypes/minivm/tests/result_contract.saw\\nindex 396d705f..a85b0233 100644\\n--- a/prototypes/minivm/tests/result_contract.saw\\n+++ b/prototypes/minivm/tests/result_contract.saw\\n@@ -92,7 +92,7 @@ func result_program() -> Program {\\n     try! functions.push(FuncIR(name: \\\"echo\\\", params: move echo_params, result: ValueType.Result(2),\\n         slots: move echo_slots, code: move echo_code))\\n     let strings: Vector<String> = [\\\"A\\\", \\\"B\\\", \\\"C\\\", \\\"payload\\\"]\\n-    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+    Program(alloc_error_record: -1, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1,\\n         strings: move strings, references: move references, enums: Vector<EnumIR>(),\\n         records: move records, sums: move sums, functions: move functions, main_index: 0)\\n }\\n@@ -104,7 +104,7 @@ func panic_program() -> Program {\\n     var functions = Vector<FuncIR>()\\n     try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n         slots: Vector<ValueType>(), code: move code))\\n-    Program(alloc_error_record: p.alloc_error_record, builder_fail_after: p.builder_fail_after,\\n+    Program(alloc_error_record: p.alloc_error_record, builder_fail_after: p.builder_fail_after, vectors: p.vectors.copy(), vector_fail_after: p.vector_fail_after,\\n         strings: p.strings.copy(), references: p.references.copy(), enums: p.enums.copy(),\\n         records: p.records.copy(), sums: p.sums.copy(), functions: move functions, main_index: 0)\\n }\\ndiff --git a/prototypes/minivm/tests/string_contract.saw b/prototypes/minivm/tests/string_contract.saw\\nindex 64903103..34f55ab1 100644\\n--- a/prototypes/minivm/tests/string_contract.saw\\n+++ b/prototypes/minivm/tests/string_contract.saw\\n@@ -52,7 +52,7 @@ func base_program() -> Program {\\n         slots: move slots, code: move code))\\n     try! funcs.push(FuncIR(name: \\\"echo\\\", params: move params, result: ValueType.String,\\n         slots: move echo_slots, code: move echo_code))\\n-    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+    Program(alloc_error_record: -1, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1,\\n         strings: move strings, references: move refs, enums: Vector<EnumIR>(), sums: Vector<SumIR>(),\\n         records: Vector<RecordIR>(), functions: move funcs, main_index: 0)\\n }\\ndiff --git a/prototypes/minivm/tests/uint_parse_contract.saw b/prototypes/minivm/tests/uint_parse_contract.saw\\nindex 121e9661..be3b1bf7 100644\\n--- a/prototypes/minivm/tests/uint_parse_contract.saw\\n+++ b/prototypes/minivm/tests/uint_parse_contract.saw\\n@@ -77,7 +77,7 @@ func direct_program() -> Program {\\n     var functions = Vector<FuncIR>()\\n     try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n         slots: move slots, code: move code))\\n-    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+    Program(alloc_error_record: -1, builder_fail_after: -1, vectors: Vector<VectorIR>(), vector_fail_after: -1,\\n         strings: move strings, references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(),\\n         records: Vector<RecordIR>(), sums: Vector<SumIR>(), functions: move functions, main_index: 0)\\n }\\ndiff --git a/prototypes/minivm/tests/vector_contract.saw b/prototypes/minivm/tests/vector_contract.saw\\nnew file mode 100644\\nindex 00000000..1064dea2\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/vector_contract.saw\\n@@ -0,0 +1,153 @@\\n+import src.model.*\\n+import src.verify.*\\n+\\n+func op(kind: Op, dst: Int, a: Int, b: Int, immediate: Int) -> Instruction {\\n+    var result = instruction(kind)\\n+    result.dst = dst\\n+    result.a = a\\n+    result.b = b\\n+    result.immediate = immediate\\n+    move result\\n+}\\n+\\n+func valid(p: &Program) -> Bool {\\n+    try verify(&p) catch { return false }\\n+    true\\n+}\\n+\\n+func program() -> Program {\\n+    var records = Vector<RecordIR>()\\n+    try! records.push(RecordIR(name: \\\"AllocError\\\", field_names: [\\\"size\\\", \\\"align\\\"],\\n+        field_types: [ValueType.I64, ValueType.I64], line: 1, col: 1))\\n+    try! records.push(RecordIR(name: \\\"Item\\\", field_names: [\\\"name\\\", \\\"suffix\\\"],\\n+        field_types: [ValueType.String, ValueType.Optional(0)], line: 1, col: 1))\\n+    try! records.push(RecordIR(name: \\\"Other\\\", field_names: [\\\"name\\\", \\\"suffix\\\"],\\n+        field_types: [ValueType.String, ValueType.Optional(0)], line: 1, col: 1))\\n+    var sums = Vector<SumIR>()\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.String, error: ValueType.Void))\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.Record(1), error: ValueType.Void))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Void, error: ValueType.Record(0)))\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.Record(2), error: ValueType.Void))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Void, error: ValueType.Record(2)))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Vector(0), error: ValueType.Record(0)))\\n+    var vectors = Vector<VectorIR>()\\n+    try! vectors.push(VectorIR(element: ValueType.Record(1)))\\n+    try! vectors.push(VectorIR(element: ValueType.I64))\\n+    var refs = Vector<ReferenceIR>()\\n+    try! refs.push(ReferenceIR(target: ValueType.Vector(0), mutable: true))\\n+    try! refs.push(ReferenceIR(target: ValueType.Vector(0), mutable: false))\\n+    try! refs.push(ReferenceIR(target: ValueType.Vector(1), mutable: true))\\n+    let slots: Vector<ValueType> = [ValueType.Vector(0), ValueType.Vector(0),\\n+        ValueType.Ref(0), ValueType.Ref(1), ValueType.I64,\\n+        ValueType.String, ValueType.Bool, ValueType.String,\\n+        ValueType.Bool, ValueType.I64, ValueType.I64,\\n+        ValueType.Bool, ValueType.String, ValueType.Bool, ValueType.String,\\n+        ValueType.I64, ValueType.Vector(1), ValueType.Ref(2)]\\n+    var code = Vector<Instruction>()\\n+    try! code.push(op(Op.VectorNew, 0, -1, -1, 0))\\n+    try! code.push(op(Op.Addr, 2, 0, -1, 0))\\n+    try! code.push(op(Op.Addr, 3, 0, -1, 0))\\n+    try! code.push(op(Op.VectorPush, 8, 2, 5, 2))\\n+    try! code.push(op(Op.VectorLen, 15, 3, -1, 0))\\n+    try! code.push(op(Op.VectorIndex, 5, 3, 4, 0))\\n+    try! code.push(op(Op.VectorGet, 11, 3, 4, 1))\\n+    try! code.push(op(Op.MoveVector, 1, 0, -1, 0))\\n+    try! code.push(op(Op.Drop, -1, 0, -1, 0))\\n+    try! code.push(op(Op.Drop, -1, 1, -1, 0))\\n+    try! code.push(instruction(Op.Return))\\n+    var functions = Vector<FuncIR>()\\n+    try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: move slots, code: move code))\\n+    Program(strings: Vector<String>(), records: move records, references: move refs,\\n+        sums: move sums, enums: Vector<EnumIR>(), functions: move functions,\\n+        main_index: 0, alloc_error_record: 0, builder_fail_after: -1,\\n+        vectors: move vectors, vector_fail_after: -1)\\n+}\\n+\\n+func main() {\\n+    let p = program()\\n+    assert(valid(&p), \\\"all vector instructions and owning Copy element\\\")\\n+    var changed = p.copy()\\n+    changed.functions[0].code[0].dst = 5\\n+    assert(not valid(&changed), \\\"New destination must be Vector\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[0].a = 0\\n+    assert(not valid(&changed), \\\"New unused operands checked\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[3].a = 3\\n+    assert(not valid(&changed), \\\"push requires mutable reference\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[3].b = 4\\n+    assert(not valid(&changed), \\\"push element layout checked\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[3].b = Int.max\\n+    assert(not valid(&changed), \\\"push input range cannot overflow\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[3].dst = Int.max\\n+    assert(not valid(&changed), \\\"push output range cannot overflow\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[3].immediate = 4\\n+    assert(not valid(&changed), \\\"push needs nominal AllocError\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[3].immediate = 5\\n+    assert(not valid(&changed), \\\"push success must be Void\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[4].dst = 5\\n+    assert(not valid(&changed), \\\"len returns Int\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[4].a = 0\\n+    assert(not valid(&changed), \\\"len uses reference\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[5].b = 8\\n+    assert(not valid(&changed), \\\"index must be Int\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[5].dst = 15\\n+    assert(not valid(&changed), \\\"index requires full element destination\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].immediate = 3\\n+    assert(not valid(&changed), \\\"get Optional payload identity checked despite equal layouts\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].immediate = Int.max\\n+    assert(not valid(&changed), \\\"get descriptor bounded\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].dst = 12\\n+    assert(not valid(&changed), \\\"get requires tag and full payload\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[7].dst = 0\\n+    assert(not valid(&changed), \\\"self move forbidden\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[7].dst = 16\\n+    assert(not valid(&changed), \\\"move requires same Vector descriptor\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[7].op = Op.Copy\\n+    assert(not valid(&changed), \\\"Vector copy forbidden\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[7] = op(Op.LoadRef, 0, 2, -1, 0)\\n+    assert(not valid(&changed), \\\"Vector load by value forbidden\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[7] = op(Op.StoreRef, -1, 2, 0, 0)\\n+    assert(not valid(&changed), \\\"Vector store by value forbidden\\\")\\n+    changed = p.copy()\\n+    changed.vectors[1].element = ValueType.Vector(0)\\n+    assert(not valid(&changed), \\\"nested Vector elements excluded\\\")\\n+    changed = p.copy()\\n+    changed.vectors[1].element = ValueType.Builder\\n+    assert(not valid(&changed), \\\"Builder element excluded\\\")\\n+    changed = p.copy()\\n+    changed.vectors[1].element = ValueType.Void\\n+    assert(not valid(&changed), \\\"Void element excluded\\\")\\n+    changed = p.copy()\\n+    changed.records[2].field_types[0] = ValueType.Vector(0)\\n+    changed.vectors[1].element = ValueType.Record(2)\\n+    assert(not valid(&changed), \\\"Vector nested in element record excluded\\\")\\n+    changed = p.copy()\\n+    changed.vector_fail_after = -2\\n+    assert(not valid(&changed), \\\"failure counter checked\\\")\\n+    changed = p.copy()\\n+    changed.references[2].target = ValueType.Vector(Int.max)\\n+    assert(not valid(&changed), \\\"invalid descriptor in reference checked\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[3].target = 0\\n+    assert(not valid(&changed), \\\"unused instruction metadata checked\\\")\\n+    print(\\\"vector verifier contract passed\\\")\\n+}\\ndiff --git a/prototypes/minivm/tests/vector_source_contract.saw b/prototypes/minivm/tests/vector_source_contract.saw\\nnew file mode 100644\\nindex 00000000..1724bb3d\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/vector_source_contract.saw\\n@@ -0,0 +1,25 @@\\n+import src.model.*\\n+import src.frontend.*\\n+import src.verify.*\\n+import src.vm.*\\n+import src.llvm\\n+import std.env.{Env}\\n+\\n+// Exercises source lowering across failed/successful calls, owning Result and\\n+// record payloads, explicit moves and inactive branch cleanup. Expected lines:\\n+// 24, 8, bundle, first, suffix, first, 1.\\n+func source_text() -> String {\\n+    \\\"struct Row \\\\{ text: String, suffix: String? \\\\}\\\\nstruct Bundle \\\\{ rows: Vector<Row>, label: String \\\\}\\\\nextension Bundle: NoCopy \\\\{\\\\}\\\\nfunc produce() -> Result<Bundle, AllocError> \\\\{\\\\n    if false \\\\{\\\\n        var unused = Vector<Row>()\\\\n        try unused.push(Row(text: \\\\\\\"never\\\\\\\", suffix: None))\\\\n    \\\\}\\\\n    var rows = Vector<Row>()\\\\n    try rows.push(Row(text: \\\\\\\"first\\\\\\\", suffix: \\\\\\\"suffix\\\\\\\"))\\\\n    Bundle(rows: move rows, label: \\\\\\\"bundle\\\\\\\")\\\\n\\\\}\\\\nfunc main() \\\\{\\\\n    match produce() \\\\{\\\\n        case Ok(value) -> \\\\{ print(\\\\\\\"unexpected success\\\\\\\") \\\\},\\\\n        case Err(error) -> \\\\{ print(error.size)\\\\nprint(error.align) \\\\}\\\\n    \\\\}\\\\n    let bundle = try! produce()\\\\n    print(bundle.label)\\\\n    let item = bundle.rows[0]\\\\n    print(item.text)\\\\n    if let suffix = item.suffix \\\\{ print(suffix) \\\\}\\\\n    var moved = move bundle\\\\n    print(item.text)\\\\n    print(moved.rows.len())\\\\n\\\\}\\\\n\\\"\\n+}\\n+\\n+func main() {\\n+    var program = try! compile_source(source_text())\\n+    program.vector_fail_after = 0\\n+    try! verify(&program)\\n+    let args = try! Env.args()\\n+    if args.len() == 2 && args[1] == \\\"emit-llvm\\\" {\\n+        print(try! llvm.emit_llvm(&program))\\n+        return\\n+    }\\n+    try! execute(&program, 10000)\\n+}\\ndiff --git a/prototypes/minivm/tests/vector_vm_contract.saw b/prototypes/minivm/tests/vector_vm_contract.saw\\nnew file mode 100644\\nindex 00000000..f5a5e37d\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/vector_vm_contract.saw\\n@@ -0,0 +1,270 @@\\n+import src.model.*\\n+import src.verify.*\\n+import src.vm.*\\n+import src.llvm\\n+import std.env.{Env}\\n+\\n+func slot(slots: &var Vector<ValueType>, t: ValueType) -> Int {\\n+    let index = slots.len()\\n+    try! slots.push(t)\\n+    index\\n+}\\n+\\n+func emit(code: &var Vector<Instruction>, kind: Op, dst: Int, a: Int, b: Int, immediate: Int) {\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    i.immediate = immediate\\n+    try! code.push(move i)\\n+}\\n+\\n+func check_word(code: &var Vector<Instruction>, slots: &var Vector<ValueType>, actual: Int,\\n+                expected: Int, t: ValueType) {\\n+    let expected_slot = slot(&var slots, t)\\n+    emit(&var code, Op.Const, expected_slot, -1, -1, expected)\\n+    let equal = slot(&var slots, ValueType.Bool)\\n+    emit(&var code, Op.Eq, equal, actual, expected_slot, 0)\\n+    var branch = instruction(Op.Branch)\\n+    branch.a = equal\\n+    branch.target = code.len() + 2\\n+    try! code.push(move branch)\\n+    try! code.push(instruction(Op.Panic))\\n+}\\n+\\n+func check_string(code: &var Vector<Instruction>, slots: &var Vector<ValueType>,\\n+                  strings: &var Vector<String>, actual: Int, expected: String) {\\n+    let pool = strings.len()\\n+    try! strings.push(expected)\\n+    let expected_slot = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.StringConst, expected_slot, -1, -1, pool)\\n+    let equal = slot(&var slots, ValueType.Bool)\\n+    emit(&var code, Op.Eq, equal, actual, expected_slot, 0)\\n+    var branch = instruction(Op.Branch)\\n+    branch.a = equal\\n+    branch.target = code.len() + 2\\n+    try! code.push(move branch)\\n+    try! code.push(instruction(Op.Panic))\\n+}\\n+\\n+func contract_program() -> Program {\\n+    var strings = Vector<String>()\\n+    try! strings.push(\\\"old\\\")\\n+    try! strings.push(\\\"new\\\")\\n+    try! strings.push(\\\"r0\\\")\\n+    try! strings.push(\\\"r1\\\")\\n+    try! strings.push(\\\"some\\\")\\n+    var vectors = Vector<VectorIR>()\\n+    try! vectors.push(VectorIR(element: ValueType.I64))\\n+    try! vectors.push(VectorIR(element: ValueType.String))\\n+    try! vectors.push(VectorIR(element: ValueType.Record(1)))\\n+    var references = Vector<ReferenceIR>()\\n+    try! references.push(ReferenceIR(target: ValueType.Vector(0), mutable: true))\\n+    try! references.push(ReferenceIR(target: ValueType.Vector(0), mutable: false))\\n+    try! references.push(ReferenceIR(target: ValueType.Vector(1), mutable: true))\\n+    try! references.push(ReferenceIR(target: ValueType.Vector(1), mutable: false))\\n+    try! references.push(ReferenceIR(target: ValueType.Vector(2), mutable: true))\\n+    try! references.push(ReferenceIR(target: ValueType.Vector(2), mutable: false))\\n+    let field_names: Vector<String> = [\\\"size\\\", \\\"align\\\"]\\n+    let field_types: Vector<ValueType> = [ValueType.I64, ValueType.I64]\\n+    var records = Vector<RecordIR>()\\n+    try! records.push(RecordIR(name: \\\"AllocError\\\", field_names: move field_names,\\n+        field_types: move field_types, line: 0, col: 0))\\n+    let item_names: Vector<String> = [\\\"name\\\", \\\"suffix\\\"]\\n+    let item_types: Vector<ValueType> = [ValueType.String, ValueType.Optional(1)]\\n+    try! records.push(RecordIR(name: \\\"Item\\\", field_names: move item_names,\\n+        field_types: move item_types, line: 0, col: 0))\\n+    var sums = Vector<SumIR>()\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.I64, error: ValueType.Void))\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.String, error: ValueType.Void))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Void,\\n+        error: ValueType.Record(0)))\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.Record(1), error: ValueType.Void))\\n+\\n+    var slots = Vector<ValueType>()\\n+    var code = Vector<Instruction>()\\n+    let ints = slot(&var slots, ValueType.Vector(0))\\n+    let ints_mut = slot(&var slots, ValueType.Ref(0))\\n+    let ints_shared = slot(&var slots, ValueType.Ref(1))\\n+    emit(&var code, Op.VectorNew, ints, -1, -1, 0)\\n+    emit(&var code, Op.Addr, ints_mut, ints, -1, 0)\\n+    emit(&var code, Op.Addr, ints_shared, ints, -1, 0)\\n+    let length = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.VectorLen, length, ints_shared, -1, 0)\\n+    check_word(&var code, &var slots, length, 0, ValueType.I64)\\n+\\n+    let seven = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.Const, seven, -1, -1, 7)\\n+    let first_push = slot(&var slots, ValueType.Bool)\\n+    let _ = slot(&var slots, ValueType.I64)\\n+    let _ = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.VectorPush, first_push, ints_mut, seven, 2)\\n+    check_word(&var code, &var slots, first_push, 1, ValueType.Bool)\\n+    let zero = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.Const, zero, -1, -1, 0)\\n+    let indexed = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.VectorIndex, indexed, ints_shared, zero, 0)\\n+    check_word(&var code, &var slots, indexed, 7, ValueType.I64)\\n+\\n+    let miss_index = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.Const, miss_index, -1, -1, -1)\\n+    let miss_tag = slot(&var slots, ValueType.Bool)\\n+    let miss_payload = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.Const, miss_payload, -1, -1, 99)\\n+    emit(&var code, Op.VectorGet, miss_tag, ints_shared, miss_index, 0)\\n+    check_word(&var code, &var slots, miss_tag, 0, ValueType.Bool)\\n+    check_word(&var code, &var slots, miss_payload, 0, ValueType.I64)\\n+\\n+    let eight = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.Const, eight, -1, -1, 8)\\n+    let failed_push = slot(&var slots, ValueType.Bool)\\n+    let failed_size = slot(&var slots, ValueType.I64)\\n+    let failed_align = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.VectorPush, failed_push, ints_mut, eight, 2)\\n+    check_word(&var code, &var slots, failed_push, 0, ValueType.Bool)\\n+    check_word(&var code, &var slots, failed_size, 16, ValueType.I64)\\n+    check_word(&var code, &var slots, failed_align, 8, ValueType.I64)\\n+    emit(&var code, Op.VectorLen, length, ints_shared, -1, 0)\\n+    check_word(&var code, &var slots, length, 1, ValueType.I64)\\n+\\n+    let nine = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.Const, nine, -1, -1, 9)\\n+    let recovered = slot(&var slots, ValueType.Bool)\\n+    let _ = slot(&var slots, ValueType.I64)\\n+    let _ = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.VectorPush, recovered, ints_mut, nine, 2)\\n+    check_word(&var code, &var slots, recovered, 1, ValueType.Bool)\\n+    check_word(&var code, &var slots, indexed, 7, ValueType.I64)\\n+\\n+    let moved = slot(&var slots, ValueType.Vector(0))\\n+    emit(&var code, Op.MoveVector, moved, ints, -1, 0)\\n+    let inactive = slot(&var slots, ValueType.Vector(0))\\n+    emit(&var code, Op.MoveVector, inactive, ints, -1, 0)\\n+    emit(&var code, Op.Drop, -1, ints, -1, 0)\\n+    emit(&var code, Op.Drop, -1, inactive, -1, 0)\\n+    emit(&var code, Op.Drop, -1, moved, -1, 0)\\n+\\n+    let texts = slot(&var slots, ValueType.Vector(1))\\n+    let texts_mut = slot(&var slots, ValueType.Ref(2))\\n+    let texts_shared = slot(&var slots, ValueType.Ref(3))\\n+    emit(&var code, Op.VectorNew, texts, -1, -1, 0)\\n+    emit(&var code, Op.Addr, texts_mut, texts, -1, 0)\\n+    emit(&var code, Op.Addr, texts_shared, texts, -1, 0)\\n+    let old_text = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.StringConst, old_text, -1, -1, 0)\\n+    let text_push = slot(&var slots, ValueType.Bool)\\n+    let _ = slot(&var slots, ValueType.I64)\\n+    let _ = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.VectorPush, text_push, texts_mut, old_text, 2)\\n+    let text_snapshot = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.VectorIndex, text_snapshot, texts_shared, zero, 0)\\n+    let new_text = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.StringConst, new_text, -1, -1, 1)\\n+    emit(&var code, Op.VectorPush, text_push, texts_mut, new_text, 2)\\n+    check_string(&var code, &var slots, &var strings, text_snapshot, \\\"old\\\")\\n+    let stale_payload = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.StringConst, stale_payload, -1, -1, 1)\\n+    let text_miss = slot(&var slots, ValueType.Bool)\\n+    let cleared_payload = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.Copy, cleared_payload, stale_payload, -1, 0)\\n+    emit(&var code, Op.VectorGet, text_miss, texts_shared, miss_index, 1)\\n+    check_word(&var code, &var slots, text_miss, 0, ValueType.Bool)\\n+    emit(&var code, Op.Drop, -1, texts, -1, 0)\\n+    check_string(&var code, &var slots, &var strings, text_snapshot, \\\"old\\\")\\n+\\n+    let items = slot(&var slots, ValueType.Vector(2))\\n+    let items_mut = slot(&var slots, ValueType.Ref(4))\\n+    let items_shared = slot(&var slots, ValueType.Ref(5))\\n+    emit(&var code, Op.VectorNew, items, -1, -1, 0)\\n+    emit(&var code, Op.Addr, items_mut, items, -1, 0)\\n+    emit(&var code, Op.Addr, items_shared, items, -1, 0)\\n+    let r0 = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.StringConst, r0, -1, -1, 2)\\n+    let none_tag = slot(&var slots, ValueType.Bool)\\n+    let none_string = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.Const, none_tag, -1, -1, 0)\\n+    let item_push = slot(&var slots, ValueType.Bool)\\n+    let _ = slot(&var slots, ValueType.I64)\\n+    let _ = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.VectorPush, item_push, items_mut, r0, 2)\\n+    check_word(&var code, &var slots, item_push, 1, ValueType.Bool)\\n+    let indexed_name = slot(&var slots, ValueType.String)\\n+    let indexed_suffix_tag = slot(&var slots, ValueType.Bool)\\n+    let indexed_suffix = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.VectorIndex, indexed_name, items_shared, zero, 0)\\n+    check_string(&var code, &var slots, &var strings, indexed_name, \\\"r0\\\")\\n+    check_word(&var code, &var slots, indexed_suffix_tag, 0, ValueType.Bool)\\n+\\n+    let r1 = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.StringConst, r1, -1, -1, 3)\\n+    let some_tag = slot(&var slots, ValueType.Bool)\\n+    emit(&var code, Op.Const, some_tag, -1, -1, 1)\\n+    let some_string = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.StringConst, some_string, -1, -1, 4)\\n+    emit(&var code, Op.VectorPush, item_push, items_mut, r1, 2)\\n+    let one = slot(&var slots, ValueType.I64)\\n+    emit(&var code, Op.Const, one, -1, -1, 1)\\n+    let got_tag = slot(&var slots, ValueType.Bool)\\n+    let got_name = slot(&var slots, ValueType.String)\\n+    let got_suffix_tag = slot(&var slots, ValueType.Bool)\\n+    let got_suffix = slot(&var slots, ValueType.String)\\n+    emit(&var code, Op.VectorGet, got_tag, items_shared, one, 3)\\n+    check_word(&var code, &var slots, got_tag, 1, ValueType.Bool)\\n+    check_string(&var code, &var slots, &var strings, got_name, \\\"r1\\\")\\n+    check_word(&var code, &var slots, got_suffix_tag, 1, ValueType.Bool)\\n+    check_string(&var code, &var slots, &var strings, got_suffix, \\\"some\\\")\\n+    emit(&var code, Op.Drop, -1, items, -1, 0)\\n+    check_string(&var code, &var slots, &var strings, indexed_name, \\\"r0\\\")\\n+    check_string(&var code, &var slots, &var strings, got_suffix, \\\"some\\\")\\n+\\n+    let returned = slot(&var slots, ValueType.Vector(0))\\n+    emit(&var code, Op.VectorNew, returned, -1, -1, 0)\\n+    var call = instruction(Op.Call)\\n+    call.dst = returned\\n+    call.target = 1\\n+    try! code.push(move call)\\n+    let returned_ref = slot(&var slots, ValueType.Ref(1))\\n+    emit(&var code, Op.Addr, returned_ref, returned, -1, 0)\\n+    emit(&var code, Op.VectorLen, length, returned_ref, -1, 0)\\n+    check_word(&var code, &var slots, length, 1, ValueType.I64)\\n+    emit(&var code, Op.VectorIndex, indexed, returned_ref, zero, 0)\\n+    check_word(&var code, &var slots, indexed, 42, ValueType.I64)\\n+    emit(&var code, Op.Drop, -1, returned, -1, 0)\\n+    try! code.push(instruction(Op.Return))\\n+\\n+    var made_slots = Vector<ValueType>()\\n+    let made = slot(&var made_slots, ValueType.Vector(0))\\n+    let made_mut = slot(&var made_slots, ValueType.Ref(0))\\n+    let forty_two = slot(&var made_slots, ValueType.I64)\\n+    let made_result = slot(&var made_slots, ValueType.Bool)\\n+    let _ = slot(&var made_slots, ValueType.I64)\\n+    let _ = slot(&var made_slots, ValueType.I64)\\n+    var made_code = Vector<Instruction>()\\n+    emit(&var made_code, Op.VectorNew, made, -1, -1, 0)\\n+    emit(&var made_code, Op.Addr, made_mut, made, -1, 0)\\n+    emit(&var made_code, Op.Const, forty_two, -1, -1, 42)\\n+    emit(&var made_code, Op.VectorPush, made_result, made_mut, forty_two, 2)\\n+    emit(&var made_code, Op.Return, -1, made, -1, 0)\\n+\\n+    var functions = Vector<FuncIR>()\\n+    try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: move slots, code: move code))\\n+    try! functions.push(FuncIR(name: \\\"make\\\", params: Vector<ValueType>(), result: ValueType.Vector(0),\\n+        slots: move made_slots, code: move made_code))\\n+    let program = Program(strings: move strings, references: move references, enums: Vector<EnumIR>(),\\n+        records: move records, sums: move sums, functions: move functions, main_index: 0,\\n+        alloc_error_record: 0, builder_fail_after: -1, vectors: move vectors, vector_fail_after: 1)\\n+    try! verify(&program)\\n+    move program\\n+}\\n+\\n+func main() {\\n+    let program = contract_program()\\n+    let args = try! Env.args()\\n+    if args.len() == 2 && args[1] == \\\"emit-llvm\\\" {\\n+        print(try! llvm.emit_llvm(&program))\\n+        return\\n+    }\\n+    try! execute(&program, 5000)\\n+    print(\\\"vector VM contract passed\\\")\\n+}\\n\",\"id\":\"SL-258\",\"request_id\":\"codex-sl258-m15-p1-20260914\",\"title\":\"M15: bounded Vector ownership in the Saw compiler prototype\"}","request_id":"codex-sl258-m15-p1-20260914","sequence":793,"timestamp":"1789405015"}
---
