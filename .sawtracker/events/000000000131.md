---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791252","description":"## Scope and status\n\nDF-151k — FILED, NOT FIXED (typechecker; found while fixing DF-151i, Aug 7). type_satisfies_copy_bound has no OPTIONAL and no TUPLE arm, so a fixed array of…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/139-enum-policy-tier.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/139-enum-policy-tier.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 8488–8516\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L8488-L8516)\n\n- **DF‑151k — FILED, NOT FIXED (typechecker; found while fixing DF‑151i,\n  Aug 7).** **`type_satisfies_copy_bound` has no OPTIONAL and no TUPLE arm, so a\n  fixed array of either is refused `.copy()` even when the element tier provides\n  one.**\n  ```saw\n  let a: [Arc<Res>?; 2] = [...]\n  let b = a.copy()\n  // error: type `[Arc<Res>?; 2]` is not Copy; its element type is not copyable\n  // ... and the same for `[(Arc<Res>, Int); 2]`\n  ```\n  Both messages are false: `Arc<Res>?` and `(Arc<Res>, Int)` each report an\n  'implicit' `copy_tier`, and `o.copy()` / `t.copy()` on those very types\n  compile. The array arm of `_check_copy_call` is the only `.copy()` path that\n  consults `type_satisfies_copy_bound` instead of `copy_tier`, and that\n  predicate answers structurally for ARRAY and FUNCTION and then falls to a\n  NAME lookup — an optional and a tuple have no name, so both return False.\n  Only NON-trivial element payloads are affected: `[Int?; 2]` and\n  `[(Int, Int); 2]` copy fine, caught by the `is_trivially_copyable` test at the\n  top, which is why this sat unnoticed.\n  Shared by two wrappers, so it is not tuple-specific and was left out of\n  DF‑151i deliberately — the surface there was the `.copy()` arm, and\n  `type_satisfies_copy_bound` also gates generic `T: Copy` bounds, giving a fix\n  a wider blast radius than that unit's scope. Expected shape: give it the two\n  structural arms its ARRAY arm already models (a wrapper satisfies the bound\n  iff its payload/elements do), then re-check what widening the `T: Copy` bound\n  admits — `Vector<(Arc, Int)>.iter()` becomes legal, which is correct per\n  design 139 but should land with a test.\n  Repro noted in `df151i_tuple_copy.saw`, where the array-of-tuples case is\n  commented out rather than written.\n","id":"SL-131","labels":["todo-import","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":131,"status":"open","title":"DF-151k — FILED, NOT FIXED (typechecker; found while fixing DF-151i, Aug 7). type_satisfies_copy_bound has no OPTIONAL and no TUPLE arm, so a fixed array of…","updated":"1788791252"},"before":null,"id":"SL-131","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-151k — FILED, NOT FIXED (typechecker; found while fixing DF-151i, Aug 7). type_satisfies_copy_bound has no OPTIONAL and no TUPLE arm, so a fixed array of…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/139-enum-policy-tier.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/139-enum-policy-tier.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 8488–8516\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L8488-L8516)\\n\\n- **DF‑151k — FILED, NOT FIXED (typechecker; found while fixing DF‑151i,\\n  Aug 7).** **`type_satisfies_copy_bound` has no OPTIONAL and no TUPLE arm, so a\\n  fixed array of either is refused `.copy()` even when the element tier provides\\n  one.**\\n  ```saw\\n  let a: [Arc<Res>?; 2] = [...]\\n  let b = a.copy()\\n  // error: type `[Arc<Res>?; 2]` is not Copy; its element type is not copyable\\n  // ... and the same for `[(Arc<Res>, Int); 2]`\\n  ```\\n  Both messages are false: `Arc<Res>?` and `(Arc<Res>, Int)` each report an\\n  'implicit' `copy_tier`, and `o.copy()` / `t.copy()` on those very types\\n  compile. The array arm of `_check_copy_call` is the only `.copy()` path that\\n  consults `type_satisfies_copy_bound` instead of `copy_tier`, and that\\n  predicate answers structurally for ARRAY and FUNCTION and then falls to a\\n  NAME lookup — an optional and a tuple have no name, so both return False.\\n  Only NON-trivial element payloads are affected: `[Int?; 2]` and\\n  `[(Int, Int); 2]` copy fine, caught by the `is_trivially_copyable` test at the\\n  top, which is why this sat unnoticed.\\n  Shared by two wrappers, so it is not tuple-specific and was left out of\\n  DF‑151i deliberately — the surface there was the `.copy()` arm, and\\n  `type_satisfies_copy_bound` also gates generic `T: Copy` bounds, giving a fix\\n  a wider blast radius than that unit's scope. Expected shape: give it the two\\n  structural arms its ARRAY arm already models (a wrapper satisfies the bound\\n  iff its payload/elements do), then re-check what widening the `T: Copy` bound\\n  admits — `Vector<(Arc, Int)>.iter()` becomes legal, which is correct per\\n  design 139 but should land with a test.\\n  Repro noted in `df151i_tuple_copy.saw`, where the array-of-tuples case is\\n  commented out rather than written.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-26fd5e5d0d0513fd56c1-create\",\"title\":\"DF-151k — FILED, NOT FIXED (typechecker; found while fixing DF-151i, Aug 7). type_satisfies_copy_bound has no OPTIONAL and no TUPLE arm, so a fixed array of…\"}","request_id":"todo-20260907-26fd5e5d0d0513fd56c1-create","sequence":131,"timestamp":"1788791252"}
---
