---
{"action":"create","actor":"agent:claude-sl280","after":{"assignee":"","author":"agent:claude-sl280","comments":[],"created":"1789657851","description":"A dependency that declares the name `main` makes the ENTRY module's `main` take design 249's `$M$root` codegen tag, so the program emits no `main` symbol at all and the LINK fails. Pre-existing on main (`be4a764e`); found by the SL-280 keying audit, not caused by it.\n\n## Repro\n\n`.build/scratch/mainprobe/modules/mpdep/lib.saw`\n\n    import std.task.{yield_now}\n\n    func main(n: Int) -> Int {\n        yield_now()\n        n\n    }\n\n    public func reach(n: Int) -> Int { main(n) }\n\n`.build/scratch/mainprobe/entry.saw`\n\n    import std.task.{yield_now}\n    import mpdep.{reach}\n\n    func worker(n: Int) -> Int { yield_now()  n + 1 }\n\n    func main() {\n        var group = TaskGroup()\n        let h = group.spawn(worker(1))\n        print(h.join())\n        print(reach(7))\n    }\n\n    ./.venv/bin/python sawc/sawc.py .build/scratch/mainprobe/entry.saw \\\n        --module-path mpdep=.build/scratch/mainprobe/modules/mpdep -o .build/scratch/mainprobe/entry\n\n    Linking failed: Undefined symbols for architecture arm64:\n      \"_main\", referenced from:\n          <initial-undefines>\n\nReproduced identically with the SL-280 branch and with the three SL-280 files reverted to HEAD, so it is not the funnel.\n\n## Mechanism\n\n`_free_function_symbol_base` (`sawc/typechecker/registration.py:1328-1354`) gives a free function a `$M$<module>` tag whenever `free_function_owners` reports that more than one module of the compilation declares the name — deliberately including the ROOT module (`module_tag(())` is `root`), because std is checked once and cached so the entry is the side that has to move. The census counts `main` like any other name. Nothing exempts it, and `main` is the C entry emitted by name.\n\nThe dependency's `main` need not be public or reachable; declaring the name is enough.\n\n## Relation to SL-274\n\nSL-274's own sweep already names `main` as exempt from BOTH the mangling and the internalization, and records why the distinction matters: \"The codegen sites key on `func.name`, so a symbol-only rename is safe there — a `mangled_symbol` stamp is not.\" This issue is the same hole reachable today, through design 249 rather than through SL-274's widening — so the exemption SL-274 will add is the fix for both, and this is the case that says it is not merely prospective.\n\nSeverity is low in practice (a dependency has little reason to declare `main`) but the failure mode is a bare linker error naming no user construct.\n","id":"SL-305","labels":["bug","compiler"],"priority":"normal","project":"SL","revision":1,"sequence":895,"stage":"backlog","status":"open","title":"A dependency declaring the name `main` renames the entry module's main symbol — the link fails with no user-facing diagnostic","updated":"1789657851"},"before":null,"id":"SL-305","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl280\",\"description\":\"A dependency that declares the name `main` makes the ENTRY module's `main` take design 249's `$M$root` codegen tag, so the program emits no `main` symbol at all and the LINK fails. Pre-existing on main (`be4a764e`); found by the SL-280 keying audit, not caused by it.\\n\\n## Repro\\n\\n`.build/scratch/mainprobe/modules/mpdep/lib.saw`\\n\\n    import std.task.{yield_now}\\n\\n    func main(n: Int) -> Int {\\n        yield_now()\\n        n\\n    }\\n\\n    public func reach(n: Int) -> Int { main(n) }\\n\\n`.build/scratch/mainprobe/entry.saw`\\n\\n    import std.task.{yield_now}\\n    import mpdep.{reach}\\n\\n    func worker(n: Int) -> Int { yield_now()  n + 1 }\\n\\n    func main() {\\n        var group = TaskGroup()\\n        let h = group.spawn(worker(1))\\n        print(h.join())\\n        print(reach(7))\\n    }\\n\\n    ./.venv/bin/python sawc/sawc.py .build/scratch/mainprobe/entry.saw \\\\\\n        --module-path mpdep=.build/scratch/mainprobe/modules/mpdep -o .build/scratch/mainprobe/entry\\n\\n    Linking failed: Undefined symbols for architecture arm64:\\n      \\\"_main\\\", referenced from:\\n          <initial-undefines>\\n\\nReproduced identically with the SL-280 branch and with the three SL-280 files reverted to HEAD, so it is not the funnel.\\n\\n## Mechanism\\n\\n`_free_function_symbol_base` (`sawc/typechecker/registration.py:1328-1354`) gives a free function a `$M$<module>` tag whenever `free_function_owners` reports that more than one module of the compilation declares the name — deliberately including the ROOT module (`module_tag(())` is `root`), because std is checked once and cached so the entry is the side that has to move. The census counts `main` like any other name. Nothing exempts it, and `main` is the C entry emitted by name.\\n\\nThe dependency's `main` need not be public or reachable; declaring the name is enough.\\n\\n## Relation to SL-274\\n\\nSL-274's own sweep already names `main` as exempt from BOTH the mangling and the internalization, and records why the distinction matters: \\\"The codegen sites key on `func.name`, so a symbol-only rename is safe there — a `mangled_symbol` stamp is not.\\\" This issue is the same hole reachable today, through design 249 rather than through SL-274's widening — so the exemption SL-274 will add is the fix for both, and this is the case that says it is not merely prospective.\\n\\nSeverity is low in practice (a dependency has little reason to declare `main`) but the failure mode is a bare linker error naming no user construct.\\n\",\"labels\":[\"bug\",\"compiler\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"pid-10758-1789657851-6000\",\"stage\":\"backlog\",\"title\":\"A dependency declaring the name `main` renames the entry module's main symbol — the link fails with no user-facing diagnostic\"}","request_id":"pid-10758-1789657851-6000","sequence":895,"timestamp":"1789657851"}
---
