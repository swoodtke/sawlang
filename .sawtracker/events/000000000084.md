---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791231","description":"## Scope and status\n\nDF-178e — a bare literal does not adopt an annotated type through a match. let ra: UInt = match a.join() { case Ok(v) -> v, case Err(_) -> 0 } is the match arms…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/195-integer-width-agreement.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/195-integer-width-agreement.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 2740–2750\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L2740-L2750)\n\n- **DF‑178e — a bare literal does not adopt an annotated type through a\n  `match`.** `let ra: UInt = match a.join() { case Ok(v) -> v, case Err(_) -> 0 }`\n  is ``the match arms have no common type: `UInt` and `Int` `` — the annotation\n  does not reach the arms, so the `0` stays platform `Int` and the merge fails.\n  Design 195 says a bare literal adopts a fixed-width EXPECTED type \"wherever\n  one is in force\" and lists if/match arms; an annotation on the BINDING is\n  evidently not one of the places that force propagates from. The workaround is\n  a named `UInt` constant per default value, which is what the two thread images\n  do. Small, clean-error, and it lands on the most ordinary shape there is:\n  reading a Result with a fallback.\n","id":"SL-84","labels":["todo-import","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":84,"status":"open","title":"DF-178e — a bare literal does not adopt an annotated type through a match. let ra: UInt = match a.join() { case Ok(v) -> v, case Err(_) -> 0 } is the match arms…","updated":"1788791231"},"before":null,"id":"SL-84","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-178e — a bare literal does not adopt an annotated type through a match. let ra: UInt = match a.join() { case Ok(v) -> v, case Err(_) -> 0 } is the match arms…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/195-integer-width-agreement.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/195-integer-width-agreement.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 2740–2750\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L2740-L2750)\\n\\n- **DF‑178e — a bare literal does not adopt an annotated type through a\\n  `match`.** `let ra: UInt = match a.join() { case Ok(v) -> v, case Err(_) -> 0 }`\\n  is ``the match arms have no common type: `UInt` and `Int` `` — the annotation\\n  does not reach the arms, so the `0` stays platform `Int` and the merge fails.\\n  Design 195 says a bare literal adopts a fixed-width EXPECTED type \\\"wherever\\n  one is in force\\\" and lists if/match arms; an annotation on the BINDING is\\n  evidently not one of the places that force propagates from. The workaround is\\n  a named `UInt` constant per default value, which is what the two thread images\\n  do. Small, clean-error, and it lands on the most ordinary shape there is:\\n  reading a Result with a fallback.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-263fedde8d79c2115ee4-create\",\"title\":\"DF-178e — a bare literal does not adopt an annotated type through a match. let ra: UInt = match a.join() { case Ok(v) -> v, case Err(_) -> 0 } is the match arms…\"}","request_id":"todo-20260907-263fedde8d79c2115ee4-create","sequence":84,"timestamp":"1788791231"}
---
