---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-243.p1 (revision 1): minivm M0: subset compiler foundation — Int32/Bool VM + textual LLVM emitter + differential harness (branch commit 66e56883)","created":"1789086412","id":"c1"}],"created":"1789086324","description":"SL-242 epic child, landing patch 1/14. The baseline: functions, let/var, if/while, checked Int32 arithmetic, recursion, print; typed linear IR; VM + textual LLVM engines; 43-case harness with three oracles. Contract: prototypes/minivm/DESIGN.md (lands with this patch). Patch = branch commit 66e56883.","id":"SL-243","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":423,"stage":"queued","status":"open","title":"minivm M0: subset compiler foundation — Int32/Bool VM + textual LLVM emitter + differential harness","updated":"1789086412"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086324","description":"SL-242 epic child, landing patch 1/14. The baseline: functions, let/var, if/while, checked Int32 arithmetic, recursion, print; typed linear IR; VM + textual LLVM engines; 43-case harness with three oracles. Contract: prototypes/minivm/DESIGN.md (lands with this patch). Patch = branch commit 66e56883.","id":"SL-243","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":405,"stage":"queued","status":"open","title":"minivm M0: subset compiler foundation — Int32/Bool VM + textual LLVM emitter + differential harness","updated":"1789086324"},"id":"SL-243","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/DESIGN.md b/prototypes/minivm/DESIGN.md\\nnew file mode 100644\\nindex 00000000..2e664da2\\n--- /dev/null\\n+++ b/prototypes/minivm/DESIGN.md\\n@@ -0,0 +1,125 @@\\n+# Minimal Saw compiler experiment\\n+\\n+This is a disposable experiment, not the self-hosting port or a change to Saw's\\n+language contract. No Sawtracker submission is part of this work. The primary\\n+agent owns this design, interfaces, integration, and review; Sol implements\\n+bounded modules against the interfaces. All implementation is synchronous Saw.\\n+\\n+## Goal and acceptance\\n+\\n+Compile a small Saw subset once into a shared instruction representation. Execute\\n+that representation in a VM, or emit textual LLVM IR from it. External clang may\\n+verify, compile, and link the emitted text; the prototype contains no LLVM binding.\\n+Positive fixtures must produce identical stdout and exit status through both\\n+paths, with explicit expected outputs as a third oracle. Compile-time rejection\\n+fixtures must have a located error. Runtime overflow and division-by-zero fixtures\\n+must fail consistently. This does not claim the prototype can compile itself.\\n+\\n+## Source contract\\n+\\n+* One source file; reuse selfhost/lexer via --module-path sawlex=selfhost/lexer.\\n+* Functions: func name(p: Int32, flag: Bool) -> Int32/Bool { ... }, or omitted\\n+  return type for Void. Exactly one main() returning Void; no parameters there.\\n+* Explicit return statements. No implicit value tails, overloads, imports,\\n+  methods, generics, closures, references, arrays, heap objects, or concurrency.\\n+* let/var with initializer, optional Int32/Bool annotation; assignment only to\\n+  mutable locals. Lexical block scopes; no duplicate declaration in a scope.\\n+* Statement if/else and while with Bool conditions, nested blocks, print(expr),\\n+  call expression statements (Void only), return expr or bare return for Void.\\n+* Decimal Int32 literals, true/false, names, parentheses, positional function\\n+  calls; unary minus/not; + - * / %, == != < <= > >=. Comparisons of integers;\\n+  equality also supports Bool. No implicit Int32/Bool conversions. No &&/|| yet.\\n+* Left-to-right operand and argument evaluation, forward calls and recursion.\\n+  All non-Void functions must return on every reachable path. An implicit Void\\n+  return at end is allowed. Reject unsupported tokens rather than ignoring them.\\n+* Newlines separate statements; newlines in parentheses are whitespace. Semicolons\\n+  are rejected. Braces may close a final statement without an intervening newline.\\n+* Int32 means signed 32-bit everywhere. Ordinary arithmetic traps on overflow.\\n+  Host Saw Int is 64-bit for this experiment: i32 operands' add/sub/multiply fit\\n+  the host intermediate, followed by [-2147483648,2147483647] validation. Divide\\n+  truncates toward zero; zero divisor traps; min/-1 division traps as overflow.\\n+  Remainder min%-1 is zero (no LLVM poison: widen to i64 before srem).\\n+  Literal -2147483648 must work; positive 2147483648 is rejected. Decimal suffix\\n+  i32 may be accepted; all other suffixes/bases outside scope are rejected.\\n+\\n+## Shared representation (src/model.saw is authoritative)\\n+\\n+Program is a vector of FuncIR plus main function index. FuncIR owns its name,\\n+parameter types, result type, slot types, and linear instruction stream. Parameters\\n+occupy slots [0, parameter count). Other slots hold locals and expression results.\\n+All scalar runtime cells are signed host Int, Bool encoded 0/1. No pointers/heap.\\n+Each call allocates a fresh frame; recursive calls never share slots.\\n+\\n+Instruction fields are intentionally uniform for the experiment. Op determines\\n+which fields are meaningful; the constructor initializes unused scalar fields to\\n+-1 except immediate=0. The verifier checks meaningful operands only; unused\\n+fields are ignored by both execution paths.\\n+\\n+| Op | Fields and behavior |\\n+|---|---|\\n+| Const | dst, immediate; slot type is I32 or Bool |\\n+| Copy | dst = a, same type |\\n+| Add/Sub/Mul/Div/Rem | dst = a op b, all I32, checked |\\n+| Neg | dst = -a, I32, checked |\\n+| Not | dst = !a, Bool |\\n+| Eq/Ne | dst Bool, a/b same non-Void type |\\n+| Lt/Le/Gt/Ge | dst Bool, a/b I32 |\\n+| Jump | target is instruction index in current function |\\n+| Branch | a Bool; true -> target, false -> next instruction |\\n+| Call | target function index, args slot indices; dst=-1 for Void |\\n+| Print | a scalar, output decimal or lowercase true/false plus newline |\\n+| Return | a return slot or -1 for Void |\\n+\\n+Except Jump/Branch/Return, execution continues to the next instruction. Branch\\n+targets always point to real instructions. There must be no reachable falloff.\\n+Frontend resolves names/types and validates initialization before emitting IR;\\n+integration verification additionally checks structural indices/types and calls.\\n+Slots may be reassigned (not SSA); LLVM uses one alloca per slot and one basic\\n+block per instruction. Thus both backends consume exactly the same CFG, with no\\n+phi construction or second control-flow lowering. LLVM can optimize externally.\\n+Emit function names from their numeric IDs, never unsanitized user identifiers.\\n+\\n+## Interfaces and file ownership\\n+\\n+* model.saw: primary-owned shared schema, ProtoError, constructors.\\n+* frontend.saw: Sol frontend task. compile_source(String) -> Result<Program,ProtoError>.\\n+  Signature scan first, then parse/typecheck bodies and lower directly to IR; a\\n+  second public AST is unnecessary for this small prototype. Bound parser depth.\\n+* vm.saw: Sol VM task. execute(&Program, budget: Int) -> Result<Void,ProtoError>.\\n+  Explicit call stack preferred; impose max depth 128 and a shared instruction\\n+  budget (default 1000000). These are VM execution limits, not language rules.\\n+* llvm.saw: Sol backend task. emit_llvm(&Program) -> Result<String,ProtoError>.\\n+  Scalar slots map to i64 cells in LLVM as well; I32 operations use i64 intermediates\\n+  and bounds checks, Bool branch converts canonical i64 to i1. Direct native\\n+  function calls use i64 scalar arguments/results, void for Void. main wrapper\\n+  returns i32 0. Declare printf/puts/exit for output and trap support. No optimizer,\\n+  LLVM library calls, target structs, FFI generality, or runtime allocation.\\n+* main.saw, verify.saw, README.md: primary integration ownership.\\n+* test_minivm.py, examples/: Sol test implementation, reviewed by the primary.\\n+\\n+Runtime error messages are exact: `runtime error: integer overflow`,\\n+`runtime error: division by zero`, `runtime error: instruction budget exceeded`,\\n+`runtime error: call depth exceeded`. VM returns ProtoError; CLI prints its message\\n+once and exits 1. LLVM trap helpers print the same line and exit 1. No extra output\\n+on successful emit-llvm: stdout is only IR. CLI: minivm run FILE [--budget N],\\n+minivm emit-llvm FILE. Usage/I/O/parse/type errors exit 1 with a message. Source\\n+errors format `FILE:line:col: message`; runtime errors omit source coordinates.\\n+\\n+## Testing and review sequence\\n+\\n+1. Compile each new module with existing Python sawc, reusing the venv. No changes\\n+   to sawc/, std, existing lexer, existing gates, or numbered designs are intended.\\n+2. Verify arithmetic, precedence, shadowing, mutation, branches, loops, calls,\\n+   forward references, recursion, early returns, and evaluation order with stdout\\n+   oracles; include each backend's error and nonzero-exit path.\\n+3. Reject unknown names/types/operators, malformed syntax, immutable assignment,\\n+   argument/return type mismatch, missing return, and unsupported features.\\n+4. Emit .ll, compile with clang, run with a timeout, compare VM/native/expected\\n+   outputs. VM budget tests are VM-only; native nontermination uses process timeout.\\n+5. Primary reviews code and independently adds adversarial tests. Do not claim\\n+   completion based on two engines agreeing without expected-output checks.\\n+\\n+Prototype allocation failure may retain existing try! behavior. Compiler-language\\n+bugs encountered must be reported to the primary with repro evidence; do not change\\n+the production compiler or create tracker entries. Record reviewed milestones as\\n+separate commits on the prototype branch.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nnew file mode 100644\\nindex 00000000..2eec9419\\n--- /dev/null\\n+++ b/prototypes/minivm/README.md\\n@@ -0,0 +1,80 @@\\n+# Saw compiler / VM prototype\\n+\\n+A small compiler written in Saw, with two consumers of the same typed instruction\\n+stream: an interpreter and a handwritten textual LLVM IR emitter. The existing\\n+Saw lexer is reused. There are no LLVM library calls in this prototype.\\n+\\n+The existing Python compiler builds the prototype executable. Once built, it\\n+parses, checks, lowers, interprets, and emits IR without invoking Python. It\\n+cannot yet compile its own source.\\n+\\n+## Build and try it\\n+\\n+From the repository root, using a Python environment with Saw's dependencies:\\n+\\n+```sh\\n+mkdir -p .build/minivm\\n+python sawc/sawc.py prototypes/minivm/src/main.saw \\\\\\n+  --module-path sawlex=selfhost/lexer -o .build/minivm/minivm\\n+.build/minivm/minivm run prototypes/minivm/examples/factorial.saw\\n+.build/minivm/minivm emit-llvm prototypes/minivm/examples/factorial.saw > .build/minivm/factorial.ll\\n+clang .build/minivm/factorial.ll -o .build/minivm/factorial\\n+.build/minivm/factorial\\n+```\\n+\\n+The LLVM path targets a 64-bit Unix C ABI (`printf`, `puts`, `exit`); validation\\n+uses Apple clang on arm64 macOS. Clang is an external validation/compilation step,\\n+not a dependency of the interpreter or text emitter.\\n+\\n+## Supported source\\n+\\n+```saw\\n+func factorial(n: Int32) -> Int32 {\\n+    if n <= 1 {\\n+        return 1\\n+    } else {\\n+        return n * factorial(n - 1)\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(factorial(5))\\n+}\\n+```\\n+\\n+Single-file programs support `Int32`, `Bool`, functions, positional calls,\\n+recursion, `let`/`var`, assignment, lexical scopes, statement `if`/`else` and\\n+`while`, explicit returns, and scalar printing. Arithmetic is checked; signed\\n+division truncates toward zero. Overflow and division by zero print an error\\n+and exit with status 1. Boolean conditions require `Bool`.\\n+\\n+Newlines separate statements. Omit the result annotation for Void functions;\\n+`main()` must take no arguments and return Void. Value-returning functions need\\n+explicit returns. There are no imports, strings, collections, references,\\n+generics, methods, implicit value tails, or concurrency in the accepted subset.\\n+Semicolons and logical `&&`/`||` are also outside this initial subset.\\n+\\n+The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n+depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n+only to VM execution; native code uses the host call stack and has no budget.\\n+\\n+## Validation\\n+\\n+```sh\\n+python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n+```\\n+\\n+The harness compares VM execution and clang-compiled IR with explicit expected\\n+outputs and statuses, checks rejected programs, and exercises VM limits. It\\n+uses temporary files and 30-second subprocess timeouts. Initial validation passed\\n+all 43 cases, including native execution at both `-O0` and `-O2`.\\n+\\n+See [DESIGN.md](DESIGN.md) for the instruction schema and semantics. Source is\\n+split into `frontend.saw`, `model.saw`, `verify.saw`, `vm.saw`, `llvm.saw`, and\\n+`main.saw`. The verifier checks structural indices and types; initialization\\n+is established by the frontend, so this is not a loader for untrusted bytecode.\\n+\\n+This is a correctness experiment. Linear name lookup, heap-allocated slot\\n+vectors, deep copies in the interpreter, and one LLVM block per instruction\\n+favor simplicity over speed. Allocation failure can still trigger Saw's\\n+`try!` behavior. The production compiler and lexer are unchanged.\\ndiff --git a/prototypes/minivm/Saw.toml b/prototypes/minivm/Saw.toml\\nnew file mode 100644\\nindex 00000000..92d6af91\\n--- /dev/null\\n+++ b/prototypes/minivm/Saw.toml\\n@@ -0,0 +1,6 @@\\n+[package]\\n+name = \\\"minivm\\\"\\n+version = \\\"0.0.1\\\"\\n+\\n+[dependencies]\\n+sawlex = { path = \\\"../../selfhost/lexer\\\" }\\ndiff --git a/prototypes/minivm/examples/arithmetic.saw b/prototypes/minivm/examples/arithmetic.saw\\nnew file mode 100644\\nindex 00000000..53db36de\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/arithmetic.saw\\n@@ -0,0 +1,8 @@\\n+func main() {\\n+    print(7 + 5)\\n+    print(7 - 10)\\n+    print(6 * 7)\\n+    print(-7 / 3)\\n+    print(-7 % 3)\\n+    print(-2147483648 % -1)\\n+}\\ndiff --git a/prototypes/minivm/examples/bool_forward.saw b/prototypes/minivm/examples/bool_forward.saw\\nnew file mode 100644\\nindex 00000000..fb265fa7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/bool_forward.saw\\n@@ -0,0 +1,7 @@\\n+func main() {\\n+    print(is_zero(0))\\n+    print(is_zero(2))\\n+}\\n+func is_zero(x: Int32) -> Bool {\\n+    return x == 0\\n+}\\ndiff --git a/prototypes/minivm/examples/budget.saw b/prototypes/minivm/examples/budget.saw\\nnew file mode 100644\\nindex 00000000..16633cf6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/budget.saw\\n@@ -0,0 +1 @@\\n+func main() { while true { } }\\ndiff --git a/prototypes/minivm/examples/comparisons.saw b/prototypes/minivm/examples/comparisons.saw\\nnew file mode 100644\\nindex 00000000..75c83f2f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/comparisons.saw\\n@@ -0,0 +1,10 @@\\n+func main() {\\n+    print(2 == 2)\\n+    print(2 != 2)\\n+    print(2 < 3)\\n+    print(3 <= 3)\\n+    print(4 > 3)\\n+    print(4 >= 5)\\n+    print(true == true)\\n+    print(true != false)\\n+}\\ndiff --git a/prototypes/minivm/examples/copy_independence.saw b/prototypes/minivm/examples/copy_independence.saw\\nnew file mode 100644\\nindex 00000000..907938d5\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/copy_independence.saw\\n@@ -0,0 +1,7 @@\\n+func main() {\\n+    var source = 4\\n+    let copy = source\\n+    source = 9\\n+    print(copy)\\n+    print(source)\\n+}\\ndiff --git a/prototypes/minivm/examples/depth.saw b/prototypes/minivm/examples/depth.saw\\nnew file mode 100644\\nindex 00000000..f2e22708\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/depth.saw\\n@@ -0,0 +1,2 @@\\n+func dive(n: Int32) -> Int32 { return dive(n + 1) }\\n+func main() { print(dive(0)) }\\ndiff --git a/prototypes/minivm/examples/div_zero.saw b/prototypes/minivm/examples/div_zero.saw\\nnew file mode 100644\\nindex 00000000..6517846f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/div_zero.saw\\n@@ -0,0 +1 @@\\n+func main() { print(1 / 0) }\\ndiff --git a/prototypes/minivm/examples/empty_blocks.saw b/prototypes/minivm/examples/empty_blocks.saw\\nnew file mode 100644\\nindex 00000000..d48a6b19\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/empty_blocks.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    if false { }\\n+    while false { }\\n+    print(3)\\n+}\\ndiff --git a/prototypes/minivm/examples/evaluation_order.saw b/prototypes/minivm/examples/evaluation_order.saw\\nnew file mode 100644\\nindex 00000000..c07935e9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/evaluation_order.saw\\n@@ -0,0 +1,10 @@\\n+func mark(x: Int32) -> Int32 {\\n+    print(x)\\n+    return x\\n+}\\n+func add(a: Int32, b: Int32) -> Int32 {\\n+    return a + b\\n+}\\n+func main() {\\n+    print(add(mark(1), mark(2)))\\n+}\\ndiff --git a/prototypes/minivm/examples/factorial.saw b/prototypes/minivm/examples/factorial.saw\\nnew file mode 100644\\nindex 00000000..783ae99f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/factorial.saw\\n@@ -0,0 +1,6 @@\\n+func factorial(n: Int32) -> Int32 {\\n+    if n <= 1 { return 1 } else { return n * factorial(n - 1) }\\n+}\\n+func main() {\\n+    print(factorial(6))\\n+}\\ndiff --git a/prototypes/minivm/examples/forward_call.saw b/prototypes/minivm/examples/forward_call.saw\\nnew file mode 100644\\nindex 00000000..6495e963\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/forward_call.saw\\n@@ -0,0 +1,6 @@\\n+func main() {\\n+    print(twice(21))\\n+}\\n+func twice(x: Int32) -> Int32 {\\n+    return x * 2\\n+}\\ndiff --git a/prototypes/minivm/examples/if_returns.saw b/prototypes/minivm/examples/if_returns.saw\\nnew file mode 100644\\nindex 00000000..8c0c7889\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/if_returns.saw\\n@@ -0,0 +1,7 @@\\n+func choose(x: Int32) -> Int32 {\\n+    if x < 0 { return -1 } else { return 1 }\\n+}\\n+func main() {\\n+    print(choose(-3))\\n+    print(choose(3))\\n+}\\ndiff --git a/prototypes/minivm/examples/loop.saw b/prototypes/minivm/examples/loop.saw\\nnew file mode 100644\\nindex 00000000..0dea2729\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/loop.saw\\n@@ -0,0 +1,9 @@\\n+func main() {\\n+    var i: Int32 = 0\\n+    var total: Int32 = 0\\n+    while i < 5 {\\n+        total = total + i\\n+        i = i + 1\\n+    }\\n+    print(total)\\n+}\\ndiff --git a/prototypes/minivm/examples/multiline_forms.saw b/prototypes/minivm/examples/multiline_forms.saw\\nnew file mode 100644\\nindex 00000000..07d7b1ab\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/multiline_forms.saw\\n@@ -0,0 +1,22 @@\\n+func add(\\n+    a\\n+    :\\n+    Int32,\\n+    b: Int32\\n+) -> Int32 {\\n+    return a + b\\n+}\\n+\\n+func main() {\\n+    print(-\\n+        2147483648)\\n+    print(not\\n+        false)\\n+    print(1\\n+        + 2\\n+        * 3)\\n+    print(add(\\n+        10,\\n+        3\\n+    ))\\n+}\\ndiff --git a/prototypes/minivm/examples/multiline_parens.saw b/prototypes/minivm/examples/multiline_parens.saw\\nnew file mode 100644\\nindex 00000000..7e3fa401\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/multiline_parens.saw\\n@@ -0,0 +1,6 @@\\n+func main() {\\n+    print((1\\n+        +\\n+        2 *\\n+        3))\\n+}\\ndiff --git a/prototypes/minivm/examples/mutual_recursion.saw b/prototypes/minivm/examples/mutual_recursion.saw\\nnew file mode 100644\\nindex 00000000..a765df95\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/mutual_recursion.saw\\n@@ -0,0 +1,10 @@\\n+func even(n: Int32) -> Bool {\\n+    if n == 0 { return true } else { return odd(n - 1) }\\n+}\\n+func odd(n: Int32) -> Bool {\\n+    if n == 0 { return false } else { return even(n - 1) }\\n+}\\n+func main() {\\n+    print(even(10))\\n+    print(odd(9))\\n+}\\ndiff --git a/prototypes/minivm/examples/overflow_add.saw b/prototypes/minivm/examples/overflow_add.saw\\nnew file mode 100644\\nindex 00000000..11a38196\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/overflow_add.saw\\n@@ -0,0 +1 @@\\n+func main() { print(2147483647 + 1) }\\ndiff --git a/prototypes/minivm/examples/overflow_div.saw b/prototypes/minivm/examples/overflow_div.saw\\nnew file mode 100644\\nindex 00000000..76d445c4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/overflow_div.saw\\n@@ -0,0 +1 @@\\n+func main() { print(-2147483648 / -1) }\\ndiff --git a/prototypes/minivm/examples/overflow_mul.saw b/prototypes/minivm/examples/overflow_mul.saw\\nnew file mode 100644\\nindex 00000000..252e7099\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/overflow_mul.saw\\n@@ -0,0 +1 @@\\n+func main() { print(50000 * 50000) }\\ndiff --git a/prototypes/minivm/examples/overflow_neg.saw b/prototypes/minivm/examples/overflow_neg.saw\\nnew file mode 100644\\nindex 00000000..86546876\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/overflow_neg.saw\\n@@ -0,0 +1 @@\\n+func main() { print(-(-2147483648)) }\\ndiff --git a/prototypes/minivm/examples/overflow_sub.saw b/prototypes/minivm/examples/overflow_sub.saw\\nnew file mode 100644\\nindex 00000000..b70518b1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/overflow_sub.saw\\n@@ -0,0 +1 @@\\n+func main() { print(-2147483648 - 1) }\\ndiff --git a/prototypes/minivm/examples/precedence.saw b/prototypes/minivm/examples/precedence.saw\\nnew file mode 100644\\nindex 00000000..c22316e2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/precedence.saw\\n@@ -0,0 +1,6 @@\\n+func main() {\\n+    print(2 + 3 * 4)\\n+    print((2 + 3) * 4)\\n+    print(-2 * 3 + 10 / 2)\\n+    print(not false == true)\\n+}\\ndiff --git a/prototypes/minivm/examples/recursion.saw b/prototypes/minivm/examples/recursion.saw\\nnew file mode 100644\\nindex 00000000..63c00dad\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/recursion.saw\\n@@ -0,0 +1,6 @@\\n+func fact(n: Int32) -> Int32 {\\n+    if n <= 1 { return 1 } else { return n * fact(n - 1) }\\n+}\\n+func main() {\\n+    print(fact(6))\\n+}\\ndiff --git a/prototypes/minivm/examples/reject_arity.saw b/prototypes/minivm/examples/reject_arity.saw\\nnew file mode 100644\\nindex 00000000..8dcc62bc\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_arity.saw\\n@@ -0,0 +1,2 @@\\n+func f(x: Int32) -> Int32 { return x }\\n+func main() { print(f()) }\\ndiff --git a/prototypes/minivm/examples/reject_bool_arithmetic.saw b/prototypes/minivm/examples/reject_bool_arithmetic.saw\\nnew file mode 100644\\nindex 00000000..883157fe\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_bool_arithmetic.saw\\n@@ -0,0 +1 @@\\n+func main() { print(true + false) }\\ndiff --git a/prototypes/minivm/examples/reject_duplicate.saw b/prototypes/minivm/examples/reject_duplicate.saw\\nnew file mode 100644\\nindex 00000000..730d487d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_duplicate.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let x = 1\\n+    let x = 2\\n+}\\ndiff --git a/prototypes/minivm/examples/reject_import.saw b/prototypes/minivm/examples/reject_import.saw\\nnew file mode 100644\\nindex 00000000..d6caba4e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_import.saw\\n@@ -0,0 +1,2 @@\\n+import other\\n+func main() { }\\ndiff --git a/prototypes/minivm/examples/reject_large_literal.saw b/prototypes/minivm/examples/reject_large_literal.saw\\nnew file mode 100644\\nindex 00000000..a7e2d8f1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_large_literal.saw\\n@@ -0,0 +1,3 @@\\n+func main() {\\n+    print(999999999999999999999999999999999999999999999999999999999999)\\n+}\\ndiff --git a/prototypes/minivm/examples/reject_missing_return.saw b/prototypes/minivm/examples/reject_missing_return.saw\\nnew file mode 100644\\nindex 00000000..793b24bc\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_missing_return.saw\\n@@ -0,0 +1,2 @@\\n+func f(x: Int32) -> Int32 { if x > 0 { return x } }\\n+func main() { print(f(1)) }\\ndiff --git a/prototypes/minivm/examples/reject_mutability.saw b/prototypes/minivm/examples/reject_mutability.saw\\nnew file mode 100644\\nindex 00000000..3fbec3cd\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_mutability.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let x = 1\\n+    x = 2\\n+}\\ndiff --git a/prototypes/minivm/examples/reject_reserved_print.saw b/prototypes/minivm/examples/reject_reserved_print.saw\\nnew file mode 100644\\nindex 00000000..e32b9484\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_reserved_print.saw\\n@@ -0,0 +1,4 @@\\n+func print(x: Bool) {\\n+    return\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/reject_scope_leak.saw b/prototypes/minivm/examples/reject_scope_leak.saw\\nnew file mode 100644\\nindex 00000000..31617b62\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_scope_leak.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    if true { let hidden = 4 }\\n+    print(hidden)\\n+}\\ndiff --git a/prototypes/minivm/examples/reject_semicolon.saw b/prototypes/minivm/examples/reject_semicolon.saw\\nnew file mode 100644\\nindex 00000000..676dd831\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_semicolon.saw\\n@@ -0,0 +1 @@\\n+func main() { print(1); }\\ndiff --git a/prototypes/minivm/examples/reject_string.saw b/prototypes/minivm/examples/reject_string.saw\\nnew file mode 100644\\nindex 00000000..d2a13253\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_string.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"no\\\") }\\ndiff --git a/prototypes/minivm/examples/reject_truncated.saw b/prototypes/minivm/examples/reject_truncated.saw\\nnew file mode 100644\\nindex 00000000..f4348aab\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_truncated.saw\\n@@ -0,0 +1,2 @@\\n+func main() {\\n+    print(1)\\ndiff --git a/prototypes/minivm/examples/reject_type.saw b/prototypes/minivm/examples/reject_type.saw\\nnew file mode 100644\\nindex 00000000..6490885b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_type.saw\\n@@ -0,0 +1 @@\\n+func main() { let x: Bool = 1 print(x) }\\ndiff --git a/prototypes/minivm/examples/reject_unary_depth.saw b/prototypes/minivm/examples/reject_unary_depth.saw\\nnew file mode 100644\\nindex 00000000..6b2881f4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_unary_depth.saw\\n@@ -0,0 +1,3 @@\\n+func main() {\\n+    print(not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not not false)\\n+}\\ndiff --git a/prototypes/minivm/examples/reject_unknown.saw b/prototypes/minivm/examples/reject_unknown.saw\\nnew file mode 100644\\nindex 00000000..b07f753e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/reject_unknown.saw\\n@@ -0,0 +1 @@\\n+func main() { print(missing) }\\ndiff --git a/prototypes/minivm/examples/rem_zero.saw b/prototypes/minivm/examples/rem_zero.saw\\nnew file mode 100644\\nindex 00000000..d1c75da2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/rem_zero.saw\\n@@ -0,0 +1 @@\\n+func main() { print(1 % 0) }\\ndiff --git a/prototypes/minivm/examples/scopes.saw b/prototypes/minivm/examples/scopes.saw\\nnew file mode 100644\\nindex 00000000..50109fcb\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scopes.saw\\n@@ -0,0 +1,9 @@\\n+func main() {\\n+    var x: Int32 = 1\\n+    if true {\\n+        let x: Int32 = 9\\n+        print(x)\\n+    }\\n+    x = x + 2\\n+    print(x)\\n+}\\ndiff --git a/prototypes/minivm/examples/shadow_initializer.saw b/prototypes/minivm/examples/shadow_initializer.saw\\nnew file mode 100644\\nindex 00000000..774a7e7d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/shadow_initializer.saw\\n@@ -0,0 +1,8 @@\\n+func main() {\\n+    let x = 5\\n+    if true {\\n+        let x = x + 1\\n+        print(x)\\n+    }\\n+    print(x)\\n+}\\ndiff --git a/prototypes/minivm/examples/unreachable.saw b/prototypes/minivm/examples/unreachable.saw\\nnew file mode 100644\\nindex 00000000..32eceac4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/unreachable.saw\\n@@ -0,0 +1,8 @@\\n+func stop() -> Int32 {\\n+    return 7\\n+    print(99)\\n+    if true { print(98) }\\n+}\\n+func main() {\\n+    print(stop())\\n+}\\ndiff --git a/prototypes/minivm/examples/void_return.saw b/prototypes/minivm/examples/void_return.saw\\nnew file mode 100644\\nindex 00000000..1f2b042d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/void_return.saw\\n@@ -0,0 +1,11 @@\\n+func announce(flag: Bool) {\\n+    if flag {\\n+        print(7)\\n+        return\\n+    }\\n+    print(8)\\n+}\\n+func main() {\\n+    announce(true)\\n+    announce(false)\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nnew file mode 100644\\nindex 00000000..534625a5\\n--- /dev/null\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -0,0 +1,653 @@\\n+import sawlex.src.lib.*\\n+import src.model.*\\n+\\n+func Some<T>(value: T) -> T? { move value }\\n+\\n+func clone_token(t: &Token) -> Token {\\n+    Token(kind: t.kind, value: t.value, line: t.line, col: t.col, suffix: t.suffix)\\n+}\\n+\\n+struct Signature {\\n+    name: String\\n+    param_names: Vector<String>\\n+    params: Vector<ValueType>\\n+    result: ValueType\\n+    body_start: Int\\n+    body_end: Int\\n+    line: Int\\n+    col: Int\\n+}\\n+extension Signature: NoCopy {}\\n+\\n+struct Binding {\\n+    name: String\\n+    ty: ValueType\\n+    slot: Int\\n+    mutable: Bool\\n+    depth: Int\\n+}\\n+\\n+struct ExprValue {\\n+    ty: ValueType\\n+    slot: Int\\n+    call: Bool\\n+}\\n+\\n+struct Compiler {\\n+    tokens: Vector<Token>\\n+    signatures: Vector<Signature>\\n+    pos: Int\\n+    depth: Int\\n+    expr_depth: Int\\n+    paren_depth: Int\\n+    bindings: Vector<Binding>\\n+    slots: Vector<ValueType>\\n+    code: Vector<Instruction>\\n+    current_result: ValueType\\n+}\\n+extension Compiler: NoCopy {}\\n+\\n+extension Compiler {\\n+    func token(&self) -> Token { clone_token(&self.tokens[self.pos]) }\\n+\\n+    func at(&self, kind: TokenKind) -> Bool { self.token().kind == kind }\\n+\\n+    func fail(&self, message: String) -> ProtoError {\\n+        problem(message, self.token().line, self.token().col)\\n+    }\\n+\\n+    func take(&var self) -> Token {\\n+        let t = clone_token(&self.tokens[self.pos])\\n+        self.pos += 1\\n+        t\\n+    }\\n+\\n+    func expect(&var self, kind: TokenKind, message: String) -> Result<Token, ProtoError> {\\n+        if not self.at(kind) { return self.fail(message) }\\n+        self.take()\\n+    }\\n+\\n+    func skip_newlines(&var self) {\\n+        while self.at(TokenKind.Newline) { let _ = self.take() }\\n+    }\\n+\\n+    func expr_space(&var self) {\\n+        if self.paren_depth > 0 { self.skip_newlines() }\\n+    }\\n+\\n+    func parse_type(&var self) -> Result<ValueType, ProtoError> {\\n+        let t = try self.expect(TokenKind.Ident, \\\"expected type\\\")\\n+        if t.value == \\\"Int32\\\" { return ValueType.I32 }\\n+        if t.value == \\\"Bool\\\" { return ValueType.Bool }\\n+        problem(\\\"unsupported type `{t.value}`\\\", t.line, t.col)\\n+    }\\n+\\n+    func signature_index(&self, name: String) -> Int {\\n+        var i = 0\\n+        while i < self.signatures.len() {\\n+            if self.signatures[i].name == name { return i }\\n+            i += 1\\n+        }\\n+        -1\\n+    }\\n+\\n+    func scan_signatures(&var self) -> Result<Void, ProtoError> {\\n+        self.pos = 0\\n+        self.skip_newlines()\\n+        while not self.at(TokenKind.Eof) {\\n+            let ft = try self.expect(TokenKind.Func, \\\"only function declarations are supported at module scope\\\")\\n+            let name = try self.expect(TokenKind.Ident, \\\"expected function name\\\")\\n+            if name.value == \\\"print\\\" {\\n+                return problem(\\\"`print` is reserved by the prototype\\\", name.line, name.col)\\n+            }\\n+            if self.signature_index(name.value) >= 0 {\\n+                return problem(\\\"duplicate function `{name.value}`\\\", name.line, name.col)\\n+            }\\n+            let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after function name\\\")\\n+            var pnames = Vector<String>()\\n+            var ptypes = Vector<ValueType>()\\n+            self.skip_newlines()\\n+            if not self.at(TokenKind.RParen) {\\n+                var more = true\\n+                while more {\\n+                    let pn = try self.expect(TokenKind.Ident, \\\"expected parameter name\\\")\\n+                    var j = 0\\n+                    while j < pnames.len() {\\n+                        if pnames[j] == pn.value {\\n+                            return problem(\\\"duplicate parameter `{pn.value}`\\\", pn.line, pn.col)\\n+                        }\\n+                        j += 1\\n+                    }\\n+                    self.skip_newlines()\\n+                    let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after parameter name\\\")\\n+                    self.skip_newlines()\\n+                    let pt = try self.parse_type()\\n+                    try! pnames.push(pn.value)\\n+                    try! ptypes.push(pt)\\n+                    self.skip_newlines()\\n+                    if self.at(TokenKind.Comma) {\\n+                        let _ = self.take()\\n+                        self.skip_newlines()\\n+                    } else { more = false }\\n+                }\\n+            }\\n+            let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after parameters\\\")\\n+            var result = ValueType.Void\\n+            if self.at(TokenKind.Arrow) {\\n+                let _ = self.take()\\n+                result = try self.parse_type()\\n+            }\\n+            let _ = try self.expect(TokenKind.LBrace, \\\"expected function body\\\")\\n+            let start = self.pos\\n+            var braces = 1\\n+            while braces > 0 && not self.at(TokenKind.Eof) {\\n+                if self.at(TokenKind.LBrace) { braces += 1 }\\n+                if self.at(TokenKind.RBrace) { braces -= 1 }\\n+                let _ = self.take()\\n+            }\\n+            if braces != 0 { return problem(\\\"unterminated function body\\\", ft.line, ft.col) }\\n+            let finish = self.pos - 1\\n+            try! self.signatures.push(Signature(name: name.value, param_names: move pnames,\\n+                params: move ptypes, result: result, body_start: start, body_end: finish,\\n+                line: name.line, col: name.col))\\n+            self.skip_newlines()\\n+        }\\n+        var mains = 0\\n+        var main_i = -1\\n+        var i = 0\\n+        while i < self.signatures.len() {\\n+            if self.signatures[i].name == \\\"main\\\" {\\n+                mains += 1\\n+                main_i = i\\n+                if self.signatures[i].params.len() != 0 || self.signatures[i].result != ValueType.Void {\\n+                    return problem(\\\"main must have no parameters and return Void\\\",\\n+                                   self.signatures[i].line, self.signatures[i].col)\\n+                }\\n+            }\\n+            i += 1\\n+        }\\n+        if mains != 1 {\\n+            let t = self.token()\\n+            return problem(\\\"program must declare exactly one main function\\\", t.line, t.col)\\n+        }\\n+        return\\n+    }\\n+\\n+    func new_slot(&var self, ty: ValueType) -> Int {\\n+        let n = self.slots.len()\\n+        try! self.slots.push(ty)\\n+        n\\n+    }\\n+\\n+    func emit(&var self, ins: Instruction) -> Int {\\n+        let n = self.code.len()\\n+        try! self.code.push(move ins)\\n+        n\\n+    }\\n+\\n+    func emit_at(&var self, op: Op, line: Int, col: Int) -> Int {\\n+        var ins = instruction(op: op)\\n+        ins.line = line\\n+        ins.col = col\\n+        self.emit(move ins)\\n+    }\\n+\\n+    func binding_index(&self, name: String) -> Int {\\n+        var i = self.bindings.len()\\n+        while i > 0 {\\n+            i -= 1\\n+            if self.bindings[i].name == name { return i }\\n+        }\\n+        -1\\n+    }\\n+\\n+    func declare(&var self, name: Token, ty: ValueType, slot: Int, mutable: Bool) -> Result<Void, ProtoError> {\\n+        var i = self.bindings.len()\\n+        while i > 0 {\\n+            i -= 1\\n+            if self.bindings[i].depth != self.depth { break }\\n+            if self.bindings[i].name == name.value {\\n+                return problem(\\\"duplicate declaration `{name.value}` in this scope\\\", name.line, name.col)\\n+            }\\n+        }\\n+        try! self.bindings.push(Binding(name: name.value, ty: ty, slot: slot,\\n+                                        mutable: mutable, depth: self.depth))\\n+        return\\n+    }\\n+\\n+    func leave_scope(&var self) {\\n+        while self.bindings.len() > 0 && self.bindings[self.bindings.len() - 1].depth == self.depth {\\n+            let _ = self.bindings.pop()\\n+        }\\n+    }\\n+\\n+    func terminator(&var self) -> Result<Void, ProtoError> {\\n+        if self.at(TokenKind.Semicolon) { return self.fail(\\\"semicolons are not supported\\\") }\\n+        if self.at(TokenKind.Newline) {\\n+            self.skip_newlines()\\n+            return\\n+        }\\n+        if self.at(TokenKind.RBrace) { return }\\n+        self.fail(\\\"expected newline after statement\\\")\\n+    }\\n+\\n+    func require_type(&self, got: ValueType, wanted: ValueType, t: &Token) -> Result<Void, ProtoError> {\\n+        if got != wanted { return problem(\\\"type mismatch\\\", t.line, t.col) }\\n+        return\\n+    }\\n+\\n+    func parse_primary(&var self) -> Result<ExprValue, ProtoError> {\\n+        if self.depth > 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n+        if self.at(TokenKind.IntLit) {\\n+            let t = self.take()\\n+            if t.value.starts_with(\\\"0x\\\") || t.value.starts_with(\\\"0b\\\") || t.value.starts_with(\\\"0o\\\") {\\n+                return problem(\\\"only decimal integer literals are supported\\\", t.line, t.col)\\n+            }\\n+            if let suffix = t.suffix {\\n+                if suffix != \\\"i32\\\" { return problem(\\\"unsupported integer suffix `{suffix}`\\\", t.line, t.col) }\\n+            }\\n+            guard let value = t.value.to_int() else { return problem(\\\"invalid integer literal\\\", t.line, t.col) }\\n+            if value > 2_147_483_647 { return problem(\\\"integer literal does not fit Int32\\\", t.line, t.col) }\\n+            let dst = self.new_slot(ValueType.I32)\\n+            let at = self.emit_at(Op.Const, t.line, t.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].immediate = value\\n+            return ExprValue(ty: ValueType.I32, slot: dst, call: false)\\n+        }\\n+        if self.at(TokenKind.True_) || self.at(TokenKind.False_) {\\n+            let t = self.take()\\n+            let dst = self.new_slot(ValueType.Bool)\\n+            let at = self.emit_at(Op.Const, t.line, t.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].immediate = if t.kind == TokenKind.True_ { 1 } else { 0 }\\n+            return ExprValue(ty: ValueType.Bool, slot: dst, call: false)\\n+        }\\n+        if self.at(TokenKind.LParen) {\\n+            let _ = self.take()\\n+            self.depth += 1\\n+            self.paren_depth += 1\\n+            self.skip_newlines()\\n+            let value = try self.parse_expr()\\n+            self.skip_newlines()\\n+            let _ = try self.expect(TokenKind.RParen, \\\"expected `)`\\\")\\n+            self.paren_depth -= 1\\n+            self.depth -= 1\\n+            return value\\n+        }\\n+        if self.at(TokenKind.Ident) {\\n+            let name = self.take()\\n+            if self.at(TokenKind.LParen) { return self.parse_call(name) }\\n+            let bi = self.binding_index(name.value)\\n+            if bi < 0 { return problem(\\\"unknown name `{name.value}`\\\", name.line, name.col) }\\n+            return ExprValue(ty: self.bindings[bi].ty, slot: self.bindings[bi].slot, call: false)\\n+        }\\n+        self.fail(\\\"expected expression; this syntax is unsupported\\\")\\n+    }\\n+\\n+    func parse_call(&var self, name: Token) -> Result<ExprValue, ProtoError> {\\n+        let fi = self.signature_index(name.value)\\n+        if fi < 0 { return problem(\\\"unknown function `{name.value}`\\\", name.line, name.col) }\\n+        let _ = self.take()\\n+        var args = Vector<Int>()\\n+        var ai = 0\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        if not self.at(TokenKind.RParen) {\\n+            var more = true\\n+            while more {\\n+                let value = try self.parse_expr()\\n+                if ai >= self.signatures[fi].params.len() {\\n+                    return problem(\\\"too many arguments to `{name.value}`\\\", name.line, name.col)\\n+                }\\n+                try self.require_type(value.ty, self.signatures[fi].params[ai], &name)\\n+                try! args.push(value.slot)\\n+                ai += 1\\n+                self.skip_newlines()\\n+                if self.at(TokenKind.Comma) {\\n+                    let _ = self.take()\\n+                    self.skip_newlines()\\n+                } else { more = false }\\n+            }\\n+        }\\n+        let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after arguments\\\")\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        if ai != self.signatures[fi].params.len() {\\n+            return problem(\\\"wrong number of arguments to `{name.value}`\\\", name.line, name.col)\\n+        }\\n+        let rt = self.signatures[fi].result\\n+        let dst = if rt == ValueType.Void { -1 } else { self.new_slot(rt) }\\n+        let at = self.emit_at(Op.Call, name.line, name.col)\\n+        self.code[at].target = fi\\n+        self.code[at].dst = dst\\n+        self.code[at].args = move args\\n+        ExprValue(ty: rt, slot: dst, call: true)\\n+    }\\n+\\n+    func parse_unary_inner(&var self) -> Result<ExprValue, ProtoError> {\\n+        self.expr_space()\\n+        if self.at(TokenKind.Exclaim) || self.at(TokenKind.Not_) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            let v = try self.parse_unary()\\n+            try self.require_type(v.ty, ValueType.Bool, &t)\\n+            let dst = self.new_slot(ValueType.Bool)\\n+            let at = self.emit_at(Op.Not, t.line, t.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = v.slot\\n+            return ExprValue(ty: ValueType.Bool, slot: dst, call: false)\\n+        }\\n+        if self.at(TokenKind.Minus) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            if self.at(TokenKind.IntLit) {\\n+                let lit = self.token()\\n+                if not lit.value.starts_with(\\\"0x\\\") && not lit.value.starts_with(\\\"0b\\\") && not lit.value.starts_with(\\\"0o\\\") {\\n+                    if let n = lit.value.to_int() {\\n+                        if n == 2_147_483_648 {\\n+                            let owned = self.take()\\n+                            if let suffix = owned.suffix {\\n+                                if suffix != \\\"i32\\\" { return problem(\\\"unsupported integer suffix `{suffix}`\\\", owned.line, owned.col) }\\n+                            }\\n+                            let dst = self.new_slot(ValueType.I32)\\n+                            let at = self.emit_at(Op.Const, t.line, t.col)\\n+                            self.code[at].dst = dst\\n+                            self.code[at].immediate = -2_147_483_648\\n+                            return ExprValue(ty: ValueType.I32, slot: dst, call: false)\\n+                        }\\n+                    }\\n+                }\\n+            }\\n+            let v = try self.parse_unary()\\n+            try self.require_type(v.ty, ValueType.I32, &t)\\n+            let dst = self.new_slot(ValueType.I32)\\n+            let at = self.emit_at(Op.Neg, t.line, t.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = v.slot\\n+            return ExprValue(ty: ValueType.I32, slot: dst, call: false)\\n+        }\\n+        self.parse_primary()\\n+    }\\n+\\n+    func parse_unary(&var self) -> Result<ExprValue, ProtoError> {\\n+        if self.expr_depth >= 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n+        self.expr_depth += 1\\n+        let value = try self.parse_unary_inner()\\n+        self.expr_depth -= 1\\n+        value\\n+    }\\n+\\n+    func binary(&var self, left: ExprValue, right: ExprValue, op: Op,\\n+                result: ValueType, t: &Token) -> Result<ExprValue, ProtoError> {\\n+        let dst = self.new_slot(result)\\n+        let at = self.emit_at(op, t.line, t.col)\\n+        self.code[at].dst = dst\\n+        self.code[at].a = left.slot\\n+        self.code[at].b = right.slot\\n+        ExprValue(ty: result, slot: dst, call: false)\\n+    }\\n+\\n+    func parse_mul(&var self) -> Result<ExprValue, ProtoError> {\\n+        self.expr_space()\\n+        var left = try self.parse_unary()\\n+        self.expr_space()\\n+        while self.at(TokenKind.Star) || self.at(TokenKind.Slash) || self.at(TokenKind.Percent) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            let right = try self.parse_unary()\\n+            self.expr_space()\\n+            try self.require_type(left.ty, ValueType.I32, &t)\\n+            try self.require_type(right.ty, ValueType.I32, &t)\\n+            let op = if t.kind == TokenKind.Star { Op.Mul } else if t.kind == TokenKind.Slash { Op.Div } else { Op.Rem }\\n+            left = try self.binary(left, right, op, ValueType.I32, &t)\\n+        }\\n+        left\\n+    }\\n+\\n+    func parse_add(&var self) -> Result<ExprValue, ProtoError> {\\n+        var left = try self.parse_mul()\\n+        self.expr_space()\\n+        while self.at(TokenKind.Plus) || self.at(TokenKind.Minus) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            let right = try self.parse_mul()\\n+            self.expr_space()\\n+            try self.require_type(left.ty, ValueType.I32, &t)\\n+            try self.require_type(right.ty, ValueType.I32, &t)\\n+            left = try self.binary(left, right, if t.kind == TokenKind.Plus { Op.Add } else { Op.Sub }, ValueType.I32, &t)\\n+        }\\n+        left\\n+    }\\n+\\n+    func parse_compare(&var self) -> Result<ExprValue, ProtoError> {\\n+        var left = try self.parse_add()\\n+        self.expr_space()\\n+        if self.at(TokenKind.Lt) || self.at(TokenKind.Lte) || self.at(TokenKind.Gt) || self.at(TokenKind.Gte) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            let right = try self.parse_add()\\n+            self.expr_space()\\n+            try self.require_type(left.ty, ValueType.I32, &t)\\n+            try self.require_type(right.ty, ValueType.I32, &t)\\n+            let op = if t.kind == TokenKind.Lt { Op.Lt } else if t.kind == TokenKind.Lte { Op.Le } else if t.kind == TokenKind.Gt { Op.Gt } else { Op.Ge }\\n+            left = try self.binary(left, right, op, ValueType.Bool, &t)\\n+        }\\n+        left\\n+    }\\n+\\n+    func parse_expr(&var self) -> Result<ExprValue, ProtoError> {\\n+        self.expr_space()\\n+        var left = try self.parse_compare()\\n+        self.expr_space()\\n+        if self.at(TokenKind.Eq) || self.at(TokenKind.Neq) {\\n+            let t = self.take()\\n+            self.expr_space()\\n+            let right = try self.parse_compare()\\n+            self.expr_space()\\n+            if left.ty == ValueType.Void || left.ty != right.ty { return problem(\\\"equality operands must have the same scalar type\\\", t.line, t.col) }\\n+            left = try self.binary(left, right, if t.kind == TokenKind.Eq { Op.Eq } else { Op.Ne }, ValueType.Bool, &t)\\n+        }\\n+        left\\n+    }\\n+\\n+    func parse_block(&var self) -> Result<Bool, ProtoError> {\\n+        if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n+        self.depth += 1\\n+        self.skip_newlines()\\n+        var returns = false\\n+        while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n+            let r = try self.parse_statement()\\n+            if not returns && r { returns = true }\\n+            try self.terminator()\\n+        }\\n+        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace\\\")\\n+        self.leave_scope()\\n+        self.depth -= 1\\n+        returns\\n+    }\\n+\\n+    func parse_statement(&var self) -> Result<Bool, ProtoError> {\\n+        if self.at(TokenKind.Semicolon) { return self.fail(\\\"semicolons are not supported\\\") }\\n+        if self.at(TokenKind.Let) || self.at(TokenKind.Var) {\\n+            let kw = self.take()\\n+            let name = try self.expect(TokenKind.Ident, \\\"expected local name\\\")\\n+            var annotated: ValueType? = None\\n+            if self.at(TokenKind.Colon) {\\n+                let _ = self.take()\\n+                annotated = Some(try self.parse_type())\\n+            }\\n+            let _ = try self.expect(TokenKind.Assign, \\\"local declarations require an initializer\\\")\\n+            let value = try self.parse_expr()\\n+            if value.ty == ValueType.Void { return problem(\\\"cannot initialize a local with Void\\\", name.line, name.col) }\\n+            if let ty = annotated { try self.require_type(value.ty, ty, &name) }\\n+            let dst = self.new_slot(value.ty)\\n+            let at = self.emit_at(Op.Copy, name.line, name.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = value.slot\\n+            try self.declare(name, value.ty, dst, kw.kind == TokenKind.Var)\\n+            return false\\n+        }\\n+        if self.at(TokenKind.Return) {\\n+            let t = self.take()\\n+            var src = -1\\n+            if not self.at(TokenKind.Newline) && not self.at(TokenKind.RBrace) {\\n+                let value = try self.parse_expr()\\n+                try self.require_type(value.ty, self.current_result, &t)\\n+                src = value.slot\\n+            } else if self.current_result != ValueType.Void { return problem(\\\"non-Void return requires a value\\\", t.line, t.col) }\\n+            let at = self.emit_at(Op.Return, t.line, t.col)\\n+            self.code[at].a = src\\n+            return true\\n+        }\\n+        if self.at(TokenKind.If) {\\n+            let t = self.take()\\n+            let cond = try self.parse_expr()\\n+            try self.require_type(cond.ty, ValueType.Bool, &t)\\n+            let _ = try self.expect(TokenKind.LBrace, \\\"expected block after if condition\\\")\\n+            let branch = self.emit_at(Op.Branch, t.line, t.col)\\n+            self.code[branch].a = cond.slot\\n+            let skip_then = self.emit_at(Op.Jump, t.line, t.col)\\n+            self.code[branch].target = self.code.len()\\n+            let then_returns = try self.parse_block()\\n+            let skip_else = self.emit_at(Op.Jump, t.line, t.col)\\n+            self.code[skip_then].target = self.code.len()\\n+            var else_returns = false\\n+            let before_else = self.pos\\n+            self.skip_newlines()\\n+            if self.at(TokenKind.Else) {\\n+                let _ = self.take()\\n+                let _ = try self.expect(TokenKind.LBrace, \\\"expected block after else\\\")\\n+                else_returns = try self.parse_block()\\n+            } else {\\n+                self.pos = before_else\\n+            }\\n+            self.code[skip_else].target = self.code.len()\\n+            return then_returns && else_returns\\n+        }\\n+        if self.at(TokenKind.While) {\\n+            let t = self.take()\\n+            let cond_start = self.code.len()\\n+            let cond = try self.parse_expr()\\n+            try self.require_type(cond.ty, ValueType.Bool, &t)\\n+            let _ = try self.expect(TokenKind.LBrace, \\\"expected block after while condition\\\")\\n+            let branch = self.emit_at(Op.Branch, t.line, t.col)\\n+            self.code[branch].a = cond.slot\\n+            let exit = self.emit_at(Op.Jump, t.line, t.col)\\n+            self.code[branch].target = self.code.len()\\n+            let _ = try self.parse_block()\\n+            let back = self.emit_at(Op.Jump, t.line, t.col)\\n+            self.code[back].target = cond_start\\n+            self.code[exit].target = self.code.len()\\n+            return false\\n+        }\\n+        if self.at(TokenKind.LBrace) {\\n+            let _ = self.take()\\n+            return self.parse_block()\\n+        }\\n+        if self.at(TokenKind.Ident) && self.token().value == \\\"print\\\" {\\n+            let t = self.take()\\n+            let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after print\\\")\\n+            self.depth += 1\\n+            self.paren_depth += 1\\n+            self.skip_newlines()\\n+            let value = try self.parse_expr()\\n+            self.skip_newlines()\\n+            let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after print argument\\\")\\n+            self.paren_depth -= 1\\n+            self.depth -= 1\\n+            if value.ty == ValueType.Void { return problem(\\\"cannot print Void\\\", t.line, t.col) }\\n+            let at = self.emit_at(Op.Print, t.line, t.col)\\n+            self.code[at].a = value.slot\\n+            return false\\n+        }\\n+        if self.at(TokenKind.Ident) && self.pos + 1 < self.tokens.len() && self.tokens[self.pos + 1].kind == TokenKind.Assign {\\n+            let name = self.take()\\n+            let _ = self.take()\\n+            let bi = self.binding_index(name.value)\\n+            if bi < 0 { return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col) }\\n+            if not self.bindings[bi].mutable { return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col) }\\n+            let value = try self.parse_expr()\\n+            try self.require_type(value.ty, self.bindings[bi].ty, &name)\\n+            let at = self.emit_at(Op.Copy, name.line, name.col)\\n+            self.code[at].dst = self.bindings[bi].slot\\n+            self.code[at].a = value.slot\\n+            return false\\n+        }\\n+        let start = self.token()\\n+        let value = try self.parse_expr()\\n+        if not value.call || value.ty != ValueType.Void {\\n+            return problem(\\\"only Void function calls may be expression statements\\\", start.line, start.col)\\n+        }\\n+        false\\n+    }\\n+\\n+    func compile_function(&var self, index: Int) -> Result<FuncIR, ProtoError> {\\n+        self.pos = self.signatures[index].body_start\\n+        self.depth = 0\\n+        self.expr_depth = 0\\n+        self.paren_depth = 0\\n+        self.bindings = Vector<Binding>()\\n+        self.slots = Vector<ValueType>()\\n+        self.code = Vector<Instruction>()\\n+        self.current_result = self.signatures[index].result\\n+        var i = 0\\n+        while i < self.signatures[index].params.len() {\\n+            let slot = self.new_slot(self.signatures[index].params[i])\\n+            let fake = Token(kind: TokenKind.Ident, value: self.signatures[index].param_names[i],\\n+                             line: self.signatures[index].line, col: self.signatures[index].col, suffix: None)\\n+            try self.declare(fake, self.signatures[index].params[i], slot, false)\\n+            i += 1\\n+        }\\n+        self.skip_newlines()\\n+        var returns = false\\n+        while self.pos < self.signatures[index].body_end {\\n+            let r = try self.parse_statement()\\n+            if not returns && r { returns = true }\\n+            try self.terminator()\\n+        }\\n+        if self.pos != self.signatures[index].body_end { return self.fail(\\\"malformed function body\\\") }\\n+        if self.current_result != ValueType.Void && not returns {\\n+            return problem(\\\"function `{self.signatures[index].name}` may reach its end without returning a value\\\",\\n+                           self.signatures[index].line, self.signatures[index].col)\\n+        }\\n+        if self.current_result == ValueType.Void {\\n+            let _ = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n+        } else {\\n+            // Return-path checking above proves this padding unreachable. It gives\\n+            // end-of-block jumps a real target and leaves every emitted block terminated.\\n+            let slot = self.new_slot(self.current_result)\\n+            let c = self.emit_at(Op.Const, self.signatures[index].line, self.signatures[index].col)\\n+            self.code[c].dst = slot\\n+            self.code[c].immediate = 0\\n+            let r = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n+            self.code[r].a = slot\\n+        }\\n+        let slots = self.slots.copy()\\n+        let code = self.code.copy()\\n+        FuncIR(name: self.signatures[index].name, params: self.signatures[index].params.copy(),\\n+               result: self.current_result, slots: move slots, code: move code)\\n+    }\\n+}\\n+\\n+public func compile_source(source: String) -> Result<Program, ProtoError> {\\n+    let tokens = match lex(source) {\\n+        case Ok(ts) -> move ts,\\n+        case Err(e) -> { return problem(e.message, e.line, e.col) }\\n+    }\\n+    var compiler = Compiler(tokens: move tokens, signatures: Vector<Signature>(), pos: 0, depth: 0,\\n+                            expr_depth: 0, paren_depth: 0,\\n+                            bindings: Vector<Binding>(), slots: Vector<ValueType>(),\\n+                            code: Vector<Instruction>(), current_result: ValueType.Void)\\n+    try compiler.scan_signatures()\\n+    var functions = Vector<FuncIR>()\\n+    var main_index = -1\\n+    var i = 0\\n+    while i < compiler.signatures.len() {\\n+        if compiler.signatures[i].name == \\\"main\\\" { main_index = i }\\n+        let f = try compiler.compile_function(i)\\n+        try! functions.push(move f)\\n+        i += 1\\n+    }\\n+    Program(functions: move functions, main_index: main_index)\\n+}\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nnew file mode 100644\\nindex 00000000..60d21ef7\\n--- /dev/null\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -0,0 +1,370 @@\\n+import src.model.*\\n+\\n+func put(out: &var StringBuilder, text: String) {\\n+    try! out.append(text)\\n+}\\n+\\n+func put_int(out: &var StringBuilder, value: Int) {\\n+    try! out.append(value)\\n+}\\n+\\n+func slot_name(out: &var StringBuilder, index: Int) {\\n+    put(&var out, \\\"%s\\\")\\n+    put_int(&var out, index)\\n+}\\n+\\n+func temp_name(out: &var StringBuilder, instruction_index: Int, suffix: String) {\\n+    put(&var out, \\\"%t\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, suffix)\\n+}\\n+\\n+func block_name(out: &var StringBuilder, instruction_index: Int) {\\n+    put(&var out, \\\"i\\\")\\n+    put_int(&var out, instruction_index)\\n+}\\n+\\n+func next_block(out: &var StringBuilder, instruction_index: Int) {\\n+    put(&var out, \\\"  br label %i\\\")\\n+    put_int(&var out, instruction_index + 1)\\n+    put(&var out, \\\"\\\\n\\\")\\n+}\\n+\\n+func load_slot(out: &var StringBuilder, instruction_index: Int,\\n+               suffix: String, slot: Int) {\\n+    temp_name(&var out, instruction_index, suffix)\\n+    put(&var out, \\\" = load i64, ptr \\\")\\n+    slot_name(&var out, slot)\\n+    put(&var out, \\\"\\\\n\\\")\\n+}\\n+\\n+func store_temp(out: &var StringBuilder, instruction_index: Int,\\n+                suffix: String, slot: Int) {\\n+    put(&var out, \\\"  store i64 \\\")\\n+    temp_name(&var out, instruction_index, suffix)\\n+    put(&var out, \\\", ptr \\\")\\n+    slot_name(&var out, slot)\\n+    put(&var out, \\\"\\\\n\\\")\\n+}\\n+\\n+func emit_checked_result(out: &var StringBuilder, instruction_index: Int,\\n+                         dst: Int, value_suffix: String) {\\n+    put(&var out, \\\"  \\\")\\n+    temp_name(&var out, instruction_index, \\\"lo\\\")\\n+    put(&var out, \\\" = icmp slt i64 \\\")\\n+    temp_name(&var out, instruction_index, value_suffix)\\n+    put(&var out, \\\", -2147483648\\\\n  \\\")\\n+    temp_name(&var out, instruction_index, \\\"hi\\\")\\n+    put(&var out, \\\" = icmp sgt i64 \\\")\\n+    temp_name(&var out, instruction_index, value_suffix)\\n+    put(&var out, \\\", 2147483647\\\\n  \\\")\\n+    temp_name(&var out, instruction_index, \\\"bad\\\")\\n+    put(&var out, \\\" = or i1 \\\")\\n+    temp_name(&var out, instruction_index, \\\"lo\\\")\\n+    put(&var out, \\\", \\\")\\n+    temp_name(&var out, instruction_index, \\\"hi\\\")\\n+    put(&var out, \\\"\\\\n  br i1 \\\")\\n+    temp_name(&var out, instruction_index, \\\"bad\\\")\\n+    put(&var out, \\\", label %i\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".overflow, label %i\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".ok\\\\n\\\\ni\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".overflow:\\\\n  call void @minivm_overflow()\\\\n  unreachable\\\\n\\\\ni\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".ok:\\\\n\\\")\\n+    store_temp(&var out, instruction_index, value_suffix, dst)\\n+    next_block(&var out, instruction_index)\\n+}\\n+\\n+func emit_binary(out: &var StringBuilder, instruction_index: Int,\\n+                 inst: &Instruction, llvm_op: String, checked: Bool) {\\n+    put(&var out, \\\"  \\\")\\n+    load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+    put(&var out, \\\"  \\\")\\n+    load_slot(&var out, instruction_index, \\\"b\\\", inst.b)\\n+    put(&var out, \\\"  \\\")\\n+    temp_name(&var out, instruction_index, \\\"v\\\")\\n+    put(&var out, \\\" = \\\")\\n+    put(&var out, llvm_op)\\n+    put(&var out, \\\" i64 \\\")\\n+    temp_name(&var out, instruction_index, \\\"a\\\")\\n+    put(&var out, \\\", \\\")\\n+    temp_name(&var out, instruction_index, \\\"b\\\")\\n+    put(&var out, \\\"\\\\n\\\")\\n+    if checked {\\n+        emit_checked_result(&var out, instruction_index, inst.dst, \\\"v\\\")\\n+    } else {\\n+        store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+        next_block(&var out, instruction_index)\\n+    }\\n+}\\n+\\n+func emit_compare(out: &var StringBuilder, instruction_index: Int,\\n+                  inst: &Instruction, predicate: String) {\\n+    put(&var out, \\\"  \\\")\\n+    load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+    put(&var out, \\\"  \\\")\\n+    load_slot(&var out, instruction_index, \\\"b\\\", inst.b)\\n+    put(&var out, \\\"  \\\")\\n+    temp_name(&var out, instruction_index, \\\"c\\\")\\n+    put(&var out, \\\" = icmp \\\")\\n+    put(&var out, predicate)\\n+    put(&var out, \\\" i64 \\\")\\n+    temp_name(&var out, instruction_index, \\\"a\\\")\\n+    put(&var out, \\\", \\\")\\n+    temp_name(&var out, instruction_index, \\\"b\\\")\\n+    put(&var out, \\\"\\\\n  \\\")\\n+    temp_name(&var out, instruction_index, \\\"v\\\")\\n+    put(&var out, \\\" = zext i1 \\\")\\n+    temp_name(&var out, instruction_index, \\\"c\\\")\\n+    put(&var out, \\\" to i64\\\\n\\\")\\n+    store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+    next_block(&var out, instruction_index)\\n+}\\n+\\n+func emit_division(out: &var StringBuilder, instruction_index: Int,\\n+                   inst: &Instruction, remainder: Bool) {\\n+    put(&var out, \\\"  \\\")\\n+    load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+    put(&var out, \\\"  \\\")\\n+    load_slot(&var out, instruction_index, \\\"b\\\", inst.b)\\n+    put(&var out, \\\"  \\\")\\n+    temp_name(&var out, instruction_index, \\\"zero\\\")\\n+    put(&var out, \\\" = icmp eq i64 \\\")\\n+    temp_name(&var out, instruction_index, \\\"b\\\")\\n+    put(&var out, \\\", 0\\\\n  br i1 \\\")\\n+    temp_name(&var out, instruction_index, \\\"zero\\\")\\n+    put(&var out, \\\", label %i\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".divzero, label %i\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".divide\\\\n\\\\ni\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".divzero:\\\\n  call void @minivm_divzero()\\\\n  unreachable\\\\n\\\\ni\\\")\\n+    put_int(&var out, instruction_index)\\n+    put(&var out, \\\".divide:\\\\n  \\\")\\n+    temp_name(&var out, instruction_index, \\\"v\\\")\\n+    put(&var out, if remainder { \\\" = srem i64 \\\" } else { \\\" = sdiv i64 \\\" })\\n+    temp_name(&var out, instruction_index, \\\"a\\\")\\n+    put(&var out, \\\", \\\")\\n+    temp_name(&var out, instruction_index, \\\"b\\\")\\n+    put(&var out, \\\"\\\\n\\\")\\n+    if remainder {\\n+        store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+        next_block(&var out, instruction_index)\\n+    } else {\\n+        emit_checked_result(&var out, instruction_index, inst.dst, \\\"v\\\")\\n+    }\\n+}\\n+\\n+func emit_instruction(out: &var StringBuilder, program: &Program,\\n+                      function: &FuncIR, instruction_index: Int,\\n+                      inst: &Instruction) {\\n+    block_name(&var out, instruction_index)\\n+    put(&var out, \\\":\\\\n\\\")\\n+    match inst.op {\\n+        case Const -> {\\n+            put(&var out, \\\"  store i64 \\\")\\n+            put_int(&var out, inst.immediate)\\n+            put(&var out, \\\", ptr \\\")\\n+            slot_name(&var out, inst.dst)\\n+            put(&var out, \\\"\\\\n\\\")\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case Copy -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"v\\\", inst.a)\\n+            store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case Add -> emit_binary(&var out, instruction_index, &inst, \\\"add\\\", true),\\n+        case Sub -> emit_binary(&var out, instruction_index, &inst, \\\"sub\\\", true),\\n+        case Mul -> emit_binary(&var out, instruction_index, &inst, \\\"mul\\\", true),\\n+        case Div -> emit_division(&var out, instruction_index, &inst, false),\\n+        case Rem -> emit_division(&var out, instruction_index, &inst, true),\\n+        case Neg -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"v\\\")\\n+            put(&var out, \\\" = sub i64 0, \\\")\\n+            temp_name(&var out, instruction_index, \\\"a\\\")\\n+            put(&var out, \\\"\\\\n\\\")\\n+            emit_checked_result(&var out, instruction_index, inst.dst, \\\"v\\\")\\n+        },\\n+        case Not -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"c\\\")\\n+            put(&var out, \\\" = icmp eq i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"a\\\")\\n+            put(&var out, \\\", 0\\\\n  \\\")\\n+            temp_name(&var out, instruction_index, \\\"v\\\")\\n+            put(&var out, \\\" = zext i1 \\\")\\n+            temp_name(&var out, instruction_index, \\\"c\\\")\\n+            put(&var out, \\\" to i64\\\\n\\\")\\n+            store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case Eq -> emit_compare(&var out, instruction_index, &inst, \\\"eq\\\"),\\n+        case Ne -> emit_compare(&var out, instruction_index, &inst, \\\"ne\\\"),\\n+        case Lt -> emit_compare(&var out, instruction_index, &inst, \\\"slt\\\"),\\n+        case Le -> emit_compare(&var out, instruction_index, &inst, \\\"sle\\\"),\\n+        case Gt -> emit_compare(&var out, instruction_index, &inst, \\\"sgt\\\"),\\n+        case Ge -> emit_compare(&var out, instruction_index, &inst, \\\"sge\\\"),\\n+        case Jump -> {\\n+            put(&var out, \\\"  br label %i\\\")\\n+            put_int(&var out, inst.target)\\n+            put(&var out, \\\"\\\\n\\\")\\n+        },\\n+        case Branch -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"c\\\")\\n+            put(&var out, \\\" = icmp ne i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"a\\\")\\n+            put(&var out, \\\", 0\\\\n  br i1 \\\")\\n+            temp_name(&var out, instruction_index, \\\"c\\\")\\n+            put(&var out, \\\", label %i\\\")\\n+            put_int(&var out, inst.target)\\n+            put(&var out, \\\", label %i\\\")\\n+            put_int(&var out, instruction_index + 1)\\n+            put(&var out, \\\"\\\\n\\\")\\n+        },\\n+        case Call -> {\\n+            var argument_index = 0\\n+            while argument_index < inst.args.len() {\\n+                put(&var out, \\\"  \\\")\\n+                load_slot(&var out, instruction_index, \\\"arg{argument_index}\\\", inst.args[argument_index])\\n+                argument_index = argument_index + 1\\n+            }\\n+            put(&var out, \\\"  \\\")\\n+            if inst.dst >= 0 {\\n+                temp_name(&var out, instruction_index, \\\"call\\\")\\n+                put(&var out, \\\" = \\\")\\n+            }\\n+            put(&var out, \\\"call \\\")\\n+                        put(&var out, if program.functions[inst.target].result == ValueType.Void { \\\"void\\\" } else { \\\"i64\\\" })\\n+            put(&var out, \\\" @f\\\")\\n+            put_int(&var out, inst.target)\\n+            put(&var out, \\\"(\\\")\\n+            argument_index = 0\\n+            while argument_index < inst.args.len() {\\n+                if argument_index > 0 { put(&var out, \\\", \\\") }\\n+                put(&var out, \\\"i64 \\\")\\n+                temp_name(&var out, instruction_index, \\\"arg{argument_index}\\\")\\n+                argument_index = argument_index + 1\\n+            }\\n+            put(&var out, \\\")\\\\n\\\")\\n+            if inst.dst >= 0 { store_temp(&var out, instruction_index, \\\"call\\\", inst.dst) }\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case Print -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+            if function.slots[inst.a] == ValueType.Bool {\\n+                put(&var out, \\\"  \\\")\\n+                temp_name(&var out, instruction_index, \\\"c\\\")\\n+                put(&var out, \\\" = icmp ne i64 \\\")\\n+                temp_name(&var out, instruction_index, \\\"a\\\")\\n+                put(&var out, \\\", 0\\\\n  \\\")\\n+                temp_name(&var out, instruction_index, \\\"str\\\")\\n+                put(&var out, \\\" = select i1 \\\")\\n+                temp_name(&var out, instruction_index, \\\"c\\\")\\n+                put(&var out, \\\", ptr @.true, ptr @.false\\\\n  call i32 @puts(ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"str\\\")\\n+                put(&var out, \\\")\\\\n\\\")\\n+            } else {\\n+                put(&var out, \\\"  call i32 (ptr, ...) @printf(ptr @.fmt_i64, i64 \\\")\\n+                temp_name(&var out, instruction_index, \\\"a\\\")\\n+                put(&var out, \\\")\\\\n\\\")\\n+            }\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case Return -> {\\n+            if inst.a < 0 {\\n+                put(&var out, \\\"  ret void\\\\n\\\")\\n+            } else {\\n+                put(&var out, \\\"  \\\")\\n+                load_slot(&var out, instruction_index, \\\"ret\\\", inst.a)\\n+                put(&var out, \\\"  ret i64 \\\")\\n+                temp_name(&var out, instruction_index, \\\"ret\\\")\\n+                put(&var out, \\\"\\\\n\\\")\\n+            }\\n+        }\\n+    }\\n+    put(&var out, \\\"\\\\n\\\")\\n+}\\n+\\n+func emit_function(out: &var StringBuilder, program: &Program,\\n+                   function_index: Int, function: &FuncIR) {\\n+    put(&var out, \\\"define \\\")\\n+    put(&var out, if function.result == ValueType.Void { \\\"void\\\" } else { \\\"i64\\\" })\\n+    put(&var out, \\\" @f\\\")\\n+    put_int(&var out, function_index)\\n+    put(&var out, \\\"(\\\")\\n+    var parameter_index = 0\\n+    while parameter_index < function.params.len() {\\n+        if parameter_index > 0 { put(&var out, \\\", \\\") }\\n+        put(&var out, \\\"i64 %arg\\\")\\n+        put_int(&var out, parameter_index)\\n+        parameter_index = parameter_index + 1\\n+    }\\n+    put(&var out, \\\") \\\\{\\\\nentry:\\\\n\\\")\\n+    var slot_index = 0\\n+    while slot_index < function.slots.len() {\\n+        put(&var out, \\\"  \\\")\\n+        slot_name(&var out, slot_index)\\n+        put(&var out, \\\" = alloca i64\\\\n\\\")\\n+        slot_index = slot_index + 1\\n+    }\\n+    parameter_index = 0\\n+    while parameter_index < function.params.len() {\\n+        put(&var out, \\\"  store i64 %arg\\\")\\n+        put_int(&var out, parameter_index)\\n+        put(&var out, \\\", ptr \\\")\\n+        slot_name(&var out, parameter_index)\\n+        put(&var out, \\\"\\\\n\\\")\\n+        parameter_index = parameter_index + 1\\n+    }\\n+    put(&var out, \\\"  br label %i0\\\\n\\\\n\\\")\\n+    var instruction_index = 0\\n+    while instruction_index < function.code.len() {\\n+        emit_instruction(&var out, &program, &function, instruction_index,\\n+                         &function.code[instruction_index])\\n+        instruction_index = instruction_index + 1\\n+    }\\n+    // Keep the text structurally valid even when verification permits an\\n+    // unreachable final non-terminator. A reachable edge here is still a\\n+    // malformed Program and has no language-level behavior.\\n+    block_name(&var out, function.code.len())\\n+    put(&var out, \\\":\\\\n  unreachable\\\\n\\\")\\n+    put(&var out, \\\"\\\\}\\\\n\\\\n\\\")\\n+}\\n+\\n+public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n+    var out = StringBuilder()\\n+    put(&var out, \\\"; minivm textual LLVM IR\\\\n\\\")\\n+    put(&var out, \\\"@.fmt_i64 = private unnamed_addr constant [5 x i8] c\\\\\\\"%ld\\\\\\\\0A\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    put(&var out, \\\"@.true = private unnamed_addr constant [5 x i8] c\\\\\\\"true\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    put(&var out, \\\"@.false = private unnamed_addr constant [6 x i8] c\\\\\\\"false\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    put(&var out, \\\"@.overflow = private unnamed_addr constant [32 x i8] c\\\\\\\"runtime error: integer overflow\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    put(&var out, \\\"@.divzero = private unnamed_addr constant [32 x i8] c\\\\\\\"runtime error: division by zero\\\\\\\\00\\\\\\\"\\\\n\\\\n\\\")\\n+    put(&var out, \\\"declare i32 @printf(ptr, ...)\\\\ndeclare i32 @puts(ptr)\\\\ndeclare void @exit(i32)\\\\n\\\\n\\\")\\n+    put(&var out, \\\"define internal void @minivm_overflow() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.overflow)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n+    put(&var out, \\\"define internal void @minivm_divzero() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.divzero)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n+    var function_index = 0\\n+    while function_index < program.functions.len() {\\n+        emit_function(&var out, &program, function_index,\\n+                      &program.functions[function_index])\\n+        function_index = function_index + 1\\n+    }\\n+    put(&var out, \\\"define i32 @main() \\\\{\\\\nentry:\\\\n  call void @f\\\")\\n+    put_int(&var out, program.main_index)\\n+    put(&var out, \\\"()\\\\n  ret i32 0\\\\n\\\\}\\\\n\\\")\\n+    out.build()\\n+}\\ndiff --git a/prototypes/minivm/src/main.saw b/prototypes/minivm/src/main.saw\\nnew file mode 100644\\nindex 00000000..0da7b9e0\\n--- /dev/null\\n+++ b/prototypes/minivm/src/main.saw\\n@@ -0,0 +1,70 @@\\n+import src.model.*\\n+import src.frontend\\n+import src.vm\\n+import src.llvm\\n+import src.verify\\n+import std.env.{Env}\\n+import std.file.{File}\\n+import std.path.{Path}\\n+\\n+func read_source(path: String) -> Result<String, ProtoError> {\\n+    var file = try File.open(Path(s: path)) catch {\\n+        return problem(\\\"cannot open {path}: {error}\\\", 0, 0)\\n+    }\\n+    let data = try file.read() catch {\\n+        return problem(\\\"cannot read {path}: {error}\\\", 0, 0)\\n+    }\\n+    let source = try data.to_string() catch {\\n+        return problem(\\\"invalid UTF-8 in {path}\\\", 0, 0)\\n+    }\\n+    return source\\n+}\\n+\\n+func report(path: String, e: &ProtoError) -> Int {\\n+    if e.line > 0 {\\n+        print(\\\"{path}:{e.line}:{e.col}: {e.message}\\\")\\n+    } else {\\n+        print(e.message)\\n+    }\\n+    return 1\\n+}\\n+\\n+func main() -> Int {\\n+    let args = try! Env.args()\\n+    if args.len() != 3 && args.len() != 5 {\\n+        print(\\\"usage: minivm run FILE [--budget N] | minivm emit-llvm FILE\\\")\\n+        return 1\\n+    }\\n+    let command = args[1]\\n+    let path = args[2]\\n+    if command != \\\"run\\\" && command != \\\"emit-llvm\\\" {\\n+        print(\\\"unknown command: {command}\\\")\\n+        return 1\\n+    }\\n+    var budget = 1_000_000\\n+    if args.len() == 5 {\\n+        if command != \\\"run\\\" || args[3] != \\\"--budget\\\" {\\n+            print(\\\"--budget is only supported by run\\\")\\n+            return 1\\n+        }\\n+        guard let value = args[4].to_int() else {\\n+            print(\\\"budget must be a positive integer\\\")\\n+            return 1\\n+        }\\n+        if value <= 0 {\\n+            print(\\\"budget must be a positive integer\\\")\\n+            return 1\\n+        }\\n+        budget = value\\n+    }\\n+    let source = try read_source(path) catch { return report(path, &error) }\\n+    let program = try frontend.compile_source(source) catch { return report(path, &error) }\\n+    try verify.verify(&program) catch { return report(path, &error) }\\n+    if command == \\\"run\\\" {\\n+        try vm.execute(&program, budget) catch { return report(path, &error) }\\n+    } else {\\n+        let text = try llvm.emit_llvm(&program) catch { return report(path, &error) }\\n+        print(text)\\n+    }\\n+    return 0\\n+}\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nnew file mode 100644\\nindex 00000000..7e04e437\\n--- /dev/null\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -0,0 +1,63 @@\\n+public enum ValueType { case I32, case Bool, case Void }\\n+@synthesize\\n+extension ValueType: Equatable {}\\n+\\n+public enum Op {\\n+    case Const, case Copy, case Add, case Sub, case Mul, case Div, case Rem,\\n+    case Neg, case Not, case Eq, case Ne, case Lt, case Le, case Gt, case Ge,\\n+    case Jump, case Branch, case Call, case Print, case Return\\n+}\\n+@synthesize\\n+extension Op: Equatable {}\\n+\\n+public struct Instruction {\\n+    public op: Op\\n+    public dst: Int\\n+    public a: Int\\n+    public b: Int\\n+    public immediate: Int\\n+    public target: Int\\n+    public args: Vector<Int>\\n+    public line: Int\\n+    public col: Int\\n+}\\n+\\n+public struct FuncIR {\\n+    public name: String\\n+    public params: Vector<ValueType>\\n+    public result: ValueType\\n+    public slots: Vector<ValueType>\\n+    public code: Vector<Instruction>\\n+}\\n+\\n+public struct Program {\\n+    public functions: Vector<FuncIR>\\n+    public main_index: Int\\n+}\\n+\\n+@synthesize\\n+extension Instruction: ExplicitCopy {}\\n+@synthesize\\n+extension FuncIR: ExplicitCopy {}\\n+@synthesize\\n+extension Program: ExplicitCopy {}\\n+\\n+public struct ProtoError {\\n+    public message: String\\n+    public line: Int\\n+    public col: Int\\n+}\\n+extension ProtoError: Error {\\n+    public func format(&self, into: &var StringBuilder) {\\n+        try! into.append(self.message)\\n+    }\\n+}\\n+\\n+public func instruction(op: Op) -> Instruction {\\n+    Instruction(op: op, dst: -1, a: -1, b: -1, immediate: 0,\\n+                target: -1, args: Vector<Int>(), line: 0, col: 0)\\n+}\\n+\\n+public func problem(message: String, line: Int, col: Int) -> ProtoError {\\n+    ProtoError(message: message, line: line, col: col)\\n+}\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nnew file mode 100644\\nindex 00000000..84a7587b\\n--- /dev/null\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -0,0 +1,127 @@\\n+import src.model.*\\n+\\n+func slot_type(f: &FuncIR, n: Int) -> ValueType {\\n+    if n < 0 || n >= f.slots.len() { return ValueType.Void }\\n+    return f.slots[n]\\n+}\\n+\\n+func invalid(i: &Instruction) -> ProtoError {\\n+    return problem(\\\"internal prototype error: invalid instruction\\\", i.line, i.col)\\n+}\\n+\\n+func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void, ProtoError> {\\n+    let dst = slot_type(&f, i.dst)\\n+    let a = slot_type(&f, i.a)\\n+    let b = slot_type(&f, i.b)\\n+    match i.op {\\n+        case Const -> {\\n+            if dst == ValueType.Void { return invalid(&i) }\\n+            if dst == ValueType.Bool && i.immediate != 0 && i.immediate != 1 { return invalid(&i) }\\n+            if dst == ValueType.I32 && (i.immediate < -2_147_483_648 || i.immediate > 2_147_483_647) { return invalid(&i) }\\n+        },\\n+        case Copy -> {\\n+            if dst == ValueType.Void || a != dst { return invalid(&i) }\\n+        },\\n+        case Add -> {\\n+            if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) }\\n+        },\\n+        case Sub -> { if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n+        case Mul -> { if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n+        case Div -> { if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n+        case Rem -> { if dst != ValueType.I32 || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n+        case Neg -> {\\n+            if dst != ValueType.I32 || a != ValueType.I32 { return invalid(&i) }\\n+        },\\n+        case Not -> {\\n+            if dst != ValueType.Bool || a != ValueType.Bool { return invalid(&i) }\\n+        },\\n+        case Eq -> {\\n+            if dst != ValueType.Bool || a == ValueType.Void || a != b { return invalid(&i) }\\n+        },\\n+        case Ne -> { if dst != ValueType.Bool || a == ValueType.Void || a != b { return invalid(&i) } },\\n+        case Lt -> {\\n+            if dst != ValueType.Bool || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) }\\n+        },\\n+        case Le -> { if dst != ValueType.Bool || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n+        case Gt -> { if dst != ValueType.Bool || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n+        case Ge -> { if dst != ValueType.Bool || a != ValueType.I32 || b != ValueType.I32 { return invalid(&i) } },\\n+        case Jump -> {\\n+            if i.target < 0 || i.target >= f.code.len() { return invalid(&i) }\\n+        },\\n+        case Branch -> { if i.target < 0 || i.target >= f.code.len() || a != ValueType.Bool { return invalid(&i) } },\\n+        case Call -> {\\n+            if i.target < 0 || i.target >= p.functions.len() { return invalid(&i) }\\n+            let callee = p.functions[i.target].copy()\\n+            if i.args.len() != callee.params.len() { return invalid(&i) }\\n+            if callee.result == ValueType.Void {\\n+                if i.dst != -1 { return invalid(&i) }\\n+            } else if dst != callee.result { return invalid(&i) }\\n+            var n = 0\\n+            while n < i.args.len() {\\n+                if slot_type(&f, i.args[n]) != callee.params[n] { return invalid(&i) }\\n+                n = n + 1\\n+            }\\n+        },\\n+        case Print -> {\\n+            if a == ValueType.Void { return invalid(&i) }\\n+        },\\n+        case Return -> {\\n+            if f.result == ValueType.Void {\\n+                if i.a != -1 { return invalid(&i) }\\n+            } else if a != f.result { return invalid(&i) }\\n+        }\\n+    }\\n+    return\\n+}\\n+\\n+func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n+    if f.code.len() == 0 || f.slots.len() < f.params.len() {\\n+        return problem(\\\"internal prototype error: invalid function\\\", 0, 0)\\n+    }\\n+    var n = 0\\n+    while n < f.slots.len() {\\n+        if f.slots[n] == ValueType.Void { return problem(\\\"internal prototype error: Void slot\\\", 0, 0) }\\n+        if n < f.params.len() && f.slots[n] != f.params[n] {\\n+            return problem(\\\"internal prototype error: parameter slot mismatch\\\", 0, 0)\\n+        }\\n+        n = n + 1\\n+    }\\n+    n = 0\\n+    while n < f.code.len() {\\n+        try check_instruction(&p, &f, &f.code[n]) catch { return error }\\n+        n = n + 1\\n+    }\\n+    var visited = Vector<Bool>()\\n+    n = 0\\n+    while n < f.code.len() { try! visited.push(false) n = n + 1 }\\n+    var pending = Vector<Int>()\\n+    try! pending.push(0)\\n+    while let pc = pending.pop() {\\n+        if pc < 0 || pc >= f.code.len() {\\n+            return problem(\\\"internal prototype error: reachable function falloff\\\", 0, 0)\\n+        }\\n+        if not visited[pc] {\\n+            visited.set(pc, true)\\n+            let i = f.code[pc].copy()\\n+            if i.op == Op.Jump || i.op == Op.Branch { try! pending.push(i.target) }\\n+            if i.op != Op.Jump && i.op != Op.Return { try! pending.push(pc + 1) }\\n+        }\\n+    }\\n+    return\\n+}\\n+\\n+public func verify(program: &Program) -> Result<Void, ProtoError> {\\n+    if program.main_index < 0 || program.main_index >= program.functions.len() {\\n+        return problem(\\\"internal prototype error: missing main\\\", 0, 0)\\n+    }\\n+    let entry = program.functions[program.main_index].copy()\\n+    if entry.result != ValueType.Void || entry.params.len() != 0 {\\n+        return problem(\\\"main must take no parameters and return Void\\\", 0, 0)\\n+    }\\n+    var n = 0\\n+    while n < program.functions.len() {\\n+        try check_function(&program, &program.functions[n]) catch { return error }\\n+        n = n + 1\\n+    }\\n+    return\\n+}\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nnew file mode 100644\\nindex 00000000..db2949cd\\n--- /dev/null\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -0,0 +1,134 @@\\n+import src.model.*\\n+\\n+static I32_MIN: Int = -2_147_483_648\\n+static I32_MAX: Int = 2_147_483_647\\n+static MAX_CALL_DEPTH: Int = 128\\n+\\n+struct CallResult {\\n+    value: Int\\n+    has_value: Bool\\n+}\\n+\\n+func runtime_error(message: String) -> ProtoError {\\n+    problem(message, 0, 0)\\n+}\\n+\\n+func checked_i32(value: Int) -> Result<Int, ProtoError> {\\n+    if value < I32_MIN || value > I32_MAX {\\n+        return runtime_error(\\\"runtime error: integer overflow\\\")\\n+    }\\n+    value\\n+}\\n+\\n+func run_function(program: &Program, function_index: Int, arguments: &Vector<Int>,\\n+                  remaining: &var Int, depth: Int) -> Result<CallResult, ProtoError> {\\n+    if depth > MAX_CALL_DEPTH {\\n+        return runtime_error(\\\"runtime error: call depth exceeded\\\")\\n+    }\\n+\\n+    let function = program.functions[function_index].copy()\\n+    var slots = Vector<Int>()\\n+    var slot_index = 0\\n+    while slot_index < function.slots.len() {\\n+        try! slots.push(0)\\n+        slot_index = slot_index + 1\\n+    }\\n+    var argument_index = 0\\n+    while argument_index < arguments.len() {\\n+        slots[argument_index] = arguments[argument_index]\\n+        argument_index = argument_index + 1\\n+    }\\n+\\n+    var pc = 0\\n+    while pc < function.code.len() {\\n+        if remaining <= 0 {\\n+            return runtime_error(\\\"runtime error: instruction budget exceeded\\\")\\n+        }\\n+        remaining = remaining - 1\\n+\\n+        let inst = function.code[pc].copy()\\n+        match inst.op {\\n+            case Const -> { slots[inst.dst] = inst.immediate },\\n+            case Copy -> { slots[inst.dst] = slots[inst.a] },\\n+            case Add -> { slots[inst.dst] = try checked_i32(slots[inst.a] + slots[inst.b]) },\\n+            case Sub -> { slots[inst.dst] = try checked_i32(slots[inst.a] - slots[inst.b]) },\\n+            case Mul -> { slots[inst.dst] = try checked_i32(slots[inst.a] * slots[inst.b]) },\\n+            case Div -> {\\n+                let lhs = slots[inst.a]\\n+                let rhs = slots[inst.b]\\n+                if rhs == 0 {\\n+                    return runtime_error(\\\"runtime error: division by zero\\\")\\n+                }\\n+                if lhs == I32_MIN && rhs == -1 {\\n+                    return runtime_error(\\\"runtime error: integer overflow\\\")\\n+                }\\n+                slots[inst.dst] = lhs / rhs\\n+            },\\n+            case Rem -> {\\n+                let lhs = slots[inst.a]\\n+                let rhs = slots[inst.b]\\n+                if rhs == 0 {\\n+                    return runtime_error(\\\"runtime error: division by zero\\\")\\n+                }\\n+                if lhs == I32_MIN && rhs == -1 {\\n+                    slots[inst.dst] = 0\\n+                } else {\\n+                    slots[inst.dst] = lhs % rhs\\n+                }\\n+            },\\n+            case Neg -> { slots[inst.dst] = try checked_i32(0 - slots[inst.a]) },\\n+            case Not -> { slots[inst.dst] = if slots[inst.a] == 0 { 1 } else { 0 } },\\n+            case Eq -> { slots[inst.dst] = if slots[inst.a] == slots[inst.b] { 1 } else { 0 } },\\n+            case Ne -> { slots[inst.dst] = if slots[inst.a] != slots[inst.b] { 1 } else { 0 } },\\n+            case Lt -> { slots[inst.dst] = if slots[inst.a] < slots[inst.b] { 1 } else { 0 } },\\n+            case Le -> { slots[inst.dst] = if slots[inst.a] <= slots[inst.b] { 1 } else { 0 } },\\n+            case Gt -> { slots[inst.dst] = if slots[inst.a] > slots[inst.b] { 1 } else { 0 } },\\n+            case Ge -> { slots[inst.dst] = if slots[inst.a] >= slots[inst.b] { 1 } else { 0 } },\\n+            case Jump -> {\\n+                pc = inst.target\\n+                continue\\n+            },\\n+            case Branch -> {\\n+                if slots[inst.a] != 0 {\\n+                    pc = inst.target\\n+                    continue\\n+                }\\n+            },\\n+            case Call -> {\\n+                var call_arguments = Vector<Int>()\\n+                var i = 0\\n+                while i < inst.args.len() {\\n+                    try! call_arguments.push(slots[inst.args[i]])\\n+                    i = i + 1\\n+                }\\n+                let result = try run_function(&program, inst.target, &call_arguments,\\n+                                              &var remaining, depth + 1)\\n+                if inst.dst >= 0 {\\n+                    slots[inst.dst] = result.value\\n+                }\\n+            },\\n+            case Print -> {\\n+                if function.slots[inst.a] == ValueType.Bool {\\n+                    if slots[inst.a] == 0 { print(\\\"false\\\") } else { print(\\\"true\\\") }\\n+                } else {\\n+                    print(slots[inst.a])\\n+                }\\n+            },\\n+            case Return -> {\\n+                if inst.a < 0 {\\n+                    return CallResult(value: 0, has_value: false)\\n+                }\\n+                return CallResult(value: slots[inst.a], has_value: true)\\n+            }\\n+        }\\n+        pc = pc + 1\\n+    }\\n+    runtime_error(\\\"runtime error: instruction budget exceeded\\\")\\n+}\\n+\\n+public func execute(program: &Program, budget: Int) -> Result<Void, ProtoError> {\\n+    var remaining = budget\\n+    let arguments = Vector<Int>()\\n+    let _ = try run_function(&program, program.main_index, &arguments, &var remaining, 1)\\n+    return\\n+}\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nnew file mode 100644\\nindex 00000000..d99b21c0\\n--- /dev/null\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -0,0 +1,197 @@\\n+#!/usr/bin/env python3\\n+\\\"\\\"\\\"End-to-end checks for the disposable minivm prototype.\\\"\\\"\\\"\\n+\\n+from __future__ import annotations\\n+\\n+import argparse\\n+from dataclasses import dataclass\\n+from pathlib import Path\\n+import re\\n+import subprocess\\n+import sys\\n+import tempfile\\n+\\n+\\n+ROOT = Path(__file__).resolve().parent\\n+EXAMPLES = ROOT / \\\"examples\\\"\\n+# Include process startup and clang/linker work on a busy developer machine.\\n+# The VM has an independent deterministic instruction budget for nontermination.\\n+TIMEOUT = 30\\n+\\n+\\n+@dataclass(frozen=True)\\n+class RunCase:\\n+    name: str\\n+    stdout: str\\n+    exit_code: int = 0\\n+\\n+\\n+@dataclass(frozen=True)\\n+class RejectCase:\\n+    name: str\\n+    message: str\\n+\\n+\\n+RUN_CASES = (\\n+    RunCase(\\\"arithmetic\\\", \\\"12\\\\n-3\\\\n42\\\\n-2\\\\n-1\\\\n0\\\\n\\\"),\\n+    RunCase(\\\"comparisons\\\", \\\"true\\\\nfalse\\\\ntrue\\\\ntrue\\\\ntrue\\\\nfalse\\\\ntrue\\\\ntrue\\\\n\\\"),\\n+    RunCase(\\\"precedence\\\", \\\"14\\\\n20\\\\n-1\\\\ntrue\\\\n\\\"),\\n+    RunCase(\\\"scopes\\\", \\\"9\\\\n3\\\\n\\\"),\\n+    RunCase(\\\"loop\\\", \\\"10\\\\n\\\"),\\n+    RunCase(\\\"forward_call\\\", \\\"42\\\\n\\\"),\\n+    RunCase(\\\"recursion\\\", \\\"720\\\\n\\\"),\\n+    RunCase(\\\"evaluation_order\\\", \\\"1\\\\n2\\\\n3\\\\n\\\"),\\n+    RunCase(\\\"void_return\\\", \\\"7\\\\n8\\\\n\\\"),\\n+    RunCase(\\\"if_returns\\\", \\\"-1\\\\n1\\\\n\\\"),\\n+    RunCase(\\\"factorial\\\", \\\"720\\\\n\\\"),\\n+    RunCase(\\\"unreachable\\\", \\\"7\\\\n\\\"),\\n+    RunCase(\\\"multiline_parens\\\", \\\"7\\\\n\\\"),\\n+    RunCase(\\\"copy_independence\\\", \\\"4\\\\n9\\\\n\\\"),\\n+    RunCase(\\\"shadow_initializer\\\", \\\"6\\\\n5\\\\n\\\"),\\n+    RunCase(\\\"empty_blocks\\\", \\\"3\\\\n\\\"),\\n+    RunCase(\\\"bool_forward\\\", \\\"true\\\\nfalse\\\\n\\\"),\\n+    RunCase(\\\"mutual_recursion\\\", \\\"true\\\\ntrue\\\\n\\\"),\\n+    RunCase(\\\"multiline_forms\\\", \\\"-2147483648\\\\ntrue\\\\n7\\\\n13\\\\n\\\"),\\n+    RunCase(\\\"overflow_add\\\", \\\"runtime error: integer overflow\\\\n\\\", 1),\\n+    RunCase(\\\"overflow_sub\\\", \\\"runtime error: integer overflow\\\\n\\\", 1),\\n+    RunCase(\\\"overflow_mul\\\", \\\"runtime error: integer overflow\\\\n\\\", 1),\\n+    RunCase(\\\"overflow_neg\\\", \\\"runtime error: integer overflow\\\\n\\\", 1),\\n+    RunCase(\\\"overflow_div\\\", \\\"runtime error: integer overflow\\\\n\\\", 1),\\n+    RunCase(\\\"div_zero\\\", \\\"runtime error: division by zero\\\\n\\\", 1),\\n+    RunCase(\\\"rem_zero\\\", \\\"runtime error: division by zero\\\\n\\\", 1),\\n+)\\n+\\n+REJECT_CASES = (\\n+    RejectCase(\\\"reject_type\\\", \\\"type mismatch\\\"),\\n+    RejectCase(\\\"reject_mutability\\\", \\\"cannot assign to immutable local\\\"),\\n+    RejectCase(\\\"reject_arity\\\", \\\"wrong number of arguments\\\"),\\n+    RejectCase(\\\"reject_unknown\\\", \\\"unknown name\\\"),\\n+    RejectCase(\\\"reject_duplicate\\\", \\\"duplicate declaration\\\"),\\n+    RejectCase(\\\"reject_missing_return\\\", \\\"may reach its end without returning\\\"),\\n+    RejectCase(\\\"reject_semicolon\\\", \\\"semicolons are not supported\\\"),\\n+    RejectCase(\\\"reject_string\\\", \\\"expected expression\\\"),\\n+    RejectCase(\\\"reject_bool_arithmetic\\\", \\\"type mismatch\\\"),\\n+    RejectCase(\\\"reject_import\\\", \\\"only function declarations are supported\\\"),\\n+    RejectCase(\\\"reject_scope_leak\\\", \\\"unknown name\\\"),\\n+    RejectCase(\\\"reject_unary_depth\\\", \\\"expression nesting limit exceeded\\\"),\\n+    RejectCase(\\\"reject_reserved_print\\\", \\\"reserved\\\"),\\n+    RejectCase(\\\"reject_truncated\\\", \\\"unterminated function body\\\"),\\n+    RejectCase(\\\"reject_large_literal\\\", \\\"integer literal\\\"),\\n+)\\n+\\n+\\n+def invoke(argv: list[str], timeout: int = TIMEOUT) -> subprocess.CompletedProcess[str]:\\n+    return subprocess.run(argv, text=True, capture_output=True, timeout=timeout, check=False)\\n+\\n+\\n+def check_result(label: str, result: subprocess.CompletedProcess[str],\\n+                 expected_stdout: str, expected_exit: int) -> list[str]:\\n+    failures: list[str] = []\\n+    if result.returncode != expected_exit:\\n+        failures.append(f\\\"{label}: exit {result.returncode}, expected {expected_exit}\\\")\\n+    if result.stdout != expected_stdout:\\n+        failures.append(f\\\"{label}: stdout {result.stdout!r}, expected {expected_stdout!r}\\\")\\n+    if result.stderr:\\n+        failures.append(f\\\"{label}: unexpected stderr {result.stderr!r}\\\")\\n+    return failures\\n+\\n+\\n+def test_shared_case(binary: Path, clang: str, case: RunCase, temp: Path) -> list[str]:\\n+    source = EXAMPLES / f\\\"{case.name}.saw\\\"\\n+    failures = check_result(\\n+        f\\\"{case.name} VM\\\", invoke([str(binary), \\\"run\\\", str(source)]),\\n+        case.stdout, case.exit_code)\\n+\\n+    emitted = invoke([str(binary), \\\"emit-llvm\\\", str(source)])\\n+    if emitted.returncode != 0 or emitted.stderr:\\n+        failures.extend(check_result(f\\\"{case.name} emit\\\", emitted, emitted.stdout, 0))\\n+        return failures\\n+    ll_path = temp / f\\\"{case.name}.ll\\\"\\n+    ll_path.write_text(emitted.stdout, encoding=\\\"utf-8\\\")\\n+    for optimization in (\\\"-O0\\\", \\\"-O2\\\"):\\n+        suffix = optimization.removeprefix(\\\"-\\\")\\n+        native_path = temp / f\\\"{case.name}-{suffix}\\\"\\n+        compiled = invoke([clang, optimization, str(ll_path), \\\"-o\\\", str(native_path)])\\n+        if compiled.returncode != 0:\\n+            failures.append(\\n+                f\\\"{case.name} clang {optimization}: exit {compiled.returncode}\\\\n\\\"\\n+                f\\\"stdout: {compiled.stdout}\\\\nstderr: {compiled.stderr}\\\"\\n+            )\\n+            continue\\n+        try:\\n+            native = invoke([str(native_path)])\\n+        except subprocess.TimeoutExpired:\\n+            failures.append(\\n+                f\\\"{case.name} native {optimization}: timed out after {TIMEOUT}s\\\")\\n+            continue\\n+        failures.extend(check_result(\\n+            f\\\"{case.name} native {optimization}\\\", native,\\n+            case.stdout, case.exit_code))\\n+    return failures\\n+\\n+\\n+def test_rejection(binary: Path, case: RejectCase) -> list[str]:\\n+    source = EXAMPLES / f\\\"{case.name}.saw\\\"\\n+    result = invoke([str(binary), \\\"run\\\", str(source)])\\n+    failures: list[str] = []\\n+    if result.returncode != 1:\\n+        failures.append(f\\\"{case.name}: exit {result.returncode}, expected 1\\\")\\n+    if result.stderr:\\n+        failures.append(f\\\"{case.name}: unexpected stderr {result.stderr!r}\\\")\\n+    located = rf\\\"^{re.escape(str(source))}:[1-9]\\\\d*:[1-9]\\\\d*: \\\"\\n+    if not re.match(located, result.stdout):\\n+        failures.append(f\\\"{case.name}: diagnostic is not located: {result.stdout!r}\\\")\\n+    if case.message not in result.stdout:\\n+        failures.append(f\\\"{case.name}: missing {case.message!r}: {result.stdout!r}\\\")\\n+    return failures\\n+\\n+\\n+def test_vm_limit(binary: Path, name: str, budget: int, message: str) -> list[str]:\\n+    source = EXAMPLES / f\\\"{name}.saw\\\"\\n+    result = invoke([str(binary), \\\"run\\\", str(source), \\\"--budget\\\", str(budget)])\\n+    return check_result(name, result, message + \\\"\\\\n\\\", 1)\\n+\\n+\\n+def main() -> int:\\n+    parser = argparse.ArgumentParser()\\n+    parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n+    parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n+    args = parser.parse_args()\\n+    binary = args.binary.resolve()\\n+    if not binary.is_file():\\n+        parser.error(f\\\"binary does not exist: {binary}\\\")\\n+\\n+    failures: list[str] = []\\n+    with tempfile.TemporaryDirectory(prefix=\\\"minivm-test-\\\") as directory:\\n+        temp = Path(directory)\\n+        for case in RUN_CASES:\\n+            try:\\n+                failures.extend(test_shared_case(binary, args.clang, case, temp))\\n+            except subprocess.TimeoutExpired:\\n+                failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n+        for case in REJECT_CASES:\\n+            try:\\n+                failures.extend(test_rejection(binary, case))\\n+            except subprocess.TimeoutExpired:\\n+                failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n+        for name, budget, message in (\\n+            (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n+            (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n+        ):\\n+            try:\\n+                failures.extend(test_vm_limit(binary, name, budget, message))\\n+            except subprocess.TimeoutExpired:\\n+                failures.append(f\\\"{name}: timed out after {TIMEOUT}s\\\")\\n+\\n+    if failures:\\n+        for failure in failures:\\n+            print(f\\\"FAIL: {failure}\\\")\\n+        print(f\\\"minivm: {len(failures)} failure(s)\\\")\\n+        return 1\\n+    total = len(RUN_CASES) + len(REJECT_CASES) + 2\\n+    print(f\\\"minivm: {total} cases passed\\\")\\n+    return 0\\n+\\n+\\n+if __name__ == \\\"__main__\\\":\\n+    sys.exit(main())\\n\",\"id\":\"SL-243\",\"request_id\":\"pid-84676-1789086411-5000\",\"title\":\"minivm M0: subset compiler foundation — Int32/Bool VM + textual LLVM emitter + differential harness (branch commit 66e56883)\"}","request_id":"pid-84676-1789086411-5000","sequence":423,"timestamp":"1789086412"}
---
