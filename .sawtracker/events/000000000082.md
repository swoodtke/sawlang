---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791230","description":"## Scope and status\n\nDF-178a — a /// doc comment cannot document an extern declaration. Inside extern \"C\" { ... } it is doc comment is not followed by a documentable declaration, so…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 2608–2618\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L2608-L2618)\n\n- **DF‑178a — a `///` doc comment cannot document an `extern` declaration.**\n  Inside `extern \"C\" { ... }` it is ``doc comment is not followed by a\n  documentable declaration``, so a C seam's contract has to be written as a\n  plain `//` comment beside the declaration it describes. The documentable\n  list (LANGUAGE_SPEC \"Doc comments\") covers func/struct/enum/trait/\n  extension/type/static, fields, cases, methods and inits — an `extern` func\n  is none of them. It is exactly the position a HAL wants documented: the\n  arm64 kernel HAL now declares four timer seams whose docstrings had to be\n  demoted, and `--emit-docs` cannot see them at all. Small, clean-error,\n  not blocking; the fix is adding `extern` functions to the attachable set.\n","id":"SL-82","labels":["todo-import","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":82,"status":"open","title":"DF-178a — a /// doc comment cannot document an extern declaration. Inside extern \"C\" { ... } it is doc comment is not followed by a documentable declaration, so…","updated":"1788791230"},"before":null,"id":"SL-82","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-178a — a /// doc comment cannot document an extern declaration. Inside extern \\\"C\\\" { ... } it is doc comment is not followed by a documentable declaration, so…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 2608–2618\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L2608-L2618)\\n\\n- **DF‑178a — a `///` doc comment cannot document an `extern` declaration.**\\n  Inside `extern \\\"C\\\" { ... }` it is ``doc comment is not followed by a\\n  documentable declaration``, so a C seam's contract has to be written as a\\n  plain `//` comment beside the declaration it describes. The documentable\\n  list (LANGUAGE_SPEC \\\"Doc comments\\\") covers func/struct/enum/trait/\\n  extension/type/static, fields, cases, methods and inits — an `extern` func\\n  is none of them. It is exactly the position a HAL wants documented: the\\n  arm64 kernel HAL now declares four timer seams whose docstrings had to be\\n  demoted, and `--emit-docs` cannot see them at all. Small, clean-error,\\n  not blocking; the fix is adding `extern` functions to the attachable set.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-d753f97c17959a7b70bc-create\",\"title\":\"DF-178a — a /// doc comment cannot document an extern declaration. Inside extern \\\"C\\\" { ... } it is doc comment is not followed by a documentable declaration, so…\"}","request_id":"todo-20260907-d753f97c17959a7b70bc-create","sequence":82,"timestamp":"1788791230"}
---
