---
{"action":"create","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790073077","description":"Found by the lead's design 275 U3 validation (Sep 22); PRE-EXISTING on main for a range `for`, and design 275 U3 makes a collection `for` meet the same refusal (its back-edge is charged now, per the ruling that retired the op-budget skip).\n\n## The finding\n\nA VALUE-position loop (`break <value>`) in a DRIVEN body whose body does NOT suspend is refused with a message about a call the author never wrote:\n\n```saw\nfunc find_driven() -> Int? {\n    yield_now()\n    let found = for i in 0..3 { if i > 1 { break i } }\n    found\n}\n// error: coroutine transform: suspending call to `yield_now` in `find_driven`\n// appears in a nested/expression position; only a top-level `let x =\n// yield_now(...)` or `yield_now(...)` statement is supported\n//   --> ...:7:17   (the `for`)\n```\n\nThe `yield_now` it names is design 127's SYNTHESIZED op-budget yield at the loop's back-edge, not the author's line-6 call; the anchor is the `for`. The refusal itself is sound (a suspension-spanning loop does not support `break <value>` — the shape table's rule) but nothing on the page says so, and the hint (\"write a top-level `let x = yield_now(...)`\") cannot be followed.\n\n## What is owed\n\nRoute the refusal through `coro_shapes.py`'s REFUSE row for the value-position spanning loop with a message that names the mechanism: a loop in a task body spans the op-budget yield the compiler inserts at its back-edge, so its value cannot be produced by `break <value>`; the ways out are a `var` result written in the body with a plain `break`, or a `sync` helper. Pin both spellings (range `for`, collection `for`) in one errors example; K136's `break <value>` driven cell is the row that records the bound today.\n\nCells: `.build/scratch/lead/l3_value_for_driven.saw` + `l5_value_range_for_driven.saw` in worktree `sl318u3` (main: collection form COMPILED and printed 2, range form refused; branch: both refused with this message).\n","id":"SL-346","labels":["bug","diagnostics","coroutine"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1171,"stage":"backlog","status":"open","title":"A value-position loop in a driven body is refused by naming the op budget's synthesized yield_now as a user call (misleading diagnostic, unfollowable hint)","updated":"1790073077"},"before":null,"id":"SL-346","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"description\":\"Found by the lead's design 275 U3 validation (Sep 22); PRE-EXISTING on main for a range `for`, and design 275 U3 makes a collection `for` meet the same refusal (its back-edge is charged now, per the ruling that retired the op-budget skip).\\n\\n## The finding\\n\\nA VALUE-position loop (`break <value>`) in a DRIVEN body whose body does NOT suspend is refused with a message about a call the author never wrote:\\n\\n```saw\\nfunc find_driven() -> Int? {\\n    yield_now()\\n    let found = for i in 0..3 { if i > 1 { break i } }\\n    found\\n}\\n// error: coroutine transform: suspending call to `yield_now` in `find_driven`\\n// appears in a nested/expression position; only a top-level `let x =\\n// yield_now(...)` or `yield_now(...)` statement is supported\\n//   --> ...:7:17   (the `for`)\\n```\\n\\nThe `yield_now` it names is design 127's SYNTHESIZED op-budget yield at the loop's back-edge, not the author's line-6 call; the anchor is the `for`. The refusal itself is sound (a suspension-spanning loop does not support `break <value>` — the shape table's rule) but nothing on the page says so, and the hint (\\\"write a top-level `let x = yield_now(...)`\\\") cannot be followed.\\n\\n## What is owed\\n\\nRoute the refusal through `coro_shapes.py`'s REFUSE row for the value-position spanning loop with a message that names the mechanism: a loop in a task body spans the op-budget yield the compiler inserts at its back-edge, so its value cannot be produced by `break <value>`; the ways out are a `var` result written in the body with a plain `break`, or a `sync` helper. Pin both spellings (range `for`, collection `for`) in one errors example; K136's `break <value>` driven cell is the row that records the bound today.\\n\\nCells: `.build/scratch/lead/l3_value_for_driven.saw` + `l5_value_range_for_driven.saw` in worktree `sl318u3` (main: collection form COMPILED and printed 2, range form refused; branch: both refused with this message).\\n\",\"labels\":[\"bug\",\"diagnostics\",\"coroutine\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"pid-73304-1790073077-8000\",\"stage\":\"backlog\",\"title\":\"A value-position loop in a driven body is refused by naming the op budget's synthesized yield_now as a user call (misleading diagnostic, unfollowable hint)\"}","request_id":"pid-73304-1790073077-8000","sequence":1171,"timestamp":"1790073077"}
---
