---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"agent:claude-sl390","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"PARKED (Sep 24): the Python compiler is frozen by user ruling (chat m176). SL-390 will not be fixed; it becomes a hazards-ledger entry. This comment preserves the parked implementer's findings, which are ledger material. The worktree `.claude/worktrees/sl390` holds uncommitted and deliberately broken work (an ImportError; a draft `sawc/alias_walk.py`); it is evidence only.\n\n## Walks that share the alias bound\n| # | Walk | What the bound counts | Past the bound |\n|---|---|---|---|\n| 1 | `noescape.first_reference_in` (12) | alias hops AND structural nesting (purely structural at the parser) | answers \"no reference\": THE SL-390 HOLE |\n| 2 | `borrowing.borrowing_struct_name` (12) | alias hops | a 13-link alias chain of a borrowing iterator is wrongly refused |\n| 3 | `borrowing.reference_field_type` (12) | alias hops | a wrong \"holds no lent place\" |\n| 4 | `types._borrowing_field_referent` (12) | alias hops | hands back the unresolved alias, which #1 then misses |\n| 5 | `borrowing._first_borrowing_struct_in` (32) | structural plus alias | refuses ANY type nested more than 32 deep, everywhere; this is why #1's hole closes at 33 |\n| 6 | `registration._alias_underlying_enum` (16) | alias hops | harmless |\n\n## Where each position stops being refused on main (ebf67de3)\nThe shape is `(…(&Int, Int)…, Int)`, wrapped N times.\n- Field, enum payload, return type, function type in a field: refused at N=12. N=13 through 32 COMPILE. N≥33 is refused only by #5's depth message.\n- Generic argument and `static`: the reference refusal is missing from N=13 up. A static of this shape is still refused by the initializer and Sync rules.\n- Alias chain: refused at 12 links; 13 and 20 links compile.\n- **Alias CYCLES are not refused cleanly.** `type A = A`, A/B mutual cycles, `A = A?`, `A = (A, Int)` and a cycle used as a local's type all end in an ICE: maximum recursion depth, from `copy_tier`, `_send_sync` and `_get_underlying_type`. A cycle nobody uses gives a raw traceback. `A = Vector<A>`, `Box<A>` and `() -> A` are refused only by accident, through #5.\n- A deep type with NO reference compiles at 12, 13 and 20, and is wrongly refused at N≥33 (28 errors at 40).\n- Borrowing structs: the alias-chain producer is wrongly refused at 13+. A `borrows struct` field through a 12-link chain fails with \"expects R11\" (registration resolves one alias hop only).\n\n## Other depth-bounded type walks (swept, each probed just under and past its bound)\n- **Rule silently not enforced past the bound:**\n  - `sigvis._check_signature_type` (>8): a private type 9 deep in a public signature compiles.\n  - `statements._result_discard_culprits` (>32): a discarded Result through 33 forwarding match/if compiles.\n  - `tierreq._tier_abstract_params_in` (>12).\n  - `types._is_no_move_type` (>12): moving a NoMove value compiles and runs at 13 wraps.\n  - `types._names_self` (>8): the object-safety refusal becomes an ICE.\n- **Legal programs wrongly refused past the bound:**\n  - `types._resolve_declared_qualified_names` (>8), `_substitute_self_type` (>16), `_unalias_top` (16 hops);\n  - `expressions._ctor_slot_candidates` (>8), `_type_names_any` (>16);\n  - `monomorphize._type_names_param` (>12);\n  - `coro_transform._names_unsafe_type` (>12);\n  - `_INLINE_EMBED_DEPTH_LIMIT` (64): a false \"recursive type has infinite size\".\n- **Harmless:** `codegen/core._payload_type_is_void`, `typechecker/core._gate_written_type`, `monomorphize._names_a_generic`, `ownership._transfer_source_identity`, `types._type_is_poisoned`.\n- **Unproven** (the probes hit the parked ImportError): `coro_transform._contains_move`, `resources._is_owned_temporary`, `typechecker/core._lend_instantiation_types`, `monomorphize.identify`, `types._spawn_storage_root_type`, `coro_transform._place_root_name`.\n- **Found along the way:**\n  - `_names_self` never looks inside tuple elements: `(Self, Int)` is an ICE.\n  - `struct R { x: Optional<Int> }` is an ICE (\"Unknown generic struct: Optional\").\n  - 63 nested `as` casts give a Python RecursionError.\n\nFor the ledger: a type walk bounded by a depth count is a family. A rule is silently skipped, or a legal program refused, somewhere between 8 and 64 levels of nesting or alias hops. Keep types shallow and avoid alias chains in code the frozen compiler builds. The probes are in `.claude/worktrees/sl390/.build/scratch/sweep390/`; copy them into the ledger's evidence before that worktree is removed.\n","created":"1790275713","id":"c1"}],"created":"1790274622","description":"Found by the SL-2 U0′ r3 implementer (Sep 24); the lead re-measured it on main (`.build/scratch/ref_depth.py` in the lead checkout).\n\nThe no-reference-in-a-field refusal stops looking after 12 levels of type nesting. The type is `(…((&Int, Int), Int)…, Int)`, wrapped N times, as a struct field:\n\n| N | result |\n|---|---|\n| 1, 11, 12 | refused: \"field `slot` of `Holder` may not be a reference\" |\n| 13, 14, 20 | COMPILES (exit 0) |\n\nA struct that stores a reference escapes the rule that keeps references from outliving their frame. That is a soundness hole: a dangling pointer from safe code.\n\nMechanism (named by the implementer; verify): `first_reference_in` in `sawc/noescape.py` returns `None` once `depth > _MAX_ALIAS_DEPTH` (12). The bound exists to stop an alias CYCLE, but `depth` counts every level of type nesting, not only alias hops. So a deep but acyclic type reads as \"no reference\".\n\nPositions to sweep (obligation 4): the same walk backs every no-escape check, and the typechecker's alias-aware twin shares it. Cover:\n- struct fields;\n- enum payloads;\n- function return types;\n- generic arguments (`Vector<(…(&T…)…)>`);\n- closure and function types;\n- `static` types;\n- an alias chain deeper than 12 and an alias cycle (the refusal the bound was written for).\n\nThe fix bounds alias hops by their own cycle detection, for example a visited set of alias names. It does not bound structural depth at all, because a type's depth is bounded by its source and SL-2's depth funnel already caps that at 256.\n","id":"SL-390","labels":["frozen"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":4,"sequence":2263,"stage":"backlog","status":"open","title":"A reference nested 13+ levels deep in a type escapes the no-reference-field refusal (first_reference_in's alias-cycle bound counts every nesting level) — soundness","updated":"1790341690"},"before":{"acceptance":[],"assignee":"agent:claude-sl390","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"PARKED (Sep 24): the Python compiler is frozen by user ruling (chat m176). SL-390 will not be fixed; it becomes a hazards-ledger entry. This comment preserves the parked implementer's findings, which are ledger material. The worktree `.claude/worktrees/sl390` holds uncommitted and deliberately broken work (an ImportError; a draft `sawc/alias_walk.py`); it is evidence only.\n\n## Walks that share the alias bound\n| # | Walk | What the bound counts | Past the bound |\n|---|---|---|---|\n| 1 | `noescape.first_reference_in` (12) | alias hops AND structural nesting (purely structural at the parser) | answers \"no reference\": THE SL-390 HOLE |\n| 2 | `borrowing.borrowing_struct_name` (12) | alias hops | a 13-link alias chain of a borrowing iterator is wrongly refused |\n| 3 | `borrowing.reference_field_type` (12) | alias hops | a wrong \"holds no lent place\" |\n| 4 | `types._borrowing_field_referent` (12) | alias hops | hands back the unresolved alias, which #1 then misses |\n| 5 | `borrowing._first_borrowing_struct_in` (32) | structural plus alias | refuses ANY type nested more than 32 deep, everywhere; this is why #1's hole closes at 33 |\n| 6 | `registration._alias_underlying_enum` (16) | alias hops | harmless |\n\n## Where each position stops being refused on main (ebf67de3)\nThe shape is `(…(&Int, Int)…, Int)`, wrapped N times.\n- Field, enum payload, return type, function type in a field: refused at N=12. N=13 through 32 COMPILE. N≥33 is refused only by #5's depth message.\n- Generic argument and `static`: the reference refusal is missing from N=13 up. A static of this shape is still refused by the initializer and Sync rules.\n- Alias chain: refused at 12 links; 13 and 20 links compile.\n- **Alias CYCLES are not refused cleanly.** `type A = A`, A/B mutual cycles, `A = A?`, `A = (A, Int)` and a cycle used as a local's type all end in an ICE: maximum recursion depth, from `copy_tier`, `_send_sync` and `_get_underlying_type`. A cycle nobody uses gives a raw traceback. `A = Vector<A>`, `Box<A>` and `() -> A` are refused only by accident, through #5.\n- A deep type with NO reference compiles at 12, 13 and 20, and is wrongly refused at N≥33 (28 errors at 40).\n- Borrowing structs: the alias-chain producer is wrongly refused at 13+. A `borrows struct` field through a 12-link chain fails with \"expects R11\" (registration resolves one alias hop only).\n\n## Other depth-bounded type walks (swept, each probed just under and past its bound)\n- **Rule silently not enforced past the bound:**\n  - `sigvis._check_signature_type` (>8): a private type 9 deep in a public signature compiles.\n  - `statements._result_discard_culprits` (>32): a discarded Result through 33 forwarding match/if compiles.\n  - `tierreq._tier_abstract_params_in` (>12).\n  - `types._is_no_move_type` (>12): moving a NoMove value compiles and runs at 13 wraps.\n  - `types._names_self` (>8): the object-safety refusal becomes an ICE.\n- **Legal programs wrongly refused past the bound:**\n  - `types._resolve_declared_qualified_names` (>8), `_substitute_self_type` (>16), `_unalias_top` (16 hops);\n  - `expressions._ctor_slot_candidates` (>8), `_type_names_any` (>16);\n  - `monomorphize._type_names_param` (>12);\n  - `coro_transform._names_unsafe_type` (>12);\n  - `_INLINE_EMBED_DEPTH_LIMIT` (64): a false \"recursive type has infinite size\".\n- **Harmless:** `codegen/core._payload_type_is_void`, `typechecker/core._gate_written_type`, `monomorphize._names_a_generic`, `ownership._transfer_source_identity`, `types._type_is_poisoned`.\n- **Unproven** (the probes hit the parked ImportError): `coro_transform._contains_move`, `resources._is_owned_temporary`, `typechecker/core._lend_instantiation_types`, `monomorphize.identify`, `types._spawn_storage_root_type`, `coro_transform._place_root_name`.\n- **Found along the way:**\n  - `_names_self` never looks inside tuple elements: `(Self, Int)` is an ICE.\n  - `struct R { x: Optional<Int> }` is an ICE (\"Unknown generic struct: Optional\").\n  - 63 nested `as` casts give a Python RecursionError.\n\nFor the ledger: a type walk bounded by a depth count is a family. A rule is silently skipped, or a legal program refused, somewhere between 8 and 64 levels of nesting or alias hops. Keep types shallow and avoid alias chains in code the frozen compiler builds. The probes are in `.claude/worktrees/sl390/.build/scratch/sweep390/`; copy them into the ledger's evidence before that worktree is removed.\n","created":"1790275713","id":"c1"}],"created":"1790274622","description":"Found by the SL-2 U0′ r3 implementer (Sep 24); the lead re-measured it on main (`.build/scratch/ref_depth.py` in the lead checkout).\n\nThe no-reference-in-a-field refusal stops looking after 12 levels of type nesting. The type is `(…((&Int, Int), Int)…, Int)`, wrapped N times, as a struct field:\n\n| N | result |\n|---|---|\n| 1, 11, 12 | refused: \"field `slot` of `Holder` may not be a reference\" |\n| 13, 14, 20 | COMPILES (exit 0) |\n\nA struct that stores a reference escapes the rule that keeps references from outliving their frame. That is a soundness hole: a dangling pointer from safe code.\n\nMechanism (named by the implementer; verify): `first_reference_in` in `sawc/noescape.py` returns `None` once `depth > _MAX_ALIAS_DEPTH` (12). The bound exists to stop an alias CYCLE, but `depth` counts every level of type nesting, not only alias hops. So a deep but acyclic type reads as \"no reference\".\n\nPositions to sweep (obligation 4): the same walk backs every no-escape check, and the typechecker's alias-aware twin shares it. Cover:\n- struct fields;\n- enum payloads;\n- function return types;\n- generic arguments (`Vector<(…(&T…)…)>`);\n- closure and function types;\n- `static` types;\n- an alias chain deeper than 12 and an alias cycle (the refusal the bound was written for).\n\nThe fix bounds alias hops by their own cycle detection, for example a visited set of alias names. It does not bound structural depth at all, because a type's depth is bounded by its source and SL-2's depth funnel already caps that at 256.\n","id":"SL-390","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":3,"sequence":1478,"stage":"backlog","status":"open","title":"A reference nested 13+ levels deep in a type escapes the no-reference-field refusal (first_reference_in's alias-cycle bound counts every nesting level) — soundness","updated":"1790275713"},"id":"SL-390","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-390\",\"labels\":[\"frozen\"],\"request_id\":\"pid-57514-1790341690-5000\",\"revision\":3}","request_id":"pid-57514-1790341690-5000","sequence":2263,"timestamp":"1790341690"}
---
