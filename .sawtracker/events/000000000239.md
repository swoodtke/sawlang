---
{"action":"edit","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791256","description":"## Scope and status\n\nDF-126a: Verify the latent pre-port AST annotation contract\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Re-run the precise historical probe at current HEAD, recording compiler revision, target and output; distinguish the original symptom from any surviving residue.\n2. Compare with later design landings and existing pins. If resolved, close with the relevant commit/test; if still present, preserve a minimal reproducer and update the scope.\n3. Do not schedule a speculative implementation until the verification identifies a remaining defect or missing contract.\n\n## Acceptance criteria\n\n- [ ] The task has a recorded current-status verdict supported by a probe, code reference or landing record.\n- [ ] Any surviving work has a bounded scope and regression oracle; otherwise the task is closed with its resolution.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 9501–9520\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L9501-L9520)\n\n- **DF‑126a — RC‑2 is LATENT, not a live bug (measured, Aug 4).** The pre-port\n  review called the un-substituted grafted annotations \"a live bug, not just a\n  port hazard\": `substitute_ast_types` walks `dataclasses.fields()`, so while\n  `resolved_type` and the ~50 other annotations were grafted at runtime, the\n  monomorphizer could not see them, and every `SawType`-valued one was carried\n  into an instantiation stale. R1 declares them, so the substituter sees them —\n  but the claimed miscompile could not be reproduced. Repro method (kept here\n  because it is the way to re-test this cheaply): make the loop at\n  `typechecker/effects.py:51` skip `resolved_type` and every field whose\n  metadata carries `saw_annotation`, i.e. reproduce exactly what the grafts hid,\n  then run the suite. Result: **1034/1034 pass**, including\n  `examples/coro_generic_mono_type_subst.saw`, which was written specifically to\n  exercise the path (a driven generic-struct method at three instantiations,\n  with a `match` over a `T`-parameterized enum and a `Vector<T>` literal live\n  across the suspension). So the corpus cannot currently reach a shape where the\n  stale annotation changes the emitted code. WANTED: either a shape that does\n  distinguish (then it becomes a real regression test), or acceptance that R1's\n  value here is contract correctness for the port rather than a bug fix. Do NOT\n  describe RC‑2 as a fixed miscompile without such a shape.\n","id":"SL-139","labels":["todo-import","needs-verification","verification","plan"],"priority":"normal","project":"SL","revision":2,"sequence":239,"status":"open","title":"DF-126a: Verify the latent pre-port AST annotation contract","updated":"1788791396"},"before":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791256","description":"## Scope and status\n\nDF-126a: Verify the latent pre-port AST annotation contract\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Re-run the precise historical probe at current HEAD, recording compiler revision, target and output; distinguish the original symptom from any surviving residue.\n2. Compare with later design landings and existing pins. If resolved, close with the relevant commit/test; if still present, preserve a minimal reproducer and update the scope.\n3. Do not schedule a speculative implementation until the verification identifies a remaining defect or missing contract.\n\n## Acceptance criteria\n\n- [ ] The task has a recorded current-status verdict supported by a probe, code reference or landing record.\n- [ ] Any surviving work has a bounded scope and regression oracle; otherwise the task is closed with its resolution.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 9501–9520\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L9501-L9520)\n\n- **DF‑126a — RC-2 is LATENT, not a live bug (measured, Aug 4).** The pre-port\n  review called the un-substituted grafted annotations \"a live bug, not just a\n  port hazard\": `substitute_ast_types` walks `dataclasses.fields()`, so while\n  `resolved_type` and the ~50 other annotations were grafted at runtime, the\n  monomorphizer could not see them, and every `SawType`-valued one was carried\n  into an instantiation stale. R1 declares them, so the substituter sees them —\n  but the claimed miscompile could not be reproduced. Repro method (kept here\n  because it is the way to re-test this cheaply): make the loop at\n  `typechecker/effects.py:51` skip `resolved_type` and every field whose\n  metadata carries `saw_annotation`, i.e. reproduce exactly what the grafts hid,\n  then run the suite. Result: **1034/1034 pass**, including\n  `examples/coro_generic_mono_type_subst.saw`, which was written specifically to\n  exercise the path (a driven generic-struct method at three instantiations,\n  with a `match` over a `T`-parameterized enum and a `Vector<T>` literal live\n  across the suspension). So the corpus cannot currently reach a shape where the\n  stale annotation changes the emitted code. WANTED: either a shape that does\n  distinguish (then it becomes a real regression test), or acceptance that R1's\n  value here is contract correctness for the port rather than a bug fix. Do NOT\n  describe RC-2 as a fixed miscompile without such a shape.\n","id":"SL-139","labels":["todo-import","needs-verification","verification","plan"],"priority":"normal","project":"SL","revision":1,"sequence":139,"status":"open","title":"DF-126a: Verify the latent pre-port AST annotation contract","updated":"1788791256"},"id":"SL-139","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-126a: Verify the latent pre-port AST annotation contract\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Re-run the precise historical probe at current HEAD, recording compiler revision, target and output; distinguish the original symptom from any surviving residue.\\n2. Compare with later design landings and existing pins. If resolved, close with the relevant commit/test; if still present, preserve a minimal reproducer and update the scope.\\n3. Do not schedule a speculative implementation until the verification identifies a remaining defect or missing contract.\\n\\n## Acceptance criteria\\n\\n- [ ] The task has a recorded current-status verdict supported by a probe, code reference or landing record.\\n- [ ] Any surviving work has a bounded scope and regression oracle; otherwise the task is closed with its resolution.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 9501–9520\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L9501-L9520)\\n\\n- **DF‑126a — RC‑2 is LATENT, not a live bug (measured, Aug 4).** The pre-port\\n  review called the un-substituted grafted annotations \\\"a live bug, not just a\\n  port hazard\\\": `substitute_ast_types` walks `dataclasses.fields()`, so while\\n  `resolved_type` and the ~50 other annotations were grafted at runtime, the\\n  monomorphizer could not see them, and every `SawType`-valued one was carried\\n  into an instantiation stale. R1 declares them, so the substituter sees them —\\n  but the claimed miscompile could not be reproduced. Repro method (kept here\\n  because it is the way to re-test this cheaply): make the loop at\\n  `typechecker/effects.py:51` skip `resolved_type` and every field whose\\n  metadata carries `saw_annotation`, i.e. reproduce exactly what the grafts hid,\\n  then run the suite. Result: **1034/1034 pass**, including\\n  `examples/coro_generic_mono_type_subst.saw`, which was written specifically to\\n  exercise the path (a driven generic-struct method at three instantiations,\\n  with a `match` over a `T`-parameterized enum and a `Vector<T>` literal live\\n  across the suspension). So the corpus cannot currently reach a shape where the\\n  stale annotation changes the emitted code. WANTED: either a shape that does\\n  distinguish (then it becomes a real regression test), or acceptance that R1's\\n  value here is contract correctness for the port rather than a bug fix. Do NOT\\n  describe RC‑2 as a fixed miscompile without such a shape.\\n\",\"id\":\"SL-139\",\"request_id\":\"todo-20260907-69d6e51708a9372be3ea-source-codes\",\"revision\":1}","request_id":"todo-20260907-69d6e51708a9372be3ea-source-codes","sequence":239,"timestamp":"1788791396"}
---
