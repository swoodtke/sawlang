---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl204","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"RULINGS (user, Sep 12, via the design-272 decision list): D2-2 APPROVED — no_delay DEFAULTS ON for TcpStream (Go-style; visible labeled opt-out). Lead-endorsed as determined: D2-1 (keep listen, collapse the overloads into one default-carrying signature), D2-3 (v6_only OUT — std.net is IPv4-only end to end, nothing to be dual about), D2-4 (IN: backlog default 16, keepalive enable; OUT: keepalive interval, reuse_port, buffer sizes, linger, raw setsockopt), D2-5 (re-point net_listen_bind_address.saw at an exact-address collision + an opt-out row; re-verify the kernel probe on Linux before finalising).","created":"1789214588","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-229.p1 (revision 1): design 272 unit 2: socket options — reuse_address and no_delay default ON, per-socket SIGPIPE suppression, two additive seams (SL-229)","created":"1789228690","id":"c2"}],"created":"1789050408","description":"MOTIVATION: restarting a server (sawtracker) can fail EADDRINUSE against the dead process's TIME_WAIT socket — there is no way to set SO_REUSEADDR, or any socket option, from Saw today.\n\nRULED DESIGN (user, Sep 10 2026):\n1. reuse_address DEFAULTS ON for TcpListener.bind (Go-style restart-just-works; the flag remains a visible labeled parameter — reuse_address: false opts out). Obligation 2: flipping the default is a behavioral-contract change — the brief owes the consumer sweep (blade, examples/ servers, devtools, the test harnesses).\n2. SHIPS AS the third member of the server-hardening trio with SL-204 (io_wait deadline) + SL-228 (std.signal): one dispatch window at the queue tail, one shared brief if the lead finds they compose.\n\nSURFACE SKETCH (brief refines): no builder/options object — Saw's labeled arguments with default values ARE the options surface. Bind-time (set between socket() and bind()): TcpListener.bind(host, port, reuse_address: true, reuse_port: false, backlog: <current default>, v6_only: <ruling needed for dual-stack>). Per-connection, Result-returning setters on TcpStream (never hide errors, no silent degradation): no_delay (Nagle — whether it defaults on like Go is a brief-time ruling), keepalive enable + interval (Duration). Receive/send buffer sizes and linger: brief decides in or out — curated, not exhaustive; UnsafeSocket-style raw setsockopt escape hatch is OUT unless a consumer demands it. Full words per the no-abbreviations doctrine (reuse_address, never reuseaddr).\n\nMECHANISM: hosted-only like the rest of std.net; one new status-carrying setsockopt-shaped op in rt/common/os_ops.saw + host net_os.saw errno mapping; rt/ABI.md is frozen so any seam addition goes through the abidoc battery lane. Freestanding unaffected.","id":"SL-229","labels":["feature","stdlib"],"priority":"normal","project":"SL","revision":4,"sequence":706,"stage":"queued","status":"open","title":"std.net socket options: reuse_address default ON at listener bind, plus a curated Result-returning option set","updated":"1789228690"},"before":{"assignee":"agent:claude-sl204","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"RULINGS (user, Sep 12, via the design-272 decision list): D2-2 APPROVED — no_delay DEFAULTS ON for TcpStream (Go-style; visible labeled opt-out). Lead-endorsed as determined: D2-1 (keep listen, collapse the overloads into one default-carrying signature), D2-3 (v6_only OUT — std.net is IPv4-only end to end, nothing to be dual about), D2-4 (IN: backlog default 16, keepalive enable; OUT: keepalive interval, reuse_port, buffer sizes, linger, raw setsockopt), D2-5 (re-point net_listen_bind_address.saw at an exact-address collision + an opt-out row; re-verify the kernel probe on Linux before finalising).","created":"1789214588","id":"c1"}],"created":"1789050408","description":"MOTIVATION: restarting a server (sawtracker) can fail EADDRINUSE against the dead process's TIME_WAIT socket — there is no way to set SO_REUSEADDR, or any socket option, from Saw today.\n\nRULED DESIGN (user, Sep 10 2026):\n1. reuse_address DEFAULTS ON for TcpListener.bind (Go-style restart-just-works; the flag remains a visible labeled parameter — reuse_address: false opts out). Obligation 2: flipping the default is a behavioral-contract change — the brief owes the consumer sweep (blade, examples/ servers, devtools, the test harnesses).\n2. SHIPS AS the third member of the server-hardening trio with SL-204 (io_wait deadline) + SL-228 (std.signal): one dispatch window at the queue tail, one shared brief if the lead finds they compose.\n\nSURFACE SKETCH (brief refines): no builder/options object — Saw's labeled arguments with default values ARE the options surface. Bind-time (set between socket() and bind()): TcpListener.bind(host, port, reuse_address: true, reuse_port: false, backlog: <current default>, v6_only: <ruling needed for dual-stack>). Per-connection, Result-returning setters on TcpStream (never hide errors, no silent degradation): no_delay (Nagle — whether it defaults on like Go is a brief-time ruling), keepalive enable + interval (Duration). Receive/send buffer sizes and linger: brief decides in or out — curated, not exhaustive; UnsafeSocket-style raw setsockopt escape hatch is OUT unless a consumer demands it. Full words per the no-abbreviations doctrine (reuse_address, never reuseaddr).\n\nMECHANISM: hosted-only like the rest of std.net; one new status-carrying setsockopt-shaped op in rt/common/os_ops.saw + host net_os.saw errno mapping; rt/ABI.md is frozen so any seam addition goes through the abidoc battery lane. Freestanding unaffected.","id":"SL-229","labels":["feature","stdlib"],"priority":"normal","project":"SL","revision":3,"sequence":676,"stage":"queued","status":"open","title":"std.net socket options: reuse_address default ON at listener bind, plus a curated Result-returning option set","updated":"1789214588"},"id":"SL-229","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/.claude/skills/saw-lang/SKILL.md b/.claude/skills/saw-lang/SKILL.md\\nindex 2d40055c..63125c31 100644\\n--- a/.claude/skills/saw-lang/SKILL.md\\n+++ b/.claude/skills/saw-lang/SKILL.md\\n@@ -2312,6 +2312,10 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n   let listener = try! TcpListener.listen(0)    // Result<TcpListener, IoError>\\n   // Explicit IPv4 binding (no DNS); one-argument listen stays on loopback.\\n   let public_listener = try! TcpListener.listen(0, host: \\\"0.0.0.0\\\")\\n+  // Bind-time options are LABELLED PARAMETERS WITH DEFAULTS — no options object.\\n+  let strict = try! TcpListener.listen(0, reuse_address: false, backlog: 128)\\n+  try! stream.set_no_delay(false)               // Result; per-connection setters\\n+  try! stream.set_keepalive(true)\\n   let port = listener.local_port()\\n   let stream = try! listener.accept()          // Result<TcpStream, IoError>; suspends\\n   let chunk = try! stream.read()               // Result<Data, IoError>; Ok(EMPTY) = EOF\\n@@ -2320,6 +2324,19 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n   let (a, b) = TcpStream.pair()                // connected pair, tests/IPC (no port)\\n   let s = try! TcpStream.connect(\\\"127.0.0.1\\\", port)  // Result; suspends until connected\\n   ```\\n+  **SOCKET OPTIONS: labelled parameters at `listen`, `Result`-returning setters\\n+  on a connection.** `reuse_address` DEFAULTS ON (restarting a server just\\n+  works) and `no_delay` is ON by default on every accepted and dialled\\n+  connection. Neither makes anything quiet: an exact address+port collision\\n+  still fails `AddressInUse` with reuse on, and a refused option is an `Err`\\n+  rather than a silent no-op. Curated, not exhaustive — buffer sizes, `linger`,\\n+  `reuse_port` and a raw `setsockopt` are deliberately ABSENT (`reuse_port`\\n+  means different things on Linux and BSD).\\n+  **A WRITE TO A CLOSED PEER IS `Err(BrokenPipe)`, never a dead process.** The\\n+  runtime suppresses SIGPIPE per socket (`SO_NOSIGPIPE` / `MSG_NOSIGNAL`), not\\n+  process-wide — a process-wide ignore is inherited across `execve` and would\\n+  follow every child into pipelines the program did not write. Pipes keep their\\n+  ordinary behaviour; only sockets change.\\n   **DEADLINES: pass `timeout: Duration` and match on `Timed<T>`.** `accept`,\\n   `read`, `read_into` and `connect` each have a twin taking a timeout; the twin\\n   returns `Result<Timed<T>, IoError>` where `Timed<T>` is `Value(value: T)` or\\ndiff --git a/LANGUAGE_SPEC.md b/LANGUAGE_SPEC.md\\nindex e5e6da25..7e99864b 100644\\n--- a/LANGUAGE_SPEC.md\\n+++ b/LANGUAGE_SPEC.md\\n@@ -8474,14 +8474,61 @@ resolution runs on a worker thread and the platform resolver cannot be\\n cancelled, so the timeout is checked once resolution returns and then governs\\n the dial. Pass a literal address when the whole span must be bounded.\\n \\n-### Explicit IPv4 listener binding\\n-\\n-`TcpListener.listen(port)` binds loopback as before.\\n-`TcpListener.listen(port, host: \\\"0.0.0.0\\\")` binds all IPv4 interfaces; another\\n-dotted IPv4 literal selects that local address. The explicit-host overload\\n-accepts ports 0..65535 (0 selects an ephemeral port), performs no DNS lookup,\\n-and reports invalid text/ports as `IoErrorKind.InvalidArgument`. Ownership,\\n-nonblocking accept, cancellation and socket-close behavior are unchanged.\\n+### Listener binding and socket options\\n+\\n+`TcpListener.listen` takes its options as labelled parameters with defaults;\\n+there is no options object.\\n+\\n+```saw-fragment\\n+public static func listen(port: Int,\\n+                          host: String = \\\"127.0.0.1\\\",\\n+                          reuse_address: Bool = true,\\n+                          backlog: Int = 16) -> Result<TcpListener, IoError>\\n+```\\n+\\n+`port` 0 selects an ephemeral port, read back with `local_port`. `host` takes a\\n+dotted IPv4 literal — \\\"0.0.0.0\\\" for all IPv4 interfaces — and listening performs\\n+no DNS lookup. Invalid text, a port outside 0..65535, and a backlog below 1 are\\n+each `IoErrorKind.InvalidArgument`.\\n+\\n+`reuse_address` defaults ON, so restarting a server does not lose to the dead\\n+process's lingering socket. It does not make a real collision quiet: binding an\\n+address and port another socket is still listening on fails with\\n+`AddressInUse` whether reuse is on or off. What reuse permits is binding over\\n+the remains of a connection that has already closed. Pass\\n+`reuse_address: false` to require a completely unused address.\\n+\\n+Two options apply to a connection rather than a listener, and both return\\n+`Result` because a refused option must not look like a set one:\\n+\\n+```saw-fragment\\n+public func set_no_delay(&self, on: Bool) -> Result<Void, IoError>\\n+public func set_keepalive(&self, on: Bool) -> Result<Void, IoError>\\n+```\\n+\\n+`no_delay` is ON by default on every accepted and dialled connection. With\\n+coalescing on instead, a small reply can sit in the kernel waiting for more data\\n+to accompany it while the peer waits for the reply, and the pair resolves only\\n+when a delayed-acknowledgement timer fires. Turn it off for a stream of many\\n+small writes where throughput matters more than latency. `keepalive` is off by\\n+default; its probe interval belongs to the system, and for a bound the\\n+application controls, give the read a `timeout` instead.\\n+\\n+The curated set stops there. Receive and send buffer sizes, `linger`,\\n+`reuse_port` and a raw `setsockopt` escape hatch are all deliberately absent —\\n+`reuse_port` because the name means different things on Linux and BSD, and the\\n+rest because nothing has needed them.\\n+\\n+### Writes to a closed peer\\n+\\n+A write to a socket whose peer has gone reports `IoErrorKind.BrokenPipe`. It\\n+never raises `SIGPIPE`, whose default action would end the process.\\n+\\n+The suppression is per socket — `SO_NOSIGPIPE` on macOS, `MSG_NOSIGNAL` on the\\n+send on Linux — rather than a process-wide change to the signal's disposition.\\n+A process-wide change would be inherited by every child the program spawns,\\n+including shells and pipelines it did not write, so pipes keep their ordinary\\n+behaviour and only sockets are affected.\\n \\n \\n **The host argument of `connect` is an IPv4 address or a name.** A dotted quad\\ndiff --git a/README.md b/README.md\\nindex 660ac193..9ef084d4 100644\\n--- a/README.md\\n+++ b/README.md\\n@@ -702,7 +702,10 @@ The standard library lives in `sawc/std/` and includes:\\n - **std.net** — `TcpListener` / `TcpStream`, owning and cooperative. `accept`,\\n   `read`, `read_into` and `connect` each take an optional `timeout: Duration`\\n   and answer `Timed<T>`, so a server can drop an idle connection instead of\\n-  waiting for a peer that never speaks.\\n+  waiting for a peer that never speaks. Socket options are labelled parameters\\n+  with defaults rather than an options object; `reuse_address` and `no_delay`\\n+  are on by default, and a write to a peer that has hung up returns a\\n+  `BrokenPipe` error instead of ending the process.\\n - **The filesystem** — `File` (std.file), `Directory` (std.directory), `Path`\\n   (std.path) and `Env` (std.env), one module each; every fallible operation\\n   returns a `Result`.\\ndiff --git a/examples/errors/match_on_suspending_call_in_a_loop.saw b/examples/errors/match_on_suspending_call_in_a_loop.saw\\nnew file mode 100644\\nindex 00000000..fb2844bd\\n--- /dev/null\\n+++ b/examples/errors/match_on_suspending_call_in_a_loop.saw\\n@@ -0,0 +1,45 @@\\n+// XFAIL: SL-273 — a match arm PAYLOAD BINDING is lost when a SUSPENSION\\n+// precedes the match inside a loop. The compiler exits with `internal compiler\\n+// error ... Undefined variable: e` instead of compiling this program.\\n+//\\n+// The scrutinee here is an ordinary NON-suspending call, deliberately: the\\n+// first filing blamed the scrutinee and was wrong. The trigger is the pair\\n+// (a suspension earlier in the loop body, a match arm payload binding after\\n+// it). Three facts pin the shape, and all three are on the issue: the same\\n+// match with the suspension AFTER it compiles; the same match outside a loop\\n+// compiles; and an `if let` payload binding in exactly this position compiles.\\n+// That last one is the useful discriminator — optional bindings survive the\\n+// resume-state split and match arm bindings do not.\\n+//\\n+// This program is CORRECT Saw. Intended behaviour is the EXPECT block: the\\n+// loop suspends on a write, `attempt` refuses once `i` passes 1, the arm binds\\n+// the error and prints it, and the loop breaks. When the bug is fixed this\\n+// XPASSes and the marker comes off.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// err nope\\n+\\n+import std.net.{TcpStream}\\n+\\n+func attempt(n: Int) -> Result<Int, String> {\\n+    if n > 1 { return \\\"nope\\\" }\\n+    return n\\n+}\\n+\\n+func main() {\\n+    let (a, b) = TcpStream.pair()\\n+    let _ = move b\\n+    var i = 0\\n+    while i < 4 {\\n+        let _ = a.write(\\\"x\\\")        // the suspension, NOT the scrutinee\\n+        match attempt(i) {\\n+            case Ok(_) -> {},\\n+            case Err(e) -> {\\n+                print(\\\"err {}\\\", e)\\n+                break\\n+            }\\n+        }\\n+        i = i + 1\\n+    }\\n+    let _ = move a\\n+}\\ndiff --git a/examples/net_listen_bind_address.saw b/examples/net_listen_bind_address.saw\\nindex 920da108..961542d8 100644\\n--- a/examples/net_listen_bind_address.saw\\n+++ b/examples/net_listen_bind_address.saw\\n@@ -1,10 +1,31 @@\\n+// Explicit IPv4 listener binding, and the bind-time options (designs 84, 272\\n+// unit 2).\\n+//\\n+// The conflict row CHANGED with design 272 unit 2, and the reason is the point\\n+// of the row. It used to hold a WILDCARD listener (0.0.0.0:P) and assert that\\n+// binding 127.0.0.1:P was refused — an OVERLAP, not a duplicate. With\\n+// `reuse_address` defaulting ON that bind now SUCCEEDS on macOS (probed: the\\n+// SECOND socket's SO_REUSEADDR is what decides, so the pre-existing wildcard\\n+// listener does not protect it), which is the behaviour the ruled default was\\n+// chosen for rather than a regression.\\n+//\\n+// So the row now asserts the thing it was always FOR — \\\"this exact address is\\n+// already taken is still an honest error\\\" — using an EXACT duplicate, which both\\n+// kernels refuse with reuse on or off. The wildcard/specific overlap is\\n+// deliberately NOT asserted here: its Linux semantics differ from BSD's and are\\n+// unverified from this checkout, and a suite row is the wrong place to pin a\\n+// behaviour nobody has measured on both hosts.\\n // EXPECT: success\\n // EXPECT-OUTPUT:\\n // default true\\n // wildcard true\\n-// conflict refused\\n+// exact conflict refused\\n+// exact conflict refused without reuse\\n+// no-reuse bind true\\n+// backlog bind true\\n // invalid host\\n // invalid port\\n+// invalid backlog\\n \\n import std.net.{TcpListener}\\n func main() {\\n@@ -12,10 +33,33 @@ func main() {\\n     print(\\\"default {local.local_port() > 0}\\\")\\n     let all = try! TcpListener.listen(0, host: \\\"0.0.0.0\\\")\\n     print(\\\"wildcard {all.local_port() > 0}\\\")\\n-    let conflict = TcpListener.listen(all.local_port(), host: \\\"127.0.0.1\\\")\\n-    match conflict { case Err(_) -> print(\\\"conflict refused\\\"), case Ok(_) -> print(\\\"wrong conflict\\\") }\\n+\\n+    // An EXACT duplicate: the same address AND port the live listener holds.\\n+    // Refused whatever reuse says — reuse permits binding over a CLOSED\\n+    // connection's remains, never over a socket that is still listening.\\n+    let held = try! TcpListener.listen(0, host: \\\"127.0.0.1\\\")\\n+    let conflict = TcpListener.listen(held.local_port(), host: \\\"127.0.0.1\\\")\\n+    match conflict {\\n+        case Err(_) -> print(\\\"exact conflict refused\\\"),\\n+        case Ok(_) -> print(\\\"WRONG: exact conflict allowed\\\")\\n+    }\\n+    let conflict_no_reuse = TcpListener.listen(held.local_port(), host: \\\"127.0.0.1\\\",\\n+                                               reuse_address: false)\\n+    match conflict_no_reuse {\\n+        case Err(_) -> print(\\\"exact conflict refused without reuse\\\"),\\n+        case Ok(_) -> print(\\\"WRONG: exact conflict allowed without reuse\\\")\\n+    }\\n+\\n+    // The opt-out and the backlog parameter both bind an unused address.\\n+    let no_reuse = try! TcpListener.listen(0, reuse_address: false)\\n+    print(\\\"no-reuse bind {no_reuse.local_port() > 0}\\\")\\n+    let deep = try! TcpListener.listen(0, backlog: 128)\\n+    print(\\\"backlog bind {deep.local_port() > 0}\\\")\\n+\\n     let bad_host = TcpListener.listen(0, host: \\\"localhost\\\")\\n     match bad_host { case Err(_) -> print(\\\"invalid host\\\"), case Ok(_) -> print(\\\"wrong host\\\") }\\n     let bad_port = TcpListener.listen(-1, host: \\\"0.0.0.0\\\")\\n     match bad_port { case Err(_) -> print(\\\"invalid port\\\"), case Ok(_) -> print(\\\"wrong port\\\") }\\n+    let bad_backlog = TcpListener.listen(0, backlog: 0)\\n+    match bad_backlog { case Err(_) -> print(\\\"invalid backlog\\\"), case Ok(_) -> print(\\\"wrong backlog\\\") }\\n }\\ndiff --git a/examples/net_socket_options.saw b/examples/net_socket_options.saw\\nnew file mode 100644\\nindex 00000000..bc699815\\n--- /dev/null\\n+++ b/examples/net_socket_options.saw\\n@@ -0,0 +1,50 @@\\n+// Socket options on a live connection (design 272 unit 2, SL-229).\\n+//\\n+// Both setters return `Result` and both are exercised in both directions, which\\n+// is the whole contract: an option that could not be set SAYS so rather than\\n+// doing nothing quietly. The values are not read back — there is no getter in\\n+// the curated surface and adding one to test the setter would be testing the\\n+// test — so what this pins is that the portable tag reaches a real `setsockopt`\\n+// on this host and is accepted. A tag the host did not know would come back\\n+// `Err(InvalidArgument)` from the runtime's own refusal, which is the failure\\n+// this would catch.\\n+//\\n+// `no_delay` is ON by default on every accepted and dialled connection (the\\n+// ruled default), so the `false` call here is the one that changes anything.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// no_delay off ok\\n+// no_delay on ok\\n+// keepalive on ok\\n+// keepalive off ok\\n+// wrote 5 bytes worth\\n+\\n+import std.net.{TcpListener, TcpStream}\\n+\\n+func main() {\\n+    let listener = try! TcpListener.listen(0)\\n+    let client = try! TcpStream.connect(\\\"127.0.0.1\\\", listener.local_port())\\n+    let server = try! listener.accept()\\n+\\n+    match client.set_no_delay(false) {\\n+        case Ok(_) -> print(\\\"no_delay off ok\\\"),\\n+        case Err(e) -> print(\\\"no_delay off FAILED {}\\\", e)\\n+    }\\n+    match client.set_no_delay(true) {\\n+        case Ok(_) -> print(\\\"no_delay on ok\\\"),\\n+        case Err(e) -> print(\\\"no_delay on FAILED {}\\\", e)\\n+    }\\n+    match client.set_keepalive(true) {\\n+        case Ok(_) -> print(\\\"keepalive on ok\\\"),\\n+        case Err(e) -> print(\\\"keepalive on FAILED {}\\\", e)\\n+    }\\n+    match client.set_keepalive(false) {\\n+        case Ok(_) -> print(\\\"keepalive off ok\\\"),\\n+        case Err(e) -> print(\\\"keepalive off FAILED {}\\\", e)\\n+    }\\n+\\n+    // The connection still works after all that.\\n+    try! client.write(\\\"hello\\\")\\n+    let got = try! server.read()\\n+    print(\\\"wrote {} bytes worth\\\", got.len())\\n+}\\ndiff --git a/examples/net_write_to_closed_peer_is_an_error.saw b/examples/net_write_to_closed_peer_is_an_error.saw\\nnew file mode 100644\\nindex 00000000..0fb4c1cf\\n--- /dev/null\\n+++ b/examples/net_write_to_closed_peer_is_an_error.saw\\n@@ -0,0 +1,67 @@\\n+// A write to a socket whose peer has gone is an ERROR, not a dead process\\n+// (design 272 unit 2, D3-3).\\n+//\\n+// By default a write to a closed socket raises SIGPIPE, whose default action\\n+// TERMINATES the process. A server would then lose to any client that hung up\\n+// mid-reply — silently, with nothing in the log, because there is no error to\\n+// report when the process is already gone. That is the opposite of what a\\n+// language surfacing failures as `Result` promises.\\n+//\\n+// The runtime suppresses SIGPIPE PER SOCKET (`SO_NOSIGPIPE` on macOS,\\n+// `MSG_NOSIGNAL` on the send on Linux), so the write reports `EPIPE`, which\\n+// std.net classifies as `BrokenPipe`. Deliberately NOT a process-wide\\n+// `SIG_IGN`: that disposition is inherited across `execve`, so it would follow\\n+// every child a Saw program spawns into shells and pipelines it did not write.\\n+//\\n+// If this ever regresses it does not fail with a wrong line of output — the\\n+// process dies on a signal and the runner reports THAT, which is exactly the\\n+// symptom being pinned. Reaching the last line is half the assertion.\\n+//\\n+// The peer is closed before the first write, which may still be accepted into a\\n+// socket buffer whose reader is gone, so `write_once` is called until it is\\n+// refused: WHICH write earns the EPIPE is a kernel buffering detail, that SOME\\n+// write does is the guarantee.\\n+//\\n+// `write_once` exists because the match must not sit in a loop that suspends\\n+// ahead of it — that is SL-273, pinned by\\n+// `examples/errors/match_on_suspending_call_in_a_loop.saw`. Keeping the match in\\n+// a loop-free helper keeps an unrelated open bug out of a test about sockets.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// write refused: broken pipe\\n+// still running\\n+\\n+import std.net.{TcpStream, IoErrorKind}\\n+\\n+// 0 = accepted, 1 = refused with BrokenPipe, 2 = refused with anything else.\\n+func write_once(s: &TcpStream) -> Int {\\n+    match s.write(\\\"payload\\\") {\\n+        case Ok(_) -> 0,\\n+        case Err(e) -> if e.kind() == IoErrorKind.BrokenPipe { 1 } else { 2 }\\n+    }\\n+}\\n+\\n+func main() {\\n+    let (a, b) = TcpStream.pair()\\n+    let _ = move b                      // the peer is gone as of here\\n+\\n+    var i = 0\\n+    var verdict = 0\\n+    while i < 64 {\\n+        verdict = write_once(&a)\\n+        if verdict != 0 {\\n+            break\\n+        }\\n+        i = i + 1\\n+    }\\n+\\n+    if verdict == 1 {\\n+        print(\\\"write refused: broken pipe\\\")\\n+    } else if verdict == 2 {\\n+        print(\\\"write refused with the WRONG kind\\\")\\n+    } else {\\n+        print(\\\"WRONG: 64 writes to a closed peer all succeeded\\\")\\n+    }\\n+    print(\\\"still running\\\")\\n+    let _ = move a\\n+}\\ndiff --git a/sawc/rt/ABI.md b/sawc/rt/ABI.md\\nindex 48f56d76..75515642 100644\\n--- a/sawc/rt/ABI.md\\n+++ b/sawc/rt/ABI.md\\n@@ -284,6 +284,38 @@ Stamp the OS-divergent prefix of a `struct sockaddr_in` at `buf` — the ONLY pa\\n whose layout differs by OS. macOS: `{ u8 sin_len=16; u8 sin_family=AF_INET }`;\\n Linux: `{ u16 sin_family=AF_INET }` (LE). `AF_INET==2` on both.\\n \\n+### Runtime-INTERNAL socket helpers (design 272 unit 2)\\n+Not `__saw_rt_*` and not part of the frozen seam set — these are how\\n+`rt/common/os_ops.saw` stays OS-independent, the same arrangement\\n+`__saw_epoll_event_size` already uses. A runtime provider supplies them for its\\n+own host and nothing outside `rt/` may call them.\\n+\\n+They live in `shim.c`, for the reason `__saw_open_flags` does: every value here\\n+is a C MACRO whose number differs by host, and C is the only language in the\\n+build that can read it. Writing them into the Saw runtime would mean hardcoding\\n+`SOL_SOCKET` as `0xffff` on one host and `1` on the other and hoping every\\n+future platform agreed; asking the headers cannot drift.\\n+\\n+`__saw_sockopt_level(option) -> word` / `__saw_sockopt_name(option) -> word`\\n+map a portable option tag to this host's `(level, name)`, or `-1` for an option\\n+this host lacks.\\n+\\n+`__saw_socket_suppress_sigpipe(fd) -> void` and\\n+`__saw_socket_send_flags() -> word` are the two halves of ONE contract: **a\\n+write to a socket whose peer has gone reports `EPIPE`; it never raises\\n+SIGPIPE.** macOS sets `SO_NOSIGPIPE` on the socket and sends with flags `0`;\\n+Linux has no such option and sends with `MSG_NOSIGNAL`. Every socket the\\n+runtime creates — listener, accepted connection, dialled connection — gets the\\n+suppression, and `__saw_rt_tcp_write` passes the flags.\\n+\\n+This is deliberately PER SOCKET rather than the process-wide `SIG_IGN` most\\n+runtimes install at startup. An ignored disposition is inherited across\\n+`execve`, and `rt/common/proc.saw` does not reset dispositions before `execvp`,\\n+so a process-wide ignore would be handed to every child a Saw program spawns,\\n+including shells and pipelines it did not write. Pipes keep their behaviour;\\n+only sockets change. The user-visible consequence is that a write to a hung-up\\n+client is an ordinary `Err(IoError)` with kind `BrokenPipe`.\\n+\\n ## Status-carrying network ops (design 117)\\n \\n Each does its syscall(s) and returns `>= 0` on success/count or `-tag` on failure.\\n@@ -302,6 +334,39 @@ interfaces. The original listen seam delegates here with loopback, preserving\\n existing callers. `TcpListener.listen(port, host: \\\"0.0.0.0\\\")` exposes the explicit\\n address path; the host must be a dotted IPv4 literal and the port 0..65535.\\n \\n+### `__saw_rt_tcp_listen_with(addr_be: word, port: word, reuse_address: word, backlog: word) -> word`\\n+**ADDITIVE (design 272 unit 2, SL-229).** The same nonblocking listener and\\n+error contract, carrying the options a listening socket can only be given\\n+BETWEEN `socket()` and `bind()` — which is why they cannot be setters on the\\n+listener the two seams above return: by then the window has closed.\\n+`reuse_address` non-zero sets `SO_REUSEADDR` before the bind; `backlog` is the\\n+`listen(2)` queue depth. `__saw_rt_tcp_listen_on` now delegates HERE with\\n+`reuse_address = 1` and backlog 16 (the value it previously hardcoded), and\\n+`__saw_rt_tcp_listen` delegates to that with loopback — so no existing seam\\n+changed signature and a runtime implementing only the older pair still links.\\n+\\n+Reuse does not make a genuine collision quiet: two sockets bound to the same\\n+address AND port still refuse the second with `AddrInUse`. What it permits is\\n+binding over the remains of a connection that has already closed.\\n+\\n+### `__saw_rt_socket_set_option(fd: word, option: word, value: word) -> word`\\n+**ADDITIVE (design 272 unit 2, SL-229).** Sets one socket option by PORTABLE\\n+TAG → `0` or `-tag`. The option tag space is the table below; the host maps each\\n+tag to its own `(level, name)` pair, exactly as it maps its errno numbers to the\\n+`SysError` space, because the platform constants disagree (`SOL_SOCKET` is\\n+`0xffff` on macOS and `1` on Linux; `SO_REUSEADDR` is `4` and `2`). `value` is a\\n+C `int`; an option needing a different value shape would take its own seam.\\n+\\n+An option this host does not have returns `-Invalid` rather than succeeding\\n+quietly — a setting that silently did nothing is the silent degradation the\\n+never-hide-errors rule forbids.\\n+\\n+| Tag | Option        | macOS                      | Linux                    |\\n+|-----|---------------|----------------------------|--------------------------|\\n+| 1   | NoDelay       | `IPPROTO_TCP`/`TCP_NODELAY` | `IPPROTO_TCP`/`TCP_NODELAY` |\\n+| 2   | KeepAlive     | `SOL_SOCKET`/`SO_KEEPALIVE` | `SOL_SOCKET`/`SO_KEEPALIVE` |\\n+| 3   | ReuseAddress  | `SOL_SOCKET`/`SO_REUSEADDR` | `SOL_SOCKET`/`SO_REUSEADDR` |\\n+\\n ### `__saw_rt_tcp_local_port(fd: word) -> word`\\n `getsockname` → the bound local port (resolves an ephemeral 0).\\n \\ndiff --git a/sawc/rt/common/os_ops.saw b/sawc/rt/common/os_ops.saw\\nindex 1720db76..ec5513dd 100644\\n--- a/sawc/rt/common/os_ops.saw\\n+++ b/sawc/rt/common/os_ops.saw\\n@@ -52,6 +52,18 @@ extern \\\"C\\\" {\\n     func __saw_rt_set_nonblocking(fd: Int) -> Int\\n     func __saw_rt_sin_set_family(buf: UnsafePointer<Int8>)\\n     func __saw_rt_last_syserror() -> Int\\n+    // design 272 unit 2 (SL-229): the socket-option seams. The portable option\\n+    // TAG -> this host's (level, name), `-1` for an option this host lacks; the\\n+    // per-host SIGPIPE suppression; and the flags every socket send passes.\\n+    // Same shape as `sin_set_family` above — the divergence lives in net_os.saw\\n+    // and this file stays OS-independent.\\n+    func __saw_sockopt_level(option: Int) -> Int\\n+    func __saw_sockopt_name(option: Int) -> Int\\n+    func __saw_socket_suppress_sigpipe(fd: Int)\\n+    func __saw_socket_send_flags() -> Int\\n+    func setsockopt(fd: Int32, level: Int32, name: Int32,\\n+                    value: UnsafePointer<Int8>, len: Int32) -> Int32\\n+    func send(fd: Int32, buf: UnsafePointer<Int8>, len: Int, flags: Int32) -> Int\\n }\\n \\n // The portable SysError tag space (ABI.md). A failing op returns the NEGATED tag.\\n@@ -89,6 +101,11 @@ static AF_INET: Int32 = 2\\n static SOCK_STREAM: Int32 = 1\\n static PROTO_DEFAULT: Int32 = 0\\n static LISTEN_BACKLOG: Int32 = 16\\n+// Portable socket-option tags (rt/ABI.md). The host says what each one means\\n+// (host_*/net_os.saw); this file only ever names the portable number.\\n+static OPT_NO_DELAY: Int = 1\\n+static OPT_KEEPALIVE: Int = 2\\n+static OPT_REUSE_ADDRESS: Int = 3\\n static SOCKADDR_IN_LEN: Int32 = 16\\n static LOOPBACK_BE: UInt32 = 0x0100007f   // 127.0.0.1, network byte order (LE)\\n \\n@@ -152,13 +169,45 @@ func rt_tcp_listen(port: Int) unsafe -> Int {\\n }\\n \\n // Explicit address, network-order IPv4 bits carried in a platform word.\\n+//\\n+// design 272 unit 2: a thin forwarder onto the options-carrying op below, with\\n+// the RULED defaults — `reuse_address` ON, the backlog this file has always\\n+// used. Kept rather than rewritten so a runtime provider that implements only\\n+// the v2 seam set still links, and picks up the new default for free.\\n @export(\\\"__saw_rt_tcp_listen_on\\\")\\n func rt_tcp_listen_on(addr_be: Int, port: Int) unsafe -> Int {\\n+    rt_tcp_listen_with(addr_be, port, 1, LISTEN_BACKLOG as Int)\\n+}\\n+\\n+// design 272 unit 2 (SL-229): bind + listen with the options a listener can only\\n+// be given BETWEEN `socket()` and `bind()`.\\n+//\\n+// `reuse_address` is why this seam exists at all. `SO_REUSEADDR` has to be set on\\n+// the socket before it is bound, so it cannot be a setter on the returned\\n+// listener — the window is already closed by then. It defaults ON one layer up\\n+// (std.net's `listen`), which is the ruled Go-style behaviour: restarting a\\n+// server does not lose to the dead process's TIME_WAIT socket.\\n+//\\n+// What it does NOT do is make a genuine collision quiet. Two sockets bound to\\n+// the SAME address and port still refuse the second with `EADDRINUSE`, reuse or\\n+// no reuse; what reuse permits is binding over the remains of a connection that\\n+// has already closed.\\n+@export(\\\"__saw_rt_tcp_listen_with\\\")\\n+func rt_tcp_listen_with(addr_be: Int, port: Int, reuse_address: Int,\\n+                        backlog: Int) unsafe -> Int {\\n     let fd = socket(AF_INET, SOCK_STREAM, PROTO_DEFAULT)\\n     if fd < (0 as Int32) {\\n         return -__saw_rt_last_syserror()\\n     }\\n     let _ = __saw_rt_set_nonblocking(fd as Int)\\n+    __saw_socket_suppress_sigpipe(fd as Int)\\n+    if reuse_address != 0 {\\n+        let r = rt_socket_set_option(fd as Int, OPT_REUSE_ADDRESS, 1)\\n+        if r < 0 {\\n+            let _ = close(fd)\\n+            return r\\n+        }\\n+    }\\n     var sa = ipv4_sockaddr(addr_be as UInt32, port)\\n     let ap = (&sa) as UnsafePointer<Int8>\\n     if bind(fd, ap, SOCKADDR_IN_LEN) != (0 as Int32) {\\n@@ -166,7 +215,7 @@ func rt_tcp_listen_on(addr_be: Int, port: Int) unsafe -> Int {\\n         let _ = close(fd)\\n         return -e\\n     }\\n-    if listen(fd, LISTEN_BACKLOG) != (0 as Int32) {\\n+    if listen(fd, backlog as Int32) != (0 as Int32) {\\n         let e = __saw_rt_last_syserror()\\n         let _ = close(fd)\\n         return -e\\n@@ -174,6 +223,30 @@ func rt_tcp_listen_on(addr_be: Int, port: Int) unsafe -> Int {\\n     fd as Int\\n }\\n \\n+// design 272 unit 2: set one socket option, by PORTABLE TAG. Returns 0 or -tag.\\n+//\\n+// The value is a C `int` in every option this space carries, which is what lets\\n+// one seam serve them all; an option needing a different value shape would need\\n+// its own seam rather than a wider `value` here.\\n+//\\n+// An option this host does not have is a REFUSAL (`Invalid`), never a silent\\n+// no-op: a caller that asked for `no_delay` and got nothing, quietly, would\\n+// debug the wrong thing for a long time.\\n+@export(\\\"__saw_rt_socket_set_option\\\")\\n+func rt_socket_set_option(fd: Int, option: Int, value: Int) unsafe -> Int {\\n+    let level = __saw_sockopt_level(option)\\n+    let name = __saw_sockopt_name(option)\\n+    if level < 0 || name < 0 {\\n+        return 0 - (SysError.Invalid as UInt8) as Int\\n+    }\\n+    var v: Int32 = value as Int32\\n+    if setsockopt(fd as Int32, level as Int32, name as Int32,\\n+                  (&v) as UnsafePointer<Int8>, 4 as Int32) != (0 as Int32) {\\n+        return 0 - __saw_rt_last_syserror()\\n+    }\\n+    0\\n+}\\n+\\n // The actual local port a (possibly ephemeral) listen fd is bound to.\\n @export(\\\"__saw_rt_tcp_local_port\\\")\\n func rt_tcp_local_port(fd: Int) unsafe -> Int {\\n@@ -196,6 +269,11 @@ func rt_tcp_accept(listen_fd: Int) unsafe -> Int {\\n     let c = accept(listen_fd as Int32, ap, lp)\\n     if c >= (0 as Int32) {\\n         let _ = __saw_rt_set_nonblocking(c as Int)\\n+        // design 272 unit 2: an ACCEPTED socket does not inherit the listener's\\n+        // options, so the hardening is re-applied per connection — this is the\\n+        // fd a server actually writes to, and the one a client can hang up on.\\n+        __saw_socket_suppress_sigpipe(c as Int)\\n+        let _ = rt_socket_set_option(c as Int, OPT_NO_DELAY, 1)\\n         return c as Int\\n     }\\n     return 0 - __saw_rt_last_syserror()\\n@@ -212,6 +290,12 @@ func rt_tcp_connect_start(addr_be: Int, port: Int) unsafe -> Int {\\n         return -__saw_rt_last_syserror()\\n     }\\n     let _ = __saw_rt_set_nonblocking(fd as Int)\\n+    // design 272 unit 2: same hardening as the accept path, for the same reason\\n+    // — a dialled socket is written to and can be hung up on just as an accepted\\n+    // one can. Set BEFORE `connect`, which is the last moment it is certainly\\n+    // still ours alone.\\n+    __saw_socket_suppress_sigpipe(fd as Int)\\n+    let _ = rt_socket_set_option(fd as Int, OPT_NO_DELAY, 1)\\n     var sa = ipv4_sockaddr(addr_be as UInt32, port)\\n     let ap = (&sa) as UnsafePointer<Int8>\\n     let r = connect(fd, ap, SOCKADDR_IN_LEN)\\n@@ -259,9 +343,16 @@ func rt_tcp_read(fd: Int, buf: UnsafePointer<Int8>, len: Int) unsafe -> Int {\\n }\\n \\n // Nonblocking write: bytes written (may be < len), or -tag.\\n+//\\n+// design 272 unit 2 (D3-3): `send` rather than `write`, so Linux can pass\\n+// `MSG_NOSIGNAL` and a write to a hung-up peer reports `EPIPE` instead of\\n+// raising SIGPIPE, whose default action would kill the process. macOS passes 0\\n+// here — it suppressed SIGPIPE on the socket at creation — and `send(fd, buf,\\n+// len, 0)` is `write(fd, buf, len)` on a socket, so the change costs that host\\n+// nothing.\\n @export(\\\"__saw_rt_tcp_write\\\")\\n func rt_tcp_write(fd: Int, buf: UnsafePointer<Int8>, len: Int) unsafe -> Int {\\n-    let n = write(fd as Int32, buf, len)\\n+    let n = send(fd as Int32, buf, len, __saw_socket_send_flags() as Int32)\\n     if n >= 0 {\\n         return n\\n     }\\ndiff --git a/sawc/rt/shim.c b/sawc/rt/shim.c\\nindex 028eed76..db15283c 100644\\n--- a/sawc/rt/shim.c\\n+++ b/sawc/rt/shim.c\\n@@ -26,6 +26,10 @@\\n #include <pthread.h>\\n #include <fcntl.h>\\n #include <unistd.h>\\n+/* design 272 unit 2: the socket-option and SIGPIPE-suppression macros. */\\n+#include <sys/socket.h>\\n+#include <netinet/in.h>\\n+#include <netinet/tcp.h>\\n \\n /* ---- DF-113a: no extern C global ---------------------------------------\\n  * `__saw_rt_write`/`_panic` route through C stdio's `stdout` FILE* (spelled\\n@@ -227,6 +231,70 @@ long __saw_rt_set_nonblocking(long fd) {\\n     return 0;\\n }\\n \\n+/* ---- design 272 unit 2 (SL-229): socket options ------------------------\\n+ * The portable option TAG -> this host's (level, name). Here for the same\\n+ * reason `__saw_open_flags` is here: these are C MACROS whose values differ by\\n+ * host, and C is the one language that can read them. Writing the numbers into\\n+ * the Saw runtime would mean hardcoding 0xffff/0x1022 on one host and 1/9 on\\n+ * the other and hoping every future platform agrees; asking the headers cannot\\n+ * drift. `-1` means this host has no such option, which\\n+ * `__saw_rt_socket_set_option` turns into a refusal rather than a silent no-op.\\n+ *\\n+ * Tags are the table in ABI.md: 1 NoDelay, 2 KeepAlive, 3 ReuseAddress.\\n+ */\\n+long __saw_sockopt_level(long option) {\\n+    switch (option) {\\n+        case 1: return IPPROTO_TCP;\\n+        case 2: return SOL_SOCKET;\\n+        case 3: return SOL_SOCKET;\\n+        default: return -1;\\n+    }\\n+}\\n+\\n+long __saw_sockopt_name(long option) {\\n+    switch (option) {\\n+        case 1: return TCP_NODELAY;\\n+        case 2: return SO_KEEPALIVE;\\n+        case 3: return SO_REUSEADDR;\\n+        default: return -1;\\n+    }\\n+}\\n+\\n+/* The two halves of ONE contract (design 272 D3-3): a write to a socket whose\\n+ * peer has gone reports EPIPE and never raises SIGPIPE, whose default action\\n+ * would kill the process — so a server cannot be killed by a client that hung\\n+ * up, and the failure arrives as an ordinary Result the handler can answer.\\n+ *\\n+ * macOS has SO_NOSIGPIPE on the socket; Linux has MSG_NOSIGNAL on the send.\\n+ * Both hosts define both functions so rt/common/os_ops.saw calls one name\\n+ * unconditionally, and each is a no-op on the host that uses the other half.\\n+ *\\n+ * PER SOCKET rather than a process-wide SIG_IGN: an ignored disposition is\\n+ * inherited across execve and rt/common/proc.saw does not reset dispositions\\n+ * before execvp, so a process-wide ignore would reach every child a Saw program\\n+ * spawns. Pipes keep their behaviour; only sockets change.\\n+ *\\n+ * The setsockopt return is ignored deliberately: this is hardening the runtime\\n+ * applies to every socket it creates, not something a caller asked for, and a\\n+ * kernel that refused it still hands back a working socket.\\n+ */\\n+void __saw_socket_suppress_sigpipe(long fd) {\\n+#ifdef SO_NOSIGPIPE\\n+    int one = 1;\\n+    (void)setsockopt((int)fd, SOL_SOCKET, SO_NOSIGPIPE, &one, sizeof(one));\\n+#else\\n+    (void)fd;\\n+#endif\\n+}\\n+\\n+long __saw_socket_send_flags(void) {\\n+#ifdef MSG_NOSIGNAL\\n+    return MSG_NOSIGNAL;\\n+#else\\n+    return 0;\\n+#endif\\n+}\\n+\\n /* ---- DF-113b: the blocking-extern offload thread thunk ------------------\\n  * The offload seams `__saw_rt_offload_start/done/pipe_fd/take` are authored in\\n  * Saw (sawc/rt/common/offload.saw); this thunk is the ONE piece that must be C\\ndiff --git a/sawc/runtime_abi.py b/sawc/runtime_abi.py\\nindex b76ce4a1..3627bac2 100644\\n--- a/sawc/runtime_abi.py\\n+++ b/sawc/runtime_abi.py\\n@@ -45,6 +45,15 @@ RUNTIME_ABI_SYMBOLS = frozenset({\\n     # Status-carrying network ops (design 117)\\n     \\\"__saw_rt_tcp_listen\\\",\\n     \\\"__saw_rt_tcp_listen_on\\\",\\n+    # design 272 unit 2 (SL-229) — ADDITIVE. `listen_with` carries the options a\\n+    # listening socket can only be given between `socket()` and `bind()`, so\\n+    # they cannot be setters on the listener the older seams return;\\n+    # `socket_set_option` sets the ones a live connection takes, by PORTABLE TAG\\n+    # (the host maps each to its own level/name, as it already does for errno).\\n+    # Neither older listen seam is removed or re-signed: `listen_on` now\\n+    # forwards here with the ruled defaults.\\n+    \\\"__saw_rt_tcp_listen_with\\\",\\n+    \\\"__saw_rt_socket_set_option\\\",\\n     \\\"__saw_rt_tcp_local_port\\\",\\n     \\\"__saw_rt_tcp_accept\\\",\\n     \\\"__saw_rt_tcp_connect_start\\\",\\ndiff --git a/sawc/std/net.saw b/sawc/std/net.saw\\nindex b322d4c1..64d25328 100644\\n--- a/sawc/std/net.saw\\n+++ b/sawc/std/net.saw\\n@@ -39,6 +39,12 @@ extern \\\"C\\\" {\\n     // SysError tag on failure (rt/ABI.md) — the errno accessors are gone.\\n     func __saw_rt_tcp_listen(port: Int) -> Int\\n     func __saw_rt_tcp_listen_on(addr_be: Int, port: Int) -> Int\\n+    // design 272 unit 2 (SL-229): bind + listen carrying the options that can\\n+    // only be set between `socket()` and `bind()`, and the by-tag option setter\\n+    // for the ones a live connection takes.\\n+    func __saw_rt_tcp_listen_with(addr_be: Int, port: Int, reuse_address: Int,\\n+                                  backlog: Int) -> Int\\n+    func __saw_rt_socket_set_option(fd: Int, option: Int, value: Int) -> Int\\n     func __saw_rt_tcp_local_port(fd: Int) -> Int\\n     func __saw_rt_tcp_accept(listen_fd: Int) -> Int\\n     func __saw_rt_tcp_connect_start(addr_be: Int, port: Int) -> Int\\n@@ -128,6 +134,17 @@ static NET_ERROR: Int = -1           // the `tcp_*` error sentinel\\n // that used to spell it now build their error from `IoErrorKind.Interrupted`\\n // directly, through `io_cancelled`, because nothing classified an errno there\\n // and the raw code must stay 0.)\\n+// design 272 unit 2: the default listen backlog — the depth of the kernel's\\n+// queue of arrived-but-not-yet-accepted connections. Unchanged from the value\\n+// the runtime hardcoded before it became a parameter, so exposing it changed\\n+// nothing for a caller who does not name it.\\n+static DEFAULT_BACKLOG: Int = 16\\n+\\n+// design 272 unit 2: the portable socket-option tags (rt/ABI.md). The host maps\\n+// each to its own `(level, name)` pair; std never names a platform constant.\\n+static OPT_NO_DELAY: Int = 1\\n+static OPT_KEEPALIVE: Int = 2\\n+\\n static SYS_WOULD_BLOCK: Int = 1\\n static SYS_IN_PROGRESS: Int = 2\\n static SYS_NOT_FOUND: Int = 10\\n@@ -709,27 +726,35 @@ public struct TcpListener {\\n }\\n \\n extension TcpListener {\\n-    // Bind + listen on 127.0.0.1:port (port 0 = an ephemeral port; read it back\\n-    // with `local_port`). Returns the owning listener or an IoError.\\n-    public static func listen(port: Int) -> Result<TcpListener, IoError> {\\n-        let fd = __saw_rt_tcp_listen(port)\\n-        if fd < 0 {\\n-            return io_error(\\\"listen\\\", -fd)\\n-        }\\n-        return TcpListener(fd: fd)\\n-    }\\n-\\n-    /// Bind an IPv4 address explicitly. Use \\\"0.0.0.0\\\" for all IPv4 interfaces.\\n-    /// Only dotted IPv4 literals are accepted; listening never performs DNS.\\n-    /// The one-argument overload keeps its loopback-only default.\\n-    public static func listen(port: Int, host: String) -> Result<TcpListener, IoError> {\\n+    /// Binds and listens on `host`:`port`, returning the owning listener.\\n+    ///\\n+    /// `port` 0 requests an ephemeral port; read the one the OS chose back with\\n+    /// `local_port`. `host` takes a dotted IPv4 literal — use \\\"0.0.0.0\\\" for all\\n+    /// IPv4 interfaces — and listening never performs DNS.\\n+    ///\\n+    /// `reuse_address` defaults ON, so restarting a server does not lose to the\\n+    /// dead process's lingering socket. It does not make a real collision quiet:\\n+    /// binding an address and port another socket already holds still fails with\\n+    /// `AddressInUse`. Pass `reuse_address: false` to require a completely\\n+    /// unused address.\\n+    ///\\n+    /// `backlog` is the depth of the kernel's queue of connections that have\\n+    /// arrived but not yet been accepted.\\n+    public static func listen(port: Int,\\n+                              host: String = \\\"127.0.0.1\\\",\\n+                              reuse_address: Bool = true,\\n+                              backlog: Int = DEFAULT_BACKLOG) -> Result<TcpListener, IoError> {\\n         if port < 0 || port > 65535 {\\n             return IoError.of(syscall: \\\"listen: port must be 0..65535\\\", kind: IoErrorKind.InvalidArgument)\\n         }\\n+        if backlog < 1 {\\n+            return IoError.of(syscall: \\\"listen: backlog must be at least 1\\\", kind: IoErrorKind.InvalidArgument)\\n+        }\\n         guard let addr = parse_ipv4_literal(host) else {\\n             return IoError.of(syscall: \\\"listen: expected an IPv4 address\\\", kind: IoErrorKind.InvalidArgument)\\n         }\\n-        let fd = __saw_rt_tcp_listen_on(addr as Int, port)\\n+        let reuse = if reuse_address { 1 } else { 0 }\\n+        let fd = __saw_rt_tcp_listen_with(addr as Int, port, reuse, backlog)\\n         if fd < 0 { return io_error(\\\"listen\\\", -fd) }\\n         return TcpListener(fd: fd)\\n     }\\n@@ -993,6 +1018,32 @@ extension TcpStream {\\n         return Timed<TcpStream>.Value(value: TcpStream(fd: fd))\\n     }\\n \\n+    /// Sets whether small writes are sent immediately rather than coalesced.\\n+    ///\\n+    /// On by default, which is what a request/response protocol wants: with\\n+    /// coalescing on, a small reply can sit in the kernel waiting for more data\\n+    /// to accompany it while the peer waits for the reply, and the pair resolves\\n+    /// only when a delayed-acknowledgement timer fires. Turn it off for a\\n+    /// stream of many small writes where throughput matters more than latency.\\n+    public func set_no_delay(&self, on: Bool) -> Result<Void, IoError> {\\n+        let r = __saw_rt_socket_set_option(self.fd, OPT_NO_DELAY, if on { 1 } else { 0 })\\n+        if r < 0 { return io_error(\\\"set_no_delay\\\", -r) }\\n+        return\\n+    }\\n+\\n+    /// Sets whether the OS probes an idle connection to learn if the peer is\\n+    /// still there.\\n+    ///\\n+    /// Off by default. The probe interval is the system's, not this API's — it\\n+    /// is measured in hours on most hosts and the tuning knobs for it do not\\n+    /// mean the same thing across platforms. For a bound an application\\n+    /// controls, give the read a `timeout` instead.\\n+    public func set_keepalive(&self, on: Bool) -> Result<Void, IoError> {\\n+        let r = __saw_rt_socket_set_option(self.fd, OPT_KEEPALIVE, if on { 1 } else { 0 })\\n+        if r < 0 { return io_error(\\\"set_keepalive\\\", -r) }\\n+        return\\n+    }\\n+\\n     // A connected, nonblocking stream pair (AF_UNIX socketpair) — for tests and\\n     // local IPC, no bound port. Both ends are owning TcpStreams.\\n     public static func pair() -> (TcpStream, TcpStream) {\\n\",\"id\":\"SL-229\",\"request_id\":\"sl229-p1-attempt1\",\"title\":\"design 272 unit 2: socket options — reuse_address and no_delay default ON, per-socket SIGPIPE suppression, two additive seams (SL-229)\"}","request_id":"sl229-p1-attempt1","sequence":706,"timestamp":"1789228690"}
---
