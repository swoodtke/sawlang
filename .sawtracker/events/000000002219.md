---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-air-sawlang","closed":"","comments":[],"created":"1790181635","description":"Found by an SL-357 comment-review agent on Sep 23; I read the code on main fd2520e9.\n\n`std/directory.saw`: `public static func current() unsafe -> Path?`\n- panics with \"Directory.current: allocation failed\" when `malloc` refuses. That is not one of LANGUAGE_SPEC's five documented panic boundaries, and design 234 says every allocating std op returns `Result<_, AllocError>`.\n- returns `None` for every `getcwd` failure (EACCES, ENOENT for a deleted cwd, the ceiling reached), so the cause is erased. That breaks the never-hide-errors doctrine.\n\nIts sibling `set_current` already returns `Result<Void, IoError>` with the errno cause.\n\nThe retry loop cannot tell ERANGE apart from the other errors, because errno is not readable from std. The fix is a runtime seam (`__saw_rt_fs_getcwd` returning status-carrying results, following the os_ops.saw pattern) plus `current() -> Result<Path, IoError>`, with allocation refusal mapped into the error.\n\nThis is an API change, so it needs a ruling on the signature before dispatch. Consumer sweep: grep `Directory.current` across blade/, devtools/, libs/ and examples/.","id":"SL-359","labels":["std"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":2219,"stage":"backlog","status":"open","title":"Directory.current hides errors: returns Path? (a getcwd failure's cause is erased into None) and panics on allocation failure outside the documented panic boundaries","updated":"1790341682"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-air-sawlang","closed":"","comments":[],"created":"1790181635","description":"Found by an SL-357 comment-review agent on Sep 23; I read the code on main fd2520e9.\n\n`std/directory.saw`: `public static func current() unsafe -> Path?`\n- panics with \"Directory.current: allocation failed\" when `malloc` refuses. That is not one of LANGUAGE_SPEC's five documented panic boundaries, and design 234 says every allocating std op returns `Result<_, AllocError>`.\n- returns `None` for every `getcwd` failure (EACCES, ENOENT for a deleted cwd, the ceiling reached), so the cause is erased. That breaks the never-hide-errors doctrine.\n\nIts sibling `set_current` already returns `Result<Void, IoError>` with the errno cause.\n\nThe retry loop cannot tell ERANGE apart from the other errors, because errno is not readable from std. The fix is a runtime seam (`__saw_rt_fs_getcwd` returning status-carrying results, following the os_ops.saw pattern) plus `current() -> Result<Path, IoError>`, with allocation refusal mapped into the error.\n\nThis is an API change, so it needs a ruling on the signature before dispatch. Consumer sweep: grep `Directory.current` across blade/, devtools/, libs/ and examples/.","id":"SL-359","labels":["bug","std"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1264,"stage":"backlog","status":"open","title":"Directory.current hides errors: returns Path? (a getcwd failure's cause is erased into None) and panics on allocation failure outside the documented panic boundaries","updated":"1790181635"},"id":"SL-359","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-359\",\"labels\":[\"std\"],\"request_id\":\"pid-57019-1790341682-6000\",\"revision\":1}","request_id":"pid-57019-1790341682-6000","sequence":2219,"timestamp":"1790341682"}
---
