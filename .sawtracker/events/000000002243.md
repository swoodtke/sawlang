---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790262864","description":"Found by the SL-340 implementer (Sep 24) while taking the census of intercepted constructions. The cell is `c6`, in `.claude/worktrees/sl340/.build/scratch/`.\n\n`UnsafeMemory.write(v)` with an owning `T` is legal. `c6` writes a `String` into a `Normal` view, and it compiles and runs. SL-340 routes `write`'s argument through the value-transfer funnel, so ownership moves into the memory, exactly as with `ptr[i] = v`.\n\nThe `read()` side hands back an UNRETAINED bitwise alias of whatever is stored. That is:\n- for a refcounted `T`, a missing retain, so the next drop over-releases;\n- for a NoCopy or ExplicitCopy `T`, an implicit copy that the value-transfer rules would refuse anywhere else.\n\nQuestion for a brief:\n- Should `read()` follow the payload's copy tier? That means retaining a Copy-tier read, and demanding an explicit form for ExplicitCopy/NoCopy, just as place reads do (design 131/141).\n- Or should `UnsafeMemory<T>` views be restricted to trivial `T`, which is what MMIO registers are?\n\nThe second is a language rule and needs a user ruling. The first is consistency with existing place reads. Sweep `read` and any other accessor that returns a `T` by value out of a view.\n","id":"SL-379","labels":["language"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":2243,"stage":"backlog","status":"open","title":"UnsafeMemory.read() returns an unretained bitwise alias of an owning T: follow the payload's copy tier, or restrict views to trivial T (needs a ruling)","updated":"1790341687"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790262864","description":"Found by the SL-340 implementer (Sep 24) while taking the census of intercepted constructions. The cell is `c6`, in `.claude/worktrees/sl340/.build/scratch/`.\n\n`UnsafeMemory.write(v)` with an owning `T` is legal. `c6` writes a `String` into a `Normal` view, and it compiles and runs. SL-340 routes `write`'s argument through the value-transfer funnel, so ownership moves into the memory, exactly as with `ptr[i] = v`.\n\nThe `read()` side hands back an UNRETAINED bitwise alias of whatever is stored. That is:\n- for a refcounted `T`, a missing retain, so the next drop over-releases;\n- for a NoCopy or ExplicitCopy `T`, an implicit copy that the value-transfer rules would refuse anywhere else.\n\nQuestion for a brief:\n- Should `read()` follow the payload's copy tier? That means retaining a Copy-tier read, and demanding an explicit form for ExplicitCopy/NoCopy, just as place reads do (design 131/141).\n- Or should `UnsafeMemory<T>` views be restricted to trivial `T`, which is what MMIO registers are?\n\nThe second is a language rule and needs a user ruling. The first is consistency with existing place reads. Sweep `read` and any other accessor that returns a `T` by value out of a view.\n","id":"SL-379","labels":[],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1449,"stage":"backlog","status":"open","title":"UnsafeMemory.read() returns an unretained bitwise alias of an owning T: follow the payload's copy tier, or restrict views to trivial T (needs a ruling)","updated":"1790262864"},"id":"SL-379","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-379\",\"labels\":[\"language\"],\"request_id\":\"pid-57291-1790341687-6000\",\"revision\":1}","request_id":"pid-57291-1790341687-6000","sequence":2243,"timestamp":"1790341687"}
---
