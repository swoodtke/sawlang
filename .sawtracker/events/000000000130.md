---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791252","description":"## Scope and status\n\nDF-151m — FILED, NOT FIXED (typechecker; found while fixing DF-151j, Aug 7). &var into a projection rooted at a let binding compiles and mutates — the let…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 8463–8487\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L8463-L8487)\n\n- **DF‑151m — FILED, NOT FIXED (typechecker; found while fixing DF‑151j,\n  Aug 7).** **`&var` into a projection rooted at a `let` binding compiles and\n  mutates — the `let` promise is broken for fields, tuple elements AND fixed\n  array elements alike.**\n  ```saw\n  func bump(x: &var Int) { x = x + 1 }\n  let p = Pair(a: 1, b: 2)\n  bump(&var p.a)\n  print(\"{p.a}\")            // 2 — no error, and the `let` was written through\n  //                           `p.a = 2` on the same binding IS rejected\n  ```\n  PRE-EXISTING and not tuple-specific; tuples inherit it because DF‑151j made\n  them consistent with fields, which is the correct outcome for that unit and\n  the reason this is filed rather than fixed there. `_check_reference_expr`\n  checks `&var` mutability for an Identifier operand, for `self`, and for a\n  projection out of a `&self` receiver (`_projects_from_self`, DF‑146b) — but\n  there is no arm for a projection rooted at a LOCAL, so the walk\n  `_assign_target_immutable_struct_root` already performs for every assignment\n  target is simply never run on a reference operand.\n  Expected shape: run that same walk in `_check_reference_expr` when\n  `expr.mutable` and the operand is a projection, with the message the\n  assignment path gives. Blast radius is why it is its own unit — the rule\n  reaches every `&var` into a field or element in std, blade and the libs, and\n  any legitimate one written through a `let` root today becomes a compile error\n  that has to be re-spelled `var`.\n","id":"SL-130","labels":["todo-import","needs-verification","bug","plan","memory-safety"],"priority":"high","project":"SL","revision":1,"sequence":130,"status":"open","title":"DF-151m — FILED, NOT FIXED (typechecker; found while fixing DF-151j, Aug 7). &var into a projection rooted at a let binding compiles and mutates — the let…","updated":"1788791252"},"before":null,"id":"SL-130","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-151m — FILED, NOT FIXED (typechecker; found while fixing DF-151j, Aug 7). &var into a projection rooted at a let binding compiles and mutates — the let…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 8463–8487\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L8463-L8487)\\n\\n- **DF‑151m — FILED, NOT FIXED (typechecker; found while fixing DF‑151j,\\n  Aug 7).** **`&var` into a projection rooted at a `let` binding compiles and\\n  mutates — the `let` promise is broken for fields, tuple elements AND fixed\\n  array elements alike.**\\n  ```saw\\n  func bump(x: &var Int) { x = x + 1 }\\n  let p = Pair(a: 1, b: 2)\\n  bump(&var p.a)\\n  print(\\\"{p.a}\\\")            // 2 — no error, and the `let` was written through\\n  //                           `p.a = 2` on the same binding IS rejected\\n  ```\\n  PRE-EXISTING and not tuple-specific; tuples inherit it because DF‑151j made\\n  them consistent with fields, which is the correct outcome for that unit and\\n  the reason this is filed rather than fixed there. `_check_reference_expr`\\n  checks `&var` mutability for an Identifier operand, for `self`, and for a\\n  projection out of a `&self` receiver (`_projects_from_self`, DF‑146b) — but\\n  there is no arm for a projection rooted at a LOCAL, so the walk\\n  `_assign_target_immutable_struct_root` already performs for every assignment\\n  target is simply never run on a reference operand.\\n  Expected shape: run that same walk in `_check_reference_expr` when\\n  `expr.mutable` and the operand is a projection, with the message the\\n  assignment path gives. Blast radius is why it is its own unit — the rule\\n  reaches every `&var` into a field or element in std, blade and the libs, and\\n  any legitimate one written through a `let` root today becomes a compile error\\n  that has to be re-spelled `var`.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\",\"memory-safety\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"todo-20260907-70d0fe56ba803d8f1ec1-create\",\"title\":\"DF-151m — FILED, NOT FIXED (typechecker; found while fixing DF-151j, Aug 7). &var into a projection rooted at a let binding compiles and mutates — the let…\"}","request_id":"todo-20260907-70d0fe56ba803d8f1ec1-create","sequence":130,"timestamp":"1788791252"}
---
