---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:codex","closed":"1790341671","comments":[{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \"oracle known wrong\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `control-flow.missing-return`.","created":"1790341671","id":"c1","kind":"landing"}],"created":"1789477110","description":"SL-260's rejection corpus found Python sawc accepting a value-returning function with a reachable fallthrough path:\n\n```saw\nfunc f(x: Int32) -> Int32 { if x > 0 { return x } }\nfunc main() { print(f(0)) }\n```\n\nActual: compile succeeds and the uncovered path prints a fabricated 0. Existing fixture prototypes/minivm/examples/reject_missing_return.saw calls f(1), prints 1, and exposes the same incorrect acceptance. Expected: a located missing-return diagnostic; an Int32 result must exist on every continuing path. The prototype correctly rejects the function.\n\nMechanism: function checking uses a global found_return_with_value fact; any return anywhere can suppress a no-value diagnostic when the block has no tail. That fact is not proof that every reachable path returns. Track control-flow outcomes at joins, preserving valid all-returning branches, divergence and implicit tails; cover if/else, loops, match, methods and nested early returns. This is distinct from the dead-statements-after-unconditional-return over-rejection filed alongside it.\n","id":"SL-298","labels":["frozen","hazard"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":3,"sequence":2158,"stage":"backlog","status":"closed","title":"sawc: conditional return permits a reachable value-less function exit","updated":"1790341671"},"before":{"acceptance":[],"assignee":"","author":"agent:codex","closed":"","comments":[],"created":"1789477110","description":"SL-260's rejection corpus found Python sawc accepting a value-returning function with a reachable fallthrough path:\n\n```saw\nfunc f(x: Int32) -> Int32 { if x > 0 { return x } }\nfunc main() { print(f(0)) }\n```\n\nActual: compile succeeds and the uncovered path prints a fabricated 0. Existing fixture prototypes/minivm/examples/reject_missing_return.saw calls f(1), prints 1, and exposes the same incorrect acceptance. Expected: a located missing-return diagnostic; an Int32 result must exist on every continuing path. The prototype correctly rejects the function.\n\nMechanism: function checking uses a global found_return_with_value fact; any return anywhere can suppress a no-value diagnostic when the block has no tail. That fact is not proof that every reachable path returns. Track control-flow outcomes at joins, preserving valid all-returning branches, divergence and implicit tails; cover if/else, loops, match, methods and nested early returns. This is distinct from the dead-statements-after-unconditional-return over-rejection filed alongside it.\n","id":"SL-298","labels":["frozen","hazard"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":2,"sequence":2157,"stage":"backlog","status":"open","title":"sawc: conditional return permits a reachable value-less function exit","updated":"1790341671"},"id":"SL-298","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \\\"oracle known wrong\\\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `control-flow.missing-return`.\",\"id\":\"SL-298\",\"request_id\":\"pid-56287-1790341671-8000\"}","request_id":"pid-56287-1790341671-8000","sequence":2158,"timestamp":"1790341671"}
---
