---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl274","closed":"1790341675","comments":[{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \"oracle known wrong\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `import.qualified-call`.","created":"1790341675","id":"c1","kind":"landing"}],"created":"1789762453","description":"A module-qualified free call into a USER module resolves to a std free function\nof the same name, when that std module is compiled in.\n\nFound by SL-274's r3 class sweep (probing tag consumers with same-named free\nfunctions); NOT caused by SL-274 — it reproduces identically on main at\n4abf24e5 and on the SL-274 branch.\n\nRepro:\n\n```saw\nimport std.json.{JsonEncoder}\n\nmodule mine {\n    public func encode(n: Int) -> Int { n + 100 }\n}\n\nfunc main() {\n    print(mine.encode(1))\n}\n```\n\n    error: cannot infer type argument `T` for function `encode`\n      --> probe.saw:8:15\n    hint: give the type argument(s) explicitly, e.g. `encode<T=...>`\n\n`encode` here is `std.json`'s `encode<T: Serialize>(value:)`. The import is\nSELECTIVE (`{JsonEncoder}`), so design 150 / DF-247b say it binds `JsonEncoder`\nand nothing else — and the call is QUALIFIED into `mine`, which declares exactly\none `encode` taking an `Int`. Neither spelling should be able to reach std's.\n\nMechanism, unverified: SL-208's module-qualified free call wears a `MethodCall`'s\nshape and is resolved through `module_free_call`; the candidate set it consults\nappears to be the merged/bare overload set rather than the named module's own\ndeclarations, so a std free function that is compiled in (std.json is, because\n`JsonEncoder` was imported) joins the candidates. If so, the same shape reaches\nany std free-function name a user module also declares, and the diagnostic is\nalways about the WRONG declaration — which is what makes it hard to read.\n\nWorkaround: rename the user function.\n","id":"SL-319","labels":["frozen","hazard"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":3,"sequence":2180,"stage":"backlog","status":"closed","title":"a module-qualified free call into a user module resolves to a std free function of the same name","updated":"1790341675"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sl274","closed":"","comments":[],"created":"1789762453","description":"A module-qualified free call into a USER module resolves to a std free function\nof the same name, when that std module is compiled in.\n\nFound by SL-274's r3 class sweep (probing tag consumers with same-named free\nfunctions); NOT caused by SL-274 — it reproduces identically on main at\n4abf24e5 and on the SL-274 branch.\n\nRepro:\n\n```saw\nimport std.json.{JsonEncoder}\n\nmodule mine {\n    public func encode(n: Int) -> Int { n + 100 }\n}\n\nfunc main() {\n    print(mine.encode(1))\n}\n```\n\n    error: cannot infer type argument `T` for function `encode`\n      --> probe.saw:8:15\n    hint: give the type argument(s) explicitly, e.g. `encode<T=...>`\n\n`encode` here is `std.json`'s `encode<T: Serialize>(value:)`. The import is\nSELECTIVE (`{JsonEncoder}`), so design 150 / DF-247b say it binds `JsonEncoder`\nand nothing else — and the call is QUALIFIED into `mine`, which declares exactly\none `encode` taking an `Int`. Neither spelling should be able to reach std's.\n\nMechanism, unverified: SL-208's module-qualified free call wears a `MethodCall`'s\nshape and is resolved through `module_free_call`; the candidate set it consults\nappears to be the merged/bare overload set rather than the named module's own\ndeclarations, so a std free function that is compiled in (std.json is, because\n`JsonEncoder` was imported) joins the candidates. If so, the same shape reaches\nany std free-function name a user module also declares, and the diagnostic is\nalways about the WRONG declaration — which is what makes it hard to read.\n\nWorkaround: rename the user function.\n","id":"SL-319","labels":["frozen","hazard"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":2179,"stage":"backlog","status":"open","title":"a module-qualified free call into a user module resolves to a std free function of the same name","updated":"1790341675"},"id":"SL-319","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \\\"oracle known wrong\\\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `import.qualified-call`.\",\"id\":\"SL-319\",\"request_id\":\"pid-56544-1790341675-6000\"}","request_id":"pid-56544-1790341675-6000","sequence":2180,"timestamp":"1790341675"}
---
