---
{"action":"create","actor":"agent:claude-sl306","after":{"assignee":"","author":"agent:claude-sl306","comments":[],"created":"1789710772","description":"Found while building SL-306's matrix (pre-existing: identical on main `607ab218` and on the pre-SL-280 compiler `f982f54c`). A cell of SL-287's class, at a location whose entry-module twin refuses cleanly — which is what makes it worth its own row.\n\nA `yield_now()` (or any real suspension) inside a CLOSURE LITERAL's body is REFUSED when the closure is written in the entry module and SILENTLY DROPPED when the same closure is written in an imported module's helper. Same program shape, one file boundary apart, two different outcomes — and the silent one is the documented-impossible third outcome designs 96/101/104 rule out.\n\n## Repro\n\n`modules/dep/lib.saw`:\n\n```saw\nimport std.task.{yield_now}\n\npublic func each_yield_public(values: &Vector<Int>) -> Int {\n    values.each({ n in\n        yield_now()\n    })\n    values.len()\n}\n```\n\nEntry module:\n\n```saw\nimport std.task.*\nimport dep.{each_yield_public}\n\nfunc through_dep(values: &Vector<Int>) -> Int {\n    each_yield_public(&values)\n}\n\nfunc main() {\n    let values: Vector<Int> = [1, 2]\n    print(__saw_drive_steps(through_dep(&values)))\n}\n```\n\nObserved: compiles, prints `0` — no frame was built for `each_yield_public`, so the closure's `yield_now()` lowered outside any frame, where it codegens to nothing. Two drive steps are owed (one per element).\n\nThe ENTRY-MODULE twin — the same `each_yield` body written in the entry file — is refused, correctly and with the right anchor:\n\n```\nerror: coroutine transform: the suspending call `yield_now(...)` appears inside a\nCLOSURE BODY in driven `each_yield`, and a closure body is not driven — its\nsuspension has no frame to park in.\n```\n\n## Mechanism (probed as far as the boundary)\n\nThe refusal in the entry-module twin comes from the driven caller's own body scan (`_FrameBuilder._reject_buried_suspend_call`), which only runs over bodies the transform FRAMED. The entry-module body is framed because the closure's real suspension reaches its enclosing function's effect node, so the closure walk follows the edge; for the IMPORTED body it does not — nothing marks `each_yield_public` suspending, no frame is built, and no scan ever reads it. So the boundary is in what the effect graph records for a closure literal written in a non-entry module (or in what the walk asks of it), not in the rejector.\n\nWorth pairing with SL-306's fix when this is picked up: SL-306 made the call-site classifier and the closure walk ask `effects.suspends_ignoring_closure_calls`, and verified (cell by cell) that a really-suspending closure body is still refused in every ENTRY-module position. This cell is the one that was never refused, before or after.\n\n## Why it is SL-287's class and not a duplicate\n\nSL-287 is the general failure MODE (a suspending callee the walk declines degrades silently). This row is a concrete, currently-reachable instance with a two-line repro and an entry-module control beside it, which SL-287 explicitly lacks; the io face of it is the dangerous one (a `stream.read()` in that closure body lowers through codegen's out-of-frame park and stops the executor's thread rather than losing a cede).\n","id":"SL-316","labels":["bug","compiler","coroutine"],"priority":"high","project":"SL","revision":1,"sequence":950,"stage":"backlog","status":"open","title":"A suspension inside a closure body is refused in the entry module and silently dropped in an imported module's helper","updated":"1789710772"},"before":null,"id":"SL-316","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl306\",\"description\":\"Found while building SL-306's matrix (pre-existing: identical on main `607ab218` and on the pre-SL-280 compiler `f982f54c`). A cell of SL-287's class, at a location whose entry-module twin refuses cleanly — which is what makes it worth its own row.\\n\\nA `yield_now()` (or any real suspension) inside a CLOSURE LITERAL's body is REFUSED when the closure is written in the entry module and SILENTLY DROPPED when the same closure is written in an imported module's helper. Same program shape, one file boundary apart, two different outcomes — and the silent one is the documented-impossible third outcome designs 96/101/104 rule out.\\n\\n## Repro\\n\\n`modules/dep/lib.saw`:\\n\\n```saw\\nimport std.task.{yield_now}\\n\\npublic func each_yield_public(values: &Vector<Int>) -> Int {\\n    values.each({ n in\\n        yield_now()\\n    })\\n    values.len()\\n}\\n```\\n\\nEntry module:\\n\\n```saw\\nimport std.task.*\\nimport dep.{each_yield_public}\\n\\nfunc through_dep(values: &Vector<Int>) -> Int {\\n    each_yield_public(&values)\\n}\\n\\nfunc main() {\\n    let values: Vector<Int> = [1, 2]\\n    print(__saw_drive_steps(through_dep(&values)))\\n}\\n```\\n\\nObserved: compiles, prints `0` — no frame was built for `each_yield_public`, so the closure's `yield_now()` lowered outside any frame, where it codegens to nothing. Two drive steps are owed (one per element).\\n\\nThe ENTRY-MODULE twin — the same `each_yield` body written in the entry file — is refused, correctly and with the right anchor:\\n\\n```\\nerror: coroutine transform: the suspending call `yield_now(...)` appears inside a\\nCLOSURE BODY in driven `each_yield`, and a closure body is not driven — its\\nsuspension has no frame to park in.\\n```\\n\\n## Mechanism (probed as far as the boundary)\\n\\nThe refusal in the entry-module twin comes from the driven caller's own body scan (`_FrameBuilder._reject_buried_suspend_call`), which only runs over bodies the transform FRAMED. The entry-module body is framed because the closure's real suspension reaches its enclosing function's effect node, so the closure walk follows the edge; for the IMPORTED body it does not — nothing marks `each_yield_public` suspending, no frame is built, and no scan ever reads it. So the boundary is in what the effect graph records for a closure literal written in a non-entry module (or in what the walk asks of it), not in the rejector.\\n\\nWorth pairing with SL-306's fix when this is picked up: SL-306 made the call-site classifier and the closure walk ask `effects.suspends_ignoring_closure_calls`, and verified (cell by cell) that a really-suspending closure body is still refused in every ENTRY-module position. This cell is the one that was never refused, before or after.\\n\\n## Why it is SL-287's class and not a duplicate\\n\\nSL-287 is the general failure MODE (a suspending callee the walk declines degrades silently). This row is a concrete, currently-reachable instance with a two-line repro and an entry-module control beside it, which SL-287 explicitly lacks; the io face of it is the dangerous one (a `stream.read()` in that closure body lowers through codegen's out-of-frame park and stops the executor's thread rather than losing a cede).\\n\",\"labels\":[\"bug\",\"compiler\",\"coroutine\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"pid-90757-1789710772-10000\",\"stage\":\"backlog\",\"title\":\"A suspension inside a closure body is refused in the entry module and silently dropped in an imported module's helper\"}","request_id":"pid-90757-1789710772-10000","sequence":950,"timestamp":"1789710772"}
---
