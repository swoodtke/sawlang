---
{"action":"comment","actor":"agent:claude-sl226","after":{"assignee":"agent:claude-sl226","author":"agent:claude-sl222","comments":[{"author":"agent:claude-sl222","body":"No XFAIL pin landed with SL-222/223/224 on purpose: the repro CRASHES the compiler (RecursionError) rather than producing a diagnostic, and a corpus file that crashes sawc would destabilize the irdet and reemit lanes, which byte-compare compiler output over the whole corpus. Land the pin together with the fix, or land it first as an examples/errors/ test once the cycle check reaches the spawn-root path.","created":"1789048720","id":"c1"},{"author":"agent:claude-sl226","body":"Claiming SL-227 (batch #2, issue 3 of 3). SL-226 (033d6bb1) and SL-234 (5e1bbfee) are in on branch agent-sl226-batch2, each gated. Plan per the issue: route spawn roots through the SAME _analyze_nesting cycle check rather than cloning it (obligation 1 — the check's docstring will name both entry paths), then sweep the other root-seeding paths (Task.spawn, group.spawn, Thread.spawn, mutual recursion f->g->f, method spawn roots) for the same bypass, and land the errors/ pin with the fix since the old crash meant no pin could exist.","created":"1789098729","id":"c2"}],"created":"1789042089","description":"`_analyze_nesting`'s suspending-recursion cycle check rejects the `__saw_drive` spelling cleanly (`examples/errors/coro_suspending_recursion.saw` pins it), but the `Task.spawn` / spawn-root spelling of the SAME cycle never reaches that check: the frame builder recurses on itself and the compiler dies with a Python traceback.\n\nRepro (single file), verified against unmodified main (d0e38f90):\n\n  import std.task.{yield_now}\n  func recurse(n: Int) -> Int {\n      yield_now()\n      if n <= 0 { return 0 }\n      return recurse(n - 1)\n  }\n  func main() {\n      let t = Task.spawn(recurse(3))\n      print(\"{t.join()}\")\n  }\n\nObserved tail of the traceback:\n\n  File \".../sawc/coro_transform.py\", line 8306, in _zeroed_value\n  File \".../sawc/coro_transform.py\", line 485, in _zero_of\n  File \".../sawc/coro_transform.py\", line 211, in _int\n  RecursionError: maximum recursion depth exceeded\n\nwith `_build_frame_init` repeated ~981 times above it (crash frame order: transform_program -> build_resume -> _build_sub_frame -> _build_frame_init recursing).\n\nMECHANISM: the cycle check runs over the DRIVE roots' call graph; a spawn root is seeded on a different path and is not put through `_analyze_nesting` before its frame is built, so a cycle that check exists to refuse reaches `_build_sub_frame`, which embeds a callee frame by value -- which for a self-cycle has no fixed point. This is a fuzz-oracle violation (a traceback rather than a clean diagnostic), so it is also a `tools/sawfuzz.py` finding shape.\n\nFound incidentally while censusing the CoroTransformError raise sites for SL-224. Not caused by and not fixed by the SL-222/223/224 batch.","id":"SL-227","labels":["bug","coroutine-transform"],"priority":"normal","project":"SL","revision":5,"sequence":494,"stage":"queued","status":"open","title":"Task.spawn of a self-recursive suspending function crashes the compiler (RecursionError) instead of the clean cycle rejection","updated":"1789098729"},"before":{"assignee":"agent:claude-sl226","author":"agent:claude-sl222","comments":[{"author":"agent:claude-sl222","body":"No XFAIL pin landed with SL-222/223/224 on purpose: the repro CRASHES the compiler (RecursionError) rather than producing a diagnostic, and a corpus file that crashes sawc would destabilize the irdet and reemit lanes, which byte-compare compiler output over the whole corpus. Land the pin together with the fix, or land it first as an examples/errors/ test once the cycle check reaches the spawn-root path.","created":"1789048720","id":"c1"}],"created":"1789042089","description":"`_analyze_nesting`'s suspending-recursion cycle check rejects the `__saw_drive` spelling cleanly (`examples/errors/coro_suspending_recursion.saw` pins it), but the `Task.spawn` / spawn-root spelling of the SAME cycle never reaches that check: the frame builder recurses on itself and the compiler dies with a Python traceback.\n\nRepro (single file), verified against unmodified main (d0e38f90):\n\n  import std.task.{yield_now}\n  func recurse(n: Int) -> Int {\n      yield_now()\n      if n <= 0 { return 0 }\n      return recurse(n - 1)\n  }\n  func main() {\n      let t = Task.spawn(recurse(3))\n      print(\"{t.join()}\")\n  }\n\nObserved tail of the traceback:\n\n  File \".../sawc/coro_transform.py\", line 8306, in _zeroed_value\n  File \".../sawc/coro_transform.py\", line 485, in _zero_of\n  File \".../sawc/coro_transform.py\", line 211, in _int\n  RecursionError: maximum recursion depth exceeded\n\nwith `_build_frame_init` repeated ~981 times above it (crash frame order: transform_program -> build_resume -> _build_sub_frame -> _build_frame_init recursing).\n\nMECHANISM: the cycle check runs over the DRIVE roots' call graph; a spawn root is seeded on a different path and is not put through `_analyze_nesting` before its frame is built, so a cycle that check exists to refuse reaches `_build_sub_frame`, which embeds a callee frame by value -- which for a self-cycle has no fixed point. This is a fuzz-oracle violation (a traceback rather than a clean diagnostic), so it is also a `tools/sawfuzz.py` finding shape.\n\nFound incidentally while censusing the CoroTransformError raise sites for SL-224. Not caused by and not fixed by the SL-222/223/224 batch.","id":"SL-227","labels":["bug","coroutine-transform"],"priority":"normal","project":"SL","revision":4,"sequence":460,"stage":"queued","status":"open","title":"Task.spawn of a self-recursive suspending function crashes the compiler (RecursionError) instead of the clean cycle rejection","updated":"1789090575"},"id":"SL-227","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sl226\",\"body\":\"Claiming SL-227 (batch #2, issue 3 of 3). SL-226 (033d6bb1) and SL-234 (5e1bbfee) are in on branch agent-sl226-batch2, each gated. Plan per the issue: route spawn roots through the SAME _analyze_nesting cycle check rather than cloning it (obligation 1 — the check's docstring will name both entry paths), then sweep the other root-seeding paths (Task.spawn, group.spawn, Thread.spawn, mutual recursion f->g->f, method spawn roots) for the same bypass, and land the errors/ pin with the fix since the old crash meant no pin could exist.\",\"id\":\"SL-227\",\"request_id\":\"pid-72267-1789098729-14000\"}","request_id":"pid-72267-1789098729-14000","sequence":494,"timestamp":"1789098729"}
---
