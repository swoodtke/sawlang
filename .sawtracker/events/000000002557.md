---
{"action":"create","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790354008","description":"## Summary\nAn extension of a generic struct that is written without its type parameters, such as `extension Gen { … }` or `extension Gen: NoCopy {}`, makes the frozen compiler crash with `internal compiler error: 'Gen'`. `extension Gen<T> { … }` works.\n\nFound by agent:claude-air-sawlang (SL:hazards t13 aside). The lead reproduced it on main 2fa71814 with `.build/scratch/hz/generic_ext_no_params.saw`.\n\n## Repro\n```saw\nstruct Gen<T> { item: T }\nextension Gen {\n    func tag(&self) -> Int { 1 }\n}\nfunc main() { print(Gen<Int>(item: 3).tag()) }\n```\n\n## Disposition\nThe Python compiler is frozen, so this goes to the ledger as SL:hazards L18 (loud) rather than being fixed. Whether a bare generic name is ever valid in an extension head is SL-66's question. The new compiler refuses it with a diagnostic, or accepts it, per that ruling.\n","id":"SL-402","labels":["hazard","frozen"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":2557,"stage":"backlog","status":"open","title":"An extension of a generic type written without its parameters crashes the frozen compiler","updated":"1790354008"},"before":null,"id":"SL-402","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"description\":\"## Summary\\nAn extension of a generic struct that is written without its type parameters, such as `extension Gen { … }` or `extension Gen: NoCopy {}`, makes the frozen compiler crash with `internal compiler error: 'Gen'`. `extension Gen<T> { … }` works.\\n\\nFound by agent:claude-air-sawlang (SL:hazards t13 aside). The lead reproduced it on main 2fa71814 with `.build/scratch/hz/generic_ext_no_params.saw`.\\n\\n## Repro\\n```saw\\nstruct Gen<T> { item: T }\\nextension Gen {\\n    func tag(&self) -> Int { 1 }\\n}\\nfunc main() { print(Gen<Int>(item: 3).tag()) }\\n```\\n\\n## Disposition\\nThe Python compiler is frozen, so this goes to the ledger as SL:hazards L18 (loud) rather than being fixed. Whether a bare generic name is ever valid in an extension head is SL-66's question. The new compiler refuses it with a diagnostic, or accepts it, per that ruling.\\n\",\"labels\":[\"hazard\",\"frozen\"],\"project\":\"SL\",\"request_id\":\"pid-45410-1790354008-5000\",\"title\":\"An extension of a generic type written without its parameters crashes the frozen compiler\"}","request_id":"pid-45410-1790354008-5000","sequence":2557,"timestamp":"1790354008"}
---
