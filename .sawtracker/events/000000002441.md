---
{"action":"create","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"agent:claude-u2a","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790346057","description":"Unit U2a of the self-hosted compiler epic (SL-398): the formal grammar. Its output is a shared document, SL:grammar, drafted locally and published by the lead after review. It touches no repository file, so it runs alongside U1 (SL-399) without conflict.\n\n## Why\n\nThe parser must handle all valid Saw, and its tests must prove it. The plan (user, Sep 25: \"that's exactly what i had in mind\"):\n1. **U2a (this unit):** a formal, machine-readable grammar of the whole language, with a stable name on every production and alternative, and the contexts each construct may appear in.\n2. **U2b:** a generator walks that grammar and emits snippets paired with their expected canonical AST dumps. It adds golden cases for the known tricky points, a negative (mutation) corpus, and a coverage lane that fails when an alternative or a construct-in-context pair goes untested.\n3. **U4 onward:** the parser, written test-first against U2b's corpus.\n\nSo this grammar is the single authority for syntax that the parser, the tests and the spec all cite. It must be complete, precise, and consumable by a program.\n\n## Sources\n\nThe spec is the authority. Where the Python parser differs, the spec wins, and the difference is listed.\n- `LANGUAGE_SPEC.md`, all of it, and the lockdown docs `SL:borrowing` and `SL:testing` (`~/bin/sawtracker doc show SL:borrowing`), which add and change syntax.\n- The rulings that live outside the spec: the SL-393 list (`~/bin/sawtracker show SL-393`), which covers SL-59 statement arms, SL-73 the general postfix call, SL-309 `??` in cast targets (the spec is wrong there), SL-310 trailing closures, SL-45, design 274's owed items, and SL-380's per-hop postfix charging. Also `designs/259-selfhost-parser.md` (rulings R1–R8), `designs/274-parser-track-reconciliation.md` (N1–N12, B1, U5′) and `designs/reviews/parser-census-sep1.md`.\n- The token set: the lexer in `selfhost/lexer/src/lib.saw`. U1 is moving it to `compiler/lex/`; read whichever exists.\n- Evidence of what parses today: `sawc/parser/`. You may also write scratch probes under `.build/scratch/` and compile them with `./.venv/bin/python sawc/sawc.py` (see CLAUDE.md, \"Scratch compilations\"). Evidence only, never authority.\n- The node vocabulary the prototypes used: `prototypes/minivm/M18_AST.md` (a reference, not a constraint).\n\n## Output: `.build/scratch/docs/grammar.md`\n\n1. **Notation.** A small, regular EBNF, defined at the top, that a Python generator can parse: productions, alternatives, optional, zero-or-more, one-or-more, terminals as token kinds or quoted spellings, and a way to attach a name to each alternative. Keep it boring and unambiguous.\n2. **The lexical layer**, briefly:\n   - the token kinds (from the lexer);\n   - where a newline ends a statement, and where it is insignificant (inside `()`, `[]` and committed generic `<>`, design 129; trailing-operator continuation, R3);\n   - interpolation;\n   - doc-comment trivia;\n   - contextual words.\n3. **The grammar**, area by area, covering *everything* the spec and the lockdown docs define:\n   - declarations: `func`, `struct`, `enum` (raw backings), `trait`, `extension`, `init`, `static`, `const`, `import` in every form, `extern` blocks, and the attributes (`@export`, `@section`, `@align`, `@synthesize`, and every `@test` form);\n   - types: paths and qualifiers, generics and const generics, defaults, `T?`, `&T`/`&var T`, slices `&[T]`, `[T; N]`, tuples and named tuples, function types with effects (`unsafe`, `sync`, `borrows`, `borrows(sync)`, `consumes`), and `any Trait`;\n   - expressions:\n     - a precedence and associativity table;\n     - literals, including interpolated strings, collection literals and repeat literals;\n     - calls with labels, trailing closures, and the general postfix call;\n     - closures and capture lists;\n     - `move`, `try` / `try!` / `try?`, the `catch` forms, `??`, `?.`, `!`, `as` casts, ranges;\n     - `if` and `match` as values;\n     - the `borrow` statement and argument forms;\n   - statements: `let`/`var` (with destructuring), assignment and compound assignment, `return`/`break`/`continue`, `for`/`while`/`loop`, `guard`, `if let`/`while let`, `match` with statement and expression arms, the `borrow` block, and `lend`;\n   - patterns: literals, ranges, guards, tuples, enum payloads, bindings, and wildcards;\n   - the lockdown forms: `borrow let|var` blocks, the statement and argument forms, `for borrow let|var` (Proposed), `func []` and `func []=`, multi-argument subscripts, the `default:` label, tuple lends, and `borrows(sync)`.\n4. **A stable name on every production and every alternative:** `syntax.<area>.<construct>[.<variant>]`, lowercase, words joined by hyphens (for example `syntax.expr.call.trailing-closure`). Tests will cite these names (SL:testing §6). Each production also carries:\n   - its spec reference (section heading);\n   - its status: `current`, `lockdown` (new or changed by SL:borrowing or SL:testing), `retired` (still parsed but refused later with a hint, such as inline place writes), `removed` (a parse error, such as `#lend_var`), or `pending` (awaiting a ruling);\n   - a preliminary AST node kind, which U2b turns into the dump contract.\n5. **The context table.** For every expression-level and statement-level construct, the contexts it may appear in, and the restrictions in each. Contexts include:\n   - statement, tail (block value), operand, argument, receiver, index;\n   - interpolation, condition (`if`/`while`/`guard`), match scrutinee, match arm body, closure body;\n   - initializer, default parameter value, static initializer, const-generic argument, return value.\n\n   Restrictions include, for example, whether a trailing closure is allowed in a condition. This is the matrix the generator walks. Missed contexts are where most of the Python parser's bugs lived.\n6. **Disambiguation rules.** Each known tricky point, with its ruled resolution and source:\n   - generic `<` versus less-than;\n   - a trailing closure versus a block;\n   - newline significance;\n   - `??` and `?` after `as T?`;\n   - statement versus value tails;\n   - `x = 1 y` (B1, refused);\n   - a leading minus after a block;\n   - struct-init versus call, which is gone: there is one `Call` node;\n   - flat `else if`;\n   - postfix chains charged per hop;\n   - the 256 depth limit and exactly what counts against it.\n7. **Open questions.** Everything the spec leaves unsaid or contradicts, each with the options. Mark the productions that depend on the three pending SL:borrowing rulings (threads t21 to t23; for example `if borrow var x = e` and `case Some(borrow var x)`) as `pending`, with the proposed spelling.\n8. **The one-time check against today's parser.** For each area, note where `sawc/parser/` accepts or refuses something differently from your grammar, with a probe file where you ran one. These become U2b golden cases or open questions.\n\n## Rules\n- Read-only except for your output file and scratch probes under `.build/scratch/`. Do not touch `compiler/`, `selfhost/`, `tools/`, `build.sh` or any tracked file, since U1 is working there. No tracker writes.\n- Do not run the test suite or any battery lane.\n- Style: plain, direct sentences. The EBNF itself should be clean enough that a reader learns the language's syntax from it.\n- Completeness beats polish. When unsure whether something is syntax, include it and mark it.\n\n## Report\n- the doc's size and structure;\n- the production and alternative counts per area;\n- the context-table size;\n- the open questions;\n- the differences from today's parser;\n- anything the lead or the user must rule on.\n","id":"SL-400","labels":["compiler","language"],"order":2,"parent":"SL-398","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":2441,"stage":"queued","status":"open","title":"U2a: the formal grammar (SL:grammar): EBNF with stable names, contexts and disambiguation","updated":"1790346057"},"before":null,"id":"SL-400","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"assignee\":\"agent:claude-u2a\",\"description\":\"Unit U2a of the self-hosted compiler epic (SL-398): the formal grammar. Its output is a shared document, SL:grammar, drafted locally and published by the lead after review. It touches no repository file, so it runs alongside U1 (SL-399) without conflict.\\n\\n## Why\\n\\nThe parser must handle all valid Saw, and its tests must prove it. The plan (user, Sep 25: \\\"that's exactly what i had in mind\\\"):\\n1. **U2a (this unit):** a formal, machine-readable grammar of the whole language, with a stable name on every production and alternative, and the contexts each construct may appear in.\\n2. **U2b:** a generator walks that grammar and emits snippets paired with their expected canonical AST dumps. It adds golden cases for the known tricky points, a negative (mutation) corpus, and a coverage lane that fails when an alternative or a construct-in-context pair goes untested.\\n3. **U4 onward:** the parser, written test-first against U2b's corpus.\\n\\nSo this grammar is the single authority for syntax that the parser, the tests and the spec all cite. It must be complete, precise, and consumable by a program.\\n\\n## Sources\\n\\nThe spec is the authority. Where the Python parser differs, the spec wins, and the difference is listed.\\n- `LANGUAGE_SPEC.md`, all of it, and the lockdown docs `SL:borrowing` and `SL:testing` (`~/bin/sawtracker doc show SL:borrowing`), which add and change syntax.\\n- The rulings that live outside the spec: the SL-393 list (`~/bin/sawtracker show SL-393`), which covers SL-59 statement arms, SL-73 the general postfix call, SL-309 `??` in cast targets (the spec is wrong there), SL-310 trailing closures, SL-45, design 274's owed items, and SL-380's per-hop postfix charging. Also `designs/259-selfhost-parser.md` (rulings R1–R8), `designs/274-parser-track-reconciliation.md` (N1–N12, B1, U5′) and `designs/reviews/parser-census-sep1.md`.\\n- The token set: the lexer in `selfhost/lexer/src/lib.saw`. U1 is moving it to `compiler/lex/`; read whichever exists.\\n- Evidence of what parses today: `sawc/parser/`. You may also write scratch probes under `.build/scratch/` and compile them with `./.venv/bin/python sawc/sawc.py` (see CLAUDE.md, \\\"Scratch compilations\\\"). Evidence only, never authority.\\n- The node vocabulary the prototypes used: `prototypes/minivm/M18_AST.md` (a reference, not a constraint).\\n\\n## Output: `.build/scratch/docs/grammar.md`\\n\\n1. **Notation.** A small, regular EBNF, defined at the top, that a Python generator can parse: productions, alternatives, optional, zero-or-more, one-or-more, terminals as token kinds or quoted spellings, and a way to attach a name to each alternative. Keep it boring and unambiguous.\\n2. **The lexical layer**, briefly:\\n   - the token kinds (from the lexer);\\n   - where a newline ends a statement, and where it is insignificant (inside `()`, `[]` and committed generic `<>`, design 129; trailing-operator continuation, R3);\\n   - interpolation;\\n   - doc-comment trivia;\\n   - contextual words.\\n3. **The grammar**, area by area, covering *everything* the spec and the lockdown docs define:\\n   - declarations: `func`, `struct`, `enum` (raw backings), `trait`, `extension`, `init`, `static`, `const`, `import` in every form, `extern` blocks, and the attributes (`@export`, `@section`, `@align`, `@synthesize`, and every `@test` form);\\n   - types: paths and qualifiers, generics and const generics, defaults, `T?`, `&T`/`&var T`, slices `&[T]`, `[T; N]`, tuples and named tuples, function types with effects (`unsafe`, `sync`, `borrows`, `borrows(sync)`, `consumes`), and `any Trait`;\\n   - expressions:\\n     - a precedence and associativity table;\\n     - literals, including interpolated strings, collection literals and repeat literals;\\n     - calls with labels, trailing closures, and the general postfix call;\\n     - closures and capture lists;\\n     - `move`, `try` / `try!` / `try?`, the `catch` forms, `??`, `?.`, `!`, `as` casts, ranges;\\n     - `if` and `match` as values;\\n     - the `borrow` statement and argument forms;\\n   - statements: `let`/`var` (with destructuring), assignment and compound assignment, `return`/`break`/`continue`, `for`/`while`/`loop`, `guard`, `if let`/`while let`, `match` with statement and expression arms, the `borrow` block, and `lend`;\\n   - patterns: literals, ranges, guards, tuples, enum payloads, bindings, and wildcards;\\n   - the lockdown forms: `borrow let|var` blocks, the statement and argument forms, `for borrow let|var` (Proposed), `func []` and `func []=`, multi-argument subscripts, the `default:` label, tuple lends, and `borrows(sync)`.\\n4. **A stable name on every production and every alternative:** `syntax.<area>.<construct>[.<variant>]`, lowercase, words joined by hyphens (for example `syntax.expr.call.trailing-closure`). Tests will cite these names (SL:testing §6). Each production also carries:\\n   - its spec reference (section heading);\\n   - its status: `current`, `lockdown` (new or changed by SL:borrowing or SL:testing), `retired` (still parsed but refused later with a hint, such as inline place writes), `removed` (a parse error, such as `#lend_var`), or `pending` (awaiting a ruling);\\n   - a preliminary AST node kind, which U2b turns into the dump contract.\\n5. **The context table.** For every expression-level and statement-level construct, the contexts it may appear in, and the restrictions in each. Contexts include:\\n   - statement, tail (block value), operand, argument, receiver, index;\\n   - interpolation, condition (`if`/`while`/`guard`), match scrutinee, match arm body, closure body;\\n   - initializer, default parameter value, static initializer, const-generic argument, return value.\\n\\n   Restrictions include, for example, whether a trailing closure is allowed in a condition. This is the matrix the generator walks. Missed contexts are where most of the Python parser's bugs lived.\\n6. **Disambiguation rules.** Each known tricky point, with its ruled resolution and source:\\n   - generic `<` versus less-than;\\n   - a trailing closure versus a block;\\n   - newline significance;\\n   - `??` and `?` after `as T?`;\\n   - statement versus value tails;\\n   - `x = 1 y` (B1, refused);\\n   - a leading minus after a block;\\n   - struct-init versus call, which is gone: there is one `Call` node;\\n   - flat `else if`;\\n   - postfix chains charged per hop;\\n   - the 256 depth limit and exactly what counts against it.\\n7. **Open questions.** Everything the spec leaves unsaid or contradicts, each with the options. Mark the productions that depend on the three pending SL:borrowing rulings (threads t21 to t23; for example `if borrow var x = e` and `case Some(borrow var x)`) as `pending`, with the proposed spelling.\\n8. **The one-time check against today's parser.** For each area, note where `sawc/parser/` accepts or refuses something differently from your grammar, with a probe file where you ran one. These become U2b golden cases or open questions.\\n\\n## Rules\\n- Read-only except for your output file and scratch probes under `.build/scratch/`. Do not touch `compiler/`, `selfhost/`, `tools/`, `build.sh` or any tracked file, since U1 is working there. No tracker writes.\\n- Do not run the test suite or any battery lane.\\n- Style: plain, direct sentences. The EBNF itself should be clean enough that a reader learns the language's syntax from it.\\n- Completeness beats polish. When unsure whether something is syntax, include it and mark it.\\n\\n## Report\\n- the doc's size and structure;\\n- the production and alternative counts per area;\\n- the context-table size;\\n- the open questions;\\n- the differences from today's parser;\\n- anything the lead or the user must rule on.\\n\",\"labels\":[\"compiler\",\"language\"],\"order\":2,\"parent\":\"SL-398\",\"project\":\"SL\",\"request_id\":\"pid-26156-1790346057-7000\",\"stage\":\"queued\",\"title\":\"U2a: the formal grammar (SL:grammar): EBNF with stable names, contexts and disambiguation\"}","request_id":"pid-26156-1790346057-7000","sequence":2441,"timestamp":"1790346057"}
---
