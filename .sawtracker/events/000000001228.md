---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"agent:claude-sl347","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sl347","body":"## SL-347 patch 1 (U1a + docs) — BUILT, branch `sl347`, PARKED for lead validation\n\nScope per the lead's Sep-22 rescope: the chokepoint, R1 accepted, R2 refused,\nR4 untouched, the pins, the docs. R3's juxtaposition refusal and the corpus\nsweep are patch 2. No XFAIL added, no corpus file swept, the skill's existing\ntwo-space one-liners untouched.\n\n### The chokepoint and its entries\n\n`Parser.expect_statement_end(previous_start=None, *, declarations=False)` in\n`sawc/parser/core.py`, beside `skip_newlines`. Called once per GAP between\nstatements — including the gap BEFORE a list's first statement, where\n`previous_start` is None, nothing precedes, and only the line-start half of R2\ncan fire. Entries, named in its docstring, all three:\n\n1. `StatementsMixin.parse_block` — the ONE block-body funnel, which is why the\n   rule reaches function and method bodies, `borrows` bodies, `if`/`else`/\n   `while`/`for` bodies, a `match` arm's BLOCK body, both blocks of\n   `try { } catch { }`, and `guard … else { }`. (Verified: `parse_block` is the\n   only callee at all 13 block sites across `declarations.py`, `statements.py`\n   and `expressions.py`.)\n2. `ExpressionsMixin._parse_closure_body` — closure bodies.\n3. `Parser.parse` — module scope, whose units are DECLARATIONS\n   (`declarations=True`).\n\nA `match` arm's BARE body ends at the arm's `,`, but that body is an expression\nand not a statement list, so it never reaches the chokepoint — `parse_match`\nowns that comma, unchanged.\n\n`Parser.at_statement_end()` is the LOOKAHEAD half of the same rule, replacing\nthe four open-coded `NEWLINE/RBRACE/EOF` tuples at the statement forms whose\noperand is optional: `return`, `break`, `lend`, and the closure body's `return`\n(which also gains EOF, which it did not test before).\n\n### The R2 diagnostics, verbatim\n\n    a `;` joins two statements on one line; it does not end one — remove it\n\nfires at all five shapes, each with its own `examples/errors/` pin:\n\n| shape | pin | anchor |\n|---|---|---|\n| before a newline | `semicolon_before_newline.saw` | 11:14 |\n| before the block's `}` | `semicolon_before_closing_brace.saw` | 12:19 |\n| at end of file | `semicolon_at_end_of_file.saw` | 12:22 |\n| doubled | `semicolon_doubled.saw` | 11:14 |\n| at the start of a line | `semicolon_at_line_start.saw` | 13:5 |\n\nand at module scope, where a `;` really does separate two declarations:\n\n    declarations are not joined by `;` — put each declaration on its own line\n\npinned by `examples/errors/semicolon_between_declarations.saw` (11:24). Order\nmatters: the R2 check runs first, so a trailing `;` at EOF gets the \"ends\nnothing\" message and only a `;` with a declaration after it gets this one.\n\nR4 needs no code: the `;` in `[Int; 3]` and `[0; 3]` sits inside brackets,\nwhere no statement can begin, and the chokepoint is never reached there.\n\n### The running pin\n\n`examples/semicolon_joins_statements_on_one_line.saw` — one `;`-joined\none-liner per block kind the chokepoint covers: function body, method body,\nboth branches of an `if`, a `while` body, a `for` body, a `match` arm BLOCK\nbody, a closure body whose last statement is the closure's TAIL\n(`run({ x in let y = x + 1; y * 2 })`), both blocks of `try { } catch { }`, a\n`guard … else { }`, and a `borrows` body — with `[Int; 3]` and `[0; 3]` beside\nthem. 17 EXPECT-OUTPUT lines, all matched.\n\n### Corpus evidence (behaviour preservation)\n\nAll **3387** tracked `.saw` files parse to the SAME outcome as main — 3278\naccept, 109 refuse — with exactly one message improved:\n`prototypes/minivm/examples/reject_semicolon.saw` was\n`Unexpected token: SEMICOLON` and is now the R2 message. It is a minivm\nRejectCase with no `compare_sawc`, and its `differential_manifest.json` row is\n`agreement` on the OUTCOME (reject), which is unchanged.\n\n### R3 dry run — what patch 2 owes (report-only; nothing shipped)\n\nMonkeypatched the chokepoint to RECORD rather than refuse. **174 juxtaposition\nsites in 83 files**, this tree (pre-U3):\n\n| tree | sites | files |\n|---|---|---|\n| `examples/` | 127 | 52 |\n| `examples/conformance/` | 31 | 20 |\n| `examples/errors/` | 9 | 7 |\n| `sawc/std/` | 5 | 2 |\n| `prototypes/` | 2 | 2 |\n| `blade/`, `libs/`, `devtools/`, `tests/`, `selfhost/`, `sawc/rt/`, `sawc/builtin.saw` | 0 | 0 |\n\nDeclaration-position sites (two declarations on one line): **0**.\n\n**A MECHANISM PATCH 2 MUST HANDLE, found by the dry run.** The naive detector\nfirst reported **6597** sites. 6423 of those are false: `parse_if_expression`\n(both the `if` and the `if let` arms) and `parse_try_catch` call\n`skip_newlines()` to look ahead for `else`/`catch`, so when no `else`/`catch`\nfollows, the statement-ending newline is already CONSUMED by the time the\nchokepoint runs. A plain multi-line `if` followed by the next statement then\nlooks exactly like juxtaposition. The 174 above are what remains once a site is\nrequired to be on the SAME source line. Patch 2 must make those lookaheads\nrestore the position when the keyword does not follow (behaviour-neutral today,\nsince the enclosing loop skips the newline anyway) before R3 can be switched on.\n\n### Gate\n\nPer-commit gate on the tip, under the machine-wide lock:\n\n- full suite: **2604 passed, 10 xfailed** (the 10 are the pre-existing cited\n  pins; none added, none removed)\n- `tools/freestanding_runner.py`: **36 passed across riscv32 + arm64**\n- `tools/docverify.py` for the three doc sources touched: LANGUAGE_SPEC 373\n  blocks OK (105 compiled / 62 refused / 206 exempt), README 27 OK, SKILL 108 OK\n\nTerminal battery: DEFERRED to patch 2's final tip by the lead's ruling. Before\nthe stop it had run these stages green — `suite` (62s, 2604/10),\n`icebreadcrumb`, `lexdiff`, `astdiff`, `astgraft`, `transferdecisions`,\n`producertaxonomy`, `corodiscovery`, `corototality`, `stdseed`, `citations`,\n`forgetgate`, `ircontract`, `preludegate`, `docverify`, `moduletag`,\n`stdtypes`, `toolchain`, `floatvectors`, `abidoc`, `bttable`, `fuzz`,\n`corodiff`, `bench`, `selfhostlex`, `minivm`, `reemit` (1614 identical, 0\nDIVERGENT) — and `irdet` was killed mid-run (SIGTERM) after 1216 of 1761\nrecords with **0 MISMATCH, 0 VIOLATED INVARIANT**. One earlier full-battery run\nreported its `suite` stage failing; the suite then passed three consecutive\ntimes (standalone, `battery.sh suite`, and inside the second battery), so that\none is unexplained and unreproduced — flagging it rather than absorbing it.\n\nSelfhost: there is no selfhost PARSER yet (`selfhost/` holds the lexer only),\nand its lexer already emits `Semicolon`, so nothing was owed there. `lexdiff`\nand `selfhostlex` are green.\n\n### Commits (branch `sl347`, on `main`)\n\n- `ef45ca5b` — SL-347 U1a: `;` is the one-line statement separator\n- `95983096` — SL-347 docs: the statement separator and the brace-body layout rule\n\nDocs: LANGUAGE_SPEC's \"Statement Boundaries\" states the grammar and adds a\n\"Layout\" subsection carrying R5; the saw-lang skill's \"No semicolons\" line is\nrewritten and R5 is quoted verbatim under \"(user ruling, Sep 22)\"; README gains\nthe rule as a fifth smaller rule; `designs/todo.md` gains the `[QUEUE]` line\nnaming the two-patch split. Spec and skill both say plainly that bare\njuxtaposition is still accepted today and is being withdrawn.\n\nPARKED. Awaiting the lead's word to start patch 2 from the post-U3 main.\n","created":"1790102472","id":"c1"},{"author":"agent:claude-sl347b","body":"## SL-347 patch 2 (the lookahead restore + U2 sweep + R3 refusal) — BUILT, branch `sl347`, PARKED\n\nThree new commits on top of patch 1, rebased onto the post-U3 main. Terminal\nbattery GREEN, all 32 stages.\n\n### The rebase\n\n`git rebase origin/main` (1938880b). One conflict, in `designs/todo.md`'s\n`[QUEUE]`: main had SL-333 rewritten from **BUILT** to **LANDED Sep 21\n(SL-333.p1 r5, merge 47373c08)**, my side had SL-333 still **BUILT** plus the\nnew SL-347 line. Resolved hunk by hunk in the editor — kept main's LANDED text\nverbatim and re-added the SL-347 line above it. Sanity-checked afterwards: the\nfile holds exactly one SL-347 entry, main's `LANDED Sep 21 (SL-333.p1 r5` text,\nthe `SL-318.p7` mention and the design-275 U3 entry, and `git diff origin/main\n-- designs/todo.md` is exactly the SL-347 addition and nothing else.\nSKILL.md / LANGUAGE_SPEC.md / README.md auto-merged.\n\nGate on the rebased tip BEFORE any new work: patch 1's running pin\n(`examples/semicolon_joins_statements_on_one_line.saw`) compiles and prints its\n17 lines; full suite **2645 passed, 10 xfailed**; freestanding **36 passed\nacross riscv32 + arm64**.\n\n### 1. The lookahead restore — `876f5f7d`\n\nFour sites peek ACROSS newlines for a keyword and did not rewind on the miss:\n`parse_if_expression`'s `else` in BOTH arms (the `if let` one and the plain\none), the inline `try … catch`, and — found by the grep the brief asked for,\nover every `skip_newlines()` followed by a keyword test — the `module name\n{ … }` head, which is the DECLARATION-scope twin of the same mistake. Each now\nsaves the position and restores it when the keyword does not follow, the shape\n`while let`'s `else` refusal (`statements.py:463`) and a trait method's default\nbody (`declarations.py:537`) already used.\n\n**The dry-run count: 6726 → 246.** Measured with the chokepoint monkeypatched\nto RECORD at its entry — exactly where R3's refusal now stands — over all 3435\ntracked `.saw` files. The SAME-LINE subset, which is the real sites, is **246\neither way**, which is what makes the restore behaviour-neutral: it removes\n6480 phantom sites and no real one. (Patch 1 reported 6597 on the pre-U3 tree\nwith 174 real; U3's new files account for the growth.) Full suite and\nfreestanding green on this commit alone.\n\n### 2. The sweep — `ec796798`\n\nThe compiler is the oracle, run to a fixpoint. **Zero sites remain** anywhere.\n\n| tree | sites | files |\n|---|---|---|\n| `examples/` | 176 | 57 |\n| `examples/conformance/` | 52 | 25 |\n| `examples/errors/` | 11 | 8 |\n| `sawc/std/` (json, taskgroup) | 5 | 2 |\n| `prototypes/` | 2 | 2 |\n| `blade/`, `libs/`, `devtools/`, `tests/`, `selfhost/`, `sawc/rt/`, `builtin.saw` | 0 | 0 |\n| **`.saw` total (fixpoint)** | **244** | **94** |\n| string INTERPOLATION, by hand | 2 | 2 |\n| doc fences (LANGUAGE_SPEC 13, SKILL 14, README 0) | 27 | 2 |\n| skill fragments neither docverify shape parses, by hand | 5 | 1 |\n\nTwo things the mechanical pass could NOT do, and why:\n\n- **A string-interpolation sub-parser numbers its own tokens from 1:1**\n  (`_parse_expression_from_string`), so a site inside one carries a position\n  that is not the file's. The recorder keeps those apart instead of trusting\n  them — a first attempt did trust them and wrote `; ; ; ; …` into a comment\n  on line 1 before I caught it and reverted. The two real ones are\n  `examples/arc_with_unique.saw:39` and\n  `examples/coro_inline_catch_loop_exit_in_child_positions.saw:2669`.\n- **`docverify.extract` DEDENTS a fence body by its opening indent**, so a\n  site's column inside the body is `indent` characters left of its column in\n  the markdown. Every doc edit is rebased by its fence's indent and then\n  VERIFIED against the token it is supposed to precede before it lands (a\n  mismatch aborts the tool). The first doc pass, without that, put a `;` two\n  characters off — reverted and redone.\n\n**R5 — every half-broken layout in this tree, listed and reformatted:**\n\n| file | what |\n|---|---|\n| `examples/errors/capture_assign_escaping.saw` | the escaping closure `{ n = n + 1` / `n }` |\n| `examples/init_fallible_result.saw` | two `Printable` match arms |\n| `examples/init_fallible_result_shapes.saw` | one `Printable` match arm |\n| `prototypes/minivm/examples/builders/` | 7 fixtures (`reject_append_arity`, `reject_append_label`, `reject_append_type`, `reject_assignment`, `reject_copy`, `reject_nested_receiver`, `reject_shared_append`) |\n| `prototypes/minivm/examples/vectors/` | 14 fixtures (`evaluation_once` ×2, `index_at_end`, `index_negative`, `reject_assignment`, `reject_borrow_after_move`, `reject_double_move`, `reject_get_label`, `reject_implicit_copy`, `reject_index_type`, `reject_move_field`, `reject_move_reference`, `reject_outer_loop_move` ×2, `reject_push_label`, `reject_push_type`) |\n\nThe four hits the R5 scan still reports are `import m.{...}` symbol lists,\nwhich design 147 makes a delimited list rather than a statement container —\nthey wrap by design.\n\n**NO EXPECT line changed**: `git diff` filtered to every `EXPECT-`, `XFAIL`\nand `COMPILE-FLAGS` line over the whole sweep is empty. Gate on this commit:\nsuite 2645/10, freestanding 36, plus the four lanes that cover what moved —\ndocverify (513 blocks OK), minivm (446 cases), lexdiff, astdiff (3435 files,\nbyte-stable).\n\n### 3. R3 — `4e96321e`\n\n`Parser.expect_statement_end` refuses anything but NEWLINE / `;` / `}` / EOF,\nanchored at the SECOND statement's first token — where the separator is\nmissing — and naming BOTH heads. Verbatim:\n\n```\nerror: Parse error at 2:19: two statements on one line need a `;` between them: the first begins with `let`, the second with `print` — write `;` where they meet\n```\n\nand at module scope, where the units are declarations:\n\n```\nerror: Parse error at 24:23: two declarations on one line: the first begins with `static`, the second with `static` — declarations are not joined by `;` — put each declaration on its own line\n```\n\nA `match` arm's BARE body is untouched — it ends at the arm's `,`, which\n`parse_match` owns, and that body is an expression, not a statement list.\n\n**THE POSITION MATRIX — 13 rows over four `examples/errors/` pins.** Split\nacross files on purpose: the file-level parse loop batches at most\n`MAX_PARSE_ERRORS = 10` and recovery adds an orphan-`}` diagnostic per row, so\na single 13-row file TRUNCATES and half the matrix would go unchecked. Every\nrow names a different pair of first tokens, so no row's expectation can be\nsatisfied by another row's error.\n\n| pin | rows |\n|---|---|\n| `statement_juxtaposition_in_function_and_method_bodies.saw` | function body, method body, `borrows` body |\n| `statement_juxtaposition_in_control_flow_bodies.saw` | `if`, `else`, `while`, `for` |\n| `statement_juxtaposition_in_match_guard_and_try_blocks.saw` | `match` arm BLOCK body (bare arm beside it), `guard … else`, `try { }`, `catch { }` |\n| `statement_juxtaposition_in_closure_and_module_scope.saw` | closure body (`_parse_closure_body`), module scope (`Parser.parse`) |\n\n### Obligation 2 — the consumer sweep, with evidence\n\n1. **The fuzzer.** `tools/sawfuzz.py`'s oracle is \"succeeds or exits with a\n   clean diagnostic\", so a line-joining mutation now yields R3's refusal, which\n   is a PASS. `tools/sawfuzz_known.txt` holds exactly ONE entry — DF-270e,\n   `internal-compiler-error: … Undefined function: UIntN` — and nothing in it\n   rests on juxtaposition. `sawfuzz --quick` in the battery: 150 mutants,\n   **0 known hits, 0 failing compiles, 0 NEW findings**.\n2. **Passes that synthesize SOURCE TEXT rather than AST.** There is exactly\n   one: `ExpressionsMixin._parse_expression_from_string`, which re-lexes the\n   text of a string interpolation — text the AUTHOR wrote, so the rule reaches\n   it exactly as it reaches the enclosing file, and the two corpus sites are\n   swept. The coroutine transform builds AST and never constructs source (zero\n   `Lexer`/`tokenize` references); `place_transform` names the lexer only in a\n   comment about running inside `parse_source`; the std seed writes a cache\n   blob and `--emit-docs` writes JSON; `ast_dump`'s `f\"func …\"` strings are a\n   DUMP, never re-parsed. The only other `Lexer(...)` callers are `sawc.py` and\n   `module_resolver.py`, both over real files.\n3. **Saw embedded in the PYTHON HARNESSES — a third home for Saw source that\n   neither the corpus sweep nor the doc sweep reaches.** The battery caught one:\n   `tools/test_transfer_decisions.py`'s `generic_body_defers_to_specialization`\n   fixture (`func hold<T>(x: T) -> Int { let a = x  1 }`), which failed the\n   `transferdecisions` lane and is fixed in the R3 commit. A scan of every\n   triple-quoted Saw string under `tools/`, `devtools/` and `prototypes/` finds\n   four more, all in `prototypes/parser/test_parser.py` — and those are INPUT\n   fed to the selfhost parser PROTOTYPE binary, not programs sawc compiles\n   (sawc only compiles `prototypes/parser/src`). Its own harness already\n   expects a `boundary` error for `func f() { x = 1 y = 2 }`, so the prototype\n   agrees with R3 and the fixtures stay as written.\n4. **`devtools/dogfood`.** 5 of 7 do not compile — `w1_chatroom`,\n   `w1_filesearch`, `w1_limiter`, `w1_mapreduce`, `w1_pipeline` — and each\n   fails IDENTICALLY under main's compiler (verified by running the main\n   checkout's `sawc` on the same files): design 234 allocator-tier drift (a\n   discarded `Result`; `Channel()`/`Mutex(...)` now returning one). Not a parse\n   error among them. `llm_client` and `w1_jobqueue` compile.\n\nObligation 3 is not engaged: R3 is a grammar refusal, not a safety guarantee,\nso it owes no `examples/conformance/` row — the four errors/ matrix pins are\nits coverage.\n\n### Docs\n\nLANGUAGE_SPEC's and the skill's \"still accepted today … being withdrawn\"\nsentences become the refusal, each carrying the diagnostic (as a pinned\n`saw-error` block) and the full position list; README's fifth smaller rule\ngains the clause. The skill adds the older-build note, since\n`{ c in c = c + n  c }` compiled until yesterday. `designs/todo.md`'s SL-347\nentry is closed in place with the p2 landing note.\n\n### The terminal battery — ONE run, on the final tip\n\n**32 of 32 stages GREEN in 3817s.**\n\n- `suite` **2649 passed, 10 xfailed** (four new pins; the xfail count did not\n  move — none added, none removed)\n- `freestanding` 36 passed across riscv32 + arm64\n- `docverify` 515 blocks OK (141 compiled, 79 refused, 295 exempt)\n- `astdiff` 3435 files, byte-stable; `lexdiff`, `selfhostlex` green\n- `minivm` 446 cases; `corodiff` 45 pairs, 0 new findings\n- `fuzz` 150 mutants, 0 known hits, 0 new findings\n- `reemit` identical 1638, **DIVERGENT 0**\n- `irdet --all` 1634 examples, byte-identical IR, 0 mismatch / 0 invariant\n- `bootstrap`, `gmgate`, `transferdecisions`, `corodiscovery`, `corototality`,\n  `windowseam`, `ircontract` and the rest green\n\n**The one failure, recorded not absorbed.** The FIRST full battery run (before\nthe harness fix) failed exactly one stage: `transferdecisions`, on the embedded\nfixture above — a real regression my sweep had not reached, not a flake. The\nfixture is fixed, the lane re-run standalone (`14 boundary fixtures, 6 identity\nchecks, 33273 decisions`), and the WHOLE battery re-run from scratch on the\namended tip, which is the 32/32 above. Nothing else flaked in either run.\n\n### Commits (branch `sl347`, on `origin/main` 1938880b)\n\n- `de3b0388` — SL-347 U1a: `;` is the one-line statement separator *(patch 1, rebased)*\n- `5c02d37e` — SL-347 docs: the statement separator and the brace-body layout rule *(patch 1, rebased)*\n- `876f5f7d` — SL-347 U1b/1: a keyword peek restores the statement-ending newline\n- `ec796798` — SL-347 U2: the corpus takes the `;` every one-line body now owes\n- `4e96321e` — SL-347 U1b: bare juxtaposition is refused\n\nNo XFAIL added. No finding filed: nothing outside the brief's scope came up.\nPARKED for the lead's validation and `patch add SL-347`.\n","created":"1790139707","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-347.p1 (revision 1): SL-347 (RULED Sep 22): semicolon is the one-line statement separator (never a terminator), bare statement juxtaposition is REFUSED with a fixit, keyword lookaheads restore the statement-ending newline, the corpus sweep (244 .saw sites + 27 doc fences, R5 reformats, no EXPECT changed), 4 matrix pins, spec/skill/README (the brace-body layout rule); battery 32/32","created":"1790139797","id":"c3"}],"created":"1790086803","description":"# `;` is the one-line statement separator; bare juxtaposition is refused; a brace body is one line or fully broken\n\nRULED by the user, Sep 22 2026. Lead-authored brief; implemented by a dispatched agent after SL-318.p7 lands (the parser change touches every one-line body in the corpus, so it must not race an in-flight branch).\n\n## 0. The finding\n\nsawtracker's source carries shapes like\n\n```saw\nif arg == \"--help\" { help()\n    return }\n```\n\nwhich the grammar accepts because Saw already parses SEVERAL STATEMENTS ON ONE LINE WITH NO SEPARATOR AT ALL: `if arg == \"--help\" { help() return 1 }` compiles and runs today (probed Sep 22; the two-space spelling the skill shows, `{ c in c = c + n  c }`, is a convention, not grammar). The separator is invisible, so a reader cannot tell one statement from two on a line (`let a = b (c)`, `x -1`), and the half-broken brace layout hides the body's last statement beside the closing brace. The user's ruling: what we allow now is confusing and could lead to unexpected bugs.\n\n## 1. The rules (RULED)\n\n**R1 — `;` separates two statements on one line.** `a; b` is two statements. Whitespace around it is free. The lexer already has the token (used in `[T; N]`), so nothing new is lexed; the type-position meaning is untouched because a `;` inside `[ ]` in a TYPE is not in statement position.\n\n**R2 — `;` is a SEPARATOR, never a terminator.** A `;` followed by a newline, by `}`, by EOF, by another `;`, or a `;` at the start of a line is a clean error: ``a `;` joins two statements on one line; it does not end one — remove it`` (Saw has no statement terminators; that stays true). This keeps the language's \"no semicolons\" character: you write one only where two statements share a line.\n\n**R3 — BARE JUXTAPOSITION IS REFUSED.** After a statement, the parser requires NEWLINE, `;`, the block's closing `}`, or EOF (and, inside a match arm's bare body, the arm's `,` — the arm grammar is unchanged). Anything else is ``two statements on one line need a `;` between them`` with a fixit naming the two statements' first tokens. The position matrix (obligation 1 — ONE chokepoint, its docstring naming the entries): function/method bodies, `if`/`else`/`while`/`for`/`match`-arm BLOCK bodies, closure bodies (`{ x in a; b }`), `try { } catch { }` blocks, `guard … else { }`, the `borrows` body, top-level module scope (two DECLARATIONS on one line are refused too — declarations are not joined by `;`, they take a newline). Every entry gets a row in the errors pin.\n\n**R4 — `[T; N]`, the repeat literal `[v; N]` and nothing else keep their `;`.** Both are inside brackets where no statement can start.\n\n**R5 — STYLE (the skill ruling, lenient form): a brace body is ONE LINE or FULLY BROKEN, never half-way.** Either the whole body sits on the brace's line (`if done { return }`, `{ c in c = c + n; c }`), or the opening brace ends its line, every statement has its own line, and the closing brace stands alone. `{ help()` + newline + `return }` is the shape the rule exists to catch, and it stays DISALLOWED BY STYLE even though the grammar accepts it (R3 does not refuse it — the newline is a legal separator). Prefer the broken form as soon as a body carries a `return`/`break`/`continue` beside anything else.\n\n## 2. Units (one branch, the two must land together — the refusal breaks the corpus without the sweep)\n\n**U1 — parser + diagnostics + docs.** The statement-end chokepoint in `sawc/parser/core.py` (name it; every block-parsing entry calls it), R2's refusals, R3's refusal with the fixit; the SELFHOST parser/lexer twins (`lexdiff`/`astdiff` parity lanes — the selfhost lexer already lexes `;`); `examples/errors/` pins: one file per R2 shape, one file with a row per R3 position, and a RUNNING example of `;`-joined one-liners in every block kind incl. the closure tail (`{ x in let y = x + 1; y * 2 }`), the match-arm block, `[T; N]` and `[v; N]` beside them (R4). LANGUAGE_SPEC (the statement grammar + R5 in the style section), the saw-lang skill (the \"NO semicolons\" line in Core syntax is rewritten: no TERMINATORS; `;` joins two statements on one line; the STYLE ruling R5 verbatim under the user's Sep-22 date; every two-space example in the skill gains its `;`), README where it says newlines end statements.\n\n**U2 — the corpus sweep (mechanical; a mech/Sonnet agent is fine under the brief's oracle: the compiler's own R3 refusal + the full suite; nothing to invent).** Every one-line multi-statement body in the tree gets its `;`: `examples/`, `examples/conformance/`, `sawc/std/`, `sawc/rt/`, `sawc/builtin.saw`, `blade/`, `libs/`, `devtools/`, `tests/`, `selfhost/`. The count is what U1's refusal reports over the corpus — record it. sawtracker's sources are NOT this repo's (record its count read-only; the user sweeps that repo). The half-broken layouts (R5) found in THIS tree are reformatted in the same sweep, listed.\n\n## 3. Obligations\n\n- **Obligation 1:** R3 is position-quantified → the one statement-end chokepoint with named entries + the row-per-position pin.\n- **Obligation 2 (consumer sweep, a contract flip: juxtaposition legal → refused):** who writes one-line multi-statement Saw? The corpus (U2 counts it), the coroutine transform and the `#lend_var` fold (they generate AST, not text — confirm no text-level emission anywhere: grep `sawc/` for source-string synthesis), `--emit-docs`/`--emit-ast` (readers, unaffected), the fuzzer (`tools/sawfuzz.py` mutates text — a mutation that joins lines now yields a clean diagnostic, which is its oracle's success case; confirm no `sawfuzz_known.txt` entry rests on juxtaposition), `devtools/dogfood` programs (records of past sessions; if they stop compiling they were not compiling before either — check), sawtracker (external, read-only).\n- **Obligation 4:** the finding's mechanism is \"the statement loop never asked what ends a statement\"; the sibling position that mechanism reaches is the top-level declaration loop (two declarations on a line) — R3 covers it as a row.\n\n## 4. Gates + sequencing\n\nCompiler branch: per-commit full suite + freestanding both arches; terminal full battery (lexdiff/astdiff parity is where a selfhost divergence would show). Sequenced AFTER SL-318.p7 lands; before SL-335 (`#lend_var` retirement) and the U3 follow-ups, since every later branch would otherwise carry the sweep's conflicts.\n","id":"SL-347","labels":["design","parser","style"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":5,"sequence":1228,"stage":"queued","status":"open","title":"Design (RULED Sep 22): semicolon is the one-line statement separator, bare juxtaposition is refused, and a brace body is one line or fully broken (style)","updated":"1790139797"},"before":{"acceptance":[],"assignee":"agent:claude-sl347","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sl347","body":"## SL-347 patch 1 (U1a + docs) — BUILT, branch `sl347`, PARKED for lead validation\n\nScope per the lead's Sep-22 rescope: the chokepoint, R1 accepted, R2 refused,\nR4 untouched, the pins, the docs. R3's juxtaposition refusal and the corpus\nsweep are patch 2. No XFAIL added, no corpus file swept, the skill's existing\ntwo-space one-liners untouched.\n\n### The chokepoint and its entries\n\n`Parser.expect_statement_end(previous_start=None, *, declarations=False)` in\n`sawc/parser/core.py`, beside `skip_newlines`. Called once per GAP between\nstatements — including the gap BEFORE a list's first statement, where\n`previous_start` is None, nothing precedes, and only the line-start half of R2\ncan fire. Entries, named in its docstring, all three:\n\n1. `StatementsMixin.parse_block` — the ONE block-body funnel, which is why the\n   rule reaches function and method bodies, `borrows` bodies, `if`/`else`/\n   `while`/`for` bodies, a `match` arm's BLOCK body, both blocks of\n   `try { } catch { }`, and `guard … else { }`. (Verified: `parse_block` is the\n   only callee at all 13 block sites across `declarations.py`, `statements.py`\n   and `expressions.py`.)\n2. `ExpressionsMixin._parse_closure_body` — closure bodies.\n3. `Parser.parse` — module scope, whose units are DECLARATIONS\n   (`declarations=True`).\n\nA `match` arm's BARE body ends at the arm's `,`, but that body is an expression\nand not a statement list, so it never reaches the chokepoint — `parse_match`\nowns that comma, unchanged.\n\n`Parser.at_statement_end()` is the LOOKAHEAD half of the same rule, replacing\nthe four open-coded `NEWLINE/RBRACE/EOF` tuples at the statement forms whose\noperand is optional: `return`, `break`, `lend`, and the closure body's `return`\n(which also gains EOF, which it did not test before).\n\n### The R2 diagnostics, verbatim\n\n    a `;` joins two statements on one line; it does not end one — remove it\n\nfires at all five shapes, each with its own `examples/errors/` pin:\n\n| shape | pin | anchor |\n|---|---|---|\n| before a newline | `semicolon_before_newline.saw` | 11:14 |\n| before the block's `}` | `semicolon_before_closing_brace.saw` | 12:19 |\n| at end of file | `semicolon_at_end_of_file.saw` | 12:22 |\n| doubled | `semicolon_doubled.saw` | 11:14 |\n| at the start of a line | `semicolon_at_line_start.saw` | 13:5 |\n\nand at module scope, where a `;` really does separate two declarations:\n\n    declarations are not joined by `;` — put each declaration on its own line\n\npinned by `examples/errors/semicolon_between_declarations.saw` (11:24). Order\nmatters: the R2 check runs first, so a trailing `;` at EOF gets the \"ends\nnothing\" message and only a `;` with a declaration after it gets this one.\n\nR4 needs no code: the `;` in `[Int; 3]` and `[0; 3]` sits inside brackets,\nwhere no statement can begin, and the chokepoint is never reached there.\n\n### The running pin\n\n`examples/semicolon_joins_statements_on_one_line.saw` — one `;`-joined\none-liner per block kind the chokepoint covers: function body, method body,\nboth branches of an `if`, a `while` body, a `for` body, a `match` arm BLOCK\nbody, a closure body whose last statement is the closure's TAIL\n(`run({ x in let y = x + 1; y * 2 })`), both blocks of `try { } catch { }`, a\n`guard … else { }`, and a `borrows` body — with `[Int; 3]` and `[0; 3]` beside\nthem. 17 EXPECT-OUTPUT lines, all matched.\n\n### Corpus evidence (behaviour preservation)\n\nAll **3387** tracked `.saw` files parse to the SAME outcome as main — 3278\naccept, 109 refuse — with exactly one message improved:\n`prototypes/minivm/examples/reject_semicolon.saw` was\n`Unexpected token: SEMICOLON` and is now the R2 message. It is a minivm\nRejectCase with no `compare_sawc`, and its `differential_manifest.json` row is\n`agreement` on the OUTCOME (reject), which is unchanged.\n\n### R3 dry run — what patch 2 owes (report-only; nothing shipped)\n\nMonkeypatched the chokepoint to RECORD rather than refuse. **174 juxtaposition\nsites in 83 files**, this tree (pre-U3):\n\n| tree | sites | files |\n|---|---|---|\n| `examples/` | 127 | 52 |\n| `examples/conformance/` | 31 | 20 |\n| `examples/errors/` | 9 | 7 |\n| `sawc/std/` | 5 | 2 |\n| `prototypes/` | 2 | 2 |\n| `blade/`, `libs/`, `devtools/`, `tests/`, `selfhost/`, `sawc/rt/`, `sawc/builtin.saw` | 0 | 0 |\n\nDeclaration-position sites (two declarations on one line): **0**.\n\n**A MECHANISM PATCH 2 MUST HANDLE, found by the dry run.** The naive detector\nfirst reported **6597** sites. 6423 of those are false: `parse_if_expression`\n(both the `if` and the `if let` arms) and `parse_try_catch` call\n`skip_newlines()` to look ahead for `else`/`catch`, so when no `else`/`catch`\nfollows, the statement-ending newline is already CONSUMED by the time the\nchokepoint runs. A plain multi-line `if` followed by the next statement then\nlooks exactly like juxtaposition. The 174 above are what remains once a site is\nrequired to be on the SAME source line. Patch 2 must make those lookaheads\nrestore the position when the keyword does not follow (behaviour-neutral today,\nsince the enclosing loop skips the newline anyway) before R3 can be switched on.\n\n### Gate\n\nPer-commit gate on the tip, under the machine-wide lock:\n\n- full suite: **2604 passed, 10 xfailed** (the 10 are the pre-existing cited\n  pins; none added, none removed)\n- `tools/freestanding_runner.py`: **36 passed across riscv32 + arm64**\n- `tools/docverify.py` for the three doc sources touched: LANGUAGE_SPEC 373\n  blocks OK (105 compiled / 62 refused / 206 exempt), README 27 OK, SKILL 108 OK\n\nTerminal battery: DEFERRED to patch 2's final tip by the lead's ruling. Before\nthe stop it had run these stages green — `suite` (62s, 2604/10),\n`icebreadcrumb`, `lexdiff`, `astdiff`, `astgraft`, `transferdecisions`,\n`producertaxonomy`, `corodiscovery`, `corototality`, `stdseed`, `citations`,\n`forgetgate`, `ircontract`, `preludegate`, `docverify`, `moduletag`,\n`stdtypes`, `toolchain`, `floatvectors`, `abidoc`, `bttable`, `fuzz`,\n`corodiff`, `bench`, `selfhostlex`, `minivm`, `reemit` (1614 identical, 0\nDIVERGENT) — and `irdet` was killed mid-run (SIGTERM) after 1216 of 1761\nrecords with **0 MISMATCH, 0 VIOLATED INVARIANT**. One earlier full-battery run\nreported its `suite` stage failing; the suite then passed three consecutive\ntimes (standalone, `battery.sh suite`, and inside the second battery), so that\none is unexplained and unreproduced — flagging it rather than absorbing it.\n\nSelfhost: there is no selfhost PARSER yet (`selfhost/` holds the lexer only),\nand its lexer already emits `Semicolon`, so nothing was owed there. `lexdiff`\nand `selfhostlex` are green.\n\n### Commits (branch `sl347`, on `main`)\n\n- `ef45ca5b` — SL-347 U1a: `;` is the one-line statement separator\n- `95983096` — SL-347 docs: the statement separator and the brace-body layout rule\n\nDocs: LANGUAGE_SPEC's \"Statement Boundaries\" states the grammar and adds a\n\"Layout\" subsection carrying R5; the saw-lang skill's \"No semicolons\" line is\nrewritten and R5 is quoted verbatim under \"(user ruling, Sep 22)\"; README gains\nthe rule as a fifth smaller rule; `designs/todo.md` gains the `[QUEUE]` line\nnaming the two-patch split. Spec and skill both say plainly that bare\njuxtaposition is still accepted today and is being withdrawn.\n\nPARKED. Awaiting the lead's word to start patch 2 from the post-U3 main.\n","created":"1790102472","id":"c1"},{"author":"agent:claude-sl347b","body":"## SL-347 patch 2 (the lookahead restore + U2 sweep + R3 refusal) — BUILT, branch `sl347`, PARKED\n\nThree new commits on top of patch 1, rebased onto the post-U3 main. Terminal\nbattery GREEN, all 32 stages.\n\n### The rebase\n\n`git rebase origin/main` (1938880b). One conflict, in `designs/todo.md`'s\n`[QUEUE]`: main had SL-333 rewritten from **BUILT** to **LANDED Sep 21\n(SL-333.p1 r5, merge 47373c08)**, my side had SL-333 still **BUILT** plus the\nnew SL-347 line. Resolved hunk by hunk in the editor — kept main's LANDED text\nverbatim and re-added the SL-347 line above it. Sanity-checked afterwards: the\nfile holds exactly one SL-347 entry, main's `LANDED Sep 21 (SL-333.p1 r5` text,\nthe `SL-318.p7` mention and the design-275 U3 entry, and `git diff origin/main\n-- designs/todo.md` is exactly the SL-347 addition and nothing else.\nSKILL.md / LANGUAGE_SPEC.md / README.md auto-merged.\n\nGate on the rebased tip BEFORE any new work: patch 1's running pin\n(`examples/semicolon_joins_statements_on_one_line.saw`) compiles and prints its\n17 lines; full suite **2645 passed, 10 xfailed**; freestanding **36 passed\nacross riscv32 + arm64**.\n\n### 1. The lookahead restore — `876f5f7d`\n\nFour sites peek ACROSS newlines for a keyword and did not rewind on the miss:\n`parse_if_expression`'s `else` in BOTH arms (the `if let` one and the plain\none), the inline `try … catch`, and — found by the grep the brief asked for,\nover every `skip_newlines()` followed by a keyword test — the `module name\n{ … }` head, which is the DECLARATION-scope twin of the same mistake. Each now\nsaves the position and restores it when the keyword does not follow, the shape\n`while let`'s `else` refusal (`statements.py:463`) and a trait method's default\nbody (`declarations.py:537`) already used.\n\n**The dry-run count: 6726 → 246.** Measured with the chokepoint monkeypatched\nto RECORD at its entry — exactly where R3's refusal now stands — over all 3435\ntracked `.saw` files. The SAME-LINE subset, which is the real sites, is **246\neither way**, which is what makes the restore behaviour-neutral: it removes\n6480 phantom sites and no real one. (Patch 1 reported 6597 on the pre-U3 tree\nwith 174 real; U3's new files account for the growth.) Full suite and\nfreestanding green on this commit alone.\n\n### 2. The sweep — `ec796798`\n\nThe compiler is the oracle, run to a fixpoint. **Zero sites remain** anywhere.\n\n| tree | sites | files |\n|---|---|---|\n| `examples/` | 176 | 57 |\n| `examples/conformance/` | 52 | 25 |\n| `examples/errors/` | 11 | 8 |\n| `sawc/std/` (json, taskgroup) | 5 | 2 |\n| `prototypes/` | 2 | 2 |\n| `blade/`, `libs/`, `devtools/`, `tests/`, `selfhost/`, `sawc/rt/`, `builtin.saw` | 0 | 0 |\n| **`.saw` total (fixpoint)** | **244** | **94** |\n| string INTERPOLATION, by hand | 2 | 2 |\n| doc fences (LANGUAGE_SPEC 13, SKILL 14, README 0) | 27 | 2 |\n| skill fragments neither docverify shape parses, by hand | 5 | 1 |\n\nTwo things the mechanical pass could NOT do, and why:\n\n- **A string-interpolation sub-parser numbers its own tokens from 1:1**\n  (`_parse_expression_from_string`), so a site inside one carries a position\n  that is not the file's. The recorder keeps those apart instead of trusting\n  them — a first attempt did trust them and wrote `; ; ; ; …` into a comment\n  on line 1 before I caught it and reverted. The two real ones are\n  `examples/arc_with_unique.saw:39` and\n  `examples/coro_inline_catch_loop_exit_in_child_positions.saw:2669`.\n- **`docverify.extract` DEDENTS a fence body by its opening indent**, so a\n  site's column inside the body is `indent` characters left of its column in\n  the markdown. Every doc edit is rebased by its fence's indent and then\n  VERIFIED against the token it is supposed to precede before it lands (a\n  mismatch aborts the tool). The first doc pass, without that, put a `;` two\n  characters off — reverted and redone.\n\n**R5 — every half-broken layout in this tree, listed and reformatted:**\n\n| file | what |\n|---|---|\n| `examples/errors/capture_assign_escaping.saw` | the escaping closure `{ n = n + 1` / `n }` |\n| `examples/init_fallible_result.saw` | two `Printable` match arms |\n| `examples/init_fallible_result_shapes.saw` | one `Printable` match arm |\n| `prototypes/minivm/examples/builders/` | 7 fixtures (`reject_append_arity`, `reject_append_label`, `reject_append_type`, `reject_assignment`, `reject_copy`, `reject_nested_receiver`, `reject_shared_append`) |\n| `prototypes/minivm/examples/vectors/` | 14 fixtures (`evaluation_once` ×2, `index_at_end`, `index_negative`, `reject_assignment`, `reject_borrow_after_move`, `reject_double_move`, `reject_get_label`, `reject_implicit_copy`, `reject_index_type`, `reject_move_field`, `reject_move_reference`, `reject_outer_loop_move` ×2, `reject_push_label`, `reject_push_type`) |\n\nThe four hits the R5 scan still reports are `import m.{...}` symbol lists,\nwhich design 147 makes a delimited list rather than a statement container —\nthey wrap by design.\n\n**NO EXPECT line changed**: `git diff` filtered to every `EXPECT-`, `XFAIL`\nand `COMPILE-FLAGS` line over the whole sweep is empty. Gate on this commit:\nsuite 2645/10, freestanding 36, plus the four lanes that cover what moved —\ndocverify (513 blocks OK), minivm (446 cases), lexdiff, astdiff (3435 files,\nbyte-stable).\n\n### 3. R3 — `4e96321e`\n\n`Parser.expect_statement_end` refuses anything but NEWLINE / `;` / `}` / EOF,\nanchored at the SECOND statement's first token — where the separator is\nmissing — and naming BOTH heads. Verbatim:\n\n```\nerror: Parse error at 2:19: two statements on one line need a `;` between them: the first begins with `let`, the second with `print` — write `;` where they meet\n```\n\nand at module scope, where the units are declarations:\n\n```\nerror: Parse error at 24:23: two declarations on one line: the first begins with `static`, the second with `static` — declarations are not joined by `;` — put each declaration on its own line\n```\n\nA `match` arm's BARE body is untouched — it ends at the arm's `,`, which\n`parse_match` owns, and that body is an expression, not a statement list.\n\n**THE POSITION MATRIX — 13 rows over four `examples/errors/` pins.** Split\nacross files on purpose: the file-level parse loop batches at most\n`MAX_PARSE_ERRORS = 10` and recovery adds an orphan-`}` diagnostic per row, so\na single 13-row file TRUNCATES and half the matrix would go unchecked. Every\nrow names a different pair of first tokens, so no row's expectation can be\nsatisfied by another row's error.\n\n| pin | rows |\n|---|---|\n| `statement_juxtaposition_in_function_and_method_bodies.saw` | function body, method body, `borrows` body |\n| `statement_juxtaposition_in_control_flow_bodies.saw` | `if`, `else`, `while`, `for` |\n| `statement_juxtaposition_in_match_guard_and_try_blocks.saw` | `match` arm BLOCK body (bare arm beside it), `guard … else`, `try { }`, `catch { }` |\n| `statement_juxtaposition_in_closure_and_module_scope.saw` | closure body (`_parse_closure_body`), module scope (`Parser.parse`) |\n\n### Obligation 2 — the consumer sweep, with evidence\n\n1. **The fuzzer.** `tools/sawfuzz.py`'s oracle is \"succeeds or exits with a\n   clean diagnostic\", so a line-joining mutation now yields R3's refusal, which\n   is a PASS. `tools/sawfuzz_known.txt` holds exactly ONE entry — DF-270e,\n   `internal-compiler-error: … Undefined function: UIntN` — and nothing in it\n   rests on juxtaposition. `sawfuzz --quick` in the battery: 150 mutants,\n   **0 known hits, 0 failing compiles, 0 NEW findings**.\n2. **Passes that synthesize SOURCE TEXT rather than AST.** There is exactly\n   one: `ExpressionsMixin._parse_expression_from_string`, which re-lexes the\n   text of a string interpolation — text the AUTHOR wrote, so the rule reaches\n   it exactly as it reaches the enclosing file, and the two corpus sites are\n   swept. The coroutine transform builds AST and never constructs source (zero\n   `Lexer`/`tokenize` references); `place_transform` names the lexer only in a\n   comment about running inside `parse_source`; the std seed writes a cache\n   blob and `--emit-docs` writes JSON; `ast_dump`'s `f\"func …\"` strings are a\n   DUMP, never re-parsed. The only other `Lexer(...)` callers are `sawc.py` and\n   `module_resolver.py`, both over real files.\n3. **Saw embedded in the PYTHON HARNESSES — a third home for Saw source that\n   neither the corpus sweep nor the doc sweep reaches.** The battery caught one:\n   `tools/test_transfer_decisions.py`'s `generic_body_defers_to_specialization`\n   fixture (`func hold<T>(x: T) -> Int { let a = x  1 }`), which failed the\n   `transferdecisions` lane and is fixed in the R3 commit. A scan of every\n   triple-quoted Saw string under `tools/`, `devtools/` and `prototypes/` finds\n   four more, all in `prototypes/parser/test_parser.py` — and those are INPUT\n   fed to the selfhost parser PROTOTYPE binary, not programs sawc compiles\n   (sawc only compiles `prototypes/parser/src`). Its own harness already\n   expects a `boundary` error for `func f() { x = 1 y = 2 }`, so the prototype\n   agrees with R3 and the fixtures stay as written.\n4. **`devtools/dogfood`.** 5 of 7 do not compile — `w1_chatroom`,\n   `w1_filesearch`, `w1_limiter`, `w1_mapreduce`, `w1_pipeline` — and each\n   fails IDENTICALLY under main's compiler (verified by running the main\n   checkout's `sawc` on the same files): design 234 allocator-tier drift (a\n   discarded `Result`; `Channel()`/`Mutex(...)` now returning one). Not a parse\n   error among them. `llm_client` and `w1_jobqueue` compile.\n\nObligation 3 is not engaged: R3 is a grammar refusal, not a safety guarantee,\nso it owes no `examples/conformance/` row — the four errors/ matrix pins are\nits coverage.\n\n### Docs\n\nLANGUAGE_SPEC's and the skill's \"still accepted today … being withdrawn\"\nsentences become the refusal, each carrying the diagnostic (as a pinned\n`saw-error` block) and the full position list; README's fifth smaller rule\ngains the clause. The skill adds the older-build note, since\n`{ c in c = c + n  c }` compiled until yesterday. `designs/todo.md`'s SL-347\nentry is closed in place with the p2 landing note.\n\n### The terminal battery — ONE run, on the final tip\n\n**32 of 32 stages GREEN in 3817s.**\n\n- `suite` **2649 passed, 10 xfailed** (four new pins; the xfail count did not\n  move — none added, none removed)\n- `freestanding` 36 passed across riscv32 + arm64\n- `docverify` 515 blocks OK (141 compiled, 79 refused, 295 exempt)\n- `astdiff` 3435 files, byte-stable; `lexdiff`, `selfhostlex` green\n- `minivm` 446 cases; `corodiff` 45 pairs, 0 new findings\n- `fuzz` 150 mutants, 0 known hits, 0 new findings\n- `reemit` identical 1638, **DIVERGENT 0**\n- `irdet --all` 1634 examples, byte-identical IR, 0 mismatch / 0 invariant\n- `bootstrap`, `gmgate`, `transferdecisions`, `corodiscovery`, `corototality`,\n  `windowseam`, `ircontract` and the rest green\n\n**The one failure, recorded not absorbed.** The FIRST full battery run (before\nthe harness fix) failed exactly one stage: `transferdecisions`, on the embedded\nfixture above — a real regression my sweep had not reached, not a flake. The\nfixture is fixed, the lane re-run standalone (`14 boundary fixtures, 6 identity\nchecks, 33273 decisions`), and the WHOLE battery re-run from scratch on the\namended tip, which is the 32/32 above. Nothing else flaked in either run.\n\n### Commits (branch `sl347`, on `origin/main` 1938880b)\n\n- `de3b0388` — SL-347 U1a: `;` is the one-line statement separator *(patch 1, rebased)*\n- `5c02d37e` — SL-347 docs: the statement separator and the brace-body layout rule *(patch 1, rebased)*\n- `876f5f7d` — SL-347 U1b/1: a keyword peek restores the statement-ending newline\n- `ec796798` — SL-347 U2: the corpus takes the `;` every one-line body now owes\n- `4e96321e` — SL-347 U1b: bare juxtaposition is refused\n\nNo XFAIL added. No finding filed: nothing outside the brief's scope came up.\nPARKED for the lead's validation and `patch add SL-347`.\n","created":"1790139707","id":"c2"}],"created":"1790086803","description":"# `;` is the one-line statement separator; bare juxtaposition is refused; a brace body is one line or fully broken\n\nRULED by the user, Sep 22 2026. Lead-authored brief; implemented by a dispatched agent after SL-318.p7 lands (the parser change touches every one-line body in the corpus, so it must not race an in-flight branch).\n\n## 0. The finding\n\nsawtracker's source carries shapes like\n\n```saw\nif arg == \"--help\" { help()\n    return }\n```\n\nwhich the grammar accepts because Saw already parses SEVERAL STATEMENTS ON ONE LINE WITH NO SEPARATOR AT ALL: `if arg == \"--help\" { help() return 1 }` compiles and runs today (probed Sep 22; the two-space spelling the skill shows, `{ c in c = c + n  c }`, is a convention, not grammar). The separator is invisible, so a reader cannot tell one statement from two on a line (`let a = b (c)`, `x -1`), and the half-broken brace layout hides the body's last statement beside the closing brace. The user's ruling: what we allow now is confusing and could lead to unexpected bugs.\n\n## 1. The rules (RULED)\n\n**R1 — `;` separates two statements on one line.** `a; b` is two statements. Whitespace around it is free. The lexer already has the token (used in `[T; N]`), so nothing new is lexed; the type-position meaning is untouched because a `;` inside `[ ]` in a TYPE is not in statement position.\n\n**R2 — `;` is a SEPARATOR, never a terminator.** A `;` followed by a newline, by `}`, by EOF, by another `;`, or a `;` at the start of a line is a clean error: ``a `;` joins two statements on one line; it does not end one — remove it`` (Saw has no statement terminators; that stays true). This keeps the language's \"no semicolons\" character: you write one only where two statements share a line.\n\n**R3 — BARE JUXTAPOSITION IS REFUSED.** After a statement, the parser requires NEWLINE, `;`, the block's closing `}`, or EOF (and, inside a match arm's bare body, the arm's `,` — the arm grammar is unchanged). Anything else is ``two statements on one line need a `;` between them`` with a fixit naming the two statements' first tokens. The position matrix (obligation 1 — ONE chokepoint, its docstring naming the entries): function/method bodies, `if`/`else`/`while`/`for`/`match`-arm BLOCK bodies, closure bodies (`{ x in a; b }`), `try { } catch { }` blocks, `guard … else { }`, the `borrows` body, top-level module scope (two DECLARATIONS on one line are refused too — declarations are not joined by `;`, they take a newline). Every entry gets a row in the errors pin.\n\n**R4 — `[T; N]`, the repeat literal `[v; N]` and nothing else keep their `;`.** Both are inside brackets where no statement can start.\n\n**R5 — STYLE (the skill ruling, lenient form): a brace body is ONE LINE or FULLY BROKEN, never half-way.** Either the whole body sits on the brace's line (`if done { return }`, `{ c in c = c + n; c }`), or the opening brace ends its line, every statement has its own line, and the closing brace stands alone. `{ help()` + newline + `return }` is the shape the rule exists to catch, and it stays DISALLOWED BY STYLE even though the grammar accepts it (R3 does not refuse it — the newline is a legal separator). Prefer the broken form as soon as a body carries a `return`/`break`/`continue` beside anything else.\n\n## 2. Units (one branch, the two must land together — the refusal breaks the corpus without the sweep)\n\n**U1 — parser + diagnostics + docs.** The statement-end chokepoint in `sawc/parser/core.py` (name it; every block-parsing entry calls it), R2's refusals, R3's refusal with the fixit; the SELFHOST parser/lexer twins (`lexdiff`/`astdiff` parity lanes — the selfhost lexer already lexes `;`); `examples/errors/` pins: one file per R2 shape, one file with a row per R3 position, and a RUNNING example of `;`-joined one-liners in every block kind incl. the closure tail (`{ x in let y = x + 1; y * 2 }`), the match-arm block, `[T; N]` and `[v; N]` beside them (R4). LANGUAGE_SPEC (the statement grammar + R5 in the style section), the saw-lang skill (the \"NO semicolons\" line in Core syntax is rewritten: no TERMINATORS; `;` joins two statements on one line; the STYLE ruling R5 verbatim under the user's Sep-22 date; every two-space example in the skill gains its `;`), README where it says newlines end statements.\n\n**U2 — the corpus sweep (mechanical; a mech/Sonnet agent is fine under the brief's oracle: the compiler's own R3 refusal + the full suite; nothing to invent).** Every one-line multi-statement body in the tree gets its `;`: `examples/`, `examples/conformance/`, `sawc/std/`, `sawc/rt/`, `sawc/builtin.saw`, `blade/`, `libs/`, `devtools/`, `tests/`, `selfhost/`. The count is what U1's refusal reports over the corpus — record it. sawtracker's sources are NOT this repo's (record its count read-only; the user sweeps that repo). The half-broken layouts (R5) found in THIS tree are reformatted in the same sweep, listed.\n\n## 3. Obligations\n\n- **Obligation 1:** R3 is position-quantified → the one statement-end chokepoint with named entries + the row-per-position pin.\n- **Obligation 2 (consumer sweep, a contract flip: juxtaposition legal → refused):** who writes one-line multi-statement Saw? The corpus (U2 counts it), the coroutine transform and the `#lend_var` fold (they generate AST, not text — confirm no text-level emission anywhere: grep `sawc/` for source-string synthesis), `--emit-docs`/`--emit-ast` (readers, unaffected), the fuzzer (`tools/sawfuzz.py` mutates text — a mutation that joins lines now yields a clean diagnostic, which is its oracle's success case; confirm no `sawfuzz_known.txt` entry rests on juxtaposition), `devtools/dogfood` programs (records of past sessions; if they stop compiling they were not compiling before either — check), sawtracker (external, read-only).\n- **Obligation 4:** the finding's mechanism is \"the statement loop never asked what ends a statement\"; the sibling position that mechanism reaches is the top-level declaration loop (two declarations on a line) — R3 covers it as a row.\n\n## 4. Gates + sequencing\n\nCompiler branch: per-commit full suite + freestanding both arches; terminal full battery (lexdiff/astdiff parity is where a selfhost divergence would show). Sequenced AFTER SL-318.p7 lands; before SL-335 (`#lend_var` retirement) and the U3 follow-ups, since every later branch would otherwise carry the sweep's conflicts.\n","id":"SL-347","labels":["design","parser","style"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":4,"sequence":1227,"stage":"queued","status":"open","title":"Design (RULED Sep 22): semicolon is the one-line statement separator, bare juxtaposition is refused, and a brace body is one line or fully broken (style)","updated":"1790139707"},"id":"SL-347","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/.claude/skills/saw-lang/SKILL.md b/.claude/skills/saw-lang/SKILL.md\\nindex ed60edd8..13d1b680 100644\\n--- a/.claude/skills/saw-lang/SKILL.md\\n+++ b/.claude/skills/saw-lang/SKILL.md\\n@@ -14,9 +14,10 @@ below.\\n ```saw-fragment\\n let x = 42                 // immutable binding (must initialize)\\n var n = 0                  // mutable\\n-n += 1                     // …and there are NO semicolons: a newline ends it\\n+n += 1                     // no TERMINATORS: a newline separates two statements\\n+let w = 3; let h = 4       // …and `;` separates two that share a line (SL-347)\\n func f(a: Int, b: Int = 2) -> Int { a * b }   // default param (trailing)\\n-f(5)  f(5, 3)  f(5, b: 3)  // labels optional; required only on ambiguity\\n+f(5); f(5, 3); f(5, b: 3)  // labels optional; required only on ambiguity\\n struct Point { x: Int, y: Int }\\n extension Point { func mag(&self) -> Int { self.x * self.x } }\\n extension Point { init(m: Int) -> Point { Point(x: m, y: m) } }\\n@@ -398,7 +399,44 @@ print(\\\"{#file}:{#line} - msg\\\")  // #file/#line/#function: definition-site consts\\n - Escapes: exactly `\\\\\\\\ \\\\\\\" \\\\n \\\\t \\\\r \\\\0 \\\\u{1F600}` + `\\\\{ \\\\}` (brace forms);\\n   `\\\\0` is an interior NUL that `len()` counts. Any other escape is a lex\\n   error (no silent drop). Strings are immutable UTF-8, refcounted.\\n-- Comments `//`. No semicolons. `not` for logical negation.\\n+- Comments `//`. `not` for logical negation.\\n+- **`;` IS THE ONE-LINE STATEMENT SEPARATOR, and Saw has no TERMINATORS\\n+  (SL-347).** A newline separates two statements; a `;` separates two that\\n+  share a line — `if done { print(\\\"ok\\\"); return }`, `{ c in c = c + n; c }`.\\n+  It JOINS, so a `;` that separates nothing is a clean error where it stands:\\n+  before a newline, before the block's `}`, at end of file, doubled, or at the\\n+  start of a line, all of them ``a `;` joins two statements on one line; it\\n+  does not end one — remove it``. Module scope takes newlines only\\n+  (``declarations are not joined by `;` — put each declaration on its own\\n+  line``). The `;` in `[Int; 3]` and `[0; 3]` is untouched: it sits inside\\n+  brackets, where no statement can begin. BARE JUXTAPOSITION — two statements\\n+  on one line with NOTHING between them — is REFUSED, in every block a\\n+  statement list sits in (function/method body, `borrows` body, `if`/`else`/\\n+  `while`/`for` body, a `match` arm's BLOCK body, both blocks of\\n+  `try { } catch { }`, `guard … else { }`, a closure body) and at module scope\\n+  as the declarations message. The diagnostic names both statements' first\\n+  tokens:\\n+  ```saw-error\\n+  // error-contains: two statements on one line need a `;` between them\\n+  func main() {\\n+      let width = 3 print(width)\\n+  }\\n+  // error: two statements on one line need a `;` between them: the first\\n+  // begins with `let`, the second with `print` — write `;` where they meet\\n+  ```\\n+  A `match` arm's BARE body is untouched: it ends at the arm's `,`, which is\\n+  the arm grammar's and not a statement separator. It COMPILED until Sep 22,\\n+  so a build that accepts `{ c in c = c + n  c }` predates the refusal — and\\n+  the two-space spelling the skill used to show was a convention, never\\n+  grammar.\\n+- **STYLE (user ruling, Sep 22): a brace body is ONE LINE or FULLY BROKEN,\\n+  never half-way.** Either the whole body sits on the brace's line\\n+  (`if done { return }`, `{ c in c = c + n; c }`), or the opening brace ends\\n+  its line, every statement has its own line, and the closing brace stands\\n+  alone. `{ help()` + newline + `return }` is the shape the rule exists to\\n+  catch, and it stays DISALLOWED BY STYLE even though the grammar accepts it\\n+  (the newline is a legal separator). Prefer the broken form as soon as a body\\n+  carries a `return`/`break`/`continue` beside anything else.\\n - Line breaks (design 129): a statement ends at end-of-line, but a newline\\n   between `(`/`)`, `[`/`]`, or inside a COMMITTED generic `<...>` is\\n   insignificant — so argument lists, parameter lists, tuples, collection\\n@@ -1245,13 +1283,13 @@ record(42)                               // and in ARGUMENT position, same rule\\n let rows: Vector<Result<Int, String>> = [1, 2]   // …and every declared slot\\n // There is no `Ok(x)`/`Err(e)` to write: the wrap IS the construction. `T == E`\\n // is the one ambiguity, and it is a compile error — spell the variant then.\\n-try! f()   try? f()   try f() catch { fallback }\\n-let x = try f() catch { print(\\\"{error}\\\")  return -1 }   // the GUARD form: the\\n+try! f(); try? f(); try f() catch { fallback }\\n+let x = try f() catch { print(\\\"{error}\\\"); return -1 }   // the GUARD form: the\\n // catch binds `error` and its body may DIVERGE (return/continue/break/panic)\\n // instead of supplying a fallback — a diverging block satisfies any expected\\n // type (design 228) — so \\\"bind on Ok, handle-and-exit on Err\\\" needs no `match`\\n try(as LocalError.Alloc) alloc(4096)     // ROUTE the error channel (design 234)\\n-try { let a = try f()  let b = try g() } catch {\\n+try { let a = try f(); let b = try g() } catch {\\n     match error { case ParseError(e) -> ..., case IoError(e) -> ... }\\n }   // multi-type: error is an ephemeral union (can't escape the catch)\\n func load() -> Result<Cfg, Box<any Error>> {   // erased: any error type\\n@@ -2069,7 +2107,7 @@ try! v.map<String>({ $0.to_string() })  // the closure's return; explicit still\\n ```saw-fragment\\n import std.task.*                                 // design 114: `yield_now` lives here\\n                                                   // (`import std.task` -> task.yield_now())\\n-func work(n: Int) -> Int { yield_now()  n * n }  // any call may suspend\\n+func work(n: Int) -> Int { yield_now(); n * n }  // any call may suspend\\n func main() {\\n     var group = TaskGroup()\\n     let a = group.spawn(work(3))\\n@@ -2332,7 +2370,7 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n   ```saw-fragment\\n   func drain(ch: Channel<Job>) -> Result<Int, ChannelError> {\\n       var n = 0\\n-      while let job = try ch.try_receive() { run(job)  n = n + 1 }\\n+      while let job = try ch.try_receive() { run(job); n = n + 1 }\\n       return n\\n   }\\n   ```\\n@@ -2355,7 +2393,7 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n       var going = true\\n       while going {\\n           match orders.receive() {\\n-              case Ok(order) -> { fulfil(order)  handled = handled + 1 },\\n+              case Ok(order) -> { fulfil(order); handled = handled + 1 },\\n               case Err(_) -> { going = false }   // closed AND drained\\n           }\\n       }\\n@@ -2759,9 +2797,9 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n   and a method that SATISFIES A TRAIT REQUIREMENT — entry-module and\\n   cross-module alike:\\n   ```saw-fragment\\n-  extension Color { func label(&self) -> String { yield_now()  \\\"red\\\" } }\\n-  extension Box2<T> { func describe(&self) -> String { yield_now()  \\\"boxed\\\" } }\\n-  extension Person: Greeter { func greet(&self) -> String { yield_now()  self.n } }\\n+  extension Color { func label(&self) -> String { yield_now(); \\\"red\\\" } }\\n+  extension Box2<T> { func describe(&self) -> String { yield_now(); \\\"boxed\\\" } }\\n+  extension Person: Greeter { func greet(&self) -> String { yield_now(); self.n } }\\n   ```\\n   Treat all five as working now and SUSPECT in older builds, where the failures\\n   were not alike: the enum receiver was a codegen ICE, the generic-struct\\n@@ -2868,7 +2906,8 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n   ```saw-fragment\\n   while let job = try ch.try_receive() { run(job) }   // in a spawned task\\n   guard let cfg = try load(path) else { return 0 }\\n-  if (try grab(n)) > 0 { … }        for i in 0..(try count()) { … }\\n+  if (try grab(n)) > 0 { … }\\n+  for i in 0..(try count()) { … }\\n   ```\\n   Until then a `try` written in a head was refused with ``cannot propagate\\n   errors from a function returning `Poll` `` — a type the author never wrote —\\n@@ -2979,7 +3018,7 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n   import std.mutex.{Mutex}\\n \\n   func add(shared: Arc<Mutex<Int>>, n: Int) -> Int {\\n-      shared.lock({ &var c in c = c + n  c })   // captures `n`, the parameter\\n+      shared.lock({ &var c in c = c + n; c })   // captures `n`, the parameter\\n   }\\n   // group.spawn(add(shared.copy(), 1))\\n   ```\\n@@ -3041,7 +3080,7 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n   // error-contains: `n` cannot be written here — the task\\n   var n = 0\\n   var group = TaskGroup()\\n-  let h = group.spawn(run({ [&var n] in n = n + 100  n }))\\n+  let h = group.spawn(run({ [&var n] in n = n + 100; n }))\\n   n = 5                    // error: `n` cannot be written here — the task\\n                            //   spawned at line 3 holds `&var n` until\\n                            //   `h.join()` releases it\\n@@ -3529,7 +3568,7 @@ OWN result** — `lock<R>(body: (&var T) sync -> R) -> R`, the same shape\\n `SpinLock.lock` has (M1 landed; DF-123c is closed, and `Arc<Mutex<T>>`\\n forwarding reaches the method-generic `lock` too):\\n ```saw-fragment\\n-let n = shared.lock({ c in c = c + 5  c })   // shared: Arc<Mutex<Int>> -> n == 5\\n+let n = shared.lock({ c in c = c + 5; c })   // shared: Arc<Mutex<Int>> -> n == 5\\n ```\\n \\n ## Systems/embedded corner\\n@@ -4227,7 +4266,7 @@ construct in the owner and lend `&driver` down.\\n   ```saw-fragment\\n   extension Counter {\\n       func viaFree(&self) -> Int { run_int({ self.n + 1 }) }\\n-      func bump(&var self) -> Int { run_int({ self.n = self.n + 10  self.n }) }\\n+      func bump(&var self) -> Int { run_int({ self.n = self.n + 10; self.n }) }\\n   }\\n   ```\\n   ONE rule over three spellings, because a receiver IS a reference: **a capture\\n@@ -4350,8 +4389,8 @@ construct in the owner and lend `&driver` down.\\n   for the other — so the copy tier never comes into it and a move-only element\\n   prints and compares like an `Int` one:\\n   ```saw-fragment\\n-  print(\\\"{v[0]}\\\")   print(v[0])   print(\\\"{}\\\", v[0])    // every rendering slot\\n-  v[0] == w[0]      v[0] < w[0]                        // all six operators\\n+  print(\\\"{v[0]}\\\"); print(v[0]); print(\\\"{}\\\", v[0])    // every rendering slot\\n+  v[0] == w[0]; v[0] < w[0]                        // all six operators\\n   ```\\n   Rendering covers an interpolation operand anywhere, a single-argument `print`\\n   of a `Printable`, and the format arguments of `print`/`panic`/`assert`. Treat\\ndiff --git a/LANGUAGE_SPEC.md b/LANGUAGE_SPEC.md\\nindex a21ed1d4..c1e224e0 100644\\n--- a/LANGUAGE_SPEC.md\\n+++ b/LANGUAGE_SPEC.md\\n@@ -53,7 +53,89 @@ chaining directly on array literals, which is separate and still planned.\\n \\n ### Statement Boundaries\\n \\n-A statement ends at the end of its line. There are no semicolons.\\n+Two statements are separated by a newline, or by a `;` when they share a line.\\n+Saw has no statement terminators: a `;` joins the statement on its left to the\\n+statement on its right, so one that ends nothing is rejected.\\n+\\n+```saw\\n+func main() {\\n+    let width = 3; let height = 4\\n+    print(width * height)       // prints: 12\\n+    if width < height { print(\\\"portrait\\\"); print(\\\"done\\\") }\\n+}\\n+```\\n+\\n+```saw-error\\n+// error-contains: it does not end one\\n+func main() {\\n+    let width = 3;\\n+    print(width)\\n+}\\n+// error: Parse error at 3:18: a `;` joins two statements on one line; it does\\n+// not end one — remove it\\n+```\\n+\\n+The rejection covers every position where a `;` separates nothing: before a\\n+newline, before the block's closing `}`, at the end of the file, doubled, and\\n+standing at the start of a line. Module scope takes newlines only. Declarations\\n+are never joined by a `;`, and one written between two of them reports that\\n+each declaration takes its own line.\\n+\\n+The `;` inside `[Int; 3]` and `[0; 3]` is the array-length and repeat-count\\n+separator. It sits inside brackets, where no statement can begin, and none of\\n+this reaches it.\\n+\\n+Writing two statements on one line with nothing between them is rejected. The\\n+separator would be invisible there, so `let a = b (c)` and `x -1` read as one\\n+statement and are two. The diagnostic names both statements' first tokens,\\n+because on a crowded line the reader's question is which two the compiler ran\\n+together.\\n+\\n+```saw-error\\n+// error-contains: two statements on one line need a `;` between them\\n+func main() {\\n+    let width = 3 print(width)\\n+}\\n+// error: Parse error at 2:19: two statements on one line need a `;` between\\n+// them: the first begins with `let`, the second with `print` — write `;`\\n+// where they meet\\n+```\\n+\\n+It is rejected in every block a statement list can sit in: function and method\\n+bodies, a `borrows` body, the bodies of `if`, `else`, `while` and `for`, a\\n+`match` arm's block body, both blocks of `try { } catch { }`, a `guard … else`\\n+block, and a closure body. At module scope the units are declarations, which\\n+are never joined by a `;` at all, so two on one line report that each takes its\\n+own line instead. A `match` arm's bare body is unaffected: it ends at the arm's\\n+`,`, which is the arm grammar's and not a statement separator.\\n+\\n+#### Layout\\n+\\n+A brace body is one line, or fully broken. Either the whole body sits on the\\n+brace's line, or the opening brace ends its line, every statement has its own\\n+line, and the closing brace stands alone.\\n+\\n+```saw-fragment\\n+if done { return }                          // one line\\n+{ c in c = c + n; c }                       // one line\\n+\\n+if pending.len() == 0 {                     // fully broken\\n+    report(\\\"nothing to do\\\")\\n+    return\\n+}\\n+```\\n+\\n+Half-broken is what the rule exists to catch — an opening line that carries\\n+the first statement while the last one shares the closing brace's line:\\n+\\n+```saw-fragment\\n+if arg == \\\"--help\\\" { help()\\n+    return }\\n+```\\n+\\n+The grammar accepts that (the newline is a legal separator) and the style rule\\n+does not. Prefer the broken form as soon as a body carries a `return`,\\n+`break` or `continue` beside anything else.\\n \\n Inside brackets a line break carries no meaning, so a list that does not fit on\\n one line wraps. This holds between `(` and its matching `)`, between `[` and\\n@@ -3826,7 +3908,7 @@ extension Counter {\\n     func viaFree(&self) -> Int { run_int({ self.n + 1 }) }\\n \\n     func bump(&var self) -> Int {\\n-        run_int({ self.n = self.n + 10  self.n })   // the write lands on the caller's value\\n+        run_int({ self.n = self.n + 10; self.n })   // the write lands on the caller's value\\n     }\\n }\\n ```\\n@@ -3866,7 +3948,7 @@ The capture may also be written out loud, behind a borrow sigil:\\n ```saw-fragment\\n extension Counter {\\n     func viaFree(&self) -> Int { run_int({ [&self] in self.n + 1 }) }\\n-    func bump(&var self) -> Int { run_int({ [&var self] in self.n += 10  self.n }) }\\n+    func bump(&var self) -> Int { run_int({ [&var self] in self.n += 10; self.n }) }\\n }\\n ```\\n \\n@@ -4072,7 +4154,7 @@ changes.\\n   here is that a spawn does not create a reference the law cannot see.\\n \\n ```saw-fragment\\n-let h = group.spawn(run({ [&var n] in n = n + 1  n }))\\n+let h = group.spawn(run({ [&var n] in n = n + 1; n }))\\n let seen = n\\n // error: exclusive access violation: `n` cannot be read here — the task spawned\\n //        at line 1 holds `&var n` until `h.join()` releases it\\n@@ -7221,7 +7303,7 @@ multiple threads (design 75) — carrying the coroutine transform, suspending\\n   say. This is what lets a closure take an early exit:\\n \\n   ```saw\\n-  func each_sync(body: (Int) sync -> Void) { body(1)  body(-2)  body(3) }\\n+  func each_sync(body: (Int) sync -> Void) { body(1); body(-2); body(3) }\\n \\n   func report(v: &Vector<Int>) -> String {\\n       each_sync({ e in\\n@@ -7454,7 +7536,7 @@ Observable rules:\\n   trait Greeter { func greet(&self) -> String }\\n \\n   extension Person: Greeter {\\n-      func greet(&self) -> String { yield_now()  self.n }\\n+      func greet(&self) -> String { yield_now(); self.n }\\n   }\\n \\n   func shout(g: &any Greeter) -> String {\\n@@ -8157,7 +8239,7 @@ struct Pool { workers: Vector<VoidThread> }\\n extension Pool: NoCopy {\\n     func deinit(&var self) {\\n         while self.workers.len() > 0 {\\n-            if let held = self.workers.pop() { var h = move held  h.join() }\\n+            if let held = self.workers.pop() { var h = move held; h.join() }\\n         }\\n     }\\n }\\n@@ -8269,7 +8351,7 @@ A `Thread.spawn { ... }` body is a `sync` context: a spawned thread runs no\\n executor, so there is nothing on it to resume a suspension.\\n \\n ```saw-fragment\\n-var t = Thread.spawn { yield_now()  7 }\\n+var t = Thread.spawn { yield_now(); 7 }\\n // error: cannot suspend in a `Thread.spawn { ... }` body: closure calls\\n //        yield_now\\n ```\\n@@ -8532,7 +8614,7 @@ through a closure the task runs; a `&`/`&var` **argument** of the spawned call\\n reaches it directly:\\n \\n ```saw-fragment\\n-let h = group.spawn(run({ [&var n] in n = n + 1  n }))   // capture\\n+let h = group.spawn(run({ [&var n] in n = n + 1; n }))   // capture\\n let g = group.spawn(bump(&var n))                        // argument\\n ```\\n \\n@@ -8543,7 +8625,7 @@ declared ahead of the group is still alive when the join runs:\\n ```saw-fragment\\n var n = 7\\n var group = TaskGroup()\\n-let h = group.spawn(run({ [&var n] in n = n + 1  n }))\\n+let h = group.spawn(run({ [&var n] in n = n + 1; n }))\\n print(h.join())        // 8\\n print(n)               // 8 — the task's write is visible at the root\\n ```\\n@@ -8556,7 +8638,7 @@ a compile error naming the order, in either spelling:\\n // error-contains: cannot capture `&var n` into a task: `n` is declared AFTER the group\\n var group = TaskGroup()\\n var n = 7\\n-let h = group.spawn(run({ [&var n] in n = n + 1  n }))\\n+let h = group.spawn(run({ [&var n] in n = n + 1; n }))\\n // error: cannot capture `&var n` into a task: `n` is declared AFTER the group\\n //        `group` it is spawned into …, and destruction is LIFO\\n \\n@@ -8597,7 +8679,7 @@ root belongs to the task:\\n // error-contains: exclusive access violation: `n` cannot be written here — the task\\n var n = 0\\n var group = TaskGroup()\\n-let h = group.spawn(run({ [&var n] in n = n + 100  n }))\\n+let h = group.spawn(run({ [&var n] in n = n + 100; n }))\\n n = 5\\n // error: exclusive access violation: `n` cannot be written here — the task\\n //        spawned at line 3 holds `&var n` until `h.join()` releases it\\n@@ -8647,7 +8729,7 @@ slot — silently, exit 0.\\n ```saw-fragment\\n var buf: Vector<Int> = [1, 2, 3]\\n var group = TaskGroup()\\n-let h = group.spawn(run({ [&var buf] in try! buf.push(9)  buf.len() }))\\n+let h = group.spawn(run({ [&var buf] in try! buf.push(9); buf.len() }))\\n let taken = consume(move buf)\\n // error: cannot `move` `buf` while a spawned task borrows it: the task spawned\\n //        at line 3 holds `&var buf` until `h.join()` releases it\\ndiff --git a/README.md b/README.md\\nindex 54fb8cac..bb4da443 100644\\n--- a/README.md\\n+++ b/README.md\\n@@ -682,8 +682,17 @@ redeclare.\\n \\n See [Module System](LANGUAGE_SPEC.md#8-module-system) in the spec.\\n \\n-### Four smaller rules\\n-\\n+### Five smaller rules\\n+\\n+- **A newline separates two statements, and so does `;`.** Saw has no\\n+  statement terminators. A `;` joins the statement on its left to the one on\\n+  its right, which is what a one-line body needs: `if done { print(\\\"ok\\\"); return }`.\\n+  A `;` that separates nothing — before a newline, before `}`, at the end of\\n+  the file, doubled, at the start of a line — is a compile error, and so is\\n+  running two statements together on one line with nothing between them: the\\n+  separator would be invisible, so `let a = b (c)` reads as one statement and\\n+  is two. See\\n+  [Statement boundaries](LANGUAGE_SPEC.md#statement-boundaries).\\n - **A method's kind is declared.** A method with no receiver is written\\n   `static func` and called on its type, `Point.origin()`. Leaving the keyword\\n   off is a compile error whose fixit names both readings, because a forgotten\\ndiff --git a/designs/todo.md b/designs/todo.md\\nindex 8d985c0a..a35638b0 100644\\n--- a/designs/todo.md\\n+++ b/designs/todo.md\\n@@ -43,6 +43,9 @@ is scheduled and in what order is the whole of what they say.\\n   - **U1 LANDED Sep 20 (SL-318.p4, merge e6d6ffd9).** `sawc/coro_ledger.py::FrameLedger` is the ONE authority for every frame decision — key, kind, cause set, buildable-with-reason, home module, splice origin — built by `_build_frame_ledger` (a worklist to stabilization, extracted out of `transform_program`) and FROZEN before any body is lowered; a write after the freeze and a MISS at a consumer both raise `LedgerMiss`, which `sawc.py` reports as an internal compiler error naming the key. Nineteen consumers routed (the census table is the ledger's docstring); `self._suspends` and the two `typechecker._*_suspending_methods_set` censuses are GONE, and `frame_keys.callee_frame_key` + `coro_ledger.method_frame_key` survive as the ledger's private composers — the transform no longer imports either. `--emit-frame-ledger` dumps both tables deterministically, and the `corodiscovery` battery lane (`tools/test_coro_discovery.py`, 9 checks — seven parse the module, two RUN the compiler) fails on any raw discovery read outside the builder. BEHAVIOUR-PRESERVING, measured: the dump is byte-identical across 2597 programs (examples + blade + libs + selfhost + devtools + the read-only sawtracker sources) before and after, and the design-220 optimized-IR artifacts match the pinned base (8d752a78) on all 1579 rows. CODEX r1 (SL-318.p4) requested changes on two gaps, both fixed in the branch's fifth commit: [P1] the miss invariant was not on the consumer path — the decision tables were filled only under `--emit-frame-ledger` and `free_call_frame` still turned a table miss into `None`, so a gap and a decided negative were one fact; the decision is THREE-VALUED now (`framed` / `no-frame-owed` / `declined(reason)`), recorded on every compile, and `free_call_frame` + `method_target`'s EMBED answer raise `LedgerMiss` naming the key, with the per-read absence-or-miss verdict written into every docstring and a lane check that DROPS one decision and requires the named-key ICE; [P2] driven METHOD ROOTS were missing from the SITE census (their FRAME row appeared beside `# sites: 0`), so discovery now resolves each root's declaration after the walk into a table of its own and the census walks it — nine corpus files gained `context=driven root` rows, six of them from zero, and the eleven bogus \\\"root whose AST no method table holds\\\" declines became real framed rows. U2's worklist, read off the dumps: 67 declined FRAME rows (49 generic METHOD templates, 15 generic FREE templates — both normal — and THREE the census found the walk never reached) and FOUR declined SITE rows, all in `examples/vector_closure_suspending_transform.saw` and all `context=closure body`: the effect graph gives `traverse` NO cause at all (its suspending calls sit inside `Vector.map`/`each`/`fold` closure literals, which raise the conservative cause on the METHOD and no direct edge on the caller), so the walk never reaches `slow_double`/`slow_show`/`slow_add`, no frame is built, and their `yield_now()` runs outside one — a PASSING corpus test whose own comment names the limitation. SL-316's shape, measured rather than inferred. NOT flipped, per the unit's contract: SL-323/K115's `existential_dispatch` membership, SL-324's `might`-where-a-frame-is-meant read, SL-287/SL-316's declines\\n   - **U4 LANDED Sep 19 (SL-318.p3, merge 668fb0c7).** The answer is a CAUSE SET per node, not the three values the brief drafted (lead amendment, Sep 18: a three-valued answer would have forced each consumer to re-derive its own subset privately, which is the secondary analysis the unit removes). ONE set-union pass over the effect graph (`effects.classify_suspensions`) computes which causes reach each node — `cooperative`, `blocking`, `test_suspend`, `closure_call`, plus `existential_dispatch`, a FIFTH cause the sweep found nobody had classified — and the four walkers it replaces (`suspends`, design 206's `really_suspending`, design 242's `suspends_ignoring_blocking`, SL-306's `suspends_ignoring_closure_calls`) are DELETED, each becoming one of five named derivations whose docstring lists its consumers: `might_suspend`, `wraps_main`, `refused_in_thread_body`, `frame_boundary`, `closure_only`. The std seed carries the SET — and, since the lead's review, a SOURCE PER CAUSE beside it — which fixed a cell nobody had measured (a `Thread.spawn` body was permitted to call a std method that parks cooperatively, because the seeded leaf answered off one representative label; the refusal then had nothing left to name and printed a `<suspension source>` placeholder). Conformance rows K111-K115 are the cause × context matrix. Findings recorded, not absorbed: SL-323, SL-324, SL-325, SL-326, SL-327\\n \\n+- SL-347 — `;` is the one-line statement separator; bare juxtaposition is refused; a brace body is one line or fully broken (brief in the tracker issue; RULED Sep 22 by the user). Lands as TWO patches (lead rescope, Sep 22): p1 is the statement-end chokepoint + R1/R2 + the pins + docs, and may land ahead of SL-318.p7; p2 is the corpus sweep (U2) plus R3's juxtaposition refusal (U1b), on a branch from the post-U3 main. Sequenced BEFORE SL-335 and the design-275 U3 follow-ups, which would otherwise carry the sweep's conflicts\\n+  - **p2 BUILT Sep 22 (branch `sl347`, awaiting the lead's validation + patch).** Three commits in order. (1) THE LOOKAHEAD RESTORE, which had to come first: `parse_if_expression` (both arms), the inline `try … catch` and the `module name { … }` head each `skip_newlines()` to peek for a keyword and did NOT rewind on the miss, so a plain multi-line `if` left its own terminator consumed and the next statement reached the chokepoint looking as if it shared the `if`'s line — 6726 apparent juxtaposition sites over the corpus against 246 real ones. Each saves and restores the position, the shape `while let`'s `else` refusal and a trait method's default body already used; behaviour-neutral (the enclosing loop skips the newline anyway), measured by the dry run dropping to exactly the 246 and the suite unchanged. (2) THE SWEEP, with the COMPILER as the oracle — the chokepoint monkeypatched to RECORD rather than refuse, run to a fixpoint: 244 `.saw` sites in 94 files (examples 176, conformance 52, errors 11, sawc/std 5, prototypes 2; ZERO in blade/, libs/, devtools/, tests/, selfhost/, sawc/rt/, builtin.saw), 2 more inside a string INTERPOLATION swept by hand (a sub-parser numbers its own tokens from 1:1, so the recorder cannot place them), 27 in documentation fences (spec 13, skill 14, README 0 — each column rebased by its fence's indent and checked against the token it must precede), and 5 in skill fragments neither docverify shape can parse, found by a residue scan. R5's half-broken layouts reformatted in the same commit and listed (3 in examples/, 21 minivm fixtures); the 4 hits left are `import m.{...}` symbol lists, which design 147 makes a delimited list. NO EXPECT line changed. (3) R3: `expect_statement_end` refuses anything but NEWLINE / `;` / `}` / EOF, anchored at the SECOND statement's first token and naming BOTH statements' heads; module scope gets the declarations message; a `match` arm's BARE body is untouched (`parse_match` owns its `,`). Position matrix over 13 rows in four `examples/errors/` pins (the parse loop batches ten errors, so the matrix is split rather than truncated), one row per block shape plus the closure-body and module-scope entries. Obligation 2, each with evidence: `tools/sawfuzz_known.txt` holds ONE entry (DF-270e, a `UIntN` ICE) and nothing rests on juxtaposition; the only place sawc synthesizes Saw SOURCE TEXT is `_parse_expression_from_string` (interpolation), re-parsing text the author wrote — the coroutine transform, the `#lend_var` fold, the std seed and `--emit-docs` are all AST or JSON; 5 of the 7 `devtools/dogfood` programs fail to compile and fail IDENTICALLY on main (design 234 allocator-tier drift, no parse error among them)\\n+\\n - SL-333 — `borrows` signatures name the lent reference and its mode (brief in the tracker issue; RULED Sep 20 by the user, r2 folds codex's review). QUEUED after design 275 U2 and BEFORE U3, which writes the borrowing-struct spec text against this spelling. **LANDED Sep 21 (SL-333.p1 r5, merge 47373c08; also closed SL-334).** `borrows -> &T` / `-> &var T` (and the `?` twins): the DECLARATION states the maximum window mode, `&var self` is required to lend `&var` (R2), the use site narrows within it (R3), and a read-only lend exists at last — which is what `libs/toml`'s document views and `Map`'s key probe wanted and design 141 could not express. R4 separates the LENT VALUE's mode from the RECEIVER's required access: an accessor is SHARED-ELIGIBLE iff its shared specialization passes the ordinary receiver-permission rules, recorded by `_self_borrow_is_exclusive`'s own five entry points (which now RECORD as well as refuse) onto a declared annotation field and read at every call site by the one window funnel — so `let v` plus `v[0]` still compiles under R2, while an accessor that mutates `self` outside the place it lends is refused on a `let` root by name and two of its read-only windows on one `var` root conflict. R5 states the reference-in-a-return-position rule once, in the parser's return-clause funnel, leaving U3 only its field clause. R6 deletes the `&var self` fence on `#lend_var` and re-keys the two-specialization split from the receiver spelling onto the DECLARED MODE, with eligibility computed on the shared copy AFTER the fold — so `Data.[]`'s copy-on-write gate costs an ordinary read nothing. R7 gives a FORWARDED lend the enclosing specialization's mode, spending design 179's one stated v1 limit (a nested CoW read no longer copies) with two refusals falling out of the same fact. `place_uses._window_call` now names its seven author-visible entries and reads both declaration facts at every one; `--emit-docs` carries the mode and the eligibility. Corpus: 105 declarations (sawc/std 12, libs 3, examples 90), blade and libs/*/tests unchanged. Conformance rows K123-K130. TWO OUTCOMES CHANGED, both by a rule: `lend_var_coro_and_forwarding.saw` (R7's gate counts) and `errors/lend_var_ungated_receiver_mutation.saw` (R4 moved the refusal from the declaration to the use site); three pins were re-keyed or repurposed (`errors/lend_var_in_var_self_accessor.saw` -> `lend_var_in_shared_lend_accessor.saw`, conformance R33, `errors/ref_return_method.saw`'s fixit text).\\n \\n ## [BACKLOG] — filed, not scheduled\\ndiff --git a/examples/arc_with_unique.saw b/examples/arc_with_unique.saw\\nindex 1a716c9b..39f45bfb 100644\\n--- a/examples/arc_with_unique.saw\\n+++ b/examples/arc_with_unique.saw\\n@@ -36,7 +36,7 @@ func main() {\\n     var solo = try! Arc<Counter>(value: Counter(n: 0))\\n \\n     // Unique: the body runs and its own result comes back.\\n-    print(\\\"unique -> {solo.with_unique({ c in c.bump()  c.n }) ?? -1}\\\")\\n+    print(\\\"unique -> {solo.with_unique({ c in c.bump(); c.n }) ?? -1}\\\")\\n \\n     // R = Void instantiates too — a critical section that computes nothing.\\n     if let _ = solo.with_unique({ c in c.bump() }) {\\n@@ -45,7 +45,7 @@ func main() {\\n \\n     // Shared: None, and the body does NOT run (n stays 2 below).\\n     let theirs = solo.copy()\\n-    if let v = solo.with_unique({ c in c.bump()  c.n }) {\\n+    if let v = solo.with_unique({ c in c.bump(); c.n }) {\\n         print(\\\"BAD: a shared payload was written, n = {v}\\\")\\n     } else {\\n         print(\\\"shared -> None (body did not run)\\\")\\ndiff --git a/examples/borrowing_rules_resolve_aliases.saw b/examples/borrowing_rules_resolve_aliases.saw\\nindex a790a758..e41db558 100644\\n--- a/examples/borrowing_rules_resolve_aliases.saw\\n+++ b/examples/borrowing_rules_resolve_aliases.saw\\n@@ -93,7 +93,7 @@ extension Pt {\\n extension Pt: Iterator {\\n     type Item = Int\\n     func next(&var self) unsafe -> Int? {\\n-        if self.done { None } else { self.done = true  self.poke()  self.source.n + self.peek() }\\n+        if self.done { None } else { self.done = true; self.poke(); self.source.n + self.peek() }\\n     }\\n }\\n \\n@@ -112,7 +112,7 @@ func alias_field(o: &Owner) {\\n }\\n \\n func alias_field_driven(o: &Owner) {\\n-    for n in o.jter() { yield_now()  print(\\\"jt driven {n}\\\") }\\n+    for n in o.jter() { yield_now(); print(\\\"jt driven {n}\\\") }\\n }\\n \\n func pointer_write() unsafe {\\ndiff --git a/examples/capture_write_allowed_forms.saw b/examples/capture_write_allowed_forms.saw\\nindex ca903cf3..f7d54718 100644\\n--- a/examples/capture_write_allowed_forms.saw\\n+++ b/examples/capture_write_allowed_forms.saw\\n@@ -13,7 +13,7 @@\\n \\n import std.mutex.*\\n \\n-func each3(body: (Int) -> Bool) { body(1) body(2) body(3) }\\n+func each3(body: (Int) -> Bool) { body(1); body(2); body(3) }\\n \\n func main() {\\n     // Borrow capture: legal in a closure passed directly to a non-escaping\\ndiff --git a/examples/closure_captures_reference_param.saw b/examples/closure_captures_reference_param.saw\\nindex c3175625..e87830fb 100644\\n--- a/examples/closure_captures_reference_param.saw\\n+++ b/examples/closure_captures_reference_param.saw\\n@@ -25,7 +25,7 @@ func read_through(r: &Thing) -> Int {\\n \\n // A mutable borrow: the closure writes through it, into the CALLER's storage.\\n func write_through(m: &var Thing) -> Int {\\n-    once({ m.x = m.x + 36  m.x })\\n+    once({ m.x = m.x + 36; m.x })\\n }\\n \\n func main() {\\ndiff --git a/examples/closure_captures_self.saw b/examples/closure_captures_self.saw\\nindex 34659bf9..f92a6aa3 100644\\n--- a/examples/closure_captures_self.saw\\n+++ b/examples/closure_captures_self.saw\\n@@ -37,7 +37,7 @@ extension Counter {\\n     func viaMethod(&self) -> Int { self.lend_n({ v in self.n + v }) }\\n \\n     // A `&var self` receiver is the caller's storage: the write lands there.\\n-    func bump(&var self) -> Int { run_int({ self.n = self.n + 10  self.n }) }\\n+    func bump(&var self) -> Int { run_int({ self.n = self.n + 10; self.n }) }\\n }\\n \\n enum Color { case Red, case Blue }\\ndiff --git a/examples/closure_nested_ref_param_capture.saw b/examples/closure_nested_ref_param_capture.saw\\nindex 5c2a8a09..d58c3874 100644\\n--- a/examples/closure_nested_ref_param_capture.saw\\n+++ b/examples/closure_nested_ref_param_capture.saw\\n@@ -51,12 +51,12 @@ func peek(x: &Int) -> Int { x }\\n func main() {\\n     // The control: the borrow capture written out. This one is correct today.\\n     print(\\\"explicit borrow capture: root {}\\\",\\n-          outer({ &var c in inner({ [&var c] in c = c + 41  c }) }))\\n+          outer({ &var c in inner({ [&var c] in c = c + 41; c }) }))\\n \\n     // The ARGUMENT spelling of the same write — no assignment, so design 132's\\n     // capture-write rule never asks, and the write is lost.\\n     print(\\\"argument face: root {}\\\",\\n-          outer({ &var c in inner({ bump(&var c)  c }) }))\\n+          outer({ &var c in inner({ bump(&var c); c }) }))\\n \\n     // A shared reference parameter reads through the copy, which is right here\\n     // only because nothing wrote to it.\\ndiff --git a/examples/closure_return_is_local.saw b/examples/closure_return_is_local.saw\\nindex 11a63808..302c83bc 100644\\n--- a/examples/closure_return_is_local.saw\\n+++ b/examples/closure_return_is_local.saw\\n@@ -33,7 +33,7 @@ func might_fail(n: Int) -> Result<Int, E> {\\n }\\n \\n func call_sync(body: (Int) sync -> Int) -> Int { body(5) }\\n-func each_sync(body: (Int) sync -> Void) { body(1)  body(-2)  body(3) }\\n+func each_sync(body: (Int) sync -> Void) { body(1); body(-2); body(3) }\\n func call_susp(body: (Int) -> Int) -> Int { body(5) }\\n func call_res(body: (Int) sync -> Result<Int, E>) -> Result<Int, E> { body(5) }\\n \\ndiff --git a/examples/closure_shorthand_parameter_inside_a_try.saw b/examples/closure_shorthand_parameter_inside_a_try.saw\\nindex a2e70b23..aeb2539a 100644\\n--- a/examples/closure_shorthand_parameter_inside_a_try.saw\\n+++ b/examples/closure_shorthand_parameter_inside_a_try.saw\\n@@ -35,5 +35,5 @@ func main() {\\n     // The subject: `$0` inside a `try!` operand.\\n     run({ print(\\\"forced {try! fallible($0)}\\\") })\\n     // And as the whole body.\\n-    run({ let v = try! fallible($0)  print(\\\"bound {v}\\\") })\\n+    run({ let v = try! fallible($0); print(\\\"bound {v}\\\") })\\n }\\ndiff --git a/examples/conformance/K135_window_conflicts_that_compose.saw b/examples/conformance/K135_window_conflicts_that_compose.saw\\nindex ee99b650..e8208444 100644\\n--- a/examples/conformance/K135_window_conflicts_that_compose.saw\\n+++ b/examples/conformance/K135_window_conflicts_that_compose.saw\\n@@ -96,7 +96,7 @@ func nested_over_one_root(v: &Vector<Int>) -> Int {\\n }\\n \\n func observe(v: &Vector<Int>) -> Int { v.len() }\\n-func bump(n: &var Int) -> Int { n = n + 1  n }\\n+func bump(n: &var Int) -> Int { n = n + 1; n }\\n \\n func shared_extent_composes(v: Vector<Int>) -> Int {\\n     var owned = move v\\n@@ -207,7 +207,7 @@ extension Holder {\\n extension Wrapper<T: Copy> {\\n     func walk(&self) -> Int {\\n         var n = 0\\n-        for _x in self.held.iter() { yield_now()  n = n + 1 }\\n+        for _x in self.held.iter() { yield_now(); n = n + 1 }\\n         n\\n     }\\n }\\ndiff --git a/examples/conformance/K136_window_release_routes.saw b/examples/conformance/K136_window_release_routes.saw\\nindex b1fd9866..b2d4898a 100644\\n--- a/examples/conformance/K136_window_release_routes.saw\\n+++ b/examples/conformance/K136_window_release_routes.saw\\n@@ -78,14 +78,14 @@ func exhaustion_driven(v: &var Vector<Int>) -> Int {\\n \\n func break_plain_sync(v: &var Vector<Int>) -> Int {\\n     var total = 0\\n-    for x in v.iter() { total = total + x  break }\\n+    for x in v.iter() { total = total + x; break }\\n     let _ = v.push(9)\\n     total\\n }\\n \\n func break_plain_driven(v: &var Vector<Int>) -> Int {\\n     var total = 0\\n-    for x in v.iter() { total = total + slow(x)  break }\\n+    for x in v.iter() { total = total + slow(x); break }\\n     let _ = v.push(9)\\n     total\\n }\\ndiff --git a/examples/conformance/K137_window_destruction_order.saw b/examples/conformance/K137_window_destruction_order.saw\\nindex 5de3ad17..1644898a 100644\\n--- a/examples/conformance/K137_window_destruction_order.saw\\n+++ b/examples/conformance/K137_window_destruction_order.saw\\n@@ -150,7 +150,7 @@ extension Counter: NoCopy {\\n extension Counter: Iterator {\\n     type Item = Int\\n     func next(&var self) -> Int? {\\n-        if self.index < 3 { self.index = self.index + 1  self.index } else { None }\\n+        if self.index < 3 { self.index = self.index + 1; self.index } else { None }\\n     }\\n }\\n \\n@@ -163,28 +163,28 @@ func walk_sync(b: &Bag) -> Int {\\n \\n func walk_driven(b: &Bag) -> Int {\\n     var total = 0\\n-    for x in b.iter() { yield_now()  total = total + x }\\n+    for x in b.iter() { yield_now(); total = total + x }\\n     print(\\\"after driven loop\\\")\\n     total\\n }\\n \\n func walk_break(b: &Bag) -> Int {\\n     var total = 0\\n-    for x in b.iter() { total = total + x  break }\\n+    for x in b.iter() { total = total + x; break }\\n     print(\\\"after break loop\\\")\\n     total\\n }\\n \\n func walk_driven_break(b: &Bag) -> Int {\\n     var total = 0\\n-    for x in b.iter() { yield_now()  total = total + x  if x == 2 { break } }\\n+    for x in b.iter() { yield_now(); total = total + x; if x == 2 { break } }\\n     print(\\\"after driven break\\\")\\n     total\\n }\\n \\n func walk_driven_return(b: &Bag) -> Int {\\n     var total = 0\\n-    for x in b.iter() { yield_now()  total = total + x  if x == 2 { return total } }\\n+    for x in b.iter() { yield_now(); total = total + x; if x == 2 { return total } }\\n     print(\\\"unreached\\\")\\n     total\\n }\\n@@ -194,7 +194,7 @@ func walk_driven_continue_outer(b: &Bag) -> Int {\\n     var round = 0\\n     while round < 2 {\\n         round = round + 1\\n-        for x in b.iter() { yield_now()  total = total + x  if x == 2 { continue } }\\n+        for x in b.iter() { yield_now(); total = total + x; if x == 2 { continue } }\\n         print(\\\"after inner {round}\\\")\\n     }\\n     print(\\\"after outer\\\")\\n@@ -203,21 +203,21 @@ func walk_driven_continue_outer(b: &Bag) -> Int {\\n \\n func walk_driven_try(b: &Bag) -> Result<Int, Bad> {\\n     var total = 0\\n-    for x in b.iter() { let k = try fails(x)  total = total + k }\\n+    for x in b.iter() { let k = try fails(x); total = total + k }\\n     print(\\\"unreached\\\")\\n     total\\n }\\n \\n func walk_owned_driven() -> Int {\\n     var total = 0\\n-    for n in Counter(index: 0) { yield_now()  total = total + n  if n == 1 { break } }\\n+    for n in Counter(index: 0) { yield_now(); total = total + n; if n == 1 { break } }\\n     print(\\\"after owned loop\\\")\\n     total + 6\\n }\\n \\n func walk_owned_driven_break() -> Int {\\n     var total = 0\\n-    for n in Counter(index: 0) { yield_now()  total = total + n  if n == 2 { break } }\\n+    for n in Counter(index: 0) { yield_now(); total = total + n; if n == 2 { break } }\\n     print(\\\"after owned break\\\")\\n     total\\n }\\ndiff --git a/examples/conformance/K138_window_acquire_sequence.saw b/examples/conformance/K138_window_acquire_sequence.saw\\nindex 8af0a31b..401bc6c7 100644\\n--- a/examples/conformance/K138_window_acquire_sequence.saw\\n+++ b/examples/conformance/K138_window_acquire_sequence.saw\\n@@ -108,7 +108,7 @@ struct Holder {\\n extension Holder: NoCopy {}\\n \\n extension Holder {\\n-    func chosen(&self) -> Int { print(\\\"a\\\")  0 }\\n+    func chosen(&self) -> Int { print(\\\"a\\\"); 0 }\\n }\\n \\n func order_sync(h: &Holder) -> Int {\\ndiff --git a/examples/conformance/K147_inline_catch_splits.saw b/examples/conformance/K147_inline_catch_splits.saw\\nindex 9fa23ef0..3bbf576c 100644\\n--- a/examples/conformance/K147_inline_catch_splits.saw\\n+++ b/examples/conformance/K147_inline_catch_splits.saw\\n@@ -120,7 +120,7 @@ func variants_sync(x: Int) -> Int {\\n func variants_driven(x: Int) -> Int {\\n     let a = try! fallible(x)\\n     let b = try? fallible(x)\\n-    let c = try fallible(0 - x) catch { respond()  8 }\\n+    let c = try fallible(0 - x) catch { respond(); 8 }\\n     a + (b ?? 0) + c\\n }\\n \\n@@ -130,7 +130,7 @@ func variants_driven(x: Int) -> Int {\\n // own (the pair is refused at the `try`), so the row is that the two coexist.\\n func routed(x: Int) -> Result<Int, Wrapped> {\\n     let a = try(as Wrapped.Inner) fallible(x)\\n-    let b = try fallible(0 - x) catch { respond()  5 }\\n+    let b = try fallible(0 - x) catch { respond(); 5 }\\n     a + b\\n }\\n \\ndiff --git a/examples/conformance/K70_driven_scope_end_release_matrix.saw b/examples/conformance/K70_driven_scope_end_release_matrix.saw\\nindex 32e05668..2d417d6c 100644\\n--- a/examples/conformance/K70_driven_scope_end_release_matrix.saw\\n+++ b/examples/conformance/K70_driven_scope_end_release_matrix.saw\\n@@ -224,7 +224,7 @@ func sc7_sync() {\\n \\n func sc7_driven() {\\n     match Held.Has(r: Res(name: \\\"m1\\\")) {\\n-        case Has(r) -> { yield_now()  print(\\\"arm {r.name}\\\") },\\n+        case Has(r) -> { yield_now(); print(\\\"arm {r.name}\\\") },\\n         case Empty -> { print(\\\"arm empty\\\") }\\n     }\\n     print(\\\"after match\\\")\\ndiff --git a/examples/conformance/K88_background_spawn_takes_no_borrows.saw b/examples/conformance/K88_background_spawn_takes_no_borrows.saw\\nindex 9ba764d4..0d10b6f1 100644\\n--- a/examples/conformance/K88_background_spawn_takes_no_borrows.saw\\n+++ b/examples/conformance/K88_background_spawn_takes_no_borrows.saw\\n@@ -35,6 +35,6 @@ func main() {\\n     filler.join()\\n \\n     var total = 0\\n-    let adder = Task.spawn(run({ [&var total] in total = total + 1  total }))\\n+    let adder = Task.spawn(run({ [&var total] in total = total + 1; total }))\\n     adder.join()\\n }\\ndiff --git a/examples/conformance/M21_write_field_of_capture.saw b/examples/conformance/M21_write_field_of_capture.saw\\nindex ba1b44e7..96e6918d 100644\\n--- a/examples/conformance/M21_write_field_of_capture.saw\\n+++ b/examples/conformance/M21_write_field_of_capture.saw\\n@@ -9,6 +9,6 @@\\n struct S { n: Int }\\n func main() {\\n     var s = S(n: 0)\\n-    let f = { s.n = 1  s.n }\\n+    let f = { s.n = 1; s.n }\\n     print(f())\\n }\\ndiff --git a/examples/conformance/M23_write_through_shared_borrow.saw b/examples/conformance/M23_write_through_shared_borrow.saw\\nindex d4f75afb..90860019 100644\\n--- a/examples/conformance/M23_write_through_shared_borrow.saw\\n+++ b/examples/conformance/M23_write_through_shared_borrow.saw\\n@@ -7,6 +7,6 @@\\n \\n func main() {\\n     var v: Vector<Int> = [1, 2, 3]\\n-    let r = v.with_ref(0, { e in e = 9  e })\\n+    let r = v.with_ref(0, { e in e = 9; e })\\n     print(r)\\n }\\ndiff --git a/examples/conformance/M36_shared_self_capture_refuses_write.saw b/examples/conformance/M36_shared_self_capture_refuses_write.saw\\nindex f6cb38e1..9a8356a6 100644\\n--- a/examples/conformance/M36_shared_self_capture_refuses_write.saw\\n+++ b/examples/conformance/M36_shared_self_capture_refuses_write.saw\\n@@ -18,7 +18,7 @@ func run_int(body: () sync -> Int) -> Int { body() }\\n struct Counter { n: Int }\\n \\n extension Counter {\\n-    func narrow(&var self) -> Int { run_int({ [&self] in self.n = 9  self.n }) }\\n+    func narrow(&var self) -> Int { run_int({ [&self] in self.n = 9; self.n }) }\\n }\\n \\n func main() {\\ndiff --git a/examples/conformance/R39_explicit_self_borrow_capture.saw b/examples/conformance/R39_explicit_self_borrow_capture.saw\\nindex 94f80530..8654438a 100644\\n--- a/examples/conformance/R39_explicit_self_borrow_capture.saw\\n+++ b/examples/conformance/R39_explicit_self_borrow_capture.saw\\n@@ -36,7 +36,7 @@ extension Counter {\\n     func syncRead(&self) -> Int { run_int({ [&self] in self.n + 1 }) }\\n \\n     func syncWrite(&var self) -> Int {\\n-        run_int({ [&var self] in self.n = self.n + 10  self.n })\\n+        run_int({ [&var self] in self.n = self.n + 10; self.n })\\n     }\\n \\n     func slowRead(&self) -> Int {\\n@@ -48,7 +48,7 @@ extension Counter {\\n \\n     func slowWrite(&var self) -> Int {\\n         task.yield_now()\\n-        run_int({ [&var self] in self.n += 10  self.n })\\n+        run_int({ [&var self] in self.n += 10; self.n })\\n     }\\n }\\n \\ndiff --git a/examples/conformance/V105_copy_capture_releases_its_duplicate.saw b/examples/conformance/V105_copy_capture_releases_its_duplicate.saw\\nindex 2f0b3ae5..c52f3322 100644\\n--- a/examples/conformance/V105_copy_capture_releases_its_duplicate.saw\\n+++ b/examples/conformance/V105_copy_capture_releases_its_duplicate.saw\\n@@ -59,7 +59,7 @@\\n \\n struct Dup { n: Int }\\n extension Dup: ExplicitCopy {\\n-    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1)  Dup(n: self.n + 1) }\\n+    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1); Dup(n: self.n + 1) }\\n     func deinit(&var self) { print(\\\"drop {}\\\", self.n) }\\n }\\n \\ndiff --git a/examples/conformance/V106_driven_copy_capture_duplicates_once.saw b/examples/conformance/V106_driven_copy_capture_duplicates_once.saw\\nindex ed2b1112..a9aef7d1 100644\\n--- a/examples/conformance/V106_driven_copy_capture_duplicates_once.saw\\n+++ b/examples/conformance/V106_driven_copy_capture_duplicates_once.saw\\n@@ -64,7 +64,7 @@\\n \\n struct Dup { n: Int }\\n extension Dup: ExplicitCopy {\\n-    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1)  Dup(n: self.n + 1) }\\n+    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1); Dup(n: self.n + 1) }\\n     func deinit(&var self) { print(\\\"drop {}\\\", self.n) }\\n }\\n \\n@@ -73,16 +73,16 @@ extension Dup: ExplicitCopy {\\n // names (design 219), so one line is one duplication.\\n struct Counted { n: Int }\\n extension Counted: Copy {\\n-    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n)  Counted(n: self.n) }\\n+    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n); Counted(n: self.n) }\\n }\\n \\n func run_int(body: () sync -> Int) -> Int { body() }\\n \\n func sync_cap(d: Dup) -> Int   { run_int({ [copy d] in d.n }) }\\n-func driven_cap(d: Dup) -> Int { __saw_suspend()  run_int({ [copy d] in d.n }) }\\n+func driven_cap(d: Dup) -> Int { __saw_suspend(); run_int({ [copy d] in d.n }) }\\n \\n func sync_quiet(c: Counted) -> Int   { run_int({ [copy c] in c.n }) }\\n-func driven_quiet(c: Counted) -> Int { __saw_suspend()  run_int({ [copy c] in c.n }) }\\n+func driven_quiet(c: Counted) -> Int { __saw_suspend(); run_int({ [copy c] in c.n }) }\\n \\n func main() {\\n     print(\\\"-- sync\\\")\\ndiff --git a/examples/conformance/V107_capture_survives_repeated_invocation.saw b/examples/conformance/V107_capture_survives_repeated_invocation.saw\\nindex 4f96784d..8d81b720 100644\\n--- a/examples/conformance/V107_capture_survives_repeated_invocation.saw\\n+++ b/examples/conformance/V107_capture_survives_repeated_invocation.saw\\n@@ -68,7 +68,7 @@\\n \\n struct Dup { n: Int }\\n extension Dup: ExplicitCopy {\\n-    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1)  Dup(n: self.n + 1) }\\n+    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1); Dup(n: self.n + 1) }\\n     func deinit(&var self) { print(\\\"drop {}\\\", self.n) }\\n }\\n \\n@@ -84,7 +84,7 @@ func run_twice(body: () sync -> Int) -> Int { body() + body() }\\n // A/B — the row the regression was in: a NON-escaping env, where a `move`\\n // re-typing would select the deferred one-shot.\\n func a_sync(d: Dup) -> Int   { run_twice({ [copy d] in d.n }) }\\n-func b_driven(d: Dup) -> Int { __saw_suspend()  run_twice({ [copy d] in d.n }) }\\n+func b_driven(d: Dup) -> Int { __saw_suspend(); run_twice({ [copy d] in d.n }) }\\n \\n // C/D — the ESCAPING twin. Here the heap env must OWN the duplicate, since the\\n // materialized local dies with the frame; it must still release it once at\\n@@ -102,8 +102,8 @@ func d_driven(d: Dup) -> Int {\\n // E — the neighbouring modes that are also supposed to be reusable, driven,\\n // two calls each. Neither is touched by SL-267's fix; they are here so the\\n // invocation dimension is covered across the mode axis rather than at one cell.\\n-func e_plain(t: Tick) -> Int  { __saw_suspend()  run_twice({ t.n }) }\\n-func e_borrow(t: Tick) -> Int { __saw_suspend()  run_twice({ [&t] in t.n }) }\\n+func e_plain(t: Tick) -> Int  { __saw_suspend(); run_twice({ t.n }) }\\n+func e_borrow(t: Tick) -> Int { __saw_suspend(); run_twice({ [&t] in t.n }) }\\n \\n func main() {\\n     print(\\\"-- A non-escaping [copy], sync, 2 calls\\\")\\ndiff --git a/examples/conformance/V108_each_copy_capture_owns_one_duplicate.saw b/examples/conformance/V108_each_copy_capture_owns_one_duplicate.saw\\nindex 5bfb5f44..8f3af429 100644\\n--- a/examples/conformance/V108_each_copy_capture_owns_one_duplicate.saw\\n+++ b/examples/conformance/V108_each_copy_capture_owns_one_duplicate.saw\\n@@ -104,7 +104,7 @@\\n \\n struct Dup { n: Int }\\n extension Dup: ExplicitCopy {\\n-    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1)  Dup(n: self.n + 1) }\\n+    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1); Dup(n: self.n + 1) }\\n     func deinit(&var self) { print(\\\"drop {}\\\", self.n) }\\n }\\n \\ndiff --git a/examples/conformance/V116_non_escaping_closure_keeps_its_stack_env.saw b/examples/conformance/V116_non_escaping_closure_keeps_its_stack_env.saw\\nindex 4320237c..67a6e458 100644\\n--- a/examples/conformance/V116_non_escaping_closure_keeps_its_stack_env.saw\\n+++ b/examples/conformance/V116_non_escaping_closure_keeps_its_stack_env.saw\\n@@ -55,6 +55,6 @@ func main() {\\n     // acceptance is a second witness that this slot is classified non-escaping\\n     // — the borrow gate and the escape analysis read the same bit.\\n     var counter = 10\\n-    let _ = run_now({ [&var counter] in counter = counter + 1  counter })\\n+    let _ = run_now({ [&var counter] in counter = counter + 1; counter })\\n     print(\\\"borrow-capture {}\\\", counter)\\n }\\ndiff --git a/examples/conformance/V86_self_receiver_read_is_a_transfer.saw b/examples/conformance/V86_self_receiver_read_is_a_transfer.saw\\nindex b3115caf..a3f29db8 100644\\n--- a/examples/conformance/V86_self_receiver_read_is_a_transfer.saw\\n+++ b/examples/conformance/V86_self_receiver_read_is_a_transfer.saw\\n@@ -51,9 +51,9 @@ extension Res {\\n     // The ARGUMENT face: `self` handed to a by-value parameter.\\n     func hand_on(&self) -> Int { sink(self) }\\n     // The BINDING face: `self` read into a local the body then owns.\\n-    func to_local(&self) -> Int { let b = self  b.w }\\n+    func to_local(&self) -> Int { let b = self; b.w }\\n     // The AGGREGATE face: `self` stored into a struct field.\\n-    func to_field(&self) -> Int { let h = Holder(r: self)  h.r.w }\\n+    func to_field(&self) -> Int { let h = Holder(r: self); h.r.w }\\n }\\n \\n func main() {\\ndiff --git a/examples/conformance/V89_forwarding_copy_tier_duplicates_exactly_once.saw b/examples/conformance/V89_forwarding_copy_tier_duplicates_exactly_once.saw\\nindex c3708b0a..dcbc42c0 100644\\n--- a/examples/conformance/V89_forwarding_copy_tier_duplicates_exactly_once.saw\\n+++ b/examples/conformance/V89_forwarding_copy_tier_duplicates_exactly_once.saw\\n@@ -36,7 +36,7 @@\\n \\n struct Counted { n: Int }\\n extension Counted: Copy {\\n-    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n)  Counted(n: self.n) }\\n+    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n); Counted(n: self.n) }\\n }\\n \\n enum Bad { case Nope }\\n@@ -49,7 +49,7 @@ extension Counted {\\n     // S1 at the Copy tier — the receiver read, returned.\\n     func self_return(&self) -> Counted { self }\\n     // S3 at the Copy tier — the receiver read into a local the body owns.\\n-    func self_binding(&self) -> Int { let b = self  b.n }\\n+    func self_binding(&self) -> Int { let b = self; b.n }\\n }\\n \\n func made(n: Int) -> Result<Counted, Bad> { Counted(n: n) }\\ndiff --git a/examples/conformance/V91_try_catch_copies_per_path.saw b/examples/conformance/V91_try_catch_copies_per_path.saw\\nindex a1c2cd29..07a83d29 100644\\n--- a/examples/conformance/V91_try_catch_copies_per_path.saw\\n+++ b/examples/conformance/V91_try_catch_copies_per_path.saw\\n@@ -68,7 +68,7 @@ import std.task.*\\n \\n struct Counted { n: Int }\\n extension Counted: Copy {\\n-    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n)  Counted(n: self.n) }\\n+    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n); Counted(n: self.n) }\\n }\\n \\n enum Bad { case Nope }\\ndiff --git a/examples/conformance/V92_merged_results_copy_once.saw b/examples/conformance/V92_merged_results_copy_once.saw\\nindex b8e05080..ef1f7c90 100644\\n--- a/examples/conformance/V92_merged_results_copy_once.saw\\n+++ b/examples/conformance/V92_merged_results_copy_once.saw\\n@@ -54,7 +54,7 @@\\n \\n struct Counted { n: Int }\\n extension Counted: Copy {\\n-    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n)  Counted(n: self.n) }\\n+    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n); Counted(n: self.n) }\\n }\\n \\n enum Bad { case Nope }\\n@@ -86,7 +86,7 @@ func value_match(n: Int) -> Int {\\n // value.\\n func try_block_ok(n: Int) -> Int {\\n     let fallback = Counted(n: 90 + n)\\n-    let got = try { let g = try made(n)  g } catch { fallback }\\n+    let got = try { let g = try made(n); g } catch { fallback }\\n     got.n\\n }\\n \\n@@ -94,7 +94,7 @@ func try_block_ok(n: Int) -> Int {\\n // form's counterpart of V91's leaking cell.\\n func try_block_catch(n: Int) -> Int {\\n     let fallback = Counted(n: n)\\n-    let got = try { let g = try fails()  g } catch { fallback }\\n+    let got = try { let g = try fails(); g } catch { fallback }\\n     got.n\\n }\\n \\ndiff --git a/examples/conformance/V93_specialization_judges_the_instance.saw b/examples/conformance/V93_specialization_judges_the_instance.saw\\nindex aacebf87..40ee697e 100644\\n--- a/examples/conformance/V93_specialization_judges_the_instance.saw\\n+++ b/examples/conformance/V93_specialization_judges_the_instance.saw\\n@@ -33,7 +33,7 @@\\n \\n struct Counted { n: Int }\\n extension Counted: Copy {\\n-    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n)  Counted(n: self.n) }\\n+    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n); Counted(n: self.n) }\\n }\\n \\n struct Held<T> { v: T }\\ndiff --git a/examples/conformance/V94_driven_body_keeps_the_sync_decision.saw b/examples/conformance/V94_driven_body_keeps_the_sync_decision.saw\\nindex 3778d64b..e3460a0f 100644\\n--- a/examples/conformance/V94_driven_body_keeps_the_sync_decision.saw\\n+++ b/examples/conformance/V94_driven_body_keeps_the_sync_decision.saw\\n@@ -36,7 +36,7 @@\\n \\n struct Counted { n: Int }\\n extension Counted: Copy {\\n-    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n)  Counted(n: self.n) }\\n+    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n); Counted(n: self.n) }\\n }\\n \\n struct Owned { n: Int }\\ndiff --git a/examples/conformance/V96_driven_capture_duplicates_once.saw b/examples/conformance/V96_driven_capture_duplicates_once.saw\\nindex c126f2f1..ad53511a 100644\\n--- a/examples/conformance/V96_driven_capture_duplicates_once.saw\\n+++ b/examples/conformance/V96_driven_capture_duplicates_once.saw\\n@@ -38,7 +38,7 @@\\n \\n struct Counted { n: Int }\\n extension Counted: Copy {\\n-    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n)  Counted(n: self.n) }\\n+    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n); Counted(n: self.n) }\\n }\\n \\n func run_int(body: () sync -> Int) -> Int { body() }\\ndiff --git a/examples/conformance/V98_driven_generic_judges_each_instance.saw b/examples/conformance/V98_driven_generic_judges_each_instance.saw\\nindex d9010f67..12dc656b 100644\\n--- a/examples/conformance/V98_driven_generic_judges_each_instance.saw\\n+++ b/examples/conformance/V98_driven_generic_judges_each_instance.saw\\n@@ -29,7 +29,7 @@\\n \\n struct Counted { n: Int }\\n extension Counted: Copy {\\n-    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n)  Counted(n: self.n) }\\n+    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n); Counted(n: self.n) }\\n }\\n \\n struct Held<T> { v: T }\\ndiff --git a/examples/conformance/V99_every_payload_read_retains_once.saw b/examples/conformance/V99_every_payload_read_retains_once.saw\\nindex c6f583f8..20a2c565 100644\\n--- a/examples/conformance/V99_every_payload_read_retains_once.saw\\n+++ b/examples/conformance/V99_every_payload_read_retains_once.saw\\n@@ -39,7 +39,7 @@\\n \\n struct Counted { n: Int }\\n extension Counted: Copy {\\n-    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n)  Counted(n: self.n) }\\n+    func copy(&self) -> Counted { print(\\\"copy {}\\\", self.n); Counted(n: self.n) }\\n }\\n \\n // A payload-free enum is trivially copyable, which keeps `Holder` on the\\ndiff --git a/examples/coro_bind_id_for_var_derived.saw b/examples/coro_bind_id_for_var_derived.saw\\nindex 0bdd7f31..fdfd2429 100644\\n--- a/examples/coro_bind_id_for_var_derived.saw\\n+++ b/examples/coro_bind_id_for_var_derived.saw\\n@@ -16,7 +16,7 @@\\n \\n import std.task.*\\n \\n-func slow(n: Int) -> Int { yield_now()  return n }\\n+func slow(n: Int) -> Int { yield_now(); return n }\\n func fast(n: Int) sync -> Int { return n }\\n \\n func suspending() {\\ndiff --git a/examples/coro_bind_id_guardlet_nonspanning.saw b/examples/coro_bind_id_guardlet_nonspanning.saw\\nindex 1e79928f..5b6a3ec2 100644\\n--- a/examples/coro_bind_id_guardlet_nonspanning.saw\\n+++ b/examples/coro_bind_id_guardlet_nonspanning.saw\\n@@ -11,7 +11,7 @@\\n \\n import std.task.*\\n \\n-func slow(n: Int) -> Int { yield_now()  return n }\\n+func slow(n: Int) -> Int { yield_now(); return n }\\n func fast(n: Int) sync -> Int { return n }\\n func maybe(k: Int) -> Int? { return k * 10 }\\n \\ndiff --git a/examples/coro_bind_id_iflet_nonspanning.saw b/examples/coro_bind_id_iflet_nonspanning.saw\\nindex 3d2f97f4..78b08a5a 100644\\n--- a/examples/coro_bind_id_iflet_nonspanning.saw\\n+++ b/examples/coro_bind_id_iflet_nonspanning.saw\\n@@ -12,7 +12,7 @@\\n \\n import std.task.*\\n \\n-func slow(n: Int) -> Int { yield_now()  return n }\\n+func slow(n: Int) -> Int { yield_now(); return n }\\n func fast(n: Int) sync -> Int { return n }\\n func maybe(k: Int) -> Int? { return k * 10 }\\n \\ndiff --git a/examples/coro_bind_id_match_arm_later_local.saw b/examples/coro_bind_id_match_arm_later_local.saw\\nindex ba0f7150..947b39dd 100644\\n--- a/examples/coro_bind_id_match_arm_later_local.saw\\n+++ b/examples/coro_bind_id_match_arm_later_local.saw\\n@@ -21,7 +21,7 @@ enum Step { case Go(n: Int), case Stop }\\n \\n func pick() -> Step { return Step.Go(n: 6) }\\n \\n-func slow(n: Int) -> Int { yield_now()  return n }\\n+func slow(n: Int) -> Int { yield_now(); return n }\\n func fast(n: Int) sync -> Int { return n }\\n \\n func suspending() {\\ndiff --git a/examples/coro_bind_id_match_arms_same_name.saw b/examples/coro_bind_id_match_arms_same_name.saw\\nindex db71df18..00acce29 100644\\n--- a/examples/coro_bind_id_match_arms_same_name.saw\\n+++ b/examples/coro_bind_id_match_arms_same_name.saw\\n@@ -37,15 +37,15 @@ func pick_mixed(k: Int) -> Mixed {\\n \\n func same_type(k: Int) {\\n     match pick(k) {\\n-        case A(v) -> { yield_now()  print(\\\"A: {v}\\\") },\\n-        case B(v) -> { yield_now()  print(\\\"B: {v}\\\") }\\n+        case A(v) -> { yield_now(); print(\\\"A: {v}\\\") },\\n+        case B(v) -> { yield_now(); print(\\\"B: {v}\\\") }\\n     }\\n }\\n \\n func mixed_type(k: Int) {\\n     match pick_mixed(k) {\\n-        case A(v) -> { yield_now()  print(\\\"flag A: {v}\\\") },\\n-        case B(v) -> { yield_now()  print(\\\"flag B: {v}\\\") }\\n+        case A(v) -> { yield_now(); print(\\\"flag A: {v}\\\") },\\n+        case B(v) -> { yield_now(); print(\\\"flag B: {v}\\\") }\\n     }\\n }\\n \\ndiff --git a/examples/coro_bind_id_nested_block_shadow.saw b/examples/coro_bind_id_nested_block_shadow.saw\\nindex afe430bc..9e9001d3 100644\\n--- a/examples/coro_bind_id_nested_block_shadow.saw\\n+++ b/examples/coro_bind_id_nested_block_shadow.saw\\n@@ -13,7 +13,7 @@\\n \\n import std.task.*\\n \\n-func slow(n: Int) -> Int { yield_now()  return n }\\n+func slow(n: Int) -> Int { yield_now(); return n }\\n func fast(n: Int) sync -> Int { return n }\\n \\n func suspending() {\\ndiff --git a/examples/coro_bind_id_scopes_stress.saw b/examples/coro_bind_id_scopes_stress.saw\\nindex b99aa254..95af4f22 100644\\n--- a/examples/coro_bind_id_scopes_stress.saw\\n+++ b/examples/coro_bind_id_scopes_stress.saw\\n@@ -27,7 +27,7 @@ func pick(k: Int) -> Step {\\n     return Step.Stop\\n }\\n \\n-func slow(n: Int) -> Int { yield_now()  return n }\\n+func slow(n: Int) -> Int { yield_now(); return n }\\n func fast(n: Int) sync -> Int { return n }\\n \\n struct Boom { code: Int }\\n@@ -60,7 +60,7 @@ func catches() -> Int {\\n         print(\\\"pre: {error}\\\")\\n     }\\n     let error = 1000                  // collides with it, so this one is renamed\\n-    let got = try fails() catch { print(\\\"caught: {error}\\\")  5 }\\n+    let got = try fails() catch { print(\\\"caught: {error}\\\"); 5 }\\n     yield_now()\\n     return error + got\\n }\\ndiff --git a/examples/coro_bind_id_shadow_regressions.saw b/examples/coro_bind_id_shadow_regressions.saw\\nindex b86609fc..88f7f3c1 100644\\n--- a/examples/coro_bind_id_shadow_regressions.saw\\n+++ b/examples/coro_bind_id_shadow_regressions.saw\\n@@ -43,7 +43,7 @@ enum Step { case Go(n: Int), case Stop }\\n \\n func inner(s: Step) -> Int {\\n     match s {\\n-        case Go(k) -> { yield_now()  return k * 3 },\\n+        case Go(k) -> { yield_now(); return k * 3 },\\n         case Stop -> { return 0 }\\n     }\\n }\\ndiff --git a/examples/coro_bind_id_sibling_scopes.saw b/examples/coro_bind_id_sibling_scopes.saw\\nindex 7092fbbc..0293bd1b 100644\\n--- a/examples/coro_bind_id_sibling_scopes.saw\\n+++ b/examples/coro_bind_id_sibling_scopes.saw\\n@@ -13,7 +13,7 @@\\n \\n import std.task.*\\n \\n-func slow(n: Int) -> Int { yield_now()  return n }\\n+func slow(n: Int) -> Int { yield_now(); return n }\\n func fast(n: Int) sync -> Int { return n }\\n \\n func suspending(c: Bool) {\\ndiff --git a/examples/coro_closure_captures_self_nested.saw b/examples/coro_closure_captures_self_nested.saw\\nindex 5876b543..c4d6c5e9 100644\\n--- a/examples/coro_closure_captures_self_nested.saw\\n+++ b/examples/coro_closure_captures_self_nested.saw\\n@@ -105,7 +105,7 @@ extension Counter {\\n     // reaches the caller's storage.\\n     func bump(&var self) -> Int {\\n         yield_now()\\n-        run_int({ self.n += 10  self.n })\\n+        run_int({ self.n += 10; self.n })\\n     }\\n }\\n \\ndiff --git a/examples/coro_inline_catch_loop_exit_in_child_positions.saw b/examples/coro_inline_catch_loop_exit_in_child_positions.saw\\nindex 3122659a..f4c29c87 100644\\n--- a/examples/coro_inline_catch_loop_exit_in_child_positions.saw\\n+++ b/examples/coro_inline_catch_loop_exit_in_child_positions.saw\\n@@ -169,9 +169,9 @@ extension Bag {\\n     func add(&self, x: Int) sync -> Int { self.n + x }\\n }\\n \\n-func fail(x: Int) sync -> Result<Int, Bad> { if x == 2 { return Bad(n: x) }  x }\\n-func failb(x: Int) sync -> Result<Bool, Bad> { if x == 2 { return Bad(n: x) }  true }\\n-func failbag(x: Int) sync -> Result<Bag, Bad> { if x == 2 { return Bad(n: x) }  Bag(n: x) }\\n+func fail(x: Int) sync -> Result<Int, Bad> { if x == 2 { return Bad(n: x) }; x }\\n+func failb(x: Int) sync -> Result<Bool, Bad> { if x == 2 { return Bad(n: x) }; true }\\n+func failbag(x: Int) sync -> Result<Bag, Bad> { if x == 2 { return Bad(n: x) }; Bag(n: x) }\\n func id(n: Int) sync -> Int { n }\\n func idl(n: Int) sync -> Int { n }\\n func read(p: &Int) sync -> Int { p }\\n@@ -334,7 +334,7 @@ func direct_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = try fail(x) catch { respond()  3 }\\n+        let n = try fail(x) catch { respond(); 3 }\\n         total = total + n\\n     }\\n     total + 100\\n@@ -497,7 +497,7 @@ func call_arg_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = id(try fail(x) catch { respond()  3 })\\n+        let n = id(try fail(x) catch { respond(); 3 })\\n         total = total + n\\n     }\\n     total + 100\\n@@ -660,7 +660,7 @@ func call_arg_labeled_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = idl(n: try fail(x) catch { respond()  3 })\\n+        let n = idl(n: try fail(x) catch { respond(); 3 })\\n         total = total + n\\n     }\\n     total + 100\\n@@ -823,7 +823,7 @@ func method_arg_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = bag.add(try fail(x) catch { respond()  3 })\\n+        let n = bag.add(try fail(x) catch { respond(); 3 })\\n         total = total + n\\n     }\\n     total + 100\\n@@ -986,7 +986,7 @@ func receiver_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = (try failbag(x) catch { respond()  Bag(n: 3) }).n\\n+        let n = (try failbag(x) catch { respond(); Bag(n: 3) }).n\\n         total = total + n\\n     }\\n     total + 100\\n@@ -1149,7 +1149,7 @@ func receiver_method_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = (try failbag(x) catch { respond()  Bag(n: 3) }).add(0)\\n+        let n = (try failbag(x) catch { respond(); Bag(n: 3) }).add(0)\\n         total = total + n\\n     }\\n     total + 100\\n@@ -1312,7 +1312,7 @@ func struct_field_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = Pair(a: try fail(x) catch { respond()  3 }, b: 1).a\\n+        let n = Pair(a: try fail(x) catch { respond(); 3 }, b: 1).a\\n         total = total + n\\n     }\\n     total + 100\\n@@ -1484,7 +1484,7 @@ func map_key_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let m: Map<Int, Int> = {try fail(x) catch { respond()  3 }: x}\\n+        let m: Map<Int, Int> = {try fail(x) catch { respond(); 3 }: x}\\n         let n = m.get(x) ?? 0\\n         total = total + n\\n     }\\n@@ -1657,7 +1657,7 @@ func map_value_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let m: Map<Int, Int> = {1: try fail(x) catch { respond()  3 }}\\n+        let m: Map<Int, Int> = {1: try fail(x) catch { respond(); 3 }}\\n         let n = m.get(1) ?? 0\\n         total = total + n\\n     }\\n@@ -1830,7 +1830,7 @@ func tuple_elem_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let t = (try fail(x) catch { respond()  3 }, 1)\\n+        let t = (try fail(x) catch { respond(); 3 }, 1)\\n         let n = t.0\\n         total = total + n\\n     }\\n@@ -2003,7 +2003,7 @@ func array_elem_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let a = [try fail(x) catch { respond()  3 }, 1]\\n+        let a = [try fail(x) catch { respond(); 3 }, 1]\\n         let n = a[0]\\n         total = total + n\\n     }\\n@@ -2176,7 +2176,7 @@ func vector_elem_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let v2: Vector<Int> = [try fail(x) catch { respond()  3 }]\\n+        let v2: Vector<Int> = [try fail(x) catch { respond(); 3 }]\\n         let n = v2[0]\\n         total = total + n\\n     }\\n@@ -2340,7 +2340,7 @@ func index_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = table[try fail(x) catch { respond()  3 }]\\n+        let n = table[try fail(x) catch { respond(); 3 }]\\n         total = total + n\\n     }\\n     total + 100\\n@@ -2503,7 +2503,7 @@ func ref_place_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = read(&table[try fail(x) catch { respond()  3 }])\\n+        let n = read(&table[try fail(x) catch { respond(); 3 }])\\n         total = total + n\\n     }\\n     total + 100\\n@@ -2666,7 +2666,7 @@ func interpolation_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = \\\"{try fail(x) catch { respond()  3 }}\\\".to_int() ?? 0\\n+        let n = \\\"{try fail(x) catch { respond(); 3 }}\\\".to_int() ?? 0\\n         total = total + n\\n     }\\n     total + 100\\n@@ -2838,7 +2838,7 @@ func return_value_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        if x >= 2 { return try fail(x) catch { respond()  3 } }\\n+        if x >= 2 { return try fail(x) catch { respond(); 3 } }\\n         let n = x\\n         total = total + n\\n     }\\n@@ -3002,7 +3002,7 @@ func coalesce_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = none_int ?? try fail(x) catch { respond()  3 }\\n+        let n = none_int ?? try fail(x) catch { respond(); 3 }\\n         total = total + n\\n     }\\n     total + 100\\n@@ -3174,7 +3174,7 @@ func and_operand_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let b = flag && (try failb(x) catch { respond()  true })\\n+        let b = flag && (try failb(x) catch { respond(); true })\\n         let n = if b { x } else { 0 }\\n         total = total + n\\n     }\\n@@ -3347,7 +3347,7 @@ func or_operand_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let b = flagf || (try failb(x) catch { respond()  true })\\n+        let b = flagf || (try failb(x) catch { respond(); true })\\n         let n = if b { x } else { 0 }\\n         total = total + n\\n     }\\n@@ -3511,7 +3511,7 @@ func nested_catch_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = try fail(x) catch { try fail(x) catch { respond()  3 } }\\n+        let n = try fail(x) catch { try fail(x) catch { respond(); 3 } }\\n         total = total + n\\n     }\\n     total + 100\\n@@ -3674,7 +3674,7 @@ func value_if_let_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = if x == 2 { respond()  3 } else { x }\\n+        let n = if x == 2 { respond(); 3 } else { x }\\n         total = total + n\\n     }\\n     total + 100\\n@@ -3837,7 +3837,7 @@ func value_if_arg_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = id(if x == 2 { respond()  3 } else { x })\\n+        let n = id(if x == 2 { respond(); 3 } else { x })\\n         total = total + n\\n     }\\n     total + 100\\n@@ -4000,7 +4000,7 @@ func value_match_arg_suspend_while_driven(v: &Vector<Int>) -> Int {\\n         let x = i\\n         i = i + 1\\n         yield_now()\\n-        let n = id(match x { case 2 -> { respond()  3 }, case _ -> x })\\n+        let n = id(match x { case 2 -> { respond(); 3 }, case _ -> x })\\n         total = total + n\\n     }\\n     total + 100\\ndiff --git a/examples/coro_match_arm_binding_patterns.saw b/examples/coro_match_arm_binding_patterns.saw\\nindex d45ea23b..11ae9ce8 100644\\n--- a/examples/coro_match_arm_binding_patterns.saw\\n+++ b/examples/coro_match_arm_binding_patterns.saw\\n@@ -89,7 +89,7 @@ func guard_spanning_driven() -> Int {\\n     while i < 10 {\\n         match next(i) {\\n             case Value(g) if g == 2 -> { return total },\\n-            case Value(n) -> { slow()  total += n },\\n+            case Value(n) -> { slow(); total += n },\\n             case Done -> { return total }\\n         }\\n         i += 1\\n@@ -118,7 +118,7 @@ func guard_shared_driven() -> Int {\\n     while i < 10 {\\n         match next(i) {\\n             case Value(n) if n == 2 -> { return total },\\n-            case Value(n) -> { slow()  total += n },\\n+            case Value(n) -> { slow(); total += n },\\n             case Done -> { return total }\\n         }\\n         i += 1\\ndiff --git a/examples/coro_optbind_split_in_try.saw b/examples/coro_optbind_split_in_try.saw\\nindex 3c6f4cc4..dd9f4937 100644\\n--- a/examples/coro_optbind_split_in_try.saw\\n+++ b/examples/coro_optbind_split_in_try.saw\\n@@ -30,7 +30,7 @@ func maybe(n: Int) sync -> Int? {\\n     return None\\n }\\n \\n-func slow(n: Int) -> Int { yield_now()  return n }\\n+func slow(n: Int) -> Int { yield_now(); return n }\\n \\n func iflet_in_try(seed: Int) -> Int {\\n     var out = 0\\ndiff --git a/examples/coro_ref_arg_beside_suspending_arg.saw b/examples/coro_ref_arg_beside_suspending_arg.saw\\nindex 7767d9d0..5c65be21 100644\\n--- a/examples/coro_ref_arg_beside_suspending_arg.saw\\n+++ b/examples/coro_ref_arg_beside_suspending_arg.saw\\n@@ -59,7 +59,7 @@ func noisy() -> Int {\\n \\n func set_one(d: &var Doc, k: Int) { d.n = d.n + k }\\n func read_one(d: &Doc, k: Int) -> Int { d.n + k }\\n-func set_two(a: &var Doc, b: &var Doc, k: Int) { a.n = a.n + k  b.n = b.n + k }\\n+func set_two(a: &var Doc, b: &var Doc, k: Int) { a.n = a.n + k; b.n = b.n + k }\\n func set_after(k: Int, d: &var Doc) { d.n = d.n + k }\\n func set_mixed(a: &var Doc, k: Int, m: Int) { a.n = a.n + k + m }\\n func depth(n: Int) -> Int { n }\\ndiff --git a/examples/coro_same_scope_redefinition_owning_slots.saw b/examples/coro_same_scope_redefinition_owning_slots.saw\\nindex 958fd67e..06fa8f49 100644\\n--- a/examples/coro_same_scope_redefinition_owning_slots.saw\\n+++ b/examples/coro_same_scope_redefinition_owning_slots.saw\\n@@ -157,8 +157,8 @@ enum Step { case Go(n: Int), case Halt(n: Int), case Stop }\\n // list and its `pattern` are two views of THAT one binding.\\n func r7(s: Step) -> Int {\\n     match s {\\n-        case Go(k) -> { yield_now()  return k + 1 },\\n-        case Halt(k) -> { yield_now()  return k + 2 },\\n+        case Go(k) -> { yield_now(); return k + 1 },\\n+        case Halt(k) -> { yield_now(); return k + 2 },\\n         case Stop -> 0\\n     }\\n }\\ndiff --git a/examples/coro_try_block_positions.saw b/examples/coro_try_block_positions.saw\\nindex 917598bd..80f8f1ad 100644\\n--- a/examples/coro_try_block_positions.saw\\n+++ b/examples/coro_try_block_positions.saw\\n@@ -244,7 +244,7 @@ func tail_value(n: Int) -> Int {\\n \\n // 13 — value position: the block initializes a `let`\\n func let_value(n: Int) -> Int {\\n-    let r = try { let a = try compute(n > 0, n)  a + 20 } catch { -2 }\\n+    let r = try { let a = try compute(n > 0, n); a + 20 } catch { -2 }\\n     r\\n }\\n \\ndiff --git a/examples/coro_try_in_borrowed_place_index.saw b/examples/coro_try_in_borrowed_place_index.saw\\nindex 2e479ab8..3a790352 100644\\n--- a/examples/coro_try_in_borrowed_place_index.saw\\n+++ b/examples/coro_try_in_borrowed_place_index.saw\\n@@ -62,8 +62,8 @@ func note(r: &Res) -> Int { r.name.len() }\\n func fail_index() sync -> Result<Int, Bad> { Bad(n: 1) }\\n func ok_index() sync -> Result<Int, Bad> { 0 }\\n func sink_ref(value: &Int, r: Res) sync -> Int { value + 1 }\\n-func sink_var(value: &var Int, r: Res) sync -> Int { value = value + 1  value }\\n-func sink_var_rev(r: Res, value: &var Int) sync -> Int { value = value + 1  value }\\n+func sink_var(value: &var Int, r: Res) sync -> Int { value = value + 1; value }\\n+func sink_var_rev(r: Res, value: &var Int) sync -> Int { value = value + 1; value }\\n func sink_cell(value: &Cell, r: Res) sync -> Int { value.n }\\n \\n // J: the index of a `&` argument (codex r2 #4's program), a Vector place\\ndiff --git a/examples/coro_try_landing_relinquishes_by_position.saw b/examples/coro_try_landing_relinquishes_by_position.saw\\nindex 605c5525..ce2625e0 100644\\n--- a/examples/coro_try_landing_relinquishes_by_position.saw\\n+++ b/examples/coro_try_landing_relinquishes_by_position.saw\\n@@ -116,14 +116,14 @@ func fail() sync -> Result<Int, Bad> { Bad(n: 1) }\\n func ok() sync -> Result<Int, Bad> { 1 }\\n func g(n: Int) sync -> Int { n + 10 }\\n func two(a: Int, b: Int) sync -> Int { a + b }\\n-func sink(n: Int, r: Res) sync -> Int { print(\\\"sink {}\\\", r.name)  n }\\n-func sink_rev(r: Res, n: Int) sync -> Int { print(\\\"sink_rev {}\\\", r.name)  n }\\n-func sink3(a: Int, r: Res, b: Int) sync -> Int { print(\\\"sink3 {}\\\", r.name)  a + b }\\n-func sink4(a: Int, r: Res, b: Int, q: Res) sync -> Int { print(\\\"sink4 {} {}\\\", r.name, q.name)  a + b }\\n-func eat(r: Res) sync -> Int { print(\\\"eat {}\\\", r.name)  1 }\\n-func take_res(r: Res) sync -> Result<Int, Bad> { print(\\\"take_res {}\\\", r.name)  Bad(n: 2) }\\n+func sink(n: Int, r: Res) sync -> Int { print(\\\"sink {}\\\", r.name); n }\\n+func sink_rev(r: Res, n: Int) sync -> Int { print(\\\"sink_rev {}\\\", r.name); n }\\n+func sink3(a: Int, r: Res, b: Int) sync -> Int { print(\\\"sink3 {}\\\", r.name); a + b }\\n+func sink4(a: Int, r: Res, b: Int, q: Res) sync -> Int { print(\\\"sink4 {} {}\\\", r.name, q.name); a + b }\\n+func eat(r: Res) sync -> Int { print(\\\"eat {}\\\", r.name); 1 }\\n+func take_res(r: Res) sync -> Result<Int, Bad> { print(\\\"take_res {}\\\", r.name); Bad(n: 2) }\\n struct Box2 { k: Int }\\n-extension Box2 { func use(&self, r: Res) -> Int { print(\\\"use {}\\\", r.name)  self.k } }\\n+extension Box2 { func use(&self, r: Res) -> Int { print(\\\"use {}\\\", r.name); self.k } }\\n func mk_fail() sync -> Result<Box2, Bad> { Bad(n: 3) }\\n \\n func row1() -> Result<Int, Bad> {\\ndiff --git a/examples/errors/capture_assign_escaping.saw b/examples/errors/capture_assign_escaping.saw\\nindex d9779e5a..5a0bbc35 100644\\n--- a/examples/errors/capture_assign_escaping.saw\\n+++ b/examples/errors/capture_assign_escaping.saw\\n@@ -11,8 +11,10 @@\\n \\n func make_counter() -> () -> Int {\\n     var n = 0\\n-    { n = n + 1\\n-      n }\\n+    {\\n+        n = n + 1\\n+        n\\n+    }\\n }\\n \\n func main() {\\ndiff --git a/examples/errors/capture_assign_non_escaping.saw b/examples/errors/capture_assign_non_escaping.saw\\nindex 080b9e8f..ff0185af 100644\\n--- a/examples/errors/capture_assign_non_escaping.saw\\n+++ b/examples/errors/capture_assign_non_escaping.saw\\n@@ -8,7 +8,7 @@\\n // EXPECT-ERROR-CONTAINS: cannot assign to `sum`: it is captured by value\\n // EXPECT-ERROR-CONTAINS: cannot assign to `total`: it is captured by value\\n \\n-func each3(body: (Int) -> Bool) { body(1) body(2) body(3) }\\n+func each3(body: (Int) -> Bool) { body(1); body(2); body(3) }\\n \\n func main() {\\n     var sum = 0\\ndiff --git a/examples/errors/consumes_field_move_in_a_loop.saw b/examples/errors/consumes_field_move_in_a_loop.saw\\nindex 11048c25..795bf15c 100644\\n--- a/examples/errors/consumes_field_move_in_a_loop.saw\\n+++ b/examples/errors/consumes_field_move_in_a_loop.saw\\n@@ -23,7 +23,7 @@ struct Pair { a: Tag, b: Tag }\\n extension Pair: NoCopy {}\\n extension Pair {\\n     func looped(&var self) consumes -> Int {\\n-        for i in 0..2 { let t = move self.a  print(\\\"{}\\\", t.id) }\\n+        for i in 0..2 { let t = move self.a; print(\\\"{}\\\", t.id) }\\n         0\\n     }\\n     func after(&var self) consumes -> Int {\\ndiff --git a/examples/errors/for_head_is_a_transfer.saw b/examples/errors/for_head_is_a_transfer.saw\\nindex 412686dc..05f723ee 100644\\n--- a/examples/errors/for_head_is_a_transfer.saw\\n+++ b/examples/errors/for_head_is_a_transfer.saw\\n@@ -31,7 +31,7 @@ extension Counter: NoCopy {\\n extension Counter: Iterator {\\n     type Item = Int\\n     func next(&var self) -> Int? {\\n-        if self.index == 0 { self.index = 1  7 } else { None }\\n+        if self.index == 0 { self.index = 1; 7 } else { None }\\n     }\\n }\\n \\n@@ -58,7 +58,7 @@ func row_nocopy_sync() sync {\\n func row_nocopy_driven() -> Int {\\n     let it = Counter(index: 0)\\n     var total = 0\\n-    for n in it { yield_now()  total = total + n }   // ROW: the driven twin\\n+    for n in it { yield_now(); total = total + n }   // ROW: the driven twin\\n     total\\n }\\n \\ndiff --git a/examples/errors/place_window_through_a_forced_lend_charges_its_root.saw b/examples/errors/place_window_through_a_forced_lend_charges_its_root.saw\\nindex 841bd02f..e919c598 100644\\n--- a/examples/errors/place_window_through_a_forced_lend_charges_its_root.saw\\n+++ b/examples/errors/place_window_through_a_forced_lend_charges_its_root.saw\\n@@ -27,7 +27,7 @@ extension Bag {\\n     public func first(&var self) -> Int { self.rows[0].n }\\n }\\n \\n-func setboth(a: &var Cell, b: &var Cell) { a.n = 1  b.n = 2 }\\n+func setboth(a: &var Cell, b: &var Cell) { a.n = 1; b.n = 2 }\\n func setroot(a: &var Cell, r: &var Bag) { a.n = r.first() }\\n \\n func main() {\\ndiff --git a/examples/errors/semicolon_at_end_of_file.saw b/examples/errors/semicolon_at_end_of_file.saw\\nnew file mode 100644\\nindex 00000000..c6ce159c\\n--- /dev/null\\n+++ b/examples/errors/semicolon_at_end_of_file.saw\\n@@ -0,0 +1,12 @@\\n+// SL-347 R2 — a `;` at END OF FILE.\\n+//\\n+// A block always closes with `}`, so the only place a `;` can sit with nothing\\n+// at all after it is module scope, at the end of the file. It separates\\n+// nothing there either, and gets the same answer.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: a `;` joins two statements on one line; it does not end one — remove it\\n+\\n+func main() { print(\\\"{}\\\", LIMIT) }\\n+\\n+static LIMIT: Int = 1;\\ndiff --git a/examples/errors/semicolon_at_line_start.saw b/examples/errors/semicolon_at_line_start.saw\\nnew file mode 100644\\nindex 00000000..29642e7f\\n--- /dev/null\\n+++ b/examples/errors/semicolon_at_line_start.saw\\n@@ -0,0 +1,14 @@\\n+// SL-347 R2 — a `;` at the START of a line.\\n+//\\n+// The newline above it already separated the two statements, so a `;` opening\\n+// the next line has nothing on its left to join. Same rule, read from the\\n+// other side; the chokepoint checks it once per statement gap, which is why\\n+// the very first statement of a block is covered too.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: a `;` joins two statements on one line; it does not end one — remove it\\n+\\n+func main() {\\n+    let a = 1\\n+    ; print(\\\"{}\\\", a)\\n+}\\ndiff --git a/examples/errors/semicolon_before_closing_brace.saw b/examples/errors/semicolon_before_closing_brace.saw\\nnew file mode 100644\\nindex 00000000..7500eb3a\\n--- /dev/null\\n+++ b/examples/errors/semicolon_before_closing_brace.saw\\n@@ -0,0 +1,12 @@\\n+// SL-347 R2 — a `;` before the block's `}`.\\n+//\\n+// The other face of \\\"a separator ends nothing\\\": the block's closing brace is\\n+// already the end of the last statement, so the `;` ahead of it joins the\\n+// statement to nothing.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: a `;` joins two statements on one line; it does not end one — remove it\\n+\\n+func main() {\\n+    let a = 1\\n+    print(\\\"{}\\\", a); }\\ndiff --git a/examples/errors/semicolon_before_newline.saw b/examples/errors/semicolon_before_newline.saw\\nnew file mode 100644\\nindex 00000000..e47ae97c\\n--- /dev/null\\n+++ b/examples/errors/semicolon_before_newline.saw\\n@@ -0,0 +1,13 @@\\n+// SL-347 R2 — a `;` before a NEWLINE.\\n+//\\n+// `;` is a SEPARATOR, never a terminator: it joins two statements that share a\\n+// line. One written at the end of a line separates nothing, so it is refused\\n+// where it stands rather than silently accepted as punctuation.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: a `;` joins two statements on one line; it does not end one — remove it\\n+\\n+func main() {\\n+    let a = 1;\\n+    print(\\\"{}\\\", a)\\n+}\\ndiff --git a/examples/errors/semicolon_between_declarations.saw b/examples/errors/semicolon_between_declarations.saw\\nnew file mode 100644\\nindex 00000000..66b5b8f8\\n--- /dev/null\\n+++ b/examples/errors/semicolon_between_declarations.saw\\n@@ -0,0 +1,13 @@\\n+// SL-347 R2 — a `;` between two DECLARATIONS.\\n+//\\n+// `;` joins two STATEMENTS that share a line. Module scope holds declarations,\\n+// not statements, and a declaration takes a line of its own — so the `;` here\\n+// does separate something and is refused for the other half of the rule, with\\n+// the answer the author needs.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: declarations are not joined by `;` — put each declaration on its own line\\n+\\n+func one() -> Int { 1 }; func two() -> Int { 2 }\\n+\\n+func main() { print(\\\"{}\\\", one() + two()) }\\ndiff --git a/examples/errors/semicolon_doubled.saw b/examples/errors/semicolon_doubled.saw\\nnew file mode 100644\\nindex 00000000..88c6fadd\\n--- /dev/null\\n+++ b/examples/errors/semicolon_doubled.saw\\n@@ -0,0 +1,13 @@\\n+// SL-347 R2 — a DOUBLED `;`.\\n+//\\n+// A separator wants a statement on each side, and the first `;` here has only\\n+// another `;` to its right — so it is the one that separates nothing, and the\\n+// one the diagnostic anchors on.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: a `;` joins two statements on one line; it does not end one — remove it\\n+\\n+func main() {\\n+    let a = 1;; let b = 2\\n+    print(\\\"{}\\\", a + b)\\n+}\\ndiff --git a/examples/errors/spawn_capture_across_iterations.saw b/examples/errors/spawn_capture_across_iterations.saw\\nindex 68e6862f..2c3436b0 100644\\n--- a/examples/errors/spawn_capture_across_iterations.saw\\n+++ b/examples/errors/spawn_capture_across_iterations.saw\\n@@ -23,7 +23,7 @@ func main() {\\n     var total = 0\\n     var group = TaskGroup()\\n     for i in 0..4 {\\n-        let h = group.spawn(run({ [&var total] in total = total + i  total }))\\n+        let h = group.spawn(run({ [&var total] in total = total + i; total }))\\n     }\\n     print(\\\"total {total}\\\")\\n }\\ndiff --git a/examples/errors/spawn_capture_mt_send.saw b/examples/errors/spawn_capture_mt_send.saw\\nindex 9a02d490..4d2e7b0b 100644\\n--- a/examples/errors/spawn_capture_mt_send.saw\\n+++ b/examples/errors/spawn_capture_mt_send.saw\\n@@ -24,6 +24,6 @@ func run(f: () -> Int) -> Int {\\n func main() {\\n     var n = 7\\n     var group = try! TaskGroup(threads: 2)\\n-    let h = group.spawn(run({ [&var n] in n = n + 1  n }))\\n+    let h = group.spawn(run({ [&var n] in n = n + 1; n }))\\n     print(\\\"task saw {h.join()}\\\")\\n }\\ndiff --git a/examples/errors/spawn_capture_unjoined_handle.saw b/examples/errors/spawn_capture_unjoined_handle.saw\\nindex 7a9e3d63..79dc4e19 100644\\n--- a/examples/errors/spawn_capture_unjoined_handle.saw\\n+++ b/examples/errors/spawn_capture_unjoined_handle.saw\\n@@ -28,14 +28,14 @@ func run(f: () -> Int) -> Int {\\n func discarded() {\\n     var n = 0\\n     var group = TaskGroup()\\n-    let _ = group.spawn(run({ [&var n] in n = n + 1  n }))\\n+    let _ = group.spawn(run({ [&var n] in n = n + 1; n }))\\n     print(\\\"n is {n}\\\")\\n }\\n \\n func conditional(flag: Bool) {\\n     var m = 0\\n     var group = TaskGroup()\\n-    let h = group.spawn(run({ [&var m] in m = m + 1  m }))\\n+    let h = group.spawn(run({ [&var m] in m = m + 1; m }))\\n     if flag {\\n         print(\\\"joined early: {h.join()}\\\")\\n     }\\ndiff --git a/examples/errors/statement_juxtaposition_in_closure_and_module_scope.saw b/examples/errors/statement_juxtaposition_in_closure_and_module_scope.saw\\nnew file mode 100644\\nindex 00000000..a3ffc08e\\n--- /dev/null\\n+++ b/examples/errors/statement_juxtaposition_in_closure_and_module_scope.saw\\n@@ -0,0 +1,28 @@\\n+// SL-347 R3 — bare JUXTAPOSITION is refused: the CLOSURE body and MODULE\\n+// SCOPE, the chokepoint's other two entry points.\\n+//\\n+// `parse_block` is one of three callers of `Parser.expect_statement_end`. The\\n+// other two are `ExpressionsMixin._parse_closure_body`, whose body is a\\n+// statement list of its own, and `Parser.parse`, whose units are\\n+// DECLARATIONS — and a declaration takes a line, so two on one line get the\\n+// declarations message rather than an offer of a `;`.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `c`, the second with `c`\\n+// EXPECT-ERROR-CONTAINS: write `;` where they meet\\n+// EXPECT-ERROR-CONTAINS: two declarations on one line: the first begins with `static`, the second with `static`\\n+// EXPECT-ERROR-CONTAINS: declarations are not joined by `;` — put each declaration on its own line\\n+\\n+func run(body: (Int) sync -> Int) -> Int { body(1) }\\n+\\n+// ROW 12 — a CLOSURE body.\\n+func closure_body_row() -> Int {\\n+    run({ c in c = c + 1 c })\\n+}\\n+\\n+// ROW 13 — MODULE SCOPE: two declarations on one line.\\n+static ALPHA: Int = 1 static BETA: Int = 2\\n+\\n+func main() {\\n+    print(\\\"{}\\\", closure_body_row())\\n+}\\ndiff --git a/examples/errors/statement_juxtaposition_in_control_flow_bodies.saw b/examples/errors/statement_juxtaposition_in_control_flow_bodies.saw\\nnew file mode 100644\\nindex 00000000..193e799a\\n--- /dev/null\\n+++ b/examples/errors/statement_juxtaposition_in_control_flow_bodies.saw\\n@@ -0,0 +1,42 @@\\n+// SL-347 R3 — bare JUXTAPOSITION is refused: the CONTROL-FLOW bodies.\\n+//\\n+// The `if`, `else`, `while` and `for` rows of the position matrix. Each body\\n+// reaches the statement-end chokepoint through the same `parse_block` funnel\\n+// its siblings do; these four are here so a body reached from a control-flow\\n+// head is shown to be covered too.\\n+//\\n+// Each row sits in its own TOP-LEVEL declaration (the parse loop batches one\\n+// error per declaration) and names a DIFFERENT pair of first tokens.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `var`, the second with `sum`\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `let`, the second with `return`\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `i`, the second with `break`\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `total`, the second with `continue`\\n+// EXPECT-ERROR-CONTAINS: write `;` where they meet\\n+\\n+// ROW 4 — an `if` body.\\n+func if_body_row(k: Int) {\\n+    if k > 0 { var sum = k sum += 1 }\\n+}\\n+\\n+// ROW 5 — an `else` body.\\n+func else_body_row(k: Int) -> Int {\\n+    if k > 0 { k } else { let e = 2 return e }\\n+}\\n+\\n+// ROW 6 — a `while` body.\\n+func while_body_row() {\\n+    var i = 0\\n+    while i < 3 { i += 1 break }\\n+}\\n+\\n+// ROW 7 — a `for` body.\\n+func for_body_row() {\\n+    var total = 0\\n+    for k in 0..3 { total += k continue }\\n+}\\n+\\n+func main() {\\n+    if_body_row(1)\\n+}\\ndiff --git a/examples/errors/statement_juxtaposition_in_function_and_method_bodies.saw b/examples/errors/statement_juxtaposition_in_function_and_method_bodies.saw\\nnew file mode 100644\\nindex 00000000..bdc60f4c\\n--- /dev/null\\n+++ b/examples/errors/statement_juxtaposition_in_function_and_method_bodies.saw\\n@@ -0,0 +1,47 @@\\n+// SL-347 R3 — bare JUXTAPOSITION is refused: FUNCTION, METHOD and `borrows`\\n+// bodies.\\n+//\\n+// After a statement the parser takes a NEWLINE, a `;`, the block's `}` or EOF\\n+// and nothing else — anything else is a second statement that never said\\n+// where the first one ended. `StatementsMixin.parse_block` is the one funnel\\n+// every block body goes through; the position matrix is one row per SHAPE of\\n+// block, so the rule is shown to reach each of them rather than one lucky\\n+// caller. This file carries three of those rows, its siblings the rest:\\n+//\\n+//   statement_juxtaposition_in_control_flow_bodies.saw\\n+//   statement_juxtaposition_in_match_guard_and_try_blocks.saw\\n+//   statement_juxtaposition_in_closure_and_module_scope.saw\\n+//\\n+// Each row sits in its own TOP-LEVEL declaration, because the file-level parse\\n+// loop batches one error per declaration and synchronizes past it. Every row\\n+// names a DIFFERENT pair of first tokens, so no row's expectation can be\\n+// satisfied by another row's error.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `let`, the second with `print`\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `self`, the second with `print`\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `let`, the second with `lend`\\n+// EXPECT-ERROR-CONTAINS: write `;` where they meet\\n+\\n+// ROW 1 — a FUNCTION body.\\n+func function_body_row() {\\n+    let a = 1 print(\\\"{}\\\", a)\\n+}\\n+\\n+struct Counter { n: Int }\\n+\\n+// ROW 2 — a METHOD body.\\n+extension Counter {\\n+    func bump(&var self) { self.n = 1 print(\\\"{}\\\", self.n) }\\n+}\\n+\\n+struct Cells { items: Vector<Int> }\\n+\\n+// ROW 3 — a `borrows` body.\\n+extension Cells {\\n+    func at(&var self, i: Int) borrows -> &var Int { let j = i lend self.items[j] }\\n+}\\n+\\n+func main() {\\n+    function_body_row()\\n+}\\ndiff --git a/examples/errors/statement_juxtaposition_in_match_guard_and_try_blocks.saw b/examples/errors/statement_juxtaposition_in_match_guard_and_try_blocks.saw\\nnew file mode 100644\\nindex 00000000..030a8032\\n--- /dev/null\\n+++ b/examples/errors/statement_juxtaposition_in_match_guard_and_try_blocks.saw\\n@@ -0,0 +1,51 @@\\n+// SL-347 R3 — bare JUXTAPOSITION is refused: a `match` arm's BLOCK body, a\\n+// `guard … else { }` and both blocks of `try { } catch { }`.\\n+//\\n+// A match arm's BARE body ends at the arm's `,` — that body is an expression\\n+// rather than a statement list, so it never reaches the chokepoint and\\n+// `parse_match` owns the comma, unchanged. An arm's BLOCK body is an ordinary\\n+// block and is covered, which is why the two are shown side by side here.\\n+//\\n+// Each row sits in its own TOP-LEVEL declaration (the parse loop batches one\\n+// error per declaration) and names a DIFFERENT pair of first tokens.\\n+//\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `tag`, the second with `print`\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `print`, the second with `return`\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `let`, the second with `a`\\n+// EXPECT-ERROR-CONTAINS: two statements on one line need a `;` between them: the first begins with `print`, the second with `let`\\n+// EXPECT-ERROR-CONTAINS: write `;` where they meet\\n+\\n+// ROW 8 — a `match` arm's BLOCK body. The second arm's BARE body is beside it\\n+// and is fine: its `,` is the arm grammar's, not a statement separator.\\n+func match_arm_block_row(k: Int) {\\n+    var tag = 0\\n+    match k {\\n+        case 0 -> { tag = 1 print(\\\"{}\\\", tag) },\\n+        case _ -> tag = 2\\n+    }\\n+}\\n+\\n+func positive(k: Int) -> Int? {\\n+    if k > 0 { k } else { None }\\n+}\\n+\\n+// ROW 9 — a `guard … else { }` body.\\n+func guard_else_row(k: Int) -> Int {\\n+    guard let v = positive(k) else { print(\\\"neg\\\") return 0 }\\n+    v\\n+}\\n+\\n+// ROW 10 — the `try { }` block.\\n+func try_block_row() -> Int {\\n+    try { let a = 1 a } catch { 0 }\\n+}\\n+\\n+// ROW 11 — the `catch { }` block.\\n+func catch_block_row() -> Int {\\n+    try { 1 } catch { print(\\\"c\\\") let n = 2 }\\n+}\\n+\\n+func main() {\\n+    match_arm_block_row(0)\\n+}\\ndiff --git a/examples/errors/unsaferef_write_through_let_closure_capture.saw b/examples/errors/unsaferef_write_through_let_closure_capture.saw\\nindex bdc09c6a..ce11fbe8 100644\\n--- a/examples/errors/unsaferef_write_through_let_closure_capture.saw\\n+++ b/examples/errors/unsaferef_write_through_let_closure_capture.saw\\n@@ -23,7 +23,7 @@ func mint(c: &var Counter) unsafe -> UnsafeRef<Counter> {\\n \\n func through_let(c: &var Counter) unsafe -> Int {\\n     let h = mint(&var c)\\n-    run_int({ [move h] in h.deref().bump()  h.deref().n })\\n+    run_int({ [move h] in h.deref().bump(); h.deref().n })\\n }\\n \\n func main() {\\ndiff --git a/examples/file_open_modes.saw b/examples/file_open_modes.saw\\nindex 562c8447..1cc5488f 100644\\n--- a/examples/file_open_modes.saw\\n+++ b/examples/file_open_modes.saw\\n@@ -30,7 +30,7 @@ func write_to(f: &File, text: String) {\\n func created(p: Path, text: String) {\\n     var f = match File.create(p) {\\n         case Ok(handle) -> move handle,\\n-        case Err(e) -> { print(\\\"create failed: {e}\\\")  return }\\n+        case Err(e) -> { print(\\\"create failed: {e}\\\"); return }\\n     }\\n     write_to(&f, text)\\n }\\n@@ -38,7 +38,7 @@ func created(p: Path, text: String) {\\n func appended(p: Path, text: String) -> Bool {\\n     var f = match File.open_append(p) {\\n         case Ok(handle) -> move handle,\\n-        case Err(e) -> { print(\\\"open_append failed: {e}\\\")  return false }\\n+        case Err(e) -> { print(\\\"open_append failed: {e}\\\"); return false }\\n     }\\n     write_to(&f, text)\\n     true\\n@@ -47,11 +47,11 @@ func appended(p: Path, text: String) -> Bool {\\n func contents(p: Path) -> String {\\n     var f = match File.open(p) {\\n         case Ok(handle) -> move handle,\\n-        case Err(e) -> { print(\\\"open failed: {e}\\\")  return \\\"\\\" }\\n+        case Err(e) -> { print(\\\"open failed: {e}\\\"); return \\\"\\\" }\\n     }\\n     let bytes = match f.read() {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"read failed: {e}\\\")  return \\\"\\\" }\\n+        case Err(e) -> { print(\\\"read failed: {e}\\\"); return \\\"\\\" }\\n     }\\n     match bytes.to_string() {\\n         case Ok(text) -> text,\\ndiff --git a/examples/for_head_transfer_accepts.saw b/examples/for_head_transfer_accepts.saw\\nindex 6d25ce90..d8f522e9 100644\\n--- a/examples/for_head_transfer_accepts.saw\\n+++ b/examples/for_head_transfer_accepts.saw\\n@@ -50,7 +50,7 @@ extension Counter: NoCopy {\\n extension Counter: Iterator {\\n     type Item = Int\\n     func next(&var self) -> Int? {\\n-        if self.index == 0 { self.index = 1  7 } else { None }\\n+        if self.index == 0 { self.index = 1; 7 } else { None }\\n     }\\n }\\n \\n@@ -58,7 +58,7 @@ struct Words { text: String, index: Int }      // the automatic Copy tier\\n extension Words: Iterator {\\n     type Item = Int\\n     func next(&var self) -> Int? {\\n-        if self.index < self.text.len() { self.index = self.index + 1  self.index }\\n+        if self.index < self.text.len() { self.index = self.index + 1; self.index }\\n         else { None }\\n     }\\n }\\n@@ -74,7 +74,7 @@ extension Shared: Copy {}\\n extension Shared: Iterator {\\n     type Item = Int\\n     func next(&var self) -> Int? {\\n-        if self.index < 2 { self.index = self.index + 1  self.index } else { None }\\n+        if self.index < 2 { self.index = self.index + 1; self.index } else { None }\\n     }\\n }\\n \\n@@ -82,7 +82,7 @@ struct Closed { f: () sync -> Int, index: Int }   // a closure env: Copy tier\\n extension Closed: Iterator {\\n     type Item = Int\\n     func next(&var self) -> Int? {\\n-        if self.index < 2 { self.index = self.index + 1  self.f() } else { None }\\n+        if self.index < 2 { self.index = self.index + 1; self.f() } else { None }\\n     }\\n }\\n \\n@@ -109,7 +109,7 @@ func move_sync() sync {\\n func move_driven() -> Int {\\n     let it = Counter(index: 0)\\n     var total = 0\\n-    for n in move it { yield_now()  total = total + n }\\n+    for n in move it { yield_now(); total = total + n }\\n     print(\\\"after move driven\\\")\\n     total\\n }\\n@@ -132,8 +132,8 @@ func copy_tier_sync(n: Int) sync {\\n func copy_tier_driven(n: Int) {\\n     let it = Words(text: \\\"abc {n}\\\", index: 0)\\n     var t = 0\\n-    for x in it { yield_now()  t = t + x }\\n-    for x in it { yield_now()  t = t + x }\\n+    for x in it { yield_now(); t = t + x }\\n+    for x in it { yield_now(); t = t + x }\\n     print(\\\"copy tier driven {t} {it.text} {it.index}\\\")\\n }\\n \\n@@ -148,8 +148,8 @@ func arc_sync(cell: Arc<Res>) sync {\\n func arc_driven(cell: Arc<Res>) {\\n     let it = Shared(cell: cell.copy(), index: 0)\\n     var t = 0\\n-    for x in it { yield_now()  t = t + x }\\n-    for x in it { yield_now()  t = t + x }\\n+    for x in it { yield_now(); t = t + x }\\n+    for x in it { yield_now(); t = t + x }\\n     print(\\\"arc driven {t} {it.index} {cell.strong_count()}\\\")\\n }\\n \\n@@ -164,8 +164,8 @@ func closure_sync(k: Int) sync {\\n func closure_driven(k: Int) {\\n     let it = Closed(f: { k * 10 }, index: 0)\\n     var t = 0\\n-    for x in it { yield_now()  t = t + x }\\n-    for x in it { yield_now()  t = t + x }\\n+    for x in it { yield_now(); t = t + x }\\n+    for x in it { yield_now(); t = t + x }\\n     print(\\\"closure driven {t} {it.index}\\\")\\n }\\n \\ndiff --git a/examples/init_fallible_result.saw b/examples/init_fallible_result.saw\\nindex 93bf9c48..e60df4f4 100644\\n--- a/examples/init_fallible_result.saw\\n+++ b/examples/init_fallible_result.saw\\n@@ -22,12 +22,16 @@ enum RangeError { case TooLow(seen: Int), case TooHigh(seen: Int) }\\n extension RangeError: Printable {\\n     func format(&self, into: &var StringBuilder) {\\n         match self {\\n-            case TooLow(seen) -> { try! into.append(\\\"reading \\\")\\n-                                   try! into.append(seen)\\n-                                   try! into.append(\\\" is out of range\\\") },\\n-            case TooHigh(seen) -> { try! into.append(\\\"reading \\\")\\n-                                    try! into.append(seen)\\n-                                    try! into.append(\\\" is out of range\\\") }\\n+            case TooLow(seen) -> {\\n+                try! into.append(\\\"reading \\\")\\n+                try! into.append(seen)\\n+                try! into.append(\\\" is out of range\\\")\\n+            },\\n+            case TooHigh(seen) -> {\\n+                try! into.append(\\\"reading \\\")\\n+                try! into.append(seen)\\n+                try! into.append(\\\" is out of range\\\")\\n+            }\\n         }\\n     }\\n }\\n@@ -75,7 +79,7 @@ enum Domain { case Range(e: RangeError) }\\n extension Domain: Printable {\\n     func format(&self, into: &var StringBuilder) {\\n         match self {\\n-            case Range(e) -> { try! into.append(\\\"range: \\\")  e.format(into: &var into) }\\n+            case Range(e) -> { try! into.append(\\\"range: \\\"); e.format(into: &var into) }\\n         }\\n     }\\n }\\ndiff --git a/examples/init_fallible_result_shapes.saw b/examples/init_fallible_result_shapes.saw\\nindex e22a2b62..c49826a4 100644\\n--- a/examples/init_fallible_result_shapes.saw\\n+++ b/examples/init_fallible_result_shapes.saw\\n@@ -27,8 +27,10 @@ enum MakeError { case DoesNotFit(cap: Int), case Empty }\\n extension MakeError: Printable {\\n     func format(&self, into: &var StringBuilder) {\\n         match self {\\n-            case DoesNotFit(cap) -> { try! into.append(\\\"does not fit \\\")\\n-                                      try! into.append(cap) },\\n+            case DoesNotFit(cap) -> {\\n+                try! into.append(\\\"does not fit \\\")\\n+                try! into.append(cap)\\n+            },\\n             case Empty -> try! into.append(\\\"empty\\\")\\n         }\\n     }\\ndiff --git a/examples/json_escape_grid.saw b/examples/json_escape_grid.saw\\nindex 12de3cfb..31e77fee 100644\\n--- a/examples/json_escape_grid.saw\\n+++ b/examples/json_escape_grid.saw\\n@@ -22,15 +22,15 @@ func check_short_escapes() {\\n     var enc = JsonEncoder()\\n     match enc.write_text(s) {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"short_escapes write failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"short_escapes write failed: {}\\\", e); return }\\n     }\\n     let text = match enc.finish() {\\n         case Ok(t) -> t,\\n-        case Err(e) -> { print(\\\"short_escapes finish failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"short_escapes finish failed: {}\\\", e); return }\\n     }\\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"short_escapes open failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"short_escapes open failed: {}\\\", e); return }\\n     }\\n     match dec.read_text() {\\n         case Ok(back) -> {\\n@@ -49,7 +49,7 @@ func check_short_escapes() {\\n func check_decodes_to_scalar(label: String, text: String) {\\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"{} open failed: {}\\\", label, e) return }\\n+        case Err(e) -> { print(\\\"{} open failed: {}\\\", label, e); return }\\n     }\\n     match dec.read_text() {\\n         case Ok(back) -> {\\n@@ -90,7 +90,7 @@ func to_hex(n: Int) -> String {\\n func check_reject(label: String, text: String) {\\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"{}: {}\\\", label, e) return }\\n+        case Err(e) -> { print(\\\"{}: {}\\\", label, e); return }\\n     }\\n     match dec.read_text() {\\n         case Ok(back) -> print(\\\"{}: unexpectedly accepted -> {}\\\", label, back),\\ndiff --git a/examples/json_map_keys.saw b/examples/json_map_keys.saw\\nindex 6d2f5a83..02de5a58 100644\\n--- a/examples/json_map_keys.saw\\n+++ b/examples/json_map_keys.saw\\n@@ -31,7 +31,7 @@ extension Bag: Serialize {\\n             }\\n             match to.write_text(k) {\\n                 case Ok(_) -> {},\\n-                case Err(e) -> { first_err = e return }\\n+                case Err(e) -> { first_err = e; return }\\n             }\\n             match to.write_int(v) {\\n                 case Ok(_) -> {},\\n@@ -71,22 +71,22 @@ func check_map_roundtrip() {\\n     var enc = JsonEncoder()\\n     match bag.serialize(to: &var enc) {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"bag encode failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"bag encode failed: {}\\\", e); return }\\n     }\\n     let text = match enc.finish() {\\n         case Ok(t) -> t,\\n-        case Err(e) -> { print(\\\"bag finish failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"bag finish failed: {}\\\", e); return }\\n     }\\n     print(\\\"bag text: {}\\\", text)\\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"bag decoder refused: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"bag decoder refused: {}\\\", e); return }\\n     }\\n     match Bag.deserialize(from: &var dec) {\\n         case Ok(back) -> {\\n             match dec.finish() {\\n                 case Ok(_) -> {},\\n-                case Err(e) -> { print(\\\"bag trailing content: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"bag trailing content: {}\\\", e); return }\\n             }\\n             let a = back.m.get(\\\"a\\\") ?? -1\\n             let b = back.m.get(\\\"b\\\") ?? -1\\n@@ -102,7 +102,7 @@ func check_int_key_rejected() {\\n     var enc = JsonEncoder()\\n     match enc.begin_map(1) {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"int_key_map: begin_map failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"int_key_map: begin_map failed: {}\\\", e); return }\\n     }\\n     match enc.write_int(7) {\\n         case Ok(_) -> print(\\\"int_key_map: unexpectedly accepted an Int key\\\"),\\n@@ -116,7 +116,7 @@ func check_int_key_rejected() {\\n func check_non_string_key_wire() {\\n     var dec = match JsonDecoder.open(text: \\\"\\\\{1:2}\\\") {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"non_string_key_wire: open rejected: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"non_string_key_wire: open rejected: {}\\\", e); return }\\n     }\\n     match dec.begin_map() {\\n         case Ok(n) -> print(\\\"non_string_key_wire: unexpectedly opened -> {}\\\", n),\\ndiff --git a/examples/json_number_edges.saw b/examples/json_number_edges.saw\\nindex 217173ac..0a9c62a7 100644\\n--- a/examples/json_number_edges.saw\\n+++ b/examples/json_number_edges.saw\\n@@ -23,7 +23,7 @@ import std.json.{JsonDecoder}\\n func check_int(label: String, text: String) {\\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"{}: open rejected: {}\\\", label, e) return }\\n+        case Err(e) -> { print(\\\"{}: open rejected: {}\\\", label, e); return }\\n     }\\n     match dec.read_int() {\\n         case Ok(v) -> print(\\\"{}: {}\\\", label, v),\\n@@ -34,7 +34,7 @@ func check_int(label: String, text: String) {\\n func check_uint(label: String, text: String) {\\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"{}: open rejected: {}\\\", label, e) return }\\n+        case Err(e) -> { print(\\\"{}: open rejected: {}\\\", label, e); return }\\n     }\\n     match dec.read_uint() {\\n         case Ok(v) -> print(\\\"{}: {}\\\", label, v),\\n@@ -45,11 +45,11 @@ func check_uint(label: String, text: String) {\\n func check_leading_zero_in_array() {\\n     var dec = match JsonDecoder.open(text: \\\"[01]\\\") {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"leading_zero_in_array: open rejected: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"leading_zero_in_array: open rejected: {}\\\", e); return }\\n     }\\n     match dec.begin_array() {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"leading_zero_in_array: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"leading_zero_in_array: {}\\\", e); return }\\n     }\\n     match dec.read_int() {\\n         case Ok(v) -> {\\n@@ -73,23 +73,23 @@ func check_exponent_forms_are_well_formed() {\\n     let hand = \\\"[5e3,-5e0,5E8]\\\"\\n     var dec = match JsonDecoder.open(text: hand) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"exponent_forms_ok: open failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"exponent_forms_ok: open failed: {}\\\", e); return }\\n     }\\n     match dec.begin_array() {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"exponent_forms_ok: begin_array failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"exponent_forms_ok: begin_array failed: {}\\\", e); return }\\n     }\\n     match dec.skip() {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"exponent_forms_ok: skip 1 failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"exponent_forms_ok: skip 1 failed: {}\\\", e); return }\\n     }\\n     match dec.skip() {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"exponent_forms_ok: skip 2 failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"exponent_forms_ok: skip 2 failed: {}\\\", e); return }\\n     }\\n     match dec.skip() {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"exponent_forms_ok: skip 3 failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"exponent_forms_ok: skip 3 failed: {}\\\", e); return }\\n     }\\n     match dec.finish() {\\n         case Ok(_) -> print(\\\"exponent_forms_ok: {} {} {}\\\", 5000, 0 - 5, 500000000),\\ndiff --git a/examples/json_roundtrip_struct.saw b/examples/json_roundtrip_struct.saw\\nindex 66ee712e..13cb4c8e 100644\\n--- a/examples/json_roundtrip_struct.saw\\n+++ b/examples/json_roundtrip_struct.saw\\n@@ -68,19 +68,19 @@ func check_endpoint() {\\n                       nums: move nums, inner: move inner)\\n     let text = match encode_endpoint(&ep) {\\n         case Ok(t) -> t,\\n-        case Err(e) -> { print(\\\"endpoint encode failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"endpoint encode failed: {}\\\", e); return }\\n     }\\n     print(\\\"endpoint text: {}\\\", text)\\n \\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"endpoint decoder refused: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"endpoint decoder refused: {}\\\", e); return }\\n     }\\n     match Endpoint.deserialize(from: &var dec) {\\n         case Ok(back) -> {\\n             match dec.finish() {\\n                 case Ok(_) -> {},\\n-                case Err(e) -> { print(\\\"endpoint trailing content: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"endpoint trailing content: {}\\\", e); return }\\n             }\\n             let tag = back.tag ?? \\\"none\\\"\\n             print(\\\"endpoint back: port={} secure={} name={} tag={} nums={},{},{} inner={}:{}\\\",\\n@@ -108,16 +108,16 @@ func check_empty_vector() {\\n     var enc = JsonEncoder()\\n     match n.serialize(to: &var enc) {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"empty vector encode failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"empty vector encode failed: {}\\\", e); return }\\n     }\\n     let text = match enc.finish() {\\n         case Ok(t) -> t,\\n-        case Err(e) -> { print(\\\"empty vector finish failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"empty vector finish failed: {}\\\", e); return }\\n     }\\n     print(\\\"empty vector text: {}\\\", text)\\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"empty vector decoder refused: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"empty vector decoder refused: {}\\\", e); return }\\n     }\\n     match Nums.deserialize(from: &var dec) {\\n         case Ok(back) -> print(\\\"empty vector back len: {}\\\", back.v.len()),\\n@@ -129,15 +129,15 @@ func check_track(t: Track, label: String) {\\n     var enc = JsonEncoder()\\n     match t.serialize(to: &var enc) {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"{} encode failed: {}\\\", label, e) return }\\n+        case Err(e) -> { print(\\\"{} encode failed: {}\\\", label, e); return }\\n     }\\n     let text = match enc.finish() {\\n         case Ok(s) -> s,\\n-        case Err(e) -> { print(\\\"{} finish failed: {}\\\", label, e) return }\\n+        case Err(e) -> { print(\\\"{} finish failed: {}\\\", label, e); return }\\n     }\\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"{} decoder refused: {}\\\", label, e) return }\\n+        case Err(e) -> { print(\\\"{} decoder refused: {}\\\", label, e); return }\\n     }\\n     match Track.deserialize(from: &var dec) {\\n         case Ok(back) -> {\\ndiff --git a/examples/json_structural_reject.saw b/examples/json_structural_reject.saw\\nindex 3c67916e..81283dca 100644\\n--- a/examples/json_structural_reject.saw\\n+++ b/examples/json_structural_reject.saw\\n@@ -20,11 +20,11 @@ import std.json.{JsonDecoder}\\n func check_trailing_bytes() {\\n     var dec = match JsonDecoder.open(text: \\\"1 2\\\") {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"trailing_bytes: open rejected: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"trailing_bytes: open rejected: {}\\\", e); return }\\n     }\\n     match dec.read_int() {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"trailing_bytes: read failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"trailing_bytes: read failed: {}\\\", e); return }\\n     }\\n     match dec.finish() {\\n         case Ok(_) -> print(\\\"trailing_bytes: unexpectedly accepted\\\"),\\n@@ -35,7 +35,7 @@ func check_trailing_bytes() {\\n func check_trailing_comma_array() {\\n     var dec = match JsonDecoder.open(text: \\\"[1,2,]\\\") {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"trailing_comma_array: open rejected: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"trailing_comma_array: open rejected: {}\\\", e); return }\\n     }\\n     match dec.begin_array() {\\n         case Ok(n) -> print(\\\"trailing_comma_array: begin_array unexpectedly returned {}\\\", n),\\n@@ -46,7 +46,7 @@ func check_trailing_comma_array() {\\n func check_trailing_comma_object() {\\n     var dec = match JsonDecoder.open(text: \\\"\\\\{\\\\\\\"a\\\\\\\":1,}\\\") {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"trailing_comma_object: open rejected: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"trailing_comma_object: open rejected: {}\\\", e); return }\\n     }\\n     match dec.begin_map() {\\n         case Ok(n) -> print(\\\"trailing_comma_object: begin_map unexpectedly returned {}\\\", n),\\n@@ -57,7 +57,7 @@ func check_trailing_comma_object() {\\n func check_unterminated_string() {\\n     var dec = match JsonDecoder.open(text: \\\"\\\\\\\"abc\\\") {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"unterminated_string: open rejected: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"unterminated_string: open rejected: {}\\\", e); return }\\n     }\\n     match dec.read_text() {\\n         case Ok(s) -> print(\\\"unterminated_string: unexpectedly accepted -> {}\\\", s),\\n@@ -68,7 +68,7 @@ func check_unterminated_string() {\\n func check_unterminated_array() {\\n     var dec = match JsonDecoder.open(text: \\\"[1,2\\\") {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"unterminated_array: open rejected: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"unterminated_array: open rejected: {}\\\", e); return }\\n     }\\n     match dec.begin_array() {\\n         case Ok(n) -> print(\\\"unterminated_array: begin_array unexpectedly returned {}\\\", n),\\n@@ -79,7 +79,7 @@ func check_unterminated_array() {\\n func check_unterminated_object() {\\n     var dec = match JsonDecoder.open(text: \\\"\\\\{\\\\\\\"a\\\\\\\":1\\\") {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"unterminated_object: open rejected: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"unterminated_object: open rejected: {}\\\", e); return }\\n     }\\n     match dec.begin_map() {\\n         case Ok(n) -> print(\\\"unterminated_object: begin_map unexpectedly returned {}\\\", n),\\n@@ -91,7 +91,7 @@ func check_depth() {\\n     // Exactly 3 levels of nesting under max_depth 3: legal.\\n     var dec_ok = match JsonDecoder.open(text: \\\"[[[0]]]\\\", max_depth: 3) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"depth_ok_at_limit: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"depth_ok_at_limit: {}\\\", e); return }\\n     }\\n     match dec_ok.begin_array() {\\n         case Ok(_) -> print(\\\"depth_ok_at_limit: opened\\\"),\\n@@ -101,7 +101,7 @@ func check_depth() {\\n     // A 4th level under the same limit is refused.\\n     var dec_bad = match JsonDecoder.open(text: \\\"[[[[0]]]]\\\", max_depth: 3) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"depth_exceeds_limit: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"depth_exceeds_limit: {}\\\", e); return }\\n     }\\n     match dec_bad.begin_array() {\\n         case Ok(_) -> print(\\\"depth_exceeds_limit: unexpectedly opened\\\"),\\n@@ -120,29 +120,29 @@ func check_duplicate_key_last_wins() {\\n     let text = \\\"\\\\{\\\\\\\"a\\\\\\\":1,\\\\\\\"a\\\\\\\":2}\\\"\\n     var dec = match JsonDecoder.open(text: text) {\\n         case Ok(d) -> move d,\\n-        case Err(e) -> { print(\\\"duplicate_key_last_wins: open failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"duplicate_key_last_wins: open failed: {}\\\", e); return }\\n     }\\n     let n = match dec.begin_map() {\\n         case Ok(c) -> c,\\n-        case Err(e) -> { print(\\\"duplicate_key_last_wins: begin_map failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"duplicate_key_last_wins: begin_map failed: {}\\\", e); return }\\n     }\\n     var m = Map<String, Int>()\\n     var i = 0\\n     while i < n {\\n         let k = match dec.read_text() {\\n             case Ok(s) -> s,\\n-            case Err(e) -> { print(\\\"duplicate_key_last_wins: key failed: {}\\\", e) return }\\n+            case Err(e) -> { print(\\\"duplicate_key_last_wins: key failed: {}\\\", e); return }\\n         }\\n         let v = match dec.read_int() {\\n             case Ok(x) -> x,\\n-            case Err(e) -> { print(\\\"duplicate_key_last_wins: value failed: {}\\\", e) return }\\n+            case Err(e) -> { print(\\\"duplicate_key_last_wins: value failed: {}\\\", e); return }\\n         }\\n         let _ = m.insert(k, v)\\n         i = i + 1\\n     }\\n     match dec.finish() {\\n         case Ok(_) -> {},\\n-        case Err(e) -> { print(\\\"duplicate_key_last_wins: trailing: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"duplicate_key_last_wins: trailing: {}\\\", e); return }\\n     }\\n     if let v = m.get(\\\"a\\\") {\\n         print(\\\"duplicate_key_last_wins: a={}\\\", v)\\ndiff --git a/examples/json_value_escape_grid.saw b/examples/json_value_escape_grid.saw\\nindex 13672377..9aae6e92 100644\\n--- a/examples/json_value_escape_grid.saw\\n+++ b/examples/json_value_escape_grid.saw\\n@@ -19,7 +19,7 @@ func check_short_escapes() {\\n     let s = \\\"\\\\\\\"\\\\\\\\/\\\\u{8}\\\\u{c}\\\\n\\\\r\\\\t\\\"\\n     let text = match JsonValue.Text(value: s).to_json_string() {\\n         case Ok(t) -> t,\\n-        case Err(e) -> { print(\\\"short_escapes: encode failed: {}\\\", e) return }\\n+        case Err(e) -> { print(\\\"short_escapes: encode failed: {}\\\", e); return }\\n     }\\n     match JsonValue.parse(text: text) {\\n         case Ok(v) -> {\\ndiff --git a/examples/json_value_object_serialize.saw b/examples/json_value_object_serialize.saw\\nindex d321f5fa..8614d607 100644\\n--- a/examples/json_value_object_serialize.saw\\n+++ b/examples/json_value_object_serialize.saw\\n@@ -39,13 +39,13 @@ func check_basic_object_roundtrip() {\\n         case Ok(v) -> {\\n             let text1 = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"basic: serialize1 failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"basic: serialize1 failed: {}\\\", e); return }\\n             }\\n             match JsonValue.parse(text: text1) {\\n                 case Ok(v2) -> {\\n                     let text2 = match v2.to_json_string() {\\n                         case Ok(t) -> t,\\n-                        case Err(e) -> { print(\\\"basic: serialize2 failed: {}\\\", e) return }\\n+                        case Err(e) -> { print(\\\"basic: serialize2 failed: {}\\\", e); return }\\n                     }\\n                     // Re-parsing the SAME key set walks the SAME hash\\n                     // buckets, so the second serialize reproduces the\\n@@ -81,7 +81,7 @@ func check_nested_object_in_object() {\\n         case Ok(v) -> {\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"nested_obj: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"nested_obj: serialize failed: {}\\\", e); return }\\n             }\\n             print(\\\"nested_obj: text={}\\\", text)\\n             let inner = v.as_object()!.get(\\\"outer\\\")!.as_object()!.get(\\\"inner\\\")!.as_int() ?? -1\\n@@ -97,7 +97,7 @@ func check_object_in_array_in_object() {\\n         case Ok(v) -> {\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"obj_in_arr_in_obj: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"obj_in_arr_in_obj: serialize failed: {}\\\", e); return }\\n             }\\n             print(\\\"obj_in_arr_in_obj: text={}\\\", text)\\n             match JsonValue.parse(text: text) {\\n@@ -146,7 +146,7 @@ func check_duplicate_key_survives_roundtrip() {\\n         case Ok(v) -> {\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"dup_key: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"dup_key: serialize failed: {}\\\", e); return }\\n             }\\n             print(\\\"dup_key: text={}\\\", text)\\n             match JsonValue.parse(text: text) {\\ndiff --git a/examples/json_value_roundtrip.saw b/examples/json_value_roundtrip.saw\\nindex 02ac0d3b..311febb4 100644\\n--- a/examples/json_value_roundtrip.saw\\n+++ b/examples/json_value_roundtrip.saw\\n@@ -31,11 +31,11 @@ func check_null() {\\n             let is_n = v.is_null()\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"null: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"null: serialize failed: {}\\\", e); return }\\n             }\\n             let reparsed_is_n = match JsonValue.parse(text: text) {\\n                 case Ok(v2) -> v2.is_null(),\\n-                case Err(e) -> { print(\\\"null: reparse failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"null: reparse failed: {}\\\", e); return }\\n             }\\n             print(\\\"null: is_null={} text={} reparsed_is_null={}\\\", is_n, text, reparsed_is_n)\\n         },\\n@@ -49,7 +49,7 @@ func check_bool(label: String, text_in: String) {\\n             let b = v.as_bool() ?? false\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"{}: serialize failed: {}\\\", label, e) return }\\n+                case Err(e) -> { print(\\\"{}: serialize failed: {}\\\", label, e); return }\\n             }\\n             print(\\\"{}: value={} text={}\\\", label, b, text)\\n         },\\n@@ -63,7 +63,7 @@ func check_int() {\\n             let n = v.as_int() ?? -1\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"int: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"int: serialize failed: {}\\\", e); return }\\n             }\\n             print(\\\"int: value={} text={}\\\", n, text)\\n         },\\n@@ -77,7 +77,7 @@ func check_negative_int() {\\n             let n = v.as_int() ?? 0\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"negative_int: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"negative_int: serialize failed: {}\\\", e); return }\\n             }\\n             print(\\\"negative_int: value={} text={}\\\", n, text)\\n         },\\n@@ -91,7 +91,7 @@ func check_text() {\\n             let s = v.as_text() ?? \\\"?\\\"\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"text: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"text: serialize failed: {}\\\", e); return }\\n             }\\n             print(\\\"text: value={} text={}\\\", s, text)\\n         },\\n@@ -105,7 +105,7 @@ func check_array() {\\n             let l = v.as_array()!.len()\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"array: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"array: serialize failed: {}\\\", e); return }\\n             }\\n             let e1 = v.as_array()![1].as_int() ?? -1\\n             print(\\\"array: len={} text={} elem1={}\\\", l, text, e1)\\n@@ -120,16 +120,16 @@ func check_nested_array() {\\n         case Ok(v) -> {\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"nested_array: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"nested_array: serialize failed: {}\\\", e); return }\\n             }\\n             print(\\\"nested_array: text={}\\\", text)\\n \\n             let reparsed_text = match JsonValue.parse(text: text) {\\n                 case Ok(v2) -> match v2.to_json_string() {\\n                     case Ok(t2) -> t2,\\n-                    case Err(e) -> { print(\\\"nested_array_stability: reserialize failed: {}\\\", e) return }\\n+                    case Err(e) -> { print(\\\"nested_array_stability: reserialize failed: {}\\\", e); return }\\n                 },\\n-                case Err(e) -> { print(\\\"nested_array_stability: reparse failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"nested_array_stability: reparse failed: {}\\\", e); return }\\n             }\\n             print(\\\"nested_array_stability: match={}\\\", reparsed_text.equals(text))\\n         },\\n@@ -142,7 +142,7 @@ func check_array_of_mixed() {\\n         case Ok(v) -> {\\n             let text = match v.to_json_string() {\\n                 case Ok(t) -> t,\\n-                case Err(e) -> { print(\\\"array_of_mixed: serialize failed: {}\\\", e) return }\\n+                case Err(e) -> { print(\\\"array_of_mixed: serialize failed: {}\\\", e); return }\\n             }\\n             print(\\\"array_of_mixed: text={}\\\", text)\\n         },\\ndiff --git a/examples/match_borrowed_payload_in_a_driven_body.saw b/examples/match_borrowed_payload_in_a_driven_body.saw\\nindex 08c429fd..839ed9f6 100644\\n--- a/examples/match_borrowed_payload_in_a_driven_body.saw\\n+++ b/examples/match_borrowed_payload_in_a_driven_body.saw\\n@@ -42,7 +42,7 @@ func consume(args: Vector<String>) -> Int {\\n \\n func execute(plan: &Plan) -> Int {\\n     match plan.job {\\n-        case Idle -> { print(\\\"idle\\\")  0 },\\n+        case Idle -> { print(\\\"idle\\\"); 0 },\\n         case Run(args) -> { consume(args.copy()) }\\n     }\\n }\\ndiff --git a/examples/move_in_a_while_condition_back_edge.saw b/examples/move_in_a_while_condition_back_edge.saw\\nindex cdfc7f23..c60751b4 100644\\n--- a/examples/move_in_a_while_condition_back_edge.saw\\n+++ b/examples/move_in_a_while_condition_back_edge.saw\\n@@ -58,7 +58,7 @@ extension Res: NoCopy {\\n }\\n \\n func consume_bool(r: Res) -> Bool { true }\\n-func src(k: Int) -> Int? { if k > 0 { return k } return None }\\n+func src(k: Int) -> Int? { if k > 0 { return k }; return None }\\n \\n // The body always RETURNS: one condition evaluation, one destruction.\\n func always_returns() -> Int {\\ndiff --git a/examples/nested_closure_captures_frame_local.saw b/examples/nested_closure_captures_frame_local.saw\\nindex 920a7180..46a3fa79 100644\\n--- a/examples/nested_closure_captures_frame_local.saw\\n+++ b/examples/nested_closure_captures_frame_local.saw\\n@@ -55,7 +55,7 @@ extension Tick: Copy { func copy(&self) -> Tick { Tick(n: self.n) } }\\n \\n struct Dup { n: Int }\\n extension Dup: ExplicitCopy {\\n-    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1)  Dup(n: self.n + 1) }\\n+    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1); Dup(n: self.n + 1) }\\n     func deinit(&var self) { print(\\\"drop {}\\\", self.n) }\\n }\\n \\ndiff --git a/examples/place_two_windows_one_nocopy_root.saw b/examples/place_two_windows_one_nocopy_root.saw\\nindex e486e2ce..9f42b3de 100644\\n--- a/examples/place_two_windows_one_nocopy_root.saw\\n+++ b/examples/place_two_windows_one_nocopy_root.saw\\n@@ -50,7 +50,7 @@ extension Plain {\\n     func at(&var self) borrows -> &var Cell { lend self.c }\\n }\\n \\n-func setboth(x: &var Int, y: &var Int) { x = 50  y = 60 }\\n+func setboth(x: &var Int, y: &var Int) { x = 50; y = 60 }\\n \\n func main() {\\n     var h = Holder(c: Cell(n: 1))\\ndiff --git a/examples/place_window_nested_writes_land.saw b/examples/place_window_nested_writes_land.saw\\nindex 9a9b75aa..0d399152 100644\\n--- a/examples/place_window_nested_writes_land.saw\\n+++ b/examples/place_window_nested_writes_land.saw\\n@@ -27,8 +27,8 @@ extension Plain {\\n     func at(&var self) borrows -> &var Cell { lend self.c }\\n }\\n \\n-func setboth(x: &var Int, y: &var Int) { x = 50  y = 60 }\\n-func setthree(x: &var Int, y: &var Int, z: &var Int) { x = 1  y = 2  z = 3 }\\n+func setboth(x: &var Int, y: &var Int) { x = 50; y = 60 }\\n+func setthree(x: &var Int, y: &var Int, z: &var Int) { x = 1; y = 2; z = 3 }\\n \\n func main() {\\n     var a = Plain(c: Cell(n: 0))\\ndiff --git a/examples/semicolon_joins_statements_on_one_line.saw b/examples/semicolon_joins_statements_on_one_line.saw\\nnew file mode 100644\\nindex 00000000..e0afb2c5\\n--- /dev/null\\n+++ b/examples/semicolon_joins_statements_on_one_line.saw\\n@@ -0,0 +1,108 @@\\n+// SL-347 R1 — `;` is the one-line statement SEPARATOR, in every block kind.\\n+//\\n+// Saw has no statement terminators: a newline separates two statements, and a\\n+// `;` separates two statements that share a line. Nothing about the separator\\n+// is context-dependent, so this pins one `;`-joined one-liner per entry of the\\n+// parser's statement-end chokepoint: a function body, a method body, the two\\n+// branches of an `if`, a `while` body, a `for` body, a `match` arm's BLOCK\\n+// body, a closure body (whose last statement is the closure's TAIL), the two\\n+// blocks of `try { } catch { }`, a `guard … else { }`, and a `borrows` body.\\n+//\\n+// R4 rides along: the `;` inside `[Int; 3]` and `[0; 3]` is the array-length\\n+// and repeat-count separator, in a position where no statement can begin, and\\n+// it is untouched by any of this.\\n+//\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// sum 3\\n+// less\\n+// second on the line\\n+// tick 1\\n+// tick 2\\n+// step 0 doubled 0\\n+// step 1 doubled 2\\n+// closure 12\\n+// buffer 7 of 3\\n+// one\\n+// still one\\n+// bumped 1 then 2\\n+// ok 6\\n+// caught bad\\n+// empty guard\\n+// first 11\\n+// lent 41\\n+\\n+struct Counter { n: Int }\\n+\\n+extension Counter {\\n+    func bump(&var self) -> Int { self.n += 1; self.n }\\n+}\\n+\\n+struct Grid { cells: Vector<Int> }\\n+extension Grid: NoCopy {}\\n+\\n+extension Grid {\\n+    func at(&var self, i: Int) borrows -> &var Int {\\n+        if i < 0 || i >= self.cells.len() {\\n+            panic(\\\"Grid.at: index out of range: {} (len {})\\\", i, self.cells.len())\\n+        }\\n+        let slot = i; lend self.cells[slot]\\n+    }\\n+}\\n+\\n+enum Fault { case Bad }\\n+\\n+extension Fault: Printable {\\n+    func format(&self, into: &var StringBuilder) { try! into.append(\\\"bad\\\") }\\n+}\\n+\\n+extension Fault: Error {}\\n+\\n+func checked(n: Int) -> Result<Int, Fault> {\\n+    if n < 0 { return Fault.Bad }\\n+    return n * 2\\n+}\\n+\\n+func run(body: (Int) sync -> Int) -> Int { body(5) }\\n+\\n+func first_positive(v: &Vector<Int>) -> Int {\\n+    guard let head = v.get(0) else { print(\\\"empty guard\\\"); return -1 }\\n+    head\\n+}\\n+\\n+func main() {\\n+    let a = 1; let b = 2\\n+    print(\\\"sum {}\\\", a + b)\\n+\\n+    if a < b { print(\\\"less\\\"); print(\\\"second on the line\\\") } else { print(\\\"not less\\\") }\\n+\\n+    var n = 0\\n+    while n < 2 { n += 1; print(\\\"tick {}\\\", n) }\\n+\\n+    for i in 0..2 { let d = i * 2; print(\\\"step {} doubled {}\\\", i, d) }\\n+\\n+    print(\\\"closure {}\\\", run({ x in let y = x + 1; y * 2 }))\\n+\\n+    var buf: [Int; 3] = [0; 3]\\n+    buf[0] = 7; print(\\\"buffer {} of {}\\\", buf[0], buf.len())\\n+\\n+    match a {\\n+        case 1 -> { print(\\\"one\\\"); print(\\\"still one\\\") },\\n+        case _ -> { print(\\\"other\\\") }\\n+    }\\n+\\n+    var c = Counter(n: 0)\\n+    let first = c.bump(); let second = c.bump()\\n+    print(\\\"bumped {} then {}\\\", first, second)\\n+\\n+    try { let ok = try checked(3); print(\\\"ok {}\\\", ok) } catch { print(\\\"caught {error}\\\") }\\n+    try { let bad = try checked(-1); print(\\\"unreachable {}\\\", bad) } catch { print(\\\"caught {error}\\\") }\\n+\\n+    var empty: Vector<Int> = []\\n+    let _ = first_positive(&empty)\\n+    try! empty.push(11)\\n+    print(\\\"first {}\\\", first_positive(&empty))\\n+\\n+    var g = Grid(cells: [40, 50])\\n+    g.at(0) += 1; print(\\\"lent {}\\\", g.at(0))\\n+}\\ndiff --git a/examples/shared_self_field_call_exemption.saw b/examples/shared_self_field_call_exemption.saw\\nindex 6769ca1a..595f2372 100644\\n--- a/examples/shared_self_field_call_exemption.saw\\n+++ b/examples/shared_self_field_call_exemption.saw\\n@@ -38,7 +38,7 @@ extension Shared: NoCopy {}\\n \\n extension Shared {\\n     func probe(&self) -> Bool { self.cell.is_locked() }\\n-    func add(&self, v: Int) -> Int { self.cell.lock({ n in n = n + v  n }) }\\n+    func add(&self, v: Int) -> Int { self.cell.lock({ n in n = n + v; n }) }\\n }\\n \\n // 3. INDIRECTION. A `Vector` field's heap elements are shared by the copy, not\\ndiff --git a/examples/spawn_capture_after_group.saw b/examples/spawn_capture_after_group.saw\\nindex 7e4a85d1..822d3257 100644\\n--- a/examples/spawn_capture_after_group.saw\\n+++ b/examples/spawn_capture_after_group.saw\\n@@ -21,7 +21,7 @@ func run(f: () -> Int) -> Int {\\n func main() {\\n     var group = TaskGroup()\\n     var n = 7\\n-    let h = group.spawn(run({ [&var n] in n = n + 1  n }))\\n+    let h = group.spawn(run({ [&var n] in n = n + 1; n }))\\n     print(\\\"task saw {h.join()}\\\")\\n     print(\\\"n is now {n}\\\")\\n }\\ndiff --git a/examples/spawn_capture_alias.saw b/examples/spawn_capture_alias.saw\\nindex 550ce999..b8fc4da5 100644\\n--- a/examples/spawn_capture_alias.saw\\n+++ b/examples/spawn_capture_alias.saw\\n@@ -19,8 +19,8 @@ func run(f: () -> Int) -> Int {\\n func main() {\\n     var n = 0\\n     var group = TaskGroup()\\n-    let a = group.spawn(run({ [&var n] in n = n + 1  n }))\\n-    let b = group.spawn(run({ [&var n] in n = n + 10  n }))\\n+    let a = group.spawn(run({ [&var n] in n = n + 1; n }))\\n+    let b = group.spawn(run({ [&var n] in n = n + 10; n }))\\n     print(\\\"a saw {a.join()}\\\")\\n     print(\\\"b saw {b.join()}\\\")\\n     print(\\\"n is {n}\\\")\\ndiff --git a/examples/spawn_capture_caller_alias.saw b/examples/spawn_capture_caller_alias.saw\\nindex c53c9d27..b7226ce9 100644\\n--- a/examples/spawn_capture_caller_alias.saw\\n+++ b/examples/spawn_capture_caller_alias.saw\\n@@ -23,7 +23,7 @@ func run(f: () -> Int) -> Int {\\n func main() {\\n     var n = 0\\n     var group = TaskGroup()\\n-    let h = group.spawn(run({ [&var n] in n = n + 100  n }))\\n+    let h = group.spawn(run({ [&var n] in n = n + 100; n }))\\n     n = 5\\n     let seen = n\\n     print(\\\"caller wrote 5, read {seen}\\\")\\ndiff --git a/examples/spawn_capture_declared_before.saw b/examples/spawn_capture_declared_before.saw\\nindex 623088db..8d4bc53c 100644\\n--- a/examples/spawn_capture_declared_before.saw\\n+++ b/examples/spawn_capture_declared_before.saw\\n@@ -29,7 +29,7 @@ func main() {\\n     let base = 12\\n     var group = TaskGroup()\\n \\n-    let h = group.spawn(run({ [&var n] in n = n + 1  n }))\\n+    let h = group.spawn(run({ [&var n] in n = n + 1; n }))\\n     print(\\\"task saw {h.join()}\\\")\\n     print(\\\"n is now {n}\\\")\\n \\ndiff --git a/examples/spawn_capture_join_releases.saw b/examples/spawn_capture_join_releases.saw\\nindex 000de21d..491cdbe3 100644\\n--- a/examples/spawn_capture_join_releases.saw\\n+++ b/examples/spawn_capture_join_releases.saw\\n@@ -43,8 +43,8 @@ func main() {\\n     var log: Vector<Int> = []\\n     var group = TaskGroup()\\n \\n-    let a = group.spawn(run({ [&var left] in left = left + 1  left }))\\n-    let b = group.spawn(run({ [&var right] in right = right + 1  right }))\\n+    let a = group.spawn(run({ [&var left] in left = left + 1; left }))\\n+    let b = group.spawn(run({ [&var right] in right = right + 1; right }))\\n     print(\\\"left {a.join()} right {b.join()}\\\")\\n     print(\\\"roots after join: {left} {right}\\\")\\n \\n@@ -54,7 +54,7 @@ func main() {\\n     print(\\\"doubled {c.join()} tripled {d.join()}\\\")\\n \\n     for i in 0..3 {\\n-        let h = group.spawn(run({ [&var log] in try! log.push(i)  log.len() }))\\n+        let h = group.spawn(run({ [&var log] in try! log.push(i); log.len() }))\\n         print(\\\"iteration {i} len {h.join()}\\\")\\n     }\\n     print(\\\"log len {log.len()}\\\")\\ndiff --git a/examples/spawn_capture_move_root.saw b/examples/spawn_capture_move_root.saw\\nindex fffa77fa..ba20a7a8 100644\\n--- a/examples/spawn_capture_move_root.saw\\n+++ b/examples/spawn_capture_move_root.saw\\n@@ -24,7 +24,7 @@ func consume(v: Vector<Int>) -> Int {\\n func main() {\\n     var buf: Vector<Int> = [1, 2, 3]\\n     var group = TaskGroup()\\n-    let h = group.spawn(run({ [&var buf] in buf.push(9)  buf.len() }))\\n+    let h = group.spawn(run({ [&var buf] in buf.push(9); buf.len() }))\\n     let taken = consume(move buf)\\n     print(\\\"consumed len {taken}; task result {h.join()}\\\")\\n }\\ndiff --git a/examples/spinlock_import_gate.saw b/examples/spinlock_import_gate.saw\\nindex 238c6a83..92134791 100644\\n--- a/examples/spinlock_import_gate.saw\\n+++ b/examples/spinlock_import_gate.saw\\n@@ -11,5 +11,5 @@\\n static LOCK: SpinLock<Int>\\n \\n func main() {\\n-    print(LOCK.lock({ c in c = c + 1  c }))\\n+    print(LOCK.lock({ c in c = c + 1; c }))\\n }\\ndiff --git a/examples/trailing_closure_inside_a_try_operand.saw b/examples/trailing_closure_inside_a_try_operand.saw\\nindex 97e00c78..272f56fb 100644\\n--- a/examples/trailing_closure_inside_a_try_operand.saw\\n+++ b/examples/trailing_closure_inside_a_try_operand.saw\\n@@ -39,19 +39,19 @@ func parenthesized() -> Int {\\n // The three cells: one per `try` variant, each with a trailing closure.\\n func forced() -> Int {\\n     let jobs = fresh()\\n-    let labels = try! jobs.map { j in j.label() }\\n+    let labels = try! jobs.map; { j in j.label() }\\n     return labels.len()\\n }\\n \\n func optional_variant() -> Int {\\n     let jobs = fresh()\\n-    let labels = try? jobs.map { j in j.label() }\\n+    let labels = try? jobs.map; { j in j.label() }\\n     return labels!.len()\\n }\\n \\n func propagating() -> Result<Int, AllocError> {\\n     let jobs = fresh()\\n-    let labels = try jobs.map { j in j.label() }\\n+    let labels = try jobs.map; { j in j.label() }\\n     return labels.len()\\n }\\n \\ndiff --git a/examples/try_routing_clause.saw b/examples/try_routing_clause.saw\\nindex 3130fff2..e06a83bd 100644\\n--- a/examples/try_routing_clause.saw\\n+++ b/examples/try_routing_clause.saw\\n@@ -100,9 +100,9 @@ enum LocalError {\\n extension LocalError: Printable {\\n     func format(&self, into: &var StringBuilder) {\\n         match self {\\n-            case Alloc(e) -> { try! into.append(\\\"alloc/\\\")  e.format(into: &var into) },\\n-            case Parse(e) -> { try! into.append(\\\"parse/\\\")  e.format(into: &var into) },\\n-            case Dep(e) -> { try! into.append(\\\"dep/\\\")  e.format(into: &var into) }\\n+            case Alloc(e) -> { try! into.append(\\\"alloc/\\\"); e.format(into: &var into) },\\n+            case Parse(e) -> { try! into.append(\\\"parse/\\\"); e.format(into: &var into) },\\n+            case Dep(e) -> { try! into.append(\\\"dep/\\\"); e.format(into: &var into) }\\n         }\\n     }\\n }\\ndiff --git a/examples/two_copy_captures.saw b/examples/two_copy_captures.saw\\nindex ba7c3722..6c30a1d5 100644\\n--- a/examples/two_copy_captures.saw\\n+++ b/examples/two_copy_captures.saw\\n@@ -39,7 +39,7 @@\\n \\n struct Dup { n: Int }\\n extension Dup: ExplicitCopy {\\n-    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1)  Dup(n: self.n + 1) }\\n+    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1); Dup(n: self.n + 1) }\\n     func deinit(&var self) { print(\\\"drop {}\\\", self.n) }\\n }\\n \\ndiff --git a/examples/two_escaping_copies.saw b/examples/two_escaping_copies.saw\\nindex 0269a337..545be0c2 100644\\n--- a/examples/two_escaping_copies.saw\\n+++ b/examples/two_escaping_copies.saw\\n@@ -34,7 +34,7 @@\\n \\n struct Dup { n: Int }\\n extension Dup: ExplicitCopy {\\n-    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1)  Dup(n: self.n + 1) }\\n+    func copy(&self) -> Dup { print(\\\"copy {} -> {}\\\", self.n, self.n + 1); Dup(n: self.n + 1) }\\n     func deinit(&var self) { print(\\\"drop {}\\\", self.n) }\\n }\\n \\ndiff --git a/examples/unsaferef_captured_by_closure.saw b/examples/unsaferef_captured_by_closure.saw\\nindex cb4d36cf..3c056b2d 100644\\n--- a/examples/unsaferef_captured_by_closure.saw\\n+++ b/examples/unsaferef_captured_by_closure.saw\\n@@ -50,7 +50,7 @@ func read_through(c: &var Counter) unsafe -> Int {\\n // and the mutation lands where the caller can see it.\\n func bump_through(c: &var Counter) unsafe -> Int {\\n     var h = mint(&var c)\\n-    run_int({ [move h] in h.deref().bump()  h.deref().n })\\n+    run_int({ [move h] in h.deref().bump(); h.deref().n })\\n }\\n \\n // A NESTED closure mints its own handle from the outer one, at a statement.\\ndiff --git a/examples/value_branch_valueless_arm_preserved.saw b/examples/value_branch_valueless_arm_preserved.saw\\nindex 85646226..14507832 100644\\n--- a/examples/value_branch_valueless_arm_preserved.saw\\n+++ b/examples/value_branch_valueless_arm_preserved.saw\\n@@ -15,7 +15,7 @@ func in_statement(flag: Bool) {\\n \\n // A DIVERGING sibling arm keeps the value `if` valid — the arm exits.\\n func with_early_return(flag: Bool) -> Int {\\n-    if flag { return 2 } else { let x = 7  x }\\n+    if flag { return 2 } else { let x = 7; x }\\n }\\n \\n func with_panic(flag: Bool) -> Int {\\ndiff --git a/examples/while_let_suspending.saw b/examples/while_let_suspending.saw\\nindex 6483e574..0294d73d 100644\\n--- a/examples/while_let_suspending.saw\\n+++ b/examples/while_let_suspending.saw\\n@@ -35,7 +35,7 @@ extension Cursor {\\n     }\\n }\\n \\n-func slow(n: Int) -> Int { yield_now()  return n }\\n+func slow(n: Int) -> Int { yield_now(); return n }\\n \\n func at_most(v: Int, cap: Int) sync -> Int? {\\n     if v > cap { return None }\\ndiff --git a/prototypes/minivm/examples/builders/reject_append_arity.saw b/prototypes/minivm/examples/builders/reject_append_arity.saw\\nindex 0f482144..7a6831f5 100644\\n--- a/prototypes/minivm/examples/builders/reject_append_arity.saw\\n+++ b/prototypes/minivm/examples/builders/reject_append_arity.saw\\n@@ -1,2 +1,4 @@\\n-func main() { var b = StringBuilder()\\n-try! b.append() }\\n+func main() {\\n+    var b = StringBuilder()\\n+    try! b.append()\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/reject_append_label.saw b/prototypes/minivm/examples/builders/reject_append_label.saw\\nindex 306c2e15..066e63d1 100644\\n--- a/prototypes/minivm/examples/builders/reject_append_label.saw\\n+++ b/prototypes/minivm/examples/builders/reject_append_label.saw\\n@@ -1,2 +1,4 @@\\n-func main() { var b = StringBuilder()\\n-try! b.append(value: \\\"x\\\") }\\n+func main() {\\n+    var b = StringBuilder()\\n+    try! b.append(value: \\\"x\\\")\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/reject_append_type.saw b/prototypes/minivm/examples/builders/reject_append_type.saw\\nindex b8cf11cc..fcb5832c 100644\\n--- a/prototypes/minivm/examples/builders/reject_append_type.saw\\n+++ b/prototypes/minivm/examples/builders/reject_append_type.saw\\n@@ -1,2 +1,4 @@\\n-func main() { var b = StringBuilder()\\n-try! b.append(true) }\\n+func main() {\\n+    var b = StringBuilder()\\n+    try! b.append(true)\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/reject_assignment.saw b/prototypes/minivm/examples/builders/reject_assignment.saw\\nindex e3270c6c..040065b8 100644\\n--- a/prototypes/minivm/examples/builders/reject_assignment.saw\\n+++ b/prototypes/minivm/examples/builders/reject_assignment.saw\\n@@ -1,3 +1,5 @@\\n-func main() { var a = StringBuilder()\\n-var b = StringBuilder()\\n-a = b }\\n+func main() {\\n+    var a = StringBuilder()\\n+    var b = StringBuilder()\\n+    a = b\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/reject_copy.saw b/prototypes/minivm/examples/builders/reject_copy.saw\\nindex aa90f9c8..83283061 100644\\n--- a/prototypes/minivm/examples/builders/reject_copy.saw\\n+++ b/prototypes/minivm/examples/builders/reject_copy.saw\\n@@ -1,2 +1,4 @@\\n-func main() { var a = StringBuilder()\\n-let b = a }\\n+func main() {\\n+    var a = StringBuilder()\\n+    let b = a\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/reject_nested_receiver.saw b/prototypes/minivm/examples/builders/reject_nested_receiver.saw\\nindex 3b1ce85b..34d83f9d 100644\\n--- a/prototypes/minivm/examples/builders/reject_nested_receiver.saw\\n+++ b/prototypes/minivm/examples/builders/reject_nested_receiver.saw\\n@@ -1,2 +1,4 @@\\n-func main() { var b = StringBuilder()\\n-try! b.append(s: b.build()) }\\n+func main() {\\n+    var b = StringBuilder()\\n+    try! b.append(s: b.build())\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/reject_shared_append.saw b/prototypes/minivm/examples/builders/reject_shared_append.saw\\nindex 0b9d89ce..5ef0a37f 100644\\n--- a/prototypes/minivm/examples/builders/reject_shared_append.saw\\n+++ b/prototypes/minivm/examples/builders/reject_shared_append.saw\\n@@ -1,3 +1,5 @@\\n func append_shared(b: &StringBuilder) { try! b.append(s: \\\"x\\\") }\\n-func main() { var b = StringBuilder()\\n-append_shared(&b) }\\n+func main() {\\n+    var b = StringBuilder()\\n+    append_shared(&b)\\n+}\\ndiff --git a/prototypes/minivm/examples/reject_type.saw b/prototypes/minivm/examples/reject_type.saw\\nindex 6490885b..99dd37a0 100644\\n--- a/prototypes/minivm/examples/reject_type.saw\\n+++ b/prototypes/minivm/examples/reject_type.saw\\n@@ -1 +1 @@\\n-func main() { let x: Bool = 1 print(x) }\\n+func main() { let x: Bool = 1; print(x) }\\ndiff --git a/prototypes/minivm/examples/vectors/evaluation_once.saw b/prototypes/minivm/examples/vectors/evaluation_once.saw\\nindex a9681b1e..82283c03 100644\\n--- a/prototypes/minivm/examples/vectors/evaluation_once.saw\\n+++ b/prototypes/minivm/examples/vectors/evaluation_once.saw\\n@@ -1,7 +1,11 @@\\n-func index() -> Int { print(\\\"index\\\")\\n-0 }\\n-func value() -> Int { print(\\\"value\\\")\\n-9 }\\n+func index() -> Int {\\n+    print(\\\"index\\\")\\n+    0\\n+}\\n+func value() -> Int {\\n+    print(\\\"value\\\")\\n+    9\\n+}\\n \\n func main() {\\n     var v = Vector<Int>()\\ndiff --git a/prototypes/minivm/examples/vectors/index_at_end.saw b/prototypes/minivm/examples/vectors/index_at_end.saw\\nindex ecb9fb4c..454ed6fa 100644\\n--- a/prototypes/minivm/examples/vectors/index_at_end.saw\\n+++ b/prototypes/minivm/examples/vectors/index_at_end.saw\\n@@ -1,3 +1,5 @@\\n-func main() { var v = Vector<Int>()\\n-try! v.push(1)\\n-print(v[1]) }\\n+func main() {\\n+    var v = Vector<Int>()\\n+    try! v.push(1)\\n+    print(v[1])\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/index_negative.saw b/prototypes/minivm/examples/vectors/index_negative.saw\\nindex a9752b76..d5162a61 100644\\n--- a/prototypes/minivm/examples/vectors/index_negative.saw\\n+++ b/prototypes/minivm/examples/vectors/index_negative.saw\\n@@ -1,2 +1,4 @@\\n-func main() { var v = Vector<Int>()\\n-print(v[-1]) }\\n+func main() {\\n+    var v = Vector<Int>()\\n+    print(v[-1])\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_assignment.saw b/prototypes/minivm/examples/vectors/reject_assignment.saw\\nindex 482968e6..8d0a031e 100644\\n--- a/prototypes/minivm/examples/vectors/reject_assignment.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_assignment.saw\\n@@ -1,3 +1,5 @@\\n-func main() { var a = Vector<Int>()\\n-let b = Vector<Int>()\\n-a = move b }\\n+func main() {\\n+    var a = Vector<Int>()\\n+    let b = Vector<Int>()\\n+    a = move b\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_borrow_after_move.saw b/prototypes/minivm/examples/vectors/reject_borrow_after_move.saw\\nindex 9370020f..bc1362cc 100644\\n--- a/prototypes/minivm/examples/vectors/reject_borrow_after_move.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_borrow_after_move.saw\\n@@ -1,3 +1,5 @@\\n-func main() { var a = Vector<Int>()\\n-let b = move a\\n-print(a.len()) }\\n+func main() {\\n+    var a = Vector<Int>()\\n+    let b = move a\\n+    print(a.len())\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_double_move.saw b/prototypes/minivm/examples/vectors/reject_double_move.saw\\nindex e85dd6d2..d9daea38 100644\\n--- a/prototypes/minivm/examples/vectors/reject_double_move.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_double_move.saw\\n@@ -1,3 +1,5 @@\\n-func main() { var a = Vector<Int>()\\n-let b = move a\\n-let c = move a }\\n+func main() {\\n+    var a = Vector<Int>()\\n+    let b = move a\\n+    let c = move a\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_get_label.saw b/prototypes/minivm/examples/vectors/reject_get_label.saw\\nindex 4f2086d7..bed3944c 100644\\n--- a/prototypes/minivm/examples/vectors/reject_get_label.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_get_label.saw\\n@@ -1,2 +1,4 @@\\n-func main() { let v = Vector<Int>()\\n-let x = v.get(value: 0) }\\n+func main() {\\n+    let v = Vector<Int>()\\n+    let x = v.get(value: 0)\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_implicit_copy.saw b/prototypes/minivm/examples/vectors/reject_implicit_copy.saw\\nindex 795f26f2..16e79ea3 100644\\n--- a/prototypes/minivm/examples/vectors/reject_implicit_copy.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_implicit_copy.saw\\n@@ -1,2 +1,4 @@\\n-func main() { let a = Vector<Int>()\\n-let b = a }\\n+func main() {\\n+    let a = Vector<Int>()\\n+    let b = a\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_index_type.saw b/prototypes/minivm/examples/vectors/reject_index_type.saw\\nindex 8b29fdb1..285e8106 100644\\n--- a/prototypes/minivm/examples/vectors/reject_index_type.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_index_type.saw\\n@@ -1,2 +1,4 @@\\n-func main() { let v = Vector<Int>()\\n-print(v[true]) }\\n+func main() {\\n+    let v = Vector<Int>()\\n+    print(v[true])\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_move_field.saw b/prototypes/minivm/examples/vectors/reject_move_field.saw\\nindex 8828d326..9fb005d9 100644\\n--- a/prototypes/minivm/examples/vectors/reject_move_field.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_move_field.saw\\n@@ -1,4 +1,6 @@\\n struct Box { values: Vector<Int> }\\n extension Box: NoCopy {}\\n-func main() { let b = Box(values: Vector<Int>())\\n-let v = move b.values }\\n+func main() {\\n+    let b = Box(values: Vector<Int>())\\n+    let v = move b.values\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_move_reference.saw b/prototypes/minivm/examples/vectors/reject_move_reference.saw\\nindex 0bea3c8d..294ffe7a 100644\\n--- a/prototypes/minivm/examples/vectors/reject_move_reference.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_move_reference.saw\\n@@ -1,3 +1,5 @@\\n func bad(v: &var Vector<Int>) { let x = move v }\\n-func main() { var v = Vector<Int>()\\n-bad(&var v) }\\n+func main() {\\n+    var v = Vector<Int>()\\n+    bad(&var v)\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_outer_loop_move.saw b/prototypes/minivm/examples/vectors/reject_outer_loop_move.saw\\nindex bc791879..a229d6d4 100644\\n--- a/prototypes/minivm/examples/vectors/reject_outer_loop_move.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_outer_loop_move.saw\\n@@ -1,4 +1,8 @@\\n-func main() { let v = Vector<Int>()\\n-var once = true\\n-while once { let x = move v\\n-once = false } }\\n+func main() {\\n+    let v = Vector<Int>()\\n+    var once = true\\n+    while once {\\n+        let x = move v\\n+        once = false\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_push_label.saw b/prototypes/minivm/examples/vectors/reject_push_label.saw\\nindex e59ee0cf..8e513486 100644\\n--- a/prototypes/minivm/examples/vectors/reject_push_label.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_push_label.saw\\n@@ -1,2 +1,4 @@\\n-func main() { var v = Vector<Int>()\\n-try! v.push(index: 1) }\\n+func main() {\\n+    var v = Vector<Int>()\\n+    try! v.push(index: 1)\\n+}\\ndiff --git a/prototypes/minivm/examples/vectors/reject_push_type.saw b/prototypes/minivm/examples/vectors/reject_push_type.saw\\nindex 6983066f..40f20b86 100644\\n--- a/prototypes/minivm/examples/vectors/reject_push_type.saw\\n+++ b/prototypes/minivm/examples/vectors/reject_push_type.saw\\n@@ -1,2 +1,4 @@\\n-func main() { var v = Vector<Int>()\\n-try! v.push(\\\"x\\\") }\\n+func main() {\\n+    var v = Vector<Int>()\\n+    try! v.push(\\\"x\\\")\\n+}\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex 16ff90ba..a07a9495 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -374,7 +374,7 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n     }\\n     var visited = Vector<Bool>()\\n     n = 0\\n-    while n < f.code.len() { try! visited.push(false) n = n + 1 }\\n+    while n < f.code.len() { try! visited.push(false); n = n + 1 }\\n     var pending = Vector<Int>()\\n     try! pending.push(0)\\n     while let pc = pending.pop() {\\ndiff --git a/sawc/parser/core.py b/sawc/parser/core.py\\nindex df3ce4e5..e6ab048e 100644\\n--- a/sawc/parser/core.py\\n+++ b/sawc/parser/core.py\\n@@ -345,6 +345,117 @@ class Parser(ExpressionsMixin, StatementsMixin, DeclarationsMixin, TypeParsingMi\\n         while self.match(TokenType.NEWLINE):\\n             self.advance()\\n \\n+    # ---- the statement-separator chokepoint (SL-347) -----------------------\\n+\\n+    # The tokens that may stand where a statement ENDS: a newline, the `;` that\\n+    # joins it to the next statement on the SAME line, the enclosing block's\\n+    # `}`, or end of file.\\n+    _STATEMENT_ENDERS = (TokenType.NEWLINE, TokenType.SEMICOLON,\\n+                         TokenType.RBRACE, TokenType.EOF)\\n+\\n+    SEPARATOR_NOT_TERMINATOR = (\\n+        \\\"a `;` joins two statements on one line; it does not end one — \\\"\\n+        \\\"remove it\\\")\\n+\\n+    DECLARATIONS_TAKE_A_NEWLINE = (\\n+        \\\"declarations are not joined by `;` — put each declaration on its own \\\"\\n+        \\\"line\\\")\\n+\\n+    JUXTAPOSITION_NEEDS_A_SEMICOLON = (\\n+        \\\"two statements on one line need a `;` between them\\\")\\n+\\n+    def expect_statement_end(self, previous_start: Optional[Token] = None, *,\\n+                             declarations: bool = False):\\n+        \\\"\\\"\\\"THE statement-separator chokepoint (SL-347).\\n+\\n+        Saw has no statement TERMINATORS. A newline separates two statements,\\n+        and a `;` separates two statements that share one line (R1) — so a `;`\\n+        that ends nothing is refused where it is written (R2): one sitting\\n+        before a newline, before the block's `}`, at end of file, doubled, or\\n+        standing at the start of a line.\\n+\\n+        Called once per GAP between statements, including the gap BEFORE a\\n+        list's first statement — there `previous_start` is None, nothing\\n+        precedes, and only the line-start half of R2 can fire. ENTRY POINTS,\\n+        all of them:\\n+\\n+          * `StatementsMixin.parse_block` — the one block-body funnel, which is\\n+            why this covers function and method bodies, `borrows` bodies,\\n+            `if`/`else`/`while`/`for` bodies, a `match` arm's BLOCK body, both\\n+            blocks of `try { } catch { }`, and `guard … else { }`.\\n+          * `ExpressionsMixin._parse_closure_body` — closure bodies.\\n+          * `Parser.parse` — top-level module scope, whose units are\\n+            DECLARATIONS: they take a newline and are never joined by a `;`\\n+            (`declarations=True`).\\n+\\n+        A `match` arm's BARE body ends at the arm's `,`, but that body is an\\n+        expression rather than a statement list, so it never reaches here —\\n+        `ExpressionsMixin.parse_match` owns that comma.\\n+\\n+        `at_statement_end` is the lookahead half of the same rule, for the\\n+        statement forms whose operand is optional.\\n+\\n+        R3 — bare JUXTAPOSITION — is refused here too: with a statement behind\\n+        it, this position takes a NEWLINE, a `;`, the block's `}` or EOF, and\\n+        anything else is a second statement that never said where the first\\n+        one ended.\\n+        \\\"\\\"\\\"\\n+        if previous_start is not None and self.match(TokenType.SEMICOLON):\\n+            semi = self.current()\\n+            self.advance()\\n+            # A `;` is a separator, so something must FOLLOW it on this line.\\n+            if self.match(*self._STATEMENT_ENDERS):\\n+                self.error_at(semi, self.SEPARATOR_NOT_TERMINATOR)\\n+            if declarations:\\n+                # It does separate — but the units at module scope are\\n+                # declarations, and those take a line each.\\n+                self.error_at(semi, self.DECLARATIONS_TAKE_A_NEWLINE)\\n+            return\\n+        if previous_start is not None and not self.at_statement_end():\\n+            self._juxtaposition_error(previous_start, declarations)\\n+        self.skip_newlines()\\n+        if self.match(TokenType.SEMICOLON):\\n+            self.error(self.SEPARATOR_NOT_TERMINATOR)\\n+\\n+    def _statement_head_text(self, token: Token) -> str:\\n+        \\\"\\\"\\\"How a statement's FIRST token is quoted back to its author.\\\"\\\"\\\"\\n+        if token.type in (TokenType.STRING, TokenType.INTERP_STRING):\\n+            return '\\\"…\\\"'\\n+        return token.value if token.value else token.type.name.lower()\\n+\\n+    def _juxtaposition_error(self, previous_start: Token, declarations: bool):\\n+        \\\"\\\"\\\"R3's refusal, anchored at the SECOND statement's first token.\\n+\\n+        The separator is missing exactly where the second statement begins, so\\n+        that is where the caret goes; the message names BOTH statements' first\\n+        tokens, because on a crowded line the reader's question is which two\\n+        the compiler ran together.\\n+        \\\"\\\"\\\"\\n+        nxt = self.current()\\n+        first = self._statement_head_text(previous_start)\\n+        second = self._statement_head_text(nxt)\\n+        if declarations:\\n+            self.error_at(\\n+                nxt,\\n+                f\\\"two declarations on one line: the first begins with \\\"\\n+                f\\\"`{first}`, the second with `{second}` — \\\"\\n+                f\\\"{self.DECLARATIONS_TAKE_A_NEWLINE}\\\")\\n+        self.error_at(\\n+            nxt,\\n+            f\\\"{self.JUXTAPOSITION_NEEDS_A_SEMICOLON}: the first begins with \\\"\\n+            f\\\"`{first}`, the second with `{second}` — write `;` where they \\\"\\n+            f\\\"meet\\\")\\n+\\n+    def at_statement_end(self) -> bool:\\n+        \\\"\\\"\\\"True if the current token could END the statement being parsed.\\n+\\n+        The lookahead half of `expect_statement_end` (SL-347), for the four\\n+        statement forms that carry an OPTIONAL operand and so have to ask\\n+        whether anything is left on the line: `return`, `break`, the closure\\n+        body's `return`, and `lend` (which refuses the empty case outright).\\n+        \\\"\\\"\\\"\\n+        return self.match(*self._STATEMENT_ENDERS)\\n+\\n     def match_ident(self, value: str) -> bool:\\n         \\\"\\\"\\\"Check if current token is IDENT with the given value (for context-sensitive keywords).\\\"\\\"\\\"\\n         return self.current().type == TokenType.IDENT and self.current().value == value\\n@@ -1052,10 +1163,15 @@ class Parser(ExpressionsMixin, StatementsMixin, DeclarationsMixin, TypeParsingMi\\n         # claimed here, before the first declaration is parsed. A `//!` anywhere\\n         # else stays unclaimed and is reported by the sweep below.\\n         program.module_doc = self._take_module_docs()\\n+        # SL-347: module scope is a statement list too — a `;` may not stand at\\n+        # the head of a line here either.\\n+        self.expect_statement_end()\\n \\n         while not self.match(TokenType.EOF):\\n             try:\\n+                decl_start = self.current()\\n                 self._parse_toplevel_decl(program)\\n+                self.expect_statement_end(decl_start, declarations=True)\\n             except SyntaxError as e:\\n                 # Batch syntax errors: record this one, then synchronize to the\\n                 # next top-level declaration and keep parsing so a single file\\n@@ -1275,9 +1391,13 @@ class Parser(ExpressionsMixin, StatementsMixin, DeclarationsMixin, TypeParsingMi\\n         self.expect_ident(\\\"module\\\")\\n         name_token = self.expect(TokenType.IDENT, \\\"Expected module name\\\")\\n \\n-        # Check for inline module: module name { ... }\\n+        # Check for inline module: module name { ... }. Peek across newlines\\n+        # and REWIND when no `{` follows (SL-347): a bare `module name`\\n+        # declaration ends at its newline, and eating it here would leave the\\n+        # next declaration looking as if it shared this one's line.\\n         is_inline = False\\n         body = None\\n+        brace_peek = self.pos\\n         self.skip_newlines()\\n         if self.match(TokenType.LBRACE):\\n             is_inline = True\\n@@ -1294,6 +1414,8 @@ class Parser(ExpressionsMixin, StatementsMixin, DeclarationsMixin, TypeParsingMi\\n                 self.skip_newlines()\\n \\n             self.expect(TokenType.RBRACE)\\n+        else:\\n+            self.pos = brace_peek\\n \\n         return ModuleDecl(\\n             name=name_token.value,\\ndiff --git a/sawc/parser/expressions.py b/sawc/parser/expressions.py\\nindex 4a5babe4..594e6ceb 100644\\n--- a/sawc/parser/expressions.py\\n+++ b/sawc/parser/expressions.py\\n@@ -1100,6 +1100,11 @@ class ExpressionsMixin:\\n             then_branch = self.parse_block()\\n \\n             else_branch = None\\n+            # Peek across newlines for the `else`, and REWIND when none\\n+            # follows (SL-347): the newline after the then-branch's `}` is the\\n+            # statement's own terminator, and eating it here would leave the\\n+            # next statement looking as if it shared this one's line.\\n+            else_peek = self.pos\\n             self.skip_newlines()\\n             if self.match(TokenType.ELSE):\\n                 self.advance()\\n@@ -1116,6 +1121,8 @@ class ExpressionsMixin:\\n                     )\\n                 else:\\n                     else_branch = self.parse_block()\\n+            else:\\n+                self.pos = else_peek\\n \\n             return IfLetExpr(\\n                 name=iflet_name,\\n@@ -1137,6 +1144,9 @@ class ExpressionsMixin:\\n         then_branch = self.parse_block()\\n \\n         else_branch = None\\n+        # Same rewind as the `if let` arm above (SL-347): a peek must not eat\\n+        # the newline that ends this statement.\\n+        else_peek = self.pos\\n         self.skip_newlines()\\n         if self.match(TokenType.ELSE):\\n             self.advance()\\n@@ -1153,6 +1163,8 @@ class ExpressionsMixin:\\n                 )\\n             else:\\n                 else_branch = self.parse_block()\\n+        else:\\n+            self.pos = else_peek\\n \\n         return IfExpr(\\n             condition=condition,\\n@@ -1411,7 +1423,10 @@ class ExpressionsMixin:\\n         expr = self.parse_expression()\\n         self.allow_trailing_closure = saved_trailing\\n \\n-        # Check for inline catch\\n+        # Check for inline catch — peeking across newlines, and REWINDING when\\n+        # no `catch` follows (SL-347), so a bare `try f()` keeps the newline\\n+        # that ends its statement.\\n+        catch_peek = self.pos\\n         self.skip_newlines()\\n         if self.match(TokenType.CATCH):\\n             self.advance()\\n@@ -1425,6 +1440,7 @@ class ExpressionsMixin:\\n                 line=start.line,\\n                 column=start.column\\n             )\\n+        self.pos = catch_peek\\n \\n         return TryExpr(\\n             expr=expr,\\n@@ -1850,16 +1866,17 @@ class ExpressionsMixin:\\n         final_expr = None\\n         start = self.current()\\n \\n+        # SL-347: every gap between the body's statements runs through the\\n+        # separator chokepoint, this first call covering the gap before the\\n+        # first one.\\n+        self.expect_statement_end()\\n         while not self.match(TokenType.RBRACE) and not self.match(TokenType.EOF):\\n-            self.skip_newlines()\\n-            if self.match(TokenType.RBRACE):\\n-                break\\n-\\n             # Try to parse a statement\\n+            stmt_start = self.current()\\n             stmt = self._parse_closure_statement()\\n             if stmt:\\n                 statements.append(stmt)\\n-            self.skip_newlines()\\n+            self.expect_statement_end(stmt_start)\\n \\n         # Check if last statement is expression (implicit return)\\n         if statements and isinstance(statements[-1], ExpressionStatement):\\n@@ -1883,7 +1900,7 @@ class ExpressionsMixin:\\n             start = self.current()\\n             self.advance()\\n             value = None\\n-            if not self.match(TokenType.NEWLINE) and not self.match(TokenType.RBRACE):\\n+            if not self.at_statement_end():\\n                 value = self.parse_expression()\\n             return ReturnStatement(value=value, line=start.line, column=start.column)\\n         elif self.match(TokenType.WHILE):\\ndiff --git a/sawc/parser/statements.py b/sawc/parser/statements.py\\nindex 1ee18769..1d644c46 100644\\n--- a/sawc/parser/statements.py\\n+++ b/sawc/parser/statements.py\\n@@ -44,15 +44,19 @@ class StatementsMixin:\\n     def parse_block(self) -> Block:\\n         start = self.current()\\n         self.expect(TokenType.LBRACE)\\n-        self.skip_newlines()\\n+        # SL-347: the block's statement gaps all run through the separator\\n+        # chokepoint, this first call covering the gap before the first\\n+        # statement (it skips the newlines `skip_newlines` used to).\\n+        self.expect_statement_end()\\n \\n         statements = []\\n         final_expr = None\\n \\n         while not self.match(TokenType.RBRACE, TokenType.EOF):\\n+            stmt_start = self.current()\\n             stmt = self.parse_statement()\\n             statements.append(stmt)\\n-            self.skip_newlines()\\n+            self.expect_statement_end(stmt_start)\\n \\n         self.expect(TokenType.RBRACE)\\n \\n@@ -344,7 +348,7 @@ class StatementsMixin:\\n         \\\"\\\"\\\"\\n         start = self.advance()  # consume 'lend'\\n \\n-        if self.match(TokenType.NEWLINE, TokenType.RBRACE, TokenType.EOF):\\n+        if self.at_statement_end():\\n             self.error(\\\"`lend` needs a place to lend — write `lend \\\"\\n                        \\\"self.buffer[i]` or `lend self.field`\\\")\\n \\n@@ -359,7 +363,7 @@ class StatementsMixin:\\n         start = self.advance()  # consume return\\n \\n         value = None\\n-        if not self.match(TokenType.NEWLINE, TokenType.RBRACE, TokenType.EOF):\\n+        if not self.at_statement_end():\\n             value = self.parse_expression()\\n \\n         return ReturnStatement(\\n@@ -523,7 +527,7 @@ class StatementsMixin:\\n \\n         # Check if there's a value to break with\\n         value = None\\n-        if not self.match(TokenType.NEWLINE, TokenType.RBRACE, TokenType.EOF):\\n+        if not self.at_statement_end():\\n             value = self.parse_expression()\\n \\n         return BreakStatement(\\ndiff --git a/sawc/std/json.saw b/sawc/std/json.saw\\nindex ed47d310..30598991 100644\\n--- a/sawc/std/json.saw\\n+++ b/sawc/std/json.saw\\n@@ -1496,7 +1496,7 @@ extension JsonValue {\\n                     // `write_text`, and `v` is only borrowed by `_write`.\\n                     match enc.write_text(k) {\\n                         case Ok(_) -> {},\\n-                        case Err(e) -> { first_err = e  return }\\n+                        case Err(e) -> { first_err = e; return }\\n                     }\\n                     match v._write(enc: &var enc) {\\n                         case Ok(_) -> {},\\ndiff --git a/sawc/std/taskgroup.saw b/sawc/std/taskgroup.saw\\nindex 0905ecbd..471c8ff8 100644\\n--- a/sawc/std/taskgroup.saw\\n+++ b/sawc/std/taskgroup.saw\\n@@ -1186,7 +1186,7 @@ extension TaskGroup {\\n         // the `Void` task's terms.\\n         let detached = self.cells.with_ref<Bool>(slot, { c in c!.__is_detached() })\\n         if detached {\\n-            let _ = self.cells.with_var_ref<Bool>(slot, { c in c!.__drop_result()  true })\\n+            let _ = self.cells.with_var_ref<Bool>(slot, { c in c!.__drop_result(); true })\\n         }\\n         var keeps = false\\n         if not detached {\\n@@ -1208,9 +1208,9 @@ extension TaskGroup {\\n     func __detach_slot(&var self, slot: Int, generation: Int) unsafe {\\n         self.__lock()\\n         if not self.__stale(slot, generation) {\\n-            let _ = self.cells.with_var_ref<Bool>(slot, { c in c!.__detach_now()  true })\\n+            let _ = self.cells.with_var_ref<Bool>(slot, { c in c!.__detach_now(); true })\\n             if self.done.get(slot)! {\\n-                let _ = self.cells.with_var_ref<Bool>(slot, { c in c!.__drop_result()  true })\\n+                let _ = self.cells.with_var_ref<Bool>(slot, { c in c!.__drop_result(); true })\\n                 self.__recycle(slot)\\n             }\\n         }\\n@@ -1226,7 +1226,7 @@ extension TaskGroup {\\n         while i < self.tasks.len() {\\n             let d = self.done.get(i)!\\n             if not d {\\n-                let _ = self.cells.with_var_ref<Bool>(i, { c in c!.__cancel_now()  true })\\n+                let _ = self.cells.with_var_ref<Bool>(i, { c in c!.__cancel_now(); true })\\n             }\\n             i = i + 1\\n         }\\ndiff --git a/tools/test_transfer_decisions.py b/tools/test_transfer_decisions.py\\nindex 21c48399..814c0652 100644\\n--- a/tools/test_transfer_decisions.py\\n+++ b/tools/test_transfer_decisions.py\\n@@ -470,7 +470,7 @@ func main() {\\n     (\\n         \\\"generic_body_defers_to_specialization\\\",\\n         \\\"\\\"\\\"\\n-func hold<T>(x: T) -> Int { let a = x  1 }\\n+func hold<T>(x: T) -> Int { let a = x; 1 }\\n \\n func main() { print(hold(5)) }\\n \\\"\\\"\\\",\\n\",\"id\":\"SL-347\",\"request_id\":\"pid-60030-1790139796-8000\",\"title\":\"SL-347 (RULED Sep 22): semicolon is the one-line statement separator (never a terminator), bare statement juxtaposition is REFUSED with a fixit, keyword lookaheads restore the statement-ending newline, the corpus sweep (244 .saw sites + 27 doc fences, R5 reformats, no EXPECT changed), 4 matrix pins, spec/skill/README (the brace-body layout rule); battery 32/32\"}","request_id":"pid-60030-1790139796-8000","sequence":1228,"timestamp":"1790139797"}
---
