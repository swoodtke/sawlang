---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791237","description":"## Scope and status\n\nDF-153c — a fixed-width backed enum costs two casts at a word-wide seam. Not a bug; the datum the brief asked the sweep to produce. SysError is UInt8 because its…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reconcile the linked brief and recorded rulings with current consumers. Keep already-landed behavior and stated deferrals explicit.\n2. Draft the remaining contract: supported syntax/API, ownership and failure behavior, alternatives, compatibility effects and the exact questions still requiring a decision.\n3. Define small implementation units and consumer migrations, then specify the positive, negative and boundary tests before dispatch.\n\n## Acceptance criteria\n\n- [ ] The design names a concrete consumer or retains its recorded revisit trigger.\n- [ ] Existing rulings are preserved; unresolved choices and a recommended option are explicit.\n- [ ] The proposed API/semantics, migration scope and acceptance tests are reviewable. Drafting this plan does not mark an unruled design approved.\n\n## Existing design references\n\n- [sawlang/designs/47-platform-int.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/47-platform-int.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 6068–6090\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L6068-L6090)\n\n- **DF‑153c — a fixed-width backed enum costs two casts at a word-wide\n  seam.** Not a bug; the datum the brief asked the sweep to produce.\n  `SysError` is `UInt8` because its numbers are ABI (design 47), but every\n  seam that carries one types it `word`, so the use site reads\n  ```saw\n  if r == 0 - ((SysError.WouldBlock as UInt8) as Int) { ... }\n  ```\n  against the old `if r == -SYS_WOULD_BLOCK`. std.file's `OpenMode` already\n  paid this (`(mode as UInt8) as Int`) at one site; the rt net ops pay it at\n  a dozen. The same shape appears at every `Atomic<Int>` state machine —\n  `LockState`/`Once.State` project with `as Int` per touch — which is what a\n  hypothetical enum-typed `Atomic<E>` would remove. Two candidate answers if\n  it is ever worth a design: a widening projection that reads the enum\n  straight to the wider integer (one `as`), or `Atomic<E>` over a raw-backed\n  enum. Recorded, not proposed.\n\n### The sweep's census (what converted, and why the rest did not)\n\nCONVERTED: SpinLock `UNLOCKED`/`HELD`; std.file `SEEK_*`; the rt-side\n`SYS_*` tag space in all four rt modules; the spawn redirection bits on\nboth sides of the seam; sosrt's two abort codes.\n\nNOT CONVERTED, by reason:\n","id":"SL-97","labels":["todo-import","needs-verification","design","plan","design-proposal","backlog"],"priority":"normal","project":"SL","revision":1,"sequence":97,"status":"open","title":"DF-153c — a fixed-width backed enum costs two casts at a word-wide seam. Not a bug; the datum the brief asked the sweep to produce. SysError is UInt8 because its…","updated":"1788791237"},"before":null,"id":"SL-97","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-153c — a fixed-width backed enum costs two casts at a word-wide seam. Not a bug; the datum the brief asked the sweep to produce. SysError is UInt8 because its…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reconcile the linked brief and recorded rulings with current consumers. Keep already-landed behavior and stated deferrals explicit.\\n2. Draft the remaining contract: supported syntax/API, ownership and failure behavior, alternatives, compatibility effects and the exact questions still requiring a decision.\\n3. Define small implementation units and consumer migrations, then specify the positive, negative and boundary tests before dispatch.\\n\\n## Acceptance criteria\\n\\n- [ ] The design names a concrete consumer or retains its recorded revisit trigger.\\n- [ ] Existing rulings are preserved; unresolved choices and a recommended option are explicit.\\n- [ ] The proposed API/semantics, migration scope and acceptance tests are reviewable. Drafting this plan does not mark an unruled design approved.\\n\\n## Existing design references\\n\\n- [sawlang/designs/47-platform-int.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/47-platform-int.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 6068–6090\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L6068-L6090)\\n\\n- **DF‑153c — a fixed-width backed enum costs two casts at a word-wide\\n  seam.** Not a bug; the datum the brief asked the sweep to produce.\\n  `SysError` is `UInt8` because its numbers are ABI (design 47), but every\\n  seam that carries one types it `word`, so the use site reads\\n  ```saw\\n  if r == 0 - ((SysError.WouldBlock as UInt8) as Int) { ... }\\n  ```\\n  against the old `if r == -SYS_WOULD_BLOCK`. std.file's `OpenMode` already\\n  paid this (`(mode as UInt8) as Int`) at one site; the rt net ops pay it at\\n  a dozen. The same shape appears at every `Atomic<Int>` state machine —\\n  `LockState`/`Once.State` project with `as Int` per touch — which is what a\\n  hypothetical enum-typed `Atomic<E>` would remove. Two candidate answers if\\n  it is ever worth a design: a widening projection that reads the enum\\n  straight to the wider integer (one `as`), or `Atomic<E>` over a raw-backed\\n  enum. Recorded, not proposed.\\n\\n### The sweep's census (what converted, and why the rest did not)\\n\\nCONVERTED: SpinLock `UNLOCKED`/`HELD`; std.file `SEEK_*`; the rt-side\\n`SYS_*` tag space in all four rt modules; the spawn redirection bits on\\nboth sides of the seam; sosrt's two abort codes.\\n\\nNOT CONVERTED, by reason:\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"design\",\"plan\",\"design-proposal\",\"backlog\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-e58f9a28dd54061cba16-create\",\"title\":\"DF-153c — a fixed-width backed enum costs two casts at a word-wide seam. Not a bug; the datum the brief asked the sweep to produce. SysError is UInt8 because its…\"}","request_id":"todo-20260907-e58f9a28dd54061cba16-create","sequence":97,"timestamp":"1788791237"}
---
