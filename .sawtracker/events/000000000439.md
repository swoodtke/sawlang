---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-246.p1 (revision 1): minivm M3: scalar control flow + module constants (branch commit cda40e2d)","created":"1789088597","id":"c1"}],"created":"1789086328","description":"SL-242 epic child, landing patch 4/14. Contract: prototypes/minivm/M3_SCALAR_CONTROL.md. Patch = branch commit cda40e2d.","id":"SL-246","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":439,"stage":"queued","status":"open","title":"minivm M3: scalar control — short-circuit &&/||, compound assignment, module static constants","updated":"1789088597"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086328","description":"SL-242 epic child, landing patch 4/14. Contract: prototypes/minivm/M3_SCALAR_CONTROL.md. Patch = branch commit cda40e2d.","id":"SL-246","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":408,"stage":"queued","status":"open","title":"minivm M3: scalar control — short-circuit &&/||, compound assignment, module static constants","updated":"1789086328"},"id":"SL-246","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/DESIGN.md b/prototypes/minivm/DESIGN.md\\nindex 0949eaf8..0a4d420a 100644\\n--- a/prototypes/minivm/DESIGN.md\\n+++ b/prototypes/minivm/DESIGN.md\\n@@ -18,6 +18,9 @@ must fail consistently. This does not claim the prototype can compile itself.\\n The original Int32-only contract below records the baseline.\\n [M1_NUMBERS.md](M1_NUMBERS.md) supersedes its numeric types, operations, default\\n literal type, and signed-minimum remainder behavior.\\n+[M2_RECORDS.md](M2_RECORDS.md) adds value records and flattened call/result layouts.\\n+[M3_SCALAR_CONTROL.md](M3_SCALAR_CONTROL.md) adds short-circuit operators, compound\\n+assignments and module constants.\\n \\n ## Source contract\\n \\ndiff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 472b74d0..f64f2a68 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -5,7 +5,8 @@ Scope: the source to admit is `selfhost/lexer/src/lib.saw` (1,215 lines at the\\n support. A feature is included only when that file uses it, or when it is an\\n unavoidable implementation dependency of one of the public std operations it\\n calls. This inventory was taken at the 43-case Int32/Bool baseline. M1 now supplies\\n-the numeric foundation (91 total cases); M2 is adding scalar value records.\\n+the numeric foundation; M2 supplies scalar value records, and M3 adds module\\n+constants, compound assignment and short-circuit Booleans (136 total cases).\\n Strings, collections, references, generics and methods remain outstanding.\\n The dependency groups below are an inventory, not the implementation commit order;\\n the numbered milestone designs define each isolated slice.\\ndiff --git a/prototypes/minivm/M3_SCALAR_CONTROL.md b/prototypes/minivm/M3_SCALAR_CONTROL.md\\nindex 649b470d..5d1b5ca0 100644\\n--- a/prototypes/minivm/M3_SCALAR_CONTROL.md\\n+++ b/prototypes/minivm/M3_SCALAR_CONTROL.md\\n@@ -1,6 +1,6 @@\\n # M3: scalar frontend support used by lexer helpers\\n \\n-Status: design, after M2. This slice needs no new VM or LLVM opcodes. It admits\\n+Status: implemented after M2; 136-case integration gate passes. This slice needs no new VM or LLVM opcodes. It admits\\n short-circuit Boolean expressions, compound arithmetic assignment, and immutable\\n module scalar constants. Tail expressions and value-producing if/match remain\\n separate subsequent frontend slices; do not silently broaden this contract.\\n@@ -25,7 +25,10 @@ separate subsequent frontend slices; do not silently broaden this contract.\\n   rules. No dynamic initialization, global mutable storage or record constants.\\n   Constants may be referenced from any function irrespective of declaration order.\\n   Reject duplicate module names and assignment to a constant. Local bindings may\\n-  shadow constants. Accept `public static` syntactically; modules remain later.\\n+  shadow constants; tests use visible refinement (`let C = C - 1`) as required\\n+  by Saw design 100. The prototype retains the baseline's broader shadowing\\n+  acceptance; enforcing design 100 is deferred to scope/ownership completion.\\n+  Accept `public static` syntactically; modules remain later.\\n \\n ## Lowering and ownership\\n \\ndiff --git a/prototypes/minivm/M4_ENUMS.md b/prototypes/minivm/M4_ENUMS.md\\nnew file mode 100644\\nindex 00000000..ba134c57\\n--- /dev/null\\n+++ b/prototypes/minivm/M4_ENUMS.md\\n@@ -0,0 +1,74 @@\\n+# M4: payload-free enums and exhaustive statement match\\n+\\n+Status: design, after M3. This isolates the TokenKind representation and dispatch\\n+required by the lexer. Value-producing match, implicit tails, strings, and payload\\n+enums remain later slices. No new instruction opcode is required.\\n+\\n+## Source contract\\n+\\n+Accept private/public enums with one or more named, payload-free cases using Saw\\n+syntax `enum Color { case Red, case Green }`. Qualified case values `Color.Red`\\n+are nominally typed. They can be copied, assigned, passed, returned, stored in\\n+records, and compared for equality/inequality only with the same declared enum.\\n+Reject duplicate enum/case names, module namespace collisions, numeric casts,\\n+arithmetic, implicit integer conversion, and direct printing. There are no raw\\n+values, payloads, enum methods, or user-defined discriminants in this slice.\\n+\\n+Accept statement `match subject { case Red -> { ... }, case Green -> { ... } }`\\n+where subject has enum type. Patterns may be unqualified cases or `Color.Red`;\\n+qualified patterns must name the subject's declared enum. A final `_` wildcard is\\n+allowed. Require exhaustive coverage without duplicates; reject arms following a\\n+wildcard. Each arm is a statement block for M4; it can contain explicit returns.\\n+A function whose exhaustive match arms all return satisfies return checking.\\n+Evaluate subject exactly once. Execute exactly one arm. Typecheck every arm.\\n+\\n+Record and function type references may refer forward to enums. Case expressions\\n+may refer to enums declared later in the file. Local values do not introduce\\n+unqualified enum cases into ordinary expression lookup.\\n+\\n+## Shared representation\\n+\\n+Add `ValueType.Enum(index: Int)`, `EnumIR { name: String, cases: Vector<String>,\\n+line: Int, col: Int }`, and `Program.enums`. A value occupies one scalar slot;\\n+its canonical immediate/cell is the case's zero-based ordinal. Enum identity stays\\n+in slot types, function signatures and record fields. No integer helper should\\n+classify enums as integers. `scalar_type` includes enums so layout/copy/call ABI\\n+remain unchanged, while printing still accepts only numeric and Bool values.\\n+\\n+The record layout routine does not dereference enum metadata: enums flatten to\\n+one word. Full Program verification must separately validate enum type indices\\n+in every record field, signature and slot; validate enum declarations as nonempty\\n+with unique names/cases; and enforce ordinal bounds for enum Const instructions.\\n+Keep nominal equality checks. Malformed enum indices and constants must return a\\n+ProtoError before any indexing. Record type validation and layout cycle/width\\n+checks remain intact. Direct contract tests must cover these trust boundaries.\\n+\\n+Both engines use existing i64 cells and Eq/Ne. LLVM returns enums as scalar i64,\\n+including enum fields flattened inside record results. Do not emit enum values\\n+as record aggregates. VM/native dispatch uses existing Const/Eq/Branch/Jump.\\n+\\n+## Frontend lowering\\n+\\n+Extend declaration-name collection to enumerate both struct and enum names before\\n+resolving types; retain M3 newline-terminated statics handling. Record/enum/function/\\n+static names share the module namespace. Resolve qualified enum cases before\\n+ordinary field access without changing numeric static-member parsing.\\n+\\n+For match, snapshot the subject once, then emit a test chain with canonical enum\\n+constants and Eq. Each true branch enters its arm; false continues to the next\\n+test. Every non-returning arm jumps to a common continuation. A final wildcard is\\n+an unconditional arm. The frontend proves coverage; the verifier still checks all\\n+branch targets and ordinary reachable-falloff rules. Existing final padding must\\n+provide real targets for end-of-function joins, including all-returning matches.\\n+\\n+## Independent tests\\n+\\n+Keep prior gates. Add small cases for qualified values, nominal equality,\\n+assignment, enum parameters/results, enum fields in records, forward declarations,\\n+qualified and unqualified exhaustive matches, wildcard selection, subject evaluation\\n+once with a marker function, and all-arms explicit-return control flow. Include\\n+located failures for duplicate declaration/case, missing case, duplicate arm,\\n+wrong enum qualifier, mixed-enum equality/transfer, integer conversion and print.\\n+Run VM and clang O0/O2 with independent expected outputs; compare one representative\\n+source fixture with the production compiler. Primary owns schema/verifier and\\n+review, Sol owns bounded frontend and fixture tasks. Commit only passing integration.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex f16d817a..6e399085 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -45,7 +45,8 @@ func main() {\\n Single-file programs support signed and unsigned 8/16/32/64-bit integers,\\n `Int`/`UInt` (64-bit), `Byte`, `Bool`, functions, positional calls,\\n recursion, `let`/`var`, assignment, lexical scopes, statement `if`/`else` and\\n-`while`, explicit returns, and scalar printing. Named structs can contain scalars or nested structs, with\\n+`while`, explicit returns, and scalar printing. Named structs can contain scalars\\n+or nested structs, with\\n field access, mutable fields, value copying, and function parameters/results.\\n Record layouts are limited to 256 words and 128 nesting levels; see\\n [M2_RECORDS.md](M2_RECORDS.md). Arithmetic is checked; signed\\n@@ -61,7 +62,11 @@ Newlines separate statements. Omit the result annotation for Void functions;\\n `main()` must take no arguments and return Void. Value-returning functions need\\n explicit returns. There are no imports, strings, collections, references,\\n generics, methods, implicit value tails, or concurrency in the accepted subset.\\n-Semicolons and logical `&&`/`||` are also outside this initial subset.\\n+Semicolons remain unsupported. Boolean `&&`/`||` short-circuit, mutable integers\\n+and fields support `+=`/`-=`/`*=`/`/=`/`%=`, and module `static` integer/Bool\\n+constants accept literal and numeric-limit initializers. See\\n+[M3_SCALAR_CONTROL.md](M3_SCALAR_CONTROL.md) for this frontend slice.\\n+The prototype still accepts broader lexical shadowing than Saw's design-100 rule.\\n \\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n@@ -75,16 +80,18 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The numeric and record milestones pass\\n-all 115 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The first three milestones\\n+pass all 136 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw` and\\n `tests/record_contract.saw`; build and run them with the Python compiler.\\n-Use `--section numbers` or `--section records` for an isolated integration gate.\\n+Use `--section numbers`, `--section records`, or `--section control` for an isolated\\n+integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\n schema and semantics. [LEXER_DEPENDENCIES.md](LEXER_DEPENDENCIES.md) tracks the\\n-remaining dependencies needed to compile the existing lexer with this prototype. Source is\\n+remaining dependencies needed to compile the existing lexer with this prototype.\\n+[M4_ENUMS.md](M4_ENUMS.md) designs the next isolated slice. Source is\\n split into `frontend.saw`, `model.saw`, `verify.saw`, `vm.saw`, `llvm.saw`, and\\n `main.saw`. The verifier checks structural indices and types; initialization\\n is established by the frontend, so this is not a loader for untrusted bytecode.\\ndiff --git a/prototypes/minivm/examples/control/compound_div_zero.saw b/prototypes/minivm/examples/control/compound_div_zero.saw\\nnew file mode 100644\\nindex 00000000..ee79fbe9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/compound_div_zero.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    var value: UInt64 = UInt64.max\\n+    value /= 0\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/compound_locals.saw b/prototypes/minivm/examples/control/compound_locals.saw\\nnew file mode 100644\\nindex 00000000..c1cefc15\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/compound_locals.saw\\n@@ -0,0 +1,13 @@\\n+func main() {\\n+    var value: Int16 = 100\\n+    value += 20\\n+    print(value)\\n+    value -= 5\\n+    print(value)\\n+    value *= 2\\n+    print(value)\\n+    value /= 3\\n+    print(value)\\n+    value %= 7\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/compound_nested_fields.saw b/prototypes/minivm/examples/control/compound_nested_fields.saw\\nnew file mode 100644\\nindex 00000000..c78bdd11\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/compound_nested_fields.saw\\n@@ -0,0 +1,17 @@\\n+struct Inner { value: UInt16 }\\n+struct Outer { inner: Inner, untouched: Int }\\n+\\n+func main() {\\n+    var item = Outer(untouched: 99, inner: Inner(value: 50))\\n+    item.inner.value += 10\\n+    print(item.inner.value)\\n+    item.inner.value -= 5\\n+    print(item.inner.value)\\n+    item.inner.value *= 3\\n+    print(item.inner.value)\\n+    item.inner.value /= 4\\n+    print(item.inner.value)\\n+    item.inner.value %= 9\\n+    print(item.inner.value)\\n+    print(item.untouched)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/compound_overflow.saw b/prototypes/minivm/examples/control/compound_overflow.saw\\nnew file mode 100644\\nindex 00000000..25e6341d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/compound_overflow.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    var value: Int8 = Int8.max\\n+    value += 1\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/compound_rem_overflow.saw b/prototypes/minivm/examples/control/compound_rem_overflow.saw\\nnew file mode 100644\\nindex 00000000..120d4fe3\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/compound_rem_overflow.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    var x: Int8 = -128\\n+    x %= -1\\n+    print(x)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/logical_loops.saw b/prototypes/minivm/examples/control/logical_loops.saw\\nnew file mode 100644\\nindex 00000000..6a49007a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/logical_loops.saw\\n@@ -0,0 +1,15 @@\\n+func check(i: Int) -> Bool {\\n+    print(i)\\n+    return true\\n+}\\n+func increment() -> Int {\\n+    print(9)\\n+    return 1\\n+}\\n+func main() {\\n+    var i = 0\\n+    while i < 3 && check(i) { i += 1 }\\n+    print(i)\\n+    i += increment()\\n+    print(i)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/precedence.saw b/prototypes/minivm/examples/control/precedence.saw\\nnew file mode 100644\\nindex 00000000..915023f0\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/precedence.saw\\n@@ -0,0 +1,8 @@\\n+func main() {\\n+    print(true || false && false)\\n+    print(false || true && false)\\n+    print((false || true) && false)\\n+    print(true && (false || true))\\n+    print((3 < 4) && (5 == 5) || false)\\n+    print(false || (1 | 2) == 3 && true)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/reject_assigned_static.saw b/prototypes/minivm/examples/control/reject_assigned_static.saw\\nnew file mode 100644\\nindex 00000000..dd092515\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/reject_assigned_static.saw\\n@@ -0,0 +1,4 @@\\n+static LIMIT: Int = 1\\n+func main() {\\n+    LIMIT += 1\\n+}\\ndiff --git a/prototypes/minivm/examples/control/reject_duplicate_static.saw b/prototypes/minivm/examples/control/reject_duplicate_static.saw\\nnew file mode 100644\\nindex 00000000..49fc6b26\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/reject_duplicate_static.saw\\n@@ -0,0 +1,3 @@\\n+static LIMIT: Int = 1\\n+static LIMIT: Int = 2\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/control/reject_dynamic_static.saw b/prototypes/minivm/examples/control/reject_dynamic_static.saw\\nnew file mode 100644\\nindex 00000000..08f466db\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/reject_dynamic_static.saw\\n@@ -0,0 +1,2 @@\\n+static LIMIT: Int = 1 + 2\\n+func main() { print(LIMIT) }\\ndiff --git a/prototypes/minivm/examples/control/reject_immutable_compound.saw b/prototypes/minivm/examples/control/reject_immutable_compound.saw\\nnew file mode 100644\\nindex 00000000..ea5f42e8\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/reject_immutable_compound.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let value: Int = 1\\n+    value += 1\\n+}\\ndiff --git a/prototypes/minivm/examples/control/reject_mixed_compound.saw b/prototypes/minivm/examples/control/reject_mixed_compound.saw\\nnew file mode 100644\\nindex 00000000..febb6ad9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/reject_mixed_compound.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    var small: Int8 = 1\\n+    let wide: Int16 = 2\\n+    small += wide\\n+}\\ndiff --git a/prototypes/minivm/examples/control/reject_skipped_rhs_type.saw b/prototypes/minivm/examples/control/reject_skipped_rhs_type.saw\\nnew file mode 100644\\nindex 00000000..e7206dad\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/reject_skipped_rhs_type.saw\\n@@ -0,0 +1,3 @@\\n+func main() {\\n+    print(false && 1)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/reject_static_function_collision.saw b/prototypes/minivm/examples/control/reject_static_function_collision.saw\\nnew file mode 100644\\nindex 00000000..55314be8\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/reject_static_function_collision.saw\\n@@ -0,0 +1,3 @@\\n+static collision: Int = 1\\n+func collision() {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/control/reject_static_negative_range.saw b/prototypes/minivm/examples/control/reject_static_negative_range.saw\\nnew file mode 100644\\nindex 00000000..63523fac\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/reject_static_negative_range.saw\\n@@ -0,0 +1,2 @@\\n+static X: Int = -9223372036854775809\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/control/reject_static_typed_narrowing.saw b/prototypes/minivm/examples/control/reject_static_typed_narrowing.saw\\nnew file mode 100644\\nindex 00000000..c9b61cdc\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/reject_static_typed_narrowing.saw\\n@@ -0,0 +1,2 @@\\n+static X: UInt8 = 1u16\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/control/short_circuit_markers.saw b/prototypes/minivm/examples/control/short_circuit_markers.saw\\nnew file mode 100644\\nindex 00000000..fac19d5d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/short_circuit_markers.saw\\n@@ -0,0 +1,13 @@\\n+func mark(label: Int, value: Bool) -> Bool {\\n+    print(label)\\n+    return value\\n+}\\n+\\n+func main() {\\n+    print(false && mark(1, true))\\n+    print(true || mark(2, false))\\n+    print(true && mark(3, true))\\n+    print(false || mark(4, false))\\n+    print(mark(5, false) && mark(6, true))\\n+    print(mark(7, true) || mark(8, false))\\n+}\\ndiff --git a/prototypes/minivm/examples/control/short_circuit_traps.saw b/prototypes/minivm/examples/control/short_circuit_traps.saw\\nnew file mode 100644\\nindex 00000000..3c54fcaa\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/short_circuit_traps.saw\\n@@ -0,0 +1,6 @@\\n+func main() {\\n+    print(false && (1 / 0 == 0))\\n+    print(true || (1 / 0 == 0))\\n+    print(false && (Int8.max + 1 == 0))\\n+    print(true || (Int8.max + 1 == 0))\\n+}\\ndiff --git a/prototypes/minivm/examples/control/static_literals.saw b/prototypes/minivm/examples/control/static_literals.saw\\nnew file mode 100644\\nindex 00000000..4b27837d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/static_literals.saw\\n@@ -0,0 +1,10 @@\\n+static SMALL: Int8 = -128\\n+static WIDE: Int64 = 32767i16\\n+static OCTET: UInt8 = 255\\n+static FULL: UInt64 = 18446744073709551615\\n+func main() {\\n+    print(SMALL)\\n+    print(WIDE)\\n+    print(OCTET)\\n+    print(FULL)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/statics.saw b/prototypes/minivm/examples/control/statics.saw\\nnew file mode 100644\\nindex 00000000..fc66b449\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/statics.saw\\n@@ -0,0 +1,19 @@\\n+func read_forward() {\\n+    print(FORWARD)\\n+    print(BIG)\\n+    print(ENABLED)\\n+    print(LOW)\\n+}\\n+\\n+public static BIG: UInt64 = UInt64.max\\n+static ENABLED: Bool = true\\n+static LOW: Int = Int.min\\n+static FORWARD: UInt32 = UInt32.max\\n+static NEGATIVE: Int16 = -32768\\n+\\n+func main() {\\n+    read_forward()\\n+    print(NEGATIVE)\\n+    let BIG: UInt64 = BIG - 1\\n+    print(BIG)\\n+}\\ndiff --git a/prototypes/minivm/examples/control/truth_tables.saw b/prototypes/minivm/examples/control/truth_tables.saw\\nnew file mode 100644\\nindex 00000000..42574aeb\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/control/truth_tables.saw\\n@@ -0,0 +1,10 @@\\n+func main() {\\n+    print(false && false)\\n+    print(false && true)\\n+    print(true && false)\\n+    print(true && true)\\n+    print(false || false)\\n+    print(false || true)\\n+    print(true || false)\\n+    print(true || true)\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 440ac841..8f9464b8 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -33,6 +33,14 @@ struct Place {\\n     mutable: Bool\\n }\\n \\n+struct StaticBinding {\\n+    name: String\\n+    ty: ValueType\\n+    value: Int\\n+    line: Int\\n+    col: Int\\n+}\\n+\\n struct ExprValue {\\n     ty: ValueType\\n     slot: Int\\n@@ -87,6 +95,7 @@ func literal_magnitude(value: String) -> UInt? {\\n struct Compiler {\\n     tokens: Vector<Token>\\n     records: Vector<RecordIR>\\n+    statics: Vector<StaticBinding>\\n     signatures: Vector<Signature>\\n     pos: Int\\n     depth: Int\\n@@ -153,6 +162,15 @@ extension Compiler {\\n         -1\\n     }\\n \\n+    func static_index(&self, name: String) -> Int {\\n+        var i = 0\\n+        while i < self.statics.len() {\\n+            if self.statics[i].name == name { return i }\\n+            i += 1\\n+        }\\n+        -1\\n+    }\\n+\\n     func display_type(&self, ty: ValueType) -> String {\\n         match ty {\\n             case Record(index) -> {\\n@@ -178,11 +196,23 @@ extension Compiler {\\n                 let _ = self.take()\\n                 let name = try self.expect(TokenKind.Ident, \\\"expected record name\\\")\\n                 if self.reserved_type_name(name.value) || name.value == \\\"print\\\" { return problem(\\\"record name `{name.value}` is reserved\\\", name.line, name.col) }\\n-                if self.record_index(name.value) >= 0 { return problem(\\\"duplicate record `{name.value}`\\\", name.line, name.col) }\\n+                if self.record_index(name.value) >= 0 || self.static_index(name.value) >= 0 { return problem(\\\"duplicate module name `{name.value}`\\\", name.line, name.col) }\\n                 try! self.records.push(RecordIR(name: name.value, field_names: Vector<String>(),\\n                                                 field_types: Vector<ValueType>(), line: name.line, col: name.col))\\n+            } else if self.at(TokenKind.Static) {\\n+                let _ = self.take()\\n+                let name = try self.expect(TokenKind.Ident, \\\"expected static name\\\")\\n+                if name.value == \\\"print\\\" || self.reserved_type_name(name.value) { return problem(\\\"static name `{name.value}` is reserved\\\", name.line, name.col) }\\n+                if self.record_index(name.value) >= 0 || self.static_index(name.value) >= 0 {\\n+                    return problem(\\\"duplicate module name `{name.value}`\\\", name.line, name.col)\\n+                }\\n+                try! self.statics.push(StaticBinding(name: name.value, ty: ValueType.Void,\\n+                                                     value: 0, line: name.line, col: name.col))\\n+                while not self.at(TokenKind.Newline) && not self.at(TokenKind.Eof) { let _ = self.take() }\\n+                self.skip_newlines()\\n+                continue\\n             } else if not self.at(TokenKind.Func) {\\n-                return self.fail(\\\"only function and record declarations are supported at module scope\\\")\\n+                return self.fail(\\\"only function, record, and static declarations are supported at module scope\\\")\\n             }\\n             while not self.at(TokenKind.LBrace) && not self.at(TokenKind.Eof) { let _ = self.take() }\\n             if self.at(TokenKind.Eof) { return self.fail(\\\"expected declaration body\\\") }\\n@@ -218,7 +248,12 @@ extension Compiler {\\n                 self.skip_newlines()\\n                 continue\\n             }\\n-            let ft = try self.expect(TokenKind.Func, \\\"only function and record declarations are supported at module scope\\\")\\n+            if self.at(TokenKind.Static) {\\n+                try self.scan_static()\\n+                self.skip_newlines()\\n+                continue\\n+            }\\n+            let ft = try self.expect(TokenKind.Func, \\\"only function, record, and static declarations are supported at module scope\\\")\\n             let name = try self.expect(TokenKind.Ident, \\\"expected function name\\\")\\n             if name.value == \\\"print\\\" {\\n                 return problem(\\\"`print` is reserved by the prototype\\\", name.line, name.col)\\n@@ -229,6 +264,9 @@ extension Compiler {\\n             if self.record_index(name.value) >= 0 {\\n                 return problem(\\\"function `{name.value}` conflicts with a record name\\\", name.line, name.col)\\n             }\\n+            if self.static_index(name.value) >= 0 {\\n+                return problem(\\\"function `{name.value}` conflicts with a static name\\\", name.line, name.col)\\n+            }\\n             let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after function name\\\")\\n             var pnames = Vector<String>()\\n             var ptypes = Vector<ValueType>()\\n@@ -299,6 +337,95 @@ extension Compiler {\\n         return\\n     }\\n \\n+    func static_integer_value(&self, magnitude: UInt, negative: Bool, bare: Bool,\\n+                              source: ValueType, target: ValueType,\\n+                              t: &Token) -> Result<Int, ProtoError> {\\n+        if not integer_type(target) || target == ValueType.Byte {\\n+            return problem(\\\"static integer initializer requires an integer type\\\", t.line, t.col)\\n+        }\\n+        if negative && not integer_signed(source) { return problem(\\\"cannot negate an unsigned integer\\\", t.line, t.col) }\\n+        var bits = Int.from(truncating: magnitude)\\n+        if negative {\\n+            if magnitude == UInt.from(truncating: Int.max) + 1 { bits = Int.min }\\n+            else { bits = 0 - bits }\\n+        }\\n+        bits = integer_normalize(bits, source)\\n+        if bare {\\n+            let probe = ExprValue(ty: source, slot: -1, call: false, literal: true,\\n+                                  literal_value: magnitude, literal_negative: negative)\\n+            if not self.literal_fits_target(&probe, target) {\\n+                return problem(\\\"integer literal does not fit {type_name(target)}\\\", t.line, t.col)\\n+            }\\n+            return integer_normalize(bits, target)\\n+        }\\n+        if source != target && not integer_widens(source, target) {\\n+            return problem(\\\"cannot implicitly convert {type_name(source)} to {type_name(target)}\\\", t.line, t.col)\\n+        }\\n+        integer_normalize(bits, target)\\n+    }\\n+\\n+    func scan_static(&var self) -> Result<Void, ProtoError> {\\n+        let _ = self.take()\\n+        let name = try self.expect(TokenKind.Ident, \\\"expected static name\\\")\\n+        let si = self.static_index(name.value)\\n+        if si < 0 { return problem(\\\"unknown static declaration `{name.value}`\\\", name.line, name.col) }\\n+        let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after static name\\\")\\n+        let target = try self.parse_type()\\n+        if target == ValueType.Byte || not scalar_type(target) {\\n+            return problem(\\\"static type must be an integer or Bool\\\", name.line, name.col)\\n+        }\\n+        let _ = try self.expect(TokenKind.Assign, \\\"static declaration requires an initializer\\\")\\n+        var value = 0\\n+        if self.at(TokenKind.True_) || self.at(TokenKind.False_) {\\n+            let lit = self.take()\\n+            if target != ValueType.Bool { return problem(\\\"Bool static initializer requires Bool type\\\", lit.line, lit.col) }\\n+            value = if lit.kind == TokenKind.True_ { 1 } else { 0 }\\n+        } else {\\n+            var negative = false\\n+            var anchor = self.token()\\n+            if self.at(TokenKind.Minus) {\\n+                negative = true\\n+                anchor = self.take()\\n+            }\\n+            if self.at(TokenKind.IntLit) {\\n+                let lit = self.take()\\n+                guard let magnitude = literal_magnitude(lit.value) else { return problem(\\\"invalid integer literal\\\", lit.line, lit.col) }\\n+                var source = ValueType.I64\\n+                var bare = true\\n+                if let suffix = lit.suffix {\\n+                    bare = false\\n+                    guard let st = suffix_type(suffix) else { return problem(\\\"unsupported integer suffix `{suffix}`\\\", lit.line, lit.col) }\\n+                    source = st\\n+                }\\n+                if not bare {\\n+                    let limit = if negative && integer_signed(source) {\\n+                        UInt.from(truncating: integer_max(source)) + 1\\n+                    } else { UInt.from(truncating: integer_max(source)) }\\n+                    if magnitude > limit { return problem(\\\"integer literal does not fit {type_name(source)}\\\", lit.line, lit.col) }\\n+                }\\n+                value = try self.static_integer_value(magnitude, negative, bare, source, target, &anchor)\\n+            } else if not negative && self.at(TokenKind.Ident) {\\n+                let type_token = self.take()\\n+                guard let source = named_integer_type(type_token.value) else { return problem(\\\"unsupported static initializer\\\", type_token.line, type_token.col) }\\n+                if source == ValueType.Byte { return problem(\\\"unsupported static initializer\\\", type_token.line, type_token.col) }\\n+                let _ = try self.expect(TokenKind.Dot, \\\"unsupported static initializer\\\")\\n+                let member = try self.expect(TokenKind.Ident, \\\"unsupported static initializer\\\")\\n+                if member.value != \\\"min\\\" && member.value != \\\"max\\\" { return problem(\\\"unsupported static initializer\\\", member.line, member.col) }\\n+                let bits = if member.value == \\\"min\\\" { integer_min(source) } else { integer_max(source) }\\n+                if target != source && not integer_widens(source, target) {\\n+                    return problem(\\\"cannot implicitly convert {type_name(source)} to {type_name(target)}\\\", type_token.line, type_token.col)\\n+                }\\n+                value = integer_normalize(bits, target)\\n+            } else { return problem(\\\"unsupported static initializer\\\", anchor.line, anchor.col) }\\n+        }\\n+        if not self.at(TokenKind.Newline) && not self.at(TokenKind.Eof) {\\n+            return self.fail(\\\"unsupported static initializer expression\\\")\\n+        }\\n+        self.statics[si].ty = target\\n+        self.statics[si].value = value\\n+        return\\n+    }\\n+\\n     func scan_record(&var self) -> Result<Void, ProtoError> {\\n         let _ = self.take()\\n         let name = try self.expect(TokenKind.Ident, \\\"expected record name\\\")\\n@@ -530,9 +657,19 @@ extension Compiler {\\n             }\\n             if self.at(TokenKind.LParen) { return self.parse_call(name) }\\n             let bi = self.binding_index(name.value)\\n-            if bi < 0 { return problem(\\\"unknown name `{name.value}`\\\", name.line, name.col) }\\n-            let value = expr_value(self.bindings[bi].ty, self.bindings[bi].slot, false)\\n-            return self.snapshot(value, &name)\\n+            if bi >= 0 {\\n+                let value = expr_value(self.bindings[bi].ty, self.bindings[bi].slot, false)\\n+                return self.snapshot(value, &name)\\n+            }\\n+            let si = self.static_index(name.value)\\n+            if si >= 0 {\\n+                let dst = self.new_slot(self.statics[si].ty)\\n+                let at = self.emit_at(Op.Const, name.line, name.col)\\n+                self.code[at].dst = dst\\n+                self.code[at].immediate = self.statics[si].value\\n+                return expr_value(self.statics[si].ty, dst, false)\\n+            }\\n+            return problem(\\\"unknown name `{name.value}`\\\", name.line, name.col)\\n         }\\n         self.fail(\\\"expected expression; this syntax is unsupported\\\")\\n     }\\n@@ -550,13 +687,18 @@ extension Compiler {\\n         if not self.at(TokenKind.Ident) { return false }\\n         var i = self.pos + 1\\n         while i + 1 < self.tokens.len() && self.tokens[i].kind == TokenKind.Dot && self.tokens[i + 1].kind == TokenKind.Ident { i += 2 }\\n-        i < self.tokens.len() && self.tokens[i].kind == TokenKind.Assign\\n+        if i >= self.tokens.len() { return false }\\n+        let kind = self.tokens[i].kind\\n+        kind == TokenKind.Assign || kind == TokenKind.PlusAssign || kind == TokenKind.MinusAssign || kind == TokenKind.StarAssign || kind == TokenKind.SlashAssign || kind == TokenKind.PercentAssign\\n     }\\n \\n     func parse_place(&var self) -> Result<(Token, Place), ProtoError> {\\n         let name = try self.expect(TokenKind.Ident, \\\"expected assignment target\\\")\\n         let bi = self.binding_index(name.value)\\n-        if bi < 0 { return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col) }\\n+        if bi < 0 {\\n+            if self.static_index(name.value) >= 0 { return problem(\\\"cannot assign to immutable static `{name.value}`\\\", name.line, name.col) }\\n+            return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col)\\n+        }\\n         var place = Place(ty: self.bindings[bi].ty, slot: self.bindings[bi].slot,\\n                           mutable: self.bindings[bi].mutable)\\n         while self.at(TokenKind.Dot) {\\n@@ -1014,7 +1156,7 @@ extension Compiler {\\n         left\\n     }\\n \\n-    func parse_expr(&var self) -> Result<ExprValue, ProtoError> {\\n+    func parse_bit_or(&var self) -> Result<ExprValue, ProtoError> {\\n         var left = try self.parse_bit_xor()\\n         self.expr_space()\\n         while self.at(TokenKind.Pipe) {\\n@@ -1032,6 +1174,58 @@ extension Compiler {\\n         left\\n     }\\n \\n+    func parse_logical_and(&var self) -> Result<ExprValue, ProtoError> {\\n+        var left = try self.parse_bit_or()\\n+        self.expr_space()\\n+        while self.at(TokenKind.And) {\\n+            let t = self.take()\\n+            if left.ty != ValueType.Bool { return problem(\\\"logical operator requires Bool operands\\\", t.line, t.col) }\\n+            let dst = self.new_slot(ValueType.Bool)\\n+            let initial = self.emit_at(Op.Const, t.line, t.col)\\n+            self.code[initial].dst = dst\\n+            self.code[initial].immediate = 0\\n+            let branch = self.emit_at(Op.Branch, t.line, t.col)\\n+            self.code[branch].a = left.slot\\n+            let skip = self.emit_at(Op.Jump, t.line, t.col)\\n+            self.code[branch].target = self.code.len()\\n+            self.expr_space()\\n+            let right = try self.parse_bit_or()\\n+            self.expr_space()\\n+            if right.ty != ValueType.Bool { return problem(\\\"logical operator requires Bool operands\\\", t.line, t.col) }\\n+            let copy = self.emit_at(Op.Copy, t.line, t.col)\\n+            self.code[copy].dst = dst\\n+            self.code[copy].a = right.slot\\n+            self.code[skip].target = self.code.len()\\n+            left = expr_value(ValueType.Bool, dst, false)\\n+        }\\n+        left\\n+    }\\n+\\n+    func parse_expr(&var self) -> Result<ExprValue, ProtoError> {\\n+        var left = try self.parse_logical_and()\\n+        self.expr_space()\\n+        while self.at(TokenKind.Or) {\\n+            let t = self.take()\\n+            if left.ty != ValueType.Bool { return problem(\\\"logical operator requires Bool operands\\\", t.line, t.col) }\\n+            let dst = self.new_slot(ValueType.Bool)\\n+            let initial = self.emit_at(Op.Const, t.line, t.col)\\n+            self.code[initial].dst = dst\\n+            self.code[initial].immediate = 1\\n+            let branch = self.emit_at(Op.Branch, t.line, t.col)\\n+            self.code[branch].a = left.slot\\n+            self.expr_space()\\n+            let right = try self.parse_logical_and()\\n+            self.expr_space()\\n+            if right.ty != ValueType.Bool { return problem(\\\"logical operator requires Bool operands\\\", t.line, t.col) }\\n+            let copy = self.emit_at(Op.Copy, t.line, t.col)\\n+            self.code[copy].dst = dst\\n+            self.code[copy].a = right.slot\\n+            self.code[branch].target = self.code.len()\\n+            left = expr_value(ValueType.Bool, dst, false)\\n+        }\\n+        left\\n+    }\\n+\\n     func parse_block(&var self) -> Result<Bool, ProtoError> {\\n         if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n         self.depth += 1\\n@@ -1147,10 +1341,17 @@ extension Compiler {\\n             let parsed = try self.parse_place()\\n             let name = parsed.0\\n             let place = parsed.1\\n-            let _ = self.take()\\n+            let assign = self.take()\\n             if not place.mutable { return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col) }\\n             var value = try self.parse_expr()\\n-            value = try self.transfer(value, place.ty, &name)\\n+            if assign.kind != TokenKind.Assign {\\n+                if not arithmetic_type(place.ty) { return problem(\\\"compound assignment requires an arithmetic integer destination\\\", assign.line, assign.col) }\\n+                if value.literal { value = try self.transfer(value, place.ty, &assign) }\\n+                else if value.ty != place.ty { return problem(\\\"compound assignment operands must have the same type\\\", assign.line, assign.col) }\\n+                let op = if assign.kind == TokenKind.PlusAssign { Op.Add } else if assign.kind == TokenKind.MinusAssign { Op.Sub } else if assign.kind == TokenKind.StarAssign { Op.Mul } else if assign.kind == TokenKind.SlashAssign { Op.Div } else { Op.Rem }\\n+                let left = expr_value(place.ty, place.slot, false)\\n+                value = try self.binary(left, value, op, place.ty, &assign)\\n+            } else { value = try self.transfer(value, place.ty, &name) }\\n             value = try self.snapshot(value, &name)\\n             try self.emit_copy_value(place.slot, value.slot, place.ty, &name)\\n             return false\\n@@ -1221,7 +1422,8 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         case Ok(ts) -> move ts,\\n         case Err(e) -> { return problem(e.message, e.line, e.col) }\\n     }\\n-    var compiler = Compiler(tokens: move tokens, records: Vector<RecordIR>(), signatures: Vector<Signature>(), pos: 0, depth: 0,\\n+    var compiler = Compiler(tokens: move tokens, records: Vector<RecordIR>(), statics: Vector<StaticBinding>(),\\n+                            signatures: Vector<Signature>(), pos: 0, depth: 0,\\n                             expr_depth: 0, paren_depth: 0,\\n                             bindings: Vector<Binding>(), slots: Vector<ValueType>(),\\n                             code: Vector<Instruction>(), current_result: ValueType.Void)\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex c851afb8..6c05d783 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -35,6 +35,9 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"control/static_literals\\\", \\\"-128\\\\n32767\\\\n255\\\\n18446744073709551615\\\\n\\\", section=\\\"control\\\"),\\n+    RunCase(\\\"control/logical_loops\\\", \\\"0\\\\n1\\\\n2\\\\n3\\\\n9\\\\n4\\\\n\\\", section=\\\"control\\\"),\\n+    RunCase(\\\"control/compound_rem_overflow\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"control\\\"),\\n     RunCase(\\\"numbers/unary_cast_order\\\", \\\"-127\\\\n18446744073709551615\\\\n-1\\\\n\\\", section=\\\"numbers\\\"),\\n     RunCase(\\\"numbers/unary_cast_range\\\", \\\"runtime error: integer cast out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n     RunCase(\\\"numbers/unary_cast_widen\\\", \\\"128\\\\n\\\", section=\\\"numbers\\\"),\\n@@ -107,9 +110,21 @@ RUN_CASES = (\\n     RunCase(\\\"records/one_word_result\\\", \\\"18446744073709551615\\\\n\\\", section=\\\"records\\\"),\\n     RunCase(\\\"records/forward_and_numeric\\\", \\\"-128\\\\n255\\\\n65535\\\\n\\\", section=\\\"records\\\"),\\n     RunCase(\\\"records/width_256\\\", \\\"0\\\\n255\\\\n17\\\\n255\\\\n\\\", section=\\\"records\\\"),\\n+    RunCase(\\\"control/truth_tables\\\", \\\"false\\\\nfalse\\\\nfalse\\\\ntrue\\\\nfalse\\\\ntrue\\\\ntrue\\\\ntrue\\\\n\\\", section=\\\"control\\\"),\\n+    RunCase(\\\"control/precedence\\\", \\\"true\\\\nfalse\\\\nfalse\\\\ntrue\\\\ntrue\\\\ntrue\\\\n\\\", section=\\\"control\\\"),\\n+    RunCase(\\\"control/short_circuit_markers\\\", \\\"false\\\\ntrue\\\\n3\\\\ntrue\\\\n4\\\\nfalse\\\\n5\\\\nfalse\\\\n7\\\\ntrue\\\\n\\\", section=\\\"control\\\"),\\n+    RunCase(\\\"control/short_circuit_traps\\\", \\\"false\\\\ntrue\\\\nfalse\\\\ntrue\\\\n\\\", section=\\\"control\\\"),\\n+    RunCase(\\\"control/compound_locals\\\", \\\"120\\\\n115\\\\n230\\\\n76\\\\n6\\\\n\\\", section=\\\"control\\\"),\\n+    RunCase(\\\"control/compound_nested_fields\\\", \\\"60\\\\n55\\\\n165\\\\n41\\\\n5\\\\n99\\\\n\\\", section=\\\"control\\\"),\\n+    RunCase(\\\"control/statics\\\", \\\"4294967295\\\\n18446744073709551615\\\\ntrue\\\\n-9223372036854775808\\\\n-32768\\\\n18446744073709551614\\\\n\\\", section=\\\"control\\\"),\\n+    RunCase(\\\"control/compound_overflow\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"control\\\"),\\n+    RunCase(\\\"control/compound_div_zero\\\", \\\"runtime error: division by zero\\\\n\\\", 1, \\\"control\\\"),\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"control/reject_static_negative_range\\\", \\\"integer literal\\\", \\\"control\\\"),\\n+    RejectCase(\\\"control/reject_static_typed_narrowing\\\", \\\"convert\\\", \\\"control\\\"),\\n+    RejectCase(\\\"control/reject_static_function_collision\\\", \\\"conflicts\\\", \\\"control\\\"),\\n     RejectCase(\\\"records/reject_builtin_name\\\", \\\"reserved\\\", \\\"records\\\"),\\n     RejectCase(\\\"records/reject_record_equality\\\", \\\"scalar\\\", \\\"records\\\"),\\n     RejectCase(\\\"records/reject_record_print\\\", \\\"print\\\", \\\"records\\\"),\\n@@ -126,7 +141,7 @@ REJECT_CASES = (\\n     RejectCase(\\\"reject_semicolon\\\", \\\"semicolons are not supported\\\"),\\n     RejectCase(\\\"reject_string\\\", \\\"expected expression\\\"),\\n     RejectCase(\\\"reject_bool_arithmetic\\\", \\\"arithmetic requires integer operands\\\"),\\n-    RejectCase(\\\"reject_import\\\", \\\"only function and record declarations are supported\\\"),\\n+    RejectCase(\\\"reject_import\\\", \\\"only function, record, and static declarations are supported\\\"),\\n     RejectCase(\\\"reject_scope_leak\\\", \\\"unknown name\\\"),\\n     RejectCase(\\\"reject_unary_depth\\\", \\\"expression nesting limit exceeded\\\"),\\n     RejectCase(\\\"reject_reserved_print\\\", \\\"reserved\\\"),\\n@@ -149,8 +164,14 @@ REJECT_CASES = (\\n     RejectCase(\\\"records/reject_direct_cycle\\\", \\\"recursive\\\", \\\"records\\\"),\\n     RejectCase(\\\"records/reject_indirect_cycle\\\", \\\"recursive\\\", \\\"records\\\"),\\n     RejectCase(\\\"records/reject_width_257\\\", \\\"256\\\", \\\"records\\\"),\\n-    RejectCase(\\\"records/reject_duplicate_record\\\", \\\"duplicate record\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_duplicate_record\\\", \\\"duplicate module name\\\", \\\"records\\\"),\\n     RejectCase(\\\"records/reject_duplicate_field\\\", \\\"duplicate record field\\\", \\\"records\\\"),\\n+    RejectCase(\\\"control/reject_skipped_rhs_type\\\", \\\"logical operator requires Bool operands\\\", \\\"control\\\"),\\n+    RejectCase(\\\"control/reject_immutable_compound\\\", \\\"cannot assign to immutable local\\\", \\\"control\\\"),\\n+    RejectCase(\\\"control/reject_mixed_compound\\\", \\\"compound assignment operands must have the same type\\\", \\\"control\\\"),\\n+    RejectCase(\\\"control/reject_duplicate_static\\\", \\\"duplicate module name\\\", \\\"control\\\"),\\n+    RejectCase(\\\"control/reject_assigned_static\\\", \\\"cannot assign to immutable static\\\", \\\"control\\\"),\\n+    RejectCase(\\\"control/reject_dynamic_static\\\", \\\"unsupported static initializer\\\", \\\"control\\\"),\\n )\\n \\n \\n@@ -235,7 +256,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -264,7 +285,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\") else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\n\",\"id\":\"SL-246\",\"request_id\":\"minivm-SL-246-cda40e2d\",\"title\":\"minivm M3: scalar control flow + module constants (branch commit cda40e2d)\"}","request_id":"minivm-SL-246-cda40e2d","sequence":439,"timestamp":"1789088597"}
---
