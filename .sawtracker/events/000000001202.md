---
{"action":"comment","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawtracker","closed":"","comments":[{"author":"agent:claude-sawlang","body":"Closes with SL-350 (RULED Sep 22): the frame is zero-filled then written in place, never built as an insertvalue chain — SL-336's repro is one of SL-350's acceptance pins.","created":"1790117231","id":"c1"}],"created":"1790019368","description":"## Summary\n\n`sawc` aborts inside LLVM (no diagnostic, no source location) when a coroutine frame type flattens to more than 65535 scalar values. Found while getting sawtracker to compile against sawlang main (sawtracker ST-37); reached as soon as the SL-329 and SL-215 refusals were worked around.\n\n```\nAssertion failed: (NumValues == VTs.NumVTs && \"NumValues wasn't wide enough for its operands!\"), function SDNode, file SelectionDAGNodes.h, line 1220\nAbort trap: 6\n```\n\nmacOS crash report path: `LLVMTargetMachineEmitToMemory` → `SelectionDAGISel` → `SelectionDAGBuilder::visitInsertValue` → `SelectionDAG::getMergeValues` → `SelectionDAG::getNode` → `llvm::SDNode::SDNode`. Reproduces at every optimization level, `-O0` included.\n\n## Mechanism\n\nThe transform initialises an embedded sub-frame at its call site by building the whole frame as an SSA aggregate: an `insertvalue` chain from `undef`, one instruction per field (visible with `--emit-ir`):\n\n```\n%.7516 = insertvalue %\"__Frame_mutate_patch$m$src_store\" undef, %\"UnsafeRef…\" %.278, 0\n%.7517 = insertvalue %\"__Frame_mutate_patch$m$src_store\" %.7516, %\"UnsafeRef…\" %.279, 1\n...\n```\n\n`visitInsertValue` flattens the aggregate with `ComputeValueVTs` and builds ONE SDNode carrying that many result values, and `SDNode::NumValues` is a 16-bit field. Two things make the limit reachable in ordinary code:\n\n- a fixed-size array in a frame costs one value per element: std's `Command.output` keeps a `{ i1, [1024 x i8] }` pipe-drain chunk buffer across its suspension, so 1025 of that frame's 1080 values are one buffer;\n- frames grow by CALL SITE, not by callee: each call site of a suspending function embeds its own copy of the callee's frame, transitively. Seven `remove_worktree(repo, wt)` cleanup calls in one function meant seven `git`+`rm` frame pairs at ~3900 bytes each.\n\nMeasured with `--emit-frame-layout` on the failing sawtracker tree: `__Frame_mutate_patch` 114,170 values (203,728 bytes); `__Frame_dispatch`, `__Frame_response`, `__Frame_run_store` ≈ 130,600 values. The same source under `f982f54c` (the last compiler sawtracker built with): `__Frame_run_store` 28,422, `__Frame_mutate_patch` 14,136, and `apply_patch_branch` / `merge_patch_branch` / `rebase_patch_branch` had no frames at all. The 4.6x growth is SL-280's fix (imported free calls really get frames) plus the SL-318 epic.\n\n## Reproducer\n\nSawtracker itself. Take sawtracker main after ST-37.p1 merged (`a19e2fb`) and back out the call-site reductions its SAW_NOTES entry ST-SAW-16 lists (restore the per-arm `apply_patch_branch` calls in `mutate_patch`, the separate `remove_worktree` calls in the three patch-branch functions, and the two-child `remove_worktree`), then `sawc src/main.saw`. Or take sawtracker at `0b10cdd`, replace the `Thread.spawn` worker in `test_runner` with a direct `run_test_job(&job)` call, and convert the seven inline `... catch { remove_worktree(repo, wt); return ... }` sites in store.saw to the block form — that is the exact tree that produced the crash reports on 2026-09-21 13:44–13:52 EDT.\n\nA standalone minimization was attempted five times and FAILED, which is itself the useful finding: `[Int8; 70000]` in a frame; 8×8 and 64×1 call sites of a helper holding `[Int8; 1024]`; 8 sites holding `[Int8; 8192]`; the same with `String` locals; the same with a `&var` borrow parameter. All built frames over the limit and all compiled, because when every inserted field is a zero constant instcombine folds the chain into a `memset` before SelectionDAG sees it. sawtracker survives to the abort because its chain has non-constant operands (pointers and live locals). A reproducer needs non-constant insertvalue operands over 65535 flattened values.\n\n## Suggested fixes (compiler side, any one suffices)\n\n- initialise embedded sub-frames through memory (`alloca` + per-field `store`, or `memset` + stores) rather than an SSA aggregate chain;\n- or split oversized aggregates so no single `insertvalue` result exceeds 65535 leaves;\n- or at minimum diagnose the condition (`ComputeValueVTs` count > 65535) with a source-located error instead of an LLVM assert.\n\nAlso worth noting: the per-call-site frame embedding means a 1 KiB fixed buffer in a std frame (`Command.output`) multiplies into every caller's frame; sharing a callee frame across call sites, or keeping large fixed buffers out of frames, would remove the pressure entirely.\n\n## Impact\n\nAny program with a large driven call tree that shells out at several sites. sawtracker's workaround was to reduce suspending call sites (SAW_NOTES ST-SAW-16), which is fragile: the next upstream change that grows frames puts it back over the limit.\n","id":"SL-336","labels":["bug","compiler","codegen","coroutine"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":2,"sequence":1202,"stage":"queued","status":"open","title":"LLVM codegen aborts (SDNode NumValues overflow in visitInsertValue) when a coroutine frame flattens to more than 65535 values; frames grow per call site and Command.output carries a 1 KiB buffer","updated":"1790117231"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawtracker","closed":"","comments":[],"created":"1790019368","description":"## Summary\n\n`sawc` aborts inside LLVM (no diagnostic, no source location) when a coroutine frame type flattens to more than 65535 scalar values. Found while getting sawtracker to compile against sawlang main (sawtracker ST-37); reached as soon as the SL-329 and SL-215 refusals were worked around.\n\n```\nAssertion failed: (NumValues == VTs.NumVTs && \"NumValues wasn't wide enough for its operands!\"), function SDNode, file SelectionDAGNodes.h, line 1220\nAbort trap: 6\n```\n\nmacOS crash report path: `LLVMTargetMachineEmitToMemory` → `SelectionDAGISel` → `SelectionDAGBuilder::visitInsertValue` → `SelectionDAG::getMergeValues` → `SelectionDAG::getNode` → `llvm::SDNode::SDNode`. Reproduces at every optimization level, `-O0` included.\n\n## Mechanism\n\nThe transform initialises an embedded sub-frame at its call site by building the whole frame as an SSA aggregate: an `insertvalue` chain from `undef`, one instruction per field (visible with `--emit-ir`):\n\n```\n%.7516 = insertvalue %\"__Frame_mutate_patch$m$src_store\" undef, %\"UnsafeRef…\" %.278, 0\n%.7517 = insertvalue %\"__Frame_mutate_patch$m$src_store\" %.7516, %\"UnsafeRef…\" %.279, 1\n...\n```\n\n`visitInsertValue` flattens the aggregate with `ComputeValueVTs` and builds ONE SDNode carrying that many result values, and `SDNode::NumValues` is a 16-bit field. Two things make the limit reachable in ordinary code:\n\n- a fixed-size array in a frame costs one value per element: std's `Command.output` keeps a `{ i1, [1024 x i8] }` pipe-drain chunk buffer across its suspension, so 1025 of that frame's 1080 values are one buffer;\n- frames grow by CALL SITE, not by callee: each call site of a suspending function embeds its own copy of the callee's frame, transitively. Seven `remove_worktree(repo, wt)` cleanup calls in one function meant seven `git`+`rm` frame pairs at ~3900 bytes each.\n\nMeasured with `--emit-frame-layout` on the failing sawtracker tree: `__Frame_mutate_patch` 114,170 values (203,728 bytes); `__Frame_dispatch`, `__Frame_response`, `__Frame_run_store` ≈ 130,600 values. The same source under `f982f54c` (the last compiler sawtracker built with): `__Frame_run_store` 28,422, `__Frame_mutate_patch` 14,136, and `apply_patch_branch` / `merge_patch_branch` / `rebase_patch_branch` had no frames at all. The 4.6x growth is SL-280's fix (imported free calls really get frames) plus the SL-318 epic.\n\n## Reproducer\n\nSawtracker itself. Take sawtracker main after ST-37.p1 merged (`a19e2fb`) and back out the call-site reductions its SAW_NOTES entry ST-SAW-16 lists (restore the per-arm `apply_patch_branch` calls in `mutate_patch`, the separate `remove_worktree` calls in the three patch-branch functions, and the two-child `remove_worktree`), then `sawc src/main.saw`. Or take sawtracker at `0b10cdd`, replace the `Thread.spawn` worker in `test_runner` with a direct `run_test_job(&job)` call, and convert the seven inline `... catch { remove_worktree(repo, wt); return ... }` sites in store.saw to the block form — that is the exact tree that produced the crash reports on 2026-09-21 13:44–13:52 EDT.\n\nA standalone minimization was attempted five times and FAILED, which is itself the useful finding: `[Int8; 70000]` in a frame; 8×8 and 64×1 call sites of a helper holding `[Int8; 1024]`; 8 sites holding `[Int8; 8192]`; the same with `String` locals; the same with a `&var` borrow parameter. All built frames over the limit and all compiled, because when every inserted field is a zero constant instcombine folds the chain into a `memset` before SelectionDAG sees it. sawtracker survives to the abort because its chain has non-constant operands (pointers and live locals). A reproducer needs non-constant insertvalue operands over 65535 flattened values.\n\n## Suggested fixes (compiler side, any one suffices)\n\n- initialise embedded sub-frames through memory (`alloca` + per-field `store`, or `memset` + stores) rather than an SSA aggregate chain;\n- or split oversized aggregates so no single `insertvalue` result exceeds 65535 leaves;\n- or at minimum diagnose the condition (`ComputeValueVTs` count > 65535) with a source-located error instead of an LLVM assert.\n\nAlso worth noting: the per-call-site frame embedding means a 1 KiB fixed buffer in a std frame (`Command.output`) multiplies into every caller's frame; sharing a callee frame across call sites, or keeping large fixed buffers out of frames, would remove the pressure entirely.\n\n## Impact\n\nAny program with a large driven call tree that shells out at several sites. sawtracker's workaround was to reduce suspending call sites (SAW_NOTES ST-SAW-16), which is fragile: the next upstream change that grows frames puts it back over the limit.\n","id":"SL-336","labels":["bug","compiler","codegen","coroutine"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":1,"sequence":1132,"stage":"queued","status":"open","title":"LLVM codegen aborts (SDNode NumValues overflow in visitInsertValue) when a coroutine frame flattens to more than 65535 values; frames grow per call site and Command.output carries a 1 KiB buffer","updated":"1790019368"},"id":"SL-336","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closes with SL-350 (RULED Sep 22): the frame is zero-filled then written in place, never built as an insertvalue chain — SL-336's repro is one of SL-350's acceptance pins.\",\"id\":\"SL-336\",\"request_id\":\"pid-58311-1790117231-19000\"}","request_id":"pid-58311-1790117231-19000","sequence":1202,"timestamp":"1790117231"}
---
