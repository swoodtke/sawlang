---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:codex","closed":"1790341670","comments":[{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \"oracle known wrong\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `exclusivity.whole-call-args`.","created":"1790341670","id":"c1","kind":"landing"}],"created":"1789477052","description":"SL-260's shared-source comparison found Python sawc accepting three calls rejected by the prototype's whole-call access checks (6f307c11; unchanged source at 6a6d57bd):\n\n* references/reject_later_read.saw: `consume(&var value, value)` reads the exclusively borrowed root in a later value argument.\n* references/reject_later_write.saw: `consume(&value, if true { value = 7; value } else { 0 })` writes the shared-borrowed root during later argument evaluation (the fixture uses Saw newlines, not semicolons).\n* references/reject_outer_borrow_preserved.saw: `consume(&var held, read(&other), held)` has the same later read after an unrelated nested call.\n\nAll three compile and execute successfully with no output. Expected: located exclusivity refusals. LANGUAGE_SPEC.md Law of Exclusivity and Nested calls say the argument's borrow extends over the whole call expression; an exclusive borrow excludes other reads and writes, and shared access excludes writes. The prototype should retain its stronger checks.\n\nMechanism to address: production's call access-set comparison covers reference/move arguments and nested reference arguments, but does not account for these ordinary value reads and inline writes while an outer explicit argument borrow is active. SL-284 is adjacent but covers a different omitted source (implicit method receivers inside nested calls); here the original explicit reference is already present.\n\nAudit argument order, shared/exclusive roots, nested calls, inline value-control writes, fields and reference forwarding. Preserve disjoint-root/field and shared-read controls. The prototype fixtures above are the three minimal regression cells; avoid fixing only the bare two-argument spelling.\n","id":"SL-294","labels":["frozen","hazard"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":3,"sequence":2149,"stage":"backlog","status":"closed","title":"sawc: later value arguments bypass whole-call borrow access checks","updated":"1790341670"},"before":{"acceptance":[],"assignee":"","author":"agent:codex","closed":"","comments":[],"created":"1789477052","description":"SL-260's shared-source comparison found Python sawc accepting three calls rejected by the prototype's whole-call access checks (6f307c11; unchanged source at 6a6d57bd):\n\n* references/reject_later_read.saw: `consume(&var value, value)` reads the exclusively borrowed root in a later value argument.\n* references/reject_later_write.saw: `consume(&value, if true { value = 7; value } else { 0 })` writes the shared-borrowed root during later argument evaluation (the fixture uses Saw newlines, not semicolons).\n* references/reject_outer_borrow_preserved.saw: `consume(&var held, read(&other), held)` has the same later read after an unrelated nested call.\n\nAll three compile and execute successfully with no output. Expected: located exclusivity refusals. LANGUAGE_SPEC.md Law of Exclusivity and Nested calls say the argument's borrow extends over the whole call expression; an exclusive borrow excludes other reads and writes, and shared access excludes writes. The prototype should retain its stronger checks.\n\nMechanism to address: production's call access-set comparison covers reference/move arguments and nested reference arguments, but does not account for these ordinary value reads and inline writes while an outer explicit argument borrow is active. SL-284 is adjacent but covers a different omitted source (implicit method receivers inside nested calls); here the original explicit reference is already present.\n\nAudit argument order, shared/exclusive roots, nested calls, inline value-control writes, fields and reference forwarding. Preserve disjoint-root/field and shared-read controls. The prototype fixtures above are the three minimal regression cells; avoid fixing only the bare two-argument spelling.\n","id":"SL-294","labels":["frozen","hazard"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":2148,"stage":"backlog","status":"open","title":"sawc: later value arguments bypass whole-call borrow access checks","updated":"1790341669"},"id":"SL-294","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \\\"oracle known wrong\\\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `exclusivity.whole-call-args`.\",\"id\":\"SL-294\",\"request_id\":\"pid-56179-1790341670-7000\"}","request_id":"pid-56179-1790341670-7000","sequence":2149,"timestamp":"1790341670"}
---
