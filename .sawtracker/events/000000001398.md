---
{"action":"patch_revise","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"TIMELINE (lead, Sep 23, EDT): 17:19 lock held by an agent gate; 17:34 the lead's SL-371 battery takes it (worktree sl371, chained until-mkdir/battery/rmdir); 18:56 the lock dir is RE-CREATED (mtime) while that battery is mid-gmgate — agent:claude-sl2-u0's selfhostlex+lexdiff re-run (its worktree's artifacts 18:55-18:58) cleared a live lock as stale and took it, then released it; 19:07 (23:07Z) the server's SL-357.p3 gate takes the lock; 19:10:19 the lead's battery ends and its chained rmdir removes the SERVER's lock; 19:19 the server's build.sh EXIT trap rmdir fails and set -e turns a passing gate into FAILED. Overlaps: the agent's two lanes (2 min) and the server's suite start (3 min) ran concurrently with the lead's freestanding stage; all three sets of results passed, so no result is suspect. The structural fault is the unowned release; the fix in the description makes a foreign release impossible and a stale judgment verifiable.","created":"1790205692","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-374.p1 (revision 1): SL-374: owner-checked suite lock — tools/suite_lock.sh is the one taker and releaser (run/acquire/release/status/clear-stale); build.sh uses it; suitelock lane; CLAUDE.md names it the only form","created":"1790206061","id":"c2"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-374.p1 (revision 1)","created":"1790208185","id":"c3"},{"author":"agent:codex","body":"Requested changes on patch SL-374.p1 (revision 1)\n\nREQUEST CHANGES — SL-374.p1 r1, base 84904ce1, applied 5a9bce7f; exact reconstructed tree d4087c4acae059936157cb852de0362c3f77f8f2.\n\nTwo independent Sol reviews (protocol; callers/tests/docs), with parent adjudication and isolated shell reproductions. All probe lock paths were under a fresh .build/scratch temporary directory. The real machine lock was never read, removed, or replaced. No suite/compiler run and no repeat of the author-reported green lane.\n\n1. [P1] Owner validation and removal must refer to the same lock generation.\nLocations: tools/suite_lock.sh:88-101,157-177.\n\nrelease_if_owner and clear-stale check ownership/liveness, then separately unlink owner and run rmdir || rm -rf on the public path. Two legitimate removers can validate the old holder before either deletes it. The first removes it; a waiter acquires; the second deletes the successor.\n\nParent reproduced this deterministically using the unchanged helper. A PATH shim only paused the first release at rm, AFTER its old-owner check. A second release --token old-owner completed; acquire --token new-owner completed and published its owner file; the first release resumed. It exited 0 and deleted new-owner's lock. No fabricated owner edit or bare removal was used in the handoff: every ownership transition used the submitted helper.\n\nMake the ownership check and destruction generation-safe through one serialized/conditional transition. Merely deleting the rm -rf fallback is insufficient: a stale remover can still remove the new directory before its owner file is published. The same check-then-remove structure exists in clear-stale; the executed reproduction used release.\n\n2. [P1] Fail acquisition closed if owner publication fails, and make interrupted publication recoverable.\nLocations: tools/suite_lock.sh:55-78,120-137,157-163.\n\nBoth run and acquire ignore wait_and_take's failure status after write_owner. Parent invoked run under umask 0222 at a private path: mkdir succeeded with an unwritable lock directory; opening owner failed with Permission denied. Nevertheless the command executed and returned its marker/status23. No owner file existed, the directory remained, and clear-stale refused it for having no recorded pid.\n\nDo not launch the command or report acquisition unless a complete owner was published. Also account for interruption between mkdir and publication: the current permanent refusal of unrecorded locks provides no supported recovery for an abandoned acquisition. Keep genuine in-progress or foreign ownership protected; do not replace this with an unverifiable stale guess.\n\n3. [P2] A token must identify one acquisition, not a reusable actor name.\nLocations: tools/suite_lock.sh:9-12,88-101,134-142; CLAUDE.md:410-420.\n\nThe documented <issue>-<actor> token can be reused. The sequence acquire T / release T / acquire T / delayed old release T removes the second lifetime, because the API has no generation to distinguish that old release. Parent exercised the sequence on a private path; every operation exits0 and the final release removes the second lock. This is distinct from #1: it requires no concurrent check/remove race.\n\nRelated recovery problem: the new guidance says to rerun a timed-out command, but acquire always waits for mkdir and cannot recognize its own already-successful token-held acquisition. Retrying after an ambiguous timeout can wait forever on itself, while clear-stale refuses token-held locks. Give acquisition/retry/release a coherent per-acquisition identity and document recovery of ambiguous completion; do not blindly reuse an actor token.\n\n4. [P1] Complete the required battery adoption and define one locking boundary.\nLocations: tools/battery.sh:60-94,207-246; build.sh:23-27; CLAUDE.md:401-423.\n\nThe patch adds a suitelock test stage, but battery.sh never takes the lock. Its suite and freestanding stages still enter the direct execution loop. The issue explicitly requires BOTH build.sh and battery.sh to use the helper; a protocol test is not caller adoption. A direct terminal battery can still overlap the server's newly locked build.\n\nConversely, the new universal instruction says to wrap every gate, explicitly including build.sh test. Following it as suite_lock.sh run -- ./build.sh test takes an outer lock before build.sh starts its own run, which waits forever on that outer holder. Parent reproduced the equivalent two-helper invocation on a private lock: the inner run remained waiting while the outer held it; the supervised process tree was stopped after15s. No real build was launched.\n\nChoose and document the self-locking top-level boundary; do not accidentally nest it. Migrate supported direct suite/battery/freestanding command surfaces accordingly (the issue also explicitly names agent prompts). Current direct commands remain in Makefile, TESTING.md, README.md and the mech agent prompt, as well as earlier CLAUDE.md examples. Lower-level runners can remain intentionally non-locking under the chosen outer boundary; do not make every layer acquire recursively.\n\nBehavioral coverage to retain/add while correcting the protocol:\n- A concurrent valid release/clear must not remove a newly acquired generation (the incident's class, reproduced in #1).\n- Failed publication must not execute the gate, and recovery must not strand a permanent bare lock.\n- Actual run cleanup after a missing/replaced lock must preserve the command's exit status and the replacement owner. Existing tests separately exercise run and a foreign token release, not this handoff.\n- clear-stale must refuse a dead recorded pid while a matching gate process still lives. Existing tests globally force the process matcher empty, so they never exercise this half of the guard.\n- Synchronize the existing two-run test on owner publication rather than sleep0.5; bound child waits and clean up on failure. Test holder identity/unchanged ownership rather than pinning the incidental phrase 'NOT released'.\n\nWhat is correct: the ordinary single-holder path preserves the command's status; an uncontended wrong-token release leaves the directory alone; the new contract-test lane uses private paths; build.sh enters the helper around its existing gate. No ordinary foreground INT/TERM early-release bug was established, and no arbitrary owner-file tampering, hostile path, or daemonized-child scenario is part of these findings.\n\nEvidence: parent artifact sl374-p1-r1-protocol-probes.json; source-only Sol protocol/integration reviews. Temporary probe processes were stopped and their private paths removed. This patch does not yet make foreign release impossible. Please revise before approval; no merge requested.","created":"1790208606","id":"c4"},{"author":"agent:claude-sawlang","body":"Revised patch SL-374.p1 to revision 2","created":"1790208875","id":"c5"}],"created":"1790205652","description":"FOUND by agent:claude-air-sawlang (chat m120, Sep 23): SL-357.p3 r1's server gate passed everything (suite 2656/10 xfail, freestanding 36/36) but exited 1 because build.sh's EXIT trap ran 'rmdir $LOCK' after something else had removed the lock mid-run (23:07-23:19Z). MECHANISM (obligation 4, a class): the lock is a bare mkdir at /private/tmp/claude-<uid>/saw-suite-lock, and EVERY release is an unconditional rmdir by whoever reaches that line — build.sh's trap, tools/battery.sh's callers, and each agent's hand-written 'until mkdir ...; gate; rmdir' chain. Nothing records who holds it, so (a) a taker whose mkdir-wait was killed by a tool timeout proceeds as if it held the lock and later rmdir's someone else's, (b) a 'stale' judgment cannot be verified, and (c) once one lock is wrongly released, the NEXT taker's lock is released by the original holder's chained rmdir at its end — a chain that the lead's own SL-371 battery (17:34-19:2x EDT) most likely continued: its lock dir was re-created at 18:56 while its battery was mid-run, so its end-of-run rmdir removed the server's. Overlapping suite runs are the other consequence. FIX: (1) ONE script, tools/suite_lock.sh: 'acquire' does mkdir + writes owner (pid, host, start time, label) into the dir, waiting with a bounded sleep; 'release' removes the dir ONLY if the owner file names the caller's pid (else prints who holds it and exits 0 without touching it); 'run -- CMD...' = acquire, run, release-own in one process with the trap tolerant of a missing dir and the command's real exit status preserved; 'status' prints the holder and whether its pid is alive; 'clear-stale' removes it only when the recorded pid is dead. (2) build.sh test and tools/battery.sh use it; CLAUDE.md's suite-lock paragraph and every agent prompt say 'tools/suite_lock.sh run -- <gate>' instead of the hand-written chain; the sawos repo keeps its own path via an env var. (3) The Air's suggested trap hardening is subsumed: release-own never fails the run. Harness change, not a compiler branch; gate = the script's own tests (acquire/release/foreign-release refused/stale cleared) + one suite run through it.","id":"SL-374","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":6,"sequence":1398,"stage":"backlog","status":"open","title":"The suite lock is unowned: any holder's chained rmdir can release ANOTHER holder's lock (SL-357.p3 gate reported FAILED after a passing run); make the lock owner-checked and give every taker one script","updated":"1790208875"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"TIMELINE (lead, Sep 23, EDT): 17:19 lock held by an agent gate; 17:34 the lead's SL-371 battery takes it (worktree sl371, chained until-mkdir/battery/rmdir); 18:56 the lock dir is RE-CREATED (mtime) while that battery is mid-gmgate — agent:claude-sl2-u0's selfhostlex+lexdiff re-run (its worktree's artifacts 18:55-18:58) cleared a live lock as stale and took it, then released it; 19:07 (23:07Z) the server's SL-357.p3 gate takes the lock; 19:10:19 the lead's battery ends and its chained rmdir removes the SERVER's lock; 19:19 the server's build.sh EXIT trap rmdir fails and set -e turns a passing gate into FAILED. Overlaps: the agent's two lanes (2 min) and the server's suite start (3 min) ran concurrently with the lead's freestanding stage; all three sets of results passed, so no result is suspect. The structural fault is the unowned release; the fix in the description makes a foreign release impossible and a stale judgment verifiable.","created":"1790205692","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-374.p1 (revision 1): SL-374: owner-checked suite lock — tools/suite_lock.sh is the one taker and releaser (run/acquire/release/status/clear-stale); build.sh uses it; suitelock lane; CLAUDE.md names it the only form","created":"1790206061","id":"c2"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-374.p1 (revision 1)","created":"1790208185","id":"c3"},{"author":"agent:codex","body":"Requested changes on patch SL-374.p1 (revision 1)\n\nREQUEST CHANGES — SL-374.p1 r1, base 84904ce1, applied 5a9bce7f; exact reconstructed tree d4087c4acae059936157cb852de0362c3f77f8f2.\n\nTwo independent Sol reviews (protocol; callers/tests/docs), with parent adjudication and isolated shell reproductions. All probe lock paths were under a fresh .build/scratch temporary directory. The real machine lock was never read, removed, or replaced. No suite/compiler run and no repeat of the author-reported green lane.\n\n1. [P1] Owner validation and removal must refer to the same lock generation.\nLocations: tools/suite_lock.sh:88-101,157-177.\n\nrelease_if_owner and clear-stale check ownership/liveness, then separately unlink owner and run rmdir || rm -rf on the public path. Two legitimate removers can validate the old holder before either deletes it. The first removes it; a waiter acquires; the second deletes the successor.\n\nParent reproduced this deterministically using the unchanged helper. A PATH shim only paused the first release at rm, AFTER its old-owner check. A second release --token old-owner completed; acquire --token new-owner completed and published its owner file; the first release resumed. It exited 0 and deleted new-owner's lock. No fabricated owner edit or bare removal was used in the handoff: every ownership transition used the submitted helper.\n\nMake the ownership check and destruction generation-safe through one serialized/conditional transition. Merely deleting the rm -rf fallback is insufficient: a stale remover can still remove the new directory before its owner file is published. The same check-then-remove structure exists in clear-stale; the executed reproduction used release.\n\n2. [P1] Fail acquisition closed if owner publication fails, and make interrupted publication recoverable.\nLocations: tools/suite_lock.sh:55-78,120-137,157-163.\n\nBoth run and acquire ignore wait_and_take's failure status after write_owner. Parent invoked run under umask 0222 at a private path: mkdir succeeded with an unwritable lock directory; opening owner failed with Permission denied. Nevertheless the command executed and returned its marker/status23. No owner file existed, the directory remained, and clear-stale refused it for having no recorded pid.\n\nDo not launch the command or report acquisition unless a complete owner was published. Also account for interruption between mkdir and publication: the current permanent refusal of unrecorded locks provides no supported recovery for an abandoned acquisition. Keep genuine in-progress or foreign ownership protected; do not replace this with an unverifiable stale guess.\n\n3. [P2] A token must identify one acquisition, not a reusable actor name.\nLocations: tools/suite_lock.sh:9-12,88-101,134-142; CLAUDE.md:410-420.\n\nThe documented <issue>-<actor> token can be reused. The sequence acquire T / release T / acquire T / delayed old release T removes the second lifetime, because the API has no generation to distinguish that old release. Parent exercised the sequence on a private path; every operation exits0 and the final release removes the second lock. This is distinct from #1: it requires no concurrent check/remove race.\n\nRelated recovery problem: the new guidance says to rerun a timed-out command, but acquire always waits for mkdir and cannot recognize its own already-successful token-held acquisition. Retrying after an ambiguous timeout can wait forever on itself, while clear-stale refuses token-held locks. Give acquisition/retry/release a coherent per-acquisition identity and document recovery of ambiguous completion; do not blindly reuse an actor token.\n\n4. [P1] Complete the required battery adoption and define one locking boundary.\nLocations: tools/battery.sh:60-94,207-246; build.sh:23-27; CLAUDE.md:401-423.\n\nThe patch adds a suitelock test stage, but battery.sh never takes the lock. Its suite and freestanding stages still enter the direct execution loop. The issue explicitly requires BOTH build.sh and battery.sh to use the helper; a protocol test is not caller adoption. A direct terminal battery can still overlap the server's newly locked build.\n\nConversely, the new universal instruction says to wrap every gate, explicitly including build.sh test. Following it as suite_lock.sh run -- ./build.sh test takes an outer lock before build.sh starts its own run, which waits forever on that outer holder. Parent reproduced the equivalent two-helper invocation on a private lock: the inner run remained waiting while the outer held it; the supervised process tree was stopped after15s. No real build was launched.\n\nChoose and document the self-locking top-level boundary; do not accidentally nest it. Migrate supported direct suite/battery/freestanding command surfaces accordingly (the issue also explicitly names agent prompts). Current direct commands remain in Makefile, TESTING.md, README.md and the mech agent prompt, as well as earlier CLAUDE.md examples. Lower-level runners can remain intentionally non-locking under the chosen outer boundary; do not make every layer acquire recursively.\n\nBehavioral coverage to retain/add while correcting the protocol:\n- A concurrent valid release/clear must not remove a newly acquired generation (the incident's class, reproduced in #1).\n- Failed publication must not execute the gate, and recovery must not strand a permanent bare lock.\n- Actual run cleanup after a missing/replaced lock must preserve the command's exit status and the replacement owner. Existing tests separately exercise run and a foreign token release, not this handoff.\n- clear-stale must refuse a dead recorded pid while a matching gate process still lives. Existing tests globally force the process matcher empty, so they never exercise this half of the guard.\n- Synchronize the existing two-run test on owner publication rather than sleep0.5; bound child waits and clean up on failure. Test holder identity/unchanged ownership rather than pinning the incidental phrase 'NOT released'.\n\nWhat is correct: the ordinary single-holder path preserves the command's status; an uncontended wrong-token release leaves the directory alone; the new contract-test lane uses private paths; build.sh enters the helper around its existing gate. No ordinary foreground INT/TERM early-release bug was established, and no arbitrary owner-file tampering, hostile path, or daemonized-child scenario is part of these findings.\n\nEvidence: parent artifact sl374-p1-r1-protocol-probes.json; source-only Sol protocol/integration reviews. Temporary probe processes were stopped and their private paths removed. This patch does not yet make foreign release impossible. Please revise before approval; no merge requested.","created":"1790208606","id":"c4"}],"created":"1790205652","description":"FOUND by agent:claude-air-sawlang (chat m120, Sep 23): SL-357.p3 r1's server gate passed everything (suite 2656/10 xfail, freestanding 36/36) but exited 1 because build.sh's EXIT trap ran 'rmdir $LOCK' after something else had removed the lock mid-run (23:07-23:19Z). MECHANISM (obligation 4, a class): the lock is a bare mkdir at /private/tmp/claude-<uid>/saw-suite-lock, and EVERY release is an unconditional rmdir by whoever reaches that line — build.sh's trap, tools/battery.sh's callers, and each agent's hand-written 'until mkdir ...; gate; rmdir' chain. Nothing records who holds it, so (a) a taker whose mkdir-wait was killed by a tool timeout proceeds as if it held the lock and later rmdir's someone else's, (b) a 'stale' judgment cannot be verified, and (c) once one lock is wrongly released, the NEXT taker's lock is released by the original holder's chained rmdir at its end — a chain that the lead's own SL-371 battery (17:34-19:2x EDT) most likely continued: its lock dir was re-created at 18:56 while its battery was mid-run, so its end-of-run rmdir removed the server's. Overlapping suite runs are the other consequence. FIX: (1) ONE script, tools/suite_lock.sh: 'acquire' does mkdir + writes owner (pid, host, start time, label) into the dir, waiting with a bounded sleep; 'release' removes the dir ONLY if the owner file names the caller's pid (else prints who holds it and exits 0 without touching it); 'run -- CMD...' = acquire, run, release-own in one process with the trap tolerant of a missing dir and the command's real exit status preserved; 'status' prints the holder and whether its pid is alive; 'clear-stale' removes it only when the recorded pid is dead. (2) build.sh test and tools/battery.sh use it; CLAUDE.md's suite-lock paragraph and every agent prompt say 'tools/suite_lock.sh run -- <gate>' instead of the hand-written chain; the sawos repo keeps its own path via an env var. (3) The Air's suggested trap hardening is subsumed: release-own never fails the run. Harness change, not a compiler branch; gate = the script's own tests (acquire/release/foreign-release refused/stale cleared) + one suite run through it.","id":"SL-374","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":5,"sequence":1397,"stage":"backlog","status":"open","title":"The suite lock is unowned: any holder's chained rmdir can release ANOTHER holder's lock (SL-357.p3 gate reported FAILED after a passing run); make the lock owner-checked and give every taker one script","updated":"1790208606"},"id":"SL-374","project":"SL","request":"{\"action\":\"patch_revise\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/.claude/agents/mech.md b/.claude/agents/mech.md\\nindex 59a5446a..882f1ccd 100644\\n--- a/.claude/agents/mech.md\\n+++ b/.claude/agents/mech.md\\n@@ -32,8 +32,11 @@ An honest partial landing with findings beats a complete landing with a\\n judgment call buried in it.\\n \\n Discipline (same as every agent here): full suite before EVERY commit\\n-(/Users/swoodtke/Projects/claudes-lang/.venv/bin/python test_runner.py —\\n-the main checkout's venv by absolute path; none exists in your worktree);\\n+(tools/suite_lock.sh run --label suite --\\n+/Users/swoodtke/Projects/claudes-lang/.venv/bin/python test_runner.py —\\n+the main checkout's venv by absolute path; none exists in your worktree;\\n+the helper takes the machine-wide suite lock and releases only what it\\n+took — never a bare mkdir/rmdir of the lock path);\\n small per-batch commits on your worktree branch; never merge to main;\\n never touch the main checkout. Final gate: the full tracked battery\\n (SAW_PYTHON=<main venv> tools/battery.sh), zero uncited xfails. Scratch\\ndiff --git a/CLAUDE.md b/CLAUDE.md\\nindex b9b815f2..d59c5bd9 100644\\n--- a/CLAUDE.md\\n+++ b/CLAUDE.md\\n@@ -398,21 +398,38 @@ finding filed by a differential/fuzz harness removes the harness's\\n known-ledger entry (corodiff_known.txt, sawfuzz_known.txt) in the SAME\\n commit, and its gate includes that harness's lane.\\n \\n-**THE SUITE LOCK (machine-wide, sawlang's):** all suite-shaped invocations\\n-in THIS repo (test_runner, battery.sh, freestanding_runner) serialize\\n-through a mkdir lock at `/private/tmp/claude-<uid>/saw-suite-lock`\\n-(uid = `id -u`; create the parent once per machine; the sawos repo has\\n-its OWN lock path so the two never queue on each other). Acquire + gate +\\n-release in ONE chained FOREGROUND command: `until mkdir <lock>\\n-2>/dev/null; do sleep 15; done; <gate>; rc=$?; rmdir <lock>; echo\\n-GATE=$rc` — in SANDBOXED agent worktrees, where the chained form is\\n-refused, SPLIT it: the bare mkdir-wait as its own call, each gate as its\\n-own call, the bare rmdir immediately after; the never-background and\\n-never-stop-while-waiting-or-holding rules apply unchanged. Never\\n-background the wait or the gate (a stopped agent's background waiters die\\n-silently); never hold the lock while editing; if the command times out,\\n-rerun the same command. Clear a stale lock only VERIFIED-DEAD: no suite\\n-process exists (pgrep) AND the holder is identified dead.\\n+**THE SUITE LOCK (machine-wide, sawlang's):** every suite-shaped run on\\n+this host serializes through ONE owner-checked lock, taken and released\\n+ONLY by `tools/suite_lock.sh` (SL-374; the lock dir is\\n+`/private/tmp/claude-<uid>/saw-suite-lock` with an `owner` file naming\\n+pid/token/host/label, and every ownership transition runs inside the\\n+helper's short critical section; the sawos repo sets `SAW_SUITE_LOCK` to\\n+its own path so the two never queue on each other). TWO self-locking\\n+boundaries: `tools/battery.sh` and `./build.sh test` take the lock\\n+themselves for their whole run. Everything else (`test_runner.py`,\\n+`tools/freestanding_runner.py`, `make test`) is non-locking and is run\\n+as `tools/suite_lock.sh run --label <what> -- <command>` — a FOREGROUND\\n+call that waits, runs the command, releases only what it took, and\\n+exits with the command's own status; wrapping a self-locking boundary\\n+this way is harmless (the helper is re-entrant under the token it\\n+exports). In SANDBOXED agent worktrees, where a long foreground call is\\n+refused, SPLIT it: `tools/suite_lock.sh acquire --label <issue>` as its\\n+own call — it PRINTS a minted per-acquisition token; keep it — then each\\n+gate as its own call, then `tools/suite_lock.sh release --token <that\\n+token>` immediately after. Re-running `acquire --token <that token>`\\n+after an ambiguous timeout is idempotent (it reports the lock already\\n+yours), so a retry never waits on itself; never reuse a token across\\n+acquisitions. A release under any other token touches nothing and says\\n+who holds it. Never write a bare `mkdir`/`rmdir` of the lock path: an\\n+unchecked release can remove ANOTHER holder's lock, and the next\\n+holder's release then removes the server's (that chain turned a passing\\n+patch gate into FAILED on Sep 23). Never background the wait or the gate\\n+(a stopped agent's background waiters die silently); never hold the\\n+lock while editing. `tools/suite_lock.sh status` says who holds it and\\n+whether that pid is alive; `tools/suite_lock.sh clear-stale` is the\\n+ONLY way to clear one, and it refuses unless the holder is provably\\n+gone (recorded pid dead and no gate process running, or an unpublished\\n+acquisition abandoned for over a minute).\\n \\n **DESIGN DOCTRINE (user rulings, standing):**\\n - **Never hide errors** — failures surface as Result/Optional; no\\ndiff --git a/Makefile b/Makefile\\nindex f34a64dc..f6ea9f60 100644\\n--- a/Makefile\\n+++ b/Makefile\\n@@ -5,17 +5,22 @@\\n # Default target\\n all: test\\n \\n+# Every suite-shaped target runs under the machine-wide suite lock through\\n+# tools/suite_lock.sh (SL-374): the lock is owner-checked, and the helper is\\n+# re-entrant, so these targets are safe inside a self-locking battery run.\\n+LOCKED = tools/suite_lock.sh run --label make --\\n+\\n # Run all tests\\n test:\\n-\\t@python3 test_runner.py\\n+\\t@$(LOCKED) python3 test_runner.py\\n \\n # Run tests with verbose output\\n test-verbose:\\n-\\t@python3 test_runner.py -v\\n+\\t@$(LOCKED) python3 test_runner.py -v\\n \\n # Run a specific test by name\\n test-filter:\\n-\\t@python3 test_runner.py -f $(FILTER)\\n+\\t@$(LOCKED) python3 test_runner.py -f $(FILTER)\\n \\n # Blade self-hosting bootstrap (design 64 B8): Blade builds + tests Blade\\n # through its own resolve/lock/module-path/incremental pipeline.\\n@@ -61,7 +66,7 @@ install: blade\\n # This is the compiler's cross-target gate; the SOS kernel itself lives in the\\n # sawos repository since design 238 unit 5 (Aug 28 2026) with its own harness.\\n freestanding-test:\\n-\\t@python3 tools/freestanding_runner.py\\n+\\t@$(LOCKED) python3 tools/freestanding_runner.py\\n \\n # Differential lexer harness (design 116): build the Saw lexer, then diff its\\n # canonical token dump against sawc's Python lexer over every tracked .saw file.\\n@@ -195,7 +200,7 @@ clean:\\n \\n # Run tests sequentially (original behavior)\\n test-sequential:\\n-\\t@python3 test_runner.py --sequential\\n+\\t@$(LOCKED) python3 test_runner.py --sequential\\n \\n # Show help\\n help:\\ndiff --git a/TESTING.md b/TESTING.md\\nindex a6b6069c..fa335cb2 100644\\n--- a/TESTING.md\\n+++ b/TESTING.md\\n@@ -397,6 +397,33 @@ checkout's:\\n SAW_PYTHON=/path/to/main/.venv/bin/python tools/battery.sh\\n ```\\n \\n+### The suite lock\\n+\\n+Every suite-shaped run on a host serializes through one owner-checked lock,\\n+taken and released only by `tools/suite_lock.sh` (the lock is a directory\\n+under `/private/tmp/claude-<uid>/` whose `owner` file names the holder; every\\n+ownership transition runs inside the helper's short critical section, so a\\n+release can never remove a lock somebody else holds). Two entry points are\\n+self-locking boundaries and need nothing from you: `tools/battery.sh` and\\n+`./build.sh test`. Everything else — `test_runner.py`,\\n+`tools/freestanding_runner.py`, the `make test` family — is non-locking and\\n+runs through the helper:\\n+\\n+```bash\\n+tools/suite_lock.sh run --label suite -- ./.venv/bin/python test_runner.py\\n+tools/suite_lock.sh status          # who holds it, and whether that pid is alive\\n+tools/suite_lock.sh clear-stale     # the only way to clear one; refuses a live holder\\n+```\\n+\\n+The helper is re-entrant under the token it exports, so wrapping a\\n+self-locking boundary is harmless. A caller that cannot keep one foreground\\n+call open splits the run: `acquire --label <what>` prints a minted\\n+per-acquisition token; the gates run; `release --token <that token>` gives the\\n+lock back. Re-running `acquire --token <that token>` after an ambiguous\\n+timeout is idempotent, and a release under any other token touches nothing.\\n+Another repository keeps its own lock by setting `SAW_SUITE_LOCK`. The\\n+`suitelock` battery lane pins this contract against a private lock path.\\n+\\n Adding a lane means editing `STAGES` in the script. That is the point of it\\n being tracked: it used to be an untracked scratch file each session rewrote\\n from prose, and a gate list nobody can diff is a gate list that quietly loses\\ndiff --git a/build.sh b/build.sh\\nindex 5d2607ee..53565336 100755\\n--- a/build.sh\\n+++ b/build.sh\\n@@ -20,10 +20,8 @@ if [ ! -x .venv/bin/python ]; then\\n     .venv/bin/python -m pip install --quiet -r sawc/requirements.txt\\n fi\\n \\n-LOCK=\\\"/private/tmp/claude-$(id -u)/saw-suite-lock\\\"\\n-mkdir -p \\\"$(dirname \\\"$LOCK\\\")\\\"\\n-until mkdir \\\"$LOCK\\\" 2>/dev/null; do sleep 15; done\\n-trap 'rmdir \\\"$LOCK\\\"' EXIT INT TERM\\n-\\n-.venv/bin/python test_runner.py\\n-.venv/bin/python tools/freestanding_runner.py\\n+# The lock is owner-checked (tools/suite_lock.sh): this run can only release\\n+# what it took, so a lock someone else holds at the end never turns a passing\\n+# gate into a failure, and the gate's own status is what the server sees.\\n+exec tools/suite_lock.sh run --label \\\"build.sh test\\\" -- sh -c \\\\\\n+    '.venv/bin/python test_runner.py && .venv/bin/python tools/freestanding_runner.py'\\ndiff --git a/tools/battery.sh b/tools/battery.sh\\nindex b9e04c9a..3d5b73f0 100755\\n--- a/tools/battery.sh\\n+++ b/tools/battery.sh\\n@@ -28,6 +28,14 @@ set -u\\n REPO=\\\"$(cd \\\"$(dirname \\\"${BASH_SOURCE[0]}\\\")/..\\\" && pwd)\\\"\\n cd \\\"$REPO\\\" || exit 2\\n \\n+# The battery is a self-locking boundary: it takes the machine-wide suite\\n+# lock for its whole run, and its stages run non-locking underneath. Wrapping\\n+# it in another `suite_lock.sh run` is harmless — the helper is re-entrant\\n+# under the token it exported.\\n+if [ -z \\\"${SAW_SUITE_LOCK_HELD:-}\\\" ] && [ \\\"${1:-}\\\" != \\\"--list\\\" ]; then\\n+    exec \\\"$REPO/tools/suite_lock.sh\\\" run --label \\\"battery\\\" -- \\\"$REPO/tools/battery.sh\\\" \\\"$@\\\"\\n+fi\\n+\\n if [ -n \\\"${SAW_PYTHON:-}\\\" ]; then\\n     PY=\\\"$SAW_PYTHON\\\"\\n elif [ -x \\\"$REPO/.venv/bin/python\\\" ]; then\\n@@ -63,6 +71,7 @@ STAGES=(\\n     \\\"lexdiff|no|the Saw lexer against sawc's, over every tracked .saw|$PY tools/lexdiff.py\\\"\\n     \\\"astdiff|no|every .saw dumps completely and byte-stably|$PY tools/astdiff.py\\\"\\n     \\\"astgraft|no|no pass stamps an AST attribute no class declares|$PY tools/test_ast_graft.py\\\"\\n+    \\\"suitelock|no|the suite lock releases only for its holder and verifies staleness|$PY tools/test_suite_lock.py\\\"\\n     \\\"transferdecisions|no|every ownership boundary carries an explicit decision|$PY tools/test_transfer_decisions.py\\\"\\n     \\\"producertaxonomy|no|every value-position node says how it produces a value|$PY tools/test_producer_taxonomy.py\\\"\\n     \\\"corodiscovery|no|one ledger answers every coroutine frame decision|$PY tools/test_coro_discovery.py\\\"\\ndiff --git a/tools/suite_lock.sh b/tools/suite_lock.sh\\nnew file mode 100755\\nindex 00000000..50e1d5f7\\n--- /dev/null\\n+++ b/tools/suite_lock.sh\\n@@ -0,0 +1,299 @@\\n+#!/bin/sh\\n+# The machine-wide suite lock, owner-checked. The ONE taker and releaser for\\n+# every suite-shaped run on this host, so a release can never remove a lock\\n+# somebody else holds and \\\"stale\\\" is a verified fact, not a guess (SL-374).\\n+#\\n+#   tools/suite_lock.sh run [--label L] -- CMD ...  acquire, run CMD, release\\n+#                                                   own; exits with CMD's status\\n+#   tools/suite_lock.sh acquire [--label L] [--token T]\\n+#                                                   wait, then hold under a token\\n+#                                                   (minted and printed unless\\n+#                                                   given; the same token again\\n+#                                                   is the idempotent retry)\\n+#   tools/suite_lock.sh release --token T           release only if T holds it\\n+#   tools/suite_lock.sh status                      the holder; exit 1 if free\\n+#   tools/suite_lock.sh clear-stale                 remove only a lock whose\\n+#                                                   holder is provably gone\\n+#\\n+# PROTOCOL. The lock is a directory at $LOCK holding an `owner` file. Every\\n+# ownership transition — publish, release, clear — runs inside one short\\n+# critical section (the `.meta` directory), so two removers cannot both\\n+# validate the same holder and a waiter cannot slip between a check and a\\n+# removal. Acquisition publishes the owner file BEFORE the lock becomes\\n+# visible under the meta section, and fails closed if it cannot; a crash\\n+# inside the section is recovered by age (a section never lives longer than\\n+# a few milliseconds of shell builtins). `run` sets SAW_SUITE_LOCK_HELD for\\n+# its command, and a nested `run` on the same lock under that variable is\\n+# re-entrant: it runs the command without taking the lock again, which is\\n+# what lets battery.sh and build.sh self-lock AND be wrapped.\\n+# SAW_SUITE_LOCK overrides the path (another repo keeps its own lock).\\n+\\n+set -u\\n+\\n+LOCK=\\\"${SAW_SUITE_LOCK:-/private/tmp/claude-$(id -u)/saw-suite-lock}\\\"\\n+OWNER=\\\"$LOCK/owner\\\"\\n+META=\\\"$LOCK.meta\\\"\\n+# The processes whose presence means a gate is running on this host;\\n+# overridable so the contract test can run beside a real gate.\\n+GATE_PATTERN=\\\"${SAW_SUITE_LOCK_GATE_PATTERN:-test_runner\\\\.py|battery\\\\.sh|freestanding_runner\\\\.py|build\\\\.sh test|suite_lock\\\\.sh run}\\\"\\n+WAIT_SECONDS=\\\"${SAW_SUITE_LOCK_WAIT:-15}\\\"\\n+# A meta section or an unpublished lock older than this is an abandoned one.\\n+ABANDON_SECONDS=\\\"${SAW_SUITE_LOCK_ABANDON:-60}\\\"\\n+\\n+usage() {\\n+    sed -n '2,20p' \\\"$0\\\" >&2\\n+    exit 2\\n+}\\n+\\n+now() { date +%s; }\\n+\\n+mtime() {\\n+    # mtime PATH — seconds since the epoch, or empty if PATH is gone.\\n+    stat -f %m \\\"$1\\\" 2>/dev/null || stat -c %Y \\\"$1\\\" 2>/dev/null || true\\n+}\\n+\\n+age_of() {\\n+    m=\\\"$(mtime \\\"$1\\\")\\\"\\n+    [ -n \\\"$m\\\" ] || { echo 0; return; }\\n+    echo $(( $(now) - m ))\\n+}\\n+\\n+owner_field() {\\n+    # owner_field NAME — the value recorded for NAME, empty if none.\\n+    [ -f \\\"$OWNER\\\" ] || return 0\\n+    sed -n \\\"s/^$1=//p\\\" \\\"$OWNER\\\" 2>/dev/null | head -n 1\\n+}\\n+\\n+describe_holder() {\\n+    if [ -f \\\"$OWNER\\\" ]; then\\n+        printf 'held by pid=%s token=%s host=%s label=%s since=%s\\\\n' \\\\\\n+            \\\"$(owner_field pid)\\\" \\\"$(owner_field token)\\\" \\\"$(owner_field host)\\\" \\\\\\n+            \\\"$(owner_field label)\\\" \\\"$(owner_field started)\\\"\\n+    elif [ -d \\\"$LOCK\\\" ]; then\\n+        echo \\\"held by an UNPUBLISHED taker (no owner file; age $(age_of \\\"$LOCK\\\")s)\\\"\\n+    else\\n+        echo \\\"free\\\"\\n+    fi\\n+}\\n+\\n+# --- the critical section ----------------------------------------------------\\n+\\n+meta_enter() {\\n+    # Spin briefly; a section is milliseconds long, so a long wait means its\\n+    # holder died inside it and the section is reclaimed by age.\\n+    mkdir -p \\\"$(dirname \\\"$LOCK\\\")\\\"\\n+    while ! mkdir \\\"$META\\\" 2>/dev/null; do\\n+        if [ \\\"$(age_of \\\"$META\\\")\\\" -gt \\\"$ABANDON_SECONDS\\\" ]; then\\n+            rmdir \\\"$META\\\" 2>/dev/null || true\\n+            continue\\n+        fi\\n+        sleep 0.05\\n+    done\\n+}\\n+\\n+meta_leave() {\\n+    rmdir \\\"$META\\\" 2>/dev/null || true\\n+}\\n+\\n+publish_owner() {\\n+    # publish_owner PID TOKEN LABEL — the lock is visible only once this has\\n+    # fully succeeded; any failure takes the directory back down.\\n+    tmp=\\\"$LOCK/owner.tmp.$$\\\"\\n+    if {\\n+        echo \\\"pid=$1\\\"\\n+        echo \\\"token=$2\\\"\\n+        echo \\\"host=$(hostname 2>/dev/null || echo unknown)\\\"\\n+        echo \\\"label=$3\\\"\\n+        echo \\\"started=$(date '+%Y-%m-%dT%H:%M:%S')\\\"\\n+    } > \\\"$tmp\\\" 2>/dev/null && mv \\\"$tmp\\\" \\\"$OWNER\\\" 2>/dev/null; then\\n+        return 0\\n+    fi\\n+    rm -f \\\"$tmp\\\" 2>/dev/null\\n+    rmdir \\\"$LOCK\\\" 2>/dev/null || rm -rf \\\"$LOCK\\\"\\n+    return 1\\n+}\\n+\\n+try_take() {\\n+    # try_take PID TOKEN LABEL — one attempt under the section.\\n+    # 0 = taken (owner published); 1 = held by someone else; 2 = held by\\n+    # this very token already (the idempotent retry); 3 = cannot publish.\\n+    meta_enter\\n+    if [ -d \\\"$LOCK\\\" ]; then\\n+        if [ -n \\\"$2\\\" ] && [ \\\"$(owner_field token)\\\" = \\\"$2\\\" ]; then\\n+            meta_leave; return 2\\n+        fi\\n+        if [ ! -f \\\"$OWNER\\\" ] && [ \\\"$(age_of \\\"$LOCK\\\")\\\" -gt \\\"$ABANDON_SECONDS\\\" ]; then\\n+            # An acquisition that died between mkdir and publish: nothing can\\n+            # ever own it, so it is reclaimed here, inside the section.\\n+            rmdir \\\"$LOCK\\\" 2>/dev/null || rm -rf \\\"$LOCK\\\"\\n+        else\\n+            meta_leave; return 1\\n+        fi\\n+    fi\\n+    if ! mkdir \\\"$LOCK\\\" 2>/dev/null; then\\n+        meta_leave; return 1\\n+    fi\\n+    if ! publish_owner \\\"$1\\\" \\\"$2\\\" \\\"$3\\\"; then\\n+        meta_leave; return 3\\n+    fi\\n+    meta_leave\\n+    return 0\\n+}\\n+\\n+wait_and_take() {\\n+    # wait_and_take PID TOKEN LABEL — block until taken; 2 if already ours.\\n+    tries=0\\n+    while :; do\\n+        try_take \\\"$1\\\" \\\"$2\\\" \\\"$3\\\"\\n+        rc=$?\\n+        case $rc in\\n+            0|2) return $rc ;;\\n+            3) echo \\\"suite lock: cannot publish an owner file under $LOCK (permissions?); not taken\\\" >&2; return 3 ;;\\n+        esac\\n+        tries=$((tries + 1))\\n+        if [ $((tries % 8)) -eq 1 ]; then\\n+            echo \\\"suite lock: waiting — $(describe_holder)\\\" >&2\\n+        fi\\n+        sleep \\\"$WAIT_SECONDS\\\"\\n+    done\\n+}\\n+\\n+remove_if() {\\n+    # remove_if FIELD VALUE — inside the section: remove the lock only when\\n+    # the CURRENT owner file's FIELD equals VALUE. Prints what happened.\\n+    meta_enter\\n+    if [ ! -d \\\"$LOCK\\\" ]; then\\n+        meta_leave\\n+        echo \\\"suite lock: already free (nothing released)\\\" >&2\\n+        return 0\\n+    fi\\n+    if [ -n \\\"$2\\\" ] && [ \\\"$(owner_field \\\"$1\\\")\\\" = \\\"$2\\\" ]; then\\n+        rm -f \\\"$OWNER\\\"\\n+        rmdir \\\"$LOCK\\\" 2>/dev/null || rm -rf \\\"$LOCK\\\"\\n+        meta_leave\\n+        return 0\\n+    fi\\n+    holder=\\\"$(describe_holder)\\\"\\n+    meta_leave\\n+    echo \\\"suite lock: NOT released — $holder; this caller ($1=$2) does not hold it\\\" >&2\\n+    return 0\\n+}\\n+\\n+pid_alive() {\\n+    [ -n \\\"$1\\\" ] && kill -0 \\\"$1\\\" 2>/dev/null\\n+}\\n+\\n+gate_processes() {\\n+    pgrep -fl \\\"$GATE_PATTERN\\\" 2>/dev/null | grep -v \\\"suite_lock.sh \\\\(status\\\\|clear-stale\\\\)\\\" || true\\n+}\\n+\\n+mint_token() {\\n+    printf '%s-%s-%s' \\\"$(hostname -s 2>/dev/null || echo host)\\\" \\\"$(now)\\\" \\\"$$\\\"\\n+}\\n+\\n+# --- commands -----------------------------------------------------------------\\n+\\n+cmd=\\\"${1:-}\\\"\\n+[ -n \\\"$cmd\\\" ] || usage\\n+shift\\n+\\n+label=\\\"\\\"\\n+token=\\\"\\\"\\n+while [ $# -gt 0 ]; do\\n+    case \\\"$1\\\" in\\n+        --label) label=\\\"${2:-}\\\"; shift 2 ;;\\n+        --token) token=\\\"${2:-}\\\"; shift 2 ;;\\n+        --) shift; break ;;\\n+        -*) usage ;;\\n+        *) break ;;\\n+    esac\\n+done\\n+\\n+case \\\"$cmd\\\" in\\n+    run)\\n+        [ $# -gt 0 ] || usage\\n+        # Re-entrant under an enclosing run on the same lock: the enclosing\\n+        # holder's token is in the environment and still on the owner file.\\n+        if [ -n \\\"${SAW_SUITE_LOCK_HELD:-}\\\" ] && [ \\\"$(owner_field token)\\\" = \\\"$SAW_SUITE_LOCK_HELD\\\" ]; then\\n+            exec \\\"$@\\\"\\n+        fi\\n+        token=\\\"run-$(mint_token)\\\"\\n+        wait_and_take \\\"$$\\\" \\\"$token\\\" \\\"${label:-$1}\\\"\\n+        rc=$?\\n+        [ $rc -eq 0 ] || exit 75\\n+        trap 'remove_if token \\\"$token\\\"; exit 130' INT\\n+        trap 'remove_if token \\\"$token\\\"; exit 143' TERM\\n+        SAW_SUITE_LOCK_HELD=\\\"$token\\\" \\\"$@\\\"\\n+        rc=$?\\n+        trap - INT TERM\\n+        remove_if token \\\"$token\\\"\\n+        exit $rc\\n+        ;;\\n+    acquire)\\n+        [ -n \\\"$token\\\" ] || token=\\\"$(mint_token)\\\"\\n+        wait_and_take \\\"\\\" \\\"$token\\\" \\\"${label:-$token}\\\"\\n+        rc=$?\\n+        case $rc in\\n+            0) echo \\\"suite lock: acquired; release with --token $token\\\" >&2; echo \\\"$token\\\" ;;\\n+            2) echo \\\"suite lock: already held by this token; nothing to do\\\" >&2; echo \\\"$token\\\" ;;\\n+            *) exit 75 ;;\\n+        esac\\n+        ;;\\n+    release)\\n+        [ -n \\\"$token\\\" ] || { echo \\\"suite lock: release needs --token T\\\" >&2; exit 2; }\\n+        remove_if token \\\"$token\\\"\\n+        ;;\\n+    status)\\n+        describe_holder\\n+        if [ -d \\\"$LOCK\\\" ]; then\\n+            pid=\\\"$(owner_field pid)\\\"\\n+            if [ -n \\\"$pid\\\" ]; then\\n+                if pid_alive \\\"$pid\\\"; then echo \\\"holder pid $pid: alive\\\"; else echo \\\"holder pid $pid: DEAD\\\"; fi\\n+            else\\n+                echo \\\"no holder pid recorded; gate processes on this host:\\\"\\n+                gate_processes\\n+            fi\\n+            exit 0\\n+        fi\\n+        exit 1\\n+        ;;\\n+    clear-stale)\\n+        meta_enter\\n+        if [ ! -d \\\"$LOCK\\\" ]; then meta_leave; echo \\\"suite lock: free\\\"; exit 0; fi\\n+        pid=\\\"$(owner_field pid)\\\"\\n+        if [ ! -f \\\"$OWNER\\\" ]; then\\n+            if [ \\\"$(age_of \\\"$LOCK\\\")\\\" -gt \\\"$ABANDON_SECONDS\\\" ]; then\\n+                echo \\\"suite lock: clearing an unpublished lock abandoned $(age_of \\\"$LOCK\\\")s ago\\\"\\n+                rmdir \\\"$LOCK\\\" 2>/dev/null || rm -rf \\\"$LOCK\\\"\\n+                meta_leave; exit 0\\n+            fi\\n+            meta_leave\\n+            echo \\\"suite lock: REFUSED — an acquisition is publishing (age $(age_of \\\"$LOCK\\\")s); retry shortly\\\" >&2\\n+            exit 1\\n+        fi\\n+        if [ -z \\\"$pid\\\" ]; then\\n+            meta_leave\\n+            echo \\\"suite lock: REFUSED — token-held ($(describe_holder)); only its taker releases it\\\" >&2\\n+            exit 1\\n+        fi\\n+        if pid_alive \\\"$pid\\\"; then\\n+            meta_leave\\n+            echo \\\"suite lock: REFUSED — holder pid $pid is alive\\\" >&2\\n+            exit 1\\n+        fi\\n+        running=\\\"$(gate_processes)\\\"\\n+        if [ -n \\\"$running\\\" ]; then\\n+            meta_leave\\n+            echo \\\"suite lock: REFUSED — pid $pid is dead but gate processes are running:\\\" >&2\\n+            echo \\\"$running\\\" >&2\\n+            exit 1\\n+        fi\\n+        echo \\\"suite lock: clearing — $(describe_holder); pid $pid is dead and no gate process runs\\\"\\n+        rm -f \\\"$OWNER\\\"\\n+        rmdir \\\"$LOCK\\\" 2>/dev/null || rm -rf \\\"$LOCK\\\"\\n+        meta_leave\\n+        ;;\\n+    *)\\n+        usage\\n+        ;;\\n+esac\\ndiff --git a/tools/test_suite_lock.py b/tools/test_suite_lock.py\\nnew file mode 100644\\nindex 00000000..330f6a74\\n--- /dev/null\\n+++ b/tools/test_suite_lock.py\\n@@ -0,0 +1,223 @@\\n+\\\"\\\"\\\"The suite lock's contract (SL-374): owner-checked, generation-safe release.\\n+\\n+Runs tools/suite_lock.sh against a private lock path, so it never touches the\\n+real lock. Each check states the rule it pins:\\n+  1. `run` holds the lock for the command's whole life and returns ITS status,\\n+     with the token exported to the command.\\n+  2. A release by a caller that does not hold the lock leaves the holder\\n+     unchanged.\\n+  3. `acquire` mints a token and prints it; the same token again is the\\n+     idempotent retry; a foreign token cannot release.\\n+  4. Failed owner publication does not run the command and leaves no lock.\\n+  5. Ownership transitions serialize through the critical section: a release\\n+     issued while the section is held does not complete until it is freed,\\n+     and by then the lock belongs to a new generation it must not remove.\\n+  6. `clear-stale` refuses a live pid, refuses a dead pid while a matching\\n+     gate process runs, clears a dead pid otherwise, and clears an\\n+     unpublished acquisition only once abandoned.\\n+  7. A second `run` waits for the first, synchronized on publication.\\n+  8. A nested `run` under an enclosing run is re-entrant (no self-deadlock).\\n+\\\"\\\"\\\"\\n+import os\\n+import shutil\\n+import subprocess\\n+import sys\\n+import tempfile\\n+import time\\n+\\n+REPO = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))\\n+SCRIPT = os.path.join(REPO, \\\"tools\\\", \\\"suite_lock.sh\\\")\\n+\\n+\\n+def sl(env, *args, **kw):\\n+    # A bounded wait: a check that never returns is a failure to report.\\n+    kw.setdefault(\\\"timeout\\\", 30)\\n+    return subprocess.run([\\\"sh\\\", SCRIPT, *args], env=env, capture_output=True,\\n+                          text=True, **kw)\\n+\\n+\\n+def owner(lock):\\n+    try:\\n+        with open(os.path.join(lock, \\\"owner\\\")) as f:\\n+            return dict(line.strip().split(\\\"=\\\", 1) for line in f if \\\"=\\\" in line)\\n+    except OSError:\\n+        return None\\n+\\n+\\n+def wait_for_owner(lock, deadline=10.0):\\n+    t0 = time.time()\\n+    while time.time() - t0 < deadline:\\n+        o = owner(lock)\\n+        if o:\\n+            return o\\n+        time.sleep(0.02)\\n+    return None\\n+\\n+\\n+def clear(lock):\\n+    shutil.rmtree(lock, ignore_errors=True)\\n+    shutil.rmtree(lock + \\\".meta\\\", ignore_errors=True)\\n+\\n+\\n+def main() -> int:\\n+    problems = []\\n+    with tempfile.TemporaryDirectory() as tmp:\\n+        lock = os.path.join(tmp, \\\"lock\\\")\\n+        meta = lock + \\\".meta\\\"\\n+        # A private lock path, fast waits, a short abandonment age, and a gate\\n+        # pattern that matches no process (the host may be running a real\\n+        # gate while this probe runs).\\n+        env = dict(os.environ, SAW_SUITE_LOCK=lock, SAW_SUITE_LOCK_WAIT=\\\"1\\\",\\n+                   SAW_SUITE_LOCK_ABANDON=\\\"2\\\",\\n+                   SAW_SUITE_LOCK_GATE_PATTERN=\\\"^no-such-gate-process$\\\")\\n+\\n+        # 1. run: held during, token exported, released after, status kept.\\n+        r = sl(env, \\\"run\\\", \\\"--label\\\", \\\"probe\\\", \\\"--\\\", \\\"sh\\\", \\\"-c\\\",\\n+               f\\\"grep -q \\\\\\\"^label=probe$\\\\\\\" '{lock}/owner' && \\\"\\n+               f\\\"grep -q \\\\\\\"^token=$SAW_SUITE_LOCK_HELD$\\\\\\\" '{lock}/owner' && exit 7\\\")\\n+        if r.returncode != 7:\\n+            problems.append(f\\\"run: expected the command's status 7, got {r.returncode}: {r.stderr}\\\")\\n+        if os.path.isdir(lock):\\n+            problems.append(\\\"run: lock still held after the command finished\\\")\\n+\\n+        # 2. foreign release leaves the holder unchanged.\\n+        clear(lock)\\n+        r = sl(env, \\\"acquire\\\", \\\"--token\\\", \\\"holder-a\\\", \\\"--label\\\", \\\"a\\\")\\n+        before = owner(lock)\\n+        r = sl(env, \\\"release\\\", \\\"--token\\\", \\\"not-mine\\\")\\n+        if r.returncode != 0 or owner(lock) != before:\\n+            problems.append(\\\"foreign release: must leave the holder unchanged and exit 0\\\")\\n+        r = sl(env, \\\"release\\\", \\\"--token\\\", \\\"holder-a\\\")\\n+        if os.path.isdir(lock):\\n+            problems.append(\\\"owner release: the holder's release must remove the lock\\\")\\n+\\n+        # 3. minted token, idempotent retry, foreign token cannot release.\\n+        clear(lock)\\n+        r = sl(env, \\\"acquire\\\", \\\"--label\\\", \\\"split use\\\")\\n+        minted = r.stdout.strip()\\n+        if r.returncode != 0 or not minted or (owner(lock) or {}).get(\\\"token\\\") != minted:\\n+            problems.append(f\\\"acquire: must print the minted token that owns the lock (rc={r.returncode}, out={minted!r})\\\")\\n+        r = sl(env, \\\"acquire\\\", \\\"--token\\\", minted)\\n+        if r.returncode != 0 or r.stdout.strip() != minted or (owner(lock) or {}).get(\\\"token\\\") != minted:\\n+            problems.append(\\\"acquire with the holding token must be the idempotent retry\\\")\\n+        r = sl(env, \\\"release\\\", \\\"--token\\\", \\\"someone-else\\\")\\n+        if (owner(lock) or {}).get(\\\"token\\\") != minted:\\n+            problems.append(\\\"release with a foreign token changed the holder\\\")\\n+        r = sl(env, \\\"release\\\", \\\"--token\\\", minted)\\n+        if os.path.isdir(lock):\\n+            problems.append(\\\"release with the minted token left the lock in place\\\")\\n+\\n+        # 4. failed publication: no command run, no lock left.\\n+        clear(lock)\\n+        marker = os.path.join(tmp, \\\"ran\\\")\\n+        r = subprocess.run([\\\"sh\\\", \\\"-c\\\", f\\\"umask 0222; sh '{SCRIPT}' run -- touch '{marker}'\\\"],\\n+                           env=env, capture_output=True, text=True, timeout=30)\\n+        if r.returncode == 0 or os.path.exists(marker):\\n+            problems.append(f\\\"failed publication must not run the command (rc={r.returncode}, ran={os.path.exists(marker)})\\\")\\n+        if os.path.isdir(lock):\\n+            problems.append(\\\"failed publication left a lock directory behind\\\")\\n+\\n+        # 5. serialization: a release blocks on the section, then sees the new\\n+        #    generation and must not remove it.\\n+        clear(lock)\\n+        sl(env, \\\"acquire\\\", \\\"--token\\\", \\\"gen-1\\\")\\n+        os.makedirs(meta)                      # hold the critical section\\n+        rel = subprocess.Popen([\\\"sh\\\", SCRIPT, \\\"release\\\", \\\"--token\\\", \\\"gen-1\\\"], env=env,\\n+                               stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True)\\n+        time.sleep(0.5)\\n+        if rel.poll() is not None:\\n+            problems.append(\\\"release completed while the critical section was held\\\")\\n+        # hand the lock to a new generation by the same protocol, outside the\\n+        # section: release gen-1 and acquire gen-2 through the helper is\\n+        # impossible while we hold the section, so switch the owner file the\\n+        # way a completed handoff would have left it.\\n+        with open(os.path.join(lock, \\\"owner\\\"), \\\"w\\\") as f:\\n+            f.write(\\\"pid=\\\\ntoken=gen-2\\\\nhost=x\\\\nlabel=y\\\\nstarted=z\\\\n\\\")\\n+        os.rmdir(meta)                         # free the section\\n+        try:\\n+            rel.wait(timeout=10)\\n+        except subprocess.TimeoutExpired:\\n+            rel.kill()\\n+            problems.append(\\\"release never completed after the section was freed\\\")\\n+        if (owner(lock) or {}).get(\\\"token\\\") != \\\"gen-2\\\":\\n+            problems.append(\\\"a delayed release removed a newer generation\\\")\\n+        sl(env, \\\"release\\\", \\\"--token\\\", \\\"gen-2\\\")\\n+\\n+        # 6. clear-stale.\\n+        clear(lock)\\n+        sleeper = subprocess.Popen([\\\"sleep\\\", \\\"30\\\"])\\n+        try:\\n+            os.makedirs(lock)\\n+            with open(os.path.join(lock, \\\"owner\\\"), \\\"w\\\") as f:\\n+                f.write(f\\\"pid={sleeper.pid}\\\\ntoken=t\\\\nhost=x\\\\nlabel=live\\\\nstarted=z\\\\n\\\")\\n+            r = sl(env, \\\"clear-stale\\\")\\n+            if r.returncode == 0 or not os.path.isdir(lock):\\n+                problems.append(\\\"clear-stale cleared a lock whose pid is alive\\\")\\n+            r = sl(env, \\\"status\\\")\\n+            if \\\"alive\\\" not in r.stdout:\\n+                problems.append(f\\\"status must report a live holder: {r.stdout!r}\\\")\\n+        finally:\\n+            sleeper.kill()\\n+            sleeper.wait()\\n+        # dead pid but a matching gate process still runs: refused\\n+        gate = subprocess.Popen([\\\"sleep\\\", \\\"31\\\"])\\n+        try:\\n+            genv = dict(env, SAW_SUITE_LOCK_GATE_PATTERN=\\\"sleep 31\\\")\\n+            r = sl(genv, \\\"clear-stale\\\")\\n+            if r.returncode == 0 or not os.path.isdir(lock):\\n+                problems.append(\\\"clear-stale cleared a dead-pid lock while a gate process was running\\\")\\n+        finally:\\n+            gate.kill()\\n+            gate.wait()\\n+        r = sl(env, \\\"clear-stale\\\")\\n+        if r.returncode != 0 or os.path.isdir(lock):\\n+            problems.append(f\\\"clear-stale must clear a dead-pid lock with no gate running: rc={r.returncode} {r.stderr}\\\")\\n+        # unpublished acquisition: refused while fresh, cleared once abandoned\\n+        os.makedirs(lock)\\n+        r = sl(env, \\\"clear-stale\\\")\\n+        if r.returncode == 0 or not os.path.isdir(lock):\\n+            problems.append(\\\"clear-stale removed a fresh unpublished acquisition\\\")\\n+        time.sleep(2.5)\\n+        r = sl(env, \\\"clear-stale\\\")\\n+        if r.returncode != 0 or os.path.isdir(lock):\\n+            problems.append(\\\"clear-stale must clear an abandoned unpublished acquisition\\\")\\n+\\n+        # 7. a second run waits for the first, synchronized on publication.\\n+        clear(lock)\\n+        first = subprocess.Popen([\\\"sh\\\", SCRIPT, \\\"run\\\", \\\"--\\\", \\\"sleep\\\", \\\"2\\\"], env=env)\\n+        try:\\n+            if not wait_for_owner(lock):\\n+                problems.append(\\\"first run never published its owner\\\")\\n+            t0 = time.time()\\n+            r = sl(env, \\\"run\\\", \\\"--\\\", \\\"true\\\")\\n+            waited = time.time() - t0\\n+            if waited < 1.0:\\n+                problems.append(f\\\"second run did not wait for the first (waited {waited:.1f}s)\\\")\\n+            if r.returncode != 0:\\n+                problems.append(f\\\"second run failed: {r.stderr}\\\")\\n+        finally:\\n+            first.wait(timeout=10)\\n+\\n+        # 8. re-entrancy: a run inside a run on the same lock does not wait.\\n+        clear(lock)\\n+        r = sl(env, \\\"run\\\", \\\"--\\\", \\\"sh\\\", SCRIPT, \\\"run\\\", \\\"--\\\", \\\"sh\\\", \\\"-c\\\", \\\"exit 5\\\", timeout=15)\\n+        if r.returncode != 5:\\n+            problems.append(f\\\"nested run must be re-entrant and keep the status (rc={r.returncode}): {r.stderr[-200:]}\\\")\\n+        if os.path.isdir(lock):\\n+            problems.append(\\\"nested run left the lock held\\\")\\n+\\n+    if problems:\\n+        print(\\\"SUITE-LOCK GATE FAILED\\\")\\n+        for p in problems:\\n+            print(\\\"  \\\" + p)\\n+        return 1\\n+    print(\\\"suite-lock gate: run holds/returns status with the token exported, a \\\"\\n+          \\\"foreign release leaves the holder unchanged, tokens are minted and \\\"\\n+          \\\"retry idempotently, failed publication runs nothing, transitions \\\"\\n+          \\\"serialize and a delayed release spares a new generation, staleness is \\\"\\n+          \\\"verified, a second run waits, a nested run is re-entrant.\\\")\\n+    return 0\\n+\\n+\\n+if __name__ == \\\"__main__\\\":\\n+    sys.exit(main())\\n\",\"diff_bytes\":32402,\"diff_files\":8,\"diff_hunks\":11,\"diff_tail\":\" return 0\\n+\\n+\\n+if __name__ == \\\"__main__\\\":\\n+    sys.exit(main())\\n\",\"id\":\"SL-374.p1\",\"request_id\":\"pid-77915-1790208873-12000\"}","request_id":"pid-77915-1790208873-12000","sequence":1398,"timestamp":"1790208875"}
---
