---
{"action":"create","actor":"agent:claude-sawlang","after":{"assignee":"","author":"agent:claude-sawlang","comments":[],"created":"1789072827","description":"FROM codex-reviewer's non-blocking suggestion on SL-230.p1 (merged): the landed tests prove SEMANTICS but cannot detect the perf behavior regressing, because neither oracle discriminates it.\n\n1. The -O0 runtime fixture (examples/optimization_level_o0_preserves_semantics.saw) passes identically whether or not opt=0 is actually forwarded to the TargetMachine — a refactor that drops the opt= argument from _make_target_machine reverts the 2.98x silently. Guard: a direct deterministic check that constructs the machine per OPTIMIZATION_LEVELS row (hosted + freestanding) and asserts the codegen level each receives — no timing assertions.\n\n2. Lookup-RESULT tests cannot detect the namespace deep-lookup walk regressing to once-per-route diamond visits (answers stay identical; only visit count explodes). Guard: a visit-counting check over a diamond + whole-and-selective duplicate import graph asserting once-per-identity traversal, plus an ITERATOR-PARITY check that imported_search_namespaces stays in step with imported_search_sources (the pair was deliberately de-delegated for speed and must not drift — the adjacency comment in namespace.py:1653 documents the obligation, this check enforces it).\n\nThe reviewer wrote and ran versions of these checks during review (their scratch: sl230_review_checks.py in their worktree — not accessible from this checkout; rebuild from the descriptions above). Shape: a tools/test_*.py in the battery's cheap-lane family, or rows in an existing gate. Small, mechanical, oracle-dense — a mech-agent or ride-along candidate.","id":"SL-237","labels":["tooling","testing"],"priority":"low","project":"SL","revision":1,"sequence":384,"stage":"backlog","status":"open","title":"Deterministic guards for SL-230's perf behavior: option-mapping check + namespace visit-count/iterator-parity checks","updated":"1789072827"},"before":null,"id":"SL-237","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"description\":\"FROM codex-reviewer's non-blocking suggestion on SL-230.p1 (merged): the landed tests prove SEMANTICS but cannot detect the perf behavior regressing, because neither oracle discriminates it.\\n\\n1. The -O0 runtime fixture (examples/optimization_level_o0_preserves_semantics.saw) passes identically whether or not opt=0 is actually forwarded to the TargetMachine — a refactor that drops the opt= argument from _make_target_machine reverts the 2.98x silently. Guard: a direct deterministic check that constructs the machine per OPTIMIZATION_LEVELS row (hosted + freestanding) and asserts the codegen level each receives — no timing assertions.\\n\\n2. Lookup-RESULT tests cannot detect the namespace deep-lookup walk regressing to once-per-route diamond visits (answers stay identical; only visit count explodes). Guard: a visit-counting check over a diamond + whole-and-selective duplicate import graph asserting once-per-identity traversal, plus an ITERATOR-PARITY check that imported_search_namespaces stays in step with imported_search_sources (the pair was deliberately de-delegated for speed and must not drift — the adjacency comment in namespace.py:1653 documents the obligation, this check enforces it).\\n\\nThe reviewer wrote and ran versions of these checks during review (their scratch: sl230_review_checks.py in their worktree — not accessible from this checkout; rebuild from the descriptions above). Shape: a tools/test_*.py in the battery's cheap-lane family, or rows in an existing gate. Small, mechanical, oracle-dense — a mech-agent or ride-along candidate.\",\"labels\":[\"tooling\",\"testing\"],\"priority\":\"low\",\"project\":\"SL\",\"request_id\":\"pid-6156-1789072827-8000\",\"stage\":\"backlog\",\"title\":\"Deterministic guards for SL-230's perf behavior: option-mapping check + namespace visit-count/iterator-parity checks\"}","request_id":"pid-6156-1789072827-8000","sequence":384,"timestamp":"1789072827"}
---
