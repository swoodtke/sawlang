---
{"action":"create","actor":"agent:claude-sawperf","after":{"assignee":"","author":"agent:claude-sawperf","comments":[],"created":"1789066227","description":"Follow-up from the compile-performance work on SL-230; filed so it is not\nlost. Backlog, no implementation order assigned.\n\n## The shape\n\n`Namespace` answers \"find this name in me or in anything I import,\ntransitively\" three times, once per table:\n\n* `_lookup_struct_deep` (sawc/namespace.py)\n* `_lookup_type_alias_deep`\n* `_lookup_enum_deep`\n\nThey are the same walk over the same `imported_search_namespaces()` iteration,\ndiffering only in which per-namespace lookup they call\n(`lookup_struct` / `lookup_type_alias` / `lookup_enum`). The triplication\npredates SL-230; that patch added a `_seen` dedup set and had to add the same\nfive lines to each of the three.\n\nBrief obligation 1 is the reason to write this down: a rule quantifying over\n\"every position where X appears\" is either a funnel with named entry points or\na matrix its tests cover row by row. Three hand-copied walks are neither, and\nthe failure mode is exactly what SL-230 ran into — a correctness-relevant\nchange (cycle termination, dedup) has to be applied three times and can be\napplied twice.\n\n## Why it was NOT folded in SL-230\n\nMeasurement, not preference. This is the hottest iteration in the type\nchecker: on a six-module app `imported_search_namespaces` yields ~40 M times\nand `_lookup_type_alias_deep` was entered ~20 M times before the dedup. A\nshared `_lookup_deep(table_lookup, name, _seen)` helper adds a Python call\nframe per visit on that path, and SL-230 was a performance patch — folding a\nstructure change with an unmeasured cost into it would have made its A/B\nnumbers mean something else. It is a separate judgement.\n\n## What a fix would have to establish\n\n1. One walk, with a docstring naming its three entry points.\n2. An A/B showing the funnel does not give back a meaningful part of SL-230's\n   measured -5.7% on a module-heavy program (the sawtracker server is the\n   workload that exercises it; a shallow program such as `devtools/irdet` shows\n   no difference either way and cannot police this).\n3. `irdet --all`, since this is an iteration order.\n\nBound-method dispatch or passing the table dict rather than a closure may make\nthe frame cost negligible; that is the thing to measure, not assume.\n\nRelated: SL-230 (the perf work that added the dedup), SL-232 (the separate,\nmuch larger back-end finding).\n","id":"SL-233","labels":["perf"],"priority":"normal","project":"SL","revision":1,"sequence":370,"stage":"backlog","status":"open","title":"the three deep-lookup walks in Namespace want one funnel (obligation 1)","updated":"1789066227"},"before":null,"id":"SL-233","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawperf\",\"description\":\"Follow-up from the compile-performance work on SL-230; filed so it is not\\nlost. Backlog, no implementation order assigned.\\n\\n## The shape\\n\\n`Namespace` answers \\\"find this name in me or in anything I import,\\ntransitively\\\" three times, once per table:\\n\\n* `_lookup_struct_deep` (sawc/namespace.py)\\n* `_lookup_type_alias_deep`\\n* `_lookup_enum_deep`\\n\\nThey are the same walk over the same `imported_search_namespaces()` iteration,\\ndiffering only in which per-namespace lookup they call\\n(`lookup_struct` / `lookup_type_alias` / `lookup_enum`). The triplication\\npredates SL-230; that patch added a `_seen` dedup set and had to add the same\\nfive lines to each of the three.\\n\\nBrief obligation 1 is the reason to write this down: a rule quantifying over\\n\\\"every position where X appears\\\" is either a funnel with named entry points or\\na matrix its tests cover row by row. Three hand-copied walks are neither, and\\nthe failure mode is exactly what SL-230 ran into — a correctness-relevant\\nchange (cycle termination, dedup) has to be applied three times and can be\\napplied twice.\\n\\n## Why it was NOT folded in SL-230\\n\\nMeasurement, not preference. This is the hottest iteration in the type\\nchecker: on a six-module app `imported_search_namespaces` yields ~40 M times\\nand `_lookup_type_alias_deep` was entered ~20 M times before the dedup. A\\nshared `_lookup_deep(table_lookup, name, _seen)` helper adds a Python call\\nframe per visit on that path, and SL-230 was a performance patch — folding a\\nstructure change with an unmeasured cost into it would have made its A/B\\nnumbers mean something else. It is a separate judgement.\\n\\n## What a fix would have to establish\\n\\n1. One walk, with a docstring naming its three entry points.\\n2. An A/B showing the funnel does not give back a meaningful part of SL-230's\\n   measured -5.7% on a module-heavy program (the sawtracker server is the\\n   workload that exercises it; a shallow program such as `devtools/irdet` shows\\n   no difference either way and cannot police this).\\n3. `irdet --all`, since this is an iteration order.\\n\\nBound-method dispatch or passing the table dict rather than a closure may make\\nthe frame cost negligible; that is the thing to measure, not assume.\\n\\nRelated: SL-230 (the perf work that added the dedup), SL-232 (the separate,\\nmuch larger back-end finding).\\n\",\"labels\":[\"perf\"],\"project\":\"SL\",\"request_id\":\"pid-40950-1789066227-10000\",\"stage\":\"backlog\",\"title\":\"the three deep-lookup walks in Namespace want one funnel (obligation 1)\"}","request_id":"pid-40950-1789066227-10000","sequence":370,"timestamp":"1789066227"}
---
