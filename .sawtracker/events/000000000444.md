---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-247.p1 (revision 1): minivm M4: enums + exhaustive statement match (branch commit 1d37cf75)","created":"1789089095","id":"c1"}],"created":"1789086342","description":"SL-242 epic child, landing patch 5/14. Nominal payload-free enums (incl. the real TokenKind in isolation), equality, exhaustive match with qualified/unqualified cases + wildcard. Contract: prototypes/minivm/M4_ENUMS.md. Patch = branch commit 1d37cf75.","id":"SL-247","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":444,"stage":"queued","status":"open","title":"minivm M4: payload-free enums + exhaustive statement match","updated":"1789089095"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086342","description":"SL-242 epic child, landing patch 5/14. Nominal payload-free enums (incl. the real TokenKind in isolation), equality, exhaustive match with qualified/unqualified cases + wildcard. Contract: prototypes/minivm/M4_ENUMS.md. Patch = branch commit 1d37cf75.","id":"SL-247","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":409,"stage":"queued","status":"open","title":"minivm M4: payload-free enums + exhaustive statement match","updated":"1789086342"},"id":"SL-247","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/DESIGN.md b/prototypes/minivm/DESIGN.md\\nindex 0a4d420a..4202df14 100644\\n--- a/prototypes/minivm/DESIGN.md\\n+++ b/prototypes/minivm/DESIGN.md\\n@@ -20,7 +20,8 @@ The original Int32-only contract below records the baseline.\\n literal type, and signed-minimum remainder behavior.\\n [M2_RECORDS.md](M2_RECORDS.md) adds value records and flattened call/result layouts.\\n [M3_SCALAR_CONTROL.md](M3_SCALAR_CONTROL.md) adds short-circuit operators, compound\\n-assignments and module constants.\\n+assignments and module constants. [M4_ENUMS.md](M4_ENUMS.md) adds nominal\\n+payload-free enums and exhaustive statement match.\\n \\n ## Source contract\\n \\ndiff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex f64f2a68..6697ea87 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -6,7 +6,9 @@ support. A feature is included only when that file uses it, or when it is an\\n unavoidable implementation dependency of one of the public std operations it\\n calls. This inventory was taken at the 43-case Int32/Bool baseline. M1 now supplies\\n the numeric foundation; M2 supplies scalar value records, and M3 adds module\\n-constants, compound assignment and short-circuit Booleans (136 total cases).\\n+constants, compound assignment and short-circuit Booleans. M4 supplies payload-free\\n+enums and exhaustive statement match, including the actual TokenKind declaration\\n+in isolation (161 total cases).\\n Strings, collections, references, generics and methods remain outstanding.\\n The dependency groups below are an inventory, not the implementation commit order;\\n the numbered milestone designs define each isolated slice.\\ndiff --git a/prototypes/minivm/M4_ENUMS.md b/prototypes/minivm/M4_ENUMS.md\\nindex ba134c57..15259e91 100644\\n--- a/prototypes/minivm/M4_ENUMS.md\\n+++ b/prototypes/minivm/M4_ENUMS.md\\n@@ -1,6 +1,6 @@\\n # M4: payload-free enums and exhaustive statement match\\n \\n-Status: design, after M3. This isolates the TokenKind representation and dispatch\\n+Status: implemented after M3 (781a7fff); all 161 integration cases pass. This isolates the TokenKind representation and dispatch\\n required by the lexer. Value-producing match, implicit tails, strings, and payload\\n enums remain later slices. No new instruction opcode is required.\\n \\n@@ -33,7 +33,9 @@ line: Int, col: Int }`, and `Program.enums`. A value occupies one scalar slot;\\n its canonical immediate/cell is the case's zero-based ordinal. Enum identity stays\\n in slot types, function signatures and record fields. No integer helper should\\n classify enums as integers. `scalar_type` includes enums so layout/copy/call ABI\\n-remain unchanged, while printing still accepts only numeric and Bool values.\\n+remain unchanged, while `printable_type` accepts only numeric and Bool values.\\n+`validate_enums(enums: &Vector<EnumIR>) -> Result<Void, ProtoError>` checks\\n+declaration shape; the verifier checks all type references before instructions.\\n \\n The record layout routine does not dereference enum metadata: enums flatten to\\n one word. Full Program verification must separately validate enum type indices\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 6e399085..58b20655 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -45,9 +45,12 @@ func main() {\\n Single-file programs support signed and unsigned 8/16/32/64-bit integers,\\n `Int`/`UInt` (64-bit), `Byte`, `Bool`, functions, positional calls,\\n recursion, `let`/`var`, assignment, lexical scopes, statement `if`/`else` and\\n-`while`, explicit returns, and scalar printing. Named structs can contain scalars\\n+`while`, explicit returns, and numeric/Bool printing. Named structs can contain scalars\\n or nested structs, with\\n field access, mutable fields, value copying, and function parameters/results.\\n+Payload-free enums support copying, equality, record fields and function\\n+parameters/results. Exhaustive statement `match` supports qualified or unqualified\\n+cases and a final wildcard; see [M4_ENUMS.md](M4_ENUMS.md).\\n Record layouts are limited to 256 words and 128 nesting levels; see\\n [M2_RECORDS.md](M2_RECORDS.md). Arithmetic is checked; signed\\n division truncates toward zero. Overflow and division by zero print an error\\n@@ -80,18 +83,20 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The first three milestones\\n-pass all 136 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The first four milestones\\n+pass all 161 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw` and\\n-`tests/record_contract.saw`; build and run them with the Python compiler.\\n-Use `--section numbers`, `--section records`, or `--section control` for an isolated\\n+`tests/record_contract.saw`, and `tests/enum_contract.saw`; build and run them\\n+with the Python compiler.\\n+Use `--section numbers`, `--section records`, `--section control`, or\\n+`--section enums` for an isolated\\n integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\n schema and semantics. [LEXER_DEPENDENCIES.md](LEXER_DEPENDENCIES.md) tracks the\\n remaining dependencies needed to compile the existing lexer with this prototype.\\n-[M4_ENUMS.md](M4_ENUMS.md) designs the next isolated slice. Source is\\n+The enum fixtures include the lexer's actual `TokenKind` declaration. Source is\\n split into `frontend.saw`, `model.saw`, `verify.saw`, `vm.saw`, `llvm.saw`, and\\n `main.saw`. The verifier checks structural indices and types; initialization\\n is established by the frontend, so this is not a loader for untrusted bytecode.\\ndiff --git a/prototypes/minivm/examples/enums/all_arms_return.saw b/prototypes/minivm/examples/enums/all_arms_return.saw\\nnew file mode 100644\\nindex 00000000..94e280c5\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/all_arms_return.saw\\n@@ -0,0 +1,15 @@\\n+enum ResultKind { case Good, case Bad, case Unknown }\\n+\\n+func code(value: ResultKind) -> Int {\\n+    match value {\\n+        case Good -> { return 20 },\\n+        case Bad -> { return 40 },\\n+        case Unknown -> { return 60 }\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(code(ResultKind.Good))\\n+    print(code(ResultKind.Bad))\\n+    print(code(ResultKind.Unknown))\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/calls_returns_forward.saw b/prototypes/minivm/examples/enums/calls_returns_forward.saw\\nnew file mode 100644\\nindex 00000000..79fb408d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/calls_returns_forward.saw\\n@@ -0,0 +1,18 @@\\n+func choose(flag: Bool) -> Direction {\\n+    if flag { return Direction.Left }\\n+    return Direction.Right\\n+}\\n+\\n+func same(value: Direction, expected: Direction) -> Bool {\\n+    return value == expected\\n+}\\n+\\n+enum Direction {\\n+    case Left,\\n+    case Right\\n+}\\n+\\n+func main() {\\n+    print(same(choose(true), Direction.Left))\\n+    print(same(choose(false), Direction.Right))\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/lexer_token_kind.saw b/prototypes/minivm/examples/enums/lexer_token_kind.saw\\nnew file mode 100644\\nindex 00000000..9ab6b4b3\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/lexer_token_kind.saw\\n@@ -0,0 +1,118 @@\\n+// TokenKind declaration copied from selfhost/lexer/src/lib.saw.\\n+public enum TokenKind {\\n+    case IntLit,\\n+    case FloatLit,\\n+    case StringLit,\\n+    case InterpString,\\n+    case BoolLit,\\n+    case Ident,\\n+    case Func,\\n+    case Let,\\n+    case Var,\\n+    case If,\\n+    case Else,\\n+    case Guard,\\n+    case Return,\\n+    case True_,\\n+    case False_,\\n+    case Struct,\\n+    case Extension,\\n+    case SelfKw,\\n+    case Init,\\n+    case NoneKw,\\n+    case Enum,\\n+    case Case,\\n+    case Match,\\n+    case While,\\n+    case Break,\\n+    case Continue,\\n+    case Trait,\\n+    case For,\\n+    case In,\\n+    case TypeKw,\\n+    case Extern,\\n+    case As,\\n+    case Try,\\n+    case Catch,\\n+    case Static,\\n+    case Module,\\n+    case Import,\\n+    case Public,\\n+    case Export,\\n+    case Package,\\n+    case Parent,\\n+    case Plus,\\n+    case Minus,\\n+    case Star,\\n+    case Slash,\\n+    case Percent,\\n+    case Eq,\\n+    case Neq,\\n+    case Lt,\\n+    case Gt,\\n+    case Lte,\\n+    case Gte,\\n+    case And,\\n+    case Or,\\n+    case Ampersand,\\n+    case Pipe,\\n+    case Caret,\\n+    case Tilde,\\n+    case WrapAdd,\\n+    case WrapSub,\\n+    case WrapMul,\\n+    case Not_,\\n+    case Move_,\\n+    case Unsafe_,\\n+    case Borrows,\\n+    case Lend,\\n+    case Assign,\\n+    case PlusAssign,\\n+    case MinusAssign,\\n+    case StarAssign,\\n+    case SlashAssign,\\n+    case PercentAssign,\\n+    case AmpAssign,\\n+    case PipeAssign,\\n+    case CaretAssign,\\n+    case ShlAssign,\\n+    case ShrAssign,\\n+    case Question,\\n+    case DoubleQuestion,\\n+    case Exclaim,\\n+    case QuestionDot,\\n+    case DotDot,\\n+    case DotDotEq,\\n+    case Ellipsis,\\n+    case LParen,\\n+    case RParen,\\n+    case LBrace,\\n+    case RBrace,\\n+    case LBracket,\\n+    case RBracket,\\n+    case Comma,\\n+    case Colon,\\n+    case Semicolon,\\n+    case Arrow,\\n+    case Dot,\\n+    case At,\\n+    case HashDirective,\\n+    case DollarParam,\\n+    case Newline,\\n+    case Eof\\n+}\\n+\\n+func classify(kind: TokenKind) -> Int {\\n+    match kind {\\n+        case IntLit -> { return 1 },\\n+        case Eof -> { return 2 },\\n+        case _ -> { return 3 }\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(classify(TokenKind.IntLit))\\n+    print(classify(TokenKind.Eof))\\n+    print(classify(TokenKind.Ident))\\n+    print(TokenKind.Eof == TokenKind.Eof)\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/match_qualified.saw b/prototypes/minivm/examples/enums/match_qualified.saw\\nnew file mode 100644\\nindex 00000000..a4b1d08c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/match_qualified.saw\\n@@ -0,0 +1,13 @@\\n+enum Switch { case Off, case On }\\n+\\n+func show(value: Switch) {\\n+    match value {\\n+        case Switch.Off -> { print(false) },\\n+        case Switch.On -> { print(true) }\\n+    }\\n+}\\n+\\n+func main() {\\n+    show(Switch.On)\\n+    show(Switch.Off)\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/match_unqualified.saw b/prototypes/minivm/examples/enums/match_unqualified.saw\\nnew file mode 100644\\nindex 00000000..3bd40ca6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/match_unqualified.saw\\n@@ -0,0 +1,15 @@\\n+enum Color { case Red, case Green, case Blue }\\n+\\n+func show(value: Color) {\\n+    match value {\\n+        case Red -> { print(1) },\\n+        case Green -> { print(2) },\\n+        case Blue -> { print(3) }\\n+    }\\n+}\\n+\\n+func main() {\\n+    show(Color.Red)\\n+    show(Color.Green)\\n+    show(Color.Blue)\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/match_wildcard.saw b/prototypes/minivm/examples/enums/match_wildcard.saw\\nnew file mode 100644\\nindex 00000000..3fade776\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/match_wildcard.saw\\n@@ -0,0 +1,14 @@\\n+enum Code { case First, case Second, case Third }\\n+\\n+func show(value: Code) {\\n+    match value {\\n+        case First -> { print(10) },\\n+        case _ -> { print(99) }\\n+    }\\n+}\\n+\\n+func main() {\\n+    show(Code.First)\\n+    show(Code.Second)\\n+    show(Code.Third)\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/nested_control.saw b/prototypes/minivm/examples/enums/nested_control.saw\\nnew file mode 100644\\nindex 00000000..eaab1a28\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/nested_control.saw\\n@@ -0,0 +1,27 @@\\n+enum Phase { case Start, case Done }\\n+func classify(a: Phase, b: Phase) -> Int {\\n+    match a {\\n+        case Start -> {\\n+            match b {\\n+                case Start -> { return 10 },\\n+                case Done -> { return 11 }\\n+            }\\n+        },\\n+        case Done -> { return 20 }\\n+    }\\n+}\\n+func main() {\\n+    var phase = Phase.Start\\n+    var count = 0\\n+    while count < 2 {\\n+        match phase {\\n+            case Start -> { phase = Phase.Done },\\n+            case Done -> { phase = Phase.Start }\\n+        }\\n+        count += 1\\n+    }\\n+    print(phase == Phase.Start)\\n+    print(classify(phase, Phase.Start))\\n+    print(classify(phase, Phase.Done))\\n+    print(classify(Phase.Done, phase))\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/record_fields.saw b/prototypes/minivm/examples/enums/record_fields.saw\\nnew file mode 100644\\nindex 00000000..ddf9a3fe\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/record_fields.saw\\n@@ -0,0 +1,18 @@\\n+struct State {\\n+    before: Mode\\n+    after: Mode\\n+    count: Int\\n+}\\n+\\n+enum Mode { case Cold, case Warm }\\n+\\n+func relay(value: State) -> State { return value }\\n+\\n+func main() {\\n+    var state = relay(State(count: 3, after: Mode.Warm, before: Mode.Cold))\\n+    print(state.before == Mode.Cold)\\n+    print(state.after == Mode.Warm)\\n+    state.before = state.after\\n+    print(state.before == Mode.Warm)\\n+    print(state.count)\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/reject_arithmetic.saw b/prototypes/minivm/examples/enums/reject_arithmetic.saw\\nnew file mode 100644\\nindex 00000000..94322489\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_arithmetic.saw\\n@@ -0,0 +1,4 @@\\n+enum Color { case Red, case Green }\\n+func main() {\\n+    let value = Color.Red + Color.Green\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/reject_arm_after_wildcard.saw b/prototypes/minivm/examples/enums/reject_arm_after_wildcard.saw\\nnew file mode 100644\\nindex 00000000..4a61fdb1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_arm_after_wildcard.saw\\n@@ -0,0 +1,7 @@\\n+enum Color { case Red, case Green }\\n+func main() {\\n+    match Color.Red {\\n+        case _ -> { print(1) },\\n+        case Green -> { print(2) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/reject_cast.saw b/prototypes/minivm/examples/enums/reject_cast.saw\\nnew file mode 100644\\nindex 00000000..d0ace880\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_cast.saw\\n@@ -0,0 +1,2 @@\\n+enum Color { case Red }\\n+func main() { print(Color.Red as Int) }\\ndiff --git a/prototypes/minivm/examples/enums/reject_duplicate_arm.saw b/prototypes/minivm/examples/enums/reject_duplicate_arm.saw\\nnew file mode 100644\\nindex 00000000..aacd2257\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_duplicate_arm.saw\\n@@ -0,0 +1,8 @@\\n+enum Color { case Red, case Green }\\n+func main() {\\n+    match Color.Red {\\n+        case Red -> { print(1) },\\n+        case Red -> { print(2) },\\n+        case Green -> { print(3) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/reject_duplicate_case.saw b/prototypes/minivm/examples/enums/reject_duplicate_case.saw\\nnew file mode 100644\\nindex 00000000..421af936\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_duplicate_case.saw\\n@@ -0,0 +1,2 @@\\n+enum Status { case Ready, case Ready }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/enums/reject_duplicate_enum.saw b/prototypes/minivm/examples/enums/reject_duplicate_enum.saw\\nnew file mode 100644\\nindex 00000000..1f92ea32\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_duplicate_enum.saw\\n@@ -0,0 +1,3 @@\\n+enum Status { case Ready }\\n+enum Status { case Done }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/enums/reject_empty.saw b/prototypes/minivm/examples/enums/reject_empty.saw\\nnew file mode 100644\\nindex 00000000..a4edc742\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_empty.saw\\n@@ -0,0 +1,2 @@\\n+enum Empty {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/enums/reject_missing_match_case.saw b/prototypes/minivm/examples/enums/reject_missing_match_case.saw\\nnew file mode 100644\\nindex 00000000..5f3a2c5a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_missing_match_case.saw\\n@@ -0,0 +1,8 @@\\n+enum Color { case Red, case Green, case Blue }\\n+func main() {\\n+    let value = Color.Red\\n+    match value {\\n+        case Red -> { print(1) },\\n+        case Green -> { print(2) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/reject_mixed_equality.saw b/prototypes/minivm/examples/enums/reject_mixed_equality.saw\\nnew file mode 100644\\nindex 00000000..de30c177\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_mixed_equality.saw\\n@@ -0,0 +1,3 @@\\n+enum Color { case Red }\\n+enum Direction { case Red }\\n+func main() { print(Color.Red == Direction.Red) }\\ndiff --git a/prototypes/minivm/examples/enums/reject_mixed_transfer.saw b/prototypes/minivm/examples/enums/reject_mixed_transfer.saw\\nnew file mode 100644\\nindex 00000000..f42ac0a5\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_mixed_transfer.saw\\n@@ -0,0 +1,4 @@\\n+enum Color { case Red }\\n+enum Direction { case Red }\\n+func take(value: Color) {}\\n+func main() { take(Direction.Red) }\\ndiff --git a/prototypes/minivm/examples/enums/reject_payload.saw b/prototypes/minivm/examples/enums/reject_payload.saw\\nnew file mode 100644\\nindex 00000000..044e27cb\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_payload.saw\\n@@ -0,0 +1,2 @@\\n+enum ResultKind { case Good(value: Int), case Bad }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/enums/reject_print.saw b/prototypes/minivm/examples/enums/reject_print.saw\\nnew file mode 100644\\nindex 00000000..160ea184\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_print.saw\\n@@ -0,0 +1,2 @@\\n+enum Color { case Red }\\n+func main() { print(Color.Red) }\\ndiff --git a/prototypes/minivm/examples/enums/reject_record_collision.saw b/prototypes/minivm/examples/enums/reject_record_collision.saw\\nnew file mode 100644\\nindex 00000000..91fc49c7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_record_collision.saw\\n@@ -0,0 +1,3 @@\\n+struct Same { x: Int }\\n+enum Same { case A }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/enums/reject_static_enum.saw b/prototypes/minivm/examples/enums/reject_static_enum.saw\\nnew file mode 100644\\nindex 00000000..026ef037\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_static_enum.saw\\n@@ -0,0 +1,3 @@\\n+enum E { case A }\\n+static X: E = E.A\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/enums/reject_wrong_qualifier.saw b/prototypes/minivm/examples/enums/reject_wrong_qualifier.saw\\nnew file mode 100644\\nindex 00000000..ea22c3dd\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/reject_wrong_qualifier.saw\\n@@ -0,0 +1,8 @@\\n+enum Color { case Red, case Green }\\n+enum Direction { case Left, case Right }\\n+func main() {\\n+    match Color.Red {\\n+        case Direction.Left -> { print(1) },\\n+        case Green -> { print(2) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/subject_once.saw b/prototypes/minivm/examples/enums/subject_once.saw\\nnew file mode 100644\\nindex 00000000..3de62a9f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/subject_once.saw\\n@@ -0,0 +1,13 @@\\n+enum Choice { case Yes, case No }\\n+\\n+func marked(value: Choice) -> Choice {\\n+    print(7)\\n+    return value\\n+}\\n+\\n+func main() {\\n+    match marked(Choice.No) {\\n+        case Yes -> { print(1) },\\n+        case No -> { print(2) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/enums/values_copy_equality.saw b/prototypes/minivm/examples/enums/values_copy_equality.saw\\nnew file mode 100644\\nindex 00000000..3c78f711\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/enums/values_copy_equality.saw\\n@@ -0,0 +1,15 @@\\n+public enum Color {\\n+    case Red,\\n+    case Green,\\n+    case Blue\\n+}\\n+\\n+func main() {\\n+    let original = Color.Red\\n+    var copy = original\\n+    print(copy == original)\\n+    copy = Color.Blue\\n+    print(copy == original)\\n+    print(copy != Color.Green)\\n+    print(original == Color.Red)\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 8f9464b8..4dc7220d 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -60,7 +60,7 @@ func type_name(t: ValueType) -> String {\\n         case I8 -> \\\"Int8\\\", case I16 -> \\\"Int16\\\", case I32 -> \\\"Int32\\\", case I64 -> \\\"Int\\\",\\n         case U8 -> \\\"UInt8\\\", case U16 -> \\\"UInt16\\\", case U32 -> \\\"UInt32\\\", case U64 -> \\\"UInt\\\",\\n         case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case Void -> \\\"Void\\\",\\n-        case Record(_) -> \\\"record\\\"\\n+        case Record(_) -> \\\"record\\\", case Enum(_) -> \\\"enum\\\"\\n     }\\n }\\n \\n@@ -95,6 +95,7 @@ func literal_magnitude(value: String) -> UInt? {\\n struct Compiler {\\n     tokens: Vector<Token>\\n     records: Vector<RecordIR>\\n+    enums: Vector<EnumIR>\\n     statics: Vector<StaticBinding>\\n     signatures: Vector<Signature>\\n     pos: Int\\n@@ -150,6 +151,8 @@ extension Compiler {\\n         if let ty = named_integer_type(t.value) { return ty }\\n         let ri = self.record_index(t.value)\\n         if ri >= 0 { return ValueType.Record(ri) }\\n+        let ei = self.enum_index(t.value)\\n+        if ei >= 0 { return ValueType.Enum(ei) }\\n         problem(\\\"unsupported type `{t.value}`\\\", t.line, t.col)\\n     }\\n \\n@@ -171,12 +174,25 @@ extension Compiler {\\n         -1\\n     }\\n \\n+    func enum_index(&self, name: String) -> Int {\\n+        var i = 0\\n+        while i < self.enums.len() {\\n+            if self.enums[i].name == name { return i }\\n+            i += 1\\n+        }\\n+        -1\\n+    }\\n+\\n     func display_type(&self, ty: ValueType) -> String {\\n         match ty {\\n             case Record(index) -> {\\n                 if index >= 0 && index < self.records.len() { return self.records[index].name }\\n                 \\\"record\\\"\\n             },\\n+            case Enum(index) -> {\\n+                if index >= 0 && index < self.enums.len() { return self.enums[index].name }\\n+                \\\"enum\\\"\\n+            },\\n             case _ -> type_name(ty)\\n         }\\n     }\\n@@ -196,14 +212,20 @@ extension Compiler {\\n                 let _ = self.take()\\n                 let name = try self.expect(TokenKind.Ident, \\\"expected record name\\\")\\n                 if self.reserved_type_name(name.value) || name.value == \\\"print\\\" { return problem(\\\"record name `{name.value}` is reserved\\\", name.line, name.col) }\\n-                if self.record_index(name.value) >= 0 || self.static_index(name.value) >= 0 { return problem(\\\"duplicate module name `{name.value}`\\\", name.line, name.col) }\\n+                if self.record_index(name.value) >= 0 || self.enum_index(name.value) >= 0 || self.static_index(name.value) >= 0 { return problem(\\\"duplicate module name `{name.value}`\\\", name.line, name.col) }\\n                 try! self.records.push(RecordIR(name: name.value, field_names: Vector<String>(),\\n                                                 field_types: Vector<ValueType>(), line: name.line, col: name.col))\\n+            } else if self.at(TokenKind.Enum) {\\n+                let _ = self.take()\\n+                let name = try self.expect(TokenKind.Ident, \\\"expected enum name\\\")\\n+                if self.reserved_type_name(name.value) || name.value == \\\"print\\\" { return problem(\\\"enum name `{name.value}` is reserved\\\", name.line, name.col) }\\n+                if self.record_index(name.value) >= 0 || self.enum_index(name.value) >= 0 || self.static_index(name.value) >= 0 { return problem(\\\"duplicate module name `{name.value}`\\\", name.line, name.col) }\\n+                try! self.enums.push(EnumIR(name: name.value, cases: Vector<String>(), line: name.line, col: name.col))\\n             } else if self.at(TokenKind.Static) {\\n                 let _ = self.take()\\n                 let name = try self.expect(TokenKind.Ident, \\\"expected static name\\\")\\n                 if name.value == \\\"print\\\" || self.reserved_type_name(name.value) { return problem(\\\"static name `{name.value}` is reserved\\\", name.line, name.col) }\\n-                if self.record_index(name.value) >= 0 || self.static_index(name.value) >= 0 {\\n+                if self.record_index(name.value) >= 0 || self.enum_index(name.value) >= 0 || self.static_index(name.value) >= 0 {\\n                     return problem(\\\"duplicate module name `{name.value}`\\\", name.line, name.col)\\n                 }\\n                 try! self.statics.push(StaticBinding(name: name.value, ty: ValueType.Void,\\n@@ -212,7 +234,7 @@ extension Compiler {\\n                 self.skip_newlines()\\n                 continue\\n             } else if not self.at(TokenKind.Func) {\\n-                return self.fail(\\\"only function, record, and static declarations are supported at module scope\\\")\\n+                return self.fail(\\\"only function, record, enum, and static declarations are supported at module scope\\\")\\n             }\\n             while not self.at(TokenKind.LBrace) && not self.at(TokenKind.Eof) { let _ = self.take() }\\n             if self.at(TokenKind.Eof) { return self.fail(\\\"expected declaration body\\\") }\\n@@ -248,12 +270,17 @@ extension Compiler {\\n                 self.skip_newlines()\\n                 continue\\n             }\\n+            if self.at(TokenKind.Enum) {\\n+                try self.scan_enum()\\n+                self.skip_newlines()\\n+                continue\\n+            }\\n             if self.at(TokenKind.Static) {\\n                 try self.scan_static()\\n                 self.skip_newlines()\\n                 continue\\n             }\\n-            let ft = try self.expect(TokenKind.Func, \\\"only function, record, and static declarations are supported at module scope\\\")\\n+            let ft = try self.expect(TokenKind.Func, \\\"only function, record, enum, and static declarations are supported at module scope\\\")\\n             let name = try self.expect(TokenKind.Ident, \\\"expected function name\\\")\\n             if name.value == \\\"print\\\" {\\n                 return problem(\\\"`print` is reserved by the prototype\\\", name.line, name.col)\\n@@ -264,6 +291,9 @@ extension Compiler {\\n             if self.record_index(name.value) >= 0 {\\n                 return problem(\\\"function `{name.value}` conflicts with a record name\\\", name.line, name.col)\\n             }\\n+            if self.enum_index(name.value) >= 0 {\\n+                return problem(\\\"function `{name.value}` conflicts with an enum name\\\", name.line, name.col)\\n+            }\\n             if self.static_index(name.value) >= 0 {\\n                 return problem(\\\"function `{name.value}` conflicts with a static name\\\", name.line, name.col)\\n             }\\n@@ -371,7 +401,7 @@ extension Compiler {\\n         if si < 0 { return problem(\\\"unknown static declaration `{name.value}`\\\", name.line, name.col) }\\n         let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after static name\\\")\\n         let target = try self.parse_type()\\n-        if target == ValueType.Byte || not scalar_type(target) {\\n+        if target == ValueType.Byte || not printable_type(target) {\\n             return problem(\\\"static type must be an integer or Bool\\\", name.line, name.col)\\n         }\\n         let _ = try self.expect(TokenKind.Assign, \\\"static declaration requires an initializer\\\")\\n@@ -461,6 +491,36 @@ extension Compiler {\\n         return\\n     }\\n \\n+    func scan_enum(&var self) -> Result<Void, ProtoError> {\\n+        let _ = self.take()\\n+        let name = try self.expect(TokenKind.Ident, \\\"expected enum name\\\")\\n+        let ei = self.enum_index(name.value)\\n+        if ei < 0 { return problem(\\\"unknown enum declaration `{name.value}`\\\", name.line, name.col) }\\n+        let _ = try self.expect(TokenKind.LBrace, \\\"expected enum body\\\")\\n+        self.skip_newlines()\\n+        var cases = Vector<String>()\\n+        while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n+            let _ = try self.expect(TokenKind.Case, \\\"expected `case` in enum declaration\\\")\\n+            let item = try self.expect(TokenKind.Ident, \\\"expected enum case name\\\")\\n+            if self.at(TokenKind.LParen) { return problem(\\\"payload enum cases are not supported\\\", item.line, item.col) }\\n+            var i = 0\\n+            while i < cases.len() {\\n+                if cases[i] == item.value { return problem(\\\"duplicate enum case `{item.value}`\\\", item.line, item.col) }\\n+                i += 1\\n+            }\\n+            try! cases.push(item.value)\\n+            if self.at(TokenKind.Comma) {\\n+                let _ = self.take()\\n+                self.skip_newlines()\\n+            } else if self.at(TokenKind.Newline) { self.skip_newlines() }\\n+            else if not self.at(TokenKind.RBrace) { return self.fail(\\\"expected comma or newline after enum case\\\") }\\n+        }\\n+        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace for enum\\\")\\n+        if cases.len() == 0 { return problem(\\\"enum must have at least one case\\\", name.line, name.col) }\\n+        self.enums[ei].cases = move cases\\n+        return\\n+    }\\n+\\n     func new_slot(&var self, ty: ValueType) -> Int {\\n         let n = self.slots.len()\\n         try! self.slots.push(ty)\\n@@ -649,6 +709,19 @@ extension Compiler {\\n         }\\n         if self.at(TokenKind.Ident) {\\n             let name = self.take()\\n+            let ei = self.enum_index(name.value)\\n+            if ei >= 0 && self.at(TokenKind.Dot) {\\n+                let _ = self.take()\\n+                let item = try self.expect(TokenKind.Ident, \\\"expected enum case after `.`\\\")\\n+                let ci = self.enum_case_index(ei, item.value)\\n+                if ci < 0 { return problem(\\\"enum `{name.value}` has no case `{item.value}`\\\", item.line, item.col) }\\n+                let ty = ValueType.Enum(ei)\\n+                let dst = self.new_slot(ty)\\n+                let at = self.emit_at(Op.Const, name.line, name.col)\\n+                self.code[at].dst = dst\\n+                self.code[at].immediate = ci\\n+                return expr_value(ty, dst, false)\\n+            }\\n             let ri = self.record_index(name.value)\\n             if ri >= 0 && self.at(TokenKind.LParen) { return self.parse_record_constructor(name, ri) }\\n             if let ty = named_integer_type(name.value) {\\n@@ -683,6 +756,15 @@ extension Compiler {\\n         -1\\n     }\\n \\n+    func enum_case_index(&self, enum_index: Int, name: String) -> Int {\\n+        var i = 0\\n+        while i < self.enums[enum_index].cases.len() {\\n+            if self.enums[enum_index].cases[i] == name { return i }\\n+            i += 1\\n+        }\\n+        -1\\n+    }\\n+\\n     func assignment_ahead(&self) -> Bool {\\n         if not self.at(TokenKind.Ident) { return false }\\n         var i = self.pos + 1\\n@@ -1014,7 +1096,7 @@ extension Compiler {\\n         }\\n         if left.literal && not right.literal { return right.ty }\\n         if right.literal && not left.literal { return left.ty }\\n-        problem(\\\"numeric operands must have the same type\\\", t.line, t.col)\\n+        problem(\\\"operands must have the same type\\\", t.line, t.col)\\n     }\\n \\n     func parse_mul(&var self) -> Result<ExprValue, ProtoError> {\\n@@ -1242,8 +1324,104 @@ extension Compiler {\\n         returns\\n     }\\n \\n+    func parse_match_statement(&var self) -> Result<Bool, ProtoError> {\\n+        let mt = self.take()\\n+        var subject = try self.parse_expr()\\n+        subject = try self.snapshot(subject, &mt)\\n+        var ei = -1\\n+        match subject.ty {\\n+            case Enum(index) -> { ei = index },\\n+            case _ -> { return problem(\\\"match subject must have enum type\\\", mt.line, mt.col) }\\n+        }\\n+        let _ = try self.expect(TokenKind.LBrace, \\\"expected `\\\\{` after match subject\\\")\\n+        self.skip_newlines()\\n+        var covered = Vector<Bool>()\\n+        var i = 0\\n+        while i < self.enums[ei].cases.len() {\\n+            try! covered.push(false)\\n+            i += 1\\n+        }\\n+        var joins = Vector<Int>()\\n+        var pending_false = -1\\n+        var wildcard = false\\n+        var arm_count = 0\\n+        var all_return = true\\n+        while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n+            if wildcard { return self.fail(\\\"match arm follows wildcard\\\") }\\n+            if pending_false >= 0 { self.code[pending_false].target = self.code.len() }\\n+            let ct = try self.expect(TokenKind.Case, \\\"expected `case` in match\\\")\\n+            let pattern = try self.expect(TokenKind.Ident, \\\"expected enum case pattern\\\")\\n+            var ordinal = -1\\n+            if pattern.value == \\\"_\\\" {\\n+                wildcard = true\\n+            } else if self.at(TokenKind.Dot) {\\n+                let qualifier = self.enum_index(pattern.value)\\n+                if qualifier != ei { return problem(\\\"enum qualifier `{pattern.value}` does not match subject enum `{self.enums[ei].name}`\\\", pattern.line, pattern.col) }\\n+                let _ = self.take()\\n+                let item = try self.expect(TokenKind.Ident, \\\"expected qualified enum case\\\")\\n+                ordinal = self.enum_case_index(ei, item.value)\\n+                if ordinal < 0 { return problem(\\\"enum `{self.enums[ei].name}` has no case `{item.value}`\\\", item.line, item.col) }\\n+            } else {\\n+                ordinal = self.enum_case_index(ei, pattern.value)\\n+                if ordinal < 0 { return problem(\\\"enum `{self.enums[ei].name}` has no case `{pattern.value}`\\\", pattern.line, pattern.col) }\\n+            }\\n+            if ordinal >= 0 {\\n+                if covered[ordinal] { return problem(\\\"duplicate match case `{self.enums[ei].cases[ordinal]}`\\\", pattern.line, pattern.col) }\\n+                covered[ordinal] = true\\n+                let constant = self.new_slot(subject.ty)\\n+                let ci = self.emit_at(Op.Const, pattern.line, pattern.col)\\n+                self.code[ci].dst = constant\\n+                self.code[ci].immediate = ordinal\\n+                let equal = self.new_slot(ValueType.Bool)\\n+                let eq = self.emit_at(Op.Eq, pattern.line, pattern.col)\\n+                self.code[eq].dst = equal\\n+                self.code[eq].a = subject.slot\\n+                self.code[eq].b = constant\\n+                let branch = self.emit_at(Op.Branch, pattern.line, pattern.col)\\n+                self.code[branch].a = equal\\n+                pending_false = self.emit_at(Op.Jump, pattern.line, pattern.col)\\n+                self.code[branch].target = self.code.len()\\n+            } else { pending_false = -1 }\\n+            let _ = try self.expect(TokenKind.Arrow, \\\"expected `->` after match pattern\\\")\\n+            let _ = try self.expect(TokenKind.LBrace, \\\"match arms require statement blocks\\\")\\n+            let returns = try self.parse_block()\\n+            if not returns {\\n+                all_return = false\\n+                let join = self.emit_at(Op.Jump, ct.line, ct.col)\\n+                try! joins.push(join)\\n+            }\\n+            arm_count += 1\\n+            self.skip_newlines()\\n+            if self.at(TokenKind.Comma) {\\n+                let _ = self.take()\\n+                self.skip_newlines()\\n+            }\\n+        }\\n+        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace for match\\\")\\n+        if arm_count == 0 { return problem(\\\"non-exhaustive match\\\", mt.line, mt.col) }\\n+        var exhaustive = wildcard\\n+        if not exhaustive {\\n+            exhaustive = true\\n+            i = 0\\n+            while i < covered.len() {\\n+                if not covered[i] { exhaustive = false }\\n+                i += 1\\n+            }\\n+        }\\n+        if not exhaustive { return problem(\\\"non-exhaustive match\\\", mt.line, mt.col) }\\n+        let continuation = self.code.len()\\n+        if pending_false >= 0 { self.code[pending_false].target = continuation }\\n+        i = 0\\n+        while i < joins.len() {\\n+            self.code[joins[i]].target = continuation\\n+            i += 1\\n+        }\\n+        all_return\\n+    }\\n+\\n     func parse_statement(&var self) -> Result<Bool, ProtoError> {\\n         if self.at(TokenKind.Semicolon) { return self.fail(\\\"semicolons are not supported\\\") }\\n+        if self.at(TokenKind.Match) { return self.parse_match_statement() }\\n         if self.at(TokenKind.Let) || self.at(TokenKind.Var) {\\n             let kw = self.take()\\n             let name = try self.expect(TokenKind.Ident, \\\"expected local name\\\")\\n@@ -1332,7 +1510,12 @@ extension Compiler {\\n             self.paren_depth -= 1\\n             self.depth -= 1\\n             if value.ty == ValueType.Void { return problem(\\\"cannot print Void\\\", t.line, t.col) }\\n-            if not scalar_type(value.ty) { return problem(\\\"cannot print a record value\\\", t.line, t.col) }\\n+            if not printable_type(value.ty) {\\n+                match value.ty {\\n+                    case Enum(_) -> { return problem(\\\"cannot print an enum value\\\", t.line, t.col) },\\n+                    case _ -> { return problem(\\\"cannot print a record value\\\", t.line, t.col) }\\n+                }\\n+            }\\n             let at = self.emit_at(Op.Print, t.line, t.col)\\n             self.code[at].a = value.slot\\n             return false\\n@@ -1422,7 +1605,7 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         case Ok(ts) -> move ts,\\n         case Err(e) -> { return problem(e.message, e.line, e.col) }\\n     }\\n-    var compiler = Compiler(tokens: move tokens, records: Vector<RecordIR>(), statics: Vector<StaticBinding>(),\\n+    var compiler = Compiler(tokens: move tokens, records: Vector<RecordIR>(), enums: Vector<EnumIR>(), statics: Vector<StaticBinding>(),\\n                             signatures: Vector<Signature>(), pos: 0, depth: 0,\\n                             expr_depth: 0, paren_depth: 0,\\n                             bindings: Vector<Binding>(), slots: Vector<ValueType>(),\\n@@ -1430,6 +1613,7 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n     try compiler.collect_record_names()\\n     try compiler.scan_signatures()\\n     try validate_records(&compiler.records)\\n+    try validate_enums(&compiler.enums)\\n     var functions = Vector<FuncIR>()\\n     var main_index = -1\\n     var i = 0\\n@@ -1439,5 +1623,5 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         try! functions.push(move f)\\n         i += 1\\n     }\\n-    Program(records: compiler.records.copy(), functions: move functions, main_index: main_index)\\n+    Program(enums: compiler.enums.copy(), records: compiler.records.copy(), functions: move functions, main_index: main_index)\\n }\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex 18ef0ece..934ed307 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -493,6 +493,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             next_block(&var out, instruction_index)\\n         },\\n         case Print -> {\\n+            if not printable_type(function.slots[inst.a]) { return problem(\\\"internal prototype error: non-printable value\\\", inst.line, inst.col) }\\n             put(&var out, \\\"  \\\")\\n             load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n             if function.slots[inst.a] == ValueType.Bool {\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex 0f696db1..3f759532 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -1,7 +1,7 @@\\n public enum ValueType {\\n     case I8, case I16, case I32, case I64,\\n     case U8, case U16, case U32, case U64, case Byte,\\n-    case Bool, case Void, case Record(index: Int)\\n+    case Bool, case Void, case Record(index: Int), case Enum(index: Int)\\n }\\n @synthesize\\n extension ValueType: Equatable {}\\n@@ -105,7 +105,17 @@ public struct FuncIR {\\n     public code: Vector<Instruction>\\n }\\n \\n+public struct EnumIR {\\n+    public name: String\\n+    public cases: Vector<String>\\n+    public line: Int\\n+    public col: Int\\n+}\\n+@synthesize\\n+extension EnumIR: ExplicitCopy {}\\n+\\n public struct Program {\\n+    public enums: Vector<EnumIR>\\n     public records: Vector<RecordIR>\\n     public functions: Vector<FuncIR>\\n     public main_index: Int\\n@@ -148,10 +158,41 @@ public func problem(message: String, line: Int, col: Int) -> ProtoError {\\n     ProtoError(message: message, line: line, col: col)\\n }\\n \\n-public func scalar_type(t: ValueType) -> Bool {\\n+public func printable_type(t: ValueType) -> Bool {\\n     integer_type(t) || t == ValueType.Bool\\n }\\n \\n+public func scalar_type(t: ValueType) -> Bool {\\n+    match t {\\n+        case Enum(_) -> true,\\n+        case _ -> printable_type(t)\\n+    }\\n+}\\n+\\n+public func validate_enums(enums: &Vector<EnumIR>) -> Result<Void, ProtoError> {\\n+    var n = 0\\n+    while n < enums.len() {\\n+        let declaration = enums[n].copy()\\n+        if declaration.cases.len() == 0 { return problem(\\\"enum must have at least one case\\\", declaration.line, declaration.col) }\\n+        var previous = 0\\n+        while previous < n {\\n+            if enums[previous].name == declaration.name { return problem(\\\"duplicate enum\\\", declaration.line, declaration.col) }\\n+            previous += 1\\n+        }\\n+        var current = 0\\n+        while current < declaration.cases.len() {\\n+            previous = 0\\n+            while previous < current {\\n+                if declaration.cases[previous] == declaration.cases[current] { return problem(\\\"duplicate enum case\\\", declaration.line, declaration.col) }\\n+                previous += 1\\n+            }\\n+            current += 1\\n+        }\\n+        n += 1\\n+    }\\n+    return\\n+}\\n+\\n func append_layout(records: &Vector<RecordIR>, t: ValueType,\\n                    path: &var Vector<Int>, words: &var Vector<ValueType>) -> Result<Void, ProtoError> {\\n     if scalar_type(t) {\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex a4413d95..3be40742 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -9,6 +9,54 @@ func invalid(i: &Instruction) -> ProtoError {\\n     return problem(\\\"internal prototype error: invalid instruction\\\", i.line, i.col)\\n }\\n \\n+func check_type(p: &Program, t: ValueType) -> Result<Void, ProtoError> {\\n+    match t {\\n+        case Enum(index) -> {\\n+            if index < 0 || index >= p.enums.len() { return problem(\\\"internal prototype error: invalid enum type index\\\", 0, 0) }\\n+        },\\n+        case Record(index) -> {\\n+            if index < 0 || index >= p.records.len() { return problem(\\\"internal prototype error: invalid record type index\\\", 0, 0) }\\n+        },\\n+        case _ -> {}\\n+    }\\n+    return\\n+}\\n+\\n+func check_declared_types(p: &Program) -> Result<Void, ProtoError> {\\n+    var n = 0\\n+    while n < p.records.len() {\\n+        let record = p.records[n].copy()\\n+        var e = 0\\n+        while e < p.enums.len() {\\n+            if record.name == p.enums[e].name { return problem(\\\"duplicate module type name\\\", record.line, record.col) }\\n+            e += 1\\n+        }\\n+        var field = 0\\n+        while field < record.field_types.len() {\\n+            try check_type(&p, record.field_types[field])\\n+            field += 1\\n+        }\\n+        n += 1\\n+    }\\n+    n = 0\\n+    while n < p.functions.len() {\\n+        let f = p.functions[n].copy()\\n+        try check_type(&p, f.result)\\n+        var word = 0\\n+        while word < f.params.len() {\\n+            try check_type(&p, f.params[word])\\n+            word += 1\\n+        }\\n+        word = 0\\n+        while word < f.slots.len() {\\n+            try check_type(&p, f.slots[word])\\n+            word += 1\\n+        }\\n+        n += 1\\n+    }\\n+    return\\n+}\\n+\\n func parameter_layout(p: &Program, f: &FuncIR) -> Result<Vector<ValueType>, ProtoError> {\\n     var words = Vector<ValueType>()\\n     var n = 0\\n@@ -44,6 +92,12 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n             if not scalar_type(dst) { return invalid(&i) }\\n             if dst == ValueType.Bool && i.immediate != 0 && i.immediate != 1 { return invalid(&i) }\\n             if integer_type(dst) && not integer_canonical(i.immediate, dst) { return invalid(&i) }\\n+            match dst {\\n+                case Enum(index) -> {\\n+                    if i.immediate < 0 || i.immediate >= p.enums[index].cases.len() { return invalid(&i) }\\n+                },\\n+                case _ -> {}\\n+            }\\n         },\\n         case Copy -> {\\n             if not scalar_type(dst) || a != dst { return invalid(&i) }\\n@@ -102,7 +156,7 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n             }\\n         },\\n         case Print -> {\\n-            if not scalar_type(a) { return invalid(&i) }\\n+            if not printable_type(a) { return invalid(&i) }\\n         },\\n         case Return -> {\\n             if f.result == ValueType.Void {\\n@@ -160,6 +214,8 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n }\\n \\n public func verify(program: &Program) -> Result<Void, ProtoError> {\\n+    try validate_enums(&program.enums)\\n+    try check_declared_types(&program)\\n     try validate_records(&program.records)\\n     if program.main_index < 0 || program.main_index >= program.functions.len() {\\n         return problem(\\\"internal prototype error: missing main\\\", 0, 0)\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 9a49dccf..14e2f8a1 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -165,6 +165,7 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 }\\n             },\\n             case Print -> {\\n+                if not printable_type(function.slots[inst.a]) { return problem(\\\"internal prototype error: non-printable value\\\", inst.line, inst.col) }\\n                 let pt = function.slots[inst.a]\\n                 if pt == ValueType.Bool { if slots[inst.a] == 0 { print(\\\"false\\\") } else { print(\\\"true\\\") } }\\n                 else if integer_signed(pt) { print(slots[inst.a]) } else { print(UInt.from(truncating: slots[inst.a])) }\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex 6c05d783..0c06d633 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -35,6 +35,7 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"enums/nested_control\\\", \\\"true\\\\n10\\\\n11\\\\n20\\\\n\\\", section=\\\"enums\\\"),\\n     RunCase(\\\"control/static_literals\\\", \\\"-128\\\\n32767\\\\n255\\\\n18446744073709551615\\\\n\\\", section=\\\"control\\\"),\\n     RunCase(\\\"control/logical_loops\\\", \\\"0\\\\n1\\\\n2\\\\n3\\\\n9\\\\n4\\\\n\\\", section=\\\"control\\\"),\\n     RunCase(\\\"control/compound_rem_overflow\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"control\\\"),\\n@@ -119,9 +120,21 @@ RUN_CASES = (\\n     RunCase(\\\"control/statics\\\", \\\"4294967295\\\\n18446744073709551615\\\\ntrue\\\\n-9223372036854775808\\\\n-32768\\\\n18446744073709551614\\\\n\\\", section=\\\"control\\\"),\\n     RunCase(\\\"control/compound_overflow\\\", \\\"runtime error: integer overflow\\\\n\\\", 1, \\\"control\\\"),\\n     RunCase(\\\"control/compound_div_zero\\\", \\\"runtime error: division by zero\\\\n\\\", 1, \\\"control\\\"),\\n+    RunCase(\\\"enums/values_copy_equality\\\", \\\"true\\\\nfalse\\\\ntrue\\\\ntrue\\\\n\\\", section=\\\"enums\\\"),\\n+    RunCase(\\\"enums/calls_returns_forward\\\", \\\"true\\\\ntrue\\\\n\\\", section=\\\"enums\\\"),\\n+    RunCase(\\\"enums/record_fields\\\", \\\"true\\\\ntrue\\\\ntrue\\\\n3\\\\n\\\", section=\\\"enums\\\"),\\n+    RunCase(\\\"enums/match_unqualified\\\", \\\"1\\\\n2\\\\n3\\\\n\\\", section=\\\"enums\\\"),\\n+    RunCase(\\\"enums/match_qualified\\\", \\\"true\\\\nfalse\\\\n\\\", section=\\\"enums\\\"),\\n+    RunCase(\\\"enums/match_wildcard\\\", \\\"10\\\\n99\\\\n99\\\\n\\\", section=\\\"enums\\\"),\\n+    RunCase(\\\"enums/subject_once\\\", \\\"7\\\\n2\\\\n\\\", section=\\\"enums\\\"),\\n+    RunCase(\\\"enums/all_arms_return\\\", \\\"20\\\\n40\\\\n60\\\\n\\\", section=\\\"enums\\\"),\\n+    RunCase(\\\"enums/lexer_token_kind\\\", \\\"1\\\\n2\\\\n3\\\\ntrue\\\\n\\\", section=\\\"enums\\\"),\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"enums/reject_empty\\\", \\\"at least one case\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_record_collision\\\", \\\"duplicate module name\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_static_enum\\\", \\\"static type\\\", \\\"enums\\\"),\\n     RejectCase(\\\"control/reject_static_negative_range\\\", \\\"integer literal\\\", \\\"control\\\"),\\n     RejectCase(\\\"control/reject_static_typed_narrowing\\\", \\\"convert\\\", \\\"control\\\"),\\n     RejectCase(\\\"control/reject_static_function_collision\\\", \\\"conflicts\\\", \\\"control\\\"),\\n@@ -141,7 +154,7 @@ REJECT_CASES = (\\n     RejectCase(\\\"reject_semicolon\\\", \\\"semicolons are not supported\\\"),\\n     RejectCase(\\\"reject_string\\\", \\\"expected expression\\\"),\\n     RejectCase(\\\"reject_bool_arithmetic\\\", \\\"arithmetic requires integer operands\\\"),\\n-    RejectCase(\\\"reject_import\\\", \\\"only function, record, and static declarations are supported\\\"),\\n+    RejectCase(\\\"reject_import\\\", \\\"only function, record, enum, and static declarations are supported\\\"),\\n     RejectCase(\\\"reject_scope_leak\\\", \\\"unknown name\\\"),\\n     RejectCase(\\\"reject_unary_depth\\\", \\\"expression nesting limit exceeded\\\"),\\n     RejectCase(\\\"reject_reserved_print\\\", \\\"reserved\\\"),\\n@@ -172,6 +185,18 @@ REJECT_CASES = (\\n     RejectCase(\\\"control/reject_duplicate_static\\\", \\\"duplicate module name\\\", \\\"control\\\"),\\n     RejectCase(\\\"control/reject_assigned_static\\\", \\\"cannot assign to immutable static\\\", \\\"control\\\"),\\n     RejectCase(\\\"control/reject_dynamic_static\\\", \\\"unsupported static initializer\\\", \\\"control\\\"),\\n+    RejectCase(\\\"enums/reject_duplicate_enum\\\", \\\"duplicate module name\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_duplicate_case\\\", \\\"duplicate enum case\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_missing_match_case\\\", \\\"non-exhaustive match\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_duplicate_arm\\\", \\\"duplicate match case\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_arm_after_wildcard\\\", \\\"match arm follows wildcard\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_wrong_qualifier\\\", \\\"does not match subject enum\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_mixed_equality\\\", \\\"same type\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_mixed_transfer\\\", \\\"cannot implicitly convert\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_cast\\\", \\\"requires integer\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_print\\\", \\\"cannot print an enum value\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_payload\\\", \\\"payload enum cases are not supported\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"enums/reject_arithmetic\\\", \\\"arithmetic requires integer operands\\\", \\\"enums\\\"),\\n )\\n \\n \\n@@ -256,7 +281,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -285,7 +310,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\") else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\ndiff --git a/prototypes/minivm/tests/enum_contract.saw b/prototypes/minivm/tests/enum_contract.saw\\nnew file mode 100644\\nindex 00000000..d38cbd8b\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/enum_contract.saw\\n@@ -0,0 +1,154 @@\\n+import src.model.*\\n+import src.verify.*\\n+\\n+func enum_decl(name: String) -> EnumIR {\\n+    var cases = Vector<String>()\\n+    try! cases.push(\\\"First\\\")\\n+    try! cases.push(\\\"Last\\\")\\n+    EnumIR(name: name, cases: move cases, line: 1, col: 1)\\n+}\\n+\\n+func enum_record() -> RecordIR {\\n+    var names = Vector<String>()\\n+    try! names.push(\\\"tag\\\")\\n+    try! names.push(\\\"count\\\")\\n+    var types = Vector<ValueType>()\\n+    try! types.push(ValueType.Enum(0))\\n+    try! types.push(ValueType.I32)\\n+    RecordIR(name: \\\"Tagged\\\", field_names: move names, field_types: move types,\\n+             line: 2, col: 1)\\n+}\\n+\\n+func valid(program: &Program) -> Bool {\\n+    try verify(&program) catch { return false }\\n+    true\\n+}\\n+\\n+func base_program() -> Program {\\n+    var enums = Vector<EnumIR>()\\n+    try! enums.push(enum_decl(\\\"Alpha\\\"))\\n+    try! enums.push(enum_decl(\\\"Beta\\\"))\\n+    var records = Vector<RecordIR>()\\n+    try! records.push(enum_record())\\n+\\n+    var main_slots = Vector<ValueType>()\\n+    try! main_slots.push(ValueType.Enum(0))\\n+    try! main_slots.push(ValueType.Enum(1))\\n+    var first = instruction(Op.Const)\\n+    first.dst = 0\\n+    first.immediate = 0\\n+    var last = instruction(Op.Const)\\n+    last.dst = 1\\n+    last.immediate = 1\\n+    var args = Vector<Int>()\\n+    try! args.push(0)\\n+    var call = instruction(Op.Call)\\n+    call.target = 1\\n+    call.dst = 0\\n+    call.args = move args\\n+    var main_code = Vector<Instruction>()\\n+    try! main_code.push(move first)\\n+    try! main_code.push(move last)\\n+    try! main_code.push(move call)\\n+    try! main_code.push(instruction(Op.Return))\\n+\\n+    var identity_params = Vector<ValueType>()\\n+    try! identity_params.push(ValueType.Enum(0))\\n+    var identity_slots = Vector<ValueType>()\\n+    try! identity_slots.push(ValueType.Enum(0))\\n+    var ret = instruction(Op.Return)\\n+    ret.a = 0\\n+    var identity_code = Vector<Instruction>()\\n+    try! identity_code.push(move ret)\\n+\\n+    var functions = Vector<FuncIR>()\\n+    try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(),\\n+        result: ValueType.Void, slots: move main_slots, code: move main_code))\\n+    try! functions.push(FuncIR(name: \\\"identity\\\", params: move identity_params,\\n+        result: ValueType.Enum(0), slots: move identity_slots,\\n+        code: move identity_code))\\n+    Program(enums: move enums, records: move records,\\n+            functions: move functions, main_index: 0)\\n+}\\n+\\n+func main() {\\n+    let program = base_program()\\n+    assert(valid(&program), \\\"valid enum program\\\")\\n+    assert(scalar_type(ValueType.Enum(0)), \\\"enum is scalar\\\")\\n+    assert(not integer_type(ValueType.Enum(0)), \\\"enum is not integer\\\")\\n+    assert(not printable_type(ValueType.Enum(0)), \\\"enum is not printable\\\")\\n+    let record_layout = try! value_layout(&program.records, ValueType.Record(0))\\n+    assert(record_layout.len() == 2 && record_layout[0] == ValueType.Enum(0) &&\\n+           record_layout[1] == ValueType.I32, \\\"record preserves enum field type\\\")\\n+\\n+    var negative_field = program.copy()\\n+    negative_field.records[0].field_types.set(0, ValueType.Enum(-1))\\n+    assert(not valid(&negative_field), \\\"negative enum index in record field\\\")\\n+    var high_field = program.copy()\\n+    high_field.records[0].field_types.set(0, ValueType.Enum(2))\\n+    assert(not valid(&high_field), \\\"high enum index in record field\\\")\\n+\\n+    var negative_param = program.copy()\\n+    negative_param.functions[1].params.set(0, ValueType.Enum(-1))\\n+    assert(not valid(&negative_param), \\\"negative enum index in parameter\\\")\\n+    var high_param = program.copy()\\n+    high_param.functions[1].params.set(0, ValueType.Enum(2))\\n+    assert(not valid(&high_param), \\\"high enum index in parameter\\\")\\n+    var negative_result = program.copy()\\n+    negative_result.functions[1].result = ValueType.Enum(-1)\\n+    assert(not valid(&negative_result), \\\"negative enum index in result\\\")\\n+    var high_result = program.copy()\\n+    high_result.functions[1].result = ValueType.Enum(2)\\n+    assert(not valid(&high_result), \\\"high enum index in result\\\")\\n+    var negative_slot = program.copy()\\n+    negative_slot.functions[0].slots.set(0, ValueType.Enum(-1))\\n+    assert(not valid(&negative_slot), \\\"negative enum index in slot\\\")\\n+    var high_slot = program.copy()\\n+    high_slot.functions[0].slots.set(0, ValueType.Enum(2))\\n+    assert(not valid(&high_slot), \\\"high enum index in slot\\\")\\n+\\n+    var first_case = program.copy()\\n+    first_case.functions[0].code[0].immediate = 0\\n+    assert(valid(&first_case), \\\"first ordinal accepted\\\")\\n+    var last_case = program.copy()\\n+    last_case.functions[0].code[0].immediate = 1\\n+    assert(valid(&last_case), \\\"last ordinal accepted\\\")\\n+    var negative_case = program.copy()\\n+    negative_case.functions[0].code[0].immediate = -1\\n+    assert(not valid(&negative_case), \\\"negative ordinal rejected\\\")\\n+    var past_last_case = program.copy()\\n+    past_last_case.functions[0].code[0].immediate = 2\\n+    assert(not valid(&past_last_case), \\\"case count ordinal rejected\\\")\\n+\\n+    var empty_enum = program.copy()\\n+    empty_enum.enums[0].cases = Vector<String>()\\n+    assert(not valid(&empty_enum), \\\"empty enum rejected\\\")\\n+    var duplicate_enum = program.copy()\\n+    duplicate_enum.enums[1].name = \\\"Alpha\\\"\\n+    assert(not valid(&duplicate_enum), \\\"duplicate enum rejected\\\")\\n+    var cross_kind = program.copy()\\n+    cross_kind.records[0].name = \\\"Alpha\\\"\\n+    assert(not valid(&cross_kind), \\\"enum and record name collision rejected\\\")\\n+    var duplicate_case = program.copy()\\n+    duplicate_case.enums[0].cases.set(1, \\\"First\\\")\\n+    assert(not valid(&duplicate_case), \\\"duplicate enum case rejected\\\")\\n+\\n+    var enum_print = program.copy()\\n+    var print_inst = instruction(Op.Print)\\n+    print_inst.a = 0\\n+    enum_print.functions[0].code.set(2, move print_inst)\\n+    assert(not valid(&enum_print), \\\"enum print rejected\\\")\\n+\\n+    var wrong_argument = program.copy()\\n+    wrong_argument.functions[0].code[2].args.set(0, 1)\\n+    assert(not valid(&wrong_argument), \\\"nominal enum call argument rejected\\\")\\n+    var wrong_destination = program.copy()\\n+    wrong_destination.functions[0].code[2].dst = 1\\n+    assert(not valid(&wrong_destination), \\\"nominal enum call result rejected\\\")\\n+    var wrong_return = program.copy()\\n+    wrong_return.functions[1].result = ValueType.Enum(1)\\n+    wrong_return.functions[0].code[2].dst = 1\\n+    assert(not valid(&wrong_return), \\\"nominal enum return rejected\\\")\\n+\\n+    print(\\\"enum contract passed\\\")\\n+}\\ndiff --git a/prototypes/minivm/tests/record_contract.saw b/prototypes/minivm/tests/record_contract.saw\\nindex e98e87bf..ed1d0f70 100644\\n--- a/prototypes/minivm/tests/record_contract.saw\\n+++ b/prototypes/minivm/tests/record_contract.saw\\n@@ -46,7 +46,7 @@ func call_program(records: &Vector<RecordIR>) -> Program {\\n         slots: slots.copy(), code: move main_code))\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move params, result: ValueType.Record(0),\\n         slots: move slots, code: move callee_code))\\n-    Program(records: records.copy(), functions: move functions, main_index: 0)\\n+    Program(enums: Vector<EnumIR>(), records: records.copy(), functions: move functions, main_index: 0)\\n }\\n \\n func main() {\\n\",\"id\":\"SL-247\",\"request_id\":\"minivm-SL-247-1d37cf75\",\"title\":\"minivm M4: enums + exhaustive statement match (branch commit 1d37cf75)\"}","request_id":"minivm-SL-247-1d37cf75","sequence":444,"timestamp":"1789089095"}
---
