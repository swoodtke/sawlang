---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl265","author":"agent:claude-sl238","comments":[{"author":"agent:claude-sawlang","body":"QUEUED (user-ruled Sep 11): slots AFTER SL-212 (unit C) — same ownership/copy-lowering territory, ruled order preserved. Queue: SL-211 (fix round in flight) -> SL-212 -> SL-265 -> server trio.","created":"1789153310","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-265.p1 (revision 1): design 271: one copy-emission funnel (SL-265) — automatic-Copy-tier aggregates retain their members in Vector/Map copy families, user generics, and the derived memberwise copy()","created":"1789183898","id":"c2"}],"created":"1789139473","description":"Found while migrating `prototypes/minivm` onto design 268's lexer API (SL-238.p1 review round). NOT caused by that patch — the defect is in copy lowering, which design 268 does not touch; the prototype simply became the first caller to copy a `Vector` of an automatic-Copy-tier struct, and it corrupted memory immediately.\n\n## The defect\n\n`Vector<T>.copy()` (and by inspection `try_copy`) accepts an element type on the AUTOMATIC Copy tier — a struct whose members are all trivial/Copy, declaring nothing (design 159/219) — and duplicates each element BITWISE, never retaining its refcounted fields. The copy's `String` fields alias the original's storage, so once the original vector drops, every element's text is freed memory: reads return empty/garbage and the second release aborts with `panic: over-release of a String reference (refcount underflow)`.\n\n`sawc/std/vector.saw:493-509` is CORRECT — it spells `result.push(buf[i].copy())`. The miscompile is in what that per-element `.copy()` lowers to when `T` is monomorphized to such a struct.\n\n## The inconsistency that pins it\n\nThe SAME type is REFUSED `.copy()` at a direct call site:\n\n```\nerror: type `Rec` is not Copy; `.copy()` requires a trivially-copyable, Copy, or ExplicitCopy type\n```\n\nSo two judgments about the automatic Copy tier disagree: the generic BOUND `Vector<T: ExplicitCopy>` admits `Rec`, while member resolution says `Rec` has no `copy()` at all. In the generic body the call then degrades to a bitwise duplicate instead of erroring or retaining.\n\n## Repro (single module, no lexer, 30 lines)\n\n```saw\nstruct Rec {\n    text: String\n    tag: String?\n}\n\nfunc heap_string(seed: String) -> String {\n    var sb = StringBuilder()\n    try! sb.append(seed)\n    try! sb.append(\"-built\")\n    sb.build()\n}\n\nfunc copy_then_drop() -> Vector<Rec> {\n    var original = Vector<Rec>()\n    try! original.push(Rec(text: heap_string(\"alpha\"), tag: None))\n    try! original.push(Rec(text: heap_string(\"beta\"), tag: heap_string(\"tag\")))\n    let duplicate = original.copy()\n    move duplicate          // `original` drops at the end of this function\n}\n\nfunc main() {\n    let v = copy_then_drop()\n    print(v.len())\n    print(\"[{v[0].text}]\")\n    print(\"[{v[1].text}]\")\n    if let t = v[1].tag { print(\"[{t}]\") }\n    print(\"done\")\n}\n```\n\nExpected: `2`, `[alpha-built]`, `[beta-built]`, `[tag-built]`, `done`.\nActual: `2`, `[]`, `[]`, `[]`, then `panic: over-release of a String reference (refcount underflow)`, SIGABRT (exit 134).\n\nGOTCHA for anyone re-probing: the fields must hold HEAP strings. A string LITERAL is immortal (rc == -1), so a literal-filled struct hides the missing retain completely — three earlier probes of mine passed for exactly that reason.\n\n## Boundary probes (all same-module, heap strings)\n\n| probe | shape | result |\n|---|---|---|\n| P1 | `Vector<Rec>.copy()`, `Rec` = automatic-Copy-tier struct | BROKEN — no retain, over-release abort |\n| P2 | `Vector<String>.copy()` (element IS the refcounted primitive) | correct |\n| P3 | implicit struct copy, `let duplicate = original` | correct — retains |\n| P4 | explicit `original.copy()` on that struct | REFUSED at compile time (message above) |\n| P5 | same as P1 but the struct is defined in ANOTHER module | BROKEN identically (first seen through the selfhost lexer's `Token`) |\n\nSo the retain works everywhere the compiler handles the value directly, and is lost exactly on the generic `ExplicitCopy`-bounded element path.\n\n## Suspected class (not verified — worth a sweep before a fix is dispatched)\n\nEvery `T: ExplicitCopy`-bounded std API that spells a per-element `.copy()` is a candidate, not just Vector: `Vector.try_copy` (`vector.saw:518-532`), and design 251's conditional `Map`/`Set` conformances, whose copies duplicate keys and values the same way. A `Map<String, Rec>` or a `Set<Rec>` copy would corrupt on the same mechanism if it shares the lowering. The automatic tier is the common ingredient: a DECLARED `@synthesize extension Rec: ExplicitCopy {}` presumably synthesizes a real `copy()` and is unaffected (unprobed).\n\n## Impact\n\nSilent memory corruption on a safe-code path with no `unsafe` anywhere, at exit 0 until the allocator or the over-release detector happens to notice. The automatic Copy tier is the tier a struct lands on by writing NOTHING, so this is reachable without the author ever naming a copy policy.\n\n## Workaround in use\n\n`prototypes/minivm/src/frontend.saw` clones the token stream element by element through its own `clone_token` (field reads + memberwise construction), which retains correctly. The comment there cites this issue.\n","id":"SL-265","labels":["bug","compiler","ownership"],"priority":"high","project":"SL","revision":5,"sequence":663,"stage":"queued","status":"open","title":"Vector<T>.copy() bitwise-duplicates an automatic-Copy-tier struct element, losing every String retain","updated":"1789183898"},"before":{"assignee":"agent:claude-sl265","author":"agent:claude-sl238","comments":[{"author":"agent:claude-sawlang","body":"QUEUED (user-ruled Sep 11): slots AFTER SL-212 (unit C) — same ownership/copy-lowering territory, ruled order preserved. Queue: SL-211 (fix round in flight) -> SL-212 -> SL-265 -> server trio.","created":"1789153310","id":"c1"}],"created":"1789139473","description":"Found while migrating `prototypes/minivm` onto design 268's lexer API (SL-238.p1 review round). NOT caused by that patch — the defect is in copy lowering, which design 268 does not touch; the prototype simply became the first caller to copy a `Vector` of an automatic-Copy-tier struct, and it corrupted memory immediately.\n\n## The defect\n\n`Vector<T>.copy()` (and by inspection `try_copy`) accepts an element type on the AUTOMATIC Copy tier — a struct whose members are all trivial/Copy, declaring nothing (design 159/219) — and duplicates each element BITWISE, never retaining its refcounted fields. The copy's `String` fields alias the original's storage, so once the original vector drops, every element's text is freed memory: reads return empty/garbage and the second release aborts with `panic: over-release of a String reference (refcount underflow)`.\n\n`sawc/std/vector.saw:493-509` is CORRECT — it spells `result.push(buf[i].copy())`. The miscompile is in what that per-element `.copy()` lowers to when `T` is monomorphized to such a struct.\n\n## The inconsistency that pins it\n\nThe SAME type is REFUSED `.copy()` at a direct call site:\n\n```\nerror: type `Rec` is not Copy; `.copy()` requires a trivially-copyable, Copy, or ExplicitCopy type\n```\n\nSo two judgments about the automatic Copy tier disagree: the generic BOUND `Vector<T: ExplicitCopy>` admits `Rec`, while member resolution says `Rec` has no `copy()` at all. In the generic body the call then degrades to a bitwise duplicate instead of erroring or retaining.\n\n## Repro (single module, no lexer, 30 lines)\n\n```saw\nstruct Rec {\n    text: String\n    tag: String?\n}\n\nfunc heap_string(seed: String) -> String {\n    var sb = StringBuilder()\n    try! sb.append(seed)\n    try! sb.append(\"-built\")\n    sb.build()\n}\n\nfunc copy_then_drop() -> Vector<Rec> {\n    var original = Vector<Rec>()\n    try! original.push(Rec(text: heap_string(\"alpha\"), tag: None))\n    try! original.push(Rec(text: heap_string(\"beta\"), tag: heap_string(\"tag\")))\n    let duplicate = original.copy()\n    move duplicate          // `original` drops at the end of this function\n}\n\nfunc main() {\n    let v = copy_then_drop()\n    print(v.len())\n    print(\"[{v[0].text}]\")\n    print(\"[{v[1].text}]\")\n    if let t = v[1].tag { print(\"[{t}]\") }\n    print(\"done\")\n}\n```\n\nExpected: `2`, `[alpha-built]`, `[beta-built]`, `[tag-built]`, `done`.\nActual: `2`, `[]`, `[]`, `[]`, then `panic: over-release of a String reference (refcount underflow)`, SIGABRT (exit 134).\n\nGOTCHA for anyone re-probing: the fields must hold HEAP strings. A string LITERAL is immortal (rc == -1), so a literal-filled struct hides the missing retain completely — three earlier probes of mine passed for exactly that reason.\n\n## Boundary probes (all same-module, heap strings)\n\n| probe | shape | result |\n|---|---|---|\n| P1 | `Vector<Rec>.copy()`, `Rec` = automatic-Copy-tier struct | BROKEN — no retain, over-release abort |\n| P2 | `Vector<String>.copy()` (element IS the refcounted primitive) | correct |\n| P3 | implicit struct copy, `let duplicate = original` | correct — retains |\n| P4 | explicit `original.copy()` on that struct | REFUSED at compile time (message above) |\n| P5 | same as P1 but the struct is defined in ANOTHER module | BROKEN identically (first seen through the selfhost lexer's `Token`) |\n\nSo the retain works everywhere the compiler handles the value directly, and is lost exactly on the generic `ExplicitCopy`-bounded element path.\n\n## Suspected class (not verified — worth a sweep before a fix is dispatched)\n\nEvery `T: ExplicitCopy`-bounded std API that spells a per-element `.copy()` is a candidate, not just Vector: `Vector.try_copy` (`vector.saw:518-532`), and design 251's conditional `Map`/`Set` conformances, whose copies duplicate keys and values the same way. A `Map<String, Rec>` or a `Set<Rec>` copy would corrupt on the same mechanism if it shares the lowering. The automatic tier is the common ingredient: a DECLARED `@synthesize extension Rec: ExplicitCopy {}` presumably synthesizes a real `copy()` and is unaffected (unprobed).\n\n## Impact\n\nSilent memory corruption on a safe-code path with no `unsafe` anywhere, at exit 0 until the allocator or the over-release detector happens to notice. The automatic Copy tier is the tier a struct lands on by writing NOTHING, so this is reachable without the author ever naming a copy policy.\n\n## Workaround in use\n\n`prototypes/minivm/src/frontend.saw` clones the token stream element by element through its own `clone_token` (field reads + memberwise construction), which retains correctly. The comment there cites this issue.\n","id":"SL-265","labels":["bug","compiler","ownership"],"priority":"high","project":"SL","revision":4,"sequence":658,"stage":"queued","status":"open","title":"Vector<T>.copy() bitwise-duplicates an automatic-Copy-tier struct element, losing every String retain","updated":"1789178728"},"id":"SL-265","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/designs/271-copy-emission-funnel.md b/designs/271-copy-emission-funnel.md\\nnew file mode 100644\\nindex 00000000..16b0c031\\n--- /dev/null\\n+++ b/designs/271-copy-emission-funnel.md\\n@@ -0,0 +1,156 @@\\n+# Design 271 — one copy-emission funnel\\n+\\n+**Status:** built (SL-265).\\n+**Issue:** SL-265 — `Vector<T>.copy()` bitwise-duplicates an automatic-Copy-tier\\n+struct element, losing every `String` retain.\\n+**Findings filed on the way:** SL-269, SL-270, SL-271 (each independent of this\\n+fix and unchanged by it).\\n+\\n+## The defect as filed\\n+\\n+`Vector<T: ExplicitCopy>.copy()` spells `result.push(buf[i].copy())`\\n+(`sawc/std/vector.saw:493-509`), which is correct. At `T` = a struct on the\\n+AUTOMATIC Copy tier — design 159's tier, a struct or enum whose members are all\\n+trivial/Copy, declaring nothing and owing nothing — that per-element `.copy()`\\n+lowered to a bare load. The duplicate's `String` fields aliased the original's,\\n+so the original's drop freed storage the duplicate still held: reads returned\\n+empty and the second release aborted with `over-release of a String reference\\n+(refcount underflow)`, exit 134, from fully safe code with no `unsafe` anywhere.\\n+\\n+The filed repro runs correctly now and prints exactly what the issue predicts.\\n+It is row V100.\\n+\\n+## Root cause: three chains, one rule\\n+\\n+The bound is not the problem and member resolution is not the problem. Design\\n+219 is explicit that every `Copy` type satisfies `T: ExplicitCopy`, so\\n+`Vector`'s bound is RIGHT to admit the tier, the call site discharged it against\\n+the concrete argument, and design 218c's `_mono_copy_is_a_retain` skip is right\\n+to let the substituted clone keep the spelling its template wrote. Its docstring\\n+even states the contract this brief had to make true — \\\"codegen lowers the\\n+element copy BY TIER\\\".\\n+\\n+Codegen did not. \\\"Duplicate this value at its tier\\\" is a rule quantified over\\n+every position a duplicate is emitted, and it had been written out THREE times:\\n+\\n+| chain | where | arms it had | arms it lacked |\\n+|---|---|---|---|\\n+| `_generate_copy` | `resources.py` | array, tuple, escaping closure, real `copy` symbol, **design-159 aggregate**, bitwise | (complete) |\\n+| `_emit_copy_value` | `resources.py` | array, tuple, optional, trivial, real symbol, **design-159 aggregate**, bitwise | escaping closure |\\n+| `.copy()` interception | `calls.py` | array, optional, tuple, DECLARED-tier enum, escaping closure, real symbol | **design-159 aggregate** — and the automatic tier answers no \\\"declares a policy\\\" test, so it fell out of the chain's last line as `return obj_val` |\\n+| derived memberwise `copy()` | `methods.py` | DECLARED-tier enum, optional, conformance-declared field type | **design-159 aggregate, ARRAY fields, TUPLE fields, closure fields** |\\n+\\n+Two of the four were miscompiling. The shared ingredient in every gap is the\\n+same confusion: a test for a DECLARED conformance standing in for a question\\n+about a DERIVED tier. The automatic tier is exactly the tier that declares\\n+nothing, so every such test is blind to it by construction.\\n+\\n+This is obligation 1's failure mode with the symptom attached. The fix is\\n+obligation 1's remedy: ONE chokepoint whose docstring names its entry points.\\n+\\n+## The fix\\n+\\n+`_emit_copy_value` (`sawc/codegen/resources.py`) becomes THE funnel. It gains\\n+the escaping-closure arm it lacked, its docstring names all five entry points\\n+and gives the arm order with reasons, and the other three sites delegate:\\n+\\n+- `_generate_copy` keeps only its monomorphization substitution, then calls the\\n+  funnel. Its arm list was the same list maintained separately, which is the\\n+  shape that drifts.\\n+- `calls.py`'s `.copy()` interception keeps only its two refusals and calls the\\n+  funnel. The NoCopy refusal is re-keyed from a conformance-name test to\\n+  `copy_tier(...) == 'nocopy'`, which is the accurate predicate — the old one\\n+  also caught every DECLARED-`ExplicitCopy` ENUM, the one shape that\\n+  legitimately has no emitted symbol.\\n+- `methods.py`'s `_generate_derived_copy_body` keeps only its\\n+  declared-policy-STRUCT branch (that resolution fills declared default type\\n+  arguments, which plain mangling does not — DF-128c) and calls the funnel for\\n+  every other field.\\n+\\n+ARM ORDER is load-bearing and is pinned by row V103: the real `copy` symbol is\\n+asked for BEFORE the trivially-copyable test, so a POD receiver whose author\\n+wrote a hook still runs that hook.\\n+\\n+Nothing in the typechecker changed. No diagnostic changed. No language rule\\n+changed — the automatic tier's copy has always been \\\"retain each member\\\"; this\\n+is the codegen catching up at two of the four places that emit one.\\n+\\n+## Obligation 4 — the class\\n+\\n+The issue named `Vector.try_copy` and design 251's `Map`/`Set` conformances as\\n+unverified siblings. The sweep went wider, because the mechanism is a\\n+copy-EMISSION chain rather than anything about generics or about std.\\n+\\n+Probed with heap strings (a string LITERAL is immortal and passes vacuously —\\n+the issue's own gotcha, and the reason row V25 could never have caught this),\\n+each pre-fix and post-fix:\\n+\\n+| cell | pre-fix | post-fix |\\n+|---|---|---|\\n+| `Vector<Rec>.copy()`, `Rec` auto-tier struct | empty reads, SIGABRT 134 | correct |\\n+| `Vector<Tag>.copy()`, `Tag` auto-tier ENUM | payload corrupted | correct |\\n+| `Vector<Rec>.try_copy()` | empty read, exit 133 | correct |\\n+| `Map<String, Rec>.copy()` | value's text aliased the KEY's buffer, SIGABRT 134 | correct |\\n+| `Map<String, Rec>.try_copy()` | same | correct |\\n+| user generic `dup<T: ExplicitCopy>` at `Rec` | corrupt | correct |\\n+| `@synthesize` copy, auto-tier STRUCT field | empty read, exit 133 | correct |\\n+| `@synthesize` copy, auto-tier ENUM field | corrupt | correct |\\n+| `@synthesize` copy, `[String; 2]` field | SIGABRT | correct |\\n+| `@synthesize` copy, `(String, Int)` field | bitwise in IR | correct |\\n+| `Vector<Rec?>` / `Vector<(Rec, Int)>` / `Vector<[Rec; 2]>` | already correct | unchanged |\\n+| `Vector<String>`, `Vector<declared ExplicitCopy>` | already correct | unchanged |\\n+\\n+The two that widened the finding past its filing are the `@synthesize` rows:\\n+one `@synthesize` line and a struct literal reproduce it, with no bound, no type\\n+parameter and no `Vector` in the program. \\\"A generic bug\\\" was the wrong reading\\n+throughout.\\n+\\n+Two cells could NOT be probed, and both are recorded rather than worked around:\\n+\\n+- `Set<Rec>` and `Map<Rec, Int>` are refused at the instantiation, the element\\n+  and key gates calling the automatic tier \\\"move-only, not retainable\\\" while the\\n+  `Map` VALUE gate accepts it. **SL-270** — the same DECLARED-vs-DERIVED\\n+  confusion in a third kind of predicate, which is why it is filed with a sweep\\n+  note rather than as a one-off.\\n+- A DIRECT `Vector<closure>` element is unusable as a refcount oracle: the\\n+  vector never releases its element's env, and a by-value capture does not\\n+  survive the frame that built the closure. Both reproduce with NO copy anywhere\\n+  in the program and are identical pre-fix and post-fix, so both are\\n+  drop/capture lowering. **SL-269.** The closure arm is counted through a STRUCT\\n+  member instead, where the glue does release and the count moves.\\n+\\n+## Obligation 3 — conformance rows, written first\\n+\\n+- **V100** — a `T: ExplicitCopy`-bounded `copy()` duplicates an automatic\\n+  Copy-tier element at its own tier. Struct and enum, heap strings, plus a\\n+  COUNTING oracle: the element owns an escaping closure whose env holds an `Arc`\\n+  reference, so a missing retain drops the count early (pre-fix it reads 1) and\\n+  a doubled one never drops it.\\n+- **V101** — the whole bounded-generic family carries the tier:\\n+  `Vector.try_copy`, `Map.copy`, `Map.try_copy`, and a user-level generic.\\n+- **V102** — a `@synthesize`d memberwise `copy()` duplicates EVERY field at its\\n+  own tier: auto-tier struct field, auto-tier enum field, array field, tuple\\n+  field.\\n+- **V103** — control, the double-retain guard: a hand-written `copy()` hook\\n+  counts duplications directly and pins the arm order; a declared-tier enum\\n+  element and an `ExplicitCopy` element keep independent buffers.\\n+\\n+V100–V102 FAIL on the pre-fix compiler (133/134 or corrupted output) and pass\\n+after, so each is a real regression test rather than a restatement.\\n+\\n+**Row V25 is noted, not changed.** It is the tier's other behavioural row and\\n+its oracle is a string literal, so it passes with or without the retain. The new\\n+rows use heap strings for exactly that reason.\\n+\\n+## Workaround removed\\n+\\n+`prototypes/minivm/src/frontend.saw` cloned the token stream element by element\\n+through its own `clone_token` with a comment citing SL-265. That is now\\n+`tokens = r.tokens.copy()`, and minivm's 346-case differential harness passes —\\n+end-to-end validation on the code that reported the bug. `clone_token` keeps its\\n+two other callers.\\n+\\n+## Gates\\n+\\n+Per-commit: full suite 2486 passed / 10 xfailed, freestanding 36 passed across\\n+riscv32 + arm64. Terminal: the full battery.\\ndiff --git a/designs/INDEX.md b/designs/INDEX.md\\nindex e62c0226..2841cfef 100644\\n--- a/designs/INDEX.md\\n+++ b/designs/INDEX.md\\n@@ -262,6 +262,7 @@ Navigation only. One line per design brief in `designs/` (number, file, the brie\\n - **267** — `267-ownership-boundary-inventory.md` — SL-209 unit A (SL-210): the ownership BOUNDARY INVENTORY (every boundary and every producer mapped to its site and its conformance rows, the three funnels named, the producer census over all 45 `Expression` subclasses) plus a STRUCTURED transfer decision per occurrence — `_check_value_transfer` stops answering in error-or-`needs_copy` and files an explicit record, so \\\"checked, nothing owed\\\" stops being indistinguishable from \\\"never checked\\\". Behaviour-preserving; findings SL-218/SL-219/SL-220 filed, not fixed (number PROVISIONAL, Sep 9)\\n - **268** — `268-string-token-typed-segments.md` — SL-238's fix by construction: the 0x01 escaped-brace marker protocol is DELETED from both lexers and a string token carries TYPED SEGMENTS instead (plain STRING = fully decoded value; INTERP_STRING = an ordered `text`/`expr` list, each `expr` carrying the raw source text and the EXACT position of its `{`), so no content byte is metadata and the parser's byte re-scan is gone. Design 99's columns become exact under escapes; the string PATTERN position's missing strip (a second face of the bug) closes with it (ruled + built Sep 11)\\n - **269** — `269-forwarding-taxonomy.md` — SL-209 unit B (SL-211): the FORWARDING and control-flow taxonomy. `_is_aliasing_expr` stops being a hand-maintained node-type list and becomes a TOTAL producer classification (`typechecker/producers.py`, six kinds) over every node that can occupy a value position — `Expression` subclasses ∪ everything carrying a `result_type`, which is 46 and is how `ForLoop` turned up — gated by `tools/test_producer_taxonomy.py`, with both transparency walks (`_is_aliasing_expr`, the ledger's identity walk) on the one funnel. The three findings the old shape was still hiding close as table rows: SL-79/DF-305a (the four auto-wraps, PEELED at the checkpoint so the author's expression is judged; ruling — the wrap is transparent, so `func f(r: Res) -> Res? { r }` is refused too, measured migration cost one line), SL-218 (`SelfExpr`), SL-219 (`TryExpr`, the one node in two buckets). SL-220 re-probed and left with unit D: its producer answer was never wrong (number PROVISIONAL, Sep 11)\\n+- **271** — `271-copy-emission-funnel.md` — SL-265's fix: ONE copy-emission funnel. \\\"Duplicate this value at its tier\\\" had been written out THREE times in codegen, and the two hand-maintained copies — the `.copy()` method-call interception and the `@synthesize`d memberwise body — each lacked the design-159 arm, so an AUTOMATIC Copy-tier aggregate (which declares nothing, and so answers no \\\"declares a policy\\\" test) duplicated BITWISE and its `String` members were never retained. `_emit_copy_value` becomes the chokepoint whose docstring names its five entry points, gains the escaping-closure arm it lacked, and the other three sites delegate. The sweep widened the finding past `Vector`: the derived memberwise body reproduces it with no bound, no type parameter and no `Vector` in the program, and had three gaps rather than one (auto-tier fields, ARRAY fields, TUPLE fields). Rows V100–V103; SL-269/SL-270/SL-271 filed on the way (number PROVISIONAL, Sep 11)\\n - **archive** — `done_aug1-aug9.md` — Aug 1 – Aug 9, 2026 — landed/closed tracker recaps, archived at the Aug-9 split\\n - **archive** — `done_aug10-aug17.md` — Aug 10 – Aug 17, 2026 — landed/closed tracker recaps, archived at the Aug-17 split\\n - **archive** — `done_aug18-aug25.md` — Aug 18 – Aug 25, 2026 — landed/closed tracker recaps, opened at the Aug-18 split\\ndiff --git a/examples/conformance/INDEX.md b/examples/conformance/INDEX.md\\nindex 08e4a64f..20125d3b 100644\\n--- a/examples/conformance/INDEX.md\\n+++ b/examples/conformance/INDEX.md\\n@@ -356,6 +356,11 @@ method receivers*; designs 34, 131, 139, 159, 202, 260\\n \\n | V99 | EVERY face of the payload-read family retains exactly once, and each one's retain is a recorded obligation | `V99_every_payload_read_retains_once.saw` | Design 270 (SL-212), the r2 review's class closure. Design 131 routes every payload spelling through one funnel; this is that funnel's behavioural matrix — `o!`, the `??` left operand, `if let`, `guard let`, a `try`'s Ok payload, and an ordinary `needs_copy` transfer beside them — one `copy` line per face at the silently-copyable tier, where a missing retain is a refcount underflow and a doubled one is a leak. It is ALSO the preservation audit's negative-test corpus: the audit's P1r takes its obligation from `_stamp_retain` (the single writer every producer routes through) rather than from the annotation it is an obligation ABOUT, because reading it off the annotation is what let a cleared stamp delete its own check — the reviewer cleared one `ForceUnwrap.payload_needs_copy` and the emitted binary stopped performing a `Copy`-tier retain while the audit stayed green. The lane mutates every (node kind, annotation) pair this file produces, which is seven |\\n \\n+| V100 | a `T: ExplicitCopy`-bounded `copy()` duplicates an AUTOMATIC Copy-tier element at its own tier | `V100_bounded_copy_at_the_auto_tier_retains.saw` | SL-265, CLOSED by design 271. Design 159 puts a struct or enum whose members are all trivial/Copy on the Copy tier owing NO declaration, and design 219 makes every Copy type satisfy `T: ExplicitCopy` — so `Vector`'s bound admits it and `buf[i].copy()` runs at it. Codegen's `.copy()` interception carried its own arm list, and the automatic tier answers no \\\"declares a policy\\\" test, so the spelling fell out of that chain's last line as a BITWISE duplicate: the copy's `String`s aliased the original's, reads came back empty and the second release aborted. The struct and ENUM rows are both counted because the chain's enum arm was gated on a DECLARED tier, which is the same blind spot one level down. The strings are HEAP strings deliberately — a literal is immortal (refcount -1), which is why V25, the tier's other behavioural row, passes either way and could never have caught this |\\n+| V101 | EVERY bounded-generic `copy()` in the family carries the element's tier, not just `Vector.copy` | `V101_copy_family_carries_the_tier.saw` | SL-265's obligation-4 sweep. The defect was in what `x.copy()` LOWERS TO under a substituted Copy-tier `T`, so every body spelling it under a Copy-family bound had it; the std census names four (`Vector.copy`, `Vector.try_copy`, `Map.copy`, `Map.try_copy` — design 251's conditional conformance duplicates keys AND values, and the probe showed the value's text aliasing the KEY's buffer). The user-level generic is the fifth row and the one that fixes the scope: nothing about `sawc/std` was ever in the lowering decision, so \\\"a std bug\\\" would have been the wrong reading. `Set` cannot hold the row — `Set<Rec>` is REFUSED at instantiation, its element gate calling the automatic tier \\\"move-only, not retainable\\\" while the Map VALUE position accepts it (SL-270) |\\n+| V102 | a `@synthesize`d memberwise `copy()` duplicates EVERY field at that field's own tier | `V102_derived_copy_duplicates_every_field.saw` | SL-265's sibling, and the row that says the finding was never about generics. `_generate_derived_copy_body` is the SECOND chain that re-implemented the decision, and it had three gaps rather than one: a field on the automatic Copy tier (struct or enum), a fixed-ARRAY field and a TUPLE field — the last two with no arm at all, though design 139 gives each the strongest tier it holds. One `@synthesize` line and a struct literal is the whole reproducer; no bound, no type parameter, no `Vector`. Probed pre-fix, the array field aborts and the struct and tuple fields emit a `copy` body that is `load`/`insertvalue`/`ret` beside a `deinit` that fully releases what it never retained |\\n+| V103 | control: routing every copy site through ONE funnel duplicates EXACTLY ONCE at every tier that already worked | `V103_copy_funnel_duplicates_exactly_once.saw` | Design 271, V80/V84's lesson at the copy-emission sites. SL-265 was a MISSING duplication and the fix gives three chains one funnel, which carries the opposite risk wherever those chains already had an answer: an arm that runs twice, or an emitter swapped for one that retains again. A doubled retain LEAKS rather than crashing — exit 0, no output change — so only a count says it did not happen. The hand-written `copy()` hook is the counter (design 219: it runs at exactly the sites the compiler inserts) and it also pins the funnel's ARM ORDER, since the hook is asked for BEFORE the trivially-copyable test and a POD receiver whose author wrote a body must still run it. The escaping-closure arm moved too and is counted in V100 through a STRUCT member instead: a DIRECT `Vector<closure>` element is useless as an oracle, because the vector never releases its element's env and a by-value capture does not survive the frame that built the closure — both probed with no copy anywhere in the program, so both are drop/capture defects (SL-269) |\\n+\\n ## Places (`borrows` / `lend`) — window discipline\\n \\n Claim source: spec 4 *Places (`borrows` and `lend`)*; designs 141, 146, 179, 188 u3\\ndiff --git a/examples/conformance/V100_bounded_copy_at_the_auto_tier_retains.saw b/examples/conformance/V100_bounded_copy_at_the_auto_tier_retains.saw\\nnew file mode 100644\\nindex 00000000..43947506\\n--- /dev/null\\n+++ b/examples/conformance/V100_bounded_copy_at_the_auto_tier_retains.saw\\n@@ -0,0 +1,98 @@\\n+// Conformance row V100 — a `T: ExplicitCopy`-bounded `copy()` duplicates an\\n+// AUTOMATIC Copy-tier element at its own tier (SL-265, design 271)\\n+//\\n+// Design 159 puts a struct or enum whose members are all trivial/Copy on the\\n+// Copy tier with NO declaration written and none owed, and its copy RETAINS\\n+// each member. Design 219 makes every Copy type satisfy `T: ExplicitCopy`, so\\n+// such a type is admitted by `Vector`'s bound and `buf[i].copy()` runs at it.\\n+// Codegen's `.copy()` interception carried its own arm list and had no arm for\\n+// that tier — it declares nothing, so no \\\"declares a policy\\\" test sees it —\\n+// and the spelling fell out as a BITWISE duplicate. The copy's `String`s\\n+// aliased the original's, so the original's drop freed storage the copy still\\n+// held: reads came back empty and the second release aborted.\\n+//\\n+// The strings MUST be heap strings. A string literal is immortal (refcount\\n+// -1), so a literal-filled element hides a missing retain completely — which\\n+// is why row V25, the tier's other behavioural row, passes either way.\\n+//\\n+// Both failure modes are counted, not just the fatal one. The `Watched` rows\\n+// own an escaping closure whose env holds an `Arc` reference: an env retain is\\n+// what keeps that reference alive, so a MISSING retain drops the count early\\n+// and a DOUBLED one never drops it at all.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// struct: alpha-built beta-built\\n+// enum: named-built\\n+// arc after the duplicate dies: 2\\n+// arc after the original dies: 1\\n+\\n+struct Rec {\\n+    text: String\\n+    tag: String?\\n+}\\n+\\n+enum Tag {\\n+    case Named(s: String),\\n+    case Anon\\n+}\\n+\\n+struct Res { id: Int }\\n+extension Res: NoCopy {}\\n+\\n+struct Watched { probe: () sync -> Int }\\n+\\n+func heap_string(seed: String) -> String {\\n+    var sb = StringBuilder()\\n+    try! sb.append(seed)\\n+    try! sb.append(\\\"-built\\\")\\n+    sb.build()\\n+}\\n+\\n+// The original drops at the end of each builder; only the duplicate comes out,\\n+// so every read below is a read of storage the duplicate must own outright.\\n+func copied_structs() -> Vector<Rec> {\\n+    var original = Vector<Rec>()\\n+    try! original.push(Rec(text: heap_string(\\\"alpha\\\"), tag: None))\\n+    try! original.push(Rec(text: heap_string(\\\"beta\\\"), tag: heap_string(\\\"tag\\\")))\\n+    let duplicate = original.copy()\\n+    move duplicate\\n+}\\n+\\n+func copied_enums() -> Vector<Tag> {\\n+    var original = Vector<Tag>()\\n+    try! original.push(Tag.Named(s: heap_string(\\\"named\\\")))\\n+    try! original.push(Tag.Anon)\\n+    let duplicate = original.copy()\\n+    move duplicate\\n+}\\n+\\n+func sink(v: Vector<Watched>) -> Int { v.len() }\\n+\\n+func main() {\\n+    let structs = copied_structs()\\n+    print(\\\"struct: {structs[0].text} {structs[1].text}\\\")\\n+\\n+    let tags = copied_enums()\\n+    match tags[0] {\\n+        case Named(s) -> print(\\\"enum: {s}\\\"),\\n+        case Anon -> print(\\\"enum: ANON\\\")\\n+    }\\n+\\n+    // The counting half. `shared` is one reference; `held` is the second, moved\\n+    // into a closure env that a `Watched` element owns.\\n+    let shared = try! Arc(value: Res(id: 1))\\n+    let held = shared.copy()\\n+    var watched = Vector<Watched>()\\n+    try! watched.push(Watched(probe: { [move held] in 7 }))\\n+\\n+    let duplicate = watched.copy()\\n+    let _ = sink(move duplicate)\\n+    // The element copy retained the env, so the env outlives the duplicate and\\n+    // still holds its Arc reference. A missing retain reads 1 here.\\n+    print(\\\"arc after the duplicate dies: {shared.strong_count()}\\\")\\n+\\n+    let _ = sink(move watched)\\n+    // ...and the env is released exactly once, never twice and never not at\\n+    // all. A doubled retain reads 2 here.\\n+    print(\\\"arc after the original dies: {shared.strong_count()}\\\")\\n+}\\ndiff --git a/examples/conformance/V101_copy_family_carries_the_tier.saw b/examples/conformance/V101_copy_family_carries_the_tier.saw\\nnew file mode 100644\\nindex 00000000..6ed7d0c4\\n--- /dev/null\\n+++ b/examples/conformance/V101_copy_family_carries_the_tier.saw\\n@@ -0,0 +1,83 @@\\n+// Conformance row V101 — EVERY bounded-generic `copy()` in the family carries\\n+// the element's tier, not just `Vector.copy` (SL-265, design 271)\\n+//\\n+// `Vector.copy` is where SL-265 was found, but the defect was in what\\n+// `x.copy()` LOWERS TO under a substituted Copy-tier `T` — so every body that\\n+// spells the same thing under a Copy-family bound had it. The std census names\\n+// four: `Vector.copy`, `Vector.try_copy`, `Map.copy` and `Map.try_copy`\\n+// (design 251's conditional conformance, which duplicates keys AND values).\\n+// A user-level generic is the fifth row because the mechanism was never\\n+// std-specific — nothing about `sawc/std` was in the lowering decision.\\n+//\\n+// `Set` is absent on purpose and not by oversight: `Set<Rec>` is REFUSED at the\\n+// instantiation, its element gate calling the automatic tier \\\"move-only, not\\n+// retainable\\\" while the `Map` VALUE gate two rows up accepts the same type\\n+// (SL-270). So `Set.copy`/`Set.try_copy` cannot reach this class today.\\n+//\\n+// Heap strings throughout: a literal is immortal and passes vacuously.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// vector try_copy: alpha-built\\n+// map copy: key-built value-built\\n+// map try_copy: key-built value-built\\n+// user generic: generic-built\\n+\\n+struct Rec {\\n+    text: String\\n+}\\n+\\n+func heap_string(seed: String) -> String {\\n+    var sb = StringBuilder()\\n+    try! sb.append(seed)\\n+    try! sb.append(\\\"-built\\\")\\n+    sb.build()\\n+}\\n+\\n+// The duplicating body a user writes: the bound is design 219's licence for\\n+// the spelling, and every Copy type satisfies it.\\n+func duplicate_of<T: ExplicitCopy>(x: &T) -> T { x.copy() }\\n+\\n+func vector_try_copy() -> Vector<Rec> {\\n+    var original = Vector<Rec>()\\n+    try! original.push(Rec(text: heap_string(\\\"alpha\\\")))\\n+    let duplicate = try! original.try_copy()\\n+    move duplicate\\n+}\\n+\\n+func map_copy() -> Map<String, Rec> {\\n+    var original = Map<String, Rec>()\\n+    let _ = try! original.insert(heap_string(\\\"key\\\"), Rec(text: heap_string(\\\"value\\\")))\\n+    let duplicate = original.copy()\\n+    move duplicate\\n+}\\n+\\n+func map_try_copy() -> Map<String, Rec> {\\n+    var original = Map<String, Rec>()\\n+    let _ = try! original.insert(heap_string(\\\"key\\\"), Rec(text: heap_string(\\\"value\\\")))\\n+    let duplicate = try! original.try_copy()\\n+    move duplicate\\n+}\\n+\\n+func user_generic() -> Rec {\\n+    let original = Rec(text: heap_string(\\\"generic\\\"))\\n+    let duplicate = duplicate_of(&original)\\n+    move duplicate\\n+}\\n+\\n+func show_map(label: String, m: &Map<String, Rec>) {\\n+    m.each({ k, v in print(\\\"{label}: {k} {v.text}\\\") })\\n+}\\n+\\n+func main() {\\n+    let v = vector_try_copy()\\n+    print(\\\"vector try_copy: {v[0].text}\\\")\\n+\\n+    let a = map_copy()\\n+    show_map(\\\"map copy\\\", &a)\\n+\\n+    let b = map_try_copy()\\n+    show_map(\\\"map try_copy\\\", &b)\\n+\\n+    let r = user_generic()\\n+    print(\\\"user generic: {r.text}\\\")\\n+}\\ndiff --git a/examples/conformance/V102_derived_copy_duplicates_every_field.saw b/examples/conformance/V102_derived_copy_duplicates_every_field.saw\\nnew file mode 100644\\nindex 00000000..d01f254f\\n--- /dev/null\\n+++ b/examples/conformance/V102_derived_copy_duplicates_every_field.saw\\n@@ -0,0 +1,75 @@\\n+// Conformance row V102 — a `@synthesize`d memberwise `copy()` duplicates EVERY\\n+// field at that field's own tier (SL-265's sibling, design 271)\\n+//\\n+// The obligation-4 sweep on SL-265: the defect is a copy-lowering decision that\\n+// had been re-implemented per emission site, so the question was which OTHER\\n+// sites had re-implemented it. `_generate_derived_copy_body` — the body behind\\n+// `@synthesize extension T: ExplicitCopy {}` — was the second, and it had more\\n+// gaps than the first, because its arm list was only three long: a\\n+// DECLARED-tier enum, an `Optional`, and a field type whose conformances name a\\n+// copy policy. Everything else survived as the bare load that produced it.\\n+//\\n+// Four field kinds therefore aliased instead of duplicating, and NONE of them\\n+// needs a generic to reach — one `@synthesize` line and a struct literal is the\\n+// whole program:\\n+//   - a field on the AUTOMATIC Copy tier (design 159), struct or enum, which\\n+//     declares nothing and so answers no \\\"declares a policy\\\" test;\\n+//   - a fixed-ARRAY field and a TUPLE field, which had no arm at all, though\\n+//     design 139 gives each the strongest tier it holds.\\n+//\\n+// Heap strings throughout: a literal is immortal and would pass either way.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// struct field: inner-built\\n+// enum field: payload-built\\n+// array field: zero-built one-built\\n+// tuple field: tuple-built 7\\n+\\n+struct Inner { text: String }\\n+\\n+enum Payload {\\n+    case Full(s: String),\\n+    case Empty\\n+}\\n+\\n+struct Holder {\\n+    inner: Inner\\n+    payload: Payload\\n+    names: [String; 2]\\n+    pair: (String, Int)\\n+}\\n+\\n+@synthesize\\n+extension Holder: ExplicitCopy {}\\n+\\n+func heap_string(seed: String) -> String {\\n+    var sb = StringBuilder()\\n+    try! sb.append(seed)\\n+    try! sb.append(\\\"-built\\\")\\n+    sb.build()\\n+}\\n+\\n+// Takes the duplicate by value and drops it, so every read afterwards is a read\\n+// of storage the ORIGINAL must still own outright. A field the derived body\\n+// aliased instead of duplicating is freed right here.\\n+func drop_it(h: Holder) -> Int { 0 }\\n+\\n+func main() {\\n+    let original = Holder(\\n+        inner: Inner(text: heap_string(\\\"inner\\\")),\\n+        payload: Payload.Full(s: heap_string(\\\"payload\\\")),\\n+        names: [heap_string(\\\"zero\\\"), heap_string(\\\"one\\\")],\\n+        pair: (heap_string(\\\"tuple\\\"), 7),\\n+    )\\n+\\n+    let duplicate = original.copy()\\n+    let _ = drop_it(move duplicate)\\n+\\n+    print(\\\"struct field: {original.inner.text}\\\")\\n+    match original.payload {\\n+        case Full(s) -> print(\\\"enum field: {s}\\\"),\\n+        case Empty -> print(\\\"enum field: EMPTY\\\")\\n+    }\\n+    print(\\\"array field: {original.names[0]} {original.names[1]}\\\")\\n+    print(\\\"tuple field: {original.pair.0} {original.pair.1}\\\")\\n+}\\ndiff --git a/examples/conformance/V103_copy_funnel_duplicates_exactly_once.saw b/examples/conformance/V103_copy_funnel_duplicates_exactly_once.saw\\nnew file mode 100644\\nindex 00000000..28e6c6f2\\n--- /dev/null\\n+++ b/examples/conformance/V103_copy_funnel_duplicates_exactly_once.saw\\n@@ -0,0 +1,101 @@\\n+// Conformance row V103 — control: routing every copy site through ONE funnel\\n+// duplicates EXACTLY ONCE, at every tier that already worked (design 271)\\n+//\\n+// V80/V84's lesson at the copy-emission sites. SL-265 was a MISSING\\n+// duplication, and the obvious fix — give the incomplete chains the arm they\\n+// lacked — carries the opposite risk everywhere those chains already had an\\n+// answer: an arm that now runs twice, or an emitter swapped for one that\\n+// retains a second time. A doubled retain LEAKS rather than crashing, so it\\n+// exits 0 and no output changes; only a count says it did not happen.\\n+//\\n+// The rows are the arms design 271 MOVED, not the whole tier list:\\n+//   - a hand-written `copy()` hook inside a `Copy` conformance, which runs at\\n+//     exactly the sites the compiler inserts (design 219) and so counts\\n+//     duplications directly. It is also what pins the funnel's ARM ORDER: the\\n+//     hook is asked for before the trivially-copyable test, so a POD receiver\\n+//     whose author wrote a body still runs that body.\\n+//   - a DECLARED-tier enum element, whose payload-deep copy moved from\\n+//     `_emit_enum_deep_copy` to the retain glue;\\n+//   - an `ExplicitCopy` element, whose duplicate must still be an INDEPENDENT\\n+//     buffer rather than a second owner of one.\\n+//\\n+// The escaping-CLOSURE arm moved too, and it is counted in V100 rather than\\n+// here — through a STRUCT member, which is the only position where a closure\\n+// env's refcount is observable today. A DIRECT `Vector<closure>` element cannot\\n+// serve as an oracle at all: the vector never releases its element's env, and\\n+// a by-value capture does not survive the frame that built the closure. Both\\n+// are probed with NO copy anywhere in the program, so both are drop/capture\\n+// defects rather than copy-emission ones, and both are filed as SL-269.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// copy hook\\n+// copy hook\\n+// hooked: 2\\n+// enum payload independent: 1 2\\n+// vector element independent: 1 2\\n+\\n+struct Counted { n: Int }\\n+\\n+extension Counted: Copy {\\n+    func copy(&self) -> Counted {\\n+        print(\\\"copy hook\\\")\\n+        Counted(n: self.n)\\n+    }\\n+}\\n+\\n+enum Bag {\\n+    case Full(v: Vector<Int>),\\n+    case Empty\\n+}\\n+\\n+@synthesize\\n+extension Bag: ExplicitCopy {}\\n+\\n+func bag_len(b: &Bag) -> Int {\\n+    match b {\\n+        case Full(v) -> v.len(),\\n+        case Empty -> 0\\n+    }\\n+}\\n+\\n+// The original drops here; only the duplicate comes out, so its payload buffers\\n+// must be its own.\\n+func copied_bags() -> Vector<Bag> {\\n+    var original = Vector<Bag>()\\n+    var one = Vector<Int>()\\n+    try! one.push(1)\\n+    var two = Vector<Int>()\\n+    try! two.push(1)\\n+    try! two.push(2)\\n+    try! original.push(Bag.Full(v: move one))\\n+    try! original.push(Bag.Full(v: move two))\\n+    let duplicate = original.copy()\\n+    move duplicate\\n+}\\n+\\n+func main() {\\n+    // 1. The hook counts implicit duplications directly: two elements, two\\n+    //    copies, never four.\\n+    var hooked = Vector<Counted>()\\n+    try! hooked.push(Counted(n: 1))\\n+    try! hooked.push(Counted(n: 2))\\n+    let hooked_dup = hooked.copy()\\n+    print(\\\"hooked: {hooked_dup.len()}\\\")\\n+\\n+    // 2. A declared-tier enum element: the duplicate's payload is its OWN\\n+    //    buffer, so it survives the original's death intact.\\n+    let bags_dup = copied_bags()\\n+    print(\\\"enum payload independent: {bag_len(&bags_dup[0])} {bag_len(&bags_dup[1])}\\\")\\n+\\n+    // 3. An ExplicitCopy element, the same claim by mutation rather than by\\n+    //    survival: a push into the duplicate is invisible through the original.\\n+    var nested = Vector<Vector<Int>>()\\n+    var row = Vector<Int>()\\n+    try! row.push(1)\\n+    try! nested.push(move row)\\n+    var nested_dup = nested.copy()\\n+    try! nested_dup[0].push(2)\\n+    let original_len = nested[0].len()\\n+    let duplicate_len = nested_dup[0].len()\\n+    print(\\\"vector element independent: {original_len} {duplicate_len}\\\")\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 41d50d53..c6cc9fba 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -3536,19 +3536,7 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n     // records and the segment arena drop with `lexed` at the end of this call.\\n     var tokens = Vector<Token>()\\n     match lex(source) {\\n-        case Ok(r) -> {\\n-            // The token stream is cloned out element by element rather than with\\n-            // `r.tokens.copy()`: a `Vector<T>.copy()` whose element is an\\n-            // automatic-Copy-tier STRUCT duplicates it bitwise and never retains\\n-            // its `String` fields (SL-265), which leaves every token's text\\n-            // dangling once `r` drops. `clone_token` reads the fields, so each\\n-            // `String` is retained the way every other clone in this file is.\\n-            var i = 0\\n-            while i < r.tokens.len() {\\n-                try! tokens.push(clone_token(&r.tokens[i]))\\n-                i = i + 1\\n-            }\\n-        },\\n+        case Ok(r) -> { tokens = r.tokens.copy() },\\n         case Err(e) -> { return problem(e.message, e.line, e.col) }\\n     }\\n     var compiler = Compiler(source: source, tokens: move tokens, records: Vector<RecordIR>(), enums: Vector<EnumIR>(),\\ndiff --git a/sawc/codegen/calls.py b/sawc/codegen/calls.py\\nindex 84b961d1..fee93417 100644\\n--- a/sawc/codegen/calls.py\\n+++ b/sawc/codegen/calls.py\\n@@ -2056,72 +2056,50 @@ class CallsMixin:\\n                         struct_name = name\\n                         break\\n \\n-        # Array `.copy()` (design 33): a fixed array copies per element in index\\n-        # order. `[trivial; N]` is a bitwise copy of the whole value; an array of\\n-        # ExplicitCopy/Copy elements calls each element's copy(). The\\n-        # receiver has no struct_name (it is an LLVM `[N x T]`), so intercept here\\n-        # before the struct-copy path.\\n-        if expr.method_name == \\\"copy\\\" and len(expr.arguments) == 0:\\n-            recv_type = self._expr_type(expr.object)\\n-            if recv_type is not None and recv_type.kind == TypeKind.ARRAY:\\n-                return self._emit_array_deep_copy(obj_val, recv_type)\\n-            # `Optional<T>.copy()` (design 139) — same reason to intercept here:\\n-            # the receiver is an LLVM `{i1, T}` with no struct_name for the\\n-            # copy-method dispatch below to mangle.\\n-            if recv_type is not None and recv_type.kind == TypeKind.OPTIONAL:\\n-                return self._emit_optional_deep_copy(obj_val, recv_type)\\n-            # `.copy()` on a TUPLE — intercepted for the same reason: the\\n-            # receiver is an anonymous LLVM struct with no struct_name, so the\\n-            # dispatch below would find no copy() and fall through to the\\n-            # bitwise \\\"auto-Copy\\\" return, aliasing every owned element while the\\n-            # tuple's drop glue released it twice (DF-151i).\\n-            if recv_type is not None and recv_type.kind == TypeKind.TUPLE:\\n-                return self._emit_tuple_deep_copy(obj_val, recv_type)\\n-            # A declared copying policy on an ENUM derives a payload-deep copy\\n-            # (design 139). Enums carry no method symbols, so it is emitted\\n-            # inline here rather than dispatched to.\\n-            if (recv_type is not None and recv_type.kind == TypeKind.ENUM\\n-                    and recv_type.enum_name\\n-                    and self.namespace.declared_copy_tier(recv_type.enum_name)\\n-                    in ('implicit', 'explicit')):\\n-                return self._emit_enum_deep_copy(obj_val, recv_type)\\n-\\n-        # Auto-Copy: `.copy()` on a trivially-copyable receiver (a primitive, or\\n-        # a POD struct with no copy() method) lowers to a bitwise copy, i.e. the\\n-        # value itself. Types with a real copy() method fall through to dispatch.\\n-        # A receiver that owns a resource (NoCopy / Deinit) but has no copy() is\\n-        # not Copy: this is where a Vector<File>.copy() monomorphization fails,\\n-        # with a diagnostic naming the offending element type.\\n+        # A SOURCE `.copy()` whose receiver owns no emitted `copy` symbol —\\n+        # a fixed array, a tuple, an `Optional`, an enum (enums carry no method\\n+        # symbols at all), an escaping closure, a primitive, or an aggregate on\\n+        # the AUTOMATIC Copy tier (design 159). Every one of those is a copy at\\n+        # its own tier, and deciding which is `_emit_copy_value`'s job (design\\n+        # 271 — read its docstring, which names this as an entry point).\\n+        #\\n+        # This site used to carry its own arm list: array / optional / tuple /\\n+        # DECLARED-tier enum / closure, then `return obj_val` for everything\\n+        # else. The automatic Copy tier matched no arm — it declares nothing, so\\n+        # no \\\"declares a policy\\\" test sees it — and fell out of that last line as\\n+        # a BITWISE duplicate whose `String` members were never retained\\n+        # (SL-265). A receiver with a real `copy` symbol still falls through to\\n+        # ordinary method dispatch below, which is where a hand-written hook's\\n+        # own body belongs.\\n         if expr.method_name == \\\"copy\\\" and len(expr.arguments) == 0:\\n             copy_mangled = self._mangle_method_name(struct_name, \\\"copy\\\") if struct_name else None\\n             if copy_mangled is None or copy_mangled not in self.functions:\\n-                # An escaping closure is Copy (design 73): `.copy()` bumps\\n-                # the env refcount so the duplicate and the original each release\\n-                # exactly once (design 77 item 3). This is the element-copy path\\n-                # `Vector<() -> Int>.copy()` reaches via `buf[i].copy()`; without\\n-                # the retain the shared env is freed twice (exit 133). The value\\n-                # bytes are unchanged (the env pointer is aliased) — only the\\n-                # atomic increment, null-env guarded inside the retain helper.\\n-                recv_saw_copy = self._expr_type(expr.object)\\n-                if (recv_saw_copy is not None\\n-                        and recv_saw_copy.kind == TypeKind.FUNCTION\\n-                        and isinstance(obj_val.type, ir.LiteralStructType)\\n-                        and len(obj_val.type.elements) == 3):\\n-                    env_ptr = self.builder.extract_value(obj_val, 1, name=\\\"copy_env\\\")\\n-                    dtor_ptr = self.builder.extract_value(obj_val, 2, name=\\\"copy_dtor\\\")\\n-                    self._emit_closure_env_retain(env_ptr, dtor_ptr)\\n-                    return obj_val\\n-                if struct_name is not None:\\n-                    conformances = self.namespace.get_conformances(struct_name)\\n-                    if (self.namespace.names_copy_tier(conformances)\\n-                            or any(c in (\\\"NoCopy\\\", \\\"ExplicitCopy\\\", \\\"Deinit\\\")\\n-                                   for c in conformances)):\\n-                        raise ValueError(\\n-                            f\\\"cannot copy value of type `{struct_name}`: it is not Copy \\\"\\n-                            f\\\"(owns a resource and has no copy()); use a copyable element \\\"\\n-                            f\\\"type or implement Copy/ExplicitCopy\\\"\\n-                        )\\n-                return obj_val\\n+                recv_type = self._expr_type(expr.object)\\n+                # A receiver that owns a resource and has no copy at any tier is\\n+                # not duplicable: this is where a `Vector<File>.copy()`\\n+                # monomorphization fails, naming the offending element type. The\\n+                # tier is the accurate predicate — the old conformance-name test\\n+                # also caught every DECLARED ExplicitCopy enum, which is exactly\\n+                # the shape that legitimately has no symbol.\\n+                if (recv_type is not None\\n+                        and self.namespace.copy_tier(recv_type) == 'nocopy'):\\n+                    raise ValueError(\\n+                        f\\\"cannot copy value of type `{recv_type}`: it is not Copy \\\"\\n+                        f\\\"(owns a resource and has no copy()); use a copyable element \\\"\\n+                        f\\\"type or implement Copy/ExplicitCopy\\\"\\n+                    )\\n+                # A STRUCT that DECLARED a copy policy always has an emitted\\n+                # `copy`; reaching here without one would silently hand the\\n+                # funnel a body the author wrote and it cannot see.\\n+                if (recv_type is not None and recv_type.kind == TypeKind.STRUCT\\n+                        and struct_name is not None\\n+                        and any(c in (\\\"Copy\\\", \\\"ExplicitCopy\\\")\\n+                                for c in self.namespace.get_conformances(struct_name))):\\n+                    raise ValueError(\\n+                        f\\\"no `copy` symbol for type `{struct_name}`, which declares \\\"\\n+                        f\\\"a copy policy\\\"\\n+                    )\\n+                return self._emit_copy_value(obj_val, recv_type)\\n \\n         # Hashable `.hash(&h)` (design 48): the single lowering point for the\\n         # streaming hash. A receiver with a real `hash` method (String, or a\\ndiff --git a/sawc/codegen/methods.py b/sawc/codegen/methods.py\\nindex 71a809fa..dfa4be69 100644\\n--- a/sawc/codegen/methods.py\\n+++ b/sawc/codegen/methods.py\\n@@ -308,52 +308,53 @@ class MethodsMixin:\\n             ], name=f\\\"{field_name}_ptr\\\")\\n             field_val = self.builder.load(field_ptr, name=field_name)\\n \\n-            # design 139: two field kinds have their copy derived INLINE and so\\n-            # own no `copy` symbol for the lookup below to find. Both must be\\n-            # handled before it, because falling through would either raise on a\\n-            # symbol that cannot exist (an enum) or, worse, silently bitwise-alias\\n-            # an owning field (an optional, whose conformance name is None, so it\\n-            # never even reached the check).\\n-            conf_name = self._get_type_name_for_conformance(field_type) if field_type else None\\n-            conformances = self.namespace.get_conformances(conf_name) if conf_name else []\\n+            # Each field duplicates at ITS OWN tier, and which tier that is is\\n+            # `_emit_copy_value`'s question (design 271 — read its docstring,\\n+            # which names this as an entry point). This loop used to answer it\\n+            # itself, with three arms: an enum of DECLARED tier, an optional,\\n+            # and a field type whose conformances name a copy policy. A field on\\n+            # the AUTOMATIC Copy tier (design 159 — `struct Inner { text: String }`,\\n+            # declaring nothing and owing nothing) matched none of them and the\\n+            # bare load survived as the \\\"copy\\\", so the derived duplicate aliased\\n+            # the original's `String` and the two releases underflowed its\\n+            # refcount (SL-265's sibling, reachable with no generic in sight).\\n+            # Tuple and fixed-array fields had no arm at all and aliased the\\n+            # same way.\\n+            #\\n             # A field annotated with a bare enum name reaches here tagged STRUCT\\n             # (the parser defaults an unknown capitalized name that way, and not\\n-            # every path re-resolves it), so retag before asking.\\n-            enum_field = (self.namespace._normalize_struct_enum(field_type)\\n-                          if field_type is not None else None)\\n-            inline_enum_copy = (\\n-                enum_field is not None\\n-                and enum_field.kind == TypeKind.ENUM\\n-                and enum_field.enum_name is not None\\n-                and self.namespace.declared_copy_tier(enum_field.enum_name)\\n-                in ('implicit', 'explicit'))\\n-            if inline_enum_copy:\\n-                field_val = self._emit_enum_deep_copy(field_val, enum_field)\\n-            elif field_type is not None and field_type.kind == TypeKind.OPTIONAL:\\n-                field_val = self._emit_optional_deep_copy(field_val, field_type)\\n-            # Does this field's type carry its own copy()? (Copy or\\n-            # ExplicitCopy). If so, invoke it; otherwise the load is a bitwise copy.\\n-            elif (self.namespace.names_copy_tier(conformances)\\n-                  or \\\"ExplicitCopy\\\" in conformances):\\n-                copy_fn = self._field_copy_fn(field_type)\\n-                if copy_fn is None:\\n-                    # The field's type declares a copy policy, so it HAS a\\n-                    # `copy()`; failing to find the symbol would silently emit a\\n-                    # bitwise alias of an owning field, and both copies would\\n-                    # then free the same storage. Refuse instead.\\n-                    # design 192 unit 2: ValueError is codegen's one\\n-                    # internal-failure convention, and the wrapper supplies the\\n-                    # \\\"internal compiler error\\\" prefix — so the message says\\n-                    # only what happened.\\n-                    raise ValueError(\\n-                        f\\\"no `copy` symbol for field `{field_name}` of type \\\"\\n-                        f\\\"`{field_type}` while deriving copy() for \\\"\\n-                        f\\\"`{struct_name}`\\\")\\n-                field_val = self.builder.call(\\n-                    copy_fn,\\n-                    [self._self_operand(copy_fn, field_val,\\n-                                        name=f\\\"{field_name}_copy_self\\\")],\\n-                    name=f\\\"{field_name}_copy\\\")\\n+            # every path re-resolves it), so retag before handing it over.\\n+            conf_name = self._get_type_name_for_conformance(field_type) if field_type else None\\n+            conformances = self.namespace.get_conformances(conf_name) if conf_name else []\\n+            copy_type = (self.namespace._normalize_struct_enum(field_type)\\n+                         if field_type is not None else None)\\n+            if copy_type is not None:\\n+                # A field type that DECLARED a copy policy HAS a `copy()`, and\\n+                # a hand-written one is a body the funnel cannot see. Resolve it\\n+                # here — `_field_copy_fn` fills declared default type arguments,\\n+                # which the funnel's plain mangling does not (DF-128c) — and\\n+                # refuse rather than emit a bitwise alias of an owning field.\\n+                # design 192 unit 2: ValueError is codegen's one internal-failure\\n+                # convention, and the wrapper supplies the \\\"internal compiler\\n+                # error\\\" prefix — so the message says only what happened.\\n+                declares_policy = (\\n+                    copy_type.kind == TypeKind.STRUCT\\n+                    and (self.namespace.names_copy_tier(conformances)\\n+                         or \\\"ExplicitCopy\\\" in conformances))\\n+                if declares_policy:\\n+                    copy_fn = self._field_copy_fn(field_type)\\n+                    if copy_fn is None:\\n+                        raise ValueError(\\n+                            f\\\"no `copy` symbol for field `{field_name}` of type \\\"\\n+                            f\\\"`{field_type}` while deriving copy() for \\\"\\n+                            f\\\"`{struct_name}`\\\")\\n+                    field_val = self.builder.call(\\n+                        copy_fn,\\n+                        [self._self_operand(copy_fn, field_val,\\n+                                            name=f\\\"{field_name}_copy_self\\\")],\\n+                        name=f\\\"{field_name}_copy\\\")\\n+                else:\\n+                    field_val = self._emit_copy_value(field_val, copy_type)\\n \\n             result = self.builder.insert_value(result, field_val, i)\\n \\ndiff --git a/sawc/codegen/resources.py b/sawc/codegen/resources.py\\nindex 242e729a..aaf95fd3 100644\\n--- a/sawc/codegen/resources.py\\n+++ b/sawc/codegen/resources.py\\n@@ -1236,61 +1236,13 @@ class ResourcesMixin:\\n         if self.type_param_context:\\n             saw_type = saw_type.substitute(self.type_param_context)\\n \\n-        if saw_type.kind == TypeKind.ARRAY:\\n-            return self._emit_array_deep_copy(value, saw_type)\\n-\\n-        # A tuple copies per element for the same reason an array does: it is a\\n-        # positional aggregate that owns its parts, and design 139 gives it the\\n-        # strongest element's tier. This has to land WITH the drop glue above —\\n-        # a bitwise tuple copy beside a real element drop is one allocation and\\n-        # two releases, which is the over-release half of DF-151f.\\n-        if saw_type.kind == TypeKind.TUPLE:\\n-            return self._emit_tuple_deep_copy(value, saw_type)\\n-\\n-        # An escaping closure is Copy (design 73): copying it bumps the\\n-        # shared heap env's refcount and returns the same (aliased) value. A\\n-        # null-env / non-owning closure retains as a no-op. Non-escaping closures\\n-        # are borrows — bitwise, no retain. (The escaping bit is reliable here:\\n-        # `saw_type` was already substituted through the monomorphization context\\n-        # above, so a container element type carries it.)\\n-        if (saw_type.kind == TypeKind.FUNCTION\\n-                and saw_type.func_is_escaping):\\n-            if (isinstance(value.type, ir.LiteralStructType)\\n-                    and len(value.type.elements) == 3):\\n-                env_ptr = self.builder.extract_value(value, 1, name=\\\"copy_env\\\")\\n-                dtor_ptr = self.builder.extract_value(value, 2, name=\\\"copy_dtor\\\")\\n-                self._emit_closure_env_retain(env_ptr, dtor_ptr)\\n-            return value\\n-\\n-        # A type with its own copy() method (Copy String/Arc/user) — call\\n-        # it (a cheap refcount bump for String/Arc).\\n-        type_name = self._type_method_base(saw_type)\\n-        if type_name is not None:\\n-            copy_method_name = self._mangle_method_name(type_name, \\\"copy\\\")\\n-            copy_fn = self.functions.get(copy_method_name)\\n-            if copy_fn is not None:\\n-                # `copy` returns Self; its RECEIVER's shape is whatever the\\n-                # emitted signature says (design 261), read through the funnel.\\n-                return self.builder.call(\\n-                    copy_fn,\\n-                    [self._self_operand(copy_fn, value, name=\\\"copy_self\\\")],\\n-                    name=\\\"copy_result\\\")\\n-\\n-        # No whole-type copy() method. If this is an aggregate that OWNS\\n-        # cleanup-needing fields (a struct/enum/optional whose top-level copy class\\n-        # is NoCopy only because of its owning payload — e.g. `MapSlot<String, V>`\\n-        # or an `Arc` inside a struct payload), copy it WITH RETAIN recursively\\n-        # (design 65, L17): reading such a value out of a container it stays in is\\n-        # a duplication, so each owning field's refcount must be bumped and the\\n-        # eventual drop of this copy releases them symmetrically. Trivial\\n-        # aggregates (no owning fields) and genuine leaves fall through to bitwise.\\n-        if (self._needs_cleanup(saw_type)\\n-                and saw_type.kind in (TypeKind.STRUCT, TypeKind.ENUM,\\n-                                      TypeKind.OPTIONAL)):\\n-            return self._deep_copy_value(value, saw_type)\\n-\\n-        # Regular / trivially-copyable types: bitwise copy (the value as-is).\\n-        return value\\n+        # Everything below the substitution is `_emit_copy_value`'s, and this is\\n+        # one of its named entry points (design 271): the transfer site's only\\n+        # extra job is resolving the type to the active monomorphization, so the\\n+        # funnel's own arms can look up the concrete struct/enum layout. The arm\\n+        # list this function used to carry was the SAME list, maintained\\n+        # separately — the drift that shape invites is what SL-265 was.\\n+        return self._emit_copy_value(value, saw_type)\\n \\n     def _transfer_type_for(self, value, dest_saw: SawType) -> SawType:\\n         \\\"\\\"\\\"The SawType that actually describes `value` at a transfer whose\\n@@ -1336,13 +1288,52 @@ class ResourcesMixin:\\n         return self._generate_copy(value, self._transfer_type_for(value, dest_saw))\\n \\n     def _emit_copy_value(self, value, saw_type: SawType):\\n-        \\\"\\\"\\\"Produce an independent copy of a single value of `saw_type`.\\n-\\n-        The per-element building block for array `.copy()` / implicit array copy\\n-        (design 33). Dispatches: nested array -> per-element copy; trivially\\n-        copyable -> the value as-is (bitwise); a type with a real `copy()` method\\n-        (Copy/ExplicitCopy, incl. String) -> a call to it. A resource\\n-        type with no copy path never reaches here (the typechecker gates it).\\n+        \\\"\\\"\\\"THE COPY-EMISSION FUNNEL (design 271) — produce an independent copy of\\n+        a VALUE of `saw_type`, at that type's own copy tier.\\n+\\n+        Obligation 1: \\\"duplicate this value\\\" is a rule quantified over every\\n+        position a duplicate is emitted, so it is ONE chokepoint. ITS ENTRY\\n+        POINTS, all of them:\\n+\\n+        - `_generate_copy` (below) — every transfer site the typechecker marked\\n+          `needs_copy`, after monomorphization substitution.\\n+        - `_emit_array_deep_copy` / `_emit_tuple_deep_copy` /\\n+          `_emit_optional_deep_copy` (below) — the per-element recursions.\\n+        - `_emit_enum_deep_copy` (above) — the per-payload-field recursion.\\n+        - `codegen/calls.py`'s `.copy()` method-call interception — a SOURCE\\n+          `.copy()` whose receiver owns no emitted `copy` symbol.\\n+        - `codegen/methods.py`'s `_generate_derived_copy_body` — the per-field\\n+          duplication inside a `@synthesize`d memberwise `copy()`.\\n+\\n+        The last two used to carry hand-maintained arm lists of their own, each\\n+        an incomplete re-implementation of this one, and each fell through to a\\n+        BITWISE copy for whatever it had no arm for. That is SL-265: an\\n+        AUTOMATIC Copy-tier aggregate (design 159 — a struct or enum whose\\n+        members are all trivial/Copy, declaring nothing and owing nothing) owns\\n+        retainable members and matches no \\\"declares a policy\\\" test, so both\\n+        chains aliased its `String` fields and the duplicate's drop released\\n+        storage the original still owned.\\n+\\n+        The arms, in order, and why the order is this one:\\n+\\n+        1. ARRAY / TUPLE / OPTIONAL — the positional wrappers, each recursing\\n+           per element so every element copies at ITS own tier (design 139).\\n+        2. An escaping CLOSURE — Copy over a refcounted heap env (design 73);\\n+           the value bytes are unchanged, only the env refcount moves.\\n+        3. A real `copy` SYMBOL (String, Arc, a declared Copy/ExplicitCopy\\n+           conformance, a hand-written hook) — asked BEFORE the trivial test,\\n+           because a hand-written `copy()` on a POD receiver is the author's\\n+           body and must run.\\n+        4. Trivially copyable — bitwise, nothing owed.\\n+        5. The design-159 arm: an aggregate with no `copy` of its own that\\n+           still OWNS cleanup-needing members. `_deep_copy_value` retains each\\n+           through `_emit_retain_at`, the exact mirror of the drop glue, so the\\n+           duplicate's eventual drop is balanced.\\n+        6. A genuine leaf with nothing to retain — bitwise.\\n+\\n+        A type that cannot be duplicated at all never reaches here: the\\n+        typechecker refuses it, and the two call sites above raise before\\n+        delegating.\\n         \\\"\\\"\\\"\\n         if saw_type.kind == TypeKind.ARRAY:\\n             return self._emit_array_deep_copy(value, saw_type)\\n@@ -1350,7 +1341,18 @@ class ResourcesMixin:\\n             return self._emit_tuple_deep_copy(value, saw_type)\\n         if saw_type.kind == TypeKind.OPTIONAL:\\n             return self._emit_optional_deep_copy(value, saw_type)\\n-        if self.namespace.is_trivially_copyable(saw_type):\\n+        # An escaping closure is Copy (design 73): duplicating it bumps the\\n+        # shared heap env's refcount and returns the same (aliased) value, so\\n+        # the duplicate and the original each release exactly once. A null-env /\\n+        # non-owning closure retains as a no-op; a NON-escaping closure is a\\n+        # borrow and is bitwise.\\n+        if saw_type.kind == TypeKind.FUNCTION:\\n+            if (saw_type.func_is_escaping\\n+                    and isinstance(value.type, ir.LiteralStructType)\\n+                    and len(value.type.elements) == 3):\\n+                env_ptr = self.builder.extract_value(value, 1, name=\\\"copy_env\\\")\\n+                dtor_ptr = self.builder.extract_value(value, 2, name=\\\"copy_dtor\\\")\\n+                self._emit_closure_env_retain(env_ptr, dtor_ptr)\\n             return value\\n         method_base = self._type_method_base(saw_type)\\n         if method_base is not None:\\n@@ -1360,12 +1362,12 @@ class ResourcesMixin:\\n                 return self.builder.call(\\n                     fn, [self._self_operand(fn, value, name=\\\"elem_copy_self\\\")],\\n                     name=\\\"elem_copy\\\")\\n-        # An aggregate with no copy() of its own but with OWNING fields — the\\n-        # undeclared Copy tier (design 159). `_generate_copy` has always\\n-        # had this fallthrough; the per-ELEMENT path did not, so `[p; 3]` on a\\n-        # `struct P { name: String }` would have splatted one String into three\\n-        # slots with no retain and released it three times. Same recursive\\n-        # retain, so an element's later drop is balanced.\\n+        if self.namespace.is_trivially_copyable(saw_type):\\n+            return value\\n+        # An aggregate with no copy() of its own but with OWNING members — the\\n+        # undeclared Copy tier (design 159). `[p; 3]` on a\\n+        # `struct P { name: String }` would otherwise splat one String into\\n+        # three slots with no retain and release it three times.\\n         if (self._needs_cleanup(saw_type)\\n                 and saw_type.kind in (TypeKind.STRUCT, TypeKind.ENUM)):\\n             return self._deep_copy_value(value, saw_type)\\n\",\"id\":\"SL-265\",\"request_id\":\"sl265-p1-attempt1\",\"title\":\"design 271: one copy-emission funnel (SL-265) — automatic-Copy-tier aggregates retain their members in Vector/Map copy families, user generics, and the derived memberwise copy()\"}","request_id":"sl265-p1-attempt1","sequence":663,"timestamp":"1789183898"}
---
