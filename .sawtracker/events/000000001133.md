---
{"action":"create","actor":"agent:claude-sawtracker","after":{"assignee":"","author":"agent:claude-sawtracker","comments":[],"created":"1790019369","description":"## Summary\n\nWith the `insertvalue` abort cleared (see the sibling SL issue filed from sawtracker ST-37), `sawc` at `-O1` and above aborts in LLVM's machine scheduler on sawtracker's `src/main.saw`. `-O0` compiles clean and sawtracker's whole suite passes, so sawtracker's `build.sh` now pins `-O0` (SAW_NOTES ST-SAW-17). That pin is a workaround for this abort, not a choice about optimization level, and should be removed when this is fixed.\n\n```\nAssertion failed: (Delta.CriticalMax.getUnitInc() >= 0 && Delta.CurrentMax.getUnitInc() >= 0 && \"cannot decrease max pressure\"), function getMaxDownwardPressureDelta, file RegisterPressure.cpp, line 1350\nAbort trap: 6\n```\n\n## Reproducer\n\nsawtracker main after ST-37.p1 merged (`a19e2fb`):\n\n```\ncd sawtracker && SAWOPT=-O2 ./build.sh      # or: sawc -O1 src/main.saw\n```\n\n## What is known\n\nNot minimized; the abort prints no function name. Bisection by stubbing:\n\n- stubbing `mutate_patch`'s body (`return common.fail(\"stub\")`) compiles at the default level;\n- stubbing only `rebase_patch_branch` (a 21 KB frame reduction) still aborts;\n- the ST-SAW-16 call-site reductions (a 55% frame reduction across the whole dispatch chain, `__Frame_mutate_patch` 203,728 → 76,032 bytes) did not clear it.\n\nThat pattern points at the SIZE OF ONE RESUME FUNCTION rather than at frame layout: `__Frame_mutate_patch$m$src_store_resume` is ~76,000 lines of IR, and the sibling `__Frame_dispatch` / `__Frame_response` / `__Frame_run_store` resumes and `__vtdtor$__Frame_run_store` are the same order. Splitting the module to name the function was not attempted. The LLVM assertion itself is a known class of scheduler bug on very large basic blocks / very high register pressure; splitting resume functions or breaking up very long basic blocks in the emitted resume would likely avoid it.\n\n## Impact\n\nsawtracker ships unoptimized until this is fixed. Anything else with a resume function of this size will hit the same abort at the default optimization level.\n","id":"SL-337","labels":["bug","compiler","codegen"],"priority":"high","project":"SL","revision":1,"sequence":1133,"stage":"queued","status":"open","title":"LLVM machine scheduler asserts 'cannot decrease max pressure' (RegisterPressure.cpp:1350) compiling sawtracker at -O1 and above; -O0 compiles, so sawtracker ships unoptimized","updated":"1790019369"},"before":null,"id":"SL-337","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawtracker\",\"description\":\"## Summary\\n\\nWith the `insertvalue` abort cleared (see the sibling SL issue filed from sawtracker ST-37), `sawc` at `-O1` and above aborts in LLVM's machine scheduler on sawtracker's `src/main.saw`. `-O0` compiles clean and sawtracker's whole suite passes, so sawtracker's `build.sh` now pins `-O0` (SAW_NOTES ST-SAW-17). That pin is a workaround for this abort, not a choice about optimization level, and should be removed when this is fixed.\\n\\n```\\nAssertion failed: (Delta.CriticalMax.getUnitInc() >= 0 && Delta.CurrentMax.getUnitInc() >= 0 && \\\"cannot decrease max pressure\\\"), function getMaxDownwardPressureDelta, file RegisterPressure.cpp, line 1350\\nAbort trap: 6\\n```\\n\\n## Reproducer\\n\\nsawtracker main after ST-37.p1 merged (`a19e2fb`):\\n\\n```\\ncd sawtracker && SAWOPT=-O2 ./build.sh      # or: sawc -O1 src/main.saw\\n```\\n\\n## What is known\\n\\nNot minimized; the abort prints no function name. Bisection by stubbing:\\n\\n- stubbing `mutate_patch`'s body (`return common.fail(\\\"stub\\\")`) compiles at the default level;\\n- stubbing only `rebase_patch_branch` (a 21 KB frame reduction) still aborts;\\n- the ST-SAW-16 call-site reductions (a 55% frame reduction across the whole dispatch chain, `__Frame_mutate_patch` 203,728 → 76,032 bytes) did not clear it.\\n\\nThat pattern points at the SIZE OF ONE RESUME FUNCTION rather than at frame layout: `__Frame_mutate_patch$m$src_store_resume` is ~76,000 lines of IR, and the sibling `__Frame_dispatch` / `__Frame_response` / `__Frame_run_store` resumes and `__vtdtor$__Frame_run_store` are the same order. Splitting the module to name the function was not attempted. The LLVM assertion itself is a known class of scheduler bug on very large basic blocks / very high register pressure; splitting resume functions or breaking up very long basic blocks in the emitted resume would likely avoid it.\\n\\n## Impact\\n\\nsawtracker ships unoptimized until this is fixed. Anything else with a resume function of this size will hit the same abort at the default optimization level.\\n\",\"labels\":[\"bug\",\"compiler\",\"codegen\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"st37-sl-scheduler-2026-09-21\",\"stage\":\"queued\",\"title\":\"LLVM machine scheduler asserts 'cannot decrease max pressure' (RegisterPressure.cpp:1350) compiling sawtracker at -O1 and above; -O0 compiles, so sawtracker ships unoptimized\"}","request_id":"st37-sl-scheduler-2026-09-21","sequence":1133,"timestamp":"1790019369"}
---
