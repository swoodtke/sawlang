---
{"action":"comment","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl238","author":"agent:codex-reviewer","comments":[{"author":"agent:claude-sawlang","body":"READY TO DISPATCH, but PAUSED for a morning ruling (overnight run, Sep 11): the fix replaces the ambiguous 0x01-brace-marker encoding, and that marker protocol is SHARED with selfhost/lexer/src/lib.saw (read_string) — so it is a token-representation change in the string layer that ripples into the selfhost lexer (now active SL-242 epic territory) and lexdiff parity. NEEDS a ruling on the encoding approach before dispatch: (a) carry escape provenance out-of-band (segment list / escape-position set) so no content byte is overloaded; (b) an unambiguous in-band marker (e.g. double the marker byte so a real 0x01 is distinguishable); (c) restructure the StringLit token to carry typed segments. Each has different reach into the selfhost lexer. Deferred to the user rather than guessed autonomously.","created":"1789112014","id":"c1"},{"author":"agent:claude-sawlang","body":"RULED (user, Sep 11 morning): option (c) — restructure the string token to carry TYPED SEGMENTS, and do it NOW as this issue's fix (not deferred to the design-259 reconciliation). The 0x01 marker protocol is deleted from BOTH lexers in one patch (lexdiff parity requires lockstep). Rationale: (a) pays a token-shape change without killing the parser re-scan; (b) fixes the bug but keeps the content-bytes-carry-metadata idiom every future consumer must know; (c) is the only option that removes the ambiguity by construction and it aligns with where the selfhost lexer (SL-242) and the reporter's prototype are already headed. Brief: designs/268 (written at dispatch). Dispatches after the SL-226.p1 review-fix round (serial dispatch in force).","created":"1789128838","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-238.p1 (revision 1): design 268: string tokens carry typed segments — delete the 0x01 escaped-brace marker from both lexers (fixes silent U+0001 deletion + the string-pattern face)","created":"1789137231","id":"c3"},{"author":"agent:claude-sawlang","body":"SL-238.p1 SUBMITTED (applied 6d5ef843), proposed, server gate queued — LEAD-VALIDATED. Design 268 implemented as ruled: the 0x01 marker protocol deleted from BOTH lexers; plain STRING tokens are fully decoded (a brace in the value is just a brace); INTERP_STRING tokens carry typed segments (frozen StringSegment dataclass on the Python Token; SegmentKind/StringSegment in a flat per-lex arena on the Saw side, seg_start/seg_count per token — the arena keeps Token in the automatic Copy tier, argued in place). Parser's byte re-scan deleted; interpolation sub-expressions now parse at the EXACT opening-brace position (design-99 columns exact under escapes; ZERO existing EXPECT files drift — the one in-literal column pin has no preceding escape — and two new error tests pin columns the old arithmetic got wrong). Consumer sweep found a SECOND LIVE FACE fixed by construction: string PATTERNS leaked the marker, so case \"a\\{b\" could never match its equal literal — pinned by string_pattern_escaped_brace_matches.saw. Lead independently compiled+ran the issue repro + the pattern face on the branch: 1 / 2 / 1 / pattern-matched, exit 0. Six new examples cover the obligation-4 matrix incl. a generated raw-0x01-byte file; selfhost tests assert the segment record shape. Gates (agent): suite 2459/14x, freestanding 36/36, terminal battery 25/25 (2936s) with lexdiff 0 mismatches over 3108 tracked files + selfhostlex 9/9. NOTE for design 259: the selfhost lexer's public API shape changed (lex returns LexResult carrying the segment arena; format_token takes the arena) — the parser work builds on that. Branch worktree-agent-a18fb0eaf6cfee2b8 tip fd374539; worktree stays until merge.","created":"1789137257","id":"c4"}],"created":"1789078484","description":"Confirmed on production sawlang checkout 30c5a55016aed806a2f94a64d62224c6cafc135a, macOS arm64, using its Python sawc compiler.\n\nThe lexer encodes an escaped brace as byte 0x01 followed by the brace. A legitimate Unicode escape for U+0001 followed by a Unicode escape for a brace produces the same token bytes. The parser unconditionally removes the marker, silently deleting real String content.\n\nReproducer:\n\n```saw\nfunc main() {\n    let escaped = \"\\{\"\n    let literal = \"\\u{1}\\u{7b}\"\n    print(escaped.len())\n    print(literal.len())\n    print(literal.byte_at(0) as Int)\n}\n```\n\nExpected stdout: `1`, `2`, `1`. Actual stdout: `1`, `1`, `123`, exit 0. The intended two-byte String (U+0001 and `{`) is changed to a one-byte `{` String. This example does not use interpolation. The same marker mechanism is used for closing braces.\n\nRelevant implementation: sawc/lexer.py:381-383 inserts `\\x01{` / `\\x01}` for escaped braces; sawc/parser/expressions.py:731 removes either sequence by replacement, without provenance. The existing selfhost/lexer/src/lib.saw read_string follows the same marker protocol, so its StringLit token representation is also ambiguous. Preserve whether a brace came from an escape independently of real content (or use an unambiguous encoding), and test literal marker bytes beside both braces as well as ordinary escaped braces/interpolation.\n\nLocal standalone source: /Users/swoodtke/Projects/sawlang/.build/scratch/string_brace_provenance.saw. Compiled with `/Users/swoodtke/Projects/sawlang/.venv/bin/python sawc/sawc.py .build/scratch/string_brace_provenance.saw -o .build/scratch/string_brace_provenance`, then executed directly. No production files changed. Found while implementing isolated owned-String support in the compiler prototype; that prototype will preserve literal provenance rather than reproduce this behavior.\n","id":"SL-238","labels":["bug","compiler","strings"],"priority":"normal","project":"SL","revision":6,"sequence":571,"stage":"queued","status":"open","title":"String escaped-brace marker deletes literal U+0001 bytes before braces","updated":"1789137257"},"before":{"assignee":"agent:claude-sl238","author":"agent:codex-reviewer","comments":[{"author":"agent:claude-sawlang","body":"READY TO DISPATCH, but PAUSED for a morning ruling (overnight run, Sep 11): the fix replaces the ambiguous 0x01-brace-marker encoding, and that marker protocol is SHARED with selfhost/lexer/src/lib.saw (read_string) — so it is a token-representation change in the string layer that ripples into the selfhost lexer (now active SL-242 epic territory) and lexdiff parity. NEEDS a ruling on the encoding approach before dispatch: (a) carry escape provenance out-of-band (segment list / escape-position set) so no content byte is overloaded; (b) an unambiguous in-band marker (e.g. double the marker byte so a real 0x01 is distinguishable); (c) restructure the StringLit token to carry typed segments. Each has different reach into the selfhost lexer. Deferred to the user rather than guessed autonomously.","created":"1789112014","id":"c1"},{"author":"agent:claude-sawlang","body":"RULED (user, Sep 11 morning): option (c) — restructure the string token to carry TYPED SEGMENTS, and do it NOW as this issue's fix (not deferred to the design-259 reconciliation). The 0x01 marker protocol is deleted from BOTH lexers in one patch (lexdiff parity requires lockstep). Rationale: (a) pays a token-shape change without killing the parser re-scan; (b) fixes the bug but keeps the content-bytes-carry-metadata idiom every future consumer must know; (c) is the only option that removes the ambiguity by construction and it aligns with where the selfhost lexer (SL-242) and the reporter's prototype are already headed. Brief: designs/268 (written at dispatch). Dispatches after the SL-226.p1 review-fix round (serial dispatch in force).","created":"1789128838","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-238.p1 (revision 1): design 268: string tokens carry typed segments — delete the 0x01 escaped-brace marker from both lexers (fixes silent U+0001 deletion + the string-pattern face)","created":"1789137231","id":"c3"}],"created":"1789078484","description":"Confirmed on production sawlang checkout 30c5a55016aed806a2f94a64d62224c6cafc135a, macOS arm64, using its Python sawc compiler.\n\nThe lexer encodes an escaped brace as byte 0x01 followed by the brace. A legitimate Unicode escape for U+0001 followed by a Unicode escape for a brace produces the same token bytes. The parser unconditionally removes the marker, silently deleting real String content.\n\nReproducer:\n\n```saw\nfunc main() {\n    let escaped = \"\\{\"\n    let literal = \"\\u{1}\\u{7b}\"\n    print(escaped.len())\n    print(literal.len())\n    print(literal.byte_at(0) as Int)\n}\n```\n\nExpected stdout: `1`, `2`, `1`. Actual stdout: `1`, `1`, `123`, exit 0. The intended two-byte String (U+0001 and `{`) is changed to a one-byte `{` String. This example does not use interpolation. The same marker mechanism is used for closing braces.\n\nRelevant implementation: sawc/lexer.py:381-383 inserts `\\x01{` / `\\x01}` for escaped braces; sawc/parser/expressions.py:731 removes either sequence by replacement, without provenance. The existing selfhost/lexer/src/lib.saw read_string follows the same marker protocol, so its StringLit token representation is also ambiguous. Preserve whether a brace came from an escape independently of real content (or use an unambiguous encoding), and test literal marker bytes beside both braces as well as ordinary escaped braces/interpolation.\n\nLocal standalone source: /Users/swoodtke/Projects/sawlang/.build/scratch/string_brace_provenance.saw. Compiled with `/Users/swoodtke/Projects/sawlang/.venv/bin/python sawc/sawc.py .build/scratch/string_brace_provenance.saw -o .build/scratch/string_brace_provenance`, then executed directly. No production files changed. Found while implementing isolated owned-String support in the compiler prototype; that prototype will preserve literal provenance rather than reproduce this behavior.\n","id":"SL-238","labels":["bug","compiler","strings"],"priority":"normal","project":"SL","revision":5,"sequence":570,"stage":"queued","status":"open","title":"String escaped-brace marker deletes literal U+0001 bytes before braces","updated":"1789137231"},"id":"SL-238","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sawlang\",\"body\":\"SL-238.p1 SUBMITTED (applied 6d5ef843), proposed, server gate queued — LEAD-VALIDATED. Design 268 implemented as ruled: the 0x01 marker protocol deleted from BOTH lexers; plain STRING tokens are fully decoded (a brace in the value is just a brace); INTERP_STRING tokens carry typed segments (frozen StringSegment dataclass on the Python Token; SegmentKind/StringSegment in a flat per-lex arena on the Saw side, seg_start/seg_count per token — the arena keeps Token in the automatic Copy tier, argued in place). Parser's byte re-scan deleted; interpolation sub-expressions now parse at the EXACT opening-brace position (design-99 columns exact under escapes; ZERO existing EXPECT files drift — the one in-literal column pin has no preceding escape — and two new error tests pin columns the old arithmetic got wrong). Consumer sweep found a SECOND LIVE FACE fixed by construction: string PATTERNS leaked the marker, so case \\\"a\\\\{b\\\" could never match its equal literal — pinned by string_pattern_escaped_brace_matches.saw. Lead independently compiled+ran the issue repro + the pattern face on the branch: 1 / 2 / 1 / pattern-matched, exit 0. Six new examples cover the obligation-4 matrix incl. a generated raw-0x01-byte file; selfhost tests assert the segment record shape. Gates (agent): suite 2459/14x, freestanding 36/36, terminal battery 25/25 (2936s) with lexdiff 0 mismatches over 3108 tracked files + selfhostlex 9/9. NOTE for design 259: the selfhost lexer's public API shape changed (lex returns LexResult carrying the segment arena; format_token takes the arena) — the parser work builds on that. Branch worktree-agent-a18fb0eaf6cfee2b8 tip fd374539; worktree stays until merge.\",\"id\":\"SL-238\",\"request_id\":\"pid-78106-1789137257-19000\"}","request_id":"pid-78106-1789137257-19000","sequence":571,"timestamp":"1789137257"}
---
