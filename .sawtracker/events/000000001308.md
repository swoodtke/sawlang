---
{"action":"comment","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl318u3c","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 8 in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188493","id":"c1"}],"created":"1790090064","description":"Found by the codex SL-318.p7 r1 #6 sweep (design 275 U3, family D), on the SYNC lowering — pre-existing, not U3's, and not fixed by the U3 branch (its driven twin is).\n\nA `move` argument evaluated BEFORE a failing propagating `try` in the same call leaks the moved value in a SYNC body: the argument is evaluated (the local's drop flag is cleared), the `try` fails, the call is never made, and the already-moved temporary is released by nothing — not by the local (its flag is clear) and not by the callee (never entered).\n\nRepro (sync; `Res` is NoCopy with a printing deinit):\n\n```saw\nstruct Res { name: String }\nextension Res: NoCopy { func deinit(&var self) { print(\"drop {}\", self.name) } }\nstruct Bad { n: Int }\nextension Bad: Error { func format(&self, into: &var StringBuilder) { try! into.append(\"bad\") } }\nfunc fail() sync -> Result<Int, Bad> { Bad(n: 1) }\nfunc sink_rev(r: Res, n: Int) sync -> Int { n }\nfunc work() -> Result<Int, Bad> {\n    var r = Res(name: \"kept\")\n    let n = sink_rev(move r, try fail())     // `move r` runs, `fail` fails, the call is abandoned\n    n\n}\nfunc main() { match work() { case Ok(n) -> print(n), case Err(e) -> print(\"caught\") } }\n```\n\nPrints `caught` and NO `drop kept` — one leak. The same in a driven body (after `yield_now()`, `&r` observed) drops once on the U3 branch: the transform's positional split leaves a pure `move` operand in place after the hoisted `try`, so the move is not performed on the failing path and `r` drops by its scope (`examples/coro_try_landing_relinquishes_by_position.saw`, rows 2, 3b, 5b). The BASE driven compiler also dropped once (by accident: the frame's claim stood).\n\nMechanism (obligation 4): codegen's try lowering — the error edge of a propagating `try` — releases nothing that the enclosing call expression already evaluated by value. Every position where a by-value temporary can precede a failing `try` in one expression is a sibling: an earlier argument (`sink_rev(move r, try f())`), a receiver (`(move r).method(try f())` where the receiver is consumed), an earlier tuple/array/struct-literal element (`(move r, try f())`), an earlier operand of a user operator, a `move` between two tries (`sink3(try ok(), move r, try fail())` — same leak, measured). Design 147/199 fix the evaluation order; the release of abandoned by-value temporaries on the error edge is what is missing. Suggested fix: the try lowering's error edge runs the cleanup of the temporaries the statement's own expression has materialized so far (codegen's cleanup stack scoped to the expression), or the same split the transform now does.\n\nMeasured on the sl318u3 worktree at ac862274 and on main (3bcd25d1): both leak identically in sync.\n","id":"SL-348","labels":["bug"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":1308,"stage":"backlog","status":"open","title":"sync: a by-value move argument evaluated before a failing propagating try in the same call is leaked","updated":"1790188493"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sl318u3c","closed":"","comments":[],"created":"1790090064","description":"Found by the codex SL-318.p7 r1 #6 sweep (design 275 U3, family D), on the SYNC lowering — pre-existing, not U3's, and not fixed by the U3 branch (its driven twin is).\n\nA `move` argument evaluated BEFORE a failing propagating `try` in the same call leaks the moved value in a SYNC body: the argument is evaluated (the local's drop flag is cleared), the `try` fails, the call is never made, and the already-moved temporary is released by nothing — not by the local (its flag is clear) and not by the callee (never entered).\n\nRepro (sync; `Res` is NoCopy with a printing deinit):\n\n```saw\nstruct Res { name: String }\nextension Res: NoCopy { func deinit(&var self) { print(\"drop {}\", self.name) } }\nstruct Bad { n: Int }\nextension Bad: Error { func format(&self, into: &var StringBuilder) { try! into.append(\"bad\") } }\nfunc fail() sync -> Result<Int, Bad> { Bad(n: 1) }\nfunc sink_rev(r: Res, n: Int) sync -> Int { n }\nfunc work() -> Result<Int, Bad> {\n    var r = Res(name: \"kept\")\n    let n = sink_rev(move r, try fail())     // `move r` runs, `fail` fails, the call is abandoned\n    n\n}\nfunc main() { match work() { case Ok(n) -> print(n), case Err(e) -> print(\"caught\") } }\n```\n\nPrints `caught` and NO `drop kept` — one leak. The same in a driven body (after `yield_now()`, `&r` observed) drops once on the U3 branch: the transform's positional split leaves a pure `move` operand in place after the hoisted `try`, so the move is not performed on the failing path and `r` drops by its scope (`examples/coro_try_landing_relinquishes_by_position.saw`, rows 2, 3b, 5b). The BASE driven compiler also dropped once (by accident: the frame's claim stood).\n\nMechanism (obligation 4): codegen's try lowering — the error edge of a propagating `try` — releases nothing that the enclosing call expression already evaluated by value. Every position where a by-value temporary can precede a failing `try` in one expression is a sibling: an earlier argument (`sink_rev(move r, try f())`), a receiver (`(move r).method(try f())` where the receiver is consumed), an earlier tuple/array/struct-literal element (`(move r, try f())`), an earlier operand of a user operator, a `move` between two tries (`sink3(try ok(), move r, try fail())` — same leak, measured). Design 147/199 fix the evaluation order; the release of abandoned by-value temporaries on the error edge is what is missing. Suggested fix: the try lowering's error edge runs the cleanup of the temporaries the statement's own expression has materialized so far (codegen's cleanup stack scoped to the expression), or the same split the transform now does.\n\nMeasured on the sl318u3 worktree at ac862274 and on main (3bcd25d1): both leak identically in sync.\n","id":"SL-348","labels":["bug"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1180,"stage":"backlog","status":"open","title":"sync: a by-value move argument evaluated before a failing propagating try in the same call is leaked","updated":"1790090064"},"id":"SL-348","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sawlang\",\"body\":\"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 8 in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.\",\"id\":\"SL-348\",\"request_id\":\"pid-69029-1790188493-15000\"}","request_id":"pid-69029-1790188493-15000","sequence":1308,"timestamp":"1790188493"}
---
