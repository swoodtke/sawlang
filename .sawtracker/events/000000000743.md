---
{"action":"create","actor":"agent:claude-sawtracker","after":{"assignee":"","author":"agent:claude-sawtracker","comments":[],"created":"1789251785","description":"Taking a move-only payload out of `Timed<T>` (SL-204) — e.g. `Timed<TcpStream>` from `accept(timeout:)` or `Timed<Data>` from `read(timeout:)` — requires `match move outcome { case Value(v) -> ..., case TimedOut -> ... }`. A plain `match outcome` is refused:\n\n    error: `self.outcome.value(…)` lends a place of type `Timed<TcpStream>`, which is move-only — reading it out as a value would alias storage the container still owns\n\nThe hint (`.value(…).method()` borrows through the window) targets a caller that wanted a borrow; a caller that wants the value needs `match move`, which is not mentioned. The asymmetry with `Result` — `match attempt { case Ok(v) -> ... }` needs no `move` on the scrutinee — makes this surprising, and sawlang's own suite has no example that moves a value out of a `Timed`. Suggest: extend the diagnostic hint to mention `match move`, and add a suite example. Found adopting design 272 in sawtracker (ST-25); analysis in SAW_NOTES ST-SAW-15. Against sawlang main 5755f60b.","id":"SL-279","labels":["usability"],"priority":"low","project":"SL","revision":1,"sequence":743,"stage":"backlog","status":"open","title":"Timed<T> ergonomics: moving the payload out requires match move; hint points at borrowing","updated":"1789251785"},"before":null,"id":"SL-279","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawtracker\",\"description\":\"Taking a move-only payload out of `Timed<T>` (SL-204) — e.g. `Timed<TcpStream>` from `accept(timeout:)` or `Timed<Data>` from `read(timeout:)` — requires `match move outcome { case Value(v) -> ..., case TimedOut -> ... }`. A plain `match outcome` is refused:\\n\\n    error: `self.outcome.value(…)` lends a place of type `Timed<TcpStream>`, which is move-only — reading it out as a value would alias storage the container still owns\\n\\nThe hint (`.value(…).method()` borrows through the window) targets a caller that wanted a borrow; a caller that wants the value needs `match move`, which is not mentioned. The asymmetry with `Result` — `match attempt { case Ok(v) -> ... }` needs no `move` on the scrutinee — makes this surprising, and sawlang's own suite has no example that moves a value out of a `Timed`. Suggest: extend the diagnostic hint to mention `match move`, and add a suite example. Found adopting design 272 in sawtracker (ST-25); analysis in SAW_NOTES ST-SAW-15. Against sawlang main 5755f60b.\",\"labels\":[\"usability\"],\"priority\":\"low\",\"project\":\"SL\",\"request_id\":\"pid-52246-1789251785-8000\",\"title\":\"Timed<T> ergonomics: moving the payload out requires match move; hint points at borrowing\"}","request_id":"pid-52246-1789251785-8000","sequence":743,"timestamp":"1789251785"}
---
