---
{"action":"create","actor":"agent:claude-sl204","after":{"assignee":"","author":"agent:claude-sl204","comments":[],"created":"1789217319","description":"Found while writing design 272 unit 2's tests (Sep 12 2026). PRE-EXISTING and unrelated to that branch — verified by reproducing at 7a4d01cb, the commit the branch started from, with the branch's own sawc/ checked out of the way.\n\nSYMPTOM: a `match` whose scrutinee is a SUSPENDING call, written INSIDE A LOOP, loses its arm's payload binding. The binding is undefined by the time codegen reads it, and the compiler exits with an internal error rather than a diagnostic:\n\n  error: internal compiler error at probe.saw:13:33 (Identifier): Undefined variable: e\n\nMINIMAL REPRO (compiles clean if the `while` is removed):\n\n  import std.net.{TcpStream}\n\n  func main() {\n      let (a, b) = TcpStream.pair()\n      let _ = move b\n      var i = 0\n      while i < 4 {\n          match a.write(\"payload\") {\n              case Ok(_) -> {},\n              case Err(e) -> {\n                  print(\"err {}\", e)\n                  break\n              }\n          }\n          i = i + 1\n      }\n      let _ = move a\n  }\n\nTHE MATRIX (obligation 4 — I probed the cells rather than assuming the shape I hit was the shape of the bug). Each row is a compile:\n\n  | scrutinee      | in a loop | use of the binding | result |\n  |----------------|-----------|--------------------|--------|\n  | suspending     | no        | direct in arm body | OK     |\n  | suspending     | no        | inside a nested if | OK     |\n  | suspending     | YES       | direct in arm body | ICE    |\n  | suspending     | YES       | inside a nested if | ICE    |\n  | NON-suspending | YES       | inside a nested if | OK     |\n\nSo the trigger is the PAIR (suspending scrutinee, enclosing loop). Nesting inside the arm is irrelevant — I first hit it through a nested `if` and that was a red herring. A non-suspending scrutinee in the same loop is fine, and the same suspending scrutinee outside a loop is fine.\n\nMECHANISM (named, not guessed at from the symptom): a suspending scrutinee is ANF-hoisted and the match is CFG-split by the coroutine transform, so the arm bodies become their own blocks reached from a resume state. The arm's payload binding is registered in the scope built for the straight-line path; the LOOP's back-edge re-entry rebuilds the block scope without it. This is the same family as design 218 unit 2's scope-end migration and design 247's scrutinee-temp migration — a binding whose scope is built once but whose block is entered from more than one predecessor.\n\nSIBLINGS THE MECHANISM REACHES, worth probing before a fix is written: `for` loops as well as `while`; a `while let` whose body holds such a match; a match arm binding read after a `continue` rather than a `break`; the same shape with the suspending call one level down (a suspending helper rather than a std method); and `if let` / `guard let` payload bindings in the same loop position, which are built by the same machinery.\n\nIMPACT: this is a plausible shape in ordinary server code — \"loop, do a suspending io call, match, report the error and break\" is the natural spelling of a retry or drain loop, and it is what I was writing when I hit it. The workaround is to hoist the call out of the match (`let r = a.write(...)` then `match r`), which costs nothing but is not discoverable from the error message. An internal compiler error is also the wrong failure mode regardless of the fix: whatever the outcome, this program should either compile or be refused with an anchored diagnostic (the fuzz oracle's rule).\n\nNOT FIXED on the design-272 branch, per the no-workarounds rule: I have filed it rather than coding around it, and design 272 unit 2's test that hit it is written in the hoisted form with a comment pointing here.\n","id":"SL-273","labels":["bug","compiler"],"priority":"high","project":"SL","revision":1,"sequence":678,"stage":"backlog","status":"open","title":"ICE: a match on a SUSPENDING scrutinee inside a loop loses its arm payload binding","updated":"1789217319"},"before":null,"id":"SL-273","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl204\",\"description\":\"Found while writing design 272 unit 2's tests (Sep 12 2026). PRE-EXISTING and unrelated to that branch — verified by reproducing at 7a4d01cb, the commit the branch started from, with the branch's own sawc/ checked out of the way.\\n\\nSYMPTOM: a `match` whose scrutinee is a SUSPENDING call, written INSIDE A LOOP, loses its arm's payload binding. The binding is undefined by the time codegen reads it, and the compiler exits with an internal error rather than a diagnostic:\\n\\n  error: internal compiler error at probe.saw:13:33 (Identifier): Undefined variable: e\\n\\nMINIMAL REPRO (compiles clean if the `while` is removed):\\n\\n  import std.net.{TcpStream}\\n\\n  func main() {\\n      let (a, b) = TcpStream.pair()\\n      let _ = move b\\n      var i = 0\\n      while i < 4 {\\n          match a.write(\\\"payload\\\") {\\n              case Ok(_) -> {},\\n              case Err(e) -> {\\n                  print(\\\"err {}\\\", e)\\n                  break\\n              }\\n          }\\n          i = i + 1\\n      }\\n      let _ = move a\\n  }\\n\\nTHE MATRIX (obligation 4 — I probed the cells rather than assuming the shape I hit was the shape of the bug). Each row is a compile:\\n\\n  | scrutinee      | in a loop | use of the binding | result |\\n  |----------------|-----------|--------------------|--------|\\n  | suspending     | no        | direct in arm body | OK     |\\n  | suspending     | no        | inside a nested if | OK     |\\n  | suspending     | YES       | direct in arm body | ICE    |\\n  | suspending     | YES       | inside a nested if | ICE    |\\n  | NON-suspending | YES       | inside a nested if | OK     |\\n\\nSo the trigger is the PAIR (suspending scrutinee, enclosing loop). Nesting inside the arm is irrelevant — I first hit it through a nested `if` and that was a red herring. A non-suspending scrutinee in the same loop is fine, and the same suspending scrutinee outside a loop is fine.\\n\\nMECHANISM (named, not guessed at from the symptom): a suspending scrutinee is ANF-hoisted and the match is CFG-split by the coroutine transform, so the arm bodies become their own blocks reached from a resume state. The arm's payload binding is registered in the scope built for the straight-line path; the LOOP's back-edge re-entry rebuilds the block scope without it. This is the same family as design 218 unit 2's scope-end migration and design 247's scrutinee-temp migration — a binding whose scope is built once but whose block is entered from more than one predecessor.\\n\\nSIBLINGS THE MECHANISM REACHES, worth probing before a fix is written: `for` loops as well as `while`; a `while let` whose body holds such a match; a match arm binding read after a `continue` rather than a `break`; the same shape with the suspending call one level down (a suspending helper rather than a std method); and `if let` / `guard let` payload bindings in the same loop position, which are built by the same machinery.\\n\\nIMPACT: this is a plausible shape in ordinary server code — \\\"loop, do a suspending io call, match, report the error and break\\\" is the natural spelling of a retry or drain loop, and it is what I was writing when I hit it. The workaround is to hoist the call out of the match (`let r = a.write(...)` then `match r`), which costs nothing but is not discoverable from the error message. An internal compiler error is also the wrong failure mode regardless of the fix: whatever the outcome, this program should either compile or be refused with an anchored diagnostic (the fuzz oracle's rule).\\n\\nNOT FIXED on the design-272 branch, per the no-workarounds rule: I have filed it rather than coding around it, and design 272 unit 2's test that hit it is written in the hoisted form with a comment pointing here.\\n\",\"labels\":[\"bug\",\"compiler\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"pid-73086-1789217319-4000\",\"stage\":\"backlog\",\"title\":\"ICE: a match on a SUSPENDING scrutinee inside a loop loses its arm payload binding\"}","request_id":"pid-73086-1789217319-4000","sequence":678,"timestamp":"1789217319"}
---
