---
{"action":"patch_revise","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl274","author":"agent:claude-sl204","comments":[{"author":"agent:claude-sawlang","body":"USER RULING (Sep 14): fix direction is options 3+1 COMBINED — every free function is emitted with a module-mangled symbol AND internal linkage unless @export'ed; @export remains the sanctioned way to pin a bare external name. Collisions become impossible by construction rather than diagnosed. Dispatching in the codegen-visibility batch (SL-269+SL-274+SL-275).","created":"1789433363","id":"c1"},{"author":"agent:claude-sl269","body":"## UNIT 3 STOPPED — the SL-280 interaction probe says this change WIDENS AN OPEN SOUNDNESS HOLE\n\nNot implemented. Per the dispatch mandate (\"if your change flips the public twin into the broken class, that is a HARD BLOCKER — stop unit 3, report, and do not land a change that widens an open soundness hole\"), I probed first, measured the flip, and reverted. My branch carries NO SL-274 change.\n\n### The probe, and the measurement\n\nRan on `worktree-agent-aa17b5b2067cc0075` rebased onto `ed4687c9`, so SL-280's just-merged XFAIL pin (`examples/coro_imported_private_helper_suspends.saw` + `examples/modules/sl280mod/lib.saw`) is in the tree. That pin is the sharp pair: two byte-identical entry points reaching one `yield_now()` through one more frame of an IMPORTED module, differing in a single word — `helper_public` is `public`, `helper_private` is not. `__saw_drive_steps` counts Pending resumes, so `1` means the suspension survived and `0` means it was silently dropped.\n\n    BASELINE (my branch, unmodified)          private 0    public 1\n    WITH UNIT-3 MANGLING APPLIED              private 0    public 0\n                                                           ^^^^^^^^\n\nThe probe change was the minimal stand-in for the ruling's option 1 — give every single-overload non-generic free function of a non-root module the design-249 `$m$<module>` tag, public as well as private, in `_stamp_module_private_functions` (`sawc/typechecker/registration.py:1379`). That is one line of the real unit 3 and it is the only part SL-280 is sensitive to.\n\nConfirmed at the IR, so this is not a step-counting artifact:\n\n    __Frame_helper_public occurrences in --emit-ir\n      baseline                    24      (the frame is built; the suspension survives)\n      with unit-3 mangling         0      (no frame at all; lowered as a plain call)\n\n### Why, exactly\n\nSL-280's own diagnosis names the mechanism and this change feeds it. `coro_transform.py:11425-11428` builds the imported-callee splice table keyed by the UNMANGLED `_f.name`, while the effect graph keys a free function by `(\"fn\", mangled_symbol or name)` (`typechecker/effects.py:431`). Today a `public` free function has `mangled_symbol=None`, so its edge key is the bare name, it matches the table, the body is spliced and a frame is built. Design 249 tags only MODULE-PRIVATE free functions, which is precisely why the private twin is the one that breaks today and the public twin works.\n\nUnit 3 gives the public twin a `$m$` tag too. Its edge key becomes `helper_public$m$sl280mod`, the table still holds `helper_public`, the test at `coro_transform.py:11583-11592` is False, the splice is declined — **and nothing is said**. So the change does not merely coexist with SL-280; it moves every imported PUBLIC free-function helper from the working side of that bug to the broken side.\n\n### What that would cost, if landed\n\nSL-280's runtime face is the expensive one and it is not hypothetical — it is the bug's own filing. When the dropped suspension is a `std.net` park rather than a `yield_now`, the park lowers through the out-of-frame fallback in `codegen/calls.py:575-597`, which registers the fd and then BLOCKS THE OS THREAD — the cooperative executor's thread — so the whole process stops until that fd is ready. Today that is reachable only through a module-private imported helper, which is why sawtracker's server hit it by factoring a read into a helper. After unit 3 it would be reachable through ANY imported free function called from an imported body: the ordinary shape of every library that splits a suspending operation across two functions, `public` or not. std is exempt only because its suspending surface is methods.\n\n### Re-sequencing\n\nUnit 3 should land AFTER SL-280's fix, not before. SL-280's investigator already measured that the minimal keying fix does not hold on its own (an `internal compiler error` from a duplicated LLVM symbol when the spliced clone and the imported module's own emission collide, plus a latent `self`-in-spliced-body bug at reference parameters), and proposed the real shape: ONE `_callee_frame_key(...)` funnel whose docstring names its entry points, with the effect-graph edge, `imported_free_fns`, the splice registration, `_classify_call` and `_callee_fb` all routed through it, plus the `structurally_susp_fns` phase ordering. That funnel is exactly what makes unit 3 safe, because it makes the answer independent of whether a free function carries a tag — which is the property unit 3 needs and today's code does not have.\n\nWorth noting for whoever sequences them: unit 3 is a good FORCING FUNCTION for that funnel. Once every free function is mangled, \"what key names this callee's frame?\" has one answer everywhere instead of two that happen to coincide for public functions, so the class of bug SL-280 represents stops being reachable rather than being fixed one site at a time.\n\n### What I did NOT do\n\nNo SL-274 change is on my branch — the probe was reverted and the tree verified back to `private 0 / public 1`. I did not attempt the keying fix (it is SL-280's, and its own investigator scoped it as a brief rather than a patch). The obligation-2 consumer sweep for SL-274 is posted separately so the later dispatch does not have to redo it.\n","created":"1789437546","id":"c2"},{"author":"agent:claude-sl269","body":"## OBLIGATION-2 CONSUMER SWEEP (owed before the fix; posted now so the later dispatch does not redo it)\n\n**Nothing in this repository relies on a Saw free function carrying a bare external link symbol.** Every C-facing and linker-facing site already goes through `@export`:\n\n* `sawc/rt/shim.c`'s ONLY forward declaration of a Saw symbol is `__saw_rt_dealloc` (`shim.c:204`, called at `:214`), which is `@export`ed at `rt/common/mem.saw:70`. No C in the tree calls a non-`@export`ed Saw free function.\n* All 18 freestanding cases `@export(\"fs_main\")` for `boot.S`'s `bl fs_main` (`grep -L` for that export over `tests/freestanding/cases/*.saw` returns nothing).\n* The C-ABI round-trips — `examples/export_roundtrip.saw`, `examples/never_extern_module_abi.saw` — are export round-trips, and codegen's extern/definition unification (`codegen/core.py:3545-3551`) is already guarded `if exported:`.\n* **No tool in `tools/` parses a symbol table at all**: `grep -rn 'nm \\|objdump\\|readelf\\|symtab' tools/*.py` is empty.\n\n**`--runtime-build` ALREADY implements exactly the ruled behaviour**, which is the strongest evidence the ruling is the right shape: `.build/rt/*/os_ops.o.ll` carries `define external @\"__saw_rt_tcp_listen\"` beside `define internal @\"htons\"` / `@\"ipv4_sockaddr\"` (20 of 43 defines internal), via `_apply_section_layout` (`codegen/core.py:2574-2583`) whose keep-set is `{@export'ed globals} ∪ {\"main\"}`. The freestanding profile does the same. So unit 3 is generalizing a funnel that exists and works, not inventing one.\n\nBlade and every package build compile ONE Saw unit per sawc invocation (`blade/src/builder.saw:514-534`), so no cross-object Saw symbol dependency exists anywhere; ~21 bare user free functions per blade binary would change spelling and nothing names them.\n\n**Battery lanes**, each opened rather than assumed: `abidoc` compares `RUNTIME_ABI_SYMBOLS` against `rt/ABI.md` text; `bttable` reads frame names, which `FrameBuilder` builds from `func.name` not the symbol (`coro_transform.py:2144`); `ircontract` matches `@\"__saw_rt_*\"`, all of which are export or extern names, never definition symbols; `stdtypes` asserts on TYPE mangling; `forgetgate`/`preludegate`/`gmgate`/`reemit`/`irdet`/`corodiff`/`docverify`/`bootstrap`/`freestanding` read no symbols. All UNAFFECTED. The 24 `EXPECT-IR-CONTAINS` directives name frame types and one METHOD symbol, no free function.\n\n**Scale**: ~7850 top-level free-function declarations across 3229 tracked `.saw` files (column-0 `func` heuristic, so an order of magnitude, not a parse). ~2600 are `main` and 148 are `@export`ed — both exempt — so roughly 5100 symbols change spelling and ~7500 gain internal linkage.\n\n## FOUR THINGS THE FIX MUST HANDLE — three small, one blocking\n\n1. **`main`.** It IS the C entry, emitted by name (`define i32 @\"main\"(i32, i8**)`), and the name is load-bearing at `codegen/core.py:3526`, `:3539`, `:3420`, `:2638`, `reachability.py:107`, `effects.py:1003` and `coro_transform.py:11701`. Exempt from BOTH the mangling and the internalization. (The codegen sites key on `func.name`, so a symbol-only rename is safe there — a `mangled_symbol` stamp is not.)\n2. **`codegen/calls.py:1509`** compares `sink.name == \"__saw_bt_panic\"` against the LLVM SYMBOL of a Saw free function (`std/taskgroup.saw:3118`). A mangled symbol silently falls through to the `\"rt\"` outlined-helper family, merging two families the docstring at `:1496-1502` says are deliberately separate. Compare the identity, not `sink.name`.\n3. **`@section` without `@export`** is externally visible hosted today (`nm` on `examples/section_compose.saw` gives `S _hot`) but is ALREADY local freestanding (`t handler`). The two profiles disagree now; the fix should make hosted match and say so at `LANGUAGE_SPEC.md:11583`, since the spec promises `@section` PLACEMENT only and reserves external linkage for `@export` (`:11535`).\n4. **THE BLOCKER — SL-280**, reached independently by this sweep and by the interaction probe in my previous comment. Full measurement is there; in one line, `effects.py:431` keys a free function `(\"fn\", mangled_symbol or name)` while `coro_transform.py:11110`/`:11428` key `funcs_by_name`/`imported_free_fns` by the SOURCE name, and giving every free function a `mangled_symbol` moves every free-function edge onto the mismatching side.\n\n## FIVE LATENT INSTANCES OF THIS BUG ARE ALREADY IN THE TREE\n\nThe runtime declares `extern \"C\"` `read`, `connect`, `rename`, `send`, `dup2`; tracked examples define top-level free functions of those names — `examples/conformance/V29_static_atomic_unaffected.saw:17` (`read`, plus four more `read`s elsewhere), `examples/labeled_args_mid_default_skip.saw:12` (`connect`), `examples/df140c_qualified_type_position.saw:28` (`rename`), `examples/errors/unsafe_send_not_erasable.saw:7` (`send`), `examples/closure_satisfies_copy_bound.saw:10` (`dup2`).\n\nNot theoretical: `nm` on a stock build of V29 shows `T _read` with `_read` NOT undefined — the linker bound `os_ops.o`'s call to the Saw function. An A/B probe differing only in the function's NAME:\n\n    func read(a: Int) -> Int { 4242 }      ->  File.read(size: 16) reports 4242 bytes\n    func read_renamed(a: Int) -> Int {...} ->  File.read(size: 16) reports 16 bytes\n\nA `Data` claiming 4242 bytes out of a 16-byte buffer, silently, exit 0 — in a file under `examples/conformance/`. These are ready-made regression material for the fix, and they are a second reason not to leave SL-274 open indefinitely: the corpus is already carrying the bug.\n\n## NOT COVERED\n\nThe **sawos repo (`../sawos`) was not swept** and is the one place a bare-Saw-free-function-from-C dependency could still exist — its kernel and root server link Saw objects against HAL C. sawlang references sawos nowhere, so nothing measured here says anything about it. Recommend the SL-274 brief include a grep there for `extern \"C\"`-declared names that a Saw free function defines without `@export`. Also unexecuted: `tools/lldb_saw.py` (its three symbol lookups are compiler-emitted GLOBALS, not free functions, so a free-function-only rule cannot reach them — but a wider internalization could); and no size/IR deltas from the mangling were measured.\n","created":"1789441057","id":"c3"},{"author":"agent:claude-sawlang","body":"RE-SEQUENCED (lead, Sep 14): the batch agent's measured probe shows the ruled fix (module-mangle + internal linkage for all free functions) FLIPS SL-280's public twin from working to silently-dropped — the coro splice table is keyed by unmangled name, so expanding mangling widens the open soundness hole from module-private helpers to every imported free-function helper. Correctly stopped, nothing landed. SL-274 now dispatches AFTER the SL-280 funnel fix (_callee_frame_key), and its fix brief should require the SL-280 pin pair plus a public-helper suspension row in its gate. The consumer sweep is already posted above (main x7, calls.py:1509 sink.name, @section-without-@export, five latent live instances incl. the V29 func read shadowing File.read); the user ruling on direction stands.","created":"1789441265","id":"c4"},{"author":"agent:claude-sawlang","body":"DISPATCHED (lead, Sep 17): SL-280's funnel landed (c19bf57a), so the flip this unit's mangling caused is closed — coro_module_tagged_free_callee_discovery.saw is the acceptance row (imported_public 1 must hold) and coro_imported_private_helper_suspends.saw must stay public 1 / private 1. Scope per the ruling (c1) + the sweep (c3): every free function module-mangled + internal linkage unless @export'ed; main exempt from both (also fixes SL-305); calls.py:1509 compares identity not sink.name; @section-without-@export goes local hosted to match freestanding, spec line updated; the five latent corpus instances (V29 func read etc.) become regression rows. Worktree sl274, fresh Opus agent.","created":"1789693214","id":"c5"},{"author":"agent:claude-sl274","body":"IMPLEMENTED on branch `sl274`, tip `a690608d` (rebased onto main `65eb3e96`; two commits). Terminal battery 27/27 GREEN. Not submitted — that is the lead's.\n\n## The tag scheme chosen, and what it retires\n\nONE rule, `<name>$m$<module tag>`, on EVERY free function, with `module_tag(())` being `root` so the entry module's own functions carry one. `$m$` is DF-140f's delimiter, shared with design 144's type identities; on a free function it now means \"the module that DEFINES it\" rather than \"private\", because every free function carries one. An overload member appends its design-55 suffix to that base (`read$m$root$OL$Int`) and a generic's instantiations append their type arguments (`read$m$root$1$Int`), so both classes are module-tagged too.\n\n**Design 249's `$M$` census is RETIRED outside std.** A per-module tag disambiguates two modules declaring one name by construction, so there is no whole-compilation census to take and no dependence on which module is checked first — `free_function_owner_census`, `_census_module_functions` and the `free_function_owners` plumbing they fed are deleted, and so is `_stamp_module_private_functions`, whose whole job the funnel now does at registration. std keeps a narrower per-FILE census (`std_free_function_owner_census`), for the reason below.\n\n`typechecker/registration.py::_free_function_symbol_base` is the funnel; its docstring names its entry points (`_register_function`, plus `_stamp_overload_symbols` building on `symbol_base`). FOUR declarations keep the plain name, each because something reads the WRITTEN name as the symbol:\n\n* the **ENTRY module's `main`** — the C entry, load-bearing at `codegen/core.py:3526`/`:3539`/`:3420`/`:2638`, `reachability.py:107`, `effects.py:1013` and the transform's seeds. A DEPENDENCY's `main` is ordinary, which is SL-305.\n* an **`@export`ed** function — the sanctioned bare external name; codegen emits it under the requested C symbol whatever the funnel says, so leaving the plain name keeps one identity per seam.\n* a **compiler-SYNTHESIZED** function (`is_synthesized`) or a **MONO INSTANCE** (`is_mono_instance`) — named by string where it is built and at every reference, exactly as design 204 records for a synthesized TYPE. The instance case is not cosmetic: an instance's name already carries the template's tag (`run$m$root$1$Fast`), and tagging it again detached the clone's effect node from the root the drive site had recorded. This is where 12 of the suite's 13 first-pass failures came from.\n* a **STDLIB** declaration (`_checking_builtins`), which keeps design 249's rule unchanged. Not a convenience: codegen EMITS calls to `__saw_print_int`, `__saw_fmt_int`, `__saw_main_exit_code`, `__saw_bt_panic`, `__saw_string_*`, `__saw_atomic_*` and a dozen more by literal string, so std's free functions are the compiler/stdlib seam and are named the same way a synthesized function is. The linkage half internalizes every one of them anyway, and a USER declaration can never collide with one because the user's side is always tagged.\n\n## Linkage — one funnel, three entries\n\n`codegen/core.py::_apply_section_layout` is now THE place a definition's linkage is decided and its docstring names all three callers. The HOSTED profile internalizes on exactly the terms `--freestanding` and `--runtime-build` already did, keep-set `{@export'ed globals} ∪ {\"main\"}`, gated on `_strip_unreachable` (sawc's `whole_program`) so a plain hosted `-c` object — somebody else's to link — keeps external linkage. Sections stay design-168's ELF-only business.\n\n`@section` without `@export` falls out with no extra rule: it is not in the keep-set. `examples/section_compose.saw` at -O0 goes from `S _hot` to `s _hot$m$root`, while its `@export(\"boot_double\")` sibling stays `S`. LANGUAGE_SPEC's `@section` paragraph now says placement only, and a new \"Link symbols and linkage\" section states the rule.\n\n## nm evidence, before and after\n\nThe A/B is `examples/free_function_never_shadows_a_runtime_symbol.saw`, the c3 probe as a committed test. Built with main's compiler (`25dc9a17`):\n\n    bytes 4242        <- a Data claiming 4242 bytes out of a 16-byte buffer\n    helper 4242\n    exit 0\n    0000000100000730 T _read\n\nBuilt on this branch:\n\n    bytes 16\n    helper 4242\n    exit 0\n    (no `_read` of any kind; `read$m$root` is internal and inlined away)\n\nOver six binaries (`hello`, the new test, `V29_static_atomic_unaffected`, `labeled_args_mid_default_skip`, `closure_satisfies_copy_bound`, `section_compose`), the ONLY external symbols left are `_main`, `__mh_execute_header`, the `___saw_*` reserved namespace (the `@export`ed `__saw_rt_*` seams from the linked runtime objects, shim.c's C helpers, and design 158's two `@export`ed statics), and — in `section_compose` — its own `@export`ed `_boot_double`. Local symbol counts rose correspondingly (V29: 10 local -> 16). `--runtime-build` was rebuilt from an empty `.build/rt/`: seams `define external @\"__saw_rt_tcp_listen\"` with their exact ABI names, helpers `define internal @\"htons$m$root\"` / `@\"ipv4_sockaddr$m$root\"`.\n\n## The SL-280 rows — the measurement the re-sequencing asked for\n\nAll three hold, and the two new ones from the same fixture family with them:\n\n| row | value |\n|---|---|\n| `coro_module_tagged_free_callee_discovery.saw` | entry_tagged 1, **imported_public 1** |\n| `coro_imported_private_helper_suspends.saw` | **public 1 / private 1** |\n| `coro_imported_callee_discovery_matrix.saw` | all 1s (E1/E2, S0-S9) |\n| `coro_imported_private_helper_parks_on_io.saw` | pass (the io-park wedge cell, IR property pinned) |\n| `coro_spliced_helper_keeps_its_own_module.saw` | pass |\n\n## TWO NEW KEYING SITES the universal tag exposed, both routed and both added to `frame_keys.py`'s entry list\n\nThe funnel made the answer tag-INDEPENDENT, which is what unblocked this unit — but two sites read a written NAME rather than asking it, and a public function's name and key coinciding was what had hidden them:\n\n1. `coro_transform.py::_promote_nested_generic_calls` spelled a nested generic call's instantiation `mangle_function(fc.name, args)` while `monomorphize._function_template_name` spells it from the template's SYMBOL. With every free function tagged the two disagree, the promotion silently declined, and the author got ``cannot suspend in `sync func` method: `__Frame_s6_entry.resume` calls `s6_helper$1$Int` `` — two frames they never wrote. It asks `callee_frame_key(fc)` now.\n2. the same function left the TEMPLATE's `resolved_symbol` on a call it had just rewritten to name the INSTANCE, so `callee_frame_key` would have answered the base. Cleared, on the drive-site rewrite's own precedent (`expressions.py`'s `inner.resolved_symbol = None`).\n\n## calls.py:1509\n\n`_panic_helper` keyed its outlined assembly routine on `sink.name == \"__saw_bt_panic\"` — the emitted LLVM symbol of a Saw free function. It asks `_panic_sink_is_bt()` now, which is the same identity question `_panic_sink` asks (`self.functions.get(BT_PANIC_SINK) is not None`), so the two cannot disagree. std's symbols do not move under this change, so the bug was latent rather than live; the two families the docstring says are deliberately separate can no longer merge on a rename.\n\n## The five latent corpus instances\n\nAll five compile and run unchanged, and the shadowing is gone: `V29_static_atomic_unaffected.saw` (`read`, plus four more elsewhere), `labeled_args_mid_default_skip.saw` (`connect`), `df140c_qualified_type_position.saw` (`rename`), `errors/unsafe_send_not_erasable.saw` (`send`), `closure_satisfies_copy_bound.saw` (`dup2`). `nm` on V29 no longer has `T _read` at all.\n\n## Two things the sweep did not predict\n\n**D07_never_method_and_generic** (conformance) pinned `define void @Killer_die` in the OPTIMIZED IR sidecar, and that directive was only ever observable BECAUSE of external linkage: every call to those four `-> Never` declarations is on a path the fixture's constant arguments prove dead, so with internal linkage `globaldce` deletes all four and the sidecar holds nothing but `main`. That deletion is the point of internal linkage, and what the row is about is the `void` + `noreturn` a declaration is emitted WITH — so the directive moved to `-O0`, where a declaration is still a declaration, and now pins all three method slots (`define internal void @Killer_die`, `@Killer_halt`, `@\"Killer_slay$1$Int\"`).\n\n**The `ircontract` lane** (second commit, `a690608d`) read each `-O` flag's size attributes as a SHARE over the module's attributed definitions. Its own comment records why a share and not a substring: LLVM stamps `cold minsize noinline noreturn` on the cold noreturn paths it forms at every level, and design 261's outlined panic routines are emitted `cold minsize noinline noreturn` by `_panic_helper` on purpose. As a fraction they were harmless while the fixture's hosted module carried ~90 definitions; internalization let globaldce cut it to FIVE attributed definitions, three of them those routines, so -O1 and -O2 read 60% `minsize` and the lane reported a level-table leak that is not there. A `noreturn` define is now excluded from the share, numerator and denominator alike — the share is over the definitions a level actually owns.\n\nThe other lanes the sweep flagged as at-risk came through untouched: `abidoc`, `bttable`, `stdtypes`, `reemit`, `irdet --all`, `gmgate`, `bootstrap`, `freestanding`.\n\n## Docs\n\nLANGUAGE_SPEC gains **Link symbols and linkage** (the rule, the three consequences, and the `@section` amendment); README's C-ABI-exports bullet says `@export` is the only way a declaration reaches the linker; the saw-lang skill gains the rule plus the two things that bite (the libc capture, and `@section` without `@export`). Conformance rows **K105** (the guarantee) and **K106** (SL-305's), with the header count recounted to 527.\n\n## Gate\n\nPer-commit, at each commit: suite **2544 passed / 10 xfailed**; freestanding **36/36** (riscv32 19 + arm64 17). Terminal battery at `a690608d`: **27/27 stages GREEN** in 3379s, including `irdet --all`, `reemit`, `gmgate`, `bootstrap`, `freestanding`, `fuzz`, `corodiff`, `citations`, `astgraft`.\n","created":"1789703794","id":"c6"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-274.p1 (revision 1): SL-274: every free function module-mangled + internal unless @export'ed (one symbol funnel, one linkage funnel); fixes SL-305; ircontract share excludes noreturn defines","created":"1789710702","id":"c7"},{"author":"agent:claude-sawlang","body":"SUBMITTED (lead, Sep 18) as SL-274.p1, branch sl274 tip rebased on main 0360c080, two commits squashed. Lead validation on the rebased tip: the three SL-280 rows (tagged-callee imported_public 1; pin pair public 1 / private 1; matrix all 1s), the new free_function_never_shadows_a_runtime_symbol + dependency_named_main_keeps_the_entry_point examples, D07, section_compose and export_roundtrip all pass; nm on a built V29 binary shows _main as the ONLY external T symbol and no bare _read. Agent gates at tip: suite 2544/10x, freestanding 36/36, terminal battery 27/27. Note for review: the std carve-out (std free functions keep written names because codegen names ~15 of them by literal string) is deliberate and documented in _free_function_symbol_base; they are still internalized. SL-305 closes with this merge. SL-306's fix (in flight, coro_transform.py _suspending_method_target) is independent; whichever merges second rebases.","created":"1789711182","id":"c8"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-274.p1 (revision 1)","created":"1789711183","id":"c9"},{"author":"agent:codex","body":"Requested changes on patch SL-274.p1 (revision 1)\n\nTwo blocking findings in revision 1:\n\n[P1] Preserve module-qualified Saw identities for exported functions.\n\nThe is_exported(func) exemption at sawc/typechecker/registration.py:1419 returns an empty symbol base. Same-named functions in different modules consequently share an internal lookup key even when they export distinct C names. The reproducer below previously compiles and prints 11 then 22; SL-274.p1 fails to link with Undefined symbols: _first_value. Keep the module-qualified Saw identity separate from the exported C symbol (codegen already has distinct lookup-key and C-symbol concepts).\n\n```saw\nmodule first {\n    @export(\"first_value\")\n    public func value() -> Int { 11 }\n}\n\nmodule second {\n    @export(\"second_value\")\n    public func value() -> Int { 22 }\n}\n\nfunc main() {\n    print(first.value())\n    print(second.value())\n}\n```\n\n[P2] Distinguish the entry module from a module named root.\n\nThe unconditional tag at sawc/typechecker/registration.py:1423 uses module_tag, which maps both () and (\"root\",) to root. The two private helper functions below therefore both become helper$m$root. Before this patch the program compiles and prints 2 then 101; with the patch it fails with internal compiler error: helper$m$root. Use a module encoding that distinguishes these identities and cover this case with a regression test.\n\n```saw\nmodule root {\n    func helper(n: Int) -> Int { n + 100 }\n    public func reach(n: Int) -> Int { helper(n) }\n}\n\nfunc helper(n: Int) -> Int { n + 1 }\n\nfunc main() {\n    print(helper(1))\n    print(root.reach(1))\n}\n```\n\nA/B evidence: the unpatched compiler checkout was 0ec310c5; git diff --name-only 0360c080 0ec310c5 -- sawc is empty, confirming its compiler sources match this patch's base. Both probes pass on that compiler and fail with the reviewed patch applied to 0360c080.\n\nIndependent validation: all 14 focused tests passed, including libc shadowing, dependency main, export_roundtrip, section_compose, D07, nested generics, and SL-280 regressions. The two additional probes above block approval.\n","created":"1789737746","id":"c10"},{"author":"agent:claude-sawlang","body":"ACK codex review c10 (lead, Sep 18): both findings confirmed against the diff. P1 — the @export exemption returned an EMPTY symbol base, collapsing same-named exported functions of different modules onto one lookup key (the Saw identity and the emitted C symbol must stay distinct; codegen already has both concepts). P2 — module_tag maps both the entry module () and a user module named root to 'root', so helper$m$root is emitted twice; the entry module needs an encoding no user module path can spell, applied consistently with design 144's type-identity tags. SL-274 agent RESUMED in the same worktree: both fixes through the funnel, both repros + a combined one as examples on the conformance row, a re-audit of every exemption branch for a same-name-across-modules collapse, per-commit gate + battery; r2 follows.","created":"1789737800","id":"c11"},{"author":"agent:claude-sl274","body":"BOTH REVIEW FINDINGS FIXED on branch `sl274`, tip `b52f6651` (rebased onto main `3043d813`; three commits — the two from r1 plus one for the review). Terminal battery 27/27 GREEN at the tip. Not submitted; the lead revises to r2.\n\nCodex was right on both counts, and both were mine.\n\n## [P1] An `@export`ed function is tagged like any other\n\n**Mechanism.** The exemption reached the wrong one of `@export`'s two jobs. `@export` decides the EMITTED C symbol and the linkage; it decides nothing about the name the compiler knows a function by — and `codegen/core.py::_declare_function` has always kept the two apart, `func_name` keying `self.functions` while `llvm_name` is `export_symbol(func)` when there is one. Returning an empty symbol base therefore bought nothing and cost the key: both `value`s shared the key `value`, the second overwrote the first, the first's body was never emitted, and its `@llvm.used` keep-root named a declaration with no definition.\n\n**Fix** — `sawc/typechecker/registration.py:1415` (the `is_exported(func)` clause is gone; the exemption list is now `is_synthesized` / `is_mono_instance` only, plus the entry module's `main`). The funnel docstring records what the exemption got wrong, so the next reader does not re-derive it.\n\n**The consumer sweep the lead asked for**, every reader of `is_exported`/`export_symbol` opened rather than assumed:\n\n* `_exported_llvm_globals` (the `_apply_section_layout` keep-set and `_emit_llvm_used`) holds llvm.Function OBJECTS and reads `g.name` — the emitted symbol. Unaffected.\n* `codegen/reachability.py` roots by llvm name, and `_defer_body` keys on `llvm_func.name` by documented design. Unaffected.\n* design 58's extern/definition unification looks up `llvm_name` (`self.module.get_global`). Unaffected.\n* the duplicate-`@export` table (`_register_export_symbol`) and design 113b's seam check key on the export name from the ATTRIBUTE. Unaffected — and the refusal still fires: two modules exporting one C name give ``duplicate `@export` symbol `shared_value` (already exported by `value` at line 4)``.\n* `effects.py::_effect_enter_function` and `expressions.py::_funcpointer_decl_is_sync` read the declaration NODE, not a name. Unaffected.\n* `funcpointer_symbol` was already `sym.mangled_name or name`, so form-2 `FuncPointer` construction follows the tag for free.\n\n**Measured, not reasoned:** `export_roundtrip` (15), `never_extern_module_abi` (its intended panic), `section_compose` (84/43), `export_static_section` (30/12345) all run; a freestanding case compiled by hand still emits `T fs_main`; `--runtime-build` rebuilt from an EMPTY `.build/rt/` keeps `define external @\"__saw_rt_alloc\"`, `@\"__saw_rt_tcp_listen\"` and the rest beside 20 internal helpers.\n\n## [P2] The entry module is the EMPTY tag\n\n**Mechanism.** `type_identity.module_tag` rendered the entry module's empty path as the word `root`, which is exactly what a user module named `root` renders as — the rendering was not injective, and two declarations took one tag.\n\n**Encoding chosen: the EMPTY tag.** A free function of the entry module is `helper$m$`; a module named `root` is `helper$m$root`. Every real module path renders at least one character, so the empty string is the one tag no module can ever take — no reserved word to defend, and nothing to re-decide if module paths grow. `$` is forbidden in a Saw identifier, so a trailing delimiter cannot collide with a name an author could write. Two riders: `ErrorReporter._QUALIFIER_RE` widens from `+` to `*` so a bare `$m$` is scrubbed out of a diagnostic like any other qualifier (checked — `helper$m$` -> `helper`, `run$m$$1$Fast` -> `run$1$Fast`, `read$m$$OL$Int` -> `read$OL$Int`), and `module_tag` now carries the rule in its own docstring.\n\n**Fix** — `sawc/type_identity.py:129` (`module_tag`) and `sawc/errors.py:149`.\n\n**Why `module_tag` and not the free-function funnel**, which is the part worth keeping: that is where every `$m$` tag in the compiler is spelled, and the free-function tags and design 144's type identities have to agree about which module is which. It also closes the SAME collision at design 142's cross-module extension-method tie-break (`_stamp_overload_symbols`' `$M$<tag>`), **where it predates SL-274**:\n\n    internal compiler error: Marker$m$sl274marker_describe$OL$$M$root\n\non main today, for an entry-module extension beside a module-`root` extension of one type with one signature. Nothing else reached it — design 144 exempts the entry module's types outright (`qualifies` is False for an empty path) and `_module_private_symbol` returns None for one — which is why `module_tag(())` had exactly one caller and the collision sat unprobed. Pinned as a regression test rather than filed, since this patch fixes it.\n\n## The re-audit (lead's item 2), probed not reasoned\n\nEvery remaining exemption branch, with a two-module program per branch (`.build/scratch/audit_*`), run on THIS branch and on main:\n\n| branch | probe | branch | main |\n|---|---|---|---|\n| `is_synthesized` | two modules' same-named SUSPENDING helper, both driven — the drivers are minted from frame keys, which are tagged | entry 2 / dep 101 | entry 2 / dep 101 |\n| `is_mono_instance` | two modules' same-named GENERIC, both instantiated at `Int` | entry 2 / dep 101 | entry 2 / dep 101 |\n| overload sets | two modules' same-named set with identical signatures | entry 2 / entry-str 2 / dep 101 | same |\n| entry `main` | SL-305's row — and there can only ever be one exempt `main` | pass | link failure |\n| std (`_checking_builtins`) | design 249's per-FILE census, unchanged by this patch | — | — |\n\nSo neither remaining exemption admits a collapse, and none of the three probes is a new hole. One thing the audit did NOT close, recorded rather than fixed: a user module path `std.data` renders `std_data`, the same as std's own file-module, so design 144 TYPE identities could collide there. It predates SL-274 by a long way, it is unreachable for free functions (std's keep their written names, the user's are tagged), and it wants its own issue rather than a rider on this one.\n\n## Regression tests — four, each failing on the compiler it pins\n\n| test | main | r1 | r2 |\n|---|---|---|---|\n| `two_modules_export_distinct_c_symbols.saw` | 11 / 22 | `Undefined symbols: _first_value` | 11 / 22 |\n| `module_named_root_is_not_the_entry_module.saw` | 2 / 101 | `internal compiler error: helper$m$root` | 2 / 101 |\n| `exported_function_in_a_module_named_root.saw` (the combined cell) | `Undefined symbols: _root_value` | fails | saw 11 22 / c 11 22 |\n| `extension_method_tie_break_distinguishes_the_entry_module.saw` + `modules/sl274marker/` | `internal compiler error: Marker$m$sl274marker_describe$OL$$M$root` | same | entry 2 |\n\nThe first two check BOTH directions where it matters: the Saw calls, which route through the module-qualified identity, and `extern \"C\"` calls back in, which route through the export names.\n\nConformance rows **K107** (the export identity/symbol separation) and **K108** (a module named `root` is not the entry module, with the extension-method site as its second file and the reason the fix belongs in `module_tag`), K105 amended to point at both, header recounted to 529.\n\n## Docs\n\nLANGUAGE_SPEC's \"Link symbols and linkage\" now states the empty tag, why it is the one unspellable rendering, and that an `@export`ed function is tagged like any other (with the two-module export example). The saw-lang skill line matches. `examples/free_function_never_shadows_a_runtime_symbol.saw` and the SL-280 `sl280tag` fixture's comments no longer claim the `$M$root` tag the retired census used to give them.\n\n## Gate\n\nPer-commit at the tip: suite **2548 passed / 10 xfailed** (up 4 — the new tests); freestanding **36/36** (riscv32 19 + arm64 17). Terminal battery after the rebase: **27/27 stages GREEN** in 3461s, including `irdet --all`, `reemit`, `gmgate`, `bootstrap`, `freestanding`, `fuzz`, `corodiff`, `citations`, `astgraft`, `ircontract`.\n","created":"1789746200","id":"c12"},{"author":"agent:claude-sl274","body":"REBASED onto main after SL-306 landed underneath. Tip `ef71bdd5` (four commits on `b7fb8fa0`), terminal battery **27/27 GREEN** at that tip. Ready for r2; not submitted.\n\n## Conflicts resolved — `examples/conformance/INDEX.md` only\n\n`sawc/coro_transform.py`, `sawc/sawc.py` and `sawc/typechecker/core.py` all AUTO-MERGED, both commits, despite SL-306 editing the same functions. INDEX.md conflicted twice (once per commit of mine that touches it), and the substance of the conflict was a ROW-NUMBER COLLISION rather than text: **SL-306 took K105**, which both of my commits also used.\n\n* **the header count**, both commits. Resolved by keeping BOTH histories in the chain rather than either side's line: `530 rows: 278 here, 252 elsewhere` for my four rows, then SL-306's `526 / 278 / 248` line, then the older chain untouched.\n* **the row block after K104**, both commits. Resolved by keeping SL-306's K105 verbatim and RENUMBERING mine: K105->K106 (libc shadowing), K106->K107 (dependency `main`), K107->K108 (the export identity), K108->K109 (a module named `root`). Their internal cross-references were renumbered with them — K107's \"fixed by K105's mangling rule\" -> K106's, K108's \"K105's first draft\" -> K106's, K108's \"meets K108\" -> K109, and K106's tail now points at K108/K109.\n\nChecked afterwards, per the hunk-by-hunk rule: SL-306's ELEVEN covering files are all still named and so are my six (11/11 and 6/6 by probe), the header adds up (278 + 252 = 530), and no row id is duplicated by the resolution. One duplicate id exists in the table — `K53` twice — and it is PRE-EXISTING on origin/main (two occurrences there as well), so it is not mine to fix in this patch; flagging it rather than touching it.\n\n## The two keying sites (lead's item 2)\n\nBoth survived and compose with SL-306's change to the very same function, `_promote_nested_generic_calls`:\n\n    sawc/coro_transform.py:10830   mangled = mangle_function(callee_frame_key(fc) or fc.name, args)\n    sawc/coro_transform.py:10831   if not instantiation_suspends(mangled):     # SL-306's funnel\n    sawc/coro_transform.py:10851   fc.resolved_symbol = None\n\nThe two changes are orthogonal and both are needed: mine SPELLS the instance (`callee_frame_key`, so the walk names the instance the registry built), SL-306's decides WHETHER it suspends (`suspends_ignoring_closure_calls`, so a conservative-only instance is not promoted). `_classify_call`'s own `callee_frame_key(fc)` (`:5192`) is untouched, and `frame_keys.py` still lists `_promote_nested_generic_calls` among its 16 named entry points.\n\nVerified by running the two units' rows together: `two_modules_export_distinct_c_symbols`, `module_named_root_is_not_the_entry_module`, `exported_function_in_a_module_named_root`, `extension_method_tie_break_distinguishes_the_entry_module`, `free_function_never_shadows_a_runtime_symbol`, `dependency_named_main_keeps_the_entry_point`, `coro_module_tagged_free_callee_discovery`, `coro_imported_private_helper_suspends`, `coro_imported_callee_discovery_matrix`, `coro_conservative_generic_instance_not_promoted`, `coro_conservative_callee_not_embedded`, `coro_json_encode_in_task` — 12/12.\n\n## One cross-unit interaction the suite caught (fourth commit, `ef71bdd5`)\n\nSL-306's `coro_conservative_generic_instance_in_spawned_task.saw` pins a generic instantiation's frame BY SYMBOL: `EXPECT-IR-CONTAINS: __Frame_yielder$1$Int`. SL-274 tags every free function, so the entry module's template base is `yielder$m$` and the instance — and the frame named for it — is `__Frame_yielder$m$$1$Int`. Only the spelling moves: the row's claim is unchanged and still checked in full (the conservative-only `apply<Int>` stays unframed, and `EXPECT-IR-ABSENT: __Frame_apply` needed nothing because a substring test matches the tagged spelling too; the INSTANCE rather than the template is framed; `__Frame_worker` is present; and the interleaved output is what says the worker really parked inside it).\n\nSwept for siblings: the corpus has exactly TWO IR directives naming a generic instance. The other is D07's `Killer_slay$1$Int`, a METHOD-generic, which carries no module tag because the entry module's TYPES are not qualified (design 144) and which passes unchanged.\n\n## Gate at `ef71bdd5`\n\nPer-commit: suite **2559 passed / 10 xfailed**; freestanding **36/36** (riscv32 19 + arm64 17). Terminal battery: **27/27 stages GREEN** in 3100s — `suite`, `icebreadcrumb`, `lexdiff`, `astdiff`, `astgraft`, `transferdecisions`, `producertaxonomy`, `citations`, `forgetgate`, `ircontract`, `preludegate`, `docverify`, `stdtypes`, `toolchain`, `floatvectors`, `abidoc`, `bttable`, `fuzz`, `corodiff`, `bench`, `selfhostlex`, `minivm`, `reemit`, `irdet --all`, `gmgate`, `bootstrap`, `freestanding`.\n\nRe-verified at the tip after the rebase: both review repros (`11 / 22` and `2 / 101`) and the original guarantee (`bytes 16`, and `_read` is `U`, resolved from libc, not `T`).\n","created":"1789751184","id":"c13"},{"author":"agent:claude-sawlang","body":"Revised patch SL-274.p1 to revision 2","created":"1789755050","id":"c14"}],"created":"1789218312","description":"Found while building design 272 unit 3 (Sep 12 2026). PRE-EXISTING and language-wide; unit 3 renamed its way out rather than coding around it.\n\nSYMPTOM: a Saw FREE FUNCTION is emitted with its BARE name as the link symbol, so a free function whose name matches a libc symbol SILENTLY SHADOWS it for the whole program — including for C code inside the Saw runtime itself. There is no diagnostic at any stage.\n\nHOW IT PRESENTED. std.signal grew a public free function `raise(which: Signal)` (POSIX's own name for \"send a signal to this process\"), whose body calls the shim seam `__saw_signal_raise`, whose body calls C `raise(3)`. The program segfaulted. `nm` on the binary:\n\n  0000000100005164 T ___saw_signal_raise\n  00000001000006fc T _raise            <- the SAW function, not libc's\n\nso the shim's `raise((int)signo)` re-entered the Saw function, which called the shim, which called the Saw function: unbounded recursion, stack exhaustion, SIGSEGV. The reported failure is a segfault with no stack and nothing pointing at the name.\n\nTHE MECHANISM, probed rather than inferred. A user program at -O0 with two top-level free functions:\n\n  func strlen(n: Int) -> Int { n + 1 }\n  func getpid(n: Int) -> Int { n + 2 }\n\nemits\n\n  0000000100002080 T _strlen$OL$Int\n  00000001000020cc T _getpid           <- UNMANGLED\n\nSo the rule is: a free function is emitted under its bare name UNLESS it participates in an overload set, in which case it picks up the `$OL$` suffix and is safe by accident. `getpid` is unmangled and would shadow libc's `getpid` for the entire process. METHODS are unaffected — they mangle as `Type_method` (`_IoError_format`, `_StringBuilder_append$OL$String`), which is why this has not been hit before: almost everything public in std is a method.\n\nWHY IT IS A CLASS, not this one name. Every libc symbol is exposed: a user writing `func open(...)`, `func read(...)`, `func write(...)`, `func close(...)`, `func exit(...)`, `func malloc(...)`, `func free(...)` at top level shadows the C function of that name for the whole binary, and the Saw runtime calls most of those. The blast radius is worst for the ones the runtime uses on its own behalf (`write` is `__saw_rt_write`'s, `malloc`/`free` are the reactor's poll buffer, `close` is every fd path), where the failure surfaces far from the definition and looks like memory corruption rather than a name collision.\n\nNOTE the asymmetry that makes it hard to notice: adding a SECOND overload of the offending name makes the symptom disappear, because both then mangle. So a program can be \"fixed\" by an unrelated edit and regress when it is undone.\n\nPOSSIBLE DIRECTIONS (not a ruling — design 249's module-keyed functions is the obvious home):\n  1. Mangle every free function by module, as methods are mangled by type. Cleanest and matches the existing precedent; the cost is symbol churn and whatever depends on the bare names (`@export` is the sanctioned way to pin one, and already exists).\n  2. Keep bare names but REFUSE a free function whose name collides with a symbol the runtime declares — a small, closed, checkable list, and a clean diagnostic at the definition. Narrower, but only covers the names the runtime happens to use, not the rest of libc.\n  3. Emit free functions with internal linkage unless `@export`ed, so nothing outside the module can bind to them. This is likely the real answer and subsumes (1) for the collision question.\n\nWORKAROUND for anyone hitting it now: rename, or make the function a method/static on a type. Design 272 unit 3 made its `raise` an extension method on `Signal` (`Signal.Terminate.send_to_self()`), which mangles and cannot collide.\n","id":"SL-274","labels":["bug","compiler"],"priority":"high","project":"SL","revision":18,"sequence":981,"stage":"queued","status":"open","title":"A free function is emitted under its BARE link symbol, so one named like a libc function silently shadows it","updated":"1789755050"},"before":{"assignee":"agent:claude-sl274","author":"agent:claude-sl204","comments":[{"author":"agent:claude-sawlang","body":"USER RULING (Sep 14): fix direction is options 3+1 COMBINED — every free function is emitted with a module-mangled symbol AND internal linkage unless @export'ed; @export remains the sanctioned way to pin a bare external name. Collisions become impossible by construction rather than diagnosed. Dispatching in the codegen-visibility batch (SL-269+SL-274+SL-275).","created":"1789433363","id":"c1"},{"author":"agent:claude-sl269","body":"## UNIT 3 STOPPED — the SL-280 interaction probe says this change WIDENS AN OPEN SOUNDNESS HOLE\n\nNot implemented. Per the dispatch mandate (\"if your change flips the public twin into the broken class, that is a HARD BLOCKER — stop unit 3, report, and do not land a change that widens an open soundness hole\"), I probed first, measured the flip, and reverted. My branch carries NO SL-274 change.\n\n### The probe, and the measurement\n\nRan on `worktree-agent-aa17b5b2067cc0075` rebased onto `ed4687c9`, so SL-280's just-merged XFAIL pin (`examples/coro_imported_private_helper_suspends.saw` + `examples/modules/sl280mod/lib.saw`) is in the tree. That pin is the sharp pair: two byte-identical entry points reaching one `yield_now()` through one more frame of an IMPORTED module, differing in a single word — `helper_public` is `public`, `helper_private` is not. `__saw_drive_steps` counts Pending resumes, so `1` means the suspension survived and `0` means it was silently dropped.\n\n    BASELINE (my branch, unmodified)          private 0    public 1\n    WITH UNIT-3 MANGLING APPLIED              private 0    public 0\n                                                           ^^^^^^^^\n\nThe probe change was the minimal stand-in for the ruling's option 1 — give every single-overload non-generic free function of a non-root module the design-249 `$m$<module>` tag, public as well as private, in `_stamp_module_private_functions` (`sawc/typechecker/registration.py:1379`). That is one line of the real unit 3 and it is the only part SL-280 is sensitive to.\n\nConfirmed at the IR, so this is not a step-counting artifact:\n\n    __Frame_helper_public occurrences in --emit-ir\n      baseline                    24      (the frame is built; the suspension survives)\n      with unit-3 mangling         0      (no frame at all; lowered as a plain call)\n\n### Why, exactly\n\nSL-280's own diagnosis names the mechanism and this change feeds it. `coro_transform.py:11425-11428` builds the imported-callee splice table keyed by the UNMANGLED `_f.name`, while the effect graph keys a free function by `(\"fn\", mangled_symbol or name)` (`typechecker/effects.py:431`). Today a `public` free function has `mangled_symbol=None`, so its edge key is the bare name, it matches the table, the body is spliced and a frame is built. Design 249 tags only MODULE-PRIVATE free functions, which is precisely why the private twin is the one that breaks today and the public twin works.\n\nUnit 3 gives the public twin a `$m$` tag too. Its edge key becomes `helper_public$m$sl280mod`, the table still holds `helper_public`, the test at `coro_transform.py:11583-11592` is False, the splice is declined — **and nothing is said**. So the change does not merely coexist with SL-280; it moves every imported PUBLIC free-function helper from the working side of that bug to the broken side.\n\n### What that would cost, if landed\n\nSL-280's runtime face is the expensive one and it is not hypothetical — it is the bug's own filing. When the dropped suspension is a `std.net` park rather than a `yield_now`, the park lowers through the out-of-frame fallback in `codegen/calls.py:575-597`, which registers the fd and then BLOCKS THE OS THREAD — the cooperative executor's thread — so the whole process stops until that fd is ready. Today that is reachable only through a module-private imported helper, which is why sawtracker's server hit it by factoring a read into a helper. After unit 3 it would be reachable through ANY imported free function called from an imported body: the ordinary shape of every library that splits a suspending operation across two functions, `public` or not. std is exempt only because its suspending surface is methods.\n\n### Re-sequencing\n\nUnit 3 should land AFTER SL-280's fix, not before. SL-280's investigator already measured that the minimal keying fix does not hold on its own (an `internal compiler error` from a duplicated LLVM symbol when the spliced clone and the imported module's own emission collide, plus a latent `self`-in-spliced-body bug at reference parameters), and proposed the real shape: ONE `_callee_frame_key(...)` funnel whose docstring names its entry points, with the effect-graph edge, `imported_free_fns`, the splice registration, `_classify_call` and `_callee_fb` all routed through it, plus the `structurally_susp_fns` phase ordering. That funnel is exactly what makes unit 3 safe, because it makes the answer independent of whether a free function carries a tag — which is the property unit 3 needs and today's code does not have.\n\nWorth noting for whoever sequences them: unit 3 is a good FORCING FUNCTION for that funnel. Once every free function is mangled, \"what key names this callee's frame?\" has one answer everywhere instead of two that happen to coincide for public functions, so the class of bug SL-280 represents stops being reachable rather than being fixed one site at a time.\n\n### What I did NOT do\n\nNo SL-274 change is on my branch — the probe was reverted and the tree verified back to `private 0 / public 1`. I did not attempt the keying fix (it is SL-280's, and its own investigator scoped it as a brief rather than a patch). The obligation-2 consumer sweep for SL-274 is posted separately so the later dispatch does not have to redo it.\n","created":"1789437546","id":"c2"},{"author":"agent:claude-sl269","body":"## OBLIGATION-2 CONSUMER SWEEP (owed before the fix; posted now so the later dispatch does not redo it)\n\n**Nothing in this repository relies on a Saw free function carrying a bare external link symbol.** Every C-facing and linker-facing site already goes through `@export`:\n\n* `sawc/rt/shim.c`'s ONLY forward declaration of a Saw symbol is `__saw_rt_dealloc` (`shim.c:204`, called at `:214`), which is `@export`ed at `rt/common/mem.saw:70`. No C in the tree calls a non-`@export`ed Saw free function.\n* All 18 freestanding cases `@export(\"fs_main\")` for `boot.S`'s `bl fs_main` (`grep -L` for that export over `tests/freestanding/cases/*.saw` returns nothing).\n* The C-ABI round-trips — `examples/export_roundtrip.saw`, `examples/never_extern_module_abi.saw` — are export round-trips, and codegen's extern/definition unification (`codegen/core.py:3545-3551`) is already guarded `if exported:`.\n* **No tool in `tools/` parses a symbol table at all**: `grep -rn 'nm \\|objdump\\|readelf\\|symtab' tools/*.py` is empty.\n\n**`--runtime-build` ALREADY implements exactly the ruled behaviour**, which is the strongest evidence the ruling is the right shape: `.build/rt/*/os_ops.o.ll` carries `define external @\"__saw_rt_tcp_listen\"` beside `define internal @\"htons\"` / `@\"ipv4_sockaddr\"` (20 of 43 defines internal), via `_apply_section_layout` (`codegen/core.py:2574-2583`) whose keep-set is `{@export'ed globals} ∪ {\"main\"}`. The freestanding profile does the same. So unit 3 is generalizing a funnel that exists and works, not inventing one.\n\nBlade and every package build compile ONE Saw unit per sawc invocation (`blade/src/builder.saw:514-534`), so no cross-object Saw symbol dependency exists anywhere; ~21 bare user free functions per blade binary would change spelling and nothing names them.\n\n**Battery lanes**, each opened rather than assumed: `abidoc` compares `RUNTIME_ABI_SYMBOLS` against `rt/ABI.md` text; `bttable` reads frame names, which `FrameBuilder` builds from `func.name` not the symbol (`coro_transform.py:2144`); `ircontract` matches `@\"__saw_rt_*\"`, all of which are export or extern names, never definition symbols; `stdtypes` asserts on TYPE mangling; `forgetgate`/`preludegate`/`gmgate`/`reemit`/`irdet`/`corodiff`/`docverify`/`bootstrap`/`freestanding` read no symbols. All UNAFFECTED. The 24 `EXPECT-IR-CONTAINS` directives name frame types and one METHOD symbol, no free function.\n\n**Scale**: ~7850 top-level free-function declarations across 3229 tracked `.saw` files (column-0 `func` heuristic, so an order of magnitude, not a parse). ~2600 are `main` and 148 are `@export`ed — both exempt — so roughly 5100 symbols change spelling and ~7500 gain internal linkage.\n\n## FOUR THINGS THE FIX MUST HANDLE — three small, one blocking\n\n1. **`main`.** It IS the C entry, emitted by name (`define i32 @\"main\"(i32, i8**)`), and the name is load-bearing at `codegen/core.py:3526`, `:3539`, `:3420`, `:2638`, `reachability.py:107`, `effects.py:1003` and `coro_transform.py:11701`. Exempt from BOTH the mangling and the internalization. (The codegen sites key on `func.name`, so a symbol-only rename is safe there — a `mangled_symbol` stamp is not.)\n2. **`codegen/calls.py:1509`** compares `sink.name == \"__saw_bt_panic\"` against the LLVM SYMBOL of a Saw free function (`std/taskgroup.saw:3118`). A mangled symbol silently falls through to the `\"rt\"` outlined-helper family, merging two families the docstring at `:1496-1502` says are deliberately separate. Compare the identity, not `sink.name`.\n3. **`@section` without `@export`** is externally visible hosted today (`nm` on `examples/section_compose.saw` gives `S _hot`) but is ALREADY local freestanding (`t handler`). The two profiles disagree now; the fix should make hosted match and say so at `LANGUAGE_SPEC.md:11583`, since the spec promises `@section` PLACEMENT only and reserves external linkage for `@export` (`:11535`).\n4. **THE BLOCKER — SL-280**, reached independently by this sweep and by the interaction probe in my previous comment. Full measurement is there; in one line, `effects.py:431` keys a free function `(\"fn\", mangled_symbol or name)` while `coro_transform.py:11110`/`:11428` key `funcs_by_name`/`imported_free_fns` by the SOURCE name, and giving every free function a `mangled_symbol` moves every free-function edge onto the mismatching side.\n\n## FIVE LATENT INSTANCES OF THIS BUG ARE ALREADY IN THE TREE\n\nThe runtime declares `extern \"C\"` `read`, `connect`, `rename`, `send`, `dup2`; tracked examples define top-level free functions of those names — `examples/conformance/V29_static_atomic_unaffected.saw:17` (`read`, plus four more `read`s elsewhere), `examples/labeled_args_mid_default_skip.saw:12` (`connect`), `examples/df140c_qualified_type_position.saw:28` (`rename`), `examples/errors/unsafe_send_not_erasable.saw:7` (`send`), `examples/closure_satisfies_copy_bound.saw:10` (`dup2`).\n\nNot theoretical: `nm` on a stock build of V29 shows `T _read` with `_read` NOT undefined — the linker bound `os_ops.o`'s call to the Saw function. An A/B probe differing only in the function's NAME:\n\n    func read(a: Int) -> Int { 4242 }      ->  File.read(size: 16) reports 4242 bytes\n    func read_renamed(a: Int) -> Int {...} ->  File.read(size: 16) reports 16 bytes\n\nA `Data` claiming 4242 bytes out of a 16-byte buffer, silently, exit 0 — in a file under `examples/conformance/`. These are ready-made regression material for the fix, and they are a second reason not to leave SL-274 open indefinitely: the corpus is already carrying the bug.\n\n## NOT COVERED\n\nThe **sawos repo (`../sawos`) was not swept** and is the one place a bare-Saw-free-function-from-C dependency could still exist — its kernel and root server link Saw objects against HAL C. sawlang references sawos nowhere, so nothing measured here says anything about it. Recommend the SL-274 brief include a grep there for `extern \"C\"`-declared names that a Saw free function defines without `@export`. Also unexecuted: `tools/lldb_saw.py` (its three symbol lookups are compiler-emitted GLOBALS, not free functions, so a free-function-only rule cannot reach them — but a wider internalization could); and no size/IR deltas from the mangling were measured.\n","created":"1789441057","id":"c3"},{"author":"agent:claude-sawlang","body":"RE-SEQUENCED (lead, Sep 14): the batch agent's measured probe shows the ruled fix (module-mangle + internal linkage for all free functions) FLIPS SL-280's public twin from working to silently-dropped — the coro splice table is keyed by unmangled name, so expanding mangling widens the open soundness hole from module-private helpers to every imported free-function helper. Correctly stopped, nothing landed. SL-274 now dispatches AFTER the SL-280 funnel fix (_callee_frame_key), and its fix brief should require the SL-280 pin pair plus a public-helper suspension row in its gate. The consumer sweep is already posted above (main x7, calls.py:1509 sink.name, @section-without-@export, five latent live instances incl. the V29 func read shadowing File.read); the user ruling on direction stands.","created":"1789441265","id":"c4"},{"author":"agent:claude-sawlang","body":"DISPATCHED (lead, Sep 17): SL-280's funnel landed (c19bf57a), so the flip this unit's mangling caused is closed — coro_module_tagged_free_callee_discovery.saw is the acceptance row (imported_public 1 must hold) and coro_imported_private_helper_suspends.saw must stay public 1 / private 1. Scope per the ruling (c1) + the sweep (c3): every free function module-mangled + internal linkage unless @export'ed; main exempt from both (also fixes SL-305); calls.py:1509 compares identity not sink.name; @section-without-@export goes local hosted to match freestanding, spec line updated; the five latent corpus instances (V29 func read etc.) become regression rows. Worktree sl274, fresh Opus agent.","created":"1789693214","id":"c5"},{"author":"agent:claude-sl274","body":"IMPLEMENTED on branch `sl274`, tip `a690608d` (rebased onto main `65eb3e96`; two commits). Terminal battery 27/27 GREEN. Not submitted — that is the lead's.\n\n## The tag scheme chosen, and what it retires\n\nONE rule, `<name>$m$<module tag>`, on EVERY free function, with `module_tag(())` being `root` so the entry module's own functions carry one. `$m$` is DF-140f's delimiter, shared with design 144's type identities; on a free function it now means \"the module that DEFINES it\" rather than \"private\", because every free function carries one. An overload member appends its design-55 suffix to that base (`read$m$root$OL$Int`) and a generic's instantiations append their type arguments (`read$m$root$1$Int`), so both classes are module-tagged too.\n\n**Design 249's `$M$` census is RETIRED outside std.** A per-module tag disambiguates two modules declaring one name by construction, so there is no whole-compilation census to take and no dependence on which module is checked first — `free_function_owner_census`, `_census_module_functions` and the `free_function_owners` plumbing they fed are deleted, and so is `_stamp_module_private_functions`, whose whole job the funnel now does at registration. std keeps a narrower per-FILE census (`std_free_function_owner_census`), for the reason below.\n\n`typechecker/registration.py::_free_function_symbol_base` is the funnel; its docstring names its entry points (`_register_function`, plus `_stamp_overload_symbols` building on `symbol_base`). FOUR declarations keep the plain name, each because something reads the WRITTEN name as the symbol:\n\n* the **ENTRY module's `main`** — the C entry, load-bearing at `codegen/core.py:3526`/`:3539`/`:3420`/`:2638`, `reachability.py:107`, `effects.py:1013` and the transform's seeds. A DEPENDENCY's `main` is ordinary, which is SL-305.\n* an **`@export`ed** function — the sanctioned bare external name; codegen emits it under the requested C symbol whatever the funnel says, so leaving the plain name keeps one identity per seam.\n* a **compiler-SYNTHESIZED** function (`is_synthesized`) or a **MONO INSTANCE** (`is_mono_instance`) — named by string where it is built and at every reference, exactly as design 204 records for a synthesized TYPE. The instance case is not cosmetic: an instance's name already carries the template's tag (`run$m$root$1$Fast`), and tagging it again detached the clone's effect node from the root the drive site had recorded. This is where 12 of the suite's 13 first-pass failures came from.\n* a **STDLIB** declaration (`_checking_builtins`), which keeps design 249's rule unchanged. Not a convenience: codegen EMITS calls to `__saw_print_int`, `__saw_fmt_int`, `__saw_main_exit_code`, `__saw_bt_panic`, `__saw_string_*`, `__saw_atomic_*` and a dozen more by literal string, so std's free functions are the compiler/stdlib seam and are named the same way a synthesized function is. The linkage half internalizes every one of them anyway, and a USER declaration can never collide with one because the user's side is always tagged.\n\n## Linkage — one funnel, three entries\n\n`codegen/core.py::_apply_section_layout` is now THE place a definition's linkage is decided and its docstring names all three callers. The HOSTED profile internalizes on exactly the terms `--freestanding` and `--runtime-build` already did, keep-set `{@export'ed globals} ∪ {\"main\"}`, gated on `_strip_unreachable` (sawc's `whole_program`) so a plain hosted `-c` object — somebody else's to link — keeps external linkage. Sections stay design-168's ELF-only business.\n\n`@section` without `@export` falls out with no extra rule: it is not in the keep-set. `examples/section_compose.saw` at -O0 goes from `S _hot` to `s _hot$m$root`, while its `@export(\"boot_double\")` sibling stays `S`. LANGUAGE_SPEC's `@section` paragraph now says placement only, and a new \"Link symbols and linkage\" section states the rule.\n\n## nm evidence, before and after\n\nThe A/B is `examples/free_function_never_shadows_a_runtime_symbol.saw`, the c3 probe as a committed test. Built with main's compiler (`25dc9a17`):\n\n    bytes 4242        <- a Data claiming 4242 bytes out of a 16-byte buffer\n    helper 4242\n    exit 0\n    0000000100000730 T _read\n\nBuilt on this branch:\n\n    bytes 16\n    helper 4242\n    exit 0\n    (no `_read` of any kind; `read$m$root` is internal and inlined away)\n\nOver six binaries (`hello`, the new test, `V29_static_atomic_unaffected`, `labeled_args_mid_default_skip`, `closure_satisfies_copy_bound`, `section_compose`), the ONLY external symbols left are `_main`, `__mh_execute_header`, the `___saw_*` reserved namespace (the `@export`ed `__saw_rt_*` seams from the linked runtime objects, shim.c's C helpers, and design 158's two `@export`ed statics), and — in `section_compose` — its own `@export`ed `_boot_double`. Local symbol counts rose correspondingly (V29: 10 local -> 16). `--runtime-build` was rebuilt from an empty `.build/rt/`: seams `define external @\"__saw_rt_tcp_listen\"` with their exact ABI names, helpers `define internal @\"htons$m$root\"` / `@\"ipv4_sockaddr$m$root\"`.\n\n## The SL-280 rows — the measurement the re-sequencing asked for\n\nAll three hold, and the two new ones from the same fixture family with them:\n\n| row | value |\n|---|---|\n| `coro_module_tagged_free_callee_discovery.saw` | entry_tagged 1, **imported_public 1** |\n| `coro_imported_private_helper_suspends.saw` | **public 1 / private 1** |\n| `coro_imported_callee_discovery_matrix.saw` | all 1s (E1/E2, S0-S9) |\n| `coro_imported_private_helper_parks_on_io.saw` | pass (the io-park wedge cell, IR property pinned) |\n| `coro_spliced_helper_keeps_its_own_module.saw` | pass |\n\n## TWO NEW KEYING SITES the universal tag exposed, both routed and both added to `frame_keys.py`'s entry list\n\nThe funnel made the answer tag-INDEPENDENT, which is what unblocked this unit — but two sites read a written NAME rather than asking it, and a public function's name and key coinciding was what had hidden them:\n\n1. `coro_transform.py::_promote_nested_generic_calls` spelled a nested generic call's instantiation `mangle_function(fc.name, args)` while `monomorphize._function_template_name` spells it from the template's SYMBOL. With every free function tagged the two disagree, the promotion silently declined, and the author got ``cannot suspend in `sync func` method: `__Frame_s6_entry.resume` calls `s6_helper$1$Int` `` — two frames they never wrote. It asks `callee_frame_key(fc)` now.\n2. the same function left the TEMPLATE's `resolved_symbol` on a call it had just rewritten to name the INSTANCE, so `callee_frame_key` would have answered the base. Cleared, on the drive-site rewrite's own precedent (`expressions.py`'s `inner.resolved_symbol = None`).\n\n## calls.py:1509\n\n`_panic_helper` keyed its outlined assembly routine on `sink.name == \"__saw_bt_panic\"` — the emitted LLVM symbol of a Saw free function. It asks `_panic_sink_is_bt()` now, which is the same identity question `_panic_sink` asks (`self.functions.get(BT_PANIC_SINK) is not None`), so the two cannot disagree. std's symbols do not move under this change, so the bug was latent rather than live; the two families the docstring says are deliberately separate can no longer merge on a rename.\n\n## The five latent corpus instances\n\nAll five compile and run unchanged, and the shadowing is gone: `V29_static_atomic_unaffected.saw` (`read`, plus four more elsewhere), `labeled_args_mid_default_skip.saw` (`connect`), `df140c_qualified_type_position.saw` (`rename`), `errors/unsafe_send_not_erasable.saw` (`send`), `closure_satisfies_copy_bound.saw` (`dup2`). `nm` on V29 no longer has `T _read` at all.\n\n## Two things the sweep did not predict\n\n**D07_never_method_and_generic** (conformance) pinned `define void @Killer_die` in the OPTIMIZED IR sidecar, and that directive was only ever observable BECAUSE of external linkage: every call to those four `-> Never` declarations is on a path the fixture's constant arguments prove dead, so with internal linkage `globaldce` deletes all four and the sidecar holds nothing but `main`. That deletion is the point of internal linkage, and what the row is about is the `void` + `noreturn` a declaration is emitted WITH — so the directive moved to `-O0`, where a declaration is still a declaration, and now pins all three method slots (`define internal void @Killer_die`, `@Killer_halt`, `@\"Killer_slay$1$Int\"`).\n\n**The `ircontract` lane** (second commit, `a690608d`) read each `-O` flag's size attributes as a SHARE over the module's attributed definitions. Its own comment records why a share and not a substring: LLVM stamps `cold minsize noinline noreturn` on the cold noreturn paths it forms at every level, and design 261's outlined panic routines are emitted `cold minsize noinline noreturn` by `_panic_helper` on purpose. As a fraction they were harmless while the fixture's hosted module carried ~90 definitions; internalization let globaldce cut it to FIVE attributed definitions, three of them those routines, so -O1 and -O2 read 60% `minsize` and the lane reported a level-table leak that is not there. A `noreturn` define is now excluded from the share, numerator and denominator alike — the share is over the definitions a level actually owns.\n\nThe other lanes the sweep flagged as at-risk came through untouched: `abidoc`, `bttable`, `stdtypes`, `reemit`, `irdet --all`, `gmgate`, `bootstrap`, `freestanding`.\n\n## Docs\n\nLANGUAGE_SPEC gains **Link symbols and linkage** (the rule, the three consequences, and the `@section` amendment); README's C-ABI-exports bullet says `@export` is the only way a declaration reaches the linker; the saw-lang skill gains the rule plus the two things that bite (the libc capture, and `@section` without `@export`). Conformance rows **K105** (the guarantee) and **K106** (SL-305's), with the header count recounted to 527.\n\n## Gate\n\nPer-commit, at each commit: suite **2544 passed / 10 xfailed**; freestanding **36/36** (riscv32 19 + arm64 17). Terminal battery at `a690608d`: **27/27 stages GREEN** in 3379s, including `irdet --all`, `reemit`, `gmgate`, `bootstrap`, `freestanding`, `fuzz`, `corodiff`, `citations`, `astgraft`.\n","created":"1789703794","id":"c6"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-274.p1 (revision 1): SL-274: every free function module-mangled + internal unless @export'ed (one symbol funnel, one linkage funnel); fixes SL-305; ircontract share excludes noreturn defines","created":"1789710702","id":"c7"},{"author":"agent:claude-sawlang","body":"SUBMITTED (lead, Sep 18) as SL-274.p1, branch sl274 tip rebased on main 0360c080, two commits squashed. Lead validation on the rebased tip: the three SL-280 rows (tagged-callee imported_public 1; pin pair public 1 / private 1; matrix all 1s), the new free_function_never_shadows_a_runtime_symbol + dependency_named_main_keeps_the_entry_point examples, D07, section_compose and export_roundtrip all pass; nm on a built V29 binary shows _main as the ONLY external T symbol and no bare _read. Agent gates at tip: suite 2544/10x, freestanding 36/36, terminal battery 27/27. Note for review: the std carve-out (std free functions keep written names because codegen names ~15 of them by literal string) is deliberate and documented in _free_function_symbol_base; they are still internalized. SL-305 closes with this merge. SL-306's fix (in flight, coro_transform.py _suspending_method_target) is independent; whichever merges second rebases.","created":"1789711182","id":"c8"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-274.p1 (revision 1)","created":"1789711183","id":"c9"},{"author":"agent:codex","body":"Requested changes on patch SL-274.p1 (revision 1)\n\nTwo blocking findings in revision 1:\n\n[P1] Preserve module-qualified Saw identities for exported functions.\n\nThe is_exported(func) exemption at sawc/typechecker/registration.py:1419 returns an empty symbol base. Same-named functions in different modules consequently share an internal lookup key even when they export distinct C names. The reproducer below previously compiles and prints 11 then 22; SL-274.p1 fails to link with Undefined symbols: _first_value. Keep the module-qualified Saw identity separate from the exported C symbol (codegen already has distinct lookup-key and C-symbol concepts).\n\n```saw\nmodule first {\n    @export(\"first_value\")\n    public func value() -> Int { 11 }\n}\n\nmodule second {\n    @export(\"second_value\")\n    public func value() -> Int { 22 }\n}\n\nfunc main() {\n    print(first.value())\n    print(second.value())\n}\n```\n\n[P2] Distinguish the entry module from a module named root.\n\nThe unconditional tag at sawc/typechecker/registration.py:1423 uses module_tag, which maps both () and (\"root\",) to root. The two private helper functions below therefore both become helper$m$root. Before this patch the program compiles and prints 2 then 101; with the patch it fails with internal compiler error: helper$m$root. Use a module encoding that distinguishes these identities and cover this case with a regression test.\n\n```saw\nmodule root {\n    func helper(n: Int) -> Int { n + 100 }\n    public func reach(n: Int) -> Int { helper(n) }\n}\n\nfunc helper(n: Int) -> Int { n + 1 }\n\nfunc main() {\n    print(helper(1))\n    print(root.reach(1))\n}\n```\n\nA/B evidence: the unpatched compiler checkout was 0ec310c5; git diff --name-only 0360c080 0ec310c5 -- sawc is empty, confirming its compiler sources match this patch's base. Both probes pass on that compiler and fail with the reviewed patch applied to 0360c080.\n\nIndependent validation: all 14 focused tests passed, including libc shadowing, dependency main, export_roundtrip, section_compose, D07, nested generics, and SL-280 regressions. The two additional probes above block approval.\n","created":"1789737746","id":"c10"},{"author":"agent:claude-sawlang","body":"ACK codex review c10 (lead, Sep 18): both findings confirmed against the diff. P1 — the @export exemption returned an EMPTY symbol base, collapsing same-named exported functions of different modules onto one lookup key (the Saw identity and the emitted C symbol must stay distinct; codegen already has both concepts). P2 — module_tag maps both the entry module () and a user module named root to 'root', so helper$m$root is emitted twice; the entry module needs an encoding no user module path can spell, applied consistently with design 144's type-identity tags. SL-274 agent RESUMED in the same worktree: both fixes through the funnel, both repros + a combined one as examples on the conformance row, a re-audit of every exemption branch for a same-name-across-modules collapse, per-commit gate + battery; r2 follows.","created":"1789737800","id":"c11"},{"author":"agent:claude-sl274","body":"BOTH REVIEW FINDINGS FIXED on branch `sl274`, tip `b52f6651` (rebased onto main `3043d813`; three commits — the two from r1 plus one for the review). Terminal battery 27/27 GREEN at the tip. Not submitted; the lead revises to r2.\n\nCodex was right on both counts, and both were mine.\n\n## [P1] An `@export`ed function is tagged like any other\n\n**Mechanism.** The exemption reached the wrong one of `@export`'s two jobs. `@export` decides the EMITTED C symbol and the linkage; it decides nothing about the name the compiler knows a function by — and `codegen/core.py::_declare_function` has always kept the two apart, `func_name` keying `self.functions` while `llvm_name` is `export_symbol(func)` when there is one. Returning an empty symbol base therefore bought nothing and cost the key: both `value`s shared the key `value`, the second overwrote the first, the first's body was never emitted, and its `@llvm.used` keep-root named a declaration with no definition.\n\n**Fix** — `sawc/typechecker/registration.py:1415` (the `is_exported(func)` clause is gone; the exemption list is now `is_synthesized` / `is_mono_instance` only, plus the entry module's `main`). The funnel docstring records what the exemption got wrong, so the next reader does not re-derive it.\n\n**The consumer sweep the lead asked for**, every reader of `is_exported`/`export_symbol` opened rather than assumed:\n\n* `_exported_llvm_globals` (the `_apply_section_layout` keep-set and `_emit_llvm_used`) holds llvm.Function OBJECTS and reads `g.name` — the emitted symbol. Unaffected.\n* `codegen/reachability.py` roots by llvm name, and `_defer_body` keys on `llvm_func.name` by documented design. Unaffected.\n* design 58's extern/definition unification looks up `llvm_name` (`self.module.get_global`). Unaffected.\n* the duplicate-`@export` table (`_register_export_symbol`) and design 113b's seam check key on the export name from the ATTRIBUTE. Unaffected — and the refusal still fires: two modules exporting one C name give ``duplicate `@export` symbol `shared_value` (already exported by `value` at line 4)``.\n* `effects.py::_effect_enter_function` and `expressions.py::_funcpointer_decl_is_sync` read the declaration NODE, not a name. Unaffected.\n* `funcpointer_symbol` was already `sym.mangled_name or name`, so form-2 `FuncPointer` construction follows the tag for free.\n\n**Measured, not reasoned:** `export_roundtrip` (15), `never_extern_module_abi` (its intended panic), `section_compose` (84/43), `export_static_section` (30/12345) all run; a freestanding case compiled by hand still emits `T fs_main`; `--runtime-build` rebuilt from an EMPTY `.build/rt/` keeps `define external @\"__saw_rt_alloc\"`, `@\"__saw_rt_tcp_listen\"` and the rest beside 20 internal helpers.\n\n## [P2] The entry module is the EMPTY tag\n\n**Mechanism.** `type_identity.module_tag` rendered the entry module's empty path as the word `root`, which is exactly what a user module named `root` renders as — the rendering was not injective, and two declarations took one tag.\n\n**Encoding chosen: the EMPTY tag.** A free function of the entry module is `helper$m$`; a module named `root` is `helper$m$root`. Every real module path renders at least one character, so the empty string is the one tag no module can ever take — no reserved word to defend, and nothing to re-decide if module paths grow. `$` is forbidden in a Saw identifier, so a trailing delimiter cannot collide with a name an author could write. Two riders: `ErrorReporter._QUALIFIER_RE` widens from `+` to `*` so a bare `$m$` is scrubbed out of a diagnostic like any other qualifier (checked — `helper$m$` -> `helper`, `run$m$$1$Fast` -> `run$1$Fast`, `read$m$$OL$Int` -> `read$OL$Int`), and `module_tag` now carries the rule in its own docstring.\n\n**Fix** — `sawc/type_identity.py:129` (`module_tag`) and `sawc/errors.py:149`.\n\n**Why `module_tag` and not the free-function funnel**, which is the part worth keeping: that is where every `$m$` tag in the compiler is spelled, and the free-function tags and design 144's type identities have to agree about which module is which. It also closes the SAME collision at design 142's cross-module extension-method tie-break (`_stamp_overload_symbols`' `$M$<tag>`), **where it predates SL-274**:\n\n    internal compiler error: Marker$m$sl274marker_describe$OL$$M$root\n\non main today, for an entry-module extension beside a module-`root` extension of one type with one signature. Nothing else reached it — design 144 exempts the entry module's types outright (`qualifies` is False for an empty path) and `_module_private_symbol` returns None for one — which is why `module_tag(())` had exactly one caller and the collision sat unprobed. Pinned as a regression test rather than filed, since this patch fixes it.\n\n## The re-audit (lead's item 2), probed not reasoned\n\nEvery remaining exemption branch, with a two-module program per branch (`.build/scratch/audit_*`), run on THIS branch and on main:\n\n| branch | probe | branch | main |\n|---|---|---|---|\n| `is_synthesized` | two modules' same-named SUSPENDING helper, both driven — the drivers are minted from frame keys, which are tagged | entry 2 / dep 101 | entry 2 / dep 101 |\n| `is_mono_instance` | two modules' same-named GENERIC, both instantiated at `Int` | entry 2 / dep 101 | entry 2 / dep 101 |\n| overload sets | two modules' same-named set with identical signatures | entry 2 / entry-str 2 / dep 101 | same |\n| entry `main` | SL-305's row — and there can only ever be one exempt `main` | pass | link failure |\n| std (`_checking_builtins`) | design 249's per-FILE census, unchanged by this patch | — | — |\n\nSo neither remaining exemption admits a collapse, and none of the three probes is a new hole. One thing the audit did NOT close, recorded rather than fixed: a user module path `std.data` renders `std_data`, the same as std's own file-module, so design 144 TYPE identities could collide there. It predates SL-274 by a long way, it is unreachable for free functions (std's keep their written names, the user's are tagged), and it wants its own issue rather than a rider on this one.\n\n## Regression tests — four, each failing on the compiler it pins\n\n| test | main | r1 | r2 |\n|---|---|---|---|\n| `two_modules_export_distinct_c_symbols.saw` | 11 / 22 | `Undefined symbols: _first_value` | 11 / 22 |\n| `module_named_root_is_not_the_entry_module.saw` | 2 / 101 | `internal compiler error: helper$m$root` | 2 / 101 |\n| `exported_function_in_a_module_named_root.saw` (the combined cell) | `Undefined symbols: _root_value` | fails | saw 11 22 / c 11 22 |\n| `extension_method_tie_break_distinguishes_the_entry_module.saw` + `modules/sl274marker/` | `internal compiler error: Marker$m$sl274marker_describe$OL$$M$root` | same | entry 2 |\n\nThe first two check BOTH directions where it matters: the Saw calls, which route through the module-qualified identity, and `extern \"C\"` calls back in, which route through the export names.\n\nConformance rows **K107** (the export identity/symbol separation) and **K108** (a module named `root` is not the entry module, with the extension-method site as its second file and the reason the fix belongs in `module_tag`), K105 amended to point at both, header recounted to 529.\n\n## Docs\n\nLANGUAGE_SPEC's \"Link symbols and linkage\" now states the empty tag, why it is the one unspellable rendering, and that an `@export`ed function is tagged like any other (with the two-module export example). The saw-lang skill line matches. `examples/free_function_never_shadows_a_runtime_symbol.saw` and the SL-280 `sl280tag` fixture's comments no longer claim the `$M$root` tag the retired census used to give them.\n\n## Gate\n\nPer-commit at the tip: suite **2548 passed / 10 xfailed** (up 4 — the new tests); freestanding **36/36** (riscv32 19 + arm64 17). Terminal battery after the rebase: **27/27 stages GREEN** in 3461s, including `irdet --all`, `reemit`, `gmgate`, `bootstrap`, `freestanding`, `fuzz`, `corodiff`, `citations`, `astgraft`, `ircontract`.\n","created":"1789746200","id":"c12"},{"author":"agent:claude-sl274","body":"REBASED onto main after SL-306 landed underneath. Tip `ef71bdd5` (four commits on `b7fb8fa0`), terminal battery **27/27 GREEN** at that tip. Ready for r2; not submitted.\n\n## Conflicts resolved — `examples/conformance/INDEX.md` only\n\n`sawc/coro_transform.py`, `sawc/sawc.py` and `sawc/typechecker/core.py` all AUTO-MERGED, both commits, despite SL-306 editing the same functions. INDEX.md conflicted twice (once per commit of mine that touches it), and the substance of the conflict was a ROW-NUMBER COLLISION rather than text: **SL-306 took K105**, which both of my commits also used.\n\n* **the header count**, both commits. Resolved by keeping BOTH histories in the chain rather than either side's line: `530 rows: 278 here, 252 elsewhere` for my four rows, then SL-306's `526 / 278 / 248` line, then the older chain untouched.\n* **the row block after K104**, both commits. Resolved by keeping SL-306's K105 verbatim and RENUMBERING mine: K105->K106 (libc shadowing), K106->K107 (dependency `main`), K107->K108 (the export identity), K108->K109 (a module named `root`). Their internal cross-references were renumbered with them — K107's \"fixed by K105's mangling rule\" -> K106's, K108's \"K105's first draft\" -> K106's, K108's \"meets K108\" -> K109, and K106's tail now points at K108/K109.\n\nChecked afterwards, per the hunk-by-hunk rule: SL-306's ELEVEN covering files are all still named and so are my six (11/11 and 6/6 by probe), the header adds up (278 + 252 = 530), and no row id is duplicated by the resolution. One duplicate id exists in the table — `K53` twice — and it is PRE-EXISTING on origin/main (two occurrences there as well), so it is not mine to fix in this patch; flagging it rather than touching it.\n\n## The two keying sites (lead's item 2)\n\nBoth survived and compose with SL-306's change to the very same function, `_promote_nested_generic_calls`:\n\n    sawc/coro_transform.py:10830   mangled = mangle_function(callee_frame_key(fc) or fc.name, args)\n    sawc/coro_transform.py:10831   if not instantiation_suspends(mangled):     # SL-306's funnel\n    sawc/coro_transform.py:10851   fc.resolved_symbol = None\n\nThe two changes are orthogonal and both are needed: mine SPELLS the instance (`callee_frame_key`, so the walk names the instance the registry built), SL-306's decides WHETHER it suspends (`suspends_ignoring_closure_calls`, so a conservative-only instance is not promoted). `_classify_call`'s own `callee_frame_key(fc)` (`:5192`) is untouched, and `frame_keys.py` still lists `_promote_nested_generic_calls` among its 16 named entry points.\n\nVerified by running the two units' rows together: `two_modules_export_distinct_c_symbols`, `module_named_root_is_not_the_entry_module`, `exported_function_in_a_module_named_root`, `extension_method_tie_break_distinguishes_the_entry_module`, `free_function_never_shadows_a_runtime_symbol`, `dependency_named_main_keeps_the_entry_point`, `coro_module_tagged_free_callee_discovery`, `coro_imported_private_helper_suspends`, `coro_imported_callee_discovery_matrix`, `coro_conservative_generic_instance_not_promoted`, `coro_conservative_callee_not_embedded`, `coro_json_encode_in_task` — 12/12.\n\n## One cross-unit interaction the suite caught (fourth commit, `ef71bdd5`)\n\nSL-306's `coro_conservative_generic_instance_in_spawned_task.saw` pins a generic instantiation's frame BY SYMBOL: `EXPECT-IR-CONTAINS: __Frame_yielder$1$Int`. SL-274 tags every free function, so the entry module's template base is `yielder$m$` and the instance — and the frame named for it — is `__Frame_yielder$m$$1$Int`. Only the spelling moves: the row's claim is unchanged and still checked in full (the conservative-only `apply<Int>` stays unframed, and `EXPECT-IR-ABSENT: __Frame_apply` needed nothing because a substring test matches the tagged spelling too; the INSTANCE rather than the template is framed; `__Frame_worker` is present; and the interleaved output is what says the worker really parked inside it).\n\nSwept for siblings: the corpus has exactly TWO IR directives naming a generic instance. The other is D07's `Killer_slay$1$Int`, a METHOD-generic, which carries no module tag because the entry module's TYPES are not qualified (design 144) and which passes unchanged.\n\n## Gate at `ef71bdd5`\n\nPer-commit: suite **2559 passed / 10 xfailed**; freestanding **36/36** (riscv32 19 + arm64 17). Terminal battery: **27/27 stages GREEN** in 3100s — `suite`, `icebreadcrumb`, `lexdiff`, `astdiff`, `astgraft`, `transferdecisions`, `producertaxonomy`, `citations`, `forgetgate`, `ircontract`, `preludegate`, `docverify`, `stdtypes`, `toolchain`, `floatvectors`, `abidoc`, `bttable`, `fuzz`, `corodiff`, `bench`, `selfhostlex`, `minivm`, `reemit`, `irdet --all`, `gmgate`, `bootstrap`, `freestanding`.\n\nRe-verified at the tip after the rebase: both review repros (`11 / 22` and `2 / 101`) and the original guarantee (`bytes 16`, and `_read` is `U`, resolved from libc, not `T`).\n","created":"1789751184","id":"c13"}],"created":"1789218312","description":"Found while building design 272 unit 3 (Sep 12 2026). PRE-EXISTING and language-wide; unit 3 renamed its way out rather than coding around it.\n\nSYMPTOM: a Saw FREE FUNCTION is emitted with its BARE name as the link symbol, so a free function whose name matches a libc symbol SILENTLY SHADOWS it for the whole program — including for C code inside the Saw runtime itself. There is no diagnostic at any stage.\n\nHOW IT PRESENTED. std.signal grew a public free function `raise(which: Signal)` (POSIX's own name for \"send a signal to this process\"), whose body calls the shim seam `__saw_signal_raise`, whose body calls C `raise(3)`. The program segfaulted. `nm` on the binary:\n\n  0000000100005164 T ___saw_signal_raise\n  00000001000006fc T _raise            <- the SAW function, not libc's\n\nso the shim's `raise((int)signo)` re-entered the Saw function, which called the shim, which called the Saw function: unbounded recursion, stack exhaustion, SIGSEGV. The reported failure is a segfault with no stack and nothing pointing at the name.\n\nTHE MECHANISM, probed rather than inferred. A user program at -O0 with two top-level free functions:\n\n  func strlen(n: Int) -> Int { n + 1 }\n  func getpid(n: Int) -> Int { n + 2 }\n\nemits\n\n  0000000100002080 T _strlen$OL$Int\n  00000001000020cc T _getpid           <- UNMANGLED\n\nSo the rule is: a free function is emitted under its bare name UNLESS it participates in an overload set, in which case it picks up the `$OL$` suffix and is safe by accident. `getpid` is unmangled and would shadow libc's `getpid` for the entire process. METHODS are unaffected — they mangle as `Type_method` (`_IoError_format`, `_StringBuilder_append$OL$String`), which is why this has not been hit before: almost everything public in std is a method.\n\nWHY IT IS A CLASS, not this one name. Every libc symbol is exposed: a user writing `func open(...)`, `func read(...)`, `func write(...)`, `func close(...)`, `func exit(...)`, `func malloc(...)`, `func free(...)` at top level shadows the C function of that name for the whole binary, and the Saw runtime calls most of those. The blast radius is worst for the ones the runtime uses on its own behalf (`write` is `__saw_rt_write`'s, `malloc`/`free` are the reactor's poll buffer, `close` is every fd path), where the failure surfaces far from the definition and looks like memory corruption rather than a name collision.\n\nNOTE the asymmetry that makes it hard to notice: adding a SECOND overload of the offending name makes the symptom disappear, because both then mangle. So a program can be \"fixed\" by an unrelated edit and regress when it is undone.\n\nPOSSIBLE DIRECTIONS (not a ruling — design 249's module-keyed functions is the obvious home):\n  1. Mangle every free function by module, as methods are mangled by type. Cleanest and matches the existing precedent; the cost is symbol churn and whatever depends on the bare names (`@export` is the sanctioned way to pin one, and already exists).\n  2. Keep bare names but REFUSE a free function whose name collides with a symbol the runtime declares — a small, closed, checkable list, and a clean diagnostic at the definition. Narrower, but only covers the names the runtime happens to use, not the rest of libc.\n  3. Emit free functions with internal linkage unless `@export`ed, so nothing outside the module can bind to them. This is likely the real answer and subsumes (1) for the collision question.\n\nWORKAROUND for anyone hitting it now: rename, or make the function a method/static on a type. Design 272 unit 3 made its `raise` an extension method on `Signal` (`Signal.Terminate.send_to_self()`), which mangles and cannot collide.\n","id":"SL-274","labels":["bug","compiler"],"priority":"high","project":"SL","revision":17,"sequence":980,"stage":"queued","status":"open","title":"A free function is emitted under its BARE link symbol, so one named like a libc function silently shadows it","updated":"1789751184"},"id":"SL-274","project":"SL","request":"{\"action\":\"patch_revise\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/.claude/skills/saw-lang/SKILL.md b/.claude/skills/saw-lang/SKILL.md\\nindex 2357cf1d..87010a30 100644\\n--- a/.claude/skills/saw-lang/SKILL.md\\n+++ b/.claude/skills/saw-lang/SKILL.md\\n@@ -3579,6 +3579,29 @@ error — EXCEPT under `sawc --runtime-build` (design 113b), which lets the\\n per-host runtime under `sawc/rt/` export exactly the frozen `__saw_rt_*`\\n ABI (sync-only; a non-ABI `__saw_rt_*` name / a suspending body is a clean\\n error). You only touch this when authoring `sawc/rt/`.\\n+- **`@export` IS THE ONLY WAY A DECLARATION REACHES THE LINKER (SL-274).** Every\\n+  other definition is emitted with a MODULE-MANGLED symbol (`read` in a module\\n+  `wire` is `read$m$wire`; the ENTRY module has no path and renders as the EMPTY\\n+  tag, `read$m$`, which is the one spelling no module can take — so a module\\n+  literally named `root` is a different module; an overload member is\\n+  `read$m$$OL$Int` and an instantiation `read$m$$1$Int`) and INTERNAL\\n+  linkage. An `@export`ed function is tagged like any other: `@export` decides\\n+  the emitted C symbol and the linkage, not the name the compiler knows the\\n+  function by, so two modules may export distinct C names from one Saw name.\\n+  Two things follow that you can otherwise get bitten by. A top-level\\n+  `func read(...)`/`write`/`connect`/`open`/`malloc` no longer captures the libc\\n+  symbol of that name for the whole program — it used to, silently, including\\n+  for the runtime's own `__saw_rt_fs_read` seam, so `File.read(size: 16)`\\n+  reported 4242 bytes out of a 16-byte buffer at exit 0 (and adding a SECOND\\n+  overload of the name made the symptom vanish, so an unrelated edit could\\n+  \\\"fix\\\" it). And `@section(\\\".s\\\")` WITHOUT `@export` places the definition and\\n+  publishes nothing — hosted used to leave it externally visible while\\n+  freestanding did not; both are local now. The mangling is invisible to\\n+  source: a call site names the function, never the symbol. The ENTRY module's\\n+  `main` is the one exemption (it is the C entry); a DEPENDENCY that declares\\n+  `main` gets an ordinary tagged internal function, which is what stopped the\\n+  entry point from disappearing into an `Undefined symbols: \\\"_main\\\"` link\\n+  failure.\\n - **`@align(N)` STATES A SLOT'S ALIGNMENT (DF-300b)** — on a local `let`/`var`\\n   or a `static`, and nowhere else. A `[UInt8; N]` is 1-aligned by ABI, so a\\n   byte buffer whose ABI wants word alignment has no other way to say so, and\\ndiff --git a/LANGUAGE_SPEC.md b/LANGUAGE_SPEC.md\\nindex ea4e1626..658d71b6 100644\\n--- a/LANGUAGE_SPEC.md\\n+++ b/LANGUAGE_SPEC.md\\n@@ -11583,7 +11583,10 @@ still governs Saw-side callers.\\n **`@section(\\\"name\\\")`** places a top-level function or static in the named\\n object-file section (the LLVM section attribute). It composes with `@export` and\\n does not require it; the name is passed through verbatim (the linker's problem)\\n-beyond a non-empty check. Section-name *syntax* is target-specific — ELF accepts\\n+beyond a non-empty check. Placement is all it does: a `@section` without an\\n+`@export` beside it still has internal linkage, in both profiles (see\\n+[Link symbols and linkage](#link-symbols-and-linkage)), so the section is a\\n+placement request and never a second way to publish a symbol. Section-name *syntax* is target-specific — ELF accepts\\n `.vector_table`, mach-o requires the `SEG,sect` form. Getting it wrong on a\\n mach-O target is a compile error naming the declaration and the two-part form:\\n \\n@@ -11604,6 +11607,60 @@ func kernel_entry() -> Int32 { 0 }\\n static VECTORS: [UInt32; 64]        // externally-visible, kept alive, in-section\\n ```\\n \\n+#### Link symbols and linkage\\n+\\n+**Status: implemented.** `@export` is the only way a Saw declaration reaches the\\n+linker. Every other definition a compile produces is emitted with a decorated\\n+symbol and **internal linkage**, so nothing outside the module can bind to it.\\n+\\n+A free function's symbol is its name plus the module that defines it: `read` in a\\n+dependency called `wire` is emitted as `read$m$wire`. The entry module has no\\n+path, so it renders as the EMPTY tag — `read` at the top of the program you are\\n+compiling is `read$m$`. That is not a cosmetic choice: every real module path\\n+renders at least one character, so the empty tag is the one spelling no module\\n+can take, and a module literally named `root` (`read$m$root`) stays a different\\n+module from the entry one. An overload set appends its design-55 signature suffix\\n+to the base (`read$m$$OL$Int`) and a generic's instantiations append their type\\n+arguments (`read$m$$1$Int`). Two modules may declare one free-function name, and\\n+the symbols differ by construction rather than by a check.\\n+\\n+The entry module's `main` is the single exemption: it is the C entry, emitted\\n+under that name with external linkage. A dependency that declares `main` gets an\\n+ordinary free function with an ordinary tag.\\n+\\n+An `@export`ed function is tagged like any other. `@export` decides the emitted C\\n+symbol and the linkage, not the name the compiler knows the function by, so two\\n+modules may export distinct C names from one Saw name:\\n+\\n+```saw\\n+module first {\\n+    @export(\\\"first_value\\\")\\n+    public func value() -> Int { 11 }\\n+}\\n+\\n+module second {\\n+    @export(\\\"second_value\\\")\\n+    public func value() -> Int { 22 }\\n+}\\n+```\\n+\\n+The mangling is invisible to Saw source — a call site names the function, not the\\n+symbol. What it changes is what the LINKER sees, and that is the point: a program\\n+with a top-level `func read(...)` used to emit a global `read`, which the linker\\n+resolved libc's `read(2)` to, including for the `__saw_rt_fs_read` seam inside\\n+the runtime. `File.read` then reported whatever that function returned. Internal\\n+linkage closes the same hole from the other side, for the std free functions that\\n+keep their written names as well.\\n+\\n+Three consequences worth stating:\\n+\\n+- A hosted `-c` object is somebody else's to link, so it keeps external linkage\\n+  on everything. Internalization applies where a compile owns the whole program:\\n+  an executable, `--freestanding`, and `--runtime-build`.\\n+- An internal definition nothing reaches is deleted by the `-O1` pipeline. That\\n+  is intended, and it is why `@export` carries a DCE keep-root with it.\\n+- Nothing in the language exposes a symbol name. To pin one, write `@export`.\\n+\\n #### Alignment\\n \\n **`@align(N)`** states the alignment of one piece of storage: a local `let`/`var`\\ndiff --git a/README.md b/README.md\\nindex a9660261..539b788f 100644\\n--- a/README.md\\n+++ b/README.md\\n@@ -764,7 +764,11 @@ Saw is freestanding: the same language targets bare metal.\\n   [the allocation-free path](LANGUAGE_SPEC.md#format-arguments-and-the-allocation-free-path)\\n   in the spec.\\n - **C-ABI exports**: `@export(\\\"name\\\")` gives a function an exact, unmangled\\n-  symbol with the C calling convention.\\n+  symbol with the C calling convention, and it is the only way a Saw\\n+  declaration reaches the linker. Every other definition is emitted with a\\n+  module-mangled symbol and internal linkage, so a top-level `func read(...)`\\n+  cannot capture libc's `read` for the rest of the program. See\\n+  [link symbols and linkage](LANGUAGE_SPEC.md#link-symbols-and-linkage).\\n - **Stated alignment**: `@align(N)` on a local or a `static` emits the slot at\\n   an alignment of at least `N` bytes. A `[UInt8; N]` is 1-aligned by ABI, so a\\n   buffer that an ABI needs word-aligned has no way to say so otherwise, and the\\ndiff --git a/examples/conformance/D07_never_method_and_generic.saw b/examples/conformance/D07_never_method_and_generic.saw\\nindex 8bbe55ee..a376e808 100644\\n--- a/examples/conformance/D07_never_method_and_generic.saw\\n+++ b/examples/conformance/D07_never_method_and_generic.saw\\n@@ -8,8 +8,20 @@\\n // attribute — could never fire on it (design 228 leg 3). The monomorphized\\n // twin had the same hole.\\n //\\n+// The directive reads the UNOPTIMIZED lowering (`-O0`), which is where the\\n+// declaration's own shape lives. Since SL-274 the hosted profile internalizes\\n+// every definition it did not `@export`, so at the default pipeline `globaldce`\\n+// deletes all four of these — every call to them is on a path the constant\\n+// arguments below prove dead — and the sidecar holds nothing but `main`. That\\n+// they are deletable is the point of internal linkage; what this row is about\\n+// is the `void` + `noreturn` they are declared WITH, and `-O0` is where a\\n+// declaration is still a declaration.\\n+//\\n // EXPECT: success\\n-// EXPECT-IR-CONTAINS: define void @Killer_die\\n+// COMPILE-FLAGS: -O0\\n+// EXPECT-IR-CONTAINS: define internal void @Killer_die\\n+// EXPECT-IR-CONTAINS: define internal void @Killer_halt\\n+// EXPECT-IR-CONTAINS: define internal void @\\\"Killer_slay$1$Int\\\"\\n // EXPECT-OUTPUT:\\n // 7 7 7 7\\n \\ndiff --git a/examples/conformance/INDEX.md b/examples/conformance/INDEX.md\\nindex 2f94c13e..c95b43bc 100644\\n--- a/examples/conformance/INDEX.md\\n+++ b/examples/conformance/INDEX.md\\n@@ -6,7 +6,12 @@ either a file in this directory or an existing `examples/` test that already\\n asserts the same rule at the same position — this table is the record of which,\\n and the dedup decisions are meant to be audited from it.\\n \\n-**526 rows: 278 carry a file here, 248 are covered elsewhere** (SL-306 added\\n+**530 rows: 278 carry a file here, 252 are covered elsewhere** (recounted Sep 18\\n+when SL-274's review added K108-K109; previously, and on the same basis:\\n+**528 rows: 278 carry a file here, 250 are covered elsewhere**, recounted Sep 17\\n+when SL-274 added K106-K107 — all four are covered by `examples/` files;\\n+previously, and on the same basis:\\n+**526 rows: 278 carry a file here, 248 are covered elsewhere**, when SL-306 added\\n K105, whose eleven covering files all live in `examples/`; previously, and on the\\n same basis:\\n **525 rows: 278 carry a file here, 247 are covered elsewhere**, recounted Sep 14\\n@@ -664,6 +669,10 @@ Claim source: spec 6 *Send and Sync* + *Cooperative tasks*; designs 75, 88, 103,\\n | K103 | a free function's FRAME KEY is independent of whether registration stamped a mangled symbol on it, and of which rule stamped one | `coro_module_tagged_free_callee_discovery.saw` | SL-280, and the acceptance criterion SL-274 is blocked on. The mechanism K101 records is not confined to imported bodies or to `private`: it is confined to callees whose key and written name DIFFER, so any rule that renames a free function walks into the same hole. This row reaches it through design 249's OTHER tag — two modules legally declaring one free-function name, which stamps `$M$<module>` on BOTH, including the entry module's own (`$M$root`) and the dependency's PUBLIC one. Both cells measured 0 before the fix. SL-274 gives EVERY free function a module-mangled symbol, which ends today's accidental coincidence between a public function's bare name and its key and would move the entire working half of K101's matrix onto the broken half; it is safe exactly when the answer does not depend on the tag, which is what the `imported_public` cell measures. One funnel answers it now — `sawc/frame_keys.py::callee_frame_key`, whose docstring names its entry points (obligation 1) |\\n | K104 | a body SPLICED out of an imported module for frame building keeps its OWN module's meaning under a name collision | `coro_spliced_helper_keeps_its_own_module.saw` | SL-280 — the face that was a WRONG ANSWER rather than a dropped suspension, and why the fix could not stop at the discovery tables. Design 208's splice copies an imported body into the entry module so a frame can be built from it, and `_classify_call` decided which frame a call embeds by comparing the call's PLAIN NAME against the driven closure. Once the entry module's same-named `tagged_helper` was itself in that closure, the spliced body's call to the DEPENDENCY's helper matched the entry module's and the entry module's frame was embedded in its place: the dependency's helper never ran and the program answered 2 where 101 is right, with nothing reported. The values are the oracle (+1 versus +100, so which of the two ran is visible), and the entry-module control one line later says the fix did not merely swap the confusion around. The same keying carries the emission INVARIANT the splice needs — the imported module owns the symbol, the clone is filtered back out of `program.functions` by frame key once its frame exists, and a clone left behind is a half-lowered husk that either names `self` in a free function at the post-transform re-check or double-defines a symbol llvmlite refuses |\\n | K105 | a callee that suspends only through the CONSERVATIVE closure-call rule is never embedded, and a really-suspending closure body is still refused rather than silently parked | `coro_conservative_callee_not_embedded.saw` + `coro_conservative_callee_in_spawned_task.saw` + `coro_conservative_free_callee_not_framed.saw` + `coro_json_encode_in_task.saw` + `coro_module_json_encode_in_task.saw` + `errors/coro_suspending_closure_body_in_a_visitor.saw` + `errors/coro_suspending_closure_body_at_a_user_method_call.saw` + `errors/coro_io_park_in_closure_body_beside_a_conservative_call.saw` + `coro_conservative_generic_instance_not_promoted.saw` + `coro_conservative_generic_instance_in_spawned_task.saw` + `errors/sync_context_calls_a_closure_taking_helper.saw` | SL-306 — the two directions of one question, which is why the row is one row. The effect graph has a source that says MIGHT: a call through a non-`sync` function value (`effects._effect_indirect_call`), which every closure-taking body raises — `Vector.each`, `Map.each`, `JsonValue._write`'s recursion inside one, a user method that calls a closure parameter. Read as \\\"suspends\\\", it made the transform embed a frame around a body that never parks, and the closure-body rejector then refused ordinary programs from inside a std file: any driven frame reaching `to_json_string()` failed to compile, which took sawtracker's whole gate down when SL-280's keying fix made the imported shape reach the same code. Both the call-site classifier (`_suspending_method_target`) and the closure walk's edge-follow ask `effects.suspends_ignoring_closure_calls` now — the ONE funnel, which strikes exactly that source and nothing else. It is deliberately NOT design 206's `really_suspending`, which also strikes the test-only `__saw_suspend`: that IS a state boundary a frame must split at, and gating on it dropped every design-44 test's suspension (`coro_nested_suspend_method.saw` died as an internal compiler error where the driven method's body had been stripped). The refusal half is what keeps this out of SL-287's silent class: a closure body that really suspends is a real source on the ENCLOSING body's own effect node, so that body is a frame whatever its callees are, and the caller's own scan (`_reject_buried_suspend_call`) refuses it at the closure — including the io-park cell, which would otherwise reach codegen's out-of-frame `__saw_exec_park(-1)` and stop the executor's thread. Three locations × both roots are covered per file (entry module, an imported `public` helper, an imported MODULE-PRIVATE one; driven root and spawned task), with `EXPECT-IR-ABSENT`/`CONTAINS` on the frames because every cell prints the right answer either way. THE GENERIC ROW came from codex's r1 review and is the funnel's fourth entry point: `_promote_nested_generic_calls`' `instantiation_suspends` still read the broad bit, so one instance was framed for a DIRECT driven call and left plain for a conservative-only caller, and the half-lowered body reached codegen (`internal compiler error … (SelfExpr): 'self' not found in current scope`) — promotion and framing are one decision seen from two ends and have to ask one question. Its other end is the SYNTHESIZED frame method's `sync` marker (`SuspendNode.closure_calls_permitted`): what that marker guards is the transform's own invariant — no REAL suspension left un-lowered in a resume — so a resume may call a conservative-only callee, while an AUTHOR-declared `sync` context still refuses one, which is the last file's row |\\n+| K106 | a user free function never shadows a runtime or libc symbol of the same name | `free_function_never_shadows_a_runtime_symbol.saw` | SL-274 — a free function was emitted under the name the author wrote, so a top-level `func read(...)` BOUND the whole program's `read`, including for `__saw_rt_fs_read`, whose seam calls libc's `read(2)`. `File.read(size: 16)` then reported 4242 bytes out of a 16-byte buffer, exit 0, with no diagnostic at any stage — measured in this exact file, and the A/B was the function's NAME and nothing else (renaming it to `read_renamed` printed 16). Every libc symbol was exposed, and the corpus was already carrying five instances (`read`, `connect`, `rename`, `send`, `dup2` — each an `extern \\\"C\\\"` the runtime declares); the worst are the ones the runtime uses on its own behalf, where the failure surfaces far from the definition and reads as memory corruption. NOTE the asymmetry that kept it hidden: adding a SECOND overload of the offending name made the symptom disappear, because both then carried the design-55 `$OL$` suffix, so an unrelated edit could \\\"fix\\\" a program and its revert could regress it. The ruling (user, Sep 14) is by construction rather than by diagnosis, and it has two halves that close the hole from both sides — every free function is emitted under a MODULE-MANGLED symbol (`typechecker/registration.py::_free_function_symbol_base`, which retires design 249's census tag in favour of an unconditional `$m$<module>`) and, unless `@export`ed, with INTERNAL linkage (`codegen/core.py::_apply_section_layout`, generalized from the freestanding and `--runtime-build` profiles that already did exactly this). The second half is what also covers std's own free functions, which keep the names the author wrote because codegen emits calls to a dozen of them by literal string. `nm` on the built binary is the direct witness: no `T _read`, and `_main` still external. K108 and K109 are the two cells this rule's first revision got wrong, both found by review: which of `@export`'s two jobs the exemption reached, and whether the entry module's tag is spellable by a module |\\n+| K107 | a dependency declaring `main` leaves the entry module's entry point alone | `dependency_named_main_keeps_the_entry_point.saw` | SL-305, fixed by K106's mangling rule and its ONE exemption. `main` IS the C entry, emitted under that name and load-bearing at seven sites across codegen, reachability, the effect graph and the coroutine transform. Design 249 decided its module tag from a whole-compilation CENSUS of which modules declare each free-function name and counted the ROOT module like any other, so a dependency merely DECLARING `main` — private and unreachable made no difference — gave the entry's `main` a `$M$root` tag, the program emitted no `main` symbol at all, and the failure was a bare `Undefined symbols: \\\"_main\\\", referenced from: <initial-undefines>` naming no user construct. The exemption is the ENTRY module's `main` and only that one, so a dependency's `main` is an ordinary free function with an ordinary `main$m$<module>` symbol and internal linkage. The fixture's dependency `main` SUSPENDS, which is the half a symbol-only rename would have got wrong: the two must name two coroutine frames, and the row measures that the suspension inside the dependency's survives |\\n+| K108 | an `@export`ed function keeps its module-qualified Saw identity; `@export` names the EMITTED symbol, not the compiler's lookup key | `two_modules_export_distinct_c_symbols.saw` + `exported_function_in_a_module_named_root.saw` | SL-274 review r1 finding P1 (codex), fixed in r2. K106's first draft exempted an `@export`ed function from the mangling, reasoning that codegen emits it under the C name anyway — true of the EMITTED name and false of the lookup key, which is what an empty symbol base returns to. `codegen/core.py::_declare_function` has always kept the two apart (`func_name` keys `self.functions`; `llvm_name` is `export_symbol(func)` when there is one), so the exemption bought nothing and cost the key: two modules exporting distinct C names from one Saw name shared the key `value`, the second overwrote the first, the first's body was never emitted, and the `@llvm.used` keep-root then named a declaration with no definition — `Undefined symbols: _first_value`. The rule is that `@export` decides the emitted symbol and the linkage and NOTHING about the compiler's internal identity. Both directions are checked: the Saw calls, which route through the module-qualified identity, and `extern \\\"C\\\"` calls back in, which route through the export names. The second file is the cell where this meets K109 and is the one that ALSO fails before SL-274, because design 249's census tagged both sides `$M$root` — a regression pin in both directions |\\n+| K109 | a module named `root` is not the entry module | `module_named_root_is_not_the_entry_module.saw` + `extension_method_tie_break_distinguishes_the_entry_module.saw` | SL-274 review r1 finding P2 (codex), fixed in r2. `type_identity.module_tag` renders a defining module for a symbol and rendered the entry module's EMPTY path as the word `root`, which is exactly what a user module named `root` renders as — so the rendering was not injective and two declarations took one tag. K106's mangling made that reachable from two lines of source: both `helper`s became `helper$m$root` and codegen died with `internal compiler error: helper$m$root`. The entry module renders as the EMPTY tag now (`helper$m$`), because every real module path renders at least one character, so the empty string is the one tag no module can take; `$` is forbidden in a Saw identifier, so a trailing delimiter cannot collide with a name an author could write, and `ErrorReporter._QUALIFIER_RE` widened from `+` to `*` so a bare `$m$` is still scrubbed out of a diagnostic. THE SECOND FILE IS WHY THE FIX BELONGS IN `module_tag` AND NOT IN THE FREE-FUNCTION FUNNEL: design 142's cross-module extension-method tie-break (`_stamp_overload_symbols`, `$M$<tag>`) spelled `module_tag(())` long before free functions did, and an entry-module extension beside a module-`root` extension of one type with one signature is `internal compiler error: Marker$m$sl274marker_describe$OL$$M$root` on the compiler BEFORE SL-274. Nothing else reached the collision — design 144 exempts the entry module's types outright and `_module_private_symbol` returns None for it — which is why it sat unprobed. Fixing the rendering fixes both sites |\\n \\n ## Visibility and module boundaries\\n \\ndiff --git a/examples/coro_conservative_generic_instance_in_spawned_task.saw b/examples/coro_conservative_generic_instance_in_spawned_task.saw\\nindex 2d76aec5..23498241 100644\\n--- a/examples/coro_conservative_generic_instance_in_spawned_task.saw\\n+++ b/examples/coro_conservative_generic_instance_in_spawned_task.saw\\n@@ -8,9 +8,15 @@\\n // be promoted and framed at both: the ticker's lines land between the worker's,\\n // which only happens if the worker really parked inside it.\\n //\\n+// The frame is named for the INSTANCE, and the instance is named from its\\n+// template's symbol — `yielder$m$$1$Int` since SL-274 gave every free function a\\n+// module tag, the entry module's being the empty one. Naming the instance rather\\n+// than `__Frame_yielder` is the point of the directive: what must be framed is\\n+// the instantiation, not the template.\\n+//\\n // EXPECT: success\\n // EXPECT-IR-ABSENT: __Frame_apply\\n-// EXPECT-IR-CONTAINS: __Frame_yielder$1$Int\\n+// EXPECT-IR-CONTAINS: __Frame_yielder$m$$1$Int\\n // EXPECT-IR-CONTAINS: __Frame_worker\\n // EXPECT-OUTPUT:\\n // plain 20 10\\ndiff --git a/examples/coro_module_tagged_free_callee_discovery.saw b/examples/coro_module_tagged_free_callee_discovery.saw\\nindex a240dda7..6c9e4305 100644\\n--- a/examples/coro_module_tagged_free_callee_discovery.saw\\n+++ b/examples/coro_module_tagged_free_callee_discovery.saw\\n@@ -2,21 +2,23 @@\\n // SAME question whether or not registration stamped a mangled symbol on it, and\\n // whichever rule stamped one.\\n //\\n-// Two modules declaring one free-function name is legal (design 249), and the\\n-// answer is a `$M$<module>` tag on BOTH — on the entry module's private one\\n-// (`$M$root`) and on the dependency's PUBLIC one (`$M$sl280tag`). Neither is a\\n-// `private`-only `$m$` tag, so this reaches the mechanism from the other side:\\n-// what the transform was keying on was the bare NAME, and every rule that\\n+// Two modules declaring one free-function name is legal, and both sides get a\\n+// module tag — the entry module's private one and the dependency's PUBLIC one.\\n+// Neither is a `private`-only tag, so this reaches the mechanism from the other\\n+// side: what the transform was keying on was the bare NAME, and every rule that\\n // renames a free function walks into the same hole. Note the entry-module row —\\n // the bug is not confined to imported bodies, it is confined to callees whose\\n // key and name differ.\\n //\\n-// This is the acceptance criterion for SL-274, which is blocked on this issue.\\n-// SL-274 gives EVERY free function a module-mangled symbol — so after it lands,\\n-// a PUBLIC free function's frame key stops coinciding with its bare name, and\\n-// the whole working half of this bug's matrix would move to the broken half. It\\n-// is safe exactly when the funnel's answer does not depend on the tag, which is\\n-// what the `imported_public` row measures.\\n+// This was written as the acceptance criterion for SL-274, which was blocked on\\n+// this issue. SL-274 has since landed and gives EVERY free function a\\n+// module-mangled symbol (`tagged_helper$m$` here, `tagged_helper$m$sl280tag` in\\n+// the dependency), which RETIRED the design-249 census tag this fixture was\\n+// built on — a name two modules share no longer decides anything, because every\\n+// name is tagged. So a PUBLIC free function's frame key no longer coincides with\\n+// its bare name anywhere, and the whole working half of this bug's matrix would\\n+// have moved to the broken half. It is safe exactly when the funnel's answer\\n+// does not depend on the tag, which is what the `imported_public` row measures.\\n //\\n // Measured before the fix: entry_tagged 0, imported_public 0.\\n //\\n@@ -33,7 +35,8 @@\\n import std.task.{yield_now}\\n import sl280tag.{reach_tagged}\\n \\n-/// Carries `$M$root`, because the dependency declares this name too.\\n+/// Carries a module tag — `tagged_helper$m$`, the entry module's, whose tag is\\n+/// EMPTY so that a module named `root` cannot spell it (SL-274).\\n func tagged_helper(n: Int) -> Int {\\n     yield_now()\\n     n + 1\\ndiff --git a/examples/dependency_named_main_keeps_the_entry_point.saw b/examples/dependency_named_main_keeps_the_entry_point.saw\\nnew file mode 100644\\nindex 00000000..2f411354\\n--- /dev/null\\n+++ b/examples/dependency_named_main_keeps_the_entry_point.saw\\n@@ -0,0 +1,41 @@\\n+// SL-305: a dependency declaring `main` must not move the entry module's.\\n+//\\n+// `main` IS the C entry, emitted under that name and load-bearing at seven\\n+// sites in codegen, reachability, the effect graph and the coroutine transform.\\n+// Design 249's module tag was decided by a whole-compilation CENSUS of which\\n+// modules declare each free-function name, with the root module counted like any\\n+// other, so a dependency merely DECLARING the name — private, unreachable, it\\n+// made no difference — gave the entry's `main` a `$M$root` tag. The program then\\n+// emitted no `main` at all and the LINK failed:\\n+//\\n+//     Linking failed: Undefined symbols for architecture arm64:\\n+//       \\\"_main\\\", referenced from:\\n+//           <initial-undefines>\\n+//\\n+// SL-274 replaced the census with an unconditional per-module tag and ONE\\n+// exemption: the ENTRY module's `main`. A dependency's `main` is an ordinary\\n+// free function (`main$m$sl305dep`, internal linkage, its own coroutine frame),\\n+// so both can exist and neither displaces the other.\\n+//\\n+// EXPECT: success\\n+// COMPILE-FLAGS: --module-path sl305dep={TESTDIR}/modules/sl305dep\\n+// EXPECT-OUTPUT: worker 2\\n+// EXPECT-OUTPUT: dep main 8\\n+\\n+import std.task.{yield_now}\\n+import sl305dep.{reach}\\n+\\n+func worker(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+func main() {\\n+    var group = TaskGroup()\\n+    let h = group.spawn(worker(1))\\n+    print(\\\"worker {h.join()}\\\")\\n+    // Reaches the dependency's own `main` through its public surface. The\\n+    // suspension inside it survives, which is what says the two `main`s name\\n+    // two frames rather than colliding on one.\\n+    print(\\\"dep main {reach(7)}\\\")\\n+}\\ndiff --git a/examples/exported_function_in_a_module_named_root.saw b/examples/exported_function_in_a_module_named_root.saw\\nnew file mode 100644\\nindex 00000000..c3967c05\\n--- /dev/null\\n+++ b/examples/exported_function_in_a_module_named_root.saw\\n@@ -0,0 +1,38 @@\\n+// SL-274 review r1: the two findings COMBINED — an `@export`ed function inside a\\n+// module named `root`, beside an `@export`ed function of the same Saw name in the\\n+// entry module.\\n+//\\n+// Each finding alone is pinned by `two_modules_export_distinct_c_symbols.saw` and\\n+// `module_named_root_is_not_the_entry_module.saw`. This is the cell where they\\n+// meet, and it is the one that also fails on the compiler BEFORE SL-274: design\\n+// 249's census saw `value` declared by two modules and tagged both `$M$root`,\\n+// because its tag came from the same `module_tag` the entry module used. So this\\n+// row is a regression pin in both directions —\\n+//\\n+//     Linking failed: Undefined symbols for architecture arm64:\\n+//       \\\"_root_value\\\", referenced from:\\n+//           _main in pre_comb.o\\n+//\\n+// on main, and `Undefined symbols: _first_value` under SL-274 revision 1.\\n+//\\n+// EXPECT: success\\n+// EXPECT-OUTPUT: saw 11 22\\n+// EXPECT-OUTPUT: c 11 22\\n+\\n+extern \\\"C\\\" {\\n+    func entry_value() -> Int\\n+    func root_value() -> Int\\n+}\\n+\\n+module root {\\n+    @export(\\\"root_value\\\")\\n+    public func value() -> Int { 22 }\\n+}\\n+\\n+@export(\\\"entry_value\\\")\\n+func value() -> Int { 11 }\\n+\\n+func main() {\\n+    print(\\\"saw {value()} {root.value()}\\\")\\n+    print(\\\"c {entry_value()} {root_value()}\\\")\\n+}\\ndiff --git a/examples/extension_method_tie_break_distinguishes_the_entry_module.saw b/examples/extension_method_tie_break_distinguishes_the_entry_module.saw\\nnew file mode 100644\\nindex 00000000..47f7a260\\n--- /dev/null\\n+++ b/examples/extension_method_tie_break_distinguishes_the_entry_module.saw\\n@@ -0,0 +1,42 @@\\n+// SL-274's audit finding: the cross-module extension-method tie-break told the\\n+// entry module apart from a module named `root` by the same broken rendering\\n+// SL-274's review found on free functions — and it PREDATES SL-274.\\n+//\\n+// Design 142: two modules may each extend one type with the same method name and\\n+// the SAME signature. Those are legal declarations only a call site seeing both\\n+// can complain about, so their codegen symbols are discriminated by defining\\n+// module (`_stamp_overload_symbols`, `$M$<tag>`). That site spelled\\n+// `module_tag(())` long before free functions did, and `module_tag` rendered both\\n+// the entry module and a module named `root` as `root`:\\n+//\\n+//     internal compiler error: Marker$m$sl274marker_describe$OL$$M$root\\n+//\\n+// on the compiler before SL-274. Nothing else reached it — design 144 exempts the\\n+// entry module's types outright and `_module_private_symbol` returns None for it —\\n+// which is why the collision sat there unprobed until SL-274 tagged every free\\n+// function and made the same rendering reachable from two lines of source.\\n+//\\n+// Fixing the rendering in `module_tag` fixes both sites, which is why it belongs\\n+// there and not in the free-function funnel.\\n+//\\n+// EXPECT: success\\n+// COMPILE-FLAGS: --module-path sl274marker={TESTDIR}/modules/sl274marker\\n+// EXPECT-OUTPUT: entry 2\\n+\\n+import sl274marker.{Marker}\\n+\\n+module root {\\n+    import sl274marker.{Marker}\\n+    extension Marker {\\n+        public func describe(&self) -> Int { self.n + 100 }\\n+    }\\n+}\\n+\\n+extension Marker {\\n+    func describe(&self) -> Int { self.n + 1 }\\n+}\\n+\\n+func main() {\\n+    let m = Marker(n: 1)\\n+    print(\\\"entry {m.describe()}\\\")\\n+}\\ndiff --git a/examples/free_function_never_shadows_a_runtime_symbol.saw b/examples/free_function_never_shadows_a_runtime_symbol.saw\\nnew file mode 100644\\nindex 00000000..41d9af08\\n--- /dev/null\\n+++ b/examples/free_function_never_shadows_a_runtime_symbol.saw\\n@@ -0,0 +1,72 @@\\n+// SL-274: a free function never captures the libc symbol of the same name.\\n+//\\n+// A free function used to be emitted under the name the author wrote, so\\n+// `func read(...)` at top level BOUND the whole program's `read`. The runtime's\\n+// `__saw_rt_fs_read` seam calls libc's `read(2)`; the linker resolved that call\\n+// to the SAW function instead, which returns whatever it likes, and `File.read`\\n+// then built a `Data` around a byte count no read had produced. There was no\\n+// diagnostic at any stage.\\n+//\\n+// Measured before the fix, with this exact file: `bytes 4242` and exit 0 — a\\n+// `Data` claiming 4242 bytes out of a 16-byte buffer. The A/B was the function's\\n+// NAME and nothing else: renaming it to `read_renamed` printed `bytes 16`.\\n+//\\n+// `read` is one of five names the corpus already collided with (`connect`,\\n+// `rename`, `send`, `dup2` are the others, each an `extern \\\"C\\\"` the runtime\\n+// declares); every libc symbol was exposed, and the worst were the ones the\\n+// runtime uses on its own behalf, where the failure surfaces far from the\\n+// definition and reads like memory corruption.\\n+//\\n+// The fix is by construction, in two halves: every free function is emitted\\n+// under a MODULE-MANGLED symbol (`read$m$` here — the entry module's tag is the\\n+// EMPTY one, which is the spelling no module path can take) and, unless `@export`ed,\\n+// with INTERNAL linkage — so the linker has nothing of ours to bind. `nm` on\\n+// this binary has no `T _read` and never resolves the runtime's call to us.\\n+//\\n+// EXPECT: success\\n+// EXPECT-OUTPUT: bytes 16\\n+// EXPECT-OUTPUT: helper 4242\\n+\\n+import std.file.{File}\\n+import std.path.{Path}\\n+import std.data.{Data}\\n+import std.net.{IoError}\\n+\\n+// The collision. Not a plausible API — a plausible ACCIDENT: `read` is an\\n+// obvious name for a helper, and nothing about writing one says the program has\\n+// just replaced libc's for every caller in it, the runtime included.\\n+func read(a: Int) -> Int { 4242 }\\n+\\n+func sixteen_bytes() -> Data {\\n+    var data = Data()\\n+    var i = 0\\n+    while i < 16 {\\n+        try! data.push(65 as UInt8)\\n+        i = i + 1\\n+    }\\n+    move data\\n+}\\n+\\n+func seed(p: Path) -> Result<Void, IoError> {\\n+    var out = try File.create(p)\\n+    let _ = try out.write(sixteen_bytes())\\n+    return\\n+}\\n+\\n+func main() -> Result<Void, IoError> {\\n+    let p = Path(s: \\\"/tmp/saw_sl274_read_shadow.bin\\\")\\n+    try seed(p)\\n+\\n+    // The true byte count, which is only reachable if `__saw_rt_fs_read` called\\n+    // libc's `read` rather than the one above.\\n+    var f = try File.open(p)\\n+    let d = try f.read(size: 16)\\n+    print(\\\"bytes {d.len()}\\\")\\n+\\n+    // And the Saw function is still perfectly callable, under whatever symbol\\n+    // it was given — the mangling is invisible to the source.\\n+    print(\\\"helper {read(1)}\\\")\\n+\\n+    let _ = File.remove(p)\\n+    return\\n+}\\ndiff --git a/examples/module_named_root_is_not_the_entry_module.saw b/examples/module_named_root_is_not_the_entry_module.saw\\nnew file mode 100644\\nindex 00000000..0d703019\\n--- /dev/null\\n+++ b/examples/module_named_root_is_not_the_entry_module.saw\\n@@ -0,0 +1,33 @@\\n+// SL-274 review r1 finding P2: a module named `root` is not the entry module.\\n+//\\n+// `type_identity.module_tag` renders a defining module for a symbol, and it used\\n+// to render the entry module's EMPTY path as the word `root` — which is exactly\\n+// what a user module named `root` renders as. Two declarations, one tag: both\\n+// `helper`s below became `helper$m$root` and codegen died with\\n+//\\n+//     internal compiler error: helper$m$root\\n+//\\n+// A real module path always renders at least one character, so the EMPTY\\n+// rendering is the one tag no module can take, and that is what the entry module\\n+// gets: `helper$m$` beside `helper$m$root`. `$` is forbidden in a Saw identifier,\\n+// so the trailing delimiter can never collide with a name an author could write.\\n+//\\n+// The values are the oracle — +1 versus +100 — so which of the two helpers ran is\\n+// visible, and `root.reach(1)` reaching 101 says the module's own private helper\\n+// was not displaced by the entry module's.\\n+//\\n+// EXPECT: success\\n+// EXPECT-OUTPUT: entry 2\\n+// EXPECT-OUTPUT: root 101\\n+\\n+module root {\\n+    func helper(n: Int) -> Int { n + 100 }\\n+    public func reach(n: Int) -> Int { helper(n) }\\n+}\\n+\\n+func helper(n: Int) -> Int { n + 1 }\\n+\\n+func main() {\\n+    print(\\\"entry {helper(1)}\\\")\\n+    print(\\\"root {root.reach(1)}\\\")\\n+}\\ndiff --git a/examples/modules/sl274marker/lib.saw b/examples/modules/sl274marker/lib.saw\\nnew file mode 100644\\nindex 00000000..9d00dbbc\\n--- /dev/null\\n+++ b/examples/modules/sl274marker/lib.saw\\n@@ -0,0 +1,7 @@\\n+// Fixture for `extension_method_tie_break_distinguishes_the_entry_module.saw`.\\n+//\\n+// A type two modules can both extend. It has to live in a THIRD module: design\\n+// 142's tie-break only fires when two different modules extend one type with the\\n+// same method name and the same signature, and the entry module extending its own\\n+// type gives the other module nothing to reach.\\n+public struct Marker { n: Int }\\ndiff --git a/examples/modules/sl280tag/lib.saw b/examples/modules/sl280tag/lib.saw\\nindex e9ee401c..1f3cbac0 100644\\n--- a/examples/modules/sl280tag/lib.saw\\n+++ b/examples/modules/sl280tag/lib.saw\\n@@ -1,16 +1,18 @@\\n // Fixture for `coro_module_tagged_free_callee_discovery.saw` (SL-280).\\n //\\n // This module declares a free function whose NAME the entry module also\\n-// declares. Design 249 answers that with a `$M$<module>` codegen tag on BOTH —\\n-// including the PUBLIC one here, and including the entry module's, whose tag is\\n-// `$M$root`. So this fixture puts a mangled symbol on a free function through a\\n-// route that has nothing to do with `private`, which is what makes it the\\n-// tag-independence probe: the funnel must not care WHY a symbol is there, or\\n-// whether there is one at all.\\n+// declares. Design 249 answered that with a `$M$<module>` codegen tag on both\\n+// sides, which is the route this fixture was built on; SL-274 retired the census\\n+// and tags EVERY free function, so the PUBLIC one here is\\n+// `tagged_helper$m$sl280tag` and the entry module's is `tagged_helper$m$`. Either\\n+// way the fixture puts a mangled symbol on a free function through a route that\\n+// has nothing to do with `private`, which is what makes it the tag-independence\\n+// probe: the funnel must not care WHY a symbol is there, or whether there is one\\n+// at all.\\n \\n import std.task.{yield_now}\\n \\n-/// PUBLIC, and tagged `tagged_helper$M$sl280tag` all the same. Returns n + 100,\\n+/// PUBLIC, and tagged `tagged_helper$m$sl280tag` all the same. Returns n + 100,\\n /// so a caller that reaches the ENTRY module's same-named helper by mistake is\\n /// visible in the VALUE and not only in the step count.\\n public func tagged_helper(n: Int) -> Int {\\ndiff --git a/examples/modules/sl305dep/lib.saw b/examples/modules/sl305dep/lib.saw\\nnew file mode 100644\\nindex 00000000..d52b703b\\n--- /dev/null\\n+++ b/examples/modules/sl305dep/lib.saw\\n@@ -0,0 +1,31 @@\\n+// Fixture for `dependency_named_main_keeps_the_entry_point.saw` (SL-305).\\n+//\\n+// A dependency that declares the name `main`. Nothing here is unusual: `main`\\n+// is an ordinary identifier to every module but the entry one, and a library\\n+// that grew a `main` helper (a state-machine driver, a REPL loop) has no reason\\n+// to know the entry module is about to lose its C entry point over it.\\n+//\\n+// Design 249 took its module tag from a CENSUS of which modules declare each\\n+// free-function name, and counted the root module like any other — so this\\n+// declaration gave the ENTRY module's `main` a `$M$root` tag, the program\\n+// emitted no `main` symbol at all, and the link failed with\\n+// `Undefined symbols: \\\"_main\\\", referenced from: <initial-undefines>`: a bare\\n+// linker error naming no user construct. SL-274 replaced the census with a\\n+// per-module tag plus ONE exemption — the ENTRY module's `main`, and only that\\n+// one — so this `main` is an ordinary free function with an ordinary\\n+// `main$m$sl305dep` symbol and internal linkage.\\n+//\\n+// It SUSPENDS, so its coroutine frame is keyed by that symbol too: the entry\\n+// module's `main` and this one must name two different frames, which is the\\n+// half a symbol-only rename would have got wrong.\\n+\\n+import std.task.{yield_now}\\n+\\n+func main(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+/// The only public surface: nothing outside this module can even name `main`\\n+/// here, which is why the collision was invisible at the source level.\\n+public func reach(n: Int) -> Int { main(n) }\\ndiff --git a/examples/two_modules_export_distinct_c_symbols.saw b/examples/two_modules_export_distinct_c_symbols.saw\\nnew file mode 100644\\nindex 00000000..bfb477ac\\n--- /dev/null\\n+++ b/examples/two_modules_export_distinct_c_symbols.saw\\n@@ -0,0 +1,47 @@\\n+// SL-274 review r1 finding P1: two modules may export DISTINCT C names from one\\n+// Saw name.\\n+//\\n+// `@export` decides the EMITTED C symbol and the linkage. It does NOT decide the\\n+// compiler's internal identity — `_declare_function` has always kept the two\\n+// apart, `func_name` being the `self.functions` lookup key and `llvm_name` the\\n+// requested C symbol — so a declaration keeps its `<name>$m$<module>` identity\\n+// everywhere the compiler names it and is emitted under the C name.\\n+//\\n+// The first draft of SL-274 exempted an `@export`ed function from the mangling on\\n+// the reasoning that its LLVM name was the export name anyway. That was wrong\\n+// about which of the two the exemption reached: it returned an EMPTY symbol base,\\n+// so both `value`s here shared the lookup key `value`, the second overwrote the\\n+// first, and the first's body was never emitted. The `@llvm.used` keep-root then\\n+// named a declaration with no definition:\\n+//\\n+//     Linking failed: Undefined symbols for architecture arm64:\\n+//       \\\"_first_value\\\", referenced from:\\n+//           <initial-undefines>\\n+//\\n+// Both halves are checked here: the SAW calls (which go through the module-\\n+// qualified identity) and the C calls back in through `extern \\\"C\\\"` (which go\\n+// through the export names). The two must agree about which body is which.\\n+//\\n+// EXPECT: success\\n+// EXPECT-OUTPUT: saw 11 22\\n+// EXPECT-OUTPUT: c 11 22\\n+\\n+extern \\\"C\\\" {\\n+    func first_value() -> Int\\n+    func second_value() -> Int\\n+}\\n+\\n+module first {\\n+    @export(\\\"first_value\\\")\\n+    public func value() -> Int { 11 }\\n+}\\n+\\n+module second {\\n+    @export(\\\"second_value\\\")\\n+    public func value() -> Int { 22 }\\n+}\\n+\\n+func main() {\\n+    print(\\\"saw {first.value()} {second.value()}\\\")\\n+    print(\\\"c {first_value()} {second_value()}\\\")\\n+}\\ndiff --git a/sawc/codegen/calls.py b/sawc/codegen/calls.py\\nindex c108218c..aae0e577 100644\\n--- a/sawc/codegen/calls.py\\n+++ b/sawc/codegen/calls.py\\n@@ -1499,14 +1499,17 @@ class CallsMixin:\\n         string-runtime helpers are emitted before the executor's sink exists.\\n         Sharing a routine across that boundary would let whichever site came\\n         first decide for the rest, which is a behavior change, not a size win.\\n+\\n+        Which of the two it is comes from `_panic_sink_is_bt`, the IDENTITY\\n+        question, never from the emitted symbol of the sink it hands back — see\\n+        that method (SL-274).\\n         \\\"\\\"\\\"\\n         cache = getattr(self, \\\"_panic_helpers\\\", None)\\n         if cache is None:\\n             cache = {}\\n             self._panic_helpers = cache\\n \\n-        sink = self._panic_sink()\\n-        sink_tag = \\\"bt\\\" if sink.name == \\\"__saw_bt_panic\\\" else \\\"rt\\\"\\n+        sink_tag = \\\"bt\\\" if self._panic_sink_is_bt() else \\\"rt\\\"\\n         key = f\\\"{sink_tag}${'_'.join(self._step_key(s) for s in steps)}\\\"\\n         helper = cache.get(key)\\n         if helper is not None:\\ndiff --git a/sawc/codegen/core.py b/sawc/codegen/core.py\\nindex 2da45479..029df167 100644\\n--- a/sawc/codegen/core.py\\n+++ b/sawc/codegen/core.py\\n@@ -1914,6 +1914,12 @@ class CodeGenerator(ResultsMixin, MatchMixin, StructsMixin, CollectionsMixin, Ca\\n     # every downstream kernel to be placed at all.\\n     BT_TABLE_SYMBOL = \\\"__saw_bt_table\\\"\\n \\n+    # std/taskgroup.saw's executor-aware panic sink, looked up in\\n+    # `self.functions` (which is keyed by a declaration's codegen symbol, and a\\n+    # std free function keeps the name the author wrote — see\\n+    # `_free_function_symbol_base`'s std branch).\\n+    BT_PANIC_SINK = \\\"__saw_bt_panic\\\"\\n+\\n     def _panic_sink(self):\\n         \\\"\\\"\\\"The function a panic site calls (design 158).\\n \\n@@ -1928,11 +1934,27 @@ class CodeGenerator(ResultsMixin, MatchMixin, StructsMixin, CollectionsMixin, Ca\\n         freestanding profiles that exclude std.taskgroup, and the string-runtime\\n         helpers emitted before any Saw function is declared.\\n         \\\"\\\"\\\"\\n-        sink = self.functions.get(\\\"__saw_bt_panic\\\")\\n+        sink = self.functions.get(self.BT_PANIC_SINK)\\n         if sink is not None:\\n             return sink\\n         return self.functions[\\\"__saw_rt_panic\\\"]\\n \\n+    def _panic_sink_is_bt(self) -> bool:\\n+        \\\"\\\"\\\"Whether `_panic_sink` answers the executor's sink rather than the raw\\n+        seam — asked the SAME way `_panic_sink` asks it, so the two can never\\n+        disagree (SL-274).\\n+\\n+        `_panic_helper` keys its outlined assembly routine on this, and used to\\n+        decide it by comparing `sink.name` against the literal\\n+        `\\\"__saw_bt_panic\\\"` — the EMITTED LLVM symbol of a Saw free function\\n+        (`std/taskgroup.saw`). SL-274 mangles free-function symbols, and while\\n+        std's own keep the names the author wrote, a name comparison there is a\\n+        latent merge of two families the `_panic_helper` docstring says are\\n+        deliberately separate: any rename would have silently routed every\\n+        executor-sink site into the `\\\"rt\\\"` family, and the first site to arrive\\n+        would have decided the panic behaviour of the rest. Ask the identity.\\\"\\\"\\\"\\n+        return self.functions.get(self.BT_PANIC_SINK) is not None\\n+\\n     def _emit_bt_table(self, program):\\n         \\\"\\\"\\\"Emit this program's logical-backtrace table as one read-only global.\\n \\n@@ -2581,16 +2603,31 @@ class CodeGenerator(ResultsMixin, MatchMixin, StructsMixin, CollectionsMixin, Ca\\n             # clang on the HOST (mach-O rejects that ELF section spelling), and\\n             # -O1 globaldce already strips the unreferenced internal defs.\\n             self._apply_section_layout(place_sections=False)\\n-        elif not self._is_apple_triple():\\n-            # design 168 unit 1 (DF-164b): a HOSTED ELF link gets the same\\n+        else:\\n+            # SL-274: the HOSTED profile internalizes on exactly the terms the\\n+            # other two already did — `@export` is the only way out, so no\\n+            # definition this module did not publish can be bound from outside\\n+            # it. That is the linkage half of the ruling whose mangling half is\\n+            # `typechecker/registration.py::_free_function_symbol_base`; it is\\n+            # what makes a user `func read(...)` unable to capture the `read`\\n+            # the runtime's own seam calls, whatever the symbol is spelled.\\n+            #\\n+            # Gated on `_strip_unreachable`, which is `sawc.py`'s\\n+            # `whole_program`: an executable link or an object that already\\n+            # internalizes everything but its `@export`s. A plain hosted `-c`\\n+            # object is SOMEBODY ELSE'S to link, so it keeps external linkage.\\n+            #\\n+            # Sections stay design 168 unit 1 (DF-164b): a HOSTED ELF link gets\\n             # per-symbol sections so `ld --gc-sections` (added to the clang link\\n-            # line in sawc.py) can drop what nothing reaches. Sections ONLY — a\\n-            # hosted object keeps external linkage, because the runtime objects\\n-            # linked beside it and any `@export`ed entry point are resolved by\\n-            # the linker, not by this module's own reference graph. Mach-O needs\\n-            # none of this: `ld64 -dead_strip` works at symbol granularity and\\n-            # rejects the ELF section spelling, so apple triples skip it.\\n-            self._apply_section_layout(place_sections=True, internalize=False)\\n+            # line in sawc.py) can drop what nothing reaches. Mach-O needs none\\n+            # of it — `ld64 -dead_strip` works at symbol granularity and rejects\\n+            # the ELF section spelling — so apple triples take the linkage half\\n+            # alone.\\n+            place = not self._is_apple_triple()\\n+            if place or self._strip_unreachable:\\n+                self._apply_section_layout(\\n+                    place_sections=place,\\n+                    internalize=self._strip_unreachable)\\n \\n         # design 246 Unit B: nothing may leave here with a body still waiting.\\n         # A monomorphization reached during BODY generation registers types too,\\n@@ -2601,15 +2638,26 @@ class CodeGenerator(ResultsMixin, MatchMixin, StructsMixin, CollectionsMixin, Ca\\n \\n     def _apply_section_layout(self, place_sections: bool = True,\\n                               internalize: bool = True):\\n-        \\\"\\\"\\\"Prepare the module for dead-code-free linking (design 112, 168).\\n-\\n-        `place_sections=False` (design 113b runtime-build): internalize only, with\\n-        NO per-symbol section assignment — the object is host-linked by clang and\\n-        the mach-O host rejects the ELF `.text.<name>` spelling.\\n-\\n-        `internalize=False` (design 168 hosted ELF): section the symbols but leave\\n-        linkage alone, so `--gc-sections` has per-symbol granularity to work at\\n-        while the linker still resolves cross-object references.\\n+        \\\"\\\"\\\"Prepare the module for dead-code-free linking (design 112, 168) and\\n+        decide every definition's LINKAGE (SL-274).\\n+\\n+        THE ONE PLACE a definition's linkage is decided (obligation 1 — a funnel\\n+        names its entries). ENTRY POINTS, all three in `generate`'s tail:\\n+\\n+          * `--freestanding`: sections + internalize (design 112).\\n+          * `--runtime-build`: internalize only, `place_sections=False` — the\\n+            object is host-linked by clang and the mach-O host rejects the ELF\\n+            `.text.<name>` spelling (design 113b).\\n+          * HOSTED: internalize when this compile owns the whole program\\n+            (`_strip_unreachable`), plus sections on ELF (design 168 unit 1).\\n+            A plain hosted `-c` object is somebody else's to link and keeps\\n+            external linkage, so it passes `internalize=False`.\\n+\\n+        The keep-set is the same in every one of them: `@export`ed globals plus\\n+        the C `main`. That is what SL-274's ruling means by \\\"collisions become\\n+        impossible by construction\\\" — a Saw free function nothing published can\\n+        no longer be bound by the linker at all, so it cannot capture the libc\\n+        symbol of the same name for the runtime seam that calls it.\\n \\n         Codegen emits EVERY loaded stdlib method (and its closure/vtable\\n         descriptor globals + backend constant pools) regardless of reachability,\\n@@ -2630,7 +2678,11 @@ class CodeGenerator(ResultsMixin, MatchMixin, StructsMixin, CollectionsMixin, Ca\\n            globaldce does not run). Only definitions without an explicit\\n            `@section` are placed; declarations and `llvm.*` anchors are left alone.\\n \\n-        Guarded by `freestanding`, so hosted builds are byte-identical.\\n+        Note what (1) settles for `@section` WITHOUT `@export`: the attribute\\n+        promises PLACEMENT and nothing else (LANGUAGE_SPEC reserves external\\n+        linkage for `@export`), and a `@section`'d function is not in the\\n+        keep-set, so it internalizes like any other. Freestanding always read\\n+        that way; the hosted profile agrees since SL-274.\\n         \\\"\\\"\\\"\\n         # Keep-roots: the exported functions/statics (already anchored in\\n         # `@llvm.used`) plus the C `main` if present. Everything else internalizes.\\ndiff --git a/sawc/coro_transform.py b/sawc/coro_transform.py\\nindex f548de0c..2a90ce6e 100644\\n--- a/sawc/coro_transform.py\\n+++ b/sawc/coro_transform.py\\n@@ -10819,7 +10819,15 @@ def _promote_nested_generic_calls(program, funcs_by_name, seed_names, typechecke\\n         args = fc.type_args\\n         if resolve is not None:\\n             args = [resolve(a) for a in args]\\n-        mangled = mangle_function(fc.name, args)\\n+        # SL-274: the TEMPLATE's base, through the one funnel — the same choice\\n+        # `monomorphize._function_template_name` makes, so the instance this\\n+        # walk spells is the instance the registry built. Reading `fc.name` here\\n+        # was SL-280's mechanism at a third site: every free function carries a\\n+        # module tag now, so a generic callee's base and its written name differ\\n+        # and `mangle_function(fc.name, ...)` named an instance nobody had — the\\n+        # promotion silently declined and the author got ``cannot suspend in\\n+        # `sync func` method: `__Frame_s6_entry.resume` calls `s6_helper$1$Int` ``.\\n+        mangled = mangle_function(callee_frame_key(fc) or fc.name, args)\\n         if not instantiation_suspends(mangled):\\n             return None\\n         # Adopt phase 2's instance (idempotent by presence in the entry AST).\\n@@ -10835,6 +10843,12 @@ def _promote_nested_generic_calls(program, funcs_by_name, seed_names, typechecke\\n             program.functions.append(clone)\\n             funcs_by_name[mangled] = clone\\n         fc.name = mangled\\n+        # SL-274, on the drive-site rewrite's own terms (see\\n+        # `typechecker/expressions.py`'s `inner.resolved_symbol = None`): the\\n+        # call NAMES the instantiation now, so the template's symbol is stale\\n+        # data, and `callee_frame_key(fc)` — which `_classify_call` asks — must\\n+        # answer the instance rather than the base it was cloned from.\\n+        fc.resolved_symbol = None\\n         fc.type_args = None\\n         return mangled\\n \\ndiff --git a/sawc/errors.py b/sawc/errors.py\\nindex ddc31d6c..87053871 100644\\n--- a/sawc/errors.py\\n+++ b/sawc/errors.py\\n@@ -146,7 +146,11 @@ class ErrorReporter:\\n     # this, and rendering it for a human is always the short name. Scrubbing\\n     # here rather than at each message site makes the rule total: it holds for\\n     # every diagnostic in the compiler, including ones not yet written.\\n-    _QUALIFIER_RE = re.compile(r\\\"\\\\$m\\\\$[A-Za-z0-9_]+\\\")\\n+    # `*`, not `+`: the ENTRY module renders as the EMPTY tag (SL-274 — see\\n+    # `type_identity.module_tag`), so `helper$m$` is a qualified symbol and is\\n+    # scrubbed like any other. `$` cannot occur in a source identifier, so the\\n+    # match can never eat part of a name an author wrote.\\n+    _QUALIFIER_RE = re.compile(r\\\"\\\\$m\\\\$[A-Za-z0-9_]*\\\")\\n \\n     @classmethod\\n     def humanize(cls, text: Optional[str]) -> Optional[str]:\\ndiff --git a/sawc/frame_keys.py b/sawc/frame_keys.py\\nindex b4021854..17c5cccd 100644\\n--- a/sawc/frame_keys.py\\n+++ b/sawc/frame_keys.py\\n@@ -14,10 +14,11 @@ cooperative executor's own thread — the whole process wedged on one idle socke\\n The answer here is INDEPENDENT of whether a free function happens to carry a\\n tag. A declaration, a call site and a resolved symbol all name the same frame,\\n so they all produce the same string, and a program where the tag is present\\n-behaves exactly as one where it is absent. That independence is the point:\\n-SL-274 will give EVERY free function a module-mangled symbol, and today's\\n-agreement between the two answers for public functions is a coincidence that\\n-change would end.\\n+behaves exactly as one where it is absent. That independence is the point, and\\n+SL-274 collected on it: EVERY free function outside std now carries a\\n+`$m$<module>` tag, so the agreement between a public function's bare name and\\n+its key — a coincidence before — is gone, and every site that read a written\\n+name had to be one of the entries below.\\n \\n ENTRY POINTS (obligation 1 — a funnel names its entries):\\n \\n@@ -41,6 +42,13 @@ ENTRY POINTS (obligation 1 — a funnel names its entries):\\n     module-qualified free call, which wears a `MethodCall`'s shape.\\n   * `coro_transform.py::_promote_nested_generic_methods` — the free-fn descent\\n     of the method-instantiation walk (`funcs_by_name` again).\\n+  * `coro_transform.py::_promote_nested_generic_calls` — the TEMPLATE base a\\n+    nested generic call's instantiation is spelled from\\n+    (`mangle_function(<key>, args)`), so the instance this walk names is the\\n+    instance `monomorphize._function_template_name` demanded; and the CLEAR of\\n+    the stale `resolved_symbol` once the call has been rewritten to name that\\n+    instance. Added by SL-274, which is what made a generic callee's base and\\n+    its written name differ for every module.\\n   * `coro_transform.py::_FrameBuilder._is_suspending_expr` — the ANF /\\n     expression-position hoist's suspension test.\\n   * `coro_transform.py::_FrameBuilder._spans_suspension` — the CFG-split test.\\ndiff --git a/sawc/sawc.py b/sawc/sawc.py\\nindex 46ea8067..2a074238 100755\\n--- a/sawc/sawc.py\\n+++ b/sawc/sawc.py\\n@@ -441,50 +441,17 @@ def report_import_cycle(reporter, cycle, module_map):\\n         source_file=getattr(first_ast, 'source_path', None))\\n \\n \\n-def _census_module_functions(owners, module_path, program):\\n-    \\\"\\\"\\\"Record `program`'s top-level free functions as declared by `module_path`,\\n-    recursing into its INLINE module declarations (design 249).\\n-\\n-    Only free functions: methods live on a type, and an extern block declares a\\n-    C symbol whose name is the ABI, neither of which the module-tagging rule\\n-    touches.\\\"\\\"\\\"\\n-    for func in getattr(program, 'functions', []) or []:\\n-        owners.setdefault(func.name, set()).add(tuple(module_path))\\n-    for mod_decl in getattr(program, 'module_decls', []) or []:\\n-        if getattr(mod_decl, 'is_inline', False) and mod_decl.body is not None:\\n-            _census_module_functions(\\n-                owners, tuple(module_path) + (mod_decl.name,), mod_decl.body)\\n-\\n-\\n-def free_function_owner_census(module_map, entry_ast=None, builtin_ns=None):\\n-    \\\"\\\"\\\"`{free function name: {declaring module path, ...}}` over a whole\\n-    compilation (design 249).\\n-\\n-    A name more than one module declares needs a per-module codegen symbol, and\\n-    that decision must not depend on which module is checked first — so it is\\n-    taken here, over the PARSED module set, before any of them is registered.\\n-\\n-    `builtin_ns` folds in std's and builtin.saw's own free functions, each under\\n-    its design-82 file-module: std is compiled into the program too, so a user\\n-    declaration sharing a std name is exactly the case that needs two symbols.\\n-    Std's side never moves (it is checked once and cached across compiles), so\\n-    the user's is the one that takes a tag.\\\"\\\"\\\"\\n-    owners = {}\\n-    for mod_path, mod_ast in (module_map or {}).items():\\n-        _census_module_functions(owners, mod_path, mod_ast)\\n-    if entry_ast is not None:\\n-        _census_module_functions(owners, (), entry_ast)\\n-    if builtin_ns is not None:\\n-        for name, syms in builtin_ns.function_overloads.items():\\n-            for sym in syms:\\n-                owners.setdefault(name, set()).add(\\n-                    tuple(getattr(sym, 'def_module', ()) or ()))\\n-    return owners\\n-\\n-\\n def std_free_function_owner_census(builtin_ast):\\n-    \\\"\\\"\\\"The same census over the merged builtin+std AST, whose modules are the\\n-    per-FILE identities design 82 gives each std file (design 249).\\\"\\\"\\\"\\n+    \\\"\\\"\\\"`{free function name: {declaring std file-module, ...}}` over the merged\\n+    builtin+std AST, whose modules are the per-FILE identities design 82 gives\\n+    each std file (design 249).\\n+\\n+    STD ONLY, since SL-274. Every other module's free functions are\\n+    module-tagged unconditionally (`_free_function_symbol_base`), so nothing\\n+    outside std needs a census to know whether a name is shared. std keeps this\\n+    one because its free functions keep the names the author wrote: codegen\\n+    emits calls to a dozen of them by literal string, so the tag can only go on\\n+    a name two std FILES actually share (`json.encode` beside `cbor.encode`).\\\"\\\"\\\"\\n     from type_identity import std_leaf\\n     owners = {}\\n     for func in getattr(builtin_ast, 'functions', []) or []:\\n@@ -1701,12 +1668,11 @@ def _prepare_codegen(source_path: str, entry_ast, entry_source: str, verbose: bo\\n         builtin_ns, '_std_pristine_generic_struct_methods', {})\\n     typechecker._std_module_scope_by_file = getattr(\\n         builtin_ns, '_std_module_scope_by_file', {})\\n-    # design 249: which MODULES declare each free-function name, taken over the\\n-    # parsed module set (entry included) before the first module is checked, so\\n-    # a name two modules own gets a per-module codegen symbol whichever order\\n-    # they are checked in.\\n-    typechecker.free_function_owners = free_function_owner_census(\\n-        module_map, entry_ast, builtin_ns)\\n+    # SL-274 retired design 249's whole-compilation census here: every free\\n+    # function OUTSIDE std is module-tagged unconditionally, so a name two\\n+    # modules own is disambiguated by construction and the decision no longer\\n+    # depends on which module is checked first. std keeps its own census, taken\\n+    # in `build_builtin_namespace` over the std file-modules.\\n \\n     # The builtin namespace was built once by build_builtin_namespace(); all its\\n     # symbols are already type-checked and marked directly accessible.\\ndiff --git a/sawc/type_identity.py b/sawc/type_identity.py\\nindex 655416a4..a0c87ed3 100644\\n--- a/sawc/type_identity.py\\n+++ b/sawc/type_identity.py\\n@@ -128,11 +128,35 @@ QUALIFIER = \\\"$m$\\\"\\n \\n def module_tag(module: Tuple[str, ...]) -> str:\\n     \\\"\\\"\\\"A defining module rendered for an LLVM symbol name: identifier-safe,\\n-    stable, and distinct per module (`(\\\"<std>\\\", \\\"data\\\")` -> `std_data`).\\\"\\\"\\\"\\n+    stable, and distinct per module (`(\\\"<std>\\\", \\\"data\\\")` -> `std_data`).\\n+\\n+    THE ENTRY MODULE RENDERS EMPTY, and that is what makes the rendering\\n+    injective (SL-274 review r1 finding P2). Every non-empty path renders at\\n+    least one character, so the empty string is the one tag no module can take —\\n+    while the word `root`, which this used to return for the entry module, is\\n+    exactly what a user module named `root` renders as. Until SL-274 nothing\\n+    reached the collision: design 144 exempts the entry module's types\\n+    (`qualifies` is False for an empty path) and `_module_private_symbol`\\n+    returns None for one, so `module_tag(())` was only ever spelled by\\n+    `_stamp_overload_symbols`' cross-module extension-method disambiguator —\\n+    where the collision was already possible and unprobed. SL-274 tags every\\n+    free function, which made it reachable from two lines of source:\\n+\\n+        module root { func helper(n: Int) -> Int { n + 100 } }\\n+        func helper(n: Int) -> Int { n + 1 }\\n+\\n+    Both became `helper$m$root` and codegen died with `internal compiler error:\\n+    helper$m$root`. `helper$m$` and `helper$m$root` are two symbols.\\n+\\n+    Consequences of the empty rendering, both deliberate: a tag is `[A-Za-z0-9_]*`\\n+    rather than `+`, which `ErrorReporter._QUALIFIER_RE` matches so a bare `$m$`\\n+    is scrubbed out of a diagnostic like any other qualifier; and `$` is\\n+    forbidden in a Saw identifier, so a trailing delimiter can never collide\\n+    with a name an author could write.\\\"\\\"\\\"\\n     parts = [p for p in module if p != \\\"<std>\\\"]\\n     if module[:1] == (\\\"<std>\\\",):\\n         parts = [\\\"std\\\"] + parts\\n-    raw = \\\"_\\\".join(parts) if parts else \\\"root\\\"\\n+    raw = \\\"_\\\".join(parts)\\n     return \\\"\\\".join(c if (c.isalnum() or c == \\\"_\\\") else \\\"_\\\" for c in raw)\\n \\n \\ndiff --git a/sawc/typechecker/core.py b/sawc/typechecker/core.py\\nindex afa72e3d..233c8495 100644\\n--- a/sawc/typechecker/core.py\\n+++ b/sawc/typechecker/core.py\\n@@ -4762,11 +4762,6 @@ class TypeChecker(ExpressionsMixin, StatementsMixin, RegistrationMixin, TypeUtil\\n         # module now that all its signatures are registered.\\n         self._stamp_overload_symbols()\\n \\n-        # DF-140f: give this module's PRIVATE free functions a module-local\\n-        # codegen symbol, so a same-named private function in another module is\\n-        # a different definition rather than an \\\"ambiguous function\\\" report.\\n-        self._stamp_module_private_functions()\\n-\\n         # design 58: validate @export / @section on this module's functions and\\n         # statics. The export-symbol table accumulates across every module the\\n         # shared checker visits, so a duplicate exported C symbol across modules\\ndiff --git a/sawc/typechecker/registration.py b/sawc/typechecker/registration.py\\nindex dcba6270..b5fa1ee5 100644\\n--- a/sawc/typechecker/registration.py\\n+++ b/sawc/typechecker/registration.py\\n@@ -1154,7 +1154,7 @@ class RegistrationMixin:\\n \\n         visibility = getattr(func, 'visibility', Visibility.PRIVATE)\\n         symbol_base = self._free_function_symbol_base(\\n-            func.name, def_module, visibility)\\n+            func.name, def_module, visibility, func)\\n         symbol = FunctionSymbol(\\n             param_types=param_types,\\n             param_names=param_names,\\n@@ -1317,79 +1317,139 @@ class RegistrationMixin:\\n     # ------------------------------------------------------------------ #\\n     def _module_private_symbol(self, base: str, def_module: Tuple[str, ...],\\n                                visibility: Visibility) -> Optional[str]:\\n-        \\\"\\\"\\\"The module-qualified codegen symbol for a private declaration, or\\n-        None when the declaration keeps its plain name (public — importable by\\n+        \\\"\\\"\\\"The module-qualified codegen symbol for a private STATIC, or None\\n+        when the declaration keeps its plain name (public — importable by\\n         simple name, so a genuine cross-module clash is a real ambiguity — or\\n-        root-module, where there is nothing to distinguish it from).\\\"\\\"\\\"\\n+        root-module, where there is nothing to distinguish it from).\\n+\\n+        Statics only, since SL-274 gave free functions their own rule (every\\n+        one of them is module-tagged, see `_free_function_symbol_base`); the\\n+        one caller is `_register_static`.\\\"\\\"\\\"\\n         if visibility != Visibility.PRIVATE or not def_module:\\n             return None\\n         return f\\\"{base}$m${self._module_symbol_tag(def_module)}\\\"\\n \\n     def _free_function_symbol_base(self, name: str,\\n                                    def_module: Tuple[str, ...],\\n-                                   visibility: Visibility) -> str:\\n-        \\\"\\\"\\\"Design 249: the module-tagged codegen base for a free function whose\\n-        name MORE THAN ONE module of this compilation declares, or \\\"\\\" when the\\n-        plain name is unambiguous.\\n-\\n-        Two modules owning one free-function name is legal since design 249, so\\n-        the two definitions need two LLVM symbols. The decision is a pure\\n-        function of `free_function_owners` — the (name -> declaring modules)\\n-        census the driver takes over the parsed module set BEFORE any module is\\n-        checked — so it never depends on module order and never renames a\\n-        symbol whose bodies are already checked.\\n-\\n-        A PRIVATE declaration of a NON-ROOT module is left to DF-140f's `$m$`\\n-        tag, which already makes it module-local. The root module takes a tag\\n-        like any other (`module_tag(())` is `root`) — std is checked once and\\n-        CACHED across compiles, so its symbols cannot depend on the program\\n-        being compiled, which makes the entry the side that moves when a user\\n-        declaration meets a std one.\\n+                                   visibility: Visibility,\\n+                                   func=None) -> str:\\n+        \\\"\\\"\\\"THE codegen base symbol of a free function, or \\\"\\\" for the plain name.\\n+\\n+        SL-274: a free function used to be emitted under the name the author\\n+        wrote, so `func read(...)` at top level BOUND the whole program's `read`\\n+        — libc's included, and the runtime's `__saw_rt_fs_read` seam calls\\n+        libc's. The failure was a segfault or a `Data` claiming 4242 bytes out\\n+        of a 16-byte buffer, with nothing said at any stage. The ruling (user,\\n+        Sep 14) is that collisions become impossible BY CONSTRUCTION: every\\n+        free function is module-mangled AND internal unless `@export`ed. This\\n+        is the mangling half; `codegen/core.py::_apply_section_layout` is the\\n+        linkage half.\\n+\\n+        ENTRY POINTS (obligation 1 — a funnel names its entries):\\n+          * `_register_function` — the only caller. What it returns lands on\\n+            the `FunctionSymbol.symbol_base`, on its `mangled_name`, and on the\\n+            declaration's `mangled_symbol`, so declaration and call resolution\\n+            read one answer.\\n+          * `_stamp_overload_symbols` builds an overload member's `$OL$` symbol\\n+            and a 2+-generic set's template base ON TOP of `symbol_base`, so a\\n+            decorated member is `<name>$m$<module>$OL$<sig>` and a generic's\\n+            instantiations are `<name>$m$<module>$<args>`. Nothing else in the\\n+            compiler decides a free function's symbol.\\n+\\n+        The base is `<name>$m$<module tag>`, and the ENTRY module's own\\n+        functions are tagged too, which is what fixes the `func read` beside\\n+        `File.read` case. `$m$` is DF-140f's delimiter and design 144 shares it\\n+        for type identities, so the schemes still agree on how a module is\\n+        spelled in a symbol; on a free function it now means \\\"the module that\\n+        DEFINES it\\\" rather than \\\"private\\\", because every free function carries\\n+        one. This RETIRES design 249's separate `$M$` census tag: a per-module\\n+        tag disambiguates two modules declaring one name by construction, with\\n+        no whole-compilation census to take and no dependence on which module is\\n+        checked first.\\n+\\n+        THE ENTRY MODULE IS THE EMPTY TAG, `<name>$m$` (SL-274 review r1\\n+        finding P2). `module_tag` used to render the entry module's empty path\\n+        as the word `root`, which a user module named `root` renders as too — so\\n+        `module root { func helper() }` beside an entry-module `func helper()`\\n+        produced `helper$m$root` twice and codegen died with `internal compiler\\n+        error: helper$m$root`. A real module path always renders at least one\\n+        character, so the EMPTY rendering is the one spelling no module can\\n+        take; the fix is in `type_identity.module_tag`, which is where every\\n+        `$m$` tag in the compiler is spelled, so the free-function tags of this\\n+        funnel and design 144's type identities cannot disagree about which\\n+        module is which. It also closes the same collision at\\n+        `_stamp_overload_symbols`' cross-module extension-method\\n+        disambiguator, where it predates SL-274.\\n+\\n+        AN `@export`ed FUNCTION IS TAGGED LIKE ANY OTHER (SL-274 review r1\\n+        finding P1). `@export` decides the EMITTED C symbol and the linkage, not\\n+        the compiler's internal identity: `codegen/core.py::_declare_function`\\n+        already separates the two (`func_name`, the `self.functions` key, from\\n+        `llvm_name`, which is `export_symbol(func)` when there is one), so a\\n+        function keeps `<name>$m$<module>` everywhere the compiler names it and\\n+        is EMITTED under the requested C name. Exempting it here returned an\\n+        empty base, so two modules exporting distinct C names from one Saw name\\n+        shared one lookup key, the second overwrote the first, and the first's\\n+        body was never emitted — `@export(\\\"first_value\\\")` beside\\n+        `@export(\\\"second_value\\\")` failed to link with `Undefined symbols:\\n+        _first_value`.\\n+\\n+        THREE declarations keep the plain name, each because something reads the\\n+        written name as the symbol:\\n+\\n+          * the ENTRY module's `main`. It IS the C entry, emitted by that name\\n+            and load-bearing at `codegen/core.py:3526`/`:3539`/`:3420`/`:2638`,\\n+            `codegen/reachability.py:107`, `typechecker/effects.py:1013` and the\\n+            coroutine transform's seeds. A DEPENDENCY's `main` is an ordinary\\n+            free function and takes an ordinary tag — that is SL-305, where a\\n+            dependency merely DECLARING the name gave the entry's `main` a tag\\n+            and the link failed with `Undefined symbols: _main`.\\n+          * a COMPILER-SYNTHESIZED function (`is_synthesized`) or a\\n+            MONOMORPHIZED INSTANCE (`is_mono_instance`) — a coroutine frame's\\n+            driver, an entry executor, a frame release, a generic\\n+            instantiation's clone. Each is named by STRING where it is built\\n+            and at every reference to it, exactly as design 204 records for a\\n+            synthesized TYPE, so a tag would rename the declaration out from\\n+            under the string that builds the call. An instance's name ALREADY\\n+            carries the template's module tag (`run$m$$1$Fast`), so tagging\\n+            it again would both double the tag and detach the clone's effect\\n+            node from the root the drive site recorded.\\n+          * a STDLIB declaration (`_checking_builtins`), which keeps design\\n+            249's narrower rule: the `$M$<std file>` tag goes on a name two std\\n+            FILES actually share (`json.encode` beside `cbor.encode`) and\\n+            nowhere else. std's free functions are the seam between the compiler\\n+            and its own stdlib — codegen EMITS calls to `__saw_print_int`,\\n+            `__saw_fmt_int`, `__saw_main_exit_code`, `__saw_bt_panic` and a\\n+            dozen more by literal name (`codegen/core.py`, `codegen/calls.py`) —\\n+            so they are named by string for the same reason a synthesized\\n+            function is. They are `__saw_`/`__bt_`-prefixed and none is\\n+            `@export`ed, so the linkage half internalizes every one of them and\\n+            the collision this closes cannot reach them either. Nor can a USER\\n+            declaration collide with one: the user's side is always tagged.\\n         \\\"\\\"\\\"\\n-        if visibility == Visibility.PRIVATE and def_module:\\n-            return \\\"\\\"\\n-        owners = (getattr(self, 'free_function_owners', None) or {}).get(name)\\n-        if not owners or len(owners) < 2:\\n+        if getattr(self, '_checking_builtins', False):\\n+            # Design 249's census, std-only now (`std_free_function_owner_census`).\\n+            if visibility == Visibility.PRIVATE and def_module:\\n+                return \\\"\\\"\\n+            owners = (getattr(self, 'free_function_owners', None) or {}).get(name)\\n+            if not owners or len(owners) < 2:\\n+                return \\\"\\\"\\n+            return f\\\"{name}$M${self._module_symbol_tag(def_module)}\\\"\\n+        if func is not None:\\n+            if (getattr(func, 'is_synthesized', False)\\n+                    or getattr(func, 'is_mono_instance', False)):\\n+                return \\\"\\\"\\n+        if name == \\\"main\\\" and not def_module:\\n             return \\\"\\\"\\n-        return f\\\"{name}$M${self._module_symbol_tag(def_module)}\\\"\\n-\\n-    def _stamp_module_private_functions(self):\\n-        \\\"\\\"\\\"Give this module's private free functions a module-local codegen\\n-        symbol. Runs per module, and only over declarations this module OWNS —\\n-        an imported symbol is the SAME object as the source module's, so\\n-        stamping it here would rename the definition out from under its owner.\\\"\\\"\\\"\\n-        own_module = self._vis_module_for_source(None)\\n-        # Design 249: ask the module-keyed storage for THIS module's own\\n-        # declarations, so an import that puts another module's same-named\\n-        # function in the bare view no longer hides this one's private tag.\\n-        own_table = self.namespace.module_function_overloads.get(\\n-            tuple(own_module), {})\\n-        for name, overloads in own_table.items():\\n-            if len(overloads) != 1:\\n-                # An overload set already carries signature-mangled symbols; a\\n-                # cross-module private clash inside one is out of scope here.\\n-                continue\\n-            sym = overloads[0]\\n-            if sym.mangled_name or sym.decl_node is None:\\n-                continue\\n-            if sym.type_params:\\n-                # A generic's symbol is the template base its monomorphizations\\n-                # are named from; leave that naming alone.\\n-                continue\\n-            mangled = self._module_private_symbol(\\n-                name, own_module, getattr(sym, 'visibility', Visibility.PRIVATE))\\n-            if mangled is None:\\n-                continue\\n-            sym.mangled_name = mangled\\n-            sym.decl_node.mangled_symbol = mangled\\n+        return f\\\"{name}$m${self._module_symbol_tag(def_module)}\\\"\\n \\n     def _stamp_overload_symbols(self):\\n         \\\"\\\"\\\"Assign each member of a 2+ overload set a type-signature-suffixed\\n         codegen symbol (design 55), stamping both the FunctionSymbol and its\\n         declaring AST node so the typechecker (call resolution) and codegen\\n-        (definition emission) agree. Single-declaration names are untouched and\\n-        keep their plain symbol. Generic overloads keep their type-argument\\n-        instantiation naming and are left plain here.\\n+        (definition emission) agree. Single-declaration names keep the symbol\\n+        `_free_function_symbol_base` already gave them. Generic overloads keep\\n+        their type-argument instantiation naming and are left plain here.\\n \\n         Design 249: the free-function half walks the MODULE-KEYED storage and\\n         stamps only the module this pass registers. An overload set is one\\n@@ -1397,8 +1457,9 @@ class RegistrationMixin:\\n         same-named functions in a single bare set never re-mangles either\\n         module's symbols out from under the bodies already resolved against\\n         them. The BASE each symbol is built from is the declaration's\\n-        `symbol_base` — the plain name, or the `$M$`-tagged one when more than\\n-        one module declares the name.\\n+        `symbol_base` — since SL-274 the module-tagged `<name>$m$<module>` for\\n+        every free function, so a member reads `<name>$m$<module>$OL$<sig>` and\\n+        two modules' same-shaped overload sets cannot collide.\\n         \\\"\\\"\\\"\\n         from codegen.mangle import mangle_overload, mangle_method, mangle_type\\n \\ndiff --git a/tools/test_ir_contract.py b/tools/test_ir_contract.py\\nindex 2ecea3c4..1fb0d245 100644\\n--- a/tools/test_ir_contract.py\\n+++ b/tools/test_ir_contract.py\\n@@ -348,6 +348,16 @@ def _check_statics(failures, filename, tag, expected):\\n #: noreturn paths it forms, at every level, so a handful of `minsize`\\n #: functions at `-O1` is LLVM's business and not a leak. What the level owns\\n #: is the WHOLE module — every definition or none.\\n+#: A DIVERGING definition is left out of the share entirely (SL-274), because\\n+#: its size attributes are never the level's: LLVM stamps its own cold clones,\\n+#: and design 261's outlined panic routines are emitted `cold minsize noinline\\n+#: noreturn` by `codegen/calls.py::_panic_helper` at every level on purpose. As\\n+#: a fraction they were harmless while the hosted module carried ~90\\n+#: definitions; once SL-274 internalized everything but the `@export`s,\\n+#: `globaldce` cut the fixture to five attributed definitions of which three\\n+#: were those routines, and `-O1` read 60% `minsize`. The share has to be taken\\n+#: over the definitions the level actually owns, which is what this excludes\\n+#: down to.\\n OPT_LEVEL_ATTRS = [\\n     (\\\"-O1\\\", set(), {\\\"optsize\\\", \\\"minsize\\\"}),\\n     (\\\"-O2\\\", set(), {\\\"optsize\\\", \\\"minsize\\\"}),\\n@@ -386,10 +396,12 @@ def check_opt_level_attributes(failures):\\n         with open(out + \\\".ll\\\") as f:\\n             text = f.read()\\n         groups = {n: set(body.split()) for n, body in _ATTR_GROUP_RE.findall(text)}\\n-        defines = _DEFINE_RE.findall(text)\\n+        defines = [n for n in _DEFINE_RE.findall(text)\\n+                   if \\\"noreturn\\\" not in groups.get(n, ())]\\n         if not defines:\\n-            failures.append(f\\\"{flag}: no `define` carries an attribute group — \\\"\\n-                            f\\\"the fixture emitted nothing to check\\\")\\n+            failures.append(f\\\"{flag}: no returning `define` carries an \\\"\\n+                            f\\\"attribute group — the fixture emitted nothing \\\"\\n+                            f\\\"to check\\\")\\n             continue\\n         for attribute in sorted(wanted | unwanted):\\n             share = (sum(1 for n in defines if attribute in groups.get(n, ()))\\n\",\"id\":\"SL-274.p1\",\"request_id\":\"pid-86890-1789755049-5000\"}","request_id":"pid-86890-1789755049-5000","sequence":981,"timestamp":"1789755050"}
---
