---
{"action":"patch_add","actor":"agent:codex","after":{"assignee":"agent:codex","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-255.p1 (revision 1): minivm M13: bounded checked Scalar DESIGN doc (branch commit 3f0b27b7); implementation remains open","created":"1789100569","id":"c1"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-255.p1 (revision 1)","created":"1789101361","id":"c2"},{"author":"agent:claude-sawlang","body":"Approved patch SL-255.p1 (revision 1)\n\nPrototype fast-track (user auth Sep 10): harness 336/336 at tip, prototypes/ only.","created":"1789101370","id":"c3"},{"author":"agent:claude-sawlang","body":"Merged patch SL-255.p1 (revision 1) as d9e70ed3","created":"1789101372","id":"c4"},{"author":"agent:codex","body":"Submitted patch SL-255.p2 (revision 1): minivm M13: checked Scalar construction and readout; 369-case prototype gate passes","created":"1789395823","id":"c5"}],"created":"1789086375","description":"SL-242 epic child. p1 = branch commit 3f0b27b7 (the design doc, prototypes/minivm/M13_SCALAR.md — landing patch in branch order). IMPLEMENTATION remains open after the doc lands; schedule via the normal process with the other remaining lexer-milestone work.","id":"SL-255","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":7,"sequence":771,"stage":"queued","status":"open","title":"minivm M13: bounded checked Scalar support (design landed; implementation open)","updated":"1789395823"},"before":{"assignee":"agent:codex","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-255.p1 (revision 1): minivm M13: bounded checked Scalar DESIGN doc (branch commit 3f0b27b7); implementation remains open","created":"1789100569","id":"c1"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-255.p1 (revision 1)","created":"1789101361","id":"c2"},{"author":"agent:claude-sawlang","body":"Approved patch SL-255.p1 (revision 1)\n\nPrototype fast-track (user auth Sep 10): harness 336/336 at tip, prototypes/ only.","created":"1789101370","id":"c3"},{"author":"agent:claude-sawlang","body":"Merged patch SL-255.p1 (revision 1) as d9e70ed3","created":"1789101372","id":"c4"}],"created":"1789086375","description":"SL-242 epic child. p1 = branch commit 3f0b27b7 (the design doc, prototypes/minivm/M13_SCALAR.md — landing patch in branch order). IMPLEMENTATION remains open after the doc lands; schedule via the normal process with the other remaining lexer-milestone work.","id":"SL-255","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":6,"sequence":770,"stage":"queued","status":"open","title":"minivm M13: bounded checked Scalar support (design landed; implementation open)","updated":"1789394931"},"id":"SL-255","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:codex\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex da9e4c4f..ad19d327 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -22,7 +22,8 @@ conditional binding, including the unchanged Token/tok extraction (307 cases).\\n M11 adds fixed `Result<T,E>` construction and matching plus `try`/`try!`\\n propagation, including owning payloads and Void success (336 cases). M12 adds\\n checked unsigned parsing and the unchanged literal_fits helper (346 cases).\\n-Next are `Scalar`, `StringBuilder`, and `Vector`. The lexer has\\n+M13 adds checked nominal `Scalar` construction and code-point readout, with\\n+opaque `InvalidScalar` results. Next are `StringBuilder` and `Vector`. The lexer has\\n no imports, so its first whole-file test can combine the unchanged library with\\n a small wrapper without implementing general module loading.\\n The dependency groups below are an inventory, not the implementation commit order;\\ndiff --git a/prototypes/minivm/M13_SCALAR.md b/prototypes/minivm/M13_SCALAR.md\\nindex 10571ced..b875aee6 100644\\n--- a/prototypes/minivm/M13_SCALAR.md\\n+++ b/prototypes/minivm/M13_SCALAR.md\\n@@ -1,6 +1,6 @@\\n # M13: checked Scalar construction\\n \\n-Planned after unsigned parsing, before StringBuilder. The lexer uses only\\n+Implemented after unsigned parsing, before StringBuilder. The lexer uses only\\n `try! Scalar(value: cp)` and passes the result to StringBuilder.append. This\\n slice must distinguish Scalar from Int and prevent source-level fabrication\\n of an invalid code point. It is not the complete std Scalar or enum surface.\\n@@ -51,3 +51,17 @@ error cause and original input since that projection is not a source API yet.\\n \\n Next, StringBuilder.append(Scalar) will encode exactly 1–4 UTF-8 bytes from a\\n validated Scalar. That integration gets its own byte-boundary tests.\\n+\\n+## Validation\\n+\\n+The `scalars` section of `test_minivm.py` covers construction boundaries,\\n+invalid inputs, nominal typing, copying, references and opaque representation\\n+restrictions through the VM and emitted native code at `-O0` and `-O2`.\\n+The marked shared-subset fixtures also run against Python sawc with `--sawc`.\\n+\\n+`tests/scalar_contract.saw` compiles source constructors and checks their\\n+returned words against literal expectations, including inactive zeros and the\\n+exact rejected input at both signed extremes. It runs the checks in the VM;\\n+its `emit-llvm` mode emits the same checks for native validation. Build it with\\n+the Python compiler and module paths `src=prototypes/minivm/src` and\\n+`sawlex=selfhost/lexer`.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 69d59c22..ff3c2d7b 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -117,6 +117,14 @@ byte strings in bases 2 through 36 and an optional leading plus. Invalid input\\n and overflow return None. Full unsigned 64-bit values are preserved; see\\n [M12_UNSIGNED_PARSE.md](M12_UNSIGNED_PARSE.md).\\n \\n+`Scalar(value: Int)` validates a Unicode code point and returns\\n+`Result<Scalar, InvalidScalar>`. `Scalar.value()` exposes the code point on a\\n+named place. Both builtin records are nominal and opaque: source cannot use\\n+their private representation fields, construct `InvalidScalar`, or extend\\n+either type. `InvalidScalar` cases and formatting are intentionally deferred;\\n+Result matching can currently observe and forward only the opaque error value.\\n+See [M13_SCALAR.md](M13_SCALAR.md).\\n+\\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n only to VM execution; native code uses the host call stack and has no budget.\\n@@ -129,20 +137,21 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second execution timeouts. The first twelve milestones\\n-cover 346 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second execution timeouts. The first thirteen milestones\\n+cover 369 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw`,\\n `tests/record_contract.saw`, `tests/enum_contract.saw`,\\n `tests/reference_contract.saw`, `tests/receiver_contract.saw`,\\n `tests/string_contract.saw`, `tests/owning_record_contract.saw`,\\n-`tests/optional_contract.saw`, `tests/result_contract.saw`, and\\n-`tests/uint_parse_contract.saw`; build and run them\\n+`tests/optional_contract.saw`, `tests/result_contract.saw`,\\n+`tests/uint_parse_contract.saw`, and `tests/scalar_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n `--section enums`, `--section values`, `--section references`, or\\n `--section receivers`, `--section strings`, `--section owning_records` or\\n-`--section optionals`, `--section results`, or `--section unsigned_parse` for an isolated\\n+`--section optionals`, `--section results`, `--section unsigned_parse`, or\\n+`--section scalars` for an isolated\\n integration gate.\\n \\n To run the initial SL-260 shared-subset differential as well:\\ndiff --git a/prototypes/minivm/examples/scalars/boundaries.saw b/prototypes/minivm/examples/scalars/boundaries.saw\\nnew file mode 100644\\nindex 00000000..69ae7920\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/boundaries.saw\\n@@ -0,0 +1,19 @@\\n+func show(value: Int) {\\n+    match Scalar(value: value) {\\n+        case Ok(scalar) -> { print(scalar.value()) },\\n+        case Err(_) -> { print(\\\"invalid\\\") }\\n+    }\\n+}\\n+\\n+func main() {\\n+    show(0)\\n+    show(0x7F)\\n+    show(0x80)\\n+    show(0x7FF)\\n+    show(0x800)\\n+    show(0xD7FF)\\n+    show(0xE000)\\n+    show(0xFFFF)\\n+    show(0x10000)\\n+    show(0x10FFFF)\\n+}\\ndiff --git a/prototypes/minivm/examples/scalars/composition.saw b/prototypes/minivm/examples/scalars/composition.saw\\nnew file mode 100644\\nindex 00000000..fb3454cf\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/composition.saw\\n@@ -0,0 +1,32 @@\\n+struct Holder { scalar: Scalar }\\n+struct Outer { holder: Holder }\\n+func next(count: &var Int) -> Int {\\n+    count += 1\\n+    count\\n+}\\n+func observe(s: &Scalar) -> Int { s.value() }\\n+func replace(s: &var Scalar, n: Int) { s = try! Scalar(value: n) }\\n+func make(n: Int) -> Result<Scalar, InvalidScalar> { Scalar(value: n) }\\n+func main() {\\n+    var count = 0\\n+    let first = try! Scalar(value: next(&var count))\\n+    print(count)\\n+    print(first.value())\\n+    var outer = Outer(holder: Holder(scalar: first))\\n+    replace(&var outer.holder.scalar, 0x10FFFF)\\n+    print(observe(&outer.holder.scalar))\\n+    print(first.value())\\n+    let optional: Scalar? = outer.holder.scalar\\n+    if let scalar = optional { print(scalar.value()) }\\n+    match make(-1) {\\n+        case Ok(_) -> { print(\\\"wrong\\\") },\\n+        case Err(error) -> {\\n+            let copied = error\\n+            let forwarded = Result<Scalar, InvalidScalar>.Err(error: copied)\\n+            match forwarded {\\n+                case Ok(_) -> { print(\\\"wrong\\\") },\\n+                case Err(_) -> { print(\\\"error\\\") }\\n+            }\\n+        }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/scalars/invalid.saw b/prototypes/minivm/examples/scalars/invalid.saw\\nnew file mode 100644\\nindex 00000000..1585d349\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/invalid.saw\\n@@ -0,0 +1,15 @@\\n+func show(value: Int) {\\n+    match Scalar(value: value) {\\n+        case Ok(_) -> { print(\\\"valid\\\") },\\n+        case Err(_) -> { print(\\\"invalid\\\") }\\n+    }\\n+}\\n+\\n+func main() {\\n+    show(-1)\\n+    show(Int.min)\\n+    show(0xD800)\\n+    show(0xDFFF)\\n+    show(0x110000)\\n+    show(Int.max)\\n+}\\ndiff --git a/prototypes/minivm/examples/scalars/record_identity_control.saw b/prototypes/minivm/examples/scalars/record_identity_control.saw\\nnew file mode 100644\\nindex 00000000..cbc5514c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/record_identity_control.saw\\n@@ -0,0 +1,7 @@\\n+struct UserScalar { raw: Int }\\n+func main() {\\n+    var value = UserScalar(raw: 7)\\n+    print(value.raw)\\n+    value.raw = 9\\n+    print(value.raw)\\n+}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_arithmetic.saw b/prototypes/minivm/examples/scalars/reject_arithmetic.saw\\nnew file mode 100644\\nindex 00000000..5dbc7b3d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_arithmetic.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let scalar = try! Scalar(value: 65)\\n+    let _ = scalar + scalar\\n+}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_arity.saw b/prototypes/minivm/examples/scalars/reject_arity.saw\\nnew file mode 100644\\nindex 00000000..3c2bc21d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_arity.saw\\n@@ -0,0 +1 @@\\n+func main() { let _ = Scalar(value: 65, value: 66) }\\ndiff --git a/prototypes/minivm/examples/scalars/reject_cast.saw b/prototypes/minivm/examples/scalars/reject_cast.saw\\nnew file mode 100644\\nindex 00000000..c6bedcc4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_cast.saw\\n@@ -0,0 +1 @@\\n+func main() { let _ = 65 as Scalar }\\ndiff --git a/prototypes/minivm/examples/scalars/reject_field_read.saw b/prototypes/minivm/examples/scalars/reject_field_read.saw\\nnew file mode 100644\\nindex 00000000..cb7a9873\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_field_read.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let scalar = try! Scalar(value: 65)\\n+    print(scalar.raw)\\n+}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_field_write.saw b/prototypes/minivm/examples/scalars/reject_field_write.saw\\nnew file mode 100644\\nindex 00000000..76e589e9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_field_write.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    var scalar = try! Scalar(value: 65)\\n+    scalar.raw = 66\\n+}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_int_assignment.saw b/prototypes/minivm/examples/scalars/reject_int_assignment.saw\\nnew file mode 100644\\nindex 00000000..57db92c3\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_int_assignment.saw\\n@@ -0,0 +1 @@\\n+func main() { let scalar: Scalar = 65 }\\ndiff --git a/prototypes/minivm/examples/scalars/reject_invalid_enum.saw b/prototypes/minivm/examples/scalars/reject_invalid_enum.saw\\nnew file mode 100644\\nindex 00000000..e55aea8e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_invalid_enum.saw\\n@@ -0,0 +1,2 @@\\n+enum InvalidScalar { case Bad }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_invalid_extension.saw b/prototypes/minivm/examples/scalars/reject_invalid_extension.saw\\nnew file mode 100644\\nindex 00000000..7b906f46\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_invalid_extension.saw\\n@@ -0,0 +1,2 @@\\n+extension InvalidScalar { func other(&self) -> Int { 0 } }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_invalid_field.saw b/prototypes/minivm/examples/scalars/reject_invalid_field.saw\\nnew file mode 100644\\nindex 00000000..1f4785c7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_invalid_field.saw\\n@@ -0,0 +1,2 @@\\n+func inspect(value: InvalidScalar) { print(value.cause) }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_invalid_function.saw b/prototypes/minivm/examples/scalars/reject_invalid_function.saw\\nnew file mode 100644\\nindex 00000000..ba7e0263\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_invalid_function.saw\\n@@ -0,0 +1,2 @@\\n+func InvalidScalar() {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_label.saw b/prototypes/minivm/examples/scalars/reject_label.saw\\nnew file mode 100644\\nindex 00000000..8c8d58d7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_label.saw\\n@@ -0,0 +1 @@\\n+func main() { let _ = Scalar(raw: 65) }\\ndiff --git a/prototypes/minivm/examples/scalars/reject_nominal_conversion.saw b/prototypes/minivm/examples/scalars/reject_nominal_conversion.saw\\nnew file mode 100644\\nindex 00000000..73d71f64\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_nominal_conversion.saw\\n@@ -0,0 +1,3 @@\\n+struct Fake { raw: Int }\\n+func take(value: Scalar) {}\\n+func main() { take(Fake(raw: 65)) }\\ndiff --git a/prototypes/minivm/examples/scalars/reject_print.saw b/prototypes/minivm/examples/scalars/reject_print.saw\\nnew file mode 100644\\nindex 00000000..872b7ca1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_print.saw\\n@@ -0,0 +1 @@\\n+func main() { print(try! Scalar(value: 65)) }\\ndiff --git a/prototypes/minivm/examples/scalars/reject_raw_invalid.saw b/prototypes/minivm/examples/scalars/reject_raw_invalid.saw\\nnew file mode 100644\\nindex 00000000..eee14a05\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_raw_invalid.saw\\n@@ -0,0 +1 @@\\n+func main() { let _ = InvalidScalar(value: 65) }\\ndiff --git a/prototypes/minivm/examples/scalars/reject_raw_scalar.saw b/prototypes/minivm/examples/scalars/reject_raw_scalar.saw\\nnew file mode 100644\\nindex 00000000..49eb5b97\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_raw_scalar.saw\\n@@ -0,0 +1 @@\\n+func main() { let _ = Scalar(raw: 55296) }\\ndiff --git a/prototypes/minivm/examples/scalars/reject_scalar_extension.saw b/prototypes/minivm/examples/scalars/reject_scalar_extension.saw\\nnew file mode 100644\\nindex 00000000..d0becd22\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_scalar_extension.saw\\n@@ -0,0 +1,2 @@\\n+extension Scalar { func other(&self) -> Int { 0 } }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_scalar_static.saw b/prototypes/minivm/examples/scalars/reject_scalar_static.saw\\nnew file mode 100644\\nindex 00000000..6b5acadf\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_scalar_static.saw\\n@@ -0,0 +1,2 @@\\n+static Scalar: Int = 0\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_scalar_struct.saw b/prototypes/minivm/examples/scalars/reject_scalar_struct.saw\\nnew file mode 100644\\nindex 00000000..3511865f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_scalar_struct.saw\\n@@ -0,0 +1,2 @@\\n+struct Scalar { value: Int }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/scalars/reject_temporary_value.saw b/prototypes/minivm/examples/scalars/reject_temporary_value.saw\\nnew file mode 100644\\nindex 00000000..98f2ba2a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/scalars/reject_temporary_value.saw\\n@@ -0,0 +1 @@\\n+func main() { print((try! Scalar(value: 65)).value()) }\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex c6cc9fba..51c7c333 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -148,6 +148,8 @@ struct Compiler {\\n     slots: Vector<ValueType>\\n     code: Vector<Instruction>\\n     current_result: ValueType\\n+    scalar_record: Int\\n+    invalid_scalar_record: Int\\n }\\n extension Compiler: NoCopy {}\\n \\n@@ -358,7 +360,7 @@ extension Compiler {\\n     }\\n \\n     func reserved_type_name(&self, name: String) -> Bool {\\n-        if name == \\\"Bool\\\" || name == \\\"String\\\" || name == \\\"Void\\\" { return true }\\n+        if name == \\\"Bool\\\" || name == \\\"String\\\" || name == \\\"Void\\\" || name == \\\"Scalar\\\" || name == \\\"InvalidScalar\\\" { return true }\\n         if let _ = named_integer_type(name) { return true }\\n         false\\n     }\\n@@ -439,6 +441,9 @@ extension Compiler {\\n         let target = try self.expect(TokenKind.Ident, \\\"expected extension target\\\")\\n         let owner = self.record_index(target.value)\\n         if owner < 0 { return problem(\\\"extension target must be a known record\\\", target.line, target.col) }\\n+        if owner == self.scalar_record || owner == self.invalid_scalar_record {\\n+            return problem(\\\"builtin record `{target.value}` cannot be extended\\\", target.line, target.col)\\n+        }\\n         if self.at(TokenKind.Colon) { return self.fail(\\\"traits are not supported\\\") }\\n         if not self.at(TokenKind.LBrace) {\\n             return problem(\\\"generic extensions are not supported\\\", extension_token.line, extension_token.col)\\n@@ -1332,6 +1337,22 @@ extension Compiler {\\n                     }\\n                     let owner = match place.ty { case Record(index) -> index, case _ -> -1 }\\n                     if owner < 0 { return problem(\\\"method receiver must be a record place\\\", dot.line, dot.col) }\\n+                    if owner == self.scalar_record {\\n+                        if method.value != \\\"value\\\" {\\n+                            return problem(\\\"Scalar has no method `{method.value}`\\\", method.line, method.col)\\n+                        }\\n+                        let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after Scalar.value\\\")\\n+                        self.skip_newlines()\\n+                        if not self.at(TokenKind.RParen) {\\n+                            return problem(\\\"Scalar.value accepts no arguments\\\", method.line, method.col)\\n+                        }\\n+                        let _ = self.take()\\n+                        let raw = try self.read_place(place, &candidate)\\n+                        return expr_value(ValueType.I64, raw.slot, false)\\n+                    }\\n+                    if owner == self.invalid_scalar_record {\\n+                        return problem(\\\"InvalidScalar has no source methods\\\", method.line, method.col)\\n+                    }\\n                     let mi = self.method_index(owner, method.value)\\n                     if mi < 0 { return problem(\\\"record `{self.records[owner].name}` has no method `{method.value}`\\\", method.line, method.col) }\\n                     return self.parse_method_call(place, method, mi)\\n@@ -1355,7 +1376,13 @@ extension Compiler {\\n                 return expr_value(ty, dst, false)\\n             }\\n             let ri = self.record_index(name.value)\\n-            if ri >= 0 && self.at(TokenKind.LParen) { return self.parse_record_constructor(name, ri) }\\n+            if ri == self.scalar_record && self.at(TokenKind.LParen) { return self.parse_scalar_constructor(name) }\\n+            if ri >= 0 && self.at(TokenKind.LParen) {\\n+                if ri == self.invalid_scalar_record {\\n+                    return problem(\\\"InvalidScalar cannot be constructed directly\\\", name.line, name.col)\\n+                }\\n+                return self.parse_record_constructor(name, ri)\\n+            }\\n             if let ty = named_integer_type(name.value) {\\n                 if self.at(TokenKind.Dot) { return self.parse_integer_member(name, ty) }\\n                 if self.at(TokenKind.LParen) && ty == ValueType.Byte { return self.parse_byte(name) }\\n@@ -1469,6 +1496,9 @@ extension Compiler {\\n             let field = try self.expect(TokenKind.Ident, \\\"expected record field after `.`\\\")\\n             match place.ty {\\n                 case Record(ri) -> {\\n+                    if ri == self.scalar_record || ri == self.invalid_scalar_record {\\n+                        return problem(\\\"builtin record fields are private\\\", field.line, field.col)\\n+                    }\\n                     let fi = self.field_index(ri, field.value)\\n                     if fi < 0 { return problem(\\\"record `{self.records[ri].name}` has no field `{field.value}`\\\", field.line, field.col) }\\n                     let field_type = self.records[ri].field_types[fi]\\n@@ -1509,6 +1539,9 @@ extension Compiler {\\n             }\\n             match value.ty {\\n                 case Record(ri) -> {\\n+                    if ri == self.scalar_record || ri == self.invalid_scalar_record {\\n+                        return problem(\\\"builtin record fields are private\\\", field.line, field.col)\\n+                    }\\n                     let fi = self.field_index(ri, field.value)\\n                     if fi < 0 { return problem(\\\"record `{self.records[ri].name}` has no field `{field.value}`\\\", field.line, field.col) }\\n                     let offset = try record_field_offset(&self.records, &self.sums, ri, fi)\\n@@ -1637,6 +1670,131 @@ extension Compiler {\\n         problem(\\\"unknown String intrinsic `{name.value}`\\\", name.line, name.col)\\n     }\\n \\n+    func parse_scalar_constructor(&var self, name: Token) -> Result<ExprValue, ProtoError> {\\n+        let _ = self.take()\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        if self.at(TokenKind.RParen) {\\n+            return problem(\\\"Scalar requires exactly one `value` argument\\\", name.line, name.col)\\n+        }\\n+        let label = try self.expect(TokenKind.Ident, \\\"Scalar argument label must be `value`\\\")\\n+        if label.value != \\\"value\\\" {\\n+            return problem(\\\"Scalar argument label must be `value`\\\", label.line, label.col)\\n+        }\\n+        let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after Scalar argument label\\\")\\n+        self.expr_space()\\n+        var input = try self.parse_expected_expr(ValueType.I64)\\n+        if input.always_returns {\\n+            return problem(\\\"all-returning control flow cannot be used as an operand\\\", label.line, label.col)\\n+        }\\n+        input = try self.snapshot(input, &label)\\n+        self.skip_newlines()\\n+        if self.at(TokenKind.Comma) {\\n+            return problem(\\\"Scalar accepts exactly one argument\\\", name.line, name.col)\\n+        }\\n+        let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after Scalar value\\\")\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+\\n+        let result_ty = self.intern_result(ValueType.Record(self.scalar_record),\\n+                                           ValueType.Record(self.invalid_scalar_record))\\n+        let dst = try self.new_value(result_ty)\\n+        var word = 0\\n+        while word < 4 {\\n+            let zero = self.emit_at(Op.Const, name.line, name.col)\\n+            self.code[zero].dst = dst + word\\n+            self.code[zero].immediate = 0\\n+            word += 1\\n+        }\\n+\\n+        let zero_slot = self.new_slot(ValueType.I64)\\n+        let zero_const = self.emit_at(Op.Const, name.line, name.col)\\n+        self.code[zero_const].dst = zero_slot\\n+        let negative = self.new_slot(ValueType.Bool)\\n+        let negative_cmp = self.emit_at(Op.Lt, name.line, name.col)\\n+        self.code[negative_cmp].dst = negative\\n+        self.code[negative_cmp].a = input.slot\\n+        self.code[negative_cmp].b = zero_slot\\n+        let negative_branch = self.emit_at(Op.Branch, name.line, name.col)\\n+        self.code[negative_branch].a = negative\\n+        let skip_negative = self.emit_at(Op.Jump, name.line, name.col)\\n+        self.code[negative_branch].target = self.code.len()\\n+        let negative_value = self.emit_at(Op.Copy, name.line, name.col)\\n+        self.code[negative_value].dst = dst + 3\\n+        self.code[negative_value].a = input.slot\\n+        let negative_done = self.emit_at(Op.Jump, name.line, name.col)\\n+\\n+        self.code[skip_negative].target = self.code.len()\\n+        let maximum_slot = self.new_slot(ValueType.I64)\\n+        let maximum_const = self.emit_at(Op.Const, name.line, name.col)\\n+        self.code[maximum_const].dst = maximum_slot\\n+        self.code[maximum_const].immediate = 0x10FFFF\\n+        let above = self.new_slot(ValueType.Bool)\\n+        let above_cmp = self.emit_at(Op.Gt, name.line, name.col)\\n+        self.code[above_cmp].dst = above\\n+        self.code[above_cmp].a = input.slot\\n+        self.code[above_cmp].b = maximum_slot\\n+        let above_branch = self.emit_at(Op.Branch, name.line, name.col)\\n+        self.code[above_branch].a = above\\n+        let skip_above = self.emit_at(Op.Jump, name.line, name.col)\\n+        self.code[above_branch].target = self.code.len()\\n+        let above_value = self.emit_at(Op.Copy, name.line, name.col)\\n+        self.code[above_value].dst = dst + 3\\n+        self.code[above_value].a = input.slot\\n+        let above_done = self.emit_at(Op.Jump, name.line, name.col)\\n+\\n+        self.code[skip_above].target = self.code.len()\\n+        let low_slot = self.new_slot(ValueType.I64)\\n+        let low_const = self.emit_at(Op.Const, name.line, name.col)\\n+        self.code[low_const].dst = low_slot\\n+        self.code[low_const].immediate = 0xD800\\n+        let at_least_low = self.new_slot(ValueType.Bool)\\n+        let low_cmp = self.emit_at(Op.Ge, name.line, name.col)\\n+        self.code[low_cmp].dst = at_least_low\\n+        self.code[low_cmp].a = input.slot\\n+        self.code[low_cmp].b = low_slot\\n+        let low_branch = self.emit_at(Op.Branch, name.line, name.col)\\n+        self.code[low_branch].a = at_least_low\\n+        let skip_surrogate_check = self.emit_at(Op.Jump, name.line, name.col)\\n+        self.code[low_branch].target = self.code.len()\\n+        let high_slot = self.new_slot(ValueType.I64)\\n+        let high_const = self.emit_at(Op.Const, name.line, name.col)\\n+        self.code[high_const].dst = high_slot\\n+        self.code[high_const].immediate = 0xDFFF\\n+        let at_most_high = self.new_slot(ValueType.Bool)\\n+        let high_cmp = self.emit_at(Op.Le, name.line, name.col)\\n+        self.code[high_cmp].dst = at_most_high\\n+        self.code[high_cmp].a = input.slot\\n+        self.code[high_cmp].b = high_slot\\n+        let surrogate_branch = self.emit_at(Op.Branch, name.line, name.col)\\n+        self.code[surrogate_branch].a = at_most_high\\n+        let skip_surrogate = self.emit_at(Op.Jump, name.line, name.col)\\n+        self.code[surrogate_branch].target = self.code.len()\\n+        let cause = self.emit_at(Op.Const, name.line, name.col)\\n+        self.code[cause].dst = dst + 2\\n+        self.code[cause].immediate = 1\\n+        let surrogate_value = self.emit_at(Op.Copy, name.line, name.col)\\n+        self.code[surrogate_value].dst = dst + 3\\n+        self.code[surrogate_value].a = input.slot\\n+        let surrogate_done = self.emit_at(Op.Jump, name.line, name.col)\\n+\\n+        let success = self.code.len()\\n+        self.code[skip_surrogate_check].target = success\\n+        self.code[skip_surrogate].target = success\\n+        let tag = self.emit_at(Op.Const, name.line, name.col)\\n+        self.code[tag].dst = dst\\n+        self.code[tag].immediate = 1\\n+        let scalar = self.emit_at(Op.Copy, name.line, name.col)\\n+        self.code[scalar].dst = dst + 1\\n+        self.code[scalar].a = input.slot\\n+        let end = self.code.len()\\n+        self.code[negative_done].target = end\\n+        self.code[above_done].target = end\\n+        self.code[surrogate_done].target = end\\n+        expr_value(result_ty, dst, false)\\n+    }\\n+\\n     func parse_record_constructor(&var self, name: Token, ri: Int) -> Result<ExprValue, ProtoError> {\\n         let _ = self.take()\\n         self.depth += 1\\n@@ -3539,14 +3697,24 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         case Ok(r) -> { tokens = r.tokens.copy() },\\n         case Err(e) -> { return problem(e.message, e.line, e.col) }\\n     }\\n-    var compiler = Compiler(source: source, tokens: move tokens, records: Vector<RecordIR>(), enums: Vector<EnumIR>(),\\n+    var records = Vector<RecordIR>()\\n+    let scalar_fields: Vector<String> = [\\\"$scalar.raw\\\"]\\n+    let scalar_types: Vector<ValueType> = [ValueType.I64]\\n+    try! records.push(RecordIR(name: \\\"Scalar\\\", field_names: move scalar_fields,\\n+                               field_types: move scalar_types, line: 0, col: 0))\\n+    let invalid_fields: Vector<String> = [\\\"$invalid-scalar.cause\\\", \\\"$invalid-scalar.original\\\"]\\n+    let invalid_types: Vector<ValueType> = [ValueType.I64, ValueType.I64]\\n+    try! records.push(RecordIR(name: \\\"InvalidScalar\\\", field_names: move invalid_fields,\\n+                               field_types: move invalid_types, line: 0, col: 0))\\n+    var compiler = Compiler(source: source, tokens: move tokens, records: move records, enums: Vector<EnumIR>(),\\n                             references: Vector<ReferenceIR>(), sums: Vector<SumIR>(), strings: Vector<String>(), statics: Vector<StaticBinding>(),\\n                             signatures: Vector<Signature>(), pos: 0, depth: 0, scope_depth: 0,\\n                             expr_depth: 0, paren_depth: 0,\\n                             bindings: Vector<Binding>(), active_borrows: Vector<Borrow>(), pending_writes: Vector<Int>(),\\n                             string_slots: Vector<StringSlot>(),\\n                             slots: Vector<ValueType>(),\\n-                            code: Vector<Instruction>(), current_result: ValueType.Void)\\n+                            code: Vector<Instruction>(), current_result: ValueType.Void,\\n+                            scalar_record: 0, invalid_scalar_record: 1)\\n     try compiler.collect_record_names()\\n     try compiler.scan_signatures()\\n     try validate_records(&compiler.records, &compiler.sums)\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex bfb6a2ea..a54f3da5 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -39,6 +39,10 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"scalars/boundaries\\\", \\\"0\\\\n127\\\\n128\\\\n2047\\\\n2048\\\\n55295\\\\n57344\\\\n65535\\\\n65536\\\\n1114111\\\\n\\\", section=\\\"scalars\\\", compare_sawc=True),\\n+    RunCase(\\\"scalars/invalid\\\", \\\"invalid\\\\n\\\" * 6, section=\\\"scalars\\\", compare_sawc=True),\\n+    RunCase(\\\"scalars/composition\\\", \\\"1\\\\n1\\\\n1114111\\\\n1\\\\n1114111\\\\nerror\\\\n\\\", section=\\\"scalars\\\", compare_sawc=True),\\n+    RunCase(\\\"scalars/record_identity_control\\\", \\\"7\\\\n9\\\\n\\\", section=\\\"scalars\\\"),\\n     RunCase(\\\"unsigned_parse/radices\\\", \\\"42\\\\n\\\" * 7 + \\\"1295\\\\n42\\\\n42\\\\n43323\\\\n11\\\\nnone\\\\nnone\\\\n0\\\\n0\\\\n\\\", section=\\\"unsigned_parse\\\", compare_sawc=True),\\n     RunCase(\\\"unsigned_parse/boundaries\\\", \\\"9223372036854775808\\\\n18446744073709551614\\\\n18446744073709551615\\\\nnone\\\\n18446744073709551614\\\\n18446744073709551615\\\\nnone\\\\n18446744073709551615\\\\nnone\\\\n18446744073709551615\\\\nnone\\\\n18446744073709551615\\\\nnone\\\\n18446744073709551615\\\\n\\\", section=\\\"unsigned_parse\\\", compare_sawc=True),\\n     RunCase(\\\"unsigned_parse/invalid\\\", \\\"none\\\\n\\\" * 21, section=\\\"unsigned_parse\\\", compare_sawc=True),\\n@@ -228,6 +232,25 @@ RUN_CASES = (\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"scalars/reject_raw_scalar\\\", \\\"argument label must be `value`\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_raw_invalid\\\", \\\"cannot be constructed directly\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_field_read\\\", \\\"fields are private\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_field_write\\\", \\\"fields are private\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_label\\\", \\\"argument label must be `value`\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_arity\\\", \\\"exactly one argument\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_cast\\\", \\\"requires integer source and target\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_scalar_struct\\\", \\\"reserved\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_invalid_enum\\\", \\\"reserved\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_scalar_static\\\", \\\"reserved\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_invalid_function\\\", \\\"conflicts with a record name\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_scalar_extension\\\", \\\"cannot be extended\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_temporary_value\\\", \\\"method calls require a named place receiver\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_nominal_conversion\\\", \\\"cannot implicitly convert\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_int_assignment\\\", \\\"cannot implicitly convert\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_invalid_extension\\\", \\\"cannot be extended\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_invalid_field\\\", \\\"fields are private\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_print\\\", \\\"cannot print a record value\\\", \\\"scalars\\\"),\\n+    RejectCase(\\\"scalars/reject_arithmetic\\\", \\\"arithmetic requires integer operands\\\", \\\"scalars\\\"),\\n     RejectCase(\\\"unsigned_parse/reject_radix_type\\\", \\\"argument must be Int\\\", \\\"unsigned_parse\\\", True),\\n     RejectCase(\\\"unsigned_parse/reject_arity\\\", \\\"wrong number of arguments\\\", \\\"unsigned_parse\\\", True),\\n     RejectCase(\\\"unsigned_parse/reject_label\\\", \\\"does not match parameter\\\", \\\"unsigned_parse\\\", True),\\n@@ -519,7 +542,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--sawc-python\\\", default=sys.executable,\\n                         help=\\\"Python interpreter with sawc dependencies (default: this interpreter)\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\", \\\"results\\\", \\\"unsigned_parse\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\", \\\"results\\\", \\\"unsigned_parse\\\", \\\"scalars\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\ndiff --git a/prototypes/minivm/tests/scalar_contract.saw b/prototypes/minivm/tests/scalar_contract.saw\\nnew file mode 100644\\nindex 00000000..437bee9a\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/scalar_contract.saw\\n@@ -0,0 +1,89 @@\\n+import src.frontend.*\\n+import src.model.*\\n+import src.verify.*\\n+import src.vm.*\\n+import src.llvm\\n+import std.env.{Env}\\n+\\n+func checked_word(code: &var Vector<Instruction>, slots: &var Vector<ValueType>, actual: Int, expected: Int) {\\n+    let constant_slot = slots.len()\\n+    let expected_type = slots[actual]\\n+    try! slots.push(expected_type)\\n+    var constant = instruction(Op.Const)\\n+    constant.dst = constant_slot\\n+    constant.immediate = expected\\n+    try! code.push(move constant)\\n+    let equal_slot = slots.len()\\n+    try! slots.push(ValueType.Bool)\\n+    var equal = instruction(Op.Eq)\\n+    equal.dst = equal_slot\\n+    equal.a = actual\\n+    equal.b = constant_slot\\n+    try! code.push(move equal)\\n+    var branch = instruction(Op.Branch)\\n+    branch.a = equal_slot\\n+    branch.target = code.len() + 2\\n+    try! code.push(move branch)\\n+    try! code.push(instruction(Op.Panic))\\n+}\\n+\\n+func checked_call(code: &var Vector<Instruction>, slots: &var Vector<ValueType>, target: Int,\\n+                  tag: Int, scalar: Int, cause: Int, original: Int) {\\n+    let dst = slots.len()\\n+    try! slots.push(ValueType.Bool)\\n+    try! slots.push(ValueType.I64)\\n+    try! slots.push(ValueType.I64)\\n+    try! slots.push(ValueType.I64)\\n+    var call = instruction(Op.Call)\\n+    call.target = target\\n+    call.dst = dst\\n+    try! code.push(move call)\\n+    checked_word(&var code, &var slots, dst, tag)\\n+    checked_word(&var code, &var slots, dst + 1, scalar)\\n+    checked_word(&var code, &var slots, dst + 2, cause)\\n+    checked_word(&var code, &var slots, dst + 3, original)\\n+}\\n+\\n+func function_index(p: &Program, name: String) -> Int {\\n+    var i = 0\\n+    while i < p.functions.len() {\\n+        if p.functions[i].name == name { return i }\\n+        i += 1\\n+    }\\n+    -1\\n+}\\n+\\n+func contract_program() -> Program {\\n+    let source = \\\"func good() -> Result<Scalar, InvalidScalar> \\\\{ Scalar(value: 65) \\\\}\\\\nfunc negative() -> Result<Scalar, InvalidScalar> \\\\{ Scalar(value: Int.min) \\\\}\\\\nfunc surrogate() -> Result<Scalar, InvalidScalar> \\\\{ Scalar(value: 0xD800) \\\\}\\\\nfunc above() -> Result<Scalar, InvalidScalar> \\\\{ Scalar(value: 0x110000) \\\\}\\\\nfunc maximum() -> Result<Scalar, InvalidScalar> \\\\{ Scalar(value: Int.max) \\\\}\\\\nfunc main() \\\\{\\\\}\\\\n\\\"\\n+    var program = try! compile_source(source)\\n+    assert(program.records.len() >= 2, \\\"builtin records installed\\\")\\n+    assert(program.records[0].name == \\\"Scalar\\\" && program.records[1].name == \\\"InvalidScalar\\\", \\\"stable builtin identities\\\")\\n+    assert(program.records[0].field_names[0] == \\\"$scalar.raw\\\" && program.records[1].field_names[0] == \\\"$invalid-scalar.cause\\\", \\\"private fields are unspellable\\\")\\n+    let scalar_result = program.functions[function_index(&program, \\\"good\\\")].result\\n+    let layout = try! value_layout(&program.records, &program.sums, scalar_result)\\n+    assert(layout.len() == 4 && layout[0] == ValueType.Bool && layout[1] == ValueType.I64 && layout[2] == ValueType.I64 && layout[3] == ValueType.I64, \\\"Scalar Result layout\\\")\\n+    let main_index = program.main_index\\n+    var slots = Vector<ValueType>()\\n+    var code = Vector<Instruction>()\\n+    checked_call(&var code, &var slots, function_index(&program, \\\"good\\\"), 1, 65, 0, 0)\\n+    checked_call(&var code, &var slots, function_index(&program, \\\"negative\\\"), 0, 0, 0, Int.min)\\n+    checked_call(&var code, &var slots, function_index(&program, \\\"surrogate\\\"), 0, 0, 1, 0xD800)\\n+    checked_call(&var code, &var slots, function_index(&program, \\\"above\\\"), 0, 0, 0, 0x110000)\\n+    checked_call(&var code, &var slots, function_index(&program, \\\"maximum\\\"), 0, 0, 0, Int.max)\\n+    try! code.push(instruction(Op.Return))\\n+    program.functions[main_index].slots = move slots\\n+    program.functions[main_index].code = move code\\n+    try! verify(&program)\\n+    move program\\n+}\\n+\\n+func main() {\\n+    let program = contract_program()\\n+    let args = try! Env.args()\\n+    if args.len() == 2 && args[1] == \\\"emit-llvm\\\" {\\n+        print(try! llvm.emit_llvm(&program))\\n+        return\\n+    }\\n+    try! execute(&program, 5000)\\n+    print(\\\"scalar contract passed\\\")\\n+}\\n\",\"id\":\"SL-255\",\"request_id\":\"codex-sl255-m13-implementation-20260914\",\"title\":\"minivm M13: checked Scalar construction and readout; 369-case prototype gate passes\"}","request_id":"codex-sl255-m13-implementation-20260914","sequence":771,"timestamp":"1789395823"}
---
