---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-254.p1 (revision 1): minivm M11: fixed Result<T,E> + try/try! (branch tip 86d671aa)","created":"1789101377","id":"c1"}],"created":"1789086373","description":"SL-242 epic child, landing patch 13/14 (lands after M13's design patch in branch order... order is: this is branch tip commit 86d671aa, the final landing patch). Compiler-known fixed Result generic, qualified Ok/Err, payload matches, try/try! with full ownership cleanup on propagation, Result<Void,E>. Contract: prototypes/minivm/M11_RESULTS.md. Patch = branch commit 86d671aa.","id":"SL-254","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":514,"stage":"queued","status":"open","title":"minivm M11: fixed Result<T,E> — construction, payload match, try/try! propagation","updated":"1789101377"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086373","description":"SL-242 epic child, landing patch 13/14 (lands after M13's design patch in branch order... order is: this is branch tip commit 86d671aa, the final landing patch). Compiler-known fixed Result generic, qualified Ok/Err, payload matches, try/try! with full ownership cleanup on propagation, Result<Void,E>. Contract: prototypes/minivm/M11_RESULTS.md. Patch = branch commit 86d671aa.","id":"SL-254","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":416,"stage":"queued","status":"open","title":"minivm M11: fixed Result<T,E> — construction, payload match, try/try! propagation","updated":"1789086373"},"id":"SL-254","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 6093b128..0c9af8f8 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -13,15 +13,15 @@ the unchanged numeric `hex_value` helper (193 total cases).\\n M6 supplies direct-call scalar references, forwarding and mutation, including\\n borrowing scalar fields of direct records. M7 adds whole-record references,\\n nested field forwarding and basic shared/mutable methods, tested with a\\n-scalar-only `Lexer.advance`-shaped record (250 total cases). Strings, collections,\\n-Optional/Result payloads, intrinsic generic types and module integration remain\\n-outstanding. M8 adds owned String literals/locals/parameters/results/references,\\n+scalar-only `Lexer.advance`-shaped record (250 total cases). M8 adds owned String literals/locals/parameters/results/references,\\n byte length/access, equality and substring with explicit copy/drop rules (277\\n total cases). M9 adds String-valued records and String-pattern match (288 cases),\\n including unchanged keyword/suffix classification and the actual String-backed\\n Lexer.advance. M10 adds Optional payloads, contextual None/wrapping and value\\n conditional binding, including the unchanged Token/tok extraction (307 cases).\\n-Next are Result control flow, checked unsigned parsing and collections. The lexer has\\n+M11 adds fixed `Result<T,E>` construction and matching plus `try`/`try!`\\n+propagation, including owning payloads and Void success (336 cases). Next are\\n+checked unsigned parsing, `Scalar`, `StringBuilder`, and `Vector`. The lexer has\\n no imports, so its first whole-file test can combine the unchanged library with\\n a small wrapper without implementing general module loading.\\n The dependency groups below are an inventory, not the implementation commit order;\\n@@ -191,10 +191,9 @@ precise rejection. Keep all earlier milestone tests green at every gate.\\n    equality, qualified members, extensions, `&self`/`&var self`, field mutation,\\n    and named calls. Test an `advance`-shaped mutable receiver and reject mutation\\n    through `&self`, wrong labels, missing fields, and non-exhaustive enum match.\\n-4. **Optional/Result control flow.** Add `T?`, `Result<T,E>`, `None`, implicit\\n-   optional injection, `if let`, `try`, and `try!`. Test success/error\\n-   propagation with owning String payloads and verify `try!` traps. Reject `try`\\n-   where the enclosing result error is incompatible.\\n+4. **Optional/Result control flow — complete through M11.** `T?`, fixed\\n+   `Result<T,E>`, `None`, implicit injection, `if let`, `try`, and `try!` are\\n+   covered with owning String payloads, propagation, and trap behavior.\\n 5. **Opaque builder and vector generics.** Expose only the contracts listed\\n    above and implement correct ownership for `Vector<Token>` /\\n    `Vector<DocComment>`. Test growth, indexing bounds, push of structs containing\\ndiff --git a/prototypes/minivm/M11_RESULTS.md b/prototypes/minivm/M11_RESULTS.md\\nindex f5fa6fcf..d0e33489 100644\\n--- a/prototypes/minivm/M11_RESULTS.md\\n+++ b/prototypes/minivm/M11_RESULTS.md\\n@@ -1,6 +1,6 @@\\n # M11: Result construction, matching and propagation\\n \\n-Planned after the M10 Optional gate. This design uses M10's SumIR metadata and\\n+Implemented after the M10 Optional gate. This design uses M10's SumIR metadata and\\n disjoint tagged payload layout. It does not authorize changing production code.\\n \\n ## Source boundary\\n@@ -29,6 +29,9 @@ Support statement and value matches with `case Ok(name)` and `case Err(name)`,\\n including `_` payload patterns; Void Ok uses `case Ok`. A final whole-value `_`\\n may cover the remaining variant. Reject duplicate/missing arms and wrong arity.\\n Bindings are local payload snapshots; evaluate the scrutinee exactly once.\\n+Statement arms use blocks, matching the existing statement-match subset; value\\n+arms may use expressions or blocks. A final wildcard after both named variants\\n+is accepted and typechecked even though it is unreachable at runtime.\\n Retain complete String/record/Optional payloads before source mutation or scope\\n cleanup. This small match surface also lets the eventual lexer test wrapper\\n observe LexError line/column without requiring catch support.\\n@@ -72,6 +75,13 @@ cleanup; explicit and implicit return contexts; malformed type/constructor/\\n match/try rejects. Add direct representation/IR checks and ASan ownership probes.\\n Full regression gate remains required before a milestone commit.\\n \\n+## Review notes\\n+\\n+Regression cases cover all-returning value-match exits, all-arm numeric type\\n+selection, standalone Void try/try!, None rejection at Void returns, and\\n+Optional<Result> plus Result references. Constructor lookahead uses a named\\n+Token local to avoid the independently filed Python bootstrap bug SL-236.\\n+\\n ## Following slice\\n \\n String.to_uint is a separate small intrinsic returning UInt?, allowing the\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 7962e3e5..0f3a5629 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -66,8 +66,8 @@ shift counts. See [M1_NUMBERS.md](M1_NUMBERS.md) for the precise numeric contrac\\n Newlines separate statements. Omit the result annotation for Void functions;\\n `main()` must take no arguments and return Void. Value-returning functions need\\n a value on every continuing path, supplied by an explicit return or final expression.\\n-There are no imports, collections, generics, or concurrency in the\\n-accepted subset.\\n+There are no imports, collections, user-defined generics, or concurrency in the\\n+accepted subset. `Result<T,E>` is a compiler-known fixed generic type.\\n Semicolons remain unsupported. Boolean `&&`/`||` short-circuit, mutable integers\\n and fields support `+=`/`-=`/`*=`/`/=`/`%=`, and module `static` integer/Bool\\n constants accept literal and numeric-limit initializers. See\\n@@ -105,6 +105,13 @@ and value `if let` / `if var` evaluate once and bind copied payloads in the then\\n scope. Coalescing, chaining and force unwrap remain unsupported. See\\n [M10_OPTIONALS.md](M10_OPTIONALS.md).\\n \\n+Fixed `Result<T,E>` types support unambiguous payload wrapping, explicit qualified\\n+Ok/Err constructors, payload matches, and `try` / `try!`. Error propagation\\n+returns immediately with full ownership cleanup. Result<Void,E> success uses a\\n+bare return. Statement match arms require blocks; value arms may be expressions\\n+or blocks. Forced errors print `runtime error: try! failed`; cause formatting\\n+and catch blocks remain unsupported. See [M11_RESULTS.md](M11_RESULTS.md).\\n+\\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n only to VM execution; native code uses the host call stack and has no budget.\\n@@ -117,19 +124,19 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The first ten milestones\\n-cover 307 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The first eleven milestones\\n+cover 336 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw`,\\n `tests/record_contract.saw`, `tests/enum_contract.saw`,\\n-`tests/reference_contract.saw`, `tests/receiver_contract.saw`, and\\n-`tests/string_contract.saw`, `tests/owning_record_contract.saw`, and\\n-`tests/optional_contract.saw`; build and run them\\n+`tests/reference_contract.saw`, `tests/receiver_contract.saw`,\\n+`tests/string_contract.saw`, `tests/owning_record_contract.saw`,\\n+`tests/optional_contract.saw`, and `tests/result_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n `--section enums`, `--section values`, `--section references`, or\\n `--section receivers`, `--section strings`, `--section owning_records` or\\n-`--section optionals` for an isolated\\n+`--section optionals` or `--section results` for an isolated\\n integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\ndiff --git a/prototypes/minivm/examples/results/construction_wrapping.saw b/prototypes/minivm/examples/results/construction_wrapping.saw\\nnew file mode 100644\\nindex 00000000..37e22ff6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/construction_wrapping.saw\\n@@ -0,0 +1,27 @@\\n+func ok_value() -> Result<Int, String> { 7 }\\n+func err_value() -> Result<Int, String> { \\\"bad\\\" }\\n+func narrow() -> Result<UInt8, String> { 255 }\\n+func explicit_ok() -> Result<Int, String> { return 11 }\\n+func explicit_err() -> Result<Int, String> { return \\\"explicit\\\" }\\n+\\n+func show(value: Result<Int, String>) {\\n+    match value {\\n+        case Ok(n) -> { print(n) },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+}\\n+\\n+func main() {\\n+    show(ok_value())\\n+    show(err_value())\\n+    match narrow() {\\n+        case Ok(n) -> { print(n) },\\n+        case Err(_) -> { print(\\\"wrong\\\") }\\n+    }\\n+    let chosen: Result<Int, String> = if true { 9 } else { \\\"no\\\" }\\n+    show(chosen)\\n+    let copied = chosen\\n+    show(copied)\\n+    show(explicit_ok())\\n+    show(explicit_err())\\n+}\\ndiff --git a/prototypes/minivm/examples/results/loop_cleanup.saw b/prototypes/minivm/examples/results/loop_cleanup.saw\\nnew file mode 100644\\nindex 00000000..cd2269ff\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/loop_cleanup.saw\\n@@ -0,0 +1,29 @@\\n+func make(index: Int) -> Result<String, String> {\\n+    if index % 2 == 0 {\\n+        Result<String, String>.Ok(value: \\\"even\\\")\\n+    } else {\\n+        Result<String, String>.Err(error: \\\"odd\\\")\\n+    }\\n+}\\n+\\n+func stop() -> Result<String, String> {\\n+    let local = \\\"early\\\"\\n+    return Result<String, String>.Ok(value: local)\\n+}\\n+\\n+func main() {\\n+    var index = 0\\n+    while index < 200 {\\n+        let result = make(index)\\n+        match result {\\n+            case Ok(_) -> {},\\n+            case Err(_) -> {}\\n+        }\\n+        index = index + 1\\n+    }\\n+    match stop() {\\n+        case Ok(value) -> { print(value) },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+    print(index)\\n+}\\ndiff --git a/prototypes/minivm/examples/results/match_snapshot_ownership.saw b/prototypes/minivm/examples/results/match_snapshot_ownership.saw\\nnew file mode 100644\\nindex 00000000..a9650110\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/match_snapshot_ownership.saw\\n@@ -0,0 +1,22 @@\\n+struct Message { text: String, fallback: String? }\\n+\\n+func subject() -> Result<Message, String> {\\n+    print(\\\"subject\\\")\\n+    Message(text: \\\"old\\\", fallback: \\\"saved\\\")\\n+}\\n+\\n+func main() {\\n+    var source = subject()\\n+    match source {\\n+        case Ok(message) -> {\\n+            source = \\\"changed\\\"\\n+            print(message.text)\\n+            if let fallback = message.fallback { print(fallback) }\\n+        },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+    match source {\\n+        case Ok(_) -> { print(\\\"wrong\\\") },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/results/nested_optional.saw b/prototypes/minivm/examples/results/nested_optional.saw\\nnew file mode 100644\\nindex 00000000..afc1043d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/nested_optional.saw\\n@@ -0,0 +1,35 @@\\n+func one_more(value: Int?) -> Result<Int??, String> { value }\\n+func absent() -> Result<Int??, String> { None }\\n+func nested() -> Result<Result<Int, String>, Bool> {\\n+    Result<Result<Int, String>, Bool>.Ok(\\n+        value: Result<Int, String>.Err(error: \\\"inner\\\")\\n+    )\\n+}\\n+\\n+func show(value: Result<Int??, String>) {\\n+    match value {\\n+        case Ok(outer) -> {\\n+            if let inner = outer {\\n+                if let n = inner { print(n) } else { print(\\\"inner-none\\\") }\\n+            } else { print(\\\"outer-none\\\") }\\n+        },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+}\\n+\\n+func main() {\\n+    let n: Int? = None\\n+    show(one_more(n))\\n+    show(absent())\\n+    let seven: Int? = 7\\n+    show(one_more(seven))\\n+    match nested() {\\n+        case Ok(inner) -> {\\n+            match inner {\\n+                case Ok(n) -> { print(n) },\\n+                case Err(e) -> { print(e) }\\n+            }\\n+        },\\n+        case Err(_) -> { print(\\\"wrong\\\") }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/results/reject_ambiguous_literal.saw b/prototypes/minivm/examples/results/reject_ambiguous_literal.saw\\nnew file mode 100644\\nindex 00000000..db222b9b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_ambiguous_literal.saw\\n@@ -0,0 +1,2 @@\\n+func bad() -> Result<Int32, Int8> { 4 }\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_ambiguous_same_type.saw b/prototypes/minivm/examples/results/reject_ambiguous_same_type.saw\\nnew file mode 100644\\nindex 00000000..4f0051f8\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_ambiguous_same_type.saw\\n@@ -0,0 +1,2 @@\\n+func bad() -> Result<Int, Int> { 1 }\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_constructor_arity.saw b/prototypes/minivm/examples/results/reject_constructor_arity.saw\\nnew file mode 100644\\nindex 00000000..1a97eb06\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_constructor_arity.saw\\n@@ -0,0 +1,4 @@\\n+func bad() -> Result<Int, String> {\\n+    Result<Int, String>.Err()\\n+}\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_constructor_label.saw b/prototypes/minivm/examples/results/reject_constructor_label.saw\\nnew file mode 100644\\nindex 00000000..35a00464\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_constructor_label.saw\\n@@ -0,0 +1,4 @@\\n+func bad() -> Result<Int, String> {\\n+    Result<Int, String>.Ok(error: 1)\\n+}\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_match_duplicate.saw b/prototypes/minivm/examples/results/reject_match_duplicate.saw\\nnew file mode 100644\\nindex 00000000..68ba5157\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_match_duplicate.saw\\n@@ -0,0 +1,8 @@\\n+func value() -> Result<Int, String> { 1 }\\n+func main() {\\n+    match value() {\\n+        case Ok(n) -> { print(n) },\\n+        case Ok(_) -> { print(0) },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/results/reject_match_missing.saw b/prototypes/minivm/examples/results/reject_match_missing.saw\\nnew file mode 100644\\nindex 00000000..79f833fe\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_match_missing.saw\\n@@ -0,0 +1,4 @@\\n+func value() -> Result<Int, String> { 1 }\\n+func main() {\\n+    match value() { case Ok(n) -> { print(n) } }\\n+}\\ndiff --git a/prototypes/minivm/examples/results/reject_match_payload_arity.saw b/prototypes/minivm/examples/results/reject_match_payload_arity.saw\\nnew file mode 100644\\nindex 00000000..848fca02\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_match_payload_arity.saw\\n@@ -0,0 +1,7 @@\\n+func value() -> Result<Int, String> { 1 }\\n+func main() {\\n+    match value() {\\n+        case Ok -> { print(1) },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/results/reject_none_non_optional.saw b/prototypes/minivm/examples/results/reject_none_non_optional.saw\\nnew file mode 100644\\nindex 00000000..04962169\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_none_non_optional.saw\\n@@ -0,0 +1,2 @@\\n+func bad() -> Result<Int, String> { None }\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_reference_payload.saw b/prototypes/minivm/examples/results/reject_reference_payload.saw\\nnew file mode 100644\\nindex 00000000..9eef0075\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_reference_payload.saw\\n@@ -0,0 +1,2 @@\\n+func bad(value: &Int) -> Result<&Int, String> { value }\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_try_context.saw b/prototypes/minivm/examples/results/reject_try_context.saw\\nnew file mode 100644\\nindex 00000000..f08c6e23\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_try_context.saw\\n@@ -0,0 +1,3 @@\\n+func value() -> Result<Int, String> { 1 }\\n+func bad() -> Int { try value() }\\n+func main() { print(bad()) }\\ndiff --git a/prototypes/minivm/examples/results/reject_try_error_type.saw b/prototypes/minivm/examples/results/reject_try_error_type.saw\\nnew file mode 100644\\nindex 00000000..64a77472\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_try_error_type.saw\\n@@ -0,0 +1,3 @@\\n+func value() -> Result<Int, String> { \\\"bad\\\" }\\n+func bad() -> Result<Int, Bool> { try value() }\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_try_non_result.saw b/prototypes/minivm/examples/results/reject_try_non_result.saw\\nnew file mode 100644\\nindex 00000000..4651de1e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_try_non_result.saw\\n@@ -0,0 +1 @@\\n+func main() { print(try! 1) }\\ndiff --git a/prototypes/minivm/examples/results/reject_type_arity.saw b/prototypes/minivm/examples/results/reject_type_arity.saw\\nnew file mode 100644\\nindex 00000000..6101811c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_type_arity.saw\\n@@ -0,0 +1,2 @@\\n+func bad() -> Result<Int> { 1 }\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_void_constructor.saw b/prototypes/minivm/examples/results/reject_void_constructor.saw\\nnew file mode 100644\\nindex 00000000..1ed06cb2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_void_constructor.saw\\n@@ -0,0 +1,4 @@\\n+func bad() -> Result<Void, String> {\\n+    Result<Void, String>.Ok()\\n+}\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_void_error.saw b/prototypes/minivm/examples/results/reject_void_error.saw\\nnew file mode 100644\\nindex 00000000..222750cb\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_void_error.saw\\n@@ -0,0 +1,2 @@\\n+func bad() -> Result<Int, Void> { 1 }\\n+func main() { print(0) }\\ndiff --git a/prototypes/minivm/examples/results/reject_void_return_none.saw b/prototypes/minivm/examples/results/reject_void_return_none.saw\\nnew file mode 100644\\nindex 00000000..60269023\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/reject_void_return_none.saw\\n@@ -0,0 +1 @@\\n+func main() { return None }\\ndiff --git a/prototypes/minivm/examples/results/review_composition.saw b/prototypes/minivm/examples/results/review_composition.saw\\nnew file mode 100644\\nindex 00000000..baf05115\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/review_composition.saw\\n@@ -0,0 +1,44 @@\\n+struct Trouble { message: String }\\n+func maybe(ok: Bool) -> Result<String, Trouble> {\\n+    if ok { \\\"hello\\\" } else { Trouble(message: \\\"bad\\\") }\\n+}\\n+func mutate(value: &var Result<String, Trouble>) {\\n+    value = Trouble(message: \\\"changed\\\")\\n+}\\n+func observe(value: Result<String, Trouble>?) -> Int {\\n+    if let result = value {\\n+        match result {\\n+            case Ok(text) -> { text.len() },\\n+            case Err(error) -> { error.message.len() }\\n+        }\\n+    } else { 0 }\\n+}\\n+func no_operation() -> Result<Void, Trouble> { return }\\n+func relay(ok: Bool) -> Result<Int, Trouble> {\\n+    let keep = \\\"kept\\\"\\n+    try no_operation()\\n+    let text = try maybe(ok)\\n+    print(keep)\\n+    text.len()\\n+}\\n+func main() {\\n+    var source = maybe(true)\\n+    match source {\\n+        case Ok(text) -> {\\n+            mutate(&var source)\\n+            print(text)\\n+        },\\n+        case Err(_) -> {}\\n+    }\\n+    print(observe(source))\\n+    print(observe(None))\\n+    try! no_operation()\\n+    match relay(true) {\\n+        case Ok(n) -> { print(n) },\\n+        case Err(error) -> { print(error.message) }\\n+    }\\n+    match relay(false) {\\n+        case Ok(n) -> { print(n) },\\n+        case Err(error) -> { print(error.message) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/results/same_type_explicit.saw b/prototypes/minivm/examples/results/same_type_explicit.saw\\nnew file mode 100644\\nindex 00000000..7d47a9dd\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/same_type_explicit.saw\\n@@ -0,0 +1,18 @@\\n+func choose(ok: Bool) -> Result<Int, Int> {\\n+    if ok {\\n+        Result<Int, Int>.Ok(value: 42)\\n+    } else {\\n+        Result<Int, Int>.Err(error: -1)\\n+    }\\n+}\\n+\\n+func main() {\\n+    match choose(true) {\\n+        case Ok(n) -> { print(n) },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+    match choose(false) {\\n+        case Ok(n) -> { print(n) },\\n+        case Err(_) -> { print(999) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/results/try_nested_success.saw b/prototypes/minivm/examples/results/try_nested_success.saw\\nnew file mode 100644\\nindex 00000000..c96e40de\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/try_nested_success.saw\\n@@ -0,0 +1,8 @@\\n+func number() -> Result<Int, String> { 6 }\\n+func add(value: Int) -> Result<Int, Bool> { value + 1 }\\n+func nested() -> Result<Int, String> { try! add(try number()) }\\n+\\n+func main() {\\n+    let value = try! nested()\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/results/try_panic.saw b/prototypes/minivm/examples/results/try_panic.saw\\nnew file mode 100644\\nindex 00000000..d3a064d1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/try_panic.saw\\n@@ -0,0 +1,5 @@\\n+func fail() -> Result<Int, String> { \\\"hidden\\\" }\\n+\\n+func main() {\\n+    print(try! fail())\\n+}\\ndiff --git a/prototypes/minivm/examples/results/try_propagation.saw b/prototypes/minivm/examples/results/try_propagation.saw\\nnew file mode 100644\\nindex 00000000..f3f85c77\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/try_propagation.saw\\n@@ -0,0 +1,35 @@\\n+func leaf(ok: Bool) -> Result<String, String> {\\n+    if ok {\\n+        Result<String, String>.Ok(value: \\\"leaf\\\")\\n+    } else {\\n+        Result<String, String>.Err(error: \\\"stop\\\")\\n+    }\\n+}\\n+\\n+func later() -> String {\\n+    print(\\\"later\\\")\\n+    \\\"tail\\\"\\n+}\\n+\\n+func pair(left: String, right: String) -> String { left }\\n+\\n+func middle(ok: Bool) -> Result<String, String> {\\n+    Result<String, String>.Ok(value: pair(try leaf(ok), later()))\\n+}\\n+\\n+func outer(ok: Bool) -> Result<Int, String> {\\n+    let value = try middle(ok)\\n+    print(value)\\n+    8\\n+}\\n+\\n+func main() {\\n+    match outer(true) {\\n+        case Ok(n) -> { print(n) },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+    match outer(false) {\\n+        case Ok(n) -> { print(n) },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/results/try_void.saw b/prototypes/minivm/examples/results/try_void.saw\\nnew file mode 100644\\nindex 00000000..f229ab70\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/try_void.saw\\n@@ -0,0 +1,23 @@\\n+func action(ok: Bool) -> Result<Void, String> {\\n+    if not ok { return \\\"stopped\\\" }\\n+    return\\n+}\\n+\\n+func propagate(ok: Bool) -> Result<Void, String> {\\n+    try action(ok)\\n+    print(\\\"after\\\")\\n+    return\\n+}\\n+\\n+func main() {\\n+    match propagate(true) {\\n+        case Ok -> { print(\\\"propagated\\\") },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+    match propagate(false) {\\n+        case Ok -> { print(\\\"wrong\\\") },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+    try! action(true)\\n+    print(\\\"forced\\\")\\n+}\\ndiff --git a/prototypes/minivm/examples/results/value_match_control.saw b/prototypes/minivm/examples/results/value_match_control.saw\\nnew file mode 100644\\nindex 00000000..145233f7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/value_match_control.saw\\n@@ -0,0 +1,22 @@\\n+func choose(value: Result<Int, String>) -> Int {\\n+    match value {\\n+        case Ok(n) -> { return n },\\n+        case Err(_) -> { return -1 }\\n+    }\\n+}\\n+\\n+func select(value: Result<String, String>) -> String {\\n+    match value {\\n+        case Ok(text) -> { text },\\n+        case Err(error) -> { error }\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(choose(8))\\n+    print(choose(\\\"bad\\\"))\\n+    let ok = Result<String, String>.Ok(value: \\\"kept\\\")\\n+    let err = Result<String, String>.Err(error: \\\"failure\\\")\\n+    print(select(ok))\\n+    print(select(err))\\n+}\\ndiff --git a/prototypes/minivm/examples/results/value_match_widening.saw b/prototypes/minivm/examples/results/value_match_widening.saw\\nnew file mode 100644\\nindex 00000000..7470f001\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/value_match_widening.saw\\n@@ -0,0 +1,35 @@\\n+func choose_error(short: Bool) -> Result<Int64, Int8> {\\n+    if short {\\n+        Result<Int64, Int8>.Err(error: -8)\\n+    } else {\\n+        Result<Int64, Int8>.Ok(value: 64)\\n+    }\\n+}\\n+\\n+func widen(value: Result<Int64, Int8>) -> Int64 {\\n+    let selected = match value {\\n+        case Ok(wide) -> { wide },\\n+        case Err(narrow) -> { narrow },\\n+        case _ -> {\\n+            let medium: Int16 = 160\\n+            medium\\n+        }\\n+    }\\n+    selected\\n+}\\n+\\n+func wildcard(value: Result<Int8, String>) -> Int16 {\\n+    match value {\\n+        case Ok(n) -> { n },\\n+        case _ -> {\\n+            let fallback: Int16 = 16\\n+            fallback\\n+        }\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(widen(choose_error(false)))\\n+    print(widen(choose_error(true)))\\n+    print(wildcard(\\\"ignored\\\"))\\n+}\\ndiff --git a/prototypes/minivm/examples/results/void_success.saw b/prototypes/minivm/examples/results/void_success.saw\\nnew file mode 100644\\nindex 00000000..5f0b9cc6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/results/void_success.saw\\n@@ -0,0 +1,15 @@\\n+func step(ok: Bool) -> Result<Void, String> {\\n+    if not ok { return \\\"failed\\\" }\\n+    return\\n+}\\n+\\n+func main() {\\n+    match step(true) {\\n+        case Ok -> { print(\\\"ok\\\") },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+    match step(false) {\\n+        case Ok -> { print(\\\"wrong\\\") },\\n+        case Err(e) -> { print(e) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 460dc64c..904cad5d 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -183,6 +183,11 @@ extension Compiler {\\n     }\\n \\n     func parse_type(&var self) -> Result<ValueType, ProtoError> {\\n+        self.parse_type_inner(0)\\n+    }\\n+\\n+    func parse_type_inner(&var self, type_depth: Int) -> Result<ValueType, ProtoError> {\\n+        if type_depth >= 128 { return self.fail(\\\"type nesting limit exceeded\\\") }\\n         if self.at(TokenKind.Ampersand) {\\n             let amp = self.take()\\n             var mutable = false\\n@@ -194,18 +199,31 @@ extension Compiler {\\n                 let nested = self.token()\\n                 return problem(\\\"references to references are not supported\\\", nested.line, nested.col)\\n             }\\n-            let target = try self.parse_type()\\n+            let target = try self.parse_type_inner(type_depth + 1)\\n             if reference_type(target) { return problem(\\\"references to references are not supported\\\", amp.line, amp.col) }\\n             var supported = scalar_type(target)\\n             match target { case Record(_) -> { supported = true }, case _ -> {} }\\n             if target == ValueType.String { supported = true }\\n             match target { case Optional(_) -> { supported = true }, case _ -> {} }\\n+            match target { case Result(_) -> { supported = true }, case _ -> {} }\\n             if not supported { return problem(\\\"only data references are supported\\\", amp.line, amp.col) }\\n             return self.intern_reference(target, mutable)\\n         }\\n         let t = try self.expect(TokenKind.Ident, \\\"expected type\\\")\\n         var ty = ValueType.Void\\n-        if t.value == \\\"Void\\\" { ty = ValueType.Void }\\n+        if t.value == \\\"Result\\\" {\\n+            let _ = try self.expect(TokenKind.Lt, \\\"Result requires exactly two type arguments\\\")\\n+            let success = try self.parse_type_inner(type_depth + 1)\\n+            let _ = try self.expect(TokenKind.Comma, \\\"Result requires exactly two type arguments\\\")\\n+            let error = try self.parse_type_inner(type_depth + 1)\\n+            let _ = try self.expect(TokenKind.Gt, \\\"Result requires exactly two type arguments\\\")\\n+            if error == ValueType.Void { return problem(\\\"Result error type cannot be Void\\\", t.line, t.col) }\\n+            if reference_type(success) || reference_type(error) {\\n+                return problem(\\\"Result reference payloads are not supported\\\", t.line, t.col)\\n+            }\\n+            ty = self.intern_result(success, error)\\n+        }\\n+        else if t.value == \\\"Void\\\" { ty = ValueType.Void }\\n         else if t.value == \\\"Bool\\\" { ty = ValueType.Bool }\\n         else if t.value == \\\"String\\\" { ty = ValueType.String }\\n         else if let integer = named_integer_type(t.value) { ty = integer }\\n@@ -247,6 +265,18 @@ extension Compiler {\\n         ValueType.Optional(self.sums.len() - 1)\\n     }\\n \\n+    func intern_result(&var self, payload: ValueType, error: ValueType) -> ValueType {\\n+        var i = 0\\n+        while i < self.sums.len() {\\n+            if self.sums[i].kind == SumKind.Result && self.sums[i].payload == payload && self.sums[i].error == error {\\n+                return ValueType.Result(i)\\n+            }\\n+            i += 1\\n+        }\\n+        try! self.sums.push(SumIR(kind: SumKind.Result, payload: payload, error: error))\\n+        ValueType.Result(self.sums.len() - 1)\\n+    }\\n+\\n     func intern_reference(&var self, target: ValueType, mutable: Bool) -> ValueType {\\n         var i = 0\\n         while i < self.references.len() {\\n@@ -312,7 +342,12 @@ extension Compiler {\\n                 }\\n                 \\\"Optional\\\"\\n             },\\n-            case Result(_) -> \\\"Result\\\",\\n+            case Result(index) -> {\\n+                if index >= 0 && index < self.sums.len() {\\n+                    return \\\"Result<{self.display_type(self.sums[index].payload)}, {self.display_type(self.sums[index].error)}>\\\"\\n+                }\\n+                \\\"Result\\\"\\n+            },\\n             case _ -> type_name(ty)\\n         }\\n     }\\n@@ -1008,6 +1043,96 @@ extension Compiler {\\n         expr_value(target, dst, false)\\n     }\\n \\n+    func can_transfer(&self, v: &ExprValue, target: ValueType) -> Bool {\\n+        if v.none_literal {\\n+            match target {\\n+                case Optional(_) -> { return true },\\n+                case Result(index) -> {\\n+                    if index < 0 || index >= self.sums.len() { return false }\\n+                    let ok = self.sums[index].payload != ValueType.Void && self.can_transfer(&v, self.sums[index].payload)\\n+                    let err = self.can_transfer(&v, self.sums[index].error)\\n+                    return ok != err\\n+                },\\n+                case _ -> { return false }\\n+            }\\n+        }\\n+        if v.ty == target { return true }\\n+        if v.literal && integer_type(target) { return self.literal_fits_target(&v, target) }\\n+        if integer_widens(v.ty, target) { return true }\\n+        match target {\\n+            case Optional(index) -> {\\n+                if index < 0 || index >= self.sums.len() { return false }\\n+                return self.can_transfer(&v, self.sums[index].payload)\\n+            },\\n+            case Result(index) -> {\\n+                if index < 0 || index >= self.sums.len() { return false }\\n+                let ok = self.sums[index].payload != ValueType.Void && self.can_transfer(&v, self.sums[index].payload)\\n+                let err = self.can_transfer(&v, self.sums[index].error)\\n+                return ok != err\\n+            },\\n+            case _ -> { return false }\\n+        }\\n+    }\\n+\\n+    func construct_result(&var self, v: ExprValue, target: ValueType, ok: Bool,\\n+                          t: &Token) -> Result<ExprValue, ProtoError> {\\n+        let payload = if ok { try result_payload(&self.sums, target) } else { try result_error(&self.sums, target) }\\n+        if payload == ValueType.Void { return problem(\\\"Result.Ok requires a value payload\\\", t.line, t.col) }\\n+        let converted = try self.transfer(v, payload, &t)\\n+        let dst = try self.new_value(target)\\n+        let layout = try value_layout(&self.records, &self.sums, target)\\n+        var i = 0\\n+        while i < layout.len() {\\n+            if layout[i] == ValueType.String {\\n+                let drop = self.emit_at(Op.Drop, t.line, t.col)\\n+                self.code[drop].a = dst + i\\n+            } else {\\n+                let zero = self.emit_at(Op.Const, t.line, t.col)\\n+                self.code[zero].dst = dst + i\\n+                self.code[zero].immediate = 0\\n+            }\\n+            i += 1\\n+        }\\n+        let tag = self.emit_at(Op.Const, t.line, t.col)\\n+        self.code[tag].dst = dst\\n+        self.code[tag].immediate = if ok { 1 } else { 0 }\\n+        var offset = 1\\n+        if not ok { offset += (try value_layout(&self.records, &self.sums, try result_payload(&self.sums, target))).len() }\\n+        try self.emit_copy_value(dst + offset, converted.slot, payload, &t)\\n+        expr_value(target, dst, false)\\n+    }\\n+\\n+    func construct_void_ok(&var self, target: ValueType, t: &Token) -> Result<ExprValue, ProtoError> {\\n+        let payload = try result_payload(&self.sums, target)\\n+        if payload != ValueType.Void { return problem(\\\"non-Void return requires a value\\\", t.line, t.col) }\\n+        let dst = try self.new_value(target)\\n+        let layout = try value_layout(&self.records, &self.sums, target)\\n+        var i = 0\\n+        while i < layout.len() {\\n+            if layout[i] == ValueType.String {\\n+                let drop = self.emit_at(Op.Drop, t.line, t.col)\\n+                self.code[drop].a = dst + i\\n+            } else {\\n+                let zero = self.emit_at(Op.Const, t.line, t.col)\\n+                self.code[zero].dst = dst + i\\n+                self.code[zero].immediate = 0\\n+            }\\n+            i += 1\\n+        }\\n+        let tag = self.emit_at(Op.Const, t.line, t.col)\\n+        self.code[tag].dst = dst\\n+        self.code[tag].immediate = 1\\n+        expr_value(target, dst, false)\\n+    }\\n+\\n+    func emit_function_return(&var self, src: Int, t: &Token) -> Result<Void, ProtoError> {\\n+        let preserve_end = if src < 0 { -1 } else { src + (try value_layout(&self.records, &self.sums, self.current_result)).len() }\\n+        self.cleanup_function(src, preserve_end, &t)\\n+        let at = self.emit_at(Op.Return, t.line, t.col)\\n+        self.code[at].a = src\\n+        return\\n+    }\\n+\\n     func wrap_optional(&var self, v: ExprValue, target: ValueType,\\n                        t: &Token) -> Result<ExprValue, ProtoError> {\\n         let payload = try optional_payload(&self.sums, target)\\n@@ -1024,10 +1149,10 @@ extension Compiler {\\n         if v.none_literal {\\n             match target {\\n                 case Optional(_) -> { return self.initialize_optional_none(target, &t) },\\n-                case _ -> { return problem(\\\"None requires an expected Optional type\\\", t.line, t.col) }\\n+                case _ -> {}\\n             }\\n         }\\n-        if v.ty == target {\\n+        if v.ty == target && not v.none_literal {\\n             if v.literal && not self.literal_fits_target(&v, target) {\\n                 return problem(\\\"integer literal does not fit {type_name(target)}\\\", t.line, t.col)\\n             }\\n@@ -1035,8 +1160,18 @@ extension Compiler {\\n         }\\n         match target {\\n             case Optional(_) -> { return self.wrap_optional(v, target, &t) },\\n+            case Result(index) -> {\\n+                let success = self.sums[index].payload\\n+                let error = self.sums[index].error\\n+                let ok = success != ValueType.Void && self.can_transfer(&v, success)\\n+                let err = self.can_transfer(&v, error)\\n+                if ok && err { return problem(\\\"ambiguous Result auto-wrap\\\", t.line, t.col) }\\n+                if ok { return self.construct_result(v, target, true, &t) }\\n+                if err { return self.construct_result(v, target, false, &t) }\\n+            },\\n             case _ -> {}\\n         }\\n+        if v.none_literal { return problem(\\\"None requires an expected Optional type\\\", t.line, t.col) }\\n         if v.literal && integer_type(target) { return self.adopt_literal(v, target, &t) }\\n         if integer_widens(v.ty, target) {\\n             let dst = self.new_slot(target)\\n@@ -1170,6 +1305,14 @@ extension Compiler {\\n             self.depth -= 1\\n             return value\\n         }\\n+        var result_constructor = false\\n+        let constructor_token = self.token()\\n+        if self.at(TokenKind.Ident) && constructor_token.value == \\\"Result\\\" && self.pos + 1 < self.tokens.len() {\\n+            result_constructor = self.tokens[self.pos + 1].kind == TokenKind.Lt\\n+        }\\n+        if result_constructor {\\n+            return self.parse_result_constructor()\\n+        }\\n         if self.at(TokenKind.Ident) || self.at(TokenKind.SelfKw) {\\n             let candidate = self.token()\\n             if self.binding_index(candidate.value) >= 0 {\\n@@ -1239,6 +1382,36 @@ extension Compiler {\\n         self.fail(\\\"expected expression; this syntax is unsupported\\\")\\n     }\\n \\n+    func parse_result_constructor(&var self) -> Result<ExprValue, ProtoError> {\\n+        let anchor = self.token()\\n+        let result_ty = try self.parse_type()\\n+        let _ = try self.expect(TokenKind.Dot, \\\"expected `.Ok` or `.Err` after Result type\\\")\\n+        let variant = try self.expect(TokenKind.Ident, \\\"expected `Ok` or `Err` Result constructor\\\")\\n+        let ok = variant.value == \\\"Ok\\\"\\n+        if not ok && variant.value != \\\"Err\\\" { return problem(\\\"unknown Result constructor `{variant.value}`\\\", variant.line, variant.col) }\\n+        let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after Result constructor\\\")\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        if self.at(TokenKind.RParen) {\\n+            return problem(if ok { \\\"Result.Ok requires a value payload\\\" } else { \\\"Result.Err requires an error payload\\\" }, variant.line, variant.col)\\n+        }\\n+        let label = try self.expect(TokenKind.Ident, if ok { \\\"Result.Ok argument label must be `value`\\\" } else { \\\"Result.Err argument label must be `error`\\\" })\\n+        let wanted = if ok { \\\"value\\\" } else { \\\"error\\\" }\\n+        if label.value != wanted { return problem(\\\"Result.{variant.value} argument label must be `{wanted}`\\\", label.line, label.col) }\\n+        let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after Result constructor argument label\\\")\\n+        self.expr_space()\\n+        let payload = if ok { try result_payload(&self.sums, result_ty) } else { try result_error(&self.sums, result_ty) }\\n+        if ok && payload == ValueType.Void { return problem(\\\"Result.Ok requires a value payload\\\", variant.line, variant.col) }\\n+        let value = try self.parse_expected_expr(payload)\\n+        self.skip_newlines()\\n+        if self.at(TokenKind.Comma) { return problem(\\\"Result.{variant.value} accepts exactly one argument\\\", variant.line, variant.col) }\\n+        let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after Result constructor\\\")\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        self.construct_result(value, result_ty, ok, &anchor)\\n+    }\\n+\\n     func field_index(&self, record: Int, name: String) -> Int {\\n         var i = 0\\n         while i < self.records[record].field_names.len() {\\n@@ -1756,6 +1929,7 @@ extension Compiler {\\n \\n     func parse_unary_inner(&var self) -> Result<ExprValue, ProtoError> {\\n         self.expr_space()\\n+        if self.at(TokenKind.Try) { return self.parse_try() }\\n         if self.at(TokenKind.Exclaim) || self.at(TokenKind.Not_) {\\n             let t = self.take()\\n             self.expr_space()\\n@@ -1819,6 +1993,43 @@ extension Compiler {\\n         self.parse_cast()\\n     }\\n \\n+    func parse_try(&var self) -> Result<ExprValue, ProtoError> {\\n+        let t = self.take()\\n+        var forced = false\\n+        if self.at(TokenKind.Exclaim) {\\n+            let _ = self.take()\\n+            forced = true\\n+        }\\n+        let operand = try self.parse_expr()\\n+        var result_index = -1\\n+        match operand.ty { case Result(index) -> { result_index = index }, case _ -> {} }\\n+        if result_index < 0 { return problem(\\\"try operand must be a Result\\\", t.line, t.col) }\\n+        let value = self.sums[result_index].payload\\n+        let error = self.sums[result_index].error\\n+        if not forced {\\n+            var caller_index = -1\\n+            match self.current_result { case Result(index) -> { caller_index = index }, case _ -> {} }\\n+            if caller_index < 0 { return problem(\\\"try requires a Result-returning function\\\", t.line, t.col) }\\n+            if self.sums[caller_index].error != error {\\n+                return problem(\\\"try error type does not match function result\\\", t.line, t.col)\\n+            }\\n+        }\\n+        let branch = self.emit_at(Op.Branch, t.line, t.col)\\n+        self.code[branch].a = operand.slot\\n+        if forced {\\n+            let _ = self.emit_at(Op.Panic, t.line, t.col)\\n+        } else {\\n+            let success_layout = try value_layout(&self.records, &self.sums, value)\\n+            let error_value = expr_value(error, operand.slot + 1 + success_layout.len(), false)\\n+            let propagated = try self.construct_result(error_value, self.current_result, false, &t)\\n+            try self.emit_function_return(propagated.slot, &t)\\n+        }\\n+        self.code[branch].target = self.code.len()\\n+        if value == ValueType.Void { return expr_value(ValueType.Void, -1, true) }\\n+        let success = expr_value(value, operand.slot + 1, false)\\n+        self.snapshot(success, &t)\\n+    }\\n+\\n     func parse_cast(&var self) -> Result<ExprValue, ProtoError> {\\n         let value = try self.parse_primary()\\n         self.parse_casts(value)\\n@@ -2308,12 +2519,18 @@ extension Compiler {\\n \\n     func stage_optional_binding(&var self, name: Token, mutable: Bool,\\n                                 subject: &ExprValue, payload: ValueType) -> Result<Void, ProtoError> {\\n+        self.stage_payload_binding(name, mutable, &subject, payload, 1)\\n+    }\\n+\\n+    func stage_payload_binding(&var self, name: Token, mutable: Bool,\\n+                               subject: &ExprValue, payload: ValueType,\\n+                               offset: Int) -> Result<Void, ProtoError> {\\n         if name.value == \\\"_\\\" { return }\\n         // The following block owns this payload snapshot. Stage its lexical depth\\n         // before the block parser enters so String leaves are dropped at that edge.\\n         self.scope_depth += 1\\n         let dst = try self.new_value(payload)\\n-        try self.emit_copy_value(dst, subject.slot + 1, payload, &name)\\n+        try self.emit_copy_value(dst, subject.slot + offset, payload, &name)\\n         self.scope_depth -= 1\\n         try! self.bindings.push(Binding(name: name.value, ty: payload, slot: dst,\\n                                         mutable: mutable, depth: self.depth + 1))\\n@@ -2498,6 +2715,10 @@ extension Compiler {\\n         let mt = self.take()\\n         var subject = try self.parse_expr()\\n         subject = try self.snapshot(subject, &mt)\\n+        match subject.ty {\\n+            case Result(index) -> { return self.parse_result_match_value(mt, subject, index, expected, has_expected) },\\n+            case _ -> {}\\n+        }\\n         var ei = -1\\n         let string_match = subject.ty == ValueType.String\\n         match subject.ty {\\n@@ -2660,6 +2881,137 @@ extension Compiler {\\n         expr_value(target, dst, false)\\n     }\\n \\n+    func parse_result_match_value(&var self, mt: Token, subject: ExprValue, index: Int,\\n+                                  expected: ValueType, has_expected: Bool) -> Result<ExprValue, ProtoError> {\\n+        let success = self.sums[index].payload\\n+        let error = self.sums[index].error\\n+        let success_words = (try value_layout(&self.records, &self.sums, success)).len()\\n+        let _ = try self.expect(TokenKind.LBrace, \\\"expected `\\\\{` after match subject\\\")\\n+        self.skip_newlines()\\n+        var covered_ok = false\\n+        var covered_err = false\\n+        var wildcard = false\\n+        var pending_false = -1\\n+        var exits = Vector<Int>()\\n+        var values = Vector<ExprValue>()\\n+        var all_return = true\\n+        while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n+            if wildcard { return self.fail(\\\"wildcard match arm must be final\\\") }\\n+            if pending_false >= 0 { self.code[pending_false].target = self.code.len() }\\n+            let _ = try self.expect(TokenKind.Case, \\\"expected `case` in match\\\")\\n+            let pattern = try self.expect(TokenKind.Ident, \\\"expected Result match pattern\\\")\\n+            var variant = -1\\n+            if pattern.value == \\\"Ok\\\" { variant = 1 }\\n+            else if pattern.value == \\\"Err\\\" { variant = 0 }\\n+            else if pattern.value == \\\"_\\\" { wildcard = true }\\n+            else { return problem(\\\"Result match patterns must be Ok, Err, or `_`\\\", pattern.line, pattern.col) }\\n+            if variant == 1 && covered_ok { return problem(\\\"duplicate Result match arm\\\", pattern.line, pattern.col) }\\n+            if variant == 0 && covered_err { return problem(\\\"duplicate Result match arm\\\", pattern.line, pattern.col) }\\n+            var binding = Token(kind: TokenKind.Ident, value: \\\"_\\\", line: pattern.line, col: pattern.col, suffix: None)\\n+            var payload = ValueType.Void\\n+            var offset = 0\\n+            if variant >= 0 {\\n+                payload = if variant == 1 { success } else { error }\\n+                offset = if variant == 1 { 1 } else { 1 + success_words }\\n+                if payload == ValueType.Void {\\n+                    if self.at(TokenKind.LParen) { return problem(\\\"Result Void Ok pattern does not accept a payload\\\", pattern.line, pattern.col) }\\n+                } else {\\n+                    if not self.at(TokenKind.LParen) { return problem(\\\"Result {pattern.value} pattern requires a payload\\\", pattern.line, pattern.col) }\\n+                    let _ = self.take()\\n+                    binding = try self.expect(TokenKind.Ident, \\\"expected Result payload binding\\\")\\n+                    let _ = try self.expect(TokenKind.RParen, \\\"Result match payload pattern accepts one binding\\\")\\n+                }\\n+                if variant == 1 { covered_ok = true } else { covered_err = true }\\n+                let condition = if variant == 1 { subject.slot } else {\\n+                    let dst = self.new_slot(ValueType.Bool)\\n+                    let invert = self.emit_at(Op.Not, pattern.line, pattern.col)\\n+                    self.code[invert].dst = dst\\n+                    self.code[invert].a = subject.slot\\n+                    dst\\n+                }\\n+                let branch = self.emit_at(Op.Branch, pattern.line, pattern.col)\\n+                self.code[branch].a = condition\\n+                pending_false = self.emit_at(Op.Jump, pattern.line, pattern.col)\\n+                self.code[branch].target = self.code.len()\\n+            } else { pending_false = -1 }\\n+            let _ = try self.expect(TokenKind.Arrow, \\\"expected `->` after match pattern\\\")\\n+            if variant >= 0 && payload != ValueType.Void {\\n+                try self.stage_payload_binding(binding, false, &subject, payload, offset)\\n+            }\\n+            var arm = BlockValue(has_value: false, value: returning_value(), always_returns: false)\\n+            if self.at(TokenKind.LBrace) {\\n+                let _ = self.take()\\n+                arm = try self.parse_value_block(expected, has_expected)\\n+            } else {\\n+                self.depth += 1\\n+                self.scope_depth += 1\\n+                let value = if has_expected { try self.parse_expected_expr(expected) } else { try self.parse_expr() }\\n+                let end = value.slot + (try value_layout(&self.records, &self.sums, value.ty)).len()\\n+                self.cleanup_scope(value.slot, end, &pattern)\\n+                self.leave_scope()\\n+                self.scope_depth -= 1\\n+                self.depth -= 1\\n+                arm = BlockValue(has_value: not value.always_returns, value: value, always_returns: value.always_returns)\\n+            }\\n+            if arm.always_returns {} else {\\n+                all_return = false\\n+                if not arm.has_value { return problem(\\\"continuing value branch requires a tail expression\\\", pattern.line, pattern.col) }\\n+                try! values.push(arm.value)\\n+                try! exits.push(self.emit_at(Op.Jump, mt.line, mt.col))\\n+            }\\n+            self.skip_newlines()\\n+            if self.at(TokenKind.Comma) {\\n+                let _ = self.take()\\n+                self.skip_newlines()\\n+            }\\n+        }\\n+        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace for match\\\")\\n+        if not wildcard && (not covered_ok || not covered_err) { return problem(\\\"non-exhaustive Result match\\\", mt.line, mt.col) }\\n+        if all_return {\\n+            if pending_false >= 0 { self.code[pending_false].target = self.code.len() }\\n+            return returning_value()\\n+        }\\n+        var target = if has_expected { expected } else { values[0].ty }\\n+        if not has_expected {\\n+            var candidate = -1\\n+            var saw_typed = false\\n+            var n = 0\\n+            while n < values.len() {\\n+                if not values[n].literal {\\n+                    saw_typed = true\\n+                    var accepts = true\\n+                    var j = 0\\n+                    while j < values.len() {\\n+                        if values[j].ty != values[n].ty && not values[j].literal && not integer_widens(values[j].ty, values[n].ty) { accepts = false }\\n+                        j += 1\\n+                    }\\n+                    if accepts {\\n+                        candidate = n\\n+                        break\\n+                    }\\n+                }\\n+                n += 1\\n+            }\\n+            if candidate >= 0 { target = values[candidate].ty }\\n+            else if saw_typed { return problem(\\\"control-flow branches have incompatible types\\\", mt.line, mt.col) }\\n+        }\\n+        let dst = try self.new_value(target)\\n+        var joins = Vector<Int>()\\n+        var n = 0\\n+        while n < values.len() {\\n+            try self.append_value_exit(exits[n], values[n], target, dst, &mt, &var joins)\\n+            n += 1\\n+        }\\n+        let continuation = self.code.len()\\n+        if pending_false >= 0 { self.code[pending_false].target = continuation }\\n+        n = 0\\n+        while n < joins.len() {\\n+            self.code[joins[n]].target = continuation\\n+            n += 1\\n+        }\\n+        expr_value(target, dst, false)\\n+    }\\n+\\n     func parse_block(&var self) -> Result<Bool, ProtoError> {\\n         if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n         self.depth += 1\\n@@ -2683,6 +3035,10 @@ extension Compiler {\\n         let mt = self.take()\\n         var subject = try self.parse_expr()\\n         subject = try self.snapshot(subject, &mt)\\n+        match subject.ty {\\n+            case Result(index) -> { return self.parse_result_match_statement(mt, subject, index) },\\n+            case _ -> {}\\n+        }\\n         var ei = -1\\n         let string_match = subject.ty == ValueType.String\\n         match subject.ty {\\n@@ -2799,6 +3155,91 @@ extension Compiler {\\n         all_return\\n     }\\n \\n+    func parse_result_match_statement(&var self, mt: Token, subject: ExprValue,\\n+                                      index: Int) -> Result<Bool, ProtoError> {\\n+        let success = self.sums[index].payload\\n+        let error = self.sums[index].error\\n+        let success_words = (try value_layout(&self.records, &self.sums, success)).len()\\n+        let _ = try self.expect(TokenKind.LBrace, \\\"expected `\\\\{` after match subject\\\")\\n+        self.skip_newlines()\\n+        var covered_ok = false\\n+        var covered_err = false\\n+        var wildcard = false\\n+        var pending_false = -1\\n+        var joins = Vector<Int>()\\n+        var all_return = true\\n+        var arms = 0\\n+        while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n+            if wildcard { return self.fail(\\\"wildcard match arm must be final\\\") }\\n+            if pending_false >= 0 { self.code[pending_false].target = self.code.len() }\\n+            let _ = try self.expect(TokenKind.Case, \\\"expected `case` in match\\\")\\n+            let pattern = try self.expect(TokenKind.Ident, \\\"expected Result match pattern\\\")\\n+            var variant = -1\\n+            if pattern.value == \\\"Ok\\\" { variant = 1 }\\n+            else if pattern.value == \\\"Err\\\" { variant = 0 }\\n+            else if pattern.value == \\\"_\\\" { wildcard = true }\\n+            else { return problem(\\\"Result match patterns must be Ok, Err, or `_`\\\", pattern.line, pattern.col) }\\n+            if variant == 1 && covered_ok { return problem(\\\"duplicate Result match arm\\\", pattern.line, pattern.col) }\\n+            if variant == 0 && covered_err { return problem(\\\"duplicate Result match arm\\\", pattern.line, pattern.col) }\\n+            var binding = Token(kind: TokenKind.Ident, value: \\\"_\\\", line: pattern.line, col: pattern.col, suffix: None)\\n+            var payload = ValueType.Void\\n+            var offset = 0\\n+            if variant >= 0 {\\n+                payload = if variant == 1 { success } else { error }\\n+                offset = if variant == 1 { 1 } else { 1 + success_words }\\n+                if payload == ValueType.Void {\\n+                    if self.at(TokenKind.LParen) { return problem(\\\"Result Void Ok pattern does not accept a payload\\\", pattern.line, pattern.col) }\\n+                } else {\\n+                    if not self.at(TokenKind.LParen) { return problem(\\\"Result {pattern.value} pattern requires a payload\\\", pattern.line, pattern.col) }\\n+                    let _ = self.take()\\n+                    binding = try self.expect(TokenKind.Ident, \\\"expected Result payload binding\\\")\\n+                    let _ = try self.expect(TokenKind.RParen, \\\"Result match payload pattern accepts one binding\\\")\\n+                }\\n+                if variant == 1 { covered_ok = true } else { covered_err = true }\\n+                let condition = if variant == 1 { subject.slot } else {\\n+                    let dst = self.new_slot(ValueType.Bool)\\n+                    let invert = self.emit_at(Op.Not, pattern.line, pattern.col)\\n+                    self.code[invert].dst = dst\\n+                    self.code[invert].a = subject.slot\\n+                    dst\\n+                }\\n+                let branch = self.emit_at(Op.Branch, pattern.line, pattern.col)\\n+                self.code[branch].a = condition\\n+                pending_false = self.emit_at(Op.Jump, pattern.line, pattern.col)\\n+                self.code[branch].target = self.code.len()\\n+            } else { pending_false = -1 }\\n+            let _ = try self.expect(TokenKind.Arrow, \\\"expected `->` after match pattern\\\")\\n+            let _ = try self.expect(TokenKind.LBrace, \\\"match arms require statement blocks\\\")\\n+            if variant >= 0 && payload != ValueType.Void {\\n+                try self.stage_payload_binding(binding, false, &subject, payload, offset)\\n+            }\\n+            let returns = try self.parse_block()\\n+            if not returns {\\n+                all_return = false\\n+                let join = self.emit_at(Op.Jump, mt.line, mt.col)\\n+                try! joins.push(join)\\n+            }\\n+            arms += 1\\n+            self.skip_newlines()\\n+            if self.at(TokenKind.Comma) {\\n+                let _ = self.take()\\n+                self.skip_newlines()\\n+            }\\n+        }\\n+        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace for match\\\")\\n+        if arms == 0 || (not wildcard && (not covered_ok || not covered_err)) {\\n+            return problem(\\\"non-exhaustive Result match\\\", mt.line, mt.col)\\n+        }\\n+        let continuation = self.code.len()\\n+        if pending_false >= 0 { self.code[pending_false].target = continuation }\\n+        var i = 0\\n+        while i < joins.len() {\\n+            self.code[joins[i]].target = continuation\\n+            i += 1\\n+        }\\n+        all_return\\n+    }\\n+\\n     func parse_statement(&var self) -> Result<Bool, ProtoError> {\\n         let statement_token = self.token()\\n         if self.at(TokenKind.Semicolon) { return self.fail(\\\"semicolons are not supported\\\") }\\n@@ -2834,11 +3275,13 @@ extension Compiler {\\n                 var value = try self.parse_expected_expr(self.current_result)\\n                 if value.always_returns { return true }\\n                 src = value.slot\\n-            } else if self.current_result != ValueType.Void { return problem(\\\"non-Void return requires a value\\\", t.line, t.col) }\\n-            let preserve_end = if src < 0 { -1 } else { src + (try value_layout(&self.records, &self.sums, self.current_result)).len() }\\n-            self.cleanup_function(src, preserve_end, &t)\\n-            let at = self.emit_at(Op.Return, t.line, t.col)\\n-            self.code[at].a = src\\n+            } else if self.current_result != ValueType.Void {\\n+                match self.current_result {\\n+                    case Result(_) -> { src = (try self.construct_void_ok(self.current_result, &t)).slot },\\n+                    case _ -> { return problem(\\\"non-Void return requires a value\\\", t.line, t.col) }\\n+                }\\n+            }\\n+            try self.emit_function_return(src, &t)\\n             return true\\n         }\\n         if self.at(TokenKind.If) {\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex 3cd071c7..b97ceb09 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -909,6 +909,9 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 temp_name(&var out, instruction_index, \\\"retagg{result_layout.len() - 1}\\\")\\n                 put(&var out, \\\"\\\\n\\\")\\n             }\\n+        },\\n+        case Panic -> {\\n+            put(&var out, \\\"  call void @minivm_try_failed()\\\\n  unreachable\\\\n\\\")\\n         }\\n     }\\n     put(&var out, \\\"\\\\n\\\")\\n@@ -1020,6 +1023,7 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     put(&var out, \\\"@.divzero = private unnamed_addr constant [32 x i8] c\\\\\\\"runtime error: division by zero\\\\\\\\00\\\\\\\"\\\\n\\\\n\\\")\\n     put(&var out, \\\"@.cast = private unnamed_addr constant [41 x i8] c\\\\\\\"runtime error: integer cast out of range\\\\\\\\00\\\\\\\"\\\\n\\\")\\n     put(&var out, \\\"@.shift = private unnamed_addr constant [34 x i8] c\\\\\\\"runtime error: shift out of range\\\\\\\\00\\\\\\\"\\\\n\\\\n\\\")\\n+    put(&var out, \\\"@.try_failed = private unnamed_addr constant [27 x i8] c\\\\\\\"runtime error: try! failed\\\\\\\\00\\\\\\\"\\\\n\\\\n\\\")\\n     emit_string_constants(&var out, &program.strings)\\n     put(&var out, \\\"declare i32 @printf(ptr, ...)\\\\ndeclare i32 @puts(ptr)\\\\ndeclare void @exit(i32)\\\\n\\\")\\n     put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.sadd.with.overflow.i64(i64, i64)\\\\n\\\")\\n@@ -1033,6 +1037,7 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     put(&var out, \\\"define internal void @minivm_divzero() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.divzero)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     put(&var out, \\\"define internal void @minivm_cast() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.cast)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     put(&var out, \\\"define internal void @minivm_shift() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.shift)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n+    put(&var out, \\\"define internal void @minivm_try_failed() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.try_failed)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     var function_index = 0\\n     while function_index < program.functions.len() {\\n         try emit_function(&var out, &program, function_index,\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex 01663278..9cf0ffaa 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -10,7 +10,7 @@ extension ValueType: Equatable {}\\n public enum Op {\\n     case Const, case Copy, case Add, case Sub, case Mul, case Div, case Rem,\\n     case Neg, case Not, case Eq, case Ne, case Lt, case Le, case Gt, case Ge,\\n-    case Jump, case Branch, case Call, case Print, case Return,\\n+    case Jump, case Branch, case Call, case Print, case Return, case Panic,\\n     case Cast, case Truncate, case WrapAdd, case WrapSub, case WrapMul,\\n     case Addr, case LoadRef, case StoreRef, case RefField,\\n     case StringConst, case StringLen, case StringByteAt, case StringSlice, case Drop,\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex b18172a3..25dd6c3a 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -230,6 +230,9 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n         case Print -> {\\n             if not printable_type(a) { return invalid(&i) }\\n         },\\n+        case Panic -> {\\n+            if i.dst != -1 || i.a != -1 || i.b != -1 || i.immediate != 0 || i.target != -1 || i.args.len() != 0 { return invalid(&i) }\\n+        },\\n         case Return -> {\\n             if f.result == ValueType.Void {\\n                 if i.a != -1 { return invalid(&i) }\\n@@ -279,7 +282,7 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n             visited.set(pc, true)\\n             let i = f.code[pc].copy()\\n             if i.op == Op.Jump || i.op == Op.Branch { try! pending.push(i.target) }\\n-            if i.op != Op.Jump && i.op != Op.Return { try! pending.push(pc + 1) }\\n+            if i.op != Op.Jump && i.op != Op.Return && i.op != Op.Panic { try! pending.push(pc + 1) }\\n         }\\n     }\\n     return\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 4eafa274..788a967d 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -333,6 +333,7 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 try string_release(&var heap, cells[base + inst.a])\\n                 cells[base + inst.a] = 0\\n             },\\n+            case Panic -> { return runtime_error(\\\"runtime error: try! failed\\\") },\\n             case Return -> {\\n                 var values = Vector<Int>()\\n                 let layout = try value_layout(&program.records, &program.sums, function.result)\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex 533bc2d6..950cd113 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -35,6 +35,19 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"results/review_composition\\\", \\\"hello\\\\n7\\\\n0\\\\nkept\\\\n5\\\\nbad\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/construction_wrapping\\\", \\\"7\\\\nbad\\\\n255\\\\n9\\\\n9\\\\n11\\\\nexplicit\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/nested_optional\\\", \\\"inner-none\\\\nouter-none\\\\n7\\\\ninner\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/same_type_explicit\\\", \\\"42\\\\n999\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/void_success\\\", \\\"ok\\\\nfailed\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/match_snapshot_ownership\\\", \\\"subject\\\\nold\\\\nsaved\\\\nchanged\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/try_propagation\\\", \\\"later\\\\nleaf\\\\n8\\\\nstop\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/try_nested_success\\\", \\\"7\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/try_panic\\\", \\\"runtime error: try! failed\\\\n\\\", 1, \\\"results\\\"),\\n+    RunCase(\\\"results/loop_cleanup\\\", \\\"early\\\\n200\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/value_match_control\\\", \\\"8\\\\n-1\\\\nkept\\\\nfailure\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/value_match_widening\\\", \\\"64\\\\n-8\\\\n16\\\\n\\\", section=\\\"results\\\"),\\n+    RunCase(\\\"results/try_void\\\", \\\"after\\\\npropagated\\\\nstopped\\\\nforced\\\\n\\\", section=\\\"results\\\"),\\n     RunCase(\\\"optionals/review_nested_context\\\", \\\"chosen\\\\nsome-none\\\\nnested-none\\\\n\\\", section=\\\"optionals\\\"),\\n     RunCase(\\\"optionals/basic_numeric\\\", \\\"7\\\\nnone\\\\n255\\\\n\\\", section=\\\"optionals\\\"),\\n     RunCase(\\\"optionals/nested_semantics\\\", \\\"outer-none\\\\ninner-none\\\\n9\\\\nbranch-inner-none\\\\n7\\\\n18446744073709551615\\\\n\\\", section=\\\"optionals\\\"),\\n@@ -205,6 +218,22 @@ RUN_CASES = (\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"results/reject_ambiguous_same_type\\\", \\\"ambiguous Result auto-wrap\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_ambiguous_literal\\\", \\\"ambiguous Result auto-wrap\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_none_non_optional\\\", \\\"None requires an expected Optional type\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_void_return_none\\\", \\\"None requires an expected Optional type\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_void_error\\\", \\\"Result error type cannot be Void\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_type_arity\\\", \\\"Result requires exactly two type arguments\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_reference_payload\\\", \\\"Result reference payloads are not supported\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_constructor_label\\\", \\\"argument label\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_constructor_arity\\\", \\\"Result.Err requires an error payload\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_void_constructor\\\", \\\"Result.Ok requires a value payload\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_match_duplicate\\\", \\\"duplicate Result match arm\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_match_missing\\\", \\\"non-exhaustive Result match\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_match_payload_arity\\\", \\\"Result Ok pattern requires a payload\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_try_non_result\\\", \\\"try operand must be a Result\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_try_context\\\", \\\"try requires a Result-returning function\\\", \\\"results\\\"),\\n+    RejectCase(\\\"results/reject_try_error_type\\\", \\\"try error type does not match function result\\\", \\\"results\\\"),\\n     RejectCase(\\\"optionals/reject_uncontextualized_none\\\", \\\"None requires an expected Optional type\\\", \\\"optionals\\\"),\\n     RejectCase(\\\"optionals/reject_if_let_non_optional\\\", \\\"if binding requires an Optional value\\\", \\\"optionals\\\"),\\n     RejectCase(\\\"optionals/reject_binding_scope\\\", \\\"unknown name\\\", \\\"optionals\\\"),\\n@@ -427,7 +456,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\", \\\"results\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -456,7 +485,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\") else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\", \\\"results\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\ndiff --git a/prototypes/minivm/tests/result_contract.saw b/prototypes/minivm/tests/result_contract.saw\\nnew file mode 100644\\nindex 00000000..ebd6739c\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/result_contract.saw\\n@@ -0,0 +1,161 @@\\n+import src.model.*\\n+import src.verify.*\\n+import src.vm.*\\n+import src.llvm\\n+import std.env.{Env}\\n+\\n+func valid(p: &Program) -> Bool {\\n+    try verify(&p) catch { return false }\\n+    true\\n+}\\n+\\n+func fixed_panic(p: &Program) -> Bool {\\n+    try execute(&p, 10) catch { return error.message == \\\"runtime error: try! failed\\\" }\\n+    false\\n+}\\n+\\n+func op(code: &var Vector<Instruction>, kind: Op, dst: Int, a: Int, b: Int, immediate: Int) {\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    i.immediate = immediate\\n+    try! code.push(move i)\\n+}\\n+\\n+func result_program() -> Program {\\n+    let names: Vector<String> = [\\\"number\\\", \\\"text\\\"]\\n+    let fields: Vector<ValueType> = [ValueType.I32, ValueType.String]\\n+    var records = Vector<RecordIR>()\\n+    try! records.push(RecordIR(name: \\\"Mixed\\\", field_names: move names,\\n+        field_types: move fields, line: 1, col: 1))\\n+    var sums = Vector<SumIR>()\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.String, error: ValueType.Void))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.I64, error: ValueType.String))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.String, error: ValueType.Record(0)))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Optional(0), error: ValueType.Result(4)))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Void, error: ValueType.String))\\n+    // [Bool, String, Bool, String] permits a two-word overlapping reference copy.\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.String, error: ValueType.Optional(0)))\\n+    var references = Vector<ReferenceIR>()\\n+    try! references.push(ReferenceIR(target: ValueType.Result(5), mutable: true))\\n+\\n+    let slots: Vector<ValueType> = [\\n+        ValueType.Bool, ValueType.String, ValueType.Bool, ValueType.String,\\n+        ValueType.Bool, ValueType.String, ValueType.Ref(0), ValueType.Ref(0),\\n+        ValueType.Bool, ValueType.String, ValueType.I32, ValueType.String,\\n+        ValueType.Bool, ValueType.String, ValueType.I32, ValueType.String\\n+    ]\\n+    var code = Vector<Instruction>()\\n+    op(&var code, Op.Const, 0, -1, -1, 1)\\n+    op(&var code, Op.StringConst, 1, -1, -1, 0)\\n+    op(&var code, Op.Const, 2, -1, -1, 0)\\n+    op(&var code, Op.StringConst, 3, -1, -1, 1)\\n+    op(&var code, Op.Const, 4, -1, -1, 1)\\n+    op(&var code, Op.StringConst, 5, -1, -1, 2)\\n+    op(&var code, Op.Addr, 6, 0, -1, 0)\\n+    op(&var code, Op.Addr, 7, 2, -1, 0)\\n+    // Snapshot [0,4) before releasing destination [2,6).\\n+    op(&var code, Op.StoreRef, -1, 7, 0, 0)\\n+    op(&var code, Op.Print, -1, 1, -1, 0)\\n+    op(&var code, Op.Print, -1, 3, -1, 0)\\n+    op(&var code, Op.Print, -1, 5, -1, 0)\\n+    // Reverse-direction overlap exercises LoadRef's same snapshot discipline.\\n+    op(&var code, Op.LoadRef, 0, 7, -1, 0)\\n+    op(&var code, Op.Print, -1, 1, -1, 0)\\n+    op(&var code, Op.Print, -1, 3, -1, 0)\\n+    op(&var code, Op.Print, -1, 5, -1, 0)\\n+\\n+    // Result<String, Mixed> carries String leaves in both alternatives.\\n+    op(&var code, Op.Const, 8, -1, -1, 1)\\n+    op(&var code, Op.StringConst, 9, -1, -1, 3)\\n+    op(&var code, Op.Const, 10, -1, -1, 0)\\n+    op(&var code, Op.Drop, -1, 11, -1, 0)\\n+    var call = instruction(Op.Call)\\n+    call.dst = 12\\n+    call.target = 1\\n+    try! call.args.push(8)\\n+    try! call.args.push(9)\\n+    try! call.args.push(10)\\n+    try! call.args.push(11)\\n+    try! code.push(move call)\\n+    op(&var code, Op.Print, -1, 13, -1, 0)\\n+    op(&var code, Op.Return, -1, -1, -1, 0)\\n+\\n+    let echo_params: Vector<ValueType> = [ValueType.Result(2)]\\n+    let echo_slots: Vector<ValueType> = [ValueType.Bool, ValueType.String, ValueType.I32, ValueType.String]\\n+    var echo_code = Vector<Instruction>()\\n+    op(&var echo_code, Op.Return, -1, 0, -1, 0)\\n+    var functions = Vector<FuncIR>()\\n+    try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: move slots, code: move code))\\n+    try! functions.push(FuncIR(name: \\\"echo\\\", params: move echo_params, result: ValueType.Result(2),\\n+        slots: move echo_slots, code: move echo_code))\\n+    let strings: Vector<String> = [\\\"A\\\", \\\"B\\\", \\\"C\\\", \\\"payload\\\"]\\n+    Program(strings: move strings, references: move references, enums: Vector<EnumIR>(),\\n+        records: move records, sums: move sums, functions: move functions, main_index: 0)\\n+}\\n+\\n+func panic_program() -> Program {\\n+    let p = result_program()\\n+    var code = Vector<Instruction>()\\n+    try! code.push(instruction(Op.Panic))\\n+    var functions = Vector<FuncIR>()\\n+    try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: Vector<ValueType>(), code: move code))\\n+    Program(strings: p.strings.copy(), references: p.references.copy(), enums: p.enums.copy(),\\n+        records: p.records.copy(), sums: p.sums.copy(), functions: move functions, main_index: 0)\\n+}\\n+\\n+func main() {\\n+    let p = result_program()\\n+    assert(valid(&p), \\\"mixed Result reference and ABI program\\\")\\n+    let basic = try! value_layout(&p.records, &p.sums, ValueType.Result(1))\\n+    assert(basic.len() == 3 && basic[0] == ValueType.Bool && basic[1] == ValueType.I64 && basic[2] == ValueType.String,\\n+        \\\"Result<Int, String> layout\\\")\\n+    let mixed = try! value_layout(&p.records, &p.sums, ValueType.Result(2))\\n+    assert(mixed.len() == 4 && mixed[0] == ValueType.Bool && mixed[1] == ValueType.String &&\\n+        mixed[2] == ValueType.I32 && mixed[3] == ValueType.String, \\\"mixed owning Result layout\\\")\\n+    let nested = try! value_layout(&p.records, &p.sums, ValueType.Result(3))\\n+    assert(nested.len() == 5 && nested[0] == ValueType.Bool && nested[1] == ValueType.Bool &&\\n+        nested[2] == ValueType.String && nested[3] == ValueType.Bool && nested[4] == ValueType.String,\\n+        \\\"nested Result and Optional layout\\\")\\n+    let void_ok = try! value_layout(&p.records, &p.sums, ValueType.Result(4))\\n+    assert(void_ok.len() == 2 && void_ok[0] == ValueType.Bool && void_ok[1] == ValueType.String,\\n+        \\\"Void Ok has no payload word\\\")\\n+\\n+    let panic = panic_program()\\n+    assert(valid(&panic), \\\"Panic is a terminal instruction\\\")\\n+    assert(fixed_panic(&panic), \\\"fixed VM panic message\\\")\\n+    var bad = panic.copy()\\n+    bad.functions[0].code[0].dst = 0\\n+    assert(not valid(&bad), \\\"Panic rejects destination\\\")\\n+    bad = panic.copy()\\n+    bad.functions[0].code[0].a = 0\\n+    assert(not valid(&bad), \\\"Panic rejects first operand\\\")\\n+    bad = panic.copy()\\n+    bad.functions[0].code[0].b = 0\\n+    assert(not valid(&bad), \\\"Panic rejects second operand\\\")\\n+    bad = panic.copy()\\n+    bad.functions[0].code[0].immediate = 1\\n+    assert(not valid(&bad), \\\"Panic rejects nondefault immediate\\\")\\n+    bad = panic.copy()\\n+    bad.functions[0].code[0].target = 0\\n+    assert(not valid(&bad), \\\"Panic rejects target\\\")\\n+    bad = panic.copy()\\n+    try! bad.functions[0].code[0].args.push(0)\\n+    assert(not valid(&bad), \\\"Panic rejects arguments\\\")\\n+\\n+    let args = try! Env.args()\\n+    if args.len() == 2 && args[1] == \\\"emit-llvm\\\" {\\n+        print(try! llvm.emit_llvm(&p))\\n+        return\\n+    }\\n+    if args.len() == 2 && args[1] == \\\"emit-panic-llvm\\\" {\\n+        print(try! llvm.emit_llvm(&panic))\\n+        return\\n+    }\\n+    // Expected output: A / A / B / A / B / B / payload.\\n+    try! execute(&p, 200)\\n+    print(\\\"result contract passed\\\")\\n+}\\n\",\"id\":\"SL-254\",\"request_id\":\"mv2-SL-254-86d671aa-a1\",\"title\":\"minivm M11: fixed Result<T,E> + try/try! (branch tip 86d671aa)\"}","request_id":"mv2-SL-254-86d671aa-a1","sequence":514,"timestamp":"1789101377"}
---
