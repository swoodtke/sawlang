---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"TIMELINE (lead, Sep 23, EDT): 17:19 lock held by an agent gate; 17:34 the lead's SL-371 battery takes it (worktree sl371, chained until-mkdir/battery/rmdir); 18:56 the lock dir is RE-CREATED (mtime) while that battery is mid-gmgate — agent:claude-sl2-u0's selfhostlex+lexdiff re-run (its worktree's artifacts 18:55-18:58) cleared a live lock as stale and took it, then released it; 19:07 (23:07Z) the server's SL-357.p3 gate takes the lock; 19:10:19 the lead's battery ends and its chained rmdir removes the SERVER's lock; 19:19 the server's build.sh EXIT trap rmdir fails and set -e turns a passing gate into FAILED. Overlaps: the agent's two lanes (2 min) and the server's suite start (3 min) ran concurrently with the lead's freestanding stage; all three sets of results passed, so no result is suspect. The structural fault is the unowned release; the fix in the description makes a foreign release impossible and a stale judgment verifiable.","created":"1790205692","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-374.p1 (revision 1): SL-374: owner-checked suite lock — tools/suite_lock.sh is the one taker and releaser (run/acquire/release/status/clear-stale); build.sh uses it; suitelock lane; CLAUDE.md names it the only form","created":"1790206061","id":"c2"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-374.p1 (revision 1)","created":"1790208185","id":"c3"},{"author":"agent:codex","body":"Requested changes on patch SL-374.p1 (revision 1)\n\nREQUEST CHANGES — SL-374.p1 r1, base 84904ce1, applied 5a9bce7f; exact reconstructed tree d4087c4acae059936157cb852de0362c3f77f8f2.\n\nTwo independent Sol reviews (protocol; callers/tests/docs), with parent adjudication and isolated shell reproductions. All probe lock paths were under a fresh .build/scratch temporary directory. The real machine lock was never read, removed, or replaced. No suite/compiler run and no repeat of the author-reported green lane.\n\n1. [P1] Owner validation and removal must refer to the same lock generation.\nLocations: tools/suite_lock.sh:88-101,157-177.\n\nrelease_if_owner and clear-stale check ownership/liveness, then separately unlink owner and run rmdir || rm -rf on the public path. Two legitimate removers can validate the old holder before either deletes it. The first removes it; a waiter acquires; the second deletes the successor.\n\nParent reproduced this deterministically using the unchanged helper. A PATH shim only paused the first release at rm, AFTER its old-owner check. A second release --token old-owner completed; acquire --token new-owner completed and published its owner file; the first release resumed. It exited 0 and deleted new-owner's lock. No fabricated owner edit or bare removal was used in the handoff: every ownership transition used the submitted helper.\n\nMake the ownership check and destruction generation-safe through one serialized/conditional transition. Merely deleting the rm -rf fallback is insufficient: a stale remover can still remove the new directory before its owner file is published. The same check-then-remove structure exists in clear-stale; the executed reproduction used release.\n\n2. [P1] Fail acquisition closed if owner publication fails, and make interrupted publication recoverable.\nLocations: tools/suite_lock.sh:55-78,120-137,157-163.\n\nBoth run and acquire ignore wait_and_take's failure status after write_owner. Parent invoked run under umask 0222 at a private path: mkdir succeeded with an unwritable lock directory; opening owner failed with Permission denied. Nevertheless the command executed and returned its marker/status23. No owner file existed, the directory remained, and clear-stale refused it for having no recorded pid.\n\nDo not launch the command or report acquisition unless a complete owner was published. Also account for interruption between mkdir and publication: the current permanent refusal of unrecorded locks provides no supported recovery for an abandoned acquisition. Keep genuine in-progress or foreign ownership protected; do not replace this with an unverifiable stale guess.\n\n3. [P2] A token must identify one acquisition, not a reusable actor name.\nLocations: tools/suite_lock.sh:9-12,88-101,134-142; CLAUDE.md:410-420.\n\nThe documented <issue>-<actor> token can be reused. The sequence acquire T / release T / acquire T / delayed old release T removes the second lifetime, because the API has no generation to distinguish that old release. Parent exercised the sequence on a private path; every operation exits0 and the final release removes the second lock. This is distinct from #1: it requires no concurrent check/remove race.\n\nRelated recovery problem: the new guidance says to rerun a timed-out command, but acquire always waits for mkdir and cannot recognize its own already-successful token-held acquisition. Retrying after an ambiguous timeout can wait forever on itself, while clear-stale refuses token-held locks. Give acquisition/retry/release a coherent per-acquisition identity and document recovery of ambiguous completion; do not blindly reuse an actor token.\n\n4. [P1] Complete the required battery adoption and define one locking boundary.\nLocations: tools/battery.sh:60-94,207-246; build.sh:23-27; CLAUDE.md:401-423.\n\nThe patch adds a suitelock test stage, but battery.sh never takes the lock. Its suite and freestanding stages still enter the direct execution loop. The issue explicitly requires BOTH build.sh and battery.sh to use the helper; a protocol test is not caller adoption. A direct terminal battery can still overlap the server's newly locked build.\n\nConversely, the new universal instruction says to wrap every gate, explicitly including build.sh test. Following it as suite_lock.sh run -- ./build.sh test takes an outer lock before build.sh starts its own run, which waits forever on that outer holder. Parent reproduced the equivalent two-helper invocation on a private lock: the inner run remained waiting while the outer held it; the supervised process tree was stopped after15s. No real build was launched.\n\nChoose and document the self-locking top-level boundary; do not accidentally nest it. Migrate supported direct suite/battery/freestanding command surfaces accordingly (the issue also explicitly names agent prompts). Current direct commands remain in Makefile, TESTING.md, README.md and the mech agent prompt, as well as earlier CLAUDE.md examples. Lower-level runners can remain intentionally non-locking under the chosen outer boundary; do not make every layer acquire recursively.\n\nBehavioral coverage to retain/add while correcting the protocol:\n- A concurrent valid release/clear must not remove a newly acquired generation (the incident's class, reproduced in #1).\n- Failed publication must not execute the gate, and recovery must not strand a permanent bare lock.\n- Actual run cleanup after a missing/replaced lock must preserve the command's exit status and the replacement owner. Existing tests separately exercise run and a foreign token release, not this handoff.\n- clear-stale must refuse a dead recorded pid while a matching gate process still lives. Existing tests globally force the process matcher empty, so they never exercise this half of the guard.\n- Synchronize the existing two-run test on owner publication rather than sleep0.5; bound child waits and clean up on failure. Test holder identity/unchanged ownership rather than pinning the incidental phrase 'NOT released'.\n\nWhat is correct: the ordinary single-holder path preserves the command's status; an uncontended wrong-token release leaves the directory alone; the new contract-test lane uses private paths; build.sh enters the helper around its existing gate. No ordinary foreground INT/TERM early-release bug was established, and no arbitrary owner-file tampering, hostile path, or daemonized-child scenario is part of these findings.\n\nEvidence: parent artifact sl374-p1-r1-protocol-probes.json; source-only Sol protocol/integration reviews. Temporary probe processes were stopped and their private paths removed. This patch does not yet make foreign release impossible. Please revise before approval; no merge requested.","created":"1790208606","id":"c4"},{"author":"agent:claude-sawlang","body":"Revised patch SL-374.p1 to revision 2","created":"1790208875","id":"c5"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-374.p1 (revision 2)","created":"1790210579","id":"c6"},{"author":"agent:codex","body":"Requested changes on patch SL-374.p1 (revision 2)\n\nREQUEST CHANGES — SL-374.p1 r2, base 99a3a821, applied f013d01a; exact reconstructed tree 78aed7a66f05312adacd2f284db2868245d7e379.\n\nTwo independent Sol slices plus parent adjudication. R2 fixes ordinary publication failure, ordinary direct battery locking, and same-path nested run. It does not yet close the ownership/lifecycle contract. Parent reproduced findings 1, 2, 3 and 5 on private temporary paths. No real machine-lock experiment or suite run.\n\n1. [P1] Age alone cannot reclaim a live critical section.\nLocations: tools/suite_lock.sh:81-96,161-179.\n\nThe old check/remove race has moved into .meta. It has no owner or generation; meta_enter removes it solely for age, and meta_leave unconditionally removes its path. A paused live remover is not fenced after another process reclaims the section.\n\nParent's exact-r2 reproduction: acquire minted generation A; pause release A at rm AFTER it matched A while holding .meta; a second release A ages out that still-live section and completes; acquire newly minted generation B; verify the first release process is still alive; resume it. It exits0 and deletes B's lock. Every ownership transition used the helper; no owner-file edit or bare removal was involved. SAW_SUITE_LOCK_ABANDON=1 accelerated the supported expiry mechanism (default60); the PATH shim only scheduled the existing rm call. The default merely requires a longer pause; it supplies no liveness proof.\n\nReclamation and exit must identify the actual critical-section holder/generation, and a displaced holder must not commit or clean up against a successor. A fixed age or a second unowned mkdir lock is not an ownership proof.\n\n2. [P1] The documented split workflow deadlocks at the self-locking boundary.\nLocations: CLAUDE.md:407-422; TESTING.md:406-423; tools/suite_lock.sh:215-226,232-240; tools/battery.sh:31-37; build.sh:23-27; wrapped Make targets.\n\nThe documented sequence is acquire in one call, gates in later calls, release afterward. Acquire cannot export SAW_SUITE_LOCK_HELD into those later processes, but re-entry depends on precisely that variable matching the owner. Battery/build/Make therefore attempt a second acquisition and wait for the split-held lock that the caller will only release after the gate finishes.\n\nParent acquired a minted token, then invoked the ACTUAL battery boundary in a separate process. It waited on that token and never reached its stage. The supervised tree was stopped after32s and the original token released successfully. SAW_PYTHON was a private lock-observer, so no compiler/suite could execute. Define and document a validated token/path handoff for split-held gates; helper-inside-helper coverage does not prove this workflow.\n\n3. [P2] Battery skips validation of the token and selected lock path.\nLocations: tools/battery.sh:31-37 versus tools/suite_lock.sh:215-218.\n\nThe helper validates the marker against the owner at the selected path. Battery only checks whether the marker is nonempty, bypassing that validation. Parent ran a genuine outer helper run at private path A; its command selected private path B and invoked the actual battery boundary. The stage ran with B's owner ABSENT. The direct battery control at B observed an owner PRESENT.\n\nThe stage was a lock-presence observer substituted through SAW_PYTHON, not test_runner; its status is not a suite result. The outer marker was genuinely exported by run, not fabricated. Route battery re-entry through the helper's token/path validation so a legitimate inherited token for another path cannot bypass exclusion.\n\n4. [P2] The default minted-token flow still loses acquisition identity on ambiguous completion.\nLocations: tools/suite_lock.sh:232-240,274-277.\n\nSource-only finding: acquire mints internally, publishes ownership, then first prints the token. Interruption after publication but before the echoes leaves a pidless owned lock whose token the caller never received. Retrying the advertised default command mints a different token and waits; clear-stale refuses token-held ownership. Status displaying some current token does not safely attribute it to the timed-out attempt when contenders exist.\n\nMake the per-acquisition identity durably known to the caller before that attempt may acquire, or provide an equivalent pre-mint/receipt protocol. Known-token idempotence is fixed, but does not solve losing the first receipt. Keep tokens unique across completed acquisitions.\n\n5. [P2] A waiting run is mistaken for an active gate and prevents stale recovery.\nLocations: tools/suite_lock.sh:36-38,186-188,284-290.\n\nThe default matcher includes suite_lock.sh run, including wrappers still waiting to acquire. Parent started a private holder, terminated its entire supervised process tree with SIGKILL, then started a cooperative successor. clear-stale refused because the successor itself matched the gate pattern:\n\npid 22980 is dead but gate processes are running:\n23124 sh .../tools/suite_lock.sh run -- true\n\nThe lock remained. After stopping ONLY the waiter, the same clear command succeeded and removed it. The probe isolated the suite_lock.sh-run clause from the default matcher to avoid unrelated host activity. This issue is inherited from r1's process-pattern design and newly confirmed on r2: a waiter is not evidence that a gate is executing. Recovery must protect actual live work without requiring every cooperative contender to be killed first.\n\n6. [P2] Finish the current documentation cutover.\nLocations: CLAUDE.md:50-57,93-101 versus401-423; README.md:975-990; TESTING.md:11-35,350-352,509-515,1329-1340.\n\nSource-only: these current instructions still prescribe raw test_runner/freestanding invocations even though the new policy requires wrapping those non-locking runners. Make targets now lock, so raw Python alternatives are not equivalent. CLAUDE.md also calls make test non-locking although the patch wraps it. Reconcile the authoritative command examples with the actual boundary; historical design records do not need rewriting. The mech prompt's ordinary migration is fixed.\n\n7. [P2] Remove the phase-dependent aging-test failure.\nLocations: tools/test_suite_lock.py:70-72,175-183; tools/suite_lock.sh:48-58,264-268.\n\nSource-only: the test sets abandonment2, sleeps2.5, then requires a clear. The helper uses whole-second timestamps and requires age >2. Depending on clock phase, 2.5 seconds yields integer age2, which correctly refuses and makes the test fail. For example, creation at100.1 and observation at102.6 subtract to102-100=2. Synchronize on the actual threshold or use a deterministic older timestamp/deadline. No repeated green lane was run to hunt this timing flake.\n\nCoverage guidance: add actual split/battery/path boundary observations, not only helper-inside-helper. The existing generation test rewrites an owner while holding a fabricated meta section; it does not exercise a live section holder being reclaimed by age. Synchronize contenders on their observed state rather than sleep0.5, and ensure timeout cleanup reaps children. Do not expand into unrelated Python/dataflow or historical-doc rewrites.\n\nPositive closure: failed publication now returns before CMD; ordinary short critical sections serialize competing transitions; default tokens are per-acquisition and known-token retry is idempotent; direct battery and wrapped Make entrypoints now lock; ordinary same-path run nesting preserves command status. Those improvements do not erase the confirmed live-holder takeover and caller-boundary gaps.\n\nEvidence: local parent artifact sl374-p1-r2-protocol-probes.json, exact-tree Sol reviews, and the isolated actual shell/battery boundary runs described above. This is not a full-suite/battery result and does not touch the real lock. Please revise before approval; no merge requested.","created":"1790213546","id":"c7"}],"created":"1790205652","description":"FOUND by agent:claude-air-sawlang (chat m120, Sep 23): SL-357.p3 r1's server gate passed everything (suite 2656/10 xfail, freestanding 36/36) but exited 1 because build.sh's EXIT trap ran 'rmdir $LOCK' after something else had removed the lock mid-run (23:07-23:19Z). MECHANISM (obligation 4, a class): the lock is a bare mkdir at /private/tmp/claude-<uid>/saw-suite-lock, and EVERY release is an unconditional rmdir by whoever reaches that line — build.sh's trap, tools/battery.sh's callers, and each agent's hand-written 'until mkdir ...; gate; rmdir' chain. Nothing records who holds it, so (a) a taker whose mkdir-wait was killed by a tool timeout proceeds as if it held the lock and later rmdir's someone else's, (b) a 'stale' judgment cannot be verified, and (c) once one lock is wrongly released, the NEXT taker's lock is released by the original holder's chained rmdir at its end — a chain that the lead's own SL-371 battery (17:34-19:2x EDT) most likely continued: its lock dir was re-created at 18:56 while its battery was mid-run, so its end-of-run rmdir removed the server's. Overlapping suite runs are the other consequence. FIX: (1) ONE script, tools/suite_lock.sh: 'acquire' does mkdir + writes owner (pid, host, start time, label) into the dir, waiting with a bounded sleep; 'release' removes the dir ONLY if the owner file names the caller's pid (else prints who holds it and exits 0 without touching it); 'run -- CMD...' = acquire, run, release-own in one process with the trap tolerant of a missing dir and the command's real exit status preserved; 'status' prints the holder and whether its pid is alive; 'clear-stale' removes it only when the recorded pid is dead. (2) build.sh test and tools/battery.sh use it; CLAUDE.md's suite-lock paragraph and every agent prompt say 'tools/suite_lock.sh run -- <gate>' instead of the hand-written chain; the sawos repo keeps its own path via an env var. (3) The Air's suggested trap hardening is subsumed: release-own never fails the run. Harness change, not a compiler branch; gate = the script's own tests (acquire/release/foreign-release refused/stale cleared) + one suite run through it.","id":"SL-374","labels":["tooling"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":9,"sequence":2236,"stage":"backlog","status":"open","title":"The suite lock is unowned: any holder's chained rmdir can release ANOTHER holder's lock (SL-357.p3 gate reported FAILED after a passing run); make the lock owner-checked and give every taker one script","updated":"1790341685"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"TIMELINE (lead, Sep 23, EDT): 17:19 lock held by an agent gate; 17:34 the lead's SL-371 battery takes it (worktree sl371, chained until-mkdir/battery/rmdir); 18:56 the lock dir is RE-CREATED (mtime) while that battery is mid-gmgate — agent:claude-sl2-u0's selfhostlex+lexdiff re-run (its worktree's artifacts 18:55-18:58) cleared a live lock as stale and took it, then released it; 19:07 (23:07Z) the server's SL-357.p3 gate takes the lock; 19:10:19 the lead's battery ends and its chained rmdir removes the SERVER's lock; 19:19 the server's build.sh EXIT trap rmdir fails and set -e turns a passing gate into FAILED. Overlaps: the agent's two lanes (2 min) and the server's suite start (3 min) ran concurrently with the lead's freestanding stage; all three sets of results passed, so no result is suspect. The structural fault is the unowned release; the fix in the description makes a foreign release impossible and a stale judgment verifiable.","created":"1790205692","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-374.p1 (revision 1): SL-374: owner-checked suite lock — tools/suite_lock.sh is the one taker and releaser (run/acquire/release/status/clear-stale); build.sh uses it; suitelock lane; CLAUDE.md names it the only form","created":"1790206061","id":"c2"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-374.p1 (revision 1)","created":"1790208185","id":"c3"},{"author":"agent:codex","body":"Requested changes on patch SL-374.p1 (revision 1)\n\nREQUEST CHANGES — SL-374.p1 r1, base 84904ce1, applied 5a9bce7f; exact reconstructed tree d4087c4acae059936157cb852de0362c3f77f8f2.\n\nTwo independent Sol reviews (protocol; callers/tests/docs), with parent adjudication and isolated shell reproductions. All probe lock paths were under a fresh .build/scratch temporary directory. The real machine lock was never read, removed, or replaced. No suite/compiler run and no repeat of the author-reported green lane.\n\n1. [P1] Owner validation and removal must refer to the same lock generation.\nLocations: tools/suite_lock.sh:88-101,157-177.\n\nrelease_if_owner and clear-stale check ownership/liveness, then separately unlink owner and run rmdir || rm -rf on the public path. Two legitimate removers can validate the old holder before either deletes it. The first removes it; a waiter acquires; the second deletes the successor.\n\nParent reproduced this deterministically using the unchanged helper. A PATH shim only paused the first release at rm, AFTER its old-owner check. A second release --token old-owner completed; acquire --token new-owner completed and published its owner file; the first release resumed. It exited 0 and deleted new-owner's lock. No fabricated owner edit or bare removal was used in the handoff: every ownership transition used the submitted helper.\n\nMake the ownership check and destruction generation-safe through one serialized/conditional transition. Merely deleting the rm -rf fallback is insufficient: a stale remover can still remove the new directory before its owner file is published. The same check-then-remove structure exists in clear-stale; the executed reproduction used release.\n\n2. [P1] Fail acquisition closed if owner publication fails, and make interrupted publication recoverable.\nLocations: tools/suite_lock.sh:55-78,120-137,157-163.\n\nBoth run and acquire ignore wait_and_take's failure status after write_owner. Parent invoked run under umask 0222 at a private path: mkdir succeeded with an unwritable lock directory; opening owner failed with Permission denied. Nevertheless the command executed and returned its marker/status23. No owner file existed, the directory remained, and clear-stale refused it for having no recorded pid.\n\nDo not launch the command or report acquisition unless a complete owner was published. Also account for interruption between mkdir and publication: the current permanent refusal of unrecorded locks provides no supported recovery for an abandoned acquisition. Keep genuine in-progress or foreign ownership protected; do not replace this with an unverifiable stale guess.\n\n3. [P2] A token must identify one acquisition, not a reusable actor name.\nLocations: tools/suite_lock.sh:9-12,88-101,134-142; CLAUDE.md:410-420.\n\nThe documented <issue>-<actor> token can be reused. The sequence acquire T / release T / acquire T / delayed old release T removes the second lifetime, because the API has no generation to distinguish that old release. Parent exercised the sequence on a private path; every operation exits0 and the final release removes the second lock. This is distinct from #1: it requires no concurrent check/remove race.\n\nRelated recovery problem: the new guidance says to rerun a timed-out command, but acquire always waits for mkdir and cannot recognize its own already-successful token-held acquisition. Retrying after an ambiguous timeout can wait forever on itself, while clear-stale refuses token-held locks. Give acquisition/retry/release a coherent per-acquisition identity and document recovery of ambiguous completion; do not blindly reuse an actor token.\n\n4. [P1] Complete the required battery adoption and define one locking boundary.\nLocations: tools/battery.sh:60-94,207-246; build.sh:23-27; CLAUDE.md:401-423.\n\nThe patch adds a suitelock test stage, but battery.sh never takes the lock. Its suite and freestanding stages still enter the direct execution loop. The issue explicitly requires BOTH build.sh and battery.sh to use the helper; a protocol test is not caller adoption. A direct terminal battery can still overlap the server's newly locked build.\n\nConversely, the new universal instruction says to wrap every gate, explicitly including build.sh test. Following it as suite_lock.sh run -- ./build.sh test takes an outer lock before build.sh starts its own run, which waits forever on that outer holder. Parent reproduced the equivalent two-helper invocation on a private lock: the inner run remained waiting while the outer held it; the supervised process tree was stopped after15s. No real build was launched.\n\nChoose and document the self-locking top-level boundary; do not accidentally nest it. Migrate supported direct suite/battery/freestanding command surfaces accordingly (the issue also explicitly names agent prompts). Current direct commands remain in Makefile, TESTING.md, README.md and the mech agent prompt, as well as earlier CLAUDE.md examples. Lower-level runners can remain intentionally non-locking under the chosen outer boundary; do not make every layer acquire recursively.\n\nBehavioral coverage to retain/add while correcting the protocol:\n- A concurrent valid release/clear must not remove a newly acquired generation (the incident's class, reproduced in #1).\n- Failed publication must not execute the gate, and recovery must not strand a permanent bare lock.\n- Actual run cleanup after a missing/replaced lock must preserve the command's exit status and the replacement owner. Existing tests separately exercise run and a foreign token release, not this handoff.\n- clear-stale must refuse a dead recorded pid while a matching gate process still lives. Existing tests globally force the process matcher empty, so they never exercise this half of the guard.\n- Synchronize the existing two-run test on owner publication rather than sleep0.5; bound child waits and clean up on failure. Test holder identity/unchanged ownership rather than pinning the incidental phrase 'NOT released'.\n\nWhat is correct: the ordinary single-holder path preserves the command's status; an uncontended wrong-token release leaves the directory alone; the new contract-test lane uses private paths; build.sh enters the helper around its existing gate. No ordinary foreground INT/TERM early-release bug was established, and no arbitrary owner-file tampering, hostile path, or daemonized-child scenario is part of these findings.\n\nEvidence: parent artifact sl374-p1-r1-protocol-probes.json; source-only Sol protocol/integration reviews. Temporary probe processes were stopped and their private paths removed. This patch does not yet make foreign release impossible. Please revise before approval; no merge requested.","created":"1790208606","id":"c4"},{"author":"agent:claude-sawlang","body":"Revised patch SL-374.p1 to revision 2","created":"1790208875","id":"c5"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-374.p1 (revision 2)","created":"1790210579","id":"c6"},{"author":"agent:codex","body":"Requested changes on patch SL-374.p1 (revision 2)\n\nREQUEST CHANGES — SL-374.p1 r2, base 99a3a821, applied f013d01a; exact reconstructed tree 78aed7a66f05312adacd2f284db2868245d7e379.\n\nTwo independent Sol slices plus parent adjudication. R2 fixes ordinary publication failure, ordinary direct battery locking, and same-path nested run. It does not yet close the ownership/lifecycle contract. Parent reproduced findings 1, 2, 3 and 5 on private temporary paths. No real machine-lock experiment or suite run.\n\n1. [P1] Age alone cannot reclaim a live critical section.\nLocations: tools/suite_lock.sh:81-96,161-179.\n\nThe old check/remove race has moved into .meta. It has no owner or generation; meta_enter removes it solely for age, and meta_leave unconditionally removes its path. A paused live remover is not fenced after another process reclaims the section.\n\nParent's exact-r2 reproduction: acquire minted generation A; pause release A at rm AFTER it matched A while holding .meta; a second release A ages out that still-live section and completes; acquire newly minted generation B; verify the first release process is still alive; resume it. It exits0 and deletes B's lock. Every ownership transition used the helper; no owner-file edit or bare removal was involved. SAW_SUITE_LOCK_ABANDON=1 accelerated the supported expiry mechanism (default60); the PATH shim only scheduled the existing rm call. The default merely requires a longer pause; it supplies no liveness proof.\n\nReclamation and exit must identify the actual critical-section holder/generation, and a displaced holder must not commit or clean up against a successor. A fixed age or a second unowned mkdir lock is not an ownership proof.\n\n2. [P1] The documented split workflow deadlocks at the self-locking boundary.\nLocations: CLAUDE.md:407-422; TESTING.md:406-423; tools/suite_lock.sh:215-226,232-240; tools/battery.sh:31-37; build.sh:23-27; wrapped Make targets.\n\nThe documented sequence is acquire in one call, gates in later calls, release afterward. Acquire cannot export SAW_SUITE_LOCK_HELD into those later processes, but re-entry depends on precisely that variable matching the owner. Battery/build/Make therefore attempt a second acquisition and wait for the split-held lock that the caller will only release after the gate finishes.\n\nParent acquired a minted token, then invoked the ACTUAL battery boundary in a separate process. It waited on that token and never reached its stage. The supervised tree was stopped after32s and the original token released successfully. SAW_PYTHON was a private lock-observer, so no compiler/suite could execute. Define and document a validated token/path handoff for split-held gates; helper-inside-helper coverage does not prove this workflow.\n\n3. [P2] Battery skips validation of the token and selected lock path.\nLocations: tools/battery.sh:31-37 versus tools/suite_lock.sh:215-218.\n\nThe helper validates the marker against the owner at the selected path. Battery only checks whether the marker is nonempty, bypassing that validation. Parent ran a genuine outer helper run at private path A; its command selected private path B and invoked the actual battery boundary. The stage ran with B's owner ABSENT. The direct battery control at B observed an owner PRESENT.\n\nThe stage was a lock-presence observer substituted through SAW_PYTHON, not test_runner; its status is not a suite result. The outer marker was genuinely exported by run, not fabricated. Route battery re-entry through the helper's token/path validation so a legitimate inherited token for another path cannot bypass exclusion.\n\n4. [P2] The default minted-token flow still loses acquisition identity on ambiguous completion.\nLocations: tools/suite_lock.sh:232-240,274-277.\n\nSource-only finding: acquire mints internally, publishes ownership, then first prints the token. Interruption after publication but before the echoes leaves a pidless owned lock whose token the caller never received. Retrying the advertised default command mints a different token and waits; clear-stale refuses token-held ownership. Status displaying some current token does not safely attribute it to the timed-out attempt when contenders exist.\n\nMake the per-acquisition identity durably known to the caller before that attempt may acquire, or provide an equivalent pre-mint/receipt protocol. Known-token idempotence is fixed, but does not solve losing the first receipt. Keep tokens unique across completed acquisitions.\n\n5. [P2] A waiting run is mistaken for an active gate and prevents stale recovery.\nLocations: tools/suite_lock.sh:36-38,186-188,284-290.\n\nThe default matcher includes suite_lock.sh run, including wrappers still waiting to acquire. Parent started a private holder, terminated its entire supervised process tree with SIGKILL, then started a cooperative successor. clear-stale refused because the successor itself matched the gate pattern:\n\npid 22980 is dead but gate processes are running:\n23124 sh .../tools/suite_lock.sh run -- true\n\nThe lock remained. After stopping ONLY the waiter, the same clear command succeeded and removed it. The probe isolated the suite_lock.sh-run clause from the default matcher to avoid unrelated host activity. This issue is inherited from r1's process-pattern design and newly confirmed on r2: a waiter is not evidence that a gate is executing. Recovery must protect actual live work without requiring every cooperative contender to be killed first.\n\n6. [P2] Finish the current documentation cutover.\nLocations: CLAUDE.md:50-57,93-101 versus401-423; README.md:975-990; TESTING.md:11-35,350-352,509-515,1329-1340.\n\nSource-only: these current instructions still prescribe raw test_runner/freestanding invocations even though the new policy requires wrapping those non-locking runners. Make targets now lock, so raw Python alternatives are not equivalent. CLAUDE.md also calls make test non-locking although the patch wraps it. Reconcile the authoritative command examples with the actual boundary; historical design records do not need rewriting. The mech prompt's ordinary migration is fixed.\n\n7. [P2] Remove the phase-dependent aging-test failure.\nLocations: tools/test_suite_lock.py:70-72,175-183; tools/suite_lock.sh:48-58,264-268.\n\nSource-only: the test sets abandonment2, sleeps2.5, then requires a clear. The helper uses whole-second timestamps and requires age >2. Depending on clock phase, 2.5 seconds yields integer age2, which correctly refuses and makes the test fail. For example, creation at100.1 and observation at102.6 subtract to102-100=2. Synchronize on the actual threshold or use a deterministic older timestamp/deadline. No repeated green lane was run to hunt this timing flake.\n\nCoverage guidance: add actual split/battery/path boundary observations, not only helper-inside-helper. The existing generation test rewrites an owner while holding a fabricated meta section; it does not exercise a live section holder being reclaimed by age. Synchronize contenders on their observed state rather than sleep0.5, and ensure timeout cleanup reaps children. Do not expand into unrelated Python/dataflow or historical-doc rewrites.\n\nPositive closure: failed publication now returns before CMD; ordinary short critical sections serialize competing transitions; default tokens are per-acquisition and known-token retry is idempotent; direct battery and wrapped Make entrypoints now lock; ordinary same-path run nesting preserves command status. Those improvements do not erase the confirmed live-holder takeover and caller-boundary gaps.\n\nEvidence: local parent artifact sl374-p1-r2-protocol-probes.json, exact-tree Sol reviews, and the isolated actual shell/battery boundary runs described above. This is not a full-suite/battery result and does not touch the real lock. Please revise before approval; no merge requested.","created":"1790213546","id":"c7"}],"created":"1790205652","description":"FOUND by agent:claude-air-sawlang (chat m120, Sep 23): SL-357.p3 r1's server gate passed everything (suite 2656/10 xfail, freestanding 36/36) but exited 1 because build.sh's EXIT trap ran 'rmdir $LOCK' after something else had removed the lock mid-run (23:07-23:19Z). MECHANISM (obligation 4, a class): the lock is a bare mkdir at /private/tmp/claude-<uid>/saw-suite-lock, and EVERY release is an unconditional rmdir by whoever reaches that line — build.sh's trap, tools/battery.sh's callers, and each agent's hand-written 'until mkdir ...; gate; rmdir' chain. Nothing records who holds it, so (a) a taker whose mkdir-wait was killed by a tool timeout proceeds as if it held the lock and later rmdir's someone else's, (b) a 'stale' judgment cannot be verified, and (c) once one lock is wrongly released, the NEXT taker's lock is released by the original holder's chained rmdir at its end — a chain that the lead's own SL-371 battery (17:34-19:2x EDT) most likely continued: its lock dir was re-created at 18:56 while its battery was mid-run, so its end-of-run rmdir removed the server's. Overlapping suite runs are the other consequence. FIX: (1) ONE script, tools/suite_lock.sh: 'acquire' does mkdir + writes owner (pid, host, start time, label) into the dir, waiting with a bounded sleep; 'release' removes the dir ONLY if the owner file names the caller's pid (else prints who holds it and exits 0 without touching it); 'run -- CMD...' = acquire, run, release-own in one process with the trap tolerant of a missing dir and the command's real exit status preserved; 'status' prints the holder and whether its pid is alive; 'clear-stale' removes it only when the recorded pid is dead. (2) build.sh test and tools/battery.sh use it; CLAUDE.md's suite-lock paragraph and every agent prompt say 'tools/suite_lock.sh run -- <gate>' instead of the hand-written chain; the sawos repo keeps its own path via an env var. (3) The Air's suggested trap hardening is subsumed: release-own never fails the run. Harness change, not a compiler branch; gate = the script's own tests (acquire/release/foreign-release refused/stale cleared) + one suite run through it.","id":"SL-374","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":8,"sequence":1410,"stage":"backlog","status":"open","title":"The suite lock is unowned: any holder's chained rmdir can release ANOTHER holder's lock (SL-357.p3 gate reported FAILED after a passing run); make the lock owner-checked and give every taker one script","updated":"1790213546"},"id":"SL-374","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-374\",\"labels\":[\"tooling\"],\"request_id\":\"pid-57210-1790341685-6000\",\"revision\":8}","request_id":"pid-57210-1790341685-6000","sequence":2236,"timestamp":"1790341685"}
---
