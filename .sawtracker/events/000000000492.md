---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-252.p1 (revision 1): minivm M9: owning records + String-pattern match (branch commit 6acc9315)","created":"1789098592","id":"c1"}],"created":"1789086370","description":"SL-242 epic child, landing patch 10/14. String-owning record fields through refs/methods/aggregate calls; String match with literal patterns + required wildcard; includes the real keyword_kind/suffix_width and String-backed Lexer.advance. Contract: prototypes/minivm/M9_OWNING_RECORDS_MATCH.md. Patch = branch commit 6acc9315.","id":"SL-252","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":492,"stage":"queued","status":"open","title":"minivm M9: owning records + String-pattern match","updated":"1789098592"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086370","description":"SL-242 epic child, landing patch 10/14. String-owning record fields through refs/methods/aggregate calls; String match with literal patterns + required wildcard; includes the real keyword_kind/suffix_width and String-backed Lexer.advance. Contract: prototypes/minivm/M9_OWNING_RECORDS_MATCH.md. Patch = branch commit 6acc9315.","id":"SL-252","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":414,"stage":"queued","status":"open","title":"minivm M9: owning records + String-pattern match","updated":"1789086370"},"id":"SL-252","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 0dc1ba60..1829b432 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -17,8 +17,11 @@ scalar-only `Lexer.advance`-shaped record (250 total cases). Strings, collection\\n Optional/Result payloads, intrinsic generic types and module integration remain\\n outstanding. M8 adds owned String literals/locals/parameters/results/references,\\n byte length/access, equality and substring with explicit copy/drop rules (277\\n-total cases). Next is String-valued records and String-pattern match, before\\n-Optional/Result payloads and collections are enabled.\\n+total cases). M9 adds String-valued records and String-pattern match (288 cases),\\n+including unchanged keyword/suffix classification and the actual String-backed\\n+Lexer.advance. Next are Optional/Result payloads and collections. The lexer has\\n+no imports, so its first whole-file test can combine the unchanged library with\\n+a small wrapper without implementing general module loading.\\n The dependency groups below are an inventory, not the implementation commit order;\\n the numbered milestone designs define each isolated slice.\\n \\ndiff --git a/prototypes/minivm/M9_OWNING_RECORDS_MATCH.md b/prototypes/minivm/M9_OWNING_RECORDS_MATCH.md\\nnew file mode 100644\\nindex 00000000..068f0c0e\\n--- /dev/null\\n+++ b/prototypes/minivm/M9_OWNING_RECORDS_MATCH.md\\n@@ -0,0 +1,82 @@\\n+# M9: String-valued records and String-pattern match\\n+\\n+Status: design after M8 (36cc9d69).\\n+\\n+## Scope and invariants\\n+\\n+Allow String leaves in existing nested value records. Record copies retain their\\n+String contents; writes release replaced contents; String fields can be borrowed\\n+and accessed through record references and methods. All existing whole-root\\n+exclusivity rules remain. No stored references, custom destructors, user-defined\\n+copy/move policy, Optional/Result or collections in this slice.\\n+\\n+Add literal String patterns plus a required final wildcard to both value and\\n+statement match. Compare decoded byte content with existing String Eq; reject\\n+duplicate decoded patterns, non-String patterns, interpolation and missing\\n+wildcard. Preserve enum match behavior. Statement arms retain existing block\\n+requirements, while value arms use the existing expression/block value rules.\\n+Snapshot subject before evaluating any arm; an arm may subsequently mutate the\\n+original binding without changing which value was matched.\\n+\\n+## Layout and ownership\\n+\\n+value_layout now permits String leaves in records; Ref leaves stay rejected.\\n+Flattened layouts remain authoritative for every copy/call/return/address.\\n+No new opcodes or type metadata are needed. Copy of an aggregate is lowered from\\n+a complete source snapshot, so writes cannot partially overlap a live source.\\n+Verify that assignment, constructor fields and value joins all maintain this\\n+invariant rather than assuming a per-word copy is safe in isolation.\\n+\\n+LLVM data arrays now include String words: every non-Ref `%sN` is a GEP into\\n+the existing `[N x i64]` allocation, with ptr loads/stores for String words.\\n+Initialize every String slot to ptr null. This is a host-64 ABI (8-byte pointers\\n+and i64 cells, naturally aligned), not a cross-target layout promise. Ref slots\\n+may remain separate ptr allocas because references cannot be stored in records.\\n+Opaque LLVM pointers preserve pointer provenance; do not use ptrtoint/inttoptr.\\n+\\n+Aggregate LLVM types derive from the exact flattened layout: ptr for String,\\n+i64 for scalar words. Use that same type for function results, calls, insertvalue\\n+and extractvalue. Parameter flattening emits ptr for String words and retains\\n+them into callee-owned slots. Callee record returns snapshot all words and retain\\n+every String word BEFORE any local cleanup; caller adopts each returned String\\n+claim, releasing overwritten destination claims without another retain.\\n+\\n+Both VM and LLVM bulk LoadRef/StoreRef use three phases: snapshot all words,\\n+retain all String words, then release and replace all destination words, adopting\\n+the snapshot claims. This protects partially overlapping ranges and repeated\\n+String handles. Whole-record source assignments use fresh frontend snapshots.\\n+\\n+Frontend lexical cleanup and returns preserve a whole result RANGE, not one\\n+slot. Preserve/promote every String leaf in the returned value layout at a\\n+value-block exit; cleanup_function excludes the complete return range. Other\\n+String slots still Drop normally. Backend return cleanup remains idempotent and\\n+retains the result before releasing frame claims. Unreachable dummy record\\n+returns initialize String leaves via StringConst of the empty string, numeric\\n+leaves with Const, never fake integer String handles.\\n+\\n+## Delegation and tests\\n+\\n+Primary owns model.saw, verify.saw as needed, direct owning-record contracts,\\n+design/docs, integration review, gates and commit. Sol frontend owns frontend.saw;\\n+Sol engines owns vm.saw/llvm.saw (String runtime changes only if necessary);\\n+Sol tests owns test_minivm.py and examples/owning_records. No suites run by agents.\\n+\\n+Supersede the M8 String-record-field rejection with positive record tests.\\n+Direct contracts include mixed layouts, aggregate ref accesses and overlapping\\n+bulk transfers with repeated String handles, plus invalid field/range/type checks.\\n+Native/VM successful execution must leave zero live String allocations. Source\\n+tests cover nested copies/replacement, field refs and mutating methods, mixed ABI,\\n+record returns from value if/match, early return and loop cleanup, alias-safe\\n+assignment and retained snapshots. String matches cover all branches/default,\\n+empty/NUL/UTF-8/escaped patterns, decoded duplicates and unsupported patterns.\\n+\\n+Integration extracts use the unchanged lexer keyword_kind and suffix_width\\n+functions, actual TokenKind declaration, and an unchanged Lexer.advance body\\n+with its real String-backed Lexer fields plus ubyte/constants. Validate keywords\\n+and suffixes against explicit expected values; advance checks ASCII, newline and\\n+UTF-8 continuation-byte column behavior. Keep the existing lexer source unchanged.\\n+Run all retained tests plus new cases on VM and clang O0/O2, all representation\\n+contracts, and targeted native memory checks for owning aggregate transfers.\\n+\\n+Next: fixed Optional/Result families and their payload cleanup/try propagation,\\n+then opaque builder/vector runtime operations, and the unchanged whole lexer.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 71f7f6f5..fa6b0bbe 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -46,7 +46,7 @@ Single-file programs support signed and unsigned 8/16/32/64-bit integers,\\n `Int`/`UInt` (64-bit), `Byte`, `Bool`, functions, positional calls,\\n recursion, `let`/`var`, assignment, lexical scopes, `if`/`else if`/`else` and\\n `while`, explicit and implicit tail returns, and numeric/Bool printing. Named structs can contain scalars\\n-or nested structs, with\\n+Strings or nested structs, with\\n field access, mutable fields, value copying, and function parameters/results.\\n Payload-free enums support copying, equality, record fields and function\\n parameters/results. Exhaustive enum `match` supports qualified or unqualified\\n@@ -89,11 +89,16 @@ and trait extensions remain unsupported. See [M7_RECORD_RECEIVERS.md](M7_RECORD_\\n Owned String values support literals, copies, locals, parameters/results,\\n references, equality, printing, and `len`, `is_empty`, `byte_at`, `substring`,\\n and `equals`. Byte offsets and embedded NUL are preserved. Intrinsic String\\n-methods also accept temporary receivers. String fields/statics, interpolation\\n+methods also accept temporary receivers. String statics, interpolation\\n and concatenation remain unsupported. Both engines retain/release owning slots\\n and check for live allocations after successful execution; see\\n [M8_OWNED_STRINGS.md](M8_OWNED_STRINGS.md).\\n \\n+Nested records can own String fields, including through references, methods and\\n+aggregate calls/returns. String matches support literal patterns and a required\\n+final wildcard, comparing decoded bytes and snapshotting the subject before\\n+arm execution. See [M9_OWNING_RECORDS_MATCH.md](M9_OWNING_RECORDS_MATCH.md).\\n+\\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n only to VM execution; native code uses the host call stack and has no budget.\\n@@ -106,24 +111,24 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The first eight milestones\\n-pass all 277 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The first nine milestones\\n+cover 288 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw`,\\n `tests/record_contract.saw`, `tests/enum_contract.saw`,\\n `tests/reference_contract.saw`, `tests/receiver_contract.saw`, and\\n-`tests/string_contract.saw`; build and run them\\n+`tests/string_contract.saw` and `tests/owning_record_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n `--section enums`, `--section values`, `--section references`, or\\n-`--section receivers` or `--section strings` for an isolated\\n+`--section receivers`, `--section strings` or `--section owning_records` for an isolated\\n integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\n schema and semantics. [LEXER_DEPENDENCIES.md](LEXER_DEPENDENCIES.md) tracks the\\n remaining dependencies needed to compile the existing lexer with this prototype.\\n-The fixtures include the lexer's actual `TokenKind` declaration and `hex_value`\\n-helper. Source is\\n+The fixtures include the lexer's actual `TokenKind` declaration, `hex_value`,\\n+`keyword_kind`, `suffix_width`, and String-backed `Lexer.advance`. Source is\\n split into `frontend.saw`, `model.saw`, `verify.saw`, `vm.saw`, `llvm.saw`, and\\n `main.saw`. The verifier checks structural indices and types; initialization\\n is established by the frontend, so this is not a loader for untrusted bytecode.\\ndiff --git a/prototypes/minivm/examples/owning_records/copy_replace_nested.saw b/prototypes/minivm/examples/owning_records/copy_replace_nested.saw\\nnew file mode 100644\\nindex 00000000..f18a35ca\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/copy_replace_nested.saw\\n@@ -0,0 +1,13 @@\\n+struct Text { value: String, number: Int }\\n+struct Outer { inner: Text, label: String }\\n+\\n+func main() {\\n+    var original = Outer(inner: Text(value: \\\"inner\\\", number: 1), label: \\\"alpha\\\")\\n+    let copy = original\\n+    original.label = \\\"beta\\\"\\n+    original.inner.value = \\\"changed\\\"\\n+    print(copy.label)\\n+    print(copy.inner.value)\\n+    print(original.label)\\n+    print(original.inner.value)\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/keyword_suffix_extract.saw b/prototypes/minivm/examples/owning_records/keyword_suffix_extract.saw\\nnew file mode 100644\\nindex 00000000..fa06a1b8\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/keyword_suffix_extract.saw\\n@@ -0,0 +1,208 @@\\n+public enum TokenKind {\\n+    case IntLit,\\n+    case FloatLit,\\n+    case StringLit,\\n+    case InterpString,\\n+    case BoolLit,\\n+    case Ident,\\n+    case Func,\\n+    case Let,\\n+    case Var,\\n+    case If,\\n+    case Else,\\n+    case Guard,\\n+    case Return,\\n+    case True_,\\n+    case False_,\\n+    case Struct,\\n+    case Extension,\\n+    case SelfKw,\\n+    case Init,\\n+    case NoneKw,\\n+    case Enum,\\n+    case Case,\\n+    case Match,\\n+    case While,\\n+    case Break,\\n+    case Continue,\\n+    case Trait,\\n+    case For,\\n+    case In,\\n+    case TypeKw,\\n+    case Extern,\\n+    case As,\\n+    case Try,\\n+    case Catch,\\n+    case Static,\\n+    case Module,\\n+    case Import,\\n+    case Public,\\n+    case Export,\\n+    case Package,\\n+    case Parent,\\n+    case Plus,\\n+    case Minus,\\n+    case Star,\\n+    case Slash,\\n+    case Percent,\\n+    case Eq,\\n+    case Neq,\\n+    case Lt,\\n+    case Gt,\\n+    case Lte,\\n+    case Gte,\\n+    case And,\\n+    case Or,\\n+    case Ampersand,\\n+    case Pipe,\\n+    case Caret,\\n+    case Tilde,\\n+    case WrapAdd,\\n+    case WrapSub,\\n+    case WrapMul,\\n+    case Not_,\\n+    case Move_,\\n+    case Unsafe_,\\n+    case Borrows,\\n+    case Lend,\\n+    case Assign,\\n+    case PlusAssign,\\n+    case MinusAssign,\\n+    case StarAssign,\\n+    case SlashAssign,\\n+    case PercentAssign,\\n+    case AmpAssign,\\n+    case PipeAssign,\\n+    case CaretAssign,\\n+    case ShlAssign,\\n+    case ShrAssign,\\n+    case Question,\\n+    case DoubleQuestion,\\n+    case Exclaim,\\n+    case QuestionDot,\\n+    case DotDot,\\n+    case DotDotEq,\\n+    case Ellipsis,\\n+    case LParen,\\n+    case RParen,\\n+    case LBrace,\\n+    case RBrace,\\n+    case LBracket,\\n+    case RBracket,\\n+    case Comma,\\n+    case Colon,\\n+    case Semicolon,\\n+    case Arrow,\\n+    case Dot,\\n+    case At,\\n+    case HashDirective,\\n+    case DollarParam,\\n+    case Newline,\\n+    case Eof\\n+}\\n+\\n+func keyword_kind(s: String) -> TokenKind {\\n+    match s {\\n+        case \\\"func\\\" -> TokenKind.Func,\\n+        case \\\"let\\\" -> TokenKind.Let,\\n+        case \\\"var\\\" -> TokenKind.Var,\\n+        case \\\"if\\\" -> TokenKind.If,\\n+        case \\\"else\\\" -> TokenKind.Else,\\n+        case \\\"guard\\\" -> TokenKind.Guard,\\n+        case \\\"return\\\" -> TokenKind.Return,\\n+        case \\\"true\\\" -> TokenKind.True_,\\n+        case \\\"false\\\" -> TokenKind.False_,\\n+        case \\\"struct\\\" -> TokenKind.Struct,\\n+        case \\\"extension\\\" -> TokenKind.Extension,\\n+        case \\\"self\\\" -> TokenKind.SelfKw,\\n+        case \\\"init\\\" -> TokenKind.Init,\\n+        case \\\"None\\\" -> TokenKind.NoneKw,\\n+        case \\\"enum\\\" -> TokenKind.Enum,\\n+        case \\\"while\\\" -> TokenKind.While,\\n+        case \\\"break\\\" -> TokenKind.Break,\\n+        case \\\"continue\\\" -> TokenKind.Continue,\\n+        case \\\"case\\\" -> TokenKind.Case,\\n+        case \\\"match\\\" -> TokenKind.Match,\\n+        case \\\"trait\\\" -> TokenKind.Trait,\\n+        case \\\"for\\\" -> TokenKind.For,\\n+        case \\\"in\\\" -> TokenKind.In,\\n+        case \\\"extern\\\" -> TokenKind.Extern,\\n+        case \\\"not\\\" -> TokenKind.Not_,\\n+        case \\\"move\\\" -> TokenKind.Move_,\\n+        case \\\"unsafe\\\" -> TokenKind.Unsafe_,\\n+        case \\\"borrows\\\" -> TokenKind.Borrows,\\n+        case \\\"lend\\\" -> TokenKind.Lend,\\n+        case \\\"as\\\" -> TokenKind.As,\\n+        case \\\"try\\\" -> TokenKind.Try,\\n+        case \\\"catch\\\" -> TokenKind.Catch,\\n+        case \\\"static\\\" -> TokenKind.Static,\\n+        case \\\"public\\\" -> TokenKind.Public,\\n+        case _ -> TokenKind.Ident\\n+    }\\n+}\\n+\\n+func suffix_width(s: String) -> Int {\\n+    match s {\\n+        case \\\"i8\\\" -> 8,\\n+        case \\\"i16\\\" -> 16,\\n+        case \\\"i32\\\" -> 32,\\n+        case \\\"i64\\\" -> 64,\\n+        case \\\"u8\\\" -> 8,\\n+        case \\\"u16\\\" -> 16,\\n+        case \\\"u32\\\" -> 32,\\n+        case \\\"u64\\\" -> 64,\\n+        case _ -> 0\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(keyword_kind(\\\"func\\\") == TokenKind.Func)\\n+    print(keyword_kind(\\\"let\\\") == TokenKind.Let)\\n+    print(keyword_kind(\\\"var\\\") == TokenKind.Var)\\n+    print(keyword_kind(\\\"if\\\") == TokenKind.If)\\n+    print(keyword_kind(\\\"else\\\") == TokenKind.Else)\\n+    print(keyword_kind(\\\"guard\\\") == TokenKind.Guard)\\n+    print(keyword_kind(\\\"return\\\") == TokenKind.Return)\\n+    print(keyword_kind(\\\"true\\\") == TokenKind.True_)\\n+    print(keyword_kind(\\\"false\\\") == TokenKind.False_)\\n+    print(keyword_kind(\\\"struct\\\") == TokenKind.Struct)\\n+    print(keyword_kind(\\\"extension\\\") == TokenKind.Extension)\\n+    print(keyword_kind(\\\"self\\\") == TokenKind.SelfKw)\\n+    print(keyword_kind(\\\"init\\\") == TokenKind.Init)\\n+    print(keyword_kind(\\\"None\\\") == TokenKind.NoneKw)\\n+    print(keyword_kind(\\\"enum\\\") == TokenKind.Enum)\\n+    print(keyword_kind(\\\"while\\\") == TokenKind.While)\\n+    print(keyword_kind(\\\"break\\\") == TokenKind.Break)\\n+    print(keyword_kind(\\\"continue\\\") == TokenKind.Continue)\\n+    print(keyword_kind(\\\"case\\\") == TokenKind.Case)\\n+    print(keyword_kind(\\\"match\\\") == TokenKind.Match)\\n+    print(keyword_kind(\\\"trait\\\") == TokenKind.Trait)\\n+    print(keyword_kind(\\\"for\\\") == TokenKind.For)\\n+    print(keyword_kind(\\\"in\\\") == TokenKind.In)\\n+    print(keyword_kind(\\\"extern\\\") == TokenKind.Extern)\\n+    print(keyword_kind(\\\"not\\\") == TokenKind.Not_)\\n+    print(keyword_kind(\\\"move\\\") == TokenKind.Move_)\\n+    print(keyword_kind(\\\"unsafe\\\") == TokenKind.Unsafe_)\\n+    print(keyword_kind(\\\"borrows\\\") == TokenKind.Borrows)\\n+    print(keyword_kind(\\\"lend\\\") == TokenKind.Lend)\\n+    print(keyword_kind(\\\"as\\\") == TokenKind.As)\\n+    print(keyword_kind(\\\"try\\\") == TokenKind.Try)\\n+    print(keyword_kind(\\\"catch\\\") == TokenKind.Catch)\\n+    print(keyword_kind(\\\"static\\\") == TokenKind.Static)\\n+    print(keyword_kind(\\\"public\\\") == TokenKind.Public)\\n+    print(keyword_kind(\\\"module\\\") == TokenKind.Ident)\\n+    print(keyword_kind(\\\"import\\\") == TokenKind.Ident)\\n+    print(keyword_kind(\\\"export\\\") == TokenKind.Ident)\\n+    print(keyword_kind(\\\"package\\\") == TokenKind.Ident)\\n+    print(keyword_kind(\\\"parent\\\") == TokenKind.Ident)\\n+    print(keyword_kind(\\\"type\\\") == TokenKind.Ident)\\n+    print(suffix_width(\\\"i8\\\") == 8)\\n+    print(suffix_width(\\\"i16\\\") == 16)\\n+    print(suffix_width(\\\"i32\\\") == 32)\\n+    print(suffix_width(\\\"i64\\\") == 64)\\n+    print(suffix_width(\\\"u8\\\") == 8)\\n+    print(suffix_width(\\\"u16\\\") == 16)\\n+    print(suffix_width(\\\"u32\\\") == 32)\\n+    print(suffix_width(\\\"u64\\\") == 64)\\n+    print(suffix_width(\\\"bad\\\") == 0)\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/lexer_advance_extract.saw b/prototypes/minivm/examples/owning_records/lexer_advance_extract.saw\\nnew file mode 100644\\nindex 00000000..989c7f6d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/lexer_advance_extract.saw\\n@@ -0,0 +1,53 @@\\n+static B_NL: Int = 10\\n+static B_CONT_LO: Int = 128\\n+static B_CONT_HI: Int = 191\\n+\\n+func ubyte(s: &String, i: Int) -> Int {\\n+    if i < 0 || i >= s.len() {\\n+        return -1\\n+    }\\n+    s.byte_at(i) as Int\\n+}\\n+\\n+struct Lexer {\\n+    src: String\\n+    slen: Int\\n+    pos: Int\\n+    line: Int\\n+    col: Int\\n+}\\n+\\n+extension Lexer {\\n+    func advance(&var self) -> Int {\\n+        let b = ubyte(&self.src, self.pos)\\n+        self.pos = self.pos + 1\\n+        if b == B_NL {\\n+            self.line = self.line + 1\\n+            self.col = 1\\n+        } else if b < B_CONT_LO || b > B_CONT_HI {\\n+            self.col = self.col + 1\\n+        }\\n+        b\\n+    }\\n+}\\n+\\n+func main() {\\n+    var lexer = Lexer(src: \\\"A\\\\né\\\", slen: 4, pos: 0, line: 1, col: 1)\\n+    print(lexer.advance())\\n+    print(lexer.pos)\\n+    print(lexer.line)\\n+    print(lexer.col)\\n+    print(lexer.advance())\\n+    print(lexer.pos)\\n+    print(lexer.line)\\n+    print(lexer.col)\\n+    print(lexer.advance())\\n+    print(lexer.pos)\\n+    print(lexer.line)\\n+    print(lexer.col)\\n+    print(lexer.advance())\\n+    print(lexer.pos)\\n+    print(lexer.line)\\n+    print(lexer.col)\\n+    print(ubyte(&lexer.src, lexer.pos))\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/loop_cleanup.saw b/prototypes/minivm/examples/owning_records/loop_cleanup.saw\\nnew file mode 100644\\nindex 00000000..d7bdbd40\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/loop_cleanup.saw\\n@@ -0,0 +1,17 @@\\n+struct Item { text: String, number: Int }\\n+\\n+func main() {\\n+    var index = 0\\n+    var last = Item(text: \\\"start\\\", number: -1)\\n+    while index < 200 {\\n+        let selected = if index % 2 == 0 {\\n+            Item(text: \\\"even\\\", number: index)\\n+        } else {\\n+            Item(text: \\\"odd\\\", number: index)\\n+        }\\n+        last = selected\\n+        index += 1\\n+    }\\n+    print(last.text)\\n+    print(last.number)\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/references_methods_abi.saw b/prototypes/minivm/examples/owning_records/references_methods_abi.saw\\nnew file mode 100644\\nindex 00000000..99e89dd5\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/references_methods_abi.saw\\n@@ -0,0 +1,30 @@\\n+struct Text { value: String, number: Int }\\n+\\n+func mixed(prefix: Int, source: &Text, replacement: Text) -> Text {\\n+    if prefix == 0 { return source }\\n+    replacement\\n+}\\n+func replace(value: &var Text, next: Text) { value = next }\\n+func read_text(value: &String) -> String { value }\\n+func set_text(value: &var String, next: String) { value = next }\\n+func forward_field(value: &var Text) {\\n+    print(read_text(&value.value))\\n+    set_text(&var value.value, \\\"field\\\")\\n+}\\n+\\n+extension Text {\\n+    func read(&self) -> String { self.value }\\n+    func update(&var self, next: String) { self.value = next }\\n+}\\n+\\n+func main() {\\n+    var value = Text(value: \\\"left\\\", number: 7)\\n+    let snapshot = mixed(0, &value, Text(value: \\\"unused\\\", number: 0))\\n+    value.update(next: \\\"right\\\")\\n+    print(snapshot.value)\\n+    print(value.read())\\n+    replace(&var value, Text(value: \\\"next\\\", number: 9))\\n+    forward_field(&var value)\\n+    print(value.read())\\n+    print(value.number)\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/reject_string_match_duplicate.saw b/prototypes/minivm/examples/owning_records/reject_string_match_duplicate.saw\\nnew file mode 100644\\nindex 00000000..b6549079\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/reject_string_match_duplicate.saw\\n@@ -0,0 +1,7 @@\\n+func main() {\\n+    match \\\"\\\\{\\\" {\\n+        case \\\"\\\\{\\\" -> { print(1) },\\n+        case \\\"\\\\u{7b}\\\" -> { print(2) },\\n+        case _ -> { print(3) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/reject_string_match_interpolation.saw b/prototypes/minivm/examples/owning_records/reject_string_match_interpolation.saw\\nnew file mode 100644\\nindex 00000000..306e10ff\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/reject_string_match_interpolation.saw\\n@@ -0,0 +1,7 @@\\n+func main() {\\n+    let value = \\\"one\\\"\\n+    match \\\"one\\\" {\\n+        case \\\"{value}\\\" -> { print(1) },\\n+        case _ -> { print(2) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/reject_string_match_no_wildcard.saw b/prototypes/minivm/examples/owning_records/reject_string_match_no_wildcard.saw\\nnew file mode 100644\\nindex 00000000..22b58a08\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/reject_string_match_no_wildcard.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    match \\\"one\\\" {\\n+        case \\\"one\\\" -> { print(1) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/reject_string_match_non_string.saw b/prototypes/minivm/examples/owning_records/reject_string_match_non_string.saw\\nnew file mode 100644\\nindex 00000000..156d2131\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/reject_string_match_non_string.saw\\n@@ -0,0 +1,6 @@\\n+func main() {\\n+    match \\\"one\\\" {\\n+        case 1 -> { print(1) },\\n+        case _ -> { print(2) }\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/string_match.saw b/prototypes/minivm/examples/owning_records/string_match.saw\\nnew file mode 100644\\nindex 00000000..8d590c32\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/string_match.saw\\n@@ -0,0 +1,21 @@\\n+func classify(value: String) -> Int {\\n+    match value {\\n+        case \\\"\\\" -> { 1 },\\n+        case \\\"plain\\\" -> { 2 },\\n+        case \\\"a\\\\0b\\\" -> { 3 },\\n+        case \\\"é\\\" -> { 4 },\\n+        case \\\"\\\\{left\\\\}\\\" -> { 5 },\\n+        case \\\"\\\\u{1}x\\\" -> { 6 },\\n+        case _ -> { 7 }\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(classify(\\\"\\\"))\\n+    print(classify(\\\"plain\\\"))\\n+    print(classify(\\\"a\\\\0b\\\"))\\n+    print(classify(\\\"é\\\"))\\n+    print(classify(\\\"\\\\{left\\\\}\\\"))\\n+    print(classify(\\\"\\\\u{1}x\\\"))\\n+    print(classify(\\\"other\\\"))\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/string_match_snapshot.saw b/prototypes/minivm/examples/owning_records/string_match_snapshot.saw\\nnew file mode 100644\\nindex 00000000..2c474aea\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/string_match_snapshot.saw\\n@@ -0,0 +1,11 @@\\n+func main() {\\n+    var value = \\\"old\\\"\\n+    match value {\\n+        case \\\"old\\\" -> {\\n+            value = \\\"changed\\\"\\n+            print(\\\"old\\\")\\n+        },\\n+        case _ -> { print(\\\"wrong\\\") }\\n+    }\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/owning_records/value_control_cleanup.saw b/prototypes/minivm/examples/owning_records/value_control_cleanup.saw\\nnew file mode 100644\\nindex 00000000..c769c919\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/owning_records/value_control_cleanup.saw\\n@@ -0,0 +1,37 @@\\n+struct Item { text: String, number: Int }\\n+\\n+func choose(flag: Bool) -> Item {\\n+    let result = if flag {\\n+        let local = Item(text: \\\"then\\\", number: 1)\\n+        local\\n+    } else {\\n+        let local = Item(text: \\\"else\\\", number: 0)\\n+        local\\n+    }\\n+    result\\n+}\\n+\\n+func classify(word: String) -> Item {\\n+    match word {\\n+        case \\\"red\\\" -> { Item(text: \\\"red\\\", number: 2) },\\n+        case _ -> { Item(text: \\\"other\\\", number: 0) }\\n+    }\\n+}\\n+\\n+func early(flag: Bool) -> Item {\\n+    let sibling = Item(text: \\\"sibling\\\", number: 0)\\n+    if flag { return Item(text: \\\"early\\\", number: 3) }\\n+    sibling\\n+}\\n+\\n+func main() {\\n+    let first = choose(true)\\n+    print(first.text)\\n+    print(first.number)\\n+    let second = classify(\\\"red\\\")\\n+    print(second.text)\\n+    print(second.number)\\n+    let third = early(true)\\n+    print(third.text)\\n+    print(third.number)\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/reject_record_field.saw b/prototypes/minivm/examples/strings/reject_record_field.saw\\ndeleted file mode 100644\\nindex cc58b067..00000000\\n--- a/prototypes/minivm/examples/strings/reject_record_field.saw\\n+++ /dev/null\\n@@ -1,2 +0,0 @@\\n-struct Bad { text: String }\\n-func main() {}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 90f35ab2..44e141ef 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -653,7 +653,6 @@ extension Compiler {\\n             let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after record field\\\")\\n             let ty = try self.parse_type()\\n             if ty == ValueType.Void { return problem(\\\"record field cannot have Void type\\\", field.line, field.col) }\\n-            if ty == ValueType.String { return problem(\\\"String record fields are not supported\\\", field.line, field.col) }\\n             if reference_type(ty) { return problem(\\\"reference record fields are not supported\\\", field.line, field.col) }\\n             try! names.push(field.value)\\n             try! types.push(ty)\\n@@ -722,6 +721,24 @@ extension Compiler {\\n                          t: &Token) -> Result<Void, ProtoError> {\\n         if dst == src { return }\\n         let layout = try value_layout(&self.records, ty)\\n+        if layout.len() > 1 {\\n+            let temporary = try self.new_value(ty)\\n+            var word = 0\\n+            while word < layout.len() {\\n+                let snapshot_copy = self.emit_at(Op.Copy, t.line, t.col)\\n+                self.code[snapshot_copy].dst = temporary + word\\n+                self.code[snapshot_copy].a = src + word\\n+                word += 1\\n+            }\\n+            word = 0\\n+            while word < layout.len() {\\n+                let destination_copy = self.emit_at(Op.Copy, t.line, t.col)\\n+                self.code[destination_copy].dst = dst + word\\n+                self.code[destination_copy].a = temporary + word\\n+                word += 1\\n+            }\\n+            return\\n+        }\\n         var i = 0\\n         while i < layout.len() {\\n             let at = self.emit_at(Op.Copy, t.line, t.col)\\n@@ -811,11 +828,11 @@ extension Compiler {\\n         while self.pending_writes.len() > count { let _ = self.pending_writes.pop() }\\n     }\\n \\n-    func cleanup_scope(&var self, preserve: Int, t: &Token) {\\n+    func cleanup_scope(&var self, preserve_start: Int, preserve_end: Int, t: &Token) {\\n         var i = 0\\n         while i < self.string_slots.len() {\\n             if self.string_slots[i].depth == self.scope_depth {\\n-                if self.string_slots[i].slot == preserve {\\n+                if self.string_slots[i].slot >= preserve_start && self.string_slots[i].slot < preserve_end {\\n                     self.string_slots[i].depth -= 1\\n                 } else {\\n                     let at = self.emit_at(Op.Drop, t.line, t.col)\\n@@ -826,10 +843,10 @@ extension Compiler {\\n         }\\n     }\\n \\n-    func cleanup_function(&var self, preserve: Int, t: &Token) {\\n+    func cleanup_function(&var self, preserve_start: Int, preserve_end: Int, t: &Token) {\\n         var i = 0\\n         while i < self.string_slots.len() {\\n-            if self.string_slots[i].slot != preserve {\\n+            if self.string_slots[i].slot < preserve_start || self.string_slots[i].slot >= preserve_end {\\n                 let at = self.emit_at(Op.Drop, t.line, t.col)\\n                 self.code[at].a = self.string_slots[i].slot\\n             }\\n@@ -2140,8 +2157,13 @@ extension Compiler {\\n             if had_newline { self.skip_newlines() }\\n             if self.at(TokenKind.RBrace) {\\n                 let close = self.token()\\n-                let preserve = if not returns && not value.always_returns && value.ty == ValueType.String { value.slot } else { -1 }\\n-                self.cleanup_scope(preserve, &close)\\n+                var preserve_start = -1\\n+                var preserve_end = -1\\n+                if not returns && not value.always_returns {\\n+                    preserve_start = value.slot\\n+                    preserve_end = value.slot + (try value_layout(&self.records, value.ty)).len()\\n+                }\\n+                self.cleanup_scope(preserve_start, preserve_end, &close)\\n                 let _ = self.take()\\n                 self.leave_scope()\\n                 self.depth -= 1\\n@@ -2159,7 +2181,7 @@ extension Compiler {\\n             if not had_newline { return problem(\\\"expected newline after statement\\\", start.line, start.col) }\\n         }\\n         let close = try self.expect(TokenKind.RBrace, \\\"expected closing brace\\\")\\n-        self.cleanup_scope(-1, &close)\\n+        self.cleanup_scope(-1, -1, &close)\\n         self.leave_scope()\\n         self.depth -= 1\\n         self.scope_depth -= 1\\n@@ -2266,15 +2288,18 @@ extension Compiler {\\n         var subject = try self.parse_expr()\\n         subject = try self.snapshot(subject, &mt)\\n         var ei = -1\\n+        let string_match = subject.ty == ValueType.String\\n         match subject.ty {\\n             case Enum(index) -> { ei = index },\\n-            case _ -> { return problem(\\\"match subject must have enum type\\\", mt.line, mt.col) }\\n+            case String -> {},\\n+            case _ -> { return problem(\\\"match subject must have enum or String type\\\", mt.line, mt.col) }\\n         }\\n         let _ = try self.expect(TokenKind.LBrace, \\\"expected `\\\\{` after match subject\\\")\\n         self.skip_newlines()\\n         var covered = Vector<Bool>()\\n+        var string_patterns = Vector<String>()\\n         var n = 0\\n-        while n < self.enums[ei].cases.len() {\\n+        while not string_match && n < self.enums[ei].cases.len() {\\n             try! covered.push(false)\\n             n += 1\\n         }\\n@@ -2287,9 +2312,27 @@ extension Compiler {\\n             if wildcard { return self.fail(\\\"match arm follows wildcard\\\") }\\n             if pending_false >= 0 { self.code[pending_false].target = self.code.len() }\\n             let ct = try self.expect(TokenKind.Case, \\\"expected `case` in match\\\")\\n-            let pattern = try self.expect(TokenKind.Ident, \\\"expected enum case pattern\\\")\\n+            if string_match && self.at(TokenKind.InterpString) { return self.fail(\\\"string interpolation is not supported in match patterns\\\") }\\n+            if string_match && not self.at(TokenKind.StringLit) && not self.at(TokenKind.Ident) {\\n+                return self.fail(\\\"String match patterns must be String literals or `_`\\\")\\n+            }\\n+            let pattern = if string_match { self.take() } else { try self.expect(TokenKind.Ident, \\\"expected enum case pattern\\\") }\\n             var ordinal = -1\\n-            if pattern.value == \\\"_\\\" { wildcard = true }\\n+            var string_pattern = false\\n+            if pattern.kind == TokenKind.Ident && pattern.value == \\\"_\\\" { wildcard = true }\\n+            else if string_match {\\n+                if pattern.kind != TokenKind.StringLit { return problem(\\\"String match patterns must be String literals or `_`\\\", pattern.line, pattern.col) }\\n+                let decoded = try self.decode_string_literal(&pattern)\\n+                var duplicate = false\\n+                var si = 0\\n+                while si < string_patterns.len() {\\n+                    if string_patterns[si] == decoded { duplicate = true }\\n+                    si += 1\\n+                }\\n+                if duplicate { return problem(\\\"duplicate String match pattern\\\", pattern.line, pattern.col) }\\n+                try! string_patterns.push(decoded)\\n+                string_pattern = true\\n+            }\\n             else if self.at(TokenKind.Dot) {\\n                 let qualifier = self.enum_index(pattern.value)\\n                 if qualifier != ei { return problem(\\\"enum qualifier `{pattern.value}` does not match subject enum `{self.enums[ei].name}`\\\", pattern.line, pattern.col) }\\n@@ -2301,13 +2344,15 @@ extension Compiler {\\n                 ordinal = self.enum_case_index(ei, pattern.value)\\n                 if ordinal < 0 { return problem(\\\"enum `{self.enums[ei].name}` has no case `{pattern.value}`\\\", pattern.line, pattern.col) }\\n             }\\n-            if ordinal >= 0 {\\n-                if covered[ordinal] { return problem(\\\"duplicate match case `{self.enums[ei].cases[ordinal]}`\\\", pattern.line, pattern.col) }\\n-                covered[ordinal] = true\\n+            if ordinal >= 0 || string_pattern {\\n+                if ordinal >= 0 {\\n+                    if covered[ordinal] { return problem(\\\"duplicate match case `{self.enums[ei].cases[ordinal]}`\\\", pattern.line, pattern.col) }\\n+                    covered[ordinal] = true\\n+                }\\n                 let constant = self.new_slot(subject.ty)\\n-                let ci = self.emit_at(Op.Const, pattern.line, pattern.col)\\n+                let ci = self.emit_at(if string_pattern { Op.StringConst } else { Op.Const }, pattern.line, pattern.col)\\n                 self.code[ci].dst = constant\\n-                self.code[ci].immediate = ordinal\\n+                self.code[ci].immediate = if string_pattern { self.intern_string(string_patterns[string_patterns.len() - 1]) } else { ordinal }\\n                 let equal = self.new_slot(ValueType.Bool)\\n                 let eq = self.emit_at(Op.Eq, pattern.line, pattern.col)\\n                 self.code[eq].dst = equal\\n@@ -2346,7 +2391,7 @@ extension Compiler {\\n         let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace for match\\\")\\n         if arms == 0 { return problem(\\\"non-exhaustive match\\\", mt.line, mt.col) }\\n         var exhaustive = wildcard\\n-        if not exhaustive {\\n+        if not string_match && not exhaustive {\\n             exhaustive = true\\n             n = 0\\n             while n < covered.len() {\\n@@ -2354,7 +2399,10 @@ extension Compiler {\\n                 n += 1\\n             }\\n         }\\n-        if not exhaustive { return problem(\\\"non-exhaustive match\\\", mt.line, mt.col) }\\n+        if not exhaustive {\\n+            if string_match { return problem(\\\"String match requires a final wildcard\\\", mt.line, mt.col) }\\n+            return problem(\\\"non-exhaustive match\\\", mt.line, mt.col)\\n+        }\\n         if values.len() == 0 {\\n             if pending_false >= 0 { self.code[pending_false].target = self.code.len() }\\n             return returning_value()\\n@@ -2413,7 +2461,7 @@ extension Compiler {\\n             try self.terminator()\\n         }\\n         let close = try self.expect(TokenKind.RBrace, \\\"expected closing brace\\\")\\n-        self.cleanup_scope(-1, &close)\\n+        self.cleanup_scope(-1, -1, &close)\\n         self.leave_scope()\\n         self.depth -= 1\\n         self.scope_depth -= 1\\n@@ -2425,15 +2473,18 @@ extension Compiler {\\n         var subject = try self.parse_expr()\\n         subject = try self.snapshot(subject, &mt)\\n         var ei = -1\\n+        let string_match = subject.ty == ValueType.String\\n         match subject.ty {\\n             case Enum(index) -> { ei = index },\\n-            case _ -> { return problem(\\\"match subject must have enum type\\\", mt.line, mt.col) }\\n+            case String -> {},\\n+            case _ -> { return problem(\\\"match subject must have enum or String type\\\", mt.line, mt.col) }\\n         }\\n         let _ = try self.expect(TokenKind.LBrace, \\\"expected `\\\\{` after match subject\\\")\\n         self.skip_newlines()\\n         var covered = Vector<Bool>()\\n+        var string_patterns = Vector<String>()\\n         var i = 0\\n-        while i < self.enums[ei].cases.len() {\\n+        while not string_match && i < self.enums[ei].cases.len() {\\n             try! covered.push(false)\\n             i += 1\\n         }\\n@@ -2446,10 +2497,27 @@ extension Compiler {\\n             if wildcard { return self.fail(\\\"match arm follows wildcard\\\") }\\n             if pending_false >= 0 { self.code[pending_false].target = self.code.len() }\\n             let ct = try self.expect(TokenKind.Case, \\\"expected `case` in match\\\")\\n-            let pattern = try self.expect(TokenKind.Ident, \\\"expected enum case pattern\\\")\\n+            if string_match && self.at(TokenKind.InterpString) { return self.fail(\\\"string interpolation is not supported in match patterns\\\") }\\n+            if string_match && not self.at(TokenKind.StringLit) && not self.at(TokenKind.Ident) {\\n+                return self.fail(\\\"String match patterns must be String literals or `_`\\\")\\n+            }\\n+            let pattern = if string_match { self.take() } else { try self.expect(TokenKind.Ident, \\\"expected enum case pattern\\\") }\\n             var ordinal = -1\\n-            if pattern.value == \\\"_\\\" {\\n+            var string_pattern = false\\n+            if pattern.kind == TokenKind.Ident && pattern.value == \\\"_\\\" {\\n                 wildcard = true\\n+            } else if string_match {\\n+                if pattern.kind != TokenKind.StringLit { return problem(\\\"String match patterns must be String literals or `_`\\\", pattern.line, pattern.col) }\\n+                let decoded = try self.decode_string_literal(&pattern)\\n+                var duplicate = false\\n+                var si = 0\\n+                while si < string_patterns.len() {\\n+                    if string_patterns[si] == decoded { duplicate = true }\\n+                    si += 1\\n+                }\\n+                if duplicate { return problem(\\\"duplicate String match pattern\\\", pattern.line, pattern.col) }\\n+                try! string_patterns.push(decoded)\\n+                string_pattern = true\\n             } else if self.at(TokenKind.Dot) {\\n                 let qualifier = self.enum_index(pattern.value)\\n                 if qualifier != ei { return problem(\\\"enum qualifier `{pattern.value}` does not match subject enum `{self.enums[ei].name}`\\\", pattern.line, pattern.col) }\\n@@ -2461,13 +2529,15 @@ extension Compiler {\\n                 ordinal = self.enum_case_index(ei, pattern.value)\\n                 if ordinal < 0 { return problem(\\\"enum `{self.enums[ei].name}` has no case `{pattern.value}`\\\", pattern.line, pattern.col) }\\n             }\\n-            if ordinal >= 0 {\\n-                if covered[ordinal] { return problem(\\\"duplicate match case `{self.enums[ei].cases[ordinal]}`\\\", pattern.line, pattern.col) }\\n-                covered[ordinal] = true\\n+            if ordinal >= 0 || string_pattern {\\n+                if ordinal >= 0 {\\n+                    if covered[ordinal] { return problem(\\\"duplicate match case `{self.enums[ei].cases[ordinal]}`\\\", pattern.line, pattern.col) }\\n+                    covered[ordinal] = true\\n+                }\\n                 let constant = self.new_slot(subject.ty)\\n-                let ci = self.emit_at(Op.Const, pattern.line, pattern.col)\\n+                let ci = self.emit_at(if string_pattern { Op.StringConst } else { Op.Const }, pattern.line, pattern.col)\\n                 self.code[ci].dst = constant\\n-                self.code[ci].immediate = ordinal\\n+                self.code[ci].immediate = if string_pattern { self.intern_string(string_patterns[string_patterns.len() - 1]) } else { ordinal }\\n                 let equal = self.new_slot(ValueType.Bool)\\n                 let eq = self.emit_at(Op.Eq, pattern.line, pattern.col)\\n                 self.code[eq].dst = equal\\n@@ -2496,7 +2566,7 @@ extension Compiler {\\n         let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace for match\\\")\\n         if arm_count == 0 { return problem(\\\"non-exhaustive match\\\", mt.line, mt.col) }\\n         var exhaustive = wildcard\\n-        if not exhaustive {\\n+        if not string_match && not exhaustive {\\n             exhaustive = true\\n             i = 0\\n             while i < covered.len() {\\n@@ -2504,7 +2574,10 @@ extension Compiler {\\n                 i += 1\\n             }\\n         }\\n-        if not exhaustive { return problem(\\\"non-exhaustive match\\\", mt.line, mt.col) }\\n+        if not exhaustive {\\n+            if string_match { return problem(\\\"String match requires a final wildcard\\\", mt.line, mt.col) }\\n+            return problem(\\\"non-exhaustive match\\\", mt.line, mt.col)\\n+        }\\n         let continuation = self.code.len()\\n         if pending_false >= 0 { self.code[pending_false].target = continuation }\\n         i = 0\\n@@ -2549,7 +2622,8 @@ extension Compiler {\\n                 if value.always_returns { return true }\\n                 src = value.slot\\n             } else if self.current_result != ValueType.Void { return problem(\\\"non-Void return requires a value\\\", t.line, t.col) }\\n-            self.cleanup_function(src, &t)\\n+            let preserve_end = if src < 0 { -1 } else { src + (try value_layout(&self.records, self.current_result)).len() }\\n+            self.cleanup_function(src, preserve_end, &t)\\n             let at = self.emit_at(Op.Return, t.line, t.col)\\n             self.code[at].a = src\\n             return true\\n@@ -2701,7 +2775,8 @@ extension Compiler {\\n                 if tail.always_returns { returns = true }\\n                 else {\\n                     let return_token = self.token()\\n-                    self.cleanup_function(tail.slot, &return_token)\\n+                    let preserve_end = tail.slot + (try value_layout(&self.records, self.current_result)).len()\\n+                    self.cleanup_function(tail.slot, preserve_end, &return_token)\\n                     let r = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n                     self.code[r].a = tail.slot\\n                     returns = true\\n@@ -2720,28 +2795,29 @@ extension Compiler {\\n         }\\n         if self.current_result == ValueType.Void {\\n             let return_token = self.token()\\n-            self.cleanup_function(-1, &return_token)\\n+            self.cleanup_function(-1, -1, &return_token)\\n             let _ = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n         } else {\\n             // Return-path checking above proves this padding unreachable. It gives\\n             // end-of-block jumps a real target and leaves every emitted block terminated.\\n             let slot = try self.new_value(self.current_result)\\n-            if self.current_result == ValueType.String {\\n-                let c = self.emit_at(Op.StringConst, self.signatures[index].line, self.signatures[index].col)\\n-                self.code[c].dst = slot\\n-                self.code[c].immediate = self.intern_string(\\\"\\\")\\n-            } else {\\n-                let layout = try value_layout(&self.records, self.current_result)\\n-                var word = 0\\n-                while word < layout.len() {\\n+            let layout = try value_layout(&self.records, self.current_result)\\n+            var word = 0\\n+            while word < layout.len() {\\n+                if layout[word] == ValueType.String {\\n+                    let c = self.emit_at(Op.StringConst, self.signatures[index].line, self.signatures[index].col)\\n+                    self.code[c].dst = slot + word\\n+                    self.code[c].immediate = self.intern_string(\\\"\\\")\\n+                } else {\\n                 let c = self.emit_at(Op.Const, self.signatures[index].line, self.signatures[index].col)\\n                 self.code[c].dst = slot + word\\n                 self.code[c].immediate = 0\\n-                word += 1\\n                 }\\n+                word += 1\\n             }\\n             let return_token = self.token()\\n-            self.cleanup_function(slot, &return_token)\\n+            let preserve_end = slot + (try value_layout(&self.records, self.current_result)).len()\\n+            self.cleanup_function(slot, preserve_end, &return_token)\\n             let r = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n             self.code[r].a = slot\\n         }\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex 169a4a9a..656a8a12 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -25,12 +25,12 @@ func block_name(out: &var StringBuilder, instruction_index: Int) {\\n     put_int(&var out, instruction_index)\\n }\\n \\n-func emit_aggregate_type(out: &var StringBuilder, word_count: Int) {\\n+func emit_aggregate_type(out: &var StringBuilder, layout: &Vector<ValueType>) {\\n     put(&var out, \\\"\\\\{ \\\")\\n     var word_index = 0\\n-    while word_index < word_count {\\n+    while word_index < layout.len() {\\n         if word_index > 0 { put(&var out, \\\", \\\") }\\n-        put(&var out, \\\"i64\\\")\\n+        put(&var out, if string_type(layout[word_index]) { \\\"ptr\\\" } else { \\\"i64\\\" })\\n         word_index = word_index + 1\\n     }\\n     put(&var out, \\\" \\\\}\\\")\\n@@ -51,7 +51,7 @@ func emit_value_type(out: &var StringBuilder, records: &Vector<RecordIR>,\\n         return\\n     }\\n     let layout = try value_layout(&records, t)\\n-    emit_aggregate_type(&var out, layout.len())\\n+    emit_aggregate_type(&var out, &layout)\\n     return\\n }\\n \\n@@ -455,17 +455,6 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n             let target = try reference_target(&program.references, function.slots[inst.a])\\n             let layout = try value_layout(&program.records, target)\\n-            if string_type(target) {\\n-                put(&var out, \\\"  \\\")\\n-                temp_name(&var out, instruction_index, \\\"v0\\\")\\n-                put(&var out, \\\" = load ptr, ptr \\\")\\n-                temp_name(&var out, instruction_index, \\\"ref\\\")\\n-                put(&var out, \\\"\\\\n\\\")\\n-                assign_string_temp(&var out, instruction_index, \\\"v0\\\", inst.dst, true)\\n-                next_block(&var out, instruction_index)\\n-                put(&var out, \\\"\\\\n\\\")\\n-                return\\n-            }\\n             var word = 0\\n             while word < layout.len() {\\n                 if word > 0 {\\n@@ -479,7 +468,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 }\\n                 put(&var out, \\\"  \\\")\\n                 temp_name(&var out, instruction_index, \\\"v{word}\\\")\\n-                put(&var out, \\\" = load i64, ptr \\\")\\n+                put(&var out, if string_type(layout[word]) { \\\" = load ptr, ptr \\\" } else { \\\" = load i64, ptr \\\" })\\n                 if word == 0 { temp_name(&var out, instruction_index, \\\"ref\\\") }\\n                 else { temp_name(&var out, instruction_index, \\\"ptr{word}\\\") }\\n                 put(&var out, \\\"\\\\n\\\")\\n@@ -487,7 +476,18 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             }\\n             word = 0\\n             while word < layout.len() {\\n-                store_temp(&var out, instruction_index, \\\"v{word}\\\", inst.dst + word)\\n+                if string_type(layout[word]) {\\n+                    put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n+                    temp_name(&var out, instruction_index, \\\"v{word}\\\")\\n+                    put(&var out, \\\")\\\\n\\\")\\n+                }\\n+                word += 1\\n+            }\\n+            word = 0\\n+            while word < layout.len() {\\n+                if string_type(layout[word]) {\\n+                    assign_string_temp(&var out, instruction_index, \\\"v{word}\\\", inst.dst + word, false)\\n+                } else { store_temp(&var out, instruction_index, \\\"v{word}\\\", inst.dst + word) }\\n                 word += 1\\n             }\\n             next_block(&var out, instruction_index)\\n@@ -497,30 +497,21 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n             let target = try reference_target(&program.references, function.slots[inst.a])\\n             let layout = try value_layout(&program.records, target)\\n-            if string_type(target) {\\n-                put(&var out, \\\"  \\\")\\n-                load_ref_slot(&var out, instruction_index, \\\"v0\\\", inst.b)\\n-                put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n-                temp_name(&var out, instruction_index, \\\"v0\\\")\\n-                put(&var out, \\\")\\\\n  \\\")\\n-                temp_name(&var out, instruction_index, \\\"old\\\")\\n-                put(&var out, \\\" = load ptr, ptr \\\")\\n-                temp_name(&var out, instruction_index, \\\"ref\\\")\\n-                put(&var out, \\\"\\\\n  call void @minivm_string_release(ptr \\\")\\n-                temp_name(&var out, instruction_index, \\\"old\\\")\\n-                put(&var out, \\\")\\\\n  store ptr \\\")\\n-                temp_name(&var out, instruction_index, \\\"v0\\\")\\n-                put(&var out, \\\", ptr \\\")\\n-                temp_name(&var out, instruction_index, \\\"ref\\\")\\n-                put(&var out, \\\"\\\\n\\\")\\n-                next_block(&var out, instruction_index)\\n-                put(&var out, \\\"\\\\n\\\")\\n-                return\\n-            }\\n             var word = 0\\n             while word < layout.len() {\\n                 put(&var out, \\\"  \\\")\\n-                load_slot(&var out, instruction_index, \\\"v{word}\\\", inst.b + word)\\n+                if string_type(layout[word]) {\\n+                    load_ref_slot(&var out, instruction_index, \\\"v{word}\\\", inst.b + word)\\n+                } else { load_slot(&var out, instruction_index, \\\"v{word}\\\", inst.b + word) }\\n+                word += 1\\n+            }\\n+            word = 0\\n+            while word < layout.len() {\\n+                if string_type(layout[word]) {\\n+                    put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n+                    temp_name(&var out, instruction_index, \\\"v{word}\\\")\\n+                    put(&var out, \\\")\\\\n\\\")\\n+                }\\n                 word += 1\\n             }\\n             word = 0\\n@@ -534,7 +525,16 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                     put_int(&var out, word)\\n                     put(&var out, \\\"\\\\n\\\")\\n                 }\\n-                put(&var out, \\\"  store i64 \\\")\\n+                if string_type(layout[word]) {\\n+                    put(&var out, \\\"  \\\")\\n+                    temp_name(&var out, instruction_index, \\\"old{word}\\\")\\n+                    put(&var out, \\\" = load ptr, ptr \\\")\\n+                    if word == 0 { temp_name(&var out, instruction_index, \\\"ref\\\") }\\n+                    else { temp_name(&var out, instruction_index, \\\"ptr{word}\\\") }\\n+                    put(&var out, \\\"\\\\n  call void @minivm_string_release(ptr \\\")\\n+                    temp_name(&var out, instruction_index, \\\"old{word}\\\")\\n+                    put(&var out, \\\")\\\\n  store ptr \\\")\\n+                } else { put(&var out, \\\"  store i64 \\\") }\\n                 temp_name(&var out, instruction_index, \\\"v{word}\\\")\\n                 put(&var out, \\\", ptr \\\")\\n                 if word == 0 { temp_name(&var out, instruction_index, \\\"ref\\\") }\\n@@ -734,13 +734,17 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                     put(&var out, \\\"  \\\")\\n                     temp_name(&var out, instruction_index, \\\"result{result_index}\\\")\\n                     put(&var out, \\\" = extractvalue \\\")\\n-                    emit_aggregate_type(&var out, result_layout.len())\\n+                    emit_aggregate_type(&var out, &result_layout)\\n                     put(&var out, \\\" \\\")\\n                     temp_name(&var out, instruction_index, \\\"call\\\")\\n                     put(&var out, \\\", \\\")\\n                     put_int(&var out, result_index)\\n                     put(&var out, \\\"\\\\n\\\")\\n-                    store_temp(&var out, instruction_index, \\\"result{result_index}\\\", inst.dst + result_index)\\n+                    if string_type(result_layout[result_index]) {\\n+                        assign_string_temp(&var out, instruction_index, \\\"result{result_index}\\\", inst.dst + result_index, false)\\n+                    } else {\\n+                        store_temp(&var out, instruction_index, \\\"result{result_index}\\\", inst.dst + result_index)\\n+                    }\\n                     result_index = result_index + 1\\n                 }\\n             }\\n@@ -842,6 +846,19 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n                 temp_name(&var out, instruction_index, \\\"ret\\\")\\n                 put(&var out, \\\")\\\\n\\\")\\n+            } else if function.result != ValueType.Void && not scalar_type(function.result) {\\n+                let result_layout = try value_layout(&program.records, function.result)\\n+                var result_index = 0\\n+                while result_index < result_layout.len() {\\n+                    put(&var out, \\\"  \\\")\\n+                    if string_type(result_layout[result_index]) {\\n+                        load_ref_slot(&var out, instruction_index, \\\"retword{result_index}\\\", inst.a + result_index)\\n+                        put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n+                        temp_name(&var out, instruction_index, \\\"retword{result_index}\\\")\\n+                        put(&var out, \\\")\\\\n\\\")\\n+                    } else { load_slot(&var out, instruction_index, \\\"retword{result_index}\\\", inst.a + result_index) }\\n+                    result_index += 1\\n+                }\\n             }\\n             var cleanup = 0\\n             while cleanup < function.slots.len() {\\n@@ -872,16 +889,14 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 let result_layout = try value_layout(&program.records, function.result)\\n                 var result_index = 0\\n                 while result_index < result_layout.len() {\\n-                    put(&var out, \\\"  \\\")\\n-                    load_slot(&var out, instruction_index, \\\"retword{result_index}\\\", inst.a + result_index)\\n                     put(&var out, \\\"  \\\")\\n                     temp_name(&var out, instruction_index, \\\"retagg{result_index}\\\")\\n                     put(&var out, \\\" = insertvalue \\\")\\n-                    emit_aggregate_type(&var out, result_layout.len())\\n+                    emit_aggregate_type(&var out, &result_layout)\\n                     put(&var out, \\\" \\\")\\n                     if result_index == 0 { put(&var out, \\\"undef\\\") }\\n                     else { temp_name(&var out, instruction_index, \\\"retagg{result_index - 1}\\\") }\\n-                    put(&var out, \\\", i64 \\\")\\n+                    put(&var out, if string_type(result_layout[result_index]) { \\\", ptr \\\" } else { \\\", i64 \\\" })\\n                     temp_name(&var out, instruction_index, \\\"retword{result_index}\\\")\\n                     put(&var out, \\\", \\\")\\n                     put_int(&var out, result_index)\\n@@ -889,7 +904,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                     result_index = result_index + 1\\n                 }\\n                 put(&var out, \\\"  ret \\\")\\n-                emit_aggregate_type(&var out, result_layout.len())\\n+                emit_aggregate_type(&var out, &result_layout)\\n                 put(&var out, \\\" \\\")\\n                 temp_name(&var out, instruction_index, \\\"retagg{result_layout.len() - 1}\\\")\\n                 put(&var out, \\\"\\\\n\\\")\\n@@ -921,7 +936,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n             var parameter_word = 0\\n             while parameter_word < parameter_layout.len() {\\n                 if argument_index > 0 { put(&var out, \\\", \\\") }\\n-                put(&var out, \\\"i64 %arg\\\")\\n+                put(&var out, if string_type(parameter_layout[parameter_word]) { \\\"ptr %arg\\\" } else { \\\"i64 %arg\\\" })\\n                 put_int(&var out, argument_index)\\n                 argument_index = argument_index + 1\\n                 parameter_word = parameter_word + 1\\n@@ -937,7 +952,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     while slot_index < function.slots.len() {\\n         put(&var out, \\\"  \\\")\\n         slot_name(&var out, slot_index)\\n-        if reference_type(function.slots[slot_index]) || string_type(function.slots[slot_index]) {\\n+        if reference_type(function.slots[slot_index]) {\\n             put(&var out, \\\" = alloca ptr\\\\n\\\")\\n         } else {\\n             put(&var out, \\\" = getelementptr [\\\")\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex 7d1bdc46..7db30436 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -253,7 +253,7 @@ public func validate_enums(enums: &Vector<EnumIR>) -> Result<Void, ProtoError> {\\n \\n func append_layout(records: &Vector<RecordIR>, t: ValueType,\\n                    path: &var Vector<Int>, words: &var Vector<ValueType>) -> Result<Void, ProtoError> {\\n-    if scalar_type(t) {\\n+    if scalar_type(t) || string_type(t) {\\n         if words.len() >= 256 { return problem(\\\"record layout exceeds 256 words\\\", 0, 0) }\\n         try! words.push(t)\\n         return\\n@@ -284,7 +284,6 @@ func append_layout(records: &Vector<RecordIR>, t: ValueType,\\n             return\\n         },\\n         case Ref(_) -> { return problem(\\\"reference record fields are not supported\\\", 0, 0) },\\n-        case String -> { return problem(\\\"String record fields are not supported\\\", 0, 0) },\\n         case _ -> { return problem(\\\"Void is not a record field type\\\", 0, 0) }\\n     }\\n }\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 3cb6e4a8..3e4fb528 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -184,9 +184,14 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                     word += 1\\n                 }\\n                 word = 0\\n+                while word < values.len() {\\n+                    if string_type(layout[word]) { try string_retain(&var heap, values[word]) }\\n+                    word += 1\\n+                }\\n+                word = 0\\n                 while word < values.len() {\\n                     if string_type(layout[word]) {\\n-                        try string_assign(&var cells, base + inst.dst + word, values[word], &var heap, true)\\n+                        try string_assign(&var cells, base + inst.dst + word, values[word], &var heap, false)\\n                     } else { cells[base + inst.dst + word] = values[word] }\\n                     word += 1\\n                 }\\n@@ -203,9 +208,14 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                     word += 1\\n                 }\\n                 word = 0\\n+                while word < values.len() {\\n+                    if string_type(layout[word]) { try string_retain(&var heap, values[word]) }\\n+                    word += 1\\n+                }\\n+                word = 0\\n                 while word < values.len() {\\n                     if string_type(layout[word]) {\\n-                        try string_assign(&var cells, address + word, values[word], &var heap, true)\\n+                        try string_assign(&var cells, address + word, values[word], &var heap, false)\\n                     } else { cells[address + word] = values[word] }\\n                     word += 1\\n                 }\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex 75e4b7e1..ab5fa564 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -35,6 +35,14 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"owning_records/copy_replace_nested\\\", \\\"alpha\\\\ninner\\\\nbeta\\\\nchanged\\\\n\\\", section=\\\"owning_records\\\"),\\n+    RunCase(\\\"owning_records/references_methods_abi\\\", \\\"left\\\\nright\\\\nnext\\\\nfield\\\\n9\\\\n\\\", section=\\\"owning_records\\\"),\\n+    RunCase(\\\"owning_records/value_control_cleanup\\\", \\\"then\\\\n1\\\\nred\\\\n2\\\\nearly\\\\n3\\\\n\\\", section=\\\"owning_records\\\"),\\n+    RunCase(\\\"owning_records/loop_cleanup\\\", \\\"odd\\\\n199\\\\n\\\", section=\\\"owning_records\\\"),\\n+    RunCase(\\\"owning_records/string_match\\\", \\\"1\\\\n2\\\\n3\\\\n4\\\\n5\\\\n6\\\\n7\\\\n\\\", section=\\\"owning_records\\\"),\\n+    RunCase(\\\"owning_records/string_match_snapshot\\\", \\\"old\\\\nchanged\\\\n\\\", section=\\\"owning_records\\\"),\\n+    RunCase(\\\"owning_records/keyword_suffix_extract\\\", \\\"true\\\\n\\\" * 49, section=\\\"owning_records\\\"),\\n+    RunCase(\\\"owning_records/lexer_advance_extract\\\", \\\"65\\\\n1\\\\n1\\\\n2\\\\n10\\\\n2\\\\n2\\\\n1\\\\n195\\\\n3\\\\n2\\\\n2\\\\n169\\\\n4\\\\n2\\\\n2\\\\n-1\\\\n\\\", section=\\\"owning_records\\\"),\\n     RunCase(\\\"strings/literals_escapes\\\", \\\"\\\\nASCII\\\\ncafé🙂\\\\na\\\\x00b\\\\n{left} \\\\\\\\ right\\\\n\\\\x01x\\\\n\\\", section=\\\"strings\\\"),\\n     RunCase(\\\"strings/intrinsics\\\", \\\"0\\\\ntrue\\\\n5\\\\nfalse\\\\n104\\\\n111\\\\n101\\\\n169\\\\n\\\\n\\\\nhello\\\\ntrue\\\\ntrue\\\\ntrue\\\\ntrue\\\\n\\\", section=\\\"strings\\\"),\\n     RunCase(\\\"strings/brace_marker_provenance\\\", \\\"1\\\\n123\\\\n2\\\\n1\\\\n123\\\\n1\\\\n125\\\\n2\\\\n1\\\\n125\\\\n\\\", section=\\\"strings\\\"),\\n@@ -187,10 +195,13 @@ RUN_CASES = (\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"owning_records/reject_string_match_duplicate\\\", \\\"duplicate String match pattern\\\", \\\"owning_records\\\"),\\n+    RejectCase(\\\"owning_records/reject_string_match_non_string\\\", \\\"String match patterns must be String literals or `_`\\\", \\\"owning_records\\\"),\\n+    RejectCase(\\\"owning_records/reject_string_match_interpolation\\\", \\\"string interpolation is not supported in match patterns\\\", \\\"owning_records\\\"),\\n+    RejectCase(\\\"owning_records/reject_string_match_no_wildcard\\\", \\\"String match requires a final wildcard\\\", \\\"owning_records\\\"),\\n     RejectCase(\\\"strings/reject_interpolation\\\", \\\"string interpolation is not supported\\\", \\\"strings\\\"),\\n     RejectCase(\\\"strings/reject_arithmetic\\\", \\\"arithmetic requires integer operands\\\", \\\"strings\\\"),\\n     RejectCase(\\\"strings/reject_cast\\\", \\\"`as` requires integer source and target types\\\", \\\"strings\\\"),\\n-    RejectCase(\\\"strings/reject_record_field\\\", \\\"String record fields are not supported\\\", \\\"strings\\\"),\\n     RejectCase(\\\"strings/reject_static\\\", \\\"String statics are not supported\\\", \\\"strings\\\"),\\n     RejectCase(\\\"strings/reject_unknown_intrinsic\\\", \\\"unknown String intrinsic\\\", \\\"strings\\\"),\\n     RejectCase(\\\"strings/reject_intrinsic_label\\\", \\\"argument label\\\", \\\"strings\\\"),\\n@@ -397,7 +408,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -426,7 +437,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\") else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\ndiff --git a/prototypes/minivm/tests/owning_record_contract.saw b/prototypes/minivm/tests/owning_record_contract.saw\\nnew file mode 100644\\nindex 00000000..327b3112\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/owning_record_contract.saw\\n@@ -0,0 +1,98 @@\\n+import src.model.*\\n+import src.verify.*\\n+import src.vm.*\\n+import src.llvm\\n+import std.env.{Env}\\n+\\n+func valid(p: &Program) -> Bool {\\n+    try verify(&p) catch { return false }\\n+    true\\n+}\\n+\\n+func op(code: &var Vector<Instruction>, kind: Op, dst: Int, a: Int, b: Int, immediate: Int) {\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    i.immediate = immediate\\n+    try! code.push(move i)\\n+}\\n+\\n+func base_program() -> Program {\\n+    let strings: Vector<String> = [\\\"A\\\", \\\"B\\\", \\\"C\\\"]\\n+    let names: Vector<String> = [\\\"left\\\", \\\"right\\\"]\\n+    let fields: Vector<ValueType> = [ValueType.String, ValueType.String]\\n+    var records = Vector<RecordIR>()\\n+    try! records.push(RecordIR(name: \\\"Pair\\\", field_names: move names,\\n+        field_types: move fields, line: 1, col: 1))\\n+    var refs = Vector<ReferenceIR>()\\n+    try! refs.push(ReferenceIR(target: ValueType.Record(0), mutable: true))\\n+    try! refs.push(ReferenceIR(target: ValueType.String, mutable: true))\\n+    let slots: Vector<ValueType> = [ValueType.String, ValueType.String,\\n+        ValueType.String, ValueType.Ref(0), ValueType.Ref(0), ValueType.Ref(1)]\\n+    var code = Vector<Instruction>()\\n+    op(&var code, Op.StringConst, 0, -1, -1, 0)\\n+    op(&var code, Op.StringConst, 1, -1, -1, 1)\\n+    op(&var code, Op.StringConst, 2, -1, -1, 2)\\n+    op(&var code, Op.Addr, 3, 0, -1, 0)\\n+    op(&var code, Op.Addr, 4, 1, -1, 0)\\n+    // Destination [1,2] overwrites the last owner of source word 1.\\n+    // Both source claims must be retained before either destination is released.\\n+    op(&var code, Op.StoreRef, -1, 4, 0, 0)\\n+    op(&var code, Op.Print, -1, 0, -1, 0)\\n+    op(&var code, Op.Print, -1, 1, -1, 0)\\n+    op(&var code, Op.Print, -1, 2, -1, 0)\\n+    op(&var code, Op.StringConst, 0, -1, -1, 0)\\n+    op(&var code, Op.StringConst, 1, -1, -1, 1)\\n+    op(&var code, Op.StringConst, 2, -1, -1, 2)\\n+    op(&var code, Op.LoadRef, 1, 3, -1, 0)\\n+    op(&var code, Op.Print, -1, 0, -1, 0)\\n+    op(&var code, Op.Print, -1, 1, -1, 0)\\n+    op(&var code, Op.Print, -1, 2, -1, 0)\\n+    op(&var code, Op.Call, 0, -1, -1, 0)\\n+    code[16].target = 1\\n+    try! code[16].args.push(1)\\n+    try! code[16].args.push(2)\\n+    op(&var code, Op.Print, -1, 0, -1, 0)\\n+    op(&var code, Op.Print, -1, 1, -1, 0)\\n+    op(&var code, Op.Print, -1, 2, -1, 0)\\n+    op(&var code, Op.RefField, 5, 3, -1, 1)\\n+    op(&var code, Op.StoreRef, -1, 5, 0, 0)\\n+    op(&var code, Op.Return, -1, -1, -1, 0)\\n+    let params: Vector<ValueType> = [ValueType.Record(0)]\\n+    let echo_slots: Vector<ValueType> = [ValueType.String, ValueType.String]\\n+    var echo_code = Vector<Instruction>()\\n+    op(&var echo_code, Op.Return, -1, 0, -1, 0)\\n+    var funcs = Vector<FuncIR>()\\n+    try! funcs.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: move slots, code: move code))\\n+    try! funcs.push(FuncIR(name: \\\"echo\\\", params: move params, result: ValueType.Record(0),\\n+        slots: move echo_slots, code: move echo_code))\\n+    Program(strings: move strings, references: move refs, enums: Vector<EnumIR>(),\\n+        records: move records, functions: move funcs, main_index: 0)\\n+}\\n+\\n+func main() {\\n+    let p = base_program()\\n+    assert(valid(&p), \\\"owning aggregate accesses and ABI\\\")\\n+    let args = try! Env.args()\\n+    if args.len() == 2 && args[1] == \\\"emit-llvm\\\" {\\n+        print(try! llvm.emit_llvm(&p))\\n+        return\\n+    }\\n+    let layout = try! value_layout(&p.records, ValueType.Record(0))\\n+    assert(layout.len() == 2 && layout[0] == ValueType.String && layout[1] == ValueType.String,\\n+        \\\"owning record layout preserves String leaves\\\")\\n+    var bad_range = p.copy()\\n+    bad_range.functions[0].code[4].a = 2\\n+    assert(not valid(&bad_range), \\\"record address cannot run through reference slot\\\")\\n+    var bad_field = p.copy()\\n+    bad_field.functions[0].code[20].immediate = 2\\n+    assert(not valid(&bad_field), \\\"String field index bounds\\\")\\n+    var reference_field = p.copy()\\n+    reference_field.records[0].field_types.set(1, ValueType.Ref(1))\\n+    assert(not valid(&reference_field), \\\"stored references remain forbidden\\\")\\n+    // Expected execution output: A A B / A A B / A B B (one item per line).\\n+    try! execute(&p, 1000)\\n+    print(\\\"owning record contract passed\\\")\\n+}\\ndiff --git a/prototypes/minivm/tests/string_contract.saw b/prototypes/minivm/tests/string_contract.saw\\nindex 9f7b5d5f..4978db81 100644\\n--- a/prototypes/minivm/tests/string_contract.saw\\n+++ b/prototypes/minivm/tests/string_contract.saw\\n@@ -102,6 +102,6 @@ func main() {\\n     let types: Vector<ValueType> = [ValueType.String]\\n     try! field.records.push(RecordIR(name: \\\"Owning\\\", field_names: move names,\\n         field_types: move types, line: 1, col: 1))\\n-    assert(not valid(&field), \\\"String record ownership deferred\\\")\\n+    assert(valid(&field), \\\"String record leaves supported after M9\\\")\\n     print(\\\"string contract passed\\\")\\n }\\n\",\"id\":\"SL-252\",\"request_id\":\"mv2-SL-252-6acc9315-a1\",\"title\":\"minivm M9: owning records + String-pattern match (branch commit 6acc9315)\"}","request_id":"mv2-SL-252-6acc9315-a1","sequence":492,"timestamp":"1789098592"}
---
