---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-253.p1 (revision 1): minivm M10: Optional values + conditional binding (+ bundled M10/M11/M12 design docs; branch commits 339395ff+2895f334)","created":"1789099170","id":"c1"}],"created":"1789086372","description":"SL-242 epic child, TWO landing patches: p1 = branch commit 339395ff (the M10/M11/M12 milestone DESIGNS — M12's doc lands here, its impl is a separate backlog issue), p2 = branch commit 2895f334 (M10 implementation: T?/T??, contextual None, implicit wrapping incl. String-owning fields, statement + value if let/if var, incl. the real Token/tok extraction). Contract: prototypes/minivm/M10_OPTIONALS.md.","id":"SL-253","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":499,"stage":"queued","status":"open","title":"minivm M10: Optional values + conditional binding (if let / if var)","updated":"1789099170"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086372","description":"SL-242 epic child, TWO landing patches: p1 = branch commit 339395ff (the M10/M11/M12 milestone DESIGNS — M12's doc lands here, its impl is a separate backlog issue), p2 = branch commit 2895f334 (M10 implementation: T?/T??, contextual None, implicit wrapping incl. String-owning fields, statement + value if let/if var, incl. the real Token/tok extraction). Contract: prototypes/minivm/M10_OPTIONALS.md.","id":"SL-253","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":415,"stage":"queued","status":"open","title":"minivm M10: Optional values + conditional binding (if let / if var)","updated":"1789086372"},"id":"SL-253","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 1829b432..6093b128 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -19,7 +19,9 @@ outstanding. M8 adds owned String literals/locals/parameters/results/references,\\n byte length/access, equality and substring with explicit copy/drop rules (277\\n total cases). M9 adds String-valued records and String-pattern match (288 cases),\\n including unchanged keyword/suffix classification and the actual String-backed\\n-Lexer.advance. Next are Optional/Result payloads and collections. The lexer has\\n+Lexer.advance. M10 adds Optional payloads, contextual None/wrapping and value\\n+conditional binding, including the unchanged Token/tok extraction (307 cases).\\n+Next are Result control flow, checked unsigned parsing and collections. The lexer has\\n no imports, so its first whole-file test can combine the unchanged library with\\n a small wrapper without implementing general module loading.\\n The dependency groups below are an inventory, not the implementation commit order;\\ndiff --git a/prototypes/minivm/M10_OPTIONALS.md b/prototypes/minivm/M10_OPTIONALS.md\\nnew file mode 100644\\nindex 00000000..17e003d2\\n--- /dev/null\\n+++ b/prototypes/minivm/M10_OPTIONALS.md\\n@@ -0,0 +1,101 @@\\n+# M10: Optional values and conditional binding\\n+\\n+## Objective and boundary\\n+\\n+Admit the lexer's `Token.suffix: String?`, contextual `None` and String wrapping,\\n+and `if let` bindings as an independently tested milestone. Preserve the M9\\n+copy/ownership contract. Result syntax, propagation, collections, String.to_uint,\\n+optional chaining, coalescing, force unwrap and general generic declarations\\n+remain later slices. Do not change the production compiler or lexer.\\n+\\n+## Representation\\n+\\n+Use a dedicated `Program.sums: Vector<SumIR>` table, not synthetic nominal\\n+records. `SumKind` has Optional and Result cases; `SumIR` contains kind, payload,\\n+and error ValueTypes. Optional uses error Void. ValueType gains Optional(index)\\n+and Result(index). The Result representation is established now to avoid another\\n+cross-cutting layout migration, but no Result source operations land in M10.\\n+\\n+Structurally intern descriptors. Optional<T> layout is Bool tag followed by the\\n+full T layout. Result<T,E> layout is Bool tag, full T layout, full E layout;\\n+Void success contributes no words. Tag true means Some/Ok. Disjoint payload\\n+regions retain a fixed String-versus-integer type for every physical word.\\n+Optional<Void>, Ref payloads and Void Result errors are invalid. Record/sum\\n+cycles, invalid indices, descriptor-kind mismatches and aggregate depth/size\\n+overflow are rejected. Existing 128-depth/256-word limits apply jointly across\\n+records and sums. Reference targets may be data sums, but references still may\\n+not be stored in aggregates or returned.\\n+\\n+Migrate value_layout and all dependent type/range/field checks to receive sum\\n+metadata. All Program constructors provide an empty sum table unless used.\\n+Do not let nominal record constructors, fields or extension lookup see sums.\\n+\\n+## Construction and ownership\\n+\\n+Every construction initializes the complete flattened range. Inactive numeric\\n+and Bool leaves are canonical zero; inactive String leaves are null, established\\n+using Drop on the zero-initialized owning slot. No fake integer String handles.\\n+Some sets the tag and copies a complete payload snapshot. None has false tag\\n+and cleared payload. Reusing slots in loops or replacing Some with None must\\n+release previous String claims. Whole-value copy, references, calls and returns\\n+use the existing complete-layout retain/release and ABI rules.\\n+\\n+No new projection opcode is required for conditional binding: the frontend\\n+branches on the canonical tag slot, then copies the known payload range only\\n+on the Some edge. The verifier retains its existing structural scope; this is\\n+not an untrusted bytecode loader. A later forced unwrap must check the tag.\\n+\\n+## Source and contextual typing\\n+\\n+Accept postfix T? in every data-type position, including record fields,\\n+parameters/results and references. Nested optionals are supported; DoubleQuestion\\n+in a declaration type means two suffixes. Bound recursive type parsing. Generic\\n+Optional<T> spelling is optional convenience, not required by this milestone.\\n+Unsupported coalescing/chaining/force unwrap must fail with a located diagnostic.\\n+\\n+None requires an expected Optional type. A bare T implicitly wraps into an\\n+expected Optional<T>, recursively through nested Optional layers; an existing\\n+Optional value of exactly the expected type is copied without wrapping again.\\n+Apply this consistently to declarations, assignments, record constructors,\\n+call arguments, explicit returns, implicit tails and each arm of contextual\\n+if/match. Preserve numeric contextual literal rules through the payload type.\\n+An uncontextualized None is rejected rather than inventing a payload type.\\n+Do not infer types from None or silently choose an arbitrary sibling-arm type.\\n+Preserve whole result ranges and all owning leaves during branch joins/returns.\\n+\\n+## Conditional binding\\n+\\n+Support statement and value `if let name = optional { ... } else { ... }`,\\n+including else-if chains; permit `if var` with a mutable copied payload and `_`\\n+for ignored payload. Evaluate the scrutinee once and snapshot it before binding.\\n+Only Optional is accepted. The binding exists only in the then scope, and the\\n+scrutinee is evaluated before introducing it (`if let x = x` works). Source\\n+mutation inside the arm cannot invalidate the bound snapshot. Missing else is\\n+valid for a statement, but continuing value paths need a value as in M5.\\n+Early returns and loop iterations clean both scrutinee and payload claims.\\n+\\n+## Validation and responsibilities\\n+\\n+Engines agent owns model.saw, verify.saw, vm.saw, llvm.saw and migration of\\n+existing direct contracts, plus a new optional_contract.saw. Frontend agent owns\\n+frontend.saw and migrates its Program/layout calls. Tests agent owns isolated\\n+examples/optionals and harness entries. Primary reviews, runs gates, documents\\n+and commits. Coordinate exact public model helpers before implementation.\\n+\\n+Contract tests must cover nested/mixed layouts, invalid/cyclic descriptors,\\n+reference storage rejection and Optional record fields. Source tests must cover\\n+Some/None replacement, String/nested record payloads, nested optionals, exact\\n+context propagation, numeric limits, calls/returns/references/methods, statement\\n+and value binding, derived shadowing, subject-once/snapshot semantics, early\\n+return and bounded-loop cleanup, and located rejection paths. Include a small\\n+unchanged lexer Token declaration and tok method extraction where practicable.\\n+Use explicit expected VM/native outputs at O0/O2; use ASan for an ownership probe.\\n+Full previous regression gate and direct contracts must remain green.\\n+\\n+## Review finding\\n+\\n+SL-239 records a separately reproduced Python sawc rejection: an annotated\\n+String? value-if/if-let is rejected when one arm is already String? and another\\n+needs a String wrap. The prototype applies the destination context per arm.\\n+`examples/optionals/review_nested_context.saw` also checks this through nested\\n+conditional binding, alongside ownership snapshots and Some(None).\\ndiff --git a/prototypes/minivm/M11_RESULTS.md b/prototypes/minivm/M11_RESULTS.md\\nnew file mode 100644\\nindex 00000000..f5fa6fcf\\n--- /dev/null\\n+++ b/prototypes/minivm/M11_RESULTS.md\\n@@ -0,0 +1,83 @@\\n+# M11: Result construction, matching and propagation\\n+\\n+Planned after the M10 Optional gate. This design uses M10's SumIR metadata and\\n+disjoint tagged payload layout. It does not authorize changing production code.\\n+\\n+## Source boundary\\n+\\n+Admit Result<T,E> type applications, explicit qualified constructors\\n+`Result<T,E>.Ok(value: expr)` / `.Err(error: expr)`, unambiguous implicit payload\\n+wrapping, and bare success return for Result<Void,E>. Require a non-Void data\\n+error type and reject stored references. No Error trait machinery is required\\n+by the actual lexer. No catch, routing clauses, try?, generic declarations,\\n+custom payload enums or error formatting are part of this slice.\\n+\\n+Implicit wrapping must select exactly one compatible variant. A literal fitting\\n+both numeric payload types is ambiguous even when those types differ. Existing\\n+exact Result values copy unchanged. Preserve contextual numeric/Optional typing\\n+inside selected payloads and each control-flow arm. None can target an Optional\\n+payload only when exactly one Result variant accepts it. Never pick a side by\\n+declaration order. Explicit constructors resolve ambiguity and require the\\n+correct label and arity. Explicit Void Ok construction is omitted: production\\n+rejects both `.Ok()` and `.Ok(value: ())`. Use a bare success return instead.\\n+Pin existing Optional payload wrapping separately: Int? into Result<Int??,E>\\n+owes one layer, while None into Result<Int??,E> denotes an absent Ok payload.\\n+\\n+## Result observation\\n+\\n+Support statement and value matches with `case Ok(name)` and `case Err(name)`,\\n+including `_` payload patterns; Void Ok uses `case Ok`. A final whole-value `_`\\n+may cover the remaining variant. Reject duplicate/missing arms and wrong arity.\\n+Bindings are local payload snapshots; evaluate the scrutinee exactly once.\\n+Retain complete String/record/Optional payloads before source mutation or scope\\n+cleanup. This small match surface also lets the eventual lexer test wrapper\\n+observe LexError line/column without requiring catch support.\\n+\\n+## try and try!\\n+\\n+Parse prefix try and try! at production precedence (their operand is a complete\\n+expression), with normal delimiters ending it. Nested `try! f(try g())` must\\n+propagate the inner error before the outer call runs. Operand must be Result.\\n+\\n+On Ok, return a complete payload snapshot (or a Void expression for Void Ok).\\n+On Err, try is legal only in a Result-returning function with the same error\\n+type. Construct its complete Err value, preserve all owning result leaves,\\n+clean the function frame via the shared return helper, and return immediately.\\n+Do not evaluate later arguments, RHS operations or following statements on\\n+the error edge. Different success types between callee and caller are valid.\\n+\\n+try! on Err emits a terminal `Panic` instruction with a fixed message\\n+`runtime error: try! failed` and exit status 1 in VM/native. Payload formatting\\n+is explicitly deferred. Verifier validates the instruction's fixed contract;\\n+VM reports its ordinary runtime failure and native calls the existing fatal\\n+runtime path. It must never read or expose an inactive payload. Panic requires\\n+default unused instruction fields (dst/a/b/target -1, immediate 0, no args),\\n+has no CFG successor, and emits native `unreachable` after the fatal call.\\n+The fixed message is a compatibility limitation: production can format an\\n+error cause, while this prototype slice deliberately cannot.\\n+\\n+## Ownership and validation\\n+\\n+Initialize every inactive payload leaf on every construction, including loop\\n+reuse. Return/Call/Copy/reference operations continue to use the complete\\n+flattened layout. Dropping inactive null String claims is safe. Keep the\\n+successful-execution zero-live-allocation check; error execution may unwind\\n+the VM-owned execution heap or terminate the native process as before.\\n+\\n+Tests: distinct and same-type variants; literal ambiguity; nested Result and\\n+Optional payloads; mixed String-owning record payloads; Result<Void,E>; exact\\n+payload observation through match; mutation after snapshot; early propagation\\n+through several frames and argument evaluation; try! Ok/Err; loops and branch\\n+cleanup; explicit and implicit return contexts; malformed type/constructor/\\n+match/try rejects. Add direct representation/IR checks and ASan ownership probes.\\n+Full regression gate remains required before a milestone commit.\\n+\\n+## Following slice\\n+\\n+String.to_uint is a separate small intrinsic returning UInt?, allowing the\\n+unchanged literal_fits helper to run. Accept default radix 10 and explicit\\n+radix 2..36. Parse the whole byte string, optional leading +, no whitespace,\\n+prefix or underscore handling; negatives, lone sign, invalid digit, embedded\\n+NUL, invalid radix and overflow yield None. Guard multiplication/addition by\\n+the UInt64 ceiling without signed overflow or undefined LLVM arithmetic.\\n+Test UInt64.max and its neighbors across bases, then actual literal_fits.\\ndiff --git a/prototypes/minivm/M12_UNSIGNED_PARSE.md b/prototypes/minivm/M12_UNSIGNED_PARSE.md\\nnew file mode 100644\\nindex 00000000..48b58f6d\\n--- /dev/null\\n+++ b/prototypes/minivm/M12_UNSIGNED_PARSE.md\\n@@ -0,0 +1,46 @@\\n+# M12: checked String.to_uint intrinsic\\n+\\n+Planned after Optional/Result integration. This is the next isolated dependency\\n+for the unchanged lexer literal_fits helper; it does not compile std/string.saw.\\n+\\n+## Source contract\\n+\\n+Accept `string.to_uint()` and `string.to_uint(radix)` (ordered `radix:` label\\n+also allowed) on the same receivers as the existing String intrinsics. Return\\n+UInt?. Default radix is 10. Explicit radix is Int and valid from 2 through 36.\\n+Snapshot a named receiver before evaluating the radix argument, preserving\\n+existing String intrinsic argument-order semantics.\\n+\\n+Parse the whole String byte sequence, with an optional leading ASCII plus.\\n+Accept ASCII digits and letters A-Z/a-z for values 0..35. Reject empty/lone-plus,\\n+minus, whitespace, separators, prefixes (unless their bytes are valid digits\\n+in the requested radix), trailing junk, non-ASCII bytes and embedded NUL.\\n+Invalid radix and overflow return None without a panic. Arbitrarily many leading\\n+zeros remain valid. UInt64.max is valid, including above signed Int.max.\\n+\\n+## IR and engine contract\\n+\\n+Introduce StringToUInt with a=String slot, b=I64 radix slot, and dst beginning a\\n+two-word [Bool,U64] range. Verifier checks this exact range and operand types.\\n+Every path writes both tag and value; invalid parses write false and zero.\\n+The frontend interns Optional<UInt> and assigns the corresponding logical type.\\n+\\n+VM may use a small explicit byte parser returning UInt?; use UInt arithmetic\\n+only after checking `acc <= (UInt.max - digit) / radix`. Check radix before the\\n+division. Convert the valid UInt bit pattern into the VM's Int slot with a\\n+truncating bit-preserving conversion. Do not use a signed numeric parser.\\n+\\n+Native helper accepts the existing String pointer and i64 radix and returns\\n+{i64,i64} tag/value. Use unsigned comparison and unsigned division for the same\\n+precondition, then ordinary wrapping-defined LLVM multiply/add without nsw/nuw.\\n+Validate the digit and radix before arithmetic. Reading the String uses its\\n+stored byte length, never strlen. No allocation or libc parsing is needed.\\n+\\n+## Validation\\n+\\n+Table-driven source tests with explicit expected UInt/None results: bases\\n+2/8/10/16/36, both letter cases, plus, invalid signs/radices/bytes, whitespace,\\n+embedded NUL, empty, max-1/max/max+1 in several bases, leading zeros and receiver\\n+snapshot mutation. Include the unchanged literal_fits function with boundaries\\n+for 8/16/32/64-bit widths and all lexer radices. Direct IR checks reject invalid\\n+destination/argument ranges. Full VM/native regression gate before commit.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex fa6b0bbe..7962e3e5 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -99,6 +99,12 @@ aggregate calls/returns. String matches support literal patterns and a required\\n final wildcard, comparing decoded bytes and snapshotting the subject before\\n arm execution. See [M9_OWNING_RECORDS_MATCH.md](M9_OWNING_RECORDS_MATCH.md).\\n \\n+Optional values support postfix `T?` and nested `T??`, contextual `None` and\\n+implicit payload wrapping, including String-owning record fields. Statement\\n+and value `if let` / `if var` evaluate once and bind copied payloads in the then\\n+scope. Coalescing, chaining and force unwrap remain unsupported. See\\n+[M10_OPTIONALS.md](M10_OPTIONALS.md).\\n+\\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n only to VM execution; native code uses the host call stack and has no budget.\\n@@ -111,17 +117,19 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The first nine milestones\\n-cover 288 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The first ten milestones\\n+cover 307 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw`,\\n `tests/record_contract.saw`, `tests/enum_contract.saw`,\\n `tests/reference_contract.saw`, `tests/receiver_contract.saw`, and\\n-`tests/string_contract.saw` and `tests/owning_record_contract.saw`; build and run them\\n+`tests/string_contract.saw`, `tests/owning_record_contract.saw`, and\\n+`tests/optional_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n `--section enums`, `--section values`, `--section references`, or\\n-`--section receivers`, `--section strings` or `--section owning_records` for an isolated\\n+`--section receivers`, `--section strings`, `--section owning_records` or\\n+`--section optionals` for an isolated\\n integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\ndiff --git a/prototypes/minivm/examples/optionals/basic_numeric.saw b/prototypes/minivm/examples/optionals/basic_numeric.saw\\nnew file mode 100644\\nindex 00000000..decafc8a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/basic_numeric.saw\\n@@ -0,0 +1,12 @@\\n+func main() {\\n+    var number: Int? = 7\\n+    if let value = number { print(value) }\\n+    number = None\\n+    if let value = number {\\n+        print(value)\\n+    } else {\\n+        print(\\\"none\\\")\\n+    }\\n+    let byte: UInt8? = 255\\n+    if let value = byte { print(value) }\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/binding_snapshot_shadow.saw b/prototypes/minivm/examples/optionals/binding_snapshot_shadow.saw\\nnew file mode 100644\\nindex 00000000..ed118c7b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/binding_snapshot_shadow.saw\\n@@ -0,0 +1,15 @@\\n+func main() {\\n+    var source: String? = \\\"old\\\"\\n+    if let source = source {\\n+        print(source)\\n+    }\\n+    if let snapshot = source {\\n+        source = \\\"new\\\"\\n+        if let current = source { print(current) }\\n+        print(\\\"changed\\\")\\n+        print(snapshot)\\n+    }\\n+    if var copy = source {\\n+        copy = \\\"local\\\"\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/contextual_calls_arms.saw b/prototypes/minivm/examples/optionals/contextual_calls_arms.saw\\nnew file mode 100644\\nindex 00000000..8fe043f5\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/contextual_calls_arms.saw\\n@@ -0,0 +1,31 @@\\n+enum Choice { case A, case B }\\n+\\n+func pass(value: UInt8?) -> UInt8? { value }\\n+func choose(flag: Bool) -> UInt8? {\\n+    if flag { 255 } else { None }\\n+}\\n+func matched(choice: Choice) -> String? {\\n+    match choice {\\n+        case A -> { \\\"a\\\" },\\n+        case B -> { None }\\n+    }\\n+}\\n+\\n+func main() {\\n+    if let value = pass(choose(true)) { print(value) }\\n+    if let value = choose(false) {\\n+        print(value)\\n+    } else {\\n+        print(\\\"none\\\")\\n+    }\\n+    if let value = matched(Choice.A) { print(value) }\\n+\\n+    let absent: String? = None\\n+    let present: String? = \\\"chain\\\"\\n+    if let _ = absent {\\n+        print(\\\"wrong\\\")\\n+    } else if let text = present {\\n+        print(text)\\n+    }\\n+    if let _ = present { print(\\\"ignored\\\") }\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/early_return_loop_cleanup.saw b/prototypes/minivm/examples/optionals/early_return_loop_cleanup.saw\\nnew file mode 100644\\nindex 00000000..e2c06048\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/early_return_loop_cleanup.saw\\n@@ -0,0 +1,21 @@\\n+func early(flag: Bool) -> String {\\n+    let value: String? = \\\"owned\\\"\\n+    if let text = value {\\n+        if flag { return \\\"early\\\" }\\n+        return text\\n+    }\\n+    return \\\"none\\\"\\n+}\\n+\\n+func main() {\\n+    print(early(true))\\n+    var index = 0\\n+    var last = \\\"start\\\"\\n+    while index < 200 {\\n+        let value: String? = if index % 2 == 0 { \\\"even\\\" } else { \\\"odd\\\" }\\n+        if let text = value { last = text }\\n+        index += 1\\n+    }\\n+    print(last)\\n+    print(index - 1)\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/nested_semantics.saw b/prototypes/minivm/examples/optionals/nested_semantics.saw\\nnew file mode 100644\\nindex 00000000..61fcce7a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/nested_semantics.saw\\n@@ -0,0 +1,41 @@\\n+func main() {\\n+    let absent: Int?? = (None)\\n+    if let inner = absent {\\n+        print(\\\"wrong\\\")\\n+    } else {\\n+        print(\\\"outer-none\\\")\\n+    }\\n+\\n+    let inner_none: Int? = None\\n+    let wrapped: Int?? = inner_none\\n+    if let inner = wrapped {\\n+        if let value = inner {\\n+            print(value)\\n+        } else {\\n+            print(\\\"inner-none\\\")\\n+        }\\n+    }\\n+\\n+    let present: Int?? = 9\\n+    if let inner = present {\\n+        if let value = inner { print(value) }\\n+    }\\n+\\n+    let branch: Int?? = if false {\\n+        None\\n+    } else {\\n+        if true { inner_none } else { None }\\n+    }\\n+    if let inner = branch {\\n+        if let value = inner {\\n+            print(value)\\n+        } else {\\n+            print(\\\"branch-inner-none\\\")\\n+        }\\n+    }\\n+\\n+    let parenthesized: Int? = (7)\\n+    if let value = parenthesized { print(value) }\\n+    let maximum: UInt? = (18_446_744_073_709_551_615)\\n+    if let value = maximum { print(value) }\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/references_methods.saw b/prototypes/minivm/examples/optionals/references_methods.saw\\nnew file mode 100644\\nindex 00000000..81459282\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/references_methods.saw\\n@@ -0,0 +1,24 @@\\n+struct Holder { value: String? }\\n+\\n+func replace(value: &var String?, next: String?) { value = next }\\n+func show(value: &String?) {\\n+    if let text = value { print(text) }\\n+}\\n+\\n+extension Holder {\\n+    func set(&var self, next: String?) { self.value = next }\\n+    func show(&self) { show(&self.value) }\\n+}\\n+\\n+func main() {\\n+    var holder = Holder(value: \\\"start\\\")\\n+    holder.show()\\n+    replace(&var holder.value, \\\"next\\\")\\n+    holder.show()\\n+    holder.set(next: None)\\n+    if let text = holder.value {\\n+        print(text)\\n+    } else {\\n+        print(\\\"none\\\")\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/reject_binding_scope.saw b/prototypes/minivm/examples/optionals/reject_binding_scope.saw\\nnew file mode 100644\\nindex 00000000..7e26a370\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/reject_binding_scope.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    let value: Int? = 1\\n+    if let inner = value { print(inner) }\\n+    print(inner)\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/reject_chaining.saw b/prototypes/minivm/examples/optionals/reject_chaining.saw\\nnew file mode 100644\\nindex 00000000..d56b11d4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/reject_chaining.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let value: String? = \\\"x\\\"\\n+    print(value?.len())\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/reject_coalescing.saw b/prototypes/minivm/examples/optionals/reject_coalescing.saw\\nnew file mode 100644\\nindex 00000000..825e4d0d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/reject_coalescing.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let value: Int? = 1\\n+    print(value ?? 0)\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/reject_force_unwrap.saw b/prototypes/minivm/examples/optionals/reject_force_unwrap.saw\\nnew file mode 100644\\nindex 00000000..a59f74f8\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/reject_force_unwrap.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let value: Int? = 1\\n+    print(value!)\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/reject_if_let_non_optional.saw b/prototypes/minivm/examples/optionals/reject_if_let_non_optional.saw\\nnew file mode 100644\\nindex 00000000..a48a8245\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/reject_if_let_non_optional.saw\\n@@ -0,0 +1,3 @@\\n+func main() {\\n+    if let value = 1 { print(value) }\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/reject_numeric_range.saw b/prototypes/minivm/examples/optionals/reject_numeric_range.saw\\nnew file mode 100644\\nindex 00000000..d226c904\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/reject_numeric_range.saw\\n@@ -0,0 +1 @@\\n+func main() { let value: UInt8? = 256 }\\ndiff --git a/prototypes/minivm/examples/optionals/reject_optional_void.saw b/prototypes/minivm/examples/optionals/reject_optional_void.saw\\nnew file mode 100644\\nindex 00000000..93c273d8\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/reject_optional_void.saw\\n@@ -0,0 +1,2 @@\\n+func bad(value: Void?) {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/optionals/reject_uncontextualized_none.saw b/prototypes/minivm/examples/optionals/reject_uncontextualized_none.saw\\nnew file mode 100644\\nindex 00000000..c55d2f97\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/reject_uncontextualized_none.saw\\n@@ -0,0 +1 @@\\n+func main() { let value = None }\\ndiff --git a/prototypes/minivm/examples/optionals/reject_value_binding_missing_tail.saw b/prototypes/minivm/examples/optionals/reject_value_binding_missing_tail.saw\\nnew file mode 100644\\nindex 00000000..d889c418\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/reject_value_binding_missing_tail.saw\\n@@ -0,0 +1,9 @@\\n+func main() {\\n+    let value: Int? = 1\\n+    let result = if let inner = value {\\n+        print(inner)\\n+    } else {\\n+        0\\n+    }\\n+    print(result)\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/review_nested_context.saw b/prototypes/minivm/examples/optionals/review_nested_context.saw\\nnew file mode 100644\\nindex 00000000..6b7fee1a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/review_nested_context.saw\\n@@ -0,0 +1,23 @@\\n+struct OptionalHolder { value: String? }\\n+func choose(flag: Bool) -> String? {\\n+    if flag { (None) } else { (\\\"chosen\\\") }\\n+}\\n+func set(box: &var OptionalHolder, value: String?) { box.value = value }\\n+func echo(value: String??) -> String?? { value }\\n+func main() {\\n+    var box = OptionalHolder(value: (None))\\n+    set(&var box, choose(false))\\n+    if let held = box.value {\\n+        set(&var box, None)\\n+        print(held)\\n+    }\\n+    let inner: String? = None\\n+    let outer: String?? = echo(inner)\\n+    if let nested = outer {\\n+        if let text = nested { print(text) } else { print(\\\"some-none\\\") }\\n+    } else { print(\\\"wrong-outer-none\\\") }\\n+    let result: String? = if let item = choose(false) {\\n+        if item == \\\"chosen\\\" { None } else { item }\\n+    } else { \\\"fallback\\\" }\\n+    if let text = result { print(text) } else { print(\\\"nested-none\\\") }\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/string_record_payload.saw b/prototypes/minivm/examples/optionals/string_record_payload.saw\\nnew file mode 100644\\nindex 00000000..7c1adb81\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/string_record_payload.saw\\n@@ -0,0 +1,20 @@\\n+struct Text { value: String }\\n+struct Item { text: Text, number: Int }\\n+\\n+func copy(value: Item?) -> Item? { value }\\n+\\n+func main() {\\n+    var item: Item? = Item(text: Text(value: \\\"alpha\\\"), number: 1)\\n+    let snapshot = copy(item)\\n+    item = Item(text: Text(value: \\\"beta\\\"), number: 2)\\n+    if let value = snapshot {\\n+        print(value.text.value)\\n+        print(value.text.value)\\n+    }\\n+    item = None\\n+    if let value = item {\\n+        print(value.text.value)\\n+    } else {\\n+        print(\\\"none\\\")\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/token_tok_extract.saw b/prototypes/minivm/examples/optionals/token_tok_extract.saw\\nnew file mode 100644\\nindex 00000000..67d11fbd\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/token_tok_extract.saw\\n@@ -0,0 +1,36 @@\\n+enum TokenKind { case Ident }\\n+\\n+public struct Token {\\n+    public kind: TokenKind\\n+    public value: String\\n+    public line: Int\\n+    public col: Int\\n+    public suffix: String?\\n+}\\n+\\n+struct Lexer {\\n+    src: String\\n+    slen: Int\\n+    pos: Int\\n+    line: Int\\n+    col: Int\\n+}\\n+\\n+extension Lexer {\\n+    func tok(&self, kind: TokenKind, value: String) -> Token {\\n+        Token(kind: kind, value: value, line: self.line, col: self.col, suffix: None)\\n+    }\\n+}\\n+\\n+func main() {\\n+    let lexer = Lexer(src: \\\"word\\\", slen: 4, pos: 0, line: 4, col: 9)\\n+    let token = lexer.tok(TokenKind.Ident, \\\"word\\\")\\n+    print(token.value)\\n+    print(token.line)\\n+    print(token.col)\\n+    if let suffix = token.suffix {\\n+        print(suffix)\\n+    } else {\\n+        print(\\\"none\\\")\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/optionals/value_binding_subject_once.saw b/prototypes/minivm/examples/optionals/value_binding_subject_once.saw\\nnew file mode 100644\\nindex 00000000..cc5ab04b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/optionals/value_binding_subject_once.saw\\n@@ -0,0 +1,18 @@\\n+func subject(present: Bool) -> String? {\\n+    print(\\\"subject\\\")\\n+    if present { return \\\"yes\\\" }\\n+    return None\\n+}\\n+\\n+func choose(present: Bool) -> String {\\n+    if let value = subject(present) {\\n+        value\\n+    } else {\\n+        \\\"fallback\\\"\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(choose(true))\\n+    print(choose(false))\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 44e141ef..460dc64c 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -63,6 +63,7 @@ struct ExprValue {\\n     literal_value: UInt\\n     literal_negative: Bool\\n     always_returns: Bool\\n+    none_literal: Bool\\n }\\n \\n struct BlockValue {\\n@@ -73,7 +74,7 @@ struct BlockValue {\\n \\n func expr_value(ty: ValueType, slot: Int, call: Bool) -> ExprValue {\\n     ExprValue(ty: ty, slot: slot, call: call, literal: false,\\n-              literal_value: 0, literal_negative: false, always_returns: false)\\n+              literal_value: 0, literal_negative: false, always_returns: false, none_literal: false)\\n }\\n \\n func returning_value() -> ExprValue {\\n@@ -87,7 +88,8 @@ func type_name(t: ValueType) -> String {\\n         case I8 -> \\\"Int8\\\", case I16 -> \\\"Int16\\\", case I32 -> \\\"Int32\\\", case I64 -> \\\"Int\\\",\\n         case U8 -> \\\"UInt8\\\", case U16 -> \\\"UInt16\\\", case U32 -> \\\"UInt32\\\", case U64 -> \\\"UInt\\\",\\n         case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case String -> \\\"String\\\", case Void -> \\\"Void\\\",\\n-        case Record(_) -> \\\"record\\\", case Enum(_) -> \\\"enum\\\", case Ref(_) -> \\\"reference\\\"\\n+        case Record(_) -> \\\"record\\\", case Enum(_) -> \\\"enum\\\", case Ref(_) -> \\\"reference\\\",\\n+        case Optional(_) -> \\\"Optional\\\", case Result(_) -> \\\"Result\\\"\\n     }\\n }\\n \\n@@ -125,6 +127,7 @@ struct Compiler {\\n     records: Vector<RecordIR>\\n     enums: Vector<EnumIR>\\n     references: Vector<ReferenceIR>\\n+    sums: Vector<SumIR>\\n     strings: Vector<String>\\n     statics: Vector<StaticBinding>\\n     signatures: Vector<Signature>\\n@@ -196,18 +199,52 @@ extension Compiler {\\n             var supported = scalar_type(target)\\n             match target { case Record(_) -> { supported = true }, case _ -> {} }\\n             if target == ValueType.String { supported = true }\\n+            match target { case Optional(_) -> { supported = true }, case _ -> {} }\\n             if not supported { return problem(\\\"only data references are supported\\\", amp.line, amp.col) }\\n             return self.intern_reference(target, mutable)\\n         }\\n         let t = try self.expect(TokenKind.Ident, \\\"expected type\\\")\\n-        if t.value == \\\"Bool\\\" { return ValueType.Bool }\\n-        if t.value == \\\"String\\\" { return ValueType.String }\\n-        if let ty = named_integer_type(t.value) { return ty }\\n-        let ri = self.record_index(t.value)\\n-        if ri >= 0 { return ValueType.Record(ri) }\\n-        let ei = self.enum_index(t.value)\\n-        if ei >= 0 { return ValueType.Enum(ei) }\\n-        problem(\\\"unsupported type `{t.value}`\\\", t.line, t.col)\\n+        var ty = ValueType.Void\\n+        if t.value == \\\"Void\\\" { ty = ValueType.Void }\\n+        else if t.value == \\\"Bool\\\" { ty = ValueType.Bool }\\n+        else if t.value == \\\"String\\\" { ty = ValueType.String }\\n+        else if let integer = named_integer_type(t.value) { ty = integer }\\n+        else {\\n+            let ri = self.record_index(t.value)\\n+            if ri >= 0 { ty = ValueType.Record(ri) }\\n+            else {\\n+                let ei = self.enum_index(t.value)\\n+                if ei >= 0 { ty = ValueType.Enum(ei) }\\n+                else { return problem(\\\"unsupported type `{t.value}`\\\", t.line, t.col) }\\n+            }\\n+        }\\n+        var nesting = 0\\n+        while self.at(TokenKind.Question) || self.at(TokenKind.DoubleQuestion) {\\n+            let suffix = self.take()\\n+            let count = if suffix.kind == TokenKind.DoubleQuestion { 2 } else { 1 }\\n+            var n = 0\\n+            while n < count {\\n+                if nesting >= 128 { return problem(\\\"Optional nesting limit exceeded\\\", suffix.line, suffix.col) }\\n+                if ty == ValueType.Void { return problem(\\\"Optional<Void> is not supported\\\", suffix.line, suffix.col) }\\n+                if reference_type(ty) { return problem(\\\"Optional reference payloads are not supported\\\", suffix.line, suffix.col) }\\n+                ty = self.intern_optional(ty)\\n+                nesting += 1\\n+                n += 1\\n+            }\\n+        }\\n+        ty\\n+    }\\n+\\n+    func intern_optional(&var self, payload: ValueType) -> ValueType {\\n+        var i = 0\\n+        while i < self.sums.len() {\\n+            if self.sums[i].kind == SumKind.Optional && self.sums[i].payload == payload {\\n+                return ValueType.Optional(i)\\n+            }\\n+            i += 1\\n+        }\\n+        try! self.sums.push(SumIR(kind: SumKind.Optional, payload: payload, error: ValueType.Void))\\n+        ValueType.Optional(self.sums.len() - 1)\\n     }\\n \\n     func intern_reference(&var self, target: ValueType, mutable: Bool) -> ValueType {\\n@@ -269,6 +306,13 @@ extension Compiler {\\n                 if index >= 0 && index < self.enums.len() { return self.enums[index].name }\\n                 \\\"enum\\\"\\n             },\\n+            case Optional(index) -> {\\n+                if index >= 0 && index < self.sums.len() {\\n+                    return \\\"{self.display_type(self.sums[index].payload)}?\\\"\\n+                }\\n+                \\\"Optional\\\"\\n+            },\\n+            case Result(_) -> \\\"Result\\\",\\n             case _ -> type_name(ty)\\n         }\\n     }\\n@@ -396,6 +440,7 @@ extension Compiler {\\n                     }\\n                     let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after parameter name\\\")\\n                     let pt = try self.parse_type()\\n+                    if pt == ValueType.Void { return problem(\\\"parameter cannot have Void type\\\", pn.line, pn.col) }\\n                     try! pnames.push(pn.value)\\n                     try! ptypes.push(pt)\\n                     self.skip_newlines()\\n@@ -491,6 +536,7 @@ extension Compiler {\\n                     let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after parameter name\\\")\\n                     self.skip_newlines()\\n                     let pt = try self.parse_type()\\n+                    if pt == ValueType.Void { return problem(\\\"parameter cannot have Void type\\\", pn.line, pn.col) }\\n                     try! pnames.push(pn.value)\\n                     try! ptypes.push(pt)\\n                     self.skip_newlines()\\n@@ -558,7 +604,7 @@ extension Compiler {\\n         bits = integer_normalize(bits, source)\\n         if bare {\\n             let probe = ExprValue(ty: source, slot: -1, call: false, literal: true,\\n-                                  literal_value: magnitude, literal_negative: negative, always_returns: false)\\n+                                  literal_value: magnitude, literal_negative: negative, always_returns: false, none_literal: false)\\n             if not self.literal_fits_target(&probe, target) {\\n                 return problem(\\\"integer literal does not fit {type_name(target)}\\\", t.line, t.col)\\n             }\\n@@ -707,7 +753,7 @@ extension Compiler {\\n     }\\n \\n     func new_value(&var self, ty: ValueType) -> Result<Int, ProtoError> {\\n-        let layout = try value_layout(&self.records, ty)\\n+        let layout = try value_layout(&self.records, &self.sums, ty)\\n         let start = self.slots.len()\\n         var i = 0\\n         while i < layout.len() {\\n@@ -720,7 +766,7 @@ extension Compiler {\\n     func emit_copy_value(&var self, dst: Int, src: Int, ty: ValueType,\\n                          t: &Token) -> Result<Void, ProtoError> {\\n         if dst == src { return }\\n-        let layout = try value_layout(&self.records, ty)\\n+        let layout = try value_layout(&self.records, &self.sums, ty)\\n         if layout.len() > 1 {\\n             let temporary = try self.new_value(ty)\\n             var word = 0\\n@@ -941,16 +987,56 @@ extension Compiler {\\n             }\\n         }\\n         ExprValue(ty: target, slot: v.slot, call: false, literal: true,\\n-                  literal_value: v.literal_value, literal_negative: v.literal_negative, always_returns: false)\\n+                  literal_value: v.literal_value, literal_negative: v.literal_negative, always_returns: false, none_literal: false)\\n+    }\\n+\\n+    func initialize_optional_none(&var self, target: ValueType, t: &Token) -> Result<ExprValue, ProtoError> {\\n+        let dst = try self.new_value(target)\\n+        let layout = try value_layout(&self.records, &self.sums, target)\\n+        var i = 0\\n+        while i < layout.len() {\\n+            if layout[i] == ValueType.String {\\n+                let drop = self.emit_at(Op.Drop, t.line, t.col)\\n+                self.code[drop].a = dst + i\\n+            } else {\\n+                let zero = self.emit_at(Op.Const, t.line, t.col)\\n+                self.code[zero].dst = dst + i\\n+                self.code[zero].immediate = 0\\n+            }\\n+            i += 1\\n+        }\\n+        expr_value(target, dst, false)\\n+    }\\n+\\n+    func wrap_optional(&var self, v: ExprValue, target: ValueType,\\n+                       t: &Token) -> Result<ExprValue, ProtoError> {\\n+        let payload = try optional_payload(&self.sums, target)\\n+        let converted = try self.transfer(v, payload, &t)\\n+        let result = try self.initialize_optional_none(target, &t)\\n+        let some = self.emit_at(Op.Const, t.line, t.col)\\n+        self.code[some].dst = result.slot\\n+        self.code[some].immediate = 1\\n+        try self.emit_copy_value(result.slot + 1, converted.slot, payload, &t)\\n+        result\\n     }\\n \\n     func transfer(&var self, v: ExprValue, target: ValueType, t: &Token) -> Result<ExprValue, ProtoError> {\\n+        if v.none_literal {\\n+            match target {\\n+                case Optional(_) -> { return self.initialize_optional_none(target, &t) },\\n+                case _ -> { return problem(\\\"None requires an expected Optional type\\\", t.line, t.col) }\\n+            }\\n+        }\\n         if v.ty == target {\\n             if v.literal && not self.literal_fits_target(&v, target) {\\n                 return problem(\\\"integer literal does not fit {type_name(target)}\\\", t.line, t.col)\\n             }\\n             return v\\n         }\\n+        match target {\\n+            case Optional(_) -> { return self.wrap_optional(v, target, &t) },\\n+            case _ -> {}\\n+        }\\n         if v.literal && integer_type(target) { return self.adopt_literal(v, target, &t) }\\n         if integer_widens(v.ty, target) {\\n             let dst = self.new_slot(target)\\n@@ -1025,6 +1111,12 @@ extension Compiler {\\n         if self.depth > 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n         if self.at(TokenKind.If) { return self.parse_if_value(ValueType.Void, false) }\\n         if self.at(TokenKind.Match) { return self.parse_match_value(ValueType.Void, false) }\\n+        if self.at(TokenKind.NoneKw) {\\n+            let _ = self.take()\\n+            var value = expr_value(ValueType.Void, -1, false)\\n+            value.none_literal = true\\n+            return value\\n+        }\\n         if self.at(TokenKind.InterpString) {\\n             let t = self.take()\\n             return problem(\\\"string interpolation is not supported\\\", t.line, t.col)\\n@@ -1056,7 +1148,7 @@ extension Compiler {\\n             self.code[at].dst = dst\\n             self.code[at].immediate = integer_normalize(Int.from(truncating: magnitude), ty)\\n             return ExprValue(ty: ty, slot: dst, call: false, literal: bare,\\n-                             literal_value: magnitude, literal_negative: false, always_returns: false)\\n+                             literal_value: magnitude, literal_negative: false, always_returns: false, none_literal: false)\\n         }\\n         if self.at(TokenKind.True_) || self.at(TokenKind.False_) {\\n             let t = self.take()\\n@@ -1212,7 +1304,7 @@ extension Compiler {\\n                         place.slot = dst\\n                         place.ref_ty = projected_ref\\n                     } else {\\n-                        place.slot += try record_field_offset(&self.records, ri, fi)\\n+                        place.slot += try record_field_offset(&self.records, &self.sums, ri, fi)\\n                     }\\n                     place.ty = field_type\\n                 },\\n@@ -1241,7 +1333,7 @@ extension Compiler {\\n                 case Record(ri) -> {\\n                     let fi = self.field_index(ri, field.value)\\n                     if fi < 0 { return problem(\\\"record `{self.records[ri].name}` has no field `{field.value}`\\\", field.line, field.col) }\\n-                    let offset = try record_field_offset(&self.records, ri, fi)\\n+                    let offset = try record_field_offset(&self.records, &self.sums, ri, fi)\\n                     value = expr_value(self.records[ri].field_types[fi], value.slot + offset, false)\\n                 },\\n                 case _ -> { return problem(\\\"field access requires a record value\\\", dot.line, dot.col) }\\n@@ -1392,7 +1484,7 @@ extension Compiler {\\n                 j += 1\\n             }\\n             if found < 0 { return problem(\\\"missing field `{self.records[ri].field_names[fi]}` in constructor\\\", name.line, name.col) }\\n-            let offset = try record_field_offset(&self.records, ri, fi)\\n+            let offset = try record_field_offset(&self.records, &self.sums, ri, fi)\\n             try self.emit_copy_value(dst + offset, values[found].slot,\\n                                      self.records[ri].field_types[fi], &name)\\n             fi += 1\\n@@ -1522,7 +1614,7 @@ extension Compiler {\\n                     var value = try self.parse_expected_expr(formal)\\n                     if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n                     value = try self.snapshot(value, &name)\\n-                    let layout = try value_layout(&self.records, value.ty)\\n+                    let layout = try value_layout(&self.records, &self.sums, value.ty)\\n                     var word = 0\\n                     while word < layout.len() {\\n                         try! args.push(value.slot + word)\\n@@ -1632,7 +1724,7 @@ extension Compiler {\\n                     var value = try self.parse_expected_expr(formal)\\n                     if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n                     value = try self.snapshot(value, &name)\\n-                    let layout = try value_layout(&self.records, value.ty)\\n+                    let layout = try value_layout(&self.records, &self.sums, value.ty)\\n                     var word = 0\\n                     while word < layout.len() {\\n                         try! args.push(value.slot + word)\\n@@ -1701,7 +1793,7 @@ extension Compiler {\\n                 self.code[at].dst = dst\\n                 self.code[at].immediate = bits\\n                 let value = ExprValue(ty: ty, slot: dst, call: false, literal: bare,\\n-                                      literal_value: magnitude, literal_negative: true, always_returns: false)\\n+                                      literal_value: magnitude, literal_negative: true, always_returns: false, none_literal: false)\\n                 return value\\n             }\\n             let v = try self.parse_unary()\\n@@ -1992,6 +2084,9 @@ extension Compiler {\\n             self.code[branch].target = self.code.len()\\n             left = expr_value(ValueType.Bool, dst, false)\\n         }\\n+        if self.at(TokenKind.DoubleQuestion) { return self.fail(\\\"optional coalescing `??` is not supported\\\") }\\n+        if self.at(TokenKind.QuestionDot) { return self.fail(\\\"optional chaining `?.` is not supported\\\") }\\n+        if self.at(TokenKind.Exclaim) { return self.fail(\\\"optional force unwrap `!` is not supported\\\") }\\n         left\\n     }\\n \\n@@ -2161,7 +2256,7 @@ extension Compiler {\\n                 var preserve_end = -1\\n                 if not returns && not value.always_returns {\\n                     preserve_start = value.slot\\n-                    preserve_end = value.slot + (try value_layout(&self.records, value.ty)).len()\\n+                    preserve_end = value.slot + (try value_layout(&self.records, &self.sums, value.ty)).len()\\n                 }\\n                 self.cleanup_scope(preserve_start, preserve_end, &close)\\n                 let _ = self.take()\\n@@ -2192,6 +2287,7 @@ extension Compiler {\\n     func merge_control_type(&self, a: &ExprValue, b: &ExprValue,\\n                             expected: ValueType, has_expected: Bool, t: &Token) -> Result<ValueType, ProtoError> {\\n         if has_expected { return expected }\\n+        if a.none_literal || b.none_literal { return problem(\\\"None requires an expected Optional type\\\", t.line, t.col) }\\n         if a.ty == b.ty { return a.ty }\\n         if a.literal && integer_type(b.ty) { return b.ty }\\n         if b.literal && integer_type(a.ty) { return a.ty }\\n@@ -2210,6 +2306,20 @@ extension Compiler {\\n         return\\n     }\\n \\n+    func stage_optional_binding(&var self, name: Token, mutable: Bool,\\n+                                subject: &ExprValue, payload: ValueType) -> Result<Void, ProtoError> {\\n+        if name.value == \\\"_\\\" { return }\\n+        // The following block owns this payload snapshot. Stage its lexical depth\\n+        // before the block parser enters so String leaves are dropped at that edge.\\n+        self.scope_depth += 1\\n+        let dst = try self.new_value(payload)\\n+        try self.emit_copy_value(dst, subject.slot + 1, payload, &name)\\n+        self.scope_depth -= 1\\n+        try! self.bindings.push(Binding(name: name.value, ty: payload, slot: dst,\\n+                                        mutable: mutable, depth: self.depth + 1))\\n+        return\\n+    }\\n+\\n     func parse_if_value(&var self, expected: ValueType, has_expected: Bool) -> Result<ExprValue, ProtoError> {\\n         if self.expr_depth >= 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n         self.expr_depth += 1\\n@@ -2220,6 +2330,9 @@ extension Compiler {\\n \\n     func parse_if_value_inner(&var self, expected: ValueType, has_expected: Bool) -> Result<ExprValue, ProtoError> {\\n         let it = self.take()\\n+        if self.at(TokenKind.Let) || self.at(TokenKind.Var) {\\n+            return self.parse_if_binding_value(it, expected, has_expected)\\n+        }\\n         let condition = try self.parse_expr()\\n         try self.require_type(condition.ty, ValueType.Bool, &it)\\n         let _ = try self.expect(TokenKind.LBrace, \\\"expected block after if condition\\\")\\n@@ -2275,6 +2388,104 @@ extension Compiler {\\n         expr_value(target, dst, false)\\n     }\\n \\n+    func parse_if_binding_value(&var self, it: Token, expected: ValueType,\\n+                                has_expected: Bool) -> Result<ExprValue, ProtoError> {\\n+        let kw = self.take()\\n+        let name = try self.expect(TokenKind.Ident, \\\"expected binding name after `if let` or `if var`\\\")\\n+        let _ = try self.expect(TokenKind.Assign, \\\"if binding requires `=`\\\")\\n+        var subject = try self.parse_expr()\\n+        if subject.none_literal { return problem(\\\"None requires an expected Optional type\\\", it.line, it.col) }\\n+        match subject.ty {\\n+            case Optional(_) -> {},\\n+            case _ -> { return problem(\\\"if binding requires an Optional value\\\", it.line, it.col) }\\n+        }\\n+        subject = try self.snapshot(subject, &it)\\n+        let payload = try optional_payload(&self.sums, subject.ty)\\n+        let _ = try self.expect(TokenKind.LBrace, \\\"expected block after if binding\\\")\\n+        let branch = self.emit_at(Op.Branch, it.line, it.col)\\n+        self.code[branch].a = subject.slot\\n+        let skip_then = self.emit_at(Op.Jump, it.line, it.col)\\n+        self.code[branch].target = self.code.len()\\n+        try self.stage_optional_binding(name, kw.kind == TokenKind.Var, &subject, payload)\\n+        let then_value = try self.parse_value_block(expected, has_expected)\\n+        var then_exit = -1\\n+        if not then_value.always_returns { then_exit = self.emit_at(Op.Jump, it.line, it.col) }\\n+        self.code[skip_then].target = self.code.len()\\n+        self.skip_newlines()\\n+        if not self.at(TokenKind.Else) { return problem(\\\"value if let requires an else\\\", it.line, it.col) }\\n+        let _ = self.take()\\n+        var else_value = BlockValue(has_value: false, value: returning_value(), always_returns: false)\\n+        if self.at(TokenKind.If) {\\n+            let nested = try self.parse_if_value(expected, has_expected)\\n+            else_value = BlockValue(has_value: not nested.always_returns, value: nested,\\n+                                    always_returns: nested.always_returns)\\n+        } else {\\n+            let _ = try self.expect(TokenKind.LBrace, \\\"expected block after else\\\")\\n+            else_value = try self.parse_value_block(expected, has_expected)\\n+        }\\n+        var else_exit = -1\\n+        if not else_value.always_returns { else_exit = self.emit_at(Op.Jump, it.line, it.col) }\\n+        if not then_value.always_returns && not then_value.has_value { return problem(\\\"continuing value branch requires a tail expression\\\", it.line, it.col) }\\n+        if not else_value.always_returns && not else_value.has_value { return problem(\\\"continuing value branch requires a tail expression\\\", it.line, it.col) }\\n+        if then_value.always_returns && else_value.always_returns { return returning_value() }\\n+        var target = ValueType.Void\\n+        if then_value.always_returns { target = if has_expected { expected } else { else_value.value.ty } }\\n+        else if else_value.always_returns { target = if has_expected { expected } else { then_value.value.ty } }\\n+        else { target = try self.merge_control_type(&then_value.value, &else_value.value, expected, has_expected, &it) }\\n+        let dst = try self.new_value(target)\\n+        var joins = Vector<Int>()\\n+        if then_exit >= 0 { try self.append_value_exit(then_exit, then_value.value, target, dst, &it, &var joins) }\\n+        if else_exit >= 0 { try self.append_value_exit(else_exit, else_value.value, target, dst, &it, &var joins) }\\n+        let continuation = self.code.len()\\n+        var j = 0\\n+        while j < joins.len() {\\n+            self.code[joins[j]].target = continuation\\n+            j += 1\\n+        }\\n+        expr_value(target, dst, false)\\n+    }\\n+\\n+    func parse_if_binding_statement(&var self) -> Result<Bool, ProtoError> {\\n+        let it = self.take()\\n+        let kw = self.take()\\n+        let name = try self.expect(TokenKind.Ident, \\\"expected binding name after `if let` or `if var`\\\")\\n+        let _ = try self.expect(TokenKind.Assign, \\\"if binding requires `=`\\\")\\n+        var subject = try self.parse_expr()\\n+        if subject.none_literal { return problem(\\\"None requires an expected Optional type\\\", it.line, it.col) }\\n+        match subject.ty {\\n+            case Optional(_) -> {},\\n+            case _ -> { return problem(\\\"if binding requires an Optional value\\\", it.line, it.col) }\\n+        }\\n+        subject = try self.snapshot(subject, &it)\\n+        let payload = try optional_payload(&self.sums, subject.ty)\\n+        let _ = try self.expect(TokenKind.LBrace, \\\"expected block after if binding\\\")\\n+        let branch = self.emit_at(Op.Branch, it.line, it.col)\\n+        self.code[branch].a = subject.slot\\n+        let skip_then = self.emit_at(Op.Jump, it.line, it.col)\\n+        self.code[branch].target = self.code.len()\\n+        try self.stage_optional_binding(name, kw.kind == TokenKind.Var, &subject, payload)\\n+        let then_returns = try self.parse_block()\\n+        let skip_else = self.emit_at(Op.Jump, it.line, it.col)\\n+        self.code[skip_then].target = self.code.len()\\n+        var else_returns = false\\n+        let before_else = self.pos\\n+        self.skip_newlines()\\n+        if self.at(TokenKind.Else) {\\n+            let _ = self.take()\\n+            if self.at(TokenKind.If) {\\n+                if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n+                self.depth += 1\\n+                else_returns = try self.parse_statement()\\n+                self.depth -= 1\\n+            } else {\\n+                let _ = try self.expect(TokenKind.LBrace, \\\"expected block after else\\\")\\n+                else_returns = try self.parse_block()\\n+            }\\n+        } else { self.pos = before_else }\\n+        self.code[skip_else].target = self.code.len()\\n+        then_returns && else_returns\\n+    }\\n+\\n     func parse_match_value(&var self, expected: ValueType, has_expected: Bool) -> Result<ExprValue, ProtoError> {\\n         if self.expr_depth >= 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n         self.expr_depth += 1\\n@@ -2601,11 +2812,13 @@ extension Compiler {\\n                 annotated = Some(try self.parse_type())\\n             }\\n             if let local_type = annotated {\\n+                if local_type == ValueType.Void { return problem(\\\"local cannot have Void type\\\", name.line, name.col) }\\n                 if reference_type(local_type) { return problem(\\\"reference locals are not supported\\\", name.line, name.col) }\\n             }\\n             let _ = try self.expect(TokenKind.Assign, \\\"local declarations require an initializer\\\")\\n             var value = if let ty = annotated { try self.parse_expected_expr(ty) } else { try self.parse_expr() }\\n             if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n+            if value.none_literal { return problem(\\\"None requires an expected Optional type\\\", name.line, name.col) }\\n             if value.ty == ValueType.Void { return problem(\\\"cannot initialize a local with Void\\\", name.line, name.col) }\\n             if reference_type(value.ty) { return problem(\\\"reference locals are not supported\\\", name.line, name.col) }\\n             if let _ = annotated {} else { value = try self.transfer(value, value.ty, &name) }\\n@@ -2622,13 +2835,20 @@ extension Compiler {\\n                 if value.always_returns { return true }\\n                 src = value.slot\\n             } else if self.current_result != ValueType.Void { return problem(\\\"non-Void return requires a value\\\", t.line, t.col) }\\n-            let preserve_end = if src < 0 { -1 } else { src + (try value_layout(&self.records, self.current_result)).len() }\\n+            let preserve_end = if src < 0 { -1 } else { src + (try value_layout(&self.records, &self.sums, self.current_result)).len() }\\n             self.cleanup_function(src, preserve_end, &t)\\n             let at = self.emit_at(Op.Return, t.line, t.col)\\n             self.code[at].a = src\\n             return true\\n         }\\n         if self.at(TokenKind.If) {\\n+            var binding_if = false\\n+            if self.pos + 1 < self.tokens.len() {\\n+                binding_if = self.tokens[self.pos + 1].kind == TokenKind.Let || self.tokens[self.pos + 1].kind == TokenKind.Var\\n+            }\\n+            if binding_if {\\n+                return self.parse_if_binding_statement()\\n+            }\\n             let t = self.take()\\n             let cond = try self.parse_expr()\\n             try self.require_type(cond.ty, ValueType.Bool, &t)\\n@@ -2775,7 +2995,7 @@ extension Compiler {\\n                 if tail.always_returns { returns = true }\\n                 else {\\n                     let return_token = self.token()\\n-                    let preserve_end = tail.slot + (try value_layout(&self.records, self.current_result)).len()\\n+                    let preserve_end = tail.slot + (try value_layout(&self.records, &self.sums, self.current_result)).len()\\n                     self.cleanup_function(tail.slot, preserve_end, &return_token)\\n                     let r = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n                     self.code[r].a = tail.slot\\n@@ -2801,7 +3021,7 @@ extension Compiler {\\n             // Return-path checking above proves this padding unreachable. It gives\\n             // end-of-block jumps a real target and leaves every emitted block terminated.\\n             let slot = try self.new_value(self.current_result)\\n-            let layout = try value_layout(&self.records, self.current_result)\\n+            let layout = try value_layout(&self.records, &self.sums, self.current_result)\\n             var word = 0\\n             while word < layout.len() {\\n                 if layout[word] == ValueType.String {\\n@@ -2816,7 +3036,7 @@ extension Compiler {\\n                 word += 1\\n             }\\n             let return_token = self.token()\\n-            let preserve_end = slot + (try value_layout(&self.records, self.current_result)).len()\\n+            let preserve_end = slot + (try value_layout(&self.records, &self.sums, self.current_result)).len()\\n             self.cleanup_function(slot, preserve_end, &return_token)\\n             let r = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n             self.code[r].a = slot\\n@@ -2836,7 +3056,7 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         case Err(e) -> { return problem(e.message, e.line, e.col) }\\n     }\\n     var compiler = Compiler(source: source, tokens: move tokens, records: Vector<RecordIR>(), enums: Vector<EnumIR>(),\\n-                            references: Vector<ReferenceIR>(), strings: Vector<String>(), statics: Vector<StaticBinding>(),\\n+                            references: Vector<ReferenceIR>(), sums: Vector<SumIR>(), strings: Vector<String>(), statics: Vector<StaticBinding>(),\\n                             signatures: Vector<Signature>(), pos: 0, depth: 0, scope_depth: 0,\\n                             expr_depth: 0, paren_depth: 0,\\n                             bindings: Vector<Binding>(), active_borrows: Vector<Borrow>(), pending_writes: Vector<Int>(),\\n@@ -2845,7 +3065,8 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n                             code: Vector<Instruction>(), current_result: ValueType.Void)\\n     try compiler.collect_record_names()\\n     try compiler.scan_signatures()\\n-    try validate_records(&compiler.records)\\n+    try validate_records(&compiler.records, &compiler.sums)\\n+    try validate_sums(&compiler.records, &compiler.sums)\\n     try validate_enums(&compiler.enums)\\n     var functions = Vector<FuncIR>()\\n     var main_index = -1\\n@@ -2856,7 +3077,7 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         try! functions.push(move f)\\n         i += 1\\n     }\\n-    Program(enums: compiler.enums.copy(), records: compiler.records.copy(), references: compiler.references.copy(),\\n+    Program(enums: compiler.enums.copy(), records: compiler.records.copy(), references: compiler.references.copy(), sums: compiler.sums.copy(),\\n             strings: compiler.strings.copy(),\\n             functions: move functions, main_index: main_index)\\n }\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex 656a8a12..3cd071c7 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -36,7 +36,7 @@ func emit_aggregate_type(out: &var StringBuilder, layout: &Vector<ValueType>) {\\n     put(&var out, \\\" \\\\}\\\")\\n }\\n \\n-func emit_value_type(out: &var StringBuilder, records: &Vector<RecordIR>,\\n+func emit_value_type(out: &var StringBuilder, records: &Vector<RecordIR>, sums: &Vector<SumIR>,\\n                      t: ValueType) -> Result<Void, ProtoError> {\\n     if t == ValueType.Void {\\n         put(&var out, \\\"void\\\")\\n@@ -50,7 +50,7 @@ func emit_value_type(out: &var StringBuilder, records: &Vector<RecordIR>,\\n         put(&var out, \\\"ptr\\\")\\n         return\\n     }\\n-    let layout = try value_layout(&records, t)\\n+    let layout = try value_layout(&records, &sums, t)\\n     emit_aggregate_type(&var out, &layout)\\n     return\\n }\\n@@ -454,7 +454,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             put(&var out, \\\"  \\\")\\n             load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n             let target = try reference_target(&program.references, function.slots[inst.a])\\n-            let layout = try value_layout(&program.records, target)\\n+            let layout = try value_layout(&program.records, &program.sums, target)\\n             var word = 0\\n             while word < layout.len() {\\n                 if word > 0 {\\n@@ -496,7 +496,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             put(&var out, \\\"  \\\")\\n             load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n             let target = try reference_target(&program.references, function.slots[inst.a])\\n-            let layout = try value_layout(&program.records, target)\\n+            let layout = try value_layout(&program.records, &program.sums, target)\\n             var word = 0\\n             while word < layout.len() {\\n                 put(&var out, \\\"  \\\")\\n@@ -550,7 +550,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             let parent = try reference_target(&program.references, function.slots[inst.a])\\n             match parent {\\n                 case Record(index) -> {\\n-                    let offset = try record_field_offset(&program.records, index, inst.immediate)\\n+                    let offset = try record_field_offset(&program.records, &program.sums, index, inst.immediate)\\n                     put(&var out, \\\"  \\\")\\n                     temp_name(&var out, instruction_index, \\\"field\\\")\\n                     put(&var out, \\\" = getelementptr i64, ptr \\\")\\n@@ -694,7 +694,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n         },\\n         case Call -> {\\n             let callee = program.functions[inst.target].copy()\\n-            let result_layout = try value_layout(&program.records, callee.result)\\n+            let result_layout = try value_layout(&program.records, &program.sums, callee.result)\\n             var argument_index = 0\\n             while argument_index < inst.args.len() {\\n                 put(&var out, \\\"  \\\")\\n@@ -711,7 +711,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 put(&var out, \\\" = \\\")\\n             }\\n             put(&var out, \\\"call \\\")\\n-            try emit_value_type(&var out, &program.records, callee.result)\\n+            try emit_value_type(&var out, &program.records, &program.sums, callee.result)\\n             put(&var out, \\\" @f\\\")\\n             put_int(&var out, inst.target)\\n             put(&var out, \\\"(\\\")\\n@@ -847,7 +847,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 temp_name(&var out, instruction_index, \\\"ret\\\")\\n                 put(&var out, \\\")\\\\n\\\")\\n             } else if function.result != ValueType.Void && not scalar_type(function.result) {\\n-                let result_layout = try value_layout(&program.records, function.result)\\n+                let result_layout = try value_layout(&program.records, &program.sums, function.result)\\n                 var result_index = 0\\n                 while result_index < result_layout.len() {\\n                     put(&var out, \\\"  \\\")\\n@@ -886,7 +886,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 temp_name(&var out, instruction_index, \\\"ret\\\")\\n                 put(&var out, \\\"\\\\n\\\")\\n             } else {\\n-                let result_layout = try value_layout(&program.records, function.result)\\n+                let result_layout = try value_layout(&program.records, &program.sums, function.result)\\n                 var result_index = 0\\n                 while result_index < result_layout.len() {\\n                     put(&var out, \\\"  \\\")\\n@@ -918,7 +918,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n func emit_function(out: &var StringBuilder, program: &Program,\\n                    function_index: Int, function: &FuncIR) -> Result<Void, ProtoError> {\\n     put(&var out, \\\"define \\\")\\n-    try emit_value_type(&var out, &program.records, function.result)\\n+    try emit_value_type(&var out, &program.records, &program.sums, function.result)\\n     put(&var out, \\\" @f\\\")\\n     put_int(&var out, function_index)\\n     put(&var out, \\\"(\\\")\\n@@ -932,7 +932,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n             put_int(&var out, argument_index)\\n             argument_index = argument_index + 1\\n         } else {\\n-            let parameter_layout = try value_layout(&program.records, parameter_type)\\n+            let parameter_layout = try value_layout(&program.records, &program.sums, parameter_type)\\n             var parameter_word = 0\\n             while parameter_word < parameter_layout.len() {\\n                 if argument_index > 0 { put(&var out, \\\", \\\") }\\n@@ -975,7 +975,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     var parameter_words = 0\\n     var source_parameter = 0\\n     while source_parameter < function.params.len() {\\n-        let parameter_layout = try value_layout(&program.records, function.params[source_parameter])\\n+        let parameter_layout = try value_layout(&program.records, &program.sums, function.params[source_parameter])\\n         parameter_words = parameter_words + parameter_layout.len()\\n         source_parameter = source_parameter + 1\\n     }\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex 7db30436..01663278 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -1,7 +1,8 @@\\n public enum ValueType {\\n     case I8, case I16, case I32, case I64,\\n     case U8, case U16, case U32, case U64, case Byte,\\n-    case Bool, case Void, case String, case Record(index: Int), case Enum(index: Int), case Ref(index: Int)\\n+    case Bool, case Void, case String, case Record(index: Int), case Enum(index: Int), case Ref(index: Int),\\n+    case Optional(index: Int), case Result(index: Int)\\n }\\n @synthesize\\n extension ValueType: Equatable {}\\n@@ -116,11 +117,24 @@ public struct EnumIR {\\n @synthesize\\n extension EnumIR: ExplicitCopy {}\\n \\n+public enum SumKind { case Optional, case Result }\\n+@synthesize\\n+extension SumKind: Equatable {}\\n+\\n+public struct SumIR {\\n+    public kind: SumKind\\n+    public payload: ValueType\\n+    public error: ValueType\\n+}\\n+@synthesize\\n+extension SumIR: ExplicitCopy {}\\n+\\n public struct Program {\\n     public strings: Vector<String>\\n     public references: Vector<ReferenceIR>\\n     public enums: Vector<EnumIR>\\n     public records: Vector<RecordIR>\\n+    public sums: Vector<SumIR>\\n     public functions: Vector<FuncIR>\\n     public main_index: Int\\n }\\n@@ -187,6 +201,47 @@ public func reference_type(t: ValueType) -> Bool {\\n     }\\n }\\n \\n+public func optional_type(t: ValueType) -> Bool {\\n+    match t { case Optional(_) -> true, case _ -> false }\\n+}\\n+\\n+public func result_type(t: ValueType) -> Bool {\\n+    match t { case Result(_) -> true, case _ -> false }\\n+}\\n+\\n+public func optional_payload(sums: &Vector<SumIR>, t: ValueType) -> Result<ValueType, ProtoError> {\\n+    match t {\\n+        case Optional(index) -> {\\n+            if index < 0 || index >= sums.len() { return problem(\\\"invalid optional type index\\\", 0, 0) }\\n+            if sums[index].kind != SumKind.Optional { return problem(\\\"optional type has mismatched descriptor kind\\\", 0, 0) }\\n+            return sums[index].payload\\n+        },\\n+        case _ -> { return problem(\\\"expected optional type\\\", 0, 0) }\\n+    }\\n+}\\n+\\n+public func result_payload(sums: &Vector<SumIR>, t: ValueType) -> Result<ValueType, ProtoError> {\\n+    match t {\\n+        case Result(index) -> {\\n+            if index < 0 || index >= sums.len() { return problem(\\\"invalid result type index\\\", 0, 0) }\\n+            if sums[index].kind != SumKind.Result { return problem(\\\"result type has mismatched descriptor kind\\\", 0, 0) }\\n+            return sums[index].payload\\n+        },\\n+        case _ -> { return problem(\\\"expected result type\\\", 0, 0) }\\n+    }\\n+}\\n+\\n+public func result_error(sums: &Vector<SumIR>, t: ValueType) -> Result<ValueType, ProtoError> {\\n+    match t {\\n+        case Result(index) -> {\\n+            if index < 0 || index >= sums.len() { return problem(\\\"invalid result type index\\\", 0, 0) }\\n+            if sums[index].kind != SumKind.Result { return problem(\\\"result type has mismatched descriptor kind\\\", 0, 0) }\\n+            return sums[index].error\\n+        },\\n+        case _ -> { return problem(\\\"expected result type\\\", 0, 0) }\\n+    }\\n+}\\n+\\n public func reference_target(refs: &Vector<ReferenceIR>, t: ValueType) -> Result<ValueType, ProtoError> {\\n     match t {\\n         case Ref(index) -> {\\n@@ -219,6 +274,8 @@ public func validate_references(refs: &Vector<ReferenceIR>) -> Result<Void, Prot\\n         if not scalar_type(refs[n].target) && not string_type(refs[n].target) {\\n             match refs[n].target {\\n                 case Record(_) -> {},\\n+                case Optional(_) -> {},\\n+                case Result(_) -> {},\\n                 case _ -> { return problem(\\\"reference target must be a data type\\\", 0, 0) }\\n             }\\n         }\\n@@ -251,8 +308,9 @@ public func validate_enums(enums: &Vector<EnumIR>) -> Result<Void, ProtoError> {\\n     return\\n }\\n \\n-func append_layout(records: &Vector<RecordIR>, t: ValueType,\\n-                   path: &var Vector<Int>, words: &var Vector<ValueType>) -> Result<Void, ProtoError> {\\n+func append_layout(records: &Vector<RecordIR>, sums: &Vector<SumIR>, t: ValueType,\\n+                   record_path: &var Vector<Int>, sum_path: &var Vector<Int>, depth: Int,\\n+                   words: &var Vector<ValueType>) -> Result<Void, ProtoError> {\\n     if scalar_type(t) || string_type(t) {\\n         if words.len() >= 256 { return problem(\\\"record layout exceeds 256 words\\\", 0, 0) }\\n         try! words.push(t)\\n@@ -266,21 +324,58 @@ func append_layout(records: &Vector<RecordIR>, t: ValueType,\\n                 return problem(\\\"record must have matching nonempty fields\\\", record.line, record.col)\\n             }\\n             var n = 0\\n-            while n < path.len() {\\n-                if path[n] == index { return problem(\\\"recursive by-value record\\\", record.line, record.col) }\\n+            while n < record_path.len() {\\n+                if record_path[n] == index { return problem(\\\"recursive by-value record\\\", record.line, record.col) }\\n                 n += 1\\n             }\\n-            if path.len() >= 128 { return problem(\\\"record nesting exceeds 128 levels\\\", record.line, record.col) }\\n-            try! path.push(index)\\n+            if depth >= 128 { return problem(\\\"record nesting exceeds 128 levels\\\", record.line, record.col) }\\n+            try! record_path.push(index)\\n             n = 0\\n             while n < record.field_types.len() {\\n-                try append_layout(&records, record.field_types[n], &var path, &var words) catch {\\n+                try append_layout(&records, &sums, record.field_types[n], &var record_path, &var sum_path, depth + 1, &var words) catch {\\n                     if error.line == 0 { return problem(error.message, record.line, record.col) }\\n                     return error\\n                 }\\n                 n += 1\\n             }\\n-            let _ = path.pop()\\n+            let _ = record_path.pop()\\n+            return\\n+        },\\n+        case Optional(index) -> {\\n+            let payload = try optional_payload(&sums, t)\\n+            if payload == ValueType.Void || reference_type(payload) { return problem(\\\"optional payload must be a data type\\\", 0, 0) }\\n+            if sums[index].error != ValueType.Void { return problem(\\\"optional descriptor error must be Void\\\", 0, 0) }\\n+            var n = 0\\n+            while n < sum_path.len() {\\n+                if sum_path[n] == index { return problem(\\\"recursive aggregate type\\\", 0, 0) }\\n+                n += 1\\n+            }\\n+            if depth >= 128 { return problem(\\\"aggregate nesting exceeds 128 levels\\\", 0, 0) }\\n+            try! sum_path.push(index)\\n+            try append_layout(&records, &sums, ValueType.Bool, &var record_path, &var sum_path, depth + 1, &var words)\\n+            try append_layout(&records, &sums, payload, &var record_path, &var sum_path, depth + 1, &var words)\\n+            let _ = sum_path.pop()\\n+            return\\n+        },\\n+        case Result(index) -> {\\n+            let payload = try result_payload(&sums, t)\\n+            let error_type = try result_error(&sums, t)\\n+            if reference_type(payload) || error_type == ValueType.Void || reference_type(error_type) {\\n+                return problem(\\\"result alternatives must be data types\\\", 0, 0)\\n+            }\\n+            var n = 0\\n+            while n < sum_path.len() {\\n+                if sum_path[n] == index { return problem(\\\"recursive aggregate type\\\", 0, 0) }\\n+                n += 1\\n+            }\\n+            if depth >= 128 { return problem(\\\"aggregate nesting exceeds 128 levels\\\", 0, 0) }\\n+            try! sum_path.push(index)\\n+            try append_layout(&records, &sums, ValueType.Bool, &var record_path, &var sum_path, depth + 1, &var words)\\n+            if payload != ValueType.Void {\\n+                try append_layout(&records, &sums, payload, &var record_path, &var sum_path, depth + 1, &var words)\\n+            }\\n+            try append_layout(&records, &sums, error_type, &var record_path, &var sum_path, depth + 1, &var words)\\n+            let _ = sum_path.pop()\\n             return\\n         },\\n         case Ref(_) -> { return problem(\\\"reference record fields are not supported\\\", 0, 0) },\\n@@ -288,20 +383,21 @@ func append_layout(records: &Vector<RecordIR>, t: ValueType,\\n     }\\n }\\n \\n-public func value_layout(records: &Vector<RecordIR>, t: ValueType) -> Result<Vector<ValueType>, ProtoError> {\\n+public func value_layout(records: &Vector<RecordIR>, sums: &Vector<SumIR>, t: ValueType) -> Result<Vector<ValueType>, ProtoError> {\\n     var words = Vector<ValueType>()\\n     if reference_type(t) || string_type(t) {\\n         try! words.push(t)\\n         return move words\\n     }\\n     if t != ValueType.Void {\\n-        var path = Vector<Int>()\\n-        try append_layout(&records, t, &var path, &var words)\\n+        var record_path = Vector<Int>()\\n+        var sum_path = Vector<Int>()\\n+        try append_layout(&records, &sums, t, &var record_path, &var sum_path, 0, &var words)\\n     }\\n     move words\\n }\\n \\n-public func validate_records(records: &Vector<RecordIR>) -> Result<Void, ProtoError> {\\n+public func validate_records(records: &Vector<RecordIR>, sums: &Vector<SumIR>) -> Result<Void, ProtoError> {\\n     var n = 0\\n     while n < records.len() {\\n         let record = records[n].copy()\\n@@ -321,15 +417,25 @@ public func validate_records(records: &Vector<RecordIR>) -> Result<Void, ProtoEr\\n             }\\n             field += 1\\n         }\\n-        let _ = try value_layout(&records, ValueType.Record(n))\\n+        let _ = try value_layout(&records, &sums, ValueType.Record(n))\\n+        n += 1\\n+    }\\n+    return\\n+}\\n+\\n+public func validate_sums(records: &Vector<RecordIR>, sums: &Vector<SumIR>) -> Result<Void, ProtoError> {\\n+    var n = 0\\n+    while n < sums.len() {\\n+        let t = if sums[n].kind == SumKind.Optional { ValueType.Optional(n) } else { ValueType.Result(n) }\\n+        let _ = try value_layout(&records, &sums, t)\\n         n += 1\\n     }\\n     return\\n }\\n \\n-public func record_field_offset(records: &Vector<RecordIR>, record_index: Int,\\n+public func record_field_offset(records: &Vector<RecordIR>, sums: &Vector<SumIR>, record_index: Int,\\n                                 field_index: Int) -> Result<Int, ProtoError> {\\n-    let _ = try value_layout(&records, ValueType.Record(record_index))\\n+    let _ = try value_layout(&records, &sums, ValueType.Record(record_index))\\n     let record = records[record_index].copy()\\n     if field_index < 0 || field_index >= record.field_types.len() {\\n         return problem(\\\"invalid record field index\\\", record.line, record.col)\\n@@ -337,7 +443,7 @@ public func record_field_offset(records: &Vector<RecordIR>, record_index: Int,\\n     var offset = 0\\n     var n = 0\\n     while n < field_index {\\n-        let field = try value_layout(&records, record.field_types[n])\\n+        let field = try value_layout(&records, &sums, record.field_types[n])\\n         offset += field.len()\\n         n += 1\\n     }\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex 59ed1bc4..b18172a3 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -20,6 +20,14 @@ func check_type(p: &Program, t: ValueType) -> Result<Void, ProtoError> {\\n         case Ref(index) -> {\\n             if index < 0 || index >= p.references.len() { return problem(\\\"internal prototype error: invalid reference type index\\\", 0, 0) }\\n         },\\n+        case Optional(index) -> {\\n+            if index < 0 || index >= p.sums.len() { return problem(\\\"internal prototype error: invalid optional type index\\\", 0, 0) }\\n+            if p.sums[index].kind != SumKind.Optional { return problem(\\\"internal prototype error: optional type has mismatched descriptor kind\\\", 0, 0) }\\n+        },\\n+        case Result(index) -> {\\n+            if index < 0 || index >= p.sums.len() { return problem(\\\"internal prototype error: invalid result type index\\\", 0, 0) }\\n+            if p.sums[index].kind != SumKind.Result { return problem(\\\"internal prototype error: result type has mismatched descriptor kind\\\", 0, 0) }\\n+        },\\n         case _ -> {}\\n     }\\n     return\\n@@ -32,6 +40,12 @@ func check_declared_types(p: &Program) -> Result<Void, ProtoError> {\\n         n += 1\\n     }\\n     n = 0\\n+    while n < p.sums.len() {\\n+        try check_type(&p, p.sums[n].payload)\\n+        try check_type(&p, p.sums[n].error)\\n+        n += 1\\n+    }\\n+    n = 0\\n     while n < p.records.len() {\\n         let record = p.records[n].copy()\\n         var e = 0\\n@@ -71,7 +85,7 @@ func parameter_layout(p: &Program, f: &FuncIR) -> Result<Vector<ValueType>, Prot\\n     var n = 0\\n     while n < f.params.len() {\\n         if f.params[n] == ValueType.Void { return problem(\\\"internal prototype error: Void parameter\\\", 0, 0) }\\n-        let layout = try value_layout(&p.records, f.params[n])\\n+        let layout = try value_layout(&p.records, &p.sums, f.params[n])\\n         var word = 0\\n         while word < layout.len() {\\n             try! words.push(layout[word])\\n@@ -132,19 +146,19 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n         case Addr -> {\\n             if not reference_type(dst) { return invalid(&i) }\\n             let target = try reference_target(&p.references, dst)\\n-            let layout = try value_layout(&p.records, target)\\n+            let layout = try value_layout(&p.records, &p.sums, target)\\n             if not range_matches(&f, i.a, &layout) { return invalid(&i) }\\n         },\\n         case LoadRef -> {\\n             if not reference_type(a) { return invalid(&i) }\\n             let target = try reference_target(&p.references, a)\\n-            let layout = try value_layout(&p.records, target)\\n+            let layout = try value_layout(&p.records, &p.sums, target)\\n             if not range_matches(&f, i.dst, &layout) { return invalid(&i) }\\n         },\\n         case StoreRef -> {\\n             if not reference_mutable(&p.references, a) { return invalid(&i) }\\n             let target = try reference_target(&p.references, a)\\n-            let layout = try value_layout(&p.records, target)\\n+            let layout = try value_layout(&p.records, &p.sums, target)\\n             if not range_matches(&f, i.b, &layout) { return invalid(&i) }\\n         },\\n         case RefField -> {\\n@@ -204,7 +218,7 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n             if callee.result == ValueType.Void {\\n                 if i.dst != -1 { return invalid(&i) }\\n             } else {\\n-                let result = try value_layout(&p.records, callee.result)\\n+                let result = try value_layout(&p.records, &p.sums, callee.result)\\n                 if not range_matches(&f, i.dst, &result) { return invalid(&i) }\\n             }\\n             var n = 0\\n@@ -220,7 +234,7 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n             if f.result == ValueType.Void {\\n                 if i.a != -1 { return invalid(&i) }\\n             } else {\\n-                let result = try value_layout(&p.records, f.result)\\n+                let result = try value_layout(&p.records, &p.sums, f.result)\\n                 if not range_matches(&f, i.a, &result) { return invalid(&i) }\\n             }\\n         }\\n@@ -235,7 +249,7 @@ func check_binary(i: &Instruction, dst: ValueType, a: ValueType, b: ValueType) -\\n \\n func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n     let params = try parameter_layout(&p, &f)\\n-    let _ = try value_layout(&p.records, f.result)\\n+    let _ = try value_layout(&p.records, &p.sums, f.result)\\n     if f.code.len() == 0 || f.slots.len() < params.len() {\\n         return problem(\\\"internal prototype error: invalid function\\\", 0, 0)\\n     }\\n@@ -275,7 +289,8 @@ public func verify(program: &Program) -> Result<Void, ProtoError> {\\n     try validate_enums(&program.enums)\\n     try validate_references(&program.references)\\n     try check_declared_types(&program)\\n-    try validate_records(&program.records)\\n+    try validate_sums(&program.records, &program.sums)\\n+    try validate_records(&program.records, &program.sums)\\n     if program.main_index < 0 || program.main_index >= program.functions.len() {\\n         return problem(\\\"internal prototype error: missing main\\\", 0, 0)\\n     }\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 3e4fb528..4eafa274 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -175,7 +175,7 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n             case LoadRef -> {\\n                 let address = cells[base + inst.a]\\n                 let target = try reference_target(&program.references, function.slots[inst.a])\\n-                let layout = try value_layout(&program.records, target)\\n+                let layout = try value_layout(&program.records, &program.sums, target)\\n                 if not valid_reference(&cells, address, layout.len()) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n                 var values = Vector<Int>()\\n                 var word = 0\\n@@ -199,7 +199,7 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n             case StoreRef -> {\\n                 let address = cells[base + inst.a]\\n                 let target = try reference_target(&program.references, function.slots[inst.a])\\n-                let layout = try value_layout(&program.records, target)\\n+                let layout = try value_layout(&program.records, &program.sums, target)\\n                 if not valid_reference(&cells, address, layout.len()) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n                 var values = Vector<Int>()\\n                 var word = 0\\n@@ -223,11 +223,11 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n             case RefField -> {\\n                 let address = cells[base + inst.a]\\n                 let parent = try reference_target(&program.references, function.slots[inst.a])\\n-                let layout = try value_layout(&program.records, parent)\\n+                let layout = try value_layout(&program.records, &program.sums, parent)\\n                 if not valid_reference(&cells, address, layout.len()) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n                 match parent {\\n                     case Record(index) -> {\\n-                        let offset = try record_field_offset(&program.records, index, inst.immediate)\\n+                        let offset = try record_field_offset(&program.records, &program.sums, index, inst.immediate)\\n                         cells[base + inst.dst] = address + offset\\n                     },\\n                     case _ -> { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n@@ -335,7 +335,7 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n             },\\n             case Return -> {\\n                 var values = Vector<Int>()\\n-                let layout = try value_layout(&program.records, function.result)\\n+                let layout = try value_layout(&program.records, &program.sums, function.result)\\n                 var result_index = 0\\n                 while result_index < layout.len() {\\n                     if string_type(layout[result_index]) { try string_retain(&var heap, cells[base + inst.a + result_index]) }\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex ab5fa564..533bc2d6 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -35,6 +35,16 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"optionals/review_nested_context\\\", \\\"chosen\\\\nsome-none\\\\nnested-none\\\\n\\\", section=\\\"optionals\\\"),\\n+    RunCase(\\\"optionals/basic_numeric\\\", \\\"7\\\\nnone\\\\n255\\\\n\\\", section=\\\"optionals\\\"),\\n+    RunCase(\\\"optionals/nested_semantics\\\", \\\"outer-none\\\\ninner-none\\\\n9\\\\nbranch-inner-none\\\\n7\\\\n18446744073709551615\\\\n\\\", section=\\\"optionals\\\"),\\n+    RunCase(\\\"optionals/contextual_calls_arms\\\", \\\"255\\\\nnone\\\\na\\\\nchain\\\\nignored\\\\n\\\", section=\\\"optionals\\\"),\\n+    RunCase(\\\"optionals/string_record_payload\\\", \\\"alpha\\\\nalpha\\\\nnone\\\\n\\\", section=\\\"optionals\\\"),\\n+    RunCase(\\\"optionals/references_methods\\\", \\\"start\\\\nnext\\\\nnone\\\\n\\\", section=\\\"optionals\\\"),\\n+    RunCase(\\\"optionals/binding_snapshot_shadow\\\", \\\"old\\\\nnew\\\\nchanged\\\\nold\\\\n\\\", section=\\\"optionals\\\"),\\n+    RunCase(\\\"optionals/value_binding_subject_once\\\", \\\"subject\\\\nyes\\\\nsubject\\\\nfallback\\\\n\\\", section=\\\"optionals\\\"),\\n+    RunCase(\\\"optionals/early_return_loop_cleanup\\\", \\\"early\\\\nodd\\\\n199\\\\n\\\", section=\\\"optionals\\\"),\\n+    RunCase(\\\"optionals/token_tok_extract\\\", \\\"word\\\\n4\\\\n9\\\\nnone\\\\n\\\", section=\\\"optionals\\\"),\\n     RunCase(\\\"owning_records/copy_replace_nested\\\", \\\"alpha\\\\ninner\\\\nbeta\\\\nchanged\\\\n\\\", section=\\\"owning_records\\\"),\\n     RunCase(\\\"owning_records/references_methods_abi\\\", \\\"left\\\\nright\\\\nnext\\\\nfield\\\\n9\\\\n\\\", section=\\\"owning_records\\\"),\\n     RunCase(\\\"owning_records/value_control_cleanup\\\", \\\"then\\\\n1\\\\nred\\\\n2\\\\nearly\\\\n3\\\\n\\\", section=\\\"owning_records\\\"),\\n@@ -195,6 +205,15 @@ RUN_CASES = (\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"optionals/reject_uncontextualized_none\\\", \\\"None requires an expected Optional type\\\", \\\"optionals\\\"),\\n+    RejectCase(\\\"optionals/reject_if_let_non_optional\\\", \\\"if binding requires an Optional value\\\", \\\"optionals\\\"),\\n+    RejectCase(\\\"optionals/reject_binding_scope\\\", \\\"unknown name\\\", \\\"optionals\\\"),\\n+    RejectCase(\\\"optionals/reject_coalescing\\\", \\\"optional coalescing `??` is not supported\\\", \\\"optionals\\\"),\\n+    RejectCase(\\\"optionals/reject_chaining\\\", \\\"optional chaining `?.` is not supported\\\", \\\"optionals\\\"),\\n+    RejectCase(\\\"optionals/reject_force_unwrap\\\", \\\"optional force unwrap `!` is not supported\\\", \\\"optionals\\\"),\\n+    RejectCase(\\\"optionals/reject_optional_void\\\", \\\"Optional<Void> is not supported\\\", \\\"optionals\\\"),\\n+    RejectCase(\\\"optionals/reject_value_binding_missing_tail\\\", \\\"continuing value branch requires a tail expression\\\", \\\"optionals\\\"),\\n+    RejectCase(\\\"optionals/reject_numeric_range\\\", \\\"integer literal does not fit UInt8\\\", \\\"optionals\\\"),\\n     RejectCase(\\\"owning_records/reject_string_match_duplicate\\\", \\\"duplicate String match pattern\\\", \\\"owning_records\\\"),\\n     RejectCase(\\\"owning_records/reject_string_match_non_string\\\", \\\"String match patterns must be String literals or `_`\\\", \\\"owning_records\\\"),\\n     RejectCase(\\\"owning_records/reject_string_match_interpolation\\\", \\\"string interpolation is not supported in match patterns\\\", \\\"owning_records\\\"),\\n@@ -408,7 +427,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -437,7 +456,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\") else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\ndiff --git a/prototypes/minivm/tests/enum_contract.saw b/prototypes/minivm/tests/enum_contract.saw\\nindex 0244a0cb..d600c049 100644\\n--- a/prototypes/minivm/tests/enum_contract.saw\\n+++ b/prototypes/minivm/tests/enum_contract.saw\\n@@ -67,7 +67,7 @@ func base_program() -> Program {\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move identity_params,\\n         result: ValueType.Enum(0), slots: move identity_slots,\\n         code: move identity_code))\\n-    Program(strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: move enums, records: move records,\\n+    Program(strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: move enums, records: move records, sums: Vector<SumIR>(),\\n             functions: move functions, main_index: 0)\\n }\\n \\n@@ -77,7 +77,7 @@ func main() {\\n     assert(scalar_type(ValueType.Enum(0)), \\\"enum is scalar\\\")\\n     assert(not integer_type(ValueType.Enum(0)), \\\"enum is not integer\\\")\\n     assert(not printable_type(ValueType.Enum(0)), \\\"enum is not printable\\\")\\n-    let record_layout = try! value_layout(&program.records, ValueType.Record(0))\\n+    let record_layout = try! value_layout(&program.records, &program.sums, ValueType.Record(0))\\n     assert(record_layout.len() == 2 && record_layout[0] == ValueType.Enum(0) &&\\n            record_layout[1] == ValueType.I32, \\\"record preserves enum field type\\\")\\n \\ndiff --git a/prototypes/minivm/tests/optional_contract.saw b/prototypes/minivm/tests/optional_contract.saw\\nnew file mode 100644\\nindex 00000000..a6c95482\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/optional_contract.saw\\n@@ -0,0 +1,114 @@\\n+import src.model.*\\n+import src.verify.*\\n+import src.vm.*\\n+import src.llvm\\n+import std.env.{Env}\\n+\\n+func valid(p: &Program) -> Bool {\\n+    try verify(&p) catch { return false }\\n+    true\\n+}\\n+\\n+func op(code: &var Vector<Instruction>, kind: Op, dst: Int, a: Int, immediate: Int) {\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.immediate = immediate\\n+    try! code.push(move i)\\n+}\\n+\\n+func base_program() -> Program {\\n+    var sums = Vector<SumIR>()\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.String, error: ValueType.Void))\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.Optional(0), error: ValueType.Void))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Void, error: ValueType.String))\\n+    let names: Vector<String> = [\\\"nested\\\", \\\"number\\\"]\\n+    let fields: Vector<ValueType> = [ValueType.Optional(1), ValueType.I64]\\n+    var records = Vector<RecordIR>()\\n+    try! records.push(RecordIR(name: \\\"Container\\\", field_names: move names,\\n+        field_types: move fields, line: 1, col: 1))\\n+    var references = Vector<ReferenceIR>()\\n+    try! references.push(ReferenceIR(target: ValueType.Optional(0), mutable: true))\\n+    let slots: Vector<ValueType> = [ValueType.Bool, ValueType.String, ValueType.Bool, ValueType.String]\\n+    var code = Vector<Instruction>()\\n+    op(&var code, Op.Const, 0, -1, 0)\\n+    // Inactive owning words are real nulls, so clearing a freshly initialized None is safe.\\n+    op(&var code, Op.Drop, -1, 1, 0)\\n+    op(&var code, Op.StringConst, 1, -1, 0)\\n+    op(&var code, Op.Const, 0, -1, 1)\\n+    var call = instruction(Op.Call)\\n+    call.dst = 2\\n+    call.target = 1\\n+    try! call.args.push(0)\\n+    try! call.args.push(1)\\n+    try! code.push(move call)\\n+    op(&var code, Op.Print, -1, 3, 0)\\n+    op(&var code, Op.Drop, -1, 1, 0)\\n+    op(&var code, Op.Drop, -1, 3, 0)\\n+    op(&var code, Op.Return, -1, -1, 0)\\n+    var functions = Vector<FuncIR>()\\n+    try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: move slots, code: move code))\\n+    let echo_params: Vector<ValueType> = [ValueType.Optional(0)]\\n+    let echo_slots: Vector<ValueType> = [ValueType.Bool, ValueType.String]\\n+    var echo_code = Vector<Instruction>()\\n+    op(&var echo_code, Op.Return, -1, 0, 0)\\n+    try! functions.push(FuncIR(name: \\\"echo\\\", params: move echo_params, result: ValueType.Optional(0),\\n+        slots: move echo_slots, code: move echo_code))\\n+    let strings: Vector<String> = [\\\"some\\\"]\\n+    Program(strings: move strings, references: move references, enums: Vector<EnumIR>(),\\n+        records: move records, sums: move sums, functions: move functions, main_index: 0)\\n+}\\n+\\n+func main() {\\n+    let p = base_program()\\n+    assert(valid(&p), \\\"valid nested optional, Result<Void, String>, record, and reference\\\")\\n+    let optional = try! value_layout(&p.records, &p.sums, ValueType.Optional(0))\\n+    assert(optional.len() == 2 && optional[0] == ValueType.Bool && optional[1] == ValueType.String,\\n+        \\\"optional tag and payload layout\\\")\\n+    let nested = try! value_layout(&p.records, &p.sums, ValueType.Optional(1))\\n+    assert(nested.len() == 3 && nested[0] == ValueType.Bool && nested[1] == ValueType.Bool && nested[2] == ValueType.String,\\n+        \\\"nested optional layout\\\")\\n+    let result = try! value_layout(&p.records, &p.sums, ValueType.Result(2))\\n+    assert(result.len() == 2 && result[0] == ValueType.Bool && result[1] == ValueType.String,\\n+        \\\"Void success contributes no Result payload words\\\")\\n+    let record = try! value_layout(&p.records, &p.sums, ValueType.Record(0))\\n+    assert(record.len() == 4 && record[3] == ValueType.I64, \\\"optional record field layout\\\")\\n+\\n+    var bad_index = p.copy()\\n+    bad_index.references[0].target = ValueType.Optional(99)\\n+    assert(not valid(&bad_index), \\\"invalid sum index\\\")\\n+    var bad_kind = p.copy()\\n+    bad_kind.references[0].target = ValueType.Optional(2)\\n+    assert(not valid(&bad_kind), \\\"descriptor kind mismatch\\\")\\n+    var bad_unused_type = p.copy()\\n+    bad_unused_type.sums[0].payload = ValueType.Enum(99)\\n+    assert(not valid(&bad_unused_type), \\\"unused sum descriptor types are checked\\\")\\n+    var bad_optional_void = p.copy()\\n+    bad_optional_void.sums[0].payload = ValueType.Void\\n+    assert(not valid(&bad_optional_void), \\\"Optional Void payload\\\")\\n+    var bad_optional_ref = p.copy()\\n+    bad_optional_ref.sums[0].payload = ValueType.Ref(0)\\n+    assert(not valid(&bad_optional_ref), \\\"Optional reference payload\\\")\\n+    var bad_optional_error = p.copy()\\n+    bad_optional_error.sums[0].error = ValueType.I64\\n+    assert(not valid(&bad_optional_error), \\\"Optional descriptor error is Void\\\")\\n+    var bad_result_error = p.copy()\\n+    bad_result_error.sums[2].error = ValueType.Void\\n+    assert(not valid(&bad_result_error), \\\"Result error cannot be Void\\\")\\n+    var bad_sum_cycle = p.copy()\\n+    bad_sum_cycle.sums[0].payload = ValueType.Optional(0)\\n+    assert(not valid(&bad_sum_cycle), \\\"direct sum cycle\\\")\\n+    var bad_cross_cycle = p.copy()\\n+    bad_cross_cycle.sums[0].payload = ValueType.Record(0)\\n+    bad_cross_cycle.records[0].field_types.set(0, ValueType.Optional(0))\\n+    assert(not valid(&bad_cross_cycle), \\\"record and sum cycle\\\")\\n+\\n+    let args = try! Env.args()\\n+    if args.len() == 2 && args[1] == \\\"emit-llvm\\\" {\\n+        print(try! llvm.emit_llvm(&p))\\n+        return\\n+    }\\n+    try! execute(&p, 100)\\n+    print(\\\"optional contract passed\\\")\\n+}\\ndiff --git a/prototypes/minivm/tests/owning_record_contract.saw b/prototypes/minivm/tests/owning_record_contract.saw\\nindex 327b3112..1eeb9c50 100644\\n--- a/prototypes/minivm/tests/owning_record_contract.saw\\n+++ b/prototypes/minivm/tests/owning_record_contract.saw\\n@@ -69,6 +69,7 @@ func base_program() -> Program {\\n     try! funcs.push(FuncIR(name: \\\"echo\\\", params: move params, result: ValueType.Record(0),\\n         slots: move echo_slots, code: move echo_code))\\n     Program(strings: move strings, references: move refs, enums: Vector<EnumIR>(),\\n+            sums: Vector<SumIR>(),\\n         records: move records, functions: move funcs, main_index: 0)\\n }\\n \\n@@ -80,7 +81,7 @@ func main() {\\n         print(try! llvm.emit_llvm(&p))\\n         return\\n     }\\n-    let layout = try! value_layout(&p.records, ValueType.Record(0))\\n+    let layout = try! value_layout(&p.records, &p.sums, ValueType.Record(0))\\n     assert(layout.len() == 2 && layout[0] == ValueType.String && layout[1] == ValueType.String,\\n         \\\"owning record layout preserves String leaves\\\")\\n     var bad_range = p.copy()\\ndiff --git a/prototypes/minivm/tests/receiver_contract.saw b/prototypes/minivm/tests/receiver_contract.saw\\nindex ca76a4ea..f8a400d8 100644\\n--- a/prototypes/minivm/tests/receiver_contract.saw\\n+++ b/prototypes/minivm/tests/receiver_contract.saw\\n@@ -57,7 +57,7 @@ func base_program() -> Program {\\n     var funcs = Vector<FuncIR>()\\n     try! funcs.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n         slots: move slots, code: move code))\\n-    Program(strings: Vector<String>(), references: move refs, enums: Vector<EnumIR>(), records: move records,\\n+    Program(strings: Vector<String>(), references: move refs, enums: Vector<EnumIR>(), records: move records, sums: Vector<SumIR>(),\\n         functions: move funcs, main_index: 0)\\n }\\n \\n@@ -85,8 +85,8 @@ func main() {\\n     let p = base_program()\\n     assert(valid(&p), \\\"nested projection, whole record access and qualification\\\")\\n     try! execute(&p, 1000)\\n-    assert((try! value_layout(&p.records, ValueType.Record(1))).len() == 3, \\\"flattened outer layout\\\")\\n-    assert((try! record_field_offset(&p.records, 1, 1)) == 1, \\\"nested record field offset\\\")\\n+    assert((try! value_layout(&p.records, &p.sums, ValueType.Record(1))).len() == 3, \\\"flattened outer layout\\\")\\n+    assert((try! record_field_offset(&p.records, &p.sums, 1, 1)) == 1, \\\"nested record field offset\\\")\\n     assert(rejected(&p, 5, Op.RefField, 5, 3, -1, -1), \\\"negative field index\\\")\\n     assert(rejected(&p, 5, Op.RefField, 5, 3, -1, 2), \\\"high field index\\\")\\n     assert(rejected(&p, 5, Op.RefField, 7, 3, -1, 1), \\\"field target must match\\\")\\ndiff --git a/prototypes/minivm/tests/record_contract.saw b/prototypes/minivm/tests/record_contract.saw\\nindex 08f9c7b0..78130a86 100644\\n--- a/prototypes/minivm/tests/record_contract.saw\\n+++ b/prototypes/minivm/tests/record_contract.saw\\n@@ -12,7 +12,8 @@ func pair_record() -> RecordIR {\\n }\\n \\n func layout_fails(records: &Vector<RecordIR>, ty: ValueType) -> Bool {\\n-    let _ = try value_layout(&records, ty) catch { return true }\\n+    let sums = Vector<SumIR>()\\n+    let _ = try value_layout(&records, &sums, ty) catch { return true }\\n     false\\n }\\n \\n@@ -46,7 +47,7 @@ func call_program(records: &Vector<RecordIR>) -> Program {\\n         slots: slots.copy(), code: move main_code))\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move params, result: ValueType.Record(0),\\n         slots: move slots, code: move callee_code))\\n-    Program(strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(), records: records.copy(), functions: move functions, main_index: 0)\\n+    Program(strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(), records: records.copy(), sums: Vector<SumIR>(), functions: move functions, main_index: 0)\\n }\\n \\n func main() {\\n@@ -61,12 +62,13 @@ func main() {\\n     try! types.push(ValueType.Record(0))\\n     try! types.push(ValueType.I16)\\n     try! records.push(RecordIR(name: \\\"Outer\\\", field_names: move names, field_types: move types, line: 2, col: 1))\\n-    try! validate_records(&records)\\n-    let layout = try! value_layout(&records, ValueType.Record(1))\\n+    let sums = Vector<SumIR>()\\n+    try! validate_records(&records, &sums)\\n+    let layout = try! value_layout(&records, &sums, ValueType.Record(1))\\n     assert(layout.len() == 4, \\\"nested width\\\")\\n     assert(layout[0] == ValueType.Bool && layout[1] == ValueType.U8 && layout[2] == ValueType.I64 && layout[3] == ValueType.I16, \\\"nested types\\\")\\n-    assert((try! record_field_offset(&records, 1, 1)) == 1, \\\"nested offset\\\")\\n-    assert((try! record_field_offset(&records, 1, 2)) == 3, \\\"following field offset\\\")\\n+    assert((try! record_field_offset(&records, &sums, 1, 1)) == 1, \\\"nested offset\\\")\\n+    assert((try! record_field_offset(&records, &sums, 1, 2)) == 3, \\\"following field offset\\\")\\n     assert(layout_fails(&records, ValueType.Record(-1)), \\\"negative record index\\\")\\n     assert(layout_fails(&records, ValueType.Record(2)), \\\"large record index\\\")\\n     var cyclic = records.copy()\\n@@ -89,7 +91,7 @@ func main() {\\n     }\\n     var wide_records = Vector<RecordIR>()\\n     try! wide_records.push(move wide)\\n-    assert((try! value_layout(&wide_records, ValueType.Record(0))).len() == 256, \\\"exact width limit\\\")\\n+    assert((try! value_layout(&wide_records, &sums, ValueType.Record(0))).len() == 256, \\\"exact width limit\\\")\\n     try! wide_records[0].field_names.push(\\\"extra\\\")\\n     try! wide_records[0].field_types.push(ValueType.I64)\\n     assert(layout_fails(&wide_records, ValueType.Record(0)), \\\"over width limit\\\")\\ndiff --git a/prototypes/minivm/tests/reference_contract.saw b/prototypes/minivm/tests/reference_contract.saw\\nindex 393fe3ac..583233b4 100644\\n--- a/prototypes/minivm/tests/reference_contract.saw\\n+++ b/prototypes/minivm/tests/reference_contract.saw\\n@@ -55,7 +55,7 @@ func base_program() -> Program {\\n         slots: move slots, code: move code))\\n     try! funcs.push(FuncIR(name: \\\"read\\\", params: move read_params, result: ValueType.I64,\\n         slots: move read_slots, code: move read_code))\\n-    Program(strings: Vector<String>(), references: move refs, enums: move enums, records: Vector<RecordIR>(),\\n+    Program(strings: Vector<String>(), references: move refs, enums: move enums, records: Vector<RecordIR>(), sums: Vector<SumIR>(),\\n         functions: move funcs, main_index: 0)\\n }\\n \\n@@ -89,7 +89,7 @@ func main() {\\n     assert(not scalar_type(ValueType.Ref(0)), \\\"reference is not scalar data\\\")\\n     assert(not printable_type(ValueType.Ref(0)), \\\"reference is not printable\\\")\\n     assert(not integer_type(ValueType.Ref(0)), \\\"reference is not an integer\\\")\\n-    assert((try! value_layout(&p.records, ValueType.Ref(0))).len() == 1, \\\"reference word layout\\\")\\n+    assert((try! value_layout(&p.records, &p.sums, ValueType.Ref(0))).len() == 1, \\\"reference word layout\\\")\\n     assert(reference_transfer(&p.references, ValueType.Ref(0), ValueType.Ref(1)), \\\"weaken mutability\\\")\\n     assert(not reference_transfer(&p.references, ValueType.Ref(1), ValueType.Ref(0)), \\\"cannot strengthen\\\")\\n     assert(not reference_transfer(&p.references, ValueType.Ref(0), ValueType.Ref(2)), \\\"same referent required\\\")\\ndiff --git a/prototypes/minivm/tests/string_contract.saw b/prototypes/minivm/tests/string_contract.saw\\nindex 4978db81..98978584 100644\\n--- a/prototypes/minivm/tests/string_contract.saw\\n+++ b/prototypes/minivm/tests/string_contract.saw\\n@@ -52,7 +52,7 @@ func base_program() -> Program {\\n         slots: move slots, code: move code))\\n     try! funcs.push(FuncIR(name: \\\"echo\\\", params: move params, result: ValueType.String,\\n         slots: move echo_slots, code: move echo_code))\\n-    Program(strings: move strings, references: move refs, enums: Vector<EnumIR>(),\\n+    Program(strings: move strings, references: move refs, enums: Vector<EnumIR>(), sums: Vector<SumIR>(),\\n         records: Vector<RecordIR>(), functions: move funcs, main_index: 0)\\n }\\n \\n@@ -73,7 +73,7 @@ func main() {\\n     try! execute(&p, 1000)\\n     assert(not scalar_type(ValueType.String), \\\"owning String is not scalar data\\\")\\n     assert(printable_type(ValueType.String), \\\"String is printable\\\")\\n-    assert((try! value_layout(&p.records, ValueType.String)).len() == 1, \\\"String logical word\\\")\\n+    assert((try! value_layout(&p.records, &p.sums, ValueType.String)).len() == 1, \\\"String logical word\\\")\\n     assert(rejected(&p, 0, Op.StringConst, 0, -1, -1, -1), \\\"negative pool index\\\")\\n     assert(rejected(&p, 0, Op.StringConst, 0, -1, -1, 2), \\\"high pool index\\\")\\n     assert(rejected(&p, 0, Op.StringConst, 2, -1, -1, 0), \\\"StringConst destination\\\")\\n\",\"id\":\"SL-253\",\"request_id\":\"mv2-SL-253-2895f334-a1\",\"title\":\"minivm M10: Optional values + conditional binding (+ bundled M10/M11/M12 design docs; branch commits 339395ff+2895f334)\"}","request_id":"mv2-SL-253-2895f334-a1","sequence":499,"timestamp":"1789099170"}
---
