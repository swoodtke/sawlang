---
{"action":"create","actor":"agent:claude-air-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-air-sawlang","closed":"","comments":[],"created":"1790279921","description":"Found by the SL-357 comment-review agent on Sep 24 while reading std/taskgroup.saw and the reactors against the code. These are SOURCE-READING candidates: none has been reproduced. Under the Python-compiler freeze they're recorded for the hazards ledger. Note that std/rt are Saw code the self-hosted compiler will reuse, so they matter beyond the freeze.\n\n1. **Unlocked generation read (handle aliasing):** the synthesized spawn helper reads `__gen_at` right after `__enqueue`, without the run-queue lock. In an MT group a worker can finish and recycle a Void task's slot in that window, so the handle may carry the retired generation and later name a successor task that reuses the slot. Check: does handle validation then accept the wrong task?\n2. **Missed cancel:** a `cancel_addr` write from another thread isn't seen by an ambient sweep that is already blocked in `poll(-1)`. Nothing wakes the poll for a cancel.\n3. **Quiescence, false deadlock:** a detached thread stops counting toward the quiescent report, so a detached thread that will still send on a channel can make a live program be reported as deadlocked. Groups beyond `BT_MAX_GROUPS` aren't walked by the report either.\n4. **Ambient list:** it is linked and unlinked without synchronization, assuming one owner thread. Check whether any path (a Thread.spawn body running ambient tasks?) breaks that.\n\nEach item needs a reproduction (for the concurrency ones, an oversubscribed or instrumented program in the SL-353 style) before it's classified as a real hazard, a fix candidate, or not reachable.","id":"SL-392","labels":["runtime","concurrency","hazard-candidate"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1496,"stage":"backlog","status":"open","title":"Runtime concurrency hazard candidates found reading std/taskgroup.saw (SL-357 final wave): unlocked __gen_at read after __enqueue, a missed cancel in a blocked ambient poll, detached threads leaving the quiescent count, an unsynchronized ambient list","updated":"1790279921"},"before":null,"id":"SL-392","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-air-sawlang\",\"body\":\"\",\"description\":\"Found by the SL-357 comment-review agent on Sep 24 while reading std/taskgroup.saw and the reactors against the code. These are SOURCE-READING candidates: none has been reproduced. Under the Python-compiler freeze they're recorded for the hazards ledger. Note that std/rt are Saw code the self-hosted compiler will reuse, so they matter beyond the freeze.\\n\\n1. **Unlocked generation read (handle aliasing):** the synthesized spawn helper reads `__gen_at` right after `__enqueue`, without the run-queue lock. In an MT group a worker can finish and recycle a Void task's slot in that window, so the handle may carry the retired generation and later name a successor task that reuses the slot. Check: does handle validation then accept the wrong task?\\n2. **Missed cancel:** a `cancel_addr` write from another thread isn't seen by an ambient sweep that is already blocked in `poll(-1)`. Nothing wakes the poll for a cancel.\\n3. **Quiescence, false deadlock:** a detached thread stops counting toward the quiescent report, so a detached thread that will still send on a channel can make a live program be reported as deadlocked. Groups beyond `BT_MAX_GROUPS` aren't walked by the report either.\\n4. **Ambient list:** it is linked and unlinked without synchronization, assuming one owner thread. Check whether any path (a Thread.spawn body running ambient tasks?) breaks that.\\n\\nEach item needs a reproduction (for the concurrency ones, an oversubscribed or instrumented program in the SL-353 style) before it's classified as a real hazard, a fix candidate, or not reachable.\",\"labels\":[\"runtime\",\"concurrency\",\"hazard-candidate\"],\"project\":\"SL\",\"request_id\":\"pid-40733-1790279921-10000\",\"title\":\"Runtime concurrency hazard candidates found reading std/taskgroup.saw (SL-357 final wave): unlocked __gen_at read after __enqueue, a missed cancel in a blocked ambient poll, detached threads leaving the quiescent count, an unsynchronized ambient list\"}","request_id":"pid-40733-1790279921-10000","sequence":1496,"timestamp":"1790279921"}
---
