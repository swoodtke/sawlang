---
{"action":"create","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790261822","description":"A cast to an optional type is refused with a message that does not explain itself. On main:\n- `(n as Int?) ?? 9` with `n: Int?` says ``cannot cast `Int?` to `Int?` ``. It reads as the compiler refusing an identity.\n- With `n: Int8?` it says ``cannot cast `Int8?` to `Int?` ``.\n- `n as Optional<Int?>` says ``cannot cast `Int?` to `Int??` ``.\n\nThe rule itself is intended: a cast converts a value and never produces an optional (SL-309, amended ruling c2). The message should say so and name the ways out:\n- coalesce first, then convert: `(n ?? 9) as Int`;\n- unwrap with `if let` / `guard let`, then cast the payload;\n- for a non-optional source, let assignment auto-wrap it (`let x: Int? = n as Int`) rather than casting to `Int?`.\n\nMechanism, per obligation 4. Name the check that produces \"cannot cast\" in the typechecker's cast path, then sweep every place an optional can appear on either side of `as`:\n- an optional target;\n- an optional source with a non-optional target (`n as Int` where `n: Int8?`), which probably gives the same unhelpful message;\n- an optional nested inside a generic target (`x as Vector<Int?>`);\n- an existential target over an optional source.\n\nEach cell gets a pinned diagnostic in examples/errors/. This is diagnostics only; no cast gains a meaning.\n\nFiled by the lead from the U0′ r2 review (SL-2.p2). Small; fits any lane after U0′ lands (the parser's cast-target note, which teaches the same rule, is in U0′).\n","id":"SL-377","labels":[],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1441,"stage":"backlog","status":"open","title":"A cast to an optional type is refused as 'cannot cast Int? to Int?': say that a cast never produces an optional, and teach coalesce-then-convert","updated":"1790261822"},"before":null,"id":"SL-377","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"description\":\"A cast to an optional type is refused with a message that does not explain itself. On main:\\n- `(n as Int?) ?? 9` with `n: Int?` says ``cannot cast `Int?` to `Int?` ``. It reads as the compiler refusing an identity.\\n- With `n: Int8?` it says ``cannot cast `Int8?` to `Int?` ``.\\n- `n as Optional<Int?>` says ``cannot cast `Int?` to `Int??` ``.\\n\\nThe rule itself is intended: a cast converts a value and never produces an optional (SL-309, amended ruling c2). The message should say so and name the ways out:\\n- coalesce first, then convert: `(n ?? 9) as Int`;\\n- unwrap with `if let` / `guard let`, then cast the payload;\\n- for a non-optional source, let assignment auto-wrap it (`let x: Int? = n as Int`) rather than casting to `Int?`.\\n\\nMechanism, per obligation 4. Name the check that produces \\\"cannot cast\\\" in the typechecker's cast path, then sweep every place an optional can appear on either side of `as`:\\n- an optional target;\\n- an optional source with a non-optional target (`n as Int` where `n: Int8?`), which probably gives the same unhelpful message;\\n- an optional nested inside a generic target (`x as Vector<Int?>`);\\n- an existential target over an optional source.\\n\\nEach cell gets a pinned diagnostic in examples/errors/. This is diagnostics only; no cast gains a meaning.\\n\\nFiled by the lead from the U0′ r2 review (SL-2.p2). Small; fits any lane after U0′ lands (the parser's cast-target note, which teaches the same rule, is in U0′).\\n\",\"label\":\"diagnostics\",\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"pid-43760-1790261822-6000\",\"title\":\"A cast to an optional type is refused as 'cannot cast Int? to Int?': say that a cast never produces an optional, and teach coalesce-then-convert\"}","request_id":"pid-43760-1790261822-6000","sequence":1441,"timestamp":"1790261822"}
---
