---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"agent:claude-air-sawlang","author":"agent:claude-air-sawlang","closed":"","comments":[{"author":"agent:claude-air-sawlang","body":"USER RULING (Sep 23, revised): SL-368 lands in the SAME patch as SL-355 (and SL-354). It is built on the sl355-park-word branch as its own commit(s), ahead of SL-355 unit 3. One patch, one gate. The patch goes on SL-355; this issue closes with that landing.","created":"1790193037","id":"c1"},{"author":"agent:claude-sawlang","body":"LEAD BISECT (Sep 23): pre-existing — the repro (p[0] = Atomic(0); p[0].store(5); load; compare_exchange(5, 9); over GlobalAllocator().alloc storage) prints load 0 / cas false / after 0 identically on main 455dae1d (post-SL-350) and on 73dd98b8 (pre-SL-350). SL-350's receiver-load removal in calls.py is NOT in the causal chain; the mechanism is _receiver_storage_pointer's missing ArrayIndex case (a pointer-index receiver spilled by value), as filed. Member of the must-fix epic SL-367 at order 1, ahead of SL-355 which it gates. The obligation-4 sweep in the description is the fix's test plan; add the &var self shapes and the coroutine-transform re-lowering path to it explicitly. Lead repro: the Studio's .build/scratch/sl368_repro.saw.","created":"1790193106","id":"c2"}],"created":"1790192960","description":"Found by the SL-355 implementer (it blocks SL-355 unit 3, the ruled Atomic wake word). I reproduced it on main 8d2c0563.\n\n**Repro.** `.build/scratch/atomic_ptr_index.saw`, with `p: UnsafePointer<Atomic<Int>>` from `GlobalAllocator().alloc`:\n```saw\np[0].store(5)\nprint(p[0].load())             // prints 0, not 5\np[0].compare_exchange(5, 9)    // false; the value stays 0\n```\n\n**Probed positions:**\n| Receiver | Result |\n|---|---|\n| `p[0].store(v)` (direct pointer-index receiver) | WRONG: the store is lost |\n| `pc[0].n.store(7)` (field of a pointer-indexed struct) | correct, reads back 7 |\n| a static `COUNTER.store(3)` | correct |\n\n**Mechanism (as the implementer named it; confirm before fixing).** `_receiver_storage_pointer` in codegen/calls.py resolves a receiver to its storage address for `&self` and `&var self` calls, but has no `ArrayIndex` case. A pointer-index receiver therefore falls back to evaluating the element by value into a temporary, and the method runs on that temporary. A plain `&self` read on a trivial type gives the right answer from a copy, which hides the bug. Any interior-mutability type (Atomic; any cell-carrying type, since design 186) silently loses its write.\n\n**Sweep owed (obligation 4).** Every receiver shape that reaches this helper and its siblings:\n- `p[i]` over UnsafePointer and UnsafeMutablePointer;\n- a `Vector` place `v[i]` and a `Data` place;\n- a `borrows` accessor place;\n- a tuple index;\n- `&var self` methods on the same shapes (does a mutating method also lose its write through `p[0].m()`?);\n- SpinLock and Mutex, whose lock word is a cell;\n- the coroutine-transform path, if receivers are re-lowered there.\n\n**Pin.** The table above as EXPECT rows, plus the SL-355 shape (a CAS through `p[0]` from two threads).\n\nThis blocks SL-355 unit 3 (ruling R5, the atomic CAS latch).","id":"SL-368","labels":["frozen","hazard","soundness"],"order":1,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":7,"sequence":2230,"stage":"backlog","status":"open","title":"A &self method called directly on a pointer-index receiver (p[0].store(v) with p: UnsafePointer<Atomic<Int>>) runs on a spilled copy: the Atomic store is lost and the CAS acts on stale data","updated":"1790341684"},"before":{"acceptance":[],"assignee":"agent:claude-air-sawlang","author":"agent:claude-air-sawlang","closed":"","comments":[{"author":"agent:claude-air-sawlang","body":"USER RULING (Sep 23, revised): SL-368 lands in the SAME patch as SL-355 (and SL-354). It is built on the sl355-park-word branch as its own commit(s), ahead of SL-355 unit 3. One patch, one gate. The patch goes on SL-355; this issue closes with that landing.","created":"1790193037","id":"c1"},{"author":"agent:claude-sawlang","body":"LEAD BISECT (Sep 23): pre-existing — the repro (p[0] = Atomic(0); p[0].store(5); load; compare_exchange(5, 9); over GlobalAllocator().alloc storage) prints load 0 / cas false / after 0 identically on main 455dae1d (post-SL-350) and on 73dd98b8 (pre-SL-350). SL-350's receiver-load removal in calls.py is NOT in the causal chain; the mechanism is _receiver_storage_pointer's missing ArrayIndex case (a pointer-index receiver spilled by value), as filed. Member of the must-fix epic SL-367 at order 1, ahead of SL-355 which it gates. The obligation-4 sweep in the description is the fix's test plan; add the &var self shapes and the coroutine-transform re-lowering path to it explicitly. Lead repro: the Studio's .build/scratch/sl368_repro.saw.","created":"1790193106","id":"c2"}],"created":"1790192960","description":"Found by the SL-355 implementer (it blocks SL-355 unit 3, the ruled Atomic wake word). I reproduced it on main 8d2c0563.\n\n**Repro.** `.build/scratch/atomic_ptr_index.saw`, with `p: UnsafePointer<Atomic<Int>>` from `GlobalAllocator().alloc`:\n```saw\np[0].store(5)\nprint(p[0].load())             // prints 0, not 5\np[0].compare_exchange(5, 9)    // false; the value stays 0\n```\n\n**Probed positions:**\n| Receiver | Result |\n|---|---|\n| `p[0].store(v)` (direct pointer-index receiver) | WRONG: the store is lost |\n| `pc[0].n.store(7)` (field of a pointer-indexed struct) | correct, reads back 7 |\n| a static `COUNTER.store(3)` | correct |\n\n**Mechanism (as the implementer named it; confirm before fixing).** `_receiver_storage_pointer` in codegen/calls.py resolves a receiver to its storage address for `&self` and `&var self` calls, but has no `ArrayIndex` case. A pointer-index receiver therefore falls back to evaluating the element by value into a temporary, and the method runs on that temporary. A plain `&self` read on a trivial type gives the right answer from a copy, which hides the bug. Any interior-mutability type (Atomic; any cell-carrying type, since design 186) silently loses its write.\n\n**Sweep owed (obligation 4).** Every receiver shape that reaches this helper and its siblings:\n- `p[i]` over UnsafePointer and UnsafeMutablePointer;\n- a `Vector` place `v[i]` and a `Data` place;\n- a `borrows` accessor place;\n- a tuple index;\n- `&var self` methods on the same shapes (does a mutating method also lose its write through `p[0].m()`?);\n- SpinLock and Mutex, whose lock word is a cell;\n- the coroutine-transform path, if receivers are re-lowered there.\n\n**Pin.** The table above as EXPECT rows, plus the SL-355 shape (a CAS through `p[0]` from two threads).\n\nThis blocks SL-355 unit 3 (ruling R5, the atomic CAS latch).","id":"SL-368","labels":["bug","codegen","soundness"],"order":1,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":6,"sequence":1348,"stage":"backlog","status":"open","title":"A &self method called directly on a pointer-index receiver (p[0].store(v) with p: UnsafePointer<Atomic<Int>>) runs on a spilled copy: the Atomic store is lost and the CAS acts on stale data","updated":"1790193106"},"id":"SL-368","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-368\",\"labels\":[\"frozen\",\"hazard\",\"soundness\"],\"request_id\":\"pid-57145-1790341684-6000\",\"revision\":6}","request_id":"pid-57145-1790341684-6000","sequence":2230,"timestamp":"1790341684"}
---
