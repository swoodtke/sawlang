---
{"action":"patch_add","actor":"agent:claude-air-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-air-sawlang","closed":"","comments":[{"author":"agent:claude-air-sawlang","body":"Built on branch sl370-unsigned-compound-div (commit 6fa95cda): _apply_compound_op picks udiv/urem when the target is unsigned. That one funnel covers every position. The pin examples/uint_compound_division_signedness.saw covers UInt8/16/32/64 and UInt in local, field, index, optional-chain and &var positions, plus a signed control row. Unfixed main is wrong on every unsigned row; the branch is right on all of them. Related tests pass (16/16). The full terminal battery is running under the lock; the patch goes up once it's green. This is the compound twin of the earlier binary-operator fix (examples/uint_division_signedness.saw); that fix missed this funnel.","created":"1790197122","id":"c1"},{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (lead, Sep 23), order 9: a silent wrong answer on accepted code belongs with the soundness set; the user re-sequences at will. Fix in flight on the Air's branch (chat m103).","created":"1790197146","id":"c2"},{"author":"agent:claude-air-sawlang","body":"Submitted patch SL-370.p1 (revision 1): Unsigned compound /= and %= divide unsigned (udiv/urem in _apply_compound_op)","created":"1790209099","id":"c3"}],"created":"1790196968","description":"Found by an SL-357 wave-3 comment agent on Sep 23; I reproduced it on main (.build/scratch/unsigned_compound.saw).\n\n| Operation | Result | Correct |\n|---|---|---|\n| `var a: UInt8 = 200; a /= 3` | 238 | 66 |\n| `c %= 7` (c: UInt8 = 200) | 0 | 4 |\n| `UInt64 18446744073709551600 /= 2` | 18446744073709551608 | 9223372036854775800 |\n\nThe binary `/` and `%` operators give the right answers (operators.py picks udiv/urem).\n\n**Mechanism:** `codegen/statements.py _apply_compound_op(op, left, right, signed)` uses `signed` only to decide whether to emit the INT_MIN/-1 overflow check, then emits `sdiv`/`srem` unconditionally. Every compound-assignment position funnels through this one function:\n- the local, field, index and deref forms in statements.py (5 call sites);\n- the optional-chain `x?.f /= y` path in optionals.py.\n\nSo the class is these two branches of one funnel. The other compound ops already respect `signed`: `+`/`-`/`*` go through `_checked_arith`, shifts through `_emit_shift`, and bitwise ops are sign-agnostic.\n\n**Fix:** udiv/urem when `not signed`, mirroring operators.py.\n\n**Pins:** `/=` and `%=` for UInt8, UInt16, UInt32, UInt64 and UInt, each in local, struct-field, Vector-index and optional-chain position; a signed control row; divide-by-zero still panics.\n\nSilent wrong code, so high priority. Fix-on-discovery; the implementer is claude-air-sawlang.","id":"SL-370","labels":["bug","codegen","miscompile"],"order":9,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":6,"sequence":1399,"stage":"backlog","status":"open","title":"Unsigned compound division miscompiles: x /= y and x %= y emit sdiv/srem for unsigned types (UInt8 200 /= 3 gives 238; binary / is correct)","updated":"1790209099"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-air-sawlang","closed":"","comments":[{"author":"agent:claude-air-sawlang","body":"Built on branch sl370-unsigned-compound-div (commit 6fa95cda): _apply_compound_op picks udiv/urem when the target is unsigned. That one funnel covers every position. The pin examples/uint_compound_division_signedness.saw covers UInt8/16/32/64 and UInt in local, field, index, optional-chain and &var positions, plus a signed control row. Unfixed main is wrong on every unsigned row; the branch is right on all of them. Related tests pass (16/16). The full terminal battery is running under the lock; the patch goes up once it's green. This is the compound twin of the earlier binary-operator fix (examples/uint_division_signedness.saw); that fix missed this funnel.","created":"1790197122","id":"c1"},{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (lead, Sep 23), order 9: a silent wrong answer on accepted code belongs with the soundness set; the user re-sequences at will. Fix in flight on the Air's branch (chat m103).","created":"1790197146","id":"c2"}],"created":"1790196968","description":"Found by an SL-357 wave-3 comment agent on Sep 23; I reproduced it on main (.build/scratch/unsigned_compound.saw).\n\n| Operation | Result | Correct |\n|---|---|---|\n| `var a: UInt8 = 200; a /= 3` | 238 | 66 |\n| `c %= 7` (c: UInt8 = 200) | 0 | 4 |\n| `UInt64 18446744073709551600 /= 2` | 18446744073709551608 | 9223372036854775800 |\n\nThe binary `/` and `%` operators give the right answers (operators.py picks udiv/urem).\n\n**Mechanism:** `codegen/statements.py _apply_compound_op(op, left, right, signed)` uses `signed` only to decide whether to emit the INT_MIN/-1 overflow check, then emits `sdiv`/`srem` unconditionally. Every compound-assignment position funnels through this one function:\n- the local, field, index and deref forms in statements.py (5 call sites);\n- the optional-chain `x?.f /= y` path in optionals.py.\n\nSo the class is these two branches of one funnel. The other compound ops already respect `signed`: `+`/`-`/`*` go through `_checked_arith`, shifts through `_emit_shift`, and bitwise ops are sign-agnostic.\n\n**Fix:** udiv/urem when `not signed`, mirroring operators.py.\n\n**Pins:** `/=` and `%=` for UInt8, UInt16, UInt32, UInt64 and UInt, each in local, struct-field, Vector-index and optional-chain position; a signed control row; divide-by-zero still panics.\n\nSilent wrong code, so high priority. Fix-on-discovery; the implementer is claude-air-sawlang.","id":"SL-370","labels":["bug","codegen","miscompile"],"order":9,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":5,"sequence":1363,"stage":"backlog","status":"open","title":"Unsigned compound division miscompiles: x /= y and x %= y emit sdiv/srem for unsigned types (UInt8 200 /= 3 gives 238; binary / is correct)","updated":"1790197146"},"id":"SL-370","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-air-sawlang\",\"diff\":\"diff --git a/examples/uint_compound_division_signedness.saw b/examples/uint_compound_division_signedness.saw\\nnew file mode 100644\\nindex 00000000..30ee13d4\\n--- /dev/null\\n+++ b/examples/uint_compound_division_signedness.saw\\n@@ -0,0 +1,89 @@\\n+// Compound `/=` and `%=` on an unsigned target divide unsigned, in every\\n+// position a compound assignment reaches (SL-370). The binary operator's twin\\n+// is uint_division_signedness.saw. Each value has the high bit set, so a signed\\n+// divide gives a different answer.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// 66 4\\n+// 21845 1\\n+// 1431655765 3\\n+// 9223372036854775800 0\\n+// 5000000000000000000 0\\n+// field 66 4\\n+// index 66 4\\n+// chain 66 4\\n+// ref 66 4\\n+// signed -66 -4\\n+\\n+struct Cell { v: UInt8 }\\n+\\n+struct Holder { cell: Cell? }\\n+\\n+func divide_in_place(n: &var UInt8) {\\n+    n /= 3\\n+}\\n+\\n+func remainder_in_place(n: &var UInt8) {\\n+    n %= 7\\n+}\\n+\\n+func main() {\\n+    var a8: UInt8 = 200\\n+    var m8: UInt8 = 200\\n+    a8 /= 3\\n+    m8 %= 7\\n+    print(\\\"{} {}\\\", a8, m8)\\n+\\n+    var a16: UInt16 = 65535\\n+    var m16: UInt16 = 65535\\n+    a16 /= 3\\n+    m16 %= 7\\n+    print(\\\"{} {}\\\", a16, m16)\\n+\\n+    var a32: UInt32 = 4294967295\\n+    var m32: UInt32 = 4294967295\\n+    a32 /= 3\\n+    m32 %= 7\\n+    print(\\\"{} {}\\\", a32, m32)\\n+\\n+    var a64: UInt64 = 18446744073709551600\\n+    var m64: UInt64 = 18446744073709551600\\n+    a64 /= 2\\n+    m64 %= 2\\n+    print(\\\"{} {}\\\", a64, m64)\\n+\\n+    var au: UInt = 10000000000000000000\\n+    var mu: UInt = 10000000000000000000\\n+    au /= 2\\n+    mu %= 2\\n+    print(\\\"{} {}\\\", au, mu)\\n+\\n+    var c = Cell(v: 200)\\n+    c.v /= 3\\n+    var d = Cell(v: 200)\\n+    d.v %= 7\\n+    print(\\\"field {} {}\\\", c.v, d.v)\\n+\\n+    var xs: Vector<UInt8> = [200, 200]\\n+    xs[0] /= 3\\n+    xs[1] %= 7\\n+    print(\\\"index {} {}\\\", xs[0], xs[1])\\n+\\n+    var h = Holder(cell: Cell(v: 200))\\n+    h.cell?.v /= 3\\n+    var k = Holder(cell: Cell(v: 200))\\n+    k.cell?.v %= 7\\n+    print(\\\"chain {} {}\\\", h.cell!.v, k.cell!.v)\\n+\\n+    var r: UInt8 = 200\\n+    divide_in_place(&var r)\\n+    var s: UInt8 = 200\\n+    remainder_in_place(&var s)\\n+    print(\\\"ref {} {}\\\", r, s)\\n+\\n+    var n: Int = -200\\n+    var q: Int = -200\\n+    n /= 3\\n+    q %= 7\\n+    print(\\\"signed {} {}\\\", n, q)\\n+}\\ndiff --git a/sawc/codegen/statements.py b/sawc/codegen/statements.py\\nindex d16a0996..4c274fba 100644\\n--- a/sawc/codegen/statements.py\\n+++ b/sawc/codegen/statements.py\\n@@ -1144,15 +1144,19 @@ class StatementsMixin:\\n         elif op == '/':\\n             if is_float:\\n                 return self.builder.fdiv(left, right, name=\\\"divtmp\\\")\\n+            # The same signed/unsigned split as the binary operator: sdiv with\\n+            # the INT_MIN / -1 check, or udiv for an unsigned target (SL-370).\\n             self._check_divisor_nonzero(right)\\n             if signed:\\n                 self._check_div_no_overflow(left, right)\\n-            return self.builder.sdiv(left, right, name=\\\"divtmp\\\")\\n+                return self.builder.sdiv(left, right, name=\\\"divtmp\\\")\\n+            return self.builder.udiv(left, right, name=\\\"udivtmp\\\")\\n         elif op == '%':\\n             self._check_divisor_nonzero(right)\\n             if signed:\\n                 self._check_div_no_overflow(left, right)\\n-            return self.builder.srem(left, right, name=\\\"modtmp\\\")\\n+                return self.builder.srem(left, right, name=\\\"modtmp\\\")\\n+            return self.builder.urem(left, right, name=\\\"umodtmp\\\")\\n         elif op in ('&', '|', '^'):\\n             # Bitwise compound assignment (design 50): `x &= y` is `x = x & y`.\\n             return self._emit_bitwise(op, left, right)\\n\",\"diff_bytes\":3558,\"diff_files\":2,\"diff_hunks\":2,\"diff_tail\":\"x & y`.\\n             return self._emit_bitwise(op, left, right)\\n\",\"id\":\"SL-370\",\"request_id\":\"pid-47976-1790209095-10000\",\"title\":\"Unsigned compound /= and %= divide unsigned (udiv/urem in _apply_compound_op)\"}","request_id":"pid-47976-1790209095-10000","sequence":1399,"timestamp":"1790209099"}
---
