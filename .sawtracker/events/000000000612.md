---
{"action":"comment","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl211","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sl211","body":"UNIT B COMPLETE on branch `worktree-agent-af5361be8244c3ca4` (2 commits, linear on origin/main after SL-238 merged under it). NOT submitted — the lead squashes and submits.\n\nBrief: `designs/269-forwarding-taxonomy.md` (number provisional; 268 landed while this ran, so 269 is the next free slot).\n\n## The mechanism, not the three symptoms (obligation 4)\n\n`_is_aliasing_expr` answered the producer question as a hand-maintained node-type list, and its answer gates EVERY tier arm of `_check_value_transfer` — so a missing member converts an aliasing read into a fresh temporary at every tier at once (NoCopy/ExplicitCopy skip their refusal, `Copy` skips its retain). Eight findings in ten months are that one shape: design 131, design 139, DF-216a, DF-299a, DF-288a (which had to hand-add `SelfExpr` to a SECOND list asking the same question), and the three closed here.\n\nThe classification is now a TOTAL function in `sawc/typechecker/producers.py` over six kinds — reads / projects / rewraps / branches / builds / own-arm — and `tools/test_producer_taxonomy.py` fails the build when a value-position node is in no bucket. Both walks that must see through the same nodes (`_is_aliasing_expr`, the ledger's `_transfer_source_identity`) are on the one funnel and the gate checks that too.\n\nTWO transparencies, not one, and collapsing them would be wrong: PROJECTS hands back a PART of the operand's storage (the node's own type transfers — `try r` yields `Res`, not `Result<Res, Bad>`), REWRAPS hands back the SAME value under a wider type (the operand's transfer is the one to judge, at the operand's type, which is where the author's `move` goes). The old code had one relation and used it for the first case only.\n\n## The universe was wrong, and the gate found it\n\nDesign 267's census enumerated the 45 `Expression` subclasses. That under-counts: a `Statement` carrying a `result_type` sits in a value position too. Exactly one does — `ForLoop` (`let n = for i in 0..3 { … }`) — and it surfaced as an ICE within one suite run of the funnel landing. The gate enumerates `Expression` subclasses UNION everything with a `result_type`: 46 classes, all classified.\n\nSecond census gap, probed rather than assumed: `NilCoalesce` appears in NO bucket of design 267's census. It is benign — `let a = x ?? Res(w: 0)` over a NoCopy binding was already refused by the payload rule, because both sub-positions are judged at the `??` node's own check — and it is now `BUILDS` with the reason written down.\n\n## The three fixes\n\n- SL-218 — `SelfExpr` joins READS. Six faces measured double-freeing at exit 0 before, all six refused now.\n- SL-219 — `TryExpr` joins PROJECTS. It is the one node in two buckets by design: its Ok value projects out of the subject, its catch handler is a branch arm (which is exactly what the bug was — DF-299b judged the arm, the comment beside it said the subject \"is that expression's question\", nothing asked it). All five spellings refused.\n- SL-79 — the four auto-wraps join REWRAPS and are PEELED at the checkpoint, ahead of the branch recursion so the two compose. `ErasedErrWrap` — the fourth wrap, which the filing predicted and nobody had measured — double freed at exit 0 on a field source; now refused.\n\n## THE RULING (SL-79 carried it)\n\nDECIDED: the wrap is TRANSPARENT. `func opt(r: Res) -> Res? { r }` is refused too, so one `?` no longer changes whether a `move` is written. Three reasons, weightiest first: (1) the alternative does not close the bug — an implicit-move reading leaves every ALIASING source double-freeing and would need the aliasing test anyway; (2) it deletes a spelling difference nobody could explain; (3) one rule at one chokepoint is auditable.\n\nMEASURED MIGRATION COST: ONE LINE, across sawc/std, blade, libs, devtools and the whole corpus — `sawc/std/json.saw:1316`, `return v` -> `return move v`. The shape the ruling worried about is essentially unwritten in practice, which is the strongest argument for taking it.\n\n## Diagnostics (three changed, each because the old wording named a dead end)\n\n- a `self` source said \"use `move`\" — a borrowed receiver has nothing to move. Now names `consumes` (design 260) or a duplicable policy + `self.copy()`, which is the wording V86's pin asked for.\n- a `try` source now spells `try move r`, the `move` on the SUBJECT, because that is what owns the payload (probed: it compiles and releases once). A FIELD subject gets the other half, since `move h.r` is the no-partial-moves refusal.\n- an auto-wrapped tail now anchors on the TAIL EXPRESSION rather than the function's declaration line — the diagnostic wart dogfood wave 1 recorded, fixed incidentally for the wrapped case.\n\n## Structural edit\n\nEvery exit of `_check_value_transfer` returns through a local `decide(...)` recorder, so no arm can forget the sub-decisions a two-source node has already filed. `tools/test_transfer_decisions.py` was taught about it AND made to prove `decide` itself files through `_decide_transfer` — accepting it without that would have been a hole the size of design 267's property.\n\n## Conformance (obligation 3)\n\nV86/V87 (unit A's rows-first pins) lose their XFAIL markers. V88 (the wrap matrix — 4 wrap kinds x 4 return targets + the ruling row), V89 (the Copy-tier duplicate-exactly-once guard, oracle = a hand-written `copy()` hook printing, so implicit duplications are counted directly), V90 (the narrow-fence control, 9 values / 9 drops / order pinned) added. `examples/forwarding_transfer_drops_once_driven.saw` is the sync+driven twin — 6 values, 6 drops, the two halves agreeing position by position. INDEX rows + the rows-ahead-of-their-fix section updated.\n\n## Gates\n\nFull `tools/battery.sh`: 26 of 26 stages GREEN in 2938s. Suite 2478 passed / 9 cited xfails / 0 failed. Freestanding 36 passed across riscv32 + arm64. New `producertaxonomy` lane added to the battery.\n\n## One corpus test fixture changed\n\n`examples/errors/vector_get_nocopy_alias.saw` had its own unrelated ownership mistake (`return e` at `-> Item?`). The new refusal fires in the TYPE CHECK, ahead of the place lowering, so the pin reported its own mistake instead of the design-146 place rule it exists to pin. Fixed to `return move e`; the pin's EXPECT is unchanged and it now reports exactly the message it names.\n","created":"1789152254","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-211.p1 (revision 1): design 269: the producer question becomes a total classification (SL-211 unit B) — closes SL-79/218/219; gated 46-class taxonomy replaces _is_aliasing_expr","created":"1789152468","id":"c2"},{"author":"agent:claude-sawlang","body":"SL-211.p1 SUBMITTED (unit B, tip e095d15b), server gate queued — LEAD-VALIDATED. _is_aliasing_expr replaced by a TOTAL producer classification (sawc/typechecker/producers.py, six kinds over 46 node classes — the enumeration corrected design 267's universe: a Statement carrying result_type sits in value position too, ForLoop surfaced as an ICE in one suite run) with a battery-gated enumeration lane (producertaxonomy) that fails the build on any unclassified value-position node. Closes SL-79 (auto-wrap transparent — REWRAPS peel at the checkpoint), SL-218 (SelfExpr joins READS), SL-219 (TryExpr joins PROJECTS). Conformance: V86/V87 markers flipped, V88/V89/V90 added + sync/driven twin. SL-220 probed and correctly left for unit D (producer answer was never wrong; the missing piece is cleanup registration). Lead independently probed W1 (aliasing source into auto-wrap: was silent double free, now clean refusal) and W6 (owned source: now demands move — the ruling's one visible change) on the branch. REVIEWER FOCUS: (1) the decide() recorder routing in _check_value_transfer — wide but mechanical, the transferdecisions gate was taught to prove it files through _decide_transfer; (2) the SL-79 transparency ruling carries a ONE-LINE corpus migration (std/json.saw return move v) — the lead has flagged the semantics change to the USER for ack before merge; (3) three reworded diagnostics. Gates: suite 2478/9x, freestanding 36/36, battery 26/26 (incl. the new lane). Docs updated per convention (LANGUAGE_SPEC + saw-lang skill).","created":"1789152481","id":"c3"}],"created":"1788966179","description":"Unit B of SL-209 (ownership uniformity). Plan step 4. Folds in and CLOSES SL-79 (DF-305a). Depends on Unit A.\n\n## Handle forwarding and control flow structurally\nBuild on `_value_branch_arm_results` (`sawc/typechecker/types.py:4327`), distinguishing three kinds of expression:\n- FORWARDS an existing value: block tails, branches, appropriate casts and wrappers.\n- CREATES a new value.\n- EXPOSES borrowed storage.\n\nRules:\n- Forwarding must preserve the source's ownership obligations. Adding parentheses, a block, or an Optional/Result wrapper must NOT turn an alias into a fresh temporary.\n- Keep ownership state PATH-SENSITIVE. Both branch arms start from the same incoming state; their outgoing states merge according to reachable paths. Checking one arm must not make the other appear to move an already-moved value. Diverging paths contribute no fall-through state.\n\n## Folds in SL-79 (DF-305a — \"check ownership before Optional/Result return auto-wraps\")\nThe Optional/Result return auto-wrap is exactly a FORWARDING position: today the wrap can happen without the ownership check the plain return receives. Route it through the same forwarding taxonomy so ownership is checked BEFORE wrapping, same-module and imported, at argument/return/field positions. Landing this unit closes SL-79.\n\n## Acceptance\nEquivalent expression spellings agree; moves remain legal on mutually exclusive paths; use after a possible move is rejected; the auto-wrap-before-check hole (SL-79) is closed with SL-79's own minimal repro promoted to a regression test.\n","id":"SL-211","labels":["compiler","ownership"],"priority":"high","project":"SL","revision":6,"sequence":612,"stage":"queued","status":"open","title":"Ownership Unit B: forwarding & control-flow taxonomy; closes SL-79 (SL-209)","updated":"1789152481"},"before":{"assignee":"agent:claude-sl211","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sl211","body":"UNIT B COMPLETE on branch `worktree-agent-af5361be8244c3ca4` (2 commits, linear on origin/main after SL-238 merged under it). NOT submitted — the lead squashes and submits.\n\nBrief: `designs/269-forwarding-taxonomy.md` (number provisional; 268 landed while this ran, so 269 is the next free slot).\n\n## The mechanism, not the three symptoms (obligation 4)\n\n`_is_aliasing_expr` answered the producer question as a hand-maintained node-type list, and its answer gates EVERY tier arm of `_check_value_transfer` — so a missing member converts an aliasing read into a fresh temporary at every tier at once (NoCopy/ExplicitCopy skip their refusal, `Copy` skips its retain). Eight findings in ten months are that one shape: design 131, design 139, DF-216a, DF-299a, DF-288a (which had to hand-add `SelfExpr` to a SECOND list asking the same question), and the three closed here.\n\nThe classification is now a TOTAL function in `sawc/typechecker/producers.py` over six kinds — reads / projects / rewraps / branches / builds / own-arm — and `tools/test_producer_taxonomy.py` fails the build when a value-position node is in no bucket. Both walks that must see through the same nodes (`_is_aliasing_expr`, the ledger's `_transfer_source_identity`) are on the one funnel and the gate checks that too.\n\nTWO transparencies, not one, and collapsing them would be wrong: PROJECTS hands back a PART of the operand's storage (the node's own type transfers — `try r` yields `Res`, not `Result<Res, Bad>`), REWRAPS hands back the SAME value under a wider type (the operand's transfer is the one to judge, at the operand's type, which is where the author's `move` goes). The old code had one relation and used it for the first case only.\n\n## The universe was wrong, and the gate found it\n\nDesign 267's census enumerated the 45 `Expression` subclasses. That under-counts: a `Statement` carrying a `result_type` sits in a value position too. Exactly one does — `ForLoop` (`let n = for i in 0..3 { … }`) — and it surfaced as an ICE within one suite run of the funnel landing. The gate enumerates `Expression` subclasses UNION everything with a `result_type`: 46 classes, all classified.\n\nSecond census gap, probed rather than assumed: `NilCoalesce` appears in NO bucket of design 267's census. It is benign — `let a = x ?? Res(w: 0)` over a NoCopy binding was already refused by the payload rule, because both sub-positions are judged at the `??` node's own check — and it is now `BUILDS` with the reason written down.\n\n## The three fixes\n\n- SL-218 — `SelfExpr` joins READS. Six faces measured double-freeing at exit 0 before, all six refused now.\n- SL-219 — `TryExpr` joins PROJECTS. It is the one node in two buckets by design: its Ok value projects out of the subject, its catch handler is a branch arm (which is exactly what the bug was — DF-299b judged the arm, the comment beside it said the subject \"is that expression's question\", nothing asked it). All five spellings refused.\n- SL-79 — the four auto-wraps join REWRAPS and are PEELED at the checkpoint, ahead of the branch recursion so the two compose. `ErasedErrWrap` — the fourth wrap, which the filing predicted and nobody had measured — double freed at exit 0 on a field source; now refused.\n\n## THE RULING (SL-79 carried it)\n\nDECIDED: the wrap is TRANSPARENT. `func opt(r: Res) -> Res? { r }` is refused too, so one `?` no longer changes whether a `move` is written. Three reasons, weightiest first: (1) the alternative does not close the bug — an implicit-move reading leaves every ALIASING source double-freeing and would need the aliasing test anyway; (2) it deletes a spelling difference nobody could explain; (3) one rule at one chokepoint is auditable.\n\nMEASURED MIGRATION COST: ONE LINE, across sawc/std, blade, libs, devtools and the whole corpus — `sawc/std/json.saw:1316`, `return v` -> `return move v`. The shape the ruling worried about is essentially unwritten in practice, which is the strongest argument for taking it.\n\n## Diagnostics (three changed, each because the old wording named a dead end)\n\n- a `self` source said \"use `move`\" — a borrowed receiver has nothing to move. Now names `consumes` (design 260) or a duplicable policy + `self.copy()`, which is the wording V86's pin asked for.\n- a `try` source now spells `try move r`, the `move` on the SUBJECT, because that is what owns the payload (probed: it compiles and releases once). A FIELD subject gets the other half, since `move h.r` is the no-partial-moves refusal.\n- an auto-wrapped tail now anchors on the TAIL EXPRESSION rather than the function's declaration line — the diagnostic wart dogfood wave 1 recorded, fixed incidentally for the wrapped case.\n\n## Structural edit\n\nEvery exit of `_check_value_transfer` returns through a local `decide(...)` recorder, so no arm can forget the sub-decisions a two-source node has already filed. `tools/test_transfer_decisions.py` was taught about it AND made to prove `decide` itself files through `_decide_transfer` — accepting it without that would have been a hole the size of design 267's property.\n\n## Conformance (obligation 3)\n\nV86/V87 (unit A's rows-first pins) lose their XFAIL markers. V88 (the wrap matrix — 4 wrap kinds x 4 return targets + the ruling row), V89 (the Copy-tier duplicate-exactly-once guard, oracle = a hand-written `copy()` hook printing, so implicit duplications are counted directly), V90 (the narrow-fence control, 9 values / 9 drops / order pinned) added. `examples/forwarding_transfer_drops_once_driven.saw` is the sync+driven twin — 6 values, 6 drops, the two halves agreeing position by position. INDEX rows + the rows-ahead-of-their-fix section updated.\n\n## Gates\n\nFull `tools/battery.sh`: 26 of 26 stages GREEN in 2938s. Suite 2478 passed / 9 cited xfails / 0 failed. Freestanding 36 passed across riscv32 + arm64. New `producertaxonomy` lane added to the battery.\n\n## One corpus test fixture changed\n\n`examples/errors/vector_get_nocopy_alias.saw` had its own unrelated ownership mistake (`return e` at `-> Item?`). The new refusal fires in the TYPE CHECK, ahead of the place lowering, so the pin reported its own mistake instead of the design-146 place rule it exists to pin. Fixed to `return move e`; the pin's EXPECT is unchanged and it now reports exactly the message it names.\n","created":"1789152254","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-211.p1 (revision 1): design 269: the producer question becomes a total classification (SL-211 unit B) — closes SL-79/218/219; gated 46-class taxonomy replaces _is_aliasing_expr","created":"1789152468","id":"c2"}],"created":"1788966179","description":"Unit B of SL-209 (ownership uniformity). Plan step 4. Folds in and CLOSES SL-79 (DF-305a). Depends on Unit A.\n\n## Handle forwarding and control flow structurally\nBuild on `_value_branch_arm_results` (`sawc/typechecker/types.py:4327`), distinguishing three kinds of expression:\n- FORWARDS an existing value: block tails, branches, appropriate casts and wrappers.\n- CREATES a new value.\n- EXPOSES borrowed storage.\n\nRules:\n- Forwarding must preserve the source's ownership obligations. Adding parentheses, a block, or an Optional/Result wrapper must NOT turn an alias into a fresh temporary.\n- Keep ownership state PATH-SENSITIVE. Both branch arms start from the same incoming state; their outgoing states merge according to reachable paths. Checking one arm must not make the other appear to move an already-moved value. Diverging paths contribute no fall-through state.\n\n## Folds in SL-79 (DF-305a — \"check ownership before Optional/Result return auto-wraps\")\nThe Optional/Result return auto-wrap is exactly a FORWARDING position: today the wrap can happen without the ownership check the plain return receives. Route it through the same forwarding taxonomy so ownership is checked BEFORE wrapping, same-module and imported, at argument/return/field positions. Landing this unit closes SL-79.\n\n## Acceptance\nEquivalent expression spellings agree; moves remain legal on mutually exclusive paths; use after a possible move is rejected; the auto-wrap-before-check hole (SL-79) is closed with SL-79's own minimal repro promoted to a regression test.\n","id":"SL-211","labels":["compiler","ownership"],"priority":"high","project":"SL","revision":5,"sequence":611,"stage":"queued","status":"open","title":"Ownership Unit B: forwarding & control-flow taxonomy; closes SL-79 (SL-209)","updated":"1789152468"},"id":"SL-211","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sawlang\",\"body\":\"SL-211.p1 SUBMITTED (unit B, tip e095d15b), server gate queued — LEAD-VALIDATED. _is_aliasing_expr replaced by a TOTAL producer classification (sawc/typechecker/producers.py, six kinds over 46 node classes — the enumeration corrected design 267's universe: a Statement carrying result_type sits in value position too, ForLoop surfaced as an ICE in one suite run) with a battery-gated enumeration lane (producertaxonomy) that fails the build on any unclassified value-position node. Closes SL-79 (auto-wrap transparent — REWRAPS peel at the checkpoint), SL-218 (SelfExpr joins READS), SL-219 (TryExpr joins PROJECTS). Conformance: V86/V87 markers flipped, V88/V89/V90 added + sync/driven twin. SL-220 probed and correctly left for unit D (producer answer was never wrong; the missing piece is cleanup registration). Lead independently probed W1 (aliasing source into auto-wrap: was silent double free, now clean refusal) and W6 (owned source: now demands move — the ruling's one visible change) on the branch. REVIEWER FOCUS: (1) the decide() recorder routing in _check_value_transfer — wide but mechanical, the transferdecisions gate was taught to prove it files through _decide_transfer; (2) the SL-79 transparency ruling carries a ONE-LINE corpus migration (std/json.saw return move v) — the lead has flagged the semantics change to the USER for ack before merge; (3) three reworded diagnostics. Gates: suite 2478/9x, freestanding 36/36, battery 26/26 (incl. the new lane). Docs updated per convention (LANGUAGE_SPEC + saw-lang skill).\",\"id\":\"SL-211\",\"request_id\":\"pid-19489-1789152481-15000\"}","request_id":"pid-19489-1789152481-15000","sequence":612,"timestamp":"1789152481"}
---
