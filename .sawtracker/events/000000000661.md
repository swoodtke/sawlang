---
{"action":"create","actor":"agent:claude-sl265","after":{"assignee":"","author":"agent:claude-sl265","comments":[],"created":"1789180523","description":"## Description\n\nFound by SL-265's obligation-4 sweep (design 271). Independent of that fix —\nthis is a REFUSAL, not a miscompile, and it is unchanged by design 271.\n\nThe `Set` element gate and the `Map` KEY gate do not know about the automatic\nCopy tier, and say so in words that contradict the tier:\n\n```saw\nstruct Rec { text: String }        // design 159: automatic Copy tier,\n                                   // declares nothing, owes nothing\n\nfunc main() {\n    var s = Set<Rec>()             // error\n    var m = Map<Rec, Int>()        // error\n}\n```\n\n```\nerror: set element type `Rec` must be copyable (trivial, Copy, or ExplicitCopy\nwith retain semantics): `Rec` owns a Deinit without a copy (it is move-only,\nnot retainable)\n```\n\n`Rec` is neither move-only nor un-retainable: it is on the Copy tier, `let b = a`\nis a free retain leaving both live, and design 219 has it satisfying both\n`T: Copy` and `T: ExplicitCopy` (conformance row V32 pins exactly that).\n\nThe inconsistency that makes it a bug rather than a policy: the `Map` VALUE\nposition accepts the same type. `Map<String, Rec>` compiles and works (it is\none of SL-265's own probes), so one container's key gate and its value gate\ndisagree about one tier.\n\n## Suspected mechanism\n\nThe gate reads a DECLARED conformance list rather than `copy_tier`, so a type\nthat earns its tier by derivation answers no. The wording (\"owns a Deinit\nwithout a copy\") is the tell: an automatic-tier struct does own a synthesized\ndeinit, and the gate concludes move-only from that alone.\n\nWorth a sweep for other predicates phrased the same way before a fix is\ndispatched — design 271 found the same DECLARED-vs-DERIVED confusion in three\nseparate copy-emission chains, so this vocabulary has form.\n","id":"SL-270","labels":["bug","compiler"],"priority":"normal","project":"SL","revision":1,"sequence":661,"stage":"backlog","status":"open","title":"Set element and Map key gates call the automatic Copy tier move-only; the Map value gate accepts it","updated":"1789180523"},"before":null,"id":"SL-270","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl265\",\"description\":\"## Description\\n\\nFound by SL-265's obligation-4 sweep (design 271). Independent of that fix —\\nthis is a REFUSAL, not a miscompile, and it is unchanged by design 271.\\n\\nThe `Set` element gate and the `Map` KEY gate do not know about the automatic\\nCopy tier, and say so in words that contradict the tier:\\n\\n```saw\\nstruct Rec { text: String }        // design 159: automatic Copy tier,\\n                                   // declares nothing, owes nothing\\n\\nfunc main() {\\n    var s = Set<Rec>()             // error\\n    var m = Map<Rec, Int>()        // error\\n}\\n```\\n\\n```\\nerror: set element type `Rec` must be copyable (trivial, Copy, or ExplicitCopy\\nwith retain semantics): `Rec` owns a Deinit without a copy (it is move-only,\\nnot retainable)\\n```\\n\\n`Rec` is neither move-only nor un-retainable: it is on the Copy tier, `let b = a`\\nis a free retain leaving both live, and design 219 has it satisfying both\\n`T: Copy` and `T: ExplicitCopy` (conformance row V32 pins exactly that).\\n\\nThe inconsistency that makes it a bug rather than a policy: the `Map` VALUE\\nposition accepts the same type. `Map<String, Rec>` compiles and works (it is\\none of SL-265's own probes), so one container's key gate and its value gate\\ndisagree about one tier.\\n\\n## Suspected mechanism\\n\\nThe gate reads a DECLARED conformance list rather than `copy_tier`, so a type\\nthat earns its tier by derivation answers no. The wording (\\\"owns a Deinit\\nwithout a copy\\\") is the tell: an automatic-tier struct does own a synthesized\\ndeinit, and the gate concludes move-only from that alone.\\n\\nWorth a sweep for other predicates phrased the same way before a fix is\\ndispatched — design 271 found the same DECLARED-vs-DERIVED confusion in three\\nseparate copy-emission chains, so this vocabulary has form.\\n\",\"labels\":[\"bug\",\"compiler\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"pid-9960-1789180523-9000\",\"stage\":\"backlog\",\"title\":\"Set element and Map key gates call the automatic Copy tier move-only; the Map value gate accepts it\"}","request_id":"pid-9960-1789180523-9000","sequence":661,"timestamp":"1789180523"}
---
