---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791241","description":"## Scope and status\n\nDF-186c — OPEN (two language gaps, one C body). The Linux half of the one-word lock (__saw_rt_lock_acquire/_release) is a futex, and it lives in rt/shim.c rather…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 7308–7318\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7308-L7318)\n\n- **DF‑186c — OPEN (two language gaps, one C body).** The Linux half of the\n  one-word lock (`__saw_rt_lock_acquire`/`_release`) is a futex, and it lives in\n  `rt/shim.c` rather than in `rt/host_linux/` because a futex needs two things\n  Saw has not got: ATOMICS ON A 32-BIT WORD REACHED THROUGH A POINTER\n  (`Atomic<T>` is `Atomic<Int>` in v1, and has no spelling for \"atomically\n  operate on this pointee\"), and a VARIADIC extern (glibc's is `long syscall(long,\n  ...)`, the same DF‑113c gap `fcntl` sits in). Either feature shrinks the body\n  back to Saw. The macOS half IS Saw (`rt/host_macos/lock.saw`, two\n  `os_unfair_lock` calls). **The Linux half is UNEXECUTED**: this host is macOS,\n  so it is reviewed C, not tested C — the first Linux run of the suite is what\n  proves it, and `mutex_static_contention_mt` is the test that would catch it.\n","id":"SL-106","labels":["todo-import","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":106,"status":"open","title":"DF-186c — OPEN (two language gaps, one C body). The Linux half of the one-word lock (__saw_rt_lock_acquire/_release) is a futex, and it lives in rt/shim.c rather…","updated":"1788791241"},"before":null,"id":"SL-106","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-186c — OPEN (two language gaps, one C body). The Linux half of the one-word lock (__saw_rt_lock_acquire/_release) is a futex, and it lives in rt/shim.c rather…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 7308–7318\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7308-L7318)\\n\\n- **DF‑186c — OPEN (two language gaps, one C body).** The Linux half of the\\n  one-word lock (`__saw_rt_lock_acquire`/`_release`) is a futex, and it lives in\\n  `rt/shim.c` rather than in `rt/host_linux/` because a futex needs two things\\n  Saw has not got: ATOMICS ON A 32-BIT WORD REACHED THROUGH A POINTER\\n  (`Atomic<T>` is `Atomic<Int>` in v1, and has no spelling for \\\"atomically\\n  operate on this pointee\\\"), and a VARIADIC extern (glibc's is `long syscall(long,\\n  ...)`, the same DF‑113c gap `fcntl` sits in). Either feature shrinks the body\\n  back to Saw. The macOS half IS Saw (`rt/host_macos/lock.saw`, two\\n  `os_unfair_lock` calls). **The Linux half is UNEXECUTED**: this host is macOS,\\n  so it is reviewed C, not tested C — the first Linux run of the suite is what\\n  proves it, and `mutex_static_contention_mt` is the test that would catch it.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-657c7607e8079d0cc5ff-create\",\"title\":\"DF-186c — OPEN (two language gaps, one C body). The Linux half of the one-word lock (__saw_rt_lock_acquire/_release) is a futex, and it lives in rt/shim.c rather…\"}","request_id":"todo-20260907-657c7607e8079d0cc5ff-create","sequence":106,"timestamp":"1788791241"}
---
