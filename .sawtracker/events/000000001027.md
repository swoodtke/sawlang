---
{"action":"patch_review","actor":"human:shawn","after":{"assignee":"agent:claude-sl318u4","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-318.p1 (revision 1): Design 275 (RULED): coroutine transform discovery uniformity — the brief for epic SL-318","created":"1789738898","id":"c1"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-318.p1 (revision 1)","created":"1789742631","id":"c2"},{"author":"agent:claude-sawlang","body":"Revised patch SL-318.p1 to revision 2","created":"1789757787","id":"c3"},{"author":"human:shawn","body":"Approved patch SL-318.p1 (revision 2)","created":"1789757803","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-318.p1 (revision 2)","created":"1789758289","id":"c5"},{"author":"agent:claude-sawlang","body":"Merged patch SL-318.p1 (revision 2) as 5c4dae22","created":"1789758314","id":"c6"},{"author":"agent:claude-sawlang","body":"LANDED (lead, Sep 18): SL-318.p1 r2 merged as 5c4dae22 (user approved r2; server gate green) — design 275 is on main (`designs/275-coro-discovery-uniformity.md`, todo.md entry, INDEX line). r2 carries the user's re-sequencing: the epic dispatches RIGHT AFTER SL-274.p1 lands, AHEAD of the small-fixes batch (SL-264/270/271/276). Epic stays OPEN as the unit carrier; unit order U4 → U1 → U2 → U3 (SL-215 then SL-317), one Opus agent per unit in its own worktree. Rulings still owed on §3 (neither blocks U4/U1): (1) SL-317 borrowed-place iterator across a split — lead recommends refuse in v1, frame-owned only; (2) K37 existential dispatch of a suspending conformance body — lead recommends permanent refusal.\n","created":"1789758341","id":"c7"},{"author":"agent:claude-sawlang","body":"USER RULINGS on design 275 §3 (Sep 18):\n\n1. SL-317 / SL-321 — SL-321 (the `for x in v.iter()` use-after-free: the std Vector iterator IS an untracked borrow of the vector) lands INSIDE U3, not as a standalone fix. The iterator becomes a tracked borrow of the collection for the loop's window, and that borrow SPANS the suspension: design 88's driven-in-place rule (held references are sound by task confinement; spawned frames keep stripping them) extends to the lent place. The refuse-in-v1 recommendation is WITHDRAWN — it would have refused the very loop U3 exists to compile (sawtracker store.saw:285). U3's matrix gains the SL-321 cells: a body mutation of the iterated collection is the clean exclusivity error inside the window, in the sync loop and across the suspension alike; a conformance row for \"no use-after-free in safe code\".\n\n2. K37 — PENDING A DYNAMIC-FRAME DESIGN, not permanent. Existential dispatch of a suspending conformance body and suspending RECURSION (SL-287, SL-304 option 2) both need a heap-allocated frame with an indirect resume, so they land together in one design; carrier issue SL-322 filed. U2's shape table records both as REFUSE pending SL-322, the refusal naming it.\n\nBrief §3/§4 updated by docs patch on this issue.\n","created":"1789767352","id":"c8"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-318.p2 (revision 1): design 275: section-3 rulings recorded — SL-321 inside U3 (borrow spans the suspension), K37 pending heap-frame design SL-322","created":"1789767393","id":"c9"},{"author":"agent:claude-sawlang","body":"Revised patch SL-318.p2 to revision 2","created":"1789767890","id":"c10"},{"author":"agent:claude-sawlang","body":"DESIGN REVIEW (codex, relayed by the user Sep 18) — five amendments, all ACCEPTED by the lead and folded into the brief (SL-318.p2 r2):\n\n1. U4: a three-valued answer loses information consumers need. CONFIRMED against effects.py — there are FOUR walkers today (`suspends`, `really_suspending`, `suspends_ignoring_blocking`, `suspends_ignoring_closure_calls`) because four consumers need four subsets of the causes: `__saw_suspend` is a frame boundary that must not wrap `main`; a `Thread.spawn` body permits `blocking` and refuses cooperative. U4 now computes ONE SET of causes per node {cooperative, blocking, test_suspend, closure_call} to a fixpoint and derives NAMED context decisions (might_suspend, wraps_main, refused_in_thread_body, frame_boundary, closure_only) with consumers in their docstrings; a new subset is a new row, never a private combination. The running U4 agent has been re-briefed.\n\n2. U1: explicit construction + freeze contract — one authoritative analysis via a WORKLIST to stabilization (imported bodies and generic instances reveal callees), discovery finishes before lowering, the ledger freezes, a miss is an invariant failure (never a plain call), and callee FACTS (`FrameDecision`) are separate from call-site DECISIONS (`SiteDecision` with a context) so one callee can be EMBED direct and REFUSE in a closure.\n\n3. U2: totality is checked AFTER lowering too — every reachable suspension site owns a `SiteDecision` (a new AST shape fails coverage until classified), and a post-lowering walk asserts no frame-boundary call survives as a plain call in any resume body.\n\n4. U3: a borrow-lifetime contract is owed by the U3 brief before dispatch — window begin/end on every exit route (exhaustion, break, return, error, cancellation, destruction), ADDRESS STABILITY for a frame-owned referent given design 44's by-value embedding and spawn's frame move, and the external-storage (`&var` param / enclosing field) case distinguished from frame-owned in the matrix.\n\n5. U1 validation claim corrected: irdet/reemit prove repeatability, not equivalence. Replaced with an explicit old-vs-new comparison — a `--emit-frame-ledger` dump diffed between a pinned pre-U1 compiler and the branch over corpus + Blade + sawtracker, optimized-IR byte-compare of a main-checkout runner manifest against the branch's, and the SL-274/SL-306 review adversarial cases promoted to permanent example gates.\n","created":"1789767891","id":"c11"},{"author":"agent:codex","body":"Approved patch SL-318.p2 (revision 2)\n\nApproved revision 2: the amendment matches all five expectations from my design review.\n\n1. U4 records the full cause set in one SCC-safe fixpoint, with named derived decisions preserving the distinctions needed by main wrapping, Thread.spawn, author sync contexts, and frame boundaries (including __saw_suspend). Std seeds carry the set too.\n2. U1 specifies worklist convergence before lowering, an immutable frozen ledger, invariant failures for missing entries, and separate callee facts versus context-specific SiteDecisions.\n3. U2 adds exhaustive site coverage and a post-lowering check for unlowered frame-boundary calls, supplementing the source-level bypass audit.\n4. U3 explicitly owes its detailed borrow-lifetime contract before dispatch: exit/error/cancellation/destruction paths, address stability and frame movement, and external versus frame-owned referents. This is an implementation-unit prerequisite, not a reason to delay the foundation units.\n5. U1 validation now compares old and new decision dumps and IR/outcomes, correctly distinguishing equivalence testing from irdet/reemit repeatability. Prior adversarial review cases become permanent gates.\n\nThe amendment also preserves the user's rulings: SL-321 belongs inside U3, and existential suspension/recursive frames are refused pending SL-322 rather than permanently excluded. No blocking design findings remain. This is approval of the design contract, not validation of its future implementation.\n\nValidation: reviewed the complete docs-only diff against the landed design and the five review points; no compiler changes or local compiler test run are involved. Server patch-test status at review time: pending. Merge remains subject to the server gate.\n","created":"1789768107","id":"c12"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-318.p2 (revision 2)","created":"1789768456","id":"c13"},{"author":"agent:claude-sl318u4","body":"# U4 BUILT — one suspension analysis, a CAUSE SET per node (branch `sl318u4`, 2 commits, not submitted)\n\nBuilt on the AMENDED contract. What the amendment reshaped: the three-valued\nanswer became a per-node cause SET with five named derivations; `SuspendClass` /\n`SuspendAnswer` became `SuspendCause` + module-level derivations;\n`SuspendNode.suspension` became `SuspendNode.causes`; `really` / `might` /\n`needs_executor` / `beyond_blocking` became `frame_boundary` / `might_suspend` /\n`wraps_main` / `refused_in_thread_body`; the std seed table gained the cause set\n(which turned out to FIX a cell — §3d); the conformance rows were restated as\ncause × context; and the corpus-wide walker-equivalence probe is new. What\ncarried over unchanged from the three-valued draft: the single set-union pass,\nthe deletion of all four walkers, and the consumer routing.\n\nCommits: `7dbd81da` (the unit), `cd0462fc` (the SL-326 pin).\n\n## 1. The chokepoint and its named readers\n\n`sawc/typechecker/effects.py::classify_suspensions(nodes) -> SuspensionAnswers`\n— ONE monotone, SCC-safe set-union pass. Per node: which CAUSES can a suspension\nreach it by. `_source_class` is the one label -> cause mapping, total over the\nlabels `_effect_direct_source` produces:\n\n    COOPERATIVE | BLOCKING | TEST_SUSPEND | EXISTENTIAL_DISPATCH | CLOSURE_CALL\n                                                               | UNCLASSIFIED\n\nAll four walkers are DELETED (`finalize_effects`' `suspends` fixpoint,\n`really_suspending`, `suspends_ignoring_blocking`,\n`suspends_ignoring_closure_calls`). Each is now one of five module-level\nderivations, each docstring naming its consumers — that table IS the funnel's\nnamed-entries list:\n\n| derivation | causes | consumers |\n|---|---|---|\n| `might_suspend` | any | the `sync` check, `SuspendNode.suspends` (5 node-holding readers), the std `might` censuses |\n| `wraps_main` | cooperative, blocking | `_main_suspends`, the std seed GATE, design 223's existential refusal |\n| `refused_in_thread_body` | all but blocking | design 242 ruling 9's narrowing |\n| `frame_boundary` | all but closure_call | the frame-method `sync` narrowing, the method census, the closure-walk edge-follow, `instantiation_suspends`, the std framing census |\n| `closure_only` | closure_call alone | none today — the diagnostics row, so nobody spells `might and not frame_boundary` at a site of their own |\n\n`SuspendNode.causes` is stamped by `finalize_effects` from that pass;\n`SuspendNode.suspends` is a derived PROPERTY over it, never assigned, so the\nbroad bit and the set cannot disagree. `SuspendNode.seeded_causes` carries the\nstd seed's set beside its one representative diagnostic label.\n\nWalks per compile: the entry graph went from up to four full walks per settling,\nplus one whole-graph walk per `_promote_nested_generic_calls` call (once per body\nJOINING the driven closure), to ONE per settling and ONE for the whole transform.\n\n## 2. Consumer census — every site that reads a suspension answer\n\n| site | value it needs | read before | reads now |\n|---|---|---|---|\n| `effects.finalize_effects` sync check | might | `node.suspends` (fixpoint) | `node.suspends` (derived) |\n| `effects.finalize_effects` `_main_suspends` | executor | `really_suspending` | `answers.wraps_main` |\n| `effects.finalize_effects` blocking narrowing | thread body | `suspends_ignoring_blocking` | `answers.refused_in_thread_body` |\n| `effects.finalize_effects` frame-method narrowing | framing | `suspends_ignoring_closure_calls` | `answers.frame_boundary` |\n| `effects._report_existential_suspend_dispatch` | executor | `really_suspending` | `answers.wraps_main` |\n| `effects._effect_seed_std_methods` | (producer) | 3-tuple table | 4-tuple + `seeded_causes` |\n| `effects._effect_path` blocking skip | (source class) | label prefix | `_source_class(s) & BLOCKING` |\n| `consumes._check_consumes_suspending_fences` | **wants framing**, reads might | `node.suspends` | `node.suspends` — SL-325 |\n| `sawc` `_std_suspending_methods` | might | `node.suspends` | `std_answers.might_suspend` |\n| `sawc` `_std_really_suspending_methods` | executor | `really_suspending` | `std_answers.wraps_main` + `causes` |\n| `sawc` `_std_suspending_methods_ignoring_closure_calls` | framing | `suspends_ignoring_closure_calls` | `std_answers.frame_boundary` |\n| `sawc` `_std_suspending_functions` (docs) | might | `node.suspends` | `std_answers.might_suspend` |\n| `sawc` main-wrap check (:1941) | executor | `_main_suspends` | unchanged (reads the flag) |\n| `coro._find_suspending_cycle` | might | `t.suspends` | `t.suspends` (derived; `is_built` narrows it — §3c) |\n| `coro._suspending_method_target` broad gate | might | `_suspending_methods_set` | unchanged (set now built from `might_suspend`) |\n| `coro._suspending_method_target` own gate | framing | `_own_suspending_methods_set` | unchanged (set now built from `frame_boundary`) |\n| `coro._default_expr_suspends` | **wants framing**, reads might | `n.suspends` | `n.suspends` — SL-324 |\n| `coro.transform_program` method census | might + framing | TWO walks | ONE table, two derivations |\n| `coro.transform_program` `_answered_locally` | might | `node.suspends` | `_answers.might_suspend` |\n| `coro` closure-walk edge-follow | framing | `_own_susp` dict | `_answers.frame_boundary` |\n| `coro._promote_nested_generic_calls` | framing | its OWN walk, per call | the table, handed down |\n| `docs_emit` (2 sites) | might | the std name sets | unchanged |\n\nNo consumer keeps a private derivation; the only cause-bit combinations in the\ncompiler are the five derivations' own definitions.\n\n## 3. The disagreement sweep (obligation 4) — 4 findings, none absorbed\n\n**(a) SL-323 — `existential_dispatch` is a FIFTH cause nobody classified.**\ndesign 223 unit 3's ``a call through `any Trait` dispatch`` is conservative (a\nvtable word carries no effect; design 223 refuses the dispatch outright when a\nconformance body really suspends), and the two deleted predicates disagreed about\nit and always had: design 206's struck it, SL-306's did not. The transform frames\non SL-306's, so a body whose ONLY cause is a dispatch joins the driven closure —\nand design 88's fence then refuses its erased `&any Trait` PARAMETER for a frame\nit never needed. Probe (main and U4, identical):\n`error: coroutine transform: g: &any Greeter cannot be a parameter of the\nsuspending function via_existential`, on a program with no suspension in\n`via_existential` at all. U4 gives the cause its own bit and KEEPS it inside\n`frame_boundary` (behaviour-preserving); the membership is recorded in that\nderivation's docstring, filed, and pinned XFAIL\n(`examples/coro_existential_only_body_is_not_framed.saw`).\n\n**(b) SL-324 / SL-325 — two consumers read `might` where the refusal they write\nis about a FRAME.** `_default_expr_suspends` (a suspending parameter DEFAULT) and\ndesign 260's two consuming-body fences. Both refuse a body whose only cause is\n`closure_call` — which is framed nowhere — with a message about a coroutine\nframe. SL-325's hint is the tell: it tells the author to make the method `sync`,\nwhich the same conservative cause also refuses. Probes on main and U4 are\nidentical. Both fixes are one word each once the ledger owns the reading; both\nare strictly widening, so they are U1/U2's to make, not this unit's.\n\n**(c) One site that reads `might` and is CORRECT.** `_find_suspending_cycle`\nfollows `t.suspends`, but `_analyze_nesting` passes `is_built`, which narrows the\nwalk to frames the closure walk actually builds — probed with a\nconservative-only mutual recursion under a driven root: compiles and runs. The\nmechanism admits no sibling there, which is why it is recorded rather than filed.\n\n**(d) FIXED BY THE UNIFICATION — the std seed's one label.** The seed minted a\nleaf carrying ONE representative source label where the std method has several\ncauses, so every derivation that EXCLUDES a cause was answered by whichever label\n`_effect_path` happened to pick. `Command.output` reaches BOTH a cooperative park\n(it parks on the child's exit) and a blocking offload (the pipe drain), was\nseeded under the blocking label, and `Thread.spawn { c.output() }` was therefore\nPERMITTED — a cooperative park on a thread with no executor, design 242 ruling\n9's exact hazard. The seed carries the cause SET now and the answer flips to\nrefused. No corpus program reaches it (no `Thread.spawn` body in the tree touches\nio or a child), so nothing in the suite changes.\n\n**THE WALKER-EQUIVALENCE PROBE** (the amendment's bar), `.build/scratch/probe_walker_equivalence.py`\n— the four old walkers re-implemented from main's source, over `node.direct`\nlabels, compared against the new derived reads at EVERY node of EVERY graph:\n\n    examples/ + errors/ + conformance/: 2499 of 2499 programs,\n      2,778,002 node comparisons (four walkers each), 7014 mismatches\n    blade/src/main.saw: 7542 comparisons, 12 mismatches\n    libs/toml/src/lib.saw: 1503 comparisons, 3 mismatches\n\nEvery mismatch is finding (d) and nothing else. Classified distribution over the\n331 suspension-heavy programs (1780 of the 7014): ONE pair\n(`ignoring_blocking` vs `refused_in_thread_body`), one cause set\n({cooperative, blocking}), seven node names, all of them `Command.output` or a\nbody that calls it. The mechanism bounds it: `seeded_causes` is the ONLY input\nthat differs between the old walkers and the new derivations, and it is nonzero\nonly on a seeded std leaf.\n\n## 4. Conformance rows (obligation 3) — cause × context, K111-K115\n\nOne row per CAUSE, each stating that cause's answer in all four contexts (sync\nbody, thread body, main, framed body), with a covering file per cell.\n\n- **K111** `cooperative` — new `K111_suspension_causes_decide_framing.saw` (the\n  framed-body column for every cause × every callee shape: free function,\n  method, generic instance, imported public + module-private helper, the test\n  intrinsic, and a `no`-cause control, with EXPECT-IR-CONTAINS/ABSENT per cell),\n  + `errors/copy_policy_hook_suspends_in_sync_fn.saw` + K83 + `coro_conservative_free_callee_not_framed.saw`.\n- **K112** `blocking` — `errors/sync_blocking_extern.saw` + K84's two files +\n  `expr_suspend_blocking.saw`; the row records finding (d).\n- **K113** `test_suspend` — new\n  `K113_test_suspend_is_a_frame_boundary_not_an_executor.saw` (framed at the\n  drive root, `__Frame_main` ABSENT for the same intrinsic in `main`) +\n  `errors/sync_direct_suspend.saw` + K114 + K111.\n- **K114** `closure_call` — new\n  `K114_thread_body_refuses_every_cause_but_blocking.saw` (the two thread-body\n  cells K83/K84 left unmeasured: `test_suspend` and `closure_call`) +\n  `errors/sync_context_calls_a_closure_taking_helper.saw` + K111 + K105's files.\n- **K115** `existential_dispatch` — `errors/any_sync_suspend.saw` + the SL-323\n  XFAIL pin.\n\nINDEX header recounted: **536 rows, 281 carry a file here, 255 covered\nelsewhere** (was 531/278/253). The pre-existing duplicate K53 row untouched.\n\n## 5. Gates\n\n- **Per-commit gate** and **TERMINAL battery**: `tools/battery.sh` full,\n  **28 stages GREEN in 3409s**, including `suite` (**2565 passed, 12 xfailed**,\n  zero XPASS, zero uncited), `reemit`, `irdet --all`, `gmgate`, `bootstrap`,\n  `corodiff`, and `freestanding` (**36 passed across riscv32 + arm64**). Log:\n  `.build/scratch/battery.log`. Run under the machine-wide lock\n  (`/private/tmp/claude-501/saw-suite-lock`), acquired and released around it.\n- The 12 xfails are the 10 pre-existing ones plus my two cited pins (SL-323,\n  SL-326). `citations`: 13 citations, 12 xfail + 1 ledger, 0 stale.\n\n## 6. Adversarial self-check — and a fifth finding\n\nProbe set beyond the tests, compiled AND run, `.build/scratch/`:\n\n- **two-module + same-name**: `adv_a`/`adv_b` declaring the same free-function\n  names (`worker`, `_shared_name`, `through_generic`) at OPPOSITE causes, and\n  `adv_c`/`adv_d` declaring the same really-suspending generic `warp`.\n- **generic-via-helper**, **closure-caller**, **imported public vs\n  module-private**: all in `K111` with per-cell IR assertions — `__Frame_really_free`,\n  `__Frame_boundary`, `__Frame_Widget_tick`, `__Frame_hop$m$$1$Int`,\n  `__Frame_yield_public$m$sl306mod`, `__Frame_yield_via_private$m$sl306mod` and\n  `__Frame__yield_private$m$sl306mod` PRESENT; `__Frame_closure_only_free`,\n  `__Frame_plain_free`, `__Frame_Widget_fold`, `__Frame_pass`,\n  `__Frame_fold_public`, `__Frame__fold_private` ABSENT. Identical frame set on\n  main and on U4.\n- **IR byte-compare, main vs U4**: 27 programs (the SL-280 rows, the SL-306\n  matrix, the design-44 nested-suspend rows, the json/module rows, the blocking\n  row) — `MISMATCHES: none`.\n\n**SL-326 (filed high) — the finding the adversarial set turned up.** A suspending\nGENERIC free call in an EXPRESSION position inside a driven body is hoisted by\ndesign 120's ANF and never PROMOTED, so the instantiation has no frame and the\nhoisted call is a plain call to a suspending body inside the synthesized resume:\n``cannot suspend in `sync func` method: method `__Frame_main.resume` calls\n`warp$1$Int` → yield_now``. The statement-level `let` cell is the control and\ncompiles. Position matrix probed: interpolation, argument and binary operand all\nfail; entry-module and imported alike; qualifier and selective-import spellings\nalike. Mechanism: `_promote_nested_generic_calls` walks the positions design 74\nshape 3 was written against while design 120/224 widened the HOIST's positions —\ndiscovery and lowering disagreeing on a position set, which is design 275's own\ndiagnosis one site over, and U2's shape table is the artifact that catches it.\nPre-existing on main; pinned XFAIL as\n`examples/coro_generic_call_in_an_expression_position.saw`.\n\n## 7. Out of scope, untouched\n\nNo ledger, no shape table, no new splits; the state machine, the seven split\nroutines, the drive loop, the executor and design 44's frame embedding are\nunchanged. SL-215 / SL-317 / SL-287 / SL-316 not touched and their pins did not\nflip. Nothing closed in the tracker; `designs/todo.md`'s design-275 entry marked\nin place.\n\n## 8. Filed this unit\n\nSL-323 (existential_dispatch framed), SL-324 (suspending parameter default),\nSL-325 (design 260's consuming fences), SL-326 (generic call in an expression\nposition, high).\n","created":"1789774386","id":"c14"},{"author":"agent:claude-sl318u4","body":"# U4 addendum — the two review fixes, regated (final tip `672d20dc`)\n\n## FIX 1 — the placeholder, and its provenance\n\nThe refusal printed ``closure calls <suspension source>``. Provenance matches\nthe lead's: NOT general to seeded leaves and NOT pre-existing. A seeded std leaf\ncarried ONE representative source label; the thread-body refusal STRIKES the\nblocking cause (design 242 ruling 9 permits it), so for a method whose\nrepresentative label is the blocking one and whose refusal is satisfied by the\nCOOPERATIVE cause — `Command.output` — the path walk found nothing left to name\nat the leaf and fell to `_effect_path`'s robustness fallback. `Channel.receive`\n(one cause) renders correctly on main and on the branch alike.\n\nFixed where the seed is minted: `build_builtin_namespace` walks the non-blocking\npath too (the builtin graph is the only place that path exists) and the leaf\ncarries a source PER CAUSE, **representative first**. Consequences, all probed:\n\n- striking reader: ``closure calls `Command.output` → io_wait (`Command.output`\n  suspends at line 494)`` — the method AND the cause that decided it;\n- non-striking reader, byte-identical to main: ``cannot suspend in `sync func`\n  declaration: `sync func run_it` calls `Command.output` → blocking extern\n  `__saw_rt_proc_read_stdout` ``;\n- single-cause control, byte-identical to main: ``closure calls\n  `Channel.receive` → __saw_chan_park``.\n\nTwo names moved into `effects.py` so nobody re-derives them: `cause_of_label`\n(the one label→cause map, now asked by the seed instead of a private prefix\ntest) and `PATH_PLACEHOLDER_LABEL` (so a caller BUILDING a table tests for the\nfallback instead of shipping it into a message).\n\n**The pin moved to its own file** — `examples/conformance/K112_thread_body_refuses_a_multi_cause_std_method.saw`,\nwith `EXPECT-ERROR-CONTAINS: closure calls \\`Command.output\\` → io_wait` and\n`EXPECT-ERROR-ABSENT: <suspension source>`. It cannot be a third cell in K114,\nand why is a finding: **SL-327** (filed low). The seed table's node ids do not\nmatch the entry graph's edges at the FIRST settling (`62190` vs `20761`,\ndumped), so this refusal is only reached at a later one — and a compile stops\nafter a settling that reported errors, so beside another refused cell the\nmessage is never emitted. Pre-existing keying (design 206's), made visible by\nthe new refusal; recorded for U1, which takes the seeded-leaf population over.\nK112's INDEX row carries the whole story; header recounted 281/255 → **282 carry\na file here, 254 elsewhere** (536 rows unchanged).\n\n## FIX 2 — stale docstrings\n\n`coro_transform.py:85-90` and `:11328` and `:11342-11344` rewritten for the\ncause set and the named derivations (`frame_boundary` / `might_suspend`). Grepped\nthe branch for `three-valued`, `closure-only` and backticked `really`: the only\nsurvivor is `effects.py:168`, which names the REJECTED draft as the record of\nwhy the set exists. K111's cell labels and its printed strings now spell the\ncause `closure_call` rather than the old `closure-only`.\n\n## Gates at the final tip (`672d20dc`)\n\n`tools/battery.sh` full, under the machine-wide lock: **28 stages GREEN in\n3389s**. `suite` **2566 passed, 12 xfailed** (the 10 pre-existing + my two cited\npins; zero XPASS, zero uncited), `freestanding` **36 passed across riscv32 +\narm64** — that pair is the per-commit gate — plus `reemit`, `irdet --all`,\n`gmgate`, `bootstrap`, `corodiff`, `fuzz`, `citations`, `astgraft`. Log:\n`.build/scratch/battery2.log`.\n\nThree commits, linear, not submitted: `7dbd81da` (the unit), `cd0462fc` (the\nSL-326 pin), `672d20dc` (these two fixes).\n\nFiled by this unit: SL-323, SL-324, SL-325, SL-326, SL-327.\n","created":"1789778644","id":"c15"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-318.p3 (revision 1): design 275 U4: one suspension analysis — a cause set per node, four walkers deleted, five named derivations; std seed carries the set (Thread-body Command.output refused)","created":"1789778697","id":"c16"},{"author":"agent:claude-sawlang","body":"SUBMITTED SL-318.p3 (lead, Sep 19): design 275 U4 at tip 672d20dc (3 commits squashed), LEAD-VALIDATED. `effects.classify_suspensions` computes ONE cause set per node {cooperative, blocking, test_suspend, existential_dispatch, closure_call, unclassified} by one set-union fixpoint; the four walkers (`suspends`, `really_suspending`, `suspends_ignoring_blocking`, `suspends_ignoring_closure_calls`) are DELETED and their consumers read five named derivations (`might_suspend`, `wraps_main`, `refused_in_thread_body`, `frame_boundary`, `closure_only`) whose docstrings list their consumers; 22 consumer sites tabled. The std seed carries the SET per method, which fixed an unmeasured cell: `Thread.spawn { c.output() }` compiled and RAN on main (Command.output parks cooperatively on the child's exit; the seed's one representative label was the blocking one) and is refused now, naming the method and the primitive (K112, pinned with EXPECT-ERROR-CONTAINS + ABSENT `<suspension source>`). Conformance K111-K115 = cause × context. Findings filed, none absorbed: SL-323 (existential_dispatch is a cause nobody classified; kept in frame_boundary for behaviour preservation, XFAIL), SL-324/SL-325 (two `might` readers whose refusal is about a frame), SL-326 (HIGH, U2's cell: a suspending generic call in an EXPRESSION position is ANF-hoisted but never promoted → plain call in the resume; XFAIL pinned), SL-327 (seed node ids vs entry-graph edges at the first settling — U1 takes seeding over). Walker-equivalence probe: 2,778,002 node comparisons over 2499 programs + blade + libs; every mismatch is the seed fix. Agent gates at the tip: battery 28/28, suite 2566/12x, freestanding 36/36. Lead validation: 17 then 10 filtered rows pass; adversarial probe (same-named parks in two modules, generic via helper, closure-caller, blocking-only, test-suspend-only) frames exactly the expected set; seed probe main-compiles/branch-refuses with the method named; Channel.receive control byte-identical both sides. Behaviour-preserving for the transform: IR byte-compare main vs branch over the SL-280/SL-306/design-44 rows, no mismatches. Does NOT overlap SL-318.p2 (the docs patch touches only the brief; this touches code + todo.md + INDEX).\n","created":"1789778729","id":"c17"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-318.p3 (revision 1)","created":"1789779179","id":"c18"},{"author":"agent:codex","body":"Approved patch SL-318.p3 (revision 1)\n\nApproved revision 1. No blocking findings.\n\nThe U4 implementation meets the cause-set portion of the amended design: one SCC-safe set-union classifier replaces the separate effect walkers, named derivations preserve the distinctions between author sync checks, thread-body refusal, executor wrapping, and frame boundaries, and generic promotion shares the transform's analysis table. The std-method seed carries the full cause set rather than reducing it to the diagnostic representative. The additional non-blocking diagnostic source makes the newly correct Command.output thread-body refusal actionable.\n\nThe deliberate existential-dispatch framing compatibility is explicit and pinned by SL-323; the generic expression-position limitation is separately pinned by SL-326. Neither is introduced by this refactor. This approves U4, not the remaining ledger, totality, or borrow-lifetime units of SL-318.\n\nIndependent validation on an isolated checkout of base 40230e95 with the exact patch applied:\n- Focused test_runner selection: 18 passed, 2 expected failures (SL-323 and SL-326). Includes K111-K114, K83/K84, ordinary sync/blocking/test-intrinsic refusals, conservative free/method/generic framing and spawned-task regressions, existential refusal, copy-hook refusal, and blocking expression suspension. IR assertions and diagnostic presence/absence checks passed.\n- Independent scratch probe: all 64 cause subsets satisfy the five derivations; all six source classes map correctly; 2,400 node answers over 200 deterministic generated graphs match a separate reachability oracle, including cycles, mixed seeded/direct causes, and missing targets.\n- Inspected the consumer replacements and generic-promotion ordering; searched for remaining legacy walker calls and writes to the derived suspends property.\n\nServer patch tests: passed. Re-fetched and verified revision 1 and its exact diff immediately before recording this approval. No merge performed.\n","created":"1789821979","id":"c19"},{"author":"agent:claude-sawlang","body":"Merged patch SL-318.p3 (revision 1) as a2ef74e1","created":"1789833215","id":"c20"},{"author":"agent:claude-sawlang","body":"Patch SL-318.p2 needs rebase; the base branch moved since it applied. Run patch rebase, or submit a revised diff if it conflicts.","created":"1789833229","id":"c21"},{"author":"agent:claude-sawlang","body":"Revised patch SL-318.p2 to revision 3","created":"1789833267","id":"c22"},{"author":"human:shawn","body":"Approved patch SL-318.p2 (revision 3)","created":"1789833361","id":"c23"}],"created":"1789738775","description":"USER RULING (Sep 18): revisit the coroutine transform's discovery seams as an EPIC rather than continue per-site funnel fixes. Four units, all ruled as the lead recommended: (1) ONE DISCOVERY LEDGER — a single pass computes the closed frame table (key, declaration, really-suspends, buildable, reason) before any body is lowered; classifier, closure walk, generic promotion, rejectors and drive-site rewriting READ it and derive nothing; a battery lane fails on any transform site that reads the raw suspension bit or a symbol name directly (the producer-taxonomy / astgraft precedent). (2) TOTALITY — every statement and expression shape is either splittable or refused by ONE rejector reading ONE shape table; a callee the walk cannot frame is a compile error, never a plain call whose park blocks the executor (closes SL-287, SL-316; the K33-K37 XFAIL family are rows). (3) COVERAGE UNITS INSIDE THE EPIC — SL-215 (inline try/catch split) and SL-317 (iterator state in the frame for a collection for) are epic units, NOT standalone point fixes; sawtracker's build (currently stopped at those two) is the epic's acceptance test and WAITS for it — the user ruled a point fix is a waste of time. (4) A THREE-VALUED ANSWER AT THE EFFECT SOURCE — the graph answers once (really suspends / only through a closure call / does not) instead of one bit meaning might plus design 206's second question bolted beside it. Census (Sep 18): coro_transform.py 12,224 lines, a 212-method _FrameBuilder, a 1,032-line transform_program with nine nested closures, ~20 distinct suspension/frame-key predicates, 7 split shapes (if, if-let, guard-let, while, RANGE for, match, BLOCK try-catch), 21 scattered refusals, 17 governing designs (44..224). The lowering is stable; every finding this week (SL-280, SL-274 keying sites, SL-306 + its review, SL-215, SL-317, SL-287, SL-316) was a discovery seam. Brief: designs/275-coro-discovery-uniformity.md (lead, next). Sequenced after SL-306.p1 r2 + SL-274.p1 r2 land and the small-fixes batch; ledger unit first.","id":"SL-318","labels":["epic","coro","design-275"],"priority":"high","project":"SL","revision":25,"sequence":1027,"stage":"queued","status":"open","title":"EPIC: coroutine transform discovery uniformity — one frame ledger, total shape coverage, no silent decline (design 275)","updated":"1789833361"},"before":{"assignee":"agent:claude-sl318u4","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-318.p1 (revision 1): Design 275 (RULED): coroutine transform discovery uniformity — the brief for epic SL-318","created":"1789738898","id":"c1"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-318.p1 (revision 1)","created":"1789742631","id":"c2"},{"author":"agent:claude-sawlang","body":"Revised patch SL-318.p1 to revision 2","created":"1789757787","id":"c3"},{"author":"human:shawn","body":"Approved patch SL-318.p1 (revision 2)","created":"1789757803","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-318.p1 (revision 2)","created":"1789758289","id":"c5"},{"author":"agent:claude-sawlang","body":"Merged patch SL-318.p1 (revision 2) as 5c4dae22","created":"1789758314","id":"c6"},{"author":"agent:claude-sawlang","body":"LANDED (lead, Sep 18): SL-318.p1 r2 merged as 5c4dae22 (user approved r2; server gate green) — design 275 is on main (`designs/275-coro-discovery-uniformity.md`, todo.md entry, INDEX line). r2 carries the user's re-sequencing: the epic dispatches RIGHT AFTER SL-274.p1 lands, AHEAD of the small-fixes batch (SL-264/270/271/276). Epic stays OPEN as the unit carrier; unit order U4 → U1 → U2 → U3 (SL-215 then SL-317), one Opus agent per unit in its own worktree. Rulings still owed on §3 (neither blocks U4/U1): (1) SL-317 borrowed-place iterator across a split — lead recommends refuse in v1, frame-owned only; (2) K37 existential dispatch of a suspending conformance body — lead recommends permanent refusal.\n","created":"1789758341","id":"c7"},{"author":"agent:claude-sawlang","body":"USER RULINGS on design 275 §3 (Sep 18):\n\n1. SL-317 / SL-321 — SL-321 (the `for x in v.iter()` use-after-free: the std Vector iterator IS an untracked borrow of the vector) lands INSIDE U3, not as a standalone fix. The iterator becomes a tracked borrow of the collection for the loop's window, and that borrow SPANS the suspension: design 88's driven-in-place rule (held references are sound by task confinement; spawned frames keep stripping them) extends to the lent place. The refuse-in-v1 recommendation is WITHDRAWN — it would have refused the very loop U3 exists to compile (sawtracker store.saw:285). U3's matrix gains the SL-321 cells: a body mutation of the iterated collection is the clean exclusivity error inside the window, in the sync loop and across the suspension alike; a conformance row for \"no use-after-free in safe code\".\n\n2. K37 — PENDING A DYNAMIC-FRAME DESIGN, not permanent. Existential dispatch of a suspending conformance body and suspending RECURSION (SL-287, SL-304 option 2) both need a heap-allocated frame with an indirect resume, so they land together in one design; carrier issue SL-322 filed. U2's shape table records both as REFUSE pending SL-322, the refusal naming it.\n\nBrief §3/§4 updated by docs patch on this issue.\n","created":"1789767352","id":"c8"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-318.p2 (revision 1): design 275: section-3 rulings recorded — SL-321 inside U3 (borrow spans the suspension), K37 pending heap-frame design SL-322","created":"1789767393","id":"c9"},{"author":"agent:claude-sawlang","body":"Revised patch SL-318.p2 to revision 2","created":"1789767890","id":"c10"},{"author":"agent:claude-sawlang","body":"DESIGN REVIEW (codex, relayed by the user Sep 18) — five amendments, all ACCEPTED by the lead and folded into the brief (SL-318.p2 r2):\n\n1. U4: a three-valued answer loses information consumers need. CONFIRMED against effects.py — there are FOUR walkers today (`suspends`, `really_suspending`, `suspends_ignoring_blocking`, `suspends_ignoring_closure_calls`) because four consumers need four subsets of the causes: `__saw_suspend` is a frame boundary that must not wrap `main`; a `Thread.spawn` body permits `blocking` and refuses cooperative. U4 now computes ONE SET of causes per node {cooperative, blocking, test_suspend, closure_call} to a fixpoint and derives NAMED context decisions (might_suspend, wraps_main, refused_in_thread_body, frame_boundary, closure_only) with consumers in their docstrings; a new subset is a new row, never a private combination. The running U4 agent has been re-briefed.\n\n2. U1: explicit construction + freeze contract — one authoritative analysis via a WORKLIST to stabilization (imported bodies and generic instances reveal callees), discovery finishes before lowering, the ledger freezes, a miss is an invariant failure (never a plain call), and callee FACTS (`FrameDecision`) are separate from call-site DECISIONS (`SiteDecision` with a context) so one callee can be EMBED direct and REFUSE in a closure.\n\n3. U2: totality is checked AFTER lowering too — every reachable suspension site owns a `SiteDecision` (a new AST shape fails coverage until classified), and a post-lowering walk asserts no frame-boundary call survives as a plain call in any resume body.\n\n4. U3: a borrow-lifetime contract is owed by the U3 brief before dispatch — window begin/end on every exit route (exhaustion, break, return, error, cancellation, destruction), ADDRESS STABILITY for a frame-owned referent given design 44's by-value embedding and spawn's frame move, and the external-storage (`&var` param / enclosing field) case distinguished from frame-owned in the matrix.\n\n5. U1 validation claim corrected: irdet/reemit prove repeatability, not equivalence. Replaced with an explicit old-vs-new comparison — a `--emit-frame-ledger` dump diffed between a pinned pre-U1 compiler and the branch over corpus + Blade + sawtracker, optimized-IR byte-compare of a main-checkout runner manifest against the branch's, and the SL-274/SL-306 review adversarial cases promoted to permanent example gates.\n","created":"1789767891","id":"c11"},{"author":"agent:codex","body":"Approved patch SL-318.p2 (revision 2)\n\nApproved revision 2: the amendment matches all five expectations from my design review.\n\n1. U4 records the full cause set in one SCC-safe fixpoint, with named derived decisions preserving the distinctions needed by main wrapping, Thread.spawn, author sync contexts, and frame boundaries (including __saw_suspend). Std seeds carry the set too.\n2. U1 specifies worklist convergence before lowering, an immutable frozen ledger, invariant failures for missing entries, and separate callee facts versus context-specific SiteDecisions.\n3. U2 adds exhaustive site coverage and a post-lowering check for unlowered frame-boundary calls, supplementing the source-level bypass audit.\n4. U3 explicitly owes its detailed borrow-lifetime contract before dispatch: exit/error/cancellation/destruction paths, address stability and frame movement, and external versus frame-owned referents. This is an implementation-unit prerequisite, not a reason to delay the foundation units.\n5. U1 validation now compares old and new decision dumps and IR/outcomes, correctly distinguishing equivalence testing from irdet/reemit repeatability. Prior adversarial review cases become permanent gates.\n\nThe amendment also preserves the user's rulings: SL-321 belongs inside U3, and existential suspension/recursive frames are refused pending SL-322 rather than permanently excluded. No blocking design findings remain. This is approval of the design contract, not validation of its future implementation.\n\nValidation: reviewed the complete docs-only diff against the landed design and the five review points; no compiler changes or local compiler test run are involved. Server patch-test status at review time: pending. Merge remains subject to the server gate.\n","created":"1789768107","id":"c12"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-318.p2 (revision 2)","created":"1789768456","id":"c13"},{"author":"agent:claude-sl318u4","body":"# U4 BUILT — one suspension analysis, a CAUSE SET per node (branch `sl318u4`, 2 commits, not submitted)\n\nBuilt on the AMENDED contract. What the amendment reshaped: the three-valued\nanswer became a per-node cause SET with five named derivations; `SuspendClass` /\n`SuspendAnswer` became `SuspendCause` + module-level derivations;\n`SuspendNode.suspension` became `SuspendNode.causes`; `really` / `might` /\n`needs_executor` / `beyond_blocking` became `frame_boundary` / `might_suspend` /\n`wraps_main` / `refused_in_thread_body`; the std seed table gained the cause set\n(which turned out to FIX a cell — §3d); the conformance rows were restated as\ncause × context; and the corpus-wide walker-equivalence probe is new. What\ncarried over unchanged from the three-valued draft: the single set-union pass,\nthe deletion of all four walkers, and the consumer routing.\n\nCommits: `7dbd81da` (the unit), `cd0462fc` (the SL-326 pin).\n\n## 1. The chokepoint and its named readers\n\n`sawc/typechecker/effects.py::classify_suspensions(nodes) -> SuspensionAnswers`\n— ONE monotone, SCC-safe set-union pass. Per node: which CAUSES can a suspension\nreach it by. `_source_class` is the one label -> cause mapping, total over the\nlabels `_effect_direct_source` produces:\n\n    COOPERATIVE | BLOCKING | TEST_SUSPEND | EXISTENTIAL_DISPATCH | CLOSURE_CALL\n                                                               | UNCLASSIFIED\n\nAll four walkers are DELETED (`finalize_effects`' `suspends` fixpoint,\n`really_suspending`, `suspends_ignoring_blocking`,\n`suspends_ignoring_closure_calls`). Each is now one of five module-level\nderivations, each docstring naming its consumers — that table IS the funnel's\nnamed-entries list:\n\n| derivation | causes | consumers |\n|---|---|---|\n| `might_suspend` | any | the `sync` check, `SuspendNode.suspends` (5 node-holding readers), the std `might` censuses |\n| `wraps_main` | cooperative, blocking | `_main_suspends`, the std seed GATE, design 223's existential refusal |\n| `refused_in_thread_body` | all but blocking | design 242 ruling 9's narrowing |\n| `frame_boundary` | all but closure_call | the frame-method `sync` narrowing, the method census, the closure-walk edge-follow, `instantiation_suspends`, the std framing census |\n| `closure_only` | closure_call alone | none today — the diagnostics row, so nobody spells `might and not frame_boundary` at a site of their own |\n\n`SuspendNode.causes` is stamped by `finalize_effects` from that pass;\n`SuspendNode.suspends` is a derived PROPERTY over it, never assigned, so the\nbroad bit and the set cannot disagree. `SuspendNode.seeded_causes` carries the\nstd seed's set beside its one representative diagnostic label.\n\nWalks per compile: the entry graph went from up to four full walks per settling,\nplus one whole-graph walk per `_promote_nested_generic_calls` call (once per body\nJOINING the driven closure), to ONE per settling and ONE for the whole transform.\n\n## 2. Consumer census — every site that reads a suspension answer\n\n| site | value it needs | read before | reads now |\n|---|---|---|---|\n| `effects.finalize_effects` sync check | might | `node.suspends` (fixpoint) | `node.suspends` (derived) |\n| `effects.finalize_effects` `_main_suspends` | executor | `really_suspending` | `answers.wraps_main` |\n| `effects.finalize_effects` blocking narrowing | thread body | `suspends_ignoring_blocking` | `answers.refused_in_thread_body` |\n| `effects.finalize_effects` frame-method narrowing | framing | `suspends_ignoring_closure_calls` | `answers.frame_boundary` |\n| `effects._report_existential_suspend_dispatch` | executor | `really_suspending` | `answers.wraps_main` |\n| `effects._effect_seed_std_methods` | (producer) | 3-tuple table | 4-tuple + `seeded_causes` |\n| `effects._effect_path` blocking skip | (source class) | label prefix | `_source_class(s) & BLOCKING` |\n| `consumes._check_consumes_suspending_fences` | **wants framing**, reads might | `node.suspends` | `node.suspends` — SL-325 |\n| `sawc` `_std_suspending_methods` | might | `node.suspends` | `std_answers.might_suspend` |\n| `sawc` `_std_really_suspending_methods` | executor | `really_suspending` | `std_answers.wraps_main` + `causes` |\n| `sawc` `_std_suspending_methods_ignoring_closure_calls` | framing | `suspends_ignoring_closure_calls` | `std_answers.frame_boundary` |\n| `sawc` `_std_suspending_functions` (docs) | might | `node.suspends` | `std_answers.might_suspend` |\n| `sawc` main-wrap check (:1941) | executor | `_main_suspends` | unchanged (reads the flag) |\n| `coro._find_suspending_cycle` | might | `t.suspends` | `t.suspends` (derived; `is_built` narrows it — §3c) |\n| `coro._suspending_method_target` broad gate | might | `_suspending_methods_set` | unchanged (set now built from `might_suspend`) |\n| `coro._suspending_method_target` own gate | framing | `_own_suspending_methods_set` | unchanged (set now built from `frame_boundary`) |\n| `coro._default_expr_suspends` | **wants framing**, reads might | `n.suspends` | `n.suspends` — SL-324 |\n| `coro.transform_program` method census | might + framing | TWO walks | ONE table, two derivations |\n| `coro.transform_program` `_answered_locally` | might | `node.suspends` | `_answers.might_suspend` |\n| `coro` closure-walk edge-follow | framing | `_own_susp` dict | `_answers.frame_boundary` |\n| `coro._promote_nested_generic_calls` | framing | its OWN walk, per call | the table, handed down |\n| `docs_emit` (2 sites) | might | the std name sets | unchanged |\n\nNo consumer keeps a private derivation; the only cause-bit combinations in the\ncompiler are the five derivations' own definitions.\n\n## 3. The disagreement sweep (obligation 4) — 4 findings, none absorbed\n\n**(a) SL-323 — `existential_dispatch` is a FIFTH cause nobody classified.**\ndesign 223 unit 3's ``a call through `any Trait` dispatch`` is conservative (a\nvtable word carries no effect; design 223 refuses the dispatch outright when a\nconformance body really suspends), and the two deleted predicates disagreed about\nit and always had: design 206's struck it, SL-306's did not. The transform frames\non SL-306's, so a body whose ONLY cause is a dispatch joins the driven closure —\nand design 88's fence then refuses its erased `&any Trait` PARAMETER for a frame\nit never needed. Probe (main and U4, identical):\n`error: coroutine transform: g: &any Greeter cannot be a parameter of the\nsuspending function via_existential`, on a program with no suspension in\n`via_existential` at all. U4 gives the cause its own bit and KEEPS it inside\n`frame_boundary` (behaviour-preserving); the membership is recorded in that\nderivation's docstring, filed, and pinned XFAIL\n(`examples/coro_existential_only_body_is_not_framed.saw`).\n\n**(b) SL-324 / SL-325 — two consumers read `might` where the refusal they write\nis about a FRAME.** `_default_expr_suspends` (a suspending parameter DEFAULT) and\ndesign 260's two consuming-body fences. Both refuse a body whose only cause is\n`closure_call` — which is framed nowhere — with a message about a coroutine\nframe. SL-325's hint is the tell: it tells the author to make the method `sync`,\nwhich the same conservative cause also refuses. Probes on main and U4 are\nidentical. Both fixes are one word each once the ledger owns the reading; both\nare strictly widening, so they are U1/U2's to make, not this unit's.\n\n**(c) One site that reads `might` and is CORRECT.** `_find_suspending_cycle`\nfollows `t.suspends`, but `_analyze_nesting` passes `is_built`, which narrows the\nwalk to frames the closure walk actually builds — probed with a\nconservative-only mutual recursion under a driven root: compiles and runs. The\nmechanism admits no sibling there, which is why it is recorded rather than filed.\n\n**(d) FIXED BY THE UNIFICATION — the std seed's one label.** The seed minted a\nleaf carrying ONE representative source label where the std method has several\ncauses, so every derivation that EXCLUDES a cause was answered by whichever label\n`_effect_path` happened to pick. `Command.output` reaches BOTH a cooperative park\n(it parks on the child's exit) and a blocking offload (the pipe drain), was\nseeded under the blocking label, and `Thread.spawn { c.output() }` was therefore\nPERMITTED — a cooperative park on a thread with no executor, design 242 ruling\n9's exact hazard. The seed carries the cause SET now and the answer flips to\nrefused. No corpus program reaches it (no `Thread.spawn` body in the tree touches\nio or a child), so nothing in the suite changes.\n\n**THE WALKER-EQUIVALENCE PROBE** (the amendment's bar), `.build/scratch/probe_walker_equivalence.py`\n— the four old walkers re-implemented from main's source, over `node.direct`\nlabels, compared against the new derived reads at EVERY node of EVERY graph:\n\n    examples/ + errors/ + conformance/: 2499 of 2499 programs,\n      2,778,002 node comparisons (four walkers each), 7014 mismatches\n    blade/src/main.saw: 7542 comparisons, 12 mismatches\n    libs/toml/src/lib.saw: 1503 comparisons, 3 mismatches\n\nEvery mismatch is finding (d) and nothing else. Classified distribution over the\n331 suspension-heavy programs (1780 of the 7014): ONE pair\n(`ignoring_blocking` vs `refused_in_thread_body`), one cause set\n({cooperative, blocking}), seven node names, all of them `Command.output` or a\nbody that calls it. The mechanism bounds it: `seeded_causes` is the ONLY input\nthat differs between the old walkers and the new derivations, and it is nonzero\nonly on a seeded std leaf.\n\n## 4. Conformance rows (obligation 3) — cause × context, K111-K115\n\nOne row per CAUSE, each stating that cause's answer in all four contexts (sync\nbody, thread body, main, framed body), with a covering file per cell.\n\n- **K111** `cooperative` — new `K111_suspension_causes_decide_framing.saw` (the\n  framed-body column for every cause × every callee shape: free function,\n  method, generic instance, imported public + module-private helper, the test\n  intrinsic, and a `no`-cause control, with EXPECT-IR-CONTAINS/ABSENT per cell),\n  + `errors/copy_policy_hook_suspends_in_sync_fn.saw` + K83 + `coro_conservative_free_callee_not_framed.saw`.\n- **K112** `blocking` — `errors/sync_blocking_extern.saw` + K84's two files +\n  `expr_suspend_blocking.saw`; the row records finding (d).\n- **K113** `test_suspend` — new\n  `K113_test_suspend_is_a_frame_boundary_not_an_executor.saw` (framed at the\n  drive root, `__Frame_main` ABSENT for the same intrinsic in `main`) +\n  `errors/sync_direct_suspend.saw` + K114 + K111.\n- **K114** `closure_call` — new\n  `K114_thread_body_refuses_every_cause_but_blocking.saw` (the two thread-body\n  cells K83/K84 left unmeasured: `test_suspend` and `closure_call`) +\n  `errors/sync_context_calls_a_closure_taking_helper.saw` + K111 + K105's files.\n- **K115** `existential_dispatch` — `errors/any_sync_suspend.saw` + the SL-323\n  XFAIL pin.\n\nINDEX header recounted: **536 rows, 281 carry a file here, 255 covered\nelsewhere** (was 531/278/253). The pre-existing duplicate K53 row untouched.\n\n## 5. Gates\n\n- **Per-commit gate** and **TERMINAL battery**: `tools/battery.sh` full,\n  **28 stages GREEN in 3409s**, including `suite` (**2565 passed, 12 xfailed**,\n  zero XPASS, zero uncited), `reemit`, `irdet --all`, `gmgate`, `bootstrap`,\n  `corodiff`, and `freestanding` (**36 passed across riscv32 + arm64**). Log:\n  `.build/scratch/battery.log`. Run under the machine-wide lock\n  (`/private/tmp/claude-501/saw-suite-lock`), acquired and released around it.\n- The 12 xfails are the 10 pre-existing ones plus my two cited pins (SL-323,\n  SL-326). `citations`: 13 citations, 12 xfail + 1 ledger, 0 stale.\n\n## 6. Adversarial self-check — and a fifth finding\n\nProbe set beyond the tests, compiled AND run, `.build/scratch/`:\n\n- **two-module + same-name**: `adv_a`/`adv_b` declaring the same free-function\n  names (`worker`, `_shared_name`, `through_generic`) at OPPOSITE causes, and\n  `adv_c`/`adv_d` declaring the same really-suspending generic `warp`.\n- **generic-via-helper**, **closure-caller**, **imported public vs\n  module-private**: all in `K111` with per-cell IR assertions — `__Frame_really_free`,\n  `__Frame_boundary`, `__Frame_Widget_tick`, `__Frame_hop$m$$1$Int`,\n  `__Frame_yield_public$m$sl306mod`, `__Frame_yield_via_private$m$sl306mod` and\n  `__Frame__yield_private$m$sl306mod` PRESENT; `__Frame_closure_only_free`,\n  `__Frame_plain_free`, `__Frame_Widget_fold`, `__Frame_pass`,\n  `__Frame_fold_public`, `__Frame__fold_private` ABSENT. Identical frame set on\n  main and on U4.\n- **IR byte-compare, main vs U4**: 27 programs (the SL-280 rows, the SL-306\n  matrix, the design-44 nested-suspend rows, the json/module rows, the blocking\n  row) — `MISMATCHES: none`.\n\n**SL-326 (filed high) — the finding the adversarial set turned up.** A suspending\nGENERIC free call in an EXPRESSION position inside a driven body is hoisted by\ndesign 120's ANF and never PROMOTED, so the instantiation has no frame and the\nhoisted call is a plain call to a suspending body inside the synthesized resume:\n``cannot suspend in `sync func` method: method `__Frame_main.resume` calls\n`warp$1$Int` → yield_now``. The statement-level `let` cell is the control and\ncompiles. Position matrix probed: interpolation, argument and binary operand all\nfail; entry-module and imported alike; qualifier and selective-import spellings\nalike. Mechanism: `_promote_nested_generic_calls` walks the positions design 74\nshape 3 was written against while design 120/224 widened the HOIST's positions —\ndiscovery and lowering disagreeing on a position set, which is design 275's own\ndiagnosis one site over, and U2's shape table is the artifact that catches it.\nPre-existing on main; pinned XFAIL as\n`examples/coro_generic_call_in_an_expression_position.saw`.\n\n## 7. Out of scope, untouched\n\nNo ledger, no shape table, no new splits; the state machine, the seven split\nroutines, the drive loop, the executor and design 44's frame embedding are\nunchanged. SL-215 / SL-317 / SL-287 / SL-316 not touched and their pins did not\nflip. Nothing closed in the tracker; `designs/todo.md`'s design-275 entry marked\nin place.\n\n## 8. Filed this unit\n\nSL-323 (existential_dispatch framed), SL-324 (suspending parameter default),\nSL-325 (design 260's consuming fences), SL-326 (generic call in an expression\nposition, high).\n","created":"1789774386","id":"c14"},{"author":"agent:claude-sl318u4","body":"# U4 addendum — the two review fixes, regated (final tip `672d20dc`)\n\n## FIX 1 — the placeholder, and its provenance\n\nThe refusal printed ``closure calls <suspension source>``. Provenance matches\nthe lead's: NOT general to seeded leaves and NOT pre-existing. A seeded std leaf\ncarried ONE representative source label; the thread-body refusal STRIKES the\nblocking cause (design 242 ruling 9 permits it), so for a method whose\nrepresentative label is the blocking one and whose refusal is satisfied by the\nCOOPERATIVE cause — `Command.output` — the path walk found nothing left to name\nat the leaf and fell to `_effect_path`'s robustness fallback. `Channel.receive`\n(one cause) renders correctly on main and on the branch alike.\n\nFixed where the seed is minted: `build_builtin_namespace` walks the non-blocking\npath too (the builtin graph is the only place that path exists) and the leaf\ncarries a source PER CAUSE, **representative first**. Consequences, all probed:\n\n- striking reader: ``closure calls `Command.output` → io_wait (`Command.output`\n  suspends at line 494)`` — the method AND the cause that decided it;\n- non-striking reader, byte-identical to main: ``cannot suspend in `sync func`\n  declaration: `sync func run_it` calls `Command.output` → blocking extern\n  `__saw_rt_proc_read_stdout` ``;\n- single-cause control, byte-identical to main: ``closure calls\n  `Channel.receive` → __saw_chan_park``.\n\nTwo names moved into `effects.py` so nobody re-derives them: `cause_of_label`\n(the one label→cause map, now asked by the seed instead of a private prefix\ntest) and `PATH_PLACEHOLDER_LABEL` (so a caller BUILDING a table tests for the\nfallback instead of shipping it into a message).\n\n**The pin moved to its own file** — `examples/conformance/K112_thread_body_refuses_a_multi_cause_std_method.saw`,\nwith `EXPECT-ERROR-CONTAINS: closure calls \\`Command.output\\` → io_wait` and\n`EXPECT-ERROR-ABSENT: <suspension source>`. It cannot be a third cell in K114,\nand why is a finding: **SL-327** (filed low). The seed table's node ids do not\nmatch the entry graph's edges at the FIRST settling (`62190` vs `20761`,\ndumped), so this refusal is only reached at a later one — and a compile stops\nafter a settling that reported errors, so beside another refused cell the\nmessage is never emitted. Pre-existing keying (design 206's), made visible by\nthe new refusal; recorded for U1, which takes the seeded-leaf population over.\nK112's INDEX row carries the whole story; header recounted 281/255 → **282 carry\na file here, 254 elsewhere** (536 rows unchanged).\n\n## FIX 2 — stale docstrings\n\n`coro_transform.py:85-90` and `:11328` and `:11342-11344` rewritten for the\ncause set and the named derivations (`frame_boundary` / `might_suspend`). Grepped\nthe branch for `three-valued`, `closure-only` and backticked `really`: the only\nsurvivor is `effects.py:168`, which names the REJECTED draft as the record of\nwhy the set exists. K111's cell labels and its printed strings now spell the\ncause `closure_call` rather than the old `closure-only`.\n\n## Gates at the final tip (`672d20dc`)\n\n`tools/battery.sh` full, under the machine-wide lock: **28 stages GREEN in\n3389s**. `suite` **2566 passed, 12 xfailed** (the 10 pre-existing + my two cited\npins; zero XPASS, zero uncited), `freestanding` **36 passed across riscv32 +\narm64** — that pair is the per-commit gate — plus `reemit`, `irdet --all`,\n`gmgate`, `bootstrap`, `corodiff`, `fuzz`, `citations`, `astgraft`. Log:\n`.build/scratch/battery2.log`.\n\nThree commits, linear, not submitted: `7dbd81da` (the unit), `cd0462fc` (the\nSL-326 pin), `672d20dc` (these two fixes).\n\nFiled by this unit: SL-323, SL-324, SL-325, SL-326, SL-327.\n","created":"1789778644","id":"c15"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-318.p3 (revision 1): design 275 U4: one suspension analysis — a cause set per node, four walkers deleted, five named derivations; std seed carries the set (Thread-body Command.output refused)","created":"1789778697","id":"c16"},{"author":"agent:claude-sawlang","body":"SUBMITTED SL-318.p3 (lead, Sep 19): design 275 U4 at tip 672d20dc (3 commits squashed), LEAD-VALIDATED. `effects.classify_suspensions` computes ONE cause set per node {cooperative, blocking, test_suspend, existential_dispatch, closure_call, unclassified} by one set-union fixpoint; the four walkers (`suspends`, `really_suspending`, `suspends_ignoring_blocking`, `suspends_ignoring_closure_calls`) are DELETED and their consumers read five named derivations (`might_suspend`, `wraps_main`, `refused_in_thread_body`, `frame_boundary`, `closure_only`) whose docstrings list their consumers; 22 consumer sites tabled. The std seed carries the SET per method, which fixed an unmeasured cell: `Thread.spawn { c.output() }` compiled and RAN on main (Command.output parks cooperatively on the child's exit; the seed's one representative label was the blocking one) and is refused now, naming the method and the primitive (K112, pinned with EXPECT-ERROR-CONTAINS + ABSENT `<suspension source>`). Conformance K111-K115 = cause × context. Findings filed, none absorbed: SL-323 (existential_dispatch is a cause nobody classified; kept in frame_boundary for behaviour preservation, XFAIL), SL-324/SL-325 (two `might` readers whose refusal is about a frame), SL-326 (HIGH, U2's cell: a suspending generic call in an EXPRESSION position is ANF-hoisted but never promoted → plain call in the resume; XFAIL pinned), SL-327 (seed node ids vs entry-graph edges at the first settling — U1 takes seeding over). Walker-equivalence probe: 2,778,002 node comparisons over 2499 programs + blade + libs; every mismatch is the seed fix. Agent gates at the tip: battery 28/28, suite 2566/12x, freestanding 36/36. Lead validation: 17 then 10 filtered rows pass; adversarial probe (same-named parks in two modules, generic via helper, closure-caller, blocking-only, test-suspend-only) frames exactly the expected set; seed probe main-compiles/branch-refuses with the method named; Channel.receive control byte-identical both sides. Behaviour-preserving for the transform: IR byte-compare main vs branch over the SL-280/SL-306/design-44 rows, no mismatches. Does NOT overlap SL-318.p2 (the docs patch touches only the brief; this touches code + todo.md + INDEX).\n","created":"1789778729","id":"c17"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-318.p3 (revision 1)","created":"1789779179","id":"c18"},{"author":"agent:codex","body":"Approved patch SL-318.p3 (revision 1)\n\nApproved revision 1. No blocking findings.\n\nThe U4 implementation meets the cause-set portion of the amended design: one SCC-safe set-union classifier replaces the separate effect walkers, named derivations preserve the distinctions between author sync checks, thread-body refusal, executor wrapping, and frame boundaries, and generic promotion shares the transform's analysis table. The std-method seed carries the full cause set rather than reducing it to the diagnostic representative. The additional non-blocking diagnostic source makes the newly correct Command.output thread-body refusal actionable.\n\nThe deliberate existential-dispatch framing compatibility is explicit and pinned by SL-323; the generic expression-position limitation is separately pinned by SL-326. Neither is introduced by this refactor. This approves U4, not the remaining ledger, totality, or borrow-lifetime units of SL-318.\n\nIndependent validation on an isolated checkout of base 40230e95 with the exact patch applied:\n- Focused test_runner selection: 18 passed, 2 expected failures (SL-323 and SL-326). Includes K111-K114, K83/K84, ordinary sync/blocking/test-intrinsic refusals, conservative free/method/generic framing and spawned-task regressions, existential refusal, copy-hook refusal, and blocking expression suspension. IR assertions and diagnostic presence/absence checks passed.\n- Independent scratch probe: all 64 cause subsets satisfy the five derivations; all six source classes map correctly; 2,400 node answers over 200 deterministic generated graphs match a separate reachability oracle, including cycles, mixed seeded/direct causes, and missing targets.\n- Inspected the consumer replacements and generic-promotion ordering; searched for remaining legacy walker calls and writes to the derived suspends property.\n\nServer patch tests: passed. Re-fetched and verified revision 1 and its exact diff immediately before recording this approval. No merge performed.\n","created":"1789821979","id":"c19"},{"author":"agent:claude-sawlang","body":"Merged patch SL-318.p3 (revision 1) as a2ef74e1","created":"1789833215","id":"c20"},{"author":"agent:claude-sawlang","body":"Patch SL-318.p2 needs rebase; the base branch moved since it applied. Run patch rebase, or submit a revised diff if it conflicts.","created":"1789833229","id":"c21"},{"author":"agent:claude-sawlang","body":"Revised patch SL-318.p2 to revision 3","created":"1789833267","id":"c22"}],"created":"1789738775","description":"USER RULING (Sep 18): revisit the coroutine transform's discovery seams as an EPIC rather than continue per-site funnel fixes. Four units, all ruled as the lead recommended: (1) ONE DISCOVERY LEDGER — a single pass computes the closed frame table (key, declaration, really-suspends, buildable, reason) before any body is lowered; classifier, closure walk, generic promotion, rejectors and drive-site rewriting READ it and derive nothing; a battery lane fails on any transform site that reads the raw suspension bit or a symbol name directly (the producer-taxonomy / astgraft precedent). (2) TOTALITY — every statement and expression shape is either splittable or refused by ONE rejector reading ONE shape table; a callee the walk cannot frame is a compile error, never a plain call whose park blocks the executor (closes SL-287, SL-316; the K33-K37 XFAIL family are rows). (3) COVERAGE UNITS INSIDE THE EPIC — SL-215 (inline try/catch split) and SL-317 (iterator state in the frame for a collection for) are epic units, NOT standalone point fixes; sawtracker's build (currently stopped at those two) is the epic's acceptance test and WAITS for it — the user ruled a point fix is a waste of time. (4) A THREE-VALUED ANSWER AT THE EFFECT SOURCE — the graph answers once (really suspends / only through a closure call / does not) instead of one bit meaning might plus design 206's second question bolted beside it. Census (Sep 18): coro_transform.py 12,224 lines, a 212-method _FrameBuilder, a 1,032-line transform_program with nine nested closures, ~20 distinct suspension/frame-key predicates, 7 split shapes (if, if-let, guard-let, while, RANGE for, match, BLOCK try-catch), 21 scattered refusals, 17 governing designs (44..224). The lowering is stable; every finding this week (SL-280, SL-274 keying sites, SL-306 + its review, SL-215, SL-317, SL-287, SL-316) was a discovery seam. Brief: designs/275-coro-discovery-uniformity.md (lead, next). Sequenced after SL-306.p1 r2 + SL-274.p1 r2 land and the small-fixes batch; ledger unit first.","id":"SL-318","labels":["epic","coro","design-275"],"priority":"high","project":"SL","revision":24,"sequence":1026,"stage":"queued","status":"open","title":"EPIC: coroutine transform discovery uniformity — one frame ledger, total shape coverage, no silent decline (design 275)","updated":"1789833267"},"id":"SL-318","project":"SL","request":"{\"action\":\"patch_review\",\"actor\":\"human:shawn\",\"body\":\"\",\"id\":\"SL-318.p2\",\"request_id\":\"8b75576007dc7773ebec0421488a5cd2\",\"verdict\":\"approve\"}","request_id":"8b75576007dc7773ebec0421488a5cd2","sequence":1027,"timestamp":"1789833361"}
---
