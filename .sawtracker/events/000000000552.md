---
{"action":"close","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl235","author":"agent:codex-reviewer","comments":[{"author":"agent:claude-sl235","body":"Claiming SL-236. Reproduced: SIGBUS (138) on the short-circuit && whose skipped RHS reads a temp token's String field.","created":"1789105869","id":"c1"},{"author":"agent:claude-sl235","body":"Fix in. Verified against -O0 IR: _generate_logical_and/_generate_logical_or evaluate the RHS in a conditionally-taken block, but a statement temp created there (a call/method result whose String field is read) was appended to the ENCLOSING statement's temp list and drained unconditionally at the statement merge — reached on the short-circuit path too, where the slot was never stored, so String_deinit ran over an uninitialized alloca (garbage pointer -> SIGBUS). Fix applies design-94 block confinement (new _confine_short_circuit_temps) to the RHS operand. Drop-count oracle (sync + suspending twin) over &&/||, LHS-temp/RHS-temp, nested, if-cond/let-bound: skipped=0 drops (no crash), evaluated=1. Suite 2464, freestanding 36/36.","created":"1789108364","id":"c2"},{"author":"agent:claude-sawlang","body":"LANDED: fixed in SL-235.p1 (merge 93c70401, Sep 11). The SIGBUS: a short-circuit-skipped RHS operand's owned temporary was drained unconditionally at the statement merge; fix confines those temps to the RHS block (design-94 block confinement, third entry point, codegen operators/resources). Drop-count oracle regressions: examples/short_circuit_skipped_operand_temp_cleanup.saw (sync + suspending twins), examples/short_circuit_token_field_no_crash.saw (the original crash shape).","created":"1789128856","id":"c3"}],"created":"1789070591","description":"The Python compiler accepts the valid source below, but the resulting executable crashes instead of printing `false`. Binding the cloned token to a named local before inspecting its String field makes the control program run correctly.\n\nFreshly reproduced on local main `30c5a55016aed806a2f94a64d62224c6cafc135a`, arm64 macOS, default compiler optimization. It also reproduces with the prototype bootstrap compiler (last sawc change `d592e716d562990656b98ced5a391b7d3d282246`). No production source changes were needed to reproduce.\n\n```saw\nenum Kind { case Ident, case Number, case Let, case Var, case Return, case While, case Brace }\nstruct Tok { kind: Kind, value: String, line: Int, col: Int, suffix: String? }\nfunc clone(t: &Tok) -> Tok {\n    Tok(kind: t.kind, value: t.value, line: t.line, col: t.col, suffix: t.suffix)\n}\nstruct Reader { tokens: Vector<Tok>, pos: Int }\nextension Reader: NoCopy {}\nextension Reader {\n    func token(&self) -> Tok { clone(&self.tokens[self.pos]) }\n    func at(&self, k: Kind) -> Bool { self.token().kind == k }\n    func classify(&self) -> Bool {\n        if self.at(Kind.Let) || self.at(Kind.Var) || self.at(Kind.Return) || self.at(Kind.While) || self.at(Kind.Brace) { return true }\n        if self.at(Kind.Ident) && self.token().value == \"print\" { return true }\n        false\n    }\n}\nfunc main() {\n    var tokens = Vector<Tok>()\n    try! tokens.push(Tok(kind: Kind.Number, value: \"42\", line: 1, col: 1, suffix: None))\n    let reader = Reader(tokens: move tokens, pos: 0)\n    print(reader.classify())\n}\n```\n\nSave as `.build/scratch/token_cleanup.saw`, compile with `.venv/bin/python sawc/sawc.py .build/scratch/token_cleanup.saw -o .build/scratch/token_cleanup`, and run the executable.\n\nExpected: stdout `false\\n`, exit 0. The token is Number, so the second condition's String-field operand must not be evaluated.\n\nActual: compilation succeeds; the executable emits no stdout and terminates with SIGBUS (shell status 138). The larger self-hosted compiler reproductions also showed SIGSEGV on a rejected-expression path. Crash reports put those larger failures in generated String cleanup. The standalone failure and named-local control are confirmed; the exact incorrect cleanup scheduling/ownership mechanism still needs diagnosis.\n\nPassing control: replace the second condition with:\n\n```saw\nlet t = self.token()\nif t.kind == Kind.Ident && t.value == \"print\" { return true }\n```\n\nThat separately compiled control prints `false` and exits 0 on the same main checkout. Local evidence lives in `/Users/swoodtke/Projects/sawlang-minivm/.build/scratch/m5_token_cleanup.saw` and `m5_token_cleanup_named.saw`; the complete failing source above makes the report portable.\n\nThis was found during the Saw-written compiler prototype's independent tests. Its statement classifiers now use named Token locals. That source restructuring does not fix Python sawc; this issue records the underlying bootstrap failure separately. Related temporary-payload ownership work exists in SL-220, but that report describes leaking fresh Optional/Result payloads, while this is an ordinary struct field on a short-circuit path with an observed crash.\n\nAcceptance: make the exact inline source run correctly; cover both condition outcomes and repeated calls; check temporary owning-field cleanup in &&/|| and diagnostic/early-return paths. Keep the named-local control as a semantic comparison, not as the production fix.\n","id":"SL-236","labels":["bug","codegen","ownership"],"priority":"high","project":"SL","revision":6,"sequence":552,"stage":"queued","status":"closed","title":"Temporary token String field in short-circuit condition produces a crashing executable","updated":"1789128856"},"before":{"assignee":"agent:claude-sl235","author":"agent:codex-reviewer","comments":[{"author":"agent:claude-sl235","body":"Claiming SL-236. Reproduced: SIGBUS (138) on the short-circuit && whose skipped RHS reads a temp token's String field.","created":"1789105869","id":"c1"},{"author":"agent:claude-sl235","body":"Fix in. Verified against -O0 IR: _generate_logical_and/_generate_logical_or evaluate the RHS in a conditionally-taken block, but a statement temp created there (a call/method result whose String field is read) was appended to the ENCLOSING statement's temp list and drained unconditionally at the statement merge — reached on the short-circuit path too, where the slot was never stored, so String_deinit ran over an uninitialized alloca (garbage pointer -> SIGBUS). Fix applies design-94 block confinement (new _confine_short_circuit_temps) to the RHS operand. Drop-count oracle (sync + suspending twin) over &&/||, LHS-temp/RHS-temp, nested, if-cond/let-bound: skipped=0 drops (no crash), evaluated=1. Suite 2464, freestanding 36/36.","created":"1789108364","id":"c2"}],"created":"1789070591","description":"The Python compiler accepts the valid source below, but the resulting executable crashes instead of printing `false`. Binding the cloned token to a named local before inspecting its String field makes the control program run correctly.\n\nFreshly reproduced on local main `30c5a55016aed806a2f94a64d62224c6cafc135a`, arm64 macOS, default compiler optimization. It also reproduces with the prototype bootstrap compiler (last sawc change `d592e716d562990656b98ced5a391b7d3d282246`). No production source changes were needed to reproduce.\n\n```saw\nenum Kind { case Ident, case Number, case Let, case Var, case Return, case While, case Brace }\nstruct Tok { kind: Kind, value: String, line: Int, col: Int, suffix: String? }\nfunc clone(t: &Tok) -> Tok {\n    Tok(kind: t.kind, value: t.value, line: t.line, col: t.col, suffix: t.suffix)\n}\nstruct Reader { tokens: Vector<Tok>, pos: Int }\nextension Reader: NoCopy {}\nextension Reader {\n    func token(&self) -> Tok { clone(&self.tokens[self.pos]) }\n    func at(&self, k: Kind) -> Bool { self.token().kind == k }\n    func classify(&self) -> Bool {\n        if self.at(Kind.Let) || self.at(Kind.Var) || self.at(Kind.Return) || self.at(Kind.While) || self.at(Kind.Brace) { return true }\n        if self.at(Kind.Ident) && self.token().value == \"print\" { return true }\n        false\n    }\n}\nfunc main() {\n    var tokens = Vector<Tok>()\n    try! tokens.push(Tok(kind: Kind.Number, value: \"42\", line: 1, col: 1, suffix: None))\n    let reader = Reader(tokens: move tokens, pos: 0)\n    print(reader.classify())\n}\n```\n\nSave as `.build/scratch/token_cleanup.saw`, compile with `.venv/bin/python sawc/sawc.py .build/scratch/token_cleanup.saw -o .build/scratch/token_cleanup`, and run the executable.\n\nExpected: stdout `false\\n`, exit 0. The token is Number, so the second condition's String-field operand must not be evaluated.\n\nActual: compilation succeeds; the executable emits no stdout and terminates with SIGBUS (shell status 138). The larger self-hosted compiler reproductions also showed SIGSEGV on a rejected-expression path. Crash reports put those larger failures in generated String cleanup. The standalone failure and named-local control are confirmed; the exact incorrect cleanup scheduling/ownership mechanism still needs diagnosis.\n\nPassing control: replace the second condition with:\n\n```saw\nlet t = self.token()\nif t.kind == Kind.Ident && t.value == \"print\" { return true }\n```\n\nThat separately compiled control prints `false` and exits 0 on the same main checkout. Local evidence lives in `/Users/swoodtke/Projects/sawlang-minivm/.build/scratch/m5_token_cleanup.saw` and `m5_token_cleanup_named.saw`; the complete failing source above makes the report portable.\n\nThis was found during the Saw-written compiler prototype's independent tests. Its statement classifiers now use named Token locals. That source restructuring does not fix Python sawc; this issue records the underlying bootstrap failure separately. Related temporary-payload ownership work exists in SL-220, but that report describes leaking fresh Optional/Result payloads, while this is an ordinary struct field on a short-circuit path with an observed crash.\n\nAcceptance: make the exact inline source run correctly; cover both condition outcomes and repeated calls; check temporary owning-field cleanup in &&/|| and diagnostic/early-return paths. Keep the named-local control as a semantic comparison, not as the production fix.\n","id":"SL-236","labels":["bug","codegen","ownership"],"priority":"high","project":"SL","revision":5,"sequence":538,"stage":"queued","status":"open","title":"Temporary token String field in short-circuit condition produces a crashing executable","updated":"1789108364"},"id":"SL-236","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"LANDED: fixed in SL-235.p1 (merge 93c70401, Sep 11). The SIGBUS: a short-circuit-skipped RHS operand's owned temporary was drained unconditionally at the statement merge; fix confines those temps to the RHS block (design-94 block confinement, third entry point, codegen operators/resources). Drop-count oracle regressions: examples/short_circuit_skipped_operand_temp_cleanup.saw (sync + suspending twins), examples/short_circuit_token_field_no_crash.saw (the original crash shape).\",\"id\":\"SL-236\",\"request_id\":\"pid-93132-1789128856-14000\"}","request_id":"pid-93132-1789128856-14000","sequence":552,"timestamp":"1789128856"}
---
