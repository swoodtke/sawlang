---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791288","description":"## Scope and status\n\nDesign address-taking through a borrows accessor’s lent field\n\nReported by SawOS at the compiler version recorded below; not freshly reproduced during import. Reconcile later upstream rulings and reproduce on current Sawlang before implementing. \n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reconcile the linked brief and recorded rulings with current consumers. Keep already-landed behavior and stated deferrals explicit.\n2. Draft the remaining contract: supported syntax/API, ownership and failure behavior, alternatives, compatibility effects and the exact questions still requiring a decision.\n3. Define small implementation units and consumer migrations, then specify the positive, negative and boundary tests before dispatch.\n\n## Acceptance criteria\n\n- [ ] The design names a concrete consumer or retains its recorded revisit trigger.\n- [ ] Existing rulings are preserved; unresolved choices and a recommended option are explicit.\n- [ ] The proposed API/semantics, migration scope and acceptance tests are reviewable. Drafting this plan does not mark an unruled design approved.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawos TODO lines 368–383\n\n[Original record](https://github.com/swoodtke/sawos/blob/754ec9570aef54609820cc7e28457c7623547670/designs/todo.md#L368-L383)\n\n- SL‑23 — A `borrows` ACCESSOR'S LENT PLACE HAS NO ADDRESS: `&var slab[i].field` IS REFUSED THOUGH THE SAME FIELD IS ADDRESSABLE INSIDE A `&var self` METHOD: ``can only take reference to a variable, field, or array element`` with ``hint: references require an addressable location`` (design 34, `kernel/core/objects.saw`, sawc 0.5.0). Design 146's whole claim for a place accessor is that it lends the element WHERE IT SITS, and design 130's one sanctioned crossing into the unsafe tier is `(&x) as UnsafePointer<T>` — but the two do not compose, so a kernel that needs the ADDRESS of a slot's field (a message body handed to a byte mover) cannot ask the accessor for it:\n  ```saw\n  // EXCHANGES: Slab<Exchange, N>, Exchange { body: [UInt8; 128], ... }\n  ((&var EXCHANGES[x].body) as UnsafePointer<UInt8>) as UInt   // error\n  ```\n  The workaround is a method on the ELEMENT type, and it works because design 261 passes every receiver by pointer, so the receiver's own field is an ordinary addressable location:\n  ```saw\n  extension Exchange {\n      public(package) func body_addr(&var self) unsafe -> UInt {\n          ((&var self.body) as UnsafePointer<UInt8>) as UInt   // fine\n      }\n  }\n  EXCHANGES[x].body_addr()\n  ```\n  So the capability exists and only the SPELLING is missing — which is what makes this a wart rather than a wall: one extra declaration per element type that needs it, and the address still comes out of the slot the accessor lent. It bit exactly once in-tree (the pipe body arena becoming a field of a slab slot) and the workaround is arguably the better code, since it names the operation. Resolution: let a `&`/`&var` of a lent place's field take the address the window already refers to, on the ordinary unsafe-tier terms — the window's extent is the expression, which is exactly as long as the existing crossing's.\n","id":"SL-193","labels":["todo-import","sawos-reported","needs-verification","design","plan","design-proposal","backlog"],"priority":"normal","project":"SL","revision":1,"sequence":193,"status":"open","title":"Design address-taking through a borrows accessor’s lent field","updated":"1788791288"},"before":null,"id":"SL-193","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDesign address-taking through a borrows accessor’s lent field\\n\\nReported by SawOS at the compiler version recorded below; not freshly reproduced during import. Reconcile later upstream rulings and reproduce on current Sawlang before implementing. \\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reconcile the linked brief and recorded rulings with current consumers. Keep already-landed behavior and stated deferrals explicit.\\n2. Draft the remaining contract: supported syntax/API, ownership and failure behavior, alternatives, compatibility effects and the exact questions still requiring a decision.\\n3. Define small implementation units and consumer migrations, then specify the positive, negative and boundary tests before dispatch.\\n\\n## Acceptance criteria\\n\\n- [ ] The design names a concrete consumer or retains its recorded revisit trigger.\\n- [ ] Existing rulings are preserved; unresolved choices and a recommended option are explicit.\\n- [ ] The proposed API/semantics, migration scope and acceptance tests are reviewable. Drafting this plan does not mark an unruled design approved.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawos TODO lines 368–383\\n\\n[Original record](https://github.com/swoodtke/sawos/blob/754ec9570aef54609820cc7e28457c7623547670/designs/todo.md#L368-L383)\\n\\n- SL‑23 — A `borrows` ACCESSOR'S LENT PLACE HAS NO ADDRESS: `&var slab[i].field` IS REFUSED THOUGH THE SAME FIELD IS ADDRESSABLE INSIDE A `&var self` METHOD: ``can only take reference to a variable, field, or array element`` with ``hint: references require an addressable location`` (design 34, `kernel/core/objects.saw`, sawc 0.5.0). Design 146's whole claim for a place accessor is that it lends the element WHERE IT SITS, and design 130's one sanctioned crossing into the unsafe tier is `(&x) as UnsafePointer<T>` — but the two do not compose, so a kernel that needs the ADDRESS of a slot's field (a message body handed to a byte mover) cannot ask the accessor for it:\\n  ```saw\\n  // EXCHANGES: Slab<Exchange, N>, Exchange { body: [UInt8; 128], ... }\\n  ((&var EXCHANGES[x].body) as UnsafePointer<UInt8>) as UInt   // error\\n  ```\\n  The workaround is a method on the ELEMENT type, and it works because design 261 passes every receiver by pointer, so the receiver's own field is an ordinary addressable location:\\n  ```saw\\n  extension Exchange {\\n      public(package) func body_addr(&var self) unsafe -> UInt {\\n          ((&var self.body) as UnsafePointer<UInt8>) as UInt   // fine\\n      }\\n  }\\n  EXCHANGES[x].body_addr()\\n  ```\\n  So the capability exists and only the SPELLING is missing — which is what makes this a wart rather than a wall: one extra declaration per element type that needs it, and the address still comes out of the slot the accessor lent. It bit exactly once in-tree (the pipe body arena becoming a field of a slab slot) and the workaround is arguably the better code, since it names the operation. Resolution: let a `&`/`&var` of a lent place's field take the address the window already refers to, on the ordinary unsafe-tier terms — the window's extent is the expression, which is exactly as long as the existing crossing's.\\n\",\"labels\":[\"todo-import\",\"sawos-reported\",\"needs-verification\",\"design\",\"plan\",\"design-proposal\",\"backlog\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-4ee2095d6e502085267e-create\",\"title\":\"Design address-taking through a borrows accessor’s lent field\"}","request_id":"todo-20260907-4ee2095d6e502085267e-create","sequence":193,"timestamp":"1788791288"}
---
