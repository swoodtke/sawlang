---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791233","description":"## Scope and status\n\nDF-218d (ICE, sawfuzz-oracle class) — a value-if statement followed by a line beginning with - dies internal compiler error (BinaryOp): 'NoneType' object has no…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 3897–3903\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L3897-L3903)\n\n- **DF‑218d (ICE, sawfuzz-oracle class) — a value-`if` statement followed by\n  a line beginning with `-`** dies `internal compiler error (BinaryOp):\n  'NoneType' object has no attribute 'type'` (the value-if binding parsed\n  as a subtraction LHS). Repro `.build/scratch/probe_s10/ice1.saw`; a `let`\n  in the same position works. Pin + sawfuzz_known entry owed at the next\n  pin batch. Sibling wart, same parse family: `move <method call>` in\n  argument position is a parse error rather than a `move` diagnostic.\n","id":"SL-88","labels":["todo-import","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":88,"status":"open","title":"DF-218d (ICE, sawfuzz-oracle class) — a value-if statement followed by a line beginning with - dies internal compiler error (BinaryOp): 'NoneType' object has no…","updated":"1788791233"},"before":null,"id":"SL-88","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-218d (ICE, sawfuzz-oracle class) — a value-if statement followed by a line beginning with - dies internal compiler error (BinaryOp): 'NoneType' object has no…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 3897–3903\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L3897-L3903)\\n\\n- **DF‑218d (ICE, sawfuzz-oracle class) — a value-`if` statement followed by\\n  a line beginning with `-`** dies `internal compiler error (BinaryOp):\\n  'NoneType' object has no attribute 'type'` (the value-if binding parsed\\n  as a subtraction LHS). Repro `.build/scratch/probe_s10/ice1.saw`; a `let`\\n  in the same position works. Pin + sawfuzz_known entry owed at the next\\n  pin batch. Sibling wart, same parse family: `move <method call>` in\\n  argument position is a parse error rather than a `move` diagnostic.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-d96ce40bbee0925211e7-create\",\"title\":\"DF-218d (ICE, sawfuzz-oracle class) — a value-if statement followed by a line beginning with - dies internal compiler error (BinaryOp): 'NoneType' object has no…\"}","request_id":"todo-20260907-d96ce40bbee0925211e7-create","sequence":88,"timestamp":"1788791233"}
---
