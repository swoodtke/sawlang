---
{"action":"edit","actor":"agent:claude-sawlang","after":{"assignee":"","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Found by the lead's adversarial validation of design 275 U1 (SL-318.p4), Sep 19. PRE-EXISTING: identical on main 8d752a78 and on the U1 branch (behaviour-preserving), so not U1's regression. Possibly the concrete cell SL-184 (\"verify nested generics whose templates suspend unconditionally\", a todo import) was pointing at.\n\nIn the ENTRY module, a generic template instantiated from inside ANOTHER generic template's body cannot name a suspending free function: the compile fails at typecheck with `undefined function`, and two cascaded errors.\n\n```saw\nimport std.task.{yield_now}\n\nfunc outer<T>(x: Int) -> Int {\n    let r = inner<T>(x)\n    r + 1\n}\nfunc inner<T>(x: Int) -> Int {\n    let s = leaf(x)          // error: undefined function `leaf`\n    s + 10                   // error: undefined variable `s`\n}                            // error: function `inner` should return `Int` but body has no value\nfunc leaf(x: Int) -> Int { yield_now()  x + 100 }\n\nfunc chain(n: Int) -> Int { outer<Int>(n) }\nfunc main() { print(\"chain {} {}\", __saw_drive(chain(1)), __saw_drive_steps(chain(1))) }\n```\n\nTHE MATRIX (all on both compilers, byte-identical):\n- SYNC twin (`leaf` without the `yield_now`, `main` calls `outer<Int>(1)` plainly) — WORKS: `chain 112`. So ordinary monomorphization of the nested template is fine; the failure needs the SUSPENDING path.\n- the nested template suspending DIRECTLY (`inner<T>` calls `yield_now()` itself, no free callee) — WORKS: `chain 12 1`. So the clone itself is fine; what fails is resolving a FREE FUNCTION NAME from inside it.\n- the SAME two templates in an IMPORTED module (`alpha.outer<T>` → `alpha.inner<T>` → `alpha.leaf`), reached by `import alpha.{outer}` — WORKS: `chain 112 1`, frames for all three. So the entry module is the failing side, which is the opposite of the usual asymmetry.\n- renaming the two `let r` locals apart changes nothing (`s` fails the same way), so it is not a shadowing/binding collision.\n\nMECHANISM (named per obligation 4 as a hypothesis from the matrix; not yet stepped): the coroutine transform's nested generic promotion clones the INNER template's body for `inner$1$Int` when it discovers the instantiation from inside `outer$1$Int`'s body (K116's \"generic via helper\" depth, one level further — generic via GENERIC). The clone is then typechecked in a namespace that does not hold the entry module's free functions — consistent with design 104 shape 4's pristine-template capture, which is built for IMPORTED templates and resolves against the template's own module, and with the imported twin working: for an imported `inner<T>` the capture IS the right namespace, for an entry-module one it is not, and `leaf` (an entry-module free function, tagged `leaf$m$` since SL-274) is not found. The cascade (`undefined variable s`, `no value`) is the failed `let`. A direct `yield_now()` resolves because it is an imported bare name, present in every capture.\n\nNot silent — a refusal — but a wrong one about a sound program, and the message points at the author's template rather than at the promotion. In U1's terms the site was never asked of the ledger (the clone fails before discovery finishes), so it is U2 coverage input; the fix belongs with the promotion's namespace for entry-module nested clones.\n\nWorkaround today: move the templates into a module and import them bare, or make the nested template call the suspending primitive directly.\n","created":"1789845299","id":"c1"}],"created":"1789845299","description":"","id":"SL-331","labels":[],"priority":"normal","project":"SL","revision":2,"sequence":1047,"stage":"queued","status":"open","title":"an ENTRY-module generic template instantiated from inside another template cannot name a suspending free function — undefined function at the nested clone; sync twin and imported twin both work","updated":"1789845359"},"before":{"assignee":"","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Found by the lead's adversarial validation of design 275 U1 (SL-318.p4), Sep 19. PRE-EXISTING: identical on main 8d752a78 and on the U1 branch (behaviour-preserving), so not U1's regression. Possibly the concrete cell SL-184 (\"verify nested generics whose templates suspend unconditionally\", a todo import) was pointing at.\n\nIn the ENTRY module, a generic template instantiated from inside ANOTHER generic template's body cannot name a suspending free function: the compile fails at typecheck with `undefined function`, and two cascaded errors.\n\n```saw\nimport std.task.{yield_now}\n\nfunc outer<T>(x: Int) -> Int {\n    let r = inner<T>(x)\n    r + 1\n}\nfunc inner<T>(x: Int) -> Int {\n    let s = leaf(x)          // error: undefined function `leaf`\n    s + 10                   // error: undefined variable `s`\n}                            // error: function `inner` should return `Int` but body has no value\nfunc leaf(x: Int) -> Int { yield_now()  x + 100 }\n\nfunc chain(n: Int) -> Int { outer<Int>(n) }\nfunc main() { print(\"chain {} {}\", __saw_drive(chain(1)), __saw_drive_steps(chain(1))) }\n```\n\nTHE MATRIX (all on both compilers, byte-identical):\n- SYNC twin (`leaf` without the `yield_now`, `main` calls `outer<Int>(1)` plainly) — WORKS: `chain 112`. So ordinary monomorphization of the nested template is fine; the failure needs the SUSPENDING path.\n- the nested template suspending DIRECTLY (`inner<T>` calls `yield_now()` itself, no free callee) — WORKS: `chain 12 1`. So the clone itself is fine; what fails is resolving a FREE FUNCTION NAME from inside it.\n- the SAME two templates in an IMPORTED module (`alpha.outer<T>` → `alpha.inner<T>` → `alpha.leaf`), reached by `import alpha.{outer}` — WORKS: `chain 112 1`, frames for all three. So the entry module is the failing side, which is the opposite of the usual asymmetry.\n- renaming the two `let r` locals apart changes nothing (`s` fails the same way), so it is not a shadowing/binding collision.\n\nMECHANISM (named per obligation 4 as a hypothesis from the matrix; not yet stepped): the coroutine transform's nested generic promotion clones the INNER template's body for `inner$1$Int` when it discovers the instantiation from inside `outer$1$Int`'s body (K116's \"generic via helper\" depth, one level further — generic via GENERIC). The clone is then typechecked in a namespace that does not hold the entry module's free functions — consistent with design 104 shape 4's pristine-template capture, which is built for IMPORTED templates and resolves against the template's own module, and with the imported twin working: for an imported `inner<T>` the capture IS the right namespace, for an entry-module one it is not, and `leaf` (an entry-module free function, tagged `leaf$m$` since SL-274) is not found. The cascade (`undefined variable s`, `no value`) is the failed `let`. A direct `yield_now()` resolves because it is an imported bare name, present in every capture.\n\nNot silent — a refusal — but a wrong one about a sound program, and the message points at the author's template rather than at the promotion. In U1's terms the site was never asked of the ledger (the clone fails before discovery finishes), so it is U2 coverage input; the fix belongs with the promotion's namespace for entry-module nested clones.\n\nWorkaround today: move the templates into a module and import them bare, or make the nested template call the suspending primitive directly.\n","created":"1789845299","id":"c1"}],"created":"1789845299","description":"","id":"SL-331","labels":[],"priority":"normal","project":"SL","revision":1,"sequence":1044,"stage":"backlog","status":"open","title":"an ENTRY-module generic template instantiated from inside another template cannot name a suspending free function — undefined function at the nested clone; sync twin and imported twin both work","updated":"1789845299"},"id":"SL-331","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-331\",\"request_id\":\"pid-83948-1789845359-15000\",\"stage\":\"queued\"}","request_id":"pid-83948-1789845359-15000","sequence":1047,"timestamp":"1789845359"}
---
