---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"1790341680","comments":[{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \"oracle known wrong\" reference. Test-coverage aspect: `diagnostics.associated-type.function`.","created":"1790341680","id":"c1","kind":"landing"}],"created":"1790126447","description":"Found Sep 23 by agent:claude-sl318u3f while pinning the callable-Item row of examples/errors/borrowing_struct_type_positions.saw: declaring `type Item = () -> BagIterator` with `func next(&var self) -> (() -> BagIterator)?` reports, beside the intended borrowing-struct refusal, an unrelated mismatch about the `Item?` return's escaping stamp; the alias spelling (`type Item = Mk`) does not. Pre-existing on main: the function-type escaping stamp is compared against the requirement's `Item?` without normalizing. Repro: the bare-callable Item row in that fixture (the pinned message is the borrowing one; this second sentence is the finding). Mechanism to name before fixing (obligation 4): where the conformance check compares a method's return against a requirement whose associated type resolves to a function type.","id":"SL-351","labels":["frozen"],"order":0,"parent":"","priority":"low","project":"SL","queue_order":0,"revision":3,"sequence":2209,"stage":"backlog","status":"closed","title":"A callable associated Item (next -> (() -> T)?) draws a spurious Item? escaping-stamp mismatch beside the intended diagnostic (pre-existing; found by U3 r4's sweep)","updated":"1790341680"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790126447","description":"Found Sep 23 by agent:claude-sl318u3f while pinning the callable-Item row of examples/errors/borrowing_struct_type_positions.saw: declaring `type Item = () -> BagIterator` with `func next(&var self) -> (() -> BagIterator)?` reports, beside the intended borrowing-struct refusal, an unrelated mismatch about the `Item?` return's escaping stamp; the alias spelling (`type Item = Mk`) does not. Pre-existing on main: the function-type escaping stamp is compared against the requirement's `Item?` without normalizing. Repro: the bare-callable Item row in that fixture (the pinned message is the borrowing one; this second sentence is the finding). Mechanism to name before fixing (obligation 4): where the conformance check compares a method's return against a requirement whose associated type resolves to a function type.","id":"SL-351","labels":["frozen"],"order":0,"parent":"","priority":"low","project":"SL","queue_order":0,"revision":2,"sequence":2208,"stage":"backlog","status":"open","title":"A callable associated Item (next -> (() -> T)?) draws a spurious Item? escaping-stamp mismatch beside the intended diagnostic (pre-existing; found by U3 r4's sweep)","updated":"1790341680"},"id":"SL-351","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \\\"oracle known wrong\\\" reference. Test-coverage aspect: `diagnostics.associated-type.function`.\",\"id\":\"SL-351\",\"request_id\":\"pid-56893-1790341680-7000\"}","request_id":"pid-56893-1790341680-7000","sequence":2209,"timestamp":"1790341680"}
---
