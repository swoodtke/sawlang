---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl226","author":"agent:claude-sl222","comments":[{"author":"agent:claude-sl226","body":"Claiming SL-226 (coroutine-transform ownership batch #2, issue 1 of 3: SL-226 -> SL-234 -> SL-227). Working in isolated worktree agent-afc12e0720f35b82f. First step per the issue text: VERIFY the frame-read-produces-a-value hypothesis against the emitted IR before writing any fix, then sweep the mechanism's siblings (obligation 4).","created":"1789090649","id":"c1"},{"author":"agent:claude-sl226","body":"FIX IN on branch agent-sl226-batch2. The issue's hypothesis was RIGHT about the symptom (\"the frame read produces a VALUE where the borrow needs the field's STORAGE\") and wrong about the location: nothing in coro_transform.py is at fault, and the defect is not specific to the fixed-array deferred family. It is in CODEGEN, and it reaches plain synchronous code too.\n\nMECHANISM (confirmed against -O0 IR before any code was written). Codegen carries TWO base-to-pointer dispatches. `_get_lvalue_pointer` (calls.py) is the WRITE path's funnel and answers every base form, ForceUnwrap included. The REFERENCE path (`_generate_reference_expr`, operators.py) kept a private `_get_array_element_pointer` knowing only Identifier / `self` / MemberAccess / nested-ArrayIndex, ending in a fallback whose own comment stated the defect: \"Fallback: materialize the container (won't propagate mutations)\". The transform rewrites a frame-resident local `arr` into `self.arr!` -- a ForceUnwrap base -- so every `&var arr[i]` in a suspending body fell into that fallback. The emitted -O0 IR is the whole bug in four instructions:\n\n  %unwrapped = extractvalue {i1, [3 x Doc]} %arr, 1\n  store [3 x Doc] %unwrapped, [3 x Doc]* %arr_tmp\n  %elem_ptr = getelementptr [3 x Doc], [3 x Doc]* %arr_tmp, i64 0, i64 1\n  call void @set_one(Doc* %elem_ptr, i64 %k)\n\nThe struct FIELD control was correct for the reason the matrix predicts, not a special case: a MemberAccess base is one of the four arms the private dispatch did have.\n\nSWEEP (obligation 4; sync twin is the spec, every cell measured, driven vs sync):\n  struct element, &var, direct        sync 8 / driven 1  BROKEN\n  struct element, &var, forwarded     sync 8 / driven 1  BROKEN\n  scalar element, &var                sync 8 / driven 1  BROKEN\n  nested array element, &var          sync 8 / driven 1  BROKEN\n  struct element, &var self receiver  sync 8 / driven 8  ok (goes through the WRITE funnel)\n  struct element, & shared read       sync 5 / driven 5  ok (a copy reads the same bytes)\n  struct FIELD control                sync 8 / driven 8  ok\n\nThe mechanism also reaches code with NO SUSPENSION, which is filed as SL-261 and fixed by the same change: `&var o![1]` (optional base) and `&var t.0[1]` (tuple base) are silent lost writes in ordinary sync code, and `&var a[1]` where `a: &var [T; N]` was an INTERNAL COMPILER ERROR (`Type of #1 arg mismatch: %\"Doc\"* != [3 x %\"Doc\"]*`) -- the reference-typed binding's alloca already holds a pointer, so the element GEP strode by whole arrays.\n\nFIX, at the mechanism's altitude and one line of behavior: `_generate_reference_expr`'s ArrayIndex arm now calls `_get_element_pointer`, which composes through `_get_lvalue_pointer`; the private `_get_array_element_pointer` is DELETED (it had exactly two callers, both in the reference path). One funnel now answers both families, so a borrow and a write of one place cannot disagree about where that place is. Per obligation 1 the funnel's docstring now NAMES its entry points.\n\nTESTS: examples/coro_ref_into_frame_resident_fixed_array.saw XFAIL REMOVED and extended from 3 cells to the 7-row matrix above, each printing its sync twin beside its driven result so a divergence is what fails. New examples/ref_into_array_element_addresses_real_storage.saw pins SL-261's seven non-coroutine cells, including the two bases that already worked so the fix is pinned as a widening rather than a swap. Gates to follow on the branch.\n","created":"1789096362","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-226.p1 (revision 1): coro/codegen ownership batch #2: SL-226 (array-element ptr funnel + SL-261 sync faces) / SL-234 (head-move relinquish + leaf refusal) / SL-227 (spawn roots through recursion check)","created":"1789105233","id":"c3"}],"created":"1789042062","description":"A `&var arr[i]` argument, where `arr` is a `[T; N]` LOCAL of a body that suspends, mutates a copy: the callee's write is discarded and the element reads back unchanged. Silent -- exit 0, no diagnostic. The SYNC twin of the same code is correct, and a frame-resident STRUCT FIELD (`&var h.doc`) is correct too, so the defect is specific to the fixed-array element read in the coroutine path.\n\nRepro (single file, no modules), compiled with main 88fa56ba / d0e38f90:\n\n  import std.task.{yield_now}\n  struct Doc { n: Int }\n  func slow() -> Int { yield_now()  7 }\n  func set_one(d: &var Doc, k: Int) { d.n = d.n + k }\n  func driven() -> Int {\n      var arr: [Doc; 3] = [Doc(n: 0); 3]\n      let k = slow()\n      set_one(&var arr[1], k)\n      arr[1].n\n  }\n  func sync_control() -> Int {\n      var arr: [Doc; 3] = [Doc(n: 0); 3]\n      set_one(&var arr[1], 7)\n      arr[1].n\n  }\n  func main() { print(__saw_drive(driven()))  print(sync_control()) }\n\nObserved: 0 then 7. Wanted: 7 then 7.\n\nNote the suspending call is HOISTED into its own `let` here, so no ANF lift is involved -- the shape is plain 'frame-resident fixed array, borrow one element, call through it'.\n\nMECHANISM (hypothesis, not yet confirmed against the IR): `arr` is frame-resident and lands on design 218's legacy 'fixed-array' deferred family rather than a `Slot<T>`, and `_address_taken` records it. The suspicion is that the frame READ for that family produces a VALUE (a copy of the array) where `&var arr[1]` needs the field's storage, so the address handed to the callee points into the copy. A struct field's read does not go that way, which is why `&var h.doc` is correct in the same body.\n\nFound while sweeping SL-223's sibling shapes (obligation 4). It is INDEPENDENT of SL-223: it reproduces on unmodified main with no ANF lift in the picture, and SL-223's fix neither causes nor cures it. SL-223's branch therefore pins only the ORDER of the impure-index cell, not its value, and cites this issue.","id":"SL-226","labels":["bug","coroutine-transform"],"priority":"high","project":"SL","revision":6,"sequence":524,"stage":"queued","status":"open","title":"Driven body: a &var into a frame-resident fixed-array element is a silently lost write","updated":"1789105233"},"before":{"assignee":"agent:claude-sl226","author":"agent:claude-sl222","comments":[{"author":"agent:claude-sl226","body":"Claiming SL-226 (coroutine-transform ownership batch #2, issue 1 of 3: SL-226 -> SL-234 -> SL-227). Working in isolated worktree agent-afc12e0720f35b82f. First step per the issue text: VERIFY the frame-read-produces-a-value hypothesis against the emitted IR before writing any fix, then sweep the mechanism's siblings (obligation 4).","created":"1789090649","id":"c1"},{"author":"agent:claude-sl226","body":"FIX IN on branch agent-sl226-batch2. The issue's hypothesis was RIGHT about the symptom (\"the frame read produces a VALUE where the borrow needs the field's STORAGE\") and wrong about the location: nothing in coro_transform.py is at fault, and the defect is not specific to the fixed-array deferred family. It is in CODEGEN, and it reaches plain synchronous code too.\n\nMECHANISM (confirmed against -O0 IR before any code was written). Codegen carries TWO base-to-pointer dispatches. `_get_lvalue_pointer` (calls.py) is the WRITE path's funnel and answers every base form, ForceUnwrap included. The REFERENCE path (`_generate_reference_expr`, operators.py) kept a private `_get_array_element_pointer` knowing only Identifier / `self` / MemberAccess / nested-ArrayIndex, ending in a fallback whose own comment stated the defect: \"Fallback: materialize the container (won't propagate mutations)\". The transform rewrites a frame-resident local `arr` into `self.arr!` -- a ForceUnwrap base -- so every `&var arr[i]` in a suspending body fell into that fallback. The emitted -O0 IR is the whole bug in four instructions:\n\n  %unwrapped = extractvalue {i1, [3 x Doc]} %arr, 1\n  store [3 x Doc] %unwrapped, [3 x Doc]* %arr_tmp\n  %elem_ptr = getelementptr [3 x Doc], [3 x Doc]* %arr_tmp, i64 0, i64 1\n  call void @set_one(Doc* %elem_ptr, i64 %k)\n\nThe struct FIELD control was correct for the reason the matrix predicts, not a special case: a MemberAccess base is one of the four arms the private dispatch did have.\n\nSWEEP (obligation 4; sync twin is the spec, every cell measured, driven vs sync):\n  struct element, &var, direct        sync 8 / driven 1  BROKEN\n  struct element, &var, forwarded     sync 8 / driven 1  BROKEN\n  scalar element, &var                sync 8 / driven 1  BROKEN\n  nested array element, &var          sync 8 / driven 1  BROKEN\n  struct element, &var self receiver  sync 8 / driven 8  ok (goes through the WRITE funnel)\n  struct element, & shared read       sync 5 / driven 5  ok (a copy reads the same bytes)\n  struct FIELD control                sync 8 / driven 8  ok\n\nThe mechanism also reaches code with NO SUSPENSION, which is filed as SL-261 and fixed by the same change: `&var o![1]` (optional base) and `&var t.0[1]` (tuple base) are silent lost writes in ordinary sync code, and `&var a[1]` where `a: &var [T; N]` was an INTERNAL COMPILER ERROR (`Type of #1 arg mismatch: %\"Doc\"* != [3 x %\"Doc\"]*`) -- the reference-typed binding's alloca already holds a pointer, so the element GEP strode by whole arrays.\n\nFIX, at the mechanism's altitude and one line of behavior: `_generate_reference_expr`'s ArrayIndex arm now calls `_get_element_pointer`, which composes through `_get_lvalue_pointer`; the private `_get_array_element_pointer` is DELETED (it had exactly two callers, both in the reference path). One funnel now answers both families, so a borrow and a write of one place cannot disagree about where that place is. Per obligation 1 the funnel's docstring now NAMES its entry points.\n\nTESTS: examples/coro_ref_into_frame_resident_fixed_array.saw XFAIL REMOVED and extended from 3 cells to the 7-row matrix above, each printing its sync twin beside its driven result so a divergence is what fails. New examples/ref_into_array_element_addresses_real_storage.saw pins SL-261's seven non-coroutine cells, including the two bases that already worked so the fix is pinned as a widening rather than a swap. Gates to follow on the branch.\n","created":"1789096362","id":"c2"}],"created":"1789042062","description":"A `&var arr[i]` argument, where `arr` is a `[T; N]` LOCAL of a body that suspends, mutates a copy: the callee's write is discarded and the element reads back unchanged. Silent -- exit 0, no diagnostic. The SYNC twin of the same code is correct, and a frame-resident STRUCT FIELD (`&var h.doc`) is correct too, so the defect is specific to the fixed-array element read in the coroutine path.\n\nRepro (single file, no modules), compiled with main 88fa56ba / d0e38f90:\n\n  import std.task.{yield_now}\n  struct Doc { n: Int }\n  func slow() -> Int { yield_now()  7 }\n  func set_one(d: &var Doc, k: Int) { d.n = d.n + k }\n  func driven() -> Int {\n      var arr: [Doc; 3] = [Doc(n: 0); 3]\n      let k = slow()\n      set_one(&var arr[1], k)\n      arr[1].n\n  }\n  func sync_control() -> Int {\n      var arr: [Doc; 3] = [Doc(n: 0); 3]\n      set_one(&var arr[1], 7)\n      arr[1].n\n  }\n  func main() { print(__saw_drive(driven()))  print(sync_control()) }\n\nObserved: 0 then 7. Wanted: 7 then 7.\n\nNote the suspending call is HOISTED into its own `let` here, so no ANF lift is involved -- the shape is plain 'frame-resident fixed array, borrow one element, call through it'.\n\nMECHANISM (hypothesis, not yet confirmed against the IR): `arr` is frame-resident and lands on design 218's legacy 'fixed-array' deferred family rather than a `Slot<T>`, and `_address_taken` records it. The suspicion is that the frame READ for that family produces a VALUE (a copy of the array) where `&var arr[1]` needs the field's storage, so the address handed to the callee points into the copy. A struct field's read does not go that way, which is why `&var h.doc` is correct in the same body.\n\nFound while sweeping SL-223's sibling shapes (obligation 4). It is INDEPENDENT of SL-223: it reproduces on unmodified main with no ANF lift in the picture, and SL-223's fix neither causes nor cures it. SL-223's branch therefore pins only the ORDER of the impure-index cell, not its value, and cites this issue.","id":"SL-226","labels":["bug","coroutine-transform"],"priority":"high","project":"SL","revision":5,"sequence":470,"stage":"queued","status":"open","title":"Driven body: a &var into a frame-resident fixed-array element is a silently lost write","updated":"1789096362"},"id":"SL-226","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/examples/coro_head_move_with_early_exit_drops_once.saw b/examples/coro_head_move_with_early_exit_drops_once.saw\\nindex 3eab6e01..df2979a4 100644\\n--- a/examples/coro_head_move_with_early_exit_drops_once.saw\\n+++ b/examples/coro_head_move_with_early_exit_drops_once.saw\\n@@ -1,46 +1,92 @@\\n-// XFAIL: SL-234 a `move` in a `guard let` / `for` / `while` HEAD is dropped\\n-// TWICE when a contained block exits early — the callee destroys the value, then\\n-// coroutine frame teardown destroys it again. Silent: exit 0, no diagnostic.\\n+// SL-234: a `move` in a `guard let`, `for` or `while` HEAD relinquishes the\\n+// frame's claim EXACTLY ONCE — so the value is destroyed once when a contained\\n+// block exits early, and a block that REINITIALIZES the local keeps its\\n+// replacement's claim.\\n //\\n // The sibling of `coro_head_move_relinquishes_before_early_exit.saw`, which pins\\n-// the four heads SL-222 fixed. Same mechanism: `_lower_inplace` emits a\\n-// statement's head-expression move-forgets AFTER the whole statement, which\\n-// every `return`/`break`/`continue` inside its blocks leaves before. The fix is\\n-// one call to `_FrameBuilder._relinquish_before_exits(forgets, blocks)` per arm,\\n-// which already exists in the tree — the `GuardLetStatement`, `ForLoop` and\\n-// `WhileExpr` arms are the three that still need it.\\n+// the four heads SL-222 fixed (inline `try`, `if`, `if let`, `match`). These are\\n+// the three that were left: they emitted the move's drop-flag clear AFTER the\\n+// whole statement, which every `return`/`break`/`continue` inside their blocks\\n+// leaves before — the callee destroyed the value, then frame teardown destroyed\\n+// it again. Silent: exit 0, no diagnostic.\\n //\\n-// PRE-EXISTING in every syntactic position: measured on unpatched origin/main,\\n-// where all three double-drop both as a block's tail and with a trailing\\n-// statement after them, so SL-222 neither caused nor cured them. Left unfixed\\n-// deliberately, per the reviewer's scoping rule for pre-existing findings.\\n+// All three now take `_hoist_head_and_relinquish`: the head is bound to a temp,\\n+// the claim is given up between it and the container, and there is NO trailing\\n+// clear — which is what also fixes the reinit half, since a trailing clear would\\n+// erase a claim a block re-established.\\n //\\n-// The oracle is the exact drop count — each cell must print ONE `drop` per `Res`\\n-// it builds, and today prints two.\\n+// WHY `while` IS SOUND HERE, the one head that re-evaluates: the hoist fires\\n+// only when the head contains a `move` of a frame local, and such a condition\\n+// cannot run twice soundly — the second evaluation would consume a value the\\n+// first gave away. For every program in which the move is sound the condition\\n+// runs exactly once, so the hoist is observationally the same loop. (A body that\\n+// genuinely falls back through such a condition is unsound before the transform\\n+// sees it: the SYNC twin destroys one value once per iteration. That is a\\n+// move-checker hole, tracked separately.)\\n //\\n-// Remove the marker in the landing that fixes SL-234.\\n-// The last two cells are the same mechanism at a LEAF statement rather than a\\n-// container: a `let` initializer and an assignment RHS whose value `if` moves in\\n-// its condition and `return`s from an arm. Both also double-drop on unpatched\\n-// origin/main, so they belong to SL-234 too and flip with it.\\n+// THE ORACLE IS THE SYNC TWIN: every cell runs the same body without a\\n+// suspension, and the two destruction sequences must be identical. Drops are\\n+// NAMED, so a doubled `old` and a leaked `new` are distinguishable rather than\\n+// just counted.\\n+//\\n+// The two LEAF cells this file used to carry — a `let` initializer and an\\n+// assignment RHS whose value `if` moves in its condition and returns from an arm\\n+// — are REFUSALS now, not fixes: a leaf statement's blocks are operands and one\\n+// can sit to the LEFT of the move, so no placement is expressible. They are\\n+// pinned by examples/errors/coro_let_init_move_beside_early_exit.saw and\\n+// examples/errors/coro_assign_rhs_move_beside_early_exit.saw.\\n+// (The output is deliberately FLAT — `EXPECT-OUTPUT` strips each expected line,\\n+// so indented output cannot be matched by it.)\\n // EXPECT: success\\n // EXPECT-OUTPUT:\\n-// drop\\n-// 1\\n-// drop\\n-// 2\\n-// drop\\n-// 3\\n-// drop\\n-// 4\\n-// drop\\n-// 5\\n+// guard-exit sync\\n+// drop old\\n+// -> 1\\n+// guard-exit driven\\n+// drop old\\n+// -> 1\\n+// for-exit sync\\n+// drop old\\n+// -> 2\\n+// for-exit driven\\n+// drop old\\n+// -> 2\\n+// while-exit sync\\n+// drop old\\n+// -> 3\\n+// while-exit driven\\n+// drop old\\n+// -> 3\\n+// guard-reinit sync\\n+// drop old\\n+// drop new\\n+// -> 4\\n+// guard-reinit driven\\n+// drop old\\n+// drop new\\n+// -> 4\\n+// for-reinit sync\\n+// drop old\\n+// drop new\\n+// -> 5\\n+// for-reinit driven\\n+// drop old\\n+// drop new\\n+// -> 5\\n+// while-reinit sync\\n+// drop old\\n+// drop new\\n+// -> 6\\n+// while-reinit driven\\n+// drop old\\n+// drop new\\n+// -> 6\\n \\n import std.task.{yield_now}\\n \\n struct Res { name: String }\\n extension Res: NoCopy {\\n-    func deinit(&var self) { print(\\\"drop\\\") }\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n }\\n \\n func note(r: &Res) -> Int { r.name.len() }\\n@@ -49,63 +95,158 @@ func consume_none(r: Res) -> Int? { None }\\n func consume_vec(r: Res) -> Vector<Int> { [1] }\\n func consume_bool(r: Res) -> Bool { true }\\n \\n-func guard_head(items: Vector<Int>) -> Int {\\n+// ---- a contained block EXITS EARLY: the value dies once, in the callee ----\\n+\\n+func guard_exit_sync(items: Vector<Int>) -> Int {\\n+    for i in items.iter() {\\n+        var r = Res(name: \\\"old\\\")\\n+        if note(&r) + i < 0 { return -1 }\\n+        guard let got = consume_none(move r) else { return 1 }\\n+    }\\n+    0\\n+}\\n+func guard_exit_driven(items: Vector<Int>) -> Int {\\n     yield_now()\\n     for i in items.iter() {\\n-        var r = Res(name: \\\"item\\\")\\n+        var r = Res(name: \\\"old\\\")\\n         if note(&r) + i < 0 { return -1 }\\n         guard let got = consume_none(move r) else { return 1 }\\n     }\\n     0\\n }\\n \\n-func for_head(items: Vector<Int>) -> Int {\\n+func for_exit_sync(items: Vector<Int>) -> Int {\\n+    for i in items.iter() {\\n+        var r = Res(name: \\\"old\\\")\\n+        if note(&r) + i < 0 { return -1 }\\n+        for j in consume_vec(move r).iter() { return 2 }\\n+    }\\n+    0\\n+}\\n+func for_exit_driven(items: Vector<Int>) -> Int {\\n     yield_now()\\n     for i in items.iter() {\\n-        var r = Res(name: \\\"item\\\")\\n+        var r = Res(name: \\\"old\\\")\\n         if note(&r) + i < 0 { return -1 }\\n         for j in consume_vec(move r).iter() { return 2 }\\n     }\\n     0\\n }\\n \\n-func while_head(items: Vector<Int>) -> Int {\\n+func while_exit_sync(items: Vector<Int>) -> Int {\\n+    for i in items.iter() {\\n+        var r = Res(name: \\\"old\\\")\\n+        if note(&r) + i < 0 { return -1 }\\n+        while consume_bool(move r) { return 3 }\\n+    }\\n+    0\\n+}\\n+func while_exit_driven(items: Vector<Int>) -> Int {\\n     yield_now()\\n     for i in items.iter() {\\n-        var r = Res(name: \\\"item\\\")\\n+        var r = Res(name: \\\"old\\\")\\n         if note(&r) + i < 0 { return -1 }\\n         while consume_bool(move r) { return 3 }\\n     }\\n     0\\n }\\n \\n-func let_value_if(items: Vector<Int>) -> Int {\\n+// ---- a contained block REINITIALIZES the local: the replacement still dies ----\\n+\\n+func guard_reinit_sync(items: Vector<Int>) -> Int {\\n+    for i in items.iter() {\\n+        var r = Res(name: \\\"old\\\")\\n+        if note(&r) + i < 0 { return -1 }\\n+        guard let got = consume_none(move r) else {\\n+            r = Res(name: \\\"new\\\")\\n+            continue\\n+        }\\n+    }\\n+    4\\n+}\\n+func guard_reinit_driven(items: Vector<Int>) -> Int {\\n+    yield_now()\\n+    for i in items.iter() {\\n+        var r = Res(name: \\\"old\\\")\\n+        if note(&r) + i < 0 { return -1 }\\n+        guard let got = consume_none(move r) else {\\n+            r = Res(name: \\\"new\\\")\\n+            continue\\n+        }\\n+    }\\n+    4\\n+}\\n+\\n+func for_reinit_sync(items: Vector<Int>) -> Int {\\n+    for i in items.iter() {\\n+        var r = Res(name: \\\"old\\\")\\n+        if note(&r) + i < 0 { return -1 }\\n+        for j in consume_vec(move r).iter() { r = Res(name: \\\"new\\\") }\\n+    }\\n+    5\\n+}\\n+func for_reinit_driven(items: Vector<Int>) -> Int {\\n     yield_now()\\n-    var acc = 0\\n     for i in items.iter() {\\n-        var r = Res(name: \\\"item\\\")\\n+        var r = Res(name: \\\"old\\\")\\n         if note(&r) + i < 0 { return -1 }\\n-        let v = if consume_bool(move r) { return 4 } else { 0 }\\n-        acc = acc + v\\n+        for j in consume_vec(move r).iter() { r = Res(name: \\\"new\\\") }\\n     }\\n-    acc\\n+    5\\n }\\n \\n-func assign_value_if(items: Vector<Int>) -> Int {\\n+func while_reinit_sync(items: Vector<Int>) -> Int {\\n+    for i in items.iter() {\\n+        var r = Res(name: \\\"old\\\")\\n+        if note(&r) + i < 0 { return -1 }\\n+        while consume_bool(move r) {\\n+            r = Res(name: \\\"new\\\")\\n+            break\\n+        }\\n+    }\\n+    6\\n+}\\n+func while_reinit_driven(items: Vector<Int>) -> Int {\\n     yield_now()\\n-    var acc = 0\\n     for i in items.iter() {\\n-        var r = Res(name: \\\"item\\\")\\n+        var r = Res(name: \\\"old\\\")\\n         if note(&r) + i < 0 { return -1 }\\n-        acc = if consume_bool(move r) { return 5 } else { 0 }\\n+        while consume_bool(move r) {\\n+            r = Res(name: \\\"new\\\")\\n+            break\\n+        }\\n     }\\n-    acc\\n+    6\\n }\\n \\n func main() {\\n-    print(__saw_drive(guard_head([1, 2])))      // drop, 1\\n-    print(__saw_drive(for_head([1, 2])))        // drop, 2\\n-    print(__saw_drive(while_head([1, 2])))      // drop, 3\\n-    print(__saw_drive(let_value_if([1, 2])))    // drop, 4\\n-    print(__saw_drive(assign_value_if([1, 2]))) // drop, 5\\n+    print(\\\"guard-exit sync\\\")\\n+    print(\\\"-> {guard_exit_sync([1, 2])}\\\")\\n+    print(\\\"guard-exit driven\\\")\\n+    print(\\\"-> {__saw_drive(guard_exit_driven([1, 2]))}\\\")\\n+\\n+    print(\\\"for-exit sync\\\")\\n+    print(\\\"-> {for_exit_sync([1, 2])}\\\")\\n+    print(\\\"for-exit driven\\\")\\n+    print(\\\"-> {__saw_drive(for_exit_driven([1, 2]))}\\\")\\n+\\n+    print(\\\"while-exit sync\\\")\\n+    print(\\\"-> {while_exit_sync([1, 2])}\\\")\\n+    print(\\\"while-exit driven\\\")\\n+    print(\\\"-> {__saw_drive(while_exit_driven([1, 2]))}\\\")\\n+\\n+    print(\\\"guard-reinit sync\\\")\\n+    print(\\\"-> {guard_reinit_sync([1])}\\\")\\n+    print(\\\"guard-reinit driven\\\")\\n+    print(\\\"-> {__saw_drive(guard_reinit_driven([1]))}\\\")\\n+\\n+    print(\\\"for-reinit sync\\\")\\n+    print(\\\"-> {for_reinit_sync([1])}\\\")\\n+    print(\\\"for-reinit driven\\\")\\n+    print(\\\"-> {__saw_drive(for_reinit_driven([1]))}\\\")\\n+\\n+    print(\\\"while-reinit sync\\\")\\n+    print(\\\"-> {while_reinit_sync([1])}\\\")\\n+    print(\\\"while-reinit driven\\\")\\n+    print(\\\"-> {__saw_drive(while_reinit_driven([1]))}\\\")\\n }\\ndiff --git a/examples/coro_ref_into_frame_resident_fixed_array.saw b/examples/coro_ref_into_frame_resident_fixed_array.saw\\nindex 3526c69e..eea9d5e1 100644\\n--- a/examples/coro_ref_into_frame_resident_fixed_array.saw\\n+++ b/examples/coro_ref_into_frame_resident_fixed_array.saw\\n@@ -1,23 +1,36 @@\\n-// XFAIL: SL-226 a `&var` into an element of a frame-resident `[T; N]` local is a\\n-// silently lost write in a driven body — the callee mutates a copy, the element\\n-// reads back unchanged, exit 0, no diagnostic.\\n+// SL-226: a `&`/`&var` into an element of a frame-resident `[T; N]` local\\n+// addresses the ELEMENT'S REAL SLOT, so a callee's write lands where the\\n+// element lives and reads back through the local.\\n //\\n-// Found by SL-223's sibling sweep (obligation 4) and INDEPENDENT of it: the\\n-// suspending call is hoisted into its own `let` here, so no ANF lift touches the\\n-// borrow, and the same program miscompiles on a compiler with none of the\\n-// SL-222/223/224 changes. What discriminates is the STORAGE, not the call\\n-// shape — `sync_control` writes the same element through the same helper and is\\n-// correct, and `field_control` shows a frame-resident struct FIELD is correct\\n-// too, so the defect is specific to the fixed-array element read in the\\n-// coroutine path (the suspicion is design 218's legacy `fixed-array` deferred\\n-// family reading the field as a VALUE where the borrow needs its storage).\\n+// It used to mutate a COPY — the callee wrote a temporary that died at the end\\n+// of the statement and the element read back unchanged, exit 0, no diagnostic.\\n+// What discriminated was the STORAGE, not the call shape: the sync twin of\\n+// every cell below was correct, and so was a frame-resident struct FIELD\\n+// (`&var h.doc`), which is why the field control is kept as a row.\\n //\\n-// Remove the marker in the landing that fixes SL-226.\\n+// MECHANISM (and why the fix is one line): codegen had TWO base-to-pointer\\n+// dispatches. `_get_lvalue_pointer` — the write path — knows every base form,\\n+// including a ForceUnwrap. The reference path kept a private\\n+// `_get_array_element_pointer` that knew only Identifier/`self`/MemberAccess/\\n+// nested-ArrayIndex and MATERIALIZED A COPY for anything else. The coroutine\\n+// transform rewrites a frame local `arr` into `self.arr!`, which is exactly the\\n+// ForceUnwrap base the private dispatch had never learned — so a BORROW and a\\n+// WRITE of one place disagreed about where that place is. The reference path\\n+// now routes through the same funnel; the non-coroutine faces of the same\\n+// defect are pinned by `ref_into_array_element_addresses_real_storage.saw`.\\n+//\\n+// THE ORACLE IS THE SYNC TWIN: every cell prints its sync result beside its\\n+// driven result, and the two must agree. A cell whose numbers differ is the bug\\n+// back.\\n // EXPECT: success\\n // EXPECT-OUTPUT:\\n-// 7\\n-// 7\\n-// 8\\n+// direct    8 8\\n+// forwarded 8 8\\n+// scalar    8 8\\n+// nested    8 8\\n+// receiver  8 8\\n+// shared    5 5\\n+// field     8 8\\n \\n import std.task.{yield_now}\\n \\n@@ -25,27 +38,108 @@ struct Doc { n: Int }\\n \\n struct Holder { doc: Doc }\\n \\n+extension Doc {\\n+    func bump(&var self, k: Int) { self.n = self.n + k }\\n+}\\n+\\n func slow() -> Int {\\n     yield_now()\\n     7\\n }\\n \\n func set_one(d: &var Doc, k: Int) { d.n = d.n + k }\\n+func read_one(d: &Doc) -> Int { d.n }\\n+func set_int(n: &var Int, k: Int) { n = n + k }\\n+func relay(d: &var Doc, k: Int) { set_one(&var d, k) }\\n \\n-func driven() -> Int {\\n-    var arr: [Doc; 3] = [Doc(n: 0); 3]\\n+// A struct element, borrowed exclusively and written directly — the found bug.\\n+func sync_direct() -> Int {\\n+    var arr: [Doc; 3] = [Doc(n: 1); 3]\\n+    set_one(&var arr[1], 7)\\n+    arr[1].n\\n+}\\n+func driven_direct() -> Int {\\n+    var arr: [Doc; 3] = [Doc(n: 1); 3]\\n     let k = slow()\\n     set_one(&var arr[1], k)\\n     arr[1].n\\n }\\n \\n-func sync_control() -> Int {\\n-    var arr: [Doc; 3] = [Doc(n: 0); 3]\\n-    set_one(&var arr[1], 7)\\n+// The same borrow FORWARDED onward as a re-borrow (design 106).\\n+func sync_forwarded() -> Int {\\n+    var arr: [Doc; 3] = [Doc(n: 1); 3]\\n+    relay(&var arr[1], 7)\\n+    arr[1].n\\n+}\\n+func driven_forwarded() -> Int {\\n+    var arr: [Doc; 3] = [Doc(n: 1); 3]\\n+    let k = slow()\\n+    relay(&var arr[1], k)\\n     arr[1].n\\n }\\n \\n-func field_control() -> Int {\\n+// A SCALAR element, written through whole-referent replacement (design 110).\\n+func sync_scalar() -> Int {\\n+    var nums: [Int; 3] = [1; 3]\\n+    set_int(&var nums[1], 7)\\n+    nums[1]\\n+}\\n+func driven_scalar() -> Int {\\n+    var nums: [Int; 3] = [1; 3]\\n+    let k = slow()\\n+    set_int(&var nums[1], k)\\n+    nums[1]\\n+}\\n+\\n+// A NESTED array, where the outer element is itself the container.\\n+func sync_nested() -> Int {\\n+    var grid: [[Doc; 2]; 2] = [[Doc(n: 1); 2]; 2]\\n+    set_one(&var grid[1][0], 7)\\n+    grid[1][0].n\\n+}\\n+func driven_nested() -> Int {\\n+    var grid: [[Doc; 2]; 2] = [[Doc(n: 1); 2]; 2]\\n+    let k = slow()\\n+    set_one(&var grid[1][0], k)\\n+    grid[1][0].n\\n+}\\n+\\n+// A `&var self` METHOD RECEIVER on the element — the one borrowing spelling\\n+// that was already correct, because it went through the write path's funnel.\\n+func sync_receiver() -> Int {\\n+    var arr: [Doc; 3] = [Doc(n: 1); 3]\\n+    arr[1].bump(7)\\n+    arr[1].n\\n+}\\n+func driven_receiver() -> Int {\\n+    var arr: [Doc; 3] = [Doc(n: 1); 3]\\n+    let k = slow()\\n+    arr[1].bump(k)\\n+    arr[1].n\\n+}\\n+\\n+// A SHARED read borrow. It read the right bytes even when broken (a copy has\\n+// the same contents), so it pins that the fix did not disturb the read side.\\n+func sync_shared() -> Int {\\n+    var arr: [Doc; 3] = [Doc(n: 1); 3]\\n+    arr[1].n = 5\\n+    read_one(&arr[1])\\n+}\\n+func driven_shared() -> Int {\\n+    var arr: [Doc; 3] = [Doc(n: 1); 3]\\n+    let k = slow()\\n+    arr[1].n = k - 2\\n+    read_one(&arr[1])\\n+}\\n+\\n+// The frame-resident struct FIELD control: correct throughout, kept as the row\\n+// that showed the defect was about the fixed-array element specifically.\\n+func sync_field() -> Int {\\n+    var h = Holder(doc: Doc(n: 1))\\n+    set_one(&var h.doc, 7)\\n+    h.doc.n\\n+}\\n+func driven_field() -> Int {\\n     var h = Holder(doc: Doc(n: 1))\\n     let k = slow()\\n     set_one(&var h.doc, k)\\n@@ -53,7 +147,11 @@ func field_control() -> Int {\\n }\\n \\n func main() {\\n-    print(__saw_drive(driven()))         // wanted 7, observed 0\\n-    print(sync_control())                // 7 — the sync twin is correct\\n-    print(__saw_drive(field_control()))  // 8 — a frame-resident FIELD is correct\\n+    print(\\\"direct    {sync_direct()} {__saw_drive(driven_direct())}\\\")\\n+    print(\\\"forwarded {sync_forwarded()} {__saw_drive(driven_forwarded())}\\\")\\n+    print(\\\"scalar    {sync_scalar()} {__saw_drive(driven_scalar())}\\\")\\n+    print(\\\"nested    {sync_nested()} {__saw_drive(driven_nested())}\\\")\\n+    print(\\\"receiver  {sync_receiver()} {__saw_drive(driven_receiver())}\\\")\\n+    print(\\\"shared    {sync_shared()} {__saw_drive(driven_shared())}\\\")\\n+    print(\\\"field     {sync_field()} {__saw_drive(driven_field())}\\\")\\n }\\ndiff --git a/examples/errors/coro_assign_rhs_move_beside_early_exit.saw b/examples/errors/coro_assign_rhs_move_beside_early_exit.saw\\nnew file mode 100644\\nindex 00000000..c84bf47c\\n--- /dev/null\\n+++ b/examples/errors/coro_assign_rhs_move_beside_early_exit.saw\\n@@ -0,0 +1,41 @@\\n+// SL-234: a `move` of a frame local in an ASSIGNMENT'S RIGHT-HAND SIDE whose\\n+// expression also exits early is refused rather than miscompiled.\\n+//\\n+// The assignment twin of `coro_let_init_move_beside_early_exit.saw`; the two are\\n+// the LEAF arms of one rule, refused through one funnel\\n+// (`_refuse_buried_early_exit`) alongside SL-222 r3's plain-statement shape in\\n+// `coro_buried_early_exit_beside_a_move.saw`. See the `let` file for why a leaf\\n+// position can state no ordering between its operands and the move.\\n+//\\n+// The message names the AUTHOR'S assignment target, `acc` — the frame rewrite\\n+// would otherwise have spelled it `self.acc`, a name they did not write.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: the right-hand side of the assignment to `acc`\\n+// EXPECT-ERROR-CONTAINS: whose expression also `return`s, `break`s or `continue`s\\n+// EXPECT-ERROR-CONTAINS: bind the moving call to its own `let` first\\n+\\n+import std.task.{yield_now}\\n+\\n+struct Res { name: String }\\n+extension Res: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop\\\") }\\n+}\\n+\\n+func note(r: &Res) -> Int { r.name.len() }\\n+\\n+func consume_bool(r: Res) -> Bool { true }\\n+\\n+func run(items: Vector<Int>) -> Int {\\n+    yield_now()\\n+    var acc = 0\\n+    for i in items.iter() {\\n+        var r = Res(name: \\\"item\\\")\\n+        if note(&r) + i < 0 { return -1 }\\n+        acc = if consume_bool(move r) { return 5 } else { 0 }\\n+    }\\n+    acc\\n+}\\n+\\n+func main() {\\n+    print(__saw_drive(run([1, 2])))\\n+}\\ndiff --git a/examples/errors/coro_cross_module_suspending_recursion.saw b/examples/errors/coro_cross_module_suspending_recursion.saw\\nnew file mode 100644\\nindex 00000000..6beabdae\\n--- /dev/null\\n+++ b/examples/errors/coro_cross_module_suspending_recursion.saw\\n@@ -0,0 +1,33 @@\\n+// SL-227, the CROSS-MODULE row: a suspending recursive function reached through\\n+// an import gets the same cycle rejection as one in the entry module.\\n+//\\n+// The callee is SPLICED into the entry closure (design 210 / SL-208), so a frame\\n+// really is built for it and the cycle really would be embedded by value —\\n+// `RecursionError` before the fix, like every other row here.\\n+//\\n+// THE CONTRAST THAT MATTERS is a cross-module callee the splice DECLINES to\\n+// take: no frame is built for it, its cycle is never embedded, and it must keep\\n+// compiling. That is blade's own dependency resolver — `visit` recurses into a\\n+// dependency's dependencies and reaches the cooperative `Command.run`, so the\\n+// cycle is in the suspending-call graph and reachable from blade's suspending\\n+// `main`, while no `__Frame_visit` is ever built. SL-227's first attempt asked\\n+// the cycle question of the raw graph and refused to compile the package\\n+// manager; the `freestanding` lane caught it. The check now asks whether every\\n+// frame on the cycle will actually be BUILT, which is the same question\\n+// `_build_sub_frame` would have answered by diverging — so this file and a\\n+// building blade are the two sides of one rule, and the blade side is gated by\\n+// the `freestanding` and `bootstrap` lanes.\\n+// (`{TESTDIR}` is this file's own directory, so the module dir — which lives\\n+// beside the success tests — is reached with `..`.)\\n+// COMPILE-FLAGS: --module-path recursion227={TESTDIR}/../modules/recursion227\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: suspending recursion is not allowed\\n+// EXPECT-ERROR-CONTAINS: descend -> descend\\n+\\n+import std.task.{yield_now}\\n+import recursion227.{descend}\\n+\\n+func main() {\\n+    yield_now()\\n+    print(\\\"depth {descend(3)}\\\")\\n+}\\ndiff --git a/examples/errors/coro_group_spawn_suspending_recursion.saw b/examples/errors/coro_group_spawn_suspending_recursion.saw\\nnew file mode 100644\\nindex 00000000..5c975ffe\\n--- /dev/null\\n+++ b/examples/errors/coro_group_spawn_suspending_recursion.saw\\n@@ -0,0 +1,25 @@\\n+// SL-227, the `group.spawn` row of the root-seeding matrix: a spawn into an\\n+// explicit TaskGroup takes the same cycle rejection `Task.spawn` and\\n+// `__saw_drive` take.\\n+//\\n+// The two spawn spellings share one root set (`_spawn_roots`) and differ only in\\n+// where the group comes from, so this is the row that proves the check is on the\\n+// ROOT and not on the spelling. It crashed with a `RecursionError` before the\\n+// fix, like its `Task.spawn` sibling, so no XFAIL preceded it either.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: suspending recursion is not allowed\\n+// EXPECT-ERROR-CONTAINS: recurse -> recurse\\n+\\n+import std.task.{yield_now}\\n+\\n+func recurse(n: Int) -> Int {\\n+    yield_now()\\n+    if n <= 0 { return 0 }\\n+    return recurse(n - 1)\\n+}\\n+\\n+func main() {\\n+    var group = TaskGroup()\\n+    let h = group.spawn(recurse(3))\\n+    print(\\\"{h.join()}\\\")\\n+}\\ndiff --git a/examples/errors/coro_let_init_move_beside_early_exit.saw b/examples/errors/coro_let_init_move_beside_early_exit.saw\\nnew file mode 100644\\nindex 00000000..bedf2bae\\n--- /dev/null\\n+++ b/examples/errors/coro_let_init_move_beside_early_exit.saw\\n@@ -0,0 +1,54 @@\\n+// SL-234: a `move` of a frame local in a `let` INITIALIZER whose expression also\\n+// exits early is refused rather than miscompiled.\\n+//\\n+// The sibling of `coro_buried_early_exit_beside_a_move.saw` (SL-222 r3), which\\n+// pins the same rule at a plain expression statement, and of the assignment\\n+// twin next to this file. One funnel refuses all three\\n+// (`_refuse_buried_early_exit`).\\n+//\\n+// WHY A REFUSAL AND NOT A FIX. The three CONTAINER heads SL-234 fixed have one\\n+// expression evaluated before all of their blocks, so \\\"after the head, before\\n+// block dispatch\\\" names a real point and the hoist writes it down. A LEAF\\n+// statement has no head: its blocks are operands of an arbitrary expression, and\\n+// one can sit to the LEFT of the move —\\n+//\\n+//     sink(if a { 1 } else { 0 } + consume(move r))\\n+//\\n+// — where relinquishing first would retire a claim the move has not yet\\n+// transferred. Deferring to after the statement is what this shape did before,\\n+// measured as a silent double free (the callee destroys the value, then frame\\n+// teardown destroys it again, exit 0).\\n+//\\n+// The message names the AUTHOR'S binding, `v` — never the synthesized `__headN`\\n+// temp the hoist would have introduced, which is a name they cannot see.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: the initializer of `v`\\n+// EXPECT-ERROR-CONTAINS: whose expression also `return`s, `break`s or `continue`s\\n+// EXPECT-ERROR-CONTAINS: bind the moving call to its own `let` first\\n+\\n+import std.task.{yield_now}\\n+\\n+struct Res { name: String }\\n+extension Res: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop\\\") }\\n+}\\n+\\n+func note(r: &Res) -> Int { r.name.len() }\\n+\\n+func consume_bool(r: Res) -> Bool { true }\\n+\\n+func run(items: Vector<Int>) -> Int {\\n+    yield_now()\\n+    var acc = 0\\n+    for i in items.iter() {\\n+        var r = Res(name: \\\"item\\\")\\n+        if note(&r) + i < 0 { return -1 }\\n+        let v = if consume_bool(move r) { return 4 } else { 0 }\\n+        acc = acc + v\\n+    }\\n+    acc\\n+}\\n+\\n+func main() {\\n+    print(__saw_drive(run([1, 2])))\\n+}\\ndiff --git a/examples/errors/coro_main_suspending_recursion.saw b/examples/errors/coro_main_suspending_recursion.saw\\nnew file mode 100644\\nindex 00000000..c9b4340c\\n--- /dev/null\\n+++ b/examples/errors/coro_main_suspending_recursion.saw\\n@@ -0,0 +1,23 @@\\n+// SL-227, the SUSPENDING-`main` row: a suspending `main` is a root with no\\n+// spelling at all — nothing in the source says `drive` or `spawn` — and it owes\\n+// the same cycle check as the roots that are written down.\\n+//\\n+// It seeds the graph (`seed_names` appends \\\"main\\\"), so before the fix it carried\\n+// a cycle into frame building exactly as a spawn root did, and crashed the\\n+// compiler with a `RecursionError`.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: suspending recursion is not allowed\\n+// EXPECT-ERROR-CONTAINS: recurse -> recurse\\n+\\n+import std.task.{yield_now}\\n+\\n+func recurse(n: Int) -> Int {\\n+    yield_now()\\n+    if n <= 0 { return 0 }\\n+    return recurse(n - 1)\\n+}\\n+\\n+func main() {\\n+    yield_now()\\n+    print(\\\"{recurse(3)}\\\")\\n+}\\ndiff --git a/examples/errors/coro_method_suspending_recursion.saw b/examples/errors/coro_method_suspending_recursion.saw\\nnew file mode 100644\\nindex 00000000..f8246fab\\n--- /dev/null\\n+++ b/examples/errors/coro_method_suspending_recursion.saw\\n@@ -0,0 +1,36 @@\\n+// SL-227, the METHOD-root row: a driven suspending METHOD that recurses gets the\\n+// cycle diagnostic, naming the method.\\n+//\\n+// This row failed DIFFERENTLY from the spawn rows and is why the check takes a\\n+// graph KEY rather than a function name: a method root is reached by node id\\n+// (a free function by its `(\\\"fn\\\", name)` tuple), so asking about it means\\n+// mapping `method_roots`' frame key across `methods_by_key`. Unasked, the cycle\\n+// survived into phase 2 and surfaced as an INTERNAL COMPILER ERROR — \\\"classified\\n+// this call as embeddable into `Walker_walk` ... but no such frame was built\\\" —\\n+// which is the frame-key invariant reporting a consequence of the missing check\\n+// rather than the cause.\\n+//\\n+// The two method spellings that reach no root are refused before this and need\\n+// no row: `group.spawn(w.walk(3))` is refused at the form (\\\"expects a direct\\n+// call to a free function\\\"), and a `Thread.spawn` body is `sync`, so a\\n+// suspending callee there is refused with the no-executor diagnostic.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: suspending recursion is not allowed\\n+// EXPECT-ERROR-CONTAINS: Walker.walk -> Walker.walk\\n+\\n+import std.task.{yield_now}\\n+\\n+struct Walker { depth: Int }\\n+\\n+extension Walker {\\n+    func walk(&self, n: Int) -> Int {\\n+        yield_now()\\n+        if n <= 0 { return 0 }\\n+        return self.walk(n - 1)\\n+    }\\n+}\\n+\\n+func main() {\\n+    let w = Walker(depth: 0)\\n+    print(\\\"{__saw_drive(w.walk(3))}\\\")\\n+}\\ndiff --git a/examples/errors/coro_mutual_suspending_recursion.saw b/examples/errors/coro_mutual_suspending_recursion.saw\\nnew file mode 100644\\nindex 00000000..467b08de\\n--- /dev/null\\n+++ b/examples/errors/coro_mutual_suspending_recursion.saw\\n@@ -0,0 +1,27 @@\\n+// SL-227, the MUTUAL-recursion row: the cycle a spawn root reaches need not be a\\n+// self-call, and the diagnostic names the whole loop rather than one function.\\n+//\\n+// This is the row that shows the check is a graph search and not a \\\"does this\\n+// function call itself\\\" test — `ping` and `pong` each call only the other.\\n+// `RecursionError` before the fix.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: suspending recursion is not allowed\\n+// EXPECT-ERROR-CONTAINS: ping -> pong -> ping\\n+\\n+import std.task.{yield_now}\\n+\\n+func ping(n: Int) -> Int {\\n+    yield_now()\\n+    if n <= 0 { return 0 }\\n+    return pong(n - 1)\\n+}\\n+\\n+func pong(n: Int) -> Int {\\n+    yield_now()\\n+    return ping(n - 1)\\n+}\\n+\\n+func main() {\\n+    let t = Task.spawn(ping(3))\\n+    print(\\\"{t.join()}\\\")\\n+}\\ndiff --git a/examples/errors/coro_spawn_suspending_recursion.saw b/examples/errors/coro_spawn_suspending_recursion.saw\\nnew file mode 100644\\nindex 00000000..e86e877d\\n--- /dev/null\\n+++ b/examples/errors/coro_spawn_suspending_recursion.saw\\n@@ -0,0 +1,31 @@\\n+// SL-227: `Task.spawn` of a self-recursive suspending function is the same clean\\n+// cycle rejection the `__saw_drive` spelling gives.\\n+//\\n+// NO XFAIL PRECEDED THIS FILE, and could not have: the shape CRASHED the\\n+// compiler with a Python `RecursionError` (~981 nested `_build_frame_init`\\n+// frames), and a corpus file that crashes sawc would destabilize the irdet and\\n+// reemit lanes, which byte-compare compiler output over the whole corpus. The\\n+// pin lands with the fix.\\n+//\\n+// MECHANISM: `_analyze_nesting` is the check, and the suspending-call graph was\\n+// already SEEDED from the spawn roots (`seed_names`) — only the asking was\\n+// missing, so a spawn root carried its cycle into `_build_sub_frame`, which\\n+// embeds a callee frame by value and for a self-cycle has no fixed point. Every\\n+// root-seeding path is asked now; `_analyze_nesting`'s docstring is the\\n+// enumeration, and the sibling files here are its rows.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: suspending recursion is not allowed\\n+// EXPECT-ERROR-CONTAINS: recurse -> recurse\\n+\\n+import std.task.{yield_now}\\n+\\n+func recurse(n: Int) -> Int {\\n+    yield_now()\\n+    if n <= 0 { return 0 }\\n+    return recurse(n - 1)\\n+}\\n+\\n+func main() {\\n+    let t = Task.spawn(recurse(3))\\n+    print(\\\"{t.join()}\\\")\\n+}\\ndiff --git a/examples/modules/recursion227/lib.saw b/examples/modules/recursion227/lib.saw\\nnew file mode 100644\\nindex 00000000..d69450b1\\n--- /dev/null\\n+++ b/examples/modules/recursion227/lib.saw\\n@@ -0,0 +1,17 @@\\n+// SL-227: the dependency half of\\n+// `examples/errors/coro_cross_module_suspending_recursion.saw`.\\n+//\\n+// `descend` is SUSPENDING and RECURSIVE, and it lives in a module the entry\\n+// module imports selectively — so design 210 / SL-208 SPLICES it into the entry\\n+// closure and a frame IS built for it. That is what makes the cycle real here,\\n+// and it is the difference from a cross-module callee the splice declines, whose\\n+// cycle is never embedded and must keep compiling (blade's resolver).\\n+import std.task.{yield_now}\\n+\\n+public func descend(depth: Int) -> Int {\\n+    yield_now()\\n+    if depth <= 0 {\\n+        return 0\\n+    }\\n+    return 1 + descend(depth - 1)\\n+}\\ndiff --git a/examples/ref_into_array_element_addresses_real_storage.saw b/examples/ref_into_array_element_addresses_real_storage.saw\\nnew file mode 100644\\nindex 00000000..0a785028\\n--- /dev/null\\n+++ b/examples/ref_into_array_element_addresses_real_storage.saw\\n@@ -0,0 +1,93 @@\\n+// SL-226 (non-coroutine faces): `&`/`&var` into an array element addresses the\\n+// element's REAL storage for EVERY base form the container can take — not just\\n+// the four the reference path used to know.\\n+//\\n+// Codegen had two base-to-pointer dispatches. `_get_lvalue_pointer` — the write\\n+// path — answers every base; the reference path kept a private\\n+// `_get_array_element_pointer` that knew only Identifier / `self` /\\n+// MemberAccess / nested-ArrayIndex and MATERIALIZED A COPY for everything else.\\n+// So `arr[i] = v` and `f(&var arr[i])` disagreed about where `arr[i]` is, and\\n+// three bases fell in the hole. None of these involve a coroutine; the driven\\n+// face (a frame local, which the transform spells `self.arr!`, an optional base)\\n+// is pinned by `coro_ref_into_frame_resident_fixed_array.saw`.\\n+//\\n+// Every cell writes +7 into an element holding 1 and reads it back, so 8 is\\n+// \\\"the write landed\\\" and 1 is \\\"the callee mutated a copy\\\".\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// optional 8\\n+// tuple    8\\n+// var-ref  8\\n+// shared-ref 8\\n+// member   8\\n+// element  8\\n+// write    8\\n+\\n+struct Doc { n: Int }\\n+\\n+struct Bag { arr: [Doc; 3] }\\n+\\n+struct Cell { doc: Doc }\\n+\\n+func set_one(d: &var Doc, k: Int) { d.n = d.n + k }\\n+func read_one(d: &Doc) -> Int { d.n }\\n+\\n+// A FORCE-UNWRAP base — the optional's payload slot, not a copy of it. This is\\n+// the same base the coroutine transform produces for every frame-resident local,\\n+// which is why one missing arm reached so much code.\\n+func optional_base() -> Int {\\n+    var o: [Doc; 3]? = [Doc(n: 1); 3]\\n+    set_one(&var o![1], 7)\\n+    o![1].n\\n+}\\n+\\n+// A TUPLE-ELEMENT base: a tuple element is storage like a struct field\\n+// (DF-151j), so an array living in one lends its elements like any other.\\n+func tuple_base() -> Int {\\n+    var t: ([Doc; 3], Int) = ([Doc(n: 1); 3], 0)\\n+    set_one(&var t.0[1], 7)\\n+    t.0[1].n\\n+}\\n+\\n+// A REFERENCE-TYPED identifier base: the binding holds a pointer to the\\n+// caller's array, so it must be loaded once before the element GEP. Indexing it\\n+// as though the binding WERE the array reached LLVM as a type mismatch — an\\n+// internal compiler error, the one cell here that was never silent.\\n+func through_var_ref(a: &var [Doc; 3], k: Int) { set_one(&var a[1], k) }\\n+func through_shared_ref(a: &[Doc; 3]) -> Int { read_one(&a[1]) }\\n+\\n+// A MEMBER base and a nested-ELEMENT base: the two arms the private dispatch\\n+// did have, kept so the fix is pinned as a widening and not a swap.\\n+func member_base() -> Int {\\n+    var bag = Bag(arr: [Doc(n: 1); 3])\\n+    set_one(&var bag.arr[1], 7)\\n+    bag.arr[1].n\\n+}\\n+\\n+func element_base() -> Int {\\n+    var grid: [[Doc; 2]; 2] = [[Doc(n: 1); 2]; 2]\\n+    set_one(&var grid[1][0], 7)\\n+    grid[1][0].n\\n+}\\n+\\n+// The WRITE spelling of the optional cell, which always worked: it is the\\n+// behavior the borrow now agrees with.\\n+func write_spelling() -> Int {\\n+    var o: [Doc; 3]? = [Doc(n: 1); 3]\\n+    o![1].n = o![1].n + 7\\n+    o![1].n\\n+}\\n+\\n+func main() {\\n+    print(\\\"optional {optional_base()}\\\")\\n+    print(\\\"tuple    {tuple_base()}\\\")\\n+\\n+    var arr: [Doc; 3] = [Doc(n: 1); 3]\\n+    through_var_ref(&var arr, 7)\\n+    print(\\\"var-ref  {arr[1].n}\\\")\\n+    print(\\\"shared-ref {through_shared_ref(&arr)}\\\")\\n+\\n+    print(\\\"member   {member_base()}\\\")\\n+    print(\\\"element  {element_base()}\\\")\\n+    print(\\\"write    {write_spelling()}\\\")\\n+}\\ndiff --git a/sawc/codegen/calls.py b/sawc/codegen/calls.py\\nindex 060128eb..84b961d1 100644\\n--- a/sawc/codegen/calls.py\\n+++ b/sawc/codegen/calls.py\\n@@ -3227,12 +3227,34 @@ class CallsMixin:\\n     def _get_lvalue_pointer(self, expr):\\n         \\\"\\\"\\\"Return a pointer to the storage backing an assignable lvalue.\\n \\n+        THE ONE base-to-pointer dispatch in codegen. Every question of the form\\n+        \\\"where does this place actually live\\\" is answered here, so a BORROW and\\n+        a WRITE of one place can never disagree about where that place is.\\n+\\n         Resolves variables (loading once through a `&`/`&var` reference so the\\n-        pointer lands on the caller's value), `self`, struct fields, and\\n-        array/pointer elements, recursing so nested forms like `a[i].inner`\\n-        reach real storage instead of a throwaway copy. Non-lvalue expressions\\n-        fall back to a materialized temporary (no write-back), matching the\\n-        historical member-access fallback.\\n+        pointer lands on the caller's value), module statics in both spellings,\\n+        `self`, struct fields, array/pointer elements, tuple elements and a\\n+        force-unwrapped optional's payload, recursing so nested forms like\\n+        `a[i].inner` reach real storage instead of a throwaway copy. Non-lvalue\\n+        expressions fall back to a materialized temporary (no write-back),\\n+        matching the historical member-access fallback.\\n+\\n+        ENTRY POINTS (design obligation 1 — the funnel names its callers):\\n+\\n+        * `_generate_assignment` and the compound-assign path — `x = v`.\\n+        * `_get_element_pointer` (`a[i]`) — reached from the assignment path,\\n+          from a method receiver on an element, from a collection read, and\\n+          from `_generate_reference_expr`'s ArrayIndex arm.\\n+        * `_tuple_slot_pointer` / `_get_tuple_element_pointer` (`t.0`).\\n+        * `_get_member_pointer` (`a.b`), and through it every field write.\\n+        * `_generate_reference_expr` (`&x` / `&var x`) for the base forms it\\n+          does not answer itself.\\n+        * `_generate_optional_take` (`o.take()`) and the array builtins\\n+          (`a.swap(i, j)`).\\n+\\n+        SL-226 is what that list is for: the reference path used to keep a\\n+        SECOND, smaller dispatch of its own, and the three bases it had never\\n+        learned about each became a silently lost write.\\n         \\\"\\\"\\\"\\n         if isinstance(expr, Identifier) and expr.name in self.variables:\\n             base_ptr = self.variables[expr.name]\\ndiff --git a/sawc/codegen/operators.py b/sawc/codegen/operators.py\\nindex 202606a4..290bc441 100644\\n--- a/sawc/codegen/operators.py\\n+++ b/sawc/codegen/operators.py\\n@@ -1495,8 +1495,21 @@ class OperatorsMixin:\\n             # Reference to struct field - get GEP pointer\\n             return self._get_member_pointer(inner_expr)\\n         elif isinstance(inner_expr, ArrayIndex):\\n-            # Reference to array/pointer element - get a stable GEP pointer\\n-            return self._get_array_element_pointer(inner_expr)\\n+            # `&arr[i]` / `&var arr[i]` — the element's REAL slot, addressed\\n+            # through the same `_get_lvalue_pointer` funnel the write spelling\\n+            # `arr[i] = v` goes through (SL-226).\\n+            #\\n+            # This used to call a private `_get_array_element_pointer` that kept\\n+            # its OWN base dispatch — Identifier, `self`, MemberAccess, nested\\n+            # ArrayIndex — and materialized a COPY for every other base, so a\\n+            # BORROW and a WRITE of one place disagreed about where that place\\n+            # is. Three bases fell in the hole, each a silent lost write: a\\n+            # ForceUnwrap (`&var o![1]`, and with it EVERY `&var arr[i]` in a\\n+            # suspending body, since the transform rewrites a frame local into\\n+            # `self.arr!`), a TupleIndex (`&var t.0[1]`), and a reference-typed\\n+            # identifier (`&var a[i]` on a `&var [T; N]` param), which GEP'd at\\n+            # array granularity and reached LLVM as a type mismatch.\\n+            return self._get_element_pointer(inner_expr)\\n         elif isinstance(inner_expr, TupleIndex):\\n             # `&t.0` / `&var t.0` (DF-151j) — a tuple element is storage like a\\n             # struct field, so lend its slot. Without this the `else` below\\n@@ -1534,59 +1547,6 @@ class OperatorsMixin:\\n             self.builder.store(value, temp)\\n             return temp\\n \\n-    def _get_array_element_pointer(self, expr: ArrayIndex):\\n-        \\\"\\\"\\\"Return a stable pointer to an array or pointer element, for `&arr[i]`.\\n-\\n-        Mirrors the lvalue logic used by array-element assignment: obtain a\\n-        pointer to the container's storage, then GEP into it -- so the reference\\n-        aliases the real element rather than a materialized copy.\\n-        \\\"\\\"\\\"\\n-        index_val = self._generate_expression(expr.index)\\n-        container_expr = expr.array_expr\\n-\\n-        # Obtain a pointer to the container's storage.\\n-        if isinstance(container_expr, Identifier):\\n-            if container_expr.name not in self.variables:\\n-                # A module STATIC's global IS its storage — the same answer\\n-                # `_get_lvalue_pointer` gives, and the reason `&ARR[i]` on one\\n-                # is a lend of the real element rather than \\\"Undefined\\n-                # variable\\\" (DF-232d's sweep).\\n-                gv = self._static_global(container_expr)\\n-                if gv is None:\\n-                    raise ValueError(f\\\"Undefined variable: {container_expr.name}\\\")\\n-                container_ptr = gv\\n-            else:\\n-                container_ptr = self.variables[container_expr.name]\\n-        elif isinstance(container_expr, SelfExpr):\\n-            container_ptr = self.variables[\\\"self\\\"]\\n-        elif isinstance(container_expr, MemberAccess):\\n-            # `&mod.ARR[i]` — the qualified static's global, asked before the\\n-            # field GEP (DF-232d).\\n-            gv = self._static_global(container_expr)\\n-            container_ptr = (gv if gv is not None\\n-                             else self._get_member_pointer(container_expr))\\n-        elif isinstance(container_expr, ArrayIndex):\\n-            container_ptr = self._get_array_element_pointer(container_expr)\\n-        else:\\n-            # Fallback: materialize the container (won't propagate mutations).\\n-            container_val = self._generate_expression(container_expr)\\n-            container_ptr = self._entry_alloca(container_val.type, name=\\\"arr_tmp\\\")\\n-            self.builder.store(container_val, container_ptr)\\n-\\n-        pointee = container_ptr.type.pointee\\n-        if isinstance(pointee, ir.ArrayType):\\n-            # Dynamic bounds check (design 63 T1b) on `&arr[i]`.\\n-            self._emit_array_bounds_check(index_val, pointee.count, expr.index)\\n-            zero = ir.Constant(ir.IntType(64), 0)\\n-            return self.builder.gep(container_ptr, [zero, index_val], name=\\\"elem_ptr\\\")\\n-        elif isinstance(pointee, ir.PointerType):\\n-            # The variable holds a pointer value; load it, then offset.\\n-            base = self.builder.load(container_ptr, name=\\\"ptr_base\\\")\\n-            return self.builder.gep(base, [index_val], name=\\\"ptr_elem\\\")\\n-        else:\\n-            raise ValueError(\\n-                f\\\"Cannot take reference to element of non-array type: {pointee}\\\")\\n-\\n     def _int_cast_fits(self, value, to_llvm, to_signed: bool, from_signed: bool):\\n         \\\"\\\"\\\"The `i1` \\\"this value has a representation in `to_llvm`\\\", or None when\\n         it always does (design 170).\\ndiff --git a/sawc/coro_transform.py b/sawc/coro_transform.py\\nindex 21af538a..1510420f 100644\\n--- a/sawc/coro_transform.py\\n+++ b/sawc/coro_transform.py\\n@@ -1715,20 +1715,61 @@ def _find_suspending_cycle(start_key, nodes):\\n     return dfs(start_key)\\n \\n \\n-def _analyze_nesting(root_name, root_func, nodes):\\n+def _analyze_nesting(start_key, anchor, nodes, is_built=None):\\n     \\\"\\\"\\\"Suspending RECURSION is a compile error naming the cycle: the flat-frame\\n     model embeds callee frames by value (Part 0b), so a suspending-call cycle has\\n     no compile-time frame size. Non-recursive nested suspending calls are now\\n-    supported (embedded + driven); only a cycle is rejected.\\\"\\\"\\\"\\n-    start = (\\\"fn\\\", root_name)\\n-    cyc = _find_suspending_cycle(start, nodes)\\n+    supported (embedded + driven); only a cycle is rejected.\\n+\\n+    THE CHECK EVERY ROOT OWES (SL-227). A root is where the closure walk starts\\n+    building frames, so a cycle reachable from one reaches `_build_sub_frame`,\\n+    which embeds a callee frame by value — and for a self-cycle that has no\\n+    fixed point. The check must therefore run on EVERY root-seeding path, not\\n+    just the one it was written for; the graph is already seeded from all of\\n+    them (`seed_names`), so what was missing was only the asking.\\n+\\n+    ENTRY POINTS (obligation 1 — the funnel names its callers), all in\\n+    `transform_program`, one per way a root is seeded:\\n+\\n+    * `__saw_drive(f(...))` — the free-function drive roots, the original\\n+      caller, pinned by `examples/errors/coro_suspending_recursion.saw`.\\n+    * `Task.spawn(f(...))` / `group.spawn(f(...))` — the spawn roots. Both\\n+      spelled `(\\\"fn\\\", name)`: a spawn takes a direct call to a free function.\\n+    * a suspending `main`, which is a root with no spelling at all.\\n+    * `__saw_drive(obj.m(...))` — the method drive roots, keyed\\n+      `(\\\"method\\\", node_id)`, which is why this takes a KEY rather than a name.\\n+\\n+    Two spellings reach no root and are refused before this: `group.spawn` of a\\n+    METHOD call is refused at the form (\\\"expects a direct call to a free\\n+    function\\\"), and a `Thread.spawn` body is `sync`, so a suspending callee in\\n+    one is refused with the no-executor diagnostic.\\n+\\n+    `anchor` is only for the diagnostic's position — the function or method AST\\n+    the cycle was entered through.\\n+\\n+    `is_built` KEEPS THE CHECK EXACTLY AS STRICT AS THE FRAME BUILDER, and is\\n+    not optional in spirit. The suspending-call GRAPH is wider than the set of\\n+    frames the transform actually builds: the closure walk follows an edge only\\n+    when it can name and build a frame for the target, so a suspending callee it\\n+    declines to reach — a cross-module free function the splice does not take,\\n+    say — is in `nodes` and not in `closure`. A cycle through such a node is\\n+    never embedded, so it has a frame size and is not an error. Blade's own\\n+    dependency resolver is that program: `visit` recurses and reaches\\n+    `Command.run`, and blade has always compiled because no `__Frame_visit` is\\n+    ever built. Without this gate SL-227's widening refused the package manager.\\n+    Asking \\\"will every frame on this cycle be built\\\" is the same question\\n+    `_build_sub_frame` would have answered by diverging.\\\"\\\"\\\"\\n+    cyc = _find_suspending_cycle(start_key, nodes)\\n+    if cyc is not None and is_built is not None and not all(\\n+            is_built(k) for k in cyc):\\n+        return\\n     if cyc is not None:\\n         chain = \\\" -> \\\".join(_node_display(k, nodes) for k in cyc)\\n         raise CoroTransformError(\\n             f\\\"suspending recursion is not allowed: the suspending-call cycle \\\"\\n             f\\\"`{chain}` has no compile-time frame size (design 44 embeds callee \\\"\\n             f\\\"frames by value). Break the cycle or drive the inner call \\\"\\n-            f\\\"separately.\\\", root_func)\\n+            f\\\"separately.\\\", anchor)\\n \\n \\n # --------------------------------------------------------------------------- #\\n@@ -7340,6 +7381,56 @@ class _FrameBuilder:\\n         ref.resolved_type = getattr(head, 'resolved_type', None)\\n         return [binding] + self._forgets(forgets), ref\\n \\n+    def _refuse_buried_early_exit(self, s, node, forgets, position):\\n+        \\\"\\\"\\\"Refuse a `move` of a frame local in a LEAF statement whose expression\\n+        also `return`s, `break`s or `continue`s (SL-222 r3, widened by SL-234).\\n+\\n+        THE OTHER HALF of `_hoist_head_and_relinquish`, and the reason the two\\n+        live side by side: that helper places the relinquish where it is\\n+        provably the live claim, and this one is what happens when NO SUCH PLACE\\n+        EXISTS.\\n+\\n+        A CONTAINER has a head — one expression, evaluated before all of its\\n+        blocks — so \\\"after the head, before block dispatch\\\" names a real point\\n+        and the hoist writes it down. A LEAF statement has no head: its blocks\\n+        are OPERANDS of an arbitrary expression, and one can sit to the LEFT of\\n+        the move —\\n+\\n+            sink(if a { 1 } else { 0 } + consume(move r))\\n+\\n+        — where relinquishing first would retire a claim the move has not yet\\n+        transferred, and the move would then read an emptied slot. Deferring to\\n+        after the statement instead is what SL-234 measured as a silent double\\n+        free: the early exit leaves before the trailing clear, the callee has\\n+        already destroyed the value, and frame teardown destroys it again.\\n+\\n+        With no ordering this position can state, the refusal is the honest\\n+        answer. It costs nothing that used to work: the shape was refused by the\\n+        nested-tail-move diagnostic before SL-222's tail demotion made it\\n+        reachable, and the workaround it names is the one the author would write\\n+        anyway. The message names the AUTHOR'S position — a binding, an\\n+        assignment target, or the statement — never a synthesized `__headN`\\n+        temp, which is a name they cannot see.\\n+\\n+        ENTRY POINTS (obligation 1 — the funnel names its callers), all three in\\n+        `_lower_inplace`, which is where a non-spanning statement is lowered:\\n+\\n+        * the `LetStatement` arm — `let v = if consume(move r) { return 4 } …`\\n+        * the `AssignStatement` arm — `acc = if consume(move r) { return 5 } …`\\n+        * the fallback — a plain expression statement, `sink_int(...)` above.\\n+\\n+        A CONTAINER never reaches here: each has an arm that hoists instead, and\\n+        the enumeration gate below the arms is what keeps that true.\\\"\\\"\\\"\\n+        if not forgets:\\n+            return\\n+        if not (_contains_return(node) or self._has_loop_ctrl(node)):\\n+            return\\n+        raise self._error(\\n+            f\\\"coroutine transform: `move` of a frame local in {position} \\\"\\n+            f\\\"of driven `{self.name}` whose expression also `return`s, \\\"\\n+            f\\\"`break`s or `continue`s is not supported; bind the moving \\\"\\n+            f\\\"call to its own `let` first, then use the binding\\\", s)\\n+\\n     def _rewrite_expr(self, node, forgets):\\n         \\\"\\\"\\\"Frame-aware expression rewrite: `Identifier(frame local)` ->\\n         `self.<field>` read; `move <frame local>` -> field read (+ record an\\n@@ -7861,6 +7952,10 @@ class _FrameBuilder:\\n         if isinstance(s, LetStatement):\\n             forgets = []\\n             cap_lets, value = self._rewrite_hosting(s.value, forgets)\\n+            # SL-234: a leaf statement can state no ordering between its\\n+            # operands and the move — see `_refuse_buried_early_exit`.\\n+            self._refuse_buried_early_exit(\\n+                s, value, forgets, f\\\"the initializer of `{s.name}`\\\")\\n             if s.name in self.encmap:\\n                 new = self._store_field(s.name, value, s.line, s.column)\\n             else:\\n@@ -7878,6 +7973,14 @@ class _FrameBuilder:\\n             # The VALUE first: `out = out + \\\"!\\\"` reads the old binding, and the\\n             # target rewrite must not change what that read means.\\n             cap_lets, s.value = self._rewrite_hosting(s.value, forgets)\\n+            # SL-234: a leaf statement can state no ordering between its\\n+            # operands and the move — see `_refuse_buried_early_exit`. Asked\\n+            # with the target still in its AUTHOR'S spelling, so the message\\n+            # names `acc` rather than the `self.acc` the rewrite would give.\\n+            self._refuse_buried_early_exit(\\n+                s, s.value, forgets,\\n+                (f\\\"the right-hand side of the assignment to `{s.target.name}`\\\"\\n+                 if isinstance(s.target, Identifier) else \\\"an assignment\\\"))\\n             # A whole-binding target on a MIGRATED field is not a write at all\\n             # any more, it is a `put` — which is why the DF-196a shape (writing\\n             # THROUGH a `!`) has no spelling here to get wrong.\\n@@ -7917,10 +8020,19 @@ class _FrameBuilder:\\n             return cap_lets + pre + [s]\\n         if isinstance(s, GuardLetStatement):\\n             forgets = []\\n-            cap_lets, s.optional_expr = self._rewrite_hosting(\\n+            cap_lets, subject = self._rewrite_hosting(\\n                 s.optional_expr, forgets)\\n+            # SL-234: see `_hoist_head_and_relinquish`. A `guard let`'s subject\\n+            # is a head like any other — evaluated first and unconditionally —\\n+            # and its `else` block ALWAYS leaves (return/break/continue), so a\\n+            # trailing clear was unreachable on the one path that runs a block.\\n+            # `consuming`: a `guard let` READS A PAYLOAD out of its subject,\\n+            # exactly as `if let` does, so the replacement must transfer rather\\n+            # than be judged as a named read by design 131's policy.\\n+            pre, s.optional_expr = self._hoist_head_and_relinquish(\\n+                subject, forgets, s, consuming=True)\\n             self._lower_block_in_place(s.else_branch)\\n-            return cap_lets + [s] + self._forgets(forgets)\\n+            return cap_lets + pre + [s]\\n         if isinstance(ctrl, ForLoop):\\n             # SL-222 review r1: `_lower_inplace` owns EVERY container in\\n             # `ast_walk.CONTAINER_KINDS`, so that a statement-position construct\\n@@ -7931,10 +8043,16 @@ class _FrameBuilder:\\n             # says \\\"discarded\\\"), and saying it here keeps the statement path\\n             # complete rather than relying on the backstop.\\n             forgets = []\\n-            cap_lets, ctrl.iterable = self._rewrite_hosting(\\n+            cap_lets, iterable = self._rewrite_hosting(\\n                 ctrl.iterable, forgets)\\n+            # SL-234: see `_hoist_head_and_relinquish`. A `for`'s ITERABLE is a\\n+            # head — evaluated once, before the first iteration — so a `move` in\\n+            # it has already transferred by the time the body runs, and a body\\n+            # that `return`s/`break`s leaves before a trailing clear.\\n+            pre, ctrl.iterable = self._hoist_head_and_relinquish(\\n+                iterable, forgets, s)\\n             self._lower_block_in_place(ctrl.body)\\n-            return cap_lets + [s] + self._forgets(forgets)\\n+            return cap_lets + pre + [s]\\n         if isinstance(ctrl, TryCatchExpr):\\n             # A NON-spanning `try { } catch { }` in STATEMENT position: both\\n             # blocks are statement lists whose value is discarded. Without this\\n@@ -7982,13 +8100,38 @@ class _FrameBuilder:\\n             if isinstance(e, WhileExpr):\\n                 forgets = []\\n                 cap_lets = []\\n+                pre = []\\n                 if e.condition is not None:\\n                     # A capture materialized here would run ONCE, ahead of the\\n                     # loop, while the condition runs every iteration — so a\\n                     # closure in a `while` condition keeps the clean refusal.\\n-                    e.condition = self._rewrite_expr(e.condition, forgets)\\n+                    cond = self._rewrite_expr(e.condition, forgets)\\n+                    # SL-234: see `_hoist_head_and_relinquish`.\\n+                    #\\n+                    # A `while` CONDITION is the one head that re-evaluates, so\\n+                    # hoisting it out of the loop is not a neutral rewrite in\\n+                    # general — it would freeze iteration one's answer. What\\n+                    # makes it sound HERE is that the hoist fires only when the\\n+                    # head contains a `move` of a frame local (`forgets` is\\n+                    # empty otherwise and the helper returns the head\\n+                    # untouched), and such a condition CANNOT be evaluated twice\\n+                    # soundly: the second evaluation would consume a value the\\n+                    # first one already gave away. So for every program in which\\n+                    # the move is sound, the condition runs exactly once and the\\n+                    # hoist is observationally the same loop — while the frame's\\n+                    # claim moves to the one place it is provably live.\\n+                    #\\n+                    # A condition the body genuinely falls back through is an\\n+                    # unsound program BEFORE this arm sees it: the SYNC twin of\\n+                    # `while consume(move r) { … }` with a falling-through body\\n+                    # destroys one value once per iteration (measured: three\\n+                    # drops of one `Res`, exit 0). That is a move-checker hole,\\n+                    # not a frame-accounting one — filed separately — and this\\n+                    # hoist neither causes nor hides it.\\n+                    pre, e.condition = self._hoist_head_and_relinquish(\\n+                        cond, forgets, s)\\n                 self._lower_block_in_place(e.body)\\n-                return cap_lets + [s] + self._forgets(forgets)\\n+                return cap_lets + pre + [s]\\n             if isinstance(e, MatchExpr):\\n                 forgets = []\\n                 # THIS is the shape DF-215f lived in: a match whose SCRUTINEE\\n@@ -8058,30 +8201,10 @@ class _FrameBuilder:\\n         # a value, etc. — rewrite in place, hosting any drop-flag clears after.\\n         forgets = []\\n         cap_lets, ns = self._rewrite_hosting(s, forgets)\\n-        if forgets and (_contains_return(ns) or self._has_loop_ctrl(ns)):\\n-            # SL-222 review r3. The clears below run after the whole statement,\\n-            # and this statement BURIES an early exit inside its own expression\\n-            # — a value `if`/`match` arm that `return`s, `break`s or\\n-            # `continue`s. The exit leaves before the clears, so the frame's\\n-            # teardown would drop a value the callee already destroyed.\\n-            #\\n-            # The container arms above fix this by HOISTING their head to a\\n-            # temp and relinquishing between it and the container\\n-            # (`_hoist_head_and_relinquish`), which is sound there because a\\n-            # container's head is one expression evaluated before all of its\\n-            # blocks. Here there is no such head: the blocks are operands of an\\n-            # arbitrary expression, and one can sit to the LEFT\\n-            # of the `move` (`sink(if a { 1 } else { 0 } + consume(move r))`),\\n-            # where clearing first would retire a claim the move has not yet\\n-            # transferred and the move would then read an emptied slot. With no\\n-            # ordering this position can state, the honest answer is the\\n-            # refusal — which is also what this shape got before SL-222 made it\\n-            # reachable, so nothing that used to compile stops compiling.\\n-            raise self._error(\\n-                f\\\"coroutine transform: `move` of a frame local in a statement \\\"\\n-                f\\\"of driven `{self.name}` whose expression also `return`s, \\\"\\n-                f\\\"`break`s or `continue`s is not supported; bind the moving \\\"\\n-                f\\\"call to its own `let` first, then use the binding\\\", s)\\n+        # SL-222 r3, the shape this refusal was written for: a plain expression\\n+        # statement burying an early exit beside a `move`. The rule now serves\\n+        # the two LEAF arms above as well — see `_refuse_buried_early_exit`.\\n+        self._refuse_buried_early_exit(s, ns, forgets, \\\"a statement\\\")\\n         return cap_lets + [ns] + self._forgets(forgets)\\n \\n     def _store_result(self, value):\\n@@ -11040,8 +11163,52 @@ def transform_program(program, typechecker, imported_ast=None):\\n                 elif isinstance(e.target, int) and e.target in methods_by_id:\\n                     work.append((\\\"method\\\", e.target))\\n \\n-    for root_name in roots:\\n-        _analyze_nesting(root_name, funcs_by_name[root_name], nodes)\\n+    # SL-227: EVERY root-seeding path is asked, not just the `__saw_drive` one.\\n+    # A spawn root used to reach `_build_sub_frame` with its cycle intact and\\n+    # the compiler died of a `RecursionError` — a Python traceback where the\\n+    # drive spelling of the SAME cycle gave a clean diagnostic. The graph was\\n+    # already seeded from all of these (`seed_names` above); only the asking was\\n+    # missing. `_analyze_nesting`'s docstring is the enumeration.\\n+    _cycle_roots = []\\n+    _seen_cycle_roots = set()\\n+    for _name in list(roots) + list(spawn_roots) + (\\n+            [\\\"main\\\"] if main_suspends else []):\\n+        if _name in _seen_cycle_roots or _name not in funcs_by_name:\\n+            continue\\n+        _seen_cycle_roots.add(_name)\\n+        _cycle_roots.append(((\\\"fn\\\", _name), funcs_by_name[_name]))\\n+    # A METHOD root is reached by its node id — `method_roots` is keyed by frame\\n+    # key, which `methods_by_key` maps across. The graph keys a method by the\\n+    # RAW id (a function is the `(\\\"fn\\\", name)` tuple), which is what\\n+    # `_find_suspending_cycle` follows through `e.target`, so the id is the start\\n+    # key as it stands. Without this a self-recursive suspending method reached\\n+    # the frame-key mismatch in phase 2 as an internal compiler error\\n+    # (`no such frame was built`) instead of the cycle diagnostic.\\n+    _root_method_ids = set()\\n+    for _fkey in method_roots:\\n+        _mid = methods_by_key.get(_fkey)\\n+        if _mid is None:\\n+            continue\\n+        _entry = methods_by_id.get(_mid)\\n+        _manchor = _entry[1] if isinstance(_entry, tuple) else _entry\\n+        _root_method_ids.add(_mid)\\n+        _cycle_roots.append((_mid, _manchor))\\n+    # The gate that keeps the check as strict as the builder and no stricter —\\n+    # see `_analyze_nesting`. `closure` (free functions, by name) and\\n+    # `method_closure` (methods, by node id) ARE the frames about to be built.\\n+    _closure_names = set(closure)\\n+\\n+    def _frame_is_built(key):\\n+        if isinstance(key, tuple) and len(key) == 2 and key[0] == \\\"fn\\\":\\n+            return key[1] in _closure_names\\n+        # A method ROOT is a frame by definition — `method_roots` is what builds\\n+        # it, so it is not in `method_closure`, which holds the CALLEES the walk\\n+        # reached. Without that half a self-recursive driven method fell back to\\n+        # the phase-2 frame-key ICE this check exists to replace.\\n+        return key in method_closure or key in _root_method_ids\\n+\\n+    for _start, _anchor in _cycle_roots:\\n+        _analyze_nesting(_start, _anchor, nodes, is_built=_frame_is_built)\\n \\n     # design 127 (RC-3): instrument every loop backedge in the bodies that are\\n     # about to become frames, BEFORE any layout is computed — the inserted\\n\",\"id\":\"SL-226\",\"request_id\":\"sl226-batch2-1\",\"title\":\"coro/codegen ownership batch #2: SL-226 (array-element ptr funnel + SL-261 sync faces) / SL-234 (head-move relinquish + leaf refusal) / SL-227 (spawn roots through recursion check)\"}","request_id":"sl226-batch2-1","sequence":524,"timestamp":"1789105233"}
---
