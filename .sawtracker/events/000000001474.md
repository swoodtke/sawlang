---
{"action":"create","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790274622","description":"Found by the SL-2 U0′ r3 implementer (Sep 24); the lead re-measured it on main (`.build/scratch/ref_depth.py` in the lead checkout).\n\nThe no-reference-in-a-field refusal stops looking after 12 levels of type nesting. The type is `(…((&Int, Int), Int)…, Int)`, wrapped N times, as a struct field:\n\n| N | result |\n|---|---|\n| 1, 11, 12 | refused: \"field `slot` of `Holder` may not be a reference\" |\n| 13, 14, 20 | COMPILES (exit 0) |\n\nA struct that stores a reference escapes the rule that keeps references from outliving their frame. That is a soundness hole: a dangling pointer from safe code.\n\nMechanism (named by the implementer; verify): `first_reference_in` in `sawc/noescape.py` returns `None` once `depth > _MAX_ALIAS_DEPTH` (12). The bound exists to stop an alias CYCLE, but `depth` counts every level of type nesting, not only alias hops. So a deep but acyclic type reads as \"no reference\".\n\nPositions to sweep (obligation 4): the same walk backs every no-escape check, and the typechecker's alias-aware twin shares it. Cover:\n- struct fields;\n- enum payloads;\n- function return types;\n- generic arguments (`Vector<(…(&T…)…)>`);\n- closure and function types;\n- `static` types;\n- an alias chain deeper than 12 and an alias cycle (the refusal the bound was written for).\n\nThe fix bounds alias hops by their own cycle detection, for example a visited set of alias names. It does not bound structural depth at all, because a type's depth is bounded by its source and SL-2's depth funnel already caps that at 256.\n","id":"SL-390","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":1,"sequence":1474,"stage":"backlog","status":"open","title":"A reference nested 13+ levels deep in a type escapes the no-reference-field refusal (first_reference_in's alias-cycle bound counts every nesting level) — soundness","updated":"1790274622"},"before":null,"id":"SL-390","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"description\":\"Found by the SL-2 U0′ r3 implementer (Sep 24); the lead re-measured it on main (`.build/scratch/ref_depth.py` in the lead checkout).\\n\\nThe no-reference-in-a-field refusal stops looking after 12 levels of type nesting. The type is `(…((&Int, Int), Int)…, Int)`, wrapped N times, as a struct field:\\n\\n| N | result |\\n|---|---|\\n| 1, 11, 12 | refused: \\\"field `slot` of `Holder` may not be a reference\\\" |\\n| 13, 14, 20 | COMPILES (exit 0) |\\n\\nA struct that stores a reference escapes the rule that keeps references from outliving their frame. That is a soundness hole: a dangling pointer from safe code.\\n\\nMechanism (named by the implementer; verify): `first_reference_in` in `sawc/noescape.py` returns `None` once `depth > _MAX_ALIAS_DEPTH` (12). The bound exists to stop an alias CYCLE, but `depth` counts every level of type nesting, not only alias hops. So a deep but acyclic type reads as \\\"no reference\\\".\\n\\nPositions to sweep (obligation 4): the same walk backs every no-escape check, and the typechecker's alias-aware twin shares it. Cover:\\n- struct fields;\\n- enum payloads;\\n- function return types;\\n- generic arguments (`Vector<(…(&T…)…)>`);\\n- closure and function types;\\n- `static` types;\\n- an alias chain deeper than 12 and an alias cycle (the refusal the bound was written for).\\n\\nThe fix bounds alias hops by their own cycle detection, for example a visited set of alias names. It does not bound structural depth at all, because a type's depth is bounded by its source and SL-2's depth funnel already caps that at 256.\\n\",\"label\":\"bug\",\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"pid-52055-1790274622-6000\",\"title\":\"A reference nested 13+ levels deep in a type escapes the no-reference-field refusal (first_reference_in's alias-cycle bound counts every nesting level) — soundness\"}","request_id":"pid-52055-1790274622-6000","sequence":1474,"timestamp":"1790274622"}
---
