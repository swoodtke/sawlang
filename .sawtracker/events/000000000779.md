---
{"action":"patch_add","actor":"agent:codex","after":{"assignee":"agent:codex","author":"agent:claude-sawlang","comments":[{"author":"agent:codex","body":"Submitted patch SL-257.p1 (revision 1): minivm M14: local StringBuilder, snapshots and fallible append\n\nAdds the lexer subset of StringBuilder to the Saw-written compiler, VM and textual LLVM emitter. Local builders append String/Byte/Int/Scalar, produce independent String snapshots and forward through shared/mutable references. Append returns Result<Void, AllocError>; allocation failure preserves content and snapshots.\n\nBuilders remain local, noncopyable values: by-value APIs, aggregate storage, assignment and general move analysis are deferred. The verifier checks builder references, operation layouts and nominal AllocError metadata. Native append uses a private recoverable allocator; the VM documents its host-allocation limits. Internal one-shot failure injection is available only to contract tests.\n\nValidation:\n- Full prototype harness: 394 cases, VM and native O0/O2.\n- Builder differential: 14 cases agree with Python sawc.\n- Unchanged lexer char_str and escape_text extracts.\n- Independent 51-code-point UTF-8/snapshot oracle: VM, native O0/O2/ASan and Python sawc.\n- Verifier contract plus identical VM/native failure, recovery, empty-append, forwarding and inactive-drop contracts.\n- Source-level propagated append failure and subsequent recovery, including an untaken builder branch.\n- Citation and whitespace checks.\n\nPython's nested implicit receiver exclusivity gap is filed as SL-284; this implementation enforces the documented rule. Intentional prototype restrictions are excluded from shared-subset comparisons.\n\nBased on the merged M13 Scalar implementation in SL-255.p2 (cbd32918). This diff contains only M14 changes. Rebasing onto main changed only tracker metadata; the validated source tree is identical.\n","created":"1789400001","id":"c1"}],"created":"1789086392","description":"SL-242 epic child, remaining work. Next on LEXER_DEPENDENCIES.md after M12/M13: the lexer's diagnostic construction path. Milestone doc owed at dispatch per the established M-doc pattern.","id":"SL-257","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":3,"sequence":779,"stage":"backlog","status":"open","title":"minivm: StringBuilder support for the lexer subset","updated":"1789400001"},"before":{"assignee":"agent:codex","author":"agent:claude-sawlang","comments":[],"created":"1789086392","description":"SL-242 epic child, remaining work. Next on LEXER_DEPENDENCIES.md after M12/M13: the lexer's diagnostic construction path. Milestone doc owed at dispatch per the established M-doc pattern.","id":"SL-257","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":775,"stage":"backlog","status":"open","title":"minivm: StringBuilder support for the lexer subset","updated":"1789396641"},"id":"SL-257","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:codex\",\"body\":\"Adds the lexer subset of StringBuilder to the Saw-written compiler, VM and textual LLVM emitter. Local builders append String/Byte/Int/Scalar, produce independent String snapshots and forward through shared/mutable references. Append returns Result<Void, AllocError>; allocation failure preserves content and snapshots.\\n\\nBuilders remain local, noncopyable values: by-value APIs, aggregate storage, assignment and general move analysis are deferred. The verifier checks builder references, operation layouts and nominal AllocError metadata. Native append uses a private recoverable allocator; the VM documents its host-allocation limits. Internal one-shot failure injection is available only to contract tests.\\n\\nValidation:\\n- Full prototype harness: 394 cases, VM and native O0/O2.\\n- Builder differential: 14 cases agree with Python sawc.\\n- Unchanged lexer char_str and escape_text extracts.\\n- Independent 51-code-point UTF-8/snapshot oracle: VM, native O0/O2/ASan and Python sawc.\\n- Verifier contract plus identical VM/native failure, recovery, empty-append, forwarding and inactive-drop contracts.\\n- Source-level propagated append failure and subsequent recovery, including an untaken builder branch.\\n- Citation and whitespace checks.\\n\\nPython's nested implicit receiver exclusivity gap is filed as SL-284; this implementation enforces the documented rule. Intentional prototype restrictions are excluded from shared-subset comparisons.\\n\\nBased on the merged M13 Scalar implementation in SL-255.p2 (cbd32918). This diff contains only M14 changes. Rebasing onto main changed only tracker metadata; the validated source tree is identical.\\n\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex ad19d327..c99246be 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -23,7 +23,9 @@ M11 adds fixed `Result<T,E>` construction and matching plus `try`/`try!`\\n propagation, including owning payloads and Void success (336 cases). M12 adds\\n checked unsigned parsing and the unchanged literal_fits helper (346 cases).\\n M13 adds checked nominal `Scalar` construction and code-point readout, with\\n-opaque `InvalidScalar` results. Next are `StringBuilder` and `Vector`. The lexer has\\n+opaque `InvalidScalar` results. M14 adds local `StringBuilder`, all four append\\n+overloads, snapshots and reference forwarding, including unchanged `char_str`\\n+and `escape_text` extracts. Next is `Vector`. The lexer has\\n no imports, so its first whole-file test can combine the unchanged library with\\n a small wrapper without implementing general module loading.\\n The dependency groups below are an inventory, not the implementation commit order;\\ndiff --git a/prototypes/minivm/M13_SCALAR.md b/prototypes/minivm/M13_SCALAR.md\\nindex b875aee6..cc8a1f69 100644\\n--- a/prototypes/minivm/M13_SCALAR.md\\n+++ b/prototypes/minivm/M13_SCALAR.md\\n@@ -63,5 +63,5 @@ The marked shared-subset fixtures also run against Python sawc with `--sawc`.\\n returned words against literal expectations, including inactive zeros and the\\n exact rejected input at both signed extremes. It runs the checks in the VM;\\n its `emit-llvm` mode emits the same checks for native validation. Build it with\\n-the Python compiler and module paths `src=prototypes/minivm/src` and\\n-`sawlex=selfhost/lexer`.\\n+the Python compiler and `--module-path sawlex=selfhost/lexer`; the package's\\n+`src` modules resolve locally.\\ndiff --git a/prototypes/minivm/M14_STRING_BUILDER.md b/prototypes/minivm/M14_STRING_BUILDER.md\\nnew file mode 100644\\nindex 00000000..171ab1dc\\n--- /dev/null\\n+++ b/prototypes/minivm/M14_STRING_BUILDER.md\\n@@ -0,0 +1,183 @@\\n+# M14: local StringBuilder support\\n+\\n+Implementation contract for SL-257, following M13. The consumer is the unchanged\\n+selfhost lexer: construction, append of String/Byte/Int/Scalar, build, and\\n+reference forwarding. This remains a bounded compiler-known API; compiling the\\n+stdlib's implementation comes later.\\n+\\n+## Source and ownership boundary\\n+\\n+Add a reserved nominal `StringBuilder` leaf type. A builder is NoCopy. Permit\\n+fresh local initialization from `StringBuilder()` and shared/mutable reference\\n+parameters. The constructor takes no arguments. A fresh local adopts the\\n+constructor's owning slot directly; it must not pass through generic Copy.\\n+Unannotated and explicitly typed local declarations both work.\\n+\\n+Reject copying a named builder, reassignment, by-value parameters/results,\\n+record fields, Optional/Result payloads, static builders, user extensions and\\n+raw construction/projection. Value-producing control flow containing builders\\n+is outside this slice. A builder may not escape its local scope. General move\\n+analysis and aggregates containing builders are deferred. Reference forwarding\\n+does not copy the builder: it passes access to the original owning slot.\\n+\\n+Methods require named places. `append` requires mutable access and exactly one\\n+String, Byte, Int or Scalar argument, returning `Result<Void, AllocError>`.\\n+Use the stdlib's labels when supplied: `s`, `b`, `value`, `scalar`, respectively.\\n+Bare integer literals select Int; other numeric widths need an explicit\\n+conversion. `build()` requires shared access and no arguments and returns an\\n+owned String snapshot. No other builder operations are admitted.\\n+\\n+Register the receiver borrow before evaluating append's argument and restore\\n+it afterward, using the same root-based exclusivity checks as ordinary methods.\\n+Do not introduce the String snapshot-borrow divergence into this API. Both\\n+direct references and forwarded reference parameters must observe mutations.\\n+\\n+Install reserved `AllocError` as a nominal record with public Int fields `size`\\n+and `align`, matching std.alloc. Named-field construction and copying are\\n+ordinary record operations; reserve its identity against declarations and user\\n+extensions. Error traits and error formatting remain outside the subset.\\n+\\n+## Runtime representation and instructions\\n+\\n+A Builder slot owns one immutable String handle using the existing string heap\\n+or native string allocation. It does not own a separate mutable heap object.\\n+Append allocates replacement content and changes the owning slot only after\\n+success. Build retains the current content as a String. Earlier snapshots can\\n+therefore outlive either later appends or destruction of the builder.\\n+\\n+Add these operations:\\n+\\n+| Operation | Destination | Operands |\\n+| --- | --- | --- |\\n+| BuilderNew | Builder slot | none |\\n+| BuilderBuild | String slot | shared or mutable Ref<Builder> |\\n+| BuilderAppendString | Result<Void, AllocError> | mutable Ref<Builder>, String |\\n+| BuilderAppendByte | same | mutable Ref<Builder>, Byte |\\n+| BuilderAppendInt | same | mutable Ref<Builder>, Int |\\n+| BuilderAppendScalar | same | mutable Ref<Builder>, raw Int code point |\\n+\\n+The frontend recognizes Scalar by its M13 nominal identity before projecting\\n+its private word for BuilderAppendScalar. The verifier sees an Int operand;\\n+the runtime also rejects an invalid raw scalar with `runtime error: invalid scalar`\\n+before encoding it or consuming a failure counter. No source\\n+constructor or field-access bypass is introduced.\\n+\\n+All append results initialize their full three-word layout `[Bool,size,align]`:\\n+success `[true,0,0]`, failure `[false,requested_size,requested_alignment]`.\\n+Verify the destination range, exact field types and nominal error identity;\\n+append instructions carry the Result descriptor index in `immediate`, checked\\n+as Result<Void,Record(program.alloc_error_record)>; all unused operands keep\\n+their normal defaults. This preserves a nominal check despite flattened slots.\\n+Concretely, Program carries `alloc_error_record: Int` (-1 when absent in older\\n+hand-built programs) and `builder_fail_after: Int` (-1 by default). The frontend\\n+installs the record and sets its index. The latter is contract-test configuration\\n+and is never set by source syntax or the CLI.\\n+\\n+Classify Builder as owning for slot cleanup and Drop, including normal scope\\n+exit, loop iterations, early return and propagating try. General Copy,\\n+LoadRef and StoreRef must reject Builder values. Copy of a Ref<Builder> remains\\n+legal. Builder operations access the referenced owning slot directly. Native\\n+Builder words are pointers, VM Builder words are handles; all layout and ABI\\n+helpers must agree. The string live-allocation check includes builder content.\\n+\\n+BuilderNew establishes valid empty content. Build of an untouched builder is\\n+an empty String, never an invalid or inactive String handle. Neither backend\\n+may mutate shared content in place. Copy-on-every-append is acceptable here;\\n+capacity growth and performance work require later measurements.\\n+\\n+## Byte conversion and failure behavior\\n+\\n+String and Byte append preserve exact bytes, including NUL and non-UTF-8 bytes.\\n+Int append uses signed decimal, including Int.min without negating Int.min.\\n+Scalar append encodes the validated code point into exactly 1–4 UTF-8 bytes.\\n+Empty String append succeeds without allocating or changing content.\\n+\\n+Every nonempty append first determines the complete byte count. Check all\\n+length/header arithmetic before allocation or copying. Unrepresentable lengths\\n+raise `runtime error: builder length overflow` rather than wrapping, truncating or inventing an\\n+AllocError size. Once a representable allocation is requested, allocation\\n+failure returns Err and leaves both builder content and existing snapshots\\n+unchanged. Error fields describe this runtime's actual requested allocation,\\n+not the production stdlib's different capacity-growth implementation.\\n+\\n+Use a Builder-private native append helper that creates the same String header\\n+and updates the existing live-allocation count. Existing String operations keep\\n+their current allocation path. Builder append must not call its fatal allocator\\n+and lose the Err channel. The requested size is new content length plus the\\n+16-byte header, with alignment 8; enforce the current `Int.max - 16` content\\n+bound before addition. Do not saturate an unrepresentable error size. On success,\\n+copy bytes before releasing old content, then install the new handle. On\\n+failure, retain the old handle and release any staging resources.\\n+\\n+The VM should propagate recoverable allocation failures from the host operations\\n+it uses. Exhaustion of the interpreter's own bookkeeping or its infallible host\\n+String allocation remains a host-runtime failure, as in earlier milestones;\\n+do not claim that this prototype can recover from all host OOM conditions.\\n+\\n+For repeatable failure tests, provide an internal execution/emission option\\n+`builder_fail_after`, default -1 (disabled). A nonnegative N injects one failure\\n+after N successful nonempty append allocation attempts, then disables itself.\\n+This exercises failure followed by recovery in one program. Empty append does\\n+not consume the counter. Positive counters decrement only after a successful\\n+allocation, not after a real failure or invalid input. Use the same policy in VM and native helpers; expose\\n+it to contract tests, not as new source syntax or a user-facing CLI option.\\n+\\n+## Acceptance tests\\n+\\n+Each source test runs through VM and emitted native code at O0/O2 with literal\\n+expected output. Mark shared-subset cases for Python sawc comparison.\\n+\\n+- Empty and repeated build; append after build; snapshot outlives builder;\\n+  multiple snapshots interleaved with appends; two builders remain independent.\\n+- String, Byte and Int formatting: empty, embedded NUL, high bytes, signed\\n+  extremes and zero. Scalar encoding at every UTF-8 width boundary and both\\n+  sides of the surrogate gap, using byte values as the oracle.\\n+- Direct and forwarded shared/mutable references; input expressions evaluate\\n+  once; nested argument evaluation follows borrow rules.\\n+- Loop and early-return cleanup, error propagation and explicit Result match.\\n+  Allocation-failure injection preserves builder/snapshots, reports nonzero\\n+  request fields, and permits a subsequent append after the one-shot failure.\\n+- Located refusal for forbidden copying/storage/transfers/extensions, wrong\\n+  labels/types/arity, mutation through shared access, temporary receivers and\\n+  overlapping borrows. Existing String/Scalar behavior remains unchanged.\\n+- Self-checking IR contracts for every instruction's operands, destination\\n+  layout and invalid forms, plus inactive-zero and failure-state assertions.\\n+  Run native ownership cases under ASan.\\n+- Compile unchanged lexer helper extracts using these APIs, including char_str\\n+  and escape_text where their remaining dependencies fit the current subset.\\n+\\n+The full minivm harness, focused differential and independent ownership/failure\\n+contracts gate submission. No Vector or production compiler changes belong in\\n+this patch. Any Python sawc defect encountered gets a minimized Sawtracker\\n+finding; do not encode a workaround or reproduce its wrong behavior here.\\n+\\n+Python sawc currently accepts nested implicit receiver borrows that violate\\n+whole-call exclusivity (SL-284); the prototype rejects overlapping builder\\n+receivers. The corresponding rejection is intentionally not a differential\\n+agreement case. Prototype-only storage and transfer restrictions are likewise\\n+excluded from shared-subset comparisons.\\n+\\n+Build and run the independent contracts from the repository root:\\n+\\n+```sh\\n+python sawc/sawc.py prototypes/minivm/tests/builder_contract.saw -o .build/minivm/builder_contract\\n+.build/minivm/builder_contract\\n+python sawc/sawc.py prototypes/minivm/tests/builder_vm_contract.saw -o .build/minivm/builder_vm_contract\\n+.build/minivm/builder_vm_contract\\n+.build/minivm/builder_vm_contract emit-llvm > .build/minivm/builder_vm_contract.ll\\n+clang -O2 .build/minivm/builder_vm_contract.ll -o .build/minivm/builder_native_contract\\n+.build/minivm/builder_native_contract\\n+```\\n+\\n+The runtime contract verifies the same instruction program before VM execution\\n+or native emission. It checks failure fields, unchanged snapshots, one-shot\\n+recovery, empty append counter behavior, reference forwarding and inactive-slot\\n+cleanup. Compile its emitted IR with `-O0` and `-fsanitize=address` as additional\\n+native ownership checks.\\n+\\n+`tests/builder_source_contract.saw` additionally compiles source containing an\\n+untaken builder branch and propagated `try` from a local builder. Build it with\\n+`--module-path sawlex=selfhost/lexer`; its VM and emitted native programs must\\n+print `21`, `8`, and `ok` on separate lines, demonstrating error fields and a\\n+successful subsequent call after the injected failure. It accepts the same\\n+`emit-llvm` test-driver argument.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex ff3c2d7b..04bb81ec 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -125,6 +125,13 @@ either type. `InvalidScalar` cases and formatting are intentionally deferred;\\n Result matching can currently observe and forward only the opaque error value.\\n See [M13_SCALAR.md](M13_SCALAR.md).\\n \\n+Local `StringBuilder()` values support String, Byte, Int and Scalar append,\\n+shared `build()` snapshots, and shared/mutable reference forwarding. Append\\n+returns `Result<Void, AllocError>`; `AllocError` has public Int `size` and `align`\\n+fields. Builders cannot yet be copied, reassigned, returned by value or stored\\n+in aggregates. Append preserves previous snapshots and leaves content unchanged\\n+on allocation failure. See [M14_STRING_BUILDER.md](M14_STRING_BUILDER.md).\\n+\\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n only to VM execution; native code uses the host call stack and has no budget.\\n@@ -137,21 +144,23 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second execution timeouts. The first thirteen milestones\\n-cover 369 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second execution timeouts. The first fourteen milestones\\n+cover 394 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw`,\\n `tests/record_contract.saw`, `tests/enum_contract.saw`,\\n `tests/reference_contract.saw`, `tests/receiver_contract.saw`,\\n `tests/string_contract.saw`, `tests/owning_record_contract.saw`,\\n `tests/optional_contract.saw`, `tests/result_contract.saw`,\\n-`tests/uint_parse_contract.saw`, and `tests/scalar_contract.saw`; build and run them\\n+`tests/uint_parse_contract.saw`, `tests/scalar_contract.saw`,\\n+`tests/builder_contract.saw`, `tests/builder_vm_contract.saw`, and\\n+`tests/builder_source_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n `--section enums`, `--section values`, `--section references`, or\\n `--section receivers`, `--section strings`, `--section owning_records` or\\n `--section optionals`, `--section results`, `--section unsigned_parse`, or\\n-`--section scalars` for an isolated\\n+`--section scalars`, or `--section builders` for an isolated\\n integration gate.\\n \\n To run the initial SL-260 shared-subset differential as well:\\ndiff --git a/prototypes/minivm/examples/builders/append_values.saw b/prototypes/minivm/examples/builders/append_values.saw\\nnew file mode 100644\\nindex 00000000..1df1f902\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/append_values.saw\\n@@ -0,0 +1,27 @@\\n+func byte_oracle(s: String) {\\n+    var i = 0\\n+    while i < s.len() {\\n+        print(s.byte_at(index: i))\\n+        i += 1\\n+    }\\n+}\\n+\\n+func main() {\\n+    var b = StringBuilder()\\n+    try! b.append(s: \\\"x\\\\0\\\")\\n+    try! b.append(b: Byte(0xFFu8))\\n+    try! b.append(value: Int.min)\\n+    try! b.append(value: 0)\\n+    byte_oracle(b.build())\\n+\\n+    var scalars = StringBuilder()\\n+    try! scalars.append(scalar: try! Scalar(value: 0x7F))\\n+    try! scalars.append(scalar: try! Scalar(value: 0x80))\\n+    try! scalars.append(scalar: try! Scalar(value: 0x7FF))\\n+    try! scalars.append(scalar: try! Scalar(value: 0x800))\\n+    try! scalars.append(scalar: try! Scalar(value: 0xD7FF))\\n+    try! scalars.append(scalar: try! Scalar(value: 0xE000))\\n+    try! scalars.append(scalar: try! Scalar(value: 0xFFFF))\\n+    try! scalars.append(scalar: try! Scalar(value: 0x10FFFF))\\n+    byte_oracle(scalars.build())\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/lexer_helpers.saw b/prototypes/minivm/examples/builders/lexer_helpers.saw\\nnew file mode 100644\\nindex 00000000..70e92699\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/lexer_helpers.saw\\n@@ -0,0 +1,58 @@\\n+static B_TAB: Int = 9\\n+static B_NL: Int = 10\\n+static B_CR: Int = 13\\n+static B_SPACE: Int = 32\\n+static B_BACKSLASH: Int = 92\\n+static B_DEL: Int = 127\\n+\\n+func ubyte(s: &String, i: Int) -> Int {\\n+    if i < 0 || i >= s.len() {\\n+        return -1\\n+    }\\n+    s.byte_at(i) as Int\\n+}\\n+\\n+func to_byte(u: Int) -> Byte {\\n+    Byte(UInt8.from(truncating: u))\\n+}\\n+\\n+func hex_digit(n: Int) -> String { \\\"0123456789abcdef\\\".substring(n, n + 1) }\\n+\\n+func char_str(u: Int) -> String {\\n+    var sb = StringBuilder()\\n+    try! sb.append(to_byte(u))\\n+    sb.build()\\n+}\\n+\\n+public func escape_text(s: String) -> String {\\n+    var out = StringBuilder()\\n+    let n = s.len()\\n+    var i = 0\\n+    while i < n {\\n+        let u = ubyte(&s, i)\\n+        if u == B_BACKSLASH { try! out.append(\\\"\\\\\\\\\\\\\\\\\\\") }\\n+        else if u == B_NL { try! out.append(\\\"\\\\\\\\n\\\") }\\n+        else if u == B_TAB { try! out.append(\\\"\\\\\\\\t\\\") }\\n+        else if u == B_CR { try! out.append(\\\"\\\\\\\\r\\\") }\\n+        else if u == 0 { try! out.append(\\\"\\\\\\\\0\\\") }\\n+        else if u < B_SPACE || u == B_DEL {\\n+            try! out.append(\\\"\\\\\\\\x\\\")\\n+            try! out.append(hex_digit(u / 16))\\n+            try! out.append(hex_digit(u % 16))\\n+        }\\n+        else { try! out.append(to_byte(u)) }\\n+        i = i + 1\\n+    }\\n+    out.build()\\n+}\\n+\\n+func main() {\\n+    print(char_str(65).byte_at(0))\\n+    print(char_str(255).byte_at(0))\\n+    var raw = StringBuilder()\\n+    try! raw.append(\\\"A\\\\\\\\\\\\\\\\\\\\n\\\\t\\\\r\\\\0\\\")\\n+    try! raw.append(to_byte(1))\\n+    try! raw.append(to_byte(127))\\n+    try! raw.append(\\\"é\\\")\\n+    print(escape_text(raw.build()))\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/loop_return_cleanup.saw b/prototypes/minivm/examples/builders/loop_return_cleanup.saw\\nnew file mode 100644\\nindex 00000000..5bc8ecbb\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/loop_return_cleanup.saw\\n@@ -0,0 +1,19 @@\\n+func early(flag: Bool) -> String {\\n+    var b = StringBuilder()\\n+    try! b.append(s: \\\"early\\\")\\n+    if flag { return b.build() }\\n+    \\\"late\\\"\\n+}\\n+\\n+func main() {\\n+    var out = StringBuilder()\\n+    var i = 0\\n+    while i < 5 {\\n+        var part = StringBuilder()\\n+        try! part.append(value: i)\\n+        try! out.append(s: part.build())\\n+        i += 1\\n+    }\\n+    print(out.build())\\n+    print(early(true))\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/references.saw b/prototypes/minivm/examples/builders/references.saw\\nnew file mode 100644\\nindex 00000000..815f0c5e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/references.saw\\n@@ -0,0 +1,20 @@\\n+func mutate(b: &var StringBuilder) {\\n+    try! b.append(s: \\\"-mut\\\")\\n+}\\n+\\n+func forward(b: &var StringBuilder) {\\n+    mutate(&var b)\\n+}\\n+\\n+func observe(b: &StringBuilder) {\\n+    print(b.build())\\n+}\\n+\\n+func main() {\\n+    var b = StringBuilder()\\n+    try! b.append(s: \\\"start\\\")\\n+    forward(&var b)\\n+    observe(&b)\\n+    try! b.append(s: \\\"-shared\\\")\\n+    observe(&b)\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/reject_append_arity.saw b/prototypes/minivm/examples/builders/reject_append_arity.saw\\nnew file mode 100644\\nindex 00000000..0f482144\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_append_arity.saw\\n@@ -0,0 +1,2 @@\\n+func main() { var b = StringBuilder()\\n+try! b.append() }\\ndiff --git a/prototypes/minivm/examples/builders/reject_append_label.saw b/prototypes/minivm/examples/builders/reject_append_label.saw\\nnew file mode 100644\\nindex 00000000..306c2e15\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_append_label.saw\\n@@ -0,0 +1,2 @@\\n+func main() { var b = StringBuilder()\\n+try! b.append(value: \\\"x\\\") }\\ndiff --git a/prototypes/minivm/examples/builders/reject_append_type.saw b/prototypes/minivm/examples/builders/reject_append_type.saw\\nnew file mode 100644\\nindex 00000000..b8cf11cc\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_append_type.saw\\n@@ -0,0 +1,2 @@\\n+func main() { var b = StringBuilder()\\n+try! b.append(true) }\\ndiff --git a/prototypes/minivm/examples/builders/reject_assignment.saw b/prototypes/minivm/examples/builders/reject_assignment.saw\\nnew file mode 100644\\nindex 00000000..e3270c6c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_assignment.saw\\n@@ -0,0 +1,3 @@\\n+func main() { var a = StringBuilder()\\n+var b = StringBuilder()\\n+a = b }\\ndiff --git a/prototypes/minivm/examples/builders/reject_builtin_alloc_error.saw b/prototypes/minivm/examples/builders/reject_builtin_alloc_error.saw\\nnew file mode 100644\\nindex 00000000..1f712c7d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_builtin_alloc_error.saw\\n@@ -0,0 +1,2 @@\\n+struct AllocError { size: Int }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/builders/reject_constructor_arity.saw b/prototypes/minivm/examples/builders/reject_constructor_arity.saw\\nnew file mode 100644\\nindex 00000000..2527602f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_constructor_arity.saw\\n@@ -0,0 +1 @@\\n+func main() { let b = StringBuilder(1) }\\ndiff --git a/prototypes/minivm/examples/builders/reject_copy.saw b/prototypes/minivm/examples/builders/reject_copy.saw\\nnew file mode 100644\\nindex 00000000..aa90f9c8\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_copy.saw\\n@@ -0,0 +1,2 @@\\n+func main() { var a = StringBuilder()\\n+let b = a }\\ndiff --git a/prototypes/minivm/examples/builders/reject_extension.saw b/prototypes/minivm/examples/builders/reject_extension.saw\\nnew file mode 100644\\nindex 00000000..9512b2d6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_extension.saw\\n@@ -0,0 +1,2 @@\\n+extension StringBuilder { func extra(&self) {} }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/builders/reject_nested_receiver.saw b/prototypes/minivm/examples/builders/reject_nested_receiver.saw\\nnew file mode 100644\\nindex 00000000..3b1ce85b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_nested_receiver.saw\\n@@ -0,0 +1,2 @@\\n+func main() { var b = StringBuilder()\\n+try! b.append(s: b.build()) }\\ndiff --git a/prototypes/minivm/examples/builders/reject_optional.saw b/prototypes/minivm/examples/builders/reject_optional.saw\\nnew file mode 100644\\nindex 00000000..f8102e8a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_optional.saw\\n@@ -0,0 +1,2 @@\\n+func take(b: StringBuilder?) {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/builders/reject_record_field.saw b/prototypes/minivm/examples/builders/reject_record_field.saw\\nnew file mode 100644\\nindex 00000000..732bab01\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_record_field.saw\\n@@ -0,0 +1,2 @@\\n+struct Box { b: StringBuilder }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/builders/reject_result.saw b/prototypes/minivm/examples/builders/reject_result.saw\\nnew file mode 100644\\nindex 00000000..65a5baed\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_result.saw\\n@@ -0,0 +1,2 @@\\n+func take(b: Result<StringBuilder, Int>) {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/builders/reject_shared_append.saw b/prototypes/minivm/examples/builders/reject_shared_append.saw\\nnew file mode 100644\\nindex 00000000..0b9d89ce\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_shared_append.saw\\n@@ -0,0 +1,3 @@\\n+func append_shared(b: &StringBuilder) { try! b.append(s: \\\"x\\\") }\\n+func main() { var b = StringBuilder()\\n+append_shared(&b) }\\ndiff --git a/prototypes/minivm/examples/builders/reject_static.saw b/prototypes/minivm/examples/builders/reject_static.saw\\nnew file mode 100644\\nindex 00000000..a86caf88\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_static.saw\\n@@ -0,0 +1,2 @@\\n+static BUILDER: StringBuilder = StringBuilder()\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/builders/reject_temporary_receiver.saw b/prototypes/minivm/examples/builders/reject_temporary_receiver.saw\\nnew file mode 100644\\nindex 00000000..60939248\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_temporary_receiver.saw\\n@@ -0,0 +1 @@\\n+func main() { print(StringBuilder().build()) }\\ndiff --git a/prototypes/minivm/examples/builders/reject_value_control.saw b/prototypes/minivm/examples/builders/reject_value_control.saw\\nnew file mode 100644\\nindex 00000000..c2f810cf\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_value_control.saw\\n@@ -0,0 +1,3 @@\\n+func main() {\\n+    let b = if true { StringBuilder() } else { StringBuilder() }\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/reject_value_parameter.saw b/prototypes/minivm/examples/builders/reject_value_parameter.saw\\nnew file mode 100644\\nindex 00000000..79c645fb\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_value_parameter.saw\\n@@ -0,0 +1,2 @@\\n+func take(b: StringBuilder) {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/builders/reject_value_result.saw b/prototypes/minivm/examples/builders/reject_value_result.saw\\nnew file mode 100644\\nindex 00000000..bcf60576\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/reject_value_result.saw\\n@@ -0,0 +1,2 @@\\n+func make() -> StringBuilder { StringBuilder() }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/builders/result_alloc_error.saw b/prototypes/minivm/examples/builders/result_alloc_error.saw\\nnew file mode 100644\\nindex 00000000..cb097703\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/result_alloc_error.saw\\n@@ -0,0 +1,15 @@\\n+func main() {\\n+    var b = StringBuilder()\\n+    let result = b.append(s: \\\"ok\\\")\\n+    match result {\\n+        case Ok -> { print(\\\"ok\\\") },\\n+        case Err(error) -> {\\n+            print(error.size)\\n+            print(error.align)\\n+        }\\n+    }\\n+    let original = AllocError(size: 3, align: 8)\\n+    let copied = original\\n+    print(copied.size)\\n+    print(copied.align)\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/single_evaluation.saw b/prototypes/minivm/examples/builders/single_evaluation.saw\\nnew file mode 100644\\nindex 00000000..a63539be\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/single_evaluation.saw\\n@@ -0,0 +1,10 @@\\n+func argument() -> String {\\n+    print(\\\"arg\\\")\\n+    \\\"one\\\"\\n+}\\n+\\n+func main() {\\n+    var b = StringBuilder()\\n+    try! b.append(s: argument())\\n+    print(b.build())\\n+}\\ndiff --git a/prototypes/minivm/examples/builders/snapshots.saw b/prototypes/minivm/examples/builders/snapshots.saw\\nnew file mode 100644\\nindex 00000000..78b0f3ee\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/builders/snapshots.saw\\n@@ -0,0 +1,16 @@\\n+func main() {\\n+    var b = StringBuilder()\\n+    print(b.build())\\n+    try! b.append(s: \\\"a\\\")\\n+    let first = b.build()\\n+    print(first)\\n+    try! b.append(s: \\\"b\\\")\\n+    let second = b.build()\\n+    print(second)\\n+    print(first)\\n+    try! b.append(s: \\\"!\\\")\\n+    print(second)\\n+    var other: StringBuilder = StringBuilder()\\n+    try! other.append(s: \\\"other\\\")\\n+    print(other.build())\\n+}\\ndiff --git a/prototypes/minivm/src/builder_runtime.saw b/prototypes/minivm/src/builder_runtime.saw\\nnew file mode 100644\\nindex 00000000..459293db\\n--- /dev/null\\n+++ b/prototypes/minivm/src/builder_runtime.saw\\n@@ -0,0 +1,33 @@\\n+func builder_ir(out: &var StringBuilder, text: String) {\\n+    try! out.append(text)\\n+}\\n+\\n+public func emit_builder_runtime(out: &var StringBuilder, fail_after: Int) {\\n+    builder_ir(&var out, \\\"@.builder_handle_error = private unnamed_addr constant [38 x i8] c\\\\\\\"runtime error: invalid builder handle\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    builder_ir(&var out, \\\"@.builder_scalar_error = private unnamed_addr constant [30 x i8] c\\\\\\\"runtime error: invalid scalar\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    builder_ir(&var out, \\\"@.builder_length_error = private unnamed_addr constant [39 x i8] c\\\\\\\"runtime error: builder length overflow\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    builder_ir(&var out, \\\"@minivm_builder_fail_after = internal global i64 \\\")\\n+    try! out.append(fail_after)\\n+    builder_ir(&var out, \\\"\\\\n\\\\n\\\")\\n+\\n+    builder_ir(&var out, \\\"define internal void @minivm_builder_bad_handle() \\\\{\\\\nentry:\\\\n  call void @minivm_string_fatal(ptr @.builder_handle_error)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n+    builder_ir(&var out, \\\"define internal void @minivm_builder_length_overflow() \\\\{\\\\nentry:\\\\n  call void @minivm_string_fatal(ptr @.builder_length_error)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n+    builder_ir(&var out, \\\"define internal void @minivm_builder_invalid_scalar() \\\\{\\\\nentry:\\\\n  call void @minivm_string_fatal(ptr @.builder_scalar_error)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n+\\n+    builder_ir(&var out, \\\"define internal ptr @minivm_builder_new() \\\\{\\\\nentry:\\\\n  %empty = call ptr @minivm_string_new(ptr null, i64 0)\\\\n  ret ptr %empty\\\\n\\\\}\\\\n\\\\n\\\")\\n+    builder_ir(&var out, \\\"define internal ptr @minivm_builder_build(ptr %slot) \\\\{\\\\nentry:\\\\n  %slot_null = icmp eq ptr %slot, null\\\\n  br i1 %slot_null, label %bad, label %load\\\\nload:\\\\n  %value = load ptr, ptr %slot\\\\n  %value_null = icmp eq ptr %value, null\\\\n  br i1 %value_null, label %bad, label %ok\\\\nbad:\\\\n  call void @minivm_builder_bad_handle()\\\\n  unreachable\\\\nok:\\\\n  call void @minivm_string_retain(ptr %value)\\\\n  ret ptr %value\\\\n\\\\}\\\\n\\\\n\\\")\\n+\\n+    // The only recoverable allocator in the native builder path.  It installs\\n+    // replacement content after both copies complete, preserving the old value\\n+    // and every retained snapshot on failure.\\n+    builder_ir(&var out, \\\"define internal \\\\{i64, i64, i64\\\\} @minivm_builder_append_bytes(ptr %slot, ptr %extra, i64 %extra_len) \\\\{\\\\nentry:\\\\n  %slot_null = icmp eq ptr %slot, null\\\\n  br i1 %slot_null, label %bad, label %load\\\\nload:\\\\n  %old = load ptr, ptr %slot\\\\n  %old_null = icmp eq ptr %old, null\\\\n  br i1 %old_null, label %bad, label %check_extra\\\\nbad:\\\\n  call void @minivm_builder_bad_handle()\\\\n  unreachable\\\\ncheck_extra:\\\\n  %extra_negative = icmp slt i64 %extra_len, 0\\\\n  br i1 %extra_negative, label %overflow, label %empty_check\\\\nempty_check:\\\\n  %empty = icmp eq i64 %extra_len, 0\\\\n  br i1 %empty, label %success_empty, label %length\\\\nlength:\\\\n  %old_len_ptr = getelementptr i64, ptr %old, i64 1\\\\n  %old_len = load i64, ptr %old_len_ptr\\\\n  %old_negative = icmp slt i64 %old_len, 0\\\\n  %old_too_long = icmp sgt i64 %old_len, 9223372036854775791\\\\n  %room = sub i64 9223372036854775791, %old_len\\\\n  %too_long = icmp ugt i64 %extra_len, %room\\\\n  %old_bad = or i1 %old_negative, %old_too_long\\\\n  %bad_length = or i1 %old_bad, %too_long\\\\n  br i1 %bad_length, label %overflow, label %request\\\\noverflow:\\\\n  call void @minivm_builder_length_overflow()\\\\n  unreachable\\\\nrequest:\\\\n  %new_len = add i64 %old_len, %extra_len\\\\n  %size = add i64 %new_len, 16\\\\n  %counter = load i64, ptr @minivm_builder_fail_after\\\\n  %inject = icmp eq i64 %counter, 0\\\\n  br i1 %inject, label %inject_fail, label %allocate\\\\ninject_fail:\\\\n  store i64 -1, ptr @minivm_builder_fail_after\\\\n  br label %failure\\\\nallocate:\\\\n  %header = call ptr @malloc(i64 %size)\\\\n  %alloc_null = icmp eq ptr %header, null\\\\n  br i1 %alloc_null, label %failure, label %copy_old\\\\ncopy_old:\\\\n  store i64 1, ptr %header\\\\n  %new_len_ptr = getelementptr i64, ptr %header, i64 1\\\\n  store i64 %new_len, ptr %new_len_ptr\\\\n  %new_data = getelementptr i8, ptr %header, i64 16\\\\n  %old_data = getelementptr i8, ptr %old, i64 16\\\\n  call ptr @memcpy(ptr %new_data, ptr %old_data, i64 %old_len)\\\\n  %tail = getelementptr i8, ptr %new_data, i64 %old_len\\\\n  call ptr @memcpy(ptr %tail, ptr %extra, i64 %extra_len)\\\\n  %live = load i64, ptr @minivm_string_live\\\\n  %live_next = add i64 %live, 1\\\\n  store i64 %live_next, ptr @minivm_string_live\\\\n  store ptr %header, ptr %slot\\\\n  call void @minivm_string_release(ptr %old)\\\\n  %positive = icmp sgt i64 %counter, 0\\\\n  br i1 %positive, label %decrement, label %success\\\\ndecrement:\\\\n  %counter_next = sub i64 %counter, 1\\\\n  store i64 %counter_next, ptr @minivm_builder_fail_after\\\\n  br label %success\\\\nsuccess_empty:\\\\n  ret \\\\{i64, i64, i64\\\\} \\\\{i64 1, i64 0, i64 0\\\\}\\\\nsuccess:\\\\n  ret \\\\{i64, i64, i64\\\\} \\\\{i64 1, i64 0, i64 0\\\\}\\\\nfailure:\\\\n  %err0 = insertvalue \\\\{i64, i64, i64\\\\} undef, i64 0, 0\\\\n  %err1 = insertvalue \\\\{i64, i64, i64\\\\} %err0, i64 %size, 1\\\\n  %err2 = insertvalue \\\\{i64, i64, i64\\\\} %err1, i64 8, 2\\\\n  ret \\\\{i64, i64, i64\\\\} %err2\\\\n\\\\}\\\\n\\\\n\\\")\\n+\\n+    builder_ir(&var out, \\\"define internal \\\\{i64, i64, i64\\\\} @minivm_builder_append_string(ptr %slot, ptr %value) \\\\{\\\\nentry:\\\\n  %null = icmp eq ptr %value, null\\\\n  br i1 %null, label %bad, label %append\\\\nbad:\\\\n  call void @minivm_builder_bad_handle()\\\\n  unreachable\\\\nappend:\\\\n  %len = call i64 @minivm_string_len(ptr %value)\\\\n  %data = getelementptr i8, ptr %value, i64 16\\\\n  %result = call \\\\{i64, i64, i64\\\\} @minivm_builder_append_bytes(ptr %slot, ptr %data, i64 %len)\\\\n  ret \\\\{i64, i64, i64\\\\} %result\\\\n\\\\}\\\\n\\\\n\\\")\\n+    builder_ir(&var out, \\\"define internal \\\\{i64, i64, i64\\\\} @minivm_builder_append_byte(ptr %slot, i64 %value) \\\\{\\\\nentry:\\\\n  %storage = alloca i8\\\\n  %byte = trunc i64 %value to i8\\\\n  store i8 %byte, ptr %storage\\\\n  %result = call \\\\{i64, i64, i64\\\\} @minivm_builder_append_bytes(ptr %slot, ptr %storage, i64 1)\\\\n  ret \\\\{i64, i64, i64\\\\} %result\\\\n\\\\}\\\\n\\\\n\\\")\\n+\\n+    // Decimal digits are produced from the unsigned magnitude.  `0 - Int.min`\\n+    // intentionally wraps to 2^63, so Int.min never needs to be negated.\\n+    builder_ir(&var out, \\\"define internal \\\\{i64, i64, i64\\\\} @minivm_builder_append_int(ptr %slot, i64 %value) \\\\{\\\\nentry:\\\\n  %storage = alloca [20 x i8]\\\\n  %negative = icmp slt i64 %value, 0\\\\n  %negated = sub i64 0, %value\\\\n  %magnitude = select i1 %negative, i64 %negated, i64 %value\\\\n  br label %digits\\\\ndigits:\\\\n  %number = phi i64 [%magnitude, %entry], [%quotient, %digit_body]\\\\n  %cursor = phi i64 [20, %entry], [%digit_at, %digit_body]\\\\n  %digit_at = sub i64 %cursor, 1\\\\n  %remainder = urem i64 %number, 10\\\\n  %digit = trunc i64 %remainder to i8\\\\n  %ascii = add i8 %digit, 48\\\\n  %digit_ptr = getelementptr [20 x i8], ptr %storage, i64 0, i64 %digit_at\\\\n  store i8 %ascii, ptr %digit_ptr\\\\n  %quotient = udiv i64 %number, 10\\\\n  %more = icmp ne i64 %quotient, 0\\\\n  br i1 %more, label %digit_body, label %sign\\\\ndigit_body:\\\\n  br label %digits\\\\nsign:\\\\n  br i1 %negative, label %minus, label %append\\\\nminus:\\\\n  %minus_at = sub i64 %digit_at, 1\\\\n  %minus_ptr = getelementptr [20 x i8], ptr %storage, i64 0, i64 %minus_at\\\\n  store i8 45, ptr %minus_ptr\\\\n  br label %append\\\\nappend:\\\\n  %start = phi i64 [%digit_at, %sign], [%minus_at, %minus]\\\\n  %data = getelementptr [20 x i8], ptr %storage, i64 0, i64 %start\\\\n  %len = sub i64 20, %start\\\\n  %result = call \\\\{i64, i64, i64\\\\} @minivm_builder_append_bytes(ptr %slot, ptr %data, i64 %len)\\\\n  ret \\\\{i64, i64, i64\\\\} %result\\\\n\\\\}\\\\n\\\\n\\\")\\n+\\n+    builder_ir(&var out, \\\"define internal \\\\{i64, i64, i64\\\\} @minivm_builder_append_scalar(ptr %slot, i64 %scalar) \\\\{\\\\nentry:\\\\n  %negative = icmp slt i64 %scalar, 0\\\\n  %high = icmp sgt i64 %scalar, 1114111\\\\n  %surrogate_low = icmp sge i64 %scalar, 55296\\\\n  %surrogate_high = icmp sle i64 %scalar, 57343\\\\n  %surrogate = and i1 %surrogate_low, %surrogate_high\\\\n  %range_bad = or i1 %negative, %high\\\\n  %bad = or i1 %range_bad, %surrogate\\\\n  br i1 %bad, label %invalid, label %encode\\\\ninvalid:\\\\n  call void @minivm_builder_invalid_scalar()\\\\n  unreachable\\\\nencode:\\\\n  %storage = alloca [4 x i8]\\\\n  %one = icmp ule i64 %scalar, 127\\\\n  br i1 %one, label %encode1, label %check2\\\\nencode1:\\\\n  %p10 = getelementptr [4 x i8], ptr %storage, i64 0, i64 0\\\\n  %b10 = trunc i64 %scalar to i8\\\\n  store i8 %b10, ptr %p10\\\\n  br label %append1\\\\ncheck2:\\\\n  %two = icmp ule i64 %scalar, 2047\\\\n  br i1 %two, label %encode2, label %check3\\\\nencode2:\\\\n  %x20 = lshr i64 %scalar, 6\\\\n  %x21 = and i64 %scalar, 63\\\\n  %b20a = trunc i64 %x20 to i8\\\\n  %b21a = trunc i64 %x21 to i8\\\\n  %b20 = or i8 %b20a, -64\\\\n  %b21 = or i8 %b21a, -128\\\\n  %p20 = getelementptr [4 x i8], ptr %storage, i64 0, i64 0\\\\n  %p21 = getelementptr [4 x i8], ptr %storage, i64 0, i64 1\\\\n  store i8 %b20, ptr %p20\\\\n  store i8 %b21, ptr %p21\\\\n  br label %append2\\\\ncheck3:\\\\n  %three = icmp ule i64 %scalar, 65535\\\\n  br i1 %three, label %encode3, label %encode4\\\\nencode3:\\\\n  %x30 = lshr i64 %scalar, 12\\\\n  %x31s = lshr i64 %scalar, 6\\\\n  %x31 = and i64 %x31s, 63\\\\n  %x32 = and i64 %scalar, 63\\\\n  %b30a = trunc i64 %x30 to i8\\\\n  %b31a = trunc i64 %x31 to i8\\\\n  %b32a = trunc i64 %x32 to i8\\\\n  %b30 = or i8 %b30a, -32\\\\n  %b31 = or i8 %b31a, -128\\\\n  %b32 = or i8 %b32a, -128\\\\n  %p30 = getelementptr [4 x i8], ptr %storage, i64 0, i64 0\\\\n  %p31 = getelementptr [4 x i8], ptr %storage, i64 0, i64 1\\\\n  %p32 = getelementptr [4 x i8], ptr %storage, i64 0, i64 2\\\\n  store i8 %b30, ptr %p30\\\\n  store i8 %b31, ptr %p31\\\\n  store i8 %b32, ptr %p32\\\\n  br label %append3\\\\nencode4:\\\\n  %x40 = lshr i64 %scalar, 18\\\\n  %x41s = lshr i64 %scalar, 12\\\\n  %x41 = and i64 %x41s, 63\\\\n  %x42s = lshr i64 %scalar, 6\\\\n  %x42 = and i64 %x42s, 63\\\\n  %x43 = and i64 %scalar, 63\\\\n  %b40a = trunc i64 %x40 to i8\\\\n  %b41a = trunc i64 %x41 to i8\\\\n  %b42a = trunc i64 %x42 to i8\\\\n  %b43a = trunc i64 %x43 to i8\\\\n  %b40 = or i8 %b40a, -16\\\\n  %b41 = or i8 %b41a, -128\\\\n  %b42 = or i8 %b42a, -128\\\\n  %b43 = or i8 %b43a, -128\\\\n  %p40 = getelementptr [4 x i8], ptr %storage, i64 0, i64 0\\\\n  %p41 = getelementptr [4 x i8], ptr %storage, i64 0, i64 1\\\\n  %p42 = getelementptr [4 x i8], ptr %storage, i64 0, i64 2\\\\n  %p43 = getelementptr [4 x i8], ptr %storage, i64 0, i64 3\\\\n  store i8 %b40, ptr %p40\\\\n  store i8 %b41, ptr %p41\\\\n  store i8 %b42, ptr %p42\\\\n  store i8 %b43, ptr %p43\\\\n  br label %append4\\\\nappend1:\\\\n  %r1 = call \\\\{i64, i64, i64\\\\} @minivm_builder_append_bytes(ptr %slot, ptr %storage, i64 1)\\\\n  ret \\\\{i64, i64, i64\\\\} %r1\\\\nappend2:\\\\n  %r2 = call \\\\{i64, i64, i64\\\\} @minivm_builder_append_bytes(ptr %slot, ptr %storage, i64 2)\\\\n  ret \\\\{i64, i64, i64\\\\} %r2\\\\nappend3:\\\\n  %r3 = call \\\\{i64, i64, i64\\\\} @minivm_builder_append_bytes(ptr %slot, ptr %storage, i64 3)\\\\n  ret \\\\{i64, i64, i64\\\\} %r3\\\\nappend4:\\\\n  %r4 = call \\\\{i64, i64, i64\\\\} @minivm_builder_append_bytes(ptr %slot, ptr %storage, i64 4)\\\\n  ret \\\\{i64, i64, i64\\\\} %r4\\\\n\\\\}\\\\n\\\\n\\\")\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 51c7c333..4930abf0 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -92,7 +92,7 @@ func type_name(t: ValueType) -> String {\\n     match t {\\n         case I8 -> \\\"Int8\\\", case I16 -> \\\"Int16\\\", case I32 -> \\\"Int32\\\", case I64 -> \\\"Int\\\",\\n         case U8 -> \\\"UInt8\\\", case U16 -> \\\"UInt16\\\", case U32 -> \\\"UInt32\\\", case U64 -> \\\"UInt\\\",\\n-        case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case String -> \\\"String\\\", case Void -> \\\"Void\\\",\\n+        case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case String -> \\\"String\\\", case Builder -> \\\"StringBuilder\\\", case Void -> \\\"Void\\\",\\n         case Record(_) -> \\\"record\\\", case Enum(_) -> \\\"enum\\\", case Ref(_) -> \\\"reference\\\",\\n         case Optional(_) -> \\\"Optional\\\", case Result(_) -> \\\"Result\\\"\\n     }\\n@@ -150,6 +150,7 @@ struct Compiler {\\n     current_result: ValueType\\n     scalar_record: Int\\n     invalid_scalar_record: Int\\n+    alloc_error_record: Int\\n }\\n extension Compiler: NoCopy {}\\n \\n@@ -210,7 +211,7 @@ extension Compiler {\\n             if reference_type(target) { return problem(\\\"references to references are not supported\\\", amp.line, amp.col) }\\n             var supported = scalar_type(target)\\n             match target { case Record(_) -> { supported = true }, case _ -> {} }\\n-            if target == ValueType.String { supported = true }\\n+            if target == ValueType.String || target == ValueType.Builder { supported = true }\\n             match target { case Optional(_) -> { supported = true }, case _ -> {} }\\n             match target { case Result(_) -> { supported = true }, case _ -> {} }\\n             if not supported { return problem(\\\"only data references are supported\\\", amp.line, amp.col) }\\n@@ -228,11 +229,15 @@ extension Compiler {\\n             if reference_type(success) || reference_type(error) {\\n                 return problem(\\\"Result reference payloads are not supported\\\", t.line, t.col)\\n             }\\n+            if success == ValueType.Builder || error == ValueType.Builder {\\n+                return problem(\\\"StringBuilder cannot be stored in Result\\\", t.line, t.col)\\n+            }\\n             ty = self.intern_result(success, error)\\n         }\\n         else if t.value == \\\"Void\\\" { ty = ValueType.Void }\\n         else if t.value == \\\"Bool\\\" { ty = ValueType.Bool }\\n         else if t.value == \\\"String\\\" { ty = ValueType.String }\\n+        else if t.value == \\\"StringBuilder\\\" { ty = ValueType.Builder }\\n         else if let integer = named_integer_type(t.value) { ty = integer }\\n         else {\\n             let ri = self.record_index(t.value)\\n@@ -252,6 +257,7 @@ extension Compiler {\\n                 if nesting >= 128 { return problem(\\\"Optional nesting limit exceeded\\\", suffix.line, suffix.col) }\\n                 if ty == ValueType.Void { return problem(\\\"Optional<Void> is not supported\\\", suffix.line, suffix.col) }\\n                 if reference_type(ty) { return problem(\\\"Optional reference payloads are not supported\\\", suffix.line, suffix.col) }\\n+                if ty == ValueType.Builder { return problem(\\\"StringBuilder cannot be stored in Optional\\\", suffix.line, suffix.col) }\\n                 ty = self.intern_optional(ty)\\n                 nesting += 1\\n                 n += 1\\n@@ -360,7 +366,7 @@ extension Compiler {\\n     }\\n \\n     func reserved_type_name(&self, name: String) -> Bool {\\n-        if name == \\\"Bool\\\" || name == \\\"String\\\" || name == \\\"Void\\\" || name == \\\"Scalar\\\" || name == \\\"InvalidScalar\\\" { return true }\\n+        if name == \\\"Bool\\\" || name == \\\"String\\\" || name == \\\"StringBuilder\\\" || name == \\\"Void\\\" || name == \\\"Scalar\\\" || name == \\\"InvalidScalar\\\" || name == \\\"AllocError\\\" { return true }\\n         if let _ = named_integer_type(name) { return true }\\n         false\\n     }\\n@@ -440,6 +446,9 @@ extension Compiler {\\n         let extension_token = self.take()\\n         let target = try self.expect(TokenKind.Ident, \\\"expected extension target\\\")\\n         let owner = self.record_index(target.value)\\n+        if target.value == \\\"StringBuilder\\\" || target.value == \\\"AllocError\\\" {\\n+            return problem(\\\"builtin type `{target.value}` cannot be extended\\\", target.line, target.col)\\n+        }\\n         if owner < 0 { return problem(\\\"extension target must be a known record\\\", target.line, target.col) }\\n         if owner == self.scalar_record || owner == self.invalid_scalar_record {\\n             return problem(\\\"builtin record `{target.value}` cannot be extended\\\", target.line, target.col)\\n@@ -486,6 +495,7 @@ extension Compiler {\\n                     let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after parameter name\\\")\\n                     let pt = try self.parse_type()\\n                     if pt == ValueType.Void { return problem(\\\"parameter cannot have Void type\\\", pn.line, pn.col) }\\n+                    if pt == ValueType.Builder { return problem(\\\"StringBuilder parameters must be references\\\", pn.line, pn.col) }\\n                     try! pnames.push(pn.value)\\n                     try! ptypes.push(pt)\\n                     self.skip_newlines()\\n@@ -501,6 +511,7 @@ extension Compiler {\\n                 let _ = self.take()\\n                 result = try self.parse_type()\\n                 if reference_type(result) { return problem(\\\"reference results are not supported\\\", method.line, method.col) }\\n+                if result == ValueType.Builder { return problem(\\\"StringBuilder cannot be returned by value\\\", method.line, method.col) }\\n             }\\n             let _ = try self.expect(TokenKind.LBrace, \\\"expected method body\\\")\\n             let start = self.pos\\n@@ -582,6 +593,7 @@ extension Compiler {\\n                     self.skip_newlines()\\n                     let pt = try self.parse_type()\\n                     if pt == ValueType.Void { return problem(\\\"parameter cannot have Void type\\\", pn.line, pn.col) }\\n+                    if pt == ValueType.Builder { return problem(\\\"StringBuilder parameters must be references\\\", pn.line, pn.col) }\\n                     try! pnames.push(pn.value)\\n                     try! ptypes.push(pt)\\n                     self.skip_newlines()\\n@@ -597,6 +609,7 @@ extension Compiler {\\n                 let _ = self.take()\\n                 result = try self.parse_type()\\n                 if reference_type(result) { return problem(\\\"reference results are not supported\\\", name.line, name.col) }\\n+                if result == ValueType.Builder { return problem(\\\"StringBuilder cannot be returned by value\\\", name.line, name.col) }\\n             }\\n             let _ = try self.expect(TokenKind.LBrace, \\\"expected function body\\\")\\n             let start = self.pos\\n@@ -745,6 +758,7 @@ extension Compiler {\\n             let ty = try self.parse_type()\\n             if ty == ValueType.Void { return problem(\\\"record field cannot have Void type\\\", field.line, field.col) }\\n             if reference_type(ty) { return problem(\\\"reference record fields are not supported\\\", field.line, field.col) }\\n+            if ty == ValueType.Builder { return problem(\\\"StringBuilder cannot be stored in a record\\\", field.line, field.col) }\\n             try! names.push(field.value)\\n             try! types.push(ty)\\n             if self.at(TokenKind.Comma) {\\n@@ -793,7 +807,7 @@ extension Compiler {\\n     func new_slot(&var self, ty: ValueType) -> Int {\\n         let n = self.slots.len()\\n         try! self.slots.push(ty)\\n-        if ty == ValueType.String { try! self.string_slots.push(StringSlot(slot: n, depth: self.scope_depth)) }\\n+        if owning_type(ty) { try! self.string_slots.push(StringSlot(slot: n, depth: self.scope_depth)) }\\n         n\\n     }\\n \\n@@ -811,6 +825,7 @@ extension Compiler {\\n     func emit_copy_value(&var self, dst: Int, src: Int, ty: ValueType,\\n                          t: &Token) -> Result<Void, ProtoError> {\\n         if dst == src { return }\\n+        if ty == ValueType.Builder { return problem(\\\"StringBuilder values cannot be copied\\\", t.line, t.col) }\\n         let layout = try value_layout(&self.records, &self.sums, ty)\\n         if layout.len() > 1 {\\n             let temporary = try self.new_value(ty)\\n@@ -842,6 +857,7 @@ extension Compiler {\\n \\n     func snapshot(&var self, value: ExprValue, t: &Token) -> Result<ExprValue, ProtoError> {\\n         if value.ty == ValueType.Void { return value }\\n+        if value.ty == ValueType.Builder { return problem(\\\"StringBuilder values cannot be copied\\\", t.line, t.col) }\\n         let dst = try self.new_value(value.ty)\\n         try self.emit_copy_value(dst, value.slot, value.ty, &t)\\n         expr_value(value.ty, dst, value.call)\\n@@ -947,6 +963,7 @@ extension Compiler {\\n \\n     func read_place(&var self, place: Place, t: &Token) -> Result<ExprValue, ProtoError> {\\n         try self.check_read(place.root, &t)\\n+        if place.ty == ValueType.Builder { return problem(\\\"StringBuilder values cannot be copied\\\", t.line, t.col) }\\n         if place.indirect {\\n             let dst = try self.new_value(place.ty)\\n             let at = self.emit_at(Op.LoadRef, t.line, t.col)\\n@@ -959,6 +976,7 @@ extension Compiler {\\n \\n     func store_place(&var self, place: Place, value: ExprValue,\\n                      t: &Token) -> Result<Void, ProtoError> {\\n+        if place.ty == ValueType.Builder { return problem(\\\"StringBuilder values cannot be assigned\\\", t.line, t.col) }\\n         if place.indirect {\\n             let at = self.emit_at(Op.StoreRef, t.line, t.col)\\n             self.code[at].a = place.slot\\n@@ -1156,6 +1174,9 @@ extension Compiler {\\n     }\\n \\n     func transfer(&var self, v: ExprValue, target: ValueType, t: &Token) -> Result<ExprValue, ProtoError> {\\n+        if v.ty == ValueType.Builder || target == ValueType.Builder {\\n+            return problem(\\\"StringBuilder values cannot be copied\\\", t.line, t.col)\\n+        }\\n         if v.none_literal {\\n             match target {\\n                 case Optional(_) -> { return self.initialize_optional_none(target, &t) },\\n@@ -1335,6 +1356,7 @@ extension Compiler {\\n                         let receiver = try self.read_place(place, &candidate)\\n                         return self.parse_string_intrinsic(receiver, method)\\n                     }\\n+                    if place.ty == ValueType.Builder { return self.parse_builder_method(place, method) }\\n                     let owner = match place.ty { case Record(index) -> index, case _ -> -1 }\\n                     if owner < 0 { return problem(\\\"method receiver must be a record place\\\", dot.line, dot.col) }\\n                     if owner == self.scalar_record {\\n@@ -1362,6 +1384,7 @@ extension Compiler {\\n         }\\n         if self.at(TokenKind.Ident) {\\n             let name = self.take()\\n+            if name.value == \\\"StringBuilder\\\" && self.at(TokenKind.LParen) { return self.parse_builder_constructor(name) }\\n             let ei = self.enum_index(name.value)\\n             if ei >= 0 && self.at(TokenKind.Dot) {\\n                 let _ = self.take()\\n@@ -1522,6 +1545,111 @@ extension Compiler {\\n         (name, place)\\n     }\\n \\n+    func builder_result_type(&var self) -> ValueType {\\n+        self.intern_result(ValueType.Void, ValueType.Record(self.alloc_error_record))\\n+    }\\n+\\n+    func parse_builder_constructor(&var self, name: Token) -> Result<ExprValue, ProtoError> {\\n+        let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after StringBuilder\\\")\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        if not self.at(TokenKind.RParen) { return problem(\\\"StringBuilder constructor accepts no arguments\\\", name.line, name.col) }\\n+        let _ = self.take()\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        let dst = self.new_slot(ValueType.Builder)\\n+        let at = self.emit_at(Op.BuilderNew, name.line, name.col)\\n+        self.code[at].dst = dst\\n+        expr_value(ValueType.Builder, dst, true)\\n+    }\\n+\\n+    func parse_builder_method(&var self, receiver: Place, name: Token) -> Result<ExprValue, ProtoError> {\\n+        let saved_borrows = self.active_borrows.len()\\n+        let mutable = name.value == \\\"append\\\"\\n+        if name.value != \\\"append\\\" && name.value != \\\"build\\\" { return problem(\\\"StringBuilder has no method `{name.value}`\\\", name.line, name.col) }\\n+        if mutable && not receiver.mutable { return problem(\\\"cannot call a mutating method on an immutable receiver\\\", name.line, name.col) }\\n+        try self.register_borrow(&receiver, mutable, &name)\\n+        let ref_ty = self.intern_reference(ValueType.Builder, mutable)\\n+        let receiver_slot = self.new_slot(ref_ty)\\n+        if receiver.indirect {\\n+            if not reference_transfer(&self.references, receiver.ref_ty, ref_ty) { return problem(\\\"receiver reference mutability does not match method\\\", name.line, name.col) }\\n+            let copy = self.emit_at(Op.Copy, name.line, name.col)\\n+            self.code[copy].dst = receiver_slot\\n+            self.code[copy].a = receiver.slot\\n+        } else {\\n+            let addr = self.emit_at(Op.Addr, name.line, name.col)\\n+            self.code[addr].dst = receiver_slot\\n+            self.code[addr].a = receiver.slot\\n+        }\\n+        let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after StringBuilder method\\\")\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        if name.value == \\\"build\\\" {\\n+            if not self.at(TokenKind.RParen) { return problem(\\\"StringBuilder.build accepts no arguments\\\", name.line, name.col) }\\n+            let _ = self.take()\\n+            self.paren_depth -= 1\\n+            self.depth -= 1\\n+            let dst = self.new_slot(ValueType.String)\\n+            let at = self.emit_at(Op.BuilderBuild, name.line, name.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = receiver_slot\\n+            self.restore_borrows(saved_borrows)\\n+            return expr_value(ValueType.String, dst, true)\\n+        }\\n+        if self.at(TokenKind.RParen) { return problem(\\\"StringBuilder.append requires exactly one argument\\\", name.line, name.col) }\\n+        var supplied_label: String? = None\\n+        var label_token = name\\n+        if self.at(TokenKind.Ident) && self.pos + 1 < self.tokens.len() && self.tokens[self.pos + 1].kind == TokenKind.Colon {\\n+            label_token = self.take()\\n+            supplied_label = Some(label_token.value)\\n+            let _ = self.take()\\n+            self.expr_space()\\n+        }\\n+        var argument = try self.parse_expr()\\n+        if argument.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n+        var expected_label = \\\"\\\"\\n+        var op = Op.BuilderAppendString\\n+        if argument.ty == ValueType.String { expected_label = \\\"s\\\" }\\n+        else if argument.ty == ValueType.Byte {\\n+            expected_label = \\\"b\\\"\\n+            op = Op.BuilderAppendByte\\n+        }\\n+        else if argument.ty == ValueType.I64 {\\n+            expected_label = \\\"value\\\"\\n+            op = Op.BuilderAppendInt\\n+        }\\n+        else {\\n+            match argument.ty {\\n+                case Record(index) -> {\\n+                    if index != self.scalar_record { return problem(\\\"StringBuilder.append argument must be String, Byte, Int, or Scalar\\\", name.line, name.col) }\\n+                    expected_label = \\\"scalar\\\"\\n+                    op = Op.BuilderAppendScalar\\n+                },\\n+                case _ -> { return problem(\\\"StringBuilder.append argument must be String, Byte, Int, or Scalar\\\", name.line, name.col) }\\n+            }\\n+        }\\n+        if let label = supplied_label {\\n+            if label != expected_label { return problem(\\\"argument label `{label}` does not match parameter `{expected_label}`\\\", label_token.line, label_token.col) }\\n+        }\\n+        argument = try self.snapshot(argument, &name)\\n+        self.skip_newlines()\\n+        if self.at(TokenKind.Comma) { return problem(\\\"StringBuilder.append requires exactly one argument\\\", name.line, name.col) }\\n+        let _ = try self.expect(TokenKind.RParen, \\\"StringBuilder.append requires exactly one argument\\\")\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        let result_ty = self.builder_result_type()\\n+        let dst = try self.new_value(result_ty)\\n+        let at = self.emit_at(op, name.line, name.col)\\n+        self.code[at].dst = dst\\n+        self.code[at].a = receiver_slot\\n+        self.code[at].b = argument.slot\\n+        match result_ty { case Result(index) -> { self.code[at].immediate = index }, case _ -> {} }\\n+        self.restore_borrows(saved_borrows)\\n+        expr_value(result_ty, dst, true)\\n+    }\\n+\\n     func parse_primary(&var self) -> Result<ExprValue, ProtoError> {\\n         var value = try self.parse_atom()\\n         self.expr_space()\\n@@ -3447,12 +3575,20 @@ extension Compiler {\\n                 if reference_type(local_type) { return problem(\\\"reference locals are not supported\\\", name.line, name.col) }\\n             }\\n             let _ = try self.expect(TokenKind.Assign, \\\"local declarations require an initializer\\\")\\n-            var value = if let ty = annotated { try self.parse_expected_expr(ty) } else { try self.parse_expr() }\\n+            var value = if let ty = annotated {\\n+                if ty == ValueType.Builder { try self.parse_expr() } else { try self.parse_expected_expr(ty) }\\n+            } else { try self.parse_expr() }\\n             if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n             if value.none_literal { return problem(\\\"None requires an expected Optional type\\\", name.line, name.col) }\\n             if value.ty == ValueType.Void { return problem(\\\"cannot initialize a local with Void\\\", name.line, name.col) }\\n             if reference_type(value.ty) { return problem(\\\"reference locals are not supported\\\", name.line, name.col) }\\n-            if let _ = annotated {} else { value = try self.transfer(value, value.ty, &name) }\\n+            if let ty = annotated {\\n+                if value.ty != ty { return problem(\\\"cannot implicitly convert {self.display_type(value.ty)} to {self.display_type(ty)}\\\", name.line, name.col) }\\n+            } else if value.ty != ValueType.Builder { value = try self.transfer(value, value.ty, &name) }\\n+            if value.ty == ValueType.Builder {\\n+                try self.declare(name, value.ty, value.slot, kw.kind == TokenKind.Var)\\n+                return false\\n+            }\\n             let dst = try self.new_value(value.ty)\\n             try self.emit_copy_value(dst, value.slot, value.ty, &name)\\n             try self.declare(name, value.ty, dst, kw.kind == TokenKind.Var)\\n@@ -3562,6 +3698,7 @@ extension Compiler {\\n             let name = parsed.0\\n             let place = parsed.1\\n             let assign = self.take()\\n+            if place.ty == ValueType.Builder { return problem(\\\"StringBuilder values cannot be assigned\\\", assign.line, assign.col) }\\n             if not place.mutable {\\n                 if place.indirect { return problem(\\\"cannot assign through a shared reference\\\", name.line, name.col) }\\n                 return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col)\\n@@ -3706,6 +3843,10 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n     let invalid_types: Vector<ValueType> = [ValueType.I64, ValueType.I64]\\n     try! records.push(RecordIR(name: \\\"InvalidScalar\\\", field_names: move invalid_fields,\\n                                field_types: move invalid_types, line: 0, col: 0))\\n+    let alloc_fields: Vector<String> = [\\\"size\\\", \\\"align\\\"]\\n+    let alloc_types: Vector<ValueType> = [ValueType.I64, ValueType.I64]\\n+    try! records.push(RecordIR(name: \\\"AllocError\\\", field_names: move alloc_fields,\\n+                               field_types: move alloc_types, line: 0, col: 0))\\n     var compiler = Compiler(source: source, tokens: move tokens, records: move records, enums: Vector<EnumIR>(),\\n                             references: Vector<ReferenceIR>(), sums: Vector<SumIR>(), strings: Vector<String>(), statics: Vector<StaticBinding>(),\\n                             signatures: Vector<Signature>(), pos: 0, depth: 0, scope_depth: 0,\\n@@ -3714,7 +3855,7 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n                             string_slots: Vector<StringSlot>(),\\n                             slots: Vector<ValueType>(),\\n                             code: Vector<Instruction>(), current_result: ValueType.Void,\\n-                            scalar_record: 0, invalid_scalar_record: 1)\\n+                            scalar_record: 0, invalid_scalar_record: 1, alloc_error_record: 2)\\n     try compiler.collect_record_names()\\n     try compiler.scan_signatures()\\n     try validate_records(&compiler.records, &compiler.sums)\\n@@ -3731,5 +3872,6 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n     }\\n     Program(enums: compiler.enums.copy(), records: compiler.records.copy(), references: compiler.references.copy(), sums: compiler.sums.copy(),\\n             strings: compiler.strings.copy(),\\n-            functions: move functions, main_index: main_index)\\n+            functions: move functions, main_index: main_index,\\n+            alloc_error_record: compiler.alloc_error_record, builder_fail_after: -1)\\n }\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex 53bc7d90..c1dedc2d 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -1,5 +1,6 @@\\n import src.model.*\\n import src.string_runtime.*\\n+import src.builder_runtime.*\\n import src.uint_parse_runtime.*\\n \\n func put(out: &var StringBuilder, text: String) {\\n@@ -405,6 +406,39 @@ func emit_division(out: &var StringBuilder, instruction_index: Int,\\n     next_block(&var out, instruction_index)\\n }\\n \\n+func emit_builder_append(out: &var StringBuilder, function: &FuncIR,\\n+                         instruction_index: Int, inst: &Instruction,\\n+                         runtime_name: String) {\\n+    put(&var out, \\\"  \\\")\\n+    load_ref_slot(&var out, instruction_index, \\\"builder\\\", inst.a)\\n+    put(&var out, \\\"  \\\")\\n+    let pointer_argument = string_type(function.slots[inst.b])\\n+    if pointer_argument { load_ref_slot(&var out, instruction_index, \\\"argument\\\", inst.b) }\\n+    else { load_slot(&var out, instruction_index, \\\"argument\\\", inst.b) }\\n+    put(&var out, \\\"  \\\")\\n+    temp_name(&var out, instruction_index, \\\"append\\\")\\n+    put(&var out, \\\" = call \\\\{i64, i64, i64\\\\} @\\\")\\n+    put(&var out, runtime_name)\\n+    put(&var out, \\\"(ptr \\\")\\n+    temp_name(&var out, instruction_index, \\\"builder\\\")\\n+    put(&var out, if pointer_argument { \\\", ptr \\\" } else { \\\", i64 \\\" })\\n+    temp_name(&var out, instruction_index, \\\"argument\\\")\\n+    put(&var out, \\\")\\\\n\\\")\\n+    var word = 0\\n+    while word < 3 {\\n+        put(&var out, \\\"  \\\")\\n+        temp_name(&var out, instruction_index, \\\"appendword{word}\\\")\\n+        put(&var out, \\\" = extractvalue \\\\{i64, i64, i64\\\\} \\\")\\n+        temp_name(&var out, instruction_index, \\\"append\\\")\\n+        put(&var out, \\\", \\\")\\n+        put_int(&var out, word)\\n+        put(&var out, \\\"\\\\n\\\")\\n+        store_temp(&var out, instruction_index, \\\"appendword{word}\\\", inst.dst + word)\\n+        word += 1\\n+    }\\n+    next_block(&var out, instruction_index)\\n+}\\n+\\n func emit_instruction(out: &var StringBuilder, program: &Program,\\n                       function: &FuncIR, instruction_index: Int,\\n                       inst: &Instruction) -> Result<Void, ProtoError> {\\n@@ -820,6 +854,40 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             store_temp(&var out, instruction_index, \\\"value\\\", inst.dst + 1)\\n             next_block(&var out, instruction_index)\\n         },\\n+        case BuilderNew -> {\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"newbuilder\\\")\\n+            put(&var out, \\\" = call ptr @minivm_builder_new()\\\\n\\\")\\n+            assign_string_temp(&var out, instruction_index, \\\"newbuilder\\\", inst.dst, false)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case BuilderBuild -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"builder\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"snapshot\\\")\\n+            put(&var out, \\\" = call ptr @minivm_builder_build(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"builder\\\")\\n+            put(&var out, \\\")\\\\n\\\")\\n+            assign_string_temp(&var out, instruction_index, \\\"snapshot\\\", inst.dst, false)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case BuilderAppendString -> {\\n+            emit_builder_append(&var out, &function, instruction_index, &inst,\\n+                                \\\"minivm_builder_append_string\\\")\\n+        },\\n+        case BuilderAppendByte -> {\\n+            emit_builder_append(&var out, &function, instruction_index, &inst,\\n+                                \\\"minivm_builder_append_byte\\\")\\n+        },\\n+        case BuilderAppendInt -> {\\n+            emit_builder_append(&var out, &function, instruction_index, &inst,\\n+                                \\\"minivm_builder_append_int\\\")\\n+        },\\n+        case BuilderAppendScalar -> {\\n+            emit_builder_append(&var out, &function, instruction_index, &inst,\\n+                                \\\"minivm_builder_append_scalar\\\")\\n+        },\\n         case Drop -> {\\n             put(&var out, \\\"  \\\")\\n             load_ref_slot(&var out, instruction_index, \\\"value\\\", inst.a)\\n@@ -887,7 +955,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             }\\n             var cleanup = 0\\n             while cleanup < function.slots.len() {\\n-                if string_type(function.slots[cleanup]) {\\n+                if owning_type(function.slots[cleanup]) {\\n                     put(&var out, \\\"  \\\")\\n                     load_ref_slot(&var out, instruction_index, \\\"cleanup{cleanup}\\\", cleanup)\\n                     put(&var out, \\\"  call void @minivm_string_release(ptr \\\")\\n@@ -993,7 +1061,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     }\\n     slot_index = 0\\n     while slot_index < function.slots.len() {\\n-        if string_type(function.slots[slot_index]) {\\n+        if owning_type(function.slots[slot_index]) {\\n             put(&var out, \\\"  store ptr null, ptr \\\")\\n             slot_name(&var out, slot_index)\\n             put(&var out, \\\"\\\\n\\\")\\n@@ -1058,6 +1126,7 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.usub.with.overflow.i64(i64, i64)\\\\n\\\")\\n     put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.umul.with.overflow.i64(i64, i64)\\\\n\\\\n\\\")\\n     emit_string_runtime(&var out)\\n+    emit_builder_runtime(&var out, program.builder_fail_after)\\n     emit_uint_parser(&var out)\\n     put(&var out, \\\"define internal void @minivm_overflow() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.overflow)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     put(&var out, \\\"define internal void @minivm_divzero() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.divzero)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex 6cf8f197..f80950e9 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -1,7 +1,7 @@\\n public enum ValueType {\\n     case I8, case I16, case I32, case I64,\\n     case U8, case U16, case U32, case U64, case Byte,\\n-    case Bool, case Void, case String, case Record(index: Int), case Enum(index: Int), case Ref(index: Int),\\n+    case Bool, case Void, case String, case Builder, case Record(index: Int), case Enum(index: Int), case Ref(index: Int),\\n     case Optional(index: Int), case Result(index: Int)\\n }\\n @synthesize\\n@@ -13,7 +13,9 @@ public enum Op {\\n     case Jump, case Branch, case Call, case Print, case Return, case Panic,\\n     case Cast, case Truncate, case WrapAdd, case WrapSub, case WrapMul,\\n     case Addr, case LoadRef, case StoreRef, case RefField,\\n-    case StringConst, case StringLen, case StringByteAt, case StringSlice, case StringToUInt, case Drop,\\n+    case StringConst, case StringLen, case StringByteAt, case StringSlice, case StringToUInt,\\n+    case BuilderNew, case BuilderBuild, case BuilderAppendString, case BuilderAppendByte,\\n+    case BuilderAppendInt, case BuilderAppendScalar, case Drop,\\n     case BitAnd, case BitOr, case BitXor, case BitNot, case Shl, case Shr\\n }\\n @synthesize\\n@@ -137,6 +139,8 @@ public struct Program {\\n     public sums: Vector<SumIR>\\n     public functions: Vector<FuncIR>\\n     public main_index: Int\\n+    public alloc_error_record: Int\\n+    public builder_fail_after: Int\\n }\\n \\n public struct ReferenceIR {\\n@@ -186,6 +190,8 @@ public func printable_type(t: ValueType) -> Bool {\\n }\\n \\n public func string_type(t: ValueType) -> Bool { t == ValueType.String }\\n+public func builder_type(t: ValueType) -> Bool { t == ValueType.Builder }\\n+public func owning_type(t: ValueType) -> Bool { string_type(t) || builder_type(t) }\\n \\n public func scalar_type(t: ValueType) -> Bool {\\n     match t {\\n@@ -271,7 +277,7 @@ public func reference_transfer(refs: &Vector<ReferenceIR>, source: ValueType, ta\\n public func validate_references(refs: &Vector<ReferenceIR>) -> Result<Void, ProtoError> {\\n     var n = 0\\n     while n < refs.len() {\\n-        if not scalar_type(refs[n].target) && not string_type(refs[n].target) {\\n+        if not scalar_type(refs[n].target) && not string_type(refs[n].target) && not builder_type(refs[n].target) {\\n             match refs[n].target {\\n                 case Record(_) -> {},\\n                 case Optional(_) -> {},\\n@@ -311,6 +317,7 @@ public func validate_enums(enums: &Vector<EnumIR>) -> Result<Void, ProtoError> {\\n func append_layout(records: &Vector<RecordIR>, sums: &Vector<SumIR>, t: ValueType,\\n                    record_path: &var Vector<Int>, sum_path: &var Vector<Int>, depth: Int,\\n                    words: &var Vector<ValueType>) -> Result<Void, ProtoError> {\\n+    if builder_type(t) { return problem(\\\"StringBuilder cannot be stored in an aggregate\\\", 0, 0) }\\n     if scalar_type(t) || string_type(t) {\\n         if words.len() >= 256 { return problem(\\\"record layout exceeds 256 words\\\", 0, 0) }\\n         try! words.push(t)\\n@@ -385,7 +392,7 @@ func append_layout(records: &Vector<RecordIR>, sums: &Vector<SumIR>, t: ValueTyp\\n \\n public func value_layout(records: &Vector<RecordIR>, sums: &Vector<SumIR>, t: ValueType) -> Result<Vector<ValueType>, ProtoError> {\\n     var words = Vector<ValueType>()\\n-    if reference_type(t) || string_type(t) {\\n+    if reference_type(t) || string_type(t) || builder_type(t) {\\n         try! words.push(t)\\n         return move words\\n     }\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex d6cb96a0..83f05536 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -65,9 +65,11 @@ func check_declared_types(p: &Program) -> Result<Void, ProtoError> {\\n         let f = p.functions[n].copy()\\n         try check_type(&p, f.result)\\n         if reference_type(f.result) { return problem(\\\"reference function results are not supported\\\", 0, 0) }\\n+        if builder_type(f.result) { return problem(\\\"StringBuilder function results are not supported\\\", 0, 0) }\\n         var word = 0\\n         while word < f.params.len() {\\n             try check_type(&p, f.params[word])\\n+            if builder_type(f.params[word]) { return problem(\\\"StringBuilder by-value parameters are not supported\\\", 0, 0) }\\n             word += 1\\n         }\\n         word = 0\\n@@ -106,6 +108,29 @@ func range_matches(f: &FuncIR, start: Int, layout: &Vector<ValueType>) -> Bool {\\n     true\\n }\\n \\n+func builder_reference(p: &Program, t: ValueType, mutable: Bool) -> Bool {\\n+    if not reference_type(t) { return false }\\n+    let target = try reference_target(&p.references, t) catch { return false }\\n+    builder_type(target) && (not mutable || reference_mutable(&p.references, t))\\n+}\\n+\\n+func check_builder_append(p: &Program, f: &FuncIR, i: &Instruction,\\n+                          input: ValueType) -> Result<Void, ProtoError> {\\n+    if not builder_reference(&p, slot_type(&f, i.a), true) || slot_type(&f, i.b) != input {\\n+        return invalid(&i)\\n+    }\\n+    if i.target != -1 || i.args.len() != 0 || p.alloc_error_record < 0 {\\n+        return invalid(&i)\\n+    }\\n+    if i.immediate < 0 || i.immediate >= p.sums.len() { return invalid(&i) }\\n+    let sum = p.sums[i.immediate].copy()\\n+    if (sum.kind != SumKind.Result || sum.payload != ValueType.Void ||\\n+        sum.error != ValueType.Record(p.alloc_error_record)) { return invalid(&i) }\\n+    let layout: Vector<ValueType> = [ValueType.Bool, ValueType.I64, ValueType.I64]\\n+    if not range_matches(&f, i.dst, &layout) { return invalid(&i) }\\n+    return\\n+}\\n+\\n func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void, ProtoError> {\\n     let dst = slot_type(&f, i.dst)\\n     let a = slot_type(&f, i.a)\\n@@ -144,8 +169,20 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n             let layout: Vector<ValueType> = [ValueType.Bool, ValueType.U64]\\n             if not string_type(a) || b != ValueType.I64 || not range_matches(&f, i.dst, &layout) { return invalid(&i) }\\n         },\\n+        case BuilderNew -> {\\n+            if (not builder_type(dst) || i.a != -1 || i.b != -1 || i.immediate != 0 ||\\n+                i.target != -1 || i.args.len() != 0) { return invalid(&i) }\\n+        },\\n+        case BuilderBuild -> {\\n+            if (not string_type(dst) || not builder_reference(&p, a, false) || i.b != -1 ||\\n+                i.immediate != 0 || i.target != -1 || i.args.len() != 0) { return invalid(&i) }\\n+        },\\n+        case BuilderAppendString -> { try check_builder_append(&p, &f, &i, ValueType.String) },\\n+        case BuilderAppendByte -> { try check_builder_append(&p, &f, &i, ValueType.Byte) },\\n+        case BuilderAppendInt -> { try check_builder_append(&p, &f, &i, ValueType.I64) },\\n+        case BuilderAppendScalar -> { try check_builder_append(&p, &f, &i, ValueType.I64) },\\n         case Drop -> {\\n-            if not string_type(a) { return invalid(&i) }\\n+            if not owning_type(a) { return invalid(&i) }\\n         },\\n         case Addr -> {\\n             if not reference_type(dst) { return invalid(&i) }\\n@@ -156,12 +193,14 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n         case LoadRef -> {\\n             if not reference_type(a) { return invalid(&i) }\\n             let target = try reference_target(&p.references, a)\\n+            if builder_type(target) { return invalid(&i) }\\n             let layout = try value_layout(&p.records, &p.sums, target)\\n             if not range_matches(&f, i.dst, &layout) { return invalid(&i) }\\n         },\\n         case StoreRef -> {\\n             if not reference_mutable(&p.references, a) { return invalid(&i) }\\n             let target = try reference_target(&p.references, a)\\n+            if builder_type(target) { return invalid(&i) }\\n             let layout = try value_layout(&p.records, &p.sums, target)\\n             if not range_matches(&f, i.b, &layout) { return invalid(&i) }\\n         },\\n@@ -262,7 +301,7 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n     }\\n     var n = 0\\n     while n < f.slots.len() {\\n-        if not scalar_type(f.slots[n]) && not reference_type(f.slots[n]) && not string_type(f.slots[n]) { return problem(\\\"internal prototype error: non-scalar slot\\\", 0, 0) }\\n+        if not scalar_type(f.slots[n]) && not reference_type(f.slots[n]) && not owning_type(f.slots[n]) { return problem(\\\"internal prototype error: non-scalar slot\\\", 0, 0) }\\n         if n < params.len() && f.slots[n] != params[n] {\\n             return problem(\\\"internal prototype error: parameter slot mismatch\\\", 0, 0)\\n         }\\n@@ -293,6 +332,20 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n }\\n \\n public func verify(program: &Program) -> Result<Void, ProtoError> {\\n+    if program.builder_fail_after < -1 { return problem(\\\"invalid builder failure counter\\\", 0, 0) }\\n+    if program.alloc_error_record < -1 || program.alloc_error_record >= program.records.len() {\\n+        return problem(\\\"invalid AllocError builtin identity\\\", 0, 0)\\n+    }\\n+    if program.alloc_error_record >= 0 {\\n+        let record = program.records[program.alloc_error_record].copy()\\n+        if record.name != \\\"AllocError\\\" || record.field_names.len() != 2 || record.field_types.len() != 2 {\\n+            return problem(\\\"invalid AllocError builtin declaration\\\", 0, 0)\\n+        }\\n+        if (record.field_names[0] != \\\"size\\\" || record.field_names[1] != \\\"align\\\" ||\\n+            record.field_types[0] != ValueType.I64 || record.field_types[1] != ValueType.I64) {\\n+            return problem(\\\"invalid AllocError builtin declaration\\\", 0, 0)\\n+        }\\n+    }\\n     try validate_enums(&program.enums)\\n     try validate_references(&program.references)\\n     try check_declared_types(&program)\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 4132d1b1..23c6249e 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -47,6 +47,108 @@ func string_allocate(heap: &var Vector<StringEntry>, value: String) -> Result<In\\n     heap.len()\\n }\\n \\n+func builder_entry(heap: &Vector<StringEntry>, handle: Int) -> Result<Int, ProtoError> {\\n+    if handle <= 0 { return runtime_error(\\\"runtime error: invalid builder handle\\\") }\\n+    let index = handle - 1\\n+    if index >= heap.len() || heap[index].refs <= 0 {\\n+        return runtime_error(\\\"runtime error: invalid builder handle\\\")\\n+    }\\n+    index\\n+}\\n+\\n+func builder_result(cells: &var Vector<Int>, dst: Int, success: Bool, size: Int) {\\n+    cells[dst] = if success { 1 } else { 0 }\\n+    cells[dst + 1] = if success { 0 } else { size }\\n+    cells[dst + 2] = if success { 0 } else { 8 }\\n+}\\n+\\n+func decimal_length(value: Int) -> Int {\\n+    if value == 0 { return 1 }\\n+    var magnitude = UInt.from(truncating: value)\\n+    var length = 0\\n+    if value < 0 {\\n+        magnitude = (~magnitude) &+ 1\\n+        length = 1\\n+    }\\n+    while magnitude != 0 {\\n+        magnitude = magnitude / 10\\n+        length += 1\\n+    }\\n+    length\\n+}\\n+\\n+func builder_append(op: Op, cells: &var Vector<Int>, base: Int, dst: Int,\\n+                    builder_ref: Int, operand: Int,\\n+                    heap: &var Vector<StringEntry>, fail_after: &var Int) -> Result<Void, ProtoError> {\\n+    let address = cells[base + builder_ref]\\n+    if not valid_reference(&cells, address, 1) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+    let old_index = try builder_entry(&heap, cells[address])\\n+    let old_value = heap[old_index].value\\n+    var suffix_length = 0\\n+    var string_operand = \\\"\\\"\\n+    if op == Op.BuilderAppendString {\\n+        let source_index = try string_entry(&heap, cells[base + operand])\\n+        string_operand = heap[source_index].value\\n+        suffix_length = string_operand.len()\\n+        if suffix_length == 0 {\\n+            builder_result(&var cells, base + dst, true, 0)\\n+            return\\n+        }\\n+    } else if op == Op.BuilderAppendByte {\\n+        suffix_length = 1\\n+    } else if op == Op.BuilderAppendScalar {\\n+        let scalar = cells[base + operand]\\n+        if scalar < 0 || scalar > 0x10FFFF || (scalar >= 0xD800 && scalar <= 0xDFFF) {\\n+            return runtime_error(\\\"runtime error: invalid scalar\\\")\\n+        }\\n+        if scalar <= 0x7F { suffix_length = 1 }\\n+        else if scalar <= 0x7FF { suffix_length = 2 }\\n+        else if scalar <= 0xFFFF { suffix_length = 3 }\\n+        else { suffix_length = 4 }\\n+    } else {\\n+        suffix_length = decimal_length(cells[base + operand])\\n+    }\\n+    let maximum_content = Int.max - 16\\n+    if suffix_length > maximum_content || old_value.len() > maximum_content - suffix_length {\\n+        return runtime_error(\\\"runtime error: builder length overflow\\\")\\n+    }\\n+    let requested = old_value.len() + suffix_length + 16\\n+    if fail_after == 0 {\\n+        fail_after = -1\\n+        builder_result(&var cells, base + dst, false, requested)\\n+        return\\n+    }\\n+    var replacement = StringBuilder()\\n+    match replacement.append(old_value) {\\n+        case Err(_) -> {\\n+            builder_result(&var cells, base + dst, false, requested)\\n+            return\\n+        },\\n+        case Ok(_) -> {}\\n+    }\\n+    let appended = if op == Op.BuilderAppendByte {\\n+        replacement.append(Byte(UInt8.from(truncating: cells[base + operand])))\\n+    } else if op == Op.BuilderAppendScalar {\\n+        replacement.append(try! Scalar(value: cells[base + operand]))\\n+    } else if op == Op.BuilderAppendInt {\\n+        replacement.append(cells[base + operand])\\n+    } else {\\n+        replacement.append(string_operand)\\n+    }\\n+    match appended {\\n+        case Err(_) -> {\\n+            builder_result(&var cells, base + dst, false, requested)\\n+            return\\n+        },\\n+        case Ok(_) -> {}\\n+    }\\n+    let handle = try string_allocate(&var heap, replacement.build())\\n+    try string_assign(&var cells, address, handle, &var heap, false)\\n+    if fail_after > 0 { fail_after -= 1 }\\n+    builder_result(&var cells, base + dst, true, 0)\\n+    return\\n+}\\n+\\n func string_assign(cells: &var Vector<Int>, dst: Int, source: Int,\\n                    heap: &var Vector<StringEntry>, retain: Bool) -> Result<Void, ProtoError> {\\n     if retain { try string_retain(&var heap, source) }\\n@@ -141,7 +243,7 @@ func valid_reference(cells: &Vector<Int>, address: Int, width: Int) -> Bool {\\n \\n func run_function(program: &Program, function_index: Int, arguments: &Vector<Int>,\\n                   cells: &var Vector<Int>, heap: &var Vector<StringEntry>, remaining: &var Int,\\n-                  depth: Int) -> Result<CallResult, ProtoError> {\\n+                  fail_after: &var Int, depth: Int) -> Result<CallResult, ProtoError> {\\n     if depth > MAX_CALL_DEPTH { return runtime_error(\\\"runtime error: call depth exceeded\\\") }\\n     let function = program.functions[function_index].copy()\\n     let base = cells.len()\\n@@ -294,7 +396,7 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                     try! args.push(cells[base + inst.args[i]])\\n                     i = i + 1\\n                 }\\n-                let result = try run_function(&program, inst.target, &args, &var cells, &var heap, &var remaining, depth + 1)\\n+                let result = try run_function(&program, inst.target, &args, &var cells, &var heap, &var remaining, &var fail_after, depth + 1)\\n                 var result_index = 0\\n                 while result_index < result.values.len() {\\n                     if string_type(function.slots[inst.dst + result_index]) {\\n@@ -342,7 +444,32 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 cells[base + inst.dst] = 1\\n                 cells[base + inst.dst + 1] = Int.from(truncating: value)\\n             },\\n+            case BuilderNew -> {\\n+                let handle = try string_allocate(&var heap, \\\"\\\")\\n+                try string_assign(&var cells, base + inst.dst, handle, &var heap, false)\\n+            },\\n+            case BuilderBuild -> {\\n+                let address = cells[base + inst.a]\\n+                if not valid_reference(&cells, address, 1) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                let _ = try builder_entry(&heap, cells[address])\\n+                try string_assign(&var cells, base + inst.dst, cells[address], &var heap, true)\\n+            },\\n+            case BuilderAppendString -> {\\n+                try builder_append(inst.op, &var cells, base, inst.dst, inst.a, inst.b, &var heap, &var fail_after)\\n+            },\\n+            case BuilderAppendByte -> {\\n+                try builder_append(inst.op, &var cells, base, inst.dst, inst.a, inst.b, &var heap, &var fail_after)\\n+            },\\n+            case BuilderAppendInt -> {\\n+                try builder_append(inst.op, &var cells, base, inst.dst, inst.a, inst.b, &var heap, &var fail_after)\\n+            },\\n+            case BuilderAppendScalar -> {\\n+                try builder_append(inst.op, &var cells, base, inst.dst, inst.a, inst.b, &var heap, &var fail_after)\\n+            },\\n             case Drop -> {\\n+                if builder_type(function.slots[inst.a]) && cells[base + inst.a] != 0 {\\n+                    let _ = try builder_entry(&heap, cells[base + inst.a])\\n+                }\\n                 try string_release(&var heap, cells[base + inst.a])\\n                 cells[base + inst.a] = 0\\n             },\\n@@ -358,7 +485,7 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 }\\n                 var slot = 0\\n                 while slot < function.slots.len() {\\n-                    if string_type(function.slots[slot]) {\\n+                    if owning_type(function.slots[slot]) {\\n                         try string_release(&var heap, cells[base + slot])\\n                         cells[base + slot] = 0\\n                     }\\n@@ -375,10 +502,11 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n \\n public func execute(program: &Program, budget: Int) -> Result<Void, ProtoError> {\\n     var remaining = budget\\n+    var fail_after = program.builder_fail_after\\n     let arguments = Vector<Int>()\\n     var cells = Vector<Int>()\\n     var heap = Vector<StringEntry>()\\n-    let _ = try run_function(&program, program.main_index, &arguments, &var cells, &var heap, &var remaining, 1)\\n+    let _ = try run_function(&program, program.main_index, &arguments, &var cells, &var heap, &var remaining, &var fail_after, 1)\\n     var index = 0\\n     while index < heap.len() {\\n         if heap[index].refs != 0 { return runtime_error(\\\"runtime error: live string allocation\\\") }\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex a54f3da5..50682cea 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -39,6 +39,13 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"builders/snapshots\\\", \\\"\\\\na\\\\nab\\\\na\\\\nab\\\\nother\\\\n\\\", section=\\\"builders\\\", compare_sawc=True),\\n+    RunCase(\\\"builders/append_values\\\", \\\"120\\\\n0\\\\n255\\\\n45\\\\n57\\\\n50\\\\n50\\\\n51\\\\n51\\\\n55\\\\n50\\\\n48\\\\n51\\\\n54\\\\n56\\\\n53\\\\n52\\\\n55\\\\n55\\\\n53\\\\n56\\\\n48\\\\n56\\\\n48\\\\n127\\\\n194\\\\n128\\\\n223\\\\n191\\\\n224\\\\n160\\\\n128\\\\n237\\\\n159\\\\n191\\\\n238\\\\n128\\\\n128\\\\n239\\\\n191\\\\n191\\\\n244\\\\n143\\\\n191\\\\n191\\\\n\\\", section=\\\"builders\\\", compare_sawc=True),\\n+    RunCase(\\\"builders/references\\\", \\\"start-mut\\\\nstart-mut-shared\\\\n\\\", section=\\\"builders\\\", compare_sawc=True),\\n+    RunCase(\\\"builders/single_evaluation\\\", \\\"arg\\\\none\\\\n\\\", section=\\\"builders\\\", compare_sawc=True),\\n+    RunCase(\\\"builders/loop_return_cleanup\\\", \\\"01234\\\\nearly\\\\n\\\", section=\\\"builders\\\", compare_sawc=True),\\n+    RunCase(\\\"builders/result_alloc_error\\\", \\\"ok\\\\n3\\\\n8\\\\n\\\", section=\\\"builders\\\"),\\n+    RunCase(\\\"builders/lexer_helpers\\\", \\\"65\\\\n255\\\\nA\\\" + \\\"\\\\\\\\\\\" * 4 + r\\\"\\\\n\\\\t\\\\r\\\\0\\\\x01\\\\x7f\\\" + \\\"é\\\\n\\\", section=\\\"builders\\\", compare_sawc=True),\\n     RunCase(\\\"scalars/boundaries\\\", \\\"0\\\\n127\\\\n128\\\\n2047\\\\n2048\\\\n55295\\\\n57344\\\\n65535\\\\n65536\\\\n1114111\\\\n\\\", section=\\\"scalars\\\", compare_sawc=True),\\n     RunCase(\\\"scalars/invalid\\\", \\\"invalid\\\\n\\\" * 6, section=\\\"scalars\\\", compare_sawc=True),\\n     RunCase(\\\"scalars/composition\\\", \\\"1\\\\n1\\\\n1114111\\\\n1\\\\n1114111\\\\nerror\\\\n\\\", section=\\\"scalars\\\", compare_sawc=True),\\n@@ -232,6 +239,24 @@ RUN_CASES = (\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"builders/reject_copy\\\", \\\"StringBuilder values cannot be copied\\\", \\\"builders\\\", True),\\n+    RejectCase(\\\"builders/reject_assignment\\\", \\\"StringBuilder values cannot be assigned\\\", \\\"builders\\\"),\\n+    RejectCase(\\\"builders/reject_value_parameter\\\", \\\"StringBuilder parameters must be references\\\", \\\"builders\\\"),\\n+    RejectCase(\\\"builders/reject_value_result\\\", \\\"StringBuilder cannot be returned by value\\\", \\\"builders\\\"),\\n+    RejectCase(\\\"builders/reject_record_field\\\", \\\"StringBuilder cannot be stored in a record\\\", \\\"builders\\\"),\\n+    RejectCase(\\\"builders/reject_optional\\\", \\\"StringBuilder cannot be stored in Optional\\\", \\\"builders\\\"),\\n+    RejectCase(\\\"builders/reject_result\\\", \\\"StringBuilder cannot be stored in Result\\\", \\\"builders\\\"),\\n+    RejectCase(\\\"builders/reject_static\\\", \\\"static type must be an integer or Bool\\\", \\\"builders\\\", True),\\n+    RejectCase(\\\"builders/reject_extension\\\", \\\"cannot be extended\\\", \\\"builders\\\"),\\n+    RejectCase(\\\"builders/reject_constructor_arity\\\", \\\"constructor accepts no arguments\\\", \\\"builders\\\", True),\\n+    RejectCase(\\\"builders/reject_temporary_receiver\\\", \\\"method calls require a named place receiver\\\", \\\"builders\\\"),\\n+    RejectCase(\\\"builders/reject_shared_append\\\", \\\"cannot call a mutating method on an immutable receiver\\\", \\\"builders\\\", True),\\n+    RejectCase(\\\"builders/reject_append_label\\\", \\\"argument label\\\", \\\"builders\\\", True),\\n+    RejectCase(\\\"builders/reject_append_type\\\", \\\"argument must be String, Byte, Int, or Scalar\\\", \\\"builders\\\", True),\\n+    RejectCase(\\\"builders/reject_append_arity\\\", \\\"requires exactly one argument\\\", \\\"builders\\\", True),\\n+    RejectCase(\\\"builders/reject_nested_receiver\\\", \\\"overlapping arguments cannot include a mutable borrow\\\", \\\"builders\\\"),\\n+    RejectCase(\\\"builders/reject_builtin_alloc_error\\\", \\\"reserved\\\", \\\"builders\\\", True),\\n+    RejectCase(\\\"builders/reject_value_control\\\", \\\"StringBuilder values cannot be copied\\\", \\\"builders\\\"),\\n     RejectCase(\\\"scalars/reject_raw_scalar\\\", \\\"argument label must be `value`\\\", \\\"scalars\\\"),\\n     RejectCase(\\\"scalars/reject_raw_invalid\\\", \\\"cannot be constructed directly\\\", \\\"scalars\\\"),\\n     RejectCase(\\\"scalars/reject_field_read\\\", \\\"fields are private\\\", \\\"scalars\\\"),\\n@@ -542,7 +567,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--sawc-python\\\", default=sys.executable,\\n                         help=\\\"Python interpreter with sawc dependencies (default: this interpreter)\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\", \\\"results\\\", \\\"unsigned_parse\\\", \\\"scalars\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\", \\\"owning_records\\\", \\\"optionals\\\", \\\"results\\\", \\\"unsigned_parse\\\", \\\"scalars\\\", \\\"builders\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\ndiff --git a/prototypes/minivm/tests/builder_contract.saw b/prototypes/minivm/tests/builder_contract.saw\\nnew file mode 100644\\nindex 00000000..fbdac86b\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/builder_contract.saw\\n@@ -0,0 +1,146 @@\\n+import src.model.*\\n+import src.verify.*\\n+\\n+func op(kind: Op, dst: Int, a: Int, b: Int, immediate: Int) -> Instruction {\\n+    var result = instruction(kind)\\n+    result.dst = dst\\n+    result.a = a\\n+    result.b = b\\n+    result.immediate = immediate\\n+    move result\\n+}\\n+\\n+func valid(p: &Program) -> Bool {\\n+    try verify(&p) catch { return false }\\n+    true\\n+}\\n+\\n+func program() -> Program {\\n+    var records = Vector<RecordIR>()\\n+    let names: Vector<String> = [\\\"size\\\", \\\"align\\\"]\\n+    let fields: Vector<ValueType> = [ValueType.I64, ValueType.I64]\\n+    try! records.push(RecordIR(name: \\\"AllocError\\\", field_names: names.copy(),\\n+        field_types: fields.copy(), line: 1, col: 1))\\n+    try! records.push(RecordIR(name: \\\"Other\\\", field_names: names.copy(),\\n+        field_types: fields.copy(), line: 1, col: 1))\\n+    var sums = Vector<SumIR>()\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Void, error: ValueType.Record(0)))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Void, error: ValueType.Record(1)))\\n+    try! sums.push(SumIR(kind: SumKind.Optional, payload: ValueType.Record(0), error: ValueType.Void))\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.I64, error: ValueType.Record(0)))\\n+    var refs = Vector<ReferenceIR>()\\n+    try! refs.push(ReferenceIR(target: ValueType.Builder, mutable: true))\\n+    try! refs.push(ReferenceIR(target: ValueType.Builder, mutable: false))\\n+    try! refs.push(ReferenceIR(target: ValueType.I64, mutable: true))\\n+    let slots: Vector<ValueType> = [ValueType.Builder, ValueType.Ref(0), ValueType.Ref(1),\\n+        ValueType.String, ValueType.I64, ValueType.Byte, ValueType.Bool, ValueType.I64,\\n+        ValueType.I64, ValueType.Ref(2)]\\n+    var code = Vector<Instruction>()\\n+    try! code.push(op(Op.BuilderNew, 0, -1, -1, 0))\\n+    try! code.push(op(Op.Addr, 1, 0, -1, 0))\\n+    try! code.push(op(Op.Addr, 2, 0, -1, 0))\\n+    try! code.push(op(Op.StringConst, 3, -1, -1, 0))\\n+    try! code.push(op(Op.Const, 4, -1, -1, 65))\\n+    try! code.push(op(Op.Const, 5, -1, -1, 65))\\n+    try! code.push(op(Op.BuilderAppendString, 6, 1, 3, 0))\\n+    try! code.push(op(Op.BuilderAppendByte, 6, 1, 5, 0))\\n+    try! code.push(op(Op.BuilderAppendInt, 6, 1, 4, 0))\\n+    try! code.push(op(Op.BuilderAppendScalar, 6, 1, 4, 0))\\n+    try! code.push(op(Op.BuilderBuild, 3, 2, -1, 0))\\n+    try! code.push(op(Op.Drop, -1, 0, -1, 0))\\n+    try! code.push(op(Op.Drop, -1, 3, -1, 0))\\n+    try! code.push(instruction(Op.Return))\\n+    var functions = Vector<FuncIR>()\\n+    try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: move slots, code: move code))\\n+    Program(strings: [\\\"ab\\\"], records: move records, references: move refs, sums: move sums,\\n+        enums: Vector<EnumIR>(), functions: move functions, main_index: 0,\\n+        alloc_error_record: 0, builder_fail_after: -1)\\n+}\\n+\\n+func main() {\\n+    let p = program()\\n+    assert(valid(&p), \\\"all six Builder operations and shared build reference\\\")\\n+    var changed = p.copy()\\n+    changed.functions[0].code[0].dst = 3\\n+    assert(not valid(&changed), \\\"New requires Builder destination\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[0].a = 0\\n+    assert(not valid(&changed), \\\"New rejects extraneous operand\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[10].a = 1\\n+    assert(valid(&changed), \\\"build permits mutable reference\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[10].a = 9\\n+    assert(not valid(&changed), \\\"build rejects reference to wrong nominal target\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[10].dst = 0\\n+    assert(not valid(&changed), \\\"build yields String, not Builder\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].a = 2\\n+    assert(not valid(&changed), \\\"append rejects shared reference\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].immediate = 1\\n+    assert(not valid(&changed), \\\"same-layout error record is not AllocError\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].immediate = 2\\n+    assert(not valid(&changed), \\\"append result is not Optional\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].immediate = 3\\n+    assert(not valid(&changed), \\\"append success must be Void\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].immediate = Int.max\\n+    assert(not valid(&changed), \\\"append descriptor index is checked\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].dst = Int.max\\n+    assert(not valid(&changed), \\\"append destination range arithmetic cannot overflow\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[6].dst = 8\\n+    assert(not valid(&changed), \\\"append requires full result range\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[7].b = 4\\n+    assert(not valid(&changed), \\\"Byte append rejects Int slot\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[8].b = 5\\n+    assert(not valid(&changed), \\\"Int append rejects Byte slot\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[9].b = 3\\n+    assert(not valid(&changed), \\\"Scalar append rejects String slot\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[0] = op(Op.Copy, 0, 0, -1, 0)\\n+    assert(not valid(&changed), \\\"Builder cannot be copied\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[0] = op(Op.LoadRef, 0, 1, -1, 0)\\n+    assert(not valid(&changed), \\\"Builder cannot be loaded by value\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].code[0] = op(Op.StoreRef, -1, 1, 0, 0)\\n+    assert(not valid(&changed), \\\"Builder cannot be stored by value\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].params = [ValueType.Builder]\\n+    assert(not valid(&changed), \\\"Builder by-value parameter forbidden\\\")\\n+    changed = p.copy()\\n+    changed.functions[0].result = ValueType.Builder\\n+    assert(not valid(&changed), \\\"Builder return forbidden\\\")\\n+    changed = p.copy()\\n+    changed.records[1].field_types[0] = ValueType.Builder\\n+    assert(not valid(&changed), \\\"Builder record field forbidden\\\")\\n+    changed = p.copy()\\n+    changed.sums[2].payload = ValueType.Builder\\n+    assert(not valid(&changed), \\\"Builder Optional payload forbidden\\\")\\n+    changed = p.copy()\\n+    changed.sums[3].payload = ValueType.Builder\\n+    assert(not valid(&changed), \\\"Builder Result payload forbidden\\\")\\n+    changed = p.copy()\\n+    changed.alloc_error_record = -1\\n+    assert(not valid(&changed), \\\"append needs explicit builtin identity\\\")\\n+    changed = p.copy()\\n+    changed.alloc_error_record = Int.max\\n+    assert(not valid(&changed), \\\"builtin index checked\\\")\\n+    changed = p.copy()\\n+    changed.records[0].field_types[0] = ValueType.Bool\\n+    assert(not valid(&changed), \\\"builtin field layout checked\\\")\\n+    changed = p.copy()\\n+    changed.builder_fail_after = -2\\n+    assert(not valid(&changed), \\\"failure injection configuration checked\\\")\\n+    print(\\\"builder verifier contract passed\\\")\\n+}\\ndiff --git a/prototypes/minivm/tests/builder_source_contract.saw b/prototypes/minivm/tests/builder_source_contract.saw\\nnew file mode 100644\\nindex 00000000..a69014bb\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/builder_source_contract.saw\\n@@ -0,0 +1,24 @@\\n+import src.model.*\\n+import src.frontend.*\\n+import src.verify.*\\n+import src.vm.*\\n+import src.llvm\\n+import std.env.{Env}\\n+\\n+func contract_program() -> Program {\\n+    let source = \\\"func make(text: String) -> Result<String, AllocError> \\\\{\\\\n    if false \\\\{\\\\n        var dormant = StringBuilder()\\\\n        try dormant.append(\\\\\\\"never\\\\\\\")\\\\n    \\\\}\\\\n    var builder = StringBuilder()\\\\n    try builder.append(text)\\\\n    builder.build()\\\\n\\\\}\\\\n\\\\nfunc main() \\\\{\\\\n    match make(\\\\\\\"first\\\\\\\") \\\\{\\\\n        case Ok(_) -> \\\\{ print(\\\\\\\"unexpected success\\\\\\\") \\\\},\\\\n        case Err(error) -> \\\\{\\\\n            print(error.size)\\\\n            print(error.align)\\\\n        \\\\}\\\\n    \\\\}\\\\n    match make(\\\\\\\"ok\\\\\\\") \\\\{\\\\n        case Ok(value) -> \\\\{ print(value) \\\\},\\\\n        case Err(_) -> \\\\{ print(\\\\\\\"unexpected failure\\\\\\\") \\\\}\\\\n    \\\\}\\\\n\\\\}\\\\n\\\"\\n+    var program = try! compile_source(source)\\n+    program.builder_fail_after = 0\\n+    try! verify(&program)\\n+    move program\\n+}\\n+\\n+func main() {\\n+    let program = contract_program()\\n+    let args = try! Env.args()\\n+    if args.len() == 2 && args[1] == \\\"emit-llvm\\\" {\\n+        print(try! llvm.emit_llvm(&program))\\n+        return\\n+    }\\n+    try! execute(&program, 1000)\\n+}\\ndiff --git a/prototypes/minivm/tests/builder_vm_contract.saw b/prototypes/minivm/tests/builder_vm_contract.saw\\nnew file mode 100644\\nindex 00000000..8746bc1c\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/builder_vm_contract.saw\\n@@ -0,0 +1,197 @@\\n+import src.model.*\\n+import src.verify.*\\n+import src.vm.*\\n+import src.llvm\\n+import std.env.{Env}\\n+\\n+func emit(code: &var Vector<Instruction>, kind: Op, dst: Int, a: Int, b: Int, immediate: Int) {\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    i.immediate = immediate\\n+    try! code.push(move i)\\n+}\\n+\\n+func check_word(code: &var Vector<Instruction>, slots: &var Vector<ValueType>, actual: Int,\\n+                expected: Int, t: ValueType) {\\n+    let expected_slot = slots.len()\\n+    try! slots.push(t)\\n+    emit(&var code, Op.Const, expected_slot, -1, -1, expected)\\n+    let equal_slot = slots.len()\\n+    try! slots.push(ValueType.Bool)\\n+    emit(&var code, Op.Eq, equal_slot, actual, expected_slot, 0)\\n+    var branch = instruction(Op.Branch)\\n+    branch.a = equal_slot\\n+    branch.target = code.len() + 2\\n+    try! code.push(move branch)\\n+    try! code.push(instruction(Op.Panic))\\n+}\\n+\\n+func check_string(code: &var Vector<Instruction>, slots: &var Vector<ValueType>,\\n+                  strings: &var Vector<String>, actual: Int, expected: String) {\\n+    let pool_index = strings.len()\\n+    try! strings.push(expected)\\n+    let expected_slot = slots.len()\\n+    try! slots.push(ValueType.String)\\n+    emit(&var code, Op.StringConst, expected_slot, -1, -1, pool_index)\\n+    let equal_slot = slots.len()\\n+    try! slots.push(ValueType.Bool)\\n+    emit(&var code, Op.Eq, equal_slot, actual, expected_slot, 0)\\n+    var branch = instruction(Op.Branch)\\n+    branch.a = equal_slot\\n+    branch.target = code.len() + 2\\n+    try! code.push(move branch)\\n+    try! code.push(instruction(Op.Panic))\\n+}\\n+\\n+func contract_program() -> Program {\\n+    var strings = Vector<String>()\\n+    try! strings.push(\\\"a\\\")\\n+    try! strings.push(\\\"b\\\")\\n+    try! strings.push(\\\"c\\\")\\n+    var refs = Vector<ReferenceIR>()\\n+    try! refs.push(ReferenceIR(target: ValueType.Builder, mutable: true))\\n+    let fields: Vector<String> = [\\\"size\\\", \\\"align\\\"]\\n+    let field_types: Vector<ValueType> = [ValueType.I64, ValueType.I64]\\n+    var records = Vector<RecordIR>()\\n+    try! records.push(RecordIR(name: \\\"AllocError\\\", field_names: move fields,\\n+        field_types: move field_types, line: 0, col: 0))\\n+    var sums = Vector<SumIR>()\\n+    try! sums.push(SumIR(kind: SumKind.Result, payload: ValueType.Void,\\n+        error: ValueType.Record(0)))\\n+\\n+    var slots: Vector<ValueType> = [ValueType.Builder, ValueType.Ref(0),\\n+        ValueType.String, ValueType.Bool, ValueType.I64, ValueType.I64,\\n+        ValueType.String, ValueType.String, ValueType.Bool, ValueType.I64, ValueType.I64,\\n+        ValueType.String, ValueType.String, ValueType.Bool, ValueType.I64, ValueType.I64,\\n+        ValueType.String, ValueType.Builder]\\n+    var code = Vector<Instruction>()\\n+    emit(&var code, Op.BuilderNew, 0, -1, -1, 0)\\n+    emit(&var code, Op.Addr, 1, 0, -1, 0)\\n+    emit(&var code, Op.StringConst, 2, -1, -1, 0)\\n+    emit(&var code, Op.BuilderAppendString, 3, 1, 2, 0)\\n+    check_word(&var code, &var slots, 3, 1, ValueType.Bool)\\n+    emit(&var code, Op.BuilderBuild, 6, 1, -1, 0)\\n+    check_string(&var code, &var slots, &var strings, 6, \\\"a\\\")\\n+\\n+    let empty_pool = strings.len()\\n+    try! strings.push(\\\"\\\")\\n+    let empty_slot = slots.len()\\n+    try! slots.push(ValueType.String)\\n+    emit(&var code, Op.StringConst, empty_slot, -1, -1, empty_pool)\\n+    let empty_result = slots.len()\\n+    try! slots.push(ValueType.Bool)\\n+    try! slots.push(ValueType.I64)\\n+    try! slots.push(ValueType.I64)\\n+    emit(&var code, Op.BuilderAppendString, empty_result, 1, empty_slot, 0)\\n+    check_word(&var code, &var slots, empty_result, 1, ValueType.Bool)\\n+\\n+    emit(&var code, Op.StringConst, 7, -1, -1, 1)\\n+    emit(&var code, Op.BuilderAppendString, 8, 1, 7, 0)\\n+    check_word(&var code, &var slots, 8, 0, ValueType.Bool)\\n+    check_word(&var code, &var slots, 9, 18, ValueType.I64)\\n+    check_word(&var code, &var slots, 10, 8, ValueType.I64)\\n+    emit(&var code, Op.BuilderBuild, 11, 1, -1, 0)\\n+    check_string(&var code, &var slots, &var strings, 11, \\\"a\\\")\\n+\\n+    emit(&var code, Op.StringConst, 12, -1, -1, 2)\\n+    emit(&var code, Op.BuilderAppendString, 13, 1, 12, 0)\\n+    check_word(&var code, &var slots, 13, 1, ValueType.Bool)\\n+    check_word(&var code, &var slots, 14, 0, ValueType.I64)\\n+    check_word(&var code, &var slots, 15, 0, ValueType.I64)\\n+    emit(&var code, Op.BuilderBuild, 16, 1, -1, 0)\\n+    check_string(&var code, &var slots, &var strings, 16, \\\"ac\\\")\\n+    check_string(&var code, &var slots, &var strings, 6, \\\"a\\\")\\n+\\n+    let byte_slot = slots.len()\\n+    try! slots.push(ValueType.Byte)\\n+    emit(&var code, Op.Const, byte_slot, -1, -1, 255)\\n+    let byte_result = slots.len()\\n+    try! slots.push(ValueType.Bool)\\n+    try! slots.push(ValueType.I64)\\n+    try! slots.push(ValueType.I64)\\n+    emit(&var code, Op.BuilderAppendByte, byte_result, 1, byte_slot, 0)\\n+    check_word(&var code, &var slots, byte_result, 1, ValueType.Bool)\\n+\\n+    let int_slot = slots.len()\\n+    try! slots.push(ValueType.I64)\\n+    emit(&var code, Op.Const, int_slot, -1, -1, Int.min)\\n+    let int_result = slots.len()\\n+    try! slots.push(ValueType.Bool)\\n+    try! slots.push(ValueType.I64)\\n+    try! slots.push(ValueType.I64)\\n+    emit(&var code, Op.BuilderAppendInt, int_result, 1, int_slot, 0)\\n+    check_word(&var code, &var slots, int_result, 1, ValueType.Bool)\\n+\\n+    let scalar_slot = slots.len()\\n+    try! slots.push(ValueType.I64)\\n+    emit(&var code, Op.Const, scalar_slot, -1, -1, 0x1F600)\\n+    let scalar_result = slots.len()\\n+    try! slots.push(ValueType.Bool)\\n+    try! slots.push(ValueType.I64)\\n+    try! slots.push(ValueType.I64)\\n+    emit(&var code, Op.BuilderAppendScalar, scalar_result, 1, scalar_slot, 0)\\n+    check_word(&var code, &var slots, scalar_result, 1, ValueType.Bool)\\n+\\n+    let final_slot = slots.len()\\n+    try! slots.push(ValueType.String)\\n+    emit(&var code, Op.BuilderBuild, final_slot, 1, -1, 0)\\n+    var expected = StringBuilder()\\n+    try! expected.append(\\\"ac\\\")\\n+    try! expected.append(Byte(255))\\n+    try! expected.append(Int.min)\\n+    try! expected.append(try! Scalar(value: 0x1F600))\\n+    check_string(&var code, &var slots, &var strings, final_slot, expected.build())\\n+\\n+    let forwarded_text = slots.len()\\n+    try! slots.push(ValueType.String)\\n+    let forwarded_pool = strings.len()\\n+    try! strings.push(\\\"z\\\")\\n+    emit(&var code, Op.StringConst, forwarded_text, -1, -1, forwarded_pool)\\n+    var call = instruction(Op.Call)\\n+    call.target = 1\\n+    try! call.args.push(1)\\n+    try! call.args.push(forwarded_text)\\n+    try! code.push(move call)\\n+    let forwarded_snapshot = slots.len()\\n+    try! slots.push(ValueType.String)\\n+    emit(&var code, Op.BuilderBuild, forwarded_snapshot, 1, -1, 0)\\n+    var forwarded_expected = StringBuilder()\\n+    try! forwarded_expected.append(expected.build())\\n+    try! forwarded_expected.append(\\\"z\\\")\\n+    check_string(&var code, &var slots, &var strings, forwarded_snapshot, forwarded_expected.build())\\n+    emit(&var code, Op.Drop, -1, 0, -1, 0)\\n+    emit(&var code, Op.Drop, -1, 0, -1, 0)\\n+    emit(&var code, Op.Drop, -1, 17, -1, 0)\\n+    try! code.push(instruction(Op.Return))\\n+\\n+    var functions = Vector<FuncIR>()\\n+    try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(),\\n+        result: ValueType.Void, slots: move slots, code: move code))\\n+    var append_slots: Vector<ValueType> = [ValueType.Ref(0), ValueType.String,\\n+        ValueType.Bool, ValueType.I64, ValueType.I64]\\n+    var append_code = Vector<Instruction>()\\n+    emit(&var append_code, Op.BuilderAppendString, 2, 0, 1, 0)\\n+    check_word(&var append_code, &var append_slots, 2, 1, ValueType.Bool)\\n+    try! append_code.push(instruction(Op.Return))\\n+    let append_params: Vector<ValueType> = [ValueType.Ref(0), ValueType.String]\\n+    try! functions.push(FuncIR(name: \\\"append_forwarded\\\", params: move append_params,\\n+        result: ValueType.Void, slots: move append_slots, code: move append_code))\\n+    let program = Program(strings: move strings, references: move refs, enums: Vector<EnumIR>(),\\n+        records: move records, sums: move sums, functions: move functions,\\n+        main_index: 0, alloc_error_record: 0, builder_fail_after: 1)\\n+    try! verify(&program)\\n+    move program\\n+}\\n+\\n+func main() {\\n+    let program = contract_program()\\n+    let args = try! Env.args()\\n+    if args.len() == 2 && args[1] == \\\"emit-llvm\\\" {\\n+        print(try! llvm.emit_llvm(&program))\\n+        return\\n+    }\\n+    try! execute(&program, 1000)\\n+    print(\\\"builder VM contract passed\\\")\\n+}\\ndiff --git a/prototypes/minivm/tests/enum_contract.saw b/prototypes/minivm/tests/enum_contract.saw\\nindex d600c049..df1be290 100644\\n--- a/prototypes/minivm/tests/enum_contract.saw\\n+++ b/prototypes/minivm/tests/enum_contract.saw\\n@@ -67,7 +67,8 @@ func base_program() -> Program {\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move identity_params,\\n         result: ValueType.Enum(0), slots: move identity_slots,\\n         code: move identity_code))\\n-    Program(strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: move enums, records: move records, sums: Vector<SumIR>(),\\n+    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+        strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: move enums, records: move records, sums: Vector<SumIR>(),\\n             functions: move functions, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/optional_contract.saw b/prototypes/minivm/tests/optional_contract.saw\\nindex a6c95482..0506c200 100644\\n--- a/prototypes/minivm/tests/optional_contract.saw\\n+++ b/prototypes/minivm/tests/optional_contract.saw\\n@@ -56,7 +56,8 @@ func base_program() -> Program {\\n     try! functions.push(FuncIR(name: \\\"echo\\\", params: move echo_params, result: ValueType.Optional(0),\\n         slots: move echo_slots, code: move echo_code))\\n     let strings: Vector<String> = [\\\"some\\\"]\\n-    Program(strings: move strings, references: move references, enums: Vector<EnumIR>(),\\n+    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+        strings: move strings, references: move references, enums: Vector<EnumIR>(),\\n         records: move records, sums: move sums, functions: move functions, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/owning_record_contract.saw b/prototypes/minivm/tests/owning_record_contract.saw\\nindex 1eeb9c50..79b1968c 100644\\n--- a/prototypes/minivm/tests/owning_record_contract.saw\\n+++ b/prototypes/minivm/tests/owning_record_contract.saw\\n@@ -68,7 +68,8 @@ func base_program() -> Program {\\n         slots: move slots, code: move code))\\n     try! funcs.push(FuncIR(name: \\\"echo\\\", params: move params, result: ValueType.Record(0),\\n         slots: move echo_slots, code: move echo_code))\\n-    Program(strings: move strings, references: move refs, enums: Vector<EnumIR>(),\\n+    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+        strings: move strings, references: move refs, enums: Vector<EnumIR>(),\\n             sums: Vector<SumIR>(),\\n         records: move records, functions: move funcs, main_index: 0)\\n }\\ndiff --git a/prototypes/minivm/tests/receiver_contract.saw b/prototypes/minivm/tests/receiver_contract.saw\\nindex f8a400d8..cdc35743 100644\\n--- a/prototypes/minivm/tests/receiver_contract.saw\\n+++ b/prototypes/minivm/tests/receiver_contract.saw\\n@@ -57,7 +57,8 @@ func base_program() -> Program {\\n     var funcs = Vector<FuncIR>()\\n     try! funcs.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n         slots: move slots, code: move code))\\n-    Program(strings: Vector<String>(), references: move refs, enums: Vector<EnumIR>(), records: move records, sums: Vector<SumIR>(),\\n+    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+        strings: Vector<String>(), references: move refs, enums: Vector<EnumIR>(), records: move records, sums: Vector<SumIR>(),\\n         functions: move funcs, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/record_contract.saw b/prototypes/minivm/tests/record_contract.saw\\nindex 78130a86..c8112fc1 100644\\n--- a/prototypes/minivm/tests/record_contract.saw\\n+++ b/prototypes/minivm/tests/record_contract.saw\\n@@ -47,7 +47,8 @@ func call_program(records: &Vector<RecordIR>) -> Program {\\n         slots: slots.copy(), code: move main_code))\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move params, result: ValueType.Record(0),\\n         slots: move slots, code: move callee_code))\\n-    Program(strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(), records: records.copy(), sums: Vector<SumIR>(), functions: move functions, main_index: 0)\\n+    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+        strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(), records: records.copy(), sums: Vector<SumIR>(), functions: move functions, main_index: 0)\\n }\\n \\n func main() {\\ndiff --git a/prototypes/minivm/tests/reference_contract.saw b/prototypes/minivm/tests/reference_contract.saw\\nindex 583233b4..c25f2d35 100644\\n--- a/prototypes/minivm/tests/reference_contract.saw\\n+++ b/prototypes/minivm/tests/reference_contract.saw\\n@@ -55,7 +55,8 @@ func base_program() -> Program {\\n         slots: move slots, code: move code))\\n     try! funcs.push(FuncIR(name: \\\"read\\\", params: move read_params, result: ValueType.I64,\\n         slots: move read_slots, code: move read_code))\\n-    Program(strings: Vector<String>(), references: move refs, enums: move enums, records: Vector<RecordIR>(), sums: Vector<SumIR>(),\\n+    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+        strings: Vector<String>(), references: move refs, enums: move enums, records: Vector<RecordIR>(), sums: Vector<SumIR>(),\\n         functions: move funcs, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/result_contract.saw b/prototypes/minivm/tests/result_contract.saw\\nindex ebd6739c..396d705f 100644\\n--- a/prototypes/minivm/tests/result_contract.saw\\n+++ b/prototypes/minivm/tests/result_contract.saw\\n@@ -92,7 +92,8 @@ func result_program() -> Program {\\n     try! functions.push(FuncIR(name: \\\"echo\\\", params: move echo_params, result: ValueType.Result(2),\\n         slots: move echo_slots, code: move echo_code))\\n     let strings: Vector<String> = [\\\"A\\\", \\\"B\\\", \\\"C\\\", \\\"payload\\\"]\\n-    Program(strings: move strings, references: move references, enums: Vector<EnumIR>(),\\n+    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+        strings: move strings, references: move references, enums: Vector<EnumIR>(),\\n         records: move records, sums: move sums, functions: move functions, main_index: 0)\\n }\\n \\n@@ -103,7 +104,8 @@ func panic_program() -> Program {\\n     var functions = Vector<FuncIR>()\\n     try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n         slots: Vector<ValueType>(), code: move code))\\n-    Program(strings: p.strings.copy(), references: p.references.copy(), enums: p.enums.copy(),\\n+    Program(alloc_error_record: p.alloc_error_record, builder_fail_after: p.builder_fail_after,\\n+        strings: p.strings.copy(), references: p.references.copy(), enums: p.enums.copy(),\\n         records: p.records.copy(), sums: p.sums.copy(), functions: move functions, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/string_contract.saw b/prototypes/minivm/tests/string_contract.saw\\nindex 98978584..64903103 100644\\n--- a/prototypes/minivm/tests/string_contract.saw\\n+++ b/prototypes/minivm/tests/string_contract.saw\\n@@ -52,7 +52,8 @@ func base_program() -> Program {\\n         slots: move slots, code: move code))\\n     try! funcs.push(FuncIR(name: \\\"echo\\\", params: move params, result: ValueType.String,\\n         slots: move echo_slots, code: move echo_code))\\n-    Program(strings: move strings, references: move refs, enums: Vector<EnumIR>(), sums: Vector<SumIR>(),\\n+    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+        strings: move strings, references: move refs, enums: Vector<EnumIR>(), sums: Vector<SumIR>(),\\n         records: Vector<RecordIR>(), functions: move funcs, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/uint_parse_contract.saw b/prototypes/minivm/tests/uint_parse_contract.saw\\nindex f4dd7b1c..121e9661 100644\\n--- a/prototypes/minivm/tests/uint_parse_contract.saw\\n+++ b/prototypes/minivm/tests/uint_parse_contract.saw\\n@@ -77,7 +77,8 @@ func direct_program() -> Program {\\n     var functions = Vector<FuncIR>()\\n     try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n         slots: move slots, code: move code))\\n-    Program(strings: move strings, references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(),\\n+    Program(alloc_error_record: -1, builder_fail_after: -1,\\n+        strings: move strings, references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(),\\n         records: Vector<RecordIR>(), sums: Vector<SumIR>(), functions: move functions, main_index: 0)\\n }\\n \\n\",\"id\":\"SL-257\",\"request_id\":\"codex-sl257-m14-implementation-20260914\",\"title\":\"minivm M14: local StringBuilder, snapshots and fallible append\"}","request_id":"codex-sl257-m14-implementation-20260914","sequence":779,"timestamp":"1789400001"}
---
