---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"1790341673","comments":[{"author":"agent:claude-sawlang","body":"USER RULING (Sep 23 2026, relayed by the lead): REFUSE THE AMBIGUITY, with a learning note. Under R5 (spelling never depends on whitespace) the parser never consults spacing, so the contract is token-shaped: in a CAST TARGET, a `??` token, or a second `?` token after the target's own `?`, is refused at that token with a teaching diagnostic that names both intended spellings — `(n as Int?) ?? 9` to coalesce the cast's result, `as Optional<Int?>` for a doubly-optional target. A single `?` stays the type's (`n as Int?`). Consequences, all whitespace-blind: `n as Int?? 9`, `n as Int ?? 9`, `n as Int? ?? 9` and `n as Int? ?` are ALL refused the same way (the first three were silent operator reads, the last a silent `Int??`); coalescing after a cast always takes parentheses; `Int??` as a cast target has one spelling, `Optional<Int?>`. Consumer sweep (obligation 2): no tracked .saw file writes `as T ?? d`, so the flip breaks nothing in the corpus. Lands in design 274 U0′ (SL-2, agent:claude-sl2-u0) with the four cells of 259 §2 as the pins plus the `Int ?? 9` and `Optional<Int?>` rows; the user may post the formal ruling kind on this issue.\n","created":"1790195387","id":"c1"},{"author":"agent:claude-sawlang","body":"USER RULING (Sep 24, relayed by the lead; the user may post it with the formal ruling kind): the TEACHING half of the Sep 23 ruling (c1) is amended; the refusal half stands unchanged.\n\nWhy: U0′ r2 found that both spellings c1 taught are unusable. Both parse, but `as` never produces an optional. On main the typechecker refuses:\n- `(n as Int?) ?? 9` with n: Int? (\"cannot cast `Int?` to `Int?`\");\n- the same shape with n: Int8? (\"cannot cast `Int8?` to `Int?`\");\n- `n as Optional<Int?>` (\"cannot cast `Int?` to `Int??`\").\nThe note would have sent readers to code that fails at the next step.\n\nRuled (option 2 of three, chosen over keeping the ruled text and over giving casts optional targets):\n- The refusal is unchanged. It is whitespace-blind and sits at the `??` token or at a second `?` token, and a single `?` stays the type's.\n- The learning note teaches what compiles today: coalesce first, then convert, `(n ?? 9) as Int`. It says plainly that a cast never produces an optional.\n- LANGUAGE_SPEC.md and the saw-lang skill teach the same, and drop the `as Optional<Int?>` cast example. The `Optional<Int?>` spelling in type annotations is unaffected.\n\nLands in U0′ r2 (SL-2.p2). The typechecker's message for a cast to an optional type is filed separately.\n","created":"1790261822","id":"c2"},{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \"oracle known wrong\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `parser.cast-optional-target`. Its ruling is carried in SL-393 (the rule inventory).","created":"1790341673","id":"c3","kind":"landing"}],"created":"1789693899","description":"Design 259 census N2, reconciled by design 274 §2a. Cells: 'n as Int? ?? 9' -> target Int? (operator-wins, documented); 'n as Int?? 9' -> target Int (?? taken whole); 'n as Int? ?' -> target Int?? ; 'n as Int??<EOL>' -> parse error. Int?? as a cast target has no spelling except 'as Optional<Int?>'; annotation position is unaffected. Ruled R5 (user, Sep 1): the PRINCIPLE is spelling never depends on whitespace; the grammar detail (re-splitting ?? after a type in a cast target, and whether 'as Int??' gains a meaning or a tailored refusal) is U0''s to design under that principle with the four cells as its matrix; the user rules on the result if it gains a meaning. Unit: design 274 U0'.","id":"SL-309","labels":["frozen","hazard"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":5,"sequence":2167,"stage":"backlog","status":"closed","title":"N2 (design 259/274): 'as Int??' — whitespace decides the cast target because ?? lexes as one token; ruled R5 = spelling never depends on whitespace","updated":"1790341673"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"USER RULING (Sep 23 2026, relayed by the lead): REFUSE THE AMBIGUITY, with a learning note. Under R5 (spelling never depends on whitespace) the parser never consults spacing, so the contract is token-shaped: in a CAST TARGET, a `??` token, or a second `?` token after the target's own `?`, is refused at that token with a teaching diagnostic that names both intended spellings — `(n as Int?) ?? 9` to coalesce the cast's result, `as Optional<Int?>` for a doubly-optional target. A single `?` stays the type's (`n as Int?`). Consequences, all whitespace-blind: `n as Int?? 9`, `n as Int ?? 9`, `n as Int? ?? 9` and `n as Int? ?` are ALL refused the same way (the first three were silent operator reads, the last a silent `Int??`); coalescing after a cast always takes parentheses; `Int??` as a cast target has one spelling, `Optional<Int?>`. Consumer sweep (obligation 2): no tracked .saw file writes `as T ?? d`, so the flip breaks nothing in the corpus. Lands in design 274 U0′ (SL-2, agent:claude-sl2-u0) with the four cells of 259 §2 as the pins plus the `Int ?? 9` and `Optional<Int?>` rows; the user may post the formal ruling kind on this issue.\n","created":"1790195387","id":"c1"},{"author":"agent:claude-sawlang","body":"USER RULING (Sep 24, relayed by the lead; the user may post it with the formal ruling kind): the TEACHING half of the Sep 23 ruling (c1) is amended; the refusal half stands unchanged.\n\nWhy: U0′ r2 found that both spellings c1 taught are unusable. Both parse, but `as` never produces an optional. On main the typechecker refuses:\n- `(n as Int?) ?? 9` with n: Int? (\"cannot cast `Int?` to `Int?`\");\n- the same shape with n: Int8? (\"cannot cast `Int8?` to `Int?`\");\n- `n as Optional<Int?>` (\"cannot cast `Int?` to `Int??`\").\nThe note would have sent readers to code that fails at the next step.\n\nRuled (option 2 of three, chosen over keeping the ruled text and over giving casts optional targets):\n- The refusal is unchanged. It is whitespace-blind and sits at the `??` token or at a second `?` token, and a single `?` stays the type's.\n- The learning note teaches what compiles today: coalesce first, then convert, `(n ?? 9) as Int`. It says plainly that a cast never produces an optional.\n- LANGUAGE_SPEC.md and the saw-lang skill teach the same, and drop the `as Optional<Int?>` cast example. The `Optional<Int?>` spelling in type annotations is unaffected.\n\nLands in U0′ r2 (SL-2.p2). The typechecker's message for a cast to an optional type is filed separately.\n","created":"1790261822","id":"c2"}],"created":"1789693899","description":"Design 259 census N2, reconciled by design 274 §2a. Cells: 'n as Int? ?? 9' -> target Int? (operator-wins, documented); 'n as Int?? 9' -> target Int (?? taken whole); 'n as Int? ?' -> target Int?? ; 'n as Int??<EOL>' -> parse error. Int?? as a cast target has no spelling except 'as Optional<Int?>'; annotation position is unaffected. Ruled R5 (user, Sep 1): the PRINCIPLE is spelling never depends on whitespace; the grammar detail (re-splitting ?? after a type in a cast target, and whether 'as Int??' gains a meaning or a tailored refusal) is U0''s to design under that principle with the four cells as its matrix; the user rules on the result if it gains a meaning. Unit: design 274 U0'.","id":"SL-309","labels":["frozen","hazard"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":4,"sequence":2166,"stage":"backlog","status":"open","title":"N2 (design 259/274): 'as Int??' — whitespace decides the cast target because ?? lexes as one token; ruled R5 = spelling never depends on whitespace","updated":"1790341673"},"id":"SL-309","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \\\"oracle known wrong\\\" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `parser.cast-optional-target`. Its ruling is carried in SL-393 (the rule inventory).\",\"id\":\"SL-309\",\"request_id\":\"pid-56384-1790341673-6000\"}","request_id":"pid-56384-1790341673-6000","sequence":2167,"timestamp":"1790341673"}
---
