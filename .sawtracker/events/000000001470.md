---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"agent:claude-sl340","author":"agent:claude-sl318u3","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 3 in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188483","id":"c1"},{"author":"agent:claude-sawlang","body":"Scope widened by the lead (Sep 24), after the implementer's census and a lead reproduction on main:\n- Nominal alias values over an owning type never drop. With type Tag = Noisy, neither Tag(Noisy(...)) nor Tag(move n) prints a drop. This is the same two halves as the cell. The alias construction also skips _check_value_transfer, and the drop glue does not resolve the alias.\n- One predicate, 'the type whose lifecycle stands for this one' (the cell's payload, the alias's underlying type), now answers drop, retain, release, copy and _needs_cleanup.\n- Codegen side of half 1: calls._generate_function_call lowers both intercepted arguments with _generate_expression rather than _gen_transfer_value. It gets a one-line fix per arm, approved; the SL-355 receiver-arm hunks are elsewhere in calls.py.\n- UnsafeMemory.write is routed like ptr[i] = v.\nFiled out of scope: SL-378 (placement write through a non-identifier pointer drops its source twice; Once.set has the shape) and SL-379 (UnsafeMemory.read returns an unretained alias; needs a ruling).\n","created":"1790262880","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-340.p1 (revision 1): A cell or alias construction is a value transfer, and a cell or alias drops, retains and copies as the type it stands for (Mutex use-after-free + cell/alias payload leak)","created":"1790273293","id":"c3"}],"created":"1790049564","description":"USE-AFTER-FREE: `Mutex(value: move v)` at a heap-owning payload FREES the payload at construction. Found on MAIN (`da84578e`) while building design 275 U3's ruled `Mutex` conformance row.\n\nREPRO (main, exit 0, no diagnostic):\n\n```saw\nimport std.mutex.{Mutex}\n\nfunc main() {\n    var v: Vector<Int> = [1, 2, 3]\n    let guarded = Mutex(value: move v)\n    print(guarded.lock({ items in items[0] }))   // prints garbage\n}\n```\n\n`items.len()` reads 3 — the length word survives — and `items[0]` reads through a buffer pointer whose allocation was already released, so the value is whatever the allocator left there. Nondeterministic across runs.\n\nMECHANISM (obligation 4). `Mutex<T>.init(value: T)` is\n\n```saw\npublic init(value: T) unsafe -> Mutex<T> {\n    Mutex<T>(state: UnsafeMutableInterior(0),\n             value: UnsafeMutableInterior(value))\n}\n```\n\nand the emitted `Mutex_init_value` reads:\n\n    %value.1 = alloca %Vector\n    store %Vector %value, %Vector* %value.1\n    %value.dropflag = alloca i1\n    store i1 1, i1* %value.dropflag          ; design 42: registered for cleanup\n    %value.2 = load %Vector, %Vector* %value.1\n    %.5 = insertvalue %Mutex undef, i64 0, 0\n    %.6 = insertvalue %Mutex %.5, %Vector %value.2, 1     ; the payload moves IN\n    %value.needsdrop = load i1, i1* %value.dropflag        ; …and the flag is STILL 1\n    br i1 %value.needsdrop, label %drop.value, ...\n    drop.value:\n      call void @Vector_deinit(%Vector* %value.1)          ; the buffer is freed here\n    ret %Mutex %.6                                         ; …and returned anyway\n\nAn `init`'s by-value parameter is registered for scope-exit cleanup and the flag is cleared by whatever CONSUMES it. Here the consumer is `UnsafeMutableInterior(value)` — a cell CONSTRUCTOR whose argument is the payload — and the transfer into it is not recognised as the move that retires the parameter, so the parameter is released at the end of the init body while its bytes have already been copied into the returned struct.\n\nCLASS — every constructor whose parameter is consumed only by an `UnsafeMutableInterior(...)` construction, at every heap-owning payload:\n- `Mutex<Vector<Int>>`, `Mutex<Data>`, `Mutex<String>`, `Mutex<Map<..>>` — measured on `Vector`;\n- `Once<T>`'s `set` path and `SpinLock<T>`'s own initializer are the neighbours to probe (they take the same shape);\n- a USER type following the design-186 interior-mutability idiom (`struct Counter { cell: UnsafeMutableInterior<Int> }` with an `init`) is the same shape, and is only invisible at `Int` because a trivial payload has no `deinit` to run.\nA payload with no `deinit` (`Mutex<Int>`, `Mutex<Bool>`) is unaffected, which is why the whole corpus passes: every `Mutex` in the tree guards a trivial or refcount-free payload.\n\nWHY IT SURFACED NOW: design 275 U3's ruled conformance row is \"a `for` over the guarded collection inside `lock` is a sync window and works\" — the first program in the tree to put a heap-owning container inside a `Mutex`.\n\nPIN: `examples/conformance/K141_mutex_guarded_for_is_sync.saw` (XFAIL, citing this issue). Its XPASS flip is the validation; the paired REFUSAL half (a suspension inside a `lock` body is refused by the `sync`-body rule) is `examples/errors/mutex_guarded_for_suspends.saw` and passes today.\n\n\n---\n\n# BRIEF (lead, Sep 24): READY\n\n## Re-measured on main\nCells are in the lead checkout at `.build/scratch/sl340_probe.saw`, `sl340_drop.saw`, `sl340_drop2.saw` and `sl340_ctrl.saw`.\n- `Mutex(value: move v)` with a `Vector<Int>`: `items[0]` reads garbage on 1 run in 3. This is the finding, and it still reproduces.\n- `Mutex(value: Noisy(name: \"kept\"))`: `drop kept` prints AT CONSTRUCTION, before the first `lock`, and nothing prints when the Mutex dies.\n- **A second, hidden half: a cell NEVER drops its payload.** A user `struct Holder { plain: Noisy, cell: UnsafeMutableInterior<Noisy> }` going out of scope prints `drop plain field` and nothing for the cell. This holds whether the payload came from an rvalue, a by-value parameter, or `move` of a local. Every heap-owning cell payload leaks.\n- **The two halves hide each other today.** Construction from a parameter frees the payload early, and the owner never frees it again, so nothing double-frees. Fixing either half alone turns the bug into a leak or a double free. **Both land in one patch.**\n\n## Mechanism (obligation 4), confirmed in code\n\n**Half 1: the intercepted construction bypasses the value-transfer checkpoint.**\n- Control: an ordinary call `sink(value)` with a NoCopy parameter is refused: \"cannot copy value of type `Noisy` which implements NoCopy; use `move`\".\n- `UnsafeMutableInterior(value)` with the same parameter is accepted silently. Its interception in `typechecker/expressions.py` (the `expr.name == \"UnsafeMutableInterior\"` branch, which stamps `is_interior_cell_construct`) checks only `_transfer_compatible`, never `_check_value_transfer`.\n- So the argument is neither refused as a copy nor recorded as moved. Codegen never clears the parameter's drop flag, and the parameter is released at scope exit after its bytes were copied into the cell.\n- This is the DF-216a class: a compiler-intercepted construction that skips the transfer funnel.\n\n**Half 2: the drop glue has no path for a layout-transparent wrapper.**\n- `UnsafeMutableInterior<T>` lowers to a bare `T` (`type_identity.is_layout_transparent`). It deliberately gets no monomorphized `deinit`, because a method taking the wrapper's address could never be called with the field's address.\n- `_emit_drop_at` (`codegen/resources.py`) therefore finds no deinit symbol, falls to the struct-field path, and drops nothing.\n- The right drop of a transparent wrapper is its payload's drop at the same address. The same holds for `_needs_cleanup`, and for the copy/retain paths wherever a transparent wrapper's payload is refcounted or Copy-tier. Answer all of these through ONE predicate, the payload's own, so the three places cannot disagree.\n\n## The fix\n1. **Construction.** Every compiler-intercepted construction whose argument is a VALUE routes that argument through `_check_value_transfer`, exactly as a call argument does:\n   - a NoCopy/ExplicitCopy argument must be written `move x` (or `.copy()`);\n   - a moved one is recorded as moved, so codegen clears its flag.\n   Enumerate the intercepted constructions (the `is_*_construct` stamps: `Atomic(...)`, `UnsafeMutableInterior(...)`, `UnsafeMemory(...)`, plus the alias back-conversion `UserId(i)`, `E.from(raw:)` and `UnsafeMemory.write` that `_transfer_compatible`'s docstring names). Probe each with an owning argument wherever the type admits one: an alias of `String`/`Vector`, and `UnsafeMemory.write` of a non-trivial `T` if legal. Route every one that can carry an owning value; record WHY each of the others cannot.\n2. **Drop.** A layout-transparent wrapper drops, retains and copies as its payload does, at the same address, and `_needs_cleanup` answers through the payload. Route this through one place, and name its entry points in the docstring.\n3. **Std migration (the consumer sweep).** Once item 1 lands, `Mutex.init`, `SpinLock`'s initializer and any other `UnsafeMutableInterior(value)` over a generic or owning `T` must spell `move value`.\n   - Grep every `UnsafeMutableInterior(` and `Atomic(` construction in sawc/std, blade/, libs/, devtools/, selfhost/, prototypes/ and examples/.\n   - Each is either migrated to `move` (a by-value owner) or already fine (a literal or a trivial type). List them in the report.\n   - `Once.set` writes through the pointer (`self.value.ptr()[0] = value`). Probe it: `Once<Vector<Int>>` set, get, then drop, with the drop count. Also check whether that assignment drops a zero-initialized old value (a hazard for a zeroed `String`/`Vector` slot).\n\n## Conformance (obligation 3) and position matrix\nConformance rows come FIRST, from the Studio's range **K170-K179** (the Air holds K154 and K160-K169). They go beside the existing `K141_mutex_guarded_for_is_sync.saw`, which is XFAIL citing this issue: remove that marker in this patch, because its XPASS is the validation.\n\nEvery row counts drops with a printing deinit and requires EXACTLY ONE drop, at the owner's end:\n1. `Mutex(value: move v)` over `Vector<Int>`: reads are correct, and the buffer is freed once when the Mutex dies. Use an ASan build or a drop-counting payload.\n2. `Mutex<Noisy>`, from an rvalue and from a moved local.\n3. `SpinLock<T>` with a heap-owning payload, both sources.\n4. `Once<T>` with a heap-owning payload: set, get (via `.copy()`), then drop.\n5. A user cell struct (`Holder` above), from an rvalue, a by-value parameter and a moved local.\n6. A cell nested as an `Optional` payload, an enum payload, a fixed-array element and a generic struct field.\n7. A Mutex held across a `yield_now()` in a driven body, dropped once. This is the frame twin.\n8. REFUSAL: `UnsafeMutableInterior(value)` with a NoCopy/ExplicitCopy parameter and no `move` → \"cannot copy … use `move`\", pinned in examples/errors/.\n9. CONTROL: `Mutex<Int>`, `Atomic(0)` and `static` Mutex/SpinLock are unchanged. The all-zero static costs no image bytes and never drops.\n\n## Gates and files\n- Full terminal battery. `transferdecisions` is live again: a new writer of `needs_copy`/`payload_needs_copy` must go through `carry_retain_stamps`, so check it.\n- Files: `sawc/typechecker/expressions.py` (the interception branches), `sawc/codegen/resources.py` (drop glue plus `_needs_cleanup`), possibly `sawc/codegen/structs.py`/`generics.py` for retain/copy, `sawc/std/{mutex,spinlock,once}.saw`, `examples/conformance/`, `examples/errors/`.\n- This does not overlap SL-345 (the Air's call-exclusivity funnel and closure checker) or SL-355's `calls.py`/`coro_transform.py` hunks. If the retain path turns out to need `calls.py`, stop and report before editing it.\n","id":"SL-340","labels":["bug","design-275"],"order":3,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":9,"sequence":1470,"stage":"queued","status":"open","title":"USE-AFTER-FREE: Mutex(value: move v) frees a heap-owning payload at construction — an UnsafeMutableInterior(...) argument does not retire the init parameter","updated":"1790273293"},"before":{"acceptance":[],"assignee":"agent:claude-sl340","author":"agent:claude-sl318u3","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 3 in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188483","id":"c1"},{"author":"agent:claude-sawlang","body":"Scope widened by the lead (Sep 24), after the implementer's census and a lead reproduction on main:\n- Nominal alias values over an owning type never drop. With type Tag = Noisy, neither Tag(Noisy(...)) nor Tag(move n) prints a drop. This is the same two halves as the cell. The alias construction also skips _check_value_transfer, and the drop glue does not resolve the alias.\n- One predicate, 'the type whose lifecycle stands for this one' (the cell's payload, the alias's underlying type), now answers drop, retain, release, copy and _needs_cleanup.\n- Codegen side of half 1: calls._generate_function_call lowers both intercepted arguments with _generate_expression rather than _gen_transfer_value. It gets a one-line fix per arm, approved; the SL-355 receiver-arm hunks are elsewhere in calls.py.\n- UnsafeMemory.write is routed like ptr[i] = v.\nFiled out of scope: SL-378 (placement write through a non-identifier pointer drops its source twice; Once.set has the shape) and SL-379 (UnsafeMemory.read returns an unretained alias; needs a ruling).\n","created":"1790262880","id":"c2"}],"created":"1790049564","description":"USE-AFTER-FREE: `Mutex(value: move v)` at a heap-owning payload FREES the payload at construction. Found on MAIN (`da84578e`) while building design 275 U3's ruled `Mutex` conformance row.\n\nREPRO (main, exit 0, no diagnostic):\n\n```saw\nimport std.mutex.{Mutex}\n\nfunc main() {\n    var v: Vector<Int> = [1, 2, 3]\n    let guarded = Mutex(value: move v)\n    print(guarded.lock({ items in items[0] }))   // prints garbage\n}\n```\n\n`items.len()` reads 3 — the length word survives — and `items[0]` reads through a buffer pointer whose allocation was already released, so the value is whatever the allocator left there. Nondeterministic across runs.\n\nMECHANISM (obligation 4). `Mutex<T>.init(value: T)` is\n\n```saw\npublic init(value: T) unsafe -> Mutex<T> {\n    Mutex<T>(state: UnsafeMutableInterior(0),\n             value: UnsafeMutableInterior(value))\n}\n```\n\nand the emitted `Mutex_init_value` reads:\n\n    %value.1 = alloca %Vector\n    store %Vector %value, %Vector* %value.1\n    %value.dropflag = alloca i1\n    store i1 1, i1* %value.dropflag          ; design 42: registered for cleanup\n    %value.2 = load %Vector, %Vector* %value.1\n    %.5 = insertvalue %Mutex undef, i64 0, 0\n    %.6 = insertvalue %Mutex %.5, %Vector %value.2, 1     ; the payload moves IN\n    %value.needsdrop = load i1, i1* %value.dropflag        ; …and the flag is STILL 1\n    br i1 %value.needsdrop, label %drop.value, ...\n    drop.value:\n      call void @Vector_deinit(%Vector* %value.1)          ; the buffer is freed here\n    ret %Mutex %.6                                         ; …and returned anyway\n\nAn `init`'s by-value parameter is registered for scope-exit cleanup and the flag is cleared by whatever CONSUMES it. Here the consumer is `UnsafeMutableInterior(value)` — a cell CONSTRUCTOR whose argument is the payload — and the transfer into it is not recognised as the move that retires the parameter, so the parameter is released at the end of the init body while its bytes have already been copied into the returned struct.\n\nCLASS — every constructor whose parameter is consumed only by an `UnsafeMutableInterior(...)` construction, at every heap-owning payload:\n- `Mutex<Vector<Int>>`, `Mutex<Data>`, `Mutex<String>`, `Mutex<Map<..>>` — measured on `Vector`;\n- `Once<T>`'s `set` path and `SpinLock<T>`'s own initializer are the neighbours to probe (they take the same shape);\n- a USER type following the design-186 interior-mutability idiom (`struct Counter { cell: UnsafeMutableInterior<Int> }` with an `init`) is the same shape, and is only invisible at `Int` because a trivial payload has no `deinit` to run.\nA payload with no `deinit` (`Mutex<Int>`, `Mutex<Bool>`) is unaffected, which is why the whole corpus passes: every `Mutex` in the tree guards a trivial or refcount-free payload.\n\nWHY IT SURFACED NOW: design 275 U3's ruled conformance row is \"a `for` over the guarded collection inside `lock` is a sync window and works\" — the first program in the tree to put a heap-owning container inside a `Mutex`.\n\nPIN: `examples/conformance/K141_mutex_guarded_for_is_sync.saw` (XFAIL, citing this issue). Its XPASS flip is the validation; the paired REFUSAL half (a suspension inside a `lock` body is refused by the `sync`-body rule) is `examples/errors/mutex_guarded_for_suspends.saw` and passes today.\n\n\n---\n\n# BRIEF (lead, Sep 24): READY\n\n## Re-measured on main\nCells are in the lead checkout at `.build/scratch/sl340_probe.saw`, `sl340_drop.saw`, `sl340_drop2.saw` and `sl340_ctrl.saw`.\n- `Mutex(value: move v)` with a `Vector<Int>`: `items[0]` reads garbage on 1 run in 3. This is the finding, and it still reproduces.\n- `Mutex(value: Noisy(name: \"kept\"))`: `drop kept` prints AT CONSTRUCTION, before the first `lock`, and nothing prints when the Mutex dies.\n- **A second, hidden half: a cell NEVER drops its payload.** A user `struct Holder { plain: Noisy, cell: UnsafeMutableInterior<Noisy> }` going out of scope prints `drop plain field` and nothing for the cell. This holds whether the payload came from an rvalue, a by-value parameter, or `move` of a local. Every heap-owning cell payload leaks.\n- **The two halves hide each other today.** Construction from a parameter frees the payload early, and the owner never frees it again, so nothing double-frees. Fixing either half alone turns the bug into a leak or a double free. **Both land in one patch.**\n\n## Mechanism (obligation 4), confirmed in code\n\n**Half 1: the intercepted construction bypasses the value-transfer checkpoint.**\n- Control: an ordinary call `sink(value)` with a NoCopy parameter is refused: \"cannot copy value of type `Noisy` which implements NoCopy; use `move`\".\n- `UnsafeMutableInterior(value)` with the same parameter is accepted silently. Its interception in `typechecker/expressions.py` (the `expr.name == \"UnsafeMutableInterior\"` branch, which stamps `is_interior_cell_construct`) checks only `_transfer_compatible`, never `_check_value_transfer`.\n- So the argument is neither refused as a copy nor recorded as moved. Codegen never clears the parameter's drop flag, and the parameter is released at scope exit after its bytes were copied into the cell.\n- This is the DF-216a class: a compiler-intercepted construction that skips the transfer funnel.\n\n**Half 2: the drop glue has no path for a layout-transparent wrapper.**\n- `UnsafeMutableInterior<T>` lowers to a bare `T` (`type_identity.is_layout_transparent`). It deliberately gets no monomorphized `deinit`, because a method taking the wrapper's address could never be called with the field's address.\n- `_emit_drop_at` (`codegen/resources.py`) therefore finds no deinit symbol, falls to the struct-field path, and drops nothing.\n- The right drop of a transparent wrapper is its payload's drop at the same address. The same holds for `_needs_cleanup`, and for the copy/retain paths wherever a transparent wrapper's payload is refcounted or Copy-tier. Answer all of these through ONE predicate, the payload's own, so the three places cannot disagree.\n\n## The fix\n1. **Construction.** Every compiler-intercepted construction whose argument is a VALUE routes that argument through `_check_value_transfer`, exactly as a call argument does:\n   - a NoCopy/ExplicitCopy argument must be written `move x` (or `.copy()`);\n   - a moved one is recorded as moved, so codegen clears its flag.\n   Enumerate the intercepted constructions (the `is_*_construct` stamps: `Atomic(...)`, `UnsafeMutableInterior(...)`, `UnsafeMemory(...)`, plus the alias back-conversion `UserId(i)`, `E.from(raw:)` and `UnsafeMemory.write` that `_transfer_compatible`'s docstring names). Probe each with an owning argument wherever the type admits one: an alias of `String`/`Vector`, and `UnsafeMemory.write` of a non-trivial `T` if legal. Route every one that can carry an owning value; record WHY each of the others cannot.\n2. **Drop.** A layout-transparent wrapper drops, retains and copies as its payload does, at the same address, and `_needs_cleanup` answers through the payload. Route this through one place, and name its entry points in the docstring.\n3. **Std migration (the consumer sweep).** Once item 1 lands, `Mutex.init`, `SpinLock`'s initializer and any other `UnsafeMutableInterior(value)` over a generic or owning `T` must spell `move value`.\n   - Grep every `UnsafeMutableInterior(` and `Atomic(` construction in sawc/std, blade/, libs/, devtools/, selfhost/, prototypes/ and examples/.\n   - Each is either migrated to `move` (a by-value owner) or already fine (a literal or a trivial type). List them in the report.\n   - `Once.set` writes through the pointer (`self.value.ptr()[0] = value`). Probe it: `Once<Vector<Int>>` set, get, then drop, with the drop count. Also check whether that assignment drops a zero-initialized old value (a hazard for a zeroed `String`/`Vector` slot).\n\n## Conformance (obligation 3) and position matrix\nConformance rows come FIRST, from the Studio's range **K170-K179** (the Air holds K154 and K160-K169). They go beside the existing `K141_mutex_guarded_for_is_sync.saw`, which is XFAIL citing this issue: remove that marker in this patch, because its XPASS is the validation.\n\nEvery row counts drops with a printing deinit and requires EXACTLY ONE drop, at the owner's end:\n1. `Mutex(value: move v)` over `Vector<Int>`: reads are correct, and the buffer is freed once when the Mutex dies. Use an ASan build or a drop-counting payload.\n2. `Mutex<Noisy>`, from an rvalue and from a moved local.\n3. `SpinLock<T>` with a heap-owning payload, both sources.\n4. `Once<T>` with a heap-owning payload: set, get (via `.copy()`), then drop.\n5. A user cell struct (`Holder` above), from an rvalue, a by-value parameter and a moved local.\n6. A cell nested as an `Optional` payload, an enum payload, a fixed-array element and a generic struct field.\n7. A Mutex held across a `yield_now()` in a driven body, dropped once. This is the frame twin.\n8. REFUSAL: `UnsafeMutableInterior(value)` with a NoCopy/ExplicitCopy parameter and no `move` → \"cannot copy … use `move`\", pinned in examples/errors/.\n9. CONTROL: `Mutex<Int>`, `Atomic(0)` and `static` Mutex/SpinLock are unchanged. The all-zero static costs no image bytes and never drops.\n\n## Gates and files\n- Full terminal battery. `transferdecisions` is live again: a new writer of `needs_copy`/`payload_needs_copy` must go through `carry_retain_stamps`, so check it.\n- Files: `sawc/typechecker/expressions.py` (the interception branches), `sawc/codegen/resources.py` (drop glue plus `_needs_cleanup`), possibly `sawc/codegen/structs.py`/`generics.py` for retain/copy, `sawc/std/{mutex,spinlock,once}.saw`, `examples/conformance/`, `examples/errors/`.\n- This does not overlap SL-345 (the Air's call-exclusivity funnel and closure checker) or SL-355's `calls.py`/`coro_transform.py` hunks. If the retain path turns out to need `calls.py`, stop and report before editing it.\n","id":"SL-340","labels":["bug","design-275"],"order":3,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":8,"sequence":1450,"stage":"queued","status":"open","title":"USE-AFTER-FREE: Mutex(value: move v) frees a heap-owning payload at construction — an UnsafeMutableInterior(...) argument does not retire the init parameter","updated":"1790262880"},"id":"SL-340","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/.claude/skills/saw-lang/SKILL.md b/.claude/skills/saw-lang/SKILL.md\\nindex 20aca26f..0cf73548 100644\\n--- a/.claude/skills/saw-lang/SKILL.md\\n+++ b/.claude/skills/saw-lang/SKILL.md\\n@@ -45,6 +45,10 @@ type UserId = Int          // DISTINCT type; flows TO Int; back via UserId(i)\\n                            // inside a function body is still refused.\\n                            // underlying may NOT be an enum (DF-194b) — no `as`\\n                            // reading exists for one, so the alias is refused\\n+                           // `Tag(x)` is a TRANSFER (SL-340): an owning `x`\\n+                           // needs `move`, and the alias value drops as its\\n+                           // underlying does. Before Sep 24 it did not drop\\n+                           // at all, and `Tag(param)` freed early\\n let raw = id as Int        // explicit projection toward underlying\\n print(\\\"hi {name}: {p}\\\")    // interpolation; user types need Printable\\n print(\\\"{#file}:{#line} - msg\\\")  // #file/#line/#function: definition-site consts\\n@@ -3708,6 +3712,16 @@ slab in std/slab.saw; `UnsafeMemory<T, Device|Normal>` for MMIO\\n   `Atomic` says (design 202). A DECLARED policy on the field's own type\\n   wins over the clause, so an `Atomic` field cascades `NoCopy` upward\\n   while an `UnsafeMutableInterior<Int>` field does not.\\n+  **THE CELL OWNS ITS PAYLOAD (SL-340).** `UnsafeMutableInterior(x)` is a\\n+  transfer, checked like a call argument: an owning `x` read out of a\\n+  binding is written `UnsafeMutableInterior(move x)` (bare is the \\\"cannot\\n+  copy\\\" refusal), a Copy-tier one retains, and a generic body forwarding its\\n+  own parameter needs nothing written. The payload drops with the cell's\\n+  owner, exactly once. SUSPECT OLDER BUILDS: until Sep 24 a cell never\\n+  dropped its payload, and a by-value parameter handed to one was freed at\\n+  the end of the function that built the cell — `Mutex(value: move v)` over\\n+  a `Vector` read freed memory. `UnsafeMemory.write(v)` is the same\\n+  transfer (a placement write, `move` for an owning value).\\n - **`UnsafeSync` / `UnsafeSend` are the DECLARED thread-safety assertion**\\n   (design 186). `Send`/`Sync` stay derivation-only (`extension X: Sync` is\\n   still rejected); these two REFINE them, so a declared conformance\\ndiff --git a/LANGUAGE_SPEC.md b/LANGUAGE_SPEC.md\\nindex 7977b8f4..624ba3d7 100644\\n--- a/LANGUAGE_SPEC.md\\n+++ b/LANGUAGE_SPEC.md\\n@@ -2858,6 +2858,17 @@ adopts that type and is range-checked there, and a value of an unrelated type is\\n a clean error. The construction is representationally free: an alias is its\\n underlying at runtime, and the distinction is the typechecker's alone.\\n \\n+The argument is a transfer, checked as a call argument is. An owning value read\\n+out of a binding needs `move` (or `.copy()` where the type has one), a `Copy`-tier\\n+value is retained, and the alias value then owns what it was built from. Its\\n+lifecycle is its underlying's: a `type Tag = Session` value deinits exactly\\n+where a `Session` would, and copying a `type Handle = Arc<Pool>` value retains.\\n+\\n+```saw-fragment\\n+let session = Session(id: 7)\\n+let tag = Tag(move session)   // `Tag(session)` is refused: `Session` is NoCopy\\n+```\\n+\\n An alias over an unsigned or fixed-width underlying has no other spelling. An\\n annotated `let` accepts an underlying-typed initializer only for the four\\n primitive kinds (`Int`, `Float`, `Bool`, `String`), so `type Handle = UInt` is\\n@@ -9821,6 +9832,12 @@ wrapper method takes on the all-safe-parameters obligation exactly where\\n exactly its `T` — so a cell field costs no wrapper, and construction is\\n positional (`UnsafeMutableInterior(v)`).\\n \\n+The cell OWNS its payload. Construction is a transfer, checked as a call\\n+argument is: an owning value read out of a binding is written\\n+`UnsafeMutableInterior(move v)`, a `Copy`-tier one is retained, and a generic\\n+body forwarding its own parameter needs nothing written. The payload deinits\\n+with whatever holds the cell, exactly once, as if the field were a plain `T`.\\n+\\n A type that transitively contains a cell is **cell-carrying**, and that answers\\n four questions at once:\\n \\n@@ -12740,6 +12757,12 @@ promise). Two markers exist today:\\n   **`len() -> Int`** (region byte length), and **`end() -> UnsafePointer<Int8>`**\\n   (one-past-the-end — a stack top or slab handoff).\\n \\n+`write(v)` is a placement write, on the terms `ptr[i] = move v` has: the memory\\n+becomes the value's owner. An owning value read out of a binding is written\\n+`move v`, and without the `move` it is the ordinary \\\"cannot copy\\\" refusal; a\\n+`Copy`-tier value is retained. Nothing releases what the memory holds, so\\n+reclaiming an owning value stored there is the program's job.\\n+\\n **Projection (both intents, one engine).** Member access on\\n `UnsafeMemory<Struct, Use>` yields `UnsafeMemory<Field, Use>` at *base +\\n compile-time offset*; it chains through nested structs and indexes through\\ndiff --git a/examples/conformance/INDEX.md b/examples/conformance/INDEX.md\\nindex 7adfa7ec..644cd0af 100644\\n--- a/examples/conformance/INDEX.md\\n+++ b/examples/conformance/INDEX.md\\n@@ -6,7 +6,11 @@ either a file in this directory or an existing `examples/` test that already\\n asserts the same rule at the same position — this table is the record of which,\\n and the dedup decisions are meant to be audited from it.\\n \\n-**575 rows: 297 carry a file here, 278 are covered elsewhere** (recounted Sep 22\\n+**585 rows: 305 carry a file here, 280 are covered elsewhere** (recounted Sep 24\\n+when SL-340 added K170-K179, the owning-payload rules for a cell and an alias —\\n+K170, K171, K173, K174, K175, K177, K178 and K179 carry a file here, K172 and\\n+K176 point at `examples/errors/` files; previously, and on the same basis:\\n+**575 rows: 297 carry a file here, 278 are covered elsewhere**, recounted Sep 22\\n when design 275 U3 added K132-K153, the statement-scoped window and the two\\n missing splits — K133, K135, K136, K137, K138, K140, K141 and K147 carry a file\\n here, K132, K134, K139, K142, K143, K144, K145, K146, K148, K149, K150, K151,\\n@@ -740,7 +744,7 @@ Claim source: spec 6 *Send and Sync* + *Cooperative tasks*; designs 75, 88, 103,\\n | K138 | THE ACQUIRE SEQUENCE IS ONE SEQUENCE — receiver place, then the head's shared borrow spanning the whole call expression, then arguments left to right, then the producer, then the hand-off to the statement charge with no gap | `K138_window_acquire_sequence.saw` + `errors/window_acquire_mutating_argument.saw` | design 275 U3, B3 (codex p5-r1 P2). It is LANGUAGE_SPEC's \\\"Argument Evaluation Order\\\" plus design 141's rule that a reference argument's window is the whole CALL EXPRESSION, applied to the head unchanged — nothing here changes evaluation order. Three cells pin it: an argument MUTATING the root is refused (by design 199's own nested-call rule, which is what makes A2 free), a SUSPENDING argument is accepted with the sync twin's value (the U2 shape table's HOIST lifts it, and the checker's answer never depended on the lowering), and a FAILING argument exits before the producer runs — no charge, no iterator, nothing to clean, and the root writable in the caller. B4's accept twin is here too: an OWNED iterator produced by a SUSPENDING head is evaluated ONCE and preserved, which is what separates it from the borrowed head's temporary-receiver refusal |\\n | K139 | the head's borrow spans its ARGUMENTS, so an argument that writes the root is refused before the producer runs | `errors/window_acquire_mutating_argument.saw` | design 275 U3, B3 step A2. Stated as its own row because it is the one ACQUIRE cell that is a refusal, and because the rule it rests on is design 199's rather than U3's: `f(&v, g(&var v))` was already a conflict, and a `for` head is a call expression like any other |\\n | K140 | CANCELLATION IS A SAFETY GUARANTEE, NOT A TERMINATION CLAIM — a cancel does not close a window, and a live window is never destroyed by dropping a still-running frame | `K140_window_under_cancellation.saw` | design 275 U3 (codex point 5). Teardown DRIVES the frame (a cancel wakes a parked task, design 102) and JOINS it (design 124 item 5, K87) before its box is released, so the frame reaches the statement's exit by ordinary control flow and iterator and owner cleanup run exactly once on the route actually taken. Four cells, each COUNTING deinits: a cancel delivered while the task is parked in a NESTED callee (one each); a caught cancellation that CONTINUES the loop (the window stays live, the iterator is not destroyed, the loop runs to exhaustion); a cancellation PROPAGATED out of the head's own evaluation (ZERO iterator constructions, ZERO iterator drops, the owner's own deinit as its initialization state says — a cancel is a REQUEST and does not stop a head from running, which is why the zero-construction cell is a failing HEAD and not a pre-head cancel); and cancel at the park then exhaustion |\\n-| K141 | a `for` over the GUARDED COLLECTION inside `Mutex.lock` is a SYNC window and works; a suspension inside it is refused by the `sync`-body rule | `K141_mutex_guarded_for_is_sync.saw` (XFAIL, SL-340) + `errors/mutex_guarded_for_suspends.saw` | design 275 U3 part (iii) — the `Mutex` row is CLOSED, not open: `lock<R>(&self, body: (&var T) sync -> R)` runs a `sync` body, so nothing here extends a guard's lifetime and nothing has to. The REFUSAL half passes today. The ACCEPT half is XFAIL on SL-340, a pre-existing USE-AFTER-FREE this row is the first program in the tree to reach: `Mutex(value: move v)` at a heap-owning payload FREES the payload at construction, because an `init`'s by-value parameter is registered for scope-exit cleanup and the transfer into `UnsafeMutableInterior(value)` does not retire it. Every `Mutex` in the tree before this one guarded a trivial payload, which is why the whole corpus passes |\\n+| K141 | a `for` over the GUARDED COLLECTION inside `Mutex.lock` is a SYNC window and works; a suspension inside it is refused by the `sync`-body rule | `K141_mutex_guarded_for_is_sync.saw` + `errors/mutex_guarded_for_suspends.saw` | design 275 U3 part (iii) — the `Mutex` row is CLOSED, not open: `lock<R>(&self, body: (&var T) sync -> R)` runs a `sync` body, so nothing here extends a guard's lifetime and nothing has to. The ACCEPT half was XFAIL on SL-340 until it landed: it was the first program in the tree to put a heap-owning payload in a `Mutex`, and `Mutex(value: move v)` freed that payload at construction. K170-K179 are SL-340's rows |\\n | K142 | THE FENCE, value positions: a borrowing struct's value is legal ONLY as the DIRECT head of a `for` whose receiver is a place rooted in a named binding — eleven other positions, one rejector, one rule sentence | `errors/borrowing_struct_value_positions.saw` | design 275 U3. The rows: a `let` and a `var` initializer, an argument, a discarded statement, a tuple element, a collection-literal element, an assignment RHS, a closure's returned value, a `??` operand, a TEMPORARY receiver, and a head that is an `if`/`match`/block/`try` EXPRESSION. The last two get their own sentence because the ACT is different — \\\"bind the collection first\\\" for a temporary, which has no persistent storage for the window to point into. The fence asks its question at the ONE line every expression's type is stamped (`_check_expression`'s `resolved_type`), which is what makes the position list a matrix of TESTS rather than a matrix of checks: there is no `let`-arm or capture-arm to forget. The fixtures are a USER `borrows struct` over a USER collection, because std is the only positive consumer migrated and the negatives are owed in the shape an author would write. INSIDE THE TYPE'S OWN METHODS the exemption is for the receiver READ itself (`self` as a receiver, a scrutinee, an operand; `&self` as a re-borrow) and not for a value that merely has the type there (codex r1 #2): `Alias(self)` through a distinct alias, a tuple of `self`, a `let` bound to `self` and a closure returning `self` are rows 12-15, and a COMPOSITION (`(It, Int)`, `It?`) is refused as the bare value is, naming the wrapper — the fence asks `_first_borrowing_struct_in`, the same walk the declaration positions ask. THE PRODUCER EXEMPTION (e1) COVERS ONLY THE VALUE BEING PROVED (codex r2 #7): the ids of the returning paths' leaves — the tail, every `return`, the arms of a value branch, the wraps and back-conversions on the way (`_producer_proved_leaf_ids`, the same set the origin proof judges) — and every other bare borrowing-typed expression inside a producer meets the ordinary fence; for one revision the exemption read the RETURN TYPE and not the position, so `var held = other.iter()` inside a producer stored a foreign borrow, mutated its origin and read 99 through it (`errors/borrowing_producer_stores_no_borrow.saw`: codex's program, a stored construction that is no path's leaf, the result handed to a call). A CLOSURE LITERAL'S OWN TYPE MEETS THE FENCE (codex r4 #1, row 16): the walk descends a function type, so a closure whose PARAMETER is annotated with the borrowing struct (`{ it: It in 0 }`) — a body with no borrowing-typed expression in it at all — is refused naming the function type that carried it, which is the one refusal that shape gets |\\n | K143 | NOTHING ERASES IT: a borrowing struct may not be a parameter, a field, a plain return, a type argument, an existential or an `Item` — refused at the DECLARATION that names the type | `errors/borrowing_struct_type_positions.saw` + `errors/borrowing_producer_composite_return.saw` | design 275 U3. A helper forwarding the result and a generic identity wrapper are not value-position rows at all — they are IMPOSSIBLE SPELLINGS, which is what makes \\\"a generic identity wrapper launders the value\\\" unwritable rather than caught. The walk recurses through every composition (`Optional<It>`, `Result<It, E>`, `Vector<It>`, `Box<It>`, a tuple, an array), so no wrapper is a way past it, and the associated-type row carries the NO-SECOND-ITEM-LIFETIME rule: a borrowing struct's `Iterator.Item` may be neither a reference nor a borrowing struct, so the loop variable OWNS what it is handed. A DISTINCT ALIAS is the struct at every one of these positions (codex r1 #2): `borrowing_struct_name` is the ONE resolver both boundaries ask, and it walks `type Alias = It` to its underlying because an alias adds a name and never a home for a borrow — for one revision both boundaries read the nominal name and `type Item = Alias` passed. AN ALIAS TO A COMPOSITION IS DESCENDED TO ITS TARGET, AND EXHAUSTING THE WALK REFUSES (codex r2 #6): `type Hidden = Vector<It>` passed as a field, a parameter, a type argument and (through `Twice = Optional<Hidden>`) a return, because the walk asked whether the alias as a whole IS a borrowing struct and then descended the alias, which has no parts; and past eight levels it answered \\\"safe\\\", so nine nested `Vector`s hid an `It`. The bound is thirty-two now and reaching it is `DEPTH_EXHAUSTED`, refused by both callers naming the depth — an unproven type is never admitted (`errors/borrowing_struct_alias_compositions.saw`). THE PRODUCER'S EXEMPTION IS THE BARE RETURN AND NOTHING WIDER (codex r3 #2): the return walk exempts exactly the `borrows -> It` the producer proof validates, so a composite spelling on a `borrows` method — `It?`, `Optional<It>`, `Result<It, E>`, `(It, Int)`, `Vector<It>`, `Box<It>`, an alias of each — takes the same refusal the signature takes without the word; for one revision the exemption keyed on `is_borrows` alone, so `borrows -> It?` compiled and `borrows -> Optional<It>` reached codegen's `Unknown generic struct: Optional`. A FUNCTION TYPE IS A COMPOSITION TOO (codex r4 #1): the walk descends a function type's RETURN and PARAMETERS — a borrowing value handed out of a closure or into one is bound in a frame the window cannot see — nested to any depth (`() -> () -> It`, `((It) -> Int) -> Int`), so `() -> It`, `(It) -> Int` and an alias of either are refused at every position above (a parameter, a return, a `borrows` return, a field, an enum payload, `Vector<() -> It>`/`Box<…>`/`Optional<…>`/`FuncPointer<…>`, a static, an `Item`, a generic default, a trait requirement); for one revision the walk answered None for the whole of `TypeKind.FUNCTION`, so `func bad() -> () -> It` compiled. The reference no-escape walk still STOPS at a function type, and the two differ on purpose: `(&T) -> R` is how every `with_ref` body is typed, while a borrowing struct is legitimate nowhere a closure can put it (`borrowing_producer_every_path.saw` keeps the `(&Bag) sync -> Int` control running) |\\n | K144 | THE ORIGIN IS A CHECKED COMPILER FACT — `lends self` is the only spelling, it requires a SHARED REFERENCE receiver, and every reference field of every returning path takes it | `errors/borrowing_struct_origin.saw` | design 275 U3 (codex c41 point 1). A `borrows` signature says a borrow EXISTS; the call site must know WHICH root to charge, so U3 admits the RECEIVER and refuses the rest by name: a PROJECTION (`lends self.items` — \\\"lend the ROOT, not the buffer\\\", which is the whole shape of the four-line iterator), another reference PARAMETER, and a `&var self` receiver (U3 admits shared fields only). A by-value receiver is not a row here — the parser already refuses `self` without a sigil. The rule-7 teaching error rides along: a `borrows struct` with no reference field claims a nature its shape does not have. The origin SUMMARY is recorded on the declaration as `Method.borrow_origin`, a DECLARED annotation field (design 126's AST contract, `astgraft`-gated), so it rides `substitute_ast_types` through monomorphization and travels with an imported declaration exactly as `is_reference` does — and the call site substitutes it onto the RECEIVER PLACE's root through design 141's root attribution rather than re-deriving a root walk. BY PATH, NOT BY SCAN (codex r1 #1): the check follows the VALUE each returning path hands the caller — the tail, every `return`, and each arm of a value branch through them (`producers.branch_arm_sources`, the one table that says where a branch's arms live) — and refuses the path whose leaf is not a construction written in this body, by line; for one revision it scanned for constructions and passed on the strength of ONE good arm, so an `if` whose false arm returned `other.iter()` compiled and the window charged the wrong root. The accept twin (`borrowing_producer_every_path.saw`) runs a producer whose every leaf — a `return`, each `match` arm, an `if` nested in one — constructs with `lends self`. THE RETURN TYPE IS RESOLVED BEFORE THE PROOF, and \\\"no struct information\\\" REFUSES (codex r2 #5): a `borrows -> Alias` producer is proven against the struct the alias resolves to, a return type resolving to nothing is refused by name, and the design-63 back-conversion the producer must write is transparent — `Alias(It(source: lends self, …))` is the construction (accept twin `borrowing_rules_resolve_aliases.saw`), `Alias(other.iter())` is the call `.iter()` and is refused (`errors/borrowing_producer_alias_return.saw`, whole-body and per arm); for one revision the proof asked `get_struct_info` for the alias's own name, got None, and certified the producer |\\n@@ -753,6 +757,16 @@ Claim source: spec 6 *Send and Sync* + *Cooperative tasks*; designs 75, 88, 103,\\n | K147 | the INLINE `try EXPR catch { … }` SPLITS — a suspending call in its catch embeds, and a `break`/`continue` written there reaches the loop it names | `K147_inline_catch_splits.saw` + `coro_suspending_call_in_inline_catch.saw` + `coro_inline_catch_loop_exit_in_child_positions.saw` | SL-215, closed by design 275 U3. A `TryExpr` carrying a `catch_block` is the inline form, and nothing in the split walk ever looked at that slot: `_collect_calls`'s try/catch arm matched `TryCatchExpr` only, so a suspending call in the catch fell to the one rejector. THE SAME SLOT COST A SILENT MISCOMPILE beside the refusal — `_mark_ob_block`'s clause-2 marking lists `IfExpr`, `MatchExpr` and `TryCatchExpr` and not `TryExpr`, so a `try f() catch { break }` inside a spanning loop kept a RAW `break` that left the resume method's `while true` DISPATCH loop: the break was lost and a `continue` spun forever, at exit 0. The transform rewrites the inline form into the BLOCK form it means (`try { try EXPR } catch { B }`), so design 196 unit 3's split does the work and both faces close at once. Rows: the suspending catch, the ERROR BINDING across the suspension, the three `try` variants in one body, a routed `try(as E.Case)` beside an inline catch, and a catch that `break`s or `continue`s its loop — each against its sync twin. Each rewrite takes a FRESH error binding (`__saw_caughtN`) with the author's `error` reads renamed, because the block form's caught error travels in a frame field NAMED by the binding and two normalized catches in one body would otherwise share one field — measured as ``argument `value` expects `Alpha` but got `Beta``, and sawtracker's accept loop has exactly two. AT EVERY CHILD POSITION (codex r3 #1): the normalization walked its children by hand and stopped at anything not an `ASTNode` — `Argument` is a plain dataclass, a struct-literal field and a map entry are tuples — so an inline catch inside `id(…)`, `Pair(a: …)`, `{k: …}` or `read(&a[…])` was never rewritten and its raw `break` left the dispatcher (the code after the loop skipped, or the driver re-entering forever); it rides `ast_walk.map_nodes` now. The SAME symptom sat one mechanism over: the value-conditional lowering asked only \\\"spans a suspension\\\", so a normalized `try { } catch { break }` (and a value `if`/`match` arm that jumps) in a `let` or an argument inside a spanning loop lowered in place — `_vc_spans` asks the loop-exit clause too. The child-positions file is the matrix: every wrapper the funnel names × `break`/`continue` × `while`/collection `for`, plus the suspending catch, each against its sync twin |\\n | K148 | DF-196b's multi-error refusal is inherited whole by the inline form | `errors/inline_catch_multi_error.saw` | design 275 U3 / SL-215. A suspending `try { } catch { }` may raise only ONE error type — the catch would bind a union the split lowering cannot build — and a one-expression inline catch satisfies that by construction rather than by an exemption |\\n | K149 | the MOTIVATING CONSUMER's loop shape compiles and runs: a collection `for` whose body SUSPENDS, PROPAGATES an error, and writes a root that is not the one being iterated | `K149_sawtracker_loop_shape.saw` | SL-317's own repro, sawtracker `src/store.saw:285`. Three things at once, which is why it is the shape worth pinning rather than the minimal one; it was the clean refusal \\\"use a `while` loop over an index\\\" before U3. The io is replaced by a suspending read so the row runs in the ordinary battery — the SHAPE is the assertion, not the syscall — and the sync twin is the oracle |\\n+| K170 | a `Mutex` over a HEAP-OWNING payload holds it for its whole life and releases it exactly once, at the mutex's own end — from an rvalue, a moved local, a moved `Vector<Int>` read through `lock`, and a container of drop-counted elements | `K170_mutex_owning_payload_drops_once.saw` | SL-340. `Mutex.init` hands its by-value parameter to `UnsafeMutableInterior(...)`, and that intercepted construction was neither checked nor lowered as a transfer, so the parameter was released at the end of the init body (a use-after-free: `items[0]` read garbage) — and the cell had no drop of its own, so the payload that was not freed early was never freed at all. The two halves hid each other, which is why they land together. Every cell counts drops with a printing deinit |\\n+| K171 | a `SpinLock` over a HEAP-OWNING payload releases it exactly once, at the lock's end, from both sources | `K171_spinlock_owning_payload_drops_once.saw` | SL-340. `SpinLock.init` has `Mutex.init`'s shape and had its bug: the payload dropped at construction for an rvalue and a moved local alike, and a `Vector` of owners was read after its free (SIGSEGV) |\\n+| K172 | a `Once` over a HEAP-OWNING payload is refused at its one home, the `static` | `errors/once_owning_payload_refused_at_its_static.saw` | SL-340. `Once<T>` has no `init` and private fields, so a bare `static` is the only place one lives, and a static that owns a resource is design 149's refusal. That refusal is what keeps `Once.set`'s write through its cell pointer away from any payload with a drop to lose; the write's own placement shape is SL-378's |\\n+| K173 | a USER type following the interior-mutability idiom owns its cell's payload: an rvalue, a by-value PARAMETER, a MOVED local and a GENERIC forward without `move` each release exactly once, at the owner's end | `K173_user_cell_payload_drops_once.saw` | SL-340. The generic forward is legal without `move` (a body that forwards its parameter once duplicates nothing, design 219 wave C), so it is retired by the construction's own transfer lowering rather than by a written `move` |\\n+| K174 | a cell NESTED in another container drops as its payload does — an Optional payload, an enum payload, fixed-array elements (reverse index order) and a generic struct's field | `K174_nested_cell_drops_as_its_payload.saw` | SL-340. There is no copy cell: a container of a cell over a duplicable payload owes a declared `Copy` and deriving it is refused (the cell is `NoCopy`), so no compiler-inserted copy duplicates a cell; the retain half of the rule is K178's |\\n+| K175 | the DRIVEN twin: a `Mutex` and a user cell held across a suspension release their payloads once, and a cell built in a driven body retires the frame local it was built from | `K175_cell_across_a_suspension_drops_once.saw` | SL-340. A local that lives across a suspension drops through the frame's glue, the same glue a cell's drop has to reach through; the sync twin is the oracle |\\n+| K176 | the intercepted constructions are TRANSFERS — `UnsafeMutableInterior(x)`, an alias back-conversion `Alias(x)` and `UnsafeMemory.write(x)` refuse a NoCopy or ExplicitCopy source read out of a binding without `move`, exactly as a call argument does | `errors/interior_cell_construct_is_a_transfer.saw` + `errors/alias_construction_is_a_transfer.saw` + `errors/unsafe_memory_write_is_a_transfer.saw` | SL-340. All three checked only type compatibility, so a NoCopy parameter handed to one compiled, was released by its binding at scope exit, and stayed live in the value built from it. They are entries 21-23 of `_check_value_transfer`'s entry-point list, and codegen lowers each through `_gen_transfer_value`, the same transfer lowering every call argument takes |\\n+| K177 | the same three constructions on the ACCEPT side: a Copy-tier source is retained exactly once and a `move` retires its source | `K177_intercepted_construction_transfers.saw` | SL-340. The `Arc` strong count is the oracle: 3 inside (the caller's, the parameter's, the constructed owner's), 1 after. Before the fix each read 2, and the memory write released the payload early and then read a freed count. The write is reclaimed with a pointer-place `move` read, which drops the stored value once |\\n+| K178 | a value of a distinct ALIAS over an owning type drops as its underlying does — an rvalue, a by-value parameter, a moved local, an alias of an alias, an ExplicitCopy underlying from `move` and `.copy()`, and in an Optional, an enum payload, an array and a generic field — and a Copy-tier alias RETAINS as its underlying does, directly, through a derived `Copy` and through the automatic Copy tier's retain glue | `K178_alias_value_drops_as_its_underlying.saw` | SL-340's widening to aliases. An alias IS its underlying and has no conformance of its own, so the drop glue found nothing to run and every alias over an owning type LEAKED; a copy of a Copy-tier alias took no retain. One predicate (`_lifecycle_type`, resources.py) answers every lifecycle question for a layout-transparent wrapper and an alias alike, with the payload's or the underlying's answer |\\n+| K179 | CONTROL: a cell over a TRIVIAL payload (`Mutex<Int>`, a user counter), `Atomic(0)` and `static` locks behave exactly as before, and an all-zero static stays zerofill and never drops | `K179_trivial_cell_payloads_unchanged.saw` + `mutex_static_inline.saw` + `spinlock_basic.saw` + `interior_cell_static_placement.saw` + `static_zero_bss.saw` | SL-340. A trivial payload has no drop, so the lifecycle rule adds nothing to it; the placement half is the `ircontract` lane's |\\n \\n \\n ## Visibility and module boundaries\\ndiff --git a/examples/conformance/K141_mutex_guarded_for_is_sync.saw b/examples/conformance/K141_mutex_guarded_for_is_sync.saw\\nindex eb95f4d1..f413134b 100644\\n--- a/examples/conformance/K141_mutex_guarded_for_is_sync.saw\\n+++ b/examples/conformance/K141_mutex_guarded_for_is_sync.saw\\n@@ -1,16 +1,3 @@\\n-// XFAIL: SL-340 — `Mutex(value: move v)` at a HEAP-OWNING payload frees the\\n-// payload at construction, on MAIN and with no diagnostic: an `init`'s\\n-// by-value parameter is registered for scope-exit cleanup and the transfer\\n-// into `UnsafeMutableInterior(value)` does not retire it, so the buffer is\\n-// released at the end of the init body and the struct is returned around it.\\n-// So `items.len()` reads 3 and `items[0]` reads freed memory — the ITERATION\\n-// this row is about is downstream of that and reads the same bytes.\\n-//\\n-// The row itself is design 275 U3's ruling and is kept as written: the XPASS\\n-// flip when SL-340 lands is the validation. Its REFUSAL half —\\n-// `examples/errors/mutex_guarded_for_suspends.saw` — passes today, which is\\n-// what says the `sync` fence is intact and only the payload is broken.\\n-//\\n // K141 (design 275 U3, part (iii)): THE `Mutex` ROW IS CLOSED, NOT OPEN.\\n //\\n // `Mutex.lock<R>(&self, body: (&var T) sync -> R)` runs a `sync` body, so a\\ndiff --git a/examples/conformance/K170_mutex_owning_payload_drops_once.saw b/examples/conformance/K170_mutex_owning_payload_drops_once.saw\\nnew file mode 100644\\nindex 00000000..b7bda67f\\n--- /dev/null\\n+++ b/examples/conformance/K170_mutex_owning_payload_drops_once.saw\\n@@ -0,0 +1,89 @@\\n+// Conformance row K170 — a `Mutex` over a HEAP-OWNING payload holds it for its\\n+// whole life and releases it exactly once, at the mutex's own end (SL-340)\\n+//\\n+// `Mutex.init` hands its by-value parameter to `UnsafeMutableInterior(...)`,\\n+// a construction the compiler intercepts. That construction is a TRANSFER: the\\n+// parameter is retired by it, so the init body does not release what the\\n+// mutex now holds, and the cell drops its payload when its owner dies, so the\\n+// payload is released once rather than never.\\n+//\\n+// Every cell counts drops with a printing deinit. A payload released early\\n+// prints its drop BEFORE the reads that need it; a payload never released\\n+// prints nothing at the scope's end.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// vector sum 6\\n+// vector first 1\\n+// vector scope end\\n+// rvalue len 6\\n+// rvalue scope end\\n+// drop rvalue\\n+// local len 5\\n+// local scope end\\n+// drop local\\n+// pair count 2\\n+// pair names left right\\n+// pair scope end\\n+// drop left\\n+// drop right\\n+// end of main\\n+\\n+import std.mutex.{Mutex}\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+// The finding's own shape: a `Vector<Int>` moved in and read through `lock`.\\n+func vector_payload() {\\n+    var v: Vector<Int> = [1, 2, 3]\\n+    let guarded = Mutex(value: move v)\\n+    let sum = guarded.lock({ items in\\n+        var total = 0\\n+        for x in items.iter() { total = total + x }\\n+        total\\n+    })\\n+    print(\\\"vector sum {}\\\", sum)\\n+    print(\\\"vector first {}\\\", guarded.lock({ items in items[0] }))\\n+    print(\\\"vector scope end\\\")\\n+}\\n+\\n+func rvalue_payload() {\\n+    let guarded = Mutex(value: Noisy(name: \\\"rvalue\\\"))\\n+    print(\\\"rvalue len {}\\\", guarded.lock({ x in x.name.len() }))\\n+    print(\\\"rvalue scope end\\\")\\n+}\\n+\\n+func moved_local_payload() {\\n+    let n = Noisy(name: \\\"local\\\")\\n+    let guarded = Mutex(value: move n)\\n+    print(\\\"local len {}\\\", guarded.lock({ x in x.name.len() }))\\n+    print(\\\"local scope end\\\")\\n+}\\n+\\n+// A container of drop-counted elements: the vector's own deinit is what the\\n+// cell's drop runs, so each element prints once, in index order.\\n+struct Crowd { members: Vector<Noisy> }\\n+\\n+extension Crowd: NoCopy {}\\n+\\n+func vector_of_owners() {\\n+    var v: Vector<Noisy> = []\\n+    try! v.push(Noisy(name: \\\"left\\\"))\\n+    try! v.push(Noisy(name: \\\"right\\\"))\\n+    let guarded = Mutex(value: Crowd(members: move v))\\n+    print(\\\"pair count {}\\\", guarded.lock({ c in c.members.len() }))\\n+    let names = guarded.lock({ c in \\\"{c.members[0].name} {c.members[1].name}\\\" })\\n+    print(\\\"pair names {}\\\", names)\\n+    print(\\\"pair scope end\\\")\\n+}\\n+\\n+func main() {\\n+    vector_payload()\\n+    rvalue_payload()\\n+    moved_local_payload()\\n+    vector_of_owners()\\n+    print(\\\"end of main\\\")\\n+}\\ndiff --git a/examples/conformance/K171_spinlock_owning_payload_drops_once.saw b/examples/conformance/K171_spinlock_owning_payload_drops_once.saw\\nnew file mode 100644\\nindex 00000000..1ef0a1b8\\n--- /dev/null\\n+++ b/examples/conformance/K171_spinlock_owning_payload_drops_once.saw\\n@@ -0,0 +1,69 @@\\n+// Conformance row K171 — a `SpinLock` over a HEAP-OWNING payload holds it for\\n+// its whole life and releases it exactly once, at the lock's own end (SL-340)\\n+//\\n+// `SpinLock.init` has `Mutex.init`'s shape: its by-value parameter goes into an\\n+// `UnsafeMutableInterior(...)`, so it owes the same two things — the\\n+// construction retires the parameter, and the cell drops its payload with its\\n+// owner. K170 is the `Mutex` half.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// rvalue len 6\\n+// rvalue scope end\\n+// drop rvalue\\n+// local len 5\\n+// local scope end\\n+// drop local\\n+// vector first 4\\n+// vector count 3\\n+// vector scope end\\n+// pair names a b\\n+// pair scope end\\n+// drop a\\n+// drop b\\n+// end of main\\n+\\n+import std.spinlock.{SpinLock}\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+func rvalue_payload() {\\n+    let lock = SpinLock(value: Noisy(name: \\\"rvalue\\\"))\\n+    print(\\\"rvalue len {}\\\", lock.lock({ x in x.name.len() }))\\n+    print(\\\"rvalue scope end\\\")\\n+}\\n+\\n+func moved_local_payload() {\\n+    let n = Noisy(name: \\\"local\\\")\\n+    let lock = SpinLock(value: move n)\\n+    print(\\\"local len {}\\\", lock.lock({ x in x.name.len() }))\\n+    print(\\\"local scope end\\\")\\n+}\\n+\\n+func vector_payload() {\\n+    var v: Vector<Int> = [4, 5, 6]\\n+    let lock = SpinLock(value: move v)\\n+    print(\\\"vector first {}\\\", lock.lock({ items in items[0] }))\\n+    print(\\\"vector count {}\\\", lock.lock({ items in items.len() }))\\n+    print(\\\"vector scope end\\\")\\n+}\\n+\\n+func vector_of_owners() {\\n+    var v: Vector<Noisy> = []\\n+    try! v.push(Noisy(name: \\\"a\\\"))\\n+    try! v.push(Noisy(name: \\\"b\\\"))\\n+    let lock = SpinLock(value: move v)\\n+    print(\\\"pair names {}\\\", lock.lock({ items in \\\"{items[0].name} {items[1].name}\\\" }))\\n+    print(\\\"pair scope end\\\")\\n+}\\n+\\n+func main() {\\n+    rvalue_payload()\\n+    moved_local_payload()\\n+    vector_payload()\\n+    vector_of_owners()\\n+    print(\\\"end of main\\\")\\n+}\\ndiff --git a/examples/conformance/K173_user_cell_payload_drops_once.saw b/examples/conformance/K173_user_cell_payload_drops_once.saw\\nnew file mode 100644\\nindex 00000000..707695ad\\n--- /dev/null\\n+++ b/examples/conformance/K173_user_cell_payload_drops_once.saw\\n@@ -0,0 +1,108 @@\\n+// Conformance row K173 — a USER type following the interior-mutability idiom\\n+// owns its cell's payload: whatever the source, the payload is released\\n+// exactly once, at the owner's end (SL-340)\\n+//\\n+// `UnsafeMutableInterior<T>` is layout-transparent: it occupies exactly its\\n+// `T`, so a cell field is the payload's own storage and the cell drops as its\\n+// payload drops. Its construction is a transfer, so a by-value parameter\\n+// handed to it is retired there and not released again at the end of the\\n+// function that built the cell.\\n+//\\n+// Four sources, one rule: an rvalue, a by-value PARAMETER (the `Mutex.init`\\n+// shape), a MOVED local, and a generic body forwarding its parameter — which\\n+// is legal without `move`, because a generic body that forwards a value once\\n+// duplicates nothing.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// rvalue built\\n+// rvalue reads 6\\n+// rvalue scope end\\n+// drop rvalue\\n+// param built\\n+// param reads 5\\n+// param scope end\\n+// drop param\\n+// local built\\n+// local reads 5\\n+// local scope end\\n+// drop local\\n+// generic built\\n+// generic reads 7\\n+// generic scope end\\n+// drop generic\\n+// end of main\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+struct Holder { cell: UnsafeMutableInterior<Noisy> }\\n+\\n+extension Holder: NoCopy {}\\n+\\n+extension Holder {\\n+    func name_len(&self) unsafe -> Int { self.cell.ptr()[0].name.len() }\\n+}\\n+\\n+struct Wrap<T> { cell: UnsafeMutableInterior<T> }\\n+\\n+extension Wrap<T>: NoCopy {}\\n+\\n+extension Wrap<T> {\\n+    func at(&self) unsafe -> UnsafePointer<T> { self.cell.ptr() }\\n+}\\n+\\n+func from_rvalue() unsafe -> Holder {\\n+    Holder(cell: UnsafeMutableInterior(Noisy(name: \\\"rvalue\\\")))\\n+}\\n+\\n+func from_param(value: Noisy) unsafe -> Holder {\\n+    Holder(cell: UnsafeMutableInterior(move value))\\n+}\\n+\\n+func from_local() unsafe -> Holder {\\n+    let n = Noisy(name: \\\"local\\\")\\n+    Holder(cell: UnsafeMutableInterior(move n))\\n+}\\n+\\n+func forward<T>(value: T) unsafe -> Wrap<T> {\\n+    Wrap<T>(cell: UnsafeMutableInterior(value))\\n+}\\n+\\n+func rvalue_scope() {\\n+    let h = from_rvalue()\\n+    print(\\\"rvalue built\\\")\\n+    print(\\\"rvalue reads {}\\\", h.name_len())\\n+    print(\\\"rvalue scope end\\\")\\n+}\\n+\\n+func param_scope() {\\n+    let h = from_param(Noisy(name: \\\"param\\\"))\\n+    print(\\\"param built\\\")\\n+    print(\\\"param reads {}\\\", h.name_len())\\n+    print(\\\"param scope end\\\")\\n+}\\n+\\n+func local_scope() {\\n+    let h = from_local()\\n+    print(\\\"local built\\\")\\n+    print(\\\"local reads {}\\\", h.name_len())\\n+    print(\\\"local scope end\\\")\\n+}\\n+\\n+func generic_scope() unsafe {\\n+    let w = forward(Noisy(name: \\\"generic\\\"))\\n+    print(\\\"generic built\\\")\\n+    print(\\\"generic reads {}\\\", w.at()[0].name.len())\\n+    print(\\\"generic scope end\\\")\\n+}\\n+\\n+func main() {\\n+    rvalue_scope()\\n+    param_scope()\\n+    local_scope()\\n+    generic_scope()\\n+    print(\\\"end of main\\\")\\n+}\\ndiff --git a/examples/conformance/K174_nested_cell_drops_as_its_payload.saw b/examples/conformance/K174_nested_cell_drops_as_its_payload.saw\\nnew file mode 100644\\nindex 00000000..d94e3697\\n--- /dev/null\\n+++ b/examples/conformance/K174_nested_cell_drops_as_its_payload.saw\\n@@ -0,0 +1,99 @@\\n+// Conformance row K174 — a cell NESTED inside another container drops as its\\n+// payload does, at every position a cell can sit (SL-340)\\n+//\\n+// The drop glue reaches a cell through its container's own glue: an Optional\\n+// payload, an enum payload, a fixed-array element, a generic struct's field.\\n+// Each is the cell's own storage, and the cell is its payload's, so each\\n+// payload is released exactly once, when the container dies.\\n+//\\n+// There is no COPY cell here: a container of a cell over a duplicable payload\\n+// owes a declared `Copy`, and deriving one is refused because the cell itself\\n+// is `NoCopy`, so no compiler-inserted copy ever duplicates a cell. The\\n+// retain half of the lifecycle rule is exercised through an alias instead\\n+// (K178).\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// optional reads 3\\n+// optional scope end\\n+// drop opt\\n+// enum reads 4\\n+// enum scope end\\n+// drop enum\\n+// array reads 5 6\\n+// array scope end\\n+// drop second\\n+// drop first\\n+// generic reads 2\\n+// generic scope end\\n+// drop g0\\n+// drop g1\\n+// end of main\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+struct OptionalCell { slot: UnsafeMutableInterior<Noisy>? }\\n+\\n+extension OptionalCell: NoCopy {}\\n+\\n+enum Held {\\n+    case Full(cell: UnsafeMutableInterior<Noisy>),\\n+    case Empty\\n+}\\n+\\n+extension Held: NoCopy {}\\n+\\n+struct Wrap<T> { cell: UnsafeMutableInterior<T> }\\n+\\n+extension Wrap<T>: NoCopy {}\\n+\\n+// Matching through a reference borrows the enum, so the payload stays where\\n+// it is and dies with the enum rather than with the arm.\\n+func held_len(held: &Held) unsafe -> Int {\\n+    match held {\\n+        case Full(cell) -> cell.ptr()[0].name.len(),\\n+        case Empty -> 0\\n+    }\\n+}\\n+\\n+func optional_scope() unsafe {\\n+    let o = OptionalCell(slot: UnsafeMutableInterior(Noisy(name: \\\"opt\\\")))\\n+    print(\\\"optional reads {}\\\", o.slot!.ptr()[0].name.len())\\n+    print(\\\"optional scope end\\\")\\n+}\\n+\\n+func enum_scope() unsafe {\\n+    let e = Held.Full(cell: UnsafeMutableInterior(Noisy(name: \\\"enum\\\")))\\n+    print(\\\"enum reads {}\\\", held_len(&e))\\n+    print(\\\"enum scope end\\\")\\n+}\\n+\\n+func array_scope() unsafe {\\n+    let cells: [UnsafeMutableInterior<Noisy>; 2] = [\\n+        UnsafeMutableInterior(Noisy(name: \\\"first\\\")),\\n+        UnsafeMutableInterior(Noisy(name: \\\"second\\\")),\\n+    ]\\n+    print(\\\"array reads {} {}\\\", cells[0].ptr()[0].name.len(),\\n+          cells[1].ptr()[0].name.len())\\n+    print(\\\"array scope end\\\")\\n+}\\n+\\n+func generic_scope() unsafe {\\n+    var v: Vector<Noisy> = []\\n+    try! v.push(Noisy(name: \\\"g0\\\"))\\n+    try! v.push(Noisy(name: \\\"g1\\\"))\\n+    let w = Wrap(cell: UnsafeMutableInterior(move v))\\n+    print(\\\"generic reads {}\\\", w.cell.ptr()[0].len())\\n+    print(\\\"generic scope end\\\")\\n+}\\n+\\n+func main() {\\n+    optional_scope()\\n+    enum_scope()\\n+    array_scope()\\n+    generic_scope()\\n+    print(\\\"end of main\\\")\\n+}\\ndiff --git a/examples/conformance/K175_cell_across_a_suspension_drops_once.saw b/examples/conformance/K175_cell_across_a_suspension_drops_once.saw\\nnew file mode 100644\\nindex 00000000..96e496db\\n--- /dev/null\\n+++ b/examples/conformance/K175_cell_across_a_suspension_drops_once.saw\\n@@ -0,0 +1,109 @@\\n+// Conformance row K175 — the DRIVEN twin: a `Mutex`, a user cell and an alias\\n+// value held across a suspension release their payloads exactly once, and a\\n+// cell or alias built in a driven body retires the frame local it was built\\n+// from (SL-340)\\n+//\\n+// A local that lives across a suspension is a frame field, so its drop is the\\n+// frame's drop glue rather than a scope-exit call — the same glue a cell's\\n+// drop has to reach through. The sync twin is the oracle: both halves print\\n+// the same sequence.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// sync mutex reads 6\\n+// sync mutex scope end\\n+// drop sync-m\\n+// driven mutex reads 8\\n+// driven mutex scope end\\n+// drop driven-m\\n+// sync cell reads 6\\n+// sync cell scope end\\n+// drop sync-c\\n+// driven cell reads 8\\n+// driven cell scope end\\n+// drop driven-c\\n+// sync alias built\\n+// sync alias scope end\\n+// drop sync-a\\n+// driven alias built\\n+// driven alias scope end\\n+// drop driven-a\\n+// end of main\\n+\\n+import std.mutex.{Mutex}\\n+import std.task.{yield_now}\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+type Tag = Noisy\\n+\\n+struct Holder { cell: UnsafeMutableInterior<Noisy> }\\n+\\n+extension Holder: NoCopy {}\\n+\\n+extension Holder {\\n+    func name_len(&self) unsafe -> Int { self.cell.ptr()[0].name.len() }\\n+}\\n+\\n+func sync_mutex() {\\n+    let n = Noisy(name: \\\"sync-m\\\")\\n+    let guarded = Mutex(value: move n)\\n+    print(\\\"sync mutex reads {}\\\", guarded.lock({ x in x.name.len() }))\\n+    print(\\\"sync mutex scope end\\\")\\n+}\\n+\\n+func driven_mutex() {\\n+    let n = Noisy(name: \\\"driven-m\\\")\\n+    yield_now()\\n+    let guarded = Mutex(value: move n)\\n+    yield_now()\\n+    print(\\\"driven mutex reads {}\\\", guarded.lock({ x in x.name.len() }))\\n+    print(\\\"driven mutex scope end\\\")\\n+}\\n+\\n+func sync_cell() unsafe {\\n+    let n = Noisy(name: \\\"sync-c\\\")\\n+    let h = Holder(cell: UnsafeMutableInterior(move n))\\n+    print(\\\"sync cell reads {}\\\", h.name_len())\\n+    print(\\\"sync cell scope end\\\")\\n+}\\n+\\n+// The cell is built from a FRAME local: the construction's `move` is what\\n+// hands the frame's value to the cell.\\n+func driven_cell() unsafe {\\n+    let n = Noisy(name: \\\"driven-c\\\")\\n+    yield_now()\\n+    let h = Holder(cell: UnsafeMutableInterior(move n))\\n+    yield_now()\\n+    print(\\\"driven cell reads {}\\\", h.name_len())\\n+    print(\\\"driven cell scope end\\\")\\n+}\\n+\\n+func sync_alias() {\\n+    let n = Noisy(name: \\\"sync-a\\\")\\n+    let t = Tag(move n)\\n+    print(\\\"sync alias built\\\")\\n+    print(\\\"sync alias scope end\\\")\\n+}\\n+\\n+func driven_alias() {\\n+    let n = Noisy(name: \\\"driven-a\\\")\\n+    yield_now()\\n+    let t = Tag(move n)\\n+    yield_now()\\n+    print(\\\"driven alias built\\\")\\n+    print(\\\"driven alias scope end\\\")\\n+}\\n+\\n+func main() {\\n+    sync_mutex()\\n+    __saw_drive(driven_mutex())\\n+    sync_cell()\\n+    __saw_drive(driven_cell())\\n+    sync_alias()\\n+    __saw_drive(driven_alias())\\n+    print(\\\"end of main\\\")\\n+}\\ndiff --git a/examples/conformance/K177_intercepted_construction_transfers.saw b/examples/conformance/K177_intercepted_construction_transfers.saw\\nnew file mode 100644\\nindex 00000000..13610fcb\\n--- /dev/null\\n+++ b/examples/conformance/K177_intercepted_construction_transfers.saw\\n@@ -0,0 +1,92 @@\\n+// Conformance row K177 — the intercepted constructions are TRANSFERS on the\\n+// accept side too: a Copy-tier source read out of a binding is retained\\n+// exactly once, and a `move` retires its source (SL-340)\\n+//\\n+// Three constructions the compiler intercepts rather than calling: the\\n+// interior cell `UnsafeMutableInterior(x)`, a distinct alias's back-conversion\\n+// `Handle(x)`, and `UnsafeMemory.write(x)`. Each hands its argument a new\\n+// owner, so each takes the checkpoint every call argument takes.\\n+//\\n+// The `Arc` strong count is the oracle. Inside each function three owners\\n+// hold a reference — the caller's binding, the by-value parameter, and the\\n+// value the construction built — so the count reads 3, and it is back to 1\\n+// once the function has released its two. A construction that took no retain\\n+// reads 2 and releases the payload one time too many.\\n+//\\n+// The memory view is reclaimed with a pointer-place `move` read, which hands\\n+// the stored value back to a binding that drops it once.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// cell count 3\\n+// cell count after 1\\n+// alias count 3\\n+// alias count after 1\\n+// write count 3\\n+// write count after 1\\n+// moved back stored\\n+// drop stored\\n+// end of main\\n+// drop res 1\\n+\\n+struct Res { id: Int }\\n+\\n+extension Res: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop res {}\\\", self.id) }\\n+}\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+struct Shared { cell: UnsafeMutableInterior<Arc<Res>> }\\n+\\n+extension Shared: NoCopy {}\\n+\\n+type Handle = Arc<Res>\\n+\\n+func cell_retains(a: Arc<Res>) unsafe {\\n+    let s = Shared(cell: UnsafeMutableInterior(a))\\n+    print(\\\"cell count {}\\\", a.strong_count())\\n+}\\n+\\n+func alias_retains(a: Arc<Res>) {\\n+    let h = Handle(a)\\n+    print(\\\"alias count {}\\\", a.strong_count())\\n+}\\n+\\n+// The raw slot is sized past the viewed type on purpose: it is storage the\\n+// view borrows, and an `Arc` is two words.\\n+func write_retains(a: Arc<Res>) unsafe {\\n+    var slot: [Int; 4] = [0; 4]\\n+    let addr = ((&slot) as UnsafePointer<Int>) as Int\\n+    let view = UnsafeMemory<Arc<Res>, Normal>(addr)\\n+    view.write(a)\\n+    let stored = addr as UnsafePointer<Arc<Res>>\\n+    let back = move stored[0]\\n+    print(\\\"write count {}\\\", a.strong_count())\\n+}\\n+\\n+func write_moves() unsafe {\\n+    var slot: [Int; 4] = [0; 4]\\n+    let addr = ((&slot) as UnsafePointer<Int>) as Int\\n+    let view = UnsafeMemory<Noisy, Normal>(addr)\\n+    let n = Noisy(name: \\\"stored\\\")\\n+    view.write(move n)\\n+    let stored = addr as UnsafePointer<Noisy>\\n+    let back = move stored[0]\\n+    print(\\\"moved back {}\\\", back.name)\\n+}\\n+\\n+func main() {\\n+    let a = try! Arc(value: Res(id: 1))\\n+    cell_retains(a.copy())\\n+    print(\\\"cell count after {}\\\", a.strong_count())\\n+    alias_retains(a.copy())\\n+    print(\\\"alias count after {}\\\", a.strong_count())\\n+    write_retains(a.copy())\\n+    print(\\\"write count after {}\\\", a.strong_count())\\n+    write_moves()\\n+    print(\\\"end of main\\\")\\n+}\\ndiff --git a/examples/conformance/K178_alias_value_drops_as_its_underlying.saw b/examples/conformance/K178_alias_value_drops_as_its_underlying.saw\\nnew file mode 100644\\nindex 00000000..de8fbb25\\n--- /dev/null\\n+++ b/examples/conformance/K178_alias_value_drops_as_its_underlying.saw\\n@@ -0,0 +1,212 @@\\n+// Conformance row K178 — a value of a distinct alias over an OWNING type\\n+// drops as its underlying does: exactly once, at its owner's end, whatever\\n+// the source and wherever it is held (SL-340)\\n+//\\n+// An alias IS its underlying — the distinction is the typechecker's alone —\\n+// so an alias has no drop of its own and its underlying's stands for it. Its\\n+// back-conversion `Tag(x)` is a transfer, so a `move`d local or parameter is\\n+// retired there rather than released a second time.\\n+//\\n+// Sources: an rvalue, a by-value PARAMETER, a MOVED local, an alias of an\\n+// alias, and an ExplicitCopy underlying built from `move` and from `.copy()`.\\n+// Positions: an Optional payload, an enum payload, a fixed-array element and a\\n+// generic struct's field.\\n+//\\n+// The last three cells are the RETAIN side of the same rule. A Copy-tier\\n+// alias copies as its underlying does, so `let b = a` over an `Arc` alias\\n+// reads a strong count of 2 and the payload drops once, after both; a derived\\n+// `Copy` over an alias field does the same; and a struct on the automatic Copy\\n+// tier retains its alias field, so a heap `String` shared by two copies is\\n+// released once per reference and never twice.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// rvalue 6\\n+// rvalue scope end\\n+// drop rvalue\\n+// param 5\\n+// param scope end\\n+// drop param\\n+// local 5\\n+// local scope end\\n+// drop local\\n+// chain 5\\n+// chain scope end\\n+// drop chain\\n+// bags 2 2\\n+// bags scope end\\n+// drop b0\\n+// drop b1\\n+// drop b0\\n+// drop b1\\n+// optional 3\\n+// optional scope end\\n+// drop opt\\n+// enum built\\n+// enum scope end\\n+// drop enum\\n+// array 5 6\\n+// array scope end\\n+// drop second\\n+// drop first\\n+// generic 3\\n+// generic scope end\\n+// drop gen\\n+// arc copy 2\\n+// arc scope end\\n+// drop res 2\\n+// pair copy 2\\n+// pair scope end\\n+// drop res 3\\n+// named copy 7 7\\n+// named scope end\\n+// end of main\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+// The duplicable twin, so the ExplicitCopy underlying can be built from a\\n+// `.copy()` as well as a `move`.\\n+struct Tick { name: String }\\n+\\n+@synthesize\\n+extension Tick: ExplicitCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+struct Res { id: Int }\\n+\\n+extension Res: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop res {}\\\", self.id) }\\n+}\\n+\\n+type Tag = Noisy\\n+type Label = Tag\\n+type Bag = Vector<Tick>\\n+type Handle = Arc<Res>\\n+type Name = String\\n+\\n+// A declared Copy over an alias field: the derived `copy()` copies the field\\n+// as its underlying does.\\n+struct Pair { h: Handle }\\n+\\n+@synthesize\\n+extension Pair: Copy {}\\n+\\n+// The automatic Copy tier over an alias field: a copy retains the field\\n+// through the structural retain glue.\\n+struct Named { n: Name }\\n+\\n+enum Held {\\n+    case Full(tag: Tag),\\n+    case Empty\\n+}\\n+\\n+extension Held: NoCopy {}\\n+\\n+struct Wrap<T> { value: T }\\n+\\n+extension Wrap<T>: NoCopy {}\\n+\\n+func wrap(value: Noisy) -> Tag {\\n+    Tag(move value)\\n+}\\n+\\n+func rvalue_scope() {\\n+    let t = Tag(Noisy(name: \\\"rvalue\\\"))\\n+    print(\\\"rvalue {}\\\", t.name.len())\\n+    print(\\\"rvalue scope end\\\")\\n+}\\n+\\n+func param_scope() {\\n+    let t = wrap(Noisy(name: \\\"param\\\"))\\n+    print(\\\"param {}\\\", t.name.len())\\n+    print(\\\"param scope end\\\")\\n+}\\n+\\n+func local_scope() {\\n+    let n = Noisy(name: \\\"local\\\")\\n+    let t = Tag(move n)\\n+    print(\\\"local {}\\\", t.name.len())\\n+    print(\\\"local scope end\\\")\\n+}\\n+\\n+func chain_scope() {\\n+    let l = Label(Tag(Noisy(name: \\\"chain\\\")))\\n+    print(\\\"chain {}\\\", l.name.len())\\n+    print(\\\"chain scope end\\\")\\n+}\\n+\\n+func bags_scope() {\\n+    var v: Vector<Tick> = []\\n+    try! v.push(Tick(name: \\\"b0\\\"))\\n+    try! v.push(Tick(name: \\\"b1\\\"))\\n+    let copied = Bag(v.copy())\\n+    let moved = Bag(move v)\\n+    print(\\\"bags {} {}\\\", copied.len(), moved.len())\\n+    print(\\\"bags scope end\\\")\\n+}\\n+\\n+func optional_scope() {\\n+    let o: Tag? = Tag(Noisy(name: \\\"opt\\\"))\\n+    print(\\\"optional {}\\\", o!.name.len())\\n+    print(\\\"optional scope end\\\")\\n+}\\n+\\n+func enum_scope() {\\n+    let e = Held.Full(tag: Tag(Noisy(name: \\\"enum\\\")))\\n+    print(\\\"enum built\\\")\\n+    print(\\\"enum scope end\\\")\\n+}\\n+\\n+func array_scope() {\\n+    let tags: [Tag; 2] = [Tag(Noisy(name: \\\"first\\\")), Tag(Noisy(name: \\\"second\\\"))]\\n+    print(\\\"array {} {}\\\", tags[0].name.len(), tags[1].name.len())\\n+    print(\\\"array scope end\\\")\\n+}\\n+\\n+func generic_scope() {\\n+    let w = Wrap<Tag>(value: Tag(Noisy(name: \\\"gen\\\")))\\n+    print(\\\"generic {}\\\", w.value.name.len())\\n+    print(\\\"generic scope end\\\")\\n+}\\n+\\n+func arc_scope() {\\n+    let a = Handle(try! Arc(value: Res(id: 2)))\\n+    let b = a\\n+    print(\\\"arc copy {}\\\", b.strong_count())\\n+    print(\\\"arc scope end\\\")\\n+}\\n+\\n+func pair_scope() {\\n+    let p = Pair(h: Handle(try! Arc(value: Res(id: 3))))\\n+    let q = p\\n+    print(\\\"pair copy {}\\\", q.h.strong_count())\\n+    print(\\\"pair scope end\\\")\\n+}\\n+\\n+func named_scope() {\\n+    let k = 42\\n+    let a = Named(n: Name(\\\"heap-{k}\\\"))\\n+    let b = a\\n+    print(\\\"named copy {} {}\\\", a.n.len(), b.n.len())\\n+    print(\\\"named scope end\\\")\\n+}\\n+\\n+func main() {\\n+    rvalue_scope()\\n+    param_scope()\\n+    local_scope()\\n+    chain_scope()\\n+    bags_scope()\\n+    optional_scope()\\n+    enum_scope()\\n+    array_scope()\\n+    generic_scope()\\n+    arc_scope()\\n+    pair_scope()\\n+    named_scope()\\n+    print(\\\"end of main\\\")\\n+}\\ndiff --git a/examples/conformance/K179_trivial_cell_payloads_unchanged.saw b/examples/conformance/K179_trivial_cell_payloads_unchanged.saw\\nnew file mode 100644\\nindex 00000000..844afc7b\\n--- /dev/null\\n+++ b/examples/conformance/K179_trivial_cell_payloads_unchanged.saw\\n@@ -0,0 +1,55 @@\\n+// Conformance row K179 — CONTROL: a cell over a TRIVIAL payload, `Atomic`, and\\n+// a `static` lock are unchanged by the owning-payload rules (SL-340)\\n+//\\n+// A cell drops as its payload drops, and a trivial payload has no drop, so a\\n+// `Mutex<Int>`, an `Atomic(0)` and a user cell over an `Int` carry no drop\\n+// glue for their payload and behave exactly as they did. A `static` never\\n+// drops at all, and an all-zero one is still zerofill — the placement half is\\n+// `mutex_static_inline.saw`, `spinlock_basic.saw`,\\n+// `interior_cell_static_placement.saw` and `static_zero_bss.saw`, plus the\\n+// `ircontract` lane.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// mutex 42\\n+// atomic 5\\n+// counter 3\\n+// static mutex 7\\n+// static spinlock 9\\n+// end of main\\n+\\n+import std.mutex.{Mutex}\\n+import std.spinlock.{SpinLock}\\n+\\n+struct Counter { cell: UnsafeMutableInterior<Int> }\\n+\\n+extension Counter {\\n+    init(start: Int) unsafe -> Counter {\\n+        Counter(cell: UnsafeMutableInterior(start))\\n+    }\\n+    func bump(&self) unsafe { self.cell.ptr()[0] = self.cell.ptr()[0] + 1 }\\n+    func get(&self) unsafe -> Int { self.cell.ptr()[0] }\\n+}\\n+\\n+static REGISTRY: Mutex<Int>\\n+static TOTALS: SpinLock<Int>\\n+\\n+func main() {\\n+    let m = Mutex(value: 40)\\n+    m.lock({ n in n = n + 2 })\\n+    print(\\\"mutex {}\\\", m.lock({ n in n }))\\n+\\n+    let a = Atomic(0)\\n+    let _ = a.fetch_add(5)\\n+    print(\\\"atomic {}\\\", a.load())\\n+\\n+    let c = Counter(start: 1)\\n+    c.bump()\\n+    c.bump()\\n+    print(\\\"counter {}\\\", c.get())\\n+\\n+    REGISTRY.lock({ n in n = n + 7 })\\n+    print(\\\"static mutex {}\\\", REGISTRY.lock({ n in n }))\\n+    TOTALS.lock({ n in n = n + 9 })\\n+    print(\\\"static spinlock {}\\\", TOTALS.lock({ n in n }))\\n+    print(\\\"end of main\\\")\\n+}\\ndiff --git a/examples/errors/alias_construction_is_a_transfer.saw b/examples/errors/alias_construction_is_a_transfer.saw\\nnew file mode 100644\\nindex 00000000..26079122\\n--- /dev/null\\n+++ b/examples/errors/alias_construction_is_a_transfer.saw\\n@@ -0,0 +1,39 @@\\n+// Conformance row K176, the alias family — a distinct alias's back-conversion\\n+// `Tag(x)` is a TRANSFER, so an owning source read out of a binding without\\n+// `move` is refused exactly as a call argument is (SL-340)\\n+//\\n+// A NoCopy local, a NoCopy by-value parameter, and an ExplicitCopy local, each\\n+// converted with no `move`.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: cannot copy value of type `Noisy` which implements NoCopy\\n+// EXPECT-ERROR-CONTAINS: cannot copy value of type `Noisy` which implements NoCopy\\n+// EXPECT-ERROR-CONTAINS: which implements ExplicitCopy\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+type Tag = Noisy\\n+type Bag = Vector<Int>\\n+\\n+func from_local() -> Tag {\\n+    let n = Noisy(name: \\\"local\\\")\\n+    Tag(n)                                  // ROW: NoCopy local\\n+}\\n+\\n+func from_param(value: Noisy) -> Tag {\\n+    Tag(value)                              // ROW: NoCopy parameter\\n+}\\n+\\n+func from_explicit() -> Bag {\\n+    var v: Vector<Int> = [1, 2, 3]\\n+    Bag(v)                                  // ROW: ExplicitCopy local\\n+}\\n+\\n+func main() {\\n+    let a = from_local()\\n+    let b = from_param(Noisy(name: \\\"param\\\"))\\n+    let c = from_explicit()\\n+}\\ndiff --git a/examples/errors/interior_cell_construct_is_a_transfer.saw b/examples/errors/interior_cell_construct_is_a_transfer.saw\\nnew file mode 100644\\nindex 00000000..c010058f\\n--- /dev/null\\n+++ b/examples/errors/interior_cell_construct_is_a_transfer.saw\\n@@ -0,0 +1,37 @@\\n+// Conformance row K176, the cell family — `UnsafeMutableInterior(x)` is a\\n+// TRANSFER, so an owning source read out of a binding without `move` is\\n+// refused exactly as a call argument is (SL-340)\\n+//\\n+// A NoCopy by-value parameter (the `Mutex.init` shape) and an ExplicitCopy\\n+// local, each handed to the cell with no `move`.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: cannot copy value of type `Noisy` which implements NoCopy\\n+// EXPECT-ERROR-CONTAINS: which implements ExplicitCopy\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+struct Holder { cell: UnsafeMutableInterior<Noisy> }\\n+\\n+extension Holder: NoCopy {}\\n+\\n+struct Bag { cell: UnsafeMutableInterior<Vector<Int>> }\\n+\\n+extension Bag: NoCopy {}\\n+\\n+func from_param(value: Noisy) unsafe -> Holder {\\n+    Holder(cell: UnsafeMutableInterior(value))          // ROW: NoCopy parameter\\n+}\\n+\\n+func from_local() unsafe -> Bag {\\n+    var v: Vector<Int> = [1, 2, 3]\\n+    Bag(cell: UnsafeMutableInterior(v))                 // ROW: ExplicitCopy local\\n+}\\n+\\n+func main() {\\n+    let h = from_param(Noisy(name: \\\"p\\\"))\\n+    let b = from_local()\\n+}\\ndiff --git a/examples/errors/once_owning_payload_refused_at_its_static.saw b/examples/errors/once_owning_payload_refused_at_its_static.saw\\nnew file mode 100644\\nindex 00000000..13ae57bf\\n--- /dev/null\\n+++ b/examples/errors/once_owning_payload_refused_at_its_static.saw\\n@@ -0,0 +1,21 @@\\n+// Conformance row K172 — a `Once` over a HEAP-OWNING payload is refused at its\\n+// one home, the `static` (SL-340)\\n+//\\n+// `Once<T>` has no `init`: the unset state is the zero pattern, so a bare\\n+// `static` is the only place one lives, and its private fields keep a\\n+// memberwise construction out of every other module. A static is immortal and\\n+// never runs deinit, so an owning payload there would be a destructor that is\\n+// written and never runs. That refusal is what keeps `Once.set`'s write\\n+// through its cell pointer away from any payload with a drop to lose.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: which owns a resource (Deinit); statics are immortal\\n+\\n+import std.once.{Once}\\n+\\n+static SEEDS: Once<Vector<Int>>\\n+\\n+func main() {\\n+    var v: Vector<Int> = [1, 2, 3]\\n+    SEEDS.set(move v)\\n+    print(\\\"{}\\\", SEEDS.get().len())\\n+}\\ndiff --git a/examples/errors/unsafe_memory_write_is_a_transfer.saw b/examples/errors/unsafe_memory_write_is_a_transfer.saw\\nnew file mode 100644\\nindex 00000000..51fd199b\\n--- /dev/null\\n+++ b/examples/errors/unsafe_memory_write_is_a_transfer.saw\\n@@ -0,0 +1,22 @@\\n+// Conformance row K176, the `UnsafeMemory.write` family — writing an owning\\n+// value into a memory view is a TRANSFER, like the placement write\\n+// `ptr[i] = move v`, so a NoCopy source read out of a binding without `move`\\n+// is refused (SL-340)\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: cannot copy value of type `Noisy` which implements NoCopy\\n+\\n+struct Noisy { name: String }\\n+\\n+extension Noisy: NoCopy {\\n+    func deinit(&var self) { print(\\\"drop {}\\\", self.name) }\\n+}\\n+\\n+func store(addr: Int) unsafe {\\n+    let n = Noisy(name: \\\"stored\\\")\\n+    let view = UnsafeMemory<Noisy, Normal>(addr)\\n+    view.write(n)                                       // ROW: NoCopy local\\n+}\\n+\\n+func main() {\\n+    store(0)\\n+}\\ndiff --git a/sawc/codegen/calls.py b/sawc/codegen/calls.py\\nindex 0b8d40d0..7c09c277 100644\\n--- a/sawc/codegen/calls.py\\n+++ b/sawc/codegen/calls.py\\n@@ -478,9 +478,11 @@ class CallsMixin:\\n             return self.builder.insert_value(cell, val, 0, name=\\\"atomic_new\\\")\\n \\n         # Interior-cell construction (design 186): the cell IS its `T` (it is\\n-        # layout-transparent), so wrapping a value emits the value.\\n+        # layout-transparent), so wrapping a value emits the value — lowered as\\n+        # the transfer the checker judged it, so the source is retired or\\n+        # retained.\\n         if expr.is_interior_cell_construct:\\n-            return self._generate_expression(expr.arguments[0].value)\\n+            return self._gen_transfer_value(expr.arguments[0].value)\\n \\n         # UnsafeMemory construction (design 46): the value IS the address (i64).\\n         if expr.is_unsafe_mem_construct:\\n@@ -488,12 +490,13 @@ class CallsMixin:\\n \\n         # Distinct alias construction (design 63): `UserId(42)`. An alias IS its\\n         # underlying — the distinction is the typechecker's alone — so there is\\n-        # no conversion to emit, only the operand. The width coercion is the\\n-        # same one a call argument gets: a bare literal arrives as the platform\\n-        # word and an alias over a fixed-width underlying needs it narrowed.\\n+        # no conversion to emit, only the operand, lowered as a transfer. The\\n+        # width coercion is the same one a call argument gets: a bare literal\\n+        # arrives as the platform word and an alias over a fixed-width\\n+        # underlying needs it narrowed.\\n         alias_name = expr.alias_construction\\n         if alias_name is not None:\\n-            value = self._generate_expression(expr.arguments[0].value)\\n+            value = self._gen_transfer_value(expr.arguments[0].value)\\n             target = self._get_llvm_type(\\n                 SawType(TypeKind.STRUCT, struct_name=alias_name))\\n             if (isinstance(target, ir.IntType)\\n@@ -2648,7 +2651,8 @@ class CallsMixin:\\n                 if volatile:\\n                     ld.volatile = True\\n                 return ld\\n-            val = self._generate_expression(expr.arguments[0].value)\\n+            # A placement write: the memory takes ownership (SL-340).\\n+            val = self._gen_transfer_value(expr.arguments[0].value)\\n             # Coerce an Int-literal value to the register width (Int -> UInt32\\n             # etc). Through the design-195 funnel, so a WIDEN extends by the\\n             # written value's own signedness — this arm used to `sext`\\ndiff --git a/sawc/codegen/resources.py b/sawc/codegen/resources.py\\nindex ff29921d..32d1e479 100644\\n--- a/sawc/codegen/resources.py\\n+++ b/sawc/codegen/resources.py\\n@@ -20,6 +20,7 @@ from ast_nodes import (SawType, TypeKind, MoveExpr, Identifier, MemberAccess,\\n                        TupleLiteral, ArrayLiteral, MapLiteral, SetLiteral,\\n                        Expression, ForLoop,\\n                        PRIMITIVE_EXT_KINDS)\\n+from type_identity import is_layout_transparent\\n from .mangle import mangle_type\\n \\n \\n@@ -169,6 +170,44 @@ class ResourcesMixin:\\n         t = self.variable_types.get(name)\\n         return t is not None and t.kind == TypeKind.REFERENCE\\n \\n+    # A chain of aliases and transparent wrappers is a handful of links deep;\\n+    # the bound only stops a malformed alias cycle from looping.\\n+    _LIFECYCLE_CHAIN_LIMIT = 32\\n+\\n+    def _lifecycle_type(self, saw_type: Optional[SawType]) -> Optional[SawType]:\\n+        \\\"\\\"\\\"The type whose lifecycle stands for `saw_type`'s.\\n+\\n+        A layout-transparent wrapper (the interior cell, the MMIO markers)\\n+        occupies exactly its payload, and a distinct alias IS its underlying:\\n+        neither has a drop, retain or copy of its own, so each answers every\\n+        lifecycle question with that type's answer, at the same address. One\\n+        answer here is what keeps the drop, retain and copy glue from\\n+        disagreeing about what a value owns (SL-340).\\n+\\n+        ENTRY POINTS:\\n+          `_get_cleanup_behavior`\\n+          `_needs_cleanup`\\n+          `_emit_drop_at`\\n+          `_emit_retain_at`\\n+          `_emit_release_at`\\n+          `_emit_copy_value`\\n+        \\\"\\\"\\\"\\n+        for _ in range(self._LIFECYCLE_CHAIN_LIMIT):\\n+            if saw_type is None or saw_type.kind != TypeKind.STRUCT:\\n+                return saw_type\\n+            name = saw_type.struct_name\\n+            if is_layout_transparent(name) and saw_type.type_args:\\n+                inner = saw_type.type_args[0]\\n+            else:\\n+                alias = self.namespace.lookup_type_alias(name) if name else None\\n+                if alias is None or alias.aliased_type is None:\\n+                    return saw_type\\n+                inner = alias.aliased_type\\n+            if self.type_param_context and inner is not None:\\n+                inner = inner.substitute(self.type_param_context)\\n+            saw_type = inner\\n+        return saw_type\\n+\\n     def _get_cleanup_behavior(self, saw_type: SawType) -> str:\\n         \\\"\\\"\\\"Determine cleanup behavior for a type.\\n \\n@@ -187,6 +226,7 @@ class ResourcesMixin:\\n         the abstract form answers \\\"none\\\", so a concrete `Slot<Res>` read would\\n         emit no copy and be over-released.\\n         \\\"\\\"\\\"\\n+        saw_type = self._lifecycle_type(saw_type)\\n         type_name = self._get_type_name_for_conformance(saw_type)\\n         if type_name is None:\\n             return \\\"none\\\"\\n@@ -263,6 +303,7 @@ class ResourcesMixin:\\n         - an `Optional<T>` whose inner `T` needs cleanup;\\n         - a fixed array or tuple whose elements do, and an escaping closure.\\n         \\\"\\\"\\\"\\n+        saw_type = self._lifecycle_type(saw_type)\\n         # `Box<any Trait, A>` always owns a heap payload: its erased teardown\\n         # (vtable destructor + dealloc) must run at scope death.\\n         if self._is_erased_box(saw_type):\\n@@ -565,6 +606,7 @@ class ResourcesMixin:\\n            cleanup-needing fields), release those fields directly, in reverse\\n            declaration order.\\n         \\\"\\\"\\\"\\n+        saw_type = self._lifecycle_type(saw_type)\\n         # `Box<any Trait, A>` (design 51): teardown is driven by the vtable\\n         # (destructor + size + align), not a monomorphized `Box_deinit` — the\\n         # payload is erased, so there is no static `sizeof<T>`.\\n@@ -871,6 +913,7 @@ class ResourcesMixin:\\n     def _emit_retain_at(self, ptr, saw_type: SawType):\\n         \\\"\\\"\\\"Bump the refcounts of the owning value stored at `ptr` (a pointer to\\n         it). The structural mirror of `_emit_drop_at`.\\\"\\\"\\\"\\n+        saw_type = self._lifecycle_type(saw_type)\\n         if not self._needs_cleanup(saw_type):\\n             return\\n         saw_type = self._retag_enum(saw_type)\\n@@ -1129,6 +1172,7 @@ class ResourcesMixin:\\n     # non-refcounted `Deinit` value (design 65).\\n \\n     def _emit_release_at(self, ptr, saw_type: SawType):\\n+        saw_type = self._lifecycle_type(saw_type)\\n         if not self._needs_cleanup(saw_type):\\n             return\\n         saw_type = self._retag_enum(saw_type)\\n@@ -1366,6 +1410,7 @@ class ResourcesMixin:\\n         A type that cannot be duplicated never reaches here: the typechecker\\n         refuses it.\\n         \\\"\\\"\\\"\\n+        saw_type = self._lifecycle_type(saw_type)\\n         if saw_type.kind == TypeKind.ARRAY:\\n             return self._emit_array_deep_copy(value, saw_type)\\n         if saw_type.kind == TypeKind.TUPLE:\\ndiff --git a/sawc/std/mutex.saw b/sawc/std/mutex.saw\\nindex c7a40baa..fcd265ac 100644\\n--- a/sawc/std/mutex.saw\\n+++ b/sawc/std/mutex.saw\\n@@ -64,7 +64,7 @@ extension Mutex<T> {\\n     /// unlocked mutex over a zeroed payload.\\n     public init(value: T) unsafe -> Mutex<T> {\\n         Mutex<T>(state: UnsafeMutableInterior(0),\\n-                 value: UnsafeMutableInterior(value))\\n+                 value: UnsafeMutableInterior(move value))\\n     }\\n \\n     /// Acquires the lock, runs `body` with a `&var` borrow of the payload,\\ndiff --git a/sawc/std/spinlock.saw b/sawc/std/spinlock.saw\\nindex 48b5ffb4..4c0cc0da 100644\\n--- a/sawc/std/spinlock.saw\\n+++ b/sawc/std/spinlock.saw\\n@@ -76,7 +76,7 @@ extension SpinLock<T> {\\n     /// zero pattern is already an unlocked lock over a zeroed payload.\\n     public init(value: T) unsafe -> SpinLock<T> {\\n         SpinLock<T>(state: Atomic(LockState.Unlocked as Int),\\n-                    value: UnsafeMutableInterior(value))\\n+                    value: UnsafeMutableInterior(move value))\\n     }\\n \\n     /// Acquires the lock, runs `body` on the payload, releases, and returns what\\ndiff --git a/sawc/typechecker/expressions.py b/sawc/typechecker/expressions.py\\nindex c0c6db0f..57f4c7aa 100644\\n--- a/sawc/typechecker/expressions.py\\n+++ b/sawc/typechecker/expressions.py\\n@@ -309,7 +309,7 @@ class ExpressionsMixin:\\n         underlying that is one of the four primitive kinds.\\n \\n         Representationally this is a no-op — an alias IS its underlying — so\\n-        codegen compiles the operand and nothing else.\\n+        codegen lowers the operand as a transfer and nothing else.\\n         \\\"\\\"\\\"\\n         if len(expr.arguments) != 1:\\n             self._error(\\n@@ -366,6 +366,10 @@ class ExpressionsMixin:\\n                 hint=self._int_conversion_hint(arg_type, underlying)\\n             )\\n             return None\\n+        # The alias value becomes the operand's owner, so the operand is a\\n+        # transfer like any call argument (SL-340).\\n+        self._check_value_transfer(arg.value, underlying, \\\"alias construction\\\",\\n+                                   arg.value.line, arg.value.column)\\n         expr.alias_construction = alias_name\\n         return alias_type\\n \\n@@ -4323,6 +4327,13 @@ class ExpressionsMixin:\\n             cell_arg = declared if declared is not None else arg_type\\n             if cell_arg is None:\\n                 return None\\n+            # The cell becomes the value's owner, so the argument is a transfer\\n+            # like any call argument: an owning source needs `move`, a Copy-tier\\n+            # one is retained, and a moved one is retired (SL-340).\\n+            self._check_value_transfer(expr.arguments[0].value, cell_arg,\\n+                                       \\\"interior cell construction\\\",\\n+                                       expr.arguments[0].value.line,\\n+                                       expr.arguments[0].value.column)\\n             expr.is_interior_cell_construct = True\\n             return SawType(TypeKind.STRUCT,\\n                            struct_name=\\\"UnsafeMutableInterior\\\",\\n@@ -7046,6 +7057,12 @@ class ExpressionsMixin:\\n                     f\\\"`write` expects `{inner}`, got `{val_type}`\\\",\\n                     expr.line, expr.column,\\n                     hint=self._int_conversion_hint(val_type, inner))\\n+            # A placement write: the memory becomes the value's owner, exactly\\n+            # as `ptr[i] = move v` does (SL-340).\\n+            self._check_value_transfer(expr.arguments[0].value, inner,\\n+                                       \\\"memory write\\\",\\n+                                       expr.arguments[0].value.line,\\n+                                       expr.arguments[0].value.column)\\n             return SawType(TypeKind.VOID)\\n \\n         # Region accessors — Normal only.\\ndiff --git a/sawc/typechecker/types.py b/sawc/typechecker/types.py\\nindex 55a0b248..ee45c43e 100644\\n--- a/sawc/typechecker/types.py\\n+++ b/sawc/typechecker/types.py\\n@@ -4506,6 +4506,15 @@ class TypeUtilsMixin:\\n              one acquisition both lowerings share, and by the driven seed's\\n              `let`.\\n \\n+        INTERCEPTED CONSTRUCTIONS (lowered by codegen's `_gen_transfer_value`)\\n+         21. `_check_function_call` (expressions.py) — the\\n+             `UnsafeMutableInterior(...)` arm, context \\\"interior cell\\n+             construction\\\".\\n+         22. `_check_alias_construction` (expressions.py) — a distinct alias's\\n+             back-conversion `Alias(x)`, context \\\"alias construction\\\".\\n+         23. `_check_um_method` (expressions.py) — `UnsafeMemory.write(v)`,\\n+             context \\\"memory write\\\".\\n+\\n         Behavior by the source expression and its resolved type:\\n         - `move x`: ownership transfers; a transfer is neither a copy nor a\\n           NoCopy violation, so it is always accepted. The source binding's\\n\",\"diff_bytes\":73082,\"diff_files\":22,\"diff_hunks\":39,\"diff_tail\":\"oCopy violation, so it is always accepted. The source binding's\\n\",\"id\":\"SL-340\",\"request_id\":\"pid-85444-1790273291-6000\",\"title\":\"A cell or alias construction is a value transfer, and a cell or alias drops, retains and copies as the type it stands for (Mutex use-after-free + cell/alias payload leak)\"}","request_id":"pid-85444-1790273291-6000","sequence":1470,"timestamp":"1790273293"}
---
