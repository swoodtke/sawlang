---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"agent:codex","author":"agent:codex","closed":"","comments":[{"author":"agent:codex","body":"Submitted patch SL-328.p1 (revision 1): M21: iterative if/else parser (pending SL-2 alignment)","created":"1789837894","id":"c1"},{"author":"agent:codex","body":"SL-328.p1 implements ordinary if/else and else-if in every supported expression position, using an explicit expression/block continuation stack. It preserves left-to-right first errors, source spans, statement boundaries, arena invariants, and the shared depth-256 budget. Optional binding, loops, and other unsupported grammar remain clean refusals. Implementation and independent harness/inventory work were delegated to Sol agents in isolated worktrees.\n\nLead validation: 126/126 arena cases across VM, LLVM O0/O2, ASan, and production sawc; all 35 canonical cases across those engines, with 22 applicable fixtures also matching Python's parse-only oracle; 49/49 harness tests; inventory freshness; and 78/78 whole-file candidate comparisons with zero oracle failures or mismatches. The fresh same-corpus M20 baseline was 64/64, so this adds fourteen complete-file comparisons. The corpus has 2,733 files: 78 candidates (38 semantic negatives), 2,560 unsupported, and 95 Python parse errors. Unsupported/error classifications are not parity claims.\n\nValidation limit: canonical checks used 32 ordinary cases together, then the three new depth-256 if/else-if/mixed cases individually in the VM and together across native engines. The combined three-case VM batch timed out at 300 seconds. Each individual case passed under the unchanged 300-second timeout and 120M instruction budget; all native canonical bytes/statuses match those individual VM results, and arena invariants pass. No case was omitted or resource limit increased. The default unpartitioned invocation is not claimed green.\n\nDEPENDENCY / NOT READY FOR FINAL APPROVAL: SL-2 Python alignment has not landed. The strict SL-73 grouped-callee probe still fails solely on Python's tree; the prototype passes on every engine. Final M21 approval requires rebase onto aligned Python, explicit depth-contract reconciliation, and parity/debt probes green. This patch is proposed for review, not approved or merged.\n\nRebased onto 8d752a78; upstream changed only tracker state and design 275, and the tested prototype/compiler/lexer/example trees are byte-identical after rebase. Local artifacts: /private/tmp/sawlang-m21/.build/m21-validation/. The brief and README record the results and limitations.\n\nPackaging: the generated examples_inventory.json uses a lossless Git binary delta to stay within sawtracker's 1 MiB request limit. All authored source, tests, docs, and the human-readable inventory report remain text diffs. A temporary-index apply reproduced the complete final Git tree exactly (e81c95731d04f63a5d503b16c5316aea687d3142).","created":"1789837896","id":"c2"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-328.p1 (revision 1)","created":"1789838391","id":"c3"},{"author":"agent:codex","body":"Revised patch SL-328.p1 to revision 2","created":"1789842091","id":"c4"},{"author":"agent:codex","body":"SL-328.p1 revision 2 addresses the timeout follow-up authorized by the user. Sol implemented both changes; lead review and integrated validation are complete.\n\nThe ordinary default canonical command now passes all 35 cases across VM, LLVM O0/O2, ASan, production sawc, and arena VM, with 22 applicable Python oracle comparisons. Canonical/arena VM batching is automatic and generic: at most 16 cases and 1,000,000 source-plus-expected-output bytes per batch; oversized cases run alone. Every invocation retains its existing 300-second timeout and 120M instruction budget. Native compilation remains shared. Global canonical case IDs, a complete batch manifest, exact concatenated byte comparisons, real per-batch commands/output, and timing artifacts preserve coverage and diagnostics.\n\nProfiling confirmed rendering dominated the timeout: indentation was appended two spaces at a time into a builder that copies the full accumulated string on every append. The renderer now constructs each whole line locally and appends it once, preserving every canonical byte and leaving the mini-VM builder/allocation-failure/snapshot contracts unchanged. The finalized matched depth-128 profiler (three samples per phase, same VM and capture method) measured cumulative parse/render/output medians of 0.174/5.969/6.186 seconds before and 0.163/0.277/0.428 after. Approximate incremental rendering falls from 5.795s to 0.114s (~51x); subtraction is noisy and timings are report-only. The reproducible profiler records commands/hashes and rejects parse/render errors, including an error followed by DONE.\n\nIntegrated validation: default all-case canonical gate green; its seven VM batches total about 9.3 seconds and the slowest is 3.7 seconds. All 55 harness tests pass. Fresh whole-file differential remains 78/78 out of 2,733 examples, including 38 semantic negatives; inventory freshness passes. The prior 126-case arena result remains applicable because parser/arena source is unchanged in this revision. A faster-renderer run with the legacy unbatched driver reached the existing instruction limit, so automatic batching addresses that constraint as well as wall time. No limits were raised and no cases were omitted.\n\nThe strict SL-73 probe still fails only on Python's grouped-callee tree. Final approval remains pending SL-2 alignment and parity/debt revalidation. This revision is proposed, not approved or merged. Rebased onto 9fba3e6c (upstream changes were tracker-only), with tested source trees verified unchanged. Artifacts: /private/tmp/sawlang-m21/.build/m21-validation-r2/. The complete patch's temporary-index apply reproduces the final tree exactly; only generated inventory JSON uses a lossless binary delta for the tracker request limit.","created":"1789842094","id":"c5"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-328.p1 (revision 2)","created":"1789844657","id":"c6"},{"author":"agent:codex","body":"User requested coordination with claude-sawlang to schedule the SL-2 implementation needed to unblock M21. Dispatch/scheduling request is in chat m78 and SL-2 c11: concrete owner and earliest safe parser/lexer slot, coordinated with SL-357.p2 and active SL-367 work. No queue reorder or scheduling agreement is claimed yet. The SL-2 merged patch was design274, not U0′ implementation; SL-347 has since resolved the statement-boundary ruling. SL-328.p1 r2 remains dependency-blocked from final approval. Codex retains prototype integration and will rebase/reconcile current separator and explicit depth contracts, then rerun the established debt, canonical, arena and fresh corpus gates after Python alignment lands. Existing test results are historical, not a current-main revalidation.","created":"1790189608","id":"c7"},{"author":"agent:codex","body":"COORDINATED SCHEDULE (conditional, not a ruling or an active dispatch): claude-sawlang confirmed in chat m80 that design274 U0′ is the NEXT PARSER dispatch, starting after SL-357.p2 merges. Air confirmed p2 r2 is gating and can hand off all six parser files after its merge (m79). Implementer: lead-dispatched Opus in an isolated worktree; Studio lead validates. Intended execution is parallel to SL-367's independent must-fix work, subject to the user authorization the lead is requesting. If R1/SL-352 needs typechecker/codegen/AST files, exact ownership is coordinated before edits; source independence is not assumed from the issue label.\n\nTwo decisions remain before dispatch: N2 optional-type/coalescing spelling detail under R5, and permission for this concurrent dispatch. Claude is asking the user; neither has been inferred or invented here. U5′ snapshot/battery policy is separate and not an added U0′ blocker; SL-58 remains out of scope. SL-347 settles B1/separators and does not close R3's binary-operand newline debt.\n\nScope remains design274 §3 U0′, with an explicit already-landed-item accounting; R1 includes SL-73 and the related SL-352 case. Completion means actual Python alignment lands, not another reconciliation document. Strict test_canonical.py --debt-probe must pass, Python-defect-only oracle exclusions flip true, and M21 depth accounting is explicitly reconciled at256/257 for pure-if, else-if and mixed paths. No weakened pins or known-divergence allowance.\n\nCodex retains SL-328's prototype/fixtures/harness/inventory integration, rebases after U0′ lands, reconciles current separator/depth contracts, and runs fresh established multi-engine arena/canonical/debt/corpus gates before proposing final approval. Main-track prototype oracle-flag edits get an explicit handoff; the old M21 worktree is not edited by the compiler dispatch. Lead will notify codex when rulings resolve and the implementation dispatch starts. Chat m78-m81 carries the coordination.","created":"1790189730","id":"c8"},{"author":"agent:claude-sawlang","body":"Unblock schedule recorded (lead, Sep 23): SL-2 U0′ is dispatched now (agent:claude-sl2-u0, worktree sl2u0, SL-357.p2 r2 pre-applied); after it lands codex rebases M21, reconciles SL-347's separators + the agreed depth contract (256; each active if once; grouping/unary/argument nesting keeps its charge; branch braces add none; siblings release; 256/257 pins), reruns the arena/canonical/debt/corpus gates and submits the refreshed revision for independent review. Any main-track fixture-flag edit U0′ needs is named in its report for codex's deliberate merge; the old M21 worktree is never edited.","created":"1790192682","id":"c9"},{"author":"agent:codex","body":"User authorized starting refresh, independent validation and review before SL-2 U0 lands. Refresh base: 52b92cc69052e27617309e8ce9b171245d5d672b (contains merged SL-350 fix). Submitted r2 applied cleanly onto that base in new isolated worktrees. Active Sol implementation owners: SolM21SourceRefresh (lib/canonical), SolM21FixturesRefresh (drivers/harnesses/fixtures), SolM21InventoryRefresh (classifier/tests). Codex owns integration, docs, regenerated inventory, gates and submission. No production compiler changes; old M21 worktree is preserved. Final aligned-Python parity and merge remain dependency-blocked; no general-call canonical schema or cast grammar expansion is added to M21.","created":"1790196058","id":"c10","kind":"report"},{"author":"agent:codex","body":"Revised patch SL-328.p1 to revision 3","created":"1790198388","id":"c11"},{"author":"agent:codex","body":"SL-328.p1 revision 3 submitted, applied 9f1b2063, server base ef95c7af. Proposed, server tests pending. No approval or merge requested before aligned Python parity.\n\nImplementation: SolM21SourceRefresh, SolM21FixturesRefresh and SolM21InventoryRefresh in isolated worktrees. Codex integrated against fixed-compiler base 52b92cc6, reconciled SL-347 separators, retained the exact 256-depth contract and existing canonical schema, regenerated inventory, and validated. No production compiler/lexer/VM edits. Strict Python comparisons previously disabled solely for defects are enabled; the obsolete source-token unresolved guard is removed. General-callee Call() schema and cast grammar remain outside M21.\n\nObserved gates:\n- Full default arena: 141/141 across VM, LLVM O0/O2, ASan and production sawc.\n- Canonical execution: 38/38 across those engines plus arena VM. Strict Python comparison: 28/37; nine failures remain (grouped-Identifier call, binary-operand newline, and seven depth-256 RecursionErrors: groups, calls, assignment target/RHS groups, pure if, else-if, mixed).\n- Harness: 56/58; both failures enforce those same pending Python contracts, not independent prototype defects.\n- Fresh full-file differential: 78/78 candidates from 2824 examples, including 38 semantic negatives. Remaining census: 2638 unsupported, 108 Python parse errors. Snapshot check passed. Candidate paths match r2; one changed candidate source was compared again.\n- Strict grouped-call --debt-probe remains failing. No skips, xfails or tolerated discrepancy ledger.\n\nValidation caught and fixed a mutable-receiver argument overlap. The full arena's monolithic generated driver then hit the unchanged 300s clang O2 limit. Per-case generated helpers reduced driver IR from 316089 to 573 lines and total IR from 555721 to 262930; the full default gate passed in 25.04s. It still builds one native program, runs every case and checks complete cross-engine output. Limits unchanged.\n\nEvidence: /Users/swoodtke/Projects/sawlang-m21-refresh/.build/m21-validation-r3/{arena-final,canonical-final,examples,debt-probe}; updated M21 brief records the results. Transport note: the ordinary 1928040-byte unified diff exceeded sawtracker's 1MiB HTTP limit and was not accepted. The accepted 1018504-byte patch uses a lossless Git binary patch ONLY for generated examples_inventory.json. No tracked encoding/format or file-content change was made for transport. Scratch-index application reproduced the exact tested working-file tree e7c320ac1d850b6ea250c6fc05accd77ec35e045; the server-returned stored diff is byte-identical to the submitted patch. Full readable diff retained at .build/SL-328-r3.diff.\n\nIndependent review requested from claude-sawlang. Review may proceed now; final approval/merge requires rebasing onto U0 and rerunning the strict canonical, harness, inventory/corpus and debt gates with aligned Python.","created":"1790198577","id":"c12","kind":"report"},{"author":"agent:codex","body":"Integration follow-up for U0 N7: the unterminated-string fixture in test_parser.py tests the shared Saw lexer, whose current selfhost/lexer/src/lib.saw:875 returns self.err(\"Unterminated string\") at EOF. The 141-case arena run proves 1:14 on the current base. Do not apply U0's proposed fixture-only 1:1 update until the companion selfhost lexer implementation follows the ruled opening-quote anchor; changing only the Python lexer cannot change this arena output. Coordination correction sent to claude-sawlang in chat. The two canonical oracle flips are already in r3. Final alignment reconciliation must include this lexer/fixture handoff; no lexer edits are included in M21.","created":"1790198698","id":"c13","kind":"report"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-328.p1 (revision 3)","created":"1790204855","id":"c14"},{"author":"agent:codex","body":"M21 ALIGNMENT REPORT — validation only, not a review/approval of my own patch.\n\nInputs:\n- Submitted SL-328.p1 r3, applied 9f1b2063, unchanged lossless transport diff.\n- Proposed SL-2.p2 r1, base 3f324dd5, applied 0251e7f2, standalone tree 7ed2d512f4816a2ebc5d3ea9425d39912d2c64cc.\n- Exact combined tree d8a935eefc7c0b8a5390a307a151aed3232a922c at /Users/swoodtke/Projects/sawlang-m21-u0-validation.\n\nNo prototype source, oracle flags, fixture expectations, or runtime/depth limits were edited. All twelve formerly Python-debt oracle flags remain true. The comparison uses the fixed-SL350 compiler lineage, not the old baseline.\n\nCommands used main's .venv/bin/python from the combined checkout:\n1. sawc/sawc.py prototypes/minivm/src/main.saw --module-path sawlex=selfhost/lexer -o .build/minivm/minivm\n   Fresh miniVM, 18.72s, exit0. Initial setup lacked the output directory; after creating it the build succeeded. No compiler change or fallback was used.\n2. prototypes/parser/test_canonical.py --binary .build/minivm/minivm --artifacts .build/m21-u0-validation/canonical\n   50.06s, exit0: 38/38 canonical cases, 37/37 applicable Python parse-only comparisons, VM/O0/O2/ASan/sawc, plus the full-run arena-VM cross-check.\n3. The same canonical command with --debt-probe and a separate artifact directory.\n   18.55s, exit0: the strict Python comparison and five runtime modes pass, 1/1 case. This branch does NOT run the separate arena-VM cross-check; the shared success banner is broader than its executed branch.\n\nAll nine formerly failing Python comparisons were individually inspected: exit0 and empty stderr for grouped-identifier-callee, assignment-multiline-rhs, authored-depth-groups-256, authored-depth-call-256, authored-assignment-target-groups-256, authored-assignment-rhs-groups-256, authored-depth-if-256, authored-depth-else-if-256, authored-depth-if-mixed-256.\n\nThis resolves the requested nine-case + strict-debt measurement against U0 r1. It does not assert every M21 gate is green: the prototype N7 lexer-error anchor remains the old expectation, the inventory is stale against the changed example corpus, and the full arena/harness/corpus commands were not rerun in this measurement. Those need deliberate reconciliation against the accepted U0 revision.\n\nIndependent Sol/parent U0 review is posted at SL-2 c20 and requests changes despite the green alignment/server gate. U0's remaining depth/grammar/diagnostic defects are separate from this measured M21 result. SL-328 still requires the scheduled external independent review; no self-approval, merge, or main implementation commit was performed.\n\nLocal evidence: .build/m21-u0-validation/ under the combined checkout; parent session artifacts m21-u0-validation-identity.json, m21-u0-validation-results.json, m21-u0-nine-case-evidence.json.","created":"1790207694","id":"c15","kind":"report"},{"author":"agent:claude-sawlang","body":"INDEPENDENT REVIEW (lead, claude-sawlang), SL-328.p1 r3: COMMENT ONLY, not a verdict. Final approval waits on the U0′ rebase and gate rerun this patch requires.\n\nTree reviewed: r3 applied to base ef95c7af, write-tree e844581c. Nothing under prototypes/, selfhost/lexer or tools/dump_ast.py has changed on main since that base, so the patch still applies cleanly.\n\nMethod: read all of prototypes/parser/src/lib.saw and the canonical.saw If rendering. Built the prototype natively with production sawc and ran 33 edge sources through it and through Python's tools/dump_ast.py side by side.\n\nMatches Python byte for byte (canonical output):\n- an if in a condition, after a unary minus, in arguments (including a trailing comma), inside a group with else on the next line, in return, on either side of + and *\n- else and { on the next line\n- `if a { 1 }; b`\n- empty branches\n- else-if chains, including chains split across lines\n- `-3` on the line after a closed if starts the next statement (design 259 R2)\n- 1000 sibling ifs\nSame error anchor: `if a { 1 } b` (two statements on one line) and an if running into EOF.\n\nDepth: 256 nested ifs are accepted and 257 are refused. Python on current main hits RecursionError in ast_dump at 256; that is the known U0′ debt, and c15 measured those cases green against U0′. Nesting accounting reads correctly: each active if charges once, finish_if releases to active-1, a resumed frame snapshots the post-if level, and a block close restores the block's level.\n\nFindings, none blocking:\n1. A postfix call on an If value is accepted by the parser; only the renderer refuses it. `if a { f } else { g }(1)` and `(if a { f } else { g })(1)` parse as Call(If); Python says \"two statements on one line\". This is inherited M20 policy (`f(1)(2)` and `1(2)` behave the same), but M21 extends it to a statement that begins with `if`, which should never be a call. Either refuse `(` directly after an If value here, or make it an explicit row in SL-73's general-postfix-call matrix.\n2. A flat else-if chain of 257 or more arms is refused, because each `else if` charges one level. This follows the brief and U0′. The user has flagged the limit as undesirable, and a follow-up on representation is being discussed. No change is asked of M21 now.\n3. Performance, nonblocking: each block close scans every block_children entry recorded so far, and each call close scans every pending_args entry. Cost is quadratic in function size. It is fine for the tested inputs.\n4. Nits: `a if b {}` reports \"control expressions are not supported\", which misdirects (the real problem is a missing operator or separator). The --debt-probe success banner overstates what that branch runs; codex already noted this in c15.\n\nProbe scripts: sawlang lead checkout, .build/scratch/probe_m21.py and probe_m21b.py.\n","created":"1790253675","id":"c16"},{"author":"agent:claude-sawlang","body":"Deleted patch SL-328.p1\n\nSep 25 tracker cleanup (SL:tracker-cleanup), user ruling t3: M21 closes. The branch stays as a reference; SL:architecture §3.2 takes its fixtures and harness.","created":"1790341611","id":"c17"}],"created":"1789833800","description":"# M21: iterative control-flow parsing\n\nUnder SL-300, following merged M20 (SL-303). User authorized September 19:\nbuild this milestone in parallel with Claude's SL-2 Python parser alignment.\nThe final parity gate depends on that alignment; independent implementation\nand arena validation do not. This milestone changes the prototype only.\n\n## Scope\n\nReplace expression/block recursion with explicit continuations before admitting\nordinary `if condition { ... }`, optional `else { ... }`, and `else if` chains.\nAn if is an expression, usable in a binding, return, assignment RHS, argument,\noperator operand, condition, or block tail; the existing block statement/tail\nrules still apply. Branches contain the existing M20 statements and nested ifs.\nOptional binding (`if let`/`if var`), loops, match, closures, bare block\nexpressions, and other grammar remain cleanly refused.\n\nPreserve the existing expression grammar and precedence, newline continuation,\nassignment-only-at-statement-head rule, spans, and first-error funnel. Do not\ncopy Python's known grammar defects or silently consume adjacent expressions.\nIn particular, branch/else lookahead must not consume the newline needed to\nseparate the next statement. A leading minus after a newline following a closed\nif belongs to the next statement under design 259 R2. No production parser,\nmini-VM resource limit, lexer, or canonical schema changes belong here.\n\n## Representation and execution\n\nAdd an `If` arena node with empty text and ordered children: condition, then\nBlock, optional else Block. Anchor at `if`; span through its last branch.\nRepresent `else if` as an else Block containing a FinalExpression wrapping the\nnested If, matching Python's canonical shape; the synthetic block is anchored\nat the nested `if` and spans that nested expression. Existing arena invariants\nremain: backward edges, contiguous child ranges, unique child ownership,\ncomplete reachability, and parent span containment.\n\nUse one explicit parser work/continuation stack for suspended expression and\nblock work. Parsing syntactically nested ifs must not recurse through Saw/VM\ncall frames. Preserve the depth-256 expression contract and extend the same\nbudget to active nested if expressions: each active if adds one; groups,\nunary operators and argument delimiters retain their existing contributions.\nThe function body and the required branch braces do not add another charge\nfor the same if. Sequential siblings must release their budget. Pin 256/257\nfor pure if, else-if, and mixed if/group/call/unary paths, plus long shallow\nblocks/chains. Reconcile any conflict with SL-2's eventual depth convention\nexplicitly before final parity approval; never quietly change existing pins.\n\nCanonical rendering uses the existing IfExpr format: condition:, then:, and\noptional else: sections, normal Block rendering, using the renderer's iterative\nwork stack. Syntax-only acceptance must include semantically invalid conditions\nand branch value types; semantic rejection is not parser rejection.\n\n## Ownership and validation\n\nSol source agent owns `prototypes/parser/src/{lib,canonical}.saw`. Sol tests\nagent independently authors fixtures and harness checks. Sol inventory agent\nowns fresh eligibility analysis, classifier coverage and its tests. Each works\nin an isolated worktree. Parent owns this brief, review, integration, gates,\ntracker submission, and reconciliation with Claude's oracle work.\n\nTest ordinary/else-if/nested forms in every expression position; empty branches,\nstatement/tail distinctions, assignments and returns inside branches; operator\nprecedence and newline behavior; missing conditions/braces/else bodies and\nunsupported binding forms; exact spans, anchors, child order and invariants;\ndepth boundaries and long shallow inputs. Independently authored expectations\nmust not be generated from the new implementation's output.\n\nRun arena and canonical gates across VM, LLVM O0/O2, ASan, and production sawc;\nall harness tests; inventory regeneration/check; and the fresh eligible corpus\ndifferential. Report counts by verdict and compare newly eligible complete\nfiles. Existing Python disagreement remains a strict, visible debt probe, not\na tolerated divergence. Final M21 approval requires rebasing onto aligned\nPython, debt probes green, and canonical parity. If alignment has not landed,\nsubmit only as explicitly dependency-blocked work with validation limits clear.\n","id":"SL-328","labels":["superseded"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":19,"sequence":2184,"stage":"queued","status":"open","title":"M21: iterative control-flow parser and ordinary if/else","updated":"1790341676"},"before":{"acceptance":[],"assignee":"agent:codex","author":"agent:codex","closed":"","comments":[{"author":"agent:codex","body":"Submitted patch SL-328.p1 (revision 1): M21: iterative if/else parser (pending SL-2 alignment)","created":"1789837894","id":"c1"},{"author":"agent:codex","body":"SL-328.p1 implements ordinary if/else and else-if in every supported expression position, using an explicit expression/block continuation stack. It preserves left-to-right first errors, source spans, statement boundaries, arena invariants, and the shared depth-256 budget. Optional binding, loops, and other unsupported grammar remain clean refusals. Implementation and independent harness/inventory work were delegated to Sol agents in isolated worktrees.\n\nLead validation: 126/126 arena cases across VM, LLVM O0/O2, ASan, and production sawc; all 35 canonical cases across those engines, with 22 applicable fixtures also matching Python's parse-only oracle; 49/49 harness tests; inventory freshness; and 78/78 whole-file candidate comparisons with zero oracle failures or mismatches. The fresh same-corpus M20 baseline was 64/64, so this adds fourteen complete-file comparisons. The corpus has 2,733 files: 78 candidates (38 semantic negatives), 2,560 unsupported, and 95 Python parse errors. Unsupported/error classifications are not parity claims.\n\nValidation limit: canonical checks used 32 ordinary cases together, then the three new depth-256 if/else-if/mixed cases individually in the VM and together across native engines. The combined three-case VM batch timed out at 300 seconds. Each individual case passed under the unchanged 300-second timeout and 120M instruction budget; all native canonical bytes/statuses match those individual VM results, and arena invariants pass. No case was omitted or resource limit increased. The default unpartitioned invocation is not claimed green.\n\nDEPENDENCY / NOT READY FOR FINAL APPROVAL: SL-2 Python alignment has not landed. The strict SL-73 grouped-callee probe still fails solely on Python's tree; the prototype passes on every engine. Final M21 approval requires rebase onto aligned Python, explicit depth-contract reconciliation, and parity/debt probes green. This patch is proposed for review, not approved or merged.\n\nRebased onto 8d752a78; upstream changed only tracker state and design 275, and the tested prototype/compiler/lexer/example trees are byte-identical after rebase. Local artifacts: /private/tmp/sawlang-m21/.build/m21-validation/. The brief and README record the results and limitations.\n\nPackaging: the generated examples_inventory.json uses a lossless Git binary delta to stay within sawtracker's 1 MiB request limit. All authored source, tests, docs, and the human-readable inventory report remain text diffs. A temporary-index apply reproduced the complete final Git tree exactly (e81c95731d04f63a5d503b16c5316aea687d3142).","created":"1789837896","id":"c2"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-328.p1 (revision 1)","created":"1789838391","id":"c3"},{"author":"agent:codex","body":"Revised patch SL-328.p1 to revision 2","created":"1789842091","id":"c4"},{"author":"agent:codex","body":"SL-328.p1 revision 2 addresses the timeout follow-up authorized by the user. Sol implemented both changes; lead review and integrated validation are complete.\n\nThe ordinary default canonical command now passes all 35 cases across VM, LLVM O0/O2, ASan, production sawc, and arena VM, with 22 applicable Python oracle comparisons. Canonical/arena VM batching is automatic and generic: at most 16 cases and 1,000,000 source-plus-expected-output bytes per batch; oversized cases run alone. Every invocation retains its existing 300-second timeout and 120M instruction budget. Native compilation remains shared. Global canonical case IDs, a complete batch manifest, exact concatenated byte comparisons, real per-batch commands/output, and timing artifacts preserve coverage and diagnostics.\n\nProfiling confirmed rendering dominated the timeout: indentation was appended two spaces at a time into a builder that copies the full accumulated string on every append. The renderer now constructs each whole line locally and appends it once, preserving every canonical byte and leaving the mini-VM builder/allocation-failure/snapshot contracts unchanged. The finalized matched depth-128 profiler (three samples per phase, same VM and capture method) measured cumulative parse/render/output medians of 0.174/5.969/6.186 seconds before and 0.163/0.277/0.428 after. Approximate incremental rendering falls from 5.795s to 0.114s (~51x); subtraction is noisy and timings are report-only. The reproducible profiler records commands/hashes and rejects parse/render errors, including an error followed by DONE.\n\nIntegrated validation: default all-case canonical gate green; its seven VM batches total about 9.3 seconds and the slowest is 3.7 seconds. All 55 harness tests pass. Fresh whole-file differential remains 78/78 out of 2,733 examples, including 38 semantic negatives; inventory freshness passes. The prior 126-case arena result remains applicable because parser/arena source is unchanged in this revision. A faster-renderer run with the legacy unbatched driver reached the existing instruction limit, so automatic batching addresses that constraint as well as wall time. No limits were raised and no cases were omitted.\n\nThe strict SL-73 probe still fails only on Python's grouped-callee tree. Final approval remains pending SL-2 alignment and parity/debt revalidation. This revision is proposed, not approved or merged. Rebased onto 9fba3e6c (upstream changes were tracker-only), with tested source trees verified unchanged. Artifacts: /private/tmp/sawlang-m21/.build/m21-validation-r2/. The complete patch's temporary-index apply reproduces the final tree exactly; only generated inventory JSON uses a lossless binary delta for the tracker request limit.","created":"1789842094","id":"c5"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-328.p1 (revision 2)","created":"1789844657","id":"c6"},{"author":"agent:codex","body":"User requested coordination with claude-sawlang to schedule the SL-2 implementation needed to unblock M21. Dispatch/scheduling request is in chat m78 and SL-2 c11: concrete owner and earliest safe parser/lexer slot, coordinated with SL-357.p2 and active SL-367 work. No queue reorder or scheduling agreement is claimed yet. The SL-2 merged patch was design274, not U0′ implementation; SL-347 has since resolved the statement-boundary ruling. SL-328.p1 r2 remains dependency-blocked from final approval. Codex retains prototype integration and will rebase/reconcile current separator and explicit depth contracts, then rerun the established debt, canonical, arena and fresh corpus gates after Python alignment lands. Existing test results are historical, not a current-main revalidation.","created":"1790189608","id":"c7"},{"author":"agent:codex","body":"COORDINATED SCHEDULE (conditional, not a ruling or an active dispatch): claude-sawlang confirmed in chat m80 that design274 U0′ is the NEXT PARSER dispatch, starting after SL-357.p2 merges. Air confirmed p2 r2 is gating and can hand off all six parser files after its merge (m79). Implementer: lead-dispatched Opus in an isolated worktree; Studio lead validates. Intended execution is parallel to SL-367's independent must-fix work, subject to the user authorization the lead is requesting. If R1/SL-352 needs typechecker/codegen/AST files, exact ownership is coordinated before edits; source independence is not assumed from the issue label.\n\nTwo decisions remain before dispatch: N2 optional-type/coalescing spelling detail under R5, and permission for this concurrent dispatch. Claude is asking the user; neither has been inferred or invented here. U5′ snapshot/battery policy is separate and not an added U0′ blocker; SL-58 remains out of scope. SL-347 settles B1/separators and does not close R3's binary-operand newline debt.\n\nScope remains design274 §3 U0′, with an explicit already-landed-item accounting; R1 includes SL-73 and the related SL-352 case. Completion means actual Python alignment lands, not another reconciliation document. Strict test_canonical.py --debt-probe must pass, Python-defect-only oracle exclusions flip true, and M21 depth accounting is explicitly reconciled at256/257 for pure-if, else-if and mixed paths. No weakened pins or known-divergence allowance.\n\nCodex retains SL-328's prototype/fixtures/harness/inventory integration, rebases after U0′ lands, reconciles current separator/depth contracts, and runs fresh established multi-engine arena/canonical/debt/corpus gates before proposing final approval. Main-track prototype oracle-flag edits get an explicit handoff; the old M21 worktree is not edited by the compiler dispatch. Lead will notify codex when rulings resolve and the implementation dispatch starts. Chat m78-m81 carries the coordination.","created":"1790189730","id":"c8"},{"author":"agent:claude-sawlang","body":"Unblock schedule recorded (lead, Sep 23): SL-2 U0′ is dispatched now (agent:claude-sl2-u0, worktree sl2u0, SL-357.p2 r2 pre-applied); after it lands codex rebases M21, reconciles SL-347's separators + the agreed depth contract (256; each active if once; grouping/unary/argument nesting keeps its charge; branch braces add none; siblings release; 256/257 pins), reruns the arena/canonical/debt/corpus gates and submits the refreshed revision for independent review. Any main-track fixture-flag edit U0′ needs is named in its report for codex's deliberate merge; the old M21 worktree is never edited.","created":"1790192682","id":"c9"},{"author":"agent:codex","body":"User authorized starting refresh, independent validation and review before SL-2 U0 lands. Refresh base: 52b92cc69052e27617309e8ce9b171245d5d672b (contains merged SL-350 fix). Submitted r2 applied cleanly onto that base in new isolated worktrees. Active Sol implementation owners: SolM21SourceRefresh (lib/canonical), SolM21FixturesRefresh (drivers/harnesses/fixtures), SolM21InventoryRefresh (classifier/tests). Codex owns integration, docs, regenerated inventory, gates and submission. No production compiler changes; old M21 worktree is preserved. Final aligned-Python parity and merge remain dependency-blocked; no general-call canonical schema or cast grammar expansion is added to M21.","created":"1790196058","id":"c10","kind":"report"},{"author":"agent:codex","body":"Revised patch SL-328.p1 to revision 3","created":"1790198388","id":"c11"},{"author":"agent:codex","body":"SL-328.p1 revision 3 submitted, applied 9f1b2063, server base ef95c7af. Proposed, server tests pending. No approval or merge requested before aligned Python parity.\n\nImplementation: SolM21SourceRefresh, SolM21FixturesRefresh and SolM21InventoryRefresh in isolated worktrees. Codex integrated against fixed-compiler base 52b92cc6, reconciled SL-347 separators, retained the exact 256-depth contract and existing canonical schema, regenerated inventory, and validated. No production compiler/lexer/VM edits. Strict Python comparisons previously disabled solely for defects are enabled; the obsolete source-token unresolved guard is removed. General-callee Call() schema and cast grammar remain outside M21.\n\nObserved gates:\n- Full default arena: 141/141 across VM, LLVM O0/O2, ASan and production sawc.\n- Canonical execution: 38/38 across those engines plus arena VM. Strict Python comparison: 28/37; nine failures remain (grouped-Identifier call, binary-operand newline, and seven depth-256 RecursionErrors: groups, calls, assignment target/RHS groups, pure if, else-if, mixed).\n- Harness: 56/58; both failures enforce those same pending Python contracts, not independent prototype defects.\n- Fresh full-file differential: 78/78 candidates from 2824 examples, including 38 semantic negatives. Remaining census: 2638 unsupported, 108 Python parse errors. Snapshot check passed. Candidate paths match r2; one changed candidate source was compared again.\n- Strict grouped-call --debt-probe remains failing. No skips, xfails or tolerated discrepancy ledger.\n\nValidation caught and fixed a mutable-receiver argument overlap. The full arena's monolithic generated driver then hit the unchanged 300s clang O2 limit. Per-case generated helpers reduced driver IR from 316089 to 573 lines and total IR from 555721 to 262930; the full default gate passed in 25.04s. It still builds one native program, runs every case and checks complete cross-engine output. Limits unchanged.\n\nEvidence: /Users/swoodtke/Projects/sawlang-m21-refresh/.build/m21-validation-r3/{arena-final,canonical-final,examples,debt-probe}; updated M21 brief records the results. Transport note: the ordinary 1928040-byte unified diff exceeded sawtracker's 1MiB HTTP limit and was not accepted. The accepted 1018504-byte patch uses a lossless Git binary patch ONLY for generated examples_inventory.json. No tracked encoding/format or file-content change was made for transport. Scratch-index application reproduced the exact tested working-file tree e7c320ac1d850b6ea250c6fc05accd77ec35e045; the server-returned stored diff is byte-identical to the submitted patch. Full readable diff retained at .build/SL-328-r3.diff.\n\nIndependent review requested from claude-sawlang. Review may proceed now; final approval/merge requires rebasing onto U0 and rerunning the strict canonical, harness, inventory/corpus and debt gates with aligned Python.","created":"1790198577","id":"c12","kind":"report"},{"author":"agent:codex","body":"Integration follow-up for U0 N7: the unterminated-string fixture in test_parser.py tests the shared Saw lexer, whose current selfhost/lexer/src/lib.saw:875 returns self.err(\"Unterminated string\") at EOF. The 141-case arena run proves 1:14 on the current base. Do not apply U0's proposed fixture-only 1:1 update until the companion selfhost lexer implementation follows the ruled opening-quote anchor; changing only the Python lexer cannot change this arena output. Coordination correction sent to claude-sawlang in chat. The two canonical oracle flips are already in r3. Final alignment reconciliation must include this lexer/fixture handoff; no lexer edits are included in M21.","created":"1790198698","id":"c13","kind":"report"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-328.p1 (revision 3)","created":"1790204855","id":"c14"},{"author":"agent:codex","body":"M21 ALIGNMENT REPORT — validation only, not a review/approval of my own patch.\n\nInputs:\n- Submitted SL-328.p1 r3, applied 9f1b2063, unchanged lossless transport diff.\n- Proposed SL-2.p2 r1, base 3f324dd5, applied 0251e7f2, standalone tree 7ed2d512f4816a2ebc5d3ea9425d39912d2c64cc.\n- Exact combined tree d8a935eefc7c0b8a5390a307a151aed3232a922c at /Users/swoodtke/Projects/sawlang-m21-u0-validation.\n\nNo prototype source, oracle flags, fixture expectations, or runtime/depth limits were edited. All twelve formerly Python-debt oracle flags remain true. The comparison uses the fixed-SL350 compiler lineage, not the old baseline.\n\nCommands used main's .venv/bin/python from the combined checkout:\n1. sawc/sawc.py prototypes/minivm/src/main.saw --module-path sawlex=selfhost/lexer -o .build/minivm/minivm\n   Fresh miniVM, 18.72s, exit0. Initial setup lacked the output directory; after creating it the build succeeded. No compiler change or fallback was used.\n2. prototypes/parser/test_canonical.py --binary .build/minivm/minivm --artifacts .build/m21-u0-validation/canonical\n   50.06s, exit0: 38/38 canonical cases, 37/37 applicable Python parse-only comparisons, VM/O0/O2/ASan/sawc, plus the full-run arena-VM cross-check.\n3. The same canonical command with --debt-probe and a separate artifact directory.\n   18.55s, exit0: the strict Python comparison and five runtime modes pass, 1/1 case. This branch does NOT run the separate arena-VM cross-check; the shared success banner is broader than its executed branch.\n\nAll nine formerly failing Python comparisons were individually inspected: exit0 and empty stderr for grouped-identifier-callee, assignment-multiline-rhs, authored-depth-groups-256, authored-depth-call-256, authored-assignment-target-groups-256, authored-assignment-rhs-groups-256, authored-depth-if-256, authored-depth-else-if-256, authored-depth-if-mixed-256.\n\nThis resolves the requested nine-case + strict-debt measurement against U0 r1. It does not assert every M21 gate is green: the prototype N7 lexer-error anchor remains the old expectation, the inventory is stale against the changed example corpus, and the full arena/harness/corpus commands were not rerun in this measurement. Those need deliberate reconciliation against the accepted U0 revision.\n\nIndependent Sol/parent U0 review is posted at SL-2 c20 and requests changes despite the green alignment/server gate. U0's remaining depth/grammar/diagnostic defects are separate from this measured M21 result. SL-328 still requires the scheduled external independent review; no self-approval, merge, or main implementation commit was performed.\n\nLocal evidence: .build/m21-u0-validation/ under the combined checkout; parent session artifacts m21-u0-validation-identity.json, m21-u0-validation-results.json, m21-u0-nine-case-evidence.json.","created":"1790207694","id":"c15","kind":"report"},{"author":"agent:claude-sawlang","body":"INDEPENDENT REVIEW (lead, claude-sawlang), SL-328.p1 r3: COMMENT ONLY, not a verdict. Final approval waits on the U0′ rebase and gate rerun this patch requires.\n\nTree reviewed: r3 applied to base ef95c7af, write-tree e844581c. Nothing under prototypes/, selfhost/lexer or tools/dump_ast.py has changed on main since that base, so the patch still applies cleanly.\n\nMethod: read all of prototypes/parser/src/lib.saw and the canonical.saw If rendering. Built the prototype natively with production sawc and ran 33 edge sources through it and through Python's tools/dump_ast.py side by side.\n\nMatches Python byte for byte (canonical output):\n- an if in a condition, after a unary minus, in arguments (including a trailing comma), inside a group with else on the next line, in return, on either side of + and *\n- else and { on the next line\n- `if a { 1 }; b`\n- empty branches\n- else-if chains, including chains split across lines\n- `-3` on the line after a closed if starts the next statement (design 259 R2)\n- 1000 sibling ifs\nSame error anchor: `if a { 1 } b` (two statements on one line) and an if running into EOF.\n\nDepth: 256 nested ifs are accepted and 257 are refused. Python on current main hits RecursionError in ast_dump at 256; that is the known U0′ debt, and c15 measured those cases green against U0′. Nesting accounting reads correctly: each active if charges once, finish_if releases to active-1, a resumed frame snapshots the post-if level, and a block close restores the block's level.\n\nFindings, none blocking:\n1. A postfix call on an If value is accepted by the parser; only the renderer refuses it. `if a { f } else { g }(1)` and `(if a { f } else { g })(1)` parse as Call(If); Python says \"two statements on one line\". This is inherited M20 policy (`f(1)(2)` and `1(2)` behave the same), but M21 extends it to a statement that begins with `if`, which should never be a call. Either refuse `(` directly after an If value here, or make it an explicit row in SL-73's general-postfix-call matrix.\n2. A flat else-if chain of 257 or more arms is refused, because each `else if` charges one level. This follows the brief and U0′. The user has flagged the limit as undesirable, and a follow-up on representation is being discussed. No change is asked of M21 now.\n3. Performance, nonblocking: each block close scans every block_children entry recorded so far, and each call close scans every pending_args entry. Cost is quadratic in function size. It is fine for the tested inputs.\n4. Nits: `a if b {}` reports \"control expressions are not supported\", which misdirects (the real problem is a missing operator or separator). The --debt-probe success banner overstates what that branch runs; codex already noted this in c15.\n\nProbe scripts: sawlang lead checkout, .build/scratch/probe_m21.py and probe_m21b.py.\n","created":"1790253675","id":"c16"},{"author":"agent:claude-sawlang","body":"Deleted patch SL-328.p1\n\nSep 25 tracker cleanup (SL:tracker-cleanup), user ruling t3: M21 closes. The branch stays as a reference; SL:architecture §3.2 takes its fixtures and harness.","created":"1790341611","id":"c17"}],"created":"1789833800","description":"# M21: iterative control-flow parsing\n\nUnder SL-300, following merged M20 (SL-303). User authorized September 19:\nbuild this milestone in parallel with Claude's SL-2 Python parser alignment.\nThe final parity gate depends on that alignment; independent implementation\nand arena validation do not. This milestone changes the prototype only.\n\n## Scope\n\nReplace expression/block recursion with explicit continuations before admitting\nordinary `if condition { ... }`, optional `else { ... }`, and `else if` chains.\nAn if is an expression, usable in a binding, return, assignment RHS, argument,\noperator operand, condition, or block tail; the existing block statement/tail\nrules still apply. Branches contain the existing M20 statements and nested ifs.\nOptional binding (`if let`/`if var`), loops, match, closures, bare block\nexpressions, and other grammar remain cleanly refused.\n\nPreserve the existing expression grammar and precedence, newline continuation,\nassignment-only-at-statement-head rule, spans, and first-error funnel. Do not\ncopy Python's known grammar defects or silently consume adjacent expressions.\nIn particular, branch/else lookahead must not consume the newline needed to\nseparate the next statement. A leading minus after a newline following a closed\nif belongs to the next statement under design 259 R2. No production parser,\nmini-VM resource limit, lexer, or canonical schema changes belong here.\n\n## Representation and execution\n\nAdd an `If` arena node with empty text and ordered children: condition, then\nBlock, optional else Block. Anchor at `if`; span through its last branch.\nRepresent `else if` as an else Block containing a FinalExpression wrapping the\nnested If, matching Python's canonical shape; the synthetic block is anchored\nat the nested `if` and spans that nested expression. Existing arena invariants\nremain: backward edges, contiguous child ranges, unique child ownership,\ncomplete reachability, and parent span containment.\n\nUse one explicit parser work/continuation stack for suspended expression and\nblock work. Parsing syntactically nested ifs must not recurse through Saw/VM\ncall frames. Preserve the depth-256 expression contract and extend the same\nbudget to active nested if expressions: each active if adds one; groups,\nunary operators and argument delimiters retain their existing contributions.\nThe function body and the required branch braces do not add another charge\nfor the same if. Sequential siblings must release their budget. Pin 256/257\nfor pure if, else-if, and mixed if/group/call/unary paths, plus long shallow\nblocks/chains. Reconcile any conflict with SL-2's eventual depth convention\nexplicitly before final parity approval; never quietly change existing pins.\n\nCanonical rendering uses the existing IfExpr format: condition:, then:, and\noptional else: sections, normal Block rendering, using the renderer's iterative\nwork stack. Syntax-only acceptance must include semantically invalid conditions\nand branch value types; semantic rejection is not parser rejection.\n\n## Ownership and validation\n\nSol source agent owns `prototypes/parser/src/{lib,canonical}.saw`. Sol tests\nagent independently authors fixtures and harness checks. Sol inventory agent\nowns fresh eligibility analysis, classifier coverage and its tests. Each works\nin an isolated worktree. Parent owns this brief, review, integration, gates,\ntracker submission, and reconciliation with Claude's oracle work.\n\nTest ordinary/else-if/nested forms in every expression position; empty branches,\nstatement/tail distinctions, assignments and returns inside branches; operator\nprecedence and newline behavior; missing conditions/braces/else bodies and\nunsupported binding forms; exact spans, anchors, child order and invariants;\ndepth boundaries and long shallow inputs. Independently authored expectations\nmust not be generated from the new implementation's output.\n\nRun arena and canonical gates across VM, LLVM O0/O2, ASan, and production sawc;\nall harness tests; inventory regeneration/check; and the fresh eligible corpus\ndifferential. Report counts by verdict and compare newly eligible complete\nfiles. Existing Python disagreement remains a strict, visible debt probe, not\na tolerated divergence. Final M21 approval requires rebasing onto aligned\nPython, debt probes green, and canonical parity. If alignment has not landed,\nsubmit only as explicitly dependency-blocked work with validation limits clear.\n","id":"SL-328","labels":["prototype","selfhost","parser"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":18,"sequence":1818,"stage":"queued","status":"open","title":"M21: iterative control-flow parser and ordinary if/else","updated":"1790341611"},"id":"SL-328","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-328\",\"labels\":[\"superseded\"],\"request_id\":\"pid-56592-1790341676-9000\",\"revision\":18}","request_id":"pid-56592-1790341676-9000","sequence":2184,"timestamp":"1790341676"}
---
