---
{"action":"patch_add","actor":"agent:claude-air-sawlang","after":{"acceptance":[],"assignee":"agent:claude-air-sawlang","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"USER RULINGS (Sep 23): (1) the wake word becomes an Atomic with a CAS latch (option ii) — the poller's latch is CAS(parked → ready), the nested propagation is a CAS-guarded MERGE, the frame-layout/bt-table instruments follow the field's new type and the design-220 IR compare classifies that uniform change. (2) Sequencing: SL-350 lands FIRST (it never touches the park path; the race is latent on main regardless), then SL-355. (3) Implementer: the user launches an Opus 5.5 session on this brief directly (outside the lead's Claude Code, which does not know that model yet); the lead validates the submitted patch per the patch flow (the soak lane under oversubscription is the acceptance, plus irdet --all twice). Interim mitigation for any battery whose irdet lane stalls before SL-355 lands: rerun irdet with --jobs 2 (the two-worker row never stalled in the sweep) and record it as the SL-353 workaround, never as a pass of the stalled run.","created":"1790176694","id":"c1"},{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 1 (with SL-354) in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188478","id":"c2"},{"author":"agent:claude-air-sawlang","body":"USER RULING (Sep 23): unit 3 (R5, the Atomic wake word + CAS latch) is blocked on SL-368, a pointer-index Atomic receiver running on a spilled copy. The order is: fix SL-368 FIRST as its own patch, then finish unit 3 on top of it, then submit SL-355 COMPLETE as one patch. SL-355 does not land without its atomics. Built so far on sl355-park-word: units 1, 2, 4, 5 and 6. parksoak on the Air: unfixed main 88/640 stalls, the branch 0/640. The Linux soak did not run (no host).","created":"1790193011","id":"c3"},{"author":"agent:claude-air-sawlang","body":"USER RULING (Sep 23, revised): one combined patch covers SL-368 (the pointer-index receiver fix), SL-354 and SL-355, including unit 3's atomics, submitted here as SL-355.p1. SL-368 closes with that landing.","created":"1790193038","id":"c4"},{"author":"agent:claude-air-sawlang","body":"Submitted patch SL-355.p1 (revision 1): The park word is set before readiness is armed and merged, never re-stored; atomic wake word with a CAS latch; a failed arm is reported (SL-354); one receiver-place funnel for interior mutation through p[i] (SL-368)","created":"1790221536","id":"c5"}],"created":"1790176301","description":"# The park word is established before readiness is armed, and propagated by MERGE — never re-stored after a wake\n\nLead-authored fix brief for SL-353 (the lost-wake stall). Sweep evidence: SL-353's sweep-report comment (reproducer, ordering table, fixes A/B measured). PRIORITY HIGH: a hang in any multithreaded group whose tasks park on io or offloads — the battery's irdet lane is the in-tree victim (SL-350's battery stalled on it). RULINGS OWED from the user before dispatch, marked below.\n\n## 0. The mechanism (obligation 4: a CLASS, probed)\nONE funnel, TWO stores, wrong order and wrong merge. The reactor token is the ROOT frame's `__wake` word (`coro_transform.py:8149-8154`). (1) Both io-park lowerings ARM readiness first (`_emit_blk_call` `:7900-7904`; `io_wait`/`io_wait_until` `:6983-6987`) and write the park value -1 second (`_suspend_to` → `:6742-6743`), so a concurrent poller can latch 0 (`reactor.saw:177-181`) in between and the leaf store erases it. (2) The nested `Pending` propagation COPIES `sub.__wake` → `self.__wake` (`:8140-8143`) once per frame in the chain, after the arm, re-storing a child's -1 over a latched 0 — the DOMINANT clobber, which is why reordering the leaf alone (fix A) is measured insufficient. The MT re-scan re-reads the word but never re-arms, and the kevent was `EV_ONESHOT`, so the loss is permanent. Reproduced on the offload path and the `std.net` path; sleep, channel, cancel and join parks are not exposed (the table says why per path).\n\n## 1. The rules\n**R1 — established once, before the arm.** The park value is written through the ROOT token BEFORE readiness is registered — in `__saw_exec_io_register` itself (the one arm funnel), so no lowering can arm first. Every readiness store is then strictly later than the park store.\n**R2 — propagated by merge.** The nested `Pending` propagation never overwrites a root word that is not -1: a child's -1 is NOT re-stored; other wake reasons (deadline, cancel, wake-word values ≥ 0) propagate as today.\n**R3 — never re-stored after.** `_suspend_to` at an io park writes nothing the register funnel has already written (the `write_wake=False` shape), so the ordering rule has exactly one writer per park.\n**R4 — the funnel's docstring names its clients** (obligation 1): `_emit_blk_call`, the `io_wait`/`io_wait_until` lowering, the token-less sync path (`codegen/calls.py:593-596`, token = 0, exempt and says why), and the propagation arm. A battery-lane check (the `corodiscovery`/`corototality` pattern) fails on any io-park site that arms outside the funnel or stores the park word after it.\n**R5 — RULING OWED (a): the wake word's memory model.** Today the word is a plain frame field read and written from several threads with no atomics (`taskgroup.saw:358-366` documents the choice for the latch read). Fix B as prototyped writes through a raw token unsynchronized and makes `__saw_exec_io_register` `unsafe`. Options: (i) keep the plain word and rely on R1-R3's ordering (measured 0/480 in scratch; a data race in the C++ sense remains); (ii) make the wake word an `Atomic<Int>` with the poller's latch a CAS(-1 → 0) and the propagation a CAS-guarded merge — the sound answer under design 186's doctrine, at the cost of atomics on every park/poll and a frame-layout change (bt-table/frame-layout instruments, `--emit-frame-layout` consumers). The lead recommends (ii) unless the user rules the ordering argument sufficient for v1.\n**R6 — never hide errors (the secondary finding).** The arm discards `kevent`'s return at `reactor.saw:105/118/142/150` and `__saw_exec_io_register` returns `Void`; a failed arm becomes this bug's exact signature. The funnel reports a failed arm (a `Result`/status the executor turns into a panic naming the fd and the errno — a failed registration is a runtime invariant violation, not a recoverable condition), so the two failure modes are distinguishable forever after.\n\n## 2. Units\n1. **Pins first (obligation 3, conformance).** A conformance row for \"a parked task is woken exactly once and never lost\" with the oversubscribed reproducer as a SOAK lane (`tools/battery.sh` stage `parksoak`: N instances × 8 workers × the two stress programs under a watchdog, stall count must be 0 — an honest test NEEDS oversubscription; a serial example passes on the broken compiler). Plus the examples-level pin: the stress programs at modest size as ordinary suite tests (behaviour, not the race).\n2. **The funnel** (R1-R4) + the propagation merge, with the ordering check lane; per-commit gate; the 28 park-exercising examples byte-identical (the sweep's `check_examples.sh`).\n3. **R5 per the ruling** (atomics if (ii)), with the frame-layout instruments updated and `reemit`/`irdet`/`bttable` lanes green.\n4. **R6** — the arm reports; a pin that injects a failed `kevent` (a closed fd) and expects the panic naming it.\n5. **Linux/epoll** (`rt/host_linux/reactor.saw`): the same funnel serves it; the soak lane runs on a Linux host if one is available, else the brief says it was not run.\n6. Docs: LANGUAGE_SPEC's executor section (one sentence: the park word is established before readiness is armed; a wake is never lost); the saw-lang skill's \"precise wakeup\" bullet; todo.md.\n\n## 3. Obligations\n1 — the one funnel with named entries + the lane. 2 — consumer sweep of everything that reads or writes `__wake`: the two lowerings, the propagation, `__park_io_runnable`, the MT and ambient re-scans, the cancel path, `dump_tasks`'s renderer, the bt-table/frame-layout emitters (if R5 (ii) changes the field's type), design 91's precise-wake tests. 4 — the class is \"arm before park, copy after wake\"; the sweep's table is the matrix; a fix that leaves any io-park position outside the funnel is incomplete.\n\n## 4. Gates + sequencing\nCompiler + std + rt branch: per-commit full suite + freestanding both arches; terminal full battery incl. the new soak lane, `irdet --all` (the victim lane — run it TWICE), `corodiff`, `gmgate`. RULING OWED (b): sequence — after SL-350 lands (codex's battery must not race a runtime change), before the U3 follow-ups. RULING OWED (c): implementer — a Fable agent (a runtime protocol with a memory-model decision) is the lead's recommendation.\n","id":"SL-355","labels":["design","runtime","concurrency"],"order":1,"parent":"SL-367","priority":"high","project":"SL","queue_order":10,"revision":10,"sequence":1413,"stage":"queued","status":"open","title":"Design (fix brief for SL-353): the park word is established before readiness is armed and propagated by MERGE — never re-stored after a wake; rulings owed on the wake word's memory model, sequencing, implementer","updated":"1790221536"},"before":{"acceptance":[],"assignee":"agent:claude-air-sawlang","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"USER RULINGS (Sep 23): (1) the wake word becomes an Atomic with a CAS latch (option ii) — the poller's latch is CAS(parked → ready), the nested propagation is a CAS-guarded MERGE, the frame-layout/bt-table instruments follow the field's new type and the design-220 IR compare classifies that uniform change. (2) Sequencing: SL-350 lands FIRST (it never touches the park path; the race is latent on main regardless), then SL-355. (3) Implementer: the user launches an Opus 5.5 session on this brief directly (outside the lead's Claude Code, which does not know that model yet); the lead validates the submitted patch per the patch flow (the soak lane under oversubscription is the acceptance, plus irdet --all twice). Interim mitigation for any battery whose irdet lane stalls before SL-355 lands: rerun irdet with --jobs 2 (the two-worker row never stalled in the sweep) and record it as the SL-353 workaround, never as a pass of the stalled run.","created":"1790176694","id":"c1"},{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 1 (with SL-354) in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188478","id":"c2"},{"author":"agent:claude-air-sawlang","body":"USER RULING (Sep 23): unit 3 (R5, the Atomic wake word + CAS latch) is blocked on SL-368, a pointer-index Atomic receiver running on a spilled copy. The order is: fix SL-368 FIRST as its own patch, then finish unit 3 on top of it, then submit SL-355 COMPLETE as one patch. SL-355 does not land without its atomics. Built so far on sl355-park-word: units 1, 2, 4, 5 and 6. parksoak on the Air: unfixed main 88/640 stalls, the branch 0/640. The Linux soak did not run (no host).","created":"1790193011","id":"c3"},{"author":"agent:claude-air-sawlang","body":"USER RULING (Sep 23, revised): one combined patch covers SL-368 (the pointer-index receiver fix), SL-354 and SL-355, including unit 3's atomics, submitted here as SL-355.p1. SL-368 closes with that landing.","created":"1790193038","id":"c4"}],"created":"1790176301","description":"# The park word is established before readiness is armed, and propagated by MERGE — never re-stored after a wake\n\nLead-authored fix brief for SL-353 (the lost-wake stall). Sweep evidence: SL-353's sweep-report comment (reproducer, ordering table, fixes A/B measured). PRIORITY HIGH: a hang in any multithreaded group whose tasks park on io or offloads — the battery's irdet lane is the in-tree victim (SL-350's battery stalled on it). RULINGS OWED from the user before dispatch, marked below.\n\n## 0. The mechanism (obligation 4: a CLASS, probed)\nONE funnel, TWO stores, wrong order and wrong merge. The reactor token is the ROOT frame's `__wake` word (`coro_transform.py:8149-8154`). (1) Both io-park lowerings ARM readiness first (`_emit_blk_call` `:7900-7904`; `io_wait`/`io_wait_until` `:6983-6987`) and write the park value -1 second (`_suspend_to` → `:6742-6743`), so a concurrent poller can latch 0 (`reactor.saw:177-181`) in between and the leaf store erases it. (2) The nested `Pending` propagation COPIES `sub.__wake` → `self.__wake` (`:8140-8143`) once per frame in the chain, after the arm, re-storing a child's -1 over a latched 0 — the DOMINANT clobber, which is why reordering the leaf alone (fix A) is measured insufficient. The MT re-scan re-reads the word but never re-arms, and the kevent was `EV_ONESHOT`, so the loss is permanent. Reproduced on the offload path and the `std.net` path; sleep, channel, cancel and join parks are not exposed (the table says why per path).\n\n## 1. The rules\n**R1 — established once, before the arm.** The park value is written through the ROOT token BEFORE readiness is registered — in `__saw_exec_io_register` itself (the one arm funnel), so no lowering can arm first. Every readiness store is then strictly later than the park store.\n**R2 — propagated by merge.** The nested `Pending` propagation never overwrites a root word that is not -1: a child's -1 is NOT re-stored; other wake reasons (deadline, cancel, wake-word values ≥ 0) propagate as today.\n**R3 — never re-stored after.** `_suspend_to` at an io park writes nothing the register funnel has already written (the `write_wake=False` shape), so the ordering rule has exactly one writer per park.\n**R4 — the funnel's docstring names its clients** (obligation 1): `_emit_blk_call`, the `io_wait`/`io_wait_until` lowering, the token-less sync path (`codegen/calls.py:593-596`, token = 0, exempt and says why), and the propagation arm. A battery-lane check (the `corodiscovery`/`corototality` pattern) fails on any io-park site that arms outside the funnel or stores the park word after it.\n**R5 — RULING OWED (a): the wake word's memory model.** Today the word is a plain frame field read and written from several threads with no atomics (`taskgroup.saw:358-366` documents the choice for the latch read). Fix B as prototyped writes through a raw token unsynchronized and makes `__saw_exec_io_register` `unsafe`. Options: (i) keep the plain word and rely on R1-R3's ordering (measured 0/480 in scratch; a data race in the C++ sense remains); (ii) make the wake word an `Atomic<Int>` with the poller's latch a CAS(-1 → 0) and the propagation a CAS-guarded merge — the sound answer under design 186's doctrine, at the cost of atomics on every park/poll and a frame-layout change (bt-table/frame-layout instruments, `--emit-frame-layout` consumers). The lead recommends (ii) unless the user rules the ordering argument sufficient for v1.\n**R6 — never hide errors (the secondary finding).** The arm discards `kevent`'s return at `reactor.saw:105/118/142/150` and `__saw_exec_io_register` returns `Void`; a failed arm becomes this bug's exact signature. The funnel reports a failed arm (a `Result`/status the executor turns into a panic naming the fd and the errno — a failed registration is a runtime invariant violation, not a recoverable condition), so the two failure modes are distinguishable forever after.\n\n## 2. Units\n1. **Pins first (obligation 3, conformance).** A conformance row for \"a parked task is woken exactly once and never lost\" with the oversubscribed reproducer as a SOAK lane (`tools/battery.sh` stage `parksoak`: N instances × 8 workers × the two stress programs under a watchdog, stall count must be 0 — an honest test NEEDS oversubscription; a serial example passes on the broken compiler). Plus the examples-level pin: the stress programs at modest size as ordinary suite tests (behaviour, not the race).\n2. **The funnel** (R1-R4) + the propagation merge, with the ordering check lane; per-commit gate; the 28 park-exercising examples byte-identical (the sweep's `check_examples.sh`).\n3. **R5 per the ruling** (atomics if (ii)), with the frame-layout instruments updated and `reemit`/`irdet`/`bttable` lanes green.\n4. **R6** — the arm reports; a pin that injects a failed `kevent` (a closed fd) and expects the panic naming it.\n5. **Linux/epoll** (`rt/host_linux/reactor.saw`): the same funnel serves it; the soak lane runs on a Linux host if one is available, else the brief says it was not run.\n6. Docs: LANGUAGE_SPEC's executor section (one sentence: the park word is established before readiness is armed; a wake is never lost); the saw-lang skill's \"precise wakeup\" bullet; todo.md.\n\n## 3. Obligations\n1 — the one funnel with named entries + the lane. 2 — consumer sweep of everything that reads or writes `__wake`: the two lowerings, the propagation, `__park_io_runnable`, the MT and ambient re-scans, the cancel path, `dump_tasks`'s renderer, the bt-table/frame-layout emitters (if R5 (ii) changes the field's type), design 91's precise-wake tests. 4 — the class is \"arm before park, copy after wake\"; the sweep's table is the matrix; a fix that leaves any io-park position outside the funnel is incomplete.\n\n## 4. Gates + sequencing\nCompiler + std + rt branch: per-commit full suite + freestanding both arches; terminal full battery incl. the new soak lane, `irdet --all` (the victim lane — run it TWICE), `corodiff`, `gmgate`. RULING OWED (b): sequence — after SL-350 lands (codex's battery must not race a runtime change), before the U3 follow-ups. RULING OWED (c): implementer — a Fable agent (a runtime protocol with a memory-model decision) is the lead's recommendation.\n","id":"SL-355","labels":["design","runtime","concurrency"],"order":1,"parent":"SL-367","priority":"high","project":"SL","queue_order":10,"revision":9,"sequence":1351,"stage":"queued","status":"open","title":"Design (fix brief for SL-353): the park word is established before readiness is armed and propagated by MERGE — never re-stored after a wake; rulings owed on the wake word's memory model, sequencing, implementer","updated":"1790195300"},"id":"SL-355","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-air-sawlang\",\"diff\":\"diff --git a/.claude/skills/saw-lang/SKILL.md b/.claude/skills/saw-lang/SKILL.md\\nindex 20aca26f..5e186d83 100644\\n--- a/.claude/skills/saw-lang/SKILL.md\\n+++ b/.claude/skills/saw-lang/SKILL.md\\n@@ -2748,6 +2748,16 @@ dump_tasks()                // every live task's logical backtrace (std.task)\\n   another's event (`net_precise_wakeup`, `net_precise_n_readers`). Every parking op\\n   still re-checks its own fd and re-parks on a spurious wake, now purely\\n   belt-and-suspenders — you never see any of this, it is internal.\\n+  **A WAKE IS NEVER LOST (SL-355, Sep 23).** Before it, a task in a\\n+  `threads: N` group parked on a socket or a blocking-call offload could hang\\n+  forever with its fd readable (`dump_tasks()` showed it `io-parked` in\\n+  `TcpStream.read` / `Command.output`): the park was recorded AFTER the reactor\\n+  was armed and re-stored up the frame chain, erasing a readiness another\\n+  worker had just latched. Rare, and only under CPU oversubscription — the\\n+  battery's irdet lane was the victim. Treat such a hang as fixed now and\\n+  SUSPECT in older builds. And an `io_wait` on a closed fd now PANICS\\n+  (`reactor: arming fd N for read readiness failed (errno 9)`) instead of\\n+  parking forever.\\n - `extern \\\"C\\\" { blocking func f(...) -> T }` marks an unbounded FFI call: it\\n   SUSPENDS, is illegal in a `sync` context, and is rejected in the freestanding\\n   profile. An unannotated extern promises promptness. Design 103 (A6): a blocking\\ndiff --git a/CLAUDE.md b/CLAUDE.md\\nindex b9b815f2..81156d6b 100644\\n--- a/CLAUDE.md\\n+++ b/CLAUDE.md\\n@@ -174,8 +174,8 @@ objects are built + cached under `.build/rt/` and auto-linked (delete\\n   an untracked scratch file each session rewrote from this prose:\\n   ```bash\\n   SAW_PYTHON=/path/to/main/.venv/bin/python tools/battery.sh   # from a worktree\\n-  tools/battery.sh --quick        # skips the slow five (reemit/irdet/gmgate/\\n-                                  # bootstrap/freestanding)\\n+  tools/battery.sh --quick        # skips the slow six (parksoak/reemit/irdet/\\n+                                  # gmgate/bootstrap/freestanding)\\n   tools/battery.sh suite fuzz     # named stages\\n   tools/battery.sh --list\\n   ```\\n@@ -214,6 +214,8 @@ objects are built + cached under `.build/rt/` and auto-linked (delete\\n   that no longer resolves, or if either the typechecker or codegen half reaches\\n   past its ONE adapter, so the generic window statement a follow-up specifies adds\\n   syntax and a binding rather than a second implementation of root accounting),\\n+  `parkorder` (SL-355: every io park records its wake word, arms readiness and\\n+  suspends through one funnel; three injected SL-353 shapes must ICE),\\n   `stdseed` (SL-327: the design-206 std seed table is keyed by `Method.node_id`,\\n   and a wrong key costs a DIAGNOSTIC rather than a failure, so nothing in the\\n   corpus can see it — `tools/test_std_seed_keying.py` checks the rule directly:\\n@@ -233,7 +235,9 @@ objects are built + cached under `.build/rt/` and auto-linked (delete\\n   TESTING.md), `selfhostlex` (the selfhost lexer's own tests — the one tree\\n   the Aug-10 coverage sweep found NO stage ran), `floatvectors` (design\\n   253: the committed Float↔text vectors, bit-exact, plus the Ryū table\\n-  re-derivation), then the slow five\\n+  re-derivation), then the slow six\\n+  `parksoak` (SL-355: the park stress programs, oversubscribed under a\\n+  watchdog; any stall fails the lane — a serial run cannot see the race),\\n   `reemit` (design 221 A2: TWO compiles in ONE process, byte-comparing the\\n   unopt IR, the OPTIMIZED IR and the object — the optimized IR is the\\n   artifact DF-220a moved and the only one nothing checked),\\ndiff --git a/LANGUAGE_SPEC.md b/LANGUAGE_SPEC.md\\nindex 7977b8f4..ec247f59 100644\\n--- a/LANGUAGE_SPEC.md\\n+++ b/LANGUAGE_SPEC.md\\n@@ -8847,7 +8847,14 @@ registers its fd carrying the parked frame's wake-word ADDRESS as the event's\\n user-data (`kevent.udata` / `epoll_event.data`); on a ready event the poller\\n latches that word, and the scheduler resumes only the frame whose word changed. The\\n latch is a persistent word (not an edge), so a readiness that races the park is\\n-never lost. The token is per-PARK (the frame's own word), not per-fd-number, and\\n+never lost. That rests on an ordering rule (SL-355): the park is written into the\\n+word BEFORE readiness is armed, and nothing on the parking side writes the word\\n+after the arm, including the propagation of a nested frame's park up to the\\n+root frame whose word the token names. The word is an atomic, and the poller's\\n+latch is a compare-and-swap from parked to ready. A wake is therefore never lost, at any\\n+embedding depth, in a single- or multi-threaded group. An arm the poller refuses\\n+(a closed fd, one it cannot watch) panics naming the fd and the errno rather than\\n+leaving the task parked on an event that cannot arrive. The token is per-PARK (the frame's own word), not per-fd-number, and\\n one-shot interest plus close both drop a registration — so a reused fd number can\\n never route a wake to a stale frame. Level-vs-edge posture: interest is one-shot\\n per park (`EV_ONESHOT` / `EPOLLONESHOT`), and each re-park re-registers. Two frames\\ndiff --git a/TESTING.md b/TESTING.md\\nindex a6b6069c..4a8d0e18 100644\\n--- a/TESTING.md\\n+++ b/TESTING.md\\n@@ -379,7 +379,7 @@ adds or updates its conformance rows as its FIRST unit.\\n \\n ```bash\\n tools/battery.sh                     # everything\\n-tools/battery.sh --quick             # skip the slow lanes (reemit, irdet, gmgate, bootstrap, freestanding)\\n+tools/battery.sh --quick             # skip the slow lanes (parksoak, reemit, irdet, gmgate, bootstrap, freestanding)\\n tools/battery.sh suite fuzz          # named stages only\\n tools/battery.sh --list              # what the stages are\\n ```\\n@@ -402,6 +402,26 @@ being tracked: it used to be an untracked scratch file each session rewrote\\n from prose, and a gate list nobody can diff is a gate list that quietly loses\\n an entry.\\n \\n+### The parksoak stage — a race needs oversubscription\\n+\\n+```bash\\n+./.venv/bin/python tools/parksoak.py                     # the lane\\n+./.venv/bin/python tools/parksoak.py --waves 2 --keep    # shorter; keep every stalled run's dump\\n+```\\n+\\n+Conformance row K154 claims a parked task is woken exactly once and never lost.\\n+The two programs that check it (`examples/park_wake_offload_stress.saw`,\\n+`examples/park_wake_net_stress.saw`) also run in the suite, where they pass\\n+whether or not the claim holds: SL-353's lost wake needed a preemption inside a\\n+window a few instructions wide, and a program running alone is never preempted\\n+there. This lane runs both programs larger (the `PARKSOAK_*` variables), twice\\n+as many instances at once as the machine has cores, eight workers each, in\\n+sixteen waves. Every run has two watchdogs — its own OS thread, which prints\\n+`dump_tasks()` and exits 99, and the driver's wall-clock timeout — and either one\\n+counts a STALL. The gate is zero stalls and zero wrong totals. On the compiler\\n+before SL-355 the default grid stalled 88 of 640 runs; a stall costs its wave the\\n+watchdog's 30 seconds, so a broken tree makes this lane slow as well as red.\\n+\\n ### The citations stage — the files nothing compiles\\n \\n ```bash\\ndiff --git a/designs/todo.md b/designs/todo.md\\nindex a35638b0..b9af4e3a 100644\\n--- a/designs/todo.md\\n+++ b/designs/todo.md\\n@@ -46,6 +46,8 @@ is scheduled and in what order is the whole of what they say.\\n - SL-347 — `;` is the one-line statement separator; bare juxtaposition is refused; a brace body is one line or fully broken (brief in the tracker issue; RULED Sep 22 by the user). Lands as TWO patches (lead rescope, Sep 22): p1 is the statement-end chokepoint + R1/R2 + the pins + docs, and may land ahead of SL-318.p7; p2 is the corpus sweep (U2) plus R3's juxtaposition refusal (U1b), on a branch from the post-U3 main. Sequenced BEFORE SL-335 and the design-275 U3 follow-ups, which would otherwise carry the sweep's conflicts\\n   - **p2 BUILT Sep 22 (branch `sl347`, awaiting the lead's validation + patch).** Three commits in order. (1) THE LOOKAHEAD RESTORE, which had to come first: `parse_if_expression` (both arms), the inline `try … catch` and the `module name { … }` head each `skip_newlines()` to peek for a keyword and did NOT rewind on the miss, so a plain multi-line `if` left its own terminator consumed and the next statement reached the chokepoint looking as if it shared the `if`'s line — 6726 apparent juxtaposition sites over the corpus against 246 real ones. Each saves and restores the position, the shape `while let`'s `else` refusal and a trait method's default body already used; behaviour-neutral (the enclosing loop skips the newline anyway), measured by the dry run dropping to exactly the 246 and the suite unchanged. (2) THE SWEEP, with the COMPILER as the oracle — the chokepoint monkeypatched to RECORD rather than refuse, run to a fixpoint: 244 `.saw` sites in 94 files (examples 176, conformance 52, errors 11, sawc/std 5, prototypes 2; ZERO in blade/, libs/, devtools/, tests/, selfhost/, sawc/rt/, builtin.saw), 2 more inside a string INTERPOLATION swept by hand (a sub-parser numbers its own tokens from 1:1, so the recorder cannot place them), 27 in documentation fences (spec 13, skill 14, README 0 — each column rebased by its fence's indent and checked against the token it must precede), and 5 in skill fragments neither docverify shape can parse, found by a residue scan. R5's half-broken layouts reformatted in the same commit and listed (3 in examples/, 21 minivm fixtures); the 4 hits left are `import m.{...}` symbol lists, which design 147 makes a delimited list. NO EXPECT line changed. (3) R3: `expect_statement_end` refuses anything but NEWLINE / `;` / `}` / EOF, anchored at the SECOND statement's first token and naming BOTH statements' heads; module scope gets the declarations message; a `match` arm's BARE body is untouched (`parse_match` owns its `,`). Position matrix over 13 rows in four `examples/errors/` pins (the parse loop batches ten errors, so the matrix is split rather than truncated), one row per block shape plus the closure-body and module-scope entries. Obligation 2, each with evidence: `tools/sawfuzz_known.txt` holds ONE entry (DF-270e, a `UIntN` ICE) and nothing rests on juxtaposition; the only place sawc synthesizes Saw SOURCE TEXT is `_parse_expression_from_string` (interpolation), re-parsing text the author wrote — the coroutine transform, the `#lend_var` fold, the std seed and `--emit-docs` are all AST or JSON; 5 of the 7 `devtools/dogfood` programs fail to compile and fail IDENTICALLY on main (design 234 allocator-tier drift, no parse error among them)\\n \\n+- SL-355 — the lost-wake fix for SL-353 (+ SL-354): the park word is established before readiness is armed and propagated by MERGE (brief in the tracker issue; RULED Sep 23: atomic wake word with a CAS latch, after SL-350).\\n+  - **BUILT Sep 23, WITH SL-368 (branch `sl355-park-word`, one combined patch, awaiting the lead's validation).** U3 DONE on top of SL-368: `__wake` is an `Atomic<Int>` frame field (same offset and size, so frame layout and the bt table are unchanged), every synthesized access is `load`/`store`, the register funnel stores the park through `UnsafePointer<Atomic<Int>>`, and both reactors latch with a CAS(-1 -> 0). SL-368 (the blocker below) is fixed in its own commit: `_receiver_place_pointer` is the one receiver-place funnel (conformance U37). U1: conformance K154 + `examples/park_wake_{offload,net}_stress.saw` + the `parksoak` lane (`tools/parksoak.py`; pre-fix 88/640 stalls on a 10-core Air, post-fix 0/640). U2: R1-R4 — `__saw_exec_io_register` writes the park through the token before it arms; `_emit_io_park` is the one io-park emitter; `_pending_merge` never re-stores a child's park over the root word; `_verify_park_order` ICEs on any violation, and the `parkorder` lane injects three SL-353 shapes. U4: R6 — `__saw_rt_reactor_register` returns 0/errno (ABI.md), the funnel panics naming fd + errno; two closed-fd pins. U5: the epoll register reports too; compiled for x86_64/aarch64 Linux, Linux soak NOT run (no host). U6: spec + skill. U3 (R5, the `Atomic<Int>` word + CAS latch) WAS BLOCKED on a compiler defect (SL-368, now fixed): an `Atomic` method called on a pointer-INDEX receiver (`p[0].store(v)`, `p[0].compare_exchange(a, b)` with `p: UnsafePointer<Atomic<Int>>`) operates on a spilled copy — `_receiver_storage_pointer` (codegen/calls.py) handles no `ArrayIndex` — so the store is lost and the CAS reports success without writing. Pre-existing on main (lead bisect: 73dd98b8 and 455dae1d).\\n - SL-333 — `borrows` signatures name the lent reference and its mode (brief in the tracker issue; RULED Sep 20 by the user, r2 folds codex's review). QUEUED after design 275 U2 and BEFORE U3, which writes the borrowing-struct spec text against this spelling. **LANDED Sep 21 (SL-333.p1 r5, merge 47373c08; also closed SL-334).** `borrows -> &T` / `-> &var T` (and the `?` twins): the DECLARATION states the maximum window mode, `&var self` is required to lend `&var` (R2), the use site narrows within it (R3), and a read-only lend exists at last — which is what `libs/toml`'s document views and `Map`'s key probe wanted and design 141 could not express. R4 separates the LENT VALUE's mode from the RECEIVER's required access: an accessor is SHARED-ELIGIBLE iff its shared specialization passes the ordinary receiver-permission rules, recorded by `_self_borrow_is_exclusive`'s own five entry points (which now RECORD as well as refuse) onto a declared annotation field and read at every call site by the one window funnel — so `let v` plus `v[0]` still compiles under R2, while an accessor that mutates `self` outside the place it lends is refused on a `let` root by name and two of its read-only windows on one `var` root conflict. R5 states the reference-in-a-return-position rule once, in the parser's return-clause funnel, leaving U3 only its field clause. R6 deletes the `&var self` fence on `#lend_var` and re-keys the two-specialization split from the receiver spelling onto the DECLARED MODE, with eligibility computed on the shared copy AFTER the fold — so `Data.[]`'s copy-on-write gate costs an ordinary read nothing. R7 gives a FORWARDED lend the enclosing specialization's mode, spending design 179's one stated v1 limit (a nested CoW read no longer copies) with two refusals falling out of the same fact. `place_uses._window_call` now names its seven author-visible entries and reads both declaration facts at every one; `--emit-docs` carries the mode and the eligibility. Corpus: 105 declarations (sawc/std 12, libs 3, examples 90), blade and libs/*/tests unchanged. Conformance rows K123-K130. TWO OUTCOMES CHANGED, both by a rule: `lend_var_coro_and_forwarding.saw` (R7's gate counts) and `errors/lend_var_ungated_receiver_mutation.saw` (R4 moved the refusal from the declaration to the use site); three pins were re-keyed or repurposed (`errors/lend_var_in_var_self_accessor.saw` -> `lend_var_in_shared_lend_accessor.saw`, conformance R33, `errors/ref_return_method.saw`'s fixit text).\\n \\n ## [BACKLOG] — filed, not scheduled\\ndiff --git a/examples/atomic_pointer_index_two_thread_cas.saw b/examples/atomic_pointer_index_two_thread_cas.saw\\nnew file mode 100644\\nindex 00000000..0345afcb\\n--- /dev/null\\n+++ b/examples/atomic_pointer_index_two_thread_cas.saw\\n@@ -0,0 +1,39 @@\\n+// A compare-and-swap through a POINTER-INDEX receiver, from two threads,\\n+// counts every increment exactly once (SL-368, conformance row U37).\\n+//\\n+// This is the shape SL-355's reactor latch uses: a word reached as\\n+// `p[0]` through an `UnsafePointer<Atomic<Int>>` rebuilt from an address.\\n+// Before SL-368, `p[0].compare_exchange(seen, seen + 1)` ran on a spilled\\n+// COPY of the element, so it always reported success and never wrote. Each\\n+// thread's loop \\\"succeeded\\\" at every step, and the shared word stayed 0.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// total=40000\\n+\\n+extern \\\"C\\\" {\\n+    func calloc(count: Int, size: Int) -> UnsafePointer<Int8>\\n+}\\n+\\n+static PER_THREAD: Int = 20000\\n+\\n+// Increment the word at `addr` PER_THREAD times with a CAS retry loop.\\n+func hammer(addr: Int) unsafe {\\n+    let p = addr as UnsafePointer<Atomic<Int>>\\n+    var done = 0\\n+    while done < PER_THREAD {\\n+        let seen = p[0].load()\\n+        if p[0].compare_exchange(seen, seen + 1) {\\n+            done = done + 1\\n+        }\\n+    }\\n+}\\n+\\n+func main() unsafe {\\n+    let raw = calloc(1, 8)\\n+    let addr = raw as Int\\n+    var a = Thread.spawn { [addr] in hammer(addr) }\\n+    var b = Thread.spawn { [addr] in hammer(addr) }\\n+    a.join()\\n+    b.join()\\n+    print(\\\"total={}\\\", (raw as UnsafePointer<Int>)[0])\\n+}\\ndiff --git a/examples/conformance/INDEX.md b/examples/conformance/INDEX.md\\nindex 7adfa7ec..52be255e 100644\\n--- a/examples/conformance/INDEX.md\\n+++ b/examples/conformance/INDEX.md\\n@@ -6,6 +6,11 @@ either a file in this directory or an existing `examples/` test that already\\n asserts the same rule at the same position — this table is the record of which,\\n and the dedup decisions are meant to be audited from it.\\n \\n+**577 rows: 297 carry a file here, 280 are covered elsewhere** (recounted Sep 23\\n+when SL-368 added U37, interior mutation through every receiver place, covered by\\n+two `examples/` pins; previously 576, when SL-355 added K154, the never-lost\\n+wake, covered by two `examples/` pins and the `parksoak` battery lane);\\n+previously, and on the same basis:\\n **575 rows: 297 carry a file here, 278 are covered elsewhere** (recounted Sep 22\\n when design 275 U3 added K132-K153, the statement-scoped window and the two\\n missing splits — K133, K135, K136, K137, K138, K140, K141 and K147 carry a file\\n@@ -513,6 +518,7 @@ Claim source: spec 10 *Unsafe Code*; designs 130, 136, 149, 188 u6\\n | U34 | the same through a method's implicit `self` | `U34_funcpointer_capture_self_error.saw` | 226 — the DF-216a lesson: a receiver appears in no capture list, and a rule written against the list would have accepted it |\\n | U35 | `F` must be `sync` — refused at the TYPE, wherever one is written | `errors/funcpointer226_arg_suspending.saw` | 226 — a suspending body runs out of a frame and a bare code address has nowhere to keep one |\\n | U36 | receiving and holding a `FuncPointer` needs no `unsafe`; only `from_raw` does | `funcpointer226_type.saw` | 226 — design 130's rule 7 satisfied by the closed-construction argument; the Vector precedent applied properly (safe type, one unsafe member) |\\n+| U37 | INTERIOR MUTATION REACHES THE STORAGE ITS RECEIVER NAMES — an `Atomic` op or a cell's `ptr()` through a pointer element `p[i]` (mutable or const pointer), a fixed-array element, a `Vector` place, a `borrows` accessor, a tuple element, a field of `p[i]`, and inside a suspending body, never runs on a spilled copy; a CAS through `p[0]` from two threads counts every increment | `interior_mutation_receiver_places.saw` + `atomic_pointer_index_two_thread_cas.saw` | SL-368. The compiler-known interior-mutability ops kept a receiver dispatch of their own with no ELEMENT case: `p[0].store(5)` stored into a copy, and `compare_exchange` reported success without writing (the two-thread CAS counted 0 of 40000). Pre-existing, and not caused by SL-350: the lead bisected it to both 73dd98b8 and 455dae1d. Fixed by routing every by-pointer receiver through ONE funnel (`_receiver_place_pointer` -> `_get_lvalue_pointer`). The `&var self` / `&self` method rows, SpinLock and Mutex were already right through the method path's own arm, and they are pinned beside the rows that were wrong |\\n \\n ## Always-on runtime checks\\n \\n@@ -753,6 +759,7 @@ Claim source: spec 6 *Send and Sync* + *Cooperative tasks*; designs 75, 88, 103,\\n | K147 | the INLINE `try EXPR catch { … }` SPLITS — a suspending call in its catch embeds, and a `break`/`continue` written there reaches the loop it names | `K147_inline_catch_splits.saw` + `coro_suspending_call_in_inline_catch.saw` + `coro_inline_catch_loop_exit_in_child_positions.saw` | SL-215, closed by design 275 U3. A `TryExpr` carrying a `catch_block` is the inline form, and nothing in the split walk ever looked at that slot: `_collect_calls`'s try/catch arm matched `TryCatchExpr` only, so a suspending call in the catch fell to the one rejector. THE SAME SLOT COST A SILENT MISCOMPILE beside the refusal — `_mark_ob_block`'s clause-2 marking lists `IfExpr`, `MatchExpr` and `TryCatchExpr` and not `TryExpr`, so a `try f() catch { break }` inside a spanning loop kept a RAW `break` that left the resume method's `while true` DISPATCH loop: the break was lost and a `continue` spun forever, at exit 0. The transform rewrites the inline form into the BLOCK form it means (`try { try EXPR } catch { B }`), so design 196 unit 3's split does the work and both faces close at once. Rows: the suspending catch, the ERROR BINDING across the suspension, the three `try` variants in one body, a routed `try(as E.Case)` beside an inline catch, and a catch that `break`s or `continue`s its loop — each against its sync twin. Each rewrite takes a FRESH error binding (`__saw_caughtN`) with the author's `error` reads renamed, because the block form's caught error travels in a frame field NAMED by the binding and two normalized catches in one body would otherwise share one field — measured as ``argument `value` expects `Alpha` but got `Beta``, and sawtracker's accept loop has exactly two. AT EVERY CHILD POSITION (codex r3 #1): the normalization walked its children by hand and stopped at anything not an `ASTNode` — `Argument` is a plain dataclass, a struct-literal field and a map entry are tuples — so an inline catch inside `id(…)`, `Pair(a: …)`, `{k: …}` or `read(&a[…])` was never rewritten and its raw `break` left the dispatcher (the code after the loop skipped, or the driver re-entering forever); it rides `ast_walk.map_nodes` now. The SAME symptom sat one mechanism over: the value-conditional lowering asked only \\\"spans a suspension\\\", so a normalized `try { } catch { break }` (and a value `if`/`match` arm that jumps) in a `let` or an argument inside a spanning loop lowered in place — `_vc_spans` asks the loop-exit clause too. The child-positions file is the matrix: every wrapper the funnel names × `break`/`continue` × `while`/collection `for`, plus the suspending catch, each against its sync twin |\\n | K148 | DF-196b's multi-error refusal is inherited whole by the inline form | `errors/inline_catch_multi_error.saw` | design 275 U3 / SL-215. A suspending `try { } catch { }` may raise only ONE error type — the catch would bind a union the split lowering cannot build — and a one-expression inline catch satisfies that by construction rather than by an exemption |\\n | K149 | the MOTIVATING CONSUMER's loop shape compiles and runs: a collection `for` whose body SUSPENDS, PROPAGATES an error, and writes a root that is not the one being iterated | `K149_sawtracker_loop_shape.saw` | SL-317's own repro, sawtracker `src/store.saw:285`. Three things at once, which is why it is the shape worth pinning rather than the minimal one; it was the clean refusal \\\"use a `while` loop over an index\\\" before U3. The io is replaced by a suspending read so the row runs in the ordinary battery — the SHAPE is the assertion, not the syscall — and the sync twin is the oracle |\\n+| K154 | A PARKED TASK IS WOKEN EXACTLY ONCE AND NEVER LOST — a readiness event that arrives while an io park (a socket op, a blocking-extern offload) is still being recorded is never erased by the parking side, at any embedding depth, in a multi-threaded group | `park_wake_offload_stress.saw` + `park_wake_net_stress.saw` (behaviour, modest size) + the `parksoak` battery lane (`tools/parksoak.py`: the same two programs, oversubscribed, under two watchdogs; the stall count must be 0) | SL-353 / SL-355. The suite files alone pass on the broken compiler — the window is a few instructions wide and only a preemption inside it loses the wake — so the LANE is the row's oracle: pre-fix it stalled 88 of 640 runs (net 58/320, offload 30/320, 20 instances x 8 workers on 10 cores). The rule it checks: the park word is established through the root token BEFORE readiness is armed (`__saw_exec_io_register`), the nested Pending propagation MERGES rather than re-storing a child's park, and the poller's latch is a CAS from parked to ready on an `Atomic<Int>` word |\\n \\n \\n ## Visibility and module boundaries\\ndiff --git a/examples/interior_mutation_receiver_places.saw b/examples/interior_mutation_receiver_places.saw\\nnew file mode 100644\\nindex 00000000..87dcd7de\\n--- /dev/null\\n+++ b/examples/interior_mutation_receiver_places.saw\\n@@ -0,0 +1,152 @@\\n+// Interior mutation through EVERY receiver place reaches the storage the place\\n+// names, never a copy of it (SL-368, conformance row U37).\\n+//\\n+// The compiler-known interior-mutability operations (the four `Atomic` methods\\n+// and a cell's `ptr()`) used to address their receiver through a dispatch of\\n+// their own that knew bindings, `self`, fields and tuple elements but not\\n+// ELEMENTS. `p[0].store(5)` over an `UnsafePointer<Atomic<Int>>` spilled the\\n+// element, stored into the copy and threw it away, and `compare_exchange`\\n+// reported SUCCESS without writing, so a CAS loop through a pointer\\n+// \\\"succeeded\\\" forever while nothing changed. A fixed-array element and a\\n+// suspending body lost the store the same way. Ordinary `&var self` and `&self`\\n+// methods reached the element through their own arm and were right all along.\\n+// That is why SpinLock and Mutex, whose operations are Saw methods, never\\n+// showed it. Both paths now go through one receiver-place funnel.\\n+//\\n+// Each row reads back through a path that does not share the receiver's code:\\n+// a raw `Int` pointer, a second call, or a field read.\\n+// A Data place has no row: its elements are `UInt8`, which has no method that\\n+// mutates through its receiver.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// A1 store: pointee=5 load=5\\n+// A1 cas: ok=true pointee=9\\n+// A1 stale cas: ok=false pointee=9\\n+// A1 fetch_add: pointee=10\\n+// A2 const-pointer store: pointee=6\\n+// A3 fixed array: load=7\\n+// A4 vector place: load=8\\n+// A5 user borrows accessor: load=9\\n+// A6 tuple index: load=10\\n+// A7 field of p[i]: pointee=11\\n+// B1 &var self via p[0]: pointee=1\\n+// B2 &self cell method via p[0]: pointee=1\\n+// C1 spinlock via p[0]: payload=1 locked-after=false\\n+// C2 mutex via p[0]: payload=1\\n+// D1 suspending body: 2122\\n+// E1 cell ptr() via p[0]: same-address=true\\n+\\n+import std.spinlock.{SpinLock}\\n+import std.mutex.{Mutex}\\n+import std.task.{yield_now}\\n+\\n+extern \\\"C\\\" {\\n+    func calloc(count: Int, size: Int) -> UnsafePointer<Int8>\\n+}\\n+\\n+struct Counter {\\n+    n: Int\\n+}\\n+\\n+extension Counter {\\n+    func bump(&var self) { self.n = self.n + 1 }\\n+}\\n+\\n+struct Hits {\\n+    a: Atomic<Int>\\n+}\\n+\\n+extension Hits: NoCopy {}\\n+\\n+extension Hits {\\n+    func hit(&self) { let _ = self.a.fetch_add(1) }\\n+}\\n+\\n+struct Cells {\\n+    slots: [Int; 2]\\n+    word: Atomic<Int>\\n+}\\n+\\n+extension Cells: NoCopy {}\\n+\\n+extension Cells {\\n+    func word_place(&self) borrows -> &Atomic<Int> {\\n+        lend self.word\\n+    }\\n+}\\n+\\n+// The coroutine transform rewrites a suspending body's locals into frame\\n+// fields; a pointer-index receiver must still address the pointee.\\n+func suspending_rows(p: UnsafePointer<Atomic<Int>>, plain: UnsafePointer<Int>) unsafe -> Int {\\n+    p[3].store(21)\\n+    yield_now()\\n+    let before = plain[3]\\n+    p[3].store(22)\\n+    yield_now()\\n+    before * 100 + plain[3]\\n+}\\n+\\n+func main() unsafe {\\n+    let raw = calloc(16, 8)\\n+    let plain = raw as UnsafePointer<Int>\\n+    let p = raw as UnsafePointer<Atomic<Int>>\\n+\\n+    p[0].store(5)\\n+    print(\\\"A1 store: pointee={} load={}\\\", plain[0], p[0].load())\\n+    let ok = p[0].compare_exchange(5, 9)\\n+    print(\\\"A1 cas: ok={} pointee={}\\\", ok, plain[0])\\n+    let stale = p[0].compare_exchange(5, 99)\\n+    print(\\\"A1 stale cas: ok={} pointee={}\\\", stale, plain[0])\\n+    let _ = p[0].fetch_add(1)\\n+    print(\\\"A1 fetch_add: pointee={}\\\", plain[0])\\n+\\n+    let cp = raw as UnsafeConstPointer<Atomic<Int>>\\n+    cp[1].store(6)\\n+    print(\\\"A2 const-pointer store: pointee={}\\\", plain[1])\\n+\\n+    var arr: [Atomic<Int>; 2] = [Atomic(0), Atomic(0)]\\n+    arr[1].store(7)\\n+    print(\\\"A3 fixed array: load={}\\\", arr[1].load())\\n+\\n+    var v = Vector<Atomic<Int>>()\\n+    try! v.push(Atomic(0))\\n+    v[0].store(8)\\n+    print(\\\"A4 vector place: load={}\\\", v[0].load())\\n+\\n+    var cells = Cells(slots: [0, 0], word: Atomic(0))\\n+    cells.word_place().store(9)\\n+    print(\\\"A5 user borrows accessor: load={}\\\", cells.word.load())\\n+\\n+    var t = (1, Atomic(0))\\n+    t.1.store(10)\\n+    print(\\\"A6 tuple index: load={}\\\", t.1.load())\\n+\\n+    let hp = raw as UnsafePointer<Hits>\\n+    hp[2].a.store(11)\\n+    print(\\\"A7 field of p[i]: pointee={}\\\", plain[2])\\n+\\n+    let cnt = (raw + 32) as UnsafePointer<Counter>\\n+    cnt[0].bump()\\n+    print(\\\"B1 &var self via p[0]: pointee={}\\\", plain[4])\\n+\\n+    let hp2 = (raw + 40) as UnsafePointer<Hits>\\n+    hp2[0].hit()\\n+    print(\\\"B2 &self cell method via p[0]: pointee={}\\\", plain[5])\\n+\\n+    // The zero pattern is an unlocked lock over a zeroed payload.\\n+    let sp = (raw + 48) as UnsafePointer<SpinLock<Int>>\\n+    sp[0].lock({ &var n in n = n + 1 })\\n+    let sv = sp[0].lock({ &var n in n })\\n+    print(\\\"C1 spinlock via p[0]: payload={} locked-after={}\\\", sv, sp[0].is_locked())\\n+\\n+    let mp = (raw + 64) as UnsafePointer<Mutex<Int>>\\n+    mp[0].lock({ &var n in n = n + 1 })\\n+    let mv = mp[0].lock({ &var n in n })\\n+    print(\\\"C2 mutex via p[0]: payload={}\\\", mv)\\n+\\n+    print(\\\"D1 suspending body: {}\\\", suspending_rows(p, plain))\\n+\\n+    let cellp = (raw + 96) as UnsafePointer<UnsafeMutableInterior<Int>>\\n+    let addr = cellp[0].ptr() as Int\\n+    print(\\\"E1 cell ptr() via p[0]: same-address={}\\\", addr == ((raw + 96) as Int))\\n+}\\ndiff --git a/examples/park_wake_net_stress.saw b/examples/park_wake_net_stress.saw\\nnew file mode 100644\\nindex 00000000..2c3780fb\\n--- /dev/null\\n+++ b/examples/park_wake_net_stress.saw\\n@@ -0,0 +1,84 @@\\n+// A task parked on a SOCKET read is woken exactly once and never lost (SL-353 /\\n+// SL-355, conformance row K154).\\n+//\\n+// The `std.net` twin of `park_wake_offload_stress.saw`: the socket park and the\\n+// offload park are one lowering (arm the reactor, park, propagate the park up\\n+// the embedded frames), so the SL-353 lost wake reached both. Two tasks per\\n+// connected pair own one end each and ping-pong a byte, so every read really\\n+// PARKS and its readiness is made by a peer running on another worker thread.\\n+//\\n+// In the suite this is a behaviour pin at modest size; the `parksoak` battery\\n+// lane runs it oversubscribed and larger through the `PARKSOAK_*` variables.\\n+// The watchdog thread dumps every live task and exits 99 if the program stalls.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// total=800\\n+\\n+import std.net\\n+import std.env\\n+import std.task.{dump_tasks}\\n+\\n+extern \\\"C\\\" {\\n+    blocking func usleep(us: UInt32) -> Int32\\n+    func _exit(code: Int32)\\n+}\\n+\\n+func env_int(name: String, fallback: Int) -> Int {\\n+    if let s = env.Env.get(name) {\\n+        if let v = s.to_int() {\\n+            return v\\n+        }\\n+    }\\n+    return fallback\\n+}\\n+\\n+// One end of a pair: `rounds` one-byte exchanges, the starter writing first.\\n+func side(s: net.TcpStream, rounds: Int, starter: Bool) -> Int {\\n+    var stream = move s\\n+    var total = 0\\n+    var i = 0\\n+    while i < rounds {\\n+        if starter {\\n+            try! stream.write(\\\"x\\\")\\n+            total = total + (try! stream.read()).len()\\n+        } else {\\n+            total = total + (try! stream.read()).len()\\n+            try! stream.write(\\\"x\\\")\\n+        }\\n+        i = i + 1\\n+    }\\n+    return total\\n+}\\n+\\n+func main() {\\n+    let threads = env_int(\\\"PARKSOAK_THREADS\\\", 4)\\n+    let pairs = env_int(\\\"PARKSOAK_TASKS\\\", 2)\\n+    let rounds = env_int(\\\"PARKSOAK_ITERS\\\", 200)\\n+    let watchdog = env_int(\\\"PARKSOAK_WATCHDOG\\\", 20)\\n+    var wd = Thread.spawn { [watchdog] in\\n+        var left = watchdog\\n+        while left > 0 {\\n+            let _ = usleep(1000000 as UInt32)\\n+            left = left - 1\\n+        }\\n+        print(\\\"WATCHDOG fired\\\")\\n+        dump_tasks()\\n+        _exit(99 as Int32)\\n+    }\\n+    wd.detach()\\n+    var group = try! TaskGroup(threads: threads)\\n+    var handles = Vector<Task<Int>>()\\n+    var p = 0\\n+    while p < pairs {\\n+        let (a, b) = net.TcpStream.pair()\\n+        try! handles.push(group.spawn(side(move a, rounds, true)))\\n+        try! handles.push(group.spawn(side(move b, rounds, false)))\\n+        p = p + 1\\n+    }\\n+    var total = 0\\n+    while handles.len() > 0 {\\n+        let h = handles.pop()!\\n+        total = total + h.join()\\n+    }\\n+    print(\\\"total={}\\\", total)\\n+}\\ndiff --git a/examples/park_wake_offload_stress.saw b/examples/park_wake_offload_stress.saw\\nnew file mode 100644\\nindex 00000000..56b1b448\\n--- /dev/null\\n+++ b/examples/park_wake_offload_stress.saw\\n@@ -0,0 +1,87 @@\\n+// A task parked on a blocking-extern OFFLOAD is woken exactly once and never\\n+// lost (SL-353 / SL-355, conformance row K154).\\n+//\\n+// Many short `Command.output()` calls from a multi-threaded group: each one\\n+// offloads the child's stdout read to a worker thread and parks the task on the\\n+// job's pipe, one embedded frame below the task's root. The lost wake SL-353\\n+// found lived there — the reactor was ARMED before the park word was written,\\n+// and the nested frame's Pending propagation then re-stored the child's `-1`\\n+// over a readiness latch a concurrent poller had already made — so a stalled run\\n+// parks forever with its pipe readable and nobody registered on it.\\n+//\\n+// At this size, in the ordinary suite, the program is a BEHAVIOUR pin: every\\n+// child's output arrives and the total is exact. The race itself needs CPU\\n+// oversubscription to reach (the window is a few instructions wide), which is\\n+// what the `parksoak` battery lane supplies: it runs this same file many times\\n+// at once, larger, through the `PARKSOAK_*` variables below.\\n+//\\n+// The watchdog is an OS thread, so it fires even when every worker is wedged:\\n+// it prints the task dump (where each live task is parked) and exits 99, which\\n+// is how a stall is told apart from a crash or a slow run.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// total=64\\n+\\n+import std.process\\n+import std.env\\n+import std.task.{dump_tasks}\\n+\\n+extern \\\"C\\\" {\\n+    blocking func usleep(us: UInt32) -> Int32\\n+    func _exit(code: Int32)\\n+}\\n+\\n+func env_int(name: String, fallback: Int) -> Int {\\n+    if let s = env.Env.get(name) {\\n+        if let v = s.to_int() {\\n+            return v\\n+        }\\n+    }\\n+    return fallback\\n+}\\n+\\n+// One task's share: `iters` children, each printing \\\"x\\\\n\\\" (two bytes).\\n+func child_burst(iters: Int) -> Int {\\n+    var total = 0\\n+    var i = 0\\n+    while i < iters {\\n+        var c = process.Command(program: \\\"/bin/echo\\\")\\n+        try! c.arg(\\\"x\\\")\\n+        if let o = try! c.output() {\\n+            total = total + o.stdout.len()\\n+        }\\n+        i = i + 1\\n+    }\\n+    return total\\n+}\\n+\\n+func main() {\\n+    let threads = env_int(\\\"PARKSOAK_THREADS\\\", 4)\\n+    let tasks = env_int(\\\"PARKSOAK_TASKS\\\", 4)\\n+    let iters = env_int(\\\"PARKSOAK_ITERS\\\", 8)\\n+    let watchdog = env_int(\\\"PARKSOAK_WATCHDOG\\\", 20)\\n+    var wd = Thread.spawn { [watchdog] in\\n+        var left = watchdog\\n+        while left > 0 {\\n+            let _ = usleep(1000000 as UInt32)\\n+            left = left - 1\\n+        }\\n+        print(\\\"WATCHDOG fired\\\")\\n+        dump_tasks()\\n+        _exit(99 as Int32)\\n+    }\\n+    wd.detach()\\n+    var group = try! TaskGroup(threads: threads)\\n+    var handles = Vector<Task<Int>>()\\n+    var t = 0\\n+    while t < tasks {\\n+        try! handles.push(group.spawn(child_burst(iters)))\\n+        t = t + 1\\n+    }\\n+    var total = 0\\n+    while handles.len() > 0 {\\n+        let h = handles.pop()!\\n+        total = total + h.join()\\n+    }\\n+    print(\\\"total={}\\\", total)\\n+}\\ndiff --git a/examples/reactor_arm_failure_panics_in_task.saw b/examples/reactor_arm_failure_panics_in_task.saw\\nnew file mode 100644\\nindex 00000000..f412c34e\\n--- /dev/null\\n+++ b/examples/reactor_arm_failure_panics_in_task.saw\\n@@ -0,0 +1,35 @@\\n+// A FAILED reactor arm is reported, never swallowed (SL-355 R6 / SL-354).\\n+//\\n+// A task parks on a descriptor that was closed under it. Arming readiness on a\\n+// closed fd fails (EBADF, errno 9 on macOS and Linux alike), and before SL-355\\n+// the failure was discarded: the task parked forever on an event that could\\n+// never arrive — exactly the signature of SL-353's lost wake, which is why the\\n+// investigation of that one had to spend a probe ruling this one out. The arm\\n+// funnel now panics naming the fd, the direction and the errno, so the two\\n+// failures can never be confused again.\\n+//\\n+// This is the IN-FRAME path (a spawned task, the coroutine transform's io-park\\n+// lowering); `reactor_arm_failure_panics_outside_frame.saw` is the sync twin.\\n+// EXPECT: panic\\n+// EXPECT-PANIC-CONTAINS: reactor: arming fd\\n+// EXPECT-PANIC-CONTAINS: for read readiness failed (errno 9)\\n+\\n+import std.net.{tcp_close, tcp_socketpair}\\n+\\n+// The first park succeeds (a fresh socket is writable at once), which also\\n+// creates the reactor — so its own descriptor cannot land on the number closed\\n+// next, and the second arm fails on a closed fd rather than on the reactor.\\n+func wait_on(live: Int, closed: Int) -> Int {\\n+    io_wait(live, 1)\\n+    tcp_close(closed)\\n+    io_wait(closed, 0)                 // the arm fails: `closed` is closed\\n+    closed\\n+}\\n+\\n+func main() unsafe {\\n+    let pair = tcp_socketpair()\\n+    var group = TaskGroup()\\n+    let h = group.spawn(wait_on(pair.1, pair.0))\\n+    print(h.join())\\n+    tcp_close(pair.1)\\n+}\\ndiff --git a/examples/reactor_arm_failure_panics_outside_frame.saw b/examples/reactor_arm_failure_panics_outside_frame.saw\\nnew file mode 100644\\nindex 00000000..d4d3bec5\\n--- /dev/null\\n+++ b/examples/reactor_arm_failure_panics_outside_frame.saw\\n@@ -0,0 +1,23 @@\\n+// A FAILED reactor arm is reported, never swallowed (SL-355 R6 / SL-354) —\\n+// the OUTSIDE-FRAME twin of `reactor_arm_failure_panics_in_task.saw`.\\n+//\\n+// A sync caller's `io_wait` has no frame to park, so codegen arms the reactor\\n+// with token 0 and blocks the thread until the fd is ready. It goes through the\\n+// same arm funnel, so a closed fd panics here too — before SL-355 the failed arm\\n+// was discarded and the thread blocked in the reactor forever.\\n+// EXPECT: panic\\n+// EXPECT-PANIC-CONTAINS: reactor: arming fd\\n+// EXPECT-PANIC-CONTAINS: for write readiness failed (errno 9)\\n+\\n+import std.net.{tcp_close, tcp_socketpair}\\n+\\n+func main() unsafe {\\n+    let pair = tcp_socketpair()\\n+    // A first wait that succeeds (a fresh socket is writable at once) creates\\n+    // the reactor, so its descriptor cannot reuse the number closed next.\\n+    io_wait(pair.0, 1)\\n+    tcp_close(pair.1)\\n+    io_wait(pair.1, 1)                 // the arm fails: the fd is closed\\n+    print(\\\"unreachable\\\")\\n+    tcp_close(pair.0)\\n+}\\ndiff --git a/sawc/codegen/calls.py b/sawc/codegen/calls.py\\nindex 0b8d40d0..c544d82b 100644\\n--- a/sawc/codegen/calls.py\\n+++ b/sawc/codegen/calls.py\\n@@ -2302,67 +2302,17 @@ class CallsMixin:\\n                 vtype = self.variable_types.get(expr.object.variable)\\n                 if vtype is not None and vtype.kind == TypeKind.REFERENCE:\\n                     self_arg = self.builder.load(self_arg, name=\\\"ref_self_deref\\\")\\n-            # If object is a variable, pass its alloca directly\\n-            elif isinstance(expr.object, Identifier) and expr.object.name in self.variables:\\n-                self_arg = self.variables[expr.object.name]\\n-                # If the receiver binding is itself a `&`/`&var` reference (e.g. a\\n-                # reference parameter like `h: &var Hasher`), its alloca holds a\\n-                # pointer TO the referent; load once so a `&var self` method gets\\n-                # the referent's pointer, not a pointer-to-pointer (design 48:\\n-                # `h.write_int(...)` inside `String.hash`).\\n-                vtype = self.variable_types.get(expr.object.name)\\n-                if vtype is not None and vtype.kind == TypeKind.REFERENCE:\\n-                    self_arg = self.builder.load(self_arg, name=\\\"ref_self_deref\\\")\\n-            elif (isinstance(expr.object, Identifier)\\n-                    and self._static_global(expr.object) is not None):\\n-                # A module static as the receiver of a by-pointer method: the\\n-                # global IS the storage (design 149). Falling through to the\\n-                # spill-a-temporary branch below would lock, CAS or mutate a copy\\n-                # of the static and throw the result away — which is what a\\n-                # `static LOCK: SpinLock<T>` would have done.\\n-                self_arg = self._static_global(expr.object)\\n-            elif isinstance(expr.object, SelfExpr) and \\\"self\\\" in self.variables:\\n-                # For 'self.method()' in a var self method, pass self's pointer directly\\n-                self_ptr = self.variables[\\\"self\\\"]\\n-                # If self is already a pointer (var self method), use it directly\\n-                if isinstance(self_ptr.type, ir.PointerType):\\n-                    self_arg = self_ptr\\n-                else:\\n-                    self_arg = self_ptr  # It's an alloca, pass it\\n-            elif (isinstance(expr.object, MemberAccess)\\n-                    and self._receiver_path_is_lvalue(expr.object)):\\n-                # Handle nested mutable access like self.keys.push(...)\\n-                # We need a pointer to the field, not a copy\\n-                self_arg = self._get_member_pointer(expr.object)\\n-            elif (isinstance(expr.object, ArrayIndex)\\n-                    and self._receiver_path_is_lvalue(expr.object)):\\n-                # A pointer/array-element receiver — `ptr[i].mutating()` (design\\n-                # 52b: `__group[0].__enqueue(...)`). Address the real element slot\\n-                # so the mutation lands on the pointee, not a materialized copy.\\n-                self_arg = self._get_element_pointer(expr.object)\\n-            elif (isinstance(expr.object, TupleIndex)\\n-                    and self._receiver_path_is_lvalue(expr.object)):\\n-                # A TUPLE-ELEMENT receiver — `t.0.push(x)` (DF-151j). The tuple\\n-                # projection is a place on the write side exactly as a struct\\n-                # field is, so address the element slot; otherwise this fell to\\n-                # the materialize-a-temporary `else` below and every mutation\\n-                # through a tuple element was silently discarded.\\n-                self_arg = self._get_tuple_element_pointer(expr.object)\\n-            elif (isinstance(expr.object, ForceUnwrap)\\n-                    and self._receiver_path_is_lvalue(expr.object)):\\n-                # A `&var self` method on an opt-encoded lvalue `x!` — most\\n-                # commonly a coroutine frame-local `self.acc!` (design 62: an\\n-                # owning across-suspend local is opt-encoded, and `_rewrite_node`\\n-                # turns a bare `acc` receiver into `self.acc!`). Address the\\n-                # optional's payload IN PLACE (design 84 `&(opt!)`) so the mutation\\n-                # lands on the real frame slot and SURVIVES the suspend. Without\\n-                # this the receiver fell through to the materialize-a-temporary\\n-                # `else` below, mutating a discarded copy — every `req.append(...)`\\n-                # / `acc.push(...)` across a park was silently lost (design 86).\\n-                self_arg = self._generate_reference_expr(\\n-                    ReferenceExpr(expr=expr.object, mutable=True,\\n-                                  line=expr.object.line,\\n-                                  column=expr.object.column))\\n+            elif self._receiver_path_is_lvalue(expr.object):\\n+                # A receiver that NAMES storage — a binding (loaded once through\\n+                # a `&`/`&var` reference, design 48), a module static (design\\n+                # 149), `self`, a field (`self.keys.push(..)`), an element\\n+                # (`ptr[i].mutating()`, design 52b), a tuple element (`t.0`,\\n+                # DF-151j) or a force-unwrapped opt-encoded frame local (`x!`,\\n+                # design 86) — is addressed IN PLACE, so the mutation lands on\\n+                # the real storage and survives a suspend. SL-368: one funnel\\n+                # for this and the interior-mutability ops, which used to keep a\\n+                # smaller dispatch of their own and lost the element case.\\n+                self_arg = self._receiver_place_pointer(expr.object)\\n             elif receiver_temp_slot is not None:\\n                 # An owned-temporary receiver already spilled to a slot for\\n                 # statement-scoped cleanup: mutate through that same slot so the\\n@@ -2508,6 +2458,35 @@ class CallsMixin:\\n         return self.builder.gep(struct_ptr, [zero, zero], inbounds=True,\\n                                 name=\\\"atomic_cell\\\")\\n \\n+    def _receiver_place_pointer(self, obj_expr):\\n+        \\\"\\\"\\\"THE RECEIVER-PLACE FUNNEL (SL-368): the address of the storage a\\n+        method receiver NAMES, or None when the receiver is a temporary.\\n+\\n+        A receiver that names storage — a binding (through a `&`/`&var`\\n+        reference once), a module static, `self`, a field, an ELEMENT of a\\n+        fixed array or a raw pointer, a tuple element, a force-unwrapped\\n+        optional — is addressed through `_get_lvalue_pointer`, the one\\n+        base-to-pointer dispatch, so a receiver and a write of the same place\\n+        can never disagree about where it lives. Whether the path names\\n+        storage at all is `_receiver_path_is_lvalue`'s answer (DF-293a): a path\\n+        rooted in a call is a temporary, evaluated once by the caller's\\n+        spill arm rather than re-walked here.\\n+\\n+        ENTRY POINTS (obligation 1):\\n+          * `_generate_method_call`'s by-pointer arm — every `&var self`\\n+            method, and every `&self` method whose receiver travels by pointer\\n+            (cell-carrying and aggregate receivers, design 186/261; `SpinLock`,\\n+            `Mutex` and `Once` reach their lock words through it).\\n+          * `_receiver_storage_pointer` — the compiler-known interior-mutability\\n+            operations: the four `Atomic` methods (`_atomic_cell_pointer`) and a\\n+            cell's `ptr()` (`_interior_cell_pointer`).\\n+        Before SL-368 the second of these kept its own smaller dispatch with no\\n+        element case, and an atomic through `p[i]` silently ran on a copy.\\n+        \\\"\\\"\\\"\\n+        if not self._receiver_path_is_lvalue(obj_expr):\\n+            return None\\n+        return self._get_lvalue_pointer(obj_expr)\\n+\\n     def _receiver_storage_pointer(self, obj_expr, what: str, want_pointee=None):\\n         \\\"\\\"\\\"A pointer to the receiver's OWN storage, never to a copy of it.\\n \\n@@ -2516,22 +2495,18 @@ class CallsMixin:\\n         (`Once<UnsafePointer<UInt8>>`) is itself pointer-to-pointer storage, so\\n         the shape test alone would strip a level that is part of the value.\\n         \\\"\\\"\\\"\\n-        if isinstance(obj_expr, Identifier):\\n-            if obj_expr.name in self.variables:\\n-                struct_ptr = self.variables[obj_expr.name]\\n-            elif self._static_global(obj_expr) is not None:\\n-                struct_ptr = self._static_global(obj_expr)\\n-            else:\\n+        # SL-368: through the one receiver-place funnel. This used to be a\\n+        # second, smaller dispatch of its own that knew bindings, `self`,\\n+        # fields and tuple elements but not ELEMENTS, so `p[0].store(v)` over\\n+        # an `UnsafePointer<Atomic<Int>>` (or a fixed array) spilled the\\n+        # element and the store, CAS or fetch_add landed on the copy.\\n+        struct_ptr = self._receiver_place_pointer(obj_expr)\\n+        if struct_ptr is None:\\n+            if isinstance(obj_expr, Identifier):\\n                 raise ValueError(f\\\"Undefined {what} receiver: {obj_expr.name}\\\")\\n-        elif isinstance(obj_expr, SelfExpr):\\n-            struct_ptr = self.variables[\\\"self\\\"]\\n-        elif isinstance(obj_expr, MemberAccess):\\n-            struct_ptr = self._get_member_pointer(obj_expr)\\n-        elif isinstance(obj_expr, TupleIndex):\\n-            # An `Atomic` held in a tuple element (`counters.0.fetch_add(1)`):\\n-            # the cell has to be the real one, not a spilled copy (DF-151j).\\n-            struct_ptr = self._get_tuple_element_pointer(obj_expr)\\n-        else:\\n+            # A TEMPORARY receiver (a call result): there is no caller storage\\n+            # for the operation to reach, so it runs on a slot holding the one\\n+            # evaluation. Its atomicity is vacuous; the code stays total.\\n             val = self._generate_expression(obj_expr)\\n             struct_ptr = self._entry_alloca(val.type, name=f\\\"{what}_tmp\\\")\\n             self.builder.store(val, struct_ptr)\\ndiff --git a/sawc/codegen/core.py b/sawc/codegen/core.py\\nindex cbde8c23..9f7e9ab7 100644\\n--- a/sawc/codegen/core.py\\n+++ b/sawc/codegen/core.py\\n@@ -2237,7 +2237,10 @@ class CodeGenerator(ResultsMixin, MatchMixin, StructsMixin, CollectionsMixin, Ca\\n         word = self.int_type\\n         create = ir.Function(self.module, ir.FunctionType(word, []),\\n                              name=\\\"__saw_rt_reactor_create\\\")\\n-        reg = ir.Function(self.module, ir.FunctionType(void, [word, word, word, word]),\\n+        # SL-355 R6: the arm REPORTS — 0 when readiness is armed, else the\\n+        # platform's errno. A discarded failure left the task parked forever\\n+        # with SL-353's lost-wake signature.\\n+        reg = ir.Function(self.module, ir.FunctionType(word, [word, word, word, word]),\\n                           name=\\\"__saw_rt_reactor_register\\\")\\n         poll = ir.Function(self.module, ir.FunctionType(word, [word, word]),\\n                            name=\\\"__saw_rt_reactor_poll\\\")\\ndiff --git a/sawc/coro_transform.py b/sawc/coro_transform.py\\nindex cd55acf6..ad93ff75 100644\\n--- a/sawc/coro_transform.py\\n+++ b/sawc/coro_transform.py\\n@@ -268,6 +268,90 @@ def _int(n):\\n     return IntLiteral(value=n)\\n \\n \\n+# --------------------------------------------------------------------------- #\\n+# SL-355: THE WAKE WORD — every synthesized read and write of a frame's `__wake`\\n+# --------------------------------------------------------------------------- #\\n+#\\n+# The ROOT frame's word is SHARED: the reactor token a registration carries is\\n+# its address, so a poller on another thread latches it while the frame's own\\n+# thread may still be recording the park. Every other frame's word is private to\\n+# the thread resuming the chain. These two builders are the only places the\\n+# transform spells an access to the word, so the word's representation is\\n+# decided here and nowhere else. `frame` is the expression naming the frame:\\n+# `SelfExpr()`, a sub-frame field `self.__subN`, or a driver's local `__f`.\\n+#\\n+# ENTRY POINTS: `_suspend_to` (a suspension's reason), `_emit_io_park` (the\\n+# own-word record ahead of the arm), `_pending_merge` (the nested propagation),\\n+# the `wake_reason` accessor in `build_resume`, and `_make_entry_executor`'s\\n+# park call. The field's declaration (`_wake_field_type`) and its initial value\\n+# (`_wake_init`) are the representation's other two faces.\\n+#\\n+# THE REPRESENTATION IS AN `Atomic<Int>` (SL-355 R5, the user's ruling (ii)).\\n+# The root word is read and written from several threads at once — the\\n+# resuming worker stores the park, a poller latches it ready, the scheduler's\\n+# scans read it — and as a plain `Int` that was a data race, however the stores\\n+# were ordered. Every access is a seq_cst atomic now, and the poller's latch is\\n+# a compare-and-swap from PARKED (-1) to READY (0) (`rt_reactor_poll`), so a\\n+# word that no longer holds the io park it was armed for — a sleep deadline, a\\n+# channel word, a later park's own record — is never overwritten by an event\\n+# left over from an earlier one. The word is still exactly one machine word,\\n+# at the same offset, so the frame layout and the backtrace table are unchanged\\n+# by it; only the field's TYPE differs.\\n+def _wake_field_type():\\n+    return SawType(TypeKind.STRUCT, struct_name=\\\"Atomic\\\",\\n+                   type_args=[SawType(TypeKind.INT)])\\n+\\n+\\n+def _wake_init():\\n+    return FunctionCall(name=\\\"Atomic\\\", arguments=[Argument(name=None,\\n+                                                           value=_int(0))])\\n+\\n+\\n+def _wake_read(frame):\\n+    return MethodCall(object=MemberAccess(object=frame, member=\\\"__wake\\\"),\\n+                      method_name=\\\"load\\\", arguments=[])\\n+\\n+\\n+def _wake_store(frame, value):\\n+    return ExpressionStatement(expression=MethodCall(\\n+        object=MemberAccess(object=frame, member=\\\"__wake\\\"),\\n+        method_name=\\\"store\\\", arguments=[Argument(name=None, value=value)]))\\n+\\n+\\n+def _is_wake_store(node, frame_is_self=True):\\n+    \\\"\\\"\\\"Is `node` a statement the builders above produced that WRITES a `__wake`?\\n+    (`frame_is_self` narrows it to this frame's own word.) The park-order\\n+    verifier's one recognizer, so it follows the representation too: an atomic\\n+    `store`/`compare_exchange`/`fetch_add` on the word, or — should a plain\\n+    assignment ever be synthesized again — that.\\\"\\\"\\\"\\n+    if isinstance(node, AssignStatement):\\n+        t = node.target\\n+    elif (isinstance(node, ExpressionStatement)\\n+          and isinstance(node.expression, MethodCall)\\n+          and node.expression.method_name in (\\\"store\\\", \\\"compare_exchange\\\",\\n+                                              \\\"fetch_add\\\")):\\n+        t = node.expression.object\\n+    else:\\n+        return False\\n+    return (isinstance(t, MemberAccess) and t.member == \\\"__wake\\\"\\n+            and (not frame_is_self or isinstance(t.object, SelfExpr)))\\n+\\n+\\n+def _own_wake_token():\\n+    \\\"\\\"\\\"This frame's `__wake`-word ADDRESS as an `Int` — the reactor token a ROOT\\n+    frame hands down its chain (design 91). A raw cast on purpose; see the\\n+    verified-unsafe note at `build_resume`'s `io_tok_init`, its first user. The\\n+    runtime turns it back into an `UnsafePointer<Atomic<Int>>` on both sides:\\n+    the register funnel's park store and the poll's latch.\\\"\\\"\\\"\\n+    return CastExpr(\\n+        expr=CastExpr(\\n+            expr=ReferenceExpr(expr=_self_field(\\\"__wake\\\"), mutable=False),\\n+            target_type=SawType(kind=TypeKind.POINTER,\\n+                                inner_type=_wake_field_type(),\\n+                                pointer_mutable=True)),\\n+        target_type=SawType(kind=TypeKind.INT))\\n+\\n+\\n # The design-144 IDENTITY of `std.compiler.frame`'s `Poll`, not its spelling.\\n # Every reference below is SYNTHESIZED, so it must reach std's declaration\\n # whatever the module it lands in declares: a user `enum Poll` binds the\\n@@ -299,6 +383,11 @@ _SUSPEND_CALLS = (\\\"__saw_suspend\\\", \\\"yield_now\\\", \\\"sleep\\\", \\\"__saw_io_park\\\", \\\"io_wa\\n # never at address 0, so it can never collide with IO_PARK_WAKE.\\n IO_PARK_WAKE = -1\\n \\n+# SL-355: the runtime's reactor-arm funnel (std/taskgroup.saw). Named once: the\\n+# transform emits it from `_FrameBuilder._emit_io_park` and nowhere else, which\\n+# the `parkorder` battery lane checks.\\n+IO_REGISTER_FUNNEL = \\\"__saw_exec_io_register\\\"\\n+\\n \\n def _suspend_call_name(stmt):\\n     if (isinstance(stmt, ExpressionStatement)\\n@@ -5584,7 +5673,9 @@ class _FrameBuilder:\\n         # The wake reason the frame communicates to the executor on a Pending\\n         # (design 45 item 4): 0 = ready (yield), >0 = sleep that many NANOSECONDS\\n         # (design 180 — the unit follows `Duration`, which is what `sleep` takes).\\n-        fields.append(StructField(name=\\\"__wake\\\", type=SawType(TypeKind.INT)))\\n+        # SL-355: an `Atomic<Int>` — the root word is shared with the reactor's\\n+        # poll on another thread (see `_wake_field_type`).\\n+        fields.append(StructField(name=\\\"__wake\\\", type=_wake_field_type()))\\n         # design 91: the reactor wake-word ADDRESS to latch on an io readiness\\n         # event. For a driven ROOT frame it is `&self.__wake` (set on first resume);\\n         # a nested sub-frame inherits its parent's token (propagated at each drive),\\n@@ -6404,13 +6495,7 @@ class _FrameBuilder:\\n             condition=BinaryOp(op=\\\"==\\\", left=_self_field(\\\"__io_tok\\\"), right=_int(0)),\\n             then_branch=Block(statements=[AssignStatement(\\n                 target=_self_field(\\\"__io_tok\\\"),\\n-                value=CastExpr(\\n-                    expr=CastExpr(\\n-                        expr=ReferenceExpr(expr=_self_field(\\\"__wake\\\"), mutable=False),\\n-                        target_type=SawType(kind=TypeKind.POINTER,\\n-                                             inner_type=SawType(kind=TypeKind.INT),\\n-                                             pointer_mutable=True)),\\n-                    target_type=SawType(kind=TypeKind.INT)))],\\n+                value=_own_wake_token())],\\n                 final_expr=None),\\n             else_branch=None))\\n \\n@@ -6432,7 +6517,7 @@ class _FrameBuilder:\\n             parameters=[Parameter(name=\\\"self\\\", type=SawType(TypeKind.VOID),\\n                                   is_reference=True, reference_mutable=False)],\\n             return_type=SawType(TypeKind.INT),\\n-            body=Block(statements=[], final_expr=_self_field(\\\"__wake\\\")),\\n+            body=Block(statements=[], final_expr=_wake_read(SelfExpr())),\\n             self_mutable=False, self_is_reference=True, is_sync=True,\\n             is_synthesized=True,\\n             line=func.line, column=func.column,\\n@@ -6733,19 +6818,95 @@ class _FrameBuilder:\\n         self._blocks[self.cur].append(ContinueStatement())\\n         self._term.add(self.cur)\\n \\n-    def _suspend_to(self, wake, target, line=None):\\n+    def _suspend_to(self, wake, target, line=None, write_wake=True):\\n+        \\\"\\\"\\\"Terminate the current block with a suspension carrying `wake`.\\n+\\n+        `write_wake=False` is the IO park's shape (SL-355 R3): `_emit_io_park`\\n+        has already recorded the park BEFORE arming the reactor, and a store\\n+        here — after the arm — is exactly the store that erased a readiness\\n+        latch a concurrent poller had made (SL-353). `wake` is then only the\\n+        reason the suspension carries, not something this writes.\\\"\\\"\\\"\\n         if self.cur in self._term:\\n             return\\n         # design 158: a frame parked here holds `__state == target`, so the\\n         # suspending statement's line is what a backtrace prints for `target`.\\n         self._state_lines[target] = line or self._cur_line\\n-        self._blocks[self.cur].append(\\n-            AssignStatement(target=_self_field(\\\"__wake\\\"), value=wake))\\n+        if write_wake:\\n+            self._blocks[self.cur].append(_wake_store(SelfExpr(), wake))\\n         self._blocks[self.cur].append(\\n             AssignStatement(target=_self_field(\\\"__state\\\"), value=_int(target)))\\n         self._blocks[self.cur].append(ReturnStatement(value=_poll(\\\"Pending\\\")))\\n         self._term.add(self.cur)\\n \\n+    def _emit_io_park(self, fd, direction, target):\\n+        \\\"\\\"\\\"THE IO-PARK FUNNEL (SL-355): record the park, THEN arm the reactor,\\n+        then suspend — and store nothing after the arm.\\n+\\n+        SL-353's lost wake was two stores in the wrong order. The reactor was\\n+        armed first and the park value (-1) written second, so a poller on\\n+        another thread could latch the root's word to 0 (ready) in between and\\n+        the park store erased it; the one-shot registration was then spent, and\\n+        the task parked forever with its fd readable. The rules this emits:\\n+\\n+          R1  the park is established BEFORE readiness is armed. This frame's\\n+              own word is recorded here (its parent's propagation reads it), and\\n+              the ROOT word the token names is written by the runtime funnel\\n+              `__saw_exec_io_register` itself, ahead of its arm — so no lowering\\n+              can arm first.\\n+          R3  nothing re-stores the word after the arm: the suspension goes\\n+              through `_suspend_to(write_wake=False)`.\\n+          R2  lives in `_pending_merge`: the nested propagation never re-stores\\n+              a child's park over the root word.\\n+\\n+        ENTRY POINTS (obligation 1 — every io park goes through here):\\n+          * `_lower_stmt` — the `io_wait` / `io_wait_until` lowering (every\\n+            std.net / std.signal / std.process park).\\n+          * `_emit_blk_call` — the blocking-extern OFFLOAD park on the job pipe.\\n+        The one io park that does not is codegen's OUTSIDE-FRAME `io_wait`\\n+        (`codegen/calls.py`): it registers token 0, which no poll latches, and\\n+        blocks its own thread in the reactor, so there is no word to order.\\n+        `_verify_park_order` checks every generated resume body against this\\n+        shape, and the `parkorder` battery lane proves that check is reached.\\n+        \\\"\\\"\\\"\\n+        self._emit([\\n+            _wake_store(SelfExpr(), _int(IO_PARK_WAKE)),\\n+            ExpressionStatement(expression=FunctionCall(\\n+                name=IO_REGISTER_FUNNEL,\\n+                arguments=[Argument(name=None, value=fd),\\n+                           Argument(name=None, value=direction),\\n+                           Argument(name=None, value=_self_field(\\\"__io_tok\\\"))])),\\n+        ])\\n+        self._suspend_to(_int(IO_PARK_WAKE), target, write_wake=False)\\n+\\n+    def _pending_merge(self, sub):\\n+        \\\"\\\"\\\"The nested `Pending` propagation, as a MERGE (SL-355 R2).\\n+\\n+        A sub-frame that returned `Pending` stopped for the reason in its own\\n+        wake word, and this frame reports that reason to ITS parent through its\\n+        own word. Every reason but the io park is copied, as it always was. The\\n+        io park is the exception: the ROOT word — the one the reactor token\\n+        names — was established by the register funnel before the arm, and a\\n+        poller may already have latched it ready. Copying the child's -1 over\\n+        it was SL-353's dominant clobber (once per frame in the chain, after\\n+        the arm). So an io park is recorded only in a frame whose word is NOT\\n+        the token's (a link in the middle of the chain, whose word only its\\n+        parent reads, on this thread), and the root word is left alone.\\n+        \\\"\\\"\\\"\\n+        child = _wake_read(_self_field(sub))\\n+        is_link = BinaryOp(op=\\\"!=\\\", left=_self_field(\\\"__io_tok\\\"),\\n+                           right=_own_wake_token())\\n+        return ExpressionStatement(expression=IfExpr(\\n+            condition=BinaryOp(op=\\\"!=\\\", left=child, right=_int(IO_PARK_WAKE)),\\n+            then_branch=Block(statements=[\\n+                _wake_store(SelfExpr(), _wake_read(_self_field(sub)))],\\n+                final_expr=None),\\n+            else_branch=Block(statements=[ExpressionStatement(expression=IfExpr(\\n+                condition=is_link,\\n+                then_branch=Block(statements=[\\n+                    _wake_store(SelfExpr(), _int(IO_PARK_WAKE))],\\n+                    final_expr=None),\\n+                else_branch=None))], final_expr=None)))\\n+\\n     def _done(self, value, forgets=None):\\n         if self.cur in self._term:\\n             return\\n@@ -6980,13 +7141,10 @@ class _FrameBuilder:\\n                     AssignStatement(target=_self_field(\\\"__io_deadline\\\"),\\n                                     value=deadline_a),\\n                 ])\\n-                self._emit([ExpressionStatement(expression=FunctionCall(\\n-                    name=\\\"__saw_exec_io_register\\\",\\n-                    arguments=[Argument(name=None, value=_self_field(\\\"__io_fd\\\")),\\n-                               Argument(name=None, value=_self_field(\\\"__io_dir\\\")),\\n-                               Argument(name=None, value=_self_field(\\\"__io_tok\\\"))]))])\\n+                # SL-355: record the park, arm, suspend — the one funnel.\\n                 nxt = self._new_block()\\n-                self._suspend_to(_int(IO_PARK_WAKE), nxt)\\n+                self._emit_io_park(_self_field(\\\"__io_fd\\\"),\\n+                                   _self_field(\\\"__io_dir\\\"), nxt)\\n                 self.cur = nxt\\n                 return\\n             # The wake expression (e.g. `sleep(d)`'s span) is ordinary body code:\\n@@ -7894,15 +8052,12 @@ class _FrameBuilder:\\n             arguments=[Argument(name=None, value=_self_field(job))]), right=_int(0))\\n         self._branch(done, after, park)\\n         # park: register the readable pipe fd + suspend (io-park), retry on wake.\\n+        # SL-355: through the one io-park funnel, which records the park before\\n+        # it arms — the offload park is where SL-353 was first seen.\\n         self.cur = park\\n         fd = FunctionCall(name=\\\"__saw_blk_pipe_fd\\\",\\n                           arguments=[Argument(name=None, value=_self_field(job))])\\n-        self._emit([ExpressionStatement(expression=FunctionCall(\\n-            name=\\\"__saw_exec_io_register\\\",\\n-            arguments=[Argument(name=None, value=fd),\\n-                       Argument(name=None, value=_int(0)),\\n-                       Argument(name=None, value=_self_field(\\\"__io_tok\\\"))]))])\\n-        self._suspend_to(_int(IO_PARK_WAKE), header)\\n+        self._emit_io_park(fd, _int(0), header)\\n         # after: take the result (joins the worker thread + frees the job).\\n         self.cur = after\\n         take = FunctionCall(name=\\\"__saw_blk_take\\\",\\n@@ -8137,10 +8292,11 @@ class _FrameBuilder:\\n         # root the same way its wake reason does. Propagating only the wake word\\n         # would leave the root reporting an UNBOUNDED io park and the timeout\\n         # would never fire — the failure this line exists to prevent.\\n+        #\\n+        # SL-355 R2: the wake word itself is propagated by MERGE, never copied —\\n+        # see `_pending_merge`.\\n         pending_body = [\\n-            AssignStatement(target=_self_field(\\\"__wake\\\"),\\n-                            value=MemberAccess(object=_self_field(sub),\\n-                                               member=\\\"__wake\\\")),\\n+            self._pending_merge(sub),\\n             AssignStatement(target=_self_field(\\\"__io_deadline\\\"),\\n                             value=MemberAccess(object=_self_field(sub),\\n                                                member=\\\"__io_deadline\\\")),\\n@@ -10390,7 +10546,10 @@ def _build_frame_init(fb: _FrameBuilder, param_values, fbs, recv_value=None,\\n     for bc in getattr(fb, 'blk_calls', []):\\n         add(f\\\"__blkjob{bc['idx']}\\\", _int(0), all_zero=True)\\n     add(\\\"__state\\\", _int(0), all_zero=True)\\n-    add(\\\"__wake\\\", _int(0), all_zero=True)\\n+    # SL-355 R5: an `Atomic(0)`. Its bytes are all zero (the cell is\\n+    # layout-transparent), so a zero-filled frame holds a valid READY word, not\\n+    # a parked one — nothing is armed for it and no latch can target it.\\n+    add(\\\"__wake\\\", _wake_init(), all_zero=True)\\n     add(\\\"__io_tok\\\", _int(0), all_zero=True)\\n     add(\\\"__io_deadline\\\", _int(0), all_zero=True)\\n     if fb.arms_io:\\n@@ -10511,8 +10670,8 @@ def _make_entry_executor(fb: _FrameBuilder, fbs):\\n                                  member=\\\"__io_deadline\\\")\\n     pending_body = Block(statements=[ExpressionStatement(expression=FunctionCall(\\n         name=\\\"__saw_exec_park\\\",\\n-        arguments=[Argument(name=None, value=MemberAccess(\\n-            object=Identifier(name=\\\"__f\\\"), member=\\\"__wake\\\")),\\n+        arguments=[Argument(name=None,\\n+                            value=_wake_read(Identifier(name=\\\"__f\\\"))),\\n                    Argument(name=None, value=park_deadline)]))], final_expr=None)\\n     done_body = Block(statements=[AssignStatement(\\n         target=Identifier(name=\\\"__done\\\"), value=BoolLiteral(value=True))],\\n@@ -12405,6 +12564,136 @@ def _record_frame_decisions(ledger, *, typechecker, structurally_suspends,\\n     _verify_site_coverage()\\n \\n \\n+class ParkOrderViolation(Exception):\\n+    \\\"\\\"\\\"A generated resume body breaks the io-park ordering rule (SL-355).\\n+\\n+    An INVARIANT failure, reported as an internal compiler error beside the\\n+    ledger and shape-table misses (`sawc.py`), never a diagnostic about the\\n+    author's code: the author wrote an `io_wait` or a blocking call, and the\\n+    transform lowered it into a lost wake.\\\"\\\"\\\"\\n+\\n+\\n+def _verify_park_order(resume_extensions):\\n+    \\\"\\\"\\\"SL-355: every io park in every generated resume body is RECORDED, then\\n+    ARMED, then SUSPENDED — and nothing stores a wake word after the arm.\\n+\\n+    The runtime half of the rule lives in `__saw_exec_io_register`, which\\n+    writes the park value through the root token before it arms. This checks\\n+    the compiler half on the code the transform actually produced, for every\\n+    frame, on every compile:\\n+\\n+      1. the funnel call is a STATEMENT of a block (not buried in an\\n+         expression), and the only thing that arms: nothing else in a resume\\n+         body names it (`_emit_io_park` is its one emitter);\\n+      2. before it, in the same block, this frame's own wake word is stored\\n+         (R1 — the record the parent's propagation reads);\\n+      3. after it, in the same block, NOTHING stores a wake word (R3) — the\\n+         store that used to follow was SL-353's leaf clobber;\\n+      4. every nested drive's `Pending` arm propagates the child's reason\\n+         through a GUARDED merge and never through a top-level store (R2) — the\\n+         unguarded copy was SL-353's dominant clobber.\\n+\\n+    `tools/test_park_order.py` (the `parkorder` battery lane) injects each\\n+    violation and requires this to name it, beside an uninjected control.\\n+    \\\"\\\"\\\"\\n+    def is_funnel_call(node):\\n+        return (isinstance(node, FunctionCall)\\n+                and node.name == IO_REGISTER_FUNNEL)\\n+\\n+    def any_node(node, pred):\\n+        if pred(node):\\n+            return True\\n+        return any(any_node(c, pred) for c in _child_nodes(node)\\n+                   if isinstance(c, ASTNode))\\n+\\n+    def check_block(stmts, frame):\\n+        recorded = False\\n+        armed = False\\n+        for s in stmts:\\n+            if armed and any_node(s, lambda n: _is_wake_store(n, False)):\\n+                raise ParkOrderViolation(\\n+                    f\\\"`{frame}` stores a wake word AFTER arming the reactor \\\"\\n+                    f\\\"through `{IO_REGISTER_FUNNEL}`: a readiness latch a \\\"\\n+                    f\\\"concurrent poller makes in between is erased, and the \\\"\\n+                    f\\\"one-shot registration never refires (SL-353; the park \\\"\\n+                    f\\\"order is record, arm, suspend — SL-355 R3)\\\")\\n+            top_call = (isinstance(s, ExpressionStatement)\\n+                        and is_funnel_call(s.expression))\\n+            if top_call:\\n+                if not recorded:\\n+                    raise ParkOrderViolation(\\n+                        f\\\"`{frame}` arms the reactor through \\\"\\n+                        f\\\"`{IO_REGISTER_FUNNEL}` before recording its own \\\"\\n+                        f\\\"park in its wake word (SL-355 R1)\\\")\\n+                armed = True\\n+                continue\\n+            if _is_wake_store(s) and not armed:\\n+                recorded = True\\n+\\n+    def is_drive(node):\\n+        return (isinstance(node, MatchExpr)\\n+                and isinstance(node.matched_expr, MethodCall)\\n+                and node.matched_expr.method_name == \\\"resume\\\"\\n+                and isinstance(node.matched_expr.object, MemberAccess)\\n+                and isinstance(node.matched_expr.object.object, SelfExpr))\\n+\\n+    def check_drive(match, frame):\\n+        sub = match.matched_expr.object.member\\n+        for arm in match.arms:\\n+            if arm.variant_name != \\\"Pending\\\":\\n+                continue\\n+            stmts = arm.body.statements\\n+            if any(_is_wake_store(s) for s in stmts):\\n+                raise ParkOrderViolation(\\n+                    f\\\"`{frame}` propagates the wake reason of `{sub}` by an \\\"\\n+                    f\\\"unguarded store: a child's io park re-stored over the \\\"\\n+                    f\\\"root word erases a latch a poller already made (SL-353; \\\"\\n+                    f\\\"the propagation is a merge — SL-355 R2)\\\")\\n+            merged = any(\\n+                isinstance(s, ExpressionStatement)\\n+                and isinstance(s.expression, IfExpr)\\n+                and any_node(s.expression.condition,\\n+                             lambda n: (isinstance(n, MemberAccess)\\n+                                        and n.member == \\\"__wake\\\"\\n+                                        and isinstance(n.object, MemberAccess)\\n+                                        and n.object.member == sub))\\n+                for s in stmts)\\n+            if not merged:\\n+                raise ParkOrderViolation(\\n+                    f\\\"`{frame}`'s drive of `{sub}` returns Pending without \\\"\\n+                    f\\\"merging the child's wake reason (SL-355 R2)\\\")\\n+\\n+    def visit(node, frame, statement_calls, all_calls):\\n+        if isinstance(node, Block):\\n+            check_block(node.statements, frame)\\n+            for s in node.statements:\\n+                if (isinstance(s, ExpressionStatement)\\n+                        and is_funnel_call(s.expression)):\\n+                    statement_calls.add(id(s.expression))\\n+        if is_funnel_call(node):\\n+            all_calls.append(node)\\n+        if is_drive(node):\\n+            check_drive(node, frame)\\n+        for c in _child_nodes(node):\\n+            if isinstance(c, ASTNode):\\n+                visit(c, frame, statement_calls, all_calls)\\n+\\n+    for ext in resume_extensions:\\n+        for m in getattr(ext, 'methods', None) or ():\\n+            if getattr(m, 'name', None) != \\\"resume\\\" or m.body is None:\\n+                continue\\n+            frame = f\\\"{getattr(ext, 'struct_name', '?')}.resume\\\"\\n+            statement_calls, all_calls = set(), []\\n+            visit(m.body, frame, statement_calls, all_calls)\\n+            for fc in all_calls:\\n+                if id(fc) not in statement_calls:\\n+                    raise ParkOrderViolation(\\n+                        f\\\"`{frame}` arms the reactor through \\\"\\n+                        f\\\"`{IO_REGISTER_FUNNEL}` from inside an expression \\\"\\n+                        f\\\"rather than through the io-park funnel \\\"\\n+                        f\\\"`_emit_io_park` (SL-355)\\\")\\n+\\n+\\n def _verify_resume_totality(resume_extensions, ledger):\\n     \\\"\\\"\\\"design 275 U2, TOTALITY CHECK (b): no `frame_boundary` call survives as a\\n     PLAIN CALL in any generated resume body.\\n@@ -13826,6 +14115,8 @@ def transform_program(program, typechecker, imported_ast=None):\\n     # runtime-facing half of the promise can be asserted — nothing that slipped\\n     # every static check reaches the executor as a blocking call.\\n     _verify_resume_totality(new_extensions, ledger)\\n+    # SL-355: and every io park in them records, arms, suspends — in that order.\\n+    _verify_park_order(new_extensions)\\n \\n     # DF-218e: consumption symmetry — a generic TEMPLATE naming a consumed\\n     # callee is consumed with it.\\ndiff --git a/sawc/rt/ABI.md b/sawc/rt/ABI.md\\nindex b07886ec..5b0bc69c 100644\\n--- a/sawc/rt/ABI.md\\n+++ b/sawc/rt/ABI.md\\n@@ -758,13 +758,29 @@ Create a reactor instance: a kqueue (macOS) / epoll (Linux) fd + its wake source\\n (an armed `EVFILT_USER` event / a nonblocking `eventfd`). Returns an opaque\\n instance pointer (as a `word`).\\n \\n-### `__saw_rt_reactor_register(r: ptr, fd: word, write: word, token: word) -> void`\\n+### `__saw_rt_reactor_register(r: ptr, fd: word, write: word, token: word) -> word  (0/errno)`\\n Arm **one-shot** readiness interest on `fd` in `r` for read (`write==0`) or write\\n (`write!=0`). `token` is carried as the event's user-data and is **the parked\\n frame's `__wake`-word ADDRESS** (design 91) — the precise-routing contract. One-shot\\n (`EV_ONESHOT`/`EPOLLONESHOT`) plus fd close drop the registration; epoll re-arms a\\n known fd with `EPOLL_CTL_MOD` on `EEXIST`.\\n \\n+**Returns `0` when readiness is armed, else the platform's raw `errno`** (SL-355\\n+R6; `void` before it). The ONE signature change to the frozen set since v2, and\\n+made for the doctrine's reason: a registration that fails and says nothing leaves\\n+its task parked forever on an event that can never arrive, which is exactly the\\n+signature of a lost wake (SL-353's investigation had to spend a probe ruling it\\n+out). The executor's funnel `__saw_exec_io_register` turns a nonzero status into a\\n+panic naming the fd, the direction and the code — a failed arm is a runtime\\n+invariant violation, never a recoverable condition — so the two failures are told\\n+apart by the program itself. A runtime that implements this seam MUST report; the\\n+arity/width check (design 149 unit c) reads this line.\\n+\\n+The PARK ORDER the executor keeps around this call (SL-355): the parking side\\n+writes the park value (`-1`) into the token's word BEFORE it calls register, and\\n+nothing on the parking side writes that word afterwards, so a latch `poll` makes\\n+from any thread is never overwritten by the park it ends.\\n+\\n ### `__saw_rt_reactor_unregister(r: ptr, fd: word, write: word) -> void`\\n Drop readiness interest on `fd` in `r` for read (`write==0`) or write\\n (`write!=0`) — `EV_DELETE` on kqueue, `EPOLL_CTL_DEL` on epoll. **Idempotent:**\\n@@ -787,7 +803,11 @@ the bookkeeping fields nor the call.\\n ### `__saw_rt_reactor_poll(r: ptr, timeout_ms: word) -> word  (ready count)`\\n Block in `kevent`/`epoll_wait` on `r` up to `timeout_ms` (`< 0` = forever). For\\n EACH ready event, **LATCH its token word to 0 (ready)** — waking exactly the\\n-frame(s) that registered for that `(fd, direction)`. The latch is a persistent word\\n+frame(s) that registered for that `(fd, direction)`. Since SL-355 the token\\n+names an `Atomic<Int>` word, and the latch is a seq_cst COMPARE-AND-SWAP from\\n+PARKED (`-1`) to READY (`0`): a word that no longer holds the park it was armed\\n+for (the park ended by cancel or deadline, and the frame is now asleep or parked\\n+on something else) is left alone. The latch is a persistent word\\n (not an edge), so a poll that fires before the scheduler finished recording the\\n park is never lost. Token `0` is skipped — it is the wake source's own token, and\\n consuming one is what makes this poll re-post the wake to the next blocked\\ndiff --git a/sawc/rt/host_linux/reactor.saw b/sawc/rt/host_linux/reactor.saw\\nindex a3290789..11cafbb8 100644\\n--- a/sawc/rt/host_linux/reactor.saw\\n+++ b/sawc/rt/host_linux/reactor.saw\\n@@ -55,8 +55,13 @@ extern \\\"C\\\" {\\n     // The `struct epoll_event` layout for THIS target (DF-113a, shim.c).\\n     func __saw_epoll_event_size() -> Int\\n     func __saw_epoll_data_offset() -> Int\\n+    // errno lives behind __errno_location() on Linux (returns `int*`).\\n+    func __errno_location() -> UnsafePointer<Int32>\\n }\\n \\n+// EEXIST — the fd is already in this epoll set, so the arm is a re-arm.\\n+static EEXIST: Int = 17\\n+\\n // The reactor instance: the epoll fd, the eventfd every thread is woken\\n // through, and the count of threads currently blocked in `epoll_wait` (the\\n // cascade's fuel). `calloc` is what makes the counter start at zero: an\\n@@ -113,8 +118,15 @@ func rt_reactor_create() unsafe -> Int {\\n \\n // Arm one-shot readiness on `fd` for read (w==0) or write (w!=0). `token` is the\\n // parked frame's `__wake`-word ADDRESS (design 91), stored as epoll_event.data.\\n+//\\n+// SL-355 R6: returns 0 when armed, else errno — the mirror of the kqueue side.\\n+// ADD is tried first; EEXIST means the fd is already in the set (a one-shot\\n+// that fired stays registered, disarmed), so that one case re-arms with MOD and\\n+// reports MOD's outcome. Every other ADD failure (EBADF for a closed fd, EPERM\\n+// for a file epoll cannot watch, ENOMEM, ...) is reported as it is: it used to\\n+// fall through to a MOD that failed too, and both were discarded.\\n @export(\\\"__saw_rt_reactor_register\\\")\\n-func rt_reactor_register(r: Int, fd: Int, w: Int, token: Int) unsafe {\\n+func rt_reactor_register(r: Int, fd: Int, w: Int, token: Int) unsafe -> Int {\\n     let rp = r as UnsafePointer<Reactor>\\n     let epfd = rp[0].fd\\n     var ev = EpollEv(lo: 0, hi: 0)\\n@@ -125,9 +137,18 @@ func rt_reactor_register(r: Int, fd: Int, w: Int, token: Int) unsafe {\\n     datap[0] = token\\n     // EPOLL_CTL_ADD; on EEXIST (already known) re-arm with EPOLL_CTL_MOD.\\n     let r0 = epoll_ctl(epfd as Int32, EPOLL_CTL_ADD, fd as Int32, evp)\\n-    if r0 != (0 as Int32) {\\n-        epoll_ctl(epfd as Int32, EPOLL_CTL_MOD, fd as Int32, evp)\\n+    if r0 == (0 as Int32) {\\n+        return 0\\n+    }\\n+    let add_errno = __errno_location()[0] as Int\\n+    if add_errno != EEXIST {\\n+        return add_errno\\n+    }\\n+    let r1 = epoll_ctl(epfd as Int32, EPOLL_CTL_MOD, fd as Int32, evp)\\n+    if r1 != (0 as Int32) {\\n+        return __errno_location()[0] as Int\\n     }\\n+    0\\n }\\n \\n // DF-134a: drop readiness interest on `fd`. The epoll mirror of the kqueue\\n@@ -180,8 +201,10 @@ func rt_reactor_poll(r: Int, timeout_ms: Int) unsafe -> Int {\\n         let datap = (buf + (i * ev_size + data_off)) as UnsafePointer<Int>\\n         let ud = datap[0]\\n         if ud != 0 {\\n-            let tokptr = ud as UnsafePointer<Int>\\n-            tokptr[0] = 0\\n+            // SL-355: the token names an `Atomic<Int>` frame word, latched by a\\n+            // CAS from PARKED (-1) to READY (0) — see host_macos/reactor.saw.\\n+            let tokptr = ud as UnsafePointer<Atomic<Int>>\\n+            let _ = tokptr[0].compare_exchange(-1, 0)\\n         } else {\\n             woke = true\\n         }\\ndiff --git a/sawc/rt/host_macos/reactor.saw b/sawc/rt/host_macos/reactor.saw\\nindex 81c0b2d0..cded274e 100644\\n--- a/sawc/rt/host_macos/reactor.saw\\n+++ b/sawc/rt/host_macos/reactor.saw\\n@@ -47,6 +47,8 @@ extern \\\"C\\\" {\\n     func calloc(count: Int, size: Int) -> UnsafePointer<Int8>\\n     func malloc(size: Int) -> UnsafePointer<Int8>\\n     func free(ptr: UnsafePointer<Int8>)\\n+    // errno lives behind __error() on macOS (returns `int*`).\\n+    func __error() -> UnsafePointer<Int32>\\n }\\n \\n // The reactor instance: the kqueue fd, plus the count of threads currently\\n@@ -108,14 +110,23 @@ func rt_reactor_create() unsafe -> Int {\\n \\n // Arm one-shot readiness on `fd` for read (w==0) or write (w!=0). `token` is the\\n // parked frame's `__wake`-word ADDRESS (design 91), carried as kevent.udata.\\n+//\\n+// SL-355 R6: returns 0 when armed, else errno. With an empty event list kevent\\n+// reports a failed change as -1 + errno (EBADF for a closed fd, ENOMEM, ...),\\n+// and that used to be discarded — leaving the task parked on an event that\\n+// could never arrive. The executor panics on a nonzero return.\\n @export(\\\"__saw_rt_reactor_register\\\")\\n-func rt_reactor_register(r: Int, fd: Int, w: Int, token: Int) unsafe {\\n+func rt_reactor_register(r: Int, fd: Int, w: Int, token: Int) unsafe -> Int {\\n     let rp = r as UnsafePointer<Reactor>\\n     let kfd = rp[0].fd\\n     let filt: Int16 = if w != 0 { -2 } else { -1 }\\n     var kev = KEvent(ident: fd, filter: filt, flags: EV_ADD_ONESHOT, fflags: 0 as UInt32, data: 0, udata: token)\\n     let nullp = 0 as UnsafePointer<Int8>\\n-    kevent(kfd as Int32, (&kev) as UnsafePointer<Int8>, 1, nullp, 0, nullp)\\n+    let rc = kevent(kfd as Int32, (&kev) as UnsafePointer<Int8>, 1, nullp, 0, nullp)\\n+    if rc < (0 as Int32) {\\n+        return __error()[0] as Int\\n+    }\\n+    0\\n }\\n \\n // DF-134a: drop readiness interest on `fd` for read (w==0) or write (w!=0).\\n@@ -131,7 +142,8 @@ func rt_reactor_register(r: Int, fd: Int, w: Int, token: Int) unsafe {\\n // Idempotent by construction: EV_DELETE against an already-fired one-shot, a\\n // closed fd, or an fd that was never armed all return ENOENT/EBADF, which is the\\n // same \\\"nothing is armed\\\" state the caller asked for. The return is ignored for\\n-// that reason, exactly as the register path ignores its own.\\n+// that reason. (The register path used to ignore its own too; since SL-355 it\\n+// reports, because a failed ARM is a task parked on nothing.)\\n @export(\\\"__saw_rt_reactor_unregister\\\")\\n func rt_reactor_unregister(r: Int, fd: Int, w: Int) unsafe {\\n     let rp = r as UnsafePointer<Reactor>\\n@@ -176,8 +188,13 @@ func rt_reactor_poll(r: Int, timeout_ms: Int) unsafe -> Int {\\n         }\\n         let ud = evs[i].udata\\n         if ud != 0 {\\n-            let tokptr = ud as UnsafePointer<Int>\\n-            tokptr[0] = 0\\n+            // SL-355: the token names an `Atomic<Int>` frame word, and the\\n+            // latch is a CAS from PARKED (-1) to READY (0). The parking side\\n+            // stored -1 before it armed, so the CAS finds it; a word that has\\n+            // since moved on (the park ended by cancel or deadline, the frame\\n+            // now sleeping or parked elsewhere) is left alone.\\n+            let tokptr = ud as UnsafePointer<Atomic<Int>>\\n+            let _ = tokptr[0].compare_exchange(-1, 0)\\n         }\\n         i = i + 1\\n     }\\ndiff --git a/sawc/sawc.py b/sawc/sawc.py\\nindex af16e3bc..8dc7951a 100755\\n--- a/sawc/sawc.py\\n+++ b/sawc/sawc.py\\n@@ -2075,7 +2075,8 @@ def _prepare_codegen(source_path: str, entry_ast, entry_source: str, verbose: bo\\n               or getattr(typechecker, \\\"_main_suspends\\\", False)\\n               or getattr(typechecker, \\\"_has_suspending_closure_body\\\", False))\\n     if driven:\\n-        from coro_transform import transform_program, CoroTransformError\\n+        from coro_transform import (transform_program, CoroTransformError,\\n+                                    ParkOrderViolation)\\n         # design 266 step 4's input, taken BEFORE the transform runs: what the\\n         # ENTRY ast held. Differencing it against the entry AST afterwards is\\n         # what tells a declaration the transform CONSUMED from one that was\\n@@ -2090,7 +2091,12 @@ def _prepare_codegen(source_path: str, entry_ast, entry_source: str, verbose: bo\\n         try:\\n             changed = transform_program(entry_ast, typechecker,\\n                                         imported_ast=merged_ast)\\n-        except (coro_ledger.LedgerMiss, coro_shapes.UnclassifiedShape) as e:\\n+        except (coro_ledger.LedgerMiss, coro_shapes.UnclassifiedShape,\\n+                ParkOrderViolation) as e:\\n+            # SL-355: ParkOrderViolation is the io-park ordering invariant\\n+            # (`_verify_park_order`) — the same kind of failure, reported the\\n+            # same way.\\n+            #\\n             # design 275 U1: a consumer asked the discovery ledger for a frame it\\n             # holds no row for, or wrote to it after the freeze. design 275 U2\\n             # adds the two totality failures beside them — a suspension position\\ndiff --git a/sawc/std/taskgroup.saw b/sawc/std/taskgroup.saw\\nindex 471c8ff8..c36e3220 100644\\n--- a/sawc/std/taskgroup.saw\\n+++ b/sawc/std/taskgroup.saw\\n@@ -138,8 +138,9 @@ extern \\\"C\\\" {\\n     func __saw_rt_reactor_create() -> Int\\n     func __saw_rt_reactor_destroy(r: Int)\\n     // Arm one-shot readiness on `fd` in `r` (write==0 => read). `token` is the\\n-    // parked frame's `__wake`-word address (design 91 precise routing).\\n-    func __saw_rt_reactor_register(r: Int, fd: Int, write: Int, token: Int)\\n+    // parked frame's `__wake`-word address (design 91 precise routing). Returns\\n+    // 0 when armed, else the platform errno (SL-355 R6).\\n+    func __saw_rt_reactor_register(r: Int, fd: Int, write: Int, token: Int) -> Int\\n     // DF-134a: drop an armed registration that never fired, so a token pointing\\n     // into a frame cannot outlive the frame. Idempotent — unregistering an\\n     // already-fired one-shot or an unarmed fd is a no-op.\\n@@ -177,7 +178,10 @@ extern \\\"C\\\" {\\n // executor module (not `public`), so no user-facing `Reactor`/`SystemReactor` name\\n // collision (design 82).\\n trait Reactor {\\n-    func register(&self, fd: Int, dir: Int, token: Int)\\n+    // SL-355 R6: 0 when readiness is armed, else the platform's error code. The\\n+    // executor's funnel panics on a failure — an arm that fails silently leaves\\n+    // its task parked forever, indistinguishable from a lost wake.\\n+    func register(&self, fd: Int, dir: Int, token: Int) -> Int\\n     // DF-134a: the inverse of `register`. A park that ends WITHOUT its event\\n     // firing must drop the registration, or the token — an address inside the\\n     // frame — outlives the frame that owns it.\\n@@ -200,7 +204,7 @@ struct SystemReactor {\\n }\\n \\n extension SystemReactor: Reactor {\\n-    func register(&self, fd: Int, dir: Int, token: Int) {\\n+    func register(&self, fd: Int, dir: Int, token: Int) -> Int {\\n         __saw_rt_reactor_register(self.instance, fd, dir, token)\\n     }\\n     func unregister(&self, fd: Int, dir: Int) {\\n@@ -563,8 +567,51 @@ func __saw_exec_park_flag_loop(wake: Int) unsafe {\\n // Saw function is what lets stage 3 route it to the `Reactor` trait without touching\\n // any synthesized call site. `dir` is the direction word (0 = read); `token` is the\\n // parked frame's `__wake`-word address (design 91 precise routing).\\n-func __saw_exec_io_register(fd: Int, dir: Int, token: Int) {\\n-    __saw_host_reactor().register(fd, dir, token)\\n+//\\n+// SL-355: THE PARK IS ESTABLISHED HERE, BEFORE THE ARM. `token` names the ROOT\\n+// frame's wake word, and the reactor's poll latches that word to ready from\\n+// whatever thread consumes the event. SL-353 lost wakes because the park value\\n+// was written AFTER the arm — by the parking lowering and again by every frame\\n+// the park propagated through — so a latch that landed in between was erased\\n+// and the one-shot registration was spent. Writing the park here means every\\n+// store a poller can make is strictly later than the park store, whatever the\\n+// lowering that called in.\\n+//\\n+// ENTRY POINTS (obligation 1):\\n+//   * `_FrameBuilder._emit_io_park` (sawc/coro_transform.py) — the ONE place\\n+//     the transform emits this call, for both of its io parks: the\\n+//     `io_wait` / `io_wait_until` lowering in `_lower_stmt` and the\\n+//     blocking-extern offload park in `_emit_blk_call`. The funnel records the\\n+//     frame's own word first and stores nothing after this call returns.\\n+//   * the nested `Pending` propagation (`_pending_merge`) is not a caller but\\n+//     is the rule's other half: it never re-stores a child's park over the\\n+//     word this writes.\\n+//   * the OUTSIDE-FRAME `io_wait` (`codegen/calls.py`), token 0 — EXEMPT: no\\n+//     frame, no word to write, and a poll never latches token 0; that caller\\n+//     blocks its own thread in the reactor until the fd is ready.\\n+// The `parkorder` battery lane checks the transform side of this contract.\\n+//\\n+// SL-355 R6: A FAILED ARM IS REPORTED, NEVER SWALLOWED. The seam returns the\\n+// platform's error code, and a nonzero one panics here, naming the fd, the\\n+// direction and the code. It is a panic rather than an error the caller could\\n+// handle because none of the callers has anywhere to send it — the park is\\n+// lowered from `io_wait` and from blocking calls, neither of which has an\\n+// error channel — and because there is nothing to recover: the fd was closed\\n+// under the park, or was never pollable, which is a bug in whatever issued the\\n+// wait. Before this the failure vanished and the task parked forever on an\\n+// event that could not arrive, which reads exactly like a lost wake (SL-354).\\n+func __saw_exec_io_register(fd: Int, dir: Int, token: Int) unsafe {\\n+    if token != 0 {\\n+        // The word is an `Atomic<Int>` frame field (SL-355 R5): this store and\\n+        // the poll's CAS latch are seq_cst operations on one cell.\\n+        let word = token as UnsafePointer<Atomic<Int>>\\n+        word[0].store(WAKE_IO)\\n+    }\\n+    let code = __saw_host_reactor().register(fd, dir, token)\\n+    if code != 0 {\\n+        let direction = if dir != 0 { \\\"write\\\" } else { \\\"read\\\" }\\n+        panic(\\\"reactor: arming fd {} for {} readiness failed (errno {})\\\", fd, direction, code)\\n+    }\\n }\\n \\n // DF-134a: the disarm entry point, called from the two places a registration can\\ndiff --git a/tools/battery.sh b/tools/battery.sh\\nindex b9e04c9a..34b58fc2 100755\\n--- a/tools/battery.sh\\n+++ b/tools/battery.sh\\n@@ -13,7 +13,7 @@\\n # The exit code is the number of failing stages.\\n #\\n #   tools/battery.sh                    # everything\\n-#   tools/battery.sh --quick            # skip the slow lanes (freestanding, bootstrap, reemit, irdet, gmgate)\\n+#   tools/battery.sh --quick            # skip the slow lanes (parksoak, freestanding, bootstrap, reemit, irdet, gmgate)\\n #   tools/battery.sh suite irdet        # named stages only\\n #   tools/battery.sh --list             # what the stages are\\n #\\n@@ -67,6 +67,7 @@ STAGES=(\\n     \\\"producertaxonomy|no|every value-position node says how it produces a value|$PY tools/test_producer_taxonomy.py\\\"\\n     \\\"corodiscovery|no|one ledger answers every coroutine frame decision|$PY tools/test_coro_discovery.py\\\"\\n     \\\"corototality|no|every shape a suspension sits in SPLITs, HOISTs, INLINEs, EMBEDs or REFUSEs|$PY tools/test_coro_shapes.py\\\"\\n+    \\\"parkorder|no|every io park records, arms, suspends through one funnel; injected SL-353 shapes ICE|$PY tools/test_park_order.py\\\"\\n     \\\"windowseam|no|the statement window is a structure, not a for-loop feature|$PY tools/test_window_seam.py\\\"\\n     \\\"stdseed|no|a cached std seed key names the METHOD ITS ENTRY IS ABOUT|$PY tools/test_std_seed_keying.py\\\"\\n     \\\"citations|no|the files nothing compiles: stale DF/SL citations, committed conflict markers|$PY tools/check_citations.py\\\"\\n@@ -85,6 +86,7 @@ STAGES=(\\n     \\\"bench|no|warehouse benchmark: checksums gate, timing reports|__BENCH__\\\"\\n     \\\"selfhostlex|no|the selfhost lexer's own tests compile and pass|__SELFHOSTLEX__\\\"\\n     \\\"minivm|no|the minivm prototype builds and its differential harness passes|__MINIVM__\\\"\\n+    \\\"parksoak|yes|a parked task is never lost: the park stress programs, oversubscribed, stalls must be 0|$PY tools/parksoak.py --python $PY\\\"\\n     \\\"reemit|yes|two compiles in ONE process emit identical unopt IR, opt IR and object|$PY tools/reemitdiff.py\\\"\\n     \\\"irdet|yes|IR determinism over the WHOLE corpus (not a sample)|__IRDET__\\\"\\n     \\\"gmgate|yes|ownership + concurrency oracles under Guard Malloc (macOS)|$PY tools/gmgate.py\\\"\\ndiff --git a/tools/parksoak.py b/tools/parksoak.py\\nnew file mode 100644\\nindex 00000000..6b0b56ad\\n--- /dev/null\\n+++ b/tools/parksoak.py\\n@@ -0,0 +1,180 @@\\n+#!/usr/bin/env python3\\n+\\\"\\\"\\\"THE PARK SOAK (SL-355 unit 1, conformance row K154): a parked task is woken\\n+exactly once and never lost.\\n+\\n+SL-353 was a lost wake in the io park: the reactor was ARMED before the park\\n+word was written, and the nested frame's Pending propagation re-stored a\\n+child's `-1` over a readiness latch a concurrent poller had already made. The\\n+one-shot registration never refires, so the task parked forever with its fd\\n+readable and nobody registered on it. The window between the arm and the\\n+clobbering store is a few instructions wide, so a program that runs alone never\\n+reaches it — 20,000 offloads in a quiet serial loop showed no stall at all.\\n+What reaches it is a PREEMPTION inside that window, which is what CPU\\n+OVERSUBSCRIPTION supplies. An honest test of the property therefore has to be\\n+oversubscribed; a serial example passes on the broken compiler.\\n+\\n+This lane is that test. It builds the two stress programs the suite already\\n+runs at modest size —\\n+\\n+    examples/park_wake_offload_stress.saw   Command.output() offload parks\\n+    examples/park_wake_net_stress.saw       TcpStream ping-pong socket parks\\n+\\n+— and runs each one in WAVES of many concurrent instances, every instance a\\n+multi-threaded group, larger than the suite runs it (the `PARKSOAK_*`\\n+environment variables the programs read). Each run has TWO watchdogs: the\\n+program's own OS thread, which prints `dump_tasks()` and exits 99 (so a stall\\n+reports WHERE every task is parked), and this driver's wall-clock timeout, which\\n+kills the process group if even that thread cannot run. Either one counts a\\n+STALL. A run that exits 0 must also print the exact total, so a run that\\n+finishes having silently dropped work is a failure too.\\n+\\n+The gate: zero stalls and zero failures. The stall count is printed per program\\n+either way, because the NUMBER is the evidence — on the pre-SL-355 compiler the\\n+default grid stalls several times per program.\\n+\\n+    tools/parksoak.py                   # the gate (default grid)\\n+    tools/parksoak.py --waves 2         # shorter, for development\\n+    tools/parksoak.py --program offload # one program\\n+    tools/parksoak.py --keep            # keep every stalled run's dump\\n+\\\"\\\"\\\"\\n+\\n+import argparse\\n+import os\\n+import signal\\n+import subprocess\\n+import sys\\n+import time\\n+\\n+HERE = os.path.dirname(os.path.abspath(__file__))\\n+REPO = os.path.dirname(HERE)\\n+BUILD = os.path.join(REPO, \\\".build\\\", \\\"parksoak\\\")\\n+\\n+# name -> (source, threads, tasks, iters, expected total)\\n+#\\n+# `threads` is the group's worker count: eight is where the sweep's stall rate\\n+# was highest (two workers never stalled). The totals follow from the programs:\\n+# every echo child prints two bytes, and every ping-pong round reads one byte on\\n+# each side of each pair.\\n+PROGRAMS = {\\n+    \\\"offload\\\": (\\\"examples/park_wake_offload_stress.saw\\\", 8, 2, 200,\\n+                lambda tasks, iters: tasks * iters * 2),\\n+    \\\"net\\\": (\\\"examples/park_wake_net_stress.saw\\\", 8, 4, 20000,\\n+            lambda tasks, iters: tasks * 2 * iters),\\n+}\\n+\\n+STALL_EXIT = 99\\n+\\n+\\n+def build(py, name, source):\\n+    os.makedirs(BUILD, exist_ok=True)\\n+    out = os.path.join(BUILD, name)\\n+    cmd = [py, os.path.join(REPO, \\\"sawc\\\", \\\"sawc.py\\\"),\\n+           os.path.join(REPO, source), \\\"-o\\\", out]\\n+    proc = subprocess.run(cmd, capture_output=True, text=True, cwd=REPO)\\n+    if proc.returncode != 0:\\n+        sys.stdout.write(proc.stdout + proc.stderr)\\n+        return None\\n+    return out\\n+\\n+\\n+def launch(binary, threads, tasks, iters, watchdog):\\n+    env = dict(os.environ)\\n+    env[\\\"PARKSOAK_THREADS\\\"] = str(threads)\\n+    env[\\\"PARKSOAK_TASKS\\\"] = str(tasks)\\n+    env[\\\"PARKSOAK_ITERS\\\"] = str(iters)\\n+    env[\\\"PARKSOAK_WATCHDOG\\\"] = str(watchdog)\\n+    return subprocess.Popen([binary], stdout=subprocess.PIPE,\\n+                            stderr=subprocess.STDOUT, text=True, env=env,\\n+                            start_new_session=True)\\n+\\n+\\n+def reap(proc, deadline):\\n+    \\\"\\\"\\\"(returncode or None on the driver's own timeout, output).\\\"\\\"\\\"\\n+    try:\\n+        out, _ = proc.communicate(timeout=max(0.0, deadline - time.time()))\\n+        return proc.returncode, out\\n+    except subprocess.TimeoutExpired:\\n+        try:\\n+            os.killpg(proc.pid, signal.SIGKILL)\\n+        except OSError:\\n+            pass\\n+        out, _ = proc.communicate()\\n+        return None, out\\n+\\n+\\n+def soak(name, binary, threads, tasks, iters, expected, waves, parallel,\\n+         watchdog, keep):\\n+    ok = stall = fail = 0\\n+    want = f\\\"total={expected}\\\"\\n+    started = time.time()\\n+    for wave in range(1, waves + 1):\\n+        procs = [launch(binary, threads, tasks, iters, watchdog)\\n+                 for _ in range(parallel)]\\n+        # The driver's timeout is the SECOND watchdog: generous, because the\\n+        # program's own fires first and reports more.\\n+        deadline = time.time() + watchdog + 30\\n+        for k, proc in enumerate(procs, 1):\\n+            rc, out = reap(proc, deadline)\\n+            if rc == 0 and want in out.split(\\\"\\\\n\\\"):\\n+                ok += 1\\n+                continue\\n+            if rc == STALL_EXIT or rc is None:\\n+                stall += 1\\n+                what = (\\\"its watchdog fired\\\" if rc == STALL_EXIT\\n+                        else \\\"the driver's timeout killed it\\\")\\n+                print(f\\\"  STALL {name} wave {wave} run {k}: {what}\\\")\\n+            else:\\n+                fail += 1\\n+                print(f\\\"  FAIL {name} wave {wave} run {k}: exit {rc}, \\\"\\n+                      f\\\"expected `{want}`\\\")\\n+            if keep or stall + fail <= 2:\\n+                dump = os.path.join(BUILD, f\\\"{name}_w{wave}_k{k}.txt\\\")\\n+                with open(dump, \\\"w\\\") as fh:\\n+                    fh.write(out)\\n+                for line in out.strip().split(\\\"\\\\n\\\")[:12]:\\n+                    print(f\\\"      | {line}\\\")\\n+                print(f\\\"      (full output: {os.path.relpath(dump, REPO)})\\\")\\n+    elapsed = time.time() - started\\n+    runs = waves * parallel\\n+    print(f\\\"parksoak {name}: runs={runs} ok={ok} stall={stall} fail={fail} \\\"\\n+          f\\\"(threads={threads} tasks={tasks} iters={iters} \\\"\\n+          f\\\"parallel={parallel} waves={waves}, {elapsed:.0f}s)\\\")\\n+    return stall, fail\\n+\\n+\\n+def main():\\n+    ap = argparse.ArgumentParser(description=__doc__.split(\\\"\\\\n\\\\n\\\")[0])\\n+    ap.add_argument(\\\"--python\\\", default=sys.executable,\\n+                    help=\\\"the interpreter that runs sawc (default: this one)\\\")\\n+    ap.add_argument(\\\"--program\\\", choices=sorted(PROGRAMS), action=\\\"append\\\",\\n+                    help=\\\"run only this program (repeatable)\\\")\\n+    ap.add_argument(\\\"--waves\\\", type=int, default=16)\\n+    ap.add_argument(\\\"--parallel\\\", type=int, default=0,\\n+                    help=\\\"concurrent instances per wave (default: 2 x cores, \\\"\\n+                         \\\"so eight workers each oversubscribe the machine 16x)\\\")\\n+    ap.add_argument(\\\"--watchdog\\\", type=int, default=30,\\n+                    help=\\\"seconds before a run's own watchdog calls it stalled\\\")\\n+    ap.add_argument(\\\"--keep\\\", action=\\\"store_true\\\",\\n+                    help=\\\"keep the output of every stalled or failed run\\\")\\n+    args = ap.parse_args()\\n+\\n+    parallel = args.parallel or 2 * (os.cpu_count() or 8)\\n+    names = args.program or sorted(PROGRAMS)\\n+    total_stall = total_fail = 0\\n+    for name in names:\\n+        source, threads, tasks, iters, total = PROGRAMS[name]\\n+        binary = build(args.python, name, source)\\n+        if binary is None:\\n+            print(f\\\"parksoak {name}: FAILED to compile {source}\\\")\\n+            total_fail += 1\\n+            continue\\n+        s, f = soak(name, binary, threads, tasks, iters, total(tasks, iters),\\n+                    args.waves, parallel, args.watchdog, args.keep)\\n+        total_stall += s\\n+        total_fail += f\\n+    print(f\\\"parksoak: stalls={total_stall} failures={total_fail}\\\")\\n+    return 0 if total_stall == 0 and total_fail == 0 else 1\\n+\\n+\\n+if __name__ == \\\"__main__\\\":\\n+    sys.exit(main())\\ndiff --git a/tools/test_park_order.py b/tools/test_park_order.py\\nnew file mode 100644\\nindex 00000000..67c339cf\\n--- /dev/null\\n+++ b/tools/test_park_order.py\\n@@ -0,0 +1,338 @@\\n+#!/usr/bin/env python3\\n+\\\"\\\"\\\"THE PARK-ORDER GATE (SL-355 R4): every io park records, arms, suspends — in\\n+that order — and nothing stores a wake word after the arm.\\n+\\n+SL-353 was a lost wake built out of two stores in the wrong order. The io park\\n+lowerings ARMED the reactor first and wrote the park value second, and the\\n+nested `Pending` propagation then COPIED each child's park value up the frame\\n+chain — also after the arm — so a readiness latch a poller on another thread\\n+had already made in the root's wake word was erased, and the one-shot\\n+registration never refired. SL-355 makes the order a funnel with two halves:\\n+\\n+  * the COMPILER half: `_FrameBuilder._emit_io_park` (sawc/coro_transform.py)\\n+    is the one place an io park is emitted — record this frame's own word, call\\n+    the runtime funnel, suspend without a store — and `_pending_merge` is the one\\n+    place the propagation is built, as a guarded merge. `_verify_park_order`\\n+    checks every generated resume body against that shape on every compile;\\n+  * the RUNTIME half: `__saw_exec_io_register` (sawc/std/taskgroup.saw) writes\\n+    the park value through the root token BEFORE it arms.\\n+\\n+WHAT THIS CHECKS:\\n+\\n+  1. `__saw_exec_io_register` is spelled ONCE in the transform (the\\n+     `IO_REGISTER_FUNNEL` constant), and only the funnel emits it and only the\\n+     verifier inspects it — a second emitter is a second place to get the order\\n+     wrong.\\n+  2. Both io-park lowerings (`_lower_stmt`, `_emit_blk_call`) call the funnel,\\n+     `_emit_nested_call` calls `_pending_merge`, and `transform_program` calls\\n+     `_verify_park_order`.\\n+  3. The runtime funnel writes the park value through the token BEFORE it\\n+     calls `register` (source order inside its body), and codegen's\\n+     outside-frame `io_wait` — the one exempt caller — passes token 0.\\n+  4. Both funnels' docstrings NAME their clients (obligation 1), and every name\\n+     they list resolves.\\n+  5. THE CHECK IS REACHED, not merely present. Three injections, each beside an\\n+     uninjected control compile, re-create the SL-353 shapes inside the\\n+     transform and require `_verify_park_order` to stop the compile with an\\n+     internal compiler error that names the rule:\\n+       - `store-after-arm`   the leaf clobber (record, arm, then store again);\\n+       - `arm-unrecorded`    the arm with no record ahead of it;\\n+       - `copy-propagation`  the nested propagation as an unguarded copy.\\n+     The injection is text spliced into the transform and restored in a\\n+     `finally`, the `corototality` pattern.\\n+\\n+The probe program is `examples/park_wake_offload_stress.saw`: its task reaches\\n+an `io_wait` park (the child-exit wait in `Command.output`), an offload park\\n+(the stdout read) and a nested drive (`child_burst` -> `Command.output`), so all\\n+three shapes are present in one compile.\\n+\\\"\\\"\\\"\\n+\\n+import os\\n+import re\\n+import subprocess\\n+import sys\\n+\\n+HERE = os.path.dirname(os.path.abspath(__file__))\\n+REPO = os.path.dirname(HERE)\\n+SAWC = os.path.join(REPO, \\\"sawc\\\")\\n+SAWC_MAIN = os.path.join(SAWC, \\\"sawc.py\\\")\\n+TRANSFORM = os.path.join(SAWC, \\\"coro_transform.py\\\")\\n+TASKGROUP = os.path.join(SAWC, \\\"std\\\", \\\"taskgroup.saw\\\")\\n+CALLS = os.path.join(SAWC, \\\"codegen\\\", \\\"calls.py\\\")\\n+PROBE = os.path.join(REPO, \\\"examples\\\", \\\"park_wake_offload_stress.saw\\\")\\n+\\n+FUNNEL = \\\"__saw_exec_io_register\\\"\\n+\\n+\\n+def _read(path):\\n+    with open(path) as fh:\\n+        return fh.read()\\n+\\n+\\n+def _functions(src):\\n+    \\\"\\\"\\\"name -> source text of every `def` in `src`, cut at the first later\\n+    CODE line at the same or a shallower indent (module-level statements end a\\n+    top-level function too). Crude, and enough: the checks below ask which\\n+    function a line lives in.\\\"\\\"\\\"\\n+    out = {}\\n+    lines = src.split(\\\"\\\\n\\\")\\n+    starts = []\\n+    for i, line in enumerate(lines):\\n+        m = re.match(r\\\"^(\\\\s*)def (\\\\w+)\\\\(\\\", line)\\n+        if m:\\n+            starts.append((i, len(m.group(1)), m.group(2)))\\n+    for i, indent, name in starts:\\n+        end = len(lines)\\n+        for j in range(i + 1, len(lines)):\\n+            text = lines[j]\\n+            stripped = text.strip()\\n+            if not stripped or stripped.startswith((\\\"#\\\", \\\")\\\", \\\"]\\\", \\\"}\\\")):\\n+                continue\\n+            if len(text) - len(text.lstrip()) <= indent:\\n+                end = j\\n+                break\\n+        out.setdefault(name, \\\"\\\\n\\\".join(lines[i:end]))\\n+    return out\\n+\\n+\\n+def _code(text):\\n+    \\\"\\\"\\\"`text` without comments and docstrings — the checks are about CODE.\\\"\\\"\\\"\\n+    text = re.sub(r'\\\"\\\"\\\"(.|\\\\n)*?\\\"\\\"\\\"', '\\\"\\\"', text)\\n+    return \\\"\\\\n\\\".join(line.split(\\\"#\\\")[0] for line in text.split(\\\"\\\\n\\\"))\\n+\\n+\\n+def check_one_emitter():\\n+    problems = []\\n+    src = _read(TRANSFORM)\\n+    code = _code(src)\\n+    spelled = code.count(f'\\\"{FUNNEL}\\\"')\\n+    if spelled != 1:\\n+        problems.append(\\n+            f\\\"coro_transform.py spells `\\\\\\\"{FUNNEL}\\\\\\\"` {spelled} times in code; \\\"\\n+            f\\\"it must be exactly once, as the IO_REGISTER_FUNNEL constant\\\")\\n+    fns = _functions(src)\\n+    users = sorted(name for name, body in fns.items()\\n+                   if \\\"IO_REGISTER_FUNNEL\\\" in _code(body))\\n+    allowed = {\\\"_emit_io_park\\\", \\\"_verify_park_order\\\", \\\"is_funnel_call\\\",\\n+               \\\"check_block\\\"}\\n+    stray = [u for u in users if u not in allowed]\\n+    if stray:\\n+        problems.append(\\n+            f\\\"coro_transform.py: {', '.join(stray)} name(s) the reactor-arm \\\"\\n+            f\\\"funnel outside `_emit_io_park` — a second io-park emitter is a \\\"\\n+            f\\\"second place to get the order wrong (SL-353)\\\")\\n+    if \\\"_emit_io_park\\\" not in users:\\n+        problems.append(\\\"`_emit_io_park` no longer emits the reactor arm\\\")\\n+    return problems\\n+\\n+\\n+def check_clients_route_through():\\n+    problems = []\\n+    fns = _functions(_read(TRANSFORM))\\n+    wants = [\\n+        (\\\"_lower_stmt\\\", \\\"self._emit_io_park(\\\",\\n+         \\\"the io_wait / io_wait_until lowering\\\"),\\n+        (\\\"_emit_blk_call\\\", \\\"self._emit_io_park(\\\",\\n+         \\\"the blocking-extern offload park\\\"),\\n+        (\\\"_emit_nested_call\\\", \\\"self._pending_merge(\\\",\\n+         \\\"the nested Pending propagation\\\"),\\n+        (\\\"transform_program\\\", \\\"_verify_park_order(\\\",\\n+         \\\"the per-compile verifier\\\"),\\n+    ]\\n+    for owner, call, what in wants:\\n+        body = fns.get(owner)\\n+        if body is None:\\n+            problems.append(f\\\"coro_transform.py: `{owner}` is gone; update \\\"\\n+                            f\\\"this gate rather than dropping the check\\\")\\n+        elif call not in _code(body):\\n+            problems.append(f\\\"coro_transform.py: `{owner}` ({what}) no longer \\\"\\n+                            f\\\"calls `{call.rstrip('(')}`\\\")\\n+    return problems\\n+\\n+\\n+def check_runtime_funnel():\\n+    problems = []\\n+    src = _read(TASKGROUP)\\n+    m = re.search(r\\\"func __saw_exec_io_register\\\\([^)]*\\\\)[^{]*\\\\{(.*?)\\\\n\\\\}\\\", src,\\n+                  re.S)\\n+    if m is None:\\n+        return [\\\"taskgroup.saw: `__saw_exec_io_register` is gone\\\"]\\n+    body = m.group(1)\\n+    arm = body.find(\\\".register(\\\")\\n+    park = min([i for i in (body.find(\\\".store(WAKE_IO)\\\"),\\n+                            body.find(\\\"] = WAKE_IO\\\")) if i >= 0] or [-1])\\n+    if arm < 0:\\n+        problems.append(\\\"taskgroup.saw: `__saw_exec_io_register` no longer \\\"\\n+                        \\\"arms the reactor\\\")\\n+    if park < 0:\\n+        problems.append(\\\"taskgroup.saw: `__saw_exec_io_register` no longer \\\"\\n+                        \\\"writes the park value through the token (SL-355 R1)\\\")\\n+    elif arm >= 0 and park > arm:\\n+        problems.append(\\\"taskgroup.saw: `__saw_exec_io_register` writes the \\\"\\n+                        \\\"park value AFTER arming — the SL-353 order\\\")\\n+    # R6: the arm's status is read and a failure panics, naming the fd.\\n+    if arm >= 0 and \\\"panic(\\\" not in body[arm:]:\\n+        problems.append(\\\"taskgroup.saw: `__saw_exec_io_register` no longer \\\"\\n+                        \\\"reports a failed arm (SL-355 R6) — a swallowed \\\"\\n+                        \\\"failure parks its task forever, like a lost wake\\\")\\n+    for host in (\\\"host_macos\\\", \\\"host_linux\\\"):\\n+        path = os.path.join(SAWC, \\\"rt\\\", host, \\\"reactor.saw\\\")\\n+        m = re.search(r\\\"func rt_reactor_register\\\\([^)]*\\\\)\\\\s*unsafe\\\\s*->\\\\s*Int\\\",\\n+                      _read(path))\\n+        if m is None:\\n+            problems.append(f\\\"rt/{host}/reactor.saw: `rt_reactor_register` no \\\"\\n+                            f\\\"longer returns its status (SL-355 R6)\\\")\\n+    calls = _read(CALLS)\\n+    m = re.search(r'self\\\\.functions\\\\[\\\"__saw_exec_io_register\\\"\\\\],\\\\s*\\\\n?\\\\s*'\\n+                  r'\\\\[fd, direction, ir\\\\.Constant\\\\(self\\\\.int_type, 0\\\\)\\\\]', calls)\\n+    if m is None:\\n+        problems.append(\\\"codegen/calls.py: the outside-frame `io_wait` no longer \\\"\\n+                        \\\"registers token 0 — it is the funnel's one EXEMPT \\\"\\n+                        \\\"caller only because it has no word to order\\\")\\n+    return problems\\n+\\n+\\n+def check_docstrings_name_clients():\\n+    problems = []\\n+    src = _read(TRANSFORM)\\n+    fns = _functions(src)\\n+    park = fns.get(\\\"_emit_io_park\\\", \\\"\\\")\\n+    for name in (\\\"_lower_stmt\\\", \\\"_emit_blk_call\\\", \\\"codegen/calls.py\\\",\\n+                 \\\"_verify_park_order\\\", \\\"_pending_merge\\\"):\\n+        if name not in park:\\n+            problems.append(f\\\"`_emit_io_park`'s docstring no longer names \\\"\\n+                            f\\\"`{name}`\\\")\\n+    tg = _read(TASKGROUP)\\n+    i = tg.find(\\\"func __saw_exec_io_register(\\\")\\n+    comment = tg[max(0, i - 2500):i]\\n+    for name in (\\\"_emit_io_park\\\", \\\"_lower_stmt\\\", \\\"_emit_blk_call\\\",\\n+                 \\\"_pending_merge\\\", \\\"codegen/calls.py\\\"):\\n+        if name not in comment:\\n+            problems.append(f\\\"`__saw_exec_io_register`'s comment no longer \\\"\\n+                            f\\\"names `{name}`\\\")\\n+    for name in (\\\"_emit_io_park\\\", \\\"_lower_stmt\\\", \\\"_emit_blk_call\\\",\\n+                 \\\"_pending_merge\\\", \\\"_verify_park_order\\\"):\\n+        if name not in fns:\\n+            problems.append(f\\\"a funnel docstring names `{name}`, which no \\\"\\n+                            f\\\"longer resolves in coro_transform.py\\\")\\n+    return problems\\n+\\n+\\n+# --------------------------------------------------------------------------- #\\n+# 5: the injections\\n+# --------------------------------------------------------------------------- #\\n+\\n+PARK_ANCHOR = \\\"    def _emit_io_park(self, fd, direction, target):\\\\n\\\"\\n+PARK_INJECTION = (\\n+    \\\"        import os as _saw_probe_os\\\\n\\\"\\n+    \\\"        _saw_probe = _saw_probe_os.environ.get('SAW_PARKORDER_INJECT')\\\\n\\\"\\n+    \\\"        if _saw_probe in ('store-after-arm', 'arm-unrecorded'):\\\\n\\\"\\n+    \\\"            _saw_arm = ExpressionStatement(expression=FunctionCall(\\\\n\\\"\\n+    \\\"                name=IO_REGISTER_FUNNEL,\\\\n\\\"\\n+    \\\"                arguments=[Argument(name=None, value=fd),\\\\n\\\"\\n+    \\\"                           Argument(name=None, value=direction),\\\\n\\\"\\n+    \\\"                           Argument(name=None,\\\\n\\\"\\n+    \\\"                                    value=_self_field('__io_tok'))]))\\\\n\\\"\\n+    \\\"            if _saw_probe == 'store-after-arm':\\\\n\\\"\\n+    \\\"                self._emit([_wake_store(SelfExpr(), _int(IO_PARK_WAKE)),\\\\n\\\"\\n+    \\\"                            _saw_arm])\\\\n\\\"\\n+    \\\"                self._suspend_to(_int(IO_PARK_WAKE), target)\\\\n\\\"\\n+    \\\"            else:\\\\n\\\"\\n+    \\\"                self._emit([_saw_arm])\\\\n\\\"\\n+    \\\"                self._suspend_to(_int(IO_PARK_WAKE), target,\\\\n\\\"\\n+    \\\"                                 write_wake=False)\\\\n\\\"\\n+    \\\"            return\\\\n\\\")\\n+\\n+MERGE_ANCHOR = \\\"    def _pending_merge(self, sub):\\\\n\\\"\\n+MERGE_INJECTION = (\\n+    \\\"        import os as _saw_probe_os\\\\n\\\"\\n+    \\\"        if (_saw_probe_os.environ.get('SAW_PARKORDER_INJECT')\\\\n\\\"\\n+    \\\"                == 'copy-propagation'):\\\\n\\\"\\n+    \\\"            return _wake_store(SelfExpr(), _wake_read(_self_field(sub)))\\\\n\\\")\\n+\\n+\\n+def _run_probe(inject):\\n+    env = dict(os.environ)\\n+    env.pop(\\\"SAW_DEBUG\\\", None)\\n+    env.pop(\\\"SAW_PARKORDER_INJECT\\\", None)\\n+    if inject:\\n+        env[\\\"SAW_PARKORDER_INJECT\\\"] = inject\\n+    out = os.path.join(REPO, \\\".build\\\", \\\"scratch\\\", \\\"park_order_probe\\\")\\n+    os.makedirs(os.path.dirname(out), exist_ok=True)\\n+    return subprocess.run([sys.executable, SAWC_MAIN, PROBE, \\\"-o\\\", out],\\n+                          capture_output=True, text=True, cwd=REPO, env=env)\\n+\\n+\\n+def check_injections_are_caught():\\n+    original = _read(TRANSFORM)\\n+    problems = []\\n+    for anchor in (PARK_ANCHOR, MERGE_ANCHOR):\\n+        if anchor not in original:\\n+            problems.append(\\n+                f\\\"coro_transform.py: the injection anchor `{anchor.strip()}` is \\\"\\n+                f\\\"gone. Update this gate rather than dropping the check — it is \\\"\\n+                f\\\"the only thing that proves the verifier is REACHED.\\\")\\n+    if problems:\\n+        return problems\\n+    runs = [\\n+        (None, None, \\\"the control compile with nothing injected\\\"),\\n+        (\\\"store-after-arm\\\", \\\"AFTER arming the reactor\\\",\\n+         \\\"injecting the leaf clobber (a wake store after the arm)\\\"),\\n+        (\\\"arm-unrecorded\\\", \\\"before recording its own park\\\",\\n+         \\\"injecting an arm with no park recorded ahead of it\\\"),\\n+        (\\\"copy-propagation\\\", \\\"unguarded store\\\",\\n+         \\\"injecting the nested propagation as an unguarded copy\\\"),\\n+    ]\\n+    try:\\n+        patched = original.replace(PARK_ANCHOR, PARK_ANCHOR + PARK_INJECTION, 1)\\n+        patched = patched.replace(MERGE_ANCHOR, MERGE_ANCHOR + MERGE_INJECTION,\\n+                                  1)\\n+        with open(TRANSFORM, \\\"w\\\") as fh:\\n+            fh.write(patched)\\n+        for inject, want, desc in runs:\\n+            run = _run_probe(inject)\\n+            text = (run.stdout + run.stderr).strip()\\n+            if want is None:\\n+                if run.returncode != 0:\\n+                    problems.append(f\\\"{desc}: the compile FAILED, so the \\\"\\n+                                    f\\\"injections beside it prove nothing:\\\\n\\\"\\n+                                    f\\\"{text[:800]}\\\")\\n+                continue\\n+            if run.returncode == 0:\\n+                problems.append(f\\\"{desc}: the compile SUCCEEDED — \\\"\\n+                                f\\\"`_verify_park_order` did not fire, so the \\\"\\n+                                f\\\"SL-353 shape is reachable again\\\")\\n+                continue\\n+            for need in (\\\"internal compiler error\\\", want):\\n+                if need not in text:\\n+                    problems.append(f\\\"{desc}: the report does not contain \\\"\\n+                                    f\\\"`{need}`:\\\\n{text[:800]}\\\")\\n+            if \\\"Traceback (most recent call last)\\\" in text:\\n+                problems.append(f\\\"{desc}: a raw Python traceback reached the \\\"\\n+                                f\\\"user; an invariant failure is an ICE line\\\")\\n+    finally:\\n+        with open(TRANSFORM, \\\"w\\\") as fh:\\n+            fh.write(original)\\n+    return problems\\n+\\n+\\n+def main():\\n+    problems = []\\n+    problems += check_one_emitter()\\n+    problems += check_clients_route_through()\\n+    problems += check_runtime_funnel()\\n+    problems += check_docstrings_name_clients()\\n+    problems += check_injections_are_caught()\\n+    if problems:\\n+        print(f\\\"parkorder: {len(problems)} problem(s)\\\")\\n+        for p in problems:\\n+            print(f\\\"  - {p}\\\")\\n+        return 1\\n+    print(\\\"parkorder: the io park records, arms, suspends through one funnel; \\\"\\n+          \\\"the propagation merges; the runtime writes the park before the arm; \\\"\\n+          \\\"three injected SL-353 shapes each stop the compile\\\")\\n+    return 0\\n+\\n+\\n+if __name__ == \\\"__main__\\\":\\n+    sys.exit(main())\\n\",\"diff_bytes\":117339,\"diff_files\":23,\"diff_hunks\":55,\"diff_tail\":\" return 0\\n+\\n+\\n+if __name__ == \\\"__main__\\\":\\n+    sys.exit(main())\\n\",\"id\":\"SL-355\",\"request_id\":\"pid-76611-1790221534-8000\",\"title\":\"The park word is set before readiness is armed and merged, never re-stored; atomic wake word with a CAS latch; a failed arm is reported (SL-354); one receiver-place funnel for interior mutation through p[i] (SL-368)\"}","request_id":"pid-76611-1790221534-8000","sequence":1413,"timestamp":"1790221536"}
---
