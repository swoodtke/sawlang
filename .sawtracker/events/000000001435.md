---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"agent:claude-sl356","author":"agent:claude-sawlang","closed":"1790256096","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 5 (rides with SL-356) in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188488","id":"c1"},{"author":"agent:claude-sawlang","body":"Landed with SL-356 as 9d285948. examples/generic_struct_literal_retains_fields.saw pins the generic-struct field retain SL-350 fixed: Arc and heap-owned String cells with strong counts and deinit traces, a generic enum payload, a nested generic literal, a literal built in a driven body, and an ExplicitCopy field. A String-specific missing-field-retain mutation fails the pin (codex c4 item 3).\n","created":"1790256096","id":"c2","kind":"landing"}],"created":"1790184459","description":"Found by the independent review of SL-350.p1 (SL-350's review comment, P2-1; Sep 23). Before SL-350, `_struct_init_info` looked field types up with `namespace.get_struct_fields(<mangled name>)`, which returns nothing for a monomorphized generic struct, so a generic struct's `Arc`/`String` field got NO retain at construction: `struct Cell<T> { value: T, tag: Int }` built as `Cell<Arc<Int>>(value: a, tag: 1)` printed strong counts 1 / 2 / 4554 and died with `over-release of an Arc (refcount underflow)` (main 73dd98b8); the `String` twin aborted with `over-release of a String reference`. SL-350 resolves the type through `_struct_field_saw_type` (base name + `mono_struct_args`, substituted), which fixes it (1 / 3 / 1) — but nothing PINS it, so a refactor of the field-type source could silently restore the bug. OWED: an `examples/` pin with the Arc and String cells (strong counts + deinit traces), plus the sibling positions the mechanism reaches (a generic ENUM payload, a nested generic literal, a generic struct built in a driven body). Small; ride the next codegen patch (SL-356's, say).","id":"SL-362","labels":["bug","codegen","generics","pin"],"order":5,"parent":"SL-367","priority":"normal","project":"SL","queue_order":0,"revision":6,"sequence":1435,"stage":"queued","status":"closed","title":"Pin the generic-struct field retain SL-350 fixed as a side effect: Cell<Arc<Int>>(value: a, tag: 1) over-released on main (strong count 1/2/4554 then panic) because the field-type lookup answered nothing for a monomorphized key","updated":"1790256096"},"before":{"acceptance":[],"assignee":"agent:claude-sl356","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 5 (rides with SL-356) in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188488","id":"c1"}],"created":"1790184459","description":"Found by the independent review of SL-350.p1 (SL-350's review comment, P2-1; Sep 23). Before SL-350, `_struct_init_info` looked field types up with `namespace.get_struct_fields(<mangled name>)`, which returns nothing for a monomorphized generic struct, so a generic struct's `Arc`/`String` field got NO retain at construction: `struct Cell<T> { value: T, tag: Int }` built as `Cell<Arc<Int>>(value: a, tag: 1)` printed strong counts 1 / 2 / 4554 and died with `over-release of an Arc (refcount underflow)` (main 73dd98b8); the `String` twin aborted with `over-release of a String reference`. SL-350 resolves the type through `_struct_field_saw_type` (base name + `mono_struct_args`, substituted), which fixes it (1 / 3 / 1) — but nothing PINS it, so a refactor of the field-type source could silently restore the bug. OWED: an `examples/` pin with the Arc and String cells (strong counts + deinit traces), plus the sibling positions the mechanism reaches (a generic ENUM payload, a nested generic literal, a generic struct built in a driven body). Small; ride the next codegen patch (SL-356's, say).","id":"SL-362","labels":["bug","codegen","generics","pin"],"order":5,"parent":"SL-367","priority":"normal","project":"SL","queue_order":0,"revision":5,"sequence":1325,"stage":"queued","status":"open","title":"Pin the generic-struct field retain SL-350 fixed as a side effect: Cell<Arc<Int>>(value: a, tag: 1) over-released on main (strong count 1/2/4554 then panic) because the field-type lookup answered nothing for a monomorphized key","updated":"1790188575"},"id":"SL-362","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Landed with SL-356 as 9d285948. examples/generic_struct_literal_retains_fields.saw pins the generic-struct field retain SL-350 fixed: Arc and heap-owned String cells with strong counts and deinit traces, a generic enum payload, a nested generic literal, a literal built in a driven body, and an ExplicitCopy field. A String-specific missing-field-retain mutation fails the pin (codex c4 item 3).\\n\",\"id\":\"SL-362\",\"request_id\":\"pid-97723-1790256096-5000\"}","request_id":"pid-97723-1790256096-5000","sequence":1435,"timestamp":"1790256096"}
---
