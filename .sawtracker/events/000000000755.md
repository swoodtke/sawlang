---
{"action":"create","actor":"agent:claude-sl213","after":{"assignee":"agent:claude-sl213","author":"agent:claude-sl213","comments":[],"created":"1789253856","description":"A closure literal NESTED inside another one, both naming a frame-resident local\nof a driven body, is an INTERNAL COMPILER ERROR — on a program whose sync twin\ncompiles and runs.\n\n    internal compiler error at f.saw:11:27 (Identifier): Undefined variable: __cap0_t\n\nFound by the SL-213 r2 obligation-4 sweep (the nested-literal cell). Reproduces\non origin/main (5755f60b) with NO capture spec written anywhere, so it is\nindependent of SL-267/SL-268; the branch reaches it too, because per-literal\nmaterialization newly routes an authored `[copy x]` through the same rename.\nFIXED IN THE SL-213 r3 LANDING (see below for why it was not separable).\n\nMECHANISM (named, per obligation 4). `_materialize_closure_captures`\n(sawc/coro_transform.py) materializes a captured frame local as a `let` ahead of\nthe closure and RENAMES the closure onto that fresh local — two closures in one\nblock cannot both declare a `let n` (DF-196e). The rename is\n`_rename_in_closure`, which does two things:\n\n    map_nodes(cexpr.body, rule)      # identifier reads, a `move`, a call head\n    ...rename cexpr.capture_specs / cexpr.captures / cexpr.capture_modes\n\nThe first reaches a NESTED literal's body — those are ordinary AST nodes under\n`cexpr.body`, and `map_nodes` walks them. The second did not: a nested literal\ncarries its OWN three capture lists, which hold plain strings that no identifier\nrewrite can see. So the inner closure went on declaring a capture of `t` while\nits body read `__cap0_t`, and codegen built an environment with a field for `t`\nand no binding named `__cap0_t` — hence the undefined-variable ICE at the inner\nread.\n\nREPRO (no capture specs at all):\n\n    struct Tick { n: Int }\n    extension Tick: Copy { func copy(&self) -> Tick { Tick(n: self.n) } }\n\n    func run_tick(body: () sync -> Int) -> Int { body() }\n\n    func both_implicit_sync(t: Tick) -> Int { run_tick({ run_tick({ t.n }) }) }\n    func both_implicit_driven(t: Tick) -> Int {\n        __saw_suspend()\n        run_tick({ run_tick({ t.n }) })\n    }\n\n    func main() {\n        print(\"-- both implicit sync\")\n        print(\"= {}\", both_implicit_sync(Tick(n: 30)))\n        print(\"-- both implicit driven\")\n        print(\"= {}\", __saw_drive(both_implicit_driven(Tick(n: 30))))\n        print(\"end\")\n    }\n\nOn origin/main the sync line compiles and the driven one is the ICE above; the\nwhole program therefore does not build.\n\nFIX. `_rename_in_closure`'s rule now renames the bookkeeping of every nested\n`ClosureExpr` it walks, through one `_rename_closure_bookkeeping` helper that\nthe outer literal also goes through — one rename, every literal in the subtree.\nSafe as a flat rename for the same reason the identifier half is:\n`_uniquify_bindings` has already made every binding in the tree unique by name,\nso no binding inside the closure can be spelled the old name and shadow it.\n\nWHY IT LANDED WITH SL-213 rather than on its own: that branch makes an authored\n`[copy x]` take the rename path (each `[copy]` capture needs its own local, so\nthe frame local's name can no longer be reused), which puts `{ [copy t] in ...\n{ t.n } ... }` on exactly this ICE. Leaving the helper incomplete would have\nmeant the branch INTRODUCING the failure for a shape that worked; the two are\none mechanism and the fix is the same three lines either way.\n\nPINNED BY: `examples/nested_closure_captures_frame_local.saw` — the all-implicit\npair (this repro), an authored outer over an implicit inner, and an authored\n`[copy]` pair, sync and driven.\n","id":"SL-282","labels":["bug"],"priority":"high","project":"SL","revision":1,"sequence":755,"stage":"queued","status":"open","title":"A nested closure literal naming a frame local is an ICE: the capture rename misses nested literals' capture bookkeeping","updated":"1789253856"},"before":null,"id":"SL-282","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl213\",\"assignee\":\"agent:claude-sl213\",\"description\":\"A closure literal NESTED inside another one, both naming a frame-resident local\\nof a driven body, is an INTERNAL COMPILER ERROR — on a program whose sync twin\\ncompiles and runs.\\n\\n    internal compiler error at f.saw:11:27 (Identifier): Undefined variable: __cap0_t\\n\\nFound by the SL-213 r2 obligation-4 sweep (the nested-literal cell). Reproduces\\non origin/main (5755f60b) with NO capture spec written anywhere, so it is\\nindependent of SL-267/SL-268; the branch reaches it too, because per-literal\\nmaterialization newly routes an authored `[copy x]` through the same rename.\\nFIXED IN THE SL-213 r3 LANDING (see below for why it was not separable).\\n\\nMECHANISM (named, per obligation 4). `_materialize_closure_captures`\\n(sawc/coro_transform.py) materializes a captured frame local as a `let` ahead of\\nthe closure and RENAMES the closure onto that fresh local — two closures in one\\nblock cannot both declare a `let n` (DF-196e). The rename is\\n`_rename_in_closure`, which does two things:\\n\\n    map_nodes(cexpr.body, rule)      # identifier reads, a `move`, a call head\\n    ...rename cexpr.capture_specs / cexpr.captures / cexpr.capture_modes\\n\\nThe first reaches a NESTED literal's body — those are ordinary AST nodes under\\n`cexpr.body`, and `map_nodes` walks them. The second did not: a nested literal\\ncarries its OWN three capture lists, which hold plain strings that no identifier\\nrewrite can see. So the inner closure went on declaring a capture of `t` while\\nits body read `__cap0_t`, and codegen built an environment with a field for `t`\\nand no binding named `__cap0_t` — hence the undefined-variable ICE at the inner\\nread.\\n\\nREPRO (no capture specs at all):\\n\\n    struct Tick { n: Int }\\n    extension Tick: Copy { func copy(&self) -> Tick { Tick(n: self.n) } }\\n\\n    func run_tick(body: () sync -> Int) -> Int { body() }\\n\\n    func both_implicit_sync(t: Tick) -> Int { run_tick({ run_tick({ t.n }) }) }\\n    func both_implicit_driven(t: Tick) -> Int {\\n        __saw_suspend()\\n        run_tick({ run_tick({ t.n }) })\\n    }\\n\\n    func main() {\\n        print(\\\"-- both implicit sync\\\")\\n        print(\\\"= {}\\\", both_implicit_sync(Tick(n: 30)))\\n        print(\\\"-- both implicit driven\\\")\\n        print(\\\"= {}\\\", __saw_drive(both_implicit_driven(Tick(n: 30))))\\n        print(\\\"end\\\")\\n    }\\n\\nOn origin/main the sync line compiles and the driven one is the ICE above; the\\nwhole program therefore does not build.\\n\\nFIX. `_rename_in_closure`'s rule now renames the bookkeeping of every nested\\n`ClosureExpr` it walks, through one `_rename_closure_bookkeeping` helper that\\nthe outer literal also goes through — one rename, every literal in the subtree.\\nSafe as a flat rename for the same reason the identifier half is:\\n`_uniquify_bindings` has already made every binding in the tree unique by name,\\nso no binding inside the closure can be spelled the old name and shadow it.\\n\\nWHY IT LANDED WITH SL-213 rather than on its own: that branch makes an authored\\n`[copy x]` take the rename path (each `[copy]` capture needs its own local, so\\nthe frame local's name can no longer be reused), which puts `{ [copy t] in ...\\n{ t.n } ... }` on exactly this ICE. Leaving the helper incomplete would have\\nmeant the branch INTRODUCING the failure for a shape that worked; the two are\\none mechanism and the fix is the same three lines either way.\\n\\nPINNED BY: `examples/nested_closure_captures_frame_local.saw` — the all-implicit\\npair (this repro), an authored outer over an implicit inner, and an authored\\n`[copy]` pair, sync and driven.\\n\",\"labels\":[\"bug\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"pid-58923-1789253856-9000\",\"stage\":\"queued\",\"title\":\"A nested closure literal naming a frame local is an ICE: the capture rename misses nested literals' capture bookkeeping\"}","request_id":"pid-58923-1789253856-9000","sequence":755,"timestamp":"1789253856"}
---
