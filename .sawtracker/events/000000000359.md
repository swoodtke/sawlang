---
{"action":"create","actor":"agent:claude-sl222","after":{"assignee":"","author":"agent:claude-sl222","comments":[],"created":"1789056936","description":"In a driven body, `let x = try f() catch { take(move r) }` -- an INLINE catch whose value is consumed, with a `move` of a frame local in the catch block's tail -- is refused: \"move of a frame local in a nested tail-expression of driven X whose value is used is not supported; bind it first\".\n\nRepro (single file, no modules):\n\n  import std.task.{yield_now}\n  struct Res { name: String }\n  extension Res: NoCopy {}\n  struct Bad { why: String }\n  extension Bad: Error {\n      func format(&self, into: &var StringBuilder) { try! into.append(self.why) }\n  }\n  func note(r: &Res) -> Int { r.name.len() }\n  func take(r: Res) -> Int { 1 }\n  func fallible(k: Int) -> Result<Int, Bad> {\n      if k < 0 { return Bad(why: \"neg\") }\n      k\n  }\n  func run(items: Vector<Int>) -> Int {\n      yield_now()\n      var total = 0\n      for i in items.iter() {\n          var r = Res(name: \"item\")\n          if note(&r) + i < 0 { return -1 }\n          let got = try fallible(0 - i) catch { take(move r) }\n          total = total + got\n      }\n      total\n  }\n  func main() { print(__saw_drive(run([1, 2]))) }\n\nMECHANISM: every OTHER value-carrying tail is turned into an ASSIGNMENT before the tail handling sees it, and an assignment hosts the drop-flag clears fine -- that is what `_lower_value_conditionals` / `_cond_to_branch` / `_attach_sink_block` do for a value `if`, a value `match`, `??`, and the BLOCK form `try { } catch { }`. The INLINE catch is a `TryExpr`, and `_is_value_conditional` does not list it, so no sink lowering runs and its catch tail stays a genuine value tail with nowhere to put the forget. Adding `TryExpr` to that lowering is the shape of the fix; it is a change to the value-conditional path (which also carries SL-215's open inline-catch limitation), so it is filed rather than bundled into SL-222's review round.\n\nWorkaround, and what the diagnostic already says: bind it first -- `let v = take(move r)` inside the catch, then `v` as the catch's tail.\n\nPRE-EXISTING: reproduces identically on the SL-222 patch base with none of the review-round changes applied. Found by the review-r2 CONTAINER_KINDS audit, which is also what makes it worth recording: it is the one source shape that DOES reach the value-used refusal, so SL-222's claim that nothing reaches it is now stated with this exception rather than as an absolute.","id":"SL-231","labels":["bug","coroutine-transform"],"priority":"normal","project":"SL","revision":1,"sequence":359,"stage":"backlog","status":"open","title":"A value-carrying inline catch cannot host a move of a frame local in its tail","updated":"1789056936"},"before":null,"id":"SL-231","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl222\",\"description\":\"In a driven body, `let x = try f() catch { take(move r) }` -- an INLINE catch whose value is consumed, with a `move` of a frame local in the catch block's tail -- is refused: \\\"move of a frame local in a nested tail-expression of driven X whose value is used is not supported; bind it first\\\".\\n\\nRepro (single file, no modules):\\n\\n  import std.task.{yield_now}\\n  struct Res { name: String }\\n  extension Res: NoCopy {}\\n  struct Bad { why: String }\\n  extension Bad: Error {\\n      func format(&self, into: &var StringBuilder) { try! into.append(self.why) }\\n  }\\n  func note(r: &Res) -> Int { r.name.len() }\\n  func take(r: Res) -> Int { 1 }\\n  func fallible(k: Int) -> Result<Int, Bad> {\\n      if k < 0 { return Bad(why: \\\"neg\\\") }\\n      k\\n  }\\n  func run(items: Vector<Int>) -> Int {\\n      yield_now()\\n      var total = 0\\n      for i in items.iter() {\\n          var r = Res(name: \\\"item\\\")\\n          if note(&r) + i < 0 { return -1 }\\n          let got = try fallible(0 - i) catch { take(move r) }\\n          total = total + got\\n      }\\n      total\\n  }\\n  func main() { print(__saw_drive(run([1, 2]))) }\\n\\nMECHANISM: every OTHER value-carrying tail is turned into an ASSIGNMENT before the tail handling sees it, and an assignment hosts the drop-flag clears fine -- that is what `_lower_value_conditionals` / `_cond_to_branch` / `_attach_sink_block` do for a value `if`, a value `match`, `??`, and the BLOCK form `try { } catch { }`. The INLINE catch is a `TryExpr`, and `_is_value_conditional` does not list it, so no sink lowering runs and its catch tail stays a genuine value tail with nowhere to put the forget. Adding `TryExpr` to that lowering is the shape of the fix; it is a change to the value-conditional path (which also carries SL-215's open inline-catch limitation), so it is filed rather than bundled into SL-222's review round.\\n\\nWorkaround, and what the diagnostic already says: bind it first -- `let v = take(move r)` inside the catch, then `v` as the catch's tail.\\n\\nPRE-EXISTING: reproduces identically on the SL-222 patch base with none of the review-round changes applied. Found by the review-r2 CONTAINER_KINDS audit, which is also what makes it worth recording: it is the one source shape that DOES reach the value-used refusal, so SL-222's claim that nothing reaches it is now stated with this exception rather than as an absolute.\",\"labels\":[\"bug\",\"coroutine-transform\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"pid-98137-1789056936-14000\",\"title\":\"A value-carrying inline catch cannot host a move of a frame local in its tail\"}","request_id":"pid-98137-1789056936-14000","sequence":359,"timestamp":"1789056936"}
---
