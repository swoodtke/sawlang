---
{"action":"create","actor":"agent:claude-sawtracker","after":{"assignee":"","author":"agent:claude-sawtracker","comments":[],"created":"1789680614","description":"Sockets the runtime creates (`rt_tcp_listen`, `rt_tcp_accept`, `rt_tcp_connect_start`) are not opened with `FD_CLOEXEC`, so every child a Saw program spawns through `Command` inherits the listening socket and every client connection open at fork time.\n\nObserved in sawtracker (recorded as ST-SAW-17 in its SAW_NOTES): each `git` and each `sh -c` test command the server spawns inherits the accepted HTTP connection. Server-side that is invisible, since the response carries `Content-Length` and the server closes its own copy. Client-side it depends on the reader: a client that reads to EOF does not see EOF until every inherited copy of the fd closes, that is, until the test command exits. Measured: a `patch add` that starts an 8-second gate run takes 8.25 s to return; a gate that never finishes hangs the client. Killing the server also does not free the port while an orphaned child still holds the inherited listener.\n\nExpected: runtime-created sockets are close-on-exec (`SOCK_CLOEXEC` at `socket()`/`accept4()`, or `fcntl(FD_CLOEXEC)` on macOS where `accept4` is unavailable), matching what every other runtime does by default. A child that genuinely needs an inherited socket is the exception and should opt in.\n\nNo standalone reproducer yet; the shape is: listen, accept one connection, spawn `sh -c 'sleep 5'` from the handler, write a `Content-Length` response, close the stream, and observe that a client reading to EOF waits the full five seconds. Sawtracker works around it in its tests by using clients that honor `Content-Length`.\n","id":"SL-307","labels":["bug","runtime","net"],"priority":"normal","project":"SL","revision":1,"sequence":924,"stage":"backlog","status":"open","title":"Runtime sockets are not FD_CLOEXEC: spawned commands inherit the listener and open connections","updated":"1789680614"},"before":null,"id":"SL-307","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawtracker\",\"description\":\"Sockets the runtime creates (`rt_tcp_listen`, `rt_tcp_accept`, `rt_tcp_connect_start`) are not opened with `FD_CLOEXEC`, so every child a Saw program spawns through `Command` inherits the listening socket and every client connection open at fork time.\\n\\nObserved in sawtracker (recorded as ST-SAW-17 in its SAW_NOTES): each `git` and each `sh -c` test command the server spawns inherits the accepted HTTP connection. Server-side that is invisible, since the response carries `Content-Length` and the server closes its own copy. Client-side it depends on the reader: a client that reads to EOF does not see EOF until every inherited copy of the fd closes, that is, until the test command exits. Measured: a `patch add` that starts an 8-second gate run takes 8.25 s to return; a gate that never finishes hangs the client. Killing the server also does not free the port while an orphaned child still holds the inherited listener.\\n\\nExpected: runtime-created sockets are close-on-exec (`SOCK_CLOEXEC` at `socket()`/`accept4()`, or `fcntl(FD_CLOEXEC)` on macOS where `accept4` is unavailable), matching what every other runtime does by default. A child that genuinely needs an inherited socket is the exception and should opt in.\\n\\nNo standalone reproducer yet; the shape is: listen, accept one connection, spawn `sh -c 'sleep 5'` from the handler, write a `Content-Length` response, close the stream, and observe that a client reading to EOF waits the full five seconds. Sawtracker works around it in its tests by using clients that honor `Content-Length`.\\n\",\"labels\":[\"bug\",\"runtime\",\"net\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"sl-cloexec-2026-09-17\",\"title\":\"Runtime sockets are not FD_CLOEXEC: spawned commands inherit the listener and open connections\"}","request_id":"sl-cloexec-2026-09-17","sequence":924,"timestamp":"1789680614"}
---
