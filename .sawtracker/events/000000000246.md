---
{"action":"edit","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791612","description":"## Scope and status\n\nDF-308a: Reassess scalar literal syntax against labeled construction cost\n\nScalar v1 is landed. Named arguments in struct/init construction are deliberate design 66 semantics, not a compiler defect. Commit 6909b668 already corrected design 245 section 6’s erroneous premise. Only re-reading section 4’s scalar-literal usability verdict against try! Scalar(value: N), plus the deferred literal/pattern design decisions, remains here; no syntax change is approved by this task.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: retain the source’s deferral or revisit trigger.\n\n## Proposed plan\n\n1. Reconcile the linked brief and recorded rulings with current consumers. Keep already-landed behavior and stated deferrals explicit.\n2. Draft the remaining contract: supported syntax/API, ownership and failure behavior, alternatives, compatibility effects and the exact questions still requiring a decision.\n3. Define small implementation units and consumer migrations, then specify the positive, negative and boundary tests before dispatch.\n\n## Acceptance criteria\n\n- [ ] The design names a concrete consumer or retains its recorded revisit trigger.\n- [ ] Existing rulings are preserved; unresolved choices and a recommended option are explicit.\n- [ ] The proposed API/semantics, migration scope and acceptance tests are reviewable. Drafting this plan does not mark an unruled design approved.\n\n## Related tracker work\n\nThese are related findings/plans; a reference alone does not imply a blocking dependency.\n\n- SL-1 — Design 245: implement the ruled Scalar v1 surface\n\n## Existing design references\n\n- [sawlang/designs/245-unicode-scalar-type.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/245-unicode-scalar-type.md)\n- [sawlang/designs/66-labeled-arguments.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/66-labeled-arguments.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 194–228\n\n[Original record](https://github.com/swoodtke/sawlang/blob/9b76bb491e088d8a7d0b8eaade56da9753f6305a/designs/todo.md#L194-L228)\n\n## DF‑308a — a struct construction takes NO POSITIONAL argument, so a one-value\n## wrapper's constructor always writes its label (filed Sep 7 by design 245 v1;\n## NOT A DEFECT — the spec rules it deliberate. Filed because a BRIEF asserts\n## the opposite and because it prices §4)\n\n```saw-error\n// error-contains: struct initialization requires named arguments\nstruct Wrap { raw: Int }\nextension Wrap {\n    init(value: Int) -> Wrap { Wrap(raw: value) }\n}\nlet a = Wrap(7)   // error: struct initialization requires named arguments\n```\nDesign 245 §6 says \"`init(value: Int) -> Result<Scalar, InvalidScalar>`\n(lenient labels make `try! Scalar(34)` legal)\". They do not, and never did.\nLANGUAGE_SPEC's own labeling section rules it: \"Struct/`init` and enum payload\nconstruction keep their own **order-independent** name matching …; they are a\nseparate resolution scheme from the ordered call binding rule, by design\n(design 66).\" MECHANISM, matching the ruling exactly:\n`_check_function_call`'s struct branch (`typechecker/expressions.py:4957`)\nrewrites the call into a `StructInit` and requires every argument to carry a\nname BEFORE `_check_struct_init` resolves which `init` (if any) the call means\n— so the refusal reaches every struct with an `init`, fallible or not, generic\nor not, one field or several. It is uniform and it is documented, which is why\nthis is a NOTE and not a bug.\n\nWhat it costs, and why the tracker carries it: design 245 v1 shipped with the\nlabeled spelling (`try! Scalar(value: 34)`), and §4's still-open scalar-literal\nquestion is priced against it. The comparison idiom the brief calls \"honest\"\nis unaffected (`ch.value() == 34` names no constructor), but every\nCONSTRUCTION in a would-be-literal position is `try! Scalar(value: N)` — nine\nwords where the brief budgeted six. Two things want doing, neither scheduled:\ncorrect §6's premise sentence, and re-read §4's \"unusable\" verdict against the\nreal spelling. [245, 66]\n","id":"SL-203","labels":["todo-import","design","plan","design-proposal","deferred","backlog"],"priority":"low","project":"SL","revision":2,"sequence":246,"status":"open","title":"DF-308a: Reassess scalar literal syntax against labeled construction cost","updated":"1788791612"},"before":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791612","description":"## Scope and status\n\nDF-308a: Reassess scalar literal syntax against labeled construction cost\n\nScalar v1 is landed. Named arguments in struct/init construction are deliberate design 66 semantics, not a compiler defect. Commit 6909b668 already corrected design 245 section 6’s erroneous premise. Only re-reading section 4’s scalar-literal usability verdict against try! Scalar(value: N), plus the deferred literal/pattern design decisions, remains here; no syntax change is approved by this task.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: retain the source’s deferral or revisit trigger.\n\n## Proposed plan\n\n1. Reconcile the linked brief and recorded rulings with current consumers. Keep already-landed behavior and stated deferrals explicit.\n2. Draft the remaining contract: supported syntax/API, ownership and failure behavior, alternatives, compatibility effects and the exact questions still requiring a decision.\n3. Define small implementation units and consumer migrations, then specify the positive, negative and boundary tests before dispatch.\n\n## Acceptance criteria\n\n- [ ] The design names a concrete consumer or retains its recorded revisit trigger.\n- [ ] Existing rulings are preserved; unresolved choices and a recommended option are explicit.\n- [ ] The proposed API/semantics, migration scope and acceptance tests are reviewable. Drafting this plan does not mark an unruled design approved.\n\n## Existing design references\n\n- [sawlang/designs/245-unicode-scalar-type.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/245-unicode-scalar-type.md)\n- [sawlang/designs/66-labeled-arguments.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/66-labeled-arguments.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 194–228\n\n[Original record](https://github.com/swoodtke/sawlang/blob/9b76bb491e088d8a7d0b8eaade56da9753f6305a/designs/todo.md#L194-L228)\n\n## DF‑308a — a struct construction takes NO POSITIONAL argument, so a one-value\n## wrapper's constructor always writes its label (filed Sep 7 by design 245 v1;\n## NOT A DEFECT — the spec rules it deliberate. Filed because a BRIEF asserts\n## the opposite and because it prices §4)\n\n```saw-error\n// error-contains: struct initialization requires named arguments\nstruct Wrap { raw: Int }\nextension Wrap {\n    init(value: Int) -> Wrap { Wrap(raw: value) }\n}\nlet a = Wrap(7)   // error: struct initialization requires named arguments\n```\nDesign 245 §6 says \"`init(value: Int) -> Result<Scalar, InvalidScalar>`\n(lenient labels make `try! Scalar(34)` legal)\". They do not, and never did.\nLANGUAGE_SPEC's own labeling section rules it: \"Struct/`init` and enum payload\nconstruction keep their own **order-independent** name matching …; they are a\nseparate resolution scheme from the ordered call binding rule, by design\n(design 66).\" MECHANISM, matching the ruling exactly:\n`_check_function_call`'s struct branch (`typechecker/expressions.py:4957`)\nrewrites the call into a `StructInit` and requires every argument to carry a\nname BEFORE `_check_struct_init` resolves which `init` (if any) the call means\n— so the refusal reaches every struct with an `init`, fallible or not, generic\nor not, one field or several. It is uniform and it is documented, which is why\nthis is a NOTE and not a bug.\n\nWhat it costs, and why the tracker carries it: design 245 v1 shipped with the\nlabeled spelling (`try! Scalar(value: 34)`), and §4's still-open scalar-literal\nquestion is priced against it. The comparison idiom the brief calls \"honest\"\nis unaffected (`ch.value() == 34` names no constructor), but every\nCONSTRUCTION in a would-be-literal position is `try! Scalar(value: N)` — nine\nwords where the brief budgeted six. Two things want doing, neither scheduled:\ncorrect §6's premise sentence, and re-read §4's \"unusable\" verdict against the\nreal spelling. [245, 66]\n","id":"SL-203","labels":["todo-import","design","plan","design-proposal","deferred","backlog"],"priority":"low","project":"SL","revision":1,"sequence":245,"status":"open","title":"DF-308a: Reassess scalar literal syntax against labeled construction cost","updated":"1788791612"},"id":"SL-203","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-308a: Reassess scalar literal syntax against labeled construction cost\\n\\nScalar v1 is landed. Named arguments in struct/init construction are deliberate design 66 semantics, not a compiler defect. Commit 6909b668 already corrected design 245 section 6’s erroneous premise. Only re-reading section 4’s scalar-literal usability verdict against try! Scalar(value: N), plus the deferred literal/pattern design decisions, remains here; no syntax change is approved by this task.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: retain the source’s deferral or revisit trigger.\\n\\n## Proposed plan\\n\\n1. Reconcile the linked brief and recorded rulings with current consumers. Keep already-landed behavior and stated deferrals explicit.\\n2. Draft the remaining contract: supported syntax/API, ownership and failure behavior, alternatives, compatibility effects and the exact questions still requiring a decision.\\n3. Define small implementation units and consumer migrations, then specify the positive, negative and boundary tests before dispatch.\\n\\n## Acceptance criteria\\n\\n- [ ] The design names a concrete consumer or retains its recorded revisit trigger.\\n- [ ] Existing rulings are preserved; unresolved choices and a recommended option are explicit.\\n- [ ] The proposed API/semantics, migration scope and acceptance tests are reviewable. Drafting this plan does not mark an unruled design approved.\\n\\n## Related tracker work\\n\\nThese are related findings/plans; a reference alone does not imply a blocking dependency.\\n\\n- SL-1 — Design 245: implement the ruled Scalar v1 surface\\n\\n## Existing design references\\n\\n- [sawlang/designs/245-unicode-scalar-type.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/245-unicode-scalar-type.md)\\n- [sawlang/designs/66-labeled-arguments.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/66-labeled-arguments.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 194–228\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/9b76bb491e088d8a7d0b8eaade56da9753f6305a/designs/todo.md#L194-L228)\\n\\n## DF‑308a — a struct construction takes NO POSITIONAL argument, so a one-value\\n## wrapper's constructor always writes its label (filed Sep 7 by design 245 v1;\\n## NOT A DEFECT — the spec rules it deliberate. Filed because a BRIEF asserts\\n## the opposite and because it prices §4)\\n\\n```saw-error\\n// error-contains: struct initialization requires named arguments\\nstruct Wrap { raw: Int }\\nextension Wrap {\\n    init(value: Int) -> Wrap { Wrap(raw: value) }\\n}\\nlet a = Wrap(7)   // error: struct initialization requires named arguments\\n```\\nDesign 245 §6 says \\\"`init(value: Int) -> Result<Scalar, InvalidScalar>`\\n(lenient labels make `try! Scalar(34)` legal)\\\". They do not, and never did.\\nLANGUAGE_SPEC's own labeling section rules it: \\\"Struct/`init` and enum payload\\nconstruction keep their own **order-independent** name matching …; they are a\\nseparate resolution scheme from the ordered call binding rule, by design\\n(design 66).\\\" MECHANISM, matching the ruling exactly:\\n`_check_function_call`'s struct branch (`typechecker/expressions.py:4957`)\\nrewrites the call into a `StructInit` and requires every argument to carry a\\nname BEFORE `_check_struct_init` resolves which `init` (if any) the call means\\n— so the refusal reaches every struct with an `init`, fallible or not, generic\\nor not, one field or several. It is uniform and it is documented, which is why\\nthis is a NOTE and not a bug.\\n\\nWhat it costs, and why the tracker carries it: design 245 v1 shipped with the\\nlabeled spelling (`try! Scalar(value: 34)`), and §4's still-open scalar-literal\\nquestion is priced against it. The comparison idiom the brief calls \\\"honest\\\"\\nis unaffected (`ch.value() == 34` names no constructor), but every\\nCONSTRUCTION in a would-be-literal position is `try! Scalar(value: N)` — nine\\nwords where the brief budgeted six. Two things want doing, neither scheduled:\\ncorrect §6's premise sentence, and re-read §4's \\\"unusable\\\" verdict against the\\nreal spelling. [245, 66]\\n\",\"id\":\"SL-203\",\"request_id\":\"todo-20260907-0eb626e5aae515667bb9-links\",\"revision\":1}","request_id":"todo-20260907-0eb626e5aae515667bb9-links","sequence":246,"timestamp":"1788791612"}
---
