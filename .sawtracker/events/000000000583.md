---
{"action":"close","actor":"agent:claude-sawlang","after":{"assignee":"","author":"agent:claude-sl226","comments":[{"author":"agent:claude-sawlang","body":"LANDED: fixed by SL-226.p1 (merge 9609696e, Sep 11) — same duplicated codegen pointer dispatch as SL-226: &var o![1] / &var t.0[1] silent lost writes in PLAIN SYNC code and the &var [T;N]-param ICE all trace to _get_array_element_pointer's spilled-copy addressing; the funnel unification fixes every face. Covered by the SL-226 regression tests' sync twins.","created":"1789138410","id":"c1"}],"created":"1789096322","description":"`&`/`&var` into an array ELEMENT addresses a materialized COPY whenever the container's base is not one of four forms the reference path happens to know. The write is silently discarded (exit 0, no diagnostic) for two of the missing bases and is an internal compiler error for the third. NO COROUTINE IS INVOLVED in any cell below -- this is plain synchronous code.\n\nRepro (single file, measured on origin/main c9991092; every cell writes +7 into an element holding 1, so 8 is correct and 1 is a lost write):\n\n  struct Doc { n: Int }\n  func set_one(d: &var Doc, k: Int) { d.n = d.n + k }\n\n  func optional_base() -> Int {                  // observed 1, wanted 8\n      var o: [Doc; 3]? = [Doc(n: 1); 3]\n      set_one(&var o![1], 7)\n      o![1].n\n  }\n  func tuple_base() -> Int {                     // observed 1, wanted 8\n      var t: ([Doc; 3], Int) = ([Doc(n: 1); 3], 0)\n      set_one(&var t.0[1], 7)\n      t.0[1].n\n  }\n  func through_var_ref(a: &var [Doc; 3], k: Int) { set_one(&var a[1], k) }\n                                                 // INTERNAL COMPILER ERROR:\n                                                 // Type of #1 arg mismatch:\n                                                 // %\"Doc\"* != [3 x %\"Doc\"]*\n\nThe WRITE spelling of the same place is correct in every cell (`o![1].n = ...` gives 8), which is the tell: a BORROW and a WRITE of one place disagree about where that place is.\n\nMECHANISM (confirmed against -O0 IR, not hypothesised): codegen carries TWO base-to-pointer dispatches. `_get_lvalue_pointer` (calls.py) is the write path's funnel and answers every base form -- Identifier incl. a reference-typed one (which it LOADS first), static, `self`, MemberAccess, ArrayIndex, TupleIndex, ForceUnwrap. The reference path (`_generate_reference_expr`, operators.py) kept a PRIVATE `_get_array_element_pointer` with a smaller set -- Identifier, `self`, MemberAccess, nested ArrayIndex -- ending in a fallback whose own comment states the defect: \"Fallback: materialize the container (won't propagate mutations)\". A base outside the small set is stored into a fresh `arr_tmp` alloca and the element GEP addresses THAT; the reference-typed identifier is worse, since its alloca's pointee is already a pointer, so the GEP strides by whole arrays and reaches LLVM as a type mismatch.\n\nEmitted -O0 IR for the optional cell, which is the whole bug in four instructions:\n\n  %unwrapped = extractvalue {i1, [3 x Doc]} %arr, 1\n  store [3 x Doc] %unwrapped, [3 x Doc]* %arr_tmp\n  %elem_ptr = getelementptr [3 x Doc], [3 x Doc]* %arr_tmp, i64 0, i64 1\n  call void @set_one(Doc* %elem_ptr, i64 %k)\n\nSAME MECHANISM AS SL-226, WHICH IS ITS COROUTINE FACE: the transform rewrites a frame-resident local `arr` into `self.arr!`, so EVERY `&var arr[i]` in a suspending body takes the ForceUnwrap base and lands in the fallback. Filed separately because these faces need no suspension at all and a reader hunting the sync bug would never look at a coroutine issue.\n\nFIXED ON THE SL-226 BRANCH by the same one-line change that fixes SL-226: `_generate_reference_expr`'s ArrayIndex arm now calls `_get_element_pointer` (which composes through `_get_lvalue_pointer`) and the private duplicate is deleted, so one funnel answers both families. Pinned by examples/ref_into_array_element_addresses_real_storage.saw (7 cells incl. the two arms that already worked, so the fix is pinned as a widening and not a swap). Close this together with SL-226 when that lands.\n","id":"SL-261","labels":["bug","codegen"],"priority":"high","project":"SL","revision":3,"sequence":583,"stage":"queued","status":"closed","title":"Borrow into an array element addresses a COPY for three container bases (optional, tuple element, &var [T;N] param) — two silent lost writes and an ICE","updated":"1789138410"},"before":{"assignee":"","author":"agent:claude-sl226","comments":[],"created":"1789096322","description":"`&`/`&var` into an array ELEMENT addresses a materialized COPY whenever the container's base is not one of four forms the reference path happens to know. The write is silently discarded (exit 0, no diagnostic) for two of the missing bases and is an internal compiler error for the third. NO COROUTINE IS INVOLVED in any cell below -- this is plain synchronous code.\n\nRepro (single file, measured on origin/main c9991092; every cell writes +7 into an element holding 1, so 8 is correct and 1 is a lost write):\n\n  struct Doc { n: Int }\n  func set_one(d: &var Doc, k: Int) { d.n = d.n + k }\n\n  func optional_base() -> Int {                  // observed 1, wanted 8\n      var o: [Doc; 3]? = [Doc(n: 1); 3]\n      set_one(&var o![1], 7)\n      o![1].n\n  }\n  func tuple_base() -> Int {                     // observed 1, wanted 8\n      var t: ([Doc; 3], Int) = ([Doc(n: 1); 3], 0)\n      set_one(&var t.0[1], 7)\n      t.0[1].n\n  }\n  func through_var_ref(a: &var [Doc; 3], k: Int) { set_one(&var a[1], k) }\n                                                 // INTERNAL COMPILER ERROR:\n                                                 // Type of #1 arg mismatch:\n                                                 // %\"Doc\"* != [3 x %\"Doc\"]*\n\nThe WRITE spelling of the same place is correct in every cell (`o![1].n = ...` gives 8), which is the tell: a BORROW and a WRITE of one place disagree about where that place is.\n\nMECHANISM (confirmed against -O0 IR, not hypothesised): codegen carries TWO base-to-pointer dispatches. `_get_lvalue_pointer` (calls.py) is the write path's funnel and answers every base form -- Identifier incl. a reference-typed one (which it LOADS first), static, `self`, MemberAccess, ArrayIndex, TupleIndex, ForceUnwrap. The reference path (`_generate_reference_expr`, operators.py) kept a PRIVATE `_get_array_element_pointer` with a smaller set -- Identifier, `self`, MemberAccess, nested ArrayIndex -- ending in a fallback whose own comment states the defect: \"Fallback: materialize the container (won't propagate mutations)\". A base outside the small set is stored into a fresh `arr_tmp` alloca and the element GEP addresses THAT; the reference-typed identifier is worse, since its alloca's pointee is already a pointer, so the GEP strides by whole arrays and reaches LLVM as a type mismatch.\n\nEmitted -O0 IR for the optional cell, which is the whole bug in four instructions:\n\n  %unwrapped = extractvalue {i1, [3 x Doc]} %arr, 1\n  store [3 x Doc] %unwrapped, [3 x Doc]* %arr_tmp\n  %elem_ptr = getelementptr [3 x Doc], [3 x Doc]* %arr_tmp, i64 0, i64 1\n  call void @set_one(Doc* %elem_ptr, i64 %k)\n\nSAME MECHANISM AS SL-226, WHICH IS ITS COROUTINE FACE: the transform rewrites a frame-resident local `arr` into `self.arr!`, so EVERY `&var arr[i]` in a suspending body takes the ForceUnwrap base and lands in the fallback. Filed separately because these faces need no suspension at all and a reader hunting the sync bug would never look at a coroutine issue.\n\nFIXED ON THE SL-226 BRANCH by the same one-line change that fixes SL-226: `_generate_reference_expr`'s ArrayIndex arm now calls `_get_element_pointer` (which composes through `_get_lvalue_pointer`) and the private duplicate is deleted, so one funnel answers both families. Pinned by examples/ref_into_array_element_addresses_real_storage.saw (7 cells incl. the two arms that already worked, so the fix is pinned as a widening and not a swap). Close this together with SL-226 when that lands.\n","id":"SL-261","labels":["bug","codegen"],"priority":"high","project":"SL","revision":2,"sequence":527,"stage":"queued","status":"open","title":"Borrow into an array element addresses a COPY for three container bases (optional, tuple element, &var [T;N] param) — two silent lost writes and an ICE","updated":"1789105672"},"id":"SL-261","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"LANDED: fixed by SL-226.p1 (merge 9609696e, Sep 11) — same duplicated codegen pointer dispatch as SL-226: &var o![1] / &var t.0[1] silent lost writes in PLAIN SYNC code and the &var [T;N]-param ICE all trace to _get_array_element_pointer's spilled-copy addressing; the funnel unification fixes every face. Covered by the SL-226 regression tests' sync twins.\",\"id\":\"SL-261\",\"request_id\":\"pid-90992-1789138410-21000\"}","request_id":"pid-90992-1789138410-21000","sequence":583,"timestamp":"1789138410"}
---
