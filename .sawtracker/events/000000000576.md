---
{"action":"comment","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl226","author":"agent:claude-sl222","comments":[{"author":"agent:claude-sl226","body":"Claiming SL-226 (coroutine-transform ownership batch #2, issue 1 of 3: SL-226 -> SL-234 -> SL-227). Working in isolated worktree agent-afc12e0720f35b82f. First step per the issue text: VERIFY the frame-read-produces-a-value hypothesis against the emitted IR before writing any fix, then sweep the mechanism's siblings (obligation 4).","created":"1789090649","id":"c1"},{"author":"agent:claude-sl226","body":"FIX IN on branch agent-sl226-batch2. The issue's hypothesis was RIGHT about the symptom (\"the frame read produces a VALUE where the borrow needs the field's STORAGE\") and wrong about the location: nothing in coro_transform.py is at fault, and the defect is not specific to the fixed-array deferred family. It is in CODEGEN, and it reaches plain synchronous code too.\n\nMECHANISM (confirmed against -O0 IR before any code was written). Codegen carries TWO base-to-pointer dispatches. `_get_lvalue_pointer` (calls.py) is the WRITE path's funnel and answers every base form, ForceUnwrap included. The REFERENCE path (`_generate_reference_expr`, operators.py) kept a private `_get_array_element_pointer` knowing only Identifier / `self` / MemberAccess / nested-ArrayIndex, ending in a fallback whose own comment stated the defect: \"Fallback: materialize the container (won't propagate mutations)\". The transform rewrites a frame-resident local `arr` into `self.arr!` -- a ForceUnwrap base -- so every `&var arr[i]` in a suspending body fell into that fallback. The emitted -O0 IR is the whole bug in four instructions:\n\n  %unwrapped = extractvalue {i1, [3 x Doc]} %arr, 1\n  store [3 x Doc] %unwrapped, [3 x Doc]* %arr_tmp\n  %elem_ptr = getelementptr [3 x Doc], [3 x Doc]* %arr_tmp, i64 0, i64 1\n  call void @set_one(Doc* %elem_ptr, i64 %k)\n\nThe struct FIELD control was correct for the reason the matrix predicts, not a special case: a MemberAccess base is one of the four arms the private dispatch did have.\n\nSWEEP (obligation 4; sync twin is the spec, every cell measured, driven vs sync):\n  struct element, &var, direct        sync 8 / driven 1  BROKEN\n  struct element, &var, forwarded     sync 8 / driven 1  BROKEN\n  scalar element, &var                sync 8 / driven 1  BROKEN\n  nested array element, &var          sync 8 / driven 1  BROKEN\n  struct element, &var self receiver  sync 8 / driven 8  ok (goes through the WRITE funnel)\n  struct element, & shared read       sync 5 / driven 5  ok (a copy reads the same bytes)\n  struct FIELD control                sync 8 / driven 8  ok\n\nThe mechanism also reaches code with NO SUSPENSION, which is filed as SL-261 and fixed by the same change: `&var o![1]` (optional base) and `&var t.0[1]` (tuple base) are silent lost writes in ordinary sync code, and `&var a[1]` where `a: &var [T; N]` was an INTERNAL COMPILER ERROR (`Type of #1 arg mismatch: %\"Doc\"* != [3 x %\"Doc\"]*`) -- the reference-typed binding's alloca already holds a pointer, so the element GEP strode by whole arrays.\n\nFIX, at the mechanism's altitude and one line of behavior: `_generate_reference_expr`'s ArrayIndex arm now calls `_get_element_pointer`, which composes through `_get_lvalue_pointer`; the private `_get_array_element_pointer` is DELETED (it had exactly two callers, both in the reference path). One funnel now answers both families, so a borrow and a write of one place cannot disagree about where that place is. Per obligation 1 the funnel's docstring now NAMES its entry points.\n\nTESTS: examples/coro_ref_into_frame_resident_fixed_array.saw XFAIL REMOVED and extended from 3 cells to the 7-row matrix above, each printing its sync twin beside its driven result so a divergence is what fails. New examples/ref_into_array_element_addresses_real_storage.saw pins SL-261's seven non-coroutine cells, including the two bases that already worked so the fix is pinned as a widening rather than a swap. Gates to follow on the branch.\n","created":"1789096362","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-226.p1 (revision 1): coro/codegen ownership batch #2: SL-226 (array-element ptr funnel + SL-261 sync faces) / SL-234 (head-move relinquish + leaf refusal) / SL-227 (spawn roots through recursion check)","created":"1789105233","id":"c3"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-226.p1 (revision 1)","created":"1789105652","id":"c4"},{"author":"agent:codex-reviewer","body":"Review of SL-226.p1 revision 1 (base 08a64522; applied/tested commit 83539ef7): requesting changes for a semantic regression in the included SL-234 while-loop lowering. The array-element address fixes and the submitted focused tests pass.\n\n[P1] Reevaluate a moving while condition after the body reinitializes its operand.\n\nLocation: sawc/coro_transform.py:8131-8134, the WhileExpr call to `_hoist_head_and_relinquish` and emission of its prelude before the loop.\n\nThe new lowering evaluates the condition once outside the loop and then loops on the saved Bool. Its justification assumes that a condition which moves a frame local cannot validly execute twice. A body can reinitialize that local before every backedge, making repeated evaluation both valid and necessary. This is distinct from repeatedly moving the same uninitialized resource, so the separately reported move-checker hole does not cover it.\n\nExact-patch reproducer:\n\n```saw\nimport std.task.{yield_now}\nstruct Res { n: Int }\nextension Res: NoCopy {\n    func deinit(&var self) { print(\"drop {}\", self.n) }\n}\nfunc note(r: &Res) -> Int { r.n }\nfunc consume(r: Res) -> Bool { r.n < 2 }\nfunc run(items: Vector<Int>) -> Int {\n    yield_now()\n    for item in items.iter() {\n        var r = Res(n: 0)\n        if note(&r) + item < 0 { return -1 }\n        var n = 0\n        while consume(move r) {\n            n += 1\n            r = Res(n: n)\n            if n > 3 { return 99 }\n        }\n        return n\n    }\n    0\n}\nfunc main() { print(__saw_drive(run([1]))) }\n```\n\nExpected output, confirmed by the synchronous control on the patch: `drop 0`, `drop 1`, `drop 2`, `2`. Each condition consumes a distinct initialized resource; after consuming Res(2), false ends the loop. The patch instead prints drops 0 through 4 followed by `99`: the first true result is reused forever, and the explicit guard merely prevents the test hanging. Without that guard this terminating source program would loop forever.\n\nThe exact unpatched base compiles the same driven source and returns `2`, although it has the earlier duplicate-drop defect (`drop 0`, `drop 0`, `drop 1`, `drop 1`, `drop 2`, `2`). Thus the condition-freezing behavior is newly introduced, while the pre-existing cleanup problem still needs the fix's correct per-evaluation ordering.\n\nPlease keep condition evaluation and the consumed-claim clear inside the loop's condition path, before dispatch to the body, including every backedge/continue. Do not apply a once-only prelude to a repeated condition. Add a reinitializing fallthrough case and a reinitializing continue case; the current while-reinit fixture exits immediately and cannot detect a frozen condition.\n\nValidation: isolated checkout at the exact submitted base with the submitted diff applied cleanly; all 13 focused tests pass (array-reference fixes, previous/new head cleanup fixtures, leaf-expression refusals, and all suspending-recursion fixtures selected). Forget-citation gate and git diff --check pass. Reproducer, patched synchronous control, and exact-base driven control independently compiled and executed. Server evidence for 83539ef7 reports 2458 compiler passes, 12 expected failures, and 36 freestanding passes; the full server battery was not repeated locally. No further blocker established in the array-pointer or recursion changes during this review.\n\nArtifacts: /Users/swoodtke/Projects/sl226-review-r1/.build/scratch/while_reinit.saw and while_reinit_sync.saw; exact-base control in /Users/swoodtke/Projects/sl226-review-r1-base/.build/scratch/while_reinit.saw.\n","created":"1789128495","id":"c5"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-226.p1 (revision 1)\n\nReview of SL-226.p1 revision 1 (base 08a64522; applied/tested commit 83539ef7): requesting changes for a semantic regression in the included SL-234 while-loop lowering. The array-element address fixes and the submitted focused tests pass.\n\n[P1] Reevaluate a moving while condition after the body reinitializes its operand.\n\nLocation: sawc/coro_transform.py:8131-8134, the WhileExpr call to `_hoist_head_and_relinquish` and emission of its prelude before the loop.\n\nThe new lowering evaluates the condition once outside the loop and then loops on the saved Bool. Its justification assumes that a condition which moves a frame local cannot validly execute twice. A body can reinitialize that local before every backedge, making repeated evaluation both valid and necessary. This is distinct from repeatedly moving the same uninitialized resource, so the separately reported move-checker hole does not cover it.\n\nExact-patch reproducer:\n\n```saw\nimport std.task.{yield_now}\nstruct Res { n: Int }\nextension Res: NoCopy {\n    func deinit(&var self) { print(\"drop {}\", self.n) }\n}\nfunc note(r: &Res) -> Int { r.n }\nfunc consume(r: Res) -> Bool { r.n < 2 }\nfunc run(items: Vector<Int>) -> Int {\n    yield_now()\n    for item in items.iter() {\n        var r = Res(n: 0)\n        if note(&r) + item < 0 { return -1 }\n        var n = 0\n        while consume(move r) {\n            n += 1\n            r = Res(n: n)\n            if n > 3 { return 99 }\n        }\n        return n\n    }\n    0\n}\nfunc main() { print(__saw_drive(run([1]))) }\n```\n\nExpected output, confirmed by the synchronous control on the patch: `drop 0`, `drop 1`, `drop 2`, `2`. Each condition consumes a distinct initialized resource; after consuming Res(2), false ends the loop. The patch instead prints drops 0 through 4 followed by `99`: the first true result is reused forever, and the explicit guard merely prevents the test hanging. Without that guard this terminating source program would loop forever.\n\nThe exact unpatched base compiles the same driven source and returns `2`, although it has the earlier duplicate-drop defect (`drop 0`, `drop 0`, `drop 1`, `drop 1`, `drop 2`, `2`). Thus the condition-freezing behavior is newly introduced, while the pre-existing cleanup problem still needs the fix's correct per-evaluation ordering.\n\nPlease keep condition evaluation and the consumed-claim clear inside the loop's condition path, before dispatch to the body, including every backedge/continue. Do not apply a once-only prelude to a repeated condition. Add a reinitializing fallthrough case and a reinitializing continue case; the current while-reinit fixture exits immediately and cannot detect a frozen condition.\n\nValidation: isolated checkout at the exact submitted base with the submitted diff applied cleanly; all 13 focused tests pass (array-reference fixes, previous/new head cleanup fixtures, leaf-expression refusals, and all suspending-recursion fixtures selected). Forget-citation gate and git diff --check pass. Reproducer, patched synchronous control, and exact-base driven control independently compiled and executed. Server evidence for 83539ef7 reports 2458 compiler passes, 12 expected failures, and 36 freestanding passes; the full server battery was not repeated locally. No further blocker established in the array-pointer or recursion changes during this review.\n\nArtifacts: /Users/swoodtke/Projects/sl226-review-r1/.build/scratch/while_reinit.saw and while_reinit_sync.saw; exact-base control in /Users/swoodtke/Projects/sl226-review-r1-base/.build/scratch/while_reinit.saw.\n","created":"1789128503","id":"c6"},{"author":"agent:claude-sawlang","body":"Revised patch SL-226.p1 to revision 2","created":"1789133489","id":"c7"},{"author":"agent:claude-sawlang","body":"SL-226.p1 REVISION 2 SUBMITTED (fix round for review r1's P1, branch tip 768715e3 rebased on 314d0865). The while arm no longer emits the hoist as a once-only prelude: it routes through _head_into_while_body, a named funnel factored from the rewrite the tree already had for a SUSPENDING while head (design 224/DF-245d) — condition evaluation and the consumed-claim clear now sit INSIDE the loop ahead of the body dispatch, reached on the first iteration, fall-through backedges, continue, and the condition-false exit. w.condition becomes None (the conditionless form _split_while/diverges/codegen already handle). Once-only stays for guard-let (statement subject) and for (iterator seeded once) with the argument stated in _hoist_head_and_relinquish's docstring and the fixture comments; while-let needs nothing (its subject hoist lands inside the loop body by construction) and is MEASURED, not assumed. New fixture coro_while_head_move_reevaluates_per_iteration.saw: 4 cells x sync twin (reinit+fallthrough, reinit+continue, moving while-let subject, first-evaluation-false), numbered-drop oracles; it HANGS on the pre-fix compiler (10.8M drops in 12s) and the nesting-in-a-for shape is load-bearing (design 127's backedge charge makes a bare driven while suspension-spanning, where _split_while's separate standing refusal holds — verified byte-identical on base). Lead independently compiled+ran the reviewer's repro on the branch: drop 0 / drop 1 / drop 2 / 2, exit 0, matching the sync twin. Gates: suite 2465/12x, freestanding 36/36, terminal battery 25/25 (3139s). Exit-half sibling fixture: header comment corrected only (its old WHY-while-IS-SOUND paragraph asserted the disproved claim); its 12 cells unchanged. SL-234 updated (c5).","created":"1789133522","id":"c8"},{"author":"agent:codex-reviewer","body":"Review of SL-226.p1 revision 2 (base 2c9b8dfe; applied commit 115e98c0): no blocking findings in this review. Code-review approval, with the current revision's full server gate still required before merge.\n\nThe revision-1 condition-freezing regression is fixed. The moving while condition and its consumed-claim clear are now inside the loop, ahead of body dispatch, so they run on initial entry, fallthrough, and continue. The rewrite lowers the original body once and reuses the existing condition-inside-loop construction rather than applying a once-only prelude.\n\nThe original review reproducer independently compiles and prints exactly `drop 0`, `drop 1`, `drop 2`, `2`, exiting 0. Revision 1 returned 99 from the safety guard. The new regression fixture covers reinitialization with fallthrough and continue, the while-let counterpart, and an initially-false condition, with exact destruction sequences compared to synchronous twins. The existing early-exit/reinitialization and array-reference cases remain green.\n\nValidation: isolated checkout at the exact submitted base with the submitted diff applied cleanly. All 16 focused tests pass, covering the new while fixture, head cleanup, array-reference fixes, leaf-expression refusals, recursion diagnostics, and selected existing while-condition/suspension cases. The original review reproducer was separately compiled and executed. Forget-citation gate and git diff --check pass. No production edits made during review.\n\nServer qualification: at the final check, revision 2 had tests=pending, applied_commit=115e98c0, and tested_commit=83539ef7. The displayed 2458-pass/36-freestanding output still belongs to revision 1. It must not be treated as validation of revision 2. I did not repeat the full server battery locally.\n\nArtifacts: /Users/swoodtke/Projects/sl226-review-r2; independent repro .build/scratch/while_reinit.saw; focused log .build/scratch/sl226-r2-focused.log.\n","created":"1789134671","id":"c9"},{"author":"agent:codex-reviewer","body":"Approved patch SL-226.p1 (revision 2)\n\nReview of SL-226.p1 revision 2 (base 2c9b8dfe; applied commit 115e98c0): no blocking findings in this review. Code-review approval, with the current revision's full server gate still required before merge.\n\nThe revision-1 condition-freezing regression is fixed. The moving while condition and its consumed-claim clear are now inside the loop, ahead of body dispatch, so they run on initial entry, fallthrough, and continue. The rewrite lowers the original body once and reuses the existing condition-inside-loop construction rather than applying a once-only prelude.\n\nThe original review reproducer independently compiles and prints exactly `drop 0`, `drop 1`, `drop 2`, `2`, exiting 0. Revision 1 returned 99 from the safety guard. The new regression fixture covers reinitialization with fallthrough and continue, the while-let counterpart, and an initially-false condition, with exact destruction sequences compared to synchronous twins. The existing early-exit/reinitialization and array-reference cases remain green.\n\nValidation: isolated checkout at the exact submitted base with the submitted diff applied cleanly. All 16 focused tests pass, covering the new while fixture, head cleanup, array-reference fixes, leaf-expression refusals, recursion diagnostics, and selected existing while-condition/suspension cases. The original review reproducer was separately compiled and executed. Forget-citation gate and git diff --check pass. No production edits made during review.\n\nServer qualification: at the final check, revision 2 had tests=pending, applied_commit=115e98c0, and tested_commit=83539ef7. The displayed 2458-pass/36-freestanding output still belongs to revision 1. It must not be treated as validation of revision 2. I did not repeat the full server battery locally.\n\nArtifacts: /Users/swoodtke/Projects/sl226-review-r2; independent repro .build/scratch/while_reinit.saw; focused log .build/scratch/sl226-r2-focused.log.\n","created":"1789134679","id":"c10"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-226.p1 (revision 2)","created":"1789137478","id":"c11"},{"author":"agent:claude-sawlang","body":"Patch SL-226.p1 needs rebase; the base branch moved since it applied. Submit a revised diff.\n\nMerged on the user's standing instruction (Sep 11: merge and close when tests land) — approved by the user, r2 server gate passed.","created":"1789137548","id":"c12"},{"author":"agent:claude-sawlang","body":"Revised patch SL-226.p1 to revision 3","created":"1789137640","id":"c13"},{"author":"agent:claude-sawlang","body":"SL-226.p1 r3 SUBMITTED — REBASE-ONLY, diff byte-identical to the r2 both the user and codex-reviewer approved (cmp-verified). The first merge attempt returned needs-rebase because SL-256.p1 (prototypes/minivm, fully disjoint) merged under r2's base; branch rebased clean onto 38ce5910, no content change. On the r3 gate passing, the lead approves as the user's delegate (standing instruction: merge and close when tests land) and merges.","created":"1789137651","id":"c14"}],"created":"1789042062","description":"A `&var arr[i]` argument, where `arr` is a `[T; N]` LOCAL of a body that suspends, mutates a copy: the callee's write is discarded and the element reads back unchanged. Silent -- exit 0, no diagnostic. The SYNC twin of the same code is correct, and a frame-resident STRUCT FIELD (`&var h.doc`) is correct too, so the defect is specific to the fixed-array element read in the coroutine path.\n\nRepro (single file, no modules), compiled with main 88fa56ba / d0e38f90:\n\n  import std.task.{yield_now}\n  struct Doc { n: Int }\n  func slow() -> Int { yield_now()  7 }\n  func set_one(d: &var Doc, k: Int) { d.n = d.n + k }\n  func driven() -> Int {\n      var arr: [Doc; 3] = [Doc(n: 0); 3]\n      let k = slow()\n      set_one(&var arr[1], k)\n      arr[1].n\n  }\n  func sync_control() -> Int {\n      var arr: [Doc; 3] = [Doc(n: 0); 3]\n      set_one(&var arr[1], 7)\n      arr[1].n\n  }\n  func main() { print(__saw_drive(driven()))  print(sync_control()) }\n\nObserved: 0 then 7. Wanted: 7 then 7.\n\nNote the suspending call is HOISTED into its own `let` here, so no ANF lift is involved -- the shape is plain 'frame-resident fixed array, borrow one element, call through it'.\n\nMECHANISM (hypothesis, not yet confirmed against the IR): `arr` is frame-resident and lands on design 218's legacy 'fixed-array' deferred family rather than a `Slot<T>`, and `_address_taken` records it. The suspicion is that the frame READ for that family produces a VALUE (a copy of the array) where `&var arr[1]` needs the field's storage, so the address handed to the callee points into the copy. A struct field's read does not go that way, which is why `&var h.doc` is correct in the same body.\n\nFound while sweeping SL-223's sibling shapes (obligation 4). It is INDEPENDENT of SL-223: it reproduces on unmodified main with no ANF lift in the picture, and SL-223's fix neither causes nor cures it. SL-223's branch therefore pins only the ORDER of the impure-index cell, not its value, and cites this issue.","id":"SL-226","labels":["bug","coroutine-transform"],"priority":"high","project":"SL","revision":17,"sequence":576,"stage":"queued","status":"open","title":"Driven body: a &var into a frame-resident fixed-array element is a silently lost write","updated":"1789137651"},"before":{"assignee":"agent:claude-sl226","author":"agent:claude-sl222","comments":[{"author":"agent:claude-sl226","body":"Claiming SL-226 (coroutine-transform ownership batch #2, issue 1 of 3: SL-226 -> SL-234 -> SL-227). Working in isolated worktree agent-afc12e0720f35b82f. First step per the issue text: VERIFY the frame-read-produces-a-value hypothesis against the emitted IR before writing any fix, then sweep the mechanism's siblings (obligation 4).","created":"1789090649","id":"c1"},{"author":"agent:claude-sl226","body":"FIX IN on branch agent-sl226-batch2. The issue's hypothesis was RIGHT about the symptom (\"the frame read produces a VALUE where the borrow needs the field's STORAGE\") and wrong about the location: nothing in coro_transform.py is at fault, and the defect is not specific to the fixed-array deferred family. It is in CODEGEN, and it reaches plain synchronous code too.\n\nMECHANISM (confirmed against -O0 IR before any code was written). Codegen carries TWO base-to-pointer dispatches. `_get_lvalue_pointer` (calls.py) is the WRITE path's funnel and answers every base form, ForceUnwrap included. The REFERENCE path (`_generate_reference_expr`, operators.py) kept a private `_get_array_element_pointer` knowing only Identifier / `self` / MemberAccess / nested-ArrayIndex, ending in a fallback whose own comment stated the defect: \"Fallback: materialize the container (won't propagate mutations)\". The transform rewrites a frame-resident local `arr` into `self.arr!` -- a ForceUnwrap base -- so every `&var arr[i]` in a suspending body fell into that fallback. The emitted -O0 IR is the whole bug in four instructions:\n\n  %unwrapped = extractvalue {i1, [3 x Doc]} %arr, 1\n  store [3 x Doc] %unwrapped, [3 x Doc]* %arr_tmp\n  %elem_ptr = getelementptr [3 x Doc], [3 x Doc]* %arr_tmp, i64 0, i64 1\n  call void @set_one(Doc* %elem_ptr, i64 %k)\n\nThe struct FIELD control was correct for the reason the matrix predicts, not a special case: a MemberAccess base is one of the four arms the private dispatch did have.\n\nSWEEP (obligation 4; sync twin is the spec, every cell measured, driven vs sync):\n  struct element, &var, direct        sync 8 / driven 1  BROKEN\n  struct element, &var, forwarded     sync 8 / driven 1  BROKEN\n  scalar element, &var                sync 8 / driven 1  BROKEN\n  nested array element, &var          sync 8 / driven 1  BROKEN\n  struct element, &var self receiver  sync 8 / driven 8  ok (goes through the WRITE funnel)\n  struct element, & shared read       sync 5 / driven 5  ok (a copy reads the same bytes)\n  struct FIELD control                sync 8 / driven 8  ok\n\nThe mechanism also reaches code with NO SUSPENSION, which is filed as SL-261 and fixed by the same change: `&var o![1]` (optional base) and `&var t.0[1]` (tuple base) are silent lost writes in ordinary sync code, and `&var a[1]` where `a: &var [T; N]` was an INTERNAL COMPILER ERROR (`Type of #1 arg mismatch: %\"Doc\"* != [3 x %\"Doc\"]*`) -- the reference-typed binding's alloca already holds a pointer, so the element GEP strode by whole arrays.\n\nFIX, at the mechanism's altitude and one line of behavior: `_generate_reference_expr`'s ArrayIndex arm now calls `_get_element_pointer`, which composes through `_get_lvalue_pointer`; the private `_get_array_element_pointer` is DELETED (it had exactly two callers, both in the reference path). One funnel now answers both families, so a borrow and a write of one place cannot disagree about where that place is. Per obligation 1 the funnel's docstring now NAMES its entry points.\n\nTESTS: examples/coro_ref_into_frame_resident_fixed_array.saw XFAIL REMOVED and extended from 3 cells to the 7-row matrix above, each printing its sync twin beside its driven result so a divergence is what fails. New examples/ref_into_array_element_addresses_real_storage.saw pins SL-261's seven non-coroutine cells, including the two bases that already worked so the fix is pinned as a widening rather than a swap. Gates to follow on the branch.\n","created":"1789096362","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-226.p1 (revision 1): coro/codegen ownership batch #2: SL-226 (array-element ptr funnel + SL-261 sync faces) / SL-234 (head-move relinquish + leaf refusal) / SL-227 (spawn roots through recursion check)","created":"1789105233","id":"c3"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-226.p1 (revision 1)","created":"1789105652","id":"c4"},{"author":"agent:codex-reviewer","body":"Review of SL-226.p1 revision 1 (base 08a64522; applied/tested commit 83539ef7): requesting changes for a semantic regression in the included SL-234 while-loop lowering. The array-element address fixes and the submitted focused tests pass.\n\n[P1] Reevaluate a moving while condition after the body reinitializes its operand.\n\nLocation: sawc/coro_transform.py:8131-8134, the WhileExpr call to `_hoist_head_and_relinquish` and emission of its prelude before the loop.\n\nThe new lowering evaluates the condition once outside the loop and then loops on the saved Bool. Its justification assumes that a condition which moves a frame local cannot validly execute twice. A body can reinitialize that local before every backedge, making repeated evaluation both valid and necessary. This is distinct from repeatedly moving the same uninitialized resource, so the separately reported move-checker hole does not cover it.\n\nExact-patch reproducer:\n\n```saw\nimport std.task.{yield_now}\nstruct Res { n: Int }\nextension Res: NoCopy {\n    func deinit(&var self) { print(\"drop {}\", self.n) }\n}\nfunc note(r: &Res) -> Int { r.n }\nfunc consume(r: Res) -> Bool { r.n < 2 }\nfunc run(items: Vector<Int>) -> Int {\n    yield_now()\n    for item in items.iter() {\n        var r = Res(n: 0)\n        if note(&r) + item < 0 { return -1 }\n        var n = 0\n        while consume(move r) {\n            n += 1\n            r = Res(n: n)\n            if n > 3 { return 99 }\n        }\n        return n\n    }\n    0\n}\nfunc main() { print(__saw_drive(run([1]))) }\n```\n\nExpected output, confirmed by the synchronous control on the patch: `drop 0`, `drop 1`, `drop 2`, `2`. Each condition consumes a distinct initialized resource; after consuming Res(2), false ends the loop. The patch instead prints drops 0 through 4 followed by `99`: the first true result is reused forever, and the explicit guard merely prevents the test hanging. Without that guard this terminating source program would loop forever.\n\nThe exact unpatched base compiles the same driven source and returns `2`, although it has the earlier duplicate-drop defect (`drop 0`, `drop 0`, `drop 1`, `drop 1`, `drop 2`, `2`). Thus the condition-freezing behavior is newly introduced, while the pre-existing cleanup problem still needs the fix's correct per-evaluation ordering.\n\nPlease keep condition evaluation and the consumed-claim clear inside the loop's condition path, before dispatch to the body, including every backedge/continue. Do not apply a once-only prelude to a repeated condition. Add a reinitializing fallthrough case and a reinitializing continue case; the current while-reinit fixture exits immediately and cannot detect a frozen condition.\n\nValidation: isolated checkout at the exact submitted base with the submitted diff applied cleanly; all 13 focused tests pass (array-reference fixes, previous/new head cleanup fixtures, leaf-expression refusals, and all suspending-recursion fixtures selected). Forget-citation gate and git diff --check pass. Reproducer, patched synchronous control, and exact-base driven control independently compiled and executed. Server evidence for 83539ef7 reports 2458 compiler passes, 12 expected failures, and 36 freestanding passes; the full server battery was not repeated locally. No further blocker established in the array-pointer or recursion changes during this review.\n\nArtifacts: /Users/swoodtke/Projects/sl226-review-r1/.build/scratch/while_reinit.saw and while_reinit_sync.saw; exact-base control in /Users/swoodtke/Projects/sl226-review-r1-base/.build/scratch/while_reinit.saw.\n","created":"1789128495","id":"c5"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-226.p1 (revision 1)\n\nReview of SL-226.p1 revision 1 (base 08a64522; applied/tested commit 83539ef7): requesting changes for a semantic regression in the included SL-234 while-loop lowering. The array-element address fixes and the submitted focused tests pass.\n\n[P1] Reevaluate a moving while condition after the body reinitializes its operand.\n\nLocation: sawc/coro_transform.py:8131-8134, the WhileExpr call to `_hoist_head_and_relinquish` and emission of its prelude before the loop.\n\nThe new lowering evaluates the condition once outside the loop and then loops on the saved Bool. Its justification assumes that a condition which moves a frame local cannot validly execute twice. A body can reinitialize that local before every backedge, making repeated evaluation both valid and necessary. This is distinct from repeatedly moving the same uninitialized resource, so the separately reported move-checker hole does not cover it.\n\nExact-patch reproducer:\n\n```saw\nimport std.task.{yield_now}\nstruct Res { n: Int }\nextension Res: NoCopy {\n    func deinit(&var self) { print(\"drop {}\", self.n) }\n}\nfunc note(r: &Res) -> Int { r.n }\nfunc consume(r: Res) -> Bool { r.n < 2 }\nfunc run(items: Vector<Int>) -> Int {\n    yield_now()\n    for item in items.iter() {\n        var r = Res(n: 0)\n        if note(&r) + item < 0 { return -1 }\n        var n = 0\n        while consume(move r) {\n            n += 1\n            r = Res(n: n)\n            if n > 3 { return 99 }\n        }\n        return n\n    }\n    0\n}\nfunc main() { print(__saw_drive(run([1]))) }\n```\n\nExpected output, confirmed by the synchronous control on the patch: `drop 0`, `drop 1`, `drop 2`, `2`. Each condition consumes a distinct initialized resource; after consuming Res(2), false ends the loop. The patch instead prints drops 0 through 4 followed by `99`: the first true result is reused forever, and the explicit guard merely prevents the test hanging. Without that guard this terminating source program would loop forever.\n\nThe exact unpatched base compiles the same driven source and returns `2`, although it has the earlier duplicate-drop defect (`drop 0`, `drop 0`, `drop 1`, `drop 1`, `drop 2`, `2`). Thus the condition-freezing behavior is newly introduced, while the pre-existing cleanup problem still needs the fix's correct per-evaluation ordering.\n\nPlease keep condition evaluation and the consumed-claim clear inside the loop's condition path, before dispatch to the body, including every backedge/continue. Do not apply a once-only prelude to a repeated condition. Add a reinitializing fallthrough case and a reinitializing continue case; the current while-reinit fixture exits immediately and cannot detect a frozen condition.\n\nValidation: isolated checkout at the exact submitted base with the submitted diff applied cleanly; all 13 focused tests pass (array-reference fixes, previous/new head cleanup fixtures, leaf-expression refusals, and all suspending-recursion fixtures selected). Forget-citation gate and git diff --check pass. Reproducer, patched synchronous control, and exact-base driven control independently compiled and executed. Server evidence for 83539ef7 reports 2458 compiler passes, 12 expected failures, and 36 freestanding passes; the full server battery was not repeated locally. No further blocker established in the array-pointer or recursion changes during this review.\n\nArtifacts: /Users/swoodtke/Projects/sl226-review-r1/.build/scratch/while_reinit.saw and while_reinit_sync.saw; exact-base control in /Users/swoodtke/Projects/sl226-review-r1-base/.build/scratch/while_reinit.saw.\n","created":"1789128503","id":"c6"},{"author":"agent:claude-sawlang","body":"Revised patch SL-226.p1 to revision 2","created":"1789133489","id":"c7"},{"author":"agent:claude-sawlang","body":"SL-226.p1 REVISION 2 SUBMITTED (fix round for review r1's P1, branch tip 768715e3 rebased on 314d0865). The while arm no longer emits the hoist as a once-only prelude: it routes through _head_into_while_body, a named funnel factored from the rewrite the tree already had for a SUSPENDING while head (design 224/DF-245d) — condition evaluation and the consumed-claim clear now sit INSIDE the loop ahead of the body dispatch, reached on the first iteration, fall-through backedges, continue, and the condition-false exit. w.condition becomes None (the conditionless form _split_while/diverges/codegen already handle). Once-only stays for guard-let (statement subject) and for (iterator seeded once) with the argument stated in _hoist_head_and_relinquish's docstring and the fixture comments; while-let needs nothing (its subject hoist lands inside the loop body by construction) and is MEASURED, not assumed. New fixture coro_while_head_move_reevaluates_per_iteration.saw: 4 cells x sync twin (reinit+fallthrough, reinit+continue, moving while-let subject, first-evaluation-false), numbered-drop oracles; it HANGS on the pre-fix compiler (10.8M drops in 12s) and the nesting-in-a-for shape is load-bearing (design 127's backedge charge makes a bare driven while suspension-spanning, where _split_while's separate standing refusal holds — verified byte-identical on base). Lead independently compiled+ran the reviewer's repro on the branch: drop 0 / drop 1 / drop 2 / 2, exit 0, matching the sync twin. Gates: suite 2465/12x, freestanding 36/36, terminal battery 25/25 (3139s). Exit-half sibling fixture: header comment corrected only (its old WHY-while-IS-SOUND paragraph asserted the disproved claim); its 12 cells unchanged. SL-234 updated (c5).","created":"1789133522","id":"c8"},{"author":"agent:codex-reviewer","body":"Review of SL-226.p1 revision 2 (base 2c9b8dfe; applied commit 115e98c0): no blocking findings in this review. Code-review approval, with the current revision's full server gate still required before merge.\n\nThe revision-1 condition-freezing regression is fixed. The moving while condition and its consumed-claim clear are now inside the loop, ahead of body dispatch, so they run on initial entry, fallthrough, and continue. The rewrite lowers the original body once and reuses the existing condition-inside-loop construction rather than applying a once-only prelude.\n\nThe original review reproducer independently compiles and prints exactly `drop 0`, `drop 1`, `drop 2`, `2`, exiting 0. Revision 1 returned 99 from the safety guard. The new regression fixture covers reinitialization with fallthrough and continue, the while-let counterpart, and an initially-false condition, with exact destruction sequences compared to synchronous twins. The existing early-exit/reinitialization and array-reference cases remain green.\n\nValidation: isolated checkout at the exact submitted base with the submitted diff applied cleanly. All 16 focused tests pass, covering the new while fixture, head cleanup, array-reference fixes, leaf-expression refusals, recursion diagnostics, and selected existing while-condition/suspension cases. The original review reproducer was separately compiled and executed. Forget-citation gate and git diff --check pass. No production edits made during review.\n\nServer qualification: at the final check, revision 2 had tests=pending, applied_commit=115e98c0, and tested_commit=83539ef7. The displayed 2458-pass/36-freestanding output still belongs to revision 1. It must not be treated as validation of revision 2. I did not repeat the full server battery locally.\n\nArtifacts: /Users/swoodtke/Projects/sl226-review-r2; independent repro .build/scratch/while_reinit.saw; focused log .build/scratch/sl226-r2-focused.log.\n","created":"1789134671","id":"c9"},{"author":"agent:codex-reviewer","body":"Approved patch SL-226.p1 (revision 2)\n\nReview of SL-226.p1 revision 2 (base 2c9b8dfe; applied commit 115e98c0): no blocking findings in this review. Code-review approval, with the current revision's full server gate still required before merge.\n\nThe revision-1 condition-freezing regression is fixed. The moving while condition and its consumed-claim clear are now inside the loop, ahead of body dispatch, so they run on initial entry, fallthrough, and continue. The rewrite lowers the original body once and reuses the existing condition-inside-loop construction rather than applying a once-only prelude.\n\nThe original review reproducer independently compiles and prints exactly `drop 0`, `drop 1`, `drop 2`, `2`, exiting 0. Revision 1 returned 99 from the safety guard. The new regression fixture covers reinitialization with fallthrough and continue, the while-let counterpart, and an initially-false condition, with exact destruction sequences compared to synchronous twins. The existing early-exit/reinitialization and array-reference cases remain green.\n\nValidation: isolated checkout at the exact submitted base with the submitted diff applied cleanly. All 16 focused tests pass, covering the new while fixture, head cleanup, array-reference fixes, leaf-expression refusals, recursion diagnostics, and selected existing while-condition/suspension cases. The original review reproducer was separately compiled and executed. Forget-citation gate and git diff --check pass. No production edits made during review.\n\nServer qualification: at the final check, revision 2 had tests=pending, applied_commit=115e98c0, and tested_commit=83539ef7. The displayed 2458-pass/36-freestanding output still belongs to revision 1. It must not be treated as validation of revision 2. I did not repeat the full server battery locally.\n\nArtifacts: /Users/swoodtke/Projects/sl226-review-r2; independent repro .build/scratch/while_reinit.saw; focused log .build/scratch/sl226-r2-focused.log.\n","created":"1789134679","id":"c10"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-226.p1 (revision 2)","created":"1789137478","id":"c11"},{"author":"agent:claude-sawlang","body":"Patch SL-226.p1 needs rebase; the base branch moved since it applied. Submit a revised diff.\n\nMerged on the user's standing instruction (Sep 11: merge and close when tests land) — approved by the user, r2 server gate passed.","created":"1789137548","id":"c12"},{"author":"agent:claude-sawlang","body":"Revised patch SL-226.p1 to revision 3","created":"1789137640","id":"c13"}],"created":"1789042062","description":"A `&var arr[i]` argument, where `arr` is a `[T; N]` LOCAL of a body that suspends, mutates a copy: the callee's write is discarded and the element reads back unchanged. Silent -- exit 0, no diagnostic. The SYNC twin of the same code is correct, and a frame-resident STRUCT FIELD (`&var h.doc`) is correct too, so the defect is specific to the fixed-array element read in the coroutine path.\n\nRepro (single file, no modules), compiled with main 88fa56ba / d0e38f90:\n\n  import std.task.{yield_now}\n  struct Doc { n: Int }\n  func slow() -> Int { yield_now()  7 }\n  func set_one(d: &var Doc, k: Int) { d.n = d.n + k }\n  func driven() -> Int {\n      var arr: [Doc; 3] = [Doc(n: 0); 3]\n      let k = slow()\n      set_one(&var arr[1], k)\n      arr[1].n\n  }\n  func sync_control() -> Int {\n      var arr: [Doc; 3] = [Doc(n: 0); 3]\n      set_one(&var arr[1], 7)\n      arr[1].n\n  }\n  func main() { print(__saw_drive(driven()))  print(sync_control()) }\n\nObserved: 0 then 7. Wanted: 7 then 7.\n\nNote the suspending call is HOISTED into its own `let` here, so no ANF lift is involved -- the shape is plain 'frame-resident fixed array, borrow one element, call through it'.\n\nMECHANISM (hypothesis, not yet confirmed against the IR): `arr` is frame-resident and lands on design 218's legacy 'fixed-array' deferred family rather than a `Slot<T>`, and `_address_taken` records it. The suspicion is that the frame READ for that family produces a VALUE (a copy of the array) where `&var arr[1]` needs the field's storage, so the address handed to the callee points into the copy. A struct field's read does not go that way, which is why `&var h.doc` is correct in the same body.\n\nFound while sweeping SL-223's sibling shapes (obligation 4). It is INDEPENDENT of SL-223: it reproduces on unmodified main with no ANF lift in the picture, and SL-223's fix neither causes nor cures it. SL-223's branch therefore pins only the ORDER of the impure-index cell, not its value, and cites this issue.","id":"SL-226","labels":["bug","coroutine-transform"],"priority":"high","project":"SL","revision":16,"sequence":575,"stage":"queued","status":"open","title":"Driven body: a &var into a frame-resident fixed-array element is a silently lost write","updated":"1789137640"},"id":"SL-226","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sawlang\",\"body\":\"SL-226.p1 r3 SUBMITTED — REBASE-ONLY, diff byte-identical to the r2 both the user and codex-reviewer approved (cmp-verified). The first merge attempt returned needs-rebase because SL-256.p1 (prototypes/minivm, fully disjoint) merged under r2's base; branch rebased clean onto 38ce5910, no content change. On the r3 gate passing, the lead approves as the user's delegate (standing instruction: merge and close when tests land) and merges.\",\"id\":\"SL-226\",\"request_id\":\"pid-82914-1789137651-8000\"}","request_id":"pid-82914-1789137651-8000","sequence":576,"timestamp":"1789137651"}
---
