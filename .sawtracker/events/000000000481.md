---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-250.p1 (revision 1): minivm M7: record references + instance methods (branch commit 910e8fa8)","created":"1789097305","id":"c1"}],"created":"1789086346","description":"SL-242 epic child, landing patch 8/14. Whole-record refs, nested field forwarding, extension instance methods, labeled args; tested with a Lexer.advance-shaped record. Contract: prototypes/minivm/M7_RECORD_RECEIVERS.md. Patch = branch commit 910e8fa8.","id":"SL-250","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":481,"stage":"queued","status":"open","title":"minivm M7: record references + instance methods (&self / &var self)","updated":"1789097305"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086346","description":"SL-242 epic child, landing patch 8/14. Whole-record refs, nested field forwarding, extension instance methods, labeled args; tested with a Lexer.advance-shaped record. Contract: prototypes/minivm/M7_RECORD_RECEIVERS.md. Patch = branch commit 910e8fa8.","id":"SL-250","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":412,"stage":"queued","status":"open","title":"minivm M7: record references + instance methods (&self / &var self)","updated":"1789086346"},"id":"SL-250","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 7eaa160e..2057cc21 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -11,10 +11,13 @@ enums and exhaustive statement match, including the actual TokenKind declaration\\n in isolation. M5 adds implicit tails, value if/match and else-if chains, including\\n the unchanged numeric `hex_value` helper (193 total cases).\\n M6 supplies direct-call scalar references, forwarding and mutation, including\\n-borrowing scalar fields of direct records. Whole-record references/receivers,\\n-strings, collections, generics and methods remain outstanding. The next bounded\\n-slice is whole-record references and basic instance methods, tested with a\\n-scalar-only `Lexer.advance`-shaped record before introducing owning strings.\\n+borrowing scalar fields of direct records. M7 adds whole-record references,\\n+nested field forwarding and basic shared/mutable methods, tested with a\\n+scalar-only `Lexer.advance`-shaped record (250 total cases). Strings, collections,\\n+Optional/Result payloads, intrinsic generic types and module integration remain\\n+outstanding. Next is a bounded owned-String slice: literals, byte length/access,\\n+equality and substring, with explicit copy/drop rules before owning records and\\n+collections are enabled.\\n The dependency groups below are an inventory, not the implementation commit order;\\n the numbered milestone designs define each isolated slice.\\n \\ndiff --git a/prototypes/minivm/M7_RECORD_RECEIVERS.md b/prototypes/minivm/M7_RECORD_RECEIVERS.md\\nnew file mode 100644\\nindex 00000000..e450219e\\n--- /dev/null\\n+++ b/prototypes/minivm/M7_RECORD_RECEIVERS.md\\n@@ -0,0 +1,125 @@\\n+# M7: record references and basic instance methods\\n+\\n+Status: implemented after M6 (94110cfd).\\n+\\n+## Source scope\\n+\\n+Allow `&Record` and `&var Record` parameters for the existing nonempty,\\n+scalar-only/nested value records. Reading a reference produces a by-value\\n+snapshot; replacing it or assigning a field updates the caller. References to\\n+scalar/nested fields can be forwarded through a record reference. The existing\\n+ban on stored/returned/nested references remains. Whole-root borrow overlap and\\n+assignment-RHS exclusivity from M6 remain intentionally conservative.\\n+\\n+Allow `extension Record { func name(&self, arg: Int) -> Int { ... } }`\\n+and `&var self`. Methods require an explicit first receiver, are unique by\\n+record/name (no overloads), and lower to ordinary functions with the receiver\\n+as first Ref parameter. Different records may have same-named methods; free\\n+functions and methods have separate lookup. Multiple extension blocks may add\\n+distinct methods. Reject unknown/non-record extension targets, traits, static\\n+methods, generic extensions, by-value receivers and duplicate methods.\\n+\\n+Call a method on a named local/reference parameter or nested record field:\\n+`cursor.advance(byte: b)`, `self.reset()`, `outer.cursor.position()`.\\n+The receiver is borrowed before explicit arguments, with mutability dictated by\\n+the method signature. An immutable receiver cannot call a mutable method.\\n+Do not snapshot the receiver before calling a mutating method. Method calls on\\n+temporaries/general value expressions remain a located subset rejection.\\n+Explicit arguments retain positional order; optional labels must match the\\n+corresponding formal parameter name (no reordering or duplicate labels).\\n+This optional-label rule also applies to existing free-function calls.\\n+\\n+## Shared IR contract\\n+\\n+ReferenceIR targets may now be scalar data or Record; value_layout of a Ref\\n+remains one reference word. Record fields cannot themselves contain Ref.\\n+Existing record layout validation checks cycles and empty/invalid records.\\n+\\n+- Addr: dst Ref points at `a`, the start of a flattened scalar/record slot range\\n+  exactly matching the target layout. No source pointer arithmetic.\\n+- LoadRef: snapshot the entire target layout into dst's contiguous data range.\\n+- StoreRef: copy b's entire target layout through mutable Ref a. Snapshot source\\n+  words before stores, so overlap cannot change copy behavior.\\n+- New RefField: a is Ref to Record, immediate is a direct field index, dst is Ref\\n+  to that field's exact declared type. Destination mutability may weaken but may\\n+  not strengthen the parent. Compute offset with record_field_offset. Nested\\n+  fields use repeated RefField instructions. Field indices and both Ref types\\n+  must be validated before metadata access; no arbitrary integer offset opcode.\\n+\\n+Primary owns model.saw and verify.saw updates. Keep helpers' signatures stable;\\n+validate_references accepts Record targets and check_declared_types validates\\n+their indices before record layout traversal. Addr/LoadRef/StoreRef use\\n+range_matches against value_layout. Methods need no new backend ABI: they are\\n+ordinary functions with a Ref parameter followed by value/reference arguments.\\n+\\n+## Engine representation\\n+\\n+VM retains shared absolute-index cells. Bounds-check the whole referent extent\\n+using subtraction after validating the start, avoiding overflow. RefField checks\\n+the parent range before adding the validated field offset. Preserve snapshots\\n+for overlapping bulk stores and loads. All invalid handles report the existing\\n+`runtime error: invalid reference`.\\n+\\n+LLVM must make data words contiguous: allocate a single `[N x i64]` data array\\n+per function, N = number of logical slots (including unused holes at Ref slots).\\n+Define each data `%sN` as a constant-index GEP into it; Ref slots remain separate\\n+native `ptr` allocas. This preserves every existing slot name and mixed ptr/i64\\n+call ABI while making a record's consecutive data words addressable from its\\n+first word. Addr stores that native pointer, RefField uses typed i64 GEP by the\\n+computed field-word offset, and bulk accesses load/store i64 at consecutive\\n+offsets. Load all source words before any stores for snapshot semantics. Keep\\n+all allocas/GEP definitions in entry, no ptrtoint/inttoptr conversions.\\n+\\n+## Frontend implementation guidance\\n+\\n+Signature carries method owner (record index or -1); free lookup excludes\\n+methods. Scan extensions and method signatures before bodies, so forward and\\n+recursive calls work. Compile receiver as named `self` Ref binding. Permit the\\n+lexer-token spelling of self explicitly if distinct from ordinary identifiers.\\n+\\n+Keep Place's root binding identity across projections. An indirect record field\\n+projection emits RefField with an interned field descriptor. Forward its actual\\n+pointer slot type, not the original root binding's whole-record Ref type.\\n+read_place allocates new_value(target), then LoadRef; store_place emits StoreRef.\\n+Resolve named place method calls before parse_atom snapshots any receiver.\\n+Use the same borrow registration and pending-write restrictions for implicit\\n+receivers as explicit address arguments. Preserve enclosing call borrow stacks.\\n+\\n+## Validation and ownership\\n+\\n+Primary authors design, reviews integration, adds direct IR contracts and runs\\n+all suites under the machine suite lock. Sol frontend owns frontend.saw; Sol\\n+engines owns vm.saw/llvm.saw. The separate patch reviewer owns no prototype files.\\n+No prototype tracker issue/patch; independently established Python bugs are filed.\\n+\\n+Tests: nested record read/replace, scalar/nested field mutation and forwarding,\\n+whole-record return snapshots, mixed reference/value ABI, ancestor references\\n+across recursive VM growth; shared/mutable methods, same name on distinct record\\n+types, forward calls, multiple extension blocks, named arguments, and a scalar\\n+Lexer.advance-shaped receiver updating position/line/column. Reject shared writes,\\n+immutable mutation, receiver/argument overlap, pending-write receiver borrowing,\\n+wrong labels/arity/types, invalid extensions/method declarations, and escaping\\n+references. Direct contracts pin RefField invalid index/type/mutability, malformed\\n+aggregate ranges and runtime bounds. Retain all previous cases except the M6\\n+whole-record-reference rejection, which is explicitly superseded by this slice.\\n+Run VM and clang O0/O2 expected-output gates and all representation contracts.\\n+\\n+## Completed validation\\n+\\n+- 250 integration cases pass: the previous 227, minus the superseded\\n+  whole-record-reference rejection, plus 24 receiver cases. Valid cases execute\\n+  through both the VM and clang at O0/O2; rejection cases require located errors.\\n+- All five direct representation contracts compile and pass. The new receiver\\n+  contract exercises nested projection, qualification, malformed field/range/type\\n+  combinations and whole-referent bounds checks, including Int.max handles.\\n+- Python sawc independently agrees with expected outputs for whole-record\\n+  replacement/snapshots, shared/mutable methods and the lexer-style receiver.\\n+- Review caught and fixed a whole-record load destination using a scalar slot\\n+  allocation instead of the flattened record layout. Final gates include that\\n+  exact whole-record snapshot/replacement case.\\n+- No new Python compiler defect was established by the prototype work. SL-222\\n+  patch review was separate and its ownership finding was posted there.\\n+\\n+Next: define owned String representation and copy/drop points, then isolate\\n+literals, byte length/access, equality and substring in VM/native tests. Keep\\n+builders, vectors and Optional/Result payload ownership for subsequent slices.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 35134183..480b4944 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -66,20 +66,25 @@ shift counts. See [M1_NUMBERS.md](M1_NUMBERS.md) for the precise numeric contrac\\n Newlines separate statements. Omit the result annotation for Void functions;\\n `main()` must take no arguments and return Void. Value-returning functions need\\n a value on every continuing path, supplied by an explicit return or final expression.\\n-There are no imports, strings, collections,\\n-generics, methods, or concurrency in the accepted subset.\\n+There are no imports, strings, collections, generics, or concurrency in the\\n+accepted subset.\\n Semicolons remain unsupported. Boolean `&&`/`||` short-circuit, mutable integers\\n and fields support `+=`/`-=`/`*=`/`/=`/`%=`, and module `static` integer/Bool\\n constants accept literal and numeric-limit initializers. See\\n [M3_SCALAR_CONTROL.md](M3_SCALAR_CONTROL.md) for this frontend slice.\\n The prototype still accepts broader lexical shadowing than Saw's design-100 rule.\\n \\n-Scalar `&T` / `&var T` function parameters support explicit call-site borrowing,\\n-forwarding, reads and caller-visible mutation. A scalar field of a direct record\\n-can be borrowed. Overlapping mutable arguments and borrowing an assignment's\\n+Scalar and record `&T` / `&var T` function parameters support explicit call-site\\n+borrowing, forwarding, snapshots and caller-visible mutation. Nested fields can\\n+be borrowed through record references. Overlapping mutable arguments and borrowing an assignment's\\n destination inside its RHS are rejected. Different fields of one record are\\n-conservatively treated as overlapping. Whole-record references and reference\\n-storage/results remain unsupported; see [M6_SCALAR_REFERENCES.md](M6_SCALAR_REFERENCES.md).\\n+conservatively treated as overlapping. Reference storage/results remain\\n+unsupported; see [M6_SCALAR_REFERENCES.md](M6_SCALAR_REFERENCES.md).\\n+\\n+Record extensions support instance methods with `&self` / `&var self`, called on\\n+named locals, reference parameters or nested fields. Optional call labels must\\n+match parameter names in order. Temporary receivers, overloads, static methods\\n+and trait extensions remain unsupported. See [M7_RECORD_RECEIVERS.md](M7_RECORD_RECEIVERS.md).\\n \\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n@@ -93,15 +98,16 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The first six milestones\\n-pass all 227 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The first seven milestones\\n+pass all 250 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw`,\\n-`tests/record_contract.saw`, `tests/enum_contract.saw`, and\\n-`tests/reference_contract.saw`; build and run them\\n+`tests/record_contract.saw`, `tests/enum_contract.saw`,\\n+`tests/reference_contract.saw`, and `tests/receiver_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n-`--section enums`, `--section values`, or `--section references` for an isolated\\n+`--section enums`, `--section values`, `--section references`, or\\n+`--section receivers` for an isolated\\n integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\ndiff --git a/prototypes/minivm/examples/receivers/lexer_advance.saw b/prototypes/minivm/examples/receivers/lexer_advance.saw\\nnew file mode 100644\\nindex 00000000..ee736b54\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/lexer_advance.saw\\n@@ -0,0 +1,26 @@\\n+struct Lexer { position: Int, line: Int, column: Int }\\n+\\n+extension Lexer {\\n+    func advance(&var self, byte: Byte) {\\n+        self.position += 1\\n+        if byte == Byte(10u8) {\\n+            self.line += 1\\n+            self.column = 1\\n+        } else {\\n+            self.column += 1\\n+        }\\n+    }\\n+}\\n+\\n+func main() {\\n+    var lexer = Lexer(position: 0, line: 1, column: 1)\\n+    lexer.advance(byte: Byte(65u8))\\n+    print(lexer.position)\\n+    print(lexer.column)\\n+    lexer.advance(byte: Byte(10u8))\\n+    print(lexer.line)\\n+    lexer.advance(byte: Byte(66u8))\\n+    print(lexer.position)\\n+    print(lexer.line)\\n+    print(lexer.column)\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/methods_basic.saw b/prototypes/minivm/examples/receivers/methods_basic.saw\\nnew file mode 100644\\nindex 00000000..68dc542a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/methods_basic.saw\\n@@ -0,0 +1,19 @@\\n+struct Counter { value: Int }\\n+\\n+extension Counter {\\n+    func read(&self) -> Int { self.value }\\n+    func twice(&var self, amount: Int) {\\n+        self.add(amount)\\n+        self.add(amount: amount)\\n+    }\\n+    func add(&var self, amount: Int) { self.value += amount }\\n+}\\n+\\n+func main() {\\n+    var counter = Counter(value: 5)\\n+    print(counter.read())\\n+    counter.twice(amount: 3)\\n+    print(counter.read())\\n+    counter.add(0)\\n+    print(counter.value)\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/methods_lookup_labels.saw b/prototypes/minivm/examples/receivers/methods_lookup_labels.saw\\nnew file mode 100644\\nindex 00000000..f17ff778\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/methods_lookup_labels.saw\\n@@ -0,0 +1,24 @@\\n+struct First { value: Int }\\n+struct Second { value: Int }\\n+\\n+extension First {\\n+    func adjust(&var self, amount: Int) { self.value += amount }\\n+}\\n+extension Second {\\n+    func adjust(&var self, amount: Int) { self.value -= amount }\\n+}\\n+extension First {\\n+    func scaled(&self, factor: Int) -> Int { self.value * factor }\\n+}\\n+\\n+func adjust(value: Int) -> Int { value + 1 }\\n+\\n+func main() {\\n+    var first = First(value: 10)\\n+    var second = Second(value: 25)\\n+    first.adjust(amount: 3)\\n+    second.adjust(3)\\n+    print(first.value)\\n+    print(second.value)\\n+    print(adjust(first.scaled(factor: 1) + 4))\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/mixed_abi.saw b/prototypes/minivm/examples/receivers/mixed_abi.saw\\nnew file mode 100644\\nindex 00000000..536fe06f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/mixed_abi.saw\\n@@ -0,0 +1,18 @@\\n+struct Pair { left: Int, right: Int }\\n+\\n+func value_ref(value: Pair, source: &Pair, delta: Int) -> Pair {\\n+    Pair(left: value.left + source.left, right: value.right + delta)\\n+}\\n+func ref_value(source: &Pair, value: Pair, delta: Int) -> Pair {\\n+    Pair(left: source.right + delta, right: value.right + source.left)\\n+}\\n+\\n+func main() {\\n+    let source = Pair(left: 4, right: 7)\\n+    let first = value_ref(value: Pair(left: 1, right: 2), source: &source, delta: 4)\\n+    print(first.left)\\n+    print(first.right)\\n+    let second = ref_value(source: &source, value: Pair(left: 8, right: 6), delta: 2)\\n+    print(second.left)\\n+    print(second.right)\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/nested_forwarding.saw b/prototypes/minivm/examples/receivers/nested_forwarding.saw\\nnew file mode 100644\\nindex 00000000..d4490acc\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/nested_forwarding.saw\\n@@ -0,0 +1,18 @@\\n+struct Inner { count: Int, flag: Bool }\\n+struct Outer { inner: Inner, other: Int }\\n+\\n+func read_inner(value: &Inner) -> Int { value.count }\\n+func bump(value: &var Int) { value += 5 }\\n+func forward(value: &var Outer) {\\n+    print(read_inner(&value.inner))\\n+    bump(&var value.inner.count)\\n+    value.inner.count += 1\\n+}\\n+\\n+func main() {\\n+    var value = Outer(inner: Inner(count: 7, flag: true), other: 20)\\n+    forward(&var value)\\n+    print(value.inner.count)\\n+    bump(&var value.inner.count)\\n+    print(value.inner.count)\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/nested_method_forward.saw b/prototypes/minivm/examples/receivers/nested_method_forward.saw\\nnew file mode 100644\\nindex 00000000..0c052962\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/nested_method_forward.saw\\n@@ -0,0 +1,20 @@\\n+struct Cursor { position: Int }\\n+struct State { cursor: Cursor, limit: Int }\\n+\\n+extension Cursor {\\n+    func position(&self) -> Int { self.position }\\n+    func advance(&var self, amount: Int) { self.position += amount }\\n+}\\n+extension State {\\n+    func step(&var self, amount: Int) {\\n+        self.cursor.advance(amount: amount)\\n+    }\\n+    func current(&self) -> Int { self.cursor.position() }\\n+}\\n+\\n+func main() {\\n+    var state = State(cursor: Cursor(position: 3), limit: 20)\\n+    print(state.current())\\n+    state.step(amount: 4)\\n+    print(state.cursor.position())\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/recursive_growth.saw b/prototypes/minivm/examples/receivers/recursive_growth.saw\\nnew file mode 100644\\nindex 00000000..f0016265\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/recursive_growth.saw\\n@@ -0,0 +1,18 @@\\n+struct Pair { left: Int, right: Int }\\n+\\n+func descend(depth: Int, value: &var Pair) {\\n+    var pad = Pair(left: depth, right: depth + 1)\\n+    if depth == 0 {\\n+        value.left += pad.left + 40\\n+        value.right += pad.right + 39\\n+        return\\n+    }\\n+    descend(depth - 1, &var value)\\n+}\\n+\\n+func main() {\\n+    var value = Pair(left: 1, right: 2)\\n+    descend(40, &var value)\\n+    print(value.left)\\n+    print(value.right)\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_duplicate_method.saw b/prototypes/minivm/examples/receivers/reject_duplicate_method.saw\\nnew file mode 100644\\nindex 00000000..e37be7ce\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_duplicate_method.saw\\n@@ -0,0 +1,8 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func read(&self) -> Int { self.value }\\n+}\\n+extension Counter {\\n+    func read(&self) -> Int { self.value + 1 }\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_enum_extension.saw b/prototypes/minivm/examples/receivers/reject_enum_extension.saw\\nnew file mode 100644\\nindex 00000000..2ffd8dbe\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_enum_extension.saw\\n@@ -0,0 +1,5 @@\\n+enum State { case Off, case On }\\n+extension State {\\n+    func read(&self) -> Int { 1 }\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_immutable_receiver.saw b/prototypes/minivm/examples/receivers/reject_immutable_receiver.saw\\nnew file mode 100644\\nindex 00000000..023bb354\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_immutable_receiver.saw\\n@@ -0,0 +1,8 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func bump(&var self) { self.value += 1 }\\n+}\\n+func main() {\\n+    let counter = Counter(value: 1)\\n+    counter.bump()\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_immutable_record_borrow.saw b/prototypes/minivm/examples/receivers/reject_immutable_record_borrow.saw\\nnew file mode 100644\\nindex 00000000..576b4117\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_immutable_record_borrow.saw\\n@@ -0,0 +1,6 @@\\n+struct Pair { left: Int, right: Int }\\n+func replace(value: &var Pair) { value.left = 3 }\\n+func main() {\\n+    let value = Pair(left: 1, right: 2)\\n+    replace(&var value)\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_method_arity.saw b/prototypes/minivm/examples/receivers/reject_method_arity.saw\\nnew file mode 100644\\nindex 00000000..3ce7123a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_method_arity.saw\\n@@ -0,0 +1,8 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func add(&var self, amount: Int) { self.value += amount }\\n+}\\n+func main() {\\n+    var counter = Counter(value: 1)\\n+    counter.add()\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_method_reference_result.saw b/prototypes/minivm/examples/receivers/reject_method_reference_result.saw\\nnew file mode 100644\\nindex 00000000..0121f2e1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_method_reference_result.saw\\n@@ -0,0 +1,5 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func bad(&self) -> &Counter { self }\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_method_type.saw b/prototypes/minivm/examples/receivers/reject_method_type.saw\\nnew file mode 100644\\nindex 00000000..a6940fb1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_method_type.saw\\n@@ -0,0 +1,8 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func add(&var self, amount: Int) { self.value += amount }\\n+}\\n+func main() {\\n+    var counter = Counter(value: 1)\\n+    counter.add(true)\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_pending_write_receiver.saw b/prototypes/minivm/examples/receivers/reject_pending_write_receiver.saw\\nnew file mode 100644\\nindex 00000000..a60422e0\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_pending_write_receiver.saw\\n@@ -0,0 +1,8 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func copy(&self) -> Counter { self }\\n+}\\n+func main() {\\n+    var counter = Counter(value: 1)\\n+    counter = counter.copy()\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_receiver_argument_overlap.saw b/prototypes/minivm/examples/receivers/reject_receiver_argument_overlap.saw\\nnew file mode 100644\\nindex 00000000..4f68ba46\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_receiver_argument_overlap.saw\\n@@ -0,0 +1,8 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func combine(&var self, other: &Counter) { self.value += other.value }\\n+}\\n+func main() {\\n+    var counter = Counter(value: 1)\\n+    counter.combine(other: &counter)\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_static_method.saw b/prototypes/minivm/examples/receivers/reject_static_method.saw\\nnew file mode 100644\\nindex 00000000..c12485df\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_static_method.saw\\n@@ -0,0 +1,5 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    static func zero() -> Counter { Counter(value: 0) }\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_temporary_receiver.saw b/prototypes/minivm/examples/receivers/reject_temporary_receiver.saw\\nnew file mode 100644\\nindex 00000000..cb811e60\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_temporary_receiver.saw\\n@@ -0,0 +1,5 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func read(&self) -> Int { self.value }\\n+}\\n+func main() { print(Counter(value: 1).read()) }\\ndiff --git a/prototypes/minivm/examples/receivers/reject_trait_extension.saw b/prototypes/minivm/examples/receivers/reject_trait_extension.saw\\nnew file mode 100644\\nindex 00000000..de5520b0\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_trait_extension.saw\\n@@ -0,0 +1,5 @@\\n+struct Counter { value: Int }\\n+extension Counter: Display {\\n+    func read(&self) -> Int { self.value }\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_unknown_extension.saw b/prototypes/minivm/examples/receivers/reject_unknown_extension.saw\\nnew file mode 100644\\nindex 00000000..78a68c30\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_unknown_extension.saw\\n@@ -0,0 +1,4 @@\\n+extension Missing {\\n+    func read(&self) -> Int { 1 }\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_value_receiver.saw b/prototypes/minivm/examples/receivers/reject_value_receiver.saw\\nnew file mode 100644\\nindex 00000000..4f71c617\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_value_receiver.saw\\n@@ -0,0 +1,5 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func read(self) -> Int { self.value }\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/references/reject_whole_record.saw b/prototypes/minivm/examples/receivers/reject_write_shared_record.saw\\nsimilarity index 55%\\nrename from prototypes/minivm/examples/references/reject_whole_record.saw\\nrename to prototypes/minivm/examples/receivers/reject_write_shared_record.saw\\nindex dfdd1ced..bb462aea 100644\\n--- a/prototypes/minivm/examples/references/reject_whole_record.saw\\n+++ b/prototypes/minivm/examples/receivers/reject_write_shared_record.saw\\n@@ -1,3 +1,3 @@\\n struct Pair { left: Int, right: Int }\\n-func bad(value: &Pair) {}\\n+func bad(value: &Pair) { value.left = 3 }\\n func main() {}\\ndiff --git a/prototypes/minivm/examples/receivers/reject_wrong_label.saw b/prototypes/minivm/examples/receivers/reject_wrong_label.saw\\nnew file mode 100644\\nindex 00000000..25addf84\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/reject_wrong_label.saw\\n@@ -0,0 +1,8 @@\\n+struct Counter { value: Int }\\n+extension Counter {\\n+    func add(&var self, amount: Int) { self.value += amount }\\n+}\\n+func main() {\\n+    var counter = Counter(value: 1)\\n+    counter.add(value: 2)\\n+}\\ndiff --git a/prototypes/minivm/examples/receivers/whole_record_reference.saw b/prototypes/minivm/examples/receivers/whole_record_reference.saw\\nnew file mode 100644\\nindex 00000000..44110b77\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/receivers/whole_record_reference.saw\\n@@ -0,0 +1,18 @@\\n+struct Inner { x: Int, y: Int }\\n+struct Outer { inner: Inner, tag: Int }\\n+\\n+func snapshot(value: &Outer) -> Outer { value }\\n+func replace(value: &var Outer, next: Outer) { value = next }\\n+\\n+func main() {\\n+    var value = Outer(inner: Inner(x: 1, y: 2), tag: 3)\\n+    let copy = snapshot(&value)\\n+    value.inner.x = 4\\n+    print(copy.inner.x)\\n+    print(copy.inner.y)\\n+    print(copy.tag)\\n+    print(value.inner.x)\\n+    replace(&var value, Outer(inner: Inner(x: 9, y: 8), tag: 7))\\n+    print(value.inner.x)\\n+    print(value.inner.y)\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 54f1bf8e..5047cce9 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -9,6 +9,7 @@ func clone_token(t: &Token) -> Token {\\n \\n struct Signature {\\n     name: String\\n+    owner: Int\\n     param_names: Vector<String>\\n     params: Vector<ValueType>\\n     result: ValueType\\n@@ -33,6 +34,7 @@ struct Place {\\n     mutable: Bool\\n     indirect: Bool\\n     root: Int\\n+    ref_ty: ValueType\\n }\\n \\n struct Borrow {\\n@@ -182,7 +184,9 @@ extension Compiler {\\n             }\\n             let target = try self.parse_type()\\n             if reference_type(target) { return problem(\\\"references to references are not supported\\\", amp.line, amp.col) }\\n-            if not scalar_type(target) { return problem(\\\"only scalar references are supported\\\", amp.line, amp.col) }\\n+            var supported = scalar_type(target)\\n+            match target { case Record(_) -> { supported = true }, case _ -> {} }\\n+            if not supported { return problem(\\\"only data references are supported\\\", amp.line, amp.col) }\\n             return self.intern_reference(target, mutable)\\n         }\\n         let t = try self.expect(TokenKind.Ident, \\\"expected type\\\")\\n@@ -284,8 +288,13 @@ extension Compiler {\\n                 while not self.at(TokenKind.Newline) && not self.at(TokenKind.Eof) { let _ = self.take() }\\n                 self.skip_newlines()\\n                 continue\\n+            } else if self.at(TokenKind.Extension) {\\n+                let _ = self.take()\\n+                let _ = try self.expect(TokenKind.Ident, \\\"expected extension target\\\")\\n+            } else if self.at(TokenKind.Trait) {\\n+                return self.fail(\\\"traits are not supported\\\")\\n             } else if not self.at(TokenKind.Func) {\\n-                return self.fail(\\\"only function, record, enum, and static declarations are supported at module scope\\\")\\n+                return self.fail(\\\"only function, record, enum, static, and extension declarations are supported at module scope\\\")\\n             }\\n             while not self.at(TokenKind.LBrace) && not self.at(TokenKind.Eof) { let _ = self.take() }\\n             if self.at(TokenKind.Eof) { return self.fail(\\\"expected declaration body\\\") }\\n@@ -305,12 +314,101 @@ extension Compiler {\\n     func signature_index(&self, name: String) -> Int {\\n         var i = 0\\n         while i < self.signatures.len() {\\n-            if self.signatures[i].name == name { return i }\\n+            if self.signatures[i].owner < 0 && self.signatures[i].name == name { return i }\\n+            i += 1\\n+        }\\n+        -1\\n+    }\\n+\\n+    func method_index(&self, owner: Int, name: String) -> Int {\\n+        var i = 0\\n+        while i < self.signatures.len() {\\n+            if self.signatures[i].owner == owner && self.signatures[i].name == name { return i }\\n             i += 1\\n         }\\n         -1\\n     }\\n \\n+    func scan_extension(&var self) -> Result<Void, ProtoError> {\\n+        let extension_token = self.take()\\n+        let target = try self.expect(TokenKind.Ident, \\\"expected extension target\\\")\\n+        let owner = self.record_index(target.value)\\n+        if owner < 0 { return problem(\\\"extension target must be a known record\\\", target.line, target.col) }\\n+        if self.at(TokenKind.Colon) { return self.fail(\\\"traits are not supported\\\") }\\n+        if not self.at(TokenKind.LBrace) {\\n+            return problem(\\\"generic extensions are not supported\\\", extension_token.line, extension_token.col)\\n+        }\\n+        let _ = self.take()\\n+        self.skip_newlines()\\n+        while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n+            if self.at(TokenKind.Public) { let _ = self.take() }\\n+            if self.at(TokenKind.Static) { return self.fail(\\\"static methods are not supported\\\") }\\n+            let ft = try self.expect(TokenKind.Func, \\\"extension bodies may contain only instance methods\\\")\\n+            let method = try self.expect(TokenKind.Ident, \\\"expected method name\\\")\\n+            if self.method_index(owner, method.value) >= 0 {\\n+                return problem(\\\"duplicate method `{method.value}`\\\", method.line, method.col)\\n+            }\\n+            let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after method name\\\")\\n+            self.skip_newlines()\\n+            let amp = try self.expect(TokenKind.Ampersand, \\\"methods require an explicit `&self` or `&var self` receiver\\\")\\n+            var receiver_mutable = false\\n+            if self.at(TokenKind.Var) {\\n+                let _ = self.take()\\n+                receiver_mutable = true\\n+            }\\n+            let receiver = try self.expect(TokenKind.SelfKw, \\\"methods require an explicit `&self` or `&var self` receiver\\\")\\n+            var pnames = Vector<String>()\\n+            var ptypes = Vector<ValueType>()\\n+            try! pnames.push(receiver.value)\\n+            try! ptypes.push(self.intern_reference(ValueType.Record(owner), receiver_mutable))\\n+            self.skip_newlines()\\n+            if self.at(TokenKind.Comma) {\\n+                let _ = self.take()\\n+                self.skip_newlines()\\n+                var more = true\\n+                while more {\\n+                    let pn = try self.expect(TokenKind.Ident, \\\"expected parameter name\\\")\\n+                    var j = 1\\n+                    while j < pnames.len() {\\n+                        if pnames[j] == pn.value { return problem(\\\"duplicate parameter `{pn.value}`\\\", pn.line, pn.col) }\\n+                        j += 1\\n+                    }\\n+                    let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after parameter name\\\")\\n+                    let pt = try self.parse_type()\\n+                    try! pnames.push(pn.value)\\n+                    try! ptypes.push(pt)\\n+                    self.skip_newlines()\\n+                    if self.at(TokenKind.Comma) {\\n+                        let _ = self.take()\\n+                        self.skip_newlines()\\n+                    } else { more = false }\\n+                }\\n+            }\\n+            let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after parameters\\\")\\n+            var result = ValueType.Void\\n+            if self.at(TokenKind.Arrow) {\\n+                let _ = self.take()\\n+                result = try self.parse_type()\\n+                if reference_type(result) { return problem(\\\"reference results are not supported\\\", method.line, method.col) }\\n+            }\\n+            let _ = try self.expect(TokenKind.LBrace, \\\"expected method body\\\")\\n+            let start = self.pos\\n+            var braces = 1\\n+            while braces > 0 && not self.at(TokenKind.Eof) {\\n+                if self.at(TokenKind.LBrace) { braces += 1 }\\n+                if self.at(TokenKind.RBrace) { braces -= 1 }\\n+                let _ = self.take()\\n+            }\\n+            if braces != 0 { return problem(\\\"unterminated method body\\\", ft.line, ft.col) }\\n+            try! self.signatures.push(Signature(name: method.value, owner: owner,\\n+                param_names: move pnames, params: move ptypes, result: result,\\n+                body_start: start, body_end: self.pos - 1, line: method.line, col: method.col))\\n+            self.skip_newlines()\\n+        }\\n+        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace for extension\\\")\\n+        return\\n+    }\\n+\\n     func scan_signatures(&var self) -> Result<Void, ProtoError> {\\n         self.pos = 0\\n         self.skip_newlines()\\n@@ -331,7 +429,12 @@ extension Compiler {\\n                 self.skip_newlines()\\n                 continue\\n             }\\n-            let ft = try self.expect(TokenKind.Func, \\\"only function, record, enum, and static declarations are supported at module scope\\\")\\n+            if self.at(TokenKind.Extension) {\\n+                try self.scan_extension()\\n+                self.skip_newlines()\\n+                continue\\n+            }\\n+            let ft = try self.expect(TokenKind.Func, \\\"only function, record, enum, static, and extension declarations are supported at module scope\\\")\\n             let name = try self.expect(TokenKind.Ident, \\\"expected function name\\\")\\n             if name.value == \\\"print\\\" {\\n                 return problem(\\\"`print` is reserved by the prototype\\\", name.line, name.col)\\n@@ -393,7 +496,7 @@ extension Compiler {\\n             }\\n             if braces != 0 { return problem(\\\"unterminated function body\\\", ft.line, ft.col) }\\n             let finish = self.pos - 1\\n-            try! self.signatures.push(Signature(name: name.value, param_names: move pnames,\\n+            try! self.signatures.push(Signature(name: name.value, owner: -1, param_names: move pnames,\\n                 params: move ptypes, result: result, body_start: start, body_end: finish,\\n                 line: name.line, col: name.col))\\n             self.skip_newlines()\\n@@ -402,7 +505,7 @@ extension Compiler {\\n         var main_i = -1\\n         var i = 0\\n         while i < self.signatures.len() {\\n-            if self.signatures[i].name == \\\"main\\\" {\\n+            if self.signatures[i].owner < 0 && self.signatures[i].name == \\\"main\\\" {\\n                 mains += 1\\n                 main_i = i\\n                 if self.signatures[i].params.len() != 0 || self.signatures[i].result != ValueType.Void {\\n@@ -687,7 +790,7 @@ extension Compiler {\\n     func read_place(&var self, place: Place, t: &Token) -> Result<ExprValue, ProtoError> {\\n         try self.check_read(place.root, &t)\\n         if place.indirect {\\n-            let dst = self.new_slot(place.ty)\\n+            let dst = try self.new_value(place.ty)\\n             let at = self.emit_at(Op.LoadRef, t.line, t.col)\\n             self.code[at].dst = dst\\n             self.code[at].a = place.slot\\n@@ -835,6 +938,23 @@ extension Compiler {\\n             self.depth -= 1\\n             return value\\n         }\\n+        if self.at(TokenKind.Ident) || self.at(TokenKind.SelfKw) {\\n+            let candidate = self.token()\\n+            if self.binding_index(candidate.value) >= 0 {\\n+                let parsed = try self.parse_place()\\n+                let place = parsed.1\\n+                if self.at(TokenKind.Dot) {\\n+                    let dot = self.take()\\n+                    let method = try self.expect(TokenKind.Ident, \\\"expected method name after `.`\\\")\\n+                    let owner = match place.ty { case Record(index) -> index, case _ -> -1 }\\n+                    if owner < 0 { return problem(\\\"method receiver must be a record place\\\", dot.line, dot.col) }\\n+                    let mi = self.method_index(owner, method.value)\\n+                    if mi < 0 { return problem(\\\"record `{self.records[owner].name}` has no method `{method.value}`\\\", method.line, method.col) }\\n+                    return self.parse_method_call(place, method, mi)\\n+                }\\n+                return self.read_place(place, &candidate)\\n+            }\\n+        }\\n         if self.at(TokenKind.Ident) {\\n             let name = self.take()\\n             let ei = self.enum_index(name.value)\\n@@ -860,8 +980,10 @@ extension Compiler {\\n             let bi = self.binding_index(name.value)\\n             if bi >= 0 {\\n                 var place = Place(ty: self.bindings[bi].ty, slot: self.bindings[bi].slot,\\n-                                  mutable: self.bindings[bi].mutable, indirect: false, root: bi)\\n+                                  mutable: self.bindings[bi].mutable, indirect: false, root: bi,\\n+                                  ref_ty: ValueType.Void)\\n                 if reference_type(place.ty) {\\n+                    place.ref_ty = place.ty\\n                     place.mutable = reference_mutable(&self.references, place.ty)\\n                     place.ty = try reference_target(&self.references, place.ty)\\n                     place.indirect = true\\n@@ -900,7 +1022,7 @@ extension Compiler {\\n     }\\n \\n     func assignment_ahead(&self) -> Bool {\\n-        if not self.at(TokenKind.Ident) { return false }\\n+        if not self.at(TokenKind.Ident) && not self.at(TokenKind.SelfKw) { return false }\\n         var i = self.pos + 1\\n         while i + 1 < self.tokens.len() && self.tokens[i].kind == TokenKind.Dot && self.tokens[i + 1].kind == TokenKind.Ident { i += 2 }\\n         if i >= self.tokens.len() { return false }\\n@@ -909,29 +1031,46 @@ extension Compiler {\\n     }\\n \\n     func parse_place(&var self) -> Result<(Token, Place), ProtoError> {\\n-        let name = try self.expect(TokenKind.Ident, \\\"expected assignment target\\\")\\n+        if not self.at(TokenKind.Ident) && not self.at(TokenKind.SelfKw) {\\n+            return self.fail(\\\"expected named place\\\")\\n+        }\\n+        let name = self.take()\\n         let bi = self.binding_index(name.value)\\n         if bi < 0 {\\n             if self.static_index(name.value) >= 0 { return problem(\\\"cannot assign to immutable static `{name.value}`\\\", name.line, name.col) }\\n             return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col)\\n         }\\n         var place = Place(ty: self.bindings[bi].ty, slot: self.bindings[bi].slot,\\n-                          mutable: self.bindings[bi].mutable, indirect: false, root: bi)\\n+                          mutable: self.bindings[bi].mutable, indirect: false, root: bi,\\n+                          ref_ty: ValueType.Void)\\n         if reference_type(place.ty) {\\n+            place.ref_ty = place.ty\\n             place.mutable = reference_mutable(&self.references, place.ty)\\n             place.ty = try reference_target(&self.references, place.ty)\\n             place.indirect = true\\n         }\\n         while self.at(TokenKind.Dot) {\\n+            if self.pos + 2 < self.tokens.len() && self.tokens[self.pos + 1].kind == TokenKind.Ident && self.tokens[self.pos + 2].kind == TokenKind.LParen { break }\\n             let _ = self.take()\\n             let field = try self.expect(TokenKind.Ident, \\\"expected record field after `.`\\\")\\n-            if place.indirect { return problem(\\\"reference parameters cannot be used as records\\\", field.line, field.col) }\\n             match place.ty {\\n                 case Record(ri) -> {\\n                     let fi = self.field_index(ri, field.value)\\n                     if fi < 0 { return problem(\\\"record `{self.records[ri].name}` has no field `{field.value}`\\\", field.line, field.col) }\\n-                    place.slot += try record_field_offset(&self.records, ri, fi)\\n-                    place.ty = self.records[ri].field_types[fi]\\n+                    let field_type = self.records[ri].field_types[fi]\\n+                    if place.indirect {\\n+                        let projected_ref = self.intern_reference(field_type, place.mutable)\\n+                        let dst = self.new_slot(projected_ref)\\n+                        let at = self.emit_at(Op.RefField, field.line, field.col)\\n+                        self.code[at].dst = dst\\n+                        self.code[at].a = place.slot\\n+                        self.code[at].immediate = fi\\n+                        place.slot = dst\\n+                        place.ref_ty = projected_ref\\n+                    } else {\\n+                        place.slot += try record_field_offset(&self.records, ri, fi)\\n+                    }\\n+                    place.ty = field_type\\n                 },\\n                 case _ -> { return problem(\\\"field assignment requires a record\\\", field.line, field.col) }\\n             }\\n@@ -946,6 +1085,9 @@ extension Compiler {\\n             let dot = self.take()\\n             self.expr_space()\\n             let field = try self.expect(TokenKind.Ident, \\\"expected record field after `.`\\\")\\n+            if self.at(TokenKind.LParen) {\\n+                return problem(\\\"method calls require a named place receiver\\\", dot.line, dot.col)\\n+            }\\n             match value.ty {\\n                 case Record(ri) -> {\\n                     let fi = self.field_index(ri, field.value)\\n@@ -1087,6 +1229,14 @@ extension Compiler {\\n                 if ai >= self.signatures[fi].params.len() {\\n                     return problem(\\\"too many arguments to `{name.value}`\\\", name.line, name.col)\\n                 }\\n+                if self.at(TokenKind.Ident) && self.pos + 1 < self.tokens.len() && self.tokens[self.pos + 1].kind == TokenKind.Colon {\\n+                    let label = self.take()\\n+                    let _ = self.take()\\n+                    if label.value != self.signatures[fi].param_names[ai] {\\n+                        return problem(\\\"argument label `{label.value}` does not match parameter `{self.signatures[fi].param_names[ai]}`\\\", label.line, label.col)\\n+                    }\\n+                    self.expr_space()\\n+                }\\n                 let formal = self.signatures[fi].params[ai]\\n                 if reference_type(formal) {\\n                     let amp = try self.expect(TokenKind.Ampersand, \\\"reference argument requires explicit `&`\\\")\\n@@ -1111,7 +1261,7 @@ extension Compiler {\\n                     try self.register_borrow(&place, explicit_mutable, &amp)\\n                     let dst = self.new_slot(formal)\\n                     if place.indirect {\\n-                        let source = self.bindings[place.root].ty\\n+                        let source = place.ref_ty\\n                         if not reference_transfer(&self.references, source, formal) {\\n                             return problem(\\\"reference argument mutability does not match parameter\\\", amp.line, amp.col)\\n                         }\\n@@ -1163,6 +1313,115 @@ extension Compiler {\\n         expr_value(rt, dst, true)\\n     }\\n \\n+    func parse_method_call(&var self, receiver: Place, name: Token,\\n+                           fi: Int) -> Result<ExprValue, ProtoError> {\\n+        let saved_borrows = self.active_borrows.len()\\n+        let formal_receiver = self.signatures[fi].params[0]\\n+        let receiver_mutable = reference_mutable(&self.references, formal_receiver)\\n+        if receiver_mutable && not receiver.mutable {\\n+            return problem(\\\"cannot call a mutating method on an immutable receiver\\\", name.line, name.col)\\n+        }\\n+        try self.register_borrow(&receiver, receiver_mutable, &name)\\n+        var args = Vector<Int>()\\n+        let receiver_slot = self.new_slot(formal_receiver)\\n+        if receiver.indirect {\\n+            if not reference_transfer(&self.references, receiver.ref_ty, formal_receiver) {\\n+                return problem(\\\"receiver reference mutability does not match method\\\", name.line, name.col)\\n+            }\\n+            let copy = self.emit_at(Op.Copy, name.line, name.col)\\n+            self.code[copy].dst = receiver_slot\\n+            self.code[copy].a = receiver.slot\\n+        } else {\\n+            let addr = self.emit_at(Op.Addr, name.line, name.col)\\n+            self.code[addr].dst = receiver_slot\\n+            self.code[addr].a = receiver.slot\\n+        }\\n+        try! args.push(receiver_slot)\\n+        let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after method name\\\")\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        var ai = 1\\n+        if not self.at(TokenKind.RParen) {\\n+            var more = true\\n+            while more {\\n+                if ai >= self.signatures[fi].params.len() {\\n+                    return problem(\\\"too many arguments to method `{name.value}`\\\", name.line, name.col)\\n+                }\\n+                if self.at(TokenKind.Ident) && self.pos + 1 < self.tokens.len() && self.tokens[self.pos + 1].kind == TokenKind.Colon {\\n+                    let label = self.take()\\n+                    let _ = self.take()\\n+                    if label.value != self.signatures[fi].param_names[ai] {\\n+                        return problem(\\\"argument label `{label.value}` does not match parameter `{self.signatures[fi].param_names[ai]}`\\\", label.line, label.col)\\n+                    }\\n+                    self.expr_space()\\n+                }\\n+                let formal = self.signatures[fi].params[ai]\\n+                if reference_type(formal) {\\n+                    let amp = try self.expect(TokenKind.Ampersand, \\\"reference argument requires explicit `&`\\\")\\n+                    var explicit_mutable = false\\n+                    if self.at(TokenKind.Var) {\\n+                        let _ = self.take()\\n+                        explicit_mutable = true\\n+                    }\\n+                    let parsed = try self.parse_place()\\n+                    let place = parsed.1\\n+                    let formal_mutable = reference_mutable(&self.references, formal)\\n+                    let target = try reference_target(&self.references, formal)\\n+                    if formal_mutable && not explicit_mutable { return problem(\\\"mutable reference argument requires `&var`\\\", amp.line, amp.col) }\\n+                    if place.ty != target { return problem(\\\"reference argument type does not match parameter\\\", amp.line, amp.col) }\\n+                    if explicit_mutable && not place.mutable { return problem(\\\"cannot mutably borrow an immutable place\\\", amp.line, amp.col) }\\n+                    try self.register_borrow(&place, explicit_mutable, &amp)\\n+                    let dst = self.new_slot(formal)\\n+                    if place.indirect {\\n+                        if not reference_transfer(&self.references, place.ref_ty, formal) { return problem(\\\"reference argument mutability does not match parameter\\\", amp.line, amp.col) }\\n+                        let copy = self.emit_at(Op.Copy, amp.line, amp.col)\\n+                        self.code[copy].dst = dst\\n+                        self.code[copy].a = place.slot\\n+                    } else {\\n+                        let addr = self.emit_at(Op.Addr, amp.line, amp.col)\\n+                        self.code[addr].dst = dst\\n+                        self.code[addr].a = place.slot\\n+                    }\\n+                    try! args.push(dst)\\n+                } else {\\n+                    if self.at(TokenKind.Ampersand) {\\n+                        let address = self.token()\\n+                        return problem(\\\"value parameter does not accept an address argument\\\", address.line, address.col)\\n+                    }\\n+                    var value = try self.parse_expected_expr(formal)\\n+                    if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n+                    value = try self.snapshot(value, &name)\\n+                    let layout = try value_layout(&self.records, value.ty)\\n+                    var word = 0\\n+                    while word < layout.len() {\\n+                        try! args.push(value.slot + word)\\n+                        word += 1\\n+                    }\\n+                }\\n+                ai += 1\\n+                self.skip_newlines()\\n+                if self.at(TokenKind.Comma) {\\n+                    let _ = self.take()\\n+                    self.skip_newlines()\\n+                }\\n+                else { more = false }\\n+            }\\n+        }\\n+        let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after method arguments\\\")\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        if ai != self.signatures[fi].params.len() { return problem(\\\"wrong number of arguments to method `{name.value}`\\\", name.line, name.col) }\\n+        let rt = self.signatures[fi].result\\n+        let dst = if rt == ValueType.Void { -1 } else { try self.new_value(rt) }\\n+        let at = self.emit_at(Op.Call, name.line, name.col)\\n+        self.code[at].target = fi\\n+        self.code[at].dst = dst\\n+        self.code[at].args = move args\\n+        self.restore_borrows(saved_borrows)\\n+        expr_value(rt, dst, true)\\n+    }\\n+\\n     func parse_unary_inner(&var self) -> Result<ExprValue, ProtoError> {\\n         self.expr_space()\\n         if self.at(TokenKind.Exclaim) || self.at(TokenKind.Not_) {\\n@@ -2240,7 +2499,9 @@ extension Compiler {\\n         }\\n         let slots = self.slots.copy()\\n         let code = self.code.copy()\\n-        FuncIR(name: self.signatures[index].name, params: self.signatures[index].params.copy(),\\n+        let ir_name = if self.signatures[index].owner < 0 { self.signatures[index].name }\\n+                      else { \\\"{self.records[self.signatures[index].owner].name}.{self.signatures[index].name}\\\" }\\n+        FuncIR(name: ir_name, params: self.signatures[index].params.copy(),\\n                result: self.current_result, slots: move slots, code: move code)\\n     }\\n }\\n@@ -2264,7 +2525,7 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n     var main_index = -1\\n     var i = 0\\n     while i < compiler.signatures.len() {\\n-        if compiler.signatures[i].name == \\\"main\\\" { main_index = i }\\n+        if compiler.signatures[i].owner < 0 && compiler.signatures[i].name == \\\"main\\\" { main_index = i }\\n         let f = try compiler.compile_function(i)\\n         try! functions.push(move f)\\n         i += 1\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex 568e215c..c22fa8fe 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -394,24 +394,84 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n         case LoadRef -> {\\n             put(&var out, \\\"  \\\")\\n             load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n-            put(&var out, \\\"  \\\")\\n-            temp_name(&var out, instruction_index, \\\"v\\\")\\n-            put(&var out, \\\" = load i64, ptr \\\")\\n-            temp_name(&var out, instruction_index, \\\"ref\\\")\\n-            put(&var out, \\\"\\\\n\\\")\\n-            store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+            let target = try reference_target(&program.references, function.slots[inst.a])\\n+            let layout = try value_layout(&program.records, target)\\n+            var word = 0\\n+            while word < layout.len() {\\n+                if word > 0 {\\n+                    put(&var out, \\\"  \\\")\\n+                    temp_name(&var out, instruction_index, \\\"ptr{word}\\\")\\n+                    put(&var out, \\\" = getelementptr i64, ptr \\\")\\n+                    temp_name(&var out, instruction_index, \\\"ref\\\")\\n+                    put(&var out, \\\", i64 \\\")\\n+                    put_int(&var out, word)\\n+                    put(&var out, \\\"\\\\n\\\")\\n+                }\\n+                put(&var out, \\\"  \\\")\\n+                temp_name(&var out, instruction_index, \\\"v{word}\\\")\\n+                put(&var out, \\\" = load i64, ptr \\\")\\n+                if word == 0 { temp_name(&var out, instruction_index, \\\"ref\\\") }\\n+                else { temp_name(&var out, instruction_index, \\\"ptr{word}\\\") }\\n+                put(&var out, \\\"\\\\n\\\")\\n+                word += 1\\n+            }\\n+            word = 0\\n+            while word < layout.len() {\\n+                store_temp(&var out, instruction_index, \\\"v{word}\\\", inst.dst + word)\\n+                word += 1\\n+            }\\n             next_block(&var out, instruction_index)\\n         },\\n         case StoreRef -> {\\n             put(&var out, \\\"  \\\")\\n             load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n+            let target = try reference_target(&program.references, function.slots[inst.a])\\n+            let layout = try value_layout(&program.records, target)\\n+            var word = 0\\n+            while word < layout.len() {\\n+                put(&var out, \\\"  \\\")\\n+                load_slot(&var out, instruction_index, \\\"v{word}\\\", inst.b + word)\\n+                word += 1\\n+            }\\n+            word = 0\\n+            while word < layout.len() {\\n+                if word > 0 {\\n+                    put(&var out, \\\"  \\\")\\n+                    temp_name(&var out, instruction_index, \\\"ptr{word}\\\")\\n+                    put(&var out, \\\" = getelementptr i64, ptr \\\")\\n+                    temp_name(&var out, instruction_index, \\\"ref\\\")\\n+                    put(&var out, \\\", i64 \\\")\\n+                    put_int(&var out, word)\\n+                    put(&var out, \\\"\\\\n\\\")\\n+                }\\n+                put(&var out, \\\"  store i64 \\\")\\n+                temp_name(&var out, instruction_index, \\\"v{word}\\\")\\n+                put(&var out, \\\", ptr \\\")\\n+                if word == 0 { temp_name(&var out, instruction_index, \\\"ref\\\") }\\n+                else { temp_name(&var out, instruction_index, \\\"ptr{word}\\\") }\\n+                put(&var out, \\\"\\\\n\\\")\\n+                word += 1\\n+            }\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case RefField -> {\\n             put(&var out, \\\"  \\\")\\n-            load_slot(&var out, instruction_index, \\\"v\\\", inst.b)\\n-            put(&var out, \\\"  store i64 \\\")\\n-            temp_name(&var out, instruction_index, \\\"v\\\")\\n-            put(&var out, \\\", ptr \\\")\\n-            temp_name(&var out, instruction_index, \\\"ref\\\")\\n-            put(&var out, \\\"\\\\n\\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n+            let parent = try reference_target(&program.references, function.slots[inst.a])\\n+            match parent {\\n+                case Record(index) -> {\\n+                    let offset = try record_field_offset(&program.records, index, inst.immediate)\\n+                    put(&var out, \\\"  \\\")\\n+                    temp_name(&var out, instruction_index, \\\"field\\\")\\n+                    put(&var out, \\\" = getelementptr i64, ptr \\\")\\n+                    temp_name(&var out, instruction_index, \\\"ref\\\")\\n+                    put(&var out, \\\", i64 \\\")\\n+                    put_int(&var out, offset)\\n+                    put(&var out, \\\"\\\\n\\\")\\n+                    store_ref_temp(&var out, instruction_index, \\\"field\\\", inst.dst)\\n+                },\\n+                case _ -> {}\\n+            }\\n             next_block(&var out, instruction_index)\\n         },\\n         case Add -> emit_binary(&var out, instruction_index, &inst, \\\"add\\\", true, function.slots[inst.dst]),\\n@@ -648,11 +708,22 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n         parameter_index = parameter_index + 1\\n     }\\n     put(&var out, \\\") \\\\{\\\\nentry:\\\\n\\\")\\n+    put(&var out, \\\"  %slots = alloca [\\\")\\n+    put_int(&var out, function.slots.len())\\n+    put(&var out, \\\" x i64]\\\\n\\\")\\n     var slot_index = 0\\n     while slot_index < function.slots.len() {\\n         put(&var out, \\\"  \\\")\\n         slot_name(&var out, slot_index)\\n-        put(&var out, if reference_type(function.slots[slot_index]) { \\\" = alloca ptr\\\\n\\\" } else { \\\" = alloca i64\\\\n\\\" })\\n+        if reference_type(function.slots[slot_index]) {\\n+            put(&var out, \\\" = alloca ptr\\\\n\\\")\\n+        } else {\\n+            put(&var out, \\\" = getelementptr [\\\")\\n+            put_int(&var out, function.slots.len())\\n+            put(&var out, \\\" x i64], ptr %slots, i64 0, i64 \\\")\\n+            put_int(&var out, slot_index)\\n+            put(&var out, \\\"\\\\n\\\")\\n+        }\\n         slot_index = slot_index + 1\\n     }\\n     var parameter_words = 0\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex 16115707..b68416d4 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -11,7 +11,7 @@ public enum Op {\\n     case Neg, case Not, case Eq, case Ne, case Lt, case Le, case Gt, case Ge,\\n     case Jump, case Branch, case Call, case Print, case Return,\\n     case Cast, case Truncate, case WrapAdd, case WrapSub, case WrapMul,\\n-    case Addr, case LoadRef, case StoreRef,\\n+    case Addr, case LoadRef, case StoreRef, case RefField,\\n     case BitAnd, case BitOr, case BitXor, case BitNot, case Shl, case Shr\\n }\\n @synthesize\\n@@ -212,7 +212,12 @@ public func reference_transfer(refs: &Vector<ReferenceIR>, source: ValueType, ta\\n public func validate_references(refs: &Vector<ReferenceIR>) -> Result<Void, ProtoError> {\\n     var n = 0\\n     while n < refs.len() {\\n-        if not scalar_type(refs[n].target) { return problem(\\\"reference target must be a scalar type\\\", 0, 0) }\\n+        if not scalar_type(refs[n].target) {\\n+            match refs[n].target {\\n+                case Record(_) -> {},\\n+                case _ -> { return problem(\\\"reference target must be a scalar or record type\\\", 0, 0) }\\n+            }\\n+        }\\n         n += 1\\n     }\\n     return\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex 73a4dbfd..90ae7b9b 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -114,19 +114,35 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n             } else if not scalar_type(dst) || a != dst { return invalid(&i) }\\n         },\\n         case Addr -> {\\n-            if not reference_type(dst) || not scalar_type(a) { return invalid(&i) }\\n+            if not reference_type(dst) { return invalid(&i) }\\n             let target = try reference_target(&p.references, dst)\\n-            if target != a { return invalid(&i) }\\n+            let layout = try value_layout(&p.records, target)\\n+            if not range_matches(&f, i.a, &layout) { return invalid(&i) }\\n         },\\n         case LoadRef -> {\\n-            if not reference_type(a) || not scalar_type(dst) { return invalid(&i) }\\n+            if not reference_type(a) { return invalid(&i) }\\n             let target = try reference_target(&p.references, a)\\n-            if target != dst { return invalid(&i) }\\n+            let layout = try value_layout(&p.records, target)\\n+            if not range_matches(&f, i.dst, &layout) { return invalid(&i) }\\n         },\\n         case StoreRef -> {\\n-            if not reference_mutable(&p.references, a) || not scalar_type(b) { return invalid(&i) }\\n+            if not reference_mutable(&p.references, a) { return invalid(&i) }\\n             let target = try reference_target(&p.references, a)\\n-            if target != b { return invalid(&i) }\\n+            let layout = try value_layout(&p.records, target)\\n+            if not range_matches(&f, i.b, &layout) { return invalid(&i) }\\n+        },\\n+        case RefField -> {\\n+            if not reference_type(a) || not reference_type(dst) { return invalid(&i) }\\n+            if reference_mutable(&p.references, dst) && not reference_mutable(&p.references, a) { return invalid(&i) }\\n+            let parent = try reference_target(&p.references, a)\\n+            let target = try reference_target(&p.references, dst)\\n+            match parent {\\n+                case Record(index) -> {\\n+                    if i.immediate < 0 || i.immediate >= p.records[index].field_types.len() { return invalid(&i) }\\n+                    if target != p.records[index].field_types[i.immediate] { return invalid(&i) }\\n+                },\\n+                case _ -> { return invalid(&i) }\\n+            }\\n         },\\n         case Add -> { try check_binary(&i, dst, a, b) },\\n         case Sub -> { try check_binary(&i, dst, a, b) },\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 1216c0eb..11ba764b 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -86,6 +86,10 @@ func compare(a: Int, b: Int, t: ValueType, op: Op) -> Int {\\n     if yes { 1 } else { 0 }\\n }\\n \\n+func valid_reference(cells: &Vector<Int>, address: Int, width: Int) -> Bool {\\n+    address >= 0 && address <= cells.len() && width <= cells.len() - address\\n+}\\n+\\n func run_function(program: &Program, function_index: Int, arguments: &Vector<Int>,\\n                   cells: &var Vector<Int>, remaining: &var Int,\\n                   depth: Int) -> Result<CallResult, ProtoError> {\\n@@ -114,13 +118,50 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n             case Addr -> { cells[base + inst.dst] = base + inst.a },\\n             case LoadRef -> {\\n                 let address = cells[base + inst.a]\\n-                if address < 0 || address >= cells.len() { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n-                cells[base + inst.dst] = cells[address]\\n+                let target = try reference_target(&program.references, function.slots[inst.a])\\n+                let layout = try value_layout(&program.records, target)\\n+                if not valid_reference(&cells, address, layout.len()) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                var values = Vector<Int>()\\n+                var word = 0\\n+                while word < layout.len() {\\n+                    try! values.push(cells[address + word])\\n+                    word += 1\\n+                }\\n+                word = 0\\n+                while word < values.len() {\\n+                    cells[base + inst.dst + word] = values[word]\\n+                    word += 1\\n+                }\\n             },\\n             case StoreRef -> {\\n                 let address = cells[base + inst.a]\\n-                if address < 0 || address >= cells.len() { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n-                cells[address] = cells[base + inst.b]\\n+                let target = try reference_target(&program.references, function.slots[inst.a])\\n+                let layout = try value_layout(&program.records, target)\\n+                if not valid_reference(&cells, address, layout.len()) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                var values = Vector<Int>()\\n+                var word = 0\\n+                while word < layout.len() {\\n+                    try! values.push(cells[base + inst.b + word])\\n+                    word += 1\\n+                }\\n+                word = 0\\n+                while word < values.len() {\\n+                    cells[address + word] = values[word]\\n+                    word += 1\\n+                }\\n+            },\\n+            case RefField -> {\\n+                let address = cells[base + inst.a]\\n+                let parent = try reference_target(&program.references, function.slots[inst.a])\\n+                let layout = try value_layout(&program.records, parent)\\n+                if not valid_reference(&cells, address, layout.len()) { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                match parent {\\n+                    case Record(index) -> {\\n+                        let offset = try record_field_offset(&program.records, index, inst.immediate)\\n+                        cells[base + inst.dst] = address + offset\\n+                    },\\n+                    case _ -> { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                }\\n             },\\n             case Add -> { cells[base + inst.dst] = try checked_binary(cells[base + inst.a], cells[base + inst.b], t, inst.op) },\\n             case Sub -> { cells[base + inst.dst] = try checked_binary(cells[base + inst.a], cells[base + inst.b], t, inst.op) },\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex 51190701..7c4447a7 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -35,6 +35,14 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"receivers/whole_record_reference\\\", \\\"1\\\\n2\\\\n3\\\\n4\\\\n9\\\\n8\\\\n\\\", section=\\\"receivers\\\"),\\n+    RunCase(\\\"receivers/nested_forwarding\\\", \\\"7\\\\n13\\\\n18\\\\n\\\", section=\\\"receivers\\\"),\\n+    RunCase(\\\"receivers/mixed_abi\\\", \\\"5\\\\n6\\\\n9\\\\n10\\\\n\\\", section=\\\"receivers\\\"),\\n+    RunCase(\\\"receivers/recursive_growth\\\", \\\"41\\\\n42\\\\n\\\", section=\\\"receivers\\\"),\\n+    RunCase(\\\"receivers/methods_basic\\\", \\\"5\\\\n11\\\\n11\\\\n\\\", section=\\\"receivers\\\"),\\n+    RunCase(\\\"receivers/methods_lookup_labels\\\", \\\"13\\\\n22\\\\n18\\\\n\\\", section=\\\"receivers\\\"),\\n+    RunCase(\\\"receivers/nested_method_forward\\\", \\\"3\\\\n7\\\\n\\\", section=\\\"receivers\\\"),\\n+    RunCase(\\\"receivers/lexer_advance\\\", \\\"1\\\\n2\\\\n2\\\\n3\\\\n2\\\\n2\\\\n\\\", section=\\\"receivers\\\"),\\n     RunCase(\\\"references/read_mutate_compound\\\", \\\"41\\\\n42\\\\n51\\\\n25\\\\n5\\\\n\\\", section=\\\"references\\\"),\\n     RunCase(\\\"references/scalar_kinds\\\", \\\"18446744073709551615\\\\n255\\\\ntrue\\\\ntrue\\\\n\\\", section=\\\"references\\\"),\\n     RunCase(\\\"references/direct_record_fields\\\", \\\"8\\\\n21\\\\n\\\", section=\\\"references\\\"),\\n@@ -160,6 +168,22 @@ RUN_CASES = (\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"receivers/reject_write_shared_record\\\", \\\"cannot assign through a shared reference\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_immutable_record_borrow\\\", \\\"cannot mutably borrow an immutable place\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_immutable_receiver\\\", \\\"cannot call a mutating method on an immutable receiver\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_receiver_argument_overlap\\\", \\\"overlapping arguments cannot include a mutable borrow\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_pending_write_receiver\\\", \\\"cannot borrow an assignment destination while evaluating its right-hand side\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_wrong_label\\\", \\\"argument label\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_method_arity\\\", \\\"arguments\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_method_type\\\", \\\"cannot implicitly convert\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_temporary_receiver\\\", \\\"method calls require a named place receiver\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_unknown_extension\\\", \\\"extension target must be a known record\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_enum_extension\\\", \\\"extension target must be a known record\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_trait_extension\\\", \\\"traits are not supported\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_static_method\\\", \\\"static methods are not supported\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_value_receiver\\\", \\\"methods require an explicit `&self` or `&var self` receiver\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_duplicate_method\\\", \\\"duplicate method\\\", \\\"receivers\\\"),\\n+    RejectCase(\\\"receivers/reject_method_reference_result\\\", \\\"reference results are not supported\\\", \\\"receivers\\\"),\\n     RejectCase(\\\"references/reject_alias_mutable_shared\\\", \\\"overlapping arguments cannot include a mutable borrow\\\", \\\"references\\\"),\\n     RejectCase(\\\"references/reject_alias_two_fields\\\", \\\"overlapping arguments cannot include a mutable borrow\\\", \\\"references\\\"),\\n     RejectCase(\\\"references/reject_later_read\\\", \\\"cannot read a place while it is mutably borrowed\\\", \\\"references\\\"),\\n@@ -175,7 +199,6 @@ REJECT_CASES = (\\n     RejectCase(\\\"references/reject_reference_field\\\", \\\"reference record fields are not supported\\\", \\\"references\\\"),\\n     RejectCase(\\\"references/reject_reference_result\\\", \\\"reference results are not supported\\\", \\\"references\\\"),\\n     RejectCase(\\\"references/reject_reference_static\\\", \\\"static type must be an integer or Bool\\\", \\\"references\\\"),\\n-    RejectCase(\\\"references/reject_whole_record\\\", \\\"only scalar references are supported\\\", \\\"references\\\"),\\n     RejectCase(\\\"references/reject_nested_reference\\\", \\\"references to references are not supported\\\", \\\"references\\\"),\\n     RejectCase(\\\"references/reject_outer_borrow_preserved\\\", \\\"cannot read a place while it is mutably borrowed\\\", \\\"references\\\"),\\n     RejectCase(\\\"references/reject_spaced_nested_reference\\\", \\\"references to references are not supported\\\", \\\"references\\\"),\\n@@ -206,7 +229,7 @@ REJECT_CASES = (\\n     RejectCase(\\\"reject_semicolon\\\", \\\"semicolons are not supported\\\"),\\n     RejectCase(\\\"reject_string\\\", \\\"expected expression\\\"),\\n     RejectCase(\\\"reject_bool_arithmetic\\\", \\\"arithmetic requires integer operands\\\"),\\n-    RejectCase(\\\"reject_import\\\", \\\"only function, record, enum, and static declarations are supported\\\"),\\n+    RejectCase(\\\"reject_import\\\", \\\"only function, record, enum, static, and extension declarations are supported\\\"),\\n     RejectCase(\\\"reject_scope_leak\\\", \\\"unknown name\\\"),\\n     RejectCase(\\\"reject_unary_depth\\\", \\\"expression nesting limit exceeded\\\"),\\n     RejectCase(\\\"reject_reserved_print\\\", \\\"reserved\\\"),\\n@@ -347,7 +370,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -376,7 +399,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\") else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\ndiff --git a/prototypes/minivm/tests/receiver_contract.saw b/prototypes/minivm/tests/receiver_contract.saw\\nnew file mode 100644\\nindex 00000000..126d8b55\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/receiver_contract.saw\\n@@ -0,0 +1,115 @@\\n+import src.model.*\\n+import src.verify.*\\n+import src.vm.*\\n+\\n+func valid(p: &Program) -> Bool {\\n+    try verify(&p) catch { return false }\\n+    true\\n+}\\n+\\n+func op(code: &var Vector<Instruction>, kind: Op, dst: Int, a: Int, b: Int, immediate: Int) {\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    i.immediate = immediate\\n+    try! code.push(move i)\\n+}\\n+\\n+func base_program() -> Program {\\n+    var records = Vector<RecordIR>()\\n+    let pair_names: Vector<String> = [\\\"number\\\", \\\"flag\\\"]\\n+    let pair_types: Vector<ValueType> = [ValueType.I64, ValueType.Bool]\\n+    try! records.push(RecordIR(name: \\\"Pair\\\", field_names: move pair_names,\\n+        field_types: move pair_types, line: 1, col: 1))\\n+    let outer_names: Vector<String> = [\\\"prefix\\\", \\\"pair\\\"]\\n+    let outer_types: Vector<ValueType> = [ValueType.I64, ValueType.Record(0)]\\n+    try! records.push(RecordIR(name: \\\"Outer\\\", field_names: move outer_names,\\n+        field_types: move outer_types, line: 1, col: 1))\\n+    var refs = Vector<ReferenceIR>()\\n+    try! refs.push(ReferenceIR(target: ValueType.Record(1), mutable: true))\\n+    try! refs.push(ReferenceIR(target: ValueType.Record(1), mutable: false))\\n+    try! refs.push(ReferenceIR(target: ValueType.Record(0), mutable: true))\\n+    try! refs.push(ReferenceIR(target: ValueType.Record(0), mutable: false))\\n+    try! refs.push(ReferenceIR(target: ValueType.Bool, mutable: true))\\n+    try! refs.push(ReferenceIR(target: ValueType.Bool, mutable: false))\\n+    try! refs.push(ReferenceIR(target: ValueType.I64, mutable: true))\\n+    let slots: Vector<ValueType> = [ValueType.I64, ValueType.I64, ValueType.Bool,\\n+        ValueType.Ref(0), ValueType.Ref(1), ValueType.Ref(2), ValueType.Ref(3),\\n+        ValueType.Ref(4), ValueType.Ref(5), ValueType.I64, ValueType.Bool,\\n+        ValueType.I64, ValueType.I64, ValueType.Bool, ValueType.Ref(6)]\\n+    var code = Vector<Instruction>()\\n+    op(&var code, Op.Const, 0, -1, -1, 7)\\n+    op(&var code, Op.Const, 1, -1, -1, 42)\\n+    op(&var code, Op.Const, 2, -1, -1, 1)\\n+    op(&var code, Op.Addr, 3, 0, -1, 0)\\n+    op(&var code, Op.Copy, 4, 3, -1, 0)\\n+    op(&var code, Op.RefField, 5, 3, -1, 1)\\n+    op(&var code, Op.Copy, 6, 5, -1, 0)\\n+    op(&var code, Op.RefField, 7, 5, -1, 1)\\n+    op(&var code, Op.Copy, 8, 7, -1, 0)\\n+    op(&var code, Op.LoadRef, 9, 6, -1, 0)\\n+    op(&var code, Op.StoreRef, -1, 5, 9, 0)\\n+    op(&var code, Op.LoadRef, 11, 4, -1, 0)\\n+    op(&var code, Op.StoreRef, -1, 3, 11, 0)\\n+    op(&var code, Op.RefField, 14, 3, -1, 0)\\n+    op(&var code, Op.Return, -1, -1, -1, 0)\\n+    var funcs = Vector<FuncIR>()\\n+    try! funcs.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: move slots, code: move code))\\n+    Program(references: move refs, enums: Vector<EnumIR>(), records: move records,\\n+        functions: move funcs, main_index: 0)\\n+}\\n+\\n+func rejected(p: &Program, index: Int, kind: Op, dst: Int, a: Int, b: Int, immediate: Int) -> Bool {\\n+    var changed = p.copy()\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    i.immediate = immediate\\n+    changed.functions[0].code.set(index, move i)\\n+    not valid(&changed)\\n+}\\n+\\n+func invalid_address(p: &Program, handle: Int) -> Bool {\\n+    // Deliberately bypass verification to test the VM's whole-range bounds guard.\\n+    var changed = p.copy()\\n+    changed.functions[0].code[3].op = Op.Const\\n+    changed.functions[0].code[3].immediate = handle\\n+    try execute(&changed, 1000) catch { return error.message == \\\"runtime error: invalid reference\\\" }\\n+    false\\n+}\\n+\\n+func main() {\\n+    let p = base_program()\\n+    assert(valid(&p), \\\"nested projection, whole record access and qualification\\\")\\n+    try! execute(&p, 1000)\\n+    assert((try! value_layout(&p.records, ValueType.Record(1))).len() == 3, \\\"flattened outer layout\\\")\\n+    assert((try! record_field_offset(&p.records, 1, 1)) == 1, \\\"nested record field offset\\\")\\n+    assert(rejected(&p, 5, Op.RefField, 5, 3, -1, -1), \\\"negative field index\\\")\\n+    assert(rejected(&p, 5, Op.RefField, 5, 3, -1, 2), \\\"high field index\\\")\\n+    assert(rejected(&p, 5, Op.RefField, 7, 3, -1, 1), \\\"field target must match\\\")\\n+    assert(rejected(&p, 5, Op.RefField, 5, 4, -1, 1), \\\"cannot strengthen field mutability\\\")\\n+    assert(rejected(&p, 5, Op.RefField, 5, 0, -1, 1), \\\"parent must be reference\\\")\\n+    assert(rejected(&p, 5, Op.RefField, 5, 7, -1, 1), \\\"parent target must be record\\\")\\n+    assert(rejected(&p, 5, Op.RefField, 0, 3, -1, 1), \\\"field result must be reference\\\")\\n+    var weaken = p.copy()\\n+    weaken.functions[0].code[5].dst = 6\\n+    // Verify alone: later code retains structurally valid but uninitialized slots.\\n+    assert(valid(&weaken), \\\"mutable record can project a shared field\\\")\\n+    assert(rejected(&p, 3, Op.Addr, 3, 1, -1, 0), \\\"Addr full target layout required\\\")\\n+    assert(rejected(&p, 3, Op.Addr, 3, 13, -1, 0), \\\"Addr truncated range\\\")\\n+    assert(rejected(&p, 9, Op.LoadRef, 13, 6, -1, 0), \\\"LoadRef truncated range\\\")\\n+    assert(rejected(&p, 9, Op.LoadRef, 0, 6, -1, 0), \\\"LoadRef exact field types\\\")\\n+    assert(rejected(&p, 10, Op.StoreRef, -1, 6, 9, 0), \\\"shared aggregate StoreRef\\\")\\n+    assert(rejected(&p, 10, Op.StoreRef, -1, 5, 14, 0), \\\"StoreRef truncated range\\\")\\n+    assert(rejected(&p, 10, Op.StoreRef, -1, 5, 0, 0), \\\"StoreRef exact field types\\\")\\n+    var bad_record = p.copy()\\n+    bad_record.references[0].target = ValueType.Record(99)\\n+    assert(not valid(&bad_record), \\\"reference record index is checked\\\")\\n+    assert(invalid_address(&p, -1), \\\"negative aggregate handle\\\")\\n+    assert(invalid_address(&p, 14), \\\"valid start with truncated referent\\\")\\n+    assert(invalid_address(&p, Int.max), \\\"overflowing aggregate handle\\\")\\n+    print(\\\"receiver contract passed\\\")\\n+}\\ndiff --git a/prototypes/minivm/tests/reference_contract.saw b/prototypes/minivm/tests/reference_contract.saw\\nindex 31cea401..248d868f 100644\\n--- a/prototypes/minivm/tests/reference_contract.saw\\n+++ b/prototypes/minivm/tests/reference_contract.saw\\n@@ -127,7 +127,7 @@ func main() {\\n     assert(not valid(&void_ref), \\\"no Void reference\\\")\\n     var record_ref = p.copy()\\n     record_ref.references[0].target = ValueType.Record(0)\\n-    assert(not valid(&record_ref), \\\"no whole-record reference\\\")\\n+    assert(not valid(&record_ref), \\\"record reference target index must exist\\\")\\n     var enum_ref = p.copy()\\n     enum_ref.references[3].target = ValueType.Enum(99)\\n     assert(not valid(&enum_ref), \\\"validate enum index in reference target\\\")\\n\",\"id\":\"SL-250\",\"request_id\":\"mv2-SL-250-910e8fa8-a1\",\"title\":\"minivm M7: record references + instance methods (branch commit 910e8fa8)\"}","request_id":"mv2-SL-250-910e8fa8-a1","sequence":481,"timestamp":"1789097305"}
---
