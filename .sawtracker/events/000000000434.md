---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-245.p1 (revision 1): minivm M2: scalar value records (branch commit e07b57a7)","created":"1789088118","id":"c1"}],"created":"1789086327","description":"SL-242 epic child, landing patch 3/14. Contract: prototypes/minivm/M2_RECORDS.md (256-word / 128-nesting layout limits). Patch = branch commit e07b57a7.","id":"SL-245","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":434,"stage":"queued","status":"open","title":"minivm M2: scalar value records — named structs, fields, copying, flattened call layouts","updated":"1789088118"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086327","description":"SL-242 epic child, landing patch 3/14. Contract: prototypes/minivm/M2_RECORDS.md (256-word / 128-nesting layout limits). Patch = branch commit e07b57a7.","id":"SL-245","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":407,"stage":"queued","status":"open","title":"minivm M2: scalar value records — named structs, fields, copying, flattened call layouts","updated":"1789086327"},"id":"SL-245","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 9cfb92da..472b74d0 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -4,10 +4,11 @@ Scope: the source to admit is `selfhost/lexer/src/lib.saw` (1,215 lines at the\\n `5af086c4` baseline). This inventory is intentionally narrower than general Saw\\n support. A feature is included only when that file uses it, or when it is an\\n unavoidable implementation dependency of one of the public std operations it\\n-calls. The existing mini-VM accepts only scalar `Int32`/`Bool` programs; its\\n-README explicitly excludes strings, collections, references, generics, and\\n-methods. The items below are therefore outstanding unless a milestone says they\\n-are already exercised by the current 43-case baseline.\\n+calls. This inventory was taken at the 43-case Int32/Bool baseline. M1 now supplies\\n+the numeric foundation (91 total cases); M2 is adding scalar value records.\\n+Strings, collections, references, generics and methods remain outstanding.\\n+The dependency groups below are an inventory, not the implementation commit order;\\n+the numbered milestone designs define each isolated slice.\\n \\n ## Exact source surface used by the lexer\\n \\n@@ -158,12 +159,12 @@ compile their full implementations.\\n ## Dependency order and isolated gates\\n \\n Each gate should add a tiny source fixture and assert both VM behavior and a\\n-precise rejection. Keep the existing 43 tests green at every gate.\\n+precise rejection. Keep all earlier milestone tests green at every gate.\\n \\n 1. **Wide scalar foundation.** Add source `Int`, unsigned fixed-width values,\\n    `Byte`, the listed operators/casts, `from(truncating:)`, and `.max`. Test\\n    `Int` values beyond `Int32`, byte 255 round-trip, the three maxima widened to\\n-   `UInt`, signed `-1`, short-circuit suppression of a trapping RHS, and reject\\n+   `UInt`, signed `-1`, short-circuit suppression of a trapping RHS (a subsequent frontend slice), and reject\\n    unsupported casts/overflow.\\n 2. **Strings and static constants.** Add module `static Int` constants, owned\\n    strings, literals, equality, `len`, `byte_at`, `substring`, `is_empty`, and\\ndiff --git a/prototypes/minivm/M2_RECORDS.md b/prototypes/minivm/M2_RECORDS.md\\nindex ddb8714c..a81d3c7e 100644\\n--- a/prototypes/minivm/M2_RECORDS.md\\n+++ b/prototypes/minivm/M2_RECORDS.md\\n@@ -1,6 +1,6 @@\\n # M2: value records over scalar fields\\n \\n-Status: design, to implement after M1 passes. This is the struct subsection of\\n+Status: implemented after numeric milestone ecc8d515; validation in README.md. This is the struct subsection of\\n the lexer dependency checklist. Strings, owning containers, methods, and references\\n will build on this layout in later isolated milestones.\\n \\n@@ -19,7 +19,8 @@ printing, references, custom initializers, or user-defined copy policies yet.\\n \\n The first slice requires at least one field and caps a flattened value at 256\\n words with a located diagnostic. Recursive by-value records are rejected, including\\n-indirect cycles; forward type references are allowed. Numeric casts and literal\\n+indirect cycles; forward type references are allowed. Nesting is limited to 128\\n+records to bound compiler recursion. Numeric casts and literal\\n adoption apply at scalar field initialization/assignment exactly as at local\\n transfers. Built-in type names, duplicate records, duplicate fields, missing or\\n extra constructor labels, and wrong field types get explicit diagnostics.\\n@@ -32,10 +33,10 @@ or provisional garbage collector, and matches scalar record value semantics.\\n Later String/Vector handles can occupy scalar words with separate ownership rules;\\n their retention/destruction is not silently supplied by this scalar-only milestone.\\n \\n-Planned model additions:\\n+Shared model contract:\\n \\n * `ValueType.Record(index: Int)` identifies a nominal declaration in Program.\\n-* `RecordIR` carries name, ordered field names and ValueTypes.\\n+* `RecordIR` carries name, ordered `field_names` and `field_types`, and line/col.\\n * `Program.records` stores the declarations. Numeric helpers return false/zero\\n   for Record. All actual FuncIR.slots remain flattened scalar types.\\n * FuncIR.params/result retain source types. Parameter storage is the concatenation\\n@@ -66,6 +67,13 @@ destination ranges; Returns validate complete source ranges. VM call arguments\\n and return words are snapshots, not borrowed slices into another frame. Even a\\n one-field record uses an LLVM aggregate return type consistently at both ends.\\n \\n+Public helpers are `scalar_type(t)`,\\n+`value_layout(records: &Vector<RecordIR>, t: ValueType) -> Result<Vector<ValueType>, ProtoError>`,\\n+`validate_records(records: &Vector<RecordIR>) -> Result<Void, ProtoError>`, and\\n+`record_field_offset(records: &Vector<RecordIR>, record_index: Int, field_index: Int) -> Result<Int, ProtoError>`.\\n+Void has an empty result layout but is prohibited in fields and parameters.\\n+Record constructors use Saw's `Pair(first: 1, second: 2)` syntax.\\n+\\n ## Tests and division of work\\n \\n Primary implements/reviews model/layout/verifier and defines all interfaces before\\ndiff --git a/prototypes/minivm/M3_SCALAR_CONTROL.md b/prototypes/minivm/M3_SCALAR_CONTROL.md\\nnew file mode 100644\\nindex 00000000..649b470d\\n--- /dev/null\\n+++ b/prototypes/minivm/M3_SCALAR_CONTROL.md\\n@@ -0,0 +1,65 @@\\n+# M3: scalar frontend support used by lexer helpers\\n+\\n+Status: design, after M2. This slice needs no new VM or LLVM opcodes. It admits\\n+short-circuit Boolean expressions, compound arithmetic assignment, and immutable\\n+module scalar constants. Tail expressions and value-producing if/match remain\\n+separate subsequent frontend slices; do not silently broaden this contract.\\n+\\n+## Source behavior\\n+\\n+* `&&` and `||` accept Bool operands and produce Bool. Both are left associative;\\n+  `&&` binds tighter than `||`, and both bind less tightly than existing bitwise\\n+  and comparison operators. Evaluate the left operand exactly once; evaluate the\\n+  right operand only when needed. A skipped runtime trap must stay skipped in\\n+  both VM execution and optimized native code. Even skipped source is typechecked.\\n+* `+=`, `-=`, `*=`, `/=`, `%=` on mutable scalar locals and scalar fields use\\n+  M1 checked arithmetic. The destination place is resolved once, the RHS evaluated\\n+  once, and assignment retains the destination type. A literal may adopt that\\n+  type; a typed expression must match it for arithmetic (no mixed-width operator).\\n+  Immutable roots, records, Byte and Bool reject arithmetic mutation. References\\n+  and indexed places are not admitted by this milestone.\\n+* Module declarations `static NAME: IntegerOrBoolType = literal` are immutable constants.\\n+  Allow signed/unsigned integer literals, unary-negative literals, Bool literals,\\n+  and numeric type `.min`/`.max`; no arbitrary expression evaluator is needed for\\n+  the lexer's B_* declarations. Validate type/range using the same M1 transfer\\n+  rules. No dynamic initialization, global mutable storage or record constants.\\n+  Constants may be referenced from any function irrespective of declaration order.\\n+  Reject duplicate module names and assignment to a constant. Local bindings may\\n+  shadow constants. Accept `public static` syntactically; modules remain later.\\n+\\n+## Lowering and ownership\\n+\\n+Keep constant metadata in the frontend, emit a fresh typed Const on each read,\\n+and preserve the existing Program schema. Reject unsupported static initializers\\n+with a located diagnostic rather than executing arbitrary code during compilation.\\n+Do not let constant collection emit instructions into a function's code or retain\\n+slot indices from a temporary context.\\n+\\n+Short-circuit lowering uses a fresh Bool result slot and existing Branch/Jump/Copy\\n+instructions. Every reachable path defines the result before the continuation.\\n+RHS instructions occupy only the conditional branch, with all branch targets patched\\n+to real instructions by the existing function finalization logic. Preserve parser\\n+nesting limits and newline behavior. Never lower &&/|| as eager bit operations.\\n+\\n+For compound assignment, reuse the place resolver and scalar binary checks.\\n+The current scalar-record subset has no reference side effects, but the design\\n+must leave an explicit point for destination evaluation before the RHS so later\\n+references and indexing will not change evaluation order.\\n+\\n+## Isolated acceptance\\n+\\n+Run both VM and native O0/O2 against explicit outputs:\\n+\\n+1. Every Boolean truth-table row, mixed &&/|| precedence, and nested parentheses.\\n+2. RHS functions print markers to show short-circuit skipping and left-to-right\\n+   execution; false && divide-by-zero and true || divide-by-zero must succeed.\\n+3. Mutable locals and nested fields for all five compound operators; a narrow\\n+   overflow and division by zero must report the existing runtime error.\\n+4. Static Int/UInt64/Bool reads, forward declaration use, local shadowing,\\n+   and numeric limits beyond Int32. Byte statics are outside this slice.\\n+5. Located rejections: non-Bool logical operand even on skipped RHS, immutable\\n+   compound destination, mixed typed arithmetic, duplicate/assigned static, and\\n+   nonconstant initializer.\\n+\\n+Primary designs and reviews, Sol implements frontend and independent fixtures.\\n+Retain M1/M2 gates; commit only reviewed passing integration.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 783b272e..f16d817a 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -45,7 +45,10 @@ func main() {\\n Single-file programs support signed and unsigned 8/16/32/64-bit integers,\\n `Int`/`UInt` (64-bit), `Byte`, `Bool`, functions, positional calls,\\n recursion, `let`/`var`, assignment, lexical scopes, statement `if`/`else` and\\n-`while`, explicit returns, and scalar printing. Arithmetic is checked; signed\\n+`while`, explicit returns, and scalar printing. Named structs can contain scalars or nested structs, with\\n+field access, mutable fields, value copying, and function parameters/results.\\n+Record layouts are limited to 256 words and 128 nesting levels; see\\n+[M2_RECORDS.md](M2_RECORDS.md). Arithmetic is checked; signed\\n division truncates toward zero. Overflow and division by zero print an error\\n and exit with status 1. Boolean conditions require `Bool`.\\n \\n@@ -72,11 +75,12 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The numeric milestone passes\\n-all 91 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The numeric and record milestones pass\\n+all 115 cases, including native execution at both `-O0` and `-O2`.\\n \\n-The independent representation checks live in `tests/numeric_contract.saw`;\\n-build and run them with the Python compiler.\\n+Independent representation checks live in `tests/numeric_contract.saw` and\\n+`tests/record_contract.saw`; build and run them with the Python compiler.\\n+Use `--section numbers` or `--section records` for an isolated integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\n schema and semantics. [LEXER_DEPENDENCIES.md](LEXER_DEPENDENCIES.md) tracks the\\ndiff --git a/prototypes/minivm/examples/records/basic_reordered.saw b/prototypes/minivm/examples/records/basic_reordered.saw\\nnew file mode 100644\\nindex 00000000..4854a749\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/basic_reordered.saw\\n@@ -0,0 +1,10 @@\\n+public struct Pair {\\n+    first: Int32\\n+    second: Bool\\n+}\\n+\\n+public func main() {\\n+    let pair = Pair(second: true, first: 7)\\n+    print(pair.first)\\n+    print(pair.second)\\n+}\\ndiff --git a/prototypes/minivm/examples/records/call_return_mixed.saw b/prototypes/minivm/examples/records/call_return_mixed.saw\\nnew file mode 100644\\nindex 00000000..60d81461\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/call_return_mixed.saw\\n@@ -0,0 +1,18 @@\\n+struct Pair {\\n+    small: Int8\\n+    large: UInt64\\n+}\\n+\\n+func relay(prefix: Int16, pair: Pair, suffix: UInt8) -> Pair {\\n+    print(prefix)\\n+    print(pair.small)\\n+    print(pair.large)\\n+    print(suffix)\\n+    return pair\\n+}\\n+\\n+func main() {\\n+    let result = relay(300, Pair(large: UInt64.max, small: -7), 250)\\n+    print(result.small)\\n+    print(result.large)\\n+}\\ndiff --git a/prototypes/minivm/examples/records/copy_and_assignment.saw b/prototypes/minivm/examples/records/copy_and_assignment.saw\\nnew file mode 100644\\nindex 00000000..a295630e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/copy_and_assignment.saw\\n@@ -0,0 +1,19 @@\\n+struct Pair {\\n+    x: Int\\n+    y: Int\\n+}\\n+\\n+func main() {\\n+    var a = Pair(x: 1, y: 2)\\n+    var b = a\\n+    b.x = 9\\n+    print(a.x)\\n+    print(b.x)\\n+    b = a\\n+    a.y = 8\\n+    print(a.y)\\n+    print(b.y)\\n+    b = b\\n+    print(b.x)\\n+    print(b.y)\\n+}\\ndiff --git a/prototypes/minivm/examples/records/evaluation_order.saw b/prototypes/minivm/examples/records/evaluation_order.saw\\nnew file mode 100644\\nindex 00000000..b8220a56\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/evaluation_order.saw\\n@@ -0,0 +1,15 @@\\n+struct Pair {\\n+    first: Int\\n+    second: Int\\n+}\\n+\\n+func mark(x: Int) -> Int {\\n+    print(x)\\n+    return x\\n+}\\n+\\n+func main() {\\n+    let pair = Pair(second: mark(2), first: mark(1))\\n+    print(pair.first)\\n+    print(pair.second)\\n+}\\ndiff --git a/prototypes/minivm/examples/records/forward_and_numeric.saw b/prototypes/minivm/examples/records/forward_and_numeric.saw\\nnew file mode 100644\\nindex 00000000..3f239b7d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/forward_and_numeric.saw\\n@@ -0,0 +1,16 @@\\n+struct Outer {\\n+    inner: Inner\\n+    count: UInt16\\n+}\\n+\\n+struct Inner {\\n+    signed: Int8\\n+    wide: UInt64\\n+}\\n+\\n+func main() {\\n+    let value = Outer(count: 65535, inner: Inner(wide: 255 as UInt64, signed: -128))\\n+    print(value.inner.signed)\\n+    print(value.inner.wide)\\n+    print(value.count)\\n+}\\ndiff --git a/prototypes/minivm/examples/records/nested_mutation.saw b/prototypes/minivm/examples/records/nested_mutation.saw\\nnew file mode 100644\\nindex 00000000..d1a6d5fd\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/nested_mutation.saw\\n@@ -0,0 +1,20 @@\\n+struct Pair {\\n+    left: Int16\\n+    right: Int16\\n+}\\n+\\n+struct Outer {\\n+    tag: UInt8\\n+    inner: Pair\\n+}\\n+\\n+func main() {\\n+    var value = Outer(tag: 4, inner: Pair(left: 5, right: 6))\\n+    value.inner.right = 9\\n+    print(value.tag)\\n+    print(value.inner.left)\\n+    print(value.inner.right)\\n+    value.inner = Pair(right: 12, left: 11)\\n+    print(value.inner.left)\\n+    print(value.inner.right)\\n+}\\ndiff --git a/prototypes/minivm/examples/records/one_word_result.saw b/prototypes/minivm/examples/records/one_word_result.saw\\nnew file mode 100644\\nindex 00000000..13b263ef\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/one_word_result.saw\\n@@ -0,0 +1,12 @@\\n+struct Box {\\n+    value: UInt64\\n+}\\n+\\n+func make() -> Box {\\n+    return Box(value: UInt64.max)\\n+}\\n+\\n+func main() {\\n+    let box = make()\\n+    print(box.value)\\n+}\\ndiff --git a/prototypes/minivm/examples/records/reject_builtin_name.saw b/prototypes/minivm/examples/records/reject_builtin_name.saw\\nnew file mode 100644\\nindex 00000000..7bd26cc7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_builtin_name.saw\\n@@ -0,0 +1,2 @@\\n+struct Int { value: Bool }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/records/reject_direct_cycle.saw b/prototypes/minivm/examples/records/reject_direct_cycle.saw\\nnew file mode 100644\\nindex 00000000..a3aae745\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_direct_cycle.saw\\n@@ -0,0 +1,2 @@\\n+struct Node { next: Node }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/records/reject_duplicate_field.saw b/prototypes/minivm/examples/records/reject_duplicate_field.saw\\nnew file mode 100644\\nindex 00000000..cc58fa60\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_duplicate_field.saw\\n@@ -0,0 +1,5 @@\\n+struct Box {\\n+    value: Int\\n+    value: Bool\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/records/reject_duplicate_label.saw b/prototypes/minivm/examples/records/reject_duplicate_label.saw\\nnew file mode 100644\\nindex 00000000..559b6205\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_duplicate_label.saw\\n@@ -0,0 +1,2 @@\\n+struct Pair { first: Int, second: Int }\\n+func main() { let pair = Pair(first: 1, second: 2, first: 3) }\\ndiff --git a/prototypes/minivm/examples/records/reject_duplicate_record.saw b/prototypes/minivm/examples/records/reject_duplicate_record.saw\\nnew file mode 100644\\nindex 00000000..f5988e9d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_duplicate_record.saw\\n@@ -0,0 +1,3 @@\\n+struct Box { first: Int }\\n+struct Box { second: Int }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/records/reject_empty_record.saw b/prototypes/minivm/examples/records/reject_empty_record.saw\\nnew file mode 100644\\nindex 00000000..37c58c76\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_empty_record.saw\\n@@ -0,0 +1,2 @@\\n+struct Empty {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/records/reject_immutable_field.saw b/prototypes/minivm/examples/records/reject_immutable_field.saw\\nnew file mode 100644\\nindex 00000000..14e912e6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_immutable_field.saw\\n@@ -0,0 +1,7 @@\\n+struct Inner { value: Int }\\n+struct Outer { inner: Inner }\\n+\\n+func main() {\\n+    let outer = Outer(inner: Inner(value: 1))\\n+    outer.inner.value = 2\\n+}\\ndiff --git a/prototypes/minivm/examples/records/reject_indirect_cycle.saw b/prototypes/minivm/examples/records/reject_indirect_cycle.saw\\nnew file mode 100644\\nindex 00000000..ac9ad85c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_indirect_cycle.saw\\n@@ -0,0 +1,4 @@\\n+struct First { second: Second }\\n+struct Second { third: Third }\\n+struct Third { first: First }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/records/reject_missing_label.saw b/prototypes/minivm/examples/records/reject_missing_label.saw\\nnew file mode 100644\\nindex 00000000..3e02de7d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_missing_label.saw\\n@@ -0,0 +1,2 @@\\n+struct Pair { first: Int, second: Int }\\n+func main() { let pair = Pair(first: 1) }\\ndiff --git a/prototypes/minivm/examples/records/reject_nominal.saw b/prototypes/minivm/examples/records/reject_nominal.saw\\nnew file mode 100644\\nindex 00000000..d8255ff2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_nominal.saw\\n@@ -0,0 +1,8 @@\\n+struct First { value: Int }\\n+struct Second { value: Int }\\n+\\n+func main() {\\n+    let first = First(value: 1)\\n+    let second: Second = first\\n+    print(second.value)\\n+}\\ndiff --git a/prototypes/minivm/examples/records/reject_parameter_field.saw b/prototypes/minivm/examples/records/reject_parameter_field.saw\\nnew file mode 100644\\nindex 00000000..2c819c49\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_parameter_field.saw\\n@@ -0,0 +1,9 @@\\n+struct Box { value: Int }\\n+\\n+func alter(box: Box) {\\n+    box.value = 2\\n+}\\n+\\n+func main() {\\n+    alter(Box(value: 1))\\n+}\\ndiff --git a/prototypes/minivm/examples/records/reject_record_equality.saw b/prototypes/minivm/examples/records/reject_record_equality.saw\\nnew file mode 100644\\nindex 00000000..40f108ac\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_record_equality.saw\\n@@ -0,0 +1,5 @@\\n+struct Pair { x: Int }\\n+func main() {\\n+    let p = Pair(x: 1)\\n+    print(p == p)\\n+}\\ndiff --git a/prototypes/minivm/examples/records/reject_record_print.saw b/prototypes/minivm/examples/records/reject_record_print.saw\\nnew file mode 100644\\nindex 00000000..43a24a71\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_record_print.saw\\n@@ -0,0 +1,2 @@\\n+struct Pair { x: Int }\\n+func main() { print(Pair(x: 1)) }\\ndiff --git a/prototypes/minivm/examples/records/reject_unknown_label.saw b/prototypes/minivm/examples/records/reject_unknown_label.saw\\nnew file mode 100644\\nindex 00000000..0606961d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_unknown_label.saw\\n@@ -0,0 +1,2 @@\\n+struct Pair { first: Int, second: Int }\\n+func main() { let pair = Pair(first: 1, second: 2, third: 3) }\\ndiff --git a/prototypes/minivm/examples/records/reject_width_257.saw b/prototypes/minivm/examples/records/reject_width_257.saw\\nnew file mode 100644\\nindex 00000000..4d969a80\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_width_257.saw\\n@@ -0,0 +1,11 @@\\n+struct R0 { value: Int }\\n+struct R1 { a: R0, b: R0 }\\n+struct R2 { a: R1, b: R1 }\\n+struct R3 { a: R2, b: R2 }\\n+struct R4 { a: R3, b: R3 }\\n+struct R5 { a: R4, b: R4 }\\n+struct R6 { a: R5, b: R5 }\\n+struct R7 { a: R6, b: R6 }\\n+struct R8 { a: R7, b: R7 }\\n+struct TooWide { tree: R8, extra: Int }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/records/reject_wrong_field_type.saw b/prototypes/minivm/examples/records/reject_wrong_field_type.saw\\nnew file mode 100644\\nindex 00000000..83ef1d63\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/reject_wrong_field_type.saw\\n@@ -0,0 +1,2 @@\\n+struct Box { value: Int }\\n+func main() { let box = Box(value: true) }\\ndiff --git a/prototypes/minivm/examples/records/width_256.saw b/prototypes/minivm/examples/records/width_256.saw\\nnew file mode 100644\\nindex 00000000..2adc24e7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/records/width_256.saw\\n@@ -0,0 +1,30 @@\\n+struct R0 { value: Int }\\n+struct R1 { a: R0, b: R0 }\\n+struct R2 { a: R1, b: R1 }\\n+struct R3 { a: R2, b: R2 }\\n+struct R4 { a: R3, b: R3 }\\n+struct R5 { a: R4, b: R4 }\\n+struct R6 { a: R5, b: R5 }\\n+struct R7 { a: R6, b: R6 }\\n+struct R8 { a: R7, b: R7 }\\n+\\n+func identity(value: R8) -> R8 { return value }\\n+\\n+func main() {\\n+    let r0 = R0(value: 17)\\n+    let r1 = R1(a: r0, b: r0)\\n+    let r2 = R2(a: r1, b: r1)\\n+    let r3 = R3(a: r2, b: r2)\\n+    let r4 = R4(a: r3, b: r3)\\n+    let r5 = R5(a: r4, b: r4)\\n+    let r6 = R6(a: r5, b: r5)\\n+    let r7 = R7(a: r6, b: r6)\\n+    var r8 = R8(a: r7, b: r7)\\n+    r8.b.b.b.b.b.b.b.b.value = 255\\n+    let snapshot = identity(r8)\\n+    r8.a.a.a.a.a.a.a.a.value = 0\\n+    print(r8.a.a.a.a.a.a.a.a.value)\\n+    print(r8.b.b.b.b.b.b.b.b.value)\\n+    print(snapshot.a.a.a.a.a.a.a.a.value)\\n+    print(snapshot.b.b.b.b.b.b.b.b.value)\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 076adc9a..440ac841 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -27,6 +27,12 @@ struct Binding {\\n     depth: Int\\n }\\n \\n+struct Place {\\n+    ty: ValueType\\n+    slot: Int\\n+    mutable: Bool\\n+}\\n+\\n struct ExprValue {\\n     ty: ValueType\\n     slot: Int\\n@@ -45,7 +51,8 @@ func type_name(t: ValueType) -> String {\\n     match t {\\n         case I8 -> \\\"Int8\\\", case I16 -> \\\"Int16\\\", case I32 -> \\\"Int32\\\", case I64 -> \\\"Int\\\",\\n         case U8 -> \\\"UInt8\\\", case U16 -> \\\"UInt16\\\", case U32 -> \\\"UInt32\\\", case U64 -> \\\"UInt\\\",\\n-        case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case Void -> \\\"Void\\\"\\n+        case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case Void -> \\\"Void\\\",\\n+        case Record(_) -> \\\"record\\\"\\n     }\\n }\\n \\n@@ -79,6 +86,7 @@ func literal_magnitude(value: String) -> UInt? {\\n \\n struct Compiler {\\n     tokens: Vector<Token>\\n+    records: Vector<RecordIR>\\n     signatures: Vector<Signature>\\n     pos: Int\\n     depth: Int\\n@@ -131,9 +139,66 @@ extension Compiler {\\n         let t = try self.expect(TokenKind.Ident, \\\"expected type\\\")\\n         if t.value == \\\"Bool\\\" { return ValueType.Bool }\\n         if let ty = named_integer_type(t.value) { return ty }\\n+        let ri = self.record_index(t.value)\\n+        if ri >= 0 { return ValueType.Record(ri) }\\n         problem(\\\"unsupported type `{t.value}`\\\", t.line, t.col)\\n     }\\n \\n+    func record_index(&self, name: String) -> Int {\\n+        var i = 0\\n+        while i < self.records.len() {\\n+            if self.records[i].name == name { return i }\\n+            i += 1\\n+        }\\n+        -1\\n+    }\\n+\\n+    func display_type(&self, ty: ValueType) -> String {\\n+        match ty {\\n+            case Record(index) -> {\\n+                if index >= 0 && index < self.records.len() { return self.records[index].name }\\n+                \\\"record\\\"\\n+            },\\n+            case _ -> type_name(ty)\\n+        }\\n+    }\\n+\\n+    func reserved_type_name(&self, name: String) -> Bool {\\n+        if name == \\\"Bool\\\" || name == \\\"Void\\\" { return true }\\n+        if let _ = named_integer_type(name) { return true }\\n+        false\\n+    }\\n+\\n+    func collect_record_names(&var self) -> Result<Void, ProtoError> {\\n+        self.pos = 0\\n+        self.skip_newlines()\\n+        while not self.at(TokenKind.Eof) {\\n+            if self.at(TokenKind.Public) { let _ = self.take() }\\n+            if self.at(TokenKind.Struct) {\\n+                let _ = self.take()\\n+                let name = try self.expect(TokenKind.Ident, \\\"expected record name\\\")\\n+                if self.reserved_type_name(name.value) || name.value == \\\"print\\\" { return problem(\\\"record name `{name.value}` is reserved\\\", name.line, name.col) }\\n+                if self.record_index(name.value) >= 0 { return problem(\\\"duplicate record `{name.value}`\\\", name.line, name.col) }\\n+                try! self.records.push(RecordIR(name: name.value, field_names: Vector<String>(),\\n+                                                field_types: Vector<ValueType>(), line: name.line, col: name.col))\\n+            } else if not self.at(TokenKind.Func) {\\n+                return self.fail(\\\"only function and record declarations are supported at module scope\\\")\\n+            }\\n+            while not self.at(TokenKind.LBrace) && not self.at(TokenKind.Eof) { let _ = self.take() }\\n+            if self.at(TokenKind.Eof) { return self.fail(\\\"expected declaration body\\\") }\\n+            let _ = self.take()\\n+            var braces = 1\\n+            while braces > 0 && not self.at(TokenKind.Eof) {\\n+                if self.at(TokenKind.LBrace) { braces += 1 }\\n+                if self.at(TokenKind.RBrace) { braces -= 1 }\\n+                let _ = self.take()\\n+            }\\n+            if braces != 0 { return self.fail(\\\"unterminated declaration body\\\") }\\n+            self.skip_newlines()\\n+        }\\n+        return\\n+    }\\n+\\n     func signature_index(&self, name: String) -> Int {\\n         var i = 0\\n         while i < self.signatures.len() {\\n@@ -147,7 +212,13 @@ extension Compiler {\\n         self.pos = 0\\n         self.skip_newlines()\\n         while not self.at(TokenKind.Eof) {\\n-            let ft = try self.expect(TokenKind.Func, \\\"only function declarations are supported at module scope\\\")\\n+            if self.at(TokenKind.Public) { let _ = self.take() }\\n+            if self.at(TokenKind.Struct) {\\n+                try self.scan_record()\\n+                self.skip_newlines()\\n+                continue\\n+            }\\n+            let ft = try self.expect(TokenKind.Func, \\\"only function and record declarations are supported at module scope\\\")\\n             let name = try self.expect(TokenKind.Ident, \\\"expected function name\\\")\\n             if name.value == \\\"print\\\" {\\n                 return problem(\\\"`print` is reserved by the prototype\\\", name.line, name.col)\\n@@ -155,6 +226,9 @@ extension Compiler {\\n             if self.signature_index(name.value) >= 0 {\\n                 return problem(\\\"duplicate function `{name.value}`\\\", name.line, name.col)\\n             }\\n+            if self.record_index(name.value) >= 0 {\\n+                return problem(\\\"function `{name.value}` conflicts with a record name\\\", name.line, name.col)\\n+            }\\n             let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after function name\\\")\\n             var pnames = Vector<String>()\\n             var ptypes = Vector<ValueType>()\\n@@ -225,12 +299,79 @@ extension Compiler {\\n         return\\n     }\\n \\n+    func scan_record(&var self) -> Result<Void, ProtoError> {\\n+        let _ = self.take()\\n+        let name = try self.expect(TokenKind.Ident, \\\"expected record name\\\")\\n+        let ri = self.record_index(name.value)\\n+        if ri < 0 { return problem(\\\"unknown record declaration `{name.value}`\\\", name.line, name.col) }\\n+        let _ = try self.expect(TokenKind.LBrace, \\\"expected record body\\\")\\n+        self.skip_newlines()\\n+        var names = Vector<String>()\\n+        var types = Vector<ValueType>()\\n+        while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n+            if self.at(TokenKind.Public) { let _ = self.take() }\\n+            let field = try self.expect(TokenKind.Ident, \\\"expected record field name\\\")\\n+            var i = 0\\n+            while i < names.len() {\\n+                if names[i] == field.value { return problem(\\\"duplicate record field `{field.value}`\\\", field.line, field.col) }\\n+                i += 1\\n+            }\\n+            let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after record field\\\")\\n+            let ty = try self.parse_type()\\n+            if ty == ValueType.Void { return problem(\\\"record field cannot have Void type\\\", field.line, field.col) }\\n+            try! names.push(field.value)\\n+            try! types.push(ty)\\n+            if self.at(TokenKind.Comma) {\\n+                let _ = self.take()\\n+                self.skip_newlines()\\n+            } else if self.at(TokenKind.Newline) { self.skip_newlines() }\\n+            else if not self.at(TokenKind.RBrace) { return self.fail(\\\"expected comma or newline after record field\\\") }\\n+        }\\n+        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace for record\\\")\\n+        if names.len() == 0 { return problem(\\\"record must have at least one field\\\", name.line, name.col) }\\n+        self.records[ri].field_names = move names\\n+        self.records[ri].field_types = move types\\n+        return\\n+    }\\n+\\n     func new_slot(&var self, ty: ValueType) -> Int {\\n         let n = self.slots.len()\\n         try! self.slots.push(ty)\\n         n\\n     }\\n \\n+    func new_value(&var self, ty: ValueType) -> Result<Int, ProtoError> {\\n+        let layout = try value_layout(&self.records, ty)\\n+        let start = self.slots.len()\\n+        var i = 0\\n+        while i < layout.len() {\\n+            try! self.slots.push(layout[i])\\n+            i += 1\\n+        }\\n+        start\\n+    }\\n+\\n+    func emit_copy_value(&var self, dst: Int, src: Int, ty: ValueType,\\n+                         t: &Token) -> Result<Void, ProtoError> {\\n+        if dst == src { return }\\n+        let layout = try value_layout(&self.records, ty)\\n+        var i = 0\\n+        while i < layout.len() {\\n+            let at = self.emit_at(Op.Copy, t.line, t.col)\\n+            self.code[at].dst = dst + i\\n+            self.code[at].a = src + i\\n+            i += 1\\n+        }\\n+        return\\n+    }\\n+\\n+    func snapshot(&var self, value: ExprValue, t: &Token) -> Result<ExprValue, ProtoError> {\\n+        if value.ty == ValueType.Void { return value }\\n+        let dst = try self.new_value(value.ty)\\n+        try self.emit_copy_value(dst, value.slot, value.ty, &t)\\n+        expr_value(value.ty, dst, value.call)\\n+    }\\n+\\n     func emit(&var self, ins: Instruction) -> Int {\\n         let n = self.code.len()\\n         try! self.code.push(move ins)\\n@@ -327,7 +468,7 @@ extension Compiler {\\n             }\\n             return v\\n         }\\n-        if v.literal { return self.adopt_literal(v, target, &t) }\\n+        if v.literal && integer_type(target) { return self.adopt_literal(v, target, &t) }\\n         if integer_widens(v.ty, target) {\\n             let dst = self.new_slot(target)\\n             let at = self.emit_at(Op.Cast, t.line, t.col)\\n@@ -335,10 +476,10 @@ extension Compiler {\\n             self.code[at].a = v.slot\\n             return expr_value(target, dst, false)\\n         }\\n-        problem(\\\"cannot implicitly convert {type_name(v.ty)} to {type_name(target)}\\\", t.line, t.col)\\n+        problem(\\\"cannot implicitly convert {self.display_type(v.ty)} to {self.display_type(target)}\\\", t.line, t.col)\\n     }\\n \\n-    func parse_primary(&var self) -> Result<ExprValue, ProtoError> {\\n+    func parse_atom(&var self) -> Result<ExprValue, ProtoError> {\\n         if self.depth > 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n         if self.at(TokenKind.IntLit) {\\n             let t = self.take()\\n@@ -381,6 +522,8 @@ extension Compiler {\\n         }\\n         if self.at(TokenKind.Ident) {\\n             let name = self.take()\\n+            let ri = self.record_index(name.value)\\n+            if ri >= 0 && self.at(TokenKind.LParen) { return self.parse_record_constructor(name, ri) }\\n             if let ty = named_integer_type(name.value) {\\n                 if self.at(TokenKind.Dot) { return self.parse_integer_member(name, ty) }\\n                 if self.at(TokenKind.LParen) && ty == ValueType.Byte { return self.parse_byte(name) }\\n@@ -388,11 +531,129 @@ extension Compiler {\\n             if self.at(TokenKind.LParen) { return self.parse_call(name) }\\n             let bi = self.binding_index(name.value)\\n             if bi < 0 { return problem(\\\"unknown name `{name.value}`\\\", name.line, name.col) }\\n-            return expr_value(self.bindings[bi].ty, self.bindings[bi].slot, false)\\n+            let value = expr_value(self.bindings[bi].ty, self.bindings[bi].slot, false)\\n+            return self.snapshot(value, &name)\\n         }\\n         self.fail(\\\"expected expression; this syntax is unsupported\\\")\\n     }\\n \\n+    func field_index(&self, record: Int, name: String) -> Int {\\n+        var i = 0\\n+        while i < self.records[record].field_names.len() {\\n+            if self.records[record].field_names[i] == name { return i }\\n+            i += 1\\n+        }\\n+        -1\\n+    }\\n+\\n+    func assignment_ahead(&self) -> Bool {\\n+        if not self.at(TokenKind.Ident) { return false }\\n+        var i = self.pos + 1\\n+        while i + 1 < self.tokens.len() && self.tokens[i].kind == TokenKind.Dot && self.tokens[i + 1].kind == TokenKind.Ident { i += 2 }\\n+        i < self.tokens.len() && self.tokens[i].kind == TokenKind.Assign\\n+    }\\n+\\n+    func parse_place(&var self) -> Result<(Token, Place), ProtoError> {\\n+        let name = try self.expect(TokenKind.Ident, \\\"expected assignment target\\\")\\n+        let bi = self.binding_index(name.value)\\n+        if bi < 0 { return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col) }\\n+        var place = Place(ty: self.bindings[bi].ty, slot: self.bindings[bi].slot,\\n+                          mutable: self.bindings[bi].mutable)\\n+        while self.at(TokenKind.Dot) {\\n+            let _ = self.take()\\n+            let field = try self.expect(TokenKind.Ident, \\\"expected record field after `.`\\\")\\n+            match place.ty {\\n+                case Record(ri) -> {\\n+                    let fi = self.field_index(ri, field.value)\\n+                    if fi < 0 { return problem(\\\"record `{self.records[ri].name}` has no field `{field.value}`\\\", field.line, field.col) }\\n+                    place.slot += try record_field_offset(&self.records, ri, fi)\\n+                    place.ty = self.records[ri].field_types[fi]\\n+                },\\n+                case _ -> { return problem(\\\"field assignment requires a record\\\", field.line, field.col) }\\n+            }\\n+        }\\n+        (name, place)\\n+    }\\n+\\n+    func parse_primary(&var self) -> Result<ExprValue, ProtoError> {\\n+        var value = try self.parse_atom()\\n+        self.expr_space()\\n+        while self.at(TokenKind.Dot) {\\n+            let dot = self.take()\\n+            self.expr_space()\\n+            let field = try self.expect(TokenKind.Ident, \\\"expected record field after `.`\\\")\\n+            match value.ty {\\n+                case Record(ri) -> {\\n+                    let fi = self.field_index(ri, field.value)\\n+                    if fi < 0 { return problem(\\\"record `{self.records[ri].name}` has no field `{field.value}`\\\", field.line, field.col) }\\n+                    let offset = try record_field_offset(&self.records, ri, fi)\\n+                    value = expr_value(self.records[ri].field_types[fi], value.slot + offset, false)\\n+                },\\n+                case _ -> { return problem(\\\"field access requires a record value\\\", dot.line, dot.col) }\\n+            }\\n+            self.expr_space()\\n+        }\\n+        value\\n+    }\\n+\\n+    func parse_record_constructor(&var self, name: Token, ri: Int) -> Result<ExprValue, ProtoError> {\\n+        let _ = self.take()\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        var fields = Vector<Int>()\\n+        var values = Vector<ExprValue>()\\n+        if not self.at(TokenKind.RParen) {\\n+            var more = true\\n+            while more {\\n+                let label = try self.expect(TokenKind.Ident, \\\"expected record field label\\\")\\n+                let fi = self.field_index(ri, label.value)\\n+                if fi < 0 { return problem(\\\"unknown field `{label.value}` for record `{name.value}`\\\", label.line, label.col) }\\n+                var j = 0\\n+                while j < fields.len() {\\n+                    if fields[j] == fi { return problem(\\\"duplicate constructor field `{label.value}`\\\", label.line, label.col) }\\n+                    j += 1\\n+                }\\n+                let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after record field label\\\")\\n+                self.expr_space()\\n+                var value = try self.parse_expr()\\n+                value = try self.transfer(value, self.records[ri].field_types[fi], &label)\\n+                value = try self.snapshot(value, &label)\\n+                try! fields.push(fi)\\n+                try! values.push(move value)\\n+                self.skip_newlines()\\n+                if self.at(TokenKind.Comma) {\\n+                    let _ = self.take()\\n+                    self.skip_newlines()\\n+                    if self.at(TokenKind.RParen) { more = false }\\n+                } else { more = false }\\n+            }\\n+        }\\n+        let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after record constructor\\\")\\n+        self.paren_depth -= 1\\n+        self.depth -= 1\\n+        if fields.len() != self.records[ri].field_names.len() {\\n+            return problem(\\\"missing field in constructor for record `{name.value}`\\\", name.line, name.col)\\n+        }\\n+        let ty = ValueType.Record(ri)\\n+        let dst = try self.new_value(ty)\\n+        var fi = 0\\n+        while fi < self.records[ri].field_names.len() {\\n+            var found = -1\\n+            var j = 0\\n+            while j < fields.len() {\\n+                if fields[j] == fi { found = j }\\n+                j += 1\\n+            }\\n+            if found < 0 { return problem(\\\"missing field `{self.records[ri].field_names[fi]}` in constructor\\\", name.line, name.col) }\\n+            let offset = try record_field_offset(&self.records, ri, fi)\\n+            try self.emit_copy_value(dst + offset, values[found].slot,\\n+                                     self.records[ri].field_types[fi], &name)\\n+            fi += 1\\n+        }\\n+        expr_value(ty, dst, false)\\n+    }\\n+\\n     func parse_byte(&var self, name: Token) -> Result<ExprValue, ProtoError> {\\n         let _ = self.take()\\n         self.depth += 1\\n@@ -463,7 +724,13 @@ extension Compiler {\\n                     return problem(\\\"too many arguments to `{name.value}`\\\", name.line, name.col)\\n                 }\\n                 value = try self.transfer(value, self.signatures[fi].params[ai], &name)\\n-                try! args.push(value.slot)\\n+                value = try self.snapshot(value, &name)\\n+                let layout = try value_layout(&self.records, value.ty)\\n+                var word = 0\\n+                while word < layout.len() {\\n+                    try! args.push(value.slot + word)\\n+                    word += 1\\n+                }\\n                 ai += 1\\n                 self.skip_newlines()\\n                 if self.at(TokenKind.Comma) {\\n@@ -479,7 +746,7 @@ extension Compiler {\\n             return problem(\\\"wrong number of arguments to `{name.value}`\\\", name.line, name.col)\\n         }\\n         let rt = self.signatures[fi].result\\n-        let dst = if rt == ValueType.Void { -1 } else { self.new_slot(rt) }\\n+        let dst = if rt == ValueType.Void { -1 } else { try self.new_value(rt) }\\n         let at = self.emit_at(Op.Call, name.line, name.col)\\n         self.code[at].target = fi\\n         self.code[at].dst = dst\\n@@ -705,7 +972,7 @@ extension Compiler {\\n             let common = try self.common_type(&left, &right, &t)\\n             if left.ty != common { left = try self.adopt_literal(left, common, &t) }\\n             if right.ty != common { right = try self.adopt_literal(right, common, &t) }\\n-            if left.ty == ValueType.Void || left.ty != right.ty { return problem(\\\"equality operands must have the same scalar type\\\", t.line, t.col) }\\n+            if not scalar_type(left.ty) || left.ty != right.ty { return problem(\\\"equality operands must have the same scalar type\\\", t.line, t.col) }\\n             left = try self.binary(left, right, if t.kind == TokenKind.Eq { Op.Eq } else { Op.Ne }, ValueType.Bool, &t)\\n         }\\n         left\\n@@ -796,10 +1063,8 @@ extension Compiler {\\n             if value.ty == ValueType.Void { return problem(\\\"cannot initialize a local with Void\\\", name.line, name.col) }\\n             if let ty = annotated { value = try self.transfer(value, ty, &name) }\\n             else { value = try self.transfer(value, value.ty, &name) }\\n-            let dst = self.new_slot(value.ty)\\n-            let at = self.emit_at(Op.Copy, name.line, name.col)\\n-            self.code[at].dst = dst\\n-            self.code[at].a = value.slot\\n+            let dst = try self.new_value(value.ty)\\n+            try self.emit_copy_value(dst, value.slot, value.ty, &name)\\n             try self.declare(name, value.ty, dst, kw.kind == TokenKind.Var)\\n             return false\\n         }\\n@@ -873,21 +1138,21 @@ extension Compiler {\\n             self.paren_depth -= 1\\n             self.depth -= 1\\n             if value.ty == ValueType.Void { return problem(\\\"cannot print Void\\\", t.line, t.col) }\\n+            if not scalar_type(value.ty) { return problem(\\\"cannot print a record value\\\", t.line, t.col) }\\n             let at = self.emit_at(Op.Print, t.line, t.col)\\n             self.code[at].a = value.slot\\n             return false\\n         }\\n-        if self.at(TokenKind.Ident) && self.pos + 1 < self.tokens.len() && self.tokens[self.pos + 1].kind == TokenKind.Assign {\\n-            let name = self.take()\\n+        if self.assignment_ahead() {\\n+            let parsed = try self.parse_place()\\n+            let name = parsed.0\\n+            let place = parsed.1\\n             let _ = self.take()\\n-            let bi = self.binding_index(name.value)\\n-            if bi < 0 { return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col) }\\n-            if not self.bindings[bi].mutable { return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col) }\\n+            if not place.mutable { return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col) }\\n             var value = try self.parse_expr()\\n-            value = try self.transfer(value, self.bindings[bi].ty, &name)\\n-            let at = self.emit_at(Op.Copy, name.line, name.col)\\n-            self.code[at].dst = self.bindings[bi].slot\\n-            self.code[at].a = value.slot\\n+            value = try self.transfer(value, place.ty, &name)\\n+            value = try self.snapshot(value, &name)\\n+            try self.emit_copy_value(place.slot, value.slot, place.ty, &name)\\n             return false\\n         }\\n         let start = self.token()\\n@@ -909,7 +1174,7 @@ extension Compiler {\\n         self.current_result = self.signatures[index].result\\n         var i = 0\\n         while i < self.signatures[index].params.len() {\\n-            let slot = self.new_slot(self.signatures[index].params[i])\\n+            let slot = try self.new_value(self.signatures[index].params[i])\\n             let fake = Token(kind: TokenKind.Ident, value: self.signatures[index].param_names[i],\\n                              line: self.signatures[index].line, col: self.signatures[index].col, suffix: None)\\n             try self.declare(fake, self.signatures[index].params[i], slot, false)\\n@@ -932,10 +1197,15 @@ extension Compiler {\\n         } else {\\n             // Return-path checking above proves this padding unreachable. It gives\\n             // end-of-block jumps a real target and leaves every emitted block terminated.\\n-            let slot = self.new_slot(self.current_result)\\n-            let c = self.emit_at(Op.Const, self.signatures[index].line, self.signatures[index].col)\\n-            self.code[c].dst = slot\\n-            self.code[c].immediate = 0\\n+            let slot = try self.new_value(self.current_result)\\n+            let layout = try value_layout(&self.records, self.current_result)\\n+            var word = 0\\n+            while word < layout.len() {\\n+                let c = self.emit_at(Op.Const, self.signatures[index].line, self.signatures[index].col)\\n+                self.code[c].dst = slot + word\\n+                self.code[c].immediate = 0\\n+                word += 1\\n+            }\\n             let r = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n             self.code[r].a = slot\\n         }\\n@@ -951,11 +1221,13 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         case Ok(ts) -> move ts,\\n         case Err(e) -> { return problem(e.message, e.line, e.col) }\\n     }\\n-    var compiler = Compiler(tokens: move tokens, signatures: Vector<Signature>(), pos: 0, depth: 0,\\n+    var compiler = Compiler(tokens: move tokens, records: Vector<RecordIR>(), signatures: Vector<Signature>(), pos: 0, depth: 0,\\n                             expr_depth: 0, paren_depth: 0,\\n                             bindings: Vector<Binding>(), slots: Vector<ValueType>(),\\n                             code: Vector<Instruction>(), current_result: ValueType.Void)\\n+    try compiler.collect_record_names()\\n     try compiler.scan_signatures()\\n+    try validate_records(&compiler.records)\\n     var functions = Vector<FuncIR>()\\n     var main_index = -1\\n     var i = 0\\n@@ -965,5 +1237,5 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         try! functions.push(move f)\\n         i += 1\\n     }\\n-    Program(functions: move functions, main_index: main_index)\\n+    Program(records: compiler.records.copy(), functions: move functions, main_index: main_index)\\n }\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex f92b2df0..18ef0ece 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -24,6 +24,32 @@ func block_name(out: &var StringBuilder, instruction_index: Int) {\\n     put_int(&var out, instruction_index)\\n }\\n \\n+func emit_aggregate_type(out: &var StringBuilder, word_count: Int) {\\n+    put(&var out, \\\"\\\\{ \\\")\\n+    var word_index = 0\\n+    while word_index < word_count {\\n+        if word_index > 0 { put(&var out, \\\", \\\") }\\n+        put(&var out, \\\"i64\\\")\\n+        word_index = word_index + 1\\n+    }\\n+    put(&var out, \\\" \\\\}\\\")\\n+}\\n+\\n+func emit_value_type(out: &var StringBuilder, records: &Vector<RecordIR>,\\n+                     t: ValueType) -> Result<Void, ProtoError> {\\n+    if t == ValueType.Void {\\n+        put(&var out, \\\"void\\\")\\n+        return\\n+    }\\n+    if scalar_type(t) {\\n+        put(&var out, \\\"i64\\\")\\n+        return\\n+    }\\n+    let layout = try value_layout(&records, t)\\n+    emit_aggregate_type(&var out, layout.len())\\n+    return\\n+}\\n+\\n func next_block(out: &var StringBuilder, instruction_index: Int) {\\n     put(&var out, \\\"  br label %i\\\")\\n     put_int(&var out, instruction_index + 1)\\n@@ -317,7 +343,7 @@ func emit_division(out: &var StringBuilder, instruction_index: Int,\\n \\n func emit_instruction(out: &var StringBuilder, program: &Program,\\n                       function: &FuncIR, instruction_index: Int,\\n-                      inst: &Instruction) {\\n+                      inst: &Instruction) -> Result<Void, ProtoError> {\\n     block_name(&var out, instruction_index)\\n     put(&var out, \\\":\\\\n\\\")\\n     match inst.op {\\n@@ -420,6 +446,8 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             put(&var out, \\\"\\\\n\\\")\\n         },\\n         case Call -> {\\n+            let callee = program.functions[inst.target].copy()\\n+            let result_layout = try value_layout(&program.records, callee.result)\\n             var argument_index = 0\\n             while argument_index < inst.args.len() {\\n                 put(&var out, \\\"  \\\")\\n@@ -432,7 +460,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 put(&var out, \\\" = \\\")\\n             }\\n             put(&var out, \\\"call \\\")\\n-                        put(&var out, if program.functions[inst.target].result == ValueType.Void { \\\"void\\\" } else { \\\"i64\\\" })\\n+            try emit_value_type(&var out, &program.records, callee.result)\\n             put(&var out, \\\" @f\\\")\\n             put_int(&var out, inst.target)\\n             put(&var out, \\\"(\\\")\\n@@ -444,7 +472,24 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n                 argument_index = argument_index + 1\\n             }\\n             put(&var out, \\\")\\\\n\\\")\\n-            if inst.dst >= 0 { store_temp(&var out, instruction_index, \\\"call\\\", inst.dst) }\\n+            if scalar_type(callee.result) {\\n+                store_temp(&var out, instruction_index, \\\"call\\\", inst.dst)\\n+            } else if callee.result != ValueType.Void {\\n+                var result_index = 0\\n+                while result_index < result_layout.len() {\\n+                    put(&var out, \\\"  \\\")\\n+                    temp_name(&var out, instruction_index, \\\"result{result_index}\\\")\\n+                    put(&var out, \\\" = extractvalue \\\")\\n+                    emit_aggregate_type(&var out, result_layout.len())\\n+                    put(&var out, \\\" \\\")\\n+                    temp_name(&var out, instruction_index, \\\"call\\\")\\n+                    put(&var out, \\\", \\\")\\n+                    put_int(&var out, result_index)\\n+                    put(&var out, \\\"\\\\n\\\")\\n+                    store_temp(&var out, instruction_index, \\\"result{result_index}\\\", inst.dst + result_index)\\n+                    result_index = result_index + 1\\n+                }\\n+            }\\n             next_block(&var out, instruction_index)\\n         },\\n         case Print -> {\\n@@ -476,30 +521,63 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n         case Return -> {\\n             if inst.a < 0 {\\n                 put(&var out, \\\"  ret void\\\\n\\\")\\n-            } else {\\n+            } else if scalar_type(function.result) {\\n                 put(&var out, \\\"  \\\")\\n                 load_slot(&var out, instruction_index, \\\"ret\\\", inst.a)\\n                 put(&var out, \\\"  ret i64 \\\")\\n                 temp_name(&var out, instruction_index, \\\"ret\\\")\\n                 put(&var out, \\\"\\\\n\\\")\\n+            } else {\\n+                let result_layout = try value_layout(&program.records, function.result)\\n+                var result_index = 0\\n+                while result_index < result_layout.len() {\\n+                    put(&var out, \\\"  \\\")\\n+                    load_slot(&var out, instruction_index, \\\"retword{result_index}\\\", inst.a + result_index)\\n+                    put(&var out, \\\"  \\\")\\n+                    temp_name(&var out, instruction_index, \\\"retagg{result_index}\\\")\\n+                    put(&var out, \\\" = insertvalue \\\")\\n+                    emit_aggregate_type(&var out, result_layout.len())\\n+                    put(&var out, \\\" \\\")\\n+                    if result_index == 0 { put(&var out, \\\"undef\\\") }\\n+                    else { temp_name(&var out, instruction_index, \\\"retagg{result_index - 1}\\\") }\\n+                    put(&var out, \\\", i64 \\\")\\n+                    temp_name(&var out, instruction_index, \\\"retword{result_index}\\\")\\n+                    put(&var out, \\\", \\\")\\n+                    put_int(&var out, result_index)\\n+                    put(&var out, \\\"\\\\n\\\")\\n+                    result_index = result_index + 1\\n+                }\\n+                put(&var out, \\\"  ret \\\")\\n+                emit_aggregate_type(&var out, result_layout.len())\\n+                put(&var out, \\\" \\\")\\n+                temp_name(&var out, instruction_index, \\\"retagg{result_layout.len() - 1}\\\")\\n+                put(&var out, \\\"\\\\n\\\")\\n             }\\n         }\\n     }\\n     put(&var out, \\\"\\\\n\\\")\\n+    return\\n }\\n \\n func emit_function(out: &var StringBuilder, program: &Program,\\n-                   function_index: Int, function: &FuncIR) {\\n+                   function_index: Int, function: &FuncIR) -> Result<Void, ProtoError> {\\n     put(&var out, \\\"define \\\")\\n-    put(&var out, if function.result == ValueType.Void { \\\"void\\\" } else { \\\"i64\\\" })\\n+    try emit_value_type(&var out, &program.records, function.result)\\n     put(&var out, \\\" @f\\\")\\n     put_int(&var out, function_index)\\n     put(&var out, \\\"(\\\")\\n     var parameter_index = 0\\n+    var argument_index = 0\\n     while parameter_index < function.params.len() {\\n-        if parameter_index > 0 { put(&var out, \\\", \\\") }\\n-        put(&var out, \\\"i64 %arg\\\")\\n-        put_int(&var out, parameter_index)\\n+        let parameter_layout = try value_layout(&program.records, function.params[parameter_index])\\n+        var parameter_word = 0\\n+        while parameter_word < parameter_layout.len() {\\n+            if argument_index > 0 { put(&var out, \\\", \\\") }\\n+            put(&var out, \\\"i64 %arg\\\")\\n+            put_int(&var out, argument_index)\\n+            argument_index = argument_index + 1\\n+            parameter_word = parameter_word + 1\\n+        }\\n         parameter_index = parameter_index + 1\\n     }\\n     put(&var out, \\\") \\\\{\\\\nentry:\\\\n\\\")\\n@@ -510,20 +588,27 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n         put(&var out, \\\" = alloca i64\\\\n\\\")\\n         slot_index = slot_index + 1\\n     }\\n-    parameter_index = 0\\n-    while parameter_index < function.params.len() {\\n+    var parameter_words = 0\\n+    var source_parameter = 0\\n+    while source_parameter < function.params.len() {\\n+        let parameter_layout = try value_layout(&program.records, function.params[source_parameter])\\n+        parameter_words = parameter_words + parameter_layout.len()\\n+        source_parameter = source_parameter + 1\\n+    }\\n+    argument_index = 0\\n+    while argument_index < parameter_words {\\n         put(&var out, \\\"  store i64 %arg\\\")\\n-        put_int(&var out, parameter_index)\\n+        put_int(&var out, argument_index)\\n         put(&var out, \\\", ptr \\\")\\n-        slot_name(&var out, parameter_index)\\n+        slot_name(&var out, argument_index)\\n         put(&var out, \\\"\\\\n\\\")\\n-        parameter_index = parameter_index + 1\\n+        argument_index = argument_index + 1\\n     }\\n     put(&var out, \\\"  br label %i0\\\\n\\\\n\\\")\\n     var instruction_index = 0\\n     while instruction_index < function.code.len() {\\n-        emit_instruction(&var out, &program, &function, instruction_index,\\n-                         &function.code[instruction_index])\\n+        try emit_instruction(&var out, &program, &function, instruction_index,\\n+                             &function.code[instruction_index])\\n         instruction_index = instruction_index + 1\\n     }\\n     // Keep the text structurally valid even when verification permits an\\n@@ -532,6 +617,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     block_name(&var out, function.code.len())\\n     put(&var out, \\\":\\\\n  unreachable\\\\n\\\")\\n     put(&var out, \\\"\\\\}\\\\n\\\\n\\\")\\n+    return\\n }\\n \\n public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n@@ -558,8 +644,8 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     put(&var out, \\\"define internal void @minivm_shift() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.shift)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     var function_index = 0\\n     while function_index < program.functions.len() {\\n-        emit_function(&var out, &program, function_index,\\n-                      &program.functions[function_index])\\n+        try emit_function(&var out, &program, function_index,\\n+                          &program.functions[function_index])\\n         function_index = function_index + 1\\n     }\\n     put(&var out, \\\"define i32 @main() \\\\{\\\\nentry:\\\\n  call void @f\\\")\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex 0ffea967..0f696db1 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -1,7 +1,7 @@\\n public enum ValueType {\\n     case I8, case I16, case I32, case I64,\\n     case U8, case U16, case U32, case U64, case Byte,\\n-    case Bool, case Void\\n+    case Bool, case Void, case Record(index: Int)\\n }\\n @synthesize\\n extension ValueType: Equatable {}\\n@@ -106,10 +106,21 @@ public struct FuncIR {\\n }\\n \\n public struct Program {\\n+    public records: Vector<RecordIR>\\n     public functions: Vector<FuncIR>\\n     public main_index: Int\\n }\\n \\n+public struct RecordIR {\\n+    public name: String\\n+    public field_names: Vector<String>\\n+    public field_types: Vector<ValueType>\\n+    public line: Int\\n+    public col: Int\\n+}\\n+@synthesize\\n+extension RecordIR: ExplicitCopy {}\\n+\\n @synthesize\\n extension Instruction: ExplicitCopy {}\\n @synthesize\\n@@ -136,3 +147,95 @@ public func instruction(op: Op) -> Instruction {\\n public func problem(message: String, line: Int, col: Int) -> ProtoError {\\n     ProtoError(message: message, line: line, col: col)\\n }\\n+\\n+public func scalar_type(t: ValueType) -> Bool {\\n+    integer_type(t) || t == ValueType.Bool\\n+}\\n+\\n+func append_layout(records: &Vector<RecordIR>, t: ValueType,\\n+                   path: &var Vector<Int>, words: &var Vector<ValueType>) -> Result<Void, ProtoError> {\\n+    if scalar_type(t) {\\n+        if words.len() >= 256 { return problem(\\\"record layout exceeds 256 words\\\", 0, 0) }\\n+        try! words.push(t)\\n+        return\\n+    }\\n+    match t {\\n+        case Record(index) -> {\\n+            if index < 0 || index >= records.len() { return problem(\\\"invalid record type index\\\", 0, 0) }\\n+            let record = records[index].copy()\\n+            if record.field_types.len() == 0 || record.field_names.len() != record.field_types.len() {\\n+                return problem(\\\"record must have matching nonempty fields\\\", record.line, record.col)\\n+            }\\n+            var n = 0\\n+            while n < path.len() {\\n+                if path[n] == index { return problem(\\\"recursive by-value record\\\", record.line, record.col) }\\n+                n += 1\\n+            }\\n+            if path.len() >= 128 { return problem(\\\"record nesting exceeds 128 levels\\\", record.line, record.col) }\\n+            try! path.push(index)\\n+            n = 0\\n+            while n < record.field_types.len() {\\n+                try append_layout(&records, record.field_types[n], &var path, &var words) catch {\\n+                    if error.line == 0 { return problem(error.message, record.line, record.col) }\\n+                    return error\\n+                }\\n+                n += 1\\n+            }\\n+            let _ = path.pop()\\n+            return\\n+        },\\n+        case _ -> { return problem(\\\"Void is not a record field type\\\", 0, 0) }\\n+    }\\n+}\\n+\\n+public func value_layout(records: &Vector<RecordIR>, t: ValueType) -> Result<Vector<ValueType>, ProtoError> {\\n+    var words = Vector<ValueType>()\\n+    if t != ValueType.Void {\\n+        var path = Vector<Int>()\\n+        try append_layout(&records, t, &var path, &var words)\\n+    }\\n+    move words\\n+}\\n+\\n+public func validate_records(records: &Vector<RecordIR>) -> Result<Void, ProtoError> {\\n+    var n = 0\\n+    while n < records.len() {\\n+        let record = records[n].copy()\\n+        var previous = 0\\n+        while previous < n {\\n+            if records[previous].name == record.name { return problem(\\\"duplicate record\\\", record.line, record.col) }\\n+            previous += 1\\n+        }\\n+        var field = 0\\n+        while field < record.field_names.len() {\\n+            previous = 0\\n+            while previous < field {\\n+                if record.field_names[previous] == record.field_names[field] {\\n+                    return problem(\\\"duplicate record field\\\", record.line, record.col)\\n+                }\\n+                previous += 1\\n+            }\\n+            field += 1\\n+        }\\n+        let _ = try value_layout(&records, ValueType.Record(n))\\n+        n += 1\\n+    }\\n+    return\\n+}\\n+\\n+public func record_field_offset(records: &Vector<RecordIR>, record_index: Int,\\n+                                field_index: Int) -> Result<Int, ProtoError> {\\n+    let _ = try value_layout(&records, ValueType.Record(record_index))\\n+    let record = records[record_index].copy()\\n+    if field_index < 0 || field_index >= record.field_types.len() {\\n+        return problem(\\\"invalid record field index\\\", record.line, record.col)\\n+    }\\n+    var offset = 0\\n+    var n = 0\\n+    while n < field_index {\\n+        let field = try value_layout(&records, record.field_types[n])\\n+        offset += field.len()\\n+        n += 1\\n+    }\\n+    offset\\n+}\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex cc4b8371..a4413d95 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -9,18 +9,44 @@ func invalid(i: &Instruction) -> ProtoError {\\n     return problem(\\\"internal prototype error: invalid instruction\\\", i.line, i.col)\\n }\\n \\n+func parameter_layout(p: &Program, f: &FuncIR) -> Result<Vector<ValueType>, ProtoError> {\\n+    var words = Vector<ValueType>()\\n+    var n = 0\\n+    while n < f.params.len() {\\n+        if f.params[n] == ValueType.Void { return problem(\\\"internal prototype error: Void parameter\\\", 0, 0) }\\n+        let layout = try value_layout(&p.records, f.params[n])\\n+        var word = 0\\n+        while word < layout.len() {\\n+            try! words.push(layout[word])\\n+            word += 1\\n+        }\\n+        n += 1\\n+    }\\n+    move words\\n+}\\n+\\n+func range_matches(f: &FuncIR, start: Int, layout: &Vector<ValueType>) -> Bool {\\n+    if start < 0 || start > f.slots.len() || layout.len() > f.slots.len() - start { return false }\\n+    var n = 0\\n+    while n < layout.len() {\\n+        if f.slots[start + n] != layout[n] { return false }\\n+        n += 1\\n+    }\\n+    true\\n+}\\n+\\n func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void, ProtoError> {\\n     let dst = slot_type(&f, i.dst)\\n     let a = slot_type(&f, i.a)\\n     let b = slot_type(&f, i.b)\\n     match i.op {\\n         case Const -> {\\n-            if dst == ValueType.Void { return invalid(&i) }\\n+            if not scalar_type(dst) { return invalid(&i) }\\n             if dst == ValueType.Bool && i.immediate != 0 && i.immediate != 1 { return invalid(&i) }\\n             if integer_type(dst) && not integer_canonical(i.immediate, dst) { return invalid(&i) }\\n         },\\n         case Copy -> {\\n-            if dst == ValueType.Void || a != dst { return invalid(&i) }\\n+            if not scalar_type(dst) || a != dst { return invalid(&i) }\\n         },\\n         case Add -> { try check_binary(&i, dst, a, b) },\\n         case Sub -> { try check_binary(&i, dst, a, b) },\\n@@ -45,9 +71,9 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n             if dst != ValueType.Bool || a != ValueType.Bool { return invalid(&i) }\\n         },\\n         case Eq -> {\\n-            if dst != ValueType.Bool || a == ValueType.Void || a != b { return invalid(&i) }\\n+            if dst != ValueType.Bool || not scalar_type(a) || a != b { return invalid(&i) }\\n         },\\n-        case Ne -> { if dst != ValueType.Bool || a == ValueType.Void || a != b { return invalid(&i) } },\\n+        case Ne -> { if dst != ValueType.Bool || not scalar_type(a) || a != b { return invalid(&i) } },\\n         case Lt -> {\\n             if dst != ValueType.Bool || not arithmetic_type(a) || b != a { return invalid(&i) }\\n         },\\n@@ -61,23 +87,30 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n         case Call -> {\\n             if i.target < 0 || i.target >= p.functions.len() { return invalid(&i) }\\n             let callee = p.functions[i.target].copy()\\n-            if i.args.len() != callee.params.len() { return invalid(&i) }\\n+            let params = try parameter_layout(&p, &callee)\\n+            if i.args.len() != params.len() { return invalid(&i) }\\n             if callee.result == ValueType.Void {\\n                 if i.dst != -1 { return invalid(&i) }\\n-            } else if dst != callee.result { return invalid(&i) }\\n+            } else {\\n+                let result = try value_layout(&p.records, callee.result)\\n+                if not range_matches(&f, i.dst, &result) { return invalid(&i) }\\n+            }\\n             var n = 0\\n             while n < i.args.len() {\\n-                if slot_type(&f, i.args[n]) != callee.params[n] { return invalid(&i) }\\n+                if slot_type(&f, i.args[n]) != params[n] { return invalid(&i) }\\n                 n = n + 1\\n             }\\n         },\\n         case Print -> {\\n-            if a == ValueType.Void { return invalid(&i) }\\n+            if not scalar_type(a) { return invalid(&i) }\\n         },\\n         case Return -> {\\n             if f.result == ValueType.Void {\\n                 if i.a != -1 { return invalid(&i) }\\n-            } else if a != f.result { return invalid(&i) }\\n+            } else {\\n+                let result = try value_layout(&p.records, f.result)\\n+                if not range_matches(&f, i.a, &result) { return invalid(&i) }\\n+            }\\n         }\\n     }\\n     return\\n@@ -89,13 +122,15 @@ func check_binary(i: &Instruction, dst: ValueType, a: ValueType, b: ValueType) -\\n }\\n \\n func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n-    if f.code.len() == 0 || f.slots.len() < f.params.len() {\\n+    let params = try parameter_layout(&p, &f)\\n+    let _ = try value_layout(&p.records, f.result)\\n+    if f.code.len() == 0 || f.slots.len() < params.len() {\\n         return problem(\\\"internal prototype error: invalid function\\\", 0, 0)\\n     }\\n     var n = 0\\n     while n < f.slots.len() {\\n-        if f.slots[n] == ValueType.Void { return problem(\\\"internal prototype error: Void slot\\\", 0, 0) }\\n-        if n < f.params.len() && f.slots[n] != f.params[n] {\\n+        if not scalar_type(f.slots[n]) { return problem(\\\"internal prototype error: non-scalar slot\\\", 0, 0) }\\n+        if n < params.len() && f.slots[n] != params[n] {\\n             return problem(\\\"internal prototype error: parameter slot mismatch\\\", 0, 0)\\n         }\\n         n = n + 1\\n@@ -125,6 +160,7 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n }\\n \\n public func verify(program: &Program) -> Result<Void, ProtoError> {\\n+    try validate_records(&program.records)\\n     if program.main_index < 0 || program.main_index >= program.functions.len() {\\n         return problem(\\\"internal prototype error: missing main\\\", 0, 0)\\n     }\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 9e0eb8ef..9a49dccf 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -1,7 +1,8 @@\\n import src.model.*\\n \\n static MAX_CALL_DEPTH: Int = 128\\n-struct CallResult { value: Int, has_value: Bool }\\n+struct CallResult { values: Vector<Int> }\\n+extension CallResult: NoCopy {}\\n func runtime_error(message: String) -> ProtoError { problem(message, 0, 0) }\\n func overflow_error() -> ProtoError { runtime_error(\\\"runtime error: integer overflow\\\") }\\n \\n@@ -157,7 +158,11 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                     i = i + 1\\n                 }\\n                 let result = try run_function(&program, inst.target, &args, &var remaining, depth + 1)\\n-                if inst.dst >= 0 { slots[inst.dst] = result.value }\\n+                var result_index = 0\\n+                while result_index < result.values.len() {\\n+                    slots[inst.dst + result_index] = result.values[result_index]\\n+                    result_index = result_index + 1\\n+                }\\n             },\\n             case Print -> {\\n                 let pt = function.slots[inst.a]\\n@@ -165,8 +170,14 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 else if integer_signed(pt) { print(slots[inst.a]) } else { print(UInt.from(truncating: slots[inst.a])) }\\n             },\\n             case Return -> {\\n-                if inst.a < 0 { return CallResult(value: 0, has_value: false) }\\n-                return CallResult(value: slots[inst.a], has_value: true)\\n+                var values = Vector<Int>()\\n+                let layout = try value_layout(&program.records, function.result)\\n+                var result_index = 0\\n+                while result_index < layout.len() {\\n+                    try! values.push(slots[inst.a + result_index])\\n+                    result_index = result_index + 1\\n+                }\\n+                return CallResult(values: move values)\\n             }\\n         }\\n         pc = pc + 1\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex 26c8bffc..c851afb8 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -99,13 +99,25 @@ RUN_CASES = (\\n     RunCase(\\\"numbers/cast_unsigned_signed\\\", \\\"runtime error: integer cast out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n     RunCase(\\\"numbers/shift_negative\\\", \\\"runtime error: shift out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n     RunCase(\\\"numbers/shift_width\\\", \\\"runtime error: shift out of range\\\\n\\\", 1, \\\"numbers\\\"),\\n+    RunCase(\\\"records/basic_reordered\\\", \\\"7\\\\ntrue\\\\n\\\", section=\\\"records\\\"),\\n+    RunCase(\\\"records/evaluation_order\\\", \\\"2\\\\n1\\\\n1\\\\n2\\\\n\\\", section=\\\"records\\\"),\\n+    RunCase(\\\"records/copy_and_assignment\\\", \\\"1\\\\n9\\\\n8\\\\n2\\\\n1\\\\n2\\\\n\\\", section=\\\"records\\\"),\\n+    RunCase(\\\"records/nested_mutation\\\", \\\"4\\\\n5\\\\n9\\\\n11\\\\n12\\\\n\\\", section=\\\"records\\\"),\\n+    RunCase(\\\"records/call_return_mixed\\\", \\\"300\\\\n-7\\\\n18446744073709551615\\\\n250\\\\n-7\\\\n18446744073709551615\\\\n\\\", section=\\\"records\\\"),\\n+    RunCase(\\\"records/one_word_result\\\", \\\"18446744073709551615\\\\n\\\", section=\\\"records\\\"),\\n+    RunCase(\\\"records/forward_and_numeric\\\", \\\"-128\\\\n255\\\\n65535\\\\n\\\", section=\\\"records\\\"),\\n+    RunCase(\\\"records/width_256\\\", \\\"0\\\\n255\\\\n17\\\\n255\\\\n\\\", section=\\\"records\\\"),\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"records/reject_builtin_name\\\", \\\"reserved\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_record_equality\\\", \\\"scalar\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_record_print\\\", \\\"print\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_empty_record\\\", \\\"field\\\", \\\"records\\\"),\\n     RejectCase(\\\"numbers/reject_byte_literal\\\", \\\"Byte\\\", \\\"numbers\\\"),\\n     RejectCase(\\\"numbers/reject_uncontextual_u64\\\", \\\"integer literal\\\", \\\"numbers\\\"),\\n     RejectCase(\\\"numbers/reject_spaced_shift\\\", \\\"expression\\\", \\\"numbers\\\"),\\n-    RejectCase(\\\"reject_type\\\", \\\"integer literal does not fit Bool\\\"),\\n+    RejectCase(\\\"reject_type\\\", \\\"cannot implicitly convert Int to Bool\\\"),\\n     RejectCase(\\\"reject_mutability\\\", \\\"cannot assign to immutable local\\\"),\\n     RejectCase(\\\"reject_arity\\\", \\\"wrong number of arguments\\\"),\\n     RejectCase(\\\"reject_unknown\\\", \\\"unknown name\\\"),\\n@@ -114,11 +126,11 @@ REJECT_CASES = (\\n     RejectCase(\\\"reject_semicolon\\\", \\\"semicolons are not supported\\\"),\\n     RejectCase(\\\"reject_string\\\", \\\"expected expression\\\"),\\n     RejectCase(\\\"reject_bool_arithmetic\\\", \\\"arithmetic requires integer operands\\\"),\\n-    RejectCase(\\\"reject_import\\\", \\\"only function declarations are supported\\\"),\\n+    RejectCase(\\\"reject_import\\\", \\\"only function and record declarations are supported\\\"),\\n     RejectCase(\\\"reject_scope_leak\\\", \\\"unknown name\\\"),\\n     RejectCase(\\\"reject_unary_depth\\\", \\\"expression nesting limit exceeded\\\"),\\n     RejectCase(\\\"reject_reserved_print\\\", \\\"reserved\\\"),\\n-    RejectCase(\\\"reject_truncated\\\", \\\"unterminated function body\\\"),\\n+    RejectCase(\\\"reject_truncated\\\", \\\"unterminated declaration body\\\"),\\n     RejectCase(\\\"reject_large_literal\\\", \\\"integer literal\\\"),\\n     RejectCase(\\\"numbers/reject_typed_narrowing\\\", \\\"convert\\\", \\\"numbers\\\"),\\n     RejectCase(\\\"numbers/reject_typed_narrowing_assignment\\\", \\\"convert\\\", \\\"numbers\\\"),\\n@@ -127,6 +139,18 @@ REJECT_CASES = (\\n     RejectCase(\\\"numbers/reject_mixed_binary\\\", \\\"same type\\\", \\\"numbers\\\"),\\n     RejectCase(\\\"numbers/reject_bool_cast\\\", \\\"requires integer source and target\\\", \\\"numbers\\\"),\\n     RejectCase(\\\"numbers/reject_literal_range\\\", \\\"range\\\", \\\"numbers\\\"),\\n+    RejectCase(\\\"records/reject_nominal\\\", \\\"convert\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_immutable_field\\\", \\\"immutable\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_parameter_field\\\", \\\"immutable\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_missing_label\\\", \\\"missing\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_duplicate_label\\\", \\\"duplicate\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_unknown_label\\\", \\\"unknown\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_wrong_field_type\\\", \\\"convert\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_direct_cycle\\\", \\\"recursive\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_indirect_cycle\\\", \\\"recursive\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_width_257\\\", \\\"256\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_duplicate_record\\\", \\\"duplicate record\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_duplicate_field\\\", \\\"duplicate record field\\\", \\\"records\\\"),\\n )\\n \\n \\n@@ -211,7 +235,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -240,7 +264,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section == \\\"numbers\\\" else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\ndiff --git a/prototypes/minivm/tests/record_contract.saw b/prototypes/minivm/tests/record_contract.saw\\nnew file mode 100644\\nindex 00000000..e98e87bf\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/record_contract.saw\\n@@ -0,0 +1,126 @@\\n+import src.model.*\\n+import src.verify.*\\n+\\n+func pair_record() -> RecordIR {\\n+    var names = Vector<String>()\\n+    try! names.push(\\\"byte\\\")\\n+    try! names.push(\\\"count\\\")\\n+    var types = Vector<ValueType>()\\n+    try! types.push(ValueType.U8)\\n+    try! types.push(ValueType.I64)\\n+    RecordIR(name: \\\"Pair\\\", field_names: move names, field_types: move types, line: 1, col: 1)\\n+}\\n+\\n+func layout_fails(records: &Vector<RecordIR>, ty: ValueType) -> Bool {\\n+    let _ = try value_layout(&records, ty) catch { return true }\\n+    false\\n+}\\n+\\n+func valid(program: &Program) -> Bool {\\n+    try verify(&program) catch { return false }\\n+    true\\n+}\\n+\\n+func call_program(records: &Vector<RecordIR>) -> Program {\\n+    var slots = Vector<ValueType>()\\n+    try! slots.push(ValueType.U8)\\n+    try! slots.push(ValueType.I64)\\n+    var args = Vector<Int>()\\n+    try! args.push(0)\\n+    try! args.push(1)\\n+    var call = instruction(Op.Call)\\n+    call.target = 1\\n+    call.dst = 0\\n+    call.args = move args\\n+    var main_code = Vector<Instruction>()\\n+    try! main_code.push(move call)\\n+    try! main_code.push(instruction(Op.Return))\\n+    var params = Vector<ValueType>()\\n+    try! params.push(ValueType.Record(0))\\n+    var ret = instruction(Op.Return)\\n+    ret.a = 0\\n+    var callee_code = Vector<Instruction>()\\n+    try! callee_code.push(move ret)\\n+    var functions = Vector<FuncIR>()\\n+    try! functions.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: slots.copy(), code: move main_code))\\n+    try! functions.push(FuncIR(name: \\\"identity\\\", params: move params, result: ValueType.Record(0),\\n+        slots: move slots, code: move callee_code))\\n+    Program(records: records.copy(), functions: move functions, main_index: 0)\\n+}\\n+\\n+func main() {\\n+    var records = Vector<RecordIR>()\\n+    try! records.push(pair_record())\\n+    var names = Vector<String>()\\n+    try! names.push(\\\"flag\\\")\\n+    try! names.push(\\\"pair\\\")\\n+    try! names.push(\\\"tail\\\")\\n+    var types = Vector<ValueType>()\\n+    try! types.push(ValueType.Bool)\\n+    try! types.push(ValueType.Record(0))\\n+    try! types.push(ValueType.I16)\\n+    try! records.push(RecordIR(name: \\\"Outer\\\", field_names: move names, field_types: move types, line: 2, col: 1))\\n+    try! validate_records(&records)\\n+    let layout = try! value_layout(&records, ValueType.Record(1))\\n+    assert(layout.len() == 4, \\\"nested width\\\")\\n+    assert(layout[0] == ValueType.Bool && layout[1] == ValueType.U8 && layout[2] == ValueType.I64 && layout[3] == ValueType.I16, \\\"nested types\\\")\\n+    assert((try! record_field_offset(&records, 1, 1)) == 1, \\\"nested offset\\\")\\n+    assert((try! record_field_offset(&records, 1, 2)) == 3, \\\"following field offset\\\")\\n+    assert(layout_fails(&records, ValueType.Record(-1)), \\\"negative record index\\\")\\n+    assert(layout_fails(&records, ValueType.Record(2)), \\\"large record index\\\")\\n+    var cyclic = records.copy()\\n+    cyclic[0].field_types.set(1, ValueType.Record(1))\\n+    assert(layout_fails(&cyclic, ValueType.Record(0)), \\\"indirect cycle\\\")\\n+    var direct_cycle = records.copy()\\n+    direct_cycle[0].field_types.set(0, ValueType.Record(0))\\n+    assert(layout_fails(&direct_cycle, ValueType.Record(0)), \\\"direct cycle\\\")\\n+    var nested_bad_index = records.copy()\\n+    nested_bad_index[0].field_types.set(0, ValueType.Record(99))\\n+    assert(layout_fails(&nested_bad_index, ValueType.Record(1)), \\\"nested invalid index\\\")\\n+    var wide = pair_record()\\n+    wide.field_names = Vector<String>()\\n+    wide.field_types = Vector<ValueType>()\\n+    var width = 0\\n+    while width < 256 {\\n+        try! wide.field_names.push(\\\"f{width}\\\")\\n+        try! wide.field_types.push(ValueType.I64)\\n+        width += 1\\n+    }\\n+    var wide_records = Vector<RecordIR>()\\n+    try! wide_records.push(move wide)\\n+    assert((try! value_layout(&wide_records, ValueType.Record(0))).len() == 256, \\\"exact width limit\\\")\\n+    try! wide_records[0].field_names.push(\\\"extra\\\")\\n+    try! wide_records[0].field_types.push(ValueType.I64)\\n+    assert(layout_fails(&wide_records, ValueType.Record(0)), \\\"over width limit\\\")\\n+    var void_field = records.copy()\\n+    void_field[0].field_types.set(0, ValueType.Void)\\n+    assert(layout_fails(&void_field, ValueType.Record(0)), \\\"Void field\\\")\\n+    var mismatched = records.copy()\\n+    let _ = mismatched[0].field_names.pop()\\n+    assert(layout_fails(&mismatched, ValueType.Record(0)), \\\"field metadata mismatch\\\")\\n+    assert(not integer_type(ValueType.Record(0)), \\\"record not integer\\\")\\n+    assert(not scalar_type(ValueType.Record(0)), \\\"record not scalar\\\")\\n+\\n+    let program = call_program(&records)\\n+    assert(valid(&program), \\\"valid flattened call and return\\\")\\n+    var truncated_call = program.copy()\\n+    truncated_call.functions[0].code[0].dst = 1\\n+    assert(not valid(&truncated_call), \\\"truncated call result range\\\")\\n+    var truncated_return = program.copy()\\n+    truncated_return.functions[1].code[0].a = 1\\n+    assert(not valid(&truncated_return), \\\"truncated return range\\\")\\n+    var wrong_argument = program.copy()\\n+    wrong_argument.functions[0].code[0].args.set(1, 0)\\n+    assert(not valid(&wrong_argument), \\\"flattened argument type\\\")\\n+    var missing_argument = program.copy()\\n+    let _ = missing_argument.functions[0].code[0].args.pop()\\n+    assert(not valid(&missing_argument), \\\"flattened argument count\\\")\\n+    var wrong_parameter = program.copy()\\n+    wrong_parameter.functions[1].slots.set(1, ValueType.U64)\\n+    assert(not valid(&wrong_parameter), \\\"flattened parameter type\\\")\\n+    var record_slot = program.copy()\\n+    record_slot.functions[0].slots.set(0, ValueType.Record(0))\\n+    assert(not valid(&record_slot), \\\"slots must be scalar\\\")\\n+    print(\\\"record contract passed\\\")\\n+}\\n\",\"id\":\"SL-245\",\"request_id\":\"minivm-SL-245-e07b57a7\",\"title\":\"minivm M2: scalar value records (branch commit e07b57a7)\"}","request_id":"minivm-SL-245-e07b57a7","sequence":434,"timestamp":"1789088118"}
---
