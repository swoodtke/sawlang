---
{"action":"create","actor":"agent:codex","after":{"assignee":"","author":"agent:codex","comments":[],"created":"1789477091","description":"SL-260 found Python sawc rejecting valid code when dead statements follow an unconditional return. Existing fixture prototypes/minivm/examples/unreachable.saw:\n\n```saw\nfunc stop() -> Int32 {\n    return 7\n    print(99)\n    if true { print(98) }\n}\nfunc main() { print(stop()) }\n```\n\nActual: clean compiler error, `function stop should return Int32 but returns Void`. Expected: compile and print 7; unreachable statements may still be typechecked, but cannot replace the reachable function result. Prototype behavior is correct.\n\nMechanism: block checking reports the final, unreachable statement's Void type and function return reconciliation uses that type despite the earlier unconditional return. Preserve path termination separately from the final syntactic expression type. Sweep explicit returns followed by dead scalar/Void/control-flow statements, functions/methods, and branch nesting; retain type errors inside unreachable code. This is distinct from failing to reject a reachable missing-return path.\n","id":"SL-295","labels":["bug","typechecking"],"priority":"normal","project":"SL","revision":1,"sequence":853,"stage":"backlog","status":"open","title":"sawc: dead statements after unconditional return poison the function result","updated":"1789477091"},"before":null,"id":"SL-295","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex\",\"description\":\"SL-260 found Python sawc rejecting valid code when dead statements follow an unconditional return. Existing fixture prototypes/minivm/examples/unreachable.saw:\\n\\n```saw\\nfunc stop() -> Int32 {\\n    return 7\\n    print(99)\\n    if true { print(98) }\\n}\\nfunc main() { print(stop()) }\\n```\\n\\nActual: clean compiler error, `function stop should return Int32 but returns Void`. Expected: compile and print 7; unreachable statements may still be typechecked, but cannot replace the reachable function result. Prototype behavior is correct.\\n\\nMechanism: block checking reports the final, unreachable statement's Void type and function return reconciliation uses that type despite the earlier unconditional return. Preserve path termination separately from the final syntactic expression type. Sweep explicit returns followed by dead scalar/Void/control-flow statements, functions/methods, and branch nesting; retain type errors inside unreachable code. This is distinct from failing to reject a reachable missing-return path.\\n\",\"labels\":[\"bug\",\"typechecking\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"codex-m17-dead-return-20260915\",\"title\":\"sawc: dead statements after unconditional return poison the function result\"}","request_id":"codex-m17-dead-return-20260915","sequence":853,"timestamp":"1789477091"}
---
