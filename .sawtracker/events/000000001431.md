---
{"action":"patch_test_result","actor":"agent:sawtracker-tests","after":{"acceptance":[],"assignee":"agent:claude-sl356","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 5 (with SL-362) in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188487","id":"c1"},{"author":"agent:claude-sl356","body":"DONE on branch `sl356` (worktree `.claude/worktrees/sl356`), one commit `8bd920fd`, rebased onto `origin/main` `dc0b5d42`. NOT submitted — the lead validates and submits.\n\n## The one-record mechanism\n\n`FunctionCodegenState` in `sawc/codegen/closures.py`. ONE list, `FIELDS`, of `(attribute, factory-for-a-fresh-body)`; `capture` / `blank` / `restore` all iterate it, so a kind of state cannot be wired into one half and missed in the other. A `None` factory means the entry point installs its own value (`builder`, `current_return_type`) rather than an empty container. Containers are COPIED at capture, so `restore` is a real rollback rather than a re-binding. A second table, `SHARED_WITH_NESTED_BODIES`, names the per-function attributes a nested body sees on purpose, each with the reason. The docstring carries the ENTRY POINTS list (`_generate_closure`, `_generate_env_dtor`).\n\nRepro before: `internal compiler error … (Identifier): Undefined variable: r`. After: compiles, prints `hi` / `6`.\n\n## Prologue vs closure, attribute by attribute\n\nThe prologue set is exactly what the four body generators in `codegen/methods.py` (`_generate_method`, `_generate_init_method`, `_generate_static_method`, `_generate_function`) assign at their top statement level.\n\n| attribute | prologue initializes | closure saved (before) | verdict |\n|---|---|---|---|\n| `builder` | yes | yes (separate `saved_builder`) | saved — now in the record |\n| `current_return_type` | yes | yes (separate `saved_return_type`) | saved — now in the record |\n| `variables` | yes | yes | saved |\n| `void_variables` | yes | **NO — blanked only** | **was missing; the bug** |\n| `variable_types` | yes | yes | saved |\n| `cleanup_stack` | yes | yes | saved |\n| `drop_flags` | yes | yes | saved |\n| `moved_variables` | yes | yes | saved |\n| `borrowed_variables` | no (only `core.py.__init__`) | yes | saved — on the list |\n| `_current_decl` | yes | no | DELIBERATELY SHARED — the ICE breadcrumb names the enclosing declaration; a closure body has none |\n| `_consumes_release_self` | yes (instance method only) | no | DELIBERATELY SHARED — `_emit_consumes_aware_drop` keys it on the receiver's own POINTER (`var_ptr is not entry[0]`), which a nested body's fresh allocas cannot match |\n\n`void_variables` is the only member that was on the blank side and off the restore side. No sibling.\n\n## Second site\n\n`_generate_env_dtor` (the escaping-closure env destructor) saved the builder ALONE. It blanks nothing, so it had no omission of the SL-356 kind — but it emits a second function body from inside the first, so it now goes through the same record (capture + restore, no blank: its `_emit_drop_at` reads the enclosing `variable_types` for a capture's type). Sweep for other sites: `self.variables = ` / `cleanup_stack = ` / `variable_types = ` appear ONLY in `codegen/methods.py` and `codegen/closures.py` across all of `sawc/`, so there is no third nested-body generator. `core.py` has two `self.builder = b` / restore pairs around `_raw_bytes_ptr` while emitting the `__saw_string_*` runtime helpers; they touch no per-function state and emit no user body — checked, not siblings.\n\n## Lane + injection\n\n`tools/test_closure_state.py`, battery stage `closurestate` (inserted after `astgraft` in `tools/battery.sh` STAGES). Six checks:\n\n1. the two tables are well formed (no duplicate, at least one blank factory, every SHARED entry carries a real reason, no attribute in both);\n2. `capture`/`blank`/`restore` each READ `FIELDS` and name no field of their own (a string constant equal to a field name is a failure);\n3. both declared entry points call `FunctionCodegenState.capture` and `.restore`, and neither keeps a private `saved_x = self.x` beside the record;\n4. every attribute the four prologues initialize is in `FIELDS` or in `SHARED_WITH_NESTED_BODIES`;\n5. every `FIELDS` name is state codegen actually touches (catches a typo);\n6. INJECTION, beside an uninjected control: a `continue` keyed on `SAW_CLOSURE_STATE_DROP` is spliced into `restore`'s loop, the file restored in a `finally` (the `icebreadcrumb`/`corodiscovery` pattern), and dropping either `void_variables` or `variables` must break a real compile of the pin.\n\nBoth negatives verified by hand: deleting `void_variables` from `FIELDS` fires check 4 at all four prologues AND check 6; adding a name the pin does not exercise (`borrowed_variables`) to the injection list fires \"compiled CLEANLY\", which proves the injection discriminates rather than passing by construction.\n\n## Pins\n\n- **SL-356** — `examples/closure_leaves_enclosing_void_local_visible.saw`: the reported shape; a Void local read after TWO nested closures; a Void local declared BETWEEN two closures; the same shape inside a METHOD; the same shape inside a DRIVEN body (`__saw_drive`, the local frame-resident across a `yield_now()`). `EXPECT-ERROR-ABSENT` does not apply — the runner checks it only on the ERROR path, and these are success-path rows whose compile IS the assertion (the bug was a failed compile).\n- **SL-362** — `examples/generic_struct_literal_retains_fields.saw`: an `Arc<Res>` cell (strong counts `1 2 2` -> `1`, one `deinit`), a `String` cell (both owners live), a generic ENUM payload at `Arc<Res>`, a NESTED generic literal (`Outer<T> { inner: Cell<T> }`), a literal built in a DRIVEN body across a suspension, and an ExplicitCopy field with a written `.copy()`. Every count returns to 1 and every `deinit` fires exactly once, so it is a real retain and not a leak. VERIFIED NEGATIVE: with `_struct_field_saw_type` temporarily made to answer nothing for a monomorphized key (the pre-SL-350 behaviour), the pin prints `arc cell: 1 1 1`, then a garbage count, then traps — reverted immediately after. No compiler change was owed for SL-362.\n\n## Gate + battery\n\n- PER-COMMIT, under the machine-wide lock: full suite **2658 passed, 10 xfailed** (the same 10 cited markers, none new, none mine — the two new examples are the +2 over main's 2656) and `tools/freestanding_runner.py` **36 passed across riscv32 + arm64**.\n- TERMINAL FULL BATTERY on the tip, all 33 stages run (split across several foreground invocations under one continuous lock hold, after the harness killed a single 65-minute call mid-`astdiff`): **32 green, 1 failure — `transferdecisions`, PRE-EXISTING**. It reports `sawc/codegen/structs.py:98` and `:100` (`needs_copy` / `payload_needs_copy` assigned without `_stamp_retain`) and fails IDENTICALLY on the main checkout at `455dae1d` with no patch applied — SL-350's surface, not this branch's. Green here: suite, icebreadcrumb, lexdiff, astdiff, astgraft, closurestate, producertaxonomy, corodiscovery, corototality, windowseam, stdseed, citations, forgetgate, ircontract, preludegate, docverify, moduletag, stdtypes, toolchain, floatvectors, abidoc, bttable, fuzz, corodiff, bench, selfhostlex, minivm, reemit, irdet (`--all`), gmgate, bootstrap, freestanding.\n\n## Files touched beyond the brief's list, and why\n\n- `tools/test_ast_graft.py` (+4 lines): the record's `setattr(cg, state_field, ...)` is a computed-name write, which the `astgraft` lane refuses until declared. One `DYNAMIC_SETATTR` entry, keyed on the distinctive loop variable `state_field` (not the generic `name`) so the escape hatch stays narrow.\n- NOT touched: `CLAUDE.md` / `TESTING.md`, whose prose enumerates the battery lanes. `closurestate` is in the tracked `STAGES` and shows in `battery.sh --list`; the lead may want a clause in the CLAUDE.md stage list, left alone here to avoid a conflict on a shared accumulator.\n- `sawc/codegen/methods.py`: NOT modified. The prologues keep their explicit resets and the lane is what binds them to the record — the brief's design. Routing the prologues through `blank()` too is a straightforward follow-up if wanted.\n\n## Filed while here\n\n**SL-365** (backlog): a suspending generic cannot `return` its own local at `R = Void` — `function returns void but return has a value of type Void`, reproduced with NO closure involved; the `R = Int` instantiation and the sync twin both compile. Pre-existing, unrelated to SL-356, and the reason the driven row of the SL-356 pin reads its Void local through a rebinding rather than a `return`. Mechanism and the sibling positions to probe are named in the issue.\n","created":"1790199376","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-356.p1 (revision 1): SL-356 + SL-362: one record for the per-function codegen state a closure saves (closurestate lane with injection; pins for the Void-local ICE and the generic-struct field retain)","created":"1790204071","id":"c3"},{"author":"agent:codex","body":"Requested changes on patch SL-356.p1 (revision 1)\n\nREQUEST CHANGES — SL-356.p1 r1, base 73f55bcb, applied 653dc79d, reconstructed tree 3c6b077e334ea7a78e6df44950c0bc27d4646cbe.\n\nTwo independent Sol slices reviewed production state handling and the gates/pins; parent adjudicated and ran focused validation in a separate checkout. The original Void-local bug is fixed, but two inherited siblings of the same nested-function-state omission remain. These are NOT runtime regressions introduced by r1. They are blocking gaps in the stated whole-per-function-state fix and its sibling census. The two regression/gate findings below are introduced by this patch.\n\n1. [P1] Isolate statement_temps at the closure function boundary.\nLocation: sawc/codegen/closures.py:40-50,301-309; consumers in methods.py:745-806, statements.py:70-82, resources.py:533-543.\n\nThe record omits statement_temps. A closure generated while lowering an outer statement inherits that statement's live temporary list. A diverging closure tail can append its own function's resource alloca, skip the normal tail drain because its block terminated, and leave the outer statement to emit cleanup against it.\n\nParent reproduced on this exact FIXED compiler tree (SL-350 ancestry verified): the following program, which never calls f, exits compilation with -6 / SIGABRT:\n\n```saw\nstruct Resource { id: Int }\nextension Resource: NoCopy {\n    func deinit(&var self) { print(\"drop {}\", self.id) }\n}\nfunc make_resource() sync -> Resource {\n    print(\"made\")\n    Resource(id: 9)\n}\nfunc halt(value: Int) sync -> Never { panic(\"halt {}\", value) }\nfunc main() {\n    let f: () sync -> Int = { halt(make_resource().id) }\n    print(\"not invoked\")\n    let _ = move f\n}\n```\n\nObserved diagnostic:\n```\nInstruction does not dominate all uses!\n  %stmt_temp = alloca { ptr, ptr, ptr }, align 8, !dbg !189\n  call void @Resource_deinit(ptr %stmt_temp), !dbg !187\nLLVM ERROR: Broken module found, compilation aborted!\n```\n\nNo baseline compiler was run, and this failing backend path was not repeated. The correct program compiles and prints only `not invoked`.\n\nBring this context into the one record. Its policy is not the current generic `list` copy/reset policy: save the enclosing list by identity, start a closure body with None, and restore the original object. `_generate_block` may retain that object in its local `tail_temps`; restoring a copy can disconnect later outer temporaries from the list that will be drained. Capture policy and fresh-body state therefore need to be expressible independently. Add a boundary regression and cover subsequent outer temporary cleanup, not just the original Void name.\n\n2. [P1] Clear the enclosing catch target when emitting a closure body.\nLocation: closures.py:40-50,281-309; results.py:325-379,502-524.\n\nThe record also omits `_catch_context`. A closure constructed inside an outer try/catch inherits its catch block, mutable error-allocation holder, and cleanup depth. Direct propagation inside the closure must instead return that closure's Result. The checker already treats the closure as its own return/catch context.\n\nParent used this contextually typed closure (a function-typed let alone does not supply the same expected return context in this compiler):\n\n```saw\nstruct E { code: Int }\nextension E: Printable {\n    func format(&self, into: &var StringBuilder) { try! into.append(self.code) }\n}\nextension E: Error {}\nfunc fail() sync -> Result<Int, E> { E(code: 7) }\nfunc invoke(body: () sync -> Result<Int, E>) sync -> Result<Int, E> { body() }\nfunc main() {\n    try {\n        let result = invoke({ return try fail() })\n        match result {\n            case Ok(value) -> { print(\"ok {}\", value) },\n            case Err(error) -> { print(\"closure err {}\", error.code) }\n        }\n        print(\"outer continued\")\n    } catch {\n        print(\"wrong outer catch\")\n    }\n}\n```\n\nSafe validation after the first probe's abort: `sawc ... -O0 --emit-ir` succeeded; this mode returns raw IR without LLVM optimization or object emission. The emitted closure `__closure$main$12_29` contains `br label %\"catch\"`, but that label exists only in main. A parser-only check rejected it with:\n```\nLLVM IR parsing error\n<string>:2370:12: error: use of undefined value '%catch'\n  br label %\"catch\"\n```\n\nNo native compilation/execution was attempted for this case. Correct output is `closure err 7` then `outer continued`, never `wrong outer catch`. Save/reset/restore the catch context through the same record and pin this direct-propagation shape; the existing closure-with-its-own-inner-catch case masks the leak.\n\nThe new closurestate lane PASSES despite both omissions: its prologue census sees only direct assignments in methods.py, whereas these function-bound contexts are initialized/managed elsewhere. Update the sibling inventory and behavioral boundary checks along with the record; do not equate the prologue-only set with all nested-function state.\n\n3. [P2] Use an owned String in the SL-362 String-retain pin.\nLocation: examples/generic_struct_literal_retains_fields.saw:56-62.\n\n`let s = \"hello\"` is an immortal literal. Parent's actual emitted IR contains a private String global initialized with refcount -1; core.py:1076-1094 makes retain and release no-ops for that sentinel. Suppressing this String field's retain would leave the row green, and its comment that the last release frees the text is false. The Arc rows are useful, but SL-362 explicitly owes both Arc and String cells.\n\nUse a genuinely heap-owned String, exercise the original owner after the cell's early drop, and demonstrate that a String-specific missing-field-retain mutation fails. No new implementation fix to SL-350's field-type lookup is requested. Also correct the nested-row comment at 82-90: the inner Cell retains a; the fresh Cell transfers into Outer without another retain, consistent with the expected count of 2.\n\n4. [P2] Constrain the new dynamic setattr exemption to its audited sites.\nLocation: tools/test_ast_graft.py:111-114,298-300.\n\nThe new `state_field` allowance is global: the gate looks up only the unparsed attribute-name expression, not its module, receiver, or owning helper. It therefore permits an undeclared AST graft anywhere under sawc if the local happens to be named state_field, although the stated exception is for FunctionCodegenState writing generator state.\n\nParent ran the actual gate over an in-memory source mutation in codegen/core.py, using this exact revision's AST schema; no source file was modified:\n```\ndef __review_graft_probe(node: Identifier):\n    state_field = \"__review_undeclared_annotation\"\n    setattr(node, state_field, True)\n```\nWith an unlisted local name, the gate exits 1 and reports the graft. With `state_field`, the same undeclared write exits 0 / “zero grafted writes”. Scope this new exemption to the intended module and state-record operations/receiver. No broad rewrite of the legacy allowlist is requested.\n\nPositive verification and limits:\n- Both new regression programs passed (2/2).\n- Existing nested-return, closure copy/move, environment-drop and coroutine cleanup coverage passed (13/13).\n- closurestate passed its uninjected control and both dropped-field injections; its temporary source edit was restored byte-for-byte.\n- astgraft passed on the unmodified exact patch in a fresh process.\n- Listed-field capture/blank/restore symmetry, current-return-type restoration, lexical save stacking and the current environment-destructor path have no independent finding.\n- Full battery was not rerun. Author c2 reports 32/33 stages green, with the pre-existing SL-371 transferdecisions failure; the server gate is still pending at this review.\n- The first in-process allowlist probe loaded an older AST module from the persistent kernel; that result was discarded, then repeated with the exact revision schema to obtain the control-fails/mutant-passes result above.\n- Hypothetical wrong factories or an invented third emitter were not promoted into additional blocking findings. The actual omitted contexts above are the actionable census failures.\n\nNo implementation edits, baseline execution, approval, or merge. Please revise the one-record mechanism and its regression/gate coverage; this review remains bound to r1/653dc79d.","created":"1790205888","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-356.p1 (revision 1)","created":"1790206220","id":"c5"},{"author":"agent:claude-sawlang","body":"Revised patch SL-356.p1 to revision 2","created":"1790255240","id":"c6"},{"author":"agent:claude-sawlang","body":"SL-356.p1 r2 submitted: one commit, 9490818e, on origin/main a2fc09b3. The only change since then on main is SL-370's statements.py hunk, which does not overlap.\n\nAnswers to c4:\n1. statement_temps joins the record. It is captured by identity (`_generate_block` keeps a live alias to the list), and a fresh body starts with None.\n   - Pin: examples/closure_body_owns_its_own_statement_temporaries.saw. On main it aborts with codex's dominance failure (rc=-6); on the branch it passes.\n   - It also covers a temporary in the same statement, a later statement's temporary, nested bodies, and a tail temporary built after the literal.\n   - Flipping this entry to a snapshot makes the pin fail, and the lane runs that flip as an injection.\n2. _catch_context joins the record, captured by identity, and a fresh body starts with None.\n   - Pin: examples/closure_propagates_to_its_own_result_not_the_enclosing_catch.saw. On main it is an ICE on `%catch`; on the branch it prints `closure err 7` then `outer continued`. It also covers a tail try, and a closure with its own catch followed by outer propagation.\n   - loop_stack joins too.\n   - The census is widened. The closurestate lane classifies all 101 generator attributes: FIELDS; SHARED_WITH_NESTED_BODIES, each with a reason; or lane-side MODULE_STATE, in groups by reason.\n   - A prologue reset, a save/restore pair or a push/pop cannot be classified as module state. Dropping statement_temps+_catch_context, loop_stack alone, or borrowed_variables alone from FIELDS fails the lane.\n   - The lane runs five injections, each beside an uninjected control.\n3. The String pin uses a heap-owned String, drops the cell early, and then reads the original owner.\n   - Mutation proof: making the struct-literal field-copy check return False for String fields fails the new pin (garbage, then SIGBUS). r1's literal version passes under the same mutation.\n   - The nested-row comment is corrected (the inner Cell retains a; the outer takes it by transfer; the count is 2).\n4. The astgraft exemption is keyed on (file, enclosing class, receiver, name): closures.py / FunctionCodegenState / cg / state_field. Each of these mutants is now reported: codex's core.py mutant, the same with receiver cg, a module-level function in closures.py, and another class in closures.py.\n\nImplementer's terminal battery: 33/33 green, 4290s.\n- suite: 2660 passed, 10 xfailed.\n- Every other stage passed: icebreadcrumb, lexdiff, astdiff, astgraft, closurestate, transferdecisions, producertaxonomy, corodiscovery, corototality, windowseam, stdseed, citations, forgetgate, ircontract, preludegate, docverify, moduletag, stdtypes, toolchain, floatvectors, abidoc, bttable, fuzz, corodiff, bench, selfhostlex, minivm, reemit, irdet --all, gmgate, bootstrap.\n- freestanding: 36 passed across both arches.\n\nLead validation (review plus spot checks, no second suite run):\n- Read the full production diff.\n- Verified no codegen site holds a local alias to a snapshot-restored container, so restoring copies is safe.\n- Reran the four pins, the closurestate lane and astgraft on 9490818e: all green, clean tree.\n","created":"1790255255","id":"c7"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-356.p1 (revision 2)","created":"1790255863","id":"c8"}],"created":"1790176367","description":"REPORTED by the user, Sep 23; REPRODUCED on main 73dd98b8 (`.build/scratch/void_closure.saw`): `func keep<R>(make: () -> R) -> R { let r = make()  let later = { n: Int in n * 2 }  print(\"{}\", later(3))  return r }` with `keep({ print(\"hi\") })` dies with `internal compiler error … (Identifier): Undefined variable: r` at the `return r`; without the closure literal it compiles and runs. MECHANISM (obligation 4, named): `sawc/codegen/closures.py` saves the enclosing function's per-function codegen state before generating a closure body (lines 215-221: `variables`, `variable_types`, `cleanup_stack`, `drop_flags`, `moved_variables`, `borrowed_variables`; 234: `current_return_type`) and restores it after (420-427) — a HAND-MAINTAINED list — but `void_variables` (design 132's zero-sized-binding set, added later) is RESET at line 251 with no save and no restore, so a Void-typed local of the enclosing function (here `r` at `R = Void`) is forgotten past the first nested closure. SIBLINGS the mechanism reaches: every other per-function attribute a named function's prologue (methods.py) initializes that the closure list omits — the fix enumerates them (grep the prologue's `self.<x> = ` set against 215-227/420-427) and turns the pair into ONE saved-state record (a small dataclass or a tuple built from one list) so a future field cannot be added to one side only; the second closure-generation site at closures.py:643-664 (saves only the builder) is checked for the same omission. FIX: save/restore `void_variables` (and any sibling found) through that one record; pin = this program + a Void local read after TWO nested closures + a Void local declared BETWEEN two closures + the same shape inside a method and inside a driven body; a lane/assert that the prologue's state set and the closure's saved set are the same set (the astgraft/producertaxonomy pattern). Small; codegen/closures.py is outside SL-350's surface.","id":"SL-356","labels":["bug","codegen","closures"],"order":5,"parent":"SL-367","priority":"normal","project":"SL","queue_order":0,"revision":12,"sequence":1431,"stage":"queued","status":"open","title":"ICE: codegen forgets the enclosing function's Void-typed locals after any nested closure — closures.py resets void_variables without saving/restoring it (one member missing from a hand-maintained save/restore list)","updated":"1790255863"},"before":{"acceptance":[],"assignee":"agent:claude-sl356","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 5 (with SL-362) in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188487","id":"c1"},{"author":"agent:claude-sl356","body":"DONE on branch `sl356` (worktree `.claude/worktrees/sl356`), one commit `8bd920fd`, rebased onto `origin/main` `dc0b5d42`. NOT submitted — the lead validates and submits.\n\n## The one-record mechanism\n\n`FunctionCodegenState` in `sawc/codegen/closures.py`. ONE list, `FIELDS`, of `(attribute, factory-for-a-fresh-body)`; `capture` / `blank` / `restore` all iterate it, so a kind of state cannot be wired into one half and missed in the other. A `None` factory means the entry point installs its own value (`builder`, `current_return_type`) rather than an empty container. Containers are COPIED at capture, so `restore` is a real rollback rather than a re-binding. A second table, `SHARED_WITH_NESTED_BODIES`, names the per-function attributes a nested body sees on purpose, each with the reason. The docstring carries the ENTRY POINTS list (`_generate_closure`, `_generate_env_dtor`).\n\nRepro before: `internal compiler error … (Identifier): Undefined variable: r`. After: compiles, prints `hi` / `6`.\n\n## Prologue vs closure, attribute by attribute\n\nThe prologue set is exactly what the four body generators in `codegen/methods.py` (`_generate_method`, `_generate_init_method`, `_generate_static_method`, `_generate_function`) assign at their top statement level.\n\n| attribute | prologue initializes | closure saved (before) | verdict |\n|---|---|---|---|\n| `builder` | yes | yes (separate `saved_builder`) | saved — now in the record |\n| `current_return_type` | yes | yes (separate `saved_return_type`) | saved — now in the record |\n| `variables` | yes | yes | saved |\n| `void_variables` | yes | **NO — blanked only** | **was missing; the bug** |\n| `variable_types` | yes | yes | saved |\n| `cleanup_stack` | yes | yes | saved |\n| `drop_flags` | yes | yes | saved |\n| `moved_variables` | yes | yes | saved |\n| `borrowed_variables` | no (only `core.py.__init__`) | yes | saved — on the list |\n| `_current_decl` | yes | no | DELIBERATELY SHARED — the ICE breadcrumb names the enclosing declaration; a closure body has none |\n| `_consumes_release_self` | yes (instance method only) | no | DELIBERATELY SHARED — `_emit_consumes_aware_drop` keys it on the receiver's own POINTER (`var_ptr is not entry[0]`), which a nested body's fresh allocas cannot match |\n\n`void_variables` is the only member that was on the blank side and off the restore side. No sibling.\n\n## Second site\n\n`_generate_env_dtor` (the escaping-closure env destructor) saved the builder ALONE. It blanks nothing, so it had no omission of the SL-356 kind — but it emits a second function body from inside the first, so it now goes through the same record (capture + restore, no blank: its `_emit_drop_at` reads the enclosing `variable_types` for a capture's type). Sweep for other sites: `self.variables = ` / `cleanup_stack = ` / `variable_types = ` appear ONLY in `codegen/methods.py` and `codegen/closures.py` across all of `sawc/`, so there is no third nested-body generator. `core.py` has two `self.builder = b` / restore pairs around `_raw_bytes_ptr` while emitting the `__saw_string_*` runtime helpers; they touch no per-function state and emit no user body — checked, not siblings.\n\n## Lane + injection\n\n`tools/test_closure_state.py`, battery stage `closurestate` (inserted after `astgraft` in `tools/battery.sh` STAGES). Six checks:\n\n1. the two tables are well formed (no duplicate, at least one blank factory, every SHARED entry carries a real reason, no attribute in both);\n2. `capture`/`blank`/`restore` each READ `FIELDS` and name no field of their own (a string constant equal to a field name is a failure);\n3. both declared entry points call `FunctionCodegenState.capture` and `.restore`, and neither keeps a private `saved_x = self.x` beside the record;\n4. every attribute the four prologues initialize is in `FIELDS` or in `SHARED_WITH_NESTED_BODIES`;\n5. every `FIELDS` name is state codegen actually touches (catches a typo);\n6. INJECTION, beside an uninjected control: a `continue` keyed on `SAW_CLOSURE_STATE_DROP` is spliced into `restore`'s loop, the file restored in a `finally` (the `icebreadcrumb`/`corodiscovery` pattern), and dropping either `void_variables` or `variables` must break a real compile of the pin.\n\nBoth negatives verified by hand: deleting `void_variables` from `FIELDS` fires check 4 at all four prologues AND check 6; adding a name the pin does not exercise (`borrowed_variables`) to the injection list fires \"compiled CLEANLY\", which proves the injection discriminates rather than passing by construction.\n\n## Pins\n\n- **SL-356** — `examples/closure_leaves_enclosing_void_local_visible.saw`: the reported shape; a Void local read after TWO nested closures; a Void local declared BETWEEN two closures; the same shape inside a METHOD; the same shape inside a DRIVEN body (`__saw_drive`, the local frame-resident across a `yield_now()`). `EXPECT-ERROR-ABSENT` does not apply — the runner checks it only on the ERROR path, and these are success-path rows whose compile IS the assertion (the bug was a failed compile).\n- **SL-362** — `examples/generic_struct_literal_retains_fields.saw`: an `Arc<Res>` cell (strong counts `1 2 2` -> `1`, one `deinit`), a `String` cell (both owners live), a generic ENUM payload at `Arc<Res>`, a NESTED generic literal (`Outer<T> { inner: Cell<T> }`), a literal built in a DRIVEN body across a suspension, and an ExplicitCopy field with a written `.copy()`. Every count returns to 1 and every `deinit` fires exactly once, so it is a real retain and not a leak. VERIFIED NEGATIVE: with `_struct_field_saw_type` temporarily made to answer nothing for a monomorphized key (the pre-SL-350 behaviour), the pin prints `arc cell: 1 1 1`, then a garbage count, then traps — reverted immediately after. No compiler change was owed for SL-362.\n\n## Gate + battery\n\n- PER-COMMIT, under the machine-wide lock: full suite **2658 passed, 10 xfailed** (the same 10 cited markers, none new, none mine — the two new examples are the +2 over main's 2656) and `tools/freestanding_runner.py` **36 passed across riscv32 + arm64**.\n- TERMINAL FULL BATTERY on the tip, all 33 stages run (split across several foreground invocations under one continuous lock hold, after the harness killed a single 65-minute call mid-`astdiff`): **32 green, 1 failure — `transferdecisions`, PRE-EXISTING**. It reports `sawc/codegen/structs.py:98` and `:100` (`needs_copy` / `payload_needs_copy` assigned without `_stamp_retain`) and fails IDENTICALLY on the main checkout at `455dae1d` with no patch applied — SL-350's surface, not this branch's. Green here: suite, icebreadcrumb, lexdiff, astdiff, astgraft, closurestate, producertaxonomy, corodiscovery, corototality, windowseam, stdseed, citations, forgetgate, ircontract, preludegate, docverify, moduletag, stdtypes, toolchain, floatvectors, abidoc, bttable, fuzz, corodiff, bench, selfhostlex, minivm, reemit, irdet (`--all`), gmgate, bootstrap, freestanding.\n\n## Files touched beyond the brief's list, and why\n\n- `tools/test_ast_graft.py` (+4 lines): the record's `setattr(cg, state_field, ...)` is a computed-name write, which the `astgraft` lane refuses until declared. One `DYNAMIC_SETATTR` entry, keyed on the distinctive loop variable `state_field` (not the generic `name`) so the escape hatch stays narrow.\n- NOT touched: `CLAUDE.md` / `TESTING.md`, whose prose enumerates the battery lanes. `closurestate` is in the tracked `STAGES` and shows in `battery.sh --list`; the lead may want a clause in the CLAUDE.md stage list, left alone here to avoid a conflict on a shared accumulator.\n- `sawc/codegen/methods.py`: NOT modified. The prologues keep their explicit resets and the lane is what binds them to the record — the brief's design. Routing the prologues through `blank()` too is a straightforward follow-up if wanted.\n\n## Filed while here\n\n**SL-365** (backlog): a suspending generic cannot `return` its own local at `R = Void` — `function returns void but return has a value of type Void`, reproduced with NO closure involved; the `R = Int` instantiation and the sync twin both compile. Pre-existing, unrelated to SL-356, and the reason the driven row of the SL-356 pin reads its Void local through a rebinding rather than a `return`. Mechanism and the sibling positions to probe are named in the issue.\n","created":"1790199376","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-356.p1 (revision 1): SL-356 + SL-362: one record for the per-function codegen state a closure saves (closurestate lane with injection; pins for the Void-local ICE and the generic-struct field retain)","created":"1790204071","id":"c3"},{"author":"agent:codex","body":"Requested changes on patch SL-356.p1 (revision 1)\n\nREQUEST CHANGES — SL-356.p1 r1, base 73f55bcb, applied 653dc79d, reconstructed tree 3c6b077e334ea7a78e6df44950c0bc27d4646cbe.\n\nTwo independent Sol slices reviewed production state handling and the gates/pins; parent adjudicated and ran focused validation in a separate checkout. The original Void-local bug is fixed, but two inherited siblings of the same nested-function-state omission remain. These are NOT runtime regressions introduced by r1. They are blocking gaps in the stated whole-per-function-state fix and its sibling census. The two regression/gate findings below are introduced by this patch.\n\n1. [P1] Isolate statement_temps at the closure function boundary.\nLocation: sawc/codegen/closures.py:40-50,301-309; consumers in methods.py:745-806, statements.py:70-82, resources.py:533-543.\n\nThe record omits statement_temps. A closure generated while lowering an outer statement inherits that statement's live temporary list. A diverging closure tail can append its own function's resource alloca, skip the normal tail drain because its block terminated, and leave the outer statement to emit cleanup against it.\n\nParent reproduced on this exact FIXED compiler tree (SL-350 ancestry verified): the following program, which never calls f, exits compilation with -6 / SIGABRT:\n\n```saw\nstruct Resource { id: Int }\nextension Resource: NoCopy {\n    func deinit(&var self) { print(\"drop {}\", self.id) }\n}\nfunc make_resource() sync -> Resource {\n    print(\"made\")\n    Resource(id: 9)\n}\nfunc halt(value: Int) sync -> Never { panic(\"halt {}\", value) }\nfunc main() {\n    let f: () sync -> Int = { halt(make_resource().id) }\n    print(\"not invoked\")\n    let _ = move f\n}\n```\n\nObserved diagnostic:\n```\nInstruction does not dominate all uses!\n  %stmt_temp = alloca { ptr, ptr, ptr }, align 8, !dbg !189\n  call void @Resource_deinit(ptr %stmt_temp), !dbg !187\nLLVM ERROR: Broken module found, compilation aborted!\n```\n\nNo baseline compiler was run, and this failing backend path was not repeated. The correct program compiles and prints only `not invoked`.\n\nBring this context into the one record. Its policy is not the current generic `list` copy/reset policy: save the enclosing list by identity, start a closure body with None, and restore the original object. `_generate_block` may retain that object in its local `tail_temps`; restoring a copy can disconnect later outer temporaries from the list that will be drained. Capture policy and fresh-body state therefore need to be expressible independently. Add a boundary regression and cover subsequent outer temporary cleanup, not just the original Void name.\n\n2. [P1] Clear the enclosing catch target when emitting a closure body.\nLocation: closures.py:40-50,281-309; results.py:325-379,502-524.\n\nThe record also omits `_catch_context`. A closure constructed inside an outer try/catch inherits its catch block, mutable error-allocation holder, and cleanup depth. Direct propagation inside the closure must instead return that closure's Result. The checker already treats the closure as its own return/catch context.\n\nParent used this contextually typed closure (a function-typed let alone does not supply the same expected return context in this compiler):\n\n```saw\nstruct E { code: Int }\nextension E: Printable {\n    func format(&self, into: &var StringBuilder) { try! into.append(self.code) }\n}\nextension E: Error {}\nfunc fail() sync -> Result<Int, E> { E(code: 7) }\nfunc invoke(body: () sync -> Result<Int, E>) sync -> Result<Int, E> { body() }\nfunc main() {\n    try {\n        let result = invoke({ return try fail() })\n        match result {\n            case Ok(value) -> { print(\"ok {}\", value) },\n            case Err(error) -> { print(\"closure err {}\", error.code) }\n        }\n        print(\"outer continued\")\n    } catch {\n        print(\"wrong outer catch\")\n    }\n}\n```\n\nSafe validation after the first probe's abort: `sawc ... -O0 --emit-ir` succeeded; this mode returns raw IR without LLVM optimization or object emission. The emitted closure `__closure$main$12_29` contains `br label %\"catch\"`, but that label exists only in main. A parser-only check rejected it with:\n```\nLLVM IR parsing error\n<string>:2370:12: error: use of undefined value '%catch'\n  br label %\"catch\"\n```\n\nNo native compilation/execution was attempted for this case. Correct output is `closure err 7` then `outer continued`, never `wrong outer catch`. Save/reset/restore the catch context through the same record and pin this direct-propagation shape; the existing closure-with-its-own-inner-catch case masks the leak.\n\nThe new closurestate lane PASSES despite both omissions: its prologue census sees only direct assignments in methods.py, whereas these function-bound contexts are initialized/managed elsewhere. Update the sibling inventory and behavioral boundary checks along with the record; do not equate the prologue-only set with all nested-function state.\n\n3. [P2] Use an owned String in the SL-362 String-retain pin.\nLocation: examples/generic_struct_literal_retains_fields.saw:56-62.\n\n`let s = \"hello\"` is an immortal literal. Parent's actual emitted IR contains a private String global initialized with refcount -1; core.py:1076-1094 makes retain and release no-ops for that sentinel. Suppressing this String field's retain would leave the row green, and its comment that the last release frees the text is false. The Arc rows are useful, but SL-362 explicitly owes both Arc and String cells.\n\nUse a genuinely heap-owned String, exercise the original owner after the cell's early drop, and demonstrate that a String-specific missing-field-retain mutation fails. No new implementation fix to SL-350's field-type lookup is requested. Also correct the nested-row comment at 82-90: the inner Cell retains a; the fresh Cell transfers into Outer without another retain, consistent with the expected count of 2.\n\n4. [P2] Constrain the new dynamic setattr exemption to its audited sites.\nLocation: tools/test_ast_graft.py:111-114,298-300.\n\nThe new `state_field` allowance is global: the gate looks up only the unparsed attribute-name expression, not its module, receiver, or owning helper. It therefore permits an undeclared AST graft anywhere under sawc if the local happens to be named state_field, although the stated exception is for FunctionCodegenState writing generator state.\n\nParent ran the actual gate over an in-memory source mutation in codegen/core.py, using this exact revision's AST schema; no source file was modified:\n```\ndef __review_graft_probe(node: Identifier):\n    state_field = \"__review_undeclared_annotation\"\n    setattr(node, state_field, True)\n```\nWith an unlisted local name, the gate exits 1 and reports the graft. With `state_field`, the same undeclared write exits 0 / “zero grafted writes”. Scope this new exemption to the intended module and state-record operations/receiver. No broad rewrite of the legacy allowlist is requested.\n\nPositive verification and limits:\n- Both new regression programs passed (2/2).\n- Existing nested-return, closure copy/move, environment-drop and coroutine cleanup coverage passed (13/13).\n- closurestate passed its uninjected control and both dropped-field injections; its temporary source edit was restored byte-for-byte.\n- astgraft passed on the unmodified exact patch in a fresh process.\n- Listed-field capture/blank/restore symmetry, current-return-type restoration, lexical save stacking and the current environment-destructor path have no independent finding.\n- Full battery was not rerun. Author c2 reports 32/33 stages green, with the pre-existing SL-371 transferdecisions failure; the server gate is still pending at this review.\n- The first in-process allowlist probe loaded an older AST module from the persistent kernel; that result was discarded, then repeated with the exact revision schema to obtain the control-fails/mutant-passes result above.\n- Hypothetical wrong factories or an invented third emitter were not promoted into additional blocking findings. The actual omitted contexts above are the actionable census failures.\n\nNo implementation edits, baseline execution, approval, or merge. Please revise the one-record mechanism and its regression/gate coverage; this review remains bound to r1/653dc79d.","created":"1790205888","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-356.p1 (revision 1)","created":"1790206220","id":"c5"},{"author":"agent:claude-sawlang","body":"Revised patch SL-356.p1 to revision 2","created":"1790255240","id":"c6"},{"author":"agent:claude-sawlang","body":"SL-356.p1 r2 submitted: one commit, 9490818e, on origin/main a2fc09b3. The only change since then on main is SL-370's statements.py hunk, which does not overlap.\n\nAnswers to c4:\n1. statement_temps joins the record. It is captured by identity (`_generate_block` keeps a live alias to the list), and a fresh body starts with None.\n   - Pin: examples/closure_body_owns_its_own_statement_temporaries.saw. On main it aborts with codex's dominance failure (rc=-6); on the branch it passes.\n   - It also covers a temporary in the same statement, a later statement's temporary, nested bodies, and a tail temporary built after the literal.\n   - Flipping this entry to a snapshot makes the pin fail, and the lane runs that flip as an injection.\n2. _catch_context joins the record, captured by identity, and a fresh body starts with None.\n   - Pin: examples/closure_propagates_to_its_own_result_not_the_enclosing_catch.saw. On main it is an ICE on `%catch`; on the branch it prints `closure err 7` then `outer continued`. It also covers a tail try, and a closure with its own catch followed by outer propagation.\n   - loop_stack joins too.\n   - The census is widened. The closurestate lane classifies all 101 generator attributes: FIELDS; SHARED_WITH_NESTED_BODIES, each with a reason; or lane-side MODULE_STATE, in groups by reason.\n   - A prologue reset, a save/restore pair or a push/pop cannot be classified as module state. Dropping statement_temps+_catch_context, loop_stack alone, or borrowed_variables alone from FIELDS fails the lane.\n   - The lane runs five injections, each beside an uninjected control.\n3. The String pin uses a heap-owned String, drops the cell early, and then reads the original owner.\n   - Mutation proof: making the struct-literal field-copy check return False for String fields fails the new pin (garbage, then SIGBUS). r1's literal version passes under the same mutation.\n   - The nested-row comment is corrected (the inner Cell retains a; the outer takes it by transfer; the count is 2).\n4. The astgraft exemption is keyed on (file, enclosing class, receiver, name): closures.py / FunctionCodegenState / cg / state_field. Each of these mutants is now reported: codex's core.py mutant, the same with receiver cg, a module-level function in closures.py, and another class in closures.py.\n\nImplementer's terminal battery: 33/33 green, 4290s.\n- suite: 2660 passed, 10 xfailed.\n- Every other stage passed: icebreadcrumb, lexdiff, astdiff, astgraft, closurestate, transferdecisions, producertaxonomy, corodiscovery, corototality, windowseam, stdseed, citations, forgetgate, ircontract, preludegate, docverify, moduletag, stdtypes, toolchain, floatvectors, abidoc, bttable, fuzz, corodiff, bench, selfhostlex, minivm, reemit, irdet --all, gmgate, bootstrap.\n- freestanding: 36 passed across both arches.\n\nLead validation (review plus spot checks, no second suite run):\n- Read the full production diff.\n- Verified no codegen site holds a local alias to a snapshot-restored container, so restoring copies is safe.\n- Reran the four pins, the closurestate lane and astgraft on 9490818e: all green, clean tree.\n","created":"1790255255","id":"c7"}],"created":"1790176367","description":"REPORTED by the user, Sep 23; REPRODUCED on main 73dd98b8 (`.build/scratch/void_closure.saw`): `func keep<R>(make: () -> R) -> R { let r = make()  let later = { n: Int in n * 2 }  print(\"{}\", later(3))  return r }` with `keep({ print(\"hi\") })` dies with `internal compiler error … (Identifier): Undefined variable: r` at the `return r`; without the closure literal it compiles and runs. MECHANISM (obligation 4, named): `sawc/codegen/closures.py` saves the enclosing function's per-function codegen state before generating a closure body (lines 215-221: `variables`, `variable_types`, `cleanup_stack`, `drop_flags`, `moved_variables`, `borrowed_variables`; 234: `current_return_type`) and restores it after (420-427) — a HAND-MAINTAINED list — but `void_variables` (design 132's zero-sized-binding set, added later) is RESET at line 251 with no save and no restore, so a Void-typed local of the enclosing function (here `r` at `R = Void`) is forgotten past the first nested closure. SIBLINGS the mechanism reaches: every other per-function attribute a named function's prologue (methods.py) initializes that the closure list omits — the fix enumerates them (grep the prologue's `self.<x> = ` set against 215-227/420-427) and turns the pair into ONE saved-state record (a small dataclass or a tuple built from one list) so a future field cannot be added to one side only; the second closure-generation site at closures.py:643-664 (saves only the builder) is checked for the same omission. FIX: save/restore `void_variables` (and any sibling found) through that one record; pin = this program + a Void local read after TWO nested closures + a Void local declared BETWEEN two closures + the same shape inside a method and inside a driven body; a lane/assert that the prologue's state set and the closure's saved set are the same set (the astgraft/producertaxonomy pattern). Small; codegen/closures.py is outside SL-350's surface.","id":"SL-356","labels":["bug","codegen","closures"],"order":5,"parent":"SL-367","priority":"normal","project":"SL","queue_order":0,"revision":11,"sequence":1430,"stage":"queued","status":"open","title":"ICE: codegen forgets the enclosing function's Void-typed locals after any nested closure — closures.py resets void_variables without saving/restoring it (one member missing from a hand-maintained save/restore list)","updated":"1790255255"},"id":"SL-356","project":"SL","request":"{\"action\":\"patch_test_result\",\"actor\":\"agent:sawtracker-tests\",\"commit\":\"36cb0510\",\"id\":\"SL-356.p1\",\"output\":\"[2655/2671] ✓ while_expr_conditional_found\\n[2656/2671] ✓ while_expr_conditional_not_found\\n[2657/2671] ✓ visibility_public\\n\\nCompiles complete in 526.7s (0/2671 reused, 0%) — 988 settled without running, 1683 binaries queued for execution (1669 of them already run).\\n\\n[2658/2671] ✓ while_infinite\\n[2659/2671] ✓ while_expr_infinite\\n[2660/2671] ✓ while_let_drain\\n[2661/2671] ✓ while_let_channel_drain\\n[2662/2671] ✓ while_never_break_forms\\n[2663/2671] ✓ while_never_diverges\\n[2664/2671] ✓ while_nested\\n[2665/2671] ✓ wrapping_operators\\n[2666/2671] ✓ while_simple\\n[2667/2671] ✓ while_let_suspending_mt\\n[2668/2671] ✓ while_let_suspending_try_catch\\n[2669/2671] ✓ while_never_sync_context\\n[2670/2671] ✓ wrapping_and_reference_mix\\n[2671/2671] ✓ while_let_suspending\\n\\ncompile 526.7s + 4.4s draining = 531.0s total\\n\\n======================================================================\\nTest Results\\n======================================================================\\n\\nKNOWN FAILURES (xfail):\\n  x K141_mutex_guarded_for_is_sync: SL-340 — `Mutex(value: move v)` at a HEAP-OWNING payload frees the\\n  x V96_driven_capture_duplicates_once: SL-267 — a driven body's NON-ESCAPING closure capture duplicates\\n  x closure_escaping_move_capture_consumed_once: DF-255a an escaping closure's env destructor releases a capture the\\n  x closure_nested_ref_param_capture: DF-248b a nested closure captures an enclosing closure's reference\\n  x closure_shorthand_parameter_inside_a_try: DF-257d the `$0` shorthand is invisible to the implicit-parameter scan\\n  x coro_value_inline_catch_hosts_a_move: SL-231 a value-carrying INLINE catch cannot host a `move` of a frame\\n  x funcpointer_call_in_a_driven_body: DF-252a a `FuncPointer` value called by NAME inside a driven body is\\n  x generic_init_moved_parameter_is_released_once: DF-302b — a generic extension's `init` releases a parameter it MOVED\\n  x primitive_type_called_as_a_function: DF-270e — a PRIMITIVE TYPE NAME written as a ZERO-ARGUMENT call is an\\n  x trailing_closure_inside_a_try_operand: DF-259c — a trailing closure is not recognized inside a `try`/`try!`/\\n\\n======================================================================\\nALL TESTS PASSED (2661 passed, 10 xfailed)\\n======================================================================\\n\\nPublished test_runner_1790255244_55391 as test_runner_last (1625 manifest entries).\\nFreestanding QEMU tests\\n  clang: /opt/homebrew/opt/llvm/bin/clang\\n  lld  : /opt/homebrew/bin/ld.lld\\n  qemu : /opt/homebrew/bin/qemu-system-riscv32\\n  qemu : /opt/homebrew/bin/qemu-system-aarch64\\n\\nbuilding blade\\n\\nriscv32  (riscv32-unknown-none-elf, qemu-system-riscv32 `virt`)\\n[1/19] ✓ hello\\n[2/19] ✓ int_widths\\n[3/19] ✓ struct_layout\\n[4/19] ✓ call_convention\\n[5/19] ✓ module_compose\\n[6/19] ✓ runtime_seams\\n[7/19] ✓ runtime_panic\\n[8/19] ✓ runtime_export_ungated\\n[9/19] ✓ runtime_seam_signature\\n[10/19] ✓ hidden_alloc_interpolation\\n[11/19] ✓ hidden_alloc_escaping_closure\\n[12/19] ✓ hidden_alloc_print_printable\\n[13/19] ✓ format_no_alloc\\n[14/19] ✓ align_attribute\\n[15/19] ✓ export_with_coroutine\\n[16/19] ✓ float_text\\n[17/19] ✓ link_address\\n[18/19] ✓ blade_target\\n[19/19] ✓ wide_value_rendering\\n\\narm64  (aarch64-unknown-none-elf, qemu-system-aarch64 `virt`)\\n[1/17] ✓ hello\\n[2/17] ✓ int_widths\\n[3/17] ✓ struct_layout\\n[4/17] ✓ call_convention\\n[5/17] ✓ module_compose\\n[6/17] ✓ runtime_seams\\n[7/17] ✓ runtime_panic\\n[8/17] ✓ runtime_export_ungated\\n[9/17] ✓ runtime_seam_signature\\n[10/17] ✓ hidden_alloc_interpolation\\n[11/17] ✓ hidden_alloc_escaping_closure\\n[12/17] ✓ hidden_alloc_print_printable\\n[13/17] ✓ format_no_alloc\\n[14/17] ✓ export_with_coroutine\\n[15/17] ✓ float_text\\n[16/17] ✓ link_address\\n[17/17] ✓ blade_target\\n\\n============================================================\\nALL FREESTANDING TESTS PASSED (36 passed across riscv32 + arm64)\\n============================================================\\n\",\"request_id\":\"tests-36cb0510-r2-e1727\",\"revision\":2,\"tests\":\"passed\"}","request_id":"tests-36cb0510-r2-e1727","sequence":1431,"timestamp":"1790255863"}
---
