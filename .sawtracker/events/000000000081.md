---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791230","description":"## Scope and status\n\nDF-306a: Design alignment for coroutine-frame-resident locals\n\nv1 deliberately refuses aligned coroutine-frame locals. Design frame storage alignment; do not weaken the refusal before the layout contract is supported.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reconcile the linked brief and recorded rulings with current consumers. Keep already-landed behavior and stated deferrals explicit.\n2. Draft the remaining contract: supported syntax/API, ownership and failure behavior, alternatives, compatibility effects and the exact questions still requiring a decision.\n3. Define small implementation units and consumer migrations, then specify the positive, negative and boundary tests before dispatch.\n\n## Acceptance criteria\n\n- [ ] The design names a concrete consumer or retains its recorded revisit trigger.\n- [ ] Existing rulings are preserved; unresolved choices and a recommended option are explicit.\n- [ ] The proposed API/semantics, migration scope and acceptance tests are reviewable. Drafting this plan does not mark an unruled design approved.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 240–271\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L240-L271)\n\n## DF‑306a — a coroutine-frame-RESIDENT local cannot carry an alignment, so\n## `@align(N)` refuses there rather than dropping it (filed Sep 5 by the\n## `@align` v1 landing; DESIGN GAP, not a bug — v1's honest answer)\n\nA local whose scope spans a suspension stops being an alloca: `_FrameBuilder.\n_collect_frame_locals` carries it as a bare `(name, SawType)` pair into a\nsynthesized `__Frame_*` `Struct`, which is lowered by the ordinary\ndeclaration-order struct path. There is no `StructField` alignment, no packed\nor aligned struct lowering, and — for a spawned root — the frame is then heap\nallocated through `__saw_rt_alloc`, whose `align` argument `sawc/rt/ABI.md`\ndocuments as currently ignored. So an `@align` on such a local has nowhere to\ntravel, and v1 REFUSES it where it is written (`_reject_frame_align`) rather\nthan accepting it and emitting a 1-aligned field — which is precisely the\nsilent-loss failure DF‑300b was filed about.\n\nMECHANISM (obligation 4) and why the refusal is keyed where it is: the trigger\nis frame RESIDENCY, not \"does this function suspend\", and those differ. The\nrefusal sits ON the promotion decision itself, so all three residency reasons\nreach it — a scope spanning a suspension, DF‑218s's owning local of a\n`return`-containing block (which needs no suspension crossing it at all), and\nDF‑245d's propagating-`try` binding. A local in a suspending function that is\nNOT promoted keeps its slot and its alignment; that half is pinned by\n`examples/align_nonresident_local_in_suspending_function.saw`, the refusal by\nconformance row N11.\n\nFIX SHAPE, if it is ever wanted: three contracts, not one — an alignment on\n`StructField` plus a struct lowering that honours it, the `(name, SawType)`\npair widened to carry it, and an allocator that honours `align`. Worth doing\nwith the type-carried half of DF‑300b rather than alone, since a frame field\nholding an aligned TYPE is the same layout question.\n[DF‑300b, 218 (DF‑218s), 234 (DF‑245d), rt/ABI.md]\n","id":"SL-81","labels":["todo-import","design","plan","design-proposal","backlog"],"priority":"normal","project":"SL","revision":1,"sequence":81,"status":"open","title":"DF-306a: Design alignment for coroutine-frame-resident locals","updated":"1788791230"},"before":null,"id":"SL-81","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-306a: Design alignment for coroutine-frame-resident locals\\n\\nv1 deliberately refuses aligned coroutine-frame locals. Design frame storage alignment; do not weaken the refusal before the layout contract is supported.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reconcile the linked brief and recorded rulings with current consumers. Keep already-landed behavior and stated deferrals explicit.\\n2. Draft the remaining contract: supported syntax/API, ownership and failure behavior, alternatives, compatibility effects and the exact questions still requiring a decision.\\n3. Define small implementation units and consumer migrations, then specify the positive, negative and boundary tests before dispatch.\\n\\n## Acceptance criteria\\n\\n- [ ] The design names a concrete consumer or retains its recorded revisit trigger.\\n- [ ] Existing rulings are preserved; unresolved choices and a recommended option are explicit.\\n- [ ] The proposed API/semantics, migration scope and acceptance tests are reviewable. Drafting this plan does not mark an unruled design approved.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 240–271\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L240-L271)\\n\\n## DF‑306a — a coroutine-frame-RESIDENT local cannot carry an alignment, so\\n## `@align(N)` refuses there rather than dropping it (filed Sep 5 by the\\n## `@align` v1 landing; DESIGN GAP, not a bug — v1's honest answer)\\n\\nA local whose scope spans a suspension stops being an alloca: `_FrameBuilder.\\n_collect_frame_locals` carries it as a bare `(name, SawType)` pair into a\\nsynthesized `__Frame_*` `Struct`, which is lowered by the ordinary\\ndeclaration-order struct path. There is no `StructField` alignment, no packed\\nor aligned struct lowering, and — for a spawned root — the frame is then heap\\nallocated through `__saw_rt_alloc`, whose `align` argument `sawc/rt/ABI.md`\\ndocuments as currently ignored. So an `@align` on such a local has nowhere to\\ntravel, and v1 REFUSES it where it is written (`_reject_frame_align`) rather\\nthan accepting it and emitting a 1-aligned field — which is precisely the\\nsilent-loss failure DF‑300b was filed about.\\n\\nMECHANISM (obligation 4) and why the refusal is keyed where it is: the trigger\\nis frame RESIDENCY, not \\\"does this function suspend\\\", and those differ. The\\nrefusal sits ON the promotion decision itself, so all three residency reasons\\nreach it — a scope spanning a suspension, DF‑218s's owning local of a\\n`return`-containing block (which needs no suspension crossing it at all), and\\nDF‑245d's propagating-`try` binding. A local in a suspending function that is\\nNOT promoted keeps its slot and its alignment; that half is pinned by\\n`examples/align_nonresident_local_in_suspending_function.saw`, the refusal by\\nconformance row N11.\\n\\nFIX SHAPE, if it is ever wanted: three contracts, not one — an alignment on\\n`StructField` plus a struct lowering that honours it, the `(name, SawType)`\\npair widened to carry it, and an allocator that honours `align`. Worth doing\\nwith the type-carried half of DF‑300b rather than alone, since a frame field\\nholding an aligned TYPE is the same layout question.\\n[DF‑300b, 218 (DF‑218s), 234 (DF‑245d), rt/ABI.md]\\n\",\"labels\":[\"todo-import\",\"design\",\"plan\",\"design-proposal\",\"backlog\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-f2be0c28b5b9ad2c04c3-create\",\"title\":\"DF-306a: Design alignment for coroutine-frame-resident locals\"}","request_id":"todo-20260907-f2be0c28b5b9ad2c04c3-create","sequence":81,"timestamp":"1788791230"}
---
