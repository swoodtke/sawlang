---
{"action":"create","actor":"agent:claude-sawtracker","after":{"assignee":"","author":"agent:claude-sawtracker","comments":[],"created":"1788904325","description":"TcpStream reads park the task on the reactor via io_wait with no way to bound the wait, so a connection that never sends data (e.g. a browser preconnect socket) occupies its handler until the peer closes. Sawtracker (ST-7) had 30+ second page stalls from worker-pool starvation and worked around it by raising its pool to 64 workers. SO_RCVTIMEO does not apply to reactor-parked nonblocking fds. Request: a deadline-carrying io_wait (or read-with-timeout on TcpStream) so servers can enforce idle-connection timeouts; the existing sleep-deadline wakeups in the reactor look like a natural attachment point.","id":"SL-204","labels":["runtime","network"],"priority":"normal","project":"SL","revision":1,"sequence":250,"stage":"queued","status":"open","title":"io_wait needs a deadline: servers cannot time out idle connections","updated":"1788904325"},"before":null,"id":"SL-204","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawtracker\",\"description\":\"TcpStream reads park the task on the reactor via io_wait with no way to bound the wait, so a connection that never sends data (e.g. a browser preconnect socket) occupies its handler until the peer closes. Sawtracker (ST-7) had 30+ second page stalls from worker-pool starvation and worked around it by raising its pool to 64 workers. SO_RCVTIMEO does not apply to reactor-parked nonblocking fds. Request: a deadline-carrying io_wait (or read-with-timeout on TcpStream) so servers can enforce idle-connection timeouts; the existing sleep-deadline wakeups in the reactor look like a natural attachment point.\",\"labels\":[\"runtime\",\"network\"],\"project\":\"SL\",\"request_id\":\"sl-iowait-deadline\",\"stage\":\"queued\",\"title\":\"io_wait needs a deadline: servers cannot time out idle connections\"}","request_id":"sl-iowait-deadline","sequence":250,"timestamp":"1788904325"}
---
