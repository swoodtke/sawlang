---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl262","closed":"1790341667","comments":[{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \"oracle known wrong\" reference. Test-coverage aspect: `closure.break-continue`. Its test-plan input is carried in SL-393 (the rule inventory).","created":"1790341667","id":"c1","kind":"landing"}],"created":"1789403981","description":"Found by the SL-262 review-r1 obligation-4 sweep (which loop does a `break`/`continue` written in a nested construct target?), Sep 14 2026. INDEPENDENT of SL-262 and not fixed by it.\n\nSYMPTOM: a `break` or `continue` written inside a CLOSURE BODY, where the closure is created inside a loop, is an INTERNAL COMPILER ERROR rather than a clean diagnostic:\n\n    error: internal compiler error: LLVM IR parsing error\n    <string>:3600:12: error: use of undefined value '%while.cond'\n      br label %\"while.cond\"\n               ^\n\nThat is the fuzz oracle's rule broken (design 192: the compiler succeeds or exits with a clean diagnostic; an `internal compiler error` is a finding), and the message names nothing the author wrote.\n\nREPRO (both halves ICE; `run` is any function taking a closure):\n\n    func run(body: () sync -> Int) -> Int { body() }\n\n    func main() {\n        var n = 0\n        var log = 0\n        while n < 3 {\n            n += 1\n            log += run({ if n == 1 { continue } else { 5 } })\n        }\n        print(\"n {n} log {log}\")\n    }\n\nReplace `continue` with `break` for the second half — same ICE, same message.\n\nMECHANISM (named, per obligation 4): a closure body is lowered as its OWN function, so it has no enclosing loop and therefore no `while.cond` / loop-exit block to branch to. Codegen emits the branch anyway — the jump is lowered against the loop context that was current when the closure LITERAL was encountered, not against the function the body actually becomes — and LLVM rejects the reference to a label that exists in a different function. Nothing refuses the construct earlier: the parser accepts it, and the typechecker's `loop_depth` is still the enclosing loop's while it checks the closure body, so the \"break outside a loop\" check that would have caught it never fires.\n\nWHAT THE ANSWER SHOULD BE: a clean refusal at the jump. Design 213 already ruled the analogous question for `return` — a `return` in a closure returns FROM THE CLOSURE, checked against the closure's own return type, with the enclosing function's signature having no say — and it noted that `try` propagates out of the closure on the same boundary. `break`/`continue` have no closure-level meaning at all (a closure is not a loop), so unlike `return` there is nothing to rebind them to; the boundary ruling that makes `return` local makes these two an error. Suggested wording in that family: \"`continue` is not valid inside a closure body — a closure is not a loop; the enclosing loop's `continue` cannot cross the closure boundary.\"\n\nSCOPE PROBED. Both keywords, in a closure passed directly to a non-escaping parameter, inside a `while` loop. The same shape OUTSIDE any loop is the ordinary \"outside a loop\" error and is unaffected. The neighbouring positions the same sweep covered are all fine and are NOT this bug: a `continue`/`break` in a nested `for`'s ITERABLE targets the OUTER loop and runs correctly, and one in a nested `while`'s CONDITION or a `while let`'s SCRUTINEE binds to the INNER loop and runs correctly (the `continue` there spins forever, which is correct-if-surprising behaviour for a jump to a head that re-evaluates, not a miscompile).\n\nINTERACTION WITH SL-262 (none, recorded so a fixer does not go looking): SL-262's back-edge predicate `_body_continues_to_this_loop` deliberately does not special-case closure bodies. Writing a `continue` in one is this ICE, so no program that COMPILES can reach the question, and counting such a `continue` as targeting the enclosing loop only ever adds a refusal to a program that cannot build either way. When this issue is fixed as a clean refusal, that stays true.\n","id":"SL-286","labels":["frozen"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":3,"sequence":2135,"stage":"backlog","status":"closed","title":"ICE: break/continue inside a closure body emits a branch to a label in another function","updated":"1790341667"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sl262","closed":"","comments":[],"created":"1789403981","description":"Found by the SL-262 review-r1 obligation-4 sweep (which loop does a `break`/`continue` written in a nested construct target?), Sep 14 2026. INDEPENDENT of SL-262 and not fixed by it.\n\nSYMPTOM: a `break` or `continue` written inside a CLOSURE BODY, where the closure is created inside a loop, is an INTERNAL COMPILER ERROR rather than a clean diagnostic:\n\n    error: internal compiler error: LLVM IR parsing error\n    <string>:3600:12: error: use of undefined value '%while.cond'\n      br label %\"while.cond\"\n               ^\n\nThat is the fuzz oracle's rule broken (design 192: the compiler succeeds or exits with a clean diagnostic; an `internal compiler error` is a finding), and the message names nothing the author wrote.\n\nREPRO (both halves ICE; `run` is any function taking a closure):\n\n    func run(body: () sync -> Int) -> Int { body() }\n\n    func main() {\n        var n = 0\n        var log = 0\n        while n < 3 {\n            n += 1\n            log += run({ if n == 1 { continue } else { 5 } })\n        }\n        print(\"n {n} log {log}\")\n    }\n\nReplace `continue` with `break` for the second half — same ICE, same message.\n\nMECHANISM (named, per obligation 4): a closure body is lowered as its OWN function, so it has no enclosing loop and therefore no `while.cond` / loop-exit block to branch to. Codegen emits the branch anyway — the jump is lowered against the loop context that was current when the closure LITERAL was encountered, not against the function the body actually becomes — and LLVM rejects the reference to a label that exists in a different function. Nothing refuses the construct earlier: the parser accepts it, and the typechecker's `loop_depth` is still the enclosing loop's while it checks the closure body, so the \"break outside a loop\" check that would have caught it never fires.\n\nWHAT THE ANSWER SHOULD BE: a clean refusal at the jump. Design 213 already ruled the analogous question for `return` — a `return` in a closure returns FROM THE CLOSURE, checked against the closure's own return type, with the enclosing function's signature having no say — and it noted that `try` propagates out of the closure on the same boundary. `break`/`continue` have no closure-level meaning at all (a closure is not a loop), so unlike `return` there is nothing to rebind them to; the boundary ruling that makes `return` local makes these two an error. Suggested wording in that family: \"`continue` is not valid inside a closure body — a closure is not a loop; the enclosing loop's `continue` cannot cross the closure boundary.\"\n\nSCOPE PROBED. Both keywords, in a closure passed directly to a non-escaping parameter, inside a `while` loop. The same shape OUTSIDE any loop is the ordinary \"outside a loop\" error and is unaffected. The neighbouring positions the same sweep covered are all fine and are NOT this bug: a `continue`/`break` in a nested `for`'s ITERABLE targets the OUTER loop and runs correctly, and one in a nested `while`'s CONDITION or a `while let`'s SCRUTINEE binds to the INNER loop and runs correctly (the `continue` there spins forever, which is correct-if-surprising behaviour for a jump to a head that re-evaluates, not a miscompile).\n\nINTERACTION WITH SL-262 (none, recorded so a fixer does not go looking): SL-262's back-edge predicate `_body_continues_to_this_loop` deliberately does not special-case closure bodies. Writing a `continue` in one is this ICE, so no program that COMPILES can reach the question, and counting such a `continue` as targeting the enclosing loop only ever adds a refusal to a program that cannot build either way. When this issue is fixed as a clean refusal, that stays true.\n","id":"SL-286","labels":["frozen"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":2134,"stage":"backlog","status":"open","title":"ICE: break/continue inside a closure body emits a branch to a label in another function","updated":"1790341667"},"id":"SL-286","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \\\"oracle known wrong\\\" reference. Test-coverage aspect: `closure.break-continue`. Its test-plan input is carried in SL-393 (the rule inventory).\",\"id\":\"SL-286\",\"request_id\":\"pid-56013-1790341667-7000\"}","request_id":"pid-56013-1790341667-7000","sequence":2135,"timestamp":"1790341667"}
---
