---
{"action":"create","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790359514","description":"## Follow-ups to the bootstrap subset checker\n\nFrom the Air's approval of SL-399.p1 r6 (SL-399 c26; probes in chat file f5). None blocks, and each is small.\n\n1. **An overloaded method's result goes untraced, even when the call picks one overload.** `Doc.cursor()` returns `Cursor` and `cursor(offset:)` returns `Int`, so `let c = d.cursor()` leaves `c` untraced. Cursor's own `peek`, `next` and `insert` are then refused as std calls (probe o1; Stage 0 prints `3 4 5 3`). Pick the overload by argument count and labels, and trace its result. `peek` and `next` are lexer and parser vocabulary.\n2. **L18 (`generic-extension-params`) misses `extension Result {`,** which crashes Stage 0 with `internal compiler error: 'Result'` (probe g3). `sawc/std` does not declare `Optional` or `Result` as generic, so `is_generic_type` misses them. Stage 0 refuses `extension Optional {` cleanly.\n3. **Cosmetic:** under the (line, rule, message) key, one `UnsafePointer` token draws two `raw-pointer` messages plus the `unsafe` one (probe d10, and the `raw-pointer.saw` fixture).\n","id":"SL-405","labels":["compiler"],"order":0,"parent":"SL-398","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":2611,"stage":"backlog","status":"open","title":"Subset checker follow-ups from the SL-399 review","updated":"1790359514"},"before":null,"id":"SL-405","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sawlang\",\"description\":\"## Follow-ups to the bootstrap subset checker\\n\\nFrom the Air's approval of SL-399.p1 r6 (SL-399 c26; probes in chat file f5). None blocks, and each is small.\\n\\n1. **An overloaded method's result goes untraced, even when the call picks one overload.** `Doc.cursor()` returns `Cursor` and `cursor(offset:)` returns `Int`, so `let c = d.cursor()` leaves `c` untraced. Cursor's own `peek`, `next` and `insert` are then refused as std calls (probe o1; Stage 0 prints `3 4 5 3`). Pick the overload by argument count and labels, and trace its result. `peek` and `next` are lexer and parser vocabulary.\\n2. **L18 (`generic-extension-params`) misses `extension Result {`,** which crashes Stage 0 with `internal compiler error: 'Result'` (probe g3). `sawc/std` does not declare `Optional` or `Result` as generic, so `is_generic_type` misses them. Stage 0 refuses `extension Optional {` cleanly.\\n3. **Cosmetic:** under the (line, rule, message) key, one `UnsafePointer` token draws two `raw-pointer` messages plus the `unsafe` one (probe d10, and the `raw-pointer.saw` fixture).\\n\",\"labels\":[\"compiler\"],\"parent\":\"SL-398\",\"project\":\"SL\",\"request_id\":\"pid-49145-1790359514-5000\",\"title\":\"Subset checker follow-ups from the SL-399 review\"}","request_id":"pid-49145-1790359514-5000","sequence":2611,"timestamp":"1790359514"}
---
