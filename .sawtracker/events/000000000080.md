---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791229","description":"## Scope and status\n\nDF-302b: Prevent moved initializer parameters from being released twice\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n4. Use counted ownership events and, where applicable, an address sanitizer; compare sync/driven and owning/copyable controls. Check error, early-return and teardown edges.\n5. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n- [ ] Each owned resource is released exactly once at the required lifetime boundary, with no lost writes or stale owner.\n\n## Existing design references\n\n- [sawlang/designs/207-constructor-inference.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/207-constructor-inference.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 130–133\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L130-L133)\n\n- DF‑302b — a generic extension's `init` RELEASES a parameter it moved into the built value, so the value is torn down TWICE (entry below, filed Sep 4 by design 207's agent; PRE-EXISTING at HEAD, reproduced with fully explicit type arguments and no inference). Pinned by `examples/generic_init_moved_parameter_is_released_once.saw`. A DF‑217m/DF‑251b sibling — the same init-cleanup analysis, one case further on\n\n### sawlang TODO lines 134–179\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L134-L179)\n\n## DF‑302b — a generic extension's `init` RELEASES a parameter it MOVED into the\n## built value, so the value is torn down TWICE (filed Sep 4 by design 207's\n## agent; PRE-EXISTING at HEAD, NOT design 207's and NOT fixed by it)\n\n```saw\nstruct Res { id: Int }\nextension Res: NoCopy { func deinit(&var self) { print(\"gone {self.id}\") } }\nstruct Wrap<T> { value: T }\nextension Wrap<T> { init(from: T) -> Wrap<T> { Wrap<T>(value: from) } }\n// let w = Wrap<Res>(from: move r);  print(w.value.id)\n```\n\nWanted `7` / `gone 7`. Got `gone 7` / `7` / `gone 7` — released BEFORE the read,\nso `w.value.id` reads freed storage and prints 7 by luck, then the caller\nreleases it again. Exit 0, silently.\n\nMECHANISM (obligation 4). The generated generic `init` aggregates the parameter\ninto the returned struct with an `insertvalue` and never clears that\nparameter's drop flag, so the init epilogue runs `deinit` on storage the caller\nnow owns:\n\n```\n%\".5\" = insertvalue %\"Wrap$1$Res\" undef, %\"Res\" %\"from.2\", 0\n%\"from.needsdrop\" = load i1, i1* %\"from.dropflag\"     ; still 1\ndrop.from:  call void @\"Res_deinit\"(%\"Res\"* %\"from.1\")\n```\n\nThe memberwise-aggregation form of \"moved into the built value\" is not counted\nby the init-cleanup analysis DF‑217m added and DF‑251b repaired for the generic\ngenerator — the same analysis, one case further on, which is why this is\npresumed a CLASS: every generic-extension `init` whose body aggregates an\nOWNING parameter is a candidate, and the plain-function and memberwise-literal\npaths are the neighbours to sweep before a fix.\n\nWHY IT WAS INVISIBLE: it needs a second bug to show. With the explicit\n`Wrap<Vector<Int>>(from: move a)` spelling the CALLER does not drop the built\nvalue either, so a leak and a spurious release cancel to exit 0 — which is\nexactly what HEAD does. Design 207's inferred spelling resolves the argument's\ntype from the argument itself, the caller then drops correctly, and the\npre-existing release becomes a visible double free. So 207 did not introduce it\nand does not fix it; it removed the mask at one spelling.\n\nPinned by `examples/generic_init_moved_parameter_is_released_once.saw`\n(deinit-count oracle: exactly one teardown per value).\n[207, DF‑217m, DF‑251b]\n","id":"SL-80","labels":["todo-import","backlog","needs-verification","bug","plan","memory-safety"],"priority":"high","project":"SL","revision":1,"sequence":80,"status":"open","title":"DF-302b: Prevent moved initializer parameters from being released twice","updated":"1788791229"},"before":null,"id":"SL-80","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-302b: Prevent moved initializer parameters from being released twice\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n4. Use counted ownership events and, where applicable, an address sanitizer; compare sync/driven and owning/copyable controls. Check error, early-return and teardown edges.\\n5. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n- [ ] Each owned resource is released exactly once at the required lifetime boundary, with no lost writes or stale owner.\\n\\n## Existing design references\\n\\n- [sawlang/designs/207-constructor-inference.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/207-constructor-inference.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 130–133\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L130-L133)\\n\\n- DF‑302b — a generic extension's `init` RELEASES a parameter it moved into the built value, so the value is torn down TWICE (entry below, filed Sep 4 by design 207's agent; PRE-EXISTING at HEAD, reproduced with fully explicit type arguments and no inference). Pinned by `examples/generic_init_moved_parameter_is_released_once.saw`. A DF‑217m/DF‑251b sibling — the same init-cleanup analysis, one case further on\\n\\n### sawlang TODO lines 134–179\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L134-L179)\\n\\n## DF‑302b — a generic extension's `init` RELEASES a parameter it MOVED into the\\n## built value, so the value is torn down TWICE (filed Sep 4 by design 207's\\n## agent; PRE-EXISTING at HEAD, NOT design 207's and NOT fixed by it)\\n\\n```saw\\nstruct Res { id: Int }\\nextension Res: NoCopy { func deinit(&var self) { print(\\\"gone {self.id}\\\") } }\\nstruct Wrap<T> { value: T }\\nextension Wrap<T> { init(from: T) -> Wrap<T> { Wrap<T>(value: from) } }\\n// let w = Wrap<Res>(from: move r);  print(w.value.id)\\n```\\n\\nWanted `7` / `gone 7`. Got `gone 7` / `7` / `gone 7` — released BEFORE the read,\\nso `w.value.id` reads freed storage and prints 7 by luck, then the caller\\nreleases it again. Exit 0, silently.\\n\\nMECHANISM (obligation 4). The generated generic `init` aggregates the parameter\\ninto the returned struct with an `insertvalue` and never clears that\\nparameter's drop flag, so the init epilogue runs `deinit` on storage the caller\\nnow owns:\\n\\n```\\n%\\\".5\\\" = insertvalue %\\\"Wrap$1$Res\\\" undef, %\\\"Res\\\" %\\\"from.2\\\", 0\\n%\\\"from.needsdrop\\\" = load i1, i1* %\\\"from.dropflag\\\"     ; still 1\\ndrop.from:  call void @\\\"Res_deinit\\\"(%\\\"Res\\\"* %\\\"from.1\\\")\\n```\\n\\nThe memberwise-aggregation form of \\\"moved into the built value\\\" is not counted\\nby the init-cleanup analysis DF‑217m added and DF‑251b repaired for the generic\\ngenerator — the same analysis, one case further on, which is why this is\\npresumed a CLASS: every generic-extension `init` whose body aggregates an\\nOWNING parameter is a candidate, and the plain-function and memberwise-literal\\npaths are the neighbours to sweep before a fix.\\n\\nWHY IT WAS INVISIBLE: it needs a second bug to show. With the explicit\\n`Wrap<Vector<Int>>(from: move a)` spelling the CALLER does not drop the built\\nvalue either, so a leak and a spurious release cancel to exit 0 — which is\\nexactly what HEAD does. Design 207's inferred spelling resolves the argument's\\ntype from the argument itself, the caller then drops correctly, and the\\npre-existing release becomes a visible double free. So 207 did not introduce it\\nand does not fix it; it removed the mask at one spelling.\\n\\nPinned by `examples/generic_init_moved_parameter_is_released_once.saw`\\n(deinit-count oracle: exactly one teardown per value).\\n[207, DF‑217m, DF‑251b]\\n\",\"labels\":[\"todo-import\",\"backlog\",\"needs-verification\",\"bug\",\"plan\",\"memory-safety\"],\"priority\":\"high\",\"project\":\"SL\",\"request_id\":\"todo-20260907-93760ca3f1a78d571271-create\",\"title\":\"DF-302b: Prevent moved initializer parameters from being released twice\"}","request_id":"todo-20260907-93760ca3f1a78d571271-create","sequence":80,"timestamp":"1788791229"}
---
