---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl235","author":"agent:codex-reviewer","comments":[{"author":"agent:claude-sl235","body":"test","created":"1789105854","id":"c1"},{"author":"agent:claude-sl235","body":"test2","created":"1789105856","id":"c2"},{"author":"agent:claude-sl235","body":"(ignore my two 'test' comments above — CLI arg-order probe.) Claiming SL-235. Reproduced on branch base eac0db0d: the value if types as the then-arm and the value-less else arm is never checked; prints fabricated 0.","created":"1789105866","id":"c3"},{"author":"agent:claude-sl235","body":"Fix in. Mechanism: a value if/match arm whose block ends in a statement types as None (no final_expr); None was treated as compatible-with-anything, so the construct returned the value arm's type on both paths (fabricated 0). Now a reachable non-diverging valueless arm makes the whole construct Void, so value position rejects (located) and statement position discards. Diverging arms (return/panic) exempt. errors/ pins for if + match; a positive pin keeps statement-pos + diverging-sibling valid. Suite 2462 pass, freestanding 36/36.","created":"1789107685","id":"c4"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-235.p1 (revision 1): value-flow miscompile batch: SL-235 (reject valueless value-branch arm) / SL-239 (keep Optional destination context both ways) / SL-236 (confine short-circuit RHS owned temps — SIGBUS fix)","created":"1789111522","id":"c5"}],"created":"1789070062","description":"The Python compiler accepts a value-producing if whose continuing else branch supplies no value, and the resulting executable returns an invented zero.\n\nFreshly reproduced on local main `30c5a55016aed806a2f94a64d62224c6cafc135a` on arm64 macOS, using the repository Python environment. Also observed in the prototype bootstrap base (last sawc change `d592e716d562990656b98ced5a391b7d3d282246`).\n\nMinimal source:\n\n```saw\nfunc choose(flag: Bool) -> Int {\n    if flag { 1 } else { let x = 2 }\n}\nfunc main() { print(\"{}\", choose(false)) }\n```\n\nReproduction: save as `.build/scratch/missing_value_tail.saw`, run `.venv/bin/python sawc/sawc.py .build/scratch/missing_value_tail.saw -o .build/scratch/missing_value_tail`, then run the executable.\n\nExpected: a located compile-time error because the reachable else branch cannot supply the required Int. An explicit return in that arm would be valid because it exits the function; a declaration alone does not.\n\nActual: compilation succeeds, execution exits 0 and prints `0`.\n\nThis was found while independently checking value-control semantics for the Saw-written compiler prototype. The prototype now explicitly rejects the missing-value case; no production fix is included here. The confirmed finding is acceptance and fabricated output; the exact checker/lowering mechanism still needs diagnosis.\n\nAcceptance: reject this source with a located diagnostic; audit equivalent if/match block arms and declared/assigned/argument result contexts; retain valid mixed early-return/value arms and fully returning branches. Add a small regression test with the intended compile error.\n","id":"SL-235","labels":["bug","typechecking"],"priority":"high","project":"SL","revision":8,"sequence":540,"stage":"queued","status":"open","title":"Value if accepts a continuing branch without a tail and returns zero","updated":"1789111522"},"before":{"assignee":"agent:claude-sl235","author":"agent:codex-reviewer","comments":[{"author":"agent:claude-sl235","body":"test","created":"1789105854","id":"c1"},{"author":"agent:claude-sl235","body":"test2","created":"1789105856","id":"c2"},{"author":"agent:claude-sl235","body":"(ignore my two 'test' comments above — CLI arg-order probe.) Claiming SL-235. Reproduced on branch base eac0db0d: the value if types as the then-arm and the value-less else arm is never checked; prints fabricated 0.","created":"1789105866","id":"c3"},{"author":"agent:claude-sl235","body":"Fix in. Mechanism: a value if/match arm whose block ends in a statement types as None (no final_expr); None was treated as compatible-with-anything, so the construct returned the value arm's type on both paths (fabricated 0). Now a reachable non-diverging valueless arm makes the whole construct Void, so value position rejects (located) and statement position discards. Diverging arms (return/panic) exempt. errors/ pins for if + match; a positive pin keeps statement-pos + diverging-sibling valid. Suite 2462 pass, freestanding 36/36.","created":"1789107685","id":"c4"}],"created":"1789070062","description":"The Python compiler accepts a value-producing if whose continuing else branch supplies no value, and the resulting executable returns an invented zero.\n\nFreshly reproduced on local main `30c5a55016aed806a2f94a64d62224c6cafc135a` on arm64 macOS, using the repository Python environment. Also observed in the prototype bootstrap base (last sawc change `d592e716d562990656b98ced5a391b7d3d282246`).\n\nMinimal source:\n\n```saw\nfunc choose(flag: Bool) -> Int {\n    if flag { 1 } else { let x = 2 }\n}\nfunc main() { print(\"{}\", choose(false)) }\n```\n\nReproduction: save as `.build/scratch/missing_value_tail.saw`, run `.venv/bin/python sawc/sawc.py .build/scratch/missing_value_tail.saw -o .build/scratch/missing_value_tail`, then run the executable.\n\nExpected: a located compile-time error because the reachable else branch cannot supply the required Int. An explicit return in that arm would be valid because it exits the function; a declaration alone does not.\n\nActual: compilation succeeds, execution exits 0 and prints `0`.\n\nThis was found while independently checking value-control semantics for the Saw-written compiler prototype. The prototype now explicitly rejects the missing-value case; no production fix is included here. The confirmed finding is acceptance and fabricated output; the exact checker/lowering mechanism still needs diagnosis.\n\nAcceptance: reject this source with a located diagnostic; audit equivalent if/match block arms and declared/assigned/argument result contexts; retain valid mixed early-return/value arms and fully returning branches. Add a small regression test with the intended compile error.\n","id":"SL-235","labels":["bug","typechecking"],"priority":"high","project":"SL","revision":7,"sequence":536,"stage":"queued","status":"open","title":"Value if accepts a continuing branch without a tail and returns zero","updated":"1789107685"},"id":"SL-235","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/examples/errors/value_if_branch_without_value.saw b/examples/errors/value_if_branch_without_value.saw\\nnew file mode 100644\\nindex 00000000..fa4a37f9\\n--- /dev/null\\n+++ b/examples/errors/value_if_branch_without_value.saw\\n@@ -0,0 +1,10 @@\\n+// SL-235: a value-producing `if` whose REACHABLE continuing branch supplies no\\n+// value — its block ends in a statement (`let x = 2`), not an expression — must\\n+// be REJECTED, not accepted with a fabricated zero returned on that path. A\\n+// `return` in the arm would be valid (it exits); a bare declaration is not.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: but returns `Void`\\n+func choose(flag: Bool) -> Int {\\n+    if flag { 1 } else { let x = 2 }\\n+}\\n+func main() { print(\\\"{}\\\", choose(false)) }\\ndiff --git a/examples/errors/value_match_arm_without_value.saw b/examples/errors/value_match_arm_without_value.saw\\nnew file mode 100644\\nindex 00000000..2a042045\\n--- /dev/null\\n+++ b/examples/errors/value_match_arm_without_value.saw\\n@@ -0,0 +1,13 @@\\n+// SL-235: the `match` twin — a reachable arm whose block supplies no value\\n+// makes the whole `match` value-less, so consuming it as an `Int` is rejected\\n+// rather than fabricating a zero on that arm. A diverging arm (`{ ...; return }`)\\n+// stays exempt; this one merely ends in a `let`.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: but returns `Void`\\n+func choose(n: Int) -> Int {\\n+    match n {\\n+        case 0 -> 1,\\n+        case _ -> { let x = 2 }\\n+    }\\n+}\\n+func main() { print(\\\"{}\\\", choose(5)) }\\ndiff --git a/examples/short_circuit_skipped_operand_temp_cleanup.saw b/examples/short_circuit_skipped_operand_temp_cleanup.saw\\nnew file mode 100644\\nindex 00000000..b5959914\\n--- /dev/null\\n+++ b/examples/short_circuit_skipped_operand_temp_cleanup.saw\\n@@ -0,0 +1,72 @@\\n+// SL-236: a short-circuit `&&`/`||` whose NOT-evaluated operand builds an owned\\n+// temporary (a method/call result whose String field is then read) must not run\\n+// that temporary's destructor on the short-circuit path — it was never\\n+// constructed, so the drop ran over an uninitialized slot (a garbage String\\n+// pointer -> SIGBUS). The RHS is evaluated in a conditionally-taken block, so\\n+// its statement temps are now confined there (design 94, third entry point)\\n+// instead of being drained at the enclosing statement's merge.\\n+//\\n+// The oracle counts destructor calls: a skipped operand builds nothing (0\\n+// drops, no crash), an evaluated one builds exactly one temp (1 drop).\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// and_skip drops=0\\n+// and_eval drops=1\\n+// or_skip drops=0\\n+// or_eval drops=1\\n+// lhs_temp drops=1\\n+// nested_skip drops=0\\n+// nested_eval drops=1\\n+\\n+static DROPS: Atomic<Int> = Atomic(0)\\n+\\n+struct Probe { value: String }\\n+extension Probe: NoCopy { func deinit(&var self) { DROPS.fetch_add(1) } }\\n+func probe(s: String) -> Probe { Probe(value: s) }\\n+\\n+func fbool(b: Bool) -> Bool { b }\\n+\\n+func and_rhs(lhs: Bool) -> Bool {\\n+    if fbool(lhs) && probe(\\\"a\\\").value == \\\"x\\\" { return true }\\n+    false\\n+}\\n+func or_rhs(lhs: Bool) -> Bool {\\n+    if fbool(lhs) || probe(\\\"b\\\").value == \\\"x\\\" { return true }\\n+    false\\n+}\\n+func lhs_temp(rhs: Bool) -> Bool {\\n+    if probe(\\\"c\\\").value == \\\"x\\\" && fbool(rhs) { return true }\\n+    false\\n+}\\n+func nested(a: Bool, b: Bool) -> Bool {\\n+    if fbool(a) || (fbool(b) && probe(\\\"d\\\").value == \\\"x\\\") { return true }\\n+    false\\n+}\\n+\\n+func report(label: String, before: Int) {\\n+    print(\\\"{} drops={}\\\", label, DROPS.load() - before)\\n+}\\n+\\n+func main() {\\n+    var b = DROPS.load()\\n+    let _ = and_rhs(false)\\n+    report(\\\"and_skip\\\", b)\\n+    b = DROPS.load()\\n+    let _ = and_rhs(true)\\n+    report(\\\"and_eval\\\", b)\\n+    b = DROPS.load()\\n+    let _ = or_rhs(true)\\n+    report(\\\"or_skip\\\", b)\\n+    b = DROPS.load()\\n+    let _ = or_rhs(false)\\n+    report(\\\"or_eval\\\", b)\\n+    b = DROPS.load()\\n+    let _ = lhs_temp(false)\\n+    report(\\\"lhs_temp\\\", b)\\n+    b = DROPS.load()\\n+    let _ = nested(true, false)\\n+    report(\\\"nested_skip\\\", b)\\n+    b = DROPS.load()\\n+    let _ = nested(false, true)\\n+    report(\\\"nested_eval\\\", b)\\n+}\\ndiff --git a/examples/short_circuit_token_field_no_crash.saw b/examples/short_circuit_token_field_no_crash.saw\\nnew file mode 100644\\nindex 00000000..b1da3d87\\n--- /dev/null\\n+++ b/examples/short_circuit_token_field_no_crash.saw\\n@@ -0,0 +1,31 @@\\n+// SL-236 (the filed reproducer): the second `if`'s `&&` RHS reads the String\\n+// field of a fresh `Tok` temporary, but the LHS `self.at(Kind.Ident)` is false\\n+// (the token is Number), so the RHS is never evaluated. Its temporary — whose\\n+// `suffix: String?` field carries a drop flag — must not be torn down on the\\n+// short-circuit path. Before the fix this ran a destructor over an\\n+// uninitialized slot and the executable SIGBUS'd instead of printing `false`.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// false\\n+enum Kind { case Ident, case Number, case Let, case Var, case Return, case While, case Brace }\\n+struct Tok { kind: Kind, value: String, line: Int, col: Int, suffix: String? }\\n+func clone(t: &Tok) -> Tok {\\n+    Tok(kind: t.kind, value: t.value, line: t.line, col: t.col, suffix: t.suffix)\\n+}\\n+struct Reader { tokens: Vector<Tok>, pos: Int }\\n+extension Reader: NoCopy {}\\n+extension Reader {\\n+    func token(&self) -> Tok { clone(&self.tokens[self.pos]) }\\n+    func at(&self, k: Kind) -> Bool { self.token().kind == k }\\n+    func classify(&self) -> Bool {\\n+        if self.at(Kind.Let) || self.at(Kind.Var) || self.at(Kind.Return) || self.at(Kind.While) || self.at(Kind.Brace) { return true }\\n+        if self.at(Kind.Ident) && self.token().value == \\\"print\\\" { return true }\\n+        false\\n+    }\\n+}\\n+func main() {\\n+    var tokens = Vector<Tok>()\\n+    try! tokens.push(Tok(kind: Kind.Number, value: \\\"42\\\", line: 1, col: 1, suffix: None))\\n+    let reader = Reader(tokens: move tokens, pos: 0)\\n+    print(reader.classify())\\n+}\\ndiff --git a/examples/value_branch_valueless_arm_preserved.saw b/examples/value_branch_valueless_arm_preserved.saw\\nnew file mode 100644\\nindex 00000000..85646226\\n--- /dev/null\\n+++ b/examples/value_branch_valueless_arm_preserved.saw\\n@@ -0,0 +1,29 @@\\n+// SL-235 (the other half): the rejection is for VALUE position only. A value-less\\n+// arm in STATEMENT position — where the branch value is discarded — stays legal,\\n+// and a DIVERGING arm (`return`/`panic`) that exits keeps its sibling's value.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// stmt-then\\n+// early-2\\n+// panic-side-11\\n+func side() { print(\\\"stmt-then\\\") }\\n+\\n+// A value-less arm is fine when the branch is a statement (value discarded).\\n+func in_statement(flag: Bool) {\\n+    if flag { side() } else { let x = 2 }\\n+}\\n+\\n+// A DIVERGING sibling arm keeps the value `if` valid — the arm exits.\\n+func with_early_return(flag: Bool) -> Int {\\n+    if flag { return 2 } else { let x = 7  x }\\n+}\\n+\\n+func with_panic(flag: Bool) -> Int {\\n+    if flag { 11 } else { panic(\\\"unreachable in this test\\\") }\\n+}\\n+\\n+func main() {\\n+    in_statement(true)\\n+    print(\\\"early-{}\\\", with_early_return(true))\\n+    print(\\\"panic-side-{}\\\", with_panic(true))\\n+}\\ndiff --git a/examples/value_if_optional_destination_context.saw b/examples/value_if_optional_destination_context.saw\\nnew file mode 100644\\nindex 00000000..625208f1\\n--- /dev/null\\n+++ b/examples/value_if_optional_destination_context.saw\\n@@ -0,0 +1,47 @@\\n+// SL-239: an explicitly Optional-typed destination reconciles a value `if` /\\n+// `if let` whose arms MIX a `T?` arm with a bare `T` arm — the bare arm wraps\\n+// into `Some(...)`, the `T?` arm stays put — in BOTH arm orders. Before the fix\\n+// the arm-to-arm compatibility test was one-directional, so a then-`String?` /\\n+// else-`String` pair was rejected as `String? vs String`, even though the same\\n+// pair with the plain arm FIRST compiled.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT:\\n+// then-optional: none\\n+// else-optional: hi\\n+// iflet then-optional: none\\n+// iflet else-optional: yo\\n+func present() -> String? { \\\"here\\\" }\\n+\\n+func main() {\\n+    // ordinary if, then arm is String?, else arm is bare String\\n+    let a: String? = if true {\\n+        let absent: String? = None\\n+        absent\\n+    } else {\\n+        \\\"fallback\\\"\\n+    }\\n+    if let t = a { print(\\\"then-optional: {}\\\", t) } else { print(\\\"then-optional: none\\\") }\\n+\\n+    // ordinary if, OTHER order: then bare String, else String?\\n+    let b: String? = if true { \\\"hi\\\" } else {\\n+        let absent: String? = None\\n+        absent\\n+    }\\n+    if let t = b { print(\\\"else-optional: {}\\\", t) } else { print(\\\"else-optional: none\\\") }\\n+\\n+    // if let, then arm String?, else arm bare String\\n+    let c: String? = if let _ = present() {\\n+        let absent: String? = None\\n+        absent\\n+    } else {\\n+        \\\"fallback\\\"\\n+    }\\n+    if let t = c { print(\\\"iflet then-optional: {}\\\", t) } else { print(\\\"iflet then-optional: none\\\") }\\n+\\n+    // if let, OTHER order: then bare String, else String?\\n+    let d: String? = if let _ = present() { \\\"yo\\\" } else {\\n+        let absent: String? = None\\n+        absent\\n+    }\\n+    if let t = d { print(\\\"iflet else-optional: {}\\\", t) } else { print(\\\"iflet else-optional: none\\\") }\\n+}\\ndiff --git a/sawc/codegen/operators.py b/sawc/codegen/operators.py\\nindex 290bc441..d2fdbe4c 100644\\n--- a/sawc/codegen/operators.py\\n+++ b/sawc/codegen/operators.py\\n@@ -1284,6 +1284,29 @@ class OperatorsMixin:\\n             self.builder.branch(merge_bb)\\n         self.builder.position_at_end(merge_bb)\\n \\n+    def _confine_short_circuit_temps(self, temp_mark):\\n+        \\\"\\\"\\\"Drop the statement temps created since `temp_mark` in the CURRENT\\n+        block, and remove them from the enclosing statement's temp list (SL-236).\\n+\\n+        THE THIRD ENTRY POINT of design 94's block confinement (obligation 1) —\\n+        `_generate_block`'s tail and a loop body being the other two. The `&&`/\\n+        `||` RHS is evaluated in a CONDITIONALLY-taken block, so an owned\\n+        statement temp created while generating it (a method-call receiver whose\\n+        field/method is then read — `token().value`) must be dropped on the path\\n+        that CONSTRUCTS it. Left to the enclosing statement's cleanup drain, it\\n+        was dropped at the statement's merge — a point the SHORT-CIRCUIT path\\n+        also reaches, where the temp's slot was never stored — so a destructor\\n+        ran over an uninitialized alloca (a garbage `String` pointer -> SIGBUS).\\n+        `temp_mark` is None when there is no statement-temp context to confine.\\\"\\\"\\\"\\n+        if temp_mark is None or self.builder.block.is_terminated:\\n+            return\\n+        branch_temps = self.statement_temps[temp_mark:]\\n+        if not branch_temps:\\n+            return\\n+        for slot, saw_type in reversed(branch_temps):\\n+            self._emit_drop_at(slot, saw_type)\\n+        del self.statement_temps[temp_mark:]\\n+\\n     def _generate_logical_and(self, expr: BinaryOp):\\n         \\\"\\\"\\\"Generate short-circuit && evaluation.\\n \\n@@ -1305,9 +1328,13 @@ class OperatorsMixin:\\n         # Branch: if left is false, go to merge with false; else evaluate right\\n         self.builder.cbranch(left, eval_right_block, merge_block)\\n \\n-        # Evaluate right operand\\n+        # Evaluate right operand. SL-236: confine any owned temp created for the\\n+        # RHS to this conditionally-taken block — see `_confine_short_circuit_temps`.\\n         self.builder.position_at_end(eval_right_block)\\n+        temp_mark = (len(self.statement_temps)\\n+                     if self.statement_temps is not None else None)\\n         right = self._generate_expression(expr.right)\\n+        self._confine_short_circuit_temps(temp_mark)\\n         right_block = self.builder.block\\n         self.builder.branch(merge_block)\\n \\n@@ -1340,9 +1367,13 @@ class OperatorsMixin:\\n         # Branch: if left is true, go to merge with true; else evaluate right\\n         self.builder.cbranch(left, merge_block, eval_right_block)\\n \\n-        # Evaluate right operand\\n+        # Evaluate right operand. SL-236: confine any owned temp created for the\\n+        # RHS to this conditionally-taken block — see `_confine_short_circuit_temps`.\\n         self.builder.position_at_end(eval_right_block)\\n+        temp_mark = (len(self.statement_temps)\\n+                     if self.statement_temps is not None else None)\\n         right = self._generate_expression(expr.right)\\n+        self._confine_short_circuit_temps(temp_mark)\\n         right_block = self.builder.block\\n         self.builder.branch(merge_block)\\n \\ndiff --git a/sawc/typechecker/expressions.py b/sawc/typechecker/expressions.py\\nindex 33151f32..2276a420 100644\\n--- a/sawc/typechecker/expressions.py\\n+++ b/sawc/typechecker/expressions.py\\n@@ -55,6 +55,22 @@ VOID_THREAD_STRUCT_NAME = _type_identity(\\\"VoidThread\\\", (\\\"<std>\\\", \\\"task\\\"))\\n _ABSTRACT_COUNT = object()\\n \\n \\n+def _branch_is_valueless(block, diverges: bool) -> bool:\\n+    \\\"\\\"\\\"True when an `if`/`if let` branch REACHABLY supplies no value (SL-235).\\n+\\n+    A branch contributes the construct's value through its block's final\\n+    EXPRESSION. A block that ends in a plain statement — `{ let x = 2 }` — has no\\n+    final expression, so `_check_block` types it `None`, meaning \\\"no value\\\". The\\n+    branch reconciliation must not read that `None` as \\\"the sibling's type\\\" or as\\n+    \\\"compatible with anything\\\": a reachable branch that supplies no value makes\\n+    the whole construct valueless. A DIVERGING branch (`{ return }`,\\n+    `{ panic() }`) is exempt — it never falls through with a missing value, so the\\n+    construct legitimately takes the other branch's type (design 49).\\\"\\\"\\\"\\n+    if diverges:\\n+        return False\\n+    return getattr(block, 'final_expr', None) is None\\n+\\n+\\n class _SpawnBorrow(NamedTuple):\\n     \\\"\\\"\\\"One root a `group.spawn(...)` borrows for its task's life.\\n \\n@@ -5351,6 +5367,22 @@ class ExpressionsMixin:\\n             entry_moves,\\n             [(then_state, then_diverges), (else_state, else_diverges)])\\n \\n+        # SL-235: a REACHABLE branch whose block supplies no value — it ends in a\\n+        # statement, not an expression, so `_check_block` returned None — cannot\\n+        # contribute the `if`'s value. Yield `Void` (not the sibling's type): a\\n+        # value-requiring consumer then rejects the whole `if` with a located\\n+        # error (`let n = <Void>`, an argument mismatch, a `-> Int` tail\\n+        # mismatch), while STATEMENT position — where the value is discarded —\\n+        # stays legal. A DIVERGING branch is exempt: it exits rather than falling\\n+        # through valueless, and the NEVER handling below hands back the other\\n+        # arm. Until this, the None type flowed on as \\\"compatible with anything\\\"\\n+        # and the `if` handed back the value arm's type on both paths, fabricating\\n+        # a zero on the reachable valueless one.\\n+        if _branch_is_valueless(expr.then_branch, then_diverges) or (\\n+                expr.else_branch is not None\\n+                and _branch_is_valueless(expr.else_branch, else_diverges)):\\n+            return SawType(TypeKind.VOID)\\n+\\n         if expr.else_branch:\\n             # design 49: a diverging branch (`panic(...)`, type NEVER) contributes\\n             # no value to the merge — the `if` takes the other branch's type.\\n@@ -5362,9 +5394,19 @@ class ExpressionsMixin:\\n             # this general test's — `_merge_value_branch_types` below owns both\\n             # the lossless-widening admission and the refusal, with the three\\n             # conversion spellings in its hint.\\n+            #\\n+            # SL-239: arm reconciliation is SYMMETRIC — a `T` arm and a `T?` arm\\n+            # merge to `T?` whichever one is written first, so the compatibility\\n+            # test is asked BOTH ways (`_types_compatible` is directional: it\\n+            # admits `T` into `T?` but not the reverse). Without the second call a\\n+            # `then: String?` / `else: String` pair — the annotated-`String?`\\n+            # destination reaching one arm and not the other — took the incompatible\\n+            # branch and errored, even though the wrapping block below already\\n+            # handles a then-optional arm; only the guard was one-directional.\\n             if (then_type and else_type\\n                     and not self._both_int_kinds(then_type, else_type)\\n-                    and not self._types_compatible(then_type, else_type)):\\n+                    and not self._types_compatible(then_type, else_type)\\n+                    and not self._types_compatible(else_type, then_type)):\\n                 # Check if branches could be Result auto-wrapped.\\n                 # design 213 entry point 2: inside a closure this is the\\n                 # CLOSURE's return type — a closure declared `-> Result<T, E>`\\n@@ -5586,6 +5628,16 @@ class ExpressionsMixin:\\n         self.moved_bindings = self._merge_move_branches(\\n             entry_moves,\\n             [(then_state, then_diverges), (else_state, else_diverges)])\\n+        # SL-235: a reachable branch that supplies no value makes the whole\\n+        # `if let` value-less — yield Void (see `_check_if_expr` for the rule and\\n+        # why a value-requiring consumer, not this reconciliation, reports it).\\n+        # `while let` is exempt: its synthesized `else` is a `break` and value\\n+        # position is refused upstream, so its own reconciliation still governs.\\n+        if not expr.while_let and (\\n+                _branch_is_valueless(expr.then_branch, then_diverges)\\n+                or (expr.else_branch is not None\\n+                    and _branch_is_valueless(expr.else_branch, else_diverges))):\\n+            return SawType(TypeKind.VOID)\\n         if expr.else_branch:\\n             # design 49: a diverging branch (`panic(...)`, type NEVER) takes the\\n             # other branch's type.\\n@@ -5599,7 +5651,17 @@ class ExpressionsMixin:\\n                 # value to merge with, and a `while let` yields nothing anyway\\n                 # (value position is refused in `_check_while_expr_as_expression`).\\n                 return then_type\\n-            if then_type and else_type and not self._types_compatible(then_type, else_type):\\n+            # SL-239: arm reconciliation is SYMMETRIC (see `_check_if_expr`) — a\\n+            # `T` arm and a `T?` arm merge to `T?` in either order, so the\\n+            # compatibility guard is asked BOTH ways (`_types_compatible` admits\\n+            # `T` into `T?` but not the reverse). Without the second call a\\n+            # then-optional / else-plain pair — the annotated-`T?` destination\\n+            # reaching one arm — was rejected outright, even though the optional\\n+            # cross-wrap below handles it. The `if let` twin of the ordinary-`if`\\n+            # reconciliation, kept in step with it.\\n+            if (then_type and else_type\\n+                    and not self._types_compatible(then_type, else_type)\\n+                    and not self._types_compatible(else_type, then_type)):\\n                 self._error(\\n                     ErrorKind.TYPE_MISMATCH,\\n                     f\\\"`if let` branches have incompatible types: `{then_type}` vs `{else_type}`\\\",\\n@@ -5620,6 +5682,30 @@ class ExpressionsMixin:\\n                 if then_type.is_none_literal() and else_type.is_optional():\\n                     self._annotate_none_in_block(expr.then_branch, else_type)\\n                     return else_type\\n+                # SL-239: one arm is `T?`, the other its payload `T`. Wrap the\\n+                # bare arm into `Some(...)` so the `if let` yields a homogeneous\\n+                # `T?` value — the exact mirror of `_check_if_expr`, which the\\n+                # `if let` path had never grown, so an annotated-`String?`\\n+                # destination whose `String` arm needed the wrap was rejected as\\n+                # `String? vs String`.\\n+                if else_type.is_optional() and not then_type.is_optional():\\n+                    if expr.then_branch.final_expr:\\n+                        expr.then_branch.final_expr = OptionalWrap(\\n+                            value=expr.then_branch.final_expr,\\n+                            target_type=else_type,\\n+                            line=expr.then_branch.final_expr.line,\\n+                            column=expr.then_branch.final_expr.column\\n+                        )\\n+                    return else_type\\n+                if then_type.is_optional() and not else_type.is_optional():\\n+                    if expr.else_branch.final_expr:\\n+                        expr.else_branch.final_expr = OptionalWrap(\\n+                            value=expr.else_branch.final_expr,\\n+                            target_type=then_type,\\n+                            line=expr.else_branch.final_expr.line,\\n+                            column=expr.else_branch.final_expr.column\\n+                        )\\n+                    return then_type\\n             return then_type or else_type\\n         else:\\n             return then_type\\n@@ -12110,7 +12196,8 @@ class ExpressionsMixin:\\n                     expr.line, expr.column,\\n                     hint=\\\"add missing cases or use `case _ ->` as a default\\\"\\n                 )\\n-        return self._reconcile_match_arm_types(expr, arm_types)\\n+        return self._reconcile_match_arm_types(\\n+            expr, arm_types, [d for (_, d) in arm_move_states])\\n \\n     @staticmethod\\n     def _arm_yields_no_value(arm_type: Optional[SawType]) -> bool:\\n@@ -12134,12 +12221,35 @@ class ExpressionsMixin:\\n         \\\"\\\"\\\"\\n         return arm_type is None or arm_type.kind == TypeKind.NEVER\\n \\n-    def _reconcile_match_arm_types(self, expr: MatchExpr, arm_types) -> Optional[SawType]:\\n+    def _reconcile_match_arm_types(self, expr: MatchExpr, arm_types,\\n+                                   arm_diverges=None) -> Optional[SawType]:\\n         \\\"\\\"\\\"Compute a match expression's result type from its arm types, honoring\\n         NEVER arms (design 49) and Result auto-wrap. Shared by the enum-switch\\n-        path and the general pattern path (design 63).\\\"\\\"\\\"\\n+        path and the general pattern path (design 63).\\n+\\n+        `arm_diverges` is the per-arm divergence flags (from `_arm_diverges`,\\n+        aligned with `arm_types`), when the caller has them. They separate the\\n+        two spellings of a `None` arm type that `_arm_yields_no_value` cannot:\\n+        a block whose every path EXITED (a value-less block that DIVERGES —\\n+        contributes nothing, design 49) from a block that merely ENDS IN A\\n+        STATEMENT (a value-less block that does NOT diverge — SL-235). The second\\n+        makes the whole `match` value-less, exactly as a valueless `if` arm does;\\n+        see below.\\\"\\\"\\\"\\n         if not arm_types:\\n             return None\\n+        # SL-235: a REACHABLE arm that supplies no value — its block ends in a\\n+        # statement, so `_check_block` typed it `None`, and it does not diverge —\\n+        # makes the whole `match` value-less. Yield `Void` (not the surviving\\n+        # arm's type): a value-requiring consumer then rejects the match with a\\n+        # located error, while statement position discards it. Until this, such\\n+        # an arm was skipped as if it diverged (`_arm_yields_no_value(None)`),\\n+        # and the match handed back a live arm's type on the reachable valueless\\n+        # path, fabricating a zero. A diverging value-less arm (`case _ -> { … ;\\n+        # return }`) is NOT this case — `arm_diverges` tells the two apart.\\n+        if arm_diverges is not None:\\n+            for at, div in zip(arm_types, arm_diverges):\\n+                if at is None and not div:\\n+                    return SawType(TypeKind.VOID)\\n         # design 49 + DF-140e: an arm that yields no value (a diverging\\n         # `panic(...)`, or a block whose every path returned) contributes\\n         # nothing to the match's type — skip such arms when computing the common\\n@@ -12881,7 +12991,8 @@ class ExpressionsMixin:\\n                 expr.line, expr.column,\\n                 hint=\\\"add a `case _ ->` (or a bare-binding) fallback arm\\\",\\n             )\\n-        return self._reconcile_match_arm_types(expr, arm_types)\\n+        return self._reconcile_match_arm_types(\\n+            expr, arm_types, [d for (_, d) in arm_move_states])\\n \\n     def _first_reference_in_type(self, t: Optional[SawType]) -> Optional[SawType]:\\n         \\\"\\\"\\\"The first reference reachable from an INFERRED type (a closure's\\n\",\"id\":\"SL-235\",\"request_id\":\"sl235-valueflow-1\",\"title\":\"value-flow miscompile batch: SL-235 (reject valueless value-branch arm) / SL-239 (keep Optional destination context both ways) / SL-236 (confine short-circuit RHS owned temps — SIGBUS fix)\"}","request_id":"sl235-valueflow-1","sequence":540,"timestamp":"1789111522"}
---
