---
{"action":"create","actor":"agent:claude-sl318u3","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl318u3","closed":"","comments":[],"created":"1790051266","description":"FOLLOW-UP (c) of design 275 U3 (its part 0, codex p5-r1 P1). THE EXCLUSIVE-WINDOW DERIVATION for borrowing structs: `&var T` reference fields, the writer-versus-reader table, and the nested-window rows.\n\nWHAT U3 RULED AND WHY. U3 is a SHARED-window feature, so a borrowing struct's reference fields are `&T` ONLY. A `&var T` field, and the exclusive `lends` that would feed one, are refused with the rule named (`examples/errors/borrowing_struct_exclusive_field.saw`, conformance row K145): a user iterator carrying `&var Collection` could REALLOCATE the very collection an outer shared iterator is reading through its own field, and recording the root alone would not catch it. That is DISTINCT from `next(&var self)`, which mutates the iterator's OWN cursor and is not mutable access to the borrowed collection.\n\nWHAT IS ALREADY BUILT FOR IT. `sawc/windows.py` reserves the MODES rather than inventing them: `SHARED` and `EXCLUSIVE` are both in `MODES`, `StatementWindow.mode` carries one, and `WindowTable.conflict_for`'s table is written for both (\"a SHARED window composes with other readers and collides with a write, a move, a reassign, a take or a swap; an EXCLUSIVE window collides with every touch\"). U3 exercises shared only, so the exclusive arm of that table has NO rows behind it — which is what this brief is for.\n\nWHAT THIS BRIEF OWES:\n\n1. THE WRITER-VERSUS-READER TABLE, with a row per pair. An exclusive window beside: a read of the root; a second exclusive window on the same root; a shared window on the same root; a `&` extent; a `&var` extent; a disjoint sibling path; an enclosing owner's path. Each in both orders, because SL-333's own review found that the mode of a receiver borrow is the ONE fact a pairwise root-charge test compares and that \"two exclusive\" is not a special case.\n2. THE NESTED-WINDOW ROWS. Two windows on one root, inner exclusive and outer shared, and the reverse; a nested window on a DISJOINT root; and an exclusive window whose body opens a shared one through the same borrowing struct. Design 141's windows nest LIFO and its epilogues run in that order; the statement form has to say the same thing about a statement-length extent.\n3. WHAT AN EXCLUSIVE ORIGIN IS. U3 admits `lends self` off a `&self` receiver; an exclusive field needs `lends self` off a `&var self` one, which U3 refuses by name today (\"`lends self` requires a SHARED receiver\"). That refusal is the thing this brief lifts, and the origin rule has to stay a CHECKED COMPILER FACT rather than becoming two rules: `Method.borrow_origin` is a declared annotation field carrying `'receiver root'`, and an exclusive origin is a second value of it, not a second field.\n4. WHAT THE MUTATING ITERATOR IS ACTUALLY FOR. The motivating shape is an in-place transform (`for x in v.iter_var() { x = f(x) }`) or a drain, and the `Item` question comes with it: U3's NO-SECOND-ITEM-LIFETIME rule says `Item` may be neither a reference nor a borrowing struct, so an exclusive iterator that yields a WRITABLE element needs that rule reopened — which is the part of this brief that is a language design question and not a table.\n5. WHETHER `Vector` GETS ONE. Today the writable element spellings are `v[i] = x`, `v.with_var_ref(i, body)` and `v.swap`, and none of them iterates. Whether std grows a `iter_var()` is a separate ruling from whether the language can express one.\n\nPREREQUISITE FOR: follow-up (a), the generic scoped-borrow statement, if its first shipped mode is exclusive — a `with_var_ref` replacement is the obvious first want, and it is an exclusive window by construction.\n","id":"SL-344","labels":["design","borrows"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1167,"stage":"backlog","status":"open","title":"The exclusive-window derivation for borrowing structs: &var fields, writer-versus-reader, nested windows","updated":"1790051266"},"before":null,"id":"SL-344","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl318u3\",\"description\":\"FOLLOW-UP (c) of design 275 U3 (its part 0, codex p5-r1 P1). THE EXCLUSIVE-WINDOW DERIVATION for borrowing structs: `&var T` reference fields, the writer-versus-reader table, and the nested-window rows.\\n\\nWHAT U3 RULED AND WHY. U3 is a SHARED-window feature, so a borrowing struct's reference fields are `&T` ONLY. A `&var T` field, and the exclusive `lends` that would feed one, are refused with the rule named (`examples/errors/borrowing_struct_exclusive_field.saw`, conformance row K145): a user iterator carrying `&var Collection` could REALLOCATE the very collection an outer shared iterator is reading through its own field, and recording the root alone would not catch it. That is DISTINCT from `next(&var self)`, which mutates the iterator's OWN cursor and is not mutable access to the borrowed collection.\\n\\nWHAT IS ALREADY BUILT FOR IT. `sawc/windows.py` reserves the MODES rather than inventing them: `SHARED` and `EXCLUSIVE` are both in `MODES`, `StatementWindow.mode` carries one, and `WindowTable.conflict_for`'s table is written for both (\\\"a SHARED window composes with other readers and collides with a write, a move, a reassign, a take or a swap; an EXCLUSIVE window collides with every touch\\\"). U3 exercises shared only, so the exclusive arm of that table has NO rows behind it — which is what this brief is for.\\n\\nWHAT THIS BRIEF OWES:\\n\\n1. THE WRITER-VERSUS-READER TABLE, with a row per pair. An exclusive window beside: a read of the root; a second exclusive window on the same root; a shared window on the same root; a `&` extent; a `&var` extent; a disjoint sibling path; an enclosing owner's path. Each in both orders, because SL-333's own review found that the mode of a receiver borrow is the ONE fact a pairwise root-charge test compares and that \\\"two exclusive\\\" is not a special case.\\n2. THE NESTED-WINDOW ROWS. Two windows on one root, inner exclusive and outer shared, and the reverse; a nested window on a DISJOINT root; and an exclusive window whose body opens a shared one through the same borrowing struct. Design 141's windows nest LIFO and its epilogues run in that order; the statement form has to say the same thing about a statement-length extent.\\n3. WHAT AN EXCLUSIVE ORIGIN IS. U3 admits `lends self` off a `&self` receiver; an exclusive field needs `lends self` off a `&var self` one, which U3 refuses by name today (\\\"`lends self` requires a SHARED receiver\\\"). That refusal is the thing this brief lifts, and the origin rule has to stay a CHECKED COMPILER FACT rather than becoming two rules: `Method.borrow_origin` is a declared annotation field carrying `'receiver root'`, and an exclusive origin is a second value of it, not a second field.\\n4. WHAT THE MUTATING ITERATOR IS ACTUALLY FOR. The motivating shape is an in-place transform (`for x in v.iter_var() { x = f(x) }`) or a drain, and the `Item` question comes with it: U3's NO-SECOND-ITEM-LIFETIME rule says `Item` may be neither a reference nor a borrowing struct, so an exclusive iterator that yields a WRITABLE element needs that rule reopened — which is the part of this brief that is a language design question and not a table.\\n5. WHETHER `Vector` GETS ONE. Today the writable element spellings are `v[i] = x`, `v.with_var_ref(i, body)` and `v.swap`, and none of them iterates. Whether std grows a `iter_var()` is a separate ruling from whether the language can express one.\\n\\nPREREQUISITE FOR: follow-up (a), the generic scoped-borrow statement, if its first shipped mode is exclusive — a `with_var_ref` replacement is the obvious first want, and it is an exclusive window by construction.\\n\",\"labels\":[\"design\",\"borrows\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"pid-9846-1790051266-14000\",\"stage\":\"backlog\",\"title\":\"The exclusive-window derivation for borrowing structs: &var fields, writer-versus-reader, nested windows\"}","request_id":"pid-9846-1790051266-14000","sequence":1167,"timestamp":"1790051266"}
---
