---
{"action":"create","actor":"agent:claude-sl318u4","after":{"assignee":"","author":"agent:claude-sl318u4","comments":[],"created":"1789769260","description":"Found by design 275 U4's disagreement sweep (obligation 4), probed on main at\n46d6db9c and unchanged by U4, which is behaviour-preserving.\n\nDesign 260's two fences on a SUSPENDING consuming body\n(`typechecker/consumes.py::_check_consumes_suspending_fences`, ~:196) gate on the\nBROAD bit — `node.suspends`, \"might\". Their reason is a FRAME: a suspending\nmethod's receiver is hoisted into the CALLER's coroutine frame, whose per-slot\nrelease can neither skip a field nor take a hand-written `deinit`'s place. A body\nthat \"suspends\" only by the conservative closure-call cause is never hoisted\nanywhere — no frame is built for it at all (SL-306) — so neither fence applies.\n\nREPRO (main, 46d6db9c — `.build/scratch/probe_consumes_conservative.saw` in the\nsl318u4 worktree):\n\n    func apply(f: (Int) -> Int) -> Int { f(1) }\n\n    struct Holder { r: Res, n: Int }\n    extension Holder: NoCopy { func deinit(&var self) { print(\"holder gone\") } }\n    extension Holder {\n        func finish(&var self) consumes -> Int { apply({ v in v + self.n }) }\n    }\n\n    error: `finish` is `consumes` on `Holder`, which has a hand-written `deinit`,\n    and it SUSPENDS — design 260 v1 does not support that combination\n    hint: ... Make the consuming method `sync`, or drop the hand-written `deinit`\n\nNothing in `finish` parks. The hint's first out is the tell: writing `sync` on\nthe method is refused too (the same conservative cause violates a `sync`\ndeclaration, correctly — it is not provably suspension-free), so the author is\ntold to do a thing the compiler will not accept, and the only way out is to stop\ncalling a closure-taking helper. The field-move fence (the same function's other\narm) reads the same bit and refuses the same way.\n\nMECHANISM (obligation 4): the same one as the parameter-default refusal filed\nbeside this — a consumer that must decide \"is a FRAME owed here?\" reading the\nderivation that answers \"is this provably suspension-free?\". Design 275 U4 makes\nboth askable by name (`frame_boundary` vs `might_suspend`) and preserves every\nconsumer's current reading.\n\nFIX DIRECTION: both fences ask `frame_boundary`. Strictly widening (a program\nrefused today would compile), and the receiver-in-the-caller's-frame argument\nthey rest on is exactly what `frame_boundary` decides, so the fix is one word per\nfence once the ledger owns the reading — SL-318 U1/U2.\n","id":"SL-325","labels":["design-275"],"priority":"normal","project":"SL","revision":1,"sequence":1015,"stage":"backlog","status":"open","title":"design 260's consuming fences fire for a body that only MIGHT suspend","updated":"1789769260"},"before":null,"id":"SL-325","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl318u4\",\"description\":\"Found by design 275 U4's disagreement sweep (obligation 4), probed on main at\\n46d6db9c and unchanged by U4, which is behaviour-preserving.\\n\\nDesign 260's two fences on a SUSPENDING consuming body\\n(`typechecker/consumes.py::_check_consumes_suspending_fences`, ~:196) gate on the\\nBROAD bit — `node.suspends`, \\\"might\\\". Their reason is a FRAME: a suspending\\nmethod's receiver is hoisted into the CALLER's coroutine frame, whose per-slot\\nrelease can neither skip a field nor take a hand-written `deinit`'s place. A body\\nthat \\\"suspends\\\" only by the conservative closure-call cause is never hoisted\\nanywhere — no frame is built for it at all (SL-306) — so neither fence applies.\\n\\nREPRO (main, 46d6db9c — `.build/scratch/probe_consumes_conservative.saw` in the\\nsl318u4 worktree):\\n\\n    func apply(f: (Int) -> Int) -> Int { f(1) }\\n\\n    struct Holder { r: Res, n: Int }\\n    extension Holder: NoCopy { func deinit(&var self) { print(\\\"holder gone\\\") } }\\n    extension Holder {\\n        func finish(&var self) consumes -> Int { apply({ v in v + self.n }) }\\n    }\\n\\n    error: `finish` is `consumes` on `Holder`, which has a hand-written `deinit`,\\n    and it SUSPENDS — design 260 v1 does not support that combination\\n    hint: ... Make the consuming method `sync`, or drop the hand-written `deinit`\\n\\nNothing in `finish` parks. The hint's first out is the tell: writing `sync` on\\nthe method is refused too (the same conservative cause violates a `sync`\\ndeclaration, correctly — it is not provably suspension-free), so the author is\\ntold to do a thing the compiler will not accept, and the only way out is to stop\\ncalling a closure-taking helper. The field-move fence (the same function's other\\narm) reads the same bit and refuses the same way.\\n\\nMECHANISM (obligation 4): the same one as the parameter-default refusal filed\\nbeside this — a consumer that must decide \\\"is a FRAME owed here?\\\" reading the\\nderivation that answers \\\"is this provably suspension-free?\\\". Design 275 U4 makes\\nboth askable by name (`frame_boundary` vs `might_suspend`) and preserves every\\nconsumer's current reading.\\n\\nFIX DIRECTION: both fences ask `frame_boundary`. Strictly widening (a program\\nrefused today would compile), and the receiver-in-the-caller's-frame argument\\nthey rest on is exactly what `frame_boundary` decides, so the fix is one word per\\nfence once the ledger owns the reading — SL-318 U1/U2.\\n\",\"labels\":[\"design-275\"],\"project\":\"SL\",\"request_id\":\"pid-23644-1789769260-5000\",\"stage\":\"backlog\",\"title\":\"design 260's consuming fences fire for a body that only MIGHT suspend\"}","request_id":"pid-23644-1789769260-5000","sequence":1015,"timestamp":"1789769260"}
---
