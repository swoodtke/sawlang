---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791152","description":"## Scope and status\n\nDF-242c: Disambiguate integer overloads using exact-typed literals\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/249-module-keyed-functions.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/249-module-keyed-functions.md)\n- [sawlang/designs/137-fixed-capacity-formatting.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/137-fixed-capacity-formatting.md)\n- [sawlang/designs/47-platform-int.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/47-platform-int.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 56–56\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L56-L56)\n\n- DF‑242c — a SUFFIXED (exact-typed) literal argument does not disambiguate an `Int`-vs-narrow overload set; same-module and cross-module alike (entry below, found probing DF‑238a's fix). RE-PROBED against design 249 Aug 27 and SURVIVES verbatim, as the entry predicted — the matcher's own question, not the registry's\n\n### sawlang TODO lines 1935–1959\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L1935-L1959)\n\n## DF‑242c — a SUFFIXED literal does not disambiguate an `Int`-vs-narrow\n## overload set (filed Aug 21, probing DF‑238a's fix)\n\n`pick(200u8)` against `pick(n: Int)` + `pick(b: UInt8)` is ``ambiguous call to\n`pick`: multiple overloads match the argument types (UInt8)``, naming both. A\nsuffixed literal is EXACT-TYPED (design 47), so `UInt8` should win outright;\ndesign 137's documented ambiguity is for a BARE literal, whose width is\ndeliberately still flexible (`h(5)` between `h(Int)` and `h(Int8)`), and the\nworkaround it names is the suffix — which does not work. SAME-MODULE and\ncross-module alike (`.build/scratch/p238c.saw` is the same-module control), so\nthis is the overload matcher's own question and not an import or qualifier one:\nthe candidate filter is admitting `Int` for a `UInt8` argument somewhere it\nshould not. One line, no pin — the shape is a two-overload file.\n\nRE-PROBED Aug 27 against design 249 and it SURVIVES, verbatim, as this entry\npredicted. Post-fix behaviour on record: `pick(200u8)` against `pick(n: Int)` +\n`pick(b: UInt8)` is still ``ambiguous call to `pick`: multiple overloads match\nthe argument types (UInt8)`` with both named, same-module and cross-module\nalike. Module identity moved WHICH candidates a call sees; it does not touch\nhow the matcher scores the ones it sees, and the cross-module cell now reaches\nthe same tie by a shorter road (a bare `pick(3)` across two modules ties for\nexactly this reason). Probes: `.build/scratch/p249_242c.saw` (same-module\ncontrol) and `p249_242b_glob.saw` before its `let n: Int` was added to route\naround this finding.\n","id":"SL-13","labels":["todo-import","backlog","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":13,"status":"open","title":"DF-242c: Disambiguate integer overloads using exact-typed literals","updated":"1788791152"},"before":null,"id":"SL-13","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-242c: Disambiguate integer overloads using exact-typed literals\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/249-module-keyed-functions.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/249-module-keyed-functions.md)\\n- [sawlang/designs/137-fixed-capacity-formatting.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/137-fixed-capacity-formatting.md)\\n- [sawlang/designs/47-platform-int.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/47-platform-int.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 56–56\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L56-L56)\\n\\n- DF‑242c — a SUFFIXED (exact-typed) literal argument does not disambiguate an `Int`-vs-narrow overload set; same-module and cross-module alike (entry below, found probing DF‑238a's fix). RE-PROBED against design 249 Aug 27 and SURVIVES verbatim, as the entry predicted — the matcher's own question, not the registry's\\n\\n### sawlang TODO lines 1935–1959\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L1935-L1959)\\n\\n## DF‑242c — a SUFFIXED literal does not disambiguate an `Int`-vs-narrow\\n## overload set (filed Aug 21, probing DF‑238a's fix)\\n\\n`pick(200u8)` against `pick(n: Int)` + `pick(b: UInt8)` is ``ambiguous call to\\n`pick`: multiple overloads match the argument types (UInt8)``, naming both. A\\nsuffixed literal is EXACT-TYPED (design 47), so `UInt8` should win outright;\\ndesign 137's documented ambiguity is for a BARE literal, whose width is\\ndeliberately still flexible (`h(5)` between `h(Int)` and `h(Int8)`), and the\\nworkaround it names is the suffix — which does not work. SAME-MODULE and\\ncross-module alike (`.build/scratch/p238c.saw` is the same-module control), so\\nthis is the overload matcher's own question and not an import or qualifier one:\\nthe candidate filter is admitting `Int` for a `UInt8` argument somewhere it\\nshould not. One line, no pin — the shape is a two-overload file.\\n\\nRE-PROBED Aug 27 against design 249 and it SURVIVES, verbatim, as this entry\\npredicted. Post-fix behaviour on record: `pick(200u8)` against `pick(n: Int)` +\\n`pick(b: UInt8)` is still ``ambiguous call to `pick`: multiple overloads match\\nthe argument types (UInt8)`` with both named, same-module and cross-module\\nalike. Module identity moved WHICH candidates a call sees; it does not touch\\nhow the matcher scores the ones it sees, and the cross-module cell now reaches\\nthe same tie by a shorter road (a bare `pick(3)` across two modules ties for\\nexactly this reason). Probes: `.build/scratch/p249_242c.saw` (same-module\\ncontrol) and `p249_242b_glob.saw` before its `let n: Int` was added to route\\naround this finding.\\n\",\"labels\":[\"todo-import\",\"backlog\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-9def7762212da2326f32-create\",\"title\":\"DF-242c: Disambiguate integer overloads using exact-typed literals\"}","request_id":"todo-20260907-9def7762212da2326f32-create","sequence":13,"timestamp":"1788791152"}
---
