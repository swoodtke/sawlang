---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791243","description":"## Scope and status\n\nDF-176a: Revisit same-root place read/write spelling when a consumer needs it\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: retain the source’s deferral or revisit trigger.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 7609–7612\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7609-L7612)\n\n- **DF‑176a: SKIPPED by choice (user)** — stays filed; the compound\n  spelling (`*=`) is the idiom; the RHS-first-vs-clean-error ruling waits\n  for a real collision.\n\n### sawlang TODO lines 7714–7732\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7714-L7732)\n\n- **DF‑176a (COMPILER, filed Aug 7 by unit 13's probing; PRE-EXISTING, verified\n  against unmodified `main`): a place READ in the RHS of a place WRITE to the\n  same root is a wrong error or an ICE.** `v[0] = v[0] * 4` on a local root\n  reports ``cannot copy value of type `Vector<Int, GlobalAllocator>` which\n  implements ExplicitCopy`` — the element is a trivial `Int` and nothing is\n  being copied; the same shape through a receiver field\n  (`self.cells[i] = self.cells[i] * by`, in a `&self` OR a `&var self` method)\n  dies with `internal compiler error: 'self' not found in current scope`. The\n  root is `place_uses._assignment`, which lowers the RHS first and then wraps\n  the whole assignment in the TARGET's window, so the RHS window ends up NESTED\n  inside the write window and two overlapping borrows of one root reach the\n  checker with no diagnostic that names them. The compound spelling\n  (`v[0] *= 4`, `self.cells[i] *= by`) works and is the idiom, so the\n  user-visible cost is a read-modify-write spelling that fails confusingly\n  rather than a capability gap. Needs a decision before a fix: either evaluate\n  a place write's RHS BEFORE opening the target window (making the shape legal,\n  which is what every other language does here) or make it a clean exclusivity\n  error naming the two windows and pointing at `*=`. Probes:\n  `.build/scratch/p176_scale{,2,4,5}.saw`.\n","id":"SL-111","labels":["todo-import","needs-verification","deferred","bug","plan"],"priority":"low","project":"SL","revision":1,"sequence":111,"status":"open","title":"DF-176a: Revisit same-root place read/write spelling when a consumer needs it","updated":"1788791243"},"before":null,"id":"SL-111","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-176a: Revisit same-root place read/write spelling when a consumer needs it\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: retain the source’s deferral or revisit trigger.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 7609–7612\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7609-L7612)\\n\\n- **DF‑176a: SKIPPED by choice (user)** — stays filed; the compound\\n  spelling (`*=`) is the idiom; the RHS-first-vs-clean-error ruling waits\\n  for a real collision.\\n\\n### sawlang TODO lines 7714–7732\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7714-L7732)\\n\\n- **DF‑176a (COMPILER, filed Aug 7 by unit 13's probing; PRE-EXISTING, verified\\n  against unmodified `main`): a place READ in the RHS of a place WRITE to the\\n  same root is a wrong error or an ICE.** `v[0] = v[0] * 4` on a local root\\n  reports ``cannot copy value of type `Vector<Int, GlobalAllocator>` which\\n  implements ExplicitCopy`` — the element is a trivial `Int` and nothing is\\n  being copied; the same shape through a receiver field\\n  (`self.cells[i] = self.cells[i] * by`, in a `&self` OR a `&var self` method)\\n  dies with `internal compiler error: 'self' not found in current scope`. The\\n  root is `place_uses._assignment`, which lowers the RHS first and then wraps\\n  the whole assignment in the TARGET's window, so the RHS window ends up NESTED\\n  inside the write window and two overlapping borrows of one root reach the\\n  checker with no diagnostic that names them. The compound spelling\\n  (`v[0] *= 4`, `self.cells[i] *= by`) works and is the idiom, so the\\n  user-visible cost is a read-modify-write spelling that fails confusingly\\n  rather than a capability gap. Needs a decision before a fix: either evaluate\\n  a place write's RHS BEFORE opening the target window (making the shape legal,\\n  which is what every other language does here) or make it a clean exclusivity\\n  error naming the two windows and pointing at `*=`. Probes:\\n  `.build/scratch/p176_scale{,2,4,5}.saw`.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"deferred\",\"bug\",\"plan\"],\"priority\":\"low\",\"project\":\"SL\",\"request_id\":\"todo-20260907-d77a54f0465522f5e246-create\",\"title\":\"DF-176a: Revisit same-root place read/write spelling when a consumer needs it\"}","request_id":"todo-20260907-d77a54f0465522f5e246-create","sequence":111,"timestamp":"1788791243"}
---
