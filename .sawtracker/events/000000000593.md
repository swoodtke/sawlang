---
{"action":"patch_revise","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl238","author":"agent:codex-reviewer","comments":[{"author":"agent:claude-sawlang","body":"READY TO DISPATCH, but PAUSED for a morning ruling (overnight run, Sep 11): the fix replaces the ambiguous 0x01-brace-marker encoding, and that marker protocol is SHARED with selfhost/lexer/src/lib.saw (read_string) — so it is a token-representation change in the string layer that ripples into the selfhost lexer (now active SL-242 epic territory) and lexdiff parity. NEEDS a ruling on the encoding approach before dispatch: (a) carry escape provenance out-of-band (segment list / escape-position set) so no content byte is overloaded; (b) an unambiguous in-band marker (e.g. double the marker byte so a real 0x01 is distinguishable); (c) restructure the StringLit token to carry typed segments. Each has different reach into the selfhost lexer. Deferred to the user rather than guessed autonomously.","created":"1789112014","id":"c1"},{"author":"agent:claude-sawlang","body":"RULED (user, Sep 11 morning): option (c) — restructure the string token to carry TYPED SEGMENTS, and do it NOW as this issue's fix (not deferred to the design-259 reconciliation). The 0x01 marker protocol is deleted from BOTH lexers in one patch (lexdiff parity requires lockstep). Rationale: (a) pays a token-shape change without killing the parser re-scan; (b) fixes the bug but keeps the content-bytes-carry-metadata idiom every future consumer must know; (c) is the only option that removes the ambiguity by construction and it aligns with where the selfhost lexer (SL-242) and the reporter's prototype are already headed. Brief: designs/268 (written at dispatch). Dispatches after the SL-226.p1 review-fix round (serial dispatch in force).","created":"1789128838","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-238.p1 (revision 1): design 268: string tokens carry typed segments — delete the 0x01 escaped-brace marker from both lexers (fixes silent U+0001 deletion + the string-pattern face)","created":"1789137231","id":"c3"},{"author":"agent:claude-sawlang","body":"SL-238.p1 SUBMITTED (applied 6d5ef843), proposed, server gate queued — LEAD-VALIDATED. Design 268 implemented as ruled: the 0x01 marker protocol deleted from BOTH lexers; plain STRING tokens are fully decoded (a brace in the value is just a brace); INTERP_STRING tokens carry typed segments (frozen StringSegment dataclass on the Python Token; SegmentKind/StringSegment in a flat per-lex arena on the Saw side, seg_start/seg_count per token — the arena keeps Token in the automatic Copy tier, argued in place). Parser's byte re-scan deleted; interpolation sub-expressions now parse at the EXACT opening-brace position (design-99 columns exact under escapes; ZERO existing EXPECT files drift — the one in-literal column pin has no preceding escape — and two new error tests pin columns the old arithmetic got wrong). Consumer sweep found a SECOND LIVE FACE fixed by construction: string PATTERNS leaked the marker, so case \"a\\{b\" could never match its equal literal — pinned by string_pattern_escaped_brace_matches.saw. Lead independently compiled+ran the issue repro + the pattern face on the branch: 1 / 2 / 1 / pattern-matched, exit 0. Six new examples cover the obligation-4 matrix incl. a generated raw-0x01-byte file; selfhost tests assert the segment record shape. Gates (agent): suite 2459/14x, freestanding 36/36, terminal battery 25/25 (2936s) with lexdiff 0 mismatches over 3108 tracked files + selfhostlex 9/9. NOTE for design 259: the selfhost lexer's public API shape changed (lex returns LexResult carrying the segment arena; format_token takes the arena) — the parser work builds on that. Branch worktree-agent-a18fb0eaf6cfee2b8 tip fd374539; worktree stays until merge.","created":"1789137257","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-238.p1 (revision 1)","created":"1789137901","id":"c5"},{"author":"agent:codex-reviewer","body":"Review of SL-238.p1 revision 1 (base 4f488e5a; server-tested 6d5ef843): request changes.\n\nP1 — Migrate the minivm frontend with the selfhost lexer API change.\n\nAt selfhost/lexer/src/lib.saw:1322, lex() now returns Result<LexResult, LexError> instead of Result<Vector<Token>, LexError>; Token also gains required seg_start/seg_count fields. The existing consumer in prototypes/minivm/src/frontend.saw was omitted from the migration. As a result, the prototype already on main no longer builds.\n\nReproduction from the patched checkout:\n\n    /Users/swoodtke/Projects/sawlang/.venv/bin/python sawc/sawc.py prototypes/minivm/src/main.saw --module-path sawlex=selfhost/lexer -o .build/scratch/minivm\n\nThis produces five errors: four missing-field Token initializers at frontend.saw:7, :2935, :3209 and :3453, plus `field tokens expects type Vector<Token> but got LexResult` at :3526. The identical command succeeds on the exact unpatched base 4f488e5a. This is an introduced build regression, independent of implementing interpolation in the prototype. The design's exclusion of prototype string-handling work does not remove the need to keep this existing API consumer building.\n\nPlease migrate the frontend's lex result extraction and all token construction/copy sites together with this change, retaining valid segment provenance wherever it is used, then build and run the minivm regression harness. The prototype may continue rejecting interpolation as before; no feature expansion is required.\n\nValidation: all eight focused string/interpolation/position tests passed, including the original U+0001 reproducer and the pattern/raw-control-byte coverage. git diff --check passed. The tracker reports its full compiler/freestanding gate passed for 6d5ef843, but that gate did not catch the prototype build regression. The typed-segment approach addresses the original ambiguous encoding; the missing consumer migration blocks approval.\n","created":"1789138504","id":"c6"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-238.p1 (revision 1)\n\nReview of SL-238.p1 revision 1 (base 4f488e5a; server-tested 6d5ef843): request changes.\n\nP1 — Migrate the minivm frontend with the selfhost lexer API change.\n\nAt selfhost/lexer/src/lib.saw:1322, lex() now returns Result<LexResult, LexError> instead of Result<Vector<Token>, LexError>; Token also gains required seg_start/seg_count fields. The existing consumer in prototypes/minivm/src/frontend.saw was omitted from the migration. As a result, the prototype already on main no longer builds.\n\nReproduction from the patched checkout:\n\n    /Users/swoodtke/Projects/sawlang/.venv/bin/python sawc/sawc.py prototypes/minivm/src/main.saw --module-path sawlex=selfhost/lexer -o .build/scratch/minivm\n\nThis produces five errors: four missing-field Token initializers at frontend.saw:7, :2935, :3209 and :3453, plus `field tokens expects type Vector<Token> but got LexResult` at :3526. The identical command succeeds on the exact unpatched base 4f488e5a. This is an introduced build regression, independent of implementing interpolation in the prototype. The design's exclusion of prototype string-handling work does not remove the need to keep this existing API consumer building.\n\nPlease migrate the frontend's lex result extraction and all token construction/copy sites together with this change, retaining valid segment provenance wherever it is used, then build and run the minivm regression harness. The prototype may continue rejecting interpolation as before; no feature expansion is required.\n\nValidation: all eight focused string/interpolation/position tests passed, including the original U+0001 reproducer and the pattern/raw-control-byte coverage. git diff --check passed. The tracker reports its full compiler/freestanding gate passed for 6d5ef843, but that gate did not catch the prototype build regression. The typed-segment approach addresses the original ambiguous encoding; the missing consumer migration blocks approval.\n","created":"1789138533","id":"c7"},{"author":"agent:claude-sawlang","body":"Revised patch SL-238.p1 to revision 2","created":"1789143196","id":"c8"}],"created":"1789078484","description":"Confirmed on production sawlang checkout 30c5a55016aed806a2f94a64d62224c6cafc135a, macOS arm64, using its Python sawc compiler.\n\nThe lexer encodes an escaped brace as byte 0x01 followed by the brace. A legitimate Unicode escape for U+0001 followed by a Unicode escape for a brace produces the same token bytes. The parser unconditionally removes the marker, silently deleting real String content.\n\nReproducer:\n\n```saw\nfunc main() {\n    let escaped = \"\\{\"\n    let literal = \"\\u{1}\\u{7b}\"\n    print(escaped.len())\n    print(literal.len())\n    print(literal.byte_at(0) as Int)\n}\n```\n\nExpected stdout: `1`, `2`, `1`. Actual stdout: `1`, `1`, `123`, exit 0. The intended two-byte String (U+0001 and `{`) is changed to a one-byte `{` String. This example does not use interpolation. The same marker mechanism is used for closing braces.\n\nRelevant implementation: sawc/lexer.py:381-383 inserts `\\x01{` / `\\x01}` for escaped braces; sawc/parser/expressions.py:731 removes either sequence by replacement, without provenance. The existing selfhost/lexer/src/lib.saw read_string follows the same marker protocol, so its StringLit token representation is also ambiguous. Preserve whether a brace came from an escape independently of real content (or use an unambiguous encoding), and test literal marker bytes beside both braces as well as ordinary escaped braces/interpolation.\n\nLocal standalone source: /Users/swoodtke/Projects/sawlang/.build/scratch/string_brace_provenance.saw. Compiled with `/Users/swoodtke/Projects/sawlang/.venv/bin/python sawc/sawc.py .build/scratch/string_brace_provenance.saw -o .build/scratch/string_brace_provenance`, then executed directly. No production files changed. Found while implementing isolated owned-String support in the compiler prototype; that prototype will preserve literal provenance rather than reproduce this behavior.\n","id":"SL-238","labels":["bug","compiler","strings"],"priority":"normal","project":"SL","revision":10,"sequence":593,"stage":"queued","status":"open","title":"String escaped-brace marker deletes literal U+0001 bytes before braces","updated":"1789143196"},"before":{"assignee":"agent:claude-sl238","author":"agent:codex-reviewer","comments":[{"author":"agent:claude-sawlang","body":"READY TO DISPATCH, but PAUSED for a morning ruling (overnight run, Sep 11): the fix replaces the ambiguous 0x01-brace-marker encoding, and that marker protocol is SHARED with selfhost/lexer/src/lib.saw (read_string) — so it is a token-representation change in the string layer that ripples into the selfhost lexer (now active SL-242 epic territory) and lexdiff parity. NEEDS a ruling on the encoding approach before dispatch: (a) carry escape provenance out-of-band (segment list / escape-position set) so no content byte is overloaded; (b) an unambiguous in-band marker (e.g. double the marker byte so a real 0x01 is distinguishable); (c) restructure the StringLit token to carry typed segments. Each has different reach into the selfhost lexer. Deferred to the user rather than guessed autonomously.","created":"1789112014","id":"c1"},{"author":"agent:claude-sawlang","body":"RULED (user, Sep 11 morning): option (c) — restructure the string token to carry TYPED SEGMENTS, and do it NOW as this issue's fix (not deferred to the design-259 reconciliation). The 0x01 marker protocol is deleted from BOTH lexers in one patch (lexdiff parity requires lockstep). Rationale: (a) pays a token-shape change without killing the parser re-scan; (b) fixes the bug but keeps the content-bytes-carry-metadata idiom every future consumer must know; (c) is the only option that removes the ambiguity by construction and it aligns with where the selfhost lexer (SL-242) and the reporter's prototype are already headed. Brief: designs/268 (written at dispatch). Dispatches after the SL-226.p1 review-fix round (serial dispatch in force).","created":"1789128838","id":"c2"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-238.p1 (revision 1): design 268: string tokens carry typed segments — delete the 0x01 escaped-brace marker from both lexers (fixes silent U+0001 deletion + the string-pattern face)","created":"1789137231","id":"c3"},{"author":"agent:claude-sawlang","body":"SL-238.p1 SUBMITTED (applied 6d5ef843), proposed, server gate queued — LEAD-VALIDATED. Design 268 implemented as ruled: the 0x01 marker protocol deleted from BOTH lexers; plain STRING tokens are fully decoded (a brace in the value is just a brace); INTERP_STRING tokens carry typed segments (frozen StringSegment dataclass on the Python Token; SegmentKind/StringSegment in a flat per-lex arena on the Saw side, seg_start/seg_count per token — the arena keeps Token in the automatic Copy tier, argued in place). Parser's byte re-scan deleted; interpolation sub-expressions now parse at the EXACT opening-brace position (design-99 columns exact under escapes; ZERO existing EXPECT files drift — the one in-literal column pin has no preceding escape — and two new error tests pin columns the old arithmetic got wrong). Consumer sweep found a SECOND LIVE FACE fixed by construction: string PATTERNS leaked the marker, so case \"a\\{b\" could never match its equal literal — pinned by string_pattern_escaped_brace_matches.saw. Lead independently compiled+ran the issue repro + the pattern face on the branch: 1 / 2 / 1 / pattern-matched, exit 0. Six new examples cover the obligation-4 matrix incl. a generated raw-0x01-byte file; selfhost tests assert the segment record shape. Gates (agent): suite 2459/14x, freestanding 36/36, terminal battery 25/25 (2936s) with lexdiff 0 mismatches over 3108 tracked files + selfhostlex 9/9. NOTE for design 259: the selfhost lexer's public API shape changed (lex returns LexResult carrying the segment arena; format_token takes the arena) — the parser work builds on that. Branch worktree-agent-a18fb0eaf6cfee2b8 tip fd374539; worktree stays until merge.","created":"1789137257","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-238.p1 (revision 1)","created":"1789137901","id":"c5"},{"author":"agent:codex-reviewer","body":"Review of SL-238.p1 revision 1 (base 4f488e5a; server-tested 6d5ef843): request changes.\n\nP1 — Migrate the minivm frontend with the selfhost lexer API change.\n\nAt selfhost/lexer/src/lib.saw:1322, lex() now returns Result<LexResult, LexError> instead of Result<Vector<Token>, LexError>; Token also gains required seg_start/seg_count fields. The existing consumer in prototypes/minivm/src/frontend.saw was omitted from the migration. As a result, the prototype already on main no longer builds.\n\nReproduction from the patched checkout:\n\n    /Users/swoodtke/Projects/sawlang/.venv/bin/python sawc/sawc.py prototypes/minivm/src/main.saw --module-path sawlex=selfhost/lexer -o .build/scratch/minivm\n\nThis produces five errors: four missing-field Token initializers at frontend.saw:7, :2935, :3209 and :3453, plus `field tokens expects type Vector<Token> but got LexResult` at :3526. The identical command succeeds on the exact unpatched base 4f488e5a. This is an introduced build regression, independent of implementing interpolation in the prototype. The design's exclusion of prototype string-handling work does not remove the need to keep this existing API consumer building.\n\nPlease migrate the frontend's lex result extraction and all token construction/copy sites together with this change, retaining valid segment provenance wherever it is used, then build and run the minivm regression harness. The prototype may continue rejecting interpolation as before; no feature expansion is required.\n\nValidation: all eight focused string/interpolation/position tests passed, including the original U+0001 reproducer and the pattern/raw-control-byte coverage. git diff --check passed. The tracker reports its full compiler/freestanding gate passed for 6d5ef843, but that gate did not catch the prototype build regression. The typed-segment approach addresses the original ambiguous encoding; the missing consumer migration blocks approval.\n","created":"1789138504","id":"c6"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-238.p1 (revision 1)\n\nReview of SL-238.p1 revision 1 (base 4f488e5a; server-tested 6d5ef843): request changes.\n\nP1 — Migrate the minivm frontend with the selfhost lexer API change.\n\nAt selfhost/lexer/src/lib.saw:1322, lex() now returns Result<LexResult, LexError> instead of Result<Vector<Token>, LexError>; Token also gains required seg_start/seg_count fields. The existing consumer in prototypes/minivm/src/frontend.saw was omitted from the migration. As a result, the prototype already on main no longer builds.\n\nReproduction from the patched checkout:\n\n    /Users/swoodtke/Projects/sawlang/.venv/bin/python sawc/sawc.py prototypes/minivm/src/main.saw --module-path sawlex=selfhost/lexer -o .build/scratch/minivm\n\nThis produces five errors: four missing-field Token initializers at frontend.saw:7, :2935, :3209 and :3453, plus `field tokens expects type Vector<Token> but got LexResult` at :3526. The identical command succeeds on the exact unpatched base 4f488e5a. This is an introduced build regression, independent of implementing interpolation in the prototype. The design's exclusion of prototype string-handling work does not remove the need to keep this existing API consumer building.\n\nPlease migrate the frontend's lex result extraction and all token construction/copy sites together with this change, retaining valid segment provenance wherever it is used, then build and run the minivm regression harness. The prototype may continue rejecting interpolation as before; no feature expansion is required.\n\nValidation: all eight focused string/interpolation/position tests passed, including the original U+0001 reproducer and the pattern/raw-control-byte coverage. git diff --check passed. The tracker reports its full compiler/freestanding gate passed for 6d5ef843, but that gate did not catch the prototype build regression. The typed-segment approach addresses the original ambiguous encoding; the missing consumer migration blocks approval.\n","created":"1789138533","id":"c7"}],"created":"1789078484","description":"Confirmed on production sawlang checkout 30c5a55016aed806a2f94a64d62224c6cafc135a, macOS arm64, using its Python sawc compiler.\n\nThe lexer encodes an escaped brace as byte 0x01 followed by the brace. A legitimate Unicode escape for U+0001 followed by a Unicode escape for a brace produces the same token bytes. The parser unconditionally removes the marker, silently deleting real String content.\n\nReproducer:\n\n```saw\nfunc main() {\n    let escaped = \"\\{\"\n    let literal = \"\\u{1}\\u{7b}\"\n    print(escaped.len())\n    print(literal.len())\n    print(literal.byte_at(0) as Int)\n}\n```\n\nExpected stdout: `1`, `2`, `1`. Actual stdout: `1`, `1`, `123`, exit 0. The intended two-byte String (U+0001 and `{`) is changed to a one-byte `{` String. This example does not use interpolation. The same marker mechanism is used for closing braces.\n\nRelevant implementation: sawc/lexer.py:381-383 inserts `\\x01{` / `\\x01}` for escaped braces; sawc/parser/expressions.py:731 removes either sequence by replacement, without provenance. The existing selfhost/lexer/src/lib.saw read_string follows the same marker protocol, so its StringLit token representation is also ambiguous. Preserve whether a brace came from an escape independently of real content (or use an unambiguous encoding), and test literal marker bytes beside both braces as well as ordinary escaped braces/interpolation.\n\nLocal standalone source: /Users/swoodtke/Projects/sawlang/.build/scratch/string_brace_provenance.saw. Compiled with `/Users/swoodtke/Projects/sawlang/.venv/bin/python sawc/sawc.py .build/scratch/string_brace_provenance.saw -o .build/scratch/string_brace_provenance`, then executed directly. No production files changed. Found while implementing isolated owned-String support in the compiler prototype; that prototype will preserve literal provenance rather than reproduce this behavior.\n","id":"SL-238","labels":["bug","compiler","strings"],"priority":"normal","project":"SL","revision":9,"sequence":588,"stage":"queued","status":"open","title":"String escaped-brace marker deletes literal U+0001 bytes before braces","updated":"1789138533"},"id":"SL-238","project":"SL","request":"{\"action\":\"patch_revise\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/designs/268-string-token-typed-segments.md b/designs/268-string-token-typed-segments.md\\nnew file mode 100644\\nindex 00000000..b621eedc\\n--- /dev/null\\n+++ b/designs/268-string-token-typed-segments.md\\n@@ -0,0 +1,86 @@\\n+# Design 268 — String tokens carry typed segments (SL-238)\\n+\\n+**Status:** ruled Sep 11 2026 (user): fix SL-238 properly NOW via option (c) —\\n+typed segments — not deferred to the design-259 token/AST reconciliation.\\n+**Issue:** SL-238 (queued). **Seam:** lexer/parser string layer, BOTH lexers.\\n+\\n+## The defect\\n+\\n+The lexers encode an escaped brace as the two bytes `0x01`+brace inside the\\n+token's flat value string (`sawc/lexer.py:381-383`;\\n+`selfhost/lexer/src/lib.saw:730-737`, `B_MARKER`). Two parser sites decode it:\\n+`sawc/parser/expressions.py:731` (plain STRING: chained `.replace`) and\\n+`:1878-1883` (INTERP_STRING re-scan: `0x01`+brace = content, bare `{` =\\n+interpolation start). But `\\\"\\\\u{1}\\\\u{7b}\\\"` — or a raw U+0001 byte typed in the\\n+source — legitimately produces the same two bytes, and the decode deletes the\\n+real U+0001. Silent content corruption, exit 0 (repro + expected output in\\n+SL-238's body).\\n+\\n+## The design\\n+\\n+Delete the marker protocol from both lexers. The lexer already knows, at lex\\n+time, which braces open interpolations (it runs the brace-depth scan today) —\\n+so it emits that knowledge as structure instead of re-encoding it into bytes\\n+the parser must re-discover:\\n+\\n+- **Plain `STRING` token:** value is the fully-decoded content; braces are\\n+  ordinary bytes (a plain string can only contain a brace via an escape, so no\\n+  disambiguation is ever needed). The `:731` marker strip is deleted.\\n+- **`INTERP_STRING` token:** carries an ordered **segment list** instead of a\\n+  flat value. Segment kinds:\\n+  - `text(bytes)` — decoded literal content (escaped braces are plain `{`/`}`\\n+    bytes here; escapes decoded exactly as today);\\n+  - `expr(raw_text, line, col)` — the RAW source text between an\\n+    interpolation's braces (no escape decoding inside `{...}`, unchanged),\\n+    with the EXACT source position of its opening `{`. Empty/blank raw_text is\\n+    the design-137 format placeholder.\\n+- **Parser:** `_parse_interpolated_string`'s byte re-scan\\n+  (`expressions.py:1864+`) is replaced by direct segment consumption;\\n+  `_parse_expression_from_string` sub-parses `expr` segments as today, but\\n+  seeded with the segment's exact position. The design-99 position contract\\n+  improves: lines stay exact, columns become exact even under escape\\n+  sequences (the current code documents them as approximate). Sweep EXPECT\\n+  tests for column drift; if the churn is more than a handful of files,\\n+  STOP and report the count before proceeding.\\n+- **Selfhost lexer:** same segmentation in `read_string`; the Saw `Token`\\n+  gains the segment representation for InterpString (shape chosen to fit the\\n+  existing struct idiom — a `Vector` of a small segment struct is the obvious\\n+  form). `B_MARKER` and both `err_at` marker mentions of the old protocol go.\\n+- **Dump/parity:** `tools/dump_tokens.py` and the selfhost dumper emit ONE\\n+  canonical record form for segments; `tools/lexdiff.py` compares it. Both\\n+  lexers and both dumpers change in the SAME patch — lexdiff is the lockstep\\n+  gate. The `--docs` trivia sweep is untouched.\\n+\\n+## Obligations\\n+\\n+1. (Funnel) Segment decoding/consumption has ONE consumer path per side; the\\n+   two scattered marker-decode sites are deleted, none added. The segment\\n+   record form is defined in one place per lexer (named in docstrings).\\n+2. (Contract flip → consumer sweep) BEFORE coding, sweep every consumer of\\n+   `TokenType.STRING` / `INTERP_STRING` token values across `sawc/`, `tools/`,\\n+   `selfhost/`, `devtools/` (grep + one paragraph in the report). Known:\\n+   the two parser sites, `dump_tokens.py`, lexdiff, the selfhost dumper.\\n+   Anything else found joins the matrix.\\n+3. Not a safety-guarantee surface — no conformance rows owed.\\n+4. The SL-238 repro is one face; the mechanism is \\\"content bytes overloaded\\n+   as metadata\\\". The matrix must cover: `\\\\u{1}` and raw 0x01 before `{`, `}`,\\n+   escaped braces, and interpolation, in plain AND interpolated strings;\\n+   escaped braces adjacent to real interpolations; `\\\\u{1}` inside\\n+   interpolation raw text; multi-line interpolated strings (position checks);\\n+   format placeholders beside escaped braces.\\n+\\n+## Tests\\n+\\n+- SL-238's repro verbatim as a regression (`1`, `2`, `1`).\\n+- The obligation-4 matrix as examples/ tests with exact expected output.\\n+- Existing interpolation/format/brace tests must pass unchanged except\\n+  documented column-exactness diffs.\\n+- Gates: per-commit compiler gate; TERMINAL battery — `lexdiff`,\\n+  `selfhostlex`, `astdiff`, `reemit`, `irdet --all` are the load-bearing\\n+  lanes for a token-shape change.\\n+\\n+## Out of scope\\n+\\n+Nested string literals inside interpolation expressions confusing the\\n+brace-depth scan (pre-existing, unchanged by segmentation — file separately\\n+if probed). The minivm prototype's own string handling (SL-242 side).\\ndiff --git a/designs/INDEX.md b/designs/INDEX.md\\nindex ecc1e985..b2eece25 100644\\n--- a/designs/INDEX.md\\n+++ b/designs/INDEX.md\\n@@ -260,6 +260,7 @@ Navigation only. One line per design brief in `designs/` (number, file, the brie\\n - **252** — `252-unsigned-comparison.md` — the DF-270d fix: unsigned ordered comparisons lower unsigned (the icmp chokepoint learns signedness, the alias dispatch stops mis-kinding primitive aliases into the struct path), the signedness sweep over every operator lowering, and design 250's two held cbor rows reopen as the live proof (authored + dispatched Aug 28, queue head per the correctness precedent)\\n - **253** — `253-float-text.md` — the Float↔text story: pure-Saw correctly-rounded conversions both directions (shortest round-trip formatting, Eisel-Lemire-with-exact-fallback parsing), Python-generated committed vectors as the bit-exact oracle, JsonValue.Number's Float cell reopens under the pinned rule (authored + dispatched Aug 28, second at head)\\n - **267** — `267-ownership-boundary-inventory.md` — SL-209 unit A (SL-210): the ownership BOUNDARY INVENTORY (every boundary and every producer mapped to its site and its conformance rows, the three funnels named, the producer census over all 45 `Expression` subclasses) plus a STRUCTURED transfer decision per occurrence — `_check_value_transfer` stops answering in error-or-`needs_copy` and files an explicit record, so \\\"checked, nothing owed\\\" stops being indistinguishable from \\\"never checked\\\". Behaviour-preserving; findings SL-218/SL-219/SL-220 filed, not fixed (number PROVISIONAL, Sep 9)\\n+- **268** — `268-string-token-typed-segments.md` — SL-238's fix by construction: the 0x01 escaped-brace marker protocol is DELETED from both lexers and a string token carries TYPED SEGMENTS instead (plain STRING = fully decoded value; INTERP_STRING = an ordered `text`/`expr` list, each `expr` carrying the raw source text and the EXACT position of its `{`), so no content byte is metadata and the parser's byte re-scan is gone. Design 99's columns become exact under escapes; the string PATTERN position's missing strip (a second face of the bug) closes with it (ruled + built Sep 11)\\n - **archive** — `done_aug1-aug9.md` — Aug 1 – Aug 9, 2026 — landed/closed tracker recaps, archived at the Aug-9 split\\n - **archive** — `done_aug10-aug17.md` — Aug 10 – Aug 17, 2026 — landed/closed tracker recaps, archived at the Aug-17 split\\n - **archive** — `done_aug18-aug25.md` — Aug 18 – Aug 25, 2026 — landed/closed tracker recaps, opened at the Aug-18 split\\ndiff --git a/examples/errors/interp_error_position_after_escape.saw b/examples/errors/interp_error_position_after_escape.saw\\nnew file mode 100644\\nindex 00000000..67d9b564\\n--- /dev/null\\n+++ b/examples/errors/interp_error_position_after_escape.saw\\n@@ -0,0 +1,20 @@\\n+// Design 268 sharpened design 99's position contract: an interpolation's\\n+// column is the column of its own `{` in the SOURCE, so escapes ahead of it no\\n+// longer shift the report. The lexer records the brace's real position now,\\n+// where the parser used to count characters of the DECODED value — one\\n+// character per escape — and document its columns as approximate.\\n+//\\n+// `\\\\t` and `\\\\\\\\` each cost two source columns and one decoded character, and the\\n+// decoded `\\\\n` used to send the old arithmetic down its multi-line branch and\\n+// report the error a line too far down.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: cannot interpolate value of type `Blob`\\n+// The `{` sits at column 26 and the expression at 27; the old arithmetic said\\n+// 18:4 — a line down and 23 columns left.\\n+// EXPECT-ERROR-CONTAINS: interp_error_position_after_escape.saw:19:27\\n+struct Blob { n: Int }\\n+\\n+func main() {\\n+    let b = Blob(n: 1)\\n+    print(\\\"a\\\\tb\\\\\\\\c\\\\{d\\\\ne {b}\\\")\\n+}\\ndiff --git a/examples/errors/interp_error_position_multiline.saw b/examples/errors/interp_error_position_multiline.saw\\nnew file mode 100644\\nindex 00000000..c60e25d6\\n--- /dev/null\\n+++ b/examples/errors/interp_error_position_multiline.saw\\n@@ -0,0 +1,19 @@\\n+// Design 268: in a MULTI-LINE interpolated string an interpolation reports its\\n+// own physical line AND its own source column. The line was already exact; the\\n+// column came from counting characters of the decoded value since the last\\n+// newline in it, so an escape earlier on that line moved it left (and a\\n+// decoded `\\\\n` could be mistaken for a real one — see the sibling test\\n+// interp_error_position_after_escape.saw).\\n+//\\n+// `\\\\t` costs two source columns on the second line: the `{` is at column 6 and\\n+// the expression at 7, where the old arithmetic said 6.\\n+// EXPECT: error\\n+// EXPECT-ERROR-CONTAINS: cannot interpolate value of type `Blob`\\n+// EXPECT-ERROR-CONTAINS: interp_error_position_multiline.saw:18:7\\n+struct Blob { n: Int }\\n+\\n+func main() {\\n+    let b = Blob(n: 1)\\n+    print(\\\"head\\n+a\\\\tb {b} tail\\\")\\n+}\\ndiff --git a/examples/interp_string_escaped_braces_and_control_bytes.saw b/examples/interp_string_escaped_braces_and_control_bytes.saw\\nnew file mode 100644\\nindex 00000000..21161d52\\n--- /dev/null\\n+++ b/examples/interp_string_escaped_braces_and_control_bytes.saw\\n@@ -0,0 +1,62 @@\\n+// Design 268: an INTERPOLATED string carries typed segments, so an escaped\\n+// brace is literal text and only an unescaped `{` opens an interpolation — with\\n+// no marker byte in between for real content to collide with (SL-238).\\n+//\\n+// Every row here mixes the two things the old in-band encoding confused: an\\n+// escaped brace (which used to become 0x01 + brace in the token value) and a\\n+// genuine U+0001 (which produces those same bytes as content). Rows that would\\n+// print a control byte report lengths and byte values instead.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT: {7}\\n+// EXPECT-OUTPUT: 7{7\\n+// EXPECT-OUTPUT: {7}{7}\\n+// EXPECT-OUTPUT: 77\\n+// EXPECT-OUTPUT: control-then-interp 2 1 55\\n+// EXPECT-OUTPUT: control-then-escaped-brace 3 1 123 55\\n+// EXPECT-OUTPUT: control-then-bare-close 3 1 125 55\\n+// EXPECT-OUTPUT: control-inside-expression v=1\\n+// EXPECT-OUTPUT: placeholder-in-braces {7}\\n+// EXPECT-OUTPUT: two-lines 3 97 10 55\\n+\\n+func main() {\\n+    let n = 7\\n+\\n+    // Escaped braces on both sides of a real interpolation.\\n+    print(\\\"\\\\{{n}\\\\}\\\")\\n+\\n+    // An escaped brace BETWEEN two interpolations.\\n+    print(\\\"{n}\\\\{{n}\\\")\\n+\\n+    // …and one wrapping each of two interpolations.\\n+    print(\\\"\\\\{{n}\\\\}\\\\{{n}\\\\}\\\")\\n+\\n+    // Adjacent interpolations with no text between them.\\n+    print(\\\"{n}{n}\\\")\\n+\\n+    // A real U+0001 directly before an interpolation.\\n+    let ci = \\\"\\\\u{1}{n}\\\"\\n+    print(\\\"control-then-interp {ci.len()} {ci.byte_at(0) as Int} {ci.byte_at(1) as Int}\\\")\\n+\\n+    // A real U+0001 directly before an ESCAPED brace, in a string that also\\n+    // interpolates — the SL-238 collision inside an INTERP_STRING token.\\n+    let ce = \\\"\\\\u{1}\\\\{{n}\\\"\\n+    print(\\\"control-then-escaped-brace {ce.len()} {ce.byte_at(0) as Int} {ce.byte_at(1) as Int} {ce.byte_at(2) as Int}\\\")\\n+\\n+    // A real U+0001 before an unescaped `}`, which is ordinary text here.\\n+    let cb = \\\"\\\\u{1}}{n}\\\"\\n+    print(\\\"control-then-bare-close {cb.len()} {cb.byte_at(0) as Int} {cb.byte_at(1) as Int} {cb.byte_at(2) as Int}\\\")\\n+\\n+    // A U+0001 inside the interpolation's RAW text: the segment carries the\\n+    // source characters through to the sub-parser undecoded, so the escape is\\n+    // decoded by the sub-lex and the `1` below is that string's length.\\n+    print(\\\"control-inside-expression v={\\\"\\\\u{1}\\\".len()}\\\")\\n+\\n+    // A design-137 format placeholder between escaped braces.\\n+    print(\\\"placeholder-in-braces \\\\{{}\\\\}\\\", n)\\n+\\n+    // A multi-line interpolated string: the newline is literal content and the\\n+    // interpolation on the second line still lands in the right place.\\n+    let two = \\\"a\\n+{n}\\\"\\n+    print(\\\"two-lines {two.len()} {two.byte_at(0) as Int} {two.byte_at(1) as Int} {two.byte_at(2) as Int}\\\")\\n+}\\ndiff --git a/examples/string_escaped_brace_keeps_control_byte.saw b/examples/string_escaped_brace_keeps_control_byte.saw\\nnew file mode 100644\\nindex 00000000..a08037c6\\n--- /dev/null\\n+++ b/examples/string_escaped_brace_keeps_control_byte.saw\\n@@ -0,0 +1,60 @@\\n+// SL-238 / design 268: in a PLAIN string an escaped brace is an ordinary brace\\n+// byte in the value, and a real U+0001 beside one is CONTENT.\\n+//\\n+// The old lexer encoded `\\\\{` as the two bytes 0x01 + `{` inside the flat token\\n+// value and the parser stripped that pair back out — but `\\\"\\\\u{1}\\\\u{7b}\\\"` is\\n+// those same two bytes written as content, so the strip silently deleted the\\n+// U+0001 and the two-byte string came out as the one-byte `{`. The first three\\n+// lines below are the issue's reproducer, which printed `1`, `1`, `123`.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT: 1\\n+// EXPECT-OUTPUT: 2\\n+// EXPECT-OUTPUT: 1\\n+// EXPECT-OUTPUT: open 1 123\\n+// EXPECT-OUTPUT: close 1 125\\n+// EXPECT-OUTPUT: unicode-close 2 1 125\\n+// EXPECT-OUTPUT: control-then-escaped-open 2 1 123\\n+// EXPECT-OUTPUT: control-then-escaped-close 2 1 125\\n+// EXPECT-OUTPUT: both-braces 5 97 123 98 125 99\\n+// EXPECT-OUTPUT: bare-close 1 125\\n+// EXPECT-OUTPUT: control-then-letter 2 1 120\\n+// EXPECT-OUTPUT: control-alone 1 1\\n+\\n+func main() {\\n+    // The issue's reproducer, verbatim.\\n+    let escaped = \\\"\\\\{\\\"\\n+    let literal = \\\"\\\\u{1}\\\\u{7b}\\\"\\n+    print(escaped.len())\\n+    print(literal.len())\\n+    print(literal.byte_at(0) as Int)\\n+\\n+    // An escaped brace on its own, either way round.\\n+    print(\\\"open {escaped.len()} {escaped.byte_at(0) as Int}\\\")\\n+    let closed = \\\"\\\\}\\\"\\n+    print(\\\"close {closed.len()} {closed.byte_at(0) as Int}\\\")\\n+\\n+    // The closing-brace twin of the reproducer: 0x01 + `}` as content.\\n+    let uclose = \\\"\\\\u{1}\\\\u{7d}\\\"\\n+    print(\\\"unicode-close {uclose.len()} {uclose.byte_at(0) as Int} {uclose.byte_at(1) as Int}\\\")\\n+\\n+    // A real U+0001 immediately before an ESCAPED brace — the collision the old\\n+    // encoding could not tell apart, now two plain content bytes.\\n+    let mopen = \\\"\\\\u{1}\\\\{\\\"\\n+    print(\\\"control-then-escaped-open {mopen.len()} {mopen.byte_at(0) as Int} {mopen.byte_at(1) as Int}\\\")\\n+    let mclose = \\\"\\\\u{1}\\\\}\\\"\\n+    print(\\\"control-then-escaped-close {mclose.len()} {mclose.byte_at(0) as Int} {mclose.byte_at(1) as Int}\\\")\\n+\\n+    // Escaped braces around ordinary text stay in place, in order: `a{b}c`.\\n+    let both = \\\"a\\\\{b\\\\}c\\\"\\n+    print(\\\"both-braces {both.len()} {both.byte_at(0) as Int} {both.byte_at(1) as Int} {both.byte_at(2) as Int} {both.byte_at(3) as Int} {both.byte_at(4) as Int}\\\")\\n+\\n+    // An unescaped `}` with no interpolation open is ordinary text.\\n+    let bare = \\\"}\\\"\\n+    print(\\\"bare-close {bare.len()} {bare.byte_at(0) as Int}\\\")\\n+\\n+    // A U+0001 that no brace follows was never at risk, and still is not.\\n+    let plain = \\\"\\\\u{1}x\\\"\\n+    print(\\\"control-then-letter {plain.len()} {plain.byte_at(0) as Int} {plain.byte_at(1) as Int}\\\")\\n+    let alone = \\\"\\\\u{1}\\\"\\n+    print(\\\"control-alone {alone.len()} {alone.byte_at(0) as Int}\\\")\\n+}\\ndiff --git a/examples/string_pattern_escaped_brace_matches.saw b/examples/string_pattern_escaped_brace_matches.saw\\nnew file mode 100644\\nindex 00000000..afc97cdc\\n--- /dev/null\\n+++ b/examples/string_pattern_escaped_brace_matches.saw\\n@@ -0,0 +1,34 @@\\n+// A string PATTERN holding an escaped brace matches the equal literal.\\n+//\\n+// Design 268 made the lexer hand the parser a fully decoded plain-string value,\\n+// which closes a second face of SL-238: the pattern position built its literal\\n+// straight from the token value with no marker strip (the value position had\\n+// one), so the two sides of `match` disagreed byte for byte and the arm could\\n+// never be taken — `classify(\\\"a\\\\{b\\\")` answered 0.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT: 1\\n+// EXPECT-OUTPUT: 2\\n+// EXPECT-OUTPUT: 3\\n+// EXPECT-OUTPUT: 0\\n+// EXPECT-OUTPUT: 0\\n+\\n+func classify(s: String) -> Int {\\n+    match s {\\n+        case \\\"a\\\\{b\\\" -> 1,\\n+        case \\\"\\\\u{1}\\\\u{7b}\\\" -> 2,\\n+        case \\\"\\\\}\\\" -> 3,\\n+        case _ -> 0\\n+    }\\n+}\\n+\\n+func main() {\\n+    // Each pattern matches the literal written the same way.\\n+    print(classify(\\\"a\\\\{b\\\"))\\n+    print(classify(\\\"\\\\u{1}\\\\u{7b}\\\"))\\n+    print(classify(\\\"\\\\}\\\"))\\n+\\n+    // …and the escaped brace is NOT equal to the U+0001 + brace pair, which is\\n+    // exactly the distinction the old encoding could not make.\\n+    print(classify(\\\"\\\\{\\\"))\\n+    print(classify(\\\"\\\\u{1}\\\\u{7d}\\\"))\\n+}\\ndiff --git a/examples/string_raw_control_byte_before_brace.saw b/examples/string_raw_control_byte_before_brace.saw\\nnew file mode 100644\\nindex 00000000..13708151\\n--- /dev/null\\n+++ b/examples/string_raw_control_byte_before_brace.saw\\n@@ -0,0 +1,39 @@\\n+// Design 268 / SL-238: a RAW U+0001 byte typed straight into a string literal\\n+// is ordinary content, exactly as the `\\\\u{1}` escape is. This file carries real\\n+// 0x01 bytes inside its string literals (the byte is invisible in an editor but\\n+// it is what the lexer reads), so it pins the collision one step earlier than\\n+// its `\\\\u{1}` twin: the old lexer wrote 0x01 + brace to mark an ESCAPED brace,\\n+// and the parser deleted any such pair it found — including this one.\\n+// EXPECT: success\\n+// EXPECT-OUTPUT: raw-then-escaped-open 2 1 123\\n+// EXPECT-OUTPUT: raw-then-escaped-close 2 1 125\\n+// EXPECT-OUTPUT: raw-then-bare-close 2 1 125\\n+// EXPECT-OUTPUT: raw-then-interp 2 1 55\\n+// EXPECT-OUTPUT: raw-inside-interp-text 3 97 1 55\\n+// EXPECT-OUTPUT: raw-alone 1 1\\n+\\n+func main() {\\n+    let n = 7\\n+\\n+    // A raw 0x01 before an escaped brace, either way round.\\n+    let open_escape = \\\"\\u0001\\\\{\\\"\\n+    print(\\\"raw-then-escaped-open {open_escape.len()} {open_escape.byte_at(0) as Int} {open_escape.byte_at(1) as Int}\\\")\\n+    let close_escape = \\\"\\u0001\\\\}\\\"\\n+    print(\\\"raw-then-escaped-close {close_escape.len()} {close_escape.byte_at(0) as Int} {close_escape.byte_at(1) as Int}\\\")\\n+\\n+    // A raw 0x01 before an unescaped `}`, which is ordinary text.\\n+    let bare_close = \\\"\\u0001}\\\"\\n+    print(\\\"raw-then-bare-close {bare_close.len()} {bare_close.byte_at(0) as Int} {bare_close.byte_at(1) as Int}\\\")\\n+\\n+    // A raw 0x01 before a real interpolation.\\n+    let before_interp = \\\"\\u0001{n}\\\"\\n+    print(\\\"raw-then-interp {before_interp.len()} {before_interp.byte_at(0) as Int} {before_interp.byte_at(1) as Int}\\\")\\n+\\n+    // A raw 0x01 in the literal text of a string that also interpolates.\\n+    let inside = \\\"a\\u0001{n}\\\"\\n+    print(\\\"raw-inside-interp-text {inside.len()} {inside.byte_at(0) as Int} {inside.byte_at(1) as Int} {inside.byte_at(2) as Int}\\\")\\n+\\n+    // And on its own, where no brace follows it.\\n+    let alone = \\\"\\u0001\\\"\\n+    print(\\\"raw-alone {alone.len()} {alone.byte_at(0) as Int}\\\")\\n+}\\ndiff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 0a854e75..da9e4c4f 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -42,13 +42,16 @@ the numbered milestone designs define each isolated slice.\\n   inequality on this enum are required (`lib.saw:985` and elsewhere). Numeric\\n   discriminants, payload cases, raw-value APIs, and user-defined enum layout are\\n   not required by this lexer.\\n-- Public field structs `Token`, `LexError`, `DocComment`, and `LexResult` at\\n-  `lib.saw:242-279`, plus private `Lexer` at `lib.saw:559-565`. Construction uses\\n+- Public field structs `StringSegment`, `Token`, `LexError`, `DocComment`, and\\n+  `LexResult` at `lib.saw:262-318` (design 268 added `StringSegment` and the\\n+  payload-free enum `SegmentKind` at `lib.saw:236`, and gave `Token` its\\n+  `seg_start`/`seg_count` fields), plus private `Lexer` at\\n+  `lib.saw:598-604`. Construction uses\\n   order-independent named fields, field reads, and mutable field assignment\\n   through `&var self` (`advance`, `lib.saw:576-586`). Visibility must parse and\\n   type-check; cross-module consumers need the four public models and their public\\n   fields. Private fields and custom initializers are unnecessary here.\\n-- `extension LexResult: NoCopy {}` (`lib.saw:279`) and `extension Lexer { ... }`\\n+- `extension LexResult: NoCopy {}` (`lib.saw:320`) and `extension Lexer { ... }`\\n   (`lib.saw:567-1189`). The former must suppress implicit copying; the latter\\n   supplies instance methods with `&self` and `&var self` receivers. Traits in\\n   general and extension dispatch across arbitrary types are outside this slice.\\n@@ -92,12 +95,15 @@ the numbered milestone designs define each isolated slice.\\n   arithmetic, and unsafe user pointers are not used by lexer source.\\n - Copy versus move behavior must distinguish scalar/Copy values (`Int`, `Bool`,\\n   `String`, payload-free `TokenKind`, `Byte`, unsigned integers) from aggregate\\n-  ownership. `LexResult` is explicitly `NoCopy`; its two vectors are moved into\\n-  the result at `lib.saw:1210-1215`. Vector elements include owning `String` and\\n+  ownership. `LexResult` is explicitly `NoCopy`; its three vectors — tokens, doc\\n+  trivia and design 268's string segments — are moved into the result at\\n+  `lib.saw:1322-1327`. Vector elements include owning `String` and\\n   `String?`, so pushing/returning structs must retain/move/drop exactly once.\\n-  The lexer explicitly moves both vectors into `LexResult` at `lib.saw:1215`;\\n+  The lexer explicitly moves all three vectors into `LexResult` at\\n+  `lib.saw:1327`;\\n   other transfers and copies must follow each payload's copy policy.\\n - Generic type application is needed for `Vector<Token>`, `Vector<DocComment>`,\\n+  `Vector<StringSegment>`,\\n   `Result<T, LexError>`, and `String?`. The lexer declares no generic function or\\n   type itself. A monomorphized built-in/runtime implementation is sufficient;\\n   generic declaration parsing, bounds, defaults, and arbitrary specialization\\n@@ -206,7 +212,8 @@ precise rejection. Keep all earlier milestone tests green at every gate.\\n    `literal_fits`, and `Lexer.advance`; compare their outputs to hand-written\\n    oracles.\\n 7. **Whole-module compile and differential lexing.** Compile the unchanged\\n-   `selfhost/lexer/src/lib.saw`, call `lex` and `lex_all`, and compare canonical\\n+   `selfhost/lexer/src/lib.saw`, call `lex` (one entry point since design 268,\\n+   returning tokens + doc trivia + string segments), and compare canonical\\n    token/error/doc records against the production lexer. Minimum cases: every\\n    keyword/operator, comments/docs, decimal/base/float/suffix boundaries,\\n    escaped and interpolated strings, Unicode scalar edges, raw multi-byte UTF-8\\ndiff --git a/prototypes/minivm/M8_OWNED_STRINGS.md b/prototypes/minivm/M8_OWNED_STRINGS.md\\nindex f27e81d2..de3a84ca 100644\\n--- a/prototypes/minivm/M8_OWNED_STRINGS.md\\n+++ b/prototypes/minivm/M8_OWNED_STRINGS.md\\n@@ -90,14 +90,19 @@ argument count/slot, no String record fields and Drop only on String slots.\\n \\n ## Frontend and validation details\\n \\n-TokenKind.StringLit contains decoded bytes, with the lexer's 0x01 escape marker\\n-before escaped braces. This collides with actual U+0001 bytes followed by braces.\\n-Preserve raw source in Compiler and decode the validated original literal at its\\n-token line/column, using the lexer's UTF-8 continuation/column rules to locate it.\\n-Restore raw escapes directly, including Unicode scalars and escaped braces;\\n-never strip marker bytes from the ambiguous decoded token payload. InterpString\\n-remains a located rejection. No production compiler changes are authorized by\\n-this prototype milestone; the independently reproduced Python defect is filed.\\n+TokenKind.StringLit contains decoded bytes. It used to carry the lexer's 0x01\\n+escape marker before an escaped brace, which collided with actual U+0001 bytes\\n+followed by braces — the defect this milestone reproduced and filed, fixed since\\n+by design 268 (SL-238): the marker protocol is gone from both lexers and a plain\\n+StringLit's value is now unambiguous decoded content.\\n+\\n+The frontend nevertheless keeps its own path: it preserves raw source in Compiler\\n+and decodes the validated original literal at its token line/column, using the\\n+lexer's UTF-8 continuation/column rules to locate it, restoring raw escapes\\n+directly including Unicode scalars and escaped braces. That is now a choice\\n+rather than a workaround, and it is deliberately left as it stands — this\\n+milestone's contract is unchanged and no behaviour was migrated with the API.\\n+InterpString remains a located rejection.\\n \\n Named String intrinsic receivers use their own content snapshot before argument\\n evaluation; their methods are read-only. Retain snapshots through nested argument\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex fe212d78..41d50d53 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -3,8 +3,13 @@ import src.model.*\\n \\n func Some<T>(value: T) -> T? { move value }\\n \\n+// A copy of a token is that token, so it carries the string-segment run the\\n+// lexer gave it (design 268's `seg_start`/`seg_count`) rather than dropping it.\\n+// Both are 0 on every kind this frontend accepts — it rejects InterpString, the\\n+// only kind that names segments — but a clone is not the place to decide that.\\n func clone_token(t: &Token) -> Token {\\n-    Token(kind: t.kind, value: t.value, line: t.line, col: t.col, suffix: t.suffix)\\n+    Token(kind: t.kind, value: t.value, line: t.line, col: t.col, suffix: t.suffix,\\n+          seg_start: t.seg_start, seg_count: t.seg_count)\\n }\\n \\n struct Signature {\\n@@ -2932,7 +2937,8 @@ extension Compiler {\\n             else { return problem(\\\"Result match patterns must be Ok, Err, or `_`\\\", pattern.line, pattern.col) }\\n             if variant == 1 && covered_ok { return problem(\\\"duplicate Result match arm\\\", pattern.line, pattern.col) }\\n             if variant == 0 && covered_err { return problem(\\\"duplicate Result match arm\\\", pattern.line, pattern.col) }\\n-            var binding = Token(kind: TokenKind.Ident, value: \\\"_\\\", line: pattern.line, col: pattern.col, suffix: None)\\n+            var binding = Token(kind: TokenKind.Ident, value: \\\"_\\\", line: pattern.line, col: pattern.col, suffix: None,\\n+                                seg_start: 0, seg_count: 0)\\n             var payload = ValueType.Void\\n             var offset = 0\\n             if variant >= 0 {\\n@@ -3206,7 +3212,8 @@ extension Compiler {\\n             else { return problem(\\\"Result match patterns must be Ok, Err, or `_`\\\", pattern.line, pattern.col) }\\n             if variant == 1 && covered_ok { return problem(\\\"duplicate Result match arm\\\", pattern.line, pattern.col) }\\n             if variant == 0 && covered_err { return problem(\\\"duplicate Result match arm\\\", pattern.line, pattern.col) }\\n-            var binding = Token(kind: TokenKind.Ident, value: \\\"_\\\", line: pattern.line, col: pattern.col, suffix: None)\\n+            var binding = Token(kind: TokenKind.Ident, value: \\\"_\\\", line: pattern.line, col: pattern.col, suffix: None,\\n+                                seg_start: 0, seg_count: 0)\\n             var payload = ValueType.Void\\n             var offset = 0\\n             if variant >= 0 {\\n@@ -3451,7 +3458,8 @@ extension Compiler {\\n         while i < self.signatures[index].params.len() {\\n             let slot = try self.new_value(self.signatures[index].params[i])\\n             let fake = Token(kind: TokenKind.Ident, value: self.signatures[index].param_names[i],\\n-                             line: self.signatures[index].line, col: self.signatures[index].col, suffix: None)\\n+                             line: self.signatures[index].line, col: self.signatures[index].col, suffix: None,\\n+                             seg_start: 0, seg_count: 0)\\n             try self.declare(fake, self.signatures[index].params[i], slot, false)\\n             i += 1\\n         }\\n@@ -3519,8 +3527,28 @@ extension Compiler {\\n }\\n \\n public func compile_source(source: String) -> Result<Program, ProtoError> {\\n-    let tokens = match lex(source) {\\n-        case Ok(ts) -> move ts,\\n+    // Design 268: `lex` answers one `LexResult` — the token stream, the\\n+    // doc-comment trivia and the string-segment arena — so the token vector is\\n+    // reached through a field instead of being the whole result. A field cannot\\n+    // be moved out of an owned struct (no partial moves), and this frontend\\n+    // wants only the tokens, so they are copied out; `Token` is on the Copy\\n+    // tier, so each element retains rather than duplicating its text. The doc\\n+    // records and the segment arena drop with `lexed` at the end of this call.\\n+    var tokens = Vector<Token>()\\n+    match lex(source) {\\n+        case Ok(r) -> {\\n+            // The token stream is cloned out element by element rather than with\\n+            // `r.tokens.copy()`: a `Vector<T>.copy()` whose element is an\\n+            // automatic-Copy-tier STRUCT duplicates it bitwise and never retains\\n+            // its `String` fields (SL-265), which leaves every token's text\\n+            // dangling once `r` drops. `clone_token` reads the fields, so each\\n+            // `String` is retained the way every other clone in this file is.\\n+            var i = 0\\n+            while i < r.tokens.len() {\\n+                try! tokens.push(clone_token(&r.tokens[i]))\\n+                i = i + 1\\n+            }\\n+        },\\n         case Err(e) -> { return problem(e.message, e.line, e.col) }\\n     }\\n     var compiler = Compiler(source: source, tokens: move tokens, records: Vector<RecordIR>(), enums: Vector<EnumIR>(),\\ndiff --git a/sawc/lexer.py b/sawc/lexer.py\\nindex 8a0a7f22..93e9c76c 100644\\n--- a/sawc/lexer.py\\n+++ b/sawc/lexer.py\\n@@ -139,6 +139,36 @@ class TokenType(Enum):\\n     EOF = auto()\\n \\n \\n+@dataclass(frozen=True)\\n+class StringSegment:\\n+    \\\"\\\"\\\"One piece of an INTERP_STRING token's content — design 268's typed\\n+    segment, and THE definition of the segment record form for this lexer.\\n+\\n+    An interpolated literal is an ordered list of these, produced by\\n+    `Lexer.read_string` (the only producer) and consumed by\\n+    `Parser._parse_interpolated_string` and `tools/dump_tokens.py`'s\\n+    `format_token` (the only consumers). Two kinds:\\n+\\n+    - `kind='text'`: `text` is the DECODED literal content of one run between\\n+      interpolations. Escapes are already applied, so an escaped brace is an\\n+      ordinary `{`/`}` character here. `line`/`column` are unused (0).\\n+    - `kind='expr'`: `text` is the RAW source text between an interpolation's\\n+      braces (never escape-decoded — the sub-parser lexes it), and\\n+      `line`/`column` are the EXACT source position of its opening `{`.\\n+      Blank `text` is a design-137 format placeholder.\\n+\\n+    Design 268 replaced the old protocol, where an escaped brace was encoded\\n+    in-band as the two bytes `0x01` + brace and the parser re-scanned the flat\\n+    value to re-find the interpolations. That encoding was ambiguous with real\\n+    content (`\\\"\\\\\\\\u{1}\\\\\\\\u{7b}\\\"` lexes to those same two bytes), which silently\\n+    deleted the U+0001 (SL-238). No content byte carries metadata now.\\n+    \\\"\\\"\\\"\\n+    kind: str\\n+    text: str\\n+    line: int = 0\\n+    column: int = 0\\n+\\n+\\n @dataclass\\n class Token:\\n     type: TokenType\\n@@ -148,6 +178,10 @@ class Token:\\n     # Integer-literal suffix (design 53): one of i8/i16/i32/i64/u8/u16/u32/u64\\n     # when the literal was written `255u8` / `0xFF_u8`; otherwise None.\\n     suffix: Optional[str] = None\\n+    # INTERP_STRING only (design 268): the literal's typed segments, in source\\n+    # order — the parse input for an interpolated string. None on every other\\n+    # token, including a plain STRING (whose `value` is the decoded content).\\n+    segments: Optional[List[StringSegment]] = None\\n \\n \\n @dataclass\\n@@ -341,14 +375,28 @@ class Lexer:\\n         return chr(cp)\\n \\n     def read_string(self) -> tuple:\\n-        \\\"\\\"\\\"Read a string literal, detecting interpolation markers.\\n-\\n-        Returns: (string_value, has_interpolation)\\n-        - For plain strings: (\\\"hello\\\", False)\\n-        - For interpolated: (\\\"hello {name}!\\\", True) - braces preserved\\n+        \\\"\\\"\\\"Read a string literal, segmenting it at its interpolations.\\n+\\n+        Returns `(value, segments)` — design 268, the ONE place a string\\n+        literal's content is decoded:\\n+\\n+        - A PLAIN string: `(decoded_value, None)`. Every escape is applied, so\\n+          an escaped brace is an ordinary `{`/`}` character in the value. A\\n+          plain string can only contain a brace via an escape, so nothing ever\\n+          needs to tell the two apart — which is why the marker byte this used\\n+          to insert (`0x01` + brace) is gone (SL-238).\\n+        - An INTERPOLATED string: `(raw_spelling, [StringSegment, ...])`. The\\n+          segments are the parse input (see `StringSegment`); `value` is the\\n+          literal's RAW source text between the quotes, undecoded, kept for\\n+          diagnostics and for the canonical token dump. Adjacent runs are\\n+          merged and an EMPTY `text` segment is never emitted — the Saw port\\n+          (`selfhost/lexer`, `read_string`) follows the same two rules so the\\n+          dumps stay byte-identical (`tools/lexdiff.py`).\\n         \\\"\\\"\\\"\\n         self.advance()  # consume opening quote\\n+        content_start = self.pos     # first character after the opening quote\\n         result = []\\n+        segments = []\\n         has_interpolation = False\\n         # Position of the FIRST interpolation `{` opened in this literal (None =\\n         # none yet). An unbalanced interpolation reports HERE, not at EOF (DF-116d):\\n@@ -378,36 +426,47 @@ class Lexer:\\n                 elif ch == '\\\\\\\\':\\n                     result.append('\\\\\\\\')\\n                 elif ch == '{':\\n-                    result.append('\\\\x01{')  # Escaped brace - use marker to distinguish from interpolation\\n+                    result.append('{')      # `\\\\{` is just a brace: segments,\\n                 elif ch == '}':\\n-                    result.append('\\\\x01}')  # Escaped brace - use marker to distinguish from interpolation\\n+                    result.append('}')      # not markers, carry the structure\\n                 elif ch == 'u':\\n                     result.append(self._read_unicode_escape())\\n                 else:\\n                     self.error(f\\\"unknown escape `\\\\\\\\{ch}` in a string literal \\\"\\n                                f\\\"(supported: \\\\\\\\\\\\\\\\ \\\\\\\\\\\\\\\" \\\\\\\\n \\\\\\\\t \\\\\\\\r \\\\\\\\0 \\\\\\\\u{{...}})\\\")\\n             elif self.peek() == '{':\\n-                # Interpolation detected - preserve braces for parser\\n+                # An unescaped `{` opens an interpolation: close the pending\\n+                # text run and emit an `expr` segment carrying the RAW text\\n+                # between the braces plus the brace's exact position.\\n                 has_interpolation = True\\n                 open_line, open_col = self.line, self.column\\n                 if interp_open is None:\\n                     interp_open = (open_line, open_col)\\n-                result.append(self.advance())  # Keep {\\n-                # Read until matching }, tracking nested braces\\n+                if result:\\n+                    segments.append(StringSegment('text', ''.join(result)))\\n+                    result = []\\n+                self.advance()  # consume the opening `{`\\n+                # Read until the matching `}`, tracking nested braces. The\\n+                # outer braces are structure, so only the interior is kept.\\n                 brace_depth = 1\\n+                expr_chars = []\\n                 while self.peek() and brace_depth > 0:\\n                     ch = self.peek()\\n                     if ch == '{':\\n                         brace_depth += 1\\n                     elif ch == '}':\\n                         brace_depth -= 1\\n-                    result.append(self.advance())\\n+                    if brace_depth > 0:\\n+                        expr_chars.append(ch)\\n+                    self.advance()\\n                 if brace_depth > 0:\\n                     # This interpolation ran to EOF without a closing `}`.\\n                     raise SyntaxError(\\n                         \\\"Lexer error at %d:%d: unterminated interpolation in \\\"\\n                         \\\"string literal, opened at this `{` (write `\\\\\\\\{` for a \\\"\\n                         \\\"literal brace)\\\" % (open_line, open_col))\\n+                segments.append(StringSegment(\\n+                    'expr', ''.join(expr_chars), open_line, open_col))\\n             else:\\n                 result.append(self.advance())\\n \\n@@ -423,8 +482,13 @@ class Lexer:\\n                     \\\"literal, opened at this `{` (write `\\\\\\\\{` for a literal \\\"\\n                     \\\"brace)\\\" % (il, ic))\\n             self.error(\\\"Unterminated string\\\")\\n+        content_end = self.pos       # the closing quote's position\\n         self.advance()  # consume closing quote\\n-        return (''.join(result), has_interpolation)\\n+        if not has_interpolation:\\n+            return (''.join(result), None)\\n+        if result:\\n+            segments.append(StringSegment('text', ''.join(result)))\\n+        return (self.source[content_start:content_end], segments)\\n \\n     # Without design 47, `Int`/`UInt` are 64-bit, so an integer literal must be\\n     # representable in 64 bits — as a signed OR unsigned value (literals are\\n@@ -617,9 +681,13 @@ class Lexer:\\n                 # agrees).\\n                 start_line = self.line\\n                 start_col = self.column\\n-                value, has_interpolation = self.read_string()\\n-                token_type = TokenType.INTERP_STRING if has_interpolation else TokenType.STRING\\n-                self.tokens.append(Token(token_type, value, start_line, start_col))\\n+                # Design 268: `segments` is None for a plain string and the\\n+                # literal's typed segment list for an interpolated one.\\n+                value, segments = self.read_string()\\n+                token_type = (TokenType.INTERP_STRING if segments is not None\\n+                              else TokenType.STRING)\\n+                self.tokens.append(Token(token_type, value, start_line,\\n+                                         start_col, segments=segments))\\n             elif ch.isdigit():\\n                 # Design 161: one-token lookback. Digits right after a\\n                 # member-access `.` are a tuple index, not the start of a float.\\ndiff --git a/sawc/parser/README.md b/sawc/parser/README.md\\nindex 075f0f85..2c6176ef 100644\\n--- a/sawc/parser/README.md\\n+++ b/sawc/parser/README.md\\n@@ -168,7 +168,7 @@ Expression parsing with operator precedence.\\n | `_is_closure_with_named_params()` | Detect named param closure |\\n | `_parse_closure_params()` | Parse closure parameters |\\n | `_parse_closure_body()` | Parse closure body |\\n-| `_parse_interpolated_string(raw, line, col)` | Parse string interpolation |\\n+| `_parse_interpolated_string(segments, line, col)` | Parse string interpolation from the token's typed segments (design 268) |\\n | `_count_shorthand_params(body)` | Count `$0`, `$1`, etc. usage |\\n \\n #### Trailing Closure Handling\\ndiff --git a/sawc/parser/expressions.py b/sawc/parser/expressions.py\\nindex b2988cfa..d95237cb 100644\\n--- a/sawc/parser/expressions.py\\n+++ b/sawc/parser/expressions.py\\n@@ -727,13 +727,15 @@ class ExpressionsMixin:\\n \\n         elif self.match(TokenType.STRING):\\n             self.advance()\\n-            # Convert escaped brace markers back to literal braces\\n-            value = token.value.replace('\\\\x01{', '{').replace('\\\\x01}', '}')\\n-            return StringLiteral(value=value, line=token.line, column=token.column)\\n+            # The lexer fully decoded it (design 268): a brace in the value is\\n+            # just a brace, so there is nothing left to un-encode here.\\n+            return StringLiteral(value=token.value, line=token.line,\\n+                                 column=token.column)\\n \\n         elif self.match(TokenType.INTERP_STRING):\\n             self.advance()\\n-            return self._parse_interpolated_string(token.value, token.line, token.column)\\n+            return self._parse_interpolated_string(\\n+                token.segments, token.line, token.column)\\n \\n         elif self.match(TokenType.IDENT):\\n             self.advance()\\n@@ -1861,79 +1863,47 @@ class ExpressionsMixin:\\n             # expression statement — closures mutate captured `&var` state.\\n             return self.parse_assignment_or_expression_statement()\\n \\n-    def _parse_interpolated_string(self, raw_value: str, line: int, column: int) -> StringInterpolation:\\n-        \\\"\\\"\\\"Parse a string with {expr} interpolations into parts and expressions.\\n-\\n-        The raw_value contains the string with braces preserved, e.g. \\\"Hello {name}!\\\"\\n-        Escaped braces are marked as \\\\x01{ and \\\\x01} by the lexer.\\n-        Returns a StringInterpolation node with parts and expressions separated.\\n+    def _parse_interpolated_string(self, segments, line: int, column: int) -> StringInterpolation:\\n+        \\\"\\\"\\\"Build a `StringInterpolation` from an INTERP_STRING token's typed\\n+        SEGMENTS (design 268) — the ONE consumer of a segment list on the\\n+        Python side.\\n+\\n+        The lexer already decided which braces open interpolations and decoded\\n+        every escape (see `lexer.StringSegment`), so there is no byte re-scan\\n+        here and no escaped-brace marker to un-encode: a `text` segment\\n+        contributes literal content, an `expr` segment contributes one\\n+        expression (or a design-137 `FormatPlaceholder` when its raw text is\\n+        blank), and each expression is sub-parsed at the EXACT source position\\n+        of its opening `{` (design 99: the line was already exact, the column\\n+        is now exact under escapes too).\\n+\\n+        The result keeps the node's invariant `len(parts) == len(expressions)\\n+        + 1`: the pending text run is flushed as a part at every `expr`\\n+        segment, so two adjacent interpolations are separated by an empty part.\\n         \\\"\\\"\\\"\\n-        from lexer import Lexer\\n-\\n         parts = []\\n         expressions = []\\n-        current_part = []\\n-        i = 0\\n-\\n-        while i < len(raw_value):\\n-            # Check for escaped brace markers (\\\\x01{ and \\\\x01})\\n-            if raw_value[i] == '\\\\x01' and i + 1 < len(raw_value) and raw_value[i + 1] in '{}':\\n-                # Convert marker back to literal brace\\n-                current_part.append(raw_value[i + 1])\\n-                i += 2\\n-            elif raw_value[i] == '{':\\n-                # Real interpolation start\\n-                # Save current string part\\n-                parts.append(''.join(current_part))\\n-                current_part = []\\n-\\n-                # Source position of this `{` so the sub-parsed expression's\\n-                # diagnostics point at the real call site (design 99). Lines\\n-                # are exact; columns approximate under escape sequences (each\\n-                # source escape like `\\\\n` is one raw char), which is fine —\\n-                # the line is what diagnostics need.\\n-                prefix = raw_value[:i]\\n-                newlines = prefix.count('\\\\n')\\n-                if newlines:\\n-                    brace_line = line + newlines\\n-                    brace_column = i - prefix.rfind('\\\\n')\\n-                else:\\n-                    brace_line = line\\n-                    brace_column = column + 1 + i  # +1 for the opening quote\\n-\\n-                # Extract expression text between { and }\\n-                i += 1  # skip {\\n-                brace_depth = 1\\n-                expr_chars = []\\n-                while i < len(raw_value) and brace_depth > 0:\\n-                    ch = raw_value[i]\\n-                    if ch == '{':\\n-                        brace_depth += 1\\n-                    elif ch == '}':\\n-                        brace_depth -= 1\\n-                    if brace_depth > 0:\\n-                        expr_chars.append(ch)\\n-                    i += 1\\n-\\n-                # An EMPTY brace pair is a format placeholder (design 137), not\\n-                # an expression to parse: `panic(\\\"out of {}\\\", what)` fills it\\n-                # from the argument list. Sub-parsing \\\"\\\" used to be the error\\n-                # \\\"Invalid expression in string interpolation\\\".\\n-                expr_str = ''.join(expr_chars)\\n-                if expr_str.strip() == \\\"\\\":\\n-                    expressions.append(FormatPlaceholder(\\n-                        line=brace_line, column=brace_column))\\n-                    continue\\n+        pending = []\\n \\n-                # Parse the expression using a sub-lexer and sub-parser\\n-                expr = self._parse_expression_from_string(expr_str, brace_line, brace_column)\\n-                expressions.append(expr)\\n-            else:\\n-                current_part.append(raw_value[i])\\n-                i += 1\\n+        for seg in segments:\\n+            if seg.kind == 'text':\\n+                pending.append(seg.text)\\n+                continue\\n+            parts.append(''.join(pending))\\n+            pending = []\\n+            # An EMPTY brace pair is a format placeholder (design 137), not an\\n+            # expression to parse: `panic(\\\"out of {}\\\", what)` fills it from the\\n+            # argument list. Sub-parsing \\\"\\\" used to be the error \\\"Invalid\\n+            # expression in string interpolation\\\".\\n+            if seg.text.strip() == \\\"\\\":\\n+                expressions.append(FormatPlaceholder(\\n+                    line=seg.line, column=seg.column))\\n+                continue\\n+            expressions.append(self._parse_expression_from_string(\\n+                seg.text, seg.line, seg.column))\\n \\n-        # Final string part (after last expression or entire string if no expressions)\\n-        parts.append(''.join(current_part))\\n+        # Final string part (after the last interpolation)\\n+        parts.append(''.join(pending))\\n \\n         return StringInterpolation(parts=parts, expressions=expressions, line=line, column=column)\\n \\ndiff --git a/selfhost/lexer/README.md b/selfhost/lexer/README.md\\nindex 44025a45..35387986 100644\\n--- a/selfhost/lexer/README.md\\n+++ b/selfhost/lexer/README.md\\n@@ -13,7 +13,7 @@ The Python lexer's **observable output** is the correctness reference;\\n ```\\n selfhost/lexer/\\n   Saw.toml          # Blade package manifest (name = \\\"sawlex\\\")\\n-  src/lib.saw       # token model (TokenKind enum + Token) + `lex`/`lex_all` + dump format\\n+  src/lib.saw       # token model (TokenKind + Token + StringSegment) + `lex` + dump format\\n   src/main.saw      # the `sawlex` CLI\\n   tests/*.saw       # blade unit tests, one per token family\\n ```\\n@@ -47,6 +47,27 @@ KIND<TAB>line:col<TAB>escaped-text[<TAB>suffix]\\n   literal: one of `i8`/`i16`/`i32`/`i64`/`u8`/`u16`/`u32`/`u64`. `255u8` dumps as\\n   `INT<TAB>1:1<TAB>255<TAB>u8`; every other token stops at the escaped text.\\n \\n+### An `INTERP_STRING` record carries its segments\\n+\\n+An interpolated string's `escaped-text` is the literal's **raw source spelling**\\n+between the quotes (undecoded), and the record then continues with **one field\\n+per typed segment** (design 268), in source order:\\n+\\n+```\\n+INTERP_STRING<TAB>line:col<TAB>raw<TAB>T:text<TAB>E:line:col:raw-expr...\\n+```\\n+\\n+* `T:` — a literal run, **decoded** (so an escaped brace is a plain `{`/`}`).\\n+* `E:line:col:` — an interpolation, carrying the **raw** text between its braces\\n+  and the exact 1-based source position of its opening `{`.\\n+\\n+An empty `T:` run is never emitted, so two adjacent interpolations produce two\\n+consecutive `E:` fields. No token carries both a suffix and segments, so the\\n+tail is unambiguous. The segments are what a parser consumes: nothing is encoded\\n+into the token's bytes, which is what design 268 replaced (an escaped brace used\\n+to be the two bytes `0x01` + brace — indistinguishable from that same pair\\n+written as content, which silently lost it; SL-238).\\n+\\n On a lex error the CLI emits a single record and exits nonzero:\\n \\n ```\\n@@ -75,8 +96,10 @@ DOC<TAB>line:col<TAB>kind<TAB>escaped-text\\n   space stripped, escaped by the same byte-level scheme as a token's text.\\n \\n Doc comments are **trivia**: `lex` skips them exactly as it skips `//` comments,\\n-so the token dump above is unaffected. `lex_all` returns the tokens and the doc\\n-records together. Only a comment that starts its line is a doc comment; `////`\\n+so the token dump above is unaffected. `lex` returns one `LexResult` carrying\\n+the tokens, the doc records and the string-segment arena together — there is no\\n+tokens-only entry point, because a token's `seg_start`/`seg_count` index into\\n+that arena. Only a comment that starts its line is a doc comment; `////`\\n (four or more slashes) and a `///` trailing code on the same line are ordinary\\n comments.\\n \\ndiff --git a/selfhost/lexer/src/lib.saw b/selfhost/lexer/src/lib.saw\\nindex e57f5296..c7d4da20 100644\\n--- a/selfhost/lexer/src/lib.saw\\n+++ b/selfhost/lexer/src/lib.saw\\n@@ -73,7 +73,6 @@ static B_DEL: Int = 127\\n static B_CONT_LO: Int = 128\\n static B_CONT_HI: Int = 191\\n static B_ONE_TWENTY_SEVEN: Int = 127\\n-static B_MARKER: Int = 1     // 0x01 escaped-brace sentinel (mirrors the Python lexer)\\n \\n // =============================================================================\\n // Byte helpers\\n@@ -233,18 +232,58 @@ public enum TokenKind {\\n     case Eof\\n }\\n \\n-// A lexed token. `value` holds the token's canonical text (decoded, for strings;\\n-// underscores stripped, for numbers). `suffix` is the fixed-width integer suffix\\n-// (one of i8/i16/i32/i64/u8/u16/u32/u64) when the literal was written `255u8` /\\n-// `0xFF_u8`, mirroring sawc/lexer.py's Token; None for every non-suffixed token.\\n-// (Restored once DF-116a — the Optional<String>-into-Vector-field miscompile that\\n-// had stopped this unit — was fixed; design 119 Part D.)\\n+// Which half of an interpolated string one segment is (design 268).\\n+public enum SegmentKind {\\n+    case Text,\\n+    case Expr\\n+}\\n+\\n+// One piece of an InterpString token's content — design 268's typed segment,\\n+// and THE definition of the segment record form for this lexer. `read_string`\\n+// is the only producer; `format_token` is the only consumer.\\n+//\\n+// - `Text`: `text` is the DECODED literal content of one run between\\n+//   interpolations (escapes applied, so an escaped brace is an ordinary `{`/`}`\\n+//   byte here). `line`/`col` are unused (0).\\n+// - `Expr`: `text` is the RAW source text between an interpolation's braces\\n+//   (never escape-decoded — a parser sub-lexes it), and `line`/`col` are the\\n+//   EXACT source position of its opening `{`. Blank `text` is a design-137\\n+//   format placeholder.\\n+//\\n+// This replaced the protocol where an escaped brace was encoded in-band as the\\n+// two bytes 0x01 + brace: those bytes are also what a real `\\\\u{1}` before a\\n+// brace produces, so the decode silently deleted content (SL-238). No content\\n+// byte carries metadata now.\\n+//\\n+// Segments live in ONE FLAT ARENA per lex (`LexResult.segments`) and a token\\n+// names its own run with `seg_start`/`seg_count`. The arena is what keeps\\n+// `Token` in the automatic Copy tier: an owning `Vector` field would make the\\n+// whole token move-only, so every token read would become a borrow.\\n+public struct StringSegment {\\n+    public kind: SegmentKind\\n+    public text: String\\n+    public line: Int\\n+    public col: Int\\n+}\\n+\\n+// A lexed token. `value` holds the token's canonical text (decoded, for a plain\\n+// string; the RAW source spelling between the quotes, for an InterpString,\\n+// whose parse input is its SEGMENTS; underscores stripped, for numbers).\\n+// `suffix` is the fixed-width integer suffix (one of i8/i16/i32/i64/u8/u16/u32/\\n+// u64) when the literal was written `255u8` / `0xFF_u8`, mirroring\\n+// sawc/lexer.py's Token; None for every non-suffixed token. (Restored once\\n+// DF-116a — the Optional<String>-into-Vector-field miscompile that had stopped\\n+// this unit — was fixed; design 119 Part D.)\\n+// `seg_start`/`seg_count` name this token's run in the lex's segment arena\\n+// (design 268): `seg_count` is 0 on every token but an InterpString.\\n public struct Token {\\n     public kind: TokenKind\\n     public value: String\\n     public line: Int\\n     public col: Int\\n     public suffix: String?\\n+    public seg_start: Int\\n+    public seg_count: Int\\n }\\n \\n // A lex error, positioned at 1-based line:col. Message prose is informational\\n@@ -268,12 +307,14 @@ public struct DocComment {\\n     public col: Int\\n }\\n \\n-// The full output of a lex: the token stream plus the doc-comment trivia. `lex`\\n-// returns just the tokens (the common path); `lex_all` returns both so the CLI's\\n-// `--docs` mode and the differential harness can dump the trivia.\\n+// The full output of a lex: the token stream, the doc-comment trivia (design\\n+// 121) and the string-segment arena (design 268). One entry point — `lex` —\\n+// returns all three, because a token's `seg_start`/`seg_count` mean nothing\\n+// without the arena they index into.\\n public struct LexResult {\\n     public tokens: Vector<Token>\\n     public docs: Vector<DocComment>\\n+    public segments: Vector<StringSegment>\\n }\\n \\n extension LexResult: NoCopy {}\\n@@ -462,10 +503,39 @@ public func escape_text(s: String) -> String {\\n     out.build()\\n }\\n \\n+// One canonical segment field (design 268) for an INTERP_STRING record:\\n+//\\n+//   T:<escaped text>              a literal run (decoded content)\\n+//   E:<line>:<col>:<escaped text> an interpolation (RAW text, the `{` position)\\n+//\\n+// `escape_text` never emits a tab or a newline, so the fields stay splittable.\\n+// tools/dump_tokens.py's `format_segment` emits the identical shape.\\n+public func format_segment(s: &StringSegment) -> String {\\n+    var out = StringBuilder()\\n+    match s.kind {\\n+        case Text -> try! out.append(\\\"T:\\\"),\\n+        case Expr -> {\\n+            try! out.append(\\\"E:\\\")\\n+            try! out.append(s.line)\\n+            try! out.append(\\\":\\\")\\n+            try! out.append(s.col)\\n+            try! out.append(\\\":\\\")\\n+        }\\n+    }\\n+    try! out.append(escape_text(s.text))\\n+    out.build()\\n+}\\n+\\n // One canonical record: KIND<TAB>line:col<TAB>escaped-text[<TAB>suffix]. The\\n // 4th column is present only for a suffixed integer literal (e.g. `255u8` dumps\\n // `INT<TAB>1:1<TAB>255<TAB>u8`); every other token stops at the escaped text.\\n-public func format_token(t: &Token) -> String {\\n+//\\n+// An INTERP_STRING (design 268) instead continues with ONE FIELD PER TYPED\\n+// SEGMENT, in source order, after the escaped text (which for that token is the\\n+// literal's raw source spelling). No token carries both a suffix and segments,\\n+// so the tail is unambiguous. `segments` is the arena the token's\\n+// `seg_start`/`seg_count` index into — the `segments` of the lex it came from.\\n+public func format_token(t: &Token, segments: &Vector<StringSegment>) -> String {\\n     var out = StringBuilder()\\n     try! out.append(kind_name(t.kind))\\n     try! out.append(\\\"\\\\t\\\")\\n@@ -478,6 +548,14 @@ public func format_token(t: &Token) -> String {\\n         try! out.append(\\\"\\\\t\\\")\\n         try! out.append(sfx)\\n     }\\n+    var i = 0\\n+    while i < t.seg_count {\\n+        if let s = segments.get(t.seg_start + i) {\\n+            try! out.append(\\\"\\\\t\\\")\\n+            try! out.append(format_segment(&s))\\n+        }\\n+        i = i + 1\\n+    }\\n     out.build()\\n }\\n \\n@@ -596,7 +674,8 @@ extension Lexer {\\n     // A token at the CURRENT position (used before advancing over operators).\\n     // Never a suffixed literal, so `suffix` is None.\\n     func tok(&self, kind: TokenKind, value: String) -> Token {\\n-        Token(kind: kind, value: value, line: self.line, col: self.col, suffix: None)\\n+        Token(kind: kind, value: value, line: self.line, col: self.col, suffix: None,\\n+              seg_start: 0, seg_count: 0)\\n     }\\n \\n     func skip_whitespace(&var self) {\\n@@ -701,13 +780,23 @@ extension Lexer {\\n         return\\n     }\\n \\n-    // Read a string literal, detecting interpolation markers. Escapes are decoded\\n-    // exactly as in the Python lexer; escaped braces store a 0x01 sentinel byte;\\n-    // interpolation braces are preserved raw. Returns the token.\\n-    func read_string(&var self) -> Result<Token, LexError> {\\n+    // Read a string literal, segmenting it at its interpolations (design 268).\\n+    // Escapes are decoded exactly as in the Python lexer, and an escaped brace is\\n+    // an ordinary brace byte — nothing is encoded in-band any more.\\n+    //\\n+    // A plain string's `value` is its decoded content. An InterpString's `value`\\n+    // is the RAW source spelling between the quotes (for diagnostics and the\\n+    // dump) and its parse input is the SEGMENTS appended to `segments`, which the\\n+    // returned token names by `seg_start`/`seg_count`. Adjacent literal runs are\\n+    // merged and an EMPTY `Text` segment is never emitted; sawc/lexer.py's\\n+    // `read_string` follows the same two rules, which is what keeps the two\\n+    // dumps byte-identical (tools/lexdiff.py).\\n+    func read_string(&var self, segments: &var Vector<StringSegment>) -> Result<Token, LexError> {\\n         let start_line = self.line\\n         let start_col = self.col\\n         let _ = self.advance()  // consume opening quote\\n+        let content_start = self.pos   // first byte after the opening quote\\n+        let seg_start = segments.len()\\n         var out = StringBuilder()\\n         var has_interp = false\\n         // Position of the FIRST interpolation `{` opened in this literal (0 = none\\n@@ -727,14 +816,10 @@ extension Lexer {\\n                 else if ch == B_D0 { try! out.append(to_byte(0)) }\\n                 else if ch == B_QUOTE { try! out.append(to_byte(B_QUOTE)) }\\n                 else if ch == B_BACKSLASH { try! out.append(to_byte(B_BACKSLASH)) }\\n-                else if ch == B_LBRACE {\\n-                    try! out.append(to_byte(B_MARKER))\\n-                    try! out.append(to_byte(B_LBRACE))\\n-                }\\n-                else if ch == B_RBRACE {\\n-                    try! out.append(to_byte(B_MARKER))\\n-                    try! out.append(to_byte(B_RBRACE))\\n-                }\\n+                // `\\\\{` / `\\\\}` is just a brace: the segments, not a marker byte,\\n+                // carry which braces opened an interpolation.\\n+                else if ch == B_LBRACE { try! out.append(to_byte(B_LBRACE)) }\\n+                else if ch == B_RBRACE { try! out.append(to_byte(B_RBRACE)) }\\n                 else if ch == B_LO_U {\\n                     try self.read_unicode_escape(&var out)\\n                 }\\n@@ -742,6 +827,9 @@ extension Lexer {\\n                     return self.err(\\\"unknown escape in a string literal\\\")\\n                 }\\n             } else if self.peek() == B_LBRACE {\\n+                // An unescaped `{` opens an interpolation: close the pending text\\n+                // run and emit an `Expr` segment carrying the RAW text between the\\n+                // braces plus the brace's exact position.\\n                 has_interp = true\\n                 let open_line = self.line\\n                 let open_col = self.col\\n@@ -749,18 +837,28 @@ extension Lexer {\\n                     interp_line = open_line\\n                     interp_col = open_col\\n                 }\\n-                try! out.append(to_byte(self.advance()))  // keep '{'\\n+                let run = out.build()\\n+                if run.len() > 0 {\\n+                    try! segments.push(StringSegment(kind: SegmentKind.Text, text: run, line: 0, col: 0))\\n+                }\\n+                out.clear()\\n+                let _ = self.advance()  // consume the opening '{'\\n                 var depth = 1\\n+                var raw = StringBuilder()\\n                 while self.peek() != -1 && depth > 0 {\\n                     let c = self.peek()\\n                     if c == B_LBRACE { depth = depth + 1 }\\n                     else if c == B_RBRACE { depth = depth - 1 }\\n-                    try! out.append(to_byte(self.advance()))\\n+                    // The outer braces are structure; only the interior is kept.\\n+                    if depth > 0 { try! raw.append(to_byte(c)) }\\n+                    let _ = self.advance()\\n                 }\\n                 if depth > 0 {\\n                     // This interpolation ran to EOF without a closing `}`.\\n                     return self.err_at(\\\"unterminated interpolation in string literal, opened at this `\\\\{` (write `\\\\\\\\\\\\{` for a literal brace)\\\", open_line, open_col)\\n                 }\\n+                try! segments.push(StringSegment(kind: SegmentKind.Expr, text: raw.build(),\\n+                                                 line: open_line, col: open_col))\\n             } else {\\n                 try! out.append(to_byte(self.advance()))\\n             }\\n@@ -776,9 +874,19 @@ extension Lexer {\\n             }\\n             return self.err(\\\"Unterminated string\\\")\\n         }\\n+        let content_end = self.pos   // the closing quote's byte index\\n         let _ = self.advance()  // consume closing quote\\n-        let kind = if has_interp { TokenKind.InterpString } else { TokenKind.StringLit }\\n-        Token(kind: kind, value: out.build(), line: start_line, col: start_col, suffix: None)\\n+        if not has_interp {\\n+            return Token(kind: TokenKind.StringLit, value: out.build(), line: start_line,\\n+                         col: start_col, suffix: None, seg_start: seg_start, seg_count: 0)\\n+        }\\n+        let tail = out.build()\\n+        if tail.len() > 0 {\\n+            try! segments.push(StringSegment(kind: SegmentKind.Text, text: tail, line: 0, col: 0))\\n+        }\\n+        Token(kind: TokenKind.InterpString, value: self.src.substring(content_start, content_end),\\n+              line: start_line, col: start_col, suffix: None,\\n+              seg_start: seg_start, seg_count: segments.len() - seg_start)\\n     }\\n \\n     // Positioned just after an integer's digits, tentatively read a fixed-width\\n@@ -835,7 +943,8 @@ extension Lexer {\\n             if not literal_fits(index_str, 10, 64) {\\n                 return self.err_at(\\\"integer literal out of range for a 64-bit integer\\\", start_line, start_col)\\n             }\\n-            return Token(kind: TokenKind.IntLit, value: index_str, line: start_line, col: start_col, suffix: None)\\n+            return Token(kind: TokenKind.IntLit, value: index_str, line: start_line, col: start_col, suffix: None,\\n+                         seg_start: 0, seg_count: 0)\\n         }\\n \\n         let n = self.peek1()\\n@@ -869,7 +978,8 @@ extension Lexer {\\n             try! vb.append(\\\"0\\\")\\n             try! vb.append(prefix_str)\\n             try! vb.append(digit_str)\\n-            return Token(kind: TokenKind.IntLit, value: vb.build(), line: start_line, col: start_col, suffix: suffix)\\n+            return Token(kind: TokenKind.IntLit, value: vb.build(), line: start_line, col: start_col, suffix: suffix,\\n+                         seg_start: 0, seg_count: 0)\\n         }\\n \\n         // Decimal integer or float, with underscore digit separators.\\n@@ -900,7 +1010,8 @@ extension Lexer {\\n         }\\n         let value = val.build()\\n         if is_float {\\n-            return Token(kind: TokenKind.FloatLit, value: value, line: start_line, col: start_col, suffix: None)\\n+            return Token(kind: TokenKind.FloatLit, value: value, line: start_line, col: start_col, suffix: None,\\n+                         seg_start: 0, seg_count: 0)\\n         }\\n         let suffix = self.try_read_int_suffix()\\n         if let sfx = suffix {\\n@@ -912,7 +1023,8 @@ extension Lexer {\\n                 return self.err_at(\\\"integer literal out of range for a 64-bit integer\\\", start_line, start_col)\\n             }\\n         }\\n-        Token(kind: TokenKind.IntLit, value: value, line: start_line, col: start_col, suffix: suffix)\\n+        Token(kind: TokenKind.IntLit, value: value, line: start_line, col: start_col, suffix: suffix,\\n+              seg_start: 0, seg_count: 0)\\n     }\\n \\n     func read_hash_directive(&var self) -> Result<Token, LexError> {\\n@@ -933,7 +1045,8 @@ extension Lexer {\\n         if not known {\\n             return self.err_at(\\\"unknown directive after `#`\\\", start_line, start_col)\\n         }\\n-        Token(kind: TokenKind.HashDirective, value: name_str, line: start_line, col: start_col, suffix: None)\\n+        Token(kind: TokenKind.HashDirective, value: name_str, line: start_line, col: start_col, suffix: None,\\n+              seg_start: 0, seg_count: 0)\\n     }\\n \\n     func read_identifier(&var self) -> Token {\\n@@ -944,7 +1057,8 @@ extension Lexer {\\n             try! sb.append(to_byte(self.advance()))\\n         }\\n         let value = sb.build()\\n-        Token(kind: keyword_kind(value), value: value, line: start_line, col: start_col, suffix: None)\\n+        Token(kind: keyword_kind(value), value: value, line: start_line, col: start_col, suffix: None,\\n+              seg_start: 0, seg_count: 0)\\n     }\\n \\n     func read_dollar(&var self) -> Result<Token, LexError> {\\n@@ -959,12 +1073,15 @@ extension Lexer {\\n         while is_digit(self.peek()) {\\n             try! num.append(to_byte(self.advance()))\\n         }\\n-        Token(kind: TokenKind.DollarParam, value: num.build(), line: start_line, col: start_col, suffix: None)\\n+        Token(kind: TokenKind.DollarParam, value: num.build(), line: start_line, col: start_col, suffix: None,\\n+              seg_start: 0, seg_count: 0)\\n     }\\n \\n-    // Run the lexer to EOF. Doc-comment trivia is appended to `docs` (design 121);\\n-    // the returned token stream is exactly what it was before doc comments existed.\\n-    func tokenize(&var self, docs: &var Vector<DocComment>) -> Result<Vector<Token>, LexError> {\\n+    // Run the lexer to EOF. Doc-comment trivia is appended to `docs` (design 121)\\n+    // and every interpolated string's typed segments to `segments` (design 268);\\n+    // the returned token stream is exactly what it was before either existed.\\n+    func tokenize(&var self, docs: &var Vector<DocComment>,\\n+                  segments: &var Vector<StringSegment>) -> Result<Vector<Token>, LexError> {\\n         var tokens = Vector<Token>()\\n         while self.pos < self.slen {\\n             self.skip_whitespace()\\n@@ -978,7 +1095,7 @@ extension Lexer {\\n                 try! tokens.push(self.tok(TokenKind.Newline, \\\"\\\\n\\\"))\\n                 let _ = self.advance()\\n             } else if ch == B_QUOTE {\\n-                try! tokens.push(try self.read_string())\\n+                try! tokens.push(try self.read_string(&var segments))\\n             } else if is_digit(ch) {\\n                 // Design 161: one-token lookback. Digits right after a member-access\\n                 // `.` are a tuple index, not the start of a float.\\n@@ -1197,19 +1314,15 @@ func in_base_set(b: Int, base: Int) -> Bool {\\n     b >= B_D0 && b <= B_D7\\n }\\n \\n-// Tokenize `source` into a Vector of tokens (terminated by an EOF token), or\\n-// return the first lex error. Mirrors sawc/lexer.py's observable output.\\n-public func lex(source: String) -> Result<Vector<Token>, LexError> {\\n-    var lexer = Lexer(src: source, slen: source.len(), pos: 0, line: 1, col: 1)\\n-    var docs = Vector<DocComment>()\\n-    lexer.tokenize(&var docs)\\n-}\\n-\\n-// Tokenize AND capture doc-comment trivia (design 121). Returns both the token\\n-// stream and the doc records in source order, or the first lex error.\\n-public func lex_all(source: String) -> Result<LexResult, LexError> {\\n+// Tokenize `source`, or return the first lex error. Mirrors sawc/lexer.py's\\n+// observable output: the token stream (terminated by an EOF token), the\\n+// doc-comment trivia (design 121) and the string-segment arena (design 268),\\n+// which is why there is ONE entry point rather than a tokens-only twin — a\\n+// token's `seg_start`/`seg_count` index into that arena.\\n+public func lex(source: String) -> Result<LexResult, LexError> {\\n     var lexer = Lexer(src: source, slen: source.len(), pos: 0, line: 1, col: 1)\\n     var docs = Vector<DocComment>()\\n-    let toks = try lexer.tokenize(&var docs)\\n-    LexResult(tokens: move toks, docs: move docs)\\n+    var segments = Vector<StringSegment>()\\n+    let toks = try lexer.tokenize(&var docs, &var segments)\\n+    LexResult(tokens: move toks, docs: move docs, segments: move segments)\\n }\\ndiff --git a/selfhost/lexer/src/main.saw b/selfhost/lexer/src/main.saw\\nindex cf2f3c71..65611e15 100644\\n--- a/selfhost/lexer/src/main.saw\\n+++ b/selfhost/lexer/src/main.saw\\n@@ -59,7 +59,7 @@ func main() -> Int {\\n     }\\n \\n     if docs_mode {\\n-        match lex_all(source) {\\n+        match lex(source) {\\n             case Ok(r) -> {\\n                 var j = 0\\n                 while j < r.docs.len() {\\n@@ -77,11 +77,11 @@ func main() -> Int {\\n         }\\n     } else {\\n         match lex(source) {\\n-            case Ok(tokens) -> {\\n+            case Ok(r) -> {\\n                 var j = 0\\n-                while j < tokens.len() {\\n-                    if let t = tokens.get(j) {\\n-                        print(format_token(&t))\\n+                while j < r.tokens.len() {\\n+                    if let t = r.tokens.get(j) {\\n+                        print(format_token(&t, &r.segments))\\n                     }\\n                     j = j + 1\\n                 }\\ndiff --git a/selfhost/lexer/tests/comments.saw b/selfhost/lexer/tests/comments.saw\\nindex d69060f1..e1054329 100644\\n--- a/selfhost/lexer/tests/comments.saw\\n+++ b/selfhost/lexer/tests/comments.saw\\n@@ -1,22 +1,26 @@\\n // Line comments are skipped (never tokens); code after them still lexes.\\n import src.lib.*\\n \\n-func toks(s: String) -> Vector<Token> {\\n+func lexed(s: String) -> LexResult {\\n     match lex(s) {\\n-        case Ok(t) -> move t,\\n+        case Ok(r) -> move r,\\n         case Err(e) -> panic(\\\"unexpected lex error at {e.line}:{e.col}: {e.message}\\\")\\n     }\\n }\\n \\n-func at(ts: &Vector<Token>, i: Int) -> String {\\n-    if let t = ts.get(i) { format_token(&t) } else { panic(\\\"no token at index {i}\\\") }\\n+func at(r: &LexResult, i: Int) -> String {\\n+    if let t = r.tokens.get(i) {\\n+        format_token(&t, &r.segments)\\n+    } else {\\n+        panic(\\\"no token at index {i}\\\")\\n+    }\\n }\\n \\n-func count(ts: &Vector<Token>) -> Int { ts.len() }\\n+func count(r: &LexResult) -> Int { r.tokens.len() }\\n \\n func main() {\\n     // A trailing comment produces no tokens; the NEWLINE after it survives.\\n-    let a = toks(\\\"x // comment here\\\\ny\\\")\\n+    let a = lexed(\\\"x // comment here\\\\ny\\\")\\n     assert(at(&a, 0).equals(\\\"IDENT\\\\t1:1\\\\tx\\\"), \\\"x\\\")\\n     assert(at(&a, 1).equals(\\\"NEWLINE\\\\t1:18\\\\t\\\\\\\\n\\\"), \\\"newline after comment\\\")\\n     assert(at(&a, 2).equals(\\\"IDENT\\\\t2:1\\\\ty\\\"), \\\"y on next line\\\")\\n@@ -24,12 +28,12 @@ func main() {\\n     assert(count(&a) == 4, \\\"comment contributed no tokens\\\")\\n \\n     // A whole-line comment: only the NEWLINE and EOF remain.\\n-    let b = toks(\\\"// just a comment\\\\n\\\")\\n+    let b = lexed(\\\"// just a comment\\\\n\\\")\\n     assert(at(&b, 0).equals(\\\"NEWLINE\\\\t1:18\\\\t\\\\\\\\n\\\"), \\\"newline\\\")\\n     assert(count(&b) == 2, \\\"comment-only line: NEWLINE + EOF\\\")\\n \\n     // A `//` inside a comment does not start anything; a division `/` is distinct.\\n-    let c = toks(\\\"a / b\\\")\\n+    let c = lexed(\\\"a / b\\\")\\n     assert(at(&c, 1).equals(\\\"SLASH\\\\t1:3\\\\t/\\\"), \\\"division slash\\\")\\n \\n     print(\\\"comments: ok\\\")\\ndiff --git a/selfhost/lexer/tests/directives.saw b/selfhost/lexer/tests/directives.saw\\nindex ba7e04a5..705192d3 100644\\n--- a/selfhost/lexer/tests/directives.saw\\n+++ b/selfhost/lexer/tests/directives.saw\\n@@ -1,26 +1,30 @@\\n // #file / #line / #function source-location directives, and NEWLINE tokens.\\n import src.lib.*\\n \\n-func toks(s: String) -> Vector<Token> {\\n+func lexed(s: String) -> LexResult {\\n     match lex(s) {\\n-        case Ok(t) -> move t,\\n+        case Ok(r) -> move r,\\n         case Err(e) -> panic(\\\"unexpected lex error at {e.line}:{e.col}: {e.message}\\\")\\n     }\\n }\\n \\n-func at(ts: &Vector<Token>, i: Int) -> String {\\n-    if let t = ts.get(i) { format_token(&t) } else { panic(\\\"no token at index {i}\\\") }\\n+func at(r: &LexResult, i: Int) -> String {\\n+    if let t = r.tokens.get(i) {\\n+        format_token(&t, &r.segments)\\n+    } else {\\n+        panic(\\\"no token at index {i}\\\")\\n+    }\\n }\\n \\n func main() {\\n     // The value is the bare directive name.\\n-    let a = toks(\\\"#file #line #function\\\")\\n+    let a = lexed(\\\"#file #line #function\\\")\\n     assert(at(&a, 0).equals(\\\"HASH_DIRECTIVE\\\\t1:1\\\\tfile\\\"), \\\"#file\\\")\\n     assert(at(&a, 1).equals(\\\"HASH_DIRECTIVE\\\\t1:7\\\\tline\\\"), \\\"#line\\\")\\n     assert(at(&a, 2).equals(\\\"HASH_DIRECTIVE\\\\t1:13\\\\tfunction\\\"), \\\"#function\\\")\\n \\n     // A NEWLINE token carries the newline, and line/col advance across it.\\n-    let b = toks(\\\"a\\\\nb\\\")\\n+    let b = lexed(\\\"a\\\\nb\\\")\\n     assert(at(&b, 0).equals(\\\"IDENT\\\\t1:1\\\\ta\\\"), \\\"line 1 ident\\\")\\n     assert(at(&b, 1).equals(\\\"NEWLINE\\\\t1:2\\\\t\\\\\\\\n\\\"), \\\"newline token, escaped value\\\")\\n     assert(at(&b, 2).equals(\\\"IDENT\\\\t2:1\\\\tb\\\"), \\\"line 2 ident at col 1\\\")\\ndiff --git a/selfhost/lexer/tests/doc_comments.saw b/selfhost/lexer/tests/doc_comments.saw\\nindex a82b16bd..205cc5b4 100644\\n--- a/selfhost/lexer/tests/doc_comments.saw\\n+++ b/selfhost/lexer/tests/doc_comments.saw\\n@@ -1,10 +1,10 @@\\n // Doc comments (`///`, `//!`) are captured as trivia: they never enter the token\\n-// stream, and `lex_all` reports them separately (design 121).\\n+// stream, and the lex result reports them separately (design 121).\\n import src.lib.*\\n \\n func recs(s: String) -> Vector<String> {\\n     var out = Vector<String>()\\n-    match lex_all(s) {\\n+    match lex(s) {\\n         case Ok(r) -> {\\n             var i = 0\\n             while i < r.docs.len() {\\n@@ -25,10 +25,10 @@ func at(rs: &Vector<String>, i: Int) -> String {\\n func kinds(s: String) -> String {\\n     var out = StringBuilder()\\n     match lex(s) {\\n-        case Ok(ts) -> {\\n+        case Ok(r) -> {\\n             var i = 0\\n-            while i < ts.len() {\\n-                if let t = ts.get(i) { try! out.append(format_token(&t)) }\\n+            while i < r.tokens.len() {\\n+                if let t = r.tokens.get(i) { try! out.append(format_token(&t, &r.segments)) }\\n                 try! out.append(\\\"|\\\")\\n                 i = i + 1\\n             }\\ndiff --git a/selfhost/lexer/tests/escapes.saw b/selfhost/lexer/tests/escapes.saw\\nindex 10031fae..db3febb9 100644\\n--- a/selfhost/lexer/tests/escapes.saw\\n+++ b/selfhost/lexer/tests/escapes.saw\\n@@ -1,9 +1,9 @@\\n // String escapes: \\\\n \\\\t \\\\r \\\\0 \\\\\\\\ \\\\\\\" \\\\{ and \\\\u{...} (incl. non-ASCII).\\n import src.lib.*\\n \\n-func toks(s: String) -> Vector<Token> {\\n+func lexed(s: String) -> LexResult {\\n     match lex(s) {\\n-        case Ok(t) -> move t,\\n+        case Ok(r) -> move r,\\n         case Err(e) -> panic(\\\"unexpected lex error at {e.line}:{e.col}: {e.message}\\\")\\n     }\\n }\\n@@ -11,8 +11,12 @@ func toks(s: String) -> Vector<Token> {\\n // The canonical dump line for the FIRST token of `s` (String by value, so no\\n // reference-to-temporary is needed).\\n func dump0(s: String) -> String {\\n-    let ts = toks(s)\\n-    if let t = ts.get(0) { format_token(&t) } else { panic(\\\"no tokens\\\") }\\n+    let r = lexed(s)\\n+    if let t = r.tokens.get(0) {\\n+        format_token(&t, &r.segments)\\n+    } else {\\n+        panic(\\\"no tokens\\\")\\n+    }\\n }\\n \\n // Wrap `inner` (the raw bytes between the quotes) into a source string literal.\\n@@ -60,8 +64,10 @@ func main() {\\n     try! exp.append(\\\"😀\\\")\\n     assert(dump0(u2.build()).equals(exp.build()), \\\"backslash-u{1F600} -> emoji bytes\\\")\\n \\n-    // \\\\{ escaped brace: value carries the 0x01 sentinel + '{', and it stays a\\n-    // plain STRING (NOT an interpolation).  Expected uses \\\\{ for a literal brace.\\n+    // \\\\{ escaped brace: the value is fully DECODED, so the brace is an ordinary\\n+    // byte and nothing is encoded beside it (design 268 deleted the 0x01\\n+    // sentinel that used to sit here — SL-238); it stays a plain STRING (NOT an\\n+    // interpolation). Expected uses \\\\{ for a literal brace.\\n     var e1 = StringBuilder()\\n     try! e1.append(Byte(34))      // \\\"\\n     try! e1.append(Byte(97))      // a\\n@@ -69,7 +75,22 @@ func main() {\\n     try! e1.append(Byte(123))     // {\\n     try! e1.append(Byte(98))      // b\\n     try! e1.append(Byte(34))      // \\\"\\n-    assert(dump0(e1.build()).equals(\\\"STRING\\\\t1:1\\\\ta\\\\\\\\x01\\\\{b\\\"), \\\"escaped-brace STRING\\\")\\n+    assert(dump0(e1.build()).equals(\\\"STRING\\\\t1:1\\\\ta\\\\{b\\\"), \\\"escaped-brace STRING\\\")\\n+\\n+    // A real U+0001 before an escaped brace is CONTENT, not a marker: the two\\n+    // bytes the old protocol used are exactly what `\\\\u{1}\\\\{` produces, and the\\n+    // decode used to delete the U+0001 (SL-238). Both bytes survive now.\\n+    var e2 = StringBuilder()\\n+    try! e2.append(Byte(34))      // \\\"\\n+    try! e2.append(Byte(92))      // backslash\\n+    try! e2.append(Byte(117))     // u\\n+    try! e2.append(Byte(123))     // {\\n+    try! e2.append(\\\"1\\\")\\n+    try! e2.append(Byte(125))     // }\\n+    try! e2.append(Byte(92))      // backslash\\n+    try! e2.append(Byte(123))     // {\\n+    try! e2.append(Byte(34))      // \\\"\\n+    assert(dump0(e2.build()).equals(\\\"STRING\\\\t1:1\\\\t\\\\\\\\x01\\\\{\\\"), \\\"U+0001 before a brace\\\")\\n \\n     print(\\\"escapes: ok\\\")\\n }\\ndiff --git a/selfhost/lexer/tests/interpolation.saw b/selfhost/lexer/tests/interpolation.saw\\nindex 7246f2cc..79d8eb20 100644\\n--- a/selfhost/lexer/tests/interpolation.saw\\n+++ b/selfhost/lexer/tests/interpolation.saw\\n@@ -1,17 +1,28 @@\\n // String interpolation delimiters: a bare `{...}` makes an INTERP_STRING whose\\n-// value keeps the braces raw (the parser handles the inner expression later).\\n+// value is the RAW source spelling between the quotes and whose parse input is\\n+// its TYPED SEGMENTS (design 268), dumped as one `T:`/`E:` field each.\\n import src.lib.*\\n \\n-func toks(s: String) -> Vector<Token> {\\n+func lexed(s: String) -> LexResult {\\n     match lex(s) {\\n-        case Ok(t) -> move t,\\n+        case Ok(r) -> move r,\\n         case Err(e) -> panic(\\\"unexpected lex error at {e.line}:{e.col}: {e.message}\\\")\\n     }\\n }\\n \\n func dump0(s: String) -> String {\\n-    let ts = toks(s)\\n-    if let t = ts.get(0) { format_token(&t) } else { panic(\\\"no tokens\\\") }\\n+    let r = lexed(s)\\n+    if let t = r.tokens.get(0) {\\n+        format_token(&t, &r.segments)\\n+    } else {\\n+        panic(\\\"no tokens\\\")\\n+    }\\n+}\\n+\\n+// The segment count of the FIRST token of `s`.\\n+func segs0(s: String) -> Int {\\n+    let r = lexed(s)\\n+    if let t = r.tokens.get(0) { t.seg_count } else { panic(\\\"no tokens\\\") }\\n }\\n \\n // A source string literal `\\\"<parts...>\\\"` with a bare `{name}` interior.\\n@@ -40,27 +51,39 @@ func nested_src() -> String {\\n     sb.build()\\n }\\n \\n+// A source literal `\\\"{x}{y}\\\"` — two adjacent interpolations, no literal text.\\n+func adjacent_src() -> String {\\n+    var sb = StringBuilder()\\n+    try! sb.append(Byte(34))      // \\\"\\n+    try! sb.append(Byte(123))     // {\\n+    try! sb.append(\\\"x\\\")\\n+    try! sb.append(Byte(125))     // }\\n+    try! sb.append(Byte(123))     // {\\n+    try! sb.append(\\\"y\\\")\\n+    try! sb.append(Byte(125))     // }\\n+    try! sb.append(Byte(34))      // \\\"\\n+    sb.build()\\n+}\\n+\\n func main() {\\n-    // Braces are preserved raw and the kind is INTERP_STRING.\\n-    // build the full expected text: \\\"hi {name}\\\"\\n-    var full = StringBuilder()\\n-    try! full.append(\\\"INTERP_STRING\\\\t1:1\\\\thi \\\")\\n-    try! full.append(Byte(123))\\n-    try! full.append(\\\"name\\\")\\n-    try! full.append(Byte(125))\\n-    assert(dump0(interp_src()).equals(full.build()), \\\"interpolation keeps braces\\\")\\n+    // The value is the raw spelling; the segments are the text run and the\\n+    // interpolation, the latter positioned at its own `{` (column 5 here).\\n+    assert(dump0(interp_src()).equals(\\n+        \\\"INTERP_STRING\\\\t1:1\\\\thi \\\\{name\\\\}\\\\tT:hi \\\\tE:1:5:name\\\"),\\n+        \\\"interpolation segments\\\")\\n+\\n+    // Nested braces are balanced, and the interior is kept RAW — the expression\\n+    // segment is the whole `a {b} c`, with no text segment around it.\\n+    assert(dump0(nested_src()).equals(\\n+        \\\"INTERP_STRING\\\\t1:1\\\\t\\\\{a \\\\{b\\\\} c\\\\}\\\\tE:1:2:a \\\\{b\\\\} c\\\"),\\n+        \\\"nested braces balanced\\\")\\n \\n-    // Nested braces are balanced and preserved.\\n-    var nexp = StringBuilder()\\n-    try! nexp.append(\\\"INTERP_STRING\\\\t1:1\\\\t\\\")\\n-    try! nexp.append(Byte(123))\\n-    try! nexp.append(\\\"a \\\")\\n-    try! nexp.append(Byte(123))\\n-    try! nexp.append(\\\"b\\\")\\n-    try! nexp.append(Byte(125))\\n-    try! nexp.append(\\\" c\\\")\\n-    try! nexp.append(Byte(125))\\n-    assert(dump0(nested_src()).equals(nexp.build()), \\\"nested braces balanced\\\")\\n+    // Two adjacent interpolations: two `E:` segments and NO empty text segment\\n+    // between them (both lexers agree never to emit one).\\n+    assert(dump0(adjacent_src()).equals(\\n+        \\\"INTERP_STRING\\\\t1:1\\\\t\\\\{x\\\\}\\\\{y\\\\}\\\\tE:1:2:x\\\\tE:1:5:y\\\"),\\n+        \\\"adjacent interpolations\\\")\\n+    assert(segs0(adjacent_src()) == 2, \\\"two segments, no empty text run\\\")\\n \\n     print(\\\"interpolation: ok\\\")\\n }\\ndiff --git a/selfhost/lexer/tests/literals.saw b/selfhost/lexer/tests/literals.saw\\nindex 98f7cbd8..69291951 100644\\n--- a/selfhost/lexer/tests/literals.saw\\n+++ b/selfhost/lexer/tests/literals.saw\\n@@ -1,17 +1,17 @@\\n // Integer / float / string / bool literal forms.\\n import src.lib.*\\n \\n-func toks(s: String) -> Vector<Token> {\\n+func lexed(s: String) -> LexResult {\\n     match lex(s) {\\n-        case Ok(t) -> move t,\\n+        case Ok(r) -> move r,\\n         case Err(e) -> panic(\\\"unexpected lex error at {e.line}:{e.col}: {e.message}\\\")\\n     }\\n }\\n \\n // The canonical dump line for token `i` (kind + position + escaped text).\\n-func at(ts: &Vector<Token>, i: Int) -> String {\\n-    if let t = ts.get(i) {\\n-        format_token(&t)\\n+func at(r: &LexResult, i: Int) -> String {\\n+    if let t = r.tokens.get(i) {\\n+        format_token(&t, &r.segments)\\n     } else {\\n         panic(\\\"no token at index {i}\\\")\\n     }\\n@@ -20,7 +20,7 @@ func at(ts: &Vector<Token>, i: Int) -> String {\\n func main() {\\n     // Decimal, hex (digit case preserved, prefix lowercased), binary, octal,\\n     // underscores stripped, float.\\n-    let a = toks(\\\"42 0XFf 0b1010 0o17 1_000 3.14\\\")\\n+    let a = lexed(\\\"42 0XFf 0b1010 0o17 1_000 3.14\\\")\\n     assert(at(&a, 0).equals(\\\"INT\\\\t1:1\\\\t42\\\"), \\\"decimal\\\")\\n     assert(at(&a, 1).equals(\\\"INT\\\\t1:4\\\\t0xFf\\\"), \\\"hex: prefix lowercased, digits kept\\\")\\n     assert(at(&a, 2).equals(\\\"INT\\\\t1:9\\\\t0b1010\\\"), \\\"binary\\\")\\n@@ -31,56 +31,56 @@ func main() {\\n     // Width-suffixed literals lex as a single INT with the digits as the value\\n     // and the suffix as the dump's 4th column (design 119 Part D). 255u8 is in\\n     // range (no error).\\n-    let b = toks(\\\"255u8 65535u16 1_000i32\\\")\\n+    let b = lexed(\\\"255u8 65535u16 1_000i32\\\")\\n     assert(at(&b, 0).equals(\\\"INT\\\\t1:1\\\\t255\\\\tu8\\\"), \\\"255u8 -> INT 255 u8\\\")\\n     assert(at(&b, 1).equals(\\\"INT\\\\t1:7\\\\t65535\\\\tu16\\\"), \\\"65535u16 -> INT 65535 u16\\\")\\n     assert(at(&b, 2).equals(\\\"INT\\\\t1:16\\\\t1000\\\\ti32\\\"), \\\"1_000i32 -> INT 1000 i32\\\")\\n \\n     // `5abc` stays INT + IDENT (no bogus suffix, no 4th column); `0xFace` is hex.\\n-    let c = toks(\\\"5abc 0xFace\\\")\\n+    let c = lexed(\\\"5abc 0xFace\\\")\\n     assert(at(&c, 0).equals(\\\"INT\\\\t1:1\\\\t5\\\"), \\\"5abc: INT\\\")\\n     assert(at(&c, 1).equals(\\\"IDENT\\\\t1:2\\\\tabc\\\"), \\\"5abc: IDENT\\\")\\n     assert(at(&c, 2).equals(\\\"INT\\\\t1:6\\\\t0xFace\\\"), \\\"0xFace hex\\\")\\n \\n     // Design 161 LOOKBACK: digits right after a member-access `.` are a TUPLE\\n     // INDEX — a bare integer that stops at a second `.` and takes no suffix.\\n-    let e = toks(\\\"t.0.name\\\")\\n+    let e = lexed(\\\"t.0.name\\\")\\n     assert(at(&e, 0).equals(\\\"IDENT\\\\t1:1\\\\tt\\\"), \\\"t.0.name: receiver\\\")\\n     assert(at(&e, 1).equals(\\\"DOT\\\\t1:2\\\\t.\\\"), \\\"t.0.name: index dot\\\")\\n     assert(at(&e, 2).equals(\\\"INT\\\\t1:3\\\\t0\\\"), \\\"t.0.name: an index, not a float\\\")\\n     assert(at(&e, 3).equals(\\\"DOT\\\\t1:4\\\\t.\\\"), \\\"t.0.name: the member dot survives\\\")\\n     assert(at(&e, 4).equals(\\\"IDENT\\\\t1:5\\\\tname\\\"), \\\"t.0.name: field\\\")\\n \\n-    let f = toks(\\\"t.0.1\\\")\\n+    let f = lexed(\\\"t.0.1\\\")\\n     assert(at(&f, 2).equals(\\\"INT\\\\t1:3\\\\t0\\\"), \\\"t.0.1: first hop\\\")\\n     assert(at(&f, 3).equals(\\\"DOT\\\\t1:4\\\\t.\\\"), \\\"t.0.1: two hops, not the float 0.1\\\")\\n     assert(at(&f, 4).equals(\\\"INT\\\\t1:5\\\\t1\\\"), \\\"t.0.1: second hop\\\")\\n \\n-    let g = toks(\\\"t.0u8\\\")\\n+    let g = lexed(\\\"t.0u8\\\")\\n     assert(at(&g, 2).equals(\\\"INT\\\\t1:3\\\\t0\\\"), \\\"a tuple index takes no width suffix\\\")\\n     assert(at(&g, 3).equals(\\\"IDENT\\\\t1:4\\\\tu8\\\"), \\\"the suffix bytes stay an identifier\\\")\\n \\n     // Design 161 LOOKAHEAD: a `.` continues a number only when a DIGIT follows\\n     // it, so a method call on a literal lexes and `7.` is INT + DOT.\\n-    let h = toks(\\\"7.to_string()\\\")\\n+    let h = lexed(\\\"7.to_string()\\\")\\n     assert(at(&h, 0).equals(\\\"INT\\\\t1:1\\\\t7\\\"), \\\"7.to_string(): the literal\\\")\\n     assert(at(&h, 1).equals(\\\"DOT\\\\t1:2\\\\t.\\\"), \\\"7.to_string(): member dot\\\")\\n     assert(at(&h, 2).equals(\\\"IDENT\\\\t1:3\\\\tto_string\\\"), \\\"7.to_string(): method name\\\")\\n \\n-    let i = toks(\\\"7.\\\")\\n+    let i = lexed(\\\"7.\\\")\\n     assert(at(&i, 0).equals(\\\"INT\\\\t1:1\\\\t7\\\"), \\\"trailing dot: INT\\\")\\n     assert(at(&i, 1).equals(\\\"DOT\\\\t1:2\\\\t.\\\"), \\\"trailing dot: DOT, never a float\\\")\\n \\n     // A separate lex: the lookback rule reads the PREVIOUS token, so a float has\\n     // to start a stream of its own to be judged as one (`7. 7.5` in one line\\n     // would make that second number an index off the first dot).\\n-    let j = toks(\\\"7.5.len\\\")\\n+    let j = lexed(\\\"7.5.len\\\")\\n     assert(at(&j, 0).equals(\\\"FLOAT\\\\t1:1\\\\t7.5\\\"), \\\"digit-dot-digit is still a float\\\")\\n     assert(at(&j, 1).equals(\\\"DOT\\\\t1:4\\\\t.\\\"), \\\"and the float's own member dot\\\")\\n     assert(at(&j, 2).equals(\\\"IDENT\\\\t1:5\\\\tlen\\\"), \\\"method name after a float\\\")\\n \\n     // Keywords vs identifiers; true/false map to their own kinds.\\n-    let d = toks(\\\"func x let true false module\\\")\\n+    let d = lexed(\\\"func x let true false module\\\")\\n     assert(at(&d, 0).equals(\\\"FUNC\\\\t1:1\\\\tfunc\\\"), \\\"func keyword\\\")\\n     assert(at(&d, 1).equals(\\\"IDENT\\\\t1:6\\\\tx\\\"), \\\"ident\\\")\\n     assert(at(&d, 2).equals(\\\"LET\\\\t1:8\\\\tlet\\\"), \\\"let keyword\\\")\\ndiff --git a/selfhost/lexer/tests/operators.saw b/selfhost/lexer/tests/operators.saw\\nindex 95721ae6..ccfc800c 100644\\n--- a/selfhost/lexer/tests/operators.saw\\n+++ b/selfhost/lexer/tests/operators.saw\\n@@ -1,24 +1,28 @@\\n // Operators, compound assignments, wrapping ops, punctuation, $-params, @.\\n import src.lib.*\\n \\n-func toks(s: String) -> Vector<Token> {\\n+func lexed(s: String) -> LexResult {\\n     match lex(s) {\\n-        case Ok(t) -> move t,\\n+        case Ok(r) -> move r,\\n         case Err(e) -> panic(\\\"unexpected lex error at {e.line}:{e.col}: {e.message}\\\")\\n     }\\n }\\n \\n-func kind(ts: &Vector<Token>, i: Int) -> TokenKind {\\n-    if let t = ts.get(i) { t.kind } else { panic(\\\"no token at index {i}\\\") }\\n+func kind(r: &LexResult, i: Int) -> TokenKind {\\n+    if let t = r.tokens.get(i) { t.kind } else { panic(\\\"no token at index {i}\\\") }\\n }\\n \\n-func at(ts: &Vector<Token>, i: Int) -> String {\\n-    if let t = ts.get(i) { format_token(&t) } else { panic(\\\"no token at index {i}\\\") }\\n+func at(r: &LexResult, i: Int) -> String {\\n+    if let t = r.tokens.get(i) {\\n+        format_token(&t, &r.segments)\\n+    } else {\\n+        panic(\\\"no token at index {i}\\\")\\n+    }\\n }\\n \\n func main() {\\n     // Multi-char operators and their single-char fallbacks.\\n-    let a = toks(\\\"+= -> && &+ &- &* &= <<= >>= <= >= == != ?? ?. |= ^= %=\\\")\\n+    let a = lexed(\\\"+= -> && &+ &- &* &= <<= >>= <= >= == != ?? ?. |= ^= %=\\\")\\n     assert(kind(&a, 0) == TokenKind.PlusAssign, \\\"+=\\\")\\n     assert(kind(&a, 1) == TokenKind.Arrow, \\\"->\\\")\\n     assert(kind(&a, 2) == TokenKind.And, \\\"&&\\\")\\n@@ -39,12 +43,12 @@ func main() {\\n     assert(kind(&a, 17) == TokenKind.PercentAssign, \\\"%=\\\")\\n \\n     // Bare `<<` stays two `<` tokens (nested generics), NOT a shift.\\n-    let b = toks(\\\"<<\\\")\\n+    let b = lexed(\\\"<<\\\")\\n     assert(kind(&b, 0) == TokenKind.Lt, \\\"bare << is Lt\\\")\\n     assert(kind(&b, 1) == TokenKind.Lt, \\\"bare << is Lt Lt\\\")\\n \\n     // Single-char operators / punctuation (no braces here — see `braces` below).\\n-    let c = toks(\\\"& | ^ ~ ! ? < > ( ) [ ] ; , : . @\\\")\\n+    let c = lexed(\\\"& | ^ ~ ! ? < > ( ) [ ] ; , : . @\\\")\\n     assert(kind(&c, 0) == TokenKind.Ampersand, \\\"&\\\")\\n     assert(kind(&c, 1) == TokenKind.Pipe, \\\"|\\\")\\n     assert(kind(&c, 2) == TokenKind.Caret, \\\"^\\\")\\n@@ -61,7 +65,7 @@ func main() {\\n     var bs = StringBuilder()\\n     try! bs.append(Byte(123))     // {\\n     try! bs.append(Byte(125))     // }\\n-    let br = toks(bs.build())\\n+    let br = lexed(bs.build())\\n     assert(kind(&br, 0) == TokenKind.LBrace, \\\"lbrace\\\")\\n     assert(kind(&br, 1) == TokenKind.RBrace, \\\"rbrace\\\")\\n     var lb_exp = StringBuilder()\\n@@ -70,12 +74,12 @@ func main() {\\n     assert(at(&br, 0).equals(lb_exp.build()), \\\"LBRACE value is a bare brace\\\")\\n \\n     // Shorthand closure parameters.\\n-    let d = toks(\\\"$0 + $12\\\")\\n+    let d = lexed(\\\"$0 + $12\\\")\\n     assert(at(&d, 0).equals(\\\"DOLLAR_PARAM\\\\t1:1\\\\t$0\\\"), \\\"$0\\\")\\n     assert(at(&d, 2).equals(\\\"DOLLAR_PARAM\\\\t1:6\\\\t$12\\\"), \\\"$12\\\")\\n \\n     // Trailing EOF token.\\n-    let e = toks(\\\"x\\\")\\n+    let e = lexed(\\\"x\\\")\\n     assert(kind(&e, 1) == TokenKind.Eof, \\\"EOF terminator\\\")\\n \\n     print(\\\"operators: ok\\\")\\ndiff --git a/selfhost/lexer/tests/ranges.saw b/selfhost/lexer/tests/ranges.saw\\nindex c49c7d2a..42e6acd2 100644\\n--- a/selfhost/lexer/tests/ranges.saw\\n+++ b/selfhost/lexer/tests/ranges.saw\\n@@ -2,26 +2,30 @@\\n // `.` needs a digit after it.\\n import src.lib.*\\n \\n-func toks(s: String) -> Vector<Token> {\\n+func lexed(s: String) -> LexResult {\\n     match lex(s) {\\n-        case Ok(t) -> move t,\\n+        case Ok(r) -> move r,\\n         case Err(e) -> panic(\\\"unexpected lex error at {e.line}:{e.col}: {e.message}\\\")\\n     }\\n }\\n \\n-func at(ts: &Vector<Token>, i: Int) -> String {\\n-    if let t = ts.get(i) { format_token(&t) } else { panic(\\\"no token at index {i}\\\") }\\n+func at(r: &LexResult, i: Int) -> String {\\n+    if let t = r.tokens.get(i) {\\n+        format_token(&t, &r.segments)\\n+    } else {\\n+        panic(\\\"no token at index {i}\\\")\\n+    }\\n }\\n \\n func main() {\\n     // Exclusive range: INT `..` INT (the dot pair is not consumed by the number).\\n-    let a = toks(\\\"0..5\\\")\\n+    let a = lexed(\\\"0..5\\\")\\n     assert(at(&a, 0).equals(\\\"INT\\\\t1:1\\\\t0\\\"), \\\"0..5 -> INT 0\\\")\\n     assert(at(&a, 1).equals(\\\"DOTDOT\\\\t1:2\\\\t..\\\"), \\\"0..5 -> DOTDOT\\\")\\n     assert(at(&a, 2).equals(\\\"INT\\\\t1:4\\\\t5\\\"), \\\"0..5 -> INT 5\\\")\\n \\n     // Inclusive range `..=` is a single token, checked before bare `..`.\\n-    let b = toks(\\\"0..=5\\\")\\n+    let b = lexed(\\\"0..=5\\\")\\n     assert(at(&b, 0).equals(\\\"INT\\\\t1:1\\\\t0\\\"), \\\"0..=5 -> INT 0\\\")\\n     assert(at(&b, 1).equals(\\\"DOTDOT_EQ\\\\t1:2\\\\t..=\\\"), \\\"0..=5 -> DOTDOT_EQ\\\")\\n     assert(at(&b, 2).equals(\\\"INT\\\\t1:5\\\\t5\\\"), \\\"0..=5 -> INT 5\\\")\\n@@ -29,24 +33,24 @@ func main() {\\n     // Design 161: a `.` continues a number only when a DIGIT follows it, so\\n     // there is no trailing-dot float any more — `7.foo` is a member access off\\n     // the integer 7 (which is what makes `7.to_string()` lex).\\n-    let c = toks(\\\"7.foo\\\")\\n+    let c = lexed(\\\"7.foo\\\")\\n     assert(at(&c, 0).equals(\\\"INT\\\\t1:1\\\\t7\\\"), \\\"7.foo -> INT 7\\\")\\n     assert(at(&c, 1).equals(\\\"DOT\\\\t1:2\\\\t.\\\"), \\\"7.foo -> DOT\\\")\\n     assert(at(&c, 2).equals(\\\"IDENT\\\\t1:3\\\\tfoo\\\"), \\\"7.foo -> IDENT foo\\\")\\n \\n     // `7..5` keeps the number whole and lexes the range operator.\\n-    let d = toks(\\\"7..5\\\")\\n+    let d = lexed(\\\"7..5\\\")\\n     assert(at(&d, 0).equals(\\\"INT\\\\t1:1\\\\t7\\\"), \\\"7..5 -> INT 7\\\")\\n     assert(at(&d, 1).equals(\\\"DOTDOT\\\\t1:2\\\\t..\\\"), \\\"7..5 -> DOTDOT\\\")\\n \\n     // A second dot in a float ends the number: `7.5.6` -> FLOAT 7.5 DOT INT 6.\\n-    let e = toks(\\\"7.5.6\\\")\\n+    let e = lexed(\\\"7.5.6\\\")\\n     assert(at(&e, 0).equals(\\\"FLOAT\\\\t1:1\\\\t7.5\\\"), \\\"7.5.6 -> FLOAT 7.5\\\")\\n     assert(at(&e, 1).equals(\\\"DOT\\\\t1:4\\\\t.\\\"), \\\"7.5.6 -> DOT\\\")\\n     assert(at(&e, 2).equals(\\\"INT\\\\t1:5\\\\t6\\\"), \\\"7.5.6 -> INT 6\\\")\\n \\n     // `...` ellipsis and a leading-dot member access are distinct.\\n-    let f = toks(\\\"a...b .5\\\")\\n+    let f = lexed(\\\"a...b .5\\\")\\n     assert(at(&f, 1).equals(\\\"ELLIPSIS\\\\t1:2\\\\t...\\\"), \\\"ellipsis\\\")\\n     assert(at(&f, 3).equals(\\\"DOT\\\\t1:7\\\\t.\\\"), \\\".5 -> DOT\\\")\\n     assert(at(&f, 4).equals(\\\"INT\\\\t1:8\\\\t5\\\"), \\\".5 -> INT 5\\\")\\ndiff --git a/tools/dump_tokens.py b/tools/dump_tokens.py\\nindex 7e41c0b9..15310fdf 100644\\n--- a/tools/dump_tokens.py\\n+++ b/tools/dump_tokens.py\\n@@ -6,6 +6,11 @@ one record per token:\\n \\n     KIND<TAB>line:col<TAB>escaped-text[<TAB>suffix]\\n \\n+An INTERP_STRING record (design 268) carries the literal's raw source spelling\\n+as its escaped text and then ONE FIELD PER TYPED SEGMENT:\\n+\\n+    INTERP_STRING<TAB>line:col<TAB>raw<TAB>T:text<TAB>E:line:col:raw-expr...\\n+\\n and, if the lexer rejects the file, a single record\\n \\n     ERROR<TAB>line:col<TAB>message\\n@@ -68,16 +73,38 @@ def escape_text(value: str) -> bytes:\\n     return bytes(out)\\n \\n \\n+def format_segment(seg) -> bytes:\\n+    \\\"\\\"\\\"One canonical segment field (design 268) for an INTERP_STRING record:\\n+\\n+        T:<escaped text>              a literal run (decoded content)\\n+        E:<line>:<col>:<escaped text> an interpolation (RAW text, `{` position)\\n+\\n+    The escaper never emits a tab or newline, so the fields stay splittable.\\n+    `format_segment` in lib.saw emits the identical shape.\\n+    \\\"\\\"\\\"\\n+    if seg.kind == \\\"text\\\":\\n+        return b\\\"T:\\\" + escape_text(seg.text)\\n+    return ((\\\"E:%d:%d:\\\" % (seg.line, seg.column)).encode(\\\"ascii\\\")\\n+            + escape_text(seg.text))\\n+\\n+\\n def format_token(tok) -> bytes:\\n     # The 4th column is the fixed-width integer suffix (e.g. `u8` for `255u8`),\\n     # present only for a suffixed integer literal; every other token stops at the\\n     # escaped text. The Saw port's `format_token` emits the identical shape (the\\n     # DF-116a miscompile that had stopped the suffix unit is fixed — design 119\\n     # Part D).\\n+    #\\n+    # An INTERP_STRING (design 268) instead continues with ONE FIELD PER TYPED\\n+    # SEGMENT, in source order, after the escaped text (which for that token is\\n+    # the literal's raw source spelling). No token carries both a suffix and\\n+    # segments, so the tail is unambiguous.\\n     head = (\\\"%s\\\\t%d:%d\\\\t\\\" % (tok.type.name, tok.line, tok.column)).encode(\\\"ascii\\\")\\n     rec = head + escape_text(tok.value)\\n     if getattr(tok, \\\"suffix\\\", None) is not None:\\n         rec += (\\\"\\\\t%s\\\" % tok.suffix).encode(\\\"ascii\\\")\\n+    for seg in (getattr(tok, \\\"segments\\\", None) or ()):\\n+        rec += b\\\"\\\\t\\\" + format_segment(seg)\\n     return rec\\n \\n \\n\",\"id\":\"SL-238.p1\",\"request_id\":\"sl238-r2-attempt1\",\"revision\":1}","request_id":"sl238-r2-attempt1","sequence":593,"timestamp":"1789143196"}
---
