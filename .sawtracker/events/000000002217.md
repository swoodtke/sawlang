---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-air-sawlang","closed":"","comments":[],"created":"1790180930","description":"FOUND by an SL-357 wave-2 agent on Sep 23, while it was compile-checking the std doc examples. Reproduced on main fd2520e9.\n\n## Repro\nProgram (`.build/scratch/fixedbuf_qual.saw`):\n```saw\nimport std.fixedbuf\nfunc main() {\n    var fb = fixedbuf.FixedStringBuilder<32>()\n    fb.append(\"hi\")\n    print(fb.as_string())\n}\n```\nOutput: `internal compiler error at 4:22 (MethodCall): Undefined struct: FixedStringBuilder`.\n\n## Probed positions\n| Position | Result |\n|---|---|\n| `var x = mod.T<N>()`, no annotation | ICE |\n| `mod.T<N>().method()`, as a receiver | ICE (q6) |\n| `var x: mod.T<N> = mod.T<N>()`, annotated | compiles |\n| `return mod.T<N>()` in `-> mod.T<N>` | compiles (q2) |\n| `import std.fixedbuf.{FixedStringBuilder}` + bare `FixedStringBuilder<64>()` | compiles and runs |\n| non-const generic `once.Once<Int>()`, qualified | clean diagnostic, no ICE (q5) |\n\n## Mechanism (not yet named precisely)\n- The qualified `mod.T<const N>()` construction is parsed as a MethodCall on the module qualifier.\n- With no expected type to steer it, the typechecker accepts the call. It never records or monomorphizes the const-generic instantiation under a name codegen can find, so codegen's struct lookup misses.\n- An expected type supplies the instantiation, which is why the annotated and return positions work.\n\n## Sweep owed before the fix (obligation 4)\n- The other qualified-construction positions: argument, field initializer, collection element, interpolation.\n- A user-module const-generic struct via `--module-path`.\n- A qualified type with both const and type parameters.\n\n## Pin\nThe table above, as EXPECT rows.\n","id":"SL-358","labels":["frozen","hazard"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":2217,"stage":"backlog","status":"open","title":"ICE: a module-qualified const-generic construction with no expected type — var fb = fixedbuf.FixedStringBuilder<32>() dies in codegen with 'Undefined struct: FixedStringBuilder'","updated":"1790341682"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-air-sawlang","closed":"","comments":[],"created":"1790180930","description":"FOUND by an SL-357 wave-2 agent on Sep 23, while it was compile-checking the std doc examples. Reproduced on main fd2520e9.\n\n## Repro\nProgram (`.build/scratch/fixedbuf_qual.saw`):\n```saw\nimport std.fixedbuf\nfunc main() {\n    var fb = fixedbuf.FixedStringBuilder<32>()\n    fb.append(\"hi\")\n    print(fb.as_string())\n}\n```\nOutput: `internal compiler error at 4:22 (MethodCall): Undefined struct: FixedStringBuilder`.\n\n## Probed positions\n| Position | Result |\n|---|---|\n| `var x = mod.T<N>()`, no annotation | ICE |\n| `mod.T<N>().method()`, as a receiver | ICE (q6) |\n| `var x: mod.T<N> = mod.T<N>()`, annotated | compiles |\n| `return mod.T<N>()` in `-> mod.T<N>` | compiles (q2) |\n| `import std.fixedbuf.{FixedStringBuilder}` + bare `FixedStringBuilder<64>()` | compiles and runs |\n| non-const generic `once.Once<Int>()`, qualified | clean diagnostic, no ICE (q5) |\n\n## Mechanism (not yet named precisely)\n- The qualified `mod.T<const N>()` construction is parsed as a MethodCall on the module qualifier.\n- With no expected type to steer it, the typechecker accepts the call. It never records or monomorphizes the const-generic instantiation under a name codegen can find, so codegen's struct lookup misses.\n- An expected type supplies the instantiation, which is why the annotated and return positions work.\n\n## Sweep owed before the fix (obligation 4)\n- The other qualified-construction positions: argument, field initializer, collection element, interpolation.\n- A user-module const-generic struct via `--module-path`.\n- A qualified type with both const and type parameters.\n\n## Pin\nThe table above, as EXPECT rows.\n","id":"SL-358","labels":["bug","typechecker","codegen"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1262,"stage":"backlog","status":"open","title":"ICE: a module-qualified const-generic construction with no expected type — var fb = fixedbuf.FixedStringBuilder<32>() dies in codegen with 'Undefined struct: FixedStringBuilder'","updated":"1790180930"},"id":"SL-358","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-358\",\"labels\":[\"frozen\",\"hazard\"],\"request_id\":\"pid-56997-1790341682-6000\",\"revision\":1}","request_id":"pid-56997-1790341682-6000","sequence":2217,"timestamp":"1790341682"}
---
