---
{"action":"patch_test_result","actor":"agent:sawtracker-tests","after":{"assignee":"agent:claude-sl204","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"RULINGS (user, Sep 12, via the design-272 decision list): D3-1 APPROVED — Signal = {Terminate, Interrupt, Hangup, Quit, User1, User2, WindowChanged}; SIGCHLD OUT (load-bearing: waitpid reaping depends on its default disposition). D3-3 APPROVED — SIGPIPE is suppressed PER SOCKET (SO_NOSIGPIPE macOS / MSG_NOSIGNAL Linux via unit 2's option op); EPIPE surfaces as an ordinary IoError Result; no process-wide disposition change; not a Signal case. Lead-endorsed as determined: D3-2 (double-watch errors; fan-out = one watch + a Channel), D3-4 (ordinary io park, budget reset, anyio covers the quiescent walk), D3-5 (reuse IoError), D3-6 (next() -> Result<Void, IoError>, coalescing documented).","created":"1789214589","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-228.p1 (revision 1): design 272 unit 3: std.signal — signals as suspending reactor events, graceful shutdown rides cancellation, no ABI change (SL-228)","created":"1789230899","id":"c2"},{"author":"agent:claude-sawlang","body":"SL-228.p1 SUBMITTED (design 272 unit 3, delta of trio commit a9d809a5, applies clean on merged units 1+2), server gate queued. Per rulings D3-1..6: Signal = {Terminate, Interrupt, Hangup, Quit, User1, User2, WindowChanged} (SIGCHLD out — waitpid reaping is load-bearing on its default disposition); watch(sig) -> Result handle, next() SUSPENDS on the reactor like a socket read; double-watch errors (fan-out = one watch + a Channel); next() -> Result<Void, IoError>, coalescing documented; NO exit registry — the K100 pattern is watch -> cancel -> deinits -> main returns; process.exit stays the no-cleanup hatch. SIGPIPE deliberately NOT a Signal case (per-socket suppression landed in unit 2, argued in the module header). MECHANISM NOTE for the reviewer: departs from the issue's kqueue/signalfd sketch for a load-bearing reason — signalfd requires the signal blocked in EVERY thread and a pre-existing thread cannot be retro-masked (a process-directed signal delivered there takes the default action and kills the process); delivery instead rides a self-pipe read end the EXISTING (fd, write, token) reactor seam already carries — NO frozen-seam change, argument recorded in rt/ABI.md. Handler-side send uses Signal.send_to_self() (a METHOD, which mangles) after SL-274's bare-symbol hazard bit a std free func named raise. Conformance K98-K100. Trio-wide terminal battery was 27/27 at the branch tip.","created":"1789230910","id":"c3"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-228.p1 (revision 1)\n\nRequest changes on revision 1 (base 78d328de, applied 178c64e8): two independently reproduced concurrency bugs in the signal bridge.\n\n1. [P1] Serialize watch ownership and lifecycle (`sawc/rt/shim.c:400–428`). The lazy initialization, already-watched check, descriptor publication and sigaction installation are unsynchronized. Two threads can both pass the check and both return a successful watch for one signal, violating AlreadyExists and the single-owner contract. Their descriptor and saved-disposition writes overwrite one another; one pipe becomes unreachable, and a saved “previous” disposition can be Saw's own handler. Dropping either handle can then invalidate the other or restore the wrong disposition. `volatile sig_atomic_t` does not serialize this transaction.\n\n   Reproduced with the exact patched shim: preinitialize with a different signal, then gate two callers at pipe() so both have passed the availability check before either publishes. Both SIGUSR1 watches succeed (`watch results: 5 3`) instead of exactly one returning -2. Harness: `/Users/swoodtke/Projects/sl228-review-r1/.build/scratch/watch_race.c`. Initialization, acquisition and teardown need coordinated synchronization, with a concurrent same-signal regression test.\n\n2. [P1] Quiesce in-flight handlers before reclaiming their pipe (`sawc/rt/shim.c:444–450`, handler fd load/write at 360–363). Restoring the disposition prevents future handler entries but does not wait for a handler already running on another thread. Such a handler can have loaded the old write descriptor before unwatch clears the slot and closes the pipe. If another pipe or file reuses that number, the delayed handler writes its notification byte into the unrelated resource. A mutex around watch/unwatch alone will not fix this handler-versus-close race.\n\n   Deterministic reproduction against the patched source: a scheduling hook pauses the actual signal handler immediately before write(), after it has loaded the descriptor; another thread unwatches and opens an unrelated pipe that reuses the descriptors; resume the handler. Output: `unrelated pipe received 1 byte, value=1`. Harness: `/Users/swoodtke/Projects/sl228-review-r1/.build/scratch/signal_close_race.c` (build with `clang -std=c11 -pthread -Wl,-dead_strip .build/scratch/signal_close_race.c -o .build/scratch/signal_close_race`). Please establish a signal-safe reclamation protocol and pin teardown concurrent with delivery and descriptor reuse.\n\nValidation: all four focused K98/K99/K100/hosted-only fixtures pass, as do the ABI documentation, citation and diff checks. These passing fixtures do not cover concurrent acquisition or in-flight delivery during teardown. Server tests were still pending when the review was prepared.\n","created":"1789232871","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-228.p1 (revision 1)","created":"1789234160","id":"c5"},{"author":"agent:claude-sawlang","body":"Revised patch SL-228.p1 to revision 2","created":"1789235202","id":"c6"},{"author":"agent:claude-sawlang","body":"SL-228.p1 r2 SUBMITTED (fix round for review r1's two shim P1s; commit 2e8a9859 as the unit-3 delta). P1a: one pthread_mutex spans the whole watch/unwatch transaction — the HANDLER stays lock-free by necessity (it can interrupt a thread inside the mutex; the constraint is now written at the top of the block). P1b: reclamation made IMPOSSIBLE rather than synchronized — the pipe is created at first watch and lives for the process; unwatch restores disposition and drains, closes nothing (two fds per watched signal, 14 worst case, 0 unwatched). The token-validation alternative was REJECTED on its own terms: a handler that validates-then-writes is the same TOCTOU in a smaller font; its useful half survives inverted — each watch bumps a generation, the handler STAMPS its byte, and the DRAIN (reader-side, race-free) validates, so a stale byte from a previous watch can never fake a delivery into a shutdown path. Both races reproduced pre-fix with independently-written harnesses landing on the reviewer's exact numbers (results 5 3; unrelated pipe received value=1, fds 3/4 reused); pinned by tools/signal_race_probe.c linking the REAL shim, run via examples/signal_concurrency_pins.saw in the ordinary suite — three cells fail against the pre-fix shim, all pass now. Obligation-4 sweep cleared every other shim transaction with reasons (the offload thunk shares the publish-then-write shape and is safe only via pthread_join-before-close — the contrast argued in place); filed SL-276 (thread_spawn discards pthread_create failure — different class). Brief mechanism section + rt/ABI.md rewritten to the shipped self-pipe design. Re-ran: suite 2503/11x, freestanding 36/36, abidoc exact, citations — full battery not re-run (no compiler code in the delta; r1's 27/27 covered unit 3's shape), stated per the mandate.","created":"1789235219","id":"c7"},{"author":"agent:sawtracker-tests","body":"Tests failed for SL-228.p1 (revision 2):\n\n```\n[2482/2514] ✓ vector_with_ref_oob_panic\n[2483/2514] ✓ while_let_tuple_over_suspension_refused\n[2484/2514] ✓ while_never_freestanding\n(2505/2514) · while_nested\n[2485/2514] ✓ vector_with_var_ref_oob_panic\n[2486/2514] ✓ version_semver_sort\n(2506/2514) · while_never_diverges\n[2487/2514] ✓ vis258_private_is_an_ordinary_identifier\n(2507/2514) · while_never_break_forms\n[2488/2514] ✓ vis258_field_inherits_public\n(2508/2514) · while_let_suspending_mt\n(2509/2514) · while_let_suspending_try_catch\n(2510/2514) · while_simple\n(2511/2514) · wrapping_operators\n(2512/2514) · while_let_suspending\n(2513/2514) · wrapping_and_reference_mix\n[2489/2514] ✓ vis80_public_members_ok\n[2490/2514] ✓ vis80_same_module_ok\n[2491/2514] ✓ visibility_package_access\n(2514/2514) · while_never_sync_context\n[2492/2514] ✓ visibility_package\n[2493/2514] ✓ visibility_parent\n[2494/2514] ✓ visibility_parent_public_works\n[2495/2514] ✓ void_match_in_closure\n[2496/2514] ✓ visibility_public\n\nCompiles complete in 395.3s (0/2514 reused, 0%) — 930 settled without running, 1584 binaries queued for execution (1566 of them already run).\n\n[2497/2514] ✓ while_break_continue\n[2498/2514] ✓ while_expr_conditional_found\n[2499/2514] ✓ void_if_chain_tail\n[2500/2514] ✓ while_expr_infinite\n[2501/2514] ✓ while_expr_conditional_not_found\n[2502/2514] ✓ while_infinite\n[2503/2514] ✓ while_let_drain\n[2504/2514] ✓ while_let_channel_drain\n[2505/2514] ✓ while_nested\n[2506/2514] ✓ while_never_diverges\n[2507/2514] ✓ while_never_break_forms\n[2508/2514] ✓ while_let_suspending_mt\n[2509/2514] ✓ while_let_suspending_try_catch\n[2510/2514] ✓ while_simple\n[2511/2514] ✓ wrapping_operators\n[2512/2514] ✓ while_let_suspending\n[2513/2514] ✓ wrapping_and_reference_mix\n[2514/2514] ✓ while_never_sync_context\n\ncompile 395.3s + 4.4s draining = 399.6s total\n\n======================================================================\nTest Results\n======================================================================\n\nFAILED TESTS:\n\n  ✗ signal_concurrency_pins\n    Output should contain 'signal race probe: all cells hold' — missing\n    Got:\n    BUILD FAILED (exit 1):\n    ld: open() failed, errno=2 (No such file or directory) for '/Users/shawn/Projects/sawtracker-production/data/worktrees/SL-228.p1.test/.build/scratch/signal_race_probe_suite'\n    clang: error: linker command failed with exit code 1 (use -v to see invocation)\n    \n    \n\nKNOWN FAILURES (xfail):\n  x V96_driven_capture_duplicates_once: SL-267 — a driven body's NON-ESCAPING closure capture duplicates\n  x closure_escaping_move_capture_consumed_once: DF-255a an escaping closure's env destructor releases a capture the\n  x closure_nested_ref_param_capture: DF-248b a nested closure captures an enclosing closure's reference\n  x closure_shorthand_parameter_inside_a_try: DF-257d the `$0` shorthand is invisible to the implicit-parameter scan\n  x coro_suspending_call_in_inline_catch: SL-215 the inline `try EXPR catch { }` catch block does not CFG-split,\n  x coro_value_inline_catch_hosts_a_move: SL-231 a value-carrying INLINE catch cannot host a `move` of a frame\n  x funcpointer_call_in_a_driven_body: DF-252a a `FuncPointer` value called by NAME inside a driven body is\n  x generic_init_moved_parameter_is_released_once: DF-302b — a generic extension's `init` releases a parameter it MOVED\n  x match_on_suspending_call_in_a_loop: SL-273 — a match arm PAYLOAD BINDING is lost when a SUSPENSION\n  x primitive_type_called_as_a_function: DF-270e — a PRIMITIVE TYPE NAME written as a ZERO-ARGUMENT call is an\n  x trailing_closure_inside_a_try_operand: DF-259c — a trailing closure is not recognized inside a `try`/`try!`/\n\n======================================================================\nSOME TESTS FAILED (2502 passed, 11 xfailed, 1 failed)\n======================================================================\n\nPublished test_runner_1789235204_36739 as test_runner_last (1542 manifest entries).\n\n```","created":"1789235605","id":"c8"}],"created":"1789050299","description":"MOTIVATION: a hosted Saw process cannot intercept SIGTERM/SIGINT — the sawtracker server dies with no flush, no socket drain, and stale lockfiles. The C idioms (signal(2) callbacks, atexit) fight deterministic destruction, the cooperative runtime, and never-hide-errors.\n\nRULED DESIGN (user, Sep 10 2026):\n1. SURFACE = SUSPENDING WATCH, no callbacks anywhere: hosted-only std.signal; watch(.terminate) returns a watch handle (Result — registration can fail; never hide errors) whose next() SUSPENDS until delivery, parking the task on the reactor exactly like a socket read. Watching a signal replaces its default disposition; unwatched signals keep OS behavior; SIGKILL/SIGSTOP are inexpressible.\n2. NO EXIT REGISTRY: cleanup rides cancellation + deinits. The pattern is a shutdown task that watches the signal, cancels the rest of the work (cancel already wakes io-parked tasks, design 102), values deinit deterministically as tasks unwind, main returns normally. process.exit(code) is documented as the no-cleanup escape hatch (no unwinding mid-flight).\n3. SCHEDULING: paired with SL-204 (io_wait deadline) as the server-hardening pair at the queue tail, behind the current soundness pipeline.\n\nMECHANISM SKETCH: both hosts integrate natively with the design-117 instance-based Saw reactor — kqueue EVFILT_SIGNAL (macOS; set SIG_IGN so the default action never fires) and signalfd (Linux; pthread_sigmask block, including at Thread/TaskGroup worker spawn — shim change). shim.c grows sigaction/sigprocmask-family wrappers (FFI-blocked bodies). Check whether any __saw_rt_* seam addition is needed or the reactor side suffices — rt/ABI.md is frozen, abidoc lane gates any change.\n\nBRIEF-TIME SCOPING (open, for the designs/NN brief at dispatch): the curated Signal enum membership + spellings (no-abbreviations doctrine applies); double-watch policy (error at second watch vs broadcast); whether the hosted runtime should default-ignore SIGPIPE for servers; interaction with op-budget fairness while a watcher task parks; freestanding simply lacks the module (like std.net). Obligation-2 consumer sweep: who relies on default signal dispositions today (test_runner child processes, blade, the QEMU harnesses).","id":"SL-228","labels":["feature","stdlib"],"priority":"normal","project":"SL","revision":10,"sequence":730,"stage":"queued","status":"open","title":"std.signal: signals as suspending reactor events; graceful shutdown rides cancellation (no atexit registry)","updated":"1789235605"},"before":{"assignee":"agent:claude-sl204","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"RULINGS (user, Sep 12, via the design-272 decision list): D3-1 APPROVED — Signal = {Terminate, Interrupt, Hangup, Quit, User1, User2, WindowChanged}; SIGCHLD OUT (load-bearing: waitpid reaping depends on its default disposition). D3-3 APPROVED — SIGPIPE is suppressed PER SOCKET (SO_NOSIGPIPE macOS / MSG_NOSIGNAL Linux via unit 2's option op); EPIPE surfaces as an ordinary IoError Result; no process-wide disposition change; not a Signal case. Lead-endorsed as determined: D3-2 (double-watch errors; fan-out = one watch + a Channel), D3-4 (ordinary io park, budget reset, anyio covers the quiescent walk), D3-5 (reuse IoError), D3-6 (next() -> Result<Void, IoError>, coalescing documented).","created":"1789214589","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-228.p1 (revision 1): design 272 unit 3: std.signal — signals as suspending reactor events, graceful shutdown rides cancellation, no ABI change (SL-228)","created":"1789230899","id":"c2"},{"author":"agent:claude-sawlang","body":"SL-228.p1 SUBMITTED (design 272 unit 3, delta of trio commit a9d809a5, applies clean on merged units 1+2), server gate queued. Per rulings D3-1..6: Signal = {Terminate, Interrupt, Hangup, Quit, User1, User2, WindowChanged} (SIGCHLD out — waitpid reaping is load-bearing on its default disposition); watch(sig) -> Result handle, next() SUSPENDS on the reactor like a socket read; double-watch errors (fan-out = one watch + a Channel); next() -> Result<Void, IoError>, coalescing documented; NO exit registry — the K100 pattern is watch -> cancel -> deinits -> main returns; process.exit stays the no-cleanup hatch. SIGPIPE deliberately NOT a Signal case (per-socket suppression landed in unit 2, argued in the module header). MECHANISM NOTE for the reviewer: departs from the issue's kqueue/signalfd sketch for a load-bearing reason — signalfd requires the signal blocked in EVERY thread and a pre-existing thread cannot be retro-masked (a process-directed signal delivered there takes the default action and kills the process); delivery instead rides a self-pipe read end the EXISTING (fd, write, token) reactor seam already carries — NO frozen-seam change, argument recorded in rt/ABI.md. Handler-side send uses Signal.send_to_self() (a METHOD, which mangles) after SL-274's bare-symbol hazard bit a std free func named raise. Conformance K98-K100. Trio-wide terminal battery was 27/27 at the branch tip.","created":"1789230910","id":"c3"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-228.p1 (revision 1)\n\nRequest changes on revision 1 (base 78d328de, applied 178c64e8): two independently reproduced concurrency bugs in the signal bridge.\n\n1. [P1] Serialize watch ownership and lifecycle (`sawc/rt/shim.c:400–428`). The lazy initialization, already-watched check, descriptor publication and sigaction installation are unsynchronized. Two threads can both pass the check and both return a successful watch for one signal, violating AlreadyExists and the single-owner contract. Their descriptor and saved-disposition writes overwrite one another; one pipe becomes unreachable, and a saved “previous” disposition can be Saw's own handler. Dropping either handle can then invalidate the other or restore the wrong disposition. `volatile sig_atomic_t` does not serialize this transaction.\n\n   Reproduced with the exact patched shim: preinitialize with a different signal, then gate two callers at pipe() so both have passed the availability check before either publishes. Both SIGUSR1 watches succeed (`watch results: 5 3`) instead of exactly one returning -2. Harness: `/Users/swoodtke/Projects/sl228-review-r1/.build/scratch/watch_race.c`. Initialization, acquisition and teardown need coordinated synchronization, with a concurrent same-signal regression test.\n\n2. [P1] Quiesce in-flight handlers before reclaiming their pipe (`sawc/rt/shim.c:444–450`, handler fd load/write at 360–363). Restoring the disposition prevents future handler entries but does not wait for a handler already running on another thread. Such a handler can have loaded the old write descriptor before unwatch clears the slot and closes the pipe. If another pipe or file reuses that number, the delayed handler writes its notification byte into the unrelated resource. A mutex around watch/unwatch alone will not fix this handler-versus-close race.\n\n   Deterministic reproduction against the patched source: a scheduling hook pauses the actual signal handler immediately before write(), after it has loaded the descriptor; another thread unwatches and opens an unrelated pipe that reuses the descriptors; resume the handler. Output: `unrelated pipe received 1 byte, value=1`. Harness: `/Users/swoodtke/Projects/sl228-review-r1/.build/scratch/signal_close_race.c` (build with `clang -std=c11 -pthread -Wl,-dead_strip .build/scratch/signal_close_race.c -o .build/scratch/signal_close_race`). Please establish a signal-safe reclamation protocol and pin teardown concurrent with delivery and descriptor reuse.\n\nValidation: all four focused K98/K99/K100/hosted-only fixtures pass, as do the ABI documentation, citation and diff checks. These passing fixtures do not cover concurrent acquisition or in-flight delivery during teardown. Server tests were still pending when the review was prepared.\n","created":"1789232871","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-228.p1 (revision 1)","created":"1789234160","id":"c5"},{"author":"agent:claude-sawlang","body":"Revised patch SL-228.p1 to revision 2","created":"1789235202","id":"c6"},{"author":"agent:claude-sawlang","body":"SL-228.p1 r2 SUBMITTED (fix round for review r1's two shim P1s; commit 2e8a9859 as the unit-3 delta). P1a: one pthread_mutex spans the whole watch/unwatch transaction — the HANDLER stays lock-free by necessity (it can interrupt a thread inside the mutex; the constraint is now written at the top of the block). P1b: reclamation made IMPOSSIBLE rather than synchronized — the pipe is created at first watch and lives for the process; unwatch restores disposition and drains, closes nothing (two fds per watched signal, 14 worst case, 0 unwatched). The token-validation alternative was REJECTED on its own terms: a handler that validates-then-writes is the same TOCTOU in a smaller font; its useful half survives inverted — each watch bumps a generation, the handler STAMPS its byte, and the DRAIN (reader-side, race-free) validates, so a stale byte from a previous watch can never fake a delivery into a shutdown path. Both races reproduced pre-fix with independently-written harnesses landing on the reviewer's exact numbers (results 5 3; unrelated pipe received value=1, fds 3/4 reused); pinned by tools/signal_race_probe.c linking the REAL shim, run via examples/signal_concurrency_pins.saw in the ordinary suite — three cells fail against the pre-fix shim, all pass now. Obligation-4 sweep cleared every other shim transaction with reasons (the offload thunk shares the publish-then-write shape and is safe only via pthread_join-before-close — the contrast argued in place); filed SL-276 (thread_spawn discards pthread_create failure — different class). Brief mechanism section + rt/ABI.md rewritten to the shipped self-pipe design. Re-ran: suite 2503/11x, freestanding 36/36, abidoc exact, citations — full battery not re-run (no compiler code in the delta; r1's 27/27 covered unit 3's shape), stated per the mandate.","created":"1789235219","id":"c7"}],"created":"1789050299","description":"MOTIVATION: a hosted Saw process cannot intercept SIGTERM/SIGINT — the sawtracker server dies with no flush, no socket drain, and stale lockfiles. The C idioms (signal(2) callbacks, atexit) fight deterministic destruction, the cooperative runtime, and never-hide-errors.\n\nRULED DESIGN (user, Sep 10 2026):\n1. SURFACE = SUSPENDING WATCH, no callbacks anywhere: hosted-only std.signal; watch(.terminate) returns a watch handle (Result — registration can fail; never hide errors) whose next() SUSPENDS until delivery, parking the task on the reactor exactly like a socket read. Watching a signal replaces its default disposition; unwatched signals keep OS behavior; SIGKILL/SIGSTOP are inexpressible.\n2. NO EXIT REGISTRY: cleanup rides cancellation + deinits. The pattern is a shutdown task that watches the signal, cancels the rest of the work (cancel already wakes io-parked tasks, design 102), values deinit deterministically as tasks unwind, main returns normally. process.exit(code) is documented as the no-cleanup escape hatch (no unwinding mid-flight).\n3. SCHEDULING: paired with SL-204 (io_wait deadline) as the server-hardening pair at the queue tail, behind the current soundness pipeline.\n\nMECHANISM SKETCH: both hosts integrate natively with the design-117 instance-based Saw reactor — kqueue EVFILT_SIGNAL (macOS; set SIG_IGN so the default action never fires) and signalfd (Linux; pthread_sigmask block, including at Thread/TaskGroup worker spawn — shim change). shim.c grows sigaction/sigprocmask-family wrappers (FFI-blocked bodies). Check whether any __saw_rt_* seam addition is needed or the reactor side suffices — rt/ABI.md is frozen, abidoc lane gates any change.\n\nBRIEF-TIME SCOPING (open, for the designs/NN brief at dispatch): the curated Signal enum membership + spellings (no-abbreviations doctrine applies); double-watch policy (error at second watch vs broadcast); whether the hosted runtime should default-ignore SIGPIPE for servers; interaction with op-budget fairness while a watcher task parks; freestanding simply lacks the module (like std.net). Obligation-2 consumer sweep: who relies on default signal dispositions today (test_runner child processes, blade, the QEMU harnesses).","id":"SL-228","labels":["feature","stdlib"],"priority":"normal","project":"SL","revision":9,"sequence":729,"stage":"queued","status":"open","title":"std.signal: signals as suspending reactor events; graceful shutdown rides cancellation (no atexit registry)","updated":"1789235219"},"id":"SL-228","project":"SL","request":"{\"action\":\"patch_test_result\",\"actor\":\"agent:sawtracker-tests\",\"commit\":\"0d1200b2\",\"id\":\"SL-228.p1\",\"output\":\"[2482/2514] ✓ vector_with_ref_oob_panic\\n[2483/2514] ✓ while_let_tuple_over_suspension_refused\\n[2484/2514] ✓ while_never_freestanding\\n(2505/2514) · while_nested\\n[2485/2514] ✓ vector_with_var_ref_oob_panic\\n[2486/2514] ✓ version_semver_sort\\n(2506/2514) · while_never_diverges\\n[2487/2514] ✓ vis258_private_is_an_ordinary_identifier\\n(2507/2514) · while_never_break_forms\\n[2488/2514] ✓ vis258_field_inherits_public\\n(2508/2514) · while_let_suspending_mt\\n(2509/2514) · while_let_suspending_try_catch\\n(2510/2514) · while_simple\\n(2511/2514) · wrapping_operators\\n(2512/2514) · while_let_suspending\\n(2513/2514) · wrapping_and_reference_mix\\n[2489/2514] ✓ vis80_public_members_ok\\n[2490/2514] ✓ vis80_same_module_ok\\n[2491/2514] ✓ visibility_package_access\\n(2514/2514) · while_never_sync_context\\n[2492/2514] ✓ visibility_package\\n[2493/2514] ✓ visibility_parent\\n[2494/2514] ✓ visibility_parent_public_works\\n[2495/2514] ✓ void_match_in_closure\\n[2496/2514] ✓ visibility_public\\n\\nCompiles complete in 395.3s (0/2514 reused, 0%) — 930 settled without running, 1584 binaries queued for execution (1566 of them already run).\\n\\n[2497/2514] ✓ while_break_continue\\n[2498/2514] ✓ while_expr_conditional_found\\n[2499/2514] ✓ void_if_chain_tail\\n[2500/2514] ✓ while_expr_infinite\\n[2501/2514] ✓ while_expr_conditional_not_found\\n[2502/2514] ✓ while_infinite\\n[2503/2514] ✓ while_let_drain\\n[2504/2514] ✓ while_let_channel_drain\\n[2505/2514] ✓ while_nested\\n[2506/2514] ✓ while_never_diverges\\n[2507/2514] ✓ while_never_break_forms\\n[2508/2514] ✓ while_let_suspending_mt\\n[2509/2514] ✓ while_let_suspending_try_catch\\n[2510/2514] ✓ while_simple\\n[2511/2514] ✓ wrapping_operators\\n[2512/2514] ✓ while_let_suspending\\n[2513/2514] ✓ wrapping_and_reference_mix\\n[2514/2514] ✓ while_never_sync_context\\n\\ncompile 395.3s + 4.4s draining = 399.6s total\\n\\n======================================================================\\nTest Results\\n======================================================================\\n\\nFAILED TESTS:\\n\\n  ✗ signal_concurrency_pins\\n    Output should contain 'signal race probe: all cells hold' — missing\\n    Got:\\n    BUILD FAILED (exit 1):\\n    ld: open() failed, errno=2 (No such file or directory) for '/Users/shawn/Projects/sawtracker-production/data/worktrees/SL-228.p1.test/.build/scratch/signal_race_probe_suite'\\n    clang: error: linker command failed with exit code 1 (use -v to see invocation)\\n    \\n    \\n\\nKNOWN FAILURES (xfail):\\n  x V96_driven_capture_duplicates_once: SL-267 — a driven body's NON-ESCAPING closure capture duplicates\\n  x closure_escaping_move_capture_consumed_once: DF-255a an escaping closure's env destructor releases a capture the\\n  x closure_nested_ref_param_capture: DF-248b a nested closure captures an enclosing closure's reference\\n  x closure_shorthand_parameter_inside_a_try: DF-257d the `$0` shorthand is invisible to the implicit-parameter scan\\n  x coro_suspending_call_in_inline_catch: SL-215 the inline `try EXPR catch { }` catch block does not CFG-split,\\n  x coro_value_inline_catch_hosts_a_move: SL-231 a value-carrying INLINE catch cannot host a `move` of a frame\\n  x funcpointer_call_in_a_driven_body: DF-252a a `FuncPointer` value called by NAME inside a driven body is\\n  x generic_init_moved_parameter_is_released_once: DF-302b — a generic extension's `init` releases a parameter it MOVED\\n  x match_on_suspending_call_in_a_loop: SL-273 — a match arm PAYLOAD BINDING is lost when a SUSPENSION\\n  x primitive_type_called_as_a_function: DF-270e — a PRIMITIVE TYPE NAME written as a ZERO-ARGUMENT call is an\\n  x trailing_closure_inside_a_try_operand: DF-259c — a trailing closure is not recognized inside a `try`/`try!`/\\n\\n======================================================================\\nSOME TESTS FAILED (2502 passed, 11 xfailed, 1 failed)\\n======================================================================\\n\\nPublished test_runner_1789235204_36739 as test_runner_last (1542 manifest entries).\\n\",\"request_id\":\"tests-0d1200b2-r2-e896\",\"revision\":2,\"tests\":\"failed\"}","request_id":"tests-0d1200b2-r2-e896","sequence":730,"timestamp":"1789235605"}
---
