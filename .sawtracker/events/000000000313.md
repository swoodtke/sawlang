---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-225.p1 (revision 1): test_runner: gate the runner's ANSI decoration on stdout being a terminal","created":"1789005941","id":"c1"}],"created":"1788993737","description":"The runner's own decoration (Colors class: progress symbols, section headers, verdict line) renders unconditional ANSI, so piped/captured output — the server's patch-test capture, redirected local runs, CI logs — is full of escape sequences. Fix: detect with sys.stdout.isatty() at startup and define the Colors constants as empty strings when stdout is not a terminal; honor NO_COLOR (force off) and FORCE_COLOR (force on) as overrides. SCOPE CARVE-OUT: compile_saw_in_process's documented invariant stays — captured COMPILER diagnostics (the reporter's spans, the ICE line at test_runner.py:626) remain byte-identical to the CLI subprocess so examples/errors assertion fidelity is untouched; only the runner's own decoration is gated. No consumer parses the colored strings (battery.sh and build.sh read exit codes).","id":"SL-225","labels":["tooling"],"priority":"normal","project":"SL","revision":2,"sequence":313,"stage":"backlog","status":"open","title":"test_runner.py: gate ANSI colors on stdout being a terminal (plain text when piped/captured)","updated":"1789005941"},"before":{"assignee":"","author":"agent:claude-sawlang","comments":[],"created":"1788993737","description":"The runner's own decoration (Colors class: progress symbols, section headers, verdict line) renders unconditional ANSI, so piped/captured output — the server's patch-test capture, redirected local runs, CI logs — is full of escape sequences. Fix: detect with sys.stdout.isatty() at startup and define the Colors constants as empty strings when stdout is not a terminal; honor NO_COLOR (force off) and FORCE_COLOR (force on) as overrides. SCOPE CARVE-OUT: compile_saw_in_process's documented invariant stays — captured COMPILER diagnostics (the reporter's spans, the ICE line at test_runner.py:626) remain byte-identical to the CLI subprocess so examples/errors assertion fidelity is untouched; only the runner's own decoration is gated. No consumer parses the colored strings (battery.sh and build.sh read exit codes).","id":"SL-225","labels":["tooling"],"priority":"normal","project":"SL","revision":1,"sequence":307,"stage":"backlog","status":"open","title":"test_runner.py: gate ANSI colors on stdout being a terminal (plain text when piped/captured)","updated":"1788993737"},"id":"SL-225","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/test_runner.py b/test_runner.py\\nindex c6ad7404..d61b38fe 100755\\n--- a/test_runner.py\\n+++ b/test_runner.py\\n@@ -207,15 +207,40 @@ class TestCase:\\n         return self.out_name or self.name\\n \\n \\n+def _colors_enabled() -> bool:\\n+    \\\"\\\"\\\"SL-225: the runner's own decoration colors only a terminal.\\n+\\n+    Piped or captured output — the sawtracker server's patch-test capture, a\\n+    redirected local run, CI logs — gets plain text: every `Colors` constant\\n+    becomes the empty string, and everything built from them (the status\\n+    symbols, headers, the verdict line) degrades with no per-site changes.\\n+    `NO_COLOR` (any value, per the no-color.org convention) forces plain even\\n+    on a terminal; `FORCE_COLOR` forces color even piped, which is the escape\\n+    hatch for a pager or a color-preserving capture.\\n+\\n+    This gates the RUNNER's decoration only. Captured COMPILER diagnostics are\\n+    deliberately untouched — `compile_saw_in_process` documents the invariant\\n+    that a test's captured stderr is byte-identical to what the CLI subprocess\\n+    emits (assertion fidelity for examples/errors), and the ICE line beside it\\n+    mimics the CLI for the same reason.\\n+    \\\"\\\"\\\"\\n+    if os.environ.get('FORCE_COLOR'):\\n+        return True\\n+    if os.environ.get('NO_COLOR') is not None:\\n+        return False\\n+    return sys.stdout.isatty()\\n+\\n+\\n class Colors:\\n-    \\\"\\\"\\\"ANSI color codes\\\"\\\"\\\"\\n-    GREEN = '\\\\033[92m'\\n-    RED = '\\\\033[91m'\\n-    YELLOW = '\\\\033[93m'\\n-    BLUE = '\\\\033[94m'\\n-    BOLD = '\\\\033[1m'\\n-    DIM = '\\\\033[2m'\\n-    RESET = '\\\\033[0m'\\n+    \\\"\\\"\\\"ANSI color codes — empty strings when stdout is not a terminal.\\\"\\\"\\\"\\n+    _ON = _colors_enabled()\\n+    GREEN = '\\\\033[92m' if _ON else ''\\n+    RED = '\\\\033[91m' if _ON else ''\\n+    YELLOW = '\\\\033[93m' if _ON else ''\\n+    BLUE = '\\\\033[94m' if _ON else ''\\n+    BOLD = '\\\\033[1m' if _ON else ''\\n+    DIM = '\\\\033[2m' if _ON else ''\\n+    RESET = '\\\\033[0m' if _ON else ''\\n \\n \\n STATUS_SYMBOLS = {\\n\",\"id\":\"SL-225\",\"request_id\":\"pid-63256-1789005940-6000\",\"title\":\"test_runner: gate the runner's ANSI decoration on stdout being a terminal\"}","request_id":"pid-63256-1789005940-6000","sequence":313,"timestamp":"1789005941"}
---
