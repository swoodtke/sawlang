---
{"action":"create","actor":"agent:codex","after":{"assignee":"","author":"agent:codex","comments":[],"created":"1789438296","description":"Found by SL-260's same-source differential at base 6f307c11, independently reproduced at 6a6d57bd. Python sawc produces an internal compiler error when a String? argument reaches a String?? parameter.\n\nMinimal repro:\n\n```saw\nfunc echo(value: String??) {}\nfunc main() {\n    let inner: String? = None\n    echo(inner)\n}\n```\n\nActual: compiler exit 1, `internal compiler error ... (FunctionCall): Type of #1 arg mismatch: {i1, {i1, i8*}} != {i1, i8*}`. Existing full fixture: prototypes/minivm/examples/optionals/review_nested_context.saw:15.\n\nExpected: a clean type diagnostic under LANGUAGE_SPEC.md's Call-site optional auto-wrap rule: an already-optional argument is passed through, never rewrapped. This report does not ask Python to imitate the prototype's broader recursive Optional wrapping (M10 deliberately permits that); the compiler must not pass incompatible layouts to codegen.\n\nMechanism: expressions.py::_arg_type_ok deliberately takes the ordinary compatibility path without setting an optional-wrap annotation when the argument is already Optional. types.py::_types_compatible recursively compares Optional inner types and permits an implicit String-to-String? match one level down. It therefore reports the outer String?/String?? pair compatible although the argument has no matching representation conversion. The call reaches LLVM with different aggregate layouts.\n\nSweep the shared compatibility/argument funnel across free/method/constructor/default/collection positions and Optional nesting depths. An independently observed annotated local `let outer: String?? = inner` also reaches an internal error (`Can't index at [0] in i8*`), so inspect storage positions too rather than patching the one call emitter. Preserve exact-depth Optional calls and the specified bare-value one-level wrap. SL-264 concerns outer-None value-branch semantics and is adjacent but distinct.\n","id":"SL-290","labels":["bug","typechecking"],"priority":"normal","project":"SL","revision":1,"sequence":843,"stage":"backlog","status":"open","title":"sawc: nested Optional argument compatibility reaches incompatible LLVM layouts","updated":"1789438296"},"before":null,"id":"SL-290","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex\",\"description\":\"Found by SL-260's same-source differential at base 6f307c11, independently reproduced at 6a6d57bd. Python sawc produces an internal compiler error when a String? argument reaches a String?? parameter.\\n\\nMinimal repro:\\n\\n```saw\\nfunc echo(value: String??) {}\\nfunc main() {\\n    let inner: String? = None\\n    echo(inner)\\n}\\n```\\n\\nActual: compiler exit 1, `internal compiler error ... (FunctionCall): Type of #1 arg mismatch: {i1, {i1, i8*}} != {i1, i8*}`. Existing full fixture: prototypes/minivm/examples/optionals/review_nested_context.saw:15.\\n\\nExpected: a clean type diagnostic under LANGUAGE_SPEC.md's Call-site optional auto-wrap rule: an already-optional argument is passed through, never rewrapped. This report does not ask Python to imitate the prototype's broader recursive Optional wrapping (M10 deliberately permits that); the compiler must not pass incompatible layouts to codegen.\\n\\nMechanism: expressions.py::_arg_type_ok deliberately takes the ordinary compatibility path without setting an optional-wrap annotation when the argument is already Optional. types.py::_types_compatible recursively compares Optional inner types and permits an implicit String-to-String? match one level down. It therefore reports the outer String?/String?? pair compatible although the argument has no matching representation conversion. The call reaches LLVM with different aggregate layouts.\\n\\nSweep the shared compatibility/argument funnel across free/method/constructor/default/collection positions and Optional nesting depths. An independently observed annotated local `let outer: String?? = inner` also reaches an internal error (`Can't index at [0] in i8*`), so inspect storage positions too rather than patching the one call emitter. Preserve exact-depth Optional calls and the specified bare-value one-level wrap. SL-264 concerns outer-None value-branch semantics and is adjacent but distinct.\\n\",\"labels\":[\"bug\",\"typechecking\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"codex-m17-python-nested-optional-20260914\",\"title\":\"sawc: nested Optional argument compatibility reaches incompatible LLVM layouts\"}","request_id":"codex-m17-python-nested-optional-20260914","sequence":843,"timestamp":"1789438296"}
---
