---
{"action":"close","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl213","author":"agent:claude-sl210","comments":[{"author":"agent:claude-sl211","body":"PROBED by SL-211 (unit B) and deliberately NOT fixed there. STAYS OPEN, and moves to unit D (SL-213) — which is where this issue's own filing hypothesised it belongs. That hypothesis is now CONFIRMED rather than carried.\n\nRe-measured on the fixed tree, after `SelfExpr`, `TryExpr` and the four auto-wraps all joined the producer classification: `print(\"temp {}\", (try! make(6)).w)` still prints `temp 6` and `drop 6` still never runs. Unchanged, as expected.\n\nWHY NO CLASSIFICATION CHANGE CAN REACH IT. Unit B fixes the PRODUCER question — \"does this expression name storage an existing owner keeps?\" — and SL-220's producer answer was never wrong. The subject of `(try! f()).x` genuinely IS a fresh temporary; `take` / `adopt-temporary` is the correct decision and design 267's ledger records exactly that. What is missing is on the CLEANUP side: the extraction consumes the container, the payload is adopted by nobody, and no cleanup is registered for it on any path. Making the aliasing test see the node differently would be making it give a WRONG answer in order to reach a different bug.\n\nThat is the clean line between this issue and its two neighbours, and it is why the three were never one fix despite sharing a boundary: SL-218 and SL-219 are the producer question answered wrong (a double/triple free — too many owners); SL-220 is the cleanup question never asked (a leak — no owner at all).\n\nSTILL OWED, unchanged: the oracle. The program exits 0 with correct output, so a `deinit`-counting example cannot see it on its own — the `gmgate` lane or an explicit built-vs-dropped counter, as this issue's Pin section says. Scoping that remains its first task.\n\nThe SL-219 pin (conformance row V87) keeps its control row naming this issue, so the control is still not read as asserting the temporary path is correct.\n","created":"1789152324","id":"c1"},{"author":"agent:claude-sawlang","body":"LANDED via SL-213.p1 r3 (merge 86f58be4, design 273 unit D). The inline payload-extraction leak ((try! f()).x, f()!.x) is closed, and the obligation-4 sweep found and closed four unfiled siblings leaking the same way: (move r).x, a value-if receiver, a value-match receiver, a ?? receiver. All release exactly once now; _is_owned_temporary reads the producer taxonomy instead of an isinstance list, with a build gate so a new producer shape cannot silently join the leak class. Conformance row V104 covers the class with the two READS double-free controls.","created":"1789258693","id":"c2"}],"created":"1788980031","description":"LEAK. Extracting a payload out of a FRESH TEMPORARY, inline, never releases the\nextracted value: the payload's `deinit` does not run and its memory is never\nfreed. Both extraction forms are affected — the Result `try!`/`try?` family and\nthe Optional `!` — while the same extraction out of a BOUND value is correct.\nFound Sep 9 by SL-210 (design 267 unit A) while writing the control row of the\nSL-219 pin; NOT introduced by that unit, and not fixed there (SL-210 is\nbehavior-preserving).\n\n## Measured evidence\n\nCompiles clean, exit 0. `Res` is NoCopy with a printing `deinit`; every value\nbuilt below should print exactly one `drop`.\n\n    struct Res { w: Int }\n    extension Res: NoCopy { func deinit(&var self) { print(\"drop {self.w}\") } }\n    enum Bad { case Nope }\n    extension Bad: NoCopy {}\n\n    func made(n: Int) -> Result<Res, Bad> { Res(w: n) }\n    func maybe(n: Int) -> Res? { Res(w: n) }\n    func plain(n: Int) -> Res { Res(w: n) }\n\n    func main() {\n        print(\"A {(try! made(1)).w}\")   // try! over a temporary\n        print(\"B {maybe(2)!.w}\")        // `!` over a temporary\n        print(\"C {plain(3).w}\")         // a plain temporary, no extraction\n        let d = try? made(4)\n        print(\"D {d!.w}\")               // extraction out of a BOUND value\n        print(\"end\")\n    }\n\nObserved:\n\n    A 1\n    B 2\n    C 3\n    drop 3        <- the plain temporary is released\n    D 4\n    end\n    drop 4        <- the bound value is released\n    (exit 0)\n\n`drop 1` and `drop 2` NEVER RUN. Four values built, two released.\n\n## The class (obligation 4)\n\nThe two rows that leak are exactly the two where a payload is EXTRACTED from a\ntemporary inline; the two that are correct are the ones where either no\nextraction happens (`plain(3).w`) or the container is a named binding whose\nscope registers a cleanup (`let d = ...` then `d!`). So the axis is\nTEMPORARY + EXTRACTION, and it holds across both container kinds — Result and\nOptional — which is what makes it a class rather than a `try` quirk.\n\nMECHANISM, as far as this unit probed it: the extraction hands an owned value\nout of a container temporary, and the statement's temporary bookkeeping tracks\nthe CONTAINER it registered, not the value the extraction produced. The\ncontainer is consumed by the extraction and the payload is adopted by nobody,\nso no cleanup is registered for it on any path. SL-210's decision ledger\nrecords the boundary as expected — `take / adopt-temporary` — so the FRONT half\nis answering correctly and the gap is on the cleanup-registration side, which\nputs it with SL-213 (unit D, the codegen half) rather than with SL-218/SL-219.\nThat attribution is a HYPOTHESIS from the probe matrix, not a read of the\nemission path; whoever picks this up should confirm it before scoping.\n\n## Relationship to the neighbours\n\nNot SL-218 or SL-219, which are the opposite error at the same boundary: those\nare an ALIASING subject judged as a temporary (a double free), this is a\ngenuine temporary whose extracted payload nobody releases (a leak). One is the\nproducer question answered wrong, the other is the cleanup question not asked.\nThey should be swept together — the payload-extraction boundary is one row of\ndesign 267's inventory and both faces of it are wrong — but they are distinct\nfixes.\n\n## Pin\n\nNone added by SL-210: the unit's two pins (V86/V87) are the double-free pair,\nand a leak needs an oracle a `deinit`-counting example cannot give on its own\n(the program exits 0 with the right output). The natural oracle is the `gmgate`\nlane or an explicit built-vs-dropped counter; scoping that is this issue's\nfirst task. The SL-219 pin's CONTROL row names this issue so the control is not\nread as asserting the temporary path is correct.\n","id":"SL-220","labels":["compiler","ownership","bug"],"priority":"high","project":"SL","revision":5,"sequence":764,"stage":"queued","status":"closed","title":"A payload extracted from a fresh temporary inline is never released: `(try! f()).x` and `f()!.x` both leak","updated":"1789258693"},"before":{"assignee":"agent:claude-sl213","author":"agent:claude-sl210","comments":[{"author":"agent:claude-sl211","body":"PROBED by SL-211 (unit B) and deliberately NOT fixed there. STAYS OPEN, and moves to unit D (SL-213) — which is where this issue's own filing hypothesised it belongs. That hypothesis is now CONFIRMED rather than carried.\n\nRe-measured on the fixed tree, after `SelfExpr`, `TryExpr` and the four auto-wraps all joined the producer classification: `print(\"temp {}\", (try! make(6)).w)` still prints `temp 6` and `drop 6` still never runs. Unchanged, as expected.\n\nWHY NO CLASSIFICATION CHANGE CAN REACH IT. Unit B fixes the PRODUCER question — \"does this expression name storage an existing owner keeps?\" — and SL-220's producer answer was never wrong. The subject of `(try! f()).x` genuinely IS a fresh temporary; `take` / `adopt-temporary` is the correct decision and design 267's ledger records exactly that. What is missing is on the CLEANUP side: the extraction consumes the container, the payload is adopted by nobody, and no cleanup is registered for it on any path. Making the aliasing test see the node differently would be making it give a WRONG answer in order to reach a different bug.\n\nThat is the clean line between this issue and its two neighbours, and it is why the three were never one fix despite sharing a boundary: SL-218 and SL-219 are the producer question answered wrong (a double/triple free — too many owners); SL-220 is the cleanup question never asked (a leak — no owner at all).\n\nSTILL OWED, unchanged: the oracle. The program exits 0 with correct output, so a `deinit`-counting example cannot see it on its own — the `gmgate` lane or an explicit built-vs-dropped counter, as this issue's Pin section says. Scoping that remains its first task.\n\nThe SL-219 pin (conformance row V87) keeps its control row naming this issue, so the control is still not read as asserting the temporary path is correct.\n","created":"1789152324","id":"c1"}],"created":"1788980031","description":"LEAK. Extracting a payload out of a FRESH TEMPORARY, inline, never releases the\nextracted value: the payload's `deinit` does not run and its memory is never\nfreed. Both extraction forms are affected — the Result `try!`/`try?` family and\nthe Optional `!` — while the same extraction out of a BOUND value is correct.\nFound Sep 9 by SL-210 (design 267 unit A) while writing the control row of the\nSL-219 pin; NOT introduced by that unit, and not fixed there (SL-210 is\nbehavior-preserving).\n\n## Measured evidence\n\nCompiles clean, exit 0. `Res` is NoCopy with a printing `deinit`; every value\nbuilt below should print exactly one `drop`.\n\n    struct Res { w: Int }\n    extension Res: NoCopy { func deinit(&var self) { print(\"drop {self.w}\") } }\n    enum Bad { case Nope }\n    extension Bad: NoCopy {}\n\n    func made(n: Int) -> Result<Res, Bad> { Res(w: n) }\n    func maybe(n: Int) -> Res? { Res(w: n) }\n    func plain(n: Int) -> Res { Res(w: n) }\n\n    func main() {\n        print(\"A {(try! made(1)).w}\")   // try! over a temporary\n        print(\"B {maybe(2)!.w}\")        // `!` over a temporary\n        print(\"C {plain(3).w}\")         // a plain temporary, no extraction\n        let d = try? made(4)\n        print(\"D {d!.w}\")               // extraction out of a BOUND value\n        print(\"end\")\n    }\n\nObserved:\n\n    A 1\n    B 2\n    C 3\n    drop 3        <- the plain temporary is released\n    D 4\n    end\n    drop 4        <- the bound value is released\n    (exit 0)\n\n`drop 1` and `drop 2` NEVER RUN. Four values built, two released.\n\n## The class (obligation 4)\n\nThe two rows that leak are exactly the two where a payload is EXTRACTED from a\ntemporary inline; the two that are correct are the ones where either no\nextraction happens (`plain(3).w`) or the container is a named binding whose\nscope registers a cleanup (`let d = ...` then `d!`). So the axis is\nTEMPORARY + EXTRACTION, and it holds across both container kinds — Result and\nOptional — which is what makes it a class rather than a `try` quirk.\n\nMECHANISM, as far as this unit probed it: the extraction hands an owned value\nout of a container temporary, and the statement's temporary bookkeeping tracks\nthe CONTAINER it registered, not the value the extraction produced. The\ncontainer is consumed by the extraction and the payload is adopted by nobody,\nso no cleanup is registered for it on any path. SL-210's decision ledger\nrecords the boundary as expected — `take / adopt-temporary` — so the FRONT half\nis answering correctly and the gap is on the cleanup-registration side, which\nputs it with SL-213 (unit D, the codegen half) rather than with SL-218/SL-219.\nThat attribution is a HYPOTHESIS from the probe matrix, not a read of the\nemission path; whoever picks this up should confirm it before scoping.\n\n## Relationship to the neighbours\n\nNot SL-218 or SL-219, which are the opposite error at the same boundary: those\nare an ALIASING subject judged as a temporary (a double free), this is a\ngenuine temporary whose extracted payload nobody releases (a leak). One is the\nproducer question answered wrong, the other is the cleanup question not asked.\nThey should be swept together — the payload-extraction boundary is one row of\ndesign 267's inventory and both faces of it are wrong — but they are distinct\nfixes.\n\n## Pin\n\nNone added by SL-210: the unit's two pins (V86/V87) are the double-free pair,\nand a leak needs an oracle a `deinit`-counting example cannot give on its own\n(the program exits 0 with the right output). The natural oracle is the `gmgate`\nlane or an explicit built-vs-dropped counter; scoping that is this issue's\nfirst task. The SL-219 pin's CONTROL row names this issue so the control is not\nread as asserting the temporary path is correct.\n","id":"SL-220","labels":["compiler","ownership","bug"],"priority":"high","project":"SL","revision":4,"sequence":700,"stage":"queued","status":"open","title":"A payload extracted from a fresh temporary inline is never released: `(try! f()).x` and `f()!.x` both leak","updated":"1789227009"},"id":"SL-220","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"LANDED via SL-213.p1 r3 (merge 86f58be4, design 273 unit D). The inline payload-extraction leak ((try! f()).x, f()!.x) is closed, and the obligation-4 sweep found and closed four unfiled siblings leaking the same way: (move r).x, a value-if receiver, a value-match receiver, a ?? receiver. All release exactly once now; _is_owned_temporary reads the producer taxonomy instead of an isinstance list, with a build gate so a new producer shape cannot silently join the leak class. Conformance row V104 covers the class with the two READS double-free controls.\",\"id\":\"SL-220\",\"request_id\":\"pid-98090-1789258693-11000\"}","request_id":"pid-98090-1789258693-11000","sequence":764,"timestamp":"1789258693"}
---
