---
{"action":"close","actor":"agent:claude-sl2-u0","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl2-u0","closed":"1790204085","comments":[{"author":"agent:claude-sl2-u0","body":"The `transferdecisions` battery lane fails on main. Reproduced on the MAIN\ncheckout at `41465de3` with no local changes, and again on `ef95c7af`:\n\n    ./.venv/bin/python tools/test_transfer_decisions.py\n\n    TRANSFER-DECISION GATE FAILED — the funnel has an exit that records nothing.\n\n      sawc/codegen/structs.py:98: `needs_copy` is assigned directly; every\n        retain stamp must go through `_stamp_retain`, which is what records the\n        obligation the preservation audit checks\n      sawc/codegen/structs.py:100: `payload_needs_copy` is assigned directly;\n        …same…\n\nBoth sites are in the block that copies an expression's transfer annotations\nonto a rewritten literal — `literal.needs_copy = expr.needs_copy` and\n`literal.payload_needs_copy = expr.payload_needs_copy`. Forwarding an existing\nstamp is not the same act as deciding one, but the gate reads the assignment\nand cannot tell them apart, so either the copy goes through `_stamp_retain` or\nthe gate learns to recognize a forward.\n\n`sawc/codegen/structs.py` was last changed by SL-350.p1 (destination\nmaterialization). Found Sep 23 by the SL-2 U0′ terminal battery, which touches\n`sawc/parser/`, `sawc/lexer.py`, `selfhost/lexer/` and `examples/` only — zero\ncodegen files — and whose other 31 stages pass. Filed rather than fixed:\ncodegen is outside that unit's surface and SL-355 has agents live there.\n","created":"1790204074","id":"c1"},{"author":"agent:claude-sl2-u0","body":"Duplicate of SL-371, which the lead filed for the same lane and the same two sites while this branch's battery was running. The reproduction here adds only that it is independent of design 274 U0': the U0' branch touches sawc/parser/, sawc/lexer.py, selfhost/lexer/ and examples/ and no codegen file, and the failure reproduces on the MAIN checkout at 41465de3 with no local changes.","created":"1790204085","id":"c2","kind":"landing"}],"created":"1790204074","description":"","id":"SL-372","labels":[],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":1376,"stage":"backlog","status":"closed","title":"The transferdecisions battery lane fails on main at sawc/codegen/structs.py","updated":"1790204085"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sl2-u0","closed":"","comments":[{"author":"agent:claude-sl2-u0","body":"The `transferdecisions` battery lane fails on main. Reproduced on the MAIN\ncheckout at `41465de3` with no local changes, and again on `ef95c7af`:\n\n    ./.venv/bin/python tools/test_transfer_decisions.py\n\n    TRANSFER-DECISION GATE FAILED — the funnel has an exit that records nothing.\n\n      sawc/codegen/structs.py:98: `needs_copy` is assigned directly; every\n        retain stamp must go through `_stamp_retain`, which is what records the\n        obligation the preservation audit checks\n      sawc/codegen/structs.py:100: `payload_needs_copy` is assigned directly;\n        …same…\n\nBoth sites are in the block that copies an expression's transfer annotations\nonto a rewritten literal — `literal.needs_copy = expr.needs_copy` and\n`literal.payload_needs_copy = expr.payload_needs_copy`. Forwarding an existing\nstamp is not the same act as deciding one, but the gate reads the assignment\nand cannot tell them apart, so either the copy goes through `_stamp_retain` or\nthe gate learns to recognize a forward.\n\n`sawc/codegen/structs.py` was last changed by SL-350.p1 (destination\nmaterialization). Found Sep 23 by the SL-2 U0′ terminal battery, which touches\n`sawc/parser/`, `sawc/lexer.py`, `selfhost/lexer/` and `examples/` only — zero\ncodegen files — and whose other 31 stages pass. Filed rather than fixed:\ncodegen is outside that unit's surface and SL-355 has agents live there.\n","created":"1790204074","id":"c1"}],"created":"1790204074","description":"","id":"SL-372","labels":[],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1375,"stage":"backlog","status":"open","title":"The transferdecisions battery lane fails on main at sawc/codegen/structs.py","updated":"1790204074"},"id":"SL-372","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sl2-u0\",\"body\":\"Duplicate of SL-371, which the lead filed for the same lane and the same two sites while this branch's battery was running. The reproduction here adds only that it is independent of design 274 U0': the U0' branch touches sawc/parser/, sawc/lexer.py, selfhost/lexer/ and examples/ and no codegen file, and the failure reproduces on the MAIN checkout at 41465de3 with no local changes.\",\"id\":\"SL-372\",\"request_id\":\"pid-65175-1790204085-18000\"}","request_id":"pid-65175-1790204085-18000","sequence":1376,"timestamp":"1790204085"}
---
