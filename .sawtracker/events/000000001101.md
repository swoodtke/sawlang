---
{"action":"close","actor":"agent:claude-sawlang","after":{"assignee":"","author":"agent:claude-sl280","comments":[{"author":"agent:claude-sawlang","body":"LANDED Sep 21 in SL-318.p6 (design 275 U2, apply 0c1e7325 / merge ded0605a). The third outcome is DELETED: `FrameLedger.unbuildable_callee` is read by the ONE rejector, so a call to a frameless suspending callee is a compile error carrying the ledger's recorded reason (a cycle names the recursive function and, since ruling 2, cites SL-322). In the ledger a callee that owes a frame and has none is `refused`, never `declined`. The runtime-facing half is totality check (b): after every resume body is generated, a walk asserts no `frame_boundary` callee survives as a plain call — free calls AND genuine methods via `ledger.method_target` (codex r1 P1-b) — as an ICE naming the frame and the callee. Rows K118-K121; `corototality` battery lane (`tools/test_coro_shapes.py`) injects the failure and requires the named ICE.\n","created":"1789993124","id":"c1"}],"created":"1789432839","description":"Split out of SL-280, where it is the FAILURE MODE rather than the cause. SL-280's cause is a keying mismatch that makes the coroutine transform decline to build a frame for one class of suspending callee. This issue is about what happens next, which is wrong independently of how that keying bug is fixed.\n\n## The guarantee\n\nDesigns 96 / 101 / 104: a suspending call embeds at any nesting depth and control-flow position, or errors cleanly — it never silently blocks. Today there is a third outcome.\n\n## What actually happens\n\nWhen the closure walk declines an edge, the callee subtree stays sync and its park lowers through the out-of-frame fallback at `sawc/codegen/calls.py:575-597`:\n\n    self.builder.call(self.functions[\"__saw_exec_io_register\"],\n                      [fd, direction, ir.Constant(self.int_type, 0)])\n    self.builder.call(self.functions[\"__saw_exec_park\"],\n                      [ir.Constant(self.int_type, -1), deadline])\n\nThat fallback is CORRECT for a genuinely sync caller with no executor to hand back to. Reached from inside a task, it registers the fd with a null token and then BLOCKS THE CALLING OS THREAD in the reactor — and that thread is the cooperative executor's. Every other task in the process stops, including the timer-bounded ones: measured on a server whose accept loop has its own `accept(timeout: Duration.secs(1))`, not one tick fired for the entire block. With an UNTIMED park the deadline argument is 0, i.e. `poll(-1)`, so the process is stopped until that single fd happens to become ready.\n\nThe non-io face is the same hole with a quieter symptom: a declined `yield_now()` is simply dropped (`__saw_drive_steps` reports 0 instead of 1), so a task that should cede never does, with nothing said at compile time or run time.\n\nNothing between the closure walk and codegen asks \"did a body that suspends actually get a frame?\". `_analyze_nesting`'s docstring already concedes the gap (`sawc/coro_transform.py:1775-1786`):\n\n    the closure walk follows an edge only when it can name and build a frame\n    for the target, so a suspending callee it declines to reach — a\n    cross-module free function the splice does not take, say — is in `nodes`\n    and not in `closure`.\n\n## Why this is worth closing separately\n\nSL-280's fix removes one reason the walk declines an edge. It does not add a reason for anyone to NOTICE the next one, and the evidence says there will be a next one: this same silent shape was design 96's bug (\"a callee whose only suspension source was a nested std method was miscompiled as a blocking call that wedged the thread\"), then SL-208/DF-300e's, and now SL-280's. Three instances of one missing check.\n\n## Possible shapes (for whoever takes it)\n\n* A compile-time refusal when a call site inside the driven closure reaches a callee whose node reports `suspends=True` and for which no frame was built. This is the honest one and matches the \"errors cleanly\" promise.\n* Failing that, a runtime detection in the park fallback: parking outside a frame while a cooperative executor is live in this process is a bug, and panicking beats stopping the world silently.\n\n## The carve-out that must not break\n\nThere is at least one program that RELIES on a suspending callee never being embedded — blade's own dependency resolver, per `coro_transform.py:1781-1784`: `visit` recurses and reaches `Command.run`, and blade compiles only because no `__Frame_visit` is ever built. A blanket refusal would refuse the package manager. So the check has to distinguish \"declined and that is fine\" from \"declined and a park is now unreachable from any frame\" — which is itself the useful design question here, and the reason this is not a one-line assert.\n\n## Evidence\n\nFull reproduction, the 12-row discovery matrix and the socket 2x2 are on SL-280.\n","id":"SL-287","labels":["bug","compiler","coroutine"],"priority":"high","project":"SL","revision":2,"sequence":1101,"stage":"backlog","status":"closed","title":"A suspending callee the coroutine transform declines to reach degrades silently — blocking the executor thread instead of erroring","updated":"1789993124"},"before":{"assignee":"","author":"agent:claude-sl280","comments":[],"created":"1789432839","description":"Split out of SL-280, where it is the FAILURE MODE rather than the cause. SL-280's cause is a keying mismatch that makes the coroutine transform decline to build a frame for one class of suspending callee. This issue is about what happens next, which is wrong independently of how that keying bug is fixed.\n\n## The guarantee\n\nDesigns 96 / 101 / 104: a suspending call embeds at any nesting depth and control-flow position, or errors cleanly — it never silently blocks. Today there is a third outcome.\n\n## What actually happens\n\nWhen the closure walk declines an edge, the callee subtree stays sync and its park lowers through the out-of-frame fallback at `sawc/codegen/calls.py:575-597`:\n\n    self.builder.call(self.functions[\"__saw_exec_io_register\"],\n                      [fd, direction, ir.Constant(self.int_type, 0)])\n    self.builder.call(self.functions[\"__saw_exec_park\"],\n                      [ir.Constant(self.int_type, -1), deadline])\n\nThat fallback is CORRECT for a genuinely sync caller with no executor to hand back to. Reached from inside a task, it registers the fd with a null token and then BLOCKS THE CALLING OS THREAD in the reactor — and that thread is the cooperative executor's. Every other task in the process stops, including the timer-bounded ones: measured on a server whose accept loop has its own `accept(timeout: Duration.secs(1))`, not one tick fired for the entire block. With an UNTIMED park the deadline argument is 0, i.e. `poll(-1)`, so the process is stopped until that single fd happens to become ready.\n\nThe non-io face is the same hole with a quieter symptom: a declined `yield_now()` is simply dropped (`__saw_drive_steps` reports 0 instead of 1), so a task that should cede never does, with nothing said at compile time or run time.\n\nNothing between the closure walk and codegen asks \"did a body that suspends actually get a frame?\". `_analyze_nesting`'s docstring already concedes the gap (`sawc/coro_transform.py:1775-1786`):\n\n    the closure walk follows an edge only when it can name and build a frame\n    for the target, so a suspending callee it declines to reach — a\n    cross-module free function the splice does not take, say — is in `nodes`\n    and not in `closure`.\n\n## Why this is worth closing separately\n\nSL-280's fix removes one reason the walk declines an edge. It does not add a reason for anyone to NOTICE the next one, and the evidence says there will be a next one: this same silent shape was design 96's bug (\"a callee whose only suspension source was a nested std method was miscompiled as a blocking call that wedged the thread\"), then SL-208/DF-300e's, and now SL-280's. Three instances of one missing check.\n\n## Possible shapes (for whoever takes it)\n\n* A compile-time refusal when a call site inside the driven closure reaches a callee whose node reports `suspends=True` and for which no frame was built. This is the honest one and matches the \"errors cleanly\" promise.\n* Failing that, a runtime detection in the park fallback: parking outside a frame while a cooperative executor is live in this process is a bug, and panicking beats stopping the world silently.\n\n## The carve-out that must not break\n\nThere is at least one program that RELIES on a suspending callee never being embedded — blade's own dependency resolver, per `coro_transform.py:1781-1784`: `visit` recurses and reaches `Command.run`, and blade compiles only because no `__Frame_visit` is ever built. A blanket refusal would refuse the package manager. So the check has to distinguish \"declined and that is fine\" from \"declined and a park is now unreachable from any frame\" — which is itself the useful design question here, and the reason this is not a one-line assert.\n\n## Evidence\n\nFull reproduction, the 12-row discovery matrix and the socket 2x2 are on SL-280.\n","id":"SL-287","labels":["bug","compiler","coroutine"],"priority":"high","project":"SL","revision":1,"sequence":825,"stage":"backlog","status":"open","title":"A suspending callee the coroutine transform declines to reach degrades silently — blocking the executor thread instead of erroring","updated":"1789432839"},"id":"SL-287","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"LANDED Sep 21 in SL-318.p6 (design 275 U2, apply 0c1e7325 / merge ded0605a). The third outcome is DELETED: `FrameLedger.unbuildable_callee` is read by the ONE rejector, so a call to a frameless suspending callee is a compile error carrying the ledger's recorded reason (a cycle names the recursive function and, since ruling 2, cites SL-322). In the ledger a callee that owes a frame and has none is `refused`, never `declined`. The runtime-facing half is totality check (b): after every resume body is generated, a walk asserts no `frame_boundary` callee survives as a plain call — free calls AND genuine methods via `ledger.method_target` (codex r1 P1-b) — as an ICE naming the frame and the callee. Rows K118-K121; `corototality` battery lane (`tools/test_coro_shapes.py`) injects the failure and requires the named ICE.\\n\",\"id\":\"SL-287\",\"request_id\":\"pid-72496-1789993124-10000\"}","request_id":"pid-72496-1789993124-10000","sequence":1101,"timestamp":"1789993124"}
---
