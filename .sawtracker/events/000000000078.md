---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791228","description":"## Scope and status\n\nDF-299d: Preserve explicit generic type arguments on qualified initializers\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/150-rust-style-imports.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/150-rust-style-imports.md)\n- [sawlang/designs/256-resolved-receiver-overloads.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/256-resolved-receiver-overloads.md)\n- [sawlang/designs/207-constructor-inference.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/207-constructor-inference.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 128–128\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L128-L128)\n\n- DF‑299d — a QUALIFIED generic `init` drops its explicit type argument: `mutex.Mutex<Int>(value: 5)` is ``argument `value` expects `T` but got `Int` `` while the selective-import spelling compiles (entry below, filed Sep 4 as an incidental of DF‑299c's census; PRE-EXISTING at HEAD). The CONSTRUCTOR position of design 150's \"a qualifier works everywhere a name appears\", which design 256 landed for methods and statics\n\n### sawlang TODO lines 640–663\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L640-L663)\n\n## DF‑299d — a QUALIFIED generic `init` drops its explicit type argument\n## (filed Sep 4 by DF‑299c's census as an incidental; PRE-EXISTING, present at\n## HEAD, unrelated to the closure defect)\n\n```saw\nimport std.mutex.{Mutex}\nimport std.mutex\n\nlet sel  = Mutex<Int>(value: 5)          // compiles and runs\nlet qual = mutex.Mutex<Int>(value: 5)    // error: argument `value` expects `T`\n                                         //        but got `Int`\n```\n\nThe qualifier spelling reaches the generic `init` with `T` still unbound, so the\nexplicit `<Int>` is dropped somewhere between the qualified head and the\nconstructor. Design 150 makes a qualifier work in EVERY position a name appears,\nand design 256 landed the qualified method/static call with its whole overload\nset — this is the CONSTRUCTOR position of the same rule, and it is the one still\nmissing. Neighbouring shapes to sweep before a fix: a qualified generic init at\nan INFERRED type argument (design 207's constructor inference), a qualified\ngeneric STATIC (`mod.Type<Int>.make(...)`), and a qualified generic enum\nconstruction. Workaround: the selective import.\n[150, 207, 256, DF‑247b]\n","id":"SL-78","labels":["todo-import","backlog","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":78,"status":"open","title":"DF-299d: Preserve explicit generic type arguments on qualified initializers","updated":"1788791228"},"before":null,"id":"SL-78","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-299d: Preserve explicit generic type arguments on qualified initializers\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/150-rust-style-imports.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/150-rust-style-imports.md)\\n- [sawlang/designs/256-resolved-receiver-overloads.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/256-resolved-receiver-overloads.md)\\n- [sawlang/designs/207-constructor-inference.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/207-constructor-inference.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 128–128\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L128-L128)\\n\\n- DF‑299d — a QUALIFIED generic `init` drops its explicit type argument: `mutex.Mutex<Int>(value: 5)` is ``argument `value` expects `T` but got `Int` `` while the selective-import spelling compiles (entry below, filed Sep 4 as an incidental of DF‑299c's census; PRE-EXISTING at HEAD). The CONSTRUCTOR position of design 150's \\\"a qualifier works everywhere a name appears\\\", which design 256 landed for methods and statics\\n\\n### sawlang TODO lines 640–663\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L640-L663)\\n\\n## DF‑299d — a QUALIFIED generic `init` drops its explicit type argument\\n## (filed Sep 4 by DF‑299c's census as an incidental; PRE-EXISTING, present at\\n## HEAD, unrelated to the closure defect)\\n\\n```saw\\nimport std.mutex.{Mutex}\\nimport std.mutex\\n\\nlet sel  = Mutex<Int>(value: 5)          // compiles and runs\\nlet qual = mutex.Mutex<Int>(value: 5)    // error: argument `value` expects `T`\\n                                         //        but got `Int`\\n```\\n\\nThe qualifier spelling reaches the generic `init` with `T` still unbound, so the\\nexplicit `<Int>` is dropped somewhere between the qualified head and the\\nconstructor. Design 150 makes a qualifier work in EVERY position a name appears,\\nand design 256 landed the qualified method/static call with its whole overload\\nset — this is the CONSTRUCTOR position of the same rule, and it is the one still\\nmissing. Neighbouring shapes to sweep before a fix: a qualified generic init at\\nan INFERRED type argument (design 207's constructor inference), a qualified\\ngeneric STATIC (`mod.Type<Int>.make(...)`), and a qualified generic enum\\nconstruction. Workaround: the selective import.\\n[150, 207, 256, DF‑247b]\\n\",\"labels\":[\"todo-import\",\"backlog\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-3b67a727b4d64e44c6cb-create\",\"title\":\"DF-299d: Preserve explicit generic type arguments on qualified initializers\"}","request_id":"todo-20260907-3b67a727b4d64e44c6cb-create","sequence":78,"timestamp":"1788791228"}
---
