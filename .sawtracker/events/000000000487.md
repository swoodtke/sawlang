---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-251.p1 (revision 1): minivm M8: owned Strings + refcounted heap (branch commit 9677cb7d)","created":"1789097774","id":"c1"}],"created":"1789086347","description":"SL-242 epic child, landing patch 9/14. len/is_empty/byte_at/substring/equals, temporary receivers, retain/release owning slots with post-run live-allocation check in BOTH engines. Contract: prototypes/minivm/M8_OWNED_STRINGS.md. Patch = branch commit 9677cb7d.","id":"SL-251","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":487,"stage":"queued","status":"open","title":"minivm M8: owned Strings — literals, copies, refs, intrinsics, refcounted heap + leak check","updated":"1789097774"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086347","description":"SL-242 epic child, landing patch 9/14. len/is_empty/byte_at/substring/equals, temporary receivers, retain/release owning slots with post-run live-allocation check in BOTH engines. Contract: prototypes/minivm/M8_OWNED_STRINGS.md. Patch = branch commit 9677cb7d.","id":"SL-251","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":413,"stage":"queued","status":"open","title":"minivm M8: owned Strings — literals, copies, refs, intrinsics, refcounted heap + leak check","updated":"1789086347"},"id":"SL-251","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 2057cc21..0dc1ba60 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -15,9 +15,10 @@ borrowing scalar fields of direct records. M7 adds whole-record references,\\n nested field forwarding and basic shared/mutable methods, tested with a\\n scalar-only `Lexer.advance`-shaped record (250 total cases). Strings, collections,\\n Optional/Result payloads, intrinsic generic types and module integration remain\\n-outstanding. Next is a bounded owned-String slice: literals, byte length/access,\\n-equality and substring, with explicit copy/drop rules before owning records and\\n-collections are enabled.\\n+outstanding. M8 adds owned String literals/locals/parameters/results/references,\\n+byte length/access, equality and substring with explicit copy/drop rules (277\\n+total cases). Next is String-valued records and String-pattern match, before\\n+Optional/Result payloads and collections are enabled.\\n The dependency groups below are an inventory, not the implementation commit order;\\n the numbered milestone designs define each isolated slice.\\n \\ndiff --git a/prototypes/minivm/M8_OWNED_STRINGS.md b/prototypes/minivm/M8_OWNED_STRINGS.md\\nnew file mode 100644\\nindex 00000000..f27e81d2\\n--- /dev/null\\n+++ b/prototypes/minivm/M8_OWNED_STRINGS.md\\n@@ -0,0 +1,138 @@\\n+# M8: owned String values\\n+\\n+Status: implemented after M7 (90cab6a1).\\n+\\n+## Source contract\\n+\\n+Accept String literals (decoded escapes, UTF-8, embedded NUL), locals,\\n+parameters/results, ordinary copies/reassignment, `&String` and `&var String`.\\n+Add `==`, `!=`, `print(String)`, and intrinsic methods `len()`, `is_empty()`,\\n+`byte_at(index: Int) -> Byte`, `substring(start: Int, end: Int) -> String`,\\n+`equals(other: String) -> Bool`. Positional arguments are also accepted, with\\n+optional exact labels as in M7. Intrinsics may operate on temporary String\\n+values and literals as well as named places. Existing user-record method\\n+temporary restrictions remain. No interpolation, concatenation, numeric casts,\\n+String statics, String fields in records, builders, vectors or payloads yet.\\n+\\n+String is immutable byte content with value semantics. Length and indices are\\n+bytes. byte_at requires 0 <= index < len; substring requires\\n+0 <= start <= end <= len and copies its half-open range, including empty ranges\\n+and raw byte slices through UTF-8. Errors are exactly\\n+`runtime error: string index out of range` and\\n+`runtime error: string range out of bounds`. Equality compares bytes, including\\n+embedded NUL. Print writes every byte followed by newline; never C-string printf.\\n+\\n+## Representation and ownership\\n+\\n+Add ValueType.String, one logical word. It is NOT scalar_type or integer_type.\\n+Add Program.strings: Vector<String> as literal pool; frontend interns literal\\n+content. Root value_layout(String) is [String], but record fields containing\\n+String reject until aggregate ownership is implemented. References may target\\n+String. Helper `string_type(t)` identifies exactly ValueType.String.\\n+\\n+Every String slot owns null or one strong reference. Null is an internal empty\\n+slot, not the source empty string. All String slots are initialized null.\\n+Copy, LoadRef, StoreRef and parameter initialization retain source BEFORE\\n+releasing destination, then store. Newly allocated strings transfer ownership\\n+into their destination (release old destination; no additional retain).\\n+Return retains its String result before frame cleanup, then transfers that owned\\n+result to the caller. Call adopts returned ownership without an extra retain.\\n+All other value and reference ABIs stay unchanged. String reference loads/stores\\n+operate on the String SLOT, not on the bytes/header behind its value.\\n+\\n+Add Op.Drop(a String slot): release its claim and clear to null; idempotent on\\n+null. At every normal block exit, drop String slots allocated within that block,\\n+including temporary slots, except a value-block result that must survive for its\\n+parent. Every function return additionally releases/clears ALL its String slots,\\n+after retaining the return result. Thus early returns and skipped branches are\\n+covered, and normal lexical cleanup cannot double-release at frame teardown.\\n+Loop body cleanup runs before its back edge. Slots reused in loops also release\\n+their previous value on replacement. No break/continue source support is added.\\n+\\n+The VM uses nonzero integer handles into an execution-owned String heap; 0 is\\n+null. Each entry holds bytes and a synthetic refcount. Reuse dead slots only for\\n+well-formed compiled programs (the verifier is not a hostile-bytecode loader);\\n+validate bounds/liveness before lookup and never expose handles in source. Last\\n+release clears the actual Saw String value. Execution-wide heap ownership also\\n+frees all values if a runtime error aborts execution; no objects survive execute.\\n+On successful execution check that no live heap entries remain.\\n+\\n+Native LLVM String values use ptr to runtime-owned header+bytes, never integer\\n+pointer casts. String slots are separate ptr allocas, like Ref slots; records\\n+remain String-free so their contiguous i64 data arrays stay valid. Header holds\\n+refcount and byte length; helpers allocate/copy bytes, retain/release, equality,\\n+length/index/substring/printing. Use handwritten LLVM runtime definitions in a\\n+new src/string_runtime.saw emitter module if that keeps llvm.saw focused. libc\\n+malloc/free/memcpy/memcmp/putchar are sufficient. Check allocation and length-add\\n+overflow; panic with `runtime error: string allocation failed`. Refcount overflow\\n+must not wrap. Native fatal errors terminate the process as existing runtime\\n+errors do; success-path main checks the runtime live allocation count is zero.\\n+Use the same stdio family for all prints, so mixed String/scalar output preserves\\n+order. LLVM pointer result/argument emission must include String and Ref, while\\n+ownership changes apply only to String.\\n+\\n+## Typed instructions\\n+\\n+| Op | Operands/result |\\n+| --- | --- |\\n+| StringConst | dst String, immediate valid Program.strings index; owned allocation |\\n+| StringLen | a String, dst I64 |\\n+| StringByteAt | a String, b I64, dst Byte |\\n+| StringSlice | a String, b I64 start, args exactly one I64 end slot, dst String |\\n+| Drop | a String slot; no result |\\n+\\n+Existing Eq/Ne accept String/String and compare contents. Existing Copy and\\n+Print admit String; Const still does not. Call/Return, Addr/LoadRef/StoreRef use\\n+the ownership behavior above. StringLen followed by integer equality to zero\\n+implements is_empty; Eq implements equals. All other operations keep existing\\n+restrictions. Verifier validates every literal index, exact operand types, slice\\n+argument count/slot, no String record fields and Drop only on String slots.\\n+\\n+## Frontend and validation details\\n+\\n+TokenKind.StringLit contains decoded bytes, with the lexer's 0x01 escape marker\\n+before escaped braces. This collides with actual U+0001 bytes followed by braces.\\n+Preserve raw source in Compiler and decode the validated original literal at its\\n+token line/column, using the lexer's UTF-8 continuation/column rules to locate it.\\n+Restore raw escapes directly, including Unicode scalars and escaped braces;\\n+never strip marker bytes from the ambiguous decoded token payload. InterpString\\n+remains a located rejection. No production compiler changes are authorized by\\n+this prototype milestone; the independently reproduced Python defect is filed.\\n+\\n+Named String intrinsic receivers use their own content snapshot before argument\\n+evaluation; their methods are read-only. Retain snapshots through nested argument\\n+calls and then release with lexical temporaries. This does not relax explicit\\n+borrow or pending-write rules for user functions. General value postfix dispatch\\n+must distinguish String intrinsics from named-place record methods.\\n+\\n+Primary owns model.saw, verify.saw, direct contracts, docs and integration gates.\\n+Sol frontend owns frontend.saw. Sol engines owns vm.saw, llvm.saw and optional\\n+string_runtime.saw. Sol tests owns test_minivm.py and examples/strings. Program\\n+constructor migration in existing direct contracts belongs to primary.\\n+\\n+Tests: empty/ASCII/Unicode/NUL/escapes, lengths/bytes/slices/equality, mixed print\\n+order, parameter/return/branch/recursive copies, self-assignment, shared/mutable\\n+String reference forwarding/replacement, temporary intrinsic calls, lexical\\n+cleanup and repeated loop allocations. Check valid boundaries and invalid\\n+negative/end/overflow indices. Reject interpolation, String arithmetic/casts,\\n+fields/statics and invalid intrinsic arguments. All prior cases stay green except\\n+the old blanket String rejection, explicitly replaced by positive String cases.\\n+Direct contracts verify typed op/index/ownership restrictions; native and VM\\n+live-allocation checks run for every successful string program. Compare selected\\n+source semantics with Python sawc and file independently established findings.\\n+\\n+## Completed validation\\n+\\n+277 integration cases pass through VM and clang O0/O2, including 28 isolated\\n+String cases and all retained prior cases. All six representation contracts\\n+compile and pass. AddressSanitizer probes for loop/value-branch cleanup and\\n+String references pass with exact output and no heap diagnostics. Both engines\\n+check that successful execution leaves no live String allocations.\\n+\\n+The raw-source literal decoder preserves U+0001 before braces, unlike the Python\\n+compiler's ambiguous marker replacement. The independently reproduced production\\n+defect is filed as SL-238. No production compiler or lexer files changed.\\n+\\n+Next: String fields in value records, preserving mixed pointer/data layouts and\\n+all ownership claims across aggregate copies/returns, plus String-pattern match\\n+for the unchanged keyword_kind and suffix_width lexer helpers.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 480b4944..71f7f6f5 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -22,7 +22,7 @@ clang .build/minivm/factorial.ll -o .build/minivm/factorial\\n .build/minivm/factorial\\n ```\\n \\n-The LLVM path targets a 64-bit Unix C ABI (`printf`, `puts`, `exit`); validation\\n+The LLVM path targets a 64-bit Unix C ABI (stdio, allocation and byte-copy routines); validation\\n uses Apple clang on arm64 macOS. Clang is an external validation/compilation step,\\n not a dependency of the interpreter or text emitter.\\n \\n@@ -66,7 +66,7 @@ shift counts. See [M1_NUMBERS.md](M1_NUMBERS.md) for the precise numeric contrac\\n Newlines separate statements. Omit the result annotation for Void functions;\\n `main()` must take no arguments and return Void. Value-returning functions need\\n a value on every continuing path, supplied by an explicit return or final expression.\\n-There are no imports, strings, collections, generics, or concurrency in the\\n+There are no imports, collections, generics, or concurrency in the\\n accepted subset.\\n Semicolons remain unsupported. Boolean `&&`/`||` short-circuit, mutable integers\\n and fields support `+=`/`-=`/`*=`/`/=`/`%=`, and module `static` integer/Bool\\n@@ -86,6 +86,14 @@ named locals, reference parameters or nested fields. Optional call labels must\\n match parameter names in order. Temporary receivers, overloads, static methods\\n and trait extensions remain unsupported. See [M7_RECORD_RECEIVERS.md](M7_RECORD_RECEIVERS.md).\\n \\n+Owned String values support literals, copies, locals, parameters/results,\\n+references, equality, printing, and `len`, `is_empty`, `byte_at`, `substring`,\\n+and `equals`. Byte offsets and embedded NUL are preserved. Intrinsic String\\n+methods also accept temporary receivers. String fields/statics, interpolation\\n+and concatenation remain unsupported. Both engines retain/release owning slots\\n+and check for live allocations after successful execution; see\\n+[M8_OWNED_STRINGS.md](M8_OWNED_STRINGS.md).\\n+\\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n only to VM execution; native code uses the host call stack and has no budget.\\n@@ -98,16 +106,17 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The first seven milestones\\n-pass all 250 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The first eight milestones\\n+pass all 277 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw`,\\n `tests/record_contract.saw`, `tests/enum_contract.saw`,\\n-`tests/reference_contract.saw`, and `tests/receiver_contract.saw`; build and run them\\n+`tests/reference_contract.saw`, `tests/receiver_contract.saw`, and\\n+`tests/string_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n `--section enums`, `--section values`, `--section references`, or\\n-`--section receivers` for an isolated\\n+`--section receivers` or `--section strings` for an isolated\\n integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\ndiff --git a/prototypes/minivm/examples/reject_string.saw b/prototypes/minivm/examples/reject_string.saw\\ndeleted file mode 100644\\nindex d2a13253..00000000\\n--- a/prototypes/minivm/examples/reject_string.saw\\n+++ /dev/null\\n@@ -1 +0,0 @@\\n-func main() { print(\\\"no\\\") }\\ndiff --git a/prototypes/minivm/examples/strings/brace_marker_provenance.saw b/prototypes/minivm/examples/strings/brace_marker_provenance.saw\\nnew file mode 100644\\nindex 00000000..522df8ed\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/brace_marker_provenance.saw\\n@@ -0,0 +1,17 @@\\n+func main() {\\n+    let escaped_left = \\\"\\\\{\\\"\\n+    let literal_left = \\\"\\\\u{1}\\\\u{7b}\\\"\\n+    print(escaped_left.len())\\n+    print(escaped_left.byte_at(0))\\n+    print(literal_left.len())\\n+    print(literal_left.byte_at(0))\\n+    print(literal_left.byte_at(1))\\n+\\n+    let escaped_right = \\\"\\\\}\\\"\\n+    let literal_right = \\\"\\\\u{1}\\\\u{7d}\\\"\\n+    print(escaped_right.len())\\n+    print(escaped_right.byte_at(0))\\n+    print(literal_right.len())\\n+    print(literal_right.byte_at(0))\\n+    print(literal_right.byte_at(1))\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/copies_calls_results.saw b/prototypes/minivm/examples/strings/copies_calls_results.saw\\nnew file mode 100644\\nindex 00000000..2a9761f2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/copies_calls_results.saw\\n@@ -0,0 +1,17 @@\\n+func identity(value: String) -> String { value }\\n+func choose(flag: Bool, left: String, right: String) -> String {\\n+    if flag { return left }\\n+    return right\\n+}\\n+\\n+func main() {\\n+    var first = \\\"alpha\\\"\\n+    let copy = first\\n+    print(first)\\n+    print(copy)\\n+    first = \\\"beta\\\"\\n+    first = first\\n+    print(first)\\n+    print(identity(first))\\n+    print(choose(false, \\\"left\\\", \\\"right\\\"))\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/index_at_end.saw b/prototypes/minivm/examples/strings/index_at_end.saw\\nnew file mode 100644\\nindex 00000000..c46d0953\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/index_at_end.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"a\\\".byte_at(1)) }\\ndiff --git a/prototypes/minivm/examples/strings/index_huge.saw b/prototypes/minivm/examples/strings/index_huge.saw\\nnew file mode 100644\\nindex 00000000..e578bf4b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/index_huge.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"a\\\".byte_at(9_223_372_036_854_775_807)) }\\ndiff --git a/prototypes/minivm/examples/strings/index_negative.saw b/prototypes/minivm/examples/strings/index_negative.saw\\nnew file mode 100644\\nindex 00000000..635b554d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/index_negative.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"a\\\".byte_at(-1)) }\\ndiff --git a/prototypes/minivm/examples/strings/intrinsics.saw b/prototypes/minivm/examples/strings/intrinsics.saw\\nnew file mode 100644\\nindex 00000000..ba15d232\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/intrinsics.saw\\n@@ -0,0 +1,20 @@\\n+func main() {\\n+    let empty = \\\"\\\"\\n+    let word = \\\"hello\\\"\\n+    let unicode = \\\"é🙂\\\"\\n+    print(empty.len())\\n+    print(empty.is_empty())\\n+    print(word.len())\\n+    print(word.is_empty())\\n+    print(word.byte_at(0))\\n+    print(word.byte_at(4))\\n+    print(word.byte_at(index: 1))\\n+    print(unicode.byte_at(1))\\n+    print(word.substring(0, 0))\\n+    print(word.substring(5, 5))\\n+    print(word.substring(start: 0, end: 5))\\n+    print(word == \\\"hello\\\")\\n+    print(word != \\\"world\\\")\\n+    print(\\\"a\\\\0b\\\" == \\\"a\\\\0b\\\")\\n+    print(\\\"a\\\\0b\\\" != \\\"a\\\")\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/literals_escapes.saw b/prototypes/minivm/examples/strings/literals_escapes.saw\\nnew file mode 100644\\nindex 00000000..054bff92\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/literals_escapes.saw\\n@@ -0,0 +1,8 @@\\n+func main() {\\n+    print(\\\"\\\")\\n+    print(\\\"ASCII\\\")\\n+    print(\\\"café🙂\\\")\\n+    print(\\\"a\\\\0b\\\")\\n+    print(\\\"\\\\{left\\\\} \\\\\\\\ right\\\")\\n+    print(\\\"\\\\u{1}x\\\")\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/range_huge.saw b/prototypes/minivm/examples/strings/range_huge.saw\\nnew file mode 100644\\nindex 00000000..afad1002\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/range_huge.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"abc\\\".substring(0, 9_223_372_036_854_775_807)) }\\ndiff --git a/prototypes/minivm/examples/strings/range_negative.saw b/prototypes/minivm/examples/strings/range_negative.saw\\nnew file mode 100644\\nindex 00000000..70a8c6ff\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/range_negative.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"abc\\\".substring(-1, 1)) }\\ndiff --git a/prototypes/minivm/examples/strings/range_past_end.saw b/prototypes/minivm/examples/strings/range_past_end.saw\\nnew file mode 100644\\nindex 00000000..4a0c2562\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/range_past_end.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"abc\\\".substring(0, 4)) }\\ndiff --git a/prototypes/minivm/examples/strings/range_reversed.saw b/prototypes/minivm/examples/strings/range_reversed.saw\\nnew file mode 100644\\nindex 00000000..1853bba3\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/range_reversed.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"abc\\\".substring(2, 1)) }\\ndiff --git a/prototypes/minivm/examples/strings/receiver_snapshot_order.saw b/prototypes/minivm/examples/strings/receiver_snapshot_order.saw\\nnew file mode 100644\\nindex 00000000..6bf53ddb\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/receiver_snapshot_order.saw\\n@@ -0,0 +1,10 @@\\n+func replace(value: &var String) -> String {\\n+    value = \\\"after\\\"\\n+    return \\\"before\\\"\\n+}\\n+\\n+func main() {\\n+    var value = \\\"before\\\"\\n+    print(value.equals(replace(&var value)))\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/recursive_results.saw b/prototypes/minivm/examples/strings/recursive_results.saw\\nnew file mode 100644\\nindex 00000000..d09545ef\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/recursive_results.saw\\n@@ -0,0 +1,10 @@\\n+func trim(value: String, count: Int) -> String {\\n+    if count == 0 { return value }\\n+    return trim(value.substring(1, value.len()), count - 1)\\n+}\\n+\\n+func main() {\\n+    let result = trim(\\\"abcdef\\\", 2)\\n+    print(result)\\n+    print(result.len())\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/references.saw b/prototypes/minivm/examples/strings/references.saw\\nnew file mode 100644\\nindex 00000000..09def71e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/references.saw\\n@@ -0,0 +1,15 @@\\n+func read(value: &String) -> String { value }\\n+func replace(value: &var String, next: String) { value = next }\\n+func forward(value: &var String) {\\n+    print(read(&value))\\n+    replace(&var value, \\\"after\\\")\\n+}\\n+\\n+func main() {\\n+    var value = \\\"before\\\"\\n+    let snapshot = read(&value)\\n+    forward(&var value)\\n+    print(snapshot)\\n+    print(value)\\n+    print(read(&value))\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/reject_arithmetic.saw b/prototypes/minivm/examples/strings/reject_arithmetic.saw\\nnew file mode 100644\\nindex 00000000..81862abf\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/reject_arithmetic.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"a\\\" + \\\"b\\\") }\\ndiff --git a/prototypes/minivm/examples/strings/reject_cast.saw b/prototypes/minivm/examples/strings/reject_cast.saw\\nnew file mode 100644\\nindex 00000000..7cfa80ed\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/reject_cast.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"1\\\" as Int) }\\ndiff --git a/prototypes/minivm/examples/strings/reject_interpolation.saw b/prototypes/minivm/examples/strings/reject_interpolation.saw\\nnew file mode 100644\\nindex 00000000..c962cc29\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/reject_interpolation.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let name = \\\"world\\\"\\n+    print(\\\"hello {name}\\\")\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/reject_intrinsic_arity.saw b/prototypes/minivm/examples/strings/reject_intrinsic_arity.saw\\nnew file mode 100644\\nindex 00000000..f23173bc\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/reject_intrinsic_arity.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"hello\\\".substring(1)) }\\ndiff --git a/prototypes/minivm/examples/strings/reject_intrinsic_label.saw b/prototypes/minivm/examples/strings/reject_intrinsic_label.saw\\nnew file mode 100644\\nindex 00000000..8ec6bda9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/reject_intrinsic_label.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"hello\\\".byte_at(position: 1)) }\\ndiff --git a/prototypes/minivm/examples/strings/reject_intrinsic_type.saw b/prototypes/minivm/examples/strings/reject_intrinsic_type.saw\\nnew file mode 100644\\nindex 00000000..08007e09\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/reject_intrinsic_type.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"hello\\\".byte_at(true)) }\\ndiff --git a/prototypes/minivm/examples/strings/reject_record_field.saw b/prototypes/minivm/examples/strings/reject_record_field.saw\\nnew file mode 100644\\nindex 00000000..cc58b067\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/reject_record_field.saw\\n@@ -0,0 +1,2 @@\\n+struct Bad { text: String }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/strings/reject_static.saw b/prototypes/minivm/examples/strings/reject_static.saw\\nnew file mode 100644\\nindex 00000000..66eeca92\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/reject_static.saw\\n@@ -0,0 +1,2 @@\\n+static TEXT: String = \\\"no\\\"\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/strings/reject_unknown_intrinsic.saw b/prototypes/minivm/examples/strings/reject_unknown_intrinsic.saw\\nnew file mode 100644\\nindex 00000000..08c46c1f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/reject_unknown_intrinsic.saw\\n@@ -0,0 +1 @@\\n+func main() { print(\\\"hello\\\".starts_with(\\\"h\\\")) }\\ndiff --git a/prototypes/minivm/examples/strings/repeated_allocations.saw b/prototypes/minivm/examples/strings/repeated_allocations.saw\\nnew file mode 100644\\nindex 00000000..07af48be\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/repeated_allocations.saw\\n@@ -0,0 +1,16 @@\\n+func make(value: String) -> String { value.substring(0, value.len()) }\\n+\\n+func main() {\\n+    var i = 0\\n+    var keep = \\\"start\\\"\\n+    while i < 500 {\\n+        let temporary = make(\\\"temporary\\\")\\n+        if temporary.equals(other: \\\"temporary\\\") {\\n+            keep = \\\"loop\\\".substring(0, 4)\\n+        }\\n+        i += 1\\n+    }\\n+    keep = \\\"final\\\"\\n+    print(keep)\\n+    print(i)\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/temporary_intrinsics.saw b/prototypes/minivm/examples/strings/temporary_intrinsics.saw\\nnew file mode 100644\\nindex 00000000..ac7a08be\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/temporary_intrinsics.saw\\n@@ -0,0 +1,10 @@\\n+func make() -> String { \\\"hello\\\" }\\n+\\n+func main() {\\n+    print(make().substring(1, 4).len())\\n+    print(\\\"\\\".is_empty())\\n+    print(\\\"hello\\\".substring(start: 1, end: 4))\\n+    print(\\\"é\\\".len())\\n+    print(\\\"é\\\".substring(0, 1).len())\\n+    print(\\\"é\\\".substring(0, 1).byte_at(0))\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/value_if_early_return_cleanup.saw b/prototypes/minivm/examples/strings/value_if_early_return_cleanup.saw\\nnew file mode 100644\\nindex 00000000..105a58dd\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/value_if_early_return_cleanup.saw\\n@@ -0,0 +1,17 @@\\n+func select(flag: Bool, stop: Bool) -> String {\\n+    let selected = if flag {\\n+        let temporary = \\\"kept\\\"\\n+        if stop { return \\\"early\\\" }\\n+        temporary\\n+    } else {\\n+        let temporary = \\\"other\\\"\\n+        temporary\\n+    }\\n+    selected\\n+}\\n+\\n+func main() {\\n+    print(select(true, false))\\n+    print(select(true, true))\\n+    print(select(false, false))\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/value_if_loop_cleanup.saw b/prototypes/minivm/examples/strings/value_if_loop_cleanup.saw\\nnew file mode 100644\\nindex 00000000..04eddb39\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/value_if_loop_cleanup.saw\\n@@ -0,0 +1,17 @@\\n+func main() {\\n+    var index = 0\\n+    var last = \\\"initial\\\"\\n+    while index < 200 {\\n+        let selected = if index % 2 == 0 {\\n+            let temporary = \\\"even\\\"\\n+            temporary\\n+        } else {\\n+            let temporary = \\\"odd\\\"\\n+            temporary\\n+        }\\n+        last = selected\\n+        index += 1\\n+    }\\n+    print(last)\\n+    print(index)\\n+}\\ndiff --git a/prototypes/minivm/examples/strings/value_if_nested_cleanup.saw b/prototypes/minivm/examples/strings/value_if_nested_cleanup.saw\\nnew file mode 100644\\nindex 00000000..fa3a7298\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/strings/value_if_nested_cleanup.saw\\n@@ -0,0 +1,19 @@\\n+func choose(flag: Bool) -> String {\\n+    let selected = if flag {\\n+        let temporary = \\\"then\\\"\\n+        if temporary.len() == 4 {\\n+            temporary\\n+        } else {\\n+            \\\"bad\\\"\\n+        }\\n+    } else {\\n+        let temporary = \\\"else\\\"\\n+        temporary\\n+    }\\n+    selected\\n+}\\n+\\n+func main() {\\n+    print(choose(true))\\n+    print(choose(false))\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 5047cce9..90f35ab2 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -42,6 +42,11 @@ struct Borrow {\\n     mutable: Bool\\n }\\n \\n+struct StringSlot {\\n+    slot: Int\\n+    depth: Int\\n+}\\n+\\n struct StaticBinding {\\n     name: String\\n     ty: ValueType\\n@@ -81,7 +86,7 @@ func type_name(t: ValueType) -> String {\\n     match t {\\n         case I8 -> \\\"Int8\\\", case I16 -> \\\"Int16\\\", case I32 -> \\\"Int32\\\", case I64 -> \\\"Int\\\",\\n         case U8 -> \\\"UInt8\\\", case U16 -> \\\"UInt16\\\", case U32 -> \\\"UInt32\\\", case U64 -> \\\"UInt\\\",\\n-        case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case Void -> \\\"Void\\\",\\n+        case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case String -> \\\"String\\\", case Void -> \\\"Void\\\",\\n         case Record(_) -> \\\"record\\\", case Enum(_) -> \\\"enum\\\", case Ref(_) -> \\\"reference\\\"\\n     }\\n }\\n@@ -115,19 +120,23 @@ func literal_magnitude(value: String) -> UInt? {\\n }\\n \\n struct Compiler {\\n+    source: String\\n     tokens: Vector<Token>\\n     records: Vector<RecordIR>\\n     enums: Vector<EnumIR>\\n     references: Vector<ReferenceIR>\\n+    strings: Vector<String>\\n     statics: Vector<StaticBinding>\\n     signatures: Vector<Signature>\\n     pos: Int\\n     depth: Int\\n+    scope_depth: Int\\n     expr_depth: Int\\n     paren_depth: Int\\n     bindings: Vector<Binding>\\n     active_borrows: Vector<Borrow>\\n     pending_writes: Vector<Int>\\n+    string_slots: Vector<StringSlot>\\n     slots: Vector<ValueType>\\n     code: Vector<Instruction>\\n     current_result: ValueType\\n@@ -186,11 +195,13 @@ extension Compiler {\\n             if reference_type(target) { return problem(\\\"references to references are not supported\\\", amp.line, amp.col) }\\n             var supported = scalar_type(target)\\n             match target { case Record(_) -> { supported = true }, case _ -> {} }\\n+            if target == ValueType.String { supported = true }\\n             if not supported { return problem(\\\"only data references are supported\\\", amp.line, amp.col) }\\n             return self.intern_reference(target, mutable)\\n         }\\n         let t = try self.expect(TokenKind.Ident, \\\"expected type\\\")\\n         if t.value == \\\"Bool\\\" { return ValueType.Bool }\\n+        if t.value == \\\"String\\\" { return ValueType.String }\\n         if let ty = named_integer_type(t.value) { return ty }\\n         let ri = self.record_index(t.value)\\n         if ri >= 0 { return ValueType.Record(ri) }\\n@@ -211,6 +222,16 @@ extension Compiler {\\n         ValueType.Ref(self.references.len() - 1)\\n     }\\n \\n+    func intern_string(&var self, value: String) -> Int {\\n+        var i = 0\\n+        while i < self.strings.len() {\\n+            if self.strings[i] == value { return i }\\n+            i += 1\\n+        }\\n+        try! self.strings.push(value)\\n+        self.strings.len() - 1\\n+    }\\n+\\n     func record_index(&self, name: String) -> Int {\\n         var i = 0\\n         while i < self.records.len() {\\n@@ -253,7 +274,7 @@ extension Compiler {\\n     }\\n \\n     func reserved_type_name(&self, name: String) -> Bool {\\n-        if name == \\\"Bool\\\" || name == \\\"Void\\\" { return true }\\n+        if name == \\\"Bool\\\" || name == \\\"String\\\" || name == \\\"Void\\\" { return true }\\n         if let _ = named_integer_type(name) { return true }\\n         false\\n     }\\n@@ -556,6 +577,7 @@ extension Compiler {\\n         if si < 0 { return problem(\\\"unknown static declaration `{name.value}`\\\", name.line, name.col) }\\n         let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after static name\\\")\\n         let target = try self.parse_type()\\n+        if target == ValueType.String { return problem(\\\"String statics are not supported\\\", name.line, name.col) }\\n         if target == ValueType.Byte || not printable_type(target) {\\n             return problem(\\\"static type must be an integer or Bool\\\", name.line, name.col)\\n         }\\n@@ -631,6 +653,7 @@ extension Compiler {\\n             let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after record field\\\")\\n             let ty = try self.parse_type()\\n             if ty == ValueType.Void { return problem(\\\"record field cannot have Void type\\\", field.line, field.col) }\\n+            if ty == ValueType.String { return problem(\\\"String record fields are not supported\\\", field.line, field.col) }\\n             if reference_type(ty) { return problem(\\\"reference record fields are not supported\\\", field.line, field.col) }\\n             try! names.push(field.value)\\n             try! types.push(ty)\\n@@ -680,6 +703,7 @@ extension Compiler {\\n     func new_slot(&var self, ty: ValueType) -> Int {\\n         let n = self.slots.len()\\n         try! self.slots.push(ty)\\n+        if ty == ValueType.String { try! self.string_slots.push(StringSlot(slot: n, depth: self.scope_depth)) }\\n         n\\n     }\\n \\n@@ -688,7 +712,7 @@ extension Compiler {\\n         let start = self.slots.len()\\n         var i = 0\\n         while i < layout.len() {\\n-            try! self.slots.push(layout[i])\\n+            let _ = self.new_slot(layout[i])\\n             i += 1\\n         }\\n         start\\n@@ -787,6 +811,32 @@ extension Compiler {\\n         while self.pending_writes.len() > count { let _ = self.pending_writes.pop() }\\n     }\\n \\n+    func cleanup_scope(&var self, preserve: Int, t: &Token) {\\n+        var i = 0\\n+        while i < self.string_slots.len() {\\n+            if self.string_slots[i].depth == self.scope_depth {\\n+                if self.string_slots[i].slot == preserve {\\n+                    self.string_slots[i].depth -= 1\\n+                } else {\\n+                    let at = self.emit_at(Op.Drop, t.line, t.col)\\n+                    self.code[at].a = self.string_slots[i].slot\\n+                }\\n+            }\\n+            i += 1\\n+        }\\n+    }\\n+\\n+    func cleanup_function(&var self, preserve: Int, t: &Token) {\\n+        var i = 0\\n+        while i < self.string_slots.len() {\\n+            if self.string_slots[i].slot != preserve {\\n+                let at = self.emit_at(Op.Drop, t.line, t.col)\\n+                self.code[at].a = self.string_slots[i].slot\\n+            }\\n+            i += 1\\n+        }\\n+    }\\n+\\n     func read_place(&var self, place: Place, t: &Token) -> Result<ExprValue, ProtoError> {\\n         try self.check_read(place.root, &t)\\n         if place.indirect {\\n@@ -895,10 +945,83 @@ extension Compiler {\\n         problem(\\\"cannot implicitly convert {self.display_type(v.ty)} to {self.display_type(target)}\\\", t.line, t.col)\\n     }\\n \\n+    func source_offset(&self, line: Int, col: Int) -> Int {\\n+        var at = 0\\n+        var current_line = 1\\n+        var current_col = 1\\n+        while at < self.source.len() {\\n+            if current_line == line && current_col == col { return at }\\n+            let b = self.source.byte_at(at) as Int\\n+            at += 1\\n+            if b == 10 {\\n+                current_line += 1\\n+                current_col = 1\\n+            } else if b < 128 || b > 191 { current_col += 1 }\\n+        }\\n+        -1\\n+    }\\n+\\n+    func literal_hex_value(&self, b: Int) -> Int {\\n+        if b >= 48 && b <= 57 { return b - 48 }\\n+        if b >= 97 && b <= 102 { return b - 97 + 10 }\\n+        b - 65 + 10\\n+    }\\n+\\n+    func decode_string_literal(&self, t: &Token) -> Result<String, ProtoError> {\\n+        var at = self.source_offset(t.line, t.col)\\n+        if at < 0 || (self.source.byte_at(at) as Int) != 34 {\\n+            return problem(\\\"cannot locate string literal source\\\", t.line, t.col)\\n+        }\\n+        at += 1\\n+        var out = StringBuilder()\\n+        while at < self.source.len() {\\n+            let b = self.source.byte_at(at) as Int\\n+            at += 1\\n+            if b == 34 { return out.build() }\\n+            if b != 92 {\\n+                try! out.append(Byte(UInt8.from(truncating: b)))\\n+                continue\\n+            }\\n+            let escaped = self.source.byte_at(at) as Int\\n+            at += 1\\n+            if escaped == 110 { try! out.append(Byte(UInt8.from(truncating: 10))) }\\n+            else if escaped == 116 { try! out.append(Byte(UInt8.from(truncating: 9))) }\\n+            else if escaped == 114 { try! out.append(Byte(UInt8.from(truncating: 13))) }\\n+            else if escaped == 48 { try! out.append(Byte(UInt8.from(truncating: 0))) }\\n+            else if escaped == 34 || escaped == 92 || escaped == 123 || escaped == 125 {\\n+                try! out.append(Byte(UInt8.from(truncating: escaped)))\\n+            } else if escaped == 117 {\\n+                at += 1\\n+                var scalar = 0\\n+                while (self.source.byte_at(at) as Int) != 125 {\\n+                    scalar = scalar * 16 + self.literal_hex_value(self.source.byte_at(at) as Int)\\n+                    at += 1\\n+                }\\n+                at += 1\\n+                try! out.append(try! Scalar(value: scalar))\\n+            }\\n+        }\\n+        problem(\\\"unterminated string literal\\\", t.line, t.col)\\n+    }\\n+\\n     func parse_atom(&var self) -> Result<ExprValue, ProtoError> {\\n         if self.depth > 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n         if self.at(TokenKind.If) { return self.parse_if_value(ValueType.Void, false) }\\n         if self.at(TokenKind.Match) { return self.parse_match_value(ValueType.Void, false) }\\n+        if self.at(TokenKind.InterpString) {\\n+            let t = self.take()\\n+            return problem(\\\"string interpolation is not supported\\\", t.line, t.col)\\n+        }\\n+        if self.at(TokenKind.StringLit) {\\n+            let t = self.take()\\n+            let value = try self.decode_string_literal(&t)\\n+            let pool_index = self.intern_string(value)\\n+            let dst = self.new_slot(ValueType.String)\\n+            let at = self.emit_at(Op.StringConst, t.line, t.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].immediate = pool_index\\n+            return expr_value(ValueType.String, dst, false)\\n+        }\\n         if self.at(TokenKind.IntLit) {\\n             let t = self.take()\\n             guard let magnitude = literal_magnitude(t.value) else { return problem(\\\"invalid integer literal\\\", t.line, t.col) }\\n@@ -946,6 +1069,10 @@ extension Compiler {\\n                 if self.at(TokenKind.Dot) {\\n                     let dot = self.take()\\n                     let method = try self.expect(TokenKind.Ident, \\\"expected method name after `.`\\\")\\n+                    if place.ty == ValueType.String {\\n+                        let receiver = try self.read_place(place, &candidate)\\n+                        return self.parse_string_intrinsic(receiver, method)\\n+                    }\\n                     let owner = match place.ty { case Record(index) -> index, case _ -> -1 }\\n                     if owner < 0 { return problem(\\\"method receiver must be a record place\\\", dot.line, dot.col) }\\n                     let mi = self.method_index(owner, method.value)\\n@@ -1085,6 +1212,11 @@ extension Compiler {\\n             let dot = self.take()\\n             self.expr_space()\\n             let field = try self.expect(TokenKind.Ident, \\\"expected record field after `.`\\\")\\n+            if value.ty == ValueType.String {\\n+                value = try self.parse_string_intrinsic(value, field)\\n+                self.expr_space()\\n+                continue\\n+            }\\n             if self.at(TokenKind.LParen) {\\n                 return problem(\\\"method calls require a named place receiver\\\", dot.line, dot.col)\\n             }\\n@@ -1102,6 +1234,97 @@ extension Compiler {\\n         value\\n     }\\n \\n+    func parse_string_argument(&var self, intrinsic: &Token, parameter: String,\\n+                               expected: ValueType) -> Result<ExprValue, ProtoError> {\\n+        if self.at(TokenKind.Ident) && self.pos + 1 < self.tokens.len() && self.tokens[self.pos + 1].kind == TokenKind.Colon {\\n+            let label = self.take()\\n+            let _ = self.take()\\n+            if label.value != parameter {\\n+                return problem(\\\"argument label `{label.value}` does not match parameter `{parameter}`\\\", label.line, label.col)\\n+            }\\n+            self.expr_space()\\n+        }\\n+        var value = try self.parse_expr()\\n+        if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", intrinsic.line, intrinsic.col) }\\n+        if value.ty != expected && not value.literal {\\n+            return problem(\\\"String.{intrinsic.value} argument must be {type_name(expected)}\\\", intrinsic.line, intrinsic.col)\\n+        }\\n+        value = try self.transfer(value, expected, &intrinsic)\\n+        self.snapshot(value, &intrinsic)\\n+    }\\n+\\n+    func parse_string_intrinsic(&var self, receiver: ExprValue,\\n+                                name: Token) -> Result<ExprValue, ProtoError> {\\n+        let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after String intrinsic\\\")\\n+        self.depth += 1\\n+        self.paren_depth += 1\\n+        self.skip_newlines()\\n+        if name.value == \\\"len\\\" || name.value == \\\"is_empty\\\" {\\n+            if not self.at(TokenKind.RParen) { return problem(\\\"wrong number of arguments to String.{name.value}\\\", name.line, name.col) }\\n+            let _ = self.take()\\n+            self.paren_depth -= 1\\n+            self.depth -= 1\\n+            let length = self.new_slot(ValueType.I64)\\n+            let at = self.emit_at(Op.StringLen, name.line, name.col)\\n+            self.code[at].dst = length\\n+            self.code[at].a = receiver.slot\\n+            if name.value == \\\"len\\\" { return expr_value(ValueType.I64, length, false) }\\n+            let zero = self.new_slot(ValueType.I64)\\n+            let constant = self.emit_at(Op.Const, name.line, name.col)\\n+            self.code[constant].dst = zero\\n+            self.code[constant].immediate = 0\\n+            let result = self.new_slot(ValueType.Bool)\\n+            let equal = self.emit_at(Op.Eq, name.line, name.col)\\n+            self.code[equal].dst = result\\n+            self.code[equal].a = length\\n+            self.code[equal].b = zero\\n+            return expr_value(ValueType.Bool, result, false)\\n+        }\\n+        if name.value == \\\"byte_at\\\" {\\n+            let index = try self.parse_string_argument(&name, \\\"index\\\", ValueType.I64)\\n+            self.skip_newlines()\\n+            if self.at(TokenKind.Comma) { return problem(\\\"wrong number of arguments to String.byte_at\\\", name.line, name.col) }\\n+            let _ = try self.expect(TokenKind.RParen, \\\"wrong number of arguments to String.byte_at\\\")\\n+            self.paren_depth -= 1\\n+            self.depth -= 1\\n+            let dst = self.new_slot(ValueType.Byte)\\n+            let at = self.emit_at(Op.StringByteAt, name.line, name.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = receiver.slot\\n+            self.code[at].b = index.slot\\n+            return expr_value(ValueType.Byte, dst, false)\\n+        }\\n+        if name.value == \\\"substring\\\" {\\n+            let start = try self.parse_string_argument(&name, \\\"start\\\", ValueType.I64)\\n+            self.skip_newlines()\\n+            let _ = try self.expect(TokenKind.Comma, \\\"wrong number of arguments to String.substring\\\")\\n+            self.skip_newlines()\\n+            let end = try self.parse_string_argument(&name, \\\"end\\\", ValueType.I64)\\n+            self.skip_newlines()\\n+            if self.at(TokenKind.Comma) { return problem(\\\"wrong number of arguments to String.substring\\\", name.line, name.col) }\\n+            let _ = try self.expect(TokenKind.RParen, \\\"wrong number of arguments to String.substring\\\")\\n+            self.paren_depth -= 1\\n+            self.depth -= 1\\n+            let dst = self.new_slot(ValueType.String)\\n+            let at = self.emit_at(Op.StringSlice, name.line, name.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = receiver.slot\\n+            self.code[at].b = start.slot\\n+            try! self.code[at].args.push(end.slot)\\n+            return expr_value(ValueType.String, dst, false)\\n+        }\\n+        if name.value == \\\"equals\\\" {\\n+            let other = try self.parse_string_argument(&name, \\\"other\\\", ValueType.String)\\n+            self.skip_newlines()\\n+            if self.at(TokenKind.Comma) { return problem(\\\"wrong number of arguments to String.equals\\\", name.line, name.col) }\\n+            let _ = try self.expect(TokenKind.RParen, \\\"wrong number of arguments to String.equals\\\")\\n+            self.paren_depth -= 1\\n+            self.depth -= 1\\n+            return self.binary(receiver, other, Op.Eq, ValueType.Bool, &name)\\n+        }\\n+        problem(\\\"unknown String intrinsic `{name.value}`\\\", name.line, name.col)\\n+    }\\n+\\n     func parse_record_constructor(&var self, name: Token, ri: Int) -> Result<ExprValue, ProtoError> {\\n         let _ = self.take()\\n         self.depth += 1\\n@@ -1643,7 +1866,7 @@ extension Compiler {\\n             let common = try self.common_type(&left, &right, &t)\\n             if left.ty != common { left = try self.adopt_literal(left, common, &t) }\\n             if right.ty != common { right = try self.adopt_literal(right, common, &t) }\\n-            if not scalar_type(left.ty) || left.ty != right.ty { return problem(\\\"equality operands must have the same scalar type\\\", t.line, t.col) }\\n+            if (not scalar_type(left.ty) && left.ty != ValueType.String) || left.ty != right.ty { return problem(\\\"equality operands must have the same comparable type\\\", t.line, t.col) }\\n             left = try self.binary(left, right, if t.kind == TokenKind.Eq { Op.Eq } else { Op.Ne }, ValueType.Bool, &t)\\n         }\\n         left\\n@@ -1896,6 +2119,7 @@ extension Compiler {\\n     func parse_value_block(&var self, expected: ValueType, has_expected: Bool) -> Result<BlockValue, ProtoError> {\\n         if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n         self.depth += 1\\n+        self.scope_depth += 1\\n         let saved_parens = self.paren_depth\\n         self.paren_depth = 0\\n         self.skip_newlines()\\n@@ -1915,9 +2139,13 @@ extension Compiler {\\n             let had_newline = self.at(TokenKind.Newline)\\n             if had_newline { self.skip_newlines() }\\n             if self.at(TokenKind.RBrace) {\\n+                let close = self.token()\\n+                let preserve = if not returns && not value.always_returns && value.ty == ValueType.String { value.slot } else { -1 }\\n+                self.cleanup_scope(preserve, &close)\\n                 let _ = self.take()\\n                 self.leave_scope()\\n                 self.depth -= 1\\n+                self.scope_depth -= 1\\n                 self.paren_depth = saved_parens\\n                 if returns || value.always_returns {\\n                     return BlockValue(has_value: false, value: returning_value(), always_returns: true)\\n@@ -1930,9 +2158,11 @@ extension Compiler {\\n             }\\n             if not had_newline { return problem(\\\"expected newline after statement\\\", start.line, start.col) }\\n         }\\n-        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace\\\")\\n+        let close = try self.expect(TokenKind.RBrace, \\\"expected closing brace\\\")\\n+        self.cleanup_scope(-1, &close)\\n         self.leave_scope()\\n         self.depth -= 1\\n+        self.scope_depth -= 1\\n         self.paren_depth = saved_parens\\n         BlockValue(has_value: false, value: returning_value(), always_returns: returns)\\n     }\\n@@ -2174,6 +2404,7 @@ extension Compiler {\\n     func parse_block(&var self) -> Result<Bool, ProtoError> {\\n         if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n         self.depth += 1\\n+        self.scope_depth += 1\\n         self.skip_newlines()\\n         var returns = false\\n         while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n@@ -2181,9 +2412,11 @@ extension Compiler {\\n             if not returns && r { returns = true }\\n             try self.terminator()\\n         }\\n-        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace\\\")\\n+        let close = try self.expect(TokenKind.RBrace, \\\"expected closing brace\\\")\\n+        self.cleanup_scope(-1, &close)\\n         self.leave_scope()\\n         self.depth -= 1\\n+        self.scope_depth -= 1\\n         returns\\n     }\\n \\n@@ -2316,6 +2549,7 @@ extension Compiler {\\n                 if value.always_returns { return true }\\n                 src = value.slot\\n             } else if self.current_result != ValueType.Void { return problem(\\\"non-Void return requires a value\\\", t.line, t.col) }\\n+            self.cleanup_function(src, &t)\\n             let at = self.emit_at(Op.Return, t.line, t.col)\\n             self.code[at].a = src\\n             return true\\n@@ -2386,7 +2620,7 @@ extension Compiler {\\n             self.paren_depth -= 1\\n             self.depth -= 1\\n             if value.ty == ValueType.Void { return problem(\\\"cannot print Void\\\", t.line, t.col) }\\n-            if not printable_type(value.ty) {\\n+            if not printable_type(value.ty) && value.ty != ValueType.String {\\n                 match value.ty {\\n                     case Enum(_) -> { return problem(\\\"cannot print an enum value\\\", t.line, t.col) },\\n                     case _ -> { return problem(\\\"cannot print a record value\\\", t.line, t.col) }\\n@@ -2441,11 +2675,13 @@ extension Compiler {\\n     func compile_function(&var self, index: Int) -> Result<FuncIR, ProtoError> {\\n         self.pos = self.signatures[index].body_start\\n         self.depth = 0\\n+        self.scope_depth = 0\\n         self.expr_depth = 0\\n         self.paren_depth = 0\\n         self.bindings = Vector<Binding>()\\n         self.active_borrows = Vector<Borrow>()\\n         self.pending_writes = Vector<Int>()\\n+        self.string_slots = Vector<StringSlot>()\\n         self.slots = Vector<ValueType>()\\n         self.code = Vector<Instruction>()\\n         self.current_result = self.signatures[index].result\\n@@ -2464,6 +2700,8 @@ extension Compiler {\\n                 let tail = try self.parse_expected_expr(self.current_result)\\n                 if tail.always_returns { returns = true }\\n                 else {\\n+                    let return_token = self.token()\\n+                    self.cleanup_function(tail.slot, &return_token)\\n                     let r = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n                     self.code[r].a = tail.slot\\n                     returns = true\\n@@ -2481,19 +2719,29 @@ extension Compiler {\\n                            self.signatures[index].line, self.signatures[index].col)\\n         }\\n         if self.current_result == ValueType.Void {\\n+            let return_token = self.token()\\n+            self.cleanup_function(-1, &return_token)\\n             let _ = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n         } else {\\n             // Return-path checking above proves this padding unreachable. It gives\\n             // end-of-block jumps a real target and leaves every emitted block terminated.\\n             let slot = try self.new_value(self.current_result)\\n-            let layout = try value_layout(&self.records, self.current_result)\\n-            var word = 0\\n-            while word < layout.len() {\\n+            if self.current_result == ValueType.String {\\n+                let c = self.emit_at(Op.StringConst, self.signatures[index].line, self.signatures[index].col)\\n+                self.code[c].dst = slot\\n+                self.code[c].immediate = self.intern_string(\\\"\\\")\\n+            } else {\\n+                let layout = try value_layout(&self.records, self.current_result)\\n+                var word = 0\\n+                while word < layout.len() {\\n                 let c = self.emit_at(Op.Const, self.signatures[index].line, self.signatures[index].col)\\n                 self.code[c].dst = slot + word\\n                 self.code[c].immediate = 0\\n                 word += 1\\n+                }\\n             }\\n+            let return_token = self.token()\\n+            self.cleanup_function(slot, &return_token)\\n             let r = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n             self.code[r].a = slot\\n         }\\n@@ -2511,10 +2759,12 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         case Ok(ts) -> move ts,\\n         case Err(e) -> { return problem(e.message, e.line, e.col) }\\n     }\\n-    var compiler = Compiler(tokens: move tokens, records: Vector<RecordIR>(), enums: Vector<EnumIR>(), references: Vector<ReferenceIR>(), statics: Vector<StaticBinding>(),\\n-                            signatures: Vector<Signature>(), pos: 0, depth: 0,\\n+    var compiler = Compiler(source: source, tokens: move tokens, records: Vector<RecordIR>(), enums: Vector<EnumIR>(),\\n+                            references: Vector<ReferenceIR>(), strings: Vector<String>(), statics: Vector<StaticBinding>(),\\n+                            signatures: Vector<Signature>(), pos: 0, depth: 0, scope_depth: 0,\\n                             expr_depth: 0, paren_depth: 0,\\n                             bindings: Vector<Binding>(), active_borrows: Vector<Borrow>(), pending_writes: Vector<Int>(),\\n+                            string_slots: Vector<StringSlot>(),\\n                             slots: Vector<ValueType>(),\\n                             code: Vector<Instruction>(), current_result: ValueType.Void)\\n     try compiler.collect_record_names()\\n@@ -2531,5 +2781,6 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         i += 1\\n     }\\n     Program(enums: compiler.enums.copy(), records: compiler.records.copy(), references: compiler.references.copy(),\\n+            strings: compiler.strings.copy(),\\n             functions: move functions, main_index: main_index)\\n }\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex c22fa8fe..169a4a9a 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -1,4 +1,5 @@\\n import src.model.*\\n+import src.string_runtime.*\\n \\n func put(out: &var StringBuilder, text: String) {\\n     try! out.append(text)\\n@@ -45,6 +46,10 @@ func emit_value_type(out: &var StringBuilder, records: &Vector<RecordIR>,\\n         put(&var out, \\\"i64\\\")\\n         return\\n     }\\n+    if string_type(t) {\\n+        put(&var out, \\\"ptr\\\")\\n+        return\\n+    }\\n     let layout = try value_layout(&records, t)\\n     emit_aggregate_type(&var out, layout.len())\\n     return\\n@@ -90,6 +95,47 @@ func store_ref_temp(out: &var StringBuilder, instruction_index: Int,\\n     put(&var out, \\\"\\\\n\\\")\\n }\\n \\n+func assign_string_temp(out: &var StringBuilder, instruction_index: Int,\\n+                        suffix: String, slot: Int, retain: Bool) {\\n+    if retain {\\n+        put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n+        temp_name(&var out, instruction_index, suffix)\\n+        put(&var out, \\\")\\\\n\\\")\\n+    }\\n+    put(&var out, \\\"  \\\")\\n+    load_ref_slot(&var out, instruction_index, \\\"old{slot}\\\", slot)\\n+    put(&var out, \\\"  call void @minivm_string_release(ptr \\\")\\n+    temp_name(&var out, instruction_index, \\\"old{slot}\\\")\\n+    put(&var out, \\\")\\\\n\\\")\\n+    store_ref_temp(&var out, instruction_index, suffix, slot)\\n+}\\n+\\n+func put_hex_byte(out: &var StringBuilder, byte: Int) {\\n+    let digits = \\\"0123456789ABCDEF\\\"\\n+    put(&var out, \\\"\\\\\\\\\\\")\\n+    put(&var out, digits.substring(byte / 16, byte / 16 + 1))\\n+    put(&var out, digits.substring(byte % 16, byte % 16 + 1))\\n+}\\n+\\n+func emit_string_constants(out: &var StringBuilder, strings: &Vector<String>) {\\n+    var index = 0\\n+    while index < strings.len() {\\n+        put(&var out, \\\"@.str\\\")\\n+        put_int(&var out, index)\\n+        put(&var out, \\\" = private unnamed_addr constant [\\\")\\n+        put_int(&var out, strings[index].len())\\n+        put(&var out, \\\" x i8] c\\\\\\\"\\\")\\n+        var byte = 0\\n+        while byte < strings[index].len() {\\n+            put_hex_byte(&var out, strings[index].byte_at(byte) as Int)\\n+            byte += 1\\n+        }\\n+        put(&var out, \\\"\\\\\\\"\\\\n\\\")\\n+        index += 1\\n+    }\\n+    put(&var out, \\\"\\\\n\\\")\\n+}\\n+\\n func emit_normalized_result(out: &var StringBuilder, instruction_index: Int,\\n                             dst: Int, suffix: String, t: ValueType) {\\n     let width = integer_width(t)\\n@@ -372,11 +418,24 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             put(&var out, \\\"\\\\n\\\")\\n             next_block(&var out, instruction_index)\\n         },\\n+        case StringConst -> {\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"new\\\")\\n+            put(&var out, \\\" = call ptr @minivm_string_new(ptr @.str\\\")\\n+            put_int(&var out, inst.immediate)\\n+            put(&var out, \\\", i64 \\\")\\n+            put_int(&var out, program.strings[inst.immediate].len())\\n+            put(&var out, \\\")\\\\n\\\")\\n+            assign_string_temp(&var out, instruction_index, \\\"new\\\", inst.dst, false)\\n+            next_block(&var out, instruction_index)\\n+        },\\n         case Copy -> {\\n             put(&var out, \\\"  \\\")\\n-            if reference_type(function.slots[inst.a]) {\\n+            if reference_type(function.slots[inst.a]) || string_type(function.slots[inst.a]) {\\n                 load_ref_slot(&var out, instruction_index, \\\"v\\\", inst.a)\\n-                store_ref_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+                if string_type(function.slots[inst.a]) {\\n+                    assign_string_temp(&var out, instruction_index, \\\"v\\\", inst.dst, true)\\n+                } else { store_ref_temp(&var out, instruction_index, \\\"v\\\", inst.dst) }\\n             } else {\\n                 load_slot(&var out, instruction_index, \\\"v\\\", inst.a)\\n                 store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n@@ -396,6 +455,17 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n             let target = try reference_target(&program.references, function.slots[inst.a])\\n             let layout = try value_layout(&program.records, target)\\n+            if string_type(target) {\\n+                put(&var out, \\\"  \\\")\\n+                temp_name(&var out, instruction_index, \\\"v0\\\")\\n+                put(&var out, \\\" = load ptr, ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"ref\\\")\\n+                put(&var out, \\\"\\\\n\\\")\\n+                assign_string_temp(&var out, instruction_index, \\\"v0\\\", inst.dst, true)\\n+                next_block(&var out, instruction_index)\\n+                put(&var out, \\\"\\\\n\\\")\\n+                return\\n+            }\\n             var word = 0\\n             while word < layout.len() {\\n                 if word > 0 {\\n@@ -427,6 +497,26 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n             let target = try reference_target(&program.references, function.slots[inst.a])\\n             let layout = try value_layout(&program.records, target)\\n+            if string_type(target) {\\n+                put(&var out, \\\"  \\\")\\n+                load_ref_slot(&var out, instruction_index, \\\"v0\\\", inst.b)\\n+                put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"v0\\\")\\n+                put(&var out, \\\")\\\\n  \\\")\\n+                temp_name(&var out, instruction_index, \\\"old\\\")\\n+                put(&var out, \\\" = load ptr, ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"ref\\\")\\n+                put(&var out, \\\"\\\\n  call void @minivm_string_release(ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"old\\\")\\n+                put(&var out, \\\")\\\\n  store ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"v0\\\")\\n+                put(&var out, \\\", ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"ref\\\")\\n+                put(&var out, \\\"\\\\n\\\")\\n+                next_block(&var out, instruction_index)\\n+                put(&var out, \\\"\\\\n\\\")\\n+                return\\n+            }\\n             var word = 0\\n             while word < layout.len() {\\n                 put(&var out, \\\"  \\\")\\n@@ -532,8 +622,52 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n             next_block(&var out, instruction_index)\\n         },\\n-        case Eq -> emit_compare(&var out, instruction_index, &inst, \\\"eq\\\"),\\n-        case Ne -> emit_compare(&var out, instruction_index, &inst, \\\"ne\\\"),\\n+        case Eq -> {\\n+            if string_type(function.slots[inst.a]) {\\n+                put(&var out, \\\"  \\\")\\n+                load_ref_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+                put(&var out, \\\"  \\\")\\n+                load_ref_slot(&var out, instruction_index, \\\"b\\\", inst.b)\\n+                put(&var out, \\\"  \\\")\\n+                temp_name(&var out, instruction_index, \\\"c\\\")\\n+                put(&var out, \\\" = call i1 @minivm_string_eq(ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"a\\\")\\n+                put(&var out, \\\", ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"b\\\")\\n+                put(&var out, \\\")\\\\n  \\\")\\n+                temp_name(&var out, instruction_index, \\\"v\\\")\\n+                put(&var out, \\\" = zext i1 \\\")\\n+                temp_name(&var out, instruction_index, \\\"c\\\")\\n+                put(&var out, \\\" to i64\\\\n\\\")\\n+                store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+                next_block(&var out, instruction_index)\\n+            } else { emit_compare(&var out, instruction_index, &inst, \\\"eq\\\") }\\n+        },\\n+        case Ne -> {\\n+            if string_type(function.slots[inst.a]) {\\n+                put(&var out, \\\"  \\\")\\n+                load_ref_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+                put(&var out, \\\"  \\\")\\n+                load_ref_slot(&var out, instruction_index, \\\"b\\\", inst.b)\\n+                put(&var out, \\\"  \\\")\\n+                temp_name(&var out, instruction_index, \\\"same\\\")\\n+                put(&var out, \\\" = call i1 @minivm_string_eq(ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"a\\\")\\n+                put(&var out, \\\", ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"b\\\")\\n+                put(&var out, \\\")\\\\n  \\\")\\n+                temp_name(&var out, instruction_index, \\\"c\\\")\\n+                put(&var out, \\\" = xor i1 \\\")\\n+                temp_name(&var out, instruction_index, \\\"same\\\")\\n+                put(&var out, \\\", true\\\\n  \\\")\\n+                temp_name(&var out, instruction_index, \\\"v\\\")\\n+                put(&var out, \\\" = zext i1 \\\")\\n+                temp_name(&var out, instruction_index, \\\"c\\\")\\n+                put(&var out, \\\" to i64\\\\n\\\")\\n+                store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+                next_block(&var out, instruction_index)\\n+            } else { emit_compare(&var out, instruction_index, &inst, \\\"ne\\\") }\\n+        },\\n         case Lt -> emit_compare(&var out, instruction_index, &inst, if integer_signed(function.slots[inst.a]) { \\\"slt\\\" } else { \\\"ult\\\" }),\\n         case Le -> emit_compare(&var out, instruction_index, &inst, if integer_signed(function.slots[inst.a]) { \\\"sle\\\" } else { \\\"ule\\\" }),\\n         case Gt -> emit_compare(&var out, instruction_index, &inst, if integer_signed(function.slots[inst.a]) { \\\"sgt\\\" } else { \\\"ugt\\\" }),\\n@@ -564,7 +698,7 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             var argument_index = 0\\n             while argument_index < inst.args.len() {\\n                 put(&var out, \\\"  \\\")\\n-                if reference_type(function.slots[inst.args[argument_index]]) {\\n+                if reference_type(function.slots[inst.args[argument_index]]) || string_type(function.slots[inst.args[argument_index]]) {\\n                     load_ref_slot(&var out, instruction_index, \\\"arg{argument_index}\\\", inst.args[argument_index])\\n                 } else {\\n                     load_slot(&var out, instruction_index, \\\"arg{argument_index}\\\", inst.args[argument_index])\\n@@ -584,13 +718,15 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             argument_index = 0\\n             while argument_index < inst.args.len() {\\n                 if argument_index > 0 { put(&var out, \\\", \\\") }\\n-                if reference_type(function.slots[inst.args[argument_index]]) { put(&var out, \\\"ptr \\\") }\\n+                if reference_type(function.slots[inst.args[argument_index]]) || string_type(function.slots[inst.args[argument_index]]) { put(&var out, \\\"ptr \\\") }\\n                 else { put(&var out, \\\"i64 \\\") }\\n                 temp_name(&var out, instruction_index, \\\"arg{argument_index}\\\")\\n                 argument_index = argument_index + 1\\n             }\\n             put(&var out, \\\")\\\\n\\\")\\n-            if scalar_type(callee.result) {\\n+            if string_type(callee.result) {\\n+                assign_string_temp(&var out, instruction_index, \\\"call\\\", inst.dst, false)\\n+            } else if scalar_type(callee.result) {\\n                 store_temp(&var out, instruction_index, \\\"call\\\", inst.dst)\\n             } else if callee.result != ValueType.Void {\\n                 var result_index = 0\\n@@ -610,11 +746,73 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             }\\n             next_block(&var out, instruction_index)\\n         },\\n+        case StringLen -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"v\\\")\\n+            put(&var out, \\\" = call i64 @minivm_string_len(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"a\\\")\\n+            put(&var out, \\\")\\\\n\\\")\\n+            store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case StringByteAt -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"b\\\", inst.b)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"v\\\")\\n+            put(&var out, \\\" = call i64 @minivm_string_byte_at(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"a\\\")\\n+            put(&var out, \\\", i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"b\\\")\\n+            put(&var out, \\\")\\\\n\\\")\\n+            store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case StringSlice -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"b\\\", inst.b)\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"end\\\", inst.args[0])\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"new\\\")\\n+            put(&var out, \\\" = call ptr @minivm_string_slice(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"a\\\")\\n+            put(&var out, \\\", i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"b\\\")\\n+            put(&var out, \\\", i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"end\\\")\\n+            put(&var out, \\\")\\\\n\\\")\\n+            assign_string_temp(&var out, instruction_index, \\\"new\\\", inst.dst, false)\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case Drop -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"value\\\", inst.a)\\n+            put(&var out, \\\"  call void @minivm_string_release(ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"value\\\")\\n+            put(&var out, \\\")\\\\n  store ptr null, ptr \\\")\\n+            slot_name(&var out, inst.a)\\n+            put(&var out, \\\"\\\\n\\\")\\n+            next_block(&var out, instruction_index)\\n+        },\\n         case Print -> {\\n-            if not printable_type(function.slots[inst.a]) { return problem(\\\"internal prototype error: non-printable value\\\", inst.line, inst.col) }\\n             put(&var out, \\\"  \\\")\\n-            load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n-            if function.slots[inst.a] == ValueType.Bool {\\n+            if string_type(function.slots[inst.a]) {\\n+                load_ref_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+                put(&var out, \\\"  call void @minivm_string_print(ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"a\\\")\\n+                put(&var out, \\\")\\\\n\\\")\\n+            } else {\\n+                load_slot(&var out, instruction_index, \\\"a\\\", inst.a)\\n+            }\\n+            if string_type(function.slots[inst.a]) {}\\n+            else if function.slots[inst.a] == ValueType.Bool {\\n                 put(&var out, \\\"  \\\")\\n                 temp_name(&var out, instruction_index, \\\"c\\\")\\n                 put(&var out, \\\" = icmp ne i64 \\\")\\n@@ -638,8 +836,32 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             next_block(&var out, instruction_index)\\n         },\\n         case Return -> {\\n+            if string_type(function.result) {\\n+                put(&var out, \\\"  \\\")\\n+                load_ref_slot(&var out, instruction_index, \\\"ret\\\", inst.a)\\n+                put(&var out, \\\"  call void @minivm_string_retain(ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"ret\\\")\\n+                put(&var out, \\\")\\\\n\\\")\\n+            }\\n+            var cleanup = 0\\n+            while cleanup < function.slots.len() {\\n+                if string_type(function.slots[cleanup]) {\\n+                    put(&var out, \\\"  \\\")\\n+                    load_ref_slot(&var out, instruction_index, \\\"cleanup{cleanup}\\\", cleanup)\\n+                    put(&var out, \\\"  call void @minivm_string_release(ptr \\\")\\n+                    temp_name(&var out, instruction_index, \\\"cleanup{cleanup}\\\")\\n+                    put(&var out, \\\")\\\\n  store ptr null, ptr \\\")\\n+                    slot_name(&var out, cleanup)\\n+                    put(&var out, \\\"\\\\n\\\")\\n+                }\\n+                cleanup += 1\\n+            }\\n             if inst.a < 0 {\\n                 put(&var out, \\\"  ret void\\\\n\\\")\\n+            } else if string_type(function.result) {\\n+                put(&var out, \\\"  ret ptr \\\")\\n+                temp_name(&var out, instruction_index, \\\"ret\\\")\\n+                put(&var out, \\\"\\\\n\\\")\\n             } else if scalar_type(function.result) {\\n                 put(&var out, \\\"  \\\")\\n                 load_slot(&var out, instruction_index, \\\"ret\\\", inst.a)\\n@@ -689,7 +911,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     var argument_index = 0\\n     while parameter_index < function.params.len() {\\n         let parameter_type = function.params[parameter_index]\\n-        if reference_type(parameter_type) {\\n+        if reference_type(parameter_type) || string_type(parameter_type) {\\n             if argument_index > 0 { put(&var out, \\\", \\\") }\\n             put(&var out, \\\"ptr %arg\\\")\\n             put_int(&var out, argument_index)\\n@@ -715,7 +937,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     while slot_index < function.slots.len() {\\n         put(&var out, \\\"  \\\")\\n         slot_name(&var out, slot_index)\\n-        if reference_type(function.slots[slot_index]) {\\n+        if reference_type(function.slots[slot_index]) || string_type(function.slots[slot_index]) {\\n             put(&var out, \\\" = alloca ptr\\\\n\\\")\\n         } else {\\n             put(&var out, \\\" = getelementptr [\\\")\\n@@ -726,6 +948,15 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n         }\\n         slot_index = slot_index + 1\\n     }\\n+    slot_index = 0\\n+    while slot_index < function.slots.len() {\\n+        if string_type(function.slots[slot_index]) {\\n+            put(&var out, \\\"  store ptr null, ptr \\\")\\n+            slot_name(&var out, slot_index)\\n+            put(&var out, \\\"\\\\n\\\")\\n+        }\\n+        slot_index += 1\\n+    }\\n     var parameter_words = 0\\n     var source_parameter = 0\\n     while source_parameter < function.params.len() {\\n@@ -735,7 +966,12 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     }\\n     argument_index = 0\\n     while argument_index < parameter_words {\\n-        put(&var out, if reference_type(function.slots[argument_index]) { \\\"  store ptr %arg\\\" } else { \\\"  store i64 %arg\\\" })\\n+        if string_type(function.slots[argument_index]) {\\n+            put(&var out, \\\"  call void @minivm_string_retain(ptr %arg\\\")\\n+            put_int(&var out, argument_index)\\n+            put(&var out, \\\")\\\\n\\\")\\n+        }\\n+        put(&var out, if reference_type(function.slots[argument_index]) || string_type(function.slots[argument_index]) { \\\"  store ptr %arg\\\" } else { \\\"  store i64 %arg\\\" })\\n         put_int(&var out, argument_index)\\n         put(&var out, \\\", ptr \\\")\\n         slot_name(&var out, argument_index)\\n@@ -769,6 +1005,7 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     put(&var out, \\\"@.divzero = private unnamed_addr constant [32 x i8] c\\\\\\\"runtime error: division by zero\\\\\\\\00\\\\\\\"\\\\n\\\\n\\\")\\n     put(&var out, \\\"@.cast = private unnamed_addr constant [41 x i8] c\\\\\\\"runtime error: integer cast out of range\\\\\\\\00\\\\\\\"\\\\n\\\")\\n     put(&var out, \\\"@.shift = private unnamed_addr constant [34 x i8] c\\\\\\\"runtime error: shift out of range\\\\\\\\00\\\\\\\"\\\\n\\\\n\\\")\\n+    emit_string_constants(&var out, &program.strings)\\n     put(&var out, \\\"declare i32 @printf(ptr, ...)\\\\ndeclare i32 @puts(ptr)\\\\ndeclare void @exit(i32)\\\\n\\\")\\n     put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.sadd.with.overflow.i64(i64, i64)\\\\n\\\")\\n     put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.ssub.with.overflow.i64(i64, i64)\\\\n\\\")\\n@@ -776,6 +1013,7 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.uadd.with.overflow.i64(i64, i64)\\\\n\\\")\\n     put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.usub.with.overflow.i64(i64, i64)\\\\n\\\")\\n     put(&var out, \\\"declare \\\\{i64, i1\\\\} @llvm.umul.with.overflow.i64(i64, i64)\\\\n\\\\n\\\")\\n+    emit_string_runtime(&var out)\\n     put(&var out, \\\"define internal void @minivm_overflow() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.overflow)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     put(&var out, \\\"define internal void @minivm_divzero() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.divzero)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n     put(&var out, \\\"define internal void @minivm_cast() \\\\{\\\\nentry:\\\\n  call i32 @puts(ptr @.cast)\\\\n  call void @exit(i32 1)\\\\n  unreachable\\\\n\\\\}\\\\n\\\\n\\\")\\n@@ -788,6 +1026,6 @@ public func emit_llvm(program: &Program) -> Result<String, ProtoError> {\\n     }\\n     put(&var out, \\\"define i32 @main() \\\\{\\\\nentry:\\\\n  call void @f\\\")\\n     put_int(&var out, program.main_index)\\n-    put(&var out, \\\"()\\\\n  ret i32 0\\\\n\\\\}\\\\n\\\")\\n+    put(&var out, \\\"()\\\\n  call void @minivm_string_check_live()\\\\n  ret i32 0\\\\n\\\\}\\\\n\\\")\\n     out.build()\\n }\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex b68416d4..7d1bdc46 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -1,7 +1,7 @@\\n public enum ValueType {\\n     case I8, case I16, case I32, case I64,\\n     case U8, case U16, case U32, case U64, case Byte,\\n-    case Bool, case Void, case Record(index: Int), case Enum(index: Int), case Ref(index: Int)\\n+    case Bool, case Void, case String, case Record(index: Int), case Enum(index: Int), case Ref(index: Int)\\n }\\n @synthesize\\n extension ValueType: Equatable {}\\n@@ -12,6 +12,7 @@ public enum Op {\\n     case Jump, case Branch, case Call, case Print, case Return,\\n     case Cast, case Truncate, case WrapAdd, case WrapSub, case WrapMul,\\n     case Addr, case LoadRef, case StoreRef, case RefField,\\n+    case StringConst, case StringLen, case StringByteAt, case StringSlice, case Drop,\\n     case BitAnd, case BitOr, case BitXor, case BitNot, case Shl, case Shr\\n }\\n @synthesize\\n@@ -116,6 +117,7 @@ public struct EnumIR {\\n extension EnumIR: ExplicitCopy {}\\n \\n public struct Program {\\n+    public strings: Vector<String>\\n     public references: Vector<ReferenceIR>\\n     public enums: Vector<EnumIR>\\n     public records: Vector<RecordIR>\\n@@ -166,13 +168,15 @@ public func problem(message: String, line: Int, col: Int) -> ProtoError {\\n }\\n \\n public func printable_type(t: ValueType) -> Bool {\\n-    integer_type(t) || t == ValueType.Bool\\n+    integer_type(t) || t == ValueType.Bool || string_type(t)\\n }\\n \\n+public func string_type(t: ValueType) -> Bool { t == ValueType.String }\\n+\\n public func scalar_type(t: ValueType) -> Bool {\\n     match t {\\n         case Enum(_) -> true,\\n-        case _ -> printable_type(t)\\n+        case _ -> integer_type(t) || t == ValueType.Bool\\n     }\\n }\\n \\n@@ -212,10 +216,10 @@ public func reference_transfer(refs: &Vector<ReferenceIR>, source: ValueType, ta\\n public func validate_references(refs: &Vector<ReferenceIR>) -> Result<Void, ProtoError> {\\n     var n = 0\\n     while n < refs.len() {\\n-        if not scalar_type(refs[n].target) {\\n+        if not scalar_type(refs[n].target) && not string_type(refs[n].target) {\\n             match refs[n].target {\\n                 case Record(_) -> {},\\n-                case _ -> { return problem(\\\"reference target must be a scalar or record type\\\", 0, 0) }\\n+                case _ -> { return problem(\\\"reference target must be a data type\\\", 0, 0) }\\n             }\\n         }\\n         n += 1\\n@@ -280,13 +284,14 @@ func append_layout(records: &Vector<RecordIR>, t: ValueType,\\n             return\\n         },\\n         case Ref(_) -> { return problem(\\\"reference record fields are not supported\\\", 0, 0) },\\n+        case String -> { return problem(\\\"String record fields are not supported\\\", 0, 0) },\\n         case _ -> { return problem(\\\"Void is not a record field type\\\", 0, 0) }\\n     }\\n }\\n \\n public func value_layout(records: &Vector<RecordIR>, t: ValueType) -> Result<Vector<ValueType>, ProtoError> {\\n     var words = Vector<ValueType>()\\n-    if reference_type(t) {\\n+    if reference_type(t) || string_type(t) {\\n         try! words.push(t)\\n         return move words\\n     }\\ndiff --git a/prototypes/minivm/src/string_runtime.saw b/prototypes/minivm/src/string_runtime.saw\\nnew file mode 100644\\nindex 00000000..3bbe2c35\\n--- /dev/null\\n+++ b/prototypes/minivm/src/string_runtime.saw\\n@@ -0,0 +1,182 @@\\n+public func emit_string_runtime(out: &var StringBuilder) {\\n+    try! out.append(\\\"@.string_alloc_error = private unnamed_addr constant [40 x i8] c\\\\\\\"runtime error: string allocation failed\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    try! out.append(\\\"@.string_index_error = private unnamed_addr constant [41 x i8] c\\\\\\\"runtime error: string index out of range\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    try! out.append(\\\"@.string_range_error = private unnamed_addr constant [42 x i8] c\\\\\\\"runtime error: string range out of bounds\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    try! out.append(\\\"@.string_live_error = private unnamed_addr constant [38 x i8] c\\\\\\\"runtime error: live string allocation\\\\\\\\00\\\\\\\"\\\\n\\\")\\n+    try! out.append(\\\"@minivm_string_live = internal global i64 0\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"declare ptr @malloc(i64)\\\\n\\\")\\n+    try! out.append(\\\"declare void @free(ptr)\\\\n\\\")\\n+    try! out.append(\\\"declare ptr @memcpy(ptr, ptr, i64)\\\\n\\\")\\n+    try! out.append(\\\"declare i32 @memcmp(ptr, ptr, i64)\\\\n\\\")\\n+    try! out.append(\\\"declare i32 @putchar(i32)\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal void @minivm_string_fatal(ptr %message) \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  call i32 @puts(ptr %message)\\\\n\\\")\\n+    try! out.append(\\\"  call void @exit(i32 1)\\\\n\\\")\\n+    try! out.append(\\\"  unreachable\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal ptr @minivm_string_new(ptr %bytes, i64 %len) \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  %too_big = icmp ugt i64 %len, 9223372036854775791\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %too_big, label %fail, label %allocate\\\\n\\\")\\n+    try! out.append(\\\"fail:\\\\n\\\")\\n+    try! out.append(\\\"  call void @minivm_string_fatal(ptr @.string_alloc_error)\\\\n\\\")\\n+    try! out.append(\\\"  unreachable\\\\n\\\")\\n+    try! out.append(\\\"allocate:\\\\n\\\")\\n+    try! out.append(\\\"  %size = add i64 %len, 16\\\\n\\\")\\n+    try! out.append(\\\"  %header = call ptr @malloc(i64 %size)\\\\n\\\")\\n+    try! out.append(\\\"  %null = icmp eq ptr %header, null\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %null, label %fail_null, label %init\\\\n\\\")\\n+    try! out.append(\\\"fail_null:\\\\n\\\")\\n+    try! out.append(\\\"  call void @minivm_string_fatal(ptr @.string_alloc_error)\\\\n\\\")\\n+    try! out.append(\\\"  unreachable\\\\n\\\")\\n+    try! out.append(\\\"init:\\\\n\\\")\\n+    try! out.append(\\\"  store i64 1, ptr %header\\\\n\\\")\\n+    try! out.append(\\\"  %lenp = getelementptr i64, ptr %header, i64 1\\\\n\\\")\\n+    try! out.append(\\\"  store i64 %len, ptr %lenp\\\\n\\\")\\n+    try! out.append(\\\"  %data = getelementptr i8, ptr %header, i64 16\\\\n\\\")\\n+    try! out.append(\\\"  %has_bytes = icmp ne i64 %len, 0\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %has_bytes, label %copy, label %done\\\\n\\\")\\n+    try! out.append(\\\"copy:\\\\n\\\")\\n+    try! out.append(\\\"  call ptr @memcpy(ptr %data, ptr %bytes, i64 %len)\\\\n\\\")\\n+    try! out.append(\\\"  br label %done\\\\n\\\")\\n+    try! out.append(\\\"done:\\\\n\\\")\\n+    try! out.append(\\\"  %live = load i64, ptr @minivm_string_live\\\\n\\\")\\n+    try! out.append(\\\"  %next = add i64 %live, 1\\\\n\\\")\\n+    try! out.append(\\\"  store i64 %next, ptr @minivm_string_live\\\\n\\\")\\n+    try! out.append(\\\"  ret ptr %header\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal void @minivm_string_retain(ptr %value) \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  %null = icmp eq ptr %value, null\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %null, label %done, label %retain\\\\n\\\")\\n+    try! out.append(\\\"retain:\\\\n\\\")\\n+    try! out.append(\\\"  %count = load i64, ptr %value\\\\n\\\")\\n+    try! out.append(\\\"  %overflow = icmp eq i64 %count, -1\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %overflow, label %fail, label %increment\\\\n\\\")\\n+    try! out.append(\\\"fail:\\\\n\\\")\\n+    try! out.append(\\\"  call void @minivm_string_fatal(ptr @.string_alloc_error)\\\\n\\\")\\n+    try! out.append(\\\"  unreachable\\\\n\\\")\\n+    try! out.append(\\\"increment:\\\\n\\\")\\n+    try! out.append(\\\"  %next = add i64 %count, 1\\\\n\\\")\\n+    try! out.append(\\\"  store i64 %next, ptr %value\\\\n\\\")\\n+    try! out.append(\\\"  br label %done\\\\n\\\")\\n+    try! out.append(\\\"done:\\\\n\\\")\\n+    try! out.append(\\\"  ret void\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal void @minivm_string_release(ptr %value) \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  %null = icmp eq ptr %value, null\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %null, label %done, label %release\\\\n\\\")\\n+    try! out.append(\\\"release:\\\\n\\\")\\n+    try! out.append(\\\"  %count = load i64, ptr %value\\\\n\\\")\\n+    try! out.append(\\\"  %next = sub i64 %count, 1\\\\n\\\")\\n+    try! out.append(\\\"  store i64 %next, ptr %value\\\\n\\\")\\n+    try! out.append(\\\"  %dead = icmp eq i64 %next, 0\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %dead, label %destroy, label %done\\\\n\\\")\\n+    try! out.append(\\\"destroy:\\\\n\\\")\\n+    try! out.append(\\\"  call void @free(ptr %value)\\\\n\\\")\\n+    try! out.append(\\\"  %live = load i64, ptr @minivm_string_live\\\\n\\\")\\n+    try! out.append(\\\"  %live_next = sub i64 %live, 1\\\\n\\\")\\n+    try! out.append(\\\"  store i64 %live_next, ptr @minivm_string_live\\\\n\\\")\\n+    try! out.append(\\\"  br label %done\\\\n\\\")\\n+    try! out.append(\\\"done:\\\\n\\\")\\n+    try! out.append(\\\"  ret void\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal i64 @minivm_string_len(ptr %value) \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  %lenp = getelementptr i64, ptr %value, i64 1\\\\n\\\")\\n+    try! out.append(\\\"  %len = load i64, ptr %lenp\\\\n\\\")\\n+    try! out.append(\\\"  ret i64 %len\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal i1 @minivm_string_eq(ptr %a, ptr %b) \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  %alen = call i64 @minivm_string_len(ptr %a)\\\\n\\\")\\n+    try! out.append(\\\"  %blen = call i64 @minivm_string_len(ptr %b)\\\\n\\\")\\n+    try! out.append(\\\"  %same_len = icmp eq i64 %alen, %blen\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %same_len, label %compare, label %different\\\\n\\\")\\n+    try! out.append(\\\"compare:\\\\n\\\")\\n+    try! out.append(\\\"  %adata = getelementptr i8, ptr %a, i64 16\\\\n\\\")\\n+    try! out.append(\\\"  %bdata = getelementptr i8, ptr %b, i64 16\\\\n\\\")\\n+    try! out.append(\\\"  %cmp = call i32 @memcmp(ptr %adata, ptr %bdata, i64 %alen)\\\\n\\\")\\n+    try! out.append(\\\"  %equal = icmp eq i32 %cmp, 0\\\\n\\\")\\n+    try! out.append(\\\"  ret i1 %equal\\\\n\\\")\\n+    try! out.append(\\\"different:\\\\n\\\")\\n+    try! out.append(\\\"  ret i1 false\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal i64 @minivm_string_byte_at(ptr %value, i64 %index) \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  %len = call i64 @minivm_string_len(ptr %value)\\\\n\\\")\\n+    try! out.append(\\\"  %bad = icmp uge i64 %index, %len\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %bad, label %fail, label %read\\\\n\\\")\\n+    try! out.append(\\\"fail:\\\\n\\\")\\n+    try! out.append(\\\"  call void @minivm_string_fatal(ptr @.string_index_error)\\\\n\\\")\\n+    try! out.append(\\\"  unreachable\\\\n\\\")\\n+    try! out.append(\\\"read:\\\\n\\\")\\n+    try! out.append(\\\"  %data = getelementptr i8, ptr %value, i64 16\\\\n\\\")\\n+    try! out.append(\\\"  %at = getelementptr i8, ptr %data, i64 %index\\\\n\\\")\\n+    try! out.append(\\\"  %byte = load i8, ptr %at\\\\n\\\")\\n+    try! out.append(\\\"  %wide = zext i8 %byte to i64\\\\n\\\")\\n+    try! out.append(\\\"  ret i64 %wide\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal ptr @minivm_string_slice(ptr %value, i64 %start, i64 %end) \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  %len = call i64 @minivm_string_len(ptr %value)\\\\n\\\")\\n+    try! out.append(\\\"  %reversed = icmp ugt i64 %start, %end\\\\n\\\")\\n+    try! out.append(\\\"  %past_end = icmp ugt i64 %end, %len\\\\n\\\")\\n+    try! out.append(\\\"  %bad = or i1 %reversed, %past_end\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %bad, label %fail, label %copy\\\\n\\\")\\n+    try! out.append(\\\"fail:\\\\n\\\")\\n+    try! out.append(\\\"  call void @minivm_string_fatal(ptr @.string_range_error)\\\\n\\\")\\n+    try! out.append(\\\"  unreachable\\\\n\\\")\\n+    try! out.append(\\\"copy:\\\\n\\\")\\n+    try! out.append(\\\"  %count = sub i64 %end, %start\\\\n\\\")\\n+    try! out.append(\\\"  %data = getelementptr i8, ptr %value, i64 16\\\\n\\\")\\n+    try! out.append(\\\"  %at = getelementptr i8, ptr %data, i64 %start\\\\n\\\")\\n+    try! out.append(\\\"  %result = call ptr @minivm_string_new(ptr %at, i64 %count)\\\\n\\\")\\n+    try! out.append(\\\"  ret ptr %result\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal void @minivm_string_print(ptr %value) \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  %len = call i64 @minivm_string_len(ptr %value)\\\\n\\\")\\n+    try! out.append(\\\"  %data = getelementptr i8, ptr %value, i64 16\\\\n\\\")\\n+    try! out.append(\\\"  br label %loop\\\\n\\\")\\n+    try! out.append(\\\"loop:\\\\n\\\")\\n+    try! out.append(\\\"  %i = phi i64 [0, %entry], [%next, %body]\\\\n\\\")\\n+    try! out.append(\\\"  %more = icmp ult i64 %i, %len\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %more, label %body, label %newline\\\\n\\\")\\n+    try! out.append(\\\"body:\\\\n\\\")\\n+    try! out.append(\\\"  %at = getelementptr i8, ptr %data, i64 %i\\\\n\\\")\\n+    try! out.append(\\\"  %byte = load i8, ptr %at\\\\n\\\")\\n+    try! out.append(\\\"  %wide = zext i8 %byte to i32\\\\n\\\")\\n+    try! out.append(\\\"  call i32 @putchar(i32 %wide)\\\\n\\\")\\n+    try! out.append(\\\"  %next = add i64 %i, 1\\\\n\\\")\\n+    try! out.append(\\\"  br label %loop\\\\n\\\")\\n+    try! out.append(\\\"newline:\\\\n\\\")\\n+    try! out.append(\\\"  call i32 @putchar(i32 10)\\\\n\\\")\\n+    try! out.append(\\\"  ret void\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+    try! out.append(\\\"define internal void @minivm_string_check_live() \\\\{\\\\n\\\")\\n+    try! out.append(\\\"entry:\\\\n\\\")\\n+    try! out.append(\\\"  %live = load i64, ptr @minivm_string_live\\\\n\\\")\\n+    try! out.append(\\\"  %leaked = icmp ne i64 %live, 0\\\\n\\\")\\n+    try! out.append(\\\"  br i1 %leaked, label %fail, label %done\\\\n\\\")\\n+    try! out.append(\\\"fail:\\\\n\\\")\\n+    try! out.append(\\\"  call void @minivm_string_fatal(ptr @.string_live_error)\\\\n\\\")\\n+    try! out.append(\\\"  unreachable\\\\n\\\")\\n+    try! out.append(\\\"done:\\\\n\\\")\\n+    try! out.append(\\\"  ret void\\\\n\\\")\\n+    try! out.append(\\\"\\\\}\\\\n\\\")\\n+    try! out.append(\\\"\\\\n\\\")\\n+}\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex 90ae7b9b..59ed1bc4 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -111,7 +111,23 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n         case Copy -> {\\n             if reference_type(dst) {\\n                 if not reference_transfer(&p.references, a, dst) { return invalid(&i) }\\n-            } else if not scalar_type(dst) || a != dst { return invalid(&i) }\\n+            } else if (not scalar_type(dst) && not string_type(dst)) || a != dst { return invalid(&i) }\\n+        },\\n+        case StringConst -> {\\n+            if not string_type(dst) || i.immediate < 0 || i.immediate >= p.strings.len() { return invalid(&i) }\\n+        },\\n+        case StringLen -> {\\n+            if not string_type(a) || dst != ValueType.I64 { return invalid(&i) }\\n+        },\\n+        case StringByteAt -> {\\n+            if not string_type(a) || b != ValueType.I64 || dst != ValueType.Byte { return invalid(&i) }\\n+        },\\n+        case StringSlice -> {\\n+            if not string_type(a) || b != ValueType.I64 || not string_type(dst) || i.args.len() != 1 { return invalid(&i) }\\n+            if slot_type(&f, i.args[0]) != ValueType.I64 { return invalid(&i) }\\n+        },\\n+        case Drop -> {\\n+            if not string_type(a) { return invalid(&i) }\\n         },\\n         case Addr -> {\\n             if not reference_type(dst) { return invalid(&i) }\\n@@ -167,9 +183,9 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n             if dst != ValueType.Bool || a != ValueType.Bool { return invalid(&i) }\\n         },\\n         case Eq -> {\\n-            if dst != ValueType.Bool || not scalar_type(a) || a != b { return invalid(&i) }\\n+            if dst != ValueType.Bool || (not scalar_type(a) && not string_type(a)) || a != b { return invalid(&i) }\\n         },\\n-        case Ne -> { if dst != ValueType.Bool || not scalar_type(a) || a != b { return invalid(&i) } },\\n+        case Ne -> { if dst != ValueType.Bool || (not scalar_type(a) && not string_type(a)) || a != b { return invalid(&i) } },\\n         case Lt -> {\\n             if dst != ValueType.Bool || not arithmetic_type(a) || b != a { return invalid(&i) }\\n         },\\n@@ -225,7 +241,7 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n     }\\n     var n = 0\\n     while n < f.slots.len() {\\n-        if not scalar_type(f.slots[n]) && not reference_type(f.slots[n]) { return problem(\\\"internal prototype error: non-scalar slot\\\", 0, 0) }\\n+        if not scalar_type(f.slots[n]) && not reference_type(f.slots[n]) && not string_type(f.slots[n]) { return problem(\\\"internal prototype error: non-scalar slot\\\", 0, 0) }\\n         if n < params.len() && f.slots[n] != params[n] {\\n             return problem(\\\"internal prototype error: parameter slot mismatch\\\", 0, 0)\\n         }\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 11ba764b..3cb6e4a8 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -3,9 +3,57 @@ import src.model.*\\n static MAX_CALL_DEPTH: Int = 128\\n struct CallResult { values: Vector<Int> }\\n extension CallResult: NoCopy {}\\n+struct StringEntry { value: String, refs: Int }\\n func runtime_error(message: String) -> ProtoError { problem(message, 0, 0) }\\n func overflow_error() -> ProtoError { runtime_error(\\\"runtime error: integer overflow\\\") }\\n \\n+func string_entry(heap: &Vector<StringEntry>, handle: Int) -> Result<Int, ProtoError> {\\n+    if handle <= 0 { return runtime_error(\\\"runtime error: invalid string handle\\\") }\\n+    let index = handle - 1\\n+    if index >= heap.len() || heap[index].refs <= 0 {\\n+        return runtime_error(\\\"runtime error: invalid string handle\\\")\\n+    }\\n+    index\\n+}\\n+\\n+func string_retain(heap: &var Vector<StringEntry>, handle: Int) -> Result<Void, ProtoError> {\\n+    if handle == 0 { return }\\n+    let index = try string_entry(&heap, handle)\\n+    if heap[index].refs == Int.max { return runtime_error(\\\"runtime error: string allocation failed\\\") }\\n+    heap[index].refs += 1\\n+    return\\n+}\\n+\\n+func string_release(heap: &var Vector<StringEntry>, handle: Int) -> Result<Void, ProtoError> {\\n+    if handle == 0 { return }\\n+    let index = try string_entry(&heap, handle)\\n+    heap[index].refs -= 1\\n+    if heap[index].refs == 0 { heap[index].value = \\\"\\\" }\\n+    return\\n+}\\n+\\n+func string_allocate(heap: &var Vector<StringEntry>, value: String) -> Result<Int, ProtoError> {\\n+    var index = 0\\n+    while index < heap.len() {\\n+        if heap[index].refs == 0 {\\n+            heap[index].value = value\\n+            heap[index].refs = 1\\n+            return index + 1\\n+        }\\n+        index += 1\\n+    }\\n+    try! heap.push(StringEntry(value: value, refs: 1))\\n+    heap.len()\\n+}\\n+\\n+func string_assign(cells: &var Vector<Int>, dst: Int, source: Int,\\n+                   heap: &var Vector<StringEntry>, retain: Bool) -> Result<Void, ProtoError> {\\n+    if retain { try string_retain(&var heap, source) }\\n+    try string_release(&var heap, cells[dst])\\n+    cells[dst] = source\\n+    return\\n+}\\n+\\n func checked_binary(a: Int, b: Int, t: ValueType, op: Op) -> Result<Int, ProtoError> {\\n     if integer_signed(t) {\\n         let lo = integer_min(t)\\n@@ -91,7 +139,7 @@ func valid_reference(cells: &Vector<Int>, address: Int, width: Int) -> Bool {\\n }\\n \\n func run_function(program: &Program, function_index: Int, arguments: &Vector<Int>,\\n-                  cells: &var Vector<Int>, remaining: &var Int,\\n+                  cells: &var Vector<Int>, heap: &var Vector<StringEntry>, remaining: &var Int,\\n                   depth: Int) -> Result<CallResult, ProtoError> {\\n     if depth > MAX_CALL_DEPTH { return runtime_error(\\\"runtime error: call depth exceeded\\\") }\\n     let function = program.functions[function_index].copy()\\n@@ -103,7 +151,8 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n     }\\n     var ai = 0\\n     while ai < arguments.len() {\\n-        cells[base + ai] = arguments[ai]\\n+        if string_type(function.slots[ai]) { try string_assign(&var cells, base + ai, arguments[ai], &var heap, true) }\\n+        else { cells[base + ai] = arguments[ai] }\\n         ai = ai + 1\\n     }\\n     var pc = 0\\n@@ -114,7 +163,14 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n         let t = if inst.dst >= 0 { function.slots[inst.dst] } else { ValueType.Void }\\n         match inst.op {\\n             case Const -> { cells[base + inst.dst] = inst.immediate },\\n-            case Copy -> { cells[base + inst.dst] = cells[base + inst.a] },\\n+            case StringConst -> {\\n+                let handle = try string_allocate(&var heap, program.strings[inst.immediate])\\n+                try string_assign(&var cells, base + inst.dst, handle, &var heap, false)\\n+            },\\n+            case Copy -> {\\n+                if string_type(t) { try string_assign(&var cells, base + inst.dst, cells[base + inst.a], &var heap, true) }\\n+                else { cells[base + inst.dst] = cells[base + inst.a] }\\n+            },\\n             case Addr -> { cells[base + inst.dst] = base + inst.a },\\n             case LoadRef -> {\\n                 let address = cells[base + inst.a]\\n@@ -129,7 +185,9 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 }\\n                 word = 0\\n                 while word < values.len() {\\n-                    cells[base + inst.dst + word] = values[word]\\n+                    if string_type(layout[word]) {\\n+                        try string_assign(&var cells, base + inst.dst + word, values[word], &var heap, true)\\n+                    } else { cells[base + inst.dst + word] = values[word] }\\n                     word += 1\\n                 }\\n             },\\n@@ -146,7 +204,9 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 }\\n                 word = 0\\n                 while word < values.len() {\\n-                    cells[address + word] = values[word]\\n+                    if string_type(layout[word]) {\\n+                        try string_assign(&var cells, address + word, values[word], &var heap, true)\\n+                    } else { cells[address + word] = values[word] }\\n                     word += 1\\n                 }\\n             },\\n@@ -188,8 +248,20 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n             case Shl -> { cells[base + inst.dst] = try shift(cells[base + inst.a], cells[base + inst.b], function.slots[inst.b], t, true) },\\n             case Shr -> { cells[base + inst.dst] = try shift(cells[base + inst.a], cells[base + inst.b], function.slots[inst.b], t, false) },\\n             case Not -> { cells[base + inst.dst] = if cells[base + inst.a] == 0 { 1 } else { 0 } },\\n-            case Eq -> { cells[base + inst.dst] = if cells[base + inst.a] == cells[base + inst.b] { 1 } else { 0 } },\\n-            case Ne -> { cells[base + inst.dst] = if cells[base + inst.a] != cells[base + inst.b] { 1 } else { 0 } },\\n+            case Eq -> {\\n+                if string_type(function.slots[inst.a]) {\\n+                    let left = try string_entry(&heap, cells[base + inst.a])\\n+                    let right = try string_entry(&heap, cells[base + inst.b])\\n+                    cells[base + inst.dst] = if heap[left].value == heap[right].value { 1 } else { 0 }\\n+                } else { cells[base + inst.dst] = if cells[base + inst.a] == cells[base + inst.b] { 1 } else { 0 } }\\n+            },\\n+            case Ne -> {\\n+                if string_type(function.slots[inst.a]) {\\n+                    let left = try string_entry(&heap, cells[base + inst.a])\\n+                    let right = try string_entry(&heap, cells[base + inst.b])\\n+                    cells[base + inst.dst] = if heap[left].value != heap[right].value { 1 } else { 0 }\\n+                } else { cells[base + inst.dst] = if cells[base + inst.a] != cells[base + inst.b] { 1 } else { 0 } }\\n+            },\\n             case Lt -> { cells[base + inst.dst] = compare(cells[base + inst.a], cells[base + inst.b], function.slots[inst.a], inst.op) },\\n             case Le -> { cells[base + inst.dst] = compare(cells[base + inst.a], cells[base + inst.b], function.slots[inst.a], inst.op) },\\n             case Gt -> { cells[base + inst.dst] = compare(cells[base + inst.a], cells[base + inst.b], function.slots[inst.a], inst.op) },\\n@@ -211,27 +283,63 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                     try! args.push(cells[base + inst.args[i]])\\n                     i = i + 1\\n                 }\\n-                let result = try run_function(&program, inst.target, &args, &var cells, &var remaining, depth + 1)\\n+                let result = try run_function(&program, inst.target, &args, &var cells, &var heap, &var remaining, depth + 1)\\n                 var result_index = 0\\n                 while result_index < result.values.len() {\\n-                    cells[base + inst.dst + result_index] = result.values[result_index]\\n+                    if string_type(function.slots[inst.dst + result_index]) {\\n+                        try string_assign(&var cells, base + inst.dst + result_index, result.values[result_index], &var heap, false)\\n+                    } else { cells[base + inst.dst + result_index] = result.values[result_index] }\\n                     result_index = result_index + 1\\n                 }\\n             },\\n             case Print -> {\\n-                if not printable_type(function.slots[inst.a]) { return problem(\\\"internal prototype error: non-printable value\\\", inst.line, inst.col) }\\n                 let pt = function.slots[inst.a]\\n-                if pt == ValueType.Bool { if cells[base + inst.a] == 0 { print(\\\"false\\\") } else { print(\\\"true\\\") } }\\n+                if string_type(pt) {\\n+                    let index = try string_entry(&heap, cells[base + inst.a])\\n+                    print(heap[index].value)\\n+                }\\n+                else if pt == ValueType.Bool { if cells[base + inst.a] == 0 { print(\\\"false\\\") } else { print(\\\"true\\\") } }\\n                 else if integer_signed(pt) { print(cells[base + inst.a]) } else { print(UInt.from(truncating: cells[base + inst.a])) }\\n             },\\n+            case StringLen -> {\\n+                let index = try string_entry(&heap, cells[base + inst.a])\\n+                cells[base + inst.dst] = heap[index].value.len()\\n+            },\\n+            case StringByteAt -> {\\n+                let index = try string_entry(&heap, cells[base + inst.a])\\n+                let offset = cells[base + inst.b]\\n+                if offset < 0 || offset >= heap[index].value.len() { return runtime_error(\\\"runtime error: string index out of range\\\") }\\n+                cells[base + inst.dst] = heap[index].value.byte_at(offset) as Int\\n+            },\\n+            case StringSlice -> {\\n+                let index = try string_entry(&heap, cells[base + inst.a])\\n+                let start = cells[base + inst.b]\\n+                let end = cells[base + inst.args[0]]\\n+                if start < 0 || start > end || end > heap[index].value.len() { return runtime_error(\\\"runtime error: string range out of bounds\\\") }\\n+                let handle = try string_allocate(&var heap, heap[index].value.substring(start, end))\\n+                try string_assign(&var cells, base + inst.dst, handle, &var heap, false)\\n+            },\\n+            case Drop -> {\\n+                try string_release(&var heap, cells[base + inst.a])\\n+                cells[base + inst.a] = 0\\n+            },\\n             case Return -> {\\n                 var values = Vector<Int>()\\n                 let layout = try value_layout(&program.records, function.result)\\n                 var result_index = 0\\n                 while result_index < layout.len() {\\n+                    if string_type(layout[result_index]) { try string_retain(&var heap, cells[base + inst.a + result_index]) }\\n                     try! values.push(cells[base + inst.a + result_index])\\n                     result_index = result_index + 1\\n                 }\\n+                var slot = 0\\n+                while slot < function.slots.len() {\\n+                    if string_type(function.slots[slot]) {\\n+                        try string_release(&var heap, cells[base + slot])\\n+                        cells[base + slot] = 0\\n+                    }\\n+                    slot += 1\\n+                }\\n                 while cells.len() > base { let _ = cells.pop() }\\n                 return CallResult(values: move values)\\n             }\\n@@ -245,6 +353,12 @@ public func execute(program: &Program, budget: Int) -> Result<Void, ProtoError>\\n     var remaining = budget\\n     let arguments = Vector<Int>()\\n     var cells = Vector<Int>()\\n-    let _ = try run_function(&program, program.main_index, &arguments, &var cells, &var remaining, 1)\\n+    var heap = Vector<StringEntry>()\\n+    let _ = try run_function(&program, program.main_index, &arguments, &var cells, &var heap, &var remaining, 1)\\n+    var index = 0\\n+    while index < heap.len() {\\n+        if heap[index].refs != 0 { return runtime_error(\\\"runtime error: live string allocation\\\") }\\n+        index += 1\\n+    }\\n     return\\n }\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex 7c4447a7..75e4b7e1 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -35,6 +35,25 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"strings/literals_escapes\\\", \\\"\\\\nASCII\\\\ncafé🙂\\\\na\\\\x00b\\\\n{left} \\\\\\\\ right\\\\n\\\\x01x\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/intrinsics\\\", \\\"0\\\\ntrue\\\\n5\\\\nfalse\\\\n104\\\\n111\\\\n101\\\\n169\\\\n\\\\n\\\\nhello\\\\ntrue\\\\ntrue\\\\ntrue\\\\ntrue\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/brace_marker_provenance\\\", \\\"1\\\\n123\\\\n2\\\\n1\\\\n123\\\\n1\\\\n125\\\\n2\\\\n1\\\\n125\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/copies_calls_results\\\", \\\"alpha\\\\nalpha\\\\nbeta\\\\nbeta\\\\nright\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/references\\\", \\\"before\\\\nbefore\\\\nafter\\\\nafter\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/receiver_snapshot_order\\\", \\\"true\\\\nafter\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/temporary_intrinsics\\\", \\\"3\\\\ntrue\\\\nell\\\\n2\\\\n1\\\\n195\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/repeated_allocations\\\", \\\"final\\\\n500\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/recursive_results\\\", \\\"cdef\\\\n4\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/value_if_nested_cleanup\\\", \\\"then\\\\nelse\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/value_if_early_return_cleanup\\\", \\\"kept\\\\nearly\\\\nother\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/value_if_loop_cleanup\\\", \\\"odd\\\\n200\\\\n\\\", section=\\\"strings\\\"),\\n+    RunCase(\\\"strings/index_negative\\\", \\\"runtime error: string index out of range\\\\n\\\", 1, \\\"strings\\\"),\\n+    RunCase(\\\"strings/index_at_end\\\", \\\"runtime error: string index out of range\\\\n\\\", 1, \\\"strings\\\"),\\n+    RunCase(\\\"strings/index_huge\\\", \\\"runtime error: string index out of range\\\\n\\\", 1, \\\"strings\\\"),\\n+    RunCase(\\\"strings/range_negative\\\", \\\"runtime error: string range out of bounds\\\\n\\\", 1, \\\"strings\\\"),\\n+    RunCase(\\\"strings/range_reversed\\\", \\\"runtime error: string range out of bounds\\\\n\\\", 1, \\\"strings\\\"),\\n+    RunCase(\\\"strings/range_past_end\\\", \\\"runtime error: string range out of bounds\\\\n\\\", 1, \\\"strings\\\"),\\n+    RunCase(\\\"strings/range_huge\\\", \\\"runtime error: string range out of bounds\\\\n\\\", 1, \\\"strings\\\"),\\n     RunCase(\\\"receivers/whole_record_reference\\\", \\\"1\\\\n2\\\\n3\\\\n4\\\\n9\\\\n8\\\\n\\\", section=\\\"receivers\\\"),\\n     RunCase(\\\"receivers/nested_forwarding\\\", \\\"7\\\\n13\\\\n18\\\\n\\\", section=\\\"receivers\\\"),\\n     RunCase(\\\"receivers/mixed_abi\\\", \\\"5\\\\n6\\\\n9\\\\n10\\\\n\\\", section=\\\"receivers\\\"),\\n@@ -168,6 +187,15 @@ RUN_CASES = (\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"strings/reject_interpolation\\\", \\\"string interpolation is not supported\\\", \\\"strings\\\"),\\n+    RejectCase(\\\"strings/reject_arithmetic\\\", \\\"arithmetic requires integer operands\\\", \\\"strings\\\"),\\n+    RejectCase(\\\"strings/reject_cast\\\", \\\"`as` requires integer source and target types\\\", \\\"strings\\\"),\\n+    RejectCase(\\\"strings/reject_record_field\\\", \\\"String record fields are not supported\\\", \\\"strings\\\"),\\n+    RejectCase(\\\"strings/reject_static\\\", \\\"String statics are not supported\\\", \\\"strings\\\"),\\n+    RejectCase(\\\"strings/reject_unknown_intrinsic\\\", \\\"unknown String intrinsic\\\", \\\"strings\\\"),\\n+    RejectCase(\\\"strings/reject_intrinsic_label\\\", \\\"argument label\\\", \\\"strings\\\"),\\n+    RejectCase(\\\"strings/reject_intrinsic_arity\\\", \\\"wrong number of arguments to String.substring\\\", \\\"strings\\\"),\\n+    RejectCase(\\\"strings/reject_intrinsic_type\\\", \\\"String.byte_at\\\", \\\"strings\\\"),\\n     RejectCase(\\\"receivers/reject_write_shared_record\\\", \\\"cannot assign through a shared reference\\\", \\\"receivers\\\"),\\n     RejectCase(\\\"receivers/reject_immutable_record_borrow\\\", \\\"cannot mutably borrow an immutable place\\\", \\\"receivers\\\"),\\n     RejectCase(\\\"receivers/reject_immutable_receiver\\\", \\\"cannot call a mutating method on an immutable receiver\\\", \\\"receivers\\\"),\\n@@ -214,7 +242,7 @@ REJECT_CASES = (\\n     RejectCase(\\\"control/reject_static_typed_narrowing\\\", \\\"convert\\\", \\\"control\\\"),\\n     RejectCase(\\\"control/reject_static_function_collision\\\", \\\"conflicts\\\", \\\"control\\\"),\\n     RejectCase(\\\"records/reject_builtin_name\\\", \\\"reserved\\\", \\\"records\\\"),\\n-    RejectCase(\\\"records/reject_record_equality\\\", \\\"scalar\\\", \\\"records\\\"),\\n+    RejectCase(\\\"records/reject_record_equality\\\", \\\"comparable type\\\", \\\"records\\\"),\\n     RejectCase(\\\"records/reject_record_print\\\", \\\"print\\\", \\\"records\\\"),\\n     RejectCase(\\\"records/reject_empty_record\\\", \\\"field\\\", \\\"records\\\"),\\n     RejectCase(\\\"numbers/reject_byte_literal\\\", \\\"Byte\\\", \\\"numbers\\\"),\\n@@ -227,7 +255,6 @@ REJECT_CASES = (\\n     RejectCase(\\\"reject_duplicate\\\", \\\"duplicate declaration\\\"),\\n     RejectCase(\\\"reject_missing_return\\\", \\\"may reach its end without returning\\\"),\\n     RejectCase(\\\"reject_semicolon\\\", \\\"semicolons are not supported\\\"),\\n-    RejectCase(\\\"reject_string\\\", \\\"expected expression\\\"),\\n     RejectCase(\\\"reject_bool_arithmetic\\\", \\\"arithmetic requires integer operands\\\"),\\n     RejectCase(\\\"reject_import\\\", \\\"only function, record, enum, static, and extension declarations are supported\\\"),\\n     RejectCase(\\\"reject_scope_leak\\\", \\\"unknown name\\\"),\\n@@ -370,7 +397,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -399,7 +426,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\") else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\", \\\"receivers\\\", \\\"strings\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\ndiff --git a/prototypes/minivm/tests/enum_contract.saw b/prototypes/minivm/tests/enum_contract.saw\\nindex 0cc1ee60..0244a0cb 100644\\n--- a/prototypes/minivm/tests/enum_contract.saw\\n+++ b/prototypes/minivm/tests/enum_contract.saw\\n@@ -67,7 +67,7 @@ func base_program() -> Program {\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move identity_params,\\n         result: ValueType.Enum(0), slots: move identity_slots,\\n         code: move identity_code))\\n-    Program(references: Vector<ReferenceIR>(), enums: move enums, records: move records,\\n+    Program(strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: move enums, records: move records,\\n             functions: move functions, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/receiver_contract.saw b/prototypes/minivm/tests/receiver_contract.saw\\nindex 126d8b55..ca76a4ea 100644\\n--- a/prototypes/minivm/tests/receiver_contract.saw\\n+++ b/prototypes/minivm/tests/receiver_contract.saw\\n@@ -57,7 +57,7 @@ func base_program() -> Program {\\n     var funcs = Vector<FuncIR>()\\n     try! funcs.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n         slots: move slots, code: move code))\\n-    Program(references: move refs, enums: Vector<EnumIR>(), records: move records,\\n+    Program(strings: Vector<String>(), references: move refs, enums: Vector<EnumIR>(), records: move records,\\n         functions: move funcs, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/record_contract.saw b/prototypes/minivm/tests/record_contract.saw\\nindex 98bfdbc7..08f9c7b0 100644\\n--- a/prototypes/minivm/tests/record_contract.saw\\n+++ b/prototypes/minivm/tests/record_contract.saw\\n@@ -46,7 +46,7 @@ func call_program(records: &Vector<RecordIR>) -> Program {\\n         slots: slots.copy(), code: move main_code))\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move params, result: ValueType.Record(0),\\n         slots: move slots, code: move callee_code))\\n-    Program(references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(), records: records.copy(), functions: move functions, main_index: 0)\\n+    Program(strings: Vector<String>(), references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(), records: records.copy(), functions: move functions, main_index: 0)\\n }\\n \\n func main() {\\ndiff --git a/prototypes/minivm/tests/reference_contract.saw b/prototypes/minivm/tests/reference_contract.saw\\nindex 248d868f..393fe3ac 100644\\n--- a/prototypes/minivm/tests/reference_contract.saw\\n+++ b/prototypes/minivm/tests/reference_contract.saw\\n@@ -55,7 +55,7 @@ func base_program() -> Program {\\n         slots: move slots, code: move code))\\n     try! funcs.push(FuncIR(name: \\\"read\\\", params: move read_params, result: ValueType.I64,\\n         slots: move read_slots, code: move read_code))\\n-    Program(references: move refs, enums: move enums, records: Vector<RecordIR>(),\\n+    Program(strings: Vector<String>(), references: move refs, enums: move enums, records: Vector<RecordIR>(),\\n         functions: move funcs, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/string_contract.saw b/prototypes/minivm/tests/string_contract.saw\\nnew file mode 100644\\nindex 00000000..9f7b5d5f\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/string_contract.saw\\n@@ -0,0 +1,107 @@\\n+import src.model.*\\n+import src.verify.*\\n+import src.vm.*\\n+\\n+func valid(p: &Program) -> Bool {\\n+    try verify(&p) catch { return false }\\n+    true\\n+}\\n+\\n+func op(code: &var Vector<Instruction>, kind: Op, dst: Int, a: Int, b: Int, immediate: Int) {\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    i.immediate = immediate\\n+    try! code.push(move i)\\n+}\\n+\\n+func base_program() -> Program {\\n+    let strings: Vector<String> = [\\\"abc\\\", \\\"\\\"]\\n+    var refs = Vector<ReferenceIR>()\\n+    try! refs.push(ReferenceIR(target: ValueType.String, mutable: true))\\n+    try! refs.push(ReferenceIR(target: ValueType.String, mutable: false))\\n+    let slots: Vector<ValueType> = [ValueType.String, ValueType.String,\\n+        ValueType.I64, ValueType.I64, ValueType.Byte, ValueType.Bool,\\n+        ValueType.Ref(0), ValueType.Ref(1), ValueType.String, ValueType.String]\\n+    var code = Vector<Instruction>()\\n+    op(&var code, Op.StringConst, 0, -1, -1, 0)\\n+    op(&var code, Op.Copy, 1, 0, -1, 0)\\n+    op(&var code, Op.Const, 2, -1, -1, 1)\\n+    op(&var code, Op.StringLen, 3, 0, -1, 0)\\n+    op(&var code, Op.StringSlice, 1, 0, 2, 0)\\n+    try! code[4].args.push(3)\\n+    op(&var code, Op.StringByteAt, 4, 1, 2, 0)\\n+    op(&var code, Op.Eq, 5, 0, 1, 0)\\n+    op(&var code, Op.Addr, 6, 0, -1, 0)\\n+    op(&var code, Op.Copy, 7, 6, -1, 0)\\n+    op(&var code, Op.LoadRef, 8, 7, -1, 0)\\n+    op(&var code, Op.StoreRef, -1, 6, 1, 0)\\n+    op(&var code, Op.Call, 9, -1, -1, 0)\\n+    code[11].target = 1\\n+    try! code[11].args.push(0)\\n+    op(&var code, Op.Drop, -1, 9, -1, 0)\\n+    op(&var code, Op.Drop, -1, 9, -1, 0)\\n+    op(&var code, Op.Return, -1, -1, -1, 0)\\n+    let params: Vector<ValueType> = [ValueType.String]\\n+    let echo_slots: Vector<ValueType> = [ValueType.String]\\n+    var echo_code = Vector<Instruction>()\\n+    op(&var echo_code, Op.Return, -1, 0, -1, 0)\\n+    var funcs = Vector<FuncIR>()\\n+    try! funcs.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: move slots, code: move code))\\n+    try! funcs.push(FuncIR(name: \\\"echo\\\", params: move params, result: ValueType.String,\\n+        slots: move echo_slots, code: move echo_code))\\n+    Program(strings: move strings, references: move refs, enums: Vector<EnumIR>(),\\n+        records: Vector<RecordIR>(), functions: move funcs, main_index: 0)\\n+}\\n+\\n+func rejected(p: &Program, index: Int, kind: Op, dst: Int, a: Int, b: Int, immediate: Int) -> Bool {\\n+    var changed = p.copy()\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    i.immediate = immediate\\n+    changed.functions[0].code.set(index, move i)\\n+    not valid(&changed)\\n+}\\n+\\n+func main() {\\n+    let p = base_program()\\n+    assert(valid(&p), \\\"String literals, copies, slices, references and calls\\\")\\n+    try! execute(&p, 1000)\\n+    assert(not scalar_type(ValueType.String), \\\"owning String is not scalar data\\\")\\n+    assert(printable_type(ValueType.String), \\\"String is printable\\\")\\n+    assert((try! value_layout(&p.records, ValueType.String)).len() == 1, \\\"String logical word\\\")\\n+    assert(rejected(&p, 0, Op.StringConst, 0, -1, -1, -1), \\\"negative pool index\\\")\\n+    assert(rejected(&p, 0, Op.StringConst, 0, -1, -1, 2), \\\"high pool index\\\")\\n+    assert(rejected(&p, 0, Op.StringConst, 2, -1, -1, 0), \\\"StringConst destination\\\")\\n+    assert(rejected(&p, 0, Op.Const, 0, -1, -1, 0), \\\"Const cannot forge String\\\")\\n+    assert(rejected(&p, 3, Op.StringLen, 4, 0, -1, 0), \\\"len result Int\\\")\\n+    assert(rejected(&p, 3, Op.StringLen, 3, 2, -1, 0), \\\"len source String\\\")\\n+    assert(rejected(&p, 5, Op.StringByteAt, 4, 1, 0, 0), \\\"byte index Int\\\")\\n+    assert(rejected(&p, 5, Op.StringByteAt, 2, 1, 2, 0), \\\"byte result Byte\\\")\\n+    assert(rejected(&p, 4, Op.StringSlice, 1, 0, 2, 0), \\\"slice end argument required\\\")\\n+    var bad_end = p.copy()\\n+    bad_end.functions[0].code[4].args.set(0, 5)\\n+    assert(not valid(&bad_end), \\\"slice end must be Int\\\")\\n+    var high_end = p.copy()\\n+    high_end.functions[0].code[4].args.set(0, 99)\\n+    assert(not valid(&high_end), \\\"slice end slot must exist\\\")\\n+    var many_end = p.copy()\\n+    try! many_end.functions[0].code[4].args.push(3)\\n+    assert(not valid(&many_end), \\\"slice exactly one end argument\\\")\\n+    assert(rejected(&p, 12, Op.Drop, -1, 2, -1, 0), \\\"Drop requires owning String\\\")\\n+    assert(rejected(&p, 6, Op.Eq, 5, 0, 2, 0), \\\"String equality requires same type\\\")\\n+    assert(rejected(&p, 1, Op.Cast, 2, 0, -1, 0), \\\"String cannot expose handle\\\")\\n+    assert(rejected(&p, 1, Op.Copy, 0, 2, -1, 0), \\\"Copy cannot forge String\\\")\\n+    assert(rejected(&p, 10, Op.StoreRef, -1, 7, 1, 0), \\\"String reference must be mutable\\\")\\n+    var field = p.copy()\\n+    let names: Vector<String> = [\\\"text\\\"]\\n+    let types: Vector<ValueType> = [ValueType.String]\\n+    try! field.records.push(RecordIR(name: \\\"Owning\\\", field_names: move names,\\n+        field_types: move types, line: 1, col: 1))\\n+    assert(not valid(&field), \\\"String record ownership deferred\\\")\\n+    print(\\\"string contract passed\\\")\\n+}\\n\",\"id\":\"SL-251\",\"request_id\":\"mv2-SL-251-9677cb7d-a1\",\"title\":\"minivm M8: owned Strings + refcounted heap (branch commit 9677cb7d)\"}","request_id":"mv2-SL-251-9677cb7d-a1","sequence":487,"timestamp":"1789097774"}
---
