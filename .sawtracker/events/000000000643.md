---
{"action":"comment","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl212","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sl212","body":"UNIT C COMPLETE on branch `worktree-agent-a4ccffc8947f41b03`, tip `901940f5`,\nrebased onto `origin/main` (`74ea88e0`). NOT submitted — the lead squashes and\nsubmits. Behaviour-preserving: same programs accepted, same programs rejected,\nsame diagnostics, same annotations.\n\nBrief: `designs/270-decision-preservation.md` (number PROVISIONAL, stated in the\ndoc).\n\n## What the measurement found, per pass\n\nSPECIALIZATION already re-derives, and that is the RIGHT mechanism: one template\ndecision that cannot answer becomes two instance decisions that do, at two\ndifferent tiers. Carrying a decision across that boundary would be this issue's\nown forbidden shape, so unit C proves re-derivation rather than transporting\nkeys. CLOSURE CONVERSION and PLACE LOWERING: zero detachments. THE COROUTINE\nTRANSFORM holds the three real holes — it moves a transfer to a different site\n(26 of one fixture's 75 decisions sit at line 0 column 0, so a position-keyed\naudit is unsound and not merely imprecise), it CHANGES the action at a site it\nkeeps (`move` -> `take`), and a frame read defers to a settlement nothing\nrecorded.\n\n## What was built\n\n`ASTNode.origin_node_id` (declared annotation, stamped by the two clone\nproducers, recording the ROOT of the chain — an instance is cloned from a\npristine snapshot nothing type-checks, so naming the immediate parent resolved 1\nof 2175 links and the root resolves 1919 of 2235);\n`TransferDecision.antecedent`/`.discharge` with a CLOSED five-member discharge\nset; and ONE funnel, `sawc/typechecker/preservation.py`, four properties, its\ndocstring naming the four passes. `SAW_TRANSFER_AUDIT=1`/`strict` runs it on\nevery compile; the `transferdecisions` lane runs it over a five-program corpus\nand ASSERTS ITS OWN COVERAGE, so a vacuous pass fails.\n\n## Findings filed, not fixed (both change an observable)\n\nSL-267 — a driven body's non-escaping closure capture duplicates twice where its\nsync twin duplicates once; at the ceremony tier the driven body computes a\nDIFFERENT VALUE. Pinned by conformance row V96, XFAIL, cited.\n\nSL-268 — a `[copy x]` capture into a non-escaping closure leaks the copy; a\nsync-side leak and a new member of SL-220's class (design 267 had already named\n`[copy x]` as the one capture mode reaching no ownership funnel). Routed to unit\nD with SL-220.\n\n## Gates\n\nFull suite 2485 passed / 10 xfailed. Freestanding 36 passed across riscv32 +\narm64. Terminal battery: 27 stages GREEN in 3142s.\n","created":"1789166278","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-212.p1 (revision 1): design 270: transfer decisions provably survive every lowering (SL-212 unit C) — preservation audit funnel with per-rule demonstrated teeth","created":"1789166407","id":"c2"},{"author":"agent:claude-sawlang","body":"SL-212.p1 SUBMITTED (unit C, tip 901940f5 on 74ea88e0), server gate queued — LEAD-VALIDATED. Design 270: the preservation audit (sawc/typechecker/preservation.py, ONE entry point run once on the finished program, docstring names the four covered passes). Findings per pass: specialization RE-DERIVES (proven, the right mechanism — template decision splits into per-tier instance decisions); closure conversion + place lowering clean; the coro transform held all three measured holes (moved sites — 26/75 decisions at line 0, so the audit keys on IDENTITY never position; changed actions recorded as discharges; frame-read settlements grounded). Mechanism: origin_node_id declared annotation stamped by the two clone producers (root-of-chain, resolving 1919/2235 vs 1/2175 for parent-keying); antecedent is a node id not a key (context-keying false-fired on a third of the coro corpus); discharge is a closed five-member set. TEETH DEMONSTRATED per rule (break applied/run/reverted: P1, P2, P4 each fire with named reports) and coverage counters assert non-zero — which already forced a corpus addition (coro_closure_capture_positions.saw). Lead independently ran the extended transferdecisions lane on the branch: 14 fixtures, 35820 decisions, preservation audit 10958 antecedents / no decision detached. Conformance V93-V98 landed FIRST; V96 is a cited XFAIL for SL-267 (new finding: driven non-escaping capture duplicates twice; SL-268 also filed: [copy x] non-escaping capture leaks — both behavior-changing, correctly routed to unit D with SL-220). REVIEWER FOCUS: (1) the antecedent-not-key argument; (2) the per-declaration audit scope with mono-instance-always-in-scope (the audit must not go quiet when a pass stops checking instances); (3) one judgement call slightly past the brief: payload-read delegations also carry a discharge (completeness at no cost — lead accepts, flagging for a second opinion). Gates: suite 2485/10x, freestanding 36/36, battery 27/27 (3142s).","created":"1789166421","id":"c3"}],"created":"1788966180","description":"Unit C of SL-209 (ownership uniformity). Plan step 5. Depends on Units A/B. SEQUENCE AFTER SL-208 lands (see note).\n\n## Preserve the contract through compiler transformations\nPlace lowering, generic specialization, closure conversion, and coroutine lowering change expression shapes. Each transformation must EITHER:\n- preserve an existing ownership decision and remap its source identity; OR\n- introduce a new transfer that goes through the same checking operation.\n\nUse design 266's `admit_declarations` path (`sawc/sawc.py:1183`) for generated declarations, while VERIFYING that it also covers newly generated transfers. Rechecking must not consume a source twice or add a second retain. Generic specialization must RESOLVE deferred requirements (Unit A's \"generic copy requirement awaiting specialization\") rather than blindly reuse decisions tied to an abstract type or another instance.\n\n## Acceptance\nSync and suspending versions have equivalent ownership behavior; repeated compilation does not accumulate ownership annotations or cleanup actions (ties directly to the irdet/reemit determinism gates — a decision that is re-stamped per compile would break byte-identity).\n\n## Sequencing note\nThis unit touches the coroutine-transform lowering path, which is exactly where SL-208 (DF-300e, cross-module frame materialization / token propagation) is being fixed right now. Land SL-208 first, then build this unit on top so the two do not fight over the same frame-materialization seam.\n","id":"SL-212","labels":["compiler","ownership"],"priority":"normal","project":"SL","revision":6,"sequence":643,"stage":"queued","status":"open","title":"Ownership Unit C: preserve transfer decisions through lowering/specialization/coro (SL-209)","updated":"1789166421"},"before":{"assignee":"agent:claude-sl212","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sl212","body":"UNIT C COMPLETE on branch `worktree-agent-a4ccffc8947f41b03`, tip `901940f5`,\nrebased onto `origin/main` (`74ea88e0`). NOT submitted — the lead squashes and\nsubmits. Behaviour-preserving: same programs accepted, same programs rejected,\nsame diagnostics, same annotations.\n\nBrief: `designs/270-decision-preservation.md` (number PROVISIONAL, stated in the\ndoc).\n\n## What the measurement found, per pass\n\nSPECIALIZATION already re-derives, and that is the RIGHT mechanism: one template\ndecision that cannot answer becomes two instance decisions that do, at two\ndifferent tiers. Carrying a decision across that boundary would be this issue's\nown forbidden shape, so unit C proves re-derivation rather than transporting\nkeys. CLOSURE CONVERSION and PLACE LOWERING: zero detachments. THE COROUTINE\nTRANSFORM holds the three real holes — it moves a transfer to a different site\n(26 of one fixture's 75 decisions sit at line 0 column 0, so a position-keyed\naudit is unsound and not merely imprecise), it CHANGES the action at a site it\nkeeps (`move` -> `take`), and a frame read defers to a settlement nothing\nrecorded.\n\n## What was built\n\n`ASTNode.origin_node_id` (declared annotation, stamped by the two clone\nproducers, recording the ROOT of the chain — an instance is cloned from a\npristine snapshot nothing type-checks, so naming the immediate parent resolved 1\nof 2175 links and the root resolves 1919 of 2235);\n`TransferDecision.antecedent`/`.discharge` with a CLOSED five-member discharge\nset; and ONE funnel, `sawc/typechecker/preservation.py`, four properties, its\ndocstring naming the four passes. `SAW_TRANSFER_AUDIT=1`/`strict` runs it on\nevery compile; the `transferdecisions` lane runs it over a five-program corpus\nand ASSERTS ITS OWN COVERAGE, so a vacuous pass fails.\n\n## Findings filed, not fixed (both change an observable)\n\nSL-267 — a driven body's non-escaping closure capture duplicates twice where its\nsync twin duplicates once; at the ceremony tier the driven body computes a\nDIFFERENT VALUE. Pinned by conformance row V96, XFAIL, cited.\n\nSL-268 — a `[copy x]` capture into a non-escaping closure leaks the copy; a\nsync-side leak and a new member of SL-220's class (design 267 had already named\n`[copy x]` as the one capture mode reaching no ownership funnel). Routed to unit\nD with SL-220.\n\n## Gates\n\nFull suite 2485 passed / 10 xfailed. Freestanding 36 passed across riscv32 +\narm64. Terminal battery: 27 stages GREEN in 3142s.\n","created":"1789166278","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-212.p1 (revision 1): design 270: transfer decisions provably survive every lowering (SL-212 unit C) — preservation audit funnel with per-rule demonstrated teeth","created":"1789166407","id":"c2"}],"created":"1788966180","description":"Unit C of SL-209 (ownership uniformity). Plan step 5. Depends on Units A/B. SEQUENCE AFTER SL-208 lands (see note).\n\n## Preserve the contract through compiler transformations\nPlace lowering, generic specialization, closure conversion, and coroutine lowering change expression shapes. Each transformation must EITHER:\n- preserve an existing ownership decision and remap its source identity; OR\n- introduce a new transfer that goes through the same checking operation.\n\nUse design 266's `admit_declarations` path (`sawc/sawc.py:1183`) for generated declarations, while VERIFYING that it also covers newly generated transfers. Rechecking must not consume a source twice or add a second retain. Generic specialization must RESOLVE deferred requirements (Unit A's \"generic copy requirement awaiting specialization\") rather than blindly reuse decisions tied to an abstract type or another instance.\n\n## Acceptance\nSync and suspending versions have equivalent ownership behavior; repeated compilation does not accumulate ownership annotations or cleanup actions (ties directly to the irdet/reemit determinism gates — a decision that is re-stamped per compile would break byte-identity).\n\n## Sequencing note\nThis unit touches the coroutine-transform lowering path, which is exactly where SL-208 (DF-300e, cross-module frame materialization / token propagation) is being fixed right now. Land SL-208 first, then build this unit on top so the two do not fight over the same frame-materialization seam.\n","id":"SL-212","labels":["compiler","ownership"],"priority":"normal","project":"SL","revision":5,"sequence":642,"stage":"queued","status":"open","title":"Ownership Unit C: preserve transfer decisions through lowering/specialization/coro (SL-209)","updated":"1789166407"},"id":"SL-212","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sawlang\",\"body\":\"SL-212.p1 SUBMITTED (unit C, tip 901940f5 on 74ea88e0), server gate queued — LEAD-VALIDATED. Design 270: the preservation audit (sawc/typechecker/preservation.py, ONE entry point run once on the finished program, docstring names the four covered passes). Findings per pass: specialization RE-DERIVES (proven, the right mechanism — template decision splits into per-tier instance decisions); closure conversion + place lowering clean; the coro transform held all three measured holes (moved sites — 26/75 decisions at line 0, so the audit keys on IDENTITY never position; changed actions recorded as discharges; frame-read settlements grounded). Mechanism: origin_node_id declared annotation stamped by the two clone producers (root-of-chain, resolving 1919/2235 vs 1/2175 for parent-keying); antecedent is a node id not a key (context-keying false-fired on a third of the coro corpus); discharge is a closed five-member set. TEETH DEMONSTRATED per rule (break applied/run/reverted: P1, P2, P4 each fire with named reports) and coverage counters assert non-zero — which already forced a corpus addition (coro_closure_capture_positions.saw). Lead independently ran the extended transferdecisions lane on the branch: 14 fixtures, 35820 decisions, preservation audit 10958 antecedents / no decision detached. Conformance V93-V98 landed FIRST; V96 is a cited XFAIL for SL-267 (new finding: driven non-escaping capture duplicates twice; SL-268 also filed: [copy x] non-escaping capture leaks — both behavior-changing, correctly routed to unit D with SL-220). REVIEWER FOCUS: (1) the antecedent-not-key argument; (2) the per-declaration audit scope with mono-instance-always-in-scope (the audit must not go quiet when a pass stops checking instances); (3) one judgement call slightly past the brief: payload-read delegations also carry a discharge (completeness at no cost — lead accepts, flagging for a second opinion). Gates: suite 2485/10x, freestanding 36/36, battery 27/27 (3142s).\",\"id\":\"SL-212\",\"request_id\":\"pid-10076-1789166421-8000\"}","request_id":"pid-10076-1789166421-8000","sequence":643,"timestamp":"1789166421"}
---
