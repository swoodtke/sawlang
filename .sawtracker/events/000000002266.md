---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"1790341705","comments":[{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): superseded by SL:architecture §1 and §5. The Python compiler is frozen, so these findings are oracle-known-wrong references, not fixes. Its children stay open where they must: SL-340 (an XFAIL pin cites it), and SL-354 and SL-355 (runtime work, which carries over).","created":"1790341705","id":"c1","kind":"landing"}],"created":"1790188438","description":"# Must-fix epic: soundness, liveness and ICE findings surfaced by design 275's landing\n\nRULED by the user, Sep 23 2026, at the close of SL-318 (design 275). This epic is the ONE carrier for the correctness findings that epic's units and reviews produced but did not own. Membership = \"a program that is accepted must not corrupt memory, hang, panic on valid code, or crash the compiler\". Each member keeps its own issue (brief, evidence, landing note); this epic holds ORDER and STATUS only.\n\n## Members, in the lead's recommended order (the user re-sequences at will)\n1. **SL-355** — the park-word race (lost wake, MT hang). HIGH. IN PROGRESS: `agent:claude-air-sawlang` (Opus 5.5), Atomic + CAS latch per the user's ruling; carries **SL-354** (the arm reports a failed kevent). Lands after SL-350 (done) — its rebase onto 8d2c0563 is under way.\n2. **SL-345** — a closure's captured reference-param root is not charged against the receiver of the call it is passed to: `v.with_var_ref(0, { e in try! v.push(k); e = 77 })` compiles and the write lands in freed memory. HIGH, soundness. Needs its fix brief (design-199 funnel: closure borrow captures join the call's access set). Unassigned.\n3. **SL-340** — `Mutex(value: move v)` at a heap-owning payload frees the payload at construction (use-after-free); K141 is its XFAIL pin. Soundness. Unassigned.\n4. **SL-349** — a DRIVEN derived shadow of a module static (`var ROOT = Owner(value: ROOT.value + 1)` after a suspension) panics `Slot.value: slot is empty` on valid code. Unassigned.\n5. **SL-356** — ICE: the closure generator forgets the enclosing function's Void-typed locals (`void_variables` reset without save/restore). IN PROGRESS: `agent:claude-sl356` (one patch with **SL-362**, the pin for the generic-struct field retain SL-350 fixed as a side effect).\n6. **SL-364** — ICE: an `Int??` parameter of a suspending function. Unassigned.\n7. **SL-363** — an oversized CONSTANT fixed-array literal still reaches LLVM's 65535-leaf limit (SL-336's remaining sibling; a compiler abort on valid code). Unassigned.\n8. **SL-348** — a SYNC abandoned call's already-moved argument is released by nothing (a leak, the orphan ordering; SL-339's sync twin). Unassigned.\n\n## Not members (tracked elsewhere)\nDesign follow-ups from U3 — SL-342 (generic scoped-borrow statement), SL-343 (with_ref/lend windows across a suspension), SL-344 (exclusive-window derivation) — and SL-322 (heap frames, the K37 refusal, pending by ruling) are DESIGN work for the user to sequence, not must-fix. Diagnostics/quality: SL-346, SL-351, SL-352 (backlog). SL-357 (comment cleanup, Air) is its own stream.\n\n## Landing rule\nEach member lands as its own patch under the ordinary gates (a soundness member updates its `examples/conformance/` rows FIRST, obligation 3; a finding is presumed a CLASS until its sweep says otherwise, obligation 4). The epic closes when every member is closed; a member the user demotes moves to the backlog with a note here.\n","id":"SL-367","labels":["superseded","epic","soundness"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":3,"sequence":2266,"stage":"queued","status":"closed","title":"EPIC: must-fix soundness, liveness and ICE findings from design 275's landing (SL-355+354, SL-345, SL-340, SL-349, SL-356+362, SL-364, SL-363, SL-348)","updated":"1790341705"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790188438","description":"# Must-fix epic: soundness, liveness and ICE findings surfaced by design 275's landing\n\nRULED by the user, Sep 23 2026, at the close of SL-318 (design 275). This epic is the ONE carrier for the correctness findings that epic's units and reviews produced but did not own. Membership = \"a program that is accepted must not corrupt memory, hang, panic on valid code, or crash the compiler\". Each member keeps its own issue (brief, evidence, landing note); this epic holds ORDER and STATUS only.\n\n## Members, in the lead's recommended order (the user re-sequences at will)\n1. **SL-355** — the park-word race (lost wake, MT hang). HIGH. IN PROGRESS: `agent:claude-air-sawlang` (Opus 5.5), Atomic + CAS latch per the user's ruling; carries **SL-354** (the arm reports a failed kevent). Lands after SL-350 (done) — its rebase onto 8d2c0563 is under way.\n2. **SL-345** — a closure's captured reference-param root is not charged against the receiver of the call it is passed to: `v.with_var_ref(0, { e in try! v.push(k); e = 77 })` compiles and the write lands in freed memory. HIGH, soundness. Needs its fix brief (design-199 funnel: closure borrow captures join the call's access set). Unassigned.\n3. **SL-340** — `Mutex(value: move v)` at a heap-owning payload frees the payload at construction (use-after-free); K141 is its XFAIL pin. Soundness. Unassigned.\n4. **SL-349** — a DRIVEN derived shadow of a module static (`var ROOT = Owner(value: ROOT.value + 1)` after a suspension) panics `Slot.value: slot is empty` on valid code. Unassigned.\n5. **SL-356** — ICE: the closure generator forgets the enclosing function's Void-typed locals (`void_variables` reset without save/restore). IN PROGRESS: `agent:claude-sl356` (one patch with **SL-362**, the pin for the generic-struct field retain SL-350 fixed as a side effect).\n6. **SL-364** — ICE: an `Int??` parameter of a suspending function. Unassigned.\n7. **SL-363** — an oversized CONSTANT fixed-array literal still reaches LLVM's 65535-leaf limit (SL-336's remaining sibling; a compiler abort on valid code). Unassigned.\n8. **SL-348** — a SYNC abandoned call's already-moved argument is released by nothing (a leak, the orphan ordering; SL-339's sync twin). Unassigned.\n\n## Not members (tracked elsewhere)\nDesign follow-ups from U3 — SL-342 (generic scoped-borrow statement), SL-343 (with_ref/lend windows across a suspension), SL-344 (exclusive-window derivation) — and SL-322 (heap frames, the K37 refusal, pending by ruling) are DESIGN work for the user to sequence, not must-fix. Diagnostics/quality: SL-346, SL-351, SL-352 (backlog). SL-357 (comment cleanup, Air) is its own stream.\n\n## Landing rule\nEach member lands as its own patch under the ordinary gates (a soundness member updates its `examples/conformance/` rows FIRST, obligation 3; a finding is presumed a CLASS until its sweep says otherwise, obligation 4). The epic closes when every member is closed; a member the user demotes moves to the backlog with a note here.\n","id":"SL-367","labels":["superseded","epic","soundness"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":2,"sequence":2229,"stage":"queued","status":"open","title":"EPIC: must-fix soundness, liveness and ICE findings from design 275's landing (SL-355+354, SL-345, SL-340, SL-349, SL-356+362, SL-364, SL-363, SL-348)","updated":"1790341684"},"id":"SL-367","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): superseded by SL:architecture §1 and §5. The Python compiler is frozen, so these findings are oracle-known-wrong references, not fixes. Its children stay open where they must: SL-340 (an XFAIL pin cites it), and SL-354 and SL-355 (runtime work, which carries over).\",\"force\":true,\"id\":\"SL-367\",\"request_id\":\"pid-58049-1790341705-7000\"}","request_id":"pid-58049-1790341705-7000","sequence":2266,"timestamp":"1790341705"}
---
