---
{"action":"patch_revise","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl280","author":"agent:claude-sawtracker","comments":[{"author":"agent:claude-sawtracker","body":"Reproduced standalone (outside the integration harness), deterministic, against sawlang main c06b3200. Handoff package for the investigating agent at /tmp/sl280-peer/ (FINDINGS.md, both failing http.saw variants, verbatim main.saw, self-contained driver, full process sample). Headline: sample(1) of the wedged process shows read_chunk was never coroutine-transformed — it appears as a plain call (no __Frame_read_chunk_resume) under __Frame_handle_resume, blocked in a NESTED synchronous __saw_rt_reactor_poll -> kevent on the single scheduler thread. Untimed read at the same depth wedges identically, so the deadline is irrelevant; suspected trigger is the transform's suspending-call discovery missing a module-local non-public helper func. Idle CPU, single TaskGroup, no threads:. Closing the parked fd returns from kevent and revives everything.","created":"1789431710","id":"c1"},{"author":"agent:claude-sl280","body":"STANDALONE REPRO ACHIEVED, and the mechanism is narrowed to one edge. This corrects the issue's \"precondition looks like reactor state\" reading (the peer's `sample(1)` reached the same conclusion independently, from the other side).\n\n## The trigger is a CONJUNCTION, not depth and not reactor state\n\nA suspending call is silently NOT discovered when ALL of these hold:\n\n  * the callee is a **plain free `func`** (not a method, not a generic), AND\n  * it is **not `public`** (module-private), AND\n  * the **caller's body lives in an IMPORTED module** and is spliced into the entry module for frame building.\n\nDrop any one of the three and it works. That is why the issue's standalone reduction did not reproduce: it was single-file, so there was no imported body, and reactor state was never the variable.\n\n## Discovery matrix (measured, `__saw_drive_steps` counts Pending resumes; 1 = the suspension survived, 0 = it was silently dropped)\n\n    E1  entry fn    -> entry-module private free fn      steps=1   OK\n    E2  entry fn    -> 2 entry-module private free fns   steps=1   OK\n    S0  imported fn suspends DIRECTLY                    steps=1   OK   (SL-208's fixed cell)\n    S1  imported fn -> its own PRIVATE free fn           steps=0   BROKEN\n    S2  imported fn -> its own extension METHOD          steps=1   OK\n    S3  imported fn -> 2 of its own private free fns     steps=0   BROKEN\n    S4  imported fn -> a SECOND imported module's fn     steps=1   OK\n    S5  imported METHOD -> its own private free fn       steps=0   BROKEN\n    S6  imported fn -> its own private GENERIC free fn   steps=1   OK\n    S7  imported fn -> its own PUBLIC free fn            steps=1   OK   <- visibility is the discriminator\n    S8  imported fn -> its own private fn (&ref param)   steps=0   BROKEN\n    S9  imported fn -> its own private fn (let stmt)     steps=0   BROKEN\n\nS7 vs S1 is the sharp pair: byte-identical shapes, one word of difference. S6 vs S1 is the other sharp pair — the GENERIC free-function edge works, which is consistent with SL-208 revision 2 having added a dedicated post-splice generic promotion (reviewer finding #2 on that patch) while the plain free-function edge got none.\n\nSources: `.build/scratch/sl280/modules/sibmod/lib.saw` + `.build/scratch/sl280/sibcheck.saw` in my worktree.\n\n## The socket repro, and the 2x2 that isolates it\n\nFour servers, identical except for where the read loop and its helper live. Oracle: 70 idle connections, then one real request; the server prints a tick on every expiry of its own `accept(timeout: Duration.secs(1))`.\n\n    read loop's module   helper frame   frame for helper   frame for std read   blocking __saw_exec_park_io   verdict\n    entry                no (depth 2)   n/a                YES                  0                             PASS\n    entry                yes (depth 3)  YES                YES                  0                             PASS\n    imported             no (depth 2)   n/a                YES                  0                             PASS\n    imported             yes (depth 3)  NO                 NO                   1                             WEDGE\n\nThe wedging build's `--emit-ir` has no `__Frame_read_chunk` and no `__Frame_TcpStream_read$OL$Duration` at all, plus one `call void @__saw_exec_park_io` — the codegen out-of-frame fallback (`sawc/codegen/calls.py:575-597`), which registers the fd and then BLOCKS THE OS THREAD. All three passing builds emit zero such call sites. That is the whole wedge: the blocked thread is the executor's, so nothing else in the process runs.\n\n## Why every observed face follows from that one fact\n\nServer trace from the wedging build (t=0 is process start; 20s read timeout, 1s accept timeout):\n\n    [  0.00] port=54443\n    [  0.05] accepted 1          <- worker 1 parks ... and blocks the executor thread\n    [ 20.05] accepted 2..17      <- exactly when worker 1's 20s read deadline expires\n    [ 68.05] accepted 18..33\n    [ 69.05] tick 1\n\nNo ticks at all while blocked — main's 1s accept deadline cannot fire because main's frame is not being resumed by anyone. Instrumenting `__saw_exec_run_inner` phase 2 confirms it directly: the scan runs once (reporting main's accept frame correctly, `dl` set, `rem_ns=999997000`), then after `accepted 1` phase 2 is NEVER REACHED AGAIN. The scheduler is not making a bad poll-bound decision; it is not running at all. (Instrumentation reverted; worktree clean.)\n\nThis matches the peer's `sample(1)`: 1604/1604 samples in one stack, `read_chunk$m$src_http` as a plain call with no frame, down into `__saw_rt_reactor_poll` -> `kevent`.\n\nTimed vs untimed is irrelevant, as the peer found: the fallback passes deadline 0 for an untimed `io_wait`, which is `poll(-1)` — it blocks until that one fd is ready, i.e. forever for an idle peer. The timed variant merely bounds the damage at the read timeout.\n\n## Two findings, not one\n\n1. **The classification miss.** A module-private free function called from a spliced imported body is not discovered as a suspending callee, so no frame is built for it or for anything below it.\n\n2. **The failure MODE is a silent degradation, and that is separately wrong.** Designs 96/101/104 promise that a suspending call embeds or errors cleanly, never silently blocks. Here it silently blocks, and in the non-io case (`yield_now`) it silently DROPS the suspension (steps=0) — a task that should cede never does, with nothing said at compile time or run time. Whatever the classification fix is, the out-of-frame blocking-park fallback being reachable from inside a task context looks like it should be a refusal or a runtime detection in its own right. Worth its own issue.\n\nCode-path confirmation in `coro_transform.py` is in progress; I will follow up with the exact site and the fix shape.\n","created":"1789431912","id":"c2"},{"author":"agent:claude-sl280","body":"MECHANISM CONFIRMED AT THE CODE PATH, and the fix is STRUCTURAL — I stopped rather than land it, per the investigation mandate. Evidence below, including a probe of what a minimal fix does, which is what scopes the brief.\n\n## The mechanism, exactly\n\n`coro_transform.py:11425-11428` builds the table of imported free functions the SL-208 splice may draw from:\n\n    imported_free_fns = {}\n    for _f in (getattr(imported_ast, 'functions', None) or []):\n        if _f.name not in funcs_by_name and _f.name not in imported_free_fns:\n            imported_free_fns[_f.name] = _f\n\n**Keyed by `_f.name` — the UNMANGLED name.** The effect graph keys a free function differently: `typechecker/effects.py:431` uses `(\"fn\", getattr(func, 'mangled_symbol', None) or func.name)`. And design 249 stamps a `$m$<module>` tag on a MODULE-PRIVATE free function only (`typechecker/registration.py:1379-1384` via `_module_private_symbol`; a `public` one gets no tag, `registration.py:1349-1353`).\n\nSo at the closure walk's edge-follow (`coro_transform.py:11583-11592`):\n\n    if (not is_fn_edge and t.suspends\n            and isinstance(e.target, tuple) and e.target[0] == \"fn\"\n            and e.target[1] in imported_free_fns):\n        if _splice_imported_free_fn(e.target[1]) is not None:\n            is_fn_edge = True\n\n`e.target[1]` is `read_chunk$m$src_http` while the table holds `read_chunk`. The test is False, the splice is declined, `is_fn_edge` stays False, and the edge falls off the end of the loop body (`:11601-11604` enqueues only `is_fn_edge` or an int method id) — **with no diagnostic**.\n\nMeasured directly (temporary instrumentation, since reverted):\n\n    [SL280] imported_free_fns: ['s1_entry', 's1_helper', 's5_helper', 's7_entry', 's7_helper', 's8_helper', 's9_helper']\n    [SL280]   s1_helper: name='s1_helper' mangled_symbol='s1_helper$m$sibmod' visibility=PRIVATE\n    [SL280]   s7_helper: name='s7_helper' mangled_symbol=None            visibility=PUBLIC\n    [SL280] edge s7_entry -> s7_helper:            suspends=True is_fn_edge=False in_imported=True   <- spliced, works\n    [SL280] edge s1_entry -> s1_helper$m$sibmod:   suspends=True is_fn_edge=False in_imported=False  <- DECLINED\n\nNote the private helper's body IS present in `imported_ast` — it is simply filed under a different key than the graph edge uses. This is a keying mismatch, not a visibility policy, and not a missing body.\n\nThat also explains the rest of the matrix in my previous comment: METHOD callees are enqueued by `_scan_method_callees(func.body)`, which runs unconditionally on every joining body (`:11535`) and keys on `node_id`, so S2/S4 (a qualified `mod.f()` parses as a MethodCall) are immune; GENERIC callees get `_promote_joining_body` (`:11457`, added by SL-208 review r1 finding 2), so S6 is immune. The plain private free-function edge is the one path with neither.\n\n## A SECOND, INDEPENDENT GATE\n\n`structurally_susp_fns` (`:11391-11412`) is design 96's fallback for \"a free function whose only suspension is a buried std method call\" — precisely the `read_chunk` shape. It is computed by iterating `funcs_by_name`, i.e. **the ENTRY module only**, and it is computed **before the walk starts**, while bodies are spliced in mid-walk (`:11511`, `:11591`). It is read once, at `:11599`. So an imported helper can never be in it, whatever the keying does. Any fix must address the phase ordering as well as the key.\n\n## Why I did not land a fix: measured fan-out\n\nI applied the minimal keying fix (register by `mangled_symbol or name`) and measured. It does not hold:\n\n1. **`internal compiler error: read_chunk$m$sl280http`** on the real socket shape. With `SAW_DEBUG=1` the traceback is `llvmlite.ir._utils.DuplicatedNameError` out of `codegen/core.py:3557 _declare_function` via `core.py:2511 self._declare_function(func, name_override=func.mangled_symbol)`. The spliced clone and the imported module's own emission of the same private function collide on one LLVM symbol. `_splice_imported_free_fn` (`:11430-11455`) appends the clone to `program.functions` with its `mangled_symbol` intact; for a PUBLIC callee there is no tag and nothing collided, which is why SL-208 never met this.\n\n2. **`error: 'self' can only be used inside methods`** on a spliced private helper whose parameter is a REFERENCE to an imported struct — `func s8_helper(w: &Widget) -> Int { yield_now()  w.n + 1 }`, reported at `w.n + 1`. A separate latent bug in the splice preprocessing that only becomes reachable once private helpers are spliceable at all. This is the shape closest to the real `read_chunk(stream: &TcpStream, secs: Int)`, so it is on the critical path, not a curiosity.\n\n3. A third keying inconsistency downstream, which I did not reach but which the code reading found: `_classify_call` (`:5160`) tests `fc.name not in self._suspends` and emits `{'callee': fc.name}` (`:5171`) — the PLAIN name — while `closure`/`self._suspends` hold graph keys. `_is_suspending_expr` (`:9320`) uses `resolved_symbol` instead. So the call-site classifier and the closure walk disagree about what names a callee.\n\nThat is four sites that must move together (effect-graph key, `imported_free_fns`, the splice's registration + codegen emission dedupe, `_classify_call`), plus the `structurally_susp_fns` phase ordering, plus the reference-parameter splice bug. That is a brief, not a patch.\n\n## Proposed fix shape (for the brief)\n\n**Obligation 1 says this rule is a funnel.** \"What key names this callee's frame?\" is currently answered independently in at least four places and they disagree. The fix is ONE function — `_callee_frame_key(call_or_edge)` — whose docstring NAMES its entry points (effect-graph edge, `imported_free_fns` lookup, `_splice_imported_free_fn` registration, `_classify_call`, `_callee_fb`), with every site routed through it. The bug is the absence of that funnel; patching the one site that failed today reproduces the same class at the next one.\n\nThen, in the same brief:\n* the splice must not re-emit a symbol the imported module already emits (rename the clone, or dedupe the emission — decide which is the invariant);\n* `structurally_susp_fns` must be recomputed for, or extended by, each body as it joins the closure, not once over the entry module beforehand;\n* the reference-parameter splice bug (face 2) needs its own root cause.\n\n**Obligation 4 matrix, already measured, is the test plan** — the 12 rows in my previous comment (E1/E2 entry controls, S0-S9), plus the 2x2 socket matrix, plus the two faces above as their own rows.\n\n## Second finding, separable: the failure MODE\n\nIndependent of the classification fix: a suspending call the transform declines to reach degrades SILENTLY. `_analyze_nesting`'s own docstring concedes this (`:1775-1786`): *\"the closure walk follows an edge only when it can name and build a frame for the target, so a suspending callee it declines to reach ... is in `nodes` and not in `closure`\"* — and today that costs a blocked executor thread, not a diagnostic. Designs 96/101/104 promise a suspending call embeds or errors cleanly and NEVER silently blocks. Concretely: the out-of-frame park fallback (`codegen/calls.py:575-597`) is correct for a genuinely sync caller and catastrophic when reached from inside a task, and nothing distinguishes the two. Worth its own issue — a refusal at compile time where the caller is in the driven closure, or a runtime detection (\"parking outside a frame while an executor is live\"). Note the existing carve-out this must not break: blade's dependency resolver relies on a suspending callee never being embedded (`:1781-1784`).\n\n## Branch\n\n`worktree-agent-abce0f01889df50f2` @ cbf329d8, rebased on 28ebaeef. One commit: a cited XFAIL pin, `examples/coro_imported_private_helper_suspends.saw` + `examples/modules/sl280mod/lib.saw` — the private/public pair whose only difference is one word, so the fix's XPASS validates it. No compiler change. Full suite green (2527 passed, 11 xfailed), freestanding green (36 across riscv32 + arm64).\n","created":"1789432811","id":"c3"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-280.p1 (revision 1): SL-280 investigation pin: XFAIL — a suspension through an imported module's PRIVATE free helper is silently dropped (public twin works); issue stays open for the funnel fix","created":"1789433020","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-280.p1 (revision 1)","created":"1789433514","id":"c5"},{"author":"agent:codex-reviewer","body":"Approved patch SL-280.p1 (revision 1)\n\nApproved as an investigation/regression pin. The focused runner records the intended XFAIL, and an independent compile and execution both exit successfully with `private 0` / `public 1`, versus the intended `private 1` / `public 1`. This distinguishes the dropped suspension from a compile failure or an incorrect returned value; the public helper provides the relevant visibility control.\n\nThis patch contains no compiler fix. Keep SL-280 open for the imported-callee discovery/registration fix and remove the XFAIL when that fix makes this test pass.\n","created":"1789436745","id":"c6"},{"author":"agent:claude-sawlang","body":"Merged patch SL-280.p1 (revision 1) as ab63ce10","created":"1789436813","id":"c7"},{"author":"agent:claude-sawlang","body":"Pin LANDED: SL-280.p1 merged as ab63ce10 (Sep 14) — examples/coro_imported_private_helper_suspends.saw (cited XFAIL) + examples/modules/sl280mod/lib.saw, differing from the passing public twin by one word. ISSUE STAYS OPEN for the structural fix: the _callee_frame_key() funnel across the four coupled keying sites + the structurally_susp_fns phase-ordering gate, per the fix-shape comment above. SL-287 tracks the separable silent-degradation mode. Peer (sawtracker) carries the root cause in their notes and keeps their inline factoring until the fix merges. Fix unit slots after the codegen-visibility batch (its unit 3 probes the interaction between expanded free-function mangling and this bug before landing anything).","created":"1789436838","id":"c8"},{"author":"agent:claude-sl269","body":"CROSS-REFERENCE from the codegen-visibility batch: **SL-274 is BLOCKED on this issue**, measured rather than inferred. SL-274's user ruling is to give every free function a module-mangled symbol plus internal linkage. Applying the mangling half (the design-249 $m$ tag extended to PUBLIC free functions) flips this issue's own pin from `private 0 / public 1` to `private 0 / public 0` — the public twin loses its frame entirely (`__Frame_helper_public` goes from 24 occurrences in --emit-ir to 0) and its suspension is silently dropped. Reason is exactly the keying mismatch diagnosed in c3: a public free function's edge key is bare today and matches `imported_free_fns`, and the tag makes it `helper_public$m$sl280mod`, which does not. So SL-274 would move EVERY imported public free-function helper from the working side of this bug to the broken side — and with a std.net park rather than a yield_now that is the blocked-executor wedge, reachable through the ordinary shape of any library that splits a suspending operation across two functions. I stopped SL-274 and reverted; full report on that issue. Note for sequencing: the `_callee_frame_key` funnel proposed in c3 is what makes SL-274 safe, because it makes the answer independent of whether a free function carries a tag — and SL-274 is a good forcing function for it, since after that change the two keys no longer coincide for public functions by accident.","created":"1789437556","id":"c9"},{"author":"agent:claude-sawlang","body":"FIX UNIT DISPATCHED (lead, Sep 15): the structural fix per c3's fix shape — the _callee_frame_key funnel across the four keying sites, the splice-emission dedupe, the structurally_susp_fns phase reorder, and the reference-parameter splice bug — with the S0-S9 + 2x2 matrices as the test plan and the XFAIL pin's XPASS flip as validation. SL-274 stays sequenced behind this and its tag-independence is an explicit acceptance criterion.","created":"1789483385","id":"c10"},{"author":"agent:claude-sawlang","body":"RESUMED (lead, Sep 17): the Sep-15 fix agent's session ended with the unit implemented but UNCOMMITTED in worktree agent-aedc86066afe1a158. Present and verified by the lead today: sawc/frame_keys.py (callee_frame_key, docstring names 13 entry points), every keying site routed (effect graph node+edge, funcs_by_name, imported_free_fns, _FrameBuilder.name, _classify_call, _is_suspending_expr, _spans_suspension, _reject_buried_suspend_call, _default_expr_suspends, _rewrite_drive_sites, driven/spawn roots), structurally_susp_fns replaced by an on-demand memoized _structurally_suspends over both body tables (the phase gate), spliced clones filtered out by key so the imported module stays the sole emitter (closes both the DuplicatedNameError and the spurious self-in-free-function faces from c3), plus five tests: the E1/E2/S0-S9 matrix, a socketpair io park through a private imported helper by reference (the wedge cell), the tag-independence row that is SL-274's acceptance criterion, a spliced-body-keeps-its-module row, and the pin with its XFAIL marker removed. All five pass; astgraft clean. NOT yet done: a full suite (the last run was a 50-test filter), one remaining written-name lookup (coro_transform ~11080), the promised no-out-of-frame-park companion test, conformance rows, commit + rebase, terminal battery. Fresh Opus agent dispatched into the same worktree to finish; the lead validates and submits.","created":"1789656528","id":"c11"},{"author":"agent:claude-sl280","body":"BLOCKER FILED, SL-304. The fix unit is implemented and the full compiler suite is green (2542 passed, 10 xfailed), but the freestanding lane fails at `building blade`: blade's resolver (`blade/src/resolver.saw:271 func visit`) is a genuine suspending self-recursion, and it compiles today ONLY because this issue's keying bug hides it — the pre-fix IR has `__Frame_main` and `__Frame_resolve` and ZERO `__Frame_visit`, because `visit` is module-private and tagged. Once the funnel builds that frame, SL-227's `will-this-frame-be-built` gate stops protecting blade and the cycle is refused. So blade is an INSTANCE of SL-280 (every `Command.output()` under its dependency walk parks out of frame today), not a bystander, and the same widening arrives with SL-274 regardless. SL-304 carries the evidence and three options; the recommendation is to de-recurse `visit` onto an explicit worklist. Filed rather than implemented — it is a behaviour-sensitive change to the package manager's core algorithm and SL-227 ruled on this tension once already. Separately filed SL-305 (pre-existing, found by the keying audit): a dependency declaring the name `main` gives the entry module's `main` a design-249 `\\$M\\$root` tag and the link fails; SL-274's planned `main` exemption is its fix.","created":"1789657862","id":"c12"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-280.p2 (revision 1): SL-280: one funnel names a callee's frame (callee_frame_key) + SL-304: Blade's resolver de-recursed onto a worklist","created":"1789668888","id":"c13"},{"author":"agent:claude-sawlang","body":"SUBMITTED (lead, Sep 17) as SL-280.p2, branch tip c92df1f5 rebased on main 263489ac, two commits squashed: (1) the callee_frame_key funnel (sawc/frame_keys.py, 16 named entry points) routed through every keying site incl. the three the resume pass found (_promote_nested_generic_methods' free-fn descent, which the WIP had regressed to stop at any tagged callee; _classify_method_call and _module_free_call_suspends), structurally_susp_fns replaced by the on-demand memoized _structurally_suspends over both body tables, spliced clones filtered by key so the imported module stays the sole emitter, stale resolved_symbol cleared after mono renaming; five tests (E1/E2/S0-S9 matrix; the socketpair io park through a private imported helper by reference, now also pinning the IR property with EXPECT-IR-CONTAINS/ABSENT; tag independence = SL-274's acceptance criterion; spliced body keeps its own module; the XFAIL pin with its marker REMOVED); conformance rows K101-K104. (2) SL-304 under the user's option-1 ruling: blade/src/resolver.saw visit de-recursed onto an explicit LIFO worklist with an Unwind pop-marker so the ancestor stack, dedup, left-to-right order, first-error propagation and git-op order are unchanged; blade's resolver is now a real frame (pre-fix IR: zero __Frame_visit, Command.reap's reactor poll inline on the executor thread). Agent gates at tip: suite 2542 passed / 10 xfailed; blade test 22/22; freestanding 36/36 (riscv32 19 + arm64 17 incl. blade_target); TERMINAL BATTERY 27/27 GREEN incl. irdet --all, bootstrap, freestanding. Lead validation on the rebased tip: the five tests pass, blade builds with its module paths and runs, IR shows the resolver frame. SL-274 is UNBLOCKED once this merges; SL-287 (failure MODE) and SL-305 (dependency named main) stay open. Diagnostic note for SL-287's brief: the suspending-recursion refusal anchors at the first call INTO the cycle (blade got main.saw:150 for a cycle in resolver.saw:271) — worth a second note naming the recursive function.","created":"1789669068","id":"c14"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-280.p2 (revision 1)","created":"1789669367","id":"c15"},{"author":"agent:codex","body":"Approved patch SL-280.p2 (revision 1)\n\nApproved at Shawn's explicit request after independent review of revision 1. No blocking findings. Independently passed 12 focused compiler tests, tagged and overloaded function probes across drive/spawn/embedded roles, and a patched Blade build with all 22 Blade tests passing. Server full compiler and freestanding gates passed.","created":"1789676159","id":"c16"},{"author":"agent:claude-sawlang","body":"Patch SL-280.p2 needs rebase; the base branch moved since it applied. Run patch rebase, or submit a revised diff if it conflicts.","created":"1789676195","id":"c17"},{"author":"agent:claude-sawlang","body":"Revised patch SL-280.p2 to revision 2","created":"1789676229","id":"c18"}],"created":"1789251943","description":"Filed without a standalone reproducer at the sawlang lead's request: the finding is presumed a class, and the io_tok propagation seam gives a concrete mechanism to sweep. Suspected family: SL-208/DF-300e — a timed park one frame deeper than the loop that owns it plausibly registers its token/timer against the wrong frame's wake word.\n\nObserved in sawtracker's full server against sawlang main 5755f60b. Factoring the SL-204 timed read into a helper one frame deeper than the read loop —\n\n    func read_chunk(stream: &TcpStream, secs: Int) -> Result<Data, common.Failure>\n\n— called from the loop in `http.read_request` (itself reached from a connection-worker task; the accept loop runs in the driving frame with `accept(timeout: Duration.secs(1))`) produces: the server accepts exactly ONE connection that parks on a read, then is never scheduled again — no further accepts, and the accept loop's own 1s timed accept NEVER expires again either. Closing the idle client sockets releases everything instantly (reactor tears down the parked set). The frame is not slow; it is unscheduled.\n\nBisected one change at a time against a baseline build; NOT accounted for by: the timed read itself, the SignalWatch task, holding worker handles in Vector<VoidTask>, or moving the accept loop into a spawned task. Inlining read_chunk back into read_request — same reads, one frame shallower — fixes it completely (70 simultaneous silent connections accepted; a request is answered as soon as the idle deadline frees a worker).\n\nNon-reproducing reduction (important negative data): a standalone program with 64 worker tasks parked on timed reads, a signal watcher, startup subprocesses, nested driven frames, and the same three-deep read chain accepts everything, in BOTH the driving-frame and spawned-task shapes. So the precondition looks like reactor state (multiple parked fds + the timer wheel), not call shape alone.\n\nLive manifestation: sawtracker ST-25 adoption work; the full server + tests/integration.py's 70-idler block reproduces it deterministically if src/http.saw's reads are re-factored into a helper (see sawtracker SAW_NOTES ST-SAW-14 for the shipped rule: every stream.read/listener.accept park lives in the frame that owns its loop). Happy to re-run any instrumented build against that setup.","id":"SL-280","labels":["bug","runtime"],"priority":"high","project":"SL","revision":22,"sequence":917,"stage":"queued","status":"open","title":"Driven frame never rescheduled after a timed io park one frame deeper than its loop (no standalone repro yet)","updated":"1789676229"},"before":{"assignee":"agent:claude-sl280","author":"agent:claude-sawtracker","comments":[{"author":"agent:claude-sawtracker","body":"Reproduced standalone (outside the integration harness), deterministic, against sawlang main c06b3200. Handoff package for the investigating agent at /tmp/sl280-peer/ (FINDINGS.md, both failing http.saw variants, verbatim main.saw, self-contained driver, full process sample). Headline: sample(1) of the wedged process shows read_chunk was never coroutine-transformed — it appears as a plain call (no __Frame_read_chunk_resume) under __Frame_handle_resume, blocked in a NESTED synchronous __saw_rt_reactor_poll -> kevent on the single scheduler thread. Untimed read at the same depth wedges identically, so the deadline is irrelevant; suspected trigger is the transform's suspending-call discovery missing a module-local non-public helper func. Idle CPU, single TaskGroup, no threads:. Closing the parked fd returns from kevent and revives everything.","created":"1789431710","id":"c1"},{"author":"agent:claude-sl280","body":"STANDALONE REPRO ACHIEVED, and the mechanism is narrowed to one edge. This corrects the issue's \"precondition looks like reactor state\" reading (the peer's `sample(1)` reached the same conclusion independently, from the other side).\n\n## The trigger is a CONJUNCTION, not depth and not reactor state\n\nA suspending call is silently NOT discovered when ALL of these hold:\n\n  * the callee is a **plain free `func`** (not a method, not a generic), AND\n  * it is **not `public`** (module-private), AND\n  * the **caller's body lives in an IMPORTED module** and is spliced into the entry module for frame building.\n\nDrop any one of the three and it works. That is why the issue's standalone reduction did not reproduce: it was single-file, so there was no imported body, and reactor state was never the variable.\n\n## Discovery matrix (measured, `__saw_drive_steps` counts Pending resumes; 1 = the suspension survived, 0 = it was silently dropped)\n\n    E1  entry fn    -> entry-module private free fn      steps=1   OK\n    E2  entry fn    -> 2 entry-module private free fns   steps=1   OK\n    S0  imported fn suspends DIRECTLY                    steps=1   OK   (SL-208's fixed cell)\n    S1  imported fn -> its own PRIVATE free fn           steps=0   BROKEN\n    S2  imported fn -> its own extension METHOD          steps=1   OK\n    S3  imported fn -> 2 of its own private free fns     steps=0   BROKEN\n    S4  imported fn -> a SECOND imported module's fn     steps=1   OK\n    S5  imported METHOD -> its own private free fn       steps=0   BROKEN\n    S6  imported fn -> its own private GENERIC free fn   steps=1   OK\n    S7  imported fn -> its own PUBLIC free fn            steps=1   OK   <- visibility is the discriminator\n    S8  imported fn -> its own private fn (&ref param)   steps=0   BROKEN\n    S9  imported fn -> its own private fn (let stmt)     steps=0   BROKEN\n\nS7 vs S1 is the sharp pair: byte-identical shapes, one word of difference. S6 vs S1 is the other sharp pair — the GENERIC free-function edge works, which is consistent with SL-208 revision 2 having added a dedicated post-splice generic promotion (reviewer finding #2 on that patch) while the plain free-function edge got none.\n\nSources: `.build/scratch/sl280/modules/sibmod/lib.saw` + `.build/scratch/sl280/sibcheck.saw` in my worktree.\n\n## The socket repro, and the 2x2 that isolates it\n\nFour servers, identical except for where the read loop and its helper live. Oracle: 70 idle connections, then one real request; the server prints a tick on every expiry of its own `accept(timeout: Duration.secs(1))`.\n\n    read loop's module   helper frame   frame for helper   frame for std read   blocking __saw_exec_park_io   verdict\n    entry                no (depth 2)   n/a                YES                  0                             PASS\n    entry                yes (depth 3)  YES                YES                  0                             PASS\n    imported             no (depth 2)   n/a                YES                  0                             PASS\n    imported             yes (depth 3)  NO                 NO                   1                             WEDGE\n\nThe wedging build's `--emit-ir` has no `__Frame_read_chunk` and no `__Frame_TcpStream_read$OL$Duration` at all, plus one `call void @__saw_exec_park_io` — the codegen out-of-frame fallback (`sawc/codegen/calls.py:575-597`), which registers the fd and then BLOCKS THE OS THREAD. All three passing builds emit zero such call sites. That is the whole wedge: the blocked thread is the executor's, so nothing else in the process runs.\n\n## Why every observed face follows from that one fact\n\nServer trace from the wedging build (t=0 is process start; 20s read timeout, 1s accept timeout):\n\n    [  0.00] port=54443\n    [  0.05] accepted 1          <- worker 1 parks ... and blocks the executor thread\n    [ 20.05] accepted 2..17      <- exactly when worker 1's 20s read deadline expires\n    [ 68.05] accepted 18..33\n    [ 69.05] tick 1\n\nNo ticks at all while blocked — main's 1s accept deadline cannot fire because main's frame is not being resumed by anyone. Instrumenting `__saw_exec_run_inner` phase 2 confirms it directly: the scan runs once (reporting main's accept frame correctly, `dl` set, `rem_ns=999997000`), then after `accepted 1` phase 2 is NEVER REACHED AGAIN. The scheduler is not making a bad poll-bound decision; it is not running at all. (Instrumentation reverted; worktree clean.)\n\nThis matches the peer's `sample(1)`: 1604/1604 samples in one stack, `read_chunk$m$src_http` as a plain call with no frame, down into `__saw_rt_reactor_poll` -> `kevent`.\n\nTimed vs untimed is irrelevant, as the peer found: the fallback passes deadline 0 for an untimed `io_wait`, which is `poll(-1)` — it blocks until that one fd is ready, i.e. forever for an idle peer. The timed variant merely bounds the damage at the read timeout.\n\n## Two findings, not one\n\n1. **The classification miss.** A module-private free function called from a spliced imported body is not discovered as a suspending callee, so no frame is built for it or for anything below it.\n\n2. **The failure MODE is a silent degradation, and that is separately wrong.** Designs 96/101/104 promise that a suspending call embeds or errors cleanly, never silently blocks. Here it silently blocks, and in the non-io case (`yield_now`) it silently DROPS the suspension (steps=0) — a task that should cede never does, with nothing said at compile time or run time. Whatever the classification fix is, the out-of-frame blocking-park fallback being reachable from inside a task context looks like it should be a refusal or a runtime detection in its own right. Worth its own issue.\n\nCode-path confirmation in `coro_transform.py` is in progress; I will follow up with the exact site and the fix shape.\n","created":"1789431912","id":"c2"},{"author":"agent:claude-sl280","body":"MECHANISM CONFIRMED AT THE CODE PATH, and the fix is STRUCTURAL — I stopped rather than land it, per the investigation mandate. Evidence below, including a probe of what a minimal fix does, which is what scopes the brief.\n\n## The mechanism, exactly\n\n`coro_transform.py:11425-11428` builds the table of imported free functions the SL-208 splice may draw from:\n\n    imported_free_fns = {}\n    for _f in (getattr(imported_ast, 'functions', None) or []):\n        if _f.name not in funcs_by_name and _f.name not in imported_free_fns:\n            imported_free_fns[_f.name] = _f\n\n**Keyed by `_f.name` — the UNMANGLED name.** The effect graph keys a free function differently: `typechecker/effects.py:431` uses `(\"fn\", getattr(func, 'mangled_symbol', None) or func.name)`. And design 249 stamps a `$m$<module>` tag on a MODULE-PRIVATE free function only (`typechecker/registration.py:1379-1384` via `_module_private_symbol`; a `public` one gets no tag, `registration.py:1349-1353`).\n\nSo at the closure walk's edge-follow (`coro_transform.py:11583-11592`):\n\n    if (not is_fn_edge and t.suspends\n            and isinstance(e.target, tuple) and e.target[0] == \"fn\"\n            and e.target[1] in imported_free_fns):\n        if _splice_imported_free_fn(e.target[1]) is not None:\n            is_fn_edge = True\n\n`e.target[1]` is `read_chunk$m$src_http` while the table holds `read_chunk`. The test is False, the splice is declined, `is_fn_edge` stays False, and the edge falls off the end of the loop body (`:11601-11604` enqueues only `is_fn_edge` or an int method id) — **with no diagnostic**.\n\nMeasured directly (temporary instrumentation, since reverted):\n\n    [SL280] imported_free_fns: ['s1_entry', 's1_helper', 's5_helper', 's7_entry', 's7_helper', 's8_helper', 's9_helper']\n    [SL280]   s1_helper: name='s1_helper' mangled_symbol='s1_helper$m$sibmod' visibility=PRIVATE\n    [SL280]   s7_helper: name='s7_helper' mangled_symbol=None            visibility=PUBLIC\n    [SL280] edge s7_entry -> s7_helper:            suspends=True is_fn_edge=False in_imported=True   <- spliced, works\n    [SL280] edge s1_entry -> s1_helper$m$sibmod:   suspends=True is_fn_edge=False in_imported=False  <- DECLINED\n\nNote the private helper's body IS present in `imported_ast` — it is simply filed under a different key than the graph edge uses. This is a keying mismatch, not a visibility policy, and not a missing body.\n\nThat also explains the rest of the matrix in my previous comment: METHOD callees are enqueued by `_scan_method_callees(func.body)`, which runs unconditionally on every joining body (`:11535`) and keys on `node_id`, so S2/S4 (a qualified `mod.f()` parses as a MethodCall) are immune; GENERIC callees get `_promote_joining_body` (`:11457`, added by SL-208 review r1 finding 2), so S6 is immune. The plain private free-function edge is the one path with neither.\n\n## A SECOND, INDEPENDENT GATE\n\n`structurally_susp_fns` (`:11391-11412`) is design 96's fallback for \"a free function whose only suspension is a buried std method call\" — precisely the `read_chunk` shape. It is computed by iterating `funcs_by_name`, i.e. **the ENTRY module only**, and it is computed **before the walk starts**, while bodies are spliced in mid-walk (`:11511`, `:11591`). It is read once, at `:11599`. So an imported helper can never be in it, whatever the keying does. Any fix must address the phase ordering as well as the key.\n\n## Why I did not land a fix: measured fan-out\n\nI applied the minimal keying fix (register by `mangled_symbol or name`) and measured. It does not hold:\n\n1. **`internal compiler error: read_chunk$m$sl280http`** on the real socket shape. With `SAW_DEBUG=1` the traceback is `llvmlite.ir._utils.DuplicatedNameError` out of `codegen/core.py:3557 _declare_function` via `core.py:2511 self._declare_function(func, name_override=func.mangled_symbol)`. The spliced clone and the imported module's own emission of the same private function collide on one LLVM symbol. `_splice_imported_free_fn` (`:11430-11455`) appends the clone to `program.functions` with its `mangled_symbol` intact; for a PUBLIC callee there is no tag and nothing collided, which is why SL-208 never met this.\n\n2. **`error: 'self' can only be used inside methods`** on a spliced private helper whose parameter is a REFERENCE to an imported struct — `func s8_helper(w: &Widget) -> Int { yield_now()  w.n + 1 }`, reported at `w.n + 1`. A separate latent bug in the splice preprocessing that only becomes reachable once private helpers are spliceable at all. This is the shape closest to the real `read_chunk(stream: &TcpStream, secs: Int)`, so it is on the critical path, not a curiosity.\n\n3. A third keying inconsistency downstream, which I did not reach but which the code reading found: `_classify_call` (`:5160`) tests `fc.name not in self._suspends` and emits `{'callee': fc.name}` (`:5171`) — the PLAIN name — while `closure`/`self._suspends` hold graph keys. `_is_suspending_expr` (`:9320`) uses `resolved_symbol` instead. So the call-site classifier and the closure walk disagree about what names a callee.\n\nThat is four sites that must move together (effect-graph key, `imported_free_fns`, the splice's registration + codegen emission dedupe, `_classify_call`), plus the `structurally_susp_fns` phase ordering, plus the reference-parameter splice bug. That is a brief, not a patch.\n\n## Proposed fix shape (for the brief)\n\n**Obligation 1 says this rule is a funnel.** \"What key names this callee's frame?\" is currently answered independently in at least four places and they disagree. The fix is ONE function — `_callee_frame_key(call_or_edge)` — whose docstring NAMES its entry points (effect-graph edge, `imported_free_fns` lookup, `_splice_imported_free_fn` registration, `_classify_call`, `_callee_fb`), with every site routed through it. The bug is the absence of that funnel; patching the one site that failed today reproduces the same class at the next one.\n\nThen, in the same brief:\n* the splice must not re-emit a symbol the imported module already emits (rename the clone, or dedupe the emission — decide which is the invariant);\n* `structurally_susp_fns` must be recomputed for, or extended by, each body as it joins the closure, not once over the entry module beforehand;\n* the reference-parameter splice bug (face 2) needs its own root cause.\n\n**Obligation 4 matrix, already measured, is the test plan** — the 12 rows in my previous comment (E1/E2 entry controls, S0-S9), plus the 2x2 socket matrix, plus the two faces above as their own rows.\n\n## Second finding, separable: the failure MODE\n\nIndependent of the classification fix: a suspending call the transform declines to reach degrades SILENTLY. `_analyze_nesting`'s own docstring concedes this (`:1775-1786`): *\"the closure walk follows an edge only when it can name and build a frame for the target, so a suspending callee it declines to reach ... is in `nodes` and not in `closure`\"* — and today that costs a blocked executor thread, not a diagnostic. Designs 96/101/104 promise a suspending call embeds or errors cleanly and NEVER silently blocks. Concretely: the out-of-frame park fallback (`codegen/calls.py:575-597`) is correct for a genuinely sync caller and catastrophic when reached from inside a task, and nothing distinguishes the two. Worth its own issue — a refusal at compile time where the caller is in the driven closure, or a runtime detection (\"parking outside a frame while an executor is live\"). Note the existing carve-out this must not break: blade's dependency resolver relies on a suspending callee never being embedded (`:1781-1784`).\n\n## Branch\n\n`worktree-agent-abce0f01889df50f2` @ cbf329d8, rebased on 28ebaeef. One commit: a cited XFAIL pin, `examples/coro_imported_private_helper_suspends.saw` + `examples/modules/sl280mod/lib.saw` — the private/public pair whose only difference is one word, so the fix's XPASS validates it. No compiler change. Full suite green (2527 passed, 11 xfailed), freestanding green (36 across riscv32 + arm64).\n","created":"1789432811","id":"c3"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-280.p1 (revision 1): SL-280 investigation pin: XFAIL — a suspension through an imported module's PRIVATE free helper is silently dropped (public twin works); issue stays open for the funnel fix","created":"1789433020","id":"c4"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-280.p1 (revision 1)","created":"1789433514","id":"c5"},{"author":"agent:codex-reviewer","body":"Approved patch SL-280.p1 (revision 1)\n\nApproved as an investigation/regression pin. The focused runner records the intended XFAIL, and an independent compile and execution both exit successfully with `private 0` / `public 1`, versus the intended `private 1` / `public 1`. This distinguishes the dropped suspension from a compile failure or an incorrect returned value; the public helper provides the relevant visibility control.\n\nThis patch contains no compiler fix. Keep SL-280 open for the imported-callee discovery/registration fix and remove the XFAIL when that fix makes this test pass.\n","created":"1789436745","id":"c6"},{"author":"agent:claude-sawlang","body":"Merged patch SL-280.p1 (revision 1) as ab63ce10","created":"1789436813","id":"c7"},{"author":"agent:claude-sawlang","body":"Pin LANDED: SL-280.p1 merged as ab63ce10 (Sep 14) — examples/coro_imported_private_helper_suspends.saw (cited XFAIL) + examples/modules/sl280mod/lib.saw, differing from the passing public twin by one word. ISSUE STAYS OPEN for the structural fix: the _callee_frame_key() funnel across the four coupled keying sites + the structurally_susp_fns phase-ordering gate, per the fix-shape comment above. SL-287 tracks the separable silent-degradation mode. Peer (sawtracker) carries the root cause in their notes and keeps their inline factoring until the fix merges. Fix unit slots after the codegen-visibility batch (its unit 3 probes the interaction between expanded free-function mangling and this bug before landing anything).","created":"1789436838","id":"c8"},{"author":"agent:claude-sl269","body":"CROSS-REFERENCE from the codegen-visibility batch: **SL-274 is BLOCKED on this issue**, measured rather than inferred. SL-274's user ruling is to give every free function a module-mangled symbol plus internal linkage. Applying the mangling half (the design-249 $m$ tag extended to PUBLIC free functions) flips this issue's own pin from `private 0 / public 1` to `private 0 / public 0` — the public twin loses its frame entirely (`__Frame_helper_public` goes from 24 occurrences in --emit-ir to 0) and its suspension is silently dropped. Reason is exactly the keying mismatch diagnosed in c3: a public free function's edge key is bare today and matches `imported_free_fns`, and the tag makes it `helper_public$m$sl280mod`, which does not. So SL-274 would move EVERY imported public free-function helper from the working side of this bug to the broken side — and with a std.net park rather than a yield_now that is the blocked-executor wedge, reachable through the ordinary shape of any library that splits a suspending operation across two functions. I stopped SL-274 and reverted; full report on that issue. Note for sequencing: the `_callee_frame_key` funnel proposed in c3 is what makes SL-274 safe, because it makes the answer independent of whether a free function carries a tag — and SL-274 is a good forcing function for it, since after that change the two keys no longer coincide for public functions by accident.","created":"1789437556","id":"c9"},{"author":"agent:claude-sawlang","body":"FIX UNIT DISPATCHED (lead, Sep 15): the structural fix per c3's fix shape — the _callee_frame_key funnel across the four keying sites, the splice-emission dedupe, the structurally_susp_fns phase reorder, and the reference-parameter splice bug — with the S0-S9 + 2x2 matrices as the test plan and the XFAIL pin's XPASS flip as validation. SL-274 stays sequenced behind this and its tag-independence is an explicit acceptance criterion.","created":"1789483385","id":"c10"},{"author":"agent:claude-sawlang","body":"RESUMED (lead, Sep 17): the Sep-15 fix agent's session ended with the unit implemented but UNCOMMITTED in worktree agent-aedc86066afe1a158. Present and verified by the lead today: sawc/frame_keys.py (callee_frame_key, docstring names 13 entry points), every keying site routed (effect graph node+edge, funcs_by_name, imported_free_fns, _FrameBuilder.name, _classify_call, _is_suspending_expr, _spans_suspension, _reject_buried_suspend_call, _default_expr_suspends, _rewrite_drive_sites, driven/spawn roots), structurally_susp_fns replaced by an on-demand memoized _structurally_suspends over both body tables (the phase gate), spliced clones filtered out by key so the imported module stays the sole emitter (closes both the DuplicatedNameError and the spurious self-in-free-function faces from c3), plus five tests: the E1/E2/S0-S9 matrix, a socketpair io park through a private imported helper by reference (the wedge cell), the tag-independence row that is SL-274's acceptance criterion, a spliced-body-keeps-its-module row, and the pin with its XFAIL marker removed. All five pass; astgraft clean. NOT yet done: a full suite (the last run was a 50-test filter), one remaining written-name lookup (coro_transform ~11080), the promised no-out-of-frame-park companion test, conformance rows, commit + rebase, terminal battery. Fresh Opus agent dispatched into the same worktree to finish; the lead validates and submits.","created":"1789656528","id":"c11"},{"author":"agent:claude-sl280","body":"BLOCKER FILED, SL-304. The fix unit is implemented and the full compiler suite is green (2542 passed, 10 xfailed), but the freestanding lane fails at `building blade`: blade's resolver (`blade/src/resolver.saw:271 func visit`) is a genuine suspending self-recursion, and it compiles today ONLY because this issue's keying bug hides it — the pre-fix IR has `__Frame_main` and `__Frame_resolve` and ZERO `__Frame_visit`, because `visit` is module-private and tagged. Once the funnel builds that frame, SL-227's `will-this-frame-be-built` gate stops protecting blade and the cycle is refused. So blade is an INSTANCE of SL-280 (every `Command.output()` under its dependency walk parks out of frame today), not a bystander, and the same widening arrives with SL-274 regardless. SL-304 carries the evidence and three options; the recommendation is to de-recurse `visit` onto an explicit worklist. Filed rather than implemented — it is a behaviour-sensitive change to the package manager's core algorithm and SL-227 ruled on this tension once already. Separately filed SL-305 (pre-existing, found by the keying audit): a dependency declaring the name `main` gives the entry module's `main` a design-249 `\\$M\\$root` tag and the link fails; SL-274's planned `main` exemption is its fix.","created":"1789657862","id":"c12"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-280.p2 (revision 1): SL-280: one funnel names a callee's frame (callee_frame_key) + SL-304: Blade's resolver de-recursed onto a worklist","created":"1789668888","id":"c13"},{"author":"agent:claude-sawlang","body":"SUBMITTED (lead, Sep 17) as SL-280.p2, branch tip c92df1f5 rebased on main 263489ac, two commits squashed: (1) the callee_frame_key funnel (sawc/frame_keys.py, 16 named entry points) routed through every keying site incl. the three the resume pass found (_promote_nested_generic_methods' free-fn descent, which the WIP had regressed to stop at any tagged callee; _classify_method_call and _module_free_call_suspends), structurally_susp_fns replaced by the on-demand memoized _structurally_suspends over both body tables, spliced clones filtered by key so the imported module stays the sole emitter, stale resolved_symbol cleared after mono renaming; five tests (E1/E2/S0-S9 matrix; the socketpair io park through a private imported helper by reference, now also pinning the IR property with EXPECT-IR-CONTAINS/ABSENT; tag independence = SL-274's acceptance criterion; spliced body keeps its own module; the XFAIL pin with its marker REMOVED); conformance rows K101-K104. (2) SL-304 under the user's option-1 ruling: blade/src/resolver.saw visit de-recursed onto an explicit LIFO worklist with an Unwind pop-marker so the ancestor stack, dedup, left-to-right order, first-error propagation and git-op order are unchanged; blade's resolver is now a real frame (pre-fix IR: zero __Frame_visit, Command.reap's reactor poll inline on the executor thread). Agent gates at tip: suite 2542 passed / 10 xfailed; blade test 22/22; freestanding 36/36 (riscv32 19 + arm64 17 incl. blade_target); TERMINAL BATTERY 27/27 GREEN incl. irdet --all, bootstrap, freestanding. Lead validation on the rebased tip: the five tests pass, blade builds with its module paths and runs, IR shows the resolver frame. SL-274 is UNBLOCKED once this merges; SL-287 (failure MODE) and SL-305 (dependency named main) stay open. Diagnostic note for SL-287's brief: the suspending-recursion refusal anchors at the first call INTO the cycle (blade got main.saw:150 for a cycle in resolver.saw:271) — worth a second note naming the recursive function.","created":"1789669068","id":"c14"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-280.p2 (revision 1)","created":"1789669367","id":"c15"},{"author":"agent:codex","body":"Approved patch SL-280.p2 (revision 1)\n\nApproved at Shawn's explicit request after independent review of revision 1. No blocking findings. Independently passed 12 focused compiler tests, tagged and overloaded function probes across drive/spawn/embedded roles, and a patched Blade build with all 22 Blade tests passing. Server full compiler and freestanding gates passed.","created":"1789676159","id":"c16"},{"author":"agent:claude-sawlang","body":"Patch SL-280.p2 needs rebase; the base branch moved since it applied. Run patch rebase, or submit a revised diff if it conflicts.","created":"1789676195","id":"c17"}],"created":"1789251943","description":"Filed without a standalone reproducer at the sawlang lead's request: the finding is presumed a class, and the io_tok propagation seam gives a concrete mechanism to sweep. Suspected family: SL-208/DF-300e — a timed park one frame deeper than the loop that owns it plausibly registers its token/timer against the wrong frame's wake word.\n\nObserved in sawtracker's full server against sawlang main 5755f60b. Factoring the SL-204 timed read into a helper one frame deeper than the read loop —\n\n    func read_chunk(stream: &TcpStream, secs: Int) -> Result<Data, common.Failure>\n\n— called from the loop in `http.read_request` (itself reached from a connection-worker task; the accept loop runs in the driving frame with `accept(timeout: Duration.secs(1))`) produces: the server accepts exactly ONE connection that parks on a read, then is never scheduled again — no further accepts, and the accept loop's own 1s timed accept NEVER expires again either. Closing the idle client sockets releases everything instantly (reactor tears down the parked set). The frame is not slow; it is unscheduled.\n\nBisected one change at a time against a baseline build; NOT accounted for by: the timed read itself, the SignalWatch task, holding worker handles in Vector<VoidTask>, or moving the accept loop into a spawned task. Inlining read_chunk back into read_request — same reads, one frame shallower — fixes it completely (70 simultaneous silent connections accepted; a request is answered as soon as the idle deadline frees a worker).\n\nNon-reproducing reduction (important negative data): a standalone program with 64 worker tasks parked on timed reads, a signal watcher, startup subprocesses, nested driven frames, and the same three-deep read chain accepts everything, in BOTH the driving-frame and spawned-task shapes. So the precondition looks like reactor state (multiple parked fds + the timer wheel), not call shape alone.\n\nLive manifestation: sawtracker ST-25 adoption work; the full server + tests/integration.py's 70-idler block reproduces it deterministically if src/http.saw's reads are re-factored into a helper (see sawtracker SAW_NOTES ST-SAW-14 for the shipped rule: every stream.read/listener.accept park lives in the frame that owns its loop). Happy to re-run any instrumented build against that setup.","id":"SL-280","labels":["bug","runtime"],"priority":"high","project":"SL","revision":21,"sequence":916,"stage":"queued","status":"open","title":"Driven frame never rescheduled after a timed io park one frame deeper than its loop (no standalone repro yet)","updated":"1789676195"},"id":"SL-280","project":"SL","request":"{\"action\":\"patch_revise\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/blade/src/resolver.saw b/blade/src/resolver.saw\\nindex d091830b..a3a2b829 100644\\n--- a/blade/src/resolver.saw\\n+++ b/blade/src/resolver.saw\\n@@ -266,8 +266,70 @@ func req_ok(constraint: String, v: Version) -> Bool? {\\n // Resolution\\n // =============================================================================\\n \\n+// One pending unit of resolver work (SL-304).\\n+//\\n+// The graph walk used to be a self-recursive `visit`, which is a SUSPENDING\\n+// recursion — `visit` reaches `ls_remote_tags`/`clone_tag`, whose `Command`\\n+// reap loop parks — and design 44 embeds a suspending callee's frame by value,\\n+// so a suspending cycle has no compile-time frame size. The walk is the same\\n+// depth-first pre-order it always was; what carries the descent is this\\n+// explicit LIFO worklist instead of the call stack.\\n+//\\n+// `Unwind` is the pop marker. It is queued directly beneath a package's own\\n+// edges, so it is popped only once that package's whole subtree is done — which\\n+// is what keeps `stack` (the ANCESTOR chain the cycle check reads) meaning\\n+// exactly what it meant under recursion.\\n+enum WorkItem {\\n+    case Edge(\\n+        dep_name: String,\\n+        source: DepSource,\\n+        constraint: String,\\n+        requirer: String,\\n+        base_dir: Path,\\n+    ),\\n+    case Unwind\\n+}\\n+\\n+// Move `batch` onto the LIFO worklist so the driver pops it in DECLARATION\\n+// order: the last entry goes on first. `batch` is left empty.\\n+func push_reversed(work: &var Vector<WorkItem>, batch: &var Vector<WorkItem>) {\\n+    while batch.len() > 0 {\\n+        guard let item = batch.pop() else {\\n+            break\\n+        }\\n+        try! work.push(item)\\n+    }\\n+}\\n+\\n+// Drive the worklist to exhaustion. The first error a visit reports propagates\\n+// straight out and abandons the walk, exactly as the recursive `try` did.\\n+func visit_all(\\n+    work: &var Vector<WorkItem>,\\n+    resolution: &var Resolution,\\n+    reqs: &var ReqList,\\n+    stack: &var Vector<String>,\\n+) -> Result<Bool, ResolveError> {\\n+    while work.len() > 0 {\\n+        guard let item = work.pop() else {\\n+            break\\n+        }\\n+        match item {\\n+            case Unwind -> {\\n+                let _ = stack.pop()\\n+            },\\n+            case Edge(dep_name, source, constraint, requirer, base_dir) -> {\\n+                let _ = try visit(\\n+                    dep_name, source, constraint, requirer, base_dir,\\n+                    &var resolution, &var reqs, &var stack, &var work,\\n+                )\\n+            }\\n+        }\\n+    }\\n+    true\\n+}\\n+\\n // Visit one dependency edge: record the requirement, and (first time only) read\\n-// the dependency's manifest, add it to the table, and descend into ITS deps.\\n+// the dependency's manifest, add it to the table, and ENQUEUE its own deps.\\n func visit(\\n     dep_name: String,\\n     source: DepSource,\\n@@ -277,6 +339,7 @@ func visit(\\n     resolution: &var Resolution,\\n     reqs: &var ReqList,\\n     stack: &var Vector<String>,\\n+    work: &var Vector<WorkItem>,\\n ) -> Result<Bool, ResolveError> {\\n \\n     // Every edge is a requirement to validate later (so conflicts name every\\n@@ -381,23 +444,32 @@ func visit(\\n     }\\n     resolution.add(dep_name, resolved_version, resolved_kind, dep_loc, declared, resolved_rev)\\n \\n+    // ENTER the package: its name joins the ancestor stack, and an `Unwind`\\n+    // marker is queued beneath its edges to take the name back off once the\\n+    // subtree is done — the worklist's stand-in for the `stack.pop()` that used\\n+    // to sit after the recursive loop.\\n     try! stack.push(dep_name)\\n     let sub = match m.dependencies() {\\n         case Ok(x) -> move x,\\n         case Err(_) -> { return ResolveError(msg: \\\"cannot read dependencies of `{dep_name}`\\\") }\\n     }\\n+    var children = Vector<WorkItem>()\\n     var j = 0\\n     while j < sub.len() {\\n         guard let sd = sub.get(j) else {\\n             break\\n         }\\n-        let _ = try visit(\\n-            sd.name, sd.source, sd.version, dep_name, dep_dir,\\n-            &var resolution, &var reqs, &var stack,\\n-        )\\n+        try! children.push(WorkItem.Edge(\\n+            dep_name: sd.name,\\n+            source: sd.source,\\n+            constraint: sd.version,\\n+            requirer: dep_name,\\n+            base_dir: dep_dir,\\n+        ))\\n         j = j + 1\\n     }\\n-    let _ = stack.pop()\\n+    try! work.push(WorkItem.Unwind)\\n+    push_reversed(&var work, &var children)\\n \\n     true\\n }\\n@@ -466,22 +538,33 @@ public func resolve(root: Manifest) -> Result<Resolution, ResolveError> {\\n     var resolution = Resolution()\\n     var reqs = ReqList()\\n     var stack = Vector<String>()\\n+    var work = Vector<WorkItem>()\\n \\n     let deps = match root.dependencies() {\\n         case Ok(x) -> move x,\\n         case Err(_) -> { return ResolveError(msg: \\\"cannot read root dependencies\\\") }\\n     }\\n+    // The root's own edges seed the worklist. The root itself never joins the\\n+    // ancestor stack (it never did) — a package depending on itself is the\\n+    // self-dependency error, not a cycle.\\n+    var seeds = Vector<WorkItem>()\\n     var i = 0\\n     while i < deps.len() {\\n         guard let d = deps.get(i) else {\\n             break\\n         }\\n-        let _ = try visit(\\n-            d.name, d.source, d.version, root.name(), root.root_dir,\\n-            &var resolution, &var reqs, &var stack,\\n-        )\\n+        try! seeds.push(WorkItem.Edge(\\n+            dep_name: d.name,\\n+            source: d.source,\\n+            constraint: d.version,\\n+            requirer: root.name(),\\n+            base_dir: root.root_dir,\\n+        ))\\n         i = i + 1\\n     }\\n+    push_reversed(&var work, &var seeds)\\n+\\n+    let _ = try visit_all(&var work, &var resolution, &var reqs, &var stack)\\n \\n     let _ = try validate(&resolution, &reqs)\\n \\ndiff --git a/examples/conformance/INDEX.md b/examples/conformance/INDEX.md\\nindex 12b02c6f..e888e967 100644\\n--- a/examples/conformance/INDEX.md\\n+++ b/examples/conformance/INDEX.md\\n@@ -656,6 +656,10 @@ Claim source: spec 6 *Send and Sync* + *Cooperative tasks*; designs 75, 88, 103,\\n | K98 | a delivered signal wakes a PARKED watcher, and a watched signal's default action does not run | `K98_signal_wakes_a_parked_watcher.saw` | design 272 unit 3 (SL-228). Two guarantees in one program because they are two halves of one fact: watching replaces the disposition, so the signal that would have ENDED the process instead makes a parked task runnable. A regression prints no wrong line — the process dies on the signal and the runner reports THAT, which is the clearest failure available. The park is FORCED, not raced: the watcher announces on a channel that it is entering `next()` and the signal is sent only after that announcement is read, so delivery provably reaches an already-parked task (the SL-208 io_wake precedent). `Signal.send_to_self()` is the oracle — deterministic, needs nothing outside the process, and works in a sandbox where an external signaller would not |\\n | K99 | a watch is SCOPED to its handle and SINGLY owned: the disposition is restored when the handle dies, and a second live watch of the same signal is refused | `K99_watch_is_scoped_and_singly_owned.saw` | design 272 unit 3, the ruled double-watch policy and the ownership argument under it — one row rather than two because the refusal is what makes the restore meaningful, and a broadcast policy would have left nobody owning it. Three steps in order: a second watch is `AlreadyExists`; the handle is dropped; watching again SUCCEEDS. The third is load-bearing — a watch that never released its claim passes the first two silently and fails only there. A different signal is watched alongside to show the refusal is per-signal, not a global one-at-a-time |\\n | K100 | graceful shutdown rides CANCELLATION and deinits: a parked signal watcher is not reported as a deadlock, and a cancelled worker's values are destroyed exactly once | `K100_shutdown_rides_cancellation.saw` | design 272 unit 3 — the ruled no-exit-registry pattern end to end, and the program the unit exists for. Three things that must hold together: a task parked on a signal while every sibling is io-parked is a CORRECT state that looks exactly like a hang, and the design-230 quiescent walk must not report it (a watcher is an ordinary io park, so `anyio` holds); the signal cancels the worker and cancellation WAKES an io-parked task (design 102 — the worker is on a socket whose peer never writes, so only the cancel can end it); and the worker's owned values deinit as it unwinds, exactly once, with no registry and no atexit hook. The group's Deinit is the synchronization point on purpose: `cancel()` IS the handle's fate (design 242) so it cannot also be joined, and the structured join is what the pattern rests on rather than a way around the fate rule |\\n+| K101 | a suspension reached through a module-PRIVATE free function of an IMPORTED module embeds and cedes — a free callee's VISIBILITY is not one of the variables | `coro_imported_callee_discovery_matrix.saw` + `coro_imported_private_helper_suspends.saw` | SL-280 — K92's free-function rule at the one route it did not reach. Designs 96/101/104 say a suspending call embeds or errors cleanly; design 249 gives a module-private free function a `$m$<module>` codegen tag and a `public` one none, and three of the transform's tables (`funcs_by_name`, `imported_free_fns`, `_classify_call`) were keyed by the bare NAME while the effect graph's edges carry the tagged symbol. The edge never matched the table, design 208's splice was declined with no diagnostic, no frame was built for the helper or for anything below it, and the suspension was dropped in silence — the third outcome again, reached by keying rather than by classification. The matrix file is the row's oracle: twelve routes from a driven root to one `yield_now()` (E1/E2 entry-module controls, S0-S9 across the boundary) counting `__saw_drive_steps`, because every row returns the same VALUE whether or not the suspension survived and only the step count tells a frame from a plain call. The five cells that were 0 (S1, S3, S5, S8, S9) are exactly the private free-function ones; S7 is their `public` twin, byte-identical but for one word, and S2/S6 are the method and generic edges that were immune because `_scan_method_callees` and `_promote_joining_body` reach them by another route. The pin file is that private/public pair alone |\\n+| K102 | an io park reached through an imported module-private free function is a FRAME park, never the out-of-frame blocking fallback | `coro_imported_private_helper_parks_on_io.saw` | SL-280 — K101's runtime face, and the one that costs a stopped process rather than a lost cede. With no frame built for the helper, `stream.read()`'s `io_wait` lowers through codegen's out-of-frame fallback (`codegen/calls.py`), which registers the fd and then parks the OS THREAD — correct for a genuinely sync caller and catastrophic inside a task, because that thread is the cooperative executor's and nothing else in the process runs until the fd is ready. Found as sawtracker's `read_chunk(stream: &TcpStream, secs: Int)` factored out of an imported read loop: the server accepted exactly one connection and never ran again, its own 1s timed accept never expiring. Two oracles in one file, because a hang is a worse diagnostic than a missing symbol — five bytes round-tripped over a socketpair whose writer is a SIBLING task (a reader that blocks its thread waits for a write that can never be scheduled, so the blocking fallback cannot complete it), and the IR asserted directly: `__Frame_read_chunk$m$sl280net` present, the fallback's unbounded `__saw_exec_park(-1, …)` absent. The failure MODE — that this degrades silently at all, rather than refusing or detecting — is SL-287, filed separately |\\n+| K103 | a free function's FRAME KEY is independent of whether registration stamped a mangled symbol on it, and of which rule stamped one | `coro_module_tagged_free_callee_discovery.saw` | SL-280, and the acceptance criterion SL-274 is blocked on. The mechanism K101 records is not confined to imported bodies or to `private`: it is confined to callees whose key and written name DIFFER, so any rule that renames a free function walks into the same hole. This row reaches it through design 249's OTHER tag — two modules legally declaring one free-function name, which stamps `$M$<module>` on BOTH, including the entry module's own (`$M$root`) and the dependency's PUBLIC one. Both cells measured 0 before the fix. SL-274 gives EVERY free function a module-mangled symbol, which ends today's accidental coincidence between a public function's bare name and its key and would move the entire working half of K101's matrix onto the broken half; it is safe exactly when the answer does not depend on the tag, which is what the `imported_public` cell measures. One funnel answers it now — `sawc/frame_keys.py::callee_frame_key`, whose docstring names its entry points (obligation 1) |\\n+| K104 | a body SPLICED out of an imported module for frame building keeps its OWN module's meaning under a name collision | `coro_spliced_helper_keeps_its_own_module.saw` | SL-280 — the face that was a WRONG ANSWER rather than a dropped suspension, and why the fix could not stop at the discovery tables. Design 208's splice copies an imported body into the entry module so a frame can be built from it, and `_classify_call` decided which frame a call embeds by comparing the call's PLAIN NAME against the driven closure. Once the entry module's same-named `tagged_helper` was itself in that closure, the spliced body's call to the DEPENDENCY's helper matched the entry module's and the entry module's frame was embedded in its place: the dependency's helper never ran and the program answered 2 where 101 is right, with nothing reported. The values are the oracle (+1 versus +100, so which of the two ran is visible), and the entry-module control one line later says the fix did not merely swap the confusion around. The same keying carries the emission INVARIANT the splice needs — the imported module owns the symbol, the clone is filtered back out of `program.functions` by frame key once its frame exists, and a clone left behind is a half-lowered husk that either names `self` in a free function at the post-transform re-check or double-defines a symbol llvmlite refuses |\\n \\n ## Visibility and module boundaries\\n \\ndiff --git a/examples/coro_imported_callee_discovery_matrix.saw b/examples/coro_imported_callee_discovery_matrix.saw\\nnew file mode 100644\\nindex 00000000..9fedc1f3\\n--- /dev/null\\n+++ b/examples/coro_imported_callee_discovery_matrix.saw\\n@@ -0,0 +1,80 @@\\n+// SL-280: the coroutine transform must DISCOVER a suspending callee however it\\n+// is reached, and a free function's module VISIBILITY must not be one of the\\n+// variables. Twelve rows, each a different route from a driven root to one\\n+// `yield_now()`.\\n+//\\n+// `__saw_drive_steps` counts Pending resumes, so the oracle is whether the\\n+// SUSPENSION SURVIVED the transform, not whether the answer is right — every\\n+// row returns 2 either way, and only the step count tells a driven frame from a\\n+// plain call. 1 = the suspension is in a frame and the task cedes. 0 = it was\\n+// dropped in silence, which for a `yield_now()` means a task that never yields\\n+// and for a `stream.read()` means the blocked executor of\\n+// `coro_imported_private_helper_parks_on_io.saw`.\\n+//\\n+// Measured before the fix: E1 E2 S0 S2 S4 S6 S7 = 1, and S1 S3 S5 S8 S9 = 0.\\n+// The five failing rows are exactly the ones whose callee is a plain\\n+// module-PRIVATE free function of an imported body — design 249 tags it\\n+// `$m$sl280matrix`, and three of the transform's tables were keyed by the bare\\n+// name. `callee_frame_key` is the one answer they all ask now.\\n+//\\n+// EXPECT: success\\n+// COMPILE-FLAGS: --module-path sl280matrix={TESTDIR}/modules/sl280matrix --module-path sl280matrix2={TESTDIR}/modules/sl280matrix2\\n+// EXPECT-OUTPUT:\\n+// E1 1\\n+// E2 1\\n+// S0 1\\n+// S1 1\\n+// S2 1\\n+// S3 1\\n+// S4 1\\n+// S5 1\\n+// S6 1\\n+// S7 1\\n+// S8 1\\n+// S9 1\\n+\\n+import std.task.{yield_now}\\n+import sl280matrix.{s0_entry, s1_entry, s2_entry, s3_entry, s4_entry, s6_entry,\\n+                    s7_entry, s8_entry, s9_entry, Widget}\\n+\\n+// E1: the entry-module control — a driven root reaching a private free function\\n+// of its OWN module. Always worked, and is what says the bug is about the\\n+// IMPORTED body rather than about private free functions as such.\\n+func e1_helper(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+func e1_entry(n: Int) -> Int { e1_helper(n) }\\n+\\n+// E2: the same, two hops down.\\n+func e2_inner(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+func e2_outer(n: Int) -> Int { e2_inner(n) }\\n+\\n+func e2_entry(n: Int) -> Int { e2_outer(n) }\\n+\\n+// S5's receiver has to be built here — the row is an imported METHOD reaching\\n+// its own module's private free function.\\n+func s5_entry(n: Int) -> Int {\\n+    let w = Widget(n: n)\\n+    w.s5_method()\\n+}\\n+\\n+func main() {\\n+    print(\\\"E1 {}\\\", __saw_drive_steps(e1_entry(1)))\\n+    print(\\\"E2 {}\\\", __saw_drive_steps(e2_entry(1)))\\n+    print(\\\"S0 {}\\\", __saw_drive_steps(s0_entry(1)))\\n+    print(\\\"S1 {}\\\", __saw_drive_steps(s1_entry(1)))\\n+    print(\\\"S2 {}\\\", __saw_drive_steps(s2_entry(1)))\\n+    print(\\\"S3 {}\\\", __saw_drive_steps(s3_entry(1)))\\n+    print(\\\"S4 {}\\\", __saw_drive_steps(s4_entry(1)))\\n+    print(\\\"S5 {}\\\", __saw_drive_steps(s5_entry(1)))\\n+    print(\\\"S6 {}\\\", __saw_drive_steps(s6_entry(1)))\\n+    print(\\\"S7 {}\\\", __saw_drive_steps(s7_entry(1)))\\n+    print(\\\"S8 {}\\\", __saw_drive_steps(s8_entry(1)))\\n+    print(\\\"S9 {}\\\", __saw_drive_steps(s9_entry(1)))\\n+}\\ndiff --git a/examples/coro_imported_private_helper_parks_on_io.saw b/examples/coro_imported_private_helper_parks_on_io.saw\\nnew file mode 100644\\nindex 00000000..6cd9a680\\n--- /dev/null\\n+++ b/examples/coro_imported_private_helper_parks_on_io.saw\\n@@ -0,0 +1,51 @@\\n+// SL-280: a real io park reached through a module-PRIVATE free function of an\\n+// IMPORTED module — the cell of the issue's 2x2 that WEDGED, and the shape the\\n+// bug was found as (sawtracker's `read_chunk(stream: &TcpStream, secs: Int)`,\\n+// factored out of the read loop in an imported `http` module).\\n+//\\n+// The failure was not a wrong answer, it was a stopped process. No frame was\\n+// built for `read_chunk`, so `stream.read()` lowered through codegen's\\n+// out-of-frame park fallback (`codegen/calls.py`), which registers the fd and\\n+// then BLOCKS the OS thread — and that thread is the cooperative executor's, so\\n+// the server accepted exactly one connection and never ran again. This test\\n+// round-trips five bytes over a socketpair instead, which the blocking fallback\\n+// cannot do: the writer is a sibling TASK, so a reader that blocks its thread\\n+// waits for a write that can never be scheduled, and the run hangs.\\n+//\\n+// The IR property is pinned HERE too, beside the runtime one, because the two\\n+// answer different halves and a hang is a worse diagnostic than a missing\\n+// symbol. `__Frame_read_chunk$m$sl280net` says the private imported helper got\\n+// a frame; `call void @\\\"__saw_exec_park\\\"(i64 -1` is the out-of-frame fallback's\\n+// unbounded park, which the pre-fix build of this very file emits once and the\\n+// fixed build emits never.\\n+//\\n+// EXPECT: success\\n+// COMPILE-FLAGS: --module-path sl280net={TESTDIR}/modules/sl280net\\n+// EXPECT-IR-CONTAINS: __Frame_read_chunk$m$sl280net\\n+// EXPECT-IR-ABSENT: call void @\\\"__saw_exec_park\\\"(i64 -1\\n+// EXPECT-OUTPUT:\\n+// 5\\n+// 5\\n+\\n+import std.net.{TcpStream, IoError}\\n+import sl280net.{read_request}\\n+\\n+func server(stream: TcpStream) -> Int {\\n+    read_request(&stream)\\n+}\\n+\\n+func client(stream: TcpStream) -> Int {\\n+    match stream.write(\\\"hello\\\") {\\n+        case Ok(_) -> 5,\\n+        case Err(_) -> -1\\n+    }\\n+}\\n+\\n+func main() {\\n+    let (a, b) = TcpStream.pair()\\n+    var group = TaskGroup()\\n+    let hs = group.spawn(server(move a))\\n+    let hc = group.spawn(client(move b))\\n+    print(hs.join())     // 5 bytes read, through an imported private helper\\n+    print(hc.join())     // 5 bytes written\\n+}\\ndiff --git a/examples/coro_imported_private_helper_suspends.saw b/examples/coro_imported_private_helper_suspends.saw\\nindex 0d5e35b3..948b07d6 100644\\n--- a/examples/coro_imported_private_helper_suspends.saw\\n+++ b/examples/coro_imported_private_helper_suspends.saw\\n@@ -1,16 +1,18 @@\\n-// XFAIL: SL-280 a suspension reached through a MODULE-PRIVATE free function of\\n-// an IMPORTED module is silently dropped — no frame is built for the helper, so\\n-// the transform lowers it as a plain call. The `public` twin beside it works,\\n-// which is the whole diagnosis: `coro_transform.py`'s `imported_free_fns` table\\n-// is keyed by the UNMANGLED name while the effect graph keys a free function by\\n+// SL-280: a suspension reached through a MODULE-PRIVATE free function of an\\n+// IMPORTED module survives the coroutine transform, exactly as the `public`\\n+// twin beside it always did. That pair — byte-identical but for one word — is\\n+// the whole diagnosis: `coro_transform.py`'s `imported_free_fns` table was\\n+// keyed by the UNMANGLED name while the effect graph keys a free function by\\n // `mangled_symbol or name`, and design 249 gives only a module-PRIVATE free\\n-// function a `$m$<module>` mangled symbol. The edge key therefore never matches\\n-// the table, the splice declines the callee, and nothing is said.\\n+// function a `$m$<module>` mangled symbol. The edge key never matched the\\n+// table, the splice declined the callee, no frame was built, and nothing was\\n+// said. Both ends ask `frame_keys.callee_frame_key` now.\\n //\\n-// This is the compile-time face of the SL-280 wedge. The runtime face is worse:\\n-// when the dropped suspension is a std.net park rather than a `yield_now`, the\\n-// park lowers through the out-of-frame codegen fallback in `codegen/calls.py`,\\n-// which registers the fd and then BLOCKS THE OS THREAD — and that thread is the\\n+// This is the compile-time face of the SL-280 wedge. The runtime face is worse,\\n+// and `coro_imported_private_helper_parks_on_io.saw` pins it: when the dropped\\n+// suspension is a std.net park rather than a `yield_now`, the park lowers\\n+// through the out-of-frame codegen fallback in `codegen/calls.py`, which\\n+// registers the fd and then BLOCKS THE OS THREAD — and that thread is the\\n // cooperative executor's, so the whole process stops until the fd is ready.\\n //\\n // `__saw_drive_steps` counts Pending resumes, so it reports whether the\\ndiff --git a/examples/coro_module_tagged_free_callee_discovery.saw b/examples/coro_module_tagged_free_callee_discovery.saw\\nnew file mode 100644\\nindex 00000000..a240dda7\\n--- /dev/null\\n+++ b/examples/coro_module_tagged_free_callee_discovery.saw\\n@@ -0,0 +1,49 @@\\n+// SL-280, the tag-independence row: a free function's frame key must be the\\n+// SAME question whether or not registration stamped a mangled symbol on it, and\\n+// whichever rule stamped one.\\n+//\\n+// Two modules declaring one free-function name is legal (design 249), and the\\n+// answer is a `$M$<module>` tag on BOTH — on the entry module's private one\\n+// (`$M$root`) and on the dependency's PUBLIC one (`$M$sl280tag`). Neither is a\\n+// `private`-only `$m$` tag, so this reaches the mechanism from the other side:\\n+// what the transform was keying on was the bare NAME, and every rule that\\n+// renames a free function walks into the same hole. Note the entry-module row —\\n+// the bug is not confined to imported bodies, it is confined to callees whose\\n+// key and name differ.\\n+//\\n+// This is the acceptance criterion for SL-274, which is blocked on this issue.\\n+// SL-274 gives EVERY free function a module-mangled symbol — so after it lands,\\n+// a PUBLIC free function's frame key stops coinciding with its bare name, and\\n+// the whole working half of this bug's matrix would move to the broken half. It\\n+// is safe exactly when the funnel's answer does not depend on the tag, which is\\n+// what the `imported_public` row measures.\\n+//\\n+// Measured before the fix: entry_tagged 0, imported_public 0.\\n+//\\n+// `coro_spliced_helper_keeps_its_own_module.saw` is the other half of this\\n+// fixture — the same two modules, arranged so the name collision produces a\\n+// WRONG ANSWER rather than a dropped suspension.\\n+//\\n+// EXPECT: success\\n+// COMPILE-FLAGS: --module-path sl280tag={TESTDIR}/modules/sl280tag\\n+// EXPECT-OUTPUT:\\n+// entry_tagged 1\\n+// imported_public 1\\n+\\n+import std.task.{yield_now}\\n+import sl280tag.{reach_tagged}\\n+\\n+/// Carries `$M$root`, because the dependency declares this name too.\\n+func tagged_helper(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+func entry_root(n: Int) -> Int { tagged_helper(n) }\\n+\\n+func main() {\\n+    // The entry module's own tagged helper, one frame below a driven root.\\n+    print(\\\"entry_tagged {}\\\", __saw_drive_steps(entry_root(1)))\\n+    // A PUBLIC imported helper that carries a mangled symbol — the SL-274 shape.\\n+    print(\\\"imported_public {}\\\", __saw_drive_steps(reach_tagged(1)))\\n+}\\ndiff --git a/examples/coro_spliced_helper_keeps_its_own_module.saw b/examples/coro_spliced_helper_keeps_its_own_module.saw\\nnew file mode 100644\\nindex 00000000..902c20a7\\n--- /dev/null\\n+++ b/examples/coro_spliced_helper_keeps_its_own_module.saw\\n@@ -0,0 +1,43 @@\\n+// SL-280: a body spliced out of an imported module into the entry module for\\n+// frame building keeps its OWN module's meaning. Two modules may declare one\\n+// free-function name (design 249), and the spliced body's call must still\\n+// reach the helper its author wrote.\\n+//\\n+// This is the face of the bug that was a WRONG ANSWER rather than a dropped\\n+// suspension, and it is why the fix could not stop at the discovery tables.\\n+// `_classify_call` decided which frame a call embeds by comparing the call's\\n+// PLAIN NAME against the driven closure. So once the entry module's\\n+// `tagged_helper` was itself in that closure — which the `entry_direct` row\\n+// below is what arranges — the spliced `reach_tagged` body's call to\\n+// `sl280tag`'s `tagged_helper` matched the entry module's instead, and the\\n+// entry module's frame was embedded in its place. The dependency's helper never\\n+// ran, nothing was reported, and the program answered 2 where 101 is right.\\n+//\\n+// Measured before the fix: entry_direct 1, imported_value 2.\\n+//\\n+// EXPECT: success\\n+// COMPILE-FLAGS: --module-path sl280tag={TESTDIR}/modules/sl280tag\\n+// EXPECT-OUTPUT:\\n+// entry_direct 1\\n+// imported_value 101\\n+// entry_value 2\\n+\\n+import std.task.{yield_now}\\n+import sl280tag.{reach_tagged}\\n+\\n+/// Same name as `sl280tag`'s, a different body, and +1 rather than +100 — so\\n+/// which of the two ran is visible in the value.\\n+func tagged_helper(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+func main() {\\n+    // Driving the entry module's helper puts it in the driven closure, which is\\n+    // the precondition for the collision.\\n+    print(\\\"entry_direct {}\\\", __saw_drive_steps(tagged_helper(1)))\\n+    // The imported caller reaches ITS module's helper: 1 + 100.\\n+    print(\\\"imported_value {}\\\", __saw_drive(reach_tagged(1)))\\n+    // …and the entry module's own call still reaches the entry module's: 1 + 1.\\n+    print(\\\"entry_value {}\\\", __saw_drive(tagged_helper(1)))\\n+}\\ndiff --git a/examples/modules/sl280matrix/lib.saw b/examples/modules/sl280matrix/lib.saw\\nnew file mode 100644\\nindex 00000000..447c10d0\\n--- /dev/null\\n+++ b/examples/modules/sl280matrix/lib.saw\\n@@ -0,0 +1,106 @@\\n+// Fixture for `coro_imported_callee_discovery_matrix.saw` (SL-280): the\\n+// IMPORTED module of the discovery matrix.\\n+//\\n+// Rows S0-S9 differ only in HOW the suspension is reached from a public entry\\n+// point of this module. Five of them (S1, S3, S5, S8, S9) were silently dropped\\n+// before the `callee_frame_key` funnel landed; the other five worked, and WHY\\n+// each worked is what localized the bug — a method callee is enqueued by\\n+// `_scan_method_callees`, a generic one by `_promote_joining_body`, and a\\n+// `public` free function's frame key happened to equal its bare name.\\n+\\n+import std.task.{yield_now}\\n+import sl280matrix2.{s4_target}\\n+\\n+public struct Widget { n: Int }\\n+\\n+extension Widget {\\n+    func hop(&self) -> Int {\\n+        yield_now()\\n+        self.n + 1\\n+    }\\n+}\\n+\\n+// S0: this module's own entry suspends DIRECTLY (SL-208's fixed cell).\\n+public func s0_entry(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+// S1: -> its own module-PRIVATE free function. The headline cell.\\n+func s1_helper(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+public func s1_entry(n: Int) -> Int { s1_helper(n) }\\n+\\n+// S2: -> its own extension METHOD (`_scan_method_callees` reaches it).\\n+public func s2_entry(n: Int) -> Int {\\n+    let w = Widget(n: n)\\n+    w.hop()\\n+}\\n+\\n+// S3: -> TWO of its own private free functions (the chain, not just one hop).\\n+func s3_inner(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+func s3_outer(n: Int) -> Int { s3_inner(n) }\\n+\\n+public func s3_entry(n: Int) -> Int { s3_outer(n) }\\n+\\n+// S4: -> a SECOND imported module's public function (a qualified call, which\\n+// parses as a MethodCall and so was never on the broken path).\\n+public func s4_entry(n: Int) -> Int { s4_target(n) }\\n+\\n+// S5: an imported METHOD -> this module's private free function.\\n+func s5_helper(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+extension Widget {\\n+    public func s5_method(&self) -> Int { s5_helper(self.n) }\\n+}\\n+\\n+// S6: -> its own private GENERIC free function (`_promote_joining_body`\\n+// reaches it, which is why the generic edge worked and the plain one did not).\\n+func s6_helper<T>(n: Int, witness: T) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+public func s6_entry(n: Int) -> Int { s6_helper<Int>(n, 0) }\\n+\\n+// S7: -> its own PUBLIC free function. The sharp control for S1: byte-identical\\n+// but for one word, and design 249 tags only the private one.\\n+public func s7_helper(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+public func s7_entry(n: Int) -> Int { s7_helper(n) }\\n+\\n+// S8: -> its own private function taking a REFERENCE parameter. The shape of\\n+// the real `read_chunk(stream: &TcpStream, secs: Int)`.\\n+func s8_helper(w: &Widget) -> Int {\\n+    yield_now()\\n+    w.n + 1\\n+}\\n+\\n+public func s8_entry(n: Int) -> Int {\\n+    let w = Widget(n: n)\\n+    s8_helper(&w)\\n+}\\n+\\n+// S9: -> its own private function through a `let` statement rather than a tail.\\n+func s9_helper(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\n+\\n+public func s9_entry(n: Int) -> Int {\\n+    let r = s9_helper(n)\\n+    r\\n+}\\ndiff --git a/examples/modules/sl280matrix2/lib.saw b/examples/modules/sl280matrix2/lib.saw\\nnew file mode 100644\\nindex 00000000..0a909adb\\n--- /dev/null\\n+++ b/examples/modules/sl280matrix2/lib.saw\\n@@ -0,0 +1,9 @@\\n+// Fixture for `coro_imported_callee_discovery_matrix.saw` (SL-280): the SECOND\\n+// imported module, reached from the first one in row S4.\\n+\\n+import std.task.{yield_now}\\n+\\n+public func s4_target(n: Int) -> Int {\\n+    yield_now()\\n+    n + 1\\n+}\\ndiff --git a/examples/modules/sl280net/lib.saw b/examples/modules/sl280net/lib.saw\\nnew file mode 100644\\nindex 00000000..e1bbc1ec\\n--- /dev/null\\n+++ b/examples/modules/sl280net/lib.saw\\n@@ -0,0 +1,31 @@\\n+// Fixture for `coro_imported_private_helper_parks_on_io.saw` (SL-280).\\n+//\\n+// The wedging cell of the issue's 2x2: the read LOOP lives in an imported\\n+// module and its read sits one frame deeper still, in a module-PRIVATE free\\n+// function taking the stream BY REFERENCE. That is the real `read_chunk(stream:\\n+// &TcpStream, secs: Int)` this was found as.\\n+//\\n+// Three of the fix's parts meet here. The callee is keyed `read_chunk$m$sl280net`\\n+// by design 249 while the splice table used to be keyed `read_chunk`, so the\\n+// edge was declined. Its only suspension is a buried `stream.read()`, which the\\n+// effect fixpoint cannot see — so design 96's structural fallback is what has to\\n+// carry it, and that fallback used to be computed over the entry module alone,\\n+// before the walk, and could never hold an imported body. And the parameter is a\\n+// REFERENCE, which is what the spliced body has to keep meaning after it lands\\n+// in a module that never declared it.\\n+\\n+import std.net.{TcpStream, IoError}\\n+\\n+/// Module-PRIVATE, takes the stream by reference, and its only suspension is a\\n+/// buried std method call.\\n+func read_chunk(stream: &TcpStream, budget: Int) -> Int {\\n+    match stream.read() {\\n+        case Ok(d) -> d.len() + budget,\\n+        case Err(_) -> -1\\n+    }\\n+}\\n+\\n+/// The public entry — the \\\"read loop\\\" frame. One more frame down to the park.\\n+public func read_request(stream: &TcpStream) -> Int {\\n+    read_chunk(&stream, 0)\\n+}\\ndiff --git a/examples/modules/sl280tag/lib.saw b/examples/modules/sl280tag/lib.saw\\nnew file mode 100644\\nindex 00000000..e9ee401c\\n--- /dev/null\\n+++ b/examples/modules/sl280tag/lib.saw\\n@@ -0,0 +1,24 @@\\n+// Fixture for `coro_module_tagged_free_callee_discovery.saw` (SL-280).\\n+//\\n+// This module declares a free function whose NAME the entry module also\\n+// declares. Design 249 answers that with a `$M$<module>` codegen tag on BOTH —\\n+// including the PUBLIC one here, and including the entry module's, whose tag is\\n+// `$M$root`. So this fixture puts a mangled symbol on a free function through a\\n+// route that has nothing to do with `private`, which is what makes it the\\n+// tag-independence probe: the funnel must not care WHY a symbol is there, or\\n+// whether there is one at all.\\n+\\n+import std.task.{yield_now}\\n+\\n+/// PUBLIC, and tagged `tagged_helper$M$sl280tag` all the same. Returns n + 100,\\n+/// so a caller that reaches the ENTRY module's same-named helper by mistake is\\n+/// visible in the VALUE and not only in the step count.\\n+public func tagged_helper(n: Int) -> Int {\\n+    yield_now()\\n+    n + 100\\n+}\\n+\\n+/// The imported caller. Its body names `tagged_helper` bare, and that name has\\n+/// to keep meaning THIS module's after the body is spliced into an entry module\\n+/// that declares one of its own.\\n+public func reach_tagged(n: Int) -> Int { tagged_helper(n) }\\ndiff --git a/sawc/coro_transform.py b/sawc/coro_transform.py\\nindex a7c154c2..7b2809da 100644\\n--- a/sawc/coro_transform.py\\n+++ b/sawc/coro_transform.py\\n@@ -151,6 +151,7 @@ from ast_nodes import (\\n )\\n from type_identity import type_identity as _type_identity\\n from typechecker.effects import _first_pristine\\n+from frame_keys import callee_frame_key\\n from ast_walk import (child_nodes, control_blocks, control_heads, map_nodes,\\n                       pattern_binding_names)\\n \\n@@ -2142,7 +2143,12 @@ class _FrameBuilder:\\n                                           reference_mutable=False)\\n                                   if self.has_recv else None)\\n         else:\\n-            self.name = func.name\\n+            # SL-280: a free function's frame is named by its FRAME KEY, not by\\n+            # its written name. Two functions of one name — an entry-module\\n+            # `helper` and an imported module's private `helper$m$dep` spliced\\n+            # in beside it — are two frames, and `__Frame_helper` for both would\\n+            # be one LLVM symbol carrying two state machines.\\n+            self.name = callee_frame_key(func)\\n             self.recv_type = None\\n             self.recv_ptr_type = None\\n             self.recv_pointee = None\\n@@ -2823,7 +2829,11 @@ class _FrameBuilder:\\n         if isinstance(expr, FunctionCall):\\n             if getattr(expr, 'type_args', None):\\n                 return False\\n-            return (expr.name in self._suspends\\n+            # SL-280: `self._suspends` holds FRAME KEYS (it is `set(closure)`),\\n+            # so the membership test asks `callee_frame_key`, never the written\\n+            # name. A tagged callee read as its bare name is a suspension\\n+            # answered False — which is a park lowered in place.\\n+            return (callee_frame_key(expr) in self._suspends\\n                     or self._is_blocking_extern(expr.name))\\n         if isinstance(expr, MethodCall):\\n             return (getattr(expr, 'is_chan_recv', False)\\n@@ -4376,7 +4386,8 @@ class _FrameBuilder:\\n             if found[0]:\\n                 return\\n             if isinstance(n, FunctionCall) and (\\n-                    n.name in _SUSPEND_CALLS or n.name in self._suspends):\\n+                    n.name in _SUSPEND_CALLS\\n+                    or callee_frame_key(n) in self._suspends):   # SL-280\\n                 found[0] = True\\n                 return\\n             # design 103 (A6): a blocking-extern call is a suspension point (the\\n@@ -5157,7 +5168,15 @@ class _FrameBuilder:\\n             # frame is a method frame (`__recv` points at the receiver's storage);\\n             # its key is `{struct}_{method}`, matching `_FrameBuilder.name`.\\n             return self._classify_method_call(stmt, target, is_ret)\\n-        if fc.name not in self._suspends:\\n+        # SL-280: the callee is named by its FRAME KEY at both ends — the\\n+        # membership test below and the `callee` this returns, which\\n+        # `_callee_fb` resolves against `fbs`. Reading `fc.name` here made the\\n+        # classifier disagree with the closure walk: a private imported helper\\n+        # was invisible to it, and an entry-module function that merely SHARED\\n+        # a name with the real callee was embedded in its place (a silently\\n+        # wrong answer, not just a dropped suspension).\\n+        key = callee_frame_key(fc)\\n+        if key not in self._suspends:\\n             return None\\n         if getattr(fc, 'type_args', None):\\n             # design 70 (A5): a TOP driven/spawned generic is monomorphized before\\n@@ -5168,7 +5187,7 @@ class _FrameBuilder:\\n                 f\\\"coroutine transform: a nested suspending call to a generic \\\"\\n                 f\\\"function `{fc.name}` inside `{self.name}` is not yet supported \\\"\\n                 f\\\"(design 70 A5-rest)\\\", fc)\\n-        return {'callee': fc.name, 'args': list(fc.arguments),\\n+        return {'callee': key, 'args': list(fc.arguments),\\n                 'plan': getattr(fc, 'arg_plan', None), 'target': target,\\n                 'ret': is_ret, 'line': getattr(fc, 'line', 0) or 0}\\n \\n@@ -5210,7 +5229,7 @@ class _FrameBuilder:\\n         # `FunctionCall` (callee keyed by name, no `__recv`). Only when the\\n         # callee is in the driven closure (`self._suspends`); a non-suspending\\n         # cross-module free call stays a plain module call for codegen.\\n-        mfree = getattr(mc, 'module_free_call', None)\\n+        mfree = callee_frame_key(mc)   # SL-280: the funnel, not a raw read\\n         if mfree is not None and mfree in self._suspends:\\n             if getattr(mc, 'type_args', None):\\n                 # Mirror `_classify_call`'s generic-nested refusal (design 70).\\n@@ -5339,8 +5358,12 @@ class _FrameBuilder:\\n         parses as a `MethodCall` but must be treated as a suspending free-call\\n         everywhere a same-module `FunctionCall` to a driven callee is — every\\n         suspension-detection site below, so the split/embed and the rejections\\n-        see it, never a plain lowering (the SL-208 wedge).\\\"\\\"\\\"\\n-        mfree = getattr(mc, 'module_free_call', None)\\n+        see it, never a plain lowering (the SL-208 wedge).\\n+\\n+        SL-280: the key comes from `callee_frame_key`, which is also what says\\n+        a `MethodCall` is a genuine METHOD (it answers None) — so this and the\\n+        classifier cannot drift on which calls are free calls.\\\"\\\"\\\"\\n+        mfree = callee_frame_key(mc)\\n         return mfree is not None and mfree in self._suspends\\n \\n     def _method_call_suspends(self, mc):\\n@@ -5529,7 +5552,8 @@ class _FrameBuilder:\\n             if isinstance(n, ClosureExpr):\\n                 in_closure = True\\n             if isinstance(n, FunctionCall) and (\\n-                    n.name in self._suspends or n.name in _SUSPEND_CALLS):\\n+                    callee_frame_key(n) in self._suspends      # SL-280\\n+                    or n.name in _SUSPEND_CALLS):\\n                 found.append((\\\"fn\\\", n))\\n             # design 103 (A6): a blocking-extern call in a position the offload\\n             # desugar cannot occupy (buried in a larger expression, a `try!`, an\\n@@ -9317,13 +9341,16 @@ def _default_expr_suspends(expr, tc):\\n         if n is None:\\n             return False\\n         if isinstance(n, FunctionCall):\\n-            if _fn_suspends(getattr(n, 'resolved_symbol', None) or n.name):\\n+            # SL-280: `callee_frame_key` is the one composer of both spellings\\n+            # this used to inline (`resolved_symbol or name` here, and\\n+            # `module_free_call` below).\\n+            if _fn_suspends(callee_frame_key(n)):\\n                 return True\\n         elif isinstance(n, MethodCall):\\n             if (getattr(n, 'is_yield_intrinsic', False)\\n                     or getattr(n, 'is_chan_recv', False)):\\n                 return True\\n-            if _fn_suspends(getattr(n, 'module_free_call', None)):\\n+            if _fn_suspends(callee_frame_key(n)):\\n                 return True\\n             if _suspending_method_target(n, tc).suspends:\\n                 return True\\n@@ -10455,9 +10482,12 @@ def _rewrite_drive_sites(node, roots, params_of, tc, src_file=None):\\n                                   inner, params_of(key), inner.method_name,\\n                                   tc, src_file)])\\n             return node\\n-        node.name = prefix + inner.name\\n+        # SL-280: the driver is named after the callee's FRAME KEY, matching\\n+        # `_FrameBuilder.name` and the root `_effect_record_driven` recorded.\\n+        key = callee_frame_key(inner)\\n+        node.name = prefix + key\\n         node.arguments = [_ref_arg_to_ptr(a) for a in _arity_arguments(\\n-            inner, params_of(inner.name), inner.name, tc, src_file)]\\n+            inner, params_of(key), inner.name, tc, src_file)]\\n         return node\\n     if isinstance(node, ASTNode):\\n         # A drive site is rewritten IN PLACE (the `FunctionCall` keeps its\\n@@ -10545,7 +10575,10 @@ def _find_method(program, struct_name, method_name, method_symbol=None):\\n \\n \\n def _called_function_names(decl, out):\\n-    \\\"\\\"\\\"Every free-function name `decl`'s body CALLS, added to `out`.\\\"\\\"\\\"\\n+    \\\"\\\"\\\"Every free-function FRAME KEY `decl`'s body CALLS, added to `out`.\\n+\\n+    SL-280: keys, not written names — `removed`/`consumed` are frame keys, and\\n+    the two sets are intersected.\\\"\\\"\\\"\\n     body = getattr(decl, 'body', None)\\n     if body is None:\\n         return out\\n@@ -10557,7 +10590,7 @@ def _called_function_names(decl, out):\\n             continue\\n         seen.add(id(node))\\n         if isinstance(node, FunctionCall):\\n-            out.add(node.name)\\n+            out.add(callee_frame_key(node))\\n         stack.extend(_all_child_nodes(node))\\n     return out\\n \\n@@ -10631,8 +10664,9 @@ def _consume_templates_naming_removed(program, removed, readded,\\n     consumed = set(removed) - set(readded)\\n     if not consumed:\\n         return\\n-    templates = {f.name: f for f in program.functions\\n-                 if getattr(f, 'type_params', None) and f.name not in removed}\\n+    templates = {callee_frame_key(f): f for f in program.functions   # SL-280\\n+                 if getattr(f, 'type_params', None)\\n+                 and callee_frame_key(f) not in removed}\\n     changed = True\\n     while changed:\\n         changed = False\\n@@ -10668,7 +10702,7 @@ def _names_the_survivors_call(program, removed, extra_decls):\\n     promoted call site ends up).\\\"\\\"\\\"\\n     live = set()\\n     for f in program.functions:\\n-        if f.name not in removed:\\n+        if callee_frame_key(f) not in removed:   # SL-280\\n             _called_function_names(f, live)\\n     for decl in extra_decls:\\n         _called_function_names(decl, live)\\n@@ -11047,7 +11081,13 @@ def _promote_nested_generic_methods(program, funcs_by_name, seed_names, all_exts\\n             if clone is not None and getattr(clone, 'body', None) is not None:\\n                 enqueue(clone.body)\\n         for fc in _iter_function_calls(body):\\n-            callee = funcs_by_name.get(fc.name)\\n+            # SL-280: `funcs_by_name` is keyed by FRAME KEY, so the free-fn\\n+            # descent asks for one. Reading `fc.name` stopped this walk dead at\\n+            # any callee registration had stamped a symbol on (`$m$`/`$M$`/an\\n+            # overload signature) — and a suspending generic METHOD call below\\n+            # such a callee then got no instantiation, which is DF-218m's\\n+            # silent plain-call lowering with a new way in.\\n+            callee = funcs_by_name.get(callee_frame_key(fc))\\n             if callee is not None and getattr(callee, 'body', None) is not None:\\n                 enqueue(callee.body)\\n \\n@@ -11107,7 +11147,13 @@ def transform_program(program, typechecker, imported_ast=None):\\n     # frame and differ only in where the group comes from.\\n     bg_spawn_roots = set(\\n         getattr(typechecker, \\\"_background_spawn_roots\\\", set()) or set())\\n-    funcs_by_name = {f.name: f for f in program.functions}\\n+    # SL-280: keyed by FRAME KEY, which is what the effect graph's edges, the\\n+    # driven/spawned roots and every call-site classifier name a callee by. It\\n+    # was `f.name`, and a free function whose registration stamped a symbol\\n+    # (design 249's `$m$`/`$M$` tags, design 55's `$OL$`) was then filed under a\\n+    # name no edge ever used — so its body was never found, no frame was built,\\n+    # and the suspension inside it was dropped in silence.\\n+    funcs_by_name = {callee_frame_key(f): f for f in program.functions}\\n     # design 84: a nested suspending method may be defined in an IMPORTED module\\n     # (std.net's TcpStream.read / TcpListener.accept), not the entry module. The\\n     # transform is otherwise entry-module-only, but a NON-generic method frame is\\n@@ -11388,28 +11434,89 @@ def transform_program(program, typechecker, imported_ast=None):\\n     # an infinite spin, DF-203b. Unit 2 made the effect FIXPOINT answer this\\n     # correctly too; the two routes agree now rather than one covering for the\\n     # other, which is the point of asking each its own question.\\n-    structurally_susp_fns = set()\\n-    for _fname, _f in funcs_by_name.items():\\n-        if getattr(_f, 'type_params', None):\\n-            continue\\n-        if _scan_method_callees(_f.body) or _body_has_chan_recv(_f.body):\\n-            structurally_susp_fns.add(_fname)\\n-    _susp_changed = True\\n-    while _susp_changed:\\n-        _susp_changed = False\\n-        for _key, _nd in nodes.items():\\n-            if not (isinstance(_key, tuple) and _key[0] == \\\"fn\\\"):\\n-                continue\\n-            _fname = _key[1]\\n-            if _fname in structurally_susp_fns or _fname not in funcs_by_name:\\n-                continue\\n-            for _e in _nd.edges:\\n+    # SL-280: this used to be a SET, computed once over `funcs_by_name` — the\\n+    # ENTRY module — and finished before the closure walk started. Both halves\\n+    # of that were wrong for a cross-module callee. Bodies are spliced into\\n+    # `funcs_by_name` DURING the walk (`_splice_imported_free_fn`), so an\\n+    # imported helper was not in the table when the set was built and could\\n+    # never be added to it afterwards; and the seed only ever ranged over entry\\n+    # bodies anyway. An imported helper whose only suspension is a buried std\\n+    # method call — `read_chunk(stream, secs)` calling `stream.read()`, the\\n+    # exact shape of the wedge — was therefore invisible to design 96's fallback\\n+    # no matter what the keying did. A second, independent gate on the same\\n+    # edge.\\n+    #\\n+    # It is a QUESTION now, answered on demand against BOTH body tables, so\\n+    # there is no phase to be on the wrong side of. Memoized, so the whole walk\\n+    # still costs one pass over the nodes and edges it reaches.\\n+    _struct_susp_cache = {}\\n+\\n+    def _structurally_suspends(key):\\n+        \\\"\\\"\\\"Does the free function `key` suspend, STRUCTURALLY, in a way the\\n+        effect fixpoint cannot see?\\n+\\n+        A std method's effect node is absent from this graph (the same gap\\n+        `_scan_method_callees` works around), so a call edge to one never\\n+        propagates `suspends`. A free function whose only suspension source is\\n+        a buried suspending method call — or a channel receive, which embeds\\n+        nothing and suspends absolutely (design 206) — is therefore left\\n+        `suspends=False`, and the edge-follow below would SKIP it: its caller\\n+        would emit a plain blocking call and the buried park would wedge the\\n+        whole thread (the design-96 hang at nesting depth >= 2).\\n+\\n+        Structural, and transitive through free-fn -> free-fn call edges. Only\\n+        ADDS genuinely-suspending functions (`_scan_method_callees` never\\n+        yields a non-suspending method), so there is no over-inclusion; a\\n+        function the fixpoint already marks is followed via `t.suspends`\\n+        instead and never reaches here.\\n+\\n+        Bodies are looked up in `funcs_by_name` FIRST (the entry module, plus\\n+        whatever the walk has already spliced) and then in `imported_free_fns`\\n+        — a body that has not been spliced yet still has to be able to answer,\\n+        which is the phase ordering this closes.\\n+        \\\"\\\"\\\"\\n+        return _struct_susp_probe(key, set())[1]\\n+\\n+    def _struct_susp_probe(key, visiting):\\n+        \\\"\\\"\\\"`_structurally_suspends`'s recursion. Returns (cacheable, answer).\\n+\\n+        A recursive call graph makes a plain memo unsound: breaking a cycle\\n+        answers False for the in-progress key, and a node that only saw that\\n+        provisional False would cache a False the outer frame then contradicts.\\n+        So an answer computed under a broken cycle is used and NOT stored. True\\n+        is always storable — the property is monotone, and nothing that reaches\\n+        a suspension can later stop reaching it.\\\"\\\"\\\"\\n+        if key in _struct_susp_cache:\\n+            return True, _struct_susp_cache[key]\\n+        if key in visiting:\\n+            # A cycle contributes nothing on its own: if any member really\\n+            # suspends, that member's own body is what says so.\\n+            return False, False\\n+        fn = funcs_by_name.get(key)\\n+        if fn is None:\\n+            fn = imported_free_fns.get(key)\\n+        if fn is None or getattr(fn, 'type_params', None):\\n+            _struct_susp_cache[key] = False\\n+            return True, False\\n+        visiting.add(key)\\n+        cacheable = True\\n+        answer = bool(_scan_method_callees(fn.body)\\n+                      or _body_has_chan_recv(fn.body))\\n+        if not answer:\\n+            node = nodes.get((\\\"fn\\\", key))\\n+            for _e in (node.edges if node is not None else ()):\\n                 _tgt = _e.target\\n-                if (isinstance(_tgt, tuple) and _tgt[0] == \\\"fn\\\"\\n-                        and _tgt[1] in structurally_susp_fns):\\n-                    structurally_susp_fns.add(_fname)\\n-                    _susp_changed = True\\n+                if not (isinstance(_tgt, tuple) and _tgt[0] == \\\"fn\\\"):\\n+                    continue\\n+                _ok, _sub = _struct_susp_probe(_tgt[1], visiting)\\n+                cacheable = cacheable and _ok\\n+                if _sub:\\n+                    answer = True\\n                     break\\n+        visiting.discard(key)\\n+        if answer or cacheable:\\n+            _struct_susp_cache[key] = answer\\n+        return (answer or cacheable), answer\\n \\n     # SL-208 / DF-300e: imported (cross-module) FREE functions, by codegen name.\\n     # A same-module free callee lives in `funcs_by_name` (the entry module); a\\n@@ -11421,11 +11528,52 @@ def transform_program(program, typechecker, imported_ast=None):\\n     # `methods_by_id`) and a generic INSTANCE is (`_promote_nested_generic_calls`)\\n     # — so a cross-module suspending free callee gets a frame and is embedded +\\n     # driven, instead of lowering as a plain call whose park no-ops / wedges the\\n-    # reactor (the SL-208 wedge). Entry names win, so shadow them out here.\\n+    # reactor (the SL-208 wedge). An entry body of the same key wins, so shadow\\n+    # it out here.\\n+    #\\n+    # SL-280: keyed by FRAME KEY — the same string the graph's edges carry and\\n+    # `funcs_by_name` files entry bodies under. It was `_f.name`, and design 249\\n+    # tags a module-PRIVATE free function `helper$m$dep` while leaving a `public`\\n+    # one bare, so `e.target[1]` was `helper$m$dep` and the table held `helper`:\\n+    # the test below never matched, the splice was declined, and the edge fell\\n+    # off the end of the walk with nothing said.\\n+    #\\n+    # The shadow-out now compares KEYS too, which matters in both directions: an\\n+    # entry `helper` no longer hides a dependency's tagged `helper$m$dep` (they\\n+    # are two functions and the walk needs both), and the entry body still wins\\n+    # wherever the two really are one key.\\n     imported_free_fns = {}\\n     for _f in (getattr(imported_ast, 'functions', None) or []):\\n-        if _f.name not in funcs_by_name and _f.name not in imported_free_fns:\\n-            imported_free_fns[_f.name] = _f\\n+        _key = callee_frame_key(_f)\\n+        if _key not in funcs_by_name and _key not in imported_free_fns:\\n+            imported_free_fns[_key] = _f\\n+\\n+    # SL-280: the frame keys whose BODY came from an imported module. The clone\\n+    # is appended to `program.functions` so the post-transform re-entry can read\\n+    # it, and it must leave again once its frame exists.\\n+    #\\n+    # THE INVARIANT: THE IMPORTED MODULE OWNS THE EMISSION. The splice owns only\\n+    # a copy to build a frame from, and that copy is filtered back out of\\n+    # `program.functions` below, by frame key. Renaming the clone is the\\n+    # alternative and is worse: the frame, the driver and every embed site would\\n+    # then carry a symbol no module declares.\\n+    #\\n+    # The filter was `f.name not in removed`, which cannot express this once a\\n+    # clone's key and its name differ — and a clone left behind is a body the\\n+    # frame builder has already REWRITTEN into a half-lowered state machine.\\n+    # Probed both ways out of it, and they are ONE bug wearing two faces: the\\n+    # husk either reaches the post-transform re-typecheck, where its rewritten\\n+    # body names `self` in a free function (``'self' can only be used inside\\n+    # methods``, reported inside the dependency at a line whose author wrote no\\n+    # `self`), or it survives that and reaches codegen, where it is a SECOND\\n+    # definition of a symbol the imported module also emits and llvmlite refuses\\n+    # it (`DuplicatedNameError: helper_private$m$dep` out of\\n+    # `codegen/core.py::_declare_function`, surfaced as an internal compiler\\n+    # error). Which face appears is a detail of the body — a reference\\n+    # parameter's reads are rewritten through `self`, so that shape takes the\\n+    # first — and neither is a splice-preprocessing bug, which is what they look\\n+    # like from the diagnostic.\\n+    spliced_free_fn_keys = set()\\n \\n     def _splice_imported_free_fn(name):\\n         \\\"\\\"\\\"Pull an imported free function into the entry driven closure: a\\n@@ -11452,6 +11600,7 @@ def transform_program(program, typechecker, imported_ast=None):\\n                                                       None))\\n         funcs_by_name[name] = clone\\n         program.functions.append(clone)\\n+        spliced_free_fn_keys.add(name)\\n         return clone\\n \\n     def _promote_joining_body(name, work):\\n@@ -11578,26 +11727,31 @@ def transform_program(program, typechecker, imported_ast=None):\\n                 t = nodes.get(e.target)\\n                 if t is None:\\n                     continue\\n-                is_fn_edge = (isinstance(e.target, tuple) and e.target[0] == \\\"fn\\\"\\n-                              and e.target[1] in funcs_by_name)\\n+                is_free_edge = (isinstance(e.target, tuple)\\n+                                and e.target[0] == \\\"fn\\\")\\n+                is_fn_edge = is_free_edge and e.target[1] in funcs_by_name\\n+                # DOES THIS EDGE CARRY A SUSPENSION? Two sources, asked in one\\n+                # place (SL-280). The fixpoint's own answer, and — design 96 —\\n+                # a free callee it left `suspends=False` that STRUCTURALLY\\n+                # suspends through a buried std method call or a channel\\n+                # receive. The structural question used to be asked only of a\\n+                # body already in `funcs_by_name`, which an imported one is not\\n+                # until the splice below puts it there: the exact shape of the\\n+                # SL-280 wedge (`read_chunk` -> `stream.read()`) was refused\\n+                # entry by the very gate that exists to catch it.\\n+                if not (t.suspends\\n+                        or (is_free_edge\\n+                            and _structurally_suspends(e.target[1]))):\\n+                    continue\\n                 # SL-208 / DF-300e: a free-fn edge to a CROSS-MODULE callee (in\\n                 # `imported_free_fns`, not yet in `funcs_by_name`). Splice its\\n                 # body into the entry closure and follow the edge — otherwise a\\n                 # cross-module suspending free callee is missed and lowers as a\\n                 # plain call whose park wedges the reactor.\\n-                if (not is_fn_edge and t.suspends\\n-                        and isinstance(e.target, tuple) and e.target[0] == \\\"fn\\\"\\n+                if (not is_fn_edge and is_free_edge\\n                         and e.target[1] in imported_free_fns):\\n                     if _splice_imported_free_fn(e.target[1]) is not None:\\n                         is_fn_edge = True\\n-                # design 96: follow a free-fn edge whose target the fixpoint left\\n-                # `suspends=False` but which STRUCTURALLY suspends via a buried\\n-                # method call (see `structurally_susp_fns` above) — otherwise a\\n-                # depth-2+ nested suspending method call is missed and its park\\n-                # wedges the thread.\\n-                if not t.suspends and not (\\n-                        is_fn_edge and e.target[1] in structurally_susp_fns):\\n-                    continue\\n                 if is_fn_edge:\\n                     work.append((\\\"fn\\\", e.target[1]))\\n                 elif isinstance(e.target, int) and e.target in methods_by_id:\\n@@ -11689,8 +11843,9 @@ def transform_program(program, typechecker, imported_ast=None):\\n                          if getattr(m, 'body', None) is not None]\\n         for _b in _role_bodies:\\n             for _fc in _iter_function_calls(_b):\\n-                if _fc.name in spawn_roots:\\n-                    dual_role_spawn_roots.add(_fc.name)\\n+                _fck = callee_frame_key(_fc)   # SL-280: `spawn_roots` is keyed\\n+                if _fck in spawn_roots:        # by frame key, so ask for one\\n+                    dual_role_spawn_roots.add(_fck)\\n         dual_role_spawn_roots.update(n for n in spawn_roots if n in roots)\\n     # design 221 unit B3: a suspending `main` that ALSO spawns rides the ambient\\n     # executor, which erases its frame into a `Box<any Resumable>` — so a\\n@@ -11985,12 +12140,22 @@ def transform_program(program, typechecker, imported_ast=None):\\n     # DF-218e: consumption symmetry — a generic TEMPLATE naming a consumed\\n     # callee is consumed with it.\\n     _consume_templates_naming_removed(\\n-        program, removed, {f.name for f in new_functions},\\n+        program, removed, {callee_frame_key(f) for f in new_functions},\\n         _required_by_conformance,\\n         extra_decls=list(new_functions) + list(new_extensions))\\n \\n     # Splice: remove driven roots, add synthesized declarations.\\n-    program.functions = [f for f in program.functions if f.name not in removed]\\n+    #\\n+    # SL-280: filtered by FRAME KEY, which is what `removed` holds. Two things\\n+    # rest on it. A driven/embedded body whose registration stamped a symbol\\n+    # (`helper$m$dep`) is now actually removed — under the old `f.name` test it\\n+    # survived as a half-lowered husk beside the frame that replaced it. And a\\n+    # SPLICED imported body leaves unconditionally: the imported module emits\\n+    # that symbol itself, so a clone left behind would be a second definition of\\n+    # it and llvmlite refuses the second (see `spliced_free_fn_keys`).\\n+    _drop = removed | spliced_free_fn_keys\\n+    program.functions = [f for f in program.functions\\n+                         if callee_frame_key(f) not in _drop]\\n     program.functions.extend(new_functions)\\n     program.structs.extend(new_structs)\\n     program.enums.extend(new_enums)\\ndiff --git a/sawc/frame_keys.py b/sawc/frame_keys.py\\nnew file mode 100644\\nindex 00000000..b4021854\\n--- /dev/null\\n+++ b/sawc/frame_keys.py\\n@@ -0,0 +1,102 @@\\n+\\\"\\\"\\\"THE key that names a free function's coroutine frame (SL-280).\\n+\\n+One question — *what key names this callee's frame?* — asked by the effect\\n+graph, by the coroutine transform's body tables, by the closure walk, and by\\n+every call-site classifier. Before SL-280 it was answered independently in six\\n+places and they disagreed: three read the AST's plain `name` while three read\\n+`mangled_symbol or name`, and design 249 gives a module-PRIVATE free function a\\n+`$m$<module>` tag that a `public` one does not carry. So the graph's edge key\\n+`read_chunk$m$src_http` never matched the table key `read_chunk`, the closure\\n+walk declined the edge with no diagnostic, no frame was built for the callee or\\n+for anything below it, and codegen's out-of-frame park fallback blocked the\\n+cooperative executor's own thread — the whole process wedged on one idle socket.\\n+\\n+The answer here is INDEPENDENT of whether a free function happens to carry a\\n+tag. A declaration, a call site and a resolved symbol all name the same frame,\\n+so they all produce the same string, and a program where the tag is present\\n+behaves exactly as one where it is absent. That independence is the point:\\n+SL-274 will give EVERY free function a module-mangled symbol, and today's\\n+agreement between the two answers for public functions is a coincidence that\\n+change would end.\\n+\\n+ENTRY POINTS (obligation 1 — a funnel names its entries):\\n+\\n+  * `typechecker/effects.py::_effect_enter_function` — the effect graph's NODE\\n+    key for a free-function definition (`(\\\"fn\\\", <key>)`).\\n+  * `typechecker/effects.py::_effect_call_function` — the effect graph's EDGE\\n+    key for a resolved free call, bare or module-qualified.\\n+  * `coro_transform.py::transform_program` — `funcs_by_name`, the entry\\n+    module's body table, and `imported_free_fns`, the importable-body table the\\n+    SL-208 splice draws from (including the shadow-out test between them).\\n+  * `coro_transform.py::_splice_imported_free_fn` — the key a spliced clone is\\n+    registered under, and the key `removed`/`program.functions` filter it back\\n+    out by once its frame exists.\\n+  * `coro_transform.py::_FrameBuilder.name` — the frame's own identity, hence\\n+    `__Frame_<key>`, `__saw_drive_<key>` and `__spawn_<key>`.\\n+  * `coro_transform.py::_FrameBuilder._classify_call` — the call-site\\n+    classifier's `self._suspends` membership test AND the `callee` it emits,\\n+    which `_callee_fb` then resolves against `fbs`.\\n+  * `coro_transform.py::_FrameBuilder._classify_method_call` and\\n+    `_module_free_call_suspends` — the same two questions for SL-208's\\n+    module-qualified free call, which wears a `MethodCall`'s shape.\\n+  * `coro_transform.py::_promote_nested_generic_methods` — the free-fn descent\\n+    of the method-instantiation walk (`funcs_by_name` again).\\n+  * `coro_transform.py::_FrameBuilder._is_suspending_expr` — the ANF /\\n+    expression-position hoist's suspension test.\\n+  * `coro_transform.py::_FrameBuilder._spans_suspension` — the CFG-split test.\\n+  * `coro_transform.py::_FrameBuilder._reject_buried_suspend_call` — the\\n+    inexpressible-position refusal.\\n+  * `coro_transform.py::_default_expr_suspends` — a defaulted argument's\\n+    suspension test.\\n+  * `coro_transform.py::_rewrite_drive_sites` — the driver a `__saw_drive`\\n+    site is rewritten to.\\n+  * `coro_transform.py::_called_function_names` and\\n+    `_consume_templates_naming_removed` — the consumption sweep, which\\n+    intersects \\\"what the survivors call\\\" with `removed`.\\n+  * `typechecker/expressions.py` — the driven-root recording\\n+    (`_effect_record_driven`) and the spawn-root recording\\n+    (`_check_spawned_call_argument`), so `roots`/`spawn_roots` live in the same\\n+    key space as the tables the transform looks them up in.\\n+\\n+NOT an entry point, deliberately: a METHOD frame, whose key is\\n+`coro_transform.py::_method_frame_key` (`{struct}_{method}` or the resolved\\n+overload symbol). A method is keyed by its owner and its node id, never by a\\n+free function's name, and the two key spaces must not be able to collide.\\n+\\\"\\\"\\\"\\n+\\n+\\n+def callee_frame_key(node=None, name=None):\\n+    \\\"\\\"\\\"The frame key for the free function `node` denotes, or None.\\n+\\n+    `node` may be any of the shapes that name a free function:\\n+\\n+      * a `Function` declaration (or a spliced clone of one) — `mangled_symbol`\\n+        when registration stamped one, else the declared `name`;\\n+      * a `FunctionCall` call site — `resolved_symbol` when call resolution\\n+        stamped one, else the written `name`;\\n+      * a `MethodCall` that is really a module-qualified FREE call\\n+        (`mod.f(...)`, SL-208) — its `module_free_call`, which the typechecker\\n+        already composed as `resolved_symbol or method_name`. A MethodCall that\\n+        is a genuine METHOD call names no free-function frame and answers None:\\n+        method frames are `_method_frame_key`'s, and the two spaces are\\n+        disjoint on purpose.\\n+      * a `FunctionSymbol` from the namespace — `mangled_name`, with the\\n+        looked-up `name` passed alongside (a symbol carries no name of its own).\\n+\\n+    `name` is the fallback spelling for a node that has none, and is ignored\\n+    whenever the node carries a symbol.\\n+    \\\"\\\"\\\"\\n+    if node is None:\\n+        return name\\n+    # A module-qualified free call wears a MethodCall's shape. `module_free_call`\\n+    # is present exactly when the typechecker resolved it to a free function, so\\n+    # its absence on a MethodCall means \\\"this is a method\\\" — answer None rather\\n+    # than composing a free-function key out of a method name.\\n+    if hasattr(node, 'method_name'):\\n+        return getattr(node, 'module_free_call', None)\\n+    symbol = (getattr(node, 'mangled_symbol', None)\\n+              or getattr(node, 'resolved_symbol', None)\\n+              or getattr(node, 'mangled_name', None))\\n+    if symbol:\\n+        return symbol\\n+    return getattr(node, 'name', None) or name\\ndiff --git a/sawc/typechecker/effects.py b/sawc/typechecker/effects.py\\nindex 9b3733c3..a6a3ddf5 100644\\n--- a/sawc/typechecker/effects.py\\n+++ b/sawc/typechecker/effects.py\\n@@ -428,7 +428,13 @@ class EffectsMixin:\\n         # distinct stamped codegen symbol; key its suspend node on that so each\\n         # overload has its OWN effect node (a sync and a non-sync overload of the\\n         # same name must not merge into one node).\\n-        key = (\\\"fn\\\", getattr(func, 'mangled_symbol', None) or func.name)\\n+        #\\n+        # SL-280: through `callee_frame_key`, the ONE answer to \\\"what key names\\n+        # this callee's frame?\\\". The coroutine transform's tables and call-site\\n+        # classifiers ask it too, so a node here and an edge there and a body\\n+        # over in `funcs_by_name` are all filed under one string.\\n+        from frame_keys import callee_frame_key\\n+        key = (\\\"fn\\\", callee_frame_key(func))\\n         node = self._suspend_nodes.get(key)\\n         if node is None:\\n             from ast_nodes import is_exported\\n@@ -582,7 +588,11 @@ class EffectsMixin:\\n             # leaf, which is exactly the \\\"extern promises promptness\\\" rule.\\n             # Overloading (design 55): edge to the RESOLVED overload's node,\\n             # which is keyed by its stamped symbol (matches _effect_enter_function).\\n-            key_name = getattr(func_info, \\\"mangled_name\\\", \\\"\\\") or name\\n+            # SL-280: both ends go through `callee_frame_key`, so the edge and\\n+            # the node cannot drift apart the way the edge and the transform's\\n+            # `imported_free_fns` table did.\\n+            from frame_keys import callee_frame_key\\n+            key_name = callee_frame_key(func_info, name=name)\\n             self._effect_add_edge((\\\"fn\\\", key_name), f\\\"`{name}`\\\", line)\\n \\n     def _effect_call_method(self, method_info, short: str, line: int):\\ndiff --git a/sawc/typechecker/expressions.py b/sawc/typechecker/expressions.py\\nindex d4857036..038ace35 100644\\n--- a/sawc/typechecker/expressions.py\\n+++ b/sawc/typechecker/expressions.py\\n@@ -4761,9 +4761,19 @@ class ExpressionsMixin:\\n                         mangled = self._effect_queue_fn_mono(tmpl, resolved_args)\\n                         self._effect_record_driven(mangled, mode)\\n                         inner.name = mangled\\n+                        # SL-280: the call now NAMES the instantiation, so the\\n+                        # template's symbol is stale data. Clearing it keeps\\n+                        # `callee_frame_key(inner)` — which the drive-site\\n+                        # rewrite and the transform's root lookup both ask —\\n+                        # equal to the root recorded one line up.\\n+                        inner.resolved_symbol = None\\n                         inner.type_args = None\\n                 else:\\n-                    self._effect_record_driven(inner.name, mode)\\n+                    # SL-280: the driven root is recorded under the frame key,\\n+                    # not the written name, so `funcs_by_name` (keyed the same\\n+                    # way) finds the body a `$m$`/`$M$`/`$OL$` tag renamed.\\n+                    from frame_keys import callee_frame_key\\n+                    self._effect_record_driven(callee_frame_key(inner), mode)\\n             if expr.name == \\\"__saw_drive_steps\\\":\\n                 return SawType(TypeKind.INT)\\n             return inner_type if inner_type is not None else SawType(TypeKind.VOID)\\n@@ -10041,7 +10051,9 @@ class ExpressionsMixin:\\n                 \\\"the task never completes and `join` on its handle could never \\\"\\n                 \\\"return\\\", result_type, expr.line, expr.column):\\n             return None\\n-        spawn_name = getattr(inner, 'resolved_symbol', None) or inner.name\\n+        # SL-280: the spawn root's frame key, through the one funnel.\\n+        from frame_keys import callee_frame_key\\n+        spawn_name = callee_frame_key(inner)\\n         if getattr(inner, 'type_args', None):\\n             resolved_args = [self._resolve_type(a) for a in inner.type_args]\\n             if not all(self._is_concrete_type(a) for a in resolved_args):\\n@@ -10052,6 +10064,9 @@ class ExpressionsMixin:\\n                 return None\\n             spawn_name = self._effect_queue_fn_mono(spawn_name, resolved_args)\\n             inner.name = spawn_name\\n+            # SL-280: see the drive-site twin — the call names the\\n+            # instantiation now, so the template's symbol must not outlive it.\\n+            inner.resolved_symbol = None\\n             inner.type_args = None\\n         return (spawn_name, result_type, inner)\\n \\n\",\"id\":\"SL-280.p2\",\"request_id\":\"pid-20130-1789676228-6000\"}","request_id":"pid-20130-1789676228-6000","sequence":917,"timestamp":"1789676229"}
---
