---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791253","description":"## Scope and status\n\nDF-148a — FILED (design 148 unit B). A repeat literal cannot repeat a GENERIC element, because no bound expresses \"copies are free\". [t; N] where t: T is…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/148-const-generics-repeat-literals.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/148-const-generics-repeat-literals.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 8600–8612\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L8600-L8612)\n\n- **DF‑148a — FILED (design 148 unit B). A repeat literal cannot repeat a\n  GENERIC element, because no bound expresses \"copies are free\".** `[t; N]`\n  where `t: T` is refused: `T: Copy` admits ExplicitCopy (which needs a\n  `.copy()` per slot, and a repeat has nowhere to write one), while\n  `T: ImplicitCopy` excludes the POD types that are freer still — so `Int` fails\n  an `ImplicitCopy` bound and the natural `Ring<T, const N: Int>` is unwritable.\n  The element type is concrete in v1 and the error says so. Two ways out worth\n  deciding between: a bound that means trivial-or-ImplicitCopy, or letting\n  `T: Copy` through and emitting a per-slot `copy()` in codegen (which is what\n  the splat loop already does for the retain case). Not urgent — the acceptance\n  shape `FixedBuf<const N: Int>` has a concrete `UInt8` element — but it is the\n  first thing anyone writing a generic container will hit.\n","id":"SL-133","labels":["todo-import","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":133,"status":"open","title":"DF-148a — FILED (design 148 unit B). A repeat literal cannot repeat a GENERIC element, because no bound expresses \"copies are free\". [t; N] where t: T is…","updated":"1788791253"},"before":null,"id":"SL-133","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-148a — FILED (design 148 unit B). A repeat literal cannot repeat a GENERIC element, because no bound expresses \\\"copies are free\\\". [t; N] where t: T is…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/148-const-generics-repeat-literals.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/148-const-generics-repeat-literals.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 8600–8612\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L8600-L8612)\\n\\n- **DF‑148a — FILED (design 148 unit B). A repeat literal cannot repeat a\\n  GENERIC element, because no bound expresses \\\"copies are free\\\".** `[t; N]`\\n  where `t: T` is refused: `T: Copy` admits ExplicitCopy (which needs a\\n  `.copy()` per slot, and a repeat has nowhere to write one), while\\n  `T: ImplicitCopy` excludes the POD types that are freer still — so `Int` fails\\n  an `ImplicitCopy` bound and the natural `Ring<T, const N: Int>` is unwritable.\\n  The element type is concrete in v1 and the error says so. Two ways out worth\\n  deciding between: a bound that means trivial-or-ImplicitCopy, or letting\\n  `T: Copy` through and emitting a per-slot `copy()` in codegen (which is what\\n  the splat loop already does for the retain case). Not urgent — the acceptance\\n  shape `FixedBuf<const N: Int>` has a concrete `UInt8` element — but it is the\\n  first thing anyone writing a generic container will hit.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-ad163f019f47a9d224ee-create\",\"title\":\"DF-148a — FILED (design 148 unit B). A repeat literal cannot repeat a GENERIC element, because no bound expresses \\\"copies are free\\\". [t; N] where t: T is…\"}","request_id":"todo-20260907-ad163f019f47a9d224ee-create","sequence":133,"timestamp":"1788791253"}
---
