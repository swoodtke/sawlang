---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-248.p1 (revision 1): minivm M5: implicit tails + value-producing control flow (branch commit 894682e2)","created":"1789089636","id":"c1"}],"created":"1789086343","description":"SL-242 epic child, landing patch 6/14. Value control flow joined via lookahead classification (statement-vs-tail decided before parse). Contract: prototypes/minivm/M5_VALUE_CONTROL.md. Patch = branch commit 894682e2.","id":"SL-248","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":450,"stage":"queued","status":"open","title":"minivm M5: implicit tails + value-producing if/match","updated":"1789089636"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086343","description":"SL-242 epic child, landing patch 6/14. Value control flow joined via lookahead classification (statement-vs-tail decided before parse). Contract: prototypes/minivm/M5_VALUE_CONTROL.md. Patch = branch commit 894682e2.","id":"SL-248","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":410,"stage":"queued","status":"open","title":"minivm M5: implicit tails + value-producing if/match","updated":"1789086343"},"id":"SL-248","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/DESIGN.md b/prototypes/minivm/DESIGN.md\\nindex 4202df14..2e36c68e 100644\\n--- a/prototypes/minivm/DESIGN.md\\n+++ b/prototypes/minivm/DESIGN.md\\n@@ -21,7 +21,8 @@ literal type, and signed-minimum remainder behavior.\\n [M2_RECORDS.md](M2_RECORDS.md) adds value records and flattened call/result layouts.\\n [M3_SCALAR_CONTROL.md](M3_SCALAR_CONTROL.md) adds short-circuit operators, compound\\n assignments and module constants. [M4_ENUMS.md](M4_ENUMS.md) adds nominal\\n-payload-free enums and exhaustive statement match.\\n+payload-free enums and exhaustive statement match. [M5_VALUE_CONTROL.md](M5_VALUE_CONTROL.md)\\n+adds implicit tails and value-producing branches.\\n \\n ## Source contract\\n \\ndiff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 6697ea87..8354c6e6 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -8,7 +8,8 @@ calls. This inventory was taken at the 43-case Int32/Bool baseline. M1 now suppl\\n the numeric foundation; M2 supplies scalar value records, and M3 adds module\\n constants, compound assignment and short-circuit Booleans. M4 supplies payload-free\\n enums and exhaustive statement match, including the actual TokenKind declaration\\n-in isolation (161 total cases).\\n+in isolation. M5 adds implicit tails, value if/match and else-if chains, including\\n+the unchanged numeric `hex_value` helper (193 total cases).\\n Strings, collections, references, generics and methods remain outstanding.\\n The dependency groups below are an inventory, not the implementation commit order;\\n the numbered milestone designs define each isolated slice.\\ndiff --git a/prototypes/minivm/M5_VALUE_CONTROL.md b/prototypes/minivm/M5_VALUE_CONTROL.md\\nnew file mode 100644\\nindex 00000000..630f1533\\n--- /dev/null\\n+++ b/prototypes/minivm/M5_VALUE_CONTROL.md\\n@@ -0,0 +1,109 @@\\n+# M5: tails and value-producing control flow\\n+\\n+Status: implemented after M4 (1259292b). All 193 integration cases pass through\\n+the VM and clang O0/O2, including 32 isolated value-control cases. All three\\n+direct numeric/record/enum representation contracts pass.\\n+\\n+## Scope and source contract\\n+\\n+Add implicit function tail returns, value-producing `if` with mandatory `else`,\\n+`else if` chains, and exhaustive enum `match` with either expression arms or\\n+block arms containing a final expression. Support numeric, Bool, enum, and\\n+flattened record results. Retain existing statement control flow and explicit\\n+returns. No new runtime representation, opcode, or engine behavior is needed.\\n+Strings, payload patterns, references, loop values, and general Never are deferred.\\n+\\n+A value block may contain declarations, mutation, loops, and explicit returns\\n+before its final expression. Only the last expression supplies its value. A\\n+return exits the enclosing function, never merely the arm. A branch that always\\n+returns needs no arm value; every continuing branch must supply a compatible\\n+value. All-returning control flow is permitted in function tails and statements;\\n+if handling it in a nested operand requires general bottom typing, reject that\\n+position cleanly for this slice rather than manufacture a reachable value.\\n+\\n+Use the existing transfer rules: nominal record/enum identity, exact Bool,\\n+lossless numeric widening, and bare literal adoption. Carry expected types into\\n+branch tails from local annotations, assignment destinations, function parameters,\\n+record fields, and explicit/implicit returns. Without an expected type, infer a\\n+common type from continuing arms, preserving contextual adoption of bare literals\\n+against a typed sibling; incompatible nominal types are errors. For typed integer\\n+arms, choose an existing arm type to which all arms widen losslessly (production\\n+design-195/205 rule), not a new larger type absent from the arms. Do not introduce\\n+general constant folding. Range-check even unselected branches. Document any\\n+deliberate narrower inference rule with located rejection tests.\\n+\\n+The no-context bare-literal sibling adoption above is an explicit prototype\\n+choice: the production compiler currently defaults the literal to Int in the\\n+probed `if flag { 5 } else { 6u8 }` initializer. Production comparisons therefore\\n+validate the shared subset, while independent oracles define this choice. Likewise,\\n+production acceptance is not sufficient evidence that missing-tail source is valid:\\n+the prototype must reject it according to the continuing-path rule above.\\n+The independently confirmed production missing-tail finding is tracked as SL-235.\\n+\\n+Discarded statement control flow must not become an implicit function return.\\n+Keep the prototype's restriction on ordinary non-Void expression statements;\\n+only a final expression in a value context can supply a value. Tail position is\\n+relative to the containing block and its consumer, not merely a closing brace.\\n+Continue to reject missing returns and reachable value branches missing a tail.\\n+\\n+## Frontend design\\n+\\n+Replace the conflation of statement termination and block value with an explicit\\n+frontend-only result carrying continuing/value/always-return information. Reuse\\n+one control-flow lowering path for statement and value forms wherever practical.\\n+The block parser must distinguish statements from a final expression, track scope\\n+and statement separators, and propagate the enclosing function return type.\\n+An expected type is contextual, not global state that leaks into conditions,\\n+call arguments, binary operands, or nested unrelated expressions.\\n+\\n+Lower each value branch to existing Branch/Jump and fresh result slots. Copy a\\n+continuing arm's entire flattened value into the common destination before its\\n+join jump; a returning arm has no copy or continuing edge. If type inference needs\\n+all arm types before result allocation, record arm exit placeholders and append\\n+small conversion/copy blocks after parsing arms, then patch their targets. Never\\n+insert instructions into the middle of the vector without remapping all targets.\\n+No default initialized value may disguise a missing source value.\\n+\\n+Evaluate conditions, match subjects, operands, and call arguments in source order.\\n+Evaluate a match subject exactly once. Only the selected arm executes, including\\n+casts and overflow checks. Keep exhaustive coverage and duplicate-pattern checks.\\n+Restore parenthesis whitespace handling at block boundaries so enclosing call\\n+parentheses do not swallow statement newlines inside an arm. Bound recursive\\n+else-if and value-block nesting using the existing parser limits.\\n+\\n+## Delegation and acceptance\\n+\\n+Primary owns design, independent review, adversarial checks, documentation and\\n+integration. Sol frontend owns src/frontend.saw only. Sol fixtures owns\\n+test_minivm.py and examples/values only. An independent Sol reviewer audits\\n+production syntax/semantics and the implementation without editing shared files.\\n+\\n+Fixtures need explicit stdout/status or located diagnostics. Cover scalar/Bool/\\n+enum/record tails, nested if/match, else-if, both sides selected, side-effect order,\\n+unselected runtime traps, subject-once, mixed explicit returns and values, all\\n+arms returning, early return before an unreachable tail, narrow and full UInt64\\n+literal contexts, argument/field/assignment contexts, and scope boundaries.\\n+Reject missing else/tail, mismatched types, out-of-range literals in unselected\\n+arms, incomplete matches and non-tail value expressions. Keep all earlier cases.\\n+Primary runs the full prototype VM + clang O0/O2 gate, direct representation\\n+contracts, and representative production compiler comparisons under the existing\\n+suite coordination policy, then commits only reviewed passing work.\\n+\\n+The `lexer_hex_value` fixture includes the lexer's unchanged numeric helper and\\n+its constants. Independent production-compiler comparisons passed for that helper,\\n+the nested record/UInt64 evaluation grid, contextual conditions, and parenthesized\\n+branch composition. Statement-position else-if and unreachable statements remain\\n+statements; they do not silently become value tails.\\n+\\n+## Bootstrap observations\\n+\\n+During integration, inspecting the String field of an unnamed cloned Token in\\n+the statement classifiers produced SIGBUS/SIGSEGV in bootstrap-generated String\\n+cleanup. Both the simple implicit-tail case and the rejected non-tail-expression\\n+case reproduced the failures. Keeping the cloned Token in a named local resolved\\n+both; those source-level cases are covered by the integration gate. The failure\\n+was then reduced to a standalone Token/Reader program and reproduced on current\\n+main with a passing named-local control. It is filed as SL-236; its exact\\n+production cleanup mechanism has not been fixed in sawc.\\n+The prototype uses explicit token locals for these classifiers; this milestone\\n+does not claim to repair production compiler ownership behavior.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex 58b20655..fb9f10bd 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -31,9 +31,9 @@ not a dependency of the interpreter or text emitter.\\n ```saw\\n func factorial(n: Int32) -> Int32 {\\n     if n <= 1 {\\n-        return 1\\n+        1\\n     } else {\\n-        return n * factorial(n - 1)\\n+        n * factorial(n - 1)\\n     }\\n }\\n \\n@@ -44,13 +44,15 @@ func main() {\\n \\n Single-file programs support signed and unsigned 8/16/32/64-bit integers,\\n `Int`/`UInt` (64-bit), `Byte`, `Bool`, functions, positional calls,\\n-recursion, `let`/`var`, assignment, lexical scopes, statement `if`/`else` and\\n-`while`, explicit returns, and numeric/Bool printing. Named structs can contain scalars\\n+recursion, `let`/`var`, assignment, lexical scopes, `if`/`else if`/`else` and\\n+`while`, explicit and implicit tail returns, and numeric/Bool printing. Named structs can contain scalars\\n or nested structs, with\\n field access, mutable fields, value copying, and function parameters/results.\\n Payload-free enums support copying, equality, record fields and function\\n-parameters/results. Exhaustive statement `match` supports qualified or unqualified\\n-cases and a final wildcard; see [M4_ENUMS.md](M4_ENUMS.md).\\n+parameters/results. Exhaustive enum `match` supports qualified or unqualified\\n+cases and a final wildcard; see [M4_ENUMS.md](M4_ENUMS.md). Value-producing `if`\\n+and `match` join numeric, Bool, enum, or record values, with explicit returns\\n+allowed inside arms; see [M5_VALUE_CONTROL.md](M5_VALUE_CONTROL.md).\\n Record layouts are limited to 256 words and 128 nesting levels; see\\n [M2_RECORDS.md](M2_RECORDS.md). Arithmetic is checked; signed\\n division truncates toward zero. Overflow and division by zero print an error\\n@@ -63,8 +65,9 @@ shift counts. See [M1_NUMBERS.md](M1_NUMBERS.md) for the precise numeric contrac\\n \\n Newlines separate statements. Omit the result annotation for Void functions;\\n `main()` must take no arguments and return Void. Value-returning functions need\\n-explicit returns. There are no imports, strings, collections, references,\\n-generics, methods, implicit value tails, or concurrency in the accepted subset.\\n+a value on every continuing path, supplied by an explicit return or final expression.\\n+There are no imports, strings, collections, references,\\n+generics, methods, or concurrency in the accepted subset.\\n Semicolons remain unsupported. Boolean `&&`/`||` short-circuit, mutable integers\\n and fields support `+=`/`-=`/`*=`/`/=`/`%=`, and module `static` integer/Bool\\n constants accept literal and numeric-limit initializers. See\\n@@ -83,20 +86,21 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The first four milestones\\n-pass all 161 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The first five milestones\\n+pass all 193 cases, including native execution at both `-O0` and `-O2`.\\n \\n Independent representation checks live in `tests/numeric_contract.saw` and\\n `tests/record_contract.saw`, and `tests/enum_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n-`--section enums` for an isolated\\n+`--section enums`, or `--section values` for an isolated\\n integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\n schema and semantics. [LEXER_DEPENDENCIES.md](LEXER_DEPENDENCIES.md) tracks the\\n remaining dependencies needed to compile the existing lexer with this prototype.\\n-The enum fixtures include the lexer's actual `TokenKind` declaration. Source is\\n+The fixtures include the lexer's actual `TokenKind` declaration and `hex_value`\\n+helper. Source is\\n split into `frontend.saw`, `model.saw`, `verify.saw`, `vm.saw`, `llvm.saw`, and\\n `main.saw`. The verifier checks structural indices and types; initialization\\n is established by the frontend, so this is not a loader for untrusted bytecode.\\ndiff --git a/prototypes/minivm/examples/values/contexts.saw b/prototypes/minivm/examples/values/contexts.saw\\nnew file mode 100644\\nindex 00000000..0f89060e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/contexts.saw\\n@@ -0,0 +1,17 @@\\n+struct ValueBox { value: UInt16 }\\n+\\n+func take(value: Int8) { print(value) }\\n+func take_u8(value: UInt8) { print(value) }\\n+\\n+func main() {\\n+    let narrow: Int8 = if true { -128 } else { 127 }\\n+    var assigned: UInt32 = 0\\n+    assigned = if false { 1 } else { UInt32.max }\\n+    take(if false { 1 } else { -7 })\\n+    let inferred = if true { 5 } else { 6u8 }\\n+    take_u8(inferred)\\n+    let box = ValueBox(value: if true { 65535 } else { 0 })\\n+    print(narrow)\\n+    print(assigned)\\n+    print(box.value)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/else_if.saw b/prototypes/minivm/examples/values/else_if.saw\\nnew file mode 100644\\nindex 00000000..fe19ab14\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/else_if.saw\\n@@ -0,0 +1,13 @@\\n+func classify(value: Int) -> Int {\\n+    if value < 0 { -1 }\\n+    else if value == 0 { 0 }\\n+    else if value < 10 { 1 }\\n+    else { 2 }\\n+}\\n+\\n+func main() {\\n+    print(classify(-4))\\n+    print(classify(0))\\n+    print(classify(7))\\n+    print(classify(20))\\n+}\\ndiff --git a/prototypes/minivm/examples/values/evaluation_order.saw b/prototypes/minivm/examples/values/evaluation_order.saw\\nnew file mode 100644\\nindex 00000000..a8581687\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/evaluation_order.saw\\n@@ -0,0 +1,14 @@\\n+func condition(label: Int, value: Bool) -> Bool {\\n+    print(label)\\n+    return value\\n+}\\n+\\n+func arm(label: Int, value: Int) -> Int {\\n+    print(label)\\n+    return value\\n+}\\n+\\n+func main() {\\n+    print(if condition(1, true) { arm(2, 20) } else { arm(3, 30) })\\n+    print(if condition(4, false) { 1 / 0 } else { arm(5, 50) })\\n+}\\ndiff --git a/prototypes/minivm/examples/values/if_nested.saw b/prototypes/minivm/examples/values/if_nested.saw\\nnew file mode 100644\\nindex 00000000..a249aa48\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/if_nested.saw\\n@@ -0,0 +1,14 @@\\n+func choose(first: Bool, second: Bool) -> Int {\\n+    if first {\\n+        if second { 11 } else { 12 }\\n+    } else {\\n+        if second { 21 } else { 22 }\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(choose(true, true))\\n+    print(choose(true, false))\\n+    print(choose(false, true))\\n+    print(choose(false, false))\\n+}\\ndiff --git a/prototypes/minivm/examples/values/inference_order_widening.saw b/prototypes/minivm/examples/values/inference_order_widening.saw\\nnew file mode 100644\\nindex 00000000..bbd35470\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/inference_order_widening.saw\\n@@ -0,0 +1,33 @@\\n+func take_u8(value: UInt8) { print(value) }\\n+func take_i16(value: Int16) { print(value) }\\n+\\n+enum Three { case Negative, case Small, case Wide }\\n+\\n+func inferred_match(value: Three) {\\n+    let result = match value {\\n+        case Negative -> -1i8,\\n+        case Small -> 1u8,\\n+        case Wide -> 2i16\\n+    }\\n+    take_i16(result)\\n+}\\n+\\n+func marker() { print(8) }\\n+\\n+func tail_after_void() -> Int {\\n+    marker()\\n+    42\\n+}\\n+\\n+func main() {\\n+    let reverse = if true { 6u8 } else { 5 }\\n+    take_u8(reverse)\\n+    let signed: Int16 = -3\\n+    let unsigned: UInt8 = 9\\n+    let widened = if true { signed } else { unsigned }\\n+    print(widened)\\n+    inferred_match(Three.Negative)\\n+    inferred_match(Three.Small)\\n+    inferred_match(Three.Wide)\\n+    print(tail_after_void())\\n+}\\ndiff --git a/prototypes/minivm/examples/values/lexer_hex_value.saw b/prototypes/minivm/examples/values/lexer_hex_value.saw\\nnew file mode 100644\\nindex 00000000..d218fcad\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/lexer_hex_value.saw\\n@@ -0,0 +1,24 @@\\n+// Constants and helper copied from selfhost/lexer/src/lib.saw.\\n+static B_D0: Int = 48\\n+static B_D9: Int = 57\\n+static B_LO_A: Int = 97\\n+static B_LO_F: Int = 102\\n+static B_UP_A: Int = 65\\n+static B_UP_F: Int = 70\\n+\\n+func is_digit(b: Int) -> Bool { b >= B_D0 && b <= B_D9 }\\n+\\n+func hex_value(b: Int) -> Int {\\n+    if is_digit(b) { b - B_D0 }\\n+    else if b >= B_LO_A && b <= B_LO_F { b - B_LO_A + 10 }\\n+    else { b - B_UP_A + 10 }\\n+}\\n+\\n+func main() {\\n+    print(hex_value(48))\\n+    print(hex_value(57))\\n+    print(hex_value(97))\\n+    print(hex_value(102))\\n+    print(hex_value(65))\\n+    print(hex_value(70))\\n+}\\ndiff --git a/prototypes/minivm/examples/values/match_arms.saw b/prototypes/minivm/examples/values/match_arms.saw\\nnew file mode 100644\\nindex 00000000..586737f6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/match_arms.saw\\n@@ -0,0 +1,34 @@\\n+enum Color { case Red, case Green, case Blue }\\n+\\n+func direct(value: Color) -> Int {\\n+    match value {\\n+        case Red -> 1,\\n+        case Green -> 2,\\n+        case Blue -> 3\\n+    }\\n+}\\n+\\n+func blocked(value: Color) -> Int {\\n+    match value {\\n+        case Red -> {\\n+            let n = 10\\n+            n + 1\\n+        },\\n+        case Green -> {\\n+            let n = 20\\n+            n + 2\\n+        },\\n+        case Blue -> {\\n+            let n = 30\\n+            n + 3\\n+        }\\n+    }\\n+}\\n+\\n+func main() {\\n+    print(direct(Color.Red))\\n+    print(direct(Color.Blue))\\n+    print(blocked(Color.Red))\\n+    print(blocked(Color.Green))\\n+    print(blocked(Color.Blue))\\n+}\\ndiff --git a/prototypes/minivm/examples/values/match_subject_once.saw b/prototypes/minivm/examples/values/match_subject_once.saw\\nnew file mode 100644\\nindex 00000000..2edcee73\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/match_subject_once.saw\\n@@ -0,0 +1,14 @@\\n+enum Pick { case First, case Second }\\n+\\n+func marked(value: Pick) -> Pick {\\n+    print(6)\\n+    return value\\n+}\\n+\\n+func main() {\\n+    let answer = match marked(Pick.Second) {\\n+        case First -> 10,\\n+        case Second -> 20\\n+    }\\n+    print(answer)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/parenthesized_argument_block.saw b/prototypes/minivm/examples/values/parenthesized_argument_block.saw\\nnew file mode 100644\\nindex 00000000..207611e9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/parenthesized_argument_block.saw\\n@@ -0,0 +1,17 @@\\n+func take(value: Int) { print(value) }\\n+\\n+func main() {\\n+    take(if true {\\n+        var n = 0\\n+        while n < 2 {\\n+            print(n)\\n+            n += 1\\n+        }\\n+        if n == 2 {\\n+            print(8)\\n+        }\\n+        n + 40\\n+    } else {\\n+        0\\n+    })\\n+}\\ndiff --git a/prototypes/minivm/examples/values/record_enum_values.saw b/prototypes/minivm/examples/values/record_enum_values.saw\\nnew file mode 100644\\nindex 00000000..e12a11a5\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/record_enum_values.saw\\n@@ -0,0 +1,27 @@\\n+enum Mode { case Cold, case Warm }\\n+struct Pair { left: Int, right: Mode }\\n+\\n+func pair(flag: Bool) -> Pair {\\n+    if flag {\\n+        Pair(right: Mode.Warm, left: 7)\\n+    } else {\\n+        Pair(left: 9, right: Mode.Cold)\\n+    }\\n+}\\n+\\n+func mode(flag: Bool) -> Mode {\\n+    match pair(flag).right {\\n+        case Cold -> Mode.Warm,\\n+        case Warm -> Mode.Cold\\n+    }\\n+}\\n+\\n+func main() {\\n+    let first = pair(true)\\n+    let second = pair(false)\\n+    print(first.left)\\n+    print(first.right == Mode.Warm)\\n+    print(second.left)\\n+    print(mode(true) == Mode.Cold)\\n+    print(mode(false) == Mode.Warm)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_all_returning_operand.saw b/prototypes/minivm/examples/values/reject_all_returning_operand.saw\\nnew file mode 100644\\nindex 00000000..b1ac6bfd\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_all_returning_operand.saw\\n@@ -0,0 +1,4 @@\\n+func choose(flag: Bool) -> Int {\\n+    1 + if flag { return 2 } else { return 3 }\\n+}\\n+func main() { print(choose(true)) }\\ndiff --git a/prototypes/minivm/examples/values/reject_arm_scope_leak.saw b/prototypes/minivm/examples/values/reject_arm_scope_leak.saw\\nnew file mode 100644\\nindex 00000000..a2f37f74\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_arm_scope_leak.saw\\n@@ -0,0 +1,12 @@\\n+enum Pick { case First, case Second }\\n+func main() {\\n+    let result = match Pick.First {\\n+        case First -> {\\n+            let hidden = 7\\n+            hidden\\n+        },\\n+        case Second -> 0\\n+    }\\n+    print(result)\\n+    print(hidden)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_deep_else_if.saw b/prototypes/minivm/examples/values/reject_deep_else_if.saw\\nnew file mode 100644\\nindex 00000000..a1a2ada2\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_deep_else_if.saw\\n@@ -0,0 +1,146 @@\\n+func deep(value: Int) -> Int {\\n+    if value == 0 { 0 }\\n+    else if value == 1 { 1 }\\n+    else if value == 2 { 2 }\\n+    else if value == 3 { 3 }\\n+    else if value == 4 { 4 }\\n+    else if value == 5 { 5 }\\n+    else if value == 6 { 6 }\\n+    else if value == 7 { 7 }\\n+    else if value == 8 { 8 }\\n+    else if value == 9 { 9 }\\n+    else if value == 10 { 10 }\\n+    else if value == 11 { 11 }\\n+    else if value == 12 { 12 }\\n+    else if value == 13 { 13 }\\n+    else if value == 14 { 14 }\\n+    else if value == 15 { 15 }\\n+    else if value == 16 { 16 }\\n+    else if value == 17 { 17 }\\n+    else if value == 18 { 18 }\\n+    else if value == 19 { 19 }\\n+    else if value == 20 { 20 }\\n+    else if value == 21 { 21 }\\n+    else if value == 22 { 22 }\\n+    else if value == 23 { 23 }\\n+    else if value == 24 { 24 }\\n+    else if value == 25 { 25 }\\n+    else if value == 26 { 26 }\\n+    else if value == 27 { 27 }\\n+    else if value == 28 { 28 }\\n+    else if value == 29 { 29 }\\n+    else if value == 30 { 30 }\\n+    else if value == 31 { 31 }\\n+    else if value == 32 { 32 }\\n+    else if value == 33 { 33 }\\n+    else if value == 34 { 34 }\\n+    else if value == 35 { 35 }\\n+    else if value == 36 { 36 }\\n+    else if value == 37 { 37 }\\n+    else if value == 38 { 38 }\\n+    else if value == 39 { 39 }\\n+    else if value == 40 { 40 }\\n+    else if value == 41 { 41 }\\n+    else if value == 42 { 42 }\\n+    else if value == 43 { 43 }\\n+    else if value == 44 { 44 }\\n+    else if value == 45 { 45 }\\n+    else if value == 46 { 46 }\\n+    else if value == 47 { 47 }\\n+    else if value == 48 { 48 }\\n+    else if value == 49 { 49 }\\n+    else if value == 50 { 50 }\\n+    else if value == 51 { 51 }\\n+    else if value == 52 { 52 }\\n+    else if value == 53 { 53 }\\n+    else if value == 54 { 54 }\\n+    else if value == 55 { 55 }\\n+    else if value == 56 { 56 }\\n+    else if value == 57 { 57 }\\n+    else if value == 58 { 58 }\\n+    else if value == 59 { 59 }\\n+    else if value == 60 { 60 }\\n+    else if value == 61 { 61 }\\n+    else if value == 62 { 62 }\\n+    else if value == 63 { 63 }\\n+    else if value == 64 { 64 }\\n+    else if value == 65 { 65 }\\n+    else if value == 66 { 66 }\\n+    else if value == 67 { 67 }\\n+    else if value == 68 { 68 }\\n+    else if value == 69 { 69 }\\n+    else if value == 70 { 70 }\\n+    else if value == 71 { 71 }\\n+    else if value == 72 { 72 }\\n+    else if value == 73 { 73 }\\n+    else if value == 74 { 74 }\\n+    else if value == 75 { 75 }\\n+    else if value == 76 { 76 }\\n+    else if value == 77 { 77 }\\n+    else if value == 78 { 78 }\\n+    else if value == 79 { 79 }\\n+    else if value == 80 { 80 }\\n+    else if value == 81 { 81 }\\n+    else if value == 82 { 82 }\\n+    else if value == 83 { 83 }\\n+    else if value == 84 { 84 }\\n+    else if value == 85 { 85 }\\n+    else if value == 86 { 86 }\\n+    else if value == 87 { 87 }\\n+    else if value == 88 { 88 }\\n+    else if value == 89 { 89 }\\n+    else if value == 90 { 90 }\\n+    else if value == 91 { 91 }\\n+    else if value == 92 { 92 }\\n+    else if value == 93 { 93 }\\n+    else if value == 94 { 94 }\\n+    else if value == 95 { 95 }\\n+    else if value == 96 { 96 }\\n+    else if value == 97 { 97 }\\n+    else if value == 98 { 98 }\\n+    else if value == 99 { 99 }\\n+    else if value == 100 { 100 }\\n+    else if value == 101 { 101 }\\n+    else if value == 102 { 102 }\\n+    else if value == 103 { 103 }\\n+    else if value == 104 { 104 }\\n+    else if value == 105 { 105 }\\n+    else if value == 106 { 106 }\\n+    else if value == 107 { 107 }\\n+    else if value == 108 { 108 }\\n+    else if value == 109 { 109 }\\n+    else if value == 110 { 110 }\\n+    else if value == 111 { 111 }\\n+    else if value == 112 { 112 }\\n+    else if value == 113 { 113 }\\n+    else if value == 114 { 114 }\\n+    else if value == 115 { 115 }\\n+    else if value == 116 { 116 }\\n+    else if value == 117 { 117 }\\n+    else if value == 118 { 118 }\\n+    else if value == 119 { 119 }\\n+    else if value == 120 { 120 }\\n+    else if value == 121 { 121 }\\n+    else if value == 122 { 122 }\\n+    else if value == 123 { 123 }\\n+    else if value == 124 { 124 }\\n+    else if value == 125 { 125 }\\n+    else if value == 126 { 126 }\\n+    else if value == 127 { 127 }\\n+    else if value == 128 { 128 }\\n+    else if value == 129 { 129 }\\n+    else if value == 130 { 130 }\\n+    else if value == 131 { 131 }\\n+    else if value == 132 { 132 }\\n+    else if value == 133 { 133 }\\n+    else if value == 134 { 134 }\\n+    else if value == 135 { 135 }\\n+    else if value == 136 { 136 }\\n+    else if value == 137 { 137 }\\n+    else if value == 138 { 138 }\\n+    else if value == 139 { 139 }\\n+    else if value == 140 { 140 }\\n+    else { 141 }\\n+}\\n+\\n+func main() { print(deep(0)) }\\ndiff --git a/prototypes/minivm/examples/values/reject_enum_branches.saw b/prototypes/minivm/examples/values/reject_enum_branches.saw\\nnew file mode 100644\\nindex 00000000..9f8657c4\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_enum_branches.saw\\n@@ -0,0 +1,5 @@\\n+enum First { case Value }\\n+enum Second { case Value }\\n+func main() {\\n+    let value = if true { First.Value } else { Second.Value }\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_extra_call_argument.saw b/prototypes/minivm/examples/values/reject_extra_call_argument.saw\\nnew file mode 100644\\nindex 00000000..87e946d6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_extra_call_argument.saw\\n@@ -0,0 +1,5 @@\\n+func take(value: Int) { print(value) }\\n+\\n+func main() {\\n+    take(if true { 1 } else { 2 }, 3)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_incomplete_match.saw b/prototypes/minivm/examples/values/reject_incomplete_match.saw\\nnew file mode 100644\\nindex 00000000..8bea61ac\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_incomplete_match.saw\\n@@ -0,0 +1,8 @@\\n+enum Color { case Red, case Green, case Blue }\\n+func main() {\\n+    let value = match Color.Red {\\n+        case Red -> 1,\\n+        case Green -> 2\\n+    }\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_match_missing_tail.saw b/prototypes/minivm/examples/values/reject_match_missing_tail.saw\\nnew file mode 100644\\nindex 00000000..6645ee48\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_match_missing_tail.saw\\n@@ -0,0 +1,8 @@\\n+enum Pick { case First, case Second }\\n+func main() {\\n+    let value = match Pick.First {\\n+        case First -> { let n = 1 },\\n+        case Second -> 2\\n+    }\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_mismatched_branches.saw b/prototypes/minivm/examples/values/reject_mismatched_branches.saw\\nnew file mode 100644\\nindex 00000000..7e0a060e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_mismatched_branches.saw\\n@@ -0,0 +1,4 @@\\n+enum Mode { case On }\\n+func main() {\\n+    let value = if true { 1 } else { Mode.On }\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_missing_else.saw b/prototypes/minivm/examples/values/reject_missing_else.saw\\nnew file mode 100644\\nindex 00000000..05cdd2db\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_missing_else.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let value = if true { 1 }\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_missing_tail.saw b/prototypes/minivm/examples/values/reject_missing_tail.saw\\nnew file mode 100644\\nindex 00000000..9313438c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_missing_tail.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let value = if true { let n = 1 } else { 2 }\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_non_tail_expression.saw b/prototypes/minivm/examples/values/reject_non_tail_expression.saw\\nnew file mode 100644\\nindex 00000000..bc02b688\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_non_tail_expression.saw\\n@@ -0,0 +1,6 @@\\n+func choose() -> Int {\\n+    1 + 2\\n+    print(4)\\n+    5\\n+}\\n+func main() { print(choose()) }\\ndiff --git a/prototypes/minivm/examples/values/reject_numeric_no_common.saw b/prototypes/minivm/examples/values/reject_numeric_no_common.saw\\nnew file mode 100644\\nindex 00000000..7101983d\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_numeric_no_common.saw\\n@@ -0,0 +1,5 @@\\n+func main() {\\n+    let signed: Int8 = 1\\n+    let unsigned: UInt8 = 2\\n+    let value = if true { signed } else { unsigned }\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_record_branches.saw b/prototypes/minivm/examples/values/reject_record_branches.saw\\nnew file mode 100644\\nindex 00000000..a7838064\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_record_branches.saw\\n@@ -0,0 +1,5 @@\\n+struct First { value: Int }\\n+struct Second { value: Int }\\n+func main() {\\n+    let value = if true { First(value: 1) } else { Second(value: 2) }\\n+}\\ndiff --git a/prototypes/minivm/examples/values/reject_unselected_range.saw b/prototypes/minivm/examples/values/reject_unselected_range.saw\\nnew file mode 100644\\nindex 00000000..81dbffce\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/reject_unselected_range.saw\\n@@ -0,0 +1,4 @@\\n+func main() {\\n+    let value: Int8 = if true { 1 } else { 128 }\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/returns_and_scope.saw b/prototypes/minivm/examples/values/returns_and_scope.saw\\nnew file mode 100644\\nindex 00000000..115c5629\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/returns_and_scope.saw\\n@@ -0,0 +1,33 @@\\n+func mixed(flag: Bool) -> Int {\\n+    if flag { return 10 } else { 20 }\\n+}\\n+\\n+func all_return(flag: Bool) -> Int {\\n+    if flag { return 30 } else { return 40 }\\n+}\\n+\\n+func scoped(flag: Bool) -> Int {\\n+    if flag {\\n+        var n = 0\\n+        while n < 3 { n += 1 }\\n+        n\\n+    } else {\\n+        let n = 9\\n+        n\\n+    }\\n+}\\n+\\n+func early(flag: Bool) -> Int {\\n+    if flag { return 50 }\\n+    1 / 0\\n+}\\n+\\n+func main() {\\n+    print(mixed(true))\\n+    print(mixed(false))\\n+    print(all_return(true))\\n+    print(all_return(false))\\n+    print(scoped(true))\\n+    print(scoped(false))\\n+    print(early(true))\\n+}\\ndiff --git a/prototypes/minivm/examples/values/review_composition.saw b/prototypes/minivm/examples/values/review_composition.saw\\nnew file mode 100644\\nindex 00000000..eafb93bc\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/review_composition.saw\\n@@ -0,0 +1,15 @@\\n+enum E { case A, case B }\\n+struct Cell { n: Int }\\n+func narrow(flag: Bool) -> UInt8 {\\n+    (if flag { 255 } else { 1 })\\n+}\\n+func main() {\\n+    let flag = false\\n+    let a: Bool = (if flag { 1 } else { 2 }) == 2\\n+    print(a)\\n+    print((if flag { 3 } else { 4 }) + 5)\\n+    print((if flag { Cell(n: 7) } else { Cell(n: 8) }).n)\\n+    print((match E.B { case A -> 10, case B -> 11 }) as UInt8)\\n+    print(narrow(true))\\n+    print(narrow(false))\\n+}\\ndiff --git a/prototypes/minivm/examples/values/review_context.saw b/prototypes/minivm/examples/values/review_context.saw\\nnew file mode 100644\\nindex 00000000..3a9d277c\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/review_context.saw\\n@@ -0,0 +1,31 @@\\n+enum E { case A, case B }\\n+func f(flag: Bool) -> Bool {\\n+    if (if flag { 1 } else { 2 }) == 1 { true } else { false }\\n+}\\n+func g(flag: Bool) -> UInt8 {\\n+    if (if flag { 300 } else { 400 }) > 350 { 1 } else { 2 }\\n+}\\n+func h(flag: Bool) -> UInt8 {\\n+    if flag {\\n+        let local: Int = if flag { 300 } else { 400 }\\n+        if local == 300 { 3 } else { 4 }\\n+    } else { 5 }\\n+}\\n+func k(flag: Bool) -> Int {\\n+    return 1 + (if flag { 2 } else { return 40 })\\n+}\\n+func main() {\\n+    print(f(true))\\n+    print(f(false))\\n+    print(g(true))\\n+    print(g(false))\\n+    print(h(true))\\n+    print(h(false))\\n+    print(k(true))\\n+    print(k(false))\\n+    let v = match E.A {\\n+        case A -> if true { 7 } else { 8 },\\n+        case B -> 9\\n+    }\\n+    print(v)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/review_grid.saw b/prototypes/minivm/examples/values/review_grid.saw\\nnew file mode 100644\\nindex 00000000..1cc74c6f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/review_grid.saw\\n@@ -0,0 +1,45 @@\\n+enum Mode { case A, case B, case C }\\n+struct Pair { left: Int, right: UInt64 }\\n+func mark(n: Int) -> Int {\\n+    print(n)\\n+    n\\n+}\\n+func pair(a: Int, b: UInt64) -> Pair { Pair(left: a, right: b) }\\n+func choose(a: Bool, b: Bool, mode: Mode) -> Pair {\\n+    pair(\\n+        if a {\\n+            let n = mark(10)\\n+            if b { n + 1 } else { n + 2 }\\n+        } else {\\n+            match mode {\\n+                case A -> mark(20),\\n+                case B -> { return Pair(left: mark(30), right: 99) },\\n+                case C -> {\\n+                    var n = 0\\n+                    while n < 3 { n += 1 }\\n+                    n + mark(40)\\n+                }\\n+            }\\n+        },\\n+        if b { 18446744073709551615 } else { 9223372036854775808 }\\n+    )\\n+}\\n+func show(a: Bool, b: Bool, m: Mode) {\\n+    let p = choose(a, b, m)\\n+    print(p.left)\\n+    print(p.right)\\n+}\\n+func main() {\\n+    show(true, true, Mode.A)\\n+    show(true, false, Mode.B)\\n+    show(false, true, Mode.A)\\n+    show(false, false, Mode.B)\\n+    show(false, false, Mode.C)\\n+    var n = 0\\n+    var total = 0\\n+    while n < 5 {\\n+        total += if n % 2 == 0 { n } else { n * 10 }\\n+        n += 1\\n+    }\\n+    print(total)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/scalar_bool_tails.saw b/prototypes/minivm/examples/values/scalar_bool_tails.saw\\nnew file mode 100644\\nindex 00000000..098921a6\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/scalar_bool_tails.saw\\n@@ -0,0 +1,9 @@\\n+func number() -> Int { 42 }\\n+func truth() -> Bool { true }\\n+func narrow() -> Int8 { -128 }\\n+\\n+func main() {\\n+    print(number())\\n+    print(truth())\\n+    print(narrow())\\n+}\\ndiff --git a/prototypes/minivm/examples/values/statement_else_if.saw b/prototypes/minivm/examples/values/statement_else_if.saw\\nnew file mode 100644\\nindex 00000000..59c2d2df\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/statement_else_if.saw\\n@@ -0,0 +1,23 @@\\n+func choose() -> Int {\\n+    if false {\\n+        print(4)\\n+    } else if true {\\n+        print(5)\\n+    } else {\\n+        print(99)\\n+    }\\n+    print(6)\\n+    7\\n+}\\n+\\n+func main() {\\n+    if false {\\n+        print(1)\\n+    } else if true {\\n+        print(2)\\n+    } else {\\n+        print(99)\\n+    }\\n+    print(3)\\n+    print(choose())\\n+}\\ndiff --git a/prototypes/minivm/examples/values/u64_context.saw b/prototypes/minivm/examples/values/u64_context.saw\\nnew file mode 100644\\nindex 00000000..9fa00e7f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/u64_context.saw\\n@@ -0,0 +1,10 @@\\n+func high(flag: Bool) -> UInt64 {\\n+    if flag { UInt64.max } else { 9223372036854775808 }\\n+}\\n+\\n+func main() {\\n+    print(high(true))\\n+    print(high(false))\\n+    let small: UInt8 = if false { 0 } else { 255 }\\n+    print(small)\\n+}\\ndiff --git a/prototypes/minivm/examples/values/value_block_void_then_tail.saw b/prototypes/minivm/examples/values/value_block_void_then_tail.saw\\nnew file mode 100644\\nindex 00000000..d1d18ce9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/values/value_block_void_then_tail.saw\\n@@ -0,0 +1,11 @@\\n+func marker() { print(10) }\\n+\\n+func main() {\\n+    let value = if true {\\n+        marker()\\n+        42\\n+    } else {\\n+        0\\n+    }\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 4dc7220d..319ddacd 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -48,11 +48,24 @@ struct ExprValue {\\n     literal: Bool\\n     literal_value: UInt\\n     literal_negative: Bool\\n+    always_returns: Bool\\n+}\\n+\\n+struct BlockValue {\\n+    has_value: Bool\\n+    value: ExprValue\\n+    always_returns: Bool\\n }\\n \\n func expr_value(ty: ValueType, slot: Int, call: Bool) -> ExprValue {\\n     ExprValue(ty: ty, slot: slot, call: call, literal: false,\\n-              literal_value: 0, literal_negative: false)\\n+              literal_value: 0, literal_negative: false, always_returns: false)\\n+}\\n+\\n+func returning_value() -> ExprValue {\\n+    var v = expr_value(ValueType.Void, -1, false)\\n+    v.always_returns = true\\n+    v\\n }\\n \\n func type_name(t: ValueType) -> String {\\n@@ -382,7 +395,7 @@ extension Compiler {\\n         bits = integer_normalize(bits, source)\\n         if bare {\\n             let probe = ExprValue(ty: source, slot: -1, call: false, literal: true,\\n-                                  literal_value: magnitude, literal_negative: negative)\\n+                                  literal_value: magnitude, literal_negative: negative, always_returns: false)\\n             if not self.literal_fits_target(&probe, target) {\\n                 return problem(\\\"integer literal does not fit {type_name(target)}\\\", t.line, t.col)\\n             }\\n@@ -645,7 +658,7 @@ extension Compiler {\\n             }\\n         }\\n         ExprValue(ty: target, slot: v.slot, call: false, literal: true,\\n-                  literal_value: v.literal_value, literal_negative: v.literal_negative)\\n+                  literal_value: v.literal_value, literal_negative: v.literal_negative, always_returns: false)\\n     }\\n \\n     func transfer(&var self, v: ExprValue, target: ValueType, t: &Token) -> Result<ExprValue, ProtoError> {\\n@@ -668,6 +681,8 @@ extension Compiler {\\n \\n     func parse_atom(&var self) -> Result<ExprValue, ProtoError> {\\n         if self.depth > 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n+        if self.at(TokenKind.If) { return self.parse_if_value(ValueType.Void, false) }\\n+        if self.at(TokenKind.Match) { return self.parse_match_value(ValueType.Void, false) }\\n         if self.at(TokenKind.IntLit) {\\n             let t = self.take()\\n             guard let magnitude = literal_magnitude(t.value) else { return problem(\\\"invalid integer literal\\\", t.line, t.col) }\\n@@ -685,7 +700,7 @@ extension Compiler {\\n             self.code[at].dst = dst\\n             self.code[at].immediate = integer_normalize(Int.from(truncating: magnitude), ty)\\n             return ExprValue(ty: ty, slot: dst, call: false, literal: bare,\\n-                             literal_value: magnitude, literal_negative: false)\\n+                             literal_value: magnitude, literal_negative: false, always_returns: false)\\n         }\\n         if self.at(TokenKind.True_) || self.at(TokenKind.False_) {\\n             let t = self.take()\\n@@ -840,8 +855,8 @@ extension Compiler {\\n                 }\\n                 let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after record field label\\\")\\n                 self.expr_space()\\n-                var value = try self.parse_expr()\\n-                value = try self.transfer(value, self.records[ri].field_types[fi], &label)\\n+                var value = try self.parse_expected_expr(self.records[ri].field_types[fi])\\n+                if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", label.line, label.col) }\\n                 value = try self.snapshot(value, &label)\\n                 try! fields.push(fi)\\n                 try! values.push(move value)\\n@@ -943,11 +958,11 @@ extension Compiler {\\n         if not self.at(TokenKind.RParen) {\\n             var more = true\\n             while more {\\n-                var value = try self.parse_expr()\\n                 if ai >= self.signatures[fi].params.len() {\\n                     return problem(\\\"too many arguments to `{name.value}`\\\", name.line, name.col)\\n                 }\\n-                value = try self.transfer(value, self.signatures[fi].params[ai], &name)\\n+                var value = try self.parse_expected_expr(self.signatures[fi].params[ai])\\n+                if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n                 value = try self.snapshot(value, &name)\\n                 let layout = try value_layout(&self.records, value.ty)\\n                 var word = 0\\n@@ -1017,7 +1032,7 @@ extension Compiler {\\n                 self.code[at].dst = dst\\n                 self.code[at].immediate = bits\\n                 let value = ExprValue(ty: ty, slot: dst, call: false, literal: bare,\\n-                                      literal_value: magnitude, literal_negative: true)\\n+                                      literal_value: magnitude, literal_negative: true, always_returns: false)\\n                 return value\\n             }\\n             let v = try self.parse_unary()\\n@@ -1089,6 +1104,9 @@ extension Compiler {\\n \\n     func common_type(&self, left: &ExprValue, right: &ExprValue,\\n                      t: &Token) -> Result<ValueType, ProtoError> {\\n+        if left.always_returns || right.always_returns {\\n+            return problem(\\\"all-returning control flow cannot be used as an operand\\\", t.line, t.col)\\n+        }\\n         if left.ty == right.ty {\\n             if left.literal && not self.literal_fits_target(&left, left.ty) { return problem(\\\"integer literal does not fit {type_name(left.ty)}\\\", t.line, t.col) }\\n             if right.literal && not self.literal_fits_target(&right, right.ty) { return problem(\\\"integer literal does not fit {type_name(right.ty)}\\\", t.line, t.col) }\\n@@ -1308,6 +1326,422 @@ extension Compiler {\\n         left\\n     }\\n \\n+    func parse_expected_expr(&var self, expected: ValueType) -> Result<ExprValue, ProtoError> {\\n+        if self.depth >= 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n+        self.expr_space()\\n+        if self.wrapped_control_is_isolated() {\\n+            let _ = self.take()\\n+            self.depth += 1\\n+            self.paren_depth += 1\\n+            self.skip_newlines()\\n+            let value = try self.parse_expected_expr(expected)\\n+            self.skip_newlines()\\n+            let _ = try self.expect(TokenKind.RParen, \\\"expected `)`\\\")\\n+            self.paren_depth -= 1\\n+            self.depth -= 1\\n+            return value\\n+        }\\n+        if self.control_is_isolated() {\\n+            if self.at(TokenKind.If) { return self.parse_if_value(expected, true) }\\n+            if self.at(TokenKind.Match) { return self.parse_match_value(expected, true) }\\n+        }\\n+        let anchor = self.token()\\n+        let value = try self.parse_expr()\\n+        if value.always_returns { return value }\\n+        self.transfer(value, expected, &anchor)\\n+    }\\n+\\n+    func value_statement_start(&self) -> Bool {\\n+        let t = self.token()\\n+        if t.kind == TokenKind.Let || t.kind == TokenKind.Var || t.kind == TokenKind.Return || t.kind == TokenKind.While || t.kind == TokenKind.LBrace { return true }\\n+        if t.kind == TokenKind.Ident && t.value == \\\"print\\\" { return true }\\n+        self.assignment_ahead()\\n+    }\\n+\\n+    func expression_is_block_tail(&self) -> Bool {\\n+        var i = self.pos\\n+        var parens = 0\\n+        var braces = 0\\n+        while i < self.tokens.len() {\\n+            let kind = self.tokens[i].kind\\n+            if kind == TokenKind.LParen { parens += 1 }\\n+            else if kind == TokenKind.RParen { if parens > 0 { parens -= 1 } }\\n+            else if kind == TokenKind.LBrace { braces += 1 }\\n+            else if kind == TokenKind.RBrace {\\n+                if braces > 0 { braces -= 1 }\\n+                else { return true }\\n+            }\\n+            if kind == TokenKind.Newline && parens == 0 && braces == 0 {\\n+                var j = i + 1\\n+                while j < self.tokens.len() && self.tokens[j].kind == TokenKind.Newline { j += 1 }\\n+                if j < self.tokens.len() && self.tokens[j].kind == TokenKind.Else { i = j }\\n+                else { return j < self.tokens.len() && self.tokens[j].kind == TokenKind.RBrace }\\n+            }\\n+            i += 1\\n+        }\\n+        false\\n+    }\\n+\\n+    func control_end_from(&self, start: Int) -> Int {\\n+        if start < 0 || start >= self.tokens.len() { return -1 }\\n+        if self.tokens[start].kind != TokenKind.If && self.tokens[start].kind != TokenKind.Match { return -1 }\\n+        let starts_if = self.tokens[start].kind == TokenKind.If\\n+        var i = start\\n+        var braces = 0\\n+        var parens = 0\\n+        var saw_brace = false\\n+        while i < self.tokens.len() {\\n+            let kind = self.tokens[i].kind\\n+            if kind == TokenKind.LParen { parens += 1 }\\n+            else if kind == TokenKind.RParen { if parens > 0 { parens -= 1 } }\\n+            else if kind == TokenKind.LBrace && (saw_brace || parens == 0) {\\n+                braces += 1\\n+                saw_brace = true\\n+            }\\n+            else if kind == TokenKind.RBrace && saw_brace {\\n+                braces -= 1\\n+                if saw_brace && braces == 0 {\\n+                    var j = i + 1\\n+                    while j < self.tokens.len() && self.tokens[j].kind == TokenKind.Newline { j += 1 }\\n+                    if starts_if && j < self.tokens.len() && self.tokens[j].kind == TokenKind.Else {\\n+                        j += 1\\n+                        while j < self.tokens.len() && self.tokens[j].kind == TokenKind.Newline { j += 1 }\\n+                        i = j - 1\\n+                        saw_brace = false\\n+                    } else { return i + 1 }\\n+                }\\n+            }\\n+            i += 1\\n+        }\\n+        -1\\n+    }\\n+\\n+    func control_is_isolated(&self) -> Bool {\\n+        let i = self.control_end_from(self.pos)\\n+        if i < 0 { return false }\\n+        if i >= self.tokens.len() { return true }\\n+        let kind = self.tokens[i].kind\\n+        kind == TokenKind.RBrace || kind == TokenKind.RParen || kind == TokenKind.Comma || kind == TokenKind.Newline || kind == TokenKind.Eof\\n+    }\\n+\\n+    func if_has_else(&self) -> Bool {\\n+        let end = self.control_end_from(self.pos)\\n+        var i = self.pos\\n+        var braces = 0\\n+        var parens = 0\\n+        while i < end {\\n+            let kind = self.tokens[i].kind\\n+            if kind == TokenKind.LParen { parens += 1 }\\n+            else if kind == TokenKind.RParen { parens -= 1 }\\n+            else if kind == TokenKind.LBrace { braces += 1 }\\n+            else if kind == TokenKind.RBrace { braces -= 1 }\\n+            else if kind == TokenKind.Else && braces == 0 && parens == 0 { return true }\\n+            i += 1\\n+        }\\n+        false\\n+    }\\n+\\n+    func wrapped_control_is_isolated(&self) -> Bool {\\n+        var start = self.pos\\n+        var wrappers = 0\\n+        while start < self.tokens.len() && self.tokens[start].kind == TokenKind.LParen {\\n+            wrappers += 1\\n+            start += 1\\n+            while start < self.tokens.len() && self.tokens[start].kind == TokenKind.Newline { start += 1 }\\n+        }\\n+        if wrappers == 0 { return false }\\n+        var finish = self.control_end_from(start)\\n+        if finish < 0 { return false }\\n+        var n = 0\\n+        while n < wrappers {\\n+            while finish < self.tokens.len() && self.tokens[finish].kind == TokenKind.Newline { finish += 1 }\\n+            if finish >= self.tokens.len() || self.tokens[finish].kind != TokenKind.RParen { return false }\\n+            finish += 1\\n+            n += 1\\n+        }\\n+        if finish >= self.tokens.len() { return true }\\n+        let kind = self.tokens[finish].kind\\n+        kind == TokenKind.RBrace || kind == TokenKind.RParen || kind == TokenKind.Comma || kind == TokenKind.Newline || kind == TokenKind.Eof\\n+    }\\n+\\n+    func parse_value_block(&var self, expected: ValueType, has_expected: Bool) -> Result<BlockValue, ProtoError> {\\n+        if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n+        self.depth += 1\\n+        let saved_parens = self.paren_depth\\n+        self.paren_depth = 0\\n+        self.skip_newlines()\\n+        var returns = false\\n+        while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n+            let tail = self.expression_is_block_tail()\\n+            if self.value_statement_start() || ((self.at(TokenKind.If) || self.at(TokenKind.Match)) && not tail) {\\n+                let r = try self.parse_statement()\\n+                if r { returns = true }\\n+                try self.terminator()\\n+                continue\\n+            }\\n+            let start = self.token()\\n+            var value = returning_value()\\n+            if has_expected && tail { value = try self.parse_expected_expr(expected) }\\n+            else { value = try self.parse_expr() }\\n+            let had_newline = self.at(TokenKind.Newline)\\n+            if had_newline { self.skip_newlines() }\\n+            if self.at(TokenKind.RBrace) {\\n+                let _ = self.take()\\n+                self.leave_scope()\\n+                self.depth -= 1\\n+                self.paren_depth = saved_parens\\n+                if returns || value.always_returns {\\n+                    return BlockValue(has_value: false, value: returning_value(), always_returns: true)\\n+                }\\n+                return BlockValue(has_value: true, value: value, always_returns: false)\\n+            }\\n+            if value.always_returns { returns = true }\\n+            else if not value.call || value.ty != ValueType.Void {\\n+                return problem(\\\"only Void function calls may be expression statements\\\", start.line, start.col)\\n+            }\\n+            if not had_newline { return problem(\\\"expected newline after statement\\\", start.line, start.col) }\\n+        }\\n+        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace\\\")\\n+        self.leave_scope()\\n+        self.depth -= 1\\n+        self.paren_depth = saved_parens\\n+        BlockValue(has_value: false, value: returning_value(), always_returns: returns)\\n+    }\\n+\\n+    func merge_control_type(&self, a: &ExprValue, b: &ExprValue,\\n+                            expected: ValueType, has_expected: Bool, t: &Token) -> Result<ValueType, ProtoError> {\\n+        if has_expected { return expected }\\n+        if a.ty == b.ty { return a.ty }\\n+        if a.literal && integer_type(b.ty) { return b.ty }\\n+        if b.literal && integer_type(a.ty) { return a.ty }\\n+        if integer_widens(a.ty, b.ty) { return b.ty }\\n+        if integer_widens(b.ty, a.ty) { return a.ty }\\n+        problem(\\\"control-flow branches have incompatible types\\\", t.line, t.col)\\n+    }\\n+\\n+    func append_value_exit(&var self, exit: Int, value: ExprValue, target: ValueType,\\n+                           dst: Int, t: &Token, joins: &var Vector<Int>) -> Result<Void, ProtoError> {\\n+        self.code[exit].target = self.code.len()\\n+        let converted = try self.transfer(value, target, &t)\\n+        try self.emit_copy_value(dst, converted.slot, target, &t)\\n+        let join = self.emit_at(Op.Jump, t.line, t.col)\\n+        try! joins.push(join)\\n+        return\\n+    }\\n+\\n+    func parse_if_value(&var self, expected: ValueType, has_expected: Bool) -> Result<ExprValue, ProtoError> {\\n+        if self.expr_depth >= 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n+        self.expr_depth += 1\\n+        let result = try self.parse_if_value_inner(expected, has_expected)\\n+        self.expr_depth -= 1\\n+        result\\n+    }\\n+\\n+    func parse_if_value_inner(&var self, expected: ValueType, has_expected: Bool) -> Result<ExprValue, ProtoError> {\\n+        let it = self.take()\\n+        let condition = try self.parse_expr()\\n+        try self.require_type(condition.ty, ValueType.Bool, &it)\\n+        let _ = try self.expect(TokenKind.LBrace, \\\"expected block after if condition\\\")\\n+        let branch = self.emit_at(Op.Branch, it.line, it.col)\\n+        self.code[branch].a = condition.slot\\n+        let skip_then = self.emit_at(Op.Jump, it.line, it.col)\\n+        self.code[branch].target = self.code.len()\\n+        let then_value = try self.parse_value_block(expected, has_expected)\\n+        var then_exit = -1\\n+        if not then_value.always_returns { then_exit = self.emit_at(Op.Jump, it.line, it.col) }\\n+        self.code[skip_then].target = self.code.len()\\n+        self.skip_newlines()\\n+        if not self.at(TokenKind.Else) { return problem(\\\"value if requires an else\\\", it.line, it.col) }\\n+        let _ = self.take()\\n+        var else_value = BlockValue(has_value: false, value: returning_value(), always_returns: false)\\n+        if self.at(TokenKind.If) {\\n+            let nested = try self.parse_if_value(expected, has_expected)\\n+            else_value = BlockValue(has_value: not nested.always_returns, value: nested,\\n+                                    always_returns: nested.always_returns)\\n+        } else {\\n+            let _ = try self.expect(TokenKind.LBrace, \\\"expected block after else\\\")\\n+            else_value = try self.parse_value_block(expected, has_expected)\\n+        }\\n+        var else_exit = -1\\n+        if not else_value.always_returns { else_exit = self.emit_at(Op.Jump, it.line, it.col) }\\n+        if not then_value.always_returns && not then_value.has_value {\\n+            return problem(\\\"continuing value branch requires a tail expression\\\", it.line, it.col)\\n+        }\\n+        if not else_value.always_returns && not else_value.has_value {\\n+            return problem(\\\"continuing value branch requires a tail expression\\\", it.line, it.col)\\n+        }\\n+        if then_value.always_returns && else_value.always_returns { return returning_value() }\\n+        var target = ValueType.Void\\n+        if then_value.always_returns {\\n+            target = else_value.value.ty\\n+            if has_expected { target = expected }\\n+        }\\n+        else if else_value.always_returns {\\n+            target = then_value.value.ty\\n+            if has_expected { target = expected }\\n+        }\\n+        else { target = try self.merge_control_type(&then_value.value, &else_value.value, expected, has_expected, &it) }\\n+        let dst = try self.new_value(target)\\n+        var joins = Vector<Int>()\\n+        if then_exit >= 0 { try self.append_value_exit(then_exit, then_value.value, target, dst, &it, &var joins) }\\n+        if else_exit >= 0 { try self.append_value_exit(else_exit, else_value.value, target, dst, &it, &var joins) }\\n+        let continuation = self.code.len()\\n+        var j = 0\\n+        while j < joins.len() {\\n+            self.code[joins[j]].target = continuation\\n+            j += 1\\n+        }\\n+        expr_value(target, dst, false)\\n+    }\\n+\\n+    func parse_match_value(&var self, expected: ValueType, has_expected: Bool) -> Result<ExprValue, ProtoError> {\\n+        if self.expr_depth >= 128 { return self.fail(\\\"expression nesting limit exceeded\\\") }\\n+        self.expr_depth += 1\\n+        let result = try self.parse_match_value_inner(expected, has_expected)\\n+        self.expr_depth -= 1\\n+        result\\n+    }\\n+\\n+    func parse_match_value_inner(&var self, expected: ValueType, has_expected: Bool) -> Result<ExprValue, ProtoError> {\\n+        let mt = self.take()\\n+        var subject = try self.parse_expr()\\n+        subject = try self.snapshot(subject, &mt)\\n+        var ei = -1\\n+        match subject.ty {\\n+            case Enum(index) -> { ei = index },\\n+            case _ -> { return problem(\\\"match subject must have enum type\\\", mt.line, mt.col) }\\n+        }\\n+        let _ = try self.expect(TokenKind.LBrace, \\\"expected `\\\\{` after match subject\\\")\\n+        self.skip_newlines()\\n+        var covered = Vector<Bool>()\\n+        var n = 0\\n+        while n < self.enums[ei].cases.len() {\\n+            try! covered.push(false)\\n+            n += 1\\n+        }\\n+        var exits = Vector<Int>()\\n+        var values = Vector<ExprValue>()\\n+        var pending_false = -1\\n+        var wildcard = false\\n+        var arms = 0\\n+        while not self.at(TokenKind.RBrace) && not self.at(TokenKind.Eof) {\\n+            if wildcard { return self.fail(\\\"match arm follows wildcard\\\") }\\n+            if pending_false >= 0 { self.code[pending_false].target = self.code.len() }\\n+            let ct = try self.expect(TokenKind.Case, \\\"expected `case` in match\\\")\\n+            let pattern = try self.expect(TokenKind.Ident, \\\"expected enum case pattern\\\")\\n+            var ordinal = -1\\n+            if pattern.value == \\\"_\\\" { wildcard = true }\\n+            else if self.at(TokenKind.Dot) {\\n+                let qualifier = self.enum_index(pattern.value)\\n+                if qualifier != ei { return problem(\\\"enum qualifier `{pattern.value}` does not match subject enum `{self.enums[ei].name}`\\\", pattern.line, pattern.col) }\\n+                let _ = self.take()\\n+                let item = try self.expect(TokenKind.Ident, \\\"expected qualified enum case\\\")\\n+                ordinal = self.enum_case_index(ei, item.value)\\n+                if ordinal < 0 { return problem(\\\"enum `{self.enums[ei].name}` has no case `{item.value}`\\\", item.line, item.col) }\\n+            } else {\\n+                ordinal = self.enum_case_index(ei, pattern.value)\\n+                if ordinal < 0 { return problem(\\\"enum `{self.enums[ei].name}` has no case `{pattern.value}`\\\", pattern.line, pattern.col) }\\n+            }\\n+            if ordinal >= 0 {\\n+                if covered[ordinal] { return problem(\\\"duplicate match case `{self.enums[ei].cases[ordinal]}`\\\", pattern.line, pattern.col) }\\n+                covered[ordinal] = true\\n+                let constant = self.new_slot(subject.ty)\\n+                let ci = self.emit_at(Op.Const, pattern.line, pattern.col)\\n+                self.code[ci].dst = constant\\n+                self.code[ci].immediate = ordinal\\n+                let equal = self.new_slot(ValueType.Bool)\\n+                let eq = self.emit_at(Op.Eq, pattern.line, pattern.col)\\n+                self.code[eq].dst = equal\\n+                self.code[eq].a = subject.slot\\n+                self.code[eq].b = constant\\n+                let branch = self.emit_at(Op.Branch, pattern.line, pattern.col)\\n+                self.code[branch].a = equal\\n+                pending_false = self.emit_at(Op.Jump, pattern.line, pattern.col)\\n+                self.code[branch].target = self.code.len()\\n+            } else { pending_false = -1 }\\n+            let _ = try self.expect(TokenKind.Arrow, \\\"expected `->` after match pattern\\\")\\n+            var outcome = BlockValue(has_value: false, value: returning_value(), always_returns: false)\\n+            if self.at(TokenKind.LBrace) {\\n+                let _ = self.take()\\n+                outcome = try self.parse_value_block(expected, has_expected)\\n+            } else {\\n+                var value = returning_value()\\n+                if has_expected { value = try self.parse_expected_expr(expected) }\\n+                else { value = try self.parse_expr() }\\n+                outcome = BlockValue(has_value: not value.always_returns, value: value,\\n+                                     always_returns: value.always_returns)\\n+            }\\n+            if not outcome.always_returns {\\n+                if not outcome.has_value { return problem(\\\"continuing value branch requires a tail expression\\\", ct.line, ct.col) }\\n+                let exit = self.emit_at(Op.Jump, ct.line, ct.col)\\n+                try! exits.push(exit)\\n+                try! values.push(outcome.value)\\n+            }\\n+            arms += 1\\n+            self.skip_newlines()\\n+            if self.at(TokenKind.Comma) {\\n+                let _ = self.take()\\n+                self.skip_newlines()\\n+            }\\n+        }\\n+        let _ = try self.expect(TokenKind.RBrace, \\\"expected closing brace for match\\\")\\n+        if arms == 0 { return problem(\\\"non-exhaustive match\\\", mt.line, mt.col) }\\n+        var exhaustive = wildcard\\n+        if not exhaustive {\\n+            exhaustive = true\\n+            n = 0\\n+            while n < covered.len() {\\n+                if not covered[n] { exhaustive = false }\\n+                n += 1\\n+            }\\n+        }\\n+        if not exhaustive { return problem(\\\"non-exhaustive match\\\", mt.line, mt.col) }\\n+        if values.len() == 0 {\\n+            if pending_false >= 0 { self.code[pending_false].target = self.code.len() }\\n+            return returning_value()\\n+        }\\n+        var target = values[0].ty\\n+        if has_expected { target = expected }\\n+        if not has_expected {\\n+            var candidate = -1\\n+            var saw_typed = false\\n+            n = 0\\n+            while n < values.len() {\\n+                if not values[n].literal {\\n+                    saw_typed = true\\n+                    var accepts = true\\n+                    var j = 0\\n+                    while j < values.len() {\\n+                        if values[j].ty != values[n].ty && not values[j].literal && not integer_widens(values[j].ty, values[n].ty) { accepts = false }\\n+                        j += 1\\n+                    }\\n+                    if accepts {\\n+                        candidate = n\\n+                        break\\n+                    }\\n+                }\\n+                n += 1\\n+            }\\n+            if candidate >= 0 { target = values[candidate].ty }\\n+            else if saw_typed { return problem(\\\"control-flow branches have incompatible types\\\", mt.line, mt.col) }\\n+        }\\n+        let dst = try self.new_value(target)\\n+        var joins = Vector<Int>()\\n+        n = 0\\n+        while n < values.len() {\\n+            try self.append_value_exit(exits[n], values[n], target, dst, &mt, &var joins)\\n+            n += 1\\n+        }\\n+        let continuation = self.code.len()\\n+        if pending_false >= 0 { self.code[pending_false].target = continuation }\\n+        n = 0\\n+        while n < joins.len() {\\n+            self.code[joins[n]].target = continuation\\n+            n += 1\\n+        }\\n+        expr_value(target, dst, false)\\n+    }\\n+\\n     func parse_block(&var self) -> Result<Bool, ProtoError> {\\n         if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n         self.depth += 1\\n@@ -1420,6 +1854,7 @@ extension Compiler {\\n     }\\n \\n     func parse_statement(&var self) -> Result<Bool, ProtoError> {\\n+        let statement_token = self.token()\\n         if self.at(TokenKind.Semicolon) { return self.fail(\\\"semicolons are not supported\\\") }\\n         if self.at(TokenKind.Match) { return self.parse_match_statement() }\\n         if self.at(TokenKind.Let) || self.at(TokenKind.Var) {\\n@@ -1431,10 +1866,10 @@ extension Compiler {\\n                 annotated = Some(try self.parse_type())\\n             }\\n             let _ = try self.expect(TokenKind.Assign, \\\"local declarations require an initializer\\\")\\n-            var value = try self.parse_expr()\\n+            var value = if let ty = annotated { try self.parse_expected_expr(ty) } else { try self.parse_expr() }\\n+            if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n             if value.ty == ValueType.Void { return problem(\\\"cannot initialize a local with Void\\\", name.line, name.col) }\\n-            if let ty = annotated { value = try self.transfer(value, ty, &name) }\\n-            else { value = try self.transfer(value, value.ty, &name) }\\n+            if let _ = annotated {} else { value = try self.transfer(value, value.ty, &name) }\\n             let dst = try self.new_value(value.ty)\\n             try self.emit_copy_value(dst, value.slot, value.ty, &name)\\n             try self.declare(name, value.ty, dst, kw.kind == TokenKind.Var)\\n@@ -1444,8 +1879,8 @@ extension Compiler {\\n             let t = self.take()\\n             var src = -1\\n             if not self.at(TokenKind.Newline) && not self.at(TokenKind.RBrace) {\\n-                var value = try self.parse_expr()\\n-                value = try self.transfer(value, self.current_result, &t)\\n+                var value = try self.parse_expected_expr(self.current_result)\\n+                if value.always_returns { return true }\\n                 src = value.slot\\n             } else if self.current_result != ValueType.Void { return problem(\\\"non-Void return requires a value\\\", t.line, t.col) }\\n             let at = self.emit_at(Op.Return, t.line, t.col)\\n@@ -1469,8 +1904,15 @@ extension Compiler {\\n             self.skip_newlines()\\n             if self.at(TokenKind.Else) {\\n                 let _ = self.take()\\n-                let _ = try self.expect(TokenKind.LBrace, \\\"expected block after else\\\")\\n-                else_returns = try self.parse_block()\\n+                if self.at(TokenKind.If) {\\n+                    if self.depth >= 128 { return self.fail(\\\"block nesting limit exceeded\\\") }\\n+                    self.depth += 1\\n+                    else_returns = try self.parse_statement()\\n+                    self.depth -= 1\\n+                } else {\\n+                    let _ = try self.expect(TokenKind.LBrace, \\\"expected block after else\\\")\\n+                    else_returns = try self.parse_block()\\n+                }\\n             } else {\\n                 self.pos = before_else\\n             }\\n@@ -1497,13 +1939,14 @@ extension Compiler {\\n             let _ = self.take()\\n             return self.parse_block()\\n         }\\n-        if self.at(TokenKind.Ident) && self.token().value == \\\"print\\\" {\\n+        if statement_token.kind == TokenKind.Ident && statement_token.value == \\\"print\\\" {\\n             let t = self.take()\\n             let _ = try self.expect(TokenKind.LParen, \\\"expected `(` after print\\\")\\n             self.depth += 1\\n             self.paren_depth += 1\\n             self.skip_newlines()\\n             var value = try self.parse_expr()\\n+            if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", t.line, t.col) }\\n             value = try self.transfer(value, value.ty, &t)\\n             self.skip_newlines()\\n             let _ = try self.expect(TokenKind.RParen, \\\"expected `)` after print argument\\\")\\n@@ -1526,7 +1969,8 @@ extension Compiler {\\n             let place = parsed.1\\n             let assign = self.take()\\n             if not place.mutable { return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col) }\\n-            var value = try self.parse_expr()\\n+            var value = if assign.kind == TokenKind.Assign { try self.parse_expected_expr(place.ty) } else { try self.parse_expr() }\\n+            if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", assign.line, assign.col) }\\n             if assign.kind != TokenKind.Assign {\\n                 if not arithmetic_type(place.ty) { return problem(\\\"compound assignment requires an arithmetic integer destination\\\", assign.line, assign.col) }\\n                 if value.literal { value = try self.transfer(value, place.ty, &assign) }\\n@@ -1534,7 +1978,7 @@ extension Compiler {\\n                 let op = if assign.kind == TokenKind.PlusAssign { Op.Add } else if assign.kind == TokenKind.MinusAssign { Op.Sub } else if assign.kind == TokenKind.StarAssign { Op.Mul } else if assign.kind == TokenKind.SlashAssign { Op.Div } else { Op.Rem }\\n                 let left = expr_value(place.ty, place.slot, false)\\n                 value = try self.binary(left, value, op, place.ty, &assign)\\n-            } else { value = try self.transfer(value, place.ty, &name) }\\n+            }\\n             value = try self.snapshot(value, &name)\\n             try self.emit_copy_value(place.slot, value.slot, place.ty, &name)\\n             return false\\n@@ -1567,6 +2011,17 @@ extension Compiler {\\n         self.skip_newlines()\\n         var returns = false\\n         while self.pos < self.signatures[index].body_end {\\n+            if self.current_result != ValueType.Void && self.expression_is_block_tail() && not self.value_statement_start() && (not self.at(TokenKind.If) || self.if_has_else()) {\\n+                let tail = try self.parse_expected_expr(self.current_result)\\n+                if tail.always_returns { returns = true }\\n+                else {\\n+                    let r = self.emit_at(Op.Return, self.signatures[index].line, self.signatures[index].col)\\n+                    self.code[r].a = tail.slot\\n+                    returns = true\\n+                }\\n+                self.skip_newlines()\\n+                break\\n+            }\\n             let r = try self.parse_statement()\\n             if not returns && r { returns = true }\\n             try self.terminator()\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex 0c06d633..161ede2b 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -129,6 +129,24 @@ RUN_CASES = (\\n     RunCase(\\\"enums/subject_once\\\", \\\"7\\\\n2\\\\n\\\", section=\\\"enums\\\"),\\n     RunCase(\\\"enums/all_arms_return\\\", \\\"20\\\\n40\\\\n60\\\\n\\\", section=\\\"enums\\\"),\\n     RunCase(\\\"enums/lexer_token_kind\\\", \\\"1\\\\n2\\\\n3\\\\ntrue\\\\n\\\", section=\\\"enums\\\"),\\n+    RunCase(\\\"values/scalar_bool_tails\\\", \\\"42\\\\ntrue\\\\n-128\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/if_nested\\\", \\\"11\\\\n12\\\\n21\\\\n22\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/else_if\\\", \\\"-1\\\\n0\\\\n1\\\\n2\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/match_arms\\\", \\\"1\\\\n3\\\\n11\\\\n22\\\\n33\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/record_enum_values\\\", \\\"7\\\\ntrue\\\\n9\\\\ntrue\\\\ntrue\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/contexts\\\", \\\"-7\\\\n5\\\\n-128\\\\n4294967295\\\\n65535\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/evaluation_order\\\", \\\"1\\\\n2\\\\n20\\\\n4\\\\n5\\\\n50\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/match_subject_once\\\", \\\"6\\\\n20\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/returns_and_scope\\\", \\\"10\\\\n20\\\\n30\\\\n40\\\\n3\\\\n9\\\\n50\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/u64_context\\\", \\\"18446744073709551615\\\\n9223372036854775808\\\\n255\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/lexer_hex_value\\\", \\\"0\\\\n9\\\\n10\\\\n15\\\\n10\\\\n15\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/inference_order_widening\\\", \\\"6\\\\n-3\\\\n-1\\\\n1\\\\n2\\\\n8\\\\n42\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/review_grid\\\", \\\"10\\\\n11\\\\n18446744073709551615\\\\n10\\\\n12\\\\n9223372036854775808\\\\n20\\\\n20\\\\n18446744073709551615\\\\n30\\\\n30\\\\n99\\\\n40\\\\n43\\\\n9223372036854775808\\\\n46\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/review_context\\\", \\\"true\\\\nfalse\\\\n2\\\\n1\\\\n3\\\\n5\\\\n3\\\\n40\\\\n7\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/review_composition\\\", \\\"true\\\\n9\\\\n8\\\\n11\\\\n255\\\\n1\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/statement_else_if\\\", \\\"2\\\\n3\\\\n5\\\\n6\\\\n7\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/value_block_void_then_tail\\\", \\\"10\\\\n42\\\\n\\\", section=\\\"values\\\"),\\n+    RunCase(\\\"values/parenthesized_argument_block\\\", \\\"0\\\\n1\\\\n8\\\\n42\\\\n\\\", section=\\\"values\\\"),\\n )\\n \\n REJECT_CASES = (\\n@@ -197,6 +215,20 @@ REJECT_CASES = (\\n     RejectCase(\\\"enums/reject_print\\\", \\\"cannot print an enum value\\\", \\\"enums\\\"),\\n     RejectCase(\\\"enums/reject_payload\\\", \\\"payload enum cases are not supported\\\", \\\"enums\\\"),\\n     RejectCase(\\\"enums/reject_arithmetic\\\", \\\"arithmetic requires integer operands\\\", \\\"enums\\\"),\\n+    RejectCase(\\\"values/reject_missing_else\\\", \\\"value if requires an else\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_missing_tail\\\", \\\"continuing value branch requires a tail expression\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_mismatched_branches\\\", \\\"control-flow branches have incompatible types\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_unselected_range\\\", \\\"integer literal does not fit\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_incomplete_match\\\", \\\"non-exhaustive match\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_non_tail_expression\\\", \\\"only Void function calls may be expression statements\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_arm_scope_leak\\\", \\\"unknown name\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_record_branches\\\", \\\"incompatible types\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_enum_branches\\\", \\\"incompatible types\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_match_missing_tail\\\", \\\"continuing value branch requires a tail expression\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_numeric_no_common\\\", \\\"incompatible types\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_all_returning_operand\\\", \\\"all-returning control flow cannot be used as an operand\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_deep_else_if\\\", \\\"nesting limit exceeded\\\", \\\"values\\\"),\\n+    RejectCase(\\\"values/reject_extra_call_argument\\\", \\\"too many arguments\\\", \\\"values\\\"),\\n )\\n \\n \\n@@ -281,7 +313,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -310,7 +342,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\") else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\n\",\"id\":\"SL-248\",\"request_id\":\"minivm-SL-248-894682e2\",\"title\":\"minivm M5: implicit tails + value-producing control flow (branch commit 894682e2)\"}","request_id":"minivm-SL-248-894682e2","sequence":450,"timestamp":"1789089636"}
---
