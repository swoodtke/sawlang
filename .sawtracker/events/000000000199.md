---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791292","description":"## Scope and status\n\nDesign 197 remainder: route the six parse_type bypasses through one parser\n\nThe qualified-slot bugs are explicitly closed. Census only rule 7’s six parse_type bypasses in design 197 unit 1, compare with the later self-hosted parser grammar-debt census, and migrate surviving paths with diagnostic parity.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Confirm the remaining scope and prerequisites against the source record and current tree; completed steps in the historical excerpt are context only.\n2. Carry out the named migration, documentation, coverage or implementation work in a bounded change, following the recorded plan and preserving excluded cases.\n3. Validate the source’s acceptance conditions, record the changed files and check results, and close only the completed remainder.\n\n## Acceptance criteria\n\n- [ ] Every in-scope item from the source has a disposition and supporting evidence.\n- [ ] Required tests/documentation agree with the final behavior; any remaining work is linked explicitly.\n\n## Existing design references\n\n- [sawlang/designs/150-rust-style-imports.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/150-rust-style-imports.md)\n- [sawlang/designs/197-declaration-position-names.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/197-declaration-position-names.md)\n- [sawlang/designs/229-export-control.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/229-export-control.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 6663–6705\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L6663-L6705)\n\n- **DF‑194a — CLOSED (Aug 17), design 197 unit 2's ruling implemented for the\n  three slots.** The module-qualifier walk is ONE function now,\n  `_resolve_qualified_symbol`, with two entry points its docstring names:\n  `_resolve_type` (every signature and expression position) and\n  `_resolve_declared_qualified_names` (the three raw slots, called from\n  `_register_struct`, `_register_enum` and `_register_type_definition`, each\n  writing the result back onto the AST so the symbol table and the AST go on\n  sharing one object). Both entries therefore get design 229's visibility\n  check, which is why the slots route to the walk rather than to a\n  canonicalizing name rewrite. The slot walk rewrites DOTTED names ONLY, so the\n  DF‑212b kind stamp — which knows the type PARAMETERS in scope, and must,\n  since `struct Holder<Cmd>` beside an `enum Cmd` means the parameter — still\n  settles the bare ones. The fourth face (a constructor's generic ARGUMENT) is\n  closed in `_check_struct_init`, and a FIFTH the fix exposed: an `as` CAST\n  target was read raw by codegen (`Undefined struct: dep.Point`), unreachable\n  before because the cast was refused one error earlier. Tests:\n  `examples/qualified_type_in_declaration_slot.saw` (pin flipped) and\n  `examples/qualified_type_declaration_slots.saw` (the matrix — both kinds,\n  every composite, a user module, the constructor argument). Docs: the spec's\n  \"every position\" list and the skill's gotcha note. Gated on the compiler\n  suite AND `sos_runner` both arches (the Aug-17 ruling: a compiler change can\n  break kernel codegen, which the suite does not cover). **STILL OPEN from\n  design 197: rule 7's six `parse_type` bypasses (unit 1's other half),\n  untouched here.**\n  As filed (SPEC/IMPL, Aug 10 by 194 u4): `struct Holder {\n  p: dep.Point }`, `case Full(p: dep.Point)` and `type Alias = dep.Point` each\n  keep the dotted spelling into type comparison, so `field `p` expects type\n  `dep.Point` but got `Point``. Those three slots are read straight off the AST\n  and never reach `_resolve_type`, the one place that walks a module qualifier —\n  the same three unit 4 had to wire the prelude gate into by hand, which is how\n  this surfaced. `_canonical_type_name` returns a dotted name unchanged ON\n  PURPOSE (\"for `_resolve_type`'s module-walk branch to handle\") and for these\n  slots that branch never runs. A fourth face: a constructor's generic ARGUMENT\n  (`Vector<data.Data>()` binds a local whose element type keeps the dot).\n  Pre-existing — reproduced identically on the unit-3 tree — and true for a USER\n  module as well as for std. Design 150 claims outright that \"a qualifier works\n  in EVERY position a name appears\", so this is a deviation from the spec, not a\n  gap in it. It MATTERS MORE after unit 4: the gate's hint offers `import\n  std.data` + `data.Data` as one of three ways to satisfy it, and that one does\n  not work in a field. NOT fixed here — the fix is about what a dotted name\n  canonicalizes to in a slot nothing resolves, which is a design-144 identity\n  question and wants a ruling rather than a patch mid-brief. PIN:\n  `examples/qualified_type_in_declaration_slot.saw` (XFAIL, cited).\n","id":"SL-199","labels":["todo-import","needs-verification","task","plan"],"priority":"normal","project":"SL","revision":1,"sequence":199,"status":"open","title":"Design 197 remainder: route the six parse_type bypasses through one parser","updated":"1788791292"},"before":null,"id":"SL-199","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDesign 197 remainder: route the six parse_type bypasses through one parser\\n\\nThe qualified-slot bugs are explicitly closed. Census only rule 7’s six parse_type bypasses in design 197 unit 1, compare with the later self-hosted parser grammar-debt census, and migrate surviving paths with diagnostic parity.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Confirm the remaining scope and prerequisites against the source record and current tree; completed steps in the historical excerpt are context only.\\n2. Carry out the named migration, documentation, coverage or implementation work in a bounded change, following the recorded plan and preserving excluded cases.\\n3. Validate the source’s acceptance conditions, record the changed files and check results, and close only the completed remainder.\\n\\n## Acceptance criteria\\n\\n- [ ] Every in-scope item from the source has a disposition and supporting evidence.\\n- [ ] Required tests/documentation agree with the final behavior; any remaining work is linked explicitly.\\n\\n## Existing design references\\n\\n- [sawlang/designs/150-rust-style-imports.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/150-rust-style-imports.md)\\n- [sawlang/designs/197-declaration-position-names.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/197-declaration-position-names.md)\\n- [sawlang/designs/229-export-control.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/229-export-control.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 6663–6705\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L6663-L6705)\\n\\n- **DF‑194a — CLOSED (Aug 17), design 197 unit 2's ruling implemented for the\\n  three slots.** The module-qualifier walk is ONE function now,\\n  `_resolve_qualified_symbol`, with two entry points its docstring names:\\n  `_resolve_type` (every signature and expression position) and\\n  `_resolve_declared_qualified_names` (the three raw slots, called from\\n  `_register_struct`, `_register_enum` and `_register_type_definition`, each\\n  writing the result back onto the AST so the symbol table and the AST go on\\n  sharing one object). Both entries therefore get design 229's visibility\\n  check, which is why the slots route to the walk rather than to a\\n  canonicalizing name rewrite. The slot walk rewrites DOTTED names ONLY, so the\\n  DF‑212b kind stamp — which knows the type PARAMETERS in scope, and must,\\n  since `struct Holder<Cmd>` beside an `enum Cmd` means the parameter — still\\n  settles the bare ones. The fourth face (a constructor's generic ARGUMENT) is\\n  closed in `_check_struct_init`, and a FIFTH the fix exposed: an `as` CAST\\n  target was read raw by codegen (`Undefined struct: dep.Point`), unreachable\\n  before because the cast was refused one error earlier. Tests:\\n  `examples/qualified_type_in_declaration_slot.saw` (pin flipped) and\\n  `examples/qualified_type_declaration_slots.saw` (the matrix — both kinds,\\n  every composite, a user module, the constructor argument). Docs: the spec's\\n  \\\"every position\\\" list and the skill's gotcha note. Gated on the compiler\\n  suite AND `sos_runner` both arches (the Aug-17 ruling: a compiler change can\\n  break kernel codegen, which the suite does not cover). **STILL OPEN from\\n  design 197: rule 7's six `parse_type` bypasses (unit 1's other half),\\n  untouched here.**\\n  As filed (SPEC/IMPL, Aug 10 by 194 u4): `struct Holder {\\n  p: dep.Point }`, `case Full(p: dep.Point)` and `type Alias = dep.Point` each\\n  keep the dotted spelling into type comparison, so `field `p` expects type\\n  `dep.Point` but got `Point``. Those three slots are read straight off the AST\\n  and never reach `_resolve_type`, the one place that walks a module qualifier —\\n  the same three unit 4 had to wire the prelude gate into by hand, which is how\\n  this surfaced. `_canonical_type_name` returns a dotted name unchanged ON\\n  PURPOSE (\\\"for `_resolve_type`'s module-walk branch to handle\\\") and for these\\n  slots that branch never runs. A fourth face: a constructor's generic ARGUMENT\\n  (`Vector<data.Data>()` binds a local whose element type keeps the dot).\\n  Pre-existing — reproduced identically on the unit-3 tree — and true for a USER\\n  module as well as for std. Design 150 claims outright that \\\"a qualifier works\\n  in EVERY position a name appears\\\", so this is a deviation from the spec, not a\\n  gap in it. It MATTERS MORE after unit 4: the gate's hint offers `import\\n  std.data` + `data.Data` as one of three ways to satisfy it, and that one does\\n  not work in a field. NOT fixed here — the fix is about what a dotted name\\n  canonicalizes to in a slot nothing resolves, which is a design-144 identity\\n  question and wants a ruling rather than a patch mid-brief. PIN:\\n  `examples/qualified_type_in_declaration_slot.saw` (XFAIL, cited).\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"task\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-e014cb4edf58d6c312bf-create\",\"title\":\"Design 197 remainder: route the six parse_type bypasses through one parser\"}","request_id":"todo-20260907-e014cb4edf58d6c312bf-create","sequence":199,"timestamp":"1788791292"}
---
