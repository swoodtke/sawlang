---
{"action":"comment","actor":"agent:claude-sl274","after":{"assignee":"","author":"agent:claude-sl280","comments":[{"author":"agent:claude-sl274","body":"FIXED on branch `sl274` (tip `a690608d`), riding SL-274's patch — NOT yet merged, so the status is the lead's to flip at merge.\n\n## The fix\n\nSL-274's ruling replaced design 249's whole-compilation census with an unconditional per-module tag, so this issue's mechanism is gone rather than patched: `typechecker/registration.py::_free_function_symbol_base` no longer asks which modules declare a name, it returns `<name>$m$<module tag>` for every free function. `free_function_owner_census`, the `free_function_owners` plumbing it fed, and `_stamp_module_private_functions` are deleted (std keeps a narrower per-FILE census of its own, for the reason recorded in the funnel's docstring).\n\nThe `main` exemption is the ENTRY module's and only the entry module's — `if name == \"main\" and not def_module` — so a dependency's `main` is an ordinary free function with an ordinary `main$m$<module>` symbol and internal linkage, and the entry's `main` is emitted by name with external linkage as the C entry.\n\n## The repro, as a test\n\n`examples/dependency_named_main_keeps_the_entry_point.saw` + `examples/modules/sl305dep/lib.saw`, the issue's own repro with one thing added: the dependency's `main` SUSPENDS (`yield_now()`), reached through its public `reach(n)`, while the entry module's `main` spawns a task of its own. That is the half a symbol-only rename would have got wrong — the two must name two coroutine frames, not collide on one — and the row measures that the suspension inside the dependency's `main` survives (`worker 2` / `dep main 8`).\n\nVerified to FAIL on main's compiler (`25dc9a17`, and again after the Sep-17 rebase), with the issue's own words:\n\n    Linking failed: Undefined symbols for architecture arm64:\n      \"_main\", referenced from:\n          <initial-undefines>\n\n## Conformance\n\n`examples/conformance/INDEX.md` row **K106** — \"a dependency declaring `main` leaves the entry module's entry point alone\" — naming that test, beside K105 for SL-274's own guarantee.\n\n## Gate at the tip\n\nSuite 2544 passed / 10 xfailed; freestanding 36/36 (riscv32 19 + arm64 17); terminal battery 27/27 stages green.\n","created":"1789703732","id":"c1"}],"created":"1789657851","description":"A dependency that declares the name `main` makes the ENTRY module's `main` take design 249's `$M$root` codegen tag, so the program emits no `main` symbol at all and the LINK fails. Pre-existing on main (`be4a764e`); found by the SL-280 keying audit, not caused by it.\n\n## Repro\n\n`.build/scratch/mainprobe/modules/mpdep/lib.saw`\n\n    import std.task.{yield_now}\n\n    func main(n: Int) -> Int {\n        yield_now()\n        n\n    }\n\n    public func reach(n: Int) -> Int { main(n) }\n\n`.build/scratch/mainprobe/entry.saw`\n\n    import std.task.{yield_now}\n    import mpdep.{reach}\n\n    func worker(n: Int) -> Int { yield_now()  n + 1 }\n\n    func main() {\n        var group = TaskGroup()\n        let h = group.spawn(worker(1))\n        print(h.join())\n        print(reach(7))\n    }\n\n    ./.venv/bin/python sawc/sawc.py .build/scratch/mainprobe/entry.saw \\\n        --module-path mpdep=.build/scratch/mainprobe/modules/mpdep -o .build/scratch/mainprobe/entry\n\n    Linking failed: Undefined symbols for architecture arm64:\n      \"_main\", referenced from:\n          <initial-undefines>\n\nReproduced identically with the SL-280 branch and with the three SL-280 files reverted to HEAD, so it is not the funnel.\n\n## Mechanism\n\n`_free_function_symbol_base` (`sawc/typechecker/registration.py:1328-1354`) gives a free function a `$M$<module>` tag whenever `free_function_owners` reports that more than one module of the compilation declares the name — deliberately including the ROOT module (`module_tag(())` is `root`), because std is checked once and cached so the entry is the side that has to move. The census counts `main` like any other name. Nothing exempts it, and `main` is the C entry emitted by name.\n\nThe dependency's `main` need not be public or reachable; declaring the name is enough.\n\n## Relation to SL-274\n\nSL-274's own sweep already names `main` as exempt from BOTH the mangling and the internalization, and records why the distinction matters: \"The codegen sites key on `func.name`, so a symbol-only rename is safe there — a `mangled_symbol` stamp is not.\" This issue is the same hole reachable today, through design 249 rather than through SL-274's widening — so the exemption SL-274 will add is the fix for both, and this is the case that says it is not merely prospective.\n\nSeverity is low in practice (a dependency has little reason to declare `main`) but the failure mode is a bare linker error naming no user construct.\n","id":"SL-305","labels":["bug","compiler"],"priority":"normal","project":"SL","revision":2,"sequence":946,"stage":"backlog","status":"open","title":"A dependency declaring the name `main` renames the entry module's main symbol — the link fails with no user-facing diagnostic","updated":"1789703732"},"before":{"assignee":"","author":"agent:claude-sl280","comments":[],"created":"1789657851","description":"A dependency that declares the name `main` makes the ENTRY module's `main` take design 249's `$M$root` codegen tag, so the program emits no `main` symbol at all and the LINK fails. Pre-existing on main (`be4a764e`); found by the SL-280 keying audit, not caused by it.\n\n## Repro\n\n`.build/scratch/mainprobe/modules/mpdep/lib.saw`\n\n    import std.task.{yield_now}\n\n    func main(n: Int) -> Int {\n        yield_now()\n        n\n    }\n\n    public func reach(n: Int) -> Int { main(n) }\n\n`.build/scratch/mainprobe/entry.saw`\n\n    import std.task.{yield_now}\n    import mpdep.{reach}\n\n    func worker(n: Int) -> Int { yield_now()  n + 1 }\n\n    func main() {\n        var group = TaskGroup()\n        let h = group.spawn(worker(1))\n        print(h.join())\n        print(reach(7))\n    }\n\n    ./.venv/bin/python sawc/sawc.py .build/scratch/mainprobe/entry.saw \\\n        --module-path mpdep=.build/scratch/mainprobe/modules/mpdep -o .build/scratch/mainprobe/entry\n\n    Linking failed: Undefined symbols for architecture arm64:\n      \"_main\", referenced from:\n          <initial-undefines>\n\nReproduced identically with the SL-280 branch and with the three SL-280 files reverted to HEAD, so it is not the funnel.\n\n## Mechanism\n\n`_free_function_symbol_base` (`sawc/typechecker/registration.py:1328-1354`) gives a free function a `$M$<module>` tag whenever `free_function_owners` reports that more than one module of the compilation declares the name — deliberately including the ROOT module (`module_tag(())` is `root`), because std is checked once and cached so the entry is the side that has to move. The census counts `main` like any other name. Nothing exempts it, and `main` is the C entry emitted by name.\n\nThe dependency's `main` need not be public or reachable; declaring the name is enough.\n\n## Relation to SL-274\n\nSL-274's own sweep already names `main` as exempt from BOTH the mangling and the internalization, and records why the distinction matters: \"The codegen sites key on `func.name`, so a symbol-only rename is safe there — a `mangled_symbol` stamp is not.\" This issue is the same hole reachable today, through design 249 rather than through SL-274's widening — so the exemption SL-274 will add is the fix for both, and this is the case that says it is not merely prospective.\n\nSeverity is low in practice (a dependency has little reason to declare `main`) but the failure mode is a bare linker error naming no user construct.\n","id":"SL-305","labels":["bug","compiler"],"priority":"normal","project":"SL","revision":1,"sequence":895,"stage":"backlog","status":"open","title":"A dependency declaring the name `main` renames the entry module's main symbol — the link fails with no user-facing diagnostic","updated":"1789657851"},"id":"SL-305","project":"SL","request":"{\"action\":\"comment\",\"actor\":\"agent:claude-sl274\",\"body\":\"FIXED on branch `sl274` (tip `a690608d`), riding SL-274's patch — NOT yet merged, so the status is the lead's to flip at merge.\\n\\n## The fix\\n\\nSL-274's ruling replaced design 249's whole-compilation census with an unconditional per-module tag, so this issue's mechanism is gone rather than patched: `typechecker/registration.py::_free_function_symbol_base` no longer asks which modules declare a name, it returns `<name>$m$<module tag>` for every free function. `free_function_owner_census`, the `free_function_owners` plumbing it fed, and `_stamp_module_private_functions` are deleted (std keeps a narrower per-FILE census of its own, for the reason recorded in the funnel's docstring).\\n\\nThe `main` exemption is the ENTRY module's and only the entry module's — `if name == \\\"main\\\" and not def_module` — so a dependency's `main` is an ordinary free function with an ordinary `main$m$<module>` symbol and internal linkage, and the entry's `main` is emitted by name with external linkage as the C entry.\\n\\n## The repro, as a test\\n\\n`examples/dependency_named_main_keeps_the_entry_point.saw` + `examples/modules/sl305dep/lib.saw`, the issue's own repro with one thing added: the dependency's `main` SUSPENDS (`yield_now()`), reached through its public `reach(n)`, while the entry module's `main` spawns a task of its own. That is the half a symbol-only rename would have got wrong — the two must name two coroutine frames, not collide on one — and the row measures that the suspension inside the dependency's `main` survives (`worker 2` / `dep main 8`).\\n\\nVerified to FAIL on main's compiler (`25dc9a17`, and again after the Sep-17 rebase), with the issue's own words:\\n\\n    Linking failed: Undefined symbols for architecture arm64:\\n      \\\"_main\\\", referenced from:\\n          <initial-undefines>\\n\\n## Conformance\\n\\n`examples/conformance/INDEX.md` row **K106** — \\\"a dependency declaring `main` leaves the entry module's entry point alone\\\" — naming that test, beside K105 for SL-274's own guarantee.\\n\\n## Gate at the tip\\n\\nSuite 2544 passed / 10 xfailed; freestanding 36/36 (riscv32 19 + arm64 17); terminal battery 27/27 stages green.\\n\",\"id\":\"SL-305\",\"request_id\":\"pid-86867-1789703732-15000\"}","request_id":"pid-86867-1789703732-15000","sequence":946,"timestamp":"1789703732"}
---
