---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"agent:claude-sl356","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 5 (rides with SL-356) in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188488","id":"c1"}],"created":"1790184459","description":"Found by the independent review of SL-350.p1 (SL-350's review comment, P2-1; Sep 23). Before SL-350, `_struct_init_info` looked field types up with `namespace.get_struct_fields(<mangled name>)`, which returns nothing for a monomorphized generic struct, so a generic struct's `Arc`/`String` field got NO retain at construction: `struct Cell<T> { value: T, tag: Int }` built as `Cell<Arc<Int>>(value: a, tag: 1)` printed strong counts 1 / 2 / 4554 and died with `over-release of an Arc (refcount underflow)` (main 73dd98b8); the `String` twin aborted with `over-release of a String reference`. SL-350 resolves the type through `_struct_field_saw_type` (base name + `mono_struct_args`, substituted), which fixes it (1 / 3 / 1) — but nothing PINS it, so a refactor of the field-type source could silently restore the bug. OWED: an `examples/` pin with the Arc and String cells (strong counts + deinit traces), plus the sibling positions the mechanism reaches (a generic ENUM payload, a nested generic literal, a generic struct built in a driven body). Small; ride the next codegen patch (SL-356's, say).","id":"SL-362","labels":["bug","codegen","generics","pin"],"order":0,"parent":"SL-367","priority":"normal","project":"SL","queue_order":0,"revision":4,"sequence":1315,"stage":"queued","status":"open","title":"Pin the generic-struct field retain SL-350 fixed as a side effect: Cell<Arc<Int>>(value: a, tag: 1) over-released on main (strong count 1/2/4554 then panic) because the field-type lookup answered nothing for a monomorphized key","updated":"1790188548"},"before":{"acceptance":[],"assignee":"agent:claude-sl356","author":"agent:claude-sawlang","closed":"","comments":[{"author":"agent:claude-sawlang","body":"MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 5 (rides with SL-356) in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.","created":"1790188488","id":"c1"}],"created":"1790184459","description":"Found by the independent review of SL-350.p1 (SL-350's review comment, P2-1; Sep 23). Before SL-350, `_struct_init_info` looked field types up with `namespace.get_struct_fields(<mangled name>)`, which returns nothing for a monomorphized generic struct, so a generic struct's `Arc`/`String` field got NO retain at construction: `struct Cell<T> { value: T, tag: Int }` built as `Cell<Arc<Int>>(value: a, tag: 1)` printed strong counts 1 / 2 / 4554 and died with `over-release of an Arc (refcount underflow)` (main 73dd98b8); the `String` twin aborted with `over-release of a String reference`. SL-350 resolves the type through `_struct_field_saw_type` (base name + `mono_struct_args`, substituted), which fixes it (1 / 3 / 1) — but nothing PINS it, so a refactor of the field-type source could silently restore the bug. OWED: an `examples/` pin with the Arc and String cells (strong counts + deinit traces), plus the sibling positions the mechanism reaches (a generic ENUM payload, a nested generic literal, a generic struct built in a driven body). Small; ride the next codegen patch (SL-356's, say).","id":"SL-362","labels":["bug","codegen","generics","pin"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":3,"sequence":1305,"stage":"queued","status":"open","title":"Pin the generic-struct field retain SL-350 fixed as a side effect: Cell<Arc<Int>>(value: a, tag: 1) over-released on main (strong count 1/2/4554 then panic) because the field-type lookup answered nothing for a monomorphized key","updated":"1790188488"},"id":"SL-362","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-362\",\"parent\":\"SL-367\",\"request_id\":\"pid-71449-1790188548-9000\",\"revision\":3}","request_id":"pid-71449-1790188548-9000","sequence":1315,"timestamp":"1790188548"}
---
