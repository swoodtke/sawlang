---
{"action":"create","actor":"agent:claude-sl2-u0","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl2-u0","closed":"","comments":[{"author":"agent:claude-sl2-u0","body":"The parser's nesting budget is contract (design 259 R4: 256 levels, one named\nconstant, cited by the diagnostic). Every pass BEHIND the parser has a budget\ntoo, but an accidental one: each walks the AST recursively, and its ceiling is\nPython's default recursion limit divided by that pass's per-node frame cost. So\na program the parser accepts can still die downstream, and the depth at which\nit dies depends on which node shape it is made of.\n\nMeasured on the SL-2 U0′ branch (parser budget 256, refusal at 257), bisecting\nthe deepest source that compiles end to end:\n\n    groups        `(((…1…)))`                       256   compiles\n    unary         `---…-1`                          256   compiles\n    mixed         groups wrapping nested ifs        256   compiles\n    calls         `identity(identity(…1…))`         245   ICE at 246\n    nested ifs    `if true { if true { … } }`       196   ICE at 197\n    else-if chain `if false { } else if … { }`      196   ICE at 197\n\nThe failure is a CLEAN internal compiler error (`maximum recursion depth\nexceeded`, with the node kind and a source position), not a traceback or a\nsegfault — the downstream funnels do their job. What is wrong is the NUMBER:\nthe compiler refuses a program its own stated grammar limit accepts, and says\n`internal compiler error` while doing it.\n\nMECHANISM (obligation 4): one mechanism, not three symptoms — every AST-walking\npass inherits the interpreter's default recursion limit, which no pass states\nor owns. The parser is the only walker with a budget of its own; it now raises\nthe interpreter limit for the span of the parse and restores it afterwards, so\nthe passes that run next are back on the default. The siblings this reaches are\nevery pass that recurses over the tree: the typechecker, `coro_transform`,\nmonomorphization, codegen and `ast_dump`. Which one trips first depends on the\nnode, which is why `calls` and `if` chains die at different depths.\n\nSHAPE OF A FIX (not this unit's — U0′ owns `sawc/parser/` only): either the\ncompiler holds ONE recursion budget across all passes, sized from the parser's\n256 the way the parser sizes its own, or the nesting limit drops to a number\nevery pass clears. The first keeps 256 as the single contract; the second makes\nthe ruled number smaller than the prototype's, which design 259 §0 freezes, so\nit is a ruling rather than a fix.\n\nPINS: `examples/nesting_limit_accepts_the_budget.saw` carries the three shapes\nthat do compile at 256. `examples/nesting_limit_accepts_deep_calls_and_ifs.saw`\ncarries the three that do not, as an XFAIL citing this issue, with the intended\nbehavior spelled out — so the fix flips it to XPASS.\n","created":"1790195881","id":"c1"}],"created":"1790195881","description":"","id":"SL-369","labels":[],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1357,"stage":"backlog","status":"open","title":"Passes behind the parser ICE below the ruled nesting limit of 256","updated":"1790195881"},"before":null,"id":"SL-369","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:claude-sl2-u0\",\"body\":\"The parser's nesting budget is contract (design 259 R4: 256 levels, one named\\nconstant, cited by the diagnostic). Every pass BEHIND the parser has a budget\\ntoo, but an accidental one: each walks the AST recursively, and its ceiling is\\nPython's default recursion limit divided by that pass's per-node frame cost. So\\na program the parser accepts can still die downstream, and the depth at which\\nit dies depends on which node shape it is made of.\\n\\nMeasured on the SL-2 U0′ branch (parser budget 256, refusal at 257), bisecting\\nthe deepest source that compiles end to end:\\n\\n    groups        `(((…1…)))`                       256   compiles\\n    unary         `---…-1`                          256   compiles\\n    mixed         groups wrapping nested ifs        256   compiles\\n    calls         `identity(identity(…1…))`         245   ICE at 246\\n    nested ifs    `if true { if true { … } }`       196   ICE at 197\\n    else-if chain `if false { } else if … { }`      196   ICE at 197\\n\\nThe failure is a CLEAN internal compiler error (`maximum recursion depth\\nexceeded`, with the node kind and a source position), not a traceback or a\\nsegfault — the downstream funnels do their job. What is wrong is the NUMBER:\\nthe compiler refuses a program its own stated grammar limit accepts, and says\\n`internal compiler error` while doing it.\\n\\nMECHANISM (obligation 4): one mechanism, not three symptoms — every AST-walking\\npass inherits the interpreter's default recursion limit, which no pass states\\nor owns. The parser is the only walker with a budget of its own; it now raises\\nthe interpreter limit for the span of the parse and restores it afterwards, so\\nthe passes that run next are back on the default. The siblings this reaches are\\nevery pass that recurses over the tree: the typechecker, `coro_transform`,\\nmonomorphization, codegen and `ast_dump`. Which one trips first depends on the\\nnode, which is why `calls` and `if` chains die at different depths.\\n\\nSHAPE OF A FIX (not this unit's — U0′ owns `sawc/parser/` only): either the\\ncompiler holds ONE recursion budget across all passes, sized from the parser's\\n256 the way the parser sizes its own, or the nesting limit drops to a number\\nevery pass clears. The first keeps 256 as the single contract; the second makes\\nthe ruled number smaller than the prototype's, which design 259 §0 freezes, so\\nit is a ruling rather than a fix.\\n\\nPINS: `examples/nesting_limit_accepts_the_budget.saw` carries the three shapes\\nthat do compile at 256. `examples/nesting_limit_accepts_deep_calls_and_ifs.saw`\\ncarries the three that do not, as an XFAIL citing this issue, with the intended\\nbehavior spelled out — so the fix flips it to XPASS.\\n\",\"project\":\"SL\",\"request_id\":\"pid-3014-1790195881-6000\",\"title\":\"Passes behind the parser ICE below the ruled nesting limit of 256\"}","request_id":"pid-3014-1790195881-6000","sequence":1357,"timestamp":"1790195881"}
---
