---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:codex","closed":"","comments":[],"created":"1789477110","description":"SL-260's rejection corpus found Python sawc accepting a value-returning function with a reachable fallthrough path:\n\n```saw\nfunc f(x: Int32) -> Int32 { if x > 0 { return x } }\nfunc main() { print(f(0)) }\n```\n\nActual: compile succeeds and the uncovered path prints a fabricated 0. Existing fixture prototypes/minivm/examples/reject_missing_return.saw calls f(1), prints 1, and exposes the same incorrect acceptance. Expected: a located missing-return diagnostic; an Int32 result must exist on every continuing path. The prototype correctly rejects the function.\n\nMechanism: function checking uses a global found_return_with_value fact; any return anywhere can suppress a no-value diagnostic when the block has no tail. That fact is not proof that every reachable path returns. Track control-flow outcomes at joins, preserving valid all-returning branches, divergence and implicit tails; cover if/else, loops, match, methods and nested early returns. This is distinct from the dead-statements-after-unconditional-return over-rejection filed alongside it.\n","id":"SL-298","labels":["frozen","hazard"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":2,"sequence":2157,"stage":"backlog","status":"open","title":"sawc: conditional return permits a reachable value-less function exit","updated":"1790341671"},"before":{"acceptance":[],"assignee":"","author":"agent:codex","closed":"","comments":[],"created":"1789477110","description":"SL-260's rejection corpus found Python sawc accepting a value-returning function with a reachable fallthrough path:\n\n```saw\nfunc f(x: Int32) -> Int32 { if x > 0 { return x } }\nfunc main() { print(f(0)) }\n```\n\nActual: compile succeeds and the uncovered path prints a fabricated 0. Existing fixture prototypes/minivm/examples/reject_missing_return.saw calls f(1), prints 1, and exposes the same incorrect acceptance. Expected: a located missing-return diagnostic; an Int32 result must exist on every continuing path. The prototype correctly rejects the function.\n\nMechanism: function checking uses a global found_return_with_value fact; any return anywhere can suppress a no-value diagnostic when the block has no tail. That fact is not proof that every reachable path returns. Track control-flow outcomes at joins, preserving valid all-returning branches, divergence and implicit tails; cover if/else, loops, match, methods and nested early returns. This is distinct from the dead-statements-after-unconditional-return over-rejection filed alongside it.\n","id":"SL-298","labels":["bug","typechecking"],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":1,"sequence":856,"stage":"backlog","status":"open","title":"sawc: conditional return permits a reachable value-less function exit","updated":"1789477110"},"id":"SL-298","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"id\":\"SL-298\",\"labels\":[\"frozen\",\"hazard\"],\"request_id\":\"pid-56266-1790341671-7000\",\"revision\":1}","request_id":"pid-56266-1790341671-7000","sequence":2157,"timestamp":"1790341671"}
---
