---
{"action":"edit","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790269823","description":"Found by the SL-340 implementer (Sep 24) in a driven body. The lead narrowed the scope: it is NOT specific to the driven lowering. Reproduces on main (e62b3164).\n\nMember access on a value whose type is a distinct ALIAS over a struct is silently typed `Void`. The sync program below reports `undefined variable n`, because the `let` initializer is Void-typed and binds nothing:\n\n```saw\nstruct Noisy { name: String }\nextension Noisy: NoCopy {}\ntype Tag = Noisy\n\nfunc work() {\n    let t = Tag(Noisy(name: \"four\"))\n    print(\"{}\", t.name)\n    let n = t.name.len()     // typed Void: `n` is then \"undefined\"\n    print(\"{}\", n)\n}\n\nfunc main() { work() }\n```\n\nAs a function tail (`func work() -> Int { … t.name.len() }`) the refusal is \"should return `Int` but body has no value\", in both the sync and the driven lowering. The implementer's driven variant showed the window typed `(&Tag) -> Void` and then \"`Tag` is not Printable\".\n\nThis is almost certainly the same mechanism as SL-383, where an enum payload of an alias type is Void in a match arm. Name the mechanism (obligation 4) and fix it once. Expected behaviour is one of two:\n- member access through a distinct alias resolves the underlying struct's members, which is what \"an alias IS its underlying\" (design 63) suggests;\n- or it is a clean diagnostic that names the alias.\nIt is never a silent `Void`, which violates \"never hide errors\".\n\nSweep the positions:\n- field read, method call and field write;\n- optional chaining through an alias;\n- payload binding (SL-383), `if let` and tuple destructuring;\n- the driven twin of each.\n","id":"SL-384","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":2,"sequence":1460,"stage":"backlog","status":"open","title":"Member access through a distinct alias over a struct is silently typed Void (let n = t.name.len() binds nothing; sync and driven)","updated":"1790269853"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1790269823","description":"Found by the SL-340 implementer (Sep 24), while building K175. Reproduces on main.\n\nReading a field of an ALIAS-typed local that is held across a suspension fails to compile in the driven lowering. The borrow window the coroutine transform builds for the read is typed `(&Tag) -> Void`, and the print then reports \"`Tag` is not Printable\". The sync twin reads the field fine.\n\nShape (the implementer's cell; a minimal version):\n\n```saw\nimport std.task.{yield_now}\nstruct Noisy { name: String }\nextension Noisy: NoCopy {}\ntype Tag = Noisy\n\nfunc work() -> Int {\n    let t = Tag(Noisy(name: \"four\"))\n    yield_now()\n    print(\"{}\", t.name)\n    t.name.len()\n}\n\nfunc main() { print(\"{}\", __saw_drive(work())) }\n```\n\nExpected output: `four`, then `4`. SL-340's K175 only counts drops for its alias cells because of this.\n\nProbably the same mechanism as the alias-payload `match` finding filed beside it: a position that resolves the alias's underlying struct for member access, reached through the transform's synthesized window. Confirm, and sweep the positions the transform synthesizes (member reads, method calls, and borrow windows over alias-typed frame fields).\n","id":"SL-384","labels":[],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":1,"sequence":1459,"stage":"backlog","status":"open","title":"A field read on an alias-typed local held across a suspension fails in the driven lowering (window typed (&Tag) -> Void; 'Tag is not Printable')","updated":"1790269823"},"id":"SL-384","project":"SL","request":"{\"action\":\"edit\",\"actor\":\"agent:claude-sawlang\",\"description\":\"Found by the SL-340 implementer (Sep 24) in a driven body. The lead narrowed the scope: it is NOT specific to the driven lowering. Reproduces on main (e62b3164).\\n\\nMember access on a value whose type is a distinct ALIAS over a struct is silently typed `Void`. The sync program below reports `undefined variable n`, because the `let` initializer is Void-typed and binds nothing:\\n\\n```saw\\nstruct Noisy { name: String }\\nextension Noisy: NoCopy {}\\ntype Tag = Noisy\\n\\nfunc work() {\\n    let t = Tag(Noisy(name: \\\"four\\\"))\\n    print(\\\"{}\\\", t.name)\\n    let n = t.name.len()     // typed Void: `n` is then \\\"undefined\\\"\\n    print(\\\"{}\\\", n)\\n}\\n\\nfunc main() { work() }\\n```\\n\\nAs a function tail (`func work() -> Int { … t.name.len() }`) the refusal is \\\"should return `Int` but body has no value\\\", in both the sync and the driven lowering. The implementer's driven variant showed the window typed `(&Tag) -> Void` and then \\\"`Tag` is not Printable\\\".\\n\\nThis is almost certainly the same mechanism as SL-383, where an enum payload of an alias type is Void in a match arm. Name the mechanism (obligation 4) and fix it once. Expected behaviour is one of two:\\n- member access through a distinct alias resolves the underlying struct's members, which is what \\\"an alias IS its underlying\\\" (design 63) suggests;\\n- or it is a clean diagnostic that names the alias.\\nIt is never a silent `Void`, which violates \\\"never hide errors\\\".\\n\\nSweep the positions:\\n- field read, method call and field write;\\n- optional chaining through an alias;\\n- payload binding (SL-383), `if let` and tuple destructuring;\\n- the driven twin of each.\\n\",\"id\":\"SL-384\",\"priority\":\"high\",\"request_id\":\"pid-50052-1790269853-5000\",\"revision\":1,\"title\":\"Member access through a distinct alias over a struct is silently typed Void (let n = t.name.len() binds nothing; sync and driven)\"}","request_id":"pid-50052-1790269853-5000","sequence":1460,"timestamp":"1790269853"}
---
