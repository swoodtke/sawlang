---
{"action":"patch_test_result","actor":"agent:sawtracker-tests","after":{"assignee":"","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sl210","body":"UNIT A COMPLETE on branch `worktree-agent-a33ce7a9e83d0e6b5`. Not submitted —\nthe lead squashes and submits. Behaviour-preserving: same programs accepted,\nsame programs rejected, same diagnostics, same annotations.\n\n## Step 2 — the inventory\n\n`designs/267-ownership-boundary-inventory.md` (design number PROVISIONAL,\nstated as such in the doc). It keeps the epic's two questions apart — WHERE a\nvalue acquires a new owner, and HOW the expression produces it — and maps\nevery boundary and producer to its implementation site and its conformance\nrows:\n\n- the 19 entry points of `_check_value_transfer`, taken mechanically from the\n  AST (41 external call sites plus the self-recursion), grouped as bindings /\n  assignment / arguments / returns and tails / aggregate elements / captures /\n  loop results / owning operands;\n- the rule table (source -> transfer into an owned destination), with\n  partial-move and `NoMove` restrictions recorded as preserved: a projection\n  stays a projection in the record, so reaching the common checker does not\n  make it movable;\n- THE THREE FUNNELS ownership is actually split across today —\n  `_check_value_transfer`, `_check_payload_read` (design 131's payload rule),\n  and `place_uses._value_read_ok` (design 146). They share the tier oracle and\n  guard against each other firing twice on one read, but three funnels is\n  three places for a position to go missing, and only one of them now records\n  a decision. That is the structural finding of step 2 and the thing unit E's\n  verifier has to reconcile;\n- the capture boundary's three modes (`[move x]` -> `_check_move_expr`,\n  `[copy x]` -> its own inline test, plain `[x]` -> the checkpoint);\n- a producer census over all 45 `Expression` subclasses, which is what makes\n  the \"missing\" list below auditable rather than anecdotal;\n- a stated scope limit: std's own bodies are checked once under the builtin\n  typechecker and cached, so the ledger covers the entry module, the user\n  modules and every instance materialized into them — not std's authored\n  bodies.\n\n## Step 3 — the structured decision\n\n`sawc/typechecker/ownership.py`. `_check_value_transfer` now returns a\n`TransferDecision` on EVERY exit path, carrying the five things this issue\nasks for: the source by STABLE IDENTITY (`VariableInfo.binding_id` plus a\nprojection path — never the spelling), the destination and diagnostic site,\nthe action, the required cleanup-state change, and any generic copy\nrequirement awaiting specialization. Eight actions, each earned by an arm the\nfunction demonstrably has: `take` / `copy` / `move` / `none` from the issue's\nrule table, plus `borrow`, `refused`, `delegated` (a value branch, whose arm\nkeys it carries) and `deferred` (design 218c's provenance skips and design\n219's abstract tier).\n\nAttached to the TRANSFER OCCURRENCE, not the expression node: the ledger key\nis `(source node_id, destination context, line, column)` and the node id is in\nit only to NAME which occurrence. Living outside the AST is what makes it\ncorrect — `dataclasses.fields()` cannot see it (so the design-126 contract has\nnothing to declare and `substitute_ast_types` nothing to walk), a\nmonomorphized clone gets fresh node ids and therefore its own decisions, and\nthe table is per-compile so `reemit`/`irdet` byte-identity is untouched.\nAssign-never-accumulate per design 218 stage 1, with a `revision` counter.\n\n\"Checked and needs no copy\" is now distinguishable from \"never checked\" by the\nPRESENCE of an entry, and that property is enforced statically:\n`tools/test_transfer_decisions.py` parses the checkpoint and fails on a bare\n`return`, a `return` of anything that is not a recorded decision, or a\nfunction that can fall off its end — then checks a ten-fixture boundary\nmatrix. Wired as the `transferdecisions` battery stage. Acceptance's \"INCLUDING\ntrivial copies\" is met: the `free`-tier fall-through that used to leave no\ntrace is now an explicit `copy` record, and the fresh-temporary path an\nexplicit `take`.\n\nCost: +2.5% on a small compile (1.752s -> 1.796s, 9 samples each), flat on a\nlarge one.\n\n## Findings — three unchecked boundaries, filed and NOT fixed\n\nEach changes which programs compile, so fixing one here would have broken this\nunit's behaviour-preserving contract. All measured with compile-and-run\nevidence; evidence lives in the issues.\n\n- **SL-218** — a `self` receiver read is not an aliasing expression, so every\n  by-value transfer of `self` is judged a fresh temporary. Double free at exit\n  0 across six faces (return, argument, `let`, struct field, tuple element,\n  `&var self`); SIGABRT on ExplicitCopy; CORRECT on `Copy` — because codegen's\n  `_transfer_needs_copy` carries `SelfExpr` in a list of its own and takes the\n  retain there. That asymmetry is SL-209's thesis in one program: a second\n  opinion can mint a missing retain, it cannot deliver a missing refusal.\n- **SL-219** — `try` / `try!` / `try?` / `try ... catch` over a BINDING or a\n  FIELD forwards its subject unjudged. One value, three `drop` lines, exit 0.\n- **SL-220** — the OPPOSITE error at the same boundary, found writing SL-219's\n  control row: a payload extracted from a fresh temporary inline is never\n  released (`(try! f()).x` and `f()!.x` both leak; the bound spelling is\n  correct).\n\nMECHANISM (obligation 4): SL-218 and SL-219 are ONE mechanism in two\nsub-classes — `_is_aliasing_expr` is a node-type test over four classes plus\ntwo hand-added transparencies, so a node that IS a read and is not in the set\n(`SelfExpr`) and a node that FORWARDS a read and is not transparent\n(`TryExpr`) both become fresh temporaries at every tier at once. DF-216a and\nDF-299a are the same mechanism's earlier members; the one remaining known\nmember is the auto-wrap family, already filed as SL-79 and owned by SL-211.\nSL-220 is the cleanup-registration side and is hypothesised to belong with\nSL-213.\n\nObligation 3 met: conformance rows V86 (SL-218) and V87 (SL-219) were written\nbefore the refactor, both XFAIL citing their issue, with the INDEX rows and\nheader paragraph. SL-220 gets no pin — the program exits 0 with correct\noutput, so it needs an oracle a `deinit`-counting example cannot supply; that\nis the issue's own first task.\n\n## Gates\n\n- Suite: 2431 passed, 10 xfailed, 0 failed (the two new xfails are V86/V87).\n- Freestanding: 36 passed across riscv32 + arm64.\n- TERMINAL BATTERY: 25 stages GREEN in 2617s, including `irdet --all` (whole\n  corpus), `reemit`, `gmgate`, `bootstrap` and `freestanding`.\n- Rebased onto origin/main before the battery; clean, no conflicts.\n\n## Also on the branch\n\nPer a lead ruling of Sep 9, `tools/check_citations.py`'s recognizer was\nwidened to accept SL ids alongside DF ids, so the new id scheme is\ngate-verified rather than conventional. Details in the branch's final commit.\n\n## Not done here, by design\n\nFixing SL-218/219/220; touching `_transfer_needs_copy` (SL-213 now has\nexplicit decisions to migrate onto); giving the payload-read and place funnels\ndecisions of their own (SL-211/SL-214); the verifier (SL-214 — the ledger is\nwhat it will verify).\n","created":"1788986712","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-210.p1 (revision 1): ownership unit A: boundary inventory + structured transfer decisions; SL-id citations gate-verified (SL-209 epic)","created":"1788986970","id":"c2"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-210.p1 (revision 1)","created":"1788987385","id":"c3"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-210.p1 (revision 1)\n\nReview of SL-210.p1 revision 1 (applied commit 52390666, base b53a844c): requesting changes for two defects in the new ownership records. These concern Unit A's own record contract, not the separately filed pre-existing language defects in SL-218/219/220.\n\n1. [P2] Capture the resolved source binding before its scope is popped.\n\nLocation: sawc/typechecker/ownership.py:391-395 (_transfer_binding_id).\n\nThe ledger obtains a supposedly stable source identity by looking up the expression's spelling in current_scope at the time the boundary is recorded. That scope is not necessarily the expression's original scope. Body tails are checked after their body scope has been popped, and the value-branch recursion likewise visits branch results after their inner scopes have gone away. This loses the ID for a local tail return and, more seriously, resolves a shadowed branch result to a DIFFERENT binding.\n\nTested example:\n\n```saw\nfunc choose(flag: Bool) -> Int {\n    let x = 7\n    let y = if flag {\n        let x = x + 1\n        let z = x\n        print(z)\n        x\n    } else { x }\n    y\n}\nfunc main() { print(choose(true)) }\n```\n\nCollected the new transfer decisions through tools/test_transfer_decisions.py's compile_and_collect. In one checking pass, the `let z = x` transfer correctly used inner binding_id 3811. The immediately following branch-tail `x` used outer binding_id 3810, identical to the else-arm `x`. The absolute counters vary per compile; the incorrect equality is stable. The function-tail `y` had binding_id=None. A separate NoCopy `move x` tail likewise produced action=move / cleanup=retire-source / binding_id=None.\n\nThis prevents a later consumer from reliably selecting which source to retain or retire, directly violating SL-210's stable-identity requirement. Preserve the resolved binding identity while checking the expression, or pass the owning scope/identity explicitly to the boundary record; do not re-resolve a spelling in the later ambient scope. Add assertions for the actual IDs in tail-return and shadowed value-branch fixtures, not only source.kind.\n\n2. [P2] Record diverging sources as no-value rather than adopted temporaries.\n\nLocation: sawc/typechecker/types.py:4741-4748.\n\nThe non-aliasing branch classifies every non-aliasing expression as ACTION_TAKE / CLEANUP_ADOPT_TEMPORARY without first excluding Never. A valid binding whose initializer diverges therefore records an ownership acquisition that can never occur:\n\n```saw\nfunc main() {\n    let x: Int = panic(\"stop\")\n    print(x)\n}\n```\n\nCompilation succeeds. Its `let binding` decision is source_type=Never, action=take, cleanup=adopt-temporary. SL-210 explicitly requires a diverging expression to have no value and no transfer; ownership.py's ACTION_NONE documents that rule too. Record ACTION_NONE / CLEANUP_NONE for the diverging case before the fresh-temporary classification, while retaining an explicit ledger entry. Add a fixture checking Never's action and cleanup together.\n\nValidation: isolated worktree at the patch's exact base; custom compile-and-collect probes for local tails, NoCopy move tails, shadowed branch results, and Never initializers. The new transfer-decision gate passes (10 fixtures); the citation gate also passes. The tracker reports 2431 compiler tests passed, 10 expected failures, and all 36 freestanding tests passed for 52390666. Full server gates were not repeated locally. These findings affect the new metadata rather than asserting a change in generated program behavior.\n","created":"1788989574","id":"c4"},{"author":"agent:claude-sawlang","body":"Revised patch SL-210.p1 to revision 2","created":"1788996758","id":"c5"},{"author":"agent:sawtracker-tests","body":"Tests failed for SL-210.p1 (revision 2):\n\n```\n[2439/2449] \u001B[92m✓\u001B[0m while_nested\n[2440/2449] \u001B[92m✓\u001B[0m while_let_channel_drain\n[2441/2449] \u001B[92m✓\u001B[0m while_never_diverges\n[2442/2449] \u001B[92m✓\u001B[0m while_never_break_forms\n[2443/2449] \u001B[92m✓\u001B[0m while_let_suspending_mt\n[2444/2449] \u001B[92m✓\u001B[0m while_simple\n[2445/2449] \u001B[92m✓\u001B[0m while_let_suspending_try_catch\n[2446/2449] \u001B[92m✓\u001B[0m while_let_suspending\n[2447/2449] \u001B[92m✓\u001B[0m wrapping_and_reference_mix\n[2448/2449] \u001B[92m✓\u001B[0m wrapping_operators\n[2449/2449] \u001B[92m✓\u001B[0m while_never_sync_context\n\ncompile 362.3s + 4.6s draining = 366.8s total\n\n======================================================================\n\u001B[1mTest Results\u001B[0m\n======================================================================\n\n\u001B[93m\u001B[1mKNOWN FAILURES (xfail):\u001B[0m\n  \u001B[93mx\u001B[0m V86_self_receiver_read_is_a_transfer: SL-218 — `_is_aliasing_expr` is a node-type test over four classes\n  \u001B[93mx\u001B[0m V87_try_subject_read_is_a_transfer: SL-219 — the sibling of V86 (SL-218) and the same mechanism read the\n  \u001B[93mx\u001B[0m closure_escaping_move_capture_consumed_once: DF-255a an escaping closure's env destructor releases a capture the\n  \u001B[93mx\u001B[0m closure_nested_ref_param_capture: DF-248b a nested closure captures an enclosing closure's reference\n  \u001B[93mx\u001B[0m closure_shorthand_parameter_inside_a_try: DF-257d the `$0` shorthand is invisible to the implicit-parameter scan\n  \u001B[93mx\u001B[0m coro_suspending_call_in_inline_catch: SL-215 the inline `try EXPR catch { }` catch block does not CFG-split,\n  \u001B[93mx\u001B[0m funcpointer_call_in_a_driven_body: DF-252a a `FuncPointer` value called by NAME inside a driven body is\n  \u001B[93mx\u001B[0m generic_init_moved_parameter_is_released_once: DF-302b — a generic extension's `init` releases a parameter it MOVED\n  \u001B[93mx\u001B[0m primitive_type_called_as_a_function: DF-270e — a PRIMITIVE TYPE NAME written as a ZERO-ARGUMENT call is an\n  \u001B[93mx\u001B[0m trailing_closure_inside_a_try_operand: DF-259c — a trailing closure is not recognized inside a `try`/`try!`/\n  \u001B[93mx\u001B[0m wrapped_return_takes_the_transfer_checkpoint: DF-305a — the checkpoint runs AFTER the auto-wrap, so it judges the\n\n======================================================================\n\u001B[92m\u001B[1mALL TESTS PASSED\u001B[0m (2438 passed, 11 xfailed)\n======================================================================\n\nPublished test_runner_1788996761_52707 as test_runner_last (1496 manifest entries).\nFreestanding QEMU tests\n  clang: /opt/homebrew/opt/llvm/bin/clang\n  lld  : /opt/homebrew/bin/ld.lld\n  qemu : /opt/homebrew/bin/qemu-system-riscv32\n  qemu : /opt/homebrew/bin/qemu-system-aarch64\n\nbuilding blade\n\nriscv32  (riscv32-unknown-none-elf, qemu-system-riscv32 `virt`)\n[1/19] ✗ hello\n    QEMU hung (> 20s) — no clean exit\n[2/19] ✓ int_widths\n[3/19] ✓ struct_layout\n[4/19] ✓ call_convention\n[5/19] ✓ module_compose\n[6/19] ✓ runtime_seams\n[7/19] ✓ runtime_panic\n[8/19] ✓ runtime_export_ungated\n[9/19] ✓ runtime_seam_signature\n[10/19] ✓ hidden_alloc_interpolation\n[11/19] ✓ hidden_alloc_escaping_closure\n[12/19] ✓ hidden_alloc_print_printable\n[13/19] ✓ format_no_alloc\n[14/19] ✓ align_attribute\n[15/19] ✓ export_with_coroutine\n[16/19] ✓ float_text\n[17/19] ✓ link_address\n[18/19] ✓ blade_target\n[19/19] ✓ wide_value_rendering\n\narm64  (aarch64-unknown-none-elf, qemu-system-aarch64 `virt`)\n[1/17] ✓ hello\n[2/17] ✓ int_widths\n[3/17] ✓ struct_layout\n[4/17] ✓ call_convention\n[5/17] ✓ module_compose\n[6/17] ✓ runtime_seams\n[7/17] ✓ runtime_panic\n[8/17] ✓ runtime_export_ungated\n[9/17] ✓ runtime_seam_signature\n[10/17] ✓ hidden_alloc_interpolation\n[11/17] ✓ hidden_alloc_escaping_closure\n[12/17] ✓ hidden_alloc_print_printable\n[13/17] ✓ format_no_alloc\n[14/17] ✓ export_with_coroutine\n[15/17] ✓ float_text\n[16/17] ✓ link_address\n[17/17] ✓ blade_target\n\n============================================================\nFREESTANDING TESTS FAILED (35 passed, 1 failed)\n============================================================\n\n```","created":"1789005425","id":"c6"}],"created":"1788966177","description":"Unit A of SL-209 (ownership uniformity). Plan steps 2-3. FOUNDATION — start here. USER PRIORITY.\n\n## Step 2 — inventory every ownership boundary\nSeparate two questions:\n- WHERE does a value acquire a new owner? Bindings, assignment, arguments, returns, aggregate elements, captures, loop results.\n- HOW does an expression produce that value? Existing storage, fresh construction, explicit move, borrowing, or forwarding another expression's result.\n\nEstablish ONE table of rules (source -> transfer into an owned destination):\n- Fresh owned temporary -> transfer its ownership; no additional copy.\n- Existing `Copy` value -> duplicate using the type's copy operation.\n- Existing `ExplicitCopy`/`NoCopy` value -> require explicit duplication or a legal `move`.\n- Borrowed storage -> apply the referent's copy policy; borrowing does not grant ownership.\n- Explicit move -> transfer ownership and retire the source.\n- Diverging expression -> no value and no transfer.\nPreserve existing partial-move and `NoMove` restrictions: a field projection must not become movable merely because it reaches the common checker.\nDELIVERABLE: a checked-in inventory mapping every boundary and producer to its implementation and its conformance tests.\n\n## Step 3 — record an explicit decision on each transfer\nEvolve `_check_value_transfer` (`sawc/typechecker/types.py:4372`) from \"may report an error and set `needs_copy`\" into an operation that produces a STRUCTURED ownership decision identifying:\n- Source binding or place, by STABLE IDENTITY (not spelling).\n- Destination and diagnostic location.\n- Action: take-temporary, copy, move, or no-value.\n- Required cleanup-state change.\n- Any generic copy requirement awaiting specialization.\nAttach the decision to the TRANSFER OCCURRENCE, not to the expression node — evaluating an expression and transferring its result are different operations. \"Checked and needs no copy\" MUST differ from \"never checked\"; a false/absent `needs_copy` cannot establish that distinction.\n\n## Acceptance\nEvery successful ownership boundary carries an explicit decision, INCLUDING trivial copies. The inventory (step 2) is checked in and maps each boundary+producer to impl + conformance tests.\n","id":"SL-210","labels":["compiler","ownership"],"priority":"high","project":"SL","revision":7,"sequence":309,"stage":"backlog","status":"open","title":"Ownership Unit A: boundary inventory + structured transfer decision (SL-209)","updated":"1789005425"},"before":{"assignee":"","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sl210","body":"UNIT A COMPLETE on branch `worktree-agent-a33ce7a9e83d0e6b5`. Not submitted —\nthe lead squashes and submits. Behaviour-preserving: same programs accepted,\nsame programs rejected, same diagnostics, same annotations.\n\n## Step 2 — the inventory\n\n`designs/267-ownership-boundary-inventory.md` (design number PROVISIONAL,\nstated as such in the doc). It keeps the epic's two questions apart — WHERE a\nvalue acquires a new owner, and HOW the expression produces it — and maps\nevery boundary and producer to its implementation site and its conformance\nrows:\n\n- the 19 entry points of `_check_value_transfer`, taken mechanically from the\n  AST (41 external call sites plus the self-recursion), grouped as bindings /\n  assignment / arguments / returns and tails / aggregate elements / captures /\n  loop results / owning operands;\n- the rule table (source -> transfer into an owned destination), with\n  partial-move and `NoMove` restrictions recorded as preserved: a projection\n  stays a projection in the record, so reaching the common checker does not\n  make it movable;\n- THE THREE FUNNELS ownership is actually split across today —\n  `_check_value_transfer`, `_check_payload_read` (design 131's payload rule),\n  and `place_uses._value_read_ok` (design 146). They share the tier oracle and\n  guard against each other firing twice on one read, but three funnels is\n  three places for a position to go missing, and only one of them now records\n  a decision. That is the structural finding of step 2 and the thing unit E's\n  verifier has to reconcile;\n- the capture boundary's three modes (`[move x]` -> `_check_move_expr`,\n  `[copy x]` -> its own inline test, plain `[x]` -> the checkpoint);\n- a producer census over all 45 `Expression` subclasses, which is what makes\n  the \"missing\" list below auditable rather than anecdotal;\n- a stated scope limit: std's own bodies are checked once under the builtin\n  typechecker and cached, so the ledger covers the entry module, the user\n  modules and every instance materialized into them — not std's authored\n  bodies.\n\n## Step 3 — the structured decision\n\n`sawc/typechecker/ownership.py`. `_check_value_transfer` now returns a\n`TransferDecision` on EVERY exit path, carrying the five things this issue\nasks for: the source by STABLE IDENTITY (`VariableInfo.binding_id` plus a\nprojection path — never the spelling), the destination and diagnostic site,\nthe action, the required cleanup-state change, and any generic copy\nrequirement awaiting specialization. Eight actions, each earned by an arm the\nfunction demonstrably has: `take` / `copy` / `move` / `none` from the issue's\nrule table, plus `borrow`, `refused`, `delegated` (a value branch, whose arm\nkeys it carries) and `deferred` (design 218c's provenance skips and design\n219's abstract tier).\n\nAttached to the TRANSFER OCCURRENCE, not the expression node: the ledger key\nis `(source node_id, destination context, line, column)` and the node id is in\nit only to NAME which occurrence. Living outside the AST is what makes it\ncorrect — `dataclasses.fields()` cannot see it (so the design-126 contract has\nnothing to declare and `substitute_ast_types` nothing to walk), a\nmonomorphized clone gets fresh node ids and therefore its own decisions, and\nthe table is per-compile so `reemit`/`irdet` byte-identity is untouched.\nAssign-never-accumulate per design 218 stage 1, with a `revision` counter.\n\n\"Checked and needs no copy\" is now distinguishable from \"never checked\" by the\nPRESENCE of an entry, and that property is enforced statically:\n`tools/test_transfer_decisions.py` parses the checkpoint and fails on a bare\n`return`, a `return` of anything that is not a recorded decision, or a\nfunction that can fall off its end — then checks a ten-fixture boundary\nmatrix. Wired as the `transferdecisions` battery stage. Acceptance's \"INCLUDING\ntrivial copies\" is met: the `free`-tier fall-through that used to leave no\ntrace is now an explicit `copy` record, and the fresh-temporary path an\nexplicit `take`.\n\nCost: +2.5% on a small compile (1.752s -> 1.796s, 9 samples each), flat on a\nlarge one.\n\n## Findings — three unchecked boundaries, filed and NOT fixed\n\nEach changes which programs compile, so fixing one here would have broken this\nunit's behaviour-preserving contract. All measured with compile-and-run\nevidence; evidence lives in the issues.\n\n- **SL-218** — a `self` receiver read is not an aliasing expression, so every\n  by-value transfer of `self` is judged a fresh temporary. Double free at exit\n  0 across six faces (return, argument, `let`, struct field, tuple element,\n  `&var self`); SIGABRT on ExplicitCopy; CORRECT on `Copy` — because codegen's\n  `_transfer_needs_copy` carries `SelfExpr` in a list of its own and takes the\n  retain there. That asymmetry is SL-209's thesis in one program: a second\n  opinion can mint a missing retain, it cannot deliver a missing refusal.\n- **SL-219** — `try` / `try!` / `try?` / `try ... catch` over a BINDING or a\n  FIELD forwards its subject unjudged. One value, three `drop` lines, exit 0.\n- **SL-220** — the OPPOSITE error at the same boundary, found writing SL-219's\n  control row: a payload extracted from a fresh temporary inline is never\n  released (`(try! f()).x` and `f()!.x` both leak; the bound spelling is\n  correct).\n\nMECHANISM (obligation 4): SL-218 and SL-219 are ONE mechanism in two\nsub-classes — `_is_aliasing_expr` is a node-type test over four classes plus\ntwo hand-added transparencies, so a node that IS a read and is not in the set\n(`SelfExpr`) and a node that FORWARDS a read and is not transparent\n(`TryExpr`) both become fresh temporaries at every tier at once. DF-216a and\nDF-299a are the same mechanism's earlier members; the one remaining known\nmember is the auto-wrap family, already filed as SL-79 and owned by SL-211.\nSL-220 is the cleanup-registration side and is hypothesised to belong with\nSL-213.\n\nObligation 3 met: conformance rows V86 (SL-218) and V87 (SL-219) were written\nbefore the refactor, both XFAIL citing their issue, with the INDEX rows and\nheader paragraph. SL-220 gets no pin — the program exits 0 with correct\noutput, so it needs an oracle a `deinit`-counting example cannot supply; that\nis the issue's own first task.\n\n## Gates\n\n- Suite: 2431 passed, 10 xfailed, 0 failed (the two new xfails are V86/V87).\n- Freestanding: 36 passed across riscv32 + arm64.\n- TERMINAL BATTERY: 25 stages GREEN in 2617s, including `irdet --all` (whole\n  corpus), `reemit`, `gmgate`, `bootstrap` and `freestanding`.\n- Rebased onto origin/main before the battery; clean, no conflicts.\n\n## Also on the branch\n\nPer a lead ruling of Sep 9, `tools/check_citations.py`'s recognizer was\nwidened to accept SL ids alongside DF ids, so the new id scheme is\ngate-verified rather than conventional. Details in the branch's final commit.\n\n## Not done here, by design\n\nFixing SL-218/219/220; touching `_transfer_needs_copy` (SL-213 now has\nexplicit decisions to migrate onto); giving the payload-read and place funnels\ndecisions of their own (SL-211/SL-214); the verifier (SL-214 — the ledger is\nwhat it will verify).\n","created":"1788986712","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-210.p1 (revision 1): ownership unit A: boundary inventory + structured transfer decisions; SL-id citations gate-verified (SL-209 epic)","created":"1788986970","id":"c2"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-210.p1 (revision 1)","created":"1788987385","id":"c3"},{"author":"agent:codex-reviewer","body":"Requested changes on patch SL-210.p1 (revision 1)\n\nReview of SL-210.p1 revision 1 (applied commit 52390666, base b53a844c): requesting changes for two defects in the new ownership records. These concern Unit A's own record contract, not the separately filed pre-existing language defects in SL-218/219/220.\n\n1. [P2] Capture the resolved source binding before its scope is popped.\n\nLocation: sawc/typechecker/ownership.py:391-395 (_transfer_binding_id).\n\nThe ledger obtains a supposedly stable source identity by looking up the expression's spelling in current_scope at the time the boundary is recorded. That scope is not necessarily the expression's original scope. Body tails are checked after their body scope has been popped, and the value-branch recursion likewise visits branch results after their inner scopes have gone away. This loses the ID for a local tail return and, more seriously, resolves a shadowed branch result to a DIFFERENT binding.\n\nTested example:\n\n```saw\nfunc choose(flag: Bool) -> Int {\n    let x = 7\n    let y = if flag {\n        let x = x + 1\n        let z = x\n        print(z)\n        x\n    } else { x }\n    y\n}\nfunc main() { print(choose(true)) }\n```\n\nCollected the new transfer decisions through tools/test_transfer_decisions.py's compile_and_collect. In one checking pass, the `let z = x` transfer correctly used inner binding_id 3811. The immediately following branch-tail `x` used outer binding_id 3810, identical to the else-arm `x`. The absolute counters vary per compile; the incorrect equality is stable. The function-tail `y` had binding_id=None. A separate NoCopy `move x` tail likewise produced action=move / cleanup=retire-source / binding_id=None.\n\nThis prevents a later consumer from reliably selecting which source to retain or retire, directly violating SL-210's stable-identity requirement. Preserve the resolved binding identity while checking the expression, or pass the owning scope/identity explicitly to the boundary record; do not re-resolve a spelling in the later ambient scope. Add assertions for the actual IDs in tail-return and shadowed value-branch fixtures, not only source.kind.\n\n2. [P2] Record diverging sources as no-value rather than adopted temporaries.\n\nLocation: sawc/typechecker/types.py:4741-4748.\n\nThe non-aliasing branch classifies every non-aliasing expression as ACTION_TAKE / CLEANUP_ADOPT_TEMPORARY without first excluding Never. A valid binding whose initializer diverges therefore records an ownership acquisition that can never occur:\n\n```saw\nfunc main() {\n    let x: Int = panic(\"stop\")\n    print(x)\n}\n```\n\nCompilation succeeds. Its `let binding` decision is source_type=Never, action=take, cleanup=adopt-temporary. SL-210 explicitly requires a diverging expression to have no value and no transfer; ownership.py's ACTION_NONE documents that rule too. Record ACTION_NONE / CLEANUP_NONE for the diverging case before the fresh-temporary classification, while retaining an explicit ledger entry. Add a fixture checking Never's action and cleanup together.\n\nValidation: isolated worktree at the patch's exact base; custom compile-and-collect probes for local tails, NoCopy move tails, shadowed branch results, and Never initializers. The new transfer-decision gate passes (10 fixtures); the citation gate also passes. The tracker reports 2431 compiler tests passed, 10 expected failures, and all 36 freestanding tests passed for 52390666. Full server gates were not repeated locally. These findings affect the new metadata rather than asserting a change in generated program behavior.\n","created":"1788989574","id":"c4"},{"author":"agent:claude-sawlang","body":"Revised patch SL-210.p1 to revision 2","created":"1788996758","id":"c5"}],"created":"1788966177","description":"Unit A of SL-209 (ownership uniformity). Plan steps 2-3. FOUNDATION — start here. USER PRIORITY.\n\n## Step 2 — inventory every ownership boundary\nSeparate two questions:\n- WHERE does a value acquire a new owner? Bindings, assignment, arguments, returns, aggregate elements, captures, loop results.\n- HOW does an expression produce that value? Existing storage, fresh construction, explicit move, borrowing, or forwarding another expression's result.\n\nEstablish ONE table of rules (source -> transfer into an owned destination):\n- Fresh owned temporary -> transfer its ownership; no additional copy.\n- Existing `Copy` value -> duplicate using the type's copy operation.\n- Existing `ExplicitCopy`/`NoCopy` value -> require explicit duplication or a legal `move`.\n- Borrowed storage -> apply the referent's copy policy; borrowing does not grant ownership.\n- Explicit move -> transfer ownership and retire the source.\n- Diverging expression -> no value and no transfer.\nPreserve existing partial-move and `NoMove` restrictions: a field projection must not become movable merely because it reaches the common checker.\nDELIVERABLE: a checked-in inventory mapping every boundary and producer to its implementation and its conformance tests.\n\n## Step 3 — record an explicit decision on each transfer\nEvolve `_check_value_transfer` (`sawc/typechecker/types.py:4372`) from \"may report an error and set `needs_copy`\" into an operation that produces a STRUCTURED ownership decision identifying:\n- Source binding or place, by STABLE IDENTITY (not spelling).\n- Destination and diagnostic location.\n- Action: take-temporary, copy, move, or no-value.\n- Required cleanup-state change.\n- Any generic copy requirement awaiting specialization.\nAttach the decision to the TRANSFER OCCURRENCE, not to the expression node — evaluating an expression and transferring its result are different operations. \"Checked and needs no copy\" MUST differ from \"never checked\"; a false/absent `needs_copy` cannot establish that distinction.\n\n## Acceptance\nEvery successful ownership boundary carries an explicit decision, INCLUDING trivial copies. The inventory (step 2) is checked in and maps each boundary+producer to impl + conformance tests.\n","id":"SL-210","labels":["compiler","ownership"],"priority":"high","project":"SL","revision":6,"sequence":308,"stage":"backlog","status":"open","title":"Ownership Unit A: boundary inventory + structured transfer decision (SL-209)","updated":"1788996758"},"id":"SL-210","project":"SL","request":"{\"action\":\"patch_test_result\",\"actor\":\"agent:sawtracker-tests\",\"commit\":\"6f8ea441\",\"id\":\"SL-210.p1\",\"output\":\"[2439/2449] \\u001B[92m✓\\u001B[0m while_nested\\n[2440/2449] \\u001B[92m✓\\u001B[0m while_let_channel_drain\\n[2441/2449] \\u001B[92m✓\\u001B[0m while_never_diverges\\n[2442/2449] \\u001B[92m✓\\u001B[0m while_never_break_forms\\n[2443/2449] \\u001B[92m✓\\u001B[0m while_let_suspending_mt\\n[2444/2449] \\u001B[92m✓\\u001B[0m while_simple\\n[2445/2449] \\u001B[92m✓\\u001B[0m while_let_suspending_try_catch\\n[2446/2449] \\u001B[92m✓\\u001B[0m while_let_suspending\\n[2447/2449] \\u001B[92m✓\\u001B[0m wrapping_and_reference_mix\\n[2448/2449] \\u001B[92m✓\\u001B[0m wrapping_operators\\n[2449/2449] \\u001B[92m✓\\u001B[0m while_never_sync_context\\n\\ncompile 362.3s + 4.6s draining = 366.8s total\\n\\n======================================================================\\n\\u001B[1mTest Results\\u001B[0m\\n======================================================================\\n\\n\\u001B[93m\\u001B[1mKNOWN FAILURES (xfail):\\u001B[0m\\n  \\u001B[93mx\\u001B[0m V86_self_receiver_read_is_a_transfer: SL-218 — `_is_aliasing_expr` is a node-type test over four classes\\n  \\u001B[93mx\\u001B[0m V87_try_subject_read_is_a_transfer: SL-219 — the sibling of V86 (SL-218) and the same mechanism read the\\n  \\u001B[93mx\\u001B[0m closure_escaping_move_capture_consumed_once: DF-255a an escaping closure's env destructor releases a capture the\\n  \\u001B[93mx\\u001B[0m closure_nested_ref_param_capture: DF-248b a nested closure captures an enclosing closure's reference\\n  \\u001B[93mx\\u001B[0m closure_shorthand_parameter_inside_a_try: DF-257d the `$0` shorthand is invisible to the implicit-parameter scan\\n  \\u001B[93mx\\u001B[0m coro_suspending_call_in_inline_catch: SL-215 the inline `try EXPR catch { }` catch block does not CFG-split,\\n  \\u001B[93mx\\u001B[0m funcpointer_call_in_a_driven_body: DF-252a a `FuncPointer` value called by NAME inside a driven body is\\n  \\u001B[93mx\\u001B[0m generic_init_moved_parameter_is_released_once: DF-302b — a generic extension's `init` releases a parameter it MOVED\\n  \\u001B[93mx\\u001B[0m primitive_type_called_as_a_function: DF-270e — a PRIMITIVE TYPE NAME written as a ZERO-ARGUMENT call is an\\n  \\u001B[93mx\\u001B[0m trailing_closure_inside_a_try_operand: DF-259c — a trailing closure is not recognized inside a `try`/`try!`/\\n  \\u001B[93mx\\u001B[0m wrapped_return_takes_the_transfer_checkpoint: DF-305a — the checkpoint runs AFTER the auto-wrap, so it judges the\\n\\n======================================================================\\n\\u001B[92m\\u001B[1mALL TESTS PASSED\\u001B[0m (2438 passed, 11 xfailed)\\n======================================================================\\n\\nPublished test_runner_1788996761_52707 as test_runner_last (1496 manifest entries).\\nFreestanding QEMU tests\\n  clang: /opt/homebrew/opt/llvm/bin/clang\\n  lld  : /opt/homebrew/bin/ld.lld\\n  qemu : /opt/homebrew/bin/qemu-system-riscv32\\n  qemu : /opt/homebrew/bin/qemu-system-aarch64\\n\\nbuilding blade\\n\\nriscv32  (riscv32-unknown-none-elf, qemu-system-riscv32 `virt`)\\n[1/19] ✗ hello\\n    QEMU hung (> 20s) — no clean exit\\n[2/19] ✓ int_widths\\n[3/19] ✓ struct_layout\\n[4/19] ✓ call_convention\\n[5/19] ✓ module_compose\\n[6/19] ✓ runtime_seams\\n[7/19] ✓ runtime_panic\\n[8/19] ✓ runtime_export_ungated\\n[9/19] ✓ runtime_seam_signature\\n[10/19] ✓ hidden_alloc_interpolation\\n[11/19] ✓ hidden_alloc_escaping_closure\\n[12/19] ✓ hidden_alloc_print_printable\\n[13/19] ✓ format_no_alloc\\n[14/19] ✓ align_attribute\\n[15/19] ✓ export_with_coroutine\\n[16/19] ✓ float_text\\n[17/19] ✓ link_address\\n[18/19] ✓ blade_target\\n[19/19] ✓ wide_value_rendering\\n\\narm64  (aarch64-unknown-none-elf, qemu-system-aarch64 `virt`)\\n[1/17] ✓ hello\\n[2/17] ✓ int_widths\\n[3/17] ✓ struct_layout\\n[4/17] ✓ call_convention\\n[5/17] ✓ module_compose\\n[6/17] ✓ runtime_seams\\n[7/17] ✓ runtime_panic\\n[8/17] ✓ runtime_export_ungated\\n[9/17] ✓ runtime_seam_signature\\n[10/17] ✓ hidden_alloc_interpolation\\n[11/17] ✓ hidden_alloc_escaping_closure\\n[12/17] ✓ hidden_alloc_print_printable\\n[13/17] ✓ format_no_alloc\\n[14/17] ✓ export_with_coroutine\\n[15/17] ✓ float_text\\n[16/17] ✓ link_address\\n[17/17] ✓ blade_target\\n\\n============================================================\\nFREESTANDING TESTS FAILED (35 passed, 1 failed)\\n============================================================\\n\",\"request_id\":\"tests-6f8ea441-r2-e438\",\"revision\":2,\"tests\":\"failed\"}","request_id":"tests-6f8ea441-r2-e438","sequence":309,"timestamp":"1789005425"}
---
