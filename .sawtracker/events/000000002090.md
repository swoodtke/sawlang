---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl208","closed":"1790341659","comments":[{"author":"agent:claude-sl208","body":"A driven (coroutine-transformed) `match` over an OPTIONAL whose arm binds and USES the Some payload ICEs:\n\n  internal compiler error ... (Identifier): Undefined variable: v\n\nMinimal repro (pre-existing; reproduces on a clean tree with no SL-208 changes):\n\n  import std.task.{yield_now}\n  func maybe(x: Int) -> Int? { yield_now(); if x < 0 { None } else { x + 1 } }\n  func pick(x: Int) -> Int {\n      let r = match maybe(x) {\n          case Some(v) -> v,        // <- ICE: Undefined variable: v\n          case _ -> 0\n      }\n      r\n  }\n  func main() { print(__saw_drive(pick(4))) }\n\nBoth the let-bound and the tail-position spellings ICE at the arm; the scrutinee suspends (yield_now), so `maybe` is embedded and `pick`'s match is lowered into the resume state machine, where the `Some(v)` payload binding is not threaded into the arm body's scope.\n\nNOT Optional-vs-Result-symmetric: the RESULT form `match f() { case Ok(v) -> v, case Err(_) -> ... }` with the same driving compiles and runs (verified). So the gap is the coro transform's handling of an OPTIONAL enum-pattern payload binding specifically (Some), where a driven Result Ok binding works.\n\nFound while writing SL-208 regression tests; SL-208's match-arm acceptance cell uses the working Result form. Separate defect from SL-208's module-boundary divergence.","created":"1788967127","id":"c1"},{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \"oracle known wrong\" reference. Test-coverage aspect: `coroutine.match-payload-binding`.","created":"1790341659","id":"c2","kind":"landing"}],"created":"1788967127","description":"","id":"SL-216","labels":["frozen"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":3,"sequence":2090,"stage":"backlog","status":"closed","title":"ICE: driven `match <Optional> { case Some(v) -> v, ... }` — payload binding v undefined in the resume body","updated":"1790341659"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sl208","closed":"","comments":[{"author":"agent:claude-sl208","body":"A driven (coroutine-transformed) `match` over an OPTIONAL whose arm binds and USES the Some payload ICEs:\n\n  internal compiler error ... (Identifier): Undefined variable: v\n\nMinimal repro (pre-existing; reproduces on a clean tree with no SL-208 changes):\n\n  import std.task.{yield_now}\n  func maybe(x: Int) -> Int? { yield_now(); if x < 0 { None } else { x + 1 } }\n  func pick(x: Int) -> Int {\n      let r = match maybe(x) {\n          case Some(v) -> v,        // <- ICE: Undefined variable: v\n          case _ -> 0\n      }\n      r\n  }\n  func main() { print(__saw_drive(pick(4))) }\n\nBoth the let-bound and the tail-position spellings ICE at the arm; the scrutinee suspends (yield_now), so `maybe` is embedded and `pick`'s match is lowered into the resume state machine, where the `Some(v)` payload binding is not threaded into the arm body's scope.\n\nNOT Optional-vs-Result-symmetric: the RESULT form `match f() { case Ok(v) -> v, case Err(_) -> ... }` with the same driving compiles and runs (verified). So the gap is the coro transform's handling of an OPTIONAL enum-pattern payload binding specifically (Some), where a driven Result Ok binding works.\n\nFound while writing SL-208 regression tests; SL-208's match-arm acceptance cell uses the working Result form. Separate defect from SL-208's module-boundary divergence.","created":"1788967127","id":"c1"}],"created":"1788967127","description":"","id":"SL-216","labels":["frozen"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":2089,"stage":"backlog","status":"open","title":"ICE: driven `match <Optional> { case Some(v) -> v, ... }` — payload binding v undefined in the resume body","updated":"1790341659"},"id":"SL-216","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \\\"oracle known wrong\\\" reference. Test-coverage aspect: `coroutine.match-payload-binding`.\",\"id\":\"SL-216\",\"request_id\":\"pid-55489-1790341659-7000\"}","request_id":"pid-55489-1790341659-7000","sequence":2090,"timestamp":"1790341659"}
---
