---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sl265","closed":"1790341664","comments":[{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \"oracle known wrong\" reference. Test-coverage aspect: `diagnostics.copy-tier`.","created":"1790341664","id":"c1","kind":"landing"}],"created":"1789180528","description":"## Description\n\nA diagnostic wart turned up by SL-265 (design 271). The behaviour is correct and\ndeliberate; only the wording is wrong, and it is wrong in a way that teaches the\nreader the opposite of the rule.\n\nA direct `.copy()` on an automatic-Copy-tier receiver is refused:\n\n```saw\nstruct Rec { text: String }        // design 159: automatic Copy tier\n\nfunc main() {\n    let a = Rec(text: \"x\")\n    let b = a.copy()               // error\n}\n```\n\n```\nerror: type `Rec` is not Copy; `.copy()` requires a trivially-copyable, Copy,\nor ExplicitCopy type\n```\n\n`Rec` IS Copy — that is the whole of design 159, and `let b = a` one line up\ncompiles and retains. The refusal itself is right: `.copy()` is the\n`ExplicitCopy` spelling, the silent tier needs no ceremony, and nothing at that\ncall site asked for a duplicate. But the message asserts a false fact about the\ntype, and the hint lists three categories one of which the receiver is in.\n\nThis is the \"inconsistency\" SL-265 was filed around: the reporter read the direct\nrefusal as evidence that the generic bound was wrong to admit the type, when the\nbound is right (design 219: every Copy type satisfies `T: ExplicitCopy`) and the\ndefect was in the lowering. A message that said so would have pointed at the\nlowering a lot sooner.\n\n## Suggested wording\n\nSay the tier and say why the spelling is refused rather than denying the tier —\nsomething in the shape of: \"`Rec` is on the Copy tier, so a copy is implicit:\nwrite `let b = a`. `.copy()` is the spelling for the ExplicitCopy tier, where\nthe ceremony is what makes the duplicate visible.\"\n\nNot fixed by design 271, which is a codegen change and deliberately touches no\ndiagnostic.\n","id":"SL-271","labels":["frozen"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":4,"sequence":2117,"stage":"queued","status":"closed","title":"The .copy() refusal on an automatic-Copy-tier receiver asserts the type is not Copy","updated":"1790341664"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sl265","closed":"","comments":[],"created":"1789180528","description":"## Description\n\nA diagnostic wart turned up by SL-265 (design 271). The behaviour is correct and\ndeliberate; only the wording is wrong, and it is wrong in a way that teaches the\nreader the opposite of the rule.\n\nA direct `.copy()` on an automatic-Copy-tier receiver is refused:\n\n```saw\nstruct Rec { text: String }        // design 159: automatic Copy tier\n\nfunc main() {\n    let a = Rec(text: \"x\")\n    let b = a.copy()               // error\n}\n```\n\n```\nerror: type `Rec` is not Copy; `.copy()` requires a trivially-copyable, Copy,\nor ExplicitCopy type\n```\n\n`Rec` IS Copy — that is the whole of design 159, and `let b = a` one line up\ncompiles and retains. The refusal itself is right: `.copy()` is the\n`ExplicitCopy` spelling, the silent tier needs no ceremony, and nothing at that\ncall site asked for a duplicate. But the message asserts a false fact about the\ntype, and the hint lists three categories one of which the receiver is in.\n\nThis is the \"inconsistency\" SL-265 was filed around: the reporter read the direct\nrefusal as evidence that the generic bound was wrong to admit the type, when the\nbound is right (design 219: every Copy type satisfies `T: ExplicitCopy`) and the\ndefect was in the lowering. A message that said so would have pointed at the\nlowering a lot sooner.\n\n## Suggested wording\n\nSay the tier and say why the spelling is refused rather than denying the tier —\nsomething in the shape of: \"`Rec` is on the Copy tier, so a copy is implicit:\nwrite `let b = a`. `.copy()` is the spelling for the ExplicitCopy tier, where\nthe ceremony is what makes the duplicate visible.\"\n\nNot fixed by design 271, which is a codegen change and deliberately touches no\ndiagnostic.\n","id":"SL-271","labels":["frozen"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":3,"sequence":2116,"stage":"queued","status":"open","title":"The .copy() refusal on an automatic-Copy-tier receiver asserts the type is not Copy","updated":"1790341664"},"id":"SL-271","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its \\\"oracle known wrong\\\" reference. Test-coverage aspect: `diagnostics.copy-tier`.\",\"id\":\"SL-271\",\"request_id\":\"pid-55802-1790341664-6000\"}","request_id":"pid-55802-1790341664-6000","sequence":2117,"timestamp":"1790341664"}
---
