---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791243","description":"## Scope and status\n\nDF-181c (filed Aug 7): Channel.recv from a cooperative task wedges the executor forever. DOCUMENTED (design 182 unit 3, Aug 8): recv's docstring now states the…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/182-command-zero-thread.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/182-command-zero-thread.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 7506–7517\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7506-L7517)\n\n- **DF‑181c (filed Aug 7): `Channel.recv` from a cooperative task wedges the\n  executor forever.** **DOCUMENTED (design 182 unit 3, Aug 8):** `recv`'s\n  docstring now states the consequence rather than only naming the engine — never\n  from a cooperative task, the thread it stops is the executor's, the sender that\n  would unblock it can no longer run, and `receive()` is a drop-in twin. Still\n  only documentation: making the call a compile error inside a suspending body\n  (the brief's \"better\" option) is unbuilt. Original finding follows.\n  It blocks the calling thread in `pthread_cond_wait`\n  with no sender bound. `channel.saw:206` documents which ENGINE it belongs\n  to but never states the consequence, and nothing prevents the call. The\n  cooperative twin `receive` is a drop-in. Cheap fix: document it loudly;\n  better: make `recv` inside a suspending body a compile error.\n","id":"SL-110","labels":["todo-import","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":110,"status":"open","title":"DF-181c (filed Aug 7): Channel.recv from a cooperative task wedges the executor forever. DOCUMENTED (design 182 unit 3, Aug 8): recv's docstring now states the…","updated":"1788791243"},"before":null,"id":"SL-110","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-181c (filed Aug 7): Channel.recv from a cooperative task wedges the executor forever. DOCUMENTED (design 182 unit 3, Aug 8): recv's docstring now states the…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/182-command-zero-thread.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/182-command-zero-thread.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 7506–7517\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7506-L7517)\\n\\n- **DF‑181c (filed Aug 7): `Channel.recv` from a cooperative task wedges the\\n  executor forever.** **DOCUMENTED (design 182 unit 3, Aug 8):** `recv`'s\\n  docstring now states the consequence rather than only naming the engine — never\\n  from a cooperative task, the thread it stops is the executor's, the sender that\\n  would unblock it can no longer run, and `receive()` is a drop-in twin. Still\\n  only documentation: making the call a compile error inside a suspending body\\n  (the brief's \\\"better\\\" option) is unbuilt. Original finding follows.\\n  It blocks the calling thread in `pthread_cond_wait`\\n  with no sender bound. `channel.saw:206` documents which ENGINE it belongs\\n  to but never states the consequence, and nothing prevents the call. The\\n  cooperative twin `receive` is a drop-in. Cheap fix: document it loudly;\\n  better: make `recv` inside a suspending body a compile error.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-9ab59df14c42bfc1024e-create\",\"title\":\"DF-181c (filed Aug 7): Channel.recv from a cooperative task wedges the executor forever. DOCUMENTED (design 182 unit 3, Aug 8): recv's docstring now states the…\"}","request_id":"todo-20260907-9ab59df14c42bfc1024e-create","sequence":110,"timestamp":"1788791243"}
---
