---
{"action":"patch_add","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"Submitted patch SL-249.p1 (revision 1): minivm M6: scalar references (branch commit 60f1e638)","created":"1789096485","id":"c1"}],"created":"1789086344","description":"SL-242 epic child, landing patch 7/14. Contract: prototypes/minivm/M6_SCALAR_REFERENCES.md (conservative field-overlap rule documented there). Patch = branch commit 60f1e638.","id":"SL-249","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":2,"sequence":475,"stage":"queued","status":"open","title":"minivm M6: scalar &/&var references — call-site borrows, forwarding, overlap rejection","updated":"1789096485"},"before":{"assignee":"agent:claude-sawlang","author":"agent:claude-sawlang","comments":[],"created":"1789086344","description":"SL-242 epic child, landing patch 7/14. Contract: prototypes/minivm/M6_SCALAR_REFERENCES.md (conservative field-overlap rule documented there). Patch = branch commit 60f1e638.","id":"SL-249","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":1,"sequence":411,"stage":"queued","status":"open","title":"minivm M6: scalar &/&var references — call-site borrows, forwarding, overlap rejection","updated":"1789086344"},"id":"SL-249","project":"SL","request":"{\"action\":\"patch_add\",\"actor\":\"agent:claude-sawlang\",\"diff\":\"diff --git a/prototypes/minivm/LEXER_DEPENDENCIES.md b/prototypes/minivm/LEXER_DEPENDENCIES.md\\nindex 8354c6e6..7eaa160e 100644\\n--- a/prototypes/minivm/LEXER_DEPENDENCIES.md\\n+++ b/prototypes/minivm/LEXER_DEPENDENCIES.md\\n@@ -10,7 +10,11 @@ constants, compound assignment and short-circuit Booleans. M4 supplies payload-f\\n enums and exhaustive statement match, including the actual TokenKind declaration\\n in isolation. M5 adds implicit tails, value if/match and else-if chains, including\\n the unchanged numeric `hex_value` helper (193 total cases).\\n-Strings, collections, references, generics and methods remain outstanding.\\n+M6 supplies direct-call scalar references, forwarding and mutation, including\\n+borrowing scalar fields of direct records. Whole-record references/receivers,\\n+strings, collections, generics and methods remain outstanding. The next bounded\\n+slice is whole-record references and basic instance methods, tested with a\\n+scalar-only `Lexer.advance`-shaped record before introducing owning strings.\\n The dependency groups below are an inventory, not the implementation commit order;\\n the numbered milestone designs define each isolated slice.\\n \\ndiff --git a/prototypes/minivm/M6_SCALAR_REFERENCES.md b/prototypes/minivm/M6_SCALAR_REFERENCES.md\\nnew file mode 100644\\nindex 00000000..2909cbb2\\n--- /dev/null\\n+++ b/prototypes/minivm/M6_SCALAR_REFERENCES.md\\n@@ -0,0 +1,170 @@\\n+# M6: direct-call scalar references\\n+\\n+Status: implemented after M5 (b6b87a58); integration review and gates recorded below.\\n+\\n+## Bounded source contract\\n+\\n+Accept `&T` and `&var T` function parameters where T is an existing numeric,\\n+Byte, Bool, or payload-free enum type. Calls spell `read(&n)` / `bump(&var n)`;\\n+the operand must be a local, reference parameter, or scalar field of a direct\\n+record local. References are transparent places: reading parameter `x: &Int`\\n+loads its Int, while assigning/compound-assigning `x: &var Int` updates the caller.\\n+Reborrow a parameter using the same explicit argument syntax. A mutable referent\\n+may be reborrowed shared; a shared referent cannot be made mutable.\\n+\\n+This milestone does not accept whole-record references, methods, references to\\n+references, reference locals/record fields/statics/results, reference arithmetic,\\n+reference comparison, or references as general expression values. Reject them\\n+with located diagnostics. Those limits isolate addressing and call lifetimes\\n+before record receivers/methods and owning strings. Existing value semantics stay\\n+unchanged. No new prototype issue or patch submission is needed.\\n+\\n+## Call lifetimes and exclusivity\\n+\\n+Every source borrow ends when its direct call finishes; it cannot escape. Track\\n+active argument borrows while parsing each call, retaining outer-call borrows\\n+while parsing nested arguments. Identify a place by its root binding, conservatively\\n+treating all fields of one record as overlapping. Shared/shared alias arguments\\n+are allowed; any overlap involving a mutable borrow is rejected. Reads of a root\\n+with an active mutable borrow, and writes to any actively borrowed root, are\\n+rejected, including subsequent argument expressions. A value argument evaluated\\n+and snapshotted before a later borrow is valid. Release only this call's borrows\\n+after emitting its Call; preserve the enclosing call's active borrows.\\n+\\n+Plain and compound assignments also reserve their destination root while parsing\\n+the RHS: an explicit shared or mutable borrow of that root is rejected, including\\n+inside nested calls/value blocks. Ordinary scalar reads remain allowed (`x += x`).\\n+This follows Saw's design 227 write-path exclusivity rule and prevents a callee's\\n+mutation from being overwritten by the pending assignment. Track pending writes\\n+separately from active call borrows so reading the RHS does not become illegal.\\n+\\n+The whole-root overlap rule is deliberately narrower than production Saw, which\\n+can prove disjoint record fields. It is a located subset restriction to remove\\n+when richer record references are added, not a proposed change to Saw semantics.\\n+\\n+Incoming reference parameters are distinct roots under this call-site contract.\\n+No mutable globals or closures exist in the subset, so callees cannot discover\\n+another alias outside their arguments. `&var` requires a mutable local/root or\\n+mutable reference parameter. Ordinary by-value parameters remain immutable.\\n+References do not implicitly widen their referent numeric type: &Int8 is not &Int.\\n+The explicit argument mutability spelling must match the requested borrow: a\\n+shared argument cannot initialize a mutable parameter. A mutable argument may\\n+weaken into a shared parameter, while still reserving its explicitly mutable\\n+borrow for the duration of the call.\\n+\\n+## Shared representation and primary-owned interfaces\\n+\\n+Add `ValueType.Ref(index: Int)` and `Program.references: Vector<ReferenceIR>`.\\n+`ReferenceIR { target: ValueType, mutable: Bool }` describes one reference type;\\n+the frontend interns descriptors structurally. References occupy one logical word\\n+(an integer cell index in the VM, a native pointer in LLVM).\\n+They are NOT scalar data for arithmetic, equality, Const, casts, or printing.\\n+\\n+Model helpers:\\n+- `reference_type(t: ValueType) -> Bool`\\n+- `reference_target(refs: &Vector<ReferenceIR>, t: ValueType) -> Result<ValueType, ProtoError>`\\n+- `reference_mutable(refs: &Vector<ReferenceIR>, t: ValueType) -> Bool` (false for invalid/non-reference)\\n+- `reference_transfer(refs: &Vector<ReferenceIR>, source: ValueType, target: ValueType) -> Bool`\\n+  permits identical referent types and either equal mutability or mutable-to-shared.\\n+- `validate_references(refs: &Vector<ReferenceIR>) -> Result<Void, ProtoError>`\\n+  requires scalar-data targets; whole-program validation additionally checks enum indices.\\n+\\n+Keep `scalar_type` unchanged. `value_layout` accepts a root Ref as one word, but\\n+record fields containing references remain rejected. Function slots may contain\\n+scalar data or Ref. Validate every reference index before dereferencing metadata.\\n+No reference result or constant is allowed. Copy may copy references according\\n+to reference_transfer; Call arguments must have the exact callee parameter type\\n+(the frontend inserts a qualifying Copy where needed).\\n+\\n+New instructions:\\n+| Op | Contract |\\n+| --- | --- |\\n+| Addr | dst is Ref, a is a scalar-data slot of exactly its target type; produces its address |\\n+| LoadRef | a is Ref, dst is a scalar-data slot of exactly its target type |\\n+| StoreRef | a is mutable Ref, b is scalar data of exactly its target type; dst unused |\\n+\\n+The verifier checks these structural/type rules and existing CFG/ranges. Source\\n+mutability, initialization, alias restrictions, and address provenance are frontend\\n+proofs, not a claim that the IR is an untrusted bytecode loader.\\n+\\n+## VM and handwritten LLVM\\n+\\n+The VM changes from independent frame slot vectors to a shared cell vector with\\n+a base offset for each active frame. Frame creation appends initialized cells;\\n+all local accesses use base + slot. Addr produces that absolute cell index.\\n+LoadRef/StoreRef access the indexed cell with explicit runtime bounds checks and\\n+`runtime error: invalid reference` on failure. Call arguments/results are still\\n+snapshots. Copy return words before popping the frame's cells. On an error the\\n+whole execution aborts and frees the shared vector. Indices remain valid across\\n+Vector growth, unlike pointers into its backing allocation. Preserve depth and\\n+instruction budgets, all numeric behavior, and record value snapshots.\\n+\\n+LLVM keeps scalar data allocas and data arguments as i64, but Ref slots and\\n+reference arguments use native `ptr`. Addr stores the scalar slot's pointer in\\n+the Ref alloca; LoadRef/StoreRef load that pointer and load/store i64 data through\\n+it. Reference Copy uses ptr loads/stores, and function/call parameter emission\\n+distinguishes Ref words from data words. No pointer-to-integer roundtrip, pointer\\n+arithmetic or heap allocation is introduced. Borrowing one scalar record field\\n+uses that field's existing alloca; whole-record reference contiguity is deferred.\\n+\\n+## Frontend implementation guidance\\n+\\n+Keep signature parameter types as Ref, but resolve a reference binding to a place\\n+with its referent type and indirect address slot. Extend Place with an explicit\\n+direct/indirect distinction and root-binding identity. Ordinary expression reads\\n+snapshot the referent through LoadRef; assignments emit StoreRef after the usual\\n+transfer checks. Compound assignment resolves/loads its destination before RHS\\n+evaluation and performs the existing checked arithmetic before storing. The\\n+write-path restriction above forbids overlapping RHS borrows regardless of this\\n+lowering order.\\n+\\n+Only parse an address argument when the known formal parameter is Ref. Parse the\\n+explicit `&` / optional `var`, then a place; verify type, mutability, and overlap.\\n+For direct places emit Addr of the requested reference descriptor. For forwarding\\n+an existing reference parameter use a qualifying Copy, never address the pointer\\n+slot itself. Reject unexpected address syntax for a value parameter. Register the\\n+borrow before parsing subsequent arguments and restore the borrow stack at call exit.\\n+Enforce read/write conflicts centrally so nested value blocks and calls participate.\\n+\\n+## Delegation and validation\\n+\\n+Primary owns this design, model.saw, verify.saw, direct reference contract tests,\\n+documentation and final integration. Sol frontend owns frontend.saw only. Sol\\n+engines owns vm.saw and llvm.saw only. Sol tests owns test_minivm.py and\\n+examples/references only. Agents may compile individual scratch probes; primary\\n+coordinates suite runs and commits. Any Python sawc findings encountered are\\n+independently reproduced and filed in Sawtracker, as the user requested.\\n+\\n+Test shared reads, caller-visible mutation, compound updates, scalar record-field\\n+addresses, Bool/Byte/enum/full UInt64 references, shared/shared aliasing, recursive\\n+forwarding and growth of VM storage while an ancestor reference stays live,\\n+mutable-to-shared forwarding, value-before-borrow snapshots, and reuse after calls.\\n+Reject mutable access through shared references, borrowing immutable places,\\n+referent type mismatch, missing/wrong address syntax, mixed alias arguments,\\n+later-argument reads/writes during exclusive borrows, escaped/stored references,\\n+and whole-record/nested references. Direct IR tests cover invalid indices, target\\n+types, Addr/LoadRef/StoreRef, qualification Copy, reference constants/results, and\\n+existing verifier boundaries. All prior cases plus the new isolated section must\\n+pass with explicit expected outputs through VM and clang O0/O2; run all direct\\n+representation contracts and compare representative source semantics with Python\\n+sawc without treating its acceptance as the correctness oracle.\\n+\\n+## Completed validation\\n+\\n+- 227 integration cases pass: the previous 193 plus 34 reference cases. Valid\\n+  programs run in the VM and through clang at both O0 and O2; invalid programs\\n+  require located diagnostics. Existing budget/depth cases remain green.\\n+- All four direct contracts (numeric, record, enum, reference) compile and pass.\\n+  The reference contract covers malformed metadata/opcodes and defensive VM\\n+  bounds checks for negative and out-of-range addresses.\\n+- Python sawc agrees with the independent expected outputs for scalar mutation,\\n+  recursive forwarding/storage growth, and mutable-to-shared forwarding.\\n+- Review corrected assignment-RHS borrowing to enforce Saw design 227 for both\\n+  plain and compound assignments. Tests cover those rejections alongside legal\\n+  self-reads and borrows of a different root. Repeated nested reference syntax is\\n+  rejected immediately rather than recursing through unsupported types.\\n+- No new production compiler defect was established in this milestone.\\n+\\n+Next: whole-record references and basic instance methods, using a scalar-only\\n+`Lexer.advance`-shaped fixture. Establish aggregate addressing and receiver\\n+exclusivity before adding owning String/Builder/Vector operations.\\ndiff --git a/prototypes/minivm/README.md b/prototypes/minivm/README.md\\nindex fb9f10bd..35134183 100644\\n--- a/prototypes/minivm/README.md\\n+++ b/prototypes/minivm/README.md\\n@@ -66,7 +66,7 @@ shift counts. See [M1_NUMBERS.md](M1_NUMBERS.md) for the precise numeric contrac\\n Newlines separate statements. Omit the result annotation for Void functions;\\n `main()` must take no arguments and return Void. Value-returning functions need\\n a value on every continuing path, supplied by an explicit return or final expression.\\n-There are no imports, strings, collections, references,\\n+There are no imports, strings, collections,\\n generics, methods, or concurrency in the accepted subset.\\n Semicolons remain unsupported. Boolean `&&`/`||` short-circuit, mutable integers\\n and fields support `+=`/`-=`/`*=`/`/=`/`%=`, and module `static` integer/Bool\\n@@ -74,6 +74,13 @@ constants accept literal and numeric-limit initializers. See\\n [M3_SCALAR_CONTROL.md](M3_SCALAR_CONTROL.md) for this frontend slice.\\n The prototype still accepts broader lexical shadowing than Saw's design-100 rule.\\n \\n+Scalar `&T` / `&var T` function parameters support explicit call-site borrowing,\\n+forwarding, reads and caller-visible mutation. A scalar field of a direct record\\n+can be borrowed. Overlapping mutable arguments and borrowing an assignment's\\n+destination inside its RHS are rejected. Different fields of one record are\\n+conservatively treated as overlapping. Whole-record references and reference\\n+storage/results remain unsupported; see [M6_SCALAR_REFERENCES.md](M6_SCALAR_REFERENCES.md).\\n+\\n The VM defaults to a shared budget of 1,000,000 instructions and a maximum call\\n depth of 128. Override the budget with `run FILE --budget N`. These limits apply\\n only to VM execution; native code uses the host call stack and has no budget.\\n@@ -86,14 +93,15 @@ python prototypes/minivm/test_minivm.py --binary .build/minivm/minivm\\n \\n The harness compares VM execution and clang-compiled IR with explicit expected\\n outputs and statuses, checks rejected programs, and exercises VM limits. It\\n-uses temporary files and 30-second subprocess timeouts. The first five milestones\\n-pass all 193 cases, including native execution at both `-O0` and `-O2`.\\n+uses temporary files and 30-second subprocess timeouts. The first six milestones\\n+pass all 227 cases, including native execution at both `-O0` and `-O2`.\\n \\n-Independent representation checks live in `tests/numeric_contract.saw` and\\n-`tests/record_contract.saw`, and `tests/enum_contract.saw`; build and run them\\n+Independent representation checks live in `tests/numeric_contract.saw`,\\n+`tests/record_contract.saw`, `tests/enum_contract.saw`, and\\n+`tests/reference_contract.saw`; build and run them\\n with the Python compiler.\\n Use `--section numbers`, `--section records`, `--section control`, or\\n-`--section enums`, or `--section values` for an isolated\\n+`--section enums`, `--section values`, or `--section references` for an isolated\\n integration gate.\\n \\n See [DESIGN.md](DESIGN.md) and [M1_NUMBERS.md](M1_NUMBERS.md) for the instruction\\ndiff --git a/prototypes/minivm/examples/references/assignment_rhs_nonborrow.saw b/prototypes/minivm/examples/references/assignment_rhs_nonborrow.saw\\nnew file mode 100644\\nindex 00000000..26ab4045\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/assignment_rhs_nonborrow.saw\\n@@ -0,0 +1,15 @@\\n+func read(value: &Int) -> Int { value }\\n+\\n+func main() {\\n+    var value = 2\\n+    value = value + 1\\n+    print(value)\\n+    value += value\\n+    print(value)\\n+\\n+    let other = 4\\n+    value = read(&other)\\n+    print(value)\\n+    value += read(&other)\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/compound_lhs_snapshot.saw b/prototypes/minivm/examples/references/compound_lhs_snapshot.saw\\nnew file mode 100644\\nindex 00000000..19022c69\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/compound_lhs_snapshot.saw\\n@@ -0,0 +1,13 @@\\n+func replace(value: &var Int) -> Int {\\n+    value = 20\\n+    return 3\\n+}\\n+\\n+func combine(value: &var Int) {\\n+    value += replace(&var value)\\n+}\\n+\\n+func main() {\\n+    var value = 10\\n+    combine(&var value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/direct_record_fields.saw b/prototypes/minivm/examples/references/direct_record_fields.saw\\nnew file mode 100644\\nindex 00000000..b018d495\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/direct_record_fields.saw\\n@@ -0,0 +1,11 @@\\n+struct Pair { left: Int, right: UInt8 }\\n+\\n+func bump(value: &var Int) { value += 3 }\\n+func widen(value: &UInt8) -> Int { value as Int }\\n+\\n+func main() {\\n+    var pair = Pair(left: 5, right: 21)\\n+    bump(&var pair.left)\\n+    print(pair.left)\\n+    print(widen(&pair.right))\\n+}\\ndiff --git a/prototypes/minivm/examples/references/forwarding_growth.saw b/prototypes/minivm/examples/references/forwarding_growth.saw\\nnew file mode 100644\\nindex 00000000..98bd50b7\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/forwarding_growth.saw\\n@@ -0,0 +1,17 @@\\n+func descend(depth: Int, value: &var Int) {\\n+    var padding0 = depth\\n+    var padding1 = depth + 1\\n+    var padding2 = depth + 2\\n+    if depth == 0 {\\n+        value += padding0 + padding1 + padding2 + 34\\n+        return\\n+    }\\n+    descend(depth - 1, &var value)\\n+}\\n+\\n+func main() {\\n+    var value = 1\\n+    descend(40, &var value)\\n+    print(value)\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/mixed_record_reference.saw b/prototypes/minivm/examples/references/mixed_record_reference.saw\\nnew file mode 100644\\nindex 00000000..f320e83a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/mixed_record_reference.saw\\n@@ -0,0 +1,22 @@\\n+struct Pair {\\n+    left: Int\\n+    right: Int\\n+}\\n+\\n+func record_then_ref(pair: Pair, amount: &Int) -> Pair {\\n+    Pair(left: pair.left + amount, right: pair.right)\\n+}\\n+\\n+func ref_then_record(amount: &Int, pair: Pair) -> Pair {\\n+    Pair(left: pair.left, right: pair.right + amount)\\n+}\\n+\\n+func main() {\\n+    let amount = 4\\n+    let first = record_then_ref(Pair(left: 1, right: 12), &amount)\\n+    print(first.left)\\n+    print(first.right)\\n+    let second = ref_then_record(&amount, Pair(left: 9, right: 12))\\n+    print(second.left)\\n+    print(second.right)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/mutable_to_shared.saw b/prototypes/minivm/examples/references/mutable_to_shared.saw\\nnew file mode 100644\\nindex 00000000..5dfee059\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/mutable_to_shared.saw\\n@@ -0,0 +1,11 @@\\n+func inspect(value: &Int) { print(value) }\\n+func forward(value: &var Int) {\\n+    inspect(&value)\\n+    value += 1\\n+}\\n+\\n+func main() {\\n+    var value = 12\\n+    forward(&var value)\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/nested_borrow_release.saw b/prototypes/minivm/examples/references/nested_borrow_release.saw\\nnew file mode 100644\\nindex 00000000..debcb717\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/nested_borrow_release.saw\\n@@ -0,0 +1,15 @@\\n+func bump(value: &var Int) -> Int {\\n+    value += 1\\n+    return value\\n+}\\n+\\n+func choose(held: &Int, nested: Int, after: Int) -> Int {\\n+    held + nested - after\\n+}\\n+\\n+func main() {\\n+    let held = 7\\n+    var other = 10\\n+    print(choose(&held, bump(&var other), other))\\n+    print(other)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/read_mutate_compound.saw b/prototypes/minivm/examples/references/read_mutate_compound.saw\\nnew file mode 100644\\nindex 00000000..5d684d0b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/read_mutate_compound.saw\\n@@ -0,0 +1,17 @@\\n+func read(value: &Int) -> Int { value }\\n+func update(value: &var Int) {\\n+    value += 9\\n+    print(value)\\n+    value /= 2\\n+    print(value)\\n+    value %= 20\\n+    print(value)\\n+}\\n+\\n+func main() {\\n+    var number = 41\\n+    print(read(&number))\\n+    number += 1\\n+    print(number)\\n+    update(&var number)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_alias_mutable_shared.saw b/prototypes/minivm/examples/references/reject_alias_mutable_shared.saw\\nnew file mode 100644\\nindex 00000000..fa59f552\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_alias_mutable_shared.saw\\n@@ -0,0 +1,5 @@\\n+func clash(a: &var Int, b: &Int) {}\\n+func main() {\\n+    var value = 1\\n+    clash(&var value, &value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_alias_two_fields.saw b/prototypes/minivm/examples/references/reject_alias_two_fields.saw\\nnew file mode 100644\\nindex 00000000..e6e8ba79\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_alias_two_fields.saw\\n@@ -0,0 +1,6 @@\\n+struct Pair { left: Int, right: Int }\\n+func clash(a: &var Int, b: &Int) {}\\n+func main() {\\n+    var pair = Pair(left: 1, right: 2)\\n+    clash(&var pair.left, &pair.right)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_assignment_rhs_mutable.saw b/prototypes/minivm/examples/references/reject_assignment_rhs_mutable.saw\\nnew file mode 100644\\nindex 00000000..8d01eeae\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_assignment_rhs_mutable.saw\\n@@ -0,0 +1,9 @@\\n+func replace(value: &var Int) -> Int {\\n+    value = 20\\n+    return 3\\n+}\\n+\\n+func main() {\\n+    var value = 1\\n+    value = replace(&var value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_assignment_rhs_shared.saw b/prototypes/minivm/examples/references/reject_assignment_rhs_shared.saw\\nnew file mode 100644\\nindex 00000000..f9e929b0\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_assignment_rhs_shared.saw\\n@@ -0,0 +1,6 @@\\n+func read(value: &Int) -> Int { value }\\n+\\n+func main() {\\n+    var value = 1\\n+    value = read(&value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_compound_rhs_borrow.saw b/prototypes/minivm/examples/references/reject_compound_rhs_borrow.saw\\nnew file mode 100644\\nindex 00000000..1bf13481\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_compound_rhs_borrow.saw\\n@@ -0,0 +1,6 @@\\n+func read(value: &Int) -> Int { value }\\n+\\n+func main() {\\n+    var value = 1\\n+    value += read(&value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_immutable_borrow.saw b/prototypes/minivm/examples/references/reject_immutable_borrow.saw\\nnew file mode 100644\\nindex 00000000..f3a4ceaf\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_immutable_borrow.saw\\n@@ -0,0 +1,5 @@\\n+func bump(value: &var Int) { value += 1 }\\n+func main() {\\n+    let value = 1\\n+    bump(&var value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_later_read.saw b/prototypes/minivm/examples/references/reject_later_read.saw\\nnew file mode 100644\\nindex 00000000..3f697ae9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_later_read.saw\\n@@ -0,0 +1,5 @@\\n+func consume(a: &var Int, b: Int) {}\\n+func main() {\\n+    var value = 1\\n+    consume(&var value, value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_later_write.saw b/prototypes/minivm/examples/references/reject_later_write.saw\\nnew file mode 100644\\nindex 00000000..116c9a3e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_later_write.saw\\n@@ -0,0 +1,10 @@\\n+func consume(a: &Int, b: Int) {}\\n+func main() {\\n+    var value = 1\\n+    consume(&value, if true {\\n+        value = 7\\n+        value\\n+    } else {\\n+        0\\n+    })\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_missing_ampersand.saw b/prototypes/minivm/examples/references/reject_missing_ampersand.saw\\nnew file mode 100644\\nindex 00000000..48e5212a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_missing_ampersand.saw\\n@@ -0,0 +1,5 @@\\n+func read(value: &Int) -> Int { value }\\n+func main() {\\n+    let value = 1\\n+    print(read(value))\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_missing_var.saw b/prototypes/minivm/examples/references/reject_missing_var.saw\\nnew file mode 100644\\nindex 00000000..cbd71dd1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_missing_var.saw\\n@@ -0,0 +1,5 @@\\n+func bump(value: &var Int) { value += 1 }\\n+func main() {\\n+    var value = 1\\n+    bump(&value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_mutate_shared.saw b/prototypes/minivm/examples/references/reject_mutate_shared.saw\\nnew file mode 100644\\nindex 00000000..a155255f\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_mutate_shared.saw\\n@@ -0,0 +1,4 @@\\n+func bad(value: &Int) {\\n+    value += 1\\n+}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/references/reject_nested_assignment_rhs_borrow.saw b/prototypes/minivm/examples/references/reject_nested_assignment_rhs_borrow.saw\\nnew file mode 100644\\nindex 00000000..79b17e16\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_nested_assignment_rhs_borrow.saw\\n@@ -0,0 +1,10 @@\\n+func read(value: &Int) -> Int { value }\\n+\\n+func main() {\\n+    var value = 1\\n+    value = if true {\\n+        read(&value)\\n+    } else {\\n+        0\\n+    }\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_nested_reference.saw b/prototypes/minivm/examples/references/reject_nested_reference.saw\\nnew file mode 100644\\nindex 00000000..e52d84ae\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_nested_reference.saw\\n@@ -0,0 +1,2 @@\\n+func bad(value: & &Int) {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/references/reject_outer_borrow_preserved.saw b/prototypes/minivm/examples/references/reject_outer_borrow_preserved.saw\\nnew file mode 100644\\nindex 00000000..a2777988\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_outer_borrow_preserved.saw\\n@@ -0,0 +1,8 @@\\n+func read(value: &Int) -> Int { value }\\n+func consume(held: &var Int, nested: Int, after: Int) {}\\n+\\n+func main() {\\n+    var held = 4\\n+    let other = 9\\n+    consume(&var held, read(&other), held)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_reference_field.saw b/prototypes/minivm/examples/references/reject_reference_field.saw\\nnew file mode 100644\\nindex 00000000..61131a08\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_reference_field.saw\\n@@ -0,0 +1,2 @@\\n+struct Bad { value: &Int }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/references/reject_reference_local.saw b/prototypes/minivm/examples/references/reject_reference_local.saw\\nnew file mode 100644\\nindex 00000000..7ce33e3a\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_reference_local.saw\\n@@ -0,0 +1,3 @@\\n+func main() {\\n+    let value: &Int = 1\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_reference_result.saw b/prototypes/minivm/examples/references/reject_reference_result.saw\\nnew file mode 100644\\nindex 00000000..c7a2ac02\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_reference_result.saw\\n@@ -0,0 +1,2 @@\\n+func bad(value: &Int) -> &Int { value }\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/references/reject_reference_static.saw b/prototypes/minivm/examples/references/reject_reference_static.saw\\nnew file mode 100644\\nindex 00000000..4036ef05\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_reference_static.saw\\n@@ -0,0 +1,2 @@\\n+static bad: &Int = 1\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/references/reject_shared_to_mutable.saw b/prototypes/minivm/examples/references/reject_shared_to_mutable.saw\\nnew file mode 100644\\nindex 00000000..fb515008\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_shared_to_mutable.saw\\n@@ -0,0 +1,6 @@\\n+func bump(value: &var Int) { value += 1 }\\n+func forward(value: &Int) { bump(&var value) }\\n+func main() {\\n+    var value = 1\\n+    forward(&value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_spaced_nested_reference.saw b/prototypes/minivm/examples/references/reject_spaced_nested_reference.saw\\nnew file mode 100644\\nindex 00000000..7052474b\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_spaced_nested_reference.saw\\n@@ -0,0 +1,2 @@\\n+func bad(value: &   &   &   &   &   &   &   &   &   &   &   &   &   &   &   &   Int) {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/references/reject_value_address.saw b/prototypes/minivm/examples/references/reject_value_address.saw\\nnew file mode 100644\\nindex 00000000..42dd84aa\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_value_address.saw\\n@@ -0,0 +1,5 @@\\n+func take(value: Int) {}\\n+func main() {\\n+    let value = 1\\n+    take(&value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/reject_whole_record.saw b/prototypes/minivm/examples/references/reject_whole_record.saw\\nnew file mode 100644\\nindex 00000000..dfdd1ced\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_whole_record.saw\\n@@ -0,0 +1,3 @@\\n+struct Pair { left: Int, right: Int }\\n+func bad(value: &Pair) {}\\n+func main() {}\\ndiff --git a/prototypes/minivm/examples/references/reject_wrong_referent.saw b/prototypes/minivm/examples/references/reject_wrong_referent.saw\\nnew file mode 100644\\nindex 00000000..cd0232b9\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/reject_wrong_referent.saw\\n@@ -0,0 +1,5 @@\\n+func read(value: &Int) -> Int { value }\\n+func main() {\\n+    let value: Int8 = 1\\n+    print(read(&value))\\n+}\\ndiff --git a/prototypes/minivm/examples/references/scalar_kinds.saw b/prototypes/minivm/examples/references/scalar_kinds.saw\\nnew file mode 100644\\nindex 00000000..01ab987e\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/scalar_kinds.saw\\n@@ -0,0 +1,18 @@\\n+enum State { case Off, case On }\\n+\\n+func show_u64(value: &UInt64) { print(value) }\\n+func show_byte(value: &Byte) { print(value) }\\n+func flip(value: &var Bool) { value = not value }\\n+func is_on(value: &State) -> Bool { value == State.On }\\n+\\n+func main() {\\n+    let wide: UInt64 = 18_446_744_073_709_551_615\\n+    let byte = Byte(255u8)\\n+    var flag = false\\n+    let state = State.On\\n+    show_u64(&wide)\\n+    show_byte(&byte)\\n+    flip(&var flag)\\n+    print(flag)\\n+    print(is_on(&state))\\n+}\\ndiff --git a/prototypes/minivm/examples/references/shared_aliases.saw b/prototypes/minivm/examples/references/shared_aliases.saw\\nnew file mode 100644\\nindex 00000000..6689aef1\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/shared_aliases.saw\\n@@ -0,0 +1,7 @@\\n+func sum(a: &Int, b: &Int) -> Int { a + b }\\n+\\n+func main() {\\n+    let value = 9\\n+    print(sum(&value, &value))\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/examples/references/value_before_borrow.saw b/prototypes/minivm/examples/references/value_before_borrow.saw\\nnew file mode 100644\\nindex 00000000..c7166f65\\n--- /dev/null\\n+++ b/prototypes/minivm/examples/references/value_before_borrow.saw\\n@@ -0,0 +1,13 @@\\n+func set_after(snapshot: Int, value: &var Int) {\\n+    print(snapshot)\\n+    value = 10\\n+}\\n+func bump(value: &var Int) { value += 1 }\\n+\\n+func main() {\\n+    var value = 4\\n+    set_after(value, &var value)\\n+    print(value)\\n+    bump(&var value)\\n+    print(value)\\n+}\\ndiff --git a/prototypes/minivm/src/frontend.saw b/prototypes/minivm/src/frontend.saw\\nindex 319ddacd..54f1bf8e 100644\\n--- a/prototypes/minivm/src/frontend.saw\\n+++ b/prototypes/minivm/src/frontend.saw\\n@@ -31,6 +31,13 @@ struct Place {\\n     ty: ValueType\\n     slot: Int\\n     mutable: Bool\\n+    indirect: Bool\\n+    root: Int\\n+}\\n+\\n+struct Borrow {\\n+    root: Int\\n+    mutable: Bool\\n }\\n \\n struct StaticBinding {\\n@@ -73,7 +80,7 @@ func type_name(t: ValueType) -> String {\\n         case I8 -> \\\"Int8\\\", case I16 -> \\\"Int16\\\", case I32 -> \\\"Int32\\\", case I64 -> \\\"Int\\\",\\n         case U8 -> \\\"UInt8\\\", case U16 -> \\\"UInt16\\\", case U32 -> \\\"UInt32\\\", case U64 -> \\\"UInt\\\",\\n         case Byte -> \\\"Byte\\\", case Bool -> \\\"Bool\\\", case Void -> \\\"Void\\\",\\n-        case Record(_) -> \\\"record\\\", case Enum(_) -> \\\"enum\\\"\\n+        case Record(_) -> \\\"record\\\", case Enum(_) -> \\\"enum\\\", case Ref(_) -> \\\"reference\\\"\\n     }\\n }\\n \\n@@ -109,6 +116,7 @@ struct Compiler {\\n     tokens: Vector<Token>\\n     records: Vector<RecordIR>\\n     enums: Vector<EnumIR>\\n+    references: Vector<ReferenceIR>\\n     statics: Vector<StaticBinding>\\n     signatures: Vector<Signature>\\n     pos: Int\\n@@ -116,6 +124,8 @@ struct Compiler {\\n     expr_depth: Int\\n     paren_depth: Int\\n     bindings: Vector<Binding>\\n+    active_borrows: Vector<Borrow>\\n+    pending_writes: Vector<Int>\\n     slots: Vector<ValueType>\\n     code: Vector<Instruction>\\n     current_result: ValueType\\n@@ -159,6 +169,22 @@ extension Compiler {\\n     }\\n \\n     func parse_type(&var self) -> Result<ValueType, ProtoError> {\\n+        if self.at(TokenKind.Ampersand) {\\n+            let amp = self.take()\\n+            var mutable = false\\n+            if self.at(TokenKind.Var) {\\n+                let _ = self.take()\\n+                mutable = true\\n+            }\\n+            if self.at(TokenKind.Ampersand) {\\n+                let nested = self.token()\\n+                return problem(\\\"references to references are not supported\\\", nested.line, nested.col)\\n+            }\\n+            let target = try self.parse_type()\\n+            if reference_type(target) { return problem(\\\"references to references are not supported\\\", amp.line, amp.col) }\\n+            if not scalar_type(target) { return problem(\\\"only scalar references are supported\\\", amp.line, amp.col) }\\n+            return self.intern_reference(target, mutable)\\n+        }\\n         let t = try self.expect(TokenKind.Ident, \\\"expected type\\\")\\n         if t.value == \\\"Bool\\\" { return ValueType.Bool }\\n         if let ty = named_integer_type(t.value) { return ty }\\n@@ -169,6 +195,18 @@ extension Compiler {\\n         problem(\\\"unsupported type `{t.value}`\\\", t.line, t.col)\\n     }\\n \\n+    func intern_reference(&var self, target: ValueType, mutable: Bool) -> ValueType {\\n+        var i = 0\\n+        while i < self.references.len() {\\n+            if self.references[i].target == target && self.references[i].mutable == mutable {\\n+                return ValueType.Ref(i)\\n+            }\\n+            i += 1\\n+        }\\n+        try! self.references.push(ReferenceIR(target: target, mutable: mutable))\\n+        ValueType.Ref(self.references.len() - 1)\\n+    }\\n+\\n     func record_index(&self, name: String) -> Int {\\n         var i = 0\\n         while i < self.records.len() {\\n@@ -343,6 +381,7 @@ extension Compiler {\\n             if self.at(TokenKind.Arrow) {\\n                 let _ = self.take()\\n                 result = try self.parse_type()\\n+                if reference_type(result) { return problem(\\\"reference results are not supported\\\", name.line, name.col) }\\n             }\\n             let _ = try self.expect(TokenKind.LBrace, \\\"expected function body\\\")\\n             let start = self.pos\\n@@ -489,6 +528,7 @@ extension Compiler {\\n             let _ = try self.expect(TokenKind.Colon, \\\"expected `:` after record field\\\")\\n             let ty = try self.parse_type()\\n             if ty == ValueType.Void { return problem(\\\"record field cannot have Void type\\\", field.line, field.col) }\\n+            if reference_type(ty) { return problem(\\\"reference record fields are not supported\\\", field.line, field.col) }\\n             try! names.push(field.value)\\n             try! types.push(ty)\\n             if self.at(TokenKind.Comma) {\\n@@ -594,6 +634,79 @@ extension Compiler {\\n         -1\\n     }\\n \\n+    func check_read(&self, root: Int, t: &Token) -> Result<Void, ProtoError> {\\n+        var i = 0\\n+        while i < self.active_borrows.len() {\\n+            if self.active_borrows[i].root == root && self.active_borrows[i].mutable {\\n+                return problem(\\\"cannot read a place while it is mutably borrowed\\\", t.line, t.col)\\n+            }\\n+            i += 1\\n+        }\\n+        return\\n+    }\\n+\\n+    func check_write(&self, root: Int, t: &Token) -> Result<Void, ProtoError> {\\n+        var i = 0\\n+        while i < self.active_borrows.len() {\\n+            if self.active_borrows[i].root == root {\\n+                return problem(\\\"cannot write a place while it is borrowed\\\", t.line, t.col)\\n+            }\\n+            i += 1\\n+        }\\n+        return\\n+    }\\n+\\n+    func register_borrow(&var self, place: &Place, mutable: Bool,\\n+                         t: &Token) -> Result<Void, ProtoError> {\\n+        var pending = 0\\n+        while pending < self.pending_writes.len() {\\n+            if self.pending_writes[pending] == place.root {\\n+                return problem(\\\"cannot borrow an assignment destination while evaluating its right-hand side\\\", t.line, t.col)\\n+            }\\n+            pending += 1\\n+        }\\n+        var i = 0\\n+        while i < self.active_borrows.len() {\\n+            if self.active_borrows[i].root == place.root && (mutable || self.active_borrows[i].mutable) {\\n+                return problem(\\\"overlapping arguments cannot include a mutable borrow\\\", t.line, t.col)\\n+            }\\n+            i += 1\\n+        }\\n+        try! self.active_borrows.push(Borrow(root: place.root, mutable: mutable))\\n+        return\\n+    }\\n+\\n+    func restore_borrows(&var self, count: Int) {\\n+        while self.active_borrows.len() > count { let _ = self.active_borrows.pop() }\\n+    }\\n+\\n+    func restore_pending_writes(&var self, count: Int) {\\n+        while self.pending_writes.len() > count { let _ = self.pending_writes.pop() }\\n+    }\\n+\\n+    func read_place(&var self, place: Place, t: &Token) -> Result<ExprValue, ProtoError> {\\n+        try self.check_read(place.root, &t)\\n+        if place.indirect {\\n+            let dst = self.new_slot(place.ty)\\n+            let at = self.emit_at(Op.LoadRef, t.line, t.col)\\n+            self.code[at].dst = dst\\n+            self.code[at].a = place.slot\\n+            return expr_value(place.ty, dst, false)\\n+        }\\n+        self.snapshot(expr_value(place.ty, place.slot, false), &t)\\n+    }\\n+\\n+    func store_place(&var self, place: Place, value: ExprValue,\\n+                     t: &Token) -> Result<Void, ProtoError> {\\n+        if place.indirect {\\n+            let at = self.emit_at(Op.StoreRef, t.line, t.col)\\n+            self.code[at].a = place.slot\\n+            self.code[at].b = value.slot\\n+            return\\n+        }\\n+        self.emit_copy_value(place.slot, value.slot, place.ty, &t)\\n+    }\\n+\\n     func declare(&var self, name: Token, ty: ValueType, slot: Int, mutable: Bool) -> Result<Void, ProtoError> {\\n         var i = self.bindings.len()\\n         while i > 0 {\\n@@ -746,8 +859,14 @@ extension Compiler {\\n             if self.at(TokenKind.LParen) { return self.parse_call(name) }\\n             let bi = self.binding_index(name.value)\\n             if bi >= 0 {\\n-                let value = expr_value(self.bindings[bi].ty, self.bindings[bi].slot, false)\\n-                return self.snapshot(value, &name)\\n+                var place = Place(ty: self.bindings[bi].ty, slot: self.bindings[bi].slot,\\n+                                  mutable: self.bindings[bi].mutable, indirect: false, root: bi)\\n+                if reference_type(place.ty) {\\n+                    place.mutable = reference_mutable(&self.references, place.ty)\\n+                    place.ty = try reference_target(&self.references, place.ty)\\n+                    place.indirect = true\\n+                }\\n+                return self.read_place(place, &name)\\n             }\\n             let si = self.static_index(name.value)\\n             if si >= 0 {\\n@@ -797,10 +916,16 @@ extension Compiler {\\n             return problem(\\\"unknown local `{name.value}`\\\", name.line, name.col)\\n         }\\n         var place = Place(ty: self.bindings[bi].ty, slot: self.bindings[bi].slot,\\n-                          mutable: self.bindings[bi].mutable)\\n+                          mutable: self.bindings[bi].mutable, indirect: false, root: bi)\\n+        if reference_type(place.ty) {\\n+            place.mutable = reference_mutable(&self.references, place.ty)\\n+            place.ty = try reference_target(&self.references, place.ty)\\n+            place.indirect = true\\n+        }\\n         while self.at(TokenKind.Dot) {\\n             let _ = self.take()\\n             let field = try self.expect(TokenKind.Ident, \\\"expected record field after `.`\\\")\\n+            if place.indirect { return problem(\\\"reference parameters cannot be used as records\\\", field.line, field.col) }\\n             match place.ty {\\n                 case Record(ri) -> {\\n                     let fi = self.field_index(ri, field.value)\\n@@ -952,6 +1077,7 @@ extension Compiler {\\n         let _ = self.take()\\n         var args = Vector<Int>()\\n         var ai = 0\\n+        let saved_borrows = self.active_borrows.len()\\n         self.depth += 1\\n         self.paren_depth += 1\\n         self.skip_newlines()\\n@@ -961,14 +1087,57 @@ extension Compiler {\\n                 if ai >= self.signatures[fi].params.len() {\\n                     return problem(\\\"too many arguments to `{name.value}`\\\", name.line, name.col)\\n                 }\\n-                var value = try self.parse_expected_expr(self.signatures[fi].params[ai])\\n-                if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n-                value = try self.snapshot(value, &name)\\n-                let layout = try value_layout(&self.records, value.ty)\\n-                var word = 0\\n-                while word < layout.len() {\\n-                    try! args.push(value.slot + word)\\n-                    word += 1\\n+                let formal = self.signatures[fi].params[ai]\\n+                if reference_type(formal) {\\n+                    let amp = try self.expect(TokenKind.Ampersand, \\\"reference argument requires explicit `&`\\\")\\n+                    var explicit_mutable = false\\n+                    if self.at(TokenKind.Var) {\\n+                        let _ = self.take()\\n+                        explicit_mutable = true\\n+                    }\\n+                    let parsed = try self.parse_place()\\n+                    let place = parsed.1\\n+                    let formal_mutable = reference_mutable(&self.references, formal)\\n+                    let target = try reference_target(&self.references, formal)\\n+                    if formal_mutable && not explicit_mutable {\\n+                        return problem(\\\"mutable reference argument requires `&var`\\\", amp.line, amp.col)\\n+                    }\\n+                    if place.ty != target {\\n+                        return problem(\\\"reference argument type does not match parameter\\\", amp.line, amp.col)\\n+                    }\\n+                    if explicit_mutable && not place.mutable {\\n+                        return problem(\\\"cannot mutably borrow an immutable place\\\", amp.line, amp.col)\\n+                    }\\n+                    try self.register_borrow(&place, explicit_mutable, &amp)\\n+                    let dst = self.new_slot(formal)\\n+                    if place.indirect {\\n+                        let source = self.bindings[place.root].ty\\n+                        if not reference_transfer(&self.references, source, formal) {\\n+                            return problem(\\\"reference argument mutability does not match parameter\\\", amp.line, amp.col)\\n+                        }\\n+                        let copy = self.emit_at(Op.Copy, amp.line, amp.col)\\n+                        self.code[copy].dst = dst\\n+                        self.code[copy].a = place.slot\\n+                    } else {\\n+                        let addr = self.emit_at(Op.Addr, amp.line, amp.col)\\n+                        self.code[addr].dst = dst\\n+                        self.code[addr].a = place.slot\\n+                    }\\n+                    try! args.push(dst)\\n+                } else {\\n+                    if self.at(TokenKind.Ampersand) {\\n+                        let address = self.token()\\n+                        return problem(\\\"value parameter does not accept an address argument\\\", address.line, address.col)\\n+                    }\\n+                    var value = try self.parse_expected_expr(formal)\\n+                    if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n+                    value = try self.snapshot(value, &name)\\n+                    let layout = try value_layout(&self.records, value.ty)\\n+                    var word = 0\\n+                    while word < layout.len() {\\n+                        try! args.push(value.slot + word)\\n+                        word += 1\\n+                    }\\n                 }\\n                 ai += 1\\n                 self.skip_newlines()\\n@@ -990,6 +1159,7 @@ extension Compiler {\\n         self.code[at].target = fi\\n         self.code[at].dst = dst\\n         self.code[at].args = move args\\n+        self.restore_borrows(saved_borrows)\\n         expr_value(rt, dst, true)\\n     }\\n \\n@@ -1865,10 +2035,14 @@ extension Compiler {\\n                 let _ = self.take()\\n                 annotated = Some(try self.parse_type())\\n             }\\n+            if let local_type = annotated {\\n+                if reference_type(local_type) { return problem(\\\"reference locals are not supported\\\", name.line, name.col) }\\n+            }\\n             let _ = try self.expect(TokenKind.Assign, \\\"local declarations require an initializer\\\")\\n             var value = if let ty = annotated { try self.parse_expected_expr(ty) } else { try self.parse_expr() }\\n             if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", name.line, name.col) }\\n             if value.ty == ValueType.Void { return problem(\\\"cannot initialize a local with Void\\\", name.line, name.col) }\\n+            if reference_type(value.ty) { return problem(\\\"reference locals are not supported\\\", name.line, name.col) }\\n             if let _ = annotated {} else { value = try self.transfer(value, value.ty, &name) }\\n             let dst = try self.new_value(value.ty)\\n             try self.emit_copy_value(dst, value.slot, value.ty, &name)\\n@@ -1968,19 +2142,33 @@ extension Compiler {\\n             let name = parsed.0\\n             let place = parsed.1\\n             let assign = self.take()\\n-            if not place.mutable { return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col) }\\n-            var value = if assign.kind == TokenKind.Assign { try self.parse_expected_expr(place.ty) } else { try self.parse_expr() }\\n-            if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", assign.line, assign.col) }\\n-            if assign.kind != TokenKind.Assign {\\n+            if not place.mutable {\\n+                if place.indirect { return problem(\\\"cannot assign through a shared reference\\\", name.line, name.col) }\\n+                return problem(\\\"cannot assign to immutable local `{name.value}`\\\", name.line, name.col)\\n+            }\\n+            try self.check_write(place.root, &name)\\n+            var value = returning_value()\\n+            if assign.kind == TokenKind.Assign {\\n+                let saved_writes = self.pending_writes.len()\\n+                try! self.pending_writes.push(place.root)\\n+                value = try self.parse_expected_expr(place.ty)\\n+                self.restore_pending_writes(saved_writes)\\n+            } else {\\n                 if not arithmetic_type(place.ty) { return problem(\\\"compound assignment requires an arithmetic integer destination\\\", assign.line, assign.col) }\\n+                let left = try self.read_place(place, &name)\\n+                let saved_writes = self.pending_writes.len()\\n+                try! self.pending_writes.push(place.root)\\n+                value = try self.parse_expr()\\n+                self.restore_pending_writes(saved_writes)\\n+                if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", assign.line, assign.col) }\\n                 if value.literal { value = try self.transfer(value, place.ty, &assign) }\\n                 else if value.ty != place.ty { return problem(\\\"compound assignment operands must have the same type\\\", assign.line, assign.col) }\\n                 let op = if assign.kind == TokenKind.PlusAssign { Op.Add } else if assign.kind == TokenKind.MinusAssign { Op.Sub } else if assign.kind == TokenKind.StarAssign { Op.Mul } else if assign.kind == TokenKind.SlashAssign { Op.Div } else { Op.Rem }\\n-                let left = expr_value(place.ty, place.slot, false)\\n                 value = try self.binary(left, value, op, place.ty, &assign)\\n             }\\n+            if value.always_returns { return problem(\\\"all-returning control flow cannot be used as an operand\\\", assign.line, assign.col) }\\n             value = try self.snapshot(value, &name)\\n-            try self.emit_copy_value(place.slot, value.slot, place.ty, &name)\\n+            try self.store_place(place, value, &name)\\n             return false\\n         }\\n         let start = self.token()\\n@@ -1997,6 +2185,8 @@ extension Compiler {\\n         self.expr_depth = 0\\n         self.paren_depth = 0\\n         self.bindings = Vector<Binding>()\\n+        self.active_borrows = Vector<Borrow>()\\n+        self.pending_writes = Vector<Int>()\\n         self.slots = Vector<ValueType>()\\n         self.code = Vector<Instruction>()\\n         self.current_result = self.signatures[index].result\\n@@ -2060,10 +2250,11 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         case Ok(ts) -> move ts,\\n         case Err(e) -> { return problem(e.message, e.line, e.col) }\\n     }\\n-    var compiler = Compiler(tokens: move tokens, records: Vector<RecordIR>(), enums: Vector<EnumIR>(), statics: Vector<StaticBinding>(),\\n+    var compiler = Compiler(tokens: move tokens, records: Vector<RecordIR>(), enums: Vector<EnumIR>(), references: Vector<ReferenceIR>(), statics: Vector<StaticBinding>(),\\n                             signatures: Vector<Signature>(), pos: 0, depth: 0,\\n                             expr_depth: 0, paren_depth: 0,\\n-                            bindings: Vector<Binding>(), slots: Vector<ValueType>(),\\n+                            bindings: Vector<Binding>(), active_borrows: Vector<Borrow>(), pending_writes: Vector<Int>(),\\n+                            slots: Vector<ValueType>(),\\n                             code: Vector<Instruction>(), current_result: ValueType.Void)\\n     try compiler.collect_record_names()\\n     try compiler.scan_signatures()\\n@@ -2078,5 +2269,6 @@ public func compile_source(source: String) -> Result<Program, ProtoError> {\\n         try! functions.push(move f)\\n         i += 1\\n     }\\n-    Program(enums: compiler.enums.copy(), records: compiler.records.copy(), functions: move functions, main_index: main_index)\\n+    Program(enums: compiler.enums.copy(), records: compiler.records.copy(), references: compiler.references.copy(),\\n+            functions: move functions, main_index: main_index)\\n }\\ndiff --git a/prototypes/minivm/src/llvm.saw b/prototypes/minivm/src/llvm.saw\\nindex 934ed307..568e215c 100644\\n--- a/prototypes/minivm/src/llvm.saw\\n+++ b/prototypes/minivm/src/llvm.saw\\n@@ -64,6 +64,14 @@ func load_slot(out: &var StringBuilder, instruction_index: Int,\\n     put(&var out, \\\"\\\\n\\\")\\n }\\n \\n+func load_ref_slot(out: &var StringBuilder, instruction_index: Int,\\n+                   suffix: String, slot: Int) {\\n+    temp_name(&var out, instruction_index, suffix)\\n+    put(&var out, \\\" = load ptr, ptr \\\")\\n+    slot_name(&var out, slot)\\n+    put(&var out, \\\"\\\\n\\\")\\n+}\\n+\\n func store_temp(out: &var StringBuilder, instruction_index: Int,\\n                 suffix: String, slot: Int) {\\n     put(&var out, \\\"  store i64 \\\")\\n@@ -73,6 +81,15 @@ func store_temp(out: &var StringBuilder, instruction_index: Int,\\n     put(&var out, \\\"\\\\n\\\")\\n }\\n \\n+func store_ref_temp(out: &var StringBuilder, instruction_index: Int,\\n+                    suffix: String, slot: Int) {\\n+    put(&var out, \\\"  store ptr \\\")\\n+    temp_name(&var out, instruction_index, suffix)\\n+    put(&var out, \\\", ptr \\\")\\n+    slot_name(&var out, slot)\\n+    put(&var out, \\\"\\\\n\\\")\\n+}\\n+\\n func emit_normalized_result(out: &var StringBuilder, instruction_index: Int,\\n                             dst: Int, suffix: String, t: ValueType) {\\n     let width = integer_width(t)\\n@@ -357,10 +374,46 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n         },\\n         case Copy -> {\\n             put(&var out, \\\"  \\\")\\n-            load_slot(&var out, instruction_index, \\\"v\\\", inst.a)\\n+            if reference_type(function.slots[inst.a]) {\\n+                load_ref_slot(&var out, instruction_index, \\\"v\\\", inst.a)\\n+                store_ref_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+            } else {\\n+                load_slot(&var out, instruction_index, \\\"v\\\", inst.a)\\n+                store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n+            }\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case Addr -> {\\n+            put(&var out, \\\"  store ptr \\\")\\n+            slot_name(&var out, inst.a)\\n+            put(&var out, \\\", ptr \\\")\\n+            slot_name(&var out, inst.dst)\\n+            put(&var out, \\\"\\\\n\\\")\\n+            next_block(&var out, instruction_index)\\n+        },\\n+        case LoadRef -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            temp_name(&var out, instruction_index, \\\"v\\\")\\n+            put(&var out, \\\" = load i64, ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"ref\\\")\\n+            put(&var out, \\\"\\\\n\\\")\\n             store_temp(&var out, instruction_index, \\\"v\\\", inst.dst)\\n             next_block(&var out, instruction_index)\\n         },\\n+        case StoreRef -> {\\n+            put(&var out, \\\"  \\\")\\n+            load_ref_slot(&var out, instruction_index, \\\"ref\\\", inst.a)\\n+            put(&var out, \\\"  \\\")\\n+            load_slot(&var out, instruction_index, \\\"v\\\", inst.b)\\n+            put(&var out, \\\"  store i64 \\\")\\n+            temp_name(&var out, instruction_index, \\\"v\\\")\\n+            put(&var out, \\\", ptr \\\")\\n+            temp_name(&var out, instruction_index, \\\"ref\\\")\\n+            put(&var out, \\\"\\\\n\\\")\\n+            next_block(&var out, instruction_index)\\n+        },\\n         case Add -> emit_binary(&var out, instruction_index, &inst, \\\"add\\\", true, function.slots[inst.dst]),\\n         case Sub -> emit_binary(&var out, instruction_index, &inst, \\\"sub\\\", true, function.slots[inst.dst]),\\n         case Mul -> emit_binary(&var out, instruction_index, &inst, \\\"mul\\\", true, function.slots[inst.dst]),\\n@@ -451,7 +504,11 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             var argument_index = 0\\n             while argument_index < inst.args.len() {\\n                 put(&var out, \\\"  \\\")\\n-                load_slot(&var out, instruction_index, \\\"arg{argument_index}\\\", inst.args[argument_index])\\n+                if reference_type(function.slots[inst.args[argument_index]]) {\\n+                    load_ref_slot(&var out, instruction_index, \\\"arg{argument_index}\\\", inst.args[argument_index])\\n+                } else {\\n+                    load_slot(&var out, instruction_index, \\\"arg{argument_index}\\\", inst.args[argument_index])\\n+                }\\n                 argument_index = argument_index + 1\\n             }\\n             put(&var out, \\\"  \\\")\\n@@ -467,7 +524,8 @@ func emit_instruction(out: &var StringBuilder, program: &Program,\\n             argument_index = 0\\n             while argument_index < inst.args.len() {\\n                 if argument_index > 0 { put(&var out, \\\", \\\") }\\n-                put(&var out, \\\"i64 \\\")\\n+                if reference_type(function.slots[inst.args[argument_index]]) { put(&var out, \\\"ptr \\\") }\\n+                else { put(&var out, \\\"i64 \\\") }\\n                 temp_name(&var out, instruction_index, \\\"arg{argument_index}\\\")\\n                 argument_index = argument_index + 1\\n             }\\n@@ -570,14 +628,22 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     var parameter_index = 0\\n     var argument_index = 0\\n     while parameter_index < function.params.len() {\\n-        let parameter_layout = try value_layout(&program.records, function.params[parameter_index])\\n-        var parameter_word = 0\\n-        while parameter_word < parameter_layout.len() {\\n+        let parameter_type = function.params[parameter_index]\\n+        if reference_type(parameter_type) {\\n             if argument_index > 0 { put(&var out, \\\", \\\") }\\n-            put(&var out, \\\"i64 %arg\\\")\\n+            put(&var out, \\\"ptr %arg\\\")\\n             put_int(&var out, argument_index)\\n             argument_index = argument_index + 1\\n-            parameter_word = parameter_word + 1\\n+        } else {\\n+            let parameter_layout = try value_layout(&program.records, parameter_type)\\n+            var parameter_word = 0\\n+            while parameter_word < parameter_layout.len() {\\n+                if argument_index > 0 { put(&var out, \\\", \\\") }\\n+                put(&var out, \\\"i64 %arg\\\")\\n+                put_int(&var out, argument_index)\\n+                argument_index = argument_index + 1\\n+                parameter_word = parameter_word + 1\\n+            }\\n         }\\n         parameter_index = parameter_index + 1\\n     }\\n@@ -586,7 +652,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     while slot_index < function.slots.len() {\\n         put(&var out, \\\"  \\\")\\n         slot_name(&var out, slot_index)\\n-        put(&var out, \\\" = alloca i64\\\\n\\\")\\n+        put(&var out, if reference_type(function.slots[slot_index]) { \\\" = alloca ptr\\\\n\\\" } else { \\\" = alloca i64\\\\n\\\" })\\n         slot_index = slot_index + 1\\n     }\\n     var parameter_words = 0\\n@@ -598,7 +664,7 @@ func emit_function(out: &var StringBuilder, program: &Program,\\n     }\\n     argument_index = 0\\n     while argument_index < parameter_words {\\n-        put(&var out, \\\"  store i64 %arg\\\")\\n+        put(&var out, if reference_type(function.slots[argument_index]) { \\\"  store ptr %arg\\\" } else { \\\"  store i64 %arg\\\" })\\n         put_int(&var out, argument_index)\\n         put(&var out, \\\", ptr \\\")\\n         slot_name(&var out, argument_index)\\ndiff --git a/prototypes/minivm/src/model.saw b/prototypes/minivm/src/model.saw\\nindex 3f759532..16115707 100644\\n--- a/prototypes/minivm/src/model.saw\\n+++ b/prototypes/minivm/src/model.saw\\n@@ -1,7 +1,7 @@\\n public enum ValueType {\\n     case I8, case I16, case I32, case I64,\\n     case U8, case U16, case U32, case U64, case Byte,\\n-    case Bool, case Void, case Record(index: Int), case Enum(index: Int)\\n+    case Bool, case Void, case Record(index: Int), case Enum(index: Int), case Ref(index: Int)\\n }\\n @synthesize\\n extension ValueType: Equatable {}\\n@@ -11,6 +11,7 @@ public enum Op {\\n     case Neg, case Not, case Eq, case Ne, case Lt, case Le, case Gt, case Ge,\\n     case Jump, case Branch, case Call, case Print, case Return,\\n     case Cast, case Truncate, case WrapAdd, case WrapSub, case WrapMul,\\n+    case Addr, case LoadRef, case StoreRef,\\n     case BitAnd, case BitOr, case BitXor, case BitNot, case Shl, case Shr\\n }\\n @synthesize\\n@@ -115,12 +116,18 @@ public struct EnumIR {\\n extension EnumIR: ExplicitCopy {}\\n \\n public struct Program {\\n+    public references: Vector<ReferenceIR>\\n     public enums: Vector<EnumIR>\\n     public records: Vector<RecordIR>\\n     public functions: Vector<FuncIR>\\n     public main_index: Int\\n }\\n \\n+public struct ReferenceIR {\\n+    public target: ValueType\\n+    public mutable: Bool\\n+}\\n+\\n public struct RecordIR {\\n     public name: String\\n     public field_names: Vector<String>\\n@@ -169,6 +176,48 @@ public func scalar_type(t: ValueType) -> Bool {\\n     }\\n }\\n \\n+public func reference_type(t: ValueType) -> Bool {\\n+    match t {\\n+        case Ref(_) -> true,\\n+        case _ -> false\\n+    }\\n+}\\n+\\n+public func reference_target(refs: &Vector<ReferenceIR>, t: ValueType) -> Result<ValueType, ProtoError> {\\n+    match t {\\n+        case Ref(index) -> {\\n+            if index < 0 || index >= refs.len() { return problem(\\\"invalid reference type index\\\", 0, 0) }\\n+            return refs[index].target\\n+        },\\n+        case _ -> { return problem(\\\"expected reference type\\\", 0, 0) }\\n+    }\\n+}\\n+\\n+public func reference_mutable(refs: &Vector<ReferenceIR>, t: ValueType) -> Bool {\\n+    match t {\\n+        case Ref(index) -> {\\n+            if index < 0 || index >= refs.len() { return false }\\n+            return refs[index].mutable\\n+        },\\n+        case _ -> false\\n+    }\\n+}\\n+\\n+public func reference_transfer(refs: &Vector<ReferenceIR>, source: ValueType, target: ValueType) -> Bool {\\n+    let source_target = try reference_target(&refs, source) catch { return false }\\n+    let target_target = try reference_target(&refs, target) catch { return false }\\n+    source_target == target_target && (not reference_mutable(&refs, target) || reference_mutable(&refs, source))\\n+}\\n+\\n+public func validate_references(refs: &Vector<ReferenceIR>) -> Result<Void, ProtoError> {\\n+    var n = 0\\n+    while n < refs.len() {\\n+        if not scalar_type(refs[n].target) { return problem(\\\"reference target must be a scalar type\\\", 0, 0) }\\n+        n += 1\\n+    }\\n+    return\\n+}\\n+\\n public func validate_enums(enums: &Vector<EnumIR>) -> Result<Void, ProtoError> {\\n     var n = 0\\n     while n < enums.len() {\\n@@ -225,12 +274,17 @@ func append_layout(records: &Vector<RecordIR>, t: ValueType,\\n             let _ = path.pop()\\n             return\\n         },\\n+        case Ref(_) -> { return problem(\\\"reference record fields are not supported\\\", 0, 0) },\\n         case _ -> { return problem(\\\"Void is not a record field type\\\", 0, 0) }\\n     }\\n }\\n \\n public func value_layout(records: &Vector<RecordIR>, t: ValueType) -> Result<Vector<ValueType>, ProtoError> {\\n     var words = Vector<ValueType>()\\n+    if reference_type(t) {\\n+        try! words.push(t)\\n+        return move words\\n+    }\\n     if t != ValueType.Void {\\n         var path = Vector<Int>()\\n         try append_layout(&records, t, &var path, &var words)\\ndiff --git a/prototypes/minivm/src/verify.saw b/prototypes/minivm/src/verify.saw\\nindex 3be40742..73a4dbfd 100644\\n--- a/prototypes/minivm/src/verify.saw\\n+++ b/prototypes/minivm/src/verify.saw\\n@@ -17,6 +17,9 @@ func check_type(p: &Program, t: ValueType) -> Result<Void, ProtoError> {\\n         case Record(index) -> {\\n             if index < 0 || index >= p.records.len() { return problem(\\\"internal prototype error: invalid record type index\\\", 0, 0) }\\n         },\\n+        case Ref(index) -> {\\n+            if index < 0 || index >= p.references.len() { return problem(\\\"internal prototype error: invalid reference type index\\\", 0, 0) }\\n+        },\\n         case _ -> {}\\n     }\\n     return\\n@@ -24,6 +27,11 @@ func check_type(p: &Program, t: ValueType) -> Result<Void, ProtoError> {\\n \\n func check_declared_types(p: &Program) -> Result<Void, ProtoError> {\\n     var n = 0\\n+    while n < p.references.len() {\\n+        try check_type(&p, p.references[n].target)\\n+        n += 1\\n+    }\\n+    n = 0\\n     while n < p.records.len() {\\n         let record = p.records[n].copy()\\n         var e = 0\\n@@ -42,6 +50,7 @@ func check_declared_types(p: &Program) -> Result<Void, ProtoError> {\\n     while n < p.functions.len() {\\n         let f = p.functions[n].copy()\\n         try check_type(&p, f.result)\\n+        if reference_type(f.result) { return problem(\\\"reference function results are not supported\\\", 0, 0) }\\n         var word = 0\\n         while word < f.params.len() {\\n             try check_type(&p, f.params[word])\\n@@ -100,7 +109,24 @@ func check_instruction(p: &Program, f: &FuncIR, i: &Instruction) -> Result<Void,\\n             }\\n         },\\n         case Copy -> {\\n-            if not scalar_type(dst) || a != dst { return invalid(&i) }\\n+            if reference_type(dst) {\\n+                if not reference_transfer(&p.references, a, dst) { return invalid(&i) }\\n+            } else if not scalar_type(dst) || a != dst { return invalid(&i) }\\n+        },\\n+        case Addr -> {\\n+            if not reference_type(dst) || not scalar_type(a) { return invalid(&i) }\\n+            let target = try reference_target(&p.references, dst)\\n+            if target != a { return invalid(&i) }\\n+        },\\n+        case LoadRef -> {\\n+            if not reference_type(a) || not scalar_type(dst) { return invalid(&i) }\\n+            let target = try reference_target(&p.references, a)\\n+            if target != dst { return invalid(&i) }\\n+        },\\n+        case StoreRef -> {\\n+            if not reference_mutable(&p.references, a) || not scalar_type(b) { return invalid(&i) }\\n+            let target = try reference_target(&p.references, a)\\n+            if target != b { return invalid(&i) }\\n         },\\n         case Add -> { try check_binary(&i, dst, a, b) },\\n         case Sub -> { try check_binary(&i, dst, a, b) },\\n@@ -183,7 +209,7 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n     }\\n     var n = 0\\n     while n < f.slots.len() {\\n-        if not scalar_type(f.slots[n]) { return problem(\\\"internal prototype error: non-scalar slot\\\", 0, 0) }\\n+        if not scalar_type(f.slots[n]) && not reference_type(f.slots[n]) { return problem(\\\"internal prototype error: non-scalar slot\\\", 0, 0) }\\n         if n < params.len() && f.slots[n] != params[n] {\\n             return problem(\\\"internal prototype error: parameter slot mismatch\\\", 0, 0)\\n         }\\n@@ -215,6 +241,7 @@ func check_function(p: &Program, f: &FuncIR) -> Result<Void, ProtoError> {\\n \\n public func verify(program: &Program) -> Result<Void, ProtoError> {\\n     try validate_enums(&program.enums)\\n+    try validate_references(&program.references)\\n     try check_declared_types(&program)\\n     try validate_records(&program.records)\\n     if program.main_index < 0 || program.main_index >= program.functions.len() {\\ndiff --git a/prototypes/minivm/src/vm.saw b/prototypes/minivm/src/vm.saw\\nindex 14e2f8a1..1216c0eb 100644\\n--- a/prototypes/minivm/src/vm.saw\\n+++ b/prototypes/minivm/src/vm.saw\\n@@ -86,18 +86,20 @@ func compare(a: Int, b: Int, t: ValueType, op: Op) -> Int {\\n     if yes { 1 } else { 0 }\\n }\\n \\n-func run_function(program: &Program, function_index: Int, arguments: &Vector<Int>, remaining: &var Int, depth: Int) -> Result<CallResult, ProtoError> {\\n+func run_function(program: &Program, function_index: Int, arguments: &Vector<Int>,\\n+                  cells: &var Vector<Int>, remaining: &var Int,\\n+                  depth: Int) -> Result<CallResult, ProtoError> {\\n     if depth > MAX_CALL_DEPTH { return runtime_error(\\\"runtime error: call depth exceeded\\\") }\\n     let function = program.functions[function_index].copy()\\n-    var slots = Vector<Int>()\\n+    let base = cells.len()\\n     var si = 0\\n     while si < function.slots.len() {\\n-        try! slots.push(0)\\n+        try! cells.push(0)\\n         si = si + 1\\n     }\\n     var ai = 0\\n     while ai < arguments.len() {\\n-        slots[ai] = arguments[ai]\\n+        cells[base + ai] = arguments[ai]\\n         ai = ai + 1\\n     }\\n     var pc = 0\\n@@ -107,45 +109,56 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n         let inst = function.code[pc].copy()\\n         let t = if inst.dst >= 0 { function.slots[inst.dst] } else { ValueType.Void }\\n         match inst.op {\\n-            case Const -> { slots[inst.dst] = inst.immediate },\\n-            case Copy -> { slots[inst.dst] = slots[inst.a] },\\n-            case Add -> { slots[inst.dst] = try checked_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n-            case Sub -> { slots[inst.dst] = try checked_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n-            case Mul -> { slots[inst.dst] = try checked_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n-            case WrapAdd -> { slots[inst.dst] = wrapping_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n-            case WrapSub -> { slots[inst.dst] = wrapping_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n-            case WrapMul -> { slots[inst.dst] = wrapping_binary(slots[inst.a], slots[inst.b], t, inst.op) },\\n-            case Div -> { slots[inst.dst] = try divide(slots[inst.a], slots[inst.b], t, false) },\\n-            case Rem -> { slots[inst.dst] = try divide(slots[inst.a], slots[inst.b], t, true) },\\n+            case Const -> { cells[base + inst.dst] = inst.immediate },\\n+            case Copy -> { cells[base + inst.dst] = cells[base + inst.a] },\\n+            case Addr -> { cells[base + inst.dst] = base + inst.a },\\n+            case LoadRef -> {\\n+                let address = cells[base + inst.a]\\n+                if address < 0 || address >= cells.len() { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                cells[base + inst.dst] = cells[address]\\n+            },\\n+            case StoreRef -> {\\n+                let address = cells[base + inst.a]\\n+                if address < 0 || address >= cells.len() { return runtime_error(\\\"runtime error: invalid reference\\\") }\\n+                cells[address] = cells[base + inst.b]\\n+            },\\n+            case Add -> { cells[base + inst.dst] = try checked_binary(cells[base + inst.a], cells[base + inst.b], t, inst.op) },\\n+            case Sub -> { cells[base + inst.dst] = try checked_binary(cells[base + inst.a], cells[base + inst.b], t, inst.op) },\\n+            case Mul -> { cells[base + inst.dst] = try checked_binary(cells[base + inst.a], cells[base + inst.b], t, inst.op) },\\n+            case WrapAdd -> { cells[base + inst.dst] = wrapping_binary(cells[base + inst.a], cells[base + inst.b], t, inst.op) },\\n+            case WrapSub -> { cells[base + inst.dst] = wrapping_binary(cells[base + inst.a], cells[base + inst.b], t, inst.op) },\\n+            case WrapMul -> { cells[base + inst.dst] = wrapping_binary(cells[base + inst.a], cells[base + inst.b], t, inst.op) },\\n+            case Div -> { cells[base + inst.dst] = try divide(cells[base + inst.a], cells[base + inst.b], t, false) },\\n+            case Rem -> { cells[base + inst.dst] = try divide(cells[base + inst.a], cells[base + inst.b], t, true) },\\n             case Neg -> {\\n-                let a = slots[inst.a]\\n+                let a = cells[base + inst.a]\\n                 if a == integer_min(t) { return overflow_error() }\\n-                slots[inst.dst] = 0 - a\\n+                cells[base + inst.dst] = 0 - a\\n             },\\n             case Cast -> {\\n-                if not integer_fits(slots[inst.a], function.slots[inst.a], t) { return runtime_error(\\\"runtime error: integer cast out of range\\\") }\\n-                slots[inst.dst] = integer_normalize(slots[inst.a], t)\\n+                if not integer_fits(cells[base + inst.a], function.slots[inst.a], t) { return runtime_error(\\\"runtime error: integer cast out of range\\\") }\\n+                cells[base + inst.dst] = integer_normalize(cells[base + inst.a], t)\\n             },\\n-            case Truncate -> { slots[inst.dst] = integer_normalize(slots[inst.a], t) },\\n-            case BitAnd -> { slots[inst.dst] = integer_normalize(slots[inst.a] & slots[inst.b], t) },\\n-            case BitOr -> { slots[inst.dst] = integer_normalize(slots[inst.a] | slots[inst.b], t) },\\n-            case BitXor -> { slots[inst.dst] = integer_normalize(slots[inst.a] ^ slots[inst.b], t) },\\n-            case BitNot -> { slots[inst.dst] = integer_normalize(~slots[inst.a], t) },\\n-            case Shl -> { slots[inst.dst] = try shift(slots[inst.a], slots[inst.b], function.slots[inst.b], t, true) },\\n-            case Shr -> { slots[inst.dst] = try shift(slots[inst.a], slots[inst.b], function.slots[inst.b], t, false) },\\n-            case Not -> { slots[inst.dst] = if slots[inst.a] == 0 { 1 } else { 0 } },\\n-            case Eq -> { slots[inst.dst] = if slots[inst.a] == slots[inst.b] { 1 } else { 0 } },\\n-            case Ne -> { slots[inst.dst] = if slots[inst.a] != slots[inst.b] { 1 } else { 0 } },\\n-            case Lt -> { slots[inst.dst] = compare(slots[inst.a], slots[inst.b], function.slots[inst.a], inst.op) },\\n-            case Le -> { slots[inst.dst] = compare(slots[inst.a], slots[inst.b], function.slots[inst.a], inst.op) },\\n-            case Gt -> { slots[inst.dst] = compare(slots[inst.a], slots[inst.b], function.slots[inst.a], inst.op) },\\n-            case Ge -> { slots[inst.dst] = compare(slots[inst.a], slots[inst.b], function.slots[inst.a], inst.op) },\\n+            case Truncate -> { cells[base + inst.dst] = integer_normalize(cells[base + inst.a], t) },\\n+            case BitAnd -> { cells[base + inst.dst] = integer_normalize(cells[base + inst.a] & cells[base + inst.b], t) },\\n+            case BitOr -> { cells[base + inst.dst] = integer_normalize(cells[base + inst.a] | cells[base + inst.b], t) },\\n+            case BitXor -> { cells[base + inst.dst] = integer_normalize(cells[base + inst.a] ^ cells[base + inst.b], t) },\\n+            case BitNot -> { cells[base + inst.dst] = integer_normalize(~cells[base + inst.a], t) },\\n+            case Shl -> { cells[base + inst.dst] = try shift(cells[base + inst.a], cells[base + inst.b], function.slots[inst.b], t, true) },\\n+            case Shr -> { cells[base + inst.dst] = try shift(cells[base + inst.a], cells[base + inst.b], function.slots[inst.b], t, false) },\\n+            case Not -> { cells[base + inst.dst] = if cells[base + inst.a] == 0 { 1 } else { 0 } },\\n+            case Eq -> { cells[base + inst.dst] = if cells[base + inst.a] == cells[base + inst.b] { 1 } else { 0 } },\\n+            case Ne -> { cells[base + inst.dst] = if cells[base + inst.a] != cells[base + inst.b] { 1 } else { 0 } },\\n+            case Lt -> { cells[base + inst.dst] = compare(cells[base + inst.a], cells[base + inst.b], function.slots[inst.a], inst.op) },\\n+            case Le -> { cells[base + inst.dst] = compare(cells[base + inst.a], cells[base + inst.b], function.slots[inst.a], inst.op) },\\n+            case Gt -> { cells[base + inst.dst] = compare(cells[base + inst.a], cells[base + inst.b], function.slots[inst.a], inst.op) },\\n+            case Ge -> { cells[base + inst.dst] = compare(cells[base + inst.a], cells[base + inst.b], function.slots[inst.a], inst.op) },\\n             case Jump -> {\\n                 pc = inst.target\\n                 continue\\n             },\\n             case Branch -> {\\n-                if slots[inst.a] != 0 {\\n+                if cells[base + inst.a] != 0 {\\n                     pc = inst.target\\n                     continue\\n                 }\\n@@ -154,30 +167,31 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n                 var args = Vector<Int>()\\n                 var i = 0\\n                 while i < inst.args.len() {\\n-                    try! args.push(slots[inst.args[i]])\\n+                    try! args.push(cells[base + inst.args[i]])\\n                     i = i + 1\\n                 }\\n-                let result = try run_function(&program, inst.target, &args, &var remaining, depth + 1)\\n+                let result = try run_function(&program, inst.target, &args, &var cells, &var remaining, depth + 1)\\n                 var result_index = 0\\n                 while result_index < result.values.len() {\\n-                    slots[inst.dst + result_index] = result.values[result_index]\\n+                    cells[base + inst.dst + result_index] = result.values[result_index]\\n                     result_index = result_index + 1\\n                 }\\n             },\\n             case Print -> {\\n                 if not printable_type(function.slots[inst.a]) { return problem(\\\"internal prototype error: non-printable value\\\", inst.line, inst.col) }\\n                 let pt = function.slots[inst.a]\\n-                if pt == ValueType.Bool { if slots[inst.a] == 0 { print(\\\"false\\\") } else { print(\\\"true\\\") } }\\n-                else if integer_signed(pt) { print(slots[inst.a]) } else { print(UInt.from(truncating: slots[inst.a])) }\\n+                if pt == ValueType.Bool { if cells[base + inst.a] == 0 { print(\\\"false\\\") } else { print(\\\"true\\\") } }\\n+                else if integer_signed(pt) { print(cells[base + inst.a]) } else { print(UInt.from(truncating: cells[base + inst.a])) }\\n             },\\n             case Return -> {\\n                 var values = Vector<Int>()\\n                 let layout = try value_layout(&program.records, function.result)\\n                 var result_index = 0\\n                 while result_index < layout.len() {\\n-                    try! values.push(slots[inst.a + result_index])\\n+                    try! values.push(cells[base + inst.a + result_index])\\n                     result_index = result_index + 1\\n                 }\\n+                while cells.len() > base { let _ = cells.pop() }\\n                 return CallResult(values: move values)\\n             }\\n         }\\n@@ -189,6 +203,7 @@ func run_function(program: &Program, function_index: Int, arguments: &Vector<Int\\n public func execute(program: &Program, budget: Int) -> Result<Void, ProtoError> {\\n     var remaining = budget\\n     let arguments = Vector<Int>()\\n-    let _ = try run_function(&program, program.main_index, &arguments, &var remaining, 1)\\n+    var cells = Vector<Int>()\\n+    let _ = try run_function(&program, program.main_index, &arguments, &var cells, &var remaining, 1)\\n     return\\n }\\ndiff --git a/prototypes/minivm/test_minivm.py b/prototypes/minivm/test_minivm.py\\nindex 161ede2b..51190701 100644\\n--- a/prototypes/minivm/test_minivm.py\\n+++ b/prototypes/minivm/test_minivm.py\\n@@ -35,6 +35,16 @@ class RejectCase:\\n \\n \\n RUN_CASES = (\\n+    RunCase(\\\"references/read_mutate_compound\\\", \\\"41\\\\n42\\\\n51\\\\n25\\\\n5\\\\n\\\", section=\\\"references\\\"),\\n+    RunCase(\\\"references/scalar_kinds\\\", \\\"18446744073709551615\\\\n255\\\\ntrue\\\\ntrue\\\\n\\\", section=\\\"references\\\"),\\n+    RunCase(\\\"references/direct_record_fields\\\", \\\"8\\\\n21\\\\n\\\", section=\\\"references\\\"),\\n+    RunCase(\\\"references/shared_aliases\\\", \\\"18\\\\n9\\\\n\\\", section=\\\"references\\\"),\\n+    RunCase(\\\"references/forwarding_growth\\\", \\\"38\\\\n38\\\\n\\\", section=\\\"references\\\"),\\n+    RunCase(\\\"references/mutable_to_shared\\\", \\\"12\\\\n13\\\\n\\\", section=\\\"references\\\"),\\n+    RunCase(\\\"references/value_before_borrow\\\", \\\"4\\\\n10\\\\n11\\\\n\\\", section=\\\"references\\\"),\\n+    RunCase(\\\"references/mixed_record_reference\\\", \\\"5\\\\n12\\\\n9\\\\n16\\\\n\\\", section=\\\"references\\\"),\\n+    RunCase(\\\"references/assignment_rhs_nonborrow\\\", \\\"3\\\\n6\\\\n4\\\\n8\\\\n\\\", section=\\\"references\\\"),\\n+    RunCase(\\\"references/nested_borrow_release\\\", \\\"7\\\\n11\\\\n\\\", section=\\\"references\\\"),\\n     RunCase(\\\"enums/nested_control\\\", \\\"true\\\\n10\\\\n11\\\\n20\\\\n\\\", section=\\\"enums\\\"),\\n     RunCase(\\\"control/static_literals\\\", \\\"-128\\\\n32767\\\\n255\\\\n18446744073709551615\\\\n\\\", section=\\\"control\\\"),\\n     RunCase(\\\"control/logical_loops\\\", \\\"0\\\\n1\\\\n2\\\\n3\\\\n9\\\\n4\\\\n\\\", section=\\\"control\\\"),\\n@@ -150,6 +160,30 @@ RUN_CASES = (\\n )\\n \\n REJECT_CASES = (\\n+    RejectCase(\\\"references/reject_alias_mutable_shared\\\", \\\"overlapping arguments cannot include a mutable borrow\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_alias_two_fields\\\", \\\"overlapping arguments cannot include a mutable borrow\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_later_read\\\", \\\"cannot read a place while it is mutably borrowed\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_later_write\\\", \\\"cannot write a place while it is borrowed\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_missing_ampersand\\\", \\\"reference argument requires explicit `&`\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_missing_var\\\", \\\"mutable reference argument requires `&var`\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_value_address\\\", \\\"value parameter does not accept an address argument\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_wrong_referent\\\", \\\"reference argument type does not match parameter\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_immutable_borrow\\\", \\\"cannot mutably borrow an immutable place\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_shared_to_mutable\\\", \\\"cannot mutably borrow an immutable place\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_mutate_shared\\\", \\\"cannot assign through a shared reference\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_reference_local\\\", \\\"reference locals are not supported\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_reference_field\\\", \\\"reference record fields are not supported\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_reference_result\\\", \\\"reference results are not supported\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_reference_static\\\", \\\"static type must be an integer or Bool\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_whole_record\\\", \\\"only scalar references are supported\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_nested_reference\\\", \\\"references to references are not supported\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_outer_borrow_preserved\\\", \\\"cannot read a place while it is mutably borrowed\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_spaced_nested_reference\\\", \\\"references to references are not supported\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/compound_lhs_snapshot\\\", \\\"cannot borrow an assignment destination while evaluating its right-hand side\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_assignment_rhs_shared\\\", \\\"cannot borrow an assignment destination while evaluating its right-hand side\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_assignment_rhs_mutable\\\", \\\"cannot borrow an assignment destination while evaluating its right-hand side\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_compound_rhs_borrow\\\", \\\"cannot borrow an assignment destination while evaluating its right-hand side\\\", \\\"references\\\"),\\n+    RejectCase(\\\"references/reject_nested_assignment_rhs_borrow\\\", \\\"cannot borrow an assignment destination while evaluating its right-hand side\\\", \\\"references\\\"),\\n     RejectCase(\\\"enums/reject_empty\\\", \\\"at least one case\\\", \\\"enums\\\"),\\n     RejectCase(\\\"enums/reject_record_collision\\\", \\\"duplicate module name\\\", \\\"enums\\\"),\\n     RejectCase(\\\"enums/reject_static_enum\\\", \\\"static type\\\", \\\"enums\\\"),\\n@@ -313,7 +347,7 @@ def main() -> int:\\n     parser.add_argument(\\\"--binary\\\", type=Path, default=Path(\\\".build/minivm/minivm\\\"))\\n     parser.add_argument(\\\"--clang\\\", default=\\\"clang\\\")\\n     parser.add_argument(\\n-        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\"), default=\\\"all\\\",\\n+        \\\"--section\\\", choices=(\\\"all\\\", \\\"core\\\", \\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\"), default=\\\"all\\\",\\n         help=\\\"run all cases or one isolated test section\\\",\\n     )\\n     args = parser.parse_args()\\n@@ -342,7 +376,7 @@ def main() -> int:\\n                 failures.extend(test_rejection(binary, case))\\n             except subprocess.TimeoutExpired:\\n                 failures.append(f\\\"{case.name}: timed out after {TIMEOUT}s\\\")\\n-        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\") else (\\n+        limit_cases = () if args.section in (\\\"numbers\\\", \\\"records\\\", \\\"control\\\", \\\"enums\\\", \\\"values\\\", \\\"references\\\") else (\\n             (\\\"budget\\\", 20, \\\"runtime error: instruction budget exceeded\\\"),\\n             (\\\"depth\\\", 10_000, \\\"runtime error: call depth exceeded\\\"),\\n         )\\ndiff --git a/prototypes/minivm/tests/enum_contract.saw b/prototypes/minivm/tests/enum_contract.saw\\nindex d38cbd8b..0cc1ee60 100644\\n--- a/prototypes/minivm/tests/enum_contract.saw\\n+++ b/prototypes/minivm/tests/enum_contract.saw\\n@@ -67,7 +67,7 @@ func base_program() -> Program {\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move identity_params,\\n         result: ValueType.Enum(0), slots: move identity_slots,\\n         code: move identity_code))\\n-    Program(enums: move enums, records: move records,\\n+    Program(references: Vector<ReferenceIR>(), enums: move enums, records: move records,\\n             functions: move functions, main_index: 0)\\n }\\n \\ndiff --git a/prototypes/minivm/tests/record_contract.saw b/prototypes/minivm/tests/record_contract.saw\\nindex ed1d0f70..98bfdbc7 100644\\n--- a/prototypes/minivm/tests/record_contract.saw\\n+++ b/prototypes/minivm/tests/record_contract.saw\\n@@ -46,7 +46,7 @@ func call_program(records: &Vector<RecordIR>) -> Program {\\n         slots: slots.copy(), code: move main_code))\\n     try! functions.push(FuncIR(name: \\\"identity\\\", params: move params, result: ValueType.Record(0),\\n         slots: move slots, code: move callee_code))\\n-    Program(enums: Vector<EnumIR>(), records: records.copy(), functions: move functions, main_index: 0)\\n+    Program(references: Vector<ReferenceIR>(), enums: Vector<EnumIR>(), records: records.copy(), functions: move functions, main_index: 0)\\n }\\n \\n func main() {\\ndiff --git a/prototypes/minivm/tests/reference_contract.saw b/prototypes/minivm/tests/reference_contract.saw\\nnew file mode 100644\\nindex 00000000..31cea401\\n--- /dev/null\\n+++ b/prototypes/minivm/tests/reference_contract.saw\\n@@ -0,0 +1,146 @@\\n+import src.model.*\\n+import src.verify.*\\n+import src.vm.*\\n+\\n+func valid(p: &Program) -> Bool {\\n+    try verify(&p) catch { return false }\\n+    true\\n+}\\n+\\n+func op(code: &var Vector<Instruction>, kind: Op, dst: Int, a: Int, b: Int) {\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    try! code.push(move i)\\n+}\\n+\\n+func base_program() -> Program {\\n+    var refs = Vector<ReferenceIR>()\\n+    try! refs.push(ReferenceIR(target: ValueType.I64, mutable: true))\\n+    try! refs.push(ReferenceIR(target: ValueType.I64, mutable: false))\\n+    try! refs.push(ReferenceIR(target: ValueType.Bool, mutable: true))\\n+    try! refs.push(ReferenceIR(target: ValueType.Enum(0), mutable: false))\\n+    var cases = Vector<String>()\\n+    try! cases.push(\\\"Only\\\")\\n+    var enums = Vector<EnumIR>()\\n+    try! enums.push(EnumIR(name: \\\"Tag\\\", cases: move cases, line: 1, col: 1))\\n+    let slots: Vector<ValueType> = [ValueType.I64, ValueType.Ref(0), ValueType.Ref(1),\\n+        ValueType.I64, ValueType.Bool, ValueType.Ref(2), ValueType.Enum(0), ValueType.Ref(3)]\\n+    var code = Vector<Instruction>()\\n+    op(&var code, Op.Const, 0, -1, -1)\\n+    op(&var code, Op.Addr, 1, 0, -1)\\n+    op(&var code, Op.Copy, 2, 1, -1)\\n+    op(&var code, Op.LoadRef, 3, 2, -1)\\n+    op(&var code, Op.StoreRef, -1, 1, 3)\\n+    op(&var code, Op.Const, 4, -1, -1)\\n+    op(&var code, Op.Addr, 5, 4, -1)\\n+    op(&var code, Op.Const, 6, -1, -1)\\n+    op(&var code, Op.Addr, 7, 6, -1)\\n+    op(&var code, Op.LoadRef, 6, 7, -1)\\n+    var call = instruction(Op.Call)\\n+    call.target = 1\\n+    call.dst = 3\\n+    try! call.args.push(2)\\n+    try! code.push(move call)\\n+    try! code.push(instruction(Op.Return))\\n+\\n+    var read_code = Vector<Instruction>()\\n+    op(&var read_code, Op.LoadRef, 1, 0, -1)\\n+    op(&var read_code, Op.Return, -1, 1, -1)\\n+    let read_slots: Vector<ValueType> = [ValueType.Ref(1), ValueType.I64]\\n+    let read_params: Vector<ValueType> = [ValueType.Ref(1)]\\n+    var funcs = Vector<FuncIR>()\\n+    try! funcs.push(FuncIR(name: \\\"main\\\", params: Vector<ValueType>(), result: ValueType.Void,\\n+        slots: move slots, code: move code))\\n+    try! funcs.push(FuncIR(name: \\\"read\\\", params: move read_params, result: ValueType.I64,\\n+        slots: move read_slots, code: move read_code))\\n+    Program(references: move refs, enums: move enums, records: Vector<RecordIR>(),\\n+        functions: move funcs, main_index: 0)\\n+}\\n+\\n+func rejected_op(p: &Program, index: Int, kind: Op, dst: Int, a: Int, b: Int) -> Bool {\\n+    var changed = p.copy()\\n+    var i = instruction(kind)\\n+    i.dst = dst\\n+    i.a = a\\n+    i.b = b\\n+    changed.functions[0].code.set(index, move i)\\n+    not valid(&changed)\\n+}\\n+\\n+func invalid_runtime_reference(p: &Program, handle: Int) -> Bool {\\n+    // Bypass verify deliberately to exercise the VM's defensive bounds guard.\\n+    // Such a reference Const is rejected by the verifier tests below.\\n+    var changed = p.copy()\\n+    changed.functions[0].code[1].op = Op.Const\\n+    changed.functions[0].code[1].immediate = handle\\n+    try execute(&changed, 1000) catch {\\n+        return error.message == \\\"runtime error: invalid reference\\\"\\n+    }\\n+    false\\n+}\\n+\\n+func main() {\\n+    let p = base_program()\\n+    assert(valid(&p), \\\"valid addresses, qualification, accesses, and call\\\")\\n+    try! execute(&p, 1000)\\n+    assert(reference_type(ValueType.Ref(0)), \\\"reference classification\\\")\\n+    assert(not scalar_type(ValueType.Ref(0)), \\\"reference is not scalar data\\\")\\n+    assert(not printable_type(ValueType.Ref(0)), \\\"reference is not printable\\\")\\n+    assert(not integer_type(ValueType.Ref(0)), \\\"reference is not an integer\\\")\\n+    assert((try! value_layout(&p.records, ValueType.Ref(0))).len() == 1, \\\"reference word layout\\\")\\n+    assert(reference_transfer(&p.references, ValueType.Ref(0), ValueType.Ref(1)), \\\"weaken mutability\\\")\\n+    assert(not reference_transfer(&p.references, ValueType.Ref(1), ValueType.Ref(0)), \\\"cannot strengthen\\\")\\n+    assert(not reference_transfer(&p.references, ValueType.Ref(0), ValueType.Ref(2)), \\\"same referent required\\\")\\n+    assert(not reference_transfer(&p.references, ValueType.Ref(-1), ValueType.Ref(1)), \\\"negative reference index\\\")\\n+    assert(not reference_mutable(&p.references, ValueType.Ref(99)), \\\"invalid reference is not writable\\\")\\n+\\n+    assert(rejected_op(&p, 1, Op.Addr, 1, 4, -1), \\\"Addr wrong referent\\\")\\n+    assert(rejected_op(&p, 1, Op.Addr, 1, -1, -1), \\\"Addr invalid source\\\")\\n+    assert(rejected_op(&p, 1, Op.Addr, 0, 0, -1), \\\"Addr requires reference destination\\\")\\n+    assert(rejected_op(&p, 3, Op.LoadRef, 4, 2, -1), \\\"LoadRef target mismatch\\\")\\n+    assert(rejected_op(&p, 3, Op.LoadRef, 3, 0, -1), \\\"LoadRef requires reference\\\")\\n+    assert(rejected_op(&p, 4, Op.StoreRef, -1, 2, 3), \\\"StoreRef shared reference\\\")\\n+    assert(rejected_op(&p, 4, Op.StoreRef, -1, 1, 4), \\\"StoreRef target mismatch\\\")\\n+    assert(rejected_op(&p, 2, Op.Copy, 1, 2, -1), \\\"Copy cannot strengthen\\\")\\n+    assert(rejected_op(&p, 2, Op.Copy, 1, 0, -1), \\\"Copy cannot invent reference\\\")\\n+    assert(rejected_op(&p, 2, Op.Const, 1, -1, -1), \\\"Const cannot invent reference\\\")\\n+    assert(rejected_op(&p, 2, Op.Cast, 0, 1, -1), \\\"Cast cannot expose address\\\")\\n+    assert(rejected_op(&p, 2, Op.Eq, 4, 1, 1), \\\"reference equality unsupported\\\")\\n+    assert(rejected_op(&p, 2, Op.Print, -1, 1, -1), \\\"cannot print address\\\")\\n+\\n+    var negative = p.copy()\\n+    negative.functions[0].slots.set(1, ValueType.Ref(-1))\\n+    assert(not valid(&negative), \\\"negative slot reference type\\\")\\n+    var high = p.copy()\\n+    high.functions[1].params.set(0, ValueType.Ref(99))\\n+    assert(not valid(&high), \\\"out-of-range parameter reference type\\\")\\n+    var returned = p.copy()\\n+    returned.functions[1].result = ValueType.Ref(1)\\n+    assert(not valid(&returned), \\\"no escaping reference results\\\")\\n+    var nested = p.copy()\\n+    nested.references[0].target = ValueType.Ref(1)\\n+    assert(not valid(&nested), \\\"no reference to reference\\\")\\n+    var void_ref = p.copy()\\n+    void_ref.references[0].target = ValueType.Void\\n+    assert(not valid(&void_ref), \\\"no Void reference\\\")\\n+    var record_ref = p.copy()\\n+    record_ref.references[0].target = ValueType.Record(0)\\n+    assert(not valid(&record_ref), \\\"no whole-record reference\\\")\\n+    var enum_ref = p.copy()\\n+    enum_ref.references[3].target = ValueType.Enum(99)\\n+    assert(not valid(&enum_ref), \\\"validate enum index in reference target\\\")\\n+    var wrong_call = p.copy()\\n+    wrong_call.functions[0].code[10].args.set(0, 1)\\n+    assert(not valid(&wrong_call), \\\"call requires explicit qualifying copy\\\")\\n+    var record_field = p.copy()\\n+    let names: Vector<String> = [\\\"ref\\\"]\\n+    let types: Vector<ValueType> = [ValueType.Ref(0)]\\n+    try! record_field.records.push(RecordIR(name: \\\"Stored\\\", field_names: move names,\\n+        field_types: move types, line: 1, col: 1))\\n+    assert(not valid(&record_field), \\\"no stored reference field\\\")\\n+    assert(invalid_runtime_reference(&p, -1), \\\"VM rejects negative handle\\\")\\n+    assert(invalid_runtime_reference(&p, 999), \\\"VM rejects out-of-range handle\\\")\\n+    print(\\\"reference contract passed\\\")\\n+}\\n\",\"id\":\"SL-249\",\"request_id\":\"mv2-SL-249-60f1e638-a1\",\"title\":\"minivm M6: scalar references (branch commit 60f1e638)\"}","request_id":"mv2-SL-249-60f1e638-a1","sequence":475,"timestamp":"1789096485"}
---
