---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791163","description":"## Scope and status\n\nDF-257d: Discover shorthand closure parameters inside try operands\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/234-fallibility-flip.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/234-fallibility-flip.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 83–83\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L83-L83)\n\n- DF‑257d — the `$0` closure shorthand is invisible to the implicit-parameter scan inside a `try` operand, so the closure infers arity 0 (entry below, filed Aug 25 by design 234 unit 3; PRE-EXISTING). Pinned XFAIL; the flip meets it because `try!` is the corpus migration spelling\n\n### sawlang TODO lines 1638–1665\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L1638-L1665)\n\n## DF‑257d — the `$0` SHORTHAND is invisible to the implicit-parameter scan\n## inside a `try` operand (filed Aug 25 by design 234 unit 3; PRE-EXISTING)\n\n```saw\nfunc fallible(n: Int) -> Result<Int, AllocError> { return n * 2 }\nfunc run(body: (Int) -> Void) { body(21) }\n\nrun({ print(\"plain {$0}\") })                      // fine\nrun({ print(\"forced {try! fallible($0)}\") })      // undefined variable `$0`\n// error: argument `body` expects `(Int) -> Void` but got `() -> Void`\n```\n\nMECHANISM (obligation 4): the walk that counts a closure's shorthand parameters\n— what turns `{ $0 * 2 }` into a one-argument closure — does not descend into a\n`TryExpr`'s operand, so a `$0` under `try`/`try!`/`try?` is neither counted nor\nbound. The closure is then built with NO parameters and refused at the argument\nposition, with a second error at the `$0` itself. The control in the pin is the\nsame `$0` with no `try` around it, so the `try` is the whole delta. The other\nexpression forms that WRAP an operand are the siblings a fix should sweep\n(`move`, a cast, `?.`) — each is a node the same walk has to enter.\n\nWHY DESIGN 234 MET IT: `try!` is the corpus's migration spelling and `$0`\nclosures are everywhere, so `v.each { [&var out] in out.push($0 * 2) }` becomes\n`v.each { [&var out] in try! out.push($0 * 2) }` and the shorthand disappears.\nTwo corpus sites take the named-parameter spelling meanwhile\n(`{ [&var out] n in try! out.push(n * 2) }`), each citing this entry. Pin:\n`examples/closure_shorthand_parameter_inside_a_try.saw` (XFAIL). [234, 19]\n","id":"SL-38","labels":["todo-import","backlog","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":38,"status":"open","title":"DF-257d: Discover shorthand closure parameters inside try operands","updated":"1788791163"},"before":null,"id":"SL-38","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-257d: Discover shorthand closure parameters inside try operands\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/234-fallibility-flip.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/234-fallibility-flip.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 83–83\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L83-L83)\\n\\n- DF‑257d — the `$0` closure shorthand is invisible to the implicit-parameter scan inside a `try` operand, so the closure infers arity 0 (entry below, filed Aug 25 by design 234 unit 3; PRE-EXISTING). Pinned XFAIL; the flip meets it because `try!` is the corpus migration spelling\\n\\n### sawlang TODO lines 1638–1665\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L1638-L1665)\\n\\n## DF‑257d — the `$0` SHORTHAND is invisible to the implicit-parameter scan\\n## inside a `try` operand (filed Aug 25 by design 234 unit 3; PRE-EXISTING)\\n\\n```saw\\nfunc fallible(n: Int) -> Result<Int, AllocError> { return n * 2 }\\nfunc run(body: (Int) -> Void) { body(21) }\\n\\nrun({ print(\\\"plain {$0}\\\") })                      // fine\\nrun({ print(\\\"forced {try! fallible($0)}\\\") })      // undefined variable `$0`\\n// error: argument `body` expects `(Int) -> Void` but got `() -> Void`\\n```\\n\\nMECHANISM (obligation 4): the walk that counts a closure's shorthand parameters\\n— what turns `{ $0 * 2 }` into a one-argument closure — does not descend into a\\n`TryExpr`'s operand, so a `$0` under `try`/`try!`/`try?` is neither counted nor\\nbound. The closure is then built with NO parameters and refused at the argument\\nposition, with a second error at the `$0` itself. The control in the pin is the\\nsame `$0` with no `try` around it, so the `try` is the whole delta. The other\\nexpression forms that WRAP an operand are the siblings a fix should sweep\\n(`move`, a cast, `?.`) — each is a node the same walk has to enter.\\n\\nWHY DESIGN 234 MET IT: `try!` is the corpus's migration spelling and `$0`\\nclosures are everywhere, so `v.each { [&var out] in out.push($0 * 2) }` becomes\\n`v.each { [&var out] in try! out.push($0 * 2) }` and the shorthand disappears.\\nTwo corpus sites take the named-parameter spelling meanwhile\\n(`{ [&var out] n in try! out.push(n * 2) }`), each citing this entry. Pin:\\n`examples/closure_shorthand_parameter_inside_a_try.saw` (XFAIL). [234, 19]\\n\",\"labels\":[\"todo-import\",\"backlog\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-1128ed04d10495a15f2f-create\",\"title\":\"DF-257d: Discover shorthand closure parameters inside try operands\"}","request_id":"todo-20260907-1128ed04d10495a15f2f-create","sequence":38,"timestamp":"1788791163"}
---
