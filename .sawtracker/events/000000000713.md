---
{"action":"close","actor":"agent:claude-sawlang","after":{"assignee":"agent:claude-sl204","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"RULINGS (user, Sep 12, via the design-272 decision list): D2-2 APPROVED — no_delay DEFAULTS ON for TcpStream (Go-style; visible labeled opt-out). Lead-endorsed as determined: D2-1 (keep listen, collapse the overloads into one default-carrying signature), D2-3 (v6_only OUT — std.net is IPv4-only end to end, nothing to be dual about), D2-4 (IN: backlog default 16, keepalive enable; OUT: keepalive interval, reuse_port, buffer sizes, linger, raw setsockopt), D2-5 (re-point net_listen_bind_address.saw at an exact-address collision + an opt-out row; re-verify the kernel probe on Linux before finalising).","created":"1789214588","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-229.p1 (revision 1): design 272 unit 2: socket options — reuse_address and no_delay default ON, per-socket SIGPIPE suppression, two additive seams (SL-229)","created":"1789228690","id":"c2"},{"author":"agent:claude-sawlang","body":"SL-229.p1 SUBMITTED (design 272 unit 2, delta of trio commit f7c677a7, applies clean on merged unit 1), server gate queued. Per rulings D2-1..5 + D3-3: listen keeps its name with one default-carrying signature; reuse_address AND no_delay default ON (labeled opt-outs); curated set = backlog (16, unchanged) + keepalive enable; per-socket SIGPIPE suppression (SO_NOSIGPIPE / MSG_NOSIGNAL) so EPIPE surfaces as an ordinary IoError Result; OUT: keepalive interval, reuse_port, buffers, linger, raw setsockopt, v6_only (std.net is IPv4-only end to end). TWO ADDITIVE frozen seams (__saw_rt_tcp_listen_with, __saw_rt_socket_set_option) — rt/ABI.md travels in the same diff, abidoc exact at 65. Consumer sweep: the ruled default-flip breaks exactly one test, re-pointed per D2-5 at the exact-address collision both kernels refuse (darwin kernel-probed; the Linux wildcard-overlap cell is documented as unverified and nothing is written against it — probe_reuseaddr.py is the instrument if the reviewer wants it closed server-side). Unit 3 (SL-228) follows on this merge.","created":"1789228702","id":"c3"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-229.p1 (revision 1)","created":"1789229459","id":"c4"},{"author":"agent:codex-reviewer","body":"Approved patch SL-229.p1 (revision 1)\n\nApproved revision 1 (base bad02f7f, applied/tested 8ef04f7c). No blocking findings.\n\nReviewed the bind-time options, accepted/dialled TCP defaults, portable option mapping and error propagation, and socket-send SIGPIPE suppression. The two new runtime seams preserve existing signatures, and the ABI documentation check matches all 65 frozen seam signatures.\n\nValidation on the exact base plus patch: the three focused network fixtures pass; the added SL-273 compiler pin remains an expected failure. An independent macOS getsockopt probe confirms reuse-address enabled by default and disabled by the opt-out, no-delay enabled on accepted and dialled connections, and the no-delay/keepalive setters changing actual kernel values. Closed-peer writes return BrokenPipe and leave the process running. Citation checks and git diff --check pass. Sawtracker reports tests passed for 8ef04f7c.\n\nLocal runtime validation was on macOS; the Linux implementation was reviewed statically, not independently executed here.\n","created":"1789230795","id":"c5"},{"author":"agent:claude-sawlang","body":"Merged patch SL-229.p1 (revision 1) as 8ad2b7c7\n\nMerged under the Sep 11 standing delegation: codex-reviewer approved, server gate passed. Design 272 unit 2 of 3.","created":"1789230881","id":"c6"},{"author":"agent:claude-sawlang","body":"LANDED: SL-229.p1 merged (8ad2b7c7, Sep 12) — design 272 unit 2. Socket options per rulings D2-1..5 + D3-3: listen keeps one default-carrying signature; reuse_address AND no_delay default ON (labeled opt-outs; server restarts just work, no Nagle/delayed-ACK stall); curated set = backlog + keepalive enable; per-socket SIGPIPE suppression (SO_NOSIGPIPE / MSG_NOSIGNAL) — a peer reset surfaces as EPIPE in a Result, never kills the process. Two additive frozen seams (__saw_rt_tcp_listen_with, __saw_rt_socket_set_option), rt/ABI.md updated in the same patch, abidoc exact at 65. The one default-flip-broken test re-pointed at the exact-address collision; the Linux wildcard-overlap cell documented as unverified with the probe instrument recorded. Unit 3 (SL-228 std.signal) submitting now.","created":"1789230889","id":"c7"}],"created":"1789050408","description":"MOTIVATION: restarting a server (sawtracker) can fail EADDRINUSE against the dead process's TIME_WAIT socket — there is no way to set SO_REUSEADDR, or any socket option, from Saw today.\n\nRULED DESIGN (user, Sep 10 2026):\n1. reuse_address DEFAULTS ON for TcpListener.bind (Go-style restart-just-works; the flag remains a visible labeled parameter — reuse_address: false opts out). Obligation 2: flipping the default is a behavioral-contract change — the brief owes the consumer sweep (blade, examples/ servers, devtools, the test harnesses).\n2. SHIPS AS the third member of the server-hardening trio with SL-204 (io_wait deadline) + SL-228 (std.signal): one dispatch window at the queue tail, one shared brief if the lead finds they compose.\n\nSURFACE SKETCH (brief refines): no builder/options object — Saw's labeled arguments with default values ARE the options surface. Bind-time (set between socket() and bind()): TcpListener.bind(host, port, reuse_address: true, reuse_port: false, backlog: <current default>, v6_only: <ruling needed for dual-stack>). Per-connection, Result-returning setters on TcpStream (never hide errors, no silent degradation): no_delay (Nagle — whether it defaults on like Go is a brief-time ruling), keepalive enable + interval (Duration). Receive/send buffer sizes and linger: brief decides in or out — curated, not exhaustive; UnsafeSocket-style raw setsockopt escape hatch is OUT unless a consumer demands it. Full words per the no-abbreviations doctrine (reuse_address, never reuseaddr).\n\nMECHANISM: hosted-only like the rest of std.net; one new status-carrying setsockopt-shaped op in rt/common/os_ops.saw + host net_os.saw errno mapping; rt/ABI.md is frozen so any seam addition goes through the abidoc battery lane. Freestanding unaffected.","id":"SL-229","labels":["feature","stdlib"],"priority":"normal","project":"SL","revision":9,"sequence":713,"stage":"queued","status":"closed","title":"std.net socket options: reuse_address default ON at listener bind, plus a curated Result-returning option set","updated":"1789230889"},"before":{"assignee":"agent:claude-sl204","author":"agent:claude-sawlang","comments":[{"author":"agent:claude-sawlang","body":"RULINGS (user, Sep 12, via the design-272 decision list): D2-2 APPROVED — no_delay DEFAULTS ON for TcpStream (Go-style; visible labeled opt-out). Lead-endorsed as determined: D2-1 (keep listen, collapse the overloads into one default-carrying signature), D2-3 (v6_only OUT — std.net is IPv4-only end to end, nothing to be dual about), D2-4 (IN: backlog default 16, keepalive enable; OUT: keepalive interval, reuse_port, buffer sizes, linger, raw setsockopt), D2-5 (re-point net_listen_bind_address.saw at an exact-address collision + an opt-out row; re-verify the kernel probe on Linux before finalising).","created":"1789214588","id":"c1"},{"author":"agent:claude-sawlang","body":"Submitted patch SL-229.p1 (revision 1): design 272 unit 2: socket options — reuse_address and no_delay default ON, per-socket SIGPIPE suppression, two additive seams (SL-229)","created":"1789228690","id":"c2"},{"author":"agent:claude-sawlang","body":"SL-229.p1 SUBMITTED (design 272 unit 2, delta of trio commit f7c677a7, applies clean on merged unit 1), server gate queued. Per rulings D2-1..5 + D3-3: listen keeps its name with one default-carrying signature; reuse_address AND no_delay default ON (labeled opt-outs); curated set = backlog (16, unchanged) + keepalive enable; per-socket SIGPIPE suppression (SO_NOSIGPIPE / MSG_NOSIGNAL) so EPIPE surfaces as an ordinary IoError Result; OUT: keepalive interval, reuse_port, buffers, linger, raw setsockopt, v6_only (std.net is IPv4-only end to end). TWO ADDITIVE frozen seams (__saw_rt_tcp_listen_with, __saw_rt_socket_set_option) — rt/ABI.md travels in the same diff, abidoc exact at 65. Consumer sweep: the ruled default-flip breaks exactly one test, re-pointed per D2-5 at the exact-address collision both kernels refuse (darwin kernel-probed; the Linux wildcard-overlap cell is documented as unverified and nothing is written against it — probe_reuseaddr.py is the instrument if the reviewer wants it closed server-side). Unit 3 (SL-228) follows on this merge.","created":"1789228702","id":"c3"},{"author":"agent:sawtracker-tests","body":"Tests passed for SL-229.p1 (revision 1)","created":"1789229459","id":"c4"},{"author":"agent:codex-reviewer","body":"Approved patch SL-229.p1 (revision 1)\n\nApproved revision 1 (base bad02f7f, applied/tested 8ef04f7c). No blocking findings.\n\nReviewed the bind-time options, accepted/dialled TCP defaults, portable option mapping and error propagation, and socket-send SIGPIPE suppression. The two new runtime seams preserve existing signatures, and the ABI documentation check matches all 65 frozen seam signatures.\n\nValidation on the exact base plus patch: the three focused network fixtures pass; the added SL-273 compiler pin remains an expected failure. An independent macOS getsockopt probe confirms reuse-address enabled by default and disabled by the opt-out, no-delay enabled on accepted and dialled connections, and the no-delay/keepalive setters changing actual kernel values. Closed-peer writes return BrokenPipe and leave the process running. Citation checks and git diff --check pass. Sawtracker reports tests passed for 8ef04f7c.\n\nLocal runtime validation was on macOS; the Linux implementation was reviewed statically, not independently executed here.\n","created":"1789230795","id":"c5"},{"author":"agent:claude-sawlang","body":"Merged patch SL-229.p1 (revision 1) as 8ad2b7c7\n\nMerged under the Sep 11 standing delegation: codex-reviewer approved, server gate passed. Design 272 unit 2 of 3.","created":"1789230881","id":"c6"}],"created":"1789050408","description":"MOTIVATION: restarting a server (sawtracker) can fail EADDRINUSE against the dead process's TIME_WAIT socket — there is no way to set SO_REUSEADDR, or any socket option, from Saw today.\n\nRULED DESIGN (user, Sep 10 2026):\n1. reuse_address DEFAULTS ON for TcpListener.bind (Go-style restart-just-works; the flag remains a visible labeled parameter — reuse_address: false opts out). Obligation 2: flipping the default is a behavioral-contract change — the brief owes the consumer sweep (blade, examples/ servers, devtools, the test harnesses).\n2. SHIPS AS the third member of the server-hardening trio with SL-204 (io_wait deadline) + SL-228 (std.signal): one dispatch window at the queue tail, one shared brief if the lead finds they compose.\n\nSURFACE SKETCH (brief refines): no builder/options object — Saw's labeled arguments with default values ARE the options surface. Bind-time (set between socket() and bind()): TcpListener.bind(host, port, reuse_address: true, reuse_port: false, backlog: <current default>, v6_only: <ruling needed for dual-stack>). Per-connection, Result-returning setters on TcpStream (never hide errors, no silent degradation): no_delay (Nagle — whether it defaults on like Go is a brief-time ruling), keepalive enable + interval (Duration). Receive/send buffer sizes and linger: brief decides in or out — curated, not exhaustive; UnsafeSocket-style raw setsockopt escape hatch is OUT unless a consumer demands it. Full words per the no-abbreviations doctrine (reuse_address, never reuseaddr).\n\nMECHANISM: hosted-only like the rest of std.net; one new status-carrying setsockopt-shaped op in rt/common/os_ops.saw + host net_os.saw errno mapping; rt/ABI.md is frozen so any seam addition goes through the abidoc battery lane. Freestanding unaffected.","id":"SL-229","labels":["feature","stdlib"],"priority":"normal","project":"SL","revision":8,"sequence":712,"stage":"queued","status":"open","title":"std.net socket options: reuse_address default ON at listener bind, plus a curated Result-returning option set","updated":"1789230881"},"id":"SL-229","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"LANDED: SL-229.p1 merged (8ad2b7c7, Sep 12) — design 272 unit 2. Socket options per rulings D2-1..5 + D3-3: listen keeps one default-carrying signature; reuse_address AND no_delay default ON (labeled opt-outs; server restarts just work, no Nagle/delayed-ACK stall); curated set = backlog + keepalive enable; per-socket SIGPIPE suppression (SO_NOSIGPIPE / MSG_NOSIGNAL) — a peer reset surfaces as EPIPE in a Result, never kills the process. Two additive frozen seams (__saw_rt_tcp_listen_with, __saw_rt_socket_set_option), rt/ABI.md updated in the same patch, abidoc exact at 65. The one default-flip-broken test re-pointed at the exact-address collision; the Linux wildcard-overlap cell documented as unverified with the probe instrument recorded. Unit 3 (SL-228 std.signal) submitting now.\",\"id\":\"SL-229\",\"request_id\":\"pid-99406-1789230889-8000\"}","request_id":"pid-99406-1789230889-8000","sequence":713,"timestamp":"1789230889"}
---
