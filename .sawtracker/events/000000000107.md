---
{"action":"create","actor":"agent:codex-todo-import","after":{"assignee":"","author":"agent:codex-todo-import","comments":[],"created":"1788791241","description":"## Scope and status\n\nDF-180a (OPEN, filed Aug 8): a static and an instance method cannot share a name. Duration.secs(2) (construct) and d.secs() (project) are never ambiguous at a…\n\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\n\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\n\nScheduling: backlog; no new implementation order is assigned by this import.\n\n## Proposed plan\n\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\n\n## Acceptance criteria\n\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\n\n## Existing design references\n\n- [sawlang/designs/180-duration-sleep.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/180-duration-sleep.md)\n\n## Source context\n\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\n\n### sawlang TODO lines 7342–7355\n\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7342-L7355)\n\n- **DF‑180a (OPEN, filed Aug 8): a static and an instance method cannot share\n  a name.** `Duration.secs(2)` (construct) and `d.secs()` (project) are never\n  ambiguous at a call site — one names the type, the other a value — but\n  declaring both is rejected: ``method `secs` is already defined for struct\n  `Duration` with an indistinguishable signature``, hinted \"overloads must\n  differ in arity or parameter types\". The distinguishability check does not\n  consider whether a method has a `self` receiver, though resolution reaches\n  the two through separate paths. It cost design 180 the accessor names the\n  brief asked to keep: the family was renamed `as_nanos` / `as_micros` /\n  `as_millis` / `as_secs` so the constructors could be `ns` / `us` / `ms` /\n  `secs`. That reads well (bare name constructs, `as_` projects) and is what\n  Rust does, so this is not urgent — but the rule as written rejects a\n  program with no ambiguity in it, and a receiver-aware key looks small.\n","id":"SL-107","labels":["todo-import","needs-verification","bug","plan"],"priority":"normal","project":"SL","revision":1,"sequence":107,"status":"open","title":"DF-180a (OPEN, filed Aug 8): a static and an instance method cannot share a name. Duration.secs(2) (construct) and d.secs() (project) are never ambiguous at a…","updated":"1788791241"},"before":null,"id":"SL-107","project":"SL","request":"{\"action\":\"create\",\"actor\":\"agent:codex-todo-import\",\"description\":\"## Scope and status\\n\\nDF-180a (OPEN, filed Aug 8): a static and an instance method cannot share a name. Duration.secs(2) (construct) and d.secs() (project) are never ambiguous at a…\\n\\nHistorical TODO entry: reconcile the latest brief, done records and current implementation before scheduling. A source report is not a fresh reproduction. Close with evidence if already resolved; preserve only any named remainder.\\n\\nImported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.\\n\\nScheduling: backlog; no new implementation order is assigned by this import.\\n\\n## Proposed plan\\n\\n1. Reproduce the scoped symptom with the source’s minimal example on the current compiler/pin; record the exact command and expected/actual behavior.\\n2. Trace the common checker/lowering/runtime path named in the source and fix the mechanism, including the sibling entry points that share it.\\n3. Promote the reproducer to a regression test, update affected consumers/docs, and run the focused matrix plus the repository’s required checks.\\n4. Cover bare and explicit types, same-module and imported calls, relevant argument/return/field positions, and genuinely ambiguous or out-of-range negative controls.\\n\\n## Acceptance criteria\\n\\n- [ ] The original case produces the specified behavior or a source-anchored diagnostic, without a compiler crash.\\n- [ ] The relevant control cases and sibling positions still behave correctly; test/commit evidence is recorded before closure.\\n\\n## Existing design references\\n\\n- [sawlang/designs/180-duration-sleep.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/180-duration-sleep.md)\\n\\n## Source context\\n\\nHistorical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.\\n\\n### sawlang TODO lines 7342–7355\\n\\n[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L7342-L7355)\\n\\n- **DF‑180a (OPEN, filed Aug 8): a static and an instance method cannot share\\n  a name.** `Duration.secs(2)` (construct) and `d.secs()` (project) are never\\n  ambiguous at a call site — one names the type, the other a value — but\\n  declaring both is rejected: ``method `secs` is already defined for struct\\n  `Duration` with an indistinguishable signature``, hinted \\\"overloads must\\n  differ in arity or parameter types\\\". The distinguishability check does not\\n  consider whether a method has a `self` receiver, though resolution reaches\\n  the two through separate paths. It cost design 180 the accessor names the\\n  brief asked to keep: the family was renamed `as_nanos` / `as_micros` /\\n  `as_millis` / `as_secs` so the constructors could be `ns` / `us` / `ms` /\\n  `secs`. That reads well (bare name constructs, `as_` projects) and is what\\n  Rust does, so this is not urgent — but the rule as written rejects a\\n  program with no ambiguity in it, and a receiver-aware key looks small.\\n\",\"labels\":[\"todo-import\",\"needs-verification\",\"bug\",\"plan\"],\"priority\":\"normal\",\"project\":\"SL\",\"request_id\":\"todo-20260907-b599bbb5ddc82b0b2022-create\",\"title\":\"DF-180a (OPEN, filed Aug 8): a static and an instance method cannot share a name. Duration.secs(2) (construct) and d.secs() (project) are never ambiguous at a…\"}","request_id":"todo-20260907-b599bbb5ddc82b0b2022-create","sequence":107,"timestamp":"1788791241"}
---
