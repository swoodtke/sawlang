---
{"action":"close","actor":"agent:claude-sawlang","after":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"1790341662","comments":[{"author":"agent:claude-sawlang","body":"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): tooling for the frozen Python compiler.","created":"1790341662","id":"c1","kind":"landing"}],"created":"1789083574","description":"FROM the SL-222.p1 review loop (rounds r3-r5, Sep 10): codex-reviewer caught three ownership failures in the coroutine transform (early-exit skipped clears, reinit-fallthrough claim erasure, hoist losing consumingness) with ONE method — instrument deinit with a print, run the driven form and its sync twin, and diff the drop traces. The sync twin IS the spec for the driven form: same program minus the suspension must produce the identical destruction sequence (names and order), and any divergence is a transform ownership bug — a lost drop is a leak, an extra drop is a double free, a reordered drop is a scope error.\n\nWANTED: a standing harness lane generalizing that oracle, so this class is machine-caught instead of reviewer-caught. Shape sketch (implementer refines): extend tools/corodiff (it already builds driven/sync pairs) or add a sibling lane that, for each corpus program in the coro family, synthesizes a NoCopy-instrumented variant (deinit printing the value's name), compiles and runs BOTH forms, and diffs the traces exactly. Needs: deterministic trace ordering (single-task programs, or per-task trace tagging), a known-ledger file for cited divergences (same discipline as corodiff_known.txt — a fix removes the entry in the same commit), and battery membership.\n\nEVIDENCE this pays: every one of r3/r4/r5's findings reproduces as a one-line trace diff (drop old missing, drop old doubled, compile refusal on the NoCopy variant); none was visible to the existing corodiff/suite oracles, and the SL-222 fixture corpus (coro_head_move_relinquishes_before_early_exit.saw's named-drop grid) is the seed corpus. Relationship to the SL-209 epic: this is the DYNAMIC check; SL-214's pre-codegen verifier is the STATIC one (its checked properties should include 'every frame claim relinquished exactly once on every path'). The two catch the same class from opposite sides; the lane is buildable now, before unit E lands.","id":"SL-241","labels":["frozen","tooling"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":3,"sequence":2106,"stage":"backlog","status":"closed","title":"corodiff lane: driven-vs-sync deinit-trace differential — the sync twin is the ownership oracle","updated":"1790341662"},"before":{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","closed":"","comments":[],"created":"1789083574","description":"FROM the SL-222.p1 review loop (rounds r3-r5, Sep 10): codex-reviewer caught three ownership failures in the coroutine transform (early-exit skipped clears, reinit-fallthrough claim erasure, hoist losing consumingness) with ONE method — instrument deinit with a print, run the driven form and its sync twin, and diff the drop traces. The sync twin IS the spec for the driven form: same program minus the suspension must produce the identical destruction sequence (names and order), and any divergence is a transform ownership bug — a lost drop is a leak, an extra drop is a double free, a reordered drop is a scope error.\n\nWANTED: a standing harness lane generalizing that oracle, so this class is machine-caught instead of reviewer-caught. Shape sketch (implementer refines): extend tools/corodiff (it already builds driven/sync pairs) or add a sibling lane that, for each corpus program in the coro family, synthesizes a NoCopy-instrumented variant (deinit printing the value's name), compiles and runs BOTH forms, and diffs the traces exactly. Needs: deterministic trace ordering (single-task programs, or per-task trace tagging), a known-ledger file for cited divergences (same discipline as corodiff_known.txt — a fix removes the entry in the same commit), and battery membership.\n\nEVIDENCE this pays: every one of r3/r4/r5's findings reproduces as a one-line trace diff (drop old missing, drop old doubled, compile refusal on the NoCopy variant); none was visible to the existing corodiff/suite oracles, and the SL-222 fixture corpus (coro_head_move_relinquishes_before_early_exit.saw's named-drop grid) is the seed corpus. Relationship to the SL-209 epic: this is the DYNAMIC check; SL-214's pre-codegen verifier is the STATIC one (its checked properties should include 'every frame claim relinquished exactly once on every path'). The two catch the same class from opposite sides; the lane is buildable now, before unit E lands.","id":"SL-241","labels":["frozen","tooling"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":2,"sequence":2105,"stage":"backlog","status":"open","title":"corodiff lane: driven-vs-sync deinit-trace differential — the sync twin is the ownership oracle","updated":"1790341662"},"id":"SL-241","project":"SL","request":"{\"action\":\"close\",\"actor\":\"agent:claude-sawlang\",\"body\":\"Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): tooling for the frozen Python compiler.\",\"id\":\"SL-241\",\"request_id\":\"pid-55669-1790341662-6000\"}","request_id":"pid-55669-1790341662-6000","sequence":2106,"timestamp":"1790341662"}
---
