---
{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","body_bytes":1115,"closed":"1790341659","created":"1788966183","id":"SL-214","labels":["superseded"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":4,"sequence":2088,"stage":"backlog","status":"closed","title":"Ownership Unit E: mandatory pre-codegen transfer-decision verifier + matrix (SL-209)","updated":"1790341659"}
---


## Description

Unit E of SL-209 (ownership uniformity). Plan step 7. LAST in the sequence — makes completeness mechanically enforced.

## Make completeness mechanically testable
Add a VERIFIER before codegen that rejects missing, stale, or incompatible transfer decisions. Then test the interaction matrix across:
- Sources: owned parameters, locals, captures, fields, indexed places, and borrowed parameters.
- Copy tiers: every tier, including refcounted aggregates.
- Positions: bindings, arguments, explicit returns, tails, branches, wrappers, and loop results.
- Variants: generic/concrete and sync/suspending.

Test oracles:
- Compile-error assertions for illegal transfers.
- Destructor/refcount assertions for legal ones.
- Guard Malloc for lifetime failures.
- Tests that DELIBERATELY remove a decision and prove the verifier catches it.

## Completion criterion (for the whole SL-209 initiative)
A new expression form cannot silently bypass ownership checking: it MUST declare how it produces or forwards a value before codegen accepts it. The verifier is what turns that from a convention into an enforced invariant.


## Comments

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":563,"created":"1789083580","id":"c1"} -->
From the SL-222.p1 review loop (Sep 10): this unit's verifier should include 'every frame claim relinquished exactly once on every path through the transformed body' as a checked property — review rounds r3/r4/r5 were each a violation of it (early-exit skipped clears, reinit-fallthrough claim erasure, hoist losing transfer), all human-caught. SL-241 files the DYNAMIC twin (driven-vs-sync deinit-trace differential lane); this verifier is the static side of the same class. The SL-222 branch's named-drop grid fixtures are ready-made acceptance rows for both.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":190,"created":"1790341659","id":"c2","kind":"landing"} -->
Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): superseded by architecture §3.0 (boundary verifiers), §3.5, §3.9. Its test-plan input is carried in SL-393 (the rule inventory).

