---
{"acceptance":[],"assignee":"agent:claude-air-sawlang","author":"agent:claude-air-sawlang","body_bytes":1831,"closed":"1790341684","created":"1790192960","id":"SL-368","labels":["frozen","hazard","soundness"],"order":1,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":8,"sequence":2231,"stage":"backlog","status":"closed","title":"A &self method called directly on a pointer-index receiver (p[0].store(v) with p: UnsafePointer<Atomic<Int>>) runs on a spilled copy: the Atomic store is lost and the CAS acts on stale data","updated":"1790341684"}
---


## Description

Found by the SL-355 implementer (it blocks SL-355 unit 3, the ruled Atomic wake word). I reproduced it on main 8d2c0563.

**Repro.** `.build/scratch/atomic_ptr_index.saw`, with `p: UnsafePointer<Atomic<Int>>` from `GlobalAllocator().alloc`:
```saw
p[0].store(5)
print(p[0].load())             // prints 0, not 5
p[0].compare_exchange(5, 9)    // false; the value stays 0
```

**Probed positions:**
| Receiver | Result |
|---|---|
| `p[0].store(v)` (direct pointer-index receiver) | WRONG: the store is lost |
| `pc[0].n.store(7)` (field of a pointer-indexed struct) | correct, reads back 7 |
| a static `COUNTER.store(3)` | correct |

**Mechanism (as the implementer named it; confirm before fixing).** `_receiver_storage_pointer` in codegen/calls.py resolves a receiver to its storage address for `&self` and `&var self` calls, but has no `ArrayIndex` case. A pointer-index receiver therefore falls back to evaluating the element by value into a temporary, and the method runs on that temporary. A plain `&self` read on a trivial type gives the right answer from a copy, which hides the bug. Any interior-mutability type (Atomic; any cell-carrying type, since design 186) silently loses its write.

**Sweep owed (obligation 4).** Every receiver shape that reaches this helper and its siblings:
- `p[i]` over UnsafePointer and UnsafeMutablePointer;
- a `Vector` place `v[i]` and a `Data` place;
- a `borrows` accessor place;
- a tuple index;
- `&var self` methods on the same shapes (does a mutating method also lose its write through `p[0].m()`?);
- SpinLock and Mutex, whose lock word is a cell;
- the coroutine-transform path, if receivers are re-lowered there.

**Pin.** The table above as EXPECT rows, plus the SL-355 shape (a CAS through `p[0]` from two threads).

This blocks SL-355 unit 3 (ruling R5, the atomic CAS latch).

## Comments

<!-- sawtracker:comment {"author":"agent:claude-air-sawlang","body_bytes":257,"created":"1790193037","id":"c1"} -->
USER RULING (Sep 23, revised): SL-368 lands in the SAME patch as SL-355 (and SL-354). It is built on the sl355-park-word branch as its own commit(s), ahead of SL-355 unit 3. One patch, one gate. The patch goes on SL-355; this issue closes with that landing.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":744,"created":"1790193106","id":"c2"} -->
LEAD BISECT (Sep 23): pre-existing — the repro (p[0] = Atomic(0); p[0].store(5); load; compare_exchange(5, 9); over GlobalAllocator().alloc storage) prints load 0 / cas false / after 0 identically on main 455dae1d (post-SL-350) and on 73dd98b8 (pre-SL-350). SL-350's receiver-load removal in calls.py is NOT in the causal chain; the mechanism is _receiver_storage_pointer's missing ArrayIndex case (a pointer-index receiver spilled by value), as filed. Member of the must-fix epic SL-367 at order 1, ahead of SL-355 which it gates. The obligation-4 sweep in the description is the fix's test plan; add the &var self shapes and the coroutine-transform re-lowering path to it explicitly. Lead repro: the Studio's .build/scratch/sl368_repro.saw.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":358,"created":"1790341684","id":"c3","kind":"landing"} -->
Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): a defect in the Python compiler, which is frozen (Sep 24). The frozen compiler stays the differential oracle, and this issue is its "oracle known wrong" reference. Its shape is carried into SL:hazards, so the new compiler's own source avoids it. Test-coverage aspect: `receiver.pointer-index-place`.

