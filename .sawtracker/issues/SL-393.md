---
{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","body_bytes":2466,"closed":"","created":"1790341527","id":"SL-393","labels":["language","rule-inventory"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":6,"sequence":2554,"stage":"backlog","status":"open","title":"Rule inventory: rulings and test inputs carried from the tracker cleanup","updated":"1790353243"}
---


## Description

Carried forward by the Sep 25 tracker cleanup (SL:tracker-cleanup), so closing the original issues loses nothing. The rule inventory (SL:testing §6) takes these in when it is built.

## Rulings recorded only in tracker issues

Each goes into the rule inventory, and into LANGUAGE_SPEC where the spec is silent or wrong:

- **SL-59:** statement arms in `match` (design 259 R7′).
- **SL-73:** the general postfix call (ruled Sep 23). SL:architecture §3.2 covers its parse, not its typing rule.
- **SL-309:** `??` or a second `?` in a cast target is refused, and the teaching form is `(n ?? 9) as Int`. LANGUAGE_SPEC still teaches "operator wins" for `x as Int? ?? y` (around line 2152), so the spec text is corrected.
- **SL-310:** a bare trailing closure on a free function is a call (R6).
- **SL-45:** design 259 R2 (a negative tail after an if-return).
- **SL-2 (design 274 §4):** N2 is SL-309. U5′ (inventory snapshot freshness) is still owed. B1 is settled: SL-347 refuses `x = 1 y`.
- **SL-380 c2:** postfix hops charge the 256 depth budget per hop (now in SL:architecture §3.2).
- **SL-105:** design 200's carve-out is retired (SL:borrowing §9). Conformance rows M28 and M32 become refusals, annotated "language changed".

## Rules the spec is silent on

- float-literal underflow: is a literal that rounds to 0.0 or to a subnormal an error? (SL-68)
- `break` / `continue` inside a closure body (SL-286; visitor APIs stay, so non-local exits from `each` may be asked for)
- `escaping` inside an optional parameter type (SL-293)
- a bare `None` arm at every nested-optional destination (SL-264, extending DF-289d beyond tails)

## Test-plan inputs

- SL-214's interaction matrix: sources × copy tiers × positions × generic/concrete × sync/suspending.
- SL-175's four owed TaskGroup rows: same-thread `Arc<Box<TaskGroup>>` accept, the MT-crossing refusal, the `static` TaskGroup refusal, the `Arc<Mutex<Box<TaskGroup>>>` refusal.
- Branch `sl340`: conformance rows K170–K179.
- SL-390's table of alias- and depth-bounded walks.
- SL-353's parksoak stress kit (a stress test, per SL:testing §4).
- `examples/conformance/INDEX.md` row K38 still says "OPEN, XFAIL citing DF-206d", but its test has no XFAIL marker.
- `examples/coro_try_block_two_error_types.saw` expects a refusal that exists only because of the old coroutine transform's limit. The new compiler accepts that program, so the file is annotated "language changed" in the differential.


## Comments

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":302,"created":"1790344910","id":"c1"} -->
Scope addition (Air, SL:tracker-cleanup t6): SL-309's correction also covers the saw-lang skill, whose Optionals section teaches the same stale rule ('EXCEPT in an as cast target, where the operator wins'), and README if it teaches it. The docs convention moves the spec, the skill and README together.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":522,"created":"1790349539","id":"c2"} -->
Carried ruling (user, Sep 25): compile-time evaluation is a function declared constexpr in the post-parameter effect slot (func f(...) constexpr -> T), beside unsafe and sync. It replaces the spec's planned const func. It is a contextual keyword and implies sync (writing both is refused), and it runs on the MIR interpreter. The v1 allowed set, the constant positions and the evaluation rules are in SL:architecture §3.10. LANGUAGE_SPEC's Compile-Time Evaluation section, the saw-lang skill and GRAMMAR.md move together.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":606,"created":"1790349803","id":"c3"} -->
Carried rulings (user, Sep 25; SL:architecture §3.10 r39). These go into LANGUAGE_SPEC's constant-expression and Const generics sections, and the skill: (1) constant arithmetic is ordinary typed arithmetic, retiring design 185's signed platform-Int fold domain, so static X: UInt64 = 1 << 63 is 2^63; (2) sizeof<Struct>() is legal in constant positions, including array lengths, with one acyclic graph over layouts and constants. The corpus refusals of the old behaviour (design 185, DF-283c, and the spec's 'array length is not a compile-time constant' for struct sizes) are annotated 'language changed'.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":1615,"created":"1790352946","id":"c4"} -->
Carry-forward from the SL:grammar rulings (SL-400 c6, with Q15 amended in c7). LANGUAGE_SPEC.md and the saw-lang skill owe these changes:

**Precedence and grouping**
- Prefix operators bind tighter than `as` (Appendix B already says so; check the examples).
- `try` covers its operand only.
- `??` groups right to left.
- Comparison chains are refused.
- `(T)` in a type is grouping, and a one-element tuple type is `(T,)`.

**Additions**
- `case None`.
- A boolean `guard cond else { … }` (SL-58).
- Open ranges `..b`, `..=b` and `..`.
- `@test(panics: …, text: …)`, where a user panic has its own rule name (see SL:testing).
- Implicit member expressions `.Case` / `.Case(…)`: enum cases, a determined expected type, looking through an optional, never selecting an overload.

**Removals and restrictions**
- `export` declarations retire in favour of `public import`.
- Identifiers are ASCII only.
- Trailing closures attach again inside brackets within a head.
- A `>=` that closes a generic list is split, and warnings flag `o!= 5` and `a&-b`.
- Fields and cases on one line need a comma.
- Generic trait requirements are refused by type checking for now.
- `constexpr` is not part of function types.

**Spec text found stale** (SL:grammar §15, "Spec text to update")
- A match arm's bare body is also a statement (SL-59).
- `x as Int? ?? y` (SL-309).
- `@synthesize` takes arguments; traits may require `borrows` methods; `borrows` function values exist; borrowing structs may hold `&var` (SL:borrowing).
- The consuming receiver is `&var self`.
- `const func` becomes `constexpr` in the effect slot.


<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":399,"created":"1790353243","id":"c5"} -->
New rule IDs for the inventory, all panic IDs:
- `panic.explicit`: a user `panic("…")`;
- `panic.assert`: a failed `assert`;
- `alloc.failed`: an allocation failure at a documented panic boundary;
- `borrow.same-place-twice`: a multi-place lend given the same place twice.

std raises them through the std-only `panic(rule: ID, message)`, as in SL:testing r21 §4 and SL:borrowing r20 §4 and §7.

