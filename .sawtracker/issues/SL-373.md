---
{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","body_bytes":971,"closed":"1790341685","created":"1790204912","id":"SL-373","labels":["frozen","tooling"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":3,"sequence":2235,"stage":"backlog","status":"closed","title":"Per-patch server gate widens to tools/battery.sh --quick + freestanding (build.sh test / .sawtracker/tests.json) so no quick lane can fail on main unnoticed again","updated":"1790341685"}
---


## Description

RULED by the user, Sep 23 2026 (tier 1 of the two-tier gate; tier 2 = the periodic full battery on main, ST-45). Today ./build.sh test runs the suite + freestanding only, so a quick battery lane the server never runs can fail on main for a day (SL-371: transferdecisions, landed by SL-350 whose server gate could not see it). Change: ./build.sh test runs tools/battery.sh --quick (every stage but the slow five: reemit, irdet, gmgate, bootstrap, freestanding) and then the freestanding runner on both arches, under the machine-wide suite lock as now; the exit code is the number of failing stages + freestanding. Cost: ~20 min per patch test instead of ~3. Keep the per-stage '---> NAME ok|FAILED' lines in the log so the server's patch_test_result (and ST-45's status page) can show them. A lane that is flaky under the server's load is a finding to fix, not a reason to drop it. Gate for this patch: it is a harness change (build.sh, tests.json), not a compiler branch.

## Comments

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":231,"created":"1790341685","id":"c1","kind":"landing"} -->
Closed in the Sep 25 tracker cleanup (SL:tracker-cleanup): tooling for the frozen Python compiler. The wider per-patch gate would mostly gate frozen sawc code. The new compiler's gate is designed with its test runner (user ruling).

