---
{"acceptance":[],"assignee":"","author":"agent:claude-sl318u3","body_bytes":9865,"closed":"","created":"1790049564","id":"SL-340","labels":["bug","design-275"],"order":3,"parent":"SL-367","priority":"high","project":"SL","queue_order":0,"revision":6,"sequence":1445,"stage":"queued","status":"open","title":"USE-AFTER-FREE: Mutex(value: move v) frees a heap-owning payload at construction — an UnsafeMutableInterior(...) argument does not retire the init parameter","updated":"1790262238"}
---


## Description

USE-AFTER-FREE: `Mutex(value: move v)` at a heap-owning payload FREES the payload at construction. Found on MAIN (`da84578e`) while building design 275 U3's ruled `Mutex` conformance row.

REPRO (main, exit 0, no diagnostic):

```saw
import std.mutex.{Mutex}

func main() {
    var v: Vector<Int> = [1, 2, 3]
    let guarded = Mutex(value: move v)
    print(guarded.lock({ items in items[0] }))   // prints garbage
}
```

`items.len()` reads 3 — the length word survives — and `items[0]` reads through a buffer pointer whose allocation was already released, so the value is whatever the allocator left there. Nondeterministic across runs.

MECHANISM (obligation 4). `Mutex<T>.init(value: T)` is

```saw
public init(value: T) unsafe -> Mutex<T> {
    Mutex<T>(state: UnsafeMutableInterior(0),
             value: UnsafeMutableInterior(value))
}
```

and the emitted `Mutex_init_value` reads:

    %value.1 = alloca %Vector
    store %Vector %value, %Vector* %value.1
    %value.dropflag = alloca i1
    store i1 1, i1* %value.dropflag          ; design 42: registered for cleanup
    %value.2 = load %Vector, %Vector* %value.1
    %.5 = insertvalue %Mutex undef, i64 0, 0
    %.6 = insertvalue %Mutex %.5, %Vector %value.2, 1     ; the payload moves IN
    %value.needsdrop = load i1, i1* %value.dropflag        ; …and the flag is STILL 1
    br i1 %value.needsdrop, label %drop.value, ...
    drop.value:
      call void @Vector_deinit(%Vector* %value.1)          ; the buffer is freed here
    ret %Mutex %.6                                         ; …and returned anyway

An `init`'s by-value parameter is registered for scope-exit cleanup and the flag is cleared by whatever CONSUMES it. Here the consumer is `UnsafeMutableInterior(value)` — a cell CONSTRUCTOR whose argument is the payload — and the transfer into it is not recognised as the move that retires the parameter, so the parameter is released at the end of the init body while its bytes have already been copied into the returned struct.

CLASS — every constructor whose parameter is consumed only by an `UnsafeMutableInterior(...)` construction, at every heap-owning payload:
- `Mutex<Vector<Int>>`, `Mutex<Data>`, `Mutex<String>`, `Mutex<Map<..>>` — measured on `Vector`;
- `Once<T>`'s `set` path and `SpinLock<T>`'s own initializer are the neighbours to probe (they take the same shape);
- a USER type following the design-186 interior-mutability idiom (`struct Counter { cell: UnsafeMutableInterior<Int> }` with an `init`) is the same shape, and is only invisible at `Int` because a trivial payload has no `deinit` to run.
A payload with no `deinit` (`Mutex<Int>`, `Mutex<Bool>`) is unaffected, which is why the whole corpus passes: every `Mutex` in the tree guards a trivial or refcount-free payload.

WHY IT SURFACED NOW: design 275 U3's ruled conformance row is "a `for` over the guarded collection inside `lock` is a sync window and works" — the first program in the tree to put a heap-owning container inside a `Mutex`.

PIN: `examples/conformance/K141_mutex_guarded_for_is_sync.saw` (XFAIL, citing this issue). Its XPASS flip is the validation; the paired REFUSAL half (a suspension inside a `lock` body is refused by the `sync`-body rule) is `examples/errors/mutex_guarded_for_suspends.saw` and passes today.


---

# BRIEF (lead, Sep 24): READY

## Re-measured on main
Cells are in the lead checkout at `.build/scratch/sl340_probe.saw`, `sl340_drop.saw`, `sl340_drop2.saw` and `sl340_ctrl.saw`.
- `Mutex(value: move v)` with a `Vector<Int>`: `items[0]` reads garbage on 1 run in 3. This is the finding, and it still reproduces.
- `Mutex(value: Noisy(name: "kept"))`: `drop kept` prints AT CONSTRUCTION, before the first `lock`, and nothing prints when the Mutex dies.
- **A second, hidden half: a cell NEVER drops its payload.** A user `struct Holder { plain: Noisy, cell: UnsafeMutableInterior<Noisy> }` going out of scope prints `drop plain field` and nothing for the cell. This holds whether the payload came from an rvalue, a by-value parameter, or `move` of a local. Every heap-owning cell payload leaks.
- **The two halves hide each other today.** Construction from a parameter frees the payload early, and the owner never frees it again, so nothing double-frees. Fixing either half alone turns the bug into a leak or a double free. **Both land in one patch.**

## Mechanism (obligation 4), confirmed in code

**Half 1: the intercepted construction bypasses the value-transfer checkpoint.**
- Control: an ordinary call `sink(value)` with a NoCopy parameter is refused: "cannot copy value of type `Noisy` which implements NoCopy; use `move`".
- `UnsafeMutableInterior(value)` with the same parameter is accepted silently. Its interception in `typechecker/expressions.py` (the `expr.name == "UnsafeMutableInterior"` branch, which stamps `is_interior_cell_construct`) checks only `_transfer_compatible`, never `_check_value_transfer`.
- So the argument is neither refused as a copy nor recorded as moved. Codegen never clears the parameter's drop flag, and the parameter is released at scope exit after its bytes were copied into the cell.
- This is the DF-216a class: a compiler-intercepted construction that skips the transfer funnel.

**Half 2: the drop glue has no path for a layout-transparent wrapper.**
- `UnsafeMutableInterior<T>` lowers to a bare `T` (`type_identity.is_layout_transparent`). It deliberately gets no monomorphized `deinit`, because a method taking the wrapper's address could never be called with the field's address.
- `_emit_drop_at` (`codegen/resources.py`) therefore finds no deinit symbol, falls to the struct-field path, and drops nothing.
- The right drop of a transparent wrapper is its payload's drop at the same address. The same holds for `_needs_cleanup`, and for the copy/retain paths wherever a transparent wrapper's payload is refcounted or Copy-tier. Answer all of these through ONE predicate, the payload's own, so the three places cannot disagree.

## The fix
1. **Construction.** Every compiler-intercepted construction whose argument is a VALUE routes that argument through `_check_value_transfer`, exactly as a call argument does:
   - a NoCopy/ExplicitCopy argument must be written `move x` (or `.copy()`);
   - a moved one is recorded as moved, so codegen clears its flag.
   Enumerate the intercepted constructions (the `is_*_construct` stamps: `Atomic(...)`, `UnsafeMutableInterior(...)`, `UnsafeMemory(...)`, plus the alias back-conversion `UserId(i)`, `E.from(raw:)` and `UnsafeMemory.write` that `_transfer_compatible`'s docstring names). Probe each with an owning argument wherever the type admits one: an alias of `String`/`Vector`, and `UnsafeMemory.write` of a non-trivial `T` if legal. Route every one that can carry an owning value; record WHY each of the others cannot.
2. **Drop.** A layout-transparent wrapper drops, retains and copies as its payload does, at the same address, and `_needs_cleanup` answers through the payload. Route this through one place, and name its entry points in the docstring.
3. **Std migration (the consumer sweep).** Once item 1 lands, `Mutex.init`, `SpinLock`'s initializer and any other `UnsafeMutableInterior(value)` over a generic or owning `T` must spell `move value`.
   - Grep every `UnsafeMutableInterior(` and `Atomic(` construction in sawc/std, blade/, libs/, devtools/, selfhost/, prototypes/ and examples/.
   - Each is either migrated to `move` (a by-value owner) or already fine (a literal or a trivial type). List them in the report.
   - `Once.set` writes through the pointer (`self.value.ptr()[0] = value`). Probe it: `Once<Vector<Int>>` set, get, then drop, with the drop count. Also check whether that assignment drops a zero-initialized old value (a hazard for a zeroed `String`/`Vector` slot).

## Conformance (obligation 3) and position matrix
Conformance rows come FIRST, from the Studio's range **K170-K179** (the Air holds K154 and K160-K169). They go beside the existing `K141_mutex_guarded_for_is_sync.saw`, which is XFAIL citing this issue: remove that marker in this patch, because its XPASS is the validation.

Every row counts drops with a printing deinit and requires EXACTLY ONE drop, at the owner's end:
1. `Mutex(value: move v)` over `Vector<Int>`: reads are correct, and the buffer is freed once when the Mutex dies. Use an ASan build or a drop-counting payload.
2. `Mutex<Noisy>`, from an rvalue and from a moved local.
3. `SpinLock<T>` with a heap-owning payload, both sources.
4. `Once<T>` with a heap-owning payload: set, get (via `.copy()`), then drop.
5. A user cell struct (`Holder` above), from an rvalue, a by-value parameter and a moved local.
6. A cell nested as an `Optional` payload, an enum payload, a fixed-array element and a generic struct field.
7. A Mutex held across a `yield_now()` in a driven body, dropped once. This is the frame twin.
8. REFUSAL: `UnsafeMutableInterior(value)` with a NoCopy/ExplicitCopy parameter and no `move` → "cannot copy … use `move`", pinned in examples/errors/.
9. CONTROL: `Mutex<Int>`, `Atomic(0)` and `static` Mutex/SpinLock are unchanged. The all-zero static costs no image bytes and never drops.

## Gates and files
- Full terminal battery. `transferdecisions` is live again: a new writer of `needs_copy`/`payload_needs_copy` must go through `carry_retain_stamps`, so check it.
- Files: `sawc/typechecker/expressions.py` (the interception branches), `sawc/codegen/resources.py` (drop glue plus `_needs_cleanup`), possibly `sawc/codegen/structs.py`/`generics.py` for retain/copy, `sawc/std/{mutex,spinlock,once}.saw`, `examples/conformance/`, `examples/errors/`.
- This does not overlap SL-345 (the Air's call-exclusivity funnel and closure checker) or SL-355's `calls.py`/`coro_transform.py` hunks. If the retain path turns out to need `calls.py`, stop and report before editing it.


## Comments

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":192,"created":"1790188483","id":"c1"} -->
MEMBER of the must-fix epic SL-367 (user ruling, Sep 23) — position 3 in the lead's recommended order; the epic holds order and status, this issue keeps its brief, evidence and landing note.

