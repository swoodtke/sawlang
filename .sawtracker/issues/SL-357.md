---
{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","body_bytes":2810,"closed":"","created":"1790177000","id":"SL-357","labels":["docs","cleanup"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":6,"sequence":1265,"stage":"backlog","status":"open","title":"Trim code comments: WHY and invariants only; history, essays and changelog narration move out of the source (comment rules in CLAUDE.md)","updated":"1790181643"}
---


## Description

REQUESTED by the user, Sep 23 2026, after a repo review found comments crowding out the code in `sawc/`: roughly 0.8 comment/docstring lines per code line overall, and more than 1.0 in `rt/shim.c` (1.48), `ast_nodes.py` (1.43), `std/vector.saw` (1.21), `typechecker/core.py` (1.15) and `coro_transform.py` (1.08).

A sampled breakdown of the comment lines:
- about 35% are real WHY/invariant comments;
- about 20% are history or changelog ("used to", "retired", "RULED by the user Aug 8");
- about 15% are design-brief essays pasted into docstrings;
- about 14% cite a design/DF number and cannot be read without `designs/` open;
- about 17% of lines contain ALLCAPS emphasis.

Stale comments already exist, for example:
- `coro_transform.py:180-213` cites `sawc.py:1950` and gives site counts that are wrong today;
- `coro_transform.py:19-25` lists landed work as "later";
- `std/vector.saw:161` says the signature carries nothing unsafe, but it is `unsafe`.

## Rules
Now in CLAUDE.md under "Code comments", landing with p1 of this issue.

## Method
- Each patch changes COMMENTS ONLY: no code, no identifiers, no behavior.
- `///` and `//!` in std `.saw` files are published API (`--emit-docs`) and follow the saw-docs skill. They are trimmed only where they narrate history.
- Before an essay or history block is deleted, check that its reasoning exists in `designs/` or the tracker. If it doesn't, move it there first; the only copy of a mechanism must never disappear.
- Funnel docstrings keep their ENTRY POINTS lists (brief obligation 1). The source-reading lanes (corodiscovery, corototality, windowseam, citations, astgraft, stdseed) run on every patch, because they parse docstrings and source text.
- Gate per patch: `tools/battery.sh --quick` plus `freestanding`. Removing comments cannot change IR, but a `.saw` doc comment feeds `--emit-docs` and the lexers.

## Waves
Waves are ordered to stay off the in-flight surfaces: SL-318 (design 275 U3: typechecker, coro_transform, codegen/loops.py) and SL-350 (codegen/structs.py, coro_transform `_build_frame_init`).
- **Wave 1 (p1):** CLAUDE.md rules, `rt/shim.c`, `std/vector.saw`, `std/map.saw`, `std/channel.saw`.
- **Wave 2:** `std/taskgroup.saw`, remaining `std/*.saw`, `rt/**/*.saw`, `ast_nodes.py`, `namespace.py`, `parser/`, `codegen/` (except `structs.py` and `loops.py` while SL-350 and SL-318 are open).
- **Wave 3 (after SL-318 and SL-350 land):** `coro_transform.py`, `coro_ledger.py`, `typechecker/*`, `codegen/structs.py`, `codegen/loops.py`.
- **Wave 4:** a lint lane that flags date stamps, `file:NNNN` references and "used to"/"no longer" in ADDED comment lines, so the clutter does not grow back.

Out of scope: code changes found along the way. They get their own issues. For example, `rt/shim.c` has duplicate `#include` lines.


## Comments

<!-- sawtracker:comment {"author":"agent:claude-air-sawlang","body_bytes":178,"created":"1790177065","id":"c1"} -->
Filed by the MacBook Air session (agent:claude-air-sawlang), not the Mac Studio agent:claude-sawlang whose name the create carries. Work is in worktree sl357-comments on the Air.

<!-- sawtracker:comment {"author":"agent:claude-air-sawlang","body_bytes":645,"created":"1790178759","id":"c2"} -->
Hold list updated from the chat room (m54/m55, Sep 23). HOLD until the owning patch lands: std/taskgroup.saw, rt/host_macos/reactor.saw, rt/host_linux/reactor.saw, codegen/calls.py (SL-355); codegen/closures.py, codegen/methods.py (SL-356); ast_nodes.py, coro_transform.py, typechecker/expressions.py, codegen/{collections,existentials,optionals,resources,statements,structs}.py (SL-350). FREE: shim.c and std/{vector,map,channel}.saw (wave 1, gating now); the other std/*.saw; the non-reactor rt/**/*.saw; namespace.py; parser/. Server applies patches with git apply, so the hold list, not byte-identity, is what avoids adjacent-hunk conflicts.

<!-- sawtracker:comment {"author":"agent:claude-air-sawlang","body_bytes":152,"created":"1790179982","id":"c3"} -->
Submitted patch SL-357.p1 (revision 1): Wave 1: comment rules in CLAUDE.md; comment-only trim of rt/shim.c, std/vector.saw, std/map.saw, std/channel.saw

<!-- sawtracker:comment {"author":"agent:sawtracker-tests","body_bytes":39,"created":"1790181029","id":"c4"} -->
Tests passed for SL-357.p1 (revision 1)

<!-- sawtracker:comment {"author":"agent:claude-air-sawlang","body_bytes":1209,"created":"1790181643","id":"c5"} -->
Wave 2 (p2, gating now): 49 files, all comment-only per the probe, -1874 lines net. Covers std/*.saw except taskgroup, net, vector, map and channel; namespace.py; parser/*.py; and the non-reactor rt/**/*.saw except offload.saw. The agents fixed about 40 stale comments, including six wrong funnel entry-point lists in namespace.py and parser/, doc examples in spinlock.saw and fixedbuf.saw that did not compile, and Resumable.wake_reason's units (nanoseconds, not ms).

Follow-ups found:
- SL-358: an ICE on a qualified const-generic construction.
- SL-359: Directory.current hides errors.
- LANGUAGE_SPEC ~12919 lists Mutex among the types that 'allocate through GlobalAllocator', which contradicts ~5141/~12842. A one-line fix, folded into p2 after the gate.

Deferred:
- sawc/rt/ABI.md has the same comment problems (88 design/DF refs, about 14 history phrases, dated rulings). Its layout block says shim.c holds 'the three FFI-blocked bodies', and its host_linux list is incomplete. It is a frozen contract doc, so it gets its own wave 5 pass.
- Namespace.is_equatable's 'docstring' sits after the first statement, so it is a bare string expression. Moving it is a code change, so it is not in this issue.

