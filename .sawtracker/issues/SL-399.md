---
{"acceptance":[],"assignee":"agent:claude-u1","author":"agent:claude-sawlang","body_bytes":6286,"closed":"","created":"1790342966","id":"SL-399","labels":["compiler"],"order":1,"parent":"SL-398","priority":"normal","project":"SL","queue_order":0,"revision":24,"sequence":2606,"stage":"queued","status":"open","title":"U1: compiler/ skeleton, the lexer moved in, golden token fixtures, the subset checker, the path-aware gate","updated":"1790358905"}
---


## Description

Unit 1 of the self-hosted compiler epic. Read the epic, SL:architecture (§3.0, §3.1 and §4) and SL:hazards first.

## Goal

The `compiler/` directory exists and builds with the frozen compiler (Stage 0). It holds the lexer, with a driver binary that dumps tokens, golden token fixtures, a test runner, and the subset checker. `./build.sh test` is path-aware. The syntax diff lanes that the golden fixtures replace are retired.

## Rulings this unit implements (user, Sep 25)

- `compiler/` at the top level, one directory per stage.
- The lexer moves in from `selfhost/lexer`. `prototypes/minivm` stays where it is, paused.
- Internal tests until self-hosting: golden dumps, plus small subset programs the frozen compiler builds.
- The per-patch gate is path-aware.
- The syntax diff lanes retire into a one-time golden snapshot.

## Work

### A. Layout and build
- `git mv selfhost/lexer compiler/lex`, so its history follows. It stays a package the other stages import through `--module-path`. Keep its package name unless a rename is clearly better, and say which you chose. Re-point `prototypes/minivm`'s `--module-path sawlex=selfhost/lexer` (its README and the battery `minivm` lane) so it still builds.
- `compiler/driver/`: the compiler binary, named `sawc2` for now (provisional). Its first subcommand is `sawc2 lex [--docs] <file>`, which emits the lexer's existing canonical dump format. It absorbs `sawlex`'s CLI, so there is one entry point, not two.
- One build entry (a small script, or a `build.sh` subcommand) that builds `sawc2` with the frozen compiler and the right `--module-path` flags. Output goes to `.build/`.

### B. Tests
- **Unit programs:** the lexer's `tests/*.saw` move with it. Each is built by the frozen compiler and passes by exiting 0.
- **Golden token fixtures** in `compiler/tests/lex/`: an input `.saw`, its expected `.tokens`, and a `.docs` expectation where doc trivia matter.
  - Choose the fixture set so that every token kind and every lex-error kind appears at least once. Take the fixtures from existing tracked `.saw` files and the lexer tests' inputs.
  - Snapshot procedure: generate each expectation with `sawc2 lex`, confirm it is byte-identical to the Python lexer's dump (the machinery `tools/lexdiff.py` uses), then commit.
  - Before retiring lexdiff, run its full sweep over every tracked `.saw` once, and record the result (it should be zero mismatches) in your report.
- **Coverage check:** the runner fails if some token kind appears in no fixture. Get the list of kinds from the lexer itself (for example a `sawc2 lex --kinds` subcommand), not from a hand-kept copy.
- **The runner**, `compiler/tests/run.py` (Python is fine for now):
  - it builds `sawc2` and the unit programs with the frozen compiler;
  - it runs the unit programs, the golden fixtures and the subset checker;
  - its output is deterministic, it prints one summary line, and it exits nonzero on any failure.

### C. The subset checker
- A tool under `compiler/` that refuses subset violations in the compiler's own source. It covers every `compiler/**/*.saw` except test fixtures (`compiler/tests/**`, which hold arbitrary Saw) and `*.test.saw`.
- Implement the syntactic rules from SL:architecture §4's blanket list, plus the SL:hazards entries whose Checker line says `yes` and whose check is cheap. Add an **import allowlist**: the std modules the compiler may import, starting with what the lexer and driver need. The allowlist keeps the compiler's std cone small for the bootstrap std unit.
- Parsing the source with the frozen compiler's own front end (for example `--emit-ast`) is fine, and it is more robust than regexes. Each diagnostic gives `file:line`, the rule's name, and the SL:hazards entry or §4 rule it enforces.
- **Self-test:** a fixture per rule that the checker must refuse, and a clean fixture it must accept.
- If the moved lexer violates a rule, bring the lexer into the subset. List every change in your report. Never weaken a rule to fit the code.

### D. The path-aware gate
- `.sawtracker/tests.json` runs `./build.sh test --changed-since HEAD^`. The server tests the applied commit, whose parent is the patch's base. Without the flag, `./build.sh test` runs everything.
- It always builds and runs the compiler tests. It runs the Python suite (`test_runner.py`) and `tools/freestanding_runner.py` only when a changed path is one those suites depend on. Derive that set from the two runners (for example `sawc/`, `examples/`, `libs/`, the runners themselves, `build.sh` and `.sawtracker/`), and list it with a comment saying why. If the changed paths cannot be determined, run everything.
- Keep the suite lock around everything the gate runs. Print which parts ran and why.
- Add a dry-run flag that prints the decision for a given diff. Use it to show a compiler-only diff skipping the Python suite, and a `sawc/` diff running it.

### E. Battery
- Remove the `lexdiff` and `selfhostlex` lanes. The compiler tests replace them.
- Add a `compiler` lane that runs `compiler/tests/run.py`, and keep `minivm` building from the new path.
- Delete `tools/lexdiff.py` after the snapshot. Delete `tools/dump_tokens.py` too, unless something else still uses it; check `astdiff` and the rest of `tools/`.

### F. Docs
- `compiler/README.md`: the layout, how to build and test, the subset and its checker, and pointers to the SL docs.
- `CLAUDE.md`: the repo map entry, the gate policy, and the retired lanes.
- `TESTING.md`: the compiler tests.
- Follow CLAUDE.md's rules for code comments.

## Gates (standing ruling: targeted only; the server runs the suite and freestanding)
- `compiler/tests/run.py` is green.
- The touched battery lanes are green: `compiler`, `minivm`, and `citations` if `tools/` changed.
- The gate's dry-run output shows both decisions.
- Do **not** run the full suite or freestanding.

## Out of scope
The parser, any new syntax, golden AST fixtures and std changes.

## Report
- the files moved and created;
- the package and module layout you chose;
- the checker's rules, with the source of each;
- fixture coverage;
- the snapshot verification and lexdiff sweep results;
- every change made to bring the lexer into the subset;
- the gate's dry-run output;
- anything that needs the lead or the user.


## Comments

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":146,"created":"1790346170","id":"c1"} -->
Submitted patch SL-399.p1 (revision 1): U1: compiler/ skeleton, the lexer moved in, golden token fixtures, the subset checker, the path-aware gate

<!-- sawtracker:comment {"author":"agent:sawtracker-tests","body_bytes":4028,"created":"1790346712","id":"c2"} -->
Tests failed for SL-399.p1 (revision 1):

```
[2654/2671] ✓ while_expr_conditional_found
[2655/2671] ✓ void_if_chain_tail
[2656/2671] ✓ visibility_public
[2657/2671] ✓ while_expr_conditional_not_found
[2658/2671] ✓ while_expr_infinite
[2659/2671] ✓ while_infinite
[2660/2671] ✓ while_let_drain
[2661/2671] ✓ while_nested
[2662/2671] ✓ while_let_channel_drain
[2663/2671] ✓ while_never_break_forms
[2664/2671] ✓ while_never_diverges
[2665/2671] ✓ wrapping_operators
[2666/2671] ✓ while_simple
[2667/2671] ✓ while_let_suspending_mt
[2668/2671] ✓ wrapping_and_reference_mix
[2669/2671] ✓ while_let_suspending
[2670/2671] ✓ while_let_suspending_try_catch
[2671/2671] ✓ while_never_sync_context

compile 440.3s + 4.6s draining = 444.9s total

======================================================================
Test Results
======================================================================

KNOWN FAILURES (xfail):
  x K141_mutex_guarded_for_is_sync: SL-340 — `Mutex(value: move v)` at a HEAP-OWNING payload frees the
  x V96_driven_capture_duplicates_once: SL-267 — a driven body's NON-ESCAPING closure capture duplicates
  x closure_escaping_move_capture_consumed_once: DF-255a an escaping closure's env destructor releases a capture the
  x closure_nested_ref_param_capture: DF-248b a nested closure captures an enclosing closure's reference
  x closure_shorthand_parameter_inside_a_try: DF-257d the `$0` shorthand is invisible to the implicit-parameter scan
  x coro_value_inline_catch_hosts_a_move: SL-231 a value-carrying INLINE catch cannot host a `move` of a frame
  x funcpointer_call_in_a_driven_body: DF-252a a `FuncPointer` value called by NAME inside a driven body is
  x generic_init_moved_parameter_is_released_once: DF-302b — a generic extension's `init` releases a parameter it MOVED
  x primitive_type_called_as_a_function: DF-270e — a PRIMITIVE TYPE NAME written as a ZERO-ARGUMENT call is an
  x trailing_closure_inside_a_try_operand: DF-259c — a trailing closure is not recognized inside a `try`/`try!`/

======================================================================
ALL TESTS PASSED (2661 passed, 10 xfailed)
======================================================================

Published test_runner_1790346191_31370 as test_runner_last (1625 manifest entries).
gate: ---- freestanding ----
Freestanding QEMU tests
  clang: /opt/homebrew/opt/llvm/bin/clang
  lld  : /opt/homebrew/bin/ld.lld
  qemu : /opt/homebrew/bin/qemu-system-riscv32
  qemu : /opt/homebrew/bin/qemu-system-aarch64

building blade

riscv32  (riscv32-unknown-none-elf, qemu-system-riscv32 `virt`)
[1/19] ✓ hello
[2/19] ✓ int_widths
[3/19] ✓ struct_layout
[4/19] ✓ call_convention
[5/19] ✓ module_compose
[6/19] ✓ runtime_seams
[7/19] ✓ runtime_panic
[8/19] ✓ runtime_export_ungated
[9/19] ✓ runtime_seam_signature
[10/19] ✓ hidden_alloc_interpolation
[11/19] ✓ hidden_alloc_escaping_closure
[12/19] ✓ hidden_alloc_print_printable
[13/19] ✓ format_no_alloc
[14/19] ✓ align_attribute
[15/19] ✓ export_with_coroutine
[16/19] ✓ float_text
[17/19] ✓ link_address
[18/19] ✓ blade_target
[19/19] ✓ wide_value_rendering

arm64  (aarch64-unknown-none-elf, qemu-system-aarch64 `virt`)
[1/17] ✓ hello
[2/17] ✓ int_widths
[3/17] ✓ struct_layout
[4/17] ✓ call_convention
[5/17] ✓ module_compose
[6/17] ✓ runtime_seams
[7/17] ✓ runtime_panic
[8/17] ✓ runtime_export_ungated
[9/17] ✓ runtime_seam_signature
[10/17] ✓ hidden_alloc_interpolation
[11/17] ✓ hidden_alloc_escaping_closure
[12/17] ✓ hidden_alloc_print_printable
[13/17] ✓ format_no_alloc
[14/17] ✓ export_with_coroutine
[15/17] ✓ float_text
[16/17] ✓ link_address
[17/17] ✓ blade_target

============================================================
ALL FREESTANDING TESTS PASSED (36 passed across riscv32 + arm64)
============================================================
gate: compiler tests FAILED (exit 1); python suite passed; freestanding passed

```

<!-- sawtracker:comment {"author":"agent:claude-air-sawlang","body_bytes":2222,"created":"1790346821","id":"c3","kind":"review"} -->
Why SL-399.p1's server gate failed (claude-air-sawlang, reviewing). The patch's code is not at fault. The Python suite (2,661 passed) and freestanding (36) are green, and the 18 compiler-test failures are one whitespace problem.

**What fails.** Every failure is the last record of a golden file:
- `sawc2 lex` prints `EOF\t6:1\t`, with a trailing tab for the empty text field;
- the server's copy of the fixture ends `EOF\t6:1`, with no tab.

The `--docs` case is the same thing for a doc record with empty text (`DOC\t8:1\tdoc\t`).

**Why.** The submitted diff has the tab (`+EOF\t6:1\t`). The server applies patches with `git apply --3way --index` (sawtracker `store.saw`, `apply_in_worktree`) and no `--whitespace` flag, so git's config decides. The global git config on the Air has `apply.whitespace = fix`, and very likely the Studio's does too: same user, same setup. I reproduced it locally:
- `git apply --3way --index` of this exact diff onto origin/main strips the tab, and `comments.tokens` ends `EOF\t6:1\n`;
- the same diff applied with `--whitespace=nowarn` keeps it, and `compiler/tests/run.py` is then green. My review run gave `compiler tests: ok`, and the battery's `compiler`, `minivm` (446 cases) and `citations` lanes all pass.

**Fixes.**
1. **In this patch (suggested):** make the canonical dump never end a record in whitespace, and regenerate the affected expectations. A record whose last field is empty (EOF's text, an empty doc line) should omit the trailing separator, or the empty field should be escaped. A fixture format with significant trailing whitespace is fragile in general: editors that trim on save, `git apply --whitespace=fix`, and review tools all remove it silently.
2. **In sawtracker (worth an ST issue):** apply with an explicit `--whitespace=nowarn`, so the tested and merged tree is byte-for-byte the reviewed diff. Today, any added line ending in whitespace is merged differently from how it was reviewed. That includes Markdown hard line breaks (two trailing spaces), and expected-output lines with trailing spaces.

The rest of my review (the gate, the runner, the driver, the lexer move, the battery, CI and docs) follows once the subset checker's deep-dive finishes.


<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":435,"created":"1790346829","id":"c4"} -->
Agreed; same diagnosis. This machine's global git config sets apply.whitespace = fix, so the server's git apply strips the golden records' trailing tab. The fix in progress on the branch: the dump format never ends a record in whitespace (an empty last field is omitted with its tab, e.g. EOF\t6:1), the goldens are regenerated, and the runner refuses any golden line ending in whitespace, naming the file and line. A revision follows.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":37,"created":"1790347094","id":"c5"} -->
Revised patch SL-399.p1 to revision 2

<!-- sawtracker:comment {"author":"agent:sawtracker-tests","body_bytes":39,"created":"1790347644","id":"c6"} -->
Tests passed for SL-399.p1 (revision 2)

<!-- sawtracker:comment {"author":"agent:claude-air-sawlang","body_bytes":6265,"created":"1790348744","id":"c7"} -->
Requested changes on patch SL-399.p1 (revision 2)

Review of SL-399.p1 r2 (claude-air-sawlang): **request changes**, for the subset checker and one CLAUDE.md paragraph. Everything else is ready.

## Verified good
- **The gate.** Its inputs match what the two runners actually read, including the vector files and the `libs/` packages the freestanding runner builds. It fails safe: no source, a bad revision, or a change to the gate itself runs everything. The dry runs decide correctly for a compiler-only change, a `sawc/` change, a `libs/` change, a docs change and this patch. `HEAD^` is the base: `origin/main` has no merge commits.
- **The runner.** Goldens compare byte for byte. Kind and error coverage come from the lexer, cross-checked against the declared `TokenKind`. Fixture markers must match exactly, and r2 adds the whitespace guard.
- **The driver, `build.py` and the lexer move.** Behavior is preserved. The never-produced kinds are gone, and the S4 fix is right. `minivm` builds (446 cases) and the parser prototype builds, both against `compiler/lex`. The battery, CI and Makefile edits are fine.
- **My runs:** `compiler/tests/run.py` ok; battery `compiler`, `minivm` and `citations` green; r2 applied server-style under `apply.whitespace=fix` passes. The server gate passed.

## The subset checker: must fix in U1
It is clean on the tree, deterministic (byte-identical output across hash seeds) and fail-closed, and every ledger entry has a rule on paper. A deep review with 45 adversarial probes (chat file `f1`; each header says what it probes) found silent hazards it lets through. I re-ran the key ones: the checker output and the Stage 0 behavior below are reproduced.

1. **Scope holes: whole regions of source are never checked.**
   - Inline `module m { … }` bodies: the declaration pass, the cross-file pass and `BuildFacts` never read `module_decls[i].body`. p01b's function falls off its end, and Stage 0 prints `0` with 0 diagnostics. p27 hides an enum Equatable split (`a == b` gives 0, `a.equals(&b)` gives 1), an alias, a prelude name and an integer overload. p22 and p22b bypass the import allowlist, and p22's `Command.run` crashes Stage 0 with `KeyError`.
   - Trait default bodies (p02, p02b: a fall-off path printing `0`, a capture, a move out of a `&var` match).
   - `init` bodies, which are exempt from `function-exit` (subset_check.py:840; p31 prints `0`).
   - `static` initializers, which the S14 check never walks (p18: `Twin(first: 1, second: 2, first: 3)` prints `3`).
2. **The `?.` branches never fire.** They match `A.OptionalChain`, which the frozen parser never builds (ast_nodes.py:1581-1585). Real chains are `BindOptional`/`OptionalEvalExpr`, so the branches at :332, :384, :394, :1159 and :1207 are dead. Stage 0 evidence:
   - `v.get(0)?.bump()` silently loses its write: p05b prints `2 2 3`, while the refused `!` form works;
   - `m["a"]?.bump()` loses it too (p08);
   - `v.get(0)?.n = 5` writes in place today but writes a copy under K14 (p05);
   - `maybe?.write_it(maybe!.read_it() + 1)` passes where the plain form is refused (p32).
3. **False positives on the ledger's own Instead spellings.**
   - S7's typed bindings (`static …: UInt64 = 14695981039346656037`, `let bound: UInt64 = 18446744073709551615`) are refused as `int-literal-range`, and `Int.min` as `leading-minus` (p25).
   - L4's `Result<Int, String>.Ok(value: 9)` and `.Err(error:)` are refused as `std-api` (p20).
   - A newline inside parentheses is a token in the frozen lexer (lexer.py:674), so `(a\n * b)` is refused as a dereference and `(a\n - b)` as a leading minus (p14b).

   A checker that refuses the recommended spelling pushes authors toward a worse one, or toward weakening a rule.
4. **Blanket-rule gaps.**
   - `borrows`, `lend` and `borrows struct` are not refused (p16b). §4 lists accessors as a feature of the new std, not of the subset.
   - An unannotated `let a = [1, 2, 3]` is a fixed array (the IR has `[3 x i64]`), and L7's rule misses it (p30).

## Can be follow-ups (fold in, or file)
5. **S13 is narrow.** A user-defined defaulted generic (p11d: a `Vector<Pool<Noisy>>` field over `struct Pool<T, K = Int>` never drops) and a tuple type argument (p11: `Plain<(Vector<Noisy>, Int)>` never drops) both pass. The rule knows only std's defaulted generics (:237-239).
6. **S3 misses an owned temporary before a failing `try`.** It leaks with no `move` written (p24: `sink2(make_res("fresh"), try fail_it())` never drops). The ledger's Checker line has the same gap.
7. **Token rules are blind inside interpolations.** S7, S20 and S17 miss `"big {18446744073709551615}"` (p13b prints `big -1`). S17 also misses `??` after a cast across a newline (p14).
8. **`std-api`'s name-shadowing exemption.** Once an unrelated compiler type declares `insert`/`pop`/`keys`/`sort`, std's calls of those names are exempt (p07a has 4 refusals, p07b has 0). The README documents this, but it switches the allowlist off as the compiler's own types grow.
9. **Smaller gaps:**
   - S14 checks only std accessors' labels (p16);
   - L17 skips import-gated std names, including `Path`, which the entry names (p12);
   - L14 does not measure `??` or `else if let` chains (p21);
   - S4's assignment half covers index targets only (p17: `p.a = p.b + 1`).

## Docs
10. **CLAUDE.md still has the old per-commit gate policy.** Its "PER-COMMIT GATE POLICY" paragraph says a compiler change runs the full suite and freestanding before every commit, and that a branch owes the full battery. That contradicts the Sep 24 targeted-only ruling (chat m169) that this unit's own brief follows, and the path-aware gate. Brief section F asks for the gate policy to be updated.
11. **Code-comment rules:**
   - subset_check.py:2-17, the docstring: a two-line summary, about 10 lines, and doc citations mid-sentence;
   - :435-436, a comment wrapped onto the `depth` line, whose list omits `for`;
   - :137-138, "anywhere else it is a dereference", which is false after a newline in parentheses;
   - what-comments at :35, :92, :199 and :206-207;
   - run.py:2-18, about 13 lines.

## For SL:hazards
- S3's Checker line is narrower than the hazard (p24).
- S15 gains Stage 0 evidence: `get(…)?.m()` and `m[k]?.m()` lose the write (p05b, p08).


<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":471,"created":"1790348800","id":"c8"} -->
Thanks; a thorough review. All eleven items went to the U1 agent to fix on the branch, the follow-ups (5 to 9) folded in rather than filed, since the checker is what the Stage 0 trust rests on. Each probe becomes a checker fixture, and each false positive in item 3 an accept fixture. The two SL:hazards notes are applied in r5: S3's checker covers an owned temporary before a failing try (p24), and S15 records the ?.-receiver write loss (p05b, p08). A revision follows.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":37,"created":"1790350954","id":"c9"} -->
Revised patch SL-399.p1 to revision 3

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":3700,"created":"1790350974","id":"c10"} -->
Revision 3 answers all eleven review items. Fixtures are in `compiler/tests/subset/`, named `RULE.VARIANT.saw`.

| # | Fix | Fixtures |
|---|---|---|
| 1 | Inline `module { }` bodies are read by every pass, including the import allowlist. Trait default bodies are checked, and `init` bodies owe `function-exit`. Every rule checks `static` initializers, and `static_assert` conditions are checked too. | `closure-capture.inline-module`, `function-exit.inline-module`, `type-alias.inline-module`, `import-allowlist.inline-module`, `import-allowlist.inline-module-data`, `closure-capture.trait-default`, `function-exit.trait-default`, `function-exit.init`, `argument-labels.static` |
| 2 | Every `?.` site matches `BindOptional` and `OptionalEvalExpr`, `x?.y = v` included. | `std-api.get-writes`, `std-api.get-bump`, `index-receiver-call.receivers`, `root-reuse.optional-receiver`, `std-api.map-subscript`, `std-api.get-presence-match`, `clean.optional-field-bump` |
| 3 | A `UInt64` literal above `Int.max` is accepted, and so is `Int.min`. `Result<…>.Ok` and `.Err` are allowed. A newline inside parentheses no longer turns `*` into a dereference or `-` into a leading minus. | `clean.instead-spellings`, `clean.names-and-bindings`, `clean.legit-subset`, `clean.nested-generic-control`; `leading-minus` and `int-literal-range` extended |
| 4 | A new rule, `borrows-accessor`, refuses `borrows`, `lend`, `lends` and `borrows struct`. `fixed-array` refuses an array literal with no written type, in the positions found by probing Stage 0's IR. | `borrows-accessor`, `fixed-array.inferred` |
| 5 | S13 covers user generics with defaults, and tuple, array and function type arguments. | `field-nested-generic` extended, `field-nested-generic.user-default`, `explicit-nested-type-args.user-default` |
| 6 | S3 refuses an owned temporary built before a failing `try` in the same expression. A call whose result is a plain scalar is exempt. | `try-ownership.temporaries` |
| 7 | The token rules run inside interpolations. S17 catches a `??` on the line after a cast. | `cast-then-optional.interpolation`, `float-literal.interpolation`, `cast-then-optional.newline` |
| 8 | `std-api` judges a method call by its receiver's type. When the type can't be traced, a name std also declares is judged as std's, so a compiler type declaring `insert` no longer switches the allowlist off. | `std-api.unshadowed`, `std-api.shadowed` |
| 9 | S14 covers accessors, L17 covers import-gated names such as `Path`, and L14 measures `??` and `else if let` chains. | `argument-labels.accessor`, `prelude-type-name.gated`, `chain-length.coalesce`, `.else-if-let`, `.and`, `.sum`, `root-reuse.assignment` |
| 10 | CLAUDE.md states the Sep 24 targeted-only gate policy, in the gate paragraph and the three other places that restated the old rule. | none |
| 11 | The comments are rewritten to the SL-357 rules, and the docstrings are trimmed. | none |

**S4's scope:** the Shape names index writes only (SL-111), so a field write such as `p.a = p.b + 1` stays accepted. The lexer writes `self.pos = self.pos + 1` throughout, and Stage 0 compiles it correctly.

**Probes:** every probe in chat file f1 was rerun on this revision. Each missed case now gets a diagnostic, and the accept probes (p05c, p11c, p14b, p20, p25, p29, p34) are clean. One control is now refused: p24c's `sink2(make_res(…), try pass_it())`. The rule refuses that shape whether or not the `try` fails at run time.

**Gates:** `compiler/tests/run.py` passes (97 checker fixtures). The `compiler` and `citations` lanes pass, the checker reports nothing on `compiler/` itself, and its output is identical under two hash seeds. `git diff --check` is clean.


<!-- sawtracker:comment {"author":"agent:sawtracker-tests","body_bytes":39,"created":"1790351486","id":"c11"} -->
Tests passed for SL-399.p1 (revision 3)

<!-- sawtracker:comment {"author":"agent:claude-air-sawlang","body_bytes":5142,"created":"1790352049","id":"c12"} -->
Requested changes on patch SL-399.p1 (revision 3)

Review of SL-399.p1 r3 (claude-air-sawlang): **request changes**, for four small items. Everything from r2 is fixed.

## Verified
- **All eleven r2 items are fixed.** I re-ran all 53 r2 probes on r3:
  - every case they missed now gets its diagnostic;
  - the accept probes (p05c, p11c, p14b, p20, p25, p29, p34) are clean;
  - p24c's second case is refused, as you said.
- **CLAUDE.md** states the targeted-only policy in all four places, and every lane it names exists in `tools/battery.sh`.
- **Comments and docstrings** follow SL-357.
- **Gates:**
  - `compiler/tests/run.py` passes;
  - the checker reports nothing on `compiler/`;
  - its output is byte-identical under two hash seeds, both on the tree and on 177 probe and fixture files;
  - the server gate passed.

New probes are in chat file f2 (`sl399-r3-probes.tar.gz`). I reproduced each finding below.

## Must fix
1. **S3 misses the same leak in four more positions.** Each one leaks in Stage 0 and passes the checker:
   - **A Map literal:** `{1: make_res("first"), 2: try fail_res()}` never prints `drop first` (b04b).
   - **A `while` condition:** `while sink2(make_res("cond"), try fail_it()) > i` (b04c). The `if`, `match`, `return` and `for` heads are refused (q4); only `while` is missed.
   - **An earlier interpolation segment:** `"{make_seg(1)} and {try fail_it()}"` (b04d; its control drops).
   - **An interpolation as the temporary:** `sink_s("key-{k}-…", try fail_it())` leaks one String per failure. `leaks --atExit` counts 1001 leaks (80 KB) over 1000 failures, against 1 each for the hoisted control and a plain literal (q1, q1c, q1d).

   Causes:
   - `operands_in_order` has no Map-literal or interpolation case.
   - `makes_owned_temporary` does not count an interpolated string.
   - The `WhileExpr` branch never passes its condition through `check_statement_expressions`.
2. **A new silent hazard: a line break inside an interpolation drops the rest of the segment.** Outside brackets, Stage 0 evaluates only the segment's first line:
   - `"plus {x⏎ + 3}"` prints `plus 7`;
   - `"and {x > 0⏎ && x > 9}"` prints `and true`;
   - `"eq {x⏎ == 8}"` prints `eq 7`;
   - `"len {s⏎ .len()}"` prints `len abc` (b10, b10c, q3).

   With parentheses it is correct: `{(x⏎ + 3)}` prints 10.
   - **What the checker does:** it reports none of these. It flags only a `-` line, as `leading-minus`, and a `*` line, mislabeled as `raw-pointer` (b09).
   - **Cause:** this is SL:grammar §16's "keeps the first token and drops the rest" defect, reached through a line break.
   - **Fix:** refusing a line break inside an interpolation's braces is cheap, and it matches C3's advice to keep interpolations simple.
3. **`&var` into an immutable `static` through an inline-module qualifier.** `bump_int(&var limits_mod.LIMIT_VALUE)` compiles, but the write is lost and the program prints 3 (b14). The unqualified form is loud in Stage 0. The README says `var-ref-into-let` covers statics, but `place_root` returns `limits_mod`.
4. **`argument-labels` judges a call by the method's name alone.**
   - **The false positive:** a build type's own ordinary methods, `get(&self, depth: Int)` and `value(&self, of: Int)`, are called with their labels on a binding typed `Scope`. The checker refuses both: "call the accessor `get` positionally" (b12).
   - **Why it's wrong:** Stage 0 checks those labels; a wrong one is loud (b12b). Labeled calls are ordinary Saw, and `get` and `value` are likely method names in the compiler.
   - **Fix:** judge by the receiver's type, as `std-api` now does.

## Follow-ups (fold in, or file)
5. **`std-api` refuses six valid calls on receivers it cannot trace** (b07):
   - `self` in a trait default body, calling that trait's own requirement;
   - a receiver of type `T: Bound`;
   - a match-arm binding;
   - a destructured tuple's elements.

   The README documents the trade-off. Tracing these four sources would remove most of it, and `peek`, `next`, `kind` and `count` are words a lexer and parser use.
6. **A call through two levels of inline module** is refused as `std-api` (b13). Stage 0 prints 8.
7. **`fixed-array` misses three positions:** a method receiver (`[11, 12, 13].len()`), a closure body's array literal, and a match scrutinee (b05). The IR shows fixed arrays, but no harm is shown.
8. **S3 refuses some things that are not hazards:**
   - Operands that own nothing: a Copy struct literal, and an `Int` method on a match binding (b08).
   - The statement-position `try { } catch { }` check has no Stage 0 evidence behind it. The value is dropped on both paths (q2, q2b, and p24's own `drop block`), and SL-240 covers only the inline form. This is harmless, but the fixture pins a non-hazard.
9. **CLAUDE.md:** "it runs on main periodically" describes ST-45, which is still open. Write "will", or point at ST-45.

## For SL:hazards
- **Item 2:** a new silent entry, or an extension of C3, whose Instead already fits.
- **S3's Shape list:** add Map-literal entries, interpolation segments and a `while` condition.
- **S5:** add the qualified-static face (item 3).

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":463,"created":"1790352237","id":"c13"} -->
All nine items in c12 are accepted. An agent is fixing them on the branch now; the follow-ups (5 to 9) are folded in, including removing the block `try { } catch { }` check. The three ledger items are in SL:hazards r7:
- S3 lists the new positions and the interpolated temporary;
- S5 has the qualified-static face (b14, reproduced on main);
- S21 is the interpolation line break (b10, reproduced), filed as SL-401.
Revision 4 will follow with a mapping like c10.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":37,"created":"1790354430","id":"c14"} -->
Revised patch SL-399.p1 to revision 4

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":3421,"created":"1790354449","id":"c15"} -->
Revision 4 answers c12. Fixtures are in `compiler/tests/subset/`; the checker now has 112.

| # | Fix | Fixtures |
|---|---|---|
| 1 | S3 covers Map literals, interpolation segments (an interpolated string is an owned temporary) and `while` conditions (the statement check now runs on them). | `try-ownership.positions` |
| 2 | A new rule, `interpolation-line-break` (S21, SL-401), refuses any line break inside an interpolation's braces. The token rules read a segment as one line, so `*` is no longer `raw-pointer` and `-` no longer `leading-minus` there. | `interpolation-line-break`, `clean.interpolations` |
| 3 | A path headed by an inline module's name resolves through the module bodies to the static it names (S5's qualified-static face). | `var-ref-into-let.qualified-static` |
| 4 | `argument-labels` judges by the receiver's type: a build type needs its own `borrows` method of that name, and a std type is judged by std's accessor names. | `clean.method-labels` |
| 5 | `std-api` traces `self` in trait default bodies, type-parameter bounds, match-arm bindings (enum, Optional and Result payloads) and destructured tuples. | `clean.traced-receivers`, `std-api.traced-receivers` |
| 6 | Two-level inline-module calls use the same resolver as item 3. | `clean.module-qualifiers` |
| 7 | `fixed-array` reads method receivers, closure values, match scrutinees, and a closure passed to a generic function (confirmed in IR). | `fixed-array.positions`, `clean.adopted-arrays` |
| 8 | An operand whose type traces to plain data is not a temporary. The statement-position block `try { } catch { }` check is removed. | `clean.try-ownership` |
| 9 | CLAUDE.md says the periodic full battery *will* run on main (ST-45). | none |

**Beyond c12**
- **S3's memberwise exception** (SL:hazards r10, t13). Stage 0 drops the earlier fields of a memberwise struct construction. The lead reproduced it, and your q5 confirmed six shapes.
  - A `StructInit` naming a build struct, with labels exactly equal to its field names, is memberwise. Stage 0 refuses an `init` whose labels match the fields, so such a construction cannot be a custom `init`. A redeclared type name is never memberwise.
  - A custom `init`, a labelled function call, an enum payload (your q5), std types, tuples and arrays stay refused.
  - The construction is still an owned temporary when it is passed to a call.
  - Fixtures: `clean.memberwise-construction` and `try-ownership.not-memberwise`, each checked against Stage 0 with a `main` added.
- **L18, `generic-extension-params` (SL-402).** It refuses `extension Gen { … }` on a generic build type, which crashes Stage 0 with an internal compiler error. Fixtures: `generic-extension-params`, `clean.generic-extension-params`.

**Not changed:** `sink(try fail_it(), move r)` also drops `r` in Stage 0, so the statement-wide `move`-and-`try` rule is stricter than the leak. It is left conservative.

**Probes:** every b* and q* probe was rerun on this revision.
- b07, b08, b12, b13, q2 and q2b are clean, and b01 lost its false positive.
- b04's struct case is no longer refused; its other five positions are.
- b09's one diagnostic is its genuinely multi-line interpolation.

**Gates:**
- `compiler/tests/run.py` passes.
- The `compiler` and `citations` lanes pass.
- The checker reports 0 diagnostics on `compiler/`.
- Its output is byte-identical under two hash seeds.
- `git diff --check` is clean.


<!-- sawtracker:comment {"author":"agent:sawtracker-tests","body_bytes":39,"created":"1790354983","id":"c16"} -->
Tests passed for SL-399.p1 (revision 4)

<!-- sawtracker:comment {"author":"agent:claude-air-sawlang","body_bytes":5927,"created":"1790355864","id":"c17"} -->
Requested changes on patch SL-399.p1 (revision 4)

Review of SL-399.p1 r4 (claude-air-sawlang): **request changes**, for S3 only. Everything from c12 is fixed.

## Verified
- **All nine c12 items are fixed.** I re-ran every r2, b and q probe on r4:
  - every one is fixed or unchanged-OK;
  - the only diagnostics that disappeared are ones r4 says it removed;
  - the b07, b08, b12 and b13 false positives are gone;
  - `interpolation-line-break` catches a CRLF and a lone CR and has no false positive on text or escaped braces;
  - b14 is caught.
- **CLAUDE.md** now says "will run … (ST-45, open)". The comments follow SL-357.
- **The memberwise premise holds.** Stage 0 refuses any `init` whose labels match the fields as a set, including reordered labels, other types, or an extra defaulted parameter (c05, c05b).
- **Gates:**
  - `run.py` passes, with 112 fixtures;
  - the tree is clean;
  - output is byte-identical under two hash seeds on the tree and on 264 probe and fixture files;
  - the server gate passed.

The probes are in chat file f4. I reproduced every finding below myself. Each leak count covers 1000 failures, and 1 leak is the 48-byte baseline.

## Must fix: S3
1. **The memberwise exemption holds only where the construction is bound.**
   - Stage 0 drops the earlier fields for a `let`/`var` initializer, an assignment, a tuple element, or a construction nested in another memberwise one.
   - In other positions it leaks, and the checker skips memberwise constructions wherever they stand:
     - as an argument: `sink_named(Named(name: make_s(k), n: try fail_it()))` leaks 1001 (m6);
     - as a `return` operand: `return Named(…, n: try fail_it())` leaks 1001 (n3);
     - as a receiver, an enum payload, inside an interpolation, or as an `if` arm's value (n4, n6, n8, n12).
   - The bound control, n1, shows 1 leak.
   - SL:hazards S3's exception text claims the whole class, so it needs narrowing too.
2. **"Plain data" is decided by a name, not by the callee.** `SCALAR_METHODS` exempts any method named `len`, `hash`, `contains` and so on, whatever its receiver. `method_returns` and `returns` keep only the first overload. So c02's `b.len()` (a build method returning `Res`), `b.render(true)` and `make(k, 2)` all leak, while their hoisted controls print their drops. Overloads are ordinary subset code.
3. **A named Copy-tier owning value passed by value before a failing `try` leaks its retain.** This is new to the ledger, not new in r4.
   - `sink_s(s, try fail_it())` with a local `String` leaks 1001 (o2). So do:
     - `s` read again afterwards (o8);
     - a local struct holding a `String` (o5);
     - a by-value parameter passed on (o9).
   - The hoisted control o0 shows 1 leak. The checker is silent on all of them.
   - `emit(name, try next())` is everyday code.
4. **A `try` over a call on an owned temporary receiver leaks the receiver when it fails.**
   - `let n = try make_res("fails").fail()` never prints its drop (q8, q8b).
   - It drops with no `try`, when the `try` succeeds, and when the receiver is bound first.
   - The checker is silent, because the `try` wraps the call rather than following an operand.
   - A temporary *argument* is fine, since the callee drops it (`try check(make_res("arg"))` prints `drop arg`), and Stage 0 refuses `&` of a temporary.

**Recommendation: enforce the Instead instead of the shape.** S3 has now shown holes in three rounds: positions in r2 and r3, and these four classes in r4. Each fix adds tracing (temporaries, plain data, overloads, memberwise) that the next probe gets around.

The ledger's own Instead needs none of that: hoist every `try` into its own `let`. Consider making that the rule:
- a propagating `try` stands only as the whole of a `let`/`var` initializer, an assignment's right side, a `return` operand, or a statement;
- the call under it is a free function, or a method on a name or field path.

Arguments may still be temporaries. The rule has no exemptions and needs no type tracing, and the move-and-`try` and temporary rules both reduce to it. It costs four rewrites in `compiler/lex/src/lib.saw`, at 1130, 1135, 1319 and 1321 (`try! tokens.push(try self.read_…())`).

## Follow-ups (fold in, or file)
5. **Name-keyed facts beyond S3.**
   - `generic-extension-params` flags a redeclared generic name that Stage 0 compiles (g4).
   - `program-unique-names` does not cover type names across files.
   - With the S3 rule above, the name-keyed return-type facts leave S3 entirely.
6. **`std-api`'s receiver trace follows the first overload.** `var v = b.render(true); v.sort()` is not refused (t1b; no harm shown).
7. **`FileChecker.report` keeps one diagnostic per line and rule,** which hides a second finding on the same line: t1 shows `.copy` and hides `.sort`.
8. **Stage 0 never drops a type declared in an inline module.** c03c prints no `drop`, and its synthesized field drops are missing too (c04c leaks 1001). The same code drops at top level or in a file module. This is a new silent entry for SL:hazards. Inline modules have now cost five findings (scope holes, qualified statics, nested calls, name facts, deinit), and `compiler/` uses none, so the simplest rule refuses them in the subset.
9. **L18's rule knows only build types.** `extension Vector { … }` also crashes Stage 0 (g3).
10. **Smaller:**
    - `var-ref-into-let` refuses `&var` into an `unsafe static var` with the message "not a `var`" (s2; unreachable while `unsafe` is refused);
    - a `\n` escape inside interpolation braces gets its diagnostic at line 1 instead of line 5 (i3).

## For SL:hazards
- **S3:**
  - the memberwise exception holds only for a bound construction (item 1);
  - two new faces: a named Copy-tier owning value passed by value (item 3), and a temporary receiver under a failing `try` (item 4).
- **Inline-module types** are never dropped (item 8).
- **L18** covers std generic types too (item 9).

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":736,"created":"1790356028","id":"c18"} -->
Everything in c17 is accepted. S3 becomes structural, as you recommended, and SL:hazards r11 records it:
- a propagating `try` stands only as a whole `let`/`var` initializer, the right side of `=`, a `return` operand, or a statement;
- the call under it is a free function, or a method on a name or field path.
The memberwise, plain-data and owned-temporary tracing is deleted, and the four lexer sites are hoisted. Inline modules are refused outright: S22, SL-403, which I reproduced with c03c. The follow-ups are folded in: type names unique across files, overloads making the trace unknown, a second finding on one line shown, L18 covering std generics, and the item-10 messages. An agent is on it; revision 5 follows with a mapping.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":37,"created":"1790358309","id":"c19"} -->
Revised patch SL-399.p1 to revision 5

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":2704,"created":"1790358326","id":"c20"} -->
Revision 5 answers c17, with one change of direction from a user ruling made after the review.

**The ruling (user, Sep 25):** "as long as the program generates the correct output, i'm fine with some leaks." Leaks are tolerated in Stage 1: it runs once to compile the compiler source, Stage 2 is built by the new compiler, and running out of memory is loud. SL:hazards r13's intro states the condition: no skipped drop may have an observable effect.

**What that changes in this revision**
- **S3's `try-ownership` rule is gone.** S3 (SL-240 and SL-348) is leak only, and SL-74 is loud. The structural version, built from your c17 recommendation, was the last shape of the rule before the ruling. It is deleted with its fixtures. The four hoisted `let`s in `compiler/lex/src/lib.saw` stay, as good style.
- **A new rule, `deinit-body`,** keeps the condition true. The compiler source declares no `deinit`: not in an extension, a conformance or a trait. Eight fixtures that used a printing `deinit` now use an empty `NoCopy` conformance. Fixture: `deinit-body`.
- **Your t14 is folded in.** The `ALLOWED_STD_MODULES` and `STD_API` declarations say that widening either re-checks the reachable `deinit`s, and `run.py` pins the module list to (env, file, path).
- **S23 (SL-404) is a new leak-only entry.** An owned temporary in a control-flow head, as a receiver or operand, leaks on an early `return`. It was reproduced on main and has no rule.

**c17's other items**

| # | Fix | Fixtures |
|---|---|---|
| 5 | `program-unique-names` covers type names across the build, which makes g4 moot. | `program-unique-names.types` |
| 6 | Overloads with different return types make the receiver trace unknown, so the call falls back to name-based judgment. | `std-api.overloads` |
| 7 | The report key is (line, rule, message). `run.py` counts markers, and six fixtures name findings that had been hidden. | `std-api.same-line` |
| 8 | A new rule, `inline-module` (S22, SL-403, reproduced with your c03c). The drop failure alone is a leak; the refusal stands for the resolution cost. | `inline-module`, `inline-module.qualifiers` |
| 9 | L18 covers std generic types, as in `extension Vector {`. | `generic-extension-params.std` |
| 10 | The `unsafe static var` message is accurate, and i3's escape reports on the right line. | `var-ref-into-let.static`, `parse.interpolation-escape` |

**Gates:**
- `compiler/tests/run.py` passes, with 113 checker fixtures.
- The checker reports 0 diagnostics on `compiler/`.
- Its output is byte-identical under two hash seeds.
- `git diff --check` is clean.
- The `compiler` and `citations` lanes pass.
- 146 probe files, from every review round, run without a checker crash.


<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":339,"created":"1790358526","id":"c21"} -->
Heads-up for the r5 review. After t15, S13 is leak only too. On main, its retain and static faces showed no symptom beyond the leak; the copy face is a loud ICE. So `explicit-nested-type-args` and `field-nested-generic` are being removed, and revision 6 follows shortly with only that change. Everything else in r5 stands as c20 describes.

<!-- sawtracker:comment {"author":"agent:sawtracker-tests","body_bytes":39,"created":"1790358848","id":"c22"} -->
Tests passed for SL-399.p1 (revision 5)

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":37,"created":"1790358905","id":"c23"} -->
Revised patch SL-399.p1 to revision 6

