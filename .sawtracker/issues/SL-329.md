---
{"assignee":"","author":"agent:claude-sl318u1","body_bytes":2927,"created":"1789844206","id":"SL-329","labels":["design-275","coro"],"priority":"normal","project":"SL","revision":1,"sequence":1038,"stage":"backlog","status":"open","title":"sawtracker's Thread.spawn test worker calls Command.output — the epic's acceptance build now stops here, before SL-215","updated":"1789844206"}
---


## Description

Found by design 275 U1's corpus-wide `--emit-frame-ledger` sweep (2597 programs,
including the read-only `/Users/shawn/Projects/sawtracker/src/main.saw`), and
present on main at 8d752a78 — U1 is behaviour-preserving, so this is U4's
refusal, not U1's.

THE EPIC'S ACCEPTANCE TEST NO LONGER STOPS WHERE THE BRIEF SAYS IT DOES. Design
275 §2 makes "a read-only build of `/Users/shawn/Projects/sawtracker/src/main.saw`
that compiles and whose IR shows no out-of-frame park in any driven path" the
epic's acceptance, and §0 records that sawtracker currently stops at SL-215's
inline-`try…catch` refusal. Since SL-318.p3 (U4) landed it stops EARLIER, at
design 242 ruling 9:

    error: cannot suspend in a `Thread.spawn { ... }` body: closure calls
    `run_test_job` → `execute_tests` → `absolute` → `checked` → `command` →
    `Command.output` → io_wait (`Command.output` suspends at line 494)
      --> src/main.saw:197:35
     197 |         var worker = Thread.spawn { [done, raw] in

This is CORRECT and it is the cell U4's own report called finding (d): std's
`Command.output` reaches BOTH a cooperative park (it parks on the child's exit)
and a blocking offload (the pipe drain), the seeded std leaf carried ONE
representative source label, the thread-body question was answered by whichever
label the diagnostic path happened to pick, and the call was therefore PERMITTED
— it compiled and ran, parking cooperatively on a thread with no executor to
resume it. U4 made the seed carry the whole cause set, so the answer flips to
refused. `examples/conformance/K112_thread_body_refuses_a_multi_cause_std_method.saw`
pins it. No corpus program reaches it, which is why the suite did not move.

WHAT IS OWED, and it is a SAWTRACKER-SIDE change rather than a compiler one:
`test_runner` (src/main.saw:190) spawns an OS thread precisely so a
minutes-long test run stays off the cooperative executor, and the body then
calls `Command.output` through five hops. A `Thread.spawn` body may only block
on FFI; suspending work on a dedicated thread is `TaskGroup(threads: 1)`, which
the diagnostic's hint names. Two shapes to weigh: move the worker into a
`TaskGroup(threads: 1)` (the hint's answer, and the body may then park), or keep
the thread and give it a blocking child-process API — which does not exist and
would be a std question.

WHY IT MATTERS FOR THE EPIC RATHER THAN JUST FOR SAWTRACKER: the acceptance
criterion cannot be reached until this is fixed, and it sits BEFORE SL-215 in
the diagnostic order, so U3's "sawtracker compiles" milestone now has two gates
in front of it, not one. Worth the lead re-sequencing against, and worth the
brief's §0/§2 recording, since the sentence about SL-215 being where the build
stops is now stale.

Verification: the ledger dump for that file is byte-identical before and after
U1 (`# COMPILE-FAILED` plus the message above), so nothing about U1 changed it.


## Comments

