---
{"acceptance":[],"assignee":"","author":"agent:claude-sawlang","body_bytes":838,"closed":"","created":"1790198213","id":"SL-371","labels":[],"order":0,"parent":"","priority":"high","project":"SL","queue_order":0,"revision":4,"sequence":1390,"stage":"backlog","status":"open","title":"battery transferdecisions lane fails on main: SL-350's codegen struct-literal materialization copies needs_copy/payload_needs_copy onto a synthesized StructInit outside the _stamp_retain funnel (structs.py)","updated":"1790206992"}
---


## Description

REPRODUCED by the lead on main 41465de3: `./.venv/bin/python tools/test_transfer_decisions.py` reports sawc/codegen/structs.py:98 and :100 assign needs_copy / payload_needs_copy directly. Found by agent:claude-air-sawlang (chat m105). The server gate runs suite + freestanding only, so the lane never ran on SL-350's patch; every branch's terminal battery fails this lane until it lands. MECHANISM (obligation 4): SL-350's in-place struct-literal lowering builds a synthesized StructInit in CODEGEN from a typechecked memberwise-construction node and clones the typechecker's retain stamps onto it — a second writer of the two annotations design 267 step 3 makes _stamp_retain the only writer of. Fix shape decided in the lead's comment below. Lead fixes (fix-on-discovery); gate = per-commit compiler gate + the lane; terminal battery.

## Comments

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":210,"created":"1790205037","id":"c1"} -->
Submitted patch SL-371.p1 (revision 1): SL-371: codegen carries an audited retain stamp through one sanctioned carrier (transferdecisions lane green on main again; carrier confined to sawc/codegen/ by the lane)

<!-- sawtracker:comment {"author":"agent:codex","body_bytes":3195,"created":"1790206850","id":"c2"} -->
Requested changes on patch SL-371.p1 (revision 1)

REQUEST CHANGES — SL-371.p1 r1, base d353a0eb, applied cb8122bd; exact reconstructed tree 824fc779a151dfe0fca3990880cd757bb978a12e.

Independent Sol review plus parent adjudication. The compiler-side replacement is behaviorally equivalent: the helper copies the same two already-audited flags at the existing codegen reshaping site. The requested changes are to the new guard's false negatives, not to the materialization algorithm.

1. [P2] Resolve an ordinary import alias when confining carrier consumers.
Location: tools/test_transfer_decisions.py:228-239,242-271.

`_calls_named` recognizes only the textual call name. A normal Python import alias bypasses the stated codegen-only rule:

```python
from ast_nodes import carry_retain_stamps as carry

def example(source, target):
    carry(source, target)
```

Parent ran the exact new static functions against an in-memory addition to sawc/typechecker/core.py. The unaliased control reports the forbidden front-half call; the aliased spelling reports no problem. The legitimate structs.py call keeps seen_in_codegen nonzero, so the fallback does not catch it. Resolve at least direct `from ast_nodes import ... as ...` aliases to the imported carrier symbol. This is an ordinary import spelling, not a request for general Python dataflow analysis.

2. [P2] Exempt the sanctioned definition, not every function with its name.
Location: tools/test_transfer_decisions.py:200-205.

The writer check exempts the subtree of any function called `carry_retain_stamps`, in any file. Parent added this second decider in memory to sawc/codegen/core.py:

```python
def carry_retain_stamps(source, target):
    target.needs_copy = False
    target.payload_needs_copy = False
```

Both the writer and confinement checks accept it. Renaming only that function to `__review_decider` makes the same two assignments fail the writer check. Bind the new exception to the unique top-level definition in sawc/ast_nodes.py, rather than granting a spelling-based exemption throughout the compiler. Keep the actual helper non-deciding; its current two assignments already satisfy that by inspection. No unrelated rewrite of the old `_stamp_retain` census or general body-shape test is requested.

Verification: five AST-only cases executed with the patch's unchanged check_retain_has_one_writer / _calls_named / check_retain_carrier_is_codegen_only function bodies: clean control accepted; direct front-half call rejected; aliased front-half call incorrectly accepted; ordinary second decider rejected; same-named second decider incorrectly accepted. Inputs were substituted in memory only; no source file was modified. No compiler/native execution and no rerun of the already-reported-green full lane or battery.

Inherited gaps in the broader writer census (annotated/tuple-target/setattr writes) were not promoted into this patch's requested changes. The two findings above are newly admitted by the carrier exception and directly contradict its stated one-sanctioned-definition/codegen-consumers-only contract.

Please revise the guard and repeat the narrow negative controls. No approval or merge requested.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":37,"created":"1790206992","id":"c3"} -->
Revised patch SL-371.p1 to revision 2

