---
{"assignee":"agent:codex","author":"agent:claude-sawlang","body_bytes":233,"created":"1789086391","id":"SL-256","labels":["prototype","selfhost"],"priority":"normal","project":"SL","revision":5,"sequence":560,"stage":"backlog","status":"open","title":"minivm M12: checked unsigned parsing implementation","updated":"1789131851"}
---


## Description

SL-242 epic child, remaining work (design doc prototypes/minivm/M12_UNSIGNED_PARSE.md lands with SL-253 p1). String.to_uint-shaped checked unsigned parsing per the doc. Schedule via the normal process after the landing patches merge.

## Comments

<!-- sawtracker:comment {"author":"agent:codex","body_bytes":106,"created":"1789131291","id":"c1"} -->
Submitted patch SL-256.p1 (revision 1): minivm M12: checked unsigned parsing and initial sawc differential

<!-- sawtracker:comment {"author":"agent:codex","body_bytes":2539,"created":"1789131401","id":"c2"} -->
SL-256 M12 is ready for review: checked String.to_uint() / String.to_uint(radix:) through the Saw-written frontend, VM, verifier, and handwritten LLVM emitter. Parsing preserves the full UInt64 range, checks bases 2..36, accepts an optional leading plus, and returns None for malformed input or overflow. Both result words are set on every path, including failure after a prior successful parse.

The patch adds six execution fixtures and four diagnostic fixtures. It includes the unchanged lexer literal_fits helper with explicit width/radix boundary oracles, and a direct representation contract with isolated invalid-instruction checks and native success/failure reset assertions.

SL-260 is started, not completed: --sawc sawc/sawc.py runs the new shared-subset fixtures through Python sawc as well. Nine cases must agree. One existing M8/M12 difference is explicitly recorded and checked: the prototype receiver snapshot contract accepts text.to_uint(radix(&var text)), while Python sawc correctly rejects the overlapping borrow. The known-divergence entry requires exactly one exclusive-access diagnostic and fails if the discrepancy changes. The common String receiver borrow rule remains SL-260 follow-up work; this is not a Python compiler bug. This limitation is documented in the M12 design and README and reported on SL-260.

Validation on the submitted source:
- Full prototype gate: 346 cases passed, VM plus native -O0 and -O2.
- Python sawc differential: 9 agreement cases, 1 known divergence verified.
- Direct uint_parse_contract: compiled and passed; its emitted native program passed at -O0 and -O2 with no output or errors.
- Native invalid-input fixture passed AddressSanitizer.
- literal_fits function text matches the existing lexer exactly.
- Full compiler suite: 2438 passed, 14 expected failures, and 15 network failures in the sandbox. All 15 failures passed on a targeted rerun with network access enabled: combined result 2453 passed, 14 expected failures.
- Local freestanding runner could not start because this host lacks QEMU, ld.lld, and cross-target LLVM clang. Sawtracker's configured test host must complete its standard compiler + freestanding gate before merge.
- git diff --cached --check passes. Changes are confined to prototypes/minivm; no production compiler edits.

Implementation/review worktree: /Users/swoodtke/Projects/sawlang-sl256. The remaining lexer milestones (Scalar, StringBuilder, Vector, whole-lexer acceptance) are not included. Approval and merge are left to the reviewer/user.


<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":1478,"created":"1789131851","id":"c3"} -->
LEAD REVIEW of SL-256.p1 (r1, base 314d0865): VALIDATED, recommending approval. Method: patch applied clean onto origin/main in a lead worktree; minivm rebuilt with the Python compiler; FULL harness 346/346 (VM + clang-native -O0/-O2 + rejections + limits); the new SL-260 differential lane run end-to-end: 9 cases agree with Python sawc, 1 known divergence (receiver_snapshot exclusivity) verified against its diagnostic-checked ledger; direct tests/uint_parse_contract.saw compiled+run green (incl. all verifier mutation rows — range_matches is overflow-safe at dst=Int.max). Semantics cross-checked against std: _parse_uint (string.saw:630) and parse_uint_bytes agree on radix bounds->None, empty/lone-sign->None, leading +, whole-string, overflow->None; the to_uint()/to_uint(radix:) overload pair matches the frontend arm incl. the four rejection shapes. Expected-output oracles spot-verified by hand (base-36 0xff=43323, zZ=1295, the 2^64-1 constants per radix). Notes, none blocking: (1) native parser hard-codes the string data offset 16 — consistent with the M8 layout and proven by the boundary cases running native; (2) the known-divergence ledger requires exit 1 + exactly one error: + the exact substring, so any drift in the sawc diagnostic fails loud — good; (3) harness limit_cases restructure is behavior-equivalent (limits still run under all/core only, as before). Review worktree lead-sl256-review retained until merge. Approval + merge are the user's.

