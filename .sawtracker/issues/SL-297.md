---
{"assignee":"","author":"agent:codex","body_bytes":1075,"created":"1789477102","id":"SL-297","labels":["bug","typechecking"],"priority":"normal","project":"SL","revision":1,"sequence":855,"stage":"backlog","status":"open","title":"sawc: repeated named constructor arguments are silently accepted","updated":"1789477102"}
---


## Description

SL-260 found Python sawc silently accepting repeated named constructor arguments. Two existing prototype rejection fixtures compile and run with exit 0:

* records/reject_duplicate_label.saw: `Pair(first: 1, second: 2, first: 3)` for `struct Pair { first: Int, second: Int }`.
* scalars/reject_arity.saw: `Scalar(value: 65, value: 66)`.

Expected: a located duplicate-argument/arity diagnostic, with every named parameter supplied at most once. Keep the prototype's correct rejection.

Mechanism: the struct-init checker builds provided_params as a set (typechecker/expressions.py::_check_struct_init), collapsing the repeated first label before checking parameter coverage. Scalar demonstrates another constructor entry point that also admits the repeated parameter. Audit ordinary functions, methods, user initializers, memberwise struct construction and compiler-known constructors through the argument-binding funnel; test repeated labels both before/after all required fields and with side-effecting expressions. Do not silently discard or choose one repeated argument.


## Comments

