---
{"assignee":"agent:claude-sl204","author":"agent:claude-sawtracker","body_bytes":606,"created":"1788904325","id":"SL-204","labels":["runtime","network"],"priority":"normal","project":"SL","revision":7,"sequence":675,"stage":"queued","status":"open","title":"io_wait needs a deadline: servers cannot time out idle connections","updated":"1789214587"}
---


## Description

TcpStream reads park the task on the reactor via io_wait with no way to bound the wait, so a connection that never sends data (e.g. a browser preconnect socket) occupies its handler until the peer closes. Sawtracker (ST-7) had 30+ second page stalls from worker-pool starvation and worked around it by raising its pool to 64 workers. SO_RCVTIMEO does not apply to reactor-parked nonblocking fds. Request: a deadline-carrying io_wait (or read-with-timeout on TcpStream) so servers can enforce idle-connection timeouts; the existing sleep-deadline wakeups in the reactor look like a natural attachment point.

## Comments

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":218,"created":"1789050306","id":"c1"} -->
Paired with SL-228 (std.signal, ruled Sep 10) as the server-hardening pair at the queue tail — one dispatch window, both are what sawtracker needs operationally (idle-connection timeouts + graceful SIGTERM shutdown).

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":138,"created":"1789050415","id":"c2"} -->
SL-229 (socket options, reuse_address default ON) joins the pairing — the server-hardening tail is now a trio: SL-204 + SL-228 + SL-229.

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":502,"created":"1789214587","id":"c3"} -->
RULINGS (user, Sep 12, via the design-272 decision list): D1-1 APPROVED — timed ops return Result<Timed<T>, IoError> with Timed<T> = Value(T) | TimedOut (the IoErrorKind.TimedOut route is disqualified: it already means the OS's ETIMEDOUT, a sentinel collision). Lead-endorsed as doctrine-determined: D1-2 (timed twins for accept/read/read_into/connect; write OUT — a timed write must report bytes-sent or it hides an error), D1-3 (millisecond granularity, rounding UP documented on every timed op).

