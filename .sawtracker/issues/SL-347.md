---
{"acceptance":[],"assignee":"agent:claude-sl347","author":"agent:claude-sawlang","body_bytes":6394,"closed":"","created":"1790086803","id":"SL-347","labels":["design","parser","style"],"order":0,"parent":"","priority":"normal","project":"SL","queue_order":0,"revision":14,"sequence":1237,"stage":"queued","status":"open","title":"Design (RULED Sep 22): semicolon is the one-line statement separator, bare juxtaposition is refused, and a brace body is one line or fully broken (style)","updated":"1790170604"}
---


## Description

# `;` is the one-line statement separator; bare juxtaposition is refused; a brace body is one line or fully broken

RULED by the user, Sep 22 2026. Lead-authored brief; implemented by a dispatched agent after SL-318.p7 lands (the parser change touches every one-line body in the corpus, so it must not race an in-flight branch).

## 0. The finding

sawtracker's source carries shapes like

```saw
if arg == "--help" { help()
    return }
```

which the grammar accepts because Saw already parses SEVERAL STATEMENTS ON ONE LINE WITH NO SEPARATOR AT ALL: `if arg == "--help" { help() return 1 }` compiles and runs today (probed Sep 22; the two-space spelling the skill shows, `{ c in c = c + n  c }`, is a convention, not grammar). The separator is invisible, so a reader cannot tell one statement from two on a line (`let a = b (c)`, `x -1`), and the half-broken brace layout hides the body's last statement beside the closing brace. The user's ruling: what we allow now is confusing and could lead to unexpected bugs.

## 1. The rules (RULED)

**R1 — `;` separates two statements on one line.** `a; b` is two statements. Whitespace around it is free. The lexer already has the token (used in `[T; N]`), so nothing new is lexed; the type-position meaning is untouched because a `;` inside `[ ]` in a TYPE is not in statement position.

**R2 — `;` is a SEPARATOR, never a terminator.** A `;` followed by a newline, by `}`, by EOF, by another `;`, or a `;` at the start of a line is a clean error: ``a `;` joins two statements on one line; it does not end one — remove it`` (Saw has no statement terminators; that stays true). This keeps the language's "no semicolons" character: you write one only where two statements share a line.

**R3 — BARE JUXTAPOSITION IS REFUSED.** After a statement, the parser requires NEWLINE, `;`, the block's closing `}`, or EOF (and, inside a match arm's bare body, the arm's `,` — the arm grammar is unchanged). Anything else is ``two statements on one line need a `;` between them`` with a fixit naming the two statements' first tokens. The position matrix (obligation 1 — ONE chokepoint, its docstring naming the entries): function/method bodies, `if`/`else`/`while`/`for`/`match`-arm BLOCK bodies, closure bodies (`{ x in a; b }`), `try { } catch { }` blocks, `guard … else { }`, the `borrows` body, top-level module scope (two DECLARATIONS on one line are refused too — declarations are not joined by `;`, they take a newline). Every entry gets a row in the errors pin.

**R4 — `[T; N]`, the repeat literal `[v; N]` and nothing else keep their `;`.** Both are inside brackets where no statement can start.

**R5 — STYLE (the skill ruling, lenient form): a brace body is ONE LINE or FULLY BROKEN, never half-way.** Either the whole body sits on the brace's line (`if done { return }`, `{ c in c = c + n; c }`), or the opening brace ends its line, every statement has its own line, and the closing brace stands alone. `{ help()` + newline + `return }` is the shape the rule exists to catch, and it stays DISALLOWED BY STYLE even though the grammar accepts it (R3 does not refuse it — the newline is a legal separator). Prefer the broken form as soon as a body carries a `return`/`break`/`continue` beside anything else.

## 2. Units (one branch, the two must land together — the refusal breaks the corpus without the sweep)

**U1 — parser + diagnostics + docs.** The statement-end chokepoint in `sawc/parser/core.py` (name it; every block-parsing entry calls it), R2's refusals, R3's refusal with the fixit; the SELFHOST parser/lexer twins (`lexdiff`/`astdiff` parity lanes — the selfhost lexer already lexes `;`); `examples/errors/` pins: one file per R2 shape, one file with a row per R3 position, and a RUNNING example of `;`-joined one-liners in every block kind incl. the closure tail (`{ x in let y = x + 1; y * 2 }`), the match-arm block, `[T; N]` and `[v; N]` beside them (R4). LANGUAGE_SPEC (the statement grammar + R5 in the style section), the saw-lang skill (the "NO semicolons" line in Core syntax is rewritten: no TERMINATORS; `;` joins two statements on one line; the STYLE ruling R5 verbatim under the user's Sep-22 date; every two-space example in the skill gains its `;`), README where it says newlines end statements.

**U2 — the corpus sweep (mechanical; a mech/Sonnet agent is fine under the brief's oracle: the compiler's own R3 refusal + the full suite; nothing to invent).** Every one-line multi-statement body in the tree gets its `;`: `examples/`, `examples/conformance/`, `sawc/std/`, `sawc/rt/`, `sawc/builtin.saw`, `blade/`, `libs/`, `devtools/`, `tests/`, `selfhost/`. The count is what U1's refusal reports over the corpus — record it. sawtracker's sources are NOT this repo's (record its count read-only; the user sweeps that repo). The half-broken layouts (R5) found in THIS tree are reformatted in the same sweep, listed.

## 3. Obligations

- **Obligation 1:** R3 is position-quantified → the one statement-end chokepoint with named entries + the row-per-position pin.
- **Obligation 2 (consumer sweep, a contract flip: juxtaposition legal → refused):** who writes one-line multi-statement Saw? The corpus (U2 counts it), the coroutine transform and the `#lend_var` fold (they generate AST, not text — confirm no text-level emission anywhere: grep `sawc/` for source-string synthesis), `--emit-docs`/`--emit-ast` (readers, unaffected), the fuzzer (`tools/sawfuzz.py` mutates text — a mutation that joins lines now yields a clean diagnostic, which is its oracle's success case; confirm no `sawfuzz_known.txt` entry rests on juxtaposition), `devtools/dogfood` programs (records of past sessions; if they stop compiling they were not compiling before either — check), sawtracker (external, read-only).
- **Obligation 4:** the finding's mechanism is "the statement loop never asked what ends a statement"; the sibling position that mechanism reaches is the top-level declaration loop (two declarations on a line) — R3 covers it as a row.

## 4. Gates + sequencing

Compiler branch: per-commit full suite + freestanding both arches; terminal full battery (lexdiff/astdiff parity is where a selfhost divergence would show). Sequenced AFTER SL-318.p7 lands; before SL-335 (`#lend_var` retirement) and the U3 follow-ups, since every later branch would otherwise carry the sweep's conflicts.


## Comments

<!-- sawtracker:comment {"author":"agent:claude-sl347","body_bytes":7319,"created":"1790102472","id":"c1"} -->
## SL-347 patch 1 (U1a + docs) — BUILT, branch `sl347`, PARKED for lead validation

Scope per the lead's Sep-22 rescope: the chokepoint, R1 accepted, R2 refused,
R4 untouched, the pins, the docs. R3's juxtaposition refusal and the corpus
sweep are patch 2. No XFAIL added, no corpus file swept, the skill's existing
two-space one-liners untouched.

### The chokepoint and its entries

`Parser.expect_statement_end(previous_start=None, *, declarations=False)` in
`sawc/parser/core.py`, beside `skip_newlines`. Called once per GAP between
statements — including the gap BEFORE a list's first statement, where
`previous_start` is None, nothing precedes, and only the line-start half of R2
can fire. Entries, named in its docstring, all three:

1. `StatementsMixin.parse_block` — the ONE block-body funnel, which is why the
   rule reaches function and method bodies, `borrows` bodies, `if`/`else`/
   `while`/`for` bodies, a `match` arm's BLOCK body, both blocks of
   `try { } catch { }`, and `guard … else { }`. (Verified: `parse_block` is the
   only callee at all 13 block sites across `declarations.py`, `statements.py`
   and `expressions.py`.)
2. `ExpressionsMixin._parse_closure_body` — closure bodies.
3. `Parser.parse` — module scope, whose units are DECLARATIONS
   (`declarations=True`).

A `match` arm's BARE body ends at the arm's `,`, but that body is an expression
and not a statement list, so it never reaches the chokepoint — `parse_match`
owns that comma, unchanged.

`Parser.at_statement_end()` is the LOOKAHEAD half of the same rule, replacing
the four open-coded `NEWLINE/RBRACE/EOF` tuples at the statement forms whose
operand is optional: `return`, `break`, `lend`, and the closure body's `return`
(which also gains EOF, which it did not test before).

### The R2 diagnostics, verbatim

    a `;` joins two statements on one line; it does not end one — remove it

fires at all five shapes, each with its own `examples/errors/` pin:

| shape | pin | anchor |
|---|---|---|
| before a newline | `semicolon_before_newline.saw` | 11:14 |
| before the block's `}` | `semicolon_before_closing_brace.saw` | 12:19 |
| at end of file | `semicolon_at_end_of_file.saw` | 12:22 |
| doubled | `semicolon_doubled.saw` | 11:14 |
| at the start of a line | `semicolon_at_line_start.saw` | 13:5 |

and at module scope, where a `;` really does separate two declarations:

    declarations are not joined by `;` — put each declaration on its own line

pinned by `examples/errors/semicolon_between_declarations.saw` (11:24). Order
matters: the R2 check runs first, so a trailing `;` at EOF gets the "ends
nothing" message and only a `;` with a declaration after it gets this one.

R4 needs no code: the `;` in `[Int; 3]` and `[0; 3]` sits inside brackets,
where no statement can begin, and the chokepoint is never reached there.

### The running pin

`examples/semicolon_joins_statements_on_one_line.saw` — one `;`-joined
one-liner per block kind the chokepoint covers: function body, method body,
both branches of an `if`, a `while` body, a `for` body, a `match` arm BLOCK
body, a closure body whose last statement is the closure's TAIL
(`run({ x in let y = x + 1; y * 2 })`), both blocks of `try { } catch { }`, a
`guard … else { }`, and a `borrows` body — with `[Int; 3]` and `[0; 3]` beside
them. 17 EXPECT-OUTPUT lines, all matched.

### Corpus evidence (behaviour preservation)

All **3387** tracked `.saw` files parse to the SAME outcome as main — 3278
accept, 109 refuse — with exactly one message improved:
`prototypes/minivm/examples/reject_semicolon.saw` was
`Unexpected token: SEMICOLON` and is now the R2 message. It is a minivm
RejectCase with no `compare_sawc`, and its `differential_manifest.json` row is
`agreement` on the OUTCOME (reject), which is unchanged.

### R3 dry run — what patch 2 owes (report-only; nothing shipped)

Monkeypatched the chokepoint to RECORD rather than refuse. **174 juxtaposition
sites in 83 files**, this tree (pre-U3):

| tree | sites | files |
|---|---|---|
| `examples/` | 127 | 52 |
| `examples/conformance/` | 31 | 20 |
| `examples/errors/` | 9 | 7 |
| `sawc/std/` | 5 | 2 |
| `prototypes/` | 2 | 2 |
| `blade/`, `libs/`, `devtools/`, `tests/`, `selfhost/`, `sawc/rt/`, `sawc/builtin.saw` | 0 | 0 |

Declaration-position sites (two declarations on one line): **0**.

**A MECHANISM PATCH 2 MUST HANDLE, found by the dry run.** The naive detector
first reported **6597** sites. 6423 of those are false: `parse_if_expression`
(both the `if` and the `if let` arms) and `parse_try_catch` call
`skip_newlines()` to look ahead for `else`/`catch`, so when no `else`/`catch`
follows, the statement-ending newline is already CONSUMED by the time the
chokepoint runs. A plain multi-line `if` followed by the next statement then
looks exactly like juxtaposition. The 174 above are what remains once a site is
required to be on the SAME source line. Patch 2 must make those lookaheads
restore the position when the keyword does not follow (behaviour-neutral today,
since the enclosing loop skips the newline anyway) before R3 can be switched on.

### Gate

Per-commit gate on the tip, under the machine-wide lock:

- full suite: **2604 passed, 10 xfailed** (the 10 are the pre-existing cited
  pins; none added, none removed)
- `tools/freestanding_runner.py`: **36 passed across riscv32 + arm64**
- `tools/docverify.py` for the three doc sources touched: LANGUAGE_SPEC 373
  blocks OK (105 compiled / 62 refused / 206 exempt), README 27 OK, SKILL 108 OK

Terminal battery: DEFERRED to patch 2's final tip by the lead's ruling. Before
the stop it had run these stages green — `suite` (62s, 2604/10),
`icebreadcrumb`, `lexdiff`, `astdiff`, `astgraft`, `transferdecisions`,
`producertaxonomy`, `corodiscovery`, `corototality`, `stdseed`, `citations`,
`forgetgate`, `ircontract`, `preludegate`, `docverify`, `moduletag`,
`stdtypes`, `toolchain`, `floatvectors`, `abidoc`, `bttable`, `fuzz`,
`corodiff`, `bench`, `selfhostlex`, `minivm`, `reemit` (1614 identical, 0
DIVERGENT) — and `irdet` was killed mid-run (SIGTERM) after 1216 of 1761
records with **0 MISMATCH, 0 VIOLATED INVARIANT**. One earlier full-battery run
reported its `suite` stage failing; the suite then passed three consecutive
times (standalone, `battery.sh suite`, and inside the second battery), so that
one is unexplained and unreproduced — flagging it rather than absorbing it.

Selfhost: there is no selfhost PARSER yet (`selfhost/` holds the lexer only),
and its lexer already emits `Semicolon`, so nothing was owed there. `lexdiff`
and `selfhostlex` are green.

### Commits (branch `sl347`, on `main`)

- `ef45ca5b` — SL-347 U1a: `;` is the one-line statement separator
- `95983096` — SL-347 docs: the statement separator and the brace-body layout rule

Docs: LANGUAGE_SPEC's "Statement Boundaries" states the grammar and adds a
"Layout" subsection carrying R5; the saw-lang skill's "No semicolons" line is
rewritten and R5 is quoted verbatim under "(user ruling, Sep 22)"; README gains
the rule as a fifth smaller rule; `designs/todo.md` gains the `[QUEUE]` line
naming the two-patch split. Spec and skill both say plainly that bare
juxtaposition is still accepted today and is being withdrawn.

PARKED. Awaiting the lead's word to start patch 2 from the post-U3 main.


<!-- sawtracker:comment {"author":"agent:claude-sl347b","body_bytes":11965,"created":"1790139707","id":"c2"} -->
## SL-347 patch 2 (the lookahead restore + U2 sweep + R3 refusal) — BUILT, branch `sl347`, PARKED

Three new commits on top of patch 1, rebased onto the post-U3 main. Terminal
battery GREEN, all 32 stages.

### The rebase

`git rebase origin/main` (1938880b). One conflict, in `designs/todo.md`'s
`[QUEUE]`: main had SL-333 rewritten from **BUILT** to **LANDED Sep 21
(SL-333.p1 r5, merge 47373c08)**, my side had SL-333 still **BUILT** plus the
new SL-347 line. Resolved hunk by hunk in the editor — kept main's LANDED text
verbatim and re-added the SL-347 line above it. Sanity-checked afterwards: the
file holds exactly one SL-347 entry, main's `LANDED Sep 21 (SL-333.p1 r5` text,
the `SL-318.p7` mention and the design-275 U3 entry, and `git diff origin/main
-- designs/todo.md` is exactly the SL-347 addition and nothing else.
SKILL.md / LANGUAGE_SPEC.md / README.md auto-merged.

Gate on the rebased tip BEFORE any new work: patch 1's running pin
(`examples/semicolon_joins_statements_on_one_line.saw`) compiles and prints its
17 lines; full suite **2645 passed, 10 xfailed**; freestanding **36 passed
across riscv32 + arm64**.

### 1. The lookahead restore — `876f5f7d`

Four sites peek ACROSS newlines for a keyword and did not rewind on the miss:
`parse_if_expression`'s `else` in BOTH arms (the `if let` one and the plain
one), the inline `try … catch`, and — found by the grep the brief asked for,
over every `skip_newlines()` followed by a keyword test — the `module name
{ … }` head, which is the DECLARATION-scope twin of the same mistake. Each now
saves the position and restores it when the keyword does not follow, the shape
`while let`'s `else` refusal (`statements.py:463`) and a trait method's default
body (`declarations.py:537`) already used.

**The dry-run count: 6726 → 246.** Measured with the chokepoint monkeypatched
to RECORD at its entry — exactly where R3's refusal now stands — over all 3435
tracked `.saw` files. The SAME-LINE subset, which is the real sites, is **246
either way**, which is what makes the restore behaviour-neutral: it removes
6480 phantom sites and no real one. (Patch 1 reported 6597 on the pre-U3 tree
with 174 real; U3's new files account for the growth.) Full suite and
freestanding green on this commit alone.

### 2. The sweep — `ec796798`

The compiler is the oracle, run to a fixpoint. **Zero sites remain** anywhere.

| tree | sites | files |
|---|---|---|
| `examples/` | 176 | 57 |
| `examples/conformance/` | 52 | 25 |
| `examples/errors/` | 11 | 8 |
| `sawc/std/` (json, taskgroup) | 5 | 2 |
| `prototypes/` | 2 | 2 |
| `blade/`, `libs/`, `devtools/`, `tests/`, `selfhost/`, `sawc/rt/`, `builtin.saw` | 0 | 0 |
| **`.saw` total (fixpoint)** | **244** | **94** |
| string INTERPOLATION, by hand | 2 | 2 |
| doc fences (LANGUAGE_SPEC 13, SKILL 14, README 0) | 27 | 2 |
| skill fragments neither docverify shape parses, by hand | 5 | 1 |

Two things the mechanical pass could NOT do, and why:

- **A string-interpolation sub-parser numbers its own tokens from 1:1**
  (`_parse_expression_from_string`), so a site inside one carries a position
  that is not the file's. The recorder keeps those apart instead of trusting
  them — a first attempt did trust them and wrote `; ; ; ; …` into a comment
  on line 1 before I caught it and reverted. The two real ones are
  `examples/arc_with_unique.saw:39` and
  `examples/coro_inline_catch_loop_exit_in_child_positions.saw:2669`.
- **`docverify.extract` DEDENTS a fence body by its opening indent**, so a
  site's column inside the body is `indent` characters left of its column in
  the markdown. Every doc edit is rebased by its fence's indent and then
  VERIFIED against the token it is supposed to precede before it lands (a
  mismatch aborts the tool). The first doc pass, without that, put a `;` two
  characters off — reverted and redone.

**R5 — every half-broken layout in this tree, listed and reformatted:**

| file | what |
|---|---|
| `examples/errors/capture_assign_escaping.saw` | the escaping closure `{ n = n + 1` / `n }` |
| `examples/init_fallible_result.saw` | two `Printable` match arms |
| `examples/init_fallible_result_shapes.saw` | one `Printable` match arm |
| `prototypes/minivm/examples/builders/` | 7 fixtures (`reject_append_arity`, `reject_append_label`, `reject_append_type`, `reject_assignment`, `reject_copy`, `reject_nested_receiver`, `reject_shared_append`) |
| `prototypes/minivm/examples/vectors/` | 14 fixtures (`evaluation_once` ×2, `index_at_end`, `index_negative`, `reject_assignment`, `reject_borrow_after_move`, `reject_double_move`, `reject_get_label`, `reject_implicit_copy`, `reject_index_type`, `reject_move_field`, `reject_move_reference`, `reject_outer_loop_move` ×2, `reject_push_label`, `reject_push_type`) |

The four hits the R5 scan still reports are `import m.{...}` symbol lists,
which design 147 makes a delimited list rather than a statement container —
they wrap by design.

**NO EXPECT line changed**: `git diff` filtered to every `EXPECT-`, `XFAIL`
and `COMPILE-FLAGS` line over the whole sweep is empty. Gate on this commit:
suite 2645/10, freestanding 36, plus the four lanes that cover what moved —
docverify (513 blocks OK), minivm (446 cases), lexdiff, astdiff (3435 files,
byte-stable).

### 3. R3 — `4e96321e`

`Parser.expect_statement_end` refuses anything but NEWLINE / `;` / `}` / EOF,
anchored at the SECOND statement's first token — where the separator is
missing — and naming BOTH heads. Verbatim:

```
error: Parse error at 2:19: two statements on one line need a `;` between them: the first begins with `let`, the second with `print` — write `;` where they meet
```

and at module scope, where the units are declarations:

```
error: Parse error at 24:23: two declarations on one line: the first begins with `static`, the second with `static` — declarations are not joined by `;` — put each declaration on its own line
```

A `match` arm's BARE body is untouched — it ends at the arm's `,`, which
`parse_match` owns, and that body is an expression, not a statement list.

**THE POSITION MATRIX — 13 rows over four `examples/errors/` pins.** Split
across files on purpose: the file-level parse loop batches at most
`MAX_PARSE_ERRORS = 10` and recovery adds an orphan-`}` diagnostic per row, so
a single 13-row file TRUNCATES and half the matrix would go unchecked. Every
row names a different pair of first tokens, so no row's expectation can be
satisfied by another row's error.

| pin | rows |
|---|---|
| `statement_juxtaposition_in_function_and_method_bodies.saw` | function body, method body, `borrows` body |
| `statement_juxtaposition_in_control_flow_bodies.saw` | `if`, `else`, `while`, `for` |
| `statement_juxtaposition_in_match_guard_and_try_blocks.saw` | `match` arm BLOCK body (bare arm beside it), `guard … else`, `try { }`, `catch { }` |
| `statement_juxtaposition_in_closure_and_module_scope.saw` | closure body (`_parse_closure_body`), module scope (`Parser.parse`) |

### Obligation 2 — the consumer sweep, with evidence

1. **The fuzzer.** `tools/sawfuzz.py`'s oracle is "succeeds or exits with a
   clean diagnostic", so a line-joining mutation now yields R3's refusal, which
   is a PASS. `tools/sawfuzz_known.txt` holds exactly ONE entry — DF-270e,
   `internal-compiler-error: … Undefined function: UIntN` — and nothing in it
   rests on juxtaposition. `sawfuzz --quick` in the battery: 150 mutants,
   **0 known hits, 0 failing compiles, 0 NEW findings**.
2. **Passes that synthesize SOURCE TEXT rather than AST.** There is exactly
   one: `ExpressionsMixin._parse_expression_from_string`, which re-lexes the
   text of a string interpolation — text the AUTHOR wrote, so the rule reaches
   it exactly as it reaches the enclosing file, and the two corpus sites are
   swept. The coroutine transform builds AST and never constructs source (zero
   `Lexer`/`tokenize` references); `place_transform` names the lexer only in a
   comment about running inside `parse_source`; the std seed writes a cache
   blob and `--emit-docs` writes JSON; `ast_dump`'s `f"func …"` strings are a
   DUMP, never re-parsed. The only other `Lexer(...)` callers are `sawc.py` and
   `module_resolver.py`, both over real files.
3. **Saw embedded in the PYTHON HARNESSES — a third home for Saw source that
   neither the corpus sweep nor the doc sweep reaches.** The battery caught one:
   `tools/test_transfer_decisions.py`'s `generic_body_defers_to_specialization`
   fixture (`func hold<T>(x: T) -> Int { let a = x  1 }`), which failed the
   `transferdecisions` lane and is fixed in the R3 commit. A scan of every
   triple-quoted Saw string under `tools/`, `devtools/` and `prototypes/` finds
   four more, all in `prototypes/parser/test_parser.py` — and those are INPUT
   fed to the selfhost parser PROTOTYPE binary, not programs sawc compiles
   (sawc only compiles `prototypes/parser/src`). Its own harness already
   expects a `boundary` error for `func f() { x = 1 y = 2 }`, so the prototype
   agrees with R3 and the fixtures stay as written.
4. **`devtools/dogfood`.** 5 of 7 do not compile — `w1_chatroom`,
   `w1_filesearch`, `w1_limiter`, `w1_mapreduce`, `w1_pipeline` — and each
   fails IDENTICALLY under main's compiler (verified by running the main
   checkout's `sawc` on the same files): design 234 allocator-tier drift (a
   discarded `Result`; `Channel()`/`Mutex(...)` now returning one). Not a parse
   error among them. `llm_client` and `w1_jobqueue` compile.

Obligation 3 is not engaged: R3 is a grammar refusal, not a safety guarantee,
so it owes no `examples/conformance/` row — the four errors/ matrix pins are
its coverage.

### Docs

LANGUAGE_SPEC's and the skill's "still accepted today … being withdrawn"
sentences become the refusal, each carrying the diagnostic (as a pinned
`saw-error` block) and the full position list; README's fifth smaller rule
gains the clause. The skill adds the older-build note, since
`{ c in c = c + n  c }` compiled until yesterday. `designs/todo.md`'s SL-347
entry is closed in place with the p2 landing note.

### The terminal battery — ONE run, on the final tip

**32 of 32 stages GREEN in 3817s.**

- `suite` **2649 passed, 10 xfailed** (four new pins; the xfail count did not
  move — none added, none removed)
- `freestanding` 36 passed across riscv32 + arm64
- `docverify` 515 blocks OK (141 compiled, 79 refused, 295 exempt)
- `astdiff` 3435 files, byte-stable; `lexdiff`, `selfhostlex` green
- `minivm` 446 cases; `corodiff` 45 pairs, 0 new findings
- `fuzz` 150 mutants, 0 known hits, 0 new findings
- `reemit` identical 1638, **DIVERGENT 0**
- `irdet --all` 1634 examples, byte-identical IR, 0 mismatch / 0 invariant
- `bootstrap`, `gmgate`, `transferdecisions`, `corodiscovery`, `corototality`,
  `windowseam`, `ircontract` and the rest green

**The one failure, recorded not absorbed.** The FIRST full battery run (before
the harness fix) failed exactly one stage: `transferdecisions`, on the embedded
fixture above — a real regression my sweep had not reached, not a flake. The
fixture is fixed, the lane re-run standalone (`14 boundary fixtures, 6 identity
checks, 33273 decisions`), and the WHOLE battery re-run from scratch on the
amended tip, which is the 32/32 above. Nothing else flaked in either run.

### Commits (branch `sl347`, on `origin/main` 1938880b)

- `de3b0388` — SL-347 U1a: `;` is the one-line statement separator *(patch 1, rebased)*
- `5c02d37e` — SL-347 docs: the statement separator and the brace-body layout rule *(patch 1, rebased)*
- `876f5f7d` — SL-347 U1b/1: a keyword peek restores the statement-ending newline
- `ec796798` — SL-347 U2: the corpus takes the `;` every one-line body now owes
- `4e96321e` — SL-347 U1b: bare juxtaposition is refused

No XFAIL added. No finding filed: nothing outside the brief's scope came up.
PARKED for the lead's validation and `patch add SL-347`.


<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":402,"created":"1790139797","id":"c3"} -->
Submitted patch SL-347.p1 (revision 1): SL-347 (RULED Sep 22): semicolon is the one-line statement separator (never a terminator), bare statement juxtaposition is REFUSED with a fixit, keyword lookaheads restore the statement-ending newline, the corpus sweep (244 .saw sites + 27 doc fences, R5 reformats, no EXPECT changed), 4 matrix pins, spec/skill/README (the brace-body layout rule); battery 32/32

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":885,"created":"1790139835","id":"c4"} -->
SUBMITTED as SL-347.p1 (lead, Sep 23): ONE patch carrying both units per the user's ruling (5 commits on main 1938880b, squashed: 136 files +1028/-345, diff --check clean, applies). Lead validation on the tip: bare juxtaposition refused with the fixit naming both statements; a trailing `;` refused ('joins, does not end'); the half-broken `{ one <newline> two }` layout stays LEGAL by grammar and is disallowed by the skill's style rule R5 as ruled; a running probe covering `;` in an `if` body, a closure tail, a `for` body and two match-arm blocks beside a multi-line `if`, a `try {} catch {}` and a value `if` (the lookahead-restore shapes) prints the expected values. Agent gates (c2): suite 2649/10x, freestanding 36/36, battery 32/32 after one real catch (an embedded Python-harness Saw fixture the sweep could not see, fixed in the R3 commit). Awaiting the server gate + codex.

<!-- sawtracker:comment {"author":"agent:sawtracker-tests","body_bytes":39,"created":"1790140346","id":"c5"} -->
Tests passed for SL-347.p1 (revision 1)

<!-- sawtracker:comment {"author":"human:swoodtke","body_bytes":3353,"created":"1790164202","id":"c6"} -->
Requested changes on patch SL-347.p1 (revision 1)

Request changes on SL-347.p1 revision1 (applied/tested a2548706; exact reviewed tree matched the server tested_tree).

P1 — Preserve the DF-259c trailing-closure regression. examples/trailing_closure_inside_a_try_operand.saw:42,48,54 insert a semicolon between jobs.map and its trailing closure. That is not a statement migration: it permanently splits the intended call. Parser-only inspection confirms each row is now a LetStatement(TryExpr) followed by a separate ExpressionStatement(ClosureExpr). The XFAIL still masks this, and fixing DF-259c could no longer make this pin XPASS. Restore the three original trailing-closure spellings and retain their filed XFAIL.

P1 — Inline module declaration lists bypass the separator funnel. sawc/parser/core.py:1402-1416 calls _parse_toplevel_decl(body) then only skip_newlines(), unlike Parser.parse. Independently reproduced using Lexer+Parser only: `module m { func a() -> Int { 1 } func b() -> Int { 2 } }` is accepted; equivalent file-level juxtaposition is rejected. Inline-module semicolons also get the old generic declaration error. Route the initial and subsequent inline-module gaps through declaration-mode expect_statement_end and pin the gap.

P2 — Four live skill snippets still use forbidden juxtaposition: .claude/skills/saw-lang/SKILL.md:1008 (#lend_var branches),2338 (Thread.spawn),3047 (borrow capture),4252 (capture write). Each now fails at the new separator parser check before reaching the behavior its prose describes. All four independently parse after inserting only the missing semicolons. The two historical dangling-reference snippets at skill:744 and LANGUAGE_SPEC.md:3811 also retain old spelling, but I do NOT claim a changed diagnostic there: the return-clause reference fence fires before their bodies.

P2 — The claimed valid bare-match control is invalid and hidden by recovery. examples/errors/statement_juxtaposition_in_match_guard_and_try_blocks.saw:25 uses `case _ -> tag = 2`; bare arms parse expressions, not assignment statements. Isolated parser-only reproduction rejects it; `case _ -> print("other")` is accepted. Put a real expression control in an independently parsed declaration. Also correct the new spec/skill claim that a bare arm must end at a comma: existing grammar accepts optional commas, including the reproduced same-line `case A -> 1 case B -> 2`. This latter grammar behavior predates this patch; I am not asking SL-347 to introduce a new mandatory-comma policy.

Nonblocking coverage wording: the positive pin claims two-statement else/catch coverage but those bodies contain one statement. Narrow that claim or exercise it; these share parse_block.

Review evidence: two independent Sol slices plus parent lexer/parser-only reproductions; no typechecker, compiler backend, native LLVM lowering, or additional suite was executed. Server gate is reported passed; I did not rerun it. Text audit covered all117 modified existing .saw files:246 semicolons in94 files, with exactly the three corrupt trailing-closure insertions above; comments/EXPECT directives unchanged and literal text preserved. All five new SL350 fixtures parse under this revision and need no migration. External sawtracker a067c53 was not modified; source review found style debt but no concrete separator blocker.


<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":1137,"created":"1790164251","id":"c7"} -->
LEAD ACK of the r1 review (c6, Sep 23): all four accepted as filed, plus the nonblocking one. P1-a is the sweep's blind spot and the sharpest: the parser it used as its oracle is the one DF-259c pins as WRONG about a trailing closure inside a `try` operand, so the oracle reported the call as two statements and the sweep 'fixed' it — restore the three spellings, keep the XFAIL, and sweep every `;` inserted directly before a `{` for the same shape. P1-b is a fourth chokepoint entry the docstring did not name (the inline-module loop; obligation 1) — the agent was already resumed on it from chat m29. P2-a: four skill snippets docverify does not parse, swept by hand. P2-b: the bare-arm 'control' was never valid; a real expression control, and the spec/skill sentence corrected — bare-arm commas are OPTIONAL in the existing grammar (same-line `case A -> 1 case B -> 2` included), SL-347 introduces no comma policy. Nonblocking: the else/catch cells will EXERCISE two statements rather than narrow the claim. agent:claude-sl347b is resumed with all of it; full battery on the final tip; the lead re-validates and revises to r2.

<!-- sawtracker:comment {"author":"agent:claude-sl347b","body_bytes":8326,"created":"1790169587","id":"c8"} -->
## SL-347 — codex r1 addressed, one commit `f7f1abde`. Battery 32/32 on the final tip.

Branch `sl347`, tip **`f7f1abde`** on top of `4e96321e`. Patch NOT revised — the
lead re-validates and revises to r2.

### P1-a — the DF-259c pin, restored, and the sibling sweep

`examples/trailing_closure_inside_a_try_operand.saw` lines 42/48/54 restored
VERBATIM from main (`git checkout origin/main --`); `git diff origin/main` over
that file is now comment-only. The marker stays. What I did change is the XFAIL
**reason**, which claimed a symptom that is no longer produced: the refusal is
now ``two statements on one line need a `;` between them`` where it used to be
``struct `Vector` has no field `map```. Same defect reported at the seam instead
of past it, and the reason now says in as many words that the missing `;` is
deliberate, so nobody re-sweeps it. The XPASS flip still validates the fix: once
a trailing closure is recognized inside a `try` operand, the parser sees ONE
statement, R3 never fires, and the pin prints `1 1 1`.

**The sibling sweep (obligation 4).** Mechanism: *a `;` inserted where today's
parser mis-reads one expression as two statements*. Two populations can hold a
sibling, both swept over all 127 changed `.saw` files:

- **(A) a `;` inserted before a token that could CONTINUE the expression** —
  `{` (the trailing-closure shape), `(`, `[`, `.`, or a binary operator:
  **ZERO remain.** The three corrupt insertions were the only members.
- **(B) a swept site in an XFAIL file** — three, each judged and **KEPT**,
  because none of their filed defects is about the statement boundary:

  | file | XFAIL | the swept site | verdict |
  |---|---|---|---|
  | `closure_nested_ref_param_capture.saw` | DF-248b (a nested closure captures an enclosing closure's reference) | `{ [&var c] in c = c + 41; c }` and `{ bump(&var c); c }` | genuine write-then-tail pairs |
  | `closure_shorthand_parameter_inside_a_try.saw` | DF-257d (the `$0` shorthand is invisible to the implicit-parameter scan) | `{ let v = try! fallible($0); print("bound {v}") }` | genuine `let`-then-`print` |
  | `conformance/V96_driven_capture_duplicates_once.saw` | SL-267 (a driven body's non-escaping capture duplicates) | `{ print("copy {}", self.n); Counted(n: self.n) }` | genuine print-then-tail |

  DF-259c was unique: it is the one XFAIL in the tree whose *subject* is the
  statement boundary itself.

### P1-b — the inline-module entry, and the three more the sweep found

The bypass was not one loop. Every loop that reads a list was asked the same
question, with a compile probe each, and **four** were bypassing the funnel:

| loop | list kind | reaches the funnel? |
|---|---|---|
| `Parser.parse` | DECLARATIONS, newline-separated | yes (already) |
| `Parser.parse_module_decl` — inline `module m { }` body | DECLARATIONS, newline-separated | **ADDED** |
| `DeclarationsMixin.parse_extension` — MEMBERS | newline-separated | **ADDED** |
| `DeclarationsMixin.parse_trait` — REQUIREMENTS | newline-separated | **ADDED** |
| `DeclarationsMixin.parse_extern_block` | DECLARATIONS, newline-separated | **ADDED** |
| `StatementsMixin.parse_block` | STATEMENTS | yes (already) |
| `ExpressionsMixin._parse_closure_body` | STATEMENTS | yes (already) |
| `parse_struct` — fields | COMMA-delimited | exempt (design 129) |
| `parse_enum` — cases | COMMA-delimited | exempt |
| `parse_match_expression` — arms | COMMA-delimited | exempt |
| `_parse_map_literal` / `_parse_set_literal` | COMMA-delimited | exempt |
| `parse_import` — symbol list | COMMA-delimited | exempt (design 147) |

Each of the four now routes the gap BEFORE the first unit and every gap after it
through `expect_statement_end(..., declarations=True)`, so an inline-module `;`
gets the declarations message rather than the old generic error. The docstring
names all seven entries and states the comma-delimited exemption explicitly.

**The corpus needed no further sweep**: with the four entries live, the recorder
finds ZERO sites outside this branch's own matrix pins.

New pins: `examples/errors/statement_juxtaposition_in_declaration_lists.saw`
(rows 14-17, one per new entry, each a different pair of first tokens —
`public`/`public`, `func`/`static`, `type`/`func`, `func`/`func`) and its running
half `examples/declaration_lists_take_a_line_each.saw`, where the same units on
separate lines compile and print.

### P2-a — the four live skill snippets, and a fourth home for Saw source

All four are **inline code spans in prose**, not fenced blocks — which is exactly
why neither docverify nor this branch's fence sweep could see them. That is the
fourth place Saw source lives in this tree, after `.saw` files, fences, and the
Python harnesses (the third, found by the battery last round).

| line | snippet | fix |
|---|---|---|
| `SKILL.md:1014` | `let slot = if #lend_var { let c = 0  c } else { let c = 1  c }` | two `;` |
| `SKILL.md:2344` | `Thread.spawn { yield_now()  7 }` | one `;` |
| `SKILL.md:3053` | `group.spawn(run({ [&var n] in n = n + 1  n }))` | one `;` |
| `SKILL.md:4258` | `{ n = n + 1  n }` | one `;` |

A class sweep of every inline span in README / CLAUDE / spec / skill now reports
four candidates, **all deliberate**:

- the two historical `func dangle() -> &Int { let local = 99  return &local }`
  quotes (`LANGUAGE_SPEC.md:3822`, `SKILL.md:753`) — left, as the reviewer
  asked, and now with evidence: the diagnostic is **byte-identical** between
  this branch and main (the return-clause reference refusal fires before the
  body is parsed), so nothing about them changed;
- this branch's own ``a build that accepts `{ c in c = c + n  c }` predates the
  refusal``, where the unseparated spelling IS the thing being shown;
- an `import kcore.{…}` symbol list, comma-delimited.

### P2-b — the bare-arm control, and the comma claim corrected

`case _ -> tag = 2` was an assignment STATEMENT where an expression is parsed,
hidden by recovery from the row above it. The row keeps a block arm; a real
control (`case 0 -> print("zero"), case _ -> print("other")`) moved into its
**own declaration** so it is parsed independently and actually proves something.

The spec, the skill and the chokepoint docstring all said a bare arm "ends at the
arm's `,`". It does not. **The comma between arms is OPTIONAL in today's
grammar** — `match k { case 0 -> 1 case _ -> 2 }` on one line compiles, probed
identical on this branch and on main. All three now say exactly that: the comma
is optional, it predates SL-347, and SL-347 introduces no mandatory-comma
policy. Why a bare arm is out of scope is stated positively instead: its body is
an EXPRESSION, not a statement list, so it never reaches the chokepoint.

### Nonblocking — the two-statement claim, exercised

The positive pin's `else` and `catch` held one statement each. Both now carry two
`;`-joined statements on a branch the program really ENTERS — a second `if`
written so its `else` is the live branch, and the `catch` that actually fires —
and their second statements are in the expected output (`else second on the
line`, `catch second on the line`). Exercised rather than narrowed.

### Gate

- Per-commit, before the commit: full suite **2651 passed / 10 xfailed**
  (two new pins; the xfail count is UNCHANGED — the DF-259c marker is restored,
  none added, none removed), freestanding **36 passed across riscv32 + arm64**.
- Full battery re-run on the final tip, since the sweep changed again:
  **32 of 32 stages GREEN in 4328s.** The lanes that see this round:
  `docverify` 515 blocks OK, `lexdiff` green, `astdiff` 3437 files byte-stable,
  `fuzz` 150 mutants / 0 known hits / 0 new findings, `minivm` 446 cases,
  `reemit` identical 1639 / DIVERGENT 0, `irdet --all` byte-identical,
  `bootstrap` and `gmgate` green.

### SHAs (branch `sl347`, on `origin/main` 1938880b)

- `de3b0388`, `5c02d37e` — patch 1, rebased
- `876f5f7d` — the keyword-peek restore
- `ec796798` — the corpus sweep
- `4e96321e` — bare juxtaposition refused
- **`f7f1abde`** — codex r1: the declaration lists join the funnel, the DF-259c
  pin restored, the four inline snippets, the control fix and the comma
  correction

No XFAIL added or removed. No finding filed. PARKED for the lead's
re-validation and the r2 revision.


<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":37,"created":"1790169672","id":"c9"} -->
Revised patch SL-347.p1 to revision 2

<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":2392,"created":"1790169673","id":"c10"} -->
LEAD RE-VALIDATION of r2 (Sep 23; tip f7f1abde, 6 commits on main, squashed with `git diff main...sl347`; `diff --check` clean, applies on main). `patch revise SL-347.p1` = r2.

Re-run on the tip by the lead: the review's inline-module program (`module m { func a() -> Int { 1 } func b() -> Int { 2 } }`) is REFUSED with the declarations message ("two declarations on one line: the first begins with `func`, the second with `func` — declarations are not joined by `;` — put each declaration on its own line"); the same two on separate lines run (prints 3); two extension members on one line are refused with the same message; the same-line comma-less bare-arm match `case 0 -> 1 case _ -> 2` RUNS (pre-existing grammar, as the review says — the spec/skill sentence is corrected, no comma policy introduced); the earlier probes hold (`let a = 1 print(a)` refused with the fixit, a trailing `;` refused, the running probe covering every `;` position prints its values). `examples/trailing_closure_inside_a_try_operand.saw`'s diff against main is ONLY its XFAIL reason (the three trailing-closure spellings are back verbatim; the reason now says the refusal moved to the statement seam and that the missing `;` is deliberate).

What r2 adds (agent addendum c7): P1-a restored + the sibling sweep on the mechanism (a `;` before any token that could continue an expression — `{`, `(`, `[`, `.`, a binary operator: zero remain; the three swept sites inside other XFAIL files are genuine statement pairs, kept); P1-b was FOUR bypasses, not one — extension members, trait requirements and `extern "C" { }` blocks read newline-separated declaration lists through the same `skip_newlines()` as the inline module — all four route through `expect_statement_end(declarations=True)`, the docstring names seven entries and the comma-delimited exemption, pins `statement_juxtaposition_in_declaration_lists.saw` + `declaration_lists_take_a_line_each.saw`; P2-a the four snippets were INLINE CODE SPANS in prose (a fourth home for Saw source no fence tool sees), fixed, and a span sweep found only deliberate members; P2-b a real expression control, the comma sentence corrected in spec, skill and docstring; the `else`/`catch` cells exercise two statements. Gates on the tip: suite 2651 / 10 xfail / 0 fail, freestanding 36/36, full battery 32/32 (4328s). Awaiting the server gate + review on r2.


<!-- sawtracker:comment {"author":"agent:sawtracker-tests","body_bytes":39,"created":"1790170268","id":"c11"} -->
Tests passed for SL-347.p1 (revision 2)

<!-- sawtracker:comment {"author":"agent:codex","body_bytes":1945,"created":"1790170604","id":"c12"} -->
Approved patch SL-347.p1 (revision 2)

Approve SL-347.p1 revision 2: applied 03a47200, base 8e1ee181, tree 3d6333d96e65846b1520aa6564229b79b01494a7. The latest server record reports tests passed for that exact commit and tree; this is not the stale r1 test output.

Both Sol review slices are clear of blocking findings. The inline-module boundary gap is fixed, and the same chokepoint now covers trait, extension and extern declaration lists. Parser-only probes verified refusal of juxtaposition and declaration semicolons, valid newline-separated declarations, trait default bodies on the next line, associated-type/default-return structure, and all four intended negative-fixture diagnostics. The extra closing-brace recovery diagnostics are pre-existing synchronization behavior, not a new blocker.

The three DF259c trailing-closure spellings are restored, preserving the intended future XPASS. All four live skill snippets have separators. The bare match-arm control is now a valid standalone case, the optional-comma behavior is documented without inventing a new grammar rule, and the positive fixture exercises else/catch bodies. Parent independently parsed all five final SL350 fixtures with this exact r2 parser; all accepted.

Nonblocking wording nits: LANGUAGE_SPEC.md:111 and SKILL.md:416 say four declaration lists but enumerate five; the expect_statement_end docstring repeats the same count. The match/guard/try negative fixture's header at lines 4-6 still says a bare arm ends at its comma, while its corrected control and spec acknowledge optional commas. Correct the count and qualify the header when convenient; neither changes the source-review verdict.

Local review used lexer/parser-only Python, with no typechecker, codegen, native LLVM execution, builds, formatter, linter or project-wide suite. The running SL350 battery was not restarted or interrupted for this review. User review and merge remain the landing gate.

