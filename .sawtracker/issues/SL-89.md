---
{"assignee":"","author":"agent:codex-todo-import","body_bytes":3290,"created":"1788791233","id":"SL-89","labels":["todo-import","needs-verification","verification","plan"],"priority":"normal","project":"SL","revision":1,"sequence":89,"status":"open","title":"DF-218u: Verify destructuring redefinition cleanup on non-frame-resident paths","updated":"1788791233"}
---


## Description

## Scope and status

DF-218u: Verify destructuring redefinition cleanup on non-frame-resident paths

The single-binding bug is explicitly closed. Only destructuring-let leaf redefinition on the non-frame-resident path is an unprobed residue; reproduce that shape before implementation.

Imported from repository TODO records on 2026-09-07. The reporter/version, evidence, workarounds and prior rulings are preserved in the source context below. This import is not a fresh reproduction or approval of a proposed design.

Scheduling: backlog; no new implementation order is assigned by this import.

## Proposed plan

1. Re-run the precise historical probe at current HEAD, recording compiler revision, target and output; distinguish the original symptom from any surviving residue.
2. Compare with later design landings and existing pins. If resolved, close with the relevant commit/test; if still present, preserve a minimal reproducer and update the scope.
3. Do not schedule a speculative implementation until the verification identifies a remaining defect or missing contract.

## Acceptance criteria

- [ ] The task has a recorded current-status verdict supported by a probe, code reference or landing record.
- [ ] Any surviving work has a bounded scope and regression oracle; otherwise the task is closed with its resolution.

## Existing design references

- [sawlang/designs/218-enforcement-architecture.md](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/218-enforcement-architecture.md)

## Source context

Historical closed subcases are context, not new work. Legacy DF/SL/SO numbers use a nonbreaking hyphen here to avoid accidental tracker links; the linked source retains the original spelling.

### sawlang TODO lines 4036–4055

[Original record](https://github.com/swoodtke/sawlang/blob/fd0b5aa8847e3edce9bdcd7880c9ed68923ca4c5/designs/todo.md#L4036-L4055)

- **DF‑218u — CLOSED on discovery (Aug 21, design 218 unit 2 stage A/B).** A
  design-107 same-scope redefinition in a body the coro transform TOUCHES but
  does not make frame-resident lost its drop-at-redefinition point: the
  replaced value survived to the scope's end. MECHANISM:
  `_uniquify_bindings` (DF‑151a) renames the second binding, and codegen's
  `_drop_redefined_same_scope` matches by NAME, so the pair read as two
  unrelated locals. Positions: a SPAWN root with no suspension (the plainest
  face — its locals stay codegen's), and a driven body whose locals are not
  frame-resident; a frame-resident pair takes the transform's own E-REDEF edge
  and was never affected, and an un-transformed body never renames. FOUND by
  the stage-A/B corodiff `--all` run, which reported it as a new
  DEINIT-ORDER cell (`let_shadow_rebind/before @ generic`) once the DRIVEN
  twin started dropping at the redefinition — the DF‑217n unmasking pattern.
  FIX: the transform hands the pairing over on a declared
  `LetStatement.coro_redefines` annotation. RESIDUE: a DESTRUCTURING `let`'s
  leaf redefinition on the same non-frame-resident path is not covered (it has
  no single-name hand-off); exotic, not probed further.
  PIN: `examples/spawn_body_same_scope_redefinition.saw` (three cells:
  un-transformed, spawned non-suspending, spawned suspending)


## Comments

