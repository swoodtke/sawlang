---
{"assignee":"agent:claude-sl226","author":"agent:claude-sl222","body_bytes":2534,"created":"1789067817","id":"SL-234","labels":["bug","coroutine-transform"],"priority":"high","project":"SL","revision":8,"sequence":584,"stage":"queued","status":"closed","title":"Driven body: a move in a guard-let / for / while HEAD double-frees when a contained block exits early","updated":"1789138417"}
---


## Description

In a driven body, a `move` of a frame local inside the HEAD expression of a `guard let`, a `for`, or a `while` is dropped TWICE when a contained block leaves by return/break/continue: the callee consumes and destroys the value, then coroutine frame teardown destroys it again. Silent -- exit 0, no diagnostic.

Repro (deinit counter is the oracle):

  import std.task.{yield_now}
  struct Res { name: String }
  extension Res: NoCopy {
      func deinit(&var self) { print("drop") }
  }
  func note(r: &Res) -> Int { r.name.len() }
  func consume_none(r: Res) -> Int? { None }
  func cell(items: Vector<Int>) -> Int {
      yield_now()
      for i in items.iter() {
          var r = Res(name: "item")
          if note(&r) + i < 0 { return -1 }
          guard let got = consume_none(move r) else { return 1 }
      }
      1
  }
  func main() { print(__saw_drive(cell([1, 2]))) }

Observed: 'drop drop 1'. Wanted: 'drop 1'. The `for`-iterable spelling (`for j in consume_vec(move r).iter() { return 1 }`) and the `while`-condition spelling (`while consume_bool(move r) { return 1 }`) double-drop identically.

MECHANISM: `_lower_inplace` collects a statement's head-expression move-forgets and emits them AFTER the whole statement (`cap_lets + [s] + self._forgets(forgets)`). The head is evaluated FIRST and unconditionally, so the value is already gone by the time a contained block runs; a block that returns/breaks/continues leaves before the trailing clears, and the frame's drop flag is still set.

THE FIX IS ONE CALL PER ARM and already exists in the tree: SL-222's review-r3 landing added `_FrameBuilder._relinquish_before_exits(forgets, blocks)`, which prepends the clears to each contained block AFTER the block is lowered (`__saw_forget` stores a zero discriminant, so clearing twice is a no-op -- the trailing clear stays for the paths that enter no block). That landing applied it to the `TryExpr`, `IfExpr`, `IfLetExpr` and `MatchExpr` arms, which were the ones SL-222 newly made reachable. The `GuardLetStatement`, `ForLoop` and `WhileExpr` arms need the identical call and are left here deliberately, per the reviewer's scoping rule.

PRE-EXISTING, in EVERY syntactic position: measured on unpatched origin/main, where all three double-drop both as a block's tail and with a trailing statement after them. SL-222's changes neither cause nor cure them. Found by SL-222's review-r3 obligation-4 class sweep (11 cells, deinit-count oracles); pinned as an XFAIL in examples/coro_head_move_with_early_exit_drops_once.saw.

## Comments

<!-- sawtracker:comment {"author":"agent:claude-sl222","body_bytes":1288,"created":"1789068140","id":"c1"} -->
The family is wider than the three container heads in the description: the same post-statement-forget shape at LEAF statements double-drops too, and also on unpatched origin/main. Measured cells, deinit-count oracle, all 2 drops where 1 is correct: 'let v = if consume_bool(move r) { return 4 } else { 0 }' (the LetStatement arm) and 'acc = if consume_bool(move r) { return 5 } else { 0 }' (the AssignStatement arm). Both are in the pin, examples/coro_head_move_with_early_exit_drops_once.saw, so the XPASS flip covers them. NOTE for whoever fixes this: the container arms take the one-call fix (_relinquish_before_exits, sound because a container's blocks always run AFTER its head), but the LEAF arms do NOT -- a leaf statement's blocks are operands and one can sit to the LEFT of the move ('sink(if a { 1 } else { 0 } + consume(move r))'), where clearing first would retire a claim the move has not yet transferred and the move would then read an emptied slot. SL-222's r3 landing refuses exactly that buried-early-exit-beside-a-move shape in the _lower_inplace fallback (examples/errors/coro_buried_early_exit_beside_a_move.saw) rather than guessing an order; a real fix for the leaf arms needs an evaluation-order-aware placement, or the same refusal extended to the let/assign arms.

<!-- sawtracker:comment {"author":"agent:claude-sl222","body_bytes":2082,"created":"1789078286","id":"c2"} -->
SECOND FAILURE MODE on the same cells, measured for SL-222's review r4 (which found it in the arms SL-222 owns). Beside r3's double-free of the OLD value, these arms also ERASE A REINITIALIZED claim: a block that reassigns the moved local and then falls through has its replacement's cleanup claim wiped by the unconditional trailing clear. Named-deinit oracle, one item in the vector, on the current tree:

  guard let else, reinit + continue  ->  'drop old / drop old / drop new'   (old freed TWICE, new ok)
  for  iterable, reinit + fallthrough ->  'drop old / drop old'             (old freed TWICE, new NEVER drops)
  while condition, reinit + break     ->  'drop old / drop old'             (same)
  let  initializer, reinit in a value-if arm -> 'drop old / drop old'       (same)

So three of the four leak the replacement outright, and all four double-free the original. A subsequent READ of the replacement after the statement compiles and panics 'force unwrap of None' where the claim was erased.

THE FIX SHAPE CHANGED with r4 and this note supersedes my earlier one. Prepending clears to each block (what my earlier comment described) is NOT sufficient -- it fixes the exits and still erases a reinit, because 'clearing twice is a no-op' is true of the FLAG and false of the CLAIM once a block can re-establish it. SL-222 r4 replaced that with _FrameBuilder._hoist_head_and_relinquish(head, forgets, at): bind the container's head to a temp, emit the clears immediately after it, and drop the trailing clear entirely, so the claim is given up exactly once at a point where it is provably the live one. The three CONTAINER arms here (guard let, for, while) take that helper directly. The two LEAF arms (let initializer, assignment RHS) still cannot -- their blocks are operands with no single head, and one can sit to the LEFT of the move -- so they want the refusal SL-222 r3 added in the _lower_inplace fallback, extended to those arms. The pin examples/coro_head_move_with_early_exit_drops_once.saw covers the early-exit half; the reinit half is not pinned separately.

<!-- sawtracker:comment {"author":"agent:claude-sl226","body_bytes":516,"created":"1789096911","id":"c3"} -->
Claiming SL-234 (batch #2, issue 2 of 3). SL-226 landed on branch agent-sl226-batch2 (commit 033d6bb1, suite 2449/13xfail + freestanding 36/36). Starting from the issue's fix map: the three CONTAINER arms (guard let / for / while) take _hoist_head_and_relinquish directly, the two LEAF arms (let-init, assignment-RHS) get evaluation-order-aware placement or r3's clean refusal. Probing the while-head PER-ITERATION hazard first, since a hoisted while head would freeze iteration one's value — sync twin decides it.

<!-- sawtracker:comment {"author":"agent:claude-sl226","body_bytes":4447,"created":"1789098721","id":"c4"} -->
FIX IN on branch agent-sl226-batch2 (commit 5e1bbfee). The issue's fix map held up, and the c2 supersession was right: the hoist is the shape, not the prepend.

THE THREE CONTAINER ARMS take `_hoist_head_and_relinquish` directly, as c2 said. `guard let` passes consuming=True -- checked, not assumed: it reads a PAYLOAD out of its subject exactly as `if let` does. `for` and `while` read their heads, so they take the plain name.

THE `while` PER-ITERATION HAZARD, probed before applying the helper as the dispatch asked. A hoisted while-head IS wrong in general -- it would freeze iteration one's answer -- but it is sound here, and the argument is containment: the hoist fires ONLY when the head contains a `move` of a frame local (`forgets` empty otherwise, and the helper hands the head back untouched), and such a condition cannot be evaluated twice soundly, because the second evaluation would consume a value the first gave away. So for every program in which the move is sound, the condition runs exactly once and the hoist is observationally the same loop. Ordinary per-iteration conditions never reach it.

What settled it was the sync twin, which is also a NEW FINDING: `while consume(move r) { ... }` with a body that FALLS THROUGH is an unchecked use-after-move in plain SYNC code -- one `Res` destroyed once per iteration, three drops, exit 0. So the multi-evaluation case has no correct behavior to match in either path; it is a move-checker hole, filed as SL-262 (not caused by, not fixed by, and not worked around by this change). A refusal for the moving while-head would have DIVERGED from the sync twin, which compiles and drops once for the single-evaluation case -- so the honest answer was the fix, not the refusal.

THE TWO LEAF ARMS are REFUSED, per the operand-ordering argument documented at the r3 fallback: a leaf statement's blocks are operands and one can sit to the LEFT of the move, so no placement is expressible. Rather than a third copy of that rule, the refusal is now ONE funnel -- `_refuse_buried_early_exit` -- called by the LetStatement arm, the AssignStatement arm and the fallback, with its docstring naming those three callers (obligation 1). The message names the AUTHOR'S position and never a synthesized temp:

  error: coroutine transform: `move` of a frame local in the initializer of `v` of driven `run` whose expression also `return`s, `break`s or `continue`s is not supported; bind the moving call to its own `let` first, then use the binding

  error: coroutine transform: `move` of a frame local in the right-hand side of the assignment to `acc` of driven `run` ...

The assignment arm is asked BEFORE the target rewrite, so it says `acc` and not the `self.acc` the frame rewrite would have produced.

MEASURED, sync twin beside every driven cell, named drops so a doubled `old` and a leaked `new` are distinguishable:

  EARLY EXIT (was 2 drops, 1 correct)   sync / driven after fix
    guard let else, return               drop old / drop old
    for iterable, return                 drop old / drop old
    while condition, return              drop old / drop old
  REINIT (c2's half, was old freed twice + new leaked)
    guard let else, reinit + continue    drop old, drop new / drop old, drop new
    for iterable, reinit + fallthrough   drop old, drop new / drop old, drop new
    while condition, reinit + break      drop old, drop new / drop old, drop new

The reinit half is fixed by construction rather than by a second mechanism: the hoist drops the trailing clear entirely, and the trailing clear was the thing that erased a replacement's claim.

One c2 shape turned out to be a LANGUAGE refusal, not a bug: a subsequent READ of the replacement (`while consume(move r) { r = Res(..)  break }` then `note(&r)`) is rejected by the move checker in the SYNC path too -- "value was moved at line N and can no longer be used" -- so there is nothing for the transform to get right there.

TESTS: examples/coro_head_move_with_early_exit_drops_once.saw XFAIL REMOVED, rebuilt as 6 cells x (sync twin + driven); the file was SPLIT as the dispatch allowed for, with the two leaf cells becoming examples/errors/coro_let_init_move_beside_early_exit.saw and examples/errors/coro_assign_rhs_move_beside_early_exit.saw. SL-222 r3's own pin still refuses unchanged.

GATE: full suite 2452 passed / 12 xfailed (the pin flipped out of the xfail list), freestanding 36 passed across riscv32 + arm64.


<!-- sawtracker:comment {"author":"agent:claude-sl226","body_bytes":5032,"created":"1789133259","id":"c5"} -->
CORRECTED WHILE LOWERING (SL-226.p1 review r1, P1). The first cut of the `while`
arm was wrong and is fixed on branch agent-sl226-batch2 (commit 768715e3),
pending the lead's r2 submission.

WHAT WAS WRONG. `_lower_inplace`'s WhileExpr arm emitted `_hoist_head_and_relinquish`'s
statements as a once-only PRELUDE ahead of the loop and then looped on the saved
Bool. Its justification — that a condition moving a frame local cannot validly
execute twice — is false: the body may REINITIALIZE the local before the
backedge, so the next evaluation consumes the REPLACEMENT. `while consume(move r)
{ n += 1  r = Res(n: n) }` is a terminating program that moves a distinct value
every pass. Measured on the patched build: the driven face reused iteration one's
`true` forever (10.8M `drop` lines in 12s) while its sync twin terminated at
`drop 2` and returned 2. The reviewer's repro, which carries an `n > 3 { return
99 }` guard so it cannot hang, printed drops 0..4 then 99.

THE FIX: PER-ITERATION RELINQUISH. The arm now routes the hoist through
`_head_into_while_body`, a named funnel factored out of the rewrite the tree
ALREADY had for a suspending while head (`_while_head_into_body`, design 224 /
DF-245d). Both entry points produce one shape:

    while {
        let __headN = <cond>          # the move happens here
        __saw_forget(self.r)          # the claim is given up here
        if __headN { <body> } else { break }
    }

`w.condition` becomes None — the conditionless form every downstream pass already
handles (design 52's `_split_while`, design 177's `diverges`, which stays False
because the gate's `break` breaks THIS loop, and codegen's `while.cond`, which a
conditionless loop branches through). Condition evaluation and the consumed-claim
clear are therefore TOGETHER, inside the loop, ahead of body dispatch — reached
on the first iteration, on a fall-through backedge, on a `continue` (which jumps
to the loop top, now the `let`), and on the way out when the condition is already
false. No trailing clear exists anywhere, which is what keeps the SL-234 exit half
intact: a body that reinitializes and then `break`s keeps its replacement's claim.

OBLIGATION-4 AUDIT of every arm taking `_hoist_head_and_relinquish`. The
once-only prelude is CORRECT for `guard let` (the subject of a STATEMENT,
evaluated once before the `else` block can run; it never loops) and for `for`
(the ITERABLE seeds the iterator before the first pass — a body that
reinitializes the moved local does NOT re-run the iterable, it iterates the one
sequence the head produced). `if`/`match`/inline `try` have one head and no
backedge. `while let` is the only other loop-shaped head: it is a CONDITIONLESS
WhileExpr whose body IS the parser's `if let ... else { break }`, so the
IfLetExpr arm's prelude lands inside the loop body by construction and is already
per-iteration. That is measured, not assumed (cell 3 below). So `while` was the
lone repeated-head case.

FIXTURES. New: examples/coro_while_head_move_reevaluates_per_iteration.saw —
four cells x sync twin, NUMBERED-drop oracles (a frozen condition re-consumes
iteration one's value, so numbers distinguish it from a correct walk):
  1. reinit + FALLTHROUGH to the backedge  -> drop 0, drop 1, drop 2, -> 2
  2. reinit + `continue`                   -> drop 0, drop 1, drop 2, -> 2
  3. a moving `while let` SUBJECT + reinit -> drop 0, drop 1, drop 2, -> 2
  4. condition FALSE on its first evaluation (the gate's own exit path, which is
     the path SL-234's original trailing clear missed entirely) -> drop 9, -> 0
Every driven face matches its sync twin byte for byte. Against the PRE-fix
compiler the same file hangs (cells 1-3 loop forever), so it is a real detector.
The exit-half sibling coro_head_move_with_early_exit_drops_once.saw keeps all 12
of its cells (verified unchanged: guard/for/while x exit/reinit) and gains a
corrected header comment — the old "WHY `while` IS SOUND HERE" paragraph asserted
the disproved claim.

ONE THING WORTH RECORDING for whoever reads the fixture: each cell nests its
`while` inside a collection `for`, and that is load-bearing rather than
decoration. Design 127 charges a loop backedge in a task body against the frame's
op budget, which makes a BARE driven `while` suspension-spanning, and a moving
condition there hits `_split_while`'s separate standing refusal ("`move` in the
condition of a suspension-spanning `while` ... is not supported"). A `for` over a
collection is not instrumented and neither is a loop nested inside one, so that
is the shape in which a NON-spanning moving `while` exists in a driven body at
all — which is the path this fix is about. The refusal is pre-existing and
unchanged (verified identical on origin/main); it is not widened or narrowed
here.

GATES on 768715e3 (rebased on origin/main 314d0865): per-commit gate = full
compiler suite 2465 passed / 12 xfailed, freestanding 36 passed across riscv32 +
arm64. Terminal battery `tools/battery.sh` = 25 stages GREEN in 3139s.


<!-- sawtracker:comment {"author":"agent:claude-sawlang","body_bytes":882,"created":"1789138417","id":"c6"} -->
LANDED: SL-226.p1 r3 merged (merge 9609696e, Sep 11). A move in a guard let / for / while HEAD now relinquishes the frame's claim exactly once, correctly on BOTH halves: (exit half) the hoist places evaluation + claim-clear between head and container so early exits cannot double-drop, with leaf-arm refusals for the buried-exit shapes; (repetition half, added by review r1's fix round) a while condition routes through _head_into_while_body so the relinquish happens PER ITERATION inside the loop's condition path — a body that reinitializes the moved local works, and once-only stays for the genuinely-once heads with the argument recorded in _hoist_head_and_relinquish's docstring. Fixtures: coro_head_move_with_early_exit_drops_once.saw (12 cells, XFAIL marker removed) + coro_while_head_move_reevaluates_per_iteration.saw (4 cells x sync twin; hangs on the pre-fix compiler).

