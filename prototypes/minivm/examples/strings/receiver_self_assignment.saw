func replace(value: &var String) {
    value = "changed"
}

func main() {
    var text = "abcd"
    text = text.substring(1, text.len())
    print(text)
    text = text.substring(0, text.len())
    print(text)
    replace(&var text)
    print(text)
}
