struct Counter { value: Int }
extension Counter {
    func combine(&var self, other: &Counter) { self.value += other.value }
}
func main() {
    var counter = Counter(value: 1)
    counter.combine(other: &counter)
}
