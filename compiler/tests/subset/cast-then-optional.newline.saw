// `??` after a cast across a newline inside parentheses; operators after one are fine.
func product(a: Int, b: Int) -> Int {
    (a
     * b)
}

func difference(a: Int, b: Int) -> Int {
    (a
     - b)
}

func main() {
    let n: Int8 = 3
    let a = (n as Int
             ?? 9)   // refuses: cast-then-optional
    print(a)
    print(product(2, 3))
    print(difference(5, 3))
}
