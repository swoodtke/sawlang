// A declared type named like a prelude type.
struct Duration { ticks: Int }   // refuses: prelude-type-name
enum Ordering { case Low, case High }   // refuses: prelude-type-name
trait Printable { func show(&self) -> Int }   // refuses: prelude-type-name
struct Tally { kind: Int }
