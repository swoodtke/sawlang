// Suffixes, pass-through, and nesting in expressions and patterns.
func main() -> Int {
    let v = Vector<Int>()
    let r = xs[2..]
    if let x = a?.b {
    } else if y > 0 {
    }
    let n = match try? g() {
        case Some((1, _)) -> 0,
        case -2..=3 if n < 4 -> 1
        case None -> return 2
    }
    let c = { [&self, move q] k: Int in k }
    -a as Int8
}
