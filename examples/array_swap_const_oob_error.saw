// Design 72 M1: a constant `swap` index out of range is a compile error
// (mirrors `a[const]`); dynamic indices get a runtime bounds check instead.
// EXPECT: error
// EXPECT-ERROR-CONTAINS: out of range
func main() {
    var a = [1, 2, 3]
    a.swap(0, 5)
    print(a[0])
}
