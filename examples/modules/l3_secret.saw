// Library fixture for l3_private_struct_bare.saw (design 40 item 1).
// `Secret` is private; only `make` is public.
struct Secret {
    code: Int
}

public func make() -> Int {
    let s = Secret(code: 7)
    s.code
}
