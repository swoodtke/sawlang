// DF-140c fixture: a package exporting a type reached by its qualified name.
public struct Section { public name: String }

extension Section {
    public init(named: String) -> Section { Section(name: named) }

    public func value(&self, key: String) -> String? {
        if key == "n" { return self.name }
        None
    }
}
