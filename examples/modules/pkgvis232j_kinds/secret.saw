// DF-232j sweep fixture (kind-generality): package-tier symbols of three
// KINDS. The widening mechanism sits in the qualified-reach decision, which
// serves every symbol table — so a static and a struct leak exactly as a
// function does.

public(package) func pkg_secret() -> Int {
    7
}

public(package) static PKG_LIMIT: Int = 9

public(package) struct PkgBox {
    public n: Int
}
