//! Design 229 fixture: the GLOB facade — every name the dependency publishes,
//! handed on bare. No form of `public import` is refused.
// EXPECT: skip

public import modules.export229_leaf.*

public func glob_make(n: Int) -> Widget {
    leaf_make(n: n)
}
