// A module that `embed_typenames` imports and the ENTRY module does not, so a
// qualified path in an embedded body names something only the provider can see.
public func deep_value() -> Int { 5 }

public struct Deep { public n: Int }

public extension Deep {
    public func twice(&self) -> Int { self.n * 2 }
}
