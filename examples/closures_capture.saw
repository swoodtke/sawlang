// Closure capture test: capturing variables from outer scope
// EXPECT: success
// EXPECT-OUTPUT:
// 15

func make_adder(n: Int) -> (Int) -> Int {
    { x: Int in x + n }
}

func main() {
    let add5 = make_adder(5)
    let result = add5(10)
    print(result)
}
