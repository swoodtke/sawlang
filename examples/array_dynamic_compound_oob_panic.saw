// Design 63 T1b: the compound-assignment path (`arr[i] += v`) is bounds
// checked too.
// EXPECT: panic
// EXPECT-PANIC-CONTAINS: index out of range

func main() {
    var arr = [10, 20, 30]
    var i = 4
    arr[i] += 1     // OOB compound write -> panic
    print(arr[0])
}
