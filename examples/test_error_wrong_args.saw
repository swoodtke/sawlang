// Test error: wrong argument count
enum Result {
    case Success(value: Int),
    case Error(code: Int)
}

func main() {
    // Missing required argument
    let r1 = Result.Success()

    // Extra argument
    let r2 = Result.Error(code: 404, extra: 123)
}
