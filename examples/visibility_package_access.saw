// Test that public(package) symbols ARE accessible from other modules in same package
// EXPECT: success
// EXPECT-OUTPUT: 142
import modules.utils

func main() {
    // public(package) function should be accessible within the same package
    let result = utils.package_only(42)
    print(result)  // 42 + 100 = 142
}
