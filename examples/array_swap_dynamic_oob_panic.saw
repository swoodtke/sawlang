// Design 72 M1: a dynamic `swap` index out of range panics at runtime (the same
// always-on bounds check as `a[i]`).
// EXPECT: panic
// EXPECT-PANIC-CONTAINS: array: index out of range: 9 (len 3)
func main() {
    var a = [1, 2, 3]
    var k = 9
    a.swap(0, k)
    print(a[0])
}
