//! Fixture for the graph-shapes grid's 3-cycle row (design 235). `a` -> `b`
//! -> `c` -> `a`.
// EXPECT: skip

import modules.graphcyc3.b.{beta}

public func alpha() -> Int { 1 }
func use_beta() -> Int { beta() }
